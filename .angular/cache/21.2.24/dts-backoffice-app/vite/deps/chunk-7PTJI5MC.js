import {
  ControlContainer,
  DefaultValueAccessor,
  FormsModule,
  NG_VALIDATORS,
  NG_VALUE_ACCESSOR,
  NgControl,
  NgControlStatus,
  NgControlStatusGroup,
  NgForm,
  NgModel,
  NgSelectOption,
  Validators,
  ɵNgNoValidate,
  ɵNgSelectMultipleOption
} from "./chunk-WGYXIEU7.js";
import {
  NavigationCancel,
  NavigationEnd,
  Router,
  RouterLink,
  RouterModule
} from "./chunk-DHDE4T3O.js";
import {
  DomSanitizer,
  Title
} from "./chunk-FFTDG3BD.js";
import {
  HTTP_INTERCEPTORS,
  HttpClient,
  HttpEventType,
  HttpHeaders,
  HttpRequest,
  HttpResponse
} from "./chunk-T6U4UQDS.js";
import {
  CommonModule,
  CurrencyPipe,
  DatePipe,
  DecimalPipe,
  JsonPipe,
  Location,
  LowerCasePipe,
  NgClass,
  NgForOf,
  NgIf,
  NgOptimizedImage,
  NgPlural,
  NgPluralCase,
  NgStyle,
  NgSwitch,
  NgSwitchCase,
  NgTemplateOutlet,
  TitleCasePipe,
  isPlatformBrowser
} from "./chunk-TRXRVUSJ.js";
import {
  ANIMATION_MODULE_TYPE,
  APP_ID,
  ApplicationRef,
  ChangeDetectionStrategy,
  ChangeDetectorRef,
  Component,
  ComponentFactoryResolver$1,
  ContentChild,
  ContentChildren,
  Directive,
  ElementRef,
  HostBinding,
  HostListener,
  Inject,
  Injectable,
  Input,
  IterableDiffers,
  NgModule,
  NgModuleRef$1,
  Optional,
  Output,
  PLATFORM_ID,
  Pipe,
  QueryList,
  Renderer2,
  RendererFactory2,
  TemplateRef,
  Type,
  ViewChild,
  ViewChildren,
  ViewContainerRef,
  ViewEncapsulation,
  afterNextRender,
  booleanAttribute,
  createComponent,
  input,
  isSignal,
  model,
  numberAttribute,
  output,
  provideAppInitializer,
  setClassMetadata,
  ɵɵHostDirectivesFeature,
  ɵɵInheritDefinitionFeature,
  ɵɵNgOnChangesFeature,
  ɵɵProvidersFeature,
  ɵɵadvance,
  ɵɵattribute,
  ɵɵclassMap,
  ɵɵclassProp,
  ɵɵcomponentInstance,
  ɵɵconditional,
  ɵɵconditionalCreate,
  ɵɵcontentQuery,
  ɵɵdeclareLet,
  ɵɵdefineComponent,
  ɵɵdefineDirective,
  ɵɵdefineNgModule,
  ɵɵdefinePipe,
  ɵɵdirectiveInject,
  ɵɵdomElement,
  ɵɵdomElementEnd,
  ɵɵdomElementStart,
  ɵɵdomProperty,
  ɵɵelement,
  ɵɵelementContainer,
  ɵɵelementContainerEnd,
  ɵɵelementContainerStart,
  ɵɵelementEnd,
  ɵɵelementStart,
  ɵɵgetCurrentView,
  ɵɵgetInheritedFactory,
  ɵɵinterpolate,
  ɵɵinterpolate1,
  ɵɵinterpolate2,
  ɵɵlistener,
  ɵɵloadQuery,
  ɵɵnextContext,
  ɵɵpipe,
  ɵɵpipeBind1,
  ɵɵpipeBind2,
  ɵɵpipeBind4,
  ɵɵprojection,
  ɵɵprojectionDef,
  ɵɵproperty,
  ɵɵpureFunction0,
  ɵɵpureFunction1,
  ɵɵpureFunction2,
  ɵɵpureFunction3,
  ɵɵpureFunction4,
  ɵɵpureFunction5,
  ɵɵqueryRefresh,
  ɵɵreadContextLet,
  ɵɵreference,
  ɵɵrepeater,
  ɵɵrepeaterCreate,
  ɵɵrepeaterTrackByIdentity,
  ɵɵrepeaterTrackByIndex,
  ɵɵresolveDocument,
  ɵɵresolveWindow,
  ɵɵsanitizeHtml,
  ɵɵsanitizeUrl,
  ɵɵstoreLet,
  ɵɵstyleProp,
  ɵɵtemplate,
  ɵɵtemplateRefExtractor,
  ɵɵtext,
  ɵɵtextInterpolate,
  ɵɵtextInterpolate1,
  ɵɵtextInterpolate2,
  ɵɵtwoWayBindingSet,
  ɵɵtwoWayListener,
  ɵɵtwoWayProperty,
  ɵɵviewQuery
} from "./chunk-QWYXQM2E.js";
import {
  DOCUMENT,
  DestroyRef,
  EnvironmentInjector,
  EventEmitter,
  InjectionToken,
  Injector,
  NgZone,
  RuntimeError,
  SecurityContext,
  computed,
  effect,
  forwardRef,
  inject,
  signal,
  untracked,
  ɵɵdefineInjectable,
  ɵɵdefineInjector,
  ɵɵinject,
  ɵɵnamespaceSVG,
  ɵɵresetView,
  ɵɵrestoreView
} from "./chunk-N66T6AKW.js";
import {
  animationFrameScheduler,
  asapScheduler,
  defer,
  fromEvent,
  isObservable,
  merge
} from "./chunk-HWYXSU2G.js";
import {
  BehaviorSubject,
  ConnectableObservable,
  Observable,
  ReplaySubject,
  Subject,
  Subscription,
  auditTime,
  catchError,
  debounceTime,
  delay,
  distinctUntilChanged,
  filter,
  finalize,
  interval,
  map,
  mergeMap,
  of,
  pairwise,
  shareReplay,
  startWith,
  switchMap,
  take,
  takeUntil,
  takeWhile,
  tap,
  throwError,
  timeout,
  timer
} from "./chunk-MARUHEWW.js";
import {
  __async,
  __export,
  __objRest,
  __spreadProps,
  __spreadValues
} from "./chunk-XWLXMCJQ.js";

// node_modules/@angular/animations/fesm2022/_private_export-chunk.mjs
var AnimationMetadataType;
(function(AnimationMetadataType2) {
  AnimationMetadataType2[AnimationMetadataType2["State"] = 0] = "State";
  AnimationMetadataType2[AnimationMetadataType2["Transition"] = 1] = "Transition";
  AnimationMetadataType2[AnimationMetadataType2["Sequence"] = 2] = "Sequence";
  AnimationMetadataType2[AnimationMetadataType2["Group"] = 3] = "Group";
  AnimationMetadataType2[AnimationMetadataType2["Animate"] = 4] = "Animate";
  AnimationMetadataType2[AnimationMetadataType2["Keyframes"] = 5] = "Keyframes";
  AnimationMetadataType2[AnimationMetadataType2["Style"] = 6] = "Style";
  AnimationMetadataType2[AnimationMetadataType2["Trigger"] = 7] = "Trigger";
  AnimationMetadataType2[AnimationMetadataType2["Reference"] = 8] = "Reference";
  AnimationMetadataType2[AnimationMetadataType2["AnimateChild"] = 9] = "AnimateChild";
  AnimationMetadataType2[AnimationMetadataType2["AnimateRef"] = 10] = "AnimateRef";
  AnimationMetadataType2[AnimationMetadataType2["Query"] = 11] = "Query";
  AnimationMetadataType2[AnimationMetadataType2["Stagger"] = 12] = "Stagger";
})(AnimationMetadataType || (AnimationMetadataType = {}));
function trigger(name, definitions) {
  return {
    type: AnimationMetadataType.Trigger,
    name,
    definitions,
    options: {}
  };
}
function animate(timings, styles = null) {
  return {
    type: AnimationMetadataType.Animate,
    styles,
    timings
  };
}
function group(steps, options = null) {
  return {
    type: AnimationMetadataType.Group,
    steps,
    options
  };
}
function sequence(steps, options = null) {
  return {
    type: AnimationMetadataType.Sequence,
    steps,
    options
  };
}
function style(tokens2) {
  return {
    type: AnimationMetadataType.Style,
    styles: tokens2,
    offset: null
  };
}
function state(name, styles, options) {
  return {
    type: AnimationMetadataType.State,
    name,
    styles,
    options
  };
}
function keyframes(steps) {
  return {
    type: AnimationMetadataType.Keyframes,
    steps
  };
}
function transition(stateChangeExpr, steps, options = null) {
  return {
    type: AnimationMetadataType.Transition,
    expr: stateChangeExpr,
    animation: steps,
    options
  };
}
function animateChild(options = null) {
  return {
    type: AnimationMetadataType.AnimateChild,
    options
  };
}
function query(selector2, animation2, options = null) {
  return {
    type: AnimationMetadataType.Query,
    selector: selector2,
    animation: animation2,
    options
  };
}

// node_modules/@angular/animations/fesm2022/animations.mjs
var AnimationBuilder = class _AnimationBuilder {
  static ɵfac = function AnimationBuilder_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _AnimationBuilder)();
  };
  static ɵprov = ɵɵdefineInjectable({
    token: _AnimationBuilder,
    factory: () => (() => inject(BrowserAnimationBuilder))(),
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(AnimationBuilder, [{
    type: Injectable,
    args: [{
      providedIn: "root",
      useFactory: () => inject(BrowserAnimationBuilder)
    }]
  }], null, null);
})();
var AnimationFactory = class {
};
var BrowserAnimationBuilder = class _BrowserAnimationBuilder extends AnimationBuilder {
  animationModuleType = inject(ANIMATION_MODULE_TYPE, {
    optional: true
  });
  _nextAnimationId = 0;
  _renderer;
  constructor(rootRenderer, doc) {
    super();
    const typeData = {
      id: "0",
      encapsulation: ViewEncapsulation.None,
      styles: [],
      data: {
        animation: []
      }
    };
    this._renderer = rootRenderer.createRenderer(doc.body, typeData);
    if (this.animationModuleType === null && !isAnimationRenderer(this._renderer)) {
      throw new RuntimeError(3600, (typeof ngDevMode === "undefined" || ngDevMode) && "Angular detected that the `AnimationBuilder` was injected, but animation support was not enabled. Please make sure that you enable animations in your application by calling `provideAnimations()` or `provideAnimationsAsync()` function.");
    }
  }
  build(animation2) {
    const id = this._nextAnimationId;
    this._nextAnimationId++;
    const entry = Array.isArray(animation2) ? sequence(animation2) : animation2;
    issueAnimationCommand(this._renderer, null, id, "register", [entry]);
    return new BrowserAnimationFactory(id, this._renderer);
  }
  static ɵfac = function BrowserAnimationBuilder_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _BrowserAnimationBuilder)(ɵɵinject(RendererFactory2), ɵɵinject(DOCUMENT));
  };
  static ɵprov = ɵɵdefineInjectable({
    token: _BrowserAnimationBuilder,
    factory: _BrowserAnimationBuilder.ɵfac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(BrowserAnimationBuilder, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: RendererFactory2
  }, {
    type: Document,
    decorators: [{
      type: Inject,
      args: [DOCUMENT]
    }]
  }], null);
})();
var BrowserAnimationFactory = class extends AnimationFactory {
  _id;
  _renderer;
  constructor(_id, _renderer) {
    super();
    this._id = _id;
    this._renderer = _renderer;
  }
  create(element, options) {
    return new RendererAnimationPlayer(this._id, element, options || {}, this._renderer);
  }
};
var RendererAnimationPlayer = class {
  id;
  element;
  _renderer;
  parentPlayer = null;
  _started = false;
  constructor(id, element, options, _renderer) {
    this.id = id;
    this.element = element;
    this._renderer = _renderer;
    this._command("create", options);
  }
  _listen(eventName, callback) {
    return this._renderer.listen(this.element, `@@${this.id}:${eventName}`, callback);
  }
  _command(command, ...args) {
    issueAnimationCommand(this._renderer, this.element, this.id, command, args);
  }
  onDone(fn) {
    this._listen("done", fn);
  }
  onStart(fn) {
    this._listen("start", fn);
  }
  onDestroy(fn) {
    this._listen("destroy", fn);
  }
  init() {
    this._command("init");
  }
  hasStarted() {
    return this._started;
  }
  play() {
    this._command("play");
    this._started = true;
  }
  pause() {
    this._command("pause");
  }
  restart() {
    this._command("restart");
  }
  finish() {
    this._command("finish");
  }
  destroy() {
    this._command("destroy");
  }
  reset() {
    this._command("reset");
    this._started = false;
  }
  setPosition(p) {
    this._command("setPosition", p);
  }
  getPosition() {
    return unwrapAnimationRenderer(this._renderer)?.engine?.players[this.id]?.getPosition() ?? 0;
  }
  totalTime = 0;
};
function issueAnimationCommand(renderer, element, id, command, args) {
  renderer.setProperty(element, `@@${id}:${command}`, args);
}
function unwrapAnimationRenderer(renderer) {
  const type = renderer.ɵtype;
  if (type === 0) {
    return renderer;
  } else if (type === 1) {
    return renderer.animationRenderer;
  }
  return null;
}
function isAnimationRenderer(renderer) {
  const type = renderer.ɵtype;
  return type === 0 || type === 1;
}

// node_modules/@angular/cdk/fesm2022/_id-generator-chunk.mjs
var counters = {};
var _IdGenerator = class __IdGenerator {
  _appId = inject(APP_ID);
  static _infix = `a${Math.floor(Math.random() * 1e5).toString()}`;
  getId(prefix, randomize = false) {
    if (this._appId !== "ng") {
      prefix += this._appId;
    }
    if (!counters.hasOwnProperty(prefix)) {
      counters[prefix] = 0;
    }
    return `${prefix}${randomize ? __IdGenerator._infix + "-" : ""}${counters[prefix]++}`;
  }
  static ɵfac = function _IdGenerator_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || __IdGenerator)();
  };
  static ɵprov = ɵɵdefineInjectable({
    token: __IdGenerator,
    factory: __IdGenerator.ɵfac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(_IdGenerator, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();

// node_modules/@angular/cdk/fesm2022/_directionality-chunk.mjs
var DIR_DOCUMENT = new InjectionToken("cdk-dir-doc", {
  providedIn: "root",
  factory: () => inject(DOCUMENT)
});
var RTL_LOCALE_PATTERN = /^(ar|ckb|dv|he|iw|fa|nqo|ps|sd|ug|ur|yi|.*[-_](Adlm|Arab|Hebr|Nkoo|Rohg|Thaa))(?!.*[-_](Latn|Cyrl)($|-|_))($|-|_)/i;
function _resolveDirectionality(rawValue) {
  const value = rawValue?.toLowerCase() || "";
  if (value === "auto" && typeof navigator !== "undefined" && navigator?.language) {
    return RTL_LOCALE_PATTERN.test(navigator.language) ? "rtl" : "ltr";
  }
  return value === "rtl" ? "rtl" : "ltr";
}
var Directionality = class _Directionality {
  get value() {
    return this.valueSignal();
  }
  valueSignal = signal("ltr", ...ngDevMode ? [{
    debugName: "valueSignal"
  }] : []);
  change = new EventEmitter();
  constructor() {
    const _document = inject(DIR_DOCUMENT, {
      optional: true
    });
    if (_document) {
      const bodyDir = _document.body ? _document.body.dir : null;
      const htmlDir = _document.documentElement ? _document.documentElement.dir : null;
      this.valueSignal.set(_resolveDirectionality(bodyDir || htmlDir || "ltr"));
    }
  }
  ngOnDestroy() {
    this.change.complete();
  }
  static ɵfac = function Directionality_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _Directionality)();
  };
  static ɵprov = ɵɵdefineInjectable({
    token: _Directionality,
    factory: _Directionality.ɵfac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(Directionality, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [], null);
})();

// node_modules/@angular/cdk/fesm2022/_keycodes-chunk.mjs
var TAB = 9;
var ENTER = 13;
var ESCAPE = 27;
var SPACE = 32;
var PAGE_UP = 33;
var PAGE_DOWN = 34;
var END = 35;
var HOME = 36;
var LEFT_ARROW = 37;
var UP_ARROW = 38;
var RIGHT_ARROW = 39;
var DOWN_ARROW = 40;
var ZERO = 48;
var NINE = 57;
var A = 65;
var Z = 90;

// node_modules/@angular/cdk/fesm2022/_typeahead-chunk.mjs
var DEFAULT_TYPEAHEAD_DEBOUNCE_INTERVAL_MS = 200;
var Typeahead = class {
  _letterKeyStream = new Subject();
  _items = [];
  _selectedItemIndex = -1;
  _pressedLetters = [];
  _skipPredicateFn;
  _selectedItem = new Subject();
  selectedItem = this._selectedItem;
  constructor(initialItems, config) {
    const typeAheadInterval = typeof config?.debounceInterval === "number" ? config.debounceInterval : DEFAULT_TYPEAHEAD_DEBOUNCE_INTERVAL_MS;
    if (config?.skipPredicate) {
      this._skipPredicateFn = config.skipPredicate;
    }
    if ((typeof ngDevMode === "undefined" || ngDevMode) && initialItems.length && initialItems.some((item) => typeof item.getLabel !== "function")) {
      throw new Error("KeyManager items in typeahead mode must implement the `getLabel` method.");
    }
    this.setItems(initialItems);
    this._setupKeyHandler(typeAheadInterval);
  }
  destroy() {
    this._pressedLetters = [];
    this._letterKeyStream.complete();
    this._selectedItem.complete();
  }
  setCurrentSelectedItemIndex(index) {
    this._selectedItemIndex = index;
  }
  setItems(items) {
    this._items = items;
  }
  handleKey(event) {
    const keyCode = event.keyCode;
    if (event.key && event.key.length === 1) {
      this._letterKeyStream.next(event.key.toLocaleUpperCase());
    } else if (keyCode >= A && keyCode <= Z || keyCode >= ZERO && keyCode <= NINE) {
      this._letterKeyStream.next(String.fromCharCode(keyCode));
    }
  }
  isTyping() {
    return this._pressedLetters.length > 0;
  }
  reset() {
    this._pressedLetters = [];
  }
  _setupKeyHandler(typeAheadInterval) {
    this._letterKeyStream.pipe(tap((letter) => this._pressedLetters.push(letter)), debounceTime(typeAheadInterval), filter(() => this._pressedLetters.length > 0), map(() => this._pressedLetters.join("").toLocaleUpperCase())).subscribe((inputString) => {
      for (let i2 = 1; i2 < this._items.length + 1; i2++) {
        const index = (this._selectedItemIndex + i2) % this._items.length;
        const item = this._items[index];
        if (!this._skipPredicateFn?.(item) && item.getLabel?.().toLocaleUpperCase().trim().indexOf(inputString) === 0) {
          this._selectedItem.next(item);
          break;
        }
      }
      this._pressedLetters = [];
    });
  }
};

// node_modules/@angular/cdk/fesm2022/keycodes.mjs
function hasModifierKey(event, ...modifiers) {
  if (modifiers.length) {
    return modifiers.some((modifier) => event[modifier]);
  }
  return event.altKey || event.shiftKey || event.ctrlKey || event.metaKey;
}

// node_modules/@angular/cdk/fesm2022/_list-key-manager-chunk.mjs
var ListKeyManager = class {
  _items;
  _activeItemIndex = signal(-1, ...ngDevMode ? [{
    debugName: "_activeItemIndex"
  }] : []);
  _activeItem = signal(null, ...ngDevMode ? [{
    debugName: "_activeItem"
  }] : []);
  _wrap = false;
  _typeaheadSubscription = Subscription.EMPTY;
  _itemChangesSubscription;
  _vertical = true;
  _horizontal = null;
  _allowedModifierKeys = [];
  _homeAndEnd = false;
  _pageUpAndDown = {
    enabled: false,
    delta: 10
  };
  _effectRef;
  _typeahead;
  _skipPredicateFn = (item) => item.disabled;
  constructor(_items, injector) {
    this._items = _items;
    if (_items instanceof QueryList) {
      this._itemChangesSubscription = _items.changes.subscribe((newItems) => this._itemsChanged(newItems.toArray()));
    } else if (isSignal(_items)) {
      if (!injector && (typeof ngDevMode === "undefined" || ngDevMode)) {
        throw new Error("ListKeyManager constructed with a signal must receive an injector");
      }
      this._effectRef = effect(() => this._itemsChanged(_items()), __spreadProps(__spreadValues({}, ngDevMode ? {
        debugName: "_effectRef"
      } : {}), {
        injector
      }));
    }
  }
  tabOut = new Subject();
  change = new Subject();
  skipPredicate(predicate) {
    this._skipPredicateFn = predicate;
    return this;
  }
  withWrap(shouldWrap = true) {
    this._wrap = shouldWrap;
    return this;
  }
  withVerticalOrientation(enabled = true) {
    this._vertical = enabled;
    return this;
  }
  withHorizontalOrientation(direction) {
    this._horizontal = direction;
    return this;
  }
  withAllowedModifierKeys(keys2) {
    this._allowedModifierKeys = keys2;
    return this;
  }
  withTypeAhead(debounceInterval = 200) {
    if (typeof ngDevMode === "undefined" || ngDevMode) {
      const items2 = this._getItemsArray();
      if (items2.length > 0 && items2.some((item) => typeof item.getLabel !== "function")) {
        throw Error("ListKeyManager items in typeahead mode must implement the `getLabel` method.");
      }
    }
    this._typeaheadSubscription.unsubscribe();
    const items = this._getItemsArray();
    this._typeahead = new Typeahead(items, {
      debounceInterval: typeof debounceInterval === "number" ? debounceInterval : void 0,
      skipPredicate: (item) => this._skipPredicateFn(item)
    });
    this._typeaheadSubscription = this._typeahead.selectedItem.subscribe((item) => {
      this.setActiveItem(item);
    });
    return this;
  }
  cancelTypeahead() {
    this._typeahead?.reset();
    return this;
  }
  withHomeAndEnd(enabled = true) {
    this._homeAndEnd = enabled;
    return this;
  }
  withPageUpDown(enabled = true, delta = 10) {
    this._pageUpAndDown = {
      enabled,
      delta
    };
    return this;
  }
  setActiveItem(item) {
    const previousActiveItem = this._activeItem();
    this.updateActiveItem(item);
    if (this._activeItem() !== previousActiveItem) {
      this.change.next(this._activeItemIndex());
    }
  }
  onKeydown(event) {
    const keyCode = event.keyCode;
    const modifiers = ["altKey", "ctrlKey", "metaKey", "shiftKey"];
    const isModifierAllowed = modifiers.every((modifier) => {
      return !event[modifier] || this._allowedModifierKeys.indexOf(modifier) > -1;
    });
    switch (keyCode) {
      case TAB:
        this.tabOut.next();
        return;
      case DOWN_ARROW:
        if (this._vertical && isModifierAllowed) {
          this.setNextItemActive();
          break;
        } else {
          return;
        }
      case UP_ARROW:
        if (this._vertical && isModifierAllowed) {
          this.setPreviousItemActive();
          break;
        } else {
          return;
        }
      case RIGHT_ARROW:
        if (this._horizontal && isModifierAllowed) {
          this._horizontal === "rtl" ? this.setPreviousItemActive() : this.setNextItemActive();
          break;
        } else {
          return;
        }
      case LEFT_ARROW:
        if (this._horizontal && isModifierAllowed) {
          this._horizontal === "rtl" ? this.setNextItemActive() : this.setPreviousItemActive();
          break;
        } else {
          return;
        }
      case HOME:
        if (this._homeAndEnd && isModifierAllowed) {
          this.setFirstItemActive();
          break;
        } else {
          return;
        }
      case END:
        if (this._homeAndEnd && isModifierAllowed) {
          this.setLastItemActive();
          break;
        } else {
          return;
        }
      case PAGE_UP:
        if (this._pageUpAndDown.enabled && isModifierAllowed) {
          const targetIndex = this._activeItemIndex() - this._pageUpAndDown.delta;
          this._setActiveItemByIndex(targetIndex > 0 ? targetIndex : 0, 1);
          break;
        } else {
          return;
        }
      case PAGE_DOWN:
        if (this._pageUpAndDown.enabled && isModifierAllowed) {
          const targetIndex = this._activeItemIndex() + this._pageUpAndDown.delta;
          const itemsLength = this._getItemsArray().length;
          this._setActiveItemByIndex(targetIndex < itemsLength ? targetIndex : itemsLength - 1, -1);
          break;
        } else {
          return;
        }
      default:
        if (isModifierAllowed || hasModifierKey(event, "shiftKey")) {
          this._typeahead?.handleKey(event);
        }
        return;
    }
    this._typeahead?.reset();
    event.preventDefault();
  }
  get activeItemIndex() {
    return this._activeItemIndex();
  }
  get activeItem() {
    return this._activeItem();
  }
  isTyping() {
    return !!this._typeahead && this._typeahead.isTyping();
  }
  setFirstItemActive() {
    this._setActiveItemByIndex(0, 1);
  }
  setLastItemActive() {
    this._setActiveItemByIndex(this._getItemsArray().length - 1, -1);
  }
  setNextItemActive() {
    this._activeItemIndex() < 0 ? this.setFirstItemActive() : this._setActiveItemByDelta(1);
  }
  setPreviousItemActive() {
    this._activeItemIndex() < 0 && this._wrap ? this.setLastItemActive() : this._setActiveItemByDelta(-1);
  }
  updateActiveItem(item) {
    const itemArray = this._getItemsArray();
    const index = typeof item === "number" ? item : itemArray.indexOf(item);
    const activeItem = itemArray[index];
    this._activeItem.set(activeItem == null ? null : activeItem);
    this._activeItemIndex.set(index);
    this._typeahead?.setCurrentSelectedItemIndex(index);
  }
  destroy() {
    this._typeaheadSubscription.unsubscribe();
    this._itemChangesSubscription?.unsubscribe();
    this._effectRef?.destroy();
    this._typeahead?.destroy();
    this.tabOut.complete();
    this.change.complete();
  }
  _setActiveItemByDelta(delta) {
    this._wrap ? this._setActiveInWrapMode(delta) : this._setActiveInDefaultMode(delta);
  }
  _setActiveInWrapMode(delta) {
    const items = this._getItemsArray();
    for (let i2 = 1; i2 <= items.length; i2++) {
      const index = (this._activeItemIndex() + delta * i2 + items.length) % items.length;
      const item = items[index];
      if (!this._skipPredicateFn(item)) {
        this.setActiveItem(index);
        return;
      }
    }
  }
  _setActiveInDefaultMode(delta) {
    this._setActiveItemByIndex(this._activeItemIndex() + delta, delta);
  }
  _setActiveItemByIndex(index, fallbackDelta) {
    const items = this._getItemsArray();
    if (!items[index]) {
      return;
    }
    while (this._skipPredicateFn(items[index])) {
      index += fallbackDelta;
      if (!items[index]) {
        return;
      }
    }
    this.setActiveItem(index);
  }
  _getItemsArray() {
    if (isSignal(this._items)) {
      return this._items();
    }
    return this._items instanceof QueryList ? this._items.toArray() : this._items;
  }
  _itemsChanged(newItems) {
    this._typeahead?.setItems(newItems);
    const activeItem = this._activeItem();
    if (activeItem) {
      const newIndex = newItems.indexOf(activeItem);
      if (newIndex > -1 && newIndex !== this._activeItemIndex()) {
        this._activeItemIndex.set(newIndex);
        this._typeahead?.setCurrentSelectedItemIndex(newIndex);
      }
    }
  }
};

// node_modules/@angular/cdk/fesm2022/_activedescendant-key-manager-chunk.mjs
var ActiveDescendantKeyManager = class extends ListKeyManager {
  setActiveItem(index) {
    if (this.activeItem) {
      this.activeItem.setInactiveStyles();
    }
    super.setActiveItem(index);
    if (this.activeItem) {
      this.activeItem.setActiveStyles();
    }
  }
};

// node_modules/@angular/cdk/fesm2022/_selection-model-chunk.mjs
var SelectionModel = class {
  _multiple;
  _emitChanges;
  compareWith;
  _selection = /* @__PURE__ */ new Set();
  _deselectedToEmit = [];
  _selectedToEmit = [];
  _selected = null;
  get selected() {
    if (!this._selected) {
      this._selected = Array.from(this._selection.values());
    }
    return this._selected;
  }
  changed = new Subject();
  constructor(_multiple = false, initiallySelectedValues, _emitChanges = true, compareWith) {
    this._multiple = _multiple;
    this._emitChanges = _emitChanges;
    this.compareWith = compareWith;
    if (initiallySelectedValues && initiallySelectedValues.length) {
      if (_multiple) {
        initiallySelectedValues.forEach((value) => this._markSelected(value));
      } else {
        this._markSelected(initiallySelectedValues[0]);
      }
      this._selectedToEmit.length = 0;
    }
  }
  select(...values) {
    this._verifyValueAssignment(values);
    values.forEach((value) => this._markSelected(value));
    const changed = this._hasQueuedChanges();
    this._emitChangeEvent();
    return changed;
  }
  deselect(...values) {
    this._verifyValueAssignment(values);
    values.forEach((value) => this._unmarkSelected(value));
    const changed = this._hasQueuedChanges();
    this._emitChangeEvent();
    return changed;
  }
  setSelection(...values) {
    this._verifyValueAssignment(values);
    const oldValues = this.selected;
    const newSelectedSet = new Set(values.map((value) => this._getConcreteValue(value)));
    values.forEach((value) => this._markSelected(value));
    oldValues.filter((value) => !newSelectedSet.has(this._getConcreteValue(value, newSelectedSet))).forEach((value) => this._unmarkSelected(value));
    const changed = this._hasQueuedChanges();
    this._emitChangeEvent();
    return changed;
  }
  toggle(value) {
    return this.isSelected(value) ? this.deselect(value) : this.select(value);
  }
  clear(flushEvent = true) {
    this._unmarkAll();
    const changed = this._hasQueuedChanges();
    if (flushEvent) {
      this._emitChangeEvent();
    }
    return changed;
  }
  isSelected(value) {
    return this._selection.has(this._getConcreteValue(value));
  }
  isEmpty() {
    return this._selection.size === 0;
  }
  hasValue() {
    return !this.isEmpty();
  }
  sort(predicate) {
    if (this._multiple && this.selected) {
      this._selected.sort(predicate);
    }
  }
  isMultipleSelection() {
    return this._multiple;
  }
  _emitChangeEvent() {
    this._selected = null;
    if (this._selectedToEmit.length || this._deselectedToEmit.length) {
      this.changed.next({
        source: this,
        added: this._selectedToEmit,
        removed: this._deselectedToEmit
      });
      this._deselectedToEmit = [];
      this._selectedToEmit = [];
    }
  }
  _markSelected(value) {
    value = this._getConcreteValue(value);
    if (!this.isSelected(value)) {
      if (!this._multiple) {
        this._unmarkAll();
      }
      if (!this.isSelected(value)) {
        this._selection.add(value);
      }
      if (this._emitChanges) {
        this._selectedToEmit.push(value);
      }
    }
  }
  _unmarkSelected(value) {
    value = this._getConcreteValue(value);
    if (this.isSelected(value)) {
      this._selection.delete(value);
      if (this._emitChanges) {
        this._deselectedToEmit.push(value);
      }
    }
  }
  _unmarkAll() {
    if (!this.isEmpty()) {
      this._selection.forEach((value) => this._unmarkSelected(value));
    }
  }
  _verifyValueAssignment(values) {
    if (values.length > 1 && !this._multiple && (typeof ngDevMode === "undefined" || ngDevMode)) {
      throw getMultipleValuesInSingleSelectionError();
    }
  }
  _hasQueuedChanges() {
    return !!(this._deselectedToEmit.length || this._selectedToEmit.length);
  }
  _getConcreteValue(inputValue, selection) {
    if (!this.compareWith) {
      return inputValue;
    } else {
      selection = selection ?? this._selection;
      for (let selectedValue of selection) {
        if (this.compareWith(inputValue, selectedValue)) {
          return selectedValue;
        }
      }
      return inputValue;
    }
  }
};
function getMultipleValuesInSingleSelectionError() {
  return Error("Cannot pass multiple values into SelectionModel with single-value mode.");
}

// node_modules/@angular/cdk/fesm2022/_platform-chunk.mjs
var hasV8BreakIterator;
try {
  hasV8BreakIterator = typeof Intl !== "undefined" && Intl.v8BreakIterator;
} catch (e2) {
  hasV8BreakIterator = false;
}
var Platform = class _Platform {
  _platformId = inject(PLATFORM_ID);
  isBrowser = this._platformId ? isPlatformBrowser(this._platformId) : typeof document === "object" && !!document;
  EDGE = this.isBrowser && /(edge)/i.test(navigator.userAgent);
  TRIDENT = this.isBrowser && /(msie|trident)/i.test(navigator.userAgent);
  BLINK = this.isBrowser && !!(window.chrome || hasV8BreakIterator) && typeof CSS !== "undefined" && !this.EDGE && !this.TRIDENT;
  WEBKIT = this.isBrowser && /AppleWebKit/i.test(navigator.userAgent) && !this.BLINK && !this.EDGE && !this.TRIDENT;
  IOS = this.isBrowser && /iPad|iPhone|iPod/.test(navigator.userAgent) && !("MSStream" in window);
  FIREFOX = this.isBrowser && /(firefox|minefield)/i.test(navigator.userAgent);
  ANDROID = this.isBrowser && /android/i.test(navigator.userAgent) && !this.TRIDENT;
  SAFARI = this.isBrowser && /safari/i.test(navigator.userAgent) && this.WEBKIT;
  constructor() {
  }
  static ɵfac = function Platform_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _Platform)();
  };
  static ɵprov = ɵɵdefineInjectable({
    token: _Platform,
    factory: _Platform.ɵfac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(Platform, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [], null);
})();

// node_modules/@angular/cdk/fesm2022/_array-chunk.mjs
function coerceArray(value) {
  return Array.isArray(value) ? value : [value];
}

// node_modules/@angular/cdk/fesm2022/listbox.mjs
var ListboxSelectionModel = class extends SelectionModel {
  multiple;
  constructor(multiple = false, initiallySelectedValues, emitChanges = true, compareWith) {
    super(true, initiallySelectedValues, emitChanges, compareWith);
    this.multiple = multiple;
  }
  isMultipleSelection() {
    return this.multiple;
  }
  select(...values) {
    if (this.multiple) {
      return super.select(...values);
    } else {
      return super.setSelection(...values);
    }
  }
};
var CdkOption = class _CdkOption {
  get id() {
    return this._id || this._generatedId;
  }
  set id(value) {
    this._id = value;
  }
  _id;
  _generatedId = inject(_IdGenerator).getId("cdk-option-");
  value;
  typeaheadLabel = null;
  get disabled() {
    return this.listbox.disabled || this._disabled();
  }
  set disabled(value) {
    this._disabled.set(value);
  }
  _disabled = signal(false, ...ngDevMode ? [{
    debugName: "_disabled"
  }] : []);
  get enabledTabIndex() {
    return this._enabledTabIndex() === void 0 ? this.listbox.enabledTabIndex : this._enabledTabIndex();
  }
  set enabledTabIndex(value) {
    this._enabledTabIndex.set(value);
  }
  _enabledTabIndex = signal(void 0, ...ngDevMode ? [{
    debugName: "_enabledTabIndex"
  }] : []);
  element = inject(ElementRef).nativeElement;
  listbox = inject(CdkListbox);
  destroyed = new Subject();
  _clicked = new Subject();
  ngOnDestroy() {
    this.destroyed.next();
    this.destroyed.complete();
  }
  isSelected() {
    return this.listbox.isSelected(this);
  }
  isActive() {
    return this.listbox.isActive(this);
  }
  toggle() {
    this.listbox.toggle(this);
  }
  select() {
    this.listbox.select(this);
  }
  deselect() {
    this.listbox.deselect(this);
  }
  focus() {
    this.element.focus();
  }
  getLabel() {
    return (this.typeaheadLabel ?? this.element.textContent?.trim()) || "";
  }
  setActiveStyles() {
    if (this.listbox.useActiveDescendant) {
      this.element.scrollIntoView({
        block: "nearest",
        inline: "nearest"
      });
    }
  }
  setInactiveStyles() {
  }
  _handleFocus() {
    if (this.listbox.useActiveDescendant) {
      this.listbox._setActiveOption(this);
      this.listbox.focus();
    }
  }
  _getTabIndex() {
    if (this.listbox.useActiveDescendant || this.disabled) {
      return -1;
    }
    return this.isActive() ? this.enabledTabIndex : -1;
  }
  static ɵfac = function CdkOption_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _CdkOption)();
  };
  static ɵdir = ɵɵdefineDirective({
    type: _CdkOption,
    selectors: [["", "cdkOption", ""]],
    hostAttrs: ["role", "option", 1, "cdk-option"],
    hostVars: 6,
    hostBindings: function CdkOption_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("click", function CdkOption_click_HostBindingHandler($event) {
          return ctx._clicked.next($event);
        })("focus", function CdkOption_focus_HostBindingHandler() {
          return ctx._handleFocus();
        });
      }
      if (rf & 2) {
        ɵɵdomProperty("id", ctx.id);
        ɵɵattribute("aria-selected", ctx.isSelected())("tabindex", ctx._getTabIndex())("aria-disabled", ctx.disabled);
        ɵɵclassProp("cdk-option-active", ctx.isActive());
      }
    },
    inputs: {
      id: "id",
      value: [0, "cdkOption", "value"],
      typeaheadLabel: [0, "cdkOptionTypeaheadLabel", "typeaheadLabel"],
      disabled: [2, "cdkOptionDisabled", "disabled", booleanAttribute],
      enabledTabIndex: [2, "tabindex", "enabledTabIndex", (value) => value == null ? void 0 : numberAttribute(value)]
    },
    exportAs: ["cdkOption"]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(CdkOption, [{
    type: Directive,
    args: [{
      selector: "[cdkOption]",
      exportAs: "cdkOption",
      host: {
        "role": "option",
        "class": "cdk-option",
        "[id]": "id",
        "[attr.aria-selected]": "isSelected()",
        "[attr.tabindex]": "_getTabIndex()",
        "[attr.aria-disabled]": "disabled",
        "[class.cdk-option-active]": "isActive()",
        "(click)": "_clicked.next($event)",
        "(focus)": "_handleFocus()"
      }
    }]
  }], null, {
    id: [{
      type: Input
    }],
    value: [{
      type: Input,
      args: ["cdkOption"]
    }],
    typeaheadLabel: [{
      type: Input,
      args: ["cdkOptionTypeaheadLabel"]
    }],
    disabled: [{
      type: Input,
      args: [{
        alias: "cdkOptionDisabled",
        transform: booleanAttribute
      }]
    }],
    enabledTabIndex: [{
      type: Input,
      args: [{
        alias: "tabindex",
        transform: (value) => value == null ? void 0 : numberAttribute(value)
      }]
    }]
  });
})();
var CdkListbox = class _CdkListbox {
  _cleanupWindowBlur;
  get id() {
    return this._id || this._generatedId;
  }
  set id(value) {
    this._id = value;
  }
  _id;
  _generatedId = inject(_IdGenerator).getId("cdk-listbox-");
  get enabledTabIndex() {
    return this._enabledTabIndex() === void 0 ? 0 : this._enabledTabIndex();
  }
  set enabledTabIndex(value) {
    this._enabledTabIndex.set(value);
  }
  _enabledTabIndex = signal(void 0, ...ngDevMode ? [{
    debugName: "_enabledTabIndex"
  }] : []);
  get value() {
    return this._invalid ? [] : this.selectionModel.selected;
  }
  set value(value) {
    this._setSelection(value);
  }
  get multiple() {
    return this.selectionModel.multiple;
  }
  set multiple(value) {
    this.selectionModel.multiple = value;
    if (this.options) {
      this._updateInternalValue();
    }
  }
  get disabled() {
    return this._disabled();
  }
  set disabled(value) {
    this._disabled.set(value);
  }
  _disabled = signal(false, ...ngDevMode ? [{
    debugName: "_disabled"
  }] : []);
  get useActiveDescendant() {
    return this._useActiveDescendant();
  }
  set useActiveDescendant(value) {
    this._useActiveDescendant.set(value);
  }
  _useActiveDescendant = signal(false, ...ngDevMode ? [{
    debugName: "_useActiveDescendant"
  }] : []);
  get orientation() {
    return this._orientation;
  }
  set orientation(value) {
    this._orientation = value === "horizontal" ? "horizontal" : "vertical";
    if (value === "horizontal") {
      this.listKeyManager?.withHorizontalOrientation(this._dir?.value || "ltr");
    } else {
      this.listKeyManager?.withVerticalOrientation();
    }
  }
  _orientation = "vertical";
  get compareWith() {
    return this.selectionModel.compareWith;
  }
  set compareWith(fn) {
    this.selectionModel.compareWith = fn;
  }
  get navigationWrapDisabled() {
    return this._navigationWrapDisabled;
  }
  set navigationWrapDisabled(wrap) {
    this._navigationWrapDisabled = wrap;
    this.listKeyManager?.withWrap(!this._navigationWrapDisabled);
  }
  _navigationWrapDisabled = false;
  get navigateDisabledOptions() {
    return this._navigateDisabledOptions;
  }
  set navigateDisabledOptions(skip) {
    this._navigateDisabledOptions = skip;
    this.listKeyManager?.skipPredicate(this._navigateDisabledOptions ? this._skipNonePredicate : this._skipDisabledPredicate);
  }
  _navigateDisabledOptions = false;
  valueChange = new Subject();
  options;
  selectionModel = new ListboxSelectionModel();
  listKeyManager;
  destroyed = new Subject();
  element = inject(ElementRef).nativeElement;
  ngZone = inject(NgZone);
  changeDetectorRef = inject(ChangeDetectorRef);
  _invalid = false;
  _lastTriggered = null;
  _onTouched = () => {
  };
  _onChange = () => {
  };
  _optionClicked = defer(() => this.options.changes.pipe(startWith(this.options), switchMap((options) => merge(...options.map((option) => option._clicked.pipe(map((event) => ({
    option,
    event
  }))))))));
  _dir = inject(Directionality, {
    optional: true
  });
  _isBrowser = inject(Platform).isBrowser;
  _skipDisabledPredicate = (option) => option.disabled;
  _skipNonePredicate = () => false;
  _hasFocus = false;
  _previousActiveOption = null;
  constructor() {
    if (this._isBrowser) {
      const renderer = inject(Renderer2);
      this._cleanupWindowBlur = this.ngZone.runOutsideAngular(() => {
        return renderer.listen("window", "blur", () => {
          if (this.element.contains(document.activeElement) && this._previousActiveOption) {
            this._setActiveOption(this._previousActiveOption);
            this._previousActiveOption = null;
          }
        });
      });
    }
  }
  ngAfterContentInit() {
    if (typeof ngDevMode === "undefined" || ngDevMode) {
      this._verifyNoOptionValueCollisions();
      this._verifyOptionValues();
    }
    this._initKeyManager();
    merge(this.selectionModel.changed, this.options.changes).pipe(startWith(null), takeUntil(this.destroyed)).subscribe(() => this._updateInternalValue());
    this._optionClicked.pipe(filter(({
      option
    }) => !option.disabled), takeUntil(this.destroyed)).subscribe(({
      option,
      event
    }) => this._handleOptionClicked(option, event));
  }
  ngOnDestroy() {
    this._cleanupWindowBlur?.();
    this.listKeyManager?.destroy();
    this.destroyed.next();
    this.destroyed.complete();
  }
  toggle(option) {
    this.toggleValue(option.value);
  }
  toggleValue(value) {
    if (this._invalid) {
      this.selectionModel.clear(false);
    }
    this.selectionModel.toggle(value);
  }
  select(option) {
    this.selectValue(option.value);
  }
  selectValue(value) {
    if (this._invalid) {
      this.selectionModel.clear(false);
    }
    this.selectionModel.select(value);
  }
  deselect(option) {
    this.deselectValue(option.value);
  }
  deselectValue(value) {
    if (this._invalid) {
      this.selectionModel.clear(false);
    }
    this.selectionModel.deselect(value);
  }
  setAllSelected(isSelected) {
    if (!isSelected) {
      this.selectionModel.clear();
    } else {
      if (this._invalid) {
        this.selectionModel.clear(false);
      }
      this.selectionModel.select(...this.options.map((option) => option.value));
    }
  }
  isSelected(option) {
    return this.isValueSelected(option.value);
  }
  isActive(option) {
    return !!(this.listKeyManager?.activeItem === option);
  }
  isValueSelected(value) {
    if (this._invalid) {
      return false;
    }
    return this.selectionModel.isSelected(value);
  }
  registerOnChange(fn) {
    this._onChange = fn;
  }
  registerOnTouched(fn) {
    this._onTouched = fn;
  }
  writeValue(value) {
    this._setSelection(value);
    this._verifyOptionValues();
  }
  setDisabledState(isDisabled) {
    this.disabled = isDisabled;
    this.changeDetectorRef.markForCheck();
  }
  focus() {
    this.element.focus();
  }
  triggerOption(option) {
    if (option && !option.disabled) {
      this._lastTriggered = option;
      const changed = this.multiple ? this.selectionModel.toggle(option.value) : this.selectionModel.select(option.value);
      if (changed) {
        this._onChange(this.value);
        this.valueChange.next({
          value: this.value,
          listbox: this,
          option
        });
      }
    }
  }
  triggerRange(trigger4, from, to, on) {
    if (this.disabled || trigger4 && trigger4.disabled) {
      return;
    }
    this._lastTriggered = trigger4;
    const isEqual = this.compareWith ?? Object.is;
    const updateValues = [...this.options].slice(Math.max(0, Math.min(from, to)), Math.min(this.options.length, Math.max(from, to) + 1)).filter((option) => !option.disabled).map((option) => option.value);
    const selected = [...this.value];
    for (const updateValue of updateValues) {
      const selectedIndex = selected.findIndex((selectedValue) => isEqual(selectedValue, updateValue));
      if (on && selectedIndex === -1) {
        selected.push(updateValue);
      } else if (!on && selectedIndex !== -1) {
        selected.splice(selectedIndex, 1);
      }
    }
    let changed = this.selectionModel.setSelection(...selected);
    if (changed) {
      this._onChange(this.value);
      this.valueChange.next({
        value: this.value,
        listbox: this,
        option: trigger4
      });
    }
  }
  _setActiveOption(option) {
    this.listKeyManager.setActiveItem(option);
  }
  _handleFocus() {
    if (!this.useActiveDescendant) {
      if (this.selectionModel.selected.length > 0) {
        this._setNextFocusToSelectedOption();
      } else {
        this.listKeyManager.setNextItemActive();
      }
      this._focusActiveOption();
    }
  }
  _handleKeydown(event) {
    if (this.disabled) {
      return;
    }
    const {
      keyCode
    } = event;
    const previousActiveIndex = this.listKeyManager.activeItemIndex;
    const ctrlKeys = ["ctrlKey", "metaKey"];
    if (this.multiple && keyCode === A && hasModifierKey(event, ...ctrlKeys)) {
      this.triggerRange(null, 0, this.options.length - 1, this.options.length !== this.value.length);
      event.preventDefault();
      return;
    }
    if (this.multiple && (keyCode === SPACE || keyCode === ENTER) && hasModifierKey(event, "shiftKey")) {
      if (this.listKeyManager.activeItem && this.listKeyManager.activeItemIndex != null) {
        this.triggerRange(this.listKeyManager.activeItem, this._getLastTriggeredIndex() ?? this.listKeyManager.activeItemIndex, this.listKeyManager.activeItemIndex, !this.listKeyManager.activeItem.isSelected());
      }
      event.preventDefault();
      return;
    }
    if (this.multiple && keyCode === HOME && hasModifierKey(event, ...ctrlKeys) && hasModifierKey(event, "shiftKey")) {
      const trigger4 = this.listKeyManager.activeItem;
      if (trigger4) {
        const from = this.listKeyManager.activeItemIndex;
        this.listKeyManager.setFirstItemActive();
        this.triggerRange(trigger4, from, this.listKeyManager.activeItemIndex, !trigger4.isSelected());
      }
      event.preventDefault();
      return;
    }
    if (this.multiple && keyCode === END && hasModifierKey(event, ...ctrlKeys) && hasModifierKey(event, "shiftKey")) {
      const trigger4 = this.listKeyManager.activeItem;
      if (trigger4) {
        const from = this.listKeyManager.activeItemIndex;
        this.listKeyManager.setLastItemActive();
        this.triggerRange(trigger4, from, this.listKeyManager.activeItemIndex, !trigger4.isSelected());
      }
      event.preventDefault();
      return;
    }
    if (keyCode === SPACE || keyCode === ENTER) {
      this.triggerOption(this.listKeyManager.activeItem);
      event.preventDefault();
      return;
    }
    const isNavKey = keyCode === UP_ARROW || keyCode === DOWN_ARROW || keyCode === LEFT_ARROW || keyCode === RIGHT_ARROW || keyCode === HOME || keyCode === END;
    this.listKeyManager.onKeydown(event);
    if (isNavKey && event.shiftKey && previousActiveIndex !== this.listKeyManager.activeItemIndex) {
      this.triggerOption(this.listKeyManager.activeItem);
    }
  }
  _handleFocusIn() {
    this._hasFocus = true;
  }
  _handleFocusOut(event) {
    this._previousActiveOption = this.listKeyManager.activeItem;
    const otherElement = event.relatedTarget;
    if (this.element !== otherElement && !this.element.contains(otherElement)) {
      this._onTouched();
      this._hasFocus = false;
      this._setNextFocusToSelectedOption();
    }
  }
  _getAriaActiveDescendant() {
    return this.useActiveDescendant ? this.listKeyManager?.activeItem?.id : null;
  }
  _getTabIndex() {
    if (this.disabled) {
      return -1;
    }
    return this.useActiveDescendant || !this.listKeyManager.activeItem ? this.enabledTabIndex : -1;
  }
  _initKeyManager() {
    this.listKeyManager = new ActiveDescendantKeyManager(this.options).withWrap(!this._navigationWrapDisabled).withTypeAhead().withHomeAndEnd().withAllowedModifierKeys(["shiftKey"]).skipPredicate(this._navigateDisabledOptions ? this._skipNonePredicate : this._skipDisabledPredicate);
    if (this.orientation === "vertical") {
      this.listKeyManager.withVerticalOrientation();
    } else {
      this.listKeyManager.withHorizontalOrientation(this._dir?.value || "ltr");
    }
    if (this.selectionModel.selected.length) {
      Promise.resolve().then(() => this._setNextFocusToSelectedOption());
    }
    this.listKeyManager.change.subscribe(() => this._focusActiveOption());
    this.options.changes.pipe(takeUntil(this.destroyed)).subscribe(() => {
      const activeOption = this.listKeyManager.activeItem;
      if (activeOption && !this.options.find((option) => option === activeOption)) {
        this.listKeyManager.setActiveItem(-1);
        this.changeDetectorRef.markForCheck();
      }
    });
  }
  _focusActiveOption() {
    if (!this.useActiveDescendant) {
      this.listKeyManager.activeItem?.focus();
    }
    this.changeDetectorRef.markForCheck();
  }
  _setSelection(value) {
    if (this._invalid) {
      this.selectionModel.clear(false);
    }
    this.selectionModel.setSelection(...this._coerceValue(value));
    if (!this._hasFocus) {
      this._setNextFocusToSelectedOption();
    }
  }
  _setNextFocusToSelectedOption() {
    const selected = this.options?.find((option) => option.isSelected());
    if (selected) {
      this.listKeyManager.updateActiveItem(selected);
    }
  }
  _updateInternalValue() {
    const indexCache = /* @__PURE__ */ new Map();
    this.selectionModel.sort((a, b) => {
      const aIndex = this._getIndexForValue(indexCache, a);
      const bIndex = this._getIndexForValue(indexCache, b);
      return aIndex - bIndex;
    });
    const selected = this.selectionModel.selected;
    this._invalid = !this.multiple && selected.length > 1 || !!this._getInvalidOptionValues(selected).length;
    this.changeDetectorRef.markForCheck();
  }
  _getIndexForValue(cache, value) {
    const isEqual = this.compareWith || Object.is;
    if (!cache.has(value)) {
      let index = -1;
      for (let i2 = 0; i2 < this.options.length; i2++) {
        if (isEqual(value, this.options.get(i2).value)) {
          index = i2;
          break;
        }
      }
      cache.set(value, index);
    }
    return cache.get(value);
  }
  _handleOptionClicked(option, event) {
    event.preventDefault();
    this.listKeyManager.setActiveItem(option);
    if (event.shiftKey && this.multiple) {
      this.triggerRange(option, this._getLastTriggeredIndex() ?? this.listKeyManager.activeItemIndex, this.listKeyManager.activeItemIndex, !option.isSelected());
    } else {
      this.triggerOption(option);
    }
  }
  _verifyNoOptionValueCollisions() {
    this.options.changes.pipe(startWith(this.options), takeUntil(this.destroyed)).subscribe(() => {
      const isEqual = this.compareWith ?? Object.is;
      for (let i2 = 0; i2 < this.options.length; i2++) {
        const option = this.options.get(i2);
        let duplicate = null;
        for (let j = i2 + 1; j < this.options.length; j++) {
          const other = this.options.get(j);
          if (isEqual(option.value, other.value)) {
            duplicate = other;
            break;
          }
        }
        if (duplicate) {
          if (this.compareWith) {
            console.warn(`Found multiple CdkOption representing the same value under the given compareWith function`, {
              option1: option.element,
              option2: duplicate.element,
              compareWith: this.compareWith
            });
          } else {
            console.warn(`Found multiple CdkOption with the same value`, {
              option1: option.element,
              option2: duplicate.element
            });
          }
          return;
        }
      }
    });
  }
  _verifyOptionValues() {
    if (this.options && (typeof ngDevMode === "undefined" || ngDevMode)) {
      const selected = this.selectionModel.selected;
      const invalidValues = this._getInvalidOptionValues(selected);
      if (!this.multiple && selected.length > 1) {
        throw Error("Listbox cannot have more than one selected value in single selection mode.");
      }
      if (invalidValues.length) {
        throw Error("Listbox has selected values that do not match any of its options.");
      }
    }
  }
  _coerceValue(value) {
    return value == null ? [] : coerceArray(value);
  }
  _getInvalidOptionValues(values) {
    const isEqual = this.compareWith || Object.is;
    const validValues = (this.options || []).map((option) => option.value);
    return values.filter((value) => !validValues.some((validValue2) => isEqual(value, validValue2)));
  }
  _getLastTriggeredIndex() {
    const index = this.options.toArray().indexOf(this._lastTriggered);
    return index === -1 ? null : index;
  }
  static ɵfac = function CdkListbox_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _CdkListbox)();
  };
  static ɵdir = ɵɵdefineDirective({
    type: _CdkListbox,
    selectors: [["", "cdkListbox", ""]],
    contentQueries: function CdkListbox_ContentQueries(rf, ctx, dirIndex) {
      if (rf & 1) {
        ɵɵcontentQuery(dirIndex, CdkOption, 5);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.options = _t);
      }
    },
    hostAttrs: ["role", "listbox", 1, "cdk-listbox"],
    hostVars: 6,
    hostBindings: function CdkListbox_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("focus", function CdkListbox_focus_HostBindingHandler() {
          return ctx._handleFocus();
        })("keydown", function CdkListbox_keydown_HostBindingHandler($event) {
          return ctx._handleKeydown($event);
        })("focusout", function CdkListbox_focusout_HostBindingHandler($event) {
          return ctx._handleFocusOut($event);
        })("focusin", function CdkListbox_focusin_HostBindingHandler() {
          return ctx._handleFocusIn();
        });
      }
      if (rf & 2) {
        ɵɵdomProperty("id", ctx.id);
        ɵɵattribute("tabindex", ctx._getTabIndex())("aria-disabled", ctx.disabled)("aria-multiselectable", ctx.multiple)("aria-activedescendant", ctx._getAriaActiveDescendant())("aria-orientation", ctx.orientation);
      }
    },
    inputs: {
      id: "id",
      enabledTabIndex: [0, "tabindex", "enabledTabIndex"],
      value: [0, "cdkListboxValue", "value"],
      multiple: [2, "cdkListboxMultiple", "multiple", booleanAttribute],
      disabled: [2, "cdkListboxDisabled", "disabled", booleanAttribute],
      useActiveDescendant: [2, "cdkListboxUseActiveDescendant", "useActiveDescendant", booleanAttribute],
      orientation: [0, "cdkListboxOrientation", "orientation"],
      compareWith: [0, "cdkListboxCompareWith", "compareWith"],
      navigationWrapDisabled: [2, "cdkListboxNavigationWrapDisabled", "navigationWrapDisabled", booleanAttribute],
      navigateDisabledOptions: [2, "cdkListboxNavigatesDisabledOptions", "navigateDisabledOptions", booleanAttribute]
    },
    outputs: {
      valueChange: "cdkListboxValueChange"
    },
    exportAs: ["cdkListbox"],
    features: [ɵɵProvidersFeature([{
      provide: NG_VALUE_ACCESSOR,
      useExisting: forwardRef(() => _CdkListbox),
      multi: true
    }])]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(CdkListbox, [{
    type: Directive,
    args: [{
      selector: "[cdkListbox]",
      exportAs: "cdkListbox",
      host: {
        "role": "listbox",
        "class": "cdk-listbox",
        "[id]": "id",
        "[attr.tabindex]": "_getTabIndex()",
        "[attr.aria-disabled]": "disabled",
        "[attr.aria-multiselectable]": "multiple",
        "[attr.aria-activedescendant]": "_getAriaActiveDescendant()",
        "[attr.aria-orientation]": "orientation",
        "(focus)": "_handleFocus()",
        "(keydown)": "_handleKeydown($event)",
        "(focusout)": "_handleFocusOut($event)",
        "(focusin)": "_handleFocusIn()"
      },
      providers: [{
        provide: NG_VALUE_ACCESSOR,
        useExisting: forwardRef(() => CdkListbox),
        multi: true
      }]
    }]
  }], () => [], {
    id: [{
      type: Input
    }],
    enabledTabIndex: [{
      type: Input,
      args: ["tabindex"]
    }],
    value: [{
      type: Input,
      args: ["cdkListboxValue"]
    }],
    multiple: [{
      type: Input,
      args: [{
        alias: "cdkListboxMultiple",
        transform: booleanAttribute
      }]
    }],
    disabled: [{
      type: Input,
      args: [{
        alias: "cdkListboxDisabled",
        transform: booleanAttribute
      }]
    }],
    useActiveDescendant: [{
      type: Input,
      args: [{
        alias: "cdkListboxUseActiveDescendant",
        transform: booleanAttribute
      }]
    }],
    orientation: [{
      type: Input,
      args: ["cdkListboxOrientation"]
    }],
    compareWith: [{
      type: Input,
      args: ["cdkListboxCompareWith"]
    }],
    navigationWrapDisabled: [{
      type: Input,
      args: [{
        alias: "cdkListboxNavigationWrapDisabled",
        transform: booleanAttribute
      }]
    }],
    navigateDisabledOptions: [{
      type: Input,
      args: [{
        alias: "cdkListboxNavigatesDisabledOptions",
        transform: booleanAttribute
      }]
    }],
    valueChange: [{
      type: Output,
      args: ["cdkListboxValueChange"]
    }],
    options: [{
      type: ContentChildren,
      args: [CdkOption, {
        descendants: true
      }]
    }]
  });
})();
var EXPORTED_DECLARATIONS = [CdkListbox, CdkOption];
var CdkListboxModule = class _CdkListboxModule {
  static ɵfac = function CdkListboxModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _CdkListboxModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _CdkListboxModule,
    imports: [CdkListbox, CdkOption],
    exports: [CdkListbox, CdkOption]
  });
  static ɵinj = ɵɵdefineInjector({});
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(CdkListboxModule, [{
    type: NgModule,
    args: [{
      imports: [...EXPORTED_DECLARATIONS],
      exports: [...EXPORTED_DECLARATIONS]
    }]
  }], null, null);
})();

// node_modules/@angular/cdk/fesm2022/_shadow-dom-chunk.mjs
var shadowDomIsSupported;
function _supportsShadowDom() {
  if (shadowDomIsSupported == null) {
    const head = typeof document !== "undefined" ? document.head : null;
    shadowDomIsSupported = !!(head && (head.createShadowRoot || head.attachShadow));
  }
  return shadowDomIsSupported;
}
function _getShadowRoot(element) {
  if (_supportsShadowDom()) {
    const rootNode = element.getRootNode ? element.getRootNode() : null;
    if (typeof ShadowRoot !== "undefined" && ShadowRoot && rootNode instanceof ShadowRoot) {
      return rootNode;
    }
  }
  return null;
}
function _getEventTarget(event) {
  return event.composedPath ? event.composedPath()[0] : event.target;
}

// node_modules/@angular/cdk/fesm2022/_test-environment-chunk.mjs
function _isTestEnvironment() {
  return typeof __karma__ !== "undefined" && !!__karma__ || typeof jasmine !== "undefined" && !!jasmine || typeof jest !== "undefined" && !!jest || typeof Mocha !== "undefined" && !!Mocha;
}

// node_modules/@angular/cdk/fesm2022/_style-loader-chunk.mjs
var appsWithLoaders = /* @__PURE__ */ new WeakMap();
var _CdkPrivateStyleLoader = class __CdkPrivateStyleLoader {
  _appRef;
  _injector = inject(Injector);
  _environmentInjector = inject(EnvironmentInjector);
  load(loader) {
    const appRef = this._appRef = this._appRef || this._injector.get(ApplicationRef);
    let data = appsWithLoaders.get(appRef);
    if (!data) {
      data = {
        loaders: /* @__PURE__ */ new Set(),
        refs: []
      };
      appsWithLoaders.set(appRef, data);
      appRef.onDestroy(() => {
        appsWithLoaders.get(appRef)?.refs.forEach((ref) => ref.destroy());
        appsWithLoaders.delete(appRef);
      });
    }
    if (!data.loaders.has(loader)) {
      data.loaders.add(loader);
      data.refs.push(createComponent(loader, {
        environmentInjector: this._environmentInjector
      }));
    }
  }
  static ɵfac = function _CdkPrivateStyleLoader_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || __CdkPrivateStyleLoader)();
  };
  static ɵprov = ɵɵdefineInjectable({
    token: __CdkPrivateStyleLoader,
    factory: __CdkPrivateStyleLoader.ɵfac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(_CdkPrivateStyleLoader, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();

// node_modules/@angular/cdk/fesm2022/_css-pixel-value-chunk.mjs
function coerceCssPixelValue(value) {
  if (value == null) {
    return "";
  }
  return typeof value === "string" ? value : `${value}px`;
}

// node_modules/@angular/cdk/fesm2022/_element-chunk.mjs
function coerceNumberProperty(value, fallbackValue = 0) {
  if (_isNumberValue(value)) {
    return Number(value);
  }
  return arguments.length === 2 ? fallbackValue : 0;
}
function _isNumberValue(value) {
  return !isNaN(parseFloat(value)) && !isNaN(Number(value));
}
function coerceElement(elementOrRef) {
  return elementOrRef instanceof ElementRef ? elementOrRef.nativeElement : elementOrRef;
}

// node_modules/@angular/cdk/fesm2022/_scrolling-chunk.mjs
var RtlScrollAxisType;
(function(RtlScrollAxisType2) {
  RtlScrollAxisType2[RtlScrollAxisType2["NORMAL"] = 0] = "NORMAL";
  RtlScrollAxisType2[RtlScrollAxisType2["NEGATED"] = 1] = "NEGATED";
  RtlScrollAxisType2[RtlScrollAxisType2["INVERTED"] = 2] = "INVERTED";
})(RtlScrollAxisType || (RtlScrollAxisType = {}));
var rtlScrollAxisType;
var scrollBehaviorSupported;
function supportsScrollBehavior() {
  if (scrollBehaviorSupported == null) {
    if (typeof document !== "object" || !document || typeof Element !== "function" || !Element) {
      scrollBehaviorSupported = false;
      return scrollBehaviorSupported;
    }
    if (document.documentElement?.style && "scrollBehavior" in document.documentElement.style) {
      scrollBehaviorSupported = true;
    } else {
      const scrollToFunction = Element.prototype.scrollTo;
      if (scrollToFunction) {
        scrollBehaviorSupported = !/\{\s*\[native code\]\s*\}/.test(scrollToFunction.toString());
      } else {
        scrollBehaviorSupported = false;
      }
    }
  }
  return scrollBehaviorSupported;
}
function getRtlScrollAxisType() {
  if (typeof document !== "object" || !document) {
    return RtlScrollAxisType.NORMAL;
  }
  if (rtlScrollAxisType == null) {
    const scrollContainer = document.createElement("div");
    const containerStyle = scrollContainer.style;
    scrollContainer.dir = "rtl";
    containerStyle.width = "1px";
    containerStyle.overflow = "auto";
    containerStyle.visibility = "hidden";
    containerStyle.pointerEvents = "none";
    containerStyle.position = "absolute";
    const content = document.createElement("div");
    const contentStyle = content.style;
    contentStyle.width = "2px";
    contentStyle.height = "1px";
    scrollContainer.appendChild(content);
    document.body.appendChild(scrollContainer);
    rtlScrollAxisType = RtlScrollAxisType.NORMAL;
    if (scrollContainer.scrollLeft === 0) {
      scrollContainer.scrollLeft = 1;
      rtlScrollAxisType = scrollContainer.scrollLeft === 0 ? RtlScrollAxisType.NEGATED : RtlScrollAxisType.INVERTED;
    }
    scrollContainer.remove();
  }
  return rtlScrollAxisType;
}

// node_modules/@angular/cdk/fesm2022/_data-source-chunk.mjs
var DataSource = class {
};
function isDataSource(value) {
  return value && typeof value.connect === "function" && !(value instanceof ConnectableObservable);
}

// node_modules/@angular/cdk/fesm2022/_recycle-view-repeater-strategy-chunk.mjs
var ArrayDataSource = class extends DataSource {
  _data;
  constructor(_data) {
    super();
    this._data = _data;
  }
  connect() {
    return isObservable(this._data) ? this._data : of(this._data);
  }
  disconnect() {
  }
};
var _ViewRepeaterOperation;
(function(_ViewRepeaterOperation2) {
  _ViewRepeaterOperation2[_ViewRepeaterOperation2["REPLACED"] = 0] = "REPLACED";
  _ViewRepeaterOperation2[_ViewRepeaterOperation2["INSERTED"] = 1] = "INSERTED";
  _ViewRepeaterOperation2[_ViewRepeaterOperation2["MOVED"] = 2] = "MOVED";
  _ViewRepeaterOperation2[_ViewRepeaterOperation2["REMOVED"] = 3] = "REMOVED";
})(_ViewRepeaterOperation || (_ViewRepeaterOperation = {}));
var _RecycleViewRepeaterStrategy = class {
  viewCacheSize = 20;
  _viewCache = [];
  applyChanges(changes, viewContainerRef, itemContextFactory, itemValueResolver, itemViewChanged) {
    changes.forEachOperation((record, adjustedPreviousIndex, currentIndex) => {
      let view;
      let operation;
      if (record.previousIndex == null) {
        const viewArgsFactory = () => itemContextFactory(record, adjustedPreviousIndex, currentIndex);
        view = this._insertView(viewArgsFactory, currentIndex, viewContainerRef, itemValueResolver(record));
        operation = view ? _ViewRepeaterOperation.INSERTED : _ViewRepeaterOperation.REPLACED;
      } else if (currentIndex == null) {
        this._detachAndCacheView(adjustedPreviousIndex, viewContainerRef);
        operation = _ViewRepeaterOperation.REMOVED;
      } else {
        view = this._moveView(adjustedPreviousIndex, currentIndex, viewContainerRef, itemValueResolver(record));
        operation = _ViewRepeaterOperation.MOVED;
      }
      if (itemViewChanged) {
        itemViewChanged({
          context: view?.context,
          operation,
          record
        });
      }
    });
  }
  detach() {
    for (const view of this._viewCache) {
      view.destroy();
    }
    this._viewCache = [];
  }
  _insertView(viewArgsFactory, currentIndex, viewContainerRef, value) {
    const cachedView = this._insertViewFromCache(currentIndex, viewContainerRef);
    if (cachedView) {
      cachedView.context.$implicit = value;
      return void 0;
    }
    const viewArgs = viewArgsFactory();
    return viewContainerRef.createEmbeddedView(viewArgs.templateRef, viewArgs.context, viewArgs.index);
  }
  _detachAndCacheView(index, viewContainerRef) {
    const detachedView = viewContainerRef.detach(index);
    this._maybeCacheView(detachedView, viewContainerRef);
  }
  _moveView(adjustedPreviousIndex, currentIndex, viewContainerRef, value) {
    const view = viewContainerRef.get(adjustedPreviousIndex);
    viewContainerRef.move(view, currentIndex);
    view.context.$implicit = value;
    return view;
  }
  _maybeCacheView(view, viewContainerRef) {
    if (this._viewCache.length < this.viewCacheSize) {
      this._viewCache.push(view);
    } else {
      const index = viewContainerRef.indexOf(view);
      if (index === -1) {
        view.destroy();
      } else {
        viewContainerRef.remove(index);
      }
    }
  }
  _insertViewFromCache(index, viewContainerRef) {
    const cachedView = this._viewCache.pop();
    if (cachedView) {
      viewContainerRef.insert(cachedView, index);
    }
    return cachedView || null;
  }
};

// node_modules/@angular/cdk/fesm2022/bidi.mjs
var Dir = class _Dir {
  _isInitialized = false;
  _rawDir = "";
  change = new EventEmitter();
  get dir() {
    return this.valueSignal();
  }
  set dir(value) {
    const previousValue = this.valueSignal();
    this.valueSignal.set(_resolveDirectionality(value));
    this._rawDir = value;
    if (previousValue !== this.valueSignal() && this._isInitialized) {
      this.change.emit(this.valueSignal());
    }
  }
  get value() {
    return this.dir;
  }
  valueSignal = signal("ltr", ...ngDevMode ? [{
    debugName: "valueSignal"
  }] : []);
  ngAfterContentInit() {
    this._isInitialized = true;
  }
  ngOnDestroy() {
    this.change.complete();
  }
  static ɵfac = function Dir_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _Dir)();
  };
  static ɵdir = ɵɵdefineDirective({
    type: _Dir,
    selectors: [["", "dir", ""]],
    hostVars: 1,
    hostBindings: function Dir_HostBindings(rf, ctx) {
      if (rf & 2) {
        ɵɵattribute("dir", ctx._rawDir);
      }
    },
    inputs: {
      dir: "dir"
    },
    outputs: {
      change: "dirChange"
    },
    exportAs: ["dir"],
    features: [ɵɵProvidersFeature([{
      provide: Directionality,
      useExisting: _Dir
    }])]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(Dir, [{
    type: Directive,
    args: [{
      selector: "[dir]",
      providers: [{
        provide: Directionality,
        useExisting: Dir
      }],
      host: {
        "[attr.dir]": "_rawDir"
      },
      exportAs: "dir"
    }]
  }], null, {
    change: [{
      type: Output,
      args: ["dirChange"]
    }],
    dir: [{
      type: Input
    }]
  });
})();
var BidiModule = class _BidiModule {
  static ɵfac = function BidiModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _BidiModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _BidiModule,
    imports: [Dir],
    exports: [Dir]
  });
  static ɵinj = ɵɵdefineInjector({});
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(BidiModule, [{
    type: NgModule,
    args: [{
      imports: [Dir],
      exports: [Dir]
    }]
  }], null, null);
})();

// node_modules/@angular/cdk/fesm2022/scrolling.mjs
var _c0 = ["contentWrapper"];
var _c1 = ["*"];
var VIRTUAL_SCROLL_STRATEGY = new InjectionToken("VIRTUAL_SCROLL_STRATEGY");
var FixedSizeVirtualScrollStrategy = class {
  _scrolledIndexChange = new Subject();
  scrolledIndexChange = this._scrolledIndexChange.pipe(distinctUntilChanged());
  _viewport = null;
  _itemSize;
  _minBufferPx;
  _maxBufferPx;
  constructor(itemSize, minBufferPx, maxBufferPx) {
    this._itemSize = itemSize;
    this._minBufferPx = minBufferPx;
    this._maxBufferPx = maxBufferPx;
  }
  attach(viewport) {
    this._viewport = viewport;
    this._updateTotalContentSize();
    this._updateRenderedRange();
  }
  detach() {
    this._scrolledIndexChange.complete();
    this._viewport = null;
  }
  updateItemAndBufferSize(itemSize, minBufferPx, maxBufferPx) {
    if (maxBufferPx < minBufferPx && (typeof ngDevMode === "undefined" || ngDevMode)) {
      throw Error("CDK virtual scroll: maxBufferPx must be greater than or equal to minBufferPx");
    }
    this._itemSize = itemSize;
    this._minBufferPx = minBufferPx;
    this._maxBufferPx = maxBufferPx;
    this._updateTotalContentSize();
    this._updateRenderedRange();
  }
  onContentScrolled() {
    this._updateRenderedRange();
  }
  onDataLengthChanged() {
    this._updateTotalContentSize();
    this._updateRenderedRange();
  }
  onContentRendered() {
  }
  onRenderedOffsetChanged() {
  }
  scrollToIndex(index, behavior) {
    if (this._viewport) {
      this._viewport.scrollToOffset(index * this._itemSize, behavior);
    }
  }
  _updateTotalContentSize() {
    if (!this._viewport) {
      return;
    }
    this._viewport.setTotalContentSize(this._viewport.getDataLength() * this._itemSize);
  }
  _updateRenderedRange() {
    if (!this._viewport) {
      return;
    }
    const renderedRange = this._viewport.getRenderedRange();
    const newRange = {
      start: renderedRange.start,
      end: renderedRange.end
    };
    const viewportSize = this._viewport.getViewportSize();
    const dataLength = this._viewport.getDataLength();
    let scrollOffset = this._viewport.measureScrollOffset();
    let firstVisibleIndex = this._itemSize > 0 ? scrollOffset / this._itemSize : 0;
    if (newRange.end > dataLength) {
      const maxVisibleItems = Math.ceil(viewportSize / this._itemSize);
      const newVisibleIndex = Math.max(0, Math.min(firstVisibleIndex, dataLength - maxVisibleItems));
      if (firstVisibleIndex != newVisibleIndex) {
        firstVisibleIndex = newVisibleIndex;
        scrollOffset = newVisibleIndex * this._itemSize;
        newRange.start = Math.floor(firstVisibleIndex);
      }
      newRange.end = Math.max(0, Math.min(dataLength, newRange.start + maxVisibleItems));
    }
    const startBuffer = scrollOffset - newRange.start * this._itemSize;
    if (startBuffer < this._minBufferPx && newRange.start != 0) {
      const expandStart = Math.ceil((this._maxBufferPx - startBuffer) / this._itemSize);
      newRange.start = Math.max(0, newRange.start - expandStart);
      newRange.end = Math.min(dataLength, Math.ceil(firstVisibleIndex + (viewportSize + this._minBufferPx) / this._itemSize));
    } else {
      const endBuffer = newRange.end * this._itemSize - (scrollOffset + viewportSize);
      if (endBuffer < this._minBufferPx && newRange.end != dataLength) {
        const expandEnd = Math.ceil((this._maxBufferPx - endBuffer) / this._itemSize);
        if (expandEnd > 0) {
          newRange.end = Math.min(dataLength, newRange.end + expandEnd);
          newRange.start = Math.max(0, Math.floor(firstVisibleIndex - this._minBufferPx / this._itemSize));
        }
      }
    }
    this._viewport.setRenderedRange(newRange);
    this._viewport.setRenderedContentOffset(Math.round(this._itemSize * newRange.start));
    this._scrolledIndexChange.next(Math.floor(firstVisibleIndex));
  }
};
function _fixedSizeVirtualScrollStrategyFactory(fixedSizeDir) {
  return fixedSizeDir._scrollStrategy;
}
var CdkFixedSizeVirtualScroll = class _CdkFixedSizeVirtualScroll {
  get itemSize() {
    return this._itemSize;
  }
  set itemSize(value) {
    this._itemSize = coerceNumberProperty(value);
  }
  _itemSize = 20;
  get minBufferPx() {
    return this._minBufferPx;
  }
  set minBufferPx(value) {
    this._minBufferPx = coerceNumberProperty(value);
  }
  _minBufferPx = 100;
  get maxBufferPx() {
    return this._maxBufferPx;
  }
  set maxBufferPx(value) {
    this._maxBufferPx = coerceNumberProperty(value);
  }
  _maxBufferPx = 200;
  _scrollStrategy = new FixedSizeVirtualScrollStrategy(this.itemSize, this.minBufferPx, this.maxBufferPx);
  ngOnChanges() {
    this._scrollStrategy.updateItemAndBufferSize(this.itemSize, this.minBufferPx, this.maxBufferPx);
  }
  static ɵfac = function CdkFixedSizeVirtualScroll_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _CdkFixedSizeVirtualScroll)();
  };
  static ɵdir = ɵɵdefineDirective({
    type: _CdkFixedSizeVirtualScroll,
    selectors: [["cdk-virtual-scroll-viewport", "itemSize", ""]],
    inputs: {
      itemSize: "itemSize",
      minBufferPx: "minBufferPx",
      maxBufferPx: "maxBufferPx"
    },
    features: [ɵɵProvidersFeature([{
      provide: VIRTUAL_SCROLL_STRATEGY,
      useFactory: _fixedSizeVirtualScrollStrategyFactory,
      deps: [forwardRef(() => _CdkFixedSizeVirtualScroll)]
    }]), ɵɵNgOnChangesFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(CdkFixedSizeVirtualScroll, [{
    type: Directive,
    args: [{
      selector: "cdk-virtual-scroll-viewport[itemSize]",
      providers: [{
        provide: VIRTUAL_SCROLL_STRATEGY,
        useFactory: _fixedSizeVirtualScrollStrategyFactory,
        deps: [forwardRef(() => CdkFixedSizeVirtualScroll)]
      }]
    }]
  }], null, {
    itemSize: [{
      type: Input
    }],
    minBufferPx: [{
      type: Input
    }],
    maxBufferPx: [{
      type: Input
    }]
  });
})();
var DEFAULT_SCROLL_TIME = 20;
var ScrollDispatcher = class _ScrollDispatcher {
  _ngZone = inject(NgZone);
  _platform = inject(Platform);
  _renderer = inject(RendererFactory2).createRenderer(null, null);
  _cleanupGlobalListener;
  constructor() {
  }
  _scrolled = new Subject();
  _scrolledCount = 0;
  scrollContainers = /* @__PURE__ */ new Map();
  register(scrollable) {
    if (!this.scrollContainers.has(scrollable)) {
      this.scrollContainers.set(scrollable, scrollable.elementScrolled().subscribe(() => this._scrolled.next(scrollable)));
    }
  }
  deregister(scrollable) {
    const scrollableReference = this.scrollContainers.get(scrollable);
    if (scrollableReference) {
      scrollableReference.unsubscribe();
      this.scrollContainers.delete(scrollable);
    }
  }
  scrolled(auditTimeInMs = DEFAULT_SCROLL_TIME) {
    if (!this._platform.isBrowser) {
      return of();
    }
    return new Observable((observer) => {
      if (!this._cleanupGlobalListener) {
        this._cleanupGlobalListener = this._ngZone.runOutsideAngular(() => this._renderer.listen("document", "scroll", () => this._scrolled.next()));
      }
      const subscription = auditTimeInMs > 0 ? this._scrolled.pipe(auditTime(auditTimeInMs)).subscribe(observer) : this._scrolled.subscribe(observer);
      this._scrolledCount++;
      return () => {
        subscription.unsubscribe();
        this._scrolledCount--;
        if (!this._scrolledCount) {
          this._cleanupGlobalListener?.();
          this._cleanupGlobalListener = void 0;
        }
      };
    });
  }
  ngOnDestroy() {
    this._cleanupGlobalListener?.();
    this._cleanupGlobalListener = void 0;
    this.scrollContainers.forEach((_, container) => this.deregister(container));
    this._scrolled.complete();
  }
  ancestorScrolled(elementOrElementRef, auditTimeInMs) {
    const ancestors = this.getAncestorScrollContainers(elementOrElementRef);
    return this.scrolled(auditTimeInMs).pipe(filter((target) => !target || ancestors.indexOf(target) > -1));
  }
  getAncestorScrollContainers(elementOrElementRef) {
    const scrollingContainers = [];
    this.scrollContainers.forEach((_subscription, scrollable) => {
      if (this._scrollableContainsElement(scrollable, elementOrElementRef)) {
        scrollingContainers.push(scrollable);
      }
    });
    return scrollingContainers;
  }
  _scrollableContainsElement(scrollable, elementOrElementRef) {
    let element = coerceElement(elementOrElementRef);
    let scrollableElement = scrollable.getElementRef().nativeElement;
    do {
      if (element == scrollableElement) {
        return true;
      }
    } while (element = element.parentElement);
    return false;
  }
  static ɵfac = function ScrollDispatcher_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _ScrollDispatcher)();
  };
  static ɵprov = ɵɵdefineInjectable({
    token: _ScrollDispatcher,
    factory: _ScrollDispatcher.ɵfac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ScrollDispatcher, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [], null);
})();
var CdkScrollable = class _CdkScrollable {
  elementRef = inject(ElementRef);
  scrollDispatcher = inject(ScrollDispatcher);
  ngZone = inject(NgZone);
  dir = inject(Directionality, {
    optional: true
  });
  _scrollElement = this.elementRef.nativeElement;
  _destroyed = new Subject();
  _renderer = inject(Renderer2);
  _cleanupScroll;
  _elementScrolled = new Subject();
  constructor() {
  }
  ngOnInit() {
    this._cleanupScroll = this.ngZone.runOutsideAngular(() => this._renderer.listen(this._scrollElement, "scroll", (event) => this._elementScrolled.next(event)));
    this.scrollDispatcher.register(this);
  }
  ngOnDestroy() {
    this._cleanupScroll?.();
    this._elementScrolled.complete();
    this.scrollDispatcher.deregister(this);
    this._destroyed.next();
    this._destroyed.complete();
  }
  elementScrolled() {
    return this._elementScrolled;
  }
  getElementRef() {
    return this.elementRef;
  }
  scrollTo(options) {
    const el = this.elementRef.nativeElement;
    const isRtl = this.dir && this.dir.value == "rtl";
    if (options.left == null) {
      options.left = isRtl ? options.end : options.start;
    }
    if (options.right == null) {
      options.right = isRtl ? options.start : options.end;
    }
    if (options.bottom != null) {
      options.top = el.scrollHeight - el.clientHeight - options.bottom;
    }
    if (isRtl && getRtlScrollAxisType() != RtlScrollAxisType.NORMAL) {
      if (options.left != null) {
        options.right = el.scrollWidth - el.clientWidth - options.left;
      }
      if (getRtlScrollAxisType() == RtlScrollAxisType.INVERTED) {
        options.left = options.right;
      } else if (getRtlScrollAxisType() == RtlScrollAxisType.NEGATED) {
        options.left = options.right ? -options.right : options.right;
      }
    } else {
      if (options.right != null) {
        options.left = el.scrollWidth - el.clientWidth - options.right;
      }
    }
    this._applyScrollToOptions(options);
  }
  _applyScrollToOptions(options) {
    const el = this.elementRef.nativeElement;
    if (supportsScrollBehavior()) {
      el.scrollTo(options);
    } else {
      if (options.top != null) {
        el.scrollTop = options.top;
      }
      if (options.left != null) {
        el.scrollLeft = options.left;
      }
    }
  }
  measureScrollOffset(from) {
    const LEFT = "left";
    const RIGHT = "right";
    const el = this.elementRef.nativeElement;
    if (from == "top") {
      return el.scrollTop;
    }
    if (from == "bottom") {
      return el.scrollHeight - el.clientHeight - el.scrollTop;
    }
    const isRtl = this.dir && this.dir.value == "rtl";
    if (from == "start") {
      from = isRtl ? RIGHT : LEFT;
    } else if (from == "end") {
      from = isRtl ? LEFT : RIGHT;
    }
    if (isRtl && getRtlScrollAxisType() == RtlScrollAxisType.INVERTED) {
      if (from == LEFT) {
        return el.scrollWidth - el.clientWidth - el.scrollLeft;
      } else {
        return el.scrollLeft;
      }
    } else if (isRtl && getRtlScrollAxisType() == RtlScrollAxisType.NEGATED) {
      if (from == LEFT) {
        return el.scrollLeft + el.scrollWidth - el.clientWidth;
      } else {
        return -el.scrollLeft;
      }
    } else {
      if (from == LEFT) {
        return el.scrollLeft;
      } else {
        return el.scrollWidth - el.clientWidth - el.scrollLeft;
      }
    }
  }
  static ɵfac = function CdkScrollable_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _CdkScrollable)();
  };
  static ɵdir = ɵɵdefineDirective({
    type: _CdkScrollable,
    selectors: [["", "cdk-scrollable", ""], ["", "cdkScrollable", ""]]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(CdkScrollable, [{
    type: Directive,
    args: [{
      selector: "[cdk-scrollable], [cdkScrollable]"
    }]
  }], () => [], null);
})();
var DEFAULT_RESIZE_TIME = 20;
var ViewportRuler = class _ViewportRuler {
  _platform = inject(Platform);
  _listeners;
  _viewportSize = null;
  _change = new Subject();
  _document = inject(DOCUMENT);
  constructor() {
    const ngZone = inject(NgZone);
    const renderer = inject(RendererFactory2).createRenderer(null, null);
    ngZone.runOutsideAngular(() => {
      if (this._platform.isBrowser) {
        const changeListener = (event) => this._change.next(event);
        this._listeners = [renderer.listen("window", "resize", changeListener), renderer.listen("window", "orientationchange", changeListener)];
      }
      this.change().subscribe(() => this._viewportSize = null);
    });
  }
  ngOnDestroy() {
    this._listeners?.forEach((cleanup) => cleanup());
    this._change.complete();
  }
  getViewportSize() {
    if (!this._viewportSize) {
      this._updateViewportSize();
    }
    const output2 = {
      width: this._viewportSize.width,
      height: this._viewportSize.height
    };
    if (!this._platform.isBrowser) {
      this._viewportSize = null;
    }
    return output2;
  }
  getViewportRect() {
    const scrollPosition = this.getViewportScrollPosition();
    const {
      width,
      height
    } = this.getViewportSize();
    return {
      top: scrollPosition.top,
      left: scrollPosition.left,
      bottom: scrollPosition.top + height,
      right: scrollPosition.left + width,
      height,
      width
    };
  }
  getViewportScrollPosition() {
    if (!this._platform.isBrowser) {
      return {
        top: 0,
        left: 0
      };
    }
    const document2 = this._document;
    const window2 = this._getWindow();
    const documentElement = document2.documentElement;
    const documentRect = documentElement.getBoundingClientRect();
    const top = -documentRect.top || document2.body?.scrollTop || window2.scrollY || documentElement.scrollTop || 0;
    const left = -documentRect.left || document2.body?.scrollLeft || window2.scrollX || documentElement.scrollLeft || 0;
    return {
      top,
      left
    };
  }
  change(throttleTime = DEFAULT_RESIZE_TIME) {
    return throttleTime > 0 ? this._change.pipe(auditTime(throttleTime)) : this._change;
  }
  _getWindow() {
    return this._document.defaultView || window;
  }
  _updateViewportSize() {
    const window2 = this._getWindow();
    this._viewportSize = this._platform.isBrowser ? {
      width: window2.innerWidth,
      height: window2.innerHeight
    } : {
      width: 0,
      height: 0
    };
  }
  static ɵfac = function ViewportRuler_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _ViewportRuler)();
  };
  static ɵprov = ɵɵdefineInjectable({
    token: _ViewportRuler,
    factory: _ViewportRuler.ɵfac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ViewportRuler, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [], null);
})();
var VIRTUAL_SCROLLABLE = new InjectionToken("VIRTUAL_SCROLLABLE");
var CdkVirtualScrollable = class _CdkVirtualScrollable extends CdkScrollable {
  constructor() {
    super();
  }
  measureViewportSize(orientation) {
    const viewportEl = this.elementRef.nativeElement;
    return orientation === "horizontal" ? viewportEl.clientWidth : viewportEl.clientHeight;
  }
  static ɵfac = function CdkVirtualScrollable_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _CdkVirtualScrollable)();
  };
  static ɵdir = ɵɵdefineDirective({
    type: _CdkVirtualScrollable,
    features: [ɵɵInheritDefinitionFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(CdkVirtualScrollable, [{
    type: Directive
  }], () => [], null);
})();
function rangesEqual(r1, r2) {
  return r1.start == r2.start && r1.end == r2.end;
}
var SCROLL_SCHEDULER = typeof requestAnimationFrame !== "undefined" ? animationFrameScheduler : asapScheduler;
var CDK_VIRTUAL_SCROLL_VIEWPORT = new InjectionToken("CDK_VIRTUAL_SCROLL_VIEWPORT");
var CdkVirtualScrollViewport = class _CdkVirtualScrollViewport extends CdkVirtualScrollable {
  elementRef = inject(ElementRef);
  _changeDetectorRef = inject(ChangeDetectorRef);
  _scrollStrategy = inject(VIRTUAL_SCROLL_STRATEGY, {
    optional: true
  });
  scrollable = inject(VIRTUAL_SCROLLABLE, {
    optional: true
  });
  _platform = inject(Platform);
  _detachedSubject = new Subject();
  _renderedRangeSubject = new Subject();
  _renderedContentOffsetSubject = new Subject();
  get orientation() {
    return this._orientation;
  }
  set orientation(orientation) {
    if (this._orientation !== orientation) {
      this._orientation = orientation;
      this._calculateSpacerSize();
    }
  }
  _orientation = "vertical";
  appendOnly = false;
  scrolledIndexChange = new Observable((observer) => this._scrollStrategy.scrolledIndexChange.subscribe((index) => Promise.resolve().then(() => this.ngZone.run(() => observer.next(index)))));
  _contentWrapper;
  renderedRangeStream = this._renderedRangeSubject;
  renderedContentOffset = this._renderedContentOffsetSubject.pipe(filter((offset) => offset !== null), distinctUntilChanged());
  _totalContentSize = 0;
  _totalContentWidth = signal("", ...ngDevMode ? [{
    debugName: "_totalContentWidth"
  }] : []);
  _totalContentHeight = signal("", ...ngDevMode ? [{
    debugName: "_totalContentHeight"
  }] : []);
  _renderedContentTransform;
  _renderedRange = {
    start: 0,
    end: 0
  };
  _dataLength = 0;
  _viewportSize = 0;
  _forOf = null;
  _renderedContentOffset = 0;
  _renderedContentOffsetNeedsRewrite = false;
  _changeDetectionNeeded = signal(false, ...ngDevMode ? [{
    debugName: "_changeDetectionNeeded"
  }] : []);
  _runAfterChangeDetection = [];
  _viewportChanges = Subscription.EMPTY;
  _injector = inject(Injector);
  _isDestroyed = false;
  constructor() {
    super();
    const viewportRuler = inject(ViewportRuler);
    if (!this._scrollStrategy && (typeof ngDevMode === "undefined" || ngDevMode)) {
      throw Error('Error: cdk-virtual-scroll-viewport requires the "itemSize" property to be set.');
    }
    this._viewportChanges = viewportRuler.change().subscribe(() => {
      this.checkViewportSize();
    });
    if (!this.scrollable) {
      this.elementRef.nativeElement.classList.add("cdk-virtual-scrollable");
      this.scrollable = this;
    }
    const ref = effect(() => {
      if (this._changeDetectionNeeded()) {
        this._doChangeDetection();
      }
    }, __spreadProps(__spreadValues({}, ngDevMode ? {
      debugName: "ref"
    } : {}), {
      injector: inject(ApplicationRef).injector
    }));
    inject(DestroyRef).onDestroy(() => void ref.destroy());
  }
  ngOnInit() {
    if (!this._platform.isBrowser) {
      return;
    }
    if (this.scrollable === this) {
      super.ngOnInit();
    }
    this.ngZone.runOutsideAngular(() => Promise.resolve().then(() => {
      this._measureViewportSize();
      this._scrollStrategy.attach(this);
      this.scrollable.elementScrolled().pipe(startWith(null), auditTime(0, SCROLL_SCHEDULER), takeUntil(this._destroyed)).subscribe(() => this._scrollStrategy.onContentScrolled());
      this._markChangeDetectionNeeded();
    }));
  }
  ngOnDestroy() {
    this.detach();
    this._scrollStrategy.detach();
    this._renderedRangeSubject.complete();
    this._detachedSubject.complete();
    this._viewportChanges.unsubscribe();
    this._isDestroyed = true;
    super.ngOnDestroy();
  }
  attach(forOf) {
    if (this._forOf && (typeof ngDevMode === "undefined" || ngDevMode)) {
      throw Error("CdkVirtualScrollViewport is already attached.");
    }
    this.ngZone.runOutsideAngular(() => {
      this._forOf = forOf;
      this._forOf.dataStream.pipe(takeUntil(this._detachedSubject)).subscribe((data) => {
        const newLength = data.length;
        if (newLength !== this._dataLength) {
          this._dataLength = newLength;
          this._scrollStrategy.onDataLengthChanged();
        }
        this._doChangeDetection();
      });
    });
  }
  detach() {
    this._forOf = null;
    this._detachedSubject.next();
  }
  getDataLength() {
    return this._dataLength;
  }
  getViewportSize() {
    return this._viewportSize;
  }
  getRenderedRange() {
    return this._renderedRange;
  }
  measureBoundingClientRectWithScrollOffset(from) {
    return this.getElementRef().nativeElement.getBoundingClientRect()[from];
  }
  setTotalContentSize(size) {
    if (this._totalContentSize !== size) {
      this._totalContentSize = size;
      this._calculateSpacerSize();
      this._markChangeDetectionNeeded();
    }
  }
  setRenderedRange(range) {
    if (!rangesEqual(this._renderedRange, range)) {
      if (this.appendOnly) {
        range = {
          start: 0,
          end: Math.max(this._renderedRange.end, range.end)
        };
      }
      this._renderedRangeSubject.next(this._renderedRange = range);
      this._markChangeDetectionNeeded(() => this._scrollStrategy.onContentRendered());
    }
  }
  getOffsetToRenderedContentStart() {
    return this._renderedContentOffsetNeedsRewrite ? null : this._renderedContentOffset;
  }
  setRenderedContentOffset(offset, to = "to-start") {
    offset = this.appendOnly && to === "to-start" ? 0 : offset;
    const isRtl = this.dir && this.dir.value == "rtl";
    const isHorizontal = this.orientation == "horizontal";
    const axis = isHorizontal ? "X" : "Y";
    const axisDirection = isHorizontal && isRtl ? -1 : 1;
    let transform = `translate${axis}(${Number(axisDirection * offset)}px)`;
    this._renderedContentOffset = offset;
    if (to === "to-end") {
      transform += ` translate${axis}(-100%)`;
      this._renderedContentOffsetNeedsRewrite = true;
    }
    if (this._renderedContentTransform != transform) {
      this._renderedContentTransform = transform;
      this._markChangeDetectionNeeded(() => {
        if (this._renderedContentOffsetNeedsRewrite) {
          this._renderedContentOffset -= this.measureRenderedContentSize();
          this._renderedContentOffsetNeedsRewrite = false;
          this.setRenderedContentOffset(this._renderedContentOffset);
        } else {
          this._scrollStrategy.onRenderedOffsetChanged();
        }
      });
    }
  }
  scrollToOffset(offset, behavior = "auto") {
    const options = {
      behavior
    };
    if (this.orientation === "horizontal") {
      options.start = offset;
    } else {
      options.top = offset;
    }
    this.scrollable.scrollTo(options);
  }
  scrollToIndex(index, behavior = "auto") {
    this._scrollStrategy.scrollToIndex(index, behavior);
  }
  measureScrollOffset(from) {
    let measureScrollOffset;
    if (this.scrollable == this) {
      measureScrollOffset = (_from) => super.measureScrollOffset(_from);
    } else {
      measureScrollOffset = (_from) => this.scrollable.measureScrollOffset(_from);
    }
    return Math.max(0, measureScrollOffset(from ?? (this.orientation === "horizontal" ? "start" : "top")) - this.measureViewportOffset());
  }
  measureViewportOffset(from) {
    let fromRect;
    const LEFT = "left";
    const RIGHT = "right";
    const isRtl = this.dir?.value == "rtl";
    if (from == "start") {
      fromRect = isRtl ? RIGHT : LEFT;
    } else if (from == "end") {
      fromRect = isRtl ? LEFT : RIGHT;
    } else if (from) {
      fromRect = from;
    } else {
      fromRect = this.orientation === "horizontal" ? "left" : "top";
    }
    const scrollerClientRect = this.scrollable.measureBoundingClientRectWithScrollOffset(fromRect);
    const viewportClientRect = this.elementRef.nativeElement.getBoundingClientRect()[fromRect];
    return viewportClientRect - scrollerClientRect;
  }
  measureRenderedContentSize() {
    const contentEl = this._contentWrapper.nativeElement;
    return this.orientation === "horizontal" ? contentEl.offsetWidth : contentEl.offsetHeight;
  }
  measureRangeSize(range) {
    if (!this._forOf) {
      return 0;
    }
    return this._forOf.measureRangeSize(range, this.orientation);
  }
  checkViewportSize() {
    this._measureViewportSize();
    this._scrollStrategy.onDataLengthChanged();
  }
  _measureViewportSize() {
    this._viewportSize = this.scrollable.measureViewportSize(this.orientation);
  }
  _markChangeDetectionNeeded(runAfter) {
    if (runAfter) {
      this._runAfterChangeDetection.push(runAfter);
    }
    if (untracked(this._changeDetectionNeeded)) {
      return;
    }
    this.ngZone.runOutsideAngular(() => {
      Promise.resolve().then(() => {
        this.ngZone.run(() => {
          this._changeDetectionNeeded.set(true);
        });
      });
    });
  }
  _doChangeDetection() {
    if (this._isDestroyed) {
      return;
    }
    this.ngZone.run(() => {
      this._changeDetectorRef.markForCheck();
      this._contentWrapper.nativeElement.style.transform = this._renderedContentTransform;
      this._renderedContentOffsetSubject.next(this.getOffsetToRenderedContentStart());
      afterNextRender(() => {
        this._changeDetectionNeeded.set(false);
        const runAfterChangeDetection = this._runAfterChangeDetection;
        this._runAfterChangeDetection = [];
        for (const fn of runAfterChangeDetection) {
          fn();
        }
      }, {
        injector: this._injector
      });
    });
  }
  _calculateSpacerSize() {
    this._totalContentHeight.set(this.orientation === "horizontal" ? "" : `${this._totalContentSize}px`);
    this._totalContentWidth.set(this.orientation === "horizontal" ? `${this._totalContentSize}px` : "");
  }
  static ɵfac = function CdkVirtualScrollViewport_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _CdkVirtualScrollViewport)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _CdkVirtualScrollViewport,
    selectors: [["cdk-virtual-scroll-viewport"]],
    viewQuery: function CdkVirtualScrollViewport_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c0, 7);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx._contentWrapper = _t.first);
      }
    },
    hostAttrs: [1, "cdk-virtual-scroll-viewport"],
    hostVars: 4,
    hostBindings: function CdkVirtualScrollViewport_HostBindings(rf, ctx) {
      if (rf & 2) {
        ɵɵclassProp("cdk-virtual-scroll-orientation-horizontal", ctx.orientation === "horizontal")("cdk-virtual-scroll-orientation-vertical", ctx.orientation !== "horizontal");
      }
    },
    inputs: {
      orientation: "orientation",
      appendOnly: [2, "appendOnly", "appendOnly", booleanAttribute]
    },
    outputs: {
      scrolledIndexChange: "scrolledIndexChange"
    },
    features: [ɵɵProvidersFeature([{
      provide: CdkScrollable,
      useFactory: () => inject(VIRTUAL_SCROLLABLE, {
        optional: true
      }) || inject(_CdkVirtualScrollViewport)
    }, {
      provide: CDK_VIRTUAL_SCROLL_VIEWPORT,
      useExisting: _CdkVirtualScrollViewport
    }]), ɵɵInheritDefinitionFeature],
    ngContentSelectors: _c1,
    decls: 4,
    vars: 4,
    consts: [["contentWrapper", ""], [1, "cdk-virtual-scroll-content-wrapper"], [1, "cdk-virtual-scroll-spacer"]],
    template: function CdkVirtualScrollViewport_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵprojectionDef();
        ɵɵdomElementStart(0, "div", 1, 0);
        ɵɵprojection(2);
        ɵɵdomElementEnd();
        ɵɵdomElement(3, "div", 2);
      }
      if (rf & 2) {
        ɵɵadvance(3);
        ɵɵstyleProp("width", ctx._totalContentWidth())("height", ctx._totalContentHeight());
      }
    },
    styles: ["cdk-virtual-scroll-viewport {\n  display: block;\n  position: relative;\n  transform: translateZ(0);\n}\n\n.cdk-virtual-scrollable {\n  overflow: auto;\n  will-change: scroll-position;\n  contain: strict;\n}\n\n.cdk-virtual-scroll-content-wrapper {\n  position: absolute;\n  top: 0;\n  left: 0;\n  contain: content;\n}\n[dir=rtl] .cdk-virtual-scroll-content-wrapper {\n  right: 0;\n  left: auto;\n}\n\n.cdk-virtual-scroll-orientation-horizontal .cdk-virtual-scroll-content-wrapper {\n  min-height: 100%;\n}\n.cdk-virtual-scroll-orientation-horizontal .cdk-virtual-scroll-content-wrapper > dl:not([cdkVirtualFor]), .cdk-virtual-scroll-orientation-horizontal .cdk-virtual-scroll-content-wrapper > ol:not([cdkVirtualFor]), .cdk-virtual-scroll-orientation-horizontal .cdk-virtual-scroll-content-wrapper > table:not([cdkVirtualFor]), .cdk-virtual-scroll-orientation-horizontal .cdk-virtual-scroll-content-wrapper > ul:not([cdkVirtualFor]) {\n  padding-left: 0;\n  padding-right: 0;\n  margin-left: 0;\n  margin-right: 0;\n  border-left-width: 0;\n  border-right-width: 0;\n  outline: none;\n}\n\n.cdk-virtual-scroll-orientation-vertical .cdk-virtual-scroll-content-wrapper {\n  min-width: 100%;\n}\n.cdk-virtual-scroll-orientation-vertical .cdk-virtual-scroll-content-wrapper > dl:not([cdkVirtualFor]), .cdk-virtual-scroll-orientation-vertical .cdk-virtual-scroll-content-wrapper > ol:not([cdkVirtualFor]), .cdk-virtual-scroll-orientation-vertical .cdk-virtual-scroll-content-wrapper > table:not([cdkVirtualFor]), .cdk-virtual-scroll-orientation-vertical .cdk-virtual-scroll-content-wrapper > ul:not([cdkVirtualFor]) {\n  padding-top: 0;\n  padding-bottom: 0;\n  margin-top: 0;\n  margin-bottom: 0;\n  border-top-width: 0;\n  border-bottom-width: 0;\n  outline: none;\n}\n\n.cdk-virtual-scroll-spacer {\n  height: 1px;\n  transform-origin: 0 0;\n  flex: 0 0 auto;\n}\n[dir=rtl] .cdk-virtual-scroll-spacer {\n  transform-origin: 100% 0;\n}\n"],
    encapsulation: 2,
    changeDetection: 0
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(CdkVirtualScrollViewport, [{
    type: Component,
    args: [{
      selector: "cdk-virtual-scroll-viewport",
      host: {
        "class": "cdk-virtual-scroll-viewport",
        "[class.cdk-virtual-scroll-orientation-horizontal]": 'orientation === "horizontal"',
        "[class.cdk-virtual-scroll-orientation-vertical]": 'orientation !== "horizontal"'
      },
      encapsulation: ViewEncapsulation.None,
      changeDetection: ChangeDetectionStrategy.OnPush,
      providers: [{
        provide: CdkScrollable,
        useFactory: () => inject(VIRTUAL_SCROLLABLE, {
          optional: true
        }) || inject(CdkVirtualScrollViewport)
      }, {
        provide: CDK_VIRTUAL_SCROLL_VIEWPORT,
        useExisting: CdkVirtualScrollViewport
      }],
      template: '<!--\n  Wrap the rendered content in an element that will be used to offset it based on the scroll\n  position.\n-->\n<div #contentWrapper class="cdk-virtual-scroll-content-wrapper">\n  <ng-content></ng-content>\n</div>\n<!--\n  Spacer used to force the scrolling container to the correct size for the *total* number of items\n  so that the scrollbar captures the size of the entire data set.\n-->\n<div class="cdk-virtual-scroll-spacer"\n     [style.width]="_totalContentWidth()" [style.height]="_totalContentHeight()"></div>\n',
      styles: ["cdk-virtual-scroll-viewport {\n  display: block;\n  position: relative;\n  transform: translateZ(0);\n}\n\n.cdk-virtual-scrollable {\n  overflow: auto;\n  will-change: scroll-position;\n  contain: strict;\n}\n\n.cdk-virtual-scroll-content-wrapper {\n  position: absolute;\n  top: 0;\n  left: 0;\n  contain: content;\n}\n[dir=rtl] .cdk-virtual-scroll-content-wrapper {\n  right: 0;\n  left: auto;\n}\n\n.cdk-virtual-scroll-orientation-horizontal .cdk-virtual-scroll-content-wrapper {\n  min-height: 100%;\n}\n.cdk-virtual-scroll-orientation-horizontal .cdk-virtual-scroll-content-wrapper > dl:not([cdkVirtualFor]), .cdk-virtual-scroll-orientation-horizontal .cdk-virtual-scroll-content-wrapper > ol:not([cdkVirtualFor]), .cdk-virtual-scroll-orientation-horizontal .cdk-virtual-scroll-content-wrapper > table:not([cdkVirtualFor]), .cdk-virtual-scroll-orientation-horizontal .cdk-virtual-scroll-content-wrapper > ul:not([cdkVirtualFor]) {\n  padding-left: 0;\n  padding-right: 0;\n  margin-left: 0;\n  margin-right: 0;\n  border-left-width: 0;\n  border-right-width: 0;\n  outline: none;\n}\n\n.cdk-virtual-scroll-orientation-vertical .cdk-virtual-scroll-content-wrapper {\n  min-width: 100%;\n}\n.cdk-virtual-scroll-orientation-vertical .cdk-virtual-scroll-content-wrapper > dl:not([cdkVirtualFor]), .cdk-virtual-scroll-orientation-vertical .cdk-virtual-scroll-content-wrapper > ol:not([cdkVirtualFor]), .cdk-virtual-scroll-orientation-vertical .cdk-virtual-scroll-content-wrapper > table:not([cdkVirtualFor]), .cdk-virtual-scroll-orientation-vertical .cdk-virtual-scroll-content-wrapper > ul:not([cdkVirtualFor]) {\n  padding-top: 0;\n  padding-bottom: 0;\n  margin-top: 0;\n  margin-bottom: 0;\n  border-top-width: 0;\n  border-bottom-width: 0;\n  outline: none;\n}\n\n.cdk-virtual-scroll-spacer {\n  height: 1px;\n  transform-origin: 0 0;\n  flex: 0 0 auto;\n}\n[dir=rtl] .cdk-virtual-scroll-spacer {\n  transform-origin: 100% 0;\n}\n"]
    }]
  }], () => [], {
    orientation: [{
      type: Input
    }],
    appendOnly: [{
      type: Input,
      args: [{
        transform: booleanAttribute
      }]
    }],
    scrolledIndexChange: [{
      type: Output
    }],
    _contentWrapper: [{
      type: ViewChild,
      args: ["contentWrapper", {
        static: true
      }]
    }]
  });
})();
function getOffset(orientation, direction, node) {
  const el = node;
  if (!el.getBoundingClientRect) {
    return 0;
  }
  const rect = el.getBoundingClientRect();
  if (orientation === "horizontal") {
    return direction === "start" ? rect.left : rect.right;
  }
  return direction === "start" ? rect.top : rect.bottom;
}
var CdkVirtualForOf = class _CdkVirtualForOf {
  _viewContainerRef = inject(ViewContainerRef);
  _template = inject(TemplateRef);
  _differs = inject(IterableDiffers);
  _viewRepeater = new _RecycleViewRepeaterStrategy();
  _viewport = inject(CDK_VIRTUAL_SCROLL_VIEWPORT, {
    skipSelf: true
  });
  viewChange = new Subject();
  _dataSourceChanges = new Subject();
  get cdkVirtualForOf() {
    return this._cdkVirtualForOf;
  }
  set cdkVirtualForOf(value) {
    this._cdkVirtualForOf = value;
    if (isDataSource(value)) {
      this._dataSourceChanges.next(value);
    } else {
      this._dataSourceChanges.next(new ArrayDataSource(isObservable(value) ? value : Array.from(value || [])));
    }
  }
  _cdkVirtualForOf;
  get cdkVirtualForTrackBy() {
    return this._cdkVirtualForTrackBy;
  }
  set cdkVirtualForTrackBy(fn) {
    this._needsUpdate = true;
    this._cdkVirtualForTrackBy = fn ? (index, item) => fn(index + (this._renderedRange ? this._renderedRange.start : 0), item) : void 0;
  }
  _cdkVirtualForTrackBy;
  set cdkVirtualForTemplate(value) {
    if (value) {
      this._needsUpdate = true;
      this._template = value;
    }
  }
  get cdkVirtualForTemplateCacheSize() {
    return this._viewRepeater.viewCacheSize;
  }
  set cdkVirtualForTemplateCacheSize(size) {
    this._viewRepeater.viewCacheSize = coerceNumberProperty(size);
  }
  dataStream = this._dataSourceChanges.pipe(startWith(null), pairwise(), switchMap(([prev, cur]) => this._changeDataSource(prev, cur)), shareReplay(1));
  _differ = null;
  _data = [];
  _renderedItems = [];
  _renderedRange = {
    start: 0,
    end: 0
  };
  _needsUpdate = false;
  _destroyed = new Subject();
  constructor() {
    const ngZone = inject(NgZone);
    this.dataStream.subscribe((data) => {
      this._data = data;
      this._onRenderedDataChange();
    });
    this._viewport.renderedRangeStream.pipe(takeUntil(this._destroyed)).subscribe((range) => {
      this._renderedRange = range;
      if (this.viewChange.observers.length) {
        ngZone.run(() => this.viewChange.next(this._renderedRange));
      }
      this._onRenderedDataChange();
    });
    this._viewport.attach(this);
  }
  measureRangeSize(range, orientation) {
    if (range.start >= range.end) {
      return 0;
    }
    if ((range.start < this._renderedRange.start || range.end > this._renderedRange.end) && (typeof ngDevMode === "undefined" || ngDevMode)) {
      throw Error(`Error: attempted to measure an item that isn't rendered.`);
    }
    const renderedStartIndex = range.start - this._renderedRange.start;
    const rangeLen = range.end - range.start;
    let firstNode;
    let lastNode;
    for (let i2 = 0; i2 < rangeLen; i2++) {
      const view = this._viewContainerRef.get(i2 + renderedStartIndex);
      if (view && view.rootNodes.length) {
        firstNode = lastNode = view.rootNodes[0];
        break;
      }
    }
    for (let i2 = rangeLen - 1; i2 > -1; i2--) {
      const view = this._viewContainerRef.get(i2 + renderedStartIndex);
      if (view && view.rootNodes.length) {
        lastNode = view.rootNodes[view.rootNodes.length - 1];
        break;
      }
    }
    return firstNode && lastNode ? getOffset(orientation, "end", lastNode) - getOffset(orientation, "start", firstNode) : 0;
  }
  ngDoCheck() {
    if (this._differ && this._needsUpdate) {
      const changes = this._differ.diff(this._renderedItems);
      if (!changes) {
        this._updateContext();
      } else {
        this._applyChanges(changes);
      }
      this._needsUpdate = false;
    }
  }
  ngOnDestroy() {
    this._viewport.detach();
    this._dataSourceChanges.next(void 0);
    this._dataSourceChanges.complete();
    this.viewChange.complete();
    this._destroyed.next();
    this._destroyed.complete();
    this._viewRepeater.detach();
  }
  _onRenderedDataChange() {
    if (!this._renderedRange) {
      return;
    }
    this._renderedItems = this._data.slice(this._renderedRange.start, this._renderedRange.end);
    if (!this._differ) {
      this._differ = this._differs.find(this._renderedItems).create((index, item) => {
        return this.cdkVirtualForTrackBy ? this.cdkVirtualForTrackBy(index, item) : item;
      });
    }
    this._needsUpdate = true;
  }
  _changeDataSource(oldDs, newDs) {
    if (oldDs) {
      oldDs.disconnect(this);
    }
    this._needsUpdate = true;
    return newDs ? newDs.connect(this) : of();
  }
  _updateContext() {
    const count2 = this._data.length;
    let i2 = this._viewContainerRef.length;
    while (i2--) {
      const view = this._viewContainerRef.get(i2);
      view.context.index = this._renderedRange.start + i2;
      view.context.count = count2;
      this._updateComputedContextProperties(view.context);
      view.detectChanges();
    }
  }
  _applyChanges(changes) {
    this._viewRepeater.applyChanges(changes, this._viewContainerRef, (record, _adjustedPreviousIndex, currentIndex) => this._getEmbeddedViewArgs(record, currentIndex), (record) => record.item);
    changes.forEachIdentityChange((record) => {
      const view = this._viewContainerRef.get(record.currentIndex);
      view.context.$implicit = record.item;
    });
    const count2 = this._data.length;
    let i2 = this._viewContainerRef.length;
    while (i2--) {
      const view = this._viewContainerRef.get(i2);
      view.context.index = this._renderedRange.start + i2;
      view.context.count = count2;
      this._updateComputedContextProperties(view.context);
    }
  }
  _updateComputedContextProperties(context) {
    context.first = context.index === 0;
    context.last = context.index === context.count - 1;
    context.even = context.index % 2 === 0;
    context.odd = !context.even;
  }
  _getEmbeddedViewArgs(record, index) {
    return {
      templateRef: this._template,
      context: {
        $implicit: record.item,
        cdkVirtualForOf: this._cdkVirtualForOf,
        index: -1,
        count: -1,
        first: false,
        last: false,
        odd: false,
        even: false
      },
      index
    };
  }
  static ngTemplateContextGuard(directive, context) {
    return true;
  }
  static ɵfac = function CdkVirtualForOf_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _CdkVirtualForOf)();
  };
  static ɵdir = ɵɵdefineDirective({
    type: _CdkVirtualForOf,
    selectors: [["", "cdkVirtualFor", "", "cdkVirtualForOf", ""]],
    inputs: {
      cdkVirtualForOf: "cdkVirtualForOf",
      cdkVirtualForTrackBy: "cdkVirtualForTrackBy",
      cdkVirtualForTemplate: "cdkVirtualForTemplate",
      cdkVirtualForTemplateCacheSize: "cdkVirtualForTemplateCacheSize"
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(CdkVirtualForOf, [{
    type: Directive,
    args: [{
      selector: "[cdkVirtualFor][cdkVirtualForOf]"
    }]
  }], () => [], {
    cdkVirtualForOf: [{
      type: Input
    }],
    cdkVirtualForTrackBy: [{
      type: Input
    }],
    cdkVirtualForTemplate: [{
      type: Input
    }],
    cdkVirtualForTemplateCacheSize: [{
      type: Input
    }]
  });
})();
var CdkVirtualScrollableElement = class _CdkVirtualScrollableElement extends CdkVirtualScrollable {
  constructor() {
    super();
  }
  measureBoundingClientRectWithScrollOffset(from) {
    return this.getElementRef().nativeElement.getBoundingClientRect()[from] - this.measureScrollOffset(from);
  }
  static ɵfac = function CdkVirtualScrollableElement_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _CdkVirtualScrollableElement)();
  };
  static ɵdir = ɵɵdefineDirective({
    type: _CdkVirtualScrollableElement,
    selectors: [["", "cdkVirtualScrollingElement", ""]],
    hostAttrs: [1, "cdk-virtual-scrollable"],
    features: [ɵɵProvidersFeature([{
      provide: VIRTUAL_SCROLLABLE,
      useExisting: _CdkVirtualScrollableElement
    }]), ɵɵInheritDefinitionFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(CdkVirtualScrollableElement, [{
    type: Directive,
    args: [{
      selector: "[cdkVirtualScrollingElement]",
      providers: [{
        provide: VIRTUAL_SCROLLABLE,
        useExisting: CdkVirtualScrollableElement
      }],
      host: {
        "class": "cdk-virtual-scrollable"
      }
    }]
  }], () => [], null);
})();
var CdkVirtualScrollableWindow = class _CdkVirtualScrollableWindow extends CdkVirtualScrollable {
  constructor() {
    super();
    const document2 = inject(DOCUMENT);
    this.elementRef = new ElementRef(document2.documentElement);
    this._scrollElement = document2;
  }
  measureBoundingClientRectWithScrollOffset(from) {
    return this.getElementRef().nativeElement.getBoundingClientRect()[from];
  }
  static ɵfac = function CdkVirtualScrollableWindow_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _CdkVirtualScrollableWindow)();
  };
  static ɵdir = ɵɵdefineDirective({
    type: _CdkVirtualScrollableWindow,
    selectors: [["cdk-virtual-scroll-viewport", "scrollWindow", ""]],
    features: [ɵɵProvidersFeature([{
      provide: VIRTUAL_SCROLLABLE,
      useExisting: _CdkVirtualScrollableWindow
    }]), ɵɵInheritDefinitionFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(CdkVirtualScrollableWindow, [{
    type: Directive,
    args: [{
      selector: "cdk-virtual-scroll-viewport[scrollWindow]",
      providers: [{
        provide: VIRTUAL_SCROLLABLE,
        useExisting: CdkVirtualScrollableWindow
      }]
    }]
  }], () => [], null);
})();
var CdkScrollableModule = class _CdkScrollableModule {
  static ɵfac = function CdkScrollableModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _CdkScrollableModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _CdkScrollableModule,
    imports: [CdkScrollable],
    exports: [CdkScrollable]
  });
  static ɵinj = ɵɵdefineInjector({});
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(CdkScrollableModule, [{
    type: NgModule,
    args: [{
      exports: [CdkScrollable],
      imports: [CdkScrollable]
    }]
  }], null, null);
})();
var ScrollingModule = class _ScrollingModule {
  static ɵfac = function ScrollingModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _ScrollingModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _ScrollingModule,
    imports: [BidiModule, CdkScrollableModule, CdkVirtualScrollViewport, CdkFixedSizeVirtualScroll, CdkVirtualForOf, CdkVirtualScrollableWindow, CdkVirtualScrollableElement],
    exports: [BidiModule, CdkScrollableModule, CdkFixedSizeVirtualScroll, CdkVirtualForOf, CdkVirtualScrollViewport, CdkVirtualScrollableWindow, CdkVirtualScrollableElement]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [BidiModule, CdkScrollableModule, BidiModule, CdkScrollableModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ScrollingModule, [{
    type: NgModule,
    args: [{
      imports: [BidiModule, CdkScrollableModule, CdkVirtualScrollViewport, CdkFixedSizeVirtualScroll, CdkVirtualForOf, CdkVirtualScrollableWindow, CdkVirtualScrollableElement],
      exports: [BidiModule, CdkScrollableModule, CdkFixedSizeVirtualScroll, CdkVirtualForOf, CdkVirtualScrollViewport, CdkVirtualScrollableWindow, CdkVirtualScrollableElement]
    }]
  }], null, null);
})();

// node_modules/@angular/cdk/fesm2022/portal.mjs
function throwNullPortalError() {
  throw Error("Must provide a portal to attach");
}
function throwPortalAlreadyAttachedError() {
  throw Error("Host already has a portal attached");
}
function throwPortalOutletAlreadyDisposedError() {
  throw Error("This PortalOutlet has already been disposed");
}
function throwUnknownPortalTypeError() {
  throw Error("Attempting to attach an unknown Portal type. BasePortalOutlet accepts either a ComponentPortal or a TemplatePortal.");
}
function throwNullPortalOutletError() {
  throw Error("Attempting to attach a portal to a null PortalOutlet");
}
function throwNoPortalAttachedError() {
  throw Error("Attempting to detach a portal that is not attached to a host");
}
var Portal = class {
  _attachedHost = null;
  attach(host) {
    if (typeof ngDevMode === "undefined" || ngDevMode) {
      if (host == null) {
        throwNullPortalOutletError();
      }
      if (host.hasAttached()) {
        throwPortalAlreadyAttachedError();
      }
    }
    this._attachedHost = host;
    return host.attach(this);
  }
  detach() {
    let host = this._attachedHost;
    if (host != null) {
      this._attachedHost = null;
      host.detach();
    } else if (typeof ngDevMode === "undefined" || ngDevMode) {
      throwNoPortalAttachedError();
    }
  }
  get isAttached() {
    return this._attachedHost != null;
  }
  setAttachedHost(host) {
    this._attachedHost = host;
  }
};
var ComponentPortal = class extends Portal {
  component;
  viewContainerRef;
  injector;
  projectableNodes;
  bindings;
  constructor(component, viewContainerRef, injector, projectableNodes, bindings) {
    super();
    this.component = component;
    this.viewContainerRef = viewContainerRef;
    this.injector = injector;
    this.projectableNodes = projectableNodes;
    this.bindings = bindings || null;
  }
};
var TemplatePortal = class extends Portal {
  templateRef;
  viewContainerRef;
  context;
  injector;
  constructor(templateRef, viewContainerRef, context, injector) {
    super();
    this.templateRef = templateRef;
    this.viewContainerRef = viewContainerRef;
    this.context = context;
    this.injector = injector;
  }
  get origin() {
    return this.templateRef.elementRef;
  }
  attach(host, context = this.context) {
    this.context = context;
    return super.attach(host);
  }
  detach() {
    this.context = void 0;
    return super.detach();
  }
};
var DomPortal = class extends Portal {
  element;
  constructor(element) {
    super();
    this.element = element instanceof ElementRef ? element.nativeElement : element;
  }
};
var BasePortalOutlet = class {
  _attachedPortal = null;
  _disposeFn = null;
  _isDisposed = false;
  hasAttached() {
    return !!this._attachedPortal;
  }
  attach(portal) {
    if (typeof ngDevMode === "undefined" || ngDevMode) {
      if (!portal) {
        throwNullPortalError();
      }
      if (this.hasAttached()) {
        throwPortalAlreadyAttachedError();
      }
      if (this._isDisposed) {
        throwPortalOutletAlreadyDisposedError();
      }
    }
    if (portal instanceof ComponentPortal) {
      this._attachedPortal = portal;
      return this.attachComponentPortal(portal);
    } else if (portal instanceof TemplatePortal) {
      this._attachedPortal = portal;
      return this.attachTemplatePortal(portal);
    } else if (this.attachDomPortal && portal instanceof DomPortal) {
      this._attachedPortal = portal;
      return this.attachDomPortal(portal);
    }
    if (typeof ngDevMode === "undefined" || ngDevMode) {
      throwUnknownPortalTypeError();
    }
  }
  attachDomPortal = null;
  detach() {
    if (this._attachedPortal) {
      this._attachedPortal.setAttachedHost(null);
      this._attachedPortal = null;
    }
    this._invokeDisposeFn();
  }
  dispose() {
    if (this.hasAttached()) {
      this.detach();
    }
    this._invokeDisposeFn();
    this._isDisposed = true;
  }
  setDisposeFn(fn) {
    this._disposeFn = fn;
  }
  _invokeDisposeFn() {
    if (this._disposeFn) {
      this._disposeFn();
      this._disposeFn = null;
    }
  }
};
var DomPortalOutlet = class extends BasePortalOutlet {
  outletElement;
  _appRef;
  _defaultInjector;
  constructor(outletElement, _appRef, _defaultInjector) {
    super();
    this.outletElement = outletElement;
    this._appRef = _appRef;
    this._defaultInjector = _defaultInjector;
  }
  attachComponentPortal(portal) {
    let componentRef;
    if (portal.viewContainerRef) {
      const injector = portal.injector || portal.viewContainerRef.injector;
      const ngModuleRef = injector.get(NgModuleRef$1, null, {
        optional: true
      }) || void 0;
      componentRef = portal.viewContainerRef.createComponent(portal.component, {
        index: portal.viewContainerRef.length,
        injector,
        ngModuleRef,
        projectableNodes: portal.projectableNodes || void 0,
        bindings: portal.bindings || void 0
      });
      this.setDisposeFn(() => componentRef.destroy());
    } else {
      if ((typeof ngDevMode === "undefined" || ngDevMode) && !this._appRef) {
        throw Error("Cannot attach component portal to outlet without an ApplicationRef.");
      }
      const appRef = this._appRef;
      const elementInjector = portal.injector || this._defaultInjector || Injector.NULL;
      const environmentInjector = elementInjector.get(EnvironmentInjector, appRef.injector);
      componentRef = createComponent(portal.component, {
        elementInjector,
        environmentInjector,
        projectableNodes: portal.projectableNodes || void 0,
        bindings: portal.bindings || void 0
      });
      appRef.attachView(componentRef.hostView);
      this.setDisposeFn(() => {
        if (appRef.viewCount > 0) {
          appRef.detachView(componentRef.hostView);
        }
        componentRef.destroy();
      });
    }
    this.outletElement.appendChild(this._getComponentRootNode(componentRef));
    this._attachedPortal = portal;
    return componentRef;
  }
  attachTemplatePortal(portal) {
    let viewContainer = portal.viewContainerRef;
    let viewRef = viewContainer.createEmbeddedView(portal.templateRef, portal.context, {
      injector: portal.injector
    });
    viewRef.rootNodes.forEach((rootNode) => this.outletElement.appendChild(rootNode));
    viewRef.detectChanges();
    this.setDisposeFn(() => {
      let index = viewContainer.indexOf(viewRef);
      if (index !== -1) {
        viewContainer.remove(index);
      }
    });
    this._attachedPortal = portal;
    return viewRef;
  }
  attachDomPortal = (portal) => {
    const element = portal.element;
    if (!element.parentNode && (typeof ngDevMode === "undefined" || ngDevMode)) {
      throw Error("DOM portal content must be attached to a parent node.");
    }
    const anchorNode = this.outletElement.ownerDocument.createComment("dom-portal");
    element.parentNode.insertBefore(anchorNode, element);
    this.outletElement.appendChild(element);
    this._attachedPortal = portal;
    super.setDisposeFn(() => {
      if (anchorNode.parentNode) {
        anchorNode.parentNode.replaceChild(element, anchorNode);
      }
    });
  };
  dispose() {
    super.dispose();
    this.outletElement.remove();
  }
  _getComponentRootNode(componentRef) {
    return componentRef.hostView.rootNodes[0];
  }
};
var CdkPortal = class _CdkPortal extends TemplatePortal {
  constructor() {
    const templateRef = inject(TemplateRef);
    const viewContainerRef = inject(ViewContainerRef);
    super(templateRef, viewContainerRef);
  }
  static ɵfac = function CdkPortal_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _CdkPortal)();
  };
  static ɵdir = ɵɵdefineDirective({
    type: _CdkPortal,
    selectors: [["", "cdkPortal", ""]],
    exportAs: ["cdkPortal"],
    features: [ɵɵInheritDefinitionFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(CdkPortal, [{
    type: Directive,
    args: [{
      selector: "[cdkPortal]",
      exportAs: "cdkPortal"
    }]
  }], () => [], null);
})();
var CdkPortalOutlet = class _CdkPortalOutlet extends BasePortalOutlet {
  _moduleRef = inject(NgModuleRef$1, {
    optional: true
  });
  _document = inject(DOCUMENT);
  _viewContainerRef = inject(ViewContainerRef);
  _isInitialized = false;
  _attachedRef = null;
  constructor() {
    super();
  }
  get portal() {
    return this._attachedPortal;
  }
  set portal(portal) {
    if (this.hasAttached() && !portal && !this._isInitialized) {
      return;
    }
    if (this.hasAttached()) {
      super.detach();
    }
    if (portal) {
      super.attach(portal);
    }
    this._attachedPortal = portal || null;
  }
  attached = new EventEmitter();
  get attachedRef() {
    return this._attachedRef;
  }
  ngOnInit() {
    this._isInitialized = true;
  }
  ngOnDestroy() {
    super.dispose();
    this._attachedRef = this._attachedPortal = null;
  }
  attachComponentPortal(portal) {
    portal.setAttachedHost(this);
    const viewContainerRef = portal.viewContainerRef != null ? portal.viewContainerRef : this._viewContainerRef;
    const ref = viewContainerRef.createComponent(portal.component, {
      index: viewContainerRef.length,
      injector: portal.injector || viewContainerRef.injector,
      projectableNodes: portal.projectableNodes || void 0,
      ngModuleRef: this._moduleRef || void 0,
      bindings: portal.bindings || void 0
    });
    if (viewContainerRef !== this._viewContainerRef) {
      this._getRootNode().appendChild(ref.hostView.rootNodes[0]);
    }
    super.setDisposeFn(() => ref.destroy());
    this._attachedPortal = portal;
    this._attachedRef = ref;
    this.attached.emit(ref);
    return ref;
  }
  attachTemplatePortal(portal) {
    portal.setAttachedHost(this);
    const viewRef = this._viewContainerRef.createEmbeddedView(portal.templateRef, portal.context, {
      injector: portal.injector
    });
    super.setDisposeFn(() => this._viewContainerRef.clear());
    this._attachedPortal = portal;
    this._attachedRef = viewRef;
    this.attached.emit(viewRef);
    return viewRef;
  }
  attachDomPortal = (portal) => {
    const element = portal.element;
    if (!element.parentNode && (typeof ngDevMode === "undefined" || ngDevMode)) {
      throw Error("DOM portal content must be attached to a parent node.");
    }
    const anchorNode = this._document.createComment("dom-portal");
    portal.setAttachedHost(this);
    element.parentNode.insertBefore(anchorNode, element);
    this._getRootNode().appendChild(element);
    this._attachedPortal = portal;
    super.setDisposeFn(() => {
      if (anchorNode.parentNode) {
        anchorNode.parentNode.replaceChild(element, anchorNode);
      }
    });
  };
  _getRootNode() {
    const nativeElement = this._viewContainerRef.element.nativeElement;
    return nativeElement.nodeType === nativeElement.ELEMENT_NODE ? nativeElement : nativeElement.parentNode;
  }
  static ɵfac = function CdkPortalOutlet_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _CdkPortalOutlet)();
  };
  static ɵdir = ɵɵdefineDirective({
    type: _CdkPortalOutlet,
    selectors: [["", "cdkPortalOutlet", ""]],
    inputs: {
      portal: [0, "cdkPortalOutlet", "portal"]
    },
    outputs: {
      attached: "attached"
    },
    exportAs: ["cdkPortalOutlet"],
    features: [ɵɵInheritDefinitionFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(CdkPortalOutlet, [{
    type: Directive,
    args: [{
      selector: "[cdkPortalOutlet]",
      exportAs: "cdkPortalOutlet"
    }]
  }], () => [], {
    portal: [{
      type: Input,
      args: ["cdkPortalOutlet"]
    }],
    attached: [{
      type: Output
    }]
  });
})();
var PortalModule = class _PortalModule {
  static ɵfac = function PortalModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PortalModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PortalModule,
    imports: [CdkPortal, CdkPortalOutlet],
    exports: [CdkPortal, CdkPortalOutlet]
  });
  static ɵinj = ɵɵdefineInjector({});
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PortalModule, [{
    type: NgModule,
    args: [{
      imports: [CdkPortal, CdkPortalOutlet],
      exports: [CdkPortal, CdkPortalOutlet]
    }]
  }], null, null);
})();

// node_modules/@angular/cdk/fesm2022/_overlay-module-chunk.mjs
var scrollBehaviorSupported2 = supportsScrollBehavior();
function createBlockScrollStrategy(injector) {
  return new BlockScrollStrategy(injector.get(ViewportRuler), injector.get(DOCUMENT));
}
var BlockScrollStrategy = class {
  _viewportRuler;
  _previousHTMLStyles = {
    top: "",
    left: ""
  };
  _previousScrollPosition;
  _isEnabled = false;
  _document;
  constructor(_viewportRuler, document2) {
    this._viewportRuler = _viewportRuler;
    this._document = document2;
  }
  attach() {
  }
  enable() {
    if (this._canBeEnabled()) {
      const root = this._document.documentElement;
      this._previousScrollPosition = this._viewportRuler.getViewportScrollPosition();
      this._previousHTMLStyles.left = root.style.left || "";
      this._previousHTMLStyles.top = root.style.top || "";
      root.style.left = coerceCssPixelValue(-this._previousScrollPosition.left);
      root.style.top = coerceCssPixelValue(-this._previousScrollPosition.top);
      root.classList.add("cdk-global-scrollblock");
      this._isEnabled = true;
    }
  }
  disable() {
    if (this._isEnabled) {
      const html = this._document.documentElement;
      const body = this._document.body;
      const htmlStyle = html.style;
      const bodyStyle = body.style;
      const previousHtmlScrollBehavior = htmlStyle.scrollBehavior || "";
      const previousBodyScrollBehavior = bodyStyle.scrollBehavior || "";
      this._isEnabled = false;
      htmlStyle.left = this._previousHTMLStyles.left;
      htmlStyle.top = this._previousHTMLStyles.top;
      html.classList.remove("cdk-global-scrollblock");
      if (scrollBehaviorSupported2) {
        htmlStyle.scrollBehavior = bodyStyle.scrollBehavior = "auto";
      }
      window.scroll(this._previousScrollPosition.left, this._previousScrollPosition.top);
      if (scrollBehaviorSupported2) {
        htmlStyle.scrollBehavior = previousHtmlScrollBehavior;
        bodyStyle.scrollBehavior = previousBodyScrollBehavior;
      }
    }
  }
  _canBeEnabled() {
    const html = this._document.documentElement;
    if (html.classList.contains("cdk-global-scrollblock") || this._isEnabled) {
      return false;
    }
    const rootElement = this._document.documentElement;
    const viewport = this._viewportRuler.getViewportSize();
    return rootElement.scrollHeight > viewport.height || rootElement.scrollWidth > viewport.width;
  }
};
function getMatScrollStrategyAlreadyAttachedError() {
  return Error(`Scroll strategy has already been attached.`);
}
function createCloseScrollStrategy(injector, config) {
  return new CloseScrollStrategy(injector.get(ScrollDispatcher), injector.get(NgZone), injector.get(ViewportRuler), config);
}
var CloseScrollStrategy = class {
  _scrollDispatcher;
  _ngZone;
  _viewportRuler;
  _config;
  _scrollSubscription = null;
  _overlayRef;
  _initialScrollPosition;
  constructor(_scrollDispatcher, _ngZone, _viewportRuler, _config) {
    this._scrollDispatcher = _scrollDispatcher;
    this._ngZone = _ngZone;
    this._viewportRuler = _viewportRuler;
    this._config = _config;
  }
  attach(overlayRef) {
    if (this._overlayRef && (typeof ngDevMode === "undefined" || ngDevMode)) {
      throw getMatScrollStrategyAlreadyAttachedError();
    }
    this._overlayRef = overlayRef;
  }
  enable() {
    if (this._scrollSubscription) {
      return;
    }
    const stream = this._scrollDispatcher.scrolled(0).pipe(filter((scrollable) => {
      return !scrollable || !this._overlayRef.overlayElement.contains(scrollable.getElementRef().nativeElement);
    }));
    if (this._config && this._config.threshold && this._config.threshold > 1) {
      this._initialScrollPosition = this._viewportRuler.getViewportScrollPosition().top;
      this._scrollSubscription = stream.subscribe(() => {
        const scrollPosition = this._viewportRuler.getViewportScrollPosition().top;
        if (Math.abs(scrollPosition - this._initialScrollPosition) > this._config.threshold) {
          this._detach();
        } else {
          this._overlayRef.updatePosition();
        }
      });
    } else {
      this._scrollSubscription = stream.subscribe(this._detach);
    }
  }
  disable() {
    if (this._scrollSubscription) {
      this._scrollSubscription.unsubscribe();
      this._scrollSubscription = null;
    }
  }
  detach() {
    this.disable();
    this._overlayRef = null;
  }
  _detach = () => {
    this.disable();
    if (this._overlayRef.hasAttached()) {
      this._ngZone.run(() => this._overlayRef.detach());
    }
  };
};
var NoopScrollStrategy = class {
  enable() {
  }
  disable() {
  }
  attach() {
  }
};
function isElementScrolledOutsideView(element, scrollContainers) {
  return scrollContainers.some((containerBounds) => {
    const outsideAbove = element.bottom < containerBounds.top;
    const outsideBelow = element.top > containerBounds.bottom;
    const outsideLeft = element.right < containerBounds.left;
    const outsideRight = element.left > containerBounds.right;
    return outsideAbove || outsideBelow || outsideLeft || outsideRight;
  });
}
function isElementClippedByScrolling(element, scrollContainers) {
  return scrollContainers.some((scrollContainerRect) => {
    const clippedAbove = element.top < scrollContainerRect.top;
    const clippedBelow = element.bottom > scrollContainerRect.bottom;
    const clippedLeft = element.left < scrollContainerRect.left;
    const clippedRight = element.right > scrollContainerRect.right;
    return clippedAbove || clippedBelow || clippedLeft || clippedRight;
  });
}
function createRepositionScrollStrategy(injector, config) {
  return new RepositionScrollStrategy(injector.get(ScrollDispatcher), injector.get(ViewportRuler), injector.get(NgZone), config);
}
var RepositionScrollStrategy = class {
  _scrollDispatcher;
  _viewportRuler;
  _ngZone;
  _config;
  _scrollSubscription = null;
  _overlayRef;
  constructor(_scrollDispatcher, _viewportRuler, _ngZone, _config) {
    this._scrollDispatcher = _scrollDispatcher;
    this._viewportRuler = _viewportRuler;
    this._ngZone = _ngZone;
    this._config = _config;
  }
  attach(overlayRef) {
    if (this._overlayRef && (typeof ngDevMode === "undefined" || ngDevMode)) {
      throw getMatScrollStrategyAlreadyAttachedError();
    }
    this._overlayRef = overlayRef;
  }
  enable() {
    if (!this._scrollSubscription) {
      const throttle2 = this._config ? this._config.scrollThrottle : 0;
      this._scrollSubscription = this._scrollDispatcher.scrolled(throttle2).subscribe(() => {
        this._overlayRef.updatePosition();
        if (this._config && this._config.autoClose) {
          const overlayRect = this._overlayRef.overlayElement.getBoundingClientRect();
          const {
            width,
            height
          } = this._viewportRuler.getViewportSize();
          const parentRects = [{
            width,
            height,
            bottom: height,
            right: width,
            top: 0,
            left: 0
          }];
          if (isElementScrolledOutsideView(overlayRect, parentRects)) {
            this.disable();
            this._ngZone.run(() => this._overlayRef.detach());
          }
        }
      });
    }
  }
  disable() {
    if (this._scrollSubscription) {
      this._scrollSubscription.unsubscribe();
      this._scrollSubscription = null;
    }
  }
  detach() {
    this.disable();
    this._overlayRef = null;
  }
};
var ScrollStrategyOptions = class _ScrollStrategyOptions {
  _injector = inject(Injector);
  constructor() {
  }
  noop = () => new NoopScrollStrategy();
  close = (config) => createCloseScrollStrategy(this._injector, config);
  block = () => createBlockScrollStrategy(this._injector);
  reposition = (config) => createRepositionScrollStrategy(this._injector, config);
  static ɵfac = function ScrollStrategyOptions_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _ScrollStrategyOptions)();
  };
  static ɵprov = ɵɵdefineInjectable({
    token: _ScrollStrategyOptions,
    factory: _ScrollStrategyOptions.ɵfac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ScrollStrategyOptions, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [], null);
})();
var OverlayConfig = class {
  positionStrategy;
  scrollStrategy = new NoopScrollStrategy();
  panelClass = "";
  hasBackdrop = false;
  backdropClass = "cdk-overlay-dark-backdrop";
  disableAnimations;
  width;
  height;
  minWidth;
  minHeight;
  maxWidth;
  maxHeight;
  direction;
  disposeOnNavigation = false;
  usePopover;
  eventPredicate;
  constructor(config) {
    if (config) {
      const configKeys = Object.keys(config);
      for (const key2 of configKeys) {
        if (config[key2] !== void 0) {
          this[key2] = config[key2];
        }
      }
    }
  }
};
var ConnectedOverlayPositionChange = class {
  connectionPair;
  scrollableViewProperties;
  constructor(connectionPair, scrollableViewProperties) {
    this.connectionPair = connectionPair;
    this.scrollableViewProperties = scrollableViewProperties;
  }
};
function validateVerticalPosition(property, value) {
  if (value !== "top" && value !== "bottom" && value !== "center") {
    throw Error(`ConnectedPosition: Invalid ${property} "${value}". Expected "top", "bottom" or "center".`);
  }
}
function validateHorizontalPosition(property, value) {
  if (value !== "start" && value !== "end" && value !== "center") {
    throw Error(`ConnectedPosition: Invalid ${property} "${value}". Expected "start", "end" or "center".`);
  }
}
var BaseOverlayDispatcher = class _BaseOverlayDispatcher {
  _attachedOverlays = [];
  _document = inject(DOCUMENT);
  _isAttached = false;
  constructor() {
  }
  ngOnDestroy() {
    this.detach();
  }
  add(overlayRef) {
    this.remove(overlayRef);
    this._attachedOverlays.push(overlayRef);
  }
  remove(overlayRef) {
    const index = this._attachedOverlays.indexOf(overlayRef);
    if (index > -1) {
      this._attachedOverlays.splice(index, 1);
    }
    if (this._attachedOverlays.length === 0) {
      this.detach();
    }
  }
  canReceiveEvent(overlayRef, event, stream) {
    if (stream.observers.length < 1) {
      return false;
    }
    if (overlayRef.eventPredicate) {
      return overlayRef.eventPredicate(event);
    }
    return true;
  }
  static ɵfac = function BaseOverlayDispatcher_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _BaseOverlayDispatcher)();
  };
  static ɵprov = ɵɵdefineInjectable({
    token: _BaseOverlayDispatcher,
    factory: _BaseOverlayDispatcher.ɵfac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(BaseOverlayDispatcher, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [], null);
})();
var OverlayKeyboardDispatcher = class _OverlayKeyboardDispatcher extends BaseOverlayDispatcher {
  _ngZone = inject(NgZone);
  _renderer = inject(RendererFactory2).createRenderer(null, null);
  _cleanupKeydown;
  add(overlayRef) {
    super.add(overlayRef);
    if (!this._isAttached) {
      this._ngZone.runOutsideAngular(() => {
        this._cleanupKeydown = this._renderer.listen("body", "keydown", this._keydownListener);
      });
      this._isAttached = true;
    }
  }
  detach() {
    if (this._isAttached) {
      this._cleanupKeydown?.();
      this._isAttached = false;
    }
  }
  _keydownListener = (event) => {
    const overlays = this._attachedOverlays;
    for (let i2 = overlays.length - 1; i2 > -1; i2--) {
      const overlayRef = overlays[i2];
      if (this.canReceiveEvent(overlayRef, event, overlayRef._keydownEvents)) {
        this._ngZone.run(() => overlayRef._keydownEvents.next(event));
        break;
      }
    }
  };
  static ɵfac = /* @__PURE__ */ (() => {
    let ɵOverlayKeyboardDispatcher_BaseFactory;
    return function OverlayKeyboardDispatcher_Factory(__ngFactoryType__) {
      return (ɵOverlayKeyboardDispatcher_BaseFactory || (ɵOverlayKeyboardDispatcher_BaseFactory = ɵɵgetInheritedFactory(_OverlayKeyboardDispatcher)))(__ngFactoryType__ || _OverlayKeyboardDispatcher);
    };
  })();
  static ɵprov = ɵɵdefineInjectable({
    token: _OverlayKeyboardDispatcher,
    factory: _OverlayKeyboardDispatcher.ɵfac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(OverlayKeyboardDispatcher, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
var OverlayOutsideClickDispatcher = class _OverlayOutsideClickDispatcher extends BaseOverlayDispatcher {
  _platform = inject(Platform);
  _ngZone = inject(NgZone);
  _renderer = inject(RendererFactory2).createRenderer(null, null);
  _cursorOriginalValue;
  _cursorStyleIsSet = false;
  _pointerDownEventTarget = null;
  _cleanups;
  add(overlayRef) {
    super.add(overlayRef);
    if (!this._isAttached) {
      const body = this._document.body;
      const eventOptions = {
        capture: true
      };
      const renderer = this._renderer;
      this._cleanups = this._ngZone.runOutsideAngular(() => [renderer.listen(body, "pointerdown", this._pointerDownListener, eventOptions), renderer.listen(body, "click", this._clickListener, eventOptions), renderer.listen(body, "auxclick", this._clickListener, eventOptions), renderer.listen(body, "contextmenu", this._clickListener, eventOptions)]);
      if (this._platform.IOS && !this._cursorStyleIsSet) {
        this._cursorOriginalValue = body.style.cursor;
        body.style.cursor = "pointer";
        this._cursorStyleIsSet = true;
      }
      this._isAttached = true;
    }
  }
  detach() {
    if (this._isAttached) {
      this._cleanups?.forEach((cleanup) => cleanup());
      this._cleanups = void 0;
      if (this._platform.IOS && this._cursorStyleIsSet) {
        this._document.body.style.cursor = this._cursorOriginalValue;
        this._cursorStyleIsSet = false;
      }
      this._isAttached = false;
    }
  }
  _pointerDownListener = (event) => {
    this._pointerDownEventTarget = _getEventTarget(event);
  };
  _clickListener = (event) => {
    const target = _getEventTarget(event);
    const origin = event.type === "click" && this._pointerDownEventTarget ? this._pointerDownEventTarget : target;
    this._pointerDownEventTarget = null;
    const overlays = this._attachedOverlays.slice();
    for (let i2 = overlays.length - 1; i2 > -1; i2--) {
      const overlayRef = overlays[i2];
      const outsidePointerEvents = overlayRef._outsidePointerEvents;
      if (!overlayRef.hasAttached() || !this.canReceiveEvent(overlayRef, event, outsidePointerEvents)) {
        continue;
      }
      if (containsPierceShadowDom(overlayRef.overlayElement, target) || containsPierceShadowDom(overlayRef.overlayElement, origin)) {
        break;
      }
      if (this._ngZone) {
        this._ngZone.run(() => outsidePointerEvents.next(event));
      } else {
        outsidePointerEvents.next(event);
      }
    }
  };
  static ɵfac = /* @__PURE__ */ (() => {
    let ɵOverlayOutsideClickDispatcher_BaseFactory;
    return function OverlayOutsideClickDispatcher_Factory(__ngFactoryType__) {
      return (ɵOverlayOutsideClickDispatcher_BaseFactory || (ɵOverlayOutsideClickDispatcher_BaseFactory = ɵɵgetInheritedFactory(_OverlayOutsideClickDispatcher)))(__ngFactoryType__ || _OverlayOutsideClickDispatcher);
    };
  })();
  static ɵprov = ɵɵdefineInjectable({
    token: _OverlayOutsideClickDispatcher,
    factory: _OverlayOutsideClickDispatcher.ɵfac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(OverlayOutsideClickDispatcher, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
function containsPierceShadowDom(parent, child) {
  const supportsShadowRoot = typeof ShadowRoot !== "undefined" && ShadowRoot;
  let current = child;
  while (current) {
    if (current === parent) {
      return true;
    }
    current = supportsShadowRoot && current instanceof ShadowRoot ? current.host : current.parentNode;
  }
  return false;
}
var _CdkOverlayStyleLoader = class __CdkOverlayStyleLoader {
  static ɵfac = function _CdkOverlayStyleLoader_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || __CdkOverlayStyleLoader)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: __CdkOverlayStyleLoader,
    selectors: [["ng-component"]],
    hostAttrs: ["cdk-overlay-style-loader", ""],
    decls: 0,
    vars: 0,
    template: function _CdkOverlayStyleLoader_Template(rf, ctx) {
    },
    styles: [".cdk-overlay-container, .cdk-global-overlay-wrapper {\n  pointer-events: none;\n  top: 0;\n  left: 0;\n  height: 100%;\n  width: 100%;\n}\n\n.cdk-overlay-container {\n  position: fixed;\n}\n@layer cdk-overlay {\n  .cdk-overlay-container {\n    z-index: 1000;\n  }\n}\n.cdk-overlay-container:empty {\n  display: none;\n}\n\n.cdk-global-overlay-wrapper {\n  display: flex;\n  position: absolute;\n}\n@layer cdk-overlay {\n  .cdk-global-overlay-wrapper {\n    z-index: 1000;\n  }\n}\n\n.cdk-overlay-pane {\n  position: absolute;\n  pointer-events: auto;\n  box-sizing: border-box;\n  display: flex;\n  max-width: 100%;\n  max-height: 100%;\n}\n@layer cdk-overlay {\n  .cdk-overlay-pane {\n    z-index: 1000;\n  }\n}\n\n.cdk-overlay-backdrop {\n  position: absolute;\n  top: 0;\n  bottom: 0;\n  left: 0;\n  right: 0;\n  pointer-events: auto;\n  -webkit-tap-highlight-color: transparent;\n  opacity: 0;\n  touch-action: manipulation;\n}\n@layer cdk-overlay {\n  .cdk-overlay-backdrop {\n    z-index: 1000;\n    transition: opacity 400ms cubic-bezier(0.25, 0.8, 0.25, 1);\n  }\n}\n@media (prefers-reduced-motion) {\n  .cdk-overlay-backdrop {\n    transition-duration: 1ms;\n  }\n}\n\n.cdk-overlay-backdrop-showing {\n  opacity: 1;\n}\n@media (forced-colors: active) {\n  .cdk-overlay-backdrop-showing {\n    opacity: 0.6;\n  }\n}\n\n@layer cdk-overlay {\n  .cdk-overlay-dark-backdrop {\n    background: rgba(0, 0, 0, 0.32);\n  }\n}\n\n.cdk-overlay-transparent-backdrop {\n  transition: visibility 1ms linear, opacity 1ms linear;\n  visibility: hidden;\n  opacity: 1;\n}\n.cdk-overlay-transparent-backdrop.cdk-overlay-backdrop-showing, .cdk-high-contrast-active .cdk-overlay-transparent-backdrop {\n  opacity: 0;\n  visibility: visible;\n}\n\n.cdk-overlay-backdrop-noop-animation {\n  transition: none;\n}\n\n.cdk-overlay-connected-position-bounding-box {\n  position: absolute;\n  display: flex;\n  flex-direction: column;\n  min-width: 1px;\n  min-height: 1px;\n}\n@layer cdk-overlay {\n  .cdk-overlay-connected-position-bounding-box {\n    z-index: 1000;\n  }\n}\n\n.cdk-global-scrollblock {\n  position: fixed;\n  width: 100%;\n  overflow-y: scroll;\n}\n\n.cdk-overlay-popover {\n  background: none;\n  border: none;\n  padding: 0;\n  outline: 0;\n  overflow: visible;\n  position: fixed;\n  pointer-events: none;\n  white-space: normal;\n  color: inherit;\n  text-decoration: none;\n  width: 100%;\n  height: 100%;\n  inset: auto;\n  top: 0;\n  left: 0;\n}\n.cdk-overlay-popover::backdrop {\n  display: none;\n}\n.cdk-overlay-popover .cdk-overlay-backdrop {\n  position: fixed;\n  z-index: auto;\n}\n"],
    encapsulation: 2,
    changeDetection: 0
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(_CdkOverlayStyleLoader, [{
    type: Component,
    args: [{
      template: "",
      changeDetection: ChangeDetectionStrategy.OnPush,
      encapsulation: ViewEncapsulation.None,
      host: {
        "cdk-overlay-style-loader": ""
      },
      styles: [".cdk-overlay-container, .cdk-global-overlay-wrapper {\n  pointer-events: none;\n  top: 0;\n  left: 0;\n  height: 100%;\n  width: 100%;\n}\n\n.cdk-overlay-container {\n  position: fixed;\n}\n@layer cdk-overlay {\n  .cdk-overlay-container {\n    z-index: 1000;\n  }\n}\n.cdk-overlay-container:empty {\n  display: none;\n}\n\n.cdk-global-overlay-wrapper {\n  display: flex;\n  position: absolute;\n}\n@layer cdk-overlay {\n  .cdk-global-overlay-wrapper {\n    z-index: 1000;\n  }\n}\n\n.cdk-overlay-pane {\n  position: absolute;\n  pointer-events: auto;\n  box-sizing: border-box;\n  display: flex;\n  max-width: 100%;\n  max-height: 100%;\n}\n@layer cdk-overlay {\n  .cdk-overlay-pane {\n    z-index: 1000;\n  }\n}\n\n.cdk-overlay-backdrop {\n  position: absolute;\n  top: 0;\n  bottom: 0;\n  left: 0;\n  right: 0;\n  pointer-events: auto;\n  -webkit-tap-highlight-color: transparent;\n  opacity: 0;\n  touch-action: manipulation;\n}\n@layer cdk-overlay {\n  .cdk-overlay-backdrop {\n    z-index: 1000;\n    transition: opacity 400ms cubic-bezier(0.25, 0.8, 0.25, 1);\n  }\n}\n@media (prefers-reduced-motion) {\n  .cdk-overlay-backdrop {\n    transition-duration: 1ms;\n  }\n}\n\n.cdk-overlay-backdrop-showing {\n  opacity: 1;\n}\n@media (forced-colors: active) {\n  .cdk-overlay-backdrop-showing {\n    opacity: 0.6;\n  }\n}\n\n@layer cdk-overlay {\n  .cdk-overlay-dark-backdrop {\n    background: rgba(0, 0, 0, 0.32);\n  }\n}\n\n.cdk-overlay-transparent-backdrop {\n  transition: visibility 1ms linear, opacity 1ms linear;\n  visibility: hidden;\n  opacity: 1;\n}\n.cdk-overlay-transparent-backdrop.cdk-overlay-backdrop-showing, .cdk-high-contrast-active .cdk-overlay-transparent-backdrop {\n  opacity: 0;\n  visibility: visible;\n}\n\n.cdk-overlay-backdrop-noop-animation {\n  transition: none;\n}\n\n.cdk-overlay-connected-position-bounding-box {\n  position: absolute;\n  display: flex;\n  flex-direction: column;\n  min-width: 1px;\n  min-height: 1px;\n}\n@layer cdk-overlay {\n  .cdk-overlay-connected-position-bounding-box {\n    z-index: 1000;\n  }\n}\n\n.cdk-global-scrollblock {\n  position: fixed;\n  width: 100%;\n  overflow-y: scroll;\n}\n\n.cdk-overlay-popover {\n  background: none;\n  border: none;\n  padding: 0;\n  outline: 0;\n  overflow: visible;\n  position: fixed;\n  pointer-events: none;\n  white-space: normal;\n  color: inherit;\n  text-decoration: none;\n  width: 100%;\n  height: 100%;\n  inset: auto;\n  top: 0;\n  left: 0;\n}\n.cdk-overlay-popover::backdrop {\n  display: none;\n}\n.cdk-overlay-popover .cdk-overlay-backdrop {\n  position: fixed;\n  z-index: auto;\n}\n"]
    }]
  }], null, null);
})();
var OverlayContainer = class _OverlayContainer {
  _platform = inject(Platform);
  _containerElement;
  _document = inject(DOCUMENT);
  _styleLoader = inject(_CdkPrivateStyleLoader);
  constructor() {
  }
  ngOnDestroy() {
    this._containerElement?.remove();
  }
  getContainerElement() {
    this._loadStyles();
    if (!this._containerElement) {
      this._createContainer();
    }
    return this._containerElement;
  }
  _createContainer() {
    const containerClass = "cdk-overlay-container";
    if (this._platform.isBrowser || _isTestEnvironment()) {
      const oppositePlatformContainers = this._document.querySelectorAll(`.${containerClass}[platform="server"], .${containerClass}[platform="test"]`);
      for (let i2 = 0; i2 < oppositePlatformContainers.length; i2++) {
        oppositePlatformContainers[i2].remove();
      }
    }
    const container = this._document.createElement("div");
    container.classList.add(containerClass);
    if (_isTestEnvironment()) {
      container.setAttribute("platform", "test");
    } else if (!this._platform.isBrowser) {
      container.setAttribute("platform", "server");
    }
    this._document.body.appendChild(container);
    this._containerElement = container;
  }
  _loadStyles() {
    this._styleLoader.load(_CdkOverlayStyleLoader);
  }
  static ɵfac = function OverlayContainer_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _OverlayContainer)();
  };
  static ɵprov = ɵɵdefineInjectable({
    token: _OverlayContainer,
    factory: _OverlayContainer.ɵfac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(OverlayContainer, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [], null);
})();
var BackdropRef = class {
  _renderer;
  _ngZone;
  element;
  _cleanupClick;
  _cleanupTransitionEnd;
  _fallbackTimeout;
  constructor(document2, _renderer, _ngZone, onClick) {
    this._renderer = _renderer;
    this._ngZone = _ngZone;
    this.element = document2.createElement("div");
    this.element.classList.add("cdk-overlay-backdrop");
    this._cleanupClick = _renderer.listen(this.element, "click", onClick);
  }
  detach() {
    this._ngZone.runOutsideAngular(() => {
      const element = this.element;
      clearTimeout(this._fallbackTimeout);
      this._cleanupTransitionEnd?.();
      this._cleanupTransitionEnd = this._renderer.listen(element, "transitionend", this.dispose);
      this._fallbackTimeout = setTimeout(this.dispose, 500);
      element.style.pointerEvents = "none";
      element.classList.remove("cdk-overlay-backdrop-showing");
    });
  }
  dispose = () => {
    clearTimeout(this._fallbackTimeout);
    this._cleanupClick?.();
    this._cleanupTransitionEnd?.();
    this._cleanupClick = this._cleanupTransitionEnd = this._fallbackTimeout = void 0;
    this.element.remove();
  };
};
function isElement(value) {
  return value && value.nodeType === 1;
}
var OverlayRef = class {
  _portalOutlet;
  _host;
  _pane;
  _config;
  _ngZone;
  _keyboardDispatcher;
  _document;
  _location;
  _outsideClickDispatcher;
  _animationsDisabled;
  _injector;
  _renderer;
  _backdropClick = new Subject();
  _attachments = new Subject();
  _detachments = new Subject();
  _positionStrategy;
  _scrollStrategy;
  _locationChanges = Subscription.EMPTY;
  _backdropRef = null;
  _detachContentMutationObserver;
  _detachContentAfterRenderRef;
  _disposed = false;
  _previousHostParent;
  _keydownEvents = new Subject();
  _outsidePointerEvents = new Subject();
  _afterNextRenderRef;
  constructor(_portalOutlet, _host, _pane, _config, _ngZone, _keyboardDispatcher, _document, _location, _outsideClickDispatcher, _animationsDisabled = false, _injector, _renderer) {
    this._portalOutlet = _portalOutlet;
    this._host = _host;
    this._pane = _pane;
    this._config = _config;
    this._ngZone = _ngZone;
    this._keyboardDispatcher = _keyboardDispatcher;
    this._document = _document;
    this._location = _location;
    this._outsideClickDispatcher = _outsideClickDispatcher;
    this._animationsDisabled = _animationsDisabled;
    this._injector = _injector;
    this._renderer = _renderer;
    if (_config.scrollStrategy) {
      this._scrollStrategy = _config.scrollStrategy;
      this._scrollStrategy.attach(this);
    }
    this._positionStrategy = _config.positionStrategy;
  }
  get overlayElement() {
    return this._pane;
  }
  get backdropElement() {
    return this._backdropRef?.element || null;
  }
  get hostElement() {
    return this._host;
  }
  get eventPredicate() {
    return this._config?.eventPredicate || null;
  }
  attach(portal) {
    if (this._disposed) {
      return null;
    }
    this._attachHost();
    const attachResult = this._portalOutlet.attach(portal);
    this._positionStrategy?.attach(this);
    this._updateStackingOrder();
    this._updateElementSize();
    this._updateElementDirection();
    if (this._scrollStrategy) {
      this._scrollStrategy.enable();
    }
    this._afterNextRenderRef?.destroy();
    this._afterNextRenderRef = afterNextRender(() => {
      if (this.hasAttached()) {
        this.updatePosition();
      }
    }, {
      injector: this._injector
    });
    this._togglePointerEvents(true);
    if (this._config.hasBackdrop) {
      this._attachBackdrop();
    }
    if (this._config.panelClass) {
      this._toggleClasses(this._pane, this._config.panelClass, true);
    }
    this._attachments.next();
    this._completeDetachContent();
    this._keyboardDispatcher.add(this);
    if (this._config.disposeOnNavigation) {
      this._locationChanges = this._location.subscribe(() => this.dispose());
    }
    this._outsideClickDispatcher.add(this);
    if (typeof attachResult?.onDestroy === "function") {
      attachResult.onDestroy(() => {
        if (this.hasAttached()) {
          this._ngZone.runOutsideAngular(() => Promise.resolve().then(() => this.detach()));
        }
      });
    }
    return attachResult;
  }
  detach() {
    if (!this.hasAttached()) {
      return;
    }
    this.detachBackdrop();
    this._togglePointerEvents(false);
    if (this._positionStrategy && this._positionStrategy.detach) {
      this._positionStrategy.detach();
    }
    if (this._scrollStrategy) {
      this._scrollStrategy.disable();
    }
    const detachmentResult = this._portalOutlet.detach();
    this._detachments.next();
    this._completeDetachContent();
    this._keyboardDispatcher.remove(this);
    this._detachContentWhenEmpty();
    this._locationChanges.unsubscribe();
    this._outsideClickDispatcher.remove(this);
    return detachmentResult;
  }
  dispose() {
    if (this._disposed) {
      return;
    }
    const isAttached = this.hasAttached();
    if (this._positionStrategy) {
      this._positionStrategy.dispose();
    }
    this._disposeScrollStrategy();
    this._backdropRef?.dispose();
    this._locationChanges.unsubscribe();
    this._keyboardDispatcher.remove(this);
    this._portalOutlet.dispose();
    this._attachments.complete();
    this._backdropClick.complete();
    this._keydownEvents.complete();
    this._outsidePointerEvents.complete();
    this._outsideClickDispatcher.remove(this);
    this._host?.remove();
    this._afterNextRenderRef?.destroy();
    this._previousHostParent = this._pane = this._host = this._backdropRef = null;
    if (isAttached) {
      this._detachments.next();
    }
    this._detachments.complete();
    this._completeDetachContent();
    this._disposed = true;
  }
  hasAttached() {
    return this._portalOutlet.hasAttached();
  }
  backdropClick() {
    return this._backdropClick;
  }
  attachments() {
    return this._attachments;
  }
  detachments() {
    return this._detachments;
  }
  keydownEvents() {
    return this._keydownEvents;
  }
  outsidePointerEvents() {
    return this._outsidePointerEvents;
  }
  getConfig() {
    return this._config;
  }
  updatePosition() {
    if (this._positionStrategy) {
      this._positionStrategy.apply();
    }
  }
  updatePositionStrategy(strategy) {
    if (strategy === this._positionStrategy) {
      return;
    }
    if (this._positionStrategy) {
      this._positionStrategy.dispose();
    }
    this._positionStrategy = strategy;
    if (this.hasAttached()) {
      strategy.attach(this);
      this.updatePosition();
    }
  }
  updateSize(sizeConfig) {
    this._config = __spreadValues(__spreadValues({}, this._config), sizeConfig);
    this._updateElementSize();
  }
  setDirection(dir3) {
    this._config = __spreadProps(__spreadValues({}, this._config), {
      direction: dir3
    });
    this._updateElementDirection();
  }
  addPanelClass(classes) {
    if (this._pane) {
      this._toggleClasses(this._pane, classes, true);
    }
  }
  removePanelClass(classes) {
    if (this._pane) {
      this._toggleClasses(this._pane, classes, false);
    }
  }
  getDirection() {
    const direction = this._config.direction;
    if (!direction) {
      return "ltr";
    }
    return typeof direction === "string" ? direction : direction.value;
  }
  updateScrollStrategy(strategy) {
    if (strategy === this._scrollStrategy) {
      return;
    }
    this._disposeScrollStrategy();
    this._scrollStrategy = strategy;
    if (this.hasAttached()) {
      strategy.attach(this);
      strategy.enable();
    }
  }
  _updateElementDirection() {
    this._host.setAttribute("dir", this.getDirection());
  }
  _updateElementSize() {
    if (!this._pane) {
      return;
    }
    const style2 = this._pane.style;
    style2.width = coerceCssPixelValue(this._config.width);
    style2.height = coerceCssPixelValue(this._config.height);
    style2.minWidth = coerceCssPixelValue(this._config.minWidth);
    style2.minHeight = coerceCssPixelValue(this._config.minHeight);
    style2.maxWidth = coerceCssPixelValue(this._config.maxWidth);
    style2.maxHeight = coerceCssPixelValue(this._config.maxHeight);
  }
  _togglePointerEvents(enablePointer) {
    this._pane.style.pointerEvents = enablePointer ? "" : "none";
  }
  _attachHost() {
    if (!this._host.parentElement) {
      const customInsertionPoint = this._config.usePopover ? this._positionStrategy?.getPopoverInsertionPoint?.() : null;
      if (isElement(customInsertionPoint)) {
        customInsertionPoint.after(this._host);
      } else if (customInsertionPoint?.type === "parent") {
        customInsertionPoint.element.appendChild(this._host);
      } else {
        this._previousHostParent?.appendChild(this._host);
      }
    }
    if (this._config.usePopover) {
      try {
        this._host["showPopover"]();
      } catch (e2) {
      }
    }
  }
  _attachBackdrop() {
    const showingClass = "cdk-overlay-backdrop-showing";
    this._backdropRef?.dispose();
    this._backdropRef = new BackdropRef(this._document, this._renderer, this._ngZone, (event) => {
      this._backdropClick.next(event);
    });
    if (this._animationsDisabled) {
      this._backdropRef.element.classList.add("cdk-overlay-backdrop-noop-animation");
    }
    if (this._config.backdropClass) {
      this._toggleClasses(this._backdropRef.element, this._config.backdropClass, true);
    }
    if (this._config.usePopover) {
      this._host.prepend(this._backdropRef.element);
    } else {
      this._host.parentElement.insertBefore(this._backdropRef.element, this._host);
    }
    if (!this._animationsDisabled && typeof requestAnimationFrame !== "undefined") {
      this._ngZone.runOutsideAngular(() => {
        requestAnimationFrame(() => this._backdropRef?.element.classList.add(showingClass));
      });
    } else {
      this._backdropRef.element.classList.add(showingClass);
    }
  }
  _updateStackingOrder() {
    if (!this._config.usePopover && this._host.nextSibling) {
      this._host.parentNode.appendChild(this._host);
    }
  }
  detachBackdrop() {
    if (this._animationsDisabled) {
      this._backdropRef?.dispose();
      this._backdropRef = null;
    } else {
      this._backdropRef?.detach();
    }
  }
  _toggleClasses(element, cssClasses, isAdd) {
    const classes = coerceArray(cssClasses || []).filter((c) => !!c);
    if (classes.length) {
      isAdd ? element.classList.add(...classes) : element.classList.remove(...classes);
    }
  }
  _detachContentWhenEmpty() {
    let rethrow = false;
    try {
      this._detachContentAfterRenderRef = afterNextRender(() => {
        rethrow = true;
        this._detachContent();
      }, {
        injector: this._injector
      });
    } catch (e2) {
      if (rethrow) {
        throw e2;
      }
      this._detachContent();
    }
    if (globalThis.MutationObserver && this._pane) {
      this._detachContentMutationObserver ||= new globalThis.MutationObserver(() => {
        this._detachContent();
      });
      this._detachContentMutationObserver.observe(this._pane, {
        childList: true
      });
    }
  }
  _detachContent() {
    if (!this._pane || !this._host || this._pane.children.length === 0) {
      if (this._pane && this._config.panelClass) {
        this._toggleClasses(this._pane, this._config.panelClass, false);
      }
      if (this._host && this._host.parentElement) {
        this._previousHostParent = this._host.parentElement;
        this._host.remove();
      }
      this._completeDetachContent();
    }
  }
  _completeDetachContent() {
    this._detachContentAfterRenderRef?.destroy();
    this._detachContentAfterRenderRef = void 0;
    this._detachContentMutationObserver?.disconnect();
  }
  _disposeScrollStrategy() {
    const scrollStrategy = this._scrollStrategy;
    scrollStrategy?.disable();
    scrollStrategy?.detach?.();
  }
};
var boundingBoxClass = "cdk-overlay-connected-position-bounding-box";
var cssUnitPattern = /([A-Za-z%]+)$/;
function createFlexibleConnectedPositionStrategy(injector, origin) {
  return new FlexibleConnectedPositionStrategy(origin, injector.get(ViewportRuler), injector.get(DOCUMENT), injector.get(Platform), injector.get(OverlayContainer));
}
var FlexibleConnectedPositionStrategy = class {
  _viewportRuler;
  _document;
  _platform;
  _overlayContainer;
  _overlayRef;
  _isInitialRender = false;
  _lastBoundingBoxSize = {
    width: 0,
    height: 0
  };
  _isPushed = false;
  _canPush = true;
  _growAfterOpen = false;
  _hasFlexibleDimensions = true;
  _positionLocked = false;
  _originRect;
  _overlayRect;
  _viewportRect;
  _containerRect;
  _viewportMargin = 0;
  _scrollables = [];
  _preferredPositions = [];
  _origin;
  _pane;
  _isDisposed = false;
  _boundingBox = null;
  _lastPosition = null;
  _lastScrollVisibility = null;
  _positionChanges = new Subject();
  _resizeSubscription = Subscription.EMPTY;
  _offsetX = 0;
  _offsetY = 0;
  _transformOriginSelector;
  _appliedPanelClasses = [];
  _previousPushAmount = null;
  _popoverLocation = "global";
  positionChanges = this._positionChanges;
  get positions() {
    return this._preferredPositions;
  }
  constructor(connectedTo, _viewportRuler, _document, _platform, _overlayContainer) {
    this._viewportRuler = _viewportRuler;
    this._document = _document;
    this._platform = _platform;
    this._overlayContainer = _overlayContainer;
    this.setOrigin(connectedTo);
  }
  attach(overlayRef) {
    if (this._overlayRef && overlayRef !== this._overlayRef && (typeof ngDevMode === "undefined" || ngDevMode)) {
      throw Error("This position strategy is already attached to an overlay");
    }
    this._validatePositions();
    overlayRef.hostElement.classList.add(boundingBoxClass);
    this._overlayRef = overlayRef;
    this._boundingBox = overlayRef.hostElement;
    this._pane = overlayRef.overlayElement;
    this._isDisposed = false;
    this._isInitialRender = true;
    this._lastPosition = null;
    this._resizeSubscription.unsubscribe();
    this._resizeSubscription = this._viewportRuler.change().subscribe(() => {
      this._isInitialRender = true;
      this.apply();
    });
  }
  apply() {
    if (this._isDisposed || !this._platform.isBrowser) {
      return;
    }
    if (!this._isInitialRender && this._positionLocked && this._lastPosition) {
      this.reapplyLastPosition();
      return;
    }
    this._clearPanelClasses();
    this._resetOverlayElementStyles();
    this._resetBoundingBoxStyles();
    this._viewportRect = this._getNarrowedViewportRect();
    this._originRect = this._getOriginRect();
    this._overlayRect = this._pane.getBoundingClientRect();
    this._containerRect = this._getContainerRect();
    const originRect = this._originRect;
    const overlayRect = this._overlayRect;
    const viewportRect = this._viewportRect;
    const containerRect = this._containerRect;
    const flexibleFits = [];
    let fallback;
    for (let pos of this._preferredPositions) {
      let originPoint = this._getOriginPoint(originRect, containerRect, pos);
      let overlayPoint = this._getOverlayPoint(originPoint, overlayRect, pos);
      let overlayFit = this._getOverlayFit(overlayPoint, overlayRect, viewportRect, pos);
      if (overlayFit.isCompletelyWithinViewport) {
        this._isPushed = false;
        this._applyPosition(pos, originPoint);
        return;
      }
      if (this._canFitWithFlexibleDimensions(overlayFit, overlayPoint, viewportRect)) {
        flexibleFits.push({
          position: pos,
          origin: originPoint,
          overlayRect,
          boundingBoxRect: this._calculateBoundingBoxRect(originPoint, pos)
        });
        continue;
      }
      if (!fallback || fallback.overlayFit.visibleArea < overlayFit.visibleArea) {
        fallback = {
          overlayFit,
          overlayPoint,
          originPoint,
          position: pos,
          overlayRect
        };
      }
    }
    if (flexibleFits.length) {
      let bestFit = null;
      let bestScore = -1;
      for (const fit of flexibleFits) {
        const score = fit.boundingBoxRect.width * fit.boundingBoxRect.height * (fit.position.weight || 1);
        if (score > bestScore) {
          bestScore = score;
          bestFit = fit;
        }
      }
      this._isPushed = false;
      this._applyPosition(bestFit.position, bestFit.origin);
      return;
    }
    if (this._canPush) {
      this._isPushed = true;
      this._applyPosition(fallback.position, fallback.originPoint);
      return;
    }
    this._applyPosition(fallback.position, fallback.originPoint);
  }
  detach() {
    this._clearPanelClasses();
    this._lastPosition = null;
    this._previousPushAmount = null;
    this._resizeSubscription.unsubscribe();
  }
  dispose() {
    if (this._isDisposed) {
      return;
    }
    if (this._boundingBox) {
      extendStyles(this._boundingBox.style, {
        top: "",
        left: "",
        right: "",
        bottom: "",
        height: "",
        width: "",
        alignItems: "",
        justifyContent: ""
      });
    }
    if (this._pane) {
      this._resetOverlayElementStyles();
    }
    if (this._overlayRef) {
      this._overlayRef.hostElement.classList.remove(boundingBoxClass);
    }
    this.detach();
    this._positionChanges.complete();
    this._overlayRef = this._boundingBox = null;
    this._isDisposed = true;
  }
  reapplyLastPosition() {
    if (this._isDisposed || !this._platform.isBrowser) {
      return;
    }
    const lastPosition = this._lastPosition;
    if (lastPosition) {
      this._originRect = this._getOriginRect();
      this._overlayRect = this._pane.getBoundingClientRect();
      this._viewportRect = this._getNarrowedViewportRect();
      this._containerRect = this._getContainerRect();
      this._applyPosition(lastPosition, this._getOriginPoint(this._originRect, this._containerRect, lastPosition));
    } else {
      this.apply();
    }
  }
  withScrollableContainers(scrollables) {
    this._scrollables = scrollables;
    return this;
  }
  withPositions(positions) {
    this._preferredPositions = positions;
    if (positions.indexOf(this._lastPosition) === -1) {
      this._lastPosition = null;
    }
    this._validatePositions();
    return this;
  }
  withViewportMargin(margin) {
    this._viewportMargin = margin;
    return this;
  }
  withFlexibleDimensions(flexibleDimensions = true) {
    this._hasFlexibleDimensions = flexibleDimensions;
    return this;
  }
  withGrowAfterOpen(growAfterOpen = true) {
    this._growAfterOpen = growAfterOpen;
    return this;
  }
  withPush(canPush = true) {
    this._canPush = canPush;
    return this;
  }
  withLockedPosition(isLocked = true) {
    this._positionLocked = isLocked;
    return this;
  }
  setOrigin(origin) {
    this._origin = origin;
    return this;
  }
  withDefaultOffsetX(offset) {
    this._offsetX = offset;
    return this;
  }
  withDefaultOffsetY(offset) {
    this._offsetY = offset;
    return this;
  }
  withTransformOriginOn(selector2) {
    this._transformOriginSelector = selector2;
    return this;
  }
  withPopoverLocation(location) {
    this._popoverLocation = location;
    return this;
  }
  getPopoverInsertionPoint() {
    if (this._popoverLocation === "global") {
      return null;
    } else if (this._popoverLocation !== "inline") {
      return this._popoverLocation;
    }
    if (this._origin instanceof ElementRef) {
      return this._origin.nativeElement;
    } else if (isElement(this._origin)) {
      return this._origin;
    } else {
      return null;
    }
  }
  _getOriginPoint(originRect, containerRect, pos) {
    let x;
    if (pos.originX == "center") {
      x = originRect.left + originRect.width / 2;
    } else {
      const startX = this._isRtl() ? originRect.right : originRect.left;
      const endX = this._isRtl() ? originRect.left : originRect.right;
      x = pos.originX == "start" ? startX : endX;
    }
    if (containerRect.left < 0) {
      x -= containerRect.left;
    }
    let y;
    if (pos.originY == "center") {
      y = originRect.top + originRect.height / 2;
    } else {
      y = pos.originY == "top" ? originRect.top : originRect.bottom;
    }
    if (containerRect.top < 0) {
      y -= containerRect.top;
    }
    return {
      x,
      y
    };
  }
  _getOverlayPoint(originPoint, overlayRect, pos) {
    let overlayStartX;
    if (pos.overlayX == "center") {
      overlayStartX = -overlayRect.width / 2;
    } else if (pos.overlayX === "start") {
      overlayStartX = this._isRtl() ? -overlayRect.width : 0;
    } else {
      overlayStartX = this._isRtl() ? 0 : -overlayRect.width;
    }
    let overlayStartY;
    if (pos.overlayY == "center") {
      overlayStartY = -overlayRect.height / 2;
    } else {
      overlayStartY = pos.overlayY == "top" ? 0 : -overlayRect.height;
    }
    return {
      x: originPoint.x + overlayStartX,
      y: originPoint.y + overlayStartY
    };
  }
  _getOverlayFit(point, rawOverlayRect, viewport, position) {
    const overlay = getRoundedBoundingClientRect(rawOverlayRect);
    let {
      x,
      y
    } = point;
    let offsetX = this._getOffset(position, "x");
    let offsetY = this._getOffset(position, "y");
    if (offsetX) {
      x += offsetX;
    }
    if (offsetY) {
      y += offsetY;
    }
    let leftOverflow = 0 - x;
    let rightOverflow = x + overlay.width - viewport.width;
    let topOverflow = 0 - y;
    let bottomOverflow = y + overlay.height - viewport.height;
    let visibleWidth = this._subtractOverflows(overlay.width, leftOverflow, rightOverflow);
    let visibleHeight = this._subtractOverflows(overlay.height, topOverflow, bottomOverflow);
    let visibleArea = visibleWidth * visibleHeight;
    return {
      visibleArea,
      isCompletelyWithinViewport: overlay.width * overlay.height === visibleArea,
      fitsInViewportVertically: visibleHeight === overlay.height,
      fitsInViewportHorizontally: visibleWidth == overlay.width
    };
  }
  _canFitWithFlexibleDimensions(fit, point, viewport) {
    if (this._hasFlexibleDimensions) {
      const availableHeight = viewport.bottom - point.y;
      const availableWidth = viewport.right - point.x;
      const minHeight = getPixelValue(this._overlayRef.getConfig().minHeight);
      const minWidth = getPixelValue(this._overlayRef.getConfig().minWidth);
      const verticalFit = fit.fitsInViewportVertically || minHeight != null && minHeight <= availableHeight;
      const horizontalFit = fit.fitsInViewportHorizontally || minWidth != null && minWidth <= availableWidth;
      return verticalFit && horizontalFit;
    }
    return false;
  }
  _pushOverlayOnScreen(start2, rawOverlayRect, scrollPosition) {
    if (this._previousPushAmount && this._positionLocked) {
      return {
        x: start2.x + this._previousPushAmount.x,
        y: start2.y + this._previousPushAmount.y
      };
    }
    const overlay = getRoundedBoundingClientRect(rawOverlayRect);
    const viewport = this._viewportRect;
    const overflowRight = Math.max(start2.x + overlay.width - viewport.width, 0);
    const overflowBottom = Math.max(start2.y + overlay.height - viewport.height, 0);
    const overflowTop = Math.max(viewport.top - scrollPosition.top - start2.y, 0);
    const overflowLeft = Math.max(viewport.left - scrollPosition.left - start2.x, 0);
    let pushX = 0;
    let pushY = 0;
    if (overlay.width <= viewport.width) {
      pushX = overflowLeft || -overflowRight;
    } else {
      pushX = start2.x < this._getViewportMarginStart() ? viewport.left - scrollPosition.left - start2.x : 0;
    }
    if (overlay.height <= viewport.height) {
      pushY = overflowTop || -overflowBottom;
    } else {
      pushY = start2.y < this._getViewportMarginTop() ? viewport.top - scrollPosition.top - start2.y : 0;
    }
    this._previousPushAmount = {
      x: pushX,
      y: pushY
    };
    return {
      x: start2.x + pushX,
      y: start2.y + pushY
    };
  }
  _applyPosition(position, originPoint) {
    this._setTransformOrigin(position);
    this._setOverlayElementStyles(originPoint, position);
    this._setBoundingBoxStyles(originPoint, position);
    if (position.panelClass) {
      this._addPanelClasses(position.panelClass);
    }
    if (this._positionChanges.observers.length) {
      const scrollVisibility = this._getScrollVisibility();
      if (position !== this._lastPosition || !this._lastScrollVisibility || !compareScrollVisibility(this._lastScrollVisibility, scrollVisibility)) {
        const changeEvent = new ConnectedOverlayPositionChange(position, scrollVisibility);
        this._positionChanges.next(changeEvent);
      }
      this._lastScrollVisibility = scrollVisibility;
    }
    this._lastPosition = position;
    this._isInitialRender = false;
  }
  _setTransformOrigin(position) {
    if (!this._transformOriginSelector) {
      return;
    }
    const elements = this._boundingBox.querySelectorAll(this._transformOriginSelector);
    let xOrigin;
    let yOrigin = position.overlayY;
    if (position.overlayX === "center") {
      xOrigin = "center";
    } else if (this._isRtl()) {
      xOrigin = position.overlayX === "start" ? "right" : "left";
    } else {
      xOrigin = position.overlayX === "start" ? "left" : "right";
    }
    for (let i2 = 0; i2 < elements.length; i2++) {
      elements[i2].style.transformOrigin = `${xOrigin} ${yOrigin}`;
    }
  }
  _calculateBoundingBoxRect(origin, position) {
    const viewport = this._viewportRect;
    const isRtl = this._isRtl();
    let height, top, bottom;
    if (position.overlayY === "top") {
      top = origin.y;
      height = viewport.height - top + this._getViewportMarginBottom();
    } else if (position.overlayY === "bottom") {
      bottom = viewport.height - origin.y + this._getViewportMarginTop() + this._getViewportMarginBottom();
      height = viewport.height - bottom + this._getViewportMarginTop();
    } else {
      const smallestDistanceToViewportEdge = Math.min(viewport.bottom - origin.y + viewport.top, origin.y);
      const previousHeight = this._lastBoundingBoxSize.height;
      height = smallestDistanceToViewportEdge * 2;
      top = origin.y - smallestDistanceToViewportEdge;
      if (height > previousHeight && !this._isInitialRender && !this._growAfterOpen) {
        top = origin.y - previousHeight / 2;
      }
    }
    const isBoundedByRightViewportEdge = position.overlayX === "start" && !isRtl || position.overlayX === "end" && isRtl;
    const isBoundedByLeftViewportEdge = position.overlayX === "end" && !isRtl || position.overlayX === "start" && isRtl;
    let width, left, right;
    if (isBoundedByLeftViewportEdge) {
      right = viewport.width - origin.x + this._getViewportMarginStart() + this._getViewportMarginEnd();
      width = origin.x - this._getViewportMarginStart();
    } else if (isBoundedByRightViewportEdge) {
      left = origin.x;
      width = viewport.right - origin.x - this._getViewportMarginEnd();
    } else {
      const smallestDistanceToViewportEdge = Math.min(viewport.right - origin.x + viewport.left, origin.x);
      const previousWidth = this._lastBoundingBoxSize.width;
      width = smallestDistanceToViewportEdge * 2;
      left = origin.x - smallestDistanceToViewportEdge;
      if (width > previousWidth && !this._isInitialRender && !this._growAfterOpen) {
        left = origin.x - previousWidth / 2;
      }
    }
    return {
      top,
      left,
      bottom,
      right,
      width,
      height
    };
  }
  _setBoundingBoxStyles(origin, position) {
    const boundingBoxRect = this._calculateBoundingBoxRect(origin, position);
    if (!this._isInitialRender && !this._growAfterOpen) {
      boundingBoxRect.height = Math.min(boundingBoxRect.height, this._lastBoundingBoxSize.height);
      boundingBoxRect.width = Math.min(boundingBoxRect.width, this._lastBoundingBoxSize.width);
    }
    const styles = {};
    if (this._hasExactPosition()) {
      styles.top = styles.left = "0";
      styles.bottom = styles.right = "auto";
      styles.maxHeight = styles.maxWidth = "";
      styles.width = styles.height = "100%";
    } else {
      const maxHeight = this._overlayRef.getConfig().maxHeight;
      const maxWidth = this._overlayRef.getConfig().maxWidth;
      styles.width = coerceCssPixelValue(boundingBoxRect.width);
      styles.height = coerceCssPixelValue(boundingBoxRect.height);
      styles.top = coerceCssPixelValue(boundingBoxRect.top) || "auto";
      styles.bottom = coerceCssPixelValue(boundingBoxRect.bottom) || "auto";
      styles.left = coerceCssPixelValue(boundingBoxRect.left) || "auto";
      styles.right = coerceCssPixelValue(boundingBoxRect.right) || "auto";
      if (position.overlayX === "center") {
        styles.alignItems = "center";
      } else {
        styles.alignItems = position.overlayX === "end" ? "flex-end" : "flex-start";
      }
      if (position.overlayY === "center") {
        styles.justifyContent = "center";
      } else {
        styles.justifyContent = position.overlayY === "bottom" ? "flex-end" : "flex-start";
      }
      if (maxHeight) {
        styles.maxHeight = coerceCssPixelValue(maxHeight);
      }
      if (maxWidth) {
        styles.maxWidth = coerceCssPixelValue(maxWidth);
      }
    }
    this._lastBoundingBoxSize = boundingBoxRect;
    extendStyles(this._boundingBox.style, styles);
  }
  _resetBoundingBoxStyles() {
    extendStyles(this._boundingBox.style, {
      top: "0",
      left: "0",
      right: "0",
      bottom: "0",
      height: "",
      width: "",
      alignItems: "",
      justifyContent: ""
    });
  }
  _resetOverlayElementStyles() {
    extendStyles(this._pane.style, {
      top: "",
      left: "",
      bottom: "",
      right: "",
      position: "",
      transform: ""
    });
  }
  _setOverlayElementStyles(originPoint, position) {
    const styles = {};
    const hasExactPosition = this._hasExactPosition();
    const hasFlexibleDimensions = this._hasFlexibleDimensions;
    const config = this._overlayRef.getConfig();
    if (hasExactPosition) {
      const scrollPosition = this._viewportRuler.getViewportScrollPosition();
      extendStyles(styles, this._getExactOverlayY(position, originPoint, scrollPosition));
      extendStyles(styles, this._getExactOverlayX(position, originPoint, scrollPosition));
    } else {
      styles.position = "static";
    }
    let transformString = "";
    let offsetX = this._getOffset(position, "x");
    let offsetY = this._getOffset(position, "y");
    if (offsetX) {
      transformString += `translateX(${offsetX}px) `;
    }
    if (offsetY) {
      transformString += `translateY(${offsetY}px)`;
    }
    styles.transform = transformString.trim();
    if (config.maxHeight) {
      if (hasExactPosition) {
        styles.maxHeight = coerceCssPixelValue(config.maxHeight);
      } else if (hasFlexibleDimensions) {
        styles.maxHeight = "";
      }
    }
    if (config.maxWidth) {
      if (hasExactPosition) {
        styles.maxWidth = coerceCssPixelValue(config.maxWidth);
      } else if (hasFlexibleDimensions) {
        styles.maxWidth = "";
      }
    }
    extendStyles(this._pane.style, styles);
  }
  _getExactOverlayY(position, originPoint, scrollPosition) {
    let styles = {
      top: "",
      bottom: ""
    };
    let overlayPoint = this._getOverlayPoint(originPoint, this._overlayRect, position);
    if (this._isPushed) {
      overlayPoint = this._pushOverlayOnScreen(overlayPoint, this._overlayRect, scrollPosition);
    }
    if (position.overlayY === "bottom") {
      const documentHeight = this._document.documentElement.clientHeight;
      styles.bottom = `${documentHeight - (overlayPoint.y + this._overlayRect.height)}px`;
    } else {
      styles.top = coerceCssPixelValue(overlayPoint.y);
    }
    return styles;
  }
  _getExactOverlayX(position, originPoint, scrollPosition) {
    let styles = {
      left: "",
      right: ""
    };
    let overlayPoint = this._getOverlayPoint(originPoint, this._overlayRect, position);
    if (this._isPushed) {
      overlayPoint = this._pushOverlayOnScreen(overlayPoint, this._overlayRect, scrollPosition);
    }
    let horizontalStyleProperty;
    if (this._isRtl()) {
      horizontalStyleProperty = position.overlayX === "end" ? "left" : "right";
    } else {
      horizontalStyleProperty = position.overlayX === "end" ? "right" : "left";
    }
    if (horizontalStyleProperty === "right") {
      const documentWidth = this._document.documentElement.clientWidth;
      styles.right = `${documentWidth - (overlayPoint.x + this._overlayRect.width)}px`;
    } else {
      styles.left = coerceCssPixelValue(overlayPoint.x);
    }
    return styles;
  }
  _getScrollVisibility() {
    const originBounds = this._getOriginRect();
    const overlayBounds = this._pane.getBoundingClientRect();
    const scrollContainerBounds = this._scrollables.map((scrollable) => {
      return scrollable.getElementRef().nativeElement.getBoundingClientRect();
    });
    return {
      isOriginClipped: isElementClippedByScrolling(originBounds, scrollContainerBounds),
      isOriginOutsideView: isElementScrolledOutsideView(originBounds, scrollContainerBounds),
      isOverlayClipped: isElementClippedByScrolling(overlayBounds, scrollContainerBounds),
      isOverlayOutsideView: isElementScrolledOutsideView(overlayBounds, scrollContainerBounds)
    };
  }
  _subtractOverflows(length, ...overflows) {
    return overflows.reduce((currentValue, currentOverflow) => {
      return currentValue - Math.max(currentOverflow, 0);
    }, length);
  }
  _getNarrowedViewportRect() {
    const width = this._document.documentElement.clientWidth;
    const height = this._document.documentElement.clientHeight;
    const scrollPosition = this._viewportRuler.getViewportScrollPosition();
    return {
      top: scrollPosition.top + this._getViewportMarginTop(),
      left: scrollPosition.left + this._getViewportMarginStart(),
      right: scrollPosition.left + width - this._getViewportMarginEnd(),
      bottom: scrollPosition.top + height - this._getViewportMarginBottom(),
      width: width - this._getViewportMarginStart() - this._getViewportMarginEnd(),
      height: height - this._getViewportMarginTop() - this._getViewportMarginBottom()
    };
  }
  _isRtl() {
    return this._overlayRef.getDirection() === "rtl";
  }
  _hasExactPosition() {
    return !this._hasFlexibleDimensions || this._isPushed;
  }
  _getOffset(position, axis) {
    if (axis === "x") {
      return position.offsetX == null ? this._offsetX : position.offsetX;
    }
    return position.offsetY == null ? this._offsetY : position.offsetY;
  }
  _validatePositions() {
    if (typeof ngDevMode === "undefined" || ngDevMode) {
      if (!this._preferredPositions.length) {
        throw Error("FlexibleConnectedPositionStrategy: At least one position is required.");
      }
      this._preferredPositions.forEach((pair) => {
        validateHorizontalPosition("originX", pair.originX);
        validateVerticalPosition("originY", pair.originY);
        validateHorizontalPosition("overlayX", pair.overlayX);
        validateVerticalPosition("overlayY", pair.overlayY);
      });
    }
  }
  _addPanelClasses(cssClasses) {
    if (this._pane) {
      coerceArray(cssClasses).forEach((cssClass) => {
        if (cssClass !== "" && this._appliedPanelClasses.indexOf(cssClass) === -1) {
          this._appliedPanelClasses.push(cssClass);
          this._pane.classList.add(cssClass);
        }
      });
    }
  }
  _clearPanelClasses() {
    if (this._pane) {
      this._appliedPanelClasses.forEach((cssClass) => {
        this._pane.classList.remove(cssClass);
      });
      this._appliedPanelClasses = [];
    }
  }
  _getViewportMarginStart() {
    if (typeof this._viewportMargin === "number") return this._viewportMargin;
    return this._viewportMargin?.start ?? 0;
  }
  _getViewportMarginEnd() {
    if (typeof this._viewportMargin === "number") return this._viewportMargin;
    return this._viewportMargin?.end ?? 0;
  }
  _getViewportMarginTop() {
    if (typeof this._viewportMargin === "number") return this._viewportMargin;
    return this._viewportMargin?.top ?? 0;
  }
  _getViewportMarginBottom() {
    if (typeof this._viewportMargin === "number") return this._viewportMargin;
    return this._viewportMargin?.bottom ?? 0;
  }
  _getOriginRect() {
    const origin = this._origin;
    if (origin instanceof ElementRef) {
      return origin.nativeElement.getBoundingClientRect();
    }
    if (origin instanceof Element) {
      return origin.getBoundingClientRect();
    }
    const width = origin.width || 0;
    const height = origin.height || 0;
    return {
      top: origin.y,
      bottom: origin.y + height,
      left: origin.x,
      right: origin.x + width,
      height,
      width
    };
  }
  _getContainerRect() {
    const isInlinePopover = this._overlayRef.getConfig().usePopover && this._popoverLocation !== "global";
    const element = this._overlayContainer.getContainerElement();
    if (isInlinePopover) {
      element.style.display = "block";
    }
    const dimensions = element.getBoundingClientRect();
    if (isInlinePopover) {
      element.style.display = "";
    }
    return dimensions;
  }
};
function extendStyles(destination, source) {
  for (let key2 in source) {
    if (source.hasOwnProperty(key2)) {
      destination[key2] = source[key2];
    }
  }
  return destination;
}
function getPixelValue(input2) {
  if (typeof input2 !== "number" && input2 != null) {
    const [value, units] = input2.split(cssUnitPattern);
    return !units || units === "px" ? parseFloat(value) : null;
  }
  return input2 || null;
}
function getRoundedBoundingClientRect(clientRect) {
  return {
    top: Math.floor(clientRect.top),
    right: Math.floor(clientRect.right),
    bottom: Math.floor(clientRect.bottom),
    left: Math.floor(clientRect.left),
    width: Math.floor(clientRect.width),
    height: Math.floor(clientRect.height)
  };
}
function compareScrollVisibility(a, b) {
  if (a === b) {
    return true;
  }
  return a.isOriginClipped === b.isOriginClipped && a.isOriginOutsideView === b.isOriginOutsideView && a.isOverlayClipped === b.isOverlayClipped && a.isOverlayOutsideView === b.isOverlayOutsideView;
}
var wrapperClass = "cdk-global-overlay-wrapper";
function createGlobalPositionStrategy(_injector) {
  return new GlobalPositionStrategy();
}
var GlobalPositionStrategy = class {
  _overlayRef;
  _cssPosition = "static";
  _topOffset = "";
  _bottomOffset = "";
  _alignItems = "";
  _xPosition = "";
  _xOffset = "";
  _width = "";
  _height = "";
  _isDisposed = false;
  attach(overlayRef) {
    const config = overlayRef.getConfig();
    this._overlayRef = overlayRef;
    if (this._width && !config.width) {
      overlayRef.updateSize({
        width: this._width
      });
    }
    if (this._height && !config.height) {
      overlayRef.updateSize({
        height: this._height
      });
    }
    overlayRef.hostElement.classList.add(wrapperClass);
    this._isDisposed = false;
  }
  top(value = "") {
    this._bottomOffset = "";
    this._topOffset = value;
    this._alignItems = "flex-start";
    return this;
  }
  left(value = "") {
    this._xOffset = value;
    this._xPosition = "left";
    return this;
  }
  bottom(value = "") {
    this._topOffset = "";
    this._bottomOffset = value;
    this._alignItems = "flex-end";
    return this;
  }
  right(value = "") {
    this._xOffset = value;
    this._xPosition = "right";
    return this;
  }
  start(value = "") {
    this._xOffset = value;
    this._xPosition = "start";
    return this;
  }
  end(value = "") {
    this._xOffset = value;
    this._xPosition = "end";
    return this;
  }
  width(value = "") {
    if (this._overlayRef) {
      this._overlayRef.updateSize({
        width: value
      });
    } else {
      this._width = value;
    }
    return this;
  }
  height(value = "") {
    if (this._overlayRef) {
      this._overlayRef.updateSize({
        height: value
      });
    } else {
      this._height = value;
    }
    return this;
  }
  centerHorizontally(offset = "") {
    this.left(offset);
    this._xPosition = "center";
    return this;
  }
  centerVertically(offset = "") {
    this.top(offset);
    this._alignItems = "center";
    return this;
  }
  apply() {
    if (!this._overlayRef || !this._overlayRef.hasAttached()) {
      return;
    }
    const styles = this._overlayRef.overlayElement.style;
    const parentStyles = this._overlayRef.hostElement.style;
    const config = this._overlayRef.getConfig();
    const {
      width,
      height,
      maxWidth,
      maxHeight
    } = config;
    const shouldBeFlushHorizontally = (width === "100%" || width === "100vw") && (!maxWidth || maxWidth === "100%" || maxWidth === "100vw");
    const shouldBeFlushVertically = (height === "100%" || height === "100vh") && (!maxHeight || maxHeight === "100%" || maxHeight === "100vh");
    const xPosition = this._xPosition;
    const xOffset = this._xOffset;
    const isRtl = this._overlayRef.getConfig().direction === "rtl";
    let marginLeft = "";
    let marginRight = "";
    let justifyContent = "";
    if (shouldBeFlushHorizontally) {
      justifyContent = "flex-start";
    } else if (xPosition === "center") {
      justifyContent = "center";
      if (isRtl) {
        marginRight = xOffset;
      } else {
        marginLeft = xOffset;
      }
    } else if (isRtl) {
      if (xPosition === "left" || xPosition === "end") {
        justifyContent = "flex-end";
        marginLeft = xOffset;
      } else if (xPosition === "right" || xPosition === "start") {
        justifyContent = "flex-start";
        marginRight = xOffset;
      }
    } else if (xPosition === "left" || xPosition === "start") {
      justifyContent = "flex-start";
      marginLeft = xOffset;
    } else if (xPosition === "right" || xPosition === "end") {
      justifyContent = "flex-end";
      marginRight = xOffset;
    }
    styles.position = this._cssPosition;
    styles.marginLeft = shouldBeFlushHorizontally ? "0" : marginLeft;
    styles.marginTop = shouldBeFlushVertically ? "0" : this._topOffset;
    styles.marginBottom = this._bottomOffset;
    styles.marginRight = shouldBeFlushHorizontally ? "0" : marginRight;
    parentStyles.justifyContent = justifyContent;
    parentStyles.alignItems = shouldBeFlushVertically ? "flex-start" : this._alignItems;
  }
  dispose() {
    if (this._isDisposed || !this._overlayRef) {
      return;
    }
    const styles = this._overlayRef.overlayElement.style;
    const parent = this._overlayRef.hostElement;
    const parentStyles = parent.style;
    parent.classList.remove(wrapperClass);
    parentStyles.justifyContent = parentStyles.alignItems = styles.marginTop = styles.marginBottom = styles.marginLeft = styles.marginRight = styles.position = "";
    this._overlayRef = null;
    this._isDisposed = true;
  }
};
var OverlayPositionBuilder = class _OverlayPositionBuilder {
  _injector = inject(Injector);
  constructor() {
  }
  global() {
    return createGlobalPositionStrategy();
  }
  flexibleConnectedTo(origin) {
    return createFlexibleConnectedPositionStrategy(this._injector, origin);
  }
  static ɵfac = function OverlayPositionBuilder_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _OverlayPositionBuilder)();
  };
  static ɵprov = ɵɵdefineInjectable({
    token: _OverlayPositionBuilder,
    factory: _OverlayPositionBuilder.ɵfac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(OverlayPositionBuilder, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [], null);
})();
var OVERLAY_DEFAULT_CONFIG = new InjectionToken("OVERLAY_DEFAULT_CONFIG");
function createOverlayRef(injector, config) {
  injector.get(_CdkPrivateStyleLoader).load(_CdkOverlayStyleLoader);
  const overlayContainer = injector.get(OverlayContainer);
  const doc = injector.get(DOCUMENT);
  const idGenerator = injector.get(_IdGenerator);
  const appRef = injector.get(ApplicationRef);
  const directionality = injector.get(Directionality);
  const renderer = injector.get(Renderer2, null, {
    optional: true
  }) || injector.get(RendererFactory2).createRenderer(null, null);
  const overlayConfig = new OverlayConfig(config);
  const defaultUsePopover = injector.get(OVERLAY_DEFAULT_CONFIG, null, {
    optional: true
  })?.usePopover ?? true;
  overlayConfig.direction = overlayConfig.direction || directionality.value;
  if (!("showPopover" in doc.body)) {
    overlayConfig.usePopover = false;
  } else {
    overlayConfig.usePopover = config?.usePopover ?? defaultUsePopover;
  }
  const pane = doc.createElement("div");
  const host = doc.createElement("div");
  pane.id = idGenerator.getId("cdk-overlay-");
  pane.classList.add("cdk-overlay-pane");
  host.appendChild(pane);
  if (overlayConfig.usePopover) {
    host.setAttribute("popover", "manual");
    host.classList.add("cdk-overlay-popover");
  }
  const customInsertionPoint = overlayConfig.usePopover ? overlayConfig.positionStrategy?.getPopoverInsertionPoint?.() : null;
  if (isElement(customInsertionPoint)) {
    customInsertionPoint.after(host);
  } else if (customInsertionPoint?.type === "parent") {
    customInsertionPoint.element.appendChild(host);
  } else {
    overlayContainer.getContainerElement().appendChild(host);
  }
  return new OverlayRef(new DomPortalOutlet(pane, appRef, injector), host, pane, overlayConfig, injector.get(NgZone), injector.get(OverlayKeyboardDispatcher), doc, injector.get(Location), injector.get(OverlayOutsideClickDispatcher), config?.disableAnimations ?? injector.get(ANIMATION_MODULE_TYPE, null, {
    optional: true
  }) === "NoopAnimations", injector.get(EnvironmentInjector), renderer);
}
var Overlay = class _Overlay {
  scrollStrategies = inject(ScrollStrategyOptions);
  _positionBuilder = inject(OverlayPositionBuilder);
  _injector = inject(Injector);
  constructor() {
  }
  create(config) {
    return createOverlayRef(this._injector, config);
  }
  position() {
    return this._positionBuilder;
  }
  static ɵfac = function Overlay_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _Overlay)();
  };
  static ɵprov = ɵɵdefineInjectable({
    token: _Overlay,
    factory: _Overlay.ɵfac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(Overlay, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [], null);
})();
var defaultPositionList = [{
  originX: "start",
  originY: "bottom",
  overlayX: "start",
  overlayY: "top"
}, {
  originX: "start",
  originY: "top",
  overlayX: "start",
  overlayY: "bottom"
}, {
  originX: "end",
  originY: "top",
  overlayX: "end",
  overlayY: "bottom"
}, {
  originX: "end",
  originY: "bottom",
  overlayX: "end",
  overlayY: "top"
}];
var CDK_CONNECTED_OVERLAY_SCROLL_STRATEGY = new InjectionToken("cdk-connected-overlay-scroll-strategy", {
  providedIn: "root",
  factory: () => {
    const injector = inject(Injector);
    return () => createRepositionScrollStrategy(injector);
  }
});
var CdkOverlayOrigin = class _CdkOverlayOrigin {
  elementRef = inject(ElementRef);
  constructor() {
  }
  static ɵfac = function CdkOverlayOrigin_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _CdkOverlayOrigin)();
  };
  static ɵdir = ɵɵdefineDirective({
    type: _CdkOverlayOrigin,
    selectors: [["", "cdk-overlay-origin", ""], ["", "overlay-origin", ""], ["", "cdkOverlayOrigin", ""]],
    exportAs: ["cdkOverlayOrigin"]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(CdkOverlayOrigin, [{
    type: Directive,
    args: [{
      selector: "[cdk-overlay-origin], [overlay-origin], [cdkOverlayOrigin]",
      exportAs: "cdkOverlayOrigin"
    }]
  }], () => [], null);
})();
var CDK_CONNECTED_OVERLAY_DEFAULT_CONFIG = new InjectionToken("cdk-connected-overlay-default-config");
var CdkConnectedOverlay = class _CdkConnectedOverlay {
  _dir = inject(Directionality, {
    optional: true
  });
  _injector = inject(Injector);
  _overlayRef;
  _templatePortal;
  _backdropSubscription = Subscription.EMPTY;
  _attachSubscription = Subscription.EMPTY;
  _detachSubscription = Subscription.EMPTY;
  _positionSubscription = Subscription.EMPTY;
  _offsetX;
  _offsetY;
  _position;
  _scrollStrategyFactory = inject(CDK_CONNECTED_OVERLAY_SCROLL_STRATEGY);
  _ngZone = inject(NgZone);
  origin;
  positions;
  positionStrategy;
  get offsetX() {
    return this._offsetX;
  }
  set offsetX(offsetX) {
    this._offsetX = offsetX;
    if (this._position) {
      this._updatePositionStrategy(this._position);
    }
  }
  get offsetY() {
    return this._offsetY;
  }
  set offsetY(offsetY) {
    this._offsetY = offsetY;
    if (this._position) {
      this._updatePositionStrategy(this._position);
    }
  }
  width;
  height;
  minWidth;
  minHeight;
  backdropClass;
  panelClass;
  viewportMargin = 0;
  scrollStrategy;
  open = false;
  disableClose = false;
  transformOriginSelector;
  hasBackdrop = false;
  lockPosition = false;
  flexibleDimensions = false;
  growAfterOpen = false;
  push = false;
  disposeOnNavigation = false;
  usePopover;
  matchWidth = false;
  set _config(value) {
    if (typeof value !== "string") {
      this._assignConfig(value);
    }
  }
  backdropClick = new EventEmitter();
  positionChange = new EventEmitter();
  attach = new EventEmitter();
  detach = new EventEmitter();
  overlayKeydown = new EventEmitter();
  overlayOutsideClick = new EventEmitter();
  constructor() {
    const templateRef = inject(TemplateRef);
    const viewContainerRef = inject(ViewContainerRef);
    const defaultConfig = inject(CDK_CONNECTED_OVERLAY_DEFAULT_CONFIG, {
      optional: true
    });
    const globalConfig = inject(OVERLAY_DEFAULT_CONFIG, {
      optional: true
    });
    this.usePopover = globalConfig?.usePopover === false ? null : "global";
    this._templatePortal = new TemplatePortal(templateRef, viewContainerRef);
    this.scrollStrategy = this._scrollStrategyFactory();
    if (defaultConfig) {
      this._assignConfig(defaultConfig);
    }
  }
  get overlayRef() {
    return this._overlayRef;
  }
  get dir() {
    return this._dir ? this._dir.value : "ltr";
  }
  ngOnDestroy() {
    this._attachSubscription.unsubscribe();
    this._detachSubscription.unsubscribe();
    this._backdropSubscription.unsubscribe();
    this._positionSubscription.unsubscribe();
    this._overlayRef?.dispose();
  }
  ngOnChanges(changes) {
    if (this._position) {
      this._updatePositionStrategy(this._position);
      this._overlayRef?.updateSize({
        width: this._getWidth(),
        minWidth: this.minWidth,
        height: this.height,
        minHeight: this.minHeight
      });
      if (changes["origin"] && this.open) {
        this._position.apply();
      }
    }
    if (changes["open"]) {
      this.open ? this.attachOverlay() : this.detachOverlay();
    }
  }
  _createOverlay() {
    if (!this.positions || !this.positions.length) {
      this.positions = defaultPositionList;
    }
    const overlayRef = this._overlayRef = createOverlayRef(this._injector, this._buildConfig());
    this._attachSubscription = overlayRef.attachments().subscribe(() => this.attach.emit());
    this._detachSubscription = overlayRef.detachments().subscribe(() => this.detach.emit());
    overlayRef.keydownEvents().subscribe((event) => {
      this.overlayKeydown.next(event);
      if (event.keyCode === ESCAPE && !this.disableClose && !hasModifierKey(event)) {
        event.preventDefault();
        this.detachOverlay();
      }
    });
    this._overlayRef.outsidePointerEvents().subscribe((event) => {
      const origin = this._getOriginElement();
      const target = _getEventTarget(event);
      if (!origin || origin !== target && !origin.contains(target)) {
        this.overlayOutsideClick.next(event);
      }
    });
  }
  _buildConfig() {
    const positionStrategy = this._position = this.positionStrategy || this._createPositionStrategy();
    const overlayConfig = new OverlayConfig({
      direction: this._dir || "ltr",
      positionStrategy,
      scrollStrategy: this.scrollStrategy,
      hasBackdrop: this.hasBackdrop,
      disposeOnNavigation: this.disposeOnNavigation,
      usePopover: !!this.usePopover
    });
    if (this.height || this.height === 0) {
      overlayConfig.height = this.height;
    }
    if (this.minWidth || this.minWidth === 0) {
      overlayConfig.minWidth = this.minWidth;
    }
    if (this.minHeight || this.minHeight === 0) {
      overlayConfig.minHeight = this.minHeight;
    }
    if (this.backdropClass) {
      overlayConfig.backdropClass = this.backdropClass;
    }
    if (this.panelClass) {
      overlayConfig.panelClass = this.panelClass;
    }
    return overlayConfig;
  }
  _updatePositionStrategy(positionStrategy) {
    const positions = this.positions.map((currentPosition) => ({
      originX: currentPosition.originX,
      originY: currentPosition.originY,
      overlayX: currentPosition.overlayX,
      overlayY: currentPosition.overlayY,
      offsetX: currentPosition.offsetX || this.offsetX,
      offsetY: currentPosition.offsetY || this.offsetY,
      panelClass: currentPosition.panelClass || void 0
    }));
    return positionStrategy.setOrigin(this._getOrigin()).withPositions(positions).withFlexibleDimensions(this.flexibleDimensions).withPush(this.push).withGrowAfterOpen(this.growAfterOpen).withViewportMargin(this.viewportMargin).withLockedPosition(this.lockPosition).withTransformOriginOn(this.transformOriginSelector).withPopoverLocation(this.usePopover === null ? "global" : this.usePopover);
  }
  _createPositionStrategy() {
    const strategy = createFlexibleConnectedPositionStrategy(this._injector, this._getOrigin());
    this._updatePositionStrategy(strategy);
    return strategy;
  }
  _getOrigin() {
    if (this.origin instanceof CdkOverlayOrigin) {
      return this.origin.elementRef;
    } else {
      return this.origin;
    }
  }
  _getOriginElement() {
    if (this.origin instanceof CdkOverlayOrigin) {
      return this.origin.elementRef.nativeElement;
    }
    if (this.origin instanceof ElementRef) {
      return this.origin.nativeElement;
    }
    if (typeof Element !== "undefined" && this.origin instanceof Element) {
      return this.origin;
    }
    return null;
  }
  _getWidth() {
    if (this.width) {
      return this.width;
    }
    return this.matchWidth ? this._getOriginElement()?.getBoundingClientRect?.().width : void 0;
  }
  attachOverlay() {
    if (!this._overlayRef) {
      this._createOverlay();
    }
    const ref = this._overlayRef;
    ref.getConfig().hasBackdrop = this.hasBackdrop;
    ref.updateSize({
      width: this._getWidth()
    });
    if (!ref.hasAttached()) {
      ref.attach(this._templatePortal);
    }
    if (this.hasBackdrop) {
      this._backdropSubscription = ref.backdropClick().subscribe((event) => this.backdropClick.emit(event));
    } else {
      this._backdropSubscription.unsubscribe();
    }
    this._positionSubscription.unsubscribe();
    if (this.positionChange.observers.length > 0) {
      this._positionSubscription = this._position.positionChanges.pipe(takeWhile(() => this.positionChange.observers.length > 0)).subscribe((position) => {
        this._ngZone.run(() => this.positionChange.emit(position));
        if (this.positionChange.observers.length === 0) {
          this._positionSubscription.unsubscribe();
        }
      });
    }
    this.open = true;
  }
  detachOverlay() {
    this._overlayRef?.detach();
    this._backdropSubscription.unsubscribe();
    this._positionSubscription.unsubscribe();
    this.open = false;
  }
  _assignConfig(config) {
    this.origin = config.origin ?? this.origin;
    this.positions = config.positions ?? this.positions;
    this.positionStrategy = config.positionStrategy ?? this.positionStrategy;
    this.offsetX = config.offsetX ?? this.offsetX;
    this.offsetY = config.offsetY ?? this.offsetY;
    this.width = config.width ?? this.width;
    this.height = config.height ?? this.height;
    this.minWidth = config.minWidth ?? this.minWidth;
    this.minHeight = config.minHeight ?? this.minHeight;
    this.backdropClass = config.backdropClass ?? this.backdropClass;
    this.panelClass = config.panelClass ?? this.panelClass;
    this.viewportMargin = config.viewportMargin ?? this.viewportMargin;
    this.scrollStrategy = config.scrollStrategy ?? this.scrollStrategy;
    this.disableClose = config.disableClose ?? this.disableClose;
    this.transformOriginSelector = config.transformOriginSelector ?? this.transformOriginSelector;
    this.hasBackdrop = config.hasBackdrop ?? this.hasBackdrop;
    this.lockPosition = config.lockPosition ?? this.lockPosition;
    this.flexibleDimensions = config.flexibleDimensions ?? this.flexibleDimensions;
    this.growAfterOpen = config.growAfterOpen ?? this.growAfterOpen;
    this.push = config.push ?? this.push;
    this.disposeOnNavigation = config.disposeOnNavigation ?? this.disposeOnNavigation;
    this.usePopover = config.usePopover ?? this.usePopover;
    this.matchWidth = config.matchWidth ?? this.matchWidth;
  }
  static ɵfac = function CdkConnectedOverlay_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _CdkConnectedOverlay)();
  };
  static ɵdir = ɵɵdefineDirective({
    type: _CdkConnectedOverlay,
    selectors: [["", "cdk-connected-overlay", ""], ["", "connected-overlay", ""], ["", "cdkConnectedOverlay", ""]],
    inputs: {
      origin: [0, "cdkConnectedOverlayOrigin", "origin"],
      positions: [0, "cdkConnectedOverlayPositions", "positions"],
      positionStrategy: [0, "cdkConnectedOverlayPositionStrategy", "positionStrategy"],
      offsetX: [0, "cdkConnectedOverlayOffsetX", "offsetX"],
      offsetY: [0, "cdkConnectedOverlayOffsetY", "offsetY"],
      width: [0, "cdkConnectedOverlayWidth", "width"],
      height: [0, "cdkConnectedOverlayHeight", "height"],
      minWidth: [0, "cdkConnectedOverlayMinWidth", "minWidth"],
      minHeight: [0, "cdkConnectedOverlayMinHeight", "minHeight"],
      backdropClass: [0, "cdkConnectedOverlayBackdropClass", "backdropClass"],
      panelClass: [0, "cdkConnectedOverlayPanelClass", "panelClass"],
      viewportMargin: [0, "cdkConnectedOverlayViewportMargin", "viewportMargin"],
      scrollStrategy: [0, "cdkConnectedOverlayScrollStrategy", "scrollStrategy"],
      open: [0, "cdkConnectedOverlayOpen", "open"],
      disableClose: [0, "cdkConnectedOverlayDisableClose", "disableClose"],
      transformOriginSelector: [0, "cdkConnectedOverlayTransformOriginOn", "transformOriginSelector"],
      hasBackdrop: [2, "cdkConnectedOverlayHasBackdrop", "hasBackdrop", booleanAttribute],
      lockPosition: [2, "cdkConnectedOverlayLockPosition", "lockPosition", booleanAttribute],
      flexibleDimensions: [2, "cdkConnectedOverlayFlexibleDimensions", "flexibleDimensions", booleanAttribute],
      growAfterOpen: [2, "cdkConnectedOverlayGrowAfterOpen", "growAfterOpen", booleanAttribute],
      push: [2, "cdkConnectedOverlayPush", "push", booleanAttribute],
      disposeOnNavigation: [2, "cdkConnectedOverlayDisposeOnNavigation", "disposeOnNavigation", booleanAttribute],
      usePopover: [0, "cdkConnectedOverlayUsePopover", "usePopover"],
      matchWidth: [2, "cdkConnectedOverlayMatchWidth", "matchWidth", booleanAttribute],
      _config: [0, "cdkConnectedOverlay", "_config"]
    },
    outputs: {
      backdropClick: "backdropClick",
      positionChange: "positionChange",
      attach: "attach",
      detach: "detach",
      overlayKeydown: "overlayKeydown",
      overlayOutsideClick: "overlayOutsideClick"
    },
    exportAs: ["cdkConnectedOverlay"],
    features: [ɵɵNgOnChangesFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(CdkConnectedOverlay, [{
    type: Directive,
    args: [{
      selector: "[cdk-connected-overlay], [connected-overlay], [cdkConnectedOverlay]",
      exportAs: "cdkConnectedOverlay"
    }]
  }], () => [], {
    origin: [{
      type: Input,
      args: ["cdkConnectedOverlayOrigin"]
    }],
    positions: [{
      type: Input,
      args: ["cdkConnectedOverlayPositions"]
    }],
    positionStrategy: [{
      type: Input,
      args: ["cdkConnectedOverlayPositionStrategy"]
    }],
    offsetX: [{
      type: Input,
      args: ["cdkConnectedOverlayOffsetX"]
    }],
    offsetY: [{
      type: Input,
      args: ["cdkConnectedOverlayOffsetY"]
    }],
    width: [{
      type: Input,
      args: ["cdkConnectedOverlayWidth"]
    }],
    height: [{
      type: Input,
      args: ["cdkConnectedOverlayHeight"]
    }],
    minWidth: [{
      type: Input,
      args: ["cdkConnectedOverlayMinWidth"]
    }],
    minHeight: [{
      type: Input,
      args: ["cdkConnectedOverlayMinHeight"]
    }],
    backdropClass: [{
      type: Input,
      args: ["cdkConnectedOverlayBackdropClass"]
    }],
    panelClass: [{
      type: Input,
      args: ["cdkConnectedOverlayPanelClass"]
    }],
    viewportMargin: [{
      type: Input,
      args: ["cdkConnectedOverlayViewportMargin"]
    }],
    scrollStrategy: [{
      type: Input,
      args: ["cdkConnectedOverlayScrollStrategy"]
    }],
    open: [{
      type: Input,
      args: ["cdkConnectedOverlayOpen"]
    }],
    disableClose: [{
      type: Input,
      args: ["cdkConnectedOverlayDisableClose"]
    }],
    transformOriginSelector: [{
      type: Input,
      args: ["cdkConnectedOverlayTransformOriginOn"]
    }],
    hasBackdrop: [{
      type: Input,
      args: [{
        alias: "cdkConnectedOverlayHasBackdrop",
        transform: booleanAttribute
      }]
    }],
    lockPosition: [{
      type: Input,
      args: [{
        alias: "cdkConnectedOverlayLockPosition",
        transform: booleanAttribute
      }]
    }],
    flexibleDimensions: [{
      type: Input,
      args: [{
        alias: "cdkConnectedOverlayFlexibleDimensions",
        transform: booleanAttribute
      }]
    }],
    growAfterOpen: [{
      type: Input,
      args: [{
        alias: "cdkConnectedOverlayGrowAfterOpen",
        transform: booleanAttribute
      }]
    }],
    push: [{
      type: Input,
      args: [{
        alias: "cdkConnectedOverlayPush",
        transform: booleanAttribute
      }]
    }],
    disposeOnNavigation: [{
      type: Input,
      args: [{
        alias: "cdkConnectedOverlayDisposeOnNavigation",
        transform: booleanAttribute
      }]
    }],
    usePopover: [{
      type: Input,
      args: [{
        alias: "cdkConnectedOverlayUsePopover"
      }]
    }],
    matchWidth: [{
      type: Input,
      args: [{
        alias: "cdkConnectedOverlayMatchWidth",
        transform: booleanAttribute
      }]
    }],
    _config: [{
      type: Input,
      args: ["cdkConnectedOverlay"]
    }],
    backdropClick: [{
      type: Output
    }],
    positionChange: [{
      type: Output
    }],
    attach: [{
      type: Output
    }],
    detach: [{
      type: Output
    }],
    overlayKeydown: [{
      type: Output
    }],
    overlayOutsideClick: [{
      type: Output
    }]
  });
})();
var OverlayModule = class _OverlayModule {
  static ɵfac = function OverlayModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _OverlayModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _OverlayModule,
    imports: [BidiModule, PortalModule, ScrollingModule, CdkConnectedOverlay, CdkOverlayOrigin],
    exports: [CdkConnectedOverlay, CdkOverlayOrigin, ScrollingModule]
  });
  static ɵinj = ɵɵdefineInjector({
    providers: [Overlay],
    imports: [BidiModule, PortalModule, ScrollingModule, ScrollingModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(OverlayModule, [{
    type: NgModule,
    args: [{
      imports: [BidiModule, PortalModule, ScrollingModule, CdkConnectedOverlay, CdkOverlayOrigin],
      exports: [CdkConnectedOverlay, CdkOverlayOrigin, ScrollingModule],
      providers: [Overlay]
    }]
  }], null, null);
})();

// node_modules/@angular/cdk/fesm2022/overlay.mjs
var FullscreenOverlayContainer = class _FullscreenOverlayContainer extends OverlayContainer {
  _renderer = inject(RendererFactory2).createRenderer(null, null);
  _fullScreenEventName;
  _cleanupFullScreenListener;
  constructor() {
    super();
  }
  ngOnDestroy() {
    super.ngOnDestroy();
    this._cleanupFullScreenListener?.();
  }
  _createContainer() {
    const eventName = this._getEventName();
    super._createContainer();
    this._adjustParentForFullscreenChange();
    if (eventName) {
      this._cleanupFullScreenListener?.();
      this._cleanupFullScreenListener = this._renderer.listen("document", eventName, () => {
        this._adjustParentForFullscreenChange();
      });
    }
  }
  _adjustParentForFullscreenChange() {
    if (this._containerElement) {
      const fullscreenElement = this.getFullscreenElement();
      const parent = fullscreenElement || this._document.body;
      parent.appendChild(this._containerElement);
    }
  }
  _getEventName() {
    if (!this._fullScreenEventName) {
      const _document = this._document;
      if (_document.fullscreenEnabled) {
        this._fullScreenEventName = "fullscreenchange";
      } else if (_document.webkitFullscreenEnabled) {
        this._fullScreenEventName = "webkitfullscreenchange";
      } else if (_document.mozFullScreenEnabled) {
        this._fullScreenEventName = "mozfullscreenchange";
      } else if (_document.msFullscreenEnabled) {
        this._fullScreenEventName = "MSFullscreenChange";
      }
    }
    return this._fullScreenEventName;
  }
  getFullscreenElement() {
    const _document = this._document;
    return _document.fullscreenElement || _document.webkitFullscreenElement || _document.mozFullScreenElement || _document.msFullscreenElement || null;
  }
  static ɵfac = function FullscreenOverlayContainer_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _FullscreenOverlayContainer)();
  };
  static ɵprov = ɵɵdefineInjectable({
    token: _FullscreenOverlayContainer,
    factory: _FullscreenOverlayContainer.ɵfac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(FullscreenOverlayContainer, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [], null);
})();

// node_modules/@angular/cdk/fesm2022/_fake-event-detection-chunk.mjs
function isFakeMousedownFromScreenReader(event) {
  return event.buttons === 0 || event.detail === 0;
}
function isFakeTouchstartFromScreenReader(event) {
  const touch = event.touches && event.touches[0] || event.changedTouches && event.changedTouches[0];
  return !!touch && touch.identifier === -1 && (touch.radiusX == null || touch.radiusX === 1) && (touch.radiusY == null || touch.radiusY === 1);
}

// node_modules/@angular/cdk/fesm2022/drag-drop.mjs
function deepCloneNode(node) {
  const clone5 = node.cloneNode(true);
  const descendantsWithId = clone5.querySelectorAll("[id]");
  const nodeName = node.nodeName.toLowerCase();
  clone5.removeAttribute("id");
  for (let i2 = 0; i2 < descendantsWithId.length; i2++) {
    descendantsWithId[i2].removeAttribute("id");
  }
  if (nodeName === "canvas") {
    transferCanvasData(node, clone5);
  } else if (nodeName === "input" || nodeName === "select" || nodeName === "textarea") {
    transferInputData(node, clone5);
  }
  transferData("canvas", node, clone5, transferCanvasData);
  transferData("input, textarea, select", node, clone5, transferInputData);
  return clone5;
}
function transferData(selector2, node, clone5, callback) {
  const descendantElements = node.querySelectorAll(selector2);
  if (descendantElements.length) {
    const cloneElements = clone5.querySelectorAll(selector2);
    for (let i2 = 0; i2 < descendantElements.length; i2++) {
      callback(descendantElements[i2], cloneElements[i2]);
    }
  }
}
var cloneUniqueId = 0;
function transferInputData(source, clone5) {
  if (clone5.type !== "file") {
    clone5.value = source.value;
  }
  if (clone5.type === "radio" && clone5.name) {
    clone5.name = `mat-clone-${clone5.name}-${cloneUniqueId++}`;
  }
}
function transferCanvasData(source, clone5) {
  const context = clone5.getContext("2d");
  if (context) {
    try {
      context.drawImage(source, 0, 0);
    } catch (e2) {
    }
  }
}
function getMutableClientRect(element) {
  const rect = element.getBoundingClientRect();
  return {
    top: rect.top,
    right: rect.right,
    bottom: rect.bottom,
    left: rect.left,
    width: rect.width,
    height: rect.height,
    x: rect.x,
    y: rect.y
  };
}
function isInsideClientRect(clientRect, x, y) {
  const {
    top,
    bottom,
    left,
    right
  } = clientRect;
  return y >= top && y <= bottom && x >= left && x <= right;
}
function isOverflowingParent(parentRect, childRect) {
  const isLeftOverflowing = childRect.left < parentRect.left;
  const isRightOverflowing = childRect.left + childRect.width > parentRect.right;
  const isTopOverflowing = childRect.top < parentRect.top;
  const isBottomOverflowing = childRect.top + childRect.height > parentRect.bottom;
  return isLeftOverflowing || isRightOverflowing || isTopOverflowing || isBottomOverflowing;
}
function adjustDomRect(domRect, top, left) {
  domRect.top += top;
  domRect.bottom = domRect.top + domRect.height;
  domRect.left += left;
  domRect.right = domRect.left + domRect.width;
}
function isPointerNearDomRect(rect, threshold, pointerX, pointerY) {
  const {
    top,
    right,
    bottom,
    left,
    width,
    height
  } = rect;
  const xThreshold = width * threshold;
  const yThreshold = height * threshold;
  return pointerY > top - yThreshold && pointerY < bottom + yThreshold && pointerX > left - xThreshold && pointerX < right + xThreshold;
}
var ParentPositionTracker = class {
  _document;
  positions = /* @__PURE__ */ new Map();
  constructor(_document) {
    this._document = _document;
  }
  clear() {
    this.positions.clear();
  }
  cache(elements) {
    this.clear();
    this.positions.set(this._document, {
      scrollPosition: this.getViewportScrollPosition()
    });
    elements.forEach((element) => {
      this.positions.set(element, {
        scrollPosition: {
          top: element.scrollTop,
          left: element.scrollLeft
        },
        clientRect: getMutableClientRect(element)
      });
    });
  }
  handleScroll(event) {
    const target = _getEventTarget(event);
    const cachedPosition = this.positions.get(target);
    if (!cachedPosition) {
      return null;
    }
    const scrollPosition = cachedPosition.scrollPosition;
    let newTop;
    let newLeft;
    if (target === this._document) {
      const viewportScrollPosition = this.getViewportScrollPosition();
      newTop = viewportScrollPosition.top;
      newLeft = viewportScrollPosition.left;
    } else {
      newTop = target.scrollTop;
      newLeft = target.scrollLeft;
    }
    const topDifference = scrollPosition.top - newTop;
    const leftDifference = scrollPosition.left - newLeft;
    this.positions.forEach((position, node) => {
      if (position.clientRect && target !== node && target.contains(node)) {
        adjustDomRect(position.clientRect, topDifference, leftDifference);
      }
    });
    scrollPosition.top = newTop;
    scrollPosition.left = newLeft;
    return {
      top: topDifference,
      left: leftDifference
    };
  }
  getViewportScrollPosition() {
    return {
      top: window.scrollY,
      left: window.scrollX
    };
  }
};
function getRootNode(viewRef, _document) {
  const rootNodes = viewRef.rootNodes;
  if (rootNodes.length === 1 && rootNodes[0].nodeType === _document.ELEMENT_NODE) {
    return rootNodes[0];
  }
  const wrapper = _document.createElement("div");
  rootNodes.forEach((node) => wrapper.appendChild(node));
  return wrapper;
}
function extendStyles2(dest, source, importantProperties2) {
  for (let key2 in source) {
    if (source.hasOwnProperty(key2)) {
      const value = source[key2];
      if (value) {
        dest.setProperty(key2, value, importantProperties2?.has(key2) ? "important" : "");
      } else {
        dest.removeProperty(key2);
      }
    }
  }
  return dest;
}
function toggleNativeDragInteractions(element, enable) {
  const userSelect = enable ? "" : "none";
  extendStyles2(element.style, {
    "touch-action": enable ? "" : "none",
    "-webkit-user-drag": enable ? "" : "none",
    "-webkit-tap-highlight-color": enable ? "" : "transparent",
    "user-select": userSelect,
    "-ms-user-select": userSelect,
    "-webkit-user-select": userSelect,
    "-moz-user-select": userSelect
  });
}
function toggleVisibility(element, enable, importantProperties2) {
  extendStyles2(element.style, {
    position: enable ? "" : "fixed",
    top: enable ? "" : "0",
    opacity: enable ? "" : "0",
    left: enable ? "" : "-999em"
  }, importantProperties2);
}
function combineTransforms(transform, initialTransform) {
  return initialTransform && initialTransform != "none" ? transform + " " + initialTransform : transform;
}
function matchElementSize(target, sourceRect) {
  target.style.width = `${sourceRect.width}px`;
  target.style.height = `${sourceRect.height}px`;
  target.style.transform = getTransform(sourceRect.left, sourceRect.top);
}
function getTransform(x, y) {
  return `translate3d(${Math.round(x)}px, ${Math.round(y)}px, 0)`;
}
var capturingEventOptions = {
  capture: true
};
var activeCapturingEventOptions$1 = {
  passive: false,
  capture: true
};
var _ResetsLoader = class __ResetsLoader {
  static ɵfac = function _ResetsLoader_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || __ResetsLoader)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: __ResetsLoader,
    selectors: [["ng-component"]],
    hostAttrs: ["cdk-drag-resets-container", ""],
    decls: 0,
    vars: 0,
    template: function _ResetsLoader_Template(rf, ctx) {
    },
    styles: ["@layer cdk-resets {\n  .cdk-drag-preview {\n    background: none;\n    border: none;\n    padding: 0;\n    color: inherit;\n    inset: auto;\n  }\n}\n.cdk-drag-placeholder *,\n.cdk-drag-preview * {\n  pointer-events: none !important;\n}\n"],
    encapsulation: 2,
    changeDetection: 0
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(_ResetsLoader, [{
    type: Component,
    args: [{
      encapsulation: ViewEncapsulation.None,
      template: "",
      changeDetection: ChangeDetectionStrategy.OnPush,
      host: {
        "cdk-drag-resets-container": ""
      },
      styles: ["@layer cdk-resets {\n  .cdk-drag-preview {\n    background: none;\n    border: none;\n    padding: 0;\n    color: inherit;\n    inset: auto;\n  }\n}\n.cdk-drag-placeholder *,\n.cdk-drag-preview * {\n  pointer-events: none !important;\n}\n"]
    }]
  }], null, null);
})();
var DragDropRegistry = class _DragDropRegistry {
  _ngZone = inject(NgZone);
  _document = inject(DOCUMENT);
  _styleLoader = inject(_CdkPrivateStyleLoader);
  _renderer = inject(RendererFactory2).createRenderer(null, null);
  _cleanupDocumentTouchmove;
  _scroll = new Subject();
  _dropInstances = /* @__PURE__ */ new Set();
  _dragInstances = /* @__PURE__ */ new Set();
  _activeDragInstances = signal([], ...ngDevMode ? [{
    debugName: "_activeDragInstances"
  }] : []);
  _globalListeners;
  _draggingPredicate = (item) => item.isDragging();
  _domNodesToDirectives = null;
  pointerMove = new Subject();
  pointerUp = new Subject();
  constructor() {
  }
  registerDropContainer(drop) {
    if (!this._dropInstances.has(drop)) {
      this._dropInstances.add(drop);
    }
  }
  registerDragItem(drag) {
    this._dragInstances.add(drag);
    if (this._dragInstances.size === 1) {
      this._ngZone.runOutsideAngular(() => {
        this._cleanupDocumentTouchmove?.();
        this._cleanupDocumentTouchmove = this._renderer.listen(this._document, "touchmove", this._persistentTouchmoveListener, activeCapturingEventOptions$1);
      });
    }
  }
  removeDropContainer(drop) {
    this._dropInstances.delete(drop);
  }
  removeDragItem(drag) {
    this._dragInstances.delete(drag);
    this.stopDragging(drag);
    if (this._dragInstances.size === 0) {
      this._cleanupDocumentTouchmove?.();
    }
  }
  startDragging(drag, event) {
    if (this._activeDragInstances().indexOf(drag) > -1) {
      return;
    }
    this._styleLoader.load(_ResetsLoader);
    this._activeDragInstances.update((instances3) => [...instances3, drag]);
    if (this._activeDragInstances().length === 1) {
      const isTouchEvent2 = event.type.startsWith("touch");
      const endEventHandler = (e2) => this.pointerUp.next(e2);
      const toBind = [["scroll", (e2) => this._scroll.next(e2), capturingEventOptions], ["selectstart", this._preventDefaultWhileDragging, activeCapturingEventOptions$1]];
      if (isTouchEvent2) {
        toBind.push(["touchend", endEventHandler, capturingEventOptions], ["touchcancel", endEventHandler, capturingEventOptions]);
      } else {
        toBind.push(["mouseup", endEventHandler, capturingEventOptions]);
      }
      if (!isTouchEvent2) {
        toBind.push(["mousemove", (e2) => this.pointerMove.next(e2), activeCapturingEventOptions$1]);
      }
      this._ngZone.runOutsideAngular(() => {
        this._globalListeners = toBind.map(([name, handler, options]) => this._renderer.listen(this._document, name, handler, options));
      });
    }
  }
  stopDragging(drag) {
    this._activeDragInstances.update((instances3) => {
      const index = instances3.indexOf(drag);
      if (index > -1) {
        instances3.splice(index, 1);
        return [...instances3];
      }
      return instances3;
    });
    if (this._activeDragInstances().length === 0) {
      this._clearGlobalListeners();
    }
  }
  isDragging(drag) {
    return this._activeDragInstances().indexOf(drag) > -1;
  }
  scrolled(shadowRoot) {
    const streams = [this._scroll];
    if (shadowRoot && shadowRoot !== this._document) {
      streams.push(new Observable((observer) => {
        return this._ngZone.runOutsideAngular(() => {
          const cleanup = this._renderer.listen(shadowRoot, "scroll", (event) => {
            if (this._activeDragInstances().length) {
              observer.next(event);
            }
          }, capturingEventOptions);
          return () => {
            cleanup();
          };
        });
      }));
    }
    return merge(...streams);
  }
  registerDirectiveNode(node, dragRef) {
    this._domNodesToDirectives ??= /* @__PURE__ */ new WeakMap();
    this._domNodesToDirectives.set(node, dragRef);
  }
  removeDirectiveNode(node) {
    this._domNodesToDirectives?.delete(node);
  }
  getDragDirectiveForNode(node) {
    return this._domNodesToDirectives?.get(node) || null;
  }
  ngOnDestroy() {
    this._dragInstances.forEach((instance) => this.removeDragItem(instance));
    this._dropInstances.forEach((instance) => this.removeDropContainer(instance));
    this._domNodesToDirectives = null;
    this._clearGlobalListeners();
    this.pointerMove.complete();
    this.pointerUp.complete();
  }
  _preventDefaultWhileDragging = (event) => {
    if (this._activeDragInstances().length > 0) {
      event.preventDefault();
    }
  };
  _persistentTouchmoveListener = (event) => {
    if (this._activeDragInstances().length > 0) {
      if (this._activeDragInstances().some(this._draggingPredicate)) {
        event.preventDefault();
      }
      this.pointerMove.next(event);
    }
  };
  _clearGlobalListeners() {
    this._globalListeners?.forEach((cleanup) => cleanup());
    this._globalListeners = void 0;
  }
  static ɵfac = function DragDropRegistry_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _DragDropRegistry)();
  };
  static ɵprov = ɵɵdefineInjectable({
    token: _DragDropRegistry,
    factory: _DragDropRegistry.ɵfac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(DragDropRegistry, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [], null);
})();
function parseCssTimeUnitsToMs(value) {
  const multiplier = value.toLowerCase().indexOf("ms") > -1 ? 1 : 1e3;
  return parseFloat(value) * multiplier;
}
function getTransformTransitionDurationInMs(element) {
  const computedStyle = getComputedStyle(element);
  const transitionedProperties = parseCssPropertyValue(computedStyle, "transition-property");
  const property = transitionedProperties.find((prop) => prop === "transform" || prop === "all");
  if (!property) {
    return 0;
  }
  const propertyIndex = transitionedProperties.indexOf(property);
  const rawDurations = parseCssPropertyValue(computedStyle, "transition-duration");
  const rawDelays = parseCssPropertyValue(computedStyle, "transition-delay");
  return parseCssTimeUnitsToMs(rawDurations[propertyIndex]) + parseCssTimeUnitsToMs(rawDelays[propertyIndex]);
}
function parseCssPropertyValue(computedStyle, name) {
  const value = computedStyle.getPropertyValue(name);
  return value.split(",").map((part) => part.trim());
}
var importantProperties = /* @__PURE__ */ new Set(["position"]);
var PreviewRef = class {
  _document;
  _rootElement;
  _direction;
  _initialDomRect;
  _previewTemplate;
  _previewClass;
  _pickupPositionOnPage;
  _initialTransform;
  _zIndex;
  _renderer;
  _previewEmbeddedView = null;
  _preview;
  get element() {
    return this._preview;
  }
  constructor(_document, _rootElement, _direction, _initialDomRect, _previewTemplate, _previewClass, _pickupPositionOnPage, _initialTransform, _zIndex, _renderer) {
    this._document = _document;
    this._rootElement = _rootElement;
    this._direction = _direction;
    this._initialDomRect = _initialDomRect;
    this._previewTemplate = _previewTemplate;
    this._previewClass = _previewClass;
    this._pickupPositionOnPage = _pickupPositionOnPage;
    this._initialTransform = _initialTransform;
    this._zIndex = _zIndex;
    this._renderer = _renderer;
  }
  attach(parent) {
    this._preview = this._createPreview();
    parent.appendChild(this._preview);
    if (supportsPopover(this._preview)) {
      this._preview["showPopover"]();
    }
  }
  destroy() {
    this._preview.remove();
    this._previewEmbeddedView?.destroy();
    this._preview = this._previewEmbeddedView = null;
  }
  setTransform(value) {
    this._preview.style.transform = value;
  }
  getBoundingClientRect() {
    return this._preview.getBoundingClientRect();
  }
  addClass(className) {
    this._preview.classList.add(className);
  }
  getTransitionDuration() {
    return getTransformTransitionDurationInMs(this._preview);
  }
  addEventListener(name, handler) {
    return this._renderer.listen(this._preview, name, handler);
  }
  _createPreview() {
    const previewConfig = this._previewTemplate;
    const previewClass = this._previewClass;
    const previewTemplate = previewConfig ? previewConfig.template : null;
    let preview;
    if (previewTemplate && previewConfig) {
      const rootRect = previewConfig.matchSize ? this._initialDomRect : null;
      const viewRef = previewConfig.viewContainer.createEmbeddedView(previewTemplate, previewConfig.context);
      viewRef.detectChanges();
      preview = getRootNode(viewRef, this._document);
      this._previewEmbeddedView = viewRef;
      if (previewConfig.matchSize) {
        matchElementSize(preview, rootRect);
      } else {
        preview.style.transform = getTransform(this._pickupPositionOnPage.x, this._pickupPositionOnPage.y);
      }
    } else {
      preview = deepCloneNode(this._rootElement);
      matchElementSize(preview, this._initialDomRect);
      if (this._initialTransform) {
        preview.style.transform = this._initialTransform;
      }
    }
    extendStyles2(preview.style, {
      "pointer-events": "none",
      "margin": supportsPopover(preview) ? "0 auto 0 0" : "0",
      "position": "fixed",
      "top": "0",
      "left": "0",
      "z-index": this._zIndex + ""
    }, importantProperties);
    toggleNativeDragInteractions(preview, false);
    preview.classList.add("cdk-drag-preview");
    preview.setAttribute("popover", "manual");
    preview.setAttribute("dir", this._direction);
    if (previewClass) {
      if (Array.isArray(previewClass)) {
        previewClass.forEach((className) => preview.classList.add(className));
      } else {
        preview.classList.add(previewClass);
      }
    }
    return preview;
  }
};
function supportsPopover(element) {
  return "showPopover" in element;
}
var passiveEventListenerOptions = {
  passive: true
};
var activeEventListenerOptions = {
  passive: false
};
var activeCapturingEventOptions = {
  passive: false,
  capture: true
};
var MOUSE_EVENT_IGNORE_TIME = 800;
var PLACEHOLDER_CLASS = "cdk-drag-placeholder";
var dragImportantProperties = /* @__PURE__ */ new Set(["position"]);
function createDragRef(injector, element, config = {
  dragStartThreshold: 5,
  pointerDirectionChangeThreshold: 5
}) {
  const renderer = injector.get(Renderer2, null, {
    optional: true
  }) || injector.get(RendererFactory2).createRenderer(null, null);
  return new DragRef(element, config, injector.get(DOCUMENT), injector.get(NgZone), injector.get(ViewportRuler), injector.get(DragDropRegistry), renderer);
}
var DragRef = class {
  _config;
  _document;
  _ngZone;
  _viewportRuler;
  _dragDropRegistry;
  _renderer;
  _rootElementCleanups;
  _cleanupShadowRootSelectStart;
  _preview = null;
  _previewContainer;
  _placeholderRef = null;
  _placeholder;
  _pickupPositionInElement;
  _pickupPositionOnPage;
  _marker;
  _anchor = null;
  _passiveTransform = {
    x: 0,
    y: 0
  };
  _activeTransform = {
    x: 0,
    y: 0
  };
  _initialTransform;
  _hasStartedDragging = signal(false, ...ngDevMode ? [{
    debugName: "_hasStartedDragging"
  }] : []);
  _hasMoved = false;
  _initialContainer;
  _initialIndex;
  _parentPositions;
  _moveEvents = new Subject();
  _pointerDirectionDelta;
  _pointerPositionAtLastDirectionChange;
  _lastKnownPointerPosition;
  _rootElement;
  _ownerSVGElement = null;
  _rootElementTapHighlight;
  _pointerMoveSubscription = Subscription.EMPTY;
  _pointerUpSubscription = Subscription.EMPTY;
  _scrollSubscription = Subscription.EMPTY;
  _resizeSubscription = Subscription.EMPTY;
  _lastTouchEventTime;
  _dragStartTime;
  _boundaryElement = null;
  _nativeInteractionsEnabled = true;
  _initialDomRect;
  _previewRect;
  _boundaryRect;
  _previewTemplate;
  _placeholderTemplate;
  _handles = [];
  _disabledHandles = /* @__PURE__ */ new Set();
  _dropContainer;
  _direction = "ltr";
  _parentDragRef = null;
  _cachedShadowRoot;
  lockAxis = null;
  dragStartDelay = 0;
  previewClass;
  scale = 1;
  get disabled() {
    return this._disabled || !!(this._dropContainer && this._dropContainer.disabled);
  }
  set disabled(value) {
    if (value !== this._disabled) {
      this._disabled = value;
      this._toggleNativeDragInteractions();
      this._handles.forEach((handle) => toggleNativeDragInteractions(handle, value));
    }
  }
  _disabled = false;
  beforeStarted = new Subject();
  started = new Subject();
  released = new Subject();
  ended = new Subject();
  entered = new Subject();
  exited = new Subject();
  dropped = new Subject();
  moved = this._moveEvents;
  data;
  constrainPosition;
  constructor(element, _config, _document, _ngZone, _viewportRuler, _dragDropRegistry, _renderer) {
    this._config = _config;
    this._document = _document;
    this._ngZone = _ngZone;
    this._viewportRuler = _viewportRuler;
    this._dragDropRegistry = _dragDropRegistry;
    this._renderer = _renderer;
    this.withRootElement(element).withParent(_config.parentDragRef || null);
    this._parentPositions = new ParentPositionTracker(_document);
    _dragDropRegistry.registerDragItem(this);
  }
  getPlaceholderElement() {
    return this._placeholder;
  }
  getRootElement() {
    return this._rootElement;
  }
  getVisibleElement() {
    return this.isDragging() ? this.getPlaceholderElement() : this.getRootElement();
  }
  withHandles(handles) {
    this._handles = handles.map((handle) => coerceElement(handle));
    this._handles.forEach((handle) => toggleNativeDragInteractions(handle, this.disabled));
    this._toggleNativeDragInteractions();
    const disabledHandles = /* @__PURE__ */ new Set();
    this._disabledHandles.forEach((handle) => {
      if (this._handles.indexOf(handle) > -1) {
        disabledHandles.add(handle);
      }
    });
    this._disabledHandles = disabledHandles;
    return this;
  }
  withPreviewTemplate(template) {
    this._previewTemplate = template;
    return this;
  }
  withPlaceholderTemplate(template) {
    this._placeholderTemplate = template;
    return this;
  }
  withRootElement(rootElement) {
    const element = coerceElement(rootElement);
    if (element !== this._rootElement) {
      this._removeRootElementListeners();
      const renderer = this._renderer;
      this._rootElementCleanups = this._ngZone.runOutsideAngular(() => [renderer.listen(element, "mousedown", this._pointerDown, activeEventListenerOptions), renderer.listen(element, "touchstart", this._pointerDown, passiveEventListenerOptions), renderer.listen(element, "dragstart", this._nativeDragStart, activeEventListenerOptions)]);
      this._initialTransform = void 0;
      this._rootElement = element;
    }
    if (typeof SVGElement !== "undefined" && this._rootElement instanceof SVGElement) {
      this._ownerSVGElement = this._rootElement.ownerSVGElement;
    }
    return this;
  }
  withBoundaryElement(boundaryElement) {
    this._boundaryElement = boundaryElement ? coerceElement(boundaryElement) : null;
    this._resizeSubscription.unsubscribe();
    if (boundaryElement) {
      this._resizeSubscription = this._viewportRuler.change(10).subscribe(() => this._containInsideBoundaryOnResize());
    }
    return this;
  }
  withParent(parent) {
    this._parentDragRef = parent;
    return this;
  }
  dispose() {
    this._removeRootElementListeners();
    if (this.isDragging()) {
      this._rootElement?.remove();
    }
    this._marker?.remove();
    this._destroyPreview();
    this._destroyPlaceholder();
    this._dragDropRegistry.removeDragItem(this);
    this._removeListeners();
    this.beforeStarted.complete();
    this.started.complete();
    this.released.complete();
    this.ended.complete();
    this.entered.complete();
    this.exited.complete();
    this.dropped.complete();
    this._moveEvents.complete();
    this._handles = [];
    this._disabledHandles.clear();
    this._dropContainer = void 0;
    this._resizeSubscription.unsubscribe();
    this._parentPositions.clear();
    this._boundaryElement = this._rootElement = this._ownerSVGElement = this._placeholderTemplate = this._previewTemplate = this._marker = this._parentDragRef = null;
  }
  isDragging() {
    return this._hasStartedDragging() && this._dragDropRegistry.isDragging(this);
  }
  reset() {
    this._rootElement.style.transform = this._initialTransform || "";
    this._activeTransform = {
      x: 0,
      y: 0
    };
    this._passiveTransform = {
      x: 0,
      y: 0
    };
  }
  resetToBoundary() {
    if (this._boundaryElement && this._rootElement && isOverflowingParent(this._boundaryElement.getBoundingClientRect(), this._rootElement.getBoundingClientRect())) {
      const parentRect = this._boundaryElement.getBoundingClientRect();
      const childRect = this._rootElement.getBoundingClientRect();
      let offsetX = 0;
      let offsetY = 0;
      if (childRect.left < parentRect.left) {
        offsetX = parentRect.left - childRect.left;
      } else if (childRect.right > parentRect.right) {
        offsetX = parentRect.right - childRect.right;
      }
      if (childRect.top < parentRect.top) {
        offsetY = parentRect.top - childRect.top;
      } else if (childRect.bottom > parentRect.bottom) {
        offsetY = parentRect.bottom - childRect.bottom;
      }
      const currentLeft = this._activeTransform.x;
      const currentTop = this._activeTransform.y;
      let x = currentLeft + offsetX, y = currentTop + offsetY;
      this._rootElement.style.transform = getTransform(x, y);
      this._activeTransform = {
        x,
        y
      };
      this._passiveTransform = {
        x,
        y
      };
    }
  }
  disableHandle(handle) {
    if (!this._disabledHandles.has(handle) && this._handles.indexOf(handle) > -1) {
      this._disabledHandles.add(handle);
      toggleNativeDragInteractions(handle, true);
    }
  }
  enableHandle(handle) {
    if (this._disabledHandles.has(handle)) {
      this._disabledHandles.delete(handle);
      toggleNativeDragInteractions(handle, this.disabled);
    }
  }
  withDirection(direction) {
    this._direction = direction;
    return this;
  }
  _withDropContainer(container) {
    this._dropContainer = container;
  }
  getFreeDragPosition() {
    const position = this.isDragging() ? this._activeTransform : this._passiveTransform;
    return {
      x: position.x,
      y: position.y
    };
  }
  setFreeDragPosition(value) {
    this._activeTransform = {
      x: 0,
      y: 0
    };
    this._passiveTransform.x = value.x;
    this._passiveTransform.y = value.y;
    if (!this._dropContainer) {
      this._applyRootElementTransform(value.x, value.y);
    }
    return this;
  }
  withPreviewContainer(value) {
    this._previewContainer = value;
    return this;
  }
  _sortFromLastPointerPosition() {
    const position = this._lastKnownPointerPosition;
    if (position && this._dropContainer) {
      this._updateActiveDropContainer(this._getConstrainedPointerPosition(position), position);
    }
  }
  _removeListeners() {
    this._pointerMoveSubscription.unsubscribe();
    this._pointerUpSubscription.unsubscribe();
    this._scrollSubscription.unsubscribe();
    this._cleanupShadowRootSelectStart?.();
    this._cleanupShadowRootSelectStart = void 0;
  }
  _destroyPreview() {
    this._preview?.destroy();
    this._preview = null;
  }
  _destroyPlaceholder() {
    this._anchor?.remove();
    this._placeholder?.remove();
    this._placeholderRef?.destroy();
    this._placeholder = this._anchor = this._placeholderRef = null;
  }
  _pointerDown = (event) => {
    this.beforeStarted.next();
    if (this._handles.length) {
      const targetHandle = this._getTargetHandle(event);
      if (targetHandle && !this._disabledHandles.has(targetHandle) && !this.disabled) {
        this._initializeDragSequence(targetHandle, event);
      }
    } else if (!this.disabled) {
      this._initializeDragSequence(this._rootElement, event);
    }
  };
  _pointerMove = (event) => {
    const pointerPosition = this._getPointerPositionOnPage(event);
    if (!this._hasStartedDragging()) {
      const distanceX = Math.abs(pointerPosition.x - this._pickupPositionOnPage.x);
      const distanceY = Math.abs(pointerPosition.y - this._pickupPositionOnPage.y);
      const isOverThreshold = distanceX + distanceY >= this._config.dragStartThreshold;
      if (isOverThreshold) {
        const isDelayElapsed = Date.now() >= this._dragStartTime + this._getDragStartDelay(event);
        const container = this._dropContainer;
        if (!isDelayElapsed) {
          this._endDragSequence(event);
          return;
        }
        if (!container || !container.isDragging() && !container.isReceiving()) {
          if (event.cancelable) {
            event.preventDefault();
          }
          this._hasStartedDragging.set(true);
          this._ngZone.run(() => this._startDragSequence(event));
        }
      }
      return;
    }
    if (event.cancelable) {
      event.preventDefault();
    }
    const constrainedPointerPosition = this._getConstrainedPointerPosition(pointerPosition);
    this._hasMoved = true;
    this._lastKnownPointerPosition = pointerPosition;
    this._updatePointerDirectionDelta(constrainedPointerPosition);
    if (this._dropContainer) {
      this._updateActiveDropContainer(constrainedPointerPosition, pointerPosition);
    } else {
      const offset = this.constrainPosition ? this._initialDomRect : this._pickupPositionOnPage;
      const activeTransform = this._activeTransform;
      activeTransform.x = constrainedPointerPosition.x - offset.x + this._passiveTransform.x;
      activeTransform.y = constrainedPointerPosition.y - offset.y + this._passiveTransform.y;
      this._applyRootElementTransform(activeTransform.x, activeTransform.y);
    }
    if (this._moveEvents.observers.length) {
      this._ngZone.run(() => {
        this._moveEvents.next({
          source: this,
          pointerPosition: constrainedPointerPosition,
          event,
          distance: this._getDragDistance(constrainedPointerPosition),
          delta: this._pointerDirectionDelta
        });
      });
    }
  };
  _pointerUp = (event) => {
    this._endDragSequence(event);
  };
  _endDragSequence(event) {
    if (!this._dragDropRegistry.isDragging(this)) {
      return;
    }
    this._removeListeners();
    this._dragDropRegistry.stopDragging(this);
    this._toggleNativeDragInteractions();
    if (this._handles) {
      this._rootElement.style.webkitTapHighlightColor = this._rootElementTapHighlight;
    }
    if (!this._hasStartedDragging()) {
      return;
    }
    this.released.next({
      source: this,
      event
    });
    if (this._dropContainer) {
      this._dropContainer._stopScrolling();
      this._animatePreviewToPlaceholder().then(() => {
        this._cleanupDragArtifacts(event);
        this._cleanupCachedDimensions();
        this._dragDropRegistry.stopDragging(this);
      });
    } else {
      this._passiveTransform.x = this._activeTransform.x;
      const pointerPosition = this._getPointerPositionOnPage(event);
      this._passiveTransform.y = this._activeTransform.y;
      this._ngZone.run(() => {
        this.ended.next({
          source: this,
          distance: this._getDragDistance(pointerPosition),
          dropPoint: pointerPosition,
          event
        });
      });
      this._cleanupCachedDimensions();
      this._dragDropRegistry.stopDragging(this);
    }
  }
  _startDragSequence(event) {
    if (isTouchEvent(event)) {
      this._lastTouchEventTime = Date.now();
    }
    this._toggleNativeDragInteractions();
    const shadowRoot = this._getShadowRoot();
    const dropContainer = this._dropContainer;
    if (shadowRoot) {
      this._ngZone.runOutsideAngular(() => {
        this._cleanupShadowRootSelectStart = this._renderer.listen(shadowRoot, "selectstart", shadowDomSelectStart, activeCapturingEventOptions);
      });
    }
    if (dropContainer) {
      const element = this._rootElement;
      const parent = element.parentNode;
      const placeholder = this._placeholder = this._createPlaceholderElement();
      const marker = this._marker = this._marker || this._document.createComment(typeof ngDevMode === "undefined" || ngDevMode ? "cdk-drag-marker" : "");
      parent.insertBefore(marker, element);
      this._initialTransform = element.style.transform || "";
      this._preview = new PreviewRef(this._document, this._rootElement, this._direction, this._initialDomRect, this._previewTemplate || null, this.previewClass || null, this._pickupPositionOnPage, this._initialTransform, this._config.zIndex || 1e3, this._renderer);
      this._preview.attach(this._getPreviewInsertionPoint(parent, shadowRoot));
      toggleVisibility(element, false, dragImportantProperties);
      this._document.body.appendChild(parent.replaceChild(placeholder, element));
      this.started.next({
        source: this,
        event
      });
      dropContainer.start();
      this._initialContainer = dropContainer;
      this._initialIndex = dropContainer.getItemIndex(this);
    } else {
      this.started.next({
        source: this,
        event
      });
      this._initialContainer = this._initialIndex = void 0;
    }
    this._parentPositions.cache(dropContainer ? dropContainer.getScrollableParents() : []);
  }
  _initializeDragSequence(referenceElement, event) {
    if (this._parentDragRef) {
      event.stopPropagation();
    }
    const isDragging = this.isDragging();
    const isTouchSequence = isTouchEvent(event);
    const isAuxiliaryMouseButton = !isTouchSequence && event.button !== 0;
    const rootElement = this._rootElement;
    const target = _getEventTarget(event);
    const isSyntheticEvent = !isTouchSequence && this._lastTouchEventTime && this._lastTouchEventTime + MOUSE_EVENT_IGNORE_TIME > Date.now();
    const isFakeEvent = isTouchSequence ? isFakeTouchstartFromScreenReader(event) : isFakeMousedownFromScreenReader(event);
    if (target && target.draggable && event.type === "mousedown") {
      event.preventDefault();
    }
    if (isDragging || isAuxiliaryMouseButton || isSyntheticEvent || isFakeEvent) {
      return;
    }
    if (this._handles.length) {
      const rootStyles = rootElement.style;
      this._rootElementTapHighlight = rootStyles.webkitTapHighlightColor || "";
      rootStyles.webkitTapHighlightColor = "transparent";
    }
    this._hasMoved = false;
    this._hasStartedDragging.set(this._hasMoved);
    this._removeListeners();
    this._initialDomRect = this._rootElement.getBoundingClientRect();
    this._pointerMoveSubscription = this._dragDropRegistry.pointerMove.subscribe(this._pointerMove);
    this._pointerUpSubscription = this._dragDropRegistry.pointerUp.subscribe(this._pointerUp);
    this._scrollSubscription = this._dragDropRegistry.scrolled(this._getShadowRoot()).subscribe((scrollEvent) => this._updateOnScroll(scrollEvent));
    if (this._boundaryElement) {
      this._boundaryRect = getMutableClientRect(this._boundaryElement);
    }
    const previewTemplate = this._previewTemplate;
    this._pickupPositionInElement = previewTemplate && previewTemplate.template && !previewTemplate.matchSize ? {
      x: 0,
      y: 0
    } : this._getPointerPositionInElement(this._initialDomRect, referenceElement, event);
    const pointerPosition = this._pickupPositionOnPage = this._lastKnownPointerPosition = this._getPointerPositionOnPage(event);
    this._pointerDirectionDelta = {
      x: 0,
      y: 0
    };
    this._pointerPositionAtLastDirectionChange = {
      x: pointerPosition.x,
      y: pointerPosition.y
    };
    this._dragStartTime = Date.now();
    this._dragDropRegistry.startDragging(this, event);
  }
  _cleanupDragArtifacts(event) {
    toggleVisibility(this._rootElement, true, dragImportantProperties);
    this._marker.parentNode.replaceChild(this._rootElement, this._marker);
    this._destroyPreview();
    this._destroyPlaceholder();
    this._initialDomRect = this._boundaryRect = this._previewRect = this._initialTransform = void 0;
    this._ngZone.run(() => {
      const container = this._dropContainer;
      const currentIndex = container.getItemIndex(this);
      const pointerPosition = this._getPointerPositionOnPage(event);
      const distance2 = this._getDragDistance(pointerPosition);
      const isPointerOverContainer = container._isOverContainer(pointerPosition.x, pointerPosition.y);
      this.ended.next({
        source: this,
        distance: distance2,
        dropPoint: pointerPosition,
        event
      });
      this.dropped.next({
        item: this,
        currentIndex,
        previousIndex: this._initialIndex,
        container,
        previousContainer: this._initialContainer,
        isPointerOverContainer,
        distance: distance2,
        dropPoint: pointerPosition,
        event
      });
      container.drop(this, currentIndex, this._initialIndex, this._initialContainer, isPointerOverContainer, distance2, pointerPosition, event);
      this._dropContainer = this._initialContainer;
    });
  }
  _updateActiveDropContainer({
    x,
    y
  }, {
    x: rawX,
    y: rawY
  }) {
    let newContainer = this._initialContainer._getSiblingContainerFromPosition(this, x, y);
    if (!newContainer && this._dropContainer !== this._initialContainer && this._initialContainer._isOverContainer(x, y)) {
      newContainer = this._initialContainer;
    }
    if (newContainer && newContainer !== this._dropContainer) {
      this._ngZone.run(() => {
        const exitIndex = this._dropContainer.getItemIndex(this);
        const nextItemElement = this._dropContainer.getItemAtIndex(exitIndex + 1)?.getVisibleElement() || null;
        this.exited.next({
          item: this,
          container: this._dropContainer
        });
        this._dropContainer.exit(this);
        this._conditionallyInsertAnchor(newContainer, this._dropContainer, nextItemElement);
        this._dropContainer = newContainer;
        this._dropContainer.enter(this, x, y, newContainer === this._initialContainer && newContainer.sortingDisabled ? this._initialIndex : void 0);
        this.entered.next({
          item: this,
          container: newContainer,
          currentIndex: newContainer.getItemIndex(this)
        });
      });
    }
    if (this.isDragging()) {
      this._dropContainer._startScrollingIfNecessary(rawX, rawY);
      this._dropContainer._sortItem(this, x, y, this._pointerDirectionDelta);
      if (this.constrainPosition) {
        this._applyPreviewTransform(x, y);
      } else {
        this._applyPreviewTransform(x - this._pickupPositionInElement.x, y - this._pickupPositionInElement.y);
      }
    }
  }
  _animatePreviewToPlaceholder() {
    if (!this._hasMoved) {
      return Promise.resolve();
    }
    const placeholderRect = this._placeholder.getBoundingClientRect();
    this._preview.addClass("cdk-drag-animating");
    this._applyPreviewTransform(placeholderRect.left, placeholderRect.top);
    const duration = this._preview.getTransitionDuration();
    if (duration === 0) {
      return Promise.resolve();
    }
    return this._ngZone.runOutsideAngular(() => {
      return new Promise((resolve) => {
        const handler = (event) => {
          if (!event || this._preview && _getEventTarget(event) === this._preview.element && event.propertyName === "transform") {
            cleanupListener();
            resolve();
            clearTimeout(timeout2);
          }
        };
        const timeout2 = setTimeout(handler, duration * 1.5);
        const cleanupListener = this._preview.addEventListener("transitionend", handler);
      });
    });
  }
  _createPlaceholderElement() {
    const placeholderConfig = this._placeholderTemplate;
    const placeholderTemplate = placeholderConfig ? placeholderConfig.template : null;
    let placeholder;
    if (placeholderTemplate) {
      this._placeholderRef = placeholderConfig.viewContainer.createEmbeddedView(placeholderTemplate, placeholderConfig.context);
      this._placeholderRef.detectChanges();
      placeholder = getRootNode(this._placeholderRef, this._document);
    } else {
      placeholder = deepCloneNode(this._rootElement);
    }
    placeholder.style.pointerEvents = "none";
    placeholder.classList.add(PLACEHOLDER_CLASS);
    return placeholder;
  }
  _getPointerPositionInElement(elementRect, referenceElement, event) {
    const handleElement = referenceElement === this._rootElement ? null : referenceElement;
    const referenceRect = handleElement ? handleElement.getBoundingClientRect() : elementRect;
    const point = isTouchEvent(event) ? event.targetTouches[0] : event;
    const scrollPosition = this._getViewportScrollPosition();
    const x = point.pageX - referenceRect.left - scrollPosition.left;
    const y = point.pageY - referenceRect.top - scrollPosition.top;
    return {
      x: referenceRect.left - elementRect.left + x,
      y: referenceRect.top - elementRect.top + y
    };
  }
  _getPointerPositionOnPage(event) {
    const scrollPosition = this._getViewportScrollPosition();
    const point = isTouchEvent(event) ? event.touches[0] || event.changedTouches[0] || {
      pageX: 0,
      pageY: 0
    } : event;
    const x = point.pageX - scrollPosition.left;
    const y = point.pageY - scrollPosition.top;
    if (this._ownerSVGElement) {
      const svgMatrix = this._ownerSVGElement.getScreenCTM();
      if (svgMatrix) {
        const svgPoint = this._ownerSVGElement.createSVGPoint();
        svgPoint.x = x;
        svgPoint.y = y;
        return svgPoint.matrixTransform(svgMatrix.inverse());
      }
    }
    return {
      x,
      y
    };
  }
  _getConstrainedPointerPosition(point) {
    const dropContainerLock = this._dropContainer ? this._dropContainer.lockAxis : null;
    let {
      x,
      y
    } = this.constrainPosition ? this.constrainPosition(point, this, this._initialDomRect, this._pickupPositionInElement) : point;
    if (this.lockAxis === "x" || dropContainerLock === "x") {
      y = this._pickupPositionOnPage.y - (this.constrainPosition ? this._pickupPositionInElement.y : 0);
    } else if (this.lockAxis === "y" || dropContainerLock === "y") {
      x = this._pickupPositionOnPage.x - (this.constrainPosition ? this._pickupPositionInElement.x : 0);
    }
    if (this._boundaryRect) {
      const {
        x: pickupX,
        y: pickupY
      } = !this.constrainPosition ? this._pickupPositionInElement : {
        x: 0,
        y: 0
      };
      const boundaryRect = this._boundaryRect;
      const {
        width: previewWidth,
        height: previewHeight
      } = this._getPreviewRect();
      const minY = boundaryRect.top + pickupY;
      const maxY = boundaryRect.bottom - (previewHeight - pickupY);
      const minX = boundaryRect.left + pickupX;
      const maxX = boundaryRect.right - (previewWidth - pickupX);
      x = clamp$1(x, minX, maxX);
      y = clamp$1(y, minY, maxY);
    }
    return {
      x,
      y
    };
  }
  _updatePointerDirectionDelta(pointerPositionOnPage) {
    const {
      x,
      y
    } = pointerPositionOnPage;
    const delta = this._pointerDirectionDelta;
    const positionSinceLastChange = this._pointerPositionAtLastDirectionChange;
    const changeX = Math.abs(x - positionSinceLastChange.x);
    const changeY = Math.abs(y - positionSinceLastChange.y);
    if (changeX > this._config.pointerDirectionChangeThreshold) {
      delta.x = x > positionSinceLastChange.x ? 1 : -1;
      positionSinceLastChange.x = x;
    }
    if (changeY > this._config.pointerDirectionChangeThreshold) {
      delta.y = y > positionSinceLastChange.y ? 1 : -1;
      positionSinceLastChange.y = y;
    }
    return delta;
  }
  _toggleNativeDragInteractions() {
    if (!this._rootElement || !this._handles) {
      return;
    }
    const shouldEnable = this._handles.length > 0 || !this.isDragging();
    if (shouldEnable !== this._nativeInteractionsEnabled) {
      this._nativeInteractionsEnabled = shouldEnable;
      toggleNativeDragInteractions(this._rootElement, shouldEnable);
    }
  }
  _removeRootElementListeners() {
    this._rootElementCleanups?.forEach((cleanup) => cleanup());
    this._rootElementCleanups = void 0;
  }
  _applyRootElementTransform(x, y) {
    const scale3 = 1 / this.scale;
    const transform = getTransform(x * scale3, y * scale3);
    const styles = this._rootElement.style;
    if (this._initialTransform == null) {
      this._initialTransform = styles.transform && styles.transform != "none" ? styles.transform : "";
    }
    styles.transform = combineTransforms(transform, this._initialTransform);
  }
  _applyPreviewTransform(x, y) {
    const initialTransform = this._previewTemplate?.template ? void 0 : this._initialTransform;
    const transform = getTransform(x, y);
    this._preview.setTransform(combineTransforms(transform, initialTransform));
  }
  _getDragDistance(currentPosition) {
    const pickupPosition = this._pickupPositionOnPage;
    if (pickupPosition) {
      return {
        x: currentPosition.x - pickupPosition.x,
        y: currentPosition.y - pickupPosition.y
      };
    }
    return {
      x: 0,
      y: 0
    };
  }
  _cleanupCachedDimensions() {
    this._boundaryRect = this._previewRect = void 0;
    this._parentPositions.clear();
  }
  _containInsideBoundaryOnResize() {
    let {
      x,
      y
    } = this._passiveTransform;
    if (x === 0 && y === 0 || this.isDragging() || !this._boundaryElement) {
      return;
    }
    const elementRect = this._rootElement.getBoundingClientRect();
    const boundaryRect = this._boundaryElement.getBoundingClientRect();
    if (boundaryRect.width === 0 && boundaryRect.height === 0 || elementRect.width === 0 && elementRect.height === 0) {
      return;
    }
    const leftOverflow = boundaryRect.left - elementRect.left;
    const rightOverflow = elementRect.right - boundaryRect.right;
    const topOverflow = boundaryRect.top - elementRect.top;
    const bottomOverflow = elementRect.bottom - boundaryRect.bottom;
    if (boundaryRect.width > elementRect.width) {
      if (leftOverflow > 0) {
        x += leftOverflow;
      }
      if (rightOverflow > 0) {
        x -= rightOverflow;
      }
    } else {
      x = 0;
    }
    if (boundaryRect.height > elementRect.height) {
      if (topOverflow > 0) {
        y += topOverflow;
      }
      if (bottomOverflow > 0) {
        y -= bottomOverflow;
      }
    } else {
      y = 0;
    }
    if (x !== this._passiveTransform.x || y !== this._passiveTransform.y) {
      this.setFreeDragPosition({
        y,
        x
      });
    }
  }
  _getDragStartDelay(event) {
    const value = this.dragStartDelay;
    if (typeof value === "number") {
      return value;
    } else if (isTouchEvent(event)) {
      return value.touch;
    }
    return value ? value.mouse : 0;
  }
  _updateOnScroll(event) {
    const scrollDifference = this._parentPositions.handleScroll(event);
    if (scrollDifference) {
      const target = _getEventTarget(event);
      if (this._boundaryRect && target !== this._boundaryElement && target.contains(this._boundaryElement)) {
        adjustDomRect(this._boundaryRect, scrollDifference.top, scrollDifference.left);
      }
      this._pickupPositionOnPage.x += scrollDifference.left;
      this._pickupPositionOnPage.y += scrollDifference.top;
      if (!this._dropContainer) {
        this._activeTransform.x -= scrollDifference.left;
        this._activeTransform.y -= scrollDifference.top;
        this._applyRootElementTransform(this._activeTransform.x, this._activeTransform.y);
      }
    }
  }
  _getViewportScrollPosition() {
    return this._parentPositions.positions.get(this._document)?.scrollPosition || this._parentPositions.getViewportScrollPosition();
  }
  _getShadowRoot() {
    if (this._cachedShadowRoot === void 0) {
      this._cachedShadowRoot = _getShadowRoot(this._rootElement);
    }
    return this._cachedShadowRoot;
  }
  _getPreviewInsertionPoint(initialParent, shadowRoot) {
    const previewContainer = this._previewContainer || "global";
    if (previewContainer === "parent") {
      return initialParent;
    }
    if (previewContainer === "global") {
      const documentRef = this._document;
      return shadowRoot || documentRef.fullscreenElement || documentRef.webkitFullscreenElement || documentRef.mozFullScreenElement || documentRef.msFullscreenElement || documentRef.body;
    }
    return coerceElement(previewContainer);
  }
  _getPreviewRect() {
    if (!this._previewRect || !this._previewRect.width && !this._previewRect.height) {
      this._previewRect = this._preview ? this._preview.getBoundingClientRect() : this._initialDomRect;
    }
    return this._previewRect;
  }
  _nativeDragStart = (event) => {
    if (this._handles.length) {
      const targetHandle = this._getTargetHandle(event);
      if (targetHandle && !this._disabledHandles.has(targetHandle) && !this.disabled) {
        event.preventDefault();
      }
    } else if (!this.disabled) {
      event.preventDefault();
    }
  };
  _getTargetHandle(event) {
    return this._handles.find((handle) => {
      return event.target && (event.target === handle || handle.contains(event.target));
    });
  }
  _conditionallyInsertAnchor(newContainer, exitContainer, nextItemElement) {
    if (newContainer === this._initialContainer) {
      this._anchor?.remove();
      this._anchor = null;
    } else if (exitContainer === this._initialContainer && exitContainer.hasAnchor) {
      const anchor = this._anchor ??= deepCloneNode(this._placeholder);
      anchor.classList.remove(PLACEHOLDER_CLASS);
      anchor.classList.add("cdk-drag-anchor");
      anchor.style.transform = "";
      if (nextItemElement) {
        nextItemElement.before(anchor);
      } else {
        coerceElement(exitContainer.element).appendChild(anchor);
      }
    }
  }
};
function clamp$1(value, min3, max3) {
  return Math.max(min3, Math.min(max3, value));
}
function isTouchEvent(event) {
  return event.type[0] === "t";
}
function shadowDomSelectStart(event) {
  event.preventDefault();
}
function moveItemInArray(array, fromIndex, toIndex) {
  const from = clamp(fromIndex, array.length - 1);
  const to = clamp(toIndex, array.length - 1);
  if (from === to) {
    return;
  }
  const target = array[from];
  const delta = to < from ? -1 : 1;
  for (let i2 = from; i2 !== to; i2 += delta) {
    array[i2] = array[i2 + delta];
  }
  array[to] = target;
}
function transferArrayItem(currentArray, targetArray, currentIndex, targetIndex) {
  const from = clamp(currentIndex, currentArray.length - 1);
  const to = clamp(targetIndex, targetArray.length);
  if (currentArray.length) {
    targetArray.splice(to, 0, currentArray.splice(from, 1)[0]);
  }
}
function clamp(value, max3) {
  return Math.max(0, Math.min(max3, value));
}
var SingleAxisSortStrategy = class {
  _dragDropRegistry;
  _element;
  _sortPredicate;
  _itemPositions = [];
  _activeDraggables;
  orientation = "vertical";
  direction = "ltr";
  constructor(_dragDropRegistry) {
    this._dragDropRegistry = _dragDropRegistry;
  }
  _previousSwap = {
    drag: null,
    delta: 0,
    overlaps: false
  };
  start(items) {
    this.withItems(items);
  }
  sort(item, pointerX, pointerY, pointerDelta) {
    const siblings = this._itemPositions;
    const newIndex = this._getItemIndexFromPointerPosition(item, pointerX, pointerY, pointerDelta);
    if (newIndex === -1 && siblings.length > 0) {
      return null;
    }
    const isHorizontal = this.orientation === "horizontal";
    const currentIndex = siblings.findIndex((currentItem) => currentItem.drag === item);
    const siblingAtNewPosition = siblings[newIndex];
    const currentPosition = siblings[currentIndex].clientRect;
    const newPosition = siblingAtNewPosition.clientRect;
    const delta = currentIndex > newIndex ? 1 : -1;
    const itemOffset = this._getItemOffsetPx(currentPosition, newPosition, delta);
    const siblingOffset = this._getSiblingOffsetPx(currentIndex, siblings, delta);
    const oldOrder = siblings.slice();
    moveItemInArray(siblings, currentIndex, newIndex);
    siblings.forEach((sibling, index) => {
      if (oldOrder[index] === sibling) {
        return;
      }
      const isDraggedItem = sibling.drag === item;
      const offset = isDraggedItem ? itemOffset : siblingOffset;
      const elementToOffset = isDraggedItem ? item.getPlaceholderElement() : sibling.drag.getRootElement();
      sibling.offset += offset;
      const transformAmount = Math.round(sibling.offset * (1 / sibling.drag.scale));
      if (isHorizontal) {
        elementToOffset.style.transform = combineTransforms(`translate3d(${transformAmount}px, 0, 0)`, sibling.initialTransform);
        adjustDomRect(sibling.clientRect, 0, offset);
      } else {
        elementToOffset.style.transform = combineTransforms(`translate3d(0, ${transformAmount}px, 0)`, sibling.initialTransform);
        adjustDomRect(sibling.clientRect, offset, 0);
      }
    });
    this._previousSwap.overlaps = isInsideClientRect(newPosition, pointerX, pointerY);
    this._previousSwap.drag = siblingAtNewPosition.drag;
    this._previousSwap.delta = isHorizontal ? pointerDelta.x : pointerDelta.y;
    return {
      previousIndex: currentIndex,
      currentIndex: newIndex
    };
  }
  enter(item, pointerX, pointerY, index) {
    const activeDraggables = this._activeDraggables;
    const currentIndex = activeDraggables.indexOf(item);
    const placeholder = item.getPlaceholderElement();
    if (currentIndex > -1) {
      activeDraggables.splice(currentIndex, 1);
    }
    const newIndex = index == null || index < 0 ? this._getItemIndexFromPointerPosition(item, pointerX, pointerY) : index;
    let newPositionReference = activeDraggables[newIndex];
    if (newPositionReference === item) {
      newPositionReference = activeDraggables[newIndex + 1];
    }
    if (!newPositionReference && (newIndex == null || newIndex === -1 || newIndex < activeDraggables.length - 1) && this._shouldEnterAsFirstChild(pointerX, pointerY)) {
      newPositionReference = activeDraggables[0];
    }
    if (newPositionReference && !this._dragDropRegistry.isDragging(newPositionReference)) {
      const element = newPositionReference.getRootElement();
      element.parentElement.insertBefore(placeholder, element);
      activeDraggables.splice(newIndex, 0, item);
    } else {
      this._element.appendChild(placeholder);
      activeDraggables.push(item);
    }
    placeholder.style.transform = "";
    this._cacheItemPositions();
  }
  withItems(items) {
    this._activeDraggables = items.slice();
    this._cacheItemPositions();
  }
  withSortPredicate(predicate) {
    this._sortPredicate = predicate;
  }
  reset() {
    this._activeDraggables?.forEach((item) => {
      const rootElement = item.getRootElement();
      if (rootElement) {
        const initialTransform = this._itemPositions.find((p) => p.drag === item)?.initialTransform;
        rootElement.style.transform = initialTransform || "";
      }
    });
    this._itemPositions = [];
    this._activeDraggables = [];
    this._previousSwap.drag = null;
    this._previousSwap.delta = 0;
    this._previousSwap.overlaps = false;
  }
  getActiveItemsSnapshot() {
    return this._activeDraggables;
  }
  getItemIndex(item) {
    return this._getVisualItemPositions().findIndex((currentItem) => currentItem.drag === item);
  }
  getItemAtIndex(index) {
    return this._getVisualItemPositions()[index]?.drag || null;
  }
  updateOnScroll(topDifference, leftDifference) {
    this._itemPositions.forEach(({
      clientRect
    }) => {
      adjustDomRect(clientRect, topDifference, leftDifference);
    });
    this._itemPositions.forEach(({
      drag
    }) => {
      if (this._dragDropRegistry.isDragging(drag)) {
        drag._sortFromLastPointerPosition();
      }
    });
  }
  withElementContainer(container) {
    this._element = container;
  }
  _cacheItemPositions() {
    const isHorizontal = this.orientation === "horizontal";
    this._itemPositions = this._activeDraggables.map((drag) => {
      const elementToMeasure = drag.getVisibleElement();
      return {
        drag,
        offset: 0,
        initialTransform: elementToMeasure.style.transform || "",
        clientRect: getMutableClientRect(elementToMeasure)
      };
    }).sort((a, b) => {
      return isHorizontal ? a.clientRect.left - b.clientRect.left : a.clientRect.top - b.clientRect.top;
    });
  }
  _getVisualItemPositions() {
    return this.orientation === "horizontal" && this.direction === "rtl" ? this._itemPositions.slice().reverse() : this._itemPositions;
  }
  _getItemOffsetPx(currentPosition, newPosition, delta) {
    const isHorizontal = this.orientation === "horizontal";
    let itemOffset = isHorizontal ? newPosition.left - currentPosition.left : newPosition.top - currentPosition.top;
    if (delta === -1) {
      itemOffset += isHorizontal ? newPosition.width - currentPosition.width : newPosition.height - currentPosition.height;
    }
    return itemOffset;
  }
  _getSiblingOffsetPx(currentIndex, siblings, delta) {
    const isHorizontal = this.orientation === "horizontal";
    const currentPosition = siblings[currentIndex].clientRect;
    const immediateSibling = siblings[currentIndex + delta * -1];
    let siblingOffset = currentPosition[isHorizontal ? "width" : "height"] * delta;
    if (immediateSibling) {
      const start2 = isHorizontal ? "left" : "top";
      const end2 = isHorizontal ? "right" : "bottom";
      if (delta === -1) {
        siblingOffset -= immediateSibling.clientRect[start2] - currentPosition[end2];
      } else {
        siblingOffset += currentPosition[start2] - immediateSibling.clientRect[end2];
      }
    }
    return siblingOffset;
  }
  _shouldEnterAsFirstChild(pointerX, pointerY) {
    if (!this._activeDraggables.length) {
      return false;
    }
    const itemPositions = this._itemPositions;
    const isHorizontal = this.orientation === "horizontal";
    const reversed = itemPositions[0].drag !== this._activeDraggables[0];
    if (reversed) {
      const lastItemRect = itemPositions[itemPositions.length - 1].clientRect;
      return isHorizontal ? pointerX >= lastItemRect.right : pointerY >= lastItemRect.bottom;
    } else {
      const firstItemRect = itemPositions[0].clientRect;
      return isHorizontal ? pointerX <= firstItemRect.left : pointerY <= firstItemRect.top;
    }
  }
  _getItemIndexFromPointerPosition(item, pointerX, pointerY, delta) {
    const isHorizontal = this.orientation === "horizontal";
    const index = this._itemPositions.findIndex(({
      drag,
      clientRect
    }) => {
      if (drag === item) {
        return false;
      }
      if (delta) {
        const direction = isHorizontal ? delta.x : delta.y;
        if (drag === this._previousSwap.drag && this._previousSwap.overlaps && direction === this._previousSwap.delta) {
          return false;
        }
      }
      return isHorizontal ? pointerX >= Math.floor(clientRect.left) && pointerX < Math.floor(clientRect.right) : pointerY >= Math.floor(clientRect.top) && pointerY < Math.floor(clientRect.bottom);
    });
    return index === -1 || !this._sortPredicate(index, item) ? -1 : index;
  }
};
var MixedSortStrategy = class {
  _document;
  _dragDropRegistry;
  _element;
  _sortPredicate;
  _rootNode;
  _activeItems;
  _previousSwap = {
    drag: null,
    deltaX: 0,
    deltaY: 0,
    overlaps: false
  };
  _relatedNodes = [];
  constructor(_document, _dragDropRegistry) {
    this._document = _document;
    this._dragDropRegistry = _dragDropRegistry;
  }
  start(items) {
    const childNodes = this._element.childNodes;
    this._relatedNodes = [];
    for (let i2 = 0; i2 < childNodes.length; i2++) {
      const node = childNodes[i2];
      this._relatedNodes.push([node, node.nextSibling]);
    }
    this.withItems(items);
  }
  sort(item, pointerX, pointerY, pointerDelta) {
    const newIndex = this._getItemIndexFromPointerPosition(item, pointerX, pointerY);
    const previousSwap = this._previousSwap;
    if (newIndex === -1 || this._activeItems[newIndex] === item) {
      return null;
    }
    const toSwapWith = this._activeItems[newIndex];
    if (previousSwap.drag === toSwapWith && previousSwap.overlaps && previousSwap.deltaX === pointerDelta.x && previousSwap.deltaY === pointerDelta.y) {
      return null;
    }
    const previousIndex = this.getItemIndex(item);
    const current = item.getPlaceholderElement();
    const overlapElement = toSwapWith.getRootElement();
    if (newIndex > previousIndex) {
      overlapElement.after(current);
    } else {
      overlapElement.before(current);
    }
    moveItemInArray(this._activeItems, previousIndex, newIndex);
    const newOverlapElement = this._getRootNode().elementFromPoint(pointerX, pointerY);
    previousSwap.deltaX = pointerDelta.x;
    previousSwap.deltaY = pointerDelta.y;
    previousSwap.drag = toSwapWith;
    previousSwap.overlaps = overlapElement === newOverlapElement || overlapElement.contains(newOverlapElement);
    return {
      previousIndex,
      currentIndex: newIndex
    };
  }
  enter(item, pointerX, pointerY, index) {
    const currentIndex = this._activeItems.indexOf(item);
    if (currentIndex > -1) {
      this._activeItems.splice(currentIndex, 1);
    }
    let enterIndex = index == null || index < 0 ? this._getItemIndexFromPointerPosition(item, pointerX, pointerY) : index;
    if (enterIndex === -1) {
      enterIndex = this._getClosestItemIndexToPointer(item, pointerX, pointerY);
    }
    const targetItem = this._activeItems[enterIndex];
    if (targetItem && !this._dragDropRegistry.isDragging(targetItem)) {
      this._activeItems.splice(enterIndex, 0, item);
      targetItem.getRootElement().before(item.getPlaceholderElement());
    } else {
      this._activeItems.push(item);
      this._element.appendChild(item.getPlaceholderElement());
    }
  }
  withItems(items) {
    this._activeItems = items.slice();
  }
  withSortPredicate(predicate) {
    this._sortPredicate = predicate;
  }
  reset() {
    const root = this._element;
    const previousSwap = this._previousSwap;
    for (let i2 = this._relatedNodes.length - 1; i2 > -1; i2--) {
      const [node, nextSibling2] = this._relatedNodes[i2];
      if (node.parentNode === root && node.nextSibling !== nextSibling2) {
        if (nextSibling2 === null) {
          root.appendChild(node);
        } else if (nextSibling2.parentNode === root) {
          root.insertBefore(node, nextSibling2);
        }
      }
    }
    this._relatedNodes = [];
    this._activeItems = [];
    previousSwap.drag = null;
    previousSwap.deltaX = previousSwap.deltaY = 0;
    previousSwap.overlaps = false;
  }
  getActiveItemsSnapshot() {
    return this._activeItems;
  }
  getItemIndex(item) {
    return this._activeItems.indexOf(item);
  }
  getItemAtIndex(index) {
    return this._activeItems[index] || null;
  }
  updateOnScroll() {
    this._activeItems.forEach((item) => {
      if (this._dragDropRegistry.isDragging(item)) {
        item._sortFromLastPointerPosition();
      }
    });
  }
  withElementContainer(container) {
    if (container !== this._element) {
      this._element = container;
      this._rootNode = void 0;
    }
  }
  _getItemIndexFromPointerPosition(item, pointerX, pointerY) {
    const elementAtPoint = this._getRootNode().elementFromPoint(Math.floor(pointerX), Math.floor(pointerY));
    const index = elementAtPoint ? this._activeItems.findIndex((item2) => {
      const root = item2.getRootElement();
      return elementAtPoint === root || root.contains(elementAtPoint);
    }) : -1;
    return index === -1 || !this._sortPredicate(index, item) ? -1 : index;
  }
  _getRootNode() {
    if (!this._rootNode) {
      this._rootNode = _getShadowRoot(this._element) || this._document;
    }
    return this._rootNode;
  }
  _getClosestItemIndexToPointer(item, pointerX, pointerY) {
    if (this._activeItems.length === 0) {
      return -1;
    }
    if (this._activeItems.length === 1) {
      return 0;
    }
    let minDistance = Infinity;
    let minIndex = -1;
    for (let i2 = 0; i2 < this._activeItems.length; i2++) {
      const current = this._activeItems[i2];
      if (current !== item) {
        const {
          x,
          y
        } = current.getRootElement().getBoundingClientRect();
        const distance2 = Math.hypot(pointerX - x, pointerY - y);
        if (distance2 < minDistance) {
          minDistance = distance2;
          minIndex = i2;
        }
      }
    }
    return minIndex;
  }
};
var DROP_PROXIMITY_THRESHOLD = 0.05;
var SCROLL_PROXIMITY_THRESHOLD = 0.05;
var AutoScrollVerticalDirection;
(function(AutoScrollVerticalDirection2) {
  AutoScrollVerticalDirection2[AutoScrollVerticalDirection2["NONE"] = 0] = "NONE";
  AutoScrollVerticalDirection2[AutoScrollVerticalDirection2["UP"] = 1] = "UP";
  AutoScrollVerticalDirection2[AutoScrollVerticalDirection2["DOWN"] = 2] = "DOWN";
})(AutoScrollVerticalDirection || (AutoScrollVerticalDirection = {}));
var AutoScrollHorizontalDirection;
(function(AutoScrollHorizontalDirection2) {
  AutoScrollHorizontalDirection2[AutoScrollHorizontalDirection2["NONE"] = 0] = "NONE";
  AutoScrollHorizontalDirection2[AutoScrollHorizontalDirection2["LEFT"] = 1] = "LEFT";
  AutoScrollHorizontalDirection2[AutoScrollHorizontalDirection2["RIGHT"] = 2] = "RIGHT";
})(AutoScrollHorizontalDirection || (AutoScrollHorizontalDirection = {}));
function createDropListRef(injector, element) {
  return new DropListRef(element, injector.get(DragDropRegistry), injector.get(DOCUMENT), injector.get(NgZone), injector.get(ViewportRuler));
}
var DropListRef = class {
  _dragDropRegistry;
  _ngZone;
  _viewportRuler;
  element;
  disabled = false;
  sortingDisabled = false;
  lockAxis = null;
  autoScrollDisabled = false;
  autoScrollStep = 2;
  hasAnchor = false;
  enterPredicate = () => true;
  sortPredicate = () => true;
  beforeStarted = new Subject();
  entered = new Subject();
  exited = new Subject();
  dropped = new Subject();
  sorted = new Subject();
  receivingStarted = new Subject();
  receivingStopped = new Subject();
  data;
  _container;
  _isDragging = false;
  _parentPositions;
  _sortStrategy;
  _domRect;
  _draggables = [];
  _siblings = [];
  _activeSiblings = /* @__PURE__ */ new Set();
  _viewportScrollSubscription = Subscription.EMPTY;
  _verticalScrollDirection = AutoScrollVerticalDirection.NONE;
  _horizontalScrollDirection = AutoScrollHorizontalDirection.NONE;
  _scrollNode;
  _stopScrollTimers = new Subject();
  _cachedShadowRoot = null;
  _document;
  _scrollableElements = [];
  _initialScrollSnap;
  _direction = "ltr";
  constructor(element, _dragDropRegistry, _document, _ngZone, _viewportRuler) {
    this._dragDropRegistry = _dragDropRegistry;
    this._ngZone = _ngZone;
    this._viewportRuler = _viewportRuler;
    const coercedElement = this.element = coerceElement(element);
    this._document = _document;
    this.withOrientation("vertical").withElementContainer(coercedElement);
    _dragDropRegistry.registerDropContainer(this);
    this._parentPositions = new ParentPositionTracker(_document);
  }
  dispose() {
    this._stopScrolling();
    this._stopScrollTimers.complete();
    this._viewportScrollSubscription.unsubscribe();
    this.beforeStarted.complete();
    this.entered.complete();
    this.exited.complete();
    this.dropped.complete();
    this.sorted.complete();
    this.receivingStarted.complete();
    this.receivingStopped.complete();
    this._activeSiblings.clear();
    this._scrollNode = null;
    this._parentPositions.clear();
    this._dragDropRegistry.removeDropContainer(this);
  }
  isDragging() {
    return this._isDragging;
  }
  start() {
    this._draggingStarted();
    this._notifyReceivingSiblings();
  }
  enter(item, pointerX, pointerY, index) {
    this._draggingStarted();
    if (index == null && this.sortingDisabled) {
      index = this._draggables.indexOf(item);
    }
    this._sortStrategy.enter(item, pointerX, pointerY, index);
    this._cacheParentPositions();
    this._notifyReceivingSiblings();
    this.entered.next({
      item,
      container: this,
      currentIndex: this.getItemIndex(item)
    });
  }
  exit(item) {
    this._reset();
    this.exited.next({
      item,
      container: this
    });
  }
  drop(item, currentIndex, previousIndex, previousContainer, isPointerOverContainer, distance2, dropPoint, event = {}) {
    this._reset();
    this.dropped.next({
      item,
      currentIndex,
      previousIndex,
      container: this,
      previousContainer,
      isPointerOverContainer,
      distance: distance2,
      dropPoint,
      event
    });
  }
  withItems(items) {
    const previousItems = this._draggables;
    this._draggables = items;
    items.forEach((item) => item._withDropContainer(this));
    if (this.isDragging()) {
      const draggedItems = previousItems.filter((item) => item.isDragging());
      if (draggedItems.every((item) => items.indexOf(item) === -1)) {
        this._reset();
      } else {
        this._sortStrategy.withItems(this._draggables);
      }
    }
    return this;
  }
  withDirection(direction) {
    this._direction = direction;
    if (this._sortStrategy instanceof SingleAxisSortStrategy) {
      this._sortStrategy.direction = direction;
    }
    return this;
  }
  connectedTo(connectedTo) {
    this._siblings = connectedTo.slice();
    return this;
  }
  withOrientation(orientation) {
    if (orientation === "mixed") {
      this._sortStrategy = new MixedSortStrategy(this._document, this._dragDropRegistry);
    } else {
      const strategy = new SingleAxisSortStrategy(this._dragDropRegistry);
      strategy.direction = this._direction;
      strategy.orientation = orientation;
      this._sortStrategy = strategy;
    }
    this._sortStrategy.withElementContainer(this._container);
    this._sortStrategy.withSortPredicate((index, item) => this.sortPredicate(index, item, this));
    return this;
  }
  withScrollableParents(elements) {
    const element = this._container;
    this._scrollableElements = elements.indexOf(element) === -1 ? [element, ...elements] : elements.slice();
    return this;
  }
  withElementContainer(container) {
    if (container === this._container) {
      return this;
    }
    const element = coerceElement(this.element);
    if ((typeof ngDevMode === "undefined" || ngDevMode) && container !== element && !element.contains(container)) {
      throw new Error("Invalid DOM structure for drop list. Alternate container element must be a descendant of the drop list.");
    }
    const oldContainerIndex = this._scrollableElements.indexOf(this._container);
    const newContainerIndex = this._scrollableElements.indexOf(container);
    if (oldContainerIndex > -1) {
      this._scrollableElements.splice(oldContainerIndex, 1);
    }
    if (newContainerIndex > -1) {
      this._scrollableElements.splice(newContainerIndex, 1);
    }
    if (this._sortStrategy) {
      this._sortStrategy.withElementContainer(container);
    }
    this._cachedShadowRoot = null;
    this._scrollableElements.unshift(container);
    this._container = container;
    return this;
  }
  getScrollableParents() {
    return this._scrollableElements;
  }
  getItemIndex(item) {
    return this._isDragging ? this._sortStrategy.getItemIndex(item) : this._draggables.indexOf(item);
  }
  getItemAtIndex(index) {
    return this._isDragging ? this._sortStrategy.getItemAtIndex(index) : this._draggables[index] || null;
  }
  isReceiving() {
    return this._activeSiblings.size > 0;
  }
  _sortItem(item, pointerX, pointerY, pointerDelta) {
    if (this.sortingDisabled || !this._domRect || !isPointerNearDomRect(this._domRect, DROP_PROXIMITY_THRESHOLD, pointerX, pointerY)) {
      return;
    }
    const result = this._sortStrategy.sort(item, pointerX, pointerY, pointerDelta);
    if (result) {
      this.sorted.next({
        previousIndex: result.previousIndex,
        currentIndex: result.currentIndex,
        container: this,
        item
      });
    }
  }
  _startScrollingIfNecessary(pointerX, pointerY) {
    if (this.autoScrollDisabled) {
      return;
    }
    let scrollNode;
    let verticalScrollDirection = AutoScrollVerticalDirection.NONE;
    let horizontalScrollDirection = AutoScrollHorizontalDirection.NONE;
    this._parentPositions.positions.forEach((position, element) => {
      if (element === this._document || !position.clientRect || scrollNode) {
        return;
      }
      if (isPointerNearDomRect(position.clientRect, DROP_PROXIMITY_THRESHOLD, pointerX, pointerY)) {
        [verticalScrollDirection, horizontalScrollDirection] = getElementScrollDirections(element, position.clientRect, this._direction, pointerX, pointerY);
        if (verticalScrollDirection || horizontalScrollDirection) {
          scrollNode = element;
        }
      }
    });
    if (!verticalScrollDirection && !horizontalScrollDirection) {
      const {
        width,
        height
      } = this._viewportRuler.getViewportSize();
      const domRect = {
        width,
        height,
        top: 0,
        right: width,
        bottom: height,
        left: 0
      };
      verticalScrollDirection = getVerticalScrollDirection(domRect, pointerY);
      horizontalScrollDirection = getHorizontalScrollDirection(domRect, pointerX);
      scrollNode = window;
    }
    if (scrollNode && (verticalScrollDirection !== this._verticalScrollDirection || horizontalScrollDirection !== this._horizontalScrollDirection || scrollNode !== this._scrollNode)) {
      this._verticalScrollDirection = verticalScrollDirection;
      this._horizontalScrollDirection = horizontalScrollDirection;
      this._scrollNode = scrollNode;
      if ((verticalScrollDirection || horizontalScrollDirection) && scrollNode) {
        this._ngZone.runOutsideAngular(this._startScrollInterval);
      } else {
        this._stopScrolling();
      }
    }
  }
  _stopScrolling() {
    this._stopScrollTimers.next();
  }
  _draggingStarted() {
    const styles = this._container.style;
    this.beforeStarted.next();
    this._isDragging = true;
    if ((typeof ngDevMode === "undefined" || ngDevMode) && this._container !== coerceElement(this.element)) {
      for (const drag of this._draggables) {
        if (!drag.isDragging() && drag.getVisibleElement().parentNode !== this._container) {
          throw new Error("Invalid DOM structure for drop list. All items must be placed directly inside of the element container.");
        }
      }
    }
    this._initialScrollSnap = styles.msScrollSnapType || styles.scrollSnapType || "";
    styles.scrollSnapType = styles.msScrollSnapType = "none";
    this._sortStrategy.start(this._draggables);
    this._cacheParentPositions();
    this._viewportScrollSubscription.unsubscribe();
    this._listenToScrollEvents();
  }
  _cacheParentPositions() {
    this._parentPositions.cache(this._scrollableElements);
    this._domRect = this._parentPositions.positions.get(this._container).clientRect;
  }
  _reset() {
    this._isDragging = false;
    const styles = this._container.style;
    styles.scrollSnapType = styles.msScrollSnapType = this._initialScrollSnap;
    this._siblings.forEach((sibling) => sibling._stopReceiving(this));
    this._sortStrategy.reset();
    this._stopScrolling();
    this._viewportScrollSubscription.unsubscribe();
    this._parentPositions.clear();
  }
  _startScrollInterval = () => {
    this._stopScrolling();
    interval(0, animationFrameScheduler).pipe(takeUntil(this._stopScrollTimers)).subscribe(() => {
      const node = this._scrollNode;
      const scrollStep = this.autoScrollStep;
      if (this._verticalScrollDirection === AutoScrollVerticalDirection.UP) {
        node.scrollBy(0, -scrollStep);
      } else if (this._verticalScrollDirection === AutoScrollVerticalDirection.DOWN) {
        node.scrollBy(0, scrollStep);
      }
      if (this._horizontalScrollDirection === AutoScrollHorizontalDirection.LEFT) {
        node.scrollBy(-scrollStep, 0);
      } else if (this._horizontalScrollDirection === AutoScrollHorizontalDirection.RIGHT) {
        node.scrollBy(scrollStep, 0);
      }
    });
  };
  _isOverContainer(x, y) {
    return this._domRect != null && isInsideClientRect(this._domRect, x, y);
  }
  _getSiblingContainerFromPosition(item, x, y) {
    return this._siblings.find((sibling) => sibling._canReceive(item, x, y));
  }
  _canReceive(item, x, y) {
    if (!this._domRect || !isInsideClientRect(this._domRect, x, y) || !this.enterPredicate(item, this)) {
      return false;
    }
    const elementFromPoint = this._getShadowRoot().elementFromPoint(x, y);
    if (!elementFromPoint) {
      return false;
    }
    return elementFromPoint === this._container || this._container.contains(elementFromPoint);
  }
  _startReceiving(sibling, items) {
    const activeSiblings = this._activeSiblings;
    if (!activeSiblings.has(sibling) && items.every((item) => {
      return this.enterPredicate(item, this) || this._draggables.indexOf(item) > -1;
    })) {
      activeSiblings.add(sibling);
      this._cacheParentPositions();
      this._listenToScrollEvents();
      this.receivingStarted.next({
        initiator: sibling,
        receiver: this,
        items
      });
    }
  }
  _stopReceiving(sibling) {
    this._activeSiblings.delete(sibling);
    this._viewportScrollSubscription.unsubscribe();
    this.receivingStopped.next({
      initiator: sibling,
      receiver: this
    });
  }
  _listenToScrollEvents() {
    this._viewportScrollSubscription = this._dragDropRegistry.scrolled(this._getShadowRoot()).subscribe((event) => {
      if (this.isDragging()) {
        const scrollDifference = this._parentPositions.handleScroll(event);
        if (scrollDifference) {
          this._sortStrategy.updateOnScroll(scrollDifference.top, scrollDifference.left);
        }
      } else if (this.isReceiving()) {
        this._cacheParentPositions();
      }
    });
  }
  _getShadowRoot() {
    if (!this._cachedShadowRoot) {
      const shadowRoot = _getShadowRoot(this._container);
      this._cachedShadowRoot = shadowRoot || this._document;
    }
    return this._cachedShadowRoot;
  }
  _notifyReceivingSiblings() {
    const draggedItems = this._sortStrategy.getActiveItemsSnapshot().filter((item) => item.isDragging());
    this._siblings.forEach((sibling) => sibling._startReceiving(this, draggedItems));
  }
};
function getVerticalScrollDirection(clientRect, pointerY) {
  const {
    top,
    bottom,
    height
  } = clientRect;
  const yThreshold = height * SCROLL_PROXIMITY_THRESHOLD;
  if (pointerY >= top - yThreshold && pointerY <= top + yThreshold) {
    return AutoScrollVerticalDirection.UP;
  } else if (pointerY >= bottom - yThreshold && pointerY <= bottom + yThreshold) {
    return AutoScrollVerticalDirection.DOWN;
  }
  return AutoScrollVerticalDirection.NONE;
}
function getHorizontalScrollDirection(clientRect, pointerX) {
  const {
    left,
    right,
    width
  } = clientRect;
  const xThreshold = width * SCROLL_PROXIMITY_THRESHOLD;
  if (pointerX >= left - xThreshold && pointerX <= left + xThreshold) {
    return AutoScrollHorizontalDirection.LEFT;
  } else if (pointerX >= right - xThreshold && pointerX <= right + xThreshold) {
    return AutoScrollHorizontalDirection.RIGHT;
  }
  return AutoScrollHorizontalDirection.NONE;
}
function getElementScrollDirections(element, clientRect, direction, pointerX, pointerY) {
  const computedVertical = getVerticalScrollDirection(clientRect, pointerY);
  const computedHorizontal = getHorizontalScrollDirection(clientRect, pointerX);
  let verticalScrollDirection = AutoScrollVerticalDirection.NONE;
  let horizontalScrollDirection = AutoScrollHorizontalDirection.NONE;
  if (computedVertical) {
    const scrollTop = element.scrollTop;
    if (computedVertical === AutoScrollVerticalDirection.UP) {
      if (scrollTop > 0) {
        verticalScrollDirection = AutoScrollVerticalDirection.UP;
      }
    } else if (element.scrollHeight - scrollTop > element.clientHeight) {
      verticalScrollDirection = AutoScrollVerticalDirection.DOWN;
    }
  }
  if (computedHorizontal) {
    const scrollLeft = element.scrollLeft;
    if (direction === "rtl") {
      if (computedHorizontal === AutoScrollHorizontalDirection.RIGHT) {
        if (scrollLeft < 0) {
          horizontalScrollDirection = AutoScrollHorizontalDirection.RIGHT;
        }
      } else if (element.scrollWidth + scrollLeft > element.clientWidth) {
        horizontalScrollDirection = AutoScrollHorizontalDirection.LEFT;
      }
    } else {
      if (computedHorizontal === AutoScrollHorizontalDirection.LEFT) {
        if (scrollLeft > 0) {
          horizontalScrollDirection = AutoScrollHorizontalDirection.LEFT;
        }
      } else if (element.scrollWidth - scrollLeft > element.clientWidth) {
        horizontalScrollDirection = AutoScrollHorizontalDirection.RIGHT;
      }
    }
  }
  return [verticalScrollDirection, horizontalScrollDirection];
}
var DragDrop = class _DragDrop {
  _injector = inject(Injector);
  constructor() {
  }
  createDrag(element, config) {
    return createDragRef(this._injector, element, config);
  }
  createDropList(element) {
    return createDropListRef(this._injector, element);
  }
  static ɵfac = function DragDrop_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _DragDrop)();
  };
  static ɵprov = ɵɵdefineInjectable({
    token: _DragDrop,
    factory: _DragDrop.ɵfac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(DragDrop, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [], null);
})();
var CDK_DRAG_PARENT = new InjectionToken("CDK_DRAG_PARENT");
function assertElementNode(node, name) {
  if (node.nodeType !== 1) {
    throw Error(`${name} must be attached to an element node. Currently attached to "${node.nodeName}".`);
  }
}
var CDK_DRAG_HANDLE = new InjectionToken("CdkDragHandle");
var CdkDragHandle = class _CdkDragHandle {
  element = inject(ElementRef);
  _parentDrag = inject(CDK_DRAG_PARENT, {
    optional: true,
    skipSelf: true
  });
  _dragDropRegistry = inject(DragDropRegistry);
  _stateChanges = new Subject();
  get disabled() {
    return this._disabled;
  }
  set disabled(value) {
    this._disabled = value;
    this._stateChanges.next(this);
  }
  _disabled = false;
  constructor() {
    if (typeof ngDevMode === "undefined" || ngDevMode) {
      assertElementNode(this.element.nativeElement, "cdkDragHandle");
    }
    this._parentDrag?._addHandle(this);
  }
  ngAfterViewInit() {
    if (!this._parentDrag) {
      let parent = this.element.nativeElement.parentElement;
      while (parent) {
        const ref = this._dragDropRegistry.getDragDirectiveForNode(parent);
        if (ref) {
          this._parentDrag = ref;
          ref._addHandle(this);
          break;
        }
        parent = parent.parentElement;
      }
    }
  }
  ngOnDestroy() {
    this._parentDrag?._removeHandle(this);
    this._stateChanges.complete();
  }
  static ɵfac = function CdkDragHandle_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _CdkDragHandle)();
  };
  static ɵdir = ɵɵdefineDirective({
    type: _CdkDragHandle,
    selectors: [["", "cdkDragHandle", ""]],
    hostAttrs: [1, "cdk-drag-handle"],
    inputs: {
      disabled: [2, "cdkDragHandleDisabled", "disabled", booleanAttribute]
    },
    features: [ɵɵProvidersFeature([{
      provide: CDK_DRAG_HANDLE,
      useExisting: _CdkDragHandle
    }])]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(CdkDragHandle, [{
    type: Directive,
    args: [{
      selector: "[cdkDragHandle]",
      host: {
        "class": "cdk-drag-handle"
      },
      providers: [{
        provide: CDK_DRAG_HANDLE,
        useExisting: CdkDragHandle
      }]
    }]
  }], () => [], {
    disabled: [{
      type: Input,
      args: [{
        alias: "cdkDragHandleDisabled",
        transform: booleanAttribute
      }]
    }]
  });
})();
var CDK_DRAG_CONFIG = new InjectionToken("CDK_DRAG_CONFIG");
var CDK_DROP_LIST = new InjectionToken("CdkDropList");
var CdkDrag = class _CdkDrag {
  element = inject(ElementRef);
  dropContainer = inject(CDK_DROP_LIST, {
    optional: true,
    skipSelf: true
  });
  _ngZone = inject(NgZone);
  _viewContainerRef = inject(ViewContainerRef);
  _dir = inject(Directionality, {
    optional: true
  });
  _changeDetectorRef = inject(ChangeDetectorRef);
  _selfHandle = inject(CDK_DRAG_HANDLE, {
    optional: true,
    self: true
  });
  _parentDrag = inject(CDK_DRAG_PARENT, {
    optional: true,
    skipSelf: true
  });
  _dragDropRegistry = inject(DragDropRegistry);
  _destroyed = new Subject();
  _handles = new BehaviorSubject([]);
  _previewTemplate = null;
  _placeholderTemplate = null;
  _dragRef;
  data;
  lockAxis = null;
  rootElementSelector;
  boundaryElement;
  dragStartDelay;
  freeDragPosition;
  get disabled() {
    return this._disabled || !!(this.dropContainer && this.dropContainer.disabled);
  }
  set disabled(value) {
    this._disabled = value;
    this._dragRef.disabled = this._disabled;
  }
  _disabled = false;
  constrainPosition;
  previewClass;
  previewContainer;
  scale = 1;
  started = new EventEmitter();
  released = new EventEmitter();
  ended = new EventEmitter();
  entered = new EventEmitter();
  exited = new EventEmitter();
  dropped = new EventEmitter();
  moved = new Observable((observer) => {
    const subscription = this._dragRef.moved.pipe(map((movedEvent) => ({
      source: this,
      pointerPosition: movedEvent.pointerPosition,
      event: movedEvent.event,
      delta: movedEvent.delta,
      distance: movedEvent.distance
    }))).subscribe(observer);
    return () => {
      subscription.unsubscribe();
    };
  });
  _injector = inject(Injector);
  constructor() {
    const dropContainer = this.dropContainer;
    const config = inject(CDK_DRAG_CONFIG, {
      optional: true
    });
    this._dragRef = createDragRef(this._injector, this.element, {
      dragStartThreshold: config && config.dragStartThreshold != null ? config.dragStartThreshold : 5,
      pointerDirectionChangeThreshold: config && config.pointerDirectionChangeThreshold != null ? config.pointerDirectionChangeThreshold : 5,
      zIndex: config?.zIndex
    });
    this._dragRef.data = this;
    this._dragDropRegistry.registerDirectiveNode(this.element.nativeElement, this);
    if (config) {
      this._assignDefaults(config);
    }
    if (dropContainer) {
      dropContainer.addItem(this);
      dropContainer._dropListRef.beforeStarted.pipe(takeUntil(this._destroyed)).subscribe(() => {
        this._dragRef.scale = this.scale;
      });
    }
    this._syncInputs(this._dragRef);
    this._handleEvents(this._dragRef);
  }
  getPlaceholderElement() {
    return this._dragRef.getPlaceholderElement();
  }
  getRootElement() {
    return this._dragRef.getRootElement();
  }
  reset() {
    this._dragRef.reset();
  }
  resetToBoundary() {
    this._dragRef.resetToBoundary();
  }
  getFreeDragPosition() {
    return this._dragRef.getFreeDragPosition();
  }
  setFreeDragPosition(value) {
    this._dragRef.setFreeDragPosition(value);
  }
  ngAfterViewInit() {
    afterNextRender(() => {
      this._updateRootElement();
      this._setupHandlesListener();
      this._dragRef.scale = this.scale;
      if (this.freeDragPosition) {
        this._dragRef.setFreeDragPosition(this.freeDragPosition);
      }
    }, {
      injector: this._injector
    });
  }
  ngOnChanges(changes) {
    const rootSelectorChange = changes["rootElementSelector"];
    const positionChange = changes["freeDragPosition"];
    if (rootSelectorChange && !rootSelectorChange.firstChange) {
      this._updateRootElement();
    }
    this._dragRef.scale = this.scale;
    if (positionChange && !positionChange.firstChange && this.freeDragPosition) {
      this._dragRef.setFreeDragPosition(this.freeDragPosition);
    }
  }
  ngOnDestroy() {
    if (this.dropContainer) {
      this.dropContainer.removeItem(this);
    }
    this._dragDropRegistry.removeDirectiveNode(this.element.nativeElement);
    this._ngZone.runOutsideAngular(() => {
      this._handles.complete();
      this._destroyed.next();
      this._destroyed.complete();
      this._dragRef.dispose();
    });
  }
  _addHandle(handle) {
    const handles = this._handles.getValue();
    handles.push(handle);
    this._handles.next(handles);
  }
  _removeHandle(handle) {
    const handles = this._handles.getValue();
    const index = handles.indexOf(handle);
    if (index > -1) {
      handles.splice(index, 1);
      this._handles.next(handles);
    }
  }
  _setPreviewTemplate(preview) {
    this._previewTemplate = preview;
  }
  _resetPreviewTemplate(preview) {
    if (preview === this._previewTemplate) {
      this._previewTemplate = null;
    }
  }
  _setPlaceholderTemplate(placeholder) {
    this._placeholderTemplate = placeholder;
  }
  _resetPlaceholderTemplate(placeholder) {
    if (placeholder === this._placeholderTemplate) {
      this._placeholderTemplate = null;
    }
  }
  _updateRootElement() {
    const element = this.element.nativeElement;
    let rootElement = element;
    if (this.rootElementSelector) {
      rootElement = element.closest !== void 0 ? element.closest(this.rootElementSelector) : element.parentElement?.closest(this.rootElementSelector);
    }
    if (rootElement && (typeof ngDevMode === "undefined" || ngDevMode)) {
      assertElementNode(rootElement, "cdkDrag");
    }
    this._dragRef.withRootElement(rootElement || element);
  }
  _getBoundaryElement() {
    const boundary = this.boundaryElement;
    if (!boundary) {
      return null;
    }
    if (typeof boundary === "string") {
      return this.element.nativeElement.closest(boundary);
    }
    return coerceElement(boundary);
  }
  _syncInputs(ref) {
    ref.beforeStarted.subscribe(() => {
      if (!ref.isDragging()) {
        const dir3 = this._dir;
        const dragStartDelay = this.dragStartDelay;
        const placeholder = this._placeholderTemplate ? {
          template: this._placeholderTemplate.templateRef,
          context: this._placeholderTemplate.data,
          viewContainer: this._viewContainerRef
        } : null;
        const preview = this._previewTemplate ? {
          template: this._previewTemplate.templateRef,
          context: this._previewTemplate.data,
          matchSize: this._previewTemplate.matchSize,
          viewContainer: this._viewContainerRef
        } : null;
        ref.disabled = this.disabled;
        ref.lockAxis = this.lockAxis;
        ref.scale = this.scale;
        ref.dragStartDelay = typeof dragStartDelay === "object" && dragStartDelay ? dragStartDelay : coerceNumberProperty(dragStartDelay);
        ref.constrainPosition = this.constrainPosition;
        ref.previewClass = this.previewClass;
        ref.withBoundaryElement(this._getBoundaryElement()).withPlaceholderTemplate(placeholder).withPreviewTemplate(preview).withPreviewContainer(this.previewContainer || "global");
        if (dir3) {
          ref.withDirection(dir3.value);
        }
      }
    });
    ref.beforeStarted.pipe(take(1)).subscribe(() => {
      if (this._parentDrag) {
        ref.withParent(this._parentDrag._dragRef);
        return;
      }
      let parent = this.element.nativeElement.parentElement;
      while (parent) {
        const parentDrag = this._dragDropRegistry.getDragDirectiveForNode(parent);
        if (parentDrag) {
          ref.withParent(parentDrag._dragRef);
          break;
        }
        parent = parent.parentElement;
      }
    });
  }
  _handleEvents(ref) {
    ref.started.subscribe((startEvent) => {
      this.started.emit({
        source: this,
        event: startEvent.event
      });
      this._changeDetectorRef.markForCheck();
    });
    ref.released.subscribe((releaseEvent) => {
      this.released.emit({
        source: this,
        event: releaseEvent.event
      });
    });
    ref.ended.subscribe((endEvent) => {
      this.ended.emit({
        source: this,
        distance: endEvent.distance,
        dropPoint: endEvent.dropPoint,
        event: endEvent.event
      });
      this._changeDetectorRef.markForCheck();
    });
    ref.entered.subscribe((enterEvent) => {
      this.entered.emit({
        container: enterEvent.container.data,
        item: this,
        currentIndex: enterEvent.currentIndex
      });
    });
    ref.exited.subscribe((exitEvent) => {
      this.exited.emit({
        container: exitEvent.container.data,
        item: this
      });
    });
    ref.dropped.subscribe((dropEvent) => {
      this.dropped.emit({
        previousIndex: dropEvent.previousIndex,
        currentIndex: dropEvent.currentIndex,
        previousContainer: dropEvent.previousContainer.data,
        container: dropEvent.container.data,
        isPointerOverContainer: dropEvent.isPointerOverContainer,
        item: this,
        distance: dropEvent.distance,
        dropPoint: dropEvent.dropPoint,
        event: dropEvent.event
      });
    });
  }
  _assignDefaults(config) {
    const {
      lockAxis,
      dragStartDelay,
      constrainPosition,
      previewClass,
      boundaryElement,
      draggingDisabled,
      rootElementSelector,
      previewContainer
    } = config;
    this.disabled = draggingDisabled == null ? false : draggingDisabled;
    this.dragStartDelay = dragStartDelay || 0;
    this.lockAxis = lockAxis || null;
    if (constrainPosition) {
      this.constrainPosition = constrainPosition;
    }
    if (previewClass) {
      this.previewClass = previewClass;
    }
    if (boundaryElement) {
      this.boundaryElement = boundaryElement;
    }
    if (rootElementSelector) {
      this.rootElementSelector = rootElementSelector;
    }
    if (previewContainer) {
      this.previewContainer = previewContainer;
    }
  }
  _setupHandlesListener() {
    this._handles.pipe(tap((handles) => {
      const handleElements = handles.map((handle) => handle.element);
      if (this._selfHandle && this.rootElementSelector) {
        handleElements.push(this.element);
      }
      this._dragRef.withHandles(handleElements);
    }), switchMap((handles) => {
      return merge(...handles.map((item) => item._stateChanges.pipe(startWith(item))));
    }), takeUntil(this._destroyed)).subscribe((handleInstance) => {
      const dragRef = this._dragRef;
      const handle = handleInstance.element.nativeElement;
      handleInstance.disabled ? dragRef.disableHandle(handle) : dragRef.enableHandle(handle);
    });
  }
  static ɵfac = function CdkDrag_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _CdkDrag)();
  };
  static ɵdir = ɵɵdefineDirective({
    type: _CdkDrag,
    selectors: [["", "cdkDrag", ""]],
    hostAttrs: [1, "cdk-drag"],
    hostVars: 4,
    hostBindings: function CdkDrag_HostBindings(rf, ctx) {
      if (rf & 2) {
        ɵɵclassProp("cdk-drag-disabled", ctx.disabled)("cdk-drag-dragging", ctx._dragRef.isDragging());
      }
    },
    inputs: {
      data: [0, "cdkDragData", "data"],
      lockAxis: [0, "cdkDragLockAxis", "lockAxis"],
      rootElementSelector: [0, "cdkDragRootElement", "rootElementSelector"],
      boundaryElement: [0, "cdkDragBoundary", "boundaryElement"],
      dragStartDelay: [0, "cdkDragStartDelay", "dragStartDelay"],
      freeDragPosition: [0, "cdkDragFreeDragPosition", "freeDragPosition"],
      disabled: [2, "cdkDragDisabled", "disabled", booleanAttribute],
      constrainPosition: [0, "cdkDragConstrainPosition", "constrainPosition"],
      previewClass: [0, "cdkDragPreviewClass", "previewClass"],
      previewContainer: [0, "cdkDragPreviewContainer", "previewContainer"],
      scale: [2, "cdkDragScale", "scale", numberAttribute]
    },
    outputs: {
      started: "cdkDragStarted",
      released: "cdkDragReleased",
      ended: "cdkDragEnded",
      entered: "cdkDragEntered",
      exited: "cdkDragExited",
      dropped: "cdkDragDropped",
      moved: "cdkDragMoved"
    },
    exportAs: ["cdkDrag"],
    features: [ɵɵProvidersFeature([{
      provide: CDK_DRAG_PARENT,
      useExisting: _CdkDrag
    }]), ɵɵNgOnChangesFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(CdkDrag, [{
    type: Directive,
    args: [{
      selector: "[cdkDrag]",
      exportAs: "cdkDrag",
      host: {
        "class": "cdk-drag",
        "[class.cdk-drag-disabled]": "disabled",
        "[class.cdk-drag-dragging]": "_dragRef.isDragging()"
      },
      providers: [{
        provide: CDK_DRAG_PARENT,
        useExisting: CdkDrag
      }]
    }]
  }], () => [], {
    data: [{
      type: Input,
      args: ["cdkDragData"]
    }],
    lockAxis: [{
      type: Input,
      args: ["cdkDragLockAxis"]
    }],
    rootElementSelector: [{
      type: Input,
      args: ["cdkDragRootElement"]
    }],
    boundaryElement: [{
      type: Input,
      args: ["cdkDragBoundary"]
    }],
    dragStartDelay: [{
      type: Input,
      args: ["cdkDragStartDelay"]
    }],
    freeDragPosition: [{
      type: Input,
      args: ["cdkDragFreeDragPosition"]
    }],
    disabled: [{
      type: Input,
      args: [{
        alias: "cdkDragDisabled",
        transform: booleanAttribute
      }]
    }],
    constrainPosition: [{
      type: Input,
      args: ["cdkDragConstrainPosition"]
    }],
    previewClass: [{
      type: Input,
      args: ["cdkDragPreviewClass"]
    }],
    previewContainer: [{
      type: Input,
      args: ["cdkDragPreviewContainer"]
    }],
    scale: [{
      type: Input,
      args: [{
        alias: "cdkDragScale",
        transform: numberAttribute
      }]
    }],
    started: [{
      type: Output,
      args: ["cdkDragStarted"]
    }],
    released: [{
      type: Output,
      args: ["cdkDragReleased"]
    }],
    ended: [{
      type: Output,
      args: ["cdkDragEnded"]
    }],
    entered: [{
      type: Output,
      args: ["cdkDragEntered"]
    }],
    exited: [{
      type: Output,
      args: ["cdkDragExited"]
    }],
    dropped: [{
      type: Output,
      args: ["cdkDragDropped"]
    }],
    moved: [{
      type: Output,
      args: ["cdkDragMoved"]
    }]
  });
})();
var CDK_DROP_LIST_GROUP = new InjectionToken("CdkDropListGroup");
var CdkDropListGroup = class _CdkDropListGroup {
  _items = /* @__PURE__ */ new Set();
  disabled = false;
  ngOnDestroy() {
    this._items.clear();
  }
  static ɵfac = function CdkDropListGroup_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _CdkDropListGroup)();
  };
  static ɵdir = ɵɵdefineDirective({
    type: _CdkDropListGroup,
    selectors: [["", "cdkDropListGroup", ""]],
    inputs: {
      disabled: [2, "cdkDropListGroupDisabled", "disabled", booleanAttribute]
    },
    exportAs: ["cdkDropListGroup"],
    features: [ɵɵProvidersFeature([{
      provide: CDK_DROP_LIST_GROUP,
      useExisting: _CdkDropListGroup
    }])]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(CdkDropListGroup, [{
    type: Directive,
    args: [{
      selector: "[cdkDropListGroup]",
      exportAs: "cdkDropListGroup",
      providers: [{
        provide: CDK_DROP_LIST_GROUP,
        useExisting: CdkDropListGroup
      }]
    }]
  }], null, {
    disabled: [{
      type: Input,
      args: [{
        alias: "cdkDropListGroupDisabled",
        transform: booleanAttribute
      }]
    }]
  });
})();
var CdkDropList = class _CdkDropList {
  element = inject(ElementRef);
  _changeDetectorRef = inject(ChangeDetectorRef);
  _scrollDispatcher = inject(ScrollDispatcher);
  _dir = inject(Directionality, {
    optional: true
  });
  _group = inject(CDK_DROP_LIST_GROUP, {
    optional: true,
    skipSelf: true
  });
  _latestSortedRefs;
  _destroyed = new Subject();
  _scrollableParentsResolved = false;
  static _dropLists = [];
  _dropListRef;
  connectedTo = [];
  data;
  orientation = "vertical";
  id = inject(_IdGenerator).getId("cdk-drop-list-");
  lockAxis = null;
  get disabled() {
    return this._disabled || !!this._group && this._group.disabled;
  }
  set disabled(value) {
    this._dropListRef.disabled = this._disabled = value;
  }
  _disabled = false;
  sortingDisabled = false;
  enterPredicate = () => true;
  sortPredicate = () => true;
  autoScrollDisabled = false;
  autoScrollStep;
  elementContainerSelector = null;
  hasAnchor = false;
  dropped = new EventEmitter();
  entered = new EventEmitter();
  exited = new EventEmitter();
  sorted = new EventEmitter();
  _unsortedItems = /* @__PURE__ */ new Set();
  constructor() {
    const config = inject(CDK_DRAG_CONFIG, {
      optional: true
    });
    const injector = inject(Injector);
    if (typeof ngDevMode === "undefined" || ngDevMode) {
      assertElementNode(this.element.nativeElement, "cdkDropList");
    }
    this._dropListRef = createDropListRef(injector, this.element);
    this._dropListRef.data = this;
    if (config) {
      this._assignDefaults(config);
    }
    this._dropListRef.enterPredicate = (drag, drop) => {
      return this.enterPredicate(drag.data, drop.data);
    };
    this._dropListRef.sortPredicate = (index, drag, drop) => {
      return this.sortPredicate(index, drag.data, drop.data);
    };
    this._setupInputSyncSubscription(this._dropListRef);
    this._handleEvents(this._dropListRef);
    _CdkDropList._dropLists.push(this);
    if (this._group) {
      this._group._items.add(this);
    }
  }
  addItem(item) {
    this._unsortedItems.add(item);
    item._dragRef._withDropContainer(this._dropListRef);
    if (this._dropListRef.isDragging()) {
      this._syncItemsWithRef(this.getSortedItems().map((item2) => item2._dragRef));
    }
  }
  removeItem(item) {
    this._unsortedItems.delete(item);
    if (this._latestSortedRefs) {
      const index = this._latestSortedRefs.indexOf(item._dragRef);
      if (index > -1) {
        this._latestSortedRefs.splice(index, 1);
        this._syncItemsWithRef(this._latestSortedRefs);
      }
    }
  }
  getSortedItems() {
    return Array.from(this._unsortedItems).sort((a, b) => {
      const documentPosition = a._dragRef.getVisibleElement().compareDocumentPosition(b._dragRef.getVisibleElement());
      return documentPosition & Node.DOCUMENT_POSITION_FOLLOWING ? -1 : 1;
    });
  }
  ngOnDestroy() {
    const index = _CdkDropList._dropLists.indexOf(this);
    if (index > -1) {
      _CdkDropList._dropLists.splice(index, 1);
    }
    if (this._group) {
      this._group._items.delete(this);
    }
    this._latestSortedRefs = void 0;
    this._unsortedItems.clear();
    this._dropListRef.dispose();
    this._destroyed.next();
    this._destroyed.complete();
  }
  _setupInputSyncSubscription(ref) {
    if (this._dir) {
      this._dir.change.pipe(startWith(this._dir.value), takeUntil(this._destroyed)).subscribe((value) => ref.withDirection(value));
    }
    ref.beforeStarted.subscribe(() => {
      const siblings = coerceArray(this.connectedTo).map((drop) => {
        if (typeof drop === "string") {
          const correspondingDropList = _CdkDropList._dropLists.find((list) => list.id === drop);
          if (!correspondingDropList && (typeof ngDevMode === "undefined" || ngDevMode)) {
            console.warn(`CdkDropList could not find connected drop list with id "${drop}"`);
          }
          return correspondingDropList;
        }
        return drop;
      });
      if (this._group) {
        this._group._items.forEach((drop) => {
          if (siblings.indexOf(drop) === -1) {
            siblings.push(drop);
          }
        });
      }
      if (!this._scrollableParentsResolved) {
        const scrollableParents = this._scrollDispatcher.getAncestorScrollContainers(this.element).map((scrollable) => scrollable.getElementRef().nativeElement);
        this._dropListRef.withScrollableParents(scrollableParents);
        this._scrollableParentsResolved = true;
      }
      if (this.elementContainerSelector) {
        const container = this.element.nativeElement.querySelector(this.elementContainerSelector);
        if (!container && (typeof ngDevMode === "undefined" || ngDevMode)) {
          throw new Error(`CdkDropList could not find an element container matching the selector "${this.elementContainerSelector}"`);
        }
        ref.withElementContainer(container);
      }
      ref.disabled = this.disabled;
      ref.lockAxis = this.lockAxis;
      ref.sortingDisabled = this.sortingDisabled;
      ref.autoScrollDisabled = this.autoScrollDisabled;
      ref.autoScrollStep = coerceNumberProperty(this.autoScrollStep, 2);
      ref.hasAnchor = this.hasAnchor;
      ref.connectedTo(siblings.filter((drop) => drop && drop !== this).map((list) => list._dropListRef)).withOrientation(this.orientation);
    });
  }
  _handleEvents(ref) {
    ref.beforeStarted.subscribe(() => {
      this._syncItemsWithRef(this.getSortedItems().map((item) => item._dragRef));
      this._changeDetectorRef.markForCheck();
    });
    ref.entered.subscribe((event) => {
      this.entered.emit({
        container: this,
        item: event.item.data,
        currentIndex: event.currentIndex
      });
    });
    ref.exited.subscribe((event) => {
      this.exited.emit({
        container: this,
        item: event.item.data
      });
      this._changeDetectorRef.markForCheck();
    });
    ref.sorted.subscribe((event) => {
      this.sorted.emit({
        previousIndex: event.previousIndex,
        currentIndex: event.currentIndex,
        container: this,
        item: event.item.data
      });
    });
    ref.dropped.subscribe((dropEvent) => {
      this.dropped.emit({
        previousIndex: dropEvent.previousIndex,
        currentIndex: dropEvent.currentIndex,
        previousContainer: dropEvent.previousContainer.data,
        container: dropEvent.container.data,
        item: dropEvent.item.data,
        isPointerOverContainer: dropEvent.isPointerOverContainer,
        distance: dropEvent.distance,
        dropPoint: dropEvent.dropPoint,
        event: dropEvent.event
      });
      this._changeDetectorRef.markForCheck();
    });
    merge(ref.receivingStarted, ref.receivingStopped).subscribe(() => this._changeDetectorRef.markForCheck());
  }
  _assignDefaults(config) {
    const {
      lockAxis,
      draggingDisabled,
      sortingDisabled,
      listAutoScrollDisabled,
      listOrientation
    } = config;
    this.disabled = draggingDisabled == null ? false : draggingDisabled;
    this.sortingDisabled = sortingDisabled == null ? false : sortingDisabled;
    this.autoScrollDisabled = listAutoScrollDisabled == null ? false : listAutoScrollDisabled;
    this.orientation = listOrientation || "vertical";
    this.lockAxis = lockAxis || null;
  }
  _syncItemsWithRef(items) {
    this._latestSortedRefs = items;
    this._dropListRef.withItems(items);
  }
  static ɵfac = function CdkDropList_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _CdkDropList)();
  };
  static ɵdir = ɵɵdefineDirective({
    type: _CdkDropList,
    selectors: [["", "cdkDropList", ""], ["cdk-drop-list"]],
    hostAttrs: [1, "cdk-drop-list"],
    hostVars: 7,
    hostBindings: function CdkDropList_HostBindings(rf, ctx) {
      if (rf & 2) {
        ɵɵattribute("id", ctx.id);
        ɵɵclassProp("cdk-drop-list-disabled", ctx.disabled)("cdk-drop-list-dragging", ctx._dropListRef.isDragging())("cdk-drop-list-receiving", ctx._dropListRef.isReceiving());
      }
    },
    inputs: {
      connectedTo: [0, "cdkDropListConnectedTo", "connectedTo"],
      data: [0, "cdkDropListData", "data"],
      orientation: [0, "cdkDropListOrientation", "orientation"],
      id: "id",
      lockAxis: [0, "cdkDropListLockAxis", "lockAxis"],
      disabled: [2, "cdkDropListDisabled", "disabled", booleanAttribute],
      sortingDisabled: [2, "cdkDropListSortingDisabled", "sortingDisabled", booleanAttribute],
      enterPredicate: [0, "cdkDropListEnterPredicate", "enterPredicate"],
      sortPredicate: [0, "cdkDropListSortPredicate", "sortPredicate"],
      autoScrollDisabled: [2, "cdkDropListAutoScrollDisabled", "autoScrollDisabled", booleanAttribute],
      autoScrollStep: [0, "cdkDropListAutoScrollStep", "autoScrollStep"],
      elementContainerSelector: [0, "cdkDropListElementContainer", "elementContainerSelector"],
      hasAnchor: [2, "cdkDropListHasAnchor", "hasAnchor", booleanAttribute]
    },
    outputs: {
      dropped: "cdkDropListDropped",
      entered: "cdkDropListEntered",
      exited: "cdkDropListExited",
      sorted: "cdkDropListSorted"
    },
    exportAs: ["cdkDropList"],
    features: [ɵɵProvidersFeature([{
      provide: CDK_DROP_LIST_GROUP,
      useValue: void 0
    }, {
      provide: CDK_DROP_LIST,
      useExisting: _CdkDropList
    }])]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(CdkDropList, [{
    type: Directive,
    args: [{
      selector: "[cdkDropList], cdk-drop-list",
      exportAs: "cdkDropList",
      providers: [{
        provide: CDK_DROP_LIST_GROUP,
        useValue: void 0
      }, {
        provide: CDK_DROP_LIST,
        useExisting: CdkDropList
      }],
      host: {
        "class": "cdk-drop-list",
        "[attr.id]": "id",
        "[class.cdk-drop-list-disabled]": "disabled",
        "[class.cdk-drop-list-dragging]": "_dropListRef.isDragging()",
        "[class.cdk-drop-list-receiving]": "_dropListRef.isReceiving()"
      }
    }]
  }], () => [], {
    connectedTo: [{
      type: Input,
      args: ["cdkDropListConnectedTo"]
    }],
    data: [{
      type: Input,
      args: ["cdkDropListData"]
    }],
    orientation: [{
      type: Input,
      args: ["cdkDropListOrientation"]
    }],
    id: [{
      type: Input
    }],
    lockAxis: [{
      type: Input,
      args: ["cdkDropListLockAxis"]
    }],
    disabled: [{
      type: Input,
      args: [{
        alias: "cdkDropListDisabled",
        transform: booleanAttribute
      }]
    }],
    sortingDisabled: [{
      type: Input,
      args: [{
        alias: "cdkDropListSortingDisabled",
        transform: booleanAttribute
      }]
    }],
    enterPredicate: [{
      type: Input,
      args: ["cdkDropListEnterPredicate"]
    }],
    sortPredicate: [{
      type: Input,
      args: ["cdkDropListSortPredicate"]
    }],
    autoScrollDisabled: [{
      type: Input,
      args: [{
        alias: "cdkDropListAutoScrollDisabled",
        transform: booleanAttribute
      }]
    }],
    autoScrollStep: [{
      type: Input,
      args: ["cdkDropListAutoScrollStep"]
    }],
    elementContainerSelector: [{
      type: Input,
      args: ["cdkDropListElementContainer"]
    }],
    hasAnchor: [{
      type: Input,
      args: [{
        alias: "cdkDropListHasAnchor",
        transform: booleanAttribute
      }]
    }],
    dropped: [{
      type: Output,
      args: ["cdkDropListDropped"]
    }],
    entered: [{
      type: Output,
      args: ["cdkDropListEntered"]
    }],
    exited: [{
      type: Output,
      args: ["cdkDropListExited"]
    }],
    sorted: [{
      type: Output,
      args: ["cdkDropListSorted"]
    }]
  });
})();
var CDK_DRAG_PREVIEW = new InjectionToken("CdkDragPreview");
var CdkDragPreview = class _CdkDragPreview {
  templateRef = inject(TemplateRef);
  _drag = inject(CDK_DRAG_PARENT, {
    optional: true
  });
  data;
  matchSize = false;
  constructor() {
    this._drag?._setPreviewTemplate(this);
  }
  ngOnDestroy() {
    this._drag?._resetPreviewTemplate(this);
  }
  static ɵfac = function CdkDragPreview_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _CdkDragPreview)();
  };
  static ɵdir = ɵɵdefineDirective({
    type: _CdkDragPreview,
    selectors: [["ng-template", "cdkDragPreview", ""]],
    inputs: {
      data: "data",
      matchSize: [2, "matchSize", "matchSize", booleanAttribute]
    },
    features: [ɵɵProvidersFeature([{
      provide: CDK_DRAG_PREVIEW,
      useExisting: _CdkDragPreview
    }])]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(CdkDragPreview, [{
    type: Directive,
    args: [{
      selector: "ng-template[cdkDragPreview]",
      providers: [{
        provide: CDK_DRAG_PREVIEW,
        useExisting: CdkDragPreview
      }]
    }]
  }], () => [], {
    data: [{
      type: Input
    }],
    matchSize: [{
      type: Input,
      args: [{
        transform: booleanAttribute
      }]
    }]
  });
})();
var CDK_DRAG_PLACEHOLDER = new InjectionToken("CdkDragPlaceholder");
var CdkDragPlaceholder = class _CdkDragPlaceholder {
  templateRef = inject(TemplateRef);
  _drag = inject(CDK_DRAG_PARENT, {
    optional: true
  });
  data;
  constructor() {
    this._drag?._setPlaceholderTemplate(this);
  }
  ngOnDestroy() {
    this._drag?._resetPlaceholderTemplate(this);
  }
  static ɵfac = function CdkDragPlaceholder_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _CdkDragPlaceholder)();
  };
  static ɵdir = ɵɵdefineDirective({
    type: _CdkDragPlaceholder,
    selectors: [["ng-template", "cdkDragPlaceholder", ""]],
    inputs: {
      data: "data"
    },
    features: [ɵɵProvidersFeature([{
      provide: CDK_DRAG_PLACEHOLDER,
      useExisting: _CdkDragPlaceholder
    }])]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(CdkDragPlaceholder, [{
    type: Directive,
    args: [{
      selector: "ng-template[cdkDragPlaceholder]",
      providers: [{
        provide: CDK_DRAG_PLACEHOLDER,
        useExisting: CdkDragPlaceholder
      }]
    }]
  }], () => [], {
    data: [{
      type: Input
    }]
  });
})();
var DRAG_DROP_DIRECTIVES = [CdkDropList, CdkDropListGroup, CdkDrag, CdkDragHandle, CdkDragPreview, CdkDragPlaceholder];
var DragDropModule = class _DragDropModule {
  static ɵfac = function DragDropModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _DragDropModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _DragDropModule,
    imports: [CdkDropList, CdkDropListGroup, CdkDrag, CdkDragHandle, CdkDragPreview, CdkDragPlaceholder],
    exports: [CdkScrollableModule, CdkDropList, CdkDropListGroup, CdkDrag, CdkDragHandle, CdkDragPreview, CdkDragPlaceholder]
  });
  static ɵinj = ɵɵdefineInjector({
    providers: [DragDrop],
    imports: [CdkScrollableModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(DragDropModule, [{
    type: NgModule,
    args: [{
      imports: DRAG_DROP_DIRECTIVES,
      exports: [CdkScrollableModule, ...DRAG_DROP_DIRECTIVES],
      providers: [DragDrop]
    }]
  }], null, null);
})();

// node_modules/echarts/node_modules/tslib/tslib.es6.js
var extendStatics = function(d, b) {
  extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d2, b2) {
    d2.__proto__ = b2;
  } || function(d2, b2) {
    for (var p in b2) if (Object.prototype.hasOwnProperty.call(b2, p)) d2[p] = b2[p];
  };
  return extendStatics(d, b);
};
function __extends(d, b) {
  if (typeof b !== "function" && b !== null)
    throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
  extendStatics(d, b);
  function __() {
    this.constructor = d;
  }
  d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
}

// node_modules/zrender/lib/core/platform.js
var DEFAULT_FONT_SIZE = 12;
var DEFAULT_FONT_FAMILY = "sans-serif";
var DEFAULT_FONT = DEFAULT_FONT_SIZE + "px " + DEFAULT_FONT_FAMILY;
var OFFSET = 20;
var SCALE = 100;
var defaultWidthMapStr = "007LLmW'55;N0500LLLLLLLLLL00NNNLzWW\\\\WQb\\0FWLg\\bWb\\WQ\\WrWWQ000CL5LLFLL0LL**F*gLLLL5F0LF\\FFF5.5N";
function getTextWidthMap(mapStr) {
  var map4 = {};
  if (typeof JSON === "undefined") {
    return map4;
  }
  for (var i2 = 0; i2 < mapStr.length; i2++) {
    var char = String.fromCharCode(i2 + 32);
    var size = (mapStr.charCodeAt(i2) - OFFSET) / SCALE;
    map4[char] = size;
  }
  return map4;
}
var DEFAULT_TEXT_WIDTH_MAP = getTextWidthMap(defaultWidthMapStr);
var platformApi = {
  createCanvas: function() {
    return typeof document !== "undefined" && document.createElement("canvas");
  },
  measureText: /* @__PURE__ */ (function() {
    var _ctx;
    var _cachedFont;
    return function(text, font) {
      if (!_ctx) {
        var canvas = platformApi.createCanvas();
        _ctx = canvas && canvas.getContext("2d");
      }
      if (_ctx) {
        if (_cachedFont !== font) {
          _cachedFont = _ctx.font = font || DEFAULT_FONT;
        }
        return _ctx.measureText(text);
      } else {
        text = text || "";
        font = font || DEFAULT_FONT;
        var res = /((?:\d+)?\.?\d*)px/.exec(font);
        var fontSize = res && +res[1] || DEFAULT_FONT_SIZE;
        var width = 0;
        if (font.indexOf("mono") >= 0) {
          width = fontSize * text.length;
        } else {
          for (var i2 = 0; i2 < text.length; i2++) {
            var preCalcWidth = DEFAULT_TEXT_WIDTH_MAP[text[i2]];
            width += preCalcWidth == null ? fontSize : preCalcWidth * fontSize;
          }
        }
        return { width };
      }
    };
  })(),
  loadImage: function(src, onload, onerror) {
    var image = new Image();
    image.onload = onload;
    image.onerror = onerror;
    image.src = src;
    return image;
  },
  getTime: function() {
    return Date.now ? Date.now() : +/* @__PURE__ */ new Date();
  }
};

// node_modules/zrender/lib/core/util.js
var BUILTIN_OBJECT = reduce([
  "Function",
  "RegExp",
  "Date",
  "Error",
  "CanvasGradient",
  "CanvasPattern",
  "Image",
  "Canvas"
], function(obj, val) {
  obj["[object " + val + "]"] = true;
  return obj;
}, {});
var TYPED_ARRAY = reduce([
  "Int8",
  "Uint8",
  "Uint8Clamped",
  "Int16",
  "Uint16",
  "Int32",
  "Uint32",
  "Float32",
  "Float64"
], function(obj, val) {
  obj["[object " + val + "Array]"] = true;
  return obj;
}, {});
var objToString = Object.prototype.toString;
var arrayProto = Array.prototype;
var nativeForEach = arrayProto.forEach;
var nativeFilter = arrayProto.filter;
var nativeSlice = arrayProto.slice;
var nativeMap = arrayProto.map;
var ctorFunction = function() {
}.constructor;
var protoFunction = ctorFunction ? ctorFunction.prototype : null;
var protoKey = "__proto__";
var idStart = 2311;
var MAX_SAFE_INTEGER = Math.pow(2, 53) - 1;
function guid() {
  if (idStart >= MAX_SAFE_INTEGER) {
    idStart = 0;
  }
  return idStart++;
}
function logError() {
  var args = [];
  for (var _i = 0; _i < arguments.length; _i++) {
    args[_i] = arguments[_i];
  }
  if (typeof console !== "undefined") {
    console.error.apply(console, args);
  }
}
function clone(source) {
  if (source == null || typeof source !== "object") {
    return source;
  }
  var result = source;
  var typeStr = objToString.call(source);
  if (typeStr === "[object Array]") {
    if (!isPrimitive(source)) {
      result = [];
      for (var i2 = 0, len2 = source.length; i2 < len2; i2++) {
        result[i2] = clone(source[i2]);
      }
    }
  } else if (TYPED_ARRAY[typeStr]) {
    if (!isPrimitive(source)) {
      var Ctor = source.constructor;
      if (Ctor.from) {
        result = Ctor.from(source);
      } else {
        result = new Ctor(source.length);
        for (var i2 = 0, len2 = source.length; i2 < len2; i2++) {
          result[i2] = source[i2];
        }
      }
    }
  } else if (!BUILTIN_OBJECT[typeStr] && !isPrimitive(source) && !isDom(source)) {
    result = {};
    for (var key2 in source) {
      if (source.hasOwnProperty(key2) && key2 !== protoKey) {
        result[key2] = clone(source[key2]);
      }
    }
  }
  return result;
}
function merge2(target, source, overwrite) {
  if (!isObject(source) || !isObject(target)) {
    return overwrite ? clone(source) : target;
  }
  for (var key2 in source) {
    if (source.hasOwnProperty(key2) && key2 !== protoKey) {
      var targetProp = target[key2];
      var sourceProp = source[key2];
      if (isObject(sourceProp) && isObject(targetProp) && !isArray(sourceProp) && !isArray(targetProp) && !isDom(sourceProp) && !isDom(targetProp) && !isBuiltInObject(sourceProp) && !isBuiltInObject(targetProp) && !isPrimitive(sourceProp) && !isPrimitive(targetProp)) {
        merge2(targetProp, sourceProp, overwrite);
      } else if (overwrite || !(key2 in target)) {
        target[key2] = clone(source[key2]);
      }
    }
  }
  return target;
}
function mergeAll(targetAndSources, overwrite) {
  var result = targetAndSources[0];
  for (var i2 = 1, len2 = targetAndSources.length; i2 < len2; i2++) {
    result = merge2(result, targetAndSources[i2], overwrite);
  }
  return result;
}
function extend(target, source) {
  if (Object.assign) {
    Object.assign(target, source);
  } else {
    for (var key2 in source) {
      if (source.hasOwnProperty(key2) && key2 !== protoKey) {
        target[key2] = source[key2];
      }
    }
  }
  return target;
}
function assignProps(tar, src, props) {
  tar = tar || {};
  for (var idx = 0; idx < props.length; idx++) {
    var prop = props[idx];
    tar[prop] = src[prop];
  }
  return tar;
}
function defaults(target, source, overlay) {
  var keysArr = keys(source);
  for (var i2 = 0, len2 = keysArr.length; i2 < len2; i2++) {
    var key2 = keysArr[i2];
    if (overlay ? source[key2] != null : target[key2] == null) {
      target[key2] = source[key2];
    }
  }
  return target;
}
var createCanvas = platformApi.createCanvas;
function indexOf(array, value) {
  if (array) {
    if (array.indexOf) {
      return array.indexOf(value);
    }
    for (var i2 = 0, len2 = array.length; i2 < len2; i2++) {
      if (array[i2] === value) {
        return i2;
      }
    }
  }
  return -1;
}
function inherits(clazz, baseClazz) {
  var clazzPrototype = clazz.prototype;
  function F13() {
  }
  F13.prototype = baseClazz.prototype;
  clazz.prototype = new F13();
  for (var prop in clazzPrototype) {
    if (clazzPrototype.hasOwnProperty(prop)) {
      clazz.prototype[prop] = clazzPrototype[prop];
    }
  }
  clazz.prototype.constructor = clazz;
  clazz.superClass = baseClazz;
}
function mixin(target, source, override) {
  target = "prototype" in target ? target.prototype : target;
  source = "prototype" in source ? source.prototype : source;
  if (Object.getOwnPropertyNames) {
    var keyList = Object.getOwnPropertyNames(source);
    for (var i2 = 0; i2 < keyList.length; i2++) {
      var key2 = keyList[i2];
      if (key2 !== "constructor") {
        if (override ? source[key2] != null : target[key2] == null) {
          target[key2] = source[key2];
        }
      }
    }
  } else {
    defaults(target, source, override);
  }
}
function isArrayLike(data) {
  if (!data) {
    return false;
  }
  if (typeof data === "string") {
    return false;
  }
  return typeof data.length === "number";
}
function each(arr, cb, context) {
  if (!(arr && cb)) {
    return;
  }
  if (arr.forEach && arr.forEach === nativeForEach) {
    arr.forEach(cb, context);
  } else if (arr.length === +arr.length) {
    for (var i2 = 0, len2 = arr.length; i2 < len2; i2++) {
      cb.call(context, arr[i2], i2, arr);
    }
  } else {
    for (var key2 in arr) {
      if (arr.hasOwnProperty(key2)) {
        cb.call(context, arr[key2], key2, arr);
      }
    }
  }
}
function map2(arr, cb, context) {
  if (!arr) {
    return [];
  }
  if (!cb) {
    return slice(arr);
  }
  if (arr.map && arr.map === nativeMap) {
    return arr.map(cb, context);
  } else {
    var result = [];
    for (var i2 = 0, len2 = arr.length; i2 < len2; i2++) {
      result.push(cb.call(context, arr[i2], i2, arr));
    }
    return result;
  }
}
function reduce(arr, cb, memo, context) {
  if (!(arr && cb)) {
    return;
  }
  for (var i2 = 0, len2 = arr.length; i2 < len2; i2++) {
    memo = cb.call(context, memo, arr[i2], i2, arr);
  }
  return memo;
}
function filter2(arr, cb, context) {
  if (!arr) {
    return [];
  }
  if (!cb) {
    return slice(arr);
  }
  if (arr.filter && arr.filter === nativeFilter) {
    return arr.filter(cb, context);
  } else {
    var result = [];
    for (var i2 = 0, len2 = arr.length; i2 < len2; i2++) {
      if (cb.call(context, arr[i2], i2, arr)) {
        result.push(arr[i2]);
      }
    }
    return result;
  }
}
function find(arr, cb, context) {
  if (!(arr && cb)) {
    return;
  }
  for (var i2 = 0, len2 = arr.length; i2 < len2; i2++) {
    if (cb.call(context, arr[i2], i2, arr)) {
      return arr[i2];
    }
  }
}
function keys(obj) {
  if (!obj) {
    return [];
  }
  if (Object.keys) {
    return Object.keys(obj);
  }
  var keyList = [];
  for (var key2 in obj) {
    if (obj.hasOwnProperty(key2)) {
      keyList.push(key2);
    }
  }
  return keyList;
}
function bindPolyfill(func, context) {
  var args = [];
  for (var _i = 2; _i < arguments.length; _i++) {
    args[_i - 2] = arguments[_i];
  }
  return function() {
    return func.apply(context, args.concat(nativeSlice.call(arguments)));
  };
}
var bind = protoFunction && isFunction(protoFunction.bind) ? protoFunction.call.bind(protoFunction.bind) : bindPolyfill;
function curry(func) {
  var args = [];
  for (var _i = 1; _i < arguments.length; _i++) {
    args[_i - 1] = arguments[_i];
  }
  return function() {
    return func.apply(this, args.concat(nativeSlice.call(arguments)));
  };
}
function isArray(value) {
  if (Array.isArray) {
    return Array.isArray(value);
  }
  return objToString.call(value) === "[object Array]";
}
function isFunction(value) {
  return typeof value === "function";
}
function isString(value) {
  return typeof value === "string";
}
function isStringSafe(value) {
  return objToString.call(value) === "[object String]";
}
function isNumber(value) {
  return typeof value === "number";
}
function isObject(value) {
  var type = typeof value;
  return type === "function" || !!value && type === "object";
}
function isBuiltInObject(value) {
  return !!BUILTIN_OBJECT[objToString.call(value)];
}
function isTypedArray(value) {
  return !!TYPED_ARRAY[objToString.call(value)];
}
function isDom(value) {
  return typeof value === "object" && typeof value.nodeType === "number" && typeof value.ownerDocument === "object";
}
function isGradientObject(value) {
  return value.colorStops != null;
}
function isImagePatternObject(value) {
  return value.image != null;
}
function isRegExp(value) {
  return objToString.call(value) === "[object RegExp]";
}
function eqNaN(value) {
  return value !== value;
}
function retrieve() {
  var args = [];
  for (var _i = 0; _i < arguments.length; _i++) {
    args[_i] = arguments[_i];
  }
  for (var i2 = 0, len2 = args.length; i2 < len2; i2++) {
    if (args[i2] != null) {
      return args[i2];
    }
  }
}
function retrieve2(value0, value1) {
  return value0 != null ? value0 : value1;
}
function retrieve3(value0, value1, value2) {
  return value0 != null ? value0 : value1 != null ? value1 : value2;
}
function slice(arr) {
  var args = [];
  for (var _i = 1; _i < arguments.length; _i++) {
    args[_i - 1] = arguments[_i];
  }
  return nativeSlice.apply(arr, args);
}
function normalizeCssArray(val) {
  if (typeof val === "number") {
    return [val, val, val, val];
  }
  var len2 = val.length;
  if (len2 === 2) {
    return [val[0], val[1], val[0], val[1]];
  } else if (len2 === 3) {
    return [val[0], val[1], val[2], val[1]];
  }
  return val;
}
function assert(condition, message) {
  if (!condition) {
    throw new Error(message);
  }
}
function trim(str) {
  if (str == null) {
    return null;
  } else if (typeof str.trim === "function") {
    return str.trim();
  } else {
    return str.replace(/^[\s\uFEFF\xA0]+|[\s\uFEFF\xA0]+$/g, "");
  }
}
var primitiveKey = "__ec_primitive__";
function setAsPrimitive(obj) {
  obj[primitiveKey] = true;
}
function isPrimitive(obj) {
  return obj[primitiveKey];
}
var MapPolyfill = (function() {
  function MapPolyfill2() {
    this.data = {};
  }
  MapPolyfill2.prototype["delete"] = function(key2) {
    var existed = this.has(key2);
    if (existed) {
      delete this.data[key2];
    }
    return existed;
  };
  MapPolyfill2.prototype.has = function(key2) {
    return this.data.hasOwnProperty(key2);
  };
  MapPolyfill2.prototype.get = function(key2) {
    return this.data[key2];
  };
  MapPolyfill2.prototype.set = function(key2, value) {
    this.data[key2] = value;
    return this;
  };
  MapPolyfill2.prototype.keys = function() {
    return keys(this.data);
  };
  MapPolyfill2.prototype.forEach = function(callback) {
    var data = this.data;
    for (var key2 in data) {
      if (data.hasOwnProperty(key2)) {
        callback(data[key2], key2);
      }
    }
  };
  return MapPolyfill2;
})();
var isNativeMapSupported = typeof Map === "function";
function maybeNativeMap() {
  return isNativeMapSupported ? /* @__PURE__ */ new Map() : new MapPolyfill();
}
var HashMap = (function() {
  function HashMap2(obj) {
    var isArr = isArray(obj);
    this.data = maybeNativeMap();
    var thisMap = this;
    obj instanceof HashMap2 ? obj.each(visit) : obj && each(obj, visit);
    function visit(value, key2) {
      isArr ? thisMap.set(value, key2) : thisMap.set(key2, value);
    }
  }
  HashMap2.prototype.hasKey = function(key2) {
    return this.data.has(key2);
  };
  HashMap2.prototype.get = function(key2) {
    return this.data.get(key2);
  };
  HashMap2.prototype.set = function(key2, value) {
    this.data.set(key2, value);
    return value;
  };
  HashMap2.prototype.each = function(cb, context) {
    this.data.forEach(function(value, key2) {
      cb.call(context, value, key2);
    });
  };
  HashMap2.prototype.keys = function() {
    var keys2 = this.data.keys();
    return isNativeMapSupported ? Array.from(keys2) : keys2;
  };
  HashMap2.prototype.removeKey = function(key2) {
    this.data["delete"](key2);
  };
  return HashMap2;
})();
function createHashMap(obj) {
  return new HashMap(obj);
}
function concatArray(a, b) {
  var newArray = new a.constructor(a.length + b.length);
  for (var i2 = 0; i2 < a.length; i2++) {
    newArray[i2] = a[i2];
  }
  var offset = a.length;
  for (var i2 = 0; i2 < b.length; i2++) {
    newArray[i2 + offset] = b[i2];
  }
  return newArray;
}
function createObject(proto, properties) {
  var obj;
  if (Object.create) {
    obj = Object.create(proto);
  } else {
    var StyleCtor = function() {
    };
    StyleCtor.prototype = proto;
    obj = new StyleCtor();
  }
  if (properties) {
    extend(obj, properties);
  }
  return obj;
}
function disableUserSelect(dom) {
  var domStyle = dom.style;
  domStyle.webkitUserSelect = "none";
  domStyle.userSelect = "none";
  domStyle.webkitTapHighlightColor = "rgba(0,0,0,0)";
  domStyle["-webkit-touch-callout"] = "none";
}
function hasOwn(own, prop) {
  return own.hasOwnProperty(prop);
}
function noop() {
}
var RADIAN_TO_DEGREE = 180 / Math.PI;
var EPSILON = Number.EPSILON || Math.pow(2, -52);

// node_modules/zrender/lib/core/env.js
var Browser = /* @__PURE__ */ (function() {
  function Browser2() {
    this.firefox = false;
    this.ie = false;
    this.edge = false;
    this.newEdge = false;
    this.weChat = false;
  }
  return Browser2;
})();
var Env = /* @__PURE__ */ (function() {
  function Env2() {
    this.browser = new Browser();
    this.node = false;
    this.wxa = false;
    this.worker = false;
    this.svgSupported = false;
    this.touchEventsSupported = false;
    this.pointerEventsSupported = false;
    this.domSupported = false;
    this.transformSupported = false;
    this.transform3dSupported = false;
    this.hasGlobalWindow = typeof window !== "undefined";
  }
  return Env2;
})();
var env = new Env();
if (typeof wx === "object" && typeof wx.getSystemInfoSync === "function") {
  env.wxa = true;
  env.touchEventsSupported = true;
} else if (typeof document === "undefined" && typeof self !== "undefined") {
  env.worker = true;
} else if (!env.hasGlobalWindow || "Deno" in window || typeof navigator !== "undefined" && typeof navigator.userAgent === "string" && navigator.userAgent.indexOf("Node.js") > -1) {
  env.node = true;
  env.svgSupported = true;
} else {
  detect(navigator.userAgent, env);
}
function detect(ua, env2) {
  var browser = env2.browser;
  var firefox = ua.match(/Firefox\/([\d.]+)/);
  var ie = ua.match(/MSIE\s([\d.]+)/) || ua.match(/Trident\/.+?rv:(([\d.]+))/);
  var edge = ua.match(/Edge?\/([\d.]+)/);
  var weChat = /micromessenger/i.test(ua);
  if (firefox) {
    browser.firefox = true;
    browser.version = firefox[1];
  }
  if (ie) {
    browser.ie = true;
    browser.version = ie[1];
  }
  if (edge) {
    browser.edge = true;
    browser.version = edge[1];
    browser.newEdge = +edge[1].split(".")[0] > 18;
  }
  if (weChat) {
    browser.weChat = true;
  }
  env2.svgSupported = typeof SVGRect !== "undefined";
  env2.touchEventsSupported = "ontouchstart" in window && !browser.ie && !browser.edge;
  env2.pointerEventsSupported = "onpointerdown" in window && (browser.edge || browser.ie && +browser.version >= 11);
  var domSupported = env2.domSupported = typeof document !== "undefined";
  if (domSupported) {
    var style2 = document.documentElement.style;
    env2.transform3dSupported = (browser.ie && "transition" in style2 || browser.edge || "WebKitCSSMatrix" in window && "m11" in new WebKitCSSMatrix() || "MozPerspective" in style2) && !("OTransition" in style2);
    env2.transformSupported = env2.transform3dSupported || browser.ie && +browser.version >= 9;
  }
}
var env_default = env;

// node_modules/echarts/lib/util/clazz.js
var TYPE_DELIMITER = ".";
var IS_CONTAINER = "___EC__COMPONENT__CONTAINER___";
var IS_EXTENDED_CLASS = "___EC__EXTENDED_CLASS___";
function parseClassType(componentType) {
  var ret = {
    main: "",
    sub: ""
  };
  if (componentType) {
    var typeArr = componentType.split(TYPE_DELIMITER);
    ret.main = typeArr[0] || "";
    ret.sub = typeArr[1] || "";
  }
  return ret;
}
function checkClassType(componentType) {
  assert(/^[a-zA-Z0-9_]+([.][a-zA-Z0-9_]+)?$/.test(componentType), 'componentType "' + componentType + '" illegal');
}
function isExtendedClass(clz) {
  return !!(clz && clz[IS_EXTENDED_CLASS]);
}
function enableClassExtend(rootClz, mandatoryMethods) {
  rootClz.$constructor = rootClz;
  rootClz.extend = function(proto) {
    if (true) {
      each(mandatoryMethods, function(method) {
        if (!proto[method]) {
          console.warn("Method `" + method + "` should be implemented" + (proto.type ? " in " + proto.type : "") + ".");
        }
      });
    }
    var superClass = this;
    var ExtendedClass;
    if (isESClass(superClass)) {
      ExtendedClass = /** @class */
      (function(_super) {
        __extends(class_1, _super);
        function class_1() {
          return _super.apply(this, arguments) || this;
        }
        return class_1;
      })(superClass);
    } else {
      ExtendedClass = function() {
        (proto.$constructor || superClass).apply(this, arguments);
      };
      inherits(ExtendedClass, this);
    }
    extend(ExtendedClass.prototype, proto);
    ExtendedClass[IS_EXTENDED_CLASS] = true;
    ExtendedClass.extend = this.extend;
    ExtendedClass.superCall = superCall;
    ExtendedClass.superApply = superApply;
    ExtendedClass.superClass = superClass;
    return ExtendedClass;
  };
}
function isESClass(fn) {
  return isFunction(fn) && /^class\s/.test(Function.prototype.toString.call(fn));
}
function mountExtend(SubClz, SupperClz) {
  SubClz.extend = SupperClz.extend;
}
var classBase = Math.round(Math.random() * 10);
function enableClassCheck(target) {
  var classAttr = ["__\0is_clz", classBase++].join("_");
  target.prototype[classAttr] = true;
  if (true) {
    assert(!target.isInstance, 'The method "is" can not be defined.');
  }
  target.isInstance = function(obj) {
    return !!(obj && obj[classAttr]);
  };
}
function superCall(context, methodName) {
  var args = [];
  for (var _i = 2; _i < arguments.length; _i++) {
    args[_i - 2] = arguments[_i];
  }
  return this.superClass.prototype[methodName].apply(context, args);
}
function superApply(context, methodName, args) {
  return this.superClass.prototype[methodName].apply(context, args);
}
function enableClassManagement(target) {
  var storage2 = {};
  target.registerClass = function(clz) {
    var componentFullType = clz.type || clz.prototype.type;
    if (componentFullType) {
      checkClassType(componentFullType);
      clz.prototype.type = componentFullType;
      var componentTypeInfo = parseClassType(componentFullType);
      if (!componentTypeInfo.sub) {
        if (true) {
          if (storage2[componentTypeInfo.main]) {
            console.warn(componentTypeInfo.main + " exists.");
          }
        }
        storage2[componentTypeInfo.main] = clz;
      } else if (componentTypeInfo.sub !== IS_CONTAINER) {
        var container = makeContainer(componentTypeInfo);
        container[componentTypeInfo.sub] = clz;
      }
    }
    return clz;
  };
  target.getClass = function(mainType, subType, throwWhenNotFound) {
    var clz = storage2[mainType];
    if (clz && clz[IS_CONTAINER]) {
      clz = subType ? clz[subType] : null;
    }
    if (throwWhenNotFound && !clz) {
      throw new Error(!subType ? mainType + ".type should be specified." : "Component " + mainType + "." + (subType || "") + " is used but not imported.");
    }
    return clz;
  };
  target.getClassesByMainType = function(componentType) {
    var componentTypeInfo = parseClassType(componentType);
    var result = [];
    var obj = storage2[componentTypeInfo.main];
    if (obj && obj[IS_CONTAINER]) {
      each(obj, function(o, type) {
        type !== IS_CONTAINER && result.push(o);
      });
    } else {
      result.push(obj);
    }
    return result;
  };
  target.hasClass = function(componentType) {
    var componentTypeInfo = parseClassType(componentType);
    return !!storage2[componentTypeInfo.main];
  };
  target.getAllClassMainTypes = function() {
    var types = [];
    each(storage2, function(obj, type) {
      types.push(type);
    });
    return types;
  };
  target.hasSubTypes = function(componentType) {
    var componentTypeInfo = parseClassType(componentType);
    var obj = storage2[componentTypeInfo.main];
    return obj && obj[IS_CONTAINER];
  };
  function makeContainer(componentTypeInfo) {
    var container = storage2[componentTypeInfo.main];
    if (!container || !container[IS_CONTAINER]) {
      container = storage2[componentTypeInfo.main] = {};
      container[IS_CONTAINER] = true;
    }
    return container;
  }
}

// node_modules/echarts/lib/model/mixin/makeStyleMapper.js
function makeStyleMapper(properties, ignoreParent) {
  for (var i2 = 0; i2 < properties.length; i2++) {
    if (!properties[i2][1]) {
      properties[i2][1] = properties[i2][0];
    }
  }
  ignoreParent = ignoreParent || false;
  return function(model2, excludes, includes) {
    var style2 = {};
    for (var i3 = 0; i3 < properties.length; i3++) {
      var propName = properties[i3][1];
      if (excludes && indexOf(excludes, propName) >= 0 || includes && indexOf(includes, propName) < 0) {
        continue;
      }
      var val = model2.getShallow(propName, ignoreParent);
      if (val != null) {
        style2[properties[i3][0]] = val;
      }
    }
    return style2;
  };
}

// node_modules/echarts/lib/model/mixin/areaStyle.js
var AREA_STYLE_KEY_MAP = [
  ["fill", "color"],
  ["shadowBlur"],
  ["shadowOffsetX"],
  ["shadowOffsetY"],
  ["opacity"],
  ["shadowColor"]
  // Option decal is in `DecalObject` but style.decal is in `PatternObject`.
  // So do not transfer decal directly.
];
var getAreaStyle = makeStyleMapper(AREA_STYLE_KEY_MAP);
var AreaStyleMixin = (
  /** @class */
  (function() {
    function AreaStyleMixin2() {
    }
    AreaStyleMixin2.prototype.getAreaStyle = function(excludes, includes) {
      return getAreaStyle(this, excludes, includes);
    };
    return AreaStyleMixin2;
  })()
);

// node_modules/zrender/node_modules/tslib/tslib.es6.js
var extendStatics2 = function(d, b) {
  extendStatics2 = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d2, b2) {
    d2.__proto__ = b2;
  } || function(d2, b2) {
    for (var p in b2) if (Object.prototype.hasOwnProperty.call(b2, p)) d2[p] = b2[p];
  };
  return extendStatics2(d, b);
};
function __extends2(d, b) {
  if (typeof b !== "function" && b !== null)
    throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
  extendStatics2(d, b);
  function __() {
    this.constructor = d;
  }
  d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
}

// node_modules/zrender/lib/core/LRU.js
var Entry = /* @__PURE__ */ (function() {
  function Entry2(val) {
    this.value = val;
  }
  return Entry2;
})();
var LinkedList = (function() {
  function LinkedList2() {
    this._len = 0;
  }
  LinkedList2.prototype.insert = function(val) {
    var entry = new Entry(val);
    this.insertEntry(entry);
    return entry;
  };
  LinkedList2.prototype.insertEntry = function(entry) {
    if (!this.head) {
      this.head = this.tail = entry;
    } else {
      this.tail.next = entry;
      entry.prev = this.tail;
      entry.next = null;
      this.tail = entry;
    }
    this._len++;
  };
  LinkedList2.prototype.remove = function(entry) {
    var prev = entry.prev;
    var next = entry.next;
    if (prev) {
      prev.next = next;
    } else {
      this.head = next;
    }
    if (next) {
      next.prev = prev;
    } else {
      this.tail = prev;
    }
    entry.next = entry.prev = null;
    this._len--;
  };
  LinkedList2.prototype.len = function() {
    return this._len;
  };
  LinkedList2.prototype.clear = function() {
    this.head = this.tail = null;
    this._len = 0;
  };
  return LinkedList2;
})();
var LRU = (function() {
  function LRU2(maxSize) {
    this._list = new LinkedList();
    this._maxSize = 10;
    this._map = {};
    this._maxSize = maxSize;
  }
  LRU2.prototype.put = function(key2, value) {
    var list = this._list;
    var map4 = this._map;
    var removed = null;
    if (map4[key2] == null) {
      var len2 = list.len();
      var entry = this._lastRemovedEntry;
      if (len2 >= this._maxSize && len2 > 0) {
        var leastUsedEntry = list.head;
        list.remove(leastUsedEntry);
        delete map4[leastUsedEntry.key];
        removed = leastUsedEntry.value;
        this._lastRemovedEntry = leastUsedEntry;
      }
      if (entry) {
        entry.value = value;
      } else {
        entry = new Entry(value);
      }
      entry.key = key2;
      list.insertEntry(entry);
      map4[key2] = entry;
    }
    return removed;
  };
  LRU2.prototype.get = function(key2) {
    var entry = this._map[key2];
    var list = this._list;
    if (entry != null) {
      if (entry !== list.tail) {
        list.remove(entry);
        list.insertEntry(entry);
      }
      return entry.value;
    }
  };
  LRU2.prototype.clear = function() {
    this._list.clear();
    this._map = {};
  };
  LRU2.prototype.len = function() {
    return this._list.len();
  };
  return LRU2;
})();
var LRU_default = LRU;

// node_modules/zrender/lib/graphic/helper/image.js
var globalImageCache = new LRU_default(50);
function findExistImage(newImageOrSrc) {
  if (typeof newImageOrSrc === "string") {
    var cachedImgObj = globalImageCache.get(newImageOrSrc);
    return cachedImgObj && cachedImgObj.image;
  } else {
    return newImageOrSrc;
  }
}
function createOrUpdateImage(newImageOrSrc, image, hostEl, onload, cbPayload) {
  if (!newImageOrSrc) {
    return image;
  } else if (typeof newImageOrSrc === "string") {
    if (image && image.__zrImageSrc === newImageOrSrc || !hostEl) {
      return image;
    }
    var cachedImgObj = globalImageCache.get(newImageOrSrc);
    var pendingWrap = { hostEl, cb: onload, cbPayload };
    if (cachedImgObj) {
      image = cachedImgObj.image;
      !isImageReady(image) && cachedImgObj.pending.push(pendingWrap);
    } else {
      image = platformApi.loadImage(newImageOrSrc, imageOnLoad, imageOnLoad);
      image.__zrImageSrc = newImageOrSrc;
      globalImageCache.put(newImageOrSrc, image.__cachedImgObj = {
        image,
        pending: [pendingWrap]
      });
    }
    return image;
  } else {
    return newImageOrSrc;
  }
}
function imageOnLoad() {
  var cachedImgObj = this.__cachedImgObj;
  this.onload = this.onerror = this.__cachedImgObj = null;
  for (var i2 = 0; i2 < cachedImgObj.pending.length; i2++) {
    var pendingWrap = cachedImgObj.pending[i2];
    var cb = pendingWrap.cb;
    cb && cb(this, pendingWrap.cbPayload);
    pendingWrap.hostEl.dirty();
  }
  cachedImgObj.pending.length = 0;
}
function isImageReady(image) {
  return image && image.width && image.height;
}

// node_modules/zrender/lib/core/matrix.js
function create() {
  return [1, 0, 0, 1, 0, 0];
}
function identity(out2) {
  out2[0] = 1;
  out2[1] = 0;
  out2[2] = 0;
  out2[3] = 1;
  out2[4] = 0;
  out2[5] = 0;
  return out2;
}
function copy(out2, m2) {
  out2[0] = m2[0];
  out2[1] = m2[1];
  out2[2] = m2[2];
  out2[3] = m2[3];
  out2[4] = m2[4];
  out2[5] = m2[5];
  return out2;
}
function mul(out2, m1, m2) {
  var out0 = m1[0] * m2[0] + m1[2] * m2[1];
  var out1 = m1[1] * m2[0] + m1[3] * m2[1];
  var out22 = m1[0] * m2[2] + m1[2] * m2[3];
  var out3 = m1[1] * m2[2] + m1[3] * m2[3];
  var out4 = m1[0] * m2[4] + m1[2] * m2[5] + m1[4];
  var out5 = m1[1] * m2[4] + m1[3] * m2[5] + m1[5];
  out2[0] = out0;
  out2[1] = out1;
  out2[2] = out22;
  out2[3] = out3;
  out2[4] = out4;
  out2[5] = out5;
  return out2;
}
function translate(out2, a, v) {
  out2[0] = a[0];
  out2[1] = a[1];
  out2[2] = a[2];
  out2[3] = a[3];
  out2[4] = a[4] + v[0];
  out2[5] = a[5] + v[1];
  return out2;
}
function rotate(out2, a, rad, pivot) {
  if (pivot === void 0) {
    pivot = [0, 0];
  }
  var aa = a[0];
  var ac = a[2];
  var atx = a[4];
  var ab = a[1];
  var ad = a[3];
  var aty = a[5];
  var st = Math.sin(rad);
  var ct = Math.cos(rad);
  out2[0] = aa * ct + ab * st;
  out2[1] = -aa * st + ab * ct;
  out2[2] = ac * ct + ad * st;
  out2[3] = -ac * st + ct * ad;
  out2[4] = ct * (atx - pivot[0]) + st * (aty - pivot[1]) + pivot[0];
  out2[5] = ct * (aty - pivot[1]) - st * (atx - pivot[0]) + pivot[1];
  return out2;
}
function scale(out2, a, v) {
  var vx = v[0];
  var vy = v[1];
  out2[0] = a[0] * vx;
  out2[1] = a[1] * vy;
  out2[2] = a[2] * vx;
  out2[3] = a[3] * vy;
  out2[4] = a[4] * vx;
  out2[5] = a[5] * vy;
  return out2;
}
function invert(out2, a) {
  var aa = a[0];
  var ac = a[2];
  var atx = a[4];
  var ab = a[1];
  var ad = a[3];
  var aty = a[5];
  var det = aa * ad - ab * ac;
  if (!det) {
    return null;
  }
  det = 1 / det;
  out2[0] = ad * det;
  out2[1] = -ab * det;
  out2[2] = -ac * det;
  out2[3] = aa * det;
  out2[4] = (ac * aty - ad * atx) * det;
  out2[5] = (ab * atx - aa * aty) * det;
  return out2;
}
function clone2(a) {
  var b = create();
  copy(b, a);
  return b;
}

// node_modules/zrender/lib/core/vector.js
function create2(x, y) {
  if (x == null) {
    x = 0;
  }
  if (y == null) {
    y = 0;
  }
  return [x, y];
}
function copy2(out2, v) {
  out2[0] = v[0];
  out2[1] = v[1];
  return out2;
}
function clone3(v) {
  return [v[0], v[1]];
}
function set(out2, a, b) {
  out2[0] = a;
  out2[1] = b;
  return out2;
}
function add(out2, v12, v22) {
  out2[0] = v12[0] + v22[0];
  out2[1] = v12[1] + v22[1];
  return out2;
}
function scaleAndAdd(out2, v12, v22, a) {
  out2[0] = v12[0] + v22[0] * a;
  out2[1] = v12[1] + v22[1] * a;
  return out2;
}
function sub(out2, v12, v22) {
  out2[0] = v12[0] - v22[0];
  out2[1] = v12[1] - v22[1];
  return out2;
}
function len(v) {
  return Math.sqrt(lenSquare(v));
}
function lenSquare(v) {
  return v[0] * v[0] + v[1] * v[1];
}
function scale2(out2, v, s) {
  out2[0] = v[0] * s;
  out2[1] = v[1] * s;
  return out2;
}
function normalize(out2, v) {
  var d = len(v);
  if (d === 0) {
    out2[0] = 0;
    out2[1] = 0;
  } else {
    out2[0] = v[0] / d;
    out2[1] = v[1] / d;
  }
  return out2;
}
function distance(v12, v22) {
  return Math.sqrt((v12[0] - v22[0]) * (v12[0] - v22[0]) + (v12[1] - v22[1]) * (v12[1] - v22[1]));
}
var dist = distance;
function distanceSquare(v12, v22) {
  return (v12[0] - v22[0]) * (v12[0] - v22[0]) + (v12[1] - v22[1]) * (v12[1] - v22[1]);
}
var distSquare = distanceSquare;
function lerp(out2, v12, v22, t) {
  out2[0] = v12[0] + t * (v22[0] - v12[0]);
  out2[1] = v12[1] + t * (v22[1] - v12[1]);
  return out2;
}
function applyTransform(out2, v, m2) {
  var x = v[0];
  var y = v[1];
  out2[0] = m2[0] * x + m2[2] * y + m2[4];
  out2[1] = m2[1] * x + m2[3] * y + m2[5];
  return out2;
}
function min(out2, v12, v22) {
  out2[0] = Math.min(v12[0], v22[0]);
  out2[1] = Math.min(v12[1], v22[1]);
  return out2;
}
function max(out2, v12, v22) {
  out2[0] = Math.max(v12[0], v22[0]);
  out2[1] = Math.max(v12[1], v22[1]);
  return out2;
}

// node_modules/zrender/lib/core/Point.js
var Point = (function() {
  function Point2(x, y) {
    this.x = x || 0;
    this.y = y || 0;
  }
  Point2.prototype.copy = function(other) {
    this.x = other.x;
    this.y = other.y;
    return this;
  };
  Point2.prototype.clone = function() {
    return new Point2(this.x, this.y);
  };
  Point2.prototype.set = function(x, y) {
    this.x = x;
    this.y = y;
    return this;
  };
  Point2.prototype.equal = function(other) {
    return other.x === this.x && other.y === this.y;
  };
  Point2.prototype.add = function(other) {
    this.x += other.x;
    this.y += other.y;
    return this;
  };
  Point2.prototype.scale = function(scalar) {
    this.x *= scalar;
    this.y *= scalar;
  };
  Point2.prototype.scaleAndAdd = function(other, scalar) {
    this.x += other.x * scalar;
    this.y += other.y * scalar;
  };
  Point2.prototype.sub = function(other) {
    this.x -= other.x;
    this.y -= other.y;
    return this;
  };
  Point2.prototype.dot = function(other) {
    return this.x * other.x + this.y * other.y;
  };
  Point2.prototype.len = function() {
    return Math.sqrt(this.x * this.x + this.y * this.y);
  };
  Point2.prototype.lenSquare = function() {
    return this.x * this.x + this.y * this.y;
  };
  Point2.prototype.normalize = function() {
    var len2 = this.len();
    this.x /= len2;
    this.y /= len2;
    return this;
  };
  Point2.prototype.distance = function(other) {
    var dx = this.x - other.x;
    var dy = this.y - other.y;
    return Math.sqrt(dx * dx + dy * dy);
  };
  Point2.prototype.distanceSquare = function(other) {
    var dx = this.x - other.x;
    var dy = this.y - other.y;
    return dx * dx + dy * dy;
  };
  Point2.prototype.negate = function() {
    this.x = -this.x;
    this.y = -this.y;
    return this;
  };
  Point2.prototype.transform = function(m2) {
    if (!m2) {
      return;
    }
    var x = this.x;
    var y = this.y;
    this.x = m2[0] * x + m2[2] * y + m2[4];
    this.y = m2[1] * x + m2[3] * y + m2[5];
    return this;
  };
  Point2.prototype.toArray = function(out2) {
    out2[0] = this.x;
    out2[1] = this.y;
    return out2;
  };
  Point2.prototype.fromArray = function(input2) {
    this.x = input2[0];
    this.y = input2[1];
  };
  Point2.set = function(p, x, y) {
    p.x = x;
    p.y = y;
  };
  Point2.copy = function(p, p2) {
    p.x = p2.x;
    p.y = p2.y;
  };
  Point2.len = function(p) {
    return Math.sqrt(p.x * p.x + p.y * p.y);
  };
  Point2.lenSquare = function(p) {
    return p.x * p.x + p.y * p.y;
  };
  Point2.dot = function(p0, p1) {
    return p0.x * p1.x + p0.y * p1.y;
  };
  Point2.add = function(out2, p0, p1) {
    out2.x = p0.x + p1.x;
    out2.y = p0.y + p1.y;
  };
  Point2.sub = function(out2, p0, p1) {
    out2.x = p0.x - p1.x;
    out2.y = p0.y - p1.y;
  };
  Point2.scale = function(out2, p0, scalar) {
    out2.x = p0.x * scalar;
    out2.y = p0.y * scalar;
  };
  Point2.scaleAndAdd = function(out2, p0, p1, scalar) {
    out2.x = p0.x + p1.x * scalar;
    out2.y = p0.y + p1.y * scalar;
  };
  Point2.lerp = function(out2, p0, p1, t) {
    var onet = 1 - t;
    out2.x = onet * p0.x + t * p1.x;
    out2.y = onet * p0.y + t * p1.y;
  };
  return Point2;
})();
var Point_default = Point;

// node_modules/zrender/lib/core/BoundingRect.js
var mathMin = Math.min;
var mathMax = Math.max;
var mathAbs = Math.abs;
var XY = ["x", "y"];
var WH = ["width", "height"];
var lt = new Point_default();
var rb = new Point_default();
var lb = new Point_default();
var rt = new Point_default();
var _intersectCtx = createIntersectContext();
var _minTv = _intersectCtx.minTv;
var _maxTv = _intersectCtx.maxTv;
var _lenMinMax = [0, 0];
var BoundingRect = (function() {
  function BoundingRect2(x, y, width, height) {
    boundingRectSet(this, x, y, width, height);
  }
  BoundingRect2.set = function(target, x, y, width, height) {
    if (width < 0) {
      x = x + width;
      width = -width;
    }
    if (height < 0) {
      y = y + height;
      height = -height;
    }
    target.x = x;
    target.y = y;
    target.width = width;
    target.height = height;
    return target;
  };
  BoundingRect2.prototype.union = function(other) {
    var x = mathMin(other.x, this.x);
    var y = mathMin(other.y, this.y);
    if (isFinite(this.x) && isFinite(this.width)) {
      this.width = mathMax(other.x + other.width, this.x + this.width) - x;
    } else {
      this.width = other.width;
    }
    if (isFinite(this.y) && isFinite(this.height)) {
      this.height = mathMax(other.y + other.height, this.y + this.height) - y;
    } else {
      this.height = other.height;
    }
    this.x = x;
    this.y = y;
  };
  BoundingRect2.prototype.applyTransform = function(m2) {
    BoundingRect2.applyTransform(this, this, m2);
  };
  BoundingRect2.prototype.calculateTransform = function(b) {
    return boundingRectCalculateTransform(create(), this, b);
  };
  BoundingRect2.prototype.intersect = function(b, mtv, opt) {
    return BoundingRect2.intersect(this, b, mtv, opt);
  };
  BoundingRect2.intersect = function(a, b, mtv, opt) {
    if (mtv) {
      Point_default.set(mtv, 0, 0);
    }
    var outIntersectRect = opt && opt.outIntersectRect || null;
    var clamp2 = opt && opt.clamp;
    if (outIntersectRect) {
      outIntersectRect.x = outIntersectRect.y = outIntersectRect.width = outIntersectRect.height = NaN;
    }
    if (!a || !b) {
      return false;
    }
    if (!(a instanceof BoundingRect2)) {
      a = boundingRectSet(_tmpIntersectA, a.x, a.y, a.width, a.height);
    }
    if (!(b instanceof BoundingRect2)) {
      b = boundingRectSet(_tmpIntersectB, b.x, b.y, b.width, b.height);
    }
    var useMTV = !!mtv;
    _intersectCtx.reset(opt, useMTV);
    var touchThreshold = _intersectCtx.touchThreshold;
    var ax0 = a.x + touchThreshold;
    var ax1 = a.x + a.width - touchThreshold;
    var ay0 = a.y + touchThreshold;
    var ay1 = a.y + a.height - touchThreshold;
    var bx0 = b.x + touchThreshold;
    var bx1 = b.x + b.width - touchThreshold;
    var by0 = b.y + touchThreshold;
    var by1 = b.y + b.height - touchThreshold;
    if (ax0 > ax1 || ay0 > ay1 || bx0 > bx1 || by0 > by1) {
      return false;
    }
    var overlap = !(ax1 < bx0 || bx1 < ax0 || ay1 < by0 || by1 < ay0);
    if (useMTV || outIntersectRect) {
      _lenMinMax[0] = Infinity;
      _lenMinMax[1] = 0;
      intersectOneDim(ax0, ax1, bx0, bx1, 0, useMTV, outIntersectRect, clamp2);
      intersectOneDim(ay0, ay1, by0, by1, 1, useMTV, outIntersectRect, clamp2);
      if (useMTV) {
        Point_default.copy(mtv, overlap ? _intersectCtx.useDir ? _intersectCtx.dirMinTv : _minTv : _maxTv);
      }
    }
    return overlap;
  };
  BoundingRect2.contain = function(rect, x, y) {
    return x >= rect.x && x <= rect.x + rect.width && y >= rect.y && y <= rect.y + rect.height;
  };
  BoundingRect2.prototype.contain = function(x, y) {
    return BoundingRect2.contain(this, x, y);
  };
  BoundingRect2.prototype.clone = function() {
    return new BoundingRect2(this.x, this.y, this.width, this.height);
  };
  BoundingRect2.prototype.copy = function(other) {
    boundingRectCopy(this, other);
  };
  BoundingRect2.prototype.plain = function() {
    return {
      x: this.x,
      y: this.y,
      width: this.width,
      height: this.height
    };
  };
  BoundingRect2.prototype.isFinite = function() {
    return isFinite(this.x) && isFinite(this.y) && isFinite(this.width) && isFinite(this.height);
  };
  BoundingRect2.prototype.isZero = function() {
    return this.width === 0 || this.height === 0;
  };
  BoundingRect2.create = function(rect) {
    return new BoundingRect2(rect ? rect.x : 0, rect ? rect.y : 0, rect ? rect.width : 0, rect ? rect.height : 0);
  };
  BoundingRect2.copy = function(target, source) {
    target.x = source.x;
    target.y = source.y;
    target.width = source.width;
    target.height = source.height;
    return target;
  };
  BoundingRect2.applyTransform = function(target, source, m2) {
    if (!m2) {
      if (target !== source) {
        boundingRectCopy(target, source);
      }
      return;
    }
    if (m2[1] < 1e-5 && m2[1] > -1e-5 && m2[2] < 1e-5 && m2[2] > -1e-5) {
      var sx = m2[0];
      var sy = m2[3];
      var tx = m2[4];
      var ty = m2[5];
      target.x = source.x * sx + tx;
      target.y = source.y * sy + ty;
      target.width = source.width * sx;
      target.height = source.height * sy;
      if (target.width < 0) {
        target.x += target.width;
        target.width = -target.width;
      }
      if (target.height < 0) {
        target.y += target.height;
        target.height = -target.height;
      }
      return;
    }
    lt.x = lb.x = source.x;
    lt.y = rt.y = source.y;
    rb.x = rt.x = source.x + source.width;
    rb.y = lb.y = source.y + source.height;
    lt.transform(m2);
    rt.transform(m2);
    rb.transform(m2);
    lb.transform(m2);
    target.x = mathMin(lt.x, rb.x, lb.x, rt.x);
    target.y = mathMin(lt.y, rb.y, lb.y, rt.y);
    var maxX = mathMax(lt.x, rb.x, lb.x, rt.x);
    var maxY = mathMax(lt.y, rb.y, lb.y, rt.y);
    target.width = maxX - target.x;
    target.height = maxY - target.y;
  };
  BoundingRect2.calculateTransform = function(out2, a, b) {
    var sx = b.width / a.width;
    var sy = b.height / a.height;
    out2 = identity(out2 || []);
    translate(out2, out2, set(_tmpCalcTrans, -a.x, -a.y));
    scale(out2, out2, set(_tmpCalcTrans, sx, sy));
    translate(out2, out2, set(_tmpCalcTrans, b.x, b.y));
    return out2;
  };
  return BoundingRect2;
})();
var boundingRectCreate = BoundingRect.create;
var boundingRectSet = BoundingRect.set;
var boundingRectCopy = BoundingRect.copy;
var boundingRectCalculateTransform = BoundingRect.calculateTransform;
var boundingRectApplyTransform = BoundingRect.applyTransform;
var boundingRectContain = BoundingRect.contain;
var _tmpIntersectA = new BoundingRect(0, 0, 0, 0);
var _tmpIntersectB = new BoundingRect(0, 0, 0, 0);
var _tmpCalcTrans = [];
function intersectOneDim(a0, a1, b0, b1, updateDimIdx, useMTV, outIntersectRect, clamp2) {
  var d0 = mathAbs(a1 - b0);
  var d1 = mathAbs(b1 - a0);
  var d01min = mathMin(d0, d1);
  var updateDim = XY[updateDimIdx];
  var zeroDim = XY[1 - updateDimIdx];
  var wh = WH[updateDimIdx];
  if (a1 < b0 || b1 < a0) {
    if (d0 < d1) {
      if (useMTV) {
        _maxTv[updateDim] = -d0;
      }
      if (clamp2) {
        outIntersectRect[updateDim] = a1;
        outIntersectRect[wh] = 0;
      }
    } else {
      if (useMTV) {
        _maxTv[updateDim] = d1;
      }
      if (clamp2) {
        outIntersectRect[updateDim] = a0;
        outIntersectRect[wh] = 0;
      }
    }
  } else {
    if (outIntersectRect) {
      outIntersectRect[updateDim] = mathMax(a0, b0);
      outIntersectRect[wh] = mathMin(a1, b1) - outIntersectRect[updateDim];
    }
    if (useMTV) {
      if (d01min < _lenMinMax[0] || _intersectCtx.useDir) {
        _lenMinMax[0] = mathMin(d01min, _lenMinMax[0]);
        if (d0 < d1 || !_intersectCtx.bidirectional) {
          _minTv[updateDim] = d0;
          _minTv[zeroDim] = 0;
          if (_intersectCtx.useDir) {
            _intersectCtx.calcDirMTV();
          }
        }
        if (d0 >= d1 || !_intersectCtx.bidirectional) {
          _minTv[updateDim] = -d1;
          _minTv[zeroDim] = 0;
          if (_intersectCtx.useDir) {
            _intersectCtx.calcDirMTV();
          }
        }
      }
    }
  }
}
function createIntersectContext() {
  var _direction = 0;
  var _dirCheckVec = new Point_default();
  var _dirTmp = new Point_default();
  var _ctx = {
    minTv: new Point_default(),
    maxTv: new Point_default(),
    useDir: false,
    dirMinTv: new Point_default(),
    touchThreshold: 0,
    bidirectional: true,
    negativeSize: false,
    reset: function(opt, useMTV) {
      _ctx.touchThreshold = 0;
      if (opt && opt.touchThreshold != null) {
        _ctx.touchThreshold = mathMax(0, opt.touchThreshold);
      }
      _ctx.negativeSize = false;
      if (!useMTV) {
        return;
      }
      _ctx.minTv.set(Infinity, Infinity);
      _ctx.maxTv.set(0, 0);
      _ctx.useDir = false;
      if (opt && opt.direction != null) {
        _ctx.useDir = true;
        _ctx.dirMinTv.copy(_ctx.minTv);
        _dirTmp.copy(_ctx.minTv);
        _direction = opt.direction;
        _ctx.bidirectional = opt.bidirectional == null || !!opt.bidirectional;
        if (!_ctx.bidirectional) {
          _dirCheckVec.set(Math.cos(_direction), Math.sin(_direction));
        }
      }
    },
    calcDirMTV: function() {
      var minTv = _ctx.minTv;
      var dirMinTv = _ctx.dirMinTv;
      var squareMag = minTv.y * minTv.y + minTv.x * minTv.x;
      var dirSin = Math.sin(_direction);
      var dirCos = Math.cos(_direction);
      var dotProd = dirSin * minTv.y + dirCos * minTv.x;
      if (nearZero2(dotProd)) {
        if (nearZero2(minTv.x) && nearZero2(minTv.y)) {
          dirMinTv.set(0, 0);
        }
        return;
      }
      _dirTmp.x = squareMag * dirCos / dotProd;
      _dirTmp.y = squareMag * dirSin / dotProd;
      if (nearZero2(_dirTmp.x) && nearZero2(_dirTmp.y)) {
        dirMinTv.set(0, 0);
        return;
      }
      if ((_ctx.bidirectional || _dirCheckVec.dot(_dirTmp) > 0) && _dirTmp.len() < dirMinTv.len()) {
        dirMinTv.copy(_dirTmp);
      }
    }
  };
  function nearZero2(val) {
    return mathAbs(val) < 1e-10;
  }
  return _ctx;
}
var BoundingRect_default = BoundingRect;

// node_modules/zrender/lib/contain/text.js
function ensureFontMeasureInfo(font) {
  if (!_fontMeasureInfoCache) {
    _fontMeasureInfoCache = new LRU_default(100);
  }
  font = font || DEFAULT_FONT;
  var measureInfo = _fontMeasureInfoCache.get(font);
  if (!measureInfo) {
    measureInfo = {
      font,
      strWidthCache: new LRU_default(500),
      asciiWidthMap: null,
      asciiWidthMapTried: false,
      stWideCharWidth: platformApi.measureText("国", font).width,
      asciiCharWidth: platformApi.measureText("a", font).width
    };
    _fontMeasureInfoCache.put(font, measureInfo);
  }
  return measureInfo;
}
var _fontMeasureInfoCache;
function tryCreateASCIIWidthMap(font) {
  if (_getASCIIWidthMapLongCount >= GET_ASCII_WIDTH_LONG_COUNT_MAX) {
    return;
  }
  font = font || DEFAULT_FONT;
  var asciiWidthMap = [];
  var start2 = +/* @__PURE__ */ new Date();
  for (var code = 0; code <= 127; code++) {
    asciiWidthMap[code] = platformApi.measureText(String.fromCharCode(code), font).width;
  }
  var cost = +/* @__PURE__ */ new Date() - start2;
  if (cost > 16) {
    _getASCIIWidthMapLongCount = GET_ASCII_WIDTH_LONG_COUNT_MAX;
  } else if (cost > 2) {
    _getASCIIWidthMapLongCount++;
  }
  return asciiWidthMap;
}
var _getASCIIWidthMapLongCount = 0;
var GET_ASCII_WIDTH_LONG_COUNT_MAX = 5;
function measureCharWidth(fontMeasureInfo, charCode) {
  if (!fontMeasureInfo.asciiWidthMapTried) {
    fontMeasureInfo.asciiWidthMap = tryCreateASCIIWidthMap(fontMeasureInfo.font);
    fontMeasureInfo.asciiWidthMapTried = true;
  }
  return 0 <= charCode && charCode <= 127 ? fontMeasureInfo.asciiWidthMap != null ? fontMeasureInfo.asciiWidthMap[charCode] : fontMeasureInfo.asciiCharWidth : fontMeasureInfo.stWideCharWidth;
}
function measureWidth(fontMeasureInfo, text) {
  var strWidthCache = fontMeasureInfo.strWidthCache;
  var width = strWidthCache.get(text);
  if (width == null) {
    width = platformApi.measureText(text, fontMeasureInfo.font).width;
    strWidthCache.put(text, width);
  }
  return width;
}
function innerGetBoundingRect(text, font, textAlign, textBaseline) {
  var width = measureWidth(ensureFontMeasureInfo(font), text);
  var height = getLineHeight(font);
  var x = adjustTextX(0, width, textAlign);
  var y = adjustTextY(0, height, textBaseline);
  var rect = new BoundingRect_default(x, y, width, height);
  return rect;
}
function getBoundingRect(text, font, textAlign, textBaseline) {
  var textLines = ((text || "") + "").split("\n");
  var len2 = textLines.length;
  if (len2 === 1) {
    return innerGetBoundingRect(textLines[0], font, textAlign, textBaseline);
  } else {
    var uniondRect = new BoundingRect_default(0, 0, 0, 0);
    for (var i2 = 0; i2 < textLines.length; i2++) {
      var rect = innerGetBoundingRect(textLines[i2], font, textAlign, textBaseline);
      i2 === 0 ? uniondRect.copy(rect) : uniondRect.union(rect);
    }
    return uniondRect;
  }
}
function adjustTextX(x, width, textAlign, inverse) {
  if (textAlign === "right") {
    !inverse ? x -= width : x += width;
  } else if (textAlign === "center") {
    !inverse ? x -= width / 2 : x += width / 2;
  }
  return x;
}
function adjustTextY(y, height, verticalAlign, inverse) {
  if (verticalAlign === "middle") {
    !inverse ? y -= height / 2 : y += height / 2;
  } else if (verticalAlign === "bottom") {
    !inverse ? y -= height : y += height;
  }
  return y;
}
function getLineHeight(font) {
  return ensureFontMeasureInfo(font).stWideCharWidth;
}
function parsePercent(value, maxValue) {
  if (typeof value === "string") {
    if (value.lastIndexOf("%") >= 0) {
      return parseFloat(value) / 100 * maxValue;
    }
    return parseFloat(value);
  }
  return value;
}
function calculateTextPosition(out2, opts, rect) {
  var textPosition = opts.position || "inside";
  var distance2 = opts.distance != null ? opts.distance : 5;
  var height = rect.height;
  var width = rect.width;
  var halfHeight = height / 2;
  var x = rect.x;
  var y = rect.y;
  var textAlign = "left";
  var textVerticalAlign = "top";
  if (textPosition instanceof Array) {
    x += parsePercent(textPosition[0], rect.width);
    y += parsePercent(textPosition[1], rect.height);
    textAlign = null;
    textVerticalAlign = null;
  } else {
    switch (textPosition) {
      case "left":
        x -= distance2;
        y += halfHeight;
        textAlign = "right";
        textVerticalAlign = "middle";
        break;
      case "right":
        x += distance2 + width;
        y += halfHeight;
        textVerticalAlign = "middle";
        break;
      case "top":
        x += width / 2;
        y -= distance2;
        textAlign = "center";
        textVerticalAlign = "bottom";
        break;
      case "bottom":
        x += width / 2;
        y += height + distance2;
        textAlign = "center";
        break;
      case "inside":
        x += width / 2;
        y += halfHeight;
        textAlign = "center";
        textVerticalAlign = "middle";
        break;
      case "insideLeft":
        x += distance2;
        y += halfHeight;
        textVerticalAlign = "middle";
        break;
      case "insideRight":
        x += width - distance2;
        y += halfHeight;
        textAlign = "right";
        textVerticalAlign = "middle";
        break;
      case "insideTop":
        x += width / 2;
        y += distance2;
        textAlign = "center";
        break;
      case "insideBottom":
        x += width / 2;
        y += height - distance2;
        textAlign = "center";
        textVerticalAlign = "bottom";
        break;
      case "insideTopLeft":
        x += distance2;
        y += distance2;
        break;
      case "insideTopRight":
        x += width - distance2;
        y += distance2;
        textAlign = "right";
        break;
      case "insideBottomLeft":
        x += distance2;
        y += height - distance2;
        textVerticalAlign = "bottom";
        break;
      case "insideBottomRight":
        x += width - distance2;
        y += height - distance2;
        textAlign = "right";
        textVerticalAlign = "bottom";
        break;
    }
  }
  out2 = out2 || {};
  out2.x = x;
  out2.y = y;
  out2.align = textAlign;
  out2.verticalAlign = textVerticalAlign;
  return out2;
}

// node_modules/zrender/lib/graphic/helper/parseText.js
var STYLE_REG = /\{([a-zA-Z0-9_]+)\|([^}]*)\}/g;
function truncateText2(out2, text, containerWidth, font, ellipsis, options) {
  if (!containerWidth) {
    out2.text = "";
    out2.isTruncated = false;
    return;
  }
  var textLines = (text + "").split("\n");
  options = prepareTruncateOptions(containerWidth, font, ellipsis, options);
  var isTruncated = false;
  var truncateOut = {};
  for (var i2 = 0, len2 = textLines.length; i2 < len2; i2++) {
    truncateSingleLine(truncateOut, textLines[i2], options);
    textLines[i2] = truncateOut.textLine;
    isTruncated = isTruncated || truncateOut.isTruncated;
  }
  out2.text = textLines.join("\n");
  out2.isTruncated = isTruncated;
}
function prepareTruncateOptions(containerWidth, font, ellipsis, options) {
  options = options || {};
  var preparedOpts = extend({}, options);
  ellipsis = retrieve2(ellipsis, "...");
  preparedOpts.maxIterations = retrieve2(options.maxIterations, 2);
  var minChar = preparedOpts.minChar = retrieve2(options.minChar, 0);
  var fontMeasureInfo = preparedOpts.fontMeasureInfo = ensureFontMeasureInfo(font);
  var ascCharWidth = fontMeasureInfo.asciiCharWidth;
  preparedOpts.placeholder = retrieve2(options.placeholder, "");
  var contentWidth = containerWidth = Math.max(0, containerWidth - 1);
  for (var i2 = 0; i2 < minChar && contentWidth >= ascCharWidth; i2++) {
    contentWidth -= ascCharWidth;
  }
  var ellipsisWidth = measureWidth(fontMeasureInfo, ellipsis);
  if (ellipsisWidth > contentWidth) {
    ellipsis = "";
    ellipsisWidth = 0;
  }
  contentWidth = containerWidth - ellipsisWidth;
  preparedOpts.ellipsis = ellipsis;
  preparedOpts.ellipsisWidth = ellipsisWidth;
  preparedOpts.contentWidth = contentWidth;
  preparedOpts.containerWidth = containerWidth;
  return preparedOpts;
}
function truncateSingleLine(out2, textLine, options) {
  var containerWidth = options.containerWidth;
  var contentWidth = options.contentWidth;
  var fontMeasureInfo = options.fontMeasureInfo;
  if (!containerWidth) {
    out2.textLine = "";
    out2.isTruncated = false;
    return;
  }
  var lineWidth = measureWidth(fontMeasureInfo, textLine);
  if (lineWidth <= containerWidth) {
    out2.textLine = textLine;
    out2.isTruncated = false;
    return;
  }
  for (var j = 0; ; j++) {
    if (lineWidth <= contentWidth || j >= options.maxIterations) {
      textLine += options.ellipsis;
      break;
    }
    var subLength = j === 0 ? estimateLength(textLine, contentWidth, fontMeasureInfo) : lineWidth > 0 ? Math.floor(textLine.length * contentWidth / lineWidth) : 0;
    textLine = textLine.substr(0, subLength);
    lineWidth = measureWidth(fontMeasureInfo, textLine);
  }
  if (textLine === "") {
    textLine = options.placeholder;
  }
  out2.textLine = textLine;
  out2.isTruncated = true;
}
function estimateLength(text, contentWidth, fontMeasureInfo) {
  var width = 0;
  var i2 = 0;
  for (var len2 = text.length; i2 < len2 && width < contentWidth; i2++) {
    width += measureCharWidth(fontMeasureInfo, text.charCodeAt(i2));
  }
  return i2;
}
function parsePlainText(rawText, style2, defaultOuterWidth, defaultOuterHeight) {
  var text = formatText(rawText);
  var overflow = style2.overflow;
  var padding = style2.padding;
  var paddingH = padding ? padding[1] + padding[3] : 0;
  var paddingV = padding ? padding[0] + padding[2] : 0;
  var font = style2.font;
  var truncate = overflow === "truncate";
  var calculatedLineHeight = getLineHeight(font);
  var lineHeight = retrieve2(style2.lineHeight, calculatedLineHeight);
  var truncateLineOverflow = style2.lineOverflow === "truncate";
  var isTruncated = false;
  var width = style2.width;
  if (width == null && defaultOuterWidth != null) {
    width = defaultOuterWidth - paddingH;
  }
  var height = style2.height;
  if (height == null && defaultOuterHeight != null) {
    height = defaultOuterHeight - paddingV;
  }
  var lines;
  if (width != null && (overflow === "break" || overflow === "breakAll")) {
    lines = text ? wrapText(text, style2.font, width, overflow === "breakAll", 0).lines : [];
  } else {
    lines = text ? text.split("\n") : [];
  }
  var contentHeight = lines.length * lineHeight;
  if (height == null) {
    height = contentHeight;
  }
  if (contentHeight > height && truncateLineOverflow) {
    var lineCount = Math.floor(height / lineHeight);
    isTruncated = isTruncated || lines.length > lineCount;
    lines = lines.slice(0, lineCount);
    contentHeight = lines.length * lineHeight;
  }
  if (text && truncate && width != null) {
    var options = prepareTruncateOptions(width, font, style2.ellipsis, {
      minChar: style2.truncateMinChar,
      placeholder: style2.placeholder
    });
    var singleOut = {};
    for (var i2 = 0; i2 < lines.length; i2++) {
      truncateSingleLine(singleOut, lines[i2], options);
      lines[i2] = singleOut.textLine;
      isTruncated = isTruncated || singleOut.isTruncated;
    }
  }
  var outerHeight = height;
  var contentWidth = 0;
  var fontMeasureInfo = ensureFontMeasureInfo(font);
  for (var i2 = 0; i2 < lines.length; i2++) {
    contentWidth = Math.max(measureWidth(fontMeasureInfo, lines[i2]), contentWidth);
  }
  if (width == null) {
    width = contentWidth;
  }
  var outerWidth = width;
  outerHeight += paddingV;
  outerWidth += paddingH;
  return {
    lines,
    height,
    outerWidth,
    outerHeight,
    lineHeight,
    calculatedLineHeight,
    contentWidth,
    contentHeight,
    width,
    isTruncated
  };
}
var RichTextToken = /* @__PURE__ */ (function() {
  function RichTextToken2() {
  }
  return RichTextToken2;
})();
var RichTextLine = /* @__PURE__ */ (function() {
  function RichTextLine2(tokens2) {
    this.tokens = [];
    if (tokens2) {
      this.tokens = tokens2;
    }
  }
  return RichTextLine2;
})();
var RichTextContentBlock = /* @__PURE__ */ (function() {
  function RichTextContentBlock2() {
    this.width = 0;
    this.height = 0;
    this.contentWidth = 0;
    this.contentHeight = 0;
    this.outerWidth = 0;
    this.outerHeight = 0;
    this.lines = [];
    this.isTruncated = false;
  }
  return RichTextContentBlock2;
})();
function parseRichText(rawText, style2, defaultOuterWidth, defaultOuterHeight, topTextAlign) {
  var contentBlock = new RichTextContentBlock();
  var text = formatText(rawText);
  if (!text) {
    return contentBlock;
  }
  var stlPadding = style2.padding;
  var stlPaddingH = stlPadding ? stlPadding[1] + stlPadding[3] : 0;
  var stlPaddingV = stlPadding ? stlPadding[0] + stlPadding[2] : 0;
  var topWidth = style2.width;
  if (topWidth == null && defaultOuterWidth != null) {
    topWidth = defaultOuterWidth - stlPaddingH;
  }
  var topHeight = style2.height;
  if (topHeight == null && defaultOuterHeight != null) {
    topHeight = defaultOuterHeight - stlPaddingV;
  }
  var overflow = style2.overflow;
  var wrapInfo = (overflow === "break" || overflow === "breakAll") && topWidth != null ? { width: topWidth, accumWidth: 0, breakAll: overflow === "breakAll" } : null;
  var lastIndex = STYLE_REG.lastIndex = 0;
  var result;
  while ((result = STYLE_REG.exec(text)) != null) {
    var matchedIndex = result.index;
    if (matchedIndex > lastIndex) {
      pushTokens(contentBlock, text.substring(lastIndex, matchedIndex), style2, wrapInfo);
    }
    pushTokens(contentBlock, result[2], style2, wrapInfo, result[1]);
    lastIndex = STYLE_REG.lastIndex;
  }
  if (lastIndex < text.length) {
    pushTokens(contentBlock, text.substring(lastIndex, text.length), style2, wrapInfo);
  }
  var pendingList = [];
  var calculatedHeight = 0;
  var calculatedWidth = 0;
  var truncate = overflow === "truncate";
  var truncateLine = style2.lineOverflow === "truncate";
  var tmpTruncateOut = {};
  function finishLine(line2, lineWidth2, lineHeight2) {
    line2.width = lineWidth2;
    line2.lineHeight = lineHeight2;
    calculatedHeight += lineHeight2;
    calculatedWidth = Math.max(calculatedWidth, lineWidth2);
  }
  outer: for (var i2 = 0; i2 < contentBlock.lines.length; i2++) {
    var line = contentBlock.lines[i2];
    var lineHeight = 0;
    var lineWidth = 0;
    for (var j = 0; j < line.tokens.length; j++) {
      var token = line.tokens[j];
      var tokenStyle = token.styleName && style2.rich[token.styleName] || {};
      var textPadding = token.textPadding = tokenStyle.padding;
      var paddingH = textPadding ? textPadding[1] + textPadding[3] : 0;
      var font = token.font = tokenStyle.font || style2.font;
      token.contentHeight = getLineHeight(font);
      var tokenHeight = retrieve2(tokenStyle.height, token.contentHeight);
      token.innerHeight = tokenHeight;
      textPadding && (tokenHeight += textPadding[0] + textPadding[2]);
      token.height = tokenHeight;
      token.lineHeight = retrieve3(tokenStyle.lineHeight, style2.lineHeight, tokenHeight);
      token.align = tokenStyle && tokenStyle.align || topTextAlign;
      token.verticalAlign = tokenStyle && tokenStyle.verticalAlign || "middle";
      if (truncateLine && topHeight != null && calculatedHeight + token.lineHeight > topHeight) {
        var originalLength = contentBlock.lines.length;
        if (j > 0) {
          line.tokens = line.tokens.slice(0, j);
          finishLine(line, lineWidth, lineHeight);
          contentBlock.lines = contentBlock.lines.slice(0, i2 + 1);
        } else {
          contentBlock.lines = contentBlock.lines.slice(0, i2);
        }
        contentBlock.isTruncated = contentBlock.isTruncated || contentBlock.lines.length < originalLength;
        break outer;
      }
      var styleTokenWidth = tokenStyle.width;
      var tokenWidthNotSpecified = styleTokenWidth == null || styleTokenWidth === "auto";
      if (typeof styleTokenWidth === "string" && styleTokenWidth.charAt(styleTokenWidth.length - 1) === "%") {
        token.percentWidth = styleTokenWidth;
        pendingList.push(token);
        token.contentWidth = measureWidth(ensureFontMeasureInfo(font), token.text);
      } else {
        if (tokenWidthNotSpecified) {
          var textBackgroundColor = tokenStyle.backgroundColor;
          var bgImg = textBackgroundColor && textBackgroundColor.image;
          if (bgImg) {
            bgImg = findExistImage(bgImg);
            if (isImageReady(bgImg)) {
              token.width = Math.max(token.width, bgImg.width * tokenHeight / bgImg.height);
            }
          }
        }
        var remainTruncWidth = truncate && topWidth != null ? topWidth - lineWidth : null;
        if (remainTruncWidth != null && remainTruncWidth < token.width) {
          if (!tokenWidthNotSpecified || remainTruncWidth < paddingH) {
            token.text = "";
            token.width = token.contentWidth = 0;
          } else {
            truncateText2(tmpTruncateOut, token.text, remainTruncWidth - paddingH, font, style2.ellipsis, { minChar: style2.truncateMinChar });
            token.text = tmpTruncateOut.text;
            contentBlock.isTruncated = contentBlock.isTruncated || tmpTruncateOut.isTruncated;
            token.width = token.contentWidth = measureWidth(ensureFontMeasureInfo(font), token.text);
          }
        } else {
          token.contentWidth = measureWidth(ensureFontMeasureInfo(font), token.text);
        }
      }
      token.width += paddingH;
      lineWidth += token.width;
      tokenStyle && (lineHeight = Math.max(lineHeight, token.lineHeight));
    }
    finishLine(line, lineWidth, lineHeight);
  }
  contentBlock.outerWidth = contentBlock.width = retrieve2(topWidth, calculatedWidth);
  contentBlock.outerHeight = contentBlock.height = retrieve2(topHeight, calculatedHeight);
  contentBlock.contentHeight = calculatedHeight;
  contentBlock.contentWidth = calculatedWidth;
  contentBlock.outerWidth += stlPaddingH;
  contentBlock.outerHeight += stlPaddingV;
  for (var i2 = 0; i2 < pendingList.length; i2++) {
    var token = pendingList[i2];
    var percentWidth = token.percentWidth;
    token.width = parseInt(percentWidth, 10) / 100 * contentBlock.width;
  }
  return contentBlock;
}
function pushTokens(block, str, style2, wrapInfo, styleName) {
  var isEmptyStr = str === "";
  var tokenStyle = styleName && style2.rich[styleName] || {};
  var lines = block.lines;
  var font = tokenStyle.font || style2.font;
  var newLine = false;
  var strLines;
  var linesWidths;
  if (wrapInfo) {
    var tokenPadding = tokenStyle.padding;
    var tokenPaddingH = tokenPadding ? tokenPadding[1] + tokenPadding[3] : 0;
    if (tokenStyle.width != null && tokenStyle.width !== "auto") {
      var outerWidth_1 = parsePercent(tokenStyle.width, wrapInfo.width) + tokenPaddingH;
      if (lines.length > 0) {
        if (outerWidth_1 + wrapInfo.accumWidth > wrapInfo.width) {
          strLines = str.split("\n");
          newLine = true;
        }
      }
      wrapInfo.accumWidth = outerWidth_1;
    } else {
      var res = wrapText(str, font, wrapInfo.width, wrapInfo.breakAll, wrapInfo.accumWidth);
      wrapInfo.accumWidth = res.accumWidth + tokenPaddingH;
      linesWidths = res.linesWidths;
      strLines = res.lines;
    }
  }
  if (!strLines) {
    strLines = str.split("\n");
  }
  var fontMeasureInfo = ensureFontMeasureInfo(font);
  for (var i2 = 0; i2 < strLines.length; i2++) {
    var text = strLines[i2];
    var token = new RichTextToken();
    token.styleName = styleName;
    token.text = text;
    token.isLineHolder = !text && !isEmptyStr;
    if (typeof tokenStyle.width === "number") {
      token.width = tokenStyle.width;
    } else {
      token.width = linesWidths ? linesWidths[i2] : measureWidth(fontMeasureInfo, text);
    }
    if (!i2 && !newLine) {
      var tokens2 = (lines[lines.length - 1] || (lines[0] = new RichTextLine())).tokens;
      var tokensLen = tokens2.length;
      tokensLen === 1 && tokens2[0].isLineHolder ? tokens2[0] = token : (text || !tokensLen || isEmptyStr) && tokens2.push(token);
    } else {
      lines.push(new RichTextLine([token]));
    }
  }
}
function isAlphabeticLetter(ch) {
  var code = ch.charCodeAt(0);
  return code >= 32 && code <= 591 || code >= 880 && code <= 4351 || code >= 4608 && code <= 5119 || code >= 7680 && code <= 8303;
}
var breakCharMap = reduce(",&?/;] ".split(""), function(obj, ch) {
  obj[ch] = true;
  return obj;
}, {});
function isWordBreakChar(ch) {
  if (isAlphabeticLetter(ch)) {
    if (breakCharMap[ch]) {
      return true;
    }
    return false;
  }
  return true;
}
function wrapText(text, font, lineWidth, isBreakAll, lastAccumWidth) {
  var lines = [];
  var linesWidths = [];
  var line = "";
  var currentWord = "";
  var currentWordWidth = 0;
  var accumWidth = 0;
  var fontMeasureInfo = ensureFontMeasureInfo(font);
  for (var i2 = 0; i2 < text.length; i2++) {
    var ch = text.charAt(i2);
    if (ch === "\n") {
      if (currentWord) {
        line += currentWord;
        accumWidth += currentWordWidth;
      }
      lines.push(line);
      linesWidths.push(accumWidth);
      line = "";
      currentWord = "";
      currentWordWidth = 0;
      accumWidth = 0;
      continue;
    }
    var chWidth = measureCharWidth(fontMeasureInfo, ch.charCodeAt(0));
    var inWord = isBreakAll ? false : !isWordBreakChar(ch);
    if (!lines.length ? lastAccumWidth + accumWidth + chWidth > lineWidth : accumWidth + chWidth > lineWidth) {
      if (!accumWidth) {
        if (inWord) {
          lines.push(currentWord);
          linesWidths.push(currentWordWidth);
          currentWord = ch;
          currentWordWidth = chWidth;
        } else {
          lines.push(ch);
          linesWidths.push(chWidth);
        }
      } else if (line || currentWord) {
        if (inWord) {
          if (!line) {
            line = currentWord;
            currentWord = "";
            currentWordWidth = 0;
            accumWidth = currentWordWidth;
          }
          lines.push(line);
          linesWidths.push(accumWidth - currentWordWidth);
          currentWord += ch;
          currentWordWidth += chWidth;
          line = "";
          accumWidth = currentWordWidth;
        } else {
          if (currentWord) {
            line += currentWord;
            currentWord = "";
            currentWordWidth = 0;
          }
          lines.push(line);
          linesWidths.push(accumWidth);
          line = ch;
          accumWidth = chWidth;
        }
      }
      continue;
    }
    accumWidth += chWidth;
    if (inWord) {
      currentWord += ch;
      currentWordWidth += chWidth;
    } else {
      if (currentWord) {
        line += currentWord;
        currentWord = "";
        currentWordWidth = 0;
      }
      line += ch;
    }
  }
  if (currentWord) {
    line += currentWord;
  }
  if (line) {
    lines.push(line);
    linesWidths.push(accumWidth);
  }
  if (lines.length === 1) {
    accumWidth += lastAccumWidth;
  }
  return {
    accumWidth,
    lines,
    linesWidths
  };
}
function calcInnerTextOverflowArea(out2, overflowRect, baseX, baseY, textAlign, textVerticalAlign) {
  out2.baseX = baseX;
  out2.baseY = baseY;
  out2.outerWidth = out2.outerHeight = null;
  if (!overflowRect) {
    return;
  }
  var textWidth = overflowRect.width * 2;
  var textHeight = overflowRect.height * 2;
  BoundingRect_default.set(tmpCITCTextRect, adjustTextX(baseX, textWidth, textAlign), adjustTextY(baseY, textHeight, textVerticalAlign), textWidth, textHeight);
  BoundingRect_default.intersect(overflowRect, tmpCITCTextRect, null, tmpCITCIntersectRectOpt);
  var outIntersectRect = tmpCITCIntersectRectOpt.outIntersectRect;
  out2.outerWidth = outIntersectRect.width;
  out2.outerHeight = outIntersectRect.height;
  out2.baseX = adjustTextX(outIntersectRect.x, outIntersectRect.width, textAlign, true);
  out2.baseY = adjustTextY(outIntersectRect.y, outIntersectRect.height, textVerticalAlign, true);
}
var tmpCITCTextRect = new BoundingRect_default(0, 0, 0, 0);
var tmpCITCIntersectRectOpt = { outIntersectRect: {}, clamp: true };
function formatText(text) {
  return text != null ? text += "" : text = "";
}
function tSpanCreateBoundingRect(style2) {
  var text = formatText(style2.text);
  var font = style2.font;
  var contentWidth = measureWidth(ensureFontMeasureInfo(font), text);
  var contentHeight = getLineHeight(font);
  return tSpanCreateBoundingRect2(style2, contentWidth, contentHeight, null);
}
function tSpanCreateBoundingRect2(style2, contentWidth, contentHeight, forceLineWidth) {
  var rect = new BoundingRect_default(adjustTextX(style2.x || 0, contentWidth, style2.textAlign), adjustTextY(style2.y || 0, contentHeight, style2.textBaseline), contentWidth, contentHeight);
  var lineWidth = forceLineWidth != null ? forceLineWidth : tSpanHasStroke(style2) ? style2.lineWidth : 0;
  if (lineWidth > 0) {
    rect.x -= lineWidth / 2;
    rect.y -= lineWidth / 2;
    rect.width += lineWidth;
    rect.height += lineWidth;
  }
  return rect;
}
function tSpanHasStroke(style2) {
  var stroke = style2.stroke;
  return stroke != null && stroke !== "none" && style2.lineWidth > 0;
}

// node_modules/zrender/lib/core/Transformable.js
var mIdentity = identity;
var EPSILON2 = 5e-5;
function isNotAroundZero(val) {
  return val > EPSILON2 || val < -EPSILON2;
}
var scaleTmp = [];
var tmpTransform = [];
var originTransform = create();
var abs = Math.abs;
var Transformable = (function() {
  function Transformable2() {
  }
  Transformable2.prototype.getLocalTransform = function(m2) {
    return transformableGetLocalTransform(this, m2);
  };
  Transformable2.prototype.setPosition = function(arr) {
    this.x = arr[0];
    this.y = arr[1];
  };
  Transformable2.prototype.setScale = function(arr) {
    this.scaleX = arr[0];
    this.scaleY = arr[1];
  };
  Transformable2.prototype.setSkew = function(arr) {
    this.skewX = arr[0];
    this.skewY = arr[1];
  };
  Transformable2.prototype.setOrigin = function(arr) {
    this.originX = arr[0];
    this.originY = arr[1];
  };
  Transformable2.prototype.needLocalTransform = function() {
    return isNotAroundZero(this.rotation) || isNotAroundZero(this.x) || isNotAroundZero(this.y) || isNotAroundZero(this.scaleX - 1) || isNotAroundZero(this.scaleY - 1) || isNotAroundZero(this.skewX) || isNotAroundZero(this.skewY);
  };
  Transformable2.prototype.updateTransform = function() {
    var parentTransform = this.parent && this.parent.transform;
    var needLocalTransform = this.needLocalTransform();
    var m2 = this.transform;
    if (!(needLocalTransform || parentTransform)) {
      if (m2) {
        mIdentity(m2);
        this.invTransform = null;
      }
      return;
    }
    m2 = m2 || create();
    if (needLocalTransform) {
      this.getLocalTransform(m2);
    } else {
      mIdentity(m2);
    }
    if (parentTransform) {
      if (needLocalTransform) {
        mul(m2, parentTransform, m2);
      } else {
        copy(m2, parentTransform);
      }
    }
    this.transform = m2;
    this._resolveGlobalScaleRatio(m2);
    this.invTransform = this.invTransform || create();
    invert(this.invTransform, m2);
  };
  Transformable2.prototype._resolveGlobalScaleRatio = function(m2) {
    var globalScaleRatio = this.globalScaleRatio;
    if (globalScaleRatio != null && globalScaleRatio !== 1) {
      this.getGlobalScale(scaleTmp);
      var relX = scaleTmp[0] < 0 ? -1 : 1;
      var relY = scaleTmp[1] < 0 ? -1 : 1;
      var sx = ((scaleTmp[0] - relX) * globalScaleRatio + relX) / scaleTmp[0] || 0;
      var sy = ((scaleTmp[1] - relY) * globalScaleRatio + relY) / scaleTmp[1] || 0;
      m2[0] *= sx;
      m2[1] *= sx;
      m2[2] *= sy;
      m2[3] *= sy;
    }
  };
  Transformable2.prototype.getComputedTransform = function() {
    var transformNode = this;
    var ancestors = [];
    while (transformNode) {
      ancestors.push(transformNode);
      transformNode = transformNode.parent;
    }
    while (transformNode = ancestors.pop()) {
      transformNode.updateTransform();
    }
    return this.transform;
  };
  Transformable2.prototype.setLocalTransform = function(m2) {
    if (!m2) {
      return;
    }
    var sx = m2[0] * m2[0] + m2[1] * m2[1];
    var sy = m2[2] * m2[2] + m2[3] * m2[3];
    var rotation = Math.atan2(m2[1], m2[0]);
    var shearX = Math.PI / 2 + rotation - Math.atan2(m2[3], m2[2]);
    sy = Math.sqrt(sy) * Math.cos(shearX);
    sx = Math.sqrt(sx);
    this.skewX = shearX;
    this.skewY = 0;
    this.rotation = -rotation;
    this.x = +m2[4];
    this.y = +m2[5];
    this.scaleX = sx;
    this.scaleY = sy;
    this.originX = 0;
    this.originY = 0;
  };
  Transformable2.prototype.decomposeTransform = function() {
    if (!this.transform) {
      return;
    }
    var parent = this.parent;
    var m2 = this.transform;
    if (parent && parent.transform) {
      parent.invTransform = parent.invTransform || create();
      mul(tmpTransform, parent.invTransform, m2);
      m2 = tmpTransform;
    }
    var ox = this.originX;
    var oy = this.originY;
    if (ox || oy) {
      originTransform[4] = ox;
      originTransform[5] = oy;
      mul(tmpTransform, m2, originTransform);
      tmpTransform[4] -= ox;
      tmpTransform[5] -= oy;
      m2 = tmpTransform;
    }
    this.setLocalTransform(m2);
  };
  Transformable2.prototype.getGlobalScale = function(out2) {
    var m2 = this.transform;
    out2 = out2 || [];
    if (!m2) {
      out2[0] = 1;
      out2[1] = 1;
      return out2;
    }
    out2[0] = Math.sqrt(m2[0] * m2[0] + m2[1] * m2[1]);
    out2[1] = Math.sqrt(m2[2] * m2[2] + m2[3] * m2[3]);
    if (m2[0] < 0) {
      out2[0] = -out2[0];
    }
    if (m2[3] < 0) {
      out2[1] = -out2[1];
    }
    return out2;
  };
  Transformable2.prototype.transformCoordToLocal = function(x, y) {
    var v22 = [x, y];
    var invTransform = this.invTransform;
    if (invTransform) {
      applyTransform(v22, v22, invTransform);
    }
    return v22;
  };
  Transformable2.prototype.transformCoordToGlobal = function(x, y) {
    var v22 = [x, y];
    var transform = this.transform;
    if (transform) {
      applyTransform(v22, v22, transform);
    }
    return v22;
  };
  Transformable2.prototype.getLineScale = function() {
    var m2 = this.transform;
    return m2 && abs(m2[0] - 1) > 1e-10 && abs(m2[3] - 1) > 1e-10 ? Math.sqrt(abs(m2[0] * m2[3] - m2[2] * m2[1])) : 1;
  };
  Transformable2.prototype.copyTransform = function(source) {
    copyTransform(this, source);
  };
  Transformable2.getLocalTransform = function(target, m2) {
    m2 = m2 || [];
    var ox = target.originX || 0;
    var oy = target.originY || 0;
    var sx = target.scaleX;
    var sy = target.scaleY;
    var ax = target.anchorX;
    var ay = target.anchorY;
    var rotation = target.rotation || 0;
    var x = target.x;
    var y = target.y;
    var skewX = target.skewX ? Math.tan(target.skewX) : 0;
    var skewY = target.skewY ? Math.tan(-target.skewY) : 0;
    if (ox || oy || ax || ay) {
      var dx = ox + ax;
      var dy = oy + ay;
      m2[4] = -dx * sx - skewX * dy * sy;
      m2[5] = -dy * sy - skewY * dx * sx;
    } else {
      m2[4] = m2[5] = 0;
    }
    m2[0] = sx;
    m2[3] = sy;
    m2[1] = skewY * sx;
    m2[2] = skewX * sy;
    rotation && rotate(m2, m2, rotation);
    m2[4] += ox + x;
    m2[5] += oy + y;
    return m2;
  };
  Transformable2.initDefaultProps = (function() {
    var proto = Transformable2.prototype;
    proto.scaleX = proto.scaleY = proto.globalScaleRatio = 1;
    proto.x = proto.y = proto.originX = proto.originY = proto.skewX = proto.skewY = proto.rotation = proto.anchorX = proto.anchorY = 0;
  })();
  return Transformable2;
})();
var transformableGetLocalTransform = Transformable.getLocalTransform;
function transformableCreate() {
  return new Transformable();
}
var TRANSFORMABLE_PROPS = [
  "x",
  "y",
  "originX",
  "originY",
  "anchorX",
  "anchorY",
  "rotation",
  "scaleX",
  "scaleY",
  "skewX",
  "skewY"
];
function copyTransform(target, source) {
  return assignProps(target, source, TRANSFORMABLE_PROPS);
}
var Transformable_default = Transformable;

// node_modules/zrender/lib/animation/easing.js
var easingFuncs = {
  linear: function(k2) {
    return k2;
  },
  quadraticIn: function(k2) {
    return k2 * k2;
  },
  quadraticOut: function(k2) {
    return k2 * (2 - k2);
  },
  quadraticInOut: function(k2) {
    if ((k2 *= 2) < 1) {
      return 0.5 * k2 * k2;
    }
    return -0.5 * (--k2 * (k2 - 2) - 1);
  },
  cubicIn: function(k2) {
    return k2 * k2 * k2;
  },
  cubicOut: function(k2) {
    return --k2 * k2 * k2 + 1;
  },
  cubicInOut: function(k2) {
    if ((k2 *= 2) < 1) {
      return 0.5 * k2 * k2 * k2;
    }
    return 0.5 * ((k2 -= 2) * k2 * k2 + 2);
  },
  quarticIn: function(k2) {
    return k2 * k2 * k2 * k2;
  },
  quarticOut: function(k2) {
    return 1 - --k2 * k2 * k2 * k2;
  },
  quarticInOut: function(k2) {
    if ((k2 *= 2) < 1) {
      return 0.5 * k2 * k2 * k2 * k2;
    }
    return -0.5 * ((k2 -= 2) * k2 * k2 * k2 - 2);
  },
  quinticIn: function(k2) {
    return k2 * k2 * k2 * k2 * k2;
  },
  quinticOut: function(k2) {
    return --k2 * k2 * k2 * k2 * k2 + 1;
  },
  quinticInOut: function(k2) {
    if ((k2 *= 2) < 1) {
      return 0.5 * k2 * k2 * k2 * k2 * k2;
    }
    return 0.5 * ((k2 -= 2) * k2 * k2 * k2 * k2 + 2);
  },
  sinusoidalIn: function(k2) {
    return 1 - Math.cos(k2 * Math.PI / 2);
  },
  sinusoidalOut: function(k2) {
    return Math.sin(k2 * Math.PI / 2);
  },
  sinusoidalInOut: function(k2) {
    return 0.5 * (1 - Math.cos(Math.PI * k2));
  },
  exponentialIn: function(k2) {
    return k2 === 0 ? 0 : Math.pow(1024, k2 - 1);
  },
  exponentialOut: function(k2) {
    return k2 === 1 ? 1 : 1 - Math.pow(2, -10 * k2);
  },
  exponentialInOut: function(k2) {
    if (k2 === 0) {
      return 0;
    }
    if (k2 === 1) {
      return 1;
    }
    if ((k2 *= 2) < 1) {
      return 0.5 * Math.pow(1024, k2 - 1);
    }
    return 0.5 * (-Math.pow(2, -10 * (k2 - 1)) + 2);
  },
  circularIn: function(k2) {
    return 1 - Math.sqrt(1 - k2 * k2);
  },
  circularOut: function(k2) {
    return Math.sqrt(1 - --k2 * k2);
  },
  circularInOut: function(k2) {
    if ((k2 *= 2) < 1) {
      return -0.5 * (Math.sqrt(1 - k2 * k2) - 1);
    }
    return 0.5 * (Math.sqrt(1 - (k2 -= 2) * k2) + 1);
  },
  elasticIn: function(k2) {
    var s;
    var a = 0.1;
    var p = 0.4;
    if (k2 === 0) {
      return 0;
    }
    if (k2 === 1) {
      return 1;
    }
    if (!a || a < 1) {
      a = 1;
      s = p / 4;
    } else {
      s = p * Math.asin(1 / a) / (2 * Math.PI);
    }
    return -(a * Math.pow(2, 10 * (k2 -= 1)) * Math.sin((k2 - s) * (2 * Math.PI) / p));
  },
  elasticOut: function(k2) {
    var s;
    var a = 0.1;
    var p = 0.4;
    if (k2 === 0) {
      return 0;
    }
    if (k2 === 1) {
      return 1;
    }
    if (!a || a < 1) {
      a = 1;
      s = p / 4;
    } else {
      s = p * Math.asin(1 / a) / (2 * Math.PI);
    }
    return a * Math.pow(2, -10 * k2) * Math.sin((k2 - s) * (2 * Math.PI) / p) + 1;
  },
  elasticInOut: function(k2) {
    var s;
    var a = 0.1;
    var p = 0.4;
    if (k2 === 0) {
      return 0;
    }
    if (k2 === 1) {
      return 1;
    }
    if (!a || a < 1) {
      a = 1;
      s = p / 4;
    } else {
      s = p * Math.asin(1 / a) / (2 * Math.PI);
    }
    if ((k2 *= 2) < 1) {
      return -0.5 * (a * Math.pow(2, 10 * (k2 -= 1)) * Math.sin((k2 - s) * (2 * Math.PI) / p));
    }
    return a * Math.pow(2, -10 * (k2 -= 1)) * Math.sin((k2 - s) * (2 * Math.PI) / p) * 0.5 + 1;
  },
  backIn: function(k2) {
    var s = 1.70158;
    return k2 * k2 * ((s + 1) * k2 - s);
  },
  backOut: function(k2) {
    var s = 1.70158;
    return --k2 * k2 * ((s + 1) * k2 + s) + 1;
  },
  backInOut: function(k2) {
    var s = 1.70158 * 1.525;
    if ((k2 *= 2) < 1) {
      return 0.5 * (k2 * k2 * ((s + 1) * k2 - s));
    }
    return 0.5 * ((k2 -= 2) * k2 * ((s + 1) * k2 + s) + 2);
  },
  bounceIn: function(k2) {
    return 1 - easingFuncs.bounceOut(1 - k2);
  },
  bounceOut: function(k2) {
    if (k2 < 1 / 2.75) {
      return 7.5625 * k2 * k2;
    } else if (k2 < 2 / 2.75) {
      return 7.5625 * (k2 -= 1.5 / 2.75) * k2 + 0.75;
    } else if (k2 < 2.5 / 2.75) {
      return 7.5625 * (k2 -= 2.25 / 2.75) * k2 + 0.9375;
    } else {
      return 7.5625 * (k2 -= 2.625 / 2.75) * k2 + 0.984375;
    }
  },
  bounceInOut: function(k2) {
    if (k2 < 0.5) {
      return easingFuncs.bounceIn(k2 * 2) * 0.5;
    }
    return easingFuncs.bounceOut(k2 * 2 - 1) * 0.5 + 0.5;
  }
};
var easing_default = easingFuncs;

// node_modules/zrender/lib/core/curve.js
var mathPow = Math.pow;
var mathSqrt = Math.sqrt;
var EPSILON3 = 1e-8;
var EPSILON_NUMERIC = 1e-4;
var THREE_SQRT = mathSqrt(3);
var ONE_THIRD = 1 / 3;
var _v0 = create2();
var _v1 = create2();
var _v2 = create2();
function isAroundZero(val) {
  return val > -EPSILON3 && val < EPSILON3;
}
function isNotAroundZero2(val) {
  return val > EPSILON3 || val < -EPSILON3;
}
function cubicAt(p0, p1, p2, p3, t) {
  var onet = 1 - t;
  return onet * onet * (onet * p0 + 3 * t * p1) + t * t * (t * p3 + 3 * onet * p2);
}
function cubicDerivativeAt(p0, p1, p2, p3, t) {
  var onet = 1 - t;
  return 3 * (((p1 - p0) * onet + 2 * (p2 - p1) * t) * onet + (p3 - p2) * t * t);
}
function cubicRootAt(p0, p1, p2, p3, val, roots2) {
  var a = p3 + 3 * (p1 - p2) - p0;
  var b = 3 * (p2 - p1 * 2 + p0);
  var c = 3 * (p1 - p0);
  var d = p0 - val;
  var A2 = b * b - 3 * a * c;
  var B2 = b * c - 9 * a * d;
  var C2 = c * c - 3 * b * d;
  var n = 0;
  if (isAroundZero(A2) && isAroundZero(B2)) {
    if (isAroundZero(b)) {
      roots2[0] = 0;
    } else {
      var t1 = -c / b;
      if (t1 >= 0 && t1 <= 1) {
        roots2[n++] = t1;
      }
    }
  } else {
    var disc = B2 * B2 - 4 * A2 * C2;
    if (isAroundZero(disc)) {
      var K2 = B2 / A2;
      var t1 = -b / a + K2;
      var t2 = -K2 / 2;
      if (t1 >= 0 && t1 <= 1) {
        roots2[n++] = t1;
      }
      if (t2 >= 0 && t2 <= 1) {
        roots2[n++] = t2;
      }
    } else if (disc > 0) {
      var discSqrt = mathSqrt(disc);
      var Y1 = A2 * b + 1.5 * a * (-B2 + discSqrt);
      var Y2 = A2 * b + 1.5 * a * (-B2 - discSqrt);
      if (Y1 < 0) {
        Y1 = -mathPow(-Y1, ONE_THIRD);
      } else {
        Y1 = mathPow(Y1, ONE_THIRD);
      }
      if (Y2 < 0) {
        Y2 = -mathPow(-Y2, ONE_THIRD);
      } else {
        Y2 = mathPow(Y2, ONE_THIRD);
      }
      var t1 = (-b - (Y1 + Y2)) / (3 * a);
      if (t1 >= 0 && t1 <= 1) {
        roots2[n++] = t1;
      }
    } else {
      var T2 = (2 * A2 * b - 3 * a * B2) / (2 * mathSqrt(A2 * A2 * A2));
      var theta = Math.acos(T2) / 3;
      var ASqrt = mathSqrt(A2);
      var tmp = Math.cos(theta);
      var t1 = (-b - 2 * ASqrt * tmp) / (3 * a);
      var t2 = (-b + ASqrt * (tmp + THREE_SQRT * Math.sin(theta))) / (3 * a);
      var t3 = (-b + ASqrt * (tmp - THREE_SQRT * Math.sin(theta))) / (3 * a);
      if (t1 >= 0 && t1 <= 1) {
        roots2[n++] = t1;
      }
      if (t2 >= 0 && t2 <= 1) {
        roots2[n++] = t2;
      }
      if (t3 >= 0 && t3 <= 1) {
        roots2[n++] = t3;
      }
    }
  }
  return n;
}
function cubicExtrema(p0, p1, p2, p3, extrema2) {
  var b = 6 * p2 - 12 * p1 + 6 * p0;
  var a = 9 * p1 + 3 * p3 - 3 * p0 - 9 * p2;
  var c = 3 * p1 - 3 * p0;
  var n = 0;
  if (isAroundZero(a)) {
    if (isNotAroundZero2(b)) {
      var t1 = -c / b;
      if (t1 >= 0 && t1 <= 1) {
        extrema2[n++] = t1;
      }
    }
  } else {
    var disc = b * b - 4 * a * c;
    if (isAroundZero(disc)) {
      extrema2[0] = -b / (2 * a);
    } else if (disc > 0) {
      var discSqrt = mathSqrt(disc);
      var t1 = (-b + discSqrt) / (2 * a);
      var t2 = (-b - discSqrt) / (2 * a);
      if (t1 >= 0 && t1 <= 1) {
        extrema2[n++] = t1;
      }
      if (t2 >= 0 && t2 <= 1) {
        extrema2[n++] = t2;
      }
    }
  }
  return n;
}
function cubicSubdivide(p0, p1, p2, p3, t, out2) {
  var p01 = (p1 - p0) * t + p0;
  var p12 = (p2 - p1) * t + p1;
  var p23 = (p3 - p2) * t + p2;
  var p012 = (p12 - p01) * t + p01;
  var p123 = (p23 - p12) * t + p12;
  var p0123 = (p123 - p012) * t + p012;
  out2[0] = p0;
  out2[1] = p01;
  out2[2] = p012;
  out2[3] = p0123;
  out2[4] = p0123;
  out2[5] = p123;
  out2[6] = p23;
  out2[7] = p3;
}
function cubicProjectPoint(x0, y0, x1, y1, x2, y2, x3, y3, x, y, out2) {
  var t;
  var interval2 = 5e-3;
  var d = Infinity;
  var prev;
  var next;
  var d1;
  var d2;
  _v0[0] = x;
  _v0[1] = y;
  for (var _t = 0; _t < 1; _t += 0.05) {
    _v1[0] = cubicAt(x0, x1, x2, x3, _t);
    _v1[1] = cubicAt(y0, y1, y2, y3, _t);
    d1 = distSquare(_v0, _v1);
    if (d1 < d) {
      t = _t;
      d = d1;
    }
  }
  d = Infinity;
  for (var i2 = 0; i2 < 32; i2++) {
    if (interval2 < EPSILON_NUMERIC) {
      break;
    }
    prev = t - interval2;
    next = t + interval2;
    _v1[0] = cubicAt(x0, x1, x2, x3, prev);
    _v1[1] = cubicAt(y0, y1, y2, y3, prev);
    d1 = distSquare(_v1, _v0);
    if (prev >= 0 && d1 < d) {
      t = prev;
      d = d1;
    } else {
      _v2[0] = cubicAt(x0, x1, x2, x3, next);
      _v2[1] = cubicAt(y0, y1, y2, y3, next);
      d2 = distSquare(_v2, _v0);
      if (next <= 1 && d2 < d) {
        t = next;
        d = d2;
      } else {
        interval2 *= 0.5;
      }
    }
  }
  if (out2) {
    out2[0] = cubicAt(x0, x1, x2, x3, t);
    out2[1] = cubicAt(y0, y1, y2, y3, t);
  }
  return mathSqrt(d);
}
function cubicLength(x0, y0, x1, y1, x2, y2, x3, y3, iteration) {
  var px = x0;
  var py = y0;
  var d = 0;
  var step = 1 / iteration;
  for (var i2 = 1; i2 <= iteration; i2++) {
    var t = i2 * step;
    var x = cubicAt(x0, x1, x2, x3, t);
    var y = cubicAt(y0, y1, y2, y3, t);
    var dx = x - px;
    var dy = y - py;
    d += Math.sqrt(dx * dx + dy * dy);
    px = x;
    py = y;
  }
  return d;
}
function quadraticAt(p0, p1, p2, t) {
  var onet = 1 - t;
  return onet * (onet * p0 + 2 * t * p1) + t * t * p2;
}
function quadraticDerivativeAt(p0, p1, p2, t) {
  return 2 * ((1 - t) * (p1 - p0) + t * (p2 - p1));
}
function quadraticRootAt(p0, p1, p2, val, roots2) {
  var a = p0 - 2 * p1 + p2;
  var b = 2 * (p1 - p0);
  var c = p0 - val;
  var n = 0;
  if (isAroundZero(a)) {
    if (isNotAroundZero2(b)) {
      var t1 = -c / b;
      if (t1 >= 0 && t1 <= 1) {
        roots2[n++] = t1;
      }
    }
  } else {
    var disc = b * b - 4 * a * c;
    if (isAroundZero(disc)) {
      var t1 = -b / (2 * a);
      if (t1 >= 0 && t1 <= 1) {
        roots2[n++] = t1;
      }
    } else if (disc > 0) {
      var discSqrt = mathSqrt(disc);
      var t1 = (-b + discSqrt) / (2 * a);
      var t2 = (-b - discSqrt) / (2 * a);
      if (t1 >= 0 && t1 <= 1) {
        roots2[n++] = t1;
      }
      if (t2 >= 0 && t2 <= 1) {
        roots2[n++] = t2;
      }
    }
  }
  return n;
}
function quadraticExtremum(p0, p1, p2) {
  var divider = p0 + p2 - 2 * p1;
  if (divider === 0) {
    return 0.5;
  } else {
    return (p0 - p1) / divider;
  }
}
function quadraticSubdivide(p0, p1, p2, t, out2) {
  var p01 = (p1 - p0) * t + p0;
  var p12 = (p2 - p1) * t + p1;
  var p012 = (p12 - p01) * t + p01;
  out2[0] = p0;
  out2[1] = p01;
  out2[2] = p012;
  out2[3] = p012;
  out2[4] = p12;
  out2[5] = p2;
}
function quadraticProjectPoint(x0, y0, x1, y1, x2, y2, x, y, out2) {
  var t;
  var interval2 = 5e-3;
  var d = Infinity;
  _v0[0] = x;
  _v0[1] = y;
  for (var _t = 0; _t < 1; _t += 0.05) {
    _v1[0] = quadraticAt(x0, x1, x2, _t);
    _v1[1] = quadraticAt(y0, y1, y2, _t);
    var d1 = distSquare(_v0, _v1);
    if (d1 < d) {
      t = _t;
      d = d1;
    }
  }
  d = Infinity;
  for (var i2 = 0; i2 < 32; i2++) {
    if (interval2 < EPSILON_NUMERIC) {
      break;
    }
    var prev = t - interval2;
    var next = t + interval2;
    _v1[0] = quadraticAt(x0, x1, x2, prev);
    _v1[1] = quadraticAt(y0, y1, y2, prev);
    var d1 = distSquare(_v1, _v0);
    if (prev >= 0 && d1 < d) {
      t = prev;
      d = d1;
    } else {
      _v2[0] = quadraticAt(x0, x1, x2, next);
      _v2[1] = quadraticAt(y0, y1, y2, next);
      var d2 = distSquare(_v2, _v0);
      if (next <= 1 && d2 < d) {
        t = next;
        d = d2;
      } else {
        interval2 *= 0.5;
      }
    }
  }
  if (out2) {
    out2[0] = quadraticAt(x0, x1, x2, t);
    out2[1] = quadraticAt(y0, y1, y2, t);
  }
  return mathSqrt(d);
}
function quadraticLength(x0, y0, x1, y1, x2, y2, iteration) {
  var px = x0;
  var py = y0;
  var d = 0;
  var step = 1 / iteration;
  for (var i2 = 1; i2 <= iteration; i2++) {
    var t = i2 * step;
    var x = quadraticAt(x0, x1, x2, t);
    var y = quadraticAt(y0, y1, y2, t);
    var dx = x - px;
    var dy = y - py;
    d += Math.sqrt(dx * dx + dy * dy);
    px = x;
    py = y;
  }
  return d;
}

// node_modules/zrender/lib/animation/cubicEasing.js
var regexp = /cubic-bezier\(([0-9,\.e ]+)\)/;
function createCubicEasingFunc(cubicEasingStr) {
  var cubic = cubicEasingStr && regexp.exec(cubicEasingStr);
  if (cubic) {
    var points4 = cubic[1].split(",");
    var a_1 = +trim(points4[0]);
    var b_1 = +trim(points4[1]);
    var c_1 = +trim(points4[2]);
    var d_1 = +trim(points4[3]);
    if (isNaN(a_1 + b_1 + c_1 + d_1)) {
      return;
    }
    var roots_1 = [];
    return function(p) {
      return p <= 0 ? 0 : p >= 1 ? 1 : cubicRootAt(0, a_1, c_1, 1, p, roots_1) && cubicAt(0, b_1, d_1, 1, roots_1[0]);
    };
  }
}

// node_modules/zrender/lib/animation/Clip.js
var Clip = (function() {
  function Clip2(opts) {
    this._inited = false;
    this._startTime = 0;
    this._pausedTime = 0;
    this._paused = false;
    this._life = opts.life || 1e3;
    this._delay = opts.delay || 0;
    this.loop = opts.loop || false;
    this.onframe = opts.onframe || noop;
    this.ondestroy = opts.ondestroy || noop;
    this.onrestart = opts.onrestart || noop;
    opts.easing && this.setEasing(opts.easing);
  }
  Clip2.prototype.step = function(globalTime, deltaTime) {
    if (!this._inited) {
      this._startTime = globalTime + this._delay;
      this._inited = true;
    }
    if (this._paused) {
      this._pausedTime += deltaTime;
      return;
    }
    var life = this._life;
    var elapsedTime = globalTime - this._startTime - this._pausedTime;
    var percent = elapsedTime / life;
    if (percent < 0) {
      percent = 0;
    }
    percent = Math.min(percent, 1);
    var easingFunc = this.easingFunc;
    var schedule = easingFunc ? easingFunc(percent) : percent;
    this.onframe(schedule);
    if (percent === 1) {
      if (this.loop) {
        var remainder = elapsedTime % life;
        this._startTime = globalTime - remainder;
        this._pausedTime = 0;
        this.onrestart();
      } else {
        return true;
      }
    }
    return false;
  };
  Clip2.prototype.pause = function() {
    this._paused = true;
  };
  Clip2.prototype.resume = function() {
    this._paused = false;
  };
  Clip2.prototype.setEasing = function(easing) {
    this.easing = easing;
    this.easingFunc = isFunction(easing) ? easing : easing_default[easing] || createCubicEasingFunc(easing);
  };
  return Clip2;
})();
var Clip_default = Clip;

// node_modules/zrender/lib/tool/color.js
var kCSSColorTable = {
  "transparent": [0, 0, 0, 0],
  "aliceblue": [240, 248, 255, 1],
  "antiquewhite": [250, 235, 215, 1],
  "aqua": [0, 255, 255, 1],
  "aquamarine": [127, 255, 212, 1],
  "azure": [240, 255, 255, 1],
  "beige": [245, 245, 220, 1],
  "bisque": [255, 228, 196, 1],
  "black": [0, 0, 0, 1],
  "blanchedalmond": [255, 235, 205, 1],
  "blue": [0, 0, 255, 1],
  "blueviolet": [138, 43, 226, 1],
  "brown": [165, 42, 42, 1],
  "burlywood": [222, 184, 135, 1],
  "cadetblue": [95, 158, 160, 1],
  "chartreuse": [127, 255, 0, 1],
  "chocolate": [210, 105, 30, 1],
  "coral": [255, 127, 80, 1],
  "cornflowerblue": [100, 149, 237, 1],
  "cornsilk": [255, 248, 220, 1],
  "crimson": [220, 20, 60, 1],
  "cyan": [0, 255, 255, 1],
  "darkblue": [0, 0, 139, 1],
  "darkcyan": [0, 139, 139, 1],
  "darkgoldenrod": [184, 134, 11, 1],
  "darkgray": [169, 169, 169, 1],
  "darkgreen": [0, 100, 0, 1],
  "darkgrey": [169, 169, 169, 1],
  "darkkhaki": [189, 183, 107, 1],
  "darkmagenta": [139, 0, 139, 1],
  "darkolivegreen": [85, 107, 47, 1],
  "darkorange": [255, 140, 0, 1],
  "darkorchid": [153, 50, 204, 1],
  "darkred": [139, 0, 0, 1],
  "darksalmon": [233, 150, 122, 1],
  "darkseagreen": [143, 188, 143, 1],
  "darkslateblue": [72, 61, 139, 1],
  "darkslategray": [47, 79, 79, 1],
  "darkslategrey": [47, 79, 79, 1],
  "darkturquoise": [0, 206, 209, 1],
  "darkviolet": [148, 0, 211, 1],
  "deeppink": [255, 20, 147, 1],
  "deepskyblue": [0, 191, 255, 1],
  "dimgray": [105, 105, 105, 1],
  "dimgrey": [105, 105, 105, 1],
  "dodgerblue": [30, 144, 255, 1],
  "firebrick": [178, 34, 34, 1],
  "floralwhite": [255, 250, 240, 1],
  "forestgreen": [34, 139, 34, 1],
  "fuchsia": [255, 0, 255, 1],
  "gainsboro": [220, 220, 220, 1],
  "ghostwhite": [248, 248, 255, 1],
  "gold": [255, 215, 0, 1],
  "goldenrod": [218, 165, 32, 1],
  "gray": [128, 128, 128, 1],
  "green": [0, 128, 0, 1],
  "greenyellow": [173, 255, 47, 1],
  "grey": [128, 128, 128, 1],
  "honeydew": [240, 255, 240, 1],
  "hotpink": [255, 105, 180, 1],
  "indianred": [205, 92, 92, 1],
  "indigo": [75, 0, 130, 1],
  "ivory": [255, 255, 240, 1],
  "khaki": [240, 230, 140, 1],
  "lavender": [230, 230, 250, 1],
  "lavenderblush": [255, 240, 245, 1],
  "lawngreen": [124, 252, 0, 1],
  "lemonchiffon": [255, 250, 205, 1],
  "lightblue": [173, 216, 230, 1],
  "lightcoral": [240, 128, 128, 1],
  "lightcyan": [224, 255, 255, 1],
  "lightgoldenrodyellow": [250, 250, 210, 1],
  "lightgray": [211, 211, 211, 1],
  "lightgreen": [144, 238, 144, 1],
  "lightgrey": [211, 211, 211, 1],
  "lightpink": [255, 182, 193, 1],
  "lightsalmon": [255, 160, 122, 1],
  "lightseagreen": [32, 178, 170, 1],
  "lightskyblue": [135, 206, 250, 1],
  "lightslategray": [119, 136, 153, 1],
  "lightslategrey": [119, 136, 153, 1],
  "lightsteelblue": [176, 196, 222, 1],
  "lightyellow": [255, 255, 224, 1],
  "lime": [0, 255, 0, 1],
  "limegreen": [50, 205, 50, 1],
  "linen": [250, 240, 230, 1],
  "magenta": [255, 0, 255, 1],
  "maroon": [128, 0, 0, 1],
  "mediumaquamarine": [102, 205, 170, 1],
  "mediumblue": [0, 0, 205, 1],
  "mediumorchid": [186, 85, 211, 1],
  "mediumpurple": [147, 112, 219, 1],
  "mediumseagreen": [60, 179, 113, 1],
  "mediumslateblue": [123, 104, 238, 1],
  "mediumspringgreen": [0, 250, 154, 1],
  "mediumturquoise": [72, 209, 204, 1],
  "mediumvioletred": [199, 21, 133, 1],
  "midnightblue": [25, 25, 112, 1],
  "mintcream": [245, 255, 250, 1],
  "mistyrose": [255, 228, 225, 1],
  "moccasin": [255, 228, 181, 1],
  "navajowhite": [255, 222, 173, 1],
  "navy": [0, 0, 128, 1],
  "oldlace": [253, 245, 230, 1],
  "olive": [128, 128, 0, 1],
  "olivedrab": [107, 142, 35, 1],
  "orange": [255, 165, 0, 1],
  "orangered": [255, 69, 0, 1],
  "orchid": [218, 112, 214, 1],
  "palegoldenrod": [238, 232, 170, 1],
  "palegreen": [152, 251, 152, 1],
  "paleturquoise": [175, 238, 238, 1],
  "palevioletred": [219, 112, 147, 1],
  "papayawhip": [255, 239, 213, 1],
  "peachpuff": [255, 218, 185, 1],
  "peru": [205, 133, 63, 1],
  "pink": [255, 192, 203, 1],
  "plum": [221, 160, 221, 1],
  "powderblue": [176, 224, 230, 1],
  "purple": [128, 0, 128, 1],
  "red": [255, 0, 0, 1],
  "rosybrown": [188, 143, 143, 1],
  "royalblue": [65, 105, 225, 1],
  "saddlebrown": [139, 69, 19, 1],
  "salmon": [250, 128, 114, 1],
  "sandybrown": [244, 164, 96, 1],
  "seagreen": [46, 139, 87, 1],
  "seashell": [255, 245, 238, 1],
  "sienna": [160, 82, 45, 1],
  "silver": [192, 192, 192, 1],
  "skyblue": [135, 206, 235, 1],
  "slateblue": [106, 90, 205, 1],
  "slategray": [112, 128, 144, 1],
  "slategrey": [112, 128, 144, 1],
  "snow": [255, 250, 250, 1],
  "springgreen": [0, 255, 127, 1],
  "steelblue": [70, 130, 180, 1],
  "tan": [210, 180, 140, 1],
  "teal": [0, 128, 128, 1],
  "thistle": [216, 191, 216, 1],
  "tomato": [255, 99, 71, 1],
  "turquoise": [64, 224, 208, 1],
  "violet": [238, 130, 238, 1],
  "wheat": [245, 222, 179, 1],
  "white": [255, 255, 255, 1],
  "whitesmoke": [245, 245, 245, 1],
  "yellow": [255, 255, 0, 1],
  "yellowgreen": [154, 205, 50, 1]
};
function clampCssByte(i2) {
  i2 = Math.round(i2);
  return i2 < 0 ? 0 : i2 > 255 ? 255 : i2;
}
function clampCssAngle(i2) {
  i2 = Math.round(i2);
  return i2 < 0 ? 0 : i2 > 360 ? 360 : i2;
}
function clampCssFloat(f) {
  return f < 0 ? 0 : f > 1 ? 1 : f;
}
function parseCssInt(val) {
  var str = val;
  if (str.length && str.charAt(str.length - 1) === "%") {
    return clampCssByte(parseFloat(str) / 100 * 255);
  }
  return clampCssByte(parseInt(str, 10));
}
function parseCssFloat(val) {
  var str = val;
  if (str.length && str.charAt(str.length - 1) === "%") {
    return clampCssFloat(parseFloat(str) / 100);
  }
  return clampCssFloat(parseFloat(str));
}
function cssHueToRgb(m1, m2, h) {
  if (h < 0) {
    h += 1;
  } else if (h > 1) {
    h -= 1;
  }
  if (h * 6 < 1) {
    return m1 + (m2 - m1) * h * 6;
  }
  if (h * 2 < 1) {
    return m2;
  }
  if (h * 3 < 2) {
    return m1 + (m2 - m1) * (2 / 3 - h) * 6;
  }
  return m1;
}
function lerpNumber(a, b, p) {
  return a + (b - a) * p;
}
function setRgba(out2, r, g, b, a) {
  out2[0] = r;
  out2[1] = g;
  out2[2] = b;
  out2[3] = a;
  return out2;
}
function copyRgba(out2, a) {
  out2[0] = a[0];
  out2[1] = a[1];
  out2[2] = a[2];
  out2[3] = a[3];
  return out2;
}
var colorCache = new LRU_default(20);
var lastRemovedArr = null;
function putToCache(colorStr, rgbaArr) {
  if (lastRemovedArr) {
    copyRgba(lastRemovedArr, rgbaArr);
  }
  lastRemovedArr = colorCache.put(colorStr, lastRemovedArr || rgbaArr.slice());
}
function parse(colorStr, rgbaArr) {
  if (!colorStr) {
    return;
  }
  rgbaArr = rgbaArr || [];
  var cached = colorCache.get(colorStr);
  if (cached) {
    return copyRgba(rgbaArr, cached);
  }
  colorStr = colorStr + "";
  var str = colorStr.replace(/ /g, "").toLowerCase();
  if (str in kCSSColorTable) {
    copyRgba(rgbaArr, kCSSColorTable[str]);
    putToCache(colorStr, rgbaArr);
    return rgbaArr;
  }
  var strLen = str.length;
  if (str.charAt(0) === "#") {
    if (strLen === 4 || strLen === 5) {
      var iv = parseInt(str.slice(1, 4), 16);
      if (!(iv >= 0 && iv <= 4095)) {
        setRgba(rgbaArr, 0, 0, 0, 1);
        return;
      }
      setRgba(rgbaArr, (iv & 3840) >> 4 | (iv & 3840) >> 8, iv & 240 | (iv & 240) >> 4, iv & 15 | (iv & 15) << 4, strLen === 5 ? parseInt(str.slice(4), 16) / 15 : 1);
      putToCache(colorStr, rgbaArr);
      return rgbaArr;
    } else if (strLen === 7 || strLen === 9) {
      var iv = parseInt(str.slice(1, 7), 16);
      if (!(iv >= 0 && iv <= 16777215)) {
        setRgba(rgbaArr, 0, 0, 0, 1);
        return;
      }
      setRgba(rgbaArr, (iv & 16711680) >> 16, (iv & 65280) >> 8, iv & 255, strLen === 9 ? parseInt(str.slice(7), 16) / 255 : 1);
      putToCache(colorStr, rgbaArr);
      return rgbaArr;
    }
    return;
  }
  var op = str.indexOf("(");
  var ep = str.indexOf(")");
  if (op !== -1 && ep + 1 === strLen) {
    var fname = str.substr(0, op);
    var params = str.substr(op + 1, ep - (op + 1)).split(",");
    var alpha = 1;
    switch (fname) {
      case "rgba":
        if (params.length !== 4) {
          return params.length === 3 ? setRgba(rgbaArr, +params[0], +params[1], +params[2], 1) : setRgba(rgbaArr, 0, 0, 0, 1);
        }
        alpha = parseCssFloat(params.pop());
      case "rgb":
        if (params.length >= 3) {
          setRgba(rgbaArr, parseCssInt(params[0]), parseCssInt(params[1]), parseCssInt(params[2]), params.length === 3 ? alpha : parseCssFloat(params[3]));
          putToCache(colorStr, rgbaArr);
          return rgbaArr;
        } else {
          setRgba(rgbaArr, 0, 0, 0, 1);
          return;
        }
      case "hsla":
        if (params.length !== 4) {
          setRgba(rgbaArr, 0, 0, 0, 1);
          return;
        }
        params[3] = parseCssFloat(params[3]);
        hsla2rgba(params, rgbaArr);
        putToCache(colorStr, rgbaArr);
        return rgbaArr;
      case "hsl":
        if (params.length !== 3) {
          setRgba(rgbaArr, 0, 0, 0, 1);
          return;
        }
        hsla2rgba(params, rgbaArr);
        putToCache(colorStr, rgbaArr);
        return rgbaArr;
      default:
        return;
    }
  }
  setRgba(rgbaArr, 0, 0, 0, 1);
  return;
}
function hsla2rgba(hsla, rgba) {
  var h = (parseFloat(hsla[0]) % 360 + 360) % 360 / 360;
  var s = parseCssFloat(hsla[1]);
  var l = parseCssFloat(hsla[2]);
  var m2 = l <= 0.5 ? l * (s + 1) : l + s - l * s;
  var m1 = l * 2 - m2;
  rgba = rgba || [];
  setRgba(rgba, clampCssByte(cssHueToRgb(m1, m2, h + 1 / 3) * 255), clampCssByte(cssHueToRgb(m1, m2, h) * 255), clampCssByte(cssHueToRgb(m1, m2, h - 1 / 3) * 255), 1);
  if (hsla.length === 4) {
    rgba[3] = hsla[3];
  }
  return rgba;
}
function rgba2hsla(rgba) {
  if (!rgba) {
    return;
  }
  var R2 = rgba[0] / 255;
  var G2 = rgba[1] / 255;
  var B2 = rgba[2] / 255;
  var vMin = Math.min(R2, G2, B2);
  var vMax = Math.max(R2, G2, B2);
  var delta = vMax - vMin;
  var L2 = (vMax + vMin) / 2;
  var H2;
  var S2;
  if (delta === 0) {
    H2 = 0;
    S2 = 0;
  } else {
    if (L2 < 0.5) {
      S2 = delta / (vMax + vMin);
    } else {
      S2 = delta / (2 - vMax - vMin);
    }
    var deltaR = ((vMax - R2) / 6 + delta / 2) / delta;
    var deltaG = ((vMax - G2) / 6 + delta / 2) / delta;
    var deltaB = ((vMax - B2) / 6 + delta / 2) / delta;
    if (R2 === vMax) {
      H2 = deltaB - deltaG;
    } else if (G2 === vMax) {
      H2 = 1 / 3 + deltaR - deltaB;
    } else if (B2 === vMax) {
      H2 = 2 / 3 + deltaG - deltaR;
    }
    if (H2 < 0) {
      H2 += 1;
    }
    if (H2 > 1) {
      H2 -= 1;
    }
  }
  var hsla = [H2 * 360, S2, L2];
  if (rgba[3] != null) {
    hsla.push(rgba[3]);
  }
  return hsla;
}
function lift(color3, level) {
  var colorArr = parse(color3);
  if (colorArr) {
    for (var i2 = 0; i2 < 3; i2++) {
      if (level < 0) {
        colorArr[i2] = colorArr[i2] * (1 - level) | 0;
      } else {
        colorArr[i2] = (255 - colorArr[i2]) * level + colorArr[i2] | 0;
      }
      if (colorArr[i2] > 255) {
        colorArr[i2] = 255;
      } else if (colorArr[i2] < 0) {
        colorArr[i2] = 0;
      }
    }
    return stringify(colorArr, colorArr.length === 4 ? "rgba" : "rgb");
  }
}
function fastLerp(normalizedValue, colors2, out2) {
  if (!(colors2 && colors2.length) || !(normalizedValue >= 0 && normalizedValue <= 1)) {
    return;
  }
  out2 = out2 || [];
  var value = normalizedValue * (colors2.length - 1);
  var leftIndex = Math.floor(value);
  var rightIndex = Math.ceil(value);
  var leftColor = colors2[leftIndex];
  var rightColor = colors2[rightIndex];
  var dv = value - leftIndex;
  out2[0] = clampCssByte(lerpNumber(leftColor[0], rightColor[0], dv));
  out2[1] = clampCssByte(lerpNumber(leftColor[1], rightColor[1], dv));
  out2[2] = clampCssByte(lerpNumber(leftColor[2], rightColor[2], dv));
  out2[3] = clampCssFloat(lerpNumber(leftColor[3], rightColor[3], dv));
  return out2;
}
function lerp2(normalizedValue, colors2, fullOutput) {
  if (!(colors2 && colors2.length) || !(normalizedValue >= 0 && normalizedValue <= 1)) {
    return;
  }
  var value = normalizedValue * (colors2.length - 1);
  var leftIndex = Math.floor(value);
  var rightIndex = Math.ceil(value);
  var leftColor = parse(colors2[leftIndex]);
  var rightColor = parse(colors2[rightIndex]);
  var dv = value - leftIndex;
  var color3 = stringify([
    clampCssByte(lerpNumber(leftColor[0], rightColor[0], dv)),
    clampCssByte(lerpNumber(leftColor[1], rightColor[1], dv)),
    clampCssByte(lerpNumber(leftColor[2], rightColor[2], dv)),
    clampCssFloat(lerpNumber(leftColor[3], rightColor[3], dv))
  ], "rgba");
  return fullOutput ? {
    color: color3,
    leftIndex,
    rightIndex,
    value
  } : color3;
}
function modifyHSL(color3, h, s, l) {
  var colorArr = parse(color3);
  if (color3) {
    colorArr = rgba2hsla(colorArr);
    h != null && (colorArr[0] = clampCssAngle(isFunction(h) ? h(colorArr[0]) : h));
    s != null && (colorArr[1] = parseCssFloat(isFunction(s) ? s(colorArr[1]) : s));
    l != null && (colorArr[2] = parseCssFloat(isFunction(l) ? l(colorArr[2]) : l));
    return stringify(hsla2rgba(colorArr), "rgba");
  }
}
function modifyAlpha(color3, alpha) {
  var colorArr = parse(color3);
  if (colorArr && alpha != null) {
    colorArr[3] = clampCssFloat(alpha);
    return stringify(colorArr, "rgba");
  }
}
function stringify(arrColor, type) {
  if (!arrColor || !arrColor.length) {
    return;
  }
  var colorStr = arrColor[0] + "," + arrColor[1] + "," + arrColor[2];
  if (type === "rgba" || type === "hsva" || type === "hsla") {
    colorStr += "," + arrColor[3];
  }
  return type + "(" + colorStr + ")";
}
function lum(color3, backgroundLum) {
  var arr = parse(color3);
  return arr ? (0.299 * arr[0] + 0.587 * arr[1] + 0.114 * arr[2]) * arr[3] / 255 + (1 - arr[3]) * backgroundLum : 0;
}
var liftedColorCache = new LRU_default(100);
function liftColor(color3) {
  if (isString(color3)) {
    var liftedColor = liftedColorCache.get(color3);
    if (!liftedColor) {
      liftedColor = lift(color3, -0.1);
      liftedColorCache.put(color3, liftedColor);
    }
    return liftedColor;
  } else if (isGradientObject(color3)) {
    var ret = extend({}, color3);
    ret.colorStops = map2(color3.colorStops, function(stop2) {
      return {
        offset: stop2.offset,
        color: lift(stop2.color, -0.1)
      };
    });
    return ret;
  }
  return color3;
}

// node_modules/zrender/lib/svg/helper.js
var mathRound = Math.round;
function normalizeColor(color3) {
  var opacity;
  if (!color3 || color3 === "transparent") {
    color3 = "none";
  } else if (typeof color3 === "string" && color3.indexOf("rgba") > -1) {
    var arr = parse(color3);
    if (arr) {
      color3 = "rgb(" + arr[0] + "," + arr[1] + "," + arr[2] + ")";
      opacity = arr[3];
    }
  }
  return {
    color: color3,
    opacity: opacity == null ? 1 : opacity
  };
}
var EPSILON4 = 1e-4;
function isAroundZero2(transform) {
  return transform < EPSILON4 && transform > -EPSILON4;
}
function round3(transform) {
  return mathRound(transform * 1e3) / 1e3;
}
function round4(transform) {
  return mathRound(transform * 1e4) / 1e4;
}
function getMatrixStr(m2) {
  return "matrix(" + round3(m2[0]) + "," + round3(m2[1]) + "," + round3(m2[2]) + "," + round3(m2[3]) + "," + round4(m2[4]) + "," + round4(m2[5]) + ")";
}
var TEXT_ALIGN_TO_ANCHOR = {
  left: "start",
  right: "end",
  center: "middle",
  middle: "middle"
};
function adjustTextY2(y, lineHeight, textBaseline) {
  if (textBaseline === "top") {
    y += lineHeight / 2;
  } else if (textBaseline === "bottom") {
    y -= lineHeight / 2;
  }
  return y;
}
function hasShadow(style2) {
  return style2 && (style2.shadowBlur || style2.shadowOffsetX || style2.shadowOffsetY);
}
function getShadowKey(displayable) {
  var style2 = displayable.style;
  var globalScale = displayable.getGlobalScale();
  return [
    style2.shadowColor,
    (style2.shadowBlur || 0).toFixed(2),
    (style2.shadowOffsetX || 0).toFixed(2),
    (style2.shadowOffsetY || 0).toFixed(2),
    globalScale[0],
    globalScale[1]
  ].join(",");
}
function isImagePattern(val) {
  return val && !!val.image;
}
function isSVGPattern(val) {
  return val && !!val.svgElement;
}
function isPattern(val) {
  return isImagePattern(val) || isSVGPattern(val);
}
function isLinearGradient(val) {
  return val.type === "linear";
}
function isRadialGradient(val) {
  return val.type === "radial";
}
function isGradient(val) {
  return val && (val.type === "linear" || val.type === "radial");
}
function getIdURL(id) {
  return "url(#" + id + ")";
}
function getPathPrecision(el) {
  var scale3 = el.getGlobalScale();
  var size = Math.max(scale3[0], scale3[1]);
  return Math.max(Math.ceil(Math.log(size) / Math.log(10)), 1);
}
function getSRTTransformString(transform) {
  var x = transform.x || 0;
  var y = transform.y || 0;
  var rotation = (transform.rotation || 0) * RADIAN_TO_DEGREE;
  var scaleX = retrieve2(transform.scaleX, 1);
  var scaleY = retrieve2(transform.scaleY, 1);
  var skewX = transform.skewX || 0;
  var skewY = transform.skewY || 0;
  var res = [];
  if (x || y) {
    res.push("translate(" + x + "px," + y + "px)");
  }
  if (rotation) {
    res.push("rotate(" + rotation + ")");
  }
  if (scaleX !== 1 || scaleY !== 1) {
    res.push("scale(" + scaleX + "," + scaleY + ")");
  }
  if (skewX || skewY) {
    res.push("skew(" + mathRound(skewX * RADIAN_TO_DEGREE) + "deg, " + mathRound(skewY * RADIAN_TO_DEGREE) + "deg)");
  }
  return res.join(" ");
}
var encodeBase64 = (function() {
  if (typeof Buffer !== "undefined" && typeof Buffer.from === "function") {
    return function(str) {
      return Buffer.from(str).toString("base64");
    };
  }
  if (typeof btoa === "function" && typeof unescape === "function" && typeof encodeURIComponent === "function") {
    return function(str) {
      return btoa(unescape(encodeURIComponent(str)));
    };
  }
  return function(str) {
    if (true) {
      logError("Base64 isn't natively supported in the current environment.");
    }
    return null;
  };
})();

// node_modules/zrender/lib/animation/Animator.js
var arraySlice = Array.prototype.slice;
function interpolateNumber(p0, p1, percent) {
  return (p1 - p0) * percent + p0;
}
function interpolate1DArray(out2, p0, p1, percent) {
  var len2 = p0.length;
  for (var i2 = 0; i2 < len2; i2++) {
    out2[i2] = interpolateNumber(p0[i2], p1[i2], percent);
  }
  return out2;
}
function interpolate2DArray(out2, p0, p1, percent) {
  var len2 = p0.length;
  var len22 = len2 && p0[0].length;
  for (var i2 = 0; i2 < len2; i2++) {
    if (!out2[i2]) {
      out2[i2] = [];
    }
    for (var j = 0; j < len22; j++) {
      out2[i2][j] = interpolateNumber(p0[i2][j], p1[i2][j], percent);
    }
  }
  return out2;
}
function add1DArray(out2, p0, p1, sign) {
  var len2 = p0.length;
  for (var i2 = 0; i2 < len2; i2++) {
    out2[i2] = p0[i2] + p1[i2] * sign;
  }
  return out2;
}
function add2DArray(out2, p0, p1, sign) {
  var len2 = p0.length;
  var len22 = len2 && p0[0].length;
  for (var i2 = 0; i2 < len2; i2++) {
    if (!out2[i2]) {
      out2[i2] = [];
    }
    for (var j = 0; j < len22; j++) {
      out2[i2][j] = p0[i2][j] + p1[i2][j] * sign;
    }
  }
  return out2;
}
function fillColorStops(val0, val1) {
  var len0 = val0.length;
  var len1 = val1.length;
  var shorterArr = len0 > len1 ? val1 : val0;
  var shorterLen = Math.min(len0, len1);
  var last = shorterArr[shorterLen - 1] || { color: [0, 0, 0, 0], offset: 0 };
  for (var i2 = shorterLen; i2 < Math.max(len0, len1); i2++) {
    shorterArr.push({
      offset: last.offset,
      color: last.color.slice()
    });
  }
}
function fillArray(val0, val1, arrDim) {
  var arr0 = val0;
  var arr1 = val1;
  if (!arr0.push || !arr1.push) {
    return;
  }
  var arr0Len = arr0.length;
  var arr1Len = arr1.length;
  if (arr0Len !== arr1Len) {
    var isPreviousLarger = arr0Len > arr1Len;
    if (isPreviousLarger) {
      arr0.length = arr1Len;
    } else {
      for (var i2 = arr0Len; i2 < arr1Len; i2++) {
        arr0.push(arrDim === 1 ? arr1[i2] : arraySlice.call(arr1[i2]));
      }
    }
  }
  var len2 = arr0[0] && arr0[0].length;
  for (var i2 = 0; i2 < arr0.length; i2++) {
    if (arrDim === 1) {
      if (isNaN(arr0[i2])) {
        arr0[i2] = arr1[i2];
      }
    } else {
      for (var j = 0; j < len2; j++) {
        if (isNaN(arr0[i2][j])) {
          arr0[i2][j] = arr1[i2][j];
        }
      }
    }
  }
}
function cloneValue(value) {
  if (isArrayLike(value)) {
    var len2 = value.length;
    if (isArrayLike(value[0])) {
      var ret = [];
      for (var i2 = 0; i2 < len2; i2++) {
        ret.push(arraySlice.call(value[i2]));
      }
      return ret;
    }
    return arraySlice.call(value);
  }
  return value;
}
function rgba2String(rgba) {
  rgba[0] = Math.floor(rgba[0]) || 0;
  rgba[1] = Math.floor(rgba[1]) || 0;
  rgba[2] = Math.floor(rgba[2]) || 0;
  rgba[3] = rgba[3] == null ? 1 : rgba[3];
  return "rgba(" + rgba.join(",") + ")";
}
function guessArrayDim(value) {
  return isArrayLike(value && value[0]) ? 2 : 1;
}
var VALUE_TYPE_NUMBER = 0;
var VALUE_TYPE_1D_ARRAY = 1;
var VALUE_TYPE_2D_ARRAY = 2;
var VALUE_TYPE_COLOR = 3;
var VALUE_TYPE_LINEAR_GRADIENT = 4;
var VALUE_TYPE_RADIAL_GRADIENT = 5;
var VALUE_TYPE_UNKOWN = 6;
function isGradientValueType(valType) {
  return valType === VALUE_TYPE_LINEAR_GRADIENT || valType === VALUE_TYPE_RADIAL_GRADIENT;
}
function isArrayValueType(valType) {
  return valType === VALUE_TYPE_1D_ARRAY || valType === VALUE_TYPE_2D_ARRAY;
}
var tmpRgba = [0, 0, 0, 0];
var Track = (function() {
  function Track2(propName) {
    this.keyframes = [];
    this.discrete = false;
    this._invalid = false;
    this._needsSort = false;
    this._lastFr = 0;
    this._lastFrP = 0;
    this.propName = propName;
  }
  Track2.prototype.isFinished = function() {
    return this._finished;
  };
  Track2.prototype.setFinished = function() {
    this._finished = true;
    if (this._additiveTrack) {
      this._additiveTrack.setFinished();
    }
  };
  Track2.prototype.needsAnimate = function() {
    return this.keyframes.length >= 1;
  };
  Track2.prototype.getAdditiveTrack = function() {
    return this._additiveTrack;
  };
  Track2.prototype.addKeyframe = function(time, rawValue, easing) {
    this._needsSort = true;
    var keyframes2 = this.keyframes;
    var len2 = keyframes2.length;
    var discrete = false;
    var valType = VALUE_TYPE_UNKOWN;
    var value = rawValue;
    if (isArrayLike(rawValue)) {
      var arrayDim = guessArrayDim(rawValue);
      valType = arrayDim;
      if (arrayDim === 1 && !isNumber(rawValue[0]) || arrayDim === 2 && !isNumber(rawValue[0][0])) {
        discrete = true;
      }
    } else {
      if (isNumber(rawValue) && !eqNaN(rawValue)) {
        valType = VALUE_TYPE_NUMBER;
      } else if (isString(rawValue)) {
        if (!isNaN(+rawValue)) {
          valType = VALUE_TYPE_NUMBER;
        } else {
          var colorArray = parse(rawValue);
          if (colorArray) {
            value = colorArray;
            valType = VALUE_TYPE_COLOR;
          }
        }
      } else if (isGradientObject(rawValue)) {
        var parsedGradient = extend({}, value);
        parsedGradient.colorStops = map2(rawValue.colorStops, function(colorStop) {
          return {
            offset: colorStop.offset,
            color: parse(colorStop.color)
          };
        });
        if (isLinearGradient(rawValue)) {
          valType = VALUE_TYPE_LINEAR_GRADIENT;
        } else if (isRadialGradient(rawValue)) {
          valType = VALUE_TYPE_RADIAL_GRADIENT;
        }
        value = parsedGradient;
      }
    }
    if (len2 === 0) {
      this.valType = valType;
    } else if (valType !== this.valType || valType === VALUE_TYPE_UNKOWN) {
      discrete = true;
    }
    this.discrete = this.discrete || discrete;
    var kf = {
      time,
      value,
      rawValue,
      percent: 0
    };
    if (easing) {
      kf.easing = easing;
      kf.easingFunc = isFunction(easing) ? easing : easing_default[easing] || createCubicEasingFunc(easing);
    }
    keyframes2.push(kf);
    return kf;
  };
  Track2.prototype.prepare = function(maxTime, additiveTrack) {
    var kfs = this.keyframes;
    if (this._needsSort) {
      kfs.sort(function(a, b) {
        return a.time - b.time;
      });
    }
    var valType = this.valType;
    var kfsLen = kfs.length;
    var lastKf = kfs[kfsLen - 1];
    var isDiscrete = this.discrete;
    var isArr = isArrayValueType(valType);
    var isGradient2 = isGradientValueType(valType);
    for (var i2 = 0; i2 < kfsLen; i2++) {
      var kf = kfs[i2];
      var value = kf.value;
      var lastValue = lastKf.value;
      kf.percent = kf.time / maxTime;
      if (!isDiscrete) {
        if (isArr && i2 !== kfsLen - 1) {
          fillArray(value, lastValue, valType);
        } else if (isGradient2) {
          fillColorStops(value.colorStops, lastValue.colorStops);
        }
      }
    }
    if (!isDiscrete && valType !== VALUE_TYPE_RADIAL_GRADIENT && additiveTrack && this.needsAnimate() && additiveTrack.needsAnimate() && valType === additiveTrack.valType && !additiveTrack._finished) {
      this._additiveTrack = additiveTrack;
      var startValue = kfs[0].value;
      for (var i2 = 0; i2 < kfsLen; i2++) {
        if (valType === VALUE_TYPE_NUMBER) {
          kfs[i2].additiveValue = kfs[i2].value - startValue;
        } else if (valType === VALUE_TYPE_COLOR) {
          kfs[i2].additiveValue = add1DArray([], kfs[i2].value, startValue, -1);
        } else if (isArrayValueType(valType)) {
          kfs[i2].additiveValue = valType === VALUE_TYPE_1D_ARRAY ? add1DArray([], kfs[i2].value, startValue, -1) : add2DArray([], kfs[i2].value, startValue, -1);
        }
      }
    }
  };
  Track2.prototype.step = function(target, percent) {
    if (this._finished) {
      return;
    }
    if (this._additiveTrack && this._additiveTrack._finished) {
      this._additiveTrack = null;
    }
    var isAdditive = this._additiveTrack != null;
    var valueKey = isAdditive ? "additiveValue" : "value";
    var valType = this.valType;
    var keyframes2 = this.keyframes;
    var kfsNum = keyframes2.length;
    var propName = this.propName;
    var isValueColor = valType === VALUE_TYPE_COLOR;
    var frameIdx;
    var lastFrame = this._lastFr;
    var mathMin11 = Math.min;
    var frame;
    var nextFrame;
    if (kfsNum === 1) {
      frame = nextFrame = keyframes2[0];
    } else {
      if (percent < 0) {
        frameIdx = 0;
      } else if (percent < this._lastFrP) {
        var start2 = mathMin11(lastFrame + 1, kfsNum - 1);
        for (frameIdx = start2; frameIdx >= 0; frameIdx--) {
          if (keyframes2[frameIdx].percent <= percent) {
            break;
          }
        }
        frameIdx = mathMin11(frameIdx, kfsNum - 2);
      } else {
        for (frameIdx = lastFrame; frameIdx < kfsNum; frameIdx++) {
          if (keyframes2[frameIdx].percent > percent) {
            break;
          }
        }
        frameIdx = mathMin11(frameIdx - 1, kfsNum - 2);
      }
      nextFrame = keyframes2[frameIdx + 1];
      frame = keyframes2[frameIdx];
    }
    if (!(frame && nextFrame)) {
      return;
    }
    this._lastFr = frameIdx;
    this._lastFrP = percent;
    var interval2 = nextFrame.percent - frame.percent;
    var w = interval2 === 0 ? 1 : mathMin11((percent - frame.percent) / interval2, 1);
    if (nextFrame.easingFunc) {
      w = nextFrame.easingFunc(w);
    }
    var targetArr = isAdditive ? this._additiveValue : isValueColor ? tmpRgba : target[propName];
    if ((isArrayValueType(valType) || isValueColor) && !targetArr) {
      targetArr = this._additiveValue = [];
    }
    if (this.discrete) {
      target[propName] = w < 1 ? frame.rawValue : nextFrame.rawValue;
    } else if (isArrayValueType(valType)) {
      valType === VALUE_TYPE_1D_ARRAY ? interpolate1DArray(targetArr, frame[valueKey], nextFrame[valueKey], w) : interpolate2DArray(targetArr, frame[valueKey], nextFrame[valueKey], w);
    } else if (isGradientValueType(valType)) {
      var val = frame[valueKey];
      var nextVal_1 = nextFrame[valueKey];
      var isLinearGradient_1 = valType === VALUE_TYPE_LINEAR_GRADIENT;
      target[propName] = {
        type: isLinearGradient_1 ? "linear" : "radial",
        x: interpolateNumber(val.x, nextVal_1.x, w),
        y: interpolateNumber(val.y, nextVal_1.y, w),
        colorStops: map2(val.colorStops, function(colorStop, idx) {
          var nextColorStop = nextVal_1.colorStops[idx];
          return {
            offset: interpolateNumber(colorStop.offset, nextColorStop.offset, w),
            color: rgba2String(interpolate1DArray([], colorStop.color, nextColorStop.color, w))
          };
        }),
        global: nextVal_1.global
      };
      if (isLinearGradient_1) {
        target[propName].x2 = interpolateNumber(val.x2, nextVal_1.x2, w);
        target[propName].y2 = interpolateNumber(val.y2, nextVal_1.y2, w);
      } else {
        target[propName].r = interpolateNumber(val.r, nextVal_1.r, w);
      }
    } else if (isValueColor) {
      interpolate1DArray(targetArr, frame[valueKey], nextFrame[valueKey], w);
      if (!isAdditive) {
        target[propName] = rgba2String(targetArr);
      }
    } else {
      var value = interpolateNumber(frame[valueKey], nextFrame[valueKey], w);
      if (isAdditive) {
        this._additiveValue = value;
      } else {
        target[propName] = value;
      }
    }
    if (isAdditive) {
      this._addToTarget(target);
    }
  };
  Track2.prototype._addToTarget = function(target) {
    var valType = this.valType;
    var propName = this.propName;
    var additiveValue = this._additiveValue;
    if (valType === VALUE_TYPE_NUMBER) {
      target[propName] = target[propName] + additiveValue;
    } else if (valType === VALUE_TYPE_COLOR) {
      parse(target[propName], tmpRgba);
      add1DArray(tmpRgba, tmpRgba, additiveValue, 1);
      target[propName] = rgba2String(tmpRgba);
    } else if (valType === VALUE_TYPE_1D_ARRAY) {
      add1DArray(target[propName], target[propName], additiveValue, 1);
    } else if (valType === VALUE_TYPE_2D_ARRAY) {
      add2DArray(target[propName], target[propName], additiveValue, 1);
    }
  };
  return Track2;
})();
var Animator = (function() {
  function Animator2(target, loop, allowDiscreteAnimation, additiveTo) {
    this._tracks = {};
    this._trackKeys = [];
    this._maxTime = 0;
    this._started = 0;
    this._clip = null;
    this._target = target;
    this._loop = loop;
    if (loop && additiveTo) {
      logError("Can' use additive animation on looped animation.");
      return;
    }
    this._additiveAnimators = additiveTo;
    this._allowDiscrete = allowDiscreteAnimation;
  }
  Animator2.prototype.getMaxTime = function() {
    return this._maxTime;
  };
  Animator2.prototype.getDelay = function() {
    return this._delay;
  };
  Animator2.prototype.getLoop = function() {
    return this._loop;
  };
  Animator2.prototype.getTarget = function() {
    return this._target;
  };
  Animator2.prototype.changeTarget = function(target) {
    this._target = target;
  };
  Animator2.prototype.when = function(time, props, easing) {
    return this.whenWithKeys(time, props, keys(props), easing);
  };
  Animator2.prototype.whenWithKeys = function(time, props, propNames, easing) {
    var tracks = this._tracks;
    for (var i2 = 0; i2 < propNames.length; i2++) {
      var propName = propNames[i2];
      var track = tracks[propName];
      if (!track) {
        track = tracks[propName] = new Track(propName);
        var initialValue = void 0;
        var additiveTrack = this._getAdditiveTrack(propName);
        if (additiveTrack) {
          var addtiveTrackKfs = additiveTrack.keyframes;
          var lastFinalKf = addtiveTrackKfs[addtiveTrackKfs.length - 1];
          initialValue = lastFinalKf && lastFinalKf.value;
          if (additiveTrack.valType === VALUE_TYPE_COLOR && initialValue) {
            initialValue = rgba2String(initialValue);
          }
        } else {
          initialValue = this._target[propName];
        }
        if (initialValue == null) {
          continue;
        }
        if (time > 0) {
          track.addKeyframe(0, cloneValue(initialValue), easing);
        }
        this._trackKeys.push(propName);
      }
      track.addKeyframe(time, cloneValue(props[propName]), easing);
    }
    this._maxTime = Math.max(this._maxTime, time);
    return this;
  };
  Animator2.prototype.pause = function() {
    this._clip.pause();
    this._paused = true;
  };
  Animator2.prototype.resume = function() {
    this._clip.resume();
    this._paused = false;
  };
  Animator2.prototype.isPaused = function() {
    return !!this._paused;
  };
  Animator2.prototype.duration = function(duration) {
    this._maxTime = duration;
    this._force = true;
    return this;
  };
  Animator2.prototype._doneCallback = function() {
    this._setTracksFinished();
    this._clip = null;
    var doneList = this._doneCbs;
    if (doneList) {
      var len2 = doneList.length;
      for (var i2 = 0; i2 < len2; i2++) {
        doneList[i2].call(this);
      }
    }
  };
  Animator2.prototype._abortedCallback = function() {
    this._setTracksFinished();
    var animation2 = this.animation;
    var abortedList = this._abortedCbs;
    if (animation2) {
      animation2.removeClip(this._clip);
    }
    this._clip = null;
    if (abortedList) {
      for (var i2 = 0; i2 < abortedList.length; i2++) {
        abortedList[i2].call(this);
      }
    }
  };
  Animator2.prototype._setTracksFinished = function() {
    var tracks = this._tracks;
    var tracksKeys = this._trackKeys;
    for (var i2 = 0; i2 < tracksKeys.length; i2++) {
      tracks[tracksKeys[i2]].setFinished();
    }
  };
  Animator2.prototype._getAdditiveTrack = function(trackName) {
    var additiveTrack;
    var additiveAnimators = this._additiveAnimators;
    if (additiveAnimators) {
      for (var i2 = 0; i2 < additiveAnimators.length; i2++) {
        var track = additiveAnimators[i2].getTrack(trackName);
        if (track) {
          additiveTrack = track;
        }
      }
    }
    return additiveTrack;
  };
  Animator2.prototype.start = function(easing) {
    if (this._started > 0) {
      return;
    }
    this._started = 1;
    var self2 = this;
    var tracks = [];
    var maxTime = this._maxTime || 0;
    for (var i2 = 0; i2 < this._trackKeys.length; i2++) {
      var propName = this._trackKeys[i2];
      var track = this._tracks[propName];
      var additiveTrack = this._getAdditiveTrack(propName);
      var kfs = track.keyframes;
      var kfsNum = kfs.length;
      track.prepare(maxTime, additiveTrack);
      if (track.needsAnimate()) {
        if (!this._allowDiscrete && track.discrete) {
          var lastKf = kfs[kfsNum - 1];
          if (lastKf) {
            self2._target[track.propName] = lastKf.rawValue;
          }
          track.setFinished();
        } else {
          tracks.push(track);
        }
      }
    }
    if (tracks.length || this._force) {
      var clip2 = new Clip_default({
        life: maxTime,
        loop: this._loop,
        delay: this._delay || 0,
        onframe: function(percent) {
          self2._started = 2;
          var additiveAnimators = self2._additiveAnimators;
          if (additiveAnimators) {
            var stillHasAdditiveAnimator = false;
            for (var i3 = 0; i3 < additiveAnimators.length; i3++) {
              if (additiveAnimators[i3]._clip) {
                stillHasAdditiveAnimator = true;
                break;
              }
            }
            if (!stillHasAdditiveAnimator) {
              self2._additiveAnimators = null;
            }
          }
          for (var i3 = 0; i3 < tracks.length; i3++) {
            tracks[i3].step(self2._target, percent);
          }
          var onframeList = self2._onframeCbs;
          if (onframeList) {
            for (var i3 = 0; i3 < onframeList.length; i3++) {
              onframeList[i3](self2._target, percent);
            }
          }
        },
        ondestroy: function() {
          self2._doneCallback();
        }
      });
      this._clip = clip2;
      if (this.animation) {
        this.animation.addClip(clip2);
      }
      if (easing) {
        clip2.setEasing(easing);
      }
    } else {
      this._doneCallback();
    }
    return this;
  };
  Animator2.prototype.stop = function(forwardToLast) {
    if (!this._clip) {
      return;
    }
    var clip2 = this._clip;
    if (forwardToLast) {
      clip2.onframe(1);
    }
    this._abortedCallback();
  };
  Animator2.prototype.delay = function(time) {
    this._delay = time;
    return this;
  };
  Animator2.prototype.during = function(cb) {
    if (cb) {
      if (!this._onframeCbs) {
        this._onframeCbs = [];
      }
      this._onframeCbs.push(cb);
    }
    return this;
  };
  Animator2.prototype.done = function(cb) {
    if (cb) {
      if (!this._doneCbs) {
        this._doneCbs = [];
      }
      this._doneCbs.push(cb);
    }
    return this;
  };
  Animator2.prototype.aborted = function(cb) {
    if (cb) {
      if (!this._abortedCbs) {
        this._abortedCbs = [];
      }
      this._abortedCbs.push(cb);
    }
    return this;
  };
  Animator2.prototype.getClip = function() {
    return this._clip;
  };
  Animator2.prototype.getTrack = function(propName) {
    return this._tracks[propName];
  };
  Animator2.prototype.getTracks = function() {
    var _this = this;
    return map2(this._trackKeys, function(key2) {
      return _this._tracks[key2];
    });
  };
  Animator2.prototype.stopTracks = function(propNames, forwardToLast) {
    if (!propNames.length || !this._clip) {
      return true;
    }
    var tracks = this._tracks;
    var tracksKeys = this._trackKeys;
    for (var i2 = 0; i2 < propNames.length; i2++) {
      var track = tracks[propNames[i2]];
      if (track && !track.isFinished()) {
        if (forwardToLast) {
          track.step(this._target, 1);
        } else if (this._started === 1) {
          track.step(this._target, 0);
        }
        track.setFinished();
      }
    }
    var allAborted = true;
    for (var i2 = 0; i2 < tracksKeys.length; i2++) {
      if (!tracks[tracksKeys[i2]].isFinished()) {
        allAborted = false;
        break;
      }
    }
    if (allAborted) {
      this._abortedCallback();
    }
    return allAborted;
  };
  Animator2.prototype.saveTo = function(target, trackKeys, firstOrLast) {
    if (!target) {
      return;
    }
    trackKeys = trackKeys || this._trackKeys;
    for (var i2 = 0; i2 < trackKeys.length; i2++) {
      var propName = trackKeys[i2];
      var track = this._tracks[propName];
      if (!track || track.isFinished()) {
        continue;
      }
      var kfs = track.keyframes;
      var kf = kfs[firstOrLast ? 0 : kfs.length - 1];
      if (kf) {
        target[propName] = cloneValue(kf.rawValue);
      }
    }
  };
  Animator2.prototype.__changeFinalValue = function(finalProps, trackKeys) {
    trackKeys = trackKeys || keys(finalProps);
    for (var i2 = 0; i2 < trackKeys.length; i2++) {
      var propName = trackKeys[i2];
      var track = this._tracks[propName];
      if (!track) {
        continue;
      }
      var kfs = track.keyframes;
      if (kfs.length > 1) {
        var lastKf = kfs.pop();
        track.addKeyframe(lastKf.time, finalProps[propName]);
        track.prepare(this._maxTime, track.getAdditiveTrack());
      }
    }
  };
  return Animator2;
})();
var Animator_default = Animator;

// node_modules/zrender/lib/core/Eventful.js
var Eventful = (function() {
  function Eventful2(eventProcessors) {
    if (eventProcessors) {
      this._$eventProcessor = eventProcessors;
    }
  }
  Eventful2.prototype.on = function(event, query2, handler, context) {
    if (!this._$handlers) {
      this._$handlers = {};
    }
    var _h = this._$handlers;
    if (typeof query2 === "function") {
      context = handler;
      handler = query2;
      query2 = null;
    }
    if (!handler || !event) {
      return this;
    }
    var eventProcessor = this._$eventProcessor;
    if (query2 != null && eventProcessor && eventProcessor.normalizeQuery) {
      query2 = eventProcessor.normalizeQuery(query2);
    }
    if (!_h[event]) {
      _h[event] = [];
    }
    for (var i2 = 0; i2 < _h[event].length; i2++) {
      if (_h[event][i2].h === handler) {
        return this;
      }
    }
    var wrap = {
      h: handler,
      query: query2,
      ctx: context || this,
      callAtLast: handler.zrEventfulCallAtLast
    };
    var lastIndex = _h[event].length - 1;
    var lastWrap = _h[event][lastIndex];
    lastWrap && lastWrap.callAtLast ? _h[event].splice(lastIndex, 0, wrap) : _h[event].push(wrap);
    return this;
  };
  Eventful2.prototype.isSilent = function(eventName) {
    var _h = this._$handlers;
    return !_h || !_h[eventName] || !_h[eventName].length;
  };
  Eventful2.prototype.off = function(eventType, handler) {
    var _h = this._$handlers;
    if (!_h) {
      return this;
    }
    if (!eventType) {
      this._$handlers = {};
      return this;
    }
    if (handler) {
      if (_h[eventType]) {
        var newList = [];
        for (var i2 = 0, l = _h[eventType].length; i2 < l; i2++) {
          if (_h[eventType][i2].h !== handler) {
            newList.push(_h[eventType][i2]);
          }
        }
        _h[eventType] = newList;
      }
      if (_h[eventType] && _h[eventType].length === 0) {
        delete _h[eventType];
      }
    } else {
      delete _h[eventType];
    }
    return this;
  };
  Eventful2.prototype.trigger = function(eventType) {
    var args = [];
    for (var _i = 1; _i < arguments.length; _i++) {
      args[_i - 1] = arguments[_i];
    }
    if (!this._$handlers) {
      return this;
    }
    var _h = this._$handlers[eventType];
    var eventProcessor = this._$eventProcessor;
    if (_h) {
      var argLen = args.length;
      var len2 = _h.length;
      for (var i2 = 0; i2 < len2; i2++) {
        var hItem = _h[i2];
        if (eventProcessor && eventProcessor.filter && hItem.query != null && !eventProcessor.filter(eventType, hItem.query)) {
          continue;
        }
        switch (argLen) {
          case 0:
            hItem.h.call(hItem.ctx);
            break;
          case 1:
            hItem.h.call(hItem.ctx, args[0]);
            break;
          case 2:
            hItem.h.call(hItem.ctx, args[0], args[1]);
            break;
          default:
            hItem.h.apply(hItem.ctx, args);
            break;
        }
      }
    }
    eventProcessor && eventProcessor.afterTrigger && eventProcessor.afterTrigger(eventType);
    return this;
  };
  Eventful2.prototype.triggerWithContext = function(type) {
    var args = [];
    for (var _i = 1; _i < arguments.length; _i++) {
      args[_i - 1] = arguments[_i];
    }
    if (!this._$handlers) {
      return this;
    }
    var _h = this._$handlers[type];
    var eventProcessor = this._$eventProcessor;
    if (_h) {
      var argLen = args.length;
      var ctx = args[argLen - 1];
      var len2 = _h.length;
      for (var i2 = 0; i2 < len2; i2++) {
        var hItem = _h[i2];
        if (eventProcessor && eventProcessor.filter && hItem.query != null && !eventProcessor.filter(type, hItem.query)) {
          continue;
        }
        switch (argLen) {
          case 0:
            hItem.h.call(ctx);
            break;
          case 1:
            hItem.h.call(ctx, args[0]);
            break;
          case 2:
            hItem.h.call(ctx, args[0], args[1]);
            break;
          default:
            hItem.h.apply(ctx, args.slice(1, argLen - 1));
            break;
        }
      }
    }
    eventProcessor && eventProcessor.afterTrigger && eventProcessor.afterTrigger(type);
    return this;
  };
  return Eventful2;
})();
var Eventful_default = Eventful;

// node_modules/zrender/lib/config.js
var dpr = 1;
if (env_default.hasGlobalWindow) {
  dpr = Math.max(window.devicePixelRatio || window.screen && window.screen.deviceXDPI / window.screen.logicalXDPI || 1, 1);
}
var devicePixelRatio = dpr;
var DARK_MODE_THRESHOLD = 0.4;
var DARK_LABEL_COLOR = "#333";
var LIGHT_LABEL_COLOR = "#ccc";
var LIGHTER_LABEL_COLOR = "#eee";

// node_modules/zrender/lib/graphic/constants.js
var REDRAW_BIT = 1;
var STYLE_CHANGED_BIT = 2;
var SHAPE_CHANGED_BIT = 4;

// node_modules/zrender/lib/Element.js
var PRESERVED_NORMAL_STATE = "__zr_normal__";
var PRIMARY_STATES_KEYS = TRANSFORMABLE_PROPS.concat(["ignore"]);
var DEFAULT_ANIMATABLE_MAP = reduce(TRANSFORMABLE_PROPS, function(obj, key2) {
  obj[key2] = true;
  return obj;
}, { ignore: false });
var tmpTextPosCalcRes = {};
var tmpBoundingRect = new BoundingRect_default(0, 0, 0, 0);
var tmpInnerTextTrans = [];
var IN_HOVER_LAYER_KIND_NO = 0;
var IN_HOVER_LAYER_KIND_ONLY_STYLE_CHANGE = 1;
var Element2 = (function() {
  function Element3(props) {
    this.id = guid();
    this.animators = [];
    this.currentStates = [];
    this.states = {};
    this._init(props);
  }
  Element3.prototype._init = function(props) {
    this.attr(props);
  };
  Element3.prototype.drift = function(dx, dy, e2) {
    switch (this.draggable) {
      case "horizontal":
        dy = 0;
        break;
      case "vertical":
        dx = 0;
        break;
    }
    var m2 = this.transform;
    if (!m2) {
      m2 = this.transform = [1, 0, 0, 1, 0, 0];
    }
    m2[4] += dx;
    m2[5] += dy;
    this.decomposeTransform();
    this.markRedraw();
  };
  Element3.prototype.beforeUpdate = function() {
  };
  Element3.prototype.afterUpdate = function() {
  };
  Element3.prototype.update = function() {
    this.updateTransform();
    if (this.__dirty) {
      this.updateInnerText();
    }
  };
  Element3.prototype.updateInnerText = function(forceUpdate) {
    var textEl = this._textContent;
    if (textEl && (!textEl.ignore || forceUpdate)) {
      if (!this.textConfig) {
        this.textConfig = {};
      }
      var textConfig = this.textConfig;
      var isLocal = textConfig.local;
      var innerTransformable = textEl.innerTransformable;
      var textAlign = void 0;
      var textVerticalAlign = void 0;
      var textStyleChanged = false;
      innerTransformable.parent = isLocal ? this : null;
      var innerOrigin = false;
      innerTransformable.copyTransform(textEl);
      var hasPosition = textConfig.position != null;
      var autoOverflowArea = textConfig.autoOverflowArea;
      var layoutRect = void 0;
      if (autoOverflowArea || hasPosition) {
        layoutRect = tmpBoundingRect;
        if (textConfig.layoutRect) {
          layoutRect.copy(textConfig.layoutRect);
        } else {
          layoutRect.copy(this.getBoundingRect());
        }
        if (!isLocal) {
          layoutRect.applyTransform(this.transform);
        }
      }
      if (hasPosition) {
        if (this.calculateTextPosition) {
          this.calculateTextPosition(tmpTextPosCalcRes, textConfig, layoutRect);
        } else {
          calculateTextPosition(tmpTextPosCalcRes, textConfig, layoutRect);
        }
        innerTransformable.x = tmpTextPosCalcRes.x;
        innerTransformable.y = tmpTextPosCalcRes.y;
        textAlign = tmpTextPosCalcRes.align;
        textVerticalAlign = tmpTextPosCalcRes.verticalAlign;
        var textOrigin = textConfig.origin;
        if (textOrigin && textConfig.rotation != null) {
          var relOriginX = void 0;
          var relOriginY = void 0;
          if (textOrigin === "center") {
            relOriginX = layoutRect.width * 0.5;
            relOriginY = layoutRect.height * 0.5;
          } else {
            relOriginX = parsePercent(textOrigin[0], layoutRect.width);
            relOriginY = parsePercent(textOrigin[1], layoutRect.height);
          }
          innerOrigin = true;
          innerTransformable.originX = -innerTransformable.x + relOriginX + (isLocal ? 0 : layoutRect.x);
          innerTransformable.originY = -innerTransformable.y + relOriginY + (isLocal ? 0 : layoutRect.y);
        }
      }
      if (textConfig.rotation != null) {
        innerTransformable.rotation = textConfig.rotation;
      }
      var textOffset = textConfig.offset;
      if (textOffset) {
        innerTransformable.x += textOffset[0];
        innerTransformable.y += textOffset[1];
        if (!innerOrigin) {
          innerTransformable.originX = -textOffset[0];
          innerTransformable.originY = -textOffset[1];
        }
      }
      var innerTextDefaultStyle = this._innerTextDefaultStyle || (this._innerTextDefaultStyle = {});
      if (autoOverflowArea) {
        var overflowRect = innerTextDefaultStyle.overflowRect = innerTextDefaultStyle.overflowRect || new BoundingRect_default(0, 0, 0, 0);
        innerTransformable.getLocalTransform(tmpInnerTextTrans);
        invert(tmpInnerTextTrans, tmpInnerTextTrans);
        BoundingRect_default.copy(overflowRect, layoutRect);
        overflowRect.applyTransform(tmpInnerTextTrans);
      } else {
        innerTextDefaultStyle.overflowRect = null;
      }
      var isInside = textConfig.inside == null ? typeof textConfig.position === "string" && textConfig.position.indexOf("inside") >= 0 : textConfig.inside;
      var textFill = void 0;
      var textStroke = void 0;
      var autoStroke = void 0;
      if (isInside && this.canBeInsideText()) {
        textFill = textConfig.insideFill;
        textStroke = textConfig.insideStroke;
        if (textFill == null || textFill === "auto") {
          textFill = this.getInsideTextFill();
        }
        if (textStroke == null || textStroke === "auto") {
          textStroke = this.getInsideTextStroke(textFill);
          autoStroke = true;
        }
      } else {
        textFill = textConfig.outsideFill;
        textStroke = textConfig.outsideStroke;
        if (textFill == null || textFill === "auto") {
          textFill = this.getOutsideFill();
        }
        if (textStroke == null || textStroke === "auto") {
          textStroke = this.getOutsideStroke(textFill);
          autoStroke = true;
        }
      }
      textFill = textFill || "#000";
      if (textFill !== innerTextDefaultStyle.fill || textStroke !== innerTextDefaultStyle.stroke || autoStroke !== innerTextDefaultStyle.autoStroke || textAlign !== innerTextDefaultStyle.align || textVerticalAlign !== innerTextDefaultStyle.verticalAlign) {
        textStyleChanged = true;
        innerTextDefaultStyle.fill = textFill;
        innerTextDefaultStyle.stroke = textStroke;
        innerTextDefaultStyle.autoStroke = autoStroke;
        innerTextDefaultStyle.align = textAlign;
        innerTextDefaultStyle.verticalAlign = textVerticalAlign;
        textEl.setDefaultTextStyle(innerTextDefaultStyle);
      }
      textEl.__dirty |= REDRAW_BIT;
      if (textStyleChanged) {
        textEl.dirtyStyle(true);
      }
    }
  };
  Element3.prototype.canBeInsideText = function() {
    return true;
  };
  Element3.prototype.getInsideTextFill = function() {
    return "#fff";
  };
  Element3.prototype.getInsideTextStroke = function(textFill) {
    return "#000";
  };
  Element3.prototype.getOutsideFill = function() {
    return this.__zr && this.__zr.isDarkMode() ? LIGHT_LABEL_COLOR : DARK_LABEL_COLOR;
  };
  Element3.prototype.getOutsideStroke = function(textFill) {
    var backgroundColor2 = this.__zr && this.__zr.getBackgroundColor();
    var colorArr = typeof backgroundColor2 === "string" && parse(backgroundColor2);
    if (!colorArr) {
      colorArr = [255, 255, 255, 1];
    }
    var alpha = colorArr[3];
    var isDark = this.__zr.isDarkMode();
    for (var i2 = 0; i2 < 3; i2++) {
      colorArr[i2] = colorArr[i2] * alpha + (isDark ? 0 : 255) * (1 - alpha);
    }
    colorArr[3] = 1;
    return stringify(colorArr, "rgba");
  };
  Element3.prototype.traverse = function(cb, context) {
  };
  Element3.prototype.attrKV = function(key2, value) {
    if (key2 === "textConfig") {
      this.setTextConfig(value);
    } else if (key2 === "textContent") {
      this.setTextContent(value);
    } else if (key2 === "clipPath") {
      this.setClipPath(value);
    } else if (key2 === "extra") {
      this.extra = this.extra || {};
      extend(this.extra, value);
    } else {
      this[key2] = value;
    }
  };
  Element3.prototype.hide = function() {
    this.ignore = true;
    this.markRedraw();
  };
  Element3.prototype.show = function() {
    this.ignore = false;
    this.markRedraw();
  };
  Element3.prototype.attr = function(keyOrObj, value) {
    if (typeof keyOrObj === "string") {
      this.attrKV(keyOrObj, value);
    } else if (isObject(keyOrObj)) {
      var obj = keyOrObj;
      var keysArr = keys(obj);
      for (var i2 = 0; i2 < keysArr.length; i2++) {
        var key2 = keysArr[i2];
        this.attrKV(key2, keyOrObj[key2]);
      }
    }
    this.markRedraw();
    return this;
  };
  Element3.prototype.saveCurrentToNormalState = function(toState) {
    this._innerSaveToNormal(toState);
    var normalState = this._normalState;
    for (var i2 = 0; i2 < this.animators.length; i2++) {
      var animator = this.animators[i2];
      var fromStateTransition = animator.__fromStateTransition;
      if (animator.getLoop() || fromStateTransition && fromStateTransition !== PRESERVED_NORMAL_STATE) {
        continue;
      }
      var targetName = animator.targetName;
      var target = targetName ? normalState[targetName] : normalState;
      animator.saveTo(target);
    }
  };
  Element3.prototype._innerSaveToNormal = function(toState) {
    var normalState = this._normalState;
    if (!normalState) {
      normalState = this._normalState = {};
    }
    if (toState.textConfig && !normalState.textConfig) {
      normalState.textConfig = this.textConfig;
    }
    this._savePrimaryToNormal(toState, normalState, PRIMARY_STATES_KEYS);
  };
  Element3.prototype._savePrimaryToNormal = function(toState, normalState, primaryKeys) {
    for (var i2 = 0; i2 < primaryKeys.length; i2++) {
      var key2 = primaryKeys[i2];
      if (toState[key2] != null && !(key2 in normalState)) {
        normalState[key2] = this[key2];
      }
    }
  };
  Element3.prototype.hasState = function() {
    return this.currentStates.length > 0;
  };
  Element3.prototype.getState = function(name) {
    return this.states[name];
  };
  Element3.prototype.ensureState = function(name) {
    var states = this.states;
    if (!states[name]) {
      states[name] = {};
    }
    return states[name];
  };
  Element3.prototype.clearStates = function(noAnimation) {
    this.useState(PRESERVED_NORMAL_STATE, false, noAnimation);
  };
  Element3.prototype.useState = function(stateName, keepCurrentStates, noAnimation, forceUseHoverLayer) {
    var toNormalState = stateName === PRESERVED_NORMAL_STATE;
    var hasStates = this.hasState();
    if (!hasStates && toNormalState) {
      return;
    }
    var currentStates = this.currentStates;
    var animationCfg = this.stateTransition;
    if (indexOf(currentStates, stateName) >= 0 && (keepCurrentStates || currentStates.length === 1)) {
      return;
    }
    var state2;
    if (this.stateProxy && !toNormalState) {
      state2 = this.stateProxy(stateName);
    }
    if (!state2) {
      state2 = this.states && this.states[stateName];
    }
    if (!state2 && !toNormalState) {
      logError("State " + stateName + " not exists.");
      return;
    }
    if (!toNormalState) {
      this.saveCurrentToNormalState(state2);
    }
    var textContent = this._textContent;
    var useHoverLayer = shouldUseHoverLayer(this, textContent, state2, forceUseHoverLayer);
    if (useHoverLayer && !this.__inHover) {
      this.__inHover = useHoverLayer;
    }
    this._applyStateObj(stateName, state2, this._normalState, keepCurrentStates, canTransition(this, noAnimation, animationCfg), animationCfg);
    var textGuide = this._textGuide;
    if (textContent) {
      textContent.useState(stateName, keepCurrentStates, noAnimation, !!useHoverLayer);
    }
    if (textGuide) {
      textGuide.useState(stateName, keepCurrentStates, noAnimation, !!useHoverLayer);
    }
    if (toNormalState) {
      this.currentStates = [];
      this._normalState = {};
    } else {
      if (!keepCurrentStates) {
        this.currentStates = [stateName];
      } else {
        this.currentStates.push(stateName);
      }
    }
    this._updateAnimationTargets();
    this.markRedraw();
    if (!useHoverLayer && this.__inHover) {
      this.__inHover = IN_HOVER_LAYER_KIND_NO;
      this.__dirty &= ~REDRAW_BIT;
    }
    return state2;
  };
  Element3.prototype.useStates = function(states, noAnimation, forceUseHoverLayer) {
    if (!states.length) {
      this.clearStates();
    } else {
      var stateObjects = [];
      var currentStates = this.currentStates;
      var len2 = states.length;
      var notChange = len2 === currentStates.length;
      if (notChange) {
        for (var i2 = 0; i2 < len2; i2++) {
          if (states[i2] !== currentStates[i2]) {
            notChange = false;
            break;
          }
        }
      }
      if (notChange) {
        return;
      }
      for (var i2 = 0; i2 < len2; i2++) {
        var stateName = states[i2];
        var stateObj = void 0;
        if (this.stateProxy) {
          stateObj = this.stateProxy(stateName, states);
        }
        if (!stateObj) {
          stateObj = this.states[stateName];
        }
        if (stateObj) {
          stateObjects.push(stateObj);
        }
      }
      var lastStateObj = stateObjects[len2 - 1];
      var textContent = this._textContent;
      var useHoverLayer = shouldUseHoverLayer(this, textContent, lastStateObj, forceUseHoverLayer);
      if (useHoverLayer && !this.__inHover) {
        this.__inHover = useHoverLayer;
      }
      var mergedState = this._mergeStates(stateObjects);
      var animationCfg = this.stateTransition;
      this.saveCurrentToNormalState(mergedState);
      this._applyStateObj(states.join(","), mergedState, this._normalState, false, canTransition(this, noAnimation, animationCfg), animationCfg);
      var textGuide = this._textGuide;
      if (textContent) {
        textContent.useStates(states, noAnimation, !!useHoverLayer);
      }
      if (textGuide) {
        textGuide.useStates(states, noAnimation, !!useHoverLayer);
      }
      this._updateAnimationTargets();
      this.currentStates = states.slice();
      this.markRedraw();
      if (!useHoverLayer && this.__inHover) {
        this.__inHover = IN_HOVER_LAYER_KIND_NO;
        this.__dirty &= ~REDRAW_BIT;
      }
    }
  };
  Element3.prototype.isSilent = function() {
    var el = this;
    while (el) {
      if (el.silent) {
        return true;
      }
      var hostEl = el.__hostTarget;
      el = hostEl ? el.ignoreHostSilent ? null : hostEl : el.parent;
    }
    return false;
  };
  Element3.prototype._updateAnimationTargets = function() {
    for (var i2 = 0; i2 < this.animators.length; i2++) {
      var animator = this.animators[i2];
      if (animator.targetName) {
        animator.changeTarget(this[animator.targetName]);
      }
    }
  };
  Element3.prototype.removeState = function(state2) {
    var idx = indexOf(this.currentStates, state2);
    if (idx >= 0) {
      var currentStates = this.currentStates.slice();
      currentStates.splice(idx, 1);
      this.useStates(currentStates);
    }
  };
  Element3.prototype.replaceState = function(oldState, newState, forceAdd) {
    var currentStates = this.currentStates.slice();
    var idx = indexOf(currentStates, oldState);
    var newStateExists = indexOf(currentStates, newState) >= 0;
    if (idx >= 0) {
      if (!newStateExists) {
        currentStates[idx] = newState;
      } else {
        currentStates.splice(idx, 1);
      }
    } else if (forceAdd && !newStateExists) {
      currentStates.push(newState);
    }
    this.useStates(currentStates);
  };
  Element3.prototype.toggleState = function(state2, enable) {
    if (enable) {
      this.useState(state2, true);
    } else {
      this.removeState(state2);
    }
  };
  Element3.prototype._mergeStates = function(states) {
    var mergedState = {};
    var mergedTextConfig;
    for (var i2 = 0; i2 < states.length; i2++) {
      var state2 = states[i2];
      extend(mergedState, state2);
      if (state2.textConfig) {
        mergedTextConfig = mergedTextConfig || {};
        extend(mergedTextConfig, state2.textConfig);
      }
    }
    if (mergedTextConfig) {
      mergedState.textConfig = mergedTextConfig;
    }
    return mergedState;
  };
  Element3.prototype._applyStateObj = function(stateName, state2, normalState, keepCurrentStates, transition2, animationCfg) {
    if (this.__inHover === IN_HOVER_LAYER_KIND_ONLY_STYLE_CHANGE) {
      return;
    }
    var needsRestoreToNormal = !(state2 && keepCurrentStates);
    if (state2 && state2.textConfig) {
      this.textConfig = extend({}, keepCurrentStates ? this.textConfig : normalState.textConfig);
      extend(this.textConfig, state2.textConfig);
    } else if (needsRestoreToNormal) {
      if (normalState.textConfig) {
        this.textConfig = normalState.textConfig;
      }
    }
    var transitionTarget = {};
    var hasTransition = false;
    for (var i2 = 0; i2 < PRIMARY_STATES_KEYS.length; i2++) {
      var key2 = PRIMARY_STATES_KEYS[i2];
      var propNeedsTransition = transition2 && DEFAULT_ANIMATABLE_MAP[key2];
      if (state2 && state2[key2] != null) {
        if (propNeedsTransition) {
          hasTransition = true;
          transitionTarget[key2] = state2[key2];
        } else {
          this[key2] = state2[key2];
        }
      } else if (needsRestoreToNormal) {
        if (normalState[key2] != null) {
          if (propNeedsTransition) {
            hasTransition = true;
            transitionTarget[key2] = normalState[key2];
          } else {
            this[key2] = normalState[key2];
          }
        }
      }
    }
    if (!transition2) {
      for (var i2 = 0; i2 < this.animators.length; i2++) {
        var animator = this.animators[i2];
        var targetName = animator.targetName;
        if (!animator.getLoop()) {
          animator.__changeFinalValue(targetName ? (state2 || normalState)[targetName] : state2 || normalState);
        }
      }
    }
    if (hasTransition) {
      this._transitionState(stateName, transitionTarget, animationCfg);
    }
  };
  Element3.prototype._attachComponent = function(componentEl) {
    if (componentEl.__zr && !componentEl.__hostTarget) {
      if (true) {
        throw new Error("Text element has been added to zrender.");
      }
      return;
    }
    if (componentEl === this) {
      if (true) {
        throw new Error("Recursive component attachment.");
      }
      return;
    }
    var zr = this.__zr;
    if (zr) {
      componentEl.addSelfToZr(zr);
    }
    componentEl.__zr = zr;
    componentEl.__hostTarget = this;
  };
  Element3.prototype._detachComponent = function(componentEl) {
    if (componentEl.__zr) {
      componentEl.removeSelfFromZr(componentEl.__zr);
    }
    componentEl.__zr = null;
    componentEl.__hostTarget = null;
  };
  Element3.prototype.getClipPath = function() {
    return this._clipPath;
  };
  Element3.prototype.setClipPath = function(clipPath) {
    if (this._clipPath && this._clipPath !== clipPath) {
      this.removeClipPath();
    }
    this._attachComponent(clipPath);
    this._clipPath = clipPath;
    this.markRedraw();
  };
  Element3.prototype.removeClipPath = function() {
    var clipPath = this._clipPath;
    if (clipPath) {
      this._detachComponent(clipPath);
      this._clipPath = null;
      this.markRedraw();
    }
  };
  Element3.prototype.getTextContent = function() {
    return this._textContent;
  };
  Element3.prototype.setTextContent = function(textEl) {
    var previousTextContent = this._textContent;
    if (previousTextContent === textEl) {
      return;
    }
    if (previousTextContent && previousTextContent !== textEl) {
      this.removeTextContent();
    }
    if (true) {
      if (textEl.__zr && !textEl.__hostTarget) {
        throw new Error("Text element has been added to zrender.");
      }
    }
    textEl.innerTransformable = new Transformable_default();
    this._attachComponent(textEl);
    this._textContent = textEl;
    this.markRedraw();
  };
  Element3.prototype.setTextConfig = function(cfg) {
    if (!this.textConfig) {
      this.textConfig = {};
    }
    extend(this.textConfig, cfg);
    this.markRedraw();
  };
  Element3.prototype.removeTextConfig = function() {
    this.textConfig = null;
    this.markRedraw();
  };
  Element3.prototype.removeTextContent = function() {
    var textEl = this._textContent;
    if (textEl) {
      textEl.innerTransformable = null;
      this._detachComponent(textEl);
      this._textContent = null;
      this._innerTextDefaultStyle = null;
      this.markRedraw();
    }
  };
  Element3.prototype.getTextGuideLine = function() {
    return this._textGuide;
  };
  Element3.prototype.setTextGuideLine = function(guideLine) {
    if (this._textGuide && this._textGuide !== guideLine) {
      this.removeTextGuideLine();
    }
    this._attachComponent(guideLine);
    this._textGuide = guideLine;
    this.markRedraw();
  };
  Element3.prototype.removeTextGuideLine = function() {
    var textGuide = this._textGuide;
    if (textGuide) {
      this._detachComponent(textGuide);
      this._textGuide = null;
      this.markRedraw();
    }
  };
  Element3.prototype.markRedraw = function() {
    this.__dirty |= REDRAW_BIT;
    var zr = this.__zr;
    if (zr) {
      if (this.__inHover) {
        zr.refreshHover();
      } else {
        zr.refresh();
      }
    }
    if (this.__hostTarget) {
      this.__hostTarget.markRedraw();
    }
  };
  Element3.prototype.dirty = function() {
    this.markRedraw();
  };
  Element3.prototype.addSelfToZr = function(zr) {
    if (this.__zr === zr) {
      return;
    }
    this.__zr = zr;
    var animators = this.animators;
    if (animators) {
      for (var i2 = 0; i2 < animators.length; i2++) {
        zr.animation.addAnimator(animators[i2]);
      }
    }
    if (this._clipPath) {
      this._clipPath.addSelfToZr(zr);
    }
    if (this._textContent) {
      this._textContent.addSelfToZr(zr);
    }
    if (this._textGuide) {
      this._textGuide.addSelfToZr(zr);
    }
  };
  Element3.prototype.removeSelfFromZr = function(zr) {
    if (!this.__zr) {
      return;
    }
    this.__zr = null;
    var animators = this.animators;
    if (animators) {
      for (var i2 = 0; i2 < animators.length; i2++) {
        zr.animation.removeAnimator(animators[i2]);
      }
    }
    if (this._clipPath) {
      this._clipPath.removeSelfFromZr(zr);
    }
    if (this._textContent) {
      this._textContent.removeSelfFromZr(zr);
    }
    if (this._textGuide) {
      this._textGuide.removeSelfFromZr(zr);
    }
  };
  Element3.prototype.animate = function(key2, loop, allowDiscreteAnimation) {
    var target = key2 ? this[key2] : this;
    if (true) {
      if (!target) {
        logError('Property "' + key2 + '" is not existed in element ' + this.id);
        return;
      }
    }
    var animator = new Animator_default(target, loop, allowDiscreteAnimation);
    key2 && (animator.targetName = key2);
    this.addAnimator(animator, key2);
    return animator;
  };
  Element3.prototype.addAnimator = function(animator, key2) {
    var zr = this.__zr;
    var el = this;
    animator.during(function() {
      el.updateDuringAnimation(key2);
    }).done(function() {
      var animators = el.animators;
      var idx = indexOf(animators, animator);
      if (idx >= 0) {
        animators.splice(idx, 1);
      }
    });
    this.animators.push(animator);
    if (zr) {
      zr.animation.addAnimator(animator);
    }
    zr && zr.wakeUp();
  };
  Element3.prototype.updateDuringAnimation = function(key2) {
    this.markRedraw();
  };
  Element3.prototype.stopAnimation = function(scope, forwardToLast) {
    var animators = this.animators;
    var len2 = animators.length;
    var leftAnimators = [];
    for (var i2 = 0; i2 < len2; i2++) {
      var animator = animators[i2];
      if (!scope || scope === animator.scope) {
        animator.stop(forwardToLast);
      } else {
        leftAnimators.push(animator);
      }
    }
    this.animators = leftAnimators;
    return this;
  };
  Element3.prototype.animateTo = function(target, cfg, animationProps) {
    animateTo(this, target, cfg, animationProps);
  };
  Element3.prototype.animateFrom = function(target, cfg, animationProps) {
    animateTo(this, target, cfg, animationProps, true);
  };
  Element3.prototype._transitionState = function(stateName, target, cfg, animationProps) {
    var animators = animateTo(this, target, cfg, animationProps);
    for (var i2 = 0; i2 < animators.length; i2++) {
      animators[i2].__fromStateTransition = stateName;
    }
  };
  Element3.prototype.getBoundingRect = function() {
    return null;
  };
  Element3.prototype.getPaintRect = function() {
    return null;
  };
  Element3.initDefaultProps = (function() {
    var elProto = Element3.prototype;
    elProto.type = "element";
    elProto.name = "";
    elProto.ignore = elProto.silent = elProto.ignoreHostSilent = elProto.isGroup = elProto.draggable = elProto.dragging = elProto.ignoreClip = false;
    elProto.__inHover = IN_HOVER_LAYER_KIND_NO;
    elProto.__dirty = REDRAW_BIT;
    var logs = {};
    function logDeprecatedError(key2, xKey, yKey) {
      if (!logs[key2 + xKey + yKey]) {
        console.warn("DEPRECATED: '" + key2 + "' has been deprecated. use '" + xKey + "', '" + yKey + "' instead");
        logs[key2 + xKey + yKey] = true;
      }
    }
    function createLegacyProperty(key2, privateKey, xKey, yKey) {
      Object.defineProperty(elProto, key2, {
        get: function() {
          if (true) {
            logDeprecatedError(key2, xKey, yKey);
          }
          if (!this[privateKey]) {
            var pos = this[privateKey] = [];
            enhanceArray(this, pos);
          }
          return this[privateKey];
        },
        set: function(pos) {
          if (true) {
            logDeprecatedError(key2, xKey, yKey);
          }
          this[xKey] = pos[0];
          this[yKey] = pos[1];
          this[privateKey] = pos;
          enhanceArray(this, pos);
        }
      });
      function enhanceArray(self2, pos) {
        Object.defineProperty(pos, 0, {
          get: function() {
            return self2[xKey];
          },
          set: function(val) {
            self2[xKey] = val;
          }
        });
        Object.defineProperty(pos, 1, {
          get: function() {
            return self2[yKey];
          },
          set: function(val) {
            self2[yKey] = val;
          }
        });
      }
    }
    if (Object.defineProperty) {
      createLegacyProperty("position", "_legacyPos", "x", "y");
      createLegacyProperty("scale", "_legacyScale", "scaleX", "scaleY");
      createLegacyProperty("origin", "_legacyOrigin", "originX", "originY");
    }
  })();
  return Element3;
})();
mixin(Element2, Eventful_default);
mixin(Element2, Transformable_default);
function animateTo(animatable, target, cfg, animationProps, reverse) {
  cfg = cfg || {};
  var animators = [];
  animateToShallow(animatable, "", animatable, target, cfg, animationProps, animators, reverse);
  var finishCount = animators.length;
  var doneHappened = false;
  var cfgDone = cfg.done;
  var cfgAborted = cfg.aborted;
  var doneCb = function() {
    doneHappened = true;
    finishCount--;
    if (finishCount <= 0) {
      doneHappened ? cfgDone && cfgDone() : cfgAborted && cfgAborted();
    }
  };
  var abortedCb = function() {
    finishCount--;
    if (finishCount <= 0) {
      doneHappened ? cfgDone && cfgDone() : cfgAborted && cfgAborted();
    }
  };
  if (!finishCount) {
    cfgDone && cfgDone();
  }
  if (animators.length > 0 && cfg.during) {
    animators[0].during(function(target2, percent) {
      cfg.during(percent);
    });
  }
  for (var i2 = 0; i2 < animators.length; i2++) {
    var animator = animators[i2];
    if (doneCb) {
      animator.done(doneCb);
    }
    if (abortedCb) {
      animator.aborted(abortedCb);
    }
    if (cfg.force) {
      animator.duration(cfg.duration);
    }
    animator.start(cfg.easing);
  }
  return animators;
}
function copyArrShallow(source, target, len2) {
  for (var i2 = 0; i2 < len2; i2++) {
    source[i2] = target[i2];
  }
}
function is2DArray(value) {
  return isArrayLike(value[0]);
}
function copyValue(target, source, key2) {
  if (isArrayLike(source[key2])) {
    if (!isArrayLike(target[key2])) {
      target[key2] = [];
    }
    if (isTypedArray(source[key2])) {
      var len2 = source[key2].length;
      if (target[key2].length !== len2) {
        target[key2] = new source[key2].constructor(len2);
        copyArrShallow(target[key2], source[key2], len2);
      }
    } else {
      var sourceArr = source[key2];
      var targetArr = target[key2];
      var len0 = sourceArr.length;
      if (is2DArray(sourceArr)) {
        var len1 = sourceArr[0].length;
        for (var i2 = 0; i2 < len0; i2++) {
          if (!targetArr[i2]) {
            targetArr[i2] = Array.prototype.slice.call(sourceArr[i2]);
          } else {
            copyArrShallow(targetArr[i2], sourceArr[i2], len1);
          }
        }
      } else {
        copyArrShallow(targetArr, sourceArr, len0);
      }
      targetArr.length = sourceArr.length;
    }
  } else {
    target[key2] = source[key2];
  }
}
function isValueSame(val1, val2) {
  return val1 === val2 || isArrayLike(val1) && isArrayLike(val2) && is1DArraySame(val1, val2);
}
function is1DArraySame(arr0, arr1) {
  var len2 = arr0.length;
  if (len2 !== arr1.length) {
    return false;
  }
  for (var i2 = 0; i2 < len2; i2++) {
    if (arr0[i2] !== arr1[i2]) {
      return false;
    }
  }
  return true;
}
function animateToShallow(animatable, topKey, animateObj, target, cfg, animationProps, animators, reverse) {
  var targetKeys = keys(target);
  var duration = cfg.duration;
  var delay2 = cfg.delay;
  var additive = cfg.additive;
  var setToFinal = cfg.setToFinal;
  var animateAll = !isObject(animationProps);
  var existsAnimators = animatable.animators;
  var animationKeys = [];
  for (var k2 = 0; k2 < targetKeys.length; k2++) {
    var innerKey = targetKeys[k2];
    var targetVal = target[innerKey];
    if (targetVal != null && animateObj[innerKey] != null && (animateAll || animationProps[innerKey])) {
      if (isObject(targetVal) && !isArrayLike(targetVal) && !isGradientObject(targetVal)) {
        if (topKey) {
          if (!reverse) {
            animateObj[innerKey] = targetVal;
            animatable.updateDuringAnimation(topKey);
          }
          continue;
        }
        animateToShallow(animatable, innerKey, animateObj[innerKey], targetVal, cfg, animationProps && animationProps[innerKey], animators, reverse);
      } else {
        animationKeys.push(innerKey);
      }
    } else if (!reverse) {
      animateObj[innerKey] = targetVal;
      animatable.updateDuringAnimation(topKey);
      animationKeys.push(innerKey);
    }
  }
  var keyLen = animationKeys.length;
  if (!additive && keyLen) {
    for (var i2 = 0; i2 < existsAnimators.length; i2++) {
      var animator = existsAnimators[i2];
      if (animator.targetName === topKey) {
        var allAborted = animator.stopTracks(animationKeys);
        if (allAborted) {
          var idx = indexOf(existsAnimators, animator);
          existsAnimators.splice(idx, 1);
        }
      }
    }
  }
  if (!cfg.force) {
    animationKeys = filter2(animationKeys, function(key2) {
      return !isValueSame(target[key2], animateObj[key2]);
    });
    keyLen = animationKeys.length;
  }
  if (keyLen > 0 || cfg.force && !animators.length) {
    var revertedSource = void 0;
    var reversedTarget = void 0;
    var sourceClone = void 0;
    if (reverse) {
      reversedTarget = {};
      if (setToFinal) {
        revertedSource = {};
      }
      for (var i2 = 0; i2 < keyLen; i2++) {
        var innerKey = animationKeys[i2];
        reversedTarget[innerKey] = animateObj[innerKey];
        if (setToFinal) {
          revertedSource[innerKey] = target[innerKey];
        } else {
          animateObj[innerKey] = target[innerKey];
        }
      }
    } else if (setToFinal) {
      sourceClone = {};
      for (var i2 = 0; i2 < keyLen; i2++) {
        var innerKey = animationKeys[i2];
        sourceClone[innerKey] = cloneValue(animateObj[innerKey]);
        copyValue(animateObj, target, innerKey);
      }
    }
    var animator = new Animator_default(animateObj, false, false, additive ? filter2(existsAnimators, function(animator2) {
      return animator2.targetName === topKey;
    }) : null);
    animator.targetName = topKey;
    if (cfg.scope) {
      animator.scope = cfg.scope;
    }
    if (setToFinal && revertedSource) {
      animator.whenWithKeys(0, revertedSource, animationKeys);
    }
    if (sourceClone) {
      animator.whenWithKeys(0, sourceClone, animationKeys);
    }
    animator.whenWithKeys(duration == null ? 500 : duration, reverse ? reversedTarget : target, animationKeys).delay(delay2 || 0);
    animatable.addAnimator(animator, topKey);
    animators.push(animator);
  }
}
function shouldUseHoverLayer(el, textContent, nextState, forceUseHoverLayer) {
  return !(nextState && nextState.hoverLayer || forceUseHoverLayer) || isTextRelatedEl(el) || textContent && isTextRelatedEl(textContent) ? IN_HOVER_LAYER_KIND_NO : IN_HOVER_LAYER_KIND_ONLY_STYLE_CHANGE;
}
function isTextRelatedEl(el) {
  return el.type === "text" || el.type === "tspan";
}
function canTransition(el, noAnimation, animationCfg) {
  return !noAnimation && !el.__inHover && animationCfg && animationCfg.duration > 0;
}
var Element_default = Element2;

// node_modules/zrender/lib/graphic/Displayable.js
var STYLE_MAGIC_KEY = "__zr_style_" + Math.round(Math.random() * 10);
var DEFAULT_COMMON_STYLE = {
  shadowBlur: 0,
  shadowOffsetX: 0,
  shadowOffsetY: 0,
  shadowColor: "#000",
  opacity: 1,
  blend: "source-over"
};
var DEFAULT_COMMON_ANIMATION_PROPS = {
  style: {
    shadowBlur: true,
    shadowOffsetX: true,
    shadowOffsetY: true,
    shadowColor: true,
    opacity: true
  }
};
DEFAULT_COMMON_STYLE[STYLE_MAGIC_KEY] = true;
var PRIMARY_STATES_KEYS2 = ["z", "z2", "invisible"];
var PRIMARY_STATES_KEYS_IN_HOVER_LAYER = ["invisible"];
var Displayable = (function(_super) {
  __extends2(Displayable2, _super);
  function Displayable2(props) {
    return _super.call(this, props) || this;
  }
  Displayable2.prototype._init = function(props) {
    var keysArr = keys(props);
    for (var i2 = 0; i2 < keysArr.length; i2++) {
      var key2 = keysArr[i2];
      if (key2 === "style") {
        this.useStyle(props[key2]);
      } else {
        _super.prototype.attrKV.call(this, key2, props[key2]);
      }
    }
    if (!this.style) {
      this.useStyle({});
    }
  };
  Displayable2.prototype.beforeBrush = function(param) {
  };
  Displayable2.prototype.afterBrush = function() {
  };
  Displayable2.prototype.innerBeforeBrush = function() {
  };
  Displayable2.prototype.innerAfterBrush = function() {
  };
  Displayable2.prototype.shouldBePainted = function(viewWidth, viewHeight, considerClipPath, considerAncestors) {
    var m2 = this.transform;
    if (this.ignore || this.invisible || this.style.opacity === 0 || this.culling && isDisplayableCulled(this, viewWidth, viewHeight) || m2 && !m2[0] && !m2[3]) {
      return false;
    }
    if (considerClipPath && this.__clipPaths && this.__clipPaths.length) {
      for (var i2 = 0; i2 < this.__clipPaths.length; ++i2) {
        if (this.__clipPaths[i2].isZeroArea()) {
          return false;
        }
      }
    }
    if (considerAncestors && this.parent) {
      var parent_1 = this.parent;
      while (parent_1) {
        if (parent_1.ignore) {
          return false;
        }
        parent_1 = parent_1.parent;
      }
    }
    return true;
  };
  Displayable2.prototype.contain = function(x, y) {
    return this.rectContain(x, y);
  };
  Displayable2.prototype.traverse = function(cb, context) {
    cb.call(context, this);
  };
  Displayable2.prototype.rectContain = function(x, y) {
    var coord = this.transformCoordToLocal(x, y);
    var rect = this.getBoundingRect();
    return rect.contain(coord[0], coord[1]);
  };
  Displayable2.prototype.getPaintRect = function() {
    var rect = this._paintRect;
    if (!this._paintRect || this.__dirty) {
      var transform = this.transform;
      var elRect = this.getBoundingRect();
      var style2 = this.style;
      var shadowSize = style2.shadowBlur || 0;
      var shadowOffsetX = style2.shadowOffsetX || 0;
      var shadowOffsetY = style2.shadowOffsetY || 0;
      rect = this._paintRect || (this._paintRect = new BoundingRect_default(0, 0, 0, 0));
      if (transform) {
        BoundingRect_default.applyTransform(rect, elRect, transform);
      } else {
        rect.copy(elRect);
      }
      if (shadowSize || shadowOffsetX || shadowOffsetY) {
        rect.width += shadowSize * 2 + Math.abs(shadowOffsetX);
        rect.height += shadowSize * 2 + Math.abs(shadowOffsetY);
        rect.x = Math.min(rect.x, rect.x + shadowOffsetX - shadowSize);
        rect.y = Math.min(rect.y, rect.y + shadowOffsetY - shadowSize);
      }
      var tolerance = this.dirtyRectTolerance;
      if (!rect.isZero()) {
        rect.x = Math.floor(rect.x - tolerance);
        rect.y = Math.floor(rect.y - tolerance);
        rect.width = Math.ceil(rect.width + 1 + tolerance * 2);
        rect.height = Math.ceil(rect.height + 1 + tolerance * 2);
      }
    }
    return rect;
  };
  Displayable2.prototype.setPrevPaintRect = function(paintRect) {
    if (paintRect) {
      this._prevPaintRect = this._prevPaintRect || new BoundingRect_default(0, 0, 0, 0);
      this._prevPaintRect.copy(paintRect);
    } else {
      this._prevPaintRect = null;
    }
  };
  Displayable2.prototype.getPrevPaintRect = function() {
    return this._prevPaintRect;
  };
  Displayable2.prototype.animateStyle = function(loop) {
    return this.animate("style", loop);
  };
  Displayable2.prototype.updateDuringAnimation = function(targetKey) {
    if (targetKey === "style") {
      this.dirtyStyle();
    } else {
      this.markRedraw();
    }
  };
  Displayable2.prototype.attrKV = function(key2, value) {
    if (key2 !== "style") {
      _super.prototype.attrKV.call(this, key2, value);
    } else {
      if (!this.style) {
        this.useStyle(value);
      } else {
        this.setStyle(value);
      }
    }
  };
  Displayable2.prototype.setStyle = function(keyOrObj, value) {
    if (typeof keyOrObj === "string") {
      this.style[keyOrObj] = value;
    } else {
      extend(this.style, keyOrObj);
    }
    this.dirtyStyle();
    return this;
  };
  Displayable2.prototype.dirtyStyle = function(notRedraw) {
    if (!notRedraw) {
      this.markRedraw();
    }
    this.__dirty |= STYLE_CHANGED_BIT;
    if (this._rect) {
      this._rect = null;
    }
  };
  Displayable2.prototype.dirty = function() {
    this.dirtyStyle();
  };
  Displayable2.prototype.styleChanged = function() {
    return !!(this.__dirty & STYLE_CHANGED_BIT);
  };
  Displayable2.prototype.styleUpdated = function() {
    this.__dirty &= ~STYLE_CHANGED_BIT;
  };
  Displayable2.prototype.createStyle = function(obj) {
    return createObject(DEFAULT_COMMON_STYLE, obj);
  };
  Displayable2.prototype.useStyle = function(obj) {
    if (!obj[STYLE_MAGIC_KEY]) {
      obj = this.createStyle(obj);
    }
    this.style = obj;
    this.dirtyStyle();
  };
  Displayable2.prototype._useHoverStyle = function(obj) {
    this.__hoverStyle = obj;
  };
  Displayable2.prototype.isStyleObject = function(obj) {
    return obj[STYLE_MAGIC_KEY];
  };
  Displayable2.prototype._innerSaveToNormal = function(toState) {
    _super.prototype._innerSaveToNormal.call(this, toState);
    var normalState = this._normalState;
    if (toState.style && !normalState.style) {
      normalState.style = this._mergeStyle(this.createStyle(), this.style);
    }
    this._savePrimaryToNormal(toState, normalState, PRIMARY_STATES_KEYS2);
  };
  Displayable2.prototype._applyStateObj = function(stateName, state2, normalState, keepCurrentStates, transition2, animationCfg) {
    _super.prototype._applyStateObj.call(this, stateName, state2, normalState, keepCurrentStates, transition2, animationCfg);
    var needsRestoreToNormal = !(state2 && keepCurrentStates);
    var inHoverOnlyStyleChange = this.__inHover === IN_HOVER_LAYER_KIND_ONLY_STYLE_CHANGE;
    var targetStyle;
    if (state2 && state2.style) {
      if (transition2) {
        if (keepCurrentStates) {
          targetStyle = state2.style;
        } else {
          targetStyle = this._mergeStyle(this.createStyle(), normalState.style);
          this._mergeStyle(targetStyle, state2.style);
        }
      } else {
        targetStyle = this._mergeStyle(this.createStyle(), keepCurrentStates ? this.style : normalState.style);
        this._mergeStyle(targetStyle, state2.style);
      }
    } else if (needsRestoreToNormal) {
      targetStyle = normalState.style;
    }
    if (targetStyle) {
      if (transition2) {
        var sourceStyle = this.style;
        this.style = this.createStyle(needsRestoreToNormal ? {} : sourceStyle);
        if (needsRestoreToNormal) {
          var changedKeys = keys(sourceStyle);
          for (var i2 = 0; i2 < changedKeys.length; i2++) {
            var key2 = changedKeys[i2];
            if (key2 in targetStyle) {
              targetStyle[key2] = targetStyle[key2];
              this.style[key2] = sourceStyle[key2];
            }
          }
        }
        var targetKeys = keys(targetStyle);
        for (var i2 = 0; i2 < targetKeys.length; i2++) {
          var key2 = targetKeys[i2];
          this.style[key2] = this.style[key2];
        }
        this._transitionState(stateName, {
          style: targetStyle
        }, animationCfg, this.getAnimationStyleProps());
      } else {
        if (inHoverOnlyStyleChange) {
          this._useHoverStyle(targetStyle);
        } else {
          this.useStyle(targetStyle);
        }
      }
    }
    if (!inHoverOnlyStyleChange) {
      var statesKeys = this.__inHover ? PRIMARY_STATES_KEYS_IN_HOVER_LAYER : PRIMARY_STATES_KEYS2;
      for (var i2 = 0; i2 < statesKeys.length; i2++) {
        var key2 = statesKeys[i2];
        if (state2 && state2[key2] != null) {
          this[key2] = state2[key2];
        } else if (needsRestoreToNormal) {
          if (normalState[key2] != null) {
            this[key2] = normalState[key2];
          }
        }
      }
    }
  };
  Displayable2.prototype._mergeStates = function(states) {
    var mergedState = _super.prototype._mergeStates.call(this, states);
    var mergedStyle;
    for (var i2 = 0; i2 < states.length; i2++) {
      var state2 = states[i2];
      if (state2.style) {
        mergedStyle = mergedStyle || {};
        this._mergeStyle(mergedStyle, state2.style);
      }
    }
    if (mergedStyle) {
      mergedState.style = mergedStyle;
    }
    return mergedState;
  };
  Displayable2.prototype._mergeStyle = function(targetStyle, sourceStyle) {
    extend(targetStyle, sourceStyle);
    return targetStyle;
  };
  Displayable2.prototype.getAnimationStyleProps = function() {
    return DEFAULT_COMMON_ANIMATION_PROPS;
  };
  Displayable2.initDefaultProps = (function() {
    var dispProto = Displayable2.prototype;
    dispProto.type = "displayable";
    dispProto.invisible = false;
    dispProto.z = 0;
    dispProto.z2 = 0;
    dispProto.zlevel = 0;
    dispProto.culling = false;
    dispProto.cursor = "pointer";
    dispProto.rectHover = false;
    dispProto.incremental = 0;
    dispProto._rect = null;
    dispProto.dirtyRectTolerance = 0;
    dispProto.__dirty = REDRAW_BIT | STYLE_CHANGED_BIT;
  })();
  return Displayable2;
})(Element_default);
var tmpRect = new BoundingRect_default(0, 0, 0, 0);
var viewRect = new BoundingRect_default(0, 0, 0, 0);
function isDisplayableCulled(el, width, height) {
  tmpRect.copy(el.getBoundingRect());
  if (el.transform) {
    tmpRect.applyTransform(el.transform);
  }
  viewRect.width = width;
  viewRect.height = height;
  return !tmpRect.intersect(viewRect);
}
var Displayable_default = Displayable;

// node_modules/zrender/lib/core/bbox.js
var mathMin2 = Math.min;
var mathMax2 = Math.max;
var mathSin = Math.sin;
var mathCos = Math.cos;
var PI2 = Math.PI * 2;
var start = create2();
var end = create2();
var extremity = create2();
function fromPoints(points4, min3, max3) {
  if (points4.length === 0) {
    return;
  }
  var p = points4[0];
  var left = p[0];
  var right = p[0];
  var top = p[1];
  var bottom = p[1];
  for (var i2 = 1; i2 < points4.length; i2++) {
    p = points4[i2];
    left = mathMin2(left, p[0]);
    right = mathMax2(right, p[0]);
    top = mathMin2(top, p[1]);
    bottom = mathMax2(bottom, p[1]);
  }
  min3[0] = left;
  min3[1] = top;
  max3[0] = right;
  max3[1] = bottom;
}
function fromLine(x0, y0, x1, y1, min3, max3) {
  min3[0] = mathMin2(x0, x1);
  min3[1] = mathMin2(y0, y1);
  max3[0] = mathMax2(x0, x1);
  max3[1] = mathMax2(y0, y1);
}
var xDim = [];
var yDim = [];
function fromCubic(x0, y0, x1, y1, x2, y2, x3, y3, min3, max3) {
  var cubicExtrema2 = cubicExtrema;
  var cubicAt2 = cubicAt;
  var n = cubicExtrema2(x0, x1, x2, x3, xDim);
  min3[0] = Infinity;
  min3[1] = Infinity;
  max3[0] = -Infinity;
  max3[1] = -Infinity;
  for (var i2 = 0; i2 < n; i2++) {
    var x = cubicAt2(x0, x1, x2, x3, xDim[i2]);
    min3[0] = mathMin2(x, min3[0]);
    max3[0] = mathMax2(x, max3[0]);
  }
  n = cubicExtrema2(y0, y1, y2, y3, yDim);
  for (var i2 = 0; i2 < n; i2++) {
    var y = cubicAt2(y0, y1, y2, y3, yDim[i2]);
    min3[1] = mathMin2(y, min3[1]);
    max3[1] = mathMax2(y, max3[1]);
  }
  min3[0] = mathMin2(x0, min3[0]);
  max3[0] = mathMax2(x0, max3[0]);
  min3[0] = mathMin2(x3, min3[0]);
  max3[0] = mathMax2(x3, max3[0]);
  min3[1] = mathMin2(y0, min3[1]);
  max3[1] = mathMax2(y0, max3[1]);
  min3[1] = mathMin2(y3, min3[1]);
  max3[1] = mathMax2(y3, max3[1]);
}
function fromQuadratic(x0, y0, x1, y1, x2, y2, min3, max3) {
  var quadraticExtremum2 = quadraticExtremum;
  var quadraticAt3 = quadraticAt;
  var tx = mathMax2(mathMin2(quadraticExtremum2(x0, x1, x2), 1), 0);
  var ty = mathMax2(mathMin2(quadraticExtremum2(y0, y1, y2), 1), 0);
  var x = quadraticAt3(x0, x1, x2, tx);
  var y = quadraticAt3(y0, y1, y2, ty);
  min3[0] = mathMin2(x0, x2, x);
  min3[1] = mathMin2(y0, y2, y);
  max3[0] = mathMax2(x0, x2, x);
  max3[1] = mathMax2(y0, y2, y);
}
function fromArc(x, y, rx, ry, startAngle, endAngle, anticlockwise, min3, max3) {
  var vec2Min = min;
  var vec2Max = max;
  var diff = Math.abs(startAngle - endAngle);
  if (diff % PI2 < 1e-4 && diff > 1e-4) {
    min3[0] = x - rx;
    min3[1] = y - ry;
    max3[0] = x + rx;
    max3[1] = y + ry;
    return;
  }
  start[0] = mathCos(startAngle) * rx + x;
  start[1] = mathSin(startAngle) * ry + y;
  end[0] = mathCos(endAngle) * rx + x;
  end[1] = mathSin(endAngle) * ry + y;
  vec2Min(min3, start, end);
  vec2Max(max3, start, end);
  startAngle = startAngle % PI2;
  if (startAngle < 0) {
    startAngle = startAngle + PI2;
  }
  endAngle = endAngle % PI2;
  if (endAngle < 0) {
    endAngle = endAngle + PI2;
  }
  if (startAngle > endAngle && !anticlockwise) {
    endAngle += PI2;
  } else if (startAngle < endAngle && anticlockwise) {
    startAngle += PI2;
  }
  if (anticlockwise) {
    var tmp = endAngle;
    endAngle = startAngle;
    startAngle = tmp;
  }
  for (var angle = 0; angle < endAngle; angle += Math.PI / 2) {
    if (angle > startAngle) {
      extremity[0] = mathCos(angle) * rx + x;
      extremity[1] = mathSin(angle) * ry + y;
      vec2Min(min3, extremity, min3);
      vec2Max(max3, extremity, max3);
    }
  }
}

// node_modules/zrender/lib/core/PathProxy.js
var CMD = {
  M: 1,
  L: 2,
  C: 3,
  Q: 4,
  A: 5,
  Z: 6,
  R: 7
};
var tmpOutX = [];
var tmpOutY = [];
var min2 = [];
var max2 = [];
var min22 = [];
var max22 = [];
var mathMin3 = Math.min;
var mathMax3 = Math.max;
var mathCos2 = Math.cos;
var mathSin2 = Math.sin;
var mathAbs2 = Math.abs;
var PI = Math.PI;
var PI22 = PI * 2;
var hasTypedArray = typeof Float32Array !== "undefined";
var tmpAngles = [];
function modPI2(radian) {
  var n = Math.round(radian / PI * 1e8) / 1e8;
  return n % 2 * PI;
}
function normalizeArcAngles(angles, anticlockwise) {
  var newStartAngle = modPI2(angles[0]);
  if (newStartAngle < 0) {
    newStartAngle += PI22;
  }
  var delta = newStartAngle - angles[0];
  var newEndAngle = angles[1];
  newEndAngle += delta;
  if (!anticlockwise && newEndAngle - newStartAngle >= PI22) {
    newEndAngle = newStartAngle + PI22;
  } else if (anticlockwise && newStartAngle - newEndAngle >= PI22) {
    newEndAngle = newStartAngle - PI22;
  } else if (!anticlockwise && newStartAngle > newEndAngle) {
    newEndAngle = newStartAngle + (PI22 - modPI2(newStartAngle - newEndAngle));
  } else if (anticlockwise && newStartAngle < newEndAngle) {
    newEndAngle = newStartAngle - (PI22 - modPI2(newEndAngle - newStartAngle));
  }
  angles[0] = newStartAngle;
  angles[1] = newEndAngle;
}
var PathProxy = (function() {
  function PathProxy2(notSaveData) {
    this.dpr = 1;
    this._xi = 0;
    this._yi = 0;
    this._x0 = 0;
    this._y0 = 0;
    this._len = 0;
    if (notSaveData) {
      this._saveData = false;
    }
    if (this._saveData) {
      this.data = [];
    }
  }
  PathProxy2.prototype.increaseVersion = function() {
    this._version++;
  };
  PathProxy2.prototype.getVersion = function() {
    return this._version;
  };
  PathProxy2.prototype.setScale = function(sx, sy, segmentIgnoreThreshold) {
    segmentIgnoreThreshold = segmentIgnoreThreshold || 0;
    if (segmentIgnoreThreshold > 0) {
      this._ux = mathAbs2(segmentIgnoreThreshold / devicePixelRatio / sx) || 0;
      this._uy = mathAbs2(segmentIgnoreThreshold / devicePixelRatio / sy) || 0;
    }
  };
  PathProxy2.prototype.setDPR = function(dpr2) {
    this.dpr = dpr2;
  };
  PathProxy2.prototype.setContext = function(ctx) {
    this._ctx = ctx;
  };
  PathProxy2.prototype.getContext = function() {
    return this._ctx;
  };
  PathProxy2.prototype.beginPath = function() {
    this._ctx && this._ctx.beginPath();
    this.reset();
    return this;
  };
  PathProxy2.prototype.reset = function() {
    if (this._saveData) {
      this._len = 0;
    }
    if (this._pathSegLen) {
      this._pathSegLen = null;
      this._pathLen = 0;
    }
    this._version++;
  };
  PathProxy2.prototype.moveTo = function(x, y) {
    this._drawPendingPt();
    this.addData(CMD.M, x, y);
    this._ctx && this._ctx.moveTo(x, y);
    this._x0 = x;
    this._y0 = y;
    this._xi = x;
    this._yi = y;
    return this;
  };
  PathProxy2.prototype.lineTo = function(x, y) {
    var dx = mathAbs2(x - this._xi);
    var dy = mathAbs2(y - this._yi);
    var exceedUnit = dx > this._ux || dy > this._uy;
    this.addData(CMD.L, x, y);
    if (this._ctx && exceedUnit) {
      this._ctx.lineTo(x, y);
    }
    if (exceedUnit) {
      this._xi = x;
      this._yi = y;
      this._pendingPtDist = 0;
    } else {
      var d2 = dx * dx + dy * dy;
      if (d2 > this._pendingPtDist) {
        this._pendingPtX = x;
        this._pendingPtY = y;
        this._pendingPtDist = d2;
      }
    }
    return this;
  };
  PathProxy2.prototype.bezierCurveTo = function(x1, y1, x2, y2, x3, y3) {
    this._drawPendingPt();
    this.addData(CMD.C, x1, y1, x2, y2, x3, y3);
    if (this._ctx) {
      this._ctx.bezierCurveTo(x1, y1, x2, y2, x3, y3);
    }
    this._xi = x3;
    this._yi = y3;
    return this;
  };
  PathProxy2.prototype.quadraticCurveTo = function(x1, y1, x2, y2) {
    this._drawPendingPt();
    this.addData(CMD.Q, x1, y1, x2, y2);
    if (this._ctx) {
      this._ctx.quadraticCurveTo(x1, y1, x2, y2);
    }
    this._xi = x2;
    this._yi = y2;
    return this;
  };
  PathProxy2.prototype.arc = function(cx, cy, r, startAngle, endAngle, anticlockwise) {
    this._drawPendingPt();
    tmpAngles[0] = startAngle;
    tmpAngles[1] = endAngle;
    normalizeArcAngles(tmpAngles, anticlockwise);
    startAngle = tmpAngles[0];
    endAngle = tmpAngles[1];
    var delta = endAngle - startAngle;
    this.addData(CMD.A, cx, cy, r, r, startAngle, delta, 0, anticlockwise ? 0 : 1);
    this._ctx && this._ctx.arc(cx, cy, r, startAngle, endAngle, anticlockwise);
    this._xi = mathCos2(endAngle) * r + cx;
    this._yi = mathSin2(endAngle) * r + cy;
    return this;
  };
  PathProxy2.prototype.arcTo = function(x1, y1, x2, y2, radius) {
    this._drawPendingPt();
    if (this._ctx) {
      this._ctx.arcTo(x1, y1, x2, y2, radius);
    }
    return this;
  };
  PathProxy2.prototype.rect = function(x, y, w, h) {
    this._drawPendingPt();
    this._ctx && this._ctx.rect(x, y, w, h);
    this.addData(CMD.R, x, y, w, h);
    return this;
  };
  PathProxy2.prototype.closePath = function() {
    this._drawPendingPt();
    this.addData(CMD.Z);
    var ctx = this._ctx;
    var x0 = this._x0;
    var y0 = this._y0;
    if (ctx) {
      ctx.closePath();
    }
    this._xi = x0;
    this._yi = y0;
    return this;
  };
  PathProxy2.prototype.fill = function(ctx) {
    ctx && ctx.fill();
    this.toStatic();
  };
  PathProxy2.prototype.stroke = function(ctx) {
    ctx && ctx.stroke();
    this.toStatic();
  };
  PathProxy2.prototype.len = function() {
    return this._len;
  };
  PathProxy2.prototype.setData = function(data) {
    if (!this._saveData) {
      return;
    }
    var len2 = data.length;
    if (!(this.data && this.data.length === len2) && hasTypedArray) {
      this.data = new Float32Array(len2);
    }
    for (var i2 = 0; i2 < len2; i2++) {
      this.data[i2] = data[i2];
    }
    this._len = len2;
  };
  PathProxy2.prototype.appendPath = function(path) {
    if (!this._saveData) {
      return;
    }
    if (!(path instanceof Array)) {
      path = [path];
    }
    var len2 = path.length;
    var appendSize = 0;
    var offset = this._len;
    for (var i2 = 0; i2 < len2; i2++) {
      appendSize += path[i2].len();
    }
    var oldData = this.data;
    if (hasTypedArray && (oldData instanceof Float32Array || !oldData)) {
      this.data = new Float32Array(offset + appendSize);
      if (offset > 0 && oldData) {
        for (var k2 = 0; k2 < offset; k2++) {
          this.data[k2] = oldData[k2];
        }
      }
    }
    for (var i2 = 0; i2 < len2; i2++) {
      var appendPathData = path[i2].data;
      for (var k2 = 0; k2 < appendPathData.length; k2++) {
        this.data[offset++] = appendPathData[k2];
      }
    }
    this._len = offset;
  };
  PathProxy2.prototype.addData = function(cmd, a, b, c, d, e2, f, g, h) {
    if (!this._saveData) {
      return;
    }
    var data = this.data;
    if (this._len + arguments.length > data.length) {
      this._expandData();
      data = this.data;
    }
    for (var i2 = 0; i2 < arguments.length; i2++) {
      data[this._len++] = arguments[i2];
    }
  };
  PathProxy2.prototype._drawPendingPt = function() {
    if (this._pendingPtDist > 0) {
      this._ctx && this._ctx.lineTo(this._pendingPtX, this._pendingPtY);
      this._pendingPtDist = 0;
    }
  };
  PathProxy2.prototype._expandData = function() {
    if (!(this.data instanceof Array)) {
      var newData = [];
      for (var i2 = 0; i2 < this._len; i2++) {
        newData[i2] = this.data[i2];
      }
      this.data = newData;
    }
  };
  PathProxy2.prototype.toStatic = function() {
    if (!this._saveData) {
      return;
    }
    this._drawPendingPt();
    var data = this.data;
    if (data instanceof Array) {
      data.length = this._len;
      if (hasTypedArray && this._len > 11) {
        this.data = new Float32Array(data);
      }
    }
  };
  PathProxy2.prototype.getBoundingRect = function() {
    min2[0] = min2[1] = min22[0] = min22[1] = Number.MAX_VALUE;
    max2[0] = max2[1] = max22[0] = max22[1] = -Number.MAX_VALUE;
    var data = this.data;
    var xi = 0;
    var yi = 0;
    var x0 = 0;
    var y0 = 0;
    var i2;
    for (i2 = 0; i2 < this._len; ) {
      var cmd = data[i2++];
      var isFirst = i2 === 1;
      if (isFirst) {
        xi = data[i2];
        yi = data[i2 + 1];
        x0 = xi;
        y0 = yi;
      }
      switch (cmd) {
        case CMD.M:
          xi = x0 = data[i2++];
          yi = y0 = data[i2++];
          min22[0] = x0;
          min22[1] = y0;
          max22[0] = x0;
          max22[1] = y0;
          break;
        case CMD.L:
          fromLine(xi, yi, data[i2], data[i2 + 1], min22, max22);
          xi = data[i2++];
          yi = data[i2++];
          break;
        case CMD.C:
          fromCubic(xi, yi, data[i2++], data[i2++], data[i2++], data[i2++], data[i2], data[i2 + 1], min22, max22);
          xi = data[i2++];
          yi = data[i2++];
          break;
        case CMD.Q:
          fromQuadratic(xi, yi, data[i2++], data[i2++], data[i2], data[i2 + 1], min22, max22);
          xi = data[i2++];
          yi = data[i2++];
          break;
        case CMD.A:
          var cx = data[i2++];
          var cy = data[i2++];
          var rx = data[i2++];
          var ry = data[i2++];
          var startAngle = data[i2++];
          var endAngle = data[i2++] + startAngle;
          i2 += 1;
          var anticlockwise = !data[i2++];
          if (isFirst) {
            x0 = mathCos2(startAngle) * rx + cx;
            y0 = mathSin2(startAngle) * ry + cy;
          }
          fromArc(cx, cy, rx, ry, startAngle, endAngle, anticlockwise, min22, max22);
          xi = mathCos2(endAngle) * rx + cx;
          yi = mathSin2(endAngle) * ry + cy;
          break;
        case CMD.R:
          x0 = xi = data[i2++];
          y0 = yi = data[i2++];
          var width = data[i2++];
          var height = data[i2++];
          fromLine(x0, y0, x0 + width, y0 + height, min22, max22);
          break;
        case CMD.Z:
          xi = x0;
          yi = y0;
          break;
      }
      min(min2, min2, min22);
      max(max2, max2, max22);
    }
    if (i2 === 0) {
      min2[0] = min2[1] = max2[0] = max2[1] = 0;
    }
    return new BoundingRect_default(min2[0], min2[1], max2[0] - min2[0], max2[1] - min2[1]);
  };
  PathProxy2.prototype._calculateLength = function() {
    var data = this.data;
    var len2 = this._len;
    var ux = this._ux;
    var uy = this._uy;
    var xi = 0;
    var yi = 0;
    var x0 = 0;
    var y0 = 0;
    if (!this._pathSegLen) {
      this._pathSegLen = [];
    }
    var pathSegLen = this._pathSegLen;
    var pathTotalLen = 0;
    var segCount = 0;
    for (var i2 = 0; i2 < len2; ) {
      var cmd = data[i2++];
      var isFirst = i2 === 1;
      if (isFirst) {
        xi = data[i2];
        yi = data[i2 + 1];
        x0 = xi;
        y0 = yi;
      }
      var l = -1;
      switch (cmd) {
        case CMD.M:
          xi = x0 = data[i2++];
          yi = y0 = data[i2++];
          break;
        case CMD.L: {
          var x2 = data[i2++];
          var y2 = data[i2++];
          var dx = x2 - xi;
          var dy = y2 - yi;
          if (mathAbs2(dx) > ux || mathAbs2(dy) > uy || i2 === len2 - 1) {
            l = Math.sqrt(dx * dx + dy * dy);
            xi = x2;
            yi = y2;
          }
          break;
        }
        case CMD.C: {
          var x1 = data[i2++];
          var y1 = data[i2++];
          var x2 = data[i2++];
          var y2 = data[i2++];
          var x3 = data[i2++];
          var y3 = data[i2++];
          l = cubicLength(xi, yi, x1, y1, x2, y2, x3, y3, 10);
          xi = x3;
          yi = y3;
          break;
        }
        case CMD.Q: {
          var x1 = data[i2++];
          var y1 = data[i2++];
          var x2 = data[i2++];
          var y2 = data[i2++];
          l = quadraticLength(xi, yi, x1, y1, x2, y2, 10);
          xi = x2;
          yi = y2;
          break;
        }
        case CMD.A:
          var cx = data[i2++];
          var cy = data[i2++];
          var rx = data[i2++];
          var ry = data[i2++];
          var startAngle = data[i2++];
          var delta = data[i2++];
          var endAngle = delta + startAngle;
          i2 += 1;
          if (isFirst) {
            x0 = mathCos2(startAngle) * rx + cx;
            y0 = mathSin2(startAngle) * ry + cy;
          }
          l = mathMax3(rx, ry) * mathMin3(PI22, Math.abs(delta));
          xi = mathCos2(endAngle) * rx + cx;
          yi = mathSin2(endAngle) * ry + cy;
          break;
        case CMD.R: {
          x0 = xi = data[i2++];
          y0 = yi = data[i2++];
          var width = data[i2++];
          var height = data[i2++];
          l = width * 2 + height * 2;
          break;
        }
        case CMD.Z: {
          var dx = x0 - xi;
          var dy = y0 - yi;
          l = Math.sqrt(dx * dx + dy * dy);
          xi = x0;
          yi = y0;
          break;
        }
      }
      if (l >= 0) {
        pathSegLen[segCount++] = l;
        pathTotalLen += l;
      }
    }
    this._pathLen = pathTotalLen;
    return pathTotalLen;
  };
  PathProxy2.prototype.rebuildPath = function(ctx, percent) {
    var d = this.data;
    var ux = this._ux;
    var uy = this._uy;
    var len2 = this._len;
    var x0;
    var y0;
    var xi;
    var yi;
    var x;
    var y;
    var drawPart = percent < 1;
    var pathSegLen;
    var pathTotalLen;
    var accumLength = 0;
    var segCount = 0;
    var displayedLength;
    var pendingPtDist = 0;
    var pendingPtX;
    var pendingPtY;
    if (drawPart) {
      if (!this._pathSegLen) {
        this._calculateLength();
      }
      pathSegLen = this._pathSegLen;
      pathTotalLen = this._pathLen;
      displayedLength = percent * pathTotalLen;
      if (!displayedLength) {
        return;
      }
    }
    lo: for (var i2 = 0; i2 < len2; ) {
      var cmd = d[i2++];
      var isFirst = i2 === 1;
      if (isFirst) {
        xi = d[i2];
        yi = d[i2 + 1];
        x0 = xi;
        y0 = yi;
      }
      if (cmd !== CMD.L && pendingPtDist > 0) {
        ctx.lineTo(pendingPtX, pendingPtY);
        pendingPtDist = 0;
      }
      switch (cmd) {
        case CMD.M:
          x0 = xi = d[i2++];
          y0 = yi = d[i2++];
          ctx.moveTo(xi, yi);
          break;
        case CMD.L: {
          x = d[i2++];
          y = d[i2++];
          var dx = mathAbs2(x - xi);
          var dy = mathAbs2(y - yi);
          if (dx > ux || dy > uy) {
            if (drawPart) {
              var l = pathSegLen[segCount++];
              if (accumLength + l > displayedLength) {
                var t = (displayedLength - accumLength) / l;
                ctx.lineTo(xi * (1 - t) + x * t, yi * (1 - t) + y * t);
                break lo;
              }
              accumLength += l;
            }
            ctx.lineTo(x, y);
            xi = x;
            yi = y;
            pendingPtDist = 0;
          } else {
            var d2 = dx * dx + dy * dy;
            if (d2 > pendingPtDist) {
              pendingPtX = x;
              pendingPtY = y;
              pendingPtDist = d2;
            }
          }
          break;
        }
        case CMD.C: {
          var x1 = d[i2++];
          var y1 = d[i2++];
          var x2 = d[i2++];
          var y2 = d[i2++];
          var x3 = d[i2++];
          var y3 = d[i2++];
          if (drawPart) {
            var l = pathSegLen[segCount++];
            if (accumLength + l > displayedLength) {
              var t = (displayedLength - accumLength) / l;
              cubicSubdivide(xi, x1, x2, x3, t, tmpOutX);
              cubicSubdivide(yi, y1, y2, y3, t, tmpOutY);
              ctx.bezierCurveTo(tmpOutX[1], tmpOutY[1], tmpOutX[2], tmpOutY[2], tmpOutX[3], tmpOutY[3]);
              break lo;
            }
            accumLength += l;
          }
          ctx.bezierCurveTo(x1, y1, x2, y2, x3, y3);
          xi = x3;
          yi = y3;
          break;
        }
        case CMD.Q: {
          var x1 = d[i2++];
          var y1 = d[i2++];
          var x2 = d[i2++];
          var y2 = d[i2++];
          if (drawPart) {
            var l = pathSegLen[segCount++];
            if (accumLength + l > displayedLength) {
              var t = (displayedLength - accumLength) / l;
              quadraticSubdivide(xi, x1, x2, t, tmpOutX);
              quadraticSubdivide(yi, y1, y2, t, tmpOutY);
              ctx.quadraticCurveTo(tmpOutX[1], tmpOutY[1], tmpOutX[2], tmpOutY[2]);
              break lo;
            }
            accumLength += l;
          }
          ctx.quadraticCurveTo(x1, y1, x2, y2);
          xi = x2;
          yi = y2;
          break;
        }
        case CMD.A:
          var cx = d[i2++];
          var cy = d[i2++];
          var rx = d[i2++];
          var ry = d[i2++];
          var startAngle = d[i2++];
          var delta = d[i2++];
          var psi = d[i2++];
          var anticlockwise = !d[i2++];
          var r = rx > ry ? rx : ry;
          var isEllipse = mathAbs2(rx - ry) > 1e-3;
          var endAngle = startAngle + delta;
          var breakBuild = false;
          if (drawPart) {
            var l = pathSegLen[segCount++];
            if (accumLength + l > displayedLength) {
              endAngle = startAngle + delta * (displayedLength - accumLength) / l;
              breakBuild = true;
            }
            accumLength += l;
          }
          if (isEllipse && ctx.ellipse) {
            ctx.ellipse(cx, cy, rx, ry, psi, startAngle, endAngle, anticlockwise);
          } else {
            ctx.arc(cx, cy, r, startAngle, endAngle, anticlockwise);
          }
          if (breakBuild) {
            break lo;
          }
          if (isFirst) {
            x0 = mathCos2(startAngle) * rx + cx;
            y0 = mathSin2(startAngle) * ry + cy;
          }
          xi = mathCos2(endAngle) * rx + cx;
          yi = mathSin2(endAngle) * ry + cy;
          break;
        case CMD.R:
          x0 = xi = d[i2];
          y0 = yi = d[i2 + 1];
          x = d[i2++];
          y = d[i2++];
          var width = d[i2++];
          var height = d[i2++];
          if (drawPart) {
            var l = pathSegLen[segCount++];
            if (accumLength + l > displayedLength) {
              var d_1 = displayedLength - accumLength;
              ctx.moveTo(x, y);
              ctx.lineTo(x + mathMin3(d_1, width), y);
              d_1 -= width;
              if (d_1 > 0) {
                ctx.lineTo(x + width, y + mathMin3(d_1, height));
              }
              d_1 -= height;
              if (d_1 > 0) {
                ctx.lineTo(x + mathMax3(width - d_1, 0), y + height);
              }
              d_1 -= width;
              if (d_1 > 0) {
                ctx.lineTo(x, y + mathMax3(height - d_1, 0));
              }
              break lo;
            }
            accumLength += l;
          }
          ctx.rect(x, y, width, height);
          break;
        case CMD.Z:
          if (drawPart) {
            var l = pathSegLen[segCount++];
            if (accumLength + l > displayedLength) {
              var t = (displayedLength - accumLength) / l;
              ctx.lineTo(xi * (1 - t) + x0 * t, yi * (1 - t) + y0 * t);
              break lo;
            }
            accumLength += l;
          }
          ctx.closePath();
          xi = x0;
          yi = y0;
      }
    }
  };
  PathProxy2.prototype.clone = function() {
    var newProxy = new PathProxy2();
    var data = this.data;
    newProxy.data = data.slice ? data.slice() : Array.prototype.slice.call(data);
    newProxy._len = this._len;
    return newProxy;
  };
  PathProxy2.prototype.canSave = function() {
    return !!this._saveData;
  };
  PathProxy2.CMD = CMD;
  PathProxy2.initDefaultProps = (function() {
    var proto = PathProxy2.prototype;
    proto._saveData = true;
    proto._ux = 0;
    proto._uy = 0;
    proto._pendingPtDist = 0;
    proto._version = 0;
  })();
  return PathProxy2;
})();
var PathProxy_default = PathProxy;

// node_modules/zrender/lib/contain/line.js
function containStroke(x0, y0, x1, y1, lineWidth, x, y) {
  if (lineWidth === 0) {
    return false;
  }
  var _l = lineWidth;
  var _a2 = 0;
  var _b2 = x0;
  if (y > y0 + _l && y > y1 + _l || y < y0 - _l && y < y1 - _l || x > x0 + _l && x > x1 + _l || x < x0 - _l && x < x1 - _l) {
    return false;
  }
  if (x0 !== x1) {
    _a2 = (y0 - y1) / (x0 - x1);
    _b2 = (x0 * y1 - x1 * y0) / (x0 - x1);
  } else {
    return Math.abs(x - x0) <= _l / 2;
  }
  var tmp = _a2 * x - y + _b2;
  var _s = tmp * tmp / (_a2 * _a2 + 1);
  return _s <= _l / 2 * _l / 2;
}

// node_modules/zrender/lib/contain/cubic.js
function containStroke2(x0, y0, x1, y1, x2, y2, x3, y3, lineWidth, x, y) {
  if (lineWidth === 0) {
    return false;
  }
  var _l = lineWidth;
  if (y > y0 + _l && y > y1 + _l && y > y2 + _l && y > y3 + _l || y < y0 - _l && y < y1 - _l && y < y2 - _l && y < y3 - _l || x > x0 + _l && x > x1 + _l && x > x2 + _l && x > x3 + _l || x < x0 - _l && x < x1 - _l && x < x2 - _l && x < x3 - _l) {
    return false;
  }
  var d = cubicProjectPoint(x0, y0, x1, y1, x2, y2, x3, y3, x, y, null);
  return d <= _l / 2;
}

// node_modules/zrender/lib/contain/quadratic.js
function containStroke3(x0, y0, x1, y1, x2, y2, lineWidth, x, y) {
  if (lineWidth === 0) {
    return false;
  }
  var _l = lineWidth;
  if (y > y0 + _l && y > y1 + _l && y > y2 + _l || y < y0 - _l && y < y1 - _l && y < y2 - _l || x > x0 + _l && x > x1 + _l && x > x2 + _l || x < x0 - _l && x < x1 - _l && x < x2 - _l) {
    return false;
  }
  var d = quadraticProjectPoint(x0, y0, x1, y1, x2, y2, x, y, null);
  return d <= _l / 2;
}

// node_modules/zrender/lib/contain/util.js
var PI23 = Math.PI * 2;
function normalizeRadian(angle) {
  angle %= PI23;
  if (angle < 0) {
    angle += PI23;
  }
  return angle;
}

// node_modules/zrender/lib/contain/arc.js
var PI24 = Math.PI * 2;
function containStroke4(cx, cy, r, startAngle, endAngle, anticlockwise, lineWidth, x, y) {
  if (lineWidth === 0) {
    return false;
  }
  var _l = lineWidth;
  x -= cx;
  y -= cy;
  var d = Math.sqrt(x * x + y * y);
  if (d - _l > r || d + _l < r) {
    return false;
  }
  if (Math.abs(startAngle - endAngle) % PI24 < 1e-4) {
    return true;
  }
  if (anticlockwise) {
    var tmp = startAngle;
    startAngle = normalizeRadian(endAngle);
    endAngle = normalizeRadian(tmp);
  } else {
    startAngle = normalizeRadian(startAngle);
    endAngle = normalizeRadian(endAngle);
  }
  if (startAngle > endAngle) {
    endAngle += PI24;
  }
  var angle = Math.atan2(y, x);
  if (angle < 0) {
    angle += PI24;
  }
  return angle >= startAngle && angle <= endAngle || angle + PI24 >= startAngle && angle + PI24 <= endAngle;
}

// node_modules/zrender/lib/contain/windingLine.js
function windingLine(x0, y0, x1, y1, x, y) {
  if (y > y0 && y > y1 || y < y0 && y < y1) {
    return 0;
  }
  if (y1 === y0) {
    return 0;
  }
  var t = (y - y0) / (y1 - y0);
  var dir3 = y1 < y0 ? 1 : -1;
  if (t === 1 || t === 0) {
    dir3 = y1 < y0 ? 0.5 : -0.5;
  }
  var x_ = t * (x1 - x0) + x0;
  return x_ === x ? Infinity : x_ > x ? dir3 : 0;
}

// node_modules/zrender/lib/contain/path.js
var CMD2 = PathProxy_default.CMD;
var PI25 = Math.PI * 2;
var EPSILON5 = 1e-4;
function isAroundEqual(a, b) {
  return Math.abs(a - b) < EPSILON5;
}
var roots = [-1, -1, -1];
var extrema = [-1, -1];
function swapExtrema() {
  var tmp = extrema[0];
  extrema[0] = extrema[1];
  extrema[1] = tmp;
}
function windingCubic(x0, y0, x1, y1, x2, y2, x3, y3, x, y) {
  if (y > y0 && y > y1 && y > y2 && y > y3 || y < y0 && y < y1 && y < y2 && y < y3) {
    return 0;
  }
  var nRoots = cubicRootAt(y0, y1, y2, y3, y, roots);
  if (nRoots === 0) {
    return 0;
  } else {
    var w = 0;
    var nExtrema = -1;
    var y0_ = void 0;
    var y1_ = void 0;
    for (var i2 = 0; i2 < nRoots; i2++) {
      var t = roots[i2];
      var unit = t === 0 || t === 1 ? 0.5 : 1;
      var x_ = cubicAt(x0, x1, x2, x3, t);
      if (x_ < x) {
        continue;
      }
      if (nExtrema < 0) {
        nExtrema = cubicExtrema(y0, y1, y2, y3, extrema);
        if (extrema[1] < extrema[0] && nExtrema > 1) {
          swapExtrema();
        }
        y0_ = cubicAt(y0, y1, y2, y3, extrema[0]);
        if (nExtrema > 1) {
          y1_ = cubicAt(y0, y1, y2, y3, extrema[1]);
        }
      }
      if (nExtrema === 2) {
        if (t < extrema[0]) {
          w += y0_ < y0 ? unit : -unit;
        } else if (t < extrema[1]) {
          w += y1_ < y0_ ? unit : -unit;
        } else {
          w += y3 < y1_ ? unit : -unit;
        }
      } else {
        if (t < extrema[0]) {
          w += y0_ < y0 ? unit : -unit;
        } else {
          w += y3 < y0_ ? unit : -unit;
        }
      }
    }
    return w;
  }
}
function windingQuadratic(x0, y0, x1, y1, x2, y2, x, y) {
  if (y > y0 && y > y1 && y > y2 || y < y0 && y < y1 && y < y2) {
    return 0;
  }
  var nRoots = quadraticRootAt(y0, y1, y2, y, roots);
  if (nRoots === 0) {
    return 0;
  } else {
    var t = quadraticExtremum(y0, y1, y2);
    if (t >= 0 && t <= 1) {
      var w = 0;
      var y_ = quadraticAt(y0, y1, y2, t);
      for (var i2 = 0; i2 < nRoots; i2++) {
        var unit = roots[i2] === 0 || roots[i2] === 1 ? 0.5 : 1;
        var x_ = quadraticAt(x0, x1, x2, roots[i2]);
        if (x_ < x) {
          continue;
        }
        if (roots[i2] < t) {
          w += y_ < y0 ? unit : -unit;
        } else {
          w += y2 < y_ ? unit : -unit;
        }
      }
      return w;
    } else {
      var unit = roots[0] === 0 || roots[0] === 1 ? 0.5 : 1;
      var x_ = quadraticAt(x0, x1, x2, roots[0]);
      if (x_ < x) {
        return 0;
      }
      return y2 < y0 ? unit : -unit;
    }
  }
}
function windingArc(cx, cy, r, startAngle, endAngle, anticlockwise, x, y) {
  y -= cy;
  if (y > r || y < -r) {
    return 0;
  }
  var tmp = Math.sqrt(r * r - y * y);
  roots[0] = -tmp;
  roots[1] = tmp;
  var dTheta = Math.abs(startAngle - endAngle);
  if (dTheta < 1e-4) {
    return 0;
  }
  if (dTheta >= PI25 - 1e-4) {
    startAngle = 0;
    endAngle = PI25;
    var dir3 = anticlockwise ? 1 : -1;
    if (x >= roots[0] + cx && x <= roots[1] + cx) {
      return dir3;
    } else {
      return 0;
    }
  }
  if (startAngle > endAngle) {
    var tmp_1 = startAngle;
    startAngle = endAngle;
    endAngle = tmp_1;
  }
  if (startAngle < 0) {
    startAngle += PI25;
    endAngle += PI25;
  }
  var w = 0;
  for (var i2 = 0; i2 < 2; i2++) {
    var x_ = roots[i2];
    if (x_ + cx > x) {
      var angle = Math.atan2(y, x_);
      var dir3 = anticlockwise ? 1 : -1;
      if (angle < 0) {
        angle = PI25 + angle;
      }
      if (angle >= startAngle && angle <= endAngle || angle + PI25 >= startAngle && angle + PI25 <= endAngle) {
        if (angle > Math.PI / 2 && angle < Math.PI * 1.5) {
          dir3 = -dir3;
        }
        w += dir3;
      }
    }
  }
  return w;
}
function containPath(path, lineWidth, isStroke, x, y) {
  var data = path.data;
  var len2 = path.len();
  var w = 0;
  var xi = 0;
  var yi = 0;
  var x0 = 0;
  var y0 = 0;
  var x1;
  var y1;
  for (var i2 = 0; i2 < len2; ) {
    var cmd = data[i2++];
    var isFirst = i2 === 1;
    if (cmd === CMD2.M && i2 > 1) {
      if (!isStroke) {
        w += windingLine(xi, yi, x0, y0, x, y);
      }
    }
    if (isFirst) {
      xi = data[i2];
      yi = data[i2 + 1];
      x0 = xi;
      y0 = yi;
    }
    switch (cmd) {
      case CMD2.M:
        x0 = data[i2++];
        y0 = data[i2++];
        xi = x0;
        yi = y0;
        break;
      case CMD2.L:
        if (isStroke) {
          if (containStroke(xi, yi, data[i2], data[i2 + 1], lineWidth, x, y)) {
            return true;
          }
        } else {
          w += windingLine(xi, yi, data[i2], data[i2 + 1], x, y) || 0;
        }
        xi = data[i2++];
        yi = data[i2++];
        break;
      case CMD2.C:
        if (isStroke) {
          if (containStroke2(xi, yi, data[i2++], data[i2++], data[i2++], data[i2++], data[i2], data[i2 + 1], lineWidth, x, y)) {
            return true;
          }
        } else {
          w += windingCubic(xi, yi, data[i2++], data[i2++], data[i2++], data[i2++], data[i2], data[i2 + 1], x, y) || 0;
        }
        xi = data[i2++];
        yi = data[i2++];
        break;
      case CMD2.Q:
        if (isStroke) {
          if (containStroke3(xi, yi, data[i2++], data[i2++], data[i2], data[i2 + 1], lineWidth, x, y)) {
            return true;
          }
        } else {
          w += windingQuadratic(xi, yi, data[i2++], data[i2++], data[i2], data[i2 + 1], x, y) || 0;
        }
        xi = data[i2++];
        yi = data[i2++];
        break;
      case CMD2.A:
        var cx = data[i2++];
        var cy = data[i2++];
        var rx = data[i2++];
        var ry = data[i2++];
        var theta = data[i2++];
        var dTheta = data[i2++];
        i2 += 1;
        var anticlockwise = !!(1 - data[i2++]);
        x1 = Math.cos(theta) * rx + cx;
        y1 = Math.sin(theta) * ry + cy;
        if (!isFirst) {
          w += windingLine(xi, yi, x1, y1, x, y);
        } else {
          x0 = x1;
          y0 = y1;
        }
        var _x = (x - cx) * ry / rx + cx;
        if (isStroke) {
          if (containStroke4(cx, cy, ry, theta, theta + dTheta, anticlockwise, lineWidth, _x, y)) {
            return true;
          }
        } else {
          w += windingArc(cx, cy, ry, theta, theta + dTheta, anticlockwise, _x, y);
        }
        xi = Math.cos(theta + dTheta) * rx + cx;
        yi = Math.sin(theta + dTheta) * ry + cy;
        break;
      case CMD2.R:
        x0 = xi = data[i2++];
        y0 = yi = data[i2++];
        var width = data[i2++];
        var height = data[i2++];
        x1 = x0 + width;
        y1 = y0 + height;
        if (isStroke) {
          if (containStroke(x0, y0, x1, y0, lineWidth, x, y) || containStroke(x1, y0, x1, y1, lineWidth, x, y) || containStroke(x1, y1, x0, y1, lineWidth, x, y) || containStroke(x0, y1, x0, y0, lineWidth, x, y)) {
            return true;
          }
        } else {
          w += windingLine(x1, y0, x1, y1, x, y);
          w += windingLine(x0, y1, x0, y0, x, y);
        }
        break;
      case CMD2.Z:
        if (isStroke) {
          if (containStroke(xi, yi, x0, y0, lineWidth, x, y)) {
            return true;
          }
        } else {
          w += windingLine(xi, yi, x0, y0, x, y);
        }
        xi = x0;
        yi = y0;
        break;
    }
  }
  if (!isStroke && !isAroundEqual(yi, y0)) {
    w += windingLine(xi, yi, x0, y0, x, y) || 0;
  }
  return w !== 0;
}
function contain(pathProxy, x, y) {
  return containPath(pathProxy, 0, false, x, y);
}
function containStroke5(pathProxy, lineWidth, x, y) {
  return containPath(pathProxy, lineWidth, true, x, y);
}

// node_modules/zrender/lib/graphic/Path.js
var DEFAULT_PATH_STYLE = defaults({
  fill: "#000",
  stroke: null,
  strokePercent: 1,
  fillOpacity: 1,
  strokeOpacity: 1,
  lineDashOffset: 0,
  lineWidth: 1,
  lineCap: "butt",
  miterLimit: 10,
  strokeNoScale: false,
  strokeFirst: false
}, DEFAULT_COMMON_STYLE);
var DEFAULT_PATH_ANIMATION_PROPS = {
  style: defaults({
    fill: true,
    stroke: true,
    strokePercent: true,
    fillOpacity: true,
    strokeOpacity: true,
    lineDashOffset: true,
    lineWidth: true,
    miterLimit: true
  }, DEFAULT_COMMON_ANIMATION_PROPS.style)
};
var pathCopyParams = TRANSFORMABLE_PROPS.concat([
  "invisible",
  "culling",
  "z",
  "z2",
  "zlevel",
  "parent"
]);
var Path = (function(_super) {
  __extends2(Path2, _super);
  function Path2(opts) {
    return _super.call(this, opts) || this;
  }
  Path2.prototype.update = function() {
    var _this = this;
    _super.prototype.update.call(this);
    var style2 = this.style;
    if (style2.decal) {
      var decalEl = this._decalEl = this._decalEl || new Path2();
      if (decalEl.buildPath === Path2.prototype.buildPath) {
        decalEl.buildPath = function(ctx) {
          _this.buildPath(ctx, _this.shape);
        };
      }
      decalEl.silent = true;
      var decalElStyle = decalEl.style;
      for (var key2 in style2) {
        if (decalElStyle[key2] !== style2[key2]) {
          decalElStyle[key2] = style2[key2];
        }
      }
      decalElStyle.fill = style2.fill ? style2.decal : null;
      decalElStyle.decal = null;
      decalElStyle.shadowColor = null;
      style2.strokeFirst && (decalElStyle.stroke = null);
      for (var i2 = 0; i2 < pathCopyParams.length; ++i2) {
        decalEl[pathCopyParams[i2]] = this[pathCopyParams[i2]];
      }
      decalEl.__dirty |= REDRAW_BIT;
    } else if (this._decalEl) {
      this._decalEl = null;
    }
  };
  Path2.prototype.getDecalElement = function() {
    return this._decalEl;
  };
  Path2.prototype._init = function(props) {
    var keysArr = keys(props);
    this.shape = this.getDefaultShape();
    var defaultStyle = this.getDefaultStyle();
    if (defaultStyle) {
      this.useStyle(defaultStyle);
    }
    for (var i2 = 0; i2 < keysArr.length; i2++) {
      var key2 = keysArr[i2];
      var value = props[key2];
      if (key2 === "style") {
        if (!this.style) {
          this.useStyle(value);
        } else {
          extend(this.style, value);
        }
      } else if (key2 === "shape") {
        extend(this.shape, value);
      } else {
        _super.prototype.attrKV.call(this, key2, value);
      }
    }
    if (!this.style) {
      this.useStyle({});
    }
  };
  Path2.prototype.getDefaultStyle = function() {
    return null;
  };
  Path2.prototype.getDefaultShape = function() {
    return {};
  };
  Path2.prototype.canBeInsideText = function() {
    return this.hasFill();
  };
  Path2.prototype.getInsideTextFill = function() {
    var pathFill = this.style.fill;
    if (pathFill !== "none") {
      if (isString(pathFill)) {
        var fillLum = lum(pathFill, 0);
        if (fillLum > 0.5) {
          return DARK_LABEL_COLOR;
        } else if (fillLum > 0.2) {
          return LIGHTER_LABEL_COLOR;
        }
        return LIGHT_LABEL_COLOR;
      } else if (pathFill) {
        return LIGHT_LABEL_COLOR;
      }
    }
    return DARK_LABEL_COLOR;
  };
  Path2.prototype.getInsideTextStroke = function(textFill) {
    var pathFill = this.style.fill;
    if (isString(pathFill)) {
      var zr = this.__zr;
      var isDarkMode2 = !!(zr && zr.isDarkMode());
      var isDarkLabel = lum(textFill, 0) < DARK_MODE_THRESHOLD;
      if (isDarkMode2 === isDarkLabel) {
        return pathFill;
      }
    }
  };
  Path2.prototype.buildPath = function(ctx, shapeCfg, inBatch) {
  };
  Path2.prototype.pathUpdated = function() {
    this.__dirty &= ~SHAPE_CHANGED_BIT;
  };
  Path2.prototype.getUpdatedPathProxy = function(inBatch) {
    !this.path && this.createPathProxy();
    this.path.beginPath();
    this.buildPath(this.path, this.shape, inBatch);
    return this.path;
  };
  Path2.prototype.createPathProxy = function() {
    this.path = new PathProxy_default(false);
  };
  Path2.prototype.hasStroke = function() {
    var style2 = this.style;
    var stroke = style2.stroke;
    return !(stroke == null || stroke === "none" || !(style2.lineWidth > 0));
  };
  Path2.prototype.hasFill = function() {
    var style2 = this.style;
    var fill = style2.fill;
    return fill != null && fill !== "none";
  };
  Path2.prototype.getBoundingRect = function() {
    var rect = this._rect;
    var style2 = this.style;
    var needsUpdateRect = !rect;
    if (needsUpdateRect) {
      var firstInvoke = false;
      if (!this.path) {
        firstInvoke = true;
        this.createPathProxy();
      }
      var path = this.path;
      if (firstInvoke || this.__dirty & SHAPE_CHANGED_BIT) {
        path.beginPath();
        this.buildPath(path, this.shape, false);
        this.pathUpdated();
      }
      rect = path.getBoundingRect();
    }
    this._rect = rect;
    if (this.hasStroke() && this.path && this.path.len() > 0) {
      var rectStroke = this._rectStroke || (this._rectStroke = rect.clone());
      if (this.__dirty || needsUpdateRect) {
        rectStroke.copy(rect);
        var lineScale = style2.strokeNoScale ? this.getLineScale() : 1;
        var w = style2.lineWidth;
        if (!this.hasFill()) {
          var strokeContainThreshold = this.strokeContainThreshold;
          w = Math.max(w, strokeContainThreshold == null ? 4 : strokeContainThreshold);
        }
        if (lineScale > 1e-10) {
          rectStroke.width += w / lineScale;
          rectStroke.height += w / lineScale;
          rectStroke.x -= w / lineScale / 2;
          rectStroke.y -= w / lineScale / 2;
        }
      }
      return rectStroke;
    }
    return rect;
  };
  Path2.prototype.contain = function(x, y) {
    var localPos = this.transformCoordToLocal(x, y);
    var rect = this.getBoundingRect();
    var style2 = this.style;
    x = localPos[0];
    y = localPos[1];
    if (rect.contain(x, y)) {
      var pathProxy = this.path;
      if (this.hasStroke()) {
        var lineWidth = style2.lineWidth;
        var lineScale = style2.strokeNoScale ? this.getLineScale() : 1;
        if (lineScale > 1e-10) {
          if (!this.hasFill()) {
            lineWidth = Math.max(lineWidth, this.strokeContainThreshold);
          }
          if (containStroke5(pathProxy, lineWidth / lineScale, x, y)) {
            return true;
          }
        }
      }
      if (this.hasFill()) {
        return contain(pathProxy, x, y);
      }
    }
    return false;
  };
  Path2.prototype.dirtyShape = function() {
    this.__dirty |= SHAPE_CHANGED_BIT;
    if (this._rect) {
      this._rect = null;
    }
    if (this._decalEl) {
      this._decalEl.dirtyShape();
    }
    this.markRedraw();
  };
  Path2.prototype.dirty = function() {
    this.dirtyStyle();
    this.dirtyShape();
  };
  Path2.prototype.animateShape = function(loop) {
    return this.animate("shape", loop);
  };
  Path2.prototype.updateDuringAnimation = function(targetKey) {
    if (targetKey === "style") {
      this.dirtyStyle();
    } else if (targetKey === "shape") {
      this.dirtyShape();
    } else {
      this.markRedraw();
    }
  };
  Path2.prototype.attrKV = function(key2, value) {
    if (key2 === "shape") {
      this.setShape(value);
    } else {
      _super.prototype.attrKV.call(this, key2, value);
    }
  };
  Path2.prototype.setShape = function(keyOrObj, value) {
    var shape = this.shape;
    if (!shape) {
      shape = this.shape = {};
    }
    if (typeof keyOrObj === "string") {
      shape[keyOrObj] = value;
    } else {
      extend(shape, keyOrObj);
    }
    this.dirtyShape();
    return this;
  };
  Path2.prototype.shapeChanged = function() {
    return !!(this.__dirty & SHAPE_CHANGED_BIT);
  };
  Path2.prototype.createStyle = function(obj) {
    return createObject(DEFAULT_PATH_STYLE, obj);
  };
  Path2.prototype._innerSaveToNormal = function(toState) {
    _super.prototype._innerSaveToNormal.call(this, toState);
    var normalState = this._normalState;
    if (toState.shape && !normalState.shape) {
      normalState.shape = extend({}, this.shape);
    }
  };
  Path2.prototype._applyStateObj = function(stateName, state2, normalState, keepCurrentStates, transition2, animationCfg) {
    _super.prototype._applyStateObj.call(this, stateName, state2, normalState, keepCurrentStates, transition2, animationCfg);
    if (this.__inHover === IN_HOVER_LAYER_KIND_ONLY_STYLE_CHANGE) {
      return;
    }
    var needsRestoreToNormal = !(state2 && keepCurrentStates);
    var targetShape;
    if (state2 && state2.shape) {
      if (transition2) {
        if (keepCurrentStates) {
          targetShape = state2.shape;
        } else {
          targetShape = extend({}, normalState.shape);
          extend(targetShape, state2.shape);
        }
      } else {
        targetShape = extend({}, keepCurrentStates ? this.shape : normalState.shape);
        extend(targetShape, state2.shape);
      }
    } else if (needsRestoreToNormal) {
      targetShape = normalState.shape;
    }
    if (targetShape) {
      if (transition2) {
        this.shape = extend({}, this.shape);
        var targetShapePrimaryProps = {};
        var shapeKeys = keys(targetShape);
        for (var i2 = 0; i2 < shapeKeys.length; i2++) {
          var key2 = shapeKeys[i2];
          if (typeof targetShape[key2] === "object") {
            this.shape[key2] = targetShape[key2];
          } else {
            targetShapePrimaryProps[key2] = targetShape[key2];
          }
        }
        this._transitionState(stateName, {
          shape: targetShapePrimaryProps
        }, animationCfg);
      } else {
        this.shape = targetShape;
        this.dirtyShape();
      }
    }
  };
  Path2.prototype._mergeStates = function(states) {
    var mergedState = _super.prototype._mergeStates.call(this, states);
    var mergedShape;
    for (var i2 = 0; i2 < states.length; i2++) {
      var state2 = states[i2];
      if (state2.shape) {
        mergedShape = mergedShape || {};
        this._mergeStyle(mergedShape, state2.shape);
      }
    }
    if (mergedShape) {
      mergedState.shape = mergedShape;
    }
    return mergedState;
  };
  Path2.prototype.getAnimationStyleProps = function() {
    return DEFAULT_PATH_ANIMATION_PROPS;
  };
  Path2.prototype.isZeroArea = function() {
    return false;
  };
  Path2.extend = function(defaultProps) {
    var Sub = (function(_super2) {
      __extends2(Sub2, _super2);
      function Sub2(opts) {
        var _this = _super2.call(this, opts) || this;
        defaultProps.init && defaultProps.init.call(_this, opts);
        return _this;
      }
      Sub2.prototype.getDefaultStyle = function() {
        return clone(defaultProps.style);
      };
      Sub2.prototype.getDefaultShape = function() {
        return clone(defaultProps.shape);
      };
      return Sub2;
    })(Path2);
    for (var key2 in defaultProps) {
      if (typeof defaultProps[key2] === "function") {
        Sub.prototype[key2] = defaultProps[key2];
      }
    }
    return Sub;
  };
  Path2.initDefaultProps = (function() {
    var pathProto = Path2.prototype;
    pathProto.type = "path";
    pathProto.strokeContainThreshold = 5;
    pathProto.segmentIgnoreThreshold = 0;
    pathProto.subPixelOptimize = false;
    pathProto.autoBatch = false;
    pathProto.__dirty = REDRAW_BIT | STYLE_CHANGED_BIT | SHAPE_CHANGED_BIT;
  })();
  return Path2;
})(Displayable_default);
var Path_default = Path;

// node_modules/zrender/lib/graphic/TSpan.js
var DEFAULT_TSPAN_STYLE = defaults({
  strokeFirst: true,
  font: DEFAULT_FONT,
  x: 0,
  y: 0,
  textAlign: "left",
  textBaseline: "top",
  miterLimit: 2
}, DEFAULT_PATH_STYLE);
var TSpan = (function(_super) {
  __extends2(TSpan2, _super);
  function TSpan2() {
    return _super !== null && _super.apply(this, arguments) || this;
  }
  TSpan2.prototype.hasStroke = function() {
    return tSpanHasStroke(this.style);
  };
  TSpan2.prototype.hasFill = function() {
    var style2 = this.style;
    var fill = style2.fill;
    return fill != null && fill !== "none";
  };
  TSpan2.prototype.createStyle = function(obj) {
    return createObject(DEFAULT_TSPAN_STYLE, obj);
  };
  TSpan2.prototype.setBoundingRect = function(rect) {
    this._rect = rect;
  };
  TSpan2.prototype.getBoundingRect = function() {
    if (!this._rect) {
      this._rect = tSpanCreateBoundingRect(this.style);
    }
    return this._rect;
  };
  TSpan2.initDefaultProps = (function() {
    var tspanProto = TSpan2.prototype;
    tspanProto.dirtyRectTolerance = 10;
  })();
  return TSpan2;
})(Displayable_default);
TSpan.prototype.type = "tspan";
var TSpan_default = TSpan;

// node_modules/zrender/lib/graphic/Image.js
var DEFAULT_IMAGE_STYLE = defaults({
  x: 0,
  y: 0
}, DEFAULT_COMMON_STYLE);
var DEFAULT_IMAGE_ANIMATION_PROPS = {
  style: defaults({
    x: true,
    y: true,
    width: true,
    height: true,
    sx: true,
    sy: true,
    sWidth: true,
    sHeight: true
  }, DEFAULT_COMMON_ANIMATION_PROPS.style)
};
function isImageLike(source) {
  return !!(source && typeof source !== "string" && source.width && source.height);
}
var ZRImage = (function(_super) {
  __extends2(ZRImage2, _super);
  function ZRImage2() {
    return _super !== null && _super.apply(this, arguments) || this;
  }
  ZRImage2.prototype.createStyle = function(obj) {
    return createObject(DEFAULT_IMAGE_STYLE, obj);
  };
  ZRImage2.prototype._getSize = function(dim) {
    var style2 = this.style;
    var size = style2[dim];
    if (size != null) {
      return size;
    }
    var imageSource = isImageLike(style2.image) ? style2.image : this.__image;
    if (!imageSource) {
      return 0;
    }
    var otherDim = dim === "width" ? "height" : "width";
    var otherDimSize = style2[otherDim];
    if (otherDimSize == null) {
      return imageSource[dim];
    } else {
      return imageSource[dim] / imageSource[otherDim] * otherDimSize;
    }
  };
  ZRImage2.prototype.getWidth = function() {
    return this._getSize("width");
  };
  ZRImage2.prototype.getHeight = function() {
    return this._getSize("height");
  };
  ZRImage2.prototype.getAnimationStyleProps = function() {
    return DEFAULT_IMAGE_ANIMATION_PROPS;
  };
  ZRImage2.prototype.getBoundingRect = function() {
    var style2 = this.style;
    if (!this._rect) {
      this._rect = new BoundingRect_default(style2.x || 0, style2.y || 0, this.getWidth(), this.getHeight());
    }
    return this._rect;
  };
  return ZRImage2;
})(Displayable_default);
ZRImage.prototype.type = "image";
var Image_default = ZRImage;

// node_modules/zrender/lib/graphic/helper/roundRect.js
function buildPath(ctx, shape) {
  var x = shape.x;
  var y = shape.y;
  var width = shape.width;
  var height = shape.height;
  var r = shape.r;
  var r1;
  var r2;
  var r3;
  var r4;
  if (width < 0) {
    x = x + width;
    width = -width;
  }
  if (height < 0) {
    y = y + height;
    height = -height;
  }
  if (typeof r === "number") {
    r1 = r2 = r3 = r4 = r;
  } else if (r instanceof Array) {
    if (r.length === 1) {
      r1 = r2 = r3 = r4 = r[0];
    } else if (r.length === 2) {
      r1 = r3 = r[0];
      r2 = r4 = r[1];
    } else if (r.length === 3) {
      r1 = r[0];
      r2 = r4 = r[1];
      r3 = r[2];
    } else {
      r1 = r[0];
      r2 = r[1];
      r3 = r[2];
      r4 = r[3];
    }
  } else {
    r1 = r2 = r3 = r4 = 0;
  }
  var total;
  if (r1 + r2 > width) {
    total = r1 + r2;
    r1 *= width / total;
    r2 *= width / total;
  }
  if (r3 + r4 > width) {
    total = r3 + r4;
    r3 *= width / total;
    r4 *= width / total;
  }
  if (r2 + r3 > height) {
    total = r2 + r3;
    r2 *= height / total;
    r3 *= height / total;
  }
  if (r1 + r4 > height) {
    total = r1 + r4;
    r1 *= height / total;
    r4 *= height / total;
  }
  ctx.moveTo(x + r1, y);
  ctx.lineTo(x + width - r2, y);
  r2 !== 0 && ctx.arc(x + width - r2, y + r2, r2, -Math.PI / 2, 0);
  ctx.lineTo(x + width, y + height - r3);
  r3 !== 0 && ctx.arc(x + width - r3, y + height - r3, r3, 0, Math.PI / 2);
  ctx.lineTo(x + r4, y + height);
  r4 !== 0 && ctx.arc(x + r4, y + height - r4, r4, Math.PI / 2, Math.PI);
  ctx.lineTo(x, y + r1);
  r1 !== 0 && ctx.arc(x + r1, y + r1, r1, Math.PI, Math.PI * 1.5);
  ctx.closePath();
}

// node_modules/zrender/lib/graphic/helper/subPixelOptimize.js
var round = Math.round;
function subPixelOptimizeLine(outputShape, inputShape, style2) {
  if (!inputShape) {
    return;
  }
  var x1 = inputShape.x1;
  var x2 = inputShape.x2;
  var y1 = inputShape.y1;
  var y2 = inputShape.y2;
  outputShape.x1 = x1;
  outputShape.x2 = x2;
  outputShape.y1 = y1;
  outputShape.y2 = y2;
  var lineWidth = style2 && style2.lineWidth;
  if (!lineWidth) {
    return outputShape;
  }
  if (round(x1 * 2) === round(x2 * 2)) {
    outputShape.x1 = outputShape.x2 = subPixelOptimize(x1, lineWidth, true);
  }
  if (round(y1 * 2) === round(y2 * 2)) {
    outputShape.y1 = outputShape.y2 = subPixelOptimize(y1, lineWidth, true);
  }
  return outputShape;
}
function subPixelOptimizeRect(outputShape, inputShape, style2) {
  if (!inputShape) {
    return;
  }
  var originX = inputShape.x;
  var originY = inputShape.y;
  var originWidth = inputShape.width;
  var originHeight = inputShape.height;
  outputShape.x = originX;
  outputShape.y = originY;
  outputShape.width = originWidth;
  outputShape.height = originHeight;
  var lineWidth = style2 && style2.lineWidth;
  if (!lineWidth) {
    return outputShape;
  }
  outputShape.x = subPixelOptimize(originX, lineWidth, true);
  outputShape.y = subPixelOptimize(originY, lineWidth, true);
  outputShape.width = Math.max(subPixelOptimize(originX + originWidth, lineWidth, false) - outputShape.x, originWidth === 0 ? 0 : 1);
  outputShape.height = Math.max(subPixelOptimize(originY + originHeight, lineWidth, false) - outputShape.y, originHeight === 0 ? 0 : 1);
  return outputShape;
}
function subPixelOptimize(position, lineWidth, positiveOrNegative) {
  if (!lineWidth) {
    return position;
  }
  var doubledPosition = round(position * 2);
  return (doubledPosition + round(lineWidth)) % 2 === 0 ? doubledPosition / 2 : (doubledPosition + (positiveOrNegative ? 1 : -1)) / 2;
}

// node_modules/zrender/lib/graphic/shape/Rect.js
var RectShape = /* @__PURE__ */ (function() {
  function RectShape2() {
    this.x = 0;
    this.y = 0;
    this.width = 0;
    this.height = 0;
  }
  return RectShape2;
})();
var subPixelOptimizeOutputShape = {};
var Rect = (function(_super) {
  __extends2(Rect4, _super);
  function Rect4(opts) {
    return _super.call(this, opts) || this;
  }
  Rect4.prototype.getDefaultShape = function() {
    return new RectShape();
  };
  Rect4.prototype.buildPath = function(ctx, shape) {
    var x;
    var y;
    var width;
    var height;
    if (this.subPixelOptimize) {
      var optimizedShape = subPixelOptimizeRect(subPixelOptimizeOutputShape, shape, this.style);
      x = optimizedShape.x;
      y = optimizedShape.y;
      width = optimizedShape.width;
      height = optimizedShape.height;
      optimizedShape.r = shape.r;
      shape = optimizedShape;
    } else {
      x = shape.x;
      y = shape.y;
      width = shape.width;
      height = shape.height;
    }
    if (!shape.r) {
      ctx.rect(x, y, width, height);
    } else {
      buildPath(ctx, shape);
    }
  };
  Rect4.prototype.isZeroArea = function() {
    return !this.shape.width || !this.shape.height;
  };
  return Rect4;
})(Path_default);
Rect.prototype.type = "rect";
var Rect_default = Rect;

// node_modules/zrender/lib/graphic/Text.js
var DEFAULT_RICH_TEXT_COLOR = {
  fill: "#000"
};
var DEFAULT_STROKE_LINE_WIDTH = 2;
var tmpCITOverflowAreaOut = {};
var DEFAULT_TEXT_ANIMATION_PROPS = {
  style: defaults({
    fill: true,
    stroke: true,
    fillOpacity: true,
    strokeOpacity: true,
    lineWidth: true,
    fontSize: true,
    lineHeight: true,
    width: true,
    height: true,
    textShadowColor: true,
    textShadowBlur: true,
    textShadowOffsetX: true,
    textShadowOffsetY: true,
    backgroundColor: true,
    padding: true,
    borderColor: true,
    borderWidth: true,
    borderRadius: true
  }, DEFAULT_COMMON_ANIMATION_PROPS.style)
};
var ZRText = (function(_super) {
  __extends2(ZRText2, _super);
  function ZRText2(opts) {
    var _this = _super.call(this) || this;
    _this.type = "text";
    _this._children = [];
    _this._defaultStyle = DEFAULT_RICH_TEXT_COLOR;
    _this.attr(opts);
    return _this;
  }
  ZRText2.prototype.childrenRef = function() {
    return this._children;
  };
  ZRText2.prototype.update = function() {
    _super.prototype.update.call(this);
    if (this.styleChanged()) {
      this._updateSubTexts();
    }
    for (var i2 = 0; i2 < this._children.length; i2++) {
      var child = this._children[i2];
      child.zlevel = this.zlevel;
      child.z = this.z;
      child.z2 = this.z2;
      child.culling = this.culling;
      child.cursor = this.cursor;
      child.invisible = this.invisible;
    }
  };
  ZRText2.prototype.updateTransform = function() {
    var innerTransformable = this.innerTransformable;
    if (innerTransformable) {
      innerTransformable.updateTransform();
      if (innerTransformable.transform) {
        this.transform = innerTransformable.transform;
      }
    } else {
      _super.prototype.updateTransform.call(this);
    }
  };
  ZRText2.prototype.getLocalTransform = function(m2) {
    var innerTransformable = this.innerTransformable;
    return innerTransformable ? innerTransformable.getLocalTransform(m2) : _super.prototype.getLocalTransform.call(this, m2);
  };
  ZRText2.prototype.getComputedTransform = function() {
    if (this.__hostTarget) {
      this.__hostTarget.getComputedTransform();
      this.__hostTarget.updateInnerText(true);
    }
    return _super.prototype.getComputedTransform.call(this);
  };
  ZRText2.prototype._updateSubTexts = function() {
    this._childCursor = 0;
    normalizeTextStyle(this.style);
    this.style.rich ? this._updateRichTexts() : this._updatePlainTexts();
    this._children.length = this._childCursor;
    this.styleUpdated();
  };
  ZRText2.prototype.addSelfToZr = function(zr) {
    _super.prototype.addSelfToZr.call(this, zr);
    for (var i2 = 0; i2 < this._children.length; i2++) {
      this._children[i2].__zr = zr;
    }
  };
  ZRText2.prototype.removeSelfFromZr = function(zr) {
    _super.prototype.removeSelfFromZr.call(this, zr);
    for (var i2 = 0; i2 < this._children.length; i2++) {
      this._children[i2].__zr = null;
    }
  };
  ZRText2.prototype.getBoundingRect = function() {
    if (this.styleChanged()) {
      this._updateSubTexts();
    }
    if (!this._rect) {
      var tmpRect3 = new BoundingRect_default(0, 0, 0, 0);
      var children = this._children;
      var tmpMat = [];
      var rect = null;
      for (var i2 = 0; i2 < children.length; i2++) {
        var child = children[i2];
        var childRect = child.getBoundingRect();
        var transform = child.getLocalTransform(tmpMat);
        if (transform) {
          tmpRect3.copy(childRect);
          tmpRect3.applyTransform(transform);
          rect = rect || tmpRect3.clone();
          rect.union(tmpRect3);
        } else {
          rect = rect || childRect.clone();
          rect.union(childRect);
        }
      }
      this._rect = rect || tmpRect3;
    }
    return this._rect;
  };
  ZRText2.prototype.setDefaultTextStyle = function(defaultTextStyle) {
    this._defaultStyle = defaultTextStyle || DEFAULT_RICH_TEXT_COLOR;
  };
  ZRText2.prototype.setTextContent = function(textContent) {
    if (true) {
      throw new Error("Can't attach text on another text");
    }
  };
  ZRText2.prototype._mergeStyle = function(targetStyle, sourceStyle) {
    if (!sourceStyle) {
      return targetStyle;
    }
    var sourceRich = sourceStyle.rich;
    var targetRich = targetStyle.rich || sourceRich && {};
    extend(targetStyle, sourceStyle);
    if (sourceRich && targetRich) {
      this._mergeRich(targetRich, sourceRich);
      targetStyle.rich = targetRich;
    } else if (targetRich) {
      targetStyle.rich = targetRich;
    }
    return targetStyle;
  };
  ZRText2.prototype._mergeRich = function(targetRich, sourceRich) {
    var richNames = keys(sourceRich);
    for (var i2 = 0; i2 < richNames.length; i2++) {
      var richName = richNames[i2];
      targetRich[richName] = targetRich[richName] || {};
      extend(targetRich[richName], sourceRich[richName]);
    }
  };
  ZRText2.prototype.getAnimationStyleProps = function() {
    return DEFAULT_TEXT_ANIMATION_PROPS;
  };
  ZRText2.prototype._getOrCreateChild = function(Ctor) {
    var child = this._children[this._childCursor];
    if (!child || !(child instanceof Ctor)) {
      child = new Ctor();
    }
    this._children[this._childCursor++] = child;
    child.__zr = this.__zr;
    child.parent = this;
    return child;
  };
  ZRText2.prototype._updatePlainTexts = function() {
    var style2 = this.style;
    var textFont = style2.font || DEFAULT_FONT;
    var textPadding = style2.padding;
    var defaultStyle = this._defaultStyle;
    var baseX = style2.x || 0;
    var baseY = style2.y || 0;
    var textAlign = style2.align || defaultStyle.align || "left";
    var verticalAlign = style2.verticalAlign || defaultStyle.verticalAlign || "top";
    calcInnerTextOverflowArea(tmpCITOverflowAreaOut, defaultStyle.overflowRect, baseX, baseY, textAlign, verticalAlign);
    baseX = tmpCITOverflowAreaOut.baseX;
    baseY = tmpCITOverflowAreaOut.baseY;
    var text = getStyleText(style2);
    var contentBlock = parsePlainText(text, style2, tmpCITOverflowAreaOut.outerWidth, tmpCITOverflowAreaOut.outerHeight);
    var needDrawBg = needDrawBackground(style2);
    var bgColorDrawn = !!style2.backgroundColor;
    var outerHeight = contentBlock.outerHeight;
    var outerWidth = contentBlock.outerWidth;
    var textLines = contentBlock.lines;
    var lineHeight = contentBlock.lineHeight;
    this.isTruncated = !!contentBlock.isTruncated;
    var textX = baseX;
    var textY = adjustTextY(baseY, contentBlock.contentHeight, verticalAlign);
    if (needDrawBg || textPadding) {
      var boxX = adjustTextX(baseX, outerWidth, textAlign);
      var boxY = adjustTextY(baseY, outerHeight, verticalAlign);
      needDrawBg && this._renderBackground(style2, style2, boxX, boxY, outerWidth, outerHeight);
    }
    textY += lineHeight / 2;
    if (textPadding) {
      textX = getTextXForPadding(baseX, textAlign, textPadding);
      if (verticalAlign === "top") {
        textY += textPadding[0];
      } else if (verticalAlign === "bottom") {
        textY -= textPadding[2];
      }
    }
    var defaultLineWidth = 0;
    var usingDefaultStroke = false;
    var useDefaultFill = false;
    var textFill = getFill("fill" in style2 ? style2.fill : (useDefaultFill = true, defaultStyle.fill));
    var textStroke = getStroke("stroke" in style2 ? style2.stroke : !bgColorDrawn && (!defaultStyle.autoStroke || useDefaultFill) ? (defaultLineWidth = DEFAULT_STROKE_LINE_WIDTH, usingDefaultStroke = true, defaultStyle.stroke) : null);
    var hasShadow2 = style2.textShadowBlur > 0;
    for (var i2 = 0; i2 < textLines.length; i2++) {
      var el = this._getOrCreateChild(TSpan_default);
      var subElStyle = el.createStyle();
      el.useStyle(subElStyle);
      subElStyle.text = textLines[i2];
      subElStyle.x = textX;
      subElStyle.y = textY;
      if (textAlign) {
        subElStyle.textAlign = textAlign;
      }
      subElStyle.textBaseline = "middle";
      subElStyle.opacity = style2.opacity;
      subElStyle.strokeFirst = true;
      if (hasShadow2) {
        subElStyle.shadowBlur = style2.textShadowBlur || 0;
        subElStyle.shadowColor = style2.textShadowColor || "transparent";
        subElStyle.shadowOffsetX = style2.textShadowOffsetX || 0;
        subElStyle.shadowOffsetY = style2.textShadowOffsetY || 0;
      }
      subElStyle.stroke = textStroke;
      subElStyle.fill = textFill;
      if (textStroke) {
        subElStyle.lineWidth = style2.lineWidth || defaultLineWidth;
        subElStyle.lineDash = style2.lineDash;
        subElStyle.lineDashOffset = style2.lineDashOffset || 0;
      }
      subElStyle.font = textFont;
      setSeparateFont(subElStyle, style2);
      textY += lineHeight;
      el.setBoundingRect(tSpanCreateBoundingRect2(subElStyle, contentBlock.contentWidth, contentBlock.calculatedLineHeight, usingDefaultStroke ? 0 : null));
    }
  };
  ZRText2.prototype._updateRichTexts = function() {
    var style2 = this.style;
    var defaultStyle = this._defaultStyle;
    var textAlign = style2.align || defaultStyle.align;
    var verticalAlign = style2.verticalAlign || defaultStyle.verticalAlign;
    var baseX = style2.x || 0;
    var baseY = style2.y || 0;
    calcInnerTextOverflowArea(tmpCITOverflowAreaOut, defaultStyle.overflowRect, baseX, baseY, textAlign, verticalAlign);
    baseX = tmpCITOverflowAreaOut.baseX;
    baseY = tmpCITOverflowAreaOut.baseY;
    var text = getStyleText(style2);
    var contentBlock = parseRichText(text, style2, tmpCITOverflowAreaOut.outerWidth, tmpCITOverflowAreaOut.outerHeight, textAlign);
    var contentWidth = contentBlock.width;
    var outerWidth = contentBlock.outerWidth;
    var outerHeight = contentBlock.outerHeight;
    var textPadding = style2.padding;
    this.isTruncated = !!contentBlock.isTruncated;
    var boxX = adjustTextX(baseX, outerWidth, textAlign);
    var boxY = adjustTextY(baseY, outerHeight, verticalAlign);
    var xLeft = boxX;
    var lineTop = boxY;
    if (textPadding) {
      xLeft += textPadding[3];
      lineTop += textPadding[0];
    }
    var xRight = xLeft + contentWidth;
    if (needDrawBackground(style2)) {
      this._renderBackground(style2, style2, boxX, boxY, outerWidth, outerHeight);
    }
    var bgColorDrawn = !!style2.backgroundColor;
    for (var i2 = 0; i2 < contentBlock.lines.length; i2++) {
      var line = contentBlock.lines[i2];
      var tokens2 = line.tokens;
      var tokenCount = tokens2.length;
      var lineHeight = line.lineHeight;
      var remainedWidth = line.width;
      var leftIndex = 0;
      var lineXLeft = xLeft;
      var lineXRight = xRight;
      var rightIndex = tokenCount - 1;
      var token = void 0;
      while (leftIndex < tokenCount && (token = tokens2[leftIndex], !token.align || token.align === "left")) {
        this._placeToken(token, style2, lineHeight, lineTop, lineXLeft, "left", bgColorDrawn);
        remainedWidth -= token.width;
        lineXLeft += token.width;
        leftIndex++;
      }
      while (rightIndex >= 0 && (token = tokens2[rightIndex], token.align === "right")) {
        this._placeToken(token, style2, lineHeight, lineTop, lineXRight, "right", bgColorDrawn);
        remainedWidth -= token.width;
        lineXRight -= token.width;
        rightIndex--;
      }
      lineXLeft += (contentWidth - (lineXLeft - xLeft) - (xRight - lineXRight) - remainedWidth) / 2;
      while (leftIndex <= rightIndex) {
        token = tokens2[leftIndex];
        this._placeToken(token, style2, lineHeight, lineTop, lineXLeft + token.width / 2, "center", bgColorDrawn);
        lineXLeft += token.width;
        leftIndex++;
      }
      lineTop += lineHeight;
    }
  };
  ZRText2.prototype._placeToken = function(token, style2, lineHeight, lineTop, x, textAlign, parentBgColorDrawn) {
    var tokenStyle = style2.rich[token.styleName] || {};
    tokenStyle.text = token.text;
    var verticalAlign = token.verticalAlign;
    var y = lineTop + lineHeight / 2;
    if (verticalAlign === "top") {
      y = lineTop + token.height / 2;
    } else if (verticalAlign === "bottom") {
      y = lineTop + lineHeight - token.height / 2;
    }
    var needDrawBg = !token.isLineHolder && needDrawBackground(tokenStyle);
    needDrawBg && this._renderBackground(tokenStyle, style2, textAlign === "right" ? x - token.width : textAlign === "center" ? x - token.width / 2 : x, y - token.height / 2, token.width, token.height);
    var bgColorDrawn = !!tokenStyle.backgroundColor;
    var textPadding = token.textPadding;
    if (textPadding) {
      x = getTextXForPadding(x, textAlign, textPadding);
      y -= token.height / 2 - textPadding[0] - token.innerHeight / 2;
    }
    var el = this._getOrCreateChild(TSpan_default);
    var subElStyle = el.createStyle();
    el.useStyle(subElStyle);
    var defaultStyle = this._defaultStyle;
    var useDefaultFill = false;
    var defaultLineWidth = 0;
    var usingDefaultStroke = false;
    var textFill = getFill("fill" in tokenStyle ? tokenStyle.fill : "fill" in style2 ? style2.fill : (useDefaultFill = true, defaultStyle.fill));
    var textStroke = getStroke("stroke" in tokenStyle ? tokenStyle.stroke : "stroke" in style2 ? style2.stroke : !bgColorDrawn && !parentBgColorDrawn && (!defaultStyle.autoStroke || useDefaultFill) ? (defaultLineWidth = DEFAULT_STROKE_LINE_WIDTH, usingDefaultStroke = true, defaultStyle.stroke) : null);
    var hasShadow2 = tokenStyle.textShadowBlur > 0 || style2.textShadowBlur > 0;
    subElStyle.text = token.text;
    subElStyle.x = x;
    subElStyle.y = y;
    if (hasShadow2) {
      subElStyle.shadowBlur = tokenStyle.textShadowBlur || style2.textShadowBlur || 0;
      subElStyle.shadowColor = tokenStyle.textShadowColor || style2.textShadowColor || "transparent";
      subElStyle.shadowOffsetX = tokenStyle.textShadowOffsetX || style2.textShadowOffsetX || 0;
      subElStyle.shadowOffsetY = tokenStyle.textShadowOffsetY || style2.textShadowOffsetY || 0;
    }
    subElStyle.textAlign = textAlign;
    subElStyle.textBaseline = "middle";
    subElStyle.font = token.font || DEFAULT_FONT;
    subElStyle.opacity = retrieve3(tokenStyle.opacity, style2.opacity, 1);
    setSeparateFont(subElStyle, tokenStyle);
    if (textStroke) {
      subElStyle.lineWidth = retrieve3(tokenStyle.lineWidth, style2.lineWidth, defaultLineWidth);
      subElStyle.lineDash = retrieve2(tokenStyle.lineDash, style2.lineDash);
      subElStyle.lineDashOffset = style2.lineDashOffset || 0;
      subElStyle.stroke = textStroke;
    }
    if (textFill) {
      subElStyle.fill = textFill;
    }
    el.setBoundingRect(tSpanCreateBoundingRect2(subElStyle, token.contentWidth, token.contentHeight, usingDefaultStroke ? 0 : null));
  };
  ZRText2.prototype._renderBackground = function(style2, topStyle, x, y, width, height) {
    var textBackgroundColor = style2.backgroundColor;
    var textBorderWidth = style2.borderWidth;
    var textBorderColor = style2.borderColor;
    var isImageBg = textBackgroundColor && textBackgroundColor.image;
    var isPlainOrGradientBg = textBackgroundColor && !isImageBg;
    var textBorderRadius = style2.borderRadius;
    var self2 = this;
    var rectEl;
    var imgEl;
    if (isPlainOrGradientBg || style2.lineHeight || textBorderWidth && textBorderColor) {
      rectEl = this._getOrCreateChild(Rect_default);
      rectEl.useStyle(rectEl.createStyle());
      rectEl.style.fill = null;
      var rectShape = rectEl.shape;
      rectShape.x = x;
      rectShape.y = y;
      rectShape.width = width;
      rectShape.height = height;
      rectShape.r = textBorderRadius;
      rectEl.dirtyShape();
    }
    if (isPlainOrGradientBg) {
      var rectStyle = rectEl.style;
      rectStyle.fill = textBackgroundColor || null;
      rectStyle.fillOpacity = retrieve2(style2.fillOpacity, 1);
    } else if (isImageBg) {
      imgEl = this._getOrCreateChild(Image_default);
      imgEl.onload = function() {
        self2.dirtyStyle();
      };
      var imgStyle = imgEl.style;
      imgStyle.image = textBackgroundColor.image;
      imgStyle.x = x;
      imgStyle.y = y;
      imgStyle.width = width;
      imgStyle.height = height;
    }
    if (textBorderWidth && textBorderColor) {
      var rectStyle = rectEl.style;
      rectStyle.lineWidth = textBorderWidth;
      rectStyle.stroke = textBorderColor;
      rectStyle.strokeOpacity = retrieve2(style2.strokeOpacity, 1);
      rectStyle.lineDash = style2.borderDash;
      rectStyle.lineDashOffset = style2.borderDashOffset || 0;
      rectEl.strokeContainThreshold = 0;
      if (rectEl.hasFill() && rectEl.hasStroke()) {
        rectStyle.strokeFirst = true;
        rectStyle.lineWidth *= 2;
      }
    }
    var commonStyle = (rectEl || imgEl).style;
    commonStyle.shadowBlur = style2.shadowBlur || 0;
    commonStyle.shadowColor = style2.shadowColor || "transparent";
    commonStyle.shadowOffsetX = style2.shadowOffsetX || 0;
    commonStyle.shadowOffsetY = style2.shadowOffsetY || 0;
    commonStyle.opacity = retrieve3(style2.opacity, topStyle.opacity, 1);
  };
  ZRText2.makeFont = function(style2) {
    var font = "";
    if (hasSeparateFont(style2)) {
      font = [
        style2.fontStyle,
        style2.fontWeight,
        parseFontSize(style2.fontSize),
        style2.fontFamily || "sans-serif"
      ].join(" ");
    }
    return font && trim(font) || style2.textFont || style2.font;
  };
  return ZRText2;
})(Displayable_default);
var VALID_TEXT_ALIGN = { left: true, right: 1, center: 1 };
var VALID_TEXT_VERTICAL_ALIGN = { top: 1, bottom: 1, middle: 1 };
var FONT_PARTS = ["fontStyle", "fontWeight", "fontSize", "fontFamily"];
function parseFontSize(fontSize) {
  if (typeof fontSize === "string" && (fontSize.indexOf("px") !== -1 || fontSize.indexOf("rem") !== -1 || fontSize.indexOf("em") !== -1)) {
    return fontSize;
  } else if (!isNaN(+fontSize)) {
    return fontSize + "px";
  } else {
    return DEFAULT_FONT_SIZE + "px";
  }
}
function setSeparateFont(targetStyle, sourceStyle) {
  for (var i2 = 0; i2 < FONT_PARTS.length; i2++) {
    var fontProp = FONT_PARTS[i2];
    var val = sourceStyle[fontProp];
    if (val != null) {
      targetStyle[fontProp] = val;
    }
  }
}
function hasSeparateFont(style2) {
  return style2.fontSize != null || style2.fontFamily || style2.fontWeight;
}
function normalizeTextStyle(style2) {
  normalizeStyle(style2);
  each(style2.rich, normalizeStyle);
  return style2;
}
function normalizeStyle(style2) {
  if (style2) {
    style2.font = ZRText.makeFont(style2);
    var textAlign = style2.align;
    textAlign === "middle" && (textAlign = "center");
    style2.align = textAlign == null || VALID_TEXT_ALIGN[textAlign] ? textAlign : "left";
    var verticalAlign = style2.verticalAlign;
    verticalAlign === "center" && (verticalAlign = "middle");
    style2.verticalAlign = verticalAlign == null || VALID_TEXT_VERTICAL_ALIGN[verticalAlign] ? verticalAlign : "top";
    var textPadding = style2.padding;
    if (textPadding) {
      style2.padding = normalizeCssArray(style2.padding);
    }
  }
}
function getStroke(stroke, lineWidth) {
  return stroke == null || lineWidth <= 0 || stroke === "transparent" || stroke === "none" ? null : stroke.image || stroke.colorStops ? "#000" : stroke;
}
function getFill(fill) {
  return fill == null || fill === "none" ? null : fill.image || fill.colorStops ? "#000" : fill;
}
function getTextXForPadding(x, textAlign, textPadding) {
  return textAlign === "right" ? x - textPadding[1] : textAlign === "center" ? x + textPadding[3] / 2 - textPadding[1] / 2 : x + textPadding[3];
}
function getStyleText(style2) {
  var text = style2.text;
  text != null && (text += "");
  return text;
}
function needDrawBackground(style2) {
  return !!(style2.backgroundColor || style2.lineHeight || style2.borderWidth && style2.borderColor);
}
var Text_default = ZRText;

// node_modules/echarts/lib/util/number.js
var RADIAN_EPSILON = 1e-4;
var TO_FIXED_SUPPORTED_PRECISION_MAX = 20;
var DEFAULT_PRECISION_FOR_ROUNDING_ERROR = 14;
function _trim(str) {
  return str.replace(/^\s+|\s+$/g, "");
}
var mathMin4 = Math.min;
var mathMax4 = Math.max;
var mathAbs3 = Math.abs;
var mathRound2 = Math.round;
var mathFloor = Math.floor;
var mathCeil = Math.ceil;
var mathPow2 = Math.pow;
var mathLog = Math.log;
var mathLN10 = Math.LN10;
var mathPI = Math.PI;
var mathRandom = Math.random;
function linearMap(val, domain, range, clamp2) {
  var d0 = domain[0];
  var d1 = domain[1];
  var r0 = range[0];
  var r1 = range[1];
  var subDomain = d1 - d0;
  var subRange = r1 - r0;
  if (subDomain === 0) {
    return subRange === 0 ? r0 : (r0 + r1) / 2;
  }
  if (clamp2) {
    if (subDomain > 0) {
      if (val <= d0) {
        return r0;
      } else if (val >= d1) {
        return r1;
      }
    } else {
      if (val >= d0) {
        return r0;
      } else if (val <= d1) {
        return r1;
      }
    }
  } else {
    if (val === d0) {
      return r0;
    }
    if (val === d1) {
      return r1;
    }
  }
  return (val - d0) / subDomain * subRange + r0;
}
var parsePercent2 = parsePositionOption;
function parsePositionOption(option, percentBase, percentOffset) {
  switch (option) {
    case "center":
    case "middle":
      option = "50%";
      break;
    case "left":
    case "top":
      option = "0%";
      break;
    case "right":
    case "bottom":
      option = "100%";
      break;
  }
  return parsePositionSizeOption(option, percentBase, percentOffset);
}
function parsePositionSizeOption(option, percentBase, percentOffset) {
  if (isString(option)) {
    if (isOptionStringPercent(option)) {
      return parseFloat(option) / 100 * percentBase + (percentOffset || 0);
    }
    return parseFloat(option);
  }
  return option == null ? NaN : +option;
}
function isOptionStringPercent(option) {
  return !!_trim(option).match(/%$/);
}
function round2(x, precision, returnStr) {
  if (true) {
    assert(precision != null);
  }
  if (isNaN(precision)) {
    return returnStr ? "" + x : +x;
  }
  precision = mathMin4(mathMax4(0, precision), TO_FIXED_SUPPORTED_PRECISION_MAX);
  x = (+x).toFixed(precision);
  return returnStr ? x : +x;
}
function asc(arr) {
  arr.sort(function(a, b) {
    return a - b;
  });
  return arr;
}
function getPrecision(val) {
  val = +val;
  if (isNaN(val)) {
    return 0;
  }
  if (val > 1e-14) {
    var e2 = 1;
    for (var i2 = 0; i2 < 15; i2++, e2 *= 10) {
      if (mathRound2(val * e2) / e2 === val) {
        return i2;
      }
    }
  }
  return getPrecisionSafe(val);
}
function getPrecisionSafe(val) {
  var str = val.toString().toLowerCase();
  var eIndex = str.indexOf("e");
  var exp = eIndex > 0 ? +str.slice(eIndex + 1) : 0;
  var significandPartLen = eIndex > 0 ? eIndex : str.length;
  var dotIndex = str.indexOf(".");
  var decimalPartLen = dotIndex < 0 ? 0 : significandPartLen - 1 - dotIndex;
  return mathMax4(0, decimalPartLen - exp);
}
function getAcceptableTickPrecision(dataExtent, pxSpan, pxDiffAcceptable) {
  var dataSpan = mathAbs3(dataExtent[1] - dataExtent[0]);
  if (!isFinite(dataSpan) || dataSpan === 0) {
    return NaN;
  }
  var dataExp2 = mathLog(2 * mathAbs3(pxDiffAcceptable || 1) * mathAbs3(dataSpan)) / mathLN10;
  var pxExp = mathLog(mathAbs3(pxSpan)) / mathLN10;
  var precision = mathMax4(0, mathCeil(-dataExp2 + pxExp));
  if (!isFinite(precision)) {
    precision = NaN;
  }
  return precision;
}
function getPercentSeats(valueList, precision) {
  var sum2 = reduce(valueList, function(acc, val) {
    return acc + (isNaN(val) ? 0 : val);
  }, 0);
  if (sum2 === 0) {
    return [];
  }
  var digits = mathPow2(10, precision);
  var votesPerQuota = map2(valueList, function(val) {
    return (isNaN(val) ? 0 : val) / sum2 * digits * 100;
  });
  var targetSeats = digits * 100;
  var seats = map2(votesPerQuota, function(votes) {
    return mathFloor(votes);
  });
  var currentSum = reduce(seats, function(acc, val) {
    return acc + val;
  }, 0);
  var remainder = map2(votesPerQuota, function(votes, idx) {
    return votes - seats[idx];
  });
  while (currentSum < targetSeats) {
    var max3 = Number.NEGATIVE_INFINITY;
    var maxId = null;
    for (var i2 = 0, len2 = remainder.length; i2 < len2; ++i2) {
      if (remainder[i2] > max3) {
        max3 = remainder[i2];
        maxId = i2;
      }
    }
    ++seats[maxId];
    remainder[maxId] = 0;
    ++currentSum;
  }
  return map2(seats, function(seat) {
    return seat / digits;
  });
}
function addSafe(val0, val1) {
  var maxPrecision = mathMax4(getPrecision(val0), getPrecision(val1));
  var sum2 = val0 + val1;
  return maxPrecision > TO_FIXED_SUPPORTED_PRECISION_MAX ? sum2 : round2(sum2, maxPrecision);
}
var MAX_SAFE_INTEGER2 = mathPow2(2, 53) - 1;
function remRadian(radian) {
  var pi2 = mathPI * 2;
  return (radian % pi2 + pi2) % pi2;
}
function isRadianAroundZero(val) {
  return val > -RADIAN_EPSILON && val < RADIAN_EPSILON;
}
var TIME_REG = /^(?:(\d{4})(?:[-\/](\d{1,2})(?:[-\/](\d{1,2})(?:[T ](\d{1,2})(?::(\d{1,2})(?::(\d{1,2})(?:[.,](\d+))?)?)?(Z|[\+\-]\d\d:?\d\d)?)?)?)?)?$/;
function parseDate(value) {
  if (value instanceof Date) {
    return value;
  } else if (isString(value)) {
    var match = TIME_REG.exec(value);
    if (!match) {
      return /* @__PURE__ */ new Date(NaN);
    }
    if (!match[8]) {
      return new Date(+match[1], +(match[2] || 1) - 1, +match[3] || 1, +match[4] || 0, +(match[5] || 0), +match[6] || 0, match[7] ? +match[7].substring(0, 3) : 0);
    } else {
      var hour = +match[4] || 0;
      if (match[8].toUpperCase() !== "Z") {
        hour -= +match[8].slice(0, 3);
      }
      return new Date(Date.UTC(+match[1], +(match[2] || 1) - 1, +match[3] || 1, hour, +(match[5] || 0), +match[6] || 0, match[7] ? +match[7].substring(0, 3) : 0));
    }
  } else if (value == null) {
    return /* @__PURE__ */ new Date(NaN);
  }
  return new Date(mathRound2(value));
}
function quantity(val) {
  return mathPow2(10, quantityExponent(val));
}
function quantityExponent(val) {
  if (val === 0) {
    return 0;
  }
  var exp = mathFloor(mathLog(val) / mathLN10);
  if (val / mathPow2(10, exp) >= 10) {
    exp++;
  }
  return exp;
}
var NICE_MODE_MIN = 2;
function nice(val, mode) {
  var exponent = quantityExponent(val);
  var exp10 = mathPow2(10, exponent);
  var f = val / exp10;
  var nf;
  if (mode === NICE_MODE_MIN) {
    nf = 1;
  } else if (mode) {
    if (f < 1.5) {
      nf = 1;
    } else if (f < 2.5) {
      nf = 2;
    } else if (f < 4) {
      nf = 3;
    } else if (f < 7) {
      nf = 5;
    } else {
      nf = 10;
    }
  } else {
    if (f < 1) {
      nf = 1;
    } else if (f < 2) {
      nf = 2;
    } else if (f < 3) {
      nf = 3;
    } else if (f < 5) {
      nf = 5;
    } else {
      nf = 10;
    }
  }
  val = nf * exp10;
  return round2(val, -exponent);
}
function reformIntervals(list) {
  list.sort(function(a, b) {
    return littleThan2(a, b, 0) ? -1 : 1;
  });
  var curr = -Infinity;
  var currClose = 1;
  for (var i2 = 0; i2 < list.length; ) {
    var interval2 = list[i2].interval;
    var close_1 = list[i2].close;
    for (var lg = 0; lg < 2; lg++) {
      if (interval2[lg] <= curr) {
        interval2[lg] = curr;
        close_1[lg] = !lg ? 1 - currClose : 1;
      }
      curr = interval2[lg];
      currClose = close_1[lg];
    }
    if (interval2[0] === interval2[1] && close_1[0] * close_1[1] !== 1) {
      list.splice(i2, 1);
    } else {
      i2++;
    }
  }
  return list;
  function littleThan2(a, b, lg2) {
    return a.interval[lg2] < b.interval[lg2] || a.interval[lg2] === b.interval[lg2] && (a.close[lg2] - b.close[lg2] === (!lg2 ? 1 : -1) || !lg2 && littleThan2(a, b, 1));
  }
}
function numericToNumber(val) {
  var valFloat = parseFloat(val);
  return valFloat == val && (valFloat !== 0 || !isString(val) || val.indexOf("x") <= 0) ? valFloat : NaN;
}
function isNumeric(val) {
  return !isNaN(numericToNumber(val));
}
function getRandomIdBase() {
  return mathRound2(mathRandom() * 9);
}
function getGreatestCommonDividor(a, b) {
  if (b === 0) {
    return a;
  }
  return getGreatestCommonDividor(b, a % b);
}
function getLeastCommonMultiple(a, b) {
  if (a == null) {
    return b;
  }
  if (b == null) {
    return a;
  }
  return a * b / getGreatestCommonDividor(a, b);
}
function isNullableNumberFinite(val) {
  return val != null && isFinite(val);
}

// node_modules/echarts/lib/util/log.js
var ECHARTS_PREFIX = "[ECharts] ";
var storedLogs = {};
var hasConsole = typeof console !== "undefined" && console.warn && console.log;
function outputLog(type, str, onlyOnce) {
  if (hasConsole) {
    if (onlyOnce) {
      if (storedLogs[str]) {
        return;
      }
      storedLogs[str] = true;
    }
    console[type](ECHARTS_PREFIX + str);
  }
}
function log(str, onlyOnce) {
  outputLog("log", str, onlyOnce);
}
function warn(str, onlyOnce) {
  outputLog("warn", str, onlyOnce);
}
function error(str, onlyOnce) {
  outputLog("error", str, onlyOnce);
}
function deprecateLog(str) {
  if (true) {
    outputLog("warn", "DEPRECATED: " + str, true);
  }
}
function deprecateReplaceLog(oldOpt, newOpt, scope) {
  if (true) {
    deprecateLog((scope ? "[" + scope + "]" : "") + (oldOpt + " is deprecated; use " + newOpt + " instead."));
  }
}
function makePrintable() {
  var hintInfo = [];
  for (var _i = 0; _i < arguments.length; _i++) {
    hintInfo[_i] = arguments[_i];
  }
  var msg = "";
  if (true) {
    var makePrintableStringIfPossible_1 = function(val) {
      return val === void 0 ? "undefined" : val === Infinity ? "Infinity" : val === -Infinity ? "-Infinity" : eqNaN(val) ? "NaN" : val instanceof Date ? "Date(" + val.toISOString() + ")" : isFunction(val) ? "function () { ... }" : isRegExp(val) ? val + "" : null;
    };
    msg = map2(hintInfo, function(arg) {
      if (isString(arg)) {
        return arg;
      } else {
        var printableStr = makePrintableStringIfPossible_1(arg);
        if (printableStr != null) {
          return printableStr;
        } else if (typeof JSON !== "undefined" && JSON.stringify) {
          try {
            return JSON.stringify(arg, function(n, val) {
              var printableStr2 = makePrintableStringIfPossible_1(val);
              return printableStr2 == null ? val : printableStr2;
            });
          } catch (err) {
            return "?";
          }
        } else {
          return "?";
        }
      }
    }).join(" ");
  }
  return msg;
}
function throwError2(msg) {
  throw new Error(msg);
}

// node_modules/echarts/lib/util/model.js
function interpolateNumber2(p0, p1, percent) {
  return (p1 - p0) * percent + p0;
}
var DUMMY_COMPONENT_NAME_PREFIX = "series\0";
var INTERNAL_COMPONENT_ID_PREFIX = "\0_ec_\0";
function normalizeToArray(value) {
  return value instanceof Array ? value : value == null ? [] : [value];
}
function defaultEmphasis(opt, key2, subOpts) {
  if (opt) {
    opt[key2] = opt[key2] || {};
    opt.emphasis = opt.emphasis || {};
    opt.emphasis[key2] = opt.emphasis[key2] || {};
    for (var i2 = 0, len2 = subOpts.length; i2 < len2; i2++) {
      var subOptName = subOpts[i2];
      if (!opt.emphasis[key2].hasOwnProperty(subOptName) && opt[key2].hasOwnProperty(subOptName)) {
        opt.emphasis[key2][subOptName] = opt[key2][subOptName];
      }
    }
  }
}
var TEXT_STYLE_OPTIONS = ["fontStyle", "fontWeight", "fontSize", "fontFamily", "rich", "tag", "color", "textBorderColor", "textBorderWidth", "width", "height", "lineHeight", "align", "verticalAlign", "baseline", "shadowColor", "shadowBlur", "shadowOffsetX", "shadowOffsetY", "textShadowColor", "textShadowBlur", "textShadowOffsetX", "textShadowOffsetY", "backgroundColor", "borderColor", "borderWidth", "borderRadius", "padding"];
function getDataItemValue(dataItem) {
  return isObject(dataItem) && !isArray(dataItem) && !(dataItem instanceof Date) ? dataItem.value : dataItem;
}
function isDataItemOption(dataItem) {
  return isObject(dataItem) && !(dataItem instanceof Array);
}
function mappingToExists(existings, newCmptOptions, mode) {
  var isNormalMergeMode = mode === "normalMerge";
  var isReplaceMergeMode = mode === "replaceMerge";
  var isReplaceAllMode = mode === "replaceAll";
  existings = existings || [];
  newCmptOptions = (newCmptOptions || []).slice();
  var existingIdIdxMap = createHashMap();
  each(newCmptOptions, function(cmptOption, index) {
    if (!isObject(cmptOption)) {
      newCmptOptions[index] = null;
      return;
    }
    if (true) {
      if (cmptOption.id != null && !isValidIdOrName(cmptOption.id)) {
        warnInvalidateIdOrName(cmptOption.id);
      }
      if (cmptOption.name != null && !isValidIdOrName(cmptOption.name)) {
        warnInvalidateIdOrName(cmptOption.name);
      }
    }
  });
  var result = prepareResult(existings, existingIdIdxMap, mode);
  if (isNormalMergeMode || isReplaceMergeMode) {
    mappingById(result, existings, existingIdIdxMap, newCmptOptions);
  }
  if (isNormalMergeMode) {
    mappingByName(result, newCmptOptions);
  }
  if (isNormalMergeMode || isReplaceMergeMode) {
    mappingByIndex(result, newCmptOptions, isReplaceMergeMode);
  } else if (isReplaceAllMode) {
    mappingInReplaceAllMode(result, newCmptOptions);
  }
  makeIdAndName(result);
  return result;
}
function prepareResult(existings, existingIdIdxMap, mode) {
  var result = [];
  if (mode === "replaceAll") {
    return result;
  }
  for (var index = 0; index < existings.length; index++) {
    var existing = existings[index];
    if (existing && existing.id != null) {
      existingIdIdxMap.set(existing.id, index);
    }
    result.push({
      existing: mode === "replaceMerge" || isComponentIdInternal(existing) ? null : existing,
      newOption: null,
      keyInfo: null,
      brandNew: null
    });
  }
  return result;
}
function mappingById(result, existings, existingIdIdxMap, newCmptOptions) {
  each(newCmptOptions, function(cmptOption, index) {
    if (!cmptOption || cmptOption.id == null) {
      return;
    }
    var optionId = makeComparableKey(cmptOption.id);
    var existingIdx = existingIdIdxMap.get(optionId);
    if (existingIdx != null) {
      var resultItem = result[existingIdx];
      assert(!resultItem.newOption, 'Duplicated option on id "' + optionId + '".');
      resultItem.newOption = cmptOption;
      resultItem.existing = existings[existingIdx];
      newCmptOptions[index] = null;
    }
  });
}
function mappingByName(result, newCmptOptions) {
  each(newCmptOptions, function(cmptOption, index) {
    if (!cmptOption || cmptOption.name == null) {
      return;
    }
    for (var i2 = 0; i2 < result.length; i2++) {
      var existing = result[i2].existing;
      if (!result[i2].newOption && existing && (existing.id == null || cmptOption.id == null) && !isComponentIdInternal(cmptOption) && !isComponentIdInternal(existing) && keyExistAndEqual("name", existing, cmptOption)) {
        result[i2].newOption = cmptOption;
        newCmptOptions[index] = null;
        return;
      }
    }
  });
}
function mappingByIndex(result, newCmptOptions, brandNew) {
  each(newCmptOptions, function(cmptOption) {
    if (!cmptOption) {
      return;
    }
    var resultItem;
    var nextIdx = 0;
    while (
      // Be `!resultItem` only when `nextIdx >= result.length`.
      (resultItem = result[nextIdx]) && (resultItem.newOption || isComponentIdInternal(resultItem.existing) || // In mode "replaceMerge", here no not-mapped-non-internal-existing.
      resultItem.existing && cmptOption.id != null && !keyExistAndEqual("id", cmptOption, resultItem.existing))
    ) {
      nextIdx++;
    }
    if (resultItem) {
      resultItem.newOption = cmptOption;
      resultItem.brandNew = brandNew;
    } else {
      result.push({
        newOption: cmptOption,
        brandNew,
        existing: null,
        keyInfo: null
      });
    }
    nextIdx++;
  });
}
function mappingInReplaceAllMode(result, newCmptOptions) {
  each(newCmptOptions, function(cmptOption) {
    result.push({
      newOption: cmptOption,
      brandNew: true,
      existing: null,
      keyInfo: null
    });
  });
}
function makeIdAndName(mapResult) {
  var idMap = createHashMap();
  each(mapResult, function(item) {
    var existing = item.existing;
    existing && idMap.set(existing.id, item);
  });
  each(mapResult, function(item) {
    var opt = item.newOption;
    assert(!opt || opt.id == null || !idMap.get(opt.id) || idMap.get(opt.id) === item, "id duplicates: " + (opt && opt.id));
    opt && opt.id != null && idMap.set(opt.id, item);
    !item.keyInfo && (item.keyInfo = {});
  });
  each(mapResult, function(item, index) {
    var existing = item.existing;
    var opt = item.newOption;
    var keyInfo = item.keyInfo;
    if (!isObject(opt)) {
      return;
    }
    keyInfo.name = opt.name != null ? makeComparableKey(opt.name) : existing ? existing.name : DUMMY_COMPONENT_NAME_PREFIX + index;
    if (existing) {
      keyInfo.id = makeComparableKey(existing.id);
    } else if (opt.id != null) {
      keyInfo.id = makeComparableKey(opt.id);
    } else {
      var idNum = 0;
      do {
        keyInfo.id = "\0" + keyInfo.name + "\0" + idNum++;
      } while (idMap.get(keyInfo.id));
    }
    idMap.set(keyInfo.id, item);
  });
}
function keyExistAndEqual(attr, obj1, obj2) {
  var key1 = convertOptionIdName(obj1[attr], null);
  var key2 = convertOptionIdName(obj2[attr], null);
  return key1 != null && key2 != null && key1 === key2;
}
function makeComparableKey(val) {
  if (true) {
    if (val == null) {
      throw new Error();
    }
  }
  return convertOptionIdName(val, "");
}
function convertOptionIdName(idOrName, defaultValue) {
  if (idOrName == null) {
    return defaultValue;
  }
  return isString(idOrName) ? idOrName : isNumber(idOrName) || isStringSafe(idOrName) ? idOrName + "" : defaultValue;
}
function warnInvalidateIdOrName(idOrName) {
  if (true) {
    warn("`" + idOrName + "` is invalid id or name. Must be a string or number.");
  }
}
function isValidIdOrName(idOrName) {
  return isStringSafe(idOrName) || isNumeric(idOrName);
}
function isNameSpecified(componentModel) {
  var name = componentModel.name;
  return !!(name && name.indexOf(DUMMY_COMPONENT_NAME_PREFIX));
}
function isComponentIdInternal(cmptOption) {
  return cmptOption && cmptOption.id != null && makeComparableKey(cmptOption.id).indexOf(INTERNAL_COMPONENT_ID_PREFIX) === 0;
}
function makeInternalComponentId(idSuffix) {
  return INTERNAL_COMPONENT_ID_PREFIX + idSuffix;
}
function setComponentTypeToKeyInfo(mappingResult, mainType, componentModelCtor) {
  each(mappingResult, function(item) {
    var newOption = item.newOption;
    if (isObject(newOption)) {
      item.keyInfo.mainType = mainType;
      item.keyInfo.subType = determineSubType(mainType, newOption, item.existing, componentModelCtor);
    }
  });
}
function determineSubType(mainType, newCmptOption, existComponent, componentModelCtor) {
  var subType = newCmptOption.type ? newCmptOption.type : existComponent ? existComponent.subType : componentModelCtor.determineSubType(mainType, newCmptOption);
  return subType;
}
function compressBatches(batchA, batchB) {
  var mapA = {};
  var mapB = {};
  makeMap(batchA || [], mapA);
  makeMap(batchB || [], mapB, mapA);
  return [mapToArray(mapA), mapToArray(mapB)];
  function makeMap(sourceBatch, map4, otherMap) {
    for (var i2 = 0, len2 = sourceBatch.length; i2 < len2; i2++) {
      var seriesId = convertOptionIdName(sourceBatch[i2].seriesId, null);
      if (seriesId == null) {
        return;
      }
      var dataIndices = normalizeToArray(sourceBatch[i2].dataIndex);
      var otherDataIndices = otherMap && otherMap[seriesId];
      for (var j = 0, lenj = dataIndices.length; j < lenj; j++) {
        var dataIndex = dataIndices[j];
        if (otherDataIndices && otherDataIndices[dataIndex]) {
          otherDataIndices[dataIndex] = null;
        } else {
          (map4[seriesId] || (map4[seriesId] = {}))[dataIndex] = 1;
        }
      }
    }
  }
  function mapToArray(map4, isData) {
    var result = [];
    for (var i2 in map4) {
      if (map4.hasOwnProperty(i2) && map4[i2] != null) {
        if (isData) {
          result.push(+i2);
        } else {
          var dataIndices = mapToArray(map4[i2], true);
          dataIndices.length && result.push({
            seriesId: i2,
            dataIndex: dataIndices
          });
        }
      }
    }
    return result;
  }
}
function queryDataIndex(data, payload) {
  if (payload.dataIndexInside != null) {
    return payload.dataIndexInside;
  } else if (payload.dataIndex != null) {
    return isArray(payload.dataIndex) ? map2(payload.dataIndex, function(value) {
      return data.indexOfRawIndex(value);
    }) : data.indexOfRawIndex(payload.dataIndex);
  } else if (payload.name != null) {
    return isArray(payload.name) ? map2(payload.name, function(value) {
      return data.indexOfName(value);
    }) : data.indexOfName(payload.name);
  }
}
function makeInner() {
  var key2 = "__ec_inner_" + innerUniqueIndex++;
  return function(hostObj) {
    return hostObj[key2] || (hostObj[key2] = {});
  };
}
var innerUniqueIndex = getRandomIdBase();
function parseFinder(ecModel, finderInput, opt) {
  var _a2 = preParseFinder(finderInput, opt), mainTypeSpecified = _a2.mainTypeSpecified, queryOptionMap = _a2.queryOptionMap, others = _a2.others;
  var result = others;
  var defaultMainType = opt ? opt.defaultMainType : null;
  if (!mainTypeSpecified && defaultMainType) {
    queryOptionMap.set(defaultMainType, {});
  }
  queryOptionMap.each(function(queryOption, mainType) {
    var queryResult = queryReferringComponents(ecModel, mainType, queryOption, {
      useDefault: defaultMainType === mainType,
      enableAll: opt && opt.enableAll != null ? opt.enableAll : true,
      enableNone: opt && opt.enableNone != null ? opt.enableNone : true
    });
    result[mainType + "Models"] = queryResult.models;
    result[mainType + "Model"] = queryResult.models[0];
  });
  return result;
}
function preParseFinder(finderInput, opt) {
  var finder;
  if (isString(finderInput)) {
    var obj = {};
    obj[finderInput + "Index"] = 0;
    finder = obj;
  } else {
    finder = finderInput;
  }
  var queryOptionMap = createHashMap();
  var others = {};
  var mainTypeSpecified = false;
  each(finder, function(value, key2) {
    if (key2 === "dataIndex" || key2 === "dataIndexInside") {
      others[key2] = value;
      return;
    }
    var parsedKey = key2.match(/^(\w+)(Index|Id|Name)$/) || [];
    var mainType = parsedKey[1];
    var queryType = (parsedKey[2] || "").toLowerCase();
    if (!mainType || !queryType || opt && opt.includeMainTypes && indexOf(opt.includeMainTypes, mainType) < 0) {
      return;
    }
    mainTypeSpecified = mainTypeSpecified || !!mainType;
    var queryOption = queryOptionMap.get(mainType) || queryOptionMap.set(mainType, {});
    queryOption[queryType] = value;
  });
  return {
    mainTypeSpecified,
    queryOptionMap,
    others
  };
}
var SINGLE_REFERRING = {
  useDefault: true,
  enableAll: false,
  enableNone: false
};
var MULTIPLE_REFERRING = {
  useDefault: false,
  enableAll: true,
  enableNone: true
};
function queryReferringComponents(ecModel, mainType, userOption, opt) {
  opt = opt || SINGLE_REFERRING;
  var indexOption = userOption.index;
  var idOption = userOption.id;
  var nameOption = userOption.name;
  var result = {
    models: null,
    specified: indexOption != null || idOption != null || nameOption != null
  };
  if (!result.specified) {
    var firstCmpt = void 0;
    result.models = opt.useDefault && (firstCmpt = ecModel.getComponent(mainType)) ? [firstCmpt] : [];
    return result;
  }
  if (indexOption === "none" || indexOption === false) {
    if (opt.enableNone) {
      result.models = [];
      return result;
    } else {
      if (true) {
        error('`"none"` or `false` is not a valid value on index option.');
      }
      indexOption = -1;
    }
  }
  if (indexOption === "all") {
    if (opt.enableAll) {
      indexOption = idOption = nameOption = null;
    } else {
      if (true) {
        error('`"all"` is not a valid value on index option.');
      }
      indexOption = -1;
    }
  }
  result.models = ecModel.queryComponents({
    mainType,
    index: indexOption,
    id: idOption,
    name: nameOption
  });
  return result;
}
function makeQueryConditionKindA(payload, mainType, subType) {
  if (true) {
    assert(mainType);
  }
  var query2 = {};
  query2[mainType + "Id"] = payload[mainType + "Id"];
  query2[mainType + "Index"] = payload[mainType + "Index"];
  query2[mainType + "Name"] = payload[mainType + "Name"];
  var condition = {
    mainType,
    query: query2
  };
  subType && (condition.subType = subType);
  return condition;
}
function setAttribute(dom, key2, value) {
  dom.setAttribute ? dom.setAttribute(key2, value) : dom[key2] = value;
}
function getAttribute(dom, key2) {
  return dom.getAttribute ? dom.getAttribute(key2) : dom[key2];
}
function getTooltipRenderMode(renderModeOption) {
  if (renderModeOption === "auto") {
    return env_default.domSupported ? "html" : "richText";
  } else {
    return renderModeOption || "html";
  }
}
function groupData(array, getKey2) {
  var buckets = createHashMap();
  var keys2 = [];
  each(array, function(item) {
    var key2 = getKey2(item);
    (buckets.get(key2) || (keys2.push(key2), buckets.set(key2, []))).push(item);
  });
  return {
    keys: keys2,
    buckets
  };
}
function interpolateRawValues(data, precision, sourceValue, targetValue, percent) {
  var isAutoPrecision = precision == null || precision === "auto";
  if (targetValue == null) {
    return targetValue;
  }
  if (isNumber(targetValue)) {
    var value = interpolateNumber2(sourceValue || 0, targetValue, percent);
    return round2(value, isAutoPrecision ? Math.max(getPrecision(sourceValue || 0), getPrecision(targetValue)) : precision);
  } else if (isString(targetValue)) {
    return percent < 1 ? sourceValue : targetValue;
  } else {
    var interpolated = [];
    var leftArr = sourceValue;
    var rightArr = targetValue;
    var length_1 = Math.max(leftArr ? leftArr.length : 0, rightArr.length);
    for (var i2 = 0; i2 < length_1; ++i2) {
      var info = data.getDimensionInfo(i2);
      if (info && info.type === "ordinal") {
        interpolated[i2] = (percent < 1 && leftArr ? leftArr : rightArr)[i2];
      } else {
        var leftVal = leftArr && leftArr[i2] ? leftArr[i2] : 0;
        var rightVal = rightArr[i2];
        var value = interpolateNumber2(leftVal, rightVal, percent);
        interpolated[i2] = round2(value, isAutoPrecision ? Math.max(getPrecision(leftVal), getPrecision(rightVal)) : precision);
      }
    }
    return interpolated;
  }
}
var ListIterator = (
  /** @class */
  (function() {
    function ListIterator2() {
    }
    ListIterator2.prototype.reset = function(list, start2, end2, step) {
      this._list = list;
      this._step = step = step || 1;
      this._idx = start2;
      this._end = end2 != null ? end2 : step > 0 ? list.length : 0;
      this.item = null;
      this.key = NaN;
      return this;
    };
    ListIterator2.prototype.next = function() {
      if (this._step > 0 ? this._idx < this._end : this._idx >= this._end) {
        this.item = this._list[this._idx];
        this.key = this._idx = this._idx + this._step;
        return true;
      }
      return false;
    };
    return ListIterator2;
  })()
);
function clearTmpModel(model2) {
  model2.option = model2.parentModel = model2.ecModel = null;
}
function initExtentForUnion() {
  return [Infinity, -Infinity];
}
function unionExtentFromNumber(extent, val) {
  if (isValidNumberForExtent(val)) {
    val < extent[0] && (extent[0] = val);
    val > extent[1] && (extent[1] = val);
  }
}
function unionExtentStartFromNumber(extent, val) {
  if (isValidNumberForExtent(val) && val < extent[0]) {
    extent[0] = val;
  }
}
function unionExtentEndFromNumber(extent, val) {
  if (isValidNumberForExtent(val) && val > extent[1]) {
    extent[1] = val;
  }
}
function unionExtentFromExtent(tarExtent, srcExtent) {
  if (isValidBoundsForExtent(srcExtent[0], srcExtent[1])) {
    srcExtent[0] < tarExtent[0] && (tarExtent[0] = srcExtent[0]);
    srcExtent[1] > tarExtent[1] && (tarExtent[1] = srcExtent[1]);
  }
}
function isValidNumberForExtent(val) {
  return val != null && isFinite(val);
}
function isValidBoundsForExtent(start2, end2) {
  return isValidNumberForExtent(start2) && isValidNumberForExtent(end2) && start2 <= end2;
}
function extentHasValue(extent) {
  var span = extent[1] - extent[0];
  return isFinite(span) && span >= 0;
}
function ensureExtentAscSimply(extent) {
  if (isValidBoundsForExtent(extent[0], extent[1]) && extent[0] > extent[1]) {
    extent[0] = extent[1];
  }
}
function makeCallOnlyOnce() {
  var hiddenKey = "__ec_once_" + onceUniqueIndex++;
  return function(hostObj, cb) {
    if (true) {
      assert(hostObj);
    }
    if (!hasOwn(hostObj, hiddenKey)) {
      hostObj[hiddenKey] = 1;
      cb();
    }
  };
}
var onceUniqueIndex = getRandomIdBase();
function removeDuplicates(arr, getKey2, resolve) {
  var dupMap = createHashMap();
  var writeIdx = 0;
  each(arr, function(item) {
    var key2 = getKey2(item);
    if (true) {
      assert(isString(key2));
    }
    var count2 = dupMap.get(key2) || 0;
    if (resolve) {
      resolve(item, count2);
    }
    if (!count2 && !resolve) {
      arr[writeIdx++] = item;
    }
    dupMap.set(key2, count2 + 1);
  });
  if (!resolve) {
    arr.length = writeIdx;
  }
}
function removeDuplicatesGetKeyFromValueProp(item) {
  if (true) {
    assert(item.value != null);
  }
  return item.value + "";
}
function removeDuplicatesGetKeyFromItemItself(item) {
  if (true) {
    assert(item != null);
  }
  return item + "";
}
function getIncrementalId(seriesModel, useIncremental) {
  return retrieve2(useIncremental, true) ? seriesModel.seriesIndex + 2 : 0;
}
function preparePipelineContext(seriesModel, view, pipeline) {
  var dataLen = seriesModel.getData().count();
  return {
    progressiveRender: pipeline.progressiveEnabled && view.incrementalPrepareRender && dataLen >= pipeline.threshold,
    large: seriesModel.get("large") && dataLen >= seriesModel.get("largeThreshold"),
    // TODO: modDataCount should not updated if `appendData`, otherwise cause whole repaint.
    // see `test/candlestick-large3.html`
    modDataCount: seriesModel.get("progressiveChunkMode") === "mod" ? seriesModel.getData().count() : null
  };
}
function validateUpstreamOutputRange(upstreamOutputRange, thisTaskPlannedRange) {
  assert(upstreamOutputRange.start === thisTaskPlannedRange.start && upstreamOutputRange.end === thisTaskPlannedRange.end);
}
function createSimpleOverallStageHandler(seriesType2, overallReset) {
  return {
    seriesType: seriesType2,
    overallReset
  };
}
function createSimpleOverallStageHandler2(overallReset) {
  return {
    overallReset
  };
}

// node_modules/echarts/lib/util/innerStore.js
var getECData = makeInner();
var setCommonECData = function(seriesIndex, dataType, dataIdx, el) {
  if (el) {
    var ecData = getECData(el);
    ecData.dataIndex = dataIdx;
    ecData.dataType = dataType;
    ecData.seriesIndex = seriesIndex;
    ecData.ssrType = "chart";
    if (el.type === "group") {
      el.traverse(function(child) {
        var childECData = getECData(child);
        childECData.seriesIndex = seriesIndex;
        childECData.dataIndex = dataIdx;
        childECData.dataType = dataType;
        childECData.ssrType = "chart";
      });
    }
  }
};

// node_modules/echarts/lib/util/types.js
var UNDEFINED_STR = "undefined";
var COMPONENT_MAIN_TYPE_SERIES = "series";
var VISUAL_DIMENSIONS = createHashMap(["tooltip", "label", "itemName", "itemId", "itemGroupId", "itemChildGroupId", "seriesName"]);
var SOURCE_FORMAT_ORIGINAL = "original";
var SOURCE_FORMAT_ARRAY_ROWS = "arrayRows";
var SOURCE_FORMAT_OBJECT_ROWS = "objectRows";
var SOURCE_FORMAT_KEYED_COLUMNS = "keyedColumns";
var SOURCE_FORMAT_TYPED_ARRAY = "typedArray";
var SOURCE_FORMAT_UNKNOWN = "unknown";
var SERIES_LAYOUT_BY_COLUMN = "column";
var SERIES_LAYOUT_BY_ROW = "row";
var ROAM_ACTION_TYPE_SUFFIX = "Roam";

// node_modules/echarts/lib/core/ExtensionAPI.js
var availableMethods = [
  "getDom",
  "getZr",
  "getWidth",
  "getHeight",
  "getDevicePixelRatio",
  "dispatchAction",
  "isSSR",
  "isDisposed",
  "on",
  "off",
  "getDataURL",
  "getConnectedDataURL",
  // 'getModel',
  "getOption",
  // 'getViewOfComponentModel',
  // 'getViewOfSeriesModel',
  "getId",
  "updateLabelLayout"
];
var ExtensionAPI = (
  /** @class */
  /* @__PURE__ */ (function() {
    function ExtensionAPI2(ecInstance) {
      each(availableMethods, function(methodName) {
        this[methodName] = bind(ecInstance[methodName], ecInstance);
      }, this);
    }
    return ExtensionAPI2;
  })()
);
function getViewOfComponentOrSeries(api, componentOrSeries) {
  return componentOrSeries.mainType === COMPONENT_MAIN_TYPE_SERIES ? api.getViewOfSeriesModel(componentOrSeries) : api.getViewOfComponentModel(componentOrSeries);
}
var ExtensionAPI_default = ExtensionAPI;

// node_modules/echarts/lib/util/states.js
var _highlightNextDigit = 1;
var _highlightKeyMap = {};
var getSavedStates = makeInner();
var getComponentStates = makeInner();
var HOVER_STATE_NORMAL = 0;
var HOVER_STATE_BLUR = 1;
var HOVER_STATE_EMPHASIS = 2;
var SPECIAL_STATES = ["emphasis", "blur", "select"];
var DISPLAY_STATES = ["normal", "emphasis", "blur", "select"];
var Z2_EMPHASIS_LIFT = 10;
var Z2_SELECT_LIFT = 9;
var HIGHLIGHT_ACTION_TYPE = "highlight";
var DOWNPLAY_ACTION_TYPE = "downplay";
var SELECT_ACTION_TYPE = "select";
var UNSELECT_ACTION_TYPE = "unselect";
var TOGGLE_SELECT_ACTION_TYPE = "toggleSelect";
var SELECT_CHANGED_EVENT_TYPE = "selectchanged";
function hasFillOrStroke(fillOrStroke) {
  return fillOrStroke != null && fillOrStroke !== "none";
}
function doChangeHoverState(el, stateName, hoverStateEnum) {
  if (el.onHoverStateChange && (el.hoverState || 0) !== hoverStateEnum) {
    el.onHoverStateChange(stateName);
  }
  el.hoverState = hoverStateEnum;
}
function singleEnterEmphasis(el) {
  doChangeHoverState(el, "emphasis", HOVER_STATE_EMPHASIS);
}
function singleLeaveEmphasis(el) {
  if (el.hoverState === HOVER_STATE_EMPHASIS) {
    doChangeHoverState(el, "normal", HOVER_STATE_NORMAL);
  }
}
function singleEnterBlur(el) {
  doChangeHoverState(el, "blur", HOVER_STATE_BLUR);
}
function singleLeaveBlur(el) {
  if (el.hoverState === HOVER_STATE_BLUR) {
    doChangeHoverState(el, "normal", HOVER_STATE_NORMAL);
  }
}
function singleEnterSelect(el) {
  el.selected = true;
}
function singleLeaveSelect(el) {
  el.selected = false;
}
function updateElementState(el, updater, commonParam) {
  updater(el, commonParam);
}
function traverseUpdateState(el, updater, commonParam) {
  updateElementState(el, updater, commonParam);
  el.isGroup && el.traverse(function(child) {
    updateElementState(child, updater, commonParam);
  });
}
function setStatesFlag(el, stateName) {
  switch (stateName) {
    case "emphasis":
      el.hoverState = HOVER_STATE_EMPHASIS;
      break;
    case "normal":
      el.hoverState = HOVER_STATE_NORMAL;
      break;
    case "blur":
      el.hoverState = HOVER_STATE_BLUR;
      break;
    case "select":
      el.selected = true;
  }
}
function getFromStateStyle(el, props, toStateName, defaultValue) {
  var style2 = el.style;
  var fromState = {};
  for (var i2 = 0; i2 < props.length; i2++) {
    var propName = props[i2];
    var val = style2[propName];
    fromState[propName] = val == null ? defaultValue && defaultValue[propName] : val;
  }
  for (var i2 = 0; i2 < el.animators.length; i2++) {
    var animator = el.animators[i2];
    if (animator.__fromStateTransition && animator.__fromStateTransition.indexOf(toStateName) < 0 && animator.targetName === "style") {
      animator.saveTo(fromState, props);
    }
  }
  return fromState;
}
function createEmphasisDefaultState(el, stateName, targetStates, state2) {
  var hasSelect = targetStates && indexOf(targetStates, "select") >= 0;
  var cloned = false;
  if (el instanceof Path_default) {
    var store = getSavedStates(el);
    var fromFill = hasSelect ? store.selectFill || store.normalFill : store.normalFill;
    var fromStroke = hasSelect ? store.selectStroke || store.normalStroke : store.normalStroke;
    if (hasFillOrStroke(fromFill) || hasFillOrStroke(fromStroke)) {
      state2 = state2 || {};
      var emphasisStyle = state2.style || {};
      if (emphasisStyle.fill === "inherit") {
        cloned = true;
        state2 = extend({}, state2);
        emphasisStyle = extend({}, emphasisStyle);
        emphasisStyle.fill = fromFill;
      } else if (!hasFillOrStroke(emphasisStyle.fill) && hasFillOrStroke(fromFill)) {
        cloned = true;
        state2 = extend({}, state2);
        emphasisStyle = extend({}, emphasisStyle);
        emphasisStyle.fill = liftColor(fromFill);
      } else if (!hasFillOrStroke(emphasisStyle.stroke) && hasFillOrStroke(fromStroke)) {
        if (!cloned) {
          state2 = extend({}, state2);
          emphasisStyle = extend({}, emphasisStyle);
        }
        emphasisStyle.stroke = liftColor(fromStroke);
      }
      state2.style = emphasisStyle;
    }
  }
  if (state2) {
    if (state2.z2 == null) {
      if (!cloned) {
        state2 = extend({}, state2);
      }
      var z2EmphasisLift = el.z2EmphasisLift;
      state2.z2 = el.z2 + (z2EmphasisLift != null ? z2EmphasisLift : Z2_EMPHASIS_LIFT);
    }
  }
  return state2;
}
function createSelectDefaultState(el, stateName, state2) {
  if (state2) {
    if (state2.z2 == null) {
      state2 = extend({}, state2);
      var z2SelectLift = el.z2SelectLift;
      state2.z2 = el.z2 + (z2SelectLift != null ? z2SelectLift : Z2_SELECT_LIFT);
    }
  }
  return state2;
}
function createBlurDefaultState(el, stateName, state2) {
  var hasBlur = indexOf(el.currentStates, stateName) >= 0;
  var currentOpacity = el.style.opacity;
  var fromState = !hasBlur ? getFromStateStyle(el, ["opacity"], stateName, {
    opacity: 1
  }) : null;
  state2 = state2 || {};
  var blurStyle = state2.style || {};
  if (blurStyle.opacity == null) {
    state2 = extend({}, state2);
    blurStyle = extend({
      // Already being applied 'emphasis'. DON'T mul opacity multiple times.
      opacity: hasBlur ? currentOpacity : fromState.opacity * 0.1
    }, blurStyle);
    state2.style = blurStyle;
  }
  return state2;
}
function elementStateProxy(stateName, targetStates) {
  var state2 = this.states[stateName];
  if (this.style) {
    if (stateName === "emphasis") {
      return createEmphasisDefaultState(this, stateName, targetStates, state2);
    } else if (stateName === "blur") {
      return createBlurDefaultState(this, stateName, state2);
    } else if (stateName === "select") {
      return createSelectDefaultState(this, stateName, state2);
    }
  }
  return state2;
}
function setDefaultStateProxy(el) {
  el.stateProxy = elementStateProxy;
  var textContent = el.getTextContent();
  var textGuide = el.getTextGuideLine();
  if (textContent) {
    textContent.stateProxy = elementStateProxy;
  }
  if (textGuide) {
    textGuide.stateProxy = elementStateProxy;
  }
}
function enterEmphasisWhenMouseOver(el, e2) {
  !shouldSilent(el, e2) && !el.__highByOuter && traverseUpdateState(el, singleEnterEmphasis);
}
function leaveEmphasisWhenMouseOut(el, e2) {
  !shouldSilent(el, e2) && !el.__highByOuter && traverseUpdateState(el, singleLeaveEmphasis);
}
function enterEmphasis(el, highlightDigit) {
  el.__highByOuter |= 1 << (highlightDigit || 0);
  traverseUpdateState(el, singleEnterEmphasis);
}
function leaveEmphasis(el, highlightDigit) {
  !(el.__highByOuter &= ~(1 << (highlightDigit || 0))) && traverseUpdateState(el, singleLeaveEmphasis);
}
function enterBlur(el) {
  traverseUpdateState(el, singleEnterBlur);
}
function leaveBlur(el) {
  traverseUpdateState(el, singleLeaveBlur);
}
function enterSelect(el) {
  traverseUpdateState(el, singleEnterSelect);
}
function leaveSelect(el) {
  traverseUpdateState(el, singleLeaveSelect);
}
function shouldSilent(el, e2) {
  return el.__highDownSilentOnTouch && e2.zrByTouch;
}
function allLeaveBlur(api) {
  var model2 = api.getModel();
  var leaveBlurredSeries = [];
  var allComponentViews = [];
  model2.eachComponent(function(componentType, componentModel) {
    var componentStates = getComponentStates(componentModel);
    var view = getViewOfComponentOrSeries(api, componentModel);
    var isSeries2 = componentType === "series";
    !isSeries2 && allComponentViews.push(view);
    if (componentStates.isBlured) {
      view.group.traverse(function(child) {
        singleLeaveBlur(child);
      });
      isSeries2 && leaveBlurredSeries.push(componentModel);
    }
    componentStates.isBlured = false;
  });
  each(allComponentViews, function(view) {
    if (view && view.toggleBlurSeries) {
      view.toggleBlurSeries(leaveBlurredSeries, false, model2);
    }
  });
}
function blurSeries(targetSeriesIndex, focus, blurScope, api) {
  var ecModel = api.getModel();
  blurScope = blurScope || "coordinateSystem";
  function leaveBlurOfIndices(data, dataIndices) {
    for (var i2 = 0; i2 < dataIndices.length; i2++) {
      var itemEl = data.getItemGraphicEl(dataIndices[i2]);
      itemEl && leaveBlur(itemEl);
    }
  }
  if (targetSeriesIndex == null) {
    return;
  }
  if (!focus || focus === "none") {
    return;
  }
  var targetSeriesModel = ecModel.getSeriesByIndex(targetSeriesIndex);
  var targetCoordSys = targetSeriesModel.coordinateSystem;
  if (targetCoordSys && targetCoordSys.master) {
    targetCoordSys = targetCoordSys.master;
  }
  var blurredSeries = [];
  ecModel.eachSeries(function(seriesModel) {
    var sameSeries = targetSeriesModel === seriesModel;
    var coordSys = seriesModel.coordinateSystem;
    if (coordSys && coordSys.master) {
      coordSys = coordSys.master;
    }
    var sameCoordSys = coordSys && targetCoordSys ? coordSys === targetCoordSys : sameSeries;
    if (!// Not blur other series if blurScope series
    (blurScope === "series" && !sameSeries || blurScope === "coordinateSystem" && !sameCoordSys || focus === "series" && sameSeries)) {
      var view = api.getViewOfSeriesModel(seriesModel);
      view.group.traverse(function(child) {
        if (child.__highByOuter && sameSeries && focus === "self") {
          return;
        }
        singleEnterBlur(child);
      });
      if (isArrayLike(focus)) {
        leaveBlurOfIndices(seriesModel.getData(), focus);
      } else if (isObject(focus)) {
        var dataTypes = keys(focus);
        for (var d = 0; d < dataTypes.length; d++) {
          leaveBlurOfIndices(seriesModel.getData(dataTypes[d]), focus[dataTypes[d]]);
        }
      }
      blurredSeries.push(seriesModel);
      getComponentStates(seriesModel).isBlured = true;
    }
  });
  ecModel.eachComponent(function(componentType, componentModel) {
    if (componentType === "series") {
      return;
    }
    var view = api.getViewOfComponentModel(componentModel);
    if (view && view.toggleBlurSeries) {
      view.toggleBlurSeries(blurredSeries, true, ecModel);
    }
  });
}
function blurComponent(componentMainType, componentIndex, api) {
  if (componentMainType == null || componentIndex == null) {
    return;
  }
  var componentModel = api.getModel().getComponent(componentMainType, componentIndex);
  if (!componentModel) {
    return;
  }
  getComponentStates(componentModel).isBlured = true;
  var view = api.getViewOfComponentModel(componentModel);
  if (!view || !view.focusBlurEnabled) {
    return;
  }
  view.group.traverse(function(child) {
    singleEnterBlur(child);
  });
}
function blurSeriesFromHighlightPayload(seriesModel, payload, api) {
  var seriesIndex = seriesModel.seriesIndex;
  var data = seriesModel.getData(payload.dataType);
  if (!data) {
    if (true) {
      error("Unknown dataType " + payload.dataType);
    }
    return;
  }
  var dataIndex = queryDataIndex(data, payload);
  dataIndex = (isArray(dataIndex) ? dataIndex[0] : dataIndex) || 0;
  var el = data.getItemGraphicEl(dataIndex);
  if (!el) {
    var count2 = data.count();
    var current = 0;
    while (!el && current < count2) {
      el = data.getItemGraphicEl(current++);
    }
  }
  if (el) {
    var ecData = getECData(el);
    blurSeries(seriesIndex, ecData.focus, ecData.blurScope, api);
  } else {
    var focus_1 = seriesModel.get(["emphasis", "focus"]);
    var blurScope = seriesModel.get(["emphasis", "blurScope"]);
    if (focus_1 != null) {
      blurSeries(seriesIndex, focus_1, blurScope, api);
    }
  }
}
function findComponentHighDownDispatchers(componentMainType, componentIndex, name, api) {
  var ret = {
    focusSelf: false,
    dispatchers: null
  };
  if (componentMainType == null || componentMainType === "series" || componentIndex == null || name == null) {
    return ret;
  }
  var componentModel = api.getModel().getComponent(componentMainType, componentIndex);
  if (!componentModel) {
    return ret;
  }
  var view = api.getViewOfComponentModel(componentModel);
  if (!view || !view.findHighDownDispatchers) {
    return ret;
  }
  var dispatchers = view.findHighDownDispatchers(name);
  var focusSelf;
  for (var i2 = 0; i2 < dispatchers.length; i2++) {
    if (!isHighDownDispatcher(dispatchers[i2])) {
      error("param should be highDownDispatcher");
    }
    if (getECData(dispatchers[i2]).focus === "self") {
      focusSelf = true;
      break;
    }
  }
  return {
    focusSelf,
    dispatchers
  };
}
function handleGlobalMouseOverForHighDown(dispatcher, e2, api) {
  if (!isHighDownDispatcher(dispatcher)) {
    error("param should be highDownDispatcher");
  }
  var ecData = getECData(dispatcher);
  var _a2 = findComponentHighDownDispatchers(ecData.componentMainType, ecData.componentIndex, ecData.componentHighDownName, api), dispatchers = _a2.dispatchers, focusSelf = _a2.focusSelf;
  if (dispatchers) {
    if (focusSelf) {
      blurComponent(ecData.componentMainType, ecData.componentIndex, api);
    }
    each(dispatchers, function(dispatcher2) {
      return enterEmphasisWhenMouseOver(dispatcher2, e2);
    });
  } else {
    blurSeries(ecData.seriesIndex, ecData.focus, ecData.blurScope, api);
    if (ecData.focus === "self") {
      blurComponent(ecData.componentMainType, ecData.componentIndex, api);
    }
    enterEmphasisWhenMouseOver(dispatcher, e2);
  }
}
function handleGlobalMouseOutForHighDown(dispatcher, e2, api) {
  if (!isHighDownDispatcher(dispatcher)) {
    error("param should be highDownDispatcher");
  }
  allLeaveBlur(api);
  var ecData = getECData(dispatcher);
  var dispatchers = findComponentHighDownDispatchers(ecData.componentMainType, ecData.componentIndex, ecData.componentHighDownName, api).dispatchers;
  if (dispatchers) {
    each(dispatchers, function(dispatcher2) {
      return leaveEmphasisWhenMouseOut(dispatcher2, e2);
    });
  } else {
    leaveEmphasisWhenMouseOut(dispatcher, e2);
  }
}
function toggleSelectionFromPayload(seriesModel, payload, api) {
  if (!isSelectChangePayload(payload)) {
    return;
  }
  var dataType = payload.dataType;
  var data = seriesModel.getData(dataType);
  var dataIndex = queryDataIndex(data, payload);
  if (!isArray(dataIndex)) {
    dataIndex = [dataIndex];
  }
  seriesModel[payload.type === TOGGLE_SELECT_ACTION_TYPE ? "toggleSelect" : payload.type === SELECT_ACTION_TYPE ? "select" : "unselect"](dataIndex, dataType);
}
function updateSeriesElementSelection(seriesModel) {
  var allData = seriesModel.getAllData();
  each(allData, function(_a2) {
    var data = _a2.data, type = _a2.type;
    data.eachItemGraphicEl(function(el, idx) {
      seriesModel.isSelected(idx, type) ? enterSelect(el) : leaveSelect(el);
    });
  });
}
function getAllSelectedIndices(ecModel) {
  var ret = [];
  ecModel.eachSeries(function(seriesModel) {
    var allData = seriesModel.getAllData();
    each(allData, function(_a2) {
      var data = _a2.data, type = _a2.type;
      var dataIndices = seriesModel.getSelectedDataIndices();
      if (dataIndices.length > 0) {
        var item = {
          dataIndex: dataIndices,
          seriesIndex: seriesModel.seriesIndex
        };
        if (type != null) {
          item.dataType = type;
        }
        ret.push(item);
      }
    });
  });
  return ret;
}
function enableHoverEmphasis(el, focus, blurScope) {
  setAsHighDownDispatcher(el, true);
  traverseUpdateState(el, setDefaultStateProxy);
  enableHoverFocus(el, focus, blurScope);
}
function disableHoverEmphasis(el) {
  setAsHighDownDispatcher(el, false);
}
function toggleHoverEmphasis(el, focus, blurScope, isDisabled) {
  isDisabled ? disableHoverEmphasis(el) : enableHoverEmphasis(el, focus, blurScope);
}
function enableHoverFocus(el, focus, blurScope) {
  var ecData = getECData(el);
  if (focus != null) {
    ecData.focus = focus;
    ecData.blurScope = blurScope;
  } else if (ecData.focus) {
    ecData.focus = null;
  }
}
var OTHER_STATES = ["emphasis", "blur", "select"];
var defaultStyleGetterMap = {
  itemStyle: "getItemStyle",
  lineStyle: "getLineStyle",
  areaStyle: "getAreaStyle"
};
function setStatesStylesFromModel(el, itemModel, styleType, getter2) {
  styleType = styleType || "itemStyle";
  for (var i2 = 0; i2 < OTHER_STATES.length; i2++) {
    var stateName = OTHER_STATES[i2];
    var model2 = itemModel.getModel([stateName, styleType]);
    var state2 = el.ensureState(stateName);
    state2.style = getter2 ? getter2(model2) : model2[defaultStyleGetterMap[styleType]]();
  }
}
function setAsHighDownDispatcher(el, asDispatcher) {
  var disable = asDispatcher === false;
  var extendedEl = el;
  if (el.highDownSilentOnTouch) {
    extendedEl.__highDownSilentOnTouch = el.highDownSilentOnTouch;
  }
  if (!disable || extendedEl.__highDownDispatcher) {
    extendedEl.__highByOuter = extendedEl.__highByOuter || 0;
    extendedEl.__highDownDispatcher = !disable;
  }
}
function isHighDownDispatcher(el) {
  return !!(el && el.__highDownDispatcher);
}
function enableComponentHighDownFeatures(el, componentModel, componentHighDownName) {
  var ecData = getECData(el);
  ecData.componentMainType = componentModel.mainType;
  ecData.componentIndex = componentModel.componentIndex;
  ecData.componentHighDownName = componentHighDownName;
}
function getHighlightDigit(highlightKey) {
  var highlightDigit = _highlightKeyMap[highlightKey];
  if (highlightDigit == null && _highlightNextDigit <= 32) {
    highlightDigit = _highlightKeyMap[highlightKey] = _highlightNextDigit++;
  }
  return highlightDigit;
}
function isSelectChangePayload(payload) {
  var payloadType = payload.type;
  return payloadType === SELECT_ACTION_TYPE || payloadType === UNSELECT_ACTION_TYPE || payloadType === TOGGLE_SELECT_ACTION_TYPE;
}
function isHighDownPayload(payload) {
  var payloadType = payload.type;
  return payloadType === HIGHLIGHT_ACTION_TYPE || payloadType === DOWNPLAY_ACTION_TYPE;
}
function savePathStates(el) {
  var store = getSavedStates(el);
  store.normalFill = el.style.fill;
  store.normalStroke = el.style.stroke;
  var selectState = el.states.select || {};
  store.selectFill = selectState.style && selectState.style.fill || null;
  store.selectStroke = selectState.style && selectState.style.stroke || null;
}

// node_modules/echarts/lib/util/graphic.js
var graphic_exports = {};
__export(graphic_exports, {
  Arc: () => Arc_default,
  BezierCurve: () => BezierCurve_default,
  BoundingRect: () => BoundingRect_default,
  Circle: () => Circle_default,
  CompoundPath: () => CompoundPath_default,
  Ellipse: () => Ellipse_default,
  Group: () => Group_default,
  HOVER_LAYER_FOR_INCREMENTAL: () => HOVER_LAYER_FOR_INCREMENTAL,
  HOVER_LAYER_FROM_THRESHOLD: () => HOVER_LAYER_FROM_THRESHOLD,
  HOVER_LAYER_NO: () => HOVER_LAYER_NO,
  Image: () => Image_default,
  IncrementalDisplayable: () => IncrementalDisplayable_default,
  Line: () => Line_default,
  LinearGradient: () => LinearGradient_default,
  OrientedBoundingRect: () => OrientedBoundingRect_default,
  Path: () => Path_default,
  Point: () => Point_default,
  Polygon: () => Polygon_default,
  Polyline: () => Polyline_default,
  RadialGradient: () => RadialGradient_default,
  Rect: () => Rect_default,
  Ring: () => Ring_default,
  Sector: () => Sector_default,
  Text: () => Text_default,
  WH: () => WH2,
  XY: () => XY2,
  applyTransform: () => applyTransform2,
  calcZ2Range: () => calcZ2Range,
  clipPointsByRect: () => clipPointsByRect,
  clipRectByRect: () => clipRectByRect,
  createIcon: () => createIcon,
  decomposeTransform: () => decomposeTransform,
  ensureCopyRect: () => ensureCopyRect,
  ensureCopyTransform: () => ensureCopyTransform,
  expandOrShrinkRect: () => expandOrShrinkRect,
  extendPath: () => extendPath,
  extendShape: () => extendShape,
  getCurrentCanvasPainter: () => getCurrentCanvasPainter,
  getShapeClass: () => getShapeClass,
  getTransform: () => getTransform2,
  groupTransition: () => groupTransition,
  initProps: () => initProps,
  isBoundingRectAxisAligned: () => isBoundingRectAxisAligned,
  isElementRemoved: () => isElementRemoved,
  lineLineIntersect: () => lineLineIntersect,
  linePolygonIntersect: () => linePolygonIntersect,
  makeImage: () => makeImage,
  makePath: () => makePath,
  mergePath: () => mergePath2,
  payloadDisableAnimation: () => payloadDisableAnimation,
  registerShape: () => registerShape,
  removeElement: () => removeElement,
  removeElementWithFadeOut: () => removeElementWithFadeOut,
  resizePath: () => resizePath,
  retrieveZInfo: () => retrieveZInfo,
  setTooltipConfig: () => setTooltipConfig,
  subPixelOptimize: () => subPixelOptimize2,
  subPixelOptimizeLine: () => subPixelOptimizeLine2,
  subPixelOptimizeRect: () => subPixelOptimizeRect2,
  transformDirection: () => transformDirection,
  traverseElements: () => traverseElements,
  traverseUpdateZ: () => traverseUpdateZ,
  updateProps: () => updateProps
});

// node_modules/zrender/lib/tool/transformPath.js
var CMD3 = PathProxy_default.CMD;
var points = [[], [], []];
var mathSqrt2 = Math.sqrt;
var mathAtan2 = Math.atan2;
function transformPath(path, m2) {
  if (!m2) {
    return;
  }
  var data = path.data;
  var len2 = path.len();
  var cmd;
  var nPoint;
  var i2;
  var j;
  var k2;
  var p;
  var M2 = CMD3.M;
  var C2 = CMD3.C;
  var L2 = CMD3.L;
  var R2 = CMD3.R;
  var A2 = CMD3.A;
  var Q2 = CMD3.Q;
  for (i2 = 0, j = 0; i2 < len2; ) {
    cmd = data[i2++];
    j = i2;
    nPoint = 0;
    switch (cmd) {
      case M2:
        nPoint = 1;
        break;
      case L2:
        nPoint = 1;
        break;
      case C2:
        nPoint = 3;
        break;
      case Q2:
        nPoint = 2;
        break;
      case A2:
        var x = m2[4];
        var y = m2[5];
        var sx = mathSqrt2(m2[0] * m2[0] + m2[1] * m2[1]);
        var sy = mathSqrt2(m2[2] * m2[2] + m2[3] * m2[3]);
        var angle = mathAtan2(-m2[1] / sy, m2[0] / sx);
        data[i2] *= sx;
        data[i2++] += x;
        data[i2] *= sy;
        data[i2++] += y;
        data[i2++] *= sx;
        data[i2++] *= sy;
        data[i2++] += angle;
        data[i2++] += angle;
        i2 += 2;
        j = i2;
        break;
      case R2:
        p[0] = data[i2++];
        p[1] = data[i2++];
        applyTransform(p, p, m2);
        data[j++] = p[0];
        data[j++] = p[1];
        p[0] += data[i2++];
        p[1] += data[i2++];
        applyTransform(p, p, m2);
        data[j++] = p[0];
        data[j++] = p[1];
    }
    for (k2 = 0; k2 < nPoint; k2++) {
      var p_1 = points[k2];
      p_1[0] = data[i2++];
      p_1[1] = data[i2++];
      applyTransform(p_1, p_1, m2);
      data[j++] = p_1[0];
      data[j++] = p_1[1];
    }
  }
  path.increaseVersion();
}

// node_modules/zrender/lib/tool/path.js
var mathSqrt3 = Math.sqrt;
var mathSin3 = Math.sin;
var mathCos3 = Math.cos;
var PI3 = Math.PI;
function vMag(v) {
  return Math.sqrt(v[0] * v[0] + v[1] * v[1]);
}
function vRatio(u, v) {
  return (u[0] * v[0] + u[1] * v[1]) / (vMag(u) * vMag(v));
}
function vAngle(u, v) {
  return (u[0] * v[1] < u[1] * v[0] ? -1 : 1) * Math.acos(vRatio(u, v));
}
function processArc(x1, y1, x2, y2, fa, fs, rx, ry, psiDeg, cmd, path) {
  var psi = psiDeg * (PI3 / 180);
  var xp = mathCos3(psi) * (x1 - x2) / 2 + mathSin3(psi) * (y1 - y2) / 2;
  var yp = -1 * mathSin3(psi) * (x1 - x2) / 2 + mathCos3(psi) * (y1 - y2) / 2;
  var lambda = xp * xp / (rx * rx) + yp * yp / (ry * ry);
  if (lambda > 1) {
    rx *= mathSqrt3(lambda);
    ry *= mathSqrt3(lambda);
  }
  var f = (fa === fs ? -1 : 1) * mathSqrt3((rx * rx * (ry * ry) - rx * rx * (yp * yp) - ry * ry * (xp * xp)) / (rx * rx * (yp * yp) + ry * ry * (xp * xp))) || 0;
  var cxp = f * rx * yp / ry;
  var cyp = f * -ry * xp / rx;
  var cx = (x1 + x2) / 2 + mathCos3(psi) * cxp - mathSin3(psi) * cyp;
  var cy = (y1 + y2) / 2 + mathSin3(psi) * cxp + mathCos3(psi) * cyp;
  var theta = vAngle([1, 0], [(xp - cxp) / rx, (yp - cyp) / ry]);
  var u = [(xp - cxp) / rx, (yp - cyp) / ry];
  var v = [(-1 * xp - cxp) / rx, (-1 * yp - cyp) / ry];
  var dTheta = vAngle(u, v);
  if (vRatio(u, v) <= -1) {
    dTheta = PI3;
  }
  if (vRatio(u, v) >= 1) {
    dTheta = 0;
  }
  if (dTheta < 0) {
    var n = Math.round(dTheta / PI3 * 1e6) / 1e6;
    dTheta = PI3 * 2 + n % 2 * PI3;
  }
  path.addData(cmd, cx, cy, rx, ry, theta, dTheta, psi, fs);
}
var commandReg = /([mlvhzcqtsa])([^mlvhzcqtsa]*)/ig;
var numberReg = /-?([0-9]*\.)?[0-9]+([eE]-?[0-9]+)?/g;
function createPathProxyFromString(data) {
  var path = new PathProxy_default();
  if (!data) {
    return path;
  }
  var cpx = 0;
  var cpy = 0;
  var subpathX = cpx;
  var subpathY = cpy;
  var prevCmd;
  var CMD5 = PathProxy_default.CMD;
  var cmdList = data.match(commandReg);
  if (!cmdList) {
    return path;
  }
  for (var l = 0; l < cmdList.length; l++) {
    var cmdText = cmdList[l];
    var cmdStr = cmdText.charAt(0);
    var cmd = void 0;
    var p = cmdText.match(numberReg) || [];
    var pLen = p.length;
    for (var i2 = 0; i2 < pLen; i2++) {
      p[i2] = parseFloat(p[i2]);
    }
    var off = 0;
    while (off < pLen) {
      var ctlPtx = void 0;
      var ctlPty = void 0;
      var rx = void 0;
      var ry = void 0;
      var psi = void 0;
      var fa = void 0;
      var fs = void 0;
      var x1 = cpx;
      var y1 = cpy;
      var len2 = void 0;
      var pathData = void 0;
      switch (cmdStr) {
        case "l":
          cpx += p[off++];
          cpy += p[off++];
          cmd = CMD5.L;
          path.addData(cmd, cpx, cpy);
          break;
        case "L":
          cpx = p[off++];
          cpy = p[off++];
          cmd = CMD5.L;
          path.addData(cmd, cpx, cpy);
          break;
        case "m":
          cpx += p[off++];
          cpy += p[off++];
          cmd = CMD5.M;
          path.addData(cmd, cpx, cpy);
          subpathX = cpx;
          subpathY = cpy;
          cmdStr = "l";
          break;
        case "M":
          cpx = p[off++];
          cpy = p[off++];
          cmd = CMD5.M;
          path.addData(cmd, cpx, cpy);
          subpathX = cpx;
          subpathY = cpy;
          cmdStr = "L";
          break;
        case "h":
          cpx += p[off++];
          cmd = CMD5.L;
          path.addData(cmd, cpx, cpy);
          break;
        case "H":
          cpx = p[off++];
          cmd = CMD5.L;
          path.addData(cmd, cpx, cpy);
          break;
        case "v":
          cpy += p[off++];
          cmd = CMD5.L;
          path.addData(cmd, cpx, cpy);
          break;
        case "V":
          cpy = p[off++];
          cmd = CMD5.L;
          path.addData(cmd, cpx, cpy);
          break;
        case "C":
          cmd = CMD5.C;
          path.addData(cmd, p[off++], p[off++], p[off++], p[off++], p[off++], p[off++]);
          cpx = p[off - 2];
          cpy = p[off - 1];
          break;
        case "c":
          cmd = CMD5.C;
          path.addData(cmd, p[off++] + cpx, p[off++] + cpy, p[off++] + cpx, p[off++] + cpy, p[off++] + cpx, p[off++] + cpy);
          cpx += p[off - 2];
          cpy += p[off - 1];
          break;
        case "S":
          ctlPtx = cpx;
          ctlPty = cpy;
          len2 = path.len();
          pathData = path.data;
          if (prevCmd === CMD5.C) {
            ctlPtx += cpx - pathData[len2 - 4];
            ctlPty += cpy - pathData[len2 - 3];
          }
          cmd = CMD5.C;
          x1 = p[off++];
          y1 = p[off++];
          cpx = p[off++];
          cpy = p[off++];
          path.addData(cmd, ctlPtx, ctlPty, x1, y1, cpx, cpy);
          break;
        case "s":
          ctlPtx = cpx;
          ctlPty = cpy;
          len2 = path.len();
          pathData = path.data;
          if (prevCmd === CMD5.C) {
            ctlPtx += cpx - pathData[len2 - 4];
            ctlPty += cpy - pathData[len2 - 3];
          }
          cmd = CMD5.C;
          x1 = cpx + p[off++];
          y1 = cpy + p[off++];
          cpx += p[off++];
          cpy += p[off++];
          path.addData(cmd, ctlPtx, ctlPty, x1, y1, cpx, cpy);
          break;
        case "Q":
          x1 = p[off++];
          y1 = p[off++];
          cpx = p[off++];
          cpy = p[off++];
          cmd = CMD5.Q;
          path.addData(cmd, x1, y1, cpx, cpy);
          break;
        case "q":
          x1 = p[off++] + cpx;
          y1 = p[off++] + cpy;
          cpx += p[off++];
          cpy += p[off++];
          cmd = CMD5.Q;
          path.addData(cmd, x1, y1, cpx, cpy);
          break;
        case "T":
          ctlPtx = cpx;
          ctlPty = cpy;
          len2 = path.len();
          pathData = path.data;
          if (prevCmd === CMD5.Q) {
            ctlPtx += cpx - pathData[len2 - 4];
            ctlPty += cpy - pathData[len2 - 3];
          }
          cpx = p[off++];
          cpy = p[off++];
          cmd = CMD5.Q;
          path.addData(cmd, ctlPtx, ctlPty, cpx, cpy);
          break;
        case "t":
          ctlPtx = cpx;
          ctlPty = cpy;
          len2 = path.len();
          pathData = path.data;
          if (prevCmd === CMD5.Q) {
            ctlPtx += cpx - pathData[len2 - 4];
            ctlPty += cpy - pathData[len2 - 3];
          }
          cpx += p[off++];
          cpy += p[off++];
          cmd = CMD5.Q;
          path.addData(cmd, ctlPtx, ctlPty, cpx, cpy);
          break;
        case "A":
          rx = p[off++];
          ry = p[off++];
          psi = p[off++];
          fa = p[off++];
          fs = p[off++];
          x1 = cpx, y1 = cpy;
          cpx = p[off++];
          cpy = p[off++];
          cmd = CMD5.A;
          processArc(x1, y1, cpx, cpy, fa, fs, rx, ry, psi, cmd, path);
          break;
        case "a":
          rx = p[off++];
          ry = p[off++];
          psi = p[off++];
          fa = p[off++];
          fs = p[off++];
          x1 = cpx, y1 = cpy;
          cpx += p[off++];
          cpy += p[off++];
          cmd = CMD5.A;
          processArc(x1, y1, cpx, cpy, fa, fs, rx, ry, psi, cmd, path);
          break;
      }
    }
    if (cmdStr === "z" || cmdStr === "Z") {
      cmd = CMD5.Z;
      path.addData(cmd);
      cpx = subpathX;
      cpy = subpathY;
    }
    prevCmd = cmd;
  }
  path.toStatic();
  return path;
}
var SVGPath = (function(_super) {
  __extends2(SVGPath2, _super);
  function SVGPath2() {
    return _super !== null && _super.apply(this, arguments) || this;
  }
  SVGPath2.prototype.applyTransform = function(m2) {
  };
  return SVGPath2;
})(Path_default);
function isPathProxy(path) {
  return path.setData != null;
}
function createPathOptions(str, opts) {
  var pathProxy = createPathProxyFromString(str);
  var innerOpts = extend({}, opts);
  innerOpts.buildPath = function(path) {
    var beProxy = isPathProxy(path);
    if (beProxy && path.canSave()) {
      path.appendPath(pathProxy);
      var ctx = path.getContext();
      if (ctx) {
        path.rebuildPath(ctx, 1);
      }
    } else {
      var ctx = beProxy ? path.getContext() : path;
      if (ctx) {
        pathProxy.rebuildPath(ctx, 1);
      }
    }
  };
  innerOpts.applyTransform = function(m2) {
    transformPath(pathProxy, m2);
    this.dirtyShape();
  };
  return innerOpts;
}
function createFromString(str, opts) {
  return new SVGPath(createPathOptions(str, opts));
}
function extendFromString(str, defaultOpts) {
  var innerOpts = createPathOptions(str, defaultOpts);
  var Sub = (function(_super) {
    __extends2(Sub2, _super);
    function Sub2(opts) {
      var _this = _super.call(this, opts) || this;
      _this.applyTransform = innerOpts.applyTransform;
      _this.buildPath = innerOpts.buildPath;
      return _this;
    }
    return Sub2;
  })(SVGPath);
  return Sub;
}
function mergePath(pathEls, opts) {
  var pathList = [];
  var len2 = pathEls.length;
  for (var i2 = 0; i2 < len2; i2++) {
    var pathEl = pathEls[i2];
    pathList.push(pathEl.getUpdatedPathProxy(true));
  }
  var pathBundle = new Path_default(opts);
  pathBundle.createPathProxy();
  pathBundle.buildPath = function(path) {
    if (isPathProxy(path)) {
      path.appendPath(pathList);
      var ctx = path.getContext();
      if (ctx) {
        path.rebuildPath(ctx, 1);
      }
    }
  };
  return pathBundle;
}

// node_modules/zrender/lib/graphic/Group.js
var Group = (function(_super) {
  __extends2(Group5, _super);
  function Group5(opts) {
    var _this = _super.call(this) || this;
    _this.isGroup = true;
    _this._children = [];
    _this.attr(opts);
    return _this;
  }
  Group5.prototype.childrenRef = function() {
    return this._children;
  };
  Group5.prototype.children = function() {
    return this._children.slice();
  };
  Group5.prototype.childAt = function(idx) {
    return this._children[idx];
  };
  Group5.prototype.childOfName = function(name) {
    var children = this._children;
    for (var i2 = 0; i2 < children.length; i2++) {
      if (children[i2].name === name) {
        return children[i2];
      }
    }
  };
  Group5.prototype.childCount = function() {
    return this._children.length;
  };
  Group5.prototype.add = function(child) {
    if (child) {
      if (child !== this && child.parent !== this) {
        this._children.push(child);
        this._doAdd(child);
      }
      if (true) {
        if (child.__hostTarget) {
          throw "This elemenet has been used as an attachment";
        }
      }
    }
    return this;
  };
  Group5.prototype.addBefore = function(child, nextSibling2) {
    if (child && child !== this && child.parent !== this && nextSibling2 && nextSibling2.parent === this) {
      var children = this._children;
      var idx = children.indexOf(nextSibling2);
      if (idx >= 0) {
        children.splice(idx, 0, child);
        this._doAdd(child);
      }
    }
    return this;
  };
  Group5.prototype.replace = function(oldChild, newChild) {
    var idx = indexOf(this._children, oldChild);
    if (idx >= 0) {
      this.replaceAt(newChild, idx);
    }
    return this;
  };
  Group5.prototype.replaceAt = function(child, index) {
    var children = this._children;
    var old = children[index];
    if (child && child !== this && child.parent !== this && child !== old) {
      children[index] = child;
      old.parent = null;
      var zr = this.__zr;
      if (zr) {
        old.removeSelfFromZr(zr);
      }
      this._doAdd(child);
    }
    return this;
  };
  Group5.prototype._doAdd = function(child) {
    if (child.parent) {
      child.parent.remove(child);
    }
    child.parent = this;
    var zr = this.__zr;
    if (zr && zr !== child.__zr) {
      child.addSelfToZr(zr);
    }
    zr && zr.refresh();
  };
  Group5.prototype.remove = function(child) {
    var zr = this.__zr;
    var children = this._children;
    var idx = indexOf(children, child);
    if (idx < 0) {
      return this;
    }
    children.splice(idx, 1);
    child.parent = null;
    if (zr) {
      child.removeSelfFromZr(zr);
    }
    zr && zr.refresh();
    return this;
  };
  Group5.prototype.removeAll = function() {
    var children = this._children;
    var zr = this.__zr;
    for (var i2 = 0; i2 < children.length; i2++) {
      var child = children[i2];
      if (zr) {
        child.removeSelfFromZr(zr);
      }
      child.parent = null;
    }
    children.length = 0;
    return this;
  };
  Group5.prototype.eachChild = function(cb, context) {
    var children = this._children;
    for (var i2 = 0; i2 < children.length; i2++) {
      var child = children[i2];
      cb.call(context, child, i2);
    }
    return this;
  };
  Group5.prototype.traverse = function(cb, context) {
    for (var i2 = 0; i2 < this._children.length; i2++) {
      var child = this._children[i2];
      var stopped = cb.call(context, child);
      if (child.isGroup && !stopped) {
        child.traverse(cb, context);
      }
    }
    return this;
  };
  Group5.prototype.addSelfToZr = function(zr) {
    _super.prototype.addSelfToZr.call(this, zr);
    for (var i2 = 0; i2 < this._children.length; i2++) {
      var child = this._children[i2];
      child.addSelfToZr(zr);
    }
  };
  Group5.prototype.removeSelfFromZr = function(zr) {
    _super.prototype.removeSelfFromZr.call(this, zr);
    for (var i2 = 0; i2 < this._children.length; i2++) {
      var child = this._children[i2];
      child.removeSelfFromZr(zr);
    }
  };
  Group5.prototype.getBoundingRect = function(includeChildren) {
    var tmpRect3 = new BoundingRect_default(0, 0, 0, 0);
    var children = includeChildren || this._children;
    var tmpMat = [];
    var rect = null;
    for (var i2 = 0; i2 < children.length; i2++) {
      var child = children[i2];
      if (child.ignore || child.invisible) {
        continue;
      }
      var childRect = child.getBoundingRect();
      var transform = child.getLocalTransform(tmpMat);
      if (transform) {
        BoundingRect_default.applyTransform(tmpRect3, childRect, transform);
        rect = rect || tmpRect3.clone();
        rect.union(tmpRect3);
      } else {
        rect = rect || childRect.clone();
        rect.union(childRect);
      }
    }
    return rect || tmpRect3;
  };
  return Group5;
})(Element_default);
Group.prototype.type = "group";
var Group_default = Group;

// node_modules/zrender/lib/graphic/shape/Circle.js
var CircleShape = /* @__PURE__ */ (function() {
  function CircleShape2() {
    this.cx = 0;
    this.cy = 0;
    this.r = 0;
  }
  return CircleShape2;
})();
var Circle = (function(_super) {
  __extends2(Circle2, _super);
  function Circle2(opts) {
    return _super.call(this, opts) || this;
  }
  Circle2.prototype.getDefaultShape = function() {
    return new CircleShape();
  };
  Circle2.prototype.buildPath = function(ctx, shape) {
    ctx.moveTo(shape.cx + shape.r, shape.cy);
    ctx.arc(shape.cx, shape.cy, shape.r, 0, Math.PI * 2);
  };
  return Circle2;
})(Path_default);
Circle.prototype.type = "circle";
var Circle_default = Circle;

// node_modules/zrender/lib/graphic/shape/Ellipse.js
var EllipseShape = /* @__PURE__ */ (function() {
  function EllipseShape2() {
    this.cx = 0;
    this.cy = 0;
    this.rx = 0;
    this.ry = 0;
  }
  return EllipseShape2;
})();
var Ellipse = (function(_super) {
  __extends2(Ellipse2, _super);
  function Ellipse2(opts) {
    return _super.call(this, opts) || this;
  }
  Ellipse2.prototype.getDefaultShape = function() {
    return new EllipseShape();
  };
  Ellipse2.prototype.buildPath = function(ctx, shape) {
    var k2 = 0.5522848;
    var x = shape.cx;
    var y = shape.cy;
    var a = shape.rx;
    var b = shape.ry;
    var ox = a * k2;
    var oy = b * k2;
    ctx.moveTo(x - a, y);
    ctx.bezierCurveTo(x - a, y - oy, x - ox, y - b, x, y - b);
    ctx.bezierCurveTo(x + ox, y - b, x + a, y - oy, x + a, y);
    ctx.bezierCurveTo(x + a, y + oy, x + ox, y + b, x, y + b);
    ctx.bezierCurveTo(x - ox, y + b, x - a, y + oy, x - a, y);
    ctx.closePath();
  };
  return Ellipse2;
})(Path_default);
Ellipse.prototype.type = "ellipse";
var Ellipse_default = Ellipse;

// node_modules/zrender/lib/graphic/helper/roundSector.js
var PI4 = Math.PI;
var PI26 = PI4 * 2;
var mathSin4 = Math.sin;
var mathCos4 = Math.cos;
var mathACos = Math.acos;
var mathATan2 = Math.atan2;
var mathAbs4 = Math.abs;
var mathSqrt4 = Math.sqrt;
var mathMax5 = Math.max;
var mathMin5 = Math.min;
var e = 1e-4;
function intersect(x0, y0, x1, y1, x2, y2, x3, y3) {
  var dx10 = x1 - x0;
  var dy10 = y1 - y0;
  var dx32 = x3 - x2;
  var dy32 = y3 - y2;
  var t = dy32 * dx10 - dx32 * dy10;
  if (t * t < e) {
    return;
  }
  t = (dx32 * (y0 - y2) - dy32 * (x0 - x2)) / t;
  return [x0 + t * dx10, y0 + t * dy10];
}
function computeCornerTangents(x0, y0, x1, y1, radius, cr, clockwise) {
  var x01 = x0 - x1;
  var y01 = y0 - y1;
  var lo = (clockwise ? cr : -cr) / mathSqrt4(x01 * x01 + y01 * y01);
  var ox = lo * y01;
  var oy = -lo * x01;
  var x11 = x0 + ox;
  var y11 = y0 + oy;
  var x10 = x1 + ox;
  var y10 = y1 + oy;
  var x00 = (x11 + x10) / 2;
  var y00 = (y11 + y10) / 2;
  var dx = x10 - x11;
  var dy = y10 - y11;
  var d2 = dx * dx + dy * dy;
  var r = radius - cr;
  var s = x11 * y10 - x10 * y11;
  var d = (dy < 0 ? -1 : 1) * mathSqrt4(mathMax5(0, r * r * d2 - s * s));
  var cx0 = (s * dy - dx * d) / d2;
  var cy0 = (-s * dx - dy * d) / d2;
  var cx1 = (s * dy + dx * d) / d2;
  var cy1 = (-s * dx + dy * d) / d2;
  var dx0 = cx0 - x00;
  var dy0 = cy0 - y00;
  var dx1 = cx1 - x00;
  var dy1 = cy1 - y00;
  if (dx0 * dx0 + dy0 * dy0 > dx1 * dx1 + dy1 * dy1) {
    cx0 = cx1;
    cy0 = cy1;
  }
  return {
    cx: cx0,
    cy: cy0,
    x0: -ox,
    y0: -oy,
    x1: cx0 * (radius / r - 1),
    y1: cy0 * (radius / r - 1)
  };
}
function normalizeCornerRadius(cr) {
  var arr;
  if (isArray(cr)) {
    var len2 = cr.length;
    if (!len2) {
      return cr;
    }
    if (len2 === 1) {
      arr = [cr[0], cr[0], 0, 0];
    } else if (len2 === 2) {
      arr = [cr[0], cr[0], cr[1], cr[1]];
    } else if (len2 === 3) {
      arr = cr.concat(cr[2]);
    } else {
      arr = cr;
    }
  } else {
    arr = [cr, cr, cr, cr];
  }
  return arr;
}
function buildPath2(ctx, shape) {
  var _a2;
  var radius = mathMax5(shape.r, 0);
  var innerRadius = mathMax5(shape.r0 || 0, 0);
  var hasRadius = radius > 0;
  var hasInnerRadius = innerRadius > 0;
  if (!hasRadius && !hasInnerRadius) {
    return;
  }
  if (!hasRadius) {
    radius = innerRadius;
    innerRadius = 0;
  }
  if (innerRadius > radius) {
    var tmp = radius;
    radius = innerRadius;
    innerRadius = tmp;
  }
  var startAngle = shape.startAngle, endAngle = shape.endAngle;
  if (isNaN(startAngle) || isNaN(endAngle)) {
    return;
  }
  var cx = shape.cx, cy = shape.cy;
  var clockwise = !!shape.clockwise;
  var arc = mathAbs4(endAngle - startAngle);
  var mod = arc > PI26 && arc % PI26;
  mod > e && (arc = mod);
  if (!(radius > e)) {
    ctx.moveTo(cx, cy);
  } else if (arc > PI26 - e) {
    ctx.moveTo(cx + radius * mathCos4(startAngle), cy + radius * mathSin4(startAngle));
    ctx.arc(cx, cy, radius, startAngle, endAngle, !clockwise);
    if (innerRadius > e) {
      ctx.moveTo(cx + innerRadius * mathCos4(endAngle), cy + innerRadius * mathSin4(endAngle));
      ctx.arc(cx, cy, innerRadius, endAngle, startAngle, clockwise);
    }
  } else {
    var icrStart = void 0;
    var icrEnd = void 0;
    var ocrStart = void 0;
    var ocrEnd = void 0;
    var ocrs = void 0;
    var ocre = void 0;
    var icrs = void 0;
    var icre = void 0;
    var ocrMax = void 0;
    var icrMax = void 0;
    var limitedOcrMax = void 0;
    var limitedIcrMax = void 0;
    var xre = void 0;
    var yre = void 0;
    var xirs = void 0;
    var yirs = void 0;
    var xrs = radius * mathCos4(startAngle);
    var yrs = radius * mathSin4(startAngle);
    var xire = innerRadius * mathCos4(endAngle);
    var yire = innerRadius * mathSin4(endAngle);
    var hasArc = arc > e;
    if (hasArc) {
      var cornerRadius = shape.cornerRadius;
      if (cornerRadius) {
        _a2 = normalizeCornerRadius(cornerRadius), icrStart = _a2[0], icrEnd = _a2[1], ocrStart = _a2[2], ocrEnd = _a2[3];
      }
      var halfRd = mathAbs4(radius - innerRadius) / 2;
      ocrs = mathMin5(halfRd, ocrStart);
      ocre = mathMin5(halfRd, ocrEnd);
      icrs = mathMin5(halfRd, icrStart);
      icre = mathMin5(halfRd, icrEnd);
      limitedOcrMax = ocrMax = mathMax5(ocrs, ocre);
      limitedIcrMax = icrMax = mathMax5(icrs, icre);
      if (ocrMax > e || icrMax > e) {
        xre = radius * mathCos4(endAngle);
        yre = radius * mathSin4(endAngle);
        xirs = innerRadius * mathCos4(startAngle);
        yirs = innerRadius * mathSin4(startAngle);
        if (arc < PI4) {
          var it_1 = intersect(xrs, yrs, xirs, yirs, xre, yre, xire, yire);
          if (it_1) {
            var x0 = xrs - it_1[0];
            var y0 = yrs - it_1[1];
            var x1 = xre - it_1[0];
            var y1 = yre - it_1[1];
            var a = 1 / mathSin4(mathACos((x0 * x1 + y0 * y1) / (mathSqrt4(x0 * x0 + y0 * y0) * mathSqrt4(x1 * x1 + y1 * y1))) / 2);
            var b = mathSqrt4(it_1[0] * it_1[0] + it_1[1] * it_1[1]);
            limitedOcrMax = mathMin5(ocrMax, (radius - b) / (a + 1));
            limitedIcrMax = mathMin5(icrMax, (innerRadius - b) / (a - 1));
          }
        }
      }
    }
    if (!hasArc) {
      ctx.moveTo(cx + xrs, cy + yrs);
    } else if (limitedOcrMax > e) {
      var crStart = mathMin5(ocrStart, limitedOcrMax);
      var crEnd = mathMin5(ocrEnd, limitedOcrMax);
      var ct0 = computeCornerTangents(xirs, yirs, xrs, yrs, radius, crStart, clockwise);
      var ct1 = computeCornerTangents(xre, yre, xire, yire, radius, crEnd, clockwise);
      ctx.moveTo(cx + ct0.cx + ct0.x0, cy + ct0.cy + ct0.y0);
      if (limitedOcrMax < ocrMax && crStart === crEnd) {
        ctx.arc(cx + ct0.cx, cy + ct0.cy, limitedOcrMax, mathATan2(ct0.y0, ct0.x0), mathATan2(ct1.y0, ct1.x0), !clockwise);
      } else {
        crStart > 0 && ctx.arc(cx + ct0.cx, cy + ct0.cy, crStart, mathATan2(ct0.y0, ct0.x0), mathATan2(ct0.y1, ct0.x1), !clockwise);
        ctx.arc(cx, cy, radius, mathATan2(ct0.cy + ct0.y1, ct0.cx + ct0.x1), mathATan2(ct1.cy + ct1.y1, ct1.cx + ct1.x1), !clockwise);
        crEnd > 0 && ctx.arc(cx + ct1.cx, cy + ct1.cy, crEnd, mathATan2(ct1.y1, ct1.x1), mathATan2(ct1.y0, ct1.x0), !clockwise);
      }
    } else {
      ctx.moveTo(cx + xrs, cy + yrs);
      ctx.arc(cx, cy, radius, startAngle, endAngle, !clockwise);
    }
    if (!(innerRadius > e) || !hasArc) {
      ctx.lineTo(cx + xire, cy + yire);
    } else if (limitedIcrMax > e) {
      var crStart = mathMin5(icrStart, limitedIcrMax);
      var crEnd = mathMin5(icrEnd, limitedIcrMax);
      var ct0 = computeCornerTangents(xire, yire, xre, yre, innerRadius, -crEnd, clockwise);
      var ct1 = computeCornerTangents(xrs, yrs, xirs, yirs, innerRadius, -crStart, clockwise);
      ctx.lineTo(cx + ct0.cx + ct0.x0, cy + ct0.cy + ct0.y0);
      if (limitedIcrMax < icrMax && crStart === crEnd) {
        ctx.arc(cx + ct0.cx, cy + ct0.cy, limitedIcrMax, mathATan2(ct0.y0, ct0.x0), mathATan2(ct1.y0, ct1.x0), !clockwise);
      } else {
        crEnd > 0 && ctx.arc(cx + ct0.cx, cy + ct0.cy, crEnd, mathATan2(ct0.y0, ct0.x0), mathATan2(ct0.y1, ct0.x1), !clockwise);
        ctx.arc(cx, cy, innerRadius, mathATan2(ct0.cy + ct0.y1, ct0.cx + ct0.x1), mathATan2(ct1.cy + ct1.y1, ct1.cx + ct1.x1), clockwise);
        crStart > 0 && ctx.arc(cx + ct1.cx, cy + ct1.cy, crStart, mathATan2(ct1.y1, ct1.x1), mathATan2(ct1.y0, ct1.x0), !clockwise);
      }
    } else {
      ctx.lineTo(cx + xire, cy + yire);
      ctx.arc(cx, cy, innerRadius, endAngle, startAngle, clockwise);
    }
  }
  ctx.closePath();
}

// node_modules/zrender/lib/graphic/shape/Sector.js
var SectorShape = /* @__PURE__ */ (function() {
  function SectorShape2() {
    this.cx = 0;
    this.cy = 0;
    this.r0 = 0;
    this.r = 0;
    this.startAngle = 0;
    this.endAngle = Math.PI * 2;
    this.clockwise = true;
    this.cornerRadius = 0;
  }
  return SectorShape2;
})();
var Sector = (function(_super) {
  __extends2(Sector2, _super);
  function Sector2(opts) {
    return _super.call(this, opts) || this;
  }
  Sector2.prototype.getDefaultShape = function() {
    return new SectorShape();
  };
  Sector2.prototype.buildPath = function(ctx, shape) {
    buildPath2(ctx, shape);
  };
  Sector2.prototype.isZeroArea = function() {
    return this.shape.startAngle === this.shape.endAngle || this.shape.r === this.shape.r0;
  };
  return Sector2;
})(Path_default);
Sector.prototype.type = "sector";
var Sector_default = Sector;

// node_modules/zrender/lib/graphic/shape/Ring.js
var RingShape = /* @__PURE__ */ (function() {
  function RingShape2() {
    this.cx = 0;
    this.cy = 0;
    this.r = 0;
    this.r0 = 0;
  }
  return RingShape2;
})();
var Ring = (function(_super) {
  __extends2(Ring2, _super);
  function Ring2(opts) {
    return _super.call(this, opts) || this;
  }
  Ring2.prototype.getDefaultShape = function() {
    return new RingShape();
  };
  Ring2.prototype.buildPath = function(ctx, shape) {
    var x = shape.cx;
    var y = shape.cy;
    var PI210 = Math.PI * 2;
    ctx.moveTo(x + shape.r, y);
    ctx.arc(x, y, shape.r, 0, PI210, false);
    ctx.moveTo(x + shape.r0, y);
    ctx.arc(x, y, shape.r0, 0, PI210, true);
  };
  return Ring2;
})(Path_default);
Ring.prototype.type = "ring";
var Ring_default = Ring;

// node_modules/zrender/lib/graphic/helper/smoothBezier.js
function smoothBezier(points4, smooth, isLoop, constraint) {
  var cps = [];
  var v = [];
  var v12 = [];
  var v22 = [];
  var prevPoint;
  var nextPoint;
  var min3;
  var max3;
  if (constraint) {
    min3 = [Infinity, Infinity];
    max3 = [-Infinity, -Infinity];
    for (var i2 = 0, len2 = points4.length; i2 < len2; i2++) {
      min(min3, min3, points4[i2]);
      max(max3, max3, points4[i2]);
    }
    min(min3, min3, constraint[0]);
    max(max3, max3, constraint[1]);
  }
  for (var i2 = 0, len2 = points4.length; i2 < len2; i2++) {
    var point = points4[i2];
    if (isLoop) {
      prevPoint = points4[i2 ? i2 - 1 : len2 - 1];
      nextPoint = points4[(i2 + 1) % len2];
    } else {
      if (i2 === 0 || i2 === len2 - 1) {
        cps.push(clone3(points4[i2]));
        continue;
      } else {
        prevPoint = points4[i2 - 1];
        nextPoint = points4[i2 + 1];
      }
    }
    sub(v, nextPoint, prevPoint);
    scale2(v, v, smooth);
    var d0 = distance(point, prevPoint);
    var d1 = distance(point, nextPoint);
    var sum2 = d0 + d1;
    if (sum2 !== 0) {
      d0 /= sum2;
      d1 /= sum2;
    }
    scale2(v12, v, -d0);
    scale2(v22, v, d1);
    var cp0 = add([], point, v12);
    var cp1 = add([], point, v22);
    if (constraint) {
      max(cp0, cp0, min3);
      min(cp0, cp0, max3);
      max(cp1, cp1, min3);
      min(cp1, cp1, max3);
    }
    cps.push(cp0);
    cps.push(cp1);
  }
  if (isLoop) {
    cps.push(cps.shift());
  }
  return cps;
}

// node_modules/zrender/lib/graphic/helper/poly.js
function buildPath3(ctx, shape, closePath) {
  var smooth = shape.smooth;
  var points4 = shape.points;
  if (points4 && points4.length >= 2) {
    if (smooth) {
      var controlPoints = smoothBezier(points4, smooth, closePath, shape.smoothConstraint);
      ctx.moveTo(points4[0][0], points4[0][1]);
      var len2 = points4.length;
      for (var i2 = 0; i2 < (closePath ? len2 : len2 - 1); i2++) {
        var cp1 = controlPoints[i2 * 2];
        var cp2 = controlPoints[i2 * 2 + 1];
        var p = points4[(i2 + 1) % len2];
        ctx.bezierCurveTo(cp1[0], cp1[1], cp2[0], cp2[1], p[0], p[1]);
      }
    } else {
      ctx.moveTo(points4[0][0], points4[0][1]);
      for (var i2 = 1, l = points4.length; i2 < l; i2++) {
        ctx.lineTo(points4[i2][0], points4[i2][1]);
      }
    }
    closePath && ctx.closePath();
  }
}

// node_modules/zrender/lib/graphic/shape/Polygon.js
var PolygonShape = /* @__PURE__ */ (function() {
  function PolygonShape2() {
    this.points = null;
    this.smooth = 0;
    this.smoothConstraint = null;
  }
  return PolygonShape2;
})();
var Polygon = (function(_super) {
  __extends2(Polygon2, _super);
  function Polygon2(opts) {
    return _super.call(this, opts) || this;
  }
  Polygon2.prototype.getDefaultShape = function() {
    return new PolygonShape();
  };
  Polygon2.prototype.buildPath = function(ctx, shape) {
    buildPath3(ctx, shape, true);
  };
  return Polygon2;
})(Path_default);
Polygon.prototype.type = "polygon";
var Polygon_default = Polygon;

// node_modules/zrender/lib/graphic/shape/Polyline.js
var PolylineShape = /* @__PURE__ */ (function() {
  function PolylineShape2() {
    this.points = null;
    this.percent = 1;
    this.smooth = 0;
    this.smoothConstraint = null;
  }
  return PolylineShape2;
})();
var Polyline = (function(_super) {
  __extends2(Polyline3, _super);
  function Polyline3(opts) {
    return _super.call(this, opts) || this;
  }
  Polyline3.prototype.getDefaultStyle = function() {
    return {
      stroke: "#000",
      fill: null
    };
  };
  Polyline3.prototype.getDefaultShape = function() {
    return new PolylineShape();
  };
  Polyline3.prototype.buildPath = function(ctx, shape) {
    buildPath3(ctx, shape, false);
  };
  return Polyline3;
})(Path_default);
Polyline.prototype.type = "polyline";
var Polyline_default = Polyline;

// node_modules/zrender/lib/graphic/shape/Line.js
var subPixelOptimizeOutputShape2 = {};
var LineShape = /* @__PURE__ */ (function() {
  function LineShape2() {
    this.x1 = 0;
    this.y1 = 0;
    this.x2 = 0;
    this.y2 = 0;
    this.percent = 1;
  }
  return LineShape2;
})();
var Line = (function(_super) {
  __extends2(Line3, _super);
  function Line3(opts) {
    return _super.call(this, opts) || this;
  }
  Line3.prototype.getDefaultStyle = function() {
    return {
      stroke: "#000",
      fill: null
    };
  };
  Line3.prototype.getDefaultShape = function() {
    return new LineShape();
  };
  Line3.prototype.buildPath = function(ctx, shape) {
    var x1;
    var y1;
    var x2;
    var y2;
    if (this.subPixelOptimize) {
      var optimizedShape = subPixelOptimizeLine(subPixelOptimizeOutputShape2, shape, this.style);
      x1 = optimizedShape.x1;
      y1 = optimizedShape.y1;
      x2 = optimizedShape.x2;
      y2 = optimizedShape.y2;
    } else {
      x1 = shape.x1;
      y1 = shape.y1;
      x2 = shape.x2;
      y2 = shape.y2;
    }
    var percent = shape.percent;
    if (percent === 0) {
      return;
    }
    ctx.moveTo(x1, y1);
    if (percent < 1) {
      x2 = x1 * (1 - percent) + x2 * percent;
      y2 = y1 * (1 - percent) + y2 * percent;
    }
    ctx.lineTo(x2, y2);
  };
  Line3.prototype.pointAt = function(p) {
    var shape = this.shape;
    return [
      shape.x1 * (1 - p) + shape.x2 * p,
      shape.y1 * (1 - p) + shape.y2 * p
    ];
  };
  return Line3;
})(Path_default);
Line.prototype.type = "line";
var Line_default = Line;

// node_modules/zrender/lib/graphic/shape/BezierCurve.js
var out = [];
var BezierCurveShape = /* @__PURE__ */ (function() {
  function BezierCurveShape2() {
    this.x1 = 0;
    this.y1 = 0;
    this.x2 = 0;
    this.y2 = 0;
    this.cpx1 = 0;
    this.cpy1 = 0;
    this.percent = 1;
  }
  return BezierCurveShape2;
})();
function someVectorAt(shape, t, isTangent) {
  var cpx2 = shape.cpx2;
  var cpy2 = shape.cpy2;
  if (cpx2 != null || cpy2 != null) {
    return [
      (isTangent ? cubicDerivativeAt : cubicAt)(shape.x1, shape.cpx1, shape.cpx2, shape.x2, t),
      (isTangent ? cubicDerivativeAt : cubicAt)(shape.y1, shape.cpy1, shape.cpy2, shape.y2, t)
    ];
  } else {
    return [
      (isTangent ? quadraticDerivativeAt : quadraticAt)(shape.x1, shape.cpx1, shape.x2, t),
      (isTangent ? quadraticDerivativeAt : quadraticAt)(shape.y1, shape.cpy1, shape.y2, t)
    ];
  }
}
var BezierCurve = (function(_super) {
  __extends2(BezierCurve2, _super);
  function BezierCurve2(opts) {
    return _super.call(this, opts) || this;
  }
  BezierCurve2.prototype.getDefaultStyle = function() {
    return {
      stroke: "#000",
      fill: null
    };
  };
  BezierCurve2.prototype.getDefaultShape = function() {
    return new BezierCurveShape();
  };
  BezierCurve2.prototype.buildPath = function(ctx, shape) {
    var x1 = shape.x1;
    var y1 = shape.y1;
    var x2 = shape.x2;
    var y2 = shape.y2;
    var cpx1 = shape.cpx1;
    var cpy1 = shape.cpy1;
    var cpx2 = shape.cpx2;
    var cpy2 = shape.cpy2;
    var percent = shape.percent;
    if (percent === 0) {
      return;
    }
    ctx.moveTo(x1, y1);
    if (cpx2 == null || cpy2 == null) {
      if (percent < 1) {
        quadraticSubdivide(x1, cpx1, x2, percent, out);
        cpx1 = out[1];
        x2 = out[2];
        quadraticSubdivide(y1, cpy1, y2, percent, out);
        cpy1 = out[1];
        y2 = out[2];
      }
      ctx.quadraticCurveTo(cpx1, cpy1, x2, y2);
    } else {
      if (percent < 1) {
        cubicSubdivide(x1, cpx1, cpx2, x2, percent, out);
        cpx1 = out[1];
        cpx2 = out[2];
        x2 = out[3];
        cubicSubdivide(y1, cpy1, cpy2, y2, percent, out);
        cpy1 = out[1];
        cpy2 = out[2];
        y2 = out[3];
      }
      ctx.bezierCurveTo(cpx1, cpy1, cpx2, cpy2, x2, y2);
    }
  };
  BezierCurve2.prototype.pointAt = function(t) {
    return someVectorAt(this.shape, t, false);
  };
  BezierCurve2.prototype.tangentAt = function(t) {
    var p = someVectorAt(this.shape, t, true);
    return normalize(p, p);
  };
  return BezierCurve2;
})(Path_default);
BezierCurve.prototype.type = "bezier-curve";
var BezierCurve_default = BezierCurve;

// node_modules/zrender/lib/graphic/shape/Arc.js
var ArcShape = /* @__PURE__ */ (function() {
  function ArcShape2() {
    this.cx = 0;
    this.cy = 0;
    this.r = 0;
    this.startAngle = 0;
    this.endAngle = Math.PI * 2;
    this.clockwise = true;
  }
  return ArcShape2;
})();
var Arc = (function(_super) {
  __extends2(Arc2, _super);
  function Arc2(opts) {
    return _super.call(this, opts) || this;
  }
  Arc2.prototype.getDefaultStyle = function() {
    return {
      stroke: "#000",
      fill: null
    };
  };
  Arc2.prototype.getDefaultShape = function() {
    return new ArcShape();
  };
  Arc2.prototype.buildPath = function(ctx, shape) {
    var x = shape.cx;
    var y = shape.cy;
    var r = Math.max(shape.r, 0);
    var startAngle = shape.startAngle;
    var endAngle = shape.endAngle;
    var clockwise = shape.clockwise;
    var unitX = Math.cos(startAngle);
    var unitY = Math.sin(startAngle);
    ctx.moveTo(unitX * r + x, unitY * r + y);
    ctx.arc(x, y, r, startAngle, endAngle, !clockwise);
  };
  return Arc2;
})(Path_default);
Arc.prototype.type = "arc";
var Arc_default = Arc;

// node_modules/zrender/lib/graphic/CompoundPath.js
var CompoundPath = (function(_super) {
  __extends2(CompoundPath2, _super);
  function CompoundPath2() {
    var _this = _super !== null && _super.apply(this, arguments) || this;
    _this.type = "compound";
    return _this;
  }
  CompoundPath2.prototype._updatePathDirty = function() {
    var paths = this.shape.paths;
    var dirtyPath = this.shapeChanged();
    for (var i2 = 0; i2 < paths.length; i2++) {
      dirtyPath = dirtyPath || paths[i2].shapeChanged();
    }
    if (dirtyPath) {
      this.dirtyShape();
    }
  };
  CompoundPath2.prototype.beforeBrush = function() {
    this._updatePathDirty();
    var paths = this.shape.paths || [];
    var scale3 = this.getGlobalScale();
    for (var i2 = 0; i2 < paths.length; i2++) {
      if (!paths[i2].path) {
        paths[i2].createPathProxy();
      }
      paths[i2].path.setScale(scale3[0], scale3[1], paths[i2].segmentIgnoreThreshold);
    }
  };
  CompoundPath2.prototype.buildPath = function(ctx, shape) {
    var paths = shape.paths || [];
    for (var i2 = 0; i2 < paths.length; i2++) {
      paths[i2].buildPath(ctx, paths[i2].shape, true);
    }
  };
  CompoundPath2.prototype.afterBrush = function() {
    var paths = this.shape.paths || [];
    for (var i2 = 0; i2 < paths.length; i2++) {
      paths[i2].pathUpdated();
    }
  };
  CompoundPath2.prototype.getBoundingRect = function() {
    this._updatePathDirty.call(this);
    return Path_default.prototype.getBoundingRect.call(this);
  };
  return CompoundPath2;
})(Path_default);
var CompoundPath_default = CompoundPath;

// node_modules/zrender/lib/graphic/Gradient.js
var Gradient = (function() {
  function Gradient2(colorStops) {
    this.colorStops = colorStops || [];
  }
  Gradient2.prototype.addColorStop = function(offset, color3) {
    this.colorStops.push({
      offset,
      color: color3
    });
  };
  return Gradient2;
})();
var Gradient_default = Gradient;

// node_modules/zrender/lib/graphic/LinearGradient.js
var LinearGradient = (function(_super) {
  __extends2(LinearGradient2, _super);
  function LinearGradient2(x, y, x2, y2, colorStops, globalCoord) {
    var _this = _super.call(this, colorStops) || this;
    _this.x = x == null ? 0 : x;
    _this.y = y == null ? 0 : y;
    _this.x2 = x2 == null ? 1 : x2;
    _this.y2 = y2 == null ? 0 : y2;
    _this.type = "linear";
    _this.global = globalCoord || false;
    return _this;
  }
  return LinearGradient2;
})(Gradient_default);
var LinearGradient_default = LinearGradient;

// node_modules/zrender/lib/graphic/RadialGradient.js
var RadialGradient = (function(_super) {
  __extends2(RadialGradient2, _super);
  function RadialGradient2(x, y, r, colorStops, globalCoord) {
    var _this = _super.call(this, colorStops) || this;
    _this.x = x == null ? 0.5 : x;
    _this.y = y == null ? 0.5 : y;
    _this.r = r == null ? 0.5 : r;
    _this.type = "radial";
    _this.global = globalCoord || false;
    return _this;
  }
  return RadialGradient2;
})(Gradient_default);
var RadialGradient_default = RadialGradient;

// node_modules/zrender/lib/core/OrientedBoundingRect.js
var mathMin6 = Math.min;
var mathMax6 = Math.max;
var mathAbs5 = Math.abs;
var _extent = [0, 0];
var _extent2 = [0, 0];
var _intersectCtx2 = createIntersectContext();
var _minTv2 = _intersectCtx2.minTv;
var _maxTv2 = _intersectCtx2.maxTv;
var OrientedBoundingRect = (function() {
  function OrientedBoundingRect2(rect, transform) {
    this._corners = [];
    this._axes = [];
    this._origin = [0, 0];
    for (var i2 = 0; i2 < 4; i2++) {
      this._corners[i2] = new Point_default();
    }
    for (var i2 = 0; i2 < 2; i2++) {
      this._axes[i2] = new Point_default();
    }
    if (rect) {
      this.fromBoundingRect(rect, transform);
    }
  }
  OrientedBoundingRect2.prototype.fromBoundingRect = function(rect, transform) {
    var corners = this._corners;
    var axes = this._axes;
    var x = rect.x;
    var y = rect.y;
    var x2 = x + rect.width;
    var y2 = y + rect.height;
    corners[0].set(x, y);
    corners[1].set(x2, y);
    corners[2].set(x2, y2);
    corners[3].set(x, y2);
    if (transform) {
      for (var i2 = 0; i2 < 4; i2++) {
        corners[i2].transform(transform);
      }
    }
    Point_default.sub(axes[0], corners[1], corners[0]);
    Point_default.sub(axes[1], corners[3], corners[0]);
    axes[0].normalize();
    axes[1].normalize();
    for (var i2 = 0; i2 < 2; i2++) {
      this._origin[i2] = axes[i2].dot(corners[0]);
    }
  };
  OrientedBoundingRect2.prototype.intersect = function(other, mtv, opt) {
    var overlapped = true;
    var noMtv = !mtv;
    if (mtv) {
      Point_default.set(mtv, 0, 0);
    }
    _intersectCtx2.reset(opt, !noMtv);
    if (!this._intersectCheckOneSide(this, other, noMtv, 1)) {
      overlapped = false;
      if (noMtv) {
        return overlapped;
      }
    }
    if (!this._intersectCheckOneSide(other, this, noMtv, -1)) {
      overlapped = false;
      if (noMtv) {
        return overlapped;
      }
    }
    if (!noMtv && !_intersectCtx2.negativeSize) {
      Point_default.copy(mtv, overlapped ? _intersectCtx2.useDir ? _intersectCtx2.dirMinTv : _minTv2 : _maxTv2);
    }
    return overlapped;
  };
  OrientedBoundingRect2.prototype._intersectCheckOneSide = function(self2, other, noMtv, inverse) {
    var overlapped = true;
    for (var i2 = 0; i2 < 2; i2++) {
      var axis = self2._axes[i2];
      self2._getProjMinMaxOnAxis(i2, self2._corners, _extent);
      self2._getProjMinMaxOnAxis(i2, other._corners, _extent2);
      if (_intersectCtx2.negativeSize || _extent[1] < _extent2[0] || _extent[0] > _extent2[1]) {
        overlapped = false;
        if (_intersectCtx2.negativeSize || noMtv) {
          return overlapped;
        }
        var dist0 = mathAbs5(_extent2[0] - _extent[1]);
        var dist1 = mathAbs5(_extent[0] - _extent2[1]);
        if (mathMin6(dist0, dist1) > _maxTv2.len()) {
          if (dist0 < dist1) {
            Point_default.scale(_maxTv2, axis, -dist0 * inverse);
          } else {
            Point_default.scale(_maxTv2, axis, dist1 * inverse);
          }
        }
      } else if (!noMtv) {
        var dist0 = mathAbs5(_extent2[0] - _extent[1]);
        var dist1 = mathAbs5(_extent[0] - _extent2[1]);
        if (_intersectCtx2.useDir || mathMin6(dist0, dist1) < _minTv2.len()) {
          if (dist0 < dist1 || !_intersectCtx2.bidirectional) {
            Point_default.scale(_minTv2, axis, dist0 * inverse);
            if (_intersectCtx2.useDir) {
              _intersectCtx2.calcDirMTV();
            }
          }
          if (dist0 >= dist1 || !_intersectCtx2.bidirectional) {
            Point_default.scale(_minTv2, axis, -dist1 * inverse);
            if (_intersectCtx2.useDir) {
              _intersectCtx2.calcDirMTV();
            }
          }
        }
      }
    }
    return overlapped;
  };
  OrientedBoundingRect2.prototype._getProjMinMaxOnAxis = function(dim, corners, out2) {
    var axis = this._axes[dim];
    var origin = this._origin;
    var proj = corners[0].dot(axis) + origin[dim];
    var min3 = proj;
    var max3 = proj;
    for (var i2 = 1; i2 < corners.length; i2++) {
      var proj_1 = corners[i2].dot(axis) + origin[dim];
      min3 = mathMin6(proj_1, min3);
      max3 = mathMax6(proj_1, max3);
    }
    out2[0] = min3 + _intersectCtx2.touchThreshold;
    out2[1] = max3 - _intersectCtx2.touchThreshold;
    _intersectCtx2.negativeSize = out2[1] < out2[0];
  };
  return OrientedBoundingRect2;
})();
var OrientedBoundingRect_default = OrientedBoundingRect;

// node_modules/zrender/lib/core/types.js
var INCREMENTAL_ID_FALSE = 0;
var INCREMENTAL_ID_TRUE_COMPAT = 1;
var ZLEVEL2_NORMAL_ABOVE = 2;
var ZLEVEL2_INCREMENTAL = 1;
var ZLEVEL2_NORMAL_BELOW = 0;

// node_modules/zrender/lib/graphic/IncrementalDisplayable.js
var m = [];
var IncrementalDisplayable = (function(_super) {
  __extends2(IncrementalDisplayable2, _super);
  function IncrementalDisplayable2() {
    var _this = _super !== null && _super.apply(this, arguments) || this;
    _this.notClear = true;
    _this.incremental = INCREMENTAL_ID_TRUE_COMPAT;
    _this._displayables = [];
    _this._temporaryDisplayables = [];
    _this._cursor = 0;
    return _this;
  }
  IncrementalDisplayable2.prototype.traverse = function(cb, context) {
    cb.call(context, this);
  };
  IncrementalDisplayable2.prototype.useStyle = function() {
    this.style = {};
  };
  IncrementalDisplayable2.prototype._useHoverStyle = function() {
    this.__hoverStyle = null;
  };
  IncrementalDisplayable2.prototype.getCursor = function() {
    return this._cursor;
  };
  IncrementalDisplayable2.prototype.innerAfterBrush = function() {
    this._cursor = this._displayables.length;
  };
  IncrementalDisplayable2.prototype.clearDisplaybles = function() {
    this._displayables = [];
    this._temporaryDisplayables = [];
    this._cursor = 0;
    this.markRedraw();
    this.notClear = false;
  };
  IncrementalDisplayable2.prototype.clearTemporalDisplayables = function() {
    this._temporaryDisplayables = [];
  };
  IncrementalDisplayable2.prototype.addDisplayable = function(displayable, notPersistent) {
    if (notPersistent) {
      this._temporaryDisplayables.push(displayable);
    } else {
      this._displayables.push(displayable);
    }
    this.markRedraw();
  };
  IncrementalDisplayable2.prototype.addDisplayables = function(displayables, notPersistent) {
    notPersistent = notPersistent || false;
    for (var i2 = 0; i2 < displayables.length; i2++) {
      this.addDisplayable(displayables[i2], notPersistent);
    }
  };
  IncrementalDisplayable2.prototype.getDisplayables = function() {
    return this._displayables;
  };
  IncrementalDisplayable2.prototype.getTemporalDisplayables = function() {
    return this._temporaryDisplayables;
  };
  IncrementalDisplayable2.prototype.eachPendingDisplayable = function(cb) {
    for (var i2 = this._cursor; i2 < this._displayables.length; i2++) {
      cb && cb(this._displayables[i2]);
    }
    for (var i2 = 0; i2 < this._temporaryDisplayables.length; i2++) {
      cb && cb(this._temporaryDisplayables[i2]);
    }
  };
  IncrementalDisplayable2.prototype.update = function() {
    this.updateTransform();
    for (var i2 = this._cursor; i2 < this._displayables.length; i2++) {
      var displayable = this._displayables[i2];
      displayable.parent = this;
      displayable.update();
      displayable.parent = null;
    }
    for (var i2 = 0; i2 < this._temporaryDisplayables.length; i2++) {
      var displayable = this._temporaryDisplayables[i2];
      displayable.parent = this;
      displayable.update();
      displayable.parent = null;
    }
  };
  IncrementalDisplayable2.prototype.getBoundingRect = function() {
    if (!this._rect) {
      var rect = new BoundingRect_default(Infinity, Infinity, -Infinity, -Infinity);
      for (var i2 = 0; i2 < this._displayables.length; i2++) {
        var displayable = this._displayables[i2];
        var childRect = displayable.getBoundingRect().clone();
        if (displayable.needLocalTransform()) {
          childRect.applyTransform(displayable.getLocalTransform(m));
        }
        rect.union(childRect);
      }
      this._rect = rect;
    }
    return this._rect;
  };
  IncrementalDisplayable2.prototype.contain = function(x, y) {
    var localPos = this.transformCoordToLocal(x, y);
    var rect = this.getBoundingRect();
    if (rect.contain(localPos[0], localPos[1])) {
      for (var i2 = 0; i2 < this._displayables.length; i2++) {
        var displayable = this._displayables[i2];
        if (displayable.contain(x, y)) {
          return true;
        }
      }
    }
    return false;
  };
  return IncrementalDisplayable2;
})(Displayable_default);
var IncrementalDisplayable_default = IncrementalDisplayable;

// node_modules/echarts/lib/animation/basicTransition.js
var transitionStore = makeInner();
function getAnimationConfig(animationType, animatableModel, dataIndex, extraOpts, extraDelayParams) {
  var animationPayload;
  if (animatableModel && animatableModel.ecModel) {
    var updatePayload = animatableModel.ecModel.getUpdatePayload();
    animationPayload = updatePayload && updatePayload.animation;
  }
  var animationEnabled = animatableModel && animatableModel.isAnimationEnabled();
  var isUpdate = animationType === "update";
  if (animationEnabled) {
    var duration = void 0;
    var easing = void 0;
    var delay2 = void 0;
    if (extraOpts) {
      duration = retrieve2(extraOpts.duration, 200);
      easing = retrieve2(extraOpts.easing, "cubicOut");
      delay2 = 0;
    } else {
      duration = animatableModel.getShallow(isUpdate ? "animationDurationUpdate" : "animationDuration");
      easing = animatableModel.getShallow(isUpdate ? "animationEasingUpdate" : "animationEasing");
      delay2 = animatableModel.getShallow(isUpdate ? "animationDelayUpdate" : "animationDelay");
    }
    if (animationPayload) {
      animationPayload.duration != null && (duration = animationPayload.duration);
      animationPayload.easing != null && (easing = animationPayload.easing);
      animationPayload.delay != null && (delay2 = animationPayload.delay);
    }
    if (isFunction(delay2)) {
      delay2 = delay2(dataIndex, extraDelayParams);
    }
    if (isFunction(duration)) {
      duration = duration(dataIndex);
    }
    var config = {
      duration: duration || 0,
      delay: delay2,
      easing
    };
    return config;
  } else {
    return null;
  }
}
function animateOrSetProps(animationType, el, props, animatableModel, dataIndex, cb, during) {
  var isFrom = false;
  var removeOpt;
  if (isFunction(dataIndex)) {
    during = cb;
    cb = dataIndex;
    dataIndex = null;
  } else if (isObject(dataIndex)) {
    cb = dataIndex.cb;
    during = dataIndex.during;
    isFrom = dataIndex.isFrom;
    removeOpt = dataIndex.removeOpt;
    dataIndex = dataIndex.dataIndex;
  }
  var isRemove = animationType === "leave";
  if (!isRemove) {
    el.stopAnimation("leave");
  }
  var animationConfig = getAnimationConfig(animationType, animatableModel, dataIndex, isRemove ? removeOpt || {} : null, animatableModel && animatableModel.getAnimationDelayParams ? animatableModel.getAnimationDelayParams(el, dataIndex) : null);
  if (animationConfig && animationConfig.duration > 0) {
    var duration = animationConfig.duration;
    var animationDelay = animationConfig.delay;
    var animationEasing = animationConfig.easing;
    var animateConfig = {
      duration,
      delay: animationDelay || 0,
      easing: animationEasing,
      done: cb,
      force: !!cb || !!during,
      // Set to final state in update/init animation.
      // So the post processing based on the path shape can be done correctly.
      setToFinal: !isRemove,
      scope: animationType,
      during
    };
    isFrom ? el.animateFrom(props, animateConfig) : el.animateTo(props, animateConfig);
  } else {
    el.stopAnimation();
    !isFrom && el.attr(props);
    during && during(1);
    cb && cb();
  }
}
function updateProps(el, props, animatableModel, dataIndex, cb, during) {
  animateOrSetProps("update", el, props, animatableModel, dataIndex, cb, during);
}
function initProps(el, props, animatableModel, dataIndex, cb, during) {
  animateOrSetProps("enter", el, props, animatableModel, dataIndex, cb, during);
}
function isElementRemoved(el) {
  if (!el.__zr) {
    return true;
  }
  for (var i2 = 0; i2 < el.animators.length; i2++) {
    var animator = el.animators[i2];
    if (animator.scope === "leave") {
      return true;
    }
  }
  return false;
}
function removeElement(el, props, animatableModel, dataIndex, cb, during) {
  if (isElementRemoved(el)) {
    return;
  }
  animateOrSetProps("leave", el, props, animatableModel, dataIndex, cb, during);
}
function fadeOutDisplayable(el, animatableModel, dataIndex, done) {
  el.removeTextContent();
  el.removeTextGuideLine();
  removeElement(el, {
    style: {
      opacity: 0
    }
  }, animatableModel, dataIndex, done);
}
function removeElementWithFadeOut(el, animatableModel, dataIndex) {
  function doRemove() {
    el.parent && el.parent.remove(el);
  }
  if (!el.isGroup) {
    fadeOutDisplayable(el, animatableModel, dataIndex, doRemove);
  } else {
    el.traverse(function(disp) {
      if (!disp.isGroup) {
        fadeOutDisplayable(disp, animatableModel, dataIndex, doRemove);
      }
    });
  }
}
function saveOldStyle(el) {
  transitionStore(el).oldStyle = el.style;
}

// node_modules/echarts/lib/util/graphic.js
var _customShapeMap = {};
var XY2 = ["x", "y"];
var WH2 = ["width", "height"];
var HOVER_LAYER_NO = 0;
var HOVER_LAYER_FROM_THRESHOLD = 1;
var HOVER_LAYER_FOR_INCREMENTAL = 2;
function extendShape(opts) {
  return Path_default.extend(opts);
}
var extendPathFromString = extendFromString;
function extendPath(pathData, opts) {
  return extendPathFromString(pathData, opts);
}
function registerShape(name, ShapeClass) {
  _customShapeMap[name] = ShapeClass;
}
function getShapeClass(name) {
  if (_customShapeMap.hasOwnProperty(name)) {
    return _customShapeMap[name];
  }
}
function makePath(pathData, opts, rect, layout3) {
  var path = createFromString(pathData, opts);
  if (rect) {
    if (layout3 === "center") {
      rect = centerGraphic(rect, path.getBoundingRect());
    }
    resizePath(path, rect);
  }
  return path;
}
function makeImage(imageUrl, rect, layout3) {
  var zrImg = new Image_default({
    style: {
      image: imageUrl,
      x: rect.x,
      y: rect.y,
      width: rect.width,
      height: rect.height
    },
    onload: function(img) {
      if (layout3 === "center") {
        var boundingRect = {
          width: img.width,
          height: img.height
        };
        zrImg.setStyle(centerGraphic(rect, boundingRect));
      }
    }
  });
  return zrImg;
}
function centerGraphic(rect, boundingRect) {
  var aspect = boundingRect.width / boundingRect.height;
  var width = rect.height * aspect;
  var height;
  if (width <= rect.width) {
    height = rect.height;
  } else {
    width = rect.width;
    height = width / aspect;
  }
  var cx = rect.x + rect.width / 2;
  var cy = rect.y + rect.height / 2;
  return {
    x: cx - width / 2,
    y: cy - height / 2,
    width,
    height
  };
}
var mergePath2 = mergePath;
function resizePath(path, rect) {
  if (!path.applyTransform) {
    return;
  }
  var pathRect = path.getBoundingRect();
  var m2 = pathRect.calculateTransform(rect);
  path.applyTransform(m2);
}
function subPixelOptimizeLine2(shape, lineWidth) {
  subPixelOptimizeLine(shape, shape, {
    lineWidth
  });
  return shape;
}
function subPixelOptimizeRect2(shape, style2) {
  subPixelOptimizeRect(shape, shape, style2);
  return shape;
}
var subPixelOptimize2 = subPixelOptimize;
function getTransform2(target, ancestor) {
  var mat = identity([]);
  while (target && target !== ancestor) {
    mul(mat, target.getLocalTransform(), mat);
    target = target.parent;
  }
  return mat;
}
function applyTransform2(target, transform, invert2) {
  if (transform && !isArrayLike(transform)) {
    transform = Transformable_default.getLocalTransform(transform);
  }
  if (invert2) {
    transform = invert([], transform);
  }
  return applyTransform([], target, transform);
}
function transformDirection(direction, transform, invert2) {
  var hBase = transform[4] === 0 || transform[5] === 0 || transform[0] === 0 ? 1 : mathAbs3(2 * transform[4] / transform[0]);
  var vBase = transform[4] === 0 || transform[5] === 0 || transform[2] === 0 ? 1 : mathAbs3(2 * transform[4] / transform[2]);
  var vertex = [direction === "left" ? -hBase : direction === "right" ? hBase : 0, direction === "top" ? -vBase : direction === "bottom" ? vBase : 0];
  vertex = applyTransform2(vertex, transform, invert2);
  return mathAbs3(vertex[0]) > mathAbs3(vertex[1]) ? vertex[0] > 0 ? "right" : "left" : vertex[1] > 0 ? "bottom" : "top";
}
function isNotGroup(el) {
  return !el.isGroup;
}
function isPath(el) {
  return el.shape != null;
}
function groupTransition(g1, g2, animatableModel) {
  if (!g1 || !g2) {
    return;
  }
  function getElMap(g) {
    var elMap = {};
    g.traverse(function(el) {
      if (isNotGroup(el) && el.anid) {
        elMap[el.anid] = el;
      }
    });
    return elMap;
  }
  function getAnimatableProps(el) {
    var obj = {
      x: el.x,
      y: el.y,
      rotation: el.rotation
    };
    if (isPath(el)) {
      obj.shape = clone(el.shape);
    }
    return obj;
  }
  var elMap1 = getElMap(g1);
  g2.traverse(function(el) {
    if (isNotGroup(el) && el.anid) {
      var oldEl = elMap1[el.anid];
      if (oldEl) {
        var newProp = getAnimatableProps(el);
        el.attr(getAnimatableProps(oldEl));
        updateProps(el, newProp, animatableModel, getECData(el).dataIndex);
      }
    }
  });
}
function clipPointsByRect(points4, rect) {
  return map2(points4, function(point) {
    var x = point[0];
    x = mathMax4(x, rect.x);
    x = mathMin4(x, rect.x + rect.width);
    var y = point[1];
    y = mathMax4(y, rect.y);
    y = mathMin4(y, rect.y + rect.height);
    return [x, y];
  });
}
function clipRectByRect(targetRect, rect) {
  var x = mathMax4(targetRect.x, rect.x);
  var x2 = mathMin4(targetRect.x + targetRect.width, rect.x + rect.width);
  var y = mathMax4(targetRect.y, rect.y);
  var y2 = mathMin4(targetRect.y + targetRect.height, rect.y + rect.height);
  if (x2 >= x && y2 >= y) {
    return {
      x,
      y,
      width: x2 - x,
      height: y2 - y
    };
  }
}
function createIcon(iconStr, opt, rect) {
  var innerOpts = extend({
    rectHover: true
  }, opt);
  var style2 = innerOpts.style = {
    strokeNoScale: true
  };
  rect = rect || {
    x: -1,
    y: -1,
    width: 2,
    height: 2
  };
  if (iconStr) {
    return iconStr.indexOf("image://") === 0 ? (style2.image = iconStr.slice(8), defaults(style2, rect), new Image_default(innerOpts)) : makePath(iconStr.replace("path://", ""), innerOpts, rect, "center");
  }
}
function linePolygonIntersect(a1x, a1y, a2x, a2y, points4) {
  for (var i2 = 0, p2 = points4[points4.length - 1]; i2 < points4.length; i2++) {
    var p = points4[i2];
    if (lineLineIntersect(a1x, a1y, a2x, a2y, p[0], p[1], p2[0], p2[1])) {
      return true;
    }
    p2 = p;
  }
}
function lineLineIntersect(a1x, a1y, a2x, a2y, b1x, b1y, b2x, b2y) {
  var mx = a2x - a1x;
  var my = a2y - a1y;
  var nx = b2x - b1x;
  var ny = b2y - b1y;
  var nmCrossProduct = crossProduct2d(nx, ny, mx, my);
  if (nearZero(nmCrossProduct)) {
    return false;
  }
  var b1a1x = a1x - b1x;
  var b1a1y = a1y - b1y;
  var q = crossProduct2d(b1a1x, b1a1y, mx, my) / nmCrossProduct;
  if (q < 0 || q > 1) {
    return false;
  }
  var p = crossProduct2d(b1a1x, b1a1y, nx, ny) / nmCrossProduct;
  if (p < 0 || p > 1) {
    return false;
  }
  return true;
}
function crossProduct2d(x1, y1, x2, y2) {
  return x1 * y2 - x2 * y1;
}
function nearZero(val) {
  return val <= 1e-6 && val >= -1e-6;
}
function expandOrShrinkRect(rect, delta, shrinkOrExpand, noNegative, minSize) {
  if (delta == null) {
    return rect;
  } else if (isNumber(delta)) {
    _tmpExpandRectDelta[0] = _tmpExpandRectDelta[1] = _tmpExpandRectDelta[2] = _tmpExpandRectDelta[3] = delta;
  } else {
    if (true) {
      assert(delta.length === 4);
    }
    _tmpExpandRectDelta[0] = delta[0];
    _tmpExpandRectDelta[1] = delta[1];
    _tmpExpandRectDelta[2] = delta[2];
    _tmpExpandRectDelta[3] = delta[3];
  }
  if (noNegative) {
    _tmpExpandRectDelta[0] = mathMax4(0, _tmpExpandRectDelta[0]);
    _tmpExpandRectDelta[1] = mathMax4(0, _tmpExpandRectDelta[1]);
    _tmpExpandRectDelta[2] = mathMax4(0, _tmpExpandRectDelta[2]);
    _tmpExpandRectDelta[3] = mathMax4(0, _tmpExpandRectDelta[3]);
  }
  if (shrinkOrExpand) {
    _tmpExpandRectDelta[0] = -_tmpExpandRectDelta[0];
    _tmpExpandRectDelta[1] = -_tmpExpandRectDelta[1];
    _tmpExpandRectDelta[2] = -_tmpExpandRectDelta[2];
    _tmpExpandRectDelta[3] = -_tmpExpandRectDelta[3];
  }
  expandRectOnOneDimension(rect, _tmpExpandRectDelta, "x", "width", 3, 1, minSize && minSize[0] || 0);
  expandRectOnOneDimension(rect, _tmpExpandRectDelta, "y", "height", 0, 2, minSize && minSize[1] || 0);
  return rect;
}
var _tmpExpandRectDelta = [0, 0, 0, 0];
function expandRectOnOneDimension(rect, delta, xy, wh, ltIdx, rbIdx, minSize) {
  var deltaSum = delta[rbIdx] + delta[ltIdx];
  var oldSize = rect[wh];
  rect[wh] += deltaSum;
  minSize = mathMax4(0, mathMin4(minSize, oldSize));
  if (rect[wh] < minSize) {
    rect[wh] = minSize;
    rect[xy] += delta[ltIdx] >= 0 ? -delta[ltIdx] : delta[rbIdx] >= 0 ? oldSize + delta[rbIdx] : mathAbs3(deltaSum) > 1e-8 ? (oldSize - minSize) * delta[ltIdx] / deltaSum : 0;
  } else {
    rect[xy] -= delta[ltIdx];
  }
}
function setTooltipConfig(opt) {
  var itemTooltipOption = opt.itemTooltipOption;
  var componentModel = opt.componentModel;
  var itemName = opt.itemName;
  var itemTooltipOptionObj = isString(itemTooltipOption) ? {
    formatter: itemTooltipOption
  } : itemTooltipOption;
  var mainType = componentModel.mainType;
  var componentIndex = componentModel.componentIndex;
  var formatterParams = {
    componentType: mainType,
    name: itemName,
    $vars: ["name"]
  };
  formatterParams[mainType + "Index"] = componentIndex;
  var formatterParamsExtra = opt.formatterParamsExtra;
  if (formatterParamsExtra) {
    each(keys(formatterParamsExtra), function(key2) {
      if (!hasOwn(formatterParams, key2)) {
        formatterParams[key2] = formatterParamsExtra[key2];
        formatterParams.$vars.push(key2);
      }
    });
  }
  var ecData = getECData(opt.el);
  ecData.componentMainType = mainType;
  ecData.componentIndex = componentIndex;
  ecData.tooltipConfig = {
    name: itemName,
    option: defaults({
      content: itemName,
      encodeHTMLContent: true,
      formatterParams
    }, itemTooltipOptionObj)
  };
}
function traverseElement(el, cb) {
  var stopped;
  if (el.isGroup) {
    stopped = cb(el);
  }
  if (!stopped) {
    el.traverse(cb);
  }
}
function traverseElements(els, cb) {
  if (els) {
    if (isArray(els)) {
      for (var i2 = 0; i2 < els.length; i2++) {
        traverseElement(els[i2], cb);
      }
    } else {
      traverseElement(els, cb);
    }
  }
}
function isBoundingRectAxisAligned(transform) {
  return !transform || mathAbs3(transform[1]) < AXIS_ALIGN_EPSILON && mathAbs3(transform[2]) < AXIS_ALIGN_EPSILON || mathAbs3(transform[0]) < AXIS_ALIGN_EPSILON && mathAbs3(transform[3]) < AXIS_ALIGN_EPSILON;
}
var AXIS_ALIGN_EPSILON = 1e-5;
function ensureCopyRect(target, source) {
  return target ? BoundingRect_default.copy(target, source) : source.clone();
}
function ensureCopyTransform(target, source) {
  return source ? copy(target || create(), source) : void 0;
}
function retrieveZInfo(model2) {
  return {
    z: model2.get("z") || 0,
    zlevel: model2.get("zlevel") || 0
  };
}
function calcZ2Range(el) {
  var max3 = -Infinity;
  var min3 = Infinity;
  traverseElement(el, function(el2) {
    visitEl(el2);
    visitEl(el2.getTextContent());
    visitEl(el2.getTextGuideLine());
  });
  function visitEl(el2) {
    if (!el2 || el2.isGroup) {
      return;
    }
    var currentStates = el2.currentStates;
    if (currentStates.length) {
      for (var idx = 0; idx < currentStates.length; idx++) {
        calcZ2(el2.states[currentStates[idx]]);
      }
    }
    calcZ2(el2);
  }
  function calcZ2(entity) {
    if (entity) {
      var z2 = entity.z2;
      if (z2 > max3) {
        max3 = z2;
      }
      if (z2 < min3) {
        min3 = z2;
      }
    }
  }
  if (min3 > max3) {
    min3 = max3 = 0;
  }
  return {
    min: min3,
    max: max3
  };
}
function traverseUpdateZ(el, z, zlevel) {
  doUpdateZ(el, z, zlevel, -Infinity);
}
function doUpdateZ(el, z, zlevel, maxZ2) {
  if (el.ignoreModelZ) {
    return maxZ2;
  }
  var label = el.getTextContent();
  var labelLine = el.getTextGuideLine();
  var isGroup = el.isGroup;
  if (isGroup) {
    var children = el.childrenRef();
    for (var i2 = 0; i2 < children.length; i2++) {
      maxZ2 = mathMax4(doUpdateZ(children[i2], z, zlevel, maxZ2), maxZ2);
    }
  } else {
    el.z = z;
    el.zlevel = zlevel;
    maxZ2 = mathMax4(el.z2 || 0, maxZ2);
  }
  if (label) {
    label.z = z;
    label.zlevel = zlevel;
    isFinite(maxZ2) && (label.z2 = maxZ2 + 2);
  }
  if (labelLine) {
    var textGuideLineConfig = el.textGuideLineConfig;
    labelLine.z = z;
    labelLine.zlevel = zlevel;
    isFinite(maxZ2) && (labelLine.z2 = maxZ2 + (textGuideLineConfig && textGuideLineConfig.showAbove ? 1 : -1));
  }
  return maxZ2;
}
function payloadDisableAnimation(payload) {
  payload.animation = {
    duration: 0
  };
  return payload;
}
function decomposeTransform(out2, mt) {
  mt ? copy(tmpDTR.transform, mt) : identity(tmpDTR.transform);
  tmpDTR.decomposeTransform();
  copyTransform(out2, tmpDTR);
  return out2;
}
var tmpDTR = new Transformable_default();
tmpDTR.transform = create();
function getCurrentCanvasPainter(api) {
  var painter = api.getZr().painter;
  return painter.getType() === "canvas" ? painter : null;
}
registerShape("circle", Circle_default);
registerShape("ellipse", Ellipse_default);
registerShape("sector", Sector_default);
registerShape("ring", Ring_default);
registerShape("polygon", Polygon_default);
registerShape("polyline", Polyline_default);
registerShape("rect", Rect_default);
registerShape("line", Line_default);
registerShape("bezierCurve", BezierCurve_default);
registerShape("arc", Arc_default);

// node_modules/echarts/lib/label/labelStyle.js
var EMPTY_OBJ = {};
function setLabelText(label, labelTexts) {
  for (var i2 = 0; i2 < SPECIAL_STATES.length; i2++) {
    var stateName = SPECIAL_STATES[i2];
    var text = labelTexts[stateName];
    var state2 = label.ensureState(stateName);
    state2.style = state2.style || {};
    state2.style.text = text;
  }
  var oldStates = label.currentStates.slice();
  label.clearStates(true);
  label.setStyle({
    text: labelTexts.normal
  });
  label.useStates(oldStates, true);
}
function getLabelText(opt, stateModels, interpolatedValue) {
  var labelFetcher = opt.labelFetcher;
  var labelDataIndex = opt.labelDataIndex;
  var labelDimIndex = opt.labelDimIndex;
  var normalModel = stateModels.normal;
  var baseText;
  if (labelFetcher) {
    baseText = labelFetcher.getFormattedLabel(labelDataIndex, "normal", null, labelDimIndex, normalModel && normalModel.get("formatter"), interpolatedValue != null ? {
      interpolatedValue
    } : null);
  }
  if (baseText == null) {
    baseText = isFunction(opt.defaultText) ? opt.defaultText(labelDataIndex, opt, interpolatedValue) : opt.defaultText;
  }
  var statesText = {
    normal: baseText
  };
  for (var i2 = 0; i2 < SPECIAL_STATES.length; i2++) {
    var stateName = SPECIAL_STATES[i2];
    var stateModel = stateModels[stateName];
    statesText[stateName] = retrieve2(labelFetcher ? labelFetcher.getFormattedLabel(labelDataIndex, stateName, null, labelDimIndex, stateModel && stateModel.get("formatter")) : null, baseText);
  }
  return statesText;
}
function setLabelStyle(targetEl, labelStatesModels, opt, stateSpecified) {
  opt = opt || EMPTY_OBJ;
  var isSetOnText = targetEl instanceof Text_default;
  var needsCreateText = false;
  for (var i2 = 0; i2 < DISPLAY_STATES.length; i2++) {
    var stateModel = labelStatesModels[DISPLAY_STATES[i2]];
    if (stateModel && stateModel.getShallow("show")) {
      needsCreateText = true;
      break;
    }
  }
  var textContent = isSetOnText ? targetEl : targetEl.getTextContent();
  if (needsCreateText) {
    if (!isSetOnText) {
      if (!textContent) {
        textContent = new Text_default();
        targetEl.setTextContent(textContent);
      }
      if (targetEl.stateProxy) {
        textContent.stateProxy = targetEl.stateProxy;
      }
    }
    var labelStatesTexts = getLabelText(opt, labelStatesModels);
    var normalModel = labelStatesModels.normal;
    var showNormal = !!normalModel.getShallow("show");
    var normalStyle = createTextStyle(normalModel, stateSpecified && stateSpecified.normal, opt, false, !isSetOnText);
    normalStyle.text = labelStatesTexts.normal;
    if (!isSetOnText) {
      targetEl.setTextConfig(createTextConfig(normalModel, opt, false));
    }
    for (var i2 = 0; i2 < SPECIAL_STATES.length; i2++) {
      var stateName = SPECIAL_STATES[i2];
      var stateModel = labelStatesModels[stateName];
      if (stateModel) {
        var stateObj = textContent.ensureState(stateName);
        var stateShow = !!retrieve2(stateModel.getShallow("show"), showNormal);
        if (stateShow !== showNormal) {
          stateObj.ignore = !stateShow;
        }
        stateObj.style = createTextStyle(stateModel, stateSpecified && stateSpecified[stateName], opt, true, !isSetOnText);
        stateObj.style.text = labelStatesTexts[stateName];
        if (!isSetOnText) {
          var targetElEmphasisState = targetEl.ensureState(stateName);
          targetElEmphasisState.textConfig = createTextConfig(stateModel, opt, true);
        }
      }
    }
    textContent.silent = !!normalModel.getShallow("silent");
    if (textContent.style.x != null) {
      normalStyle.x = textContent.style.x;
    }
    if (textContent.style.y != null) {
      normalStyle.y = textContent.style.y;
    }
    textContent.ignore = !showNormal;
    textContent.useStyle(normalStyle);
    textContent.dirty();
    if (opt.enableTextSetter) {
      labelInner(textContent).setLabelText = function(interpolatedValue) {
        var labelStatesTexts2 = getLabelText(opt, labelStatesModels, interpolatedValue);
        setLabelText(textContent, labelStatesTexts2);
      };
    }
  } else if (textContent) {
    textContent.ignore = true;
  }
  targetEl.dirty();
}
function getLabelStatesModels(itemModel, labelName) {
  labelName = labelName || "label";
  var statesModels = {
    normal: itemModel.getModel(labelName)
  };
  for (var i2 = 0; i2 < SPECIAL_STATES.length; i2++) {
    var stateName = SPECIAL_STATES[i2];
    statesModels[stateName] = itemModel.getModel([stateName, labelName]);
  }
  return statesModels;
}
function createTextStyle(textStyleModel, specifiedTextStyle, opt, isNotNormal, isAttached) {
  var textStyle = {};
  setTextStyleCommon(textStyle, textStyleModel, opt, isNotNormal, isAttached);
  specifiedTextStyle && extend(textStyle, specifiedTextStyle);
  return textStyle;
}
function createTextConfig(textStyleModel, opt, isNotNormal) {
  opt = opt || {};
  var textConfig = {};
  var labelPosition;
  var labelRotate = textStyleModel.getShallow("rotate");
  var labelDistance = retrieve2(textStyleModel.getShallow("distance"), isNotNormal ? null : 5);
  var labelOffset = textStyleModel.getShallow("offset");
  labelPosition = textStyleModel.getShallow("position") || (isNotNormal ? null : "inside");
  labelPosition === "outside" && (labelPosition = opt.defaultOutsidePosition || "top");
  if (labelPosition != null) {
    textConfig.position = labelPosition;
  }
  if (labelOffset != null) {
    textConfig.offset = labelOffset;
  }
  if (labelRotate != null) {
    labelRotate *= Math.PI / 180;
    textConfig.rotation = labelRotate;
  }
  if (labelDistance != null) {
    textConfig.distance = labelDistance;
  }
  textConfig.outsideFill = textStyleModel.get("color") === "inherit" ? opt.inheritColor || null : "auto";
  if (opt.autoOverflowArea != null) {
    textConfig.autoOverflowArea = opt.autoOverflowArea;
  }
  if (opt.layoutRect != null) {
    textConfig.layoutRect = opt.layoutRect;
  }
  return textConfig;
}
function setTextStyleCommon(textStyle, textStyleModel, opt, isNotNormal, isAttached) {
  opt = opt || EMPTY_OBJ;
  var ecModel = textStyleModel.ecModel;
  var globalTextStyle = ecModel && ecModel.option.textStyle;
  var richItemNames = getRichItemNames(textStyleModel);
  var richResult;
  if (richItemNames) {
    richResult = {};
    var richInheritPlainLabelOptionName = "richInheritPlainLabel";
    var richInheritPlainLabel = retrieve2(textStyleModel.get(richInheritPlainLabelOptionName), ecModel ? ecModel.get(richInheritPlainLabelOptionName) : void 0);
    for (var name_1 in richItemNames) {
      if (richItemNames.hasOwnProperty(name_1)) {
        var richTextStyle = textStyleModel.getModel(["rich", name_1]);
        setTokenTextStyle(richResult[name_1] = {}, richTextStyle, globalTextStyle, textStyleModel, richInheritPlainLabel, opt, isNotNormal, isAttached, false, true);
      }
    }
  }
  if (richResult) {
    textStyle.rich = richResult;
  }
  var overflow = textStyleModel.get("overflow");
  if (overflow) {
    textStyle.overflow = overflow;
  }
  var lineOverflow = textStyleModel.get("lineOverflow");
  if (lineOverflow) {
    textStyle.lineOverflow = lineOverflow;
  }
  var labelTextStyle = textStyle;
  var minMargin = textStyleModel.get("minMargin");
  if (minMargin != null) {
    minMargin = !isNumber(minMargin) ? 0 : minMargin / 2;
    labelTextStyle.margin = [minMargin, minMargin, minMargin, minMargin];
    labelTextStyle.__marginType = LabelMarginType.minMargin;
  } else {
    var textMargin = textStyleModel.get("textMargin");
    if (textMargin != null) {
      labelTextStyle.margin = normalizeCssArray(textMargin);
      labelTextStyle.__marginType = LabelMarginType.textMargin;
    }
  }
  setTokenTextStyle(textStyle, textStyleModel, globalTextStyle, null, null, opt, isNotNormal, isAttached, true, false);
}
function getRichItemNames(textStyleModel) {
  var richItemNameMap;
  while (textStyleModel && textStyleModel !== textStyleModel.ecModel) {
    var rich = (textStyleModel.option || EMPTY_OBJ).rich;
    if (rich) {
      richItemNameMap = richItemNameMap || {};
      var richKeys = keys(rich);
      for (var i2 = 0; i2 < richKeys.length; i2++) {
        var richKey = richKeys[i2];
        richItemNameMap[richKey] = 1;
      }
    }
    textStyleModel = textStyleModel.parentModel;
  }
  return richItemNameMap;
}
var TEXT_PROPS_WITH_GLOBAL = ["fontStyle", "fontWeight", "fontSize", "fontFamily", "textShadowColor", "textShadowBlur", "textShadowOffsetX", "textShadowOffsetY"];
var TEXT_PROPS_SELF = ["align", "lineHeight", "width", "height", "tag", "verticalAlign", "ellipsis"];
var TEXT_PROPS_BOX = ["padding", "borderWidth", "borderRadius", "borderDashOffset", "backgroundColor", "borderColor", "shadowColor", "shadowBlur", "shadowOffsetX", "shadowOffsetY"];
function setTokenTextStyle(textStyle, textStyleModel, globalTextStyle, plainTextModel, richInheritPlainLabel, opt, isNotNormal, isAttached, isBlock, inRich) {
  globalTextStyle = !isNotNormal && globalTextStyle || EMPTY_OBJ;
  var inheritColor = opt && opt.inheritColor;
  var fillColor = textStyleModel.getShallow("color");
  var strokeColor = textStyleModel.getShallow("textBorderColor");
  var opacity = retrieve2(textStyleModel.getShallow("opacity"), globalTextStyle.opacity);
  if (fillColor === "inherit" || fillColor === "auto") {
    if (true) {
      if (fillColor === "auto") {
        deprecateReplaceLog("color: 'auto'", "color: 'inherit'");
      }
    }
    if (inheritColor) {
      fillColor = inheritColor;
    } else {
      fillColor = null;
    }
  }
  if (strokeColor === "inherit" || strokeColor === "auto") {
    if (true) {
      if (strokeColor === "auto") {
        deprecateReplaceLog("color: 'auto'", "color: 'inherit'");
      }
    }
    if (inheritColor) {
      strokeColor = inheritColor;
    } else {
      strokeColor = null;
    }
  }
  if (!isAttached) {
    fillColor = fillColor || globalTextStyle.color;
    strokeColor = strokeColor || globalTextStyle.textBorderColor;
  }
  if (fillColor != null) {
    textStyle.fill = fillColor;
  }
  if (strokeColor != null) {
    textStyle.stroke = strokeColor;
  }
  var textBorderWidth = retrieve2(textStyleModel.getShallow("textBorderWidth"), globalTextStyle.textBorderWidth);
  if (textBorderWidth != null) {
    textStyle.lineWidth = textBorderWidth;
  }
  var textBorderType = retrieve2(textStyleModel.getShallow("textBorderType"), globalTextStyle.textBorderType);
  if (textBorderType != null) {
    textStyle.lineDash = textBorderType;
  }
  var textBorderDashOffset = retrieve2(textStyleModel.getShallow("textBorderDashOffset"), globalTextStyle.textBorderDashOffset);
  if (textBorderDashOffset != null) {
    textStyle.lineDashOffset = textBorderDashOffset;
  }
  if (!isNotNormal && opacity == null && !inRich) {
    opacity = opt && opt.defaultOpacity;
  }
  if (opacity != null) {
    textStyle.opacity = opacity;
  }
  if (!isNotNormal && !isAttached) {
    if (textStyle.fill == null && opt.inheritColor) {
      textStyle.fill = opt.inheritColor;
    }
  }
  for (var i2 = 0; i2 < TEXT_PROPS_WITH_GLOBAL.length; i2++) {
    var key2 = TEXT_PROPS_WITH_GLOBAL[i2];
    var val = richInheritPlainLabel !== false && plainTextModel ? retrieve3(textStyleModel.getShallow(key2), plainTextModel.getShallow(key2), globalTextStyle[key2]) : retrieve2(textStyleModel.getShallow(key2), globalTextStyle[key2]);
    if (val != null) {
      textStyle[key2] = val;
    }
  }
  for (var i2 = 0; i2 < TEXT_PROPS_SELF.length; i2++) {
    var key2 = TEXT_PROPS_SELF[i2];
    var val = textStyleModel.getShallow(key2);
    if (val != null) {
      textStyle[key2] = val;
    }
  }
  if (textStyle.verticalAlign == null) {
    var baseline = textStyleModel.getShallow("baseline");
    if (baseline != null) {
      textStyle.verticalAlign = baseline;
    }
  }
  if (!isBlock || !opt.disableBox) {
    for (var i2 = 0; i2 < TEXT_PROPS_BOX.length; i2++) {
      var key2 = TEXT_PROPS_BOX[i2];
      var val = textStyleModel.getShallow(key2);
      if (val != null) {
        textStyle[key2] = val;
      }
    }
    var borderType = textStyleModel.getShallow("borderType");
    if (borderType != null) {
      textStyle.borderDash = borderType;
    }
    if ((textStyle.backgroundColor === "auto" || textStyle.backgroundColor === "inherit") && inheritColor) {
      if (true) {
        if (textStyle.backgroundColor === "auto") {
          deprecateReplaceLog("backgroundColor: 'auto'", "backgroundColor: 'inherit'");
        }
      }
      textStyle.backgroundColor = inheritColor;
    }
    if ((textStyle.borderColor === "auto" || textStyle.borderColor === "inherit") && inheritColor) {
      if (true) {
        if (textStyle.borderColor === "auto") {
          deprecateReplaceLog("borderColor: 'auto'", "borderColor: 'inherit'");
        }
      }
      textStyle.borderColor = inheritColor;
    }
  }
}
function getFont(opt, ecModel) {
  var gTextStyleModel = ecModel && ecModel.getModel("textStyle");
  return trim([
    // FIXME in node-canvas fontWeight is before fontStyle
    opt.fontStyle || gTextStyleModel && gTextStyleModel.getShallow("fontStyle") || "",
    opt.fontWeight || gTextStyleModel && gTextStyleModel.getShallow("fontWeight") || "",
    (opt.fontSize || gTextStyleModel && gTextStyleModel.getShallow("fontSize") || 12) + "px",
    opt.fontFamily || gTextStyleModel && gTextStyleModel.getShallow("fontFamily") || "sans-serif"
  ].join(" "));
}
var labelInner = makeInner();
function setLabelValueAnimation(label, labelStatesModels, value, getDefaultText) {
  if (!label) {
    return;
  }
  var obj = labelInner(label);
  obj.prevValue = obj.value;
  obj.value = value;
  var normalLabelModel = labelStatesModels.normal;
  obj.valueAnimation = normalLabelModel.get("valueAnimation");
  if (obj.valueAnimation) {
    obj.precision = normalLabelModel.get("precision");
    obj.defaultInterpolatedText = getDefaultText;
    obj.statesModels = labelStatesModels;
  }
}
function animateLabelValue(textEl, dataIndex, data, animatableModel, labelFetcher) {
  var labelInnerStore = labelInner(textEl);
  if (!labelInnerStore.valueAnimation || labelInnerStore.prevValue === labelInnerStore.value) {
    return;
  }
  var defaultInterpolatedText = labelInnerStore.defaultInterpolatedText;
  var currValue = retrieve2(labelInnerStore.interpolatedValue, labelInnerStore.prevValue);
  var targetValue = labelInnerStore.value;
  function during(percent) {
    var interpolated = interpolateRawValues(data, labelInnerStore.precision, currValue, targetValue, percent);
    labelInnerStore.interpolatedValue = percent === 1 ? null : interpolated;
    var labelText = getLabelText({
      labelDataIndex: dataIndex,
      labelFetcher,
      defaultText: defaultInterpolatedText ? defaultInterpolatedText(interpolated) : interpolated + ""
    }, labelInnerStore.statesModels, interpolated);
    setLabelText(textEl, labelText);
  }
  textEl.percent = 0;
  (labelInnerStore.prevValue == null ? initProps : updateProps)(textEl, {
    // percent is used to prevent animation from being aborted #15916
    percent: 1
  }, animatableModel, dataIndex, null, during);
}
var LabelMarginType = {
  minMargin: 1,
  textMargin: 2
};

// node_modules/echarts/lib/model/mixin/textStyle.js
var PATH_COLOR = ["textStyle", "color"];
var textStyleParams = ["fontStyle", "fontWeight", "fontSize", "fontFamily", "padding", "lineHeight", "rich", "width", "height", "overflow"];
var tmpText = new Text_default();
var TextStyleMixin = (
  /** @class */
  (function() {
    function TextStyleMixin2() {
    }
    TextStyleMixin2.prototype.getTextColor = function(isEmphasis) {
      var ecModel = this.ecModel;
      return this.getShallow("color") || (!isEmphasis && ecModel ? ecModel.get(PATH_COLOR) : null);
    };
    TextStyleMixin2.prototype.getFont = function() {
      return getFont({
        fontStyle: this.getShallow("fontStyle"),
        fontWeight: this.getShallow("fontWeight"),
        fontSize: this.getShallow("fontSize"),
        fontFamily: this.getShallow("fontFamily")
      }, this.ecModel);
    };
    TextStyleMixin2.prototype.getTextRect = function(text) {
      var style2 = {
        text,
        verticalAlign: this.getShallow("verticalAlign") || this.getShallow("baseline")
      };
      for (var i2 = 0; i2 < textStyleParams.length; i2++) {
        style2[textStyleParams[i2]] = this.getShallow(textStyleParams[i2]);
      }
      tmpText.useStyle(style2);
      tmpText.update();
      return tmpText.getBoundingRect();
    };
    return TextStyleMixin2;
  })()
);
var textStyle_default = TextStyleMixin;

// node_modules/echarts/lib/model/mixin/lineStyle.js
var LINE_STYLE_KEY_MAP = [
  ["lineWidth", "width"],
  ["stroke", "color"],
  ["opacity"],
  ["shadowBlur"],
  ["shadowOffsetX"],
  ["shadowOffsetY"],
  ["shadowColor"],
  ["lineDash", "type"],
  ["lineDashOffset", "dashOffset"],
  ["lineCap", "cap"],
  ["lineJoin", "join"],
  ["miterLimit"]
  // Option decal is in `DecalObject` but style.decal is in `PatternObject`.
  // So do not transfer decal directly.
];
var getLineStyle = makeStyleMapper(LINE_STYLE_KEY_MAP);
var LineStyleMixin = (
  /** @class */
  (function() {
    function LineStyleMixin2() {
    }
    LineStyleMixin2.prototype.getLineStyle = function(excludes) {
      return getLineStyle(this, excludes);
    };
    return LineStyleMixin2;
  })()
);

// node_modules/echarts/lib/model/mixin/itemStyle.js
var ITEM_STYLE_KEY_MAP = [
  ["fill", "color"],
  ["stroke", "borderColor"],
  ["lineWidth", "borderWidth"],
  ["opacity"],
  ["shadowBlur"],
  ["shadowOffsetX"],
  ["shadowOffsetY"],
  ["shadowColor"],
  ["lineDash", "borderType"],
  ["lineDashOffset", "borderDashOffset"],
  ["lineCap", "borderCap"],
  ["lineJoin", "borderJoin"],
  ["miterLimit", "borderMiterLimit"]
  // Option decal is in `DecalObject` but style.decal is in `PatternObject`.
  // So do not transfer decal directly.
];
var getItemStyle = makeStyleMapper(ITEM_STYLE_KEY_MAP);
var ItemStyleMixin = (
  /** @class */
  (function() {
    function ItemStyleMixin2() {
    }
    ItemStyleMixin2.prototype.getItemStyle = function(excludes, includes) {
      return getItemStyle(this, excludes, includes);
    };
    return ItemStyleMixin2;
  })()
);

// node_modules/echarts/lib/model/Model.js
var Model = (
  /** @class */
  (function() {
    function Model2(option, parentModel, ecModel) {
      this.parentModel = parentModel;
      this.ecModel = ecModel;
      this.option = option;
    }
    Model2.prototype.init = function(option, parentModel, ecModel) {
      var rest = [];
      for (var _i = 3; _i < arguments.length; _i++) {
        rest[_i - 3] = arguments[_i];
      }
    };
    Model2.prototype.mergeOption = function(option, ecModel) {
      merge2(this.option, option, true);
    };
    Model2.prototype.get = function(path, ignoreParent) {
      if (path == null) {
        return this.option;
      }
      return this._doGet(this.parsePath(path), !ignoreParent && this.parentModel);
    };
    Model2.prototype.getShallow = function(key2, ignoreParent) {
      var option = this.option;
      var val = option == null ? option : option[key2];
      if (val == null && !ignoreParent) {
        var parentModel = this.parentModel;
        if (parentModel) {
          val = parentModel.getShallow(key2);
        }
      }
      return val;
    };
    Model2.prototype.getModel = function(path, parentModel) {
      var hasPath = path != null;
      var pathFinal = hasPath ? this.parsePath(path) : null;
      var obj = hasPath ? this._doGet(pathFinal) : this.option;
      parentModel = parentModel || this.parentModel && this.parentModel.getModel(this.resolveParentPath(pathFinal));
      return new Model2(obj, parentModel, this.ecModel);
    };
    Model2.prototype.isEmpty = function() {
      return this.option == null;
    };
    Model2.prototype.restoreData = function() {
    };
    Model2.prototype.clone = function() {
      var Ctor = this.constructor;
      return new Ctor(clone(this.option));
    };
    Model2.prototype.parsePath = function(path) {
      if (typeof path === "string") {
        return path.split(".");
      }
      return path;
    };
    Model2.prototype.resolveParentPath = function(path) {
      return path;
    };
    Model2.prototype.isAnimationEnabled = function() {
      if (!env_default.node && this.option) {
        if (this.option.animation != null) {
          return !!this.option.animation;
        } else if (this.parentModel) {
          return this.parentModel.isAnimationEnabled();
        }
      }
    };
    Model2.prototype._doGet = function(pathArr, parentModel) {
      var obj = this.option;
      if (!pathArr) {
        return obj;
      }
      for (var i2 = 0; i2 < pathArr.length; i2++) {
        if (!pathArr[i2]) {
          continue;
        }
        obj = obj && typeof obj === "object" ? obj[pathArr[i2]] : null;
        if (obj == null) {
          break;
        }
      }
      if (obj == null && parentModel) {
        obj = parentModel._doGet(this.resolveParentPath(pathArr), parentModel.parentModel);
      }
      return obj;
    };
    return Model2;
  })()
);
enableClassExtend(Model);
enableClassCheck(Model);
mixin(Model, LineStyleMixin);
mixin(Model, ItemStyleMixin);
mixin(Model, AreaStyleMixin);
mixin(Model, textStyle_default);
var Model_default = Model;

// node_modules/echarts/lib/data/DataDiffer.js
function dataIndexMapValueLength(valNumOrArrLengthMoreThan2) {
  return valNumOrArrLengthMoreThan2 == null ? 0 : valNumOrArrLengthMoreThan2.length || 1;
}
function defaultKeyGetter(item) {
  return item;
}
var DataDiffer = (
  /** @class */
  (function() {
    function DataDiffer2(oldArr, newArr, oldKeyGetter, newKeyGetter, context, diffMode) {
      this._old = oldArr;
      this._new = newArr;
      this._oldKeyGetter = oldKeyGetter || defaultKeyGetter;
      this._newKeyGetter = newKeyGetter || defaultKeyGetter;
      this.context = context;
      this._diffModeMultiple = diffMode === "multiple";
    }
    DataDiffer2.prototype.add = function(func) {
      this._add = func;
      return this;
    };
    DataDiffer2.prototype.update = function(func) {
      this._update = func;
      return this;
    };
    DataDiffer2.prototype.updateManyToOne = function(func) {
      this._updateManyToOne = func;
      return this;
    };
    DataDiffer2.prototype.updateOneToMany = function(func) {
      this._updateOneToMany = func;
      return this;
    };
    DataDiffer2.prototype.updateManyToMany = function(func) {
      this._updateManyToMany = func;
      return this;
    };
    DataDiffer2.prototype.remove = function(func) {
      this._remove = func;
      return this;
    };
    DataDiffer2.prototype.execute = function() {
      this[this._diffModeMultiple ? "_executeMultiple" : "_executeOneToOne"]();
    };
    DataDiffer2.prototype._executeOneToOne = function() {
      var oldArr = this._old;
      var newArr = this._new;
      var newDataIndexMap = {};
      var oldDataKeyArr = new Array(oldArr.length);
      var newDataKeyArr = new Array(newArr.length);
      this._initIndexMap(oldArr, null, oldDataKeyArr, "_oldKeyGetter");
      this._initIndexMap(newArr, newDataIndexMap, newDataKeyArr, "_newKeyGetter");
      for (var i2 = 0; i2 < oldArr.length; i2++) {
        var oldKey = oldDataKeyArr[i2];
        var newIdxMapVal = newDataIndexMap[oldKey];
        var newIdxMapValLen = dataIndexMapValueLength(newIdxMapVal);
        if (newIdxMapValLen > 1) {
          var newIdx = newIdxMapVal.shift();
          if (newIdxMapVal.length === 1) {
            newDataIndexMap[oldKey] = newIdxMapVal[0];
          }
          this._update && this._update(newIdx, i2);
        } else if (newIdxMapValLen === 1) {
          newDataIndexMap[oldKey] = null;
          this._update && this._update(newIdxMapVal, i2);
        } else {
          this._remove && this._remove(i2);
        }
      }
      this._performRestAdd(newDataKeyArr, newDataIndexMap);
    };
    DataDiffer2.prototype._executeMultiple = function() {
      var oldArr = this._old;
      var newArr = this._new;
      var oldDataIndexMap = {};
      var newDataIndexMap = {};
      var oldDataKeyArr = [];
      var newDataKeyArr = [];
      this._initIndexMap(oldArr, oldDataIndexMap, oldDataKeyArr, "_oldKeyGetter");
      this._initIndexMap(newArr, newDataIndexMap, newDataKeyArr, "_newKeyGetter");
      for (var i2 = 0; i2 < oldDataKeyArr.length; i2++) {
        var oldKey = oldDataKeyArr[i2];
        var oldIdxMapVal = oldDataIndexMap[oldKey];
        var newIdxMapVal = newDataIndexMap[oldKey];
        var oldIdxMapValLen = dataIndexMapValueLength(oldIdxMapVal);
        var newIdxMapValLen = dataIndexMapValueLength(newIdxMapVal);
        if (oldIdxMapValLen > 1 && newIdxMapValLen === 1) {
          this._updateManyToOne && this._updateManyToOne(newIdxMapVal, oldIdxMapVal);
          newDataIndexMap[oldKey] = null;
        } else if (oldIdxMapValLen === 1 && newIdxMapValLen > 1) {
          this._updateOneToMany && this._updateOneToMany(newIdxMapVal, oldIdxMapVal);
          newDataIndexMap[oldKey] = null;
        } else if (oldIdxMapValLen === 1 && newIdxMapValLen === 1) {
          this._update && this._update(newIdxMapVal, oldIdxMapVal);
          newDataIndexMap[oldKey] = null;
        } else if (oldIdxMapValLen > 1 && newIdxMapValLen > 1) {
          this._updateManyToMany && this._updateManyToMany(newIdxMapVal, oldIdxMapVal);
          newDataIndexMap[oldKey] = null;
        } else if (oldIdxMapValLen > 1) {
          for (var i_1 = 0; i_1 < oldIdxMapValLen; i_1++) {
            this._remove && this._remove(oldIdxMapVal[i_1]);
          }
        } else {
          this._remove && this._remove(oldIdxMapVal);
        }
      }
      this._performRestAdd(newDataKeyArr, newDataIndexMap);
    };
    DataDiffer2.prototype._performRestAdd = function(newDataKeyArr, newDataIndexMap) {
      for (var i2 = 0; i2 < newDataKeyArr.length; i2++) {
        var newKey = newDataKeyArr[i2];
        var newIdxMapVal = newDataIndexMap[newKey];
        var idxMapValLen = dataIndexMapValueLength(newIdxMapVal);
        if (idxMapValLen > 1) {
          for (var j = 0; j < idxMapValLen; j++) {
            this._add && this._add(newIdxMapVal[j]);
          }
        } else if (idxMapValLen === 1) {
          this._add && this._add(newIdxMapVal);
        }
        newDataIndexMap[newKey] = null;
      }
    };
    DataDiffer2.prototype._initIndexMap = function(arr, map4, keyArr, keyGetterName) {
      var cbModeMultiple = this._diffModeMultiple;
      for (var i2 = 0; i2 < arr.length; i2++) {
        var key2 = "_ec_" + this[keyGetterName](arr[i2], i2);
        if (!cbModeMultiple) {
          keyArr[i2] = key2;
        }
        if (!map4) {
          continue;
        }
        var idxMapVal = map4[key2];
        var idxMapValLen = dataIndexMapValueLength(idxMapVal);
        if (idxMapValLen === 0) {
          map4[key2] = i2;
          if (cbModeMultiple) {
            keyArr.push(key2);
          }
        } else if (idxMapValLen === 1) {
          map4[key2] = [idxMapVal, i2];
        } else {
          idxMapVal.push(i2);
        }
      }
    };
    return DataDiffer2;
  })()
);
var DataDiffer_default = DataDiffer;

// node_modules/echarts/lib/data/helper/sourceHelper.js
var BE_ORDINAL = {
  Must: 1,
  Might: 2,
  Not: 3
  // Other cases
};
var innerGlobalModel = makeInner();
function resetSourceDefaulter(ecModel) {
  innerGlobalModel(ecModel).datasetMap = createHashMap();
}
function makeSeriesEncodeForAxisCoordSys(coordDimensions, seriesModel, source) {
  var encode = {};
  var datasetModel = querySeriesUpstreamDatasetModel(seriesModel);
  if (!datasetModel || !coordDimensions) {
    return encode;
  }
  var encodeItemName = [];
  var encodeSeriesName = [];
  var ecModel = seriesModel.ecModel;
  var datasetMap = innerGlobalModel(ecModel).datasetMap;
  var key2 = datasetModel.uid + "_" + source.seriesLayoutBy;
  var baseCategoryDimIndex;
  var categoryWayValueDimStart;
  coordDimensions = coordDimensions.slice();
  each(coordDimensions, function(coordDimInfoLoose, coordDimIdx) {
    var coordDimInfo = isObject(coordDimInfoLoose) ? coordDimInfoLoose : coordDimensions[coordDimIdx] = {
      name: coordDimInfoLoose
    };
    if (coordDimInfo.type === "ordinal" && baseCategoryDimIndex == null) {
      baseCategoryDimIndex = coordDimIdx;
      categoryWayValueDimStart = getDataDimCountOnCoordDim(coordDimInfo);
    }
    encode[coordDimInfo.name] = [];
  });
  var datasetRecord = datasetMap.get(key2) || datasetMap.set(key2, {
    categoryWayDim: categoryWayValueDimStart,
    valueWayDim: 0
  });
  each(coordDimensions, function(coordDimInfo, coordDimIdx) {
    var coordDimName = coordDimInfo.name;
    var count2 = getDataDimCountOnCoordDim(coordDimInfo);
    if (baseCategoryDimIndex == null) {
      var start2 = datasetRecord.valueWayDim;
      pushDim(encode[coordDimName], start2, count2);
      pushDim(encodeSeriesName, start2, count2);
      datasetRecord.valueWayDim += count2;
    } else if (baseCategoryDimIndex === coordDimIdx) {
      pushDim(encode[coordDimName], 0, count2);
      pushDim(encodeItemName, 0, count2);
    } else {
      var start2 = datasetRecord.categoryWayDim;
      pushDim(encode[coordDimName], start2, count2);
      pushDim(encodeSeriesName, start2, count2);
      datasetRecord.categoryWayDim += count2;
    }
  });
  function pushDim(dimIdxArr, idxFrom, idxCount) {
    for (var i2 = 0; i2 < idxCount; i2++) {
      dimIdxArr.push(idxFrom + i2);
    }
  }
  function getDataDimCountOnCoordDim(coordDimInfo) {
    var dimsDef = coordDimInfo.dimsDef;
    return dimsDef ? dimsDef.length : 1;
  }
  encodeItemName.length && (encode.itemName = encodeItemName);
  encodeSeriesName.length && (encode.seriesName = encodeSeriesName);
  return encode;
}
function makeSeriesEncodeForNameBased(seriesModel, source, dimCount) {
  var encode = {};
  var datasetModel = querySeriesUpstreamDatasetModel(seriesModel);
  if (!datasetModel) {
    return encode;
  }
  var sourceFormat = source.sourceFormat;
  var dimensionsDefine = source.dimensionsDefine;
  var potentialNameDimIndex;
  if (sourceFormat === SOURCE_FORMAT_OBJECT_ROWS || sourceFormat === SOURCE_FORMAT_KEYED_COLUMNS) {
    each(dimensionsDefine, function(dim, idx) {
      if ((isObject(dim) ? dim.name : dim) === "name") {
        potentialNameDimIndex = idx;
      }
    });
  }
  var idxResult = (function() {
    var idxRes0 = {};
    var idxRes1 = {};
    var guessRecords = [];
    for (var i2 = 0, len2 = Math.min(5, dimCount); i2 < len2; i2++) {
      var guessResult = doGuessOrdinal(source.data, sourceFormat, source.seriesLayoutBy, dimensionsDefine, source.startIndex, i2);
      guessRecords.push(guessResult);
      var isPureNumber = guessResult === BE_ORDINAL.Not;
      if (isPureNumber && idxRes0.v == null && i2 !== potentialNameDimIndex) {
        idxRes0.v = i2;
      }
      if (idxRes0.n == null || idxRes0.n === idxRes0.v || !isPureNumber && guessRecords[idxRes0.n] === BE_ORDINAL.Not) {
        idxRes0.n = i2;
      }
      if (fulfilled(idxRes0) && guessRecords[idxRes0.n] !== BE_ORDINAL.Not) {
        return idxRes0;
      }
      if (!isPureNumber) {
        if (guessResult === BE_ORDINAL.Might && idxRes1.v == null && i2 !== potentialNameDimIndex) {
          idxRes1.v = i2;
        }
        if (idxRes1.n == null || idxRes1.n === idxRes1.v) {
          idxRes1.n = i2;
        }
      }
    }
    function fulfilled(idxResult2) {
      return idxResult2.v != null && idxResult2.n != null;
    }
    return fulfilled(idxRes0) ? idxRes0 : fulfilled(idxRes1) ? idxRes1 : null;
  })();
  if (idxResult) {
    encode.value = [idxResult.v];
    var nameDimIndex = potentialNameDimIndex != null ? potentialNameDimIndex : idxResult.n;
    encode.itemName = [nameDimIndex];
    encode.seriesName = [nameDimIndex];
  }
  return encode;
}
function querySeriesUpstreamDatasetModel(seriesModel) {
  var thisData = seriesModel.get("data", true);
  if (!thisData) {
    return queryReferringComponents(seriesModel.ecModel, "dataset", {
      index: seriesModel.get("datasetIndex", true),
      id: seriesModel.get("datasetId", true)
    }, SINGLE_REFERRING).models[0];
  }
}
function queryDatasetUpstreamDatasetModels(datasetModel) {
  if (!datasetModel.get("transform", true) && !datasetModel.get("fromTransformResult", true)) {
    return [];
  }
  return queryReferringComponents(datasetModel.ecModel, "dataset", {
    index: datasetModel.get("fromDatasetIndex", true),
    id: datasetModel.get("fromDatasetId", true)
  }, SINGLE_REFERRING).models;
}
function guessOrdinal(source, dimIndex) {
  return doGuessOrdinal(source.data, source.sourceFormat, source.seriesLayoutBy, source.dimensionsDefine, source.startIndex, dimIndex);
}
function doGuessOrdinal(data, sourceFormat, seriesLayoutBy, dimensionsDefine, startIndex, dimIndex) {
  var result;
  var maxLoop = 5;
  if (isTypedArray(data)) {
    return BE_ORDINAL.Not;
  }
  var dimName;
  var dimType;
  if (dimensionsDefine) {
    var dimDefItem = dimensionsDefine[dimIndex];
    if (isObject(dimDefItem)) {
      dimName = dimDefItem.name;
      dimType = dimDefItem.type;
    } else if (isString(dimDefItem)) {
      dimName = dimDefItem;
    }
  }
  if (dimType != null) {
    return dimType === "ordinal" ? BE_ORDINAL.Must : BE_ORDINAL.Not;
  }
  if (sourceFormat === SOURCE_FORMAT_ARRAY_ROWS) {
    var dataArrayRows = data;
    if (seriesLayoutBy === SERIES_LAYOUT_BY_ROW) {
      var sample = dataArrayRows[dimIndex];
      for (var i2 = 0; i2 < (sample || []).length && i2 < maxLoop; i2++) {
        if ((result = detectValue(sample[startIndex + i2])) != null) {
          return result;
        }
      }
    } else {
      for (var i2 = 0; i2 < dataArrayRows.length && i2 < maxLoop; i2++) {
        var row = dataArrayRows[startIndex + i2];
        if (row && (result = detectValue(row[dimIndex])) != null) {
          return result;
        }
      }
    }
  } else if (sourceFormat === SOURCE_FORMAT_OBJECT_ROWS) {
    var dataObjectRows = data;
    if (!dimName) {
      return BE_ORDINAL.Not;
    }
    for (var i2 = 0; i2 < dataObjectRows.length && i2 < maxLoop; i2++) {
      var item = dataObjectRows[i2];
      if (item && (result = detectValue(item[dimName])) != null) {
        return result;
      }
    }
  } else if (sourceFormat === SOURCE_FORMAT_KEYED_COLUMNS) {
    var dataKeyedColumns = data;
    if (!dimName) {
      return BE_ORDINAL.Not;
    }
    var sample = dataKeyedColumns[dimName];
    if (!sample || isTypedArray(sample)) {
      return BE_ORDINAL.Not;
    }
    for (var i2 = 0; i2 < sample.length && i2 < maxLoop; i2++) {
      if ((result = detectValue(sample[i2])) != null) {
        return result;
      }
    }
  } else if (sourceFormat === SOURCE_FORMAT_ORIGINAL) {
    var dataOriginal = data;
    for (var i2 = 0; i2 < dataOriginal.length && i2 < maxLoop; i2++) {
      var item = dataOriginal[i2];
      var val = getDataItemValue(item);
      if (!isArray(val)) {
        return BE_ORDINAL.Not;
      }
      if ((result = detectValue(val[dimIndex])) != null) {
        return result;
      }
    }
  }
  function detectValue(val2) {
    var beStr = isString(val2);
    if (val2 != null && isFinite(Number(val2)) && val2 !== "") {
      return beStr ? BE_ORDINAL.Might : BE_ORDINAL.Not;
    } else if (beStr && val2 !== "-") {
      return BE_ORDINAL.Must;
    }
  }
  return BE_ORDINAL.Not;
}

// node_modules/echarts/lib/data/Source.js
var SourceImpl = (
  /** @class */
  /* @__PURE__ */ (function() {
    function SourceImpl2(fields) {
      this.data = fields.data || (fields.sourceFormat === SOURCE_FORMAT_KEYED_COLUMNS ? {} : []);
      this.sourceFormat = fields.sourceFormat || SOURCE_FORMAT_UNKNOWN;
      this.seriesLayoutBy = fields.seriesLayoutBy || SERIES_LAYOUT_BY_COLUMN;
      this.startIndex = fields.startIndex || 0;
      this.dimensionsDetectedCount = fields.dimensionsDetectedCount;
      this.metaRawOption = fields.metaRawOption;
      var dimensionsDefine = this.dimensionsDefine = fields.dimensionsDefine;
      if (dimensionsDefine) {
        for (var i2 = 0; i2 < dimensionsDefine.length; i2++) {
          var dim = dimensionsDefine[i2];
          if (dim.type == null) {
            if (guessOrdinal(this, i2) === BE_ORDINAL.Must) {
              dim.type = "ordinal";
            }
          }
        }
      }
    }
    return SourceImpl2;
  })()
);
function isSourceInstance(val) {
  return val instanceof SourceImpl;
}
function createSource(sourceData, thisMetaRawOption, sourceFormat) {
  sourceFormat = sourceFormat || detectSourceFormat(sourceData);
  var seriesLayoutBy = thisMetaRawOption.seriesLayoutBy;
  var determined = determineSourceDimensions(sourceData, sourceFormat, seriesLayoutBy, thisMetaRawOption.sourceHeader, thisMetaRawOption.dimensions);
  var source = new SourceImpl({
    data: sourceData,
    sourceFormat,
    seriesLayoutBy,
    dimensionsDefine: determined.dimensionsDefine,
    startIndex: determined.startIndex,
    dimensionsDetectedCount: determined.dimensionsDetectedCount,
    metaRawOption: clone(thisMetaRawOption)
  });
  return source;
}
function createSourceFromSeriesDataOption(data) {
  return new SourceImpl({
    data,
    sourceFormat: isTypedArray(data) ? SOURCE_FORMAT_TYPED_ARRAY : SOURCE_FORMAT_ORIGINAL
  });
}
function cloneSourceShallow(source) {
  return new SourceImpl({
    data: source.data,
    sourceFormat: source.sourceFormat,
    seriesLayoutBy: source.seriesLayoutBy,
    dimensionsDefine: clone(source.dimensionsDefine),
    startIndex: source.startIndex,
    dimensionsDetectedCount: source.dimensionsDetectedCount
  });
}
function detectSourceFormat(data) {
  var sourceFormat = SOURCE_FORMAT_UNKNOWN;
  if (isTypedArray(data)) {
    sourceFormat = SOURCE_FORMAT_TYPED_ARRAY;
  } else if (isArray(data)) {
    if (data.length === 0) {
      sourceFormat = SOURCE_FORMAT_ARRAY_ROWS;
    }
    for (var i2 = 0, len2 = data.length; i2 < len2; i2++) {
      var item = data[i2];
      if (item == null) {
        continue;
      } else if (isArray(item) || isTypedArray(item)) {
        sourceFormat = SOURCE_FORMAT_ARRAY_ROWS;
        break;
      } else if (isObject(item)) {
        sourceFormat = SOURCE_FORMAT_OBJECT_ROWS;
        break;
      }
    }
  } else if (isObject(data)) {
    for (var key2 in data) {
      if (hasOwn(data, key2) && isArrayLike(data[key2])) {
        sourceFormat = SOURCE_FORMAT_KEYED_COLUMNS;
        break;
      }
    }
  }
  return sourceFormat;
}
function determineSourceDimensions(data, sourceFormat, seriesLayoutBy, sourceHeader, dimensionsDefine) {
  var dimensionsDetectedCount;
  var startIndex;
  if (!data) {
    return {
      dimensionsDefine: normalizeDimensionsOption(dimensionsDefine),
      startIndex,
      dimensionsDetectedCount
    };
  }
  if (sourceFormat === SOURCE_FORMAT_ARRAY_ROWS) {
    var dataArrayRows = data;
    if (sourceHeader === "auto" || sourceHeader == null) {
      arrayRowsTravelFirst(function(val) {
        if (val != null && val !== "-") {
          if (isString(val)) {
            startIndex == null && (startIndex = 1);
          } else {
            startIndex = 0;
          }
        }
      }, seriesLayoutBy, dataArrayRows, 10);
    } else {
      startIndex = isNumber(sourceHeader) ? sourceHeader : sourceHeader ? 1 : 0;
    }
    if (!dimensionsDefine && startIndex === 1) {
      dimensionsDefine = [];
      arrayRowsTravelFirst(function(val, index) {
        dimensionsDefine[index] = val != null ? val + "" : "";
      }, seriesLayoutBy, dataArrayRows, Infinity);
    }
    dimensionsDetectedCount = dimensionsDefine ? dimensionsDefine.length : seriesLayoutBy === SERIES_LAYOUT_BY_ROW ? dataArrayRows.length : dataArrayRows[0] ? dataArrayRows[0].length : null;
  } else if (sourceFormat === SOURCE_FORMAT_OBJECT_ROWS) {
    if (!dimensionsDefine) {
      dimensionsDefine = objectRowsCollectDimensions(data);
    }
  } else if (sourceFormat === SOURCE_FORMAT_KEYED_COLUMNS) {
    if (!dimensionsDefine) {
      dimensionsDefine = [];
      each(data, function(colArr, key2) {
        dimensionsDefine.push(key2);
      });
    }
  } else if (sourceFormat === SOURCE_FORMAT_ORIGINAL) {
    var value0 = getDataItemValue(data[0]);
    dimensionsDetectedCount = isArray(value0) && value0.length || 1;
  } else if (sourceFormat === SOURCE_FORMAT_TYPED_ARRAY) {
    if (true) {
      assert(!!dimensionsDefine, "dimensions must be given if data is TypedArray.");
    }
  }
  return {
    startIndex,
    dimensionsDefine: normalizeDimensionsOption(dimensionsDefine),
    dimensionsDetectedCount
  };
}
function objectRowsCollectDimensions(data) {
  var firstIndex = 0;
  var obj;
  while (firstIndex < data.length && !(obj = data[firstIndex++])) {
  }
  if (obj) {
    return keys(obj);
  }
}
function normalizeDimensionsOption(dimensionsDefine) {
  if (!dimensionsDefine) {
    return;
  }
  var nameMap = createHashMap();
  return map2(dimensionsDefine, function(rawItem, index) {
    rawItem = isObject(rawItem) ? rawItem : {
      name: rawItem
    };
    var item = {
      name: rawItem.name,
      displayName: rawItem.displayName,
      type: rawItem.type
    };
    if (item.name == null) {
      return item;
    }
    item.name += "";
    if (item.displayName == null) {
      item.displayName = item.name;
    }
    var exist = nameMap.get(item.name);
    if (!exist) {
      nameMap.set(item.name, {
        count: 1
      });
    } else {
      item.name += "-" + exist.count++;
    }
    return item;
  });
}
function arrayRowsTravelFirst(cb, seriesLayoutBy, data, maxLoop) {
  if (seriesLayoutBy === SERIES_LAYOUT_BY_ROW) {
    for (var i2 = 0; i2 < data.length && i2 < maxLoop; i2++) {
      cb(data[i2] ? data[i2][0] : null, i2);
    }
  } else {
    var value0 = data[0] || [];
    for (var i2 = 0; i2 < value0.length && i2 < maxLoop; i2++) {
      cb(value0[i2], i2);
    }
  }
}
function shouldRetrieveDataByName(source) {
  var sourceFormat = source.sourceFormat;
  return sourceFormat === SOURCE_FORMAT_OBJECT_ROWS || sourceFormat === SOURCE_FORMAT_KEYED_COLUMNS;
}

// node_modules/echarts/lib/data/helper/dataProvider.js
var _a;
var _b;
var _c;
var _d;
var providerMethods;
var mountMethods;
var DefaultDataProvider = (
  /** @class */
  (function() {
    function DefaultDataProvider2(sourceParam, dimSize) {
      var source = !isSourceInstance(sourceParam) ? createSourceFromSeriesDataOption(sourceParam) : sourceParam;
      this._source = source;
      var data = this._data = source.data;
      var sourceFormat = source.sourceFormat;
      var seriesLayoutBy = source.seriesLayoutBy;
      if (sourceFormat === SOURCE_FORMAT_TYPED_ARRAY) {
        if (true) {
          if (dimSize == null) {
            throw new Error("Typed array data must specify dimension size");
          }
        }
        this._offset = 0;
        this._dimSize = dimSize;
        this._data = data;
      }
      if (true) {
        var validator = rawSourceDataValidatorMap[getMethodMapKey(sourceFormat, seriesLayoutBy)];
        validator && validator(data, source.dimensionsDefine);
      }
      mountMethods(this, data, source);
    }
    DefaultDataProvider2.prototype.getSource = function() {
      return this._source;
    };
    DefaultDataProvider2.prototype.count = function() {
      return 0;
    };
    DefaultDataProvider2.prototype.getItem = function(idx, out2) {
      return;
    };
    DefaultDataProvider2.prototype.appendData = function(newData) {
    };
    DefaultDataProvider2.prototype.clean = function() {
    };
    DefaultDataProvider2.protoInitialize = (function() {
      var proto = DefaultDataProvider2.prototype;
      proto.pure = false;
      proto.persistent = true;
    })();
    DefaultDataProvider2.internalField = (function() {
      var _a2;
      mountMethods = function(provider, data, source) {
        var sourceFormat = source.sourceFormat;
        var seriesLayoutBy = source.seriesLayoutBy;
        var startIndex = source.startIndex;
        var dimsDef = source.dimensionsDefine;
        var methods = providerMethods[getMethodMapKey(sourceFormat, seriesLayoutBy)];
        if (true) {
          assert(methods, "Invalide sourceFormat: " + sourceFormat);
        }
        extend(provider, methods);
        if (sourceFormat === SOURCE_FORMAT_TYPED_ARRAY) {
          provider.getItem = getItemForTypedArray;
          provider.count = countForTypedArray;
          provider.fillStorage = fillStorageForTypedArray;
        } else {
          var rawItemGetter = getRawSourceItemGetter(sourceFormat, seriesLayoutBy);
          provider.getItem = bind(rawItemGetter, null, data, startIndex, dimsDef);
          var rawCounter = getRawSourceDataCounter(sourceFormat, seriesLayoutBy);
          provider.count = bind(rawCounter, null, data, startIndex, dimsDef);
        }
      };
      var getItemForTypedArray = function(idx, out2) {
        idx = idx - this._offset;
        out2 = out2 || [];
        var data = this._data;
        var dimSize = this._dimSize;
        var offset = dimSize * idx;
        for (var i2 = 0; i2 < dimSize; i2++) {
          out2[i2] = data[offset + i2];
        }
        return out2;
      };
      var fillStorageForTypedArray = function(start2, end2, storage2, extent) {
        var data = this._data;
        var dimSize = this._dimSize;
        for (var dim = 0; dim < dimSize; dim++) {
          var dimExtent = extent[dim];
          var min3 = dimExtent[0] == null ? Infinity : dimExtent[0];
          var max3 = dimExtent[1] == null ? -Infinity : dimExtent[1];
          var count2 = end2 - start2;
          var arr = storage2[dim];
          for (var i2 = 0; i2 < count2; i2++) {
            var val = data[i2 * dimSize + dim];
            arr[start2 + i2] = val;
            val < min3 && (min3 = val);
            val > max3 && (max3 = val);
          }
          dimExtent[0] = min3;
          dimExtent[1] = max3;
        }
      };
      var countForTypedArray = function() {
        return this._data ? this._data.length / this._dimSize : 0;
      };
      providerMethods = (_a2 = {}, _a2[SOURCE_FORMAT_ARRAY_ROWS + "_" + SERIES_LAYOUT_BY_COLUMN] = {
        pure: true,
        appendData: appendDataSimply
      }, _a2[SOURCE_FORMAT_ARRAY_ROWS + "_" + SERIES_LAYOUT_BY_ROW] = {
        pure: true,
        appendData: function() {
          throw new Error('Do not support appendData when set seriesLayoutBy: "row".');
        }
      }, _a2[SOURCE_FORMAT_OBJECT_ROWS] = {
        pure: true,
        appendData: appendDataSimply
      }, _a2[SOURCE_FORMAT_KEYED_COLUMNS] = {
        pure: true,
        appendData: function(newData) {
          var data = this._data;
          each(newData, function(newCol, key2) {
            var oldCol = data[key2] || (data[key2] = []);
            for (var i2 = 0; i2 < (newCol || []).length; i2++) {
              oldCol.push(newCol[i2]);
            }
          });
        }
      }, _a2[SOURCE_FORMAT_ORIGINAL] = {
        appendData: appendDataSimply
      }, _a2[SOURCE_FORMAT_TYPED_ARRAY] = {
        persistent: false,
        pure: true,
        appendData: function(newData) {
          if (true) {
            assert(isTypedArray(newData), "Added data must be TypedArray if data in initialization is TypedArray");
          }
          this._data = newData;
        },
        // Clean self if data is already used.
        clean: function() {
          this._offset += this.count();
          this._data = null;
        }
      }, _a2);
      function appendDataSimply(newData) {
        for (var i2 = 0; i2 < newData.length; i2++) {
          this._data.push(newData[i2]);
        }
      }
    })();
    return DefaultDataProvider2;
  })()
);
var validateSimply = function(rawData) {
  if (!isArray(rawData)) {
    error("series.data or dataset.source must be an array.");
  }
};
var rawSourceDataValidatorMap = (_a = {}, _a[SOURCE_FORMAT_ARRAY_ROWS + "_" + SERIES_LAYOUT_BY_COLUMN] = validateSimply, _a[SOURCE_FORMAT_ARRAY_ROWS + "_" + SERIES_LAYOUT_BY_ROW] = validateSimply, _a[SOURCE_FORMAT_OBJECT_ROWS] = validateSimply, _a[SOURCE_FORMAT_KEYED_COLUMNS] = function(rawData, dimsDef) {
  for (var i2 = 0; i2 < dimsDef.length; i2++) {
    var dimName = dimsDef[i2].name;
    if (dimName == null) {
      error("dimension name must not be null/undefined.");
    }
  }
}, _a[SOURCE_FORMAT_ORIGINAL] = validateSimply, _a);
var getItemSimply = function(rawData, startIndex, dimsDef, idx) {
  return rawData[idx];
};
var rawSourceItemGetterMap = (_b = {}, _b[SOURCE_FORMAT_ARRAY_ROWS + "_" + SERIES_LAYOUT_BY_COLUMN] = function(rawData, startIndex, dimsDef, idx) {
  return rawData[idx + startIndex];
}, _b[SOURCE_FORMAT_ARRAY_ROWS + "_" + SERIES_LAYOUT_BY_ROW] = function(rawData, startIndex, dimsDef, idx, out2) {
  idx += startIndex;
  var item = out2 || [];
  var data = rawData;
  for (var i2 = 0; i2 < data.length; i2++) {
    var row = data[i2];
    item[i2] = row ? row[idx] : null;
  }
  return item;
}, _b[SOURCE_FORMAT_OBJECT_ROWS] = getItemSimply, _b[SOURCE_FORMAT_KEYED_COLUMNS] = function(rawData, startIndex, dimsDef, idx, out2) {
  var item = out2 || [];
  for (var i2 = 0; i2 < dimsDef.length; i2++) {
    var dimName = dimsDef[i2].name;
    var col = dimName != null ? rawData[dimName] : null;
    item[i2] = col ? col[idx] : null;
  }
  return item;
}, _b[SOURCE_FORMAT_ORIGINAL] = getItemSimply, _b);
function getRawSourceItemGetter(sourceFormat, seriesLayoutBy) {
  var method = rawSourceItemGetterMap[getMethodMapKey(sourceFormat, seriesLayoutBy)];
  if (true) {
    assert(method, 'Do not support get item on "' + sourceFormat + '", "' + seriesLayoutBy + '".');
  }
  return method;
}
var countSimply = function(rawData, startIndex, dimsDef) {
  return rawData.length;
};
var rawSourceDataCounterMap = (_c = {}, _c[SOURCE_FORMAT_ARRAY_ROWS + "_" + SERIES_LAYOUT_BY_COLUMN] = function(rawData, startIndex, dimsDef) {
  return Math.max(0, rawData.length - startIndex);
}, _c[SOURCE_FORMAT_ARRAY_ROWS + "_" + SERIES_LAYOUT_BY_ROW] = function(rawData, startIndex, dimsDef) {
  var row = rawData[0];
  return row ? Math.max(0, row.length - startIndex) : 0;
}, _c[SOURCE_FORMAT_OBJECT_ROWS] = countSimply, _c[SOURCE_FORMAT_KEYED_COLUMNS] = function(rawData, startIndex, dimsDef) {
  var dimName = dimsDef[0].name;
  var col = dimName != null ? rawData[dimName] : null;
  return col ? col.length : 0;
}, _c[SOURCE_FORMAT_ORIGINAL] = countSimply, _c);
function getRawSourceDataCounter(sourceFormat, seriesLayoutBy) {
  var method = rawSourceDataCounterMap[getMethodMapKey(sourceFormat, seriesLayoutBy)];
  if (true) {
    assert(method, 'Do not support count on "' + sourceFormat + '", "' + seriesLayoutBy + '".');
  }
  return method;
}
var getRawValueSimply = function(dataItem, dimIndex, property) {
  return dataItem[dimIndex];
};
var rawSourceValueGetterMap = (_d = {}, _d[SOURCE_FORMAT_ARRAY_ROWS] = getRawValueSimply, _d[SOURCE_FORMAT_OBJECT_ROWS] = function(dataItem, dimIndex, property) {
  return dataItem[property];
}, _d[SOURCE_FORMAT_KEYED_COLUMNS] = getRawValueSimply, _d[SOURCE_FORMAT_ORIGINAL] = function(dataItem, dimIndex, property) {
  var value = getDataItemValue(dataItem);
  return !(value instanceof Array) ? value : value[dimIndex];
}, _d[SOURCE_FORMAT_TYPED_ARRAY] = getRawValueSimply, _d);
function getRawSourceValueGetter(sourceFormat) {
  var method = rawSourceValueGetterMap[sourceFormat];
  if (true) {
    assert(method, 'Do not support get value on "' + sourceFormat + '".');
  }
  return method;
}
function getMethodMapKey(sourceFormat, seriesLayoutBy) {
  return sourceFormat === SOURCE_FORMAT_ARRAY_ROWS ? sourceFormat + "_" + seriesLayoutBy : sourceFormat;
}
function retrieveRawValue(data, dataIndex, dim) {
  if (!data) {
    return;
  }
  var dataItem = data.getRawDataItem(dataIndex);
  if (dataItem == null) {
    return;
  }
  var store = data.getStore();
  var sourceFormat = store.getSource().sourceFormat;
  if (dim != null) {
    var dimIndex = data.getDimensionIndex(dim);
    var property = store.getDimensionProperty(dimIndex);
    return getRawSourceValueGetter(sourceFormat)(dataItem, dimIndex, property);
  } else {
    var result = dataItem;
    if (sourceFormat === SOURCE_FORMAT_ORIGINAL) {
      result = getDataItemValue(dataItem);
    }
    return result;
  }
}

// node_modules/echarts/lib/data/helper/dimensionHelper.js
var DimensionUserOuput = (
  /** @class */
  (function() {
    function DimensionUserOuput2(encode, dimRequest) {
      this._encode = encode;
      this._schema = dimRequest;
    }
    DimensionUserOuput2.prototype.get = function() {
      return {
        // Do not generate full dimension name until fist used.
        fullDimensions: this._getFullDimensionNames(),
        encode: this._encode
      };
    };
    DimensionUserOuput2.prototype._getFullDimensionNames = function() {
      if (!this._cachedDimNames) {
        this._cachedDimNames = this._schema ? this._schema.makeOutputDimensionNames() : [];
      }
      return this._cachedDimNames;
    };
    return DimensionUserOuput2;
  })()
);
function summarizeDimensions(data, schema) {
  var summary = {};
  var encode = summary.encode = {};
  var notExtraCoordDimMap = createHashMap();
  var defaultedLabel = [];
  var defaultedTooltip = [];
  var userOutputEncode = {};
  each(data.dimensions, function(dimName) {
    var dimItem = data.getDimensionInfo(dimName);
    var coordDim = dimItem.coordDim;
    if (coordDim) {
      if (true) {
        assert(VISUAL_DIMENSIONS.get(coordDim) == null);
      }
      var coordDimIndex = dimItem.coordDimIndex;
      getOrCreateEncodeArr(encode, coordDim)[coordDimIndex] = dimName;
      if (!dimItem.isExtraCoord) {
        notExtraCoordDimMap.set(coordDim, 1);
        if (mayLabelDimType(dimItem.type)) {
          defaultedLabel[0] = dimName;
        }
        getOrCreateEncodeArr(userOutputEncode, coordDim)[coordDimIndex] = data.getDimensionIndex(dimItem.name);
      }
      if (dimItem.defaultTooltip) {
        defaultedTooltip.push(dimName);
      }
    }
    VISUAL_DIMENSIONS.each(function(v, otherDim) {
      var encodeArr = getOrCreateEncodeArr(encode, otherDim);
      var dimIndex = dimItem.otherDims[otherDim];
      if (dimIndex != null && dimIndex !== false) {
        encodeArr[dimIndex] = dimItem.name;
      }
    });
  });
  var dataDimsOnCoord = [];
  var encodeFirstDimNotExtra = {};
  notExtraCoordDimMap.each(function(v, coordDim) {
    var dimArr = encode[coordDim];
    encodeFirstDimNotExtra[coordDim] = dimArr[0];
    dataDimsOnCoord = dataDimsOnCoord.concat(dimArr);
  });
  summary.dataDimsOnCoord = dataDimsOnCoord;
  summary.dataDimIndicesOnCoord = map2(dataDimsOnCoord, function(dimName) {
    return data.getDimensionInfo(dimName).storeDimIndex;
  });
  summary.encodeFirstDimNotExtra = encodeFirstDimNotExtra;
  var encodeLabel = encode.label;
  if (encodeLabel && encodeLabel.length) {
    defaultedLabel = encodeLabel.slice();
  }
  var encodeTooltip = encode.tooltip;
  if (encodeTooltip && encodeTooltip.length) {
    defaultedTooltip = encodeTooltip.slice();
  } else if (!defaultedTooltip.length) {
    defaultedTooltip = defaultedLabel.slice();
  }
  encode.defaultedLabel = defaultedLabel;
  encode.defaultedTooltip = defaultedTooltip;
  summary.userOutput = new DimensionUserOuput(userOutputEncode, schema);
  return summary;
}
function getOrCreateEncodeArr(encode, dim) {
  if (!encode.hasOwnProperty(dim)) {
    encode[dim] = [];
  }
  return encode[dim];
}
function getDimensionTypeByAxis(axisType) {
  return axisType === "category" ? "ordinal" : axisType === "time" ? "time" : "float";
}
function mayLabelDimType(dimType) {
  return !(dimType === "ordinal" || dimType === "time");
}

// node_modules/echarts/lib/data/SeriesDimensionDefine.js
var SeriesDimensionDefine = (
  /** @class */
  /* @__PURE__ */ (function() {
    function SeriesDimensionDefine2(opt) {
      this.otherDims = {};
      if (opt != null) {
        extend(this, opt);
      }
    }
    return SeriesDimensionDefine2;
  })()
);
var SeriesDimensionDefine_default = SeriesDimensionDefine;

// node_modules/echarts/lib/data/helper/dataValueHelper.js
function parseDataValue(value, opt) {
  var dimType = opt && opt.type;
  if (dimType === "ordinal") {
    return value;
  }
  if (dimType === "time" && !isNumber(value) && value != null && value !== "-") {
    value = +parseDate(value);
  }
  return value == null || value === "" ? NaN : Number(value);
}
var valueParserMap = createHashMap({
  "number": function(val) {
    return parseFloat(val);
  },
  "time": function(val) {
    return +parseDate(val);
  },
  "trim": function(val) {
    return isString(val) ? trim(val) : val;
  }
});
function getRawValueParser(type) {
  return valueParserMap.get(type);
}
var ORDER_COMPARISON_OP_MAP = {
  lt: function(lval, rval) {
    return lval < rval;
  },
  lte: function(lval, rval) {
    return lval <= rval;
  },
  gt: function(lval, rval) {
    return lval > rval;
  },
  gte: function(lval, rval) {
    return lval >= rval;
  }
};
var FilterOrderComparator = (
  /** @class */
  (function() {
    function FilterOrderComparator2(op, rval) {
      if (!isNumber(rval)) {
        var errMsg = "";
        if (true) {
          errMsg = 'rvalue of "<", ">", "<=", ">=" can only be number in filter.';
        }
        throwError2(errMsg);
      }
      this._opFn = ORDER_COMPARISON_OP_MAP[op];
      this._rvalFloat = numericToNumber(rval);
    }
    FilterOrderComparator2.prototype.evaluate = function(lval) {
      return isNumber(lval) ? this._opFn(lval, this._rvalFloat) : this._opFn(numericToNumber(lval), this._rvalFloat);
    };
    return FilterOrderComparator2;
  })()
);
var SortOrderComparator = (
  /** @class */
  (function() {
    function SortOrderComparator2(order, incomparable) {
      var isDesc = order === "desc";
      this._resultLT = isDesc ? 1 : -1;
      if (incomparable == null) {
        incomparable = isDesc ? "min" : "max";
      }
      this._incomparable = incomparable === "min" ? -Infinity : Infinity;
    }
    SortOrderComparator2.prototype.evaluate = function(lval, rval) {
      var lvalFloat = isNumber(lval) ? lval : numericToNumber(lval);
      var rvalFloat = isNumber(rval) ? rval : numericToNumber(rval);
      var lvalNotNumeric = isNaN(lvalFloat);
      var rvalNotNumeric = isNaN(rvalFloat);
      if (lvalNotNumeric) {
        lvalFloat = this._incomparable;
      }
      if (rvalNotNumeric) {
        rvalFloat = this._incomparable;
      }
      if (lvalNotNumeric && rvalNotNumeric) {
        var lvalIsStr = isString(lval);
        var rvalIsStr = isString(rval);
        if (lvalIsStr) {
          lvalFloat = rvalIsStr ? lval : 0;
        }
        if (rvalIsStr) {
          rvalFloat = lvalIsStr ? rval : 0;
        }
      }
      return lvalFloat < rvalFloat ? this._resultLT : lvalFloat > rvalFloat ? -this._resultLT : 0;
    };
    return SortOrderComparator2;
  })()
);
var FilterEqualityComparator = (
  /** @class */
  (function() {
    function FilterEqualityComparator2(isEq, rval) {
      this._rval = rval;
      this._isEQ = isEq;
      this._rvalTypeof = typeof rval;
      this._rvalFloat = numericToNumber(rval);
    }
    FilterEqualityComparator2.prototype.evaluate = function(lval) {
      var eqResult = lval === this._rval;
      if (!eqResult) {
        var lvalTypeof = typeof lval;
        if (lvalTypeof !== this._rvalTypeof && (lvalTypeof === "number" || this._rvalTypeof === "number")) {
          eqResult = numericToNumber(lval) === this._rvalFloat;
        }
      }
      return this._isEQ ? eqResult : !eqResult;
    };
    return FilterEqualityComparator2;
  })()
);
function createFilterComparator(op, rval) {
  return op === "eq" || op === "ne" ? new FilterEqualityComparator(op === "eq", rval) : hasOwn(ORDER_COMPARISON_OP_MAP, op) ? new FilterOrderComparator(op, rval) : null;
}
function parseSanitizationFilter(filter3) {
  var filterKey = "";
  var filterG = -Infinity;
  var filterGE = -Infinity;
  var filterL = Infinity;
  var filterLE = Infinity;
  if (filter3) {
    if (filter3.g != null) {
      filterKey += "G" + filter3.g;
      filterG = filter3.g;
    }
    if (filter3.ge != null) {
      filterKey += "GE" + filter3.ge;
      filterGE = filter3.ge;
    }
    if (filter3.l != null) {
      filterKey += "L" + filter3.l;
      filterL = filter3.l;
    }
    if (filter3.le != null) {
      filterKey += "LE" + filter3.le;
      filterLE = filter3.le;
    }
  }
  return {
    key: filterKey,
    g: filterG,
    ge: filterGE,
    l: filterL,
    le: filterLE
  };
}
function passesSanitizationFilter(filterParsed, value) {
  return value > filterParsed.g && value >= filterParsed.ge && value < filterParsed.l && value <= filterParsed.le;
}

// node_modules/echarts/lib/data/DataStore.js
var CtorUint32Array = typeof Uint32Array === UNDEFINED_STR ? Array : Uint32Array;
var CtorUint16Array = typeof Uint16Array === UNDEFINED_STR ? Array : Uint16Array;
var CtorInt32Array = typeof Int32Array === UNDEFINED_STR ? Array : Int32Array;
var CtorFloat64Array = typeof Float64Array === UNDEFINED_STR ? Array : Float64Array;
var dataCtors = {
  "float": CtorFloat64Array,
  "int": CtorInt32Array,
  // Ordinal data type can be string or int
  "ordinal": Array,
  "number": Array,
  "time": CtorFloat64Array
};
var defaultDimValueGetters;
function getIndicesCtor(rawCount) {
  return rawCount > 65535 ? CtorUint32Array : CtorUint16Array;
}
function cloneChunk(originalChunk) {
  var Ctor = originalChunk.constructor;
  return Ctor === Array ? originalChunk.slice() : new Ctor(originalChunk);
}
function prepareStore(store, dimIdx, dimType, end2, append) {
  var DataCtor = dataCtors[dimType || "float"];
  if (append) {
    var oldStore = store[dimIdx];
    var oldLen = oldStore && oldStore.length;
    if (!(oldLen === end2)) {
      var newStore = new DataCtor(end2);
      for (var j = 0; j < oldLen; j++) {
        newStore[j] = oldStore[j];
      }
      store[dimIdx] = newStore;
    }
  } else {
    store[dimIdx] = new DataCtor(end2);
  }
}
var DataStore = (
  /** @class */
  (function() {
    function DataStore2() {
      this._chunks = [];
      this._rawExtent = [];
      this._extent = [];
      this._count = 0;
      this._rawCount = 0;
      this._calcDimNameToIdx = createHashMap();
    }
    DataStore2.prototype.initData = function(provider, inputDimensions, dimValueGetter) {
      if (true) {
        assert(isFunction(provider.getItem) && isFunction(provider.count), "Invalid data provider.");
      }
      this._provider = provider;
      this._chunks = [];
      this._indices = null;
      this.getRawIndex = this._getRawIdxIdentity;
      var source = provider.getSource();
      var defaultGetter = this.defaultDimValueGetter = defaultDimValueGetters[source.sourceFormat];
      this._dimValueGetter = dimValueGetter || defaultGetter;
      this._rawExtent = [];
      var willRetrieveDataByName = shouldRetrieveDataByName(source);
      this._dimensions = map2(inputDimensions, function(dim) {
        if (true) {
          if (willRetrieveDataByName) {
            assert(dim.property != null);
          }
        }
        return {
          // Only pick these two props. Not leak other properties like orderMeta.
          type: dim.type,
          property: dim.property
        };
      });
      this._initDataFromProvider(0, provider.count());
    };
    DataStore2.prototype.getProvider = function() {
      return this._provider;
    };
    DataStore2.prototype.getSource = function() {
      return this._provider.getSource();
    };
    DataStore2.prototype.ensureCalculationDimension = function(dimName, type) {
      var calcDimNameToIdx = this._calcDimNameToIdx;
      var dimensions = this._dimensions;
      var calcDimIdx = calcDimNameToIdx.get(dimName);
      if (calcDimIdx != null) {
        if (dimensions[calcDimIdx].type === type) {
          return calcDimIdx;
        }
      } else {
        calcDimIdx = dimensions.length;
      }
      dimensions[calcDimIdx] = {
        type
      };
      calcDimNameToIdx.set(dimName, calcDimIdx);
      this._chunks[calcDimIdx] = new dataCtors[type || "float"](this._rawCount);
      this._rawExtent[calcDimIdx] = initExtentForUnion();
      return calcDimIdx;
    };
    DataStore2.prototype.collectOrdinalMeta = function(dimIdx, ordinalMeta) {
      var chunk = this._chunks[dimIdx];
      var dim = this._dimensions[dimIdx];
      var rawExtents = this._rawExtent;
      var offset = dim.ordinalOffset || 0;
      var len2 = chunk.length;
      if (offset === 0) {
        rawExtents[dimIdx] = initExtentForUnion();
      }
      var dimRawExtent = rawExtents[dimIdx];
      for (var i2 = offset; i2 < len2; i2++) {
        var val = chunk[i2] = ordinalMeta.parseAndCollect(chunk[i2]);
        if (!isNaN(val)) {
          dimRawExtent[0] = Math.min(val, dimRawExtent[0]);
          dimRawExtent[1] = Math.max(val, dimRawExtent[1]);
        }
      }
      dim.ordinalMeta = ordinalMeta;
      dim.ordinalOffset = len2;
      dim.type = "ordinal";
    };
    DataStore2.prototype.getOrdinalMeta = function(dimIdx) {
      var dimInfo = this._dimensions[dimIdx];
      var ordinalMeta = dimInfo.ordinalMeta;
      return ordinalMeta;
    };
    DataStore2.prototype.getDimensionProperty = function(dimIndex) {
      var item = this._dimensions[dimIndex];
      return item && item.property;
    };
    DataStore2.prototype.appendData = function(data) {
      if (true) {
        assert(!this._indices, "appendData can only be called on raw data.");
      }
      var provider = this._provider;
      var start2 = this.count();
      provider.appendData(data);
      var end2 = provider.count();
      if (!provider.persistent) {
        end2 += start2;
      }
      if (start2 < end2) {
        this._initDataFromProvider(start2, end2, true);
      }
      return [start2, end2];
    };
    DataStore2.prototype.appendValues = function(values, minFillLen) {
      var chunks = this._chunks;
      var dimensions = this._dimensions;
      var dimLen = dimensions.length;
      var rawExtent = this._rawExtent;
      var start2 = this.count();
      var end2 = start2 + Math.max(values.length, minFillLen || 0);
      for (var i2 = 0; i2 < dimLen; i2++) {
        var dim = dimensions[i2];
        prepareStore(chunks, i2, dim.type, end2, true);
      }
      var emptyDataItem = [];
      for (var idx = start2; idx < end2; idx++) {
        var sourceIdx = idx - start2;
        for (var dimIdx = 0; dimIdx < dimLen; dimIdx++) {
          var dim = dimensions[dimIdx];
          var val = defaultDimValueGetters.arrayRows.call(this, values[sourceIdx] || emptyDataItem, dim.property, sourceIdx, dimIdx);
          chunks[dimIdx][idx] = val;
          var dimRawExtent = rawExtent[dimIdx];
          val < dimRawExtent[0] && (dimRawExtent[0] = val);
          val > dimRawExtent[1] && (dimRawExtent[1] = val);
        }
      }
      this._rawCount = this._count = end2;
      return {
        start: start2,
        end: end2
      };
    };
    DataStore2.prototype._initDataFromProvider = function(start2, end2, append) {
      var provider = this._provider;
      var chunks = this._chunks;
      var dimensions = this._dimensions;
      var dimLen = dimensions.length;
      var rawExtent = this._rawExtent;
      var dimNames = map2(dimensions, function(dim2) {
        return dim2.property;
      });
      for (var i2 = 0; i2 < dimLen; i2++) {
        var dim = dimensions[i2];
        if (!rawExtent[i2]) {
          rawExtent[i2] = initExtentForUnion();
        }
        prepareStore(chunks, i2, dim.type, end2, append);
      }
      if (provider.fillStorage) {
        provider.fillStorage(start2, end2, chunks, rawExtent);
      } else {
        var dataItem = [];
        for (var idx = start2; idx < end2; idx++) {
          dataItem = provider.getItem(idx, dataItem);
          for (var dimIdx = 0; dimIdx < dimLen; dimIdx++) {
            var dimStorage = chunks[dimIdx];
            var val = this._dimValueGetter(dataItem, dimNames[dimIdx], idx, dimIdx);
            dimStorage[idx] = val;
            var dimRawExtent = rawExtent[dimIdx];
            val < dimRawExtent[0] && (dimRawExtent[0] = val);
            val > dimRawExtent[1] && (dimRawExtent[1] = val);
          }
        }
      }
      if (!provider.persistent && provider.clean) {
        provider.clean();
      }
      this._rawCount = this._count = end2;
      this._extent = [];
    };
    DataStore2.prototype.count = function() {
      return this._count;
    };
    DataStore2.prototype.get = function(dim, idx) {
      if (!(idx >= 0 && idx < this._count)) {
        return NaN;
      }
      var dimStore = this._chunks[dim];
      return dimStore ? dimStore[this.getRawIndex(idx)] : NaN;
    };
    DataStore2.prototype.getValues = function(dimensions, idx) {
      var values = [];
      var dimArr = [];
      if (idx == null) {
        idx = dimensions;
        dimensions = [];
        for (var i2 = 0; i2 < this._dimensions.length; i2++) {
          dimArr.push(i2);
        }
      } else {
        dimArr = dimensions;
      }
      for (var i2 = 0, len2 = dimArr.length; i2 < len2; i2++) {
        values.push(this.get(dimArr[i2], idx));
      }
      return values;
    };
    DataStore2.prototype.getByRawIndex = function(dim, rawIdx) {
      if (!(rawIdx >= 0 && rawIdx < this._rawCount)) {
        return NaN;
      }
      var dimStore = this._chunks[dim];
      return dimStore ? dimStore[rawIdx] : NaN;
    };
    DataStore2.prototype.getSum = function(dim) {
      var dimData = this._chunks[dim];
      var sum2 = 0;
      if (dimData) {
        for (var i2 = 0, len2 = this.count(); i2 < len2; i2++) {
          var value = this.get(dim, i2);
          if (!isNaN(value)) {
            sum2 += value;
          }
        }
      }
      return sum2;
    };
    DataStore2.prototype.getMedian = function(dim) {
      var dimDataArray = [];
      this.each([dim], function(val) {
        if (!isNaN(val)) {
          dimDataArray.push(val);
        }
      });
      asc(dimDataArray);
      var len2 = this.count();
      return len2 === 0 ? 0 : len2 % 2 === 1 ? dimDataArray[(len2 - 1) / 2] : (dimDataArray[len2 / 2] + dimDataArray[len2 / 2 - 1]) / 2;
    };
    DataStore2.prototype.indexOfRawIndex = function(rawIndex) {
      if (rawIndex >= this._rawCount || rawIndex < 0) {
        return -1;
      }
      if (!this._indices) {
        return rawIndex;
      }
      var indices = this._indices;
      var rawDataIndex = indices[rawIndex];
      if (rawDataIndex != null && rawDataIndex < this._count && rawDataIndex === rawIndex) {
        return rawIndex;
      }
      var left = 0;
      var right = this._count - 1;
      while (left <= right) {
        var mid = (left + right) / 2 | 0;
        if (indices[mid] < rawIndex) {
          left = mid + 1;
        } else if (indices[mid] > rawIndex) {
          right = mid - 1;
        } else {
          return mid;
        }
      }
      return -1;
    };
    DataStore2.prototype.getIndices = function() {
      var newIndices;
      var indices = this._indices;
      if (indices) {
        var Ctor = indices.constructor;
        var thisCount = this._count;
        if (Ctor === Array) {
          newIndices = new Ctor(thisCount);
          for (var i2 = 0; i2 < thisCount; i2++) {
            newIndices[i2] = indices[i2];
          }
        } else {
          newIndices = new Ctor(indices.buffer, 0, thisCount);
        }
      } else {
        var Ctor = getIndicesCtor(this._rawCount);
        newIndices = new Ctor(this.count());
        for (var i2 = 0; i2 < newIndices.length; i2++) {
          newIndices[i2] = i2;
        }
      }
      return newIndices;
    };
    DataStore2.prototype.filter = function(dims, cb) {
      if (!this._count) {
        return this;
      }
      var newStore = this.clone();
      var count2 = newStore.count();
      var Ctor = getIndicesCtor(newStore._rawCount);
      var newIndices = new Ctor(count2);
      var value = [];
      var dimSize = dims.length;
      var offset = 0;
      var dim0 = dims[0];
      var chunks = newStore._chunks;
      for (var i2 = 0; i2 < count2; i2++) {
        var keep = void 0;
        var rawIdx = newStore.getRawIndex(i2);
        if (dimSize === 0) {
          keep = cb(i2);
        } else if (dimSize === 1) {
          var val = chunks[dim0][rawIdx];
          keep = cb(val, i2);
        } else {
          var k2 = 0;
          for (; k2 < dimSize; k2++) {
            value[k2] = chunks[dims[k2]][rawIdx];
          }
          value[k2] = i2;
          keep = cb.apply(null, value);
        }
        if (keep) {
          newIndices[offset++] = rawIdx;
        }
      }
      if (offset < count2) {
        newStore._indices = newIndices;
      }
      newStore._count = offset;
      newStore._extent = [];
      newStore._updateGetRawIdx();
      return newStore;
    };
    DataStore2.prototype.selectRange = function(range) {
      var newStore = this.clone();
      var len2 = newStore._count;
      if (!len2) {
        return this;
      }
      var dims = keys(range);
      var dimSize = dims.length;
      if (!dimSize) {
        return this;
      }
      var originalCount = newStore.count();
      var Ctor = getIndicesCtor(newStore._rawCount);
      var newIndices = new Ctor(originalCount);
      var offset = 0;
      var dim0 = dims[0];
      var min3 = range[dim0][0];
      var max3 = range[dim0][1];
      var storeArr = newStore._chunks;
      var quickFinished = false;
      if (!newStore._indices) {
        var idx = 0;
        if (dimSize === 1) {
          var dimStorage = storeArr[dims[0]];
          for (var i2 = 0; i2 < len2; i2++) {
            var val = dimStorage[i2];
            if (val >= min3 && val <= max3 || isNaN(val)) {
              newIndices[offset++] = idx;
            }
            idx++;
          }
          quickFinished = true;
        } else if (dimSize === 2) {
          var dimStorage = storeArr[dims[0]];
          var dimStorage2 = storeArr[dims[1]];
          var min23 = range[dims[1]][0];
          var max23 = range[dims[1]][1];
          for (var i2 = 0; i2 < len2; i2++) {
            var val = dimStorage[i2];
            var val2 = dimStorage2[i2];
            if ((val >= min3 && val <= max3 || isNaN(val)) && (val2 >= min23 && val2 <= max23 || isNaN(val2))) {
              newIndices[offset++] = idx;
            }
            idx++;
          }
          quickFinished = true;
        }
      }
      if (!quickFinished) {
        if (dimSize === 1) {
          for (var i2 = 0; i2 < originalCount; i2++) {
            var rawIndex = newStore.getRawIndex(i2);
            var val = storeArr[dims[0]][rawIndex];
            if (val >= min3 && val <= max3 || isNaN(val)) {
              newIndices[offset++] = rawIndex;
            }
          }
        } else {
          for (var i2 = 0; i2 < originalCount; i2++) {
            var keep = true;
            var rawIndex = newStore.getRawIndex(i2);
            for (var k2 = 0; k2 < dimSize; k2++) {
              var dimk = dims[k2];
              var val = storeArr[dimk][rawIndex];
              if (val < range[dimk][0] || val > range[dimk][1]) {
                keep = false;
              }
            }
            if (keep) {
              newIndices[offset++] = newStore.getRawIndex(i2);
            }
          }
        }
      }
      if (offset < originalCount) {
        newStore._indices = newIndices;
      }
      newStore._count = offset;
      newStore._extent = [];
      newStore._updateGetRawIdx();
      return newStore;
    };
    DataStore2.prototype.map = function(dims, cb) {
      var target = this.clone(dims);
      this._updateDims(target, dims, cb);
      return target;
    };
    DataStore2.prototype.modify = function(dims, cb) {
      this._updateDims(this, dims, cb);
    };
    DataStore2.prototype._updateDims = function(target, dims, cb) {
      var targetChunks = target._chunks;
      var tmpRetValue = [];
      var dimSize = dims.length;
      var dataCount = target.count();
      var values = [];
      var rawExtent = target._rawExtent;
      for (var i2 = 0; i2 < dims.length; i2++) {
        rawExtent[dims[i2]] = initExtentForUnion();
      }
      for (var dataIndex = 0; dataIndex < dataCount; dataIndex++) {
        var rawIndex = target.getRawIndex(dataIndex);
        for (var k2 = 0; k2 < dimSize; k2++) {
          values[k2] = targetChunks[dims[k2]][rawIndex];
        }
        values[dimSize] = dataIndex;
        var retValue = cb && cb.apply(null, values);
        if (retValue != null) {
          if (typeof retValue !== "object") {
            tmpRetValue[0] = retValue;
            retValue = tmpRetValue;
          }
          for (var i2 = 0; i2 < retValue.length; i2++) {
            var dim = dims[i2];
            var val = retValue[i2];
            var rawExtentOnDim = rawExtent[dim];
            var dimStore = targetChunks[dim];
            if (dimStore) {
              dimStore[rawIndex] = val;
            }
            if (val < rawExtentOnDim[0]) {
              rawExtentOnDim[0] = val;
            }
            if (val > rawExtentOnDim[1]) {
              rawExtentOnDim[1] = val;
            }
          }
        }
      }
    };
    DataStore2.prototype.lttbDownSample = function(valueDimension, rate) {
      var target = this.clone([valueDimension], true);
      var targetStorage = target._chunks;
      var dimStore = targetStorage[valueDimension];
      var len2 = this.count();
      var sampledIndex = 0;
      var frameSize = Math.floor(1 / rate);
      var currentRawIndex = this.getRawIndex(0);
      var maxArea;
      var area;
      var nextRawIndex;
      var newIndices = new (getIndicesCtor(this._rawCount))(Math.min((Math.ceil(len2 / frameSize) + 2) * 2, len2));
      newIndices[sampledIndex++] = currentRawIndex;
      for (var i2 = 1; i2 < len2 - 1; i2 += frameSize) {
        var nextFrameStart = Math.min(i2 + frameSize, len2 - 1);
        var nextFrameEnd = Math.min(i2 + frameSize * 2, len2);
        var avgX = (nextFrameEnd + nextFrameStart) / 2;
        var avgY = 0;
        for (var idx = nextFrameStart; idx < nextFrameEnd; idx++) {
          var rawIndex = this.getRawIndex(idx);
          var y = dimStore[rawIndex];
          if (isNaN(y)) {
            continue;
          }
          avgY += y;
        }
        avgY /= nextFrameEnd - nextFrameStart;
        var frameStart = i2;
        var frameEnd = Math.min(i2 + frameSize, len2);
        var pointAX = i2 - 1;
        var pointAY = dimStore[currentRawIndex];
        maxArea = -1;
        nextRawIndex = frameStart;
        var firstNaNIndex = -1;
        var countNaN = 0;
        for (var idx = frameStart; idx < frameEnd; idx++) {
          var rawIndex = this.getRawIndex(idx);
          var y = dimStore[rawIndex];
          if (isNaN(y)) {
            countNaN++;
            if (firstNaNIndex < 0) {
              firstNaNIndex = rawIndex;
            }
            continue;
          }
          area = Math.abs((pointAX - avgX) * (y - pointAY) - (pointAX - idx) * (avgY - pointAY));
          if (area > maxArea) {
            maxArea = area;
            nextRawIndex = rawIndex;
          }
        }
        if (countNaN > 0 && countNaN < frameEnd - frameStart) {
          newIndices[sampledIndex++] = Math.min(firstNaNIndex, nextRawIndex);
          nextRawIndex = Math.max(firstNaNIndex, nextRawIndex);
        }
        newIndices[sampledIndex++] = nextRawIndex;
        currentRawIndex = nextRawIndex;
      }
      newIndices[sampledIndex++] = this.getRawIndex(len2 - 1);
      target._count = sampledIndex;
      target._indices = newIndices;
      target.getRawIndex = this._getRawIdx;
      return target;
    };
    DataStore2.prototype.minmaxDownSample = function(valueDimension, rate) {
      var target = this.clone([valueDimension], true);
      var targetStorage = target._chunks;
      var frameSize = Math.floor(1 / rate);
      var dimStore = targetStorage[valueDimension];
      var len2 = this.count();
      var newIndices = new (getIndicesCtor(this._rawCount))(Math.ceil(len2 / frameSize) * 2);
      var offset = 0;
      for (var i2 = 0; i2 < len2; i2 += frameSize) {
        var minIndex = i2;
        var minValue = dimStore[this.getRawIndex(minIndex)];
        var maxIndex = i2;
        var maxValue = dimStore[this.getRawIndex(maxIndex)];
        var thisFrameSize = frameSize;
        if (i2 + frameSize > len2) {
          thisFrameSize = len2 - i2;
        }
        for (var k2 = 0; k2 < thisFrameSize; k2++) {
          var rawIndex = this.getRawIndex(i2 + k2);
          var value = dimStore[rawIndex];
          if (value < minValue) {
            minValue = value;
            minIndex = i2 + k2;
          }
          if (value > maxValue) {
            maxValue = value;
            maxIndex = i2 + k2;
          }
        }
        var rawMinIndex = this.getRawIndex(minIndex);
        var rawMaxIndex = this.getRawIndex(maxIndex);
        if (minIndex < maxIndex) {
          newIndices[offset++] = rawMinIndex;
          newIndices[offset++] = rawMaxIndex;
        } else {
          newIndices[offset++] = rawMaxIndex;
          newIndices[offset++] = rawMinIndex;
        }
      }
      target._count = offset;
      target._indices = newIndices;
      target._updateGetRawIdx();
      return target;
    };
    DataStore2.prototype.downSample = function(dimension, rate, sampleValue, sampleIndex) {
      var target = this.clone([dimension], true);
      var targetStorage = target._chunks;
      var frameValues = [];
      var frameSize = Math.floor(1 / rate);
      var dimStore = targetStorage[dimension];
      var len2 = this.count();
      var rawExtentOnDim = target._rawExtent[dimension] = initExtentForUnion();
      var newIndices = new (getIndicesCtor(this._rawCount))(Math.ceil(len2 / frameSize));
      var offset = 0;
      for (var i2 = 0; i2 < len2; i2 += frameSize) {
        if (frameSize > len2 - i2) {
          frameSize = len2 - i2;
          frameValues.length = frameSize;
        }
        for (var k2 = 0; k2 < frameSize; k2++) {
          var dataIdx = this.getRawIndex(i2 + k2);
          frameValues[k2] = dimStore[dataIdx];
        }
        var value = sampleValue(frameValues);
        var sampleFrameIdx = this.getRawIndex(Math.min(i2 + sampleIndex(frameValues, value) || 0, len2 - 1));
        dimStore[sampleFrameIdx] = value;
        if (value < rawExtentOnDim[0]) {
          rawExtentOnDim[0] = value;
        }
        if (value > rawExtentOnDim[1]) {
          rawExtentOnDim[1] = value;
        }
        newIndices[offset++] = sampleFrameIdx;
      }
      target._count = offset;
      target._indices = newIndices;
      target._updateGetRawIdx();
      return target;
    };
    DataStore2.prototype.each = function(dims, cb) {
      if (!this._count) {
        return;
      }
      var dimSize = dims.length;
      var chunks = this._chunks;
      for (var i2 = 0, len2 = this.count(); i2 < len2; i2++) {
        var rawIdx = this.getRawIndex(i2);
        switch (dimSize) {
          case 0:
            cb(i2);
            break;
          case 1:
            cb(chunks[dims[0]][rawIdx], i2);
            break;
          case 2:
            cb(chunks[dims[0]][rawIdx], chunks[dims[1]][rawIdx], i2);
            break;
          default:
            var k2 = 0;
            var value = [];
            for (; k2 < dimSize; k2++) {
              value[k2] = chunks[dims[k2]][rawIdx];
            }
            value[k2] = i2;
            cb.apply(null, value);
        }
      }
    };
    DataStore2.prototype.getDataExtent = function(dim, filter3) {
      var dimData = this._chunks[dim];
      var initialExtent = initExtentForUnion();
      if (!dimData) {
        return initialExtent;
      }
      var currEnd = this.count();
      var useRaw = !this._indices && !filter3;
      if (useRaw) {
        return this._rawExtent[dim].slice();
      }
      var thisExtent = this._extent;
      var dimExtentRecord = thisExtent[dim] || (thisExtent[dim] = {});
      var filterParsed = parseSanitizationFilter(filter3);
      var filterKey = filterParsed.key;
      var dimExtent = dimExtentRecord[filterKey];
      if (dimExtent) {
        return dimExtent.slice();
      }
      var min3 = initialExtent[0];
      var max3 = initialExtent[1];
      for (var i2 = 0; i2 < currEnd; i2++) {
        var rawIdx = this.getRawIndex(i2);
        var value = dimData[rawIdx];
        if (!filter3 || passesSanitizationFilter(filterParsed, value)) {
          if (value < min3) {
            min3 = value;
          }
          if (value > max3) {
            max3 = value;
          }
        }
      }
      return dimExtentRecord[filterKey] = [min3, max3];
    };
    DataStore2.prototype.getRawDataItem = function(idx) {
      var rawIdx = this.getRawIndex(idx);
      if (!this._provider.persistent) {
        var val = [];
        var chunks = this._chunks;
        for (var i2 = 0; i2 < chunks.length; i2++) {
          val.push(chunks[i2][rawIdx]);
        }
        return val;
      } else {
        return this._provider.getItem(rawIdx);
      }
    };
    DataStore2.prototype.clone = function(clonedDims, ignoreIndices) {
      var target = new DataStore2();
      var chunks = this._chunks;
      var clonedDimsMap = clonedDims && reduce(clonedDims, function(obj, dimIdx) {
        obj[dimIdx] = true;
        return obj;
      }, {});
      if (clonedDimsMap) {
        for (var i2 = 0; i2 < chunks.length; i2++) {
          target._chunks[i2] = !clonedDimsMap[i2] ? chunks[i2] : cloneChunk(chunks[i2]);
        }
      } else {
        target._chunks = chunks;
      }
      this._copyCommonProps(target);
      if (!ignoreIndices) {
        target._indices = this._cloneIndices();
      }
      target._updateGetRawIdx();
      return target;
    };
    DataStore2.prototype._copyCommonProps = function(target) {
      target._count = this._count;
      target._rawCount = this._rawCount;
      target._provider = this._provider;
      target._dimensions = this._dimensions;
      target._extent = clone(this._extent);
      target._rawExtent = clone(this._rawExtent);
    };
    DataStore2.prototype._cloneIndices = function() {
      if (this._indices) {
        var Ctor = this._indices.constructor;
        var indices = void 0;
        if (Ctor === Array) {
          var thisCount = this._indices.length;
          indices = new Ctor(thisCount);
          for (var i2 = 0; i2 < thisCount; i2++) {
            indices[i2] = this._indices[i2];
          }
        } else {
          indices = new Ctor(this._indices);
        }
        return indices;
      }
      return null;
    };
    DataStore2.prototype._getRawIdxIdentity = function(idx) {
      return idx;
    };
    DataStore2.prototype._getRawIdx = function(idx) {
      if (idx < this._count && idx >= 0) {
        return this._indices[idx];
      }
      return -1;
    };
    DataStore2.prototype._updateGetRawIdx = function() {
      this.getRawIndex = this._indices ? this._getRawIdx : this._getRawIdxIdentity;
    };
    DataStore2.internalField = (function() {
      function getDimValueSimply(dataItem, property, dataIndex, dimIndex) {
        return parseDataValue(dataItem[dimIndex], this._dimensions[dimIndex]);
      }
      defaultDimValueGetters = {
        arrayRows: getDimValueSimply,
        objectRows: function(dataItem, property, dataIndex, dimIndex) {
          return parseDataValue(dataItem[property], this._dimensions[dimIndex]);
        },
        keyedColumns: getDimValueSimply,
        original: function(dataItem, property, dataIndex, dimIndex) {
          var value = dataItem && (dataItem.value == null ? dataItem : dataItem.value);
          return parseDataValue(value instanceof Array ? value[dimIndex] : value, this._dimensions[dimIndex]);
        },
        typedArray: function(dataItem, property, dataIndex, dimIndex) {
          return dataItem[dimIndex];
        }
      };
    })();
    return DataStore2;
  })()
);
var DataStore_default = DataStore;

// node_modules/echarts/lib/data/helper/SeriesDataSchema.js
var inner = makeInner();
var dimTypeShort = {
  float: "f",
  int: "i",
  ordinal: "o",
  number: "n",
  time: "t"
};
var SeriesDataSchema = (
  /** @class */
  (function() {
    function SeriesDataSchema2(opt) {
      this.dimensions = opt.dimensions;
      this._dimOmitted = opt.dimensionOmitted;
      this.source = opt.source;
      this._fullDimCount = opt.fullDimensionCount;
      this._updateDimOmitted(opt.dimensionOmitted);
    }
    SeriesDataSchema2.prototype.isDimensionOmitted = function() {
      return this._dimOmitted;
    };
    SeriesDataSchema2.prototype._updateDimOmitted = function(dimensionOmitted) {
      this._dimOmitted = dimensionOmitted;
      if (!dimensionOmitted) {
        return;
      }
      if (!this._dimNameMap) {
        this._dimNameMap = ensureSourceDimNameMap(this.source);
      }
    };
    SeriesDataSchema2.prototype.getSourceDimensionIndex = function(dimName) {
      return retrieve2(this._dimNameMap.get(dimName), -1);
    };
    SeriesDataSchema2.prototype.getSourceDimension = function(dimIndex) {
      var dimensionsDefine = this.source.dimensionsDefine;
      if (dimensionsDefine) {
        return dimensionsDefine[dimIndex];
      }
    };
    SeriesDataSchema2.prototype.makeStoreSchema = function() {
      var dimCount = this._fullDimCount;
      var willRetrieveDataByName = shouldRetrieveDataByName(this.source);
      var makeHashStrict = !shouldOmitUnusedDimensions(dimCount);
      var dimHash = "";
      var dims = [];
      for (var fullDimIdx = 0, seriesDimIdx = 0; fullDimIdx < dimCount; fullDimIdx++) {
        var property = void 0;
        var type = void 0;
        var ordinalMeta = void 0;
        var seriesDimDef = this.dimensions[seriesDimIdx];
        if (seriesDimDef && seriesDimDef.storeDimIndex === fullDimIdx) {
          property = willRetrieveDataByName ? seriesDimDef.name : null;
          type = seriesDimDef.type;
          ordinalMeta = seriesDimDef.ordinalMeta;
          seriesDimIdx++;
        } else {
          var sourceDimDef = this.getSourceDimension(fullDimIdx);
          if (sourceDimDef) {
            property = willRetrieveDataByName ? sourceDimDef.name : null;
            type = sourceDimDef.type;
          }
        }
        dims.push({
          property,
          type,
          ordinalMeta
        });
        if (willRetrieveDataByName && property != null && (!seriesDimDef || !seriesDimDef.isCalculationCoord)) {
          dimHash += makeHashStrict ? property.replace(/\`/g, "`1").replace(/\$/g, "`2") : property;
        }
        dimHash += "$";
        dimHash += dimTypeShort[type] || "f";
        if (ordinalMeta) {
          dimHash += ordinalMeta.uid;
        }
        dimHash += "$";
      }
      var source = this.source;
      var hash = [source.seriesLayoutBy, source.startIndex, dimHash].join("$$");
      return {
        dimensions: dims,
        hash
      };
    };
    SeriesDataSchema2.prototype.makeOutputDimensionNames = function() {
      var result = [];
      for (var fullDimIdx = 0, seriesDimIdx = 0; fullDimIdx < this._fullDimCount; fullDimIdx++) {
        var name_1 = void 0;
        var seriesDimDef = this.dimensions[seriesDimIdx];
        if (seriesDimDef && seriesDimDef.storeDimIndex === fullDimIdx) {
          if (!seriesDimDef.isCalculationCoord) {
            name_1 = seriesDimDef.name;
          }
          seriesDimIdx++;
        } else {
          var sourceDimDef = this.getSourceDimension(fullDimIdx);
          if (sourceDimDef) {
            name_1 = sourceDimDef.name;
          }
        }
        result.push(name_1);
      }
      return result;
    };
    SeriesDataSchema2.prototype.appendCalculationDimension = function(dimDef) {
      this.dimensions.push(dimDef);
      dimDef.isCalculationCoord = true;
      this._fullDimCount++;
      this._updateDimOmitted(true);
    };
    return SeriesDataSchema2;
  })()
);
function isSeriesDataSchema(schema) {
  return schema instanceof SeriesDataSchema;
}
function createDimNameMap(dimsDef) {
  var dataDimNameMap = createHashMap();
  for (var i2 = 0; i2 < (dimsDef || []).length; i2++) {
    var dimDefItemRaw = dimsDef[i2];
    var userDimName = isObject(dimDefItemRaw) ? dimDefItemRaw.name : dimDefItemRaw;
    if (userDimName != null && dataDimNameMap.get(userDimName) == null) {
      dataDimNameMap.set(userDimName, i2);
    }
  }
  return dataDimNameMap;
}
function ensureSourceDimNameMap(source) {
  var innerSource = inner(source);
  return innerSource.dimNameMap || (innerSource.dimNameMap = createDimNameMap(source.dimensionsDefine));
}
function shouldOmitUnusedDimensions(dimCount) {
  return dimCount > 30;
}

// node_modules/echarts/lib/data/SeriesData.js
var isObject2 = isObject;
var map3 = map2;
var CtorInt32Array2 = typeof Int32Array === "undefined" ? Array : Int32Array;
var ID_PREFIX = "e\0\0";
var INDEX_NOT_FOUND = -1;
var TRANSFERABLE_PROPERTIES = ["hasItemOption", "_nameList", "_idList", "_invertedIndicesMap", "_dimSummary", "userOutput", "_rawData", "_dimValueGetter", "_nameDimIdx", "_idDimIdx", "_nameRepeatCount"];
var CLONE_PROPERTIES = ["_approximateExtent"];
var prepareInvertedIndex;
var getId;
var getIdNameFromStore;
var normalizeDimensions;
var transferProperties;
var cloneListForMapAndSample;
var makeIdFromName;
var SeriesData = (
  /** @class */
  (function() {
    function SeriesData2(dimensionsInput, hostModel) {
      this.type = "list";
      this._dimOmitted = false;
      this._nameList = [];
      this._idList = [];
      this._visual = {};
      this._layout = {};
      this._itemVisuals = [];
      this._itemLayouts = [];
      this._graphicEls = [];
      this._approximateExtent = {};
      this._calculationInfo = {};
      this.hasItemOption = false;
      this.TRANSFERABLE_METHODS = ["cloneShallow", "downSample", "minmaxDownSample", "lttbDownSample", "map"];
      this.CHANGABLE_METHODS = ["filterSelf", "selectRange"];
      this.DOWNSAMPLE_METHODS = ["downSample", "minmaxDownSample", "lttbDownSample"];
      var dimensions;
      var assignStoreDimIdx = false;
      if (isSeriesDataSchema(dimensionsInput)) {
        dimensions = dimensionsInput.dimensions;
        this._dimOmitted = dimensionsInput.isDimensionOmitted();
        this._schema = dimensionsInput;
      } else {
        assignStoreDimIdx = true;
        dimensions = dimensionsInput;
      }
      dimensions = dimensions || ["x", "y"];
      var dimensionInfos = {};
      var dimensionNames = [];
      var invertedIndicesMap = {};
      var needsHasOwn = false;
      var emptyObj = {};
      for (var i2 = 0; i2 < dimensions.length; i2++) {
        var dimInfoInput = dimensions[i2];
        var dimensionInfo = isString(dimInfoInput) ? new SeriesDimensionDefine_default({
          name: dimInfoInput
        }) : !(dimInfoInput instanceof SeriesDimensionDefine_default) ? new SeriesDimensionDefine_default(dimInfoInput) : dimInfoInput;
        var dimensionName = dimensionInfo.name;
        dimensionInfo.type = dimensionInfo.type || "float";
        if (!dimensionInfo.coordDim) {
          dimensionInfo.coordDim = dimensionName;
          dimensionInfo.coordDimIndex = 0;
        }
        var otherDims = dimensionInfo.otherDims = dimensionInfo.otherDims || {};
        dimensionNames.push(dimensionName);
        dimensionInfos[dimensionName] = dimensionInfo;
        if (emptyObj[dimensionName] != null) {
          needsHasOwn = true;
        }
        if (dimensionInfo.createInvertedIndices) {
          invertedIndicesMap[dimensionName] = [];
        }
        if (true) {
          assert(assignStoreDimIdx || dimensionInfo.storeDimIndex >= 0);
        }
        if (assignStoreDimIdx) {
          dimensionInfo.storeDimIndex = i2;
        }
        if (otherDims.itemName === 0) {
          this._nameDimIdx = dimensionInfo.storeDimIndex;
        }
        if (otherDims.itemId === 0) {
          this._idDimIdx = dimensionInfo.storeDimIndex;
        }
      }
      this.dimensions = dimensionNames;
      this._dimInfos = dimensionInfos;
      this._initGetDimensionInfo(needsHasOwn);
      this.hostModel = hostModel;
      this._invertedIndicesMap = invertedIndicesMap;
      if (this._dimOmitted) {
        var dimIdxToName_1 = this._dimIdxToName = createHashMap();
        each(dimensionNames, function(dimName) {
          dimIdxToName_1.set(dimensionInfos[dimName].storeDimIndex, dimName);
        });
      }
    }
    SeriesData2.prototype.getDimension = function(dim) {
      var dimIdx = this._recognizeDimIndex(dim);
      if (dimIdx == null) {
        return dim;
      }
      dimIdx = dim;
      if (!this._dimOmitted) {
        return this.dimensions[dimIdx];
      }
      var dimName = this._dimIdxToName.get(dimIdx);
      if (dimName != null) {
        return dimName;
      }
      var sourceDimDef = this._schema.getSourceDimension(dimIdx);
      if (sourceDimDef) {
        return sourceDimDef.name;
      }
    };
    SeriesData2.prototype.getDimensionIndex = function(dim) {
      var dimIdx = this._recognizeDimIndex(dim);
      if (dimIdx != null) {
        return dimIdx;
      }
      if (dim == null) {
        return -1;
      }
      var dimInfo = this._getDimInfo(dim);
      return dimInfo ? dimInfo.storeDimIndex : this._dimOmitted ? this._schema.getSourceDimensionIndex(dim) : -1;
    };
    SeriesData2.prototype._recognizeDimIndex = function(dim) {
      if (isNumber(dim) || dim != null && !isNaN(dim) && !this._getDimInfo(dim) && (!this._dimOmitted || this._schema.getSourceDimensionIndex(dim) < 0)) {
        return +dim;
      }
    };
    SeriesData2.prototype._getStoreDimIndex = function(dim) {
      var dimIdx = this.getDimensionIndex(dim);
      if (true) {
        if (dimIdx == null) {
          throw new Error("Unknown dimension " + dim);
        }
      }
      return dimIdx;
    };
    SeriesData2.prototype.getDimensionInfo = function(dim) {
      return this._getDimInfo(this.getDimension(dim));
    };
    SeriesData2.prototype._initGetDimensionInfo = function(needsHasOwn) {
      var dimensionInfos = this._dimInfos;
      this._getDimInfo = needsHasOwn ? function(dimName) {
        return dimensionInfos.hasOwnProperty(dimName) ? dimensionInfos[dimName] : void 0;
      } : function(dimName) {
        return dimensionInfos[dimName];
      };
    };
    SeriesData2.prototype.getDimensionsOnCoord = function() {
      return this._dimSummary.dataDimsOnCoord.slice();
    };
    SeriesData2.prototype.mapDimension = function(coordDim, idx) {
      var dimensionsSummary = this._dimSummary;
      if (idx == null) {
        return dimensionsSummary.encodeFirstDimNotExtra[coordDim];
      }
      var dims = dimensionsSummary.encode[coordDim];
      return dims ? dims[idx] : null;
    };
    SeriesData2.prototype.mapDimensionsAll = function(coordDim) {
      var dimensionsSummary = this._dimSummary;
      var dims = dimensionsSummary.encode[coordDim];
      return (dims || []).slice();
    };
    SeriesData2.prototype.getStore = function() {
      return this._store;
    };
    SeriesData2.prototype.initData = function(data, nameList, dimValueGetter) {
      var _this = this;
      var store;
      if (data instanceof DataStore_default) {
        store = data;
      }
      if (!store) {
        var dimensions = this.dimensions;
        var provider = isSourceInstance(data) || isArrayLike(data) ? new DefaultDataProvider(data, dimensions.length) : data;
        store = new DataStore_default();
        var dimensionInfos = map3(dimensions, function(dimName) {
          return {
            type: _this._dimInfos[dimName].type,
            property: dimName
          };
        });
        store.initData(provider, dimensionInfos, dimValueGetter);
      }
      this._store = store;
      this._nameList = (nameList || []).slice();
      this._idList = [];
      this._nameRepeatCount = {};
      this._doInit(0, store.count());
      this._dimSummary = summarizeDimensions(this, this._schema);
      this.userOutput = this._dimSummary.userOutput;
    };
    SeriesData2.prototype.appendData = function(data) {
      var range = this._store.appendData(data);
      this._doInit(range[0], range[1]);
    };
    SeriesData2.prototype.appendValues = function(values, names) {
      var _a2 = this._store.appendValues(values, names && names.length), start2 = _a2.start, end2 = _a2.end;
      var shouldMakeIdFromName = this._shouldMakeIdFromName();
      this._updateOrdinalMeta();
      if (names) {
        for (var idx = start2; idx < end2; idx++) {
          var sourceIdx = idx - start2;
          this._nameList[idx] = names[sourceIdx];
          if (shouldMakeIdFromName) {
            makeIdFromName(this, idx);
          }
        }
      }
    };
    SeriesData2.prototype._updateOrdinalMeta = function() {
      var store = this._store;
      var dimensions = this.dimensions;
      for (var i2 = 0; i2 < dimensions.length; i2++) {
        var dimInfo = this._dimInfos[dimensions[i2]];
        if (dimInfo.ordinalMeta) {
          store.collectOrdinalMeta(dimInfo.storeDimIndex, dimInfo.ordinalMeta);
        }
      }
    };
    SeriesData2.prototype._shouldMakeIdFromName = function() {
      var provider = this._store.getProvider();
      return this._idDimIdx == null && provider.getSource().sourceFormat !== SOURCE_FORMAT_TYPED_ARRAY && !provider.fillStorage;
    };
    SeriesData2.prototype._doInit = function(start2, end2) {
      if (start2 >= end2) {
        return;
      }
      var store = this._store;
      var provider = store.getProvider();
      this._updateOrdinalMeta();
      var nameList = this._nameList;
      var idList = this._idList;
      var sourceFormat = provider.getSource().sourceFormat;
      var isFormatOriginal = sourceFormat === SOURCE_FORMAT_ORIGINAL;
      if (isFormatOriginal && !provider.pure) {
        var sharedDataItem = [];
        for (var idx = start2; idx < end2; idx++) {
          var dataItem = provider.getItem(idx, sharedDataItem);
          if (!this.hasItemOption && isDataItemOption(dataItem)) {
            this.hasItemOption = true;
          }
          if (dataItem) {
            var itemName = dataItem.name;
            if (nameList[idx] == null && itemName != null) {
              nameList[idx] = convertOptionIdName(itemName, null);
            }
            var itemId = dataItem.id;
            if (idList[idx] == null && itemId != null) {
              idList[idx] = convertOptionIdName(itemId, null);
            }
          }
        }
      }
      if (this._shouldMakeIdFromName()) {
        for (var idx = start2; idx < end2; idx++) {
          makeIdFromName(this, idx);
        }
      }
      prepareInvertedIndex(this);
    };
    SeriesData2.prototype.getApproximateExtent = function(dim, filter3) {
      return this._approximateExtent[dim] || this._store.getDataExtent(this._getStoreDimIndex(dim), filter3);
    };
    SeriesData2.prototype.setApproximateExtent = function(extent, dim) {
      dim = this.getDimension(dim);
      this._approximateExtent[dim] = extent.slice();
    };
    SeriesData2.prototype.getCalculationInfo = function(key2) {
      return this._calculationInfo[key2];
    };
    SeriesData2.prototype.setCalculationInfo = function(key2, value) {
      isObject2(key2) ? extend(this._calculationInfo, key2) : this._calculationInfo[key2] = value;
    };
    SeriesData2.prototype.getName = function(idx) {
      var rawIndex = this.getRawIndex(idx);
      var name = this._nameList[rawIndex];
      if (name == null && this._nameDimIdx != null) {
        name = getIdNameFromStore(this, this._nameDimIdx, rawIndex);
      }
      if (name == null) {
        name = "";
      }
      return name;
    };
    SeriesData2.prototype._getCategory = function(dimIdx, idx) {
      var ordinal = this._store.get(dimIdx, idx);
      var ordinalMeta = this._store.getOrdinalMeta(dimIdx);
      if (ordinalMeta) {
        return ordinalMeta.categories[ordinal];
      }
      return ordinal;
    };
    SeriesData2.prototype.getId = function(idx) {
      return getId(this, this.getRawIndex(idx));
    };
    SeriesData2.prototype.count = function() {
      return this._store.count();
    };
    SeriesData2.prototype.get = function(dim, idx) {
      var store = this._store;
      var dimInfo = this._dimInfos[dim];
      if (dimInfo) {
        return store.get(dimInfo.storeDimIndex, idx);
      }
    };
    SeriesData2.prototype.getByRawIndex = function(dim, rawIdx) {
      var store = this._store;
      var dimInfo = this._dimInfos[dim];
      if (dimInfo) {
        return store.getByRawIndex(dimInfo.storeDimIndex, rawIdx);
      }
    };
    SeriesData2.prototype.getIndices = function() {
      return this._store.getIndices();
    };
    SeriesData2.prototype.getDataExtent = function(dim) {
      return this._store.getDataExtent(this._getStoreDimIndex(dim), null);
    };
    SeriesData2.prototype.getSum = function(dim) {
      return this._store.getSum(this._getStoreDimIndex(dim));
    };
    SeriesData2.prototype.getMedian = function(dim) {
      return this._store.getMedian(this._getStoreDimIndex(dim));
    };
    SeriesData2.prototype.getValues = function(dimensions, idx) {
      var _this = this;
      var store = this._store;
      return isArray(dimensions) ? store.getValues(map3(dimensions, function(dim) {
        return _this._getStoreDimIndex(dim);
      }), idx) : store.getValues(dimensions);
    };
    SeriesData2.prototype.hasValue = function(idx) {
      var dataDimIndicesOnCoord = this._dimSummary.dataDimIndicesOnCoord;
      for (var i2 = 0, len2 = dataDimIndicesOnCoord.length; i2 < len2; i2++) {
        if (isNaN(this._store.get(dataDimIndicesOnCoord[i2], idx))) {
          return false;
        }
      }
      return true;
    };
    SeriesData2.prototype.indexOfName = function(name) {
      for (var i2 = 0, len2 = this._store.count(); i2 < len2; i2++) {
        if (this.getName(i2) === name) {
          return i2;
        }
      }
      return -1;
    };
    SeriesData2.prototype.getRawIndex = function(idx) {
      return this._store.getRawIndex(idx);
    };
    SeriesData2.prototype.indexOfRawIndex = function(rawIndex) {
      return this._store.indexOfRawIndex(rawIndex);
    };
    SeriesData2.prototype.rawIndexOf = function(dim, value) {
      var invertedIndices = dim && this._invertedIndicesMap[dim];
      if (true) {
        if (!invertedIndices) {
          throw new Error("Do not supported yet");
        }
      }
      var rawIndex = invertedIndices && invertedIndices[value];
      if (rawIndex == null || isNaN(rawIndex)) {
        return INDEX_NOT_FOUND;
      }
      return rawIndex;
    };
    SeriesData2.prototype.each = function(dims, cb, ctx) {
      "use strict";
      if (isFunction(dims)) {
        ctx = cb;
        cb = dims;
        dims = [];
      }
      var fCtx = ctx || this;
      var dimIndices = map3(normalizeDimensions(dims), this._getStoreDimIndex, this);
      this._store.each(dimIndices, fCtx ? bind(cb, fCtx) : cb);
    };
    SeriesData2.prototype.filterSelf = function(dims, cb, ctx) {
      "use strict";
      if (isFunction(dims)) {
        ctx = cb;
        cb = dims;
        dims = [];
      }
      var fCtx = ctx || this;
      var dimIndices = map3(normalizeDimensions(dims), this._getStoreDimIndex, this);
      this._store = this._store.filter(dimIndices, fCtx ? bind(cb, fCtx) : cb);
      return this;
    };
    SeriesData2.prototype.selectRange = function(range) {
      "use strict";
      var _this = this;
      var innerRange = {};
      var dims = keys(range);
      var dimIndices = [];
      each(dims, function(dim) {
        var dimIdx = _this._getStoreDimIndex(dim);
        innerRange[dimIdx] = range[dim];
        dimIndices.push(dimIdx);
      });
      this._store = this._store.selectRange(innerRange);
      return this;
    };
    SeriesData2.prototype.mapArray = function(dims, cb, ctx) {
      "use strict";
      if (isFunction(dims)) {
        ctx = cb;
        cb = dims;
        dims = [];
      }
      ctx = ctx || this;
      var result = [];
      this.each(dims, function() {
        result.push(cb && cb.apply(this, arguments));
      }, ctx);
      return result;
    };
    SeriesData2.prototype.map = function(dims, cb, ctx, ctxCompat) {
      "use strict";
      var fCtx = ctx || ctxCompat || this;
      var dimIndices = map3(normalizeDimensions(dims), this._getStoreDimIndex, this);
      var list = cloneListForMapAndSample(this);
      list._store = this._store.map(dimIndices, fCtx ? bind(cb, fCtx) : cb);
      return list;
    };
    SeriesData2.prototype.modify = function(dims, cb, ctx, ctxCompat) {
      var _this = this;
      var fCtx = ctx || ctxCompat || this;
      if (true) {
        each(normalizeDimensions(dims), function(dim) {
          var dimInfo = _this.getDimensionInfo(dim);
          if (!dimInfo.isCalculationCoord) {
            console.error("Danger: only stack dimension can be modified");
          }
        });
      }
      var dimIndices = map3(normalizeDimensions(dims), this._getStoreDimIndex, this);
      this._store.modify(dimIndices, fCtx ? bind(cb, fCtx) : cb);
    };
    SeriesData2.prototype.downSample = function(dimension, rate, sampleValue, sampleIndex) {
      var list = cloneListForMapAndSample(this);
      list._store = this._store.downSample(this._getStoreDimIndex(dimension), rate, sampleValue, sampleIndex);
      return list;
    };
    SeriesData2.prototype.minmaxDownSample = function(valueDimension, rate) {
      var list = cloneListForMapAndSample(this);
      list._store = this._store.minmaxDownSample(this._getStoreDimIndex(valueDimension), rate);
      return list;
    };
    SeriesData2.prototype.lttbDownSample = function(valueDimension, rate) {
      var list = cloneListForMapAndSample(this);
      list._store = this._store.lttbDownSample(this._getStoreDimIndex(valueDimension), rate);
      return list;
    };
    SeriesData2.prototype.getRawDataItem = function(idx) {
      return this._store.getRawDataItem(idx);
    };
    SeriesData2.prototype.getItemModel = function(idx) {
      var hostModel = this.hostModel;
      var dataItem = this.getRawDataItem(idx);
      return new Model_default(dataItem, hostModel, hostModel && hostModel.ecModel);
    };
    SeriesData2.prototype.diff = function(otherList) {
      var thisList = this;
      return new DataDiffer_default(otherList ? otherList.getStore().getIndices() : [], this.getStore().getIndices(), function(idx) {
        return getId(otherList, idx);
      }, function(idx) {
        return getId(thisList, idx);
      });
    };
    SeriesData2.prototype.getVisual = function(key2) {
      var visual = this._visual;
      return visual && visual[key2];
    };
    SeriesData2.prototype.setVisual = function(kvObj, val) {
      this._visual = this._visual || {};
      if (isObject2(kvObj)) {
        extend(this._visual, kvObj);
      } else {
        this._visual[kvObj] = val;
      }
    };
    SeriesData2.prototype.getItemVisual = function(idx, key2) {
      var itemVisual = this._itemVisuals[idx];
      var val = itemVisual && itemVisual[key2];
      if (val == null) {
        return this.getVisual(key2);
      }
      return val;
    };
    SeriesData2.prototype.hasItemVisual = function() {
      return this._itemVisuals.length > 0;
    };
    SeriesData2.prototype.ensureUniqueItemVisual = function(idx, key2) {
      var itemVisuals = this._itemVisuals;
      var itemVisual = itemVisuals[idx];
      if (!itemVisual) {
        itemVisual = itemVisuals[idx] = {};
      }
      var val = itemVisual[key2];
      if (val == null) {
        val = this.getVisual(key2);
        if (isArray(val)) {
          val = val.slice();
        } else if (isObject2(val)) {
          val = extend({}, val);
        }
        itemVisual[key2] = val;
      }
      return val;
    };
    SeriesData2.prototype.setItemVisual = function(idx, key2, value) {
      var itemVisual = this._itemVisuals[idx] || {};
      this._itemVisuals[idx] = itemVisual;
      if (isObject2(key2)) {
        extend(itemVisual, key2);
      } else {
        itemVisual[key2] = value;
      }
    };
    SeriesData2.prototype.clearAllVisual = function() {
      this._visual = {};
      this._itemVisuals = [];
    };
    SeriesData2.prototype.setLayout = function(key2, val) {
      isObject2(key2) ? extend(this._layout, key2) : this._layout[key2] = val;
    };
    SeriesData2.prototype.getLayout = function(key2) {
      return this._layout[key2];
    };
    SeriesData2.prototype.getItemLayout = function(idx) {
      return this._itemLayouts[idx];
    };
    SeriesData2.prototype.setItemLayout = function(idx, layout3, merge3) {
      this._itemLayouts[idx] = merge3 ? extend(this._itemLayouts[idx] || {}, layout3) : layout3;
    };
    SeriesData2.prototype.clearItemLayouts = function() {
      this._itemLayouts.length = 0;
    };
    SeriesData2.prototype.setItemGraphicEl = function(idx, el) {
      var seriesIndex = this.hostModel && this.hostModel.seriesIndex;
      setCommonECData(seriesIndex, this.dataType, idx, el);
      this._graphicEls[idx] = el;
    };
    SeriesData2.prototype.getItemGraphicEl = function(idx) {
      return this._graphicEls[idx];
    };
    SeriesData2.prototype.eachItemGraphicEl = function(cb, context) {
      each(this._graphicEls, function(el, idx) {
        if (el) {
          cb && cb.call(context, el, idx);
        }
      });
    };
    SeriesData2.prototype.cloneShallow = function(list) {
      if (!list) {
        list = new SeriesData2(this._schema ? this._schema : map3(this.dimensions, this._getDimInfo, this), this.hostModel);
      }
      transferProperties(list, this);
      list._store = this._store;
      return list;
    };
    SeriesData2.prototype.wrapMethod = function(methodName, injectFunction) {
      var originalMethod = this[methodName];
      if (!isFunction(originalMethod)) {
        return;
      }
      this.__wrappedMethods = this.__wrappedMethods || [];
      this.__wrappedMethods.push(methodName);
      this[methodName] = function() {
        var res = originalMethod.apply(this, arguments);
        return injectFunction.apply(this, [res].concat(slice(arguments)));
      };
    };
    SeriesData2.internalField = (function() {
      prepareInvertedIndex = function(data) {
        var invertedIndicesMap = data._invertedIndicesMap;
        each(invertedIndicesMap, function(invertedIndices, dim) {
          var dimInfo = data._dimInfos[dim];
          var ordinalMeta = dimInfo.ordinalMeta;
          var store = data._store;
          if (ordinalMeta) {
            invertedIndices = invertedIndicesMap[dim] = new CtorInt32Array2(ordinalMeta.categories.length);
            for (var i2 = 0; i2 < invertedIndices.length; i2++) {
              invertedIndices[i2] = INDEX_NOT_FOUND;
            }
            for (var i2 = 0; i2 < store.count(); i2++) {
              invertedIndices[store.get(dimInfo.storeDimIndex, i2)] = i2;
            }
          }
        });
      };
      getIdNameFromStore = function(data, dimIdx, idx) {
        return convertOptionIdName(data._getCategory(dimIdx, idx), null);
      };
      getId = function(data, rawIndex) {
        var id = data._idList[rawIndex];
        if (id == null && data._idDimIdx != null) {
          id = getIdNameFromStore(data, data._idDimIdx, rawIndex);
        }
        if (id == null) {
          id = ID_PREFIX + rawIndex;
        }
        return id;
      };
      normalizeDimensions = function(dimensions) {
        if (!isArray(dimensions)) {
          dimensions = dimensions != null ? [dimensions] : [];
        }
        return dimensions;
      };
      cloneListForMapAndSample = function(original) {
        var list = new SeriesData2(original._schema ? original._schema : map3(original.dimensions, original._getDimInfo, original), original.hostModel);
        transferProperties(list, original);
        return list;
      };
      transferProperties = function(target, source) {
        each(TRANSFERABLE_PROPERTIES.concat(source.__wrappedMethods || []), function(propName) {
          if (source.hasOwnProperty(propName)) {
            target[propName] = source[propName];
          }
        });
        target.__wrappedMethods = source.__wrappedMethods;
        each(CLONE_PROPERTIES, function(propName) {
          target[propName] = clone(source[propName]);
        });
        target._calculationInfo = extend({}, source._calculationInfo);
      };
      makeIdFromName = function(data, idx) {
        var nameList = data._nameList;
        var idList = data._idList;
        var nameDimIdx = data._nameDimIdx;
        var idDimIdx = data._idDimIdx;
        var name = nameList[idx];
        var id = idList[idx];
        if (name == null && nameDimIdx != null) {
          nameList[idx] = name = getIdNameFromStore(data, nameDimIdx, idx);
        }
        if (id == null && idDimIdx != null) {
          idList[idx] = id = getIdNameFromStore(data, idDimIdx, idx);
        }
        if (id == null && name != null) {
          var nameRepeatCount = data._nameRepeatCount;
          var nmCnt = nameRepeatCount[name] = (nameRepeatCount[name] || 0) + 1;
          id = name;
          if (nmCnt > 1) {
            id += "__ec__" + nmCnt;
          }
          idList[idx] = id;
        }
      };
    })();
    return SeriesData2;
  })()
);
var SeriesData_default = SeriesData;

// node_modules/echarts/lib/data/helper/createDimensions.js
function prepareSeriesDataSchema(source, opt) {
  if (!isSourceInstance(source)) {
    source = createSourceFromSeriesDataOption(source);
  }
  opt = opt || {};
  var sysDims = opt.coordDimensions || [];
  var dimsDef = opt.dimensionsDefine || source.dimensionsDefine || [];
  var coordDimNameMap = createHashMap();
  var resultList = [];
  var dimCount = getDimCount(source, sysDims, dimsDef, opt.dimensionsCount);
  var omitUnusedDimensions = opt.canOmitUnusedDimensions && shouldOmitUnusedDimensions(dimCount);
  var isUsingSourceDimensionsDef = dimsDef === source.dimensionsDefine;
  var dataDimNameMap = isUsingSourceDimensionsDef ? ensureSourceDimNameMap(source) : createDimNameMap(dimsDef);
  var encodeDef = opt.encodeDefine;
  if (!encodeDef && opt.encodeDefaulter) {
    encodeDef = opt.encodeDefaulter(source, dimCount);
  }
  var encodeDefMap = createHashMap(encodeDef);
  var indicesMap = new CtorInt32Array(dimCount);
  for (var i2 = 0; i2 < indicesMap.length; i2++) {
    indicesMap[i2] = -1;
  }
  function getResultItem(dimIdx) {
    var idx = indicesMap[dimIdx];
    if (idx < 0) {
      var dimDefItemRaw = dimsDef[dimIdx];
      var dimDefItem = isObject(dimDefItemRaw) ? dimDefItemRaw : {
        name: dimDefItemRaw
      };
      var resultItem2 = new SeriesDimensionDefine_default();
      var userDimName = dimDefItem.name;
      if (userDimName != null && dataDimNameMap.get(userDimName) != null) {
        resultItem2.name = resultItem2.displayName = userDimName;
      }
      dimDefItem.type != null && (resultItem2.type = dimDefItem.type);
      dimDefItem.displayName != null && (resultItem2.displayName = dimDefItem.displayName);
      var newIdx = resultList.length;
      indicesMap[dimIdx] = newIdx;
      resultItem2.storeDimIndex = dimIdx;
      resultList.push(resultItem2);
      return resultItem2;
    }
    return resultList[idx];
  }
  if (!omitUnusedDimensions) {
    for (var i2 = 0; i2 < dimCount; i2++) {
      getResultItem(i2);
    }
  }
  encodeDefMap.each(function(dataDimsRaw, coordDim2) {
    var dataDims = normalizeToArray(dataDimsRaw).slice();
    if (dataDims.length === 1 && !isString(dataDims[0]) && dataDims[0] < 0) {
      encodeDefMap.set(coordDim2, false);
      return;
    }
    var validDataDims = encodeDefMap.set(coordDim2, []);
    each(dataDims, function(resultDimIdxOrName, idx) {
      var resultDimIdx2 = isString(resultDimIdxOrName) ? dataDimNameMap.get(resultDimIdxOrName) : resultDimIdxOrName;
      if (resultDimIdx2 != null && resultDimIdx2 < dimCount) {
        validDataDims[idx] = resultDimIdx2;
        applyDim(getResultItem(resultDimIdx2), coordDim2, idx);
      }
    });
  });
  var availDimIdx = 0;
  each(sysDims, function(sysDimItemRaw) {
    var coordDim2;
    var sysDimItemDimsDef;
    var sysDimItemOtherDims;
    var sysDimItem;
    if (isString(sysDimItemRaw)) {
      coordDim2 = sysDimItemRaw;
      sysDimItem = {};
    } else {
      sysDimItem = sysDimItemRaw;
      coordDim2 = sysDimItem.name;
      var ordinalMeta = sysDimItem.ordinalMeta;
      sysDimItem.ordinalMeta = null;
      sysDimItem = extend({}, sysDimItem);
      sysDimItem.ordinalMeta = ordinalMeta;
      sysDimItemDimsDef = sysDimItem.dimsDef;
      sysDimItemOtherDims = sysDimItem.otherDims;
      sysDimItem.name = sysDimItem.coordDim = sysDimItem.coordDimIndex = sysDimItem.dimsDef = sysDimItem.otherDims = null;
    }
    var dataDims = encodeDefMap.get(coordDim2);
    if (dataDims === false) {
      return;
    }
    dataDims = normalizeToArray(dataDims);
    if (!dataDims.length) {
      for (var i3 = 0; i3 < (sysDimItemDimsDef && sysDimItemDimsDef.length || 1); i3++) {
        while (availDimIdx < dimCount && getResultItem(availDimIdx).coordDim != null) {
          availDimIdx++;
        }
        availDimIdx < dimCount && dataDims.push(availDimIdx++);
      }
    }
    each(dataDims, function(resultDimIdx2, coordDimIndex) {
      var resultItem2 = getResultItem(resultDimIdx2);
      if (isUsingSourceDimensionsDef && sysDimItem.type != null) {
        resultItem2.type = sysDimItem.type;
      }
      applyDim(defaults(resultItem2, sysDimItem), coordDim2, coordDimIndex);
      if (resultItem2.name == null && sysDimItemDimsDef) {
        var sysDimItemDimsDefItem = sysDimItemDimsDef[coordDimIndex];
        !isObject(sysDimItemDimsDefItem) && (sysDimItemDimsDefItem = {
          name: sysDimItemDimsDefItem
        });
        resultItem2.name = resultItem2.displayName = sysDimItemDimsDefItem.name;
        resultItem2.defaultTooltip = sysDimItemDimsDefItem.defaultTooltip;
      }
      sysDimItemOtherDims && defaults(resultItem2.otherDims, sysDimItemOtherDims);
    });
  });
  function applyDim(resultItem2, coordDim2, coordDimIndex) {
    if (VISUAL_DIMENSIONS.get(coordDim2) != null) {
      resultItem2.otherDims[coordDim2] = coordDimIndex;
    } else {
      resultItem2.coordDim = coordDim2;
      resultItem2.coordDimIndex = coordDimIndex;
      coordDimNameMap.set(coordDim2, true);
    }
  }
  var generateCoord = opt.generateCoord;
  var generateCoordCount = opt.generateCoordCount;
  var fromZero = generateCoordCount != null;
  generateCoordCount = generateCoord ? generateCoordCount || 1 : 0;
  var extra = generateCoord || "value";
  function ifNoNameFillWithCoordName(resultItem2) {
    if (resultItem2.name == null) {
      resultItem2.name = resultItem2.coordDim;
    }
  }
  if (!omitUnusedDimensions) {
    for (var resultDimIdx = 0; resultDimIdx < dimCount; resultDimIdx++) {
      var resultItem = getResultItem(resultDimIdx);
      var coordDim = resultItem.coordDim;
      if (coordDim == null) {
        resultItem.coordDim = genCoordDimName(extra, coordDimNameMap, fromZero);
        resultItem.coordDimIndex = 0;
        if (!generateCoord || generateCoordCount <= 0) {
          resultItem.isExtraCoord = true;
        }
        generateCoordCount--;
      }
      ifNoNameFillWithCoordName(resultItem);
      if (resultItem.type == null && (guessOrdinal(source, resultDimIdx) === BE_ORDINAL.Must || resultItem.isExtraCoord && (resultItem.otherDims.itemName != null || resultItem.otherDims.seriesName != null))) {
        resultItem.type = "ordinal";
      }
    }
  } else {
    each(resultList, function(resultItem2) {
      ifNoNameFillWithCoordName(resultItem2);
    });
    resultList.sort(function(item0, item1) {
      return item0.storeDimIndex - item1.storeDimIndex;
    });
  }
  removeDuplicates(resultList, function(item) {
    return item.name;
  }, function(item, existingCount) {
    if (existingCount > 0) {
      item.name = item.name + (existingCount - 1);
    }
  });
  return new SeriesDataSchema({
    source,
    dimensions: resultList,
    fullDimensionCount: dimCount,
    dimensionOmitted: omitUnusedDimensions
  });
}
function getDimCount(source, sysDims, dimsDef, optDimCount) {
  var dimCount = Math.max(source.dimensionsDetectedCount || 1, sysDims.length, dimsDef.length, optDimCount || 0);
  each(sysDims, function(sysDimItem) {
    var sysDimItemDimsDef;
    if (isObject(sysDimItem) && (sysDimItemDimsDef = sysDimItem.dimsDef)) {
      dimCount = Math.max(dimCount, sysDimItemDimsDef.length);
    }
  });
  return dimCount;
}
function genCoordDimName(name, map4, fromZero) {
  if (fromZero || map4.hasKey(name)) {
    var i2 = 0;
    while (map4.hasKey(name + i2)) {
      i2++;
    }
    name += i2;
  }
  map4.set(name, true);
  return name;
}

// node_modules/echarts/lib/core/CoordinateSystem.js
var nonSeriesBoxCoordSysCreators = {};
var normalCoordSysCreators = {};
var CoordinateSystemManager = (
  /** @class */
  (function() {
    function CoordinateSystemManager2() {
      this._normalMasterList = [];
      this._nonSeriesBoxMasterList = [];
    }
    CoordinateSystemManager2.prototype.create = function(ecModel, api) {
      this._nonSeriesBoxMasterList = dealCreate(nonSeriesBoxCoordSysCreators, true);
      this._normalMasterList = dealCreate(normalCoordSysCreators, false);
      function dealCreate(creatorMap, canBeNonSeriesBox) {
        var coordinateSystems = [];
        each(creatorMap, function(creator, type) {
          var list = creator.create(ecModel, api);
          coordinateSystems = coordinateSystems.concat(list || []);
          if (true) {
            if (canBeNonSeriesBox) {
              each(list, function(master) {
                return assert(!master.update);
              });
            }
          }
        });
        return coordinateSystems;
      }
    };
    CoordinateSystemManager2.prototype.update = function(ecModel, api) {
      each(this._normalMasterList, function(coordSys) {
        coordSys.update && coordSys.update(ecModel, api);
      });
    };
    CoordinateSystemManager2.prototype.getCoordinateSystems = function() {
      return this._normalMasterList.concat(this._nonSeriesBoxMasterList);
    };
    CoordinateSystemManager2.register = function(type, creator) {
      if (type === "matrix" || type === "calendar") {
        nonSeriesBoxCoordSysCreators[type] = creator;
        return;
      }
      normalCoordSysCreators[type] = creator;
    };
    CoordinateSystemManager2.get = function(type) {
      return normalCoordSysCreators[type] || nonSeriesBoxCoordSysCreators[type];
    };
    return CoordinateSystemManager2;
  })()
);
function canBeNonSeriesBoxCoordSys(coordSysType) {
  return !!nonSeriesBoxCoordSysCreators[coordSysType];
}
var BOX_COORD_SYS_COORD_FROM_PROP_COORD = 1;
var BOX_COORD_SYS_COORD_FROM_PROP_COORD2 = 2;
function registerLayOutOnCoordSysUsage(opt) {
  if (true) {
    assert(!coordSysUseMap.get(opt.fullType));
  }
  coordSysUseMap.set(opt.fullType, {
    getCoord2: void 0
  }).getCoord2 = opt.getCoord2;
}
var coordSysUseMap = createHashMap();
function getCoordForCoordSysUsageKindBox(model2) {
  var coord = model2.getShallow("coord", true);
  var from = BOX_COORD_SYS_COORD_FROM_PROP_COORD;
  if (coord == null) {
    var store = coordSysUseMap.get(model2.type);
    if (store && store.getCoord2) {
      from = BOX_COORD_SYS_COORD_FROM_PROP_COORD2;
      coord = store.getCoord2(model2);
    }
  }
  return {
    coord,
    from
  };
}
var COORD_SYS_USAGE_KIND_NONE = 0;
var COORD_SYS_USAGE_KIND_DATA = 1;
var COORD_SYS_USAGE_KIND_BOX = 2;
function decideCoordSysUsageKind(model2, printError) {
  var coordSysType = model2.getShallow("coordinateSystem");
  var coordSysUsageOption = model2.getShallow("coordinateSystemUsage", true);
  var isDeclaredExplicitly = coordSysUsageOption != null;
  var kind = COORD_SYS_USAGE_KIND_NONE;
  if (coordSysType) {
    var isSeries2 = model2.mainType === "series";
    if (coordSysUsageOption == null) {
      coordSysUsageOption = isSeries2 ? "data" : "box";
    }
    if (coordSysUsageOption === "data") {
      kind = COORD_SYS_USAGE_KIND_DATA;
      if (!isSeries2) {
        if (true) {
          if (isDeclaredExplicitly && printError) {
            error('coordinateSystemUsage "data" is not supported in non-series components.');
          }
        }
        kind = COORD_SYS_USAGE_KIND_NONE;
      }
    } else if (coordSysUsageOption === "box") {
      kind = COORD_SYS_USAGE_KIND_BOX;
      if (!isSeries2 && !canBeNonSeriesBoxCoordSys(coordSysType)) {
        if (true) {
          if (isDeclaredExplicitly && printError) {
            error('coordinateSystem "' + coordSysType + '" cannot be used' + (' as coordinateSystemUsage "box" for "' + model2.type + '" yet.'));
          }
        }
        kind = COORD_SYS_USAGE_KIND_NONE;
      }
    }
  }
  return {
    coordSysType,
    kind
  };
}
function injectCoordSysByOption(opt) {
  var targetModel = opt.targetModel, coordSysType = opt.coordSysType, coordSysProvider = opt.coordSysProvider, isDefaultDataCoordSys = opt.isDefaultDataCoordSys, allowNotFound = opt.allowNotFound;
  if (true) {
    assert(!!coordSysType);
  }
  var _a2 = decideCoordSysUsageKind(targetModel, true), kind = _a2.kind, declaredType = _a2.coordSysType;
  if (isDefaultDataCoordSys && kind !== COORD_SYS_USAGE_KIND_DATA) {
    kind = COORD_SYS_USAGE_KIND_DATA;
    declaredType = coordSysType;
  }
  if (kind === COORD_SYS_USAGE_KIND_NONE || declaredType !== coordSysType) {
    return COORD_SYS_USAGE_KIND_NONE;
  }
  var coordSys = coordSysProvider(coordSysType, targetModel);
  if (!coordSys) {
    if (true) {
      if (!allowNotFound) {
        error(coordSysType + " cannot be found for" + (" " + targetModel.type + " (index: " + targetModel.componentIndex + ")."));
      }
    }
    return COORD_SYS_USAGE_KIND_NONE;
  }
  if (kind === COORD_SYS_USAGE_KIND_DATA) {
    if (true) {
      assert(targetModel.mainType === "series");
    }
    targetModel.coordinateSystem = coordSys;
  } else {
    targetModel.boxCoordinateSystem = coordSys;
  }
  return kind;
}
var simpleCoordSysInjectionProvider = function(coordSysType, injectTargetModel) {
  var coordSysModel = injectTargetModel.getReferringComponents(coordSysType, SINGLE_REFERRING).models[0];
  return coordSysModel && coordSysModel.coordinateSystem;
};
var CoordinateSystem_default = CoordinateSystemManager;

// node_modules/echarts/lib/model/referHelper.js
var SeriesModelCoordSysInfo = (
  /** @class */
  /* @__PURE__ */ (function() {
    function SeriesModelCoordSysInfo2(coordSysName) {
      this.coordSysDims = [];
      this.axisMap = createHashMap();
      this.categoryAxisMap = createHashMap();
      this.coordSysName = coordSysName;
    }
    return SeriesModelCoordSysInfo2;
  })()
);
function getCoordSysInfoBySeries(seriesModel) {
  var coordSysName = seriesModel.get("coordinateSystem");
  var result = new SeriesModelCoordSysInfo(coordSysName);
  var fetch = fetchers[coordSysName];
  if (fetch) {
    fetch(seriesModel, result, result.axisMap, result.categoryAxisMap);
    return result;
  }
}
var fetchers = {
  cartesian2d: function(seriesModel, result, axisMap, categoryAxisMap) {
    var xAxisModel = seriesModel.getReferringComponents("xAxis", SINGLE_REFERRING).models[0];
    var yAxisModel = seriesModel.getReferringComponents("yAxis", SINGLE_REFERRING).models[0];
    if (true) {
      if (!xAxisModel) {
        throw new Error('xAxis "' + retrieve(seriesModel.get("xAxisIndex"), seriesModel.get("xAxisId"), 0) + '" not found');
      }
      if (!yAxisModel) {
        throw new Error('yAxis "' + retrieve(seriesModel.get("xAxisIndex"), seriesModel.get("yAxisId"), 0) + '" not found');
      }
    }
    result.coordSysDims = ["x", "y"];
    axisMap.set("x", xAxisModel);
    axisMap.set("y", yAxisModel);
    if (isCategory(xAxisModel)) {
      categoryAxisMap.set("x", xAxisModel);
      result.firstCategoryDimIndex = 0;
    }
    if (isCategory(yAxisModel)) {
      categoryAxisMap.set("y", yAxisModel);
      result.firstCategoryDimIndex == null && (result.firstCategoryDimIndex = 1);
    }
  },
  singleAxis: function(seriesModel, result, axisMap, categoryAxisMap) {
    var singleAxisModel = seriesModel.getReferringComponents("singleAxis", SINGLE_REFERRING).models[0];
    if (true) {
      if (!singleAxisModel) {
        throw new Error("singleAxis should be specified.");
      }
    }
    result.coordSysDims = ["single"];
    axisMap.set("single", singleAxisModel);
    if (isCategory(singleAxisModel)) {
      categoryAxisMap.set("single", singleAxisModel);
      result.firstCategoryDimIndex = 0;
    }
  },
  polar: function(seriesModel, result, axisMap, categoryAxisMap) {
    var polarModel = seriesModel.getReferringComponents("polar", SINGLE_REFERRING).models[0];
    var radiusAxisModel = polarModel.findAxisModel("radiusAxis");
    var angleAxisModel = polarModel.findAxisModel("angleAxis");
    if (true) {
      if (!angleAxisModel) {
        throw new Error("angleAxis option not found");
      }
      if (!radiusAxisModel) {
        throw new Error("radiusAxis option not found");
      }
    }
    result.coordSysDims = ["radius", "angle"];
    axisMap.set("radius", radiusAxisModel);
    axisMap.set("angle", angleAxisModel);
    if (isCategory(radiusAxisModel)) {
      categoryAxisMap.set("radius", radiusAxisModel);
      result.firstCategoryDimIndex = 0;
    }
    if (isCategory(angleAxisModel)) {
      categoryAxisMap.set("angle", angleAxisModel);
      result.firstCategoryDimIndex == null && (result.firstCategoryDimIndex = 1);
    }
  },
  geo: function(seriesModel, result, axisMap, categoryAxisMap) {
    result.coordSysDims = ["lng", "lat"];
  },
  parallel: function(seriesModel, result, axisMap, categoryAxisMap) {
    var ecModel = seriesModel.ecModel;
    var parallelModel = ecModel.getComponent("parallel", seriesModel.get("parallelIndex"));
    var coordSysDims = result.coordSysDims = parallelModel.dimensions.slice();
    each(parallelModel.parallelAxisIndex, function(axisIndex, index) {
      var axisModel = ecModel.getComponent("parallelAxis", axisIndex);
      var axisDim = coordSysDims[index];
      axisMap.set(axisDim, axisModel);
      if (isCategory(axisModel)) {
        categoryAxisMap.set(axisDim, axisModel);
        if (result.firstCategoryDimIndex == null) {
          result.firstCategoryDimIndex = index;
        }
      }
    });
  },
  matrix: function(seriesModel, result, axisMap, categoryAxisMap) {
    var matrixModel = seriesModel.getReferringComponents("matrix", SINGLE_REFERRING).models[0];
    if (true) {
      if (!matrixModel) {
        throw new Error("matrix coordinate system should be specified.");
      }
    }
    result.coordSysDims = ["x", "y"];
    var xModel = matrixModel.getDimensionModel("x");
    var yModel = matrixModel.getDimensionModel("y");
    axisMap.set("x", xModel);
    axisMap.set("y", yModel);
    categoryAxisMap.set("x", xModel);
    categoryAxisMap.set("y", yModel);
  }
};
function isCategory(axisModel) {
  return axisModel.get("type") === "category";
}

// node_modules/echarts/lib/data/helper/dataStackHelper.js
function enableDataStack(seriesModel, dimensionsInput, opt) {
  opt = opt || {};
  var byIndex = opt.byIndex;
  var stackedCoordDimension = opt.stackedCoordDimension;
  var dimensionDefineList;
  var schema;
  var store;
  if (isLegacyDimensionsInput(dimensionsInput)) {
    dimensionDefineList = dimensionsInput;
  } else {
    schema = dimensionsInput.schema;
    dimensionDefineList = schema.dimensions;
    store = dimensionsInput.store;
  }
  var mayStack = !!(seriesModel && seriesModel.get("stack"));
  var stackedByDimInfo;
  var stackedDimInfo;
  var stackResultDimension;
  var stackedOverDimension;
  var allDimTypesAreNotOrdinalAndTime = true;
  function dimTypeIsNotOrdinalAndTime(dimensionInfo) {
    return dimensionInfo.type !== "ordinal" && dimensionInfo.type !== "time";
  }
  each(dimensionDefineList, function(dimensionInfo, index) {
    if (isString(dimensionInfo)) {
      dimensionDefineList[index] = dimensionInfo = {
        name: dimensionInfo
      };
    }
    if (!dimTypeIsNotOrdinalAndTime(dimensionInfo)) {
      allDimTypesAreNotOrdinalAndTime = false;
    }
  });
  each(dimensionDefineList, function(dimensionInfo, index) {
    if (mayStack && !dimensionInfo.isExtraCoord) {
      if (!byIndex && !stackedByDimInfo && dimensionInfo.ordinalMeta) {
        stackedByDimInfo = dimensionInfo;
      }
      if (!stackedDimInfo && dimTypeIsNotOrdinalAndTime(dimensionInfo) && (!allDimTypesAreNotOrdinalAndTime || dimensionInfo.coordDim !== "x" && dimensionInfo.coordDim !== "angle") && (!stackedCoordDimension || stackedCoordDimension === dimensionInfo.coordDim)) {
        stackedDimInfo = dimensionInfo;
      }
    }
  });
  if (stackedDimInfo && !byIndex && !stackedByDimInfo) {
    byIndex = true;
  }
  if (stackedDimInfo) {
    stackResultDimension = "__\0ecstackresult_" + seriesModel.id;
    stackedOverDimension = "__\0ecstackedover_" + seriesModel.id;
    if (stackedByDimInfo) {
      stackedByDimInfo.createInvertedIndices = true;
    }
    var stackedDimCoordDim_1 = stackedDimInfo.coordDim;
    var stackedDimType = stackedDimInfo.type;
    var stackedDimCoordIndex_1 = 0;
    each(dimensionDefineList, function(dimensionInfo) {
      if (dimensionInfo.coordDim === stackedDimCoordDim_1) {
        stackedDimCoordIndex_1++;
      }
    });
    var stackedOverDimensionDefine = {
      name: stackResultDimension,
      coordDim: stackedDimCoordDim_1,
      coordDimIndex: stackedDimCoordIndex_1,
      type: stackedDimType,
      isExtraCoord: true,
      isCalculationCoord: true,
      storeDimIndex: dimensionDefineList.length
    };
    var stackResultDimensionDefine = {
      name: stackedOverDimension,
      // This dimension contains stack base (generally, 0), so do not set it as
      // `stackedDimCoordDim` to avoid extent calculation, consider log scale.
      coordDim: stackedOverDimension,
      coordDimIndex: stackedDimCoordIndex_1 + 1,
      type: stackedDimType,
      isExtraCoord: true,
      isCalculationCoord: true,
      storeDimIndex: dimensionDefineList.length + 1
    };
    if (schema) {
      if (store) {
        stackedOverDimensionDefine.storeDimIndex = store.ensureCalculationDimension(stackedOverDimension, stackedDimType);
        stackResultDimensionDefine.storeDimIndex = store.ensureCalculationDimension(stackResultDimension, stackedDimType);
      }
      schema.appendCalculationDimension(stackedOverDimensionDefine);
      schema.appendCalculationDimension(stackResultDimensionDefine);
    } else {
      dimensionDefineList.push(stackedOverDimensionDefine);
      dimensionDefineList.push(stackResultDimensionDefine);
    }
  }
  return {
    stackedDimension: stackedDimInfo && stackedDimInfo.name,
    stackedByDimension: stackedByDimInfo && stackedByDimInfo.name,
    isStackedByIndex: byIndex,
    stackedOverDimension,
    stackResultDimension
  };
}
function isLegacyDimensionsInput(dimensionsInput) {
  return !isSeriesDataSchema(dimensionsInput.schema);
}
function isDimensionStacked(data, stackedDim) {
  return !!stackedDim && stackedDim === data.getCalculationInfo("stackedDimension");
}
function getStackedDimension(data, targetDim) {
  return isDimensionStacked(data, targetDim) ? data.getCalculationInfo("stackResultDimension") : targetDim;
}

// node_modules/echarts/lib/chart/helper/createSeriesData.js
function getCoordSysDimDefs(seriesModel, coordSysInfo) {
  var coordSysName = seriesModel.get("coordinateSystem");
  var registeredCoordSys = CoordinateSystem_default.get(coordSysName);
  var coordSysDimDefs;
  if (coordSysInfo && coordSysInfo.coordSysDims) {
    coordSysDimDefs = map2(coordSysInfo.coordSysDims, function(dim) {
      var dimInfo = {
        name: dim
      };
      var axisModel = coordSysInfo.axisMap.get(dim);
      if (axisModel) {
        var axisType = axisModel.get("type");
        dimInfo.type = getDimensionTypeByAxis(axisType);
      }
      return dimInfo;
    });
  }
  if (!coordSysDimDefs) {
    coordSysDimDefs = registeredCoordSys && (registeredCoordSys.getDimensionsInfo ? registeredCoordSys.getDimensionsInfo() : registeredCoordSys.dimensions.slice()) || ["x", "y"];
  }
  return coordSysDimDefs;
}
function injectOrdinalMeta(dimInfoList, createInvertedIndices, coordSysInfo) {
  var firstCategoryDimIndex;
  var hasNameEncode;
  coordSysInfo && each(dimInfoList, function(dimInfo, dimIndex) {
    var coordDim = dimInfo.coordDim;
    var categoryAxisModel = coordSysInfo.categoryAxisMap.get(coordDim);
    if (categoryAxisModel) {
      if (firstCategoryDimIndex == null) {
        firstCategoryDimIndex = dimIndex;
      }
      dimInfo.ordinalMeta = categoryAxisModel.getOrdinalMeta();
      if (createInvertedIndices) {
        dimInfo.createInvertedIndices = true;
      }
    }
    if (dimInfo.otherDims.itemName != null) {
      hasNameEncode = true;
    }
  });
  if (!hasNameEncode && firstCategoryDimIndex != null) {
    dimInfoList[firstCategoryDimIndex].otherDims.itemName = 0;
  }
  return firstCategoryDimIndex;
}
function createSeriesData(sourceRaw, seriesModel, opt) {
  opt = opt || {};
  var sourceManager = seriesModel.getSourceManager();
  var source;
  var isOriginalSource = false;
  if (sourceRaw) {
    isOriginalSource = true;
    source = createSourceFromSeriesDataOption(sourceRaw);
  } else {
    source = sourceManager.getSource();
    isOriginalSource = source.sourceFormat === SOURCE_FORMAT_ORIGINAL;
  }
  var coordSysInfo = getCoordSysInfoBySeries(seriesModel);
  var coordSysDimDefs = getCoordSysDimDefs(seriesModel, coordSysInfo);
  var useEncodeDefaulter = opt.useEncodeDefaulter;
  var encodeDefaulter = isFunction(useEncodeDefaulter) ? useEncodeDefaulter : useEncodeDefaulter ? curry(makeSeriesEncodeForAxisCoordSys, coordSysDimDefs, seriesModel) : null;
  var createDimensionOptions = {
    coordDimensions: coordSysDimDefs,
    generateCoord: opt.generateCoord,
    encodeDefine: seriesModel.getEncode(),
    encodeDefaulter,
    canOmitUnusedDimensions: !isOriginalSource
  };
  var schema = prepareSeriesDataSchema(source, createDimensionOptions);
  var firstCategoryDimIndex = injectOrdinalMeta(schema.dimensions, opt.createInvertedIndices, coordSysInfo);
  var store = !isOriginalSource ? sourceManager.getSharedDataStore(schema) : null;
  var stackCalculationInfo = enableDataStack(seriesModel, {
    schema,
    store
  });
  var data = new SeriesData_default(schema, seriesModel);
  data.setCalculationInfo(stackCalculationInfo);
  var dimValueGetter = firstCategoryDimIndex != null && isNeedCompleteOrdinalData(source) ? function(itemOpt, dimName, dataIndex, dimIndex) {
    return dimIndex === firstCategoryDimIndex ? dataIndex : this.defaultDimValueGetter(itemOpt, dimName, dataIndex, dimIndex);
  } : null;
  data.hasItemOption = false;
  data.initData(
    // Try to reuse the data store in sourceManager if using dataset.
    isOriginalSource ? source : store,
    null,
    dimValueGetter
  );
  return data;
}
function isNeedCompleteOrdinalData(source) {
  if (source.sourceFormat === SOURCE_FORMAT_ORIGINAL) {
    var sampleItem = firstDataNotNull(source.data || []);
    return !isArray(getDataItemValue(sampleItem));
  }
}
function firstDataNotNull(arr) {
  var i2 = 0;
  while (i2 < arr.length && arr[i2] == null) {
    i2++;
  }
  return arr[i2];
}
var createSeriesData_default = createSeriesData;

// node_modules/echarts/lib/util/component.js
var base = Math.round(Math.random() * 10);
function getUID(type) {
  return [type || "", base++].join("_");
}
function enableSubTypeDefaulter(target) {
  var subTypeDefaulters = {};
  target.registerSubTypeDefaulter = function(componentType, defaulter) {
    var componentTypeInfo = parseClassType(componentType);
    subTypeDefaulters[componentTypeInfo.main] = defaulter;
  };
  target.determineSubType = function(componentType, option) {
    var type = option.type;
    if (!type) {
      var componentTypeMain = parseClassType(componentType).main;
      if (target.hasSubTypes(componentType) && subTypeDefaulters[componentTypeMain]) {
        type = subTypeDefaulters[componentTypeMain](option);
      }
    }
    return type;
  };
}
function enableTopologicalTravel(entity, dependencyGetter) {
  entity.topologicalTravel = function(targetNameList, fullNameList, callback, context) {
    if (!targetNameList.length) {
      return;
    }
    var result = makeDepndencyGraph(fullNameList);
    var graph = result.graph;
    var noEntryList = result.noEntryList;
    var targetNameSet = {};
    each(targetNameList, function(name) {
      targetNameSet[name] = true;
    });
    while (noEntryList.length) {
      var currComponentType = noEntryList.pop();
      var currVertex = graph[currComponentType];
      var isInTargetNameSet = !!targetNameSet[currComponentType];
      if (isInTargetNameSet) {
        callback.call(context, currComponentType, currVertex.originalDeps.slice());
        delete targetNameSet[currComponentType];
      }
      each(currVertex.successor, isInTargetNameSet ? removeEdgeAndAdd : removeEdge);
    }
    each(targetNameSet, function() {
      var errMsg = "";
      if (true) {
        errMsg = makePrintable("Circular dependency may exists: ", targetNameSet, targetNameList, fullNameList);
      }
      throw new Error(errMsg);
    });
    function removeEdge(succComponentType) {
      graph[succComponentType].entryCount--;
      if (graph[succComponentType].entryCount === 0) {
        noEntryList.push(succComponentType);
      }
    }
    function removeEdgeAndAdd(succComponentType) {
      targetNameSet[succComponentType] = true;
      removeEdge(succComponentType);
    }
  };
  function makeDepndencyGraph(fullNameList) {
    var graph = {};
    var noEntryList = [];
    each(fullNameList, function(name) {
      var thisItem = createDependencyGraphItem(graph, name);
      var originalDeps = thisItem.originalDeps = dependencyGetter(name);
      var availableDeps = getAvailableDependencies(originalDeps, fullNameList);
      thisItem.entryCount = availableDeps.length;
      if (thisItem.entryCount === 0) {
        noEntryList.push(name);
      }
      each(availableDeps, function(dependentName) {
        if (indexOf(thisItem.predecessor, dependentName) < 0) {
          thisItem.predecessor.push(dependentName);
        }
        var thatItem = createDependencyGraphItem(graph, dependentName);
        if (indexOf(thatItem.successor, dependentName) < 0) {
          thatItem.successor.push(name);
        }
      });
    });
    return {
      graph,
      noEntryList
    };
  }
  function createDependencyGraphItem(graph, name) {
    if (!graph[name]) {
      graph[name] = {
        predecessor: [],
        successor: []
      };
    }
    return graph[name];
  }
  function getAvailableDependencies(originalDeps, fullNameList) {
    var availableDeps = [];
    each(originalDeps, function(dep) {
      indexOf(fullNameList, dep) >= 0 && availableDeps.push(dep);
    });
    return availableDeps;
  }
}
function inheritDefaultOption(superOption, subOption) {
  return merge2(merge2({}, superOption, true), subOption, true);
}

// node_modules/zrender/lib/core/fourPointsTransform.js
var LN2 = Math.log(2);
function determinant(rows, rank, rowStart, rowMask, colMask, detCache) {
  var cacheKey = rowMask + "-" + colMask;
  var fullRank = rows.length;
  if (detCache.hasOwnProperty(cacheKey)) {
    return detCache[cacheKey];
  }
  if (rank === 1) {
    var colStart = Math.round(Math.log((1 << fullRank) - 1 & ~colMask) / LN2);
    return rows[rowStart][colStart];
  }
  var subRowMask = rowMask | 1 << rowStart;
  var subRowStart = rowStart + 1;
  while (rowMask & 1 << subRowStart) {
    subRowStart++;
  }
  var sum2 = 0;
  for (var j = 0, colLocalIdx = 0; j < fullRank; j++) {
    var colTag = 1 << j;
    if (!(colTag & colMask)) {
      sum2 += (colLocalIdx % 2 ? -1 : 1) * rows[rowStart][j] * determinant(rows, rank - 1, subRowStart, subRowMask, colMask | colTag, detCache);
      colLocalIdx++;
    }
  }
  detCache[cacheKey] = sum2;
  return sum2;
}
function buildTransformer(src, dest) {
  var mA = [
    [src[0], src[1], 1, 0, 0, 0, -dest[0] * src[0], -dest[0] * src[1]],
    [0, 0, 0, src[0], src[1], 1, -dest[1] * src[0], -dest[1] * src[1]],
    [src[2], src[3], 1, 0, 0, 0, -dest[2] * src[2], -dest[2] * src[3]],
    [0, 0, 0, src[2], src[3], 1, -dest[3] * src[2], -dest[3] * src[3]],
    [src[4], src[5], 1, 0, 0, 0, -dest[4] * src[4], -dest[4] * src[5]],
    [0, 0, 0, src[4], src[5], 1, -dest[5] * src[4], -dest[5] * src[5]],
    [src[6], src[7], 1, 0, 0, 0, -dest[6] * src[6], -dest[6] * src[7]],
    [0, 0, 0, src[6], src[7], 1, -dest[7] * src[6], -dest[7] * src[7]]
  ];
  var detCache = {};
  var det = determinant(mA, 8, 0, 0, 0, detCache);
  if (det === 0) {
    return;
  }
  var vh = [];
  for (var i2 = 0; i2 < 8; i2++) {
    for (var j = 0; j < 8; j++) {
      vh[j] == null && (vh[j] = 0);
      vh[j] += ((i2 + j) % 2 ? -1 : 1) * determinant(mA, 7, i2 === 0 ? 1 : 0, 1 << i2, 1 << j, detCache) / det * dest[i2];
    }
  }
  return function(out2, srcPointX, srcPointY) {
    var pk = srcPointX * vh[6] + srcPointY * vh[7] + 1;
    out2[0] = (srcPointX * vh[0] + srcPointY * vh[1] + vh[2]) / pk;
    out2[1] = (srcPointX * vh[3] + srcPointY * vh[4] + vh[5]) / pk;
  };
}

// node_modules/zrender/lib/core/dom.js
var EVENT_SAVED_PROP = "___zrEVENTSAVED";
var _calcOut = [];
function transformLocalCoord(out2, elFrom, elTarget, inX, inY) {
  return transformCoordWithViewport(_calcOut, elFrom, inX, inY, true) && transformCoordWithViewport(out2, elTarget, _calcOut[0], _calcOut[1]);
}
function transformLocalCoordClear(elFrom, elTarget) {
  elFrom && dealClear(elFrom);
  elTarget && dealClear(elTarget);
  function dealClear(el) {
    var saved = el[EVENT_SAVED_PROP];
    if (saved) {
      saved.clearMarkers && saved.clearMarkers();
      delete el[EVENT_SAVED_PROP];
    }
  }
}
function transformCoordWithViewport(out2, el, inX, inY, inverse) {
  if (el.getBoundingClientRect && env_default.domSupported && !isCanvasEl(el)) {
    var saved = el[EVENT_SAVED_PROP] || (el[EVENT_SAVED_PROP] = {});
    var markers = prepareCoordMarkers(el, saved);
    var transformer = preparePointerTransformer(markers, saved, inverse);
    if (transformer) {
      transformer(out2, inX, inY);
      return true;
    }
  }
  return false;
}
function prepareCoordMarkers(el, saved) {
  var markers = saved.markers;
  if (markers) {
    return markers;
  }
  markers = saved.markers = [];
  var propLR = ["left", "right"];
  var propTB = ["top", "bottom"];
  for (var i2 = 0; i2 < 4; i2++) {
    var marker = document.createElement("div");
    var stl = marker.style;
    var idxLR = i2 % 2;
    var idxTB = (i2 >> 1) % 2;
    stl.cssText = [
      "position: absolute",
      "visibility: hidden",
      "padding: 0",
      "margin: 0",
      "border-width: 0",
      "user-select: none",
      "width:0",
      "height:0",
      propLR[idxLR] + ":0",
      propTB[idxTB] + ":0",
      propLR[1 - idxLR] + ":auto",
      propTB[1 - idxTB] + ":auto",
      ""
    ].join("!important;");
    el.appendChild(marker);
    markers.push(marker);
  }
  saved.clearMarkers = function() {
    each(markers, function(marker2) {
      marker2.parentNode && marker2.parentNode.removeChild(marker2);
    });
  };
  return markers;
}
function preparePointerTransformer(markers, saved, inverse) {
  var transformerName = inverse ? "invTrans" : "trans";
  var transformer = saved[transformerName];
  var oldSrcCoords = saved.srcCoords;
  var srcCoords = [];
  var destCoords = [];
  var oldCoordTheSame = true;
  for (var i2 = 0; i2 < 4; i2++) {
    var rect = markers[i2].getBoundingClientRect();
    var ii = 2 * i2;
    var x = rect.left;
    var y = rect.top;
    srcCoords.push(x, y);
    oldCoordTheSame = oldCoordTheSame && oldSrcCoords && x === oldSrcCoords[ii] && y === oldSrcCoords[ii + 1];
    destCoords.push(markers[i2].offsetLeft, markers[i2].offsetTop);
  }
  return oldCoordTheSame && transformer ? transformer : (saved.srcCoords = srcCoords, saved[transformerName] = inverse ? buildTransformer(destCoords, srcCoords) : buildTransformer(srcCoords, destCoords));
}
function isCanvasEl(el) {
  return el.nodeName.toUpperCase() === "CANVAS";
}
var replaceReg = /([&<>"'])/g;
var replaceMap = {
  "&": "&amp;",
  "<": "&lt;",
  ">": "&gt;",
  '"': "&quot;",
  "'": "&#39;"
};
function encodeHTML(source) {
  return source == null ? "" : (source + "").replace(replaceReg, function(str, c) {
    return replaceMap[c];
  });
}

// node_modules/echarts/lib/i18n/langEN.js
var langEN_default = {
  time: {
    month: ["January", "February", "March", "April", "May", "June", "July", "August", "September", "October", "November", "December"],
    monthAbbr: ["Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec"],
    dayOfWeek: ["Sunday", "Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday"],
    dayOfWeekAbbr: ["Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat"]
  },
  legend: {
    selector: {
      all: "All",
      inverse: "Inv"
    }
  },
  toolbox: {
    brush: {
      title: {
        rect: "Box Select",
        polygon: "Lasso Select",
        lineX: "Horizontally Select",
        lineY: "Vertically Select",
        keep: "Keep Selections",
        clear: "Clear Selections"
      }
    },
    dataView: {
      title: "Data View",
      lang: ["Data View", "Close", "Refresh"]
    },
    dataZoom: {
      title: {
        zoom: "Zoom",
        back: "Zoom Reset"
      }
    },
    magicType: {
      title: {
        line: "Switch to Line Chart",
        bar: "Switch to Bar Chart",
        stack: "Stack",
        tiled: "Tile"
      }
    },
    restore: {
      title: "Restore"
    },
    saveAsImage: {
      title: "Save as Image",
      lang: ["Right Click to Save Image"]
    }
  },
  series: {
    typeNames: {
      pie: "Pie chart",
      bar: "Bar chart",
      line: "Line chart",
      scatter: "Scatter plot",
      effectScatter: "Ripple scatter plot",
      radar: "Radar chart",
      tree: "Tree",
      treemap: "Treemap",
      boxplot: "Boxplot",
      candlestick: "Candlestick",
      k: "K line chart",
      heatmap: "Heat map",
      map: "Map",
      parallel: "Parallel coordinate map",
      lines: "Line graph",
      graph: "Relationship graph",
      sankey: "Sankey diagram",
      funnel: "Funnel chart",
      gauge: "Gauge",
      pictorialBar: "Pictorial bar",
      themeRiver: "Theme River Map",
      sunburst: "Sunburst",
      custom: "Custom chart",
      chart: "Chart"
    }
  },
  aria: {
    general: {
      withTitle: 'This is a chart about "{title}"',
      withoutTitle: "This is a chart"
    },
    series: {
      single: {
        prefix: "",
        withName: " with type {seriesType} named {seriesName}.",
        withoutName: " with type {seriesType}."
      },
      multiple: {
        prefix: ". It consists of {seriesCount} series count.",
        withName: " The {seriesId} series is a {seriesType} representing {seriesName}.",
        withoutName: " The {seriesId} series is a {seriesType}.",
        separator: {
          middle: "",
          end: ""
        }
      }
    },
    data: {
      allData: "The data is as follows: ",
      partialData: "The first {displayCnt} items are: ",
      withName: "the data for {name} is {value}",
      withoutName: "{value}",
      separator: {
        middle: ", ",
        end: ". "
      }
    }
  }
};

// node_modules/echarts/lib/i18n/langZH.js
var langZH_default = {
  time: {
    month: ["一月", "二月", "三月", "四月", "五月", "六月", "七月", "八月", "九月", "十月", "十一月", "十二月"],
    monthAbbr: ["1月", "2月", "3月", "4月", "5月", "6月", "7月", "8月", "9月", "10月", "11月", "12月"],
    dayOfWeek: ["星期日", "星期一", "星期二", "星期三", "星期四", "星期五", "星期六"],
    dayOfWeekAbbr: ["日", "一", "二", "三", "四", "五", "六"]
  },
  legend: {
    selector: {
      all: "全选",
      inverse: "反选"
    }
  },
  toolbox: {
    brush: {
      title: {
        rect: "矩形选择",
        polygon: "圈选",
        lineX: "横向选择",
        lineY: "纵向选择",
        keep: "保持选择",
        clear: "清除选择"
      }
    },
    dataView: {
      title: "数据视图",
      lang: ["数据视图", "关闭", "刷新"]
    },
    dataZoom: {
      title: {
        zoom: "区域缩放",
        back: "区域缩放还原"
      }
    },
    magicType: {
      title: {
        line: "切换为折线图",
        bar: "切换为柱状图",
        stack: "切换为堆叠",
        tiled: "切换为平铺"
      }
    },
    restore: {
      title: "还原"
    },
    saveAsImage: {
      title: "保存为图片",
      lang: ["右键另存为图片"]
    }
  },
  series: {
    typeNames: {
      pie: "饼图",
      bar: "柱状图",
      line: "折线图",
      scatter: "散点图",
      effectScatter: "涟漪散点图",
      radar: "雷达图",
      tree: "树图",
      treemap: "矩形树图",
      boxplot: "箱型图",
      candlestick: "K线图",
      k: "K线图",
      heatmap: "热力图",
      map: "地图",
      parallel: "平行坐标图",
      lines: "线图",
      graph: "关系图",
      sankey: "桑基图",
      funnel: "漏斗图",
      gauge: "仪表盘图",
      pictorialBar: "象形柱图",
      themeRiver: "主题河流图",
      sunburst: "旭日图",
      custom: "自定义图表",
      chart: "图表"
    }
  },
  aria: {
    general: {
      withTitle: "这是一个关于“{title}”的图表。",
      withoutTitle: "这是一个图表，"
    },
    series: {
      single: {
        prefix: "",
        withName: "图表类型是{seriesType}，表示{seriesName}。",
        withoutName: "图表类型是{seriesType}。"
      },
      multiple: {
        prefix: "它由{seriesCount}个图表系列组成。",
        withName: "第{seriesId}个系列是一个表示{seriesName}的{seriesType}，",
        withoutName: "第{seriesId}个系列是一个{seriesType}，",
        separator: {
          middle: "；",
          end: "。"
        }
      }
    },
    data: {
      allData: "其数据是——",
      partialData: "其中，前{displayCnt}项是——",
      withName: "{name}的数据是{value}",
      withoutName: "{value}",
      separator: {
        middle: "，",
        end: ""
      }
    }
  }
};

// node_modules/echarts/lib/core/locale.js
var LOCALE_ZH = "ZH";
var LOCALE_EN = "EN";
var DEFAULT_LOCALE = LOCALE_EN;
var localeStorage = {};
var localeModels = {};
var SYSTEM_LANG = !env_default.domSupported ? DEFAULT_LOCALE : (function() {
  var langStr = (document.documentElement.lang || navigator.language || navigator.browserLanguage || DEFAULT_LOCALE).toUpperCase();
  return langStr.indexOf(LOCALE_ZH) > -1 ? LOCALE_ZH : DEFAULT_LOCALE;
})();
function registerLocale(locale, localeObj) {
  locale = locale.toUpperCase();
  localeModels[locale] = new Model_default(localeObj);
  localeStorage[locale] = localeObj;
}
function createLocaleObject(locale) {
  if (isString(locale)) {
    var localeObj = localeStorage[locale.toUpperCase()] || {};
    if (locale === LOCALE_ZH || locale === LOCALE_EN) {
      return clone(localeObj);
    } else {
      return merge2(clone(localeObj), clone(localeStorage[DEFAULT_LOCALE]), false);
    }
  } else {
    return merge2(clone(locale), clone(localeStorage[DEFAULT_LOCALE]), false);
  }
}
function getLocaleModel(lang) {
  return localeModels[lang];
}
function getDefaultLocaleModel() {
  return localeModels[DEFAULT_LOCALE];
}
registerLocale(LOCALE_EN, langEN_default);
registerLocale(LOCALE_ZH, langZH_default);

// node_modules/echarts/lib/scale/break.js
var _impl = null;
function getScaleBreakHelper() {
  return _impl;
}
function simplyParseBreakOption(scale3, opt) {
  var scaleBreakHelper = getScaleBreakHelper();
  var breakOption = opt.breakOption;
  var breakParsed = opt.breakParsed;
  if (!breakParsed && scaleBreakHelper) {
    breakParsed = scaleBreakHelper.parseAxisBreakOption(breakOption, scale3);
  }
  return breakParsed;
}
function getBreaksUnsafe(scale3) {
  var brk = scale3.brk;
  return brk ? brk.breaks : [];
}
function hasBreaks(scale3) {
  var brk = scale3.brk;
  return brk ? brk.hasBreaks() : false;
}

// node_modules/echarts/lib/util/time.js
var ONE_SECOND = 1e3;
var ONE_MINUTE = ONE_SECOND * 60;
var ONE_HOUR = ONE_MINUTE * 60;
var ONE_DAY = ONE_HOUR * 24;
var ONE_YEAR = ONE_DAY * 365;
var primaryTimeUnitFormatterMatchers = {
  year: /({yyyy}|{yy})/,
  month: /({MMMM}|{MMM}|{MM}|{M})/,
  day: /({dd}|{d})/,
  hour: /({HH}|{H}|{hh}|{h})/,
  minute: /({mm}|{m})/,
  second: /({ss}|{s})/,
  millisecond: /({SSS}|{S})/
};
var defaultFormatterSeed = {
  year: "{yyyy}",
  month: "{MMM}",
  day: "{d}",
  hour: "{HH}:{mm}",
  minute: "{HH}:{mm}",
  second: "{HH}:{mm}:{ss}",
  millisecond: "{HH}:{mm}:{ss} {SSS}"
};
var defaultFullFormatter = "{yyyy}-{MM}-{dd} {HH}:{mm}:{ss} {SSS}";
var fullDayFormatter = "{yyyy}-{MM}-{dd}";
var fullLeveledFormatter = {
  year: "{yyyy}",
  month: "{yyyy}-{MM}",
  day: fullDayFormatter,
  hour: fullDayFormatter + " " + defaultFormatterSeed.hour,
  minute: fullDayFormatter + " " + defaultFormatterSeed.minute,
  second: fullDayFormatter + " " + defaultFormatterSeed.second,
  millisecond: defaultFullFormatter
};
var primaryTimeUnits = ["year", "month", "day", "hour", "minute", "second", "millisecond"];
var timeUnits = ["year", "half-year", "quarter", "month", "week", "half-week", "day", "half-day", "quarter-day", "hour", "minute", "second", "millisecond"];
function parseTimeAxisLabelFormatter(formatter) {
  return !isString(formatter) && !isFunction(formatter) ? parseTimeAxisLabelFormatterDictionary(formatter) : formatter;
}
function parseTimeAxisLabelFormatterDictionary(dictOption) {
  dictOption = dictOption || {};
  var dict = {};
  var canAddHighlight = true;
  each(primaryTimeUnits, function(lowestUnit) {
    canAddHighlight && (canAddHighlight = dictOption[lowestUnit] == null);
  });
  each(primaryTimeUnits, function(lowestUnit, lowestUnitIdx) {
    var upperDictOption = dictOption[lowestUnit];
    dict[lowestUnit] = {};
    var lowerTpl = null;
    for (var upperUnitIdx = lowestUnitIdx; upperUnitIdx >= 0; upperUnitIdx--) {
      var upperUnit = primaryTimeUnits[upperUnitIdx];
      var upperDictItemOption = isObject(upperDictOption) && !isArray(upperDictOption) ? upperDictOption[upperUnit] : upperDictOption;
      var tplArr = void 0;
      if (isArray(upperDictItemOption)) {
        tplArr = upperDictItemOption.slice();
        lowerTpl = tplArr[0] || "";
      } else if (isString(upperDictItemOption)) {
        lowerTpl = upperDictItemOption;
        tplArr = [lowerTpl];
      } else {
        if (lowerTpl == null) {
          lowerTpl = defaultFormatterSeed[lowestUnit];
        } else if (!primaryTimeUnitFormatterMatchers[upperUnit].test(lowerTpl)) {
          lowerTpl = dict[upperUnit][upperUnit][0] + " " + lowerTpl;
        }
        tplArr = [lowerTpl];
        if (canAddHighlight) {
          tplArr[1] = "{primary|" + lowerTpl + "}";
        }
      }
      dict[lowestUnit][upperUnit] = tplArr;
    }
  });
  return dict;
}
function pad(str, len2) {
  str += "";
  return "0000".substr(0, len2 - str.length) + str;
}
function getPrimaryTimeUnit(timeUnit) {
  switch (timeUnit) {
    case "half-year":
    case "quarter":
      return "month";
    case "week":
    case "half-week":
      return "day";
    case "half-day":
    case "quarter-day":
      return "hour";
    default:
      return timeUnit;
  }
}
function isPrimaryTimeUnit(timeUnit) {
  return timeUnit === getPrimaryTimeUnit(timeUnit);
}
function getDefaultFormatPrecisionOfInterval(timeUnit) {
  switch (timeUnit) {
    case "year":
    case "month":
      return "day";
    case "millisecond":
      return "millisecond";
    default:
      return "second";
  }
}
function format(time, template, isUTC, lang) {
  var date = parseDate(time);
  var y = date[fullYearGetterName(isUTC)]();
  var M2 = date[monthGetterName(isUTC)]() + 1;
  var q = Math.floor((M2 - 1) / 3) + 1;
  var d = date[dateGetterName(isUTC)]();
  var e2 = date["get" + (isUTC ? "UTC" : "") + "Day"]();
  var H2 = date[hoursGetterName(isUTC)]();
  var h = (H2 - 1) % 12 + 1;
  var m2 = date[minutesGetterName(isUTC)]();
  var s = date[secondsGetterName(isUTC)]();
  var S2 = date[millisecondsGetterName(isUTC)]();
  var a = H2 >= 12 ? "pm" : "am";
  var A2 = a.toUpperCase();
  var localeModel = lang instanceof Model_default ? lang : getLocaleModel(lang || SYSTEM_LANG) || getDefaultLocaleModel();
  var timeModel = localeModel.getModel("time");
  var month = timeModel.get("month");
  var monthAbbr = timeModel.get("monthAbbr");
  var dayOfWeek = timeModel.get("dayOfWeek");
  var dayOfWeekAbbr = timeModel.get("dayOfWeekAbbr");
  return (template || "").replace(/{a}/g, a + "").replace(/{A}/g, A2 + "").replace(/{yyyy}/g, y + "").replace(/{yy}/g, pad(y % 100 + "", 2)).replace(/{Q}/g, q + "").replace(/{MMMM}/g, month[M2 - 1]).replace(/{MMM}/g, monthAbbr[M2 - 1]).replace(/{MM}/g, pad(M2, 2)).replace(/{M}/g, M2 + "").replace(/{dd}/g, pad(d, 2)).replace(/{d}/g, d + "").replace(/{eeee}/g, dayOfWeek[e2]).replace(/{ee}/g, dayOfWeekAbbr[e2]).replace(/{e}/g, e2 + "").replace(/{HH}/g, pad(H2, 2)).replace(/{H}/g, H2 + "").replace(/{hh}/g, pad(h + "", 2)).replace(/{h}/g, h + "").replace(/{mm}/g, pad(m2, 2)).replace(/{m}/g, m2 + "").replace(/{ss}/g, pad(s, 2)).replace(/{s}/g, s + "").replace(/{SSS}/g, pad(S2, 3)).replace(/{S}/g, S2 + "");
}
function leveledFormat(tick, idx, formatter, lang, isUTC) {
  var template = null;
  if (isString(formatter)) {
    template = formatter;
  } else if (isFunction(formatter)) {
    var extra = {
      time: tick.time,
      level: tick.time ? tick.time.level : 0
    };
    var scaleBreakHelper = getScaleBreakHelper();
    if (scaleBreakHelper) {
      scaleBreakHelper.makeAxisLabelFormatterParamBreak(extra, tick["break"]);
    }
    template = formatter(tick.value, idx, extra);
  } else {
    var tickTime = tick.time;
    if (tickTime) {
      var leveledTplArr = formatter[tickTime.lowerTimeUnit][tickTime.upperTimeUnit];
      template = leveledTplArr[Math.min(tickTime.level, leveledTplArr.length - 1)] || "";
    } else {
      var unit = getUnitFromValue(tick.value, isUTC);
      template = formatter[unit][unit][0];
    }
  }
  return format(new Date(tick.value), template, isUTC, lang);
}
function getUnitFromValue(value, isUTC) {
  var date = parseDate(value);
  var M2 = date[monthGetterName(isUTC)]() + 1;
  var d = date[dateGetterName(isUTC)]();
  var h = date[hoursGetterName(isUTC)]();
  var m2 = date[minutesGetterName(isUTC)]();
  var s = date[secondsGetterName(isUTC)]();
  var S2 = date[millisecondsGetterName(isUTC)]();
  var isSecond = S2 === 0;
  var isMinute = isSecond && s === 0;
  var isHour = isMinute && m2 === 0;
  var isDay = isHour && h === 0;
  var isMonth = isDay && d === 1;
  var isYear = isMonth && M2 === 1;
  if (isYear) {
    return "year";
  } else if (isMonth) {
    return "month";
  } else if (isDay) {
    return "day";
  } else if (isHour) {
    return "hour";
  } else if (isMinute) {
    return "minute";
  } else if (isSecond) {
    return "second";
  } else {
    return "millisecond";
  }
}
function roundTime(date, timeUnit, isUTC) {
  switch (timeUnit) {
    case "year":
      date[monthSetterName(isUTC)](0);
    case "month":
      date[dateSetterName(isUTC)](1);
    case "day":
      date[hoursSetterName(isUTC)](0);
    case "hour":
      date[minutesSetterName(isUTC)](0);
    case "minute":
      date[secondsSetterName(isUTC)](0);
    case "second":
      date[millisecondsSetterName(isUTC)](0);
  }
  return date;
}
function fullYearGetterName(isUTC) {
  return isUTC ? "getUTCFullYear" : "getFullYear";
}
function monthGetterName(isUTC) {
  return isUTC ? "getUTCMonth" : "getMonth";
}
function dateGetterName(isUTC) {
  return isUTC ? "getUTCDate" : "getDate";
}
function hoursGetterName(isUTC) {
  return isUTC ? "getUTCHours" : "getHours";
}
function minutesGetterName(isUTC) {
  return isUTC ? "getUTCMinutes" : "getMinutes";
}
function secondsGetterName(isUTC) {
  return isUTC ? "getUTCSeconds" : "getSeconds";
}
function millisecondsGetterName(isUTC) {
  return isUTC ? "getUTCMilliseconds" : "getMilliseconds";
}
function fullYearSetterName(isUTC) {
  return isUTC ? "setUTCFullYear" : "setFullYear";
}
function monthSetterName(isUTC) {
  return isUTC ? "setUTCMonth" : "setMonth";
}
function dateSetterName(isUTC) {
  return isUTC ? "setUTCDate" : "setDate";
}
function hoursSetterName(isUTC) {
  return isUTC ? "setUTCHours" : "setHours";
}
function minutesSetterName(isUTC) {
  return isUTC ? "setUTCMinutes" : "setMinutes";
}
function secondsSetterName(isUTC) {
  return isUTC ? "setUTCSeconds" : "setSeconds";
}
function millisecondsSetterName(isUTC) {
  return isUTC ? "setUTCMilliseconds" : "setMilliseconds";
}

// node_modules/echarts/lib/util/format.js
function addCommas(x) {
  if (!isNumeric(x)) {
    return isString(x) ? x : "-";
  }
  var parts = (x + "").split(".");
  return parts[0].replace(/(\d{1,3})(?=(?:\d{3})+(?!\d))/g, "$1,") + (parts.length > 1 ? "." + parts[1] : "");
}
function toCamelCase(str, upperCaseFirst) {
  str = (str || "").toLowerCase().replace(/-(.)/g, function(match, group1) {
    return group1.toUpperCase();
  });
  if (upperCaseFirst && str) {
    str = str.charAt(0).toUpperCase() + str.slice(1);
  }
  return str;
}
var normalizeCssArray2 = normalizeCssArray;
function makeValueReadable(value, valueType, useUTC) {
  var USER_READABLE_DEFUALT_TIME_PATTERN = "{yyyy}-{MM}-{dd} {HH}:{mm}:{ss}";
  function stringToUserReadable(str) {
    return str && trim(str) ? str : "-";
  }
  function isNumberUserReadable(num) {
    return isNullableNumberFinite(num);
  }
  var isTypeTime = valueType === "time";
  var isValueDate = value instanceof Date;
  if (isTypeTime || isValueDate) {
    var date = isTypeTime ? parseDate(value) : value;
    if (!isNaN(+date)) {
      return format(date, USER_READABLE_DEFUALT_TIME_PATTERN, useUTC);
    } else if (isValueDate) {
      return "-";
    }
  }
  if (valueType === "ordinal") {
    return isStringSafe(value) ? stringToUserReadable(value) : isNumber(value) ? isNumberUserReadable(value) ? value + "" : "-" : "-";
  }
  var numericResult = numericToNumber(value);
  return isNumberUserReadable(numericResult) ? addCommas(numericResult) : isStringSafe(value) ? stringToUserReadable(value) : typeof value === "boolean" ? value + "" : "-";
}
var TPL_VAR_ALIAS = ["a", "b", "c", "d", "e", "f", "g"];
var wrapVar = function(varName, seriesIdx) {
  return "{" + varName + (seriesIdx == null ? "" : seriesIdx) + "}";
};
function formatTpl(tpl, paramsList, encode) {
  if (!isArray(paramsList)) {
    paramsList = [paramsList];
  }
  var seriesLen = paramsList.length;
  if (!seriesLen) {
    return "";
  }
  var $vars = paramsList[0].$vars || [];
  for (var i2 = 0; i2 < $vars.length; i2++) {
    var alias = TPL_VAR_ALIAS[i2];
    tpl = tpl.replace(wrapVar(alias), wrapVar(alias, 0));
  }
  for (var seriesIdx = 0; seriesIdx < seriesLen; seriesIdx++) {
    for (var k2 = 0; k2 < $vars.length; k2++) {
      var val = paramsList[seriesIdx][$vars[k2]];
      tpl = tpl.replace(wrapVar(TPL_VAR_ALIAS[k2], seriesIdx), encode ? encodeHTML(val) : val);
    }
  }
  return tpl;
}
function formatTplSimple(tpl, param, encode) {
  each(param, function(value, key2) {
    tpl = tpl.replace("{" + key2 + "}", encode ? encodeHTML(value) : value);
  });
  return tpl;
}
function getTooltipMarker(inOpt, extraCssText) {
  var opt = isString(inOpt) ? {
    color: inOpt,
    extraCssText
  } : inOpt || {};
  var color3 = opt.color;
  var type = opt.type;
  extraCssText = opt.extraCssText;
  var renderMode = opt.renderMode || "html";
  if (!color3) {
    return "";
  }
  if (renderMode === "html") {
    return type === "subItem" ? '<span style="display:inline-block;vertical-align:middle;margin-right:8px;margin-left:3px;border-radius:4px;width:4px;height:4px;background-color:' + encodeHTML(color3) + ";" + (extraCssText || "") + '"></span>' : '<span style="display:inline-block;margin-right:4px;border-radius:10px;width:10px;height:10px;background-color:' + encodeHTML(color3) + ";" + (extraCssText || "") + '"></span>';
  } else {
    var markerId = opt.markerId || "markerX";
    return {
      renderMode,
      content: "{" + markerId + "|}  ",
      style: type === "subItem" ? {
        width: 4,
        height: 4,
        borderRadius: 2,
        backgroundColor: color3
      } : {
        width: 10,
        height: 10,
        borderRadius: 5,
        backgroundColor: color3
      }
    };
  }
}
function convertToColorString(color3, defaultColor) {
  defaultColor = defaultColor || "transparent";
  return isString(color3) ? color3 : isObject(color3) ? color3.colorStops && (color3.colorStops[0] || {}).color || defaultColor : defaultColor;
}
function windowOpen(link, target) {
  if (target === "_blank" || target === "blank") {
    var blank = window.open();
    blank.opener = null;
    blank.location.href = link;
  } else {
    window.open(link, target);
  }
}

// node_modules/echarts/lib/util/layout.js
var each2 = each;
var LOCATION_PARAMS = ["left", "right", "top", "bottom", "width", "height"];
var HV_NAMES = [["width", "left", "right"], ["height", "top", "bottom"]];
function boxLayout(orient, group2, gap, maxWidth, maxHeight) {
  var x = 0;
  var y = 0;
  if (maxWidth == null) {
    maxWidth = Infinity;
  }
  if (maxHeight == null) {
    maxHeight = Infinity;
  }
  var currentLineMaxSize = 0;
  group2.eachChild(function(child, idx) {
    var rect = child.getBoundingRect();
    var nextChild = group2.childAt(idx + 1);
    var nextChildRect = nextChild && nextChild.getBoundingRect();
    var nextX;
    var nextY;
    if (orient === "horizontal") {
      var moveX = rect.width + (nextChildRect ? -nextChildRect.x + rect.x : 0);
      nextX = x + moveX;
      if (nextX > maxWidth || child.newline) {
        x = 0;
        nextX = moveX;
        y += currentLineMaxSize + gap;
        currentLineMaxSize = rect.height;
      } else {
        currentLineMaxSize = Math.max(currentLineMaxSize, rect.height);
      }
    } else {
      var moveY = rect.height + (nextChildRect ? -nextChildRect.y + rect.y : 0);
      nextY = y + moveY;
      if (nextY > maxHeight || child.newline) {
        x += currentLineMaxSize + gap;
        y = 0;
        nextY = moveY;
        currentLineMaxSize = rect.width;
      } else {
        currentLineMaxSize = Math.max(currentLineMaxSize, rect.width);
      }
    }
    if (child.newline) {
      return;
    }
    child.x = x;
    child.y = y;
    child.markRedraw();
    orient === "horizontal" ? x = nextX + gap : y = nextY + gap;
  });
}
var box = boxLayout;
var vbox = curry(boxLayout, "vertical");
var hbox = curry(boxLayout, "horizontal");
function getBoxLayoutParams(boxLayoutModel, ignoreParent) {
  return {
    left: boxLayoutModel.getShallow("left", ignoreParent),
    top: boxLayoutModel.getShallow("top", ignoreParent),
    right: boxLayoutModel.getShallow("right", ignoreParent),
    bottom: boxLayoutModel.getShallow("bottom", ignoreParent),
    width: boxLayoutModel.getShallow("width", ignoreParent),
    height: boxLayoutModel.getShallow("height", ignoreParent)
  };
}
function getViewRectAndCenterForCircleLayout(seriesModel, api) {
  var layoutRef = createBoxLayoutReference(seriesModel, api, {
    enableLayoutOnlyByCenter: true
  });
  var boxLayoutParams = seriesModel.getBoxLayoutParams();
  var viewRect2;
  var center3;
  if (layoutRef.type === BoxLayoutReferenceType.point) {
    center3 = layoutRef.refPoint;
    viewRect2 = getLayoutRect(boxLayoutParams, {
      width: api.getWidth(),
      height: api.getHeight()
    });
  } else {
    var centerOption = seriesModel.get("center");
    var centerOptionArr = isArray(centerOption) ? centerOption : [centerOption, centerOption];
    viewRect2 = getLayoutRect(boxLayoutParams, layoutRef.refContainer);
    center3 = layoutRef.boxCoordFrom === BOX_COORD_SYS_COORD_FROM_PROP_COORD2 ? layoutRef.refPoint : [parsePercent2(centerOptionArr[0], viewRect2.width) + viewRect2.x, parsePercent2(centerOptionArr[1], viewRect2.height) + viewRect2.y];
  }
  return {
    viewRect: viewRect2,
    center: center3
  };
}
function getCircleLayout(seriesModel, api) {
  var _a2 = getViewRectAndCenterForCircleLayout(seriesModel, api), viewRect2 = _a2.viewRect, center3 = _a2.center;
  var radius = seriesModel.get("radius");
  if (!isArray(radius)) {
    radius = [0, radius];
  }
  var width = parsePercent2(viewRect2.width, api.getWidth());
  var height = parsePercent2(viewRect2.height, api.getHeight());
  var size = Math.min(width, height);
  var r0 = parsePercent2(radius[0], size / 2);
  var r = parsePercent2(radius[1], size / 2);
  return {
    cx: center3[0],
    cy: center3[1],
    r0,
    r,
    viewRect: viewRect2
  };
}
function getLayoutRect(positionInfo, containerRect, margin) {
  margin = normalizeCssArray2(margin || 0);
  var containerWidth = containerRect.width;
  var containerHeight = containerRect.height;
  var left = parsePercent2(positionInfo.left, containerWidth);
  var top = parsePercent2(positionInfo.top, containerHeight);
  var right = parsePercent2(positionInfo.right, containerWidth);
  var bottom = parsePercent2(positionInfo.bottom, containerHeight);
  var width = parsePercent2(positionInfo.width, containerWidth);
  var height = parsePercent2(positionInfo.height, containerHeight);
  var verticalMargin = margin[2] + margin[0];
  var horizontalMargin = margin[1] + margin[3];
  var aspect = positionInfo.aspect;
  if (isNaN(width)) {
    width = containerWidth - right - horizontalMargin - left;
  }
  if (isNaN(height)) {
    height = containerHeight - bottom - verticalMargin - top;
  }
  if (aspect != null) {
    if (isNaN(width) && isNaN(height)) {
      if (aspect > containerWidth / containerHeight) {
        width = containerWidth * 0.8;
      } else {
        height = containerHeight * 0.8;
      }
    }
    if (isNaN(width)) {
      width = aspect * height;
    }
    if (isNaN(height)) {
      height = width / aspect;
    }
  }
  if (isNaN(left)) {
    left = containerWidth - right - width - horizontalMargin;
  }
  if (isNaN(top)) {
    top = containerHeight - bottom - height - verticalMargin;
  }
  switch (positionInfo.left || positionInfo.right) {
    case "center":
      left = containerWidth / 2 - width / 2 - margin[3];
      break;
    case "right":
      left = containerWidth - width - horizontalMargin;
      break;
  }
  switch (positionInfo.top || positionInfo.bottom) {
    case "middle":
    case "center":
      top = containerHeight / 2 - height / 2 - margin[0];
      break;
    case "bottom":
      top = containerHeight - height - verticalMargin;
      break;
  }
  left = left || 0;
  top = top || 0;
  if (isNaN(width)) {
    width = containerWidth - horizontalMargin - left - (right || 0);
  }
  if (isNaN(height)) {
    height = containerHeight - verticalMargin - top - (bottom || 0);
  }
  var rect = new BoundingRect_default((containerRect.x || 0) + left + margin[3], (containerRect.y || 0) + top + margin[0], width, height);
  rect.margin = margin;
  return rect;
}
function applyPreserveAspect(component, layoutRect, aspect) {
  var preserveAspect = component.getShallow("preserveAspect", true);
  if (!preserveAspect) {
    return layoutRect;
  }
  var actualAspect = layoutRect.width / layoutRect.height;
  if (Math.abs(Math.atan(aspect) - Math.atan(actualAspect)) < 1e-9) {
    return layoutRect;
  }
  var preserveAspectAlign = component.getShallow("preserveAspectAlign", true);
  var preserveAspectVerticalAlign = component.getShallow("preserveAspectVerticalAlign", true);
  var layoutOptInner = {
    width: layoutRect.width,
    height: layoutRect.height
  };
  var isCover = preserveAspect === "cover";
  if (actualAspect > aspect && !isCover || actualAspect < aspect && isCover) {
    layoutOptInner.width = layoutRect.height * aspect;
    preserveAspectAlign === "left" ? layoutOptInner.left = 0 : preserveAspectAlign === "right" ? layoutOptInner.right = 0 : layoutOptInner.left = "center";
  } else {
    layoutOptInner.height = layoutRect.width / aspect;
    preserveAspectVerticalAlign === "top" ? layoutOptInner.top = 0 : preserveAspectVerticalAlign === "bottom" ? layoutOptInner.bottom = 0 : layoutOptInner.top = "middle";
  }
  return getLayoutRect(layoutOptInner, layoutRect);
}
var BoxLayoutReferenceType = {
  rect: 1,
  point: 2
};
function createBoxLayoutReference(model2, api, opt) {
  var refContainer;
  var refPoint;
  var layoutRefType;
  var boxCoordSys = model2.boxCoordinateSystem;
  var boxCoordFrom;
  if (boxCoordSys) {
    var _a2 = getCoordForCoordSysUsageKindBox(model2), coord = _a2.coord, from = _a2.from;
    if (boxCoordSys.dataToLayout) {
      layoutRefType = BoxLayoutReferenceType.rect;
      boxCoordFrom = from;
      var result = boxCoordSys.dataToLayout(coord);
      refContainer = result.contentRect || result.rect;
    } else if (opt && opt.enableLayoutOnlyByCenter && boxCoordSys.dataToPoint) {
      layoutRefType = BoxLayoutReferenceType.point;
      boxCoordFrom = from;
      refPoint = boxCoordSys.dataToPoint(coord);
    } else {
      if (true) {
        error(model2.type + "[" + model2.componentIndex + "]" + (" layout based on " + boxCoordSys.type + " is not supported."));
      }
    }
  }
  if (layoutRefType == null) {
    layoutRefType = BoxLayoutReferenceType.rect;
  }
  if (layoutRefType === BoxLayoutReferenceType.rect) {
    if (!refContainer) {
      refContainer = {
        x: 0,
        y: 0,
        width: api.getWidth(),
        height: api.getHeight()
      };
    }
    refPoint = [refContainer.x + refContainer.width / 2, refContainer.y + refContainer.height / 2];
  }
  return {
    type: layoutRefType,
    refContainer,
    refPoint,
    boxCoordFrom
  };
}
function positionElement(el, positionInfo, containerRect, margin, opt, out2) {
  var h = !opt || !opt.hv || opt.hv[0];
  var v = !opt || !opt.hv || opt.hv[1];
  var boundingMode = opt && opt.boundingMode || "all";
  out2 = out2 || el;
  out2.x = el.x;
  out2.y = el.y;
  if (!h && !v) {
    return false;
  }
  var rect;
  if (boundingMode === "raw") {
    rect = el.type === "group" ? new BoundingRect_default(0, 0, +positionInfo.width || 0, +positionInfo.height || 0) : el.getBoundingRect();
  } else {
    rect = el.getBoundingRect();
    if (el.needLocalTransform()) {
      var transform = el.getLocalTransform();
      rect = rect.clone();
      rect.applyTransform(transform);
    }
  }
  var layoutRect = getLayoutRect(defaults({
    width: rect.width,
    height: rect.height
  }, positionInfo), containerRect, margin);
  var dx = h ? layoutRect.x - rect.x : 0;
  var dy = v ? layoutRect.y - rect.y : 0;
  if (boundingMode === "raw") {
    out2.x = dx;
    out2.y = dy;
  } else {
    out2.x += dx;
    out2.y += dy;
  }
  if (out2 === el) {
    el.markRedraw();
  }
  return true;
}
function sizeCalculable(option, hvIdx) {
  return option[HV_NAMES[hvIdx][0]] != null || option[HV_NAMES[hvIdx][1]] != null && option[HV_NAMES[hvIdx][2]] != null;
}
function fetchLayoutMode(ins) {
  var layoutMode = ins.layoutMode || ins.constructor.layoutMode;
  return isObject(layoutMode) ? layoutMode : layoutMode ? {
    type: layoutMode
  } : null;
}
function mergeLayoutParam(targetOption, newOption, opt) {
  var ignoreSize = opt && opt.ignoreSize;
  !isArray(ignoreSize) && (ignoreSize = [ignoreSize, ignoreSize]);
  var hResult = merge3(HV_NAMES[0], 0);
  var vResult = merge3(HV_NAMES[1], 1);
  copy3(HV_NAMES[0], targetOption, hResult);
  copy3(HV_NAMES[1], targetOption, vResult);
  function merge3(names, hvIdx) {
    var newParams = {};
    var newValueCount = 0;
    var merged = {};
    var mergedValueCount = 0;
    var enoughParamNumber = 2;
    each2(names, function(name) {
      merged[name] = targetOption[name];
    });
    each2(names, function(name) {
      hasOwn(newOption, name) && (newParams[name] = merged[name] = newOption[name]);
      hasValue(newParams, name) && newValueCount++;
      hasValue(merged, name) && mergedValueCount++;
    });
    if (ignoreSize[hvIdx]) {
      if (hasValue(newOption, names[1])) {
        merged[names[2]] = null;
      } else if (hasValue(newOption, names[2])) {
        merged[names[1]] = null;
      }
      return merged;
    }
    if (mergedValueCount === enoughParamNumber || !newValueCount) {
      return merged;
    } else if (newValueCount >= enoughParamNumber) {
      return newParams;
    } else {
      for (var i2 = 0; i2 < names.length; i2++) {
        var name_1 = names[i2];
        if (!hasOwn(newParams, name_1) && hasOwn(targetOption, name_1)) {
          newParams[name_1] = targetOption[name_1];
          break;
        }
      }
      return newParams;
    }
  }
  function hasValue(obj, name) {
    return obj[name] != null && obj[name] !== "auto";
  }
  function copy3(names, target, source) {
    each2(names, function(name) {
      target[name] = source[name];
    });
  }
}
function getLayoutParams(source) {
  return copyLayoutParams({}, source);
}
function copyLayoutParams(target, source) {
  source && target && each2(LOCATION_PARAMS, function(name) {
    hasOwn(source, name) && (target[name] = source[name]);
  });
  return target;
}

// node_modules/echarts/lib/model/Component.js
var inner2 = makeInner();
var ComponentModel = (
  /** @class */
  (function(_super) {
    __extends(ComponentModel2, _super);
    function ComponentModel2(option, parentModel, ecModel) {
      var _this = _super.call(this, option, parentModel, ecModel) || this;
      _this.uid = getUID("ec_cpt_model");
      return _this;
    }
    ComponentModel2.prototype.init = function(option, parentModel, ecModel) {
      this.mergeDefaultAndTheme(option, ecModel);
    };
    ComponentModel2.prototype.mergeDefaultAndTheme = function(option, ecModel) {
      var layoutMode = fetchLayoutMode(this);
      var inputPositionParams = layoutMode ? getLayoutParams(option) : {};
      var themeModel = ecModel.getTheme();
      merge2(option, themeModel.get(this.mainType));
      merge2(option, this.getDefaultOption());
      if (layoutMode) {
        mergeLayoutParam(option, inputPositionParams, layoutMode);
      }
    };
    ComponentModel2.prototype.mergeOption = function(option, ecModel) {
      merge2(this.option, option, true);
      var layoutMode = fetchLayoutMode(this);
      if (layoutMode) {
        mergeLayoutParam(this.option, option, layoutMode);
      }
    };
    ComponentModel2.prototype.optionUpdated = function(newCptOption, isInit) {
    };
    ComponentModel2.prototype.getDefaultOption = function() {
      var ctor = this.constructor;
      if (!isExtendedClass(ctor)) {
        return ctor.defaultOption;
      }
      var fields = inner2(this);
      if (!fields.defaultOption) {
        var optList = [];
        var clz = ctor;
        while (clz) {
          var opt = clz.prototype.defaultOption;
          opt && optList.push(opt);
          clz = clz.superClass;
        }
        var defaultOption3 = {};
        for (var i2 = optList.length - 1; i2 >= 0; i2--) {
          defaultOption3 = merge2(defaultOption3, optList[i2], true);
        }
        fields.defaultOption = defaultOption3;
      }
      return fields.defaultOption;
    };
    ComponentModel2.prototype.getReferringComponents = function(mainType, opt) {
      var indexKey = mainType + "Index";
      var idKey = mainType + "Id";
      return queryReferringComponents(this.ecModel, mainType, {
        index: this.get(indexKey, true),
        id: this.get(idKey, true)
      }, opt);
    };
    ComponentModel2.prototype.getBoxLayoutParams = function() {
      return getBoxLayoutParams(this, false);
    };
    ComponentModel2.prototype.getZLevelKey = function() {
      return "";
    };
    ComponentModel2.prototype.setZLevel = function(zlevel) {
      this.option.zlevel = zlevel;
    };
    ComponentModel2.protoInitialize = (function() {
      var proto = ComponentModel2.prototype;
      proto.type = "component";
      proto.id = "";
      proto.name = "";
      proto.mainType = "";
      proto.subType = "";
      proto.componentIndex = 0;
    })();
    return ComponentModel2;
  })(Model_default)
);
mountExtend(ComponentModel, Model_default);
enableClassManagement(ComponentModel);
enableSubTypeDefaulter(ComponentModel);
enableTopologicalTravel(ComponentModel, getDependencies);
function getDependencies(componentType) {
  var deps = [];
  each(ComponentModel.getClassesByMainType(componentType), function(clz) {
    deps = deps.concat(clz.dependencies || clz.prototype.dependencies || []);
  });
  deps = map2(deps, function(type) {
    return parseClassType(type).main;
  });
  if (componentType !== "dataset" && indexOf(deps, "dataset") <= 0) {
    deps.unshift("dataset");
  }
  return deps;
}
var Component_default = ComponentModel;

// node_modules/echarts/lib/model/mixin/palette.js
var innerColor = makeInner();
var innerDecal = makeInner();
var PaletteMixin = (
  /** @class */
  (function() {
    function PaletteMixin2() {
    }
    PaletteMixin2.prototype.getColorFromPalette = function(name, scope, requestNum) {
      var defaultPalette = normalizeToArray(this.get("color", true));
      var layeredPalette = this.get("colorLayer", true);
      return getFromPalette(this, innerColor, defaultPalette, layeredPalette, name, scope, requestNum);
    };
    PaletteMixin2.prototype.clearColorPalette = function() {
      clearPalette(this, innerColor);
    };
    return PaletteMixin2;
  })()
);
function getDecalFromPalette(ecModel, name, scope, requestNum) {
  var defaultDecals = normalizeToArray(ecModel.get(["aria", "decal", "decals"]));
  return getFromPalette(ecModel, innerDecal, defaultDecals, null, name, scope, requestNum);
}
function getNearestPalette(palettes, requestColorNum) {
  var paletteNum = palettes.length;
  for (var i2 = 0; i2 < paletteNum; i2++) {
    if (palettes[i2].length > requestColorNum) {
      return palettes[i2];
    }
  }
  return palettes[paletteNum - 1];
}
function getFromPalette(that, inner25, defaultPalette, layeredPalette, name, scope, requestNum) {
  scope = scope || that;
  var scopeFields = inner25(scope);
  var paletteIdx = scopeFields.paletteIdx || 0;
  var paletteNameMap = scopeFields.paletteNameMap = scopeFields.paletteNameMap || {};
  if (paletteNameMap.hasOwnProperty(name)) {
    return paletteNameMap[name];
  }
  var palette = requestNum == null || !layeredPalette ? defaultPalette : getNearestPalette(layeredPalette, requestNum);
  palette = palette || defaultPalette;
  if (!palette || !palette.length) {
    return;
  }
  var pickedPaletteItem = palette[paletteIdx];
  if (name) {
    paletteNameMap[name] = pickedPaletteItem;
  }
  scopeFields.paletteIdx = (paletteIdx + 1) % palette.length;
  return pickedPaletteItem;
}
function clearPalette(that, inner25) {
  inner25(that).paletteIdx = 0;
  inner25(that).paletteNameMap = {};
}

// node_modules/echarts/lib/model/mixin/dataFormat.js
var DIMENSION_LABEL_REG = /\{@(.+?)\}/g;
var DataFormatMixin = (
  /** @class */
  (function() {
    function DataFormatMixin2() {
    }
    DataFormatMixin2.prototype.getDataParams = function(dataIndex, dataType) {
      var data = this.getData(dataType);
      var rawValue = this.getRawValue(dataIndex, dataType);
      var rawDataIndex = data.getRawIndex(dataIndex);
      var name = data.getName(dataIndex);
      var itemOpt = data.getRawDataItem(dataIndex);
      var style2 = data.getItemVisual(dataIndex, "style");
      var color3 = style2 && style2[data.getItemVisual(dataIndex, "drawType") || "fill"];
      var borderColor = style2 && style2.stroke;
      var mainType = this.mainType;
      var isSeries2 = mainType === "series";
      var userOutput = data.userOutput && data.userOutput.get();
      return {
        componentType: mainType,
        componentSubType: this.subType,
        componentIndex: this.componentIndex,
        seriesType: isSeries2 ? this.subType : null,
        seriesIndex: this.seriesIndex,
        seriesId: isSeries2 ? this.id : null,
        seriesName: isSeries2 ? this.name : null,
        name,
        dataIndex: rawDataIndex,
        data: itemOpt,
        dataType,
        value: rawValue,
        color: color3,
        borderColor,
        dimensionNames: userOutput ? userOutput.fullDimensions : null,
        encode: userOutput ? userOutput.encode : null,
        // Param name list for mapping `a`, `b`, `c`, `d`, `e`
        $vars: ["seriesName", "name", "value"]
      };
    };
    DataFormatMixin2.prototype.getFormattedLabel = function(dataIndex, status, dataType, labelDimIndex, formatter, extendParams) {
      status = status || "normal";
      var data = this.getData(dataType);
      var params = this.getDataParams(dataIndex, dataType);
      if (extendParams) {
        params.value = extendParams.interpolatedValue;
      }
      if (labelDimIndex != null && isArray(params.value)) {
        params.value = params.value[labelDimIndex];
      }
      if (!formatter) {
        var itemModel = data.getItemModel(dataIndex);
        formatter = itemModel.get(status === "normal" ? ["label", "formatter"] : [status, "label", "formatter"]);
      }
      if (isFunction(formatter)) {
        params.status = status;
        params.dimensionIndex = labelDimIndex;
        return formatter(params);
      } else if (isString(formatter)) {
        var str = formatTpl(formatter, params);
        return str.replace(DIMENSION_LABEL_REG, function(origin, dimStr) {
          var len2 = dimStr.length;
          var dimLoose = dimStr;
          if (dimLoose.charAt(0) === "[" && dimLoose.charAt(len2 - 1) === "]") {
            dimLoose = +dimLoose.slice(1, len2 - 1);
            if (true) {
              if (isNaN(dimLoose)) {
                error("Invalide label formatter: @" + dimStr + ", only support @[0], @[1], @[2], ...");
              }
            }
          }
          var val = retrieveRawValue(data, dataIndex, dimLoose);
          if (extendParams && isArray(extendParams.interpolatedValue)) {
            var dimIndex = data.getDimensionIndex(dimLoose);
            if (dimIndex >= 0) {
              val = extendParams.interpolatedValue[dimIndex];
            }
          }
          return val != null ? val + "" : "";
        });
      }
    };
    DataFormatMixin2.prototype.getRawValue = function(idx, dataType) {
      return retrieveRawValue(this.getData(dataType), idx);
    };
    DataFormatMixin2.prototype.formatTooltip = function(dataIndex, multipleSeries, dataType) {
      return;
    };
    return DataFormatMixin2;
  })()
);
function normalizeTooltipFormatResult(result) {
  var markupText;
  var markupFragment;
  if (isObject(result)) {
    if (result.type) {
      markupFragment = result;
    } else {
      if (true) {
        console.warn("The return type of `formatTooltip` is not supported: " + makePrintable(result));
      }
    }
  } else {
    markupText = result;
  }
  return {
    text: markupText,
    // markers: markers || markersExisting,
    frag: markupFragment
  };
}

// node_modules/echarts/lib/core/task.js
function createTask(define) {
  return new Task(define);
}
var Task = (
  /** @class */
  (function() {
    function Task2(define) {
      define = define || {};
      this._reset = define.reset;
      this._plan = define.plan;
      this._count = define.count;
      this._onDirty = define.onDirty;
      this._dirty = true;
    }
    Task2.prototype.perform = function(performArgs) {
      var upTask = this._upstream;
      var skip = performArgs && performArgs.skip;
      if (this._dirty && upTask) {
        var context = this.context;
        context.data = context.outputData = upTask.context.outputData;
      }
      if (this.__pipeline) {
        this.__pipeline.currentTask = this;
      }
      var planResult;
      if (this._plan && !skip) {
        planResult = this._plan(this.context);
      }
      var lastModBy = normalizeModBy(this._modBy);
      var lastModDataCount = this._modDataCount || 0;
      var modBy = normalizeModBy(performArgs && performArgs.modBy);
      var modDataCount = performArgs && performArgs.modDataCount || 0;
      if (lastModBy !== modBy || lastModDataCount !== modDataCount) {
        planResult = "reset";
      }
      function normalizeModBy(val) {
        !(val >= 1) && (val = 1);
        return val;
      }
      var forceFirstProgress;
      if (this._dirty || planResult === "reset") {
        this._dirty = false;
        forceFirstProgress = this._doReset(skip);
      }
      this._modBy = modBy;
      this._modDataCount = modDataCount;
      var step = performArgs && performArgs.step;
      if (upTask) {
        if (true) {
          assert(upTask._outputDueEnd != null);
        }
        this._dueEnd = upTask._outputDueEnd;
      } else {
        if (true) {
          assert(!this._progress || this._count);
        }
        this._dueEnd = this._count ? this._count(this.context) : Infinity;
      }
      if (this._progress) {
        var start2 = this._dueIndex;
        var end2 = Math.min(step != null ? this._dueIndex + step : Infinity, this._dueEnd);
        if (!skip && (forceFirstProgress || start2 < end2)) {
          var progress = this._progress;
          if (isArray(progress)) {
            for (var i2 = 0; i2 < progress.length; i2++) {
              this._doProgress(progress[i2], start2, end2, modBy, modDataCount);
            }
          } else {
            this._doProgress(progress, start2, end2, modBy, modDataCount);
          }
        }
        this._dueIndex = end2;
        var outputDueEnd = this._settedOutputEnd != null ? this._settedOutputEnd : end2;
        if (true) {
          assert(outputDueEnd >= this._outputDueEnd);
        }
        this._outputDueEnd = outputDueEnd;
      } else {
        this._dueIndex = this._outputDueEnd = this._settedOutputEnd != null ? this._settedOutputEnd : this._dueEnd;
      }
      return this.unfinished();
    };
    Task2.prototype.dirty = function() {
      this._dirty = true;
      this._onDirty && this._onDirty(this.context);
    };
    Task2.prototype._doProgress = function(progress, start2, end2, modBy, modDataCount) {
      iterator.reset(start2, end2, modBy, modDataCount);
      this._callingProgress = progress;
      this._callingProgress({
        start: start2,
        end: end2,
        count: end2 - start2,
        next: iterator.next
      }, this.context);
    };
    Task2.prototype._doReset = function(skip) {
      this._dueIndex = this._outputDueEnd = this._dueEnd = 0;
      this._settedOutputEnd = null;
      var progress;
      var forceFirstProgress;
      if (!skip && this._reset) {
        progress = this._reset(this.context);
        if (progress && progress.progress) {
          forceFirstProgress = progress.forceFirstProgress;
          progress = progress.progress;
        }
        if (isArray(progress) && !progress.length) {
          progress = null;
        }
      }
      this._progress = progress;
      this._modBy = this._modDataCount = null;
      var downstream = this._downstream;
      downstream && downstream.dirty();
      return forceFirstProgress;
    };
    Task2.prototype.unfinished = function() {
      return this._progress && this._dueIndex < this._dueEnd;
    };
    Task2.prototype.pipe = function(downTask) {
      if (true) {
        assert(downTask && !downTask._disposed && downTask !== this);
      }
      if (this._downstream !== downTask || this._dirty) {
        this._downstream = downTask;
        downTask._upstream = this;
        downTask.dirty();
      }
    };
    Task2.prototype.dispose = function() {
      if (this._disposed) {
        return;
      }
      this._upstream && (this._upstream._downstream = null);
      this._downstream && (this._downstream._upstream = null);
      this._dirty = false;
      this._disposed = true;
    };
    Task2.prototype.getUpstream = function() {
      return this._upstream;
    };
    Task2.prototype.getDownstream = function() {
      return this._downstream;
    };
    Task2.prototype.setOutputEnd = function(end2) {
      this._outputDueEnd = this._settedOutputEnd = end2;
    };
    return Task2;
  })()
);
var iterator = /* @__PURE__ */ (function() {
  var end2;
  var current;
  var modBy;
  var modDataCount;
  var winCount;
  var it = {
    reset: function(s, e2, sStep, sCount) {
      current = s;
      end2 = e2;
      modBy = sStep;
      modDataCount = sCount;
      winCount = Math.ceil(modDataCount / modBy);
      it.next = modBy > 1 && modDataCount > 0 ? modNext : sequentialNext;
    }
  };
  return it;
  function sequentialNext() {
    return current < end2 ? current++ : null;
  }
  function modNext() {
    var dataIndex = current % winCount * modBy + Math.ceil(current / winCount);
    var result = current >= end2 ? null : dataIndex < modDataCount ? dataIndex : current;
    current++;
    return result;
  }
})();

// node_modules/echarts/lib/data/helper/transform.js
var ExternalSource = (
  /** @class */
  (function() {
    function ExternalSource2() {
    }
    ExternalSource2.prototype.getRawData = function() {
      throw new Error("not supported");
    };
    ExternalSource2.prototype.getRawDataItem = function(dataIndex) {
      throw new Error("not supported");
    };
    ExternalSource2.prototype.cloneRawData = function() {
      return;
    };
    ExternalSource2.prototype.getDimensionInfo = function(dim) {
      return;
    };
    ExternalSource2.prototype.cloneAllDimensionInfo = function() {
      return;
    };
    ExternalSource2.prototype.count = function() {
      return;
    };
    ExternalSource2.prototype.retrieveValue = function(dataIndex, dimIndex) {
      return;
    };
    ExternalSource2.prototype.retrieveValueFromItem = function(dataItem, dimIndex) {
      return;
    };
    ExternalSource2.prototype.convertValue = function(rawVal, dimInfo) {
      return parseDataValue(rawVal, dimInfo);
    };
    return ExternalSource2;
  })()
);
function createExternalSource(internalSource, externalTransform) {
  var extSource = new ExternalSource();
  var data = internalSource.data;
  var sourceFormat = extSource.sourceFormat = internalSource.sourceFormat;
  var sourceHeaderCount = internalSource.startIndex;
  var errMsg = "";
  if (internalSource.seriesLayoutBy !== SERIES_LAYOUT_BY_COLUMN) {
    if (true) {
      errMsg = '`seriesLayoutBy` of upstream dataset can only be "column" in data transform.';
    }
    throwError2(errMsg);
  }
  var dimensions = [];
  var dimsByName = {};
  var dimsDef = internalSource.dimensionsDefine;
  if (dimsDef) {
    each(dimsDef, function(dimDef, idx) {
      var name = dimDef.name;
      var dimDefExt = {
        index: idx,
        name,
        displayName: dimDef.displayName
      };
      dimensions.push(dimDefExt);
      if (name != null) {
        var errMsg_1 = "";
        if (hasOwn(dimsByName, name)) {
          if (true) {
            errMsg_1 = 'dimension name "' + name + '" duplicated.';
          }
          throwError2(errMsg_1);
        }
        dimsByName[name] = dimDefExt;
      }
    });
  } else {
    for (var i2 = 0; i2 < internalSource.dimensionsDetectedCount || 0; i2++) {
      dimensions.push({
        index: i2
      });
    }
  }
  var rawItemGetter = getRawSourceItemGetter(sourceFormat, SERIES_LAYOUT_BY_COLUMN);
  if (externalTransform.__isBuiltIn) {
    extSource.getRawDataItem = function(dataIndex) {
      return rawItemGetter(data, sourceHeaderCount, dimensions, dataIndex);
    };
    extSource.getRawData = bind(getRawData, null, internalSource);
  }
  extSource.cloneRawData = bind(cloneRawData, null, internalSource);
  var rawCounter = getRawSourceDataCounter(sourceFormat, SERIES_LAYOUT_BY_COLUMN);
  extSource.count = bind(rawCounter, null, data, sourceHeaderCount, dimensions);
  var rawValueGetter = getRawSourceValueGetter(sourceFormat);
  extSource.retrieveValue = function(dataIndex, dimIndex) {
    var rawItem = rawItemGetter(data, sourceHeaderCount, dimensions, dataIndex);
    return retrieveValueFromItem(rawItem, dimIndex);
  };
  var retrieveValueFromItem = extSource.retrieveValueFromItem = function(dataItem, dimIndex) {
    if (dataItem == null) {
      return;
    }
    var dimDef = dimensions[dimIndex];
    if (dimDef) {
      return rawValueGetter(dataItem, dimIndex, dimDef.name);
    }
  };
  extSource.getDimensionInfo = bind(getDimensionInfo, null, dimensions, dimsByName);
  extSource.cloneAllDimensionInfo = bind(cloneAllDimensionInfo, null, dimensions);
  return extSource;
}
function getRawData(upstream) {
  var sourceFormat = upstream.sourceFormat;
  if (!isSupportedSourceFormat(sourceFormat)) {
    var errMsg = "";
    if (true) {
      errMsg = "`getRawData` is not supported in source format " + sourceFormat;
    }
    throwError2(errMsg);
  }
  return upstream.data;
}
function cloneRawData(upstream) {
  var sourceFormat = upstream.sourceFormat;
  var data = upstream.data;
  if (!isSupportedSourceFormat(sourceFormat)) {
    var errMsg = "";
    if (true) {
      errMsg = "`cloneRawData` is not supported in source format " + sourceFormat;
    }
    throwError2(errMsg);
  }
  if (sourceFormat === SOURCE_FORMAT_ARRAY_ROWS) {
    var result = [];
    for (var i2 = 0, len2 = data.length; i2 < len2; i2++) {
      result.push(data[i2].slice());
    }
    return result;
  } else if (sourceFormat === SOURCE_FORMAT_OBJECT_ROWS) {
    var result = [];
    for (var i2 = 0, len2 = data.length; i2 < len2; i2++) {
      result.push(extend({}, data[i2]));
    }
    return result;
  }
}
function getDimensionInfo(dimensions, dimsByName, dim) {
  if (dim == null) {
    return;
  }
  if (isNumber(dim) || !isNaN(dim) && !hasOwn(dimsByName, dim)) {
    return dimensions[dim];
  } else if (hasOwn(dimsByName, dim)) {
    return dimsByName[dim];
  }
}
function cloneAllDimensionInfo(dimensions) {
  return clone(dimensions);
}
var externalTransformMap = createHashMap();
function registerExternalTransform(externalTransform) {
  externalTransform = clone(externalTransform);
  var type = externalTransform.type;
  var errMsg = "";
  if (!type) {
    if (true) {
      errMsg = "Must have a `type` when `registerTransform`.";
    }
    throwError2(errMsg);
  }
  var typeParsed = type.split(":");
  if (typeParsed.length !== 2) {
    if (true) {
      errMsg = 'Name must include namespace like "ns:regression".';
    }
    throwError2(errMsg);
  }
  var isBuiltIn = false;
  if (typeParsed[0] === "echarts") {
    type = typeParsed[1];
    isBuiltIn = true;
  }
  externalTransform.__isBuiltIn = isBuiltIn;
  externalTransformMap.set(type, externalTransform);
}
function applyDataTransform(rawTransOption, sourceList, infoForPrint) {
  var pipedTransOption = normalizeToArray(rawTransOption);
  var pipeLen = pipedTransOption.length;
  var errMsg = "";
  if (!pipeLen) {
    if (true) {
      errMsg = "If `transform` declared, it should at least contain one transform.";
    }
    throwError2(errMsg);
  }
  for (var i2 = 0, len2 = pipeLen; i2 < len2; i2++) {
    var transOption = pipedTransOption[i2];
    sourceList = applySingleDataTransform(transOption, sourceList, infoForPrint, pipeLen === 1 ? null : i2);
    if (i2 !== len2 - 1) {
      sourceList.length = Math.max(sourceList.length, 1);
    }
  }
  return sourceList;
}
function applySingleDataTransform(transOption, upSourceList, infoForPrint, pipeIndex) {
  var errMsg = "";
  if (!upSourceList.length) {
    if (true) {
      errMsg = "Must have at least one upstream dataset.";
    }
    throwError2(errMsg);
  }
  if (!isObject(transOption)) {
    if (true) {
      errMsg = "transform declaration must be an object rather than " + typeof transOption + ".";
    }
    throwError2(errMsg);
  }
  var transType = transOption.type;
  var externalTransform = externalTransformMap.get(transType);
  if (!externalTransform) {
    if (true) {
      errMsg = 'Can not find transform on type "' + transType + '".';
    }
    throwError2(errMsg);
  }
  var extUpSourceList = map2(upSourceList, function(upSource) {
    return createExternalSource(upSource, externalTransform);
  });
  var resultList = normalizeToArray(externalTransform.transform({
    upstream: extUpSourceList[0],
    upstreamList: extUpSourceList,
    config: clone(transOption.config)
  }));
  if (true) {
    if (transOption.print) {
      var printStrArr = map2(resultList, function(extSource) {
        var pipeIndexStr = pipeIndex != null ? " === pipe index: " + pipeIndex : "";
        return ["=== dataset index: " + infoForPrint.datasetIndex + pipeIndexStr + " ===", "- transform result data:", makePrintable(extSource.data), "- transform result dimensions:", makePrintable(extSource.dimensions)].join("\n");
      }).join("\n");
      log(printStrArr);
    }
  }
  return map2(resultList, function(result, resultIndex) {
    var errMsg2 = "";
    if (!isObject(result)) {
      if (true) {
        errMsg2 = "A transform should not return some empty results.";
      }
      throwError2(errMsg2);
    }
    if (!result.data) {
      if (true) {
        errMsg2 = "Transform result data should be not be null or undefined";
      }
      throwError2(errMsg2);
    }
    var sourceFormat = detectSourceFormat(result.data);
    if (!isSupportedSourceFormat(sourceFormat)) {
      if (true) {
        errMsg2 = "Transform result data should be array rows or object rows.";
      }
      throwError2(errMsg2);
    }
    var resultMetaRawOption;
    var firstUpSource = upSourceList[0];
    if (firstUpSource && resultIndex === 0 && !result.dimensions) {
      var startIndex = firstUpSource.startIndex;
      if (startIndex) {
        result.data = firstUpSource.data.slice(0, startIndex).concat(result.data);
      }
      resultMetaRawOption = {
        seriesLayoutBy: SERIES_LAYOUT_BY_COLUMN,
        sourceHeader: startIndex,
        dimensions: firstUpSource.metaRawOption.dimensions
      };
    } else {
      resultMetaRawOption = {
        seriesLayoutBy: SERIES_LAYOUT_BY_COLUMN,
        sourceHeader: 0,
        dimensions: result.dimensions
      };
    }
    return createSource(result.data, resultMetaRawOption, null);
  });
}
function isSupportedSourceFormat(sourceFormat) {
  return sourceFormat === SOURCE_FORMAT_ARRAY_ROWS || sourceFormat === SOURCE_FORMAT_OBJECT_ROWS;
}

// node_modules/echarts/lib/data/helper/sourceManager.js
var SourceManager = (
  /** @class */
  (function() {
    function SourceManager2(sourceHost) {
      this._sourceList = [];
      this._storeList = [];
      this._upstreamSignList = [];
      this._versionSignBase = 0;
      this._dirty = true;
      this._sourceHost = sourceHost;
    }
    SourceManager2.prototype.dirty = function() {
      this._setLocalSource([], []);
      this._storeList = [];
      this._dirty = true;
    };
    SourceManager2.prototype._setLocalSource = function(sourceList, upstreamSignList) {
      this._sourceList = sourceList;
      this._upstreamSignList = upstreamSignList;
      this._versionSignBase++;
      if (this._versionSignBase > 9e10) {
        this._versionSignBase = 0;
      }
    };
    SourceManager2.prototype._getVersionSign = function() {
      return this._sourceHost.uid + "_" + this._versionSignBase;
    };
    SourceManager2.prototype.prepareSource = function() {
      if (this._isDirty()) {
        this._createSource();
        this._dirty = false;
      }
    };
    SourceManager2.prototype._createSource = function() {
      this._setLocalSource([], []);
      var sourceHost = this._sourceHost;
      var upSourceMgrList = this._getUpstreamSourceManagers();
      var hasUpstream = !!upSourceMgrList.length;
      var resultSourceList;
      var upstreamSignList;
      if (isSeries(sourceHost)) {
        var seriesModel = sourceHost;
        var data = void 0;
        var sourceFormat = void 0;
        var upSource = void 0;
        if (hasUpstream) {
          var upSourceMgr = upSourceMgrList[0];
          upSourceMgr.prepareSource();
          upSource = upSourceMgr.getSource();
          data = upSource.data;
          sourceFormat = upSource.sourceFormat;
          upstreamSignList = [upSourceMgr._getVersionSign()];
        } else {
          data = seriesModel.get("data", true);
          sourceFormat = isTypedArray(data) ? SOURCE_FORMAT_TYPED_ARRAY : SOURCE_FORMAT_ORIGINAL;
          upstreamSignList = [];
        }
        var newMetaRawOption = this._getSourceMetaRawOption() || {};
        var upMetaRawOption = upSource && upSource.metaRawOption || {};
        var seriesLayoutBy = retrieve2(newMetaRawOption.seriesLayoutBy, upMetaRawOption.seriesLayoutBy) || null;
        var sourceHeader = retrieve2(newMetaRawOption.sourceHeader, upMetaRawOption.sourceHeader);
        var dimensions = retrieve2(newMetaRawOption.dimensions, upMetaRawOption.dimensions);
        var needsCreateSource = seriesLayoutBy !== upMetaRawOption.seriesLayoutBy || !!sourceHeader !== !!upMetaRawOption.sourceHeader || dimensions;
        resultSourceList = needsCreateSource ? [createSource(data, {
          seriesLayoutBy,
          sourceHeader,
          dimensions
        }, sourceFormat)] : [];
      } else {
        var datasetModel = sourceHost;
        if (hasUpstream) {
          var result = this._applyTransform(upSourceMgrList);
          resultSourceList = result.sourceList;
          upstreamSignList = result.upstreamSignList;
        } else {
          var sourceData = datasetModel.get("source", true);
          resultSourceList = [createSource(sourceData, this._getSourceMetaRawOption(), null)];
          upstreamSignList = [];
        }
      }
      if (true) {
        assert(resultSourceList && upstreamSignList);
      }
      this._setLocalSource(resultSourceList, upstreamSignList);
    };
    SourceManager2.prototype._applyTransform = function(upMgrList) {
      var datasetModel = this._sourceHost;
      var transformOption = datasetModel.get("transform", true);
      var fromTransformResult = datasetModel.get("fromTransformResult", true);
      if (true) {
        assert(fromTransformResult != null || transformOption != null);
      }
      if (fromTransformResult != null) {
        var errMsg = "";
        if (upMgrList.length !== 1) {
          if (true) {
            errMsg = "When using `fromTransformResult`, there should be only one upstream dataset";
          }
          doThrow(errMsg);
        }
      }
      var sourceList;
      var upSourceList = [];
      var upstreamSignList = [];
      each(upMgrList, function(upMgr) {
        upMgr.prepareSource();
        var upSource = upMgr.getSource(fromTransformResult || 0);
        var errMsg2 = "";
        if (fromTransformResult != null && !upSource) {
          if (true) {
            errMsg2 = "Can not retrieve result by `fromTransformResult`: " + fromTransformResult;
          }
          doThrow(errMsg2);
        }
        upSourceList.push(upSource);
        upstreamSignList.push(upMgr._getVersionSign());
      });
      if (transformOption) {
        sourceList = applyDataTransform(transformOption, upSourceList, {
          datasetIndex: datasetModel.componentIndex
        });
      } else if (fromTransformResult != null) {
        sourceList = [cloneSourceShallow(upSourceList[0])];
      }
      return {
        sourceList,
        upstreamSignList
      };
    };
    SourceManager2.prototype._isDirty = function() {
      if (this._dirty) {
        return true;
      }
      var upSourceMgrList = this._getUpstreamSourceManagers();
      for (var i2 = 0; i2 < upSourceMgrList.length; i2++) {
        var upSrcMgr = upSourceMgrList[i2];
        if (
          // Consider the case that there is ancestor diry, call it recursively.
          // The performance is probably not an issue because usually the chain is not long.
          upSrcMgr._isDirty() || this._upstreamSignList[i2] !== upSrcMgr._getVersionSign()
        ) {
          return true;
        }
      }
    };
    SourceManager2.prototype.getSource = function(sourceIndex) {
      sourceIndex = sourceIndex || 0;
      var source = this._sourceList[sourceIndex];
      if (!source) {
        var upSourceMgrList = this._getUpstreamSourceManagers();
        return upSourceMgrList[0] && upSourceMgrList[0].getSource(sourceIndex);
      }
      return source;
    };
    SourceManager2.prototype.getSharedDataStore = function(seriesDimRequest) {
      if (true) {
        assert(isSeries(this._sourceHost), "Can only call getDataStore on series source manager.");
      }
      var schema = seriesDimRequest.makeStoreSchema();
      return this._innerGetDataStore(schema.dimensions, seriesDimRequest.source, schema.hash);
    };
    SourceManager2.prototype._innerGetDataStore = function(storeDims, seriesSource, sourceReadKey) {
      var sourceIndex = 0;
      var storeList = this._storeList;
      var cachedStoreMap = storeList[sourceIndex];
      if (!cachedStoreMap) {
        cachedStoreMap = storeList[sourceIndex] = {};
      }
      var cachedStore = cachedStoreMap[sourceReadKey];
      if (!cachedStore) {
        var upSourceMgr = this._getUpstreamSourceManagers()[0];
        if (isSeries(this._sourceHost) && upSourceMgr) {
          cachedStore = upSourceMgr._innerGetDataStore(storeDims, seriesSource, sourceReadKey);
        } else {
          cachedStore = new DataStore_default();
          cachedStore.initData(new DefaultDataProvider(seriesSource, storeDims.length), storeDims);
        }
        cachedStoreMap[sourceReadKey] = cachedStore;
      }
      return cachedStore;
    };
    SourceManager2.prototype._getUpstreamSourceManagers = function() {
      var sourceHost = this._sourceHost;
      if (isSeries(sourceHost)) {
        var datasetModel = querySeriesUpstreamDatasetModel(sourceHost);
        return !datasetModel ? [] : [datasetModel.getSourceManager()];
      } else {
        return map2(queryDatasetUpstreamDatasetModels(sourceHost), function(datasetModel2) {
          return datasetModel2.getSourceManager();
        });
      }
    };
    SourceManager2.prototype._getSourceMetaRawOption = function() {
      var sourceHost = this._sourceHost;
      var seriesLayoutBy;
      var sourceHeader;
      var dimensions;
      if (isSeries(sourceHost)) {
        seriesLayoutBy = sourceHost.get("seriesLayoutBy", true);
        sourceHeader = sourceHost.get("sourceHeader", true);
        dimensions = sourceHost.get("dimensions", true);
      } else if (!this._getUpstreamSourceManagers().length) {
        var model2 = sourceHost;
        seriesLayoutBy = model2.get("seriesLayoutBy", true);
        sourceHeader = model2.get("sourceHeader", true);
        dimensions = model2.get("dimensions", true);
      }
      return {
        seriesLayoutBy,
        sourceHeader,
        dimensions
      };
    };
    return SourceManager2;
  })()
);
function disableTransformOptionMerge(datasetModel) {
  var transformOption = datasetModel.option.transform;
  transformOption && setAsPrimitive(datasetModel.option.transform);
}
function isSeries(sourceHost) {
  return sourceHost.mainType === "series";
}
function doThrow(errMsg) {
  throw new Error(errMsg);
}

// node_modules/echarts/lib/visual/tokens.js
var tokens = {
  color: {},
  darkColor: {},
  size: {}
};
var color = tokens.color = {
  theme: ["#5070dd", "#b6d634", "#505372", "#ff994d", "#0ca8df", "#ffd10a", "#fb628b", "#785db0", "#3fbe95"],
  neutral00: "#fff",
  neutral05: "#f4f7fd",
  neutral10: "#e8ebf0",
  neutral15: "#dbdee4",
  neutral20: "#cfd2d7",
  neutral25: "#c3c5cb",
  neutral30: "#b7b9be",
  neutral35: "#aaacb2",
  neutral40: "#9ea0a5",
  neutral45: "#929399",
  neutral50: "#86878c",
  neutral55: "#797b7f",
  neutral60: "#6d6e73",
  neutral65: "#616266",
  neutral70: "#54555a",
  neutral75: "#48494d",
  neutral80: "#3c3c41",
  neutral85: "#303034",
  neutral90: "#232328",
  neutral95: "#17171b",
  neutral99: "#000",
  accent05: "#eff1f9",
  accent10: "#e0e4f2",
  accent15: "#d0d6ec",
  accent20: "#c0c9e6",
  accent25: "#b1bbdf",
  accent30: "#a1aed9",
  accent35: "#91a0d3",
  accent40: "#8292cc",
  accent45: "#7285c6",
  accent50: "#6578ba",
  accent55: "#5c6da9",
  accent60: "#536298",
  accent65: "#4a5787",
  accent70: "#404c76",
  accent75: "#374165",
  accent80: "#2e3654",
  accent85: "#252b43",
  accent90: "#1b2032",
  accent95: "#121521",
  transparent: "rgba(0,0,0,0)",
  highlight: "rgba(255,231,130,0.8)"
};
extend(color, {
  primary: color.neutral80,
  secondary: color.neutral70,
  tertiary: color.neutral60,
  quaternary: color.neutral50,
  disabled: color.neutral20,
  border: color.neutral30,
  borderTint: color.neutral20,
  borderShade: color.neutral40,
  background: color.neutral05,
  backgroundTint: "rgba(234,237,245,0.5)",
  backgroundTransparent: "rgba(255,255,255,0)",
  backgroundShade: color.neutral10,
  shadow: "rgba(0,0,0,0.2)",
  shadowTint: "rgba(129,130,136,0.2)",
  axisLine: color.neutral70,
  axisLineTint: color.neutral40,
  axisTick: color.neutral70,
  axisTickMinor: color.neutral60,
  axisLabel: color.neutral70,
  axisSplitLine: color.neutral15,
  axisMinorSplitLine: color.neutral05
});
for (key in color) {
  if (color.hasOwnProperty(key)) {
    hex = color[key];
    if (key === "theme") {
      tokens.darkColor.theme = color.theme.slice();
    } else if (key === "highlight") {
      tokens.darkColor.highlight = "rgba(255,231,130,0.4)";
    } else if (key.indexOf("accent") === 0) {
      tokens.darkColor[key] = modifyHSL(hex, null, function(s) {
        return s * 0.5;
      }, function(l) {
        return Math.min(1, 1.3 - l);
      });
    } else {
      tokens.darkColor[key] = modifyHSL(hex, null, function(s) {
        return s * 0.9;
      }, function(l) {
        return 1 - Math.pow(l, 1.5);
      });
    }
  }
}
var hex;
var key;
tokens.size = {
  xxs: 2,
  xs: 5,
  s: 10,
  m: 15,
  l: 20,
  xl: 30,
  xxl: 40,
  xxxl: 50
};
var tokens_default = tokens;

// node_modules/echarts/lib/component/tooltip/tooltipMarkup.js
var TOOLTIP_LINE_HEIGHT_CSS = "line-height:1";
function getTooltipLineHeight(textStyle) {
  var lineHeight = textStyle.lineHeight;
  if (lineHeight == null) {
    return TOOLTIP_LINE_HEIGHT_CSS;
  } else {
    return "line-height:" + encodeHTML(lineHeight + "") + "px";
  }
}
function getTooltipTextStyle(textStyle, renderMode) {
  var nameFontColor = textStyle.color || tokens_default.color.tertiary;
  var nameFontSize = textStyle.fontSize || 12;
  var nameFontWeight = textStyle.fontWeight || "400";
  var valueFontColor = textStyle.color || tokens_default.color.secondary;
  var valueFontSize = textStyle.fontSize || 14;
  var valueFontWeight = textStyle.fontWeight || "900";
  if (renderMode === "html") {
    return {
      // eslint-disable-next-line max-len
      nameStyle: "font-size:" + encodeHTML(nameFontSize + "") + "px;color:" + encodeHTML(nameFontColor) + ";font-weight:" + encodeHTML(nameFontWeight + ""),
      // eslint-disable-next-line max-len
      valueStyle: "font-size:" + encodeHTML(valueFontSize + "") + "px;color:" + encodeHTML(valueFontColor) + ";font-weight:" + encodeHTML(valueFontWeight + "")
    };
  } else {
    return {
      nameStyle: {
        fontSize: nameFontSize,
        fill: nameFontColor,
        fontWeight: nameFontWeight
      },
      valueStyle: {
        fontSize: valueFontSize,
        fill: valueFontColor,
        fontWeight: valueFontWeight
      }
    };
  }
}
var HTML_GAPS = [0, 10, 20, 30];
var RICH_TEXT_GAPS = ["", "\n", "\n\n", "\n\n\n"];
function createTooltipMarkup(type, option) {
  option.type = type;
  return option;
}
function isSectionFragment(frag) {
  return frag.type === "section";
}
function getBuilder(frag) {
  return isSectionFragment(frag) ? buildSection : buildNameValue;
}
function getBlockGapLevel(frag) {
  if (isSectionFragment(frag)) {
    var gapLevel_1 = 0;
    var subBlockLen = frag.blocks.length;
    var hasInnerGap_1 = subBlockLen > 1 || subBlockLen > 0 && !frag.noHeader;
    each(frag.blocks, function(subBlock) {
      var subGapLevel = getBlockGapLevel(subBlock);
      if (subGapLevel >= gapLevel_1) {
        gapLevel_1 = subGapLevel + +(hasInnerGap_1 && // 0 always can not be readable gap level.
        (!subGapLevel || isSectionFragment(subBlock) && !subBlock.noHeader));
      }
    });
    return gapLevel_1;
  }
  return 0;
}
function buildSection(ctx, fragment, topMarginForOuterGap, toolTipTextStyle) {
  var noHeader = fragment.noHeader;
  var gaps = getGap(getBlockGapLevel(fragment));
  var subMarkupTextList = [];
  var subBlocks = fragment.blocks || [];
  assert(!subBlocks || isArray(subBlocks));
  subBlocks = subBlocks || [];
  var orderMode = ctx.orderMode;
  if (fragment.sortBlocks && orderMode) {
    subBlocks = subBlocks.slice();
    var orderMap = {
      valueAsc: "asc",
      valueDesc: "desc"
    };
    if (hasOwn(orderMap, orderMode)) {
      var comparator_1 = new SortOrderComparator(orderMap[orderMode], null);
      subBlocks.sort(function(a, b) {
        return comparator_1.evaluate(a.sortParam, b.sortParam);
      });
    } else if (orderMode === "seriesDesc") {
      subBlocks.reverse();
    }
  }
  each(subBlocks, function(subBlock, idx) {
    var valueFormatter = fragment.valueFormatter;
    var subMarkupText2 = getBuilder(subBlock)(
      // Inherit valueFormatter
      valueFormatter ? extend(extend({}, ctx), {
        valueFormatter
      }) : ctx,
      subBlock,
      idx > 0 ? gaps.html : 0,
      toolTipTextStyle
    );
    subMarkupText2 != null && subMarkupTextList.push(subMarkupText2);
  });
  var subMarkupText = ctx.renderMode === "richText" ? subMarkupTextList.join(gaps.richText) : wrapBlockHTML(toolTipTextStyle, subMarkupTextList.join(""), noHeader ? topMarginForOuterGap : gaps.html);
  if (noHeader) {
    return subMarkupText;
  }
  var displayableHeader = makeValueReadable(fragment.header, "ordinal", ctx.useUTC);
  var nameStyle = getTooltipTextStyle(toolTipTextStyle, ctx.renderMode).nameStyle;
  var tooltipLineHeight = getTooltipLineHeight(toolTipTextStyle);
  if (ctx.renderMode === "richText") {
    return wrapInlineNameRichText(ctx, displayableHeader, nameStyle) + gaps.richText + subMarkupText;
  } else {
    return wrapBlockHTML(toolTipTextStyle, '<div style="' + nameStyle + ";" + tooltipLineHeight + ';">' + encodeHTML(displayableHeader) + "</div>" + subMarkupText, topMarginForOuterGap);
  }
}
function buildNameValue(ctx, fragment, topMarginForOuterGap, toolTipTextStyle) {
  var renderMode = ctx.renderMode;
  var noName = fragment.noName;
  var noValue = fragment.noValue;
  var noMarker = !fragment.markerType;
  var name = fragment.name;
  var useUTC = ctx.useUTC;
  var valueFormatter = fragment.valueFormatter || ctx.valueFormatter || function(value) {
    value = isArray(value) ? value : [value];
    return map2(value, function(val, idx) {
      return makeValueReadable(val, isArray(valueTypeOption) ? valueTypeOption[idx] : valueTypeOption, useUTC);
    });
  };
  if (noName && noValue) {
    return;
  }
  var markerStr = noMarker ? "" : ctx.markupStyleCreator.makeTooltipMarker(fragment.markerType, fragment.markerColor || tokens_default.color.secondary, renderMode);
  var readableName = noName ? "" : makeValueReadable(name, "ordinal", useUTC);
  var valueTypeOption = fragment.valueType;
  var readableValueList = noValue ? [] : valueFormatter(fragment.value, fragment.rawDataIndex);
  var valueAlignRight = !noMarker || !noName;
  var valueCloseToMarker = !noMarker && noName;
  var _a2 = getTooltipTextStyle(toolTipTextStyle, renderMode), nameStyle = _a2.nameStyle, valueStyle = _a2.valueStyle;
  return renderMode === "richText" ? (noMarker ? "" : markerStr) + (noName ? "" : wrapInlineNameRichText(ctx, readableName, nameStyle)) + (noValue ? "" : wrapInlineValueRichText(ctx, readableValueList, valueAlignRight, valueCloseToMarker, valueStyle)) : wrapBlockHTML(toolTipTextStyle, (noMarker ? "" : markerStr) + (noName ? "" : wrapInlineNameHTML(readableName, !noMarker, nameStyle)) + (noValue ? "" : wrapInlineValueHTML(readableValueList, valueAlignRight, valueCloseToMarker, valueStyle)), topMarginForOuterGap);
}
function buildTooltipMarkup(fragment, markupStyleCreator, renderMode, orderMode, useUTC, toolTipTextStyle) {
  if (!fragment) {
    return;
  }
  var builder = getBuilder(fragment);
  var ctx = {
    useUTC,
    renderMode,
    orderMode,
    markupStyleCreator,
    valueFormatter: fragment.valueFormatter
  };
  return builder(ctx, fragment, 0, toolTipTextStyle);
}
function getGap(gapLevel) {
  return {
    html: HTML_GAPS[gapLevel],
    richText: RICH_TEXT_GAPS[gapLevel]
  };
}
function wrapBlockHTML(textStyle, encodedContent, topGap) {
  var clearfix = '<div style="clear:both"></div>';
  var marginCSS = "margin: " + topGap + "px 0 0";
  var tooltipLineHeight = getTooltipLineHeight(textStyle);
  return '<div style="' + marginCSS + ";" + tooltipLineHeight + ';">' + encodedContent + clearfix + "</div>";
}
function wrapInlineNameHTML(name, leftHasMarker, style2) {
  var marginCss = leftHasMarker ? "margin-left:2px" : "";
  return '<span style="' + style2 + ";" + marginCss + '">' + encodeHTML(name) + "</span>";
}
function wrapInlineValueHTML(valueList, alignRight, valueCloseToMarker, style2) {
  var paddingStr = valueCloseToMarker ? "10px" : "20px";
  var alignCSS = alignRight ? "float:right;margin-left:" + paddingStr : "";
  valueList = isArray(valueList) ? valueList : [valueList];
  return '<span style="' + alignCSS + ";" + style2 + '">' + map2(valueList, function(value) {
    return encodeHTML(value);
  }).join("&nbsp;&nbsp;") + "</span>";
}
function wrapInlineNameRichText(ctx, name, style2) {
  return ctx.markupStyleCreator.wrapRichTextStyle(name, style2);
}
function wrapInlineValueRichText(ctx, values, alignRight, valueCloseToMarker, style2) {
  var styles = [style2];
  var paddingLeft = valueCloseToMarker ? 10 : 20;
  alignRight && styles.push({
    padding: [0, 0, 0, paddingLeft],
    align: "right"
  });
  return ctx.markupStyleCreator.wrapRichTextStyle(isArray(values) ? values.join("  ") : values, styles);
}
function retrieveVisualColorForTooltipMarker(series, dataIndex) {
  var style2 = series.getData().getItemVisual(dataIndex, "style");
  var color3 = style2[series.visualDrawType];
  return convertToColorString(color3);
}
function getPaddingFromTooltipModel(model2, renderMode) {
  var padding = model2.get("padding");
  return padding != null ? padding : renderMode === "richText" ? [8, 10] : 10;
}
var TooltipMarkupStyleCreator = (
  /** @class */
  (function() {
    function TooltipMarkupStyleCreator2() {
      this.richTextStyles = {};
      this._nextStyleNameId = getRandomIdBase();
    }
    TooltipMarkupStyleCreator2.prototype._generateStyleName = function() {
      return "__EC_aUTo_" + this._nextStyleNameId++;
    };
    TooltipMarkupStyleCreator2.prototype.makeTooltipMarker = function(markerType, colorStr, renderMode) {
      var markerId = renderMode === "richText" ? this._generateStyleName() : null;
      var marker = getTooltipMarker({
        color: colorStr,
        type: markerType,
        renderMode,
        markerId
      });
      if (isString(marker)) {
        return marker;
      } else {
        if (true) {
          assert(markerId);
        }
        this.richTextStyles[markerId] = marker.style;
        return marker.content;
      }
    };
    TooltipMarkupStyleCreator2.prototype.wrapRichTextStyle = function(text, styles) {
      var finalStl = {};
      if (isArray(styles)) {
        each(styles, function(stl) {
          return extend(finalStl, stl);
        });
      } else {
        extend(finalStl, styles);
      }
      var styleName = this._generateStyleName();
      this.richTextStyles[styleName] = finalStl;
      return "{" + styleName + "|" + text + "}";
    };
    return TooltipMarkupStyleCreator2;
  })()
);

// node_modules/echarts/lib/component/tooltip/seriesFormatTooltip.js
function defaultSeriesFormatTooltip(opt) {
  var series = opt.series;
  var dataIndex = opt.dataIndex;
  var multipleSeries = opt.multipleSeries;
  var data = series.getData();
  var tooltipDims = data.mapDimensionsAll("defaultedTooltip");
  var tooltipDimLen = tooltipDims.length;
  var value = series.getRawValue(dataIndex);
  var isValueArr = isArray(value);
  var markerColor = retrieveVisualColorForTooltipMarker(series, dataIndex);
  var inlineValue;
  var inlineValueType;
  var subBlocks;
  var sortParam;
  if (tooltipDimLen > 1 || isValueArr && !tooltipDimLen) {
    var formatArrResult = formatTooltipArrayValue(value, series, dataIndex, tooltipDims, markerColor);
    inlineValue = formatArrResult.inlineValues;
    inlineValueType = formatArrResult.inlineValueTypes;
    subBlocks = formatArrResult.blocks;
    sortParam = formatArrResult.inlineValues[0];
  } else if (tooltipDimLen) {
    var dimInfo = data.getDimensionInfo(tooltipDims[0]);
    sortParam = inlineValue = retrieveRawValue(data, dataIndex, tooltipDims[0]);
    inlineValueType = dimInfo.type;
  } else {
    sortParam = inlineValue = isValueArr ? value[0] : value;
  }
  var seriesNameSpecified = isNameSpecified(series);
  var seriesName = seriesNameSpecified && series.name || "";
  var itemName = data.getName(dataIndex);
  var inlineName = multipleSeries ? seriesName : itemName;
  return createTooltipMarkup("section", {
    header: seriesName,
    // When series name is not specified, do not show a header line with only '-'.
    // This case always happens in tooltip.trigger: 'item'.
    noHeader: multipleSeries || !seriesNameSpecified,
    sortParam,
    blocks: [createTooltipMarkup("nameValue", {
      markerType: "item",
      markerColor,
      // Do not mix display seriesName and itemName in one tooltip,
      // which might confuses users.
      name: inlineName,
      // name dimension might be auto assigned, where the name might
      // be not readable. So we check trim here.
      noName: !trim(inlineName),
      value: inlineValue,
      valueType: inlineValueType,
      rawDataIndex: data.getRawIndex(dataIndex)
    })].concat(subBlocks || [])
  });
}
function formatTooltipArrayValue(value, series, dataIndex, tooltipDims, colorStr) {
  var data = series.getData();
  var isValueMultipleLine = reduce(value, function(isValueMultipleLine2, val, idx) {
    var dimItem = data.getDimensionInfo(idx);
    return isValueMultipleLine2 = isValueMultipleLine2 || dimItem && dimItem.tooltip !== false && dimItem.displayName != null;
  }, false);
  var inlineValues = [];
  var inlineValueTypes = [];
  var blocks = [];
  tooltipDims.length ? each(tooltipDims, function(dim) {
    setEachItem(retrieveRawValue(data, dataIndex, dim), dim);
  }) : each(value, setEachItem);
  function setEachItem(val, dim) {
    var dimInfo = data.getDimensionInfo(dim);
    if (!dimInfo || dimInfo.otherDims.tooltip === false) {
      return;
    }
    if (isValueMultipleLine) {
      blocks.push(createTooltipMarkup("nameValue", {
        markerType: "subItem",
        markerColor: colorStr,
        name: dimInfo.displayName,
        value: val,
        valueType: dimInfo.type
      }));
    } else {
      inlineValues.push(val);
      inlineValueTypes.push(dimInfo.type);
    }
  }
  return {
    inlineValues,
    inlineValueTypes,
    blocks
  };
}

// node_modules/echarts/lib/model/Series.js
var inner3 = makeInner();
function getSelectionKey(data, dataIndex) {
  return data.getName(dataIndex) || data.getId(dataIndex);
}
var SERIES_UNIVERSAL_TRANSITION_PROP = "__universalTransitionEnabled";
var SeriesModel = (
  /** @class */
  (function(_super) {
    __extends(SeriesModel2, _super);
    function SeriesModel2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this._selectedDataIndicesMap = {};
      return _this;
    }
    SeriesModel2.prototype.init = function(option, parentModel, ecModel) {
      this.seriesIndex = this.componentIndex;
      this.dataTask = createTask({
        count: dataTaskCount,
        reset: dataTaskReset
      });
      this.dataTask.context = {
        model: this
      };
      this.mergeDefaultAndTheme(option, ecModel);
      var sourceManager = inner3(this).sourceManager = new SourceManager(this);
      sourceManager.prepareSource();
      var data = this.getInitialData(option, ecModel);
      wrapData(data, this);
      this.dataTask.context.data = data;
      if (true) {
        assert(data, "getInitialData returned invalid data.");
      }
      inner3(this).dataBeforeProcessed = data;
      autoSeriesName(this);
      this._initSelectedMapFromData(data);
    };
    SeriesModel2.prototype.mergeDefaultAndTheme = function(option, ecModel) {
      var layoutMode = fetchLayoutMode(this);
      var inputPositionParams = layoutMode ? getLayoutParams(option) : {};
      var themeSubType = this.subType;
      if (Component_default.hasClass(themeSubType)) {
        themeSubType += "Series";
      }
      merge2(option, ecModel.getTheme().get(this.subType));
      merge2(option, this.getDefaultOption());
      defaultEmphasis(option, "label", ["show"]);
      this.fillDataTextStyle(option.data);
      if (layoutMode) {
        mergeLayoutParam(option, inputPositionParams, layoutMode);
      }
    };
    SeriesModel2.prototype.mergeOption = function(newSeriesOption, ecModel) {
      newSeriesOption = merge2(this.option, newSeriesOption, true);
      this.fillDataTextStyle(newSeriesOption.data);
      var layoutMode = fetchLayoutMode(this);
      if (layoutMode) {
        mergeLayoutParam(this.option, newSeriesOption, layoutMode);
      }
      var sourceManager = inner3(this).sourceManager;
      sourceManager.dirty();
      sourceManager.prepareSource();
      var data = this.getInitialData(newSeriesOption, ecModel);
      wrapData(data, this);
      this.dataTask.dirty();
      this.dataTask.context.data = data;
      inner3(this).dataBeforeProcessed = data;
      autoSeriesName(this);
      this._initSelectedMapFromData(data);
    };
    SeriesModel2.prototype.fillDataTextStyle = function(data) {
      if (data && !isTypedArray(data)) {
        var props = ["show"];
        for (var i2 = 0; i2 < data.length; i2++) {
          if (data[i2] && data[i2].label) {
            defaultEmphasis(data[i2], "label", props);
          }
        }
      }
    };
    SeriesModel2.prototype.getInitialData = function(option, ecModel) {
      return;
    };
    SeriesModel2.prototype.appendData = function(params) {
      var data = this.getRawData();
      data.appendData(params.data);
    };
    SeriesModel2.prototype.getData = function(dataType) {
      var task = getCurrentTask(this);
      if (task) {
        var data = task.context.data;
        return dataType == null || !data.getLinkedData ? data : data.getLinkedData(dataType);
      } else {
        return inner3(this).data;
      }
    };
    SeriesModel2.prototype.getAllData = function() {
      var mainData = this.getData();
      return mainData && mainData.getLinkedDataAll ? mainData.getLinkedDataAll() : [{
        data: mainData
      }];
    };
    SeriesModel2.prototype.setData = function(data) {
      var task = getCurrentTask(this);
      if (task) {
        var context = task.context;
        context.outputData = data;
        if (task !== this.dataTask) {
          context.data = data;
        }
      }
      inner3(this).data = data;
    };
    SeriesModel2.prototype.getEncode = function() {
      var encode = this.get("encode", true);
      if (encode) {
        return createHashMap(encode);
      }
    };
    SeriesModel2.prototype.getSourceManager = function() {
      return inner3(this).sourceManager;
    };
    SeriesModel2.prototype.getSource = function() {
      return this.getSourceManager().getSource();
    };
    SeriesModel2.prototype.getRawData = function() {
      return inner3(this).dataBeforeProcessed;
    };
    SeriesModel2.prototype.getColorBy = function() {
      var colorBy = this.get("colorBy");
      return colorBy || "series";
    };
    SeriesModel2.prototype.isColorBySeries = function() {
      return this.getColorBy() === "series";
    };
    SeriesModel2.prototype.getBaseAxis = function() {
      var coordSys = this.coordinateSystem;
      return coordSys && coordSys.getBaseAxis && coordSys.getBaseAxis();
    };
    SeriesModel2.prototype.indicesOfNearest = function(axisDim, dim, value, maxDistance) {
      var data = this.getData();
      var coordSys = this.coordinateSystem;
      var axis = coordSys && coordSys.getAxis(axisDim);
      if (!coordSys || !axis) {
        return [];
      }
      var targetCoord = axis.dataToCoord(value);
      if (maxDistance == null) {
        maxDistance = Infinity;
      }
      var nearestIndices = [];
      var minDist = Infinity;
      var minDiff = -1;
      var nearestIndicesLen = 0;
      var dimIdx = data.getDimensionIndex(dim);
      var store = data.getStore();
      for (var idx = 0, len2 = store.count(); idx < len2; idx++) {
        var dimValue = store.get(dimIdx, idx);
        var dataCoord = axis.dataToCoord(dimValue);
        var diff = targetCoord - dataCoord;
        var dist3 = Math.abs(diff);
        if (dist3 <= maxDistance) {
          if (dist3 < minDist || dist3 === minDist && diff >= 0 && minDiff < 0) {
            minDist = dist3;
            minDiff = diff;
            nearestIndicesLen = 0;
          }
          if (diff === minDiff) {
            nearestIndices[nearestIndicesLen++] = idx;
          }
        }
      }
      nearestIndices.length = nearestIndicesLen;
      return nearestIndices;
    };
    SeriesModel2.prototype.formatTooltip = function(dataIndex, multipleSeries, dataType) {
      return defaultSeriesFormatTooltip({
        series: this,
        dataIndex,
        multipleSeries
      });
    };
    SeriesModel2.prototype.isAnimationEnabled = function() {
      var ecModel = this.ecModel;
      if (env_default.node && !(ecModel && ecModel.ssr)) {
        return false;
      }
      var animationEnabled = this.getShallow("animation");
      if (animationEnabled) {
        if (this.getData().count() > this.getShallow("animationThreshold")) {
          animationEnabled = false;
        }
      }
      return !!animationEnabled;
    };
    SeriesModel2.prototype.restoreData = function() {
      this.dataTask.dirty();
    };
    SeriesModel2.prototype.getColorFromPalette = function(name, scope, requestColorNum) {
      var ecModel = this.ecModel;
      var color3 = PaletteMixin.prototype.getColorFromPalette.call(this, name, scope, requestColorNum);
      if (!color3) {
        color3 = ecModel.getColorFromPalette(name, scope, requestColorNum);
      }
      return color3;
    };
    SeriesModel2.prototype.coordDimToDataDim = function(coordDim) {
      return this.getRawData().mapDimensionsAll(coordDim);
    };
    SeriesModel2.prototype.getProgressive = function() {
      return this.get("progressive");
    };
    SeriesModel2.prototype.getProgressiveThreshold = function() {
      return this.get("progressiveThreshold");
    };
    SeriesModel2.prototype.select = function(innerDataIndices, dataType) {
      this._innerSelect(this.getData(dataType), innerDataIndices);
    };
    SeriesModel2.prototype.unselect = function(innerDataIndices, dataType) {
      var selectedMap = this.option.selectedMap;
      if (!selectedMap) {
        return;
      }
      var selectedMode = this.option.selectedMode;
      var data = this.getData(dataType);
      if (selectedMode === "series" || selectedMap === "all") {
        this.option.selectedMap = {};
        this._selectedDataIndicesMap = {};
        return;
      }
      for (var i2 = 0; i2 < innerDataIndices.length; i2++) {
        var dataIndex = innerDataIndices[i2];
        var nameOrId = getSelectionKey(data, dataIndex);
        selectedMap[nameOrId] = false;
        this._selectedDataIndicesMap[nameOrId] = -1;
      }
    };
    SeriesModel2.prototype.toggleSelect = function(innerDataIndices, dataType) {
      var tmpArr2 = [];
      for (var i2 = 0; i2 < innerDataIndices.length; i2++) {
        tmpArr2[0] = innerDataIndices[i2];
        this.isSelected(innerDataIndices[i2], dataType) ? this.unselect(tmpArr2, dataType) : this.select(tmpArr2, dataType);
      }
    };
    SeriesModel2.prototype.getSelectedDataIndices = function() {
      if (this.option.selectedMap === "all") {
        return [].slice.call(this.getData().getIndices());
      }
      var selectedDataIndicesMap = this._selectedDataIndicesMap;
      var nameOrIds = keys(selectedDataIndicesMap);
      var dataIndices = [];
      for (var i2 = 0; i2 < nameOrIds.length; i2++) {
        var dataIndex = selectedDataIndicesMap[nameOrIds[i2]];
        if (dataIndex >= 0) {
          dataIndices.push(dataIndex);
        }
      }
      return dataIndices;
    };
    SeriesModel2.prototype.isSelected = function(dataIndex, dataType) {
      var selectedMap = this.option.selectedMap;
      if (!selectedMap) {
        return false;
      }
      var data = this.getData(dataType);
      return (selectedMap === "all" || selectedMap[getSelectionKey(data, dataIndex)]) && !data.getItemModel(dataIndex).get(["select", "disabled"]);
    };
    SeriesModel2.prototype.isUniversalTransitionEnabled = function() {
      if (this[SERIES_UNIVERSAL_TRANSITION_PROP]) {
        return true;
      }
      var universalTransitionOpt = this.option.universalTransition;
      if (!universalTransitionOpt) {
        return false;
      }
      if (universalTransitionOpt === true) {
        return true;
      }
      return universalTransitionOpt && universalTransitionOpt.enabled;
    };
    SeriesModel2.prototype._innerSelect = function(data, innerDataIndices) {
      var _a2, _b2;
      var option = this.option;
      var selectedMode = option.selectedMode;
      var len2 = innerDataIndices.length;
      if (!selectedMode || !len2) {
        return;
      }
      if (selectedMode === "series") {
        option.selectedMap = "all";
      } else if (selectedMode === "multiple") {
        if (!isObject(option.selectedMap)) {
          option.selectedMap = {};
        }
        var selectedMap = option.selectedMap;
        for (var i2 = 0; i2 < len2; i2++) {
          var dataIndex = innerDataIndices[i2];
          var nameOrId = getSelectionKey(data, dataIndex);
          selectedMap[nameOrId] = true;
          this._selectedDataIndicesMap[nameOrId] = data.getRawIndex(dataIndex);
        }
      } else if (selectedMode === "single" || selectedMode === true) {
        var lastDataIndex = innerDataIndices[len2 - 1];
        var nameOrId = getSelectionKey(data, lastDataIndex);
        option.selectedMap = (_a2 = {}, _a2[nameOrId] = true, _a2);
        this._selectedDataIndicesMap = (_b2 = {}, _b2[nameOrId] = data.getRawIndex(lastDataIndex), _b2);
      }
    };
    SeriesModel2.prototype._initSelectedMapFromData = function(data) {
      if (this.option.selectedMap) {
        return;
      }
      var dataIndices = [];
      if (data.hasItemOption) {
        data.each(function(idx) {
          var rawItem = data.getRawDataItem(idx);
          if (rawItem && rawItem.selected) {
            dataIndices.push(idx);
          }
        });
      }
      if (dataIndices.length > 0) {
        this._innerSelect(data, dataIndices);
      }
    };
    SeriesModel2.registerClass = function(clz) {
      return Component_default.registerClass(clz);
    };
    SeriesModel2.protoInitialize = (function() {
      var proto = SeriesModel2.prototype;
      proto.type = "series.__base__";
      proto.seriesIndex = 0;
      proto.ignoreStyleOnData = false;
      proto.hasSymbolVisual = false;
      proto.defaultSymbol = "circle";
      proto.visualStyleAccessPath = "itemStyle";
      proto.visualDrawType = "fill";
    })();
    return SeriesModel2;
  })(Component_default)
);
mixin(SeriesModel, DataFormatMixin);
mixin(SeriesModel, PaletteMixin);
mountExtend(SeriesModel, Component_default);
function autoSeriesName(seriesModel) {
  var name = seriesModel.name;
  if (!isNameSpecified(seriesModel)) {
    seriesModel.name = getSeriesAutoName(seriesModel) || name;
  }
}
function getSeriesAutoName(seriesModel) {
  var data = seriesModel.getRawData();
  var dataDims = data.mapDimensionsAll("seriesName");
  var nameArr = [];
  each(dataDims, function(dataDim) {
    var dimInfo = data.getDimensionInfo(dataDim);
    dimInfo.displayName && nameArr.push(dimInfo.displayName);
  });
  return nameArr.join(" ");
}
function dataTaskCount(context) {
  return context.model.getRawData().count();
}
function dataTaskReset(context) {
  var seriesModel = context.model;
  seriesModel.setData(seriesModel.getRawData().cloneShallow());
  return dataTaskProgress;
}
function dataTaskProgress(param, context) {
  if (context.outputData && param.end > context.outputData.count()) {
    context.model.getRawData().cloneShallow(context.outputData);
  }
}
function wrapData(data, seriesModel) {
  each(concatArray(data.CHANGABLE_METHODS, data.DOWNSAMPLE_METHODS), function(methodName) {
    data.wrapMethod(methodName, curry(onDataChange, seriesModel));
  });
}
function onDataChange(seriesModel, newList) {
  var task = getCurrentTask(seriesModel);
  if (task) {
    task.setOutputEnd((newList || this).count());
  }
  return newList;
}
function getCurrentTask(seriesModel) {
  var scheduler = (seriesModel.ecModel || {}).scheduler;
  var pipeline = scheduler && scheduler.getPipeline(seriesModel.uid);
  if (pipeline) {
    var task = pipeline.currentTask;
    if (task) {
      var agentStubMap = task.agentStubMap;
      if (agentStubMap) {
        task = agentStubMap.get(seriesModel.uid);
      }
    }
    return task;
  }
}
var Series_default = SeriesModel;

// node_modules/echarts/lib/util/symbol.js
var Triangle = Path_default.extend({
  type: "triangle",
  shape: {
    cx: 0,
    cy: 0,
    width: 0,
    height: 0
  },
  buildPath: function(path, shape) {
    var cx = shape.cx;
    var cy = shape.cy;
    var width = shape.width / 2;
    var height = shape.height / 2;
    path.moveTo(cx, cy - height);
    path.lineTo(cx + width, cy + height);
    path.lineTo(cx - width, cy + height);
    path.closePath();
  }
});
var Diamond = Path_default.extend({
  type: "diamond",
  shape: {
    cx: 0,
    cy: 0,
    width: 0,
    height: 0
  },
  buildPath: function(path, shape) {
    var cx = shape.cx;
    var cy = shape.cy;
    var width = shape.width / 2;
    var height = shape.height / 2;
    path.moveTo(cx, cy - height);
    path.lineTo(cx + width, cy);
    path.lineTo(cx, cy + height);
    path.lineTo(cx - width, cy);
    path.closePath();
  }
});
var Pin = Path_default.extend({
  type: "pin",
  shape: {
    // x, y on the cusp
    x: 0,
    y: 0,
    width: 0,
    height: 0
  },
  buildPath: function(path, shape) {
    var x = shape.x;
    var y = shape.y;
    var w = shape.width / 5 * 3;
    var h = Math.max(w, shape.height);
    var r = w / 2;
    var dy = r * r / (h - r);
    var cy = y - h + r + dy;
    var angle = Math.asin(dy / r);
    var dx = Math.cos(angle) * r;
    var tanX = Math.sin(angle);
    var tanY = Math.cos(angle);
    var cpLen = r * 0.6;
    var cpLen2 = r * 0.7;
    path.moveTo(x - dx, cy + dy);
    path.arc(x, cy, r, Math.PI - angle, Math.PI * 2 + angle);
    path.bezierCurveTo(x + dx - tanX * cpLen, cy + dy + tanY * cpLen, x, y - cpLen2, x, y);
    path.bezierCurveTo(x, y - cpLen2, x - dx + tanX * cpLen, cy + dy + tanY * cpLen, x - dx, cy + dy);
    path.closePath();
  }
});
var Arrow = Path_default.extend({
  type: "arrow",
  shape: {
    x: 0,
    y: 0,
    width: 0,
    height: 0
  },
  buildPath: function(ctx, shape) {
    var height = shape.height;
    var width = shape.width;
    var x = shape.x;
    var y = shape.y;
    var dx = width / 3 * 2;
    ctx.moveTo(x, y);
    ctx.lineTo(x + dx, y + height);
    ctx.lineTo(x, y + height / 4 * 3);
    ctx.lineTo(x - dx, y + height);
    ctx.lineTo(x, y);
    ctx.closePath();
  }
});
var symbolCtors = {
  line: Line_default,
  rect: Rect_default,
  roundRect: Rect_default,
  square: Rect_default,
  circle: Circle_default,
  diamond: Diamond,
  pin: Pin,
  arrow: Arrow,
  triangle: Triangle
};
var symbolShapeMakers = {
  line: function(x, y, w, h, shape) {
    shape.x1 = x;
    shape.y1 = y + h / 2;
    shape.x2 = x + w;
    shape.y2 = y + h / 2;
  },
  rect: function(x, y, w, h, shape) {
    shape.x = x;
    shape.y = y;
    shape.width = w;
    shape.height = h;
  },
  roundRect: function(x, y, w, h, shape) {
    shape.x = x;
    shape.y = y;
    shape.width = w;
    shape.height = h;
    shape.r = Math.min(w, h) / 4;
  },
  square: function(x, y, w, h, shape) {
    var size = Math.min(w, h);
    shape.x = x;
    shape.y = y;
    shape.width = size;
    shape.height = size;
  },
  circle: function(x, y, w, h, shape) {
    shape.cx = x + w / 2;
    shape.cy = y + h / 2;
    shape.r = Math.min(w, h) / 2;
  },
  diamond: function(x, y, w, h, shape) {
    shape.cx = x + w / 2;
    shape.cy = y + h / 2;
    shape.width = w;
    shape.height = h;
  },
  pin: function(x, y, w, h, shape) {
    shape.x = x + w / 2;
    shape.y = y + h / 2;
    shape.width = w;
    shape.height = h;
  },
  arrow: function(x, y, w, h, shape) {
    shape.x = x + w / 2;
    shape.y = y + h / 2;
    shape.width = w;
    shape.height = h;
  },
  triangle: function(x, y, w, h, shape) {
    shape.cx = x + w / 2;
    shape.cy = y + h / 2;
    shape.width = w;
    shape.height = h;
  }
};
var symbolBuildProxies = {};
each(symbolCtors, function(Ctor, name) {
  symbolBuildProxies[name] = new Ctor();
});
var SymbolClz = Path_default.extend({
  type: "symbol",
  shape: {
    symbolType: "",
    x: 0,
    y: 0,
    width: 0,
    height: 0
  },
  calculateTextPosition: function(out2, config, rect) {
    var res = calculateTextPosition(out2, config, rect);
    var shape = this.shape;
    if (shape && shape.symbolType === "pin" && config.position === "inside") {
      res.y = rect.y + rect.height * 0.4;
    }
    return res;
  },
  buildPath: function(ctx, shape, inBundle) {
    var symbolType = shape.symbolType;
    if (symbolType !== "none") {
      var proxySymbol = symbolBuildProxies[symbolType];
      if (!proxySymbol) {
        symbolType = "rect";
        proxySymbol = symbolBuildProxies[symbolType];
      }
      symbolShapeMakers[symbolType](shape.x, shape.y, shape.width, shape.height, proxySymbol.shape);
      proxySymbol.buildPath(ctx, proxySymbol.shape, inBundle);
    }
  }
});
function symbolPathSetColor(color3, innerColor2) {
  if (this.type !== "image") {
    var symbolStyle = this.style;
    if (this.__isEmptyBrush) {
      symbolStyle.stroke = color3;
      symbolStyle.fill = innerColor2 || tokens_default.color.neutral00;
      symbolStyle.lineWidth = 2;
    } else if (this.shape.symbolType === "line") {
      symbolStyle.stroke = color3;
    } else {
      symbolStyle.fill = color3;
    }
    this.markRedraw();
  }
}
function createSymbol(symbolType, x, y, w, h, color3, keepAspect) {
  var isEmpty = symbolType.indexOf("empty") === 0;
  if (isEmpty) {
    symbolType = symbolType.substr(5, 1).toLowerCase() + symbolType.substr(6);
  }
  var symbolPath;
  if (symbolType.indexOf("image://") === 0) {
    symbolPath = makeImage(symbolType.slice(8), new BoundingRect_default(x, y, w, h), keepAspect ? "center" : "cover");
  } else if (symbolType.indexOf("path://") === 0) {
    symbolPath = makePath(symbolType.slice(7), {}, new BoundingRect_default(x, y, w, h), keepAspect ? "center" : "cover");
  } else {
    symbolPath = new SymbolClz({
      shape: {
        symbolType,
        x,
        y,
        width: w,
        height: h
      }
    });
  }
  symbolPath.__isEmptyBrush = isEmpty;
  symbolPath.setColor = symbolPathSetColor;
  if (color3) {
    symbolPath.setColor(color3);
  }
  return symbolPath;
}
function normalizeSymbolSize(symbolSize) {
  if (!isArray(symbolSize)) {
    symbolSize = [+symbolSize, +symbolSize];
  }
  return [symbolSize[0] || 0, symbolSize[1] || 0];
}
function normalizeSymbolOffset(symbolOffset, symbolSize) {
  if (symbolOffset == null) {
    return;
  }
  if (!isArray(symbolOffset)) {
    symbolOffset = [symbolOffset, symbolOffset];
  }
  return [parsePercent2(symbolOffset[0], symbolSize[0]) || 0, parsePercent2(retrieve2(symbolOffset[1], symbolOffset[0]), symbolSize[1]) || 0];
}

// node_modules/echarts/lib/chart/line/LineSeries.js
var LineSeriesModel = (
  /** @class */
  (function(_super) {
    __extends(LineSeriesModel2, _super);
    function LineSeriesModel2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = LineSeriesModel2.type;
      _this.hasSymbolVisual = true;
      return _this;
    }
    LineSeriesModel2.prototype.getInitialData = function(option) {
      if (true) {
        var coordSys = option.coordinateSystem;
        if (coordSys !== "polar" && coordSys !== "cartesian2d") {
          throw new Error("Line not support coordinateSystem besides cartesian and polar");
        }
      }
      return createSeriesData_default(null, this, {
        useEncodeDefaulter: true
      });
    };
    LineSeriesModel2.prototype.getLegendIcon = function(opt) {
      var group2 = new Group_default();
      var line = createSymbol("line", 0, opt.itemHeight / 2, opt.itemWidth, 0, opt.lineStyle.stroke, false);
      group2.add(line);
      line.setStyle(opt.lineStyle);
      var visualType = this.getData().getVisual("symbol");
      var visualRotate = this.getData().getVisual("symbolRotate");
      var symbolType = visualType === "none" ? "circle" : visualType;
      var size = opt.itemHeight * 0.8;
      var symbol = createSymbol(symbolType, (opt.itemWidth - size) / 2, (opt.itemHeight - size) / 2, size, size, opt.itemStyle.fill);
      group2.add(symbol);
      symbol.setStyle(opt.itemStyle);
      var symbolRotate = opt.iconRotate === "inherit" ? visualRotate : opt.iconRotate || 0;
      symbol.rotation = symbolRotate * Math.PI / 180;
      symbol.setOrigin([opt.itemWidth / 2, opt.itemHeight / 2]);
      if (symbolType.indexOf("empty") > -1) {
        symbol.style.stroke = symbol.style.fill;
        symbol.style.fill = tokens_default.color.neutral00;
        symbol.style.lineWidth = 2;
      }
      return group2;
    };
    LineSeriesModel2.type = "series.line";
    LineSeriesModel2.dependencies = ["grid", "polar"];
    LineSeriesModel2.defaultOption = {
      // zlevel: 0,
      z: 3,
      coordinateSystem: "cartesian2d",
      legendHoverLink: true,
      clip: true,
      label: {
        position: "top"
      },
      // itemStyle: {
      // },
      endLabel: {
        show: false,
        valueAnimation: true,
        distance: 8
      },
      lineStyle: {
        width: 2,
        type: "solid"
      },
      emphasis: {
        scale: true
      },
      // areaStyle: {
      // origin of areaStyle. Valid values:
      // `'auto'/null/undefined`: from axisLine to data
      // `'start'`: from min to data
      // `'end'`: from data to max
      // origin: 'auto'
      // },
      // false, 'start', 'end', 'middle'
      step: false,
      // Disabled if step is true
      smooth: false,
      smoothMonotone: null,
      symbol: "emptyCircle",
      symbolSize: 6,
      symbolRotate: null,
      showSymbol: true,
      // `false`: follow the label interval strategy.
      // `true`: show all symbols.
      // `'auto'`: If possible, show all symbols, otherwise
      //           follow the label interval strategy.
      showAllSymbol: "auto",
      // Whether to connect break point. (non-finite values)
      connectNulls: false,
      // Sampling for large data. Can be: 'average', 'max', 'min', 'sum', 'lttb'.
      sampling: "none",
      animationEasing: "linear",
      // Disable progressive
      progressive: 0,
      hoverLayerThreshold: Infinity,
      universalTransition: {
        divideShape: "clone"
      },
      /**
       * @deprecated
       */
      triggerLineEvent: false,
      triggerEvent: false
    };
    return LineSeriesModel2;
  })(Series_default)
);
var LineSeries_default = LineSeriesModel;

// node_modules/echarts/lib/chart/helper/labelHelper.js
function getDefaultLabel(data, dataIndex) {
  var labelDims = data.mapDimensionsAll("defaultedLabel");
  var len2 = labelDims.length;
  if (len2 === 1) {
    var rawVal = retrieveRawValue(data, dataIndex, labelDims[0]);
    return rawVal != null ? rawVal + "" : null;
  } else if (len2) {
    var vals = [];
    for (var i2 = 0; i2 < labelDims.length; i2++) {
      vals.push(retrieveRawValue(data, dataIndex, labelDims[i2]));
    }
    return vals.join(" ");
  }
}
function getDefaultInterpolatedLabel(data, interpolatedValue) {
  var labelDims = data.mapDimensionsAll("defaultedLabel");
  if (!isArray(interpolatedValue)) {
    return interpolatedValue + "";
  }
  var vals = [];
  for (var i2 = 0; i2 < labelDims.length; i2++) {
    var dimIndex = data.getDimensionIndex(labelDims[i2]);
    if (dimIndex >= 0) {
      vals.push(interpolatedValue[dimIndex]);
    }
  }
  return vals.join(" ");
}

// node_modules/echarts/lib/chart/helper/Symbol.js
var Symbol2 = (
  /** @class */
  (function(_super) {
    __extends(Symbol3, _super);
    function Symbol3(data, idx, seriesScope, opts) {
      var _this = _super.call(this) || this;
      _this.updateData(data, idx, seriesScope, opts);
      return _this;
    }
    Symbol3.prototype._createSymbol = function(symbolType, data, idx, symbolSize, z2, keepAspect) {
      this.removeAll();
      var symbolPath = createSymbol(symbolType, -1, -1, 2, 2, null, keepAspect);
      symbolPath.attr({
        z2: retrieve2(z2, 100),
        culling: true,
        scaleX: symbolSize[0] / 2,
        scaleY: symbolSize[1] / 2
      });
      symbolPath.drift = driftSymbol;
      this._symbolType = symbolType;
      this.add(symbolPath);
    };
    Symbol3.prototype.stopSymbolAnimation = function(toLastFrame) {
      this.childAt(0).stopAnimation(null, toLastFrame);
    };
    Symbol3.prototype.getSymbolType = function() {
      return this._symbolType;
    };
    Symbol3.prototype.getSymbolPath = function() {
      return this.childAt(0);
    };
    Symbol3.prototype.highlight = function() {
      enterEmphasis(this.childAt(0));
    };
    Symbol3.prototype.downplay = function() {
      leaveEmphasis(this.childAt(0));
    };
    Symbol3.prototype.setZ = function(zlevel, z) {
      var symbolPath = this.childAt(0);
      symbolPath.zlevel = zlevel;
      symbolPath.z = z;
    };
    Symbol3.prototype.setDraggable = function(draggable, hasCursorOption) {
      var symbolPath = this.childAt(0);
      symbolPath.draggable = draggable;
      symbolPath.cursor = !hasCursorOption && draggable ? "move" : symbolPath.cursor;
    };
    Symbol3.prototype.updateData = function(data, idx, seriesScope, opts) {
      this.silent = false;
      var symbolType = data.getItemVisual(idx, "symbol") || "circle";
      var seriesModel = data.hostModel;
      var symbolSize = Symbol3.getSymbolSize(data, idx);
      var z2 = Symbol3.getSymbolZ2(data, idx);
      var isInit = symbolType !== this._symbolType;
      var disableAnimation = opts && opts.disableAnimation;
      if (isInit) {
        var keepAspect = data.getItemVisual(idx, "symbolKeepAspect");
        this._createSymbol(symbolType, data, idx, symbolSize, z2, keepAspect);
      } else {
        var symbolPath = this.childAt(0);
        symbolPath.silent = false;
        var target = {
          scaleX: symbolSize[0] / 2,
          scaleY: symbolSize[1] / 2
        };
        disableAnimation ? symbolPath.attr(target) : updateProps(symbolPath, target, seriesModel, idx);
        saveOldStyle(symbolPath);
      }
      this._updateCommon(data, idx, symbolSize, seriesScope, opts);
      if (isInit) {
        var symbolPath = this.childAt(0);
        if (!disableAnimation) {
          var target = {
            scaleX: this._sizeX,
            scaleY: this._sizeY,
            style: {
              // Always fadeIn. Because it has fadeOut animation when symbol is removed..
              opacity: symbolPath.style.opacity
            }
          };
          symbolPath.scaleX = symbolPath.scaleY = 0;
          symbolPath.style.opacity = 0;
          initProps(symbolPath, target, seriesModel, idx);
        }
      }
      if (disableAnimation) {
        this.childAt(0).stopAnimation("leave");
      }
    };
    Symbol3.prototype._updateCommon = function(data, idx, symbolSize, seriesScope, opts) {
      var symbolPath = this.childAt(0);
      var seriesModel = data.hostModel;
      var emphasisItemStyle;
      var blurItemStyle;
      var selectItemStyle;
      var focus;
      var blurScope;
      var emphasisDisabled;
      var labelStatesModels;
      var hoverScale;
      var cursorStyle;
      if (seriesScope) {
        emphasisItemStyle = seriesScope.emphasisItemStyle;
        blurItemStyle = seriesScope.blurItemStyle;
        selectItemStyle = seriesScope.selectItemStyle;
        focus = seriesScope.focus;
        blurScope = seriesScope.blurScope;
        labelStatesModels = seriesScope.labelStatesModels;
        hoverScale = seriesScope.hoverScale;
        cursorStyle = seriesScope.cursorStyle;
        emphasisDisabled = seriesScope.emphasisDisabled;
      }
      if (!seriesScope || data.hasItemOption) {
        var itemModel = seriesScope && seriesScope.itemModel ? seriesScope.itemModel : data.getItemModel(idx);
        var emphasisModel = itemModel.getModel("emphasis");
        emphasisItemStyle = emphasisModel.getModel("itemStyle").getItemStyle();
        selectItemStyle = itemModel.getModel(["select", "itemStyle"]).getItemStyle();
        blurItemStyle = itemModel.getModel(["blur", "itemStyle"]).getItemStyle();
        focus = emphasisModel.get("focus");
        blurScope = emphasisModel.get("blurScope");
        emphasisDisabled = emphasisModel.get("disabled");
        labelStatesModels = getLabelStatesModels(itemModel);
        hoverScale = emphasisModel.getShallow("scale");
        cursorStyle = itemModel.getShallow("cursor");
      }
      var symbolRotate = data.getItemVisual(idx, "symbolRotate");
      symbolPath.attr("rotation", (symbolRotate || 0) * Math.PI / 180 || 0);
      var symbolOffset = normalizeSymbolOffset(data.getItemVisual(idx, "symbolOffset"), symbolSize);
      if (symbolOffset) {
        symbolPath.x = symbolOffset[0];
        symbolPath.y = symbolOffset[1];
      }
      cursorStyle && symbolPath.attr("cursor", cursorStyle);
      var symbolStyle = data.getItemVisual(idx, "style");
      var visualColor = symbolStyle.fill;
      if (symbolPath instanceof Image_default) {
        var pathStyle = symbolPath.style;
        symbolPath.useStyle(extend({
          // TODO other properties like x, y ?
          image: pathStyle.image,
          x: pathStyle.x,
          y: pathStyle.y,
          width: pathStyle.width,
          height: pathStyle.height
        }, symbolStyle));
      } else {
        if (symbolPath.__isEmptyBrush) {
          symbolPath.useStyle(extend({}, symbolStyle));
        } else {
          symbolPath.useStyle(symbolStyle);
        }
        symbolPath.style.decal = null;
        symbolPath.setColor(visualColor, opts && opts.symbolInnerColor);
        symbolPath.style.strokeNoScale = true;
      }
      var liftZ = data.getItemVisual(idx, "liftZ");
      var z2Origin = this._z2;
      if (liftZ != null) {
        if (z2Origin == null) {
          this._z2 = symbolPath.z2;
          symbolPath.z2 += liftZ;
        }
      } else if (z2Origin != null) {
        symbolPath.z2 = z2Origin;
        this._z2 = null;
      }
      var useNameLabel = opts && opts.useNameLabel;
      setLabelStyle(symbolPath, labelStatesModels, {
        labelFetcher: seriesModel,
        labelDataIndex: idx,
        defaultText: getLabelDefaultText,
        inheritColor: visualColor,
        defaultOpacity: symbolStyle.opacity
      });
      function getLabelDefaultText(idx2) {
        return useNameLabel ? data.getName(idx2) : getDefaultLabel(data, idx2);
      }
      this._sizeX = symbolSize[0] / 2;
      this._sizeY = symbolSize[1] / 2;
      var emphasisState = symbolPath.ensureState("emphasis");
      emphasisState.style = emphasisItemStyle;
      symbolPath.ensureState("select").style = selectItemStyle;
      symbolPath.ensureState("blur").style = blurItemStyle;
      var scaleRatio = hoverScale == null || hoverScale === true ? Math.max(1.1, 3 / this._sizeY) : isFinite(hoverScale) && hoverScale > 0 ? +hoverScale : 1;
      emphasisState.scaleX = this._sizeX * scaleRatio;
      emphasisState.scaleY = this._sizeY * scaleRatio;
      this.setSymbolScale(1);
      toggleHoverEmphasis(this, focus, blurScope, emphasisDisabled);
    };
    Symbol3.prototype.setSymbolScale = function(scale3) {
      this.scaleX = this.scaleY = scale3;
    };
    Symbol3.prototype.fadeOut = function(cb, seriesModel, opt) {
      var symbolPath = this.childAt(0);
      var dataIndex = getECData(this).dataIndex;
      var animationOpt = opt && opt.animation;
      this.silent = symbolPath.silent = true;
      if (opt && opt.fadeLabel) {
        var textContent = symbolPath.getTextContent();
        if (textContent) {
          removeElement(textContent, {
            style: {
              opacity: 0
            }
          }, seriesModel, {
            dataIndex,
            removeOpt: animationOpt,
            cb: function() {
              symbolPath.removeTextContent();
            }
          });
        }
      } else {
        symbolPath.removeTextContent();
      }
      removeElement(symbolPath, {
        style: {
          opacity: 0
        },
        scaleX: 0,
        scaleY: 0
      }, seriesModel, {
        dataIndex,
        cb,
        removeOpt: animationOpt
      });
    };
    Symbol3.getSymbolSize = function(data, idx) {
      return normalizeSymbolSize(data.getItemVisual(idx, "symbolSize"));
    };
    Symbol3.getSymbolZ2 = function(data, idx) {
      return data.getItemVisual(idx, "z2");
    };
    return Symbol3;
  })(Group_default)
);
function driftSymbol(dx, dy) {
  this.parent.drift(dx, dy);
}
var Symbol_default = Symbol2;

// node_modules/echarts/lib/chart/helper/SymbolDraw.js
function symbolNeedsDraw(data, point, idx, opt) {
  return point && !isNaN(point[0]) && !isNaN(point[1]) && !(opt && opt.isIgnore && opt.isIgnore(idx)) && !(opt && opt.clipShape && !opt.clipShape.contain(point[0], point[1])) && data.getItemVisual(idx, "symbol") !== "none";
}
function normalizeUpdateOpt(opt) {
  if (opt != null && !isObject(opt)) {
    opt = {
      isIgnore: opt
    };
  }
  return opt || {};
}
function makeSeriesScope(data) {
  var seriesModel = data.hostModel;
  var emphasisModel = seriesModel.getModel("emphasis");
  return {
    emphasisItemStyle: emphasisModel.getModel("itemStyle").getItemStyle(),
    blurItemStyle: seriesModel.getModel(["blur", "itemStyle"]).getItemStyle(),
    selectItemStyle: seriesModel.getModel(["select", "itemStyle"]).getItemStyle(),
    focus: emphasisModel.get("focus"),
    blurScope: emphasisModel.get("blurScope"),
    emphasisDisabled: emphasisModel.get("disabled"),
    hoverScale: emphasisModel.get("scale"),
    labelStatesModels: getLabelStatesModels(seriesModel),
    cursorStyle: seriesModel.get("cursor")
  };
}
function createEl(SymbolCtor, data, newIdx, seriesScope, symbolUpdateOpt, point, group2) {
  var symbolEl = new SymbolCtor(data, newIdx, seriesScope, symbolUpdateOpt);
  symbolEl.setPosition(point);
  data.setItemGraphicEl(newIdx, symbolEl);
  group2.add(symbolEl);
  return symbolEl;
}
var SymbolDraw = (
  /** @class */
  (function() {
    function SymbolDraw2(SymbolCtor) {
      this.group = new Group_default();
      this._SymbolCtor = SymbolCtor || Symbol_default;
    }
    SymbolDraw2.prototype.updateData = function(data, opt) {
      this._progressiveEls = null;
      opt = normalizeUpdateOpt(opt);
      var group2 = this.group;
      var seriesModel = data.hostModel;
      var oldData = this._data;
      var SymbolCtor = this._SymbolCtor;
      var disableAnimation = opt.disableAnimation;
      var seriesScope = this._seriesScope = makeSeriesScope(data);
      var symbolUpdateOpt = {
        disableAnimation
      };
      var getSymbolPoint = opt.getSymbolPoint || function(idx) {
        return data.getItemLayout(idx);
      };
      if (!oldData) {
        group2.removeAll();
      }
      data.diff(oldData).add(function(newIdx) {
        var point = getSymbolPoint(newIdx);
        if (symbolNeedsDraw(data, point, newIdx, opt)) {
          createEl(SymbolCtor, data, newIdx, seriesScope, symbolUpdateOpt, point, group2);
        }
      }).update(function(newIdx, oldIdx) {
        var symbolEl = oldData.getItemGraphicEl(oldIdx);
        var point = getSymbolPoint(newIdx);
        if (!symbolNeedsDraw(data, point, newIdx, opt)) {
          group2.remove(symbolEl);
          return;
        }
        var newSymbolType = data.getItemVisual(newIdx, "symbol") || "circle";
        var oldSymbolType = symbolEl && symbolEl.getSymbolType && symbolEl.getSymbolType();
        if (!symbolEl || oldSymbolType && oldSymbolType !== newSymbolType) {
          group2.remove(symbolEl);
          symbolEl = new SymbolCtor(data, newIdx, seriesScope, symbolUpdateOpt);
          symbolEl.setPosition(point);
        } else {
          symbolEl.updateData(data, newIdx, seriesScope, symbolUpdateOpt);
          var target = {
            x: point[0],
            y: point[1]
          };
          disableAnimation ? symbolEl.attr(target) : updateProps(symbolEl, target, seriesModel);
        }
        group2.add(symbolEl);
        data.setItemGraphicEl(newIdx, symbolEl);
      }).remove(function(oldIdx) {
        var el = oldData.getItemGraphicEl(oldIdx);
        el && el.fadeOut(function() {
          group2.remove(el);
        }, seriesModel);
      }).execute();
      this._getSymbolPoint = getSymbolPoint;
      this._data = data;
    };
    ;
    SymbolDraw2.prototype.updateLayout = function(opt) {
      var data = this._data;
      if (!data) {
        return;
      }
      var symbolDraw = this;
      var store = data.getStore();
      for (var idx = 0, len2 = store.count(); idx < len2; idx++) {
        var el = data.getItemGraphicEl(idx);
        var point = symbolDraw._getSymbolPoint(idx);
        if (symbolNeedsDraw(data, point, idx, opt)) {
          el = el || createEl(symbolDraw._SymbolCtor, data, idx, symbolDraw._seriesScope, {
            disableAnimation: true
          }, point, symbolDraw.group);
          el.stopAnimation();
          el.setPosition(point);
          el.markRedraw();
        } else if (el) {
          symbolDraw.group.remove(el);
          data.setItemGraphicEl(idx, null);
        }
      }
    };
    ;
    SymbolDraw2.prototype.incrementalPrepareUpdate = function(data) {
      this._seriesScope = makeSeriesScope(data);
      this._data = null;
      this.group.removeAll();
    };
    ;
    SymbolDraw2.prototype.incrementalUpdate = function(taskParams, data, incrementalId, opt) {
      this._progressiveEls = [];
      opt = normalizeUpdateOpt(opt);
      function updateIncrementalAndHover(el2) {
        if (!el2.isGroup) {
          el2.incremental = incrementalId;
          el2.ensureState("emphasis").hoverLayer = HOVER_LAYER_FOR_INCREMENTAL;
        }
      }
      for (var idx = taskParams.start; idx < taskParams.end; idx++) {
        var point = data.getItemLayout(idx);
        if (symbolNeedsDraw(data, point, idx, opt)) {
          var el = new this._SymbolCtor(data, idx, this._seriesScope);
          el.traverse(updateIncrementalAndHover);
          el.setPosition(point);
          this.group.add(el);
          data.setItemGraphicEl(idx, el);
          this._progressiveEls.push(el);
        }
      }
    };
    ;
    SymbolDraw2.prototype.eachRendered = function(cb) {
      traverseElements(this._progressiveEls || this.group, cb);
    };
    SymbolDraw2.prototype.remove = function(enableAnimation) {
      var group2 = this.group;
      var data = this._data;
      if (data && enableAnimation) {
        data.eachItemGraphicEl(function(el) {
          el.fadeOut(function() {
            group2.remove(el);
          }, data.hostModel);
        });
      } else {
        group2.removeAll();
      }
    };
    ;
    return SymbolDraw2;
  })()
);
var SymbolDraw_default = SymbolDraw;

// node_modules/echarts/lib/chart/line/helper.js
function prepareDataCoordInfo(coordSys, data, valueOrigin) {
  var baseAxis = coordSys.getBaseAxis();
  var valueAxis2 = coordSys.getOtherAxis(baseAxis);
  var valueStart = getValueStart(valueAxis2, valueOrigin);
  var baseAxisDim = baseAxis.dim;
  var valueAxisDim = valueAxis2.dim;
  var valueDim = data.mapDimension(valueAxisDim);
  var baseDim = data.mapDimension(baseAxisDim);
  var baseDataOffset = valueAxisDim === "x" || valueAxisDim === "radius" ? 1 : 0;
  var dims = map2(coordSys.dimensions, function(coordDim) {
    return data.mapDimension(coordDim);
  });
  var stacked = false;
  var stackResultDim = data.getCalculationInfo("stackResultDimension");
  if (isDimensionStacked(
    data,
    dims[0]
    /* , dims[1] */
  )) {
    stacked = true;
    dims[0] = stackResultDim;
  }
  if (isDimensionStacked(
    data,
    dims[1]
    /* , dims[0] */
  )) {
    stacked = true;
    dims[1] = stackResultDim;
  }
  return {
    dataDimsForPoint: dims,
    valueStart,
    valueAxisDim,
    baseAxisDim,
    stacked: !!stacked,
    valueDim,
    baseDim,
    baseDataOffset,
    stackedOverDimension: data.getCalculationInfo("stackedOverDimension")
  };
}
function getValueStart(valueAxis2, valueOrigin) {
  var valueStart = 0;
  var extent = valueAxis2.scale.getExtent();
  if (valueOrigin === "start") {
    valueStart = extent[0];
  } else if (valueOrigin === "end") {
    valueStart = extent[1];
  } else if (isNumber(valueOrigin) && !isNaN(valueOrigin)) {
    valueStart = valueOrigin;
  } else {
    if (extent[0] > 0) {
      valueStart = extent[0];
    } else if (extent[1] < 0) {
      valueStart = extent[1];
    }
  }
  return valueStart;
}
function getStackedOnPoint(dataCoordInfo, coordSys, data, idx) {
  var value = NaN;
  if (dataCoordInfo.stacked) {
    value = data.get(data.getCalculationInfo("stackedOverDimension"), idx);
  }
  if (isNaN(value)) {
    value = dataCoordInfo.valueStart;
  }
  var baseDataOffset = dataCoordInfo.baseDataOffset;
  var stackedData = [];
  stackedData[baseDataOffset] = data.get(dataCoordInfo.baseDim, idx);
  stackedData[1 - baseDataOffset] = value;
  return coordSys.dataToPoint(stackedData);
}
function isPointIllegal(xOrY, yOrX) {
  return !isFinite(xOrY) || !isFinite(yOrX);
}

// node_modules/echarts/lib/util/vendor.js
var Float32ArrayCtor = typeof Float32Array !== UNDEFINED_STR ? Float32Array : void 0;
var Float64ArrayCtor = typeof Float64Array !== UNDEFINED_STR ? Float64Array : void 0;
function createFloat32Array(capacity) {
  return tryEnsureTypedArray({
    ctor: Float32ArrayCtor
  }, capacity).arr;
}
function tryEnsureTypedArray(tyArr, capacity) {
  if (true) {
    assert(capacity != null && isFinite(capacity) && capacity >= 0 && tyArr.hasOwnProperty("ctor"));
  }
  var existingArr = tyArr.arr;
  var ctor = tyArr.ctor;
  if (capacity > MAX_SAFE_INTEGER2) {
    capacity = MAX_SAFE_INTEGER2;
  }
  if (!existingArr || tyArr.typed && existingArr.length < capacity) {
    var nextArr = void 0;
    if (ctor) {
      try {
        nextArr = new ctor(capacity);
        tyArr.typed = true;
        existingArr && nextArr.set(existingArr);
      } catch (e2) {
        if (true) {
          error(e2);
        }
      }
    }
    if (!nextArr) {
      nextArr = [];
      tyArr.typed = false;
      if (existingArr) {
        for (var i2 = 0, len2 = existingArr.length; i2 < len2; i2++) {
          nextArr[i2] = existingArr[i2];
        }
      }
    }
    tyArr.arr = nextArr;
  }
  return tyArr;
}

// node_modules/echarts/lib/chart/line/lineAnimationDiff.js
function diffData(oldData, newData) {
  var diffResult = [];
  newData.diff(oldData).add(function(idx) {
    diffResult.push({
      cmd: "+",
      idx
    });
  }).update(function(newIdx, oldIdx) {
    diffResult.push({
      cmd: "=",
      idx: oldIdx,
      idx1: newIdx
    });
  }).remove(function(idx) {
    diffResult.push({
      cmd: "-",
      idx
    });
  }).execute();
  return diffResult;
}
function lineAnimationDiff(oldData, newData, oldStackedOnPoints, newStackedOnPoints, oldCoordSys, newCoordSys, oldValueOrigin, newValueOrigin) {
  var diff = diffData(oldData, newData);
  var currPoints = [];
  var nextPoints = [];
  var currStackedPoints = [];
  var nextStackedPoints = [];
  var status = [];
  var sortedIndices = [];
  var rawIndices = [];
  var newDataOldCoordInfo = prepareDataCoordInfo(oldCoordSys, newData, oldValueOrigin);
  var oldPoints = oldData.getLayout("points") || [];
  var newPoints = newData.getLayout("points") || [];
  for (var i2 = 0; i2 < diff.length; i2++) {
    var diffItem = diff[i2];
    var pointAdded = true;
    var oldIdx2 = void 0;
    var newIdx2 = void 0;
    switch (diffItem.cmd) {
      case "=":
        oldIdx2 = diffItem.idx * 2;
        newIdx2 = diffItem.idx1 * 2;
        var currentX = oldPoints[oldIdx2];
        var currentY = oldPoints[oldIdx2 + 1];
        var nextX = newPoints[newIdx2];
        var nextY = newPoints[newIdx2 + 1];
        if (isNaN(currentX) || isNaN(currentY)) {
          currentX = nextX;
          currentY = nextY;
        }
        currPoints.push(currentX, currentY);
        nextPoints.push(nextX, nextY);
        currStackedPoints.push(oldStackedOnPoints[oldIdx2], oldStackedOnPoints[oldIdx2 + 1]);
        nextStackedPoints.push(newStackedOnPoints[newIdx2], newStackedOnPoints[newIdx2 + 1]);
        rawIndices.push(newData.getRawIndex(diffItem.idx1));
        break;
      case "+":
        var newIdx = diffItem.idx;
        var newDataDimsForPoint = newDataOldCoordInfo.dataDimsForPoint;
        var oldPt = oldCoordSys.dataToPoint([newData.get(newDataDimsForPoint[0], newIdx), newData.get(newDataDimsForPoint[1], newIdx)]);
        newIdx2 = newIdx * 2;
        currPoints.push(oldPt[0], oldPt[1]);
        nextPoints.push(newPoints[newIdx2], newPoints[newIdx2 + 1]);
        var stackedOnPoint = getStackedOnPoint(newDataOldCoordInfo, oldCoordSys, newData, newIdx);
        currStackedPoints.push(stackedOnPoint[0], stackedOnPoint[1]);
        nextStackedPoints.push(newStackedOnPoints[newIdx2], newStackedOnPoints[newIdx2 + 1]);
        rawIndices.push(newData.getRawIndex(newIdx));
        break;
      case "-":
        pointAdded = false;
    }
    if (pointAdded) {
      status.push(diffItem);
      sortedIndices.push(sortedIndices.length);
    }
  }
  sortedIndices.sort(function(a, b) {
    return rawIndices[a] - rawIndices[b];
  });
  var len2 = currPoints.length;
  var sortedCurrPoints = createFloat32Array(len2);
  var sortedNextPoints = createFloat32Array(len2);
  var sortedCurrStackedPoints = createFloat32Array(len2);
  var sortedNextStackedPoints = createFloat32Array(len2);
  var sortedStatus = [];
  for (var i2 = 0; i2 < sortedIndices.length; i2++) {
    var idx = sortedIndices[i2];
    var i22 = i2 * 2;
    var idx2 = idx * 2;
    sortedCurrPoints[i22] = currPoints[idx2];
    sortedCurrPoints[i22 + 1] = currPoints[idx2 + 1];
    sortedNextPoints[i22] = nextPoints[idx2];
    sortedNextPoints[i22 + 1] = nextPoints[idx2 + 1];
    sortedCurrStackedPoints[i22] = currStackedPoints[idx2];
    sortedCurrStackedPoints[i22 + 1] = currStackedPoints[idx2 + 1];
    sortedNextStackedPoints[i22] = nextStackedPoints[idx2];
    sortedNextStackedPoints[i22 + 1] = nextStackedPoints[idx2 + 1];
    sortedStatus[i2] = status[idx];
  }
  return {
    current: sortedCurrPoints,
    next: sortedNextPoints,
    stackedOnCurrent: sortedCurrStackedPoints,
    stackedOnNext: sortedNextStackedPoints,
    status: sortedStatus
  };
}

// node_modules/echarts/lib/chart/line/poly.js
var mathMin7 = Math.min;
var mathMax7 = Math.max;
function drawSegment(ctx, points4, start2, segLen, allLen, dir3, smooth, smoothMonotone, connectNulls) {
  var prevX;
  var prevY;
  var cpx0;
  var cpy0;
  var cpx1;
  var cpy1;
  var idx = start2;
  var k2 = 0;
  for (; k2 < segLen; k2++) {
    var x = points4[idx * 2];
    var y = points4[idx * 2 + 1];
    if (idx >= allLen || idx < 0) {
      break;
    }
    if (isPointIllegal(x, y)) {
      if (connectNulls) {
        idx += dir3;
        continue;
      }
      break;
    }
    if (idx === start2) {
      ctx[dir3 > 0 ? "moveTo" : "lineTo"](x, y);
      cpx0 = x;
      cpy0 = y;
    } else {
      var dx = x - prevX;
      var dy = y - prevY;
      if (dx * dx + dy * dy < 0.5) {
        idx += dir3;
        continue;
      }
      if (smooth > 0) {
        var nextIdx = idx + dir3;
        var nextX = points4[nextIdx * 2];
        var nextY = points4[nextIdx * 2 + 1];
        while (nextX === x && nextY === y && k2 < segLen) {
          k2++;
          nextIdx += dir3;
          idx += dir3;
          nextX = points4[nextIdx * 2];
          nextY = points4[nextIdx * 2 + 1];
          x = points4[idx * 2];
          y = points4[idx * 2 + 1];
          dx = x - prevX;
          dy = y - prevY;
        }
        var tmpK = k2 + 1;
        if (connectNulls) {
          while (isPointIllegal(nextX, nextY) && tmpK < segLen) {
            tmpK++;
            nextIdx += dir3;
            nextX = points4[nextIdx * 2];
            nextY = points4[nextIdx * 2 + 1];
          }
        }
        var ratioNextSeg = 0.5;
        var vx = 0;
        var vy = 0;
        var nextCpx0 = void 0;
        var nextCpy0 = void 0;
        if (tmpK >= segLen || isPointIllegal(nextX, nextY)) {
          cpx1 = x;
          cpy1 = y;
        } else {
          vx = nextX - prevX;
          vy = nextY - prevY;
          var dx0 = x - prevX;
          var dx1 = nextX - x;
          var dy0 = y - prevY;
          var dy1 = nextY - y;
          var lenPrevSeg = void 0;
          var lenNextSeg = void 0;
          if (smoothMonotone === "x") {
            lenPrevSeg = Math.abs(dx0);
            lenNextSeg = Math.abs(dx1);
            var dir_1 = vx > 0 ? 1 : -1;
            cpx1 = x - dir_1 * lenPrevSeg * smooth;
            cpy1 = y;
            nextCpx0 = x + dir_1 * lenNextSeg * smooth;
            nextCpy0 = y;
          } else if (smoothMonotone === "y") {
            lenPrevSeg = Math.abs(dy0);
            lenNextSeg = Math.abs(dy1);
            var dir_2 = vy > 0 ? 1 : -1;
            cpx1 = x;
            cpy1 = y - dir_2 * lenPrevSeg * smooth;
            nextCpx0 = x;
            nextCpy0 = y + dir_2 * lenNextSeg * smooth;
          } else {
            lenPrevSeg = Math.sqrt(dx0 * dx0 + dy0 * dy0);
            lenNextSeg = Math.sqrt(dx1 * dx1 + dy1 * dy1);
            ratioNextSeg = lenNextSeg / (lenNextSeg + lenPrevSeg);
            cpx1 = x - vx * smooth * (1 - ratioNextSeg);
            cpy1 = y - vy * smooth * (1 - ratioNextSeg);
            nextCpx0 = x + vx * smooth * ratioNextSeg;
            nextCpy0 = y + vy * smooth * ratioNextSeg;
            nextCpx0 = mathMin7(nextCpx0, mathMax7(nextX, x));
            nextCpy0 = mathMin7(nextCpy0, mathMax7(nextY, y));
            nextCpx0 = mathMax7(nextCpx0, mathMin7(nextX, x));
            nextCpy0 = mathMax7(nextCpy0, mathMin7(nextY, y));
            vx = nextCpx0 - x;
            vy = nextCpy0 - y;
            cpx1 = x - vx * lenPrevSeg / lenNextSeg;
            cpy1 = y - vy * lenPrevSeg / lenNextSeg;
            cpx1 = mathMin7(cpx1, mathMax7(prevX, x));
            cpy1 = mathMin7(cpy1, mathMax7(prevY, y));
            cpx1 = mathMax7(cpx1, mathMin7(prevX, x));
            cpy1 = mathMax7(cpy1, mathMin7(prevY, y));
            vx = x - cpx1;
            vy = y - cpy1;
            nextCpx0 = x + vx * lenNextSeg / lenPrevSeg;
            nextCpy0 = y + vy * lenNextSeg / lenPrevSeg;
          }
        }
        ctx.bezierCurveTo(cpx0, cpy0, cpx1, cpy1, x, y);
        cpx0 = nextCpx0;
        cpy0 = nextCpy0;
      } else {
        ctx.lineTo(x, y);
      }
    }
    prevX = x;
    prevY = y;
    idx += dir3;
  }
  return k2;
}
var ECPolylineShape = (
  /** @class */
  /* @__PURE__ */ (function() {
    function ECPolylineShape2() {
      this.smooth = 0;
      this.smoothConstraint = true;
    }
    return ECPolylineShape2;
  })()
);
var ECPolyline = (
  /** @class */
  (function(_super) {
    __extends(ECPolyline2, _super);
    function ECPolyline2(opts) {
      var _this = _super.call(this, opts) || this;
      _this.type = "ec-polyline";
      return _this;
    }
    ECPolyline2.prototype.getDefaultStyle = function() {
      return {
        stroke: tokens_default.color.neutral99,
        fill: null
      };
    };
    ECPolyline2.prototype.getDefaultShape = function() {
      return new ECPolylineShape();
    };
    ECPolyline2.prototype.buildPath = function(ctx, shape) {
      var points4 = shape.points;
      var i2 = 0;
      var len2 = points4.length / 2;
      if (shape.connectNulls) {
        for (; len2 > 0; len2--) {
          if (!isPointIllegal(points4[len2 * 2 - 2], points4[len2 * 2 - 1])) {
            break;
          }
        }
        for (; i2 < len2; i2++) {
          if (!isPointIllegal(points4[i2 * 2], points4[i2 * 2 + 1])) {
            break;
          }
        }
      }
      while (i2 < len2) {
        i2 += drawSegment(ctx, points4, i2, len2, len2, 1, shape.smooth, shape.smoothMonotone, shape.connectNulls) + 1;
      }
    };
    ECPolyline2.prototype.getPointOn = function(xOrY, dim) {
      if (!this.path) {
        this.createPathProxy();
        this.buildPath(this.path, this.shape);
      }
      var path = this.path;
      var data = path.data;
      var CMD5 = PathProxy_default.CMD;
      var x0;
      var y0;
      var isDimX = dim === "x";
      var roots2 = [];
      for (var i2 = 0; i2 < data.length; ) {
        var cmd = data[i2++];
        var x = void 0;
        var y = void 0;
        var x2 = void 0;
        var y2 = void 0;
        var x3 = void 0;
        var y3 = void 0;
        var t = void 0;
        switch (cmd) {
          case CMD5.M:
            x0 = data[i2++];
            y0 = data[i2++];
            break;
          case CMD5.L:
            x = data[i2++];
            y = data[i2++];
            t = isDimX ? (xOrY - x0) / (x - x0) : (xOrY - y0) / (y - y0);
            if (t <= 1 && t >= 0) {
              var val = isDimX ? (y - y0) * t + y0 : (x - x0) * t + x0;
              return isDimX ? [xOrY, val] : [val, xOrY];
            }
            x0 = x;
            y0 = y;
            break;
          case CMD5.C:
            x = data[i2++];
            y = data[i2++];
            x2 = data[i2++];
            y2 = data[i2++];
            x3 = data[i2++];
            y3 = data[i2++];
            var nRoot = isDimX ? cubicRootAt(x0, x, x2, x3, xOrY, roots2) : cubicRootAt(y0, y, y2, y3, xOrY, roots2);
            if (nRoot > 0) {
              for (var i_1 = 0; i_1 < nRoot; i_1++) {
                var t_1 = roots2[i_1];
                if (t_1 <= 1 && t_1 >= 0) {
                  var val = isDimX ? cubicAt(y0, y, y2, y3, t_1) : cubicAt(x0, x, x2, x3, t_1);
                  return isDimX ? [xOrY, val] : [val, xOrY];
                }
              }
            }
            x0 = x3;
            y0 = y3;
            break;
        }
      }
    };
    return ECPolyline2;
  })(Path_default)
);
var ECPolygonShape = (
  /** @class */
  (function(_super) {
    __extends(ECPolygonShape2, _super);
    function ECPolygonShape2() {
      return _super !== null && _super.apply(this, arguments) || this;
    }
    return ECPolygonShape2;
  })(ECPolylineShape)
);
var ECPolygon = (
  /** @class */
  (function(_super) {
    __extends(ECPolygon2, _super);
    function ECPolygon2(opts) {
      var _this = _super.call(this, opts) || this;
      _this.type = "ec-polygon";
      return _this;
    }
    ECPolygon2.prototype.getDefaultShape = function() {
      return new ECPolygonShape();
    };
    ECPolygon2.prototype.buildPath = function(ctx, shape) {
      var points4 = shape.points;
      var stackedOnPoints = shape.stackedOnPoints;
      var i2 = 0;
      var len2 = points4.length / 2;
      var smoothMonotone = shape.smoothMonotone;
      if (shape.connectNulls) {
        for (; len2 > 0; len2--) {
          if (!isPointIllegal(points4[len2 * 2 - 2], points4[len2 * 2 - 1])) {
            break;
          }
        }
        for (; i2 < len2; i2++) {
          if (!isPointIllegal(points4[i2 * 2], points4[i2 * 2 + 1])) {
            break;
          }
        }
      }
      while (i2 < len2) {
        var k2 = drawSegment(ctx, points4, i2, len2, len2, 1, shape.smooth, smoothMonotone, shape.connectNulls);
        drawSegment(ctx, stackedOnPoints, i2 + k2 - 1, k2, len2, -1, shape.stackedOnSmooth, smoothMonotone, shape.connectNulls);
        i2 += k2 + 1;
        ctx.closePath();
      }
    };
    return ECPolygon2;
  })(Path_default)
);

// node_modules/echarts/lib/chart/helper/createRenderPlanner.js
function createRenderPlanner() {
  var inner25 = makeInner();
  return function(seriesModel) {
    var fields = inner25(seriesModel);
    var pipelineContext = seriesModel.pipelineContext;
    var originalLarge = !!fields.large;
    var originalProgressive = !!fields.progressiveRender;
    var large = fields.large = !!(pipelineContext && pipelineContext.large);
    var progressive = fields.progressiveRender = !!(pipelineContext && pipelineContext.progressiveRender);
    return !!(originalLarge !== large || originalProgressive !== progressive) && "reset";
  };
}

// node_modules/echarts/lib/view/Chart.js
var inner4 = makeInner();
var renderPlanner = createRenderPlanner();
var ChartView = (
  /** @class */
  (function() {
    function ChartView2() {
      this.group = new Group_default();
      this.uid = getUID("viewChart");
      this.renderTask = createTask({
        plan: renderTaskPlan,
        reset: renderTaskReset
      });
      this.renderTask.context = {
        view: this
      };
    }
    ChartView2.prototype.init = function(ecModel, api) {
    };
    ChartView2.prototype.render = function(seriesModel, ecModel, api, payload) {
      if (true) {
        throw new Error("render method must been implemented");
      }
    };
    ChartView2.prototype.highlight = function(seriesModel, ecModel, api, payload) {
      var data = seriesModel.getData(payload && payload.dataType);
      if (!data) {
        if (true) {
          error("Unknown dataType " + payload.dataType);
        }
        return;
      }
      toggleHighlight(data, payload, "emphasis");
    };
    ChartView2.prototype.downplay = function(seriesModel, ecModel, api, payload) {
      var data = seriesModel.getData(payload && payload.dataType);
      if (!data) {
        if (true) {
          error("Unknown dataType " + payload.dataType);
        }
        return;
      }
      toggleHighlight(data, payload, "normal");
    };
    ChartView2.prototype.remove = function(ecModel, api) {
      this.group.removeAll();
    };
    ChartView2.prototype.dispose = function(ecModel, api) {
    };
    ChartView2.prototype.updateView = function(seriesModel, ecModel, api, payload) {
      this.render(seriesModel, ecModel, api, payload);
    };
    ChartView2.prototype.updateVisual = function(seriesModel, ecModel, api, payload) {
      this.render(seriesModel, ecModel, api, payload);
    };
    ChartView2.prototype.eachRendered = function(cb) {
      traverseElements(this.group, cb);
    };
    ChartView2.markUpdateMethod = function(payload, methodName) {
      inner4(payload).updateMethod = methodName;
    };
    ChartView2.protoInitialize = (function() {
      var proto = ChartView2.prototype;
      proto.type = "chart";
    })();
    return ChartView2;
  })()
);
function elSetState(el, state2, highlightDigit) {
  if (el && isHighDownDispatcher(el)) {
    (state2 === "emphasis" ? enterEmphasis : leaveEmphasis)(el, highlightDigit);
  }
}
function toggleHighlight(data, payload, state2) {
  var dataIndex = queryDataIndex(data, payload);
  var highlightDigit = payload && payload.highlightKey != null ? getHighlightDigit(payload.highlightKey) : null;
  if (dataIndex != null) {
    each(normalizeToArray(dataIndex), function(dataIdx) {
      elSetState(data.getItemGraphicEl(dataIdx), state2, highlightDigit);
    });
  } else {
    data.eachItemGraphicEl(function(el) {
      elSetState(el, state2, highlightDigit);
    });
  }
}
enableClassExtend(ChartView, ["dispose"]);
enableClassManagement(ChartView);
function renderTaskPlan(context) {
  return renderPlanner(context.model);
}
function renderTaskReset(context) {
  var seriesModel = context.model;
  var ecModel = context.ecModel;
  var api = context.api;
  var payload = context.payload;
  var progressiveRender = seriesModel.pipelineContext.progressiveRender;
  var view = context.view;
  var updateMethod = payload && inner4(payload).updateMethod;
  var methodName = progressiveRender ? "incrementalPrepareRender" : updateMethod && view[updateMethod] ? updateMethod : "render";
  if (methodName !== "render") {
    view[methodName](seriesModel, ecModel, api, payload);
  }
  return progressMethodMap[methodName];
}
var progressMethodMap = {
  incrementalPrepareRender: {
    progress: function(params, context) {
      context.view.incrementalRender(params, context.model, context.ecModel, context.api, context.payload);
    }
  },
  render: {
    // Put view.render in `progress` to support appendData. But in this case
    // view.render should not be called in reset, otherwise it will be called
    // twise. Use `forceFirstProgress` to make sure that view.render is called
    // in any cases.
    forceFirstProgress: true,
    progress: function(params, context) {
      context.view.render(context.model, context.ecModel, context.api, context.payload);
    }
  }
};
var Chart_default = ChartView;

// node_modules/echarts/lib/chart/helper/createClipPathFromCoordSys.js
function createGridClipPath(cartesian, hasAnimation, seriesModel, done, during) {
  var rect = cartesian.getArea();
  var x = rect.x;
  var y = rect.y;
  var width = rect.width;
  var height = rect.height;
  var lineWidth = seriesModel.get(["lineStyle", "width"]) || 0;
  x -= lineWidth / 2;
  y -= lineWidth / 2;
  width += lineWidth;
  height += lineWidth;
  width = Math.ceil(width);
  if (x !== Math.floor(x)) {
    x = Math.floor(x);
    width++;
  }
  var clipPath = new Rect_default({
    shape: {
      x,
      y,
      width,
      height
    }
  });
  if (hasAnimation) {
    var baseAxis = cartesian.getBaseAxis();
    var isHorizontal = baseAxis.isHorizontal();
    var isAxisInversed = baseAxis.inverse;
    if (isHorizontal) {
      if (isAxisInversed) {
        clipPath.shape.x += width;
      }
      clipPath.shape.width = 0;
    } else {
      if (!isAxisInversed) {
        clipPath.shape.y += height;
      }
      clipPath.shape.height = 0;
    }
    var duringCb = isFunction(during) ? function(percent) {
      during(percent, clipPath);
    } : null;
    initProps(clipPath, {
      shape: {
        width,
        height,
        x,
        y
      }
    }, seriesModel, null, done, duringCb);
  }
  return clipPath;
}
function createPolarClipPath(polar, hasAnimation, seriesModel) {
  var sectorArea = polar.getArea();
  var r0 = round2(sectorArea.r0, 1);
  var r = round2(sectorArea.r, 1);
  var clipPath = new Sector_default({
    shape: {
      cx: round2(polar.cx, 1),
      cy: round2(polar.cy, 1),
      r0,
      r,
      startAngle: sectorArea.startAngle,
      endAngle: sectorArea.endAngle,
      clockwise: sectorArea.clockwise
    }
  });
  if (hasAnimation) {
    var isRadial = polar.getBaseAxis().dim === "angle";
    if (isRadial) {
      clipPath.shape.endAngle = sectorArea.startAngle;
    } else {
      clipPath.shape.r = r0;
    }
    initProps(clipPath, {
      shape: {
        endAngle: sectorArea.endAngle,
        r
      }
    }, seriesModel);
  }
  return clipPath;
}
function createClipPath(coordSys, hasAnimation, seriesModel, done, during) {
  if (!coordSys) {
    return null;
  } else if (coordSys.type === "polar") {
    return createPolarClipPath(coordSys, hasAnimation, seriesModel);
  } else if (coordSys.type === "cartesian2d") {
    return createGridClipPath(coordSys, hasAnimation, seriesModel, done, during);
  }
  return null;
}
var SHAPE_CLIP_KIND_NOT_CLIPPED = 0;
var SHAPE_CLIP_KIND_PARTIALLY_CLIPPED = 1;
var SHAPE_CLIP_KIND_FULLY_CLIPPED = 2;
function updateClipPath(clip2, symbolEl, clipPath) {
  if (clip2) {
    if (true) {
      assert(clipPath);
    }
    symbolEl.setClipPath(clipPath);
  } else {
    symbolEl.removeClipPath();
  }
}
function createCoordSysClipAreaSimply(seriesModel) {
  var coordSys = seriesModel.coordinateSystem;
  if (seriesModel.get("clip", true) && coordSys && (!coordSys.shouldClip || coordSys.shouldClip())) {
    return coordSys.getArea && coordSys.getArea(0.1);
  }
}

// node_modules/echarts/lib/coord/CoordinateSystem.js
function isCoordinateSystemType(coordSys, type) {
  return coordSys.type === type;
}
function isGeoLikeCoordSys(coordSys) {
  var dimensions = coordSys.dimensions;
  return dimensions[0] === "lng" && dimensions[1] === "lat" && !!coordSys.getViewRect;
}

// node_modules/echarts/lib/util/styleCompat.js
var deprecatedLogs = {};
function isEC4CompatibleStyle(style2, elType, hasOwnTextContentOption, hasOwnTextConfig) {
  return style2 && (style2.legacy || style2.legacy !== false && !hasOwnTextContentOption && !hasOwnTextConfig && elType !== "tspan" && (elType === "text" || hasOwn(style2, "text")));
}
function convertFromEC4CompatibleStyle(hostStyle, elType, isNormal) {
  var srcStyle = hostStyle;
  var textConfig;
  var textContent;
  var textContentStyle;
  if (elType === "text") {
    textContentStyle = srcStyle;
  } else {
    textContentStyle = {};
    hasOwn(srcStyle, "text") && (textContentStyle.text = srcStyle.text);
    hasOwn(srcStyle, "rich") && (textContentStyle.rich = srcStyle.rich);
    hasOwn(srcStyle, "textFill") && (textContentStyle.fill = srcStyle.textFill);
    hasOwn(srcStyle, "textStroke") && (textContentStyle.stroke = srcStyle.textStroke);
    hasOwn(srcStyle, "fontFamily") && (textContentStyle.fontFamily = srcStyle.fontFamily);
    hasOwn(srcStyle, "fontSize") && (textContentStyle.fontSize = srcStyle.fontSize);
    hasOwn(srcStyle, "fontStyle") && (textContentStyle.fontStyle = srcStyle.fontStyle);
    hasOwn(srcStyle, "fontWeight") && (textContentStyle.fontWeight = srcStyle.fontWeight);
    textContent = {
      type: "text",
      style: textContentStyle,
      // ec4 does not support rectText trigger.
      // And when text position is different in normal and emphasis
      // => hover text trigger emphasis;
      // => text position changed, leave mouse pointer immediately;
      // That might cause incorrect state.
      silent: true
    };
    textConfig = {};
    var hasOwnPos = hasOwn(srcStyle, "textPosition");
    if (isNormal) {
      textConfig.position = hasOwnPos ? srcStyle.textPosition : "inside";
    } else {
      hasOwnPos && (textConfig.position = srcStyle.textPosition);
    }
    hasOwn(srcStyle, "textPosition") && (textConfig.position = srcStyle.textPosition);
    hasOwn(srcStyle, "textOffset") && (textConfig.offset = srcStyle.textOffset);
    hasOwn(srcStyle, "textRotation") && (textConfig.rotation = srcStyle.textRotation);
    hasOwn(srcStyle, "textDistance") && (textConfig.distance = srcStyle.textDistance);
  }
  convertEC4CompatibleRichItem(textContentStyle, hostStyle);
  each(textContentStyle.rich, function(richItem) {
    convertEC4CompatibleRichItem(richItem, richItem);
  });
  return {
    textConfig,
    textContent
  };
}
function convertEC4CompatibleRichItem(out2, richItem) {
  if (!richItem) {
    return;
  }
  richItem.font = richItem.textFont || richItem.font;
  hasOwn(richItem, "textStrokeWidth") && (out2.lineWidth = richItem.textStrokeWidth);
  hasOwn(richItem, "textAlign") && (out2.align = richItem.textAlign);
  hasOwn(richItem, "textVerticalAlign") && (out2.verticalAlign = richItem.textVerticalAlign);
  hasOwn(richItem, "textLineHeight") && (out2.lineHeight = richItem.textLineHeight);
  hasOwn(richItem, "textWidth") && (out2.width = richItem.textWidth);
  hasOwn(richItem, "textHeight") && (out2.height = richItem.textHeight);
  hasOwn(richItem, "textBackgroundColor") && (out2.backgroundColor = richItem.textBackgroundColor);
  hasOwn(richItem, "textPadding") && (out2.padding = richItem.textPadding);
  hasOwn(richItem, "textBorderColor") && (out2.borderColor = richItem.textBorderColor);
  hasOwn(richItem, "textBorderWidth") && (out2.borderWidth = richItem.textBorderWidth);
  hasOwn(richItem, "textBorderRadius") && (out2.borderRadius = richItem.textBorderRadius);
  hasOwn(richItem, "textBoxShadowColor") && (out2.shadowColor = richItem.textBoxShadowColor);
  hasOwn(richItem, "textBoxShadowBlur") && (out2.shadowBlur = richItem.textBoxShadowBlur);
  hasOwn(richItem, "textBoxShadowOffsetX") && (out2.shadowOffsetX = richItem.textBoxShadowOffsetX);
  hasOwn(richItem, "textBoxShadowOffsetY") && (out2.shadowOffsetY = richItem.textBoxShadowOffsetY);
}
function convertToEC4StyleForCustomSerise(itemStl, txStl, txCfg) {
  var out2 = itemStl;
  out2.textPosition = out2.textPosition || txCfg.position || "inside";
  txCfg.offset != null && (out2.textOffset = txCfg.offset);
  txCfg.rotation != null && (out2.textRotation = txCfg.rotation);
  txCfg.distance != null && (out2.textDistance = txCfg.distance);
  var isInside = out2.textPosition.indexOf("inside") >= 0;
  var hostFill = itemStl.fill || tokens_default.color.neutral99;
  convertToEC4RichItem(out2, txStl);
  var textFillNotSet = out2.textFill == null;
  if (isInside) {
    if (textFillNotSet) {
      out2.textFill = txCfg.insideFill || tokens_default.color.neutral00;
      !out2.textStroke && txCfg.insideStroke && (out2.textStroke = txCfg.insideStroke);
      !out2.textStroke && (out2.textStroke = hostFill);
      out2.textStrokeWidth == null && (out2.textStrokeWidth = 2);
    }
  } else {
    if (textFillNotSet) {
      out2.textFill = itemStl.fill || txCfg.outsideFill || tokens_default.color.neutral00;
    }
    !out2.textStroke && txCfg.outsideStroke && (out2.textStroke = txCfg.outsideStroke);
  }
  out2.text = txStl.text;
  out2.rich = txStl.rich;
  each(txStl.rich, function(richItem) {
    convertToEC4RichItem(richItem, richItem);
  });
  return out2;
}
function convertToEC4RichItem(out2, richItem) {
  if (!richItem) {
    return;
  }
  hasOwn(richItem, "fill") && (out2.textFill = richItem.fill);
  hasOwn(richItem, "stroke") && (out2.textStroke = richItem.fill);
  hasOwn(richItem, "lineWidth") && (out2.textStrokeWidth = richItem.lineWidth);
  hasOwn(richItem, "font") && (out2.font = richItem.font);
  hasOwn(richItem, "fontStyle") && (out2.fontStyle = richItem.fontStyle);
  hasOwn(richItem, "fontWeight") && (out2.fontWeight = richItem.fontWeight);
  hasOwn(richItem, "fontSize") && (out2.fontSize = richItem.fontSize);
  hasOwn(richItem, "fontFamily") && (out2.fontFamily = richItem.fontFamily);
  hasOwn(richItem, "align") && (out2.textAlign = richItem.align);
  hasOwn(richItem, "verticalAlign") && (out2.textVerticalAlign = richItem.verticalAlign);
  hasOwn(richItem, "lineHeight") && (out2.textLineHeight = richItem.lineHeight);
  hasOwn(richItem, "width") && (out2.textWidth = richItem.width);
  hasOwn(richItem, "height") && (out2.textHeight = richItem.height);
  hasOwn(richItem, "backgroundColor") && (out2.textBackgroundColor = richItem.backgroundColor);
  hasOwn(richItem, "padding") && (out2.textPadding = richItem.padding);
  hasOwn(richItem, "borderColor") && (out2.textBorderColor = richItem.borderColor);
  hasOwn(richItem, "borderWidth") && (out2.textBorderWidth = richItem.borderWidth);
  hasOwn(richItem, "borderRadius") && (out2.textBorderRadius = richItem.borderRadius);
  hasOwn(richItem, "shadowColor") && (out2.textBoxShadowColor = richItem.shadowColor);
  hasOwn(richItem, "shadowBlur") && (out2.textBoxShadowBlur = richItem.shadowBlur);
  hasOwn(richItem, "shadowOffsetX") && (out2.textBoxShadowOffsetX = richItem.shadowOffsetX);
  hasOwn(richItem, "shadowOffsetY") && (out2.textBoxShadowOffsetY = richItem.shadowOffsetY);
  hasOwn(richItem, "textShadowColor") && (out2.textShadowColor = richItem.textShadowColor);
  hasOwn(richItem, "textShadowBlur") && (out2.textShadowBlur = richItem.textShadowBlur);
  hasOwn(richItem, "textShadowOffsetX") && (out2.textShadowOffsetX = richItem.textShadowOffsetX);
  hasOwn(richItem, "textShadowOffsetY") && (out2.textShadowOffsetY = richItem.textShadowOffsetY);
}
function warnDeprecated(deprecated, insteadApproach) {
  if (true) {
    var key2 = deprecated + "^_^" + insteadApproach;
    if (!deprecatedLogs[key2]) {
      console.warn('[ECharts] DEPRECATED: "' + deprecated + '" has been deprecated. ' + insteadApproach);
      deprecatedLogs[key2] = true;
    }
  }
}

// node_modules/echarts/lib/scale/Scale.js
var Scale = (
  /** @class */
  (function() {
    function Scale2() {
    }
    Scale2.prototype.isBlank = function() {
      return this._isBlank;
    };
    Scale2.prototype.setBlank = function(isBlank) {
      this._isBlank = isBlank;
    };
    return Scale2;
  })()
);
enableClassManagement(Scale);
var Scale_default = Scale;

// node_modules/echarts/lib/data/OrdinalMeta.js
var uidBase = 0;
var OrdinalMeta = (
  /** @class */
  (function() {
    function OrdinalMeta2(opt) {
      this.categories = opt.categories || [];
      this._needCollect = opt.needCollect;
      this._deduplication = opt.deduplication;
      this.uid = ++uidBase;
      this._onCollect = opt.onCollect;
    }
    OrdinalMeta2.createByAxisModel = function(axisModel) {
      var option = axisModel.option;
      var data = option.data;
      var categories = data && map2(data, getName);
      return new OrdinalMeta2({
        categories,
        needCollect: !categories,
        // deduplication is default in axis.
        deduplication: option.dedplication !== false
      });
    };
    ;
    OrdinalMeta2.prototype.getOrdinal = function(category) {
      return this._getOrCreateMap().get(category);
    };
    OrdinalMeta2.prototype.parseAndCollect = function(category) {
      var index;
      var needCollect = this._needCollect;
      if (!isString(category) && !needCollect) {
        return category;
      }
      if (needCollect && !this._deduplication) {
        index = this.categories.length;
        this.categories[index] = category;
        this._onCollect && this._onCollect(category, index);
        return index;
      }
      var map4 = this._getOrCreateMap();
      index = map4.get(category);
      if (index == null) {
        if (needCollect) {
          index = this.categories.length;
          this.categories[index] = category;
          map4.set(category, index);
          this._onCollect && this._onCollect(category, index);
        } else {
          index = NaN;
        }
      }
      return index;
    };
    OrdinalMeta2.prototype._getOrCreateMap = function() {
      return this._map || (this._map = createHashMap(this.categories));
    };
    return OrdinalMeta2;
  })()
);
function getName(obj) {
  if (isObject(obj) && obj.value != null) {
    return obj.value;
  } else {
    return obj + "";
  }
}
var OrdinalMeta_default = OrdinalMeta;

// node_modules/echarts/lib/scale/scaleMapper.js
var SCALE_EXTENT_KIND_EFFECTIVE = 0;
var SCALE_EXTENT_KIND_MAPPING = 1;
var SCALE_MAPPER_METHOD_NAMES_MAP = {
  needTransform: 1,
  normalize: 1,
  scale: 1,
  transformIn: 1,
  transformOut: 1,
  contain: 1,
  getExtent: 1,
  getExtentUnsafe: 1,
  setExtent: 1,
  setExtent2: 1,
  getFilter: 1,
  sanitize: 1,
  getDefaultStartValue: 1,
  freeze: 1
};
var SCALE_MAPPER_METHOD_NAMES = keys(SCALE_MAPPER_METHOD_NAMES_MAP);
var SCALE_MAPPER_DEPTH_OUT_OF_BREAK = 2;
var SCALE_MAPPER_DEPTH_INNERMOST = 3;
function initBreakOrLinearMapper(mapper, breakParsed, initialExtent) {
  var brk;
  mapper = mapper || {};
  var scaleBreakHelper = getScaleBreakHelper();
  if (scaleBreakHelper) {
    var brkMapper_1 = scaleBreakHelper.createBreakScaleMapper(breakParsed, initialExtent);
    if (brkMapper_1.hasBreaks()) {
      each(SCALE_MAPPER_METHOD_NAMES, function(methodName) {
        if (brkMapper_1[methodName]) {
          mapper[methodName] = bind(brkMapper_1[methodName], brkMapper_1);
        }
      });
      brk = brkMapper_1;
    }
  }
  if (brk == null) {
    initLinearScaleMapper(mapper, initialExtent);
  }
  return {
    brk,
    mapper
  };
}
function decorateScaleMapper(host, decoratedMapperMethods) {
  each(SCALE_MAPPER_METHOD_NAMES, function(methodName) {
    host[methodName] = decoratedMapperMethods[methodName];
  });
}
function enableScaleMapperFreeze(host, subMapper) {
  host.freeze = noop;
  if (true) {
    host.freeze = function() {
      subMapper.freeze();
    };
  }
  ;
}
function getScaleExtentForTickUnsafe(mapper) {
  return mapper.getExtentUnsafe(SCALE_EXTENT_KIND_EFFECTIVE, SCALE_MAPPER_DEPTH_OUT_OF_BREAK);
}
function getScaleExtentForMappingUnsafe(mapper, depth) {
  return mapper.getExtentUnsafe(SCALE_EXTENT_KIND_MAPPING, depth) || mapper.getExtentUnsafe(SCALE_EXTENT_KIND_EFFECTIVE, depth);
}
function getScaleLinearSpanForMapping(mapper) {
  var extent = getScaleExtentForMappingUnsafe(mapper, SCALE_MAPPER_DEPTH_INNERMOST);
  return extent[1] - extent[0];
}
function getScaleLinearSpanEffective(mapper) {
  var extent = mapper.getExtentUnsafe(SCALE_EXTENT_KIND_EFFECTIVE, SCALE_MAPPER_DEPTH_INNERMOST);
  return extent[1] - extent[0];
}
function initLinearScaleMapper(mapper, initialExtent) {
  var linearMapper = mapper || {};
  var extendList = [];
  linearMapper._extents = extendList;
  extendList[SCALE_EXTENT_KIND_EFFECTIVE] = initialExtent ? initialExtent.slice() : initExtentForUnion();
  extend(linearMapper, linearScaleMapperMethods);
  return linearMapper;
}
var linearScaleMapperMethods = {
  needTransform: function() {
    return false;
  },
  normalize: function(val) {
    var extent = this._extents[SCALE_EXTENT_KIND_MAPPING] || this._extents[SCALE_EXTENT_KIND_EFFECTIVE];
    if (extent[1] === extent[0]) {
      return 0.5;
    }
    return (val - extent[0]) / (extent[1] - extent[0]);
  },
  scale: function(val) {
    var extent = this._extents[SCALE_EXTENT_KIND_MAPPING] || this._extents[SCALE_EXTENT_KIND_EFFECTIVE];
    return val * (extent[1] - extent[0]) + extent[0];
  },
  transformIn: function(val) {
    return val;
  },
  transformOut: function(val) {
    return val;
  },
  contain: function(val) {
    var extent = getScaleExtentForMappingUnsafe(this, null);
    return val >= extent[0] && val <= extent[1];
  },
  getExtent: function() {
    return this._extents[SCALE_EXTENT_KIND_EFFECTIVE].slice();
  },
  getExtentUnsafe: function(kind) {
    return this._extents[kind];
  },
  setExtent: function(start2, end2) {
    if (true) {
      assert(!this._frozen);
    }
    writeExtent(this._extents, SCALE_EXTENT_KIND_EFFECTIVE, start2, end2);
  },
  setExtent2: function(kind, start2, end2) {
    if (true) {
      assert(!this._frozen);
    }
    var extentList = this._extents;
    if (!extentList[kind]) {
      extentList[kind] = extentList[SCALE_EXTENT_KIND_EFFECTIVE].slice();
    }
    writeExtent(extentList, kind, start2, end2);
  },
  freeze: function() {
    if (true) {
      this._frozen = true;
    }
  }
};
function writeExtent(extentList, kind, start2, end2) {
  if (isValidBoundsForExtent(start2, end2)) {
    extentList[kind][0] = start2;
    extentList[kind][1] = end2;
  } else {
    if (true) {
      if (start2 != null && end2 != null && start2 <= end2) {
        error("Invalid setExtent call - start: " + start2 + ", end: " + end2);
      }
    }
  }
}

// node_modules/echarts/lib/scale/helper.js
function isIntervalOrLogScale(scale3) {
  return isIntervalScale(scale3) || isLogScale(scale3);
}
function isIntervalScale(scale3) {
  return scale3.type === "interval";
}
function isTimeScale(scale3) {
  return scale3.type === "time";
}
function isLogScale(scale3) {
  return scale3.type === "log";
}
function isOrdinalScale(scale3) {
  return scale3.type === "ordinal";
}
function increaseInterval(niceInterval) {
  var exponent = quantityExponent(niceInterval);
  var exp10 = mathPow2(10, exponent);
  var f = mathRound2(niceInterval / exp10);
  if (!f) {
    f = 1;
  } else if (f === 2) {
    f = 3;
  } else if (f === 3) {
    f = 5;
  } else {
    f *= 2;
  }
  return round2(f * exp10, -exponent);
}
function getIntervalPrecision(niceInterval) {
  return getPrecision(niceInterval) + 2;
}
function logScaleLogTick(val, base2) {
  return mathLog(val) / mathLog(base2);
}
function logScalePowTick(linearTickVal, base2, opt) {
  var lookup = opt && opt.lookup;
  if (lookup) {
    for (var i2 = 0; i2 < lookup.from.length; i2++) {
      if (linearTickVal === lookup.from[i2]) {
        return lookup.to[i2];
      }
    }
  }
  return mathPow2(base2, linearTickVal);
}
function intervalScaleEnsureValidExtent(rawExtent, fixMinMax, rawExtentResult) {
  var extent = rawExtent.slice();
  if (extent[0] === extent[1]) {
    var containShapeRequired = rawExtentResult && rawExtentResult.ctnShp;
    if (extent[0] !== 0) {
      var expandSize = mathAbs3(extent[0]);
      if (!fixMinMax[1]) {
        extent[1] += expandSize / 2;
        extent[0] -= expandSize / 2;
      } else {
        extent[0] -= expandSize / 2;
      }
    } else {
      if (containShapeRequired) {
        extent[0] = -1;
        extent[1] = 1;
      } else {
        extent[1] = 1;
      }
    }
  }
  if (!isValidNumberForExtent(extent[0]) || !isValidNumberForExtent(extent[1])) {
    extent[0] = 0;
    extent[1] = 1;
  }
  if (extent[1] < extent[0]) {
    extent.reverse();
  }
  return extent;
}
function extentDiffers(extent1, extent2) {
  return [extent1[0] !== extent2[0], extent1[1] !== extent2[1]];
}
function ensureValidSplitNumber(rawSplitNumber, defaultSplitNumber) {
  rawSplitNumber = rawSplitNumber || defaultSplitNumber;
  return mathRound2(mathMax4(rawSplitNumber, 1));
}
function ordinalScaleCreateTicks(ordinalScale, categoryInterval, addItem) {
  var extent = getScaleExtentForTickUnsafe(ordinalScale);
  var startTick = extent[0];
  var tickCount = ordinalScale.count();
  var step = Math.max((categoryInterval || 0) + 1, 1);
  if (startTick !== 0 && step > 1 && tickCount / step > 2) {
    startTick = Math.round(Math.ceil(startTick / step) * step);
  }
  if (startTick !== extent[0]) {
    addItemInternally(extent[0], true, true);
  }
  var tickValue = startTick;
  for (; tickValue <= extent[1]; tickValue += step) {
    addItemInternally(tickValue, false, tickValue === extent[0] || tickValue === extent[1]);
  }
  if (tickValue - step !== extent[1]) {
    addItemInternally(extent[1], true, true);
  }
  function addItemInternally(tickValue2, offInterval, isExtentBoundary) {
    addItem({
      value: tickValue2,
      offInterval
    }, isExtentBoundary);
  }
}

// node_modules/echarts/lib/scale/Ordinal.js
var OrdinalScale = (
  /** @class */
  (function(_super) {
    __extends(OrdinalScale2, _super);
    function OrdinalScale2(setting) {
      var _this = _super.call(this) || this;
      _this.type = "ordinal";
      _this.parse = OrdinalScale2.parse;
      decorateScaleMapper(_this, OrdinalScale2.decoratedMethods);
      var ordinalMeta = setting.ordinalMeta;
      if (!ordinalMeta) {
        ordinalMeta = new OrdinalMeta_default({});
      }
      if (isArray(ordinalMeta)) {
        ordinalMeta = new OrdinalMeta_default({
          categories: map2(ordinalMeta, function(item) {
            return isObject(item) ? item.value : item;
          })
        });
      }
      _this._ordinalMeta = ordinalMeta;
      var res = initBreakOrLinearMapper(
        null,
        null,
        // Do not support break in OrdinalScale yet.
        setting.extent || [0, ordinalMeta.categories.length - 1]
      );
      _this._mapper = res.mapper;
      enableScaleMapperFreeze(_this, res.mapper);
      return _this;
    }
    OrdinalScale2.parse = function(val) {
      if (val == null) {
        val = NaN;
      } else if (isString(val)) {
        val = this._ordinalMeta.getOrdinal(val);
        if (val == null) {
          val = NaN;
        }
      } else {
        val = mathRound2(val);
      }
      return val;
    };
    OrdinalScale2.prototype.getTicks = function() {
      var ticks = [];
      ordinalScaleCreateTicks(this, 0, function(tick) {
        ticks.push(tick);
      });
      return ticks;
    };
    OrdinalScale2.prototype.getMinorTicks = function(splitNumber) {
      return;
    };
    OrdinalScale2.prototype.setSortInfo = function(info) {
      if (info == null) {
        this._ordinalNumbersByTick = this._ticksByOrdinalNumber = null;
        return;
      }
      var infoOrdinalNumbers = info.ordinalNumbers;
      var ordinalsByTick = this._ordinalNumbersByTick = [];
      var ticksByOrdinal = this._ticksByOrdinalNumber = [];
      var tickNum = 0;
      var allCategoryLen = this._ordinalMeta.categories.length;
      for (var len2 = mathMin4(allCategoryLen, infoOrdinalNumbers.length); tickNum < len2; ++tickNum) {
        var ordinalNumber = ordinalsByTick[tickNum] = infoOrdinalNumbers[tickNum];
        ticksByOrdinal[ordinalNumber] = tickNum;
      }
      var unusedOrdinal = 0;
      for (; tickNum < allCategoryLen; ++tickNum) {
        while (ticksByOrdinal[unusedOrdinal] != null) {
          unusedOrdinal++;
        }
        ;
        ordinalsByTick[tickNum] = unusedOrdinal;
        ticksByOrdinal[unusedOrdinal] = tickNum;
      }
    };
    OrdinalScale2.prototype._getTickNumber = function(ordinal) {
      var ticksByOrdinalNumber = this._ticksByOrdinalNumber;
      return ticksByOrdinalNumber && ordinal >= 0 && ordinal < ticksByOrdinalNumber.length ? ticksByOrdinalNumber[ordinal] : ordinal;
    };
    OrdinalScale2.prototype.getRawOrdinalNumber = function(tickValue) {
      var ordinalNumbersByTick = this._ordinalNumbersByTick;
      return ordinalNumbersByTick && tickValue >= 0 && tickValue < ordinalNumbersByTick.length ? ordinalNumbersByTick[tickValue] : tickValue;
    };
    OrdinalScale2.prototype.getLabel = function(tick) {
      if (!this.isBlank()) {
        var ordinalNumber = this.getRawOrdinalNumber(tick.value);
        var category = this._ordinalMeta.categories[ordinalNumber];
        return category == null ? "" : category + "";
      }
    };
    OrdinalScale2.prototype.count = function() {
      var extent = getScaleExtentForTickUnsafe(this._mapper);
      return extent[1] - extent[0] + 1;
    };
    OrdinalScale2.prototype.getOrdinalMeta = function() {
      return this._ordinalMeta;
    };
    OrdinalScale2.type = "ordinal";
    OrdinalScale2.decoratedMethods = {
      needTransform: function() {
        return this._mapper.needTransform();
      },
      contain: function(val) {
        return this._mapper.contain(this._getTickNumber(val)) && val >= 0 && val < this._ordinalMeta.categories.length;
      },
      normalize: function(val) {
        return this._mapper.normalize(this._getTickNumber(val));
      },
      scale: function(val) {
        return this.getRawOrdinalNumber(mathRound2(this._mapper.scale(val)));
      },
      transformIn: function(val, opt) {
        return this._mapper.transformIn(this._getTickNumber(val), opt);
      },
      transformOut: function(val, opt) {
        return this.getRawOrdinalNumber(this._mapper.transformOut(val, opt));
      },
      getExtent: function() {
        return this._mapper.getExtent();
      },
      getExtentUnsafe: function(kind, depth) {
        return this._mapper.getExtentUnsafe(kind, depth);
      },
      /**
       * NOTICE: OrdinalScale extent should always originates from
       * `[0, ordinalMeta.categories.length - 1]`, regardless of min/max of `series.data`.
       * But settings like `xxxAxis.min/max` can still modify the extent.
       * It is handled by constructor of `ScaleRawExtentInfo`.
       */
      setExtent: function(start2, end2) {
        return this._mapper.setExtent(start2, end2);
      },
      setExtent2: function(kind, start2, end2) {
        return this._mapper.setExtent2(kind, start2, end2);
      }
    };
    return OrdinalScale2;
  })(Scale_default)
);
Scale_default.registerClass(OrdinalScale);
var Ordinal_default = OrdinalScale;

// node_modules/echarts/lib/scale/minorTicks.js
function getMinorTicks(scale3, splitNumber, breaks, scaleInterval) {
  var ticks = scale3.getTicks({
    expandToNicedExtent: true
  });
  var minorTicks = [];
  var extent = scale3.getExtent();
  for (var i2 = 1; i2 < ticks.length; i2++) {
    var nextTick = ticks[i2];
    var prevTick = ticks[i2 - 1];
    if (prevTick["break"] || nextTick["break"]) {
      continue;
    }
    var count2 = 0;
    var minorTicksGroup = [];
    var interval2 = nextTick.value - prevTick.value;
    var minorInterval = interval2 / splitNumber;
    var minorIntervalPrecision = getIntervalPrecision(minorInterval);
    while (count2 < splitNumber - 1) {
      var minorTick = round2(prevTick.value + (count2 + 1) * minorInterval, minorIntervalPrecision);
      if (minorTick > extent[0] && minorTick < extent[1]) {
        minorTicksGroup.push(minorTick);
      }
      count2++;
    }
    var scaleBreakHelper = getScaleBreakHelper();
    scaleBreakHelper && scaleBreakHelper.pruneTicksByBreak("auto", minorTicksGroup, breaks, function(value) {
      return value;
    }, scaleInterval, extent);
    minorTicks.push(minorTicksGroup);
  }
  return minorTicks;
}

// node_modules/echarts/lib/scale/Interval.js
var IntervalScale = (
  /** @class */
  (function(_super) {
    __extends(IntervalScale2, _super);
    function IntervalScale2(setting) {
      var _this = _super.call(this) || this;
      _this.type = "interval";
      _this.parse = IntervalScale2.parse;
      setting = setting || {};
      var breakParsed = simplyParseBreakOption(_this, setting);
      var res = initBreakOrLinearMapper(_this, breakParsed, null);
      _this.brk = res.brk;
      _this._cfg = {
        interval: 0,
        intervalPrecision: 2,
        intervalCount: void 0,
        niceExtent: void 0
      };
      return _this;
    }
    IntervalScale2.parse = function(val) {
      return val == null || val === "" ? NaN : Number(val);
    };
    IntervalScale2.prototype.getConfig = function() {
      return clone(this._cfg);
    };
    IntervalScale2.prototype.setConfig = function(cfg) {
      var extent = getScaleExtentForTickUnsafe(this);
      if (true) {
        assert(cfg.interval != null);
        if (cfg.intervalCount != null) {
          assert(cfg.intervalCount >= -1 && cfg.intervalPrecision != null && !hasBreaks(this));
        }
        if (cfg.niceExtent != null) {
          assert(isFinite(cfg.niceExtent[0]) && isFinite(cfg.niceExtent[1]));
          assert(extent[0] <= cfg.niceExtent[0] && cfg.niceExtent[1] <= extent[1]);
          assert(round2(cfg.niceExtent[0] - cfg.niceExtent[1], getPrecision(cfg.interval)) <= cfg.interval);
        }
      }
      this._cfg = cfg = clone(cfg);
      if (cfg.niceExtent == null) {
        cfg.niceExtent = extent.slice();
      }
      if (cfg.intervalPrecision == null) {
        cfg.intervalPrecision = getIntervalPrecision(cfg.interval);
      }
    };
    IntervalScale2.prototype.getTicks = function(opt) {
      opt = opt || {};
      var cfg = this._cfg;
      var interval2 = cfg.interval;
      var extent = getScaleExtentForTickUnsafe(this);
      var niceExtent = cfg.niceExtent;
      var intervalPrecision = cfg.intervalPrecision;
      var scaleBreakHelper = getScaleBreakHelper();
      var brk = this.brk;
      var brkAvailable = scaleBreakHelper && brk;
      var ticks = [];
      if (!interval2) {
        return ticks;
      }
      if (opt.breakTicks === "only_break" && brkAvailable) {
        scaleBreakHelper.addBreaksToTicks(ticks, brk.breaks, extent);
        return ticks;
      }
      if (true) {
        assert(niceExtent != null);
      }
      var safeLimit = 3e3;
      if (extent[0] < niceExtent[0]) {
        ticks.push({
          value: opt.expandToNicedExtent ? round2(niceExtent[0] - interval2, intervalPrecision) : extent[0]
        });
      }
      var estimateNiceMultiple = function(tickVal, targetTick) {
        return mathRound2((targetTick - tickVal) / interval2);
      };
      var intervalCount = cfg.intervalCount;
      for (var tick = niceExtent[0], niceTickIdx = 0; ; niceTickIdx++) {
        if (intervalCount == null) {
          if (tick > niceExtent[1] || !isFinite(tick) || !isFinite(niceExtent[1])) {
            break;
          }
        } else {
          if (niceTickIdx > intervalCount) {
            break;
          }
          tick = mathMin4(tick, niceExtent[1]);
          if (niceTickIdx === intervalCount) {
            tick = niceExtent[1];
          }
        }
        ticks.push({
          value: tick
        });
        tick = round2(tick + interval2, intervalPrecision);
        if (brk) {
          var moreMultiple = brk.calcNiceTickMultiple(tick, estimateNiceMultiple);
          if (moreMultiple >= 0) {
            tick = round2(tick + moreMultiple * interval2, intervalPrecision);
          }
        }
        if (ticks.length > 0 && tick === ticks[ticks.length - 1].value) {
          break;
        }
        if (ticks.length > safeLimit) {
          if (true) {
            warn('Exceed safe limit in IntervalScale["getTicks"].');
          }
          return [];
        }
      }
      var lastNiceTick = ticks.length ? ticks[ticks.length - 1].value : niceExtent[1];
      if (extent[1] > lastNiceTick) {
        ticks.push({
          value: opt.expandToNicedExtent ? round2(lastNiceTick + interval2, intervalPrecision) : extent[1]
        });
      }
      if (brkAvailable) {
        scaleBreakHelper.pruneTicksByBreak(opt.pruneByBreak, ticks, brk.breaks, function(item) {
          return item.value;
        }, cfg.interval, extent);
      }
      if (brkAvailable && opt.breakTicks !== "none") {
        scaleBreakHelper.addBreaksToTicks(ticks, brk.breaks, extent);
      }
      return ticks;
    };
    IntervalScale2.prototype.getMinorTicks = function(splitNumber) {
      return getMinorTicks(this, splitNumber, getBreaksUnsafe(this), this._cfg.interval);
    };
    IntervalScale2.prototype.getLabel = function(tick, opt) {
      if (tick == null) {
        return "";
      }
      var precision = opt && opt.precision;
      if (precision == null) {
        precision = getPrecision(tick.value) || 0;
      } else if (precision === "auto") {
        precision = this._cfg.intervalPrecision;
      }
      var dataNum = round2(tick.value, precision, true);
      return addCommas(dataNum);
    };
    IntervalScale2.type = "interval";
    return IntervalScale2;
  })(Scale_default)
);
Scale_default.registerClass(IntervalScale);
var Interval_default = IntervalScale;

// node_modules/echarts/lib/scale/Time.js
var bisect = function(a, x, lo, hi) {
  while (lo < hi) {
    var mid = lo + hi >>> 1;
    if (a[mid][1] < x) {
      lo = mid + 1;
    } else {
      hi = mid;
    }
  }
  return lo;
};
var TimeScale = (
  /** @class */
  (function(_super) {
    __extends(TimeScale2, _super);
    function TimeScale2(setting) {
      var _this = _super.call(this) || this;
      _this.type = "time";
      _this.parse = TimeScale2.parse;
      _this._locale = setting.locale;
      _this._useUTC = setting.useUTC;
      _this._interval = 0;
      var breakParsed = simplyParseBreakOption(_this, setting);
      var res = initBreakOrLinearMapper(_this, breakParsed, null);
      _this.brk = res.brk;
      return _this;
    }
    TimeScale2.prototype.getLabel = function(tick) {
      return format(tick.value, fullLeveledFormatter[getDefaultFormatPrecisionOfInterval(getPrimaryTimeUnit(this._minLevelUnit))] || fullLeveledFormatter.second, this._useUTC, this._locale);
    };
    TimeScale2.prototype.getFormattedLabel = function(tick, idx, labelFormatter) {
      return leveledFormat(tick, idx, labelFormatter, this._locale, this._useUTC);
    };
    TimeScale2.prototype.getTicks = function(opt) {
      opt = opt || {};
      var interval2 = this._interval;
      var extent = getScaleExtentForTickUnsafe(this);
      var scaleBreakHelper = getScaleBreakHelper();
      var brk = this.brk;
      var brkAvailable = scaleBreakHelper && brk;
      var ticks = [];
      if (!interval2) {
        return ticks;
      }
      var useUTC = this._useUTC;
      if (brkAvailable && opt.breakTicks === "only_break") {
        getScaleBreakHelper().addBreaksToTicks(ticks, brk.breaks, extent);
        return ticks;
      }
      ticks = createIntervalTicks(this._minLevelUnit, this._approxInterval, useUTC, extent, getScaleLinearSpanEffective(this), brk);
      var upperUnitIndex = primaryTimeUnits.length - 1;
      var maxLevel = 0;
      each(ticks, function(tick) {
        if (tick.time) {
          upperUnitIndex = Math.min(upperUnitIndex, indexOf(primaryTimeUnits, tick.time.upperTimeUnit));
          maxLevel = Math.max(maxLevel, tick.time.level);
        }
      });
      if (brkAvailable) {
        getScaleBreakHelper().pruneTicksByBreak(opt.pruneByBreak, ticks, brk.breaks, function(item) {
          return item.value;
        }, this._approxInterval, extent);
      }
      if (brkAvailable && opt.breakTicks !== "none") {
        getScaleBreakHelper().addBreaksToTicks(ticks, brk.breaks, extent, function(trimmedBrk) {
          var lowerBrkUnitIndex = Math.max(indexOf(primaryTimeUnits, getUnitFromValue(trimmedBrk.vmin, useUTC)), indexOf(primaryTimeUnits, getUnitFromValue(trimmedBrk.vmax, useUTC)));
          var upperBrkUnitIndex = 0;
          for (var unitIdx = 0; unitIdx < primaryTimeUnits.length; unitIdx++) {
            if (!isPrimaryUnitValueAndGreaterSame(primaryTimeUnits[unitIdx], trimmedBrk.vmin, trimmedBrk.vmax, useUTC)) {
              upperBrkUnitIndex = unitIdx;
              break;
            }
          }
          var upperIdx = Math.min(upperBrkUnitIndex, upperUnitIndex);
          var lowerIdx = Math.max(upperIdx, lowerBrkUnitIndex);
          return {
            level: maxLevel,
            lowerTimeUnit: primaryTimeUnits[lowerIdx],
            upperTimeUnit: primaryTimeUnits[upperIdx]
          };
        });
      }
      return ticks;
    };
    TimeScale2.prototype.getMinorTicks = function(splitNumber) {
      return getMinorTicks(this, splitNumber, getBreaksUnsafe(this), this._interval);
    };
    TimeScale2.prototype.setTimeInterval = function(opt) {
      this._interval = opt.interval;
      this._approxInterval = opt.approxInterval;
      this._minLevelUnit = opt.minLevelUnit;
    };
    TimeScale2.parse = function(val) {
      return isNumber(val) ? Math.round(val) : +parseDate(val);
    };
    TimeScale2.type = "time";
    return TimeScale2;
  })(Scale_default)
);
var scaleIntervals = [
  // Format                           interval
  ["second", ONE_SECOND],
  ["minute", ONE_MINUTE],
  ["hour", ONE_HOUR],
  ["quarter-day", ONE_HOUR * 6],
  ["half-day", ONE_HOUR * 12],
  ["day", ONE_DAY * 1.2],
  ["half-week", ONE_DAY * 3.5],
  ["week", ONE_DAY * 7],
  ["month", ONE_DAY * 31],
  ["quarter", ONE_DAY * 95],
  ["half-year", ONE_YEAR / 2],
  ["year", ONE_YEAR]
  // 1Y
];
function isPrimaryUnitValueAndGreaterSame(unit, valueA, valueB, isUTC) {
  return roundTime(new Date(valueA), unit, isUTC).getTime() === roundTime(new Date(valueB), unit, isUTC).getTime();
}
function getDateInterval(approxInterval, daysInMonth) {
  approxInterval /= ONE_DAY;
  return approxInterval > 16 ? 16 : approxInterval > 7.5 ? 7 : approxInterval > 3.5 ? 4 : approxInterval > 1.5 ? 2 : 1;
}
function getMonthInterval(approxInterval) {
  var APPROX_ONE_MONTH = 30 * ONE_DAY;
  approxInterval /= APPROX_ONE_MONTH;
  return approxInterval > 6 ? 6 : approxInterval > 3 ? 3 : approxInterval > 2 ? 2 : 1;
}
function getHourInterval(approxInterval) {
  approxInterval /= ONE_HOUR;
  return approxInterval > 12 ? 12 : approxInterval > 6 ? 6 : approxInterval > 3.5 ? 4 : approxInterval > 2 ? 2 : 1;
}
function getMinutesAndSecondsInterval(approxInterval, isMinutes) {
  approxInterval /= isMinutes ? ONE_MINUTE : ONE_SECOND;
  return approxInterval > 30 ? 30 : approxInterval > 20 ? 20 : approxInterval > 15 ? 15 : approxInterval > 10 ? 10 : approxInterval > 5 ? 5 : approxInterval > 2 ? 2 : 1;
}
function getMillisecondsInterval(approxInterval) {
  return mathMax4(nice(approxInterval, true), 1);
}
function getFirstTimestampOfUnit(timestamp, unitName, isUTC) {
  var upperUnitIdx = Math.max(0, indexOf(primaryTimeUnits, unitName) - 1);
  return roundTime(new Date(timestamp), primaryTimeUnits[upperUnitIdx], isUTC).getTime();
}
function createEstimateNiceMultiple(setMethodName, dateMethodInterval) {
  var tmpDate = /* @__PURE__ */ new Date(0);
  tmpDate[setMethodName](1);
  var tmpTime = tmpDate.getTime();
  tmpDate[setMethodName](1 + dateMethodInterval);
  var approxTimeInterval = tmpDate.getTime() - tmpTime;
  return function(tickVal, targetValue) {
    return Math.max(0, Math.round((targetValue - tickVal) / approxTimeInterval));
  };
}
function createIntervalTicks(bottomUnitName, approxInterval, isUTC, extent, innermostSpan, brk) {
  var safeLimit = 3e3;
  var unitNames = timeUnits;
  var iter = 0;
  function addTicksInSpan(interval2, minTimestamp, maxTimestamp, getMethodName, setMethodName, isDate, out2) {
    var estimateNiceMultiple = createEstimateNiceMultiple(setMethodName, interval2);
    var dateTime = minTimestamp;
    var date = new Date(dateTime);
    while (dateTime < maxTimestamp && dateTime <= extent[1]) {
      out2.push({
        value: dateTime
      });
      if (iter++ > safeLimit) {
        if (true) {
          warn('Exceed safe limit in TimeScale["getTicks"].');
        }
        break;
      }
      date[setMethodName](date[getMethodName]() + interval2);
      dateTime = date.getTime();
      if (brk) {
        var moreMultiple = brk.calcNiceTickMultiple(dateTime, estimateNiceMultiple);
        if (moreMultiple > 0) {
          date[setMethodName](date[getMethodName]() + moreMultiple * interval2);
          dateTime = date.getTime();
        }
      }
    }
    out2.push({
      value: dateTime,
      // extent[1] should be added; deduplication will be performed later.
      notAdd: dateTime > extent[1]
    });
  }
  function addLevelTicks(unitName, lastLevelTicks, levelTicks2) {
    var newAddedTicks = [];
    var isFirstLevel = !lastLevelTicks.length;
    if (isPrimaryUnitValueAndGreaterSame(getPrimaryTimeUnit(unitName), extent[0], extent[1], isUTC)) {
      return;
    }
    if (isFirstLevel) {
      lastLevelTicks = [{
        value: getFirstTimestampOfUnit(extent[0], unitName, isUTC)
      }, {
        value: extent[1]
      }];
    }
    for (var i3 = 0; i3 < lastLevelTicks.length - 1; i3++) {
      var startTick = lastLevelTicks[i3].value;
      var endTick = lastLevelTicks[i3 + 1].value;
      if (startTick === endTick) {
        continue;
      }
      var interval2 = void 0;
      var getterName = void 0;
      var setterName = void 0;
      var isDate = false;
      switch (unitName) {
        case "year":
          interval2 = Math.max(1, Math.round(approxInterval / ONE_DAY / 365));
          getterName = fullYearGetterName(isUTC);
          setterName = fullYearSetterName(isUTC);
          break;
        case "half-year":
        case "quarter":
        case "month":
          interval2 = getMonthInterval(approxInterval);
          getterName = monthGetterName(isUTC);
          setterName = monthSetterName(isUTC);
          break;
        case "week":
        // PENDING If week is added. Ignore day.
        case "half-week":
        case "day":
          interval2 = getDateInterval(approxInterval, 31);
          getterName = dateGetterName(isUTC);
          setterName = dateSetterName(isUTC);
          isDate = true;
          break;
        case "half-day":
        case "quarter-day":
        case "hour":
          interval2 = getHourInterval(approxInterval);
          getterName = hoursGetterName(isUTC);
          setterName = hoursSetterName(isUTC);
          break;
        case "minute":
          interval2 = getMinutesAndSecondsInterval(approxInterval, true);
          getterName = minutesGetterName(isUTC);
          setterName = minutesSetterName(isUTC);
          break;
        case "second":
          interval2 = getMinutesAndSecondsInterval(approxInterval, false);
          getterName = secondsGetterName(isUTC);
          setterName = secondsSetterName(isUTC);
          break;
        case "millisecond":
          interval2 = getMillisecondsInterval(approxInterval);
          getterName = millisecondsGetterName(isUTC);
          setterName = millisecondsSetterName(isUTC);
          break;
      }
      if (endTick >= extent[0] && startTick <= extent[1]) {
        addTicksInSpan(interval2, startTick, endTick, getterName, setterName, isDate, newAddedTicks);
      }
      if (unitName === "year" && levelTicks2.length > 1 && i3 === 0) {
        levelTicks2.unshift({
          value: levelTicks2[0].value - interval2
        });
      }
    }
    for (var i3 = 0; i3 < newAddedTicks.length; i3++) {
      levelTicks2.push(newAddedTicks[i3]);
    }
  }
  var levelsTicks = [];
  var currentLevelTicks = [];
  var tickCount = 0;
  var lastLevelTickCount = 0;
  for (var i2 = 0; i2 < unitNames.length; ++i2) {
    var primaryTimeUnit = getPrimaryTimeUnit(unitNames[i2]);
    if (!isPrimaryTimeUnit(unitNames[i2])) {
      continue;
    }
    addLevelTicks(unitNames[i2], levelsTicks[levelsTicks.length - 1] || [], currentLevelTicks);
    var nextPrimaryTimeUnit = unitNames[i2 + 1] ? getPrimaryTimeUnit(unitNames[i2 + 1]) : null;
    if (primaryTimeUnit !== nextPrimaryTimeUnit) {
      if (currentLevelTicks.length) {
        lastLevelTickCount = tickCount;
        currentLevelTicks.sort(function(a, b) {
          return a.value - b.value;
        });
        var levelTicksRemoveDuplicated = [];
        for (var i_1 = 0; i_1 < currentLevelTicks.length; ++i_1) {
          var tickValue = currentLevelTicks[i_1].value;
          if (i_1 === 0 || currentLevelTicks[i_1 - 1].value !== tickValue) {
            levelTicksRemoveDuplicated.push(currentLevelTicks[i_1]);
            if (tickValue >= extent[0] && tickValue <= extent[1]) {
              tickCount++;
            }
          }
        }
        var targetTickNum = innermostSpan / approxInterval;
        if (tickCount > targetTickNum * 1.5 && lastLevelTickCount > targetTickNum / 1.5) {
          break;
        }
        levelsTicks.push(levelTicksRemoveDuplicated);
        if (tickCount > targetTickNum || bottomUnitName === unitNames[i2]) {
          break;
        }
      }
      currentLevelTicks = [];
    }
  }
  var levelsTicksInExtent = filter2(map2(levelsTicks, function(levelTicks2) {
    return filter2(levelTicks2, function(tick) {
      return tick.value >= extent[0] && tick.value <= extent[1] && !tick.notAdd;
    });
  }), function(levelTicks2) {
    return levelTicks2.length > 0;
  });
  var maxLevel = levelsTicksInExtent.length - 1;
  var ticks = [];
  for (var i2 = 0; i2 < levelsTicksInExtent.length; ++i2) {
    var levelTicks = levelsTicksInExtent[i2];
    for (var k2 = 0; k2 < levelTicks.length; ++k2) {
      var unit = getUnitFromValue(levelTicks[k2].value, isUTC);
      ticks.push({
        value: levelTicks[k2].value,
        time: {
          level: maxLevel - i2,
          upperTimeUnit: unit,
          lowerTimeUnit: unit
        }
      });
    }
  }
  removeDuplicates(ticks, removeDuplicatesGetKeyFromValueProp, null);
  ticks.sort(function(a, b) {
    return a.value - b.value;
  });
  var currMinTick = ticks[0];
  var currMaxTick = ticks[ticks.length - 1];
  var extent0Unit = getUnitFromValue(extent[0], isUTC);
  var extent1Unit = getUnitFromValue(extent[1], isUTC);
  if (!currMinTick || currMinTick.value > extent[0]) {
    ticks.unshift({
      value: extent[0],
      time: {
        level: 0,
        upperTimeUnit: extent0Unit,
        lowerTimeUnit: extent0Unit
      },
      notNice: true
    });
  }
  if (!currMaxTick || currMaxTick.value < extent[1]) {
    ticks.push({
      value: extent[1],
      time: {
        level: 0,
        upperTimeUnit: extent1Unit,
        lowerTimeUnit: extent1Unit
      },
      notNice: true
    });
  }
  return ticks;
}
var calcNiceForTimeScale = function(scale3, opt) {
  var extent = scale3.getExtent();
  if (extent[0] === extent[1]) {
    extent[0] -= ONE_DAY;
    extent[1] += ONE_DAY;
  }
  if (extent[1] === -Infinity && extent[0] === Infinity) {
    var d = /* @__PURE__ */ new Date();
    extent[1] = +new Date(d.getFullYear(), d.getMonth(), d.getDate());
    extent[0] = extent[1] - ONE_DAY;
  }
  scale3.setExtent(extent[0], extent[1]);
  var splitNumber = ensureValidSplitNumber(opt.splitNumber, 10);
  var approxInterval = getScaleLinearSpanEffective(scale3) / splitNumber;
  var minInterval = opt.minInterval;
  var maxInterval = opt.maxInterval;
  if (minInterval != null && approxInterval < minInterval) {
    approxInterval = minInterval;
  }
  if (maxInterval != null && approxInterval > maxInterval) {
    approxInterval = maxInterval;
  }
  var scaleIntervalsLen = scaleIntervals.length;
  var idx = Math.min(bisect(scaleIntervals, approxInterval, 0, scaleIntervalsLen), scaleIntervalsLen - 1);
  var interval2 = scaleIntervals[idx][1];
  var minLevelUnit = scaleIntervals[Math.max(idx - 1, 0)][0];
  scale3.setTimeInterval({
    approxInterval,
    interval: interval2,
    minLevelUnit
  });
};
Scale_default.registerClass(TimeScale);
var Time_default = TimeScale;

// node_modules/echarts/lib/scale/Log.js
var LOOKUP_IDX_EXTENT_START = 0;
var LOOKUP_IDX_EXTENT_END = 1;
var LOOKUP_IDX_BREAK_START = 2;
var LogScale = (
  /** @class */
  (function(_super) {
    __extends(LogScale2, _super);
    function LogScale2(setting) {
      var _this = _super.call(this) || this;
      _this.type = "log";
      _this.parse = Interval_default.parse;
      _this.base = setting.logBase || 10;
      var lookupFrom = [];
      var lookupTo = [];
      var lookup = _this._lookup = {
        from: lookupFrom,
        to: lookupTo
      };
      lookupFrom[LOOKUP_IDX_EXTENT_START] = lookupFrom[LOOKUP_IDX_EXTENT_END] = lookupTo[LOOKUP_IDX_EXTENT_START] = lookupTo[LOOKUP_IDX_EXTENT_END] = NaN;
      decorateScaleMapper(_this, LogScale2.mapperMethods);
      var scaleBreakHelper = getScaleBreakHelper();
      var breakOption = setting.breakOption;
      var out2 = {
        lookup
      };
      if (scaleBreakHelper) {
        scaleBreakHelper.parseAxisBreakOptionInwardTransform(breakOption, _this, {
          noNegative: true
        }, LOOKUP_IDX_BREAK_START, out2);
      }
      _this.powStub = new Interval_default({
        breakParsed: out2.original
      });
      _this.intervalStub = new Interval_default({
        breakParsed: out2.transformed
      });
      enableScaleMapperFreeze(_this, _this.intervalStub);
      return _this;
    }
    LogScale2.prototype.getTicks = function(opt) {
      var base2 = this.base;
      var powStub = this.powStub;
      var scaleBreakHelper = getScaleBreakHelper();
      var intervalStub = this.intervalStub;
      var intervalExtent = intervalStub.getExtent();
      var powExtent = powStub.getExtent();
      var powOpt = {
        lookup: {
          from: intervalExtent,
          to: powExtent
        }
      };
      return map2(intervalStub.getTicks(opt || {}), function(tick) {
        var val = tick.value;
        var powVal = logScalePowTick(val, base2, powOpt);
        var vBreak;
        if (scaleBreakHelper) {
          var brkPowResult = scaleBreakHelper.getTicksBreakOutwardTransform(this, tick, getBreaksUnsafe(powStub), this._lookup);
          if (brkPowResult) {
            vBreak = brkPowResult.vBreak;
            powVal = brkPowResult.tickVal;
          }
        }
        return {
          value: powVal,
          "break": vBreak
        };
      }, this);
    };
    LogScale2.prototype.getMinorTicks = function(splitNumber) {
      return getMinorTicks(
        this,
        splitNumber,
        getBreaksUnsafe(this.powStub),
        // NOTE: minor ticks are in the log scale value to visually hint users "logarithm".
        this.intervalStub.getConfig().interval
      );
    };
    LogScale2.prototype.getLabel = function(data, opt) {
      return this.intervalStub.getLabel(data, opt);
    };
    LogScale2.type = "log";
    LogScale2.mapperMethods = {
      needTransform: function() {
        return true;
      },
      normalize: function(val) {
        return this.intervalStub.normalize(logScaleLogTick(val, this.base));
      },
      scale: function(val) {
        return logScalePowTick(this.intervalStub.scale(val), this.base, null);
      },
      transformIn: function(val, opt) {
        val = logScaleLogTick(val, this.base);
        return opt && opt.depth === SCALE_MAPPER_DEPTH_OUT_OF_BREAK ? val : this.intervalStub.transformIn(val, opt);
      },
      transformOut: function(val, opt) {
        var depth = opt ? opt.depth : null;
        tmpTransformOutOpt1.depth = depth;
        tmpTransformOutOpt2.lookup = this._lookup;
        return logScalePowTick(depth === SCALE_MAPPER_DEPTH_OUT_OF_BREAK ? val : this.intervalStub.transformOut(val, tmpTransformOutOpt1), this.base, tmpTransformOutOpt2);
      },
      contain: function(val) {
        return this.powStub.contain(val);
      },
      /**
       * NOTICE: The caller should ensure `start` and `end` are both non-negative.
       */
      setExtent: function(start2, end2) {
        this.setExtent2(SCALE_EXTENT_KIND_EFFECTIVE, start2, end2);
      },
      setExtent2: function(kind, start2, end2) {
        if (!isValidBoundsForExtent(start2, end2) || start2 <= 0 || end2 <= 0) {
          return;
        }
        var lookupTo = tmpNotUsedArr;
        var lookupFrom = tmpNotUsedArr;
        if (kind === SCALE_EXTENT_KIND_EFFECTIVE) {
          var lookup = this._lookup;
          lookupTo = lookup.to;
          lookupFrom = lookup.from;
        }
        this.powStub.setExtent2(kind, lookupTo[LOOKUP_IDX_EXTENT_START] = start2, lookupTo[LOOKUP_IDX_EXTENT_END] = end2);
        var base2 = this.base;
        this.intervalStub.setExtent2(kind, lookupFrom[LOOKUP_IDX_EXTENT_START] = logScaleLogTick(start2, base2), lookupFrom[LOOKUP_IDX_EXTENT_END] = logScaleLogTick(end2, base2));
      },
      getFilter: function() {
        return {
          g: 0
        };
      },
      sanitize: function(value, dataExtent) {
        if (isValidBoundsForExtent(dataExtent[0], dataExtent[1]) && isNullableNumberFinite(value) && value <= 0) {
          value = dataExtent[0];
        }
        return value;
      },
      getDefaultStartValue: function() {
        return 1;
      },
      getExtent: function() {
        return this.powStub.getExtent();
      },
      getExtentUnsafe: function(kind, depth) {
        return depth === null ? this.powStub.getExtentUnsafe(kind, null) : this.intervalStub.getExtentUnsafe(kind, depth);
      }
    };
    return LogScale2;
  })(Scale_default)
);
Scale_default.registerClass(LogScale);
var tmpTransformOutOpt1 = {};
var tmpTransformOutOpt2 = {};
var tmpNotUsedArr = [];
var Log_default = LogScale;

// node_modules/echarts/lib/coord/axisCommonTypes.js
var AXIS_TYPES = {
  value: 1,
  category: 1,
  time: 1,
  log: 1
};

// node_modules/echarts/lib/coord/axisHelper.js
var axisInner = makeInner();
function determineAxisType(model2) {
  var type = model2.get("type");
  if (
    // In ec option, `xxxAxis.type` may be undefined.
    type == null || !hasOwn(AXIS_TYPES, type) && !Scale_default.getClass(type)
  ) {
    type = "value";
  }
  return type;
}
function createScaleByModel(model2, type, coordSysSupportAxisBreaks) {
  var breakHelper = getScaleBreakHelper();
  var breakOption;
  if (breakHelper) {
    breakOption = retrieveAxisBreaksOption(model2, type, coordSysSupportAxisBreaks);
  }
  switch (type) {
    case "category":
      return new Ordinal_default({
        ordinalMeta: model2.getOrdinalMeta ? model2.getOrdinalMeta() : model2.getCategories(),
        extent: initExtentForUnion()
      });
    case "time":
      return new Time_default({
        locale: model2.ecModel.getLocaleModel(),
        useUTC: model2.ecModel.get("useUTC"),
        breakOption
      });
    case "log":
      return new Log_default({
        logBase: model2.get("logBase"),
        breakOption
      });
    case "value":
      return new Interval_default({
        breakOption
      });
    default:
      return new (Scale_default.getClass(type) || Interval_default)({});
  }
}
function getScaleValuePositionKind(scale3, value, considerMappingExtent) {
  var dataExtent = considerMappingExtent ? getScaleExtentForMappingUnsafe(scale3, null) : scale3.getExtentUnsafe(SCALE_EXTENT_KIND_EFFECTIVE, null);
  var min3 = dataExtent[0];
  var max3 = dataExtent[1];
  return !isValidBoundsForExtent(min3, max3) ? SCALE_VALUE_POSITION_KIND_OUTSIDE : min3 === value || max3 === value ? SCALE_VALUE_POSITION_KIND_EDGE : min3 < value && max3 > value ? SCALE_VALUE_POSITION_KIND_INSIDE : SCALE_VALUE_POSITION_KIND_OUTSIDE;
}
var SCALE_VALUE_POSITION_KIND_INSIDE = 1;
var SCALE_VALUE_POSITION_KIND_EDGE = 2;
var SCALE_VALUE_POSITION_KIND_OUTSIDE = 3;
function discourageOnAxisZero(axis) {
  axisInner(axis).noOnMyZero = true;
}
function isOnAxisZeroDiscouraged(axis) {
  return axisInner(axis).noOnMyZero;
}
function makeLabelFormatter(axis) {
  var labelFormatter = axis.getLabelModel().get("formatter");
  if (axis.type === "time") {
    var parsed_1 = parseTimeAxisLabelFormatter(labelFormatter);
    return function(tick, idx) {
      return axis.scale.getFormattedLabel(tick, idx, parsed_1);
    };
  } else if (isString(labelFormatter)) {
    return function(tick) {
      var label = axis.scale.getLabel(tick);
      var text = labelFormatter.replace("{value}", label != null ? label : "");
      return text;
    };
  } else if (isFunction(labelFormatter)) {
    if (axis.type === "category") {
      return function(tick, idx) {
        return labelFormatter(
          getAxisRawValue(axis, tick),
          tick.value - axis.scale.getExtent()[0],
          null
          // Using `null` just for backward compat.
        );
      };
    }
    var scaleBreakHelper_1 = getScaleBreakHelper();
    return function(tick, idx) {
      var extra = null;
      if (scaleBreakHelper_1) {
        extra = scaleBreakHelper_1.makeAxisLabelFormatterParamBreak(extra, tick["break"]);
      }
      return labelFormatter(getAxisRawValue(axis, tick), idx, extra);
    };
  } else {
    return function(tick) {
      return axis.scale.getLabel(tick);
    };
  }
}
function getAxisRawValue(axis, tick) {
  var scale3 = axis.scale;
  return isOrdinalScale(scale3) ? scale3.getLabel(tick) : tick.value;
}
function getOptionCategoryInterval(model2) {
  var interval2 = model2.get("interval");
  return interval2 == null ? "auto" : interval2;
}
function shouldShowAllLabels(axis) {
  return axis.type === "category" && getOptionCategoryInterval(axis.getLabelModel()) === 0;
}
function getDataDimensionsOnAxis(data, axisDim) {
  var dataDimMap = {};
  each(data.mapDimensionsAll(axisDim), function(dataDim) {
    dataDimMap[getStackedDimension(data, dataDim)] = true;
  });
  return keys(dataDimMap);
}
function isNameLocationCenter(nameLocation) {
  return nameLocation === "middle" || nameLocation === "center";
}
function shouldAxisShow(axisModel) {
  return axisModel.getShallow("show");
}
function retrieveAxisBreaksOption(model2, axisType, coordSysSupportAxisBreaks) {
  var option = model2.get("breaks", true);
  if (option != null) {
    if (!getScaleBreakHelper()) {
      if (true) {
        error('Must `import {AxisBreak} from "echarts/features.js"; use(AxisBreak);` first if using breaks option.');
      }
      return void 0;
    }
    if (!coordSysSupportAxisBreaks || !isAxisTypeSupportAxisBreak(axisType)) {
      if (true) {
        var axisInfo = model2 instanceof Component_default ? " " + model2.type + "[" + model2.componentIndex + "]" : "";
        error("Axis" + axisInfo + " does not support break.");
      }
      return void 0;
    }
    return option;
  }
}
function isAxisTypeSupportAxisBreak(axisType) {
  return axisType !== "category";
}
function updateIntervalOrLogScaleForNiceOrAligned(scale3, fixMinMax, oldIntervalExtent, newIntervalExtent, oldOutermostExtent, cfg) {
  var isTargetLogScale = isLogScale(scale3);
  var intervalStub = isTargetLogScale ? scale3.intervalStub : scale3;
  intervalStub.setExtent(newIntervalExtent[0], newIntervalExtent[1]);
  if (isTargetLogScale) {
    var powStub = scale3.powStub;
    var opt = {
      depth: SCALE_MAPPER_DEPTH_OUT_OF_BREAK
    };
    var minPow = scale3.transformOut(newIntervalExtent[0], opt);
    var maxPow = scale3.transformOut(newIntervalExtent[1], opt);
    var extentChanged = extentDiffers(oldIntervalExtent, newIntervalExtent);
    if (fixMinMax[0] && !extentChanged[0]) {
      minPow = oldOutermostExtent[0];
    }
    if (fixMinMax[1] && !extentChanged[1]) {
      maxPow = oldOutermostExtent[1];
    }
    powStub.setExtent(minPow, maxPow);
  }
  intervalStub.setConfig(cfg);
}
function getTickValueOutermost(scale3, tick) {
  return isOrdinalScale(scale3) ? scale3.getRawOrdinalNumber(tick.value) : tick.value;
}
function isAxisOnBand(scale3, axisModel) {
  return isOrdinalScale(scale3) && !!axisModel.get("boundaryGap");
}

// node_modules/echarts/lib/chart/line/LineView.js
function isPointsSame(points1, points22) {
  if (points1.length !== points22.length) {
    return;
  }
  for (var i2 = 0; i2 < points1.length; i2++) {
    if (points1[i2] !== points22[i2]) {
      return;
    }
  }
  return true;
}
function xyExtentFromPoints(points4) {
  var xExtent = initExtentForUnion();
  var yExtent = initExtentForUnion();
  for (var i2 = 0; i2 < points4.length; ) {
    var x = points4[i2++];
    var y = points4[i2++];
    if (!isPointIllegal(x, y)) {
      unionExtentFromNumber(xExtent, x);
      unionExtentFromNumber(yExtent, y);
    }
  }
  return [xExtent, yExtent];
}
function getBoundingDiff(points1, points22) {
  var _a2 = xyExtentFromPoints(points1), xExtent1 = _a2[0], yExtent1 = _a2[1];
  var _b2 = xyExtentFromPoints(points22), xExtent2 = _b2[0], yExtent2 = _b2[1];
  return Math.max(Math.abs(xExtent1[0] - xExtent2[0]), Math.abs(yExtent1[0] - yExtent2[0]), Math.abs(xExtent1[1] - xExtent2[1]), Math.abs(yExtent1[1] - yExtent2[1]));
}
function getSmooth(smooth) {
  return isNumber(smooth) ? smooth : smooth ? 0.5 : 0;
}
function getStackedOnPoints(coordSys, data, dataCoordInfo) {
  if (dataCoordInfo.valueDim == null) {
    return [];
  }
  var len2 = data.count();
  var points4 = createFloat32Array(len2 * 2);
  for (var idx = 0; idx < len2; idx++) {
    var pt = getStackedOnPoint(dataCoordInfo, coordSys, data, idx);
    points4[idx * 2] = pt[0];
    points4[idx * 2 + 1] = pt[1];
  }
  return points4;
}
function turnPointsIntoStep(points4, basePoints, coordSys, stepTurnAt, connectNulls) {
  var baseAxis = coordSys.getBaseAxis();
  var baseIndex = baseAxis.dim === "x" || baseAxis.dim === "radius" ? 0 : 1;
  var stepPoints = [];
  var i2 = 0;
  var stepPt = [];
  var pt = [];
  var nextPt = [];
  var filteredPoints = [];
  if (connectNulls) {
    for (i2 = 0; i2 < points4.length; i2 += 2) {
      var reference = basePoints || points4;
      if (!isPointIllegal(reference[i2], reference[i2 + 1])) {
        filteredPoints.push(points4[i2], points4[i2 + 1]);
      }
    }
    points4 = filteredPoints;
  }
  for (i2 = 0; i2 < points4.length - 2; i2 += 2) {
    nextPt[0] = points4[i2 + 2];
    nextPt[1] = points4[i2 + 3];
    pt[0] = points4[i2];
    pt[1] = points4[i2 + 1];
    stepPoints.push(pt[0], pt[1]);
    switch (stepTurnAt) {
      case "end":
        stepPt[baseIndex] = nextPt[baseIndex];
        stepPt[1 - baseIndex] = pt[1 - baseIndex];
        stepPoints.push(stepPt[0], stepPt[1]);
        break;
      case "middle":
        var middle = (pt[baseIndex] + nextPt[baseIndex]) / 2;
        var stepPt2 = [];
        stepPt[baseIndex] = stepPt2[baseIndex] = middle;
        stepPt[1 - baseIndex] = pt[1 - baseIndex];
        stepPt2[1 - baseIndex] = nextPt[1 - baseIndex];
        stepPoints.push(stepPt[0], stepPt[1]);
        stepPoints.push(stepPt2[0], stepPt2[1]);
        break;
      default:
        stepPt[baseIndex] = pt[baseIndex];
        stepPt[1 - baseIndex] = nextPt[1 - baseIndex];
        stepPoints.push(stepPt[0], stepPt[1]);
    }
  }
  stepPoints.push(points4[i2++], points4[i2++]);
  return stepPoints;
}
function clipColorStops(colorStops, maxSize) {
  var newColorStops = [];
  var len2 = colorStops.length;
  var prevOutOfRangeColorStop;
  var prevInRangeColorStop;
  function lerpStop(stop0, stop1, clippedCoord) {
    var coord0 = stop0.coord;
    var p = (clippedCoord - coord0) / (stop1.coord - coord0);
    var color3 = lerp2(p, [stop0.color, stop1.color]);
    return {
      coord: clippedCoord,
      color: color3
    };
  }
  for (var i2 = 0; i2 < len2; i2++) {
    var stop_1 = colorStops[i2];
    var coord = stop_1.coord;
    if (coord < 0) {
      prevOutOfRangeColorStop = stop_1;
    } else if (coord > maxSize) {
      if (prevInRangeColorStop) {
        newColorStops.push(lerpStop(prevInRangeColorStop, stop_1, maxSize));
      } else if (prevOutOfRangeColorStop) {
        newColorStops.push(lerpStop(prevOutOfRangeColorStop, stop_1, 0), lerpStop(prevOutOfRangeColorStop, stop_1, maxSize));
      }
      break;
    } else {
      if (prevOutOfRangeColorStop) {
        newColorStops.push(lerpStop(prevOutOfRangeColorStop, stop_1, 0));
        prevOutOfRangeColorStop = null;
      }
      newColorStops.push(stop_1);
      prevInRangeColorStop = stop_1;
    }
  }
  return newColorStops;
}
function getVisualGradient(data, coordSys, api) {
  var visualMetaList = data.getVisual("visualMeta");
  if (!visualMetaList || !visualMetaList.length || !data.count()) {
    return;
  }
  if (coordSys.type !== "cartesian2d") {
    if (true) {
      console.warn("Visual map on line style is only supported on cartesian2d.");
    }
    return;
  }
  var coordDim;
  var visualMeta;
  for (var i2 = visualMetaList.length - 1; i2 >= 0; i2--) {
    var dimInfo = data.getDimensionInfo(visualMetaList[i2].dimension);
    coordDim = dimInfo && dimInfo.coordDim;
    if (coordDim === "x" || coordDim === "y") {
      visualMeta = visualMetaList[i2];
      break;
    }
  }
  if (!visualMeta) {
    if (true) {
      console.warn("Visual map on line style only support x or y dimension.");
    }
    return;
  }
  var axis = coordSys.getAxis(coordDim);
  var colorStops = map2(visualMeta.stops, function(stop2) {
    return {
      coord: axis.toGlobalCoord(axis.dataToCoord(stop2.value)),
      color: stop2.color
    };
  });
  var stopLen = colorStops.length;
  var outerColors = visualMeta.outerColors.slice();
  if (stopLen && colorStops[0].coord > colorStops[stopLen - 1].coord) {
    colorStops.reverse();
    outerColors.reverse();
  }
  var colorStopsInRange = clipColorStops(colorStops, coordDim === "x" ? api.getWidth() : api.getHeight());
  var inRangeStopLen = colorStopsInRange.length;
  if (!inRangeStopLen && stopLen) {
    return colorStops[0].coord < 0 ? outerColors[1] ? outerColors[1] : colorStops[stopLen - 1].color : outerColors[0] ? outerColors[0] : colorStops[0].color;
  }
  var tinyExtent = 10;
  var minCoord = colorStopsInRange[0].coord - tinyExtent;
  var maxCoord = colorStopsInRange[inRangeStopLen - 1].coord + tinyExtent;
  var coordSpan = maxCoord - minCoord;
  if (coordSpan < 1e-3) {
    return "transparent";
  }
  each(colorStopsInRange, function(stop2) {
    stop2.offset = (stop2.coord - minCoord) / coordSpan;
  });
  colorStopsInRange.push({
    // NOTE: inRangeStopLen may still be 0 if stoplen is zero.
    offset: inRangeStopLen ? colorStopsInRange[inRangeStopLen - 1].offset : 0.5,
    color: outerColors[1] || "transparent"
  });
  colorStopsInRange.unshift({
    offset: inRangeStopLen ? colorStopsInRange[0].offset : 0.5,
    color: outerColors[0] || "transparent"
  });
  var gradient = new LinearGradient_default(0, 0, 0, 0, colorStopsInRange, true);
  gradient[coordDim] = minCoord;
  gradient[coordDim + "2"] = maxCoord;
  return gradient;
}
function getIsIgnoreFunc(seriesModel, data, coordSys) {
  var showAllSymbol = seriesModel.get("showAllSymbol");
  var isAuto = showAllSymbol === "auto";
  if (showAllSymbol && !isAuto) {
    return;
  }
  var categoryAxis2 = coordSys.getAxesByScale("ordinal")[0];
  if (!categoryAxis2) {
    return;
  }
  if (isAuto && canShowAllSymbolForCategory(categoryAxis2, data)) {
    return;
  }
  var categoryDataDim = data.mapDimension(categoryAxis2.dim);
  var labelMap = {};
  each(categoryAxis2.getViewLabels(), function(labelItem) {
    if (!labelItem.tick.offInterval) {
      labelMap[getTickValueOutermost(categoryAxis2.scale, labelItem.tick)] = 1;
    }
  });
  return function(dataIndex) {
    return !labelMap.hasOwnProperty(data.get(categoryDataDim, dataIndex));
  };
}
function canShowAllSymbolForCategory(categoryAxis2, data) {
  var axisExtent = categoryAxis2.getExtent();
  var availSize = Math.abs(axisExtent[1] - axisExtent[0]) / categoryAxis2.scale.count();
  isNaN(availSize) && (availSize = 0);
  var dataLen = data.count();
  var step = Math.max(1, Math.round(dataLen / 5));
  for (var dataIndex = 0; dataIndex < dataLen; dataIndex += step) {
    if (Symbol_default.getSymbolSize(
      data,
      dataIndex
      // Only for cartesian, where `isHorizontal` exists.
    )[categoryAxis2.isHorizontal() ? 1 : 0] * 1.5 > availSize) {
      return false;
    }
  }
  return true;
}
function getLastIndexNotNull(points4) {
  var len2 = points4.length / 2;
  for (; len2 > 0; len2--) {
    if (!isPointIllegal(points4[len2 * 2 - 2], points4[len2 * 2 - 1])) {
      break;
    }
  }
  return len2 - 1;
}
function getPointAtIndex(points4, idx) {
  return [points4[idx * 2], points4[idx * 2 + 1]];
}
function getIndexRange(points4, xOrY, dim) {
  var len2 = points4.length / 2;
  var dimIdx = dim === "x" ? 0 : 1;
  var a;
  var b;
  var prevIndex = 0;
  var nextIndex = -1;
  for (var i2 = 0; i2 < len2; i2++) {
    b = points4[i2 * 2 + dimIdx];
    if (isPointIllegal(b, points4[i2 * 2 + 1 - dimIdx])) {
      continue;
    }
    if (i2 === 0) {
      a = b;
      continue;
    }
    if (a <= xOrY && b >= xOrY || a >= xOrY && b <= xOrY) {
      nextIndex = i2;
      break;
    }
    prevIndex = i2;
    a = b;
  }
  return {
    range: [prevIndex, nextIndex],
    t: (xOrY - a) / (b - a)
  };
}
function anyStateShowEndLabel(seriesModel) {
  if (seriesModel.get(["endLabel", "show"])) {
    return true;
  }
  for (var i2 = 0; i2 < SPECIAL_STATES.length; i2++) {
    if (seriesModel.get([SPECIAL_STATES[i2], "endLabel", "show"])) {
      return true;
    }
  }
  return false;
}
function createLineClipPath(lineView, coordSys, hasAnimation, seriesModel) {
  if (isCoordinateSystemType(coordSys, "cartesian2d")) {
    var endLabelModel_1 = seriesModel.getModel("endLabel");
    var valueAnimation_1 = endLabelModel_1.get("valueAnimation");
    var data_1 = seriesModel.getData();
    var labelAnimationRecord_1 = {
      lastFrameIndex: 0
    };
    var during = anyStateShowEndLabel(seriesModel) ? function(percent, clipRect) {
      lineView._endLabelOnDuring(percent, clipRect, data_1, labelAnimationRecord_1, valueAnimation_1, endLabelModel_1, coordSys);
    } : null;
    var isHorizontal = coordSys.getBaseAxis().isHorizontal();
    var clipPath = createGridClipPath(coordSys, hasAnimation, seriesModel, function() {
      var endLabel = lineView._endLabel;
      if (endLabel && hasAnimation) {
        if (labelAnimationRecord_1.originalX != null) {
          endLabel.attr({
            x: labelAnimationRecord_1.originalX,
            y: labelAnimationRecord_1.originalY
          });
        }
      }
    }, during);
    if (!seriesModel.get("clip", true)) {
      var rectShape = clipPath.shape;
      var expandSize = Math.max(rectShape.width, rectShape.height);
      if (isHorizontal) {
        rectShape.y -= expandSize;
        rectShape.height += expandSize * 2;
      } else {
        rectShape.x -= expandSize;
        rectShape.width += expandSize * 2;
      }
    }
    if (during) {
      during(1, clipPath);
    }
    return clipPath;
  } else {
    if (true) {
      if (seriesModel.get(["endLabel", "show"])) {
        console.warn("endLabel is not supported for lines in polar systems.");
      }
    }
    return createPolarClipPath(coordSys, hasAnimation, seriesModel);
  }
}
function getEndLabelStateSpecified(endLabelModel, coordSys) {
  var baseAxis = coordSys.getBaseAxis();
  var isHorizontal = baseAxis.isHorizontal();
  var isBaseInversed = baseAxis.inverse;
  var align = isHorizontal ? isBaseInversed ? "right" : "left" : "center";
  var verticalAlign = isHorizontal ? "middle" : isBaseInversed ? "top" : "bottom";
  return {
    normal: {
      align: endLabelModel.get("align") || align,
      verticalAlign: endLabelModel.get("verticalAlign") || verticalAlign
    }
  };
}
var LineView = (
  /** @class */
  (function(_super) {
    __extends(LineView2, _super);
    function LineView2() {
      return _super !== null && _super.apply(this, arguments) || this;
    }
    LineView2.prototype.init = function() {
      var lineGroup = new Group_default();
      var symbolDraw = new SymbolDraw_default();
      this.group.add(symbolDraw.group);
      this._symbolDraw = symbolDraw;
      this._lineGroup = lineGroup;
      this._changePolyState = bind(this._changePolyState, this);
    };
    LineView2.prototype.render = function(seriesModel, ecModel, api) {
      var coordSys = seriesModel.coordinateSystem;
      var group2 = this.group;
      var data = seriesModel.getData();
      var lineStyleModel = seriesModel.getModel("lineStyle");
      var areaStyleModel = seriesModel.getModel("areaStyle");
      var points4 = data.getLayout("points") || [];
      var isCoordSysPolar = coordSys.type === "polar";
      var prevCoordSys = this._coordSys;
      var symbolDraw = this._symbolDraw;
      var polyline = this._polyline;
      var polygon = this._polygon;
      var lineGroup = this._lineGroup;
      var hasAnimation = !ecModel.ssr && seriesModel.get("animation");
      var isAreaChart = !areaStyleModel.isEmpty();
      var valueOrigin = areaStyleModel.get("origin");
      var dataCoordInfo = prepareDataCoordInfo(coordSys, data, valueOrigin);
      var stackedOnPoints = isAreaChart && getStackedOnPoints(coordSys, data, dataCoordInfo);
      var showSymbol = seriesModel.get("showSymbol");
      var connectNulls = seriesModel.get("connectNulls");
      var isIgnoreFunc = showSymbol && !isCoordSysPolar && getIsIgnoreFunc(seriesModel, data, coordSys);
      var oldData = this._data;
      oldData && oldData.eachItemGraphicEl(function(el, idx) {
        if (el.__temp) {
          group2.remove(el);
          oldData.setItemGraphicEl(idx, null);
        }
      });
      if (!showSymbol) {
        symbolDraw.remove();
      }
      group2.add(lineGroup);
      var step = !isCoordSysPolar ? seriesModel.get("step") : false;
      var clipShapeForSymbol;
      if (coordSys && coordSys.getArea && seriesModel.get("clip", true)) {
        clipShapeForSymbol = coordSys.getArea();
        if (clipShapeForSymbol.width != null) {
          clipShapeForSymbol.x -= 0.1;
          clipShapeForSymbol.y -= 0.1;
          clipShapeForSymbol.width += 0.2;
          clipShapeForSymbol.height += 0.2;
        } else if (clipShapeForSymbol.r0) {
          clipShapeForSymbol.r0 -= 0.5;
          clipShapeForSymbol.r += 0.5;
        }
      }
      this._clipShapeForSymbol = clipShapeForSymbol;
      var visualColor = getVisualGradient(data, coordSys, api) || data.getVisual("style")[data.getVisual("drawType")];
      if (!(polyline && prevCoordSys.type === coordSys.type && step === this._step)) {
        showSymbol && symbolDraw.updateData(data, {
          isIgnore: isIgnoreFunc,
          clipShape: clipShapeForSymbol,
          disableAnimation: true,
          getSymbolPoint: function(idx) {
            return [points4[idx * 2], points4[idx * 2 + 1]];
          }
        });
        hasAnimation && this._initSymbolLabelAnimation(data, coordSys, clipShapeForSymbol);
        if (step) {
          if (stackedOnPoints) {
            stackedOnPoints = turnPointsIntoStep(stackedOnPoints, points4, coordSys, step, connectNulls);
          }
          points4 = turnPointsIntoStep(points4, null, coordSys, step, connectNulls);
        }
        polyline = this._newPolyline(points4);
        if (isAreaChart) {
          polygon = this._newPolygon(points4, stackedOnPoints);
        } else if (polygon) {
          lineGroup.remove(polygon);
          polygon = this._polygon = null;
        }
        if (!isCoordSysPolar) {
          this._initOrUpdateEndLabel(seriesModel, coordSys, convertToColorString(visualColor));
        }
        lineGroup.setClipPath(createLineClipPath(this, coordSys, true, seriesModel));
      } else {
        if (isAreaChart && !polygon) {
          polygon = this._newPolygon(points4, stackedOnPoints);
        } else if (polygon && !isAreaChart) {
          lineGroup.remove(polygon);
          polygon = this._polygon = null;
        }
        if (!isCoordSysPolar) {
          this._initOrUpdateEndLabel(seriesModel, coordSys, convertToColorString(visualColor));
        }
        var oldClipPath = lineGroup.getClipPath();
        if (oldClipPath) {
          var newClipPath = createLineClipPath(this, coordSys, false, seriesModel);
          initProps(oldClipPath, {
            shape: newClipPath.shape
          }, seriesModel);
        } else {
          lineGroup.setClipPath(createLineClipPath(this, coordSys, true, seriesModel));
        }
        showSymbol && symbolDraw.updateData(data, {
          isIgnore: isIgnoreFunc,
          clipShape: clipShapeForSymbol,
          disableAnimation: true,
          getSymbolPoint: function(idx) {
            return [points4[idx * 2], points4[idx * 2 + 1]];
          }
        });
        if (!isPointsSame(this._stackedOnPoints, stackedOnPoints) || !isPointsSame(this._points, points4)) {
          if (hasAnimation) {
            this._doUpdateAnimation(data, stackedOnPoints, coordSys, api, step, valueOrigin, connectNulls);
          } else {
            if (step) {
              if (stackedOnPoints) {
                stackedOnPoints = turnPointsIntoStep(stackedOnPoints, points4, coordSys, step, connectNulls);
              }
              points4 = turnPointsIntoStep(points4, null, coordSys, step, connectNulls);
            }
            polyline.setShape({
              points: points4
            });
            polygon && polygon.setShape({
              points: points4,
              stackedOnPoints
            });
          }
        }
      }
      var emphasisModel = seriesModel.getModel("emphasis");
      var focus = emphasisModel.get("focus");
      var blurScope = emphasisModel.get("blurScope");
      var emphasisDisabled = emphasisModel.get("disabled");
      polyline.useStyle(defaults(
        // Use color in lineStyle first
        lineStyleModel.getLineStyle(),
        {
          fill: "none",
          stroke: visualColor,
          lineJoin: "bevel"
        }
      ));
      setStatesStylesFromModel(polyline, seriesModel, "lineStyle");
      if (polyline.style.lineWidth > 0 && seriesModel.get(["emphasis", "lineStyle", "width"]) === "bolder") {
        var emphasisLineStyle = polyline.getState("emphasis").style;
        emphasisLineStyle.lineWidth = +polyline.style.lineWidth + 1;
      }
      getECData(polyline).seriesIndex = seriesModel.seriesIndex;
      toggleHoverEmphasis(polyline, focus, blurScope, emphasisDisabled);
      var smooth = getSmooth(seriesModel.get("smooth"));
      var smoothMonotone = seriesModel.get("smoothMonotone");
      polyline.setShape({
        smooth,
        smoothMonotone,
        connectNulls
      });
      if (polygon) {
        var stackedOnSeries = data.getCalculationInfo("stackedOnSeries");
        var stackedOnSmooth = 0;
        polygon.useStyle(defaults(areaStyleModel.getAreaStyle(), {
          fill: visualColor,
          opacity: 0.7,
          lineJoin: "bevel",
          decal: data.getVisual("style").decal
        }));
        if (stackedOnSeries) {
          stackedOnSmooth = getSmooth(stackedOnSeries.get("smooth"));
        }
        polygon.setShape({
          smooth,
          stackedOnSmooth,
          smoothMonotone,
          connectNulls
        });
        setStatesStylesFromModel(polygon, seriesModel, "areaStyle");
        getECData(polygon).seriesIndex = seriesModel.seriesIndex;
        toggleHoverEmphasis(polygon, focus, blurScope, emphasisDisabled);
      }
      var changePolyState = this._changePolyState;
      data.eachItemGraphicEl(function(el) {
        el && (el.onHoverStateChange = changePolyState);
      });
      this._polyline.onHoverStateChange = changePolyState;
      this._data = data;
      this._coordSys = coordSys;
      this._stackedOnPoints = stackedOnPoints;
      this._points = points4;
      this._step = step;
      this._valueOrigin = valueOrigin;
      var triggerEvent = seriesModel.get("triggerEvent");
      var triggerLineEvent = seriesModel.get("triggerLineEvent");
      if (true) {
        triggerLineEvent && warnDeprecated("triggerLineEvent", "Use the `triggerEvent` option instead.");
      }
      var shouldTriggerLineEvent = triggerLineEvent === true || triggerEvent === true || triggerEvent === "line";
      var shouldTriggerAreaEvent = triggerLineEvent === true || triggerEvent === true || triggerEvent === "area";
      this.packEventData(seriesModel, polyline, shouldTriggerLineEvent);
      polygon && this.packEventData(seriesModel, polygon, shouldTriggerAreaEvent);
    };
    LineView2.prototype.packEventData = function(seriesModel, el, enable) {
      getECData(el).eventData = enable ? {
        componentType: "series",
        componentSubType: "line",
        componentIndex: seriesModel.componentIndex,
        seriesIndex: seriesModel.seriesIndex,
        seriesName: seriesModel.name,
        seriesType: "line",
        // for determining this event is triggered by area or line
        selfType: el === this._polygon ? "area" : "line"
      } : null;
    };
    LineView2.prototype.highlight = function(seriesModel, ecModel, api, payload) {
      var data = seriesModel.getData();
      var dataIndex = queryDataIndex(data, payload);
      this._changePolyState("emphasis");
      if (!(dataIndex instanceof Array) && dataIndex != null && dataIndex >= 0) {
        var points4 = data.getLayout("points");
        var symbol = data.getItemGraphicEl(dataIndex);
        if (!symbol) {
          var x = points4[dataIndex * 2];
          var y = points4[dataIndex * 2 + 1];
          if (isPointIllegal(x, y)) {
            return;
          }
          if (this._clipShapeForSymbol && !this._clipShapeForSymbol.contain(x, y)) {
            return;
          }
          var zlevel = seriesModel.get("zlevel") || 0;
          var z = seriesModel.get("z") || 0;
          symbol = new Symbol_default(data, dataIndex);
          symbol.x = x;
          symbol.y = y;
          symbol.setZ(zlevel, z);
          var symbolLabel = symbol.getSymbolPath().getTextContent();
          if (symbolLabel) {
            symbolLabel.zlevel = zlevel;
            symbolLabel.z = z;
            symbolLabel.z2 = this._polyline.z2 + 1;
          }
          symbol.__temp = true;
          data.setItemGraphicEl(dataIndex, symbol);
          symbol.stopSymbolAnimation(true);
          this.group.add(symbol);
        }
        symbol.highlight();
      } else {
        Chart_default.prototype.highlight.call(this, seriesModel, ecModel, api, payload);
      }
    };
    LineView2.prototype.downplay = function(seriesModel, ecModel, api, payload) {
      var data = seriesModel.getData();
      var dataIndex = queryDataIndex(data, payload);
      this._changePolyState("normal");
      if (dataIndex != null && dataIndex >= 0) {
        var symbol = data.getItemGraphicEl(dataIndex);
        if (symbol) {
          if (symbol.__temp) {
            data.setItemGraphicEl(dataIndex, null);
            this.group.remove(symbol);
          } else {
            symbol.downplay();
          }
        }
      } else {
        Chart_default.prototype.downplay.call(this, seriesModel, ecModel, api, payload);
      }
    };
    LineView2.prototype._changePolyState = function(toState) {
      var polygon = this._polygon;
      setStatesFlag(this._polyline, toState);
      polygon && setStatesFlag(polygon, toState);
    };
    LineView2.prototype._newPolyline = function(points4) {
      var polyline = this._polyline;
      if (polyline) {
        this._lineGroup.remove(polyline);
      }
      polyline = new ECPolyline({
        shape: {
          points: points4
        },
        segmentIgnoreThreshold: 2,
        z2: 10
      });
      this._lineGroup.add(polyline);
      this._polyline = polyline;
      return polyline;
    };
    LineView2.prototype._newPolygon = function(points4, stackedOnPoints) {
      var polygon = this._polygon;
      if (polygon) {
        this._lineGroup.remove(polygon);
      }
      polygon = new ECPolygon({
        shape: {
          points: points4,
          stackedOnPoints
        },
        segmentIgnoreThreshold: 2
      });
      this._lineGroup.add(polygon);
      this._polygon = polygon;
      return polygon;
    };
    LineView2.prototype._initSymbolLabelAnimation = function(data, coordSys, clipShape) {
      var isHorizontalOrRadial;
      var isCoordSysPolar;
      var baseAxis = coordSys.getBaseAxis();
      var isAxisInverse = baseAxis.inverse;
      if (coordSys.type === "cartesian2d") {
        isHorizontalOrRadial = baseAxis.isHorizontal();
        isCoordSysPolar = false;
      } else if (coordSys.type === "polar") {
        isHorizontalOrRadial = baseAxis.dim === "angle";
        isCoordSysPolar = true;
      }
      var seriesModel = data.hostModel;
      var seriesDuration = seriesModel.get("animationDuration");
      if (isFunction(seriesDuration)) {
        seriesDuration = seriesDuration(null);
      }
      var seriesDelay = seriesModel.get("animationDelay") || 0;
      var seriesDelayValue = isFunction(seriesDelay) ? seriesDelay(null) : seriesDelay;
      data.eachItemGraphicEl(function(symbol, idx) {
        var el = symbol;
        if (el) {
          var point = [symbol.x, symbol.y];
          var start2 = void 0;
          var end2 = void 0;
          var current = void 0;
          if (clipShape) {
            if (isCoordSysPolar) {
              var polarClip = clipShape;
              var coord = coordSys.pointToCoord(point);
              if (isHorizontalOrRadial) {
                start2 = polarClip.startAngle;
                end2 = polarClip.endAngle;
                current = -coord[1] / 180 * Math.PI;
              } else {
                start2 = polarClip.r0;
                end2 = polarClip.r;
                current = coord[0];
              }
            } else {
              var gridClip = clipShape;
              if (isHorizontalOrRadial) {
                start2 = gridClip.x;
                end2 = gridClip.x + gridClip.width;
                current = symbol.x;
              } else {
                start2 = gridClip.y + gridClip.height;
                end2 = gridClip.y;
                current = symbol.y;
              }
            }
          }
          var ratio = end2 === start2 ? 0 : (current - start2) / (end2 - start2);
          if (isAxisInverse) {
            ratio = 1 - ratio;
          }
          var delay2 = isFunction(seriesDelay) ? seriesDelay(idx) : seriesDuration * ratio + seriesDelayValue;
          var symbolPath = el.getSymbolPath();
          var text = symbolPath.getTextContent();
          el.attr({
            scaleX: 0,
            scaleY: 0
          });
          el.animateTo({
            scaleX: 1,
            scaleY: 1
          }, {
            duration: 200,
            setToFinal: true,
            delay: delay2
          });
          if (text) {
            text.animateFrom({
              style: {
                opacity: 0
              }
            }, {
              duration: 300,
              delay: delay2
            });
          }
          symbolPath.disableLabelAnimation = true;
        }
      });
    };
    LineView2.prototype._initOrUpdateEndLabel = function(seriesModel, coordSys, inheritColor) {
      var endLabelModel = seriesModel.getModel("endLabel");
      if (anyStateShowEndLabel(seriesModel)) {
        var data_2 = seriesModel.getData();
        var polyline = this._polyline;
        var points4 = data_2.getLayout("points");
        if (!points4) {
          polyline.removeTextContent();
          this._endLabel = null;
          return;
        }
        var endLabel = this._endLabel;
        if (!endLabel) {
          endLabel = this._endLabel = new Text_default({
            z2: 200
            // should be higher than item symbol
          });
          endLabel.ignoreClip = true;
          polyline.setTextContent(this._endLabel);
          polyline.disableLabelAnimation = true;
        }
        var dataIndex = getLastIndexNotNull(points4);
        if (dataIndex >= 0) {
          setLabelStyle(polyline, getLabelStatesModels(seriesModel, "endLabel"), {
            inheritColor,
            labelFetcher: seriesModel,
            labelDataIndex: dataIndex,
            defaultText: function(dataIndex2, opt, interpolatedValue) {
              return interpolatedValue != null ? getDefaultInterpolatedLabel(data_2, interpolatedValue) : getDefaultLabel(data_2, dataIndex2);
            },
            enableTextSetter: true
          }, getEndLabelStateSpecified(endLabelModel, coordSys));
          polyline.textConfig.position = null;
        }
      } else if (this._endLabel) {
        this._polyline.removeTextContent();
        this._endLabel = null;
      }
    };
    LineView2.prototype._endLabelOnDuring = function(percent, clipRect, data, animationRecord, valueAnimation, endLabelModel, coordSys) {
      var endLabel = this._endLabel;
      var polyline = this._polyline;
      if (endLabel) {
        if (percent < 1 && animationRecord.originalX == null) {
          animationRecord.originalX = endLabel.x;
          animationRecord.originalY = endLabel.y;
        }
        var points4 = data.getLayout("points");
        var seriesModel = data.hostModel;
        var connectNulls = seriesModel.get("connectNulls");
        var precision = endLabelModel.get("precision");
        var distance2 = endLabelModel.get("distance") || 0;
        var baseAxis = coordSys.getBaseAxis();
        var isHorizontal = baseAxis.isHorizontal();
        var isBaseInversed = baseAxis.inverse;
        var clipShape = clipRect.shape;
        var xOrY = isBaseInversed ? isHorizontal ? clipShape.x : clipShape.y + clipShape.height : isHorizontal ? clipShape.x + clipShape.width : clipShape.y;
        var distanceX = (isHorizontal ? distance2 : 0) * (isBaseInversed ? -1 : 1);
        var distanceY = (isHorizontal ? 0 : -distance2) * (isBaseInversed ? -1 : 1);
        var dim = isHorizontal ? "x" : "y";
        var dataIndexRange = getIndexRange(points4, xOrY, dim);
        var indices = dataIndexRange.range;
        var diff = indices[1] - indices[0];
        var value = void 0;
        if (diff >= 1) {
          if (diff > 1 && !connectNulls) {
            var pt = getPointAtIndex(points4, indices[0]);
            endLabel.attr({
              x: pt[0] + distanceX,
              y: pt[1] + distanceY
            });
            valueAnimation && (value = seriesModel.getRawValue(indices[0]));
          } else {
            var pt = polyline.getPointOn(xOrY, dim);
            pt && endLabel.attr({
              x: pt[0] + distanceX,
              y: pt[1] + distanceY
            });
            var startValue = seriesModel.getRawValue(indices[0]);
            var endValue = seriesModel.getRawValue(indices[1]);
            valueAnimation && (value = interpolateRawValues(data, precision, startValue, endValue, dataIndexRange.t));
          }
          animationRecord.lastFrameIndex = indices[0];
        } else {
          var idx = percent === 1 || animationRecord.lastFrameIndex > 0 ? indices[0] : 0;
          var pt = getPointAtIndex(points4, idx);
          valueAnimation && (value = seriesModel.getRawValue(idx));
          endLabel.attr({
            x: pt[0] + distanceX,
            y: pt[1] + distanceY
          });
        }
        if (valueAnimation) {
          var inner25 = labelInner(endLabel);
          if (typeof inner25.setLabelText === "function") {
            inner25.setLabelText(value);
          }
        }
      }
    };
    LineView2.prototype._doUpdateAnimation = function(data, stackedOnPoints, coordSys, api, step, valueOrigin, connectNulls) {
      var polyline = this._polyline;
      var polygon = this._polygon;
      var seriesModel = data.hostModel;
      var diff = lineAnimationDiff(this._data, data, this._stackedOnPoints, stackedOnPoints, this._coordSys, coordSys, this._valueOrigin, valueOrigin);
      var current = diff.current;
      var stackedOnCurrent = diff.stackedOnCurrent;
      var next = diff.next;
      var stackedOnNext = diff.stackedOnNext;
      if (step) {
        stackedOnCurrent = turnPointsIntoStep(diff.stackedOnCurrent, diff.current, coordSys, step, connectNulls);
        current = turnPointsIntoStep(diff.current, null, coordSys, step, connectNulls);
        stackedOnNext = turnPointsIntoStep(diff.stackedOnNext, diff.next, coordSys, step, connectNulls);
        next = turnPointsIntoStep(diff.next, null, coordSys, step, connectNulls);
      }
      if (getBoundingDiff(current, next) > 3e3 || polygon && getBoundingDiff(stackedOnCurrent, stackedOnNext) > 3e3) {
        polyline.stopAnimation();
        polyline.setShape({
          points: next
        });
        if (polygon) {
          polygon.stopAnimation();
          polygon.setShape({
            points: next,
            stackedOnPoints: stackedOnNext
          });
        }
        return;
      }
      polyline.shape.__points = diff.current;
      polyline.shape.points = current;
      var target = {
        shape: {
          points: next
        }
      };
      if (diff.current !== current) {
        target.shape.__points = diff.next;
      }
      polyline.stopAnimation();
      updateProps(polyline, target, seriesModel);
      if (polygon) {
        polygon.setShape({
          // Reuse the points with polyline.
          points: current,
          stackedOnPoints: stackedOnCurrent
        });
        polygon.stopAnimation();
        updateProps(polygon, {
          shape: {
            stackedOnPoints: stackedOnNext
          }
        }, seriesModel);
        if (polyline.shape.points !== polygon.shape.points) {
          polygon.shape.points = polyline.shape.points;
        }
      }
      var updatedDataInfo = [];
      var diffStatus = diff.status;
      for (var i2 = 0; i2 < diffStatus.length; i2++) {
        var cmd = diffStatus[i2].cmd;
        if (cmd === "=") {
          var el = data.getItemGraphicEl(diffStatus[i2].idx1);
          if (el) {
            updatedDataInfo.push({
              el,
              ptIdx: i2
              // Index of points
            });
          }
        }
      }
      if (polyline.animators && polyline.animators.length) {
        polyline.animators[0].during(function() {
          polygon && polygon.dirtyShape();
          var points4 = polyline.shape.__points;
          for (var i3 = 0; i3 < updatedDataInfo.length; i3++) {
            var el2 = updatedDataInfo[i3].el;
            var offset = updatedDataInfo[i3].ptIdx * 2;
            el2.x = points4[offset];
            el2.y = points4[offset + 1];
            el2.markRedraw();
          }
        });
      }
    };
    LineView2.prototype.remove = function(ecModel) {
      var group2 = this.group;
      var oldData = this._data;
      this._lineGroup.removeAll();
      this._symbolDraw.remove(true);
      oldData && oldData.eachItemGraphicEl(function(el, idx) {
        if (el.__temp) {
          group2.remove(el);
          oldData.setItemGraphicEl(idx, null);
        }
      });
      this._polyline = this._polygon = this._coordSys = this._points = this._stackedOnPoints = this._endLabel = this._data = null;
    };
    LineView2.type = "line";
    return LineView2;
  })(Chart_default)
);
var LineView_default = LineView;

// node_modules/echarts/lib/layout/points.js
function pointsLayout(seriesType2, forceStoreInTypedArray) {
  return {
    seriesType: seriesType2,
    plan: createRenderPlanner(),
    reset: function(seriesModel) {
      var data = seriesModel.getData();
      var coordSys = seriesModel.coordinateSystem;
      var pipelineContext = seriesModel.pipelineContext;
      var useTypedArray = forceStoreInTypedArray || pipelineContext.large;
      if (!coordSys) {
        return;
      }
      var dims = map2(coordSys.dimensions, function(dim) {
        return data.mapDimension(dim);
      }).slice(0, 2);
      var dimLen = dims.length;
      var stackResultDim = data.getCalculationInfo("stackResultDimension");
      if (isDimensionStacked(data, dims[0])) {
        dims[0] = stackResultDim;
      }
      if (isDimensionStacked(data, dims[1])) {
        dims[1] = stackResultDim;
      }
      var store = data.getStore();
      var dimIdx0 = data.getDimensionIndex(dims[0]);
      var dimIdx1 = data.getDimensionIndex(dims[1]);
      return dimLen && {
        progress: function(params, data2) {
          var segCount = params.end - params.start;
          var points4 = useTypedArray && createFloat32Array(segCount * dimLen);
          var tmpIn = [];
          var tmpOut = [];
          for (var i2 = params.start, offset = 0; i2 < params.end; i2++) {
            var point = void 0;
            if (dimLen === 1) {
              var x = store.get(dimIdx0, i2);
              point = coordSys.dataToPoint(x, null, tmpOut);
            } else {
              tmpIn[0] = store.get(dimIdx0, i2);
              tmpIn[1] = store.get(dimIdx1, i2);
              point = coordSys.dataToPoint(tmpIn, null, tmpOut);
            }
            if (useTypedArray) {
              points4[offset++] = point[0];
              points4[offset++] = point[1];
            } else {
              data2.setItemLayout(i2, point.slice());
            }
          }
          if (useTypedArray) {
            data2.setLayout("points", points4);
            data2.setLayout("pointsRange", {
              start: params.start,
              end: params.end
            });
          }
        }
      };
    }
  };
}

// node_modules/echarts/lib/processor/dataSample.js
var samplers = {
  average: function(frame) {
    var sum2 = 0;
    var count2 = 0;
    for (var i2 = 0; i2 < frame.length; i2++) {
      if (!isNaN(frame[i2])) {
        sum2 += frame[i2];
        count2++;
      }
    }
    return count2 === 0 ? NaN : sum2 / count2;
  },
  sum: function(frame) {
    var sum2 = 0;
    for (var i2 = 0; i2 < frame.length; i2++) {
      sum2 += frame[i2] || 0;
    }
    return sum2;
  },
  max: function(frame) {
    var max3 = -Infinity;
    for (var i2 = 0; i2 < frame.length; i2++) {
      frame[i2] > max3 && (max3 = frame[i2]);
    }
    return isFinite(max3) ? max3 : NaN;
  },
  min: function(frame) {
    var min3 = Infinity;
    for (var i2 = 0; i2 < frame.length; i2++) {
      frame[i2] < min3 && (min3 = frame[i2]);
    }
    return isFinite(min3) ? min3 : NaN;
  },
  // TODO
  // Median
  nearest: function(frame) {
    return frame[0];
  }
};
var indexSampler = function(frame) {
  return Math.round(frame.length / 2);
};
function dataSample(seriesType2) {
  return {
    seriesType: seriesType2,
    // FIXME:TS never used, so comment it
    // modifyOutputEnd: true,
    reset: function(seriesModel, ecModel, api) {
      var data = seriesModel.getData();
      var sampling = seriesModel.get("sampling");
      var coordSys = seriesModel.coordinateSystem;
      var count2 = data.count();
      if (count2 > 10 && coordSys.type === "cartesian2d" && sampling) {
        var baseAxis = coordSys.getBaseAxis();
        var valueAxis2 = coordSys.getOtherAxis(baseAxis);
        var extent = baseAxis.getExtent();
        var dpr2 = api.getDevicePixelRatio();
        var size = Math.abs(extent[1] - extent[0]) * (dpr2 || 1);
        var rate = Math.round(count2 / size);
        if (isFinite(rate) && rate > 1) {
          if (sampling === "lttb") {
            seriesModel.setData(data.lttbDownSample(data.mapDimension(valueAxis2.dim), 1 / rate));
          } else if (sampling === "minmax") {
            seriesModel.setData(data.minmaxDownSample(data.mapDimension(valueAxis2.dim), 1 / rate));
          }
          var sampler = void 0;
          if (isString(sampling)) {
            sampler = samplers[sampling];
          } else if (isFunction(sampling)) {
            sampler = sampling;
          }
          if (sampler) {
            seriesModel.setData(data.downSample(data.mapDimension(valueAxis2.dim), 1 / rate, sampler, indexSampler));
          }
        }
      }
    }
  };
}

// node_modules/echarts/lib/chart/line/install.js
function install(registers) {
  registers.registerChartView(LineView_default);
  registers.registerSeriesModel(LineSeries_default);
  registers.registerLayout(pointsLayout("line", true));
  registers.registerVisual({
    seriesType: "line",
    reset: function(seriesModel) {
      var data = seriesModel.getData();
      var lineStyle = seriesModel.getModel("lineStyle").getLineStyle();
      if (lineStyle && !lineStyle.stroke) {
        lineStyle.stroke = data.getVisual("style").fill;
      }
      data.setVisual("legendLineStyle", lineStyle);
    }
  });
  registers.registerProcessor(registers.PRIORITY.PROCESSOR.STATISTIC, dataSample("line"));
}

// node_modules/echarts/lib/coord/axisTickLabelBuilder.js
var modelInner = makeInner();
var axisInner2 = makeInner();
var AxisTickLabelComputingKind = {
  estimate: 1,
  determine: 2
};
function createAxisLabelsComputingContext(kind) {
  return {
    out: {
      noPxChangeTryDetermine: []
    },
    kind
  };
}
function createAxisLabels(axis, ctx) {
  var custom = axis.getLabelModel().get("customValues");
  if (custom) {
    var scale_1 = axis.scale;
    return {
      labels: map2(parseTickLabelCustomValues(custom, scale_1), function(tick, index) {
        return {
          formattedLabel: makeLabelFormatter(axis)(tick, index),
          rawLabel: scale_1.getLabel(tick),
          tick
        };
      })
    };
  }
  return axis.type === "category" ? makeCategoryLabels(axis, ctx) : makeRealNumberLabels(axis);
}
function createAxisTicks(axis, tickModel, opt) {
  var scale3 = axis.scale;
  var custom = axis.getTickModel().get("customValues");
  if (custom) {
    return {
      ticks: parseTickLabelCustomValues(custom, scale3)
    };
  }
  return axis.type === "category" ? makeCategoryTicks(axis, tickModel) : {
    ticks: scale3.getTicks(opt)
  };
}
function parseTickLabelCustomValues(customValues, scale3) {
  var extent = scale3.getExtent();
  var tickNumbers = [];
  each(customValues, function(val) {
    val = scale3.parse(val);
    if (val >= extent[0] && val <= extent[1]) {
      tickNumbers.push(val);
    }
  });
  removeDuplicates(tickNumbers, removeDuplicatesGetKeyFromItemItself, null);
  asc(tickNumbers);
  return map2(tickNumbers, function(tickVal) {
    return {
      value: tickVal
    };
  });
}
function makeCategoryLabels(axis, ctx) {
  var labelModel = axis.getLabelModel();
  var result = makeCategoryLabelsActually(axis, labelModel, ctx);
  return !labelModel.get("show") || axis.scale.isBlank() ? {
    labels: []
  } : result;
}
function makeCategoryLabelsActually(axis, labelModel, ctx) {
  var labelsCache = ensureCategoryLabelCache(axis);
  var optionLabelInterval = getOptionCategoryInterval(labelModel);
  var isEstimate = ctx.kind === AxisTickLabelComputingKind.estimate;
  if (!isEstimate) {
    var result_1 = axisCacheGet(labelsCache, optionLabelInterval);
    if (result_1) {
      return result_1;
    }
  }
  var labels;
  var numericLabelInterval;
  if (isFunction(optionLabelInterval)) {
    labels = makeTicksLabelsByCategoryIntervalNumOrCb(axis, optionLabelInterval, false);
  } else {
    numericLabelInterval = optionLabelInterval === "auto" ? makeAutoCategoryInterval(axis, ctx) : optionLabelInterval;
    labels = makeTicksLabelsByCategoryIntervalNumOrCb(axis, numericLabelInterval, false);
  }
  var result = {
    labels,
    labelCategoryInterval: numericLabelInterval
  };
  if (!isEstimate) {
    axisCacheSet(labelsCache, optionLabelInterval, result);
  } else {
    ctx.out.noPxChangeTryDetermine.push(function() {
      axisCacheSet(labelsCache, optionLabelInterval, result);
      return true;
    });
  }
  return result;
}
function makeCategoryTicks(axis, tickModel) {
  var ticksCache = ensureCategoryTickCache(axis);
  var optionTickInterval = getOptionCategoryInterval(tickModel);
  var result = axisCacheGet(ticksCache, optionTickInterval);
  if (result) {
    return result;
  }
  var ticks;
  var tickCategoryInterval;
  if (!tickModel.get("show") || axis.scale.isBlank()) {
    ticks = [];
  }
  if (isFunction(optionTickInterval)) {
    ticks = makeTicksLabelsByCategoryIntervalNumOrCb(axis, optionTickInterval, true);
  } else if (optionTickInterval === "auto") {
    var labelsResult = makeCategoryLabelsActually(axis, axis.getLabelModel(), createAxisLabelsComputingContext(AxisTickLabelComputingKind.determine));
    tickCategoryInterval = labelsResult.labelCategoryInterval;
    ticks = map2(labelsResult.labels, function(labelItem) {
      return labelItem.tick;
    });
  } else {
    tickCategoryInterval = optionTickInterval;
    ticks = makeTicksLabelsByCategoryIntervalNumOrCb(axis, tickCategoryInterval, true);
  }
  return axisCacheSet(ticksCache, optionTickInterval, {
    ticks,
    tickCategoryInterval
  });
}
function makeRealNumberLabels(axis) {
  var ticks = axis.scale.getTicks();
  var labelFormatter = makeLabelFormatter(axis);
  return {
    labels: map2(ticks, function(tick, idx) {
      return {
        formattedLabel: labelFormatter(tick, idx),
        rawLabel: axis.scale.getLabel(tick),
        tick
      };
    })
  };
}
var ensureCategoryTickCache = initAxisCacheMethod("axisTick");
var ensureCategoryLabelCache = initAxisCacheMethod("axisLabel");
function initAxisCacheMethod(prop) {
  return function ensureCache(axis) {
    return axisInner2(axis)[prop] || (axisInner2(axis)[prop] = {
      list: []
    });
  };
}
function axisCacheGet(cache, key2) {
  for (var i2 = 0; i2 < cache.list.length; i2++) {
    if (cache.list[i2].key === key2) {
      return cache.list[i2].value;
    }
  }
}
function axisCacheSet(cache, key2, value) {
  cache.list.push({
    key: key2,
    value
  });
  return value;
}
function makeAutoCategoryInterval(axis, ctx) {
  if (ctx.kind === AxisTickLabelComputingKind.estimate) {
    var result_2 = axis.calculateCategoryInterval(ctx);
    ctx.out.noPxChangeTryDetermine.push(function() {
      axisInner2(axis).autoInterval = result_2;
      return true;
    });
    return result_2;
  }
  var result = axisInner2(axis).autoInterval;
  return result != null ? result : axisInner2(axis).autoInterval = axis.calculateCategoryInterval(ctx);
}
function calculateCategoryInterval(axis, ctx) {
  var kind = ctx.kind;
  var params = fetchAutoCategoryIntervalCalculationParams(axis);
  var labelFormatter = makeLabelFormatter(axis);
  var rotation = (params.axisRotate - params.labelRotate) / 180 * Math.PI;
  var ordinalScale = axis.scale;
  var ordinalExtent = ordinalScale.getExtent();
  var tickCount = ordinalScale.count();
  if (ordinalExtent[1] - ordinalExtent[0] < 1) {
    return 0;
  }
  var step = 1;
  var maxCount = 40;
  if (tickCount > maxCount) {
    step = Math.max(1, Math.floor(tickCount / maxCount));
  }
  var tickValue = ordinalExtent[0];
  var unitSpan = axis.dataToCoord(tickValue + 1) - axis.dataToCoord(tickValue);
  var unitW = Math.abs(unitSpan * Math.cos(rotation));
  var unitH = Math.abs(unitSpan * Math.sin(rotation));
  var maxW = 0;
  var maxH = 0;
  for (; tickValue <= ordinalExtent[1]; tickValue += step) {
    var width = 0;
    var height = 0;
    var rect = getBoundingRect(labelFormatter({
      value: tickValue
    }), params.font, "center", "top");
    width = rect.width * 1.3;
    height = rect.height * 1.3;
    maxW = Math.max(maxW, width, 7);
    maxH = Math.max(maxH, height, 7);
  }
  var dw = maxW / unitW;
  var dh = maxH / unitH;
  isNaN(dw) && (dw = Infinity);
  isNaN(dh) && (dh = Infinity);
  var interval2 = Math.max(0, Math.floor(Math.min(dw, dh)));
  if (kind === AxisTickLabelComputingKind.estimate) {
    ctx.out.noPxChangeTryDetermine.push(bind(calculateCategoryIntervalTryDetermine, null, axis, interval2, tickCount));
    return interval2;
  }
  var lastInterval = calculateCategoryIntervalDealCache(axis, interval2, tickCount);
  return lastInterval != null ? lastInterval : interval2;
}
function calculateCategoryIntervalTryDetermine(axis, interval2, tickCount) {
  return calculateCategoryIntervalDealCache(axis, interval2, tickCount) == null;
}
function calculateCategoryIntervalDealCache(axis, interval2, tickCount) {
  var cache = modelInner(axis.model);
  var axisExtent = axis.getExtent();
  var lastAutoInterval = cache.lastAutoInterval;
  var lastTickCount = cache.lastTickCount;
  if (lastAutoInterval != null && lastTickCount != null && Math.abs(lastAutoInterval - interval2) <= 1 && Math.abs(lastTickCount - tickCount) <= 1 && lastAutoInterval > interval2 && cache.axisExtent0 === axisExtent[0] && cache.axisExtent1 === axisExtent[1]) {
    return lastAutoInterval;
  } else {
    cache.lastTickCount = tickCount;
    cache.lastAutoInterval = interval2;
    cache.axisExtent0 = axisExtent[0];
    cache.axisExtent1 = axisExtent[1];
  }
}
function fetchAutoCategoryIntervalCalculationParams(axis) {
  var labelModel = axis.getLabelModel();
  return {
    axisRotate: axis.getRotate ? axis.getRotate() : axis.isHorizontal && !axis.isHorizontal() ? 90 : 0,
    labelRotate: labelModel.get("rotate") || 0,
    font: labelModel.getFont()
  };
}
function makeTicksLabelsByCategoryIntervalNumOrCb(axis, categoryInterval, onlyTick) {
  var labelFormatter = makeLabelFormatter(axis);
  var ordinalScale = axis.scale;
  var result = [];
  var categoryIntervalIsCb = isFunction(categoryInterval);
  ordinalScaleCreateTicks(ordinalScale, categoryIntervalIsCb ? 0 : categoryInterval, function(tickObj, isExtentBoundary) {
    var tickLabel = ordinalScale.getLabel(tickObj);
    if (categoryIntervalIsCb) {
      var isOnInterval = !!categoryInterval(tickObj.value, tickLabel);
      tickObj.offInterval = !isOnInterval;
      if (!isOnInterval && !isExtentBoundary) {
        return;
      }
    }
    result.push(onlyTick ? tickObj : {
      formattedLabel: labelFormatter(tickObj),
      rawLabel: tickLabel,
      tick: tickObj
    });
  });
  return result;
}

// node_modules/echarts/lib/util/cycleCache.js
var ecModelCacheInner = makeInner();
function resetCachePerECPrepare(ecModel) {
  ecModelCacheInner(ecModel).prepare = {};
}
function resetCachePerECFullUpdate(ecModel) {
  ecModelCacheInner(ecModel).fullUpdate = {};
}
function getCachePerECPrepare(ecModel) {
  return ecModelCacheInner(ecModel).prepare;
}
function getCachePerECFullUpdate(ecModel) {
  return ecModelCacheInner(ecModel).fullUpdate;
}

// node_modules/echarts/lib/coord/axisStatistics.js
var callOnlyOnce = makeCallOnlyOnce();
var AXIS_STAT_KEY_DELIMITER = "|&";
var ecModelCacheFullUpdateInner = makeInner();
var LINEAR_POSITIVE_MIN_GAP_SINGLE_VALID_VALUE = -2;
var LINEAR_POSITIVE_MIN_GAP_NO_VALID_VALUE = -1;
var ecModelCachePrepareInner = makeInner();
var validateInputAxis;
if (true) {
  validateInputAxis = function(axis) {
    assert(axis && axis.model && axis.model.uid && axis.model.ecModel);
  };
}
function getAxisStatPerKeyPerAxis(axis, axisStatKey) {
  var axisModel = axis.model;
  var keyed = ecModelCacheFullUpdateInner(getCachePerECFullUpdate(axisModel.ecModel)).keyed;
  var perKey = keyed && keyed.get(axisStatKey);
  return perKey && perKey.get(axisModel.uid);
}
function getAxisStat(axis, axisStatKey) {
  if (true) {
    assert(axisStatKey != null);
    validateInputAxis(axis);
  }
  return wrapStatResult(getAxisStatPerKeyPerAxis(axis, axisStatKey));
}
function getAxisStatBySeries(axis, seriesList) {
  if (true) {
    validateInputAxis(axis);
  }
  var result = [];
  eachKeyEachAxis(axis.model.ecModel, function(perKeyPerAxis) {
    for (var idx = 0; idx < seriesList.length; idx++) {
      if (seriesList[idx] && perKeyPerAxis.serByIdx[seriesList[idx].seriesIndex]) {
        result.push(wrapStatResult(perKeyPerAxis));
      }
    }
  });
  return result;
}
function eachKeyEachAxis(ecModel, cb) {
  var keyed = ecModelCacheFullUpdateInner(getCachePerECFullUpdate(ecModel)).keyed;
  keyed && keyed.each(function(perKey, axisStatKey) {
    perKey.each(function(perKeyPerAxis, axisModelUid) {
      cb(perKeyPerAxis, axisStatKey, axisModelUid);
    });
  });
}
function wrapStatResult(record) {
  return {
    liPosMinGap: record ? record.liPosMinGap : void 0
  };
}
function eachSeriesOnAxis(axis, cb) {
  if (true) {
    validateInputAxis(axis);
  }
  var ecModel = axis.model.ecModel;
  var seriesOnAxisMap = ecModelCacheFullUpdateInner(getCachePerECFullUpdate(ecModel)).axSer;
  seriesOnAxisMap && eachSeriesDealForAxisStat(ecModel, seriesOnAxisMap.get(axis.model.uid), cb);
}
function eachSeriesOnAxisOnKey(axis, axisStatKey, cb) {
  if (true) {
    assert(axisStatKey != null);
    validateInputAxis(axis);
  }
  var perKeyPerAxis = getAxisStatPerKeyPerAxis(axis, axisStatKey);
  perKeyPerAxis && eachSeriesDealForAxisStat(axis.model.ecModel, perKeyPerAxis.sers, cb);
}
function eachSeriesDealForAxisStat(ecModel, seriesList, cb) {
  if (!seriesList) {
    return;
  }
  for (var i2 = 0; i2 < seriesList.length; i2++) {
    var seriesModel = seriesList[i2];
    if (!ecModel.isSeriesFiltered(seriesModel)) {
      cb(seriesModel);
    }
  }
}
function countSeriesOnAxisOnKey(axis, axisStatKey) {
  if (true) {
    assert(axisStatKey != null);
    validateInputAxis(axis);
  }
  var perKeyPerAxis = getAxisStatPerKeyPerAxis(axis, axisStatKey);
  if (!perKeyPerAxis || !perKeyPerAxis.sers.length) {
    return 0;
  }
  var count2 = 0;
  eachSeriesDealForAxisStat(axis.model.ecModel, perKeyPerAxis.sers, function() {
    count2++;
  });
  return count2;
}
function eachAxisOnKey(ecModel, axisStatKey, cb) {
  if (true) {
    assert(axisStatKey != null);
  }
  var keyed = ecModelCacheFullUpdateInner(getCachePerECFullUpdate(ecModel)).keyed;
  var perKey = keyed && keyed.get(axisStatKey);
  perKey && perKey.each(function(perKeyPerAxis) {
    if (true) {
      assert(perKeyPerAxis.sers.length > 0);
    }
    cb(perKeyPerAxis.axis);
  });
}
function eachKeyOnAxis(axis, cb) {
  if (true) {
    validateInputAxis(axis);
  }
  var model2 = axis.model;
  var keysByAxisModelUid = ecModelCacheFullUpdateInner(getCachePerECFullUpdate(model2.ecModel)).keys;
  keysByAxisModelUid && each(keysByAxisModelUid.get(model2.uid), function(axisStatKey) {
    if (true) {
      var stat = getAxisStatPerKeyPerAxis(axis, axisStatKey);
      assert(stat && stat.sers.length > 0);
    }
    cb(axisStatKey);
  });
}
function performAxisStatisticsOnOverallReset(ecModel) {
  var ecPrepareCache = ecModelCachePrepareInner(getCachePerECPrepare(ecModel));
  var ecPrepareCacheKeyed = ecPrepareCache.keyed || (ecPrepareCache.keyed = createHashMap());
  eachKeyEachAxis(ecModel, function(perKeyPerAxis, axisStatKey, axisModelUid) {
    var ecPrepareCachePerKey = ecPrepareCacheKeyed.get(axisStatKey) || ecPrepareCacheKeyed.set(axisStatKey, createHashMap());
    var ecPreparePerKeyPerAxis = ecPrepareCachePerKey.get(axisModelUid) || ecPrepareCachePerKey.set(axisModelUid, {});
    if (perKeyPerAxis.metrics.liPosMinGap) {
      _metricImpl.liPosMinGap(ecModel, perKeyPerAxis, ecPreparePerKeyPerAxis);
    }
  });
}
function registerMetricImpl(metricType, impl) {
  _metricImpl[metricType] = impl;
}
var _metricImpl = {};
function associateSeriesWithAxis(axis, seriesModel, coordSysType) {
  if (!axis) {
    return;
  }
  var ecModel = seriesModel.ecModel;
  var ecFullUpdateCache = ecModelCacheFullUpdateInner(getCachePerECFullUpdate(ecModel));
  var axisModelUid = axis.model.uid;
  if (true) {
    validateInputAxis(axis);
    var axSerPairCheck = ecFullUpdateCache.axSerPairCheck || (ecFullUpdateCache.axSerPairCheck = createHashMap());
    var pairKey = "" + axisModelUid + AXIS_STAT_KEY_DELIMITER + seriesModel.uid;
    assert(!axSerPairCheck.get(pairKey));
    axSerPairCheck.set(pairKey, 1);
  }
  var seriesOnAxisMap = ecFullUpdateCache.axSer || (ecFullUpdateCache.axSer = createHashMap());
  var seriesListPerAxis = seriesOnAxisMap.get(axisModelUid) || seriesOnAxisMap.set(axisModelUid, []);
  if (true) {
    var lastSeries = seriesListPerAxis[seriesListPerAxis.length - 1];
    if (lastSeries) {
      assert(lastSeries.seriesIndex < seriesModel.seriesIndex);
    }
  }
  seriesListPerAxis.push(seriesModel);
  var seriesType2 = seriesModel.subType;
  var isBaseAxis = seriesModel.getBaseAxis() === axis;
  var client = clientsForLookup.get(makeClientLookupKey(seriesType2, isBaseAxis, coordSysType)) || clientsForLookup.get(makeClientLookupKey(seriesType2, isBaseAxis, null));
  if (!client) {
    return;
  }
  var keyed = ecFullUpdateCache.keyed || (ecFullUpdateCache.keyed = createHashMap());
  var keys2 = ecFullUpdateCache.keys || (ecFullUpdateCache.keys = createHashMap());
  var axisStatKey = client.key;
  var perKey = keyed.get(axisStatKey) || keyed.set(axisStatKey, createHashMap());
  var perKeyPerAxis = perKey.get(axisModelUid);
  if (!perKeyPerAxis) {
    perKeyPerAxis = perKey.set(axisModelUid, {
      axis,
      sers: [],
      serByIdx: []
    });
    perKeyPerAxis.metrics = client.getMetrics(axis);
    (keys2.get(axisModelUid) || keys2.set(axisModelUid, [])).push(axisStatKey);
  }
  perKeyPerAxis.sers.push(seriesModel);
  perKeyPerAxis.serByIdx[seriesModel.seriesIndex] = seriesModel;
}
function makeClientLookupKey(seriesType2, isBaseAxis, coordSysType) {
  return seriesType2 + AXIS_STAT_KEY_DELIMITER + retrieve2(isBaseAxis, true) + AXIS_STAT_KEY_DELIMITER + (coordSysType || "");
}
function requireAxisStatistics(registers, client) {
  var clientKey = makeClientLookupKey(client.seriesType, client.baseAxis, client.coordSysType);
  if (true) {
    assert(client.seriesType && client.key && !clientsForCheckingStatKey.get(client.key) && !clientsForLookup.get(clientKey));
    clientsForCheckingStatKey.set(client.key, 1);
  }
  clientsForLookup.set(clientKey, client);
  callOnlyOnce(registers, function() {
    registers.registerProcessor(registers.PRIORITY.PROCESSOR.AXIS_STATISTICS, {
      // NOTE: Theoretically, `appendData` requires `dirtyOnOverallProgress: true` here to re-calculate them.
      // But this OVERALL_STAGE_TASK is applied to all series (no `getTargetSeries` specified),
      // `dirtyOnOverallProgress: true` can cause irrelevant series (e.g., series on geo)
      // to be re-rendered when `appendData` is called, which cause `appendData` meaningless,
      // thereby not setting `dirtyOnOverallProgress: true`.
      overallReset: performAxisStatisticsOnOverallReset
    });
  });
}
var clientsForCheckingStatKey;
if (true) {
  clientsForCheckingStatKey = createHashMap();
}
var clientsForLookup = createHashMap();

// node_modules/echarts/lib/coord/axisBand.js
var FALLBACK_BAND_WIDTH_RATIO = 0.8;
function calcBandWidth(axis, opt) {
  opt = opt || {};
  var out2 = {
    w: NaN,
    w2: NaN
  };
  var scale3 = axis.scale;
  var fromStat = opt.fromStat;
  var min3 = opt.min;
  var scaleLinearSpan = getScaleLinearSpanForMapping(scale3);
  if (!isNullableNumberFinite(scaleLinearSpan)) {
    scaleLinearSpan = NaN;
  }
  var axisExtent = axis.getExtent();
  var pxSpan = mathAbs3(axisExtent[1] - axisExtent[0]);
  if (isOrdinalScale(scale3)) {
    calcBandWidthForCategoryAxis(out2, axis, scaleLinearSpan, pxSpan);
  } else if (fromStat) {
    calcBandWidthForNumericAxis(out2, axis, scaleLinearSpan, pxSpan, fromStat);
  } else if (min3 == null) {
    if (true) {
      assert(false);
    }
  }
  if (min3 != null) {
    out2.w = isNullableNumberFinite(out2.w) ? mathMax4(min3, out2.w) : min3;
  }
  return out2;
}
function calcBandWidthForCategoryAxis(out2, axis, scaleLinearSpan, pxSpan) {
  var onBand = axis.onBand;
  var len2 = scaleLinearSpan + (onBand ? 1 : 0);
  len2 === 0 && (len2 = 1);
  out2.w = pxSpan / len2;
  if (!onBand && scaleLinearSpan && pxSpan) {
    out2.w2 = out2.w * scaleLinearSpan / pxSpan;
  }
}
function calcBandWidthForNumericAxis(out2, axis, scaleLinearSpan, pxSpan, fromStat) {
  if (true) {
    assert(fromStat);
  }
  var onlySingular = false;
  var bandWidthInData = -Infinity;
  each(fromStat.key ? [getAxisStat(axis, fromStat.key)] : getAxisStatBySeries(axis, fromStat.sers || []), function(stat) {
    var liPosMinGap = stat.liPosMinGap;
    if (liPosMinGap != null) {
      if (liPosMinGap > 0) {
        if (liPosMinGap > bandWidthInData) {
          bandWidthInData = liPosMinGap;
        }
        onlySingular = false;
      } else if (liPosMinGap === LINEAR_POSITIVE_MIN_GAP_SINGLE_VALID_VALUE) {
        onlySingular = true;
      }
    }
  });
  if (isNullableNumberFinite(scaleLinearSpan) && scaleLinearSpan > 0 && isNullableNumberFinite(bandWidthInData)) {
    out2.w = pxSpan / scaleLinearSpan * bandWidthInData;
    out2.w2 = bandWidthInData;
  } else if (onlySingular) {
    out2.w = pxSpan * FALLBACK_BAND_WIDTH_RATIO;
    out2.w2 = out2.w * scaleLinearSpan / pxSpan;
  }
}

// node_modules/echarts/lib/coord/Axis.js
var NORMALIZED_EXTENT = [0, 1];
var Axis = (
  /** @class */
  (function() {
    function Axis2(dim, scale3, extent) {
      this.onBand = false;
      this.inverse = false;
      this.dim = dim;
      this.scale = scale3;
      this._extent = extent || [0, 0];
    }
    Axis2.prototype.contain = function(coord) {
      var extent = this._extent;
      var min3 = Math.min(extent[0], extent[1]);
      var max3 = Math.max(extent[0], extent[1]);
      return coord >= min3 && coord <= max3;
    };
    Axis2.prototype.containData = function(data) {
      return this.scale.contain(this.scale.parse(data));
    };
    Axis2.prototype.getExtent = function() {
      return this._extent.slice();
    };
    Axis2.prototype.setExtent = function(start2, end2) {
      var extent = this._extent;
      extent[0] = start2;
      extent[1] = end2;
    };
    Axis2.prototype.dataToCoord = function(data, clamp2) {
      var scale3 = this.scale;
      data = scale3.normalize(scale3.parse(data));
      return linearMap(data, NORMALIZED_EXTENT, makeExtentWithBands(this), clamp2);
    };
    Axis2.prototype.coordToData = function(coord, clamp2) {
      var t = linearMap(coord, makeExtentWithBands(this), NORMALIZED_EXTENT, clamp2);
      return this.scale.scale(t);
    };
    Axis2.prototype.pointToData = function(point, clamp2) {
      return;
    };
    Axis2.prototype.getTicksCoords = function(opt) {
      opt = opt || {};
      var tickModel = opt.tickModel || this.getTickModel();
      var result = createAxisTicks(this, tickModel, {
        breakTicks: opt.breakTicks,
        pruneByBreak: opt.pruneByBreak
      });
      var preTicksCoords = map2(result.ticks, function(tick) {
        return {
          coord: this.dataToCoord(getTickValueOutermost(this.scale, tick)),
          tick
        };
      }, this);
      var alignWithLabel = tickModel.get("alignWithLabel");
      var onBandModified = fixOnBandTicksCoords(this, preTicksCoords, alignWithLabel);
      return map2(preTicksCoords, function(item) {
        return {
          coord: item.coord,
          tickValue: item.tick.value,
          onBand: onBandModified
        };
      });
    };
    Axis2.prototype.getMinorTicksCoords = function() {
      if (isOrdinalScale(this.scale)) {
        return [];
      }
      var minorTickModel = this.model.getModel("minorTick");
      var splitNumber = minorTickModel.get("splitNumber");
      if (!(splitNumber > 0 && splitNumber < 100)) {
        splitNumber = 5;
      }
      var minorTicks = this.scale.getMinorTicks(splitNumber);
      var minorTicksCoords = map2(minorTicks, function(minorTicksGroup) {
        return map2(minorTicksGroup, function(minorTick) {
          return {
            coord: this.dataToCoord(minorTick),
            tickValue: minorTick
          };
        }, this);
      }, this);
      return minorTicksCoords;
    };
    Axis2.prototype.getViewLabels = function(ctx) {
      ctx = ctx || createAxisLabelsComputingContext(AxisTickLabelComputingKind.determine);
      return createAxisLabels(this, ctx).labels;
    };
    Axis2.prototype.getLabelModel = function() {
      return this.model.getModel("axisLabel");
    };
    Axis2.prototype.getTickModel = function() {
      return this.model.getModel("axisTick");
    };
    Axis2.prototype.getBandWidth = function() {
      return calcBandWidth(this, {
        min: 1
      }).w;
    };
    Axis2.prototype.calculateCategoryInterval = function(ctx) {
      ctx = ctx || createAxisLabelsComputingContext(AxisTickLabelComputingKind.determine);
      return calculateCategoryInterval(this, ctx);
    };
    return Axis2;
  })()
);
function makeExtentWithBands(axis) {
  var extent = axis.getExtent();
  if (axis.onBand) {
    var size = extent[1] - extent[0];
    var margin = size / axis.scale.count() / 2;
    extent[0] += margin;
    extent[1] -= margin;
  }
  return extent;
}
function fixOnBandTicksCoords(axis, preTicksCoords, alignWithLabel) {
  var ticksLen = preTicksCoords.length;
  if (!axis.onBand || alignWithLabel || !ticksLen) {
    return false;
  }
  var bandWidth = calcBandWidth(axis).w;
  if (!bandWidth) {
    return false;
  }
  each(preTicksCoords, function(ticksItem) {
    ticksItem.coord -= bandWidth / 2;
  });
  var dataExtent = axis.scale.getExtent();
  var oldLast = preTicksCoords[ticksLen - 1];
  if (oldLast.tick.offInterval) {
    preTicksCoords.pop();
  }
  preTicksCoords.push({
    coord: oldLast.coord + bandWidth,
    tick: {
      value: dataExtent[1] + 1
    }
  });
  return true;
}
var Axis_default = Axis;

// node_modules/echarts/lib/coord/cartesian/Axis2D.js
var Axis2D = (
  /** @class */
  (function(_super) {
    __extends(Axis2D2, _super);
    function Axis2D2(dim, scale3, coordExtent, axisType, position) {
      var _this = _super.call(this, dim, scale3, coordExtent) || this;
      _this.index = 0;
      _this.type = axisType || "value";
      _this.position = position || "bottom";
      return _this;
    }
    Axis2D2.prototype.isHorizontal = function() {
      var position = this.position;
      return position === "top" || position === "bottom";
    };
    Axis2D2.prototype.getGlobalExtent = function(asc3) {
      var ret = this.getExtent();
      ret[0] = this.toGlobalCoord(ret[0]);
      ret[1] = this.toGlobalCoord(ret[1]);
      asc3 && ret[0] > ret[1] && ret.reverse();
      return ret;
    };
    Axis2D2.prototype.pointToData = function(point, clamp2) {
      return this.coordToData(this.toLocalCoord(point[this.dim === "x" ? 0 : 1]), clamp2);
    };
    Axis2D2.prototype.setCategorySortInfo = function(info) {
      if (this.type !== "category") {
        return false;
      }
      this.model.option.categorySortInfo = info;
      this.scale.setSortInfo(info);
    };
    return Axis2D2;
  })(Axis_default)
);
var Axis2D_default = Axis2D;

// node_modules/echarts/lib/label/labelLayoutHelper.js
var LABEL_LAYOUT_BASE_PROPS = ["label", "labelLine", "layoutOption", "priority", "defaultAttr", "marginForce", "minMarginForce", "marginDefault", "suggestIgnore"];
var LABEL_LAYOUT_DIRTY_BIT_OTHERS = 1;
var LABEL_LAYOUT_DIRTY_BIT_OBB = 2;
var LABEL_LAYOUT_DIRTY_ALL = LABEL_LAYOUT_DIRTY_BIT_OTHERS | LABEL_LAYOUT_DIRTY_BIT_OBB;
function setLabelLayoutDirty(labelGeometry, dirtyOrClear, dirtyBits) {
  dirtyBits = dirtyBits || LABEL_LAYOUT_DIRTY_ALL;
  dirtyOrClear ? labelGeometry.dirty |= dirtyBits : labelGeometry.dirty &= ~dirtyBits;
}
function isLabelLayoutDirty(labelGeometry, dirtyBits) {
  dirtyBits = dirtyBits || LABEL_LAYOUT_DIRTY_ALL;
  return labelGeometry.dirty == null || !!(labelGeometry.dirty & dirtyBits);
}
function ensureLabelLayoutWithGeometry(labelLayout2) {
  if (!labelLayout2) {
    return;
  }
  if (isLabelLayoutDirty(labelLayout2)) {
    computeLabelGeometry(labelLayout2, labelLayout2.label, labelLayout2);
  }
  return labelLayout2;
}
function computeLabelGeometry(out2, label, opt) {
  var rawTransform = label.getComputedTransform();
  out2.transform = ensureCopyTransform(out2.transform, rawTransform);
  var outLocalRect = out2.localRect = ensureCopyRect(out2.localRect, label.getBoundingRect());
  var labelStyleExt = label.style;
  var margin = labelStyleExt.margin;
  var marginForce = opt && opt.marginForce;
  var minMarginForce = opt && opt.minMarginForce;
  var marginDefault = opt && opt.marginDefault;
  var marginType = labelStyleExt.__marginType;
  if (marginType == null && marginDefault) {
    margin = marginDefault;
    marginType = LabelMarginType.textMargin;
  }
  for (var i2 = 0; i2 < 4; i2++) {
    _tmpLabelMargin[i2] = marginType === LabelMarginType.minMargin && minMarginForce && minMarginForce[i2] != null ? minMarginForce[i2] : marginForce && marginForce[i2] != null ? marginForce[i2] : margin ? margin[i2] : 0;
  }
  if (marginType === LabelMarginType.textMargin) {
    expandOrShrinkRect(outLocalRect, _tmpLabelMargin, false, false);
  }
  var outGlobalRect = out2.rect = ensureCopyRect(out2.rect, outLocalRect);
  if (rawTransform) {
    outGlobalRect.applyTransform(rawTransform);
  }
  if (marginType === LabelMarginType.minMargin) {
    expandOrShrinkRect(outGlobalRect, _tmpLabelMargin, false, false);
  }
  out2.axisAligned = isBoundingRectAxisAligned(rawTransform);
  (out2.label = out2.label || {}).ignore = label.ignore;
  setLabelLayoutDirty(out2, false);
  setLabelLayoutDirty(out2, true, LABEL_LAYOUT_DIRTY_BIT_OBB);
  return out2;
}
var _tmpLabelMargin = [0, 0, 0, 0];
function computeLabelGeometry2(out2, rawLocalRect, rawTransform) {
  out2.transform = ensureCopyTransform(out2.transform, rawTransform);
  out2.localRect = ensureCopyRect(out2.localRect, rawLocalRect);
  out2.rect = ensureCopyRect(out2.rect, rawLocalRect);
  if (rawTransform) {
    out2.rect.applyTransform(rawTransform);
  }
  out2.axisAligned = isBoundingRectAxisAligned(rawTransform);
  out2.obb = void 0;
  (out2.label = out2.label || {}).ignore = false;
  return out2;
}
function labelLayoutApplyTranslation(labelLayout2, offset) {
  if (!labelLayout2) {
    return;
  }
  labelLayout2.label.x += offset.x;
  labelLayout2.label.y += offset.y;
  labelLayout2.label.markRedraw();
  var transform = labelLayout2.transform;
  if (transform) {
    transform[4] += offset.x;
    transform[5] += offset.y;
  }
  var globalRect = labelLayout2.rect;
  if (globalRect) {
    globalRect.x += offset.x;
    globalRect.y += offset.y;
  }
  var obb = labelLayout2.obb;
  if (obb) {
    obb.fromBoundingRect(labelLayout2.localRect, transform);
  }
}
function newLabelLayoutWithGeometry(newBaseWithDefaults, source) {
  for (var i2 = 0; i2 < LABEL_LAYOUT_BASE_PROPS.length; i2++) {
    var prop = LABEL_LAYOUT_BASE_PROPS[i2];
    if (newBaseWithDefaults[prop] == null) {
      newBaseWithDefaults[prop] = source[prop];
    }
  }
  return ensureLabelLayoutWithGeometry(newBaseWithDefaults);
}
function ensureOBB(labelGeometry) {
  var obb = labelGeometry.obb;
  if (!obb || isLabelLayoutDirty(labelGeometry, LABEL_LAYOUT_DIRTY_BIT_OBB)) {
    labelGeometry.obb = obb = obb || new OrientedBoundingRect_default();
    obb.fromBoundingRect(labelGeometry.localRect, labelGeometry.transform);
    setLabelLayoutDirty(labelGeometry, false, LABEL_LAYOUT_DIRTY_BIT_OBB);
  }
  return obb;
}
function shiftLayoutOnXY(list, xyDimIdx, minBound, maxBound, balanceShift) {
  var len2 = list.length;
  var xyDim = XY2[xyDimIdx];
  var sizeDim = WH2[xyDimIdx];
  if (len2 < 2) {
    return false;
  }
  list.sort(function(a, b) {
    return a.rect[xyDim] - b.rect[xyDim];
  });
  var lastPos = 0;
  var delta;
  var adjusted = false;
  var totalShifts = 0;
  for (var i2 = 0; i2 < len2; i2++) {
    var item = list[i2];
    var rect = item.rect;
    delta = rect[xyDim] - lastPos;
    if (delta < 0) {
      rect[xyDim] -= delta;
      item.label[xyDim] -= delta;
      adjusted = true;
    }
    var shift = Math.max(-delta, 0);
    totalShifts += shift;
    lastPos = rect[xyDim] + rect[sizeDim];
  }
  if (totalShifts > 0 && balanceShift) {
    shiftList(-totalShifts / len2, 0, len2);
  }
  var first = list[0];
  var last = list[len2 - 1];
  var minGap;
  var maxGap;
  updateMinMaxGap();
  minGap < 0 && squeezeGaps(-minGap, 0.8);
  maxGap < 0 && squeezeGaps(maxGap, 0.8);
  updateMinMaxGap();
  takeBoundsGap(minGap, maxGap, 1);
  takeBoundsGap(maxGap, minGap, -1);
  updateMinMaxGap();
  if (minGap < 0) {
    squeezeWhenBailout(-minGap);
  }
  if (maxGap < 0) {
    squeezeWhenBailout(maxGap);
  }
  function updateMinMaxGap() {
    minGap = first.rect[xyDim] - minBound;
    maxGap = maxBound - last.rect[xyDim] - last.rect[sizeDim];
  }
  function takeBoundsGap(gapThisBound, gapOtherBound, moveDir) {
    if (gapThisBound < 0) {
      var moveFromMaxGap = Math.min(gapOtherBound, -gapThisBound);
      if (moveFromMaxGap > 0) {
        shiftList(moveFromMaxGap * moveDir, 0, len2);
        var remained = moveFromMaxGap + gapThisBound;
        if (remained < 0) {
          squeezeGaps(-remained * moveDir, 1);
        }
      } else {
        squeezeGaps(-gapThisBound * moveDir, 1);
      }
    }
  }
  function shiftList(delta2, start2, end2) {
    if (delta2 !== 0) {
      adjusted = true;
    }
    for (var i3 = start2; i3 < end2; i3++) {
      var item2 = list[i3];
      var rect2 = item2.rect;
      rect2[xyDim] += delta2;
      item2.label[xyDim] += delta2;
    }
  }
  function squeezeGaps(delta2, maxSqeezePercent) {
    var gaps = [];
    var totalGaps = 0;
    for (var i3 = 1; i3 < len2; i3++) {
      var prevItemRect = list[i3 - 1].rect;
      var gap = Math.max(list[i3].rect[xyDim] - prevItemRect[xyDim] - prevItemRect[sizeDim], 0);
      gaps.push(gap);
      totalGaps += gap;
    }
    if (!totalGaps) {
      return;
    }
    var squeezePercent = Math.min(Math.abs(delta2) / totalGaps, maxSqeezePercent);
    if (delta2 > 0) {
      for (var i3 = 0; i3 < len2 - 1; i3++) {
        var movement = gaps[i3] * squeezePercent;
        shiftList(movement, 0, i3 + 1);
      }
    } else {
      for (var i3 = len2 - 1; i3 > 0; i3--) {
        var movement = gaps[i3 - 1] * squeezePercent;
        shiftList(-movement, i3, len2);
      }
    }
  }
  function squeezeWhenBailout(delta2) {
    var dir3 = delta2 < 0 ? -1 : 1;
    delta2 = Math.abs(delta2);
    var moveForEachLabel = Math.ceil(delta2 / (len2 - 1));
    for (var i3 = 0; i3 < len2 - 1; i3++) {
      if (dir3 > 0) {
        shiftList(moveForEachLabel, 0, i3 + 1);
      } else {
        shiftList(-moveForEachLabel, len2 - i3 - 1, len2);
      }
      delta2 -= moveForEachLabel;
      if (delta2 <= 0) {
        return;
      }
    }
  }
  return adjusted;
}
function restoreIgnore(labelList) {
  for (var i2 = 0; i2 < labelList.length; i2++) {
    var labelItem = labelList[i2];
    var defaultAttr = labelItem.defaultAttr;
    var labelLine = labelItem.labelLine;
    labelItem.label.attr("ignore", defaultAttr.ignore);
    labelLine && labelLine.attr("ignore", defaultAttr.labelGuideIgnore);
  }
}
function hideOverlap(labelList) {
  var displayedLabels = [];
  labelList.sort(function(a, b) {
    return (b.suggestIgnore ? 1 : 0) - (a.suggestIgnore ? 1 : 0) || b.priority - a.priority;
  });
  function hideEl(el) {
    if (!el.ignore) {
      var emphasisState = el.ensureState("emphasis");
      if (emphasisState.ignore == null) {
        emphasisState.ignore = false;
      }
    }
    el.ignore = true;
  }
  for (var i2 = 0; i2 < labelList.length; i2++) {
    var labelItem = ensureLabelLayoutWithGeometry(labelList[i2]);
    if (labelItem.label.ignore) {
      continue;
    }
    var label = labelItem.label;
    var labelLine = labelItem.labelLine;
    var overlapped = false;
    for (var j = 0; j < displayedLabels.length; j++) {
      if (labelIntersect(labelItem, displayedLabels[j], null, {
        touchThreshold: 0.05
      })) {
        overlapped = true;
        break;
      }
    }
    if (overlapped) {
      hideEl(label);
      labelLine && hideEl(labelLine);
    } else {
      displayedLabels.push(labelItem);
    }
  }
}
function labelIntersect(baseLayoutInfo, targetLayoutInfo, mtv, intersectOpt) {
  if (!baseLayoutInfo || !targetLayoutInfo) {
    return false;
  }
  if (baseLayoutInfo.label && baseLayoutInfo.label.ignore || targetLayoutInfo.label && targetLayoutInfo.label.ignore) {
    return false;
  }
  if (!baseLayoutInfo.rect.intersect(targetLayoutInfo.rect, mtv, intersectOpt)) {
    return false;
  }
  if (baseLayoutInfo.axisAligned && targetLayoutInfo.axisAligned) {
    return true;
  }
  return ensureOBB(baseLayoutInfo).intersect(ensureOBB(targetLayoutInfo), mtv, intersectOpt);
}

// node_modules/echarts/lib/component/axis/axisBreakHelper.js
var _impl2 = null;
function getAxisBreakHelper() {
  return _impl2;
}

// node_modules/echarts/lib/component/axis/axisAction.js
var AXIS_BREAK_EXPAND_ACTION_TYPE = "expandAxisBreak";

// node_modules/echarts/lib/component/axis/AxisBuilder.js
var PI5 = Math.PI;
var DEFAULT_CENTER_NAME_MARGIN_LEVELS = [[1, 2, 1, 2], [5, 3, 5, 3], [8, 3, 8, 3]];
var DEFAULT_ENDS_NAME_MARGIN_LEVELS = [[0, 1, 0, 1], [0, 3, 0, 3], [0, 3, 0, 3]];
var getLabelInner = makeInner();
var getTickInner = makeInner();
var AxisBuilderSharedContext = (
  /** @class */
  (function() {
    function AxisBuilderSharedContext2(resolveAxisNameOverlap) {
      this.recordMap = {};
      this.resolveAxisNameOverlap = resolveAxisNameOverlap;
    }
    AxisBuilderSharedContext2.prototype.ensureRecord = function(axisModel) {
      var dim = axisModel.axis.dim;
      var idx = axisModel.componentIndex;
      var recordMap = this.recordMap;
      var records = recordMap[dim] || (recordMap[dim] = []);
      return records[idx] || (records[idx] = {
        ready: {}
      });
    };
    return AxisBuilderSharedContext2;
  })()
);
function resetOverlapRecordToShared(cfg, shared, axisModel, labelLayoutList) {
  var axis = axisModel.axis;
  var record = shared.ensureRecord(axisModel);
  var labelInfoList = [];
  var stOccupiedRect;
  var useStOccupiedRect = hasAxisName(cfg.axisName) && isNameLocationCenter(cfg.nameLocation);
  each(labelLayoutList, function(layout3) {
    var layoutInfo = ensureLabelLayoutWithGeometry(layout3);
    if (!layoutInfo || layoutInfo.label.ignore) {
      return;
    }
    labelInfoList.push(layoutInfo);
    var transGroup = record.transGroup;
    if (useStOccupiedRect) {
      transGroup.transform ? invert(_stTransTmp, transGroup.transform) : identity(_stTransTmp);
      if (layoutInfo.transform) {
        mul(_stTransTmp, _stTransTmp, layoutInfo.transform);
      }
      BoundingRect_default.copy(_stLabelRectTmp, layoutInfo.localRect);
      _stLabelRectTmp.applyTransform(_stTransTmp);
      stOccupiedRect ? stOccupiedRect.union(_stLabelRectTmp) : BoundingRect_default.copy(stOccupiedRect = new BoundingRect_default(0, 0, 0, 0), _stLabelRectTmp);
    }
  });
  var sortByDim = Math.abs(record.dirVec.x) > 0.1 ? "x" : "y";
  var sortByValue = record.transGroup[sortByDim];
  labelInfoList.sort(function(info1, info2) {
    return Math.abs(info1.label[sortByDim] - sortByValue) - Math.abs(info2.label[sortByDim] - sortByValue);
  });
  if (useStOccupiedRect && stOccupiedRect) {
    var extent = axis.getExtent();
    var axisLineX = Math.min(extent[0], extent[1]);
    var axisLineWidth = Math.max(extent[0], extent[1]) - axisLineX;
    stOccupiedRect.union(new BoundingRect_default(axisLineX, 0, axisLineWidth, 1));
  }
  record.stOccupiedRect = stOccupiedRect;
  record.labelInfoList = labelInfoList;
}
var _stTransTmp = create();
var _stLabelRectTmp = new BoundingRect_default(0, 0, 0, 0);
var resolveAxisNameOverlapDefault = function(cfg, ctx, axisModel, nameLayoutInfo, nameMoveDirVec, thisRecord) {
  if (isNameLocationCenter(cfg.nameLocation)) {
    var stOccupiedRect = thisRecord.stOccupiedRect;
    if (stOccupiedRect) {
      moveIfOverlap(computeLabelGeometry2({}, stOccupiedRect, thisRecord.transGroup.transform), nameLayoutInfo, nameMoveDirVec);
    }
  } else {
    moveIfOverlapByLinearLabels(thisRecord.labelInfoList, thisRecord.dirVec, nameLayoutInfo, nameMoveDirVec);
  }
};
function moveIfOverlap(basedLayoutInfo, movableLayoutInfo, moveDirVec) {
  var mtv = new Point_default();
  if (labelIntersect(basedLayoutInfo, movableLayoutInfo, mtv, {
    direction: Math.atan2(moveDirVec.y, moveDirVec.x),
    bidirectional: false,
    touchThreshold: 0.05
  })) {
    labelLayoutApplyTranslation(movableLayoutInfo, mtv);
  }
}
function moveIfOverlapByLinearLabels(baseLayoutInfoList, baseDirVec, movableLayoutInfo, moveDirVec) {
  var sameDir = Point_default.dot(moveDirVec, baseDirVec) >= 0;
  for (var idx = 0, len2 = baseLayoutInfoList.length; idx < len2; idx++) {
    var labelInfo = baseLayoutInfoList[sameDir ? idx : len2 - 1 - idx];
    if (!labelInfo.label.ignore) {
      moveIfOverlap(labelInfo, movableLayoutInfo, moveDirVec);
    }
  }
}
var AxisBuilder = (
  /** @class */
  (function() {
    function AxisBuilder2(axisModel, api, opt, shared) {
      this.group = new Group_default();
      this._axisModel = axisModel;
      this._api = api;
      this._local = {};
      this._shared = shared || new AxisBuilderSharedContext(resolveAxisNameOverlapDefault);
      this._resetCfgDetermined(opt);
    }
    AxisBuilder2.prototype.updateCfg = function(opt) {
      if (true) {
        var ready = this._shared.ensureRecord(this._axisModel).ready;
        assert(!ready.axisLine && !ready.axisTickLabelDetermine);
        ready.axisName = ready.axisTickLabelEstimate = false;
      }
      var raw = this._cfg.raw;
      raw.position = opt.position;
      raw.labelOffset = opt.labelOffset;
      this._resetCfgDetermined(raw);
    };
    AxisBuilder2.prototype.__getRawCfg = function() {
      return this._cfg.raw;
    };
    AxisBuilder2.prototype._resetCfgDetermined = function(raw) {
      var axisModel = this._axisModel;
      var axisModelDefaultOption = axisModel.getDefaultOption ? axisModel.getDefaultOption() : {};
      var axisName = retrieve2(raw.axisName, axisModel.get("name"));
      var nameMoveOverlapOption = axisModel.get("nameMoveOverlap");
      if (nameMoveOverlapOption == null || nameMoveOverlapOption === "auto") {
        nameMoveOverlapOption = retrieve2(raw.defaultNameMoveOverlap, true);
      }
      var cfg = {
        raw,
        position: raw.position,
        rotation: raw.rotation,
        nameDirection: retrieve2(raw.nameDirection, 1),
        tickDirection: retrieve2(raw.tickDirection, 1),
        labelDirection: retrieve2(raw.labelDirection, 1),
        labelOffset: retrieve2(raw.labelOffset, 0),
        silent: retrieve2(raw.silent, true),
        axisName,
        nameLocation: retrieve3(axisModel.get("nameLocation"), axisModelDefaultOption.nameLocation, "end"),
        shouldNameMoveOverlap: hasAxisName(axisName) && nameMoveOverlapOption,
        optionHideOverlap: axisModel.get(["axisLabel", "hideOverlap"]),
        showMinorTicks: axisModel.get(["minorTick", "show"])
      };
      if (true) {
        assert(cfg.position != null);
        assert(cfg.rotation != null);
      }
      this._cfg = cfg;
      var transformGroup = new Group_default({
        x: cfg.position[0],
        y: cfg.position[1],
        rotation: cfg.rotation
      });
      transformGroup.updateTransform();
      this._transformGroup = transformGroup;
      var record = this._shared.ensureRecord(axisModel);
      record.transGroup = this._transformGroup;
      record.dirVec = new Point_default(Math.cos(-cfg.rotation), Math.sin(-cfg.rotation));
    };
    AxisBuilder2.prototype.build = function(axisPartNameMap, extraParams) {
      var _this = this;
      if (!axisPartNameMap) {
        axisPartNameMap = {
          axisLine: true,
          axisTickLabelEstimate: false,
          axisTickLabelDetermine: true,
          axisName: true
        };
      }
      each(AXIS_BUILDER_AXIS_PART_NAMES, function(partName) {
        if (axisPartNameMap[partName]) {
          builders[partName](_this._cfg, _this._local, _this._shared, _this._axisModel, _this.group, _this._transformGroup, _this._api, extraParams || {});
        }
      });
      return this;
    };
    AxisBuilder2.innerTextLayout = function(axisRotation, textRotation, direction) {
      var rotationDiff = remRadian(textRotation - axisRotation);
      var textAlign;
      var textVerticalAlign;
      if (isRadianAroundZero(rotationDiff)) {
        textVerticalAlign = direction > 0 ? "top" : "bottom";
        textAlign = "center";
      } else if (isRadianAroundZero(rotationDiff - PI5)) {
        textVerticalAlign = direction > 0 ? "bottom" : "top";
        textAlign = "center";
      } else {
        textVerticalAlign = "middle";
        if (rotationDiff > 0 && rotationDiff < PI5) {
          textAlign = direction > 0 ? "right" : "left";
        } else {
          textAlign = direction > 0 ? "left" : "right";
        }
      }
      return {
        rotation: rotationDiff,
        textAlign,
        textVerticalAlign
      };
    };
    AxisBuilder2.makeAxisEventDataBase = function(axisModel) {
      var eventData = {
        componentType: axisModel.mainType,
        componentIndex: axisModel.componentIndex
      };
      eventData[axisModel.mainType + "Index"] = axisModel.componentIndex;
      return eventData;
    };
    AxisBuilder2.isLabelSilent = function(axisModel) {
      var tooltipOpt = axisModel.get("tooltip");
      return axisModel.get("silent") || !(axisModel.get("triggerEvent") || tooltipOpt && tooltipOpt.show);
    };
    return AxisBuilder2;
  })()
);
var AXIS_BUILDER_AXIS_PART_NAMES = ["axisLine", "axisTickLabelEstimate", "axisTickLabelDetermine", "axisName"];
var builders = {
  axisLine: function(cfg, local, shared, axisModel, group2, transformGroup, api) {
    if (true) {
      var ready = shared.ensureRecord(axisModel).ready;
      assert(!ready.axisLine);
      ready.axisLine = true;
    }
    var shown = axisModel.get(["axisLine", "show"]);
    if (shown === "auto") {
      shown = true;
      if (cfg.raw.axisLineAutoShow != null) {
        shown = !!cfg.raw.axisLineAutoShow;
      }
    }
    if (!shown) {
      return;
    }
    var extent = axisModel.axis.getExtent();
    var matrix = transformGroup.transform;
    var pt12 = [extent[0], 0];
    var pt22 = [extent[1], 0];
    var inverse = pt12[0] > pt22[0];
    if (matrix) {
      applyTransform(pt12, pt12, matrix);
      applyTransform(pt22, pt22, matrix);
    }
    var lineStyle = extend({
      lineCap: "round"
    }, axisModel.getModel(["axisLine", "lineStyle"]).getLineStyle());
    var pathBaseProp = {
      strokeContainThreshold: cfg.raw.strokeContainThreshold || 5,
      silent: true,
      z2: 1,
      style: lineStyle
    };
    if (axisModel.get(["axisLine", "breakLine"]) && hasBreaks(axisModel.axis.scale)) {
      getAxisBreakHelper().buildAxisBreakLine(axisModel, group2, transformGroup, pathBaseProp);
    } else {
      var line = new Line_default(extend({
        shape: {
          x1: pt12[0],
          y1: pt12[1],
          x2: pt22[0],
          y2: pt22[1]
        }
      }, pathBaseProp));
      subPixelOptimizeLine2(line.shape, line.style.lineWidth);
      line.anid = "line";
      group2.add(line);
    }
    var arrows = axisModel.get(["axisLine", "symbol"]);
    if (arrows != null) {
      var arrowSize = axisModel.get(["axisLine", "symbolSize"]);
      if (isString(arrows)) {
        arrows = [arrows, arrows];
      }
      if (isString(arrowSize) || isNumber(arrowSize)) {
        arrowSize = [arrowSize, arrowSize];
      }
      var arrowOffset = normalizeSymbolOffset(axisModel.get(["axisLine", "symbolOffset"]) || 0, arrowSize);
      var symbolWidth_1 = arrowSize[0];
      var symbolHeight_1 = arrowSize[1];
      each([{
        rotate: cfg.rotation + Math.PI / 2,
        offset: arrowOffset[0],
        r: 0
      }, {
        rotate: cfg.rotation - Math.PI / 2,
        offset: arrowOffset[1],
        r: Math.sqrt((pt12[0] - pt22[0]) * (pt12[0] - pt22[0]) + (pt12[1] - pt22[1]) * (pt12[1] - pt22[1]))
      }], function(point, index) {
        if (arrows[index] !== "none" && arrows[index] != null) {
          var symbol = createSymbol(arrows[index], -symbolWidth_1 / 2, -symbolHeight_1 / 2, symbolWidth_1, symbolHeight_1, lineStyle.stroke, true);
          var r = point.r + point.offset;
          var pt = inverse ? pt22 : pt12;
          symbol.attr({
            rotation: point.rotate,
            x: pt[0] + r * Math.cos(cfg.rotation),
            y: pt[1] - r * Math.sin(cfg.rotation),
            silent: true,
            z2: 11
          });
          group2.add(symbol);
        }
      });
    }
  },
  /**
   * [CAUTION] This method can be called multiple times, following the change due to `resetCfg` called
   *  in size measurement. Thus this method should be idempotent, and should be performant.
   */
  axisTickLabelEstimate: function(cfg, local, shared, axisModel, group2, transformGroup, api, extraParams) {
    if (true) {
      var ready = shared.ensureRecord(axisModel).ready;
      assert(!ready.axisTickLabelDetermine);
      ready.axisTickLabelEstimate = true;
    }
    var needCallLayout = dealLastTickLabelResultReusable(local, group2, extraParams);
    if (needCallLayout) {
      layOutAxisTickLabel(cfg, local, shared, axisModel, group2, transformGroup, api, AxisTickLabelComputingKind.estimate);
    }
  },
  /**
   * Finish axis tick label build.
   * Can be only called once.
   */
  axisTickLabelDetermine: function(cfg, local, shared, axisModel, group2, transformGroup, api, extraParams) {
    if (true) {
      var ready = shared.ensureRecord(axisModel).ready;
      ready.axisTickLabelDetermine = true;
    }
    var needCallLayout = dealLastTickLabelResultReusable(local, group2, extraParams);
    if (needCallLayout) {
      layOutAxisTickLabel(cfg, local, shared, axisModel, group2, transformGroup, api, AxisTickLabelComputingKind.determine);
    }
    var ticksEls = buildAxisMajorTicks(cfg, group2, transformGroup, axisModel);
    syncLabelIgnoreToMajorTicks(cfg, local.labelLayoutList, ticksEls);
    buildAxisMinorTicks(cfg, group2, transformGroup, axisModel, cfg.tickDirection);
  },
  /**
   * [CAUTION] This method can be called multiple times, following the change due to `resetCfg` called
   *  in size measurement. Thus this method should be idempotent, and should be performant.
   */
  axisName: function(cfg, local, shared, axisModel, group2, transformGroup, api, extraParams) {
    var sharedRecord = shared.ensureRecord(axisModel);
    if (true) {
      var ready = sharedRecord.ready;
      assert(ready.axisTickLabelEstimate || ready.axisTickLabelDetermine);
      ready.axisName = true;
    }
    if (local.nameEl) {
      group2.remove(local.nameEl);
      local.nameEl = sharedRecord.nameLayout = sharedRecord.nameLocation = null;
    }
    var name = cfg.axisName;
    if (!hasAxisName(name)) {
      return;
    }
    var nameLocation = cfg.nameLocation;
    var nameDirection = cfg.nameDirection;
    var textStyleModel = axisModel.getModel("nameTextStyle");
    var gap = axisModel.get("nameGap") || 0;
    var extent = axisModel.axis.getExtent();
    var gapStartEndSignal = axisModel.axis.inverse ? -1 : 1;
    var pos = new Point_default(0, 0);
    var nameMoveDirVec = new Point_default(0, 0);
    if (nameLocation === "start") {
      pos.x = extent[0] - gapStartEndSignal * gap;
      nameMoveDirVec.x = -gapStartEndSignal;
    } else if (nameLocation === "end") {
      pos.x = extent[1] + gapStartEndSignal * gap;
      nameMoveDirVec.x = gapStartEndSignal;
    } else {
      pos.x = (extent[0] + extent[1]) / 2;
      pos.y = cfg.labelOffset + nameDirection * gap;
      nameMoveDirVec.y = nameDirection;
    }
    var mt = create();
    nameMoveDirVec.transform(rotate(mt, mt, cfg.rotation));
    var nameRotation = axisModel.get("nameRotate");
    if (nameRotation != null) {
      nameRotation = nameRotation * PI5 / 180;
    }
    var labelLayout2;
    var axisNameAvailableWidth;
    if (isNameLocationCenter(nameLocation)) {
      labelLayout2 = AxisBuilder.innerTextLayout(
        cfg.rotation,
        nameRotation != null ? nameRotation : cfg.rotation,
        // Adapt to axis.
        nameDirection
      );
    } else {
      labelLayout2 = endTextLayout(cfg.rotation, nameLocation, nameRotation || 0, extent);
      axisNameAvailableWidth = cfg.raw.axisNameAvailableWidth;
      if (axisNameAvailableWidth != null) {
        axisNameAvailableWidth = Math.abs(axisNameAvailableWidth / Math.sin(labelLayout2.rotation));
        !isFinite(axisNameAvailableWidth) && (axisNameAvailableWidth = null);
      }
    }
    var textFont = textStyleModel.getFont();
    var truncateOpt = axisModel.get("nameTruncate", true) || {};
    var ellipsis = truncateOpt.ellipsis;
    var maxWidth = retrieve(cfg.raw.nameTruncateMaxWidth, truncateOpt.maxWidth, axisNameAvailableWidth);
    var nameMarginLevel = extraParams.nameMarginLevel || 0;
    var textEl = new Text_default({
      x: pos.x,
      y: pos.y,
      rotation: labelLayout2.rotation,
      silent: AxisBuilder.isLabelSilent(axisModel),
      style: createTextStyle(textStyleModel, {
        text: name,
        font: textFont,
        overflow: "truncate",
        width: maxWidth,
        ellipsis,
        fill: textStyleModel.getTextColor() || axisModel.get(["axisLine", "lineStyle", "color"]),
        align: textStyleModel.get("align") || labelLayout2.textAlign,
        verticalAlign: textStyleModel.get("verticalAlign") || labelLayout2.textVerticalAlign
      }),
      z2: 1
    });
    setTooltipConfig({
      el: textEl,
      componentModel: axisModel,
      itemName: name
    });
    textEl.__fullText = name;
    textEl.anid = "name";
    if (axisModel.get("triggerEvent")) {
      var eventData = AxisBuilder.makeAxisEventDataBase(axisModel);
      eventData.targetType = "axisName";
      eventData.name = name;
      getECData(textEl).eventData = eventData;
    }
    transformGroup.add(textEl);
    textEl.updateTransform();
    local.nameEl = textEl;
    var nameLayout = sharedRecord.nameLayout = ensureLabelLayoutWithGeometry({
      label: textEl,
      priority: textEl.z2,
      defaultAttr: {
        ignore: textEl.ignore
      },
      marginDefault: isNameLocationCenter(nameLocation) ? DEFAULT_CENTER_NAME_MARGIN_LEVELS[nameMarginLevel] : DEFAULT_ENDS_NAME_MARGIN_LEVELS[nameMarginLevel]
    });
    sharedRecord.nameLocation = nameLocation;
    group2.add(textEl);
    textEl.decomposeTransform();
    if (cfg.shouldNameMoveOverlap && nameLayout) {
      var record = shared.ensureRecord(axisModel);
      if (true) {
        assert(record.labelInfoList);
      }
      shared.resolveAxisNameOverlap(cfg, shared, axisModel, nameLayout, nameMoveDirVec, record);
    }
  }
};
function layOutAxisTickLabel(cfg, local, shared, axisModel, group2, transformGroup, api, kind) {
  if (!axisLabelBuildResultExists(local)) {
    buildAxisLabel(cfg, local, group2, kind, axisModel, api);
  }
  var labelLayoutList = local.labelLayoutList;
  updateAxisLabelChangableProps(cfg, axisModel, labelLayoutList, transformGroup);
  adjustBreakLabels(axisModel, cfg.rotation, labelLayoutList);
  var optionHideOverlap = cfg.optionHideOverlap;
  fixMinMaxLabelShow(axisModel, labelLayoutList, optionHideOverlap);
  if (optionHideOverlap) {
    hideOverlap(
      // Filter the already ignored labels by the previous overlap resolving methods.
      filter2(labelLayoutList, function(layout3) {
        return layout3 && !layout3.label.ignore;
      })
    );
  }
  resetOverlapRecordToShared(cfg, shared, axisModel, labelLayoutList);
}
function endTextLayout(rotation, textPosition, textRotate, extent) {
  var rotationDiff = remRadian(textRotate - rotation);
  var textAlign;
  var textVerticalAlign;
  var inverse = extent[0] > extent[1];
  var onLeft = textPosition === "start" && !inverse || textPosition !== "start" && inverse;
  if (isRadianAroundZero(rotationDiff - PI5 / 2)) {
    textVerticalAlign = onLeft ? "bottom" : "top";
    textAlign = "center";
  } else if (isRadianAroundZero(rotationDiff - PI5 * 1.5)) {
    textVerticalAlign = onLeft ? "top" : "bottom";
    textAlign = "center";
  } else {
    textVerticalAlign = "middle";
    if (rotationDiff < PI5 * 1.5 && rotationDiff > PI5 / 2) {
      textAlign = onLeft ? "left" : "right";
    } else {
      textAlign = onLeft ? "right" : "left";
    }
  }
  return {
    rotation: rotationDiff,
    textAlign,
    textVerticalAlign
  };
}
function fixMinMaxLabelShow(axisModel, labelLayoutList, optionHideOverlap) {
  var axis = axisModel.axis;
  var customValuesOption = axisModel.get(["axisLabel", "customValues"]);
  if (shouldShowAllLabels(axis)) {
    return;
  }
  function deal(showMinMaxLabelOption, outmostLabelIdx, innerLabelIdx) {
    var outmostLabelLayout = ensureLabelLayoutWithGeometry(labelLayoutList[outmostLabelIdx]);
    var innerLabelLayout = ensureLabelLayoutWithGeometry(labelLayoutList[innerLabelIdx]);
    var scale3 = axis.scale;
    if (!outmostLabelLayout || !innerLabelLayout) {
      return;
    }
    if (showMinMaxLabelOption == null) {
      if (!optionHideOverlap && customValuesOption) {
        return;
      }
      var tick = getLabelInner(outmostLabelLayout.label).labelInfo.tick;
      if (
        // TimeScale does not expand extent to "nice", so eliminate labels that are not nice.
        isTimeScale(scale3) && tick.notNice || isOrdinalScale(scale3) && tick.offInterval
      ) {
        ignoreEl(outmostLabelLayout.label);
        return;
      }
    }
    if (showMinMaxLabelOption === false || outmostLabelLayout.suggestIgnore) {
      ignoreEl(outmostLabelLayout.label);
      return;
    }
    if (innerLabelLayout.suggestIgnore) {
      ignoreEl(innerLabelLayout.label);
      return;
    }
    var touchThreshold = 0.1;
    if (!optionHideOverlap) {
      var marginForce = [0, 0, 0, 0];
      outmostLabelLayout = newLabelLayoutWithGeometry({
        marginForce
      }, outmostLabelLayout);
      innerLabelLayout = newLabelLayoutWithGeometry({
        marginForce
      }, innerLabelLayout);
    }
    if (labelIntersect(outmostLabelLayout, innerLabelLayout, null, {
      touchThreshold
    })) {
      if (showMinMaxLabelOption) {
        ignoreEl(innerLabelLayout.label);
      } else {
        ignoreEl(outmostLabelLayout.label);
      }
    }
  }
  var showMinLabelOption = axisModel.get(["axisLabel", "showMinLabel"]);
  var showMaxLabelOption = axisModel.get(["axisLabel", "showMaxLabel"]);
  var labelsLen = labelLayoutList.length;
  deal(showMinLabelOption, 0, 1);
  deal(showMaxLabelOption, labelsLen - 1, labelsLen - 2);
}
function syncLabelIgnoreToMajorTicks(cfg, labelLayoutList, tickEls) {
  if (cfg.showMinorTicks) {
    return;
  }
  each(labelLayoutList, function(labelLayout2) {
    if (labelLayout2 && labelLayout2.label.ignore) {
      for (var idx = 0; idx < tickEls.length; idx++) {
        var tickEl = tickEls[idx];
        var tickInner = getTickInner(tickEl);
        var labelInner2 = getLabelInner(labelLayout2.label);
        if (tickInner.tickValue != null && !tickInner.onBand && tickInner.tickValue === labelInner2.labelInfo.tick.value) {
          ignoreEl(tickEl);
          return;
        }
      }
    }
  });
}
function ignoreEl(el) {
  el && (el.ignore = true);
}
function createTicks(ticksCoords, tickTransform, tickEndCoord, tickLineStyle, anidPrefix) {
  var tickEls = [];
  var pt12 = [];
  var pt22 = [];
  for (var i2 = 0; i2 < ticksCoords.length; i2++) {
    var tickCoord = ticksCoords[i2].coord;
    pt12[0] = tickCoord;
    pt12[1] = 0;
    pt22[0] = tickCoord;
    pt22[1] = tickEndCoord;
    if (tickTransform) {
      applyTransform(pt12, pt12, tickTransform);
      applyTransform(pt22, pt22, tickTransform);
    }
    var tickEl = new Line_default({
      shape: {
        x1: pt12[0],
        y1: pt12[1],
        x2: pt22[0],
        y2: pt22[1]
      },
      style: tickLineStyle,
      z2: 2,
      autoBatch: true,
      silent: true
    });
    subPixelOptimizeLine2(tickEl.shape, tickEl.style.lineWidth);
    tickEl.anid = anidPrefix + "_" + ticksCoords[i2].tickValue;
    tickEls.push(tickEl);
    var inner25 = getTickInner(tickEl);
    inner25.onBand = !!ticksCoords[i2].onBand;
    inner25.tickValue = ticksCoords[i2].tickValue;
  }
  return tickEls;
}
function buildAxisMajorTicks(cfg, group2, transformGroup, axisModel) {
  var axis = axisModel.axis;
  var tickModel = axisModel.getModel("axisTick");
  var shown = tickModel.get("show");
  if (shown === "auto") {
    shown = true;
    if (cfg.raw.axisTickAutoShow != null) {
      shown = !!cfg.raw.axisTickAutoShow;
    }
  }
  if (!shown || axis.scale.isBlank()) {
    return [];
  }
  var lineStyleModel = tickModel.getModel("lineStyle");
  var tickEndCoord = cfg.tickDirection * tickModel.get("length");
  var ticksCoords = axis.getTicksCoords();
  var ticksEls = createTicks(ticksCoords, transformGroup.transform, tickEndCoord, defaults(lineStyleModel.getLineStyle(), {
    stroke: axisModel.get(["axisLine", "lineStyle", "color"])
  }), "ticks");
  for (var i2 = 0; i2 < ticksEls.length; i2++) {
    group2.add(ticksEls[i2]);
  }
  return ticksEls;
}
function buildAxisMinorTicks(cfg, group2, transformGroup, axisModel, tickDirection) {
  var axis = axisModel.axis;
  var minorTickModel = axisModel.getModel("minorTick");
  if (!cfg.showMinorTicks || axis.scale.isBlank()) {
    return;
  }
  var minorTicksCoords = axis.getMinorTicksCoords();
  if (!minorTicksCoords.length) {
    return;
  }
  var lineStyleModel = minorTickModel.getModel("lineStyle");
  var tickEndCoord = tickDirection * minorTickModel.get("length");
  var minorTickLineStyle = defaults(lineStyleModel.getLineStyle(), defaults(axisModel.getModel("axisTick").getLineStyle(), {
    stroke: axisModel.get(["axisLine", "lineStyle", "color"])
  }));
  for (var i2 = 0; i2 < minorTicksCoords.length; i2++) {
    var minorTicksEls = createTicks(minorTicksCoords[i2], transformGroup.transform, tickEndCoord, minorTickLineStyle, "minorticks_" + i2);
    for (var k2 = 0; k2 < minorTicksEls.length; k2++) {
      group2.add(minorTicksEls[k2]);
    }
  }
}
function dealLastTickLabelResultReusable(local, group2, extraParams) {
  if (axisLabelBuildResultExists(local)) {
    var axisLabelsCreationContext = local.axisLabelsCreationContext;
    if (true) {
      assert(local.labelGroup && axisLabelsCreationContext);
    }
    var noPxChangeTryDetermine = axisLabelsCreationContext.out.noPxChangeTryDetermine;
    if (extraParams.noPxChange) {
      var canDetermine = true;
      for (var idx = 0; idx < noPxChangeTryDetermine.length; idx++) {
        canDetermine = canDetermine && noPxChangeTryDetermine[idx]();
      }
      if (canDetermine) {
        return false;
      }
    }
    if (noPxChangeTryDetermine.length) {
      group2.remove(local.labelGroup);
      axisLabelBuildResultSet(local, null, null, null);
    }
  }
  return true;
}
function buildAxisLabel(cfg, local, group2, kind, axisModel, api) {
  var axis = axisModel.axis;
  var show = retrieve(cfg.raw.axisLabelShow, axisModel.get(["axisLabel", "show"]));
  var labelGroup = new Group_default();
  group2.add(labelGroup);
  var axisLabelCreationCtx = createAxisLabelsComputingContext(kind);
  if (!show || axis.scale.isBlank()) {
    axisLabelBuildResultSet(local, [], labelGroup, axisLabelCreationCtx);
    return;
  }
  var labelModel = axisModel.getModel("axisLabel");
  var labels = axis.getViewLabels(axisLabelCreationCtx);
  var labelRotation = (retrieve(cfg.raw.labelRotate, labelModel.get("rotate")) || 0) * PI5 / 180;
  var labelLayout2 = AxisBuilder.innerTextLayout(cfg.rotation, labelRotation, cfg.labelDirection);
  var rawCategoryData = axisModel.getCategories && axisModel.getCategories(true);
  var labelEls = [];
  var triggerEvent = axisModel.get("triggerEvent");
  var z2Min = Infinity;
  var z2Max = -Infinity;
  each(labels, function(labelItem, index) {
    var _a2;
    var labelItemTick = labelItem.tick;
    var formattedLabel = labelItem.formattedLabel;
    var rawLabel = labelItem.rawLabel;
    var itemLabelModel = labelModel;
    var tickValue = getTickValueOutermost(axis.scale, labelItemTick);
    if (rawCategoryData && rawCategoryData[tickValue]) {
      var rawCategoryItem = rawCategoryData[tickValue];
      if (isObject(rawCategoryItem) && rawCategoryItem.textStyle) {
        itemLabelModel = new Model_default(rawCategoryItem.textStyle, labelModel, axisModel.ecModel);
      }
    }
    var textColor = itemLabelModel.getTextColor() || axisModel.get(["axisLine", "lineStyle", "color"]);
    var align = itemLabelModel.getShallow("align", true) || labelLayout2.textAlign;
    var alignMin = retrieve2(itemLabelModel.getShallow("alignMinLabel", true), align);
    var alignMax = retrieve2(itemLabelModel.getShallow("alignMaxLabel", true), align);
    var verticalAlign = itemLabelModel.getShallow("verticalAlign", true) || itemLabelModel.getShallow("baseline", true) || labelLayout2.textVerticalAlign;
    var verticalAlignMin = retrieve2(itemLabelModel.getShallow("verticalAlignMinLabel", true), verticalAlign);
    var verticalAlignMax = retrieve2(itemLabelModel.getShallow("verticalAlignMaxLabel", true), verticalAlign);
    var z2 = 10 + (((_a2 = labelItemTick.time) === null || _a2 === void 0 ? void 0 : _a2.level) || 0);
    z2Min = Math.min(z2Min, z2);
    z2Max = Math.max(z2Max, z2);
    var textEl = new Text_default({
      // --- transform props start ---
      // All of the transform props MUST not be set here, but should be set in
      // `updateAxisLabelChangableProps`, because they may change in estimation,
      // and need to calculate based on global coord sys by `decomposeTransform`.
      x: 0,
      y: 0,
      rotation: 0,
      // --- transform props end ---
      silent: AxisBuilder.isLabelSilent(axisModel),
      z2,
      style: createTextStyle(itemLabelModel, {
        text: formattedLabel,
        align: index === 0 ? alignMin : index === labels.length - 1 ? alignMax : align,
        verticalAlign: index === 0 ? verticalAlignMin : index === labels.length - 1 ? verticalAlignMax : verticalAlign,
        fill: isFunction(textColor) ? textColor(
          // (1) In category axis with data zoom, tick is not the original
          // index of axis.data. So tick should not be exposed to user
          // in category axis.
          // (2) Compatible with previous version, which always use formatted label as
          // input. But in interval scale the formatted label is like '223,445', which
          // maked user replace ','. So we modify it to return original val but remain
          // it as 'string' to avoid error in replacing.
          axis.type === "category" ? rawLabel : axis.type === "value" ? tickValue + "" : tickValue,
          index
        ) : textColor
      })
    });
    textEl.anid = "label_" + tickValue;
    var inner25 = getLabelInner(textEl);
    inner25.labelInfo = labelItem;
    inner25.layoutRotation = labelLayout2.rotation;
    setTooltipConfig({
      el: textEl,
      componentModel: axisModel,
      itemName: formattedLabel,
      formatterParamsExtra: {
        isTruncated: function() {
          return textEl.isTruncated;
        },
        value: rawLabel,
        tickIndex: index
      }
    });
    if (triggerEvent) {
      var eventData = AxisBuilder.makeAxisEventDataBase(axisModel);
      eventData.targetType = "axisLabel";
      eventData.value = rawLabel;
      eventData.tickIndex = index;
      var labelItemTickBreak = labelItem.tick["break"];
      if (labelItemTickBreak) {
        var labelItemTickBreakParsedBreak = labelItemTickBreak.parsedBreak;
        eventData["break"] = {
          // type: labelItem.break.type,
          start: labelItemTickBreakParsedBreak.vmin,
          end: labelItemTickBreakParsedBreak.vmax
        };
      }
      if (axis.type === "category") {
        eventData.dataIndex = tickValue;
      }
      getECData(textEl).eventData = eventData;
      if (labelItemTickBreak) {
        addBreakEventHandler(axisModel, api, textEl, labelItemTickBreak);
      }
    }
    labelEls.push(textEl);
    labelGroup.add(textEl);
  });
  var labelLayoutList = map2(labelEls, function(label) {
    return {
      label,
      priority: getLabelInner(label).labelInfo.tick["break"] ? label.z2 + (z2Max - z2Min + 1) : label.z2,
      defaultAttr: {
        ignore: label.ignore
      }
    };
  });
  axisLabelBuildResultSet(local, labelLayoutList, labelGroup, axisLabelCreationCtx);
}
function axisLabelBuildResultExists(local) {
  return !!local.labelLayoutList;
}
function axisLabelBuildResultSet(local, labelLayoutList, labelGroup, axisLabelsCreationContext) {
  local.labelLayoutList = labelLayoutList;
  local.labelGroup = labelGroup;
  local.axisLabelsCreationContext = axisLabelsCreationContext;
}
function updateAxisLabelChangableProps(cfg, axisModel, labelLayoutList, transformGroup) {
  var labelMargin = axisModel.get(["axisLabel", "margin"]);
  each(labelLayoutList, function(layout3, idx) {
    var geometry = ensureLabelLayoutWithGeometry(layout3);
    if (!geometry) {
      return;
    }
    var labelEl = geometry.label;
    var inner25 = getLabelInner(labelEl);
    geometry.suggestIgnore = labelEl.ignore;
    labelEl.ignore = false;
    copyTransform(_tmpLayoutEl, _tmpLayoutElReset);
    var axis = axisModel.axis;
    _tmpLayoutEl.x = axis.dataToCoord(getTickValueOutermost(axis.scale, inner25.labelInfo.tick));
    _tmpLayoutEl.y = cfg.labelOffset + cfg.labelDirection * labelMargin;
    _tmpLayoutEl.rotation = inner25.layoutRotation;
    transformGroup.add(_tmpLayoutEl);
    _tmpLayoutEl.updateTransform();
    transformGroup.remove(_tmpLayoutEl);
    _tmpLayoutEl.decomposeTransform();
    copyTransform(labelEl, _tmpLayoutEl);
    labelEl.markRedraw();
    setLabelLayoutDirty(geometry, true);
    ensureLabelLayoutWithGeometry(geometry);
  });
}
var _tmpLayoutEl = new Rect_default();
var _tmpLayoutElReset = new Rect_default();
function hasAxisName(axisName) {
  return !!axisName;
}
function addBreakEventHandler(axisModel, api, textEl, visualBreak) {
  textEl.on("click", function(params) {
    var payload = {
      type: AXIS_BREAK_EXPAND_ACTION_TYPE,
      breaks: [{
        start: visualBreak.parsedBreak.breakOption.start,
        end: visualBreak.parsedBreak.breakOption.end
      }]
    };
    payload[axisModel.axis.dim + "AxisIndex"] = axisModel.componentIndex;
    api.dispatchAction(payload);
  });
}
function adjustBreakLabels(axisModel, axisRotation, labelLayoutList) {
  var scaleBreakHelper = getScaleBreakHelper();
  if (!scaleBreakHelper) {
    return;
  }
  var breakLabelIndexPairs = scaleBreakHelper.retrieveAxisBreakPairs(labelLayoutList, function(layoutInfo) {
    return layoutInfo && getLabelInner(layoutInfo.label).labelInfo.tick["break"];
  }, true);
  var moveOverlap = axisModel.get(["breakLabelLayout", "moveOverlap"], true);
  if (moveOverlap === true || moveOverlap === "auto") {
    each(breakLabelIndexPairs, function(idxPair) {
      getAxisBreakHelper().adjustBreakLabelPair(axisModel.axis.inverse, axisRotation, [ensureLabelLayoutWithGeometry(labelLayoutList[idxPair[0]]), ensureLabelLayoutWithGeometry(labelLayoutList[idxPair[1]])]);
    });
  }
}
var AxisBuilder_default = AxisBuilder;

// node_modules/echarts/lib/coord/cartesian/cartesianAxisHelper.js
function layout(rect, axisModel, opt) {
  opt = opt || {};
  var axis = axisModel.axis;
  var layout3 = {};
  var otherAxisOnZeroOf = axis.getAxesOnZeroOf()[0];
  var rawAxisPosition = axis.position;
  var axisPosition = otherAxisOnZeroOf ? "onZero" : rawAxisPosition;
  var axisDim = axis.dim;
  var rectBound = [rect.x, rect.x + rect.width, rect.y, rect.y + rect.height];
  var idx = {
    left: 0,
    right: 1,
    top: 0,
    bottom: 1,
    onZero: 2
  };
  var axisOffset = axisModel.get("offset") || 0;
  var posBound = axisDim === "x" ? [rectBound[2] - axisOffset, rectBound[3] + axisOffset] : [rectBound[0] - axisOffset, rectBound[1] + axisOffset];
  if (otherAxisOnZeroOf) {
    var onZeroCoord = otherAxisOnZeroOf.toGlobalCoord(otherAxisOnZeroOf.dataToCoord(0));
    posBound[idx.onZero] = Math.max(Math.min(onZeroCoord, posBound[1]), posBound[0]);
  }
  layout3.position = [axisDim === "y" ? posBound[idx[axisPosition]] : rectBound[0], axisDim === "x" ? posBound[idx[axisPosition]] : rectBound[3]];
  layout3.rotation = Math.PI / 2 * (axisDim === "x" ? 0 : 1);
  var dirMap = {
    top: -1,
    bottom: 1,
    left: -1,
    right: 1
  };
  layout3.labelDirection = layout3.tickDirection = layout3.nameDirection = dirMap[rawAxisPosition];
  layout3.labelOffset = otherAxisOnZeroOf ? posBound[idx[rawAxisPosition]] - posBound[idx.onZero] : 0;
  if (axisModel.get(["axisTick", "inside"])) {
    layout3.tickDirection = -layout3.tickDirection;
  }
  if (retrieve(opt.labelInside, axisModel.get(["axisLabel", "inside"]))) {
    layout3.labelDirection = -layout3.labelDirection;
  }
  var labelRotate = axisModel.get(["axisLabel", "rotate"]);
  layout3.labelRotate = axisPosition === "top" ? -labelRotate : labelRotate;
  layout3.z2 = 1;
  return layout3;
}
function isCartesian2DInjectedAsDataCoordSys(seriesModel) {
  return seriesModel.coordinateSystem && seriesModel.coordinateSystem.type === "cartesian2d";
}
function findAxisModels(seriesModel) {
  var axisModelMap = {
    xAxisModel: null,
    yAxisModel: null
  };
  each(axisModelMap, function(v, key2) {
    var axisType = key2.replace(/Model$/, "");
    var axisModel = seriesModel.getReferringComponents(axisType, SINGLE_REFERRING).models[0];
    if (true) {
      if (!axisModel) {
        throw new Error(axisType + ' "' + retrieve3(seriesModel.get(axisType + "Index"), seriesModel.get(axisType + "Id"), 0) + '" not found');
      }
    }
    axisModelMap[key2] = axisModel;
  });
  return axisModelMap;
}
function createCartesianAxisViewCommonPartBuilder(gridRect, cartesians, axisModel, api, ctx, defaultNameMoveOverlap) {
  var layoutResult = layout(gridRect, axisModel);
  var axisLineAutoShow = false;
  var axisTickAutoShow = false;
  for (var i2 = 0; i2 < cartesians.length; i2++) {
    if (isIntervalOrLogScale(cartesians[i2].getOtherAxis(axisModel.axis).scale)) {
      axisLineAutoShow = axisTickAutoShow = true;
      if (axisModel.axis.type === "category" && axisModel.axis.onBand) {
        axisTickAutoShow = false;
      }
    }
  }
  layoutResult.axisLineAutoShow = axisLineAutoShow;
  layoutResult.axisTickAutoShow = axisTickAutoShow;
  layoutResult.defaultNameMoveOverlap = defaultNameMoveOverlap;
  return new AxisBuilder_default(axisModel, api, layoutResult, ctx);
}
function updateCartesianAxisViewCommonPartBuilder(axisBuilder, gridRect, axisModel) {
  var newRaw = layout(gridRect, axisModel);
  if (true) {
    var oldRaw_1 = axisBuilder.__getRawCfg();
    each(keys(newRaw), function(prop) {
      if (prop !== "position" && prop !== "labelOffset") {
        assert(newRaw[prop] === oldRaw_1[prop]);
      }
    });
  }
  axisBuilder.updateCfg(newRaw);
}
function getCartesianAxisHashKey(axis) {
  return axis.dim + "_" + axis.index;
}

// node_modules/echarts/lib/coord/scaleRawExtentInfo.js
var scaleInner = makeInner();
var AXIS_EXTENT_INFO_BUILD_FROM_COORD_SYS_UPDATE = 1;
var AXIS_EXTENT_INFO_BUILD_FROM_DATA_ZOOM = 2;
var AXIS_EXTENT_INFO_BUILD_FROM_EMPTY = 3;
var ScaleRawExtentInfo = (
  /** @class */
  (function() {
    function ScaleRawExtentInfo2(scale3, model2, dataExtent, requireStartValue, requireContainShape) {
      var isOrdinal = isOrdinalScale(scale3);
      var axisDataLen = isOrdinal ? model2.getCategories().length : null;
      var categoryAxisModelDataIsEmptyArray;
      if (isOrdinal) {
        var axisModelDataArray = model2.getCategories(true);
        categoryAxisModelDataIsEmptyArray = axisModelDataArray && !axisModelDataArray.length;
      }
      var dataMM = dataExtent.slice();
      if (isIntervalScale(scale3) || isLogScale(scale3) || isTimeScale(scale3)) {
        unionExtentStartFromNumber(dataMM, parseAxisModelMinMax(scale3, model2.get("dataMin", true)));
        unionExtentEndFromNumber(dataMM, parseAxisModelMinMax(scale3, model2.get("dataMax", true)));
      }
      if (!extentHasValue(dataMM)) {
        dataMM[0] = dataMM[1] = NaN;
      }
      var noZoomEffMM = [];
      var fixMM = [false, false];
      var modelMinRaw = model2.get("min", true);
      if (modelMinRaw === "dataMin") {
        noZoomEffMM[0] = dataMM[0];
        fixMM[0] = true;
      } else {
        noZoomEffMM[0] = parseAxisModelMinMax(scale3, isFunction(modelMinRaw) ? modelMinRaw({
          min: dataMM[0],
          max: dataMM[1]
        }) : modelMinRaw);
        fixMM[0] = noZoomEffMM[0] != null;
      }
      var modelMaxRaw = model2.get("max", true);
      if (modelMaxRaw === "dataMax") {
        noZoomEffMM[1] = dataMM[1];
        fixMM[1] = true;
      } else {
        noZoomEffMM[1] = parseAxisModelMinMax(scale3, isFunction(modelMaxRaw) ? modelMaxRaw({
          min: dataMM[0],
          max: dataMM[1]
        }) : modelMaxRaw);
        fixMM[1] = noZoomEffMM[1] != null;
      }
      var boundaryGap = parseBoundaryGapOption(scale3, model2);
      var span = !isOrdinal ? dataMM[1] - dataMM[0] || Math.abs(dataMM[0]) : null;
      if (noZoomEffMM[0] == null) {
        noZoomEffMM[0] = isOrdinal ? categoryAxisModelDataIsEmptyArray ? dataMM[0] : axisDataLen ? 0 : NaN : dataMM[0] - boundaryGap[0] * span;
      }
      if (noZoomEffMM[1] == null) {
        noZoomEffMM[1] = isOrdinal ? categoryAxisModelDataIsEmptyArray ? dataMM[1] : axisDataLen ? axisDataLen - 1 : NaN : dataMM[1] + boundaryGap[1] * span;
      }
      !isValidNumberForExtent(noZoomEffMM[0]) && (noZoomEffMM[0] = NaN);
      !isValidNumberForExtent(noZoomEffMM[1]) && (noZoomEffMM[1] = NaN);
      var isBlank = categoryAxisModelDataIsEmptyArray || eqNaN(noZoomEffMM[0]) || eqNaN(noZoomEffMM[1]) || isOrdinal && !axisDataLen;
      var needIncludeZeroApplicable = isIntervalScale(scale3);
      var needIncludeZero = needIncludeZeroApplicable && model2.needIncludeZero && model2.needIncludeZero();
      if (needIncludeZero) {
        if (noZoomEffMM[0] > 0 && noZoomEffMM[1] > 0 && !fixMM[0]) {
          noZoomEffMM[0] = 0;
        }
        if (noZoomEffMM[0] < 0 && noZoomEffMM[1] < 0 && !fixMM[1]) {
          noZoomEffMM[1] = 0;
        }
      }
      var needToggleAxisInverse = false;
      if (noZoomEffMM[0] > noZoomEffMM[1]) {
        noZoomEffMM.reverse();
        needToggleAxisInverse = true;
      }
      var startValue = parseAxisModelMinMax(scale3, model2.get("startValue", true));
      var startValueSpecified = startValue != null;
      if (!isNullableNumberFinite(startValue) && requireStartValue) {
        startValue = scale3.getDefaultStartValue ? scale3.getDefaultStartValue() : 0;
      }
      if (isNullableNumberFinite(startValue) && (startValueSpecified || !needIncludeZeroApplicable || needIncludeZero)) {
        if (startValue < noZoomEffMM[0] && !fixMM[0]) {
          noZoomEffMM[0] = startValue;
          fixMM[0] = true;
        } else if (startValue > noZoomEffMM[1] && !fixMM[1]) {
          noZoomEffMM[1] = startValue;
          fixMM[1] = true;
        }
      }
      var internal = this._i = {
        scale: scale3,
        dataMM,
        noZoomEffMM,
        zoomMM: [],
        fixMM,
        zoomFixMM: [false, false],
        startValue,
        isBlank,
        incl0: needIncludeZero,
        tggAxInv: needToggleAxisInverse,
        ctnShp: requireContainShape
      };
      sanitizeExtent(internal, noZoomEffMM);
    }
    ScaleRawExtentInfo2.prototype.makeNoZoom = function() {
      return this._i.noZoomEffMM.slice();
    };
    ScaleRawExtentInfo2.prototype.makeFinal = function() {
      var internal = this._i;
      var zoomMM = internal.zoomMM;
      var noZoomEffMM = internal.noZoomEffMM;
      var zoomFixMM = internal.zoomFixMM;
      var fixMM = internal.fixMM;
      var result = {
        fixMM,
        zoomFixMM,
        isBlank: internal.isBlank,
        incl0: internal.incl0,
        tggAxInv: internal.tggAxInv,
        ctnShp: internal.ctnShp,
        effMM: noZoomEffMM.slice()
      };
      var effMM = result.effMM;
      if (zoomMM[0] != null) {
        effMM[0] = zoomMM[0];
        fixMM[0] = zoomFixMM[0] = true;
      }
      if (zoomMM[1] != null) {
        effMM[1] = zoomMM[1];
        fixMM[1] = zoomFixMM[1] = true;
      }
      sanitizeExtent(internal, effMM);
      return result;
    };
    ScaleRawExtentInfo2.prototype.makeRenderInfo = function() {
      return {
        startValue: this._i.startValue
      };
    };
    ScaleRawExtentInfo2.prototype.setZoomMM = function(idxMinMax, val) {
      this._i.zoomMM[idxMinMax] = val;
    };
    return ScaleRawExtentInfo2;
  })()
);
function sanitizeExtent(internal, mm) {
  var scale3 = internal.scale;
  var dataMM = internal.dataMM;
  if (scale3.sanitize) {
    mm[0] = scale3.sanitize(mm[0], dataMM);
    mm[1] = scale3.sanitize(mm[1], dataMM);
    ensureExtentAscSimply(mm);
  }
}
function parseAxisModelMinMax(scale3, minMax) {
  return minMax == null ? null : eqNaN(minMax) ? NaN : scale3.parse(minMax);
}
function parseBoundaryGapOption(scale3, model2) {
  var boundaryGapOptionArr;
  if (isOrdinalScale(scale3)) {
    boundaryGapOptionArr = [0, 0];
  } else {
    var boundaryGap = model2.get("boundaryGap");
    if (typeof boundaryGap === "boolean") {
      if (true) {
        if (boundaryGap === true) {
          console.warn('Boolean type for boundaryGap is only allowed for ordinal axis. Please use string in percentage instead, e.g., "20%". Currently, boundaryGap is set to 0.');
        }
      }
      boundaryGap = null;
    }
    boundaryGapOptionArr = isArray(boundaryGap) ? boundaryGap : [boundaryGap, boundaryGap];
  }
  return [parseBoundaryGapOptionItem(boundaryGapOptionArr[0]), parseBoundaryGapOptionItem(boundaryGapOptionArr[1])];
}
function parseBoundaryGapOptionItem(opt) {
  return parsePercent(typeof opt === "boolean" ? 0 : opt, 1) || 0;
}
function ensureScaleStore(axisLike) {
  var store = scaleInner(axisLike.scale);
  if (!store.extent) {
    store.extent = initExtentForUnion();
  }
  return store;
}
function scaleRawExtentInfoEnableBoxCoordSysUsage(axisLike, coordSysDimIdxMap) {
  ensureScaleStore(axisLike).dimIdxInCoord = coordSysDimIdxMap.get(axisLike.dim);
}
function scaleRawExtentInfoCreate(axis, from) {
  var scale3 = axis.scale;
  var model2 = axis.model;
  var axisDim = axis.dim;
  if (true) {
    assert(scale3 && model2 && axisDim);
  }
  if (scale3.rawExtentInfo) {
    if (true) {
      assert(scale3.rawExtentInfo.from !== from || from === AXIS_EXTENT_INFO_BUILD_FROM_DATA_ZOOM);
    }
    return;
  }
  scaleRawExtentInfoCreateDeal(scale3, axis, axisDim, model2, from);
}
function scaleRawExtentInfoCreateDeal(scale3, axis, axisDim, model2, from) {
  var scaleStore = ensureScaleStore(axis);
  var extent = scaleStore.extent;
  var requireStartValue = false;
  eachSeriesOnAxis(axis, function(seriesModel) {
    if (seriesModel.boxCoordinateSystem) {
      var coord = getCoordForCoordSysUsageKindBox(seriesModel).coord;
      var dimIdx = scaleStore.dimIdxInCoord;
      if (!(dimIdx >= 0)) {
        if (true) {
          error('Property "series.coord" is not supported on axis ' + seriesModel.boxCoordinateSystem.type + ".");
        }
      } else if (isArray(coord)) {
        var coordItem = coord[dimIdx];
        if (coordItem != null && !isArray(coordItem)) {
          unionExtentFromNumber(extent, scale3.parse(coordItem));
        }
      }
    } else if (seriesModel.coordinateSystem) {
      var data_1 = seriesModel.getData();
      if (data_1) {
        var filter_1 = scale3.getFilter ? scale3.getFilter() : null;
        each(getDataDimensionsOnAxis(data_1, axisDim), function(dim) {
          unionExtentFromExtent(extent, data_1.getApproximateExtent(dim, filter_1));
        });
      }
      if (seriesModel.__requireStartValue && seriesModel.__requireStartValue(axis)) {
        requireStartValue = true;
      }
    }
  });
  var requireContainShape = determineRequireContainShape(scale3, axis, model2);
  var rawExtentInfo = new ScaleRawExtentInfo(scale3, model2, extent, requireStartValue, requireContainShape);
  injectScaleRawExtentInfo(scale3, rawExtentInfo, from);
  scaleStore.extent = null;
}
function scaleRawExtentInfoBuildDefault(axisLike, dataExtent) {
  var scale3 = axisLike.scale;
  if (true) {
    assert(!scale3.rawExtentInfo);
  }
  injectScaleRawExtentInfo(scale3, new ScaleRawExtentInfo(scale3, axisLike.model, dataExtent, false, false), AXIS_EXTENT_INFO_BUILD_FROM_EMPTY);
}
function injectScaleRawExtentInfo(scale3, scaleRawExtentInfo, from) {
  scale3.rawExtentInfo = scaleRawExtentInfo;
  scaleRawExtentInfo.from = from;
}
function registerAxisContainShapeHandler(axisStatKey, handler) {
  if (true) {
    assert(!axisContainShapeHandlerMap.get(axisStatKey));
  }
  axisContainShapeHandlerMap.set(axisStatKey, handler);
}
var axisContainShapeHandlerMap = createHashMap();
function adoptScaleRawExtentInfoAndPrepare(scale3, model2, ecModel, axis, externalDataExtent) {
  if (true) {
    assert(!externalDataExtent || !scale3.rawExtentInfo);
  }
  if (!scale3.rawExtentInfo) {
    scaleRawExtentInfoBuildDefault({
      scale: scale3,
      model: model2
    }, externalDataExtent || initExtentForUnion());
  }
  var rawExtentResult = scale3.rawExtentInfo.makeFinal();
  var effectiveMinMax = rawExtentResult.effMM;
  scale3.setExtent(effectiveMinMax[0], effectiveMinMax[1]);
  scale3.setBlank(rawExtentResult.isBlank);
  if (axis && rawExtentResult.tggAxInv && ecModel && !ecModel.get("legacyMinMaxDontInverseAxis")) {
    axis.inverse = !axis.inverse;
  }
  return rawExtentResult;
}
function determineRequireContainShape(scale3, axis, model2) {
  var onBand = isAxisOnBand(scale3, model2);
  var modelContainShape = model2.get("containShape", true);
  if (modelContainShape == null && !onBand) {
    modelContainShape = true;
  }
  if (!modelContainShape) {
    return false;
  }
  var requireContainShape = false;
  eachKeyOnAxis(axis, function(axisStatKey) {
    requireContainShape = !!axisContainShapeHandlerMap.get(axisStatKey) || requireContainShape;
  });
  return requireContainShape;
}
function adoptScaleExtentKindMapping(axis, scale3, rawExtentResult, ecModel) {
  if (!rawExtentResult.ctnShp) {
    return;
  }
  var linearSupplement;
  eachKeyOnAxis(axis, function(axisStatKey) {
    var handler = axisContainShapeHandlerMap.get(axisStatKey);
    if (handler) {
      var singleLinearSupplement = handler(axis, ecModel);
      if (singleLinearSupplement) {
        linearSupplement = linearSupplement || [0, 0];
        unionExtentStartFromNumber(linearSupplement, singleLinearSupplement[0]);
        unionExtentEndFromNumber(linearSupplement, singleLinearSupplement[1]);
        discourageOnAxisZero(axis);
      }
    }
  });
  if (!linearSupplement) {
    return;
  }
  var scaleExtent = scale3.getExtent();
  if (isOrdinalScale(scale3)) {
    if (!axis.onBand) {
      scale3.setExtent2(SCALE_EXTENT_KIND_MAPPING, mathMin4(scaleExtent[0], scaleExtent[0] + linearSupplement[0]), mathMax4(scaleExtent[1], scaleExtent[1] + linearSupplement[1]));
    }
  } else {
    var scaleExtentExpanded = scaleExtent.slice();
    if (!rawExtentResult.zoomFixMM[0]) {
      scaleExtentExpanded[0] = mathMin4(scaleExtentExpanded[0], scale3.transformOut(scale3.transformIn(scaleExtentExpanded[0], null) + linearSupplement[0], null));
    }
    if (!rawExtentResult.zoomFixMM[1]) {
      scaleExtentExpanded[1] = mathMax4(scaleExtentExpanded[1], scale3.transformOut(scale3.transformIn(scaleExtentExpanded[1], null) + linearSupplement[1], null));
    }
    if (scaleExtentExpanded[0] < scaleExtent[0] || scaleExtentExpanded[1] > scaleExtent[1]) {
      scale3.setExtent2(SCALE_EXTENT_KIND_MAPPING, scaleExtentExpanded[0], scaleExtentExpanded[1]);
    }
  }
}

// node_modules/echarts/lib/coord/axisStatisticsMetricsImpl.js
function registerMetricImplLiPosMinGap() {
  registerMetricImpl("liPosMinGap", metricLiPosMinGapImpl);
}
function metricLiPosMinGapImpl(ecModel, perKeyPerAxis, ecPreparePerKeyPerAxis) {
  var newSerUids = createHashMap();
  var ecPrepareSerUids = ecPreparePerKeyPerAxis.serUids;
  var ecPrepareLiPosMinGap = ecPreparePerKeyPerAxis.liPosMinGap;
  var ecPrepareCacheMiss;
  var axis = perKeyPerAxis.axis;
  var scale3 = axis.scale;
  var needTransform = scale3.needTransform();
  var filter3 = scale3.getFilter ? scale3.getFilter() : null;
  var filterParsed = parseSanitizationFilter(filter3);
  function eachSeries(cb) {
    eachSeriesDealForAxisStat(ecModel, perKeyPerAxis.sers, function(seriesModel) {
      var rawData = seriesModel.getRawData();
      var dimStoreIdx = rawData.getDimensionIndex(rawData.mapDimension(axis.dim));
      if (dimStoreIdx >= 0) {
        cb(dimStoreIdx, seriesModel, rawData.getStore());
      }
    });
  }
  var bufferCapacity = 0;
  eachSeries(function(dimStoreIdx, seriesModel, rawDataStore) {
    newSerUids.set(seriesModel.uid, 1);
    if (!ecPrepareSerUids || !ecPrepareSerUids.hasKey(seriesModel.uid)) {
      ecPrepareCacheMiss = true;
    }
    bufferCapacity += rawDataStore.count();
  });
  if (!ecPrepareSerUids || ecPrepareSerUids.keys().length !== newSerUids.keys().length) {
    ecPrepareCacheMiss = true;
  }
  if (!ecPrepareCacheMiss && ecPrepareLiPosMinGap != null) {
    perKeyPerAxis.liPosMinGap = ecPrepareLiPosMinGap;
    return;
  }
  tryEnsureTypedArray(tmpValueBuffer, bufferCapacity);
  var writeIdx = 0;
  eachSeries(function(dimStoreIdx, seriesModel, store) {
    for (var i2 = 0, cnt = store.count(); i2 < cnt; ++i2) {
      var val = store.get(dimStoreIdx, i2);
      if (isFinite(val) && (!filter3 || passesSanitizationFilter(filterParsed, val))) {
        if (needTransform) {
          val = scale3.transformIn(val, null);
        }
        tmpValueBuffer.arr[writeIdx++] = val;
      }
    }
  });
  var tmpValueBufferView = tmpValueBuffer.typed ? tmpValueBuffer.arr.subarray(0, writeIdx) : (tmpValueBuffer.arr.length = writeIdx, tmpValueBuffer.arr);
  if (tmpValueBuffer.typed) {
    tmpValueBufferView.sort();
  } else {
    asc(tmpValueBufferView);
  }
  var min3 = Infinity;
  for (var j = 1; j < writeIdx; ++j) {
    var delta = tmpValueBufferView[j] - tmpValueBufferView[j - 1];
    if (
      // - Different series normally have the same values (e.g., barA, barB, barC),
      //   which should be ignored.
      // - A single series with multiple same values is often not meaningful to
      //   create `bandWidth`, so it is also ignored.
      delta > 0 && delta < min3
    ) {
      min3 = delta;
    }
  }
  ecPreparePerKeyPerAxis.liPosMinGap = perKeyPerAxis.liPosMinGap = isNullableNumberFinite(min3) ? min3 : writeIdx > 0 ? LINEAR_POSITIVE_MIN_GAP_SINGLE_VALID_VALUE : LINEAR_POSITIVE_MIN_GAP_NO_VALID_VALUE;
  ecPreparePerKeyPerAxis.serUids = newSerUids;
}
var tmpValueBuffer = tryEnsureTypedArray(
  {
    ctor: Float64ArrayCtor
  },
  50
  // An arbitrary initial capability.
);

// node_modules/echarts/lib/chart/helper/axisSnippets.js
function createBandWidthBasedAxisContainShapeHandler(axisStatKey) {
  return function(axis, ecModel) {
    var bandWidthResult = calcBandWidth(axis, {
      fromStat: {
        key: axisStatKey
      }
    });
    if (isNullableNumberFinite(bandWidthResult.w2)) {
      return [-bandWidthResult.w2 / 2, bandWidthResult.w2 / 2];
    }
  };
}
function makeAxisStatKey(seriesType2) {
  return seriesType2 + AXIS_STAT_KEY_DELIMITER;
}
function makeAxisStatKey2(seriesType2, coordSysType) {
  return seriesType2 + AXIS_STAT_KEY_DELIMITER + coordSysType;
}
function createMetricsNonOrdinalLinearPositiveMinGap(axis) {
  registerMetricImplLiPosMinGap();
  return {
    // non-category scale do not use `liPosMinGap` to calculate `bandWidth`.
    liPosMinGap: !isOrdinalScale(axis.scale)
  };
}

// node_modules/echarts/lib/layout/barCommon.js
var SERIES_TYPE_BAR = "bar";
var SERIES_TYPE_PICTORIAL_BAR = "pictorialBar";
function requireAxisStatisticsForBaseBar(registers, axisStatKey, seriesType2, coordSysType) {
  requireAxisStatistics(registers, {
    key: axisStatKey,
    seriesType: seriesType2,
    coordSysType,
    getMetrics: createMetricsNonOrdinalLinearPositiveMinGap
  });
}
function getStartValue(baseAxis) {
  var val = baseAxis.scale.rawExtentInfo.makeRenderInfo().startValue;
  if (true) {
    assert(isNullableNumberFinite(val));
  }
  return val;
}

// node_modules/echarts/lib/coord/cartesian/GridModel.js
var OUTER_BOUNDS_DEFAULT = {
  left: 0,
  right: 0,
  top: 0,
  bottom: 0
};
var OUTER_BOUNDS_CLAMP_DEFAULT = ["25%", "25%"];
var COORD_SYS_TYPE_CARTESIAN_2D = "cartesian2d";
var GridModel = (
  /** @class */
  (function(_super) {
    __extends(GridModel2, _super);
    function GridModel2() {
      return _super !== null && _super.apply(this, arguments) || this;
    }
    GridModel2.prototype.mergeDefaultAndTheme = function(option, ecModel) {
      var outerBoundsCp = getLayoutParams(option.outerBounds);
      _super.prototype.mergeDefaultAndTheme.apply(this, arguments);
      if (outerBoundsCp && option.outerBounds) {
        mergeLayoutParam(option.outerBounds, outerBoundsCp);
      }
    };
    GridModel2.prototype.mergeOption = function(newOption, ecModel) {
      _super.prototype.mergeOption.apply(this, arguments);
      if (this.option.outerBounds && newOption.outerBounds) {
        mergeLayoutParam(this.option.outerBounds, newOption.outerBounds);
      }
    };
    GridModel2.type = "grid";
    GridModel2.dependencies = ["xAxis", "yAxis"];
    GridModel2.layoutMode = "box";
    GridModel2.defaultOption = {
      show: false,
      // zlevel: 0,
      z: 0,
      left: "15%",
      top: 65,
      right: "10%",
      bottom: 80,
      // If grid size contain label
      containLabel: false,
      outerBoundsMode: "auto",
      outerBounds: OUTER_BOUNDS_DEFAULT,
      outerBoundsContain: "all",
      outerBoundsClampWidth: OUTER_BOUNDS_CLAMP_DEFAULT[0],
      outerBoundsClampHeight: OUTER_BOUNDS_CLAMP_DEFAULT[1],
      // width: {totalWidth} - left - right,
      // height: {totalHeight} - top - bottom,
      backgroundColor: tokens_default.color.transparent,
      borderWidth: 1,
      borderColor: tokens_default.color.neutral30
    };
    return GridModel2;
  })(Component_default)
);
var GridModel_default = GridModel;

// node_modules/echarts/lib/layout/barGrid.js
var callOnlyOnce2 = makeCallOnlyOnce();
var STACK_PREFIX = "__ec_stack_";
function getSeriesStackId(seriesModel) {
  return seriesModel.get("stack") || STACK_PREFIX + seriesModel.seriesIndex;
}
function computeBarLayoutForCustomSeries(opt) {
  if (!isOrdinalScale(opt.axis.scale)) {
    return;
  }
  var bandWidthResult = calcBandWidth(opt.axis);
  var params = [];
  for (var i2 = 0; i2 < opt.count || 0; i2++) {
    params.push(defaults({
      stackId: STACK_PREFIX + i2
    }, opt));
  }
  var widthAndOffsets = calcBarWidthAndOffset({
    bandWidthResult,
    seriesInfo: params
  });
  var result = [];
  for (var i2 = 0; i2 < opt.count; i2++) {
    var item = widthAndOffsets[STACK_PREFIX + i2];
    item.offsetCenter = item.offset + item.width / 2;
    result.push(item);
  }
  return result;
}
function makeColumnLayoutOnAxisReal(baseAxis, seriesType2) {
  var seriesInfoListOnAxis = createLayoutInfoListOnAxis(baseAxis, seriesType2);
  seriesInfoListOnAxis.columnMap = calcBarWidthAndOffset(seriesInfoListOnAxis);
  return seriesInfoListOnAxis;
}
function createLayoutInfoListOnAxis(baseAxis, seriesType2) {
  var axisStatKey = makeAxisStatKey2(seriesType2, COORD_SYS_TYPE_CARTESIAN_2D);
  var seriesInfoOnAxis = [];
  var bandWidthResult = calcBandWidth(baseAxis, {
    fromStat: {
      key: axisStatKey
    },
    min: 1
  });
  eachSeriesOnAxisOnKey(baseAxis, axisStatKey, function(seriesModel) {
    seriesInfoOnAxis.push({
      barWidth: parsePercent2(seriesModel.get("barWidth"), bandWidthResult.w),
      barMaxWidth: parsePercent2(seriesModel.get("barMaxWidth"), bandWidthResult.w),
      barMinWidth: parsePercent2(
        // barMinWidth by default is 0.5 / 1 in cartesian. Because in value axis,
        // the auto-calculated bar width might be less than 0.5 / 1.
        seriesModel.get("barMinWidth") || (isInLargeMode(seriesModel) ? 0.5 : 1),
        bandWidthResult.w
      ),
      barGap: seriesModel.get("barGap"),
      barCategoryGap: seriesModel.get("barCategoryGap"),
      defaultBarGap: seriesModel.get("defaultBarGap"),
      stackId: getSeriesStackId(seriesModel)
    });
  });
  return {
    bandWidthResult,
    seriesInfo: seriesInfoOnAxis
  };
}
function calcBarWidthAndOffset(seriesInfoOnAxis) {
  var bandWidth = seriesInfoOnAxis.bandWidthResult.w;
  var remainedWidth = bandWidth;
  var autoWidthCount = 0;
  var barCategoryGapOption;
  var barGapOption;
  var stackIdList = [];
  var stackMap = {};
  each(seriesInfoOnAxis.seriesInfo, function(seriesInfo, idx) {
    if (!idx) {
      barGapOption = seriesInfo.defaultBarGap || 0;
    }
    var stackId = seriesInfo.stackId;
    if (!hasOwn(stackMap, stackId)) {
      autoWidthCount++;
    }
    var stackItem = stackMap[stackId];
    if (!stackItem) {
      stackItem = stackMap[stackId] = {
        width: 0,
        maxWidth: 0
      };
      stackIdList.push(stackId);
    }
    var barWidth = seriesInfo.barWidth;
    if (barWidth && !stackItem.width) {
      stackItem.width = barWidth;
      barWidth = mathMin4(remainedWidth, barWidth);
      remainedWidth -= barWidth;
    }
    var barMaxWidth = seriesInfo.barMaxWidth;
    barMaxWidth && (stackItem.maxWidth = barMaxWidth);
    var barMinWidth = seriesInfo.barMinWidth;
    barMinWidth && (stackItem.minWidth = barMinWidth);
    var barGap = seriesInfo.barGap;
    barGap != null && (barGapOption = barGap);
    var barCategoryGap = seriesInfo.barCategoryGap;
    barCategoryGap != null && (barCategoryGapOption = barCategoryGap);
  });
  if (barCategoryGapOption == null) {
    barCategoryGapOption = mathMax4(35 - stackIdList.length * 4, 15) + "%";
  }
  var barCategoryGapNum = parsePercent2(barCategoryGapOption, bandWidth);
  var barGapPercent = parsePercent2(barGapOption, 1);
  var autoWidth = (remainedWidth - barCategoryGapNum) / (autoWidthCount + (autoWidthCount - 1) * barGapPercent);
  autoWidth = mathMax4(autoWidth, 0);
  each(stackIdList, function(stackId) {
    var column = stackMap[stackId];
    var maxWidth = column.maxWidth;
    var minWidth = column.minWidth;
    if (!column.width) {
      var finalWidth = autoWidth;
      if (maxWidth && maxWidth < finalWidth) {
        finalWidth = mathMin4(maxWidth, remainedWidth);
      }
      if (minWidth && minWidth > finalWidth) {
        finalWidth = minWidth;
      }
      if (finalWidth !== autoWidth) {
        column.width = finalWidth;
        remainedWidth -= finalWidth + barGapPercent * finalWidth;
        autoWidthCount--;
      }
    } else {
      var finalWidth = column.width;
      if (maxWidth) {
        finalWidth = mathMin4(finalWidth, maxWidth);
      }
      if (minWidth) {
        finalWidth = mathMax4(finalWidth, minWidth);
      }
      column.width = finalWidth;
      remainedWidth -= finalWidth + barGapPercent * finalWidth;
      autoWidthCount--;
    }
  });
  autoWidth = (remainedWidth - barCategoryGapNum) / (autoWidthCount + (autoWidthCount - 1) * barGapPercent);
  autoWidth = mathMax4(autoWidth, 0);
  var widthSum = 0;
  var lastColumn;
  each(stackIdList, function(stackId) {
    var column = stackMap[stackId];
    if (!column.width) {
      column.width = autoWidth;
    }
    lastColumn = column;
    widthSum += column.width * (1 + barGapPercent);
  });
  if (lastColumn) {
    widthSum -= lastColumn.width * barGapPercent;
  }
  var result = {};
  var offset = -widthSum / 2;
  each(stackIdList, function(stackId) {
    var column = stackMap[stackId];
    result[stackId] = result[stackId] || {
      bandWidth,
      offset,
      width: column.width
    };
    offset += column.width * (1 + barGapPercent);
  });
  return result;
}
function createCrossSeriesLayoutHandler(seriesType2) {
  return {
    seriesType: seriesType2,
    overallReset: function(ecModel) {
      var axisStatKey = makeAxisStatKey2(seriesType2, COORD_SYS_TYPE_CARTESIAN_2D);
      eachAxisOnKey(ecModel, axisStatKey, function(axis) {
        if (true) {
          assert(axis instanceof Axis2D_default);
        }
        var columnLayout = makeColumnLayoutOnAxisReal(axis, seriesType2);
        eachSeriesOnAxisOnKey(axis, axisStatKey, function(seriesModel) {
          var columnLayoutInfo = columnLayout.columnMap[getSeriesStackId(seriesModel)];
          seriesModel.getData().setLayout({
            bandWidth: columnLayoutInfo.bandWidth,
            offset: columnLayoutInfo.offset,
            size: columnLayoutInfo.width
          });
        });
      });
    }
  };
}
function createProgressiveLayout(seriesType2) {
  return {
    seriesType: seriesType2,
    plan: createRenderPlanner(),
    reset: function(seriesModel) {
      if (!isCartesian2DInjectedAsDataCoordSys(seriesModel)) {
        return;
      }
      var data = seriesModel.getData();
      var cartesian = seriesModel.coordinateSystem;
      var baseAxis = cartesian.getBaseAxis();
      var valueAxis2 = cartesian.getOtherAxis(baseAxis);
      var valueDimIdx = data.getDimensionIndex(data.mapDimension(valueAxis2.dim));
      var baseDimIdx = data.getDimensionIndex(data.mapDimension(baseAxis.dim));
      var drawBackground = seriesModel.get("showBackground", true);
      var valueDim = data.mapDimension(valueAxis2.dim);
      var stackResultDim = data.getCalculationInfo("stackResultDimension");
      var stacked = isDimensionStacked(data, valueDim) && !!data.getCalculationInfo("stackedOnSeries");
      var isValueAxisH = valueAxis2.isHorizontal();
      var valueAxisStart = valueAxis2.toGlobalCoord(valueAxis2.dataToCoord(getStartValue(valueAxis2)));
      var isLarge = isInLargeMode(seriesModel);
      var barMinHeight = seriesModel.get("barMinHeight") || 0;
      var stackedDimIdx = stackResultDim && data.getDimensionIndex(stackResultDim);
      var columnWidth = data.getLayout("size");
      var columnOffset = data.getLayout("offset");
      return {
        progress: function(params, data2) {
          var count2 = params.count;
          var largePoints = isLarge && createFloat32Array(count2 * 3);
          var largeBackgroundPoints = isLarge && drawBackground && createFloat32Array(count2 * 3);
          var largeDataIndices = isLarge && createFloat32Array(count2);
          var coordLayout = cartesian.master.getRect();
          var bgSize = isValueAxisH ? coordLayout.width : coordLayout.height;
          var dataIndex;
          var store = data2.getStore();
          var idxOffset = 0;
          while ((dataIndex = params.next()) != null) {
            var value = store.get(stacked ? stackedDimIdx : valueDimIdx, dataIndex);
            var baseValue = store.get(baseDimIdx, dataIndex);
            var baseCoord = valueAxisStart;
            var stackStartValue = void 0;
            if (stacked) {
              stackStartValue = +value - store.get(valueDimIdx, dataIndex);
            }
            var x = void 0;
            var y = void 0;
            var width = void 0;
            var height = void 0;
            if (isValueAxisH) {
              var coord = cartesian.dataToPoint([value, baseValue]);
              if (stacked) {
                baseCoord = cartesian.dataToPoint([stackStartValue, baseValue])[0];
              }
              x = baseCoord;
              y = coord[1] + columnOffset;
              width = coord[0] - baseCoord;
              height = columnWidth;
              if (mathAbs3(width) < barMinHeight) {
                width = (width < 0 ? -1 : 1) * barMinHeight;
              }
            } else {
              var coord = cartesian.dataToPoint([baseValue, value]);
              if (stacked) {
                baseCoord = cartesian.dataToPoint([baseValue, stackStartValue])[1];
              }
              x = coord[0] + columnOffset;
              y = baseCoord;
              width = columnWidth;
              height = coord[1] - baseCoord;
              if (mathAbs3(height) < barMinHeight) {
                height = (height <= 0 ? -1 : 1) * barMinHeight;
              }
            }
            if (!isLarge) {
              data2.setItemLayout(dataIndex, {
                x,
                y,
                width,
                height
              });
            } else {
              largePoints[idxOffset] = x;
              largePoints[idxOffset + 1] = y;
              largePoints[idxOffset + 2] = isValueAxisH ? width : height;
              if (largeBackgroundPoints) {
                largeBackgroundPoints[idxOffset] = isValueAxisH ? coordLayout.x : x;
                largeBackgroundPoints[idxOffset + 1] = isValueAxisH ? y : coordLayout.y;
                largeBackgroundPoints[idxOffset + 2] = bgSize;
              }
              largeDataIndices[dataIndex] = dataIndex;
            }
            idxOffset += 3;
          }
          if (isLarge) {
            data2.setLayout({
              largePoints,
              largeDataIndices,
              largeBackgroundPoints,
              valueAxisHorizontal: isValueAxisH
            });
          }
        }
      };
    }
  };
}
function isInLargeMode(seriesModel) {
  return seriesModel.pipelineContext && seriesModel.pipelineContext.large;
}
function barGridCreateAxisContainShapeHandler(seriesType2) {
  return createBandWidthBasedAxisContainShapeHandler(makeAxisStatKey2(seriesType2, COORD_SYS_TYPE_CARTESIAN_2D));
}
function registerBarGridAxisHandlers(registers) {
  callOnlyOnce2(registers, function() {
    function register2(seriesType2) {
      var axisStatKey = makeAxisStatKey2(seriesType2, COORD_SYS_TYPE_CARTESIAN_2D);
      requireAxisStatisticsForBaseBar(registers, axisStatKey, seriesType2, COORD_SYS_TYPE_CARTESIAN_2D);
      registerAxisContainShapeHandler(axisStatKey, barGridCreateAxisContainShapeHandler(seriesType2));
    }
    register2("bar");
    register2("pictorialBar");
  });
}

// node_modules/echarts/lib/chart/bar/BaseBarSeries.js
var BaseBarSeriesModel = (
  /** @class */
  (function(_super) {
    __extends(BaseBarSeriesModel2, _super);
    function BaseBarSeriesModel2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = BaseBarSeriesModel2.type;
      return _this;
    }
    BaseBarSeriesModel2.prototype.getInitialData = function(option, ecModel) {
      return createSeriesData_default(null, this, {
        useEncodeDefaulter: true
      });
    };
    BaseBarSeriesModel2.prototype.getMarkerPosition = function(value, dims, startingAtTick) {
      var coordSys = this.coordinateSystem;
      if (coordSys && coordSys.clampData) {
        var clampData_1 = coordSys.clampData(value);
        var pt_1 = coordSys.dataToPoint(clampData_1);
        if (startingAtTick) {
          each(coordSys.getAxes(), function(axis, idx) {
            if (axis.type === "category" && dims != null) {
              var tickCoords = axis.getTicksCoords();
              var alignTicksWithLabel = axis.getTickModel().get("alignWithLabel");
              var targetTickId = clampData_1[idx];
              var isEnd = dims[idx] === "x1" || dims[idx] === "y1";
              if (isEnd && !alignTicksWithLabel) {
                targetTickId += 1;
              }
              if (tickCoords.length < 2) {
                return;
              } else if (tickCoords.length === 2) {
                pt_1[idx] = axis.toGlobalCoord(axis.getExtent()[isEnd ? 1 : 0]);
                return;
              }
              var leftCoord = void 0;
              var coord = void 0;
              var stepTickValue = 1;
              for (var i2 = 0; i2 < tickCoords.length; i2++) {
                var tickCoord = tickCoords[i2].coord;
                var tickValue = i2 === tickCoords.length - 1 ? tickCoords[i2 - 1].tickValue + stepTickValue : tickCoords[i2].tickValue;
                if (tickValue === targetTickId) {
                  coord = tickCoord;
                  break;
                } else if (tickValue < targetTickId) {
                  leftCoord = tickCoord;
                } else if (leftCoord != null && tickValue > targetTickId) {
                  coord = (tickCoord + leftCoord) / 2;
                  break;
                }
                if (i2 === 1) {
                  stepTickValue = tickValue - tickCoords[0].tickValue;
                }
              }
              if (coord == null) {
                if (!leftCoord) {
                  coord = tickCoords[0].coord;
                } else if (leftCoord) {
                  coord = tickCoords[tickCoords.length - 1].coord;
                }
              }
              pt_1[idx] = axis.toGlobalCoord(coord);
            }
          });
        } else {
          var data = this.getData();
          var offset = data.getLayout("offset");
          var size = data.getLayout("size");
          var offsetIndex = coordSys.getBaseAxis().isHorizontal() ? 0 : 1;
          pt_1[offsetIndex] += offset + size / 2;
        }
        return pt_1;
      }
      return [NaN, NaN];
    };
    BaseBarSeriesModel2.prototype.__requireStartValue = function(axis) {
      return this.getBaseAxis() !== axis;
    };
    BaseBarSeriesModel2.type = "series.__base_bar__";
    BaseBarSeriesModel2.defaultOption = {
      // zlevel: 0,
      z: 2,
      coordinateSystem: "cartesian2d",
      legendHoverLink: true,
      // stack: null
      // Cartesian coordinate system
      // xAxisIndex: 0,
      // yAxisIndex: 0,
      barMinHeight: 0,
      barMinAngle: 0,
      // cursor: null,
      large: false,
      largeThreshold: 400,
      progressive: 3e3,
      progressiveChunkMode: "mod",
      defaultBarGap: "10%"
    };
    return BaseBarSeriesModel2;
  })(Series_default)
);
Series_default.registerClass(BaseBarSeriesModel);
var BaseBarSeries_default = BaseBarSeriesModel;

// node_modules/echarts/lib/chart/bar/BarSeries.js
var BarSeriesModel = (
  /** @class */
  (function(_super) {
    __extends(BarSeriesModel2, _super);
    function BarSeriesModel2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = BarSeriesModel2.type;
      return _this;
    }
    BarSeriesModel2.prototype.getInitialData = function() {
      return createSeriesData_default(null, this, {
        useEncodeDefaulter: true,
        createInvertedIndices: !!this.get("realtimeSort", true) || null
      });
    };
    BarSeriesModel2.prototype.getProgressive = function() {
      return this.get("large") ? this.get("progressive") : false;
    };
    BarSeriesModel2.prototype.__preparePipelineContext = function(view, pipeline) {
      var context = preparePipelineContext(this, view, pipeline);
      if (context.progressiveRender) {
        context.large = true;
      }
      return context;
    };
    BarSeriesModel2.prototype.brushSelector = function(dataIndex, data, selectors) {
      return selectors.rect(data.getItemLayout(dataIndex));
    };
    BarSeriesModel2.type = "series." + SERIES_TYPE_BAR;
    BarSeriesModel2.dependencies = ["grid", "polar"];
    BarSeriesModel2.defaultOption = inheritDefaultOption(BaseBarSeries_default.defaultOption, {
      // If clipped
      // Only available on cartesian2d
      clip: true,
      roundCap: false,
      showBackground: false,
      backgroundStyle: {
        color: "rgba(180, 180, 180, 0.2)",
        borderColor: null,
        borderWidth: 0,
        borderType: "solid",
        borderRadius: 0,
        shadowBlur: 0,
        shadowColor: null,
        shadowOffsetX: 0,
        shadowOffsetY: 0,
        opacity: 1
      },
      select: {
        itemStyle: {
          borderColor: tokens_default.color.primary,
          borderWidth: 2
        }
      },
      realtimeSort: false
    });
    return BarSeriesModel2;
  })(BaseBarSeries_default)
);
var BarSeries_default = BarSeriesModel;

// node_modules/echarts/lib/util/throttle.js
var ORIGIN_METHOD = "\0__throttleOriginMethod";
var RATE = "\0__throttleRate";
var THROTTLE_TYPE = "\0__throttleType";
function throttle(fn, delay2, debounce) {
  var currCall;
  var lastCall = 0;
  var lastExec = 0;
  var timer2 = null;
  var diff;
  var scope;
  var args;
  var debounceNextCall;
  delay2 = delay2 || 0;
  function exec() {
    lastExec = (/* @__PURE__ */ new Date()).getTime();
    timer2 = null;
    fn.apply(scope, args || []);
  }
  var cb = function() {
    var cbArgs = [];
    for (var _i = 0; _i < arguments.length; _i++) {
      cbArgs[_i] = arguments[_i];
    }
    currCall = (/* @__PURE__ */ new Date()).getTime();
    scope = this;
    args = cbArgs;
    var thisDelay = debounceNextCall || delay2;
    var thisDebounce = debounceNextCall || debounce;
    debounceNextCall = null;
    diff = currCall - (thisDebounce ? lastCall : lastExec) - thisDelay;
    clearTimeout(timer2);
    if (thisDebounce) {
      timer2 = setTimeout(exec, thisDelay);
    } else {
      if (diff >= 0) {
        exec();
      } else {
        timer2 = setTimeout(exec, -diff);
      }
    }
    lastCall = currCall;
  };
  cb.clear = function() {
    if (timer2) {
      clearTimeout(timer2);
      timer2 = null;
    }
  };
  cb.debounceNextCall = function(debounceDelay) {
    debounceNextCall = debounceDelay;
  };
  return cb;
}
function createOrUpdate(obj, fnAttr, rate, throttleType) {
  var fn = obj[fnAttr];
  if (!fn) {
    return;
  }
  var originFn = fn[ORIGIN_METHOD] || fn;
  var lastThrottleType = fn[THROTTLE_TYPE];
  var lastRate = fn[RATE];
  if (lastRate !== rate || lastThrottleType !== throttleType) {
    if (rate == null || !throttleType) {
      return obj[fnAttr] = originFn;
    }
    fn = obj[fnAttr] = throttle(originFn, rate, throttleType === "debounce");
    fn[ORIGIN_METHOD] = originFn;
    fn[THROTTLE_TYPE] = throttleType;
    fn[RATE] = rate;
  }
  return fn;
}
function clear(obj, fnAttr) {
  var fn = obj[fnAttr];
  if (fn && fn[ORIGIN_METHOD]) {
    fn.clear && fn.clear();
    obj[fnAttr] = fn[ORIGIN_METHOD];
  }
}

// node_modules/echarts/lib/util/shape/sausage.js
var SausageShape = (
  /** @class */
  /* @__PURE__ */ (function() {
    function SausageShape2() {
      this.cx = 0;
      this.cy = 0;
      this.r0 = 0;
      this.r = 0;
      this.startAngle = 0;
      this.endAngle = Math.PI * 2;
      this.clockwise = true;
    }
    return SausageShape2;
  })()
);
var SausagePath = (
  /** @class */
  (function(_super) {
    __extends(SausagePath2, _super);
    function SausagePath2(opts) {
      var _this = _super.call(this, opts) || this;
      _this.type = "sausage";
      return _this;
    }
    SausagePath2.prototype.getDefaultShape = function() {
      return new SausageShape();
    };
    SausagePath2.prototype.buildPath = function(ctx, shape) {
      var cx = shape.cx;
      var cy = shape.cy;
      var r0 = Math.max(shape.r0 || 0, 0);
      var r = Math.max(shape.r, 0);
      var dr = (r - r0) * 0.5;
      var rCenter = r0 + dr;
      var startAngle = shape.startAngle;
      var endAngle = shape.endAngle;
      var clockwise = shape.clockwise;
      var PI210 = Math.PI * 2;
      var lessThanCircle = clockwise ? endAngle - startAngle < PI210 : startAngle - endAngle < PI210;
      if (!lessThanCircle) {
        startAngle = endAngle - (clockwise ? PI210 : -PI210);
      }
      var unitStartX = Math.cos(startAngle);
      var unitStartY = Math.sin(startAngle);
      var unitEndX = Math.cos(endAngle);
      var unitEndY = Math.sin(endAngle);
      if (lessThanCircle) {
        ctx.moveTo(unitStartX * r0 + cx, unitStartY * r0 + cy);
        ctx.arc(unitStartX * rCenter + cx, unitStartY * rCenter + cy, dr, -Math.PI + startAngle, startAngle, !clockwise);
      } else {
        ctx.moveTo(unitStartX * r + cx, unitStartY * r + cy);
      }
      ctx.arc(cx, cy, r, startAngle, endAngle, !clockwise);
      ctx.arc(unitEndX * rCenter + cx, unitEndY * rCenter + cy, dr, endAngle - Math.PI * 2, endAngle - Math.PI, !clockwise);
      if (r0 !== 0) {
        ctx.arc(cx, cy, r0, endAngle, startAngle, clockwise);
      }
    };
    return SausagePath2;
  })(Path_default)
);
var sausage_default = SausagePath;

// node_modules/echarts/lib/label/sectorLabel.js
function createSectorCalculateTextPosition(positionMapping, opts) {
  opts = opts || {};
  var isRoundCap = opts.isRoundCap;
  return function(out2, opts2, boundingRect) {
    var textPosition = opts2.position;
    if (!textPosition || textPosition instanceof Array) {
      return calculateTextPosition(out2, opts2, boundingRect);
    }
    var mappedSectorPosition = positionMapping(textPosition);
    var distance2 = opts2.distance != null ? opts2.distance : 5;
    var sector = this.shape;
    var cx = sector.cx;
    var cy = sector.cy;
    var r = sector.r;
    var r0 = sector.r0;
    var middleR = (r + r0) / 2;
    var startAngle = sector.startAngle;
    var endAngle = sector.endAngle;
    var middleAngle = (startAngle + endAngle) / 2;
    var extraDist = isRoundCap ? Math.abs(r - r0) / 2 : 0;
    var mathCos6 = Math.cos;
    var mathSin6 = Math.sin;
    var x = cx + r * mathCos6(startAngle);
    var y = cy + r * mathSin6(startAngle);
    var textAlign = "left";
    var textVerticalAlign = "top";
    switch (mappedSectorPosition) {
      case "startArc":
        x = cx + (r0 - distance2) * mathCos6(middleAngle);
        y = cy + (r0 - distance2) * mathSin6(middleAngle);
        textAlign = "center";
        textVerticalAlign = "top";
        break;
      case "insideStartArc":
        x = cx + (r0 + distance2) * mathCos6(middleAngle);
        y = cy + (r0 + distance2) * mathSin6(middleAngle);
        textAlign = "center";
        textVerticalAlign = "bottom";
        break;
      case "startAngle":
        x = cx + middleR * mathCos6(startAngle) + adjustAngleDistanceX(startAngle, distance2 + extraDist, false);
        y = cy + middleR * mathSin6(startAngle) + adjustAngleDistanceY(startAngle, distance2 + extraDist, false);
        textAlign = "right";
        textVerticalAlign = "middle";
        break;
      case "insideStartAngle":
        x = cx + middleR * mathCos6(startAngle) + adjustAngleDistanceX(startAngle, -distance2 + extraDist, false);
        y = cy + middleR * mathSin6(startAngle) + adjustAngleDistanceY(startAngle, -distance2 + extraDist, false);
        textAlign = "left";
        textVerticalAlign = "middle";
        break;
      case "middle":
        x = cx + middleR * mathCos6(middleAngle);
        y = cy + middleR * mathSin6(middleAngle);
        textAlign = "center";
        textVerticalAlign = "middle";
        break;
      case "endArc":
        x = cx + (r + distance2) * mathCos6(middleAngle);
        y = cy + (r + distance2) * mathSin6(middleAngle);
        textAlign = "center";
        textVerticalAlign = "bottom";
        break;
      case "insideEndArc":
        x = cx + (r - distance2) * mathCos6(middleAngle);
        y = cy + (r - distance2) * mathSin6(middleAngle);
        textAlign = "center";
        textVerticalAlign = "top";
        break;
      case "endAngle":
        x = cx + middleR * mathCos6(endAngle) + adjustAngleDistanceX(endAngle, distance2 + extraDist, true);
        y = cy + middleR * mathSin6(endAngle) + adjustAngleDistanceY(endAngle, distance2 + extraDist, true);
        textAlign = "left";
        textVerticalAlign = "middle";
        break;
      case "insideEndAngle":
        x = cx + middleR * mathCos6(endAngle) + adjustAngleDistanceX(endAngle, -distance2 + extraDist, true);
        y = cy + middleR * mathSin6(endAngle) + adjustAngleDistanceY(endAngle, -distance2 + extraDist, true);
        textAlign = "right";
        textVerticalAlign = "middle";
        break;
      default:
        return calculateTextPosition(out2, opts2, boundingRect);
    }
    out2 = out2 || {};
    out2.x = x;
    out2.y = y;
    out2.align = textAlign;
    out2.verticalAlign = textVerticalAlign;
    return out2;
  };
}
function setSectorTextRotation(sector, textPosition, positionMapping, rotateType) {
  if (isNumber(rotateType)) {
    sector.setTextConfig({
      rotation: rotateType
    });
    return;
  } else if (isArray(textPosition)) {
    sector.setTextConfig({
      rotation: 0
    });
    return;
  }
  var shape = sector.shape;
  var startAngle = shape.clockwise ? shape.startAngle : shape.endAngle;
  var endAngle = shape.clockwise ? shape.endAngle : shape.startAngle;
  var middleAngle = (startAngle + endAngle) / 2;
  var anchorAngle;
  var mappedSectorPosition = positionMapping(textPosition);
  switch (mappedSectorPosition) {
    case "startArc":
    case "insideStartArc":
    case "middle":
    case "insideEndArc":
    case "endArc":
      anchorAngle = middleAngle;
      break;
    case "startAngle":
    case "insideStartAngle":
      anchorAngle = startAngle;
      break;
    case "endAngle":
    case "insideEndAngle":
      anchorAngle = endAngle;
      break;
    default:
      sector.setTextConfig({
        rotation: 0
      });
      return;
  }
  var rotate2 = Math.PI * 1.5 - anchorAngle;
  if (mappedSectorPosition === "middle" && rotate2 > Math.PI / 2 && rotate2 < Math.PI * 1.5) {
    rotate2 -= Math.PI;
  }
  sector.setTextConfig({
    rotation: rotate2
  });
}
function adjustAngleDistanceX(angle, distance2, isEnd) {
  return distance2 * Math.sin(angle) * (isEnd ? -1 : 1);
}
function adjustAngleDistanceY(angle, distance2, isEnd) {
  return distance2 * Math.cos(angle) * (isEnd ? 1 : -1);
}

// node_modules/echarts/lib/chart/helper/sectorHelper.js
function getSectorCornerRadius(model2, shape, zeroIfNull) {
  var cornerRadius = model2.get("borderRadius");
  if (cornerRadius == null) {
    return zeroIfNull ? {
      cornerRadius: 0
    } : null;
  }
  if (!isArray(cornerRadius)) {
    cornerRadius = [cornerRadius, cornerRadius, cornerRadius, cornerRadius];
  }
  var dr = Math.abs(shape.r || 0 - shape.r0 || 0);
  return {
    cornerRadius: map2(cornerRadius, function(cr) {
      return parsePercent(cr, dr);
    })
  };
}

// node_modules/echarts/lib/chart/bar/BarView.js
var mathMax8 = Math.max;
var mathMin8 = Math.min;
var BarView = (
  /** @class */
  (function(_super) {
    __extends(BarView2, _super);
    function BarView2() {
      var _this = _super.call(this) || this;
      _this.type = SERIES_TYPE_BAR;
      _this._isFirstFrame = true;
      return _this;
    }
    BarView2.prototype.render = function(seriesModel, ecModel, api, payload) {
      this._model = seriesModel;
      this._removeOnRenderedListener(api);
      this._updateDrawMode(seriesModel);
      var coordinateSystemType = seriesModel.get("coordinateSystem");
      if (coordinateSystemType === "cartesian2d" || coordinateSystemType === "polar") {
        this._progressiveEls = null;
        this._isLargeDraw ? this._renderLarge(seriesModel, ecModel, api) : this._renderNormal(seriesModel, ecModel, api, payload);
      } else if (true) {
        warn("Only cartesian2d and polar supported for bar.");
      }
    };
    BarView2.prototype.incrementalPrepareRender = function(seriesModel) {
      this._clear();
      this._updateDrawMode(seriesModel);
      this._updateLargeClip(seriesModel);
    };
    BarView2.prototype.incrementalRender = function(params, seriesModel) {
      this._progressiveEls = [];
      this._incrementalRenderLarge(params, seriesModel);
    };
    BarView2.prototype.eachRendered = function(cb) {
      traverseElements(this._progressiveEls || this.group, cb);
    };
    BarView2.prototype._updateDrawMode = function(seriesModel) {
      var isLargeDraw = seriesModel.pipelineContext.large;
      if (this._isLargeDraw == null || isLargeDraw !== this._isLargeDraw) {
        this._isLargeDraw = isLargeDraw;
        this._clear();
      }
    };
    BarView2.prototype._renderNormal = function(seriesModel, ecModel, api, payload) {
      var group2 = this.group;
      var data = seriesModel.getData();
      var oldData = this._data;
      var coord = seriesModel.coordinateSystem;
      var baseAxis = coord.getBaseAxis();
      var isHorizontalOrRadial;
      if (coord.type === "cartesian2d") {
        isHorizontalOrRadial = baseAxis.isHorizontal();
      } else if (coord.type === "polar") {
        isHorizontalOrRadial = baseAxis.dim === "angle";
      }
      var animationModel = seriesModel.isAnimationEnabled() ? seriesModel : null;
      var realtimeSortCfg = shouldRealtimeSort(seriesModel, coord);
      if (realtimeSortCfg) {
        this._enableRealtimeSort(realtimeSortCfg, data, api);
      }
      var needsClip = seriesModel.get("clip", true) || realtimeSortCfg;
      var coordSysClipArea = coord.getArea();
      group2.removeClipPath();
      var roundCap = seriesModel.get("roundCap", true);
      var drawBackground = seriesModel.get("showBackground", true);
      var backgroundModel = seriesModel.getModel("backgroundStyle");
      var barBorderRadius = backgroundModel.get("borderRadius") || 0;
      var bgEls = [];
      var oldBgEls = this._backgroundEls;
      var isInitSort = payload && payload.isInitSort;
      var isChangeOrder = payload && payload.type === "changeAxisOrder";
      function createBackground(dataIndex) {
        var bgLayout = getLayout[coord.type](data, dataIndex);
        if (!bgLayout) {
          return null;
        }
        var bgEl = createBackgroundEl(coord, isHorizontalOrRadial, bgLayout);
        bgEl.useStyle(backgroundModel.getItemStyle());
        if (coord.type === "cartesian2d") {
          bgEl.setShape("r", barBorderRadius);
        } else {
          bgEl.setShape("cornerRadius", barBorderRadius);
        }
        bgEls[dataIndex] = bgEl;
        return bgEl;
      }
      ;
      data.diff(oldData).add(function(dataIndex) {
        var itemModel = data.getItemModel(dataIndex);
        var layout3 = getLayout[coord.type](data, dataIndex, itemModel);
        if (!layout3) {
          return;
        }
        if (drawBackground) {
          createBackground(dataIndex);
        }
        if (!data.hasValue(dataIndex) || !isValidLayout[coord.type](layout3)) {
          return;
        }
        var isClipped = false;
        if (needsClip) {
          isClipped = clip[coord.type](coordSysClipArea, layout3);
        }
        var el = elementCreator[coord.type](seriesModel, data, dataIndex, layout3, isHorizontalOrRadial, animationModel, baseAxis.model, false, roundCap);
        if (realtimeSortCfg) {
          el.forceLabelAnimation = true;
        }
        updateStyle(el, data, dataIndex, itemModel, layout3, seriesModel, isHorizontalOrRadial, coord.type === "polar");
        if (isInitSort) {
          el.attr({
            shape: layout3
          });
        } else if (realtimeSortCfg) {
          updateRealtimeAnimation(realtimeSortCfg, animationModel, el, layout3, dataIndex, isHorizontalOrRadial, false, false);
        } else {
          initProps(el, {
            shape: layout3
          }, seriesModel, dataIndex);
        }
        data.setItemGraphicEl(dataIndex, el);
        group2.add(el);
        el.ignore = isClipped;
      }).update(function(newIndex, oldIndex) {
        var itemModel = data.getItemModel(newIndex);
        var layout3 = getLayout[coord.type](data, newIndex, itemModel);
        if (!layout3) {
          return;
        }
        if (drawBackground) {
          var bgEl = void 0;
          if (oldBgEls.length === 0) {
            bgEl = createBackground(oldIndex);
          } else {
            bgEl = oldBgEls[oldIndex];
            bgEl.useStyle(backgroundModel.getItemStyle());
            if (coord.type === "cartesian2d") {
              bgEl.setShape("r", barBorderRadius);
            } else {
              bgEl.setShape("cornerRadius", barBorderRadius);
            }
            bgEls[newIndex] = bgEl;
          }
          var bgLayout = getLayout[coord.type](data, newIndex);
          var shape = createBackgroundShape(isHorizontalOrRadial, bgLayout, coord);
          updateProps(bgEl, {
            shape
          }, animationModel, newIndex);
        }
        var el = oldData.getItemGraphicEl(oldIndex);
        if (!data.hasValue(newIndex) || !isValidLayout[coord.type](layout3)) {
          group2.remove(el);
          return;
        }
        var isClipped = false;
        if (needsClip) {
          isClipped = clip[coord.type](coordSysClipArea, layout3);
          if (isClipped) {
            group2.remove(el);
          }
        }
        var roundCapChanged = el && (el.type === "sector" && roundCap || el.type === "sausage" && !roundCap);
        if (roundCapChanged) {
          el && removeElementWithFadeOut(el, seriesModel, oldIndex);
          el = null;
        }
        if (!el) {
          el = elementCreator[coord.type](seriesModel, data, newIndex, layout3, isHorizontalOrRadial, animationModel, baseAxis.model, true, roundCap);
        } else {
          saveOldStyle(el);
        }
        if (realtimeSortCfg) {
          el.forceLabelAnimation = true;
        }
        if (isChangeOrder) {
          var textEl = el.getTextContent();
          if (textEl) {
            var labelInnerStore = labelInner(textEl);
            if (labelInnerStore.prevValue != null) {
              labelInnerStore.prevValue = labelInnerStore.value;
            }
          }
        } else {
          updateStyle(el, data, newIndex, itemModel, layout3, seriesModel, isHorizontalOrRadial, coord.type === "polar");
        }
        if (isInitSort) {
          el.attr({
            shape: layout3
          });
        } else if (realtimeSortCfg) {
          updateRealtimeAnimation(realtimeSortCfg, animationModel, el, layout3, newIndex, isHorizontalOrRadial, true, isChangeOrder);
        } else {
          updateProps(el, {
            shape: layout3
          }, seriesModel, newIndex, null);
        }
        data.setItemGraphicEl(newIndex, el);
        el.ignore = isClipped;
        group2.add(el);
      }).remove(function(dataIndex) {
        var el = oldData.getItemGraphicEl(dataIndex);
        el && removeElementWithFadeOut(el, seriesModel, dataIndex);
      }).execute();
      var bgGroup = this._backgroundGroup || (this._backgroundGroup = new Group_default());
      bgGroup.removeAll();
      for (var i2 = 0; i2 < bgEls.length; ++i2) {
        bgGroup.add(bgEls[i2]);
      }
      group2.add(bgGroup);
      this._backgroundEls = bgEls;
      this._data = data;
    };
    BarView2.prototype._renderLarge = function(seriesModel, ecModel, api) {
      this._clear();
      createLarge(seriesModel, this.group);
      this._updateLargeClip(seriesModel);
    };
    BarView2.prototype._incrementalRenderLarge = function(params, seriesModel) {
      this._removeBackground();
      createLarge(seriesModel, this.group, this._progressiveEls, true);
    };
    BarView2.prototype._updateLargeClip = function(seriesModel) {
      var clipPath = seriesModel.get("clip", true) && createClipPath(seriesModel.coordinateSystem, false, seriesModel);
      var group2 = this.group;
      if (clipPath) {
        group2.setClipPath(clipPath);
      } else {
        group2.removeClipPath();
      }
    };
    BarView2.prototype._enableRealtimeSort = function(realtimeSortCfg, data, api) {
      var _this = this;
      if (!data.count()) {
        return;
      }
      var baseAxis = realtimeSortCfg.baseAxis;
      if (this._isFirstFrame) {
        this._dispatchInitSort(data, realtimeSortCfg, api);
        this._isFirstFrame = false;
      } else {
        var orderMapping_1 = function(idx) {
          var el = data.getItemGraphicEl(idx);
          var shape = el && el.shape;
          return shape && // The result should be consistent with the initial sort by data value.
          // Do not support the case that both positive and negative exist.
          Math.abs(baseAxis.isHorizontal() ? shape.height : shape.width) || 0;
        };
        this._onRendered = function() {
          _this._updateSortWithinSameData(data, orderMapping_1, baseAxis, api);
        };
        api.getZr().on("rendered", this._onRendered);
      }
    };
    BarView2.prototype._dataSort = function(data, baseAxis, orderMapping) {
      var info = [];
      data.each(data.mapDimension(baseAxis.dim), function(ordinalNumber, dataIdx) {
        var mappedValue = orderMapping(dataIdx);
        mappedValue = mappedValue == null ? NaN : mappedValue;
        info.push({
          dataIndex: dataIdx,
          mappedValue,
          ordinalNumber
        });
      });
      info.sort(function(a, b) {
        return b.mappedValue - a.mappedValue;
      });
      return {
        ordinalNumbers: map2(info, function(item) {
          return item.ordinalNumber;
        })
      };
    };
    BarView2.prototype._isOrderChangedWithinSameData = function(data, orderMapping, baseAxis) {
      var scale3 = baseAxis.scale;
      var ordinalDataDim = data.mapDimension(baseAxis.dim);
      var lastValue = Number.MAX_VALUE;
      for (var tickNum = 0, len2 = scale3.getOrdinalMeta().categories.length; tickNum < len2; ++tickNum) {
        var rawIdx = data.rawIndexOf(ordinalDataDim, scale3.getRawOrdinalNumber(tickNum));
        var value = rawIdx < 0 ? Number.MIN_VALUE : orderMapping(data.indexOfRawIndex(rawIdx));
        if (value > lastValue) {
          return true;
        }
        lastValue = value;
      }
      return false;
    };
    BarView2.prototype._isOrderDifferentInView = function(orderInfo, baseAxis) {
      var scale3 = baseAxis.scale;
      var extent = scale3.getExtent();
      var tickNum = Math.max(0, extent[0]);
      var tickMax = Math.min(extent[1], scale3.getOrdinalMeta().categories.length - 1);
      for (; tickNum <= tickMax; ++tickNum) {
        if (orderInfo.ordinalNumbers[tickNum] !== scale3.getRawOrdinalNumber(tickNum)) {
          return true;
        }
      }
    };
    BarView2.prototype._updateSortWithinSameData = function(data, orderMapping, baseAxis, api) {
      if (!this._isOrderChangedWithinSameData(data, orderMapping, baseAxis)) {
        return;
      }
      var sortInfo = this._dataSort(data, baseAxis, orderMapping);
      if (this._isOrderDifferentInView(sortInfo, baseAxis)) {
        this._removeOnRenderedListener(api);
        api.dispatchAction({
          type: "changeAxisOrder",
          componentType: baseAxis.dim + "Axis",
          axisId: baseAxis.index,
          sortInfo
        });
      }
    };
    BarView2.prototype._dispatchInitSort = function(data, realtimeSortCfg, api) {
      var baseAxis = realtimeSortCfg.baseAxis;
      var sortResult = this._dataSort(data, baseAxis, function(dataIdx) {
        return data.get(data.mapDimension(realtimeSortCfg.otherAxis.dim), dataIdx);
      });
      api.dispatchAction({
        type: "changeAxisOrder",
        componentType: baseAxis.dim + "Axis",
        isInitSort: true,
        axisId: baseAxis.index,
        sortInfo: sortResult
      });
    };
    BarView2.prototype.remove = function(ecModel, api) {
      this._clear(this._model);
      this._removeOnRenderedListener(api);
    };
    BarView2.prototype.dispose = function(ecModel, api) {
      this._removeOnRenderedListener(api);
    };
    BarView2.prototype._removeOnRenderedListener = function(api) {
      if (this._onRendered) {
        api.getZr().off("rendered", this._onRendered);
        this._onRendered = null;
      }
    };
    BarView2.prototype._clear = function(model2) {
      var group2 = this.group;
      var data = this._data;
      if (model2 && model2.isAnimationEnabled() && data && !this._isLargeDraw) {
        this._removeBackground();
        this._backgroundEls = [];
        data.eachItemGraphicEl(function(el) {
          removeElementWithFadeOut(el, model2, getECData(el).dataIndex);
        });
      } else {
        group2.removeAll();
      }
      this._data = null;
      this._isFirstFrame = true;
    };
    BarView2.prototype._removeBackground = function() {
      this.group.remove(this._backgroundGroup);
      this._backgroundGroup = null;
    };
    BarView2.type = SERIES_TYPE_BAR;
    return BarView2;
  })(Chart_default)
);
var clip = {
  cartesian2d: function(coordSysClipArea, layout3) {
    var signWidth = layout3.width < 0 ? -1 : 1;
    var signHeight = layout3.height < 0 ? -1 : 1;
    if (signWidth < 0) {
      layout3.x += layout3.width;
      layout3.width = -layout3.width;
    }
    if (signHeight < 0) {
      layout3.y += layout3.height;
      layout3.height = -layout3.height;
    }
    var coordSysX2 = coordSysClipArea.x + coordSysClipArea.width;
    var coordSysY2 = coordSysClipArea.y + coordSysClipArea.height;
    var x = mathMax8(layout3.x, coordSysClipArea.x);
    var x2 = mathMin8(layout3.x + layout3.width, coordSysX2);
    var y = mathMax8(layout3.y, coordSysClipArea.y);
    var y2 = mathMin8(layout3.y + layout3.height, coordSysY2);
    var xClipped = x2 < x;
    var yClipped = y2 < y;
    layout3.x = xClipped && x > coordSysX2 ? x2 : x;
    layout3.y = yClipped && y > coordSysY2 ? y2 : y;
    layout3.width = xClipped ? 0 : x2 - x;
    layout3.height = yClipped ? 0 : y2 - y;
    if (signWidth < 0) {
      layout3.x += layout3.width;
      layout3.width = -layout3.width;
    }
    if (signHeight < 0) {
      layout3.y += layout3.height;
      layout3.height = -layout3.height;
    }
    return xClipped || yClipped;
  },
  polar: function(coordSysClipArea, layout3) {
    var signR = layout3.r0 <= layout3.r ? 1 : -1;
    if (signR < 0) {
      var tmp = layout3.r;
      layout3.r = layout3.r0;
      layout3.r0 = tmp;
    }
    var r = mathMin8(layout3.r, coordSysClipArea.r);
    var r0 = mathMax8(layout3.r0, coordSysClipArea.r0);
    layout3.r = r;
    layout3.r0 = r0;
    var clipped = r - r0 < 0;
    if (signR < 0) {
      var tmp = layout3.r;
      layout3.r = layout3.r0;
      layout3.r0 = tmp;
    }
    return clipped;
  }
};
var elementCreator = {
  cartesian2d: function(seriesModel, data, newIndex, layout3, isHorizontal, animationModel, axisModel, isUpdate, roundCap) {
    var rect = new Rect_default({
      shape: extend({}, layout3),
      z2: 1
    });
    rect.__dataIndex = newIndex;
    rect.name = "item";
    if (animationModel) {
      var rectShape = rect.shape;
      var animateProperty = isHorizontal ? "height" : "width";
      rectShape[animateProperty] = 0;
    }
    return rect;
  },
  polar: function(seriesModel, data, newIndex, layout3, isRadial, animationModel, axisModel, isUpdate, roundCap) {
    var ShapeClass = !isRadial && roundCap ? sausage_default : Sector_default;
    var sector = new ShapeClass({
      shape: layout3,
      z2: 1
    });
    sector.name = "item";
    var positionMap = createPolarPositionMapping(isRadial);
    sector.calculateTextPosition = createSectorCalculateTextPosition(positionMap, {
      isRoundCap: ShapeClass === sausage_default
    });
    if (animationModel) {
      var sectorShape = sector.shape;
      var animateProperty = isRadial ? "r" : "endAngle";
      var animateTarget = {};
      sectorShape[animateProperty] = isRadial ? layout3.r0 : layout3.startAngle;
      animateTarget[animateProperty] = layout3[animateProperty];
      (isUpdate ? updateProps : initProps)(sector, {
        shape: animateTarget
        // __value: typeof dataValue === 'string' ? parseInt(dataValue, 10) : dataValue
      }, animationModel);
    }
    return sector;
  }
};
function shouldRealtimeSort(seriesModel, coordSys) {
  var realtimeSortOption = seriesModel.get("realtimeSort", true);
  var baseAxis = coordSys.getBaseAxis();
  if (true) {
    if (realtimeSortOption) {
      if (baseAxis.type !== "category") {
        warn("`realtimeSort` will not work because this bar series is not based on a category axis.");
      }
      if (coordSys.type !== "cartesian2d") {
        warn("`realtimeSort` will not work because this bar series is not on cartesian2d.");
      }
    }
  }
  if (realtimeSortOption && baseAxis.type === "category" && coordSys.type === "cartesian2d") {
    return {
      baseAxis,
      otherAxis: coordSys.getOtherAxis(baseAxis)
    };
  }
}
function updateRealtimeAnimation(realtimeSortCfg, seriesAnimationModel, el, layout3, newIndex, isHorizontal, isUpdate, isChangeOrder) {
  var seriesTarget;
  var axisTarget;
  if (isHorizontal) {
    axisTarget = {
      x: layout3.x,
      width: layout3.width
    };
    seriesTarget = {
      y: layout3.y,
      height: layout3.height
    };
  } else {
    axisTarget = {
      y: layout3.y,
      height: layout3.height
    };
    seriesTarget = {
      x: layout3.x,
      width: layout3.width
    };
  }
  if (!isChangeOrder) {
    (isUpdate ? updateProps : initProps)(el, {
      shape: seriesTarget
    }, seriesAnimationModel, newIndex, null);
  }
  var axisAnimationModel = seriesAnimationModel ? realtimeSortCfg.baseAxis.model : null;
  (isUpdate ? updateProps : initProps)(el, {
    shape: axisTarget
  }, axisAnimationModel, newIndex);
}
function checkPropertiesNotValid(obj, props) {
  for (var i2 = 0; i2 < props.length; i2++) {
    if (!isFinite(obj[props[i2]])) {
      return true;
    }
  }
  return false;
}
var rectPropties = ["x", "y", "width", "height"];
var polarPropties = ["cx", "cy", "r", "startAngle", "endAngle"];
var isValidLayout = {
  cartesian2d: function(layout3) {
    return !checkPropertiesNotValid(layout3, rectPropties);
  },
  polar: function(layout3) {
    return !checkPropertiesNotValid(layout3, polarPropties);
  }
};
var getLayout = {
  // itemModel is only used to get borderWidth, which is not needed
  // when calculating bar background layout.
  cartesian2d: function(data, dataIndex, itemModel) {
    var layout3 = data.getItemLayout(dataIndex);
    if (!layout3) {
      return null;
    }
    var fixedLineWidth = itemModel ? getLineWidth(itemModel, layout3) : 0;
    var signX = layout3.width > 0 ? 1 : -1;
    var signY = layout3.height > 0 ? 1 : -1;
    return {
      x: layout3.x + signX * fixedLineWidth / 2,
      y: layout3.y + signY * fixedLineWidth / 2,
      width: layout3.width - signX * fixedLineWidth,
      height: layout3.height - signY * fixedLineWidth
    };
  },
  polar: function(data, dataIndex, itemModel) {
    var layout3 = data.getItemLayout(dataIndex);
    return {
      cx: layout3.cx,
      cy: layout3.cy,
      r0: layout3.r0,
      r: layout3.r,
      startAngle: layout3.startAngle,
      endAngle: layout3.endAngle,
      clockwise: layout3.clockwise
    };
  }
};
function isZeroOnPolar(layout3) {
  return layout3.startAngle != null && layout3.endAngle != null && layout3.startAngle === layout3.endAngle;
}
function createPolarPositionMapping(isRadial) {
  return /* @__PURE__ */ (function(isRadial2) {
    var arcOrAngle = isRadial2 ? "Arc" : "Angle";
    return function(position) {
      switch (position) {
        case "start":
        case "insideStart":
        case "end":
        case "insideEnd":
          return position + arcOrAngle;
        default:
          return position;
      }
    };
  })(isRadial);
}
function updateStyle(el, data, dataIndex, itemModel, layout3, seriesModel, isHorizontalOrRadial, isPolar) {
  var style2 = data.getItemVisual(dataIndex, "style");
  if (!isPolar) {
    var borderRadius = itemModel.get(["itemStyle", "borderRadius"]) || 0;
    el.setShape("r", borderRadius);
  } else if (!seriesModel.get("roundCap")) {
    var sectorShape = el.shape;
    var cornerRadius = getSectorCornerRadius(itemModel.getModel("itemStyle"), sectorShape, true);
    extend(sectorShape, cornerRadius);
    el.setShape(sectorShape);
  }
  el.useStyle(style2);
  var cursorStyle = itemModel.getShallow("cursor");
  cursorStyle && el.attr("cursor", cursorStyle);
  var labelPositionOutside = isPolar ? isHorizontalOrRadial ? layout3.r >= layout3.r0 ? "endArc" : "startArc" : layout3.endAngle >= layout3.startAngle ? "endAngle" : "startAngle" : isHorizontalOrRadial ? getLabelPositionForHorizontal(layout3, seriesModel.coordinateSystem) : getLabelPositionForVertical(layout3, seriesModel.coordinateSystem);
  var labelStatesModels = getLabelStatesModels(itemModel);
  setLabelStyle(el, labelStatesModels, {
    labelFetcher: seriesModel,
    labelDataIndex: dataIndex,
    defaultText: getDefaultLabel(seriesModel.getData(), dataIndex),
    inheritColor: style2.fill,
    defaultOpacity: style2.opacity,
    defaultOutsidePosition: labelPositionOutside
  });
  var label = el.getTextContent();
  if (isPolar && label) {
    var position = itemModel.get(["label", "position"]);
    el.textConfig.inside = position === "middle" ? true : null;
    setSectorTextRotation(el, position === "outside" ? labelPositionOutside : position, createPolarPositionMapping(isHorizontalOrRadial), itemModel.get(["label", "rotate"]));
  }
  setLabelValueAnimation(label, labelStatesModels, seriesModel.getRawValue(dataIndex), function(value) {
    return getDefaultInterpolatedLabel(data, value);
  });
  var emphasisModel = itemModel.getModel(["emphasis"]);
  toggleHoverEmphasis(el, emphasisModel.get("focus"), emphasisModel.get("blurScope"), emphasisModel.get("disabled"));
  setStatesStylesFromModel(el, itemModel);
  if (isZeroOnPolar(layout3)) {
    el.style.fill = "none";
    el.style.stroke = "none";
    each(el.states, function(state2) {
      if (state2.style) {
        state2.style.fill = state2.style.stroke = "none";
      }
    });
  }
}
function getLineWidth(itemModel, rawLayout) {
  var borderColor = itemModel.get(["itemStyle", "borderColor"]);
  if (!borderColor || borderColor === "none") {
    return 0;
  }
  var lineWidth = itemModel.get(["itemStyle", "borderWidth"]) || 0;
  var width = isNaN(rawLayout.width) ? Number.MAX_VALUE : Math.abs(rawLayout.width);
  var height = isNaN(rawLayout.height) ? Number.MAX_VALUE : Math.abs(rawLayout.height);
  return Math.min(lineWidth, width, height);
}
var LagePathShape = (
  /** @class */
  /* @__PURE__ */ (function() {
    function LagePathShape2() {
    }
    return LagePathShape2;
  })()
);
var LargePath = (
  /** @class */
  (function(_super) {
    __extends(LargePath2, _super);
    function LargePath2(opts) {
      var _this = _super.call(this, opts) || this;
      _this.type = "largeBar";
      return _this;
    }
    LargePath2.prototype.getDefaultShape = function() {
      return new LagePathShape();
    };
    LargePath2.prototype.buildPath = function(ctx, shape) {
      var points4 = shape.points;
      var baseDimIdx = this.baseDimIdx;
      var valueDimIdx = 1 - this.baseDimIdx;
      var startPoint = [];
      var size = [];
      var barWidth = this.barWidth;
      for (var i2 = 0; i2 < points4.length; i2 += 3) {
        size[baseDimIdx] = barWidth;
        size[valueDimIdx] = points4[i2 + 2];
        startPoint[baseDimIdx] = points4[i2 + baseDimIdx];
        startPoint[valueDimIdx] = points4[i2 + valueDimIdx];
        ctx.rect(startPoint[0], startPoint[1], size[0], size[1]);
      }
    };
    return LargePath2;
  })(Path_default)
);
function createLarge(seriesModel, group2, progressiveEls, incremental) {
  var data = seriesModel.getData();
  var baseDimIdx = data.getLayout("valueAxisHorizontal") ? 1 : 0;
  var largeDataIndices = data.getLayout("largeDataIndices");
  var barWidth = data.getLayout("size");
  var backgroundModel = seriesModel.getModel("backgroundStyle");
  var bgPoints = data.getLayout("largeBackgroundPoints");
  var incrementalId = incremental ? getIncrementalId(seriesModel) : 0;
  if (bgPoints) {
    var bgEl = new LargePath({
      shape: {
        points: bgPoints
      },
      incremental: incrementalId,
      silent: true,
      z2: 0
    });
    bgEl.baseDimIdx = baseDimIdx;
    bgEl.largeDataIndices = largeDataIndices;
    bgEl.barWidth = barWidth;
    bgEl.useStyle(backgroundModel.getItemStyle());
    group2.add(bgEl);
    progressiveEls && progressiveEls.push(bgEl);
  }
  var el = new LargePath({
    shape: {
      points: data.getLayout("largePoints")
    },
    incremental: incrementalId,
    ignoreCoarsePointer: true,
    z2: 1
  });
  el.baseDimIdx = baseDimIdx;
  el.largeDataIndices = largeDataIndices;
  el.barWidth = barWidth;
  group2.add(el);
  el.useStyle(data.getVisual("style"));
  el.style.stroke = null;
  getECData(el).seriesIndex = seriesModel.seriesIndex;
  if (!seriesModel.get("silent")) {
    el.on("mousedown", largePathUpdateDataIndex);
    el.on("mousemove", largePathUpdateDataIndex);
  }
  progressiveEls && progressiveEls.push(el);
}
var largePathUpdateDataIndex = throttle(function(event) {
  var largePath = this;
  var dataIndex = largePathFindDataIndex(largePath, event.offsetX, event.offsetY);
  getECData(largePath).dataIndex = dataIndex >= 0 ? dataIndex : null;
}, 30, false);
function largePathFindDataIndex(largePath, x, y) {
  var baseDimIdx = largePath.baseDimIdx;
  var valueDimIdx = 1 - baseDimIdx;
  var points4 = largePath.shape.points;
  var largeDataIndices = largePath.largeDataIndices;
  var startPoint = [];
  var size = [];
  var barWidth = largePath.barWidth;
  for (var i2 = 0, len2 = points4.length / 3; i2 < len2; i2++) {
    var ii = i2 * 3;
    size[baseDimIdx] = barWidth;
    size[valueDimIdx] = points4[ii + 2];
    startPoint[baseDimIdx] = points4[ii + baseDimIdx];
    startPoint[valueDimIdx] = points4[ii + valueDimIdx];
    if (size[valueDimIdx] < 0) {
      startPoint[valueDimIdx] += size[valueDimIdx];
      size[valueDimIdx] = -size[valueDimIdx];
    }
    if (x >= startPoint[0] && x <= startPoint[0] + size[0] && y >= startPoint[1] && y <= startPoint[1] + size[1]) {
      return largeDataIndices[i2];
    }
  }
  return -1;
}
function createBackgroundShape(isHorizontalOrRadial, layout3, coord) {
  if (isCoordinateSystemType(coord, "cartesian2d")) {
    var rectShape = layout3;
    var coordLayout = coord.getArea();
    return {
      x: isHorizontalOrRadial ? rectShape.x : coordLayout.x,
      y: isHorizontalOrRadial ? coordLayout.y : rectShape.y,
      width: isHorizontalOrRadial ? rectShape.width : coordLayout.width,
      height: isHorizontalOrRadial ? coordLayout.height : rectShape.height
    };
  } else {
    var coordLayout = coord.getArea();
    var sectorShape = layout3;
    return {
      cx: coordLayout.cx,
      cy: coordLayout.cy,
      r0: isHorizontalOrRadial ? coordLayout.r0 : sectorShape.r0,
      r: isHorizontalOrRadial ? coordLayout.r : sectorShape.r,
      startAngle: isHorizontalOrRadial ? sectorShape.startAngle : 0,
      endAngle: isHorizontalOrRadial ? sectorShape.endAngle : Math.PI * 2
    };
  }
}
function createBackgroundEl(coord, isHorizontalOrRadial, layout3) {
  var ElementClz = coord.type === "polar" ? Sector_default : Rect_default;
  return new ElementClz({
    shape: createBackgroundShape(isHorizontalOrRadial, layout3, coord),
    silent: true,
    z2: 0
  });
}
function getLabelPositionForHorizontal(layout3, coordSys) {
  if (layout3.height === 0) {
    var valueAxis2 = coordSys.getOtherAxis(coordSys.getBaseAxis());
    return valueAxis2.inverse ? "bottom" : "top";
  }
  return layout3.height > 0 ? "bottom" : "top";
}
function getLabelPositionForVertical(layout3, coordSys) {
  if (layout3.width === 0) {
    var valueAxis2 = coordSys.getOtherAxis(coordSys.getBaseAxis());
    return valueAxis2.inverse ? "left" : "right";
  }
  return layout3.width >= 0 ? "right" : "left";
}
var BarView_default = BarView;

// node_modules/echarts/lib/chart/bar/install.js
function install2(registers) {
  registers.registerChartView(BarView_default);
  registers.registerSeriesModel(BarSeries_default);
  registers.registerLayout(registers.PRIORITY.VISUAL.LAYOUT, createCrossSeriesLayoutHandler(SERIES_TYPE_BAR));
  registers.registerLayout(registers.PRIORITY.VISUAL.PROGRESSIVE_LAYOUT, createProgressiveLayout(SERIES_TYPE_BAR));
  registers.registerProcessor(registers.PRIORITY.PROCESSOR.STATISTIC, dataSample(SERIES_TYPE_BAR));
  registers.registerAction({
    type: "changeAxisOrder",
    event: "changeAxisOrder",
    update: "update"
  }, function(payload, ecModel) {
    var componentType = payload.componentType || "series";
    ecModel.eachComponent({
      mainType: componentType,
      query: payload
    }, function(componentModel) {
      if (payload.sortInfo) {
        componentModel.axis.setCategorySortInfo(payload.sortInfo);
      }
    });
  });
  registerBarGridAxisHandlers(registers);
}

// node_modules/echarts/lib/legacy/dataSelectAction.js
function createLegacyDataSelectAction(seriesType2, ecRegisterAction) {
  function getSeriesIndices(ecModel, payload) {
    var seriesIndices = [];
    ecModel.eachComponent({
      mainType: "series",
      subType: seriesType2,
      query: payload
    }, function(seriesModel) {
      seriesIndices.push(seriesModel.seriesIndex);
    });
    return seriesIndices;
  }
  each([[seriesType2 + "ToggleSelect", "toggleSelect"], [seriesType2 + "Select", "select"], [seriesType2 + "UnSelect", "unselect"]], function(eventsMap) {
    ecRegisterAction(eventsMap[0], function(payload, ecModel, api) {
      payload = extend({}, payload);
      if (true) {
        deprecateReplaceLog(payload.type, eventsMap[1]);
      }
      api.dispatchAction(extend(payload, {
        type: eventsMap[1],
        seriesIndex: getSeriesIndices(ecModel, payload)
      }));
    });
  });
}
function handleSeriesLegacySelectEvents(type, eventPostfix, ecIns, ecModel, payload) {
  var legacyEventName = type + eventPostfix;
  if (!ecIns.isSilent(legacyEventName)) {
    if (true) {
      deprecateLog("event " + legacyEventName + " is deprecated.");
    }
    ecModel.eachComponent({
      mainType: "series",
      subType: "pie"
    }, function(seriesModel) {
      var seriesIndex = seriesModel.seriesIndex;
      var selectedMap = seriesModel.option.selectedMap;
      var selected = payload.selected;
      for (var i2 = 0; i2 < selected.length; i2++) {
        if (selected[i2].seriesIndex === seriesIndex) {
          var data = seriesModel.getData();
          var dataIndex = queryDataIndex(data, payload.fromActionPayload);
          ecIns.trigger(legacyEventName, {
            type: legacyEventName,
            seriesId: seriesModel.id,
            name: isArray(dataIndex) ? data.getName(dataIndex[0]) : data.getName(dataIndex),
            selected: isString(selectedMap) ? selectedMap : extend({}, selectedMap)
          });
        }
      }
    });
  }
}
function handleLegacySelectEvents(messageCenter, ecIns, api) {
  messageCenter.on("selectchanged", function(params) {
    var ecModel = api.getModel();
    if (params.isFromClick) {
      handleSeriesLegacySelectEvents("map", "selectchanged", ecIns, ecModel, params);
      handleSeriesLegacySelectEvents("pie", "selectchanged", ecIns, ecModel, params);
    } else if (params.fromAction === "select") {
      handleSeriesLegacySelectEvents("map", "selected", ecIns, ecModel, params);
      handleSeriesLegacySelectEvents("pie", "selected", ecIns, ecModel, params);
    } else if (params.fromAction === "unselect") {
      handleSeriesLegacySelectEvents("map", "unselected", ecIns, ecModel, params);
      handleSeriesLegacySelectEvents("pie", "unselected", ecIns, ecModel, params);
    }
  });
}

// node_modules/echarts/lib/processor/dataFilter.js
function dataFilter(seriesType2) {
  return {
    seriesType: seriesType2,
    reset: function(seriesModel, ecModel) {
      var legendModels = ecModel.findComponents({
        mainType: "legend"
      });
      if (!legendModels || !legendModels.length) {
        return;
      }
      var data = seriesModel.getData();
      data.filterSelf(function(idx) {
        var name = data.getName(idx);
        for (var i2 = 0; i2 < legendModels.length; i2++) {
          if (!legendModels[i2].isSelected(name)) {
            return false;
          }
        }
        return true;
      });
    }
  };
}

// node_modules/echarts/lib/chart/helper/createSeriesDataSimply.js
function createSeriesDataSimply(seriesModel, opt, nameList) {
  opt = isArray(opt) && {
    coordDimensions: opt
  } || extend({
    encodeDefine: seriesModel.getEncode()
  }, opt);
  var source = seriesModel.getSource();
  var dimensions = prepareSeriesDataSchema(source, opt).dimensions;
  var list = new SeriesData_default(dimensions, seriesModel);
  list.initData(source, nameList);
  return list;
}

// node_modules/echarts/lib/visual/LegendVisualProvider.js
var LegendVisualProvider = (
  /** @class */
  (function() {
    function LegendVisualProvider2(getDataWithEncodedVisual, getRawData2) {
      this._getDataWithEncodedVisual = getDataWithEncodedVisual;
      this._getRawData = getRawData2;
    }
    LegendVisualProvider2.prototype.getAllNames = function() {
      var rawData = this._getRawData();
      return rawData.mapArray(rawData.getName);
    };
    LegendVisualProvider2.prototype.containName = function(name) {
      var rawData = this._getRawData();
      return rawData.indexOfName(name) >= 0;
    };
    LegendVisualProvider2.prototype.indexOfName = function(name) {
      var dataWithEncodedVisual = this._getDataWithEncodedVisual();
      return dataWithEncodedVisual.indexOfName(name);
    };
    LegendVisualProvider2.prototype.getItemVisual = function(dataIndex, key2) {
      var dataWithEncodedVisual = this._getDataWithEncodedVisual();
      return dataWithEncodedVisual.getItemVisual(dataIndex, key2);
    };
    return LegendVisualProvider2;
  })()
);
var LegendVisualProvider_default = LegendVisualProvider;

// node_modules/echarts/lib/chart/pie/PieSeries.js
var SERIES_TYPE_PIE = "pie";
var innerData = makeInner();
var PieSeriesModel = (
  /** @class */
  (function(_super) {
    __extends(PieSeriesModel2, _super);
    function PieSeriesModel2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = PieSeriesModel2.type;
      return _this;
    }
    PieSeriesModel2.prototype.init = function(option) {
      _super.prototype.init.apply(this, arguments);
      this.legendVisualProvider = new LegendVisualProvider_default(bind(this.getData, this), bind(this.getRawData, this));
      this._defaultLabelLine(option);
    };
    PieSeriesModel2.prototype.mergeOption = function() {
      _super.prototype.mergeOption.apply(this, arguments);
    };
    PieSeriesModel2.prototype.getInitialData = function() {
      return createSeriesDataSimply(this, {
        coordDimensions: ["value"],
        encodeDefaulter: curry(makeSeriesEncodeForNameBased, this)
      });
    };
    PieSeriesModel2.prototype.getDataParams = function(dataIndex) {
      var data = this.getData();
      var dataInner = innerData(data);
      var seats = dataInner.seats;
      if (!seats) {
        var valueList_1 = [];
        data.each(data.mapDimension("value"), function(value) {
          valueList_1.push(value);
        });
        seats = dataInner.seats = getPercentSeats(valueList_1, data.hostModel.get("percentPrecision"));
      }
      var params = _super.prototype.getDataParams.call(this, dataIndex);
      params.percent = seats[dataIndex] || 0;
      params.$vars.push("percent");
      return params;
    };
    PieSeriesModel2.prototype._defaultLabelLine = function(option) {
      defaultEmphasis(option, "labelLine", ["show"]);
      var labelLineNormalOpt = option.labelLine;
      var labelLineEmphasisOpt = option.emphasis.labelLine;
      labelLineNormalOpt.show = labelLineNormalOpt.show && option.label.show;
      labelLineEmphasisOpt.show = labelLineEmphasisOpt.show && option.emphasis.label.show;
    };
    PieSeriesModel2.type = "series." + SERIES_TYPE_PIE;
    PieSeriesModel2.defaultOption = {
      // zlevel: 0,
      z: 2,
      legendHoverLink: true,
      colorBy: "data",
      // 默认全局居中
      center: ["50%", "50%"],
      radius: [0, "50%"],
      // 默认顺时针
      clockwise: true,
      startAngle: 90,
      endAngle: "auto",
      padAngle: 0,
      // 最小角度改为0
      minAngle: 0,
      // If the angle of a sector less than `minShowLabelAngle`,
      // the label will not be displayed.
      minShowLabelAngle: 0,
      // 选中时扇区偏移量
      selectedOffset: 10,
      // 选择模式，默认关闭，可选single，multiple
      // selectedMode: false,
      // 南丁格尔玫瑰图模式，'radius'（半径） | 'area'（面积）
      // roseType: null,
      percentPrecision: 2,
      // If still show when all data zero.
      stillShowZeroSum: true,
      // cursor: null,
      coordinateSystemUsage: "box",
      left: 0,
      top: 0,
      right: 0,
      bottom: 0,
      width: null,
      height: null,
      label: {
        // color: 'inherit',
        // If rotate around circle
        rotate: 0,
        show: true,
        overflow: "truncate",
        // 'outer', 'inside', 'center'
        position: "outer",
        // 'none', 'labelLine', 'edge'. Works only when position is 'outer'
        alignTo: "none",
        // Closest distance between label and chart edge.
        // Works only position is 'outer' and alignTo is 'edge'.
        edgeDistance: "25%",
        // Works only position is 'outer' and alignTo is not 'edge'.
        // The default `bleedMargin` is auto determined according to view rect size.
        // bleedMargin: 10,
        // Distance between text and label line.
        distanceToLabelLine: 5
        // formatter: 标签文本格式器，同 tooltip.formatter，不支持异步回调
        // 默认使用全局文本样式，详见 textStyle
        // distance: 当position为inner时有效，为label位置到圆心的距离与圆半径(环状图为内外半径和)的比例系数
      },
      // Enabled when label.normal.position is 'outer'
      labelLine: {
        show: true,
        // 引导线两段中的第一段长度
        length: 15,
        // 引导线两段中的第二段长度
        length2: 30,
        smooth: false,
        minTurnAngle: 90,
        maxSurfaceAngle: 90,
        lineStyle: {
          // color: 各异,
          width: 1,
          type: "solid"
        }
      },
      itemStyle: {
        borderWidth: 1,
        borderJoin: "round"
      },
      showEmptyCircle: true,
      emptyCircleStyle: {
        color: "lightgray",
        opacity: 1
      },
      labelLayout: {
        // Hide the overlapped label.
        hideOverlap: true
      },
      emphasis: {
        scale: true,
        scaleSize: 5
      },
      // If use strategy to avoid label overlapping
      avoidLabelOverlap: true,
      // Animation type. Valid values: expansion, scale
      animationType: "expansion",
      animationDuration: 1e3,
      // Animation type when update. Valid values: transition, expansion
      animationTypeUpdate: "transition",
      animationEasingUpdate: "cubicInOut",
      animationDurationUpdate: 500,
      animationEasing: "cubicInOut"
    };
    return PieSeriesModel2;
  })(Series_default)
);
registerLayOutOnCoordSysUsage({
  fullType: PieSeriesModel.type,
  getCoord2: function(model2) {
    return model2.getShallow("center");
  }
});
var PieSeries_default = PieSeriesModel;

// node_modules/echarts/lib/label/labelGuideHelper.js
var PI27 = Math.PI * 2;
var CMD4 = PathProxy_default.CMD;
var DEFAULT_SEARCH_SPACE = ["top", "right", "bottom", "left"];
function getCandidateAnchor(pos, distance2, rect, outPt, outDir) {
  var width = rect.width;
  var height = rect.height;
  switch (pos) {
    case "top":
      outPt.set(rect.x + width / 2, rect.y - distance2);
      outDir.set(0, -1);
      break;
    case "bottom":
      outPt.set(rect.x + width / 2, rect.y + height + distance2);
      outDir.set(0, 1);
      break;
    case "left":
      outPt.set(rect.x - distance2, rect.y + height / 2);
      outDir.set(-1, 0);
      break;
    case "right":
      outPt.set(rect.x + width + distance2, rect.y + height / 2);
      outDir.set(1, 0);
      break;
  }
}
function projectPointToArc(cx, cy, r, startAngle, endAngle, anticlockwise, x, y, out2) {
  x -= cx;
  y -= cy;
  var d = Math.sqrt(x * x + y * y);
  x /= d;
  y /= d;
  var ox = x * r + cx;
  var oy = y * r + cy;
  if (Math.abs(startAngle - endAngle) % PI27 < 1e-4) {
    out2[0] = ox;
    out2[1] = oy;
    return d - r;
  }
  if (anticlockwise) {
    var tmp = startAngle;
    startAngle = normalizeRadian(endAngle);
    endAngle = normalizeRadian(tmp);
  } else {
    startAngle = normalizeRadian(startAngle);
    endAngle = normalizeRadian(endAngle);
  }
  if (startAngle > endAngle) {
    endAngle += PI27;
  }
  var angle = Math.atan2(y, x);
  if (angle < 0) {
    angle += PI27;
  }
  if (angle >= startAngle && angle <= endAngle || angle + PI27 >= startAngle && angle + PI27 <= endAngle) {
    out2[0] = ox;
    out2[1] = oy;
    return d - r;
  }
  var x1 = r * Math.cos(startAngle) + cx;
  var y1 = r * Math.sin(startAngle) + cy;
  var x2 = r * Math.cos(endAngle) + cx;
  var y2 = r * Math.sin(endAngle) + cy;
  var d1 = (x1 - x) * (x1 - x) + (y1 - y) * (y1 - y);
  var d2 = (x2 - x) * (x2 - x) + (y2 - y) * (y2 - y);
  if (d1 < d2) {
    out2[0] = x1;
    out2[1] = y1;
    return Math.sqrt(d1);
  } else {
    out2[0] = x2;
    out2[1] = y2;
    return Math.sqrt(d2);
  }
}
function projectPointToLine(x1, y1, x2, y2, x, y, out2, limitToEnds) {
  var dx = x - x1;
  var dy = y - y1;
  var dx1 = x2 - x1;
  var dy1 = y2 - y1;
  var lineLen = Math.sqrt(dx1 * dx1 + dy1 * dy1);
  dx1 /= lineLen;
  dy1 /= lineLen;
  var projectedLen = dx * dx1 + dy * dy1;
  var t = projectedLen / lineLen;
  if (limitToEnds) {
    t = Math.min(Math.max(t, 0), 1);
  }
  t *= lineLen;
  var ox = out2[0] = x1 + t * dx1;
  var oy = out2[1] = y1 + t * dy1;
  return Math.sqrt((ox - x) * (ox - x) + (oy - y) * (oy - y));
}
function projectPointToRect(x1, y1, width, height, x, y, out2) {
  if (width < 0) {
    x1 = x1 + width;
    width = -width;
  }
  if (height < 0) {
    y1 = y1 + height;
    height = -height;
  }
  var x2 = x1 + width;
  var y2 = y1 + height;
  var ox = out2[0] = Math.min(Math.max(x, x1), x2);
  var oy = out2[1] = Math.min(Math.max(y, y1), y2);
  return Math.sqrt((ox - x) * (ox - x) + (oy - y) * (oy - y));
}
var tmpPt = [];
function nearestPointOnRect(pt, rect, out2) {
  var dist3 = projectPointToRect(rect.x, rect.y, rect.width, rect.height, pt.x, pt.y, tmpPt);
  out2.set(tmpPt[0], tmpPt[1]);
  return dist3;
}
function nearestPointOnPath(pt, path, out2) {
  var xi = 0;
  var yi = 0;
  var x0 = 0;
  var y0 = 0;
  var x1;
  var y1;
  var minDist = Infinity;
  var data = path.data;
  var x = pt.x;
  var y = pt.y;
  for (var i2 = 0; i2 < data.length; ) {
    var cmd = data[i2++];
    if (i2 === 1) {
      xi = data[i2];
      yi = data[i2 + 1];
      x0 = xi;
      y0 = yi;
    }
    var d = minDist;
    switch (cmd) {
      case CMD4.M:
        x0 = data[i2++];
        y0 = data[i2++];
        xi = x0;
        yi = y0;
        break;
      case CMD4.L:
        d = projectPointToLine(xi, yi, data[i2], data[i2 + 1], x, y, tmpPt, true);
        xi = data[i2++];
        yi = data[i2++];
        break;
      case CMD4.C:
        d = cubicProjectPoint(xi, yi, data[i2++], data[i2++], data[i2++], data[i2++], data[i2], data[i2 + 1], x, y, tmpPt);
        xi = data[i2++];
        yi = data[i2++];
        break;
      case CMD4.Q:
        d = quadraticProjectPoint(xi, yi, data[i2++], data[i2++], data[i2], data[i2 + 1], x, y, tmpPt);
        xi = data[i2++];
        yi = data[i2++];
        break;
      case CMD4.A:
        var cx = data[i2++];
        var cy = data[i2++];
        var rx = data[i2++];
        var ry = data[i2++];
        var theta = data[i2++];
        var dTheta = data[i2++];
        i2 += 1;
        var anticlockwise = !!(1 - data[i2++]);
        x1 = Math.cos(theta) * rx + cx;
        y1 = Math.sin(theta) * ry + cy;
        if (i2 <= 1) {
          x0 = x1;
          y0 = y1;
        }
        var _x = (x - cx) * ry / rx + cx;
        d = projectPointToArc(cx, cy, ry, theta, theta + dTheta, anticlockwise, _x, y, tmpPt);
        xi = Math.cos(theta + dTheta) * rx + cx;
        yi = Math.sin(theta + dTheta) * ry + cy;
        break;
      case CMD4.R:
        x0 = xi = data[i2++];
        y0 = yi = data[i2++];
        var width = data[i2++];
        var height = data[i2++];
        d = projectPointToRect(x0, y0, width, height, x, y, tmpPt);
        break;
      case CMD4.Z:
        d = projectPointToLine(xi, yi, x0, y0, x, y, tmpPt, true);
        xi = x0;
        yi = y0;
        break;
    }
    if (d < minDist) {
      minDist = d;
      out2.set(tmpPt[0], tmpPt[1]);
    }
  }
  return minDist;
}
var pt0 = new Point_default();
var pt1 = new Point_default();
var pt2 = new Point_default();
var dir = new Point_default();
var dir2 = new Point_default();
function updateLabelLinePoints(target, labelLineModel) {
  if (!target) {
    return;
  }
  var labelLine = target.getTextGuideLine();
  var label = target.getTextContent();
  if (!(label && labelLine)) {
    return;
  }
  var labelGuideConfig = target.textGuideLineConfig || {};
  var points4 = [[0, 0], [0, 0], [0, 0]];
  var searchSpace = labelGuideConfig.candidates || DEFAULT_SEARCH_SPACE;
  var labelRect = label.getBoundingRect().clone();
  labelRect.applyTransform(label.getComputedTransform());
  var minDist = Infinity;
  var anchorPoint = labelGuideConfig.anchor;
  var targetTransform = target.getComputedTransform();
  var targetInversedTransform = targetTransform && invert([], targetTransform);
  var len2 = labelLineModel.get("length2") || 0;
  if (anchorPoint) {
    pt2.copy(anchorPoint);
  }
  for (var i2 = 0; i2 < searchSpace.length; i2++) {
    var candidate = searchSpace[i2];
    getCandidateAnchor(candidate, 0, labelRect, pt0, dir);
    Point_default.scaleAndAdd(pt1, pt0, dir, len2);
    pt1.transform(targetInversedTransform);
    var boundingRect = target.getBoundingRect();
    var dist3 = anchorPoint ? anchorPoint.distance(pt1) : target instanceof Path_default ? nearestPointOnPath(pt1, target.path, pt2) : nearestPointOnRect(pt1, boundingRect, pt2);
    if (dist3 < minDist) {
      minDist = dist3;
      pt1.transform(targetTransform);
      pt2.transform(targetTransform);
      pt2.toArray(points4[0]);
      pt1.toArray(points4[1]);
      pt0.toArray(points4[2]);
    }
  }
  limitTurnAngle(points4, labelLineModel.get("minTurnAngle"));
  labelLine.setShape({
    points: points4
  });
}
var tmpArr = [];
var tmpProjPoint = new Point_default();
function limitTurnAngle(linePoints, minTurnAngle) {
  if (!(minTurnAngle <= 180 && minTurnAngle > 0)) {
    return;
  }
  minTurnAngle = minTurnAngle / 180 * Math.PI;
  pt0.fromArray(linePoints[0]);
  pt1.fromArray(linePoints[1]);
  pt2.fromArray(linePoints[2]);
  Point_default.sub(dir, pt0, pt1);
  Point_default.sub(dir2, pt2, pt1);
  var len1 = dir.len();
  var len2 = dir2.len();
  if (len1 < 1e-3 || len2 < 1e-3) {
    return;
  }
  dir.scale(1 / len1);
  dir2.scale(1 / len2);
  var angleCos = dir.dot(dir2);
  var minTurnAngleCos = Math.cos(minTurnAngle);
  if (minTurnAngleCos < angleCos) {
    var d = projectPointToLine(pt1.x, pt1.y, pt2.x, pt2.y, pt0.x, pt0.y, tmpArr, false);
    tmpProjPoint.fromArray(tmpArr);
    tmpProjPoint.scaleAndAdd(dir2, d / Math.tan(Math.PI - minTurnAngle));
    var t = pt2.x !== pt1.x ? (tmpProjPoint.x - pt1.x) / (pt2.x - pt1.x) : (tmpProjPoint.y - pt1.y) / (pt2.y - pt1.y);
    if (isNaN(t)) {
      return;
    }
    if (t < 0) {
      Point_default.copy(tmpProjPoint, pt1);
    } else if (t > 1) {
      Point_default.copy(tmpProjPoint, pt2);
    }
    tmpProjPoint.toArray(linePoints[1]);
  }
}
function limitSurfaceAngle(linePoints, surfaceNormal, maxSurfaceAngle) {
  if (!(maxSurfaceAngle <= 180 && maxSurfaceAngle > 0)) {
    return;
  }
  maxSurfaceAngle = maxSurfaceAngle / 180 * Math.PI;
  pt0.fromArray(linePoints[0]);
  pt1.fromArray(linePoints[1]);
  pt2.fromArray(linePoints[2]);
  Point_default.sub(dir, pt1, pt0);
  Point_default.sub(dir2, pt2, pt1);
  var len1 = dir.len();
  var len2 = dir2.len();
  if (len1 < 1e-3 || len2 < 1e-3) {
    return;
  }
  dir.scale(1 / len1);
  dir2.scale(1 / len2);
  var angleCos = dir.dot(surfaceNormal);
  var maxSurfaceAngleCos = Math.cos(maxSurfaceAngle);
  if (angleCos < maxSurfaceAngleCos) {
    var d = projectPointToLine(pt1.x, pt1.y, pt2.x, pt2.y, pt0.x, pt0.y, tmpArr, false);
    tmpProjPoint.fromArray(tmpArr);
    var HALF_PI = Math.PI / 2;
    var angle2 = Math.acos(dir2.dot(surfaceNormal));
    var newAngle = HALF_PI + angle2 - maxSurfaceAngle;
    if (newAngle >= HALF_PI) {
      Point_default.copy(tmpProjPoint, pt2);
    } else {
      tmpProjPoint.scaleAndAdd(dir2, d / Math.tan(Math.PI / 2 - newAngle));
      var t = pt2.x !== pt1.x ? (tmpProjPoint.x - pt1.x) / (pt2.x - pt1.x) : (tmpProjPoint.y - pt1.y) / (pt2.y - pt1.y);
      if (isNaN(t)) {
        return;
      }
      if (t < 0) {
        Point_default.copy(tmpProjPoint, pt1);
      } else if (t > 1) {
        Point_default.copy(tmpProjPoint, pt2);
      }
    }
    tmpProjPoint.toArray(linePoints[1]);
  }
}
function setLabelLineState(labelLine, ignore, stateName, stateModel) {
  var isNormal = stateName === "normal";
  var stateObj = isNormal ? labelLine : labelLine.ensureState(stateName);
  stateObj.ignore = ignore;
  var smooth = stateModel.get("smooth");
  smooth = smooth === true ? 0.3 : Math.max(+smooth, 0) || 0;
  stateObj.shape = stateObj.shape || {};
  stateObj.shape.smooth = smooth;
  var styleObj = stateModel.getModel("lineStyle").getLineStyle();
  isNormal ? labelLine.useStyle(styleObj) : stateObj.style = styleObj;
}
function buildLabelLinePath(path, shape) {
  var smooth = shape.smooth;
  var points4 = shape.points;
  if (!points4) {
    return;
  }
  path.moveTo(points4[0][0], points4[0][1]);
  if (smooth > 0 && points4.length >= 3) {
    var len1 = dist(points4[0], points4[1]);
    var len2 = dist(points4[1], points4[2]);
    if (!len1 || !len2) {
      path.lineTo(points4[1][0], points4[1][1]);
      path.lineTo(points4[2][0], points4[2][1]);
      return;
    }
    var moveLen = Math.min(len1, len2) * smooth;
    var midPoint0 = lerp([], points4[1], points4[0], moveLen / len1);
    var midPoint2 = lerp([], points4[1], points4[2], moveLen / len2);
    var midPoint1 = lerp([], midPoint0, midPoint2, 0.5);
    path.bezierCurveTo(midPoint0[0], midPoint0[1], midPoint0[0], midPoint0[1], midPoint1[0], midPoint1[1]);
    path.bezierCurveTo(midPoint2[0], midPoint2[1], midPoint2[0], midPoint2[1], points4[2][0], points4[2][1]);
  } else {
    for (var i2 = 1; i2 < points4.length; i2++) {
      path.lineTo(points4[i2][0], points4[i2][1]);
    }
  }
}
function setLabelLineStyle(targetEl, statesModels, defaultStyle) {
  var labelLine = targetEl.getTextGuideLine();
  var label = targetEl.getTextContent();
  if (!label) {
    if (labelLine) {
      targetEl.removeTextGuideLine();
    }
    return;
  }
  var normalModel = statesModels.normal;
  var showNormal = normalModel.get("show");
  var labelIgnoreNormal = label.ignore;
  for (var i2 = 0; i2 < DISPLAY_STATES.length; i2++) {
    var stateName = DISPLAY_STATES[i2];
    var stateModel = statesModels[stateName];
    var isNormal = stateName === "normal";
    if (stateModel) {
      var stateShow = stateModel.get("show");
      var isLabelIgnored = isNormal ? labelIgnoreNormal : retrieve2(label.states[stateName] && label.states[stateName].ignore, labelIgnoreNormal);
      if (isLabelIgnored || !retrieve2(stateShow, showNormal)) {
        var stateObj = isNormal ? labelLine : labelLine && labelLine.states[stateName];
        if (stateObj) {
          stateObj.ignore = true;
        }
        if (!!labelLine) {
          setLabelLineState(labelLine, true, stateName, stateModel);
        }
        continue;
      }
      if (!labelLine) {
        labelLine = new Polyline_default();
        targetEl.setTextGuideLine(labelLine);
        if (!isNormal && (labelIgnoreNormal || !showNormal)) {
          setLabelLineState(labelLine, true, "normal", statesModels.normal);
        }
        if (targetEl.stateProxy) {
          labelLine.stateProxy = targetEl.stateProxy;
        }
      }
      setLabelLineState(labelLine, false, stateName, stateModel);
    }
  }
  if (labelLine) {
    defaults(labelLine.style, defaultStyle);
    labelLine.style.fill = null;
    var showAbove = normalModel.get("showAbove");
    var labelLineConfig = targetEl.textGuideLineConfig = targetEl.textGuideLineConfig || {};
    labelLineConfig.showAbove = showAbove || false;
    labelLine.buildPath = buildLabelLinePath;
  }
}
function getLabelLineStatesModels(itemModel, labelLineName) {
  labelLineName = labelLineName || "labelLine";
  var statesModels = {
    normal: itemModel.getModel(labelLineName)
  };
  for (var i2 = 0; i2 < SPECIAL_STATES.length; i2++) {
    var stateName = SPECIAL_STATES[i2];
    statesModels[stateName] = itemModel.getModel([stateName, labelLineName]);
  }
  return statesModels;
}

// node_modules/echarts/lib/chart/pie/labelLayout.js
var RADIAN = Math.PI / 180;
function adjustSingleSide(list, cx, cy, r, dir3, viewWidth, viewHeight, viewLeft, viewTop, farthestX) {
  if (list.length < 2) {
    return;
  }
  ;
  function recalculateXOnSemiToAlignOnEllipseCurve(semi) {
    var rB = semi.rB;
    var rB2 = rB * rB;
    for (var i3 = 0; i3 < semi.list.length; i3++) {
      var item = semi.list[i3];
      var dy = Math.abs(item.label.y - cy);
      var rA = r + item.len;
      var rA2 = rA * rA;
      var dx2 = Math.sqrt(Math.abs((1 - dy * dy / rB2) * rA2));
      var newX = cx + (dx2 + item.len2) * dir3;
      var deltaX = newX - item.label.x;
      var newTargetWidth = item.targetTextWidth - deltaX * dir3;
      constrainTextWidth(item, newTargetWidth, true);
      item.label.x = newX;
    }
  }
  function recalculateX(items) {
    var topSemi = {
      list: [],
      maxY: 0
    };
    var bottomSemi = {
      list: [],
      maxY: 0
    };
    for (var i3 = 0; i3 < items.length; i3++) {
      if (items[i3].labelAlignTo !== "none") {
        continue;
      }
      var item = items[i3];
      var semi = item.label.y > cy ? bottomSemi : topSemi;
      var dy = Math.abs(item.label.y - cy);
      if (dy >= semi.maxY) {
        var dx2 = item.label.x - cx - item.len2 * dir3;
        var rA = r + item.len;
        var rB = Math.abs(dx2) < rA ? Math.sqrt(dy * dy / (1 - dx2 * dx2 / rA / rA)) : rA;
        semi.rB = rB;
        semi.maxY = dy;
      }
      semi.list.push(item);
    }
    recalculateXOnSemiToAlignOnEllipseCurve(topSemi);
    recalculateXOnSemiToAlignOnEllipseCurve(bottomSemi);
  }
  var len2 = list.length;
  for (var i2 = 0; i2 < len2; i2++) {
    if (list[i2].position === "outer" && list[i2].labelAlignTo === "labelLine") {
      var dx = list[i2].label.x - farthestX;
      list[i2].linePoints[1][0] += dx;
      list[i2].label.x = farthestX;
    }
  }
  if (shiftLayoutOnXY(list, 1, viewTop, viewTop + viewHeight)) {
    recalculateX(list);
  }
}
function avoidOverlap(labelLayoutList, cx, cy, r, viewWidth, viewHeight, viewLeft, viewTop) {
  var leftList = [];
  var rightList = [];
  var leftmostX = Number.MAX_VALUE;
  var rightmostX = -Number.MAX_VALUE;
  for (var i2 = 0; i2 < labelLayoutList.length; i2++) {
    var label = labelLayoutList[i2].label;
    if (isPositionCenter(labelLayoutList[i2])) {
      continue;
    }
    if (label.x < cx) {
      leftmostX = Math.min(leftmostX, label.x);
      leftList.push(labelLayoutList[i2]);
    } else {
      rightmostX = Math.max(rightmostX, label.x);
      rightList.push(labelLayoutList[i2]);
    }
  }
  for (var i2 = 0; i2 < labelLayoutList.length; i2++) {
    var layout3 = labelLayoutList[i2];
    if (!isPositionCenter(layout3) && layout3.linePoints) {
      if (layout3.labelStyleWidth != null) {
        continue;
      }
      var label = layout3.label;
      var linePoints = layout3.linePoints;
      var targetTextWidth = void 0;
      if (layout3.labelAlignTo === "edge") {
        if (label.x < cx) {
          targetTextWidth = linePoints[2][0] - layout3.labelDistance - viewLeft - layout3.edgeDistance;
        } else {
          targetTextWidth = viewLeft + viewWidth - layout3.edgeDistance - linePoints[2][0] - layout3.labelDistance;
        }
      } else if (layout3.labelAlignTo === "labelLine") {
        if (label.x < cx) {
          targetTextWidth = leftmostX - viewLeft - layout3.bleedMargin;
        } else {
          targetTextWidth = viewLeft + viewWidth - rightmostX - layout3.bleedMargin;
        }
      } else {
        if (label.x < cx) {
          targetTextWidth = label.x - viewLeft - layout3.bleedMargin;
        } else {
          targetTextWidth = viewLeft + viewWidth - label.x - layout3.bleedMargin;
        }
      }
      layout3.targetTextWidth = targetTextWidth;
      constrainTextWidth(layout3, targetTextWidth, false);
    }
  }
  adjustSingleSide(rightList, cx, cy, r, 1, viewWidth, viewHeight, viewLeft, viewTop, rightmostX);
  adjustSingleSide(leftList, cx, cy, r, -1, viewWidth, viewHeight, viewLeft, viewTop, leftmostX);
  for (var i2 = 0; i2 < labelLayoutList.length; i2++) {
    var layout3 = labelLayoutList[i2];
    if (!isPositionCenter(layout3) && layout3.linePoints) {
      var label = layout3.label;
      var linePoints = layout3.linePoints;
      var isAlignToEdge = layout3.labelAlignTo === "edge";
      var padding = label.style.padding;
      var paddingH = padding ? padding[1] + padding[3] : 0;
      var extraPaddingH = label.style.backgroundColor ? 0 : paddingH;
      var realTextWidth = layout3.rect.width + extraPaddingH;
      var dist3 = linePoints[1][0] - linePoints[2][0];
      if (isAlignToEdge) {
        if (label.x < cx) {
          linePoints[2][0] = viewLeft + layout3.edgeDistance + realTextWidth + layout3.labelDistance;
        } else {
          linePoints[2][0] = viewLeft + viewWidth - layout3.edgeDistance - realTextWidth - layout3.labelDistance;
        }
      } else {
        if (label.x < cx) {
          linePoints[2][0] = label.x + layout3.labelDistance;
        } else {
          linePoints[2][0] = label.x - layout3.labelDistance;
        }
        linePoints[1][0] = linePoints[2][0] + dist3;
      }
      linePoints[1][1] = linePoints[2][1] = label.y;
    }
  }
}
function constrainTextWidth(layout3, availableWidth, forceRecalculate) {
  if (layout3.labelStyleWidth != null) {
    return;
  }
  var label = layout3.label;
  var style2 = label.style;
  var textRect = layout3.rect;
  var bgColor = style2.backgroundColor;
  var padding = style2.padding;
  var paddingH = padding ? padding[1] + padding[3] : 0;
  var overflow = style2.overflow;
  var oldOuterWidth = textRect.width + (bgColor ? 0 : paddingH);
  if (availableWidth < oldOuterWidth || forceRecalculate) {
    if (overflow && overflow.match("break")) {
      label.setStyle("backgroundColor", null);
      label.setStyle("width", availableWidth - paddingH);
      var innerRect = label.getBoundingRect();
      label.setStyle("width", Math.ceil(innerRect.width));
      label.setStyle("backgroundColor", bgColor);
    } else {
      var availableInnerWidth = availableWidth - paddingH;
      var newWidth = availableWidth < oldOuterWidth ? availableInnerWidth : (
        // Current available width is enough, but the text may have
        // already been wrapped with a smaller available width.
        forceRecalculate ? availableInnerWidth > layout3.unconstrainedWidth ? null : availableInnerWidth : null
      );
      label.setStyle("width", newWidth);
    }
    computeLabelGlobalRect(textRect, label);
  }
}
function computeLabelGlobalRect(out2, label) {
  _tmpLabelGeometry.rect = out2;
  computeLabelGeometry(_tmpLabelGeometry, label, _computeLabelGeometryOpt);
}
var _computeLabelGeometryOpt = {
  minMarginForce: [null, 0, null, 0],
  marginDefault: [1, 0, 1, 0]
};
var _tmpLabelGeometry = {};
function isPositionCenter(sectorShape) {
  return sectorShape.position === "center";
}
function pieLabelLayout(seriesModel) {
  var data = seriesModel.getData();
  var labelLayoutList = [];
  var cx;
  var cy;
  var hasLabelRotate = false;
  var minShowLabelRadian = (seriesModel.get("minShowLabelAngle") || 0) * RADIAN;
  var viewRect2 = data.getLayout("viewRect");
  var r = data.getLayout("r");
  var viewWidth = viewRect2.width;
  var viewLeft = viewRect2.x;
  var viewTop = viewRect2.y;
  var viewHeight = viewRect2.height;
  function setNotShow(el) {
    el.ignore = true;
  }
  function isLabelShown(label2) {
    if (!label2.ignore) {
      return true;
    }
    for (var key2 in label2.states) {
      if (label2.states[key2].ignore === false) {
        return true;
      }
    }
    return false;
  }
  data.each(function(idx) {
    var sector = data.getItemGraphicEl(idx);
    var sectorShape = sector.shape;
    var label2 = sector.getTextContent();
    var labelLine2 = sector.getTextGuideLine();
    var itemModel = data.getItemModel(idx);
    var labelModel = itemModel.getModel("label");
    var labelPosition = labelModel.get("position") || itemModel.get(["emphasis", "label", "position"]);
    var labelDistance = labelModel.get("distanceToLabelLine");
    var labelAlignTo = labelModel.get("alignTo");
    var edgeDistance = parsePercent2(labelModel.get("edgeDistance"), viewWidth);
    var bleedMargin = labelModel.get("bleedMargin");
    if (bleedMargin == null) {
      bleedMargin = Math.min(viewWidth, viewHeight) > 200 ? 10 : 2;
    }
    var labelLineModel = itemModel.getModel("labelLine");
    var labelLineLen = labelLineModel.get("length");
    labelLineLen = parsePercent2(labelLineLen, viewWidth);
    var labelLineLen2 = labelLineModel.get("length2");
    labelLineLen2 = parsePercent2(labelLineLen2, viewWidth);
    if (Math.abs(sectorShape.endAngle - sectorShape.startAngle) < minShowLabelRadian) {
      each(label2.states, setNotShow);
      label2.ignore = true;
      if (labelLine2) {
        each(labelLine2.states, setNotShow);
        labelLine2.ignore = true;
      }
      return;
    }
    if (!isLabelShown(label2)) {
      return;
    }
    var midAngle = (sectorShape.startAngle + sectorShape.endAngle) / 2;
    var nx = Math.cos(midAngle);
    var ny = Math.sin(midAngle);
    var textX;
    var textY;
    var linePoints2;
    var textAlign;
    cx = sectorShape.cx;
    cy = sectorShape.cy;
    var isLabelInside = labelPosition === "inside" || labelPosition === "inner";
    if (labelPosition === "center") {
      textX = sectorShape.cx;
      textY = sectorShape.cy;
      textAlign = "center";
    } else {
      var x1 = (isLabelInside ? (sectorShape.r + sectorShape.r0) / 2 * nx : sectorShape.r * nx) + cx;
      var y1 = (isLabelInside ? (sectorShape.r + sectorShape.r0) / 2 * ny : sectorShape.r * ny) + cy;
      textX = x1 + nx * 3;
      textY = y1 + ny * 3;
      if (!isLabelInside) {
        var x2 = x1 + nx * (labelLineLen + r - sectorShape.r);
        var y2 = y1 + ny * (labelLineLen + r - sectorShape.r);
        var x3 = x2 + (nx < 0 ? -1 : 1) * labelLineLen2;
        var y3 = y2;
        if (labelAlignTo === "edge") {
          textX = nx < 0 ? viewLeft + edgeDistance : viewLeft + viewWidth - edgeDistance;
        } else {
          textX = x3 + (nx < 0 ? -labelDistance : labelDistance);
        }
        textY = y3;
        linePoints2 = [[x1, y1], [x2, y2], [x3, y3]];
      }
      textAlign = isLabelInside ? "center" : labelAlignTo === "edge" ? nx > 0 ? "right" : "left" : nx > 0 ? "left" : "right";
    }
    var PI10 = Math.PI;
    var labelRotate = 0;
    var rotate2 = labelModel.get("rotate");
    if (isNumber(rotate2)) {
      labelRotate = rotate2 * (PI10 / 180);
    } else if (labelPosition === "center") {
      labelRotate = 0;
    } else if (rotate2 === "radial" || rotate2 === true) {
      var radialAngle = nx < 0 ? -midAngle + PI10 : -midAngle;
      labelRotate = radialAngle;
    } else if (rotate2 === "tangential" || rotate2 === "tangential-noflip" && labelPosition !== "outside" && labelPosition !== "outer") {
      var rad = Math.atan2(nx, ny);
      if (rad < 0) {
        rad = PI10 * 2 + rad;
      }
      var isDown = ny > 0;
      if (isDown && rotate2 !== "tangential-noflip") {
        rad = PI10 + rad;
      }
      labelRotate = rad - PI10;
    }
    hasLabelRotate = !!labelRotate;
    label2.x = textX;
    label2.y = textY;
    label2.rotation = labelRotate;
    label2.setStyle({
      verticalAlign: "middle"
    });
    if (!isLabelInside) {
      var textRect = new BoundingRect_default(0, 0, 0, 0);
      computeLabelGlobalRect(textRect, label2);
      labelLayoutList.push({
        label: label2,
        labelLine: labelLine2,
        position: labelPosition,
        len: labelLineLen,
        len2: labelLineLen2,
        minTurnAngle: labelLineModel.get("minTurnAngle"),
        maxSurfaceAngle: labelLineModel.get("maxSurfaceAngle"),
        surfaceNormal: new Point_default(nx, ny),
        linePoints: linePoints2,
        textAlign,
        labelDistance,
        labelAlignTo,
        edgeDistance,
        bleedMargin,
        rect: textRect,
        unconstrainedWidth: textRect.width,
        labelStyleWidth: label2.style.width
      });
    } else {
      label2.setStyle({
        align: textAlign
      });
      var selectState2 = label2.states.select;
      if (selectState2) {
        selectState2.x += label2.x;
        selectState2.y += label2.y;
      }
    }
    sector.setTextConfig({
      inside: isLabelInside
    });
  });
  if (!hasLabelRotate && seriesModel.get("avoidLabelOverlap")) {
    avoidOverlap(labelLayoutList, cx, cy, r, viewWidth, viewHeight, viewLeft, viewTop);
  }
  for (var i2 = 0; i2 < labelLayoutList.length; i2++) {
    var layout3 = labelLayoutList[i2];
    var label = layout3.label;
    var labelLine = layout3.labelLine;
    var notShowLabel = isNaN(label.x) || isNaN(label.y);
    if (label) {
      label.setStyle({
        align: layout3.textAlign
      });
      if (notShowLabel) {
        each(label.states, setNotShow);
        label.ignore = true;
      }
      var selectState = label.states.select;
      if (selectState) {
        selectState.x += label.x;
        selectState.y += label.y;
      }
    }
    if (labelLine) {
      var linePoints = layout3.linePoints;
      if (notShowLabel || !linePoints) {
        each(labelLine.states, setNotShow);
        labelLine.ignore = true;
      } else {
        limitTurnAngle(linePoints, layout3.minTurnAngle);
        limitSurfaceAngle(linePoints, layout3.surfaceNormal, layout3.maxSurfaceAngle);
        labelLine.setShape({
          points: linePoints
        });
        label.__hostTarget.textGuideLineConfig = {
          anchor: new Point_default(linePoints[0][0], linePoints[0][1])
        };
      }
    }
  }
}

// node_modules/echarts/lib/chart/pie/pieLayout.js
var PI28 = Math.PI * 2;
var RADIAN2 = Math.PI / 180;
var pieLayoutStageHandler = createSimpleOverallStageHandler(SERIES_TYPE_PIE, pieLayout);
function pieLayout(ecModel, api) {
  ecModel.eachSeriesByType(SERIES_TYPE_PIE, function(seriesModel) {
    var data = seriesModel.getData();
    var valueDim = data.mapDimension("value");
    var _a2 = getCircleLayout(seriesModel, api), cx = _a2.cx, cy = _a2.cy, r = _a2.r, r0 = _a2.r0, viewRect2 = _a2.viewRect;
    var startAngle = -seriesModel.get("startAngle") * RADIAN2;
    var endAngle = seriesModel.get("endAngle");
    var padAngle = seriesModel.get("padAngle") * RADIAN2;
    endAngle = endAngle === "auto" ? startAngle - PI28 : -endAngle * RADIAN2;
    var minAngle = seriesModel.get("minAngle") * RADIAN2;
    var minAndPadAngle = minAngle + padAngle;
    var validDataCount = 0;
    data.each(valueDim, function(value) {
      !isNaN(value) && validDataCount++;
    });
    var sum2 = data.getSum(valueDim);
    var unitRadian = Math.PI / (sum2 || validDataCount) * 2;
    var clockwise = seriesModel.get("clockwise");
    var roseType = seriesModel.get("roseType");
    var stillShowZeroSum = seriesModel.get("stillShowZeroSum");
    var extent = data.getDataExtent(valueDim);
    extent[0] = 0;
    var dir3 = clockwise ? 1 : -1;
    var angles = [startAngle, endAngle];
    var halfPadAngle = dir3 * padAngle / 2;
    normalizeArcAngles(angles, !clockwise);
    startAngle = angles[0], endAngle = angles[1];
    var layoutData = getSeriesLayoutData(seriesModel);
    layoutData.startAngle = startAngle;
    layoutData.endAngle = endAngle;
    layoutData.clockwise = clockwise;
    layoutData.cx = cx;
    layoutData.cy = cy;
    layoutData.r = r;
    layoutData.r0 = r0;
    var angleRange = Math.abs(endAngle - startAngle);
    var restAngle = angleRange;
    var valueSumLargerThanMinAngle = 0;
    var currentAngle = startAngle;
    data.setLayout({
      viewRect: viewRect2,
      r
    });
    data.each(valueDim, function(value, idx) {
      var angle;
      if (isNaN(value)) {
        data.setItemLayout(idx, {
          angle: NaN,
          startAngle: NaN,
          endAngle: NaN,
          clockwise,
          cx,
          cy,
          r0,
          r: roseType ? NaN : r
        });
        return;
      }
      if (roseType !== "area") {
        angle = sum2 === 0 && stillShowZeroSum ? unitRadian : value * unitRadian;
      } else {
        angle = angleRange / validDataCount;
      }
      if (angle < minAndPadAngle) {
        angle = minAndPadAngle;
        restAngle -= minAndPadAngle;
      } else {
        valueSumLargerThanMinAngle += value;
      }
      var endAngle2 = currentAngle + dir3 * angle;
      var actualStartAngle = 0;
      var actualEndAngle = 0;
      if (padAngle > angle) {
        actualStartAngle = currentAngle + dir3 * angle / 2;
        actualEndAngle = actualStartAngle;
      } else {
        actualStartAngle = currentAngle + halfPadAngle;
        actualEndAngle = endAngle2 - halfPadAngle;
      }
      data.setItemLayout(idx, {
        angle,
        startAngle: actualStartAngle,
        endAngle: actualEndAngle,
        clockwise,
        cx,
        cy,
        r0,
        r: roseType ? linearMap(value, extent, [r0, r]) : r
      });
      currentAngle = endAngle2;
    });
    if (restAngle < PI28 && validDataCount) {
      if (restAngle <= 1e-3) {
        var angle_1 = angleRange / validDataCount;
        data.each(valueDim, function(value, idx) {
          if (!isNaN(value)) {
            var layout3 = data.getItemLayout(idx);
            layout3.angle = angle_1;
            var actualStartAngle = 0;
            var actualEndAngle = 0;
            if (angle_1 < padAngle) {
              actualStartAngle = startAngle + dir3 * (idx + 1 / 2) * angle_1;
              actualEndAngle = actualStartAngle;
            } else {
              actualStartAngle = startAngle + dir3 * idx * angle_1 + halfPadAngle;
              actualEndAngle = startAngle + dir3 * (idx + 1) * angle_1 - halfPadAngle;
            }
            layout3.startAngle = actualStartAngle;
            layout3.endAngle = actualEndAngle;
          }
        });
      } else {
        unitRadian = restAngle / valueSumLargerThanMinAngle;
        currentAngle = startAngle;
        data.each(valueDim, function(value, idx) {
          if (!isNaN(value)) {
            var layout3 = data.getItemLayout(idx);
            var angle = layout3.angle === minAndPadAngle ? minAndPadAngle : value * unitRadian;
            var actualStartAngle = 0;
            var actualEndAngle = 0;
            if (angle < padAngle) {
              actualStartAngle = currentAngle + dir3 * angle / 2;
              actualEndAngle = actualStartAngle;
            } else {
              actualStartAngle = currentAngle + halfPadAngle;
              actualEndAngle = currentAngle + dir3 * angle - halfPadAngle;
            }
            layout3.startAngle = actualStartAngle;
            layout3.endAngle = actualEndAngle;
            currentAngle += dir3 * angle;
          }
        });
      }
    }
  });
}
var getSeriesLayoutData = makeInner();

// node_modules/echarts/lib/chart/pie/PieView.js
var PiePiece = (
  /** @class */
  (function(_super) {
    __extends(PiePiece2, _super);
    function PiePiece2(data, idx, startAngle) {
      var _this = _super.call(this) || this;
      _this.z2 = 2;
      var text = new Text_default();
      _this.setTextContent(text);
      _this.updateData(data, idx, startAngle, true);
      return _this;
    }
    PiePiece2.prototype.updateData = function(data, idx, startAngle, firstCreate) {
      var sector = this;
      var seriesModel = data.hostModel;
      var itemModel = data.getItemModel(idx);
      var emphasisModel = itemModel.getModel("emphasis");
      var layout3 = data.getItemLayout(idx);
      var sectorShape = extend(getSectorCornerRadius(itemModel.getModel("itemStyle"), layout3, true), layout3);
      if (isNaN(sectorShape.startAngle)) {
        sector.setShape(sectorShape);
        return;
      }
      if (firstCreate) {
        sector.setShape(sectorShape);
        var animationType = seriesModel.getShallow("animationType");
        if (seriesModel.ecModel.ssr) {
          initProps(sector, {
            scaleX: 0,
            scaleY: 0
          }, seriesModel, {
            dataIndex: idx,
            isFrom: true
          });
          sector.originX = sectorShape.cx;
          sector.originY = sectorShape.cy;
        } else if (animationType === "scale") {
          sector.shape.r = layout3.r0;
          initProps(sector, {
            shape: {
              r: layout3.r
            }
          }, seriesModel, idx);
        } else {
          if (startAngle != null) {
            sector.setShape({
              startAngle,
              endAngle: startAngle
            });
            initProps(sector, {
              shape: {
                startAngle: layout3.startAngle,
                endAngle: layout3.endAngle
              }
            }, seriesModel, idx);
          } else {
            sector.shape.endAngle = layout3.startAngle;
            updateProps(sector, {
              shape: {
                endAngle: layout3.endAngle
              }
            }, seriesModel, idx);
          }
        }
      } else {
        saveOldStyle(sector);
        updateProps(sector, {
          shape: sectorShape
        }, seriesModel, idx);
      }
      sector.useStyle(data.getItemVisual(idx, "style"));
      setStatesStylesFromModel(sector, itemModel);
      var midAngle = (layout3.startAngle + layout3.endAngle) / 2;
      var offset = seriesModel.get("selectedOffset");
      var dx = Math.cos(midAngle) * offset;
      var dy = Math.sin(midAngle) * offset;
      var cursorStyle = itemModel.getShallow("cursor");
      cursorStyle && sector.attr("cursor", cursorStyle);
      this._updateLabel(seriesModel, data, idx);
      sector.ensureState("emphasis").shape = extend({
        r: layout3.r + (emphasisModel.get("scale") ? emphasisModel.get("scaleSize") || 0 : 0)
      }, getSectorCornerRadius(emphasisModel.getModel("itemStyle"), layout3));
      extend(sector.ensureState("select"), {
        x: dx,
        y: dy,
        shape: getSectorCornerRadius(itemModel.getModel(["select", "itemStyle"]), layout3)
      });
      extend(sector.ensureState("blur"), {
        shape: getSectorCornerRadius(itemModel.getModel(["blur", "itemStyle"]), layout3)
      });
      var labelLine = sector.getTextGuideLine();
      var labelText = sector.getTextContent();
      labelLine && extend(labelLine.ensureState("select"), {
        x: dx,
        y: dy
      });
      extend(labelText.ensureState("select"), {
        x: dx,
        y: dy
      });
      toggleHoverEmphasis(this, emphasisModel.get("focus"), emphasisModel.get("blurScope"), emphasisModel.get("disabled"));
    };
    PiePiece2.prototype._updateLabel = function(seriesModel, data, idx) {
      var sector = this;
      var itemModel = data.getItemModel(idx);
      var labelLineModel = itemModel.getModel("labelLine");
      var style2 = data.getItemVisual(idx, "style");
      var visualColor = style2 && style2.fill;
      var visualOpacity = style2 && style2.opacity;
      setLabelStyle(sector, getLabelStatesModels(itemModel), {
        labelFetcher: data.hostModel,
        labelDataIndex: idx,
        inheritColor: visualColor,
        defaultOpacity: visualOpacity,
        defaultText: seriesModel.getFormattedLabel(idx, "normal") || data.getName(idx)
      });
      var labelText = sector.getTextContent();
      sector.setTextConfig({
        // reset position, rotation
        position: null,
        rotation: null
      });
      labelText.attr({
        z2: 10
      });
      var labelPosition = itemModel.get(["label", "position"]);
      if (labelPosition !== "outside" && labelPosition !== "outer") {
        sector.removeTextGuideLine();
      } else {
        var polyline = this.getTextGuideLine();
        if (!polyline) {
          polyline = new Polyline_default();
          this.setTextGuideLine(polyline);
        }
        setLabelLineStyle(this, getLabelLineStatesModels(itemModel), {
          stroke: visualColor,
          opacity: retrieve3(labelLineModel.get(["lineStyle", "opacity"]), visualOpacity, 1)
        });
      }
    };
    return PiePiece2;
  })(Sector_default)
);
var PieView = (
  /** @class */
  (function(_super) {
    __extends(PieView2, _super);
    function PieView2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = SERIES_TYPE_PIE;
      _this.ignoreLabelLineUpdate = true;
      return _this;
    }
    PieView2.prototype.render = function(seriesModel, ecModel, api, payload) {
      var data = seriesModel.getData();
      var oldData = this._data;
      var group2 = this.group;
      var startAngle;
      if (!oldData && data.count() > 0) {
        var shape = data.getItemLayout(0);
        for (var s = 1; isNaN(shape && shape.startAngle) && s < data.count(); ++s) {
          shape = data.getItemLayout(s);
        }
        if (shape) {
          startAngle = shape.startAngle;
        }
      }
      if (this._emptyCircleSector) {
        group2.remove(this._emptyCircleSector);
      }
      if (data.count() === 0 && seriesModel.get("showEmptyCircle")) {
        var layoutData = getSeriesLayoutData(seriesModel);
        var sector = new Sector_default({
          shape: clone(layoutData)
        });
        sector.useStyle(seriesModel.getModel("emptyCircleStyle").getItemStyle());
        this._emptyCircleSector = sector;
        group2.add(sector);
      }
      data.diff(oldData).add(function(idx) {
        var piePiece = new PiePiece(data, idx, startAngle);
        data.setItemGraphicEl(idx, piePiece);
        group2.add(piePiece);
      }).update(function(newIdx, oldIdx) {
        var piePiece = oldData.getItemGraphicEl(oldIdx);
        piePiece.updateData(data, newIdx, startAngle);
        piePiece.off("click");
        group2.add(piePiece);
        data.setItemGraphicEl(newIdx, piePiece);
      }).remove(function(idx) {
        var piePiece = oldData.getItemGraphicEl(idx);
        removeElementWithFadeOut(piePiece, seriesModel, idx);
      }).execute();
      pieLabelLayout(seriesModel);
      if (seriesModel.get("animationTypeUpdate") !== "expansion") {
        this._data = data;
      }
    };
    PieView2.prototype.dispose = function() {
    };
    PieView2.prototype.containPoint = function(point, seriesModel) {
      var data = seriesModel.getData();
      var itemLayout = data.getItemLayout(0);
      if (itemLayout) {
        var dx = point[0] - itemLayout.cx;
        var dy = point[1] - itemLayout.cy;
        var radius = Math.sqrt(dx * dx + dy * dy);
        return radius <= itemLayout.r && radius >= itemLayout.r0;
      }
    };
    PieView2.type = SERIES_TYPE_PIE;
    return PieView2;
  })(Chart_default)
);
var PieView_default = PieView;

// node_modules/echarts/lib/processor/negativeDataFilter.js
function negativeDataFilter(seriesType2) {
  return {
    seriesType: seriesType2,
    reset: function(seriesModel, ecModel) {
      var data = seriesModel.getData();
      data.filterSelf(function(idx) {
        var valueDim = data.mapDimension("value");
        var curValue = data.get(valueDim, idx);
        if (isNumber(curValue) && !isNaN(curValue) && curValue < 0) {
          return false;
        }
        return true;
      });
    }
  };
}

// node_modules/echarts/lib/chart/pie/install.js
function install3(registers) {
  registers.registerChartView(PieView_default);
  registers.registerSeriesModel(PieSeries_default);
  createLegacyDataSelectAction(SERIES_TYPE_PIE, registers.registerAction);
  registers.registerLayout(pieLayoutStageHandler);
  registers.registerProcessor(dataFilter(SERIES_TYPE_PIE));
  registers.registerProcessor(negativeDataFilter(SERIES_TYPE_PIE));
}

// node_modules/zrender/lib/mixin/Draggable.js
var Param = /* @__PURE__ */ (function() {
  function Param2(target, e2) {
    this.target = target;
    this.topTarget = e2 && e2.topTarget;
  }
  return Param2;
})();
var Draggable = (function() {
  function Draggable2(handler) {
    this.handler = handler;
    handler.on("mousedown", this._dragStart, this);
    handler.on("mousemove", this._drag, this);
    handler.on("mouseup", this._dragEnd, this);
  }
  Draggable2.prototype._dragStart = function(e2) {
    var draggingTarget = e2.target;
    while (draggingTarget && !draggingTarget.draggable) {
      draggingTarget = draggingTarget.parent || draggingTarget.__hostTarget;
    }
    if (draggingTarget) {
      this._draggingTarget = draggingTarget;
      draggingTarget.dragging = true;
      this._x = e2.offsetX;
      this._y = e2.offsetY;
      this.handler.dispatchToElement(new Param(draggingTarget, e2), "dragstart", e2.event);
    }
  };
  Draggable2.prototype._drag = function(e2) {
    var draggingTarget = this._draggingTarget;
    if (draggingTarget) {
      var x = e2.offsetX;
      var y = e2.offsetY;
      var dx = x - this._x;
      var dy = y - this._y;
      this._x = x;
      this._y = y;
      draggingTarget.drift(dx, dy, e2);
      this.handler.dispatchToElement(new Param(draggingTarget, e2), "drag", e2.event);
      var dropTarget = this.handler.findHover(x, y, draggingTarget).target;
      var lastDropTarget = this._dropTarget;
      this._dropTarget = dropTarget;
      if (draggingTarget !== dropTarget) {
        if (lastDropTarget && dropTarget !== lastDropTarget) {
          this.handler.dispatchToElement(new Param(lastDropTarget, e2), "dragleave", e2.event);
        }
        if (dropTarget && dropTarget !== lastDropTarget) {
          this.handler.dispatchToElement(new Param(dropTarget, e2), "dragenter", e2.event);
        }
      }
    }
  };
  Draggable2.prototype._dragEnd = function(e2) {
    var draggingTarget = this._draggingTarget;
    if (draggingTarget) {
      draggingTarget.dragging = false;
    }
    this.handler.dispatchToElement(new Param(draggingTarget, e2), "dragend", e2.event);
    if (this._dropTarget) {
      this.handler.dispatchToElement(new Param(this._dropTarget, e2), "drop", e2.event);
    }
    this._draggingTarget = null;
    this._dropTarget = null;
  };
  return Draggable2;
})();
var Draggable_default = Draggable;

// node_modules/zrender/lib/core/event.js
var MOUSE_EVENT_REG = /^(?:mouse|pointer|contextmenu|drag|drop)|click/;
var _calcOut2 = [];
var firefoxNotSupportOffsetXY = env_default.browser.firefox && +env_default.browser.version.split(".")[0] < 39;
function clientToLocal(el, e2, out2, calculate) {
  out2 = out2 || {};
  if (calculate) {
    calculateZrXY(el, e2, out2);
  } else if (firefoxNotSupportOffsetXY && e2.layerX != null && e2.layerX !== e2.offsetX) {
    out2.zrX = e2.layerX;
    out2.zrY = e2.layerY;
  } else if (e2.offsetX != null) {
    out2.zrX = e2.offsetX;
    out2.zrY = e2.offsetY;
  } else {
    calculateZrXY(el, e2, out2);
  }
  return out2;
}
function calculateZrXY(el, e2, out2) {
  if (env_default.domSupported && el.getBoundingClientRect) {
    var ex = e2.clientX;
    var ey = e2.clientY;
    if (isCanvasEl(el)) {
      var box2 = el.getBoundingClientRect();
      out2.zrX = ex - box2.left;
      out2.zrY = ey - box2.top;
      return;
    } else {
      if (transformCoordWithViewport(_calcOut2, el, ex, ey)) {
        out2.zrX = _calcOut2[0];
        out2.zrY = _calcOut2[1];
        return;
      }
    }
  }
  out2.zrX = out2.zrY = 0;
}
function getNativeEvent(e2) {
  return e2 || window.event;
}
function normalizeEvent(el, e2, calculate) {
  e2 = getNativeEvent(e2);
  if (e2.zrX != null) {
    return e2;
  }
  var eventType = e2.type;
  var isTouch = eventType && eventType.indexOf("touch") >= 0;
  if (!isTouch) {
    clientToLocal(el, e2, e2, calculate);
    var wheelDelta = getWheelDeltaMayPolyfill(e2);
    e2.zrDelta = wheelDelta ? wheelDelta / 120 : -(e2.detail || 0) / 3;
  } else {
    var touch = eventType !== "touchend" ? e2.targetTouches[0] : e2.changedTouches[0];
    touch && clientToLocal(el, touch, e2, calculate);
  }
  var button = e2.button;
  if (e2.which == null && button !== void 0 && MOUSE_EVENT_REG.test(e2.type)) {
    e2.which = button & 1 ? 1 : button & 2 ? 3 : button & 4 ? 2 : 0;
  }
  return e2;
}
function getWheelDeltaMayPolyfill(e2) {
  var rawWheelDelta = e2.wheelDelta;
  if (rawWheelDelta) {
    return rawWheelDelta;
  }
  var deltaX = e2.deltaX;
  var deltaY = e2.deltaY;
  if (deltaX == null || deltaY == null) {
    return rawWheelDelta;
  }
  var delta = deltaY !== 0 ? Math.abs(deltaY) : Math.abs(deltaX);
  var sign = deltaY > 0 ? -1 : deltaY < 0 ? 1 : deltaX > 0 ? -1 : 1;
  return 3 * delta * sign;
}
function addEventListener(el, name, handler, opt) {
  el.addEventListener(name, handler, opt);
}
function removeEventListener(el, name, handler, opt) {
  el.removeEventListener(name, handler, opt);
}
var stop = function(e2) {
  e2.preventDefault();
  e2.stopPropagation();
  e2.cancelBubble = true;
};
function isMiddleOrRightButtonOnMouseUpDown(e2) {
  return e2.which === 2 || e2.which === 3;
}

// node_modules/zrender/lib/core/GestureMgr.js
var GestureMgr = (function() {
  function GestureMgr2() {
    this._track = [];
  }
  GestureMgr2.prototype.recognize = function(event, target, root) {
    this._doTrack(event, target, root);
    return this._recognize(event);
  };
  GestureMgr2.prototype.clear = function() {
    this._track.length = 0;
    return this;
  };
  GestureMgr2.prototype._doTrack = function(event, target, root) {
    var touches = event.touches;
    if (!touches) {
      return;
    }
    var trackItem = {
      points: [],
      touches: [],
      target,
      event
    };
    for (var i2 = 0, len2 = touches.length; i2 < len2; i2++) {
      var touch = touches[i2];
      var pos = clientToLocal(root, touch, {});
      trackItem.points.push([pos.zrX, pos.zrY]);
      trackItem.touches.push(touch);
    }
    this._track.push(trackItem);
  };
  GestureMgr2.prototype._recognize = function(event) {
    for (var eventName in recognizers) {
      if (recognizers.hasOwnProperty(eventName)) {
        var gestureInfo = recognizers[eventName](this._track, event);
        if (gestureInfo) {
          return gestureInfo;
        }
      }
    }
  };
  return GestureMgr2;
})();
function dist2(pointPair) {
  var dx = pointPair[1][0] - pointPair[0][0];
  var dy = pointPair[1][1] - pointPair[0][1];
  return Math.sqrt(dx * dx + dy * dy);
}
function center(pointPair) {
  return [
    (pointPair[0][0] + pointPair[1][0]) / 2,
    (pointPair[0][1] + pointPair[1][1]) / 2
  ];
}
var recognizers = {
  pinch: function(tracks, event) {
    var trackLen = tracks.length;
    if (!trackLen) {
      return;
    }
    var pinchEnd = (tracks[trackLen - 1] || {}).points;
    var pinchPre = (tracks[trackLen - 2] || {}).points || pinchEnd;
    if (pinchPre && pinchPre.length > 1 && pinchEnd && pinchEnd.length > 1) {
      var pinchScale = dist2(pinchEnd) / dist2(pinchPre);
      !isFinite(pinchScale) && (pinchScale = 1);
      event.pinchScale = pinchScale;
      var pinchCenter = center(pinchEnd);
      event.pinchX = pinchCenter[0];
      event.pinchY = pinchCenter[1];
      return {
        type: "pinch",
        target: tracks[0].target,
        event
      };
    }
  }
};

// node_modules/zrender/lib/Handler.js
var SILENT = "silent";
function makeEventPacket(eveType, targetInfo, event) {
  return {
    type: eveType,
    event,
    target: targetInfo.target,
    topTarget: targetInfo.topTarget,
    cancelBubble: false,
    offsetX: event.zrX,
    offsetY: event.zrY,
    gestureEvent: event.gestureEvent,
    pinchX: event.pinchX,
    pinchY: event.pinchY,
    pinchScale: event.pinchScale,
    wheelDelta: event.zrDelta,
    zrByTouch: event.zrByTouch,
    which: event.which,
    stop: stopEvent
  };
}
function stopEvent() {
  stop(this.event);
}
var EmptyProxy = (function(_super) {
  __extends2(EmptyProxy2, _super);
  function EmptyProxy2() {
    var _this = _super !== null && _super.apply(this, arguments) || this;
    _this.handler = null;
    return _this;
  }
  EmptyProxy2.prototype.dispose = function() {
  };
  EmptyProxy2.prototype.setCursor = function() {
  };
  return EmptyProxy2;
})(Eventful_default);
var HoveredResult = /* @__PURE__ */ (function() {
  function HoveredResult2(x, y) {
    this.x = x;
    this.y = y;
  }
  return HoveredResult2;
})();
var handlerNames = [
  "click",
  "dblclick",
  "mousewheel",
  "mouseout",
  "mouseup",
  "mousedown",
  "mousemove",
  "contextmenu"
];
var tmpRect2 = new BoundingRect_default(0, 0, 0, 0);
var Handler = (function(_super) {
  __extends2(Handler2, _super);
  function Handler2(storage2, painter, proxy, painterRoot, pointerSize) {
    var _this = _super.call(this) || this;
    _this._hovered = new HoveredResult(0, 0);
    _this.storage = storage2;
    _this.painter = painter;
    _this.painterRoot = painterRoot;
    _this._pointerSize = pointerSize;
    proxy = proxy || new EmptyProxy();
    _this.proxy = null;
    _this.setHandlerProxy(proxy);
    _this._draggingMgr = new Draggable_default(_this);
    return _this;
  }
  Handler2.prototype.setHandlerProxy = function(proxy) {
    if (this.proxy) {
      this.proxy.dispose();
    }
    if (proxy) {
      each(handlerNames, function(name) {
        proxy.on && proxy.on(name, this[name], this);
      }, this);
      proxy.handler = this;
    }
    this.proxy = proxy;
  };
  Handler2.prototype.mousemove = function(event) {
    var x = event.zrX;
    var y = event.zrY;
    var isOutside = isOutsideBoundary(this, x, y);
    var lastHovered = this._hovered;
    var lastHoveredTarget = lastHovered.target;
    if (lastHoveredTarget && !lastHoveredTarget.__zr) {
      lastHovered = this.findHover(lastHovered.x, lastHovered.y);
      lastHoveredTarget = lastHovered.target;
    }
    var hovered = this._hovered = isOutside ? new HoveredResult(x, y) : this.findHover(x, y);
    var hoveredTarget = hovered.target;
    var proxy = this.proxy;
    proxy.setCursor && proxy.setCursor(hoveredTarget ? hoveredTarget.cursor : "default");
    if (lastHoveredTarget && hoveredTarget !== lastHoveredTarget) {
      this.dispatchToElement(lastHovered, "mouseout", event);
    }
    this.dispatchToElement(hovered, "mousemove", event);
    if (hoveredTarget && hoveredTarget !== lastHoveredTarget) {
      this.dispatchToElement(hovered, "mouseover", event);
    }
  };
  Handler2.prototype.mouseout = function(event) {
    var eventControl = event.zrEventControl;
    if (eventControl !== "only_globalout") {
      this.dispatchToElement(this._hovered, "mouseout", event);
    }
    if (eventControl !== "no_globalout") {
      this.trigger("globalout", { type: "globalout", event });
    }
  };
  Handler2.prototype.resize = function() {
    this._hovered = new HoveredResult(0, 0);
  };
  Handler2.prototype.dispatch = function(eventName, eventArgs) {
    var handler = this[eventName];
    handler && handler.call(this, eventArgs);
  };
  Handler2.prototype.dispose = function() {
    this.proxy.dispose();
    this.storage = null;
    this.proxy = null;
    this.painter = null;
  };
  Handler2.prototype.setCursorStyle = function(cursorStyle) {
    var proxy = this.proxy;
    proxy.setCursor && proxy.setCursor(cursorStyle);
  };
  Handler2.prototype.dispatchToElement = function(targetInfo, eventName, event) {
    targetInfo = targetInfo || {};
    var el = targetInfo.target;
    if (el && el.silent) {
      return;
    }
    var eventKey = "on" + eventName;
    var eventPacket = makeEventPacket(eventName, targetInfo, event);
    while (el) {
      el[eventKey] && (eventPacket.cancelBubble = !!el[eventKey].call(el, eventPacket));
      el.trigger(eventName, eventPacket);
      el = el.__hostTarget ? el.__hostTarget : el.parent;
      if (eventPacket.cancelBubble) {
        break;
      }
    }
    if (!eventPacket.cancelBubble) {
      this.trigger(eventName, eventPacket);
      if (this.painter && this.painter.eachOtherLayer) {
        this.painter.eachOtherLayer(function(layer) {
          if (typeof layer[eventKey] === "function") {
            layer[eventKey].call(layer, eventPacket);
          }
          if (layer.trigger) {
            layer.trigger(eventName, eventPacket);
          }
        });
      }
    }
  };
  Handler2.prototype.findHover = function(x, y, exclude) {
    var list = this.storage.getDisplayList();
    var out2 = new HoveredResult(x, y);
    setHoverTarget(list, out2, x, y, exclude);
    if (this._pointerSize && !out2.target) {
      var candidates = [];
      var pointerSize = this._pointerSize;
      var targetSizeHalf = pointerSize / 2;
      var pointerRect = new BoundingRect_default(x - targetSizeHalf, y - targetSizeHalf, pointerSize, pointerSize);
      for (var i2 = list.length - 1; i2 >= 0; i2--) {
        var el = list[i2];
        if (el !== exclude && !el.ignore && !el.ignoreCoarsePointer && (!el.parent || !el.parent.ignoreCoarsePointer)) {
          tmpRect2.copy(el.getBoundingRect());
          if (el.transform) {
            tmpRect2.applyTransform(el.transform);
          }
          if (tmpRect2.intersect(pointerRect)) {
            candidates.push(el);
          }
        }
      }
      if (candidates.length) {
        var rStep = 4;
        var thetaStep = Math.PI / 12;
        var PI210 = Math.PI * 2;
        for (var r = 0; r < targetSizeHalf; r += rStep) {
          for (var theta = 0; theta < PI210; theta += thetaStep) {
            var x1 = x + r * Math.cos(theta);
            var y1 = y + r * Math.sin(theta);
            setHoverTarget(candidates, out2, x1, y1, exclude);
            if (out2.target) {
              return out2;
            }
          }
        }
      }
    }
    return out2;
  };
  Handler2.prototype.processGesture = function(event, stage) {
    if (!this._gestureMgr) {
      this._gestureMgr = new GestureMgr();
    }
    var gestureMgr = this._gestureMgr;
    stage === "start" && gestureMgr.clear();
    var gestureInfo = gestureMgr.recognize(event, this.findHover(event.zrX, event.zrY, null).target, this.proxy.dom);
    stage === "end" && gestureMgr.clear();
    if (gestureInfo) {
      var type = gestureInfo.type;
      event.gestureEvent = type;
      var res = new HoveredResult();
      res.target = gestureInfo.target;
      this.dispatchToElement(res, type, gestureInfo.event);
    }
  };
  return Handler2;
})(Eventful_default);
each(["click", "mousedown", "mouseup", "mousewheel", "dblclick", "contextmenu"], function(name) {
  Handler.prototype[name] = function(event) {
    var x = event.zrX;
    var y = event.zrY;
    var isOutside = isOutsideBoundary(this, x, y);
    var hovered;
    var hoveredTarget;
    if (name !== "mouseup" || !isOutside) {
      hovered = this.findHover(x, y);
      hoveredTarget = hovered.target;
    }
    if (name === "mousedown") {
      this._downEl = hoveredTarget;
      this._downPoint = [event.zrX, event.zrY];
      this._upEl = hoveredTarget;
    } else if (name === "mouseup") {
      this._upEl = hoveredTarget;
    } else if (name === "click") {
      if (this._downEl !== this._upEl || !this._downPoint || dist(this._downPoint, [event.zrX, event.zrY]) > 4) {
        return;
      }
      this._downPoint = null;
    }
    this.dispatchToElement(hovered, name, event);
  };
});
function isHover(displayable, x, y) {
  if (displayable[displayable.rectHover ? "rectContain" : "contain"](x, y)) {
    var el = displayable;
    var isSilent = void 0;
    var ignoreClip = false;
    while (el) {
      if (el.ignoreClip) {
        ignoreClip = true;
      }
      if (!ignoreClip) {
        var clipPath = el.getClipPath();
        if (clipPath && !clipPath.contain(x, y)) {
          return false;
        }
      }
      if (el.silent) {
        isSilent = true;
      }
      var hostEl = el.__hostTarget;
      el = hostEl ? el.ignoreHostSilent ? null : hostEl : el.parent;
    }
    return isSilent ? SILENT : true;
  }
  return false;
}
function setHoverTarget(list, out2, x, y, exclude) {
  for (var i2 = list.length - 1; i2 >= 0; i2--) {
    var el = list[i2];
    var hoverCheckResult = void 0;
    if (el !== exclude && !el.ignore && (hoverCheckResult = isHover(el, x, y))) {
      !out2.topTarget && (out2.topTarget = el);
      if (hoverCheckResult !== SILENT) {
        out2.target = el;
        break;
      }
    }
  }
}
function isOutsideBoundary(handlerInstance, x, y) {
  var painter = handlerInstance.painter;
  return x < 0 || x > painter.getWidth() || y < 0 || y > painter.getHeight();
}
var Handler_default = Handler;

// node_modules/zrender/lib/core/timsort.js
var DEFAULT_MIN_MERGE = 32;
var DEFAULT_MIN_GALLOPING = 7;
function minRunLength(n) {
  var r = 0;
  while (n >= DEFAULT_MIN_MERGE) {
    r |= n & 1;
    n >>= 1;
  }
  return n + r;
}
function makeAscendingRun(array, lo, hi, compare2) {
  var runHi = lo + 1;
  if (runHi === hi) {
    return 1;
  }
  if (compare2(array[runHi++], array[lo]) < 0) {
    while (runHi < hi && compare2(array[runHi], array[runHi - 1]) < 0) {
      runHi++;
    }
    reverseRun(array, lo, runHi);
  } else {
    while (runHi < hi && compare2(array[runHi], array[runHi - 1]) >= 0) {
      runHi++;
    }
  }
  return runHi - lo;
}
function reverseRun(array, lo, hi) {
  hi--;
  while (lo < hi) {
    var t = array[lo];
    array[lo++] = array[hi];
    array[hi--] = t;
  }
}
function binaryInsertionSort(array, lo, hi, start2, compare2) {
  if (start2 === lo) {
    start2++;
  }
  for (; start2 < hi; start2++) {
    var pivot = array[start2];
    var left = lo;
    var right = start2;
    var mid;
    while (left < right) {
      mid = left + right >>> 1;
      if (compare2(pivot, array[mid]) < 0) {
        right = mid;
      } else {
        left = mid + 1;
      }
    }
    var n = start2 - left;
    switch (n) {
      case 3:
        array[left + 3] = array[left + 2];
      case 2:
        array[left + 2] = array[left + 1];
      case 1:
        array[left + 1] = array[left];
        break;
      default:
        while (n > 0) {
          array[left + n] = array[left + n - 1];
          n--;
        }
    }
    array[left] = pivot;
  }
}
function gallopLeft(value, array, start2, length, hint, compare2) {
  var lastOffset = 0;
  var maxOffset = 0;
  var offset = 1;
  if (compare2(value, array[start2 + hint]) > 0) {
    maxOffset = length - hint;
    while (offset < maxOffset && compare2(value, array[start2 + hint + offset]) > 0) {
      lastOffset = offset;
      offset = (offset << 1) + 1;
      if (offset <= 0) {
        offset = maxOffset;
      }
    }
    if (offset > maxOffset) {
      offset = maxOffset;
    }
    lastOffset += hint;
    offset += hint;
  } else {
    maxOffset = hint + 1;
    while (offset < maxOffset && compare2(value, array[start2 + hint - offset]) <= 0) {
      lastOffset = offset;
      offset = (offset << 1) + 1;
      if (offset <= 0) {
        offset = maxOffset;
      }
    }
    if (offset > maxOffset) {
      offset = maxOffset;
    }
    var tmp = lastOffset;
    lastOffset = hint - offset;
    offset = hint - tmp;
  }
  lastOffset++;
  while (lastOffset < offset) {
    var m2 = lastOffset + (offset - lastOffset >>> 1);
    if (compare2(value, array[start2 + m2]) > 0) {
      lastOffset = m2 + 1;
    } else {
      offset = m2;
    }
  }
  return offset;
}
function gallopRight(value, array, start2, length, hint, compare2) {
  var lastOffset = 0;
  var maxOffset = 0;
  var offset = 1;
  if (compare2(value, array[start2 + hint]) < 0) {
    maxOffset = hint + 1;
    while (offset < maxOffset && compare2(value, array[start2 + hint - offset]) < 0) {
      lastOffset = offset;
      offset = (offset << 1) + 1;
      if (offset <= 0) {
        offset = maxOffset;
      }
    }
    if (offset > maxOffset) {
      offset = maxOffset;
    }
    var tmp = lastOffset;
    lastOffset = hint - offset;
    offset = hint - tmp;
  } else {
    maxOffset = length - hint;
    while (offset < maxOffset && compare2(value, array[start2 + hint + offset]) >= 0) {
      lastOffset = offset;
      offset = (offset << 1) + 1;
      if (offset <= 0) {
        offset = maxOffset;
      }
    }
    if (offset > maxOffset) {
      offset = maxOffset;
    }
    lastOffset += hint;
    offset += hint;
  }
  lastOffset++;
  while (lastOffset < offset) {
    var m2 = lastOffset + (offset - lastOffset >>> 1);
    if (compare2(value, array[start2 + m2]) < 0) {
      offset = m2;
    } else {
      lastOffset = m2 + 1;
    }
  }
  return offset;
}
function TimSort(array, compare2) {
  var minGallop = DEFAULT_MIN_GALLOPING;
  var runStart;
  var runLength;
  var stackSize = 0;
  var tmp = [];
  runStart = [];
  runLength = [];
  function pushRun(_runStart, _runLength) {
    runStart[stackSize] = _runStart;
    runLength[stackSize] = _runLength;
    stackSize += 1;
  }
  function mergeRuns() {
    while (stackSize > 1) {
      var n = stackSize - 2;
      if (n >= 1 && runLength[n - 1] <= runLength[n] + runLength[n + 1] || n >= 2 && runLength[n - 2] <= runLength[n] + runLength[n - 1]) {
        if (runLength[n - 1] < runLength[n + 1]) {
          n--;
        }
      } else if (runLength[n] > runLength[n + 1]) {
        break;
      }
      mergeAt(n);
    }
  }
  function forceMergeRuns() {
    while (stackSize > 1) {
      var n = stackSize - 2;
      if (n > 0 && runLength[n - 1] < runLength[n + 1]) {
        n--;
      }
      mergeAt(n);
    }
  }
  function mergeAt(i2) {
    var start1 = runStart[i2];
    var length1 = runLength[i2];
    var start2 = runStart[i2 + 1];
    var length2 = runLength[i2 + 1];
    runLength[i2] = length1 + length2;
    if (i2 === stackSize - 3) {
      runStart[i2 + 1] = runStart[i2 + 2];
      runLength[i2 + 1] = runLength[i2 + 2];
    }
    stackSize--;
    var k2 = gallopRight(array[start2], array, start1, length1, 0, compare2);
    start1 += k2;
    length1 -= k2;
    if (length1 === 0) {
      return;
    }
    length2 = gallopLeft(array[start1 + length1 - 1], array, start2, length2, length2 - 1, compare2);
    if (length2 === 0) {
      return;
    }
    if (length1 <= length2) {
      mergeLow(start1, length1, start2, length2);
    } else {
      mergeHigh(start1, length1, start2, length2);
    }
  }
  function mergeLow(start1, length1, start2, length2) {
    var i2 = 0;
    for (i2 = 0; i2 < length1; i2++) {
      tmp[i2] = array[start1 + i2];
    }
    var cursor1 = 0;
    var cursor2 = start2;
    var dest = start1;
    array[dest++] = array[cursor2++];
    if (--length2 === 0) {
      for (i2 = 0; i2 < length1; i2++) {
        array[dest + i2] = tmp[cursor1 + i2];
      }
      return;
    }
    if (length1 === 1) {
      for (i2 = 0; i2 < length2; i2++) {
        array[dest + i2] = array[cursor2 + i2];
      }
      array[dest + length2] = tmp[cursor1];
      return;
    }
    var _minGallop = minGallop;
    var count1;
    var count2;
    var exit;
    while (1) {
      count1 = 0;
      count2 = 0;
      exit = false;
      do {
        if (compare2(array[cursor2], tmp[cursor1]) < 0) {
          array[dest++] = array[cursor2++];
          count2++;
          count1 = 0;
          if (--length2 === 0) {
            exit = true;
            break;
          }
        } else {
          array[dest++] = tmp[cursor1++];
          count1++;
          count2 = 0;
          if (--length1 === 1) {
            exit = true;
            break;
          }
        }
      } while ((count1 | count2) < _minGallop);
      if (exit) {
        break;
      }
      do {
        count1 = gallopRight(array[cursor2], tmp, cursor1, length1, 0, compare2);
        if (count1 !== 0) {
          for (i2 = 0; i2 < count1; i2++) {
            array[dest + i2] = tmp[cursor1 + i2];
          }
          dest += count1;
          cursor1 += count1;
          length1 -= count1;
          if (length1 <= 1) {
            exit = true;
            break;
          }
        }
        array[dest++] = array[cursor2++];
        if (--length2 === 0) {
          exit = true;
          break;
        }
        count2 = gallopLeft(tmp[cursor1], array, cursor2, length2, 0, compare2);
        if (count2 !== 0) {
          for (i2 = 0; i2 < count2; i2++) {
            array[dest + i2] = array[cursor2 + i2];
          }
          dest += count2;
          cursor2 += count2;
          length2 -= count2;
          if (length2 === 0) {
            exit = true;
            break;
          }
        }
        array[dest++] = tmp[cursor1++];
        if (--length1 === 1) {
          exit = true;
          break;
        }
        _minGallop--;
      } while (count1 >= DEFAULT_MIN_GALLOPING || count2 >= DEFAULT_MIN_GALLOPING);
      if (exit) {
        break;
      }
      if (_minGallop < 0) {
        _minGallop = 0;
      }
      _minGallop += 2;
    }
    minGallop = _minGallop;
    minGallop < 1 && (minGallop = 1);
    if (length1 === 1) {
      for (i2 = 0; i2 < length2; i2++) {
        array[dest + i2] = array[cursor2 + i2];
      }
      array[dest + length2] = tmp[cursor1];
    } else if (length1 === 0) {
      throw new Error();
    } else {
      for (i2 = 0; i2 < length1; i2++) {
        array[dest + i2] = tmp[cursor1 + i2];
      }
    }
  }
  function mergeHigh(start1, length1, start2, length2) {
    var i2 = 0;
    for (i2 = 0; i2 < length2; i2++) {
      tmp[i2] = array[start2 + i2];
    }
    var cursor1 = start1 + length1 - 1;
    var cursor2 = length2 - 1;
    var dest = start2 + length2 - 1;
    var customCursor = 0;
    var customDest = 0;
    array[dest--] = array[cursor1--];
    if (--length1 === 0) {
      customCursor = dest - (length2 - 1);
      for (i2 = 0; i2 < length2; i2++) {
        array[customCursor + i2] = tmp[i2];
      }
      return;
    }
    if (length2 === 1) {
      dest -= length1;
      cursor1 -= length1;
      customDest = dest + 1;
      customCursor = cursor1 + 1;
      for (i2 = length1 - 1; i2 >= 0; i2--) {
        array[customDest + i2] = array[customCursor + i2];
      }
      array[dest] = tmp[cursor2];
      return;
    }
    var _minGallop = minGallop;
    while (true) {
      var count1 = 0;
      var count2 = 0;
      var exit = false;
      do {
        if (compare2(tmp[cursor2], array[cursor1]) < 0) {
          array[dest--] = array[cursor1--];
          count1++;
          count2 = 0;
          if (--length1 === 0) {
            exit = true;
            break;
          }
        } else {
          array[dest--] = tmp[cursor2--];
          count2++;
          count1 = 0;
          if (--length2 === 1) {
            exit = true;
            break;
          }
        }
      } while ((count1 | count2) < _minGallop);
      if (exit) {
        break;
      }
      do {
        count1 = length1 - gallopRight(tmp[cursor2], array, start1, length1, length1 - 1, compare2);
        if (count1 !== 0) {
          dest -= count1;
          cursor1 -= count1;
          length1 -= count1;
          customDest = dest + 1;
          customCursor = cursor1 + 1;
          for (i2 = count1 - 1; i2 >= 0; i2--) {
            array[customDest + i2] = array[customCursor + i2];
          }
          if (length1 === 0) {
            exit = true;
            break;
          }
        }
        array[dest--] = tmp[cursor2--];
        if (--length2 === 1) {
          exit = true;
          break;
        }
        count2 = length2 - gallopLeft(array[cursor1], tmp, 0, length2, length2 - 1, compare2);
        if (count2 !== 0) {
          dest -= count2;
          cursor2 -= count2;
          length2 -= count2;
          customDest = dest + 1;
          customCursor = cursor2 + 1;
          for (i2 = 0; i2 < count2; i2++) {
            array[customDest + i2] = tmp[customCursor + i2];
          }
          if (length2 <= 1) {
            exit = true;
            break;
          }
        }
        array[dest--] = array[cursor1--];
        if (--length1 === 0) {
          exit = true;
          break;
        }
        _minGallop--;
      } while (count1 >= DEFAULT_MIN_GALLOPING || count2 >= DEFAULT_MIN_GALLOPING);
      if (exit) {
        break;
      }
      if (_minGallop < 0) {
        _minGallop = 0;
      }
      _minGallop += 2;
    }
    minGallop = _minGallop;
    if (minGallop < 1) {
      minGallop = 1;
    }
    if (length2 === 1) {
      dest -= length1;
      cursor1 -= length1;
      customDest = dest + 1;
      customCursor = cursor1 + 1;
      for (i2 = length1 - 1; i2 >= 0; i2--) {
        array[customDest + i2] = array[customCursor + i2];
      }
      array[dest] = tmp[cursor2];
    } else if (length2 === 0) {
      throw new Error();
    } else {
      customCursor = dest - (length2 - 1);
      for (i2 = 0; i2 < length2; i2++) {
        array[customCursor + i2] = tmp[i2];
      }
    }
  }
  return {
    mergeRuns,
    forceMergeRuns,
    pushRun
  };
}
function sort(array, compare2, lo, hi) {
  if (!lo) {
    lo = 0;
  }
  if (!hi) {
    hi = array.length;
  }
  var remaining = hi - lo;
  if (remaining < 2) {
    return;
  }
  var runLength = 0;
  if (remaining < DEFAULT_MIN_MERGE) {
    runLength = makeAscendingRun(array, lo, hi, compare2);
    binaryInsertionSort(array, lo, hi, lo + runLength, compare2);
    return;
  }
  var ts = TimSort(array, compare2);
  var minRun = minRunLength(remaining);
  do {
    runLength = makeAscendingRun(array, lo, hi, compare2);
    if (runLength < minRun) {
      var force = remaining;
      if (force > minRun) {
        force = minRun;
      }
      binaryInsertionSort(array, lo, lo + force, lo + runLength, compare2);
      runLength = force;
    }
    ts.pushRun(lo, runLength);
    ts.mergeRuns();
    remaining -= runLength;
    lo += runLength;
  } while (remaining !== 0);
  ts.forceMergeRuns();
}

// node_modules/zrender/lib/Storage.js
var invalidZErrorLogged = false;
function logInvalidZError() {
  if (invalidZErrorLogged) {
    return;
  }
  invalidZErrorLogged = true;
  console.warn("z / z2 / zlevel of displayable is invalid, which may cause unexpected errors");
}
function shapeCompareFunc(a, b) {
  if (a.zlevel === b.zlevel) {
    if (a.z === b.z) {
      return a.z2 - b.z2;
    }
    return a.z - b.z;
  }
  return a.zlevel - b.zlevel;
}
var Storage = (function() {
  function Storage2() {
    this._roots = [];
    this._displayList = [];
    this._displayListLen = 0;
    this.displayableSortFunc = shapeCompareFunc;
  }
  Storage2.prototype.traverse = function(cb, context) {
    for (var i2 = 0; i2 < this._roots.length; i2++) {
      this._roots[i2].traverse(cb, context);
    }
  };
  Storage2.prototype.getDisplayList = function(update, includeIgnore) {
    includeIgnore = includeIgnore || false;
    var displayList = this._displayList;
    if (update || !displayList.length) {
      this.updateDisplayList(includeIgnore);
    }
    return displayList;
  };
  Storage2.prototype.updateDisplayList = function(includeIgnore) {
    this._displayListLen = 0;
    var roots2 = this._roots;
    var displayList = this._displayList;
    for (var i2 = 0, len2 = roots2.length; i2 < len2; i2++) {
      this._updateAndAddDisplayable(roots2[i2], null, includeIgnore);
    }
    displayList.length = this._displayListLen;
    sort(displayList, shapeCompareFunc);
  };
  Storage2.prototype._updateAndAddDisplayable = function(el, parentClipPaths, includeIgnore) {
    if (el.ignore && !includeIgnore) {
      return;
    }
    el.beforeUpdate();
    el.update();
    el.afterUpdate();
    var userSetClipPath = el.getClipPath();
    var parentHasClipPaths = parentClipPaths && parentClipPaths.length;
    var clipPathIdx = 0;
    var thisClipPaths = el.__clipPaths;
    if (!el.ignoreClip && (parentHasClipPaths || userSetClipPath)) {
      if (!thisClipPaths) {
        thisClipPaths = el.__clipPaths = [];
      }
      if (parentHasClipPaths) {
        for (var idx = 0; idx < parentClipPaths.length; idx++) {
          thisClipPaths[clipPathIdx++] = parentClipPaths[idx];
        }
      }
      var currentClipPath = userSetClipPath;
      var parentClipPath = el;
      while (currentClipPath) {
        currentClipPath.parent = parentClipPath;
        currentClipPath.updateTransform();
        thisClipPaths[clipPathIdx++] = currentClipPath;
        parentClipPath = currentClipPath;
        currentClipPath = currentClipPath.getClipPath();
      }
    }
    if (thisClipPaths) {
      thisClipPaths.length = clipPathIdx;
    }
    if (el.childrenRef) {
      var children = el.childrenRef();
      for (var i2 = 0; i2 < children.length; i2++) {
        var child = children[i2];
        if (el.__dirty) {
          child.__dirty |= REDRAW_BIT;
        }
        this._updateAndAddDisplayable(child, thisClipPaths, includeIgnore);
      }
      el.__dirty = 0;
    } else {
      var disp = el;
      if (isNaN(disp.z)) {
        logInvalidZError();
        disp.z = 0;
      }
      if (isNaN(disp.z2)) {
        logInvalidZError();
        disp.z2 = 0;
      }
      if (isNaN(disp.zlevel)) {
        logInvalidZError();
        disp.zlevel = 0;
      }
      this._displayList[this._displayListLen++] = disp;
    }
    var decalEl = el.getDecalElement && el.getDecalElement();
    if (decalEl) {
      this._updateAndAddDisplayable(decalEl, thisClipPaths, includeIgnore);
    }
    var textGuide = el.getTextGuideLine();
    if (textGuide) {
      this._updateAndAddDisplayable(textGuide, thisClipPaths, includeIgnore);
    }
    var textEl = el.getTextContent();
    if (textEl) {
      this._updateAndAddDisplayable(textEl, thisClipPaths, includeIgnore);
    }
  };
  Storage2.prototype.addRoot = function(el) {
    if (el.__zr && el.__zr.storage === this) {
      return;
    }
    this._roots.push(el);
  };
  Storage2.prototype.delRoot = function(el) {
    if (el instanceof Array) {
      for (var i2 = 0, l = el.length; i2 < l; i2++) {
        this.delRoot(el[i2]);
      }
      return;
    }
    var idx = indexOf(this._roots, el);
    if (idx >= 0) {
      this._roots.splice(idx, 1);
    }
  };
  Storage2.prototype.delAllRoots = function() {
    this._roots = [];
    this._displayList = [];
    this._displayListLen = 0;
    return;
  };
  Storage2.prototype.getRoots = function() {
    return this._roots;
  };
  Storage2.prototype.dispose = function() {
    this._displayList = null;
    this._roots = null;
  };
  return Storage2;
})();
var Storage_default = Storage;

// node_modules/zrender/lib/animation/requestAnimationFrame.js
var requestAnimationFrame2;
requestAnimationFrame2 = env_default.hasGlobalWindow && (window.requestAnimationFrame && window.requestAnimationFrame.bind(window) || window.msRequestAnimationFrame && window.msRequestAnimationFrame.bind(window) || window.mozRequestAnimationFrame || window.webkitRequestAnimationFrame) || function(func) {
  return setTimeout(func, 16);
};
var requestAnimationFrame_default = requestAnimationFrame2;

// node_modules/zrender/lib/animation/Animation.js
function getTime() {
  return (/* @__PURE__ */ new Date()).getTime();
}
var Animation = (function(_super) {
  __extends2(Animation2, _super);
  function Animation2(opts) {
    var _this = _super.call(this) || this;
    _this._running = false;
    _this._time = 0;
    _this._pausedTime = 0;
    _this._pauseStart = 0;
    _this._paused = false;
    opts = opts || {};
    _this.stage = opts.stage || {};
    return _this;
  }
  Animation2.prototype.addClip = function(clip2) {
    if (clip2.animation) {
      this.removeClip(clip2);
    }
    if (!this._head) {
      this._head = this._tail = clip2;
    } else {
      this._tail.next = clip2;
      clip2.prev = this._tail;
      clip2.next = null;
      this._tail = clip2;
    }
    clip2.animation = this;
  };
  Animation2.prototype.addAnimator = function(animator) {
    animator.animation = this;
    var clip2 = animator.getClip();
    if (clip2) {
      this.addClip(clip2);
    }
  };
  Animation2.prototype.removeClip = function(clip2) {
    if (!clip2.animation) {
      return;
    }
    var prev = clip2.prev;
    var next = clip2.next;
    if (prev) {
      prev.next = next;
    } else {
      this._head = next;
    }
    if (next) {
      next.prev = prev;
    } else {
      this._tail = prev;
    }
    clip2.next = clip2.prev = clip2.animation = null;
  };
  Animation2.prototype.removeAnimator = function(animator) {
    var clip2 = animator.getClip();
    if (clip2) {
      this.removeClip(clip2);
    }
    animator.animation = null;
  };
  Animation2.prototype.update = function(notTriggerFrameAndStageUpdate) {
    var time = getTime() - this._pausedTime;
    var delta = time - this._time;
    var clip2 = this._head;
    while (clip2) {
      var nextClip = clip2.next;
      var finished = clip2.step(time, delta);
      if (finished) {
        clip2.ondestroy();
        this.removeClip(clip2);
        clip2 = nextClip;
      } else {
        clip2 = nextClip;
      }
    }
    this._time = time;
    if (!notTriggerFrameAndStageUpdate) {
      this.trigger("frame", delta);
      this.stage.update && this.stage.update();
    }
  };
  Animation2.prototype._startLoop = function() {
    var self2 = this;
    this._running = true;
    function step() {
      if (self2._running) {
        requestAnimationFrame_default(step);
        !self2._paused && self2.update();
      }
    }
    requestAnimationFrame_default(step);
  };
  Animation2.prototype.start = function() {
    if (this._running) {
      return;
    }
    this._time = getTime();
    this._pausedTime = 0;
    this._startLoop();
  };
  Animation2.prototype.stop = function() {
    this._running = false;
  };
  Animation2.prototype.pause = function() {
    if (!this._paused) {
      this._pauseStart = getTime();
      this._paused = true;
    }
  };
  Animation2.prototype.resume = function() {
    if (this._paused) {
      this._pausedTime += getTime() - this._pauseStart;
      this._paused = false;
    }
  };
  Animation2.prototype.clear = function() {
    var clip2 = this._head;
    while (clip2) {
      var nextClip = clip2.next;
      clip2.prev = clip2.next = clip2.animation = null;
      clip2 = nextClip;
    }
    this._head = this._tail = null;
  };
  Animation2.prototype.isFinished = function() {
    return this._head == null;
  };
  Animation2.prototype.animate = function(target, options) {
    options = options || {};
    this.start();
    var animator = new Animator_default(target, options.loop);
    this.addAnimator(animator);
    return animator;
  };
  return Animation2;
})(Eventful_default);
var Animation_default = Animation;

// node_modules/zrender/lib/dom/HandlerProxy.js
var TOUCH_CLICK_DELAY = 300;
var globalEventSupported = env_default.domSupported;
var localNativeListenerNames = (function() {
  var mouseHandlerNames = [
    "click",
    "dblclick",
    "mousewheel",
    "wheel",
    "mouseout",
    "mouseup",
    "mousedown",
    "mousemove",
    "contextmenu"
  ];
  var touchHandlerNames = [
    "touchstart",
    "touchend",
    "touchmove"
  ];
  var pointerEventNameMap = {
    pointerdown: 1,
    pointerup: 1,
    pointermove: 1,
    pointerout: 1
  };
  var pointerHandlerNames = map2(mouseHandlerNames, function(name) {
    var nm = name.replace("mouse", "pointer");
    return pointerEventNameMap.hasOwnProperty(nm) ? nm : name;
  });
  return {
    mouse: mouseHandlerNames,
    touch: touchHandlerNames,
    pointer: pointerHandlerNames
  };
})();
var globalNativeListenerNames = {
  mouse: ["mousemove", "mouseup"],
  pointer: ["pointermove", "pointerup"]
};
var wheelEventSupported = false;
function isPointerFromTouch(event) {
  var pointerType = event.pointerType;
  return pointerType === "pen" || pointerType === "touch";
}
function setTouchTimer(scope) {
  scope.touching = true;
  if (scope.touchTimer != null) {
    clearTimeout(scope.touchTimer);
    scope.touchTimer = null;
  }
  scope.touchTimer = setTimeout(function() {
    scope.touching = false;
    scope.touchTimer = null;
  }, 700);
}
function markTouch(event) {
  event && (event.zrByTouch = true);
}
function normalizeGlobalEvent(instance, event) {
  return normalizeEvent(instance.dom, new FakeGlobalEvent(instance, event), true);
}
function isLocalEl(instance, el) {
  var elTmp = el;
  var isLocal = false;
  while (elTmp && elTmp.nodeType !== 9 && !(isLocal = elTmp.domBelongToZr || elTmp !== el && elTmp === instance.painterRoot)) {
    elTmp = elTmp.parentNode;
  }
  return isLocal;
}
var FakeGlobalEvent = /* @__PURE__ */ (function() {
  function FakeGlobalEvent2(instance, event) {
    this.stopPropagation = noop;
    this.stopImmediatePropagation = noop;
    this.preventDefault = noop;
    this.type = event.type;
    this.target = this.currentTarget = instance.dom;
    this.pointerType = event.pointerType;
    this.clientX = event.clientX;
    this.clientY = event.clientY;
  }
  return FakeGlobalEvent2;
})();
var localDOMHandlers = {
  mousedown: function(event) {
    event = normalizeEvent(this.dom, event);
    this.__mayPointerCapture = [event.zrX, event.zrY];
    this.trigger("mousedown", event);
  },
  mousemove: function(event) {
    event = normalizeEvent(this.dom, event);
    var downPoint = this.__mayPointerCapture;
    if (downPoint && (event.zrX !== downPoint[0] || event.zrY !== downPoint[1])) {
      this.__togglePointerCapture(true);
    }
    this.trigger("mousemove", event);
  },
  mouseup: function(event) {
    event = normalizeEvent(this.dom, event);
    this.__togglePointerCapture(false);
    this.trigger("mouseup", event);
  },
  mouseout: function(event) {
    event = normalizeEvent(this.dom, event);
    var element = event.toElement || event.relatedTarget;
    if (!isLocalEl(this, element)) {
      if (this.__pointerCapturing) {
        event.zrEventControl = "no_globalout";
      }
      this.trigger("mouseout", event);
    }
  },
  wheel: function(event) {
    wheelEventSupported = true;
    event = normalizeEvent(this.dom, event);
    this.trigger("mousewheel", event);
  },
  mousewheel: function(event) {
    if (wheelEventSupported) {
      return;
    }
    event = normalizeEvent(this.dom, event);
    this.trigger("mousewheel", event);
  },
  touchstart: function(event) {
    event = normalizeEvent(this.dom, event);
    markTouch(event);
    this.__lastTouchMoment = /* @__PURE__ */ new Date();
    this.handler.processGesture(event, "start");
    localDOMHandlers.mousemove.call(this, event);
    localDOMHandlers.mousedown.call(this, event);
  },
  touchmove: function(event) {
    event = normalizeEvent(this.dom, event);
    markTouch(event);
    this.handler.processGesture(event, "change");
    localDOMHandlers.mousemove.call(this, event);
  },
  touchend: function(event) {
    event = normalizeEvent(this.dom, event);
    markTouch(event);
    this.handler.processGesture(event, "end");
    localDOMHandlers.mouseup.call(this, event);
    if (+/* @__PURE__ */ new Date() - +this.__lastTouchMoment < TOUCH_CLICK_DELAY) {
      localDOMHandlers.click.call(this, event);
    }
  },
  pointerdown: function(event) {
    localDOMHandlers.mousedown.call(this, event);
  },
  pointermove: function(event) {
    if (!isPointerFromTouch(event)) {
      localDOMHandlers.mousemove.call(this, event);
    }
  },
  pointerup: function(event) {
    localDOMHandlers.mouseup.call(this, event);
  },
  pointerout: function(event) {
    if (!isPointerFromTouch(event)) {
      localDOMHandlers.mouseout.call(this, event);
    }
  }
};
each(["click", "dblclick", "contextmenu"], function(name) {
  localDOMHandlers[name] = function(event) {
    event = normalizeEvent(this.dom, event);
    this.trigger(name, event);
  };
});
var globalDOMHandlers = {
  pointermove: function(event) {
    if (!isPointerFromTouch(event)) {
      globalDOMHandlers.mousemove.call(this, event);
    }
  },
  pointerup: function(event) {
    globalDOMHandlers.mouseup.call(this, event);
  },
  mousemove: function(event) {
    this.trigger("mousemove", event);
  },
  mouseup: function(event) {
    var pointerCaptureReleasing = this.__pointerCapturing;
    this.__togglePointerCapture(false);
    this.trigger("mouseup", event);
    if (pointerCaptureReleasing) {
      event.zrEventControl = "only_globalout";
      this.trigger("mouseout", event);
    }
  }
};
function mountLocalDOMEventListeners(instance, scope) {
  var domHandlers = scope.domHandlers;
  if (env_default.pointerEventsSupported) {
    each(localNativeListenerNames.pointer, function(nativeEventName) {
      mountSingleDOMEventListener(scope, nativeEventName, function(event) {
        domHandlers[nativeEventName].call(instance, event);
      });
    });
  } else {
    if (env_default.touchEventsSupported) {
      each(localNativeListenerNames.touch, function(nativeEventName) {
        mountSingleDOMEventListener(scope, nativeEventName, function(event) {
          domHandlers[nativeEventName].call(instance, event);
          setTouchTimer(scope);
        });
      });
    }
    each(localNativeListenerNames.mouse, function(nativeEventName) {
      mountSingleDOMEventListener(scope, nativeEventName, function(event) {
        event = getNativeEvent(event);
        if (!scope.touching) {
          domHandlers[nativeEventName].call(instance, event);
        }
      });
    });
  }
}
function mountGlobalDOMEventListeners(instance, scope) {
  if (env_default.pointerEventsSupported) {
    each(globalNativeListenerNames.pointer, mount);
  } else if (!env_default.touchEventsSupported) {
    each(globalNativeListenerNames.mouse, mount);
  }
  function mount(nativeEventName) {
    function nativeEventListener(event) {
      event = getNativeEvent(event);
      if (!isLocalEl(instance, event.target)) {
        event = normalizeGlobalEvent(instance, event);
        scope.domHandlers[nativeEventName].call(instance, event);
      }
    }
    mountSingleDOMEventListener(scope, nativeEventName, nativeEventListener, { capture: true });
  }
}
function mountSingleDOMEventListener(scope, nativeEventName, listener, opt) {
  scope.mounted[nativeEventName] = listener;
  scope.listenerOpts[nativeEventName] = opt;
  addEventListener(scope.domTarget, nativeEventName, listener, opt);
}
function unmountDOMEventListeners(scope) {
  var mounted = scope.mounted;
  for (var nativeEventName in mounted) {
    if (mounted.hasOwnProperty(nativeEventName)) {
      removeEventListener(scope.domTarget, nativeEventName, mounted[nativeEventName], scope.listenerOpts[nativeEventName]);
    }
  }
  scope.mounted = {};
}
var DOMHandlerScope = /* @__PURE__ */ (function() {
  function DOMHandlerScope2(domTarget, domHandlers) {
    this.mounted = {};
    this.listenerOpts = {};
    this.touching = false;
    this.domTarget = domTarget;
    this.domHandlers = domHandlers;
  }
  return DOMHandlerScope2;
})();
var HandlerDomProxy = (function(_super) {
  __extends2(HandlerDomProxy2, _super);
  function HandlerDomProxy2(dom, painterRoot) {
    var _this = _super.call(this) || this;
    _this.__pointerCapturing = false;
    _this.dom = dom;
    _this.painterRoot = painterRoot;
    _this._localHandlerScope = new DOMHandlerScope(dom, localDOMHandlers);
    if (globalEventSupported) {
      _this._globalHandlerScope = new DOMHandlerScope(document, globalDOMHandlers);
    }
    mountLocalDOMEventListeners(_this, _this._localHandlerScope);
    return _this;
  }
  HandlerDomProxy2.prototype.dispose = function() {
    unmountDOMEventListeners(this._localHandlerScope);
    if (globalEventSupported) {
      unmountDOMEventListeners(this._globalHandlerScope);
    }
  };
  HandlerDomProxy2.prototype.setCursor = function(cursorStyle) {
    this.dom.style && (this.dom.style.cursor = cursorStyle || "default");
  };
  HandlerDomProxy2.prototype.__togglePointerCapture = function(isPointerCapturing) {
    this.__mayPointerCapture = null;
    if (globalEventSupported && +this.__pointerCapturing ^ +isPointerCapturing) {
      this.__pointerCapturing = isPointerCapturing;
      var globalHandlerScope = this._globalHandlerScope;
      isPointerCapturing ? mountGlobalDOMEventListeners(this, globalHandlerScope) : unmountDOMEventListeners(globalHandlerScope);
    }
  };
  return HandlerDomProxy2;
})(Eventful_default);
var HandlerProxy_default = HandlerDomProxy;

// node_modules/zrender/lib/zrender.js
var painterCtors = {};
var instances = {};
function delInstance(id) {
  delete instances[id];
}
function isDarkMode(backgroundColor2) {
  if (!backgroundColor2) {
    return false;
  }
  if (typeof backgroundColor2 === "string") {
    return lum(backgroundColor2, 1) < DARK_MODE_THRESHOLD;
  } else if (backgroundColor2.colorStops) {
    var colorStops = backgroundColor2.colorStops;
    var totalLum = 0;
    var len2 = colorStops.length;
    for (var i2 = 0; i2 < len2; i2++) {
      totalLum += lum(colorStops[i2].color, 1);
    }
    totalLum /= len2;
    return totalLum < DARK_MODE_THRESHOLD;
  }
  return false;
}
var ZRender = (function() {
  function ZRender2(id, dom, opts) {
    var _this = this;
    this._sleepAfterStill = 10;
    this._stillFrameAccum = 0;
    this._needsRefresh = true;
    this._needsRefreshHover = false;
    this._darkMode = false;
    opts = opts || {};
    this.dom = dom;
    this.id = id;
    var storage2 = new Storage_default();
    var rendererType = opts.renderer || "canvas";
    if (!painterCtors[rendererType]) {
      rendererType = keys(painterCtors)[0];
    }
    if (true) {
      if (!painterCtors[rendererType]) {
        throw new Error("Renderer '" + rendererType + "' is not imported. Please import it first.");
      }
    }
    opts.useDirtyRect = opts.useDirtyRect == null ? false : opts.useDirtyRect;
    var painter = new painterCtors[rendererType](dom, storage2, opts, id);
    var ssrMode = opts.ssr || painter.ssrOnly;
    this.storage = storage2;
    this.painter = painter;
    var handlerProxy = !env_default.node && !env_default.worker && !ssrMode ? new HandlerProxy_default(painter.getViewportRoot(), painter.root) : null;
    var useCoarsePointer = opts.useCoarsePointer;
    var usePointerSize = useCoarsePointer == null || useCoarsePointer === "auto" ? env_default.touchEventsSupported : !!useCoarsePointer;
    var defaultPointerSize = 44;
    var pointerSize;
    if (usePointerSize) {
      pointerSize = retrieve2(opts.pointerSize, defaultPointerSize);
    }
    this.handler = new Handler_default(storage2, painter, handlerProxy, painter.root, pointerSize);
    this.animation = new Animation_default({
      stage: {
        update: ssrMode ? null : function() {
          return _this._flush(false);
        }
      }
    });
    if (!ssrMode) {
      this.animation.start();
    }
  }
  ZRender2.prototype.add = function(el) {
    if (this._disposed || !el) {
      return;
    }
    this.storage.addRoot(el);
    el.addSelfToZr(this);
    this.refresh();
  };
  ZRender2.prototype.remove = function(el) {
    if (this._disposed || !el) {
      return;
    }
    this.storage.delRoot(el);
    el.removeSelfFromZr(this);
    this.refresh();
  };
  ZRender2.prototype.configLayer = function(zLevel, config) {
    if (this._disposed) {
      return;
    }
    if (this.painter.configLayer) {
      this.painter.configLayer(zLevel, config);
    }
    this.refresh();
  };
  ZRender2.prototype.setBackgroundColor = function(backgroundColor2) {
    if (this._disposed) {
      return;
    }
    if (this.painter.setBackgroundColor) {
      this.painter.setBackgroundColor(backgroundColor2);
    }
    this.refresh();
    this._backgroundColor = backgroundColor2;
    this._darkMode = isDarkMode(backgroundColor2);
  };
  ZRender2.prototype.getBackgroundColor = function() {
    return this._backgroundColor;
  };
  ZRender2.prototype.setDarkMode = function(darkMode) {
    this._darkMode = darkMode;
  };
  ZRender2.prototype.isDarkMode = function() {
    return this._darkMode;
  };
  ZRender2.prototype.refreshImmediately = function(noAnimationUpdate) {
    if (this._disposed) {
      return;
    }
    this._refresh({
      animUpdate: !noAnimationUpdate,
      refresh: true,
      refreshHover: false
    });
  };
  ZRender2.prototype._refresh = function(opt) {
    if (opt.animUpdate) {
      this.animation.update(true);
    }
    this._needsRefresh = this._needsRefreshHover = false;
    this.painter.refresh({
      refresh: opt.refresh,
      refreshHover: opt.refreshHover
    });
    this._needsRefresh = this._needsRefreshHover = false;
  };
  ZRender2.prototype.refresh = function() {
    if (this._disposed) {
      return;
    }
    this._needsRefresh = true;
    this.animation.start();
  };
  ZRender2.prototype.flush = function() {
    if (this._disposed) {
      return;
    }
    this._flush(true);
  };
  ZRender2.prototype._flush = function(animationUpdate) {
    var triggerRendered;
    var start2 = getTime();
    var needsRefresh = this._needsRefresh;
    var needsRefreshHover = this._needsRefreshHover;
    if (needsRefresh || needsRefreshHover) {
      triggerRendered = true;
      this._refresh({
        animUpdate: animationUpdate,
        refresh: needsRefresh,
        refreshHover: needsRefreshHover
      });
    }
    var end2 = getTime();
    if (triggerRendered) {
      this._stillFrameAccum = 0;
      this.trigger("rendered", {
        elapsedTime: end2 - start2
      });
    } else if (this._sleepAfterStill > 0) {
      this._stillFrameAccum++;
      if (this._stillFrameAccum > this._sleepAfterStill) {
        this.animation.stop();
      }
    }
  };
  ZRender2.prototype.setSleepAfterStill = function(stillFramesCount) {
    this._sleepAfterStill = stillFramesCount;
  };
  ZRender2.prototype.wakeUp = function() {
    if (this._disposed) {
      return;
    }
    this.animation.start();
    this._stillFrameAccum = 0;
  };
  ZRender2.prototype.refreshHover = function() {
    this._needsRefreshHover = true;
  };
  ZRender2.prototype.refreshHoverImmediately = function() {
    if (this._disposed) {
      return;
    }
    this._refresh({
      animUpdate: false,
      refresh: false,
      refreshHover: true
    });
  };
  ZRender2.prototype.resize = function(opts) {
    if (this._disposed) {
      return;
    }
    opts = opts || {};
    this.painter.resize(opts.width, opts.height);
    this.handler.resize();
  };
  ZRender2.prototype.clearAnimation = function() {
    if (this._disposed) {
      return;
    }
    this.animation.clear();
  };
  ZRender2.prototype.getWidth = function() {
    if (this._disposed) {
      return;
    }
    return this.painter.getWidth();
  };
  ZRender2.prototype.getHeight = function() {
    if (this._disposed) {
      return;
    }
    return this.painter.getHeight();
  };
  ZRender2.prototype.setCursorStyle = function(cursorStyle) {
    if (this._disposed) {
      return;
    }
    this.handler.setCursorStyle(cursorStyle);
  };
  ZRender2.prototype.findHover = function(x, y) {
    if (this._disposed) {
      return;
    }
    return this.handler.findHover(x, y);
  };
  ZRender2.prototype.on = function(eventName, eventHandler, context) {
    if (!this._disposed) {
      this.handler.on(eventName, eventHandler, context);
    }
    return this;
  };
  ZRender2.prototype.off = function(eventName, eventHandler) {
    if (this._disposed) {
      return;
    }
    this.handler.off(eventName, eventHandler);
  };
  ZRender2.prototype.trigger = function(eventName, event) {
    if (this._disposed) {
      return;
    }
    this.handler.trigger(eventName, event);
  };
  ZRender2.prototype.clear = function() {
    if (this._disposed) {
      return;
    }
    var roots2 = this.storage.getRoots();
    for (var i2 = 0; i2 < roots2.length; i2++) {
      if (roots2[i2] instanceof Group_default) {
        roots2[i2].removeSelfFromZr(this);
      }
    }
    this.storage.delAllRoots();
    this.painter.clear();
  };
  ZRender2.prototype.dispose = function() {
    if (this._disposed) {
      return;
    }
    this.animation.stop();
    this.clear();
    this.storage.dispose();
    this.painter.dispose();
    this.handler.dispose();
    this.animation = this.storage = this.painter = this.handler = null;
    this._disposed = true;
    delInstance(this.id);
  };
  return ZRender2;
})();
function init(dom, opts) {
  var zr = new ZRender(guid(), dom, opts);
  instances[zr.id] = zr;
  return zr;
}
function registerPainter(name, Ctor) {
  painterCtors[name] = Ctor;
}
var ssrDataGetter;
function getElementSSRData(el) {
  if (typeof ssrDataGetter === "function") {
    return ssrDataGetter(el);
  }
}
function registerSSRDataGetter(getter2) {
  ssrDataGetter = getter2;
}

// node_modules/echarts/lib/model/globalDefault.js
var platform = "";
if (typeof navigator !== "undefined") {
  platform = navigator.platform || "";
}
var decalColor = "rgba(0, 0, 0, 0.2)";
var themeColor = tokens_default.color.theme[0];
var lightThemeColor = modifyHSL(themeColor, null, null, 0.9);
var globalDefault_default = {
  darkMode: "auto",
  // backgroundColor: 'rgba(0,0,0,0)',
  colorBy: "series",
  color: tokens_default.color.theme,
  gradientColor: [lightThemeColor, themeColor],
  aria: {
    decal: {
      decals: [{
        color: decalColor,
        dashArrayX: [1, 0],
        dashArrayY: [2, 5],
        symbolSize: 1,
        rotation: Math.PI / 6
      }, {
        color: decalColor,
        symbol: "circle",
        dashArrayX: [[8, 8], [0, 8, 8, 0]],
        dashArrayY: [6, 0],
        symbolSize: 0.8
      }, {
        color: decalColor,
        dashArrayX: [1, 0],
        dashArrayY: [4, 3],
        rotation: -Math.PI / 4
      }, {
        color: decalColor,
        dashArrayX: [[6, 6], [0, 6, 6, 0]],
        dashArrayY: [6, 0]
      }, {
        color: decalColor,
        dashArrayX: [[1, 0], [1, 6]],
        dashArrayY: [1, 0, 6, 0],
        rotation: Math.PI / 4
      }, {
        color: decalColor,
        symbol: "triangle",
        dashArrayX: [[9, 9], [0, 9, 9, 0]],
        dashArrayY: [7, 2],
        symbolSize: 0.75
      }]
    }
  },
  // If xAxis and yAxis declared, grid is created by default.
  // grid: {},
  textStyle: {
    // color: '#000',
    // decoration: 'none',
    // PENDING
    fontFamily: platform.match(/^Win/) ? "Microsoft YaHei" : "sans-serif",
    // fontFamily: 'Arial, Verdana, sans-serif',
    fontSize: 12,
    fontStyle: "normal",
    fontWeight: "normal"
  },
  // http://blogs.adobe.com/webplatform/2014/02/24/using-blend-modes-in-html-canvas/
  // https://developer.mozilla.org/en-US/docs/Web/API/CanvasRenderingContext2D/globalCompositeOperation
  // Default is source-over
  blendMode: null,
  stateAnimation: {
    duration: 300,
    easing: "cubicOut"
  },
  animation: "auto",
  animationDuration: 1e3,
  animationDurationUpdate: 500,
  animationEasing: "cubicInOut",
  animationEasingUpdate: "cubicInOut",
  animationThreshold: 2e3,
  // Configuration for progressive/incremental rendering
  progressiveThreshold: 3e3,
  progressive: 400,
  // Threshold of if use single hover layer to optimize.
  // It is recommended that `hoverLayerThreshold` is equivalent to or less than
  // `progressiveThreshold`, otherwise hover will cause restart of progressive,
  // which is unexpected.
  // see example <echarts/test/heatmap-large.html>.
  hoverLayerThreshold: 3e3,
  // See: module:echarts/scale/Time
  useUTC: false
};

// node_modules/echarts/lib/model/internalComponentCreator.js
var internalOptionCreatorMap = createHashMap();
function registerInternalOptionCreator(mainType, creator) {
  assert(internalOptionCreatorMap.get(mainType) == null && creator);
  internalOptionCreatorMap.set(mainType, creator);
}
function concatInternalOptions(ecModel, mainType, newCmptOptionList) {
  var internalOptionCreator = internalOptionCreatorMap.get(mainType);
  if (!internalOptionCreator) {
    return newCmptOptionList;
  }
  var internalOptions = internalOptionCreator(ecModel);
  if (!internalOptions) {
    return newCmptOptionList;
  }
  if (true) {
    for (var i2 = 0; i2 < internalOptions.length; i2++) {
      assert(isComponentIdInternal(internalOptions[i2]));
    }
  }
  return newCmptOptionList.concat(internalOptions);
}

// node_modules/echarts/lib/model/Global.js
var reCreateSeriesIndices;
var assertSeriesInitialized;
var initBase;
var OPTION_INNER_KEY = "\0_ec_inner";
var OPTION_INNER_VALUE = 1;
var BUITIN_COMPONENTS_MAP = {
  grid: "GridComponent",
  polar: "PolarComponent",
  geo: "GeoComponent",
  singleAxis: "SingleAxisComponent",
  parallel: "ParallelComponent",
  calendar: "CalendarComponent",
  matrix: "MatrixComponent",
  graphic: "GraphicComponent",
  toolbox: "ToolboxComponent",
  tooltip: "TooltipComponent",
  axisPointer: "AxisPointerComponent",
  brush: "BrushComponent",
  title: "TitleComponent",
  timeline: "TimelineComponent",
  markPoint: "MarkPointComponent",
  markLine: "MarkLineComponent",
  markArea: "MarkAreaComponent",
  legend: "LegendComponent",
  dataZoom: "DataZoomComponent",
  visualMap: "VisualMapComponent",
  // aria: 'AriaComponent',
  // dataset: 'DatasetComponent',
  // Dependencies
  xAxis: "GridComponent",
  yAxis: "GridComponent",
  angleAxis: "PolarComponent",
  radiusAxis: "PolarComponent"
};
var BUILTIN_CHARTS_MAP = {
  line: "LineChart",
  bar: "BarChart",
  pie: "PieChart",
  scatter: "ScatterChart",
  radar: "RadarChart",
  map: "MapChart",
  tree: "TreeChart",
  treemap: "TreemapChart",
  graph: "GraphChart",
  chord: "ChordChart",
  gauge: "GaugeChart",
  funnel: "FunnelChart",
  parallel: "ParallelChart",
  sankey: "SankeyChart",
  boxplot: "BoxplotChart",
  candlestick: "CandlestickChart",
  effectScatter: "EffectScatterChart",
  lines: "LinesChart",
  heatmap: "HeatmapChart",
  pictorialBar: "PictorialBarChart",
  themeRiver: "ThemeRiverChart",
  sunburst: "SunburstChart",
  custom: "CustomChart"
};
var componetsMissingLogPrinted = {};
function checkMissingComponents(option) {
  each(option, function(componentOption, mainType) {
    if (!Component_default.hasClass(mainType)) {
      var componentImportName = BUITIN_COMPONENTS_MAP[mainType];
      if (componentImportName && !componetsMissingLogPrinted[componentImportName]) {
        error("Component " + mainType + " is used but not imported.\nimport { " + componentImportName + " } from 'echarts/components';\necharts.use([" + componentImportName + "]);");
        componetsMissingLogPrinted[componentImportName] = true;
      }
    }
  });
}
var GlobalModel = (
  /** @class */
  (function(_super) {
    __extends(GlobalModel2, _super);
    function GlobalModel2() {
      return _super !== null && _super.apply(this, arguments) || this;
    }
    GlobalModel2.prototype.init = function(option, parentModel, ecModel, theme2, locale, optionManager) {
      theme2 = theme2 || {};
      this.option = null;
      this._theme = new Model_default(theme2);
      this._locale = new Model_default(locale);
      this._optionManager = optionManager;
    };
    GlobalModel2.prototype.setOption = function(option, opts, optionPreprocessorFuncs2) {
      if (true) {
        assert(option != null, "option is null/undefined");
        assert(option[OPTION_INNER_KEY] !== OPTION_INNER_VALUE, "please use chart.getOption()");
      }
      var innerOpt = normalizeSetOptionInput(opts);
      this._optionManager.setOption(option, optionPreprocessorFuncs2, innerOpt);
      this._resetOption(null, innerOpt);
    };
    GlobalModel2.prototype.resetOption = function(type, opt) {
      return this._resetOption(type, normalizeSetOptionInput(opt));
    };
    GlobalModel2.prototype._resetOption = function(type, opt) {
      var optionChanged = false;
      var optionManager = this._optionManager;
      if (!type || type === "recreate") {
        var baseOption = optionManager.mountOption(type === "recreate");
        if (true) {
          checkMissingComponents(baseOption);
        }
        if (!this.option || type === "recreate") {
          initBase(this, baseOption);
        } else {
          this.restoreData();
          this._mergeOption(baseOption, opt);
        }
        optionChanged = true;
      }
      if (type === "timeline" || type === "media") {
        this.restoreData();
      }
      if (!type || type === "recreate" || type === "timeline") {
        var timelineOption = optionManager.getTimelineOption(this);
        if (timelineOption) {
          optionChanged = true;
          this._mergeOption(timelineOption, opt);
        }
      }
      if (!type || type === "recreate" || type === "media") {
        var mediaOptions = optionManager.getMediaOption(this);
        if (mediaOptions.length) {
          each(mediaOptions, function(mediaOption) {
            optionChanged = true;
            this._mergeOption(mediaOption, opt);
          }, this);
        }
      }
      return optionChanged;
    };
    GlobalModel2.prototype.mergeOption = function(option) {
      this._mergeOption(option, null);
    };
    GlobalModel2.prototype._mergeOption = function(newOption, opt) {
      var option = this.option;
      var componentsMap = this._componentsMap;
      var componentsCount = this._componentsCount;
      var newCmptTypes = [];
      var newCmptTypeMap = createHashMap();
      var replaceMergeMainTypeMap = opt && opt.replaceMergeMainTypeMap;
      resetSourceDefaulter(this);
      each(newOption, function(componentOption, mainType) {
        if (componentOption == null) {
          return;
        }
        if (!Component_default.hasClass(mainType)) {
          option[mainType] = option[mainType] == null ? clone(componentOption) : merge2(option[mainType], componentOption, true);
        } else if (mainType) {
          newCmptTypes.push(mainType);
          newCmptTypeMap.set(mainType, true);
        }
      });
      if (replaceMergeMainTypeMap) {
        replaceMergeMainTypeMap.each(function(val, mainTypeInReplaceMerge) {
          if (Component_default.hasClass(mainTypeInReplaceMerge) && !newCmptTypeMap.get(mainTypeInReplaceMerge)) {
            newCmptTypes.push(mainTypeInReplaceMerge);
            newCmptTypeMap.set(mainTypeInReplaceMerge, true);
          }
        });
      }
      Component_default.topologicalTravel(newCmptTypes, Component_default.getAllClassMainTypes(), visitComponent, this);
      function visitComponent(mainType) {
        var newCmptOptionList = concatInternalOptions(this, mainType, normalizeToArray(newOption[mainType]));
        var oldCmptList = componentsMap.get(mainType);
        var mergeMode = (
          // `!oldCmptList` means init. See the comment in `mappingToExists`
          !oldCmptList ? "replaceAll" : replaceMergeMainTypeMap && replaceMergeMainTypeMap.get(mainType) ? "replaceMerge" : "normalMerge"
        );
        var mappingResult = mappingToExists(oldCmptList, newCmptOptionList, mergeMode);
        setComponentTypeToKeyInfo(mappingResult, mainType, Component_default);
        option[mainType] = null;
        componentsMap.set(mainType, null);
        componentsCount.set(mainType, 0);
        var optionsByMainType = [];
        var cmptsByMainType = [];
        var cmptsCountByMainType = 0;
        var tooltipExists;
        var tooltipWarningLogged;
        each(mappingResult, function(resultItem, index) {
          var componentModel = resultItem.existing;
          var newCmptOption = resultItem.newOption;
          if (!newCmptOption) {
            if (componentModel) {
              componentModel.mergeOption({}, this);
              componentModel.optionUpdated({}, false);
            }
          } else {
            var isSeriesType = mainType === "series";
            var ComponentModelClass = Component_default.getClass(
              mainType,
              resultItem.keyInfo.subType,
              !isSeriesType
              // Give a more detailed warn later if series don't exists
            );
            if (!ComponentModelClass) {
              if (true) {
                var subType = resultItem.keyInfo.subType;
                var seriesImportName = BUILTIN_CHARTS_MAP[subType];
                if (!componetsMissingLogPrinted[subType]) {
                  componetsMissingLogPrinted[subType] = true;
                  if (seriesImportName) {
                    error("Series " + subType + " is used but not imported.\nimport { " + seriesImportName + " } from 'echarts/charts';\necharts.use([" + seriesImportName + "]);");
                  } else {
                    error("Unknown series " + subType);
                  }
                }
              }
              return;
            }
            if (mainType === "tooltip") {
              if (tooltipExists) {
                if (true) {
                  if (!tooltipWarningLogged) {
                    warn("Currently only one tooltip component is allowed.");
                    tooltipWarningLogged = true;
                  }
                }
                return;
              }
              tooltipExists = true;
            }
            if (componentModel && componentModel.constructor === ComponentModelClass) {
              componentModel.name = resultItem.keyInfo.name;
              componentModel.mergeOption(newCmptOption, this);
              componentModel.optionUpdated(newCmptOption, false);
            } else {
              var extraOpt = extend({
                componentIndex: index
              }, resultItem.keyInfo);
              componentModel = new ComponentModelClass(newCmptOption, this, this, extraOpt);
              extend(componentModel, extraOpt);
              if (resultItem.brandNew) {
                componentModel.__requireNewView = true;
              }
              componentModel.init(newCmptOption, this, this);
              componentModel.optionUpdated(null, true);
            }
          }
          if (componentModel) {
            optionsByMainType.push(componentModel.option);
            cmptsByMainType.push(componentModel);
            cmptsCountByMainType++;
          } else {
            optionsByMainType.push(void 0);
            cmptsByMainType.push(void 0);
          }
        }, this);
        option[mainType] = optionsByMainType;
        componentsMap.set(mainType, cmptsByMainType);
        componentsCount.set(mainType, cmptsCountByMainType);
        if (mainType === "series") {
          reCreateSeriesIndices(this);
        }
      }
      if (!this._seriesIndices) {
        reCreateSeriesIndices(this);
      }
    };
    GlobalModel2.prototype.getOption = function() {
      var option = clone(this.option);
      each(option, function(optInMainType, mainType) {
        if (Component_default.hasClass(mainType)) {
          var opts = normalizeToArray(optInMainType);
          var realLen = opts.length;
          var metNonInner = false;
          for (var i2 = realLen - 1; i2 >= 0; i2--) {
            if (opts[i2] && !isComponentIdInternal(opts[i2])) {
              metNonInner = true;
            } else {
              opts[i2] = null;
              !metNonInner && realLen--;
            }
          }
          opts.length = realLen;
          option[mainType] = opts;
        }
      });
      delete option[OPTION_INNER_KEY];
      return option;
    };
    GlobalModel2.prototype.setTheme = function(theme2) {
      this._theme = new Model_default(theme2);
      this._resetOption("recreate", null);
    };
    GlobalModel2.prototype.getTheme = function() {
      return this._theme;
    };
    GlobalModel2.prototype.getLocaleModel = function() {
      return this._locale;
    };
    GlobalModel2.prototype.setUpdatePayload = function(payload) {
      this._payload = payload;
    };
    GlobalModel2.prototype.getUpdatePayload = function() {
      return this._payload;
    };
    GlobalModel2.prototype.getComponent = function(mainType, idx) {
      var list = this._componentsMap.get(mainType);
      if (list) {
        var cmpt = list[idx || 0];
        if (cmpt) {
          return cmpt;
        } else if (idx == null) {
          for (var i2 = 0; i2 < list.length; i2++) {
            if (list[i2]) {
              return list[i2];
            }
          }
        }
      }
    };
    GlobalModel2.prototype.queryComponents = function(condition) {
      var mainType = condition.mainType;
      if (!mainType) {
        return [];
      }
      var index = condition.index;
      var id = condition.id;
      var name = condition.name;
      var cmpts = this._componentsMap.get(mainType);
      if (!cmpts || !cmpts.length) {
        return [];
      }
      var result;
      if (index != null) {
        result = [];
        each(normalizeToArray(index), function(idx) {
          cmpts[idx] && result.push(cmpts[idx]);
        });
      } else if (id != null) {
        result = queryByIdOrName("id", id, cmpts);
      } else if (name != null) {
        result = queryByIdOrName("name", name, cmpts);
      } else {
        result = filter2(cmpts, function(cmpt) {
          return !!cmpt;
        });
      }
      return filterBySubType(result, condition);
    };
    GlobalModel2.prototype.findComponents = function(condition) {
      var query2 = condition.query;
      var mainType = condition.mainType;
      var queryCond = getQueryCond(query2);
      var result = queryCond ? this.queryComponents(queryCond) : filter2(this._componentsMap.get(mainType), function(cmpt) {
        return !!cmpt;
      });
      return doFilter(filterBySubType(result, condition));
      function getQueryCond(q) {
        var indexAttr = mainType + "Index";
        var idAttr = mainType + "Id";
        var nameAttr = mainType + "Name";
        return q && (q[indexAttr] != null || q[idAttr] != null || q[nameAttr] != null) ? {
          mainType,
          // subType will be filtered finally.
          index: q[indexAttr],
          id: q[idAttr],
          name: q[nameAttr]
        } : null;
      }
      function doFilter(res) {
        return condition.filter ? filter2(res, condition.filter) : res;
      }
    };
    GlobalModel2.prototype.eachComponent = function(mainType, cb, context) {
      var componentsMap = this._componentsMap;
      if (isFunction(mainType)) {
        var ctxForAll_1 = cb;
        var cbForAll_1 = mainType;
        componentsMap.each(function(cmpts2, componentType) {
          for (var i3 = 0; cmpts2 && i3 < cmpts2.length; i3++) {
            var cmpt2 = cmpts2[i3];
            cmpt2 && cbForAll_1.call(ctxForAll_1, componentType, cmpt2, cmpt2.componentIndex);
          }
        });
      } else {
        var cmpts = isString(mainType) ? componentsMap.get(mainType) : isObject(mainType) ? this.findComponents(mainType) : null;
        for (var i2 = 0; cmpts && i2 < cmpts.length; i2++) {
          var cmpt = cmpts[i2];
          cmpt && cb.call(context, cmpt, cmpt.componentIndex);
        }
      }
    };
    GlobalModel2.prototype.getSeriesByName = function(name) {
      var nameStr = convertOptionIdName(name, null);
      return filter2(this._componentsMap.get("series"), function(oneSeries) {
        return !!oneSeries && nameStr != null && oneSeries.name === nameStr;
      });
    };
    GlobalModel2.prototype.getSeriesByIndex = function(seriesIndex) {
      return this._componentsMap.get("series")[seriesIndex];
    };
    GlobalModel2.prototype.getSeriesByType = function(subType) {
      return filter2(this._componentsMap.get("series"), function(oneSeries) {
        return !!oneSeries && oneSeries.subType === subType;
      });
    };
    GlobalModel2.prototype.getSeries = function() {
      return filter2(this._componentsMap.get("series"), function(oneSeries) {
        return !!oneSeries;
      });
    };
    GlobalModel2.prototype.getSeriesCount = function() {
      return this._componentsCount.get("series");
    };
    GlobalModel2.prototype.eachSeries = function(cb, context) {
      assertSeriesInitialized(this);
      each(this._seriesIndices, function(rawSeriesIndex) {
        var series = this._componentsMap.get("series")[rawSeriesIndex];
        cb.call(context, series, rawSeriesIndex);
      }, this);
    };
    GlobalModel2.prototype.eachRawSeries = function(cb, context) {
      each(this._componentsMap.get("series"), function(series) {
        series && cb.call(context, series, series.componentIndex);
      });
    };
    GlobalModel2.prototype.eachSeriesByType = function(subType, cb, context) {
      assertSeriesInitialized(this);
      each(this._seriesIndices, function(rawSeriesIndex) {
        var series = this._componentsMap.get("series")[rawSeriesIndex];
        if (series.subType === subType) {
          cb.call(context, series, rawSeriesIndex);
        }
      }, this);
    };
    GlobalModel2.prototype.eachRawSeriesByType = function(subType, cb, context) {
      return each(this.getSeriesByType(subType), cb, context);
    };
    GlobalModel2.prototype.isSeriesFiltered = function(seriesModel) {
      assertSeriesInitialized(this);
      return this._seriesIndicesMap.get(seriesModel.componentIndex) == null;
    };
    GlobalModel2.prototype.getCurrentSeriesIndices = function() {
      return (this._seriesIndices || []).slice();
    };
    GlobalModel2.prototype.filterSeries = function(cb, context) {
      assertSeriesInitialized(this);
      var newSeriesIndices = [];
      each(this._seriesIndices, function(seriesRawIdx) {
        var series = this._componentsMap.get("series")[seriesRawIdx];
        cb.call(context, series, seriesRawIdx) && newSeriesIndices.push(seriesRawIdx);
      }, this);
      this._seriesIndices = newSeriesIndices;
      this._seriesIndicesMap = createHashMap(newSeriesIndices);
    };
    GlobalModel2.prototype.restoreData = function(payload) {
      reCreateSeriesIndices(this);
      var componentsMap = this._componentsMap;
      var componentTypes = [];
      componentsMap.each(function(components, componentType) {
        if (Component_default.hasClass(componentType)) {
          componentTypes.push(componentType);
        }
      });
      Component_default.topologicalTravel(componentTypes, Component_default.getAllClassMainTypes(), function(componentType) {
        each(componentsMap.get(componentType), function(component) {
          if (component && (componentType !== "series" || !isNotTargetSeries(component, payload))) {
            component.restoreData();
          }
        });
      });
    };
    GlobalModel2.internalField = (function() {
      reCreateSeriesIndices = function(ecModel) {
        var seriesIndices = ecModel._seriesIndices = [];
        each(ecModel._componentsMap.get("series"), function(series) {
          series && seriesIndices.push(series.componentIndex);
        });
        ecModel._seriesIndicesMap = createHashMap(seriesIndices);
      };
      assertSeriesInitialized = function(ecModel) {
        if (true) {
          if (!ecModel._seriesIndices) {
            throw new Error("Option should contains series.");
          }
        }
      };
      initBase = function(ecModel, baseOption) {
        ecModel.option = {};
        ecModel.option[OPTION_INNER_KEY] = OPTION_INNER_VALUE;
        ecModel._componentsMap = createHashMap({
          series: []
        });
        ecModel._componentsCount = createHashMap();
        var airaOption = baseOption.aria;
        if (isObject(airaOption) && airaOption.enabled == null) {
          airaOption.enabled = true;
        }
        mergeTheme(baseOption, ecModel._theme.option);
        merge2(baseOption, globalDefault_default, false);
        ecModel._mergeOption(baseOption, null);
      };
    })();
    return GlobalModel2;
  })(Model_default)
);
function isNotTargetSeries(seriesModel, payload) {
  if (payload) {
    var index = payload.seriesIndex;
    var id = payload.seriesId;
    var name_1 = payload.seriesName;
    return index != null && seriesModel.componentIndex !== index || id != null && seriesModel.id !== id || name_1 != null && seriesModel.name !== name_1;
  }
}
function mergeTheme(option, theme2) {
  var notMergeColorLayer = option.color && !option.colorLayer;
  each(theme2, function(themeItem, name) {
    if (name === "colorLayer" && notMergeColorLayer || name === "color" && option.color) {
      return;
    }
    if (!Component_default.hasClass(name)) {
      if (typeof themeItem === "object") {
        option[name] = !option[name] ? clone(themeItem) : merge2(option[name], themeItem, false);
      } else {
        if (option[name] == null) {
          option[name] = themeItem;
        }
      }
    }
  });
}
function queryByIdOrName(attr, idOrName, cmpts) {
  if (isArray(idOrName)) {
    var keyMap_1 = createHashMap();
    each(idOrName, function(idOrNameItem) {
      if (idOrNameItem != null) {
        var idName = convertOptionIdName(idOrNameItem, null);
        idName != null && keyMap_1.set(idOrNameItem, true);
      }
    });
    return filter2(cmpts, function(cmpt) {
      return cmpt && keyMap_1.get(cmpt[attr]);
    });
  } else {
    var idName_1 = convertOptionIdName(idOrName, null);
    return filter2(cmpts, function(cmpt) {
      return cmpt && idName_1 != null && cmpt[attr] === idName_1;
    });
  }
}
function filterBySubType(components, condition) {
  return condition.hasOwnProperty("subType") ? filter2(components, function(cmpt) {
    return cmpt && cmpt.subType === condition.subType;
  }) : components;
}
function normalizeSetOptionInput(opts) {
  var replaceMergeMainTypeMap = createHashMap();
  opts && each(normalizeToArray(opts.replaceMerge), function(mainType) {
    if (true) {
      assert(Component_default.hasClass(mainType), '"' + mainType + '" is not valid component main type in "replaceMerge"');
    }
    replaceMergeMainTypeMap.set(mainType, true);
  });
  return {
    replaceMergeMainTypeMap
  };
}
mixin(GlobalModel, PaletteMixin);
var Global_default = GlobalModel;

// node_modules/echarts/lib/model/OptionManager.js
var QUERY_REG = /^(min|max)?(.+)$/;
var OptionManager = (
  /** @class */
  (function() {
    function OptionManager2(api) {
      this._timelineOptions = [];
      this._mediaList = [];
      this._currentMediaIndices = [];
      this._api = api;
    }
    OptionManager2.prototype.setOption = function(rawOption, optionPreprocessorFuncs2, opt) {
      if (rawOption) {
        each(normalizeToArray(rawOption.series), function(series) {
          series && series.data && isTypedArray(series.data) && setAsPrimitive(series.data);
        });
        each(normalizeToArray(rawOption.dataset), function(dataset) {
          dataset && dataset.source && isTypedArray(dataset.source) && setAsPrimitive(dataset.source);
        });
      }
      rawOption = clone(rawOption);
      var optionBackup = this._optionBackup;
      var newParsedOption = parseRawOption(rawOption, optionPreprocessorFuncs2, !optionBackup);
      this._newBaseOption = newParsedOption.baseOption;
      if (optionBackup) {
        if (newParsedOption.timelineOptions.length) {
          optionBackup.timelineOptions = newParsedOption.timelineOptions;
        }
        if (newParsedOption.mediaList.length) {
          optionBackup.mediaList = newParsedOption.mediaList;
        }
        if (newParsedOption.mediaDefault) {
          optionBackup.mediaDefault = newParsedOption.mediaDefault;
        }
      } else {
        this._optionBackup = newParsedOption;
      }
    };
    OptionManager2.prototype.mountOption = function(isRecreate) {
      var optionBackup = this._optionBackup;
      this._timelineOptions = optionBackup.timelineOptions;
      this._mediaList = optionBackup.mediaList;
      this._mediaDefault = optionBackup.mediaDefault;
      this._currentMediaIndices = [];
      return clone(isRecreate ? optionBackup.baseOption : this._newBaseOption);
    };
    OptionManager2.prototype.getTimelineOption = function(ecModel) {
      var option;
      var timelineOptions = this._timelineOptions;
      if (timelineOptions.length) {
        var timelineModel = ecModel.getComponent("timeline");
        if (timelineModel) {
          option = clone(
            // FIXME:TS as TimelineModel or quivlant interface
            timelineOptions[timelineModel.getCurrentIndex()]
          );
        }
      }
      return option;
    };
    OptionManager2.prototype.getMediaOption = function(ecModel) {
      var ecWidth = this._api.getWidth();
      var ecHeight = this._api.getHeight();
      var mediaList = this._mediaList;
      var mediaDefault = this._mediaDefault;
      var indices = [];
      var result = [];
      if (!mediaList.length && !mediaDefault) {
        return result;
      }
      for (var i2 = 0, len2 = mediaList.length; i2 < len2; i2++) {
        if (applyMediaQuery(mediaList[i2].query, ecWidth, ecHeight)) {
          indices.push(i2);
        }
      }
      if (!indices.length && mediaDefault) {
        indices = [-1];
      }
      if (indices.length && !indicesEquals(indices, this._currentMediaIndices)) {
        result = map2(indices, function(index) {
          return clone(index === -1 ? mediaDefault.option : mediaList[index].option);
        });
      }
      this._currentMediaIndices = indices;
      return result;
    };
    return OptionManager2;
  })()
);
function parseRawOption(rawOption, optionPreprocessorFuncs2, isNew) {
  var mediaList = [];
  var mediaDefault;
  var baseOption;
  var declaredBaseOption = rawOption.baseOption;
  var timelineOnRoot = rawOption.timeline;
  var timelineOptionsOnRoot = rawOption.options;
  var mediaOnRoot = rawOption.media;
  var hasMedia = !!rawOption.media;
  var hasTimeline = !!(timelineOptionsOnRoot || timelineOnRoot || declaredBaseOption && declaredBaseOption.timeline);
  if (declaredBaseOption) {
    baseOption = declaredBaseOption;
    if (!baseOption.timeline) {
      baseOption.timeline = timelineOnRoot;
    }
  } else {
    if (hasTimeline || hasMedia) {
      rawOption.options = rawOption.media = null;
    }
    baseOption = rawOption;
  }
  if (hasMedia) {
    if (isArray(mediaOnRoot)) {
      each(mediaOnRoot, function(singleMedia) {
        if (true) {
          if (singleMedia && !singleMedia.option && isObject(singleMedia.query) && isObject(singleMedia.query.option)) {
            error("Illegal media option. Must be like { media: [ { query: {}, option: {} } ] }");
          }
        }
        if (singleMedia && singleMedia.option) {
          if (singleMedia.query) {
            mediaList.push(singleMedia);
          } else if (!mediaDefault) {
            mediaDefault = singleMedia;
          }
        }
      });
    } else {
      if (true) {
        error("Illegal media option. Must be an array. Like { media: [ {...}, {...} ] }");
      }
    }
  }
  doPreprocess(baseOption);
  each(timelineOptionsOnRoot, function(option) {
    return doPreprocess(option);
  });
  each(mediaList, function(media) {
    return doPreprocess(media.option);
  });
  function doPreprocess(option) {
    each(optionPreprocessorFuncs2, function(preProcess) {
      preProcess(option, isNew);
    });
  }
  return {
    baseOption,
    timelineOptions: timelineOptionsOnRoot || [],
    mediaDefault,
    mediaList
  };
}
function applyMediaQuery(query2, ecWidth, ecHeight) {
  var realMap = {
    width: ecWidth,
    height: ecHeight,
    aspectratio: ecWidth / ecHeight
    // lower case for convenience.
  };
  var applicable = true;
  each(query2, function(value, attr) {
    var matched = attr.match(QUERY_REG);
    if (!matched || !matched[1] || !matched[2]) {
      return;
    }
    var operator = matched[1];
    var realAttr = matched[2].toLowerCase();
    if (!compare(realMap[realAttr], value, operator)) {
      applicable = false;
    }
  });
  return applicable;
}
function compare(real, expect2, operator) {
  if (operator === "min") {
    return real >= expect2;
  } else if (operator === "max") {
    return real <= expect2;
  } else {
    return real === expect2;
  }
}
function indicesEquals(indices1, indices2) {
  return indices1.join(",") === indices2.join(",");
}
var OptionManager_default = OptionManager;

// node_modules/echarts/lib/preprocessor/helper/compatStyle.js
var each3 = each;
var isObject3 = isObject;
var POSSIBLE_STYLES = ["areaStyle", "lineStyle", "nodeStyle", "linkStyle", "chordStyle", "label", "labelLine"];
function compatEC2ItemStyle(opt) {
  var itemStyleOpt = opt && opt.itemStyle;
  if (!itemStyleOpt) {
    return;
  }
  for (var i2 = 0, len2 = POSSIBLE_STYLES.length; i2 < len2; i2++) {
    var styleName = POSSIBLE_STYLES[i2];
    var normalItemStyleOpt = itemStyleOpt.normal;
    var emphasisItemStyleOpt = itemStyleOpt.emphasis;
    if (normalItemStyleOpt && normalItemStyleOpt[styleName]) {
      if (true) {
        deprecateReplaceLog("itemStyle.normal." + styleName, styleName);
      }
      opt[styleName] = opt[styleName] || {};
      if (!opt[styleName].normal) {
        opt[styleName].normal = normalItemStyleOpt[styleName];
      } else {
        merge2(opt[styleName].normal, normalItemStyleOpt[styleName]);
      }
      normalItemStyleOpt[styleName] = null;
    }
    if (emphasisItemStyleOpt && emphasisItemStyleOpt[styleName]) {
      if (true) {
        deprecateReplaceLog("itemStyle.emphasis." + styleName, "emphasis." + styleName);
      }
      opt[styleName] = opt[styleName] || {};
      if (!opt[styleName].emphasis) {
        opt[styleName].emphasis = emphasisItemStyleOpt[styleName];
      } else {
        merge2(opt[styleName].emphasis, emphasisItemStyleOpt[styleName]);
      }
      emphasisItemStyleOpt[styleName] = null;
    }
  }
}
function convertNormalEmphasis(opt, optType, useExtend) {
  if (opt && opt[optType] && (opt[optType].normal || opt[optType].emphasis)) {
    var normalOpt = opt[optType].normal;
    var emphasisOpt = opt[optType].emphasis;
    if (normalOpt) {
      if (true) {
        deprecateLog("'normal' hierarchy in " + optType + " has been removed since 4.0. All style properties are configured in " + optType + " directly now.");
      }
      if (useExtend) {
        opt[optType].normal = opt[optType].emphasis = null;
        defaults(opt[optType], normalOpt);
      } else {
        opt[optType] = normalOpt;
      }
    }
    if (emphasisOpt) {
      if (true) {
        deprecateLog(optType + ".emphasis has been changed to emphasis." + optType + " since 4.0");
      }
      opt.emphasis = opt.emphasis || {};
      opt.emphasis[optType] = emphasisOpt;
      if (emphasisOpt.focus) {
        opt.emphasis.focus = emphasisOpt.focus;
      }
      if (emphasisOpt.blurScope) {
        opt.emphasis.blurScope = emphasisOpt.blurScope;
      }
    }
  }
}
function removeEC3NormalStatus(opt) {
  convertNormalEmphasis(opt, "itemStyle");
  convertNormalEmphasis(opt, "lineStyle");
  convertNormalEmphasis(opt, "areaStyle");
  convertNormalEmphasis(opt, "label");
  convertNormalEmphasis(opt, "labelLine");
  convertNormalEmphasis(opt, "upperLabel");
  convertNormalEmphasis(opt, "edgeLabel");
}
function compatTextStyle(opt, propName) {
  var labelOptSingle = isObject3(opt) && opt[propName];
  var textStyle = isObject3(labelOptSingle) && labelOptSingle.textStyle;
  if (textStyle) {
    if (true) {
      deprecateLog("textStyle hierarchy in " + propName + " has been removed since 4.0. All textStyle properties are configured in " + propName + " directly now.");
    }
    for (var i2 = 0, len2 = TEXT_STYLE_OPTIONS.length; i2 < len2; i2++) {
      var textPropName = TEXT_STYLE_OPTIONS[i2];
      if (textStyle.hasOwnProperty(textPropName)) {
        labelOptSingle[textPropName] = textStyle[textPropName];
      }
    }
  }
}
function compatEC3CommonStyles(opt) {
  if (opt) {
    removeEC3NormalStatus(opt);
    compatTextStyle(opt, "label");
    opt.emphasis && compatTextStyle(opt.emphasis, "label");
  }
}
function processSeries(seriesOpt) {
  if (!isObject3(seriesOpt)) {
    return;
  }
  compatEC2ItemStyle(seriesOpt);
  removeEC3NormalStatus(seriesOpt);
  compatTextStyle(seriesOpt, "label");
  compatTextStyle(seriesOpt, "upperLabel");
  compatTextStyle(seriesOpt, "edgeLabel");
  if (seriesOpt.emphasis) {
    compatTextStyle(seriesOpt.emphasis, "label");
    compatTextStyle(seriesOpt.emphasis, "upperLabel");
    compatTextStyle(seriesOpt.emphasis, "edgeLabel");
  }
  var markPoint = seriesOpt.markPoint;
  if (markPoint) {
    compatEC2ItemStyle(markPoint);
    compatEC3CommonStyles(markPoint);
  }
  var markLine = seriesOpt.markLine;
  if (markLine) {
    compatEC2ItemStyle(markLine);
    compatEC3CommonStyles(markLine);
  }
  var markArea = seriesOpt.markArea;
  if (markArea) {
    compatEC3CommonStyles(markArea);
  }
  var data = seriesOpt.data;
  if (seriesOpt.type === "graph") {
    data = data || seriesOpt.nodes;
    var edgeData = seriesOpt.links || seriesOpt.edges;
    if (edgeData && !isTypedArray(edgeData)) {
      for (var i2 = 0; i2 < edgeData.length; i2++) {
        compatEC3CommonStyles(edgeData[i2]);
      }
    }
    each(seriesOpt.categories, function(opt) {
      removeEC3NormalStatus(opt);
    });
  }
  if (data && !isTypedArray(data)) {
    for (var i2 = 0; i2 < data.length; i2++) {
      compatEC3CommonStyles(data[i2]);
    }
  }
  markPoint = seriesOpt.markPoint;
  if (markPoint && markPoint.data) {
    var mpData = markPoint.data;
    for (var i2 = 0; i2 < mpData.length; i2++) {
      compatEC3CommonStyles(mpData[i2]);
    }
  }
  markLine = seriesOpt.markLine;
  if (markLine && markLine.data) {
    var mlData = markLine.data;
    for (var i2 = 0; i2 < mlData.length; i2++) {
      if (isArray(mlData[i2])) {
        compatEC3CommonStyles(mlData[i2][0]);
        compatEC3CommonStyles(mlData[i2][1]);
      } else {
        compatEC3CommonStyles(mlData[i2]);
      }
    }
  }
  if (seriesOpt.type === "gauge") {
    compatTextStyle(seriesOpt, "axisLabel");
    compatTextStyle(seriesOpt, "title");
    compatTextStyle(seriesOpt, "detail");
  } else if (seriesOpt.type === "treemap") {
    convertNormalEmphasis(seriesOpt.breadcrumb, "itemStyle");
    each(seriesOpt.levels, function(opt) {
      removeEC3NormalStatus(opt);
    });
  } else if (seriesOpt.type === "tree") {
    removeEC3NormalStatus(seriesOpt.leaves);
  }
}
function toArr(o) {
  return isArray(o) ? o : o ? [o] : [];
}
function toObj(o) {
  return (isArray(o) ? o[0] : o) || {};
}
function globalCompatStyle(option, isTheme) {
  each3(toArr(option.series), function(seriesOpt) {
    isObject3(seriesOpt) && processSeries(seriesOpt);
  });
  var axes = ["xAxis", "yAxis", "radiusAxis", "angleAxis", "singleAxis", "parallelAxis", "radar"];
  isTheme && axes.push("valueAxis", "categoryAxis", "logAxis", "timeAxis");
  each3(axes, function(axisName) {
    each3(toArr(option[axisName]), function(axisOpt) {
      if (axisOpt) {
        compatTextStyle(axisOpt, "axisLabel");
        compatTextStyle(axisOpt.axisPointer, "label");
      }
    });
  });
  each3(toArr(option.parallel), function(parallelOpt) {
    var parallelAxisDefault = parallelOpt && parallelOpt.parallelAxisDefault;
    compatTextStyle(parallelAxisDefault, "axisLabel");
    compatTextStyle(parallelAxisDefault && parallelAxisDefault.axisPointer, "label");
  });
  each3(toArr(option.calendar), function(calendarOpt) {
    convertNormalEmphasis(calendarOpt, "itemStyle");
    compatTextStyle(calendarOpt, "dayLabel");
    compatTextStyle(calendarOpt, "monthLabel");
    compatTextStyle(calendarOpt, "yearLabel");
  });
  each3(toArr(option.radar), function(radarOpt) {
    compatTextStyle(radarOpt, "name");
    if (radarOpt.name && radarOpt.axisName == null) {
      radarOpt.axisName = radarOpt.name;
      delete radarOpt.name;
      if (true) {
        deprecateLog("name property in radar component has been changed to axisName");
      }
    }
    if (radarOpt.nameGap != null && radarOpt.axisNameGap == null) {
      radarOpt.axisNameGap = radarOpt.nameGap;
      delete radarOpt.nameGap;
      if (true) {
        deprecateLog("nameGap property in radar component has been changed to axisNameGap");
      }
    }
    if (true) {
      each3(radarOpt.indicator, function(indicatorOpt) {
        if (indicatorOpt.text) {
          deprecateReplaceLog("text", "name", "radar.indicator");
        }
      });
    }
  });
  each3(toArr(option.geo), function(geoOpt) {
    if (isObject3(geoOpt)) {
      compatEC3CommonStyles(geoOpt);
      each3(toArr(geoOpt.regions), function(regionObj) {
        compatEC3CommonStyles(regionObj);
      });
    }
  });
  each3(toArr(option.timeline), function(timelineOpt) {
    compatEC3CommonStyles(timelineOpt);
    convertNormalEmphasis(timelineOpt, "label");
    convertNormalEmphasis(timelineOpt, "itemStyle");
    convertNormalEmphasis(timelineOpt, "controlStyle", true);
    var data = timelineOpt.data;
    isArray(data) && each(data, function(item) {
      if (isObject(item)) {
        convertNormalEmphasis(item, "label");
        convertNormalEmphasis(item, "itemStyle");
      }
    });
  });
  each3(toArr(option.toolbox), function(toolboxOpt) {
    convertNormalEmphasis(toolboxOpt, "iconStyle");
    each3(toolboxOpt.feature, function(featureOpt) {
      convertNormalEmphasis(featureOpt, "iconStyle");
    });
  });
  compatTextStyle(toObj(option.axisPointer), "label");
  compatTextStyle(toObj(option.tooltip).axisPointer, "label");
}

// node_modules/echarts/lib/preprocessor/backwardCompat.js
function get(opt, path) {
  var pathArr = path.split(",");
  var obj = opt;
  for (var i2 = 0; i2 < pathArr.length; i2++) {
    obj = obj && obj[pathArr[i2]];
    if (obj == null) {
      break;
    }
  }
  return obj;
}
function set2(opt, path, val, overwrite) {
  var pathArr = path.split(",");
  var obj = opt;
  var key2;
  var i2 = 0;
  for (; i2 < pathArr.length - 1; i2++) {
    key2 = pathArr[i2];
    if (obj[key2] == null) {
      obj[key2] = {};
    }
    obj = obj[key2];
  }
  if (overwrite || obj[pathArr[i2]] == null) {
    obj[pathArr[i2]] = val;
  }
}
function compatLayoutProperties(option) {
  option && each(LAYOUT_PROPERTIES, function(prop) {
    if (prop[0] in option && !(prop[1] in option)) {
      option[prop[1]] = option[prop[0]];
    }
  });
}
var LAYOUT_PROPERTIES = [["x", "left"], ["y", "top"], ["x2", "right"], ["y2", "bottom"]];
var COMPATITABLE_COMPONENTS = ["grid", "geo", "parallel", "legend", "toolbox", "title", "visualMap", "dataZoom", "timeline"];
var BAR_ITEM_STYLE_MAP = [["borderRadius", "barBorderRadius"], ["borderColor", "barBorderColor"], ["borderWidth", "barBorderWidth"]];
function compatBarItemStyle(option) {
  var itemStyle = option && option.itemStyle;
  if (itemStyle) {
    for (var i2 = 0; i2 < BAR_ITEM_STYLE_MAP.length; i2++) {
      var oldName = BAR_ITEM_STYLE_MAP[i2][1];
      var newName = BAR_ITEM_STYLE_MAP[i2][0];
      if (itemStyle[oldName] != null) {
        itemStyle[newName] = itemStyle[oldName];
        if (true) {
          deprecateReplaceLog(oldName, newName);
        }
      }
    }
  }
}
function compatPieLabel(option) {
  if (!option) {
    return;
  }
  if (option.alignTo === "edge" && option.margin != null && option.edgeDistance == null) {
    if (true) {
      deprecateReplaceLog("label.margin", "label.edgeDistance", "pie");
    }
    option.edgeDistance = option.margin;
  }
}
function compatSunburstState(option) {
  if (!option) {
    return;
  }
  if (option.downplay && !option.blur) {
    option.blur = option.downplay;
    if (true) {
      deprecateReplaceLog("downplay", "blur", "sunburst");
    }
  }
}
function compatGraphFocus(option) {
  if (!option) {
    return;
  }
  if (option.focusNodeAdjacency != null) {
    option.emphasis = option.emphasis || {};
    if (option.emphasis.focus == null) {
      if (true) {
        deprecateReplaceLog("focusNodeAdjacency", "emphasis: { focus: 'adjacency'}", "graph/sankey");
      }
      option.emphasis.focus = "adjacency";
    }
  }
}
function traverseTree(data, cb) {
  if (data) {
    for (var i2 = 0; i2 < data.length; i2++) {
      cb(data[i2]);
      data[i2] && traverseTree(data[i2].children, cb);
    }
  }
}
function globalBackwardCompat(option, isTheme) {
  globalCompatStyle(option, isTheme);
  option.series = normalizeToArray(option.series);
  each(option.series, function(seriesOpt) {
    if (!isObject(seriesOpt)) {
      return;
    }
    var seriesType2 = seriesOpt.type;
    if (seriesType2 === "line") {
      if (seriesOpt.clipOverflow != null) {
        seriesOpt.clip = seriesOpt.clipOverflow;
        if (true) {
          deprecateReplaceLog("clipOverflow", "clip", "line");
        }
      }
    } else if (seriesType2 === "pie" || seriesType2 === "gauge") {
      if (seriesOpt.clockWise != null) {
        seriesOpt.clockwise = seriesOpt.clockWise;
        if (true) {
          deprecateReplaceLog("clockWise", "clockwise");
        }
      }
      compatPieLabel(seriesOpt.label);
      var data = seriesOpt.data;
      if (data && !isTypedArray(data)) {
        for (var i2 = 0; i2 < data.length; i2++) {
          compatPieLabel(data[i2]);
        }
      }
      if (seriesOpt.hoverOffset != null) {
        seriesOpt.emphasis = seriesOpt.emphasis || {};
        if (seriesOpt.emphasis.scaleSize = null) {
          if (true) {
            deprecateReplaceLog("hoverOffset", "emphasis.scaleSize");
          }
          seriesOpt.emphasis.scaleSize = seriesOpt.hoverOffset;
        }
      }
    } else if (seriesType2 === "gauge") {
      var pointerColor = get(seriesOpt, "pointer.color");
      pointerColor != null && set2(seriesOpt, "itemStyle.color", pointerColor);
    } else if (seriesType2 === "bar") {
      compatBarItemStyle(seriesOpt);
      compatBarItemStyle(seriesOpt.backgroundStyle);
      compatBarItemStyle(seriesOpt.emphasis);
      var data = seriesOpt.data;
      if (data && !isTypedArray(data)) {
        for (var i2 = 0; i2 < data.length; i2++) {
          if (typeof data[i2] === "object") {
            compatBarItemStyle(data[i2]);
            compatBarItemStyle(data[i2] && data[i2].emphasis);
          }
        }
      }
    } else if (seriesType2 === "sunburst") {
      var highlightPolicy = seriesOpt.highlightPolicy;
      if (highlightPolicy) {
        seriesOpt.emphasis = seriesOpt.emphasis || {};
        if (!seriesOpt.emphasis.focus) {
          seriesOpt.emphasis.focus = highlightPolicy;
          if (true) {
            deprecateReplaceLog("highlightPolicy", "emphasis.focus", "sunburst");
          }
        }
      }
      compatSunburstState(seriesOpt);
      traverseTree(seriesOpt.data, compatSunburstState);
    } else if (seriesType2 === "graph" || seriesType2 === "sankey") {
      compatGraphFocus(seriesOpt);
    } else if (seriesType2 === "map") {
      if (seriesOpt.mapType && !seriesOpt.map) {
        if (true) {
          deprecateReplaceLog("mapType", "map", "map");
        }
        seriesOpt.map = seriesOpt.mapType;
      }
      if (seriesOpt.mapLocation) {
        if (true) {
          deprecateLog("`mapLocation` is not used anymore.");
        }
        defaults(seriesOpt, seriesOpt.mapLocation);
      }
    }
    if (seriesOpt.hoverAnimation != null) {
      seriesOpt.emphasis = seriesOpt.emphasis || {};
      if (seriesOpt.emphasis && seriesOpt.emphasis.scale == null) {
        if (true) {
          deprecateReplaceLog("hoverAnimation", "emphasis.scale");
        }
        seriesOpt.emphasis.scale = seriesOpt.hoverAnimation;
      }
    }
    compatLayoutProperties(seriesOpt);
  });
  if (option.dataRange) {
    option.visualMap = option.dataRange;
  }
  each(COMPATITABLE_COMPONENTS, function(componentName) {
    var options = option[componentName];
    if (options) {
      if (!isArray(options)) {
        options = [options];
      }
      each(options, function(option2) {
        compatLayoutProperties(option2);
      });
    }
  });
}

// node_modules/echarts/lib/processor/dataStack.js
var dataStackStageHandler = createSimpleOverallStageHandler2(dataStack);
function dataStack(ecModel) {
  var stackInfoMap = createHashMap();
  ecModel.eachSeries(function(seriesModel) {
    var stack = seriesModel.get("stack");
    if (stack) {
      var stackInfoList = stackInfoMap.get(stack) || stackInfoMap.set(stack, []);
      var data = seriesModel.getData();
      var stackInfo = {
        // Used for calculate axis extent automatically.
        // TODO: Type getCalculationInfo return more specific type?
        stackResultDimension: data.getCalculationInfo("stackResultDimension"),
        stackedOverDimension: data.getCalculationInfo("stackedOverDimension"),
        stackedDimension: data.getCalculationInfo("stackedDimension"),
        stackedByDimension: data.getCalculationInfo("stackedByDimension"),
        isStackedByIndex: data.getCalculationInfo("isStackedByIndex"),
        data,
        seriesModel
      };
      if (!stackInfo.stackedDimension || !(stackInfo.isStackedByIndex || stackInfo.stackedByDimension)) {
        return;
      }
      stackInfoList.push(stackInfo);
    }
  });
  stackInfoMap.each(function(stackInfoList) {
    if (stackInfoList.length === 0) {
      return;
    }
    var firstSeries = stackInfoList[0].seriesModel;
    var stackOrder = firstSeries.get("stackOrder") || "seriesAsc";
    if (stackOrder === "seriesDesc") {
      stackInfoList.reverse();
    }
    each(stackInfoList, function(stackInfo, index) {
      stackInfo.data.setCalculationInfo("stackedOnSeries", index > 0 ? stackInfoList[index - 1].seriesModel : null);
    });
    calculateStack(stackInfoList);
  });
}
function calculateStack(stackInfoList) {
  each(stackInfoList, function(targetStackInfo, idxInStack) {
    var resultVal = [];
    var resultNaN = [NaN, NaN];
    var dims = [targetStackInfo.stackResultDimension, targetStackInfo.stackedOverDimension];
    var targetData = targetStackInfo.data;
    var isStackedByIndex = targetStackInfo.isStackedByIndex;
    var stackStrategy = targetStackInfo.seriesModel.get("stackStrategy") || "samesign";
    targetData.modify(dims, function(v0, v12, dataIndex) {
      var sum2 = targetData.get(targetStackInfo.stackedDimension, dataIndex);
      if (isNaN(sum2)) {
        return resultNaN;
      }
      var byValue;
      var stackedDataRawIndex;
      if (isStackedByIndex) {
        stackedDataRawIndex = targetData.getRawIndex(dataIndex);
      } else {
        byValue = targetData.get(targetStackInfo.stackedByDimension, dataIndex);
      }
      var stackedOver = NaN;
      for (var j = idxInStack - 1; j >= 0; j--) {
        var stackInfo = stackInfoList[j];
        if (!isStackedByIndex) {
          stackedDataRawIndex = stackInfo.data.rawIndexOf(stackInfo.stackedByDimension, byValue);
        }
        if (stackedDataRawIndex >= 0) {
          var val = stackInfo.data.getByRawIndex(stackInfo.stackResultDimension, stackedDataRawIndex);
          if (stackStrategy === "all" || stackStrategy === "positive" && val > 0 || stackStrategy === "negative" && val < 0 || stackStrategy === "samesign" && sum2 >= 0 && val > 0 || stackStrategy === "samesign" && sum2 <= 0 && val < 0) {
            sum2 = addSafe(sum2, val);
            stackedOver = val;
            break;
          }
        }
      }
      resultVal[0] = sum2;
      resultVal[1] = stackedOver;
      return resultVal;
    });
  });
}

// node_modules/echarts/lib/view/Component.js
var ComponentView = (
  /** @class */
  (function() {
    function ComponentView2() {
      this.group = new Group_default();
      this.uid = getUID("viewComponent");
    }
    ComponentView2.prototype.init = function(ecModel, api) {
    };
    ComponentView2.prototype.render = function(model2, ecModel, api, payload) {
    };
    ComponentView2.prototype.dispose = function(ecModel, api) {
    };
    ComponentView2.prototype.updateView = function(model2, ecModel, api, payload) {
    };
    ComponentView2.prototype.updateLayout = function(model2, ecModel, api, payload) {
    };
    ComponentView2.prototype.updateVisual = function(model2, ecModel, api, payload) {
    };
    ComponentView2.prototype.toggleBlurSeries = function(seriesModels, isBlur, ecModel) {
    };
    ComponentView2.prototype.eachRendered = function(cb) {
      var group2 = this.group;
      if (group2) {
        group2.traverse(cb);
      }
    };
    return ComponentView2;
  })()
);
enableClassExtend(ComponentView);
enableClassManagement(ComponentView);
var Component_default2 = ComponentView;

// node_modules/echarts/lib/visual/style.js
var inner5 = makeInner();
var defaultStyleMappers = {
  itemStyle: makeStyleMapper(ITEM_STYLE_KEY_MAP, true),
  lineStyle: makeStyleMapper(LINE_STYLE_KEY_MAP, true)
};
var defaultColorKey = {
  lineStyle: "stroke",
  itemStyle: "fill"
};
function getStyleMapper(seriesModel, stylePath) {
  var styleMapper = seriesModel.visualStyleMapper || defaultStyleMappers[stylePath];
  if (!styleMapper) {
    console.warn("Unknown style type '" + stylePath + "'.");
    return defaultStyleMappers.itemStyle;
  }
  return styleMapper;
}
function getDefaultColorKey(seriesModel, stylePath) {
  var colorKey = seriesModel.visualDrawType || defaultColorKey[stylePath];
  if (!colorKey) {
    console.warn("Unknown style type '" + stylePath + "'.");
    return "fill";
  }
  return colorKey;
}
var seriesStyleTask = {
  createOnAllSeries: true,
  performRawSeries: true,
  reset: function(seriesModel, ecModel) {
    var data = seriesModel.getData();
    var stylePath = seriesModel.visualStyleAccessPath || "itemStyle";
    var styleModel = seriesModel.getModel(stylePath);
    var getStyle = getStyleMapper(seriesModel, stylePath);
    var globalStyle = getStyle(styleModel);
    var decalOption = styleModel.getShallow("decal");
    if (decalOption) {
      data.setVisual("decal", decalOption);
      decalOption.dirty = true;
    }
    var colorKey = getDefaultColorKey(seriesModel, stylePath);
    var color3 = globalStyle[colorKey];
    var colorCallback = isFunction(color3) ? color3 : null;
    var hasAutoColor = globalStyle.fill === "auto" || globalStyle.stroke === "auto";
    if (!globalStyle[colorKey] || colorCallback || hasAutoColor) {
      var colorPalette = seriesModel.getColorFromPalette(
        // TODO series count changed.
        seriesModel.name,
        null,
        ecModel.getSeriesCount()
      );
      if (!globalStyle[colorKey]) {
        globalStyle[colorKey] = colorPalette;
        data.setVisual("colorFromPalette", true);
      }
      globalStyle.fill = globalStyle.fill === "auto" || isFunction(globalStyle.fill) ? colorPalette : globalStyle.fill;
      globalStyle.stroke = globalStyle.stroke === "auto" || isFunction(globalStyle.stroke) ? colorPalette : globalStyle.stroke;
    }
    data.setVisual("style", globalStyle);
    data.setVisual("drawType", colorKey);
    if (!ecModel.isSeriesFiltered(seriesModel) && colorCallback) {
      data.setVisual("colorFromPalette", false);
      return {
        dataEach: function(data2, idx) {
          var dataParams = seriesModel.getDataParams(idx);
          var itemStyle = extend({}, globalStyle);
          itemStyle[colorKey] = colorCallback(dataParams);
          data2.setItemVisual(idx, "style", itemStyle);
        }
      };
    }
  }
};
var sharedModel = new Model_default();
var dataStyleTask = {
  createOnAllSeries: true,
  reset: function(seriesModel, ecModel) {
    if (seriesModel.ignoreStyleOnData) {
      return;
    }
    var data = seriesModel.getData();
    var stylePath = seriesModel.visualStyleAccessPath || "itemStyle";
    var getStyle = getStyleMapper(seriesModel, stylePath);
    var colorKey = data.getVisual("drawType");
    return {
      dataEach: data.hasItemOption ? function(data2, idx) {
        var rawItem = data2.getRawDataItem(idx);
        if (rawItem && rawItem[stylePath]) {
          sharedModel.option = rawItem[stylePath];
          var style2 = getStyle(sharedModel);
          var existsStyle = data2.ensureUniqueItemVisual(idx, "style");
          extend(existsStyle, style2);
          if (sharedModel.option.decal) {
            data2.setItemVisual(idx, "decal", sharedModel.option.decal);
            sharedModel.option.decal.dirty = true;
          }
          if (colorKey in style2) {
            data2.setItemVisual(idx, "colorFromPalette", false);
          }
        }
      } : null
    };
  }
};
var dataColorPaletteTask = {
  performRawSeries: true,
  overallReset: function(ecModel) {
    var paletteScopeGroupByType = createHashMap();
    ecModel.eachSeries(function(seriesModel) {
      if (!seriesModel.isColorBySeries()) {
        var key2 = seriesModel.type + "-" + seriesModel.getColorBy();
        inner5(seriesModel).scope = paletteScopeGroupByType.get(key2) || paletteScopeGroupByType.set(key2, {});
      }
    });
    ecModel.eachSeries(function(seriesModel) {
      if (seriesModel.isColorBySeries()) {
        return;
      }
      var dataAll = seriesModel.getRawData();
      var idxMap = {};
      var data = seriesModel.getData();
      var colorScope = inner5(seriesModel).scope;
      var stylePath = seriesModel.visualStyleAccessPath || "itemStyle";
      var colorKey = getDefaultColorKey(seriesModel, stylePath);
      data.each(function(idx) {
        var rawIdx = data.getRawIndex(idx);
        idxMap[rawIdx] = idx;
      });
      dataAll.each(function(rawIdx) {
        var idx = idxMap[rawIdx];
        var fromPalette = data.getItemVisual(idx, "colorFromPalette");
        if (fromPalette) {
          var itemStyle = data.ensureUniqueItemVisual(idx, "style");
          var name_1 = dataAll.getName(rawIdx) || rawIdx + "";
          var dataCount = dataAll.count();
          itemStyle[colorKey] = seriesModel.getColorFromPalette(name_1, colorScope, dataCount);
        }
      });
    });
  }
};

// node_modules/echarts/lib/loading/default.js
var PI6 = Math.PI;
function defaultLoading(api, opts) {
  opts = opts || {};
  defaults(opts, {
    text: "loading",
    textColor: tokens_default.color.primary,
    fontSize: 12,
    fontWeight: "normal",
    fontStyle: "normal",
    fontFamily: "sans-serif",
    maskColor: "rgba(255,255,255,0.8)",
    showSpinner: true,
    color: tokens_default.color.theme[0],
    spinnerRadius: 10,
    lineWidth: 5,
    zlevel: 0
  });
  var group2 = new Group_default();
  var mask = new Rect_default({
    style: {
      fill: opts.maskColor
    },
    zlevel: opts.zlevel,
    z: 1e4
  });
  group2.add(mask);
  var textContent = new Text_default({
    style: {
      text: opts.text,
      fill: opts.textColor,
      fontSize: opts.fontSize,
      fontWeight: opts.fontWeight,
      fontStyle: opts.fontStyle,
      fontFamily: opts.fontFamily
    },
    zlevel: opts.zlevel,
    z: 10001
  });
  var labelRect = new Rect_default({
    style: {
      fill: "none"
    },
    textContent,
    textConfig: {
      position: "right",
      distance: 10
    },
    zlevel: opts.zlevel,
    z: 10001
  });
  group2.add(labelRect);
  var arc;
  if (opts.showSpinner) {
    arc = new Arc_default({
      shape: {
        startAngle: -PI6 / 2,
        endAngle: -PI6 / 2 + 0.1,
        r: opts.spinnerRadius
      },
      style: {
        stroke: opts.color,
        lineCap: "round",
        lineWidth: opts.lineWidth
      },
      zlevel: opts.zlevel,
      z: 10001
    });
    arc.animateShape(true).when(1e3, {
      endAngle: PI6 * 3 / 2
    }).start("circularInOut");
    arc.animateShape(true).when(1e3, {
      startAngle: PI6 * 3 / 2
    }).delay(300).start("circularInOut");
    group2.add(arc);
  }
  group2.resize = function() {
    var textWidth = textContent.getBoundingRect().width;
    var r = opts.showSpinner ? opts.spinnerRadius : 0;
    var cx = (api.getWidth() - r * 2 - (opts.showSpinner && textWidth ? 10 : 0) - textWidth) / 2 - (opts.showSpinner && textWidth ? 0 : 5 + textWidth / 2) + (opts.showSpinner ? 0 : textWidth / 2) + (textWidth ? 0 : r);
    var cy = api.getHeight() / 2;
    opts.showSpinner && arc.setShape({
      cx,
      cy
    });
    labelRect.setShape({
      x: cx - r,
      y: cy - r,
      width: r * 2,
      height: r * 2
    });
    mask.setShape({
      x: 0,
      y: 0,
      width: api.getWidth(),
      height: api.getHeight()
    });
  };
  group2.resize();
  return group2;
}

// node_modules/echarts/lib/core/Scheduler.js
var Scheduler = (
  /** @class */
  (function() {
    function Scheduler2(ecInstance, api, dataProcessorHandlers, visualHandlers) {
      this._stageTaskMap = createHashMap();
      this.ecInstance = ecInstance;
      this.api = api;
      dataProcessorHandlers = this._dataProcessorHandlers = dataProcessorHandlers.slice();
      visualHandlers = this._visualHandlers = visualHandlers.slice();
      this._allHandlers = dataProcessorHandlers.concat(visualHandlers);
    }
    Scheduler2.prototype.restoreData = function(ecModel, payload) {
      ecModel.restoreData(payload);
      this._stageTaskMap.each(function(taskRecord) {
        var overallTask = taskRecord.overallTask;
        overallTask && overallTask.dirty();
      });
    };
    Scheduler2.prototype.getPerformArgs = function(task, isBlock) {
      if (!task.__pipeline) {
        return;
      }
      var pipeline = this._pipelineMap.get(task.__pipeline.id);
      var pCtx = pipeline.context;
      var incremental = !isBlock && pipeline.progressiveEnabled && (!pCtx || pCtx.progressiveRender) && task.__idxInPipeline > pipeline.blockIndex;
      var step = incremental ? pipeline.step : null;
      var modDataCount = pCtx && pCtx.modDataCount;
      var modBy = modDataCount != null ? Math.ceil(modDataCount / step) : null;
      return {
        step,
        modBy,
        modDataCount
      };
    };
    Scheduler2.prototype.getPipeline = function(pipelineId) {
      return this._pipelineMap.get(pipelineId);
    };
    Scheduler2.prototype.updateStreamModes = function(seriesModel, view) {
      var pipeline = this._pipelineMap.get(seriesModel.uid);
      var context = seriesModel.__preparePipelineContext ? seriesModel.__preparePipelineContext(view, pipeline) : preparePipelineContext(seriesModel, view, pipeline);
      seriesModel.pipelineContext = pipeline.context = context;
    };
    Scheduler2.prototype.restorePipelines = function(zr, ecModel) {
      var scheduler = this;
      var pipelineMap = scheduler._pipelineMap = createHashMap();
      ecModel.eachSeries(function(seriesModel) {
        var progressive = zr.painter.type === "canvas" && seriesModel.getProgressive();
        var pipelineId = seriesModel.uid;
        pipelineMap.set(pipelineId, {
          id: pipelineId,
          head: null,
          tail: null,
          threshold: seriesModel.getProgressiveThreshold(),
          progressiveEnabled: progressive && !(seriesModel.preventIncremental && seriesModel.preventIncremental()),
          blockIndex: -1,
          step: Math.round(progressive || 700),
          count: 0
        });
        scheduler._pipe(seriesModel, seriesModel.dataTask);
      });
    };
    Scheduler2.prototype.prepareStageTasks = function() {
      var stageTaskMap = this._stageTaskMap;
      var ecModel = this.api.getModel();
      var api = this.api;
      each(this._allHandlers, function(handler) {
        var record = stageTaskMap.get(handler.uid) || stageTaskMap.set(handler.uid, {});
        var errMsg = "";
        if (true) {
          errMsg = '"reset" and "overallReset" must not be both specified.';
        }
        assert(!(handler.reset && handler.overallReset), errMsg);
        handler.reset && this._createSeriesStageTask(handler, record, ecModel, api);
        handler.overallReset && this._createOverallStageTask(handler, record, ecModel, api);
      }, this);
    };
    Scheduler2.prototype.prepareView = function(view, model2, ecModel, api) {
      var renderTask = view.renderTask;
      var context = renderTask.context;
      context.model = model2;
      context.ecModel = ecModel;
      context.api = api;
      renderTask.__block = !view.incrementalPrepareRender;
      this._pipe(model2, renderTask);
    };
    Scheduler2.prototype.performDataProcessorTasks = function(ecModel, payload) {
      this._performStageTasks(this._dataProcessorHandlers, ecModel, payload, {
        block: true
      });
    };
    Scheduler2.prototype.performVisualTasks = function(ecModel, payload, opt) {
      this._performStageTasks(this._visualHandlers, ecModel, payload, opt);
    };
    Scheduler2.prototype._performStageTasks = function(stageHandlers, ecModel, payload, opt) {
      opt = opt || {};
      var unfinished = false;
      var scheduler = this;
      each(stageHandlers, function(stageHandler, idx) {
        if (opt.visualType && opt.visualType !== stageHandler.visualType) {
          return;
        }
        var stageHandlerRecord = scheduler._stageTaskMap.get(stageHandler.uid);
        var seriesTaskMap = stageHandlerRecord.seriesTaskMap;
        var overallTask = stageHandlerRecord.overallTask;
        if (overallTask) {
          var overallNeedDirty_1;
          var agentStubMap = overallTask.agentStubMap;
          agentStubMap.each(function(stub) {
            if (needSetDirty(opt, stub)) {
              stub.dirty();
              overallNeedDirty_1 = true;
            }
          });
          overallNeedDirty_1 && overallTask.dirty();
          scheduler.updatePayload(overallTask, payload);
          var performArgs_1 = scheduler.getPerformArgs(overallTask, opt.block);
          agentStubMap.each(function(stub) {
            stub.perform(performArgs_1);
          });
          if (overallTask.perform(performArgs_1)) {
            unfinished = true;
          }
        } else if (seriesTaskMap) {
          seriesTaskMap.each(function(task, pipelineId) {
            if (needSetDirty(opt, task)) {
              task.dirty();
            }
            var performArgs = scheduler.getPerformArgs(task, opt.block);
            performArgs.skip = !stageHandler.performRawSeries && ecModel.isSeriesFiltered(task.context.model);
            scheduler.updatePayload(task, payload);
            if (task.perform(performArgs)) {
              unfinished = true;
            }
          });
        }
      });
      function needSetDirty(opt2, task) {
        return opt2.setDirty && (!opt2.dirtyMap || opt2.dirtyMap.get(task.__pipeline.id));
      }
      this.unfinished = unfinished || this.unfinished;
    };
    Scheduler2.prototype.performSeriesTasks = function(ecModel) {
      var unfinished;
      ecModel.eachSeries(function(seriesModel) {
        unfinished = seriesModel.dataTask.perform() || unfinished;
      });
      this.unfinished = unfinished || this.unfinished;
    };
    Scheduler2.prototype.plan = function() {
      this._pipelineMap.each(function(pipeline) {
        var task = pipeline.tail;
        do {
          if (task.__block) {
            pipeline.blockIndex = task.__idxInPipeline;
            break;
          }
          task = task.getUpstream();
        } while (task);
      });
    };
    Scheduler2.prototype.updatePayload = function(task, payload) {
      payload !== "remain" && (task.context.payload = payload);
    };
    Scheduler2.prototype._createSeriesStageTask = function(stageHandler, stageHandlerRecord, ecModel, api) {
      var scheduler = this;
      var oldSeriesTaskMap = stageHandlerRecord.seriesTaskMap;
      var newSeriesTaskMap = stageHandlerRecord.seriesTaskMap = createHashMap();
      var seriesType2 = stageHandler.seriesType;
      var getTargetSeries = stageHandler.getTargetSeries;
      if (stageHandler.createOnAllSeries) {
        ecModel.eachRawSeries(create3);
      } else if (seriesType2) {
        ecModel.eachRawSeriesByType(seriesType2, create3);
      } else if (getTargetSeries) {
        getTargetSeries(ecModel, api).each(create3);
      }
      function create3(seriesModel) {
        var pipelineId = seriesModel.uid;
        var task = newSeriesTaskMap.set(pipelineId, oldSeriesTaskMap && oldSeriesTaskMap.get(pipelineId) || createTask({
          plan: seriesTaskPlan,
          reset: seriesTaskReset,
          count: seriesTaskCount
        }));
        task.context = {
          model: seriesModel,
          ecModel,
          api,
          // PENDING: `useClearVisual` not used?
          useClearVisual: stageHandler.isVisual && !stageHandler.isLayout,
          plan: stageHandler.plan,
          reset: stageHandler.reset,
          scheduler
        };
        scheduler._pipe(seriesModel, task);
      }
    };
    Scheduler2.prototype._createOverallStageTask = function(stageHandler, stageHandlerRecord, ecModel, api) {
      var scheduler = this;
      var overallTask = stageHandlerRecord.overallTask = stageHandlerRecord.overallTask || createTask({
        reset: overallTaskReset
      });
      overallTask.context = {
        ecModel,
        api,
        overallReset: stageHandler.overallReset,
        scheduler
      };
      var oldAgentStubMap = overallTask.agentStubMap;
      var newAgentStubMap = overallTask.agentStubMap = createHashMap();
      var seriesType2 = stageHandler.seriesType;
      var getTargetSeries = stageHandler.getTargetSeries;
      var dirtyOnOverallProgress = stageHandler.dirtyOnOverallProgress;
      var shouldOverallTaskDirty = false;
      var errMsg = "";
      if (true) {
        errMsg = '"createOnAllSeries" is not supported for "overallReset", because it will block all streams.';
      }
      assert(!stageHandler.createOnAllSeries, errMsg);
      if (seriesType2) {
        ecModel.eachRawSeriesByType(seriesType2, createStub);
      } else if (getTargetSeries) {
        getTargetSeries(ecModel, api).each(createStub);
      } else {
        each(ecModel.getSeries(), createStub);
      }
      function createStub(seriesModel) {
        var pipelineId = seriesModel.uid;
        var stub = newAgentStubMap.set(pipelineId, oldAgentStubMap && oldAgentStubMap.get(pipelineId) || // When the result of `getTargetSeries` changed, the overallTask
        // should be set as dirty and re-performed.
        (shouldOverallTaskDirty = true, createTask({
          reset: stubReset,
          onDirty: stubOnDirty
        })));
        stub.context = {
          model: seriesModel,
          dirtyOnOverallProgress
          // FIXME:TS never used, so comment it
          // modifyOutputEnd: modifyOutputEnd
        };
        stub.agent = overallTask;
        stub.__block = dirtyOnOverallProgress;
        scheduler._pipe(seriesModel, stub);
      }
      if (shouldOverallTaskDirty) {
        overallTask.dirty();
      }
    };
    Scheduler2.prototype._pipe = function(seriesModel, task) {
      var pipelineId = seriesModel.uid;
      var pipeline = this._pipelineMap.get(pipelineId);
      !pipeline.head && (pipeline.head = task);
      pipeline.tail && pipeline.tail.pipe(task);
      pipeline.tail = task;
      task.__idxInPipeline = pipeline.count++;
      task.__pipeline = pipeline;
    };
    Scheduler2.wrapStageHandler = function(stageHandler, visualType) {
      if (isFunction(stageHandler)) {
        stageHandler = {
          overallReset: stageHandler,
          seriesType: detectSeriseType(stageHandler)
        };
      }
      stageHandler.uid = getUID("stageHandler");
      visualType && (stageHandler.visualType = visualType);
      return stageHandler;
    };
    ;
    return Scheduler2;
  })()
);
function overallTaskReset(context) {
  context.overallReset(context.ecModel, context.api, context.payload);
}
function stubReset(context) {
  return context.dirtyOnOverallProgress && stubProgress;
}
function stubProgress() {
  this.agent.dirty();
  this.getDownstream().dirty();
}
function stubOnDirty() {
  this.agent && this.agent.dirty();
}
function seriesTaskPlan(context) {
  return context.plan ? context.plan(context.model, context.ecModel, context.api, context.payload) : null;
}
function seriesTaskReset(context) {
  if (context.useClearVisual) {
    context.data.clearAllVisual();
  }
  var resetDefines = context.resetDefines = normalizeToArray(context.reset(context.model, context.ecModel, context.api, context.payload));
  return resetDefines.length > 1 ? map2(resetDefines, function(v, idx) {
    return makeSeriesTaskProgress(idx);
  }) : singleSeriesTaskProgress;
}
var singleSeriesTaskProgress = makeSeriesTaskProgress(0);
function makeSeriesTaskProgress(resetDefineIdx) {
  return function(params, context) {
    var data = context.data;
    var resetDefine = context.resetDefines[resetDefineIdx];
    if (resetDefine && resetDefine.dataEach) {
      for (var i2 = params.start; i2 < params.end; i2++) {
        resetDefine.dataEach(data, i2);
      }
    } else if (resetDefine && resetDefine.progress) {
      resetDefine.progress(params, data);
    }
  };
}
function seriesTaskCount(context) {
  return context.data.count();
}
function detectSeriseType(legacyFunc) {
  seriesType = null;
  try {
    legacyFunc(ecModelMock, apiMock);
  } catch (e2) {
  }
  return seriesType;
}
var ecModelMock = {};
var apiMock = {};
var seriesType;
mockMethods(ecModelMock, Global_default);
mockMethods(apiMock, ExtensionAPI_default);
ecModelMock.eachSeriesByType = ecModelMock.eachRawSeriesByType = function(type) {
  seriesType = type;
};
ecModelMock.eachComponent = function(cond) {
  if (cond.mainType === "series" && cond.subType) {
    seriesType = cond.subType;
  }
};
function mockMethods(target, Clz) {
  for (var name_1 in Clz.prototype) {
    target[name_1] = noop;
  }
}
var Scheduler_default = Scheduler;

// node_modules/echarts/lib/theme/dark.js
var color2 = tokens_default.darkColor;
var backgroundColor = color2.background;
var axisCommon = function() {
  return {
    axisLine: {
      lineStyle: {
        color: color2.axisLine
      }
    },
    splitLine: {
      lineStyle: {
        color: color2.axisSplitLine
      }
    },
    splitArea: {
      areaStyle: {
        color: [color2.backgroundTint, color2.backgroundTransparent]
      }
    },
    minorSplitLine: {
      lineStyle: {
        color: color2.axisMinorSplitLine
      }
    },
    axisLabel: {
      color: color2.axisLabel
    },
    axisName: {}
  };
};
var matrixAxis = {
  label: {
    color: color2.secondary
  },
  itemStyle: {
    borderColor: color2.borderTint
  },
  dividerLineStyle: {
    color: color2.border
  }
};
var theme = {
  darkMode: true,
  color: color2.theme,
  backgroundColor,
  axisPointer: {
    lineStyle: {
      color: color2.border
    },
    crossStyle: {
      color: color2.borderShade
    },
    label: {
      color: color2.tertiary
    }
  },
  legend: {
    textStyle: {
      color: color2.secondary
    },
    pageTextStyle: {
      color: color2.tertiary
    }
  },
  textStyle: {
    color: color2.secondary
  },
  title: {
    textStyle: {
      color: color2.primary
    },
    subtextStyle: {
      color: color2.quaternary
    }
  },
  toolbox: {
    iconStyle: {
      borderColor: color2.accent50
    },
    feature: {
      dataView: {
        backgroundColor,
        textColor: color2.primary,
        textareaColor: color2.background,
        textareaBorderColor: color2.border,
        buttonColor: color2.accent50,
        buttonTextColor: color2.neutral00
      }
    }
  },
  tooltip: {
    backgroundColor: color2.neutral20,
    defaultBorderColor: color2.border,
    textStyle: {
      color: color2.tertiary
    }
  },
  dataZoom: {
    borderColor: color2.accent10,
    textStyle: {
      color: color2.tertiary
    },
    brushStyle: {
      color: color2.backgroundTint
    },
    handleStyle: {
      color: color2.neutral00,
      borderColor: color2.accent20
    },
    moveHandleStyle: {
      color: color2.accent40
    },
    emphasis: {
      handleStyle: {
        borderColor: color2.accent50
      }
    },
    dataBackground: {
      lineStyle: {
        color: color2.accent30
      },
      areaStyle: {
        color: color2.accent20
      }
    },
    selectedDataBackground: {
      lineStyle: {
        color: color2.accent50
      },
      areaStyle: {
        color: color2.accent30
      }
    }
  },
  visualMap: {
    textStyle: {
      color: color2.secondary
    },
    handleStyle: {
      borderColor: color2.neutral30
    }
  },
  timeline: {
    lineStyle: {
      color: color2.accent10
    },
    label: {
      color: color2.tertiary
    },
    controlStyle: {
      color: color2.accent30,
      borderColor: color2.accent30
    }
  },
  calendar: {
    itemStyle: {
      color: color2.neutral00,
      borderColor: color2.neutral20
    },
    dayLabel: {
      color: color2.tertiary
    },
    monthLabel: {
      color: color2.secondary
    },
    yearLabel: {
      color: color2.secondary
    }
  },
  matrix: {
    x: matrixAxis,
    y: matrixAxis,
    backgroundColor: {
      borderColor: color2.axisLine
    },
    body: {
      itemStyle: {
        borderColor: color2.borderTint
      }
    }
  },
  timeAxis: axisCommon(),
  logAxis: axisCommon(),
  valueAxis: axisCommon(),
  categoryAxis: axisCommon(),
  line: {
    symbol: "circle"
  },
  graph: {
    color: color2.theme
  },
  gauge: {
    title: {
      color: color2.secondary
    },
    axisLine: {
      lineStyle: {
        color: [[1, color2.neutral05]]
      }
    },
    axisLabel: {
      color: color2.axisLabel
    },
    detail: {
      color: color2.primary
    }
  },
  candlestick: {
    itemStyle: {
      color: "#f64e56",
      color0: "#54ea92",
      borderColor: "#f64e56",
      borderColor0: "#54ea92"
      // borderColor: '#ca2824',
      // borderColor0: '#09a443'
    }
  },
  funnel: {
    itemStyle: {
      borderColor: color2.background
    }
  },
  radar: (function() {
    var radar = axisCommon();
    radar.axisName = {
      color: color2.axisLabel
    };
    radar.axisLine.lineStyle.color = color2.neutral20;
    return radar;
  })(),
  treemap: {
    breadcrumb: {
      itemStyle: {
        color: color2.neutral20,
        textStyle: {
          color: color2.secondary
        }
      },
      emphasis: {
        itemStyle: {
          color: color2.neutral30
        }
      }
    }
  },
  sunburst: {
    itemStyle: {
      borderColor: color2.background
    }
  },
  map: {
    itemStyle: {
      borderColor: color2.border,
      areaColor: color2.neutral10
    },
    label: {
      color: color2.tertiary
    },
    emphasis: {
      label: {
        color: color2.primary
      },
      itemStyle: {
        areaColor: color2.highlight
      }
    },
    select: {
      label: {
        color: color2.primary
      },
      itemStyle: {
        areaColor: color2.highlight
      }
    }
  },
  geo: {
    itemStyle: {
      borderColor: color2.border,
      areaColor: color2.neutral10
    },
    emphasis: {
      label: {
        color: color2.primary
      },
      itemStyle: {
        areaColor: color2.highlight
      }
    },
    select: {
      label: {
        color: color2.primary
      },
      itemStyle: {
        color: color2.highlight
      }
    }
  }
};
theme.categoryAxis.splitLine.show = false;
var dark_default = theme;

// node_modules/echarts/lib/util/ECEventProcessor.js
var ECEventProcessor = (
  /** @class */
  (function() {
    function ECEventProcessor2() {
    }
    ECEventProcessor2.prototype.normalizeQuery = function(query2) {
      var cptQuery = {};
      var dataQuery = {};
      var otherQuery = {};
      if (isString(query2)) {
        var condCptType = parseClassType(query2);
        cptQuery.mainType = condCptType.main || null;
        cptQuery.subType = condCptType.sub || null;
      } else {
        var suffixes_1 = ["Index", "Name", "Id"];
        var dataKeys_1 = {
          name: 1,
          dataIndex: 1,
          dataType: 1
        };
        each(query2, function(val, key2) {
          var reserved = false;
          for (var i2 = 0; i2 < suffixes_1.length; i2++) {
            var propSuffix = suffixes_1[i2];
            var suffixPos = key2.lastIndexOf(propSuffix);
            if (suffixPos > 0 && suffixPos === key2.length - propSuffix.length) {
              var mainType = key2.slice(0, suffixPos);
              if (mainType !== "data") {
                cptQuery.mainType = mainType;
                cptQuery[propSuffix.toLowerCase()] = val;
                reserved = true;
              }
            }
          }
          if (dataKeys_1.hasOwnProperty(key2)) {
            dataQuery[key2] = val;
            reserved = true;
          }
          if (!reserved) {
            otherQuery[key2] = val;
          }
        });
      }
      return {
        cptQuery,
        dataQuery,
        otherQuery
      };
    };
    ECEventProcessor2.prototype.filter = function(eventType, query2) {
      var eventInfo = this.eventInfo;
      if (!eventInfo) {
        return true;
      }
      var targetEl = eventInfo.targetEl;
      var packedEvent = eventInfo.packedEvent;
      var model2 = eventInfo.model;
      var view = eventInfo.view;
      if (!model2 || !view) {
        return true;
      }
      var cptQuery = query2.cptQuery;
      var dataQuery = query2.dataQuery;
      return check(cptQuery, model2, "mainType") && check(cptQuery, model2, "subType") && check(cptQuery, model2, "index", "componentIndex") && check(cptQuery, model2, "name") && check(cptQuery, model2, "id") && check(dataQuery, packedEvent, "name") && check(dataQuery, packedEvent, "dataIndex") && check(dataQuery, packedEvent, "dataType") && (!view.filterForExposedEvent || view.filterForExposedEvent(eventType, query2.otherQuery, targetEl, packedEvent));
      function check(query3, host, prop, propOnHost) {
        return query3[prop] == null || host[propOnHost || prop] === query3[prop];
      }
    };
    ECEventProcessor2.prototype.afterTrigger = function() {
      this.eventInfo = null;
    };
    return ECEventProcessor2;
  })()
);

// node_modules/echarts/lib/visual/symbol.js
var SYMBOL_PROPS_WITH_CB = ["symbol", "symbolSize", "symbolRotate", "symbolOffset"];
var SYMBOL_PROPS = SYMBOL_PROPS_WITH_CB.concat(["symbolKeepAspect"]);
var seriesSymbolTask = {
  createOnAllSeries: true,
  // For legend.
  performRawSeries: true,
  reset: function(seriesModel, ecModel) {
    var data = seriesModel.getData();
    if (seriesModel.legendIcon) {
      data.setVisual("legendIcon", seriesModel.legendIcon);
    }
    if (!seriesModel.hasSymbolVisual) {
      return;
    }
    var symbolOptions = {};
    var symbolOptionsCb = {};
    var hasCallback = false;
    for (var i2 = 0; i2 < SYMBOL_PROPS_WITH_CB.length; i2++) {
      var symbolPropName = SYMBOL_PROPS_WITH_CB[i2];
      var val = seriesModel.get(symbolPropName);
      if (isFunction(val)) {
        hasCallback = true;
        symbolOptionsCb[symbolPropName] = val;
      } else {
        symbolOptions[symbolPropName] = val;
      }
    }
    symbolOptions.symbol = symbolOptions.symbol || seriesModel.defaultSymbol;
    data.setVisual(extend({
      legendIcon: seriesModel.legendIcon || symbolOptions.symbol,
      symbolKeepAspect: seriesModel.get("symbolKeepAspect")
    }, symbolOptions));
    if (ecModel.isSeriesFiltered(seriesModel)) {
      return;
    }
    var symbolPropsCb = keys(symbolOptionsCb);
    function dataEach(data2, idx) {
      var rawValue = seriesModel.getRawValue(idx);
      var params = seriesModel.getDataParams(idx);
      for (var i3 = 0; i3 < symbolPropsCb.length; i3++) {
        var symbolPropName2 = symbolPropsCb[i3];
        data2.setItemVisual(idx, symbolPropName2, symbolOptionsCb[symbolPropName2](rawValue, params));
      }
    }
    return {
      dataEach: hasCallback ? dataEach : null
    };
  }
};
var dataSymbolTask = {
  createOnAllSeries: true,
  // For legend.
  performRawSeries: true,
  reset: function(seriesModel, ecModel) {
    if (!seriesModel.hasSymbolVisual) {
      return;
    }
    if (ecModel.isSeriesFiltered(seriesModel)) {
      return;
    }
    var data = seriesModel.getData();
    function dataEach(data2, idx) {
      var itemModel = data2.getItemModel(idx);
      for (var i2 = 0; i2 < SYMBOL_PROPS.length; i2++) {
        var symbolPropName = SYMBOL_PROPS[i2];
        var val = itemModel.getShallow(symbolPropName, true);
        if (val != null) {
          data2.setItemVisual(idx, symbolPropName, val);
        }
      }
    }
    return {
      dataEach: data.hasItemOption ? dataEach : null
    };
  }
};

// node_modules/echarts/lib/visual/helper.js
function getItemVisualFromData(data, dataIndex, key2) {
  switch (key2) {
    case "color":
      var style2 = data.getItemVisual(dataIndex, "style");
      return style2[data.getVisual("drawType")];
    case "opacity":
      return data.getItemVisual(dataIndex, "style").opacity;
    case "symbol":
    case "symbolSize":
    case "liftZ":
      return data.getItemVisual(dataIndex, key2);
    default:
      if (true) {
        console.warn("Unknown visual type " + key2);
      }
  }
}
function getVisualFromData(data, key2) {
  switch (key2) {
    case "color":
      var style2 = data.getVisual("style");
      return style2[data.getVisual("drawType")];
    case "opacity":
      return data.getVisual("style").opacity;
    case "symbol":
    case "symbolSize":
    case "liftZ":
      return data.getVisual(key2);
    default:
      if (true) {
        console.warn("Unknown visual type " + key2);
      }
  }
}
function setItemVisualFromData(data, dataIndex, key2, value) {
  switch (key2) {
    case "color":
      var style2 = data.ensureUniqueItemVisual(dataIndex, "style");
      style2[data.getVisual("drawType")] = value;
      data.setItemVisual(dataIndex, "colorFromPalette", false);
      break;
    case "opacity":
      data.ensureUniqueItemVisual(dataIndex, "style").opacity = value;
      break;
    case "symbol":
    case "symbolSize":
    case "liftZ":
      data.setItemVisual(dataIndex, key2, value);
      break;
    default:
      if (true) {
        console.warn("Unknown visual type " + key2);
      }
  }
}

// node_modules/echarts/lib/util/event.js
function findEventDispatcher(target, det, returnFirstMatch) {
  var found;
  while (target) {
    if (det(target)) {
      found = target;
      if (returnFirstMatch) {
        break;
      }
    }
    target = target.__hostTarget || target.parent;
  }
  return found;
}

// node_modules/echarts/lib/core/lifecycle.js
var lifecycle = new Eventful_default();
var lifecycle_default = lifecycle;

// node_modules/echarts/lib/core/impl.js
var implsStore = {};
function registerImpl(name, impl) {
  if (true) {
    if (implsStore[name]) {
      error("Already has an implementation of " + name + ".");
    }
  }
  implsStore[name] = impl;
}
function getImpl(name) {
  if (true) {
    if (!implsStore[name]) {
      error("Implementation of " + name + " doesn't exists.");
    }
  }
  return implsStore[name];
}

// node_modules/echarts/lib/chart/custom/customSeriesRegister.js
var customRenderers = {};
function registerCustomSeries(type, renderItem) {
  customRenderers[type] = renderItem;
}
function getCustomSeries(type) {
  return customRenderers[type];
}

// node_modules/zrender/lib/core/WeakMap.js
var wmUniqueIndex = Math.round(Math.random() * 9);
var supportDefineProperty = typeof Object.defineProperty === "function";
var WeakMap2 = (function() {
  function WeakMap3() {
    this._id = "__ec_inner_" + wmUniqueIndex++;
  }
  WeakMap3.prototype.get = function(key2) {
    return this._guard(key2)[this._id];
  };
  WeakMap3.prototype.set = function(key2, value) {
    var target = this._guard(key2);
    if (supportDefineProperty) {
      Object.defineProperty(target, this._id, {
        value,
        enumerable: false,
        configurable: true
      });
    } else {
      target[this._id] = value;
    }
    return this;
  };
  WeakMap3.prototype["delete"] = function(key2) {
    if (this.has(key2)) {
      delete this._guard(key2)[this._id];
      return true;
    }
    return false;
  };
  WeakMap3.prototype.has = function(key2) {
    return !!this._guard(key2)[this._id];
  };
  WeakMap3.prototype._guard = function(key2) {
    if (key2 !== Object(key2)) {
      throw TypeError("Value of WeakMap is not a non-null object.");
    }
    return key2;
  };
  return WeakMap3;
})();
var WeakMap_default = WeakMap2;

// node_modules/zrender/lib/canvas/helper.js
function isSafeNum(num) {
  return isFinite(num);
}
function createLinearGradient(ctx, obj, rect) {
  var x = obj.x == null ? 0 : obj.x;
  var x2 = obj.x2 == null ? 1 : obj.x2;
  var y = obj.y == null ? 0 : obj.y;
  var y2 = obj.y2 == null ? 0 : obj.y2;
  if (!obj.global) {
    x = x * rect.width + rect.x;
    x2 = x2 * rect.width + rect.x;
    y = y * rect.height + rect.y;
    y2 = y2 * rect.height + rect.y;
  }
  x = isSafeNum(x) ? x : 0;
  x2 = isSafeNum(x2) ? x2 : 1;
  y = isSafeNum(y) ? y : 0;
  y2 = isSafeNum(y2) ? y2 : 0;
  var canvasGradient = ctx.createLinearGradient(x, y, x2, y2);
  return canvasGradient;
}
function createRadialGradient(ctx, obj, rect) {
  var width = rect.width;
  var height = rect.height;
  var min3 = Math.min(width, height);
  var x = obj.x == null ? 0.5 : obj.x;
  var y = obj.y == null ? 0.5 : obj.y;
  var r = obj.r == null ? 0.5 : obj.r;
  if (!obj.global) {
    x = x * width + rect.x;
    y = y * height + rect.y;
    r = r * min3;
  }
  x = isSafeNum(x) ? x : 0.5;
  y = isSafeNum(y) ? y : 0.5;
  r = r >= 0 && isSafeNum(r) ? r : 0.5;
  var canvasGradient = ctx.createRadialGradient(x, y, 0, x, y, r);
  return canvasGradient;
}
function getCanvasGradient(ctx, obj, rect) {
  var canvasGradient = obj.type === "radial" ? createRadialGradient(ctx, obj, rect) : createLinearGradient(ctx, obj, rect);
  var colorStops = obj.colorStops;
  for (var i2 = 0; i2 < colorStops.length; i2++) {
    canvasGradient.addColorStop(colorStops[i2].offset, colorStops[i2].color);
  }
  return canvasGradient;
}
function isClipPathChanged(clipPaths, prevClipPaths) {
  if (clipPaths === prevClipPaths || !clipPaths && !prevClipPaths) {
    return false;
  }
  if (!clipPaths || !prevClipPaths || clipPaths.length !== prevClipPaths.length) {
    return true;
  }
  for (var i2 = 0; i2 < clipPaths.length; i2++) {
    if (clipPaths[i2] !== prevClipPaths[i2]) {
      return true;
    }
  }
  return false;
}
function parseInt10(val) {
  return parseInt(val, 10);
}
function getSize(root, whIdx, opts) {
  var wh = ["width", "height"][whIdx];
  var cwh = ["clientWidth", "clientHeight"][whIdx];
  var plt = ["paddingLeft", "paddingTop"][whIdx];
  var prb = ["paddingRight", "paddingBottom"][whIdx];
  if (opts[wh] != null && opts[wh] !== "auto") {
    return parseFloat(opts[wh]);
  }
  var stl = document.defaultView.getComputedStyle(root);
  return (root[cwh] || parseInt10(stl[wh]) || parseInt10(root.style[wh])) - (parseInt10(stl[plt]) || 0) - (parseInt10(stl[prb]) || 0) || 0;
}

// node_modules/zrender/lib/canvas/dashStyle.js
function normalizeLineDash(lineType, lineWidth) {
  if (!lineType || lineType === "solid" || !(lineWidth > 0)) {
    return null;
  }
  return lineType === "dashed" ? [4 * lineWidth, 2 * lineWidth] : lineType === "dotted" ? [lineWidth] : isNumber(lineType) ? [lineType] : isArray(lineType) ? lineType : null;
}
function getLineDash(el) {
  var style2 = el.style;
  var lineDash = style2.lineDash && style2.lineWidth > 0 && normalizeLineDash(style2.lineDash, style2.lineWidth);
  var lineDashOffset = style2.lineDashOffset;
  if (lineDash) {
    var lineScale_1 = style2.strokeNoScale && el.getLineScale ? el.getLineScale() : 1;
    if (lineScale_1 && lineScale_1 !== 1) {
      lineDash = map2(lineDash, function(rawVal) {
        return rawVal / lineScale_1;
      });
      lineDashOffset /= lineScale_1;
    }
  }
  return [lineDash, lineDashOffset];
}

// node_modules/zrender/lib/canvas/graphic.js
var pathProxyForDraw = new PathProxy_default(true);
function styleHasStroke(style2) {
  var stroke = style2.stroke;
  return !(stroke == null || stroke === "none" || !(style2.lineWidth > 0));
}
function isValidStrokeFillStyle(strokeOrFill) {
  return typeof strokeOrFill === "string" && strokeOrFill !== "none";
}
function styleHasFill(style2) {
  var fill = style2.fill;
  return fill != null && fill !== "none";
}
function doFillPath(ctx, style2) {
  if (style2.fillOpacity != null && style2.fillOpacity !== 1) {
    var originalGlobalAlpha = ctx.globalAlpha;
    ctx.globalAlpha = style2.fillOpacity * style2.opacity;
    ctx.fill();
    ctx.globalAlpha = originalGlobalAlpha;
  } else {
    ctx.fill();
  }
}
function doStrokePath(ctx, style2) {
  if (style2.strokeOpacity != null && style2.strokeOpacity !== 1) {
    var originalGlobalAlpha = ctx.globalAlpha;
    ctx.globalAlpha = style2.strokeOpacity * style2.opacity;
    ctx.stroke();
    ctx.globalAlpha = originalGlobalAlpha;
  } else {
    ctx.stroke();
  }
}
function createCanvasPattern(ctx, pattern, el) {
  var image = createOrUpdateImage(pattern.image, pattern.__image, el);
  if (isImageReady(image)) {
    var canvasPattern = ctx.createPattern(image, pattern.repeat || "repeat");
    if (typeof DOMMatrix === "function" && canvasPattern && canvasPattern.setTransform) {
      var matrix = new DOMMatrix();
      matrix.translateSelf(pattern.x || 0, pattern.y || 0);
      matrix.rotateSelf(0, 0, (pattern.rotation || 0) * RADIAN_TO_DEGREE);
      matrix.scaleSelf(pattern.scaleX || 1, pattern.scaleY || 1);
      canvasPattern.setTransform(matrix);
    }
    return canvasPattern;
  }
}
function brushPath(ctx, el, style2, canBatch, scope) {
  var _a2;
  var hasStroke = styleHasStroke(style2);
  var hasFill = styleHasFill(style2);
  var strokePercent = style2.strokePercent;
  var strokePart = strokePercent < 1;
  var firstDraw = !el.path;
  if ((!el.silent || strokePart) && firstDraw) {
    el.createPathProxy();
  }
  var path = el.path || pathProxyForDraw;
  var dirtyFlag = el.__dirty;
  if (!canBatch) {
    var fill = style2.fill;
    var stroke = style2.stroke;
    var hasFillGradient = hasFill && !!fill.colorStops;
    var hasStrokeGradient = hasStroke && !!stroke.colorStops;
    var hasFillPattern = hasFill && !!fill.image;
    var hasStrokePattern = hasStroke && !!stroke.image;
    var fillGradient = void 0;
    var strokeGradient = void 0;
    var fillPattern = void 0;
    var strokePattern = void 0;
    var rect = void 0;
    if (hasFillGradient || hasStrokeGradient) {
      rect = el.getBoundingRect();
    }
    if (hasFillGradient) {
      fillGradient = dirtyFlag ? getCanvasGradient(ctx, fill, rect) : el.__canvasFillGradient;
      el.__canvasFillGradient = fillGradient;
    }
    if (hasStrokeGradient) {
      strokeGradient = dirtyFlag ? getCanvasGradient(ctx, stroke, rect) : el.__canvasStrokeGradient;
      el.__canvasStrokeGradient = strokeGradient;
    }
    if (hasFillPattern) {
      fillPattern = dirtyFlag || !el.__canvasFillPattern ? createCanvasPattern(ctx, fill, el) : el.__canvasFillPattern;
      el.__canvasFillPattern = fillPattern;
    }
    if (hasStrokePattern) {
      strokePattern = dirtyFlag || !el.__canvasStrokePattern ? createCanvasPattern(ctx, stroke, el) : el.__canvasStrokePattern;
      el.__canvasStrokePattern = strokePattern;
    }
    if (hasFillGradient) {
      ctx.fillStyle = fillGradient;
    } else if (hasFillPattern) {
      if (fillPattern) {
        ctx.fillStyle = fillPattern;
      } else {
        hasFill = false;
      }
    }
    if (hasStrokeGradient) {
      ctx.strokeStyle = strokeGradient;
    } else if (hasStrokePattern) {
      if (strokePattern) {
        ctx.strokeStyle = strokePattern;
      } else {
        hasStroke = false;
      }
    }
  }
  var scale3 = el.getGlobalScale();
  path.setScale(scale3[0], scale3[1], el.segmentIgnoreThreshold);
  var lineDash;
  var lineDashOffset;
  if (ctx.setLineDash && style2.lineDash) {
    _a2 = getLineDash(el), lineDash = _a2[0], lineDashOffset = _a2[1];
  }
  var needsRebuild = true;
  if (firstDraw || dirtyFlag & SHAPE_CHANGED_BIT) {
    path.setDPR(ctx.dpr);
    if (strokePart) {
      path.setContext(null);
    } else {
      path.setContext(ctx);
      needsRebuild = false;
    }
    path.reset();
    el.buildPath(path, el.shape, canBatch);
    path.toStatic();
    el.pathUpdated();
  }
  if (needsRebuild) {
    path.rebuildPath(ctx, strokePart ? strokePercent : 1);
  }
  if (lineDash) {
    ctx.setLineDash(lineDash);
    ctx.lineDashOffset = lineDashOffset;
  }
  if (!canBatch) {
    if (style2.strokeFirst) {
      if (hasStroke) {
        doStrokePath(ctx, style2);
      }
      if (hasFill) {
        doFillPath(ctx, style2);
      }
    } else {
      if (hasFill) {
        doFillPath(ctx, style2);
      }
      if (hasStroke) {
        doStrokePath(ctx, style2);
      }
    }
  } else {
    scope.batchFill = hasFill;
    scope.batchStroke = hasStroke;
  }
  if (lineDash) {
    ctx.setLineDash([]);
  }
}
function brushImage(ctx, el, style2) {
  var image = el.__image = createOrUpdateImage(style2.image, el.__image, el, el.onload);
  if (!image || !isImageReady(image)) {
    return;
  }
  var x = style2.x || 0;
  var y = style2.y || 0;
  var width = el.getWidth();
  var height = el.getHeight();
  var aspect = image.width / image.height;
  if (width == null && height != null) {
    width = height * aspect;
  } else if (height == null && width != null) {
    height = width / aspect;
  } else if (width == null && height == null) {
    width = image.width;
    height = image.height;
  }
  if (style2.sWidth && style2.sHeight) {
    var sx = style2.sx || 0;
    var sy = style2.sy || 0;
    ctx.drawImage(image, sx, sy, style2.sWidth, style2.sHeight, x, y, width, height);
  } else if (style2.sx && style2.sy) {
    var sx = style2.sx;
    var sy = style2.sy;
    var sWidth = width - sx;
    var sHeight = height - sy;
    ctx.drawImage(image, sx, sy, sWidth, sHeight, x, y, width, height);
  } else {
    ctx.drawImage(image, x, y, width, height);
  }
}
function brushText(ctx, el, style2) {
  var _a2;
  var text = style2.text;
  text != null && (text += "");
  if (text) {
    ctx.font = style2.font || DEFAULT_FONT;
    ctx.textAlign = style2.textAlign;
    ctx.textBaseline = style2.textBaseline;
    var lineDash = void 0;
    var lineDashOffset = void 0;
    if (ctx.setLineDash && style2.lineDash) {
      _a2 = getLineDash(el), lineDash = _a2[0], lineDashOffset = _a2[1];
    }
    if (lineDash) {
      ctx.setLineDash(lineDash);
      ctx.lineDashOffset = lineDashOffset;
    }
    if (style2.strokeFirst) {
      if (styleHasStroke(style2)) {
        ctx.strokeText(text, style2.x, style2.y);
      }
      if (styleHasFill(style2)) {
        ctx.fillText(text, style2.x, style2.y);
      }
    } else {
      if (styleHasFill(style2)) {
        ctx.fillText(text, style2.x, style2.y);
      }
      if (styleHasStroke(style2)) {
        ctx.strokeText(text, style2.x, style2.y);
      }
    }
    if (lineDash) {
      ctx.setLineDash([]);
    }
  }
}
var SHADOW_NUMBER_PROPS = ["shadowBlur", "shadowOffsetX", "shadowOffsetY"];
var STROKE_PROPS = [
  ["lineCap", "butt"],
  ["lineJoin", "miter"],
  ["miterLimit", 10]
];
function bindCommonProps(ctx, style2, prevStyle, forceSetAll, scope) {
  var styleChanged = false;
  if (!forceSetAll) {
    prevStyle = prevStyle || {};
    if (style2 === prevStyle) {
      return false;
    }
  }
  if (forceSetAll || style2.opacity !== prevStyle.opacity) {
    flushPathDrawn(ctx, scope);
    styleChanged = true;
    var opacity = Math.max(Math.min(style2.opacity, 1), 0);
    ctx.globalAlpha = isNaN(opacity) ? DEFAULT_COMMON_STYLE.opacity : opacity;
  }
  if (forceSetAll || style2.blend !== prevStyle.blend) {
    if (!styleChanged) {
      flushPathDrawn(ctx, scope);
      styleChanged = true;
    }
    ctx.globalCompositeOperation = style2.blend || DEFAULT_COMMON_STYLE.blend;
  }
  for (var i2 = 0; i2 < SHADOW_NUMBER_PROPS.length; i2++) {
    var propName = SHADOW_NUMBER_PROPS[i2];
    if (forceSetAll || style2[propName] !== prevStyle[propName]) {
      if (!styleChanged) {
        flushPathDrawn(ctx, scope);
        styleChanged = true;
      }
      ctx[propName] = ctx.dpr * (style2[propName] || 0);
    }
  }
  if (forceSetAll || style2.shadowColor !== prevStyle.shadowColor) {
    if (!styleChanged) {
      flushPathDrawn(ctx, scope);
      styleChanged = true;
    }
    ctx.shadowColor = style2.shadowColor || DEFAULT_COMMON_STYLE.shadowColor;
  }
  return styleChanged;
}
function bindPathAndTextCommonStyle(ctx, el, prevEl, forceSetAll, scope) {
  var style2 = el.style;
  var prevStyle = forceSetAll ? null : prevEl && prevEl.style || {};
  if (style2 === prevStyle) {
    return false;
  }
  var styleChanged = bindCommonProps(ctx, style2, prevStyle, forceSetAll, scope);
  if (forceSetAll || style2.fill !== prevStyle.fill) {
    if (!styleChanged) {
      flushPathDrawn(ctx, scope);
      styleChanged = true;
    }
    isValidStrokeFillStyle(style2.fill) && (ctx.fillStyle = style2.fill);
  }
  if (forceSetAll || style2.stroke !== prevStyle.stroke) {
    if (!styleChanged) {
      flushPathDrawn(ctx, scope);
      styleChanged = true;
    }
    isValidStrokeFillStyle(style2.stroke) && (ctx.strokeStyle = style2.stroke);
  }
  if (forceSetAll || style2.opacity !== prevStyle.opacity) {
    if (!styleChanged) {
      flushPathDrawn(ctx, scope);
      styleChanged = true;
    }
    ctx.globalAlpha = style2.opacity == null ? 1 : style2.opacity;
  }
  if (el.hasStroke()) {
    var lineWidth = style2.lineWidth;
    var newLineWidth = lineWidth / (style2.strokeNoScale && el.getLineScale ? el.getLineScale() : 1);
    if (ctx.lineWidth !== newLineWidth) {
      if (!styleChanged) {
        flushPathDrawn(ctx, scope);
        styleChanged = true;
      }
      ctx.lineWidth = newLineWidth;
    }
  }
  for (var i2 = 0; i2 < STROKE_PROPS.length; i2++) {
    var prop = STROKE_PROPS[i2];
    var propName = prop[0];
    if (forceSetAll || style2[propName] !== prevStyle[propName]) {
      if (!styleChanged) {
        flushPathDrawn(ctx, scope);
        styleChanged = true;
      }
      ctx[propName] = style2[propName] || prop[1];
    }
  }
  return styleChanged;
}
function bindImageStyle(ctx, el, prevEl, forceSetAll, scope) {
  return bindCommonProps(ctx, el.style, prevEl && prevEl.style, forceSetAll, scope);
}
function setContextTransform(ctx, el) {
  var m2 = el.transform;
  var dpr2 = ctx.dpr || 1;
  if (m2) {
    ctx.setTransform(dpr2 * m2[0], dpr2 * m2[1], dpr2 * m2[2], dpr2 * m2[3], dpr2 * m2[4], dpr2 * m2[5]);
  } else {
    ctx.setTransform(dpr2, 0, 0, dpr2, 0, 0);
  }
}
function updateClipStatus(clipPaths, ctx, scope) {
  var allClipped = false;
  for (var i2 = 0; i2 < clipPaths.length; i2++) {
    var clipPath = clipPaths[i2];
    allClipped = allClipped || clipPath.isZeroArea();
    setContextTransform(ctx, clipPath);
    ctx.beginPath();
    clipPath.buildPath(ctx, clipPath.shape);
    ctx.clip();
  }
  scope.allClipped = allClipped;
}
function isTransformChanged(m0, m1) {
  if (m0 && m1) {
    return m0[0] !== m1[0] || m0[1] !== m1[1] || m0[2] !== m1[2] || m0[3] !== m1[3] || m0[4] !== m1[4] || m0[5] !== m1[5];
  } else if (!m0 && !m1) {
    return false;
  }
  return true;
}
var DRAW_TYPE_PATH = 1;
var DRAW_TYPE_IMAGE = 2;
var DRAW_TYPE_TEXT = 3;
var DRAW_TYPE_INCREMENTAL = 4;
function canPathBatch(style2) {
  var hasFill = styleHasFill(style2);
  var hasStroke = styleHasStroke(style2);
  return !(style2.lineDash || !(+hasFill ^ +hasStroke) || hasFill && typeof style2.fill !== "string" || hasStroke && typeof style2.stroke !== "string" || style2.strokePercent < 1 || style2.strokeOpacity < 1 || style2.fillOpacity < 1);
}
function flushPathDrawn(ctx, scope) {
  if (scope.batchFill) {
    scope.batchFill = false;
    ctx.fill();
  }
  if (scope.batchStroke) {
    scope.batchStroke = false;
    ctx.stroke();
  }
}
function brushSingle(ctx, el) {
  var scope = { inHover: false, viewWidth: 0, viewHeight: 0, beforeBrushParam: {} };
  brush(ctx, el, scope);
  brushLoopFinalize(ctx, scope);
}
function brush(ctx, el, scope) {
  var m2 = el.transform;
  if (!el.shouldBePainted(scope.viewWidth, scope.viewHeight, false, false)) {
    el.__dirty &= ~REDRAW_BIT;
    el.__isRendered = false;
    return;
  }
  var clipPaths = el.__clipPaths;
  var prevElClipPaths = scope.prevElClipPaths;
  var style2 = el.style;
  var forceSetTransform = false;
  var forceSetStyle = false;
  if (!prevElClipPaths || isClipPathChanged(clipPaths, prevElClipPaths)) {
    if (prevElClipPaths) {
      flushPathDrawn(ctx, scope);
      ctx.restore();
      forceSetStyle = forceSetTransform = true;
      scope.prevElClipPaths = null;
      scope.allClipped = false;
      scope.prevEl = null;
    }
    if (clipPaths && clipPaths.length) {
      flushPathDrawn(ctx, scope);
      ctx.save();
      updateClipStatus(clipPaths, ctx, scope);
      forceSetTransform = true;
      scope.prevElClipPaths = clipPaths;
    }
  }
  if (scope.allClipped) {
    el.__dirty &= ~REDRAW_BIT;
    el.__isRendered = false;
    return;
  }
  el.beforeBrush && el.beforeBrush(scope.beforeBrushParam);
  el.innerBeforeBrush();
  var prevEl = scope.prevEl;
  if (!prevEl) {
    forceSetStyle = forceSetTransform = true;
  }
  var canBatchPath = el instanceof Path_default && el.autoBatch && canPathBatch(style2);
  if (forceSetTransform || isTransformChanged(m2, prevEl.transform)) {
    flushPathDrawn(ctx, scope);
    setContextTransform(ctx, el);
  } else if (!canBatchPath) {
    flushPathDrawn(ctx, scope);
  }
  if (el instanceof Path_default) {
    if (scope.lastDrawType !== DRAW_TYPE_PATH) {
      forceSetStyle = true;
      scope.lastDrawType = DRAW_TYPE_PATH;
    }
    bindPathAndTextCommonStyle(ctx, el, prevEl, forceSetStyle, scope);
    if (!canBatchPath || !scope.batchFill && !scope.batchStroke) {
      ctx.beginPath();
    }
    brushPath(ctx, el, style2, canBatchPath, scope);
  } else {
    if (el instanceof TSpan_default) {
      if (scope.lastDrawType !== DRAW_TYPE_TEXT) {
        forceSetStyle = true;
        scope.lastDrawType = DRAW_TYPE_TEXT;
      }
      bindPathAndTextCommonStyle(ctx, el, prevEl, forceSetStyle, scope);
      brushText(ctx, el, style2);
    } else if (el instanceof Image_default) {
      if (scope.lastDrawType !== DRAW_TYPE_IMAGE) {
        forceSetStyle = true;
        scope.lastDrawType = DRAW_TYPE_IMAGE;
      }
      bindImageStyle(ctx, el, prevEl, forceSetStyle, scope);
      brushImage(ctx, el, style2);
    } else if (el.getTemporalDisplayables) {
      if (scope.lastDrawType !== DRAW_TYPE_INCREMENTAL) {
        forceSetStyle = true;
        scope.lastDrawType = DRAW_TYPE_INCREMENTAL;
      }
      brushIncremental(ctx, el, scope);
    }
  }
  el.innerAfterBrush();
  if (el.afterBrush) {
    if (canBatchPath) {
      flushPathDrawn(ctx, scope);
    }
    el.afterBrush();
  }
  scope.prevEl = el;
  el.__dirty = 0;
  el.__isRendered = true;
}
function brushLoopFinalize(ctx, scope) {
  flushPathDrawn(ctx, scope);
  if (scope.prevElClipPaths) {
    ctx.restore();
  }
}
function brushIncremental(ctx, el, scope) {
  var displayables = el.getDisplayables();
  var temporalDisplayables = el.getTemporalDisplayables();
  ctx.save();
  var innerScope = {
    prevElClipPaths: null,
    prevEl: null,
    allClipped: false,
    viewWidth: scope.viewWidth,
    viewHeight: scope.viewHeight,
    inHover: scope.inHover,
    beforeBrushParam: {}
  };
  var i2;
  var len2;
  for (i2 = el.getCursor(), len2 = displayables.length; i2 < len2; i2++) {
    var displayable = displayables[i2];
    displayable.beforeBrush && displayable.beforeBrush(scope.beforeBrushParam);
    displayable.innerBeforeBrush();
    brush(ctx, displayable, innerScope);
    displayable.innerAfterBrush();
    displayable.afterBrush && displayable.afterBrush();
    innerScope.prevEl = displayable;
  }
  brushLoopFinalize(ctx, innerScope);
  for (var i_1 = 0, len_1 = temporalDisplayables.length; i_1 < len_1; i_1++) {
    var displayable = temporalDisplayables[i_1];
    displayable.beforeBrush && displayable.beforeBrush(scope.beforeBrushParam);
    displayable.innerBeforeBrush();
    brush(ctx, displayable, innerScope);
    displayable.innerAfterBrush();
    displayable.afterBrush && displayable.afterBrush();
    innerScope.prevEl = displayable;
  }
  brushLoopFinalize(ctx, innerScope);
  el.clearTemporalDisplayables();
  el.notClear = true;
  ctx.restore();
}

// node_modules/echarts/lib/util/decal.js
var decalMap = new WeakMap_default();
var decalCache = new LRU_default(100);
var decalKeys = ["symbol", "symbolSize", "symbolKeepAspect", "color", "backgroundColor", "dashArrayX", "dashArrayY", "maxTileWidth", "maxTileHeight"];
function createOrUpdatePatternFromDecal(decalObject, api) {
  if (decalObject === "none") {
    return null;
  }
  var dpr2 = api.getDevicePixelRatio();
  var zr = api.getZr();
  var isSVG = zr.painter.type === "svg";
  if (decalObject.dirty) {
    decalMap["delete"](decalObject);
  }
  var oldPattern = decalMap.get(decalObject);
  if (oldPattern) {
    return oldPattern;
  }
  var decalOpt = defaults(decalObject, {
    symbol: "rect",
    symbolSize: 1,
    symbolKeepAspect: true,
    color: "rgba(0, 0, 0, 0.2)",
    backgroundColor: null,
    dashArrayX: 5,
    dashArrayY: 5,
    rotation: 0,
    maxTileWidth: 512,
    maxTileHeight: 512
  });
  if (decalOpt.backgroundColor === "none") {
    decalOpt.backgroundColor = null;
  }
  var pattern = {
    repeat: "repeat"
  };
  setPatternnSource(pattern);
  pattern.rotation = decalOpt.rotation;
  pattern.scaleX = pattern.scaleY = isSVG ? 1 : 1 / dpr2;
  decalMap.set(decalObject, pattern);
  decalObject.dirty = false;
  return pattern;
  function setPatternnSource(pattern2) {
    var keys2 = [dpr2];
    var isValidKey = true;
    for (var i2 = 0; i2 < decalKeys.length; ++i2) {
      var value = decalOpt[decalKeys[i2]];
      if (value != null && !isArray(value) && !isString(value) && !isNumber(value) && typeof value !== "boolean") {
        isValidKey = false;
        break;
      }
      keys2.push(value);
    }
    var cacheKey;
    if (isValidKey) {
      cacheKey = keys2.join(",") + (isSVG ? "-svg" : "");
      var cache = decalCache.get(cacheKey);
      if (cache) {
        isSVG ? pattern2.svgElement = cache : pattern2.image = cache;
      }
    }
    var dashArrayX = normalizeDashArrayX(decalOpt.dashArrayX);
    var dashArrayY = normalizeDashArrayY(decalOpt.dashArrayY);
    var symbolArray = normalizeSymbolArray(decalOpt.symbol);
    var lineBlockLengthsX = getLineBlockLengthX(dashArrayX);
    var lineBlockLengthY = getLineBlockLengthY(dashArrayY);
    var canvas = !isSVG && platformApi.createCanvas();
    var svgRoot = isSVG && {
      tag: "g",
      attrs: {},
      key: "dcl",
      children: []
    };
    var pSize = getPatternSize();
    var ctx;
    if (canvas) {
      canvas.width = pSize.width * dpr2;
      canvas.height = pSize.height * dpr2;
      ctx = canvas.getContext("2d");
    }
    brushDecal();
    if (isValidKey) {
      decalCache.put(cacheKey, canvas || svgRoot);
    }
    pattern2.image = canvas;
    pattern2.svgElement = svgRoot;
    pattern2.svgWidth = pSize.width;
    pattern2.svgHeight = pSize.height;
    function getPatternSize() {
      var width = 1;
      for (var i3 = 0, xlen = lineBlockLengthsX.length; i3 < xlen; ++i3) {
        width = getLeastCommonMultiple(width, lineBlockLengthsX[i3]);
      }
      var symbolRepeats = 1;
      for (var i3 = 0, xlen = symbolArray.length; i3 < xlen; ++i3) {
        symbolRepeats = getLeastCommonMultiple(symbolRepeats, symbolArray[i3].length);
      }
      width *= symbolRepeats;
      var height = lineBlockLengthY * lineBlockLengthsX.length * symbolArray.length;
      if (true) {
        var warn2 = function(attrName) {
          console.warn("Calculated decal size is greater than " + attrName + " due to decal option settings so " + attrName + " is used for the decal size. Please consider changing the decal option to make a smaller decal or set " + attrName + " to be larger to avoid incontinuity.");
        };
        if (width > decalOpt.maxTileWidth) {
          warn2("maxTileWidth");
        }
        if (height > decalOpt.maxTileHeight) {
          warn2("maxTileHeight");
        }
      }
      return {
        width: Math.max(1, Math.min(width, decalOpt.maxTileWidth)),
        height: Math.max(1, Math.min(height, decalOpt.maxTileHeight))
      };
    }
    function brushDecal() {
      if (ctx) {
        ctx.clearRect(0, 0, canvas.width, canvas.height);
        if (decalOpt.backgroundColor) {
          ctx.fillStyle = decalOpt.backgroundColor;
          ctx.fillRect(0, 0, canvas.width, canvas.height);
        }
      }
      var ySum = 0;
      for (var i3 = 0; i3 < dashArrayY.length; ++i3) {
        ySum += dashArrayY[i3];
      }
      if (ySum <= 0) {
        return;
      }
      var y = -lineBlockLengthY;
      var yId = 0;
      var yIdTotal = 0;
      var xId0 = 0;
      while (y < pSize.height) {
        if (yId % 2 === 0) {
          var symbolYId = yIdTotal / 2 % symbolArray.length;
          var x = 0;
          var xId1 = 0;
          var xId1Total = 0;
          while (x < pSize.width * 2) {
            var xSum = 0;
            for (var i3 = 0; i3 < dashArrayX[xId0].length; ++i3) {
              xSum += dashArrayX[xId0][i3];
            }
            if (xSum <= 0) {
              break;
            }
            if (xId1 % 2 === 0) {
              var size = (1 - decalOpt.symbolSize) * 0.5;
              var left = x + dashArrayX[xId0][xId1] * size;
              var top_1 = y + dashArrayY[yId] * size;
              var width = dashArrayX[xId0][xId1] * decalOpt.symbolSize;
              var height = dashArrayY[yId] * decalOpt.symbolSize;
              var symbolXId = xId1Total / 2 % symbolArray[symbolYId].length;
              brushSymbol(left, top_1, width, height, symbolArray[symbolYId][symbolXId]);
            }
            x += dashArrayX[xId0][xId1];
            ++xId1Total;
            ++xId1;
            if (xId1 === dashArrayX[xId0].length) {
              xId1 = 0;
            }
          }
          ++xId0;
          if (xId0 === dashArrayX.length) {
            xId0 = 0;
          }
        }
        y += dashArrayY[yId];
        ++yIdTotal;
        ++yId;
        if (yId === dashArrayY.length) {
          yId = 0;
        }
      }
      function brushSymbol(x2, y2, width2, height2, symbolType) {
        var scale3 = isSVG ? 1 : dpr2;
        var symbol = createSymbol(symbolType, x2 * scale3, y2 * scale3, width2 * scale3, height2 * scale3, decalOpt.color, decalOpt.symbolKeepAspect);
        if (isSVG) {
          var symbolVNode = zr.painter.renderOneToVNode(symbol);
          if (symbolVNode) {
            svgRoot.children.push(symbolVNode);
          }
        } else {
          brushSingle(ctx, symbol);
        }
      }
    }
  }
}
function normalizeSymbolArray(symbol) {
  if (!symbol || symbol.length === 0) {
    return [["rect"]];
  }
  if (isString(symbol)) {
    return [[symbol]];
  }
  var isAllString = true;
  for (var i2 = 0; i2 < symbol.length; ++i2) {
    if (!isString(symbol[i2])) {
      isAllString = false;
      break;
    }
  }
  if (isAllString) {
    return normalizeSymbolArray([symbol]);
  }
  var result = [];
  for (var i2 = 0; i2 < symbol.length; ++i2) {
    if (isString(symbol[i2])) {
      result.push([symbol[i2]]);
    } else {
      result.push(symbol[i2]);
    }
  }
  return result;
}
function normalizeDashArrayX(dash) {
  if (!dash || dash.length === 0) {
    return [[0, 0]];
  }
  if (isNumber(dash)) {
    var dashValue = Math.ceil(dash);
    return [[dashValue, dashValue]];
  }
  var isAllNumber = true;
  for (var i2 = 0; i2 < dash.length; ++i2) {
    if (!isNumber(dash[i2])) {
      isAllNumber = false;
      break;
    }
  }
  if (isAllNumber) {
    return normalizeDashArrayX([dash]);
  }
  var result = [];
  for (var i2 = 0; i2 < dash.length; ++i2) {
    if (isNumber(dash[i2])) {
      var dashValue = Math.ceil(dash[i2]);
      result.push([dashValue, dashValue]);
    } else {
      var dashValue = map2(dash[i2], function(n) {
        return Math.ceil(n);
      });
      if (dashValue.length % 2 === 1) {
        result.push(dashValue.concat(dashValue));
      } else {
        result.push(dashValue);
      }
    }
  }
  return result;
}
function normalizeDashArrayY(dash) {
  if (!dash || typeof dash === "object" && dash.length === 0) {
    return [0, 0];
  }
  if (isNumber(dash)) {
    var dashValue_1 = Math.ceil(dash);
    return [dashValue_1, dashValue_1];
  }
  var dashValue = map2(dash, function(n) {
    return Math.ceil(n);
  });
  return dash.length % 2 ? dashValue.concat(dashValue) : dashValue;
}
function getLineBlockLengthX(dash) {
  return map2(dash, function(line) {
    return getLineBlockLengthY(line);
  });
}
function getLineBlockLengthY(dash) {
  var blockLength = 0;
  for (var i2 = 0; i2 < dash.length; ++i2) {
    blockLength += dash[i2];
  }
  if (dash.length % 2 === 1) {
    return blockLength * 2;
  }
  return blockLength;
}

// node_modules/echarts/lib/visual/decal.js
var decalVisualStageHandler = createSimpleOverallStageHandler2(decalVisual);
function decalVisual(ecModel, api) {
  ecModel.eachRawSeries(function(seriesModel) {
    if (ecModel.isSeriesFiltered(seriesModel)) {
      return;
    }
    var data = seriesModel.getData();
    if (data.hasItemVisual()) {
      data.each(function(idx) {
        var decal2 = data.getItemVisual(idx, "decal");
        if (decal2) {
          var itemStyle = data.ensureUniqueItemVisual(idx, "style");
          itemStyle.decal = createOrUpdatePatternFromDecal(decal2, api);
        }
      });
    }
    var decal = data.getVisual("decal");
    if (decal) {
      var style2 = data.getVisual("style");
      style2.decal = createOrUpdatePatternFromDecal(decal, api);
    }
  });
}

// node_modules/echarts/lib/core/echarts.js
var TEST_FRAME_REMAIN_TIME = 1;
var PRIORITY_PROCESSOR_SERIES_FILTER = 800;
var PRIORITY_PROCESSOR_DATASTACK = 900;
var PRIORITY_PROCESSOR_AXIS_STATISTICS = 920;
var PRIORITY_PROCESSOR_FILTER = 1e3;
var PRIORITY_PROCESSOR_DEFAULT = 2e3;
var PRIORITY_PROCESSOR_STATISTICS = 5e3;
var PRIORITY_VISUAL_LAYOUT = 1e3;
var PRIORITY_VISUAL_PROGRESSIVE_LAYOUT = 1100;
var PRIORITY_VISUAL_GLOBAL = 2e3;
var PRIORITY_VISUAL_CHART = 3e3;
var PRIORITY_VISUAL_COMPONENT = 4e3;
var PRIORITY_VISUAL_CHART_DATA_CUSTOM = 4500;
var PRIORITY_VISUAL_POST_CHART_LAYOUT = 4600;
var PRIORITY_VISUAL_BRUSH = 5e3;
var PRIORITY_VISUAL_ARIA = 6e3;
var PRIORITY_VISUAL_DECAL = 7e3;
var PRIORITY = {
  PROCESSOR: {
    SERIES_FILTER: PRIORITY_PROCESSOR_SERIES_FILTER,
    AXIS_STATISTICS: PRIORITY_PROCESSOR_AXIS_STATISTICS,
    FILTER: PRIORITY_PROCESSOR_FILTER,
    STATISTIC: PRIORITY_PROCESSOR_STATISTICS,
    STATISTICS: PRIORITY_PROCESSOR_STATISTICS
  },
  VISUAL: {
    LAYOUT: PRIORITY_VISUAL_LAYOUT,
    PROGRESSIVE_LAYOUT: PRIORITY_VISUAL_PROGRESSIVE_LAYOUT,
    GLOBAL: PRIORITY_VISUAL_GLOBAL,
    CHART: PRIORITY_VISUAL_CHART,
    POST_CHART_LAYOUT: PRIORITY_VISUAL_POST_CHART_LAYOUT,
    COMPONENT: PRIORITY_VISUAL_COMPONENT,
    BRUSH: PRIORITY_VISUAL_BRUSH,
    CHART_ITEM: PRIORITY_VISUAL_CHART_DATA_CUSTOM,
    ARIA: PRIORITY_VISUAL_ARIA,
    DECAL: PRIORITY_VISUAL_DECAL
  }
};
var IN_EC_CYCLE_KEY = "__flagInMainProcess";
var EC_UPDATE_CYCLE_VERSION_KEY = "__mainProcessVersion";
var PENDING_UPDATE = "__pendingUpdate";
var STATUS_NEEDS_UPDATE_KEY = "__needsUpdateStatus";
var ACTION_REG = /^[a-zA-Z0-9_]+$/;
var CONNECT_STATUS_KEY = "__connectUpdateStatus";
var CONNECT_STATUS_PENDING = 0;
var CONNECT_STATUS_UPDATING = 1;
var CONNECT_STATUS_UPDATED = 2;
function createRegisterEventWithLowercaseECharts(method) {
  return function() {
    var args = [];
    for (var _i = 0; _i < arguments.length; _i++) {
      args[_i] = arguments[_i];
    }
    if (this.isDisposed()) {
      disposedWarning(this.id);
      return;
    }
    return toLowercaseNameAndCallEventful(this, method, args);
  };
}
function createRegisterEventWithLowercaseMessageCenter(method) {
  return function() {
    var args = [];
    for (var _i = 0; _i < arguments.length; _i++) {
      args[_i] = arguments[_i];
    }
    return toLowercaseNameAndCallEventful(this, method, args);
  };
}
function toLowercaseNameAndCallEventful(host, method, args) {
  args[0] = args[0] && args[0].toLowerCase();
  return Eventful_default.prototype[method].apply(host, args);
}
var MessageCenter = (
  /** @class */
  (function(_super) {
    __extends(MessageCenter2, _super);
    function MessageCenter2() {
      return _super !== null && _super.apply(this, arguments) || this;
    }
    return MessageCenter2;
  })(Eventful_default)
);
var messageCenterProto = MessageCenter.prototype;
messageCenterProto.on = createRegisterEventWithLowercaseMessageCenter("on");
messageCenterProto.off = createRegisterEventWithLowercaseMessageCenter("off");
var prepare;
var prepareView;
var updateDirectly;
var updateMethods;
var doConvertPixel;
var updateStreamModes;
var doDispatchAction;
var flushPendingActions;
var triggerUpdatedEvent;
var bindRenderedEvent;
var bindMouseEvent;
var render;
var renderComponents;
var renderSeries;
var createExtensionAPI;
var enableConnect;
var markStatusToUpdate;
var applyChangedStates;
var updateECUpdateCycleVersion;
var ECharts = (
  /** @class */
  (function(_super) {
    __extends(ECharts2, _super);
    function ECharts2(dom, theme2, opts) {
      var _this = _super.call(this, new ECEventProcessor()) || this;
      _this._chartsViews = [];
      _this._chartsMap = {};
      _this._componentsViews = [];
      _this._componentsMap = {};
      _this._pendingActions = [];
      opts = opts || {};
      _this.__v_skip = true;
      _this._dom = dom;
      var defaultRenderer = "canvas";
      var defaultCoarsePointer = "auto";
      var defaultUseDirtyRect = false;
      _this[EC_UPDATE_CYCLE_VERSION_KEY] = 1;
      if (true) {
        var root = (
          /* eslint-disable-next-line */
          env_default.hasGlobalWindow ? window : global
        );
        if (root) {
          defaultRenderer = retrieve2(root.__ECHARTS__DEFAULT__RENDERER__, defaultRenderer);
          defaultCoarsePointer = retrieve2(root.__ECHARTS__DEFAULT__COARSE_POINTER, defaultCoarsePointer);
          defaultUseDirtyRect = retrieve2(root.__ECHARTS__DEFAULT__USE_DIRTY_RECT__, defaultUseDirtyRect);
        }
      }
      if (opts.ssr) {
        registerSSRDataGetter(function(el) {
          var ecData = getECData(el);
          var dataIndex = ecData.dataIndex;
          if (dataIndex == null) {
            return;
          }
          var hashMap = createHashMap();
          hashMap.set("series_index", ecData.seriesIndex);
          hashMap.set("data_index", dataIndex);
          ecData.ssrType && hashMap.set("ssr_type", ecData.ssrType);
          return hashMap;
        });
      }
      var zr = _this._zr = init(dom, {
        renderer: opts.renderer || defaultRenderer,
        devicePixelRatio: opts.devicePixelRatio,
        width: opts.width,
        height: opts.height,
        ssr: opts.ssr,
        useDirtyRect: retrieve2(opts.useDirtyRect, defaultUseDirtyRect),
        useCoarsePointer: retrieve2(opts.useCoarsePointer, defaultCoarsePointer),
        pointerSize: opts.pointerSize
      });
      _this._ssr = opts.ssr;
      _this._throttledZrFlush = throttle(bind(zr.flush, zr), 17);
      _this._updateTheme(theme2);
      _this._locale = createLocaleObject(opts.locale || SYSTEM_LANG);
      _this._coordSysMgr = new CoordinateSystem_default();
      var api = _this._api = createExtensionAPI(_this);
      function prioritySortFunc(a, b) {
        return a.__prio - b.__prio;
      }
      sort(visualFuncs, prioritySortFunc);
      sort(dataProcessorFuncs, prioritySortFunc);
      _this._scheduler = new Scheduler_default(_this, api, dataProcessorFuncs, visualFuncs);
      _this._messageCenter = new MessageCenter();
      _this._initEvents();
      _this.resize = bind(_this.resize, _this);
      zr.animation.on("frame", _this._onframe, _this);
      bindRenderedEvent(zr, _this);
      bindMouseEvent(zr, _this);
      setAsPrimitive(_this);
      return _this;
    }
    ECharts2.prototype._onframe = function() {
      if (this._disposed) {
        return;
      }
      var scheduler = this._scheduler;
      var ecModel = this._model;
      var api = this._api;
      applyChangedStates(this);
      if (this[PENDING_UPDATE]) {
        var silent = this[PENDING_UPDATE].silent;
        this[IN_EC_CYCLE_KEY] = true;
        updateECUpdateCycleVersion(this);
        try {
          prepare(this);
          updateMethods.update.call(this, null, this[PENDING_UPDATE].updateParams);
        } catch (e2) {
          this[IN_EC_CYCLE_KEY] = false;
          this[PENDING_UPDATE] = null;
          throw e2;
        }
        this._zr.flush();
        this[IN_EC_CYCLE_KEY] = false;
        this[PENDING_UPDATE] = null;
        flushPendingActions.call(this, silent);
        triggerUpdatedEvent.call(this, silent);
      } else if (scheduler.unfinished) {
        var remainTime = TEST_FRAME_REMAIN_TIME;
        do {
          scheduler.unfinished = false;
          var startTime = platformApi.getTime();
          scheduler.performSeriesTasks(ecModel);
          scheduler.performDataProcessorTasks(ecModel);
          updateStreamModes(this, ecModel);
          scheduler.performVisualTasks(ecModel);
          renderSeries(this, this._model, api, "remain", {});
          remainTime -= platformApi.getTime() - startTime;
        } while (remainTime > 0 && scheduler.unfinished);
        if (!scheduler.unfinished) {
          this._zr.flush();
        }
      }
    };
    ECharts2.prototype.getDom = function() {
      return this._dom;
    };
    ECharts2.prototype.getId = function() {
      return this.id;
    };
    ECharts2.prototype.getZr = function() {
      return this._zr;
    };
    ECharts2.prototype.isSSR = function() {
      return this._ssr;
    };
    ECharts2.prototype.setOption = function(option, notMerge, lazyUpdate) {
      if (this[IN_EC_CYCLE_KEY]) {
        if (true) {
          error("`setOption` should not be called during main process.");
        }
        return;
      }
      if (this._disposed) {
        disposedWarning(this.id);
        return;
      }
      var silent;
      var replaceMerge;
      var transitionOpt;
      if (isObject(notMerge)) {
        lazyUpdate = notMerge.lazyUpdate;
        silent = notMerge.silent;
        replaceMerge = notMerge.replaceMerge;
        transitionOpt = notMerge.transition;
        notMerge = notMerge.notMerge;
      }
      this[IN_EC_CYCLE_KEY] = true;
      updateECUpdateCycleVersion(this);
      if (!this._model || notMerge) {
        var optionManager = new OptionManager_default(this._api);
        var theme2 = this._theme;
        var ecModel = this._model = new Global_default();
        ecModel.scheduler = this._scheduler;
        ecModel.ssr = this._ssr;
        ecModel.init(null, null, null, theme2, this._locale, optionManager);
      }
      this._model.setOption(option, {
        replaceMerge
      }, optionPreprocessorFuncs);
      var updateParams = {
        seriesTransition: transitionOpt,
        optionChanged: true
      };
      if (lazyUpdate) {
        this[PENDING_UPDATE] = {
          silent,
          updateParams
        };
        this[IN_EC_CYCLE_KEY] = false;
        this.getZr().wakeUp();
      } else {
        try {
          prepare(this);
          updateMethods.update.call(this, null, updateParams);
        } catch (e2) {
          this[PENDING_UPDATE] = null;
          this[IN_EC_CYCLE_KEY] = false;
          throw e2;
        }
        if (!this._ssr) {
          this._zr.flush();
        }
        this[PENDING_UPDATE] = null;
        this[IN_EC_CYCLE_KEY] = false;
        flushPendingActions.call(this, silent);
        triggerUpdatedEvent.call(this, silent);
      }
    };
    ECharts2.prototype.setTheme = function(theme2, opts) {
      if (this[IN_EC_CYCLE_KEY]) {
        if (true) {
          error("`setTheme` should not be called during main process.");
        }
        return;
      }
      if (this._disposed) {
        disposedWarning(this.id);
        return;
      }
      var ecModel = this._model;
      if (!ecModel) {
        return;
      }
      var silent = opts && opts.silent;
      var updateParams = null;
      if (this[PENDING_UPDATE]) {
        if (silent == null) {
          silent = this[PENDING_UPDATE].silent;
        }
        updateParams = this[PENDING_UPDATE].updateParams;
        this[PENDING_UPDATE] = null;
      }
      this[IN_EC_CYCLE_KEY] = true;
      updateECUpdateCycleVersion(this);
      try {
        this._updateTheme(theme2);
        ecModel.setTheme(this._theme);
        prepare(this);
        updateMethods.update.call(this, {
          type: "setTheme"
        }, updateParams);
      } catch (e2) {
        this[IN_EC_CYCLE_KEY] = false;
        throw e2;
      }
      this[IN_EC_CYCLE_KEY] = false;
      flushPendingActions.call(this, silent);
      triggerUpdatedEvent.call(this, silent);
    };
    ECharts2.prototype._updateTheme = function(theme2) {
      if (isString(theme2)) {
        theme2 = themeStorage[theme2];
      }
      if (theme2) {
        theme2 = clone(theme2);
        theme2 && globalBackwardCompat(theme2, true);
        this._theme = theme2;
      }
    };
    ECharts2.prototype.getModel = function() {
      return this._model;
    };
    ECharts2.prototype.getOption = function() {
      return this._model && this._model.getOption();
    };
    ECharts2.prototype.getWidth = function() {
      return this._zr.getWidth();
    };
    ECharts2.prototype.getHeight = function() {
      return this._zr.getHeight();
    };
    ECharts2.prototype.getDevicePixelRatio = function() {
      return this._zr.painter.dpr || env_default.hasGlobalWindow && window.devicePixelRatio || 1;
    };
    ECharts2.prototype.getRenderedCanvas = function(opts) {
      if (true) {
        deprecateReplaceLog("getRenderedCanvas", "renderToCanvas");
      }
      return this.renderToCanvas(opts);
    };
    ECharts2.prototype.renderToCanvas = function(opts) {
      opts = opts || {};
      var painter = this._zr.painter;
      if (true) {
        if (painter.type !== "canvas") {
          throw new Error("renderToCanvas can only be used in the canvas renderer.");
        }
      }
      return painter.getRenderedCanvas({
        backgroundColor: opts.backgroundColor || this._model.get("backgroundColor"),
        pixelRatio: opts.pixelRatio || this.getDevicePixelRatio()
      });
    };
    ECharts2.prototype.renderToSVGString = function(opts) {
      opts = opts || {};
      var painter = this._zr.painter;
      if (true) {
        if (painter.type !== "svg") {
          throw new Error("renderToSVGString can only be used in the svg renderer.");
        }
      }
      return painter.renderToString({
        useViewBox: opts.useViewBox
      });
    };
    ECharts2.prototype.getSvgDataURL = function() {
      var zr = this._zr;
      var list = zr.storage.getDisplayList();
      each(list, function(el) {
        el.stopAnimation(null, true);
      });
      return zr.painter.toDataURL();
    };
    ECharts2.prototype.getDataURL = function(opts) {
      if (this._disposed) {
        disposedWarning(this.id);
        return;
      }
      opts = opts || {};
      var excludeComponents = opts.excludeComponents;
      var ecModel = this._model;
      var excludesComponentViews = [];
      var self2 = this;
      each(excludeComponents, function(componentType) {
        ecModel.eachComponent({
          mainType: componentType
        }, function(component) {
          var view = self2._componentsMap[component.__viewId];
          if (!view.group.ignore) {
            excludesComponentViews.push(view);
            view.group.ignore = true;
          }
        });
      });
      var url = this._zr.painter.getType() === "svg" ? this.getSvgDataURL() : this.renderToCanvas(opts).toDataURL("image/" + (opts && opts.type || "png"));
      each(excludesComponentViews, function(view) {
        view.group.ignore = false;
      });
      return url;
    };
    ECharts2.prototype.getConnectedDataURL = function(opts) {
      if (this._disposed) {
        disposedWarning(this.id);
        return;
      }
      var isSvg = opts.type === "svg";
      var groupId = this.group;
      var mathMin11 = Math.min;
      var mathMax11 = Math.max;
      var MAX_NUMBER = Infinity;
      if (connectedGroups[groupId]) {
        var left_1 = MAX_NUMBER;
        var top_1 = MAX_NUMBER;
        var right_1 = -MAX_NUMBER;
        var bottom_1 = -MAX_NUMBER;
        var canvasList_1 = [];
        var dpr_1 = opts && opts.pixelRatio || this.getDevicePixelRatio();
        each(instances2, function(chart, id) {
          if (chart.group === groupId) {
            var canvas = isSvg ? chart.getZr().painter.getSvgDom().innerHTML : chart.renderToCanvas(clone(opts));
            var boundingRect = chart.getDom().getBoundingClientRect();
            left_1 = mathMin11(boundingRect.left, left_1);
            top_1 = mathMin11(boundingRect.top, top_1);
            right_1 = mathMax11(boundingRect.right, right_1);
            bottom_1 = mathMax11(boundingRect.bottom, bottom_1);
            canvasList_1.push({
              dom: canvas,
              left: boundingRect.left,
              top: boundingRect.top
            });
          }
        });
        left_1 *= dpr_1;
        top_1 *= dpr_1;
        right_1 *= dpr_1;
        bottom_1 *= dpr_1;
        var width = right_1 - left_1;
        var height = bottom_1 - top_1;
        var targetCanvas = platformApi.createCanvas();
        var zr_1 = init(targetCanvas, {
          renderer: isSvg ? "svg" : "canvas"
        });
        zr_1.resize({
          width,
          height
        });
        if (isSvg) {
          var content_1 = "";
          each(canvasList_1, function(item) {
            var x = item.left - left_1;
            var y = item.top - top_1;
            content_1 += '<g transform="translate(' + x + "," + y + ')">' + item.dom + "</g>";
          });
          zr_1.painter.getSvgRoot().innerHTML = content_1;
          if (opts.connectedBackgroundColor) {
            zr_1.painter.setBackgroundColor(opts.connectedBackgroundColor);
          }
          zr_1.refreshImmediately();
          return zr_1.painter.toDataURL();
        } else {
          if (opts.connectedBackgroundColor) {
            zr_1.add(new Rect_default({
              shape: {
                x: 0,
                y: 0,
                width,
                height
              },
              style: {
                fill: opts.connectedBackgroundColor
              }
            }));
          }
          each(canvasList_1, function(item) {
            var img = new Image_default({
              style: {
                x: item.left * dpr_1 - left_1,
                y: item.top * dpr_1 - top_1,
                image: item.dom
              }
            });
            zr_1.add(img);
          });
          zr_1.refreshImmediately();
          return targetCanvas.toDataURL("image/" + (opts && opts.type || "png"));
        }
      } else {
        return this.getDataURL(opts);
      }
    };
    ECharts2.prototype.convertToPixel = function(finder, value, opt) {
      return doConvertPixel(this, "convertToPixel", finder, value, opt);
    };
    ECharts2.prototype.convertToLayout = function(finder, value, opt) {
      return doConvertPixel(this, "convertToLayout", finder, value, opt);
    };
    ECharts2.prototype.convertFromPixel = function(finder, value, opt) {
      return doConvertPixel(this, "convertFromPixel", finder, value, opt);
    };
    ECharts2.prototype.containPixel = function(finder, value) {
      if (this._disposed) {
        disposedWarning(this.id);
        return;
      }
      var ecModel = this._model;
      var result;
      var findResult = parseFinder(ecModel, finder);
      each(findResult, function(models, key2) {
        key2.indexOf("Models") >= 0 && each(models, function(model2) {
          var coordSys = model2.coordinateSystem;
          if (coordSys && coordSys.containPoint) {
            result = result || !!coordSys.containPoint(value);
          } else if (key2 === "seriesModels") {
            var view = this._chartsMap[model2.__viewId];
            if (view && view.containPoint) {
              result = result || view.containPoint(value, model2);
            } else {
              if (true) {
                warn(key2 + ": " + (view ? "The found component do not support containPoint." : "No view mapping to the found component."));
              }
            }
          } else {
            if (true) {
              warn(key2 + ": containPoint is not supported");
            }
          }
        }, this);
      }, this);
      return !!result;
    };
    ECharts2.prototype.getVisual = function(finder, visualType) {
      var ecModel = this._model;
      var parsedFinder = parseFinder(ecModel, finder, {
        defaultMainType: "series"
      });
      var seriesModel = parsedFinder.seriesModel;
      if (true) {
        if (!seriesModel) {
          warn("There is no specified series model");
        }
      }
      var data = seriesModel.getData();
      var dataIndexInside = parsedFinder.hasOwnProperty("dataIndexInside") ? parsedFinder.dataIndexInside : parsedFinder.hasOwnProperty("dataIndex") ? data.indexOfRawIndex(parsedFinder.dataIndex) : null;
      return dataIndexInside != null ? getItemVisualFromData(data, dataIndexInside, visualType) : getVisualFromData(data, visualType);
    };
    ECharts2.prototype.getViewOfComponentModel = function(componentModel) {
      return this._componentsMap[componentModel.__viewId];
    };
    ECharts2.prototype.getViewOfSeriesModel = function(seriesModel) {
      return this._chartsMap[seriesModel.__viewId];
    };
    ECharts2.prototype._initEvents = function() {
      var _this = this;
      each(MOUSE_EVENT_NAMES, function(eveName) {
        var handler = function(e2) {
          var ecModel = _this.getModel();
          var el = e2.target;
          var params;
          var isGlobalOut = eveName === "globalout";
          if (isGlobalOut) {
            params = {};
          } else {
            el && findEventDispatcher(el, function(parent) {
              var ecData = getECData(parent);
              if (ecData && ecData.dataIndex != null) {
                var dataModel = ecData.dataModel || ecModel.getSeriesByIndex(ecData.seriesIndex);
                params = dataModel && dataModel.getDataParams(ecData.dataIndex, ecData.dataType, el) || {};
                return true;
              } else if (ecData.eventData) {
                params = extend({}, ecData.eventData);
                return true;
              }
            }, true);
          }
          if (params) {
            var componentType = params.componentType;
            var componentIndex = params.componentIndex;
            if (componentType === "markLine" || componentType === "markPoint" || componentType === "markArea") {
              componentType = "series";
              componentIndex = params.seriesIndex;
            }
            var model2 = componentType && componentIndex != null && ecModel.getComponent(componentType, componentIndex);
            var view = model2 && _this[model2.mainType === "series" ? "_chartsMap" : "_componentsMap"][model2.__viewId];
            if (true) {
              if (!isGlobalOut && !(model2 && view)) {
                warn("model or view can not be found by params");
              }
            }
            params.event = e2;
            params.type = eveName;
            _this._$eventProcessor.eventInfo = {
              targetEl: el,
              packedEvent: params,
              model: model2,
              view
            };
            _this.trigger(eveName, params);
          }
        };
        handler.zrEventfulCallAtLast = true;
        _this._zr.on(eveName, handler, _this);
      });
      var messageCenter = this._messageCenter;
      each(publicEventTypeMap, function(_, eventType) {
        messageCenter.on(eventType, function(event) {
          _this.trigger(eventType, event);
        });
      });
      handleLegacySelectEvents(messageCenter, this, this._api);
    };
    ECharts2.prototype.isDisposed = function() {
      return this._disposed;
    };
    ECharts2.prototype.clear = function() {
      if (this._disposed) {
        disposedWarning(this.id);
        return;
      }
      this.setOption({
        series: []
      }, true);
    };
    ECharts2.prototype.dispose = function() {
      if (this._disposed) {
        disposedWarning(this.id);
        return;
      }
      this._disposed = true;
      var dom = this.getDom();
      if (dom) {
        setAttribute(this.getDom(), DOM_ATTRIBUTE_KEY, "");
      }
      var chart = this;
      var api = chart._api;
      var ecModel = chart._model;
      each(chart._componentsViews, function(component) {
        component.dispose(ecModel, api);
      });
      each(chart._chartsViews, function(chart2) {
        chart2.dispose(ecModel, api);
      });
      chart._zr.dispose();
      chart._dom = chart._model = chart._chartsMap = chart._componentsMap = chart._chartsViews = chart._componentsViews = chart._scheduler = chart._api = chart._zr = chart._throttledZrFlush = chart._theme = chart._coordSysMgr = chart._messageCenter = null;
      delete instances2[chart.id];
    };
    ECharts2.prototype.resize = function(opts) {
      if (this[IN_EC_CYCLE_KEY]) {
        if (true) {
          error("`resize` should not be called during main process.");
        }
        return;
      }
      if (this._disposed) {
        disposedWarning(this.id);
        return;
      }
      this._zr.resize(opts);
      var ecModel = this._model;
      this._loadingFX && this._loadingFX.resize();
      if (!ecModel) {
        return;
      }
      var needPrepare = ecModel.resetOption("media");
      var silent = opts && opts.silent;
      if (this[PENDING_UPDATE]) {
        if (silent == null) {
          silent = this[PENDING_UPDATE].silent;
        }
        needPrepare = true;
        this[PENDING_UPDATE] = null;
      }
      this[IN_EC_CYCLE_KEY] = true;
      updateECUpdateCycleVersion(this);
      try {
        needPrepare && prepare(this);
        updateMethods.update.call(this, {
          type: "resize",
          animation: extend({
            // Disable animation
            duration: 0
          }, opts && opts.animation)
        });
      } catch (e2) {
        this[IN_EC_CYCLE_KEY] = false;
        throw e2;
      }
      this[IN_EC_CYCLE_KEY] = false;
      flushPendingActions.call(this, silent);
      triggerUpdatedEvent.call(this, silent);
    };
    ECharts2.prototype.showLoading = function(name, cfg) {
      if (this._disposed) {
        disposedWarning(this.id);
        return;
      }
      if (isObject(name)) {
        cfg = name;
        name = "";
      }
      name = name || "default";
      this.hideLoading();
      if (!loadingEffects[name]) {
        if (true) {
          warn("Loading effects " + name + " not exists.");
        }
        return;
      }
      var el = loadingEffects[name](this._api, cfg);
      var zr = this._zr;
      this._loadingFX = el;
      zr.add(el);
    };
    ECharts2.prototype.hideLoading = function() {
      if (this._disposed) {
        disposedWarning(this.id);
        return;
      }
      this._loadingFX && this._zr.remove(this._loadingFX);
      this._loadingFX = null;
    };
    ECharts2.prototype.makeActionFromEvent = function(eventObj) {
      var payload = extend({}, eventObj);
      payload.type = connectionEventRevertMap[eventObj.type];
      return payload;
    };
    ECharts2.prototype.dispatchAction = function(payload, opt) {
      if (this._disposed) {
        disposedWarning(this.id);
        return;
      }
      if (!isObject(opt)) {
        opt = {
          silent: !!opt
        };
      }
      if (!actions[payload.type]) {
        return;
      }
      if (!this._model) {
        return;
      }
      if (this[IN_EC_CYCLE_KEY]) {
        this._pendingActions.push(payload);
        return;
      }
      var silent = opt.silent;
      doDispatchAction.call(this, payload, silent);
      var flush = opt.flush;
      if (flush) {
        this._zr.flush();
      } else if (flush !== false && env_default.browser.weChat) {
        this._throttledZrFlush();
      }
      flushPendingActions.call(this, silent);
      triggerUpdatedEvent.call(this, silent);
    };
    ECharts2.prototype.updateLabelLayout = function() {
      lifecycle_default.trigger("series:layoutlabels", this._model, this._api, {
        // Not adding series labels.
        // TODO
        updatedSeries: []
      });
    };
    ECharts2.prototype.appendData = function(params) {
      if (this._disposed) {
        disposedWarning(this.id);
        return;
      }
      var seriesIndex = params.seriesIndex;
      var ecModel = this.getModel();
      var seriesModel = ecModel.getSeriesByIndex(seriesIndex);
      if (true) {
        assert(params.data && seriesModel);
      }
      seriesModel.appendData(params);
      this._scheduler.unfinished = true;
      this.getZr().wakeUp();
    };
    ECharts2.internalField = (function() {
      prepare = function(ecIns) {
        resetCachePerECPrepare(ecIns._model);
        var scheduler = ecIns._scheduler;
        scheduler.restorePipelines(ecIns._zr, ecIns._model);
        scheduler.prepareStageTasks();
        prepareView(ecIns, true);
        prepareView(ecIns, false);
        scheduler.plan();
      };
      prepareView = function(ecIns, isComponent) {
        var ecModel = ecIns._model;
        var scheduler = ecIns._scheduler;
        var viewList = isComponent ? ecIns._componentsViews : ecIns._chartsViews;
        var viewMap = isComponent ? ecIns._componentsMap : ecIns._chartsMap;
        var zr = ecIns._zr;
        var api = ecIns._api;
        for (var i2 = 0; i2 < viewList.length; i2++) {
          viewList[i2].__alive = false;
        }
        isComponent ? ecModel.eachComponent(function(componentType, model2) {
          componentType !== "series" && doPrepare(model2);
        }) : ecModel.eachSeries(doPrepare);
        function doPrepare(model2) {
          var requireNewView = model2.__requireNewView;
          model2.__requireNewView = false;
          var viewId = "_ec_" + model2.id + "_" + model2.type;
          var view2 = !requireNewView && viewMap[viewId];
          if (!view2) {
            var classType = parseClassType(model2.type);
            var Clazz = isComponent ? Component_default2.getClass(classType.main, classType.sub) : (
              // FIXME:TS
              // (ChartView as ChartViewConstructor).getClass('series', classType.sub)
              // For backward compat, still support a chart type declared as only subType
              // like "liquidfill", but recommend "series.liquidfill"
              // But need a base class to make a type series.
              Chart_default.getClass(classType.sub)
            );
            if (true) {
              assert(Clazz, classType.sub + " does not exist.");
            }
            view2 = new Clazz();
            view2.init(ecModel, api);
            viewMap[viewId] = view2;
            viewList.push(view2);
            zr.add(view2.group);
          }
          model2.__viewId = view2.__id = viewId;
          view2.__alive = true;
          view2.__model = model2;
          view2.group.__ecComponentInfo = {
            mainType: model2.mainType,
            index: model2.componentIndex
          };
          !isComponent && scheduler.prepareView(view2, model2, ecModel, api);
        }
        for (var i2 = 0; i2 < viewList.length; ) {
          var view = viewList[i2];
          if (!view.__alive) {
            !isComponent && view.renderTask.dispose();
            zr.remove(view.group);
            view.dispose(ecModel, api);
            viewList.splice(i2, 1);
            if (viewMap[view.__id] === view) {
              delete viewMap[view.__id];
            }
            view.__id = view.group.__ecComponentInfo = null;
          } else {
            i2++;
          }
        }
      };
      updateDirectly = function(ecIns, method, payload, mainType, subType) {
        var ecModel = ecIns._model;
        ecModel.setUpdatePayload(payload);
        if (!mainType) {
          each([].concat(ecIns._componentsViews).concat(ecIns._chartsViews), callView);
          return;
        }
        var condition = makeQueryConditionKindA(payload, mainType, subType);
        var excludeSeriesId = payload.excludeSeriesId;
        var excludeSeriesIdMap;
        if (excludeSeriesId != null) {
          excludeSeriesIdMap = createHashMap();
          each(normalizeToArray(excludeSeriesId), function(id) {
            var modelId = convertOptionIdName(id, null);
            if (modelId != null) {
              excludeSeriesIdMap.set(modelId, true);
            }
          });
        }
        ecModel && ecModel.eachComponent(condition, function(model2) {
          var isExcluded = excludeSeriesIdMap && excludeSeriesIdMap.get(model2.id) != null;
          if (isExcluded) {
            return;
          }
          ;
          if (isHighDownPayload(payload)) {
            if (model2 instanceof Series_default) {
              if (payload.type === HIGHLIGHT_ACTION_TYPE && !payload.notBlur && !model2.get(["emphasis", "disabled"])) {
                blurSeriesFromHighlightPayload(model2, payload, ecIns._api);
              }
            } else {
              var _a2 = findComponentHighDownDispatchers(model2.mainType, model2.componentIndex, payload.name, ecIns._api), focusSelf = _a2.focusSelf, dispatchers = _a2.dispatchers;
              if (payload.type === HIGHLIGHT_ACTION_TYPE && focusSelf && !payload.notBlur) {
                blurComponent(model2.mainType, model2.componentIndex, ecIns._api);
              }
              if (dispatchers) {
                each(dispatchers, function(dispatcher) {
                  payload.type === HIGHLIGHT_ACTION_TYPE ? enterEmphasis(dispatcher) : leaveEmphasis(dispatcher);
                });
              }
            }
          } else if (isSelectChangePayload(payload)) {
            if (model2 instanceof Series_default) {
              toggleSelectionFromPayload(model2, payload, ecIns._api);
              updateSeriesElementSelection(model2);
              markStatusToUpdate(ecIns);
            }
          }
        }, ecIns);
        ecModel && ecModel.eachComponent(condition, function(model2) {
          var isExcluded = excludeSeriesIdMap && excludeSeriesIdMap.get(model2.id) != null;
          if (isExcluded) {
            return;
          }
          ;
          callView(ecIns[mainType === "series" ? "_chartsMap" : "_componentsMap"][model2.__viewId]);
        }, ecIns);
        function callView(view) {
          view && view.__alive && view[method] && view[method](view.__model, ecModel, ecIns._api, payload);
        }
      };
      updateMethods = {
        prepareAndUpdate: function(payload) {
          prepare(this);
          updateMethods.update.call(this, payload, payload && {
            // Needs to mark option changed if newOption is given.
            // It's from MagicType.
            // TODO If use a separate flag optionChanged in payload?
            optionChanged: payload.newOption != null
          });
        },
        update: function(payload, updateParams) {
          var ecModel = this._model;
          var api = this._api;
          var zr = this._zr;
          var coordSysMgr = this._coordSysMgr;
          var scheduler = this._scheduler;
          if (!ecModel) {
            return;
          }
          resetCachePerECFullUpdate(ecModel);
          ecModel.setUpdatePayload(payload);
          scheduler.restoreData(ecModel, payload);
          scheduler.performSeriesTasks(ecModel);
          coordSysMgr.create(ecModel, api);
          lifecycle_default.trigger("coordsys:aftercreate", ecModel, api);
          scheduler.performDataProcessorTasks(ecModel, payload);
          updateStreamModes(this, ecModel);
          coordSysMgr.update(ecModel, api);
          clearColorPalette(ecModel);
          scheduler.performVisualTasks(ecModel, payload);
          var backgroundColor2 = ecModel.get("backgroundColor") || "transparent";
          zr.setBackgroundColor(backgroundColor2);
          var darkMode = ecModel.get("darkMode");
          if (darkMode != null && darkMode !== "auto") {
            zr.setDarkMode(darkMode);
          }
          render(this, ecModel, api, payload, updateParams);
          lifecycle_default.trigger("afterupdate", ecModel, api);
        },
        /**
         * PENDING: See INCONSISTENCY_OF_BRUSH_SELECTED_EVENT_IN_UPDATE_TRANSFORM
         */
        updateTransform: function(payload) {
          var ecIns = this;
          var ecModel = ecIns._model;
          var api = ecIns._api;
          if (!ecModel) {
            return;
          }
          ecModel.setUpdatePayload(payload);
          var componentDirtyList = [];
          ecModel.eachComponent(function(mainType, componentModel) {
            if (mainType === COMPONENT_MAIN_TYPE_SERIES) {
              return;
            }
            var componentView = ecIns.getViewOfComponentModel(componentModel);
            if (componentView && componentView.__alive) {
              if (componentView.updateTransform) {
                var result = componentView.updateTransform(componentModel, ecModel, api, payload);
                result && result.update && componentDirtyList.push(componentView);
              } else {
                componentDirtyList.push(componentView);
              }
            }
          });
          var seriesDirtyMap = createHashMap();
          ecModel.eachSeries(function(seriesModel) {
            var chartView = ecIns._chartsMap[seriesModel.__viewId];
            var pipelineContext = seriesModel.pipelineContext;
            if (chartView.updateTransform && !pipelineContext.progressiveRender) {
              var result = chartView.updateTransform(seriesModel, ecModel, api, payload);
              result && result.update && seriesDirtyMap.set(seriesModel.uid, 1);
            } else {
              seriesDirtyMap.set(seriesModel.uid, 1);
            }
          });
          ecIns._scheduler.performVisualTasks(ecModel, payload, {
            setDirty: true,
            dirtyMap: seriesDirtyMap
          });
          renderSeries(ecIns, ecModel, api, payload, {}, seriesDirtyMap);
          lifecycle_default.trigger("afterupdate", ecModel, api);
        },
        updateView: function(payload) {
          var ecModel = this._model;
          if (!ecModel) {
            return;
          }
          ecModel.setUpdatePayload(payload);
          Chart_default.markUpdateMethod(payload, "updateView");
          clearColorPalette(ecModel);
          this._scheduler.performVisualTasks(ecModel, payload, {
            setDirty: true
          });
          render(this, ecModel, this._api, payload, {});
          lifecycle_default.trigger("afterupdate", ecModel, this._api);
        },
        updateVisual: function(payload) {
          var _this = this;
          var ecModel = this._model;
          if (!ecModel) {
            return;
          }
          ecModel.setUpdatePayload(payload);
          ecModel.eachSeries(function(seriesModel) {
            seriesModel.getData().clearAllVisual();
          });
          Chart_default.markUpdateMethod(payload, "updateVisual");
          clearColorPalette(ecModel);
          this._scheduler.performVisualTasks(ecModel, payload, {
            visualType: "visual",
            setDirty: true
          });
          ecModel.eachComponent(function(componentType, componentModel) {
            if (componentType !== "series") {
              var componentView = _this.getViewOfComponentModel(componentModel);
              componentView && componentView.__alive && componentView.updateVisual(componentModel, ecModel, _this._api, payload);
            }
          });
          ecModel.eachSeries(function(seriesModel) {
            var chartView = _this._chartsMap[seriesModel.__viewId];
            chartView.updateVisual(seriesModel, ecModel, _this._api, payload);
          });
          lifecycle_default.trigger("afterupdate", ecModel, this._api);
        },
        /**
         * @deprecated
         */
        updateLayout: function(payload) {
          updateMethods.update.call(this, payload);
        }
      };
      function doConvertPixelImpl(ecIns, methodName, finder, value, opt) {
        if (ecIns._disposed) {
          disposedWarning(ecIns.id);
          return;
        }
        var ecModel = ecIns._model;
        var coordSysList = ecIns._coordSysMgr.getCoordinateSystems();
        var result;
        var parsedFinder = parseFinder(ecModel, finder);
        for (var i2 = 0; i2 < coordSysList.length; i2++) {
          var coordSys = coordSysList[i2];
          if (coordSys[methodName] && (result = coordSys[methodName](ecModel, parsedFinder, value, opt)) != null) {
            return result;
          }
        }
        if (true) {
          warn("No coordinate system that supports " + methodName + " found by the given finder.");
        }
      }
      ;
      doConvertPixel = doConvertPixelImpl;
      updateStreamModes = function(ecIns, ecModel) {
        var chartsMap = ecIns._chartsMap;
        var scheduler = ecIns._scheduler;
        ecModel.eachSeries(function(seriesModel) {
          scheduler.updateStreamModes(seriesModel, chartsMap[seriesModel.__viewId]);
        });
      };
      doDispatchAction = function(payload, silent) {
        var _this = this;
        var ecModel = this.getModel();
        var payloadType = payload.type;
        var escapeConnect = payload.escapeConnect;
        var actionInfo = actions[payloadType];
        var cptTypeTmp = (actionInfo.update || "update").split(":");
        var updateMethod = cptTypeTmp.pop();
        var cptType = cptTypeTmp[0] != null && parseClassType(cptTypeTmp[0]);
        this[IN_EC_CYCLE_KEY] = true;
        updateECUpdateCycleVersion(this);
        var payloads = [payload];
        var batched = false;
        if (payload.batch) {
          batched = true;
          payloads = map2(payload.batch, function(item) {
            item = defaults(extend({}, item), payload);
            item.batch = null;
            return item;
          });
        }
        var eventObjBatch = [];
        var eventObj;
        var actionResultBatch = [];
        var nonRefinedEventType = actionInfo.nonRefinedEventType;
        var isSelectChange = isSelectChangePayload(payload);
        var isHighDown = isHighDownPayload(payload);
        if (isHighDown) {
          allLeaveBlur(this._api);
        }
        each(payloads, function(batchItem) {
          var actionResult = actionInfo.action(batchItem, ecModel, _this._api);
          if (actionInfo.refineEvent) {
            actionResultBatch.push(actionResult);
          } else {
            eventObj = actionResult;
          }
          eventObj = eventObj || extend({}, batchItem);
          eventObj.type = nonRefinedEventType;
          eventObjBatch.push(eventObj);
          if (isHighDown) {
            var _a2 = preParseFinder(payload), queryOptionMap = _a2.queryOptionMap, mainTypeSpecified = _a2.mainTypeSpecified;
            var componentMainType = mainTypeSpecified ? queryOptionMap.keys()[0] : "series";
            updateDirectly(_this, updateMethod, batchItem, componentMainType);
            markStatusToUpdate(_this);
          } else if (isSelectChange) {
            updateDirectly(_this, updateMethod, batchItem, "series");
            markStatusToUpdate(_this);
          } else if (cptType) {
            updateDirectly(_this, updateMethod, batchItem, cptType.main, cptType.sub);
          }
        });
        if (updateMethod !== "none" && !isHighDown && !isSelectChange && !cptType) {
          try {
            if (this[PENDING_UPDATE]) {
              prepare(this);
              updateMethods.update.call(this, payload);
              this[PENDING_UPDATE] = null;
            } else {
              updateMethods[updateMethod].call(this, payload);
            }
          } catch (e2) {
            this[IN_EC_CYCLE_KEY] = false;
            throw e2;
          }
        }
        if (batched) {
          eventObj = {
            type: nonRefinedEventType,
            escapeConnect,
            batch: eventObjBatch
          };
        } else {
          eventObj = eventObjBatch[0];
        }
        this[IN_EC_CYCLE_KEY] = false;
        if (!silent) {
          var refinedEvent = void 0;
          if (actionInfo.refineEvent) {
            var eventContent = actionInfo.refineEvent(actionResultBatch, payload, ecModel, this._api).eventContent;
            assert(isObject(eventContent));
            refinedEvent = defaults({
              type: actionInfo.refinedEventType
            }, eventContent);
            refinedEvent.fromAction = payload.type;
            refinedEvent.fromActionPayload = payload;
            refinedEvent.escapeConnect = true;
          }
          var messageCenter = this._messageCenter;
          messageCenter.trigger(eventObj.type, eventObj);
          if (refinedEvent) {
            messageCenter.trigger(refinedEvent.type, refinedEvent);
          }
        }
      };
      flushPendingActions = function(silent) {
        var pendingActions = this._pendingActions;
        while (pendingActions.length) {
          var payload = pendingActions.shift();
          doDispatchAction.call(this, payload, silent);
        }
      };
      triggerUpdatedEvent = function(silent) {
        !silent && this.trigger("updated");
      };
      bindRenderedEvent = function(zr, ecIns) {
        zr.on("rendered", function(params) {
          ecIns.trigger("rendered", params);
          if (
            // Although zr is dirty if initial animation is not finished
            // and this checking is called on frame, we also check
            // animation finished for robustness.
            zr.animation.isFinished() && !ecIns[PENDING_UPDATE] && !ecIns._scheduler.unfinished && !ecIns._pendingActions.length
          ) {
            ecIns.trigger("finished");
          } else {
            zr.refresh();
          }
        });
      };
      bindMouseEvent = function(zr, ecIns) {
        zr.on("mouseover", function(e2) {
          var el = e2.target;
          var dispatcher = findEventDispatcher(el, isHighDownDispatcher);
          if (dispatcher) {
            handleGlobalMouseOverForHighDown(dispatcher, e2, ecIns._api);
            markStatusToUpdate(ecIns);
          }
        }).on("mouseout", function(e2) {
          var el = e2.target;
          var dispatcher = findEventDispatcher(el, isHighDownDispatcher);
          if (dispatcher) {
            handleGlobalMouseOutForHighDown(dispatcher, e2, ecIns._api);
            markStatusToUpdate(ecIns);
          }
        }).on("click", function(e2) {
          var el = e2.target;
          var dispatcher = findEventDispatcher(el, function(target) {
            return getECData(target).dataIndex != null;
          }, true);
          if (dispatcher) {
            var actionType = dispatcher.selected ? "unselect" : "select";
            var ecData = getECData(dispatcher);
            ecIns._api.dispatchAction({
              type: actionType,
              dataType: ecData.dataType,
              dataIndexInside: ecData.dataIndex,
              seriesIndex: ecData.seriesIndex,
              isFromClick: true
            });
          }
        });
      };
      function clearColorPalette(ecModel) {
        ecModel.clearColorPalette();
        ecModel.eachSeries(function(seriesModel) {
          seriesModel.clearColorPalette();
        });
      }
      ;
      function allocateZlevels(ecModel) {
        ;
        var componentZLevels = [];
        var seriesZLevels = [];
        var hasSeparateZLevel = false;
        ecModel.eachComponent(function(componentType, componentModel) {
          var zlevel = componentModel.get("zlevel") || 0;
          var z = componentModel.get("z") || 0;
          var zlevelKey = componentModel.getZLevelKey();
          hasSeparateZLevel = hasSeparateZLevel || !!zlevelKey;
          (componentType === "series" ? seriesZLevels : componentZLevels).push({
            zlevel,
            z,
            idx: componentModel.componentIndex,
            type: componentType,
            key: zlevelKey
          });
        });
        if (hasSeparateZLevel) {
          var zLevels = componentZLevels.concat(seriesZLevels);
          var lastSeriesZLevel_1;
          var lastSeriesKey_1;
          sort(zLevels, function(a, b) {
            if (a.zlevel === b.zlevel) {
              return a.z - b.z;
            }
            return a.zlevel - b.zlevel;
          });
          each(zLevels, function(item) {
            var componentModel = ecModel.getComponent(item.type, item.idx);
            var zlevel = item.zlevel;
            var key2 = item.key;
            if (lastSeriesZLevel_1 != null) {
              zlevel = Math.max(lastSeriesZLevel_1, zlevel);
            }
            if (key2) {
              if (zlevel === lastSeriesZLevel_1 && key2 !== lastSeriesKey_1) {
                zlevel++;
              }
              lastSeriesKey_1 = key2;
            } else if (lastSeriesKey_1) {
              if (zlevel === lastSeriesZLevel_1) {
                zlevel++;
              }
              lastSeriesKey_1 = "";
            }
            lastSeriesZLevel_1 = zlevel;
            componentModel.setZLevel(zlevel);
          });
        }
      }
      render = function(ecIns, ecModel, api, payload, updateParams) {
        allocateZlevels(ecModel);
        renderComponents(ecIns, ecModel, api, payload, updateParams);
        each(ecIns._chartsViews, function(chart) {
          chart.__alive = false;
        });
        renderSeries(ecIns, ecModel, api, payload, updateParams);
        each(ecIns._chartsViews, function(chart) {
          if (!chart.__alive) {
            chart.remove(ecModel, api);
          }
        });
      };
      renderComponents = function(ecIns, ecModel, api, payload, updateParams, dirtyList) {
        each(dirtyList || ecIns._componentsViews, function(componentView) {
          var componentModel = componentView.__model;
          clearStates(componentModel, componentView);
          componentView.render(componentModel, ecModel, api, payload);
          updateZ5(componentModel, componentView);
          updateStates(componentModel, componentView);
        });
      };
      renderSeries = function(ecIns, ecModel, api, payload, updateParams, dirtyMap) {
        var scheduler = ecIns._scheduler;
        updateParams = extend(updateParams || {}, {
          updatedSeries: ecModel.getSeries()
        });
        lifecycle_default.trigger("series:beforeupdate", ecModel, api, updateParams);
        var unfinished = false;
        ecModel.eachSeries(function(seriesModel) {
          var chartView = ecIns._chartsMap[seriesModel.__viewId];
          chartView.__alive = true;
          var renderTask = chartView.renderTask;
          scheduler.updatePayload(renderTask, payload);
          clearStates(seriesModel, chartView);
          if (dirtyMap && dirtyMap.get(seriesModel.uid)) {
            renderTask.dirty();
          }
          if (renderTask.perform(scheduler.getPerformArgs(renderTask))) {
            unfinished = true;
          }
          chartView.group.silent = !!seriesModel.get("silent");
          updateBlend(seriesModel, chartView);
          updateSeriesElementSelection(seriesModel);
        });
        scheduler.unfinished = unfinished || scheduler.unfinished;
        lifecycle_default.trigger("series:layoutlabels", ecModel, api, updateParams);
        lifecycle_default.trigger("series:transition", ecModel, api, updateParams);
        ecModel.eachSeries(function(seriesModel) {
          var chartView = ecIns._chartsMap[seriesModel.__viewId];
          updateZ5(seriesModel, chartView);
          updateStates(seriesModel, chartView);
        });
        updateHoverLayerStatus(ecIns, ecModel);
        lifecycle_default.trigger("series:afterupdate", ecModel, api, updateParams);
      };
      markStatusToUpdate = function(ecIns) {
        ecIns[STATUS_NEEDS_UPDATE_KEY] = true;
        ecIns.getZr().wakeUp();
      };
      updateECUpdateCycleVersion = function(ecIns) {
        ecIns[EC_UPDATE_CYCLE_VERSION_KEY] = (ecIns[EC_UPDATE_CYCLE_VERSION_KEY] + 1) % 1e6;
      };
      applyChangedStates = function(ecIns) {
        if (!ecIns[STATUS_NEEDS_UPDATE_KEY]) {
          return;
        }
        ecIns.getZr().storage.traverse(function(el) {
          if (isElementRemoved(el)) {
            return;
          }
          applyElementStates(el);
        });
        ecIns[STATUS_NEEDS_UPDATE_KEY] = false;
      };
      function applyElementStates(el) {
        var newStates = [];
        var oldStates = el.currentStates;
        for (var i2 = 0; i2 < oldStates.length; i2++) {
          var stateName = oldStates[i2];
          if (!(stateName === "emphasis" || stateName === "blur" || stateName === "select")) {
            newStates.push(stateName);
          }
        }
        if (el.selected && el.states.select) {
          newStates.push("select");
        }
        if (el.hoverState === HOVER_STATE_EMPHASIS && el.states.emphasis) {
          newStates.push("emphasis");
        } else if (el.hoverState === HOVER_STATE_BLUR && el.states.blur) {
          newStates.push("blur");
        }
        el.useStates(newStates);
      }
      function updateHoverLayerStatus(ecIns, ecModel) {
        var zr = ecIns._zr;
        if (zr.painter.type !== "canvas") {
          return;
        }
        var storage2 = zr.storage;
        var elCount = 0;
        storage2.traverse(function(el) {
          if (!el.isGroup) {
            elCount++;
          }
        });
        var shouldUseHoverLayer2 = elCount > retrieve2(ecModel.get("hoverLayerThreshold"), globalDefault_default.hoverLayerThreshold) && !env_default.node && !env_default.worker;
        if (ecIns._usingTHL || shouldUseHoverLayer2) {
          ecModel.eachSeries(function(seriesModel) {
            if (seriesModel.preventUsingHoverLayer) {
              return;
            }
            var chartView = ecIns._chartsMap[seriesModel.__viewId];
            if (chartView.__alive) {
              chartView.eachRendered(function(el) {
                var emphasis = el.states.emphasis;
                if (emphasis && emphasis.hoverLayer !== HOVER_LAYER_FOR_INCREMENTAL) {
                  emphasis.hoverLayer = shouldUseHoverLayer2 ? HOVER_LAYER_FROM_THRESHOLD : HOVER_LAYER_NO;
                }
              });
            }
          });
          ecIns._usingTHL = shouldUseHoverLayer2;
        }
      }
      ;
      function updateBlend(seriesModel, chartView) {
        var blendMode = seriesModel.get("blendMode") || null;
        chartView.eachRendered(function(el) {
          if (!el.isGroup) {
            el.style.blend = blendMode;
          }
        });
      }
      ;
      function updateZ5(model2, view) {
        if (model2.preventAutoZ) {
          return;
        }
        var zInfo = retrieveZInfo(model2);
        view.eachRendered(function(el) {
          traverseUpdateZ(el, zInfo.z, zInfo.zlevel);
          return true;
        });
      }
      ;
      function clearStates(model2, view) {
        view.eachRendered(function(el) {
          if (isElementRemoved(el)) {
            return;
          }
          var textContent = el.getTextContent();
          var textGuide = el.getTextGuideLine();
          if (el.stateTransition) {
            el.stateTransition = null;
          }
          if (textContent && textContent.stateTransition) {
            textContent.stateTransition = null;
          }
          if (textGuide && textGuide.stateTransition) {
            textGuide.stateTransition = null;
          }
          if (el.hasState()) {
            el.prevStates = el.currentStates;
            el.clearStates();
          } else if (el.prevStates) {
            el.prevStates = null;
          }
        });
      }
      function updateStates(model2, view) {
        var stateAnimationModel = model2.getModel("stateAnimation");
        var enableAnimation = model2.isAnimationEnabled();
        var duration = stateAnimationModel.get("duration");
        var stateTransition = duration > 0 ? {
          duration,
          delay: stateAnimationModel.get("delay"),
          easing: stateAnimationModel.get("easing")
          // additive: stateAnimationModel.get('additive')
        } : null;
        view.eachRendered(function(el) {
          if (el.states && el.states.emphasis) {
            if (isElementRemoved(el)) {
              return;
            }
            if (el instanceof Path_default) {
              savePathStates(el);
            }
            if (el.__dirty) {
              var prevStates = el.prevStates;
              if (prevStates) {
                el.useStates(prevStates);
              }
            }
            if (enableAnimation) {
              el.stateTransition = stateTransition;
              var textContent = el.getTextContent();
              var textGuide = el.getTextGuideLine();
              if (textContent) {
                textContent.stateTransition = stateTransition;
              }
              if (textGuide) {
                textGuide.stateTransition = stateTransition;
              }
            }
            if (el.__dirty) {
              applyElementStates(el);
            }
          }
        });
      }
      ;
      createExtensionAPI = function(ecIns) {
        return new /** @class */
        ((function(_super2) {
          __extends(class_1, _super2);
          function class_1() {
            return _super2 !== null && _super2.apply(this, arguments) || this;
          }
          class_1.prototype.getCoordinateSystems = function() {
            return ecIns._coordSysMgr.getCoordinateSystems();
          };
          class_1.prototype.getComponentByElement = function(el) {
            while (el) {
              var modelInfo = el.__ecComponentInfo;
              if (modelInfo != null) {
                return ecIns._model.getComponent(modelInfo.mainType, modelInfo.index);
              }
              el = el.parent;
            }
          };
          class_1.prototype.enterEmphasis = function(el, highlightDigit) {
            enterEmphasis(el, highlightDigit);
            markStatusToUpdate(ecIns);
          };
          class_1.prototype.leaveEmphasis = function(el, highlightDigit) {
            leaveEmphasis(el, highlightDigit);
            markStatusToUpdate(ecIns);
          };
          class_1.prototype.enterBlur = function(el) {
            enterBlur(el);
            markStatusToUpdate(ecIns);
          };
          class_1.prototype.leaveBlur = function(el) {
            leaveBlur(el);
            markStatusToUpdate(ecIns);
          };
          class_1.prototype.enterSelect = function(el) {
            enterSelect(el);
            markStatusToUpdate(ecIns);
          };
          class_1.prototype.leaveSelect = function(el) {
            leaveSelect(el);
            markStatusToUpdate(ecIns);
          };
          class_1.prototype.getModel = function() {
            return ecIns.getModel();
          };
          class_1.prototype.getViewOfComponentModel = function(componentModel) {
            return ecIns.getViewOfComponentModel(componentModel);
          };
          class_1.prototype.getViewOfSeriesModel = function(seriesModel) {
            return ecIns.getViewOfSeriesModel(seriesModel);
          };
          class_1.prototype.getECUpdateCycleVersion = function() {
            return ecIns[EC_UPDATE_CYCLE_VERSION_KEY];
          };
          class_1.prototype.usingTHL = function() {
            return ecIns._usingTHL;
          };
          return class_1;
        })(ExtensionAPI_default))(ecIns);
      };
      enableConnect = function(chart) {
        function updateConnectedChartsStatus(charts, status) {
          for (var i2 = 0; i2 < charts.length; i2++) {
            var otherChart = charts[i2];
            otherChart[CONNECT_STATUS_KEY] = status;
          }
        }
        each(connectionEventRevertMap, function(_, eventType) {
          chart._messageCenter.on(eventType, function(event) {
            if (connectedGroups[chart.group] && chart[CONNECT_STATUS_KEY] !== CONNECT_STATUS_PENDING) {
              if (event && event.escapeConnect) {
                return;
              }
              var action_1 = chart.makeActionFromEvent(event);
              var otherCharts_1 = [];
              each(instances2, function(otherChart) {
                if (otherChart !== chart && otherChart.group === chart.group) {
                  otherCharts_1.push(otherChart);
                }
              });
              updateConnectedChartsStatus(otherCharts_1, CONNECT_STATUS_PENDING);
              each(otherCharts_1, function(otherChart) {
                if (otherChart[CONNECT_STATUS_KEY] !== CONNECT_STATUS_UPDATING) {
                  otherChart.dispatchAction(action_1);
                }
              });
              updateConnectedChartsStatus(otherCharts_1, CONNECT_STATUS_UPDATED);
            }
          });
        });
      };
    })();
    return ECharts2;
  })(Eventful_default)
);
var echartsProto = ECharts.prototype;
echartsProto.on = createRegisterEventWithLowercaseECharts("on");
echartsProto.off = createRegisterEventWithLowercaseECharts("off");
echartsProto.one = function(eventName, cb, ctx) {
  var self2 = this;
  deprecateLog("ECharts#one is deprecated.");
  function wrapped() {
    var args2 = [];
    for (var _i = 0; _i < arguments.length; _i++) {
      args2[_i] = arguments[_i];
    }
    cb && cb.apply && cb.apply(this, args2);
    self2.off(eventName, wrapped);
  }
  ;
  this.on.call(this, eventName, wrapped, ctx);
};
var MOUSE_EVENT_NAMES = ["click", "dblclick", "mouseover", "mouseout", "mousemove", "mousedown", "mouseup", "globalout", "contextmenu"];
function disposedWarning(id) {
  if (true) {
    warn("Instance " + id + " has been disposed");
  }
}
var actions = {};
var connectionEventRevertMap = {};
var publicEventTypeMap = {};
var dataProcessorFuncs = [];
var optionPreprocessorFuncs = [];
var visualFuncs = [];
var themeStorage = {};
var loadingEffects = {};
var instances2 = {};
var connectedGroups = {};
var idBase = +/* @__PURE__ */ new Date() - 0;
var groupIdBase = +/* @__PURE__ */ new Date() - 0;
var DOM_ATTRIBUTE_KEY = "_echarts_instance_";
function init2(dom, theme2, opts) {
  var isClient = !(opts && opts.ssr);
  if (isClient) {
    if (true) {
      if (!dom) {
        throw new Error("Initialize failed: invalid dom.");
      }
    }
    var existInstance = getInstanceByDom(dom);
    if (existInstance) {
      if (true) {
        warn("There is a chart instance already initialized on the dom.");
      }
      return existInstance;
    }
    if (true) {
      if (isDom(dom) && dom.nodeName.toUpperCase() !== "CANVAS" && (!dom.clientWidth && (!opts || opts.width == null) || !dom.clientHeight && (!opts || opts.height == null))) {
        warn("Can't get DOM width or height. Please check dom.clientWidth and dom.clientHeight. They should not be 0.For example, you may need to call this in the callback of window.onload.");
      }
    }
  }
  var chart = new ECharts(dom, theme2, opts);
  chart.id = "ec_" + idBase++;
  instances2[chart.id] = chart;
  isClient && setAttribute(dom, DOM_ATTRIBUTE_KEY, chart.id);
  enableConnect(chart);
  lifecycle_default.trigger("afterinit", chart);
  return chart;
}
function getInstanceByDom(dom) {
  return instances2[getAttribute(dom, DOM_ATTRIBUTE_KEY)];
}
function registerTheme(name, theme2) {
  themeStorage[name] = theme2;
}
function registerPreprocessor(preprocessorFunc) {
  if (indexOf(optionPreprocessorFuncs, preprocessorFunc) < 0) {
    optionPreprocessorFuncs.push(preprocessorFunc);
  }
}
function registerProcessor(priority, processor) {
  normalizeRegister(dataProcessorFuncs, priority, processor, PRIORITY_PROCESSOR_DEFAULT);
}
function registerPostInit(postInitFunc) {
  registerUpdateLifecycle("afterinit", postInitFunc);
}
function registerPostUpdate(postUpdateFunc) {
  registerUpdateLifecycle("afterupdate", postUpdateFunc);
}
function registerUpdateLifecycle(name, cb) {
  lifecycle_default.on(name, cb);
}
function registerAction(arg0, arg1, action) {
  var actionType;
  var publicEventType;
  var refineEvent;
  var update;
  var publishNonRefinedEvent;
  if (isFunction(arg1)) {
    action = arg1;
    arg1 = "";
  }
  if (isObject(arg0)) {
    actionType = arg0.type;
    publicEventType = arg0.event;
    update = arg0.update;
    publishNonRefinedEvent = arg0.publishNonRefinedEvent;
    if (!action) {
      action = arg0.action;
    }
    refineEvent = arg0.refineEvent;
  } else {
    actionType = arg0;
    publicEventType = arg1;
  }
  function createEventType(actionOrEventType) {
    return actionOrEventType.toLowerCase();
  }
  publicEventType = createEventType(publicEventType || actionType);
  var nonRefinedEventType = refineEvent ? createEventType(actionType) : publicEventType;
  if (actions[actionType]) {
    return;
  }
  assert(ACTION_REG.test(actionType) && ACTION_REG.test(publicEventType));
  if (refineEvent) {
    assert(publicEventType !== actionType);
  }
  actions[actionType] = {
    actionType,
    refinedEventType: publicEventType,
    nonRefinedEventType,
    update,
    action,
    refineEvent
  };
  publicEventTypeMap[publicEventType] = 1;
  if (refineEvent && publishNonRefinedEvent) {
    publicEventTypeMap[nonRefinedEventType] = 1;
  }
  if (connectionEventRevertMap[nonRefinedEventType]) {
    error(nonRefinedEventType + ' must not be shared; use "refineEvent" if you intend to share an event name.');
  }
  connectionEventRevertMap[nonRefinedEventType] = actionType;
}
function registerCoordinateSystem(type, coordSysCreator) {
  CoordinateSystem_default.register(type, coordSysCreator);
}
function registerLayout(priority, layoutTask) {
  normalizeRegister(visualFuncs, priority, layoutTask, PRIORITY_VISUAL_LAYOUT, "layout", true);
}
function registerVisual(priority, visualTask) {
  normalizeRegister(visualFuncs, priority, visualTask, PRIORITY_VISUAL_CHART, "visual", true);
}
var registeredTasks = [];
function normalizeRegister(targetList, priority, fn, defaultPriority, visualType, checkBlock) {
  if (isFunction(priority) || isObject(priority)) {
    fn = priority;
    priority = defaultPriority;
  }
  if (true) {
    if (isNaN(priority) || priority == null) {
      throw new Error("Illegal priority");
    }
    each(targetList, function(wrap) {
      assert(wrap.__raw !== fn);
    });
  }
  if (indexOf(registeredTasks, fn) >= 0) {
    return;
  }
  registeredTasks.push(fn);
  var stageHandler = Scheduler_default.wrapStageHandler(fn, visualType);
  stageHandler.__prio = priority;
  stageHandler.__raw = fn;
  targetList.push(stageHandler);
  if (true) {
    if (checkBlock) {
      assert(!stageHandler.dirtyOnOverallProgress, "dirtyOnOverallProgress is not allowed in " + visualType + " stage; otherwise progressive rendering is disabled on all series.");
    }
  }
}
function registerLoading(name, loadingFx) {
  loadingEffects[name] = loadingFx;
}
function registerMap(mapName, geoJson, specialAreas) {
  var registerMap2 = getImpl("registerMap");
  registerMap2 && registerMap2(mapName, geoJson, specialAreas);
}
var registerTransform = registerExternalTransform;
registerVisual(PRIORITY_VISUAL_GLOBAL, seriesStyleTask);
registerVisual(PRIORITY_VISUAL_CHART_DATA_CUSTOM, dataStyleTask);
registerVisual(PRIORITY_VISUAL_CHART_DATA_CUSTOM, dataColorPaletteTask);
registerVisual(PRIORITY_VISUAL_GLOBAL, seriesSymbolTask);
registerVisual(PRIORITY_VISUAL_CHART_DATA_CUSTOM, dataSymbolTask);
registerVisual(PRIORITY_VISUAL_DECAL, decalVisualStageHandler);
registerPreprocessor(globalBackwardCompat);
registerProcessor(PRIORITY_PROCESSOR_DATASTACK, dataStackStageHandler);
registerLoading("default", defaultLoading);
registerAction({
  type: HIGHLIGHT_ACTION_TYPE,
  event: HIGHLIGHT_ACTION_TYPE,
  update: HIGHLIGHT_ACTION_TYPE
}, noop);
registerAction({
  type: DOWNPLAY_ACTION_TYPE,
  event: DOWNPLAY_ACTION_TYPE,
  update: DOWNPLAY_ACTION_TYPE
}, noop);
registerAction({
  type: SELECT_ACTION_TYPE,
  event: SELECT_CHANGED_EVENT_TYPE,
  update: SELECT_ACTION_TYPE,
  action: noop,
  refineEvent: makeSelectChangedEvent,
  publishNonRefinedEvent: true
});
registerAction({
  type: UNSELECT_ACTION_TYPE,
  event: SELECT_CHANGED_EVENT_TYPE,
  update: UNSELECT_ACTION_TYPE,
  action: noop,
  refineEvent: makeSelectChangedEvent,
  publishNonRefinedEvent: true
});
registerAction({
  type: TOGGLE_SELECT_ACTION_TYPE,
  event: SELECT_CHANGED_EVENT_TYPE,
  update: TOGGLE_SELECT_ACTION_TYPE,
  action: noop,
  refineEvent: makeSelectChangedEvent,
  publishNonRefinedEvent: true
});
function makeSelectChangedEvent(actionResultBatch, payload, ecModel, api) {
  return {
    eventContent: {
      selected: getAllSelectedIndices(ecModel),
      isFromClick: payload.isFromClick || false
    }
  };
}
registerTheme("default", {});
registerTheme("dark", dark_default);

// node_modules/echarts/lib/extension.js
var extensions = [];
var extensionRegisters = {
  registerPreprocessor,
  registerProcessor,
  registerPostInit,
  registerPostUpdate,
  registerUpdateLifecycle,
  registerAction,
  registerCoordinateSystem,
  registerLayout,
  registerVisual,
  registerTransform,
  registerLoading,
  registerMap,
  registerImpl,
  PRIORITY,
  ComponentModel: Component_default,
  ComponentView: Component_default2,
  SeriesModel: Series_default,
  ChartView: Chart_default,
  // TODO Use ComponentModel and SeriesModel instead of Constructor
  registerComponentModel: function(ComponentModelClass) {
    Component_default.registerClass(ComponentModelClass);
  },
  registerComponentView: function(ComponentViewClass) {
    Component_default2.registerClass(ComponentViewClass);
  },
  registerSeriesModel: function(SeriesModelClass) {
    Series_default.registerClass(SeriesModelClass);
  },
  registerChartView: function(ChartViewClass) {
    Chart_default.registerClass(ChartViewClass);
  },
  registerCustomSeries: function(seriesType2, renderItem) {
    registerCustomSeries(seriesType2, renderItem);
  },
  registerSubTypeDefaulter: function(componentType, defaulter) {
    Component_default.registerSubTypeDefaulter(componentType, defaulter);
  },
  registerPainter: function(painterType, PainterCtor) {
    registerPainter(painterType, PainterCtor);
  }
};
function use(ext) {
  if (isArray(ext)) {
    each(ext, function(singleExt) {
      use(singleExt);
    });
    return;
  }
  if (indexOf(extensions, ext) >= 0) {
    return;
  }
  extensions.push(ext);
  if (isFunction(ext)) {
    ext = {
      install: ext
    };
  }
  ext.install(extensionRegisters);
}

// node_modules/echarts/lib/chart/scatter/ScatterSeries.js
var ScatterSeriesModel = (
  /** @class */
  (function(_super) {
    __extends(ScatterSeriesModel2, _super);
    function ScatterSeriesModel2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = ScatterSeriesModel2.type;
      _this.hasSymbolVisual = true;
      return _this;
    }
    ScatterSeriesModel2.prototype.getInitialData = function(option, ecModel) {
      return createSeriesData_default(null, this, {
        useEncodeDefaulter: true
      });
    };
    ScatterSeriesModel2.prototype.getProgressive = function() {
      var progressive = this.option.progressive;
      if (progressive == null) {
        return this.option.large ? 5e3 : this.get("progressive");
      }
      return progressive;
    };
    ScatterSeriesModel2.prototype.getProgressiveThreshold = function() {
      var progressiveThreshold = this.option.progressiveThreshold;
      if (progressiveThreshold == null) {
        return this.option.large ? 1e4 : this.get("progressiveThreshold");
      }
      return progressiveThreshold;
    };
    ScatterSeriesModel2.prototype.brushSelector = function(dataIndex, data, selectors) {
      return selectors.point(data.getItemLayout(dataIndex));
    };
    ScatterSeriesModel2.prototype.getZLevelKey = function() {
      return this.getData().count() > this.getProgressiveThreshold() ? this.id : "";
    };
    ScatterSeriesModel2.type = "series.scatter";
    ScatterSeriesModel2.dependencies = ["grid", "polar", "geo", "singleAxis", "calendar", "matrix"];
    ScatterSeriesModel2.defaultOption = {
      coordinateSystem: "cartesian2d",
      // zlevel: 0,
      z: 2,
      legendHoverLink: true,
      symbolSize: 10,
      // symbolRotate: null,
      large: false,
      // Available when large is true
      largeThreshold: 2e3,
      // cursor: null,
      itemStyle: {
        opacity: 0.8
      },
      emphasis: {
        scale: true
      },
      clip: true,
      select: {
        itemStyle: {
          borderColor: tokens_default.color.primary
        }
      },
      universalTransition: {
        divideShape: "clone"
      }
      // progressive: null
    };
    return ScatterSeriesModel2;
  })(Series_default)
);

// node_modules/echarts/lib/chart/helper/LargeSymbolDraw.js
var BOOST_SIZE_THRESHOLD = 4;
var LargeSymbolPathShape = (
  /** @class */
  /* @__PURE__ */ (function() {
    function LargeSymbolPathShape2() {
    }
    return LargeSymbolPathShape2;
  })()
);
var LargeSymbolPath = (
  /** @class */
  (function(_super) {
    __extends(LargeSymbolPath2, _super);
    function LargeSymbolPath2(opts) {
      var _this = _super.call(this, opts) || this;
      _this._off = 0;
      _this.hoverDataIdx = -1;
      return _this;
    }
    LargeSymbolPath2.prototype.getDefaultShape = function() {
      return new LargeSymbolPathShape();
    };
    LargeSymbolPath2.prototype.reset = function() {
      this.notClear = false;
      this._off = 0;
    };
    LargeSymbolPath2.prototype.beforeBrush = function(param) {
      if (param && !param.contentRetained) {
        this.reset();
      }
    };
    LargeSymbolPath2.prototype.buildPath = function(path, shape) {
      var points4 = shape.points;
      var size = shape.size;
      var symbolProxy = this.symbolProxy;
      var symbolProxyShape = symbolProxy.shape;
      var ctx = path.getContext ? path.getContext() : path;
      var canBoost = ctx && size[0] < BOOST_SIZE_THRESHOLD;
      var softClipShape = this.softClipShape;
      var i2;
      if (canBoost) {
        this._ctx = ctx;
        return;
      }
      this._ctx = null;
      for (i2 = this._off; i2 < points4.length; ) {
        var x = points4[i2++];
        var y = points4[i2++];
        if (isNaN(x) || isNaN(y)) {
          continue;
        }
        if (softClipShape && !softClipShape.contain(x, y)) {
          continue;
        }
        symbolProxyShape.x = x - size[0] / 2;
        symbolProxyShape.y = y - size[1] / 2;
        symbolProxyShape.width = size[0];
        symbolProxyShape.height = size[1];
        symbolProxy.buildPath(path, symbolProxyShape, true);
      }
      if (this.incremental) {
        this._off = i2;
        this.notClear = true;
      }
    };
    LargeSymbolPath2.prototype.afterBrush = function() {
      var shape = this.shape;
      var points4 = shape.points;
      var size = shape.size;
      var ctx = this._ctx;
      var softClipShape = this.softClipShape;
      var i2;
      if (!ctx) {
        return;
      }
      for (i2 = this._off; i2 < points4.length; ) {
        var x = points4[i2++];
        var y = points4[i2++];
        if (isNaN(x) || isNaN(y)) {
          continue;
        }
        if (softClipShape && !softClipShape.contain(x, y)) {
          continue;
        }
        ctx.fillRect(x - size[0] / 2, y - size[1] / 2, size[0], size[1]);
      }
      if (this.incremental) {
        this._off = i2;
        this.notClear = true;
      }
    };
    LargeSymbolPath2.prototype.findDataIndex = function(x, y) {
      var shape = this.shape;
      var points4 = shape.points;
      var size = shape.size;
      var w = Math.max(size[0], 4);
      var h = Math.max(size[1], 4);
      for (var idx = points4.length / 2 - 1; idx >= 0; idx--) {
        var i2 = idx * 2;
        var x0 = points4[i2] - w / 2;
        var y0 = points4[i2 + 1] - h / 2;
        if (x >= x0 && y >= y0 && x <= x0 + w && y <= y0 + h) {
          return idx;
        }
      }
      return -1;
    };
    LargeSymbolPath2.prototype.contain = function(x, y) {
      var localPos = this.transformCoordToLocal(x, y);
      var rect = this.getBoundingRect();
      x = localPos[0];
      y = localPos[1];
      if (rect.contain(x, y)) {
        var dataIdx = this.hoverDataIdx = this.findDataIndex(x, y);
        return dataIdx >= 0;
      }
      this.hoverDataIdx = -1;
      return false;
    };
    LargeSymbolPath2.prototype.getBoundingRect = function() {
      var rect = this._rect;
      if (!rect) {
        var shape = this.shape;
        var points4 = shape.points;
        var size = shape.size;
        var w = size[0];
        var h = size[1];
        var minX = Infinity;
        var minY = Infinity;
        var maxX = -Infinity;
        var maxY = -Infinity;
        for (var i2 = 0; i2 < points4.length; ) {
          var x = points4[i2++];
          var y = points4[i2++];
          minX = Math.min(x, minX);
          maxX = Math.max(x, maxX);
          minY = Math.min(y, minY);
          maxY = Math.max(y, maxY);
        }
        rect = this._rect = new BoundingRect_default(minX - w / 2, minY - h / 2, maxX - minX + w, maxY - minY + h);
      }
      return rect;
    };
    return LargeSymbolPath2;
  })(Path_default)
);
var LargeSymbolDraw = (
  /** @class */
  (function() {
    function LargeSymbolDraw2() {
      this.group = new Group_default();
    }
    LargeSymbolDraw2.prototype.updateData = function(data, opt) {
      this._clear();
      this._data = data;
      var symbolEl = this._create();
      symbolEl.setShape({
        points: data.getLayout("points")
      });
      this._setCommon(symbolEl, data, opt);
    };
    LargeSymbolDraw2.prototype.updateLayout = function(opt) {
      var data = this._data;
      if (!data) {
        return;
      }
      var points4 = data.getLayout("points");
      this.group.eachChild(function(child) {
        if (child.startIndex != null) {
          var len2 = (child.endIndex - child.startIndex) * 2;
          var byteOffset = child.startIndex * 4 * 2;
          points4 = new Float32Array(points4.buffer, byteOffset, len2);
        }
        child.setShape("points", points4);
        child.reset();
        child.stopAnimation();
      });
    };
    LargeSymbolDraw2.prototype.incrementalPrepareUpdate = function(data) {
      this._clear();
    };
    LargeSymbolDraw2.prototype.incrementalUpdate = function(taskParams, data, incrementalId, opt) {
      var lastAdded = this._newAdded[0];
      var points4 = data.getLayout("points");
      var oldPoints = lastAdded && lastAdded.shape.points;
      if (true) {
        validateUpstreamOutputRange(data.getLayout("pointsRange"), taskParams);
      }
      if (oldPoints && oldPoints.length < 2e4) {
        var oldLen = oldPoints.length;
        var newPoints = new Float32Array(oldLen + points4.length);
        newPoints.set(oldPoints);
        newPoints.set(points4, oldLen);
        lastAdded.endIndex = taskParams.end;
        lastAdded.setShape({
          points: newPoints
        });
      } else {
        this._newAdded = [];
        var symbolEl = this._create();
        symbolEl.startIndex = taskParams.start;
        symbolEl.endIndex = taskParams.end;
        symbolEl.incremental = incrementalId;
        symbolEl.setShape({
          points: points4
        });
        this._setCommon(symbolEl, data, opt);
      }
    };
    LargeSymbolDraw2.prototype.eachRendered = function(cb) {
      this._newAdded[0] && cb(this._newAdded[0]);
    };
    LargeSymbolDraw2.prototype._create = function() {
      var symbolEl = new LargeSymbolPath({
        cursor: "default"
      });
      symbolEl.ignoreCoarsePointer = true;
      this.group.add(symbolEl);
      this._newAdded.push(symbolEl);
      return symbolEl;
    };
    LargeSymbolDraw2.prototype._setCommon = function(symbolEl, data, opt) {
      var hostModel = data.hostModel;
      opt = opt || {};
      var size = data.getVisual("symbolSize");
      symbolEl.setShape("size", size instanceof Array ? size : [size, size]);
      symbolEl.softClipShape = opt.clipShape || null;
      symbolEl.symbolProxy = createSymbol(data.getVisual("symbol"), 0, 0, 0, 0);
      symbolEl.setColor = symbolEl.symbolProxy.setColor;
      var extrudeShadow = symbolEl.shape.size[0] < BOOST_SIZE_THRESHOLD;
      symbolEl.useStyle(
        // Draw shadow when doing fillRect is extremely slow.
        hostModel.getModel("itemStyle").getItemStyle(extrudeShadow ? ["color", "shadowBlur", "shadowColor"] : ["color"])
      );
      var globalStyle = data.getVisual("style");
      var visualColor = globalStyle && globalStyle.fill;
      if (visualColor) {
        symbolEl.setColor(visualColor);
      }
      var ecData = getECData(symbolEl);
      ecData.seriesIndex = hostModel.seriesIndex;
      symbolEl.on("mousemove", function(e2) {
        ecData.dataIndex = null;
        var dataIndex = symbolEl.hoverDataIdx;
        if (dataIndex >= 0) {
          ecData.dataIndex = dataIndex + (symbolEl.startIndex || 0);
        }
      });
    };
    LargeSymbolDraw2.prototype.remove = function() {
      this._clear();
    };
    LargeSymbolDraw2.prototype._clear = function() {
      this._newAdded = [];
      this.group.removeAll();
    };
    return LargeSymbolDraw2;
  })()
);
var LargeSymbolDraw_default = LargeSymbolDraw;

// node_modules/echarts/lib/chart/scatter/ScatterView.js
var ScatterView = (
  /** @class */
  (function(_super) {
    __extends(ScatterView2, _super);
    function ScatterView2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = ScatterView2.type;
      return _this;
    }
    ScatterView2.prototype.render = function(seriesModel, ecModel, api) {
      var data = seriesModel.getData();
      var symbolDraw = this._updateSymbolDraw(data, seriesModel);
      symbolDraw.updateData(data, createSymbolDrawOpt(seriesModel));
      this._finished = true;
    };
    ScatterView2.prototype.incrementalPrepareRender = function(seriesModel, ecModel, api) {
      var data = seriesModel.getData();
      var symbolDraw = this._updateSymbolDraw(data, seriesModel);
      symbolDraw.incrementalPrepareUpdate(data);
      this._finished = false;
    };
    ScatterView2.prototype.incrementalRender = function(taskParams, seriesModel, ecModel) {
      this._symbolDraw.incrementalUpdate(taskParams, seriesModel.getData(), getIncrementalId(seriesModel), createSymbolDrawOpt(seriesModel));
      this._finished = taskParams.end === seriesModel.getData().count();
    };
    ScatterView2.prototype.updateTransform = function(seriesModel, ecModel, api) {
      var data = seriesModel.getData();
      this.group.dirty();
      if (!this._finished) {
        return {
          update: true
        };
      } else {
        var res = pointsLayout("").reset(seriesModel, ecModel, api);
        if (res.progress) {
          res.progress({
            start: 0,
            end: data.count(),
            count: data.count()
          }, data);
        }
        this._symbolDraw.updateLayout(createSymbolDrawOpt(seriesModel));
      }
    };
    ScatterView2.prototype.eachRendered = function(cb) {
      this._symbolDraw && this._symbolDraw.eachRendered(cb);
    };
    ScatterView2.prototype._updateSymbolDraw = function(data, seriesModel) {
      var symbolDraw = this._symbolDraw;
      var pipelineContext = seriesModel.pipelineContext;
      var isLargeDraw = pipelineContext.large;
      if (!symbolDraw || isLargeDraw !== this._isLargeDraw) {
        symbolDraw && symbolDraw.remove();
        symbolDraw = this._symbolDraw = isLargeDraw ? new LargeSymbolDraw_default() : new SymbolDraw_default();
        this._isLargeDraw = isLargeDraw;
        this.group.removeAll();
      }
      this.group.add(symbolDraw.group);
      return symbolDraw;
    };
    ScatterView2.prototype.remove = function(ecModel, api) {
      this._symbolDraw && this._symbolDraw.remove(true);
      this._symbolDraw = null;
    };
    ScatterView2.prototype.dispose = function() {
    };
    ScatterView2.type = "scatter";
    return ScatterView2;
  })(Chart_default)
);
function createSymbolDrawOpt(seriesModel) {
  return {
    // TODO
    // If this parameter should be a shape or a bounding volume
    // shape will be more general.
    // But bounding volume like bounding rect will be much faster in the contain calculation
    clipShape: createCoordSysClipAreaSimply(seriesModel)
  };
}

// node_modules/echarts/lib/coord/axisModelCommonMixin.js
var AxisModelCommonMixin = (
  /** @class */
  (function() {
    function AxisModelCommonMixin2() {
    }
    AxisModelCommonMixin2.prototype.needIncludeZero = function() {
      return !this.option.scale;
    };
    AxisModelCommonMixin2.prototype.getCoordSysModel = function() {
      return;
    };
    return AxisModelCommonMixin2;
  })()
);

// node_modules/echarts/lib/coord/cartesian/AxisModel.js
var CartesianAxisModel = (
  /** @class */
  (function(_super) {
    __extends(CartesianAxisModel2, _super);
    function CartesianAxisModel2() {
      return _super !== null && _super.apply(this, arguments) || this;
    }
    CartesianAxisModel2.prototype.getCoordSysModel = function() {
      return this.getReferringComponents("grid", SINGLE_REFERRING).models[0];
    };
    CartesianAxisModel2.type = "cartesian2dAxis";
    return CartesianAxisModel2;
  })(Component_default)
);
mixin(CartesianAxisModel, AxisModelCommonMixin);

// node_modules/echarts/lib/coord/axisDefault.js
var defaultOption = {
  show: true,
  // zlevel: 0,
  z: 0,
  // Inverse the axis.
  inverse: false,
  // Axis name displayed.
  name: "",
  // 'start' | 'middle' | 'end'
  nameLocation: "end",
  // By degree. By default auto rotate by nameLocation.
  nameRotate: null,
  nameTruncate: {
    maxWidth: null,
    ellipsis: "...",
    placeholder: "."
  },
  // Use global text style by default.
  nameTextStyle: {
    // textMargin: never, // The default value will be specified based on `nameLocation`.
  },
  // The gap between axisName and axisLine.
  nameGap: 15,
  // Default `false` to support tooltip.
  silent: false,
  // Default `false` to avoid legacy user event listener fail.
  triggerEvent: false,
  tooltip: {
    show: false
  },
  axisPointer: {},
  axisLine: {
    show: true,
    onZero: "auto",
    onZeroAxisIndex: null,
    lineStyle: {
      color: tokens_default.color.axisLine,
      width: 1,
      type: "solid"
    },
    // The arrow at both ends the the axis.
    symbol: ["none", "none"],
    symbolSize: [10, 15],
    breakLine: true
  },
  axisTick: {
    show: true,
    // Whether axisTick is inside the grid or outside the grid.
    inside: false,
    // The length of axisTick.
    length: 5,
    lineStyle: {
      width: 1
    }
  },
  axisLabel: {
    show: true,
    // Whether axisLabel is inside the grid or outside the grid.
    inside: false,
    rotate: 0,
    // true | false | null/undefined (auto)
    showMinLabel: null,
    // true | false | null/undefined (auto)
    showMaxLabel: null,
    margin: 8,
    // formatter: null,
    fontSize: 12,
    color: tokens_default.color.axisLabel,
    // In scenarios like axis labels, when labels text's progression direction matches the label
    // layout direction (e.g., when all letters are in a single line), extra start/end margin is
    // needed to prevent the text from appearing visually joined. In the other case, when lables
    // are stacked (e.g., having rotation or horizontal labels on yAxis), the layout needs to be
    // compact, so NO extra top/bottom margin should be applied.
    textMargin: [0, 3]
  },
  splitLine: {
    show: true,
    showMinLine: true,
    showMaxLine: true,
    lineStyle: {
      color: tokens_default.color.axisSplitLine,
      width: 1,
      type: "solid"
    }
  },
  splitArea: {
    show: false,
    areaStyle: {
      color: [tokens_default.color.backgroundTint, tokens_default.color.backgroundTransparent]
    }
  },
  breakArea: {
    show: true,
    itemStyle: {
      color: tokens_default.color.neutral00,
      // Break border color should be darker than the splitLine
      // because it has opacity and should be more prominent
      borderColor: tokens_default.color.border,
      borderWidth: 1,
      borderType: [3, 3],
      opacity: 0.6
    },
    zigzagAmplitude: 4,
    zigzagMinSpan: 4,
    zigzagMaxSpan: 20,
    zigzagZ: 100,
    expandOnClick: true
  },
  breakLabelLayout: {
    moveOverlap: "auto"
  }
};
var categoryAxis = merge2({
  // The gap at both ends of the axis. For categoryAxis, boolean.
  boundaryGap: true,
  // Set false to faster category collection.
  deduplication: null,
  jitter: 0,
  jitterOverlap: true,
  jitterMargin: 2,
  // splitArea: {
  // show: false
  // },
  splitLine: {
    show: false
  },
  axisTick: {
    // If tick is align with label when boundaryGap is true
    alignWithLabel: false,
    interval: "auto",
    show: "auto"
  },
  axisLabel: {
    interval: "auto"
  }
}, defaultOption);
var valueAxis = merge2({
  boundaryGap: [0, 0],
  axisLine: {
    // Not shown when other axis is categoryAxis in cartesian
    show: "auto"
  },
  axisTick: {
    // Not shown when other axis is categoryAxis in cartesian
    show: "auto"
  },
  // TODO
  // min/max: [30, datamin, 60] or [20, datamin] or [datamin, 60]
  splitNumber: 5,
  minorTick: {
    // Minor tick, not available for cateogry axis.
    show: false,
    // Split number of minor ticks. The value should be in range of (0, 100)
    splitNumber: 5,
    // Length of minor tick
    length: 3,
    // Line style
    lineStyle: {
      // Default to be same with axisTick
    }
  },
  minorSplitLine: {
    show: false,
    lineStyle: {
      color: tokens_default.color.axisMinorSplitLine,
      width: 1
    }
  }
}, defaultOption);
var timeAxis = merge2({
  splitNumber: 6,
  axisLabel: {
    // The default value of TimeScale is determined in `AxisBuilder`
    // showMinLabel: false,
    // showMaxLabel: false,
    rich: {
      primary: {
        fontWeight: "bold"
      }
    }
  },
  splitLine: {
    show: false
  }
}, valueAxis);
var logAxis = defaults({
  logBase: 10
}, valueAxis);
var axisDefault_default = {
  category: categoryAxis,
  value: valueAxis,
  time: timeAxis,
  log: logAxis
};

// node_modules/echarts/lib/coord/axisModelCreator.js
function axisModelCreator(registers, axisName, BaseAxisModelClass, extraDefaultOption) {
  each(AXIS_TYPES, function(v, axisType) {
    var defaultOption3 = merge2(merge2({}, axisDefault_default[axisType], true), extraDefaultOption, true);
    var AxisModel = (
      /** @class */
      (function(_super) {
        __extends(AxisModel2, _super);
        function AxisModel2() {
          var _this = _super !== null && _super.apply(this, arguments) || this;
          _this.type = axisName + "Axis." + axisType;
          return _this;
        }
        AxisModel2.prototype.mergeDefaultAndTheme = function(option, ecModel) {
          var layoutMode = fetchLayoutMode(this);
          var inputPositionParams = layoutMode ? getLayoutParams(option) : {};
          var themeModel = ecModel.getTheme();
          merge2(option, themeModel.get(axisType + "Axis"));
          merge2(option, this.getDefaultOption());
          option.type = getAxisType(option);
          if (layoutMode) {
            mergeLayoutParam(option, inputPositionParams, layoutMode);
          }
        };
        AxisModel2.prototype.optionUpdated = function() {
          var thisOption = this.option;
          if (thisOption.type === "category") {
            this.__ordinalMeta = OrdinalMeta_default.createByAxisModel(this);
          }
        };
        AxisModel2.prototype.getCategories = function(rawData) {
          var option = this.option;
          if (option.type === "category") {
            if (rawData) {
              return option.data;
            }
            return this.__ordinalMeta.categories;
          }
        };
        AxisModel2.prototype.getOrdinalMeta = function() {
          return this.__ordinalMeta;
        };
        AxisModel2.prototype.updateAxisBreaks = function(payload) {
          var axisBreakHelper = getAxisBreakHelper();
          return axisBreakHelper ? axisBreakHelper.updateModelAxisBreak(this, payload) : {
            breaks: []
          };
        };
        AxisModel2.type = axisName + "Axis." + axisType;
        AxisModel2.defaultOption = defaultOption3;
        return AxisModel2;
      })(BaseAxisModelClass)
    );
    registers.registerComponentModel(AxisModel);
  });
  registers.registerSubTypeDefaulter(axisName + "Axis", getAxisType);
}
function getAxisType(option) {
  return option.type || (option.data ? "category" : "value");
}

// node_modules/echarts/lib/coord/cartesian/Cartesian.js
var Cartesian = (
  /** @class */
  (function() {
    function Cartesian2(name) {
      this.type = "cartesian";
      this._dimList = [];
      this._axes = {};
      this.name = name || "";
    }
    Cartesian2.prototype.getAxis = function(dim) {
      return this._axes[dim];
    };
    Cartesian2.prototype.getAxes = function() {
      return map2(this._dimList, function(dim) {
        return this._axes[dim];
      }, this);
    };
    Cartesian2.prototype.getAxesByScale = function(scaleType) {
      scaleType = scaleType.toLowerCase();
      return filter2(this.getAxes(), function(axis) {
        return axis.scale.type === scaleType;
      });
    };
    Cartesian2.prototype.addAxis = function(axis) {
      var dim = axis.dim;
      this._axes[dim] = axis;
      this._dimList.push(dim);
    };
    return Cartesian2;
  })()
);
var Cartesian_default = Cartesian;

// node_modules/echarts/lib/coord/cartesian/Cartesian2D.js
var cartesian2DDimensions = ["x", "y"];
function canCalculateAffineTransform(scale3) {
  return (scale3.type === "interval" || scale3.type === "time") && !hasBreaks(scale3);
}
var Cartesian2D = (
  /** @class */
  (function(_super) {
    __extends(Cartesian2D2, _super);
    function Cartesian2D2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = COORD_SYS_TYPE_CARTESIAN_2D;
      _this.dimensions = cartesian2DDimensions;
      return _this;
    }
    Cartesian2D2.prototype.calcAffineTransform = function() {
      this._transform = this._invTransform = null;
      var xAxisScale = this.getAxis("x").scale;
      var yAxisScale = this.getAxis("y").scale;
      if (!canCalculateAffineTransform(xAxisScale) || !canCalculateAffineTransform(yAxisScale)) {
        return;
      }
      var xScaleExtent = getScaleExtentForMappingUnsafe(xAxisScale, null);
      var yScaleExtent = getScaleExtentForMappingUnsafe(yAxisScale, null);
      var start2 = this.dataToPoint([xScaleExtent[0], yScaleExtent[0]]);
      var end2 = this.dataToPoint([xScaleExtent[1], yScaleExtent[1]]);
      var xScaleSpan = xScaleExtent[1] - xScaleExtent[0];
      var yScaleSpan = yScaleExtent[1] - yScaleExtent[0];
      if (!xScaleSpan || !yScaleSpan) {
        return;
      }
      var scaleX = (end2[0] - start2[0]) / xScaleSpan;
      var scaleY = (end2[1] - start2[1]) / yScaleSpan;
      var translateX = start2[0] - xScaleExtent[0] * scaleX;
      var translateY = start2[1] - yScaleExtent[0] * scaleY;
      var m2 = this._transform = [scaleX, 0, 0, scaleY, translateX, translateY];
      this._invTransform = invert([], m2);
    };
    Cartesian2D2.prototype.getBaseAxis = function() {
      return this.getAxesByScale("ordinal")[0] || this.getAxesByScale("time")[0] || this.getAxis("x");
    };
    Cartesian2D2.prototype.containPoint = function(point) {
      var axisX = this.getAxis("x");
      var axisY = this.getAxis("y");
      return axisX.contain(axisX.toLocalCoord(point[0])) && axisY.contain(axisY.toLocalCoord(point[1]));
    };
    Cartesian2D2.prototype.containData = function(data) {
      return this.getAxis("x").containData(data[0]) && this.getAxis("y").containData(data[1]);
    };
    Cartesian2D2.prototype.containZone = function(data1, data2) {
      var zoneDiag1 = this.dataToPoint(data1);
      var zoneDiag2 = this.dataToPoint(data2);
      var area = this.getArea();
      var zone = new BoundingRect_default(zoneDiag1[0], zoneDiag1[1], zoneDiag2[0] - zoneDiag1[0], zoneDiag2[1] - zoneDiag1[1]);
      return area.intersect(zone);
    };
    Cartesian2D2.prototype.dataToPoint = function(data, clamp2, out2) {
      out2 = out2 || [];
      var xVal = data[0];
      var yVal = data[1];
      if (this._transform && xVal != null && isFinite(xVal) && yVal != null && isFinite(yVal)) {
        return applyTransform(out2, data, this._transform);
      }
      var xAxis = this.getAxis("x");
      var yAxis = this.getAxis("y");
      out2[0] = xAxis.toGlobalCoord(xAxis.dataToCoord(xVal, clamp2));
      out2[1] = yAxis.toGlobalCoord(yAxis.dataToCoord(yVal, clamp2));
      return out2;
    };
    Cartesian2D2.prototype.clampData = function(data, out2) {
      var xScale = this.getAxis("x").scale;
      var yScale = this.getAxis("y").scale;
      var xAxisExtent = xScale.getExtent();
      var yAxisExtent = yScale.getExtent();
      var x = xScale.parse(data[0]);
      var y = yScale.parse(data[1]);
      out2 = out2 || [];
      out2[0] = Math.min(Math.max(Math.min(xAxisExtent[0], xAxisExtent[1]), x), Math.max(xAxisExtent[0], xAxisExtent[1]));
      out2[1] = Math.min(Math.max(Math.min(yAxisExtent[0], yAxisExtent[1]), y), Math.max(yAxisExtent[0], yAxisExtent[1]));
      return out2;
    };
    Cartesian2D2.prototype.pointToData = function(point, clamp2, out2) {
      out2 = out2 || [];
      if (this._invTransform) {
        return applyTransform(out2, point, this._invTransform);
      }
      var xAxis = this.getAxis("x");
      var yAxis = this.getAxis("y");
      out2[0] = xAxis.coordToData(xAxis.toLocalCoord(point[0]), clamp2);
      out2[1] = yAxis.coordToData(yAxis.toLocalCoord(point[1]), clamp2);
      return out2;
    };
    Cartesian2D2.prototype.getOtherAxis = function(axis) {
      return this.getAxis(axis.dim === "x" ? "y" : "x");
    };
    Cartesian2D2.prototype.getArea = function(tolerance) {
      tolerance = tolerance || 0;
      var xExtent = this.getAxis("x").getGlobalExtent();
      var yExtent = this.getAxis("y").getGlobalExtent();
      var x = Math.min(xExtent[0], xExtent[1]) - tolerance;
      var y = Math.min(yExtent[0], yExtent[1]) - tolerance;
      var width = Math.max(xExtent[0], xExtent[1]) - x + tolerance;
      var height = Math.max(yExtent[0], yExtent[1]) - y + tolerance;
      return new BoundingRect_default(x, y, width, height);
    };
    return Cartesian2D2;
  })(Cartesian_default)
);
var Cartesian2D_default = Cartesian2D;

// node_modules/echarts/lib/coord/axisAlignTicks.js
function scaleCalcAlign(targetAxis, alignToScale) {
  var targetScale = targetAxis.scale;
  var targetAxisModel = targetAxis.model;
  if (true) {
    assert(targetScale && targetAxisModel && (targetScale instanceof Interval_default || targetScale instanceof Log_default) && (alignToScale instanceof Interval_default || alignToScale instanceof Log_default));
  }
  var targetExtentInfo = adoptScaleRawExtentInfoAndPrepare(targetScale, targetAxisModel, targetAxisModel.ecModel, targetAxis, null);
  var isTargetLogScale = isLogScale(targetScale);
  var alignToScaleLinear = isLogScale(alignToScale) ? alignToScale.intervalStub : alignToScale;
  var targetIntervalStub = isTargetLogScale ? targetScale.intervalStub : targetScale;
  var targetLogScaleBase = targetScale.base;
  var alignToTicks = alignToScaleLinear.getTicks();
  var alignToExpNiceTicks = alignToScaleLinear.getTicks({
    expandToNicedExtent: true
  });
  var alignToSegCount = alignToTicks.length - 1;
  if (true) {
    assert(!hasBreaks(alignToScale) && !hasBreaks(targetScale));
    assert(alignToSegCount > 0);
    assert(alignToExpNiceTicks.length === alignToTicks.length);
    assert(alignToTicks[0].value <= alignToTicks[alignToSegCount].value);
    assert(alignToExpNiceTicks[0].value <= alignToTicks[0].value && alignToTicks[alignToSegCount].value <= alignToExpNiceTicks[alignToSegCount].value);
    if (alignToSegCount >= 2) {
      assert(alignToExpNiceTicks[1].value === alignToTicks[1].value);
      assert(alignToExpNiceTicks[alignToSegCount - 1].value === alignToTicks[alignToSegCount - 1].value);
    }
  }
  var t0;
  var t1;
  var alignToNiceSegCount;
  if (alignToSegCount === 1) {
    t0 = t1 = 0;
    alignToNiceSegCount = 1;
  } else if (alignToSegCount === 2) {
    var interval0 = mathAbs3(alignToTicks[0].value - alignToTicks[1].value);
    var interval1 = mathAbs3(alignToTicks[1].value - alignToTicks[2].value);
    t0 = t1 = 0;
    if (interval0 === interval1) {
      alignToNiceSegCount = 2;
    } else {
      alignToNiceSegCount = 1;
      if (interval0 < interval1) {
        t0 = interval0 / interval1;
      } else {
        t1 = interval1 / interval0;
      }
    }
  } else {
    var alignToInterval = alignToScaleLinear.getConfig().interval;
    t0 = (1 - (alignToTicks[0].value - alignToExpNiceTicks[0].value) / alignToInterval) % 1;
    t1 = (1 - (alignToExpNiceTicks[alignToSegCount].value - alignToTicks[alignToSegCount].value) / alignToInterval) % 1;
    alignToNiceSegCount = alignToSegCount - (t0 ? 1 : 0) - (t1 ? 1 : 0);
  }
  if (true) {
    assert(alignToNiceSegCount >= 1);
  }
  var dataZoomFixMinMax = targetExtentInfo.zoomFixMM;
  var hasDataZoomFixMinMax = dataZoomFixMinMax[0] || dataZoomFixMinMax[1];
  var targetMinMaxFixed = [targetExtentInfo.fixMM[0] || hasDataZoomFixMinMax, targetExtentInfo.fixMM[1] || hasDataZoomFixMinMax];
  var targetOldOutermostExtent = targetScale.getExtent();
  var targetOldIntervalExtent = targetIntervalStub.getExtent();
  var targetExtent = intervalScaleEnsureValidExtent(targetOldIntervalExtent, targetMinMaxFixed);
  var min3;
  var max3;
  var interval2;
  var intervalPrecision;
  var maxNice;
  var minNice;
  function loopIncreaseInterval(cb) {
    var LOOP_MAX = 50;
    var loopGuard = 0;
    for (; loopGuard < LOOP_MAX; loopGuard++) {
      if (cb()) {
        break;
      }
      interval2 = isTargetLogScale ? interval2 * mathMax4(targetLogScaleBase, 2) : increaseInterval(interval2);
      intervalPrecision = getIntervalPrecision(interval2);
    }
    if (true) {
      if (loopGuard >= LOOP_MAX) {
        warn("incorrect impl in `scaleCalcAlign`.");
      }
    }
  }
  function updateMinFromMinNice() {
    min3 = round2(minNice - interval2 * t0, intervalPrecision);
  }
  function updateMaxFromMaxNice() {
    max3 = round2(maxNice + interval2 * t1, intervalPrecision);
  }
  function updateMinNiceFromMinT0Interval() {
    minNice = t0 ? round2(min3 + interval2 * t0, intervalPrecision) : min3;
  }
  function updateMaxNiceFromMaxT1Interval() {
    maxNice = t1 ? round2(max3 - interval2 * t1, intervalPrecision) : max3;
  }
  if (targetMinMaxFixed[0] && targetMinMaxFixed[1]) {
    min3 = targetExtent[0];
    max3 = targetExtent[1];
    interval2 = (max3 - min3) / (alignToNiceSegCount + t0 + t1);
    var axisPxExtent = targetAxis.getExtent();
    var pxSpan = mathAbs3(axisPxExtent[1] - axisPxExtent[0]);
    intervalPrecision = getAcceptableTickPrecision([max3, min3], pxSpan, 0.5 / alignToNiceSegCount);
    updateMinNiceFromMinT0Interval();
    updateMaxNiceFromMaxT1Interval();
    if (isNullableNumberFinite(intervalPrecision)) {
      interval2 = round2(interval2, intervalPrecision);
    }
  } else {
    var targetSpan = targetExtent[1] - targetExtent[0];
    interval2 = isTargetLogScale ? mathMax4(quantity(targetSpan), 1) : nice(targetSpan / alignToNiceSegCount, NICE_MODE_MIN);
    intervalPrecision = getIntervalPrecision(interval2);
    if (targetMinMaxFixed[0]) {
      min3 = targetExtent[0];
      loopIncreaseInterval(function() {
        updateMinNiceFromMinT0Interval();
        maxNice = round2(minNice + interval2 * alignToNiceSegCount, intervalPrecision);
        updateMaxFromMaxNice();
        if (max3 >= targetExtent[1]) {
          return true;
        }
      });
    } else if (targetMinMaxFixed[1]) {
      max3 = targetExtent[1];
      loopIncreaseInterval(function() {
        updateMaxNiceFromMaxT1Interval();
        minNice = round2(maxNice - interval2 * alignToNiceSegCount, intervalPrecision);
        updateMinFromMinNice();
        if (min3 <= targetExtent[0]) {
          return true;
        }
      });
    } else {
      loopIncreaseInterval(function() {
        minNice = round2(mathCeil(targetExtent[0] / interval2) * interval2, intervalPrecision);
        maxNice = round2(mathFloor(targetExtent[1] / interval2) * interval2, intervalPrecision);
        var currIntervalCount = mathRound2((maxNice - minNice) / interval2);
        if (currIntervalCount <= alignToNiceSegCount) {
          var moreCount = alignToNiceSegCount - currIntervalCount;
          var moreCountPair = void 0;
          var mayEnhanceZero = targetExtentInfo.incl0 || isTargetLogScale;
          if (mayEnhanceZero && targetExtent[0] === 0) {
            moreCountPair = [0, moreCount];
          } else if (mayEnhanceZero && targetExtent[1] === 0) {
            moreCountPair = [moreCount, 0];
          } else {
            var lessHalfCount = mathFloor(moreCount / 2);
            moreCountPair = moreCount % 2 === 0 ? [lessHalfCount, lessHalfCount] : min3 + max3 < targetExtent[0] + targetExtent[1] ? [lessHalfCount, lessHalfCount + 1] : [lessHalfCount + 1, lessHalfCount];
          }
          minNice = round2(minNice - interval2 * moreCountPair[0], intervalPrecision);
          maxNice = round2(maxNice + interval2 * moreCountPair[1], intervalPrecision);
          updateMinFromMinNice();
          updateMaxFromMaxNice();
          if (min3 <= targetExtent[0] && max3 >= targetExtent[1]) {
            return true;
          }
        }
      });
    }
  }
  updateIntervalOrLogScaleForNiceOrAligned(targetScale, targetMinMaxFixed, targetOldIntervalExtent, [min3, max3], targetOldOutermostExtent, {
    // NOTE: Even in LogScale, `interval` should not be in log space.
    interval: interval2,
    // Force ticks count, otherwise cumulative error may cause more unexpected ticks to be generated.
    // Though the overlapping tick labels may be auto-ignored, but probably unexpected, e.g., the min
    // tick label is ignored but the secondary min tick label is shown, which is unexpected when
    // `axis.min` is user-specified or dataZoom-specified.
    intervalCount: alignToNiceSegCount,
    intervalPrecision,
    niceExtent: [minNice, maxNice]
  });
  if (true) {
    targetScale.freeze();
  }
}

// node_modules/echarts/lib/coord/axisNiceTicks.js
function calcNiceForIntervalOrLogScale(scale3, opt) {
  var isTargetLogScale = isLogScale(scale3);
  var intervalStub = isTargetLogScale ? scale3.intervalStub : scale3;
  var fixMinMax = opt.fixMinMax || [];
  var oldOutermostExtent = isTargetLogScale ? scale3.getExtent() : null;
  var oldIntervalExtent = intervalStub.getExtent();
  var newIntervalExtent = intervalScaleEnsureValidExtent(oldIntervalExtent, fixMinMax, opt.rawExtentResult);
  intervalStub.setExtent(newIntervalExtent[0], newIntervalExtent[1]);
  newIntervalExtent = intervalStub.getExtent();
  var config = isTargetLogScale ? logScaleCalcNiceTicks(intervalStub, opt) : intervalScaleCalcNiceTicks(intervalStub, opt);
  var autoIntervalPrecision = config.intervalPrecision;
  var autoInterval = config.interval;
  var userInterval = opt.userInterval;
  if (userInterval != null) {
    config.interval = userInterval;
    config.intervalPrecision = getIntervalPrecision(userInterval);
  }
  if (!fixMinMax[0]) {
    newIntervalExtent[0] = round2(mathFloor(newIntervalExtent[0] / autoInterval) * autoInterval, autoIntervalPrecision);
  }
  if (!fixMinMax[1]) {
    newIntervalExtent[1] = round2(mathCeil(newIntervalExtent[1] / autoInterval) * autoInterval, autoIntervalPrecision);
  }
  if (userInterval != null) {
    config.niceExtent = newIntervalExtent.slice();
  }
  updateIntervalOrLogScaleForNiceOrAligned(scale3, fixMinMax, oldIntervalExtent, newIntervalExtent, oldOutermostExtent, config);
}
function intervalScaleCalcNiceTicks(scale3, opt) {
  var splitNumber = ensureValidSplitNumber(opt.splitNumber, 5);
  var span = getScaleLinearSpanEffective(scale3);
  if (true) {
    assert(isFinite(span) && span > 0);
  }
  var minInterval = opt.minInterval;
  var maxInterval = opt.maxInterval;
  var interval2 = nice(span / splitNumber, true);
  if (minInterval != null && interval2 < minInterval) {
    interval2 = minInterval;
  }
  if (maxInterval != null && interval2 > maxInterval) {
    interval2 = maxInterval;
  }
  var intervalPrecision = getIntervalPrecision(interval2);
  var extent = scale3.getExtent();
  var niceExtent = [round2(mathCeil(extent[0] / interval2) * interval2, intervalPrecision), round2(mathFloor(extent[1] / interval2) * interval2, intervalPrecision)];
  return {
    interval: interval2,
    intervalPrecision,
    niceExtent
  };
}
function logScaleCalcNiceTicks(intervalStub, opt) {
  var splitNumber = ensureValidSplitNumber(opt.splitNumber, 10);
  var intervalExtent = intervalStub.getExtent();
  var span = getScaleLinearSpanEffective(intervalStub);
  if (true) {
    assert(isFinite(span) && span > 0);
  }
  var interval2 = mathMax4(quantity(span), 1);
  var err = splitNumber / span * interval2;
  if (err <= 0.5) {
    interval2 *= 10;
  }
  var intervalPrecision = getIntervalPrecision(interval2);
  var niceExtent = [round2(mathCeil(intervalExtent[0] / interval2) * interval2, intervalPrecision), round2(mathFloor(intervalExtent[1] / interval2) * interval2, intervalPrecision)];
  return {
    intervalPrecision,
    interval: interval2,
    niceExtent
  };
}
function scaleCalcNice(axisLike) {
  var scale3 = axisLike.scale;
  var model2 = axisLike.model;
  var axis = model2.axis;
  var ecModel = model2.ecModel;
  if (true) {
    assert(axis && ecModel);
  }
  scaleCalcNice2(scale3, model2, axis, ecModel, null);
}
function scaleCalcNice2(scale3, model2, axis, ecModel, externalDataExtent) {
  var rawExtentResult = adoptScaleRawExtentInfoAndPrepare(scale3, model2, ecModel, axis, externalDataExtent);
  var isIntervalOrTime = isIntervalScale(scale3) || isTimeScale(scale3);
  scaleCalcNiceDirectly(scale3, {
    splitNumber: model2.get("splitNumber"),
    fixMinMax: rawExtentResult.fixMM,
    userInterval: model2.get("interval"),
    minInterval: isIntervalOrTime ? model2.get("minInterval") : null,
    maxInterval: isIntervalOrTime ? model2.get("maxInterval") : null,
    rawExtentResult
  });
  if (axis && ecModel) {
    adoptScaleExtentKindMapping(axis, scale3, rawExtentResult, ecModel);
  }
  if (true) {
    scale3.freeze();
  }
}
function scaleCalcNiceDirectly(scale3, opt) {
  scaleCalcNiceMethods[scale3.type](scale3, opt);
}
var scaleCalcNiceMethods = {
  interval: calcNiceForIntervalOrLogScale,
  log: calcNiceForIntervalOrLogScale,
  time: calcNiceForTimeScale,
  ordinal: noop
};

// node_modules/echarts/lib/coord/cartesian/Grid.js
var XY_TO_MARGIN_IDX = [
  [3, 1],
  [0, 2]
  // xyIdx 1 => 'y'
];
var Grid = (
  /** @class */
  (function() {
    function Grid2(gridModel, ecModel, api) {
      this.type = "grid";
      this._coordsMap = {};
      this._coordsList = [];
      this._axesMap = {};
      this._axesList = [];
      this.axisPointerEnabled = true;
      this.dimensions = cartesian2DDimensions;
      this._initCartesian(gridModel, ecModel, api);
      this.model = gridModel;
    }
    Grid2.prototype.getRect = function() {
      return this._rect;
    };
    Grid2.prototype.update = function(ecModel, api) {
      var axesMap = this._axesMap;
      each(this._axesList, function(axis) {
        scaleRawExtentInfoCreate(axis, AXIS_EXTENT_INFO_BUILD_FROM_COORD_SYS_UPDATE);
        var scale3 = axis.scale;
        if (isOrdinalScale(scale3)) {
          scale3.setSortInfo(axis.model.get("categorySortInfo"));
        }
      });
      function updateAxisTicks(axes) {
        var axesIndices = keys(axes);
        var axisNeedsAlign = [];
        for (var i2 = axesIndices.length - 1; i2 >= 0; i2--) {
          var axis = axes[+axesIndices[i2]];
          if (axis.__alignTo) {
            axisNeedsAlign.push(axis);
          } else {
            scaleCalcNice(axis);
          }
        }
        ;
        each(axisNeedsAlign, function(axis2) {
          if (incapableOfAlignNeedFallback(axis2, axis2.__alignTo)) {
            scaleCalcNice(axis2);
          } else {
            scaleCalcAlign(axis2, axis2.__alignTo.scale);
          }
        });
      }
      updateAxisTicks(axesMap.x);
      updateAxisTicks(axesMap.y);
      var onZeroRecords = {};
      each(axesMap.x, function(xAxis) {
        fixAxisOnZero(axesMap, "y", xAxis, onZeroRecords);
      });
      each(axesMap.y, function(yAxis) {
        fixAxisOnZero(axesMap, "x", yAxis, onZeroRecords);
      });
      this.resize(this.model, api);
    };
    Grid2.prototype.resize = function(gridModel, api, beforeDataProcessing) {
      var layoutRef = createBoxLayoutReference(gridModel, api);
      var gridRect = this._rect = getLayoutRect(gridModel.getBoxLayoutParams(), layoutRef.refContainer);
      var axesMap = this._axesMap;
      var coordsList = this._coordsList;
      var optionContainLabel = gridModel.get("containLabel");
      updateAllAxisExtentTransByGridRect(axesMap, gridRect);
      if (!beforeDataProcessing) {
        var axisBuilderSharedCtx = createAxisBiulders(gridRect, coordsList, axesMap, optionContainLabel, api);
        var noPxChange = void 0;
        if (optionContainLabel) {
          if (legacyLayOutGridByContainLabel) {
            legacyLayOutGridByContainLabel(this._axesList, gridRect);
            updateAllAxisExtentTransByGridRect(axesMap, gridRect);
          } else {
            if (true) {
              log("Specified `grid.containLabel` but no `use(LegacyGridContainLabel)`;use `grid.outerBounds` instead.", true);
            }
            noPxChange = layOutGridByOuterBounds(gridRect.clone(), "axisLabel", null, gridRect, axesMap, axisBuilderSharedCtx, layoutRef);
          }
        } else {
          var _a2 = prepareOuterBounds(gridModel, gridRect, layoutRef), outerBoundsRect = _a2.outerBoundsRect, parsedOuterBoundsContain = _a2.parsedOuterBoundsContain, outerBoundsClamp = _a2.outerBoundsClamp;
          if (outerBoundsRect) {
            noPxChange = layOutGridByOuterBounds(outerBoundsRect, parsedOuterBoundsContain, outerBoundsClamp, gridRect, axesMap, axisBuilderSharedCtx, layoutRef);
          }
        }
        createOrUpdateAxesView(gridRect, axesMap, AxisTickLabelComputingKind.determine, null, noPxChange, layoutRef);
        each(this._coordsList, function(coord) {
          coord.calcAffineTransform();
        });
      }
    };
    Grid2.prototype.getAxis = function(dim, axisIndex) {
      var axesMapOnDim = this._axesMap[dim];
      if (axesMapOnDim != null) {
        return axesMapOnDim[axisIndex || 0];
      }
    };
    Grid2.prototype.getAxes = function() {
      return this._axesList.slice();
    };
    Grid2.prototype.getCartesian = function(xAxisIndex, yAxisIndex) {
      if (xAxisIndex != null && yAxisIndex != null) {
        var key2 = "x" + xAxisIndex + "y" + yAxisIndex;
        return this._coordsMap[key2];
      }
      if (isObject(xAxisIndex)) {
        yAxisIndex = xAxisIndex.yAxisIndex;
        xAxisIndex = xAxisIndex.xAxisIndex;
      }
      for (var i2 = 0, coordList = this._coordsList; i2 < coordList.length; i2++) {
        if (coordList[i2].getAxis("x").index === xAxisIndex || coordList[i2].getAxis("y").index === yAxisIndex) {
          return coordList[i2];
        }
      }
    };
    Grid2.prototype.getCartesians = function() {
      return this._coordsList.slice();
    };
    Grid2.prototype.convertToPixel = function(ecModel, finder, value) {
      var target = this._findConvertTarget(finder);
      return target.cartesian ? target.cartesian.dataToPoint(value) : target.axis ? target.axis.toGlobalCoord(target.axis.dataToCoord(value)) : null;
    };
    Grid2.prototype.convertFromPixel = function(ecModel, finder, value) {
      var target = this._findConvertTarget(finder);
      return target.cartesian ? target.cartesian.pointToData(value) : target.axis ? target.axis.coordToData(target.axis.toLocalCoord(value)) : null;
    };
    Grid2.prototype._findConvertTarget = function(finder) {
      var seriesModel = finder.seriesModel;
      var xAxisModel = finder.xAxisModel || seriesModel && seriesModel.getReferringComponents("xAxis", SINGLE_REFERRING).models[0];
      var yAxisModel = finder.yAxisModel || seriesModel && seriesModel.getReferringComponents("yAxis", SINGLE_REFERRING).models[0];
      var gridModel = finder.gridModel;
      var coordsList = this._coordsList;
      var cartesian;
      var axis;
      if (seriesModel) {
        cartesian = seriesModel.coordinateSystem;
        indexOf(coordsList, cartesian) < 0 && (cartesian = null);
      } else if (xAxisModel && yAxisModel) {
        cartesian = this.getCartesian(xAxisModel.componentIndex, yAxisModel.componentIndex);
      } else if (xAxisModel) {
        axis = this.getAxis("x", xAxisModel.componentIndex);
      } else if (yAxisModel) {
        axis = this.getAxis("y", yAxisModel.componentIndex);
      } else if (gridModel) {
        var grid = gridModel.coordinateSystem;
        if (grid === this) {
          cartesian = this._coordsList[0];
        }
      }
      return {
        cartesian,
        axis
      };
    };
    Grid2.prototype.containPoint = function(point) {
      var coord = this._coordsList[0];
      if (coord) {
        return coord.containPoint(point);
      }
    };
    Grid2.prototype._initCartesian = function(gridModel, ecModel, api) {
      var _this = this;
      var grid = this;
      var axisPositionUsed = {
        left: false,
        right: false,
        top: false,
        bottom: false
      };
      var axesMap = {
        x: {},
        y: {}
      };
      var axesCount = {
        x: 0,
        y: 0
      };
      ecModel.eachComponent("xAxis", createAxisCreator("x"), this);
      ecModel.eachComponent("yAxis", createAxisCreator("y"), this);
      if (!axesCount.x || !axesCount.y) {
        this._axesMap = {};
        this._axesList = [];
        return;
      }
      this._axesMap = axesMap;
      each(axesMap.x, function(xAxis, xAxisIndex) {
        each(axesMap.y, function(yAxis, yAxisIndex) {
          var key2 = "x" + xAxisIndex + "y" + yAxisIndex;
          var cartesian = new Cartesian2D_default(key2);
          cartesian.master = _this;
          cartesian.model = gridModel;
          _this._coordsMap[key2] = cartesian;
          _this._coordsList.push(cartesian);
          cartesian.addAxis(xAxis);
          cartesian.addAxis(yAxis);
        });
      });
      prepareAlignToInCoordSysCreate(axesMap.x);
      prepareAlignToInCoordSysCreate(axesMap.y);
      function createAxisCreator(dimName) {
        return function(axisModel, idx) {
          if (!isAxisUsedInTheGrid(axisModel, gridModel)) {
            return;
          }
          var axisPosition = axisModel.get("position");
          if (dimName === "x") {
            if (axisPosition !== "top" && axisPosition !== "bottom") {
              axisPosition = axisPositionUsed.bottom ? "top" : "bottom";
            }
          } else {
            if (axisPosition !== "left" && axisPosition !== "right") {
              axisPosition = axisPositionUsed.left ? "right" : "left";
            }
          }
          axisPositionUsed[axisPosition] = true;
          var axisType = determineAxisType(axisModel);
          var axis = new Axis2D_default(dimName, createScaleByModel(axisModel, axisType, true), [0, 0], axisType, axisPosition);
          axis.onBand = isAxisOnBand(axis.scale, axisModel);
          axis.inverse = axisModel.get("inverse");
          axisModel.axis = axis;
          axis.model = axisModel;
          axis.grid = grid;
          axis.index = idx;
          grid._axesList.push(axis);
          axesMap[dimName][idx] = axis;
          axesCount[dimName]++;
        };
      }
    };
    Grid2.prototype.getTooltipAxes = function(dim) {
      var baseAxes = [];
      var otherAxes = [];
      each(this.getCartesians(), function(cartesian) {
        var baseAxis = dim != null && dim !== "auto" ? cartesian.getAxis(dim) : cartesian.getBaseAxis();
        var otherAxis = cartesian.getOtherAxis(baseAxis);
        indexOf(baseAxes, baseAxis) < 0 && baseAxes.push(baseAxis);
        indexOf(otherAxes, otherAxis) < 0 && otherAxes.push(otherAxis);
      });
      return {
        baseAxes,
        otherAxes
      };
    };
    Grid2.create = function(ecModel, api) {
      var grids = [];
      ecModel.eachComponent("grid", function(gridModel, idx) {
        var grid = new Grid2(gridModel, ecModel, api);
        grid.name = "grid_" + idx;
        grid.resize(gridModel, api, true);
        gridModel.coordinateSystem = grid;
        grids.push(grid);
        each(grid._axesList, function(axis) {
          scaleRawExtentInfoEnableBoxCoordSysUsage(axis, Grid2.dimIdxMap);
        });
      });
      ecModel.eachSeries(function(seriesModel) {
        var xAxis;
        var yAxis;
        injectCoordSysByOption({
          targetModel: seriesModel,
          coordSysType: COORD_SYS_TYPE_CARTESIAN_2D,
          coordSysProvider
        });
        function coordSysProvider() {
          var axesModelMap = findAxisModels(seriesModel);
          var xAxisModel = axesModelMap.xAxisModel;
          var yAxisModel = axesModelMap.yAxisModel;
          xAxis = xAxisModel.axis;
          yAxis = yAxisModel.axis;
          var gridModel = xAxisModel.getCoordSysModel();
          if (true) {
            if (!gridModel) {
              throw new Error('Grid "' + retrieve3(xAxisModel.get("gridIndex"), xAxisModel.get("gridId"), 0) + '" not found');
            }
            if (xAxisModel.getCoordSysModel() !== yAxisModel.getCoordSysModel()) {
              throw new Error("xAxis and yAxis must use the same grid");
            }
          }
          var grid = gridModel.coordinateSystem;
          return grid.getCartesian(xAxisModel.componentIndex, yAxisModel.componentIndex);
        }
        if (xAxis && yAxis) {
          associateSeriesWithAxis(xAxis, seriesModel, COORD_SYS_TYPE_CARTESIAN_2D);
          associateSeriesWithAxis(yAxis, seriesModel, COORD_SYS_TYPE_CARTESIAN_2D);
        }
      }, this);
      return grids;
    };
    Grid2.dimensions = cartesian2DDimensions;
    Grid2.dimIdxMap = createDimNameMap(cartesian2DDimensions);
    return Grid2;
  })()
);
function isAxisUsedInTheGrid(axisModel, gridModel) {
  return axisModel.getCoordSysModel() === gridModel;
}
function fixAxisOnZero(axesMap, otherAxisDim, axis, onZeroRecords) {
  axis.getAxesOnZeroOf = function() {
    return otherAxisOnZeroOf ? [otherAxisOnZeroOf] : [];
  };
  var otherAxes = axesMap[otherAxisDim];
  var otherAxisOnZeroOf;
  var axisModel = axis.model;
  var onZero = axisModel.get(["axisLine", "onZero"]);
  var onZeroAxisIndex = axisModel.get(["axisLine", "onZeroAxisIndex"]);
  if (!onZero) {
    return;
  }
  if (onZeroAxisIndex != null) {
    if (canOnZeroToAxis(onZero, otherAxes[onZeroAxisIndex])) {
      otherAxisOnZeroOf = otherAxes[onZeroAxisIndex];
    }
  } else {
    for (var idx in otherAxes) {
      if (hasOwn(otherAxes, idx) && canOnZeroToAxis(onZero, otherAxes[idx]) && !onZeroRecords[getOnZeroRecordKey(otherAxes[idx])]) {
        otherAxisOnZeroOf = otherAxes[idx];
        break;
      }
    }
  }
  if (otherAxisOnZeroOf) {
    onZeroRecords[getOnZeroRecordKey(otherAxisOnZeroOf)] = true;
  }
  function getOnZeroRecordKey(axis2) {
    return axis2.dim + "_" + axis2.index;
  }
}
function canOnZeroToAxis(onZeroOption, axis) {
  if (!axis) {
    return false;
  }
  var scale3 = axis.scale;
  var kindEffective = getScaleValuePositionKind(scale3, 0, false);
  var can = axis && axis.type !== "category" && axis.type !== "time" && kindEffective !== SCALE_VALUE_POSITION_KIND_OUTSIDE;
  if (can && onZeroOption === "auto" && isOnAxisZeroDiscouraged(axis)) {
    can = false;
  }
  return can;
}
function prepareAlignToInCoordSysCreate(axes) {
  var axesIndices = keys(axes);
  var alignTo;
  var axisNeedsAlign = [];
  for (var i2 = axesIndices.length - 1; i2 >= 0; i2--) {
    var axis = axes[+axesIndices[i2]];
    if (isIntervalOrLogScale(axis.scale) && retrieveAxisBreaksOption(axis.model, axis.type, true) == null) {
      if (axis.model.get("alignTicks") && axis.model.get("interval") == null) {
        axisNeedsAlign.push(axis);
      } else {
        alignTo = axis;
      }
    }
  }
  ;
  if (!alignTo) {
    alignTo = axisNeedsAlign.pop();
  }
  if (alignTo) {
    each(axisNeedsAlign, function(axis2) {
      axis2.__alignTo = alignTo;
    });
  }
}
function incapableOfAlignNeedFallback(targetAxis, alignTo) {
  return hasBreaks(targetAxis.scale) || hasBreaks(alignTo.scale) || alignTo.scale.getTicks().length < 2;
}
function updateAxisTransform(axis, coordBase) {
  var axisExtent = axis.getExtent();
  var axisExtentSum = axisExtent[0] + axisExtent[1];
  axis.toGlobalCoord = axis.dim === "x" ? function(coord) {
    return coord + coordBase;
  } : function(coord) {
    return axisExtentSum - coord + coordBase;
  };
  axis.toLocalCoord = axis.dim === "x" ? function(coord) {
    return coord - coordBase;
  } : function(coord) {
    return axisExtentSum - coord + coordBase;
  };
}
function updateAllAxisExtentTransByGridRect(axesMap, gridRect) {
  each(axesMap.x, function(axis) {
    return updateAxisExtentTransByGridRect(axis, gridRect.x, gridRect.width);
  });
  each(axesMap.y, function(axis) {
    return updateAxisExtentTransByGridRect(axis, gridRect.y, gridRect.height);
  });
}
function updateAxisExtentTransByGridRect(axis, gridXY, gridWH) {
  var extent = [0, gridWH];
  var idx = axis.inverse ? 1 : 0;
  axis.setExtent(extent[idx], extent[1 - idx]);
  updateAxisTransform(axis, gridXY);
}
var legacyLayOutGridByContainLabel;
function layOutGridByOuterBounds(outerBoundsRect, outerBoundsContain, outerBoundsClamp, gridRect, axesMap, axisBuilderSharedCtx, layoutRef) {
  if (true) {
    assert(outerBoundsContain === "all" || outerBoundsContain === "axisLabel");
  }
  createOrUpdateAxesView(gridRect, axesMap, AxisTickLabelComputingKind.estimate, outerBoundsContain, false, layoutRef);
  var margin = [0, 0, 0, 0];
  fillLabelNameOverflowOnOneDimension(0);
  fillLabelNameOverflowOnOneDimension(1);
  fillMarginOnOneDimension(gridRect, 0, NaN);
  fillMarginOnOneDimension(gridRect, 1, NaN);
  var noPxChange = find(margin, function(item) {
    return item > 0;
  }) == null;
  expandOrShrinkRect(gridRect, margin, true, true, outerBoundsClamp);
  updateAllAxisExtentTransByGridRect(axesMap, gridRect);
  return noPxChange;
  function fillLabelNameOverflowOnOneDimension(xyIdx) {
    each(axesMap[XY2[xyIdx]], function(axis) {
      if (!shouldAxisShow(axis.model)) {
        return;
      }
      var sharedRecord = axisBuilderSharedCtx.ensureRecord(axis.model);
      var labelInfoList = sharedRecord.labelInfoList;
      if (labelInfoList) {
        for (var idx = 0; idx < labelInfoList.length; idx++) {
          var labelInfo = labelInfoList[idx];
          var proportion = axis.scale.normalize(getTickValueOutermost(axis.scale, getLabelInner(labelInfo.label).labelInfo.tick));
          proportion = xyIdx === 1 ? 1 - proportion : proportion;
          fillMarginOnOneDimension(labelInfo.rect, xyIdx, proportion);
          fillMarginOnOneDimension(labelInfo.rect, 1 - xyIdx, NaN);
        }
      }
      var nameLayout = sharedRecord.nameLayout;
      if (nameLayout) {
        var proportion = isNameLocationCenter(sharedRecord.nameLocation) ? 0.5 : NaN;
        fillMarginOnOneDimension(nameLayout.rect, xyIdx, proportion);
        fillMarginOnOneDimension(nameLayout.rect, 1 - xyIdx, NaN);
      }
    });
  }
  function fillMarginOnOneDimension(itemRect, xyIdx, proportion) {
    var overflow1 = outerBoundsRect[XY2[xyIdx]] - itemRect[XY2[xyIdx]];
    var overflow2 = itemRect[WH2[xyIdx]] + itemRect[XY2[xyIdx]] - (outerBoundsRect[WH2[xyIdx]] + outerBoundsRect[XY2[xyIdx]]);
    overflow1 = applyProportion(overflow1, 1 - proportion);
    overflow2 = applyProportion(overflow2, proportion);
    var minIdx = XY_TO_MARGIN_IDX[xyIdx][0];
    var maxIdx = XY_TO_MARGIN_IDX[xyIdx][1];
    margin[minIdx] = mathMax4(margin[minIdx], overflow1);
    margin[maxIdx] = mathMax4(margin[maxIdx], overflow2);
  }
  function applyProportion(overflow, proportion) {
    if (overflow > 0 && !eqNaN(proportion) && proportion > 1e-4) {
      overflow /= proportion;
    }
    return overflow;
  }
}
function createAxisBiulders(gridRect, cartesians, axesMap, optionContainLabel, api) {
  var axisBuilderSharedCtx = new AxisBuilderSharedContext(resolveAxisNameOverlapForGrid);
  each(axesMap, function(axisList) {
    return each(axisList, function(axis) {
      if (shouldAxisShow(axis.model)) {
        var defaultNameMoveOverlap = !optionContainLabel;
        axis.axisBuilder = createCartesianAxisViewCommonPartBuilder(gridRect, cartesians, axis.model, api, axisBuilderSharedCtx, defaultNameMoveOverlap);
      }
    });
  });
  return axisBuilderSharedCtx;
}
function createOrUpdateAxesView(gridRect, axesMap, kind, outerBoundsContain, noPxChange, layoutRef) {
  var isDetermine = kind === AxisTickLabelComputingKind.determine;
  each(axesMap, function(axisList) {
    return each(axisList, function(axis) {
      if (shouldAxisShow(axis.model)) {
        updateCartesianAxisViewCommonPartBuilder(axis.axisBuilder, gridRect, axis.model);
        axis.axisBuilder.build(isDetermine ? {
          axisTickLabelDetermine: true
        } : {
          axisTickLabelEstimate: true
        }, {
          noPxChange
        });
      }
    });
  });
  var nameMarginLevelMap = {
    x: 0,
    y: 0
  };
  calcNameMarginLevel(0);
  calcNameMarginLevel(1);
  function calcNameMarginLevel(xyIdx) {
    nameMarginLevelMap[XY2[1 - xyIdx]] = gridRect[WH2[xyIdx]] <= layoutRef.refContainer[WH2[xyIdx]] * 0.5 ? 0 : 1 - xyIdx === 1 ? 2 : 1;
  }
  each(axesMap, function(axisList, xy) {
    return each(axisList, function(axis) {
      if (shouldAxisShow(axis.model)) {
        if (outerBoundsContain === "all" || isDetermine) {
          axis.axisBuilder.build({
            axisName: true
          }, {
            nameMarginLevel: nameMarginLevelMap[xy]
          });
        }
        if (isDetermine) {
          axis.axisBuilder.build({
            axisLine: true
          });
        }
      }
    });
  });
}
function prepareOuterBounds(gridModel, rawGridRect, layoutRef) {
  var outerBoundsRect;
  var optionOuterBoundsMode = gridModel.get("outerBoundsMode", true);
  if (optionOuterBoundsMode === "same") {
    outerBoundsRect = rawGridRect.clone();
  } else if (optionOuterBoundsMode == null || optionOuterBoundsMode === "auto") {
    outerBoundsRect = getLayoutRect(gridModel.get("outerBounds", true) || OUTER_BOUNDS_DEFAULT, layoutRef.refContainer);
  } else if (optionOuterBoundsMode !== "none") {
    if (true) {
      error("Invalid grid[" + gridModel.componentIndex + "].outerBoundsMode.");
    }
  }
  var optionOuterBoundsContain = gridModel.get("outerBoundsContain", true);
  var parsedOuterBoundsContain;
  if (optionOuterBoundsContain == null || optionOuterBoundsContain === "auto") {
    parsedOuterBoundsContain = "all";
  } else if (indexOf(["all", "axisLabel"], optionOuterBoundsContain) < 0) {
    if (true) {
      error("Invalid grid[" + gridModel.componentIndex + "].outerBoundsContain.");
    }
    parsedOuterBoundsContain = "all";
  } else {
    parsedOuterBoundsContain = optionOuterBoundsContain;
  }
  var outerBoundsClamp = [parsePositionSizeOption(retrieve2(gridModel.get("outerBoundsClampWidth", true), OUTER_BOUNDS_CLAMP_DEFAULT[0]), rawGridRect.width), parsePositionSizeOption(retrieve2(gridModel.get("outerBoundsClampHeight", true), OUTER_BOUNDS_CLAMP_DEFAULT[1]), rawGridRect.height)];
  return {
    outerBoundsRect,
    parsedOuterBoundsContain,
    outerBoundsClamp
  };
}
var resolveAxisNameOverlapForGrid = function(cfg, ctx, axisModel, nameLayoutInfo, nameMoveDirVec, thisRecord) {
  var perpendicularDim = axisModel.axis.dim === "x" ? "y" : "x";
  resolveAxisNameOverlapDefault(cfg, ctx, axisModel, nameLayoutInfo, nameMoveDirVec, thisRecord);
  if (!isNameLocationCenter(cfg.nameLocation)) {
    each(ctx.recordMap[perpendicularDim], function(perpenRecord) {
      if (perpenRecord && perpenRecord.labelInfoList && perpenRecord.dirVec) {
        moveIfOverlapByLinearLabels(perpenRecord.labelInfoList, perpenRecord.dirVec, nameLayoutInfo, nameMoveDirVec);
      }
    });
  }
};
var Grid_default = Grid;

// node_modules/echarts/lib/component/axisPointer/modelHelper.js
function collect(ecModel, api) {
  var result = {
    /**
     * key: makeKey(axis.model)
     * value: {
     *      axis,
     *      coordSys,
     *      axisPointerModel,
     *      triggerTooltip,
     *      triggerEmphasis,
     *      involveSeries,
     *      snap,
     *      seriesModels,
     *      seriesDataCount
     * }
     */
    axesInfo: {},
    seriesInvolved: false,
    /**
     * key: makeKey(coordSys.model)
     * value: Object: key makeKey(axis.model), value: axisInfo
     */
    coordSysAxesInfo: {},
    coordSysMap: {}
  };
  collectAxesInfo(result, ecModel, api);
  result.seriesInvolved && collectSeriesInfo(result, ecModel);
  return result;
}
function collectAxesInfo(result, ecModel, api) {
  var globalTooltipModel = ecModel.getComponent("tooltip");
  var globalAxisPointerModel = ecModel.getComponent("axisPointer");
  var linksOption = globalAxisPointerModel.get("link", true) || [];
  var linkGroups = [];
  each(api.getCoordinateSystems(), function(coordSys) {
    if (!coordSys.axisPointerEnabled) {
      return;
    }
    var coordSysKey = makeKey(coordSys.model);
    var axesInfoInCoordSys = result.coordSysAxesInfo[coordSysKey] = {};
    result.coordSysMap[coordSysKey] = coordSys;
    var coordSysModel = coordSys.model;
    var baseTooltipModel = coordSysModel.getModel("tooltip", globalTooltipModel);
    each(coordSys.getAxes(), curry(saveTooltipAxisInfo, false, null));
    if (coordSys.getTooltipAxes && globalTooltipModel && baseTooltipModel.get("show")) {
      var triggerAxis = baseTooltipModel.get("trigger") === "axis";
      var cross = baseTooltipModel.get(["axisPointer", "type"]) === "cross";
      var tooltipAxes = coordSys.getTooltipAxes(baseTooltipModel.get(["axisPointer", "axis"]));
      if (triggerAxis || cross) {
        each(tooltipAxes.baseAxes, curry(saveTooltipAxisInfo, cross ? "cross" : true, triggerAxis));
      }
      if (cross) {
        each(tooltipAxes.otherAxes, curry(saveTooltipAxisInfo, "cross", false));
      }
    }
    function saveTooltipAxisInfo(fromTooltip, triggerTooltip, axis) {
      var axisPointerModel = axis.model.getModel("axisPointer", globalAxisPointerModel);
      var axisPointerShow = axisPointerModel.get("show");
      if (!axisPointerShow || axisPointerShow === "auto" && !fromTooltip && !isHandleTrigger(axisPointerModel)) {
        return;
      }
      if (triggerTooltip == null) {
        triggerTooltip = axisPointerModel.get("triggerTooltip");
      }
      axisPointerModel = fromTooltip ? makeAxisPointerModel(axis, baseTooltipModel, globalAxisPointerModel, ecModel, fromTooltip, triggerTooltip) : axisPointerModel;
      var snap = axisPointerModel.get("snap");
      var triggerEmphasis = axisPointerModel.get("triggerEmphasis");
      var axisKey = makeKey(axis.model);
      var involveSeries = triggerTooltip || snap || axis.type === "category";
      var axisInfo = result.axesInfo[axisKey] = {
        key: axisKey,
        axis,
        coordSys,
        axisPointerModel,
        triggerTooltip,
        triggerEmphasis,
        involveSeries,
        snap,
        useHandle: isHandleTrigger(axisPointerModel),
        seriesModels: [],
        linkGroup: null
      };
      axesInfoInCoordSys[axisKey] = axisInfo;
      result.seriesInvolved = result.seriesInvolved || involveSeries;
      var groupIndex = getLinkGroupIndex(linksOption, axis);
      if (groupIndex != null) {
        var linkGroup = linkGroups[groupIndex] || (linkGroups[groupIndex] = {
          axesInfo: {}
        });
        linkGroup.axesInfo[axisKey] = axisInfo;
        linkGroup.mapper = linksOption[groupIndex].mapper;
        axisInfo.linkGroup = linkGroup;
      }
    }
  });
}
function makeAxisPointerModel(axis, baseTooltipModel, globalAxisPointerModel, ecModel, fromTooltip, triggerTooltip) {
  var tooltipAxisPointerModel = baseTooltipModel.getModel("axisPointer");
  var fields = ["type", "snap", "lineStyle", "shadowStyle", "label", "animation", "animationDurationUpdate", "animationEasingUpdate", "z"];
  var volatileOption = {};
  each(fields, function(field) {
    volatileOption[field] = clone(tooltipAxisPointerModel.get(field));
  });
  volatileOption.snap = axis.type !== "category" && !!triggerTooltip;
  if (tooltipAxisPointerModel.get("type") === "cross") {
    volatileOption.type = "line";
  }
  var labelOption = volatileOption.label || (volatileOption.label = {});
  labelOption.show == null && (labelOption.show = false);
  if (fromTooltip === "cross") {
    var tooltipAxisPointerLabelShow = tooltipAxisPointerModel.get(["label", "show"]);
    labelOption.show = tooltipAxisPointerLabelShow != null ? tooltipAxisPointerLabelShow : true;
    if (!triggerTooltip) {
      var crossStyle = volatileOption.lineStyle = tooltipAxisPointerModel.get("crossStyle");
      crossStyle && defaults(labelOption, crossStyle.textStyle);
    }
  }
  return axis.model.getModel("axisPointer", new Model_default(volatileOption, globalAxisPointerModel, ecModel));
}
function collectSeriesInfo(result, ecModel) {
  ecModel.eachSeries(function(seriesModel) {
    var coordSys = seriesModel.coordinateSystem;
    var seriesTooltipTrigger = seriesModel.get(["tooltip", "trigger"], true);
    var seriesTooltipShow = seriesModel.get(["tooltip", "show"], true);
    if (!coordSys || !coordSys.model || seriesTooltipTrigger === "none" || seriesTooltipTrigger === false || seriesTooltipTrigger === "item" || seriesTooltipShow === false || seriesModel.get(["axisPointer", "show"], true) === false) {
      return;
    }
    each(result.coordSysAxesInfo[makeKey(coordSys.model)], function(axisInfo) {
      var axis = axisInfo.axis;
      if (coordSys.getAxis(axis.dim) === axis) {
        axisInfo.seriesModels.push(seriesModel);
        axisInfo.seriesDataCount == null && (axisInfo.seriesDataCount = 0);
        axisInfo.seriesDataCount += seriesModel.getData().count();
      }
    });
  });
}
function getLinkGroupIndex(linksOption, axis) {
  var axisModel = axis.model;
  var dim = axis.dim;
  for (var i2 = 0; i2 < linksOption.length; i2++) {
    var linkOption = linksOption[i2] || {};
    if (checkPropInLink(linkOption[dim + "AxisId"], axisModel.id) || checkPropInLink(linkOption[dim + "AxisIndex"], axisModel.componentIndex) || checkPropInLink(linkOption[dim + "AxisName"], axisModel.name)) {
      return i2;
    }
  }
}
function checkPropInLink(linkPropValue, axisPropValue) {
  return linkPropValue === "all" || isArray(linkPropValue) && indexOf(linkPropValue, axisPropValue) >= 0 || linkPropValue === axisPropValue;
}
function fixValue(axisModel) {
  var axisInfo = getAxisInfo(axisModel);
  if (!axisInfo) {
    return;
  }
  var axisPointerModel = axisInfo.axisPointerModel;
  var scale3 = axisInfo.axis.scale;
  var option = axisPointerModel.option;
  var status = axisPointerModel.get("status");
  var value = axisPointerModel.get("value");
  if (value != null) {
    value = scale3.parse(value);
  }
  var useHandle = isHandleTrigger(axisPointerModel);
  if (status == null) {
    option.status = useHandle ? "show" : "hide";
  }
  var extent = scale3.getExtent();
  if (
    // Pick a value on axis when initializing.
    value == null || value > extent[1]
  ) {
    value = extent[1];
  }
  if (value < extent[0]) {
    value = extent[0];
  }
  option.value = value;
  if (useHandle) {
    option.status = axisInfo.axis.scale.isBlank() ? "hide" : "show";
  }
}
function getAxisInfo(axisModel) {
  var coordSysAxesInfo = (axisModel.ecModel.getComponent("axisPointer") || {}).coordSysAxesInfo;
  return coordSysAxesInfo && coordSysAxesInfo.axesInfo[makeKey(axisModel)];
}
function getAxisPointerModel(axisModel) {
  var axisInfo = getAxisInfo(axisModel);
  return axisInfo && axisInfo.axisPointerModel;
}
function isHandleTrigger(axisPointerModel) {
  return !!axisPointerModel.get(["handle", "show"]);
}
function makeKey(model2) {
  return model2.type + "||" + model2.id;
}

// node_modules/echarts/lib/component/axis/AxisView.js
var axisPointerClazz = {};
var AxisView = (
  /** @class */
  (function(_super) {
    __extends(AxisView2, _super);
    function AxisView2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = AxisView2.type;
      return _this;
    }
    AxisView2.prototype.render = function(axisModel, ecModel, api, payload) {
      this.axisPointerClass && fixValue(axisModel);
      _super.prototype.render.apply(this, arguments);
      this._doUpdateAxisPointerClass(axisModel, api, true);
    };
    AxisView2.prototype.updateAxisPointer = function(axisModel, ecModel, api, payload) {
      this._doUpdateAxisPointerClass(axisModel, api, false);
    };
    AxisView2.prototype.remove = function(ecModel, api) {
      var axisPointer = this._axisPointer;
      axisPointer && axisPointer.remove(api);
    };
    AxisView2.prototype.dispose = function(ecModel, api) {
      this._disposeAxisPointer(api);
      _super.prototype.dispose.apply(this, arguments);
    };
    AxisView2.prototype._doUpdateAxisPointerClass = function(axisModel, api, forceRender) {
      var Clazz = AxisView2.getAxisPointerClass(this.axisPointerClass);
      if (!Clazz) {
        return;
      }
      var axisPointerModel = getAxisPointerModel(axisModel);
      axisPointerModel ? (this._axisPointer || (this._axisPointer = new Clazz())).render(axisModel, axisPointerModel, api, forceRender) : this._disposeAxisPointer(api);
    };
    AxisView2.prototype._disposeAxisPointer = function(api) {
      this._axisPointer && this._axisPointer.dispose(api);
      this._axisPointer = null;
    };
    AxisView2.registerAxisPointerClass = function(type, clazz) {
      if (true) {
        if (axisPointerClazz[type]) {
          throw new Error("axisPointer " + type + " exists");
        }
      }
      axisPointerClazz[type] = clazz;
    };
    ;
    AxisView2.getAxisPointerClass = function(type) {
      return type && axisPointerClazz[type];
    };
    ;
    AxisView2.type = "axis";
    return AxisView2;
  })(Component_default2)
);
var AxisView_default = AxisView;

// node_modules/echarts/lib/component/axis/axisSplitHelper.js
var inner6 = makeInner();
function rectCoordAxisBuildSplitArea(axisView, axisGroup, axisModel, gridModel) {
  var axis = axisModel.axis;
  if (axis.scale.isBlank()) {
    return;
  }
  var splitAreaModel = axisModel.getModel("splitArea");
  var areaStyleModel = splitAreaModel.getModel("areaStyle");
  var areaColors = areaStyleModel.get("color");
  var gridRect = gridModel.coordinateSystem.getRect();
  var ticksCoords = axis.getTicksCoords({
    tickModel: splitAreaModel,
    breakTicks: "none",
    pruneByBreak: "preserve_extent_bound"
  });
  if (!ticksCoords.length) {
    return;
  }
  var areaColorsLen = areaColors.length;
  var lastSplitAreaColors = inner6(axisView).splitAreaColors;
  var newSplitAreaColors = createHashMap();
  var colorIndex = 0;
  if (lastSplitAreaColors) {
    for (var i2 = 0; i2 < ticksCoords.length; i2++) {
      var cIndex = lastSplitAreaColors.get(ticksCoords[i2].tickValue);
      if (cIndex != null) {
        colorIndex = (cIndex + (areaColorsLen - 1) * i2) % areaColorsLen;
        break;
      }
    }
  }
  var prev = axis.toGlobalCoord(ticksCoords[0].coord);
  var areaStyle = areaStyleModel.getAreaStyle();
  areaColors = isArray(areaColors) ? areaColors : [areaColors];
  for (var i2 = 1; i2 < ticksCoords.length; i2++) {
    var tickCoord = axis.toGlobalCoord(ticksCoords[i2].coord);
    var x = void 0;
    var y = void 0;
    var width = void 0;
    var height = void 0;
    if (axis.isHorizontal()) {
      x = prev;
      y = gridRect.y;
      width = tickCoord - x;
      height = gridRect.height;
      prev = x + width;
    } else {
      x = gridRect.x;
      y = prev;
      width = gridRect.width;
      height = tickCoord - y;
      prev = y + height;
    }
    var tickValue = ticksCoords[i2 - 1].tickValue;
    tickValue != null && newSplitAreaColors.set(tickValue, colorIndex);
    axisGroup.add(new Rect_default({
      anid: tickValue != null ? "area_" + tickValue : null,
      shape: {
        x,
        y,
        width,
        height
      },
      style: defaults({
        fill: areaColors[colorIndex]
      }, areaStyle),
      autoBatch: true,
      silent: true
    }));
    colorIndex = (colorIndex + 1) % areaColorsLen;
  }
  inner6(axisView).splitAreaColors = newSplitAreaColors;
}
function rectCoordAxisHandleRemove(axisView) {
  inner6(axisView).splitAreaColors = null;
}

// node_modules/echarts/lib/component/axis/CartesianAxisView.js
var selfBuilderAttrs = ["splitArea", "splitLine", "minorSplitLine", "breakArea"];
var CartesianAxisView = (
  /** @class */
  (function(_super) {
    __extends(CartesianAxisView2, _super);
    function CartesianAxisView2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = CartesianAxisView2.type;
      _this.axisPointerClass = "CartesianAxisPointer";
      return _this;
    }
    CartesianAxisView2.prototype.render = function(axisModel, ecModel, api, payload) {
      this.group.removeAll();
      var oldAxisGroup = this._axisGroup;
      this._axisGroup = new Group_default();
      this.group.add(this._axisGroup);
      if (!shouldAxisShow(axisModel)) {
        return;
      }
      this._axisGroup.add(axisModel.axis.axisBuilder.group);
      each(selfBuilderAttrs, function(name) {
        if (axisModel.get([name, "show"])) {
          axisElementBuilders[name](this, this._axisGroup, axisModel, axisModel.getCoordSysModel(), api);
        }
      }, this);
      var isInitialSortFromBarRacing = payload && payload.type === "changeAxisOrder" && payload.isInitSort;
      if (!isInitialSortFromBarRacing) {
        groupTransition(oldAxisGroup, this._axisGroup, axisModel);
      }
      _super.prototype.render.call(this, axisModel, ecModel, api, payload);
    };
    CartesianAxisView2.prototype.remove = function() {
      rectCoordAxisHandleRemove(this);
    };
    CartesianAxisView2.type = "cartesianAxis";
    return CartesianAxisView2;
  })(AxisView_default)
);
var axisElementBuilders = {
  splitLine: function(axisView, axisGroup, axisModel, gridModel, api) {
    var axis = axisModel.axis;
    if (axis.scale.isBlank()) {
      return;
    }
    var splitLineModel = axisModel.getModel("splitLine");
    var lineStyleModel = splitLineModel.getModel("lineStyle");
    var lineColors = lineStyleModel.get("color");
    var showMinLine = splitLineModel.get("showMinLine") !== false;
    var showMaxLine = splitLineModel.get("showMaxLine") !== false;
    lineColors = isArray(lineColors) ? lineColors : [lineColors];
    var gridRect = gridModel.coordinateSystem.getRect();
    var isHorizontal = axis.isHorizontal();
    var lineCount = 0;
    var ticksCoords = axis.getTicksCoords({
      tickModel: splitLineModel,
      breakTicks: "none",
      pruneByBreak: "preserve_extent_bound"
    });
    var p1 = [];
    var p2 = [];
    var lineStyle = lineStyleModel.getLineStyle();
    for (var i2 = 0; i2 < ticksCoords.length; i2++) {
      var tickCoord = axis.toGlobalCoord(ticksCoords[i2].coord);
      if (i2 === 0 && !showMinLine || i2 === ticksCoords.length - 1 && !showMaxLine) {
        continue;
      }
      var tickValue = ticksCoords[i2].tickValue;
      if (isHorizontal) {
        p1[0] = tickCoord;
        p1[1] = gridRect.y;
        p2[0] = tickCoord;
        p2[1] = gridRect.y + gridRect.height;
      } else {
        p1[0] = gridRect.x;
        p1[1] = tickCoord;
        p2[0] = gridRect.x + gridRect.width;
        p2[1] = tickCoord;
      }
      var colorIndex = lineCount++ % lineColors.length;
      var line = new Line_default({
        anid: tickValue != null ? "line_" + tickValue : null,
        autoBatch: true,
        shape: {
          x1: p1[0],
          y1: p1[1],
          x2: p2[0],
          y2: p2[1]
        },
        style: defaults({
          stroke: lineColors[colorIndex]
        }, lineStyle),
        silent: true
      });
      subPixelOptimizeLine2(line.shape, lineStyle.lineWidth);
      axisGroup.add(line);
    }
  },
  minorSplitLine: function(axisView, axisGroup, axisModel, gridModel, api) {
    var axis = axisModel.axis;
    var minorSplitLineModel = axisModel.getModel("minorSplitLine");
    var lineStyleModel = minorSplitLineModel.getModel("lineStyle");
    var gridRect = gridModel.coordinateSystem.getRect();
    var isHorizontal = axis.isHorizontal();
    var minorTicksCoords = axis.getMinorTicksCoords();
    if (!minorTicksCoords.length) {
      return;
    }
    var p1 = [];
    var p2 = [];
    var lineStyle = lineStyleModel.getLineStyle();
    for (var i2 = 0; i2 < minorTicksCoords.length; i2++) {
      for (var k2 = 0; k2 < minorTicksCoords[i2].length; k2++) {
        var tickCoord = axis.toGlobalCoord(minorTicksCoords[i2][k2].coord);
        if (isHorizontal) {
          p1[0] = tickCoord;
          p1[1] = gridRect.y;
          p2[0] = tickCoord;
          p2[1] = gridRect.y + gridRect.height;
        } else {
          p1[0] = gridRect.x;
          p1[1] = tickCoord;
          p2[0] = gridRect.x + gridRect.width;
          p2[1] = tickCoord;
        }
        var line = new Line_default({
          anid: "minor_line_" + minorTicksCoords[i2][k2].tickValue,
          autoBatch: true,
          shape: {
            x1: p1[0],
            y1: p1[1],
            x2: p2[0],
            y2: p2[1]
          },
          style: lineStyle,
          silent: true
        });
        subPixelOptimizeLine2(line.shape, lineStyle.lineWidth);
        axisGroup.add(line);
      }
    }
  },
  splitArea: function(axisView, axisGroup, axisModel, gridModel, api) {
    rectCoordAxisBuildSplitArea(axisView, axisGroup, axisModel, gridModel);
  },
  breakArea: function(axisView, axisGroup, axisModel, gridModel, api) {
    var axisBreakHelper = getAxisBreakHelper();
    var scale3 = axisModel.axis.scale;
    if (axisBreakHelper && scale3.type !== "ordinal") {
      axisBreakHelper.rectCoordBuildBreakAxis(axisGroup, axisView, axisModel, gridModel.coordinateSystem.getRect(), api);
    }
  }
};
var CartesianXAxisView = (
  /** @class */
  (function(_super) {
    __extends(CartesianXAxisView2, _super);
    function CartesianXAxisView2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = CartesianXAxisView2.type;
      return _this;
    }
    CartesianXAxisView2.type = "xAxis";
    return CartesianXAxisView2;
  })(CartesianAxisView)
);
var CartesianYAxisView = (
  /** @class */
  (function(_super) {
    __extends(CartesianYAxisView2, _super);
    function CartesianYAxisView2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = CartesianXAxisView.type;
      return _this;
    }
    CartesianYAxisView2.type = "yAxis";
    return CartesianYAxisView2;
  })(CartesianAxisView)
);

// node_modules/echarts/lib/component/grid/installSimple.js
var GridView = (
  /** @class */
  (function(_super) {
    __extends(GridView2, _super);
    function GridView2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = "grid";
      return _this;
    }
    GridView2.prototype.render = function(gridModel, ecModel) {
      this.group.removeAll();
      if (gridModel.get("show")) {
        this.group.add(new Rect_default({
          shape: gridModel.coordinateSystem.getRect(),
          style: defaults({
            fill: gridModel.get("backgroundColor")
          }, gridModel.getItemStyle()),
          silent: true,
          z2: -1
        }));
      }
    };
    GridView2.type = "grid";
    return GridView2;
  })(Component_default2)
);
var extraOption = {
  // gridIndex: 0,
  // gridId: '',
  offset: 0
};
function install4(registers) {
  registers.registerComponentView(GridView);
  registers.registerComponentModel(GridModel_default);
  registers.registerCoordinateSystem("cartesian2d", Grid_default);
  axisModelCreator(registers, "x", CartesianAxisModel, extraOption);
  axisModelCreator(registers, "y", CartesianAxisModel, extraOption);
  registers.registerComponentView(CartesianXAxisView);
  registers.registerComponentView(CartesianYAxisView);
  registers.registerPreprocessor(function(option) {
    if (option.xAxis && option.yAxis && !option.grid) {
      option.grid = {};
    }
  });
}

// node_modules/echarts/lib/coord/single/AxisModel.js
var COORD_SYS_TYPE_SINGLE = "single";
var COMPONENT_TYPE_SINGLE_AXIS = "singleAxis";
var SingleAxisModel = (
  /** @class */
  (function(_super) {
    __extends(SingleAxisModel2, _super);
    function SingleAxisModel2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = SingleAxisModel2.type;
      return _this;
    }
    SingleAxisModel2.prototype.getCoordSysModel = function() {
      return this;
    };
    SingleAxisModel2.type = COMPONENT_TYPE_SINGLE_AXIS;
    SingleAxisModel2.layoutMode = "box";
    SingleAxisModel2.defaultOption = {
      left: "5%",
      top: "5%",
      right: "5%",
      bottom: "5%",
      type: "value",
      position: "bottom",
      orient: "horizontal",
      axisLine: {
        show: true,
        lineStyle: {
          width: 1,
          type: "solid"
        }
      },
      // Single coordinate system and single axis is the,
      // which is used as the parent tooltip model.
      // same model, so we set default tooltip show as true.
      tooltip: {
        show: true
      },
      axisTick: {
        show: true,
        length: 6,
        lineStyle: {
          width: 1
        }
      },
      axisLabel: {
        show: true,
        interval: "auto"
      },
      splitLine: {
        show: true,
        lineStyle: {
          type: "dashed",
          opacity: 0.2
        }
      },
      jitter: 0,
      jitterOverlap: true,
      jitterMargin: 2
    };
    return SingleAxisModel2;
  })(Component_default)
);
mixin(SingleAxisModel, AxisModelCommonMixin.prototype);

// node_modules/echarts/lib/util/jitter.js
var inner7 = makeInner();

// node_modules/echarts/lib/chart/radar/backwardCompat.js
function radarBackwardCompat(option) {
  var polarOptArr = option.polar;
  if (polarOptArr) {
    if (!isArray(polarOptArr)) {
      polarOptArr = [polarOptArr];
    }
    var polarNotRadar_1 = [];
    each(polarOptArr, function(polarOpt, idx) {
      if (polarOpt.indicator) {
        if (polarOpt.type && !polarOpt.shape) {
          polarOpt.shape = polarOpt.type;
        }
        option.radar = option.radar || [];
        if (!isArray(option.radar)) {
          option.radar = [option.radar];
        }
        option.radar.push(polarOpt);
      } else {
        polarNotRadar_1.push(polarOpt);
      }
    });
    option.polar = polarNotRadar_1;
  }
  each(option.series, function(seriesOpt) {
    if (seriesOpt && seriesOpt.type === "radar" && seriesOpt.polarIndex) {
      seriesOpt.radarIndex = seriesOpt.polarIndex;
    }
  });
}

// node_modules/echarts/lib/chart/radar/RadarSeries.js
var SERIES_TYPE_RADAR = "radar";
var RadarSeriesModel = (
  /** @class */
  (function(_super) {
    __extends(RadarSeriesModel2, _super);
    function RadarSeriesModel2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = RadarSeriesModel2.type;
      _this.hasSymbolVisual = true;
      return _this;
    }
    RadarSeriesModel2.prototype.init = function(option) {
      _super.prototype.init.apply(this, arguments);
      this.legendVisualProvider = new LegendVisualProvider_default(bind(this.getData, this), bind(this.getRawData, this));
    };
    RadarSeriesModel2.prototype.getInitialData = function(option, ecModel) {
      return createSeriesDataSimply(this, {
        generateCoord: "indicator_",
        generateCoordCount: Infinity
      });
    };
    RadarSeriesModel2.prototype.formatTooltip = function(dataIndex, multipleSeries, dataType) {
      var data = this.getData();
      var coordSys = this.coordinateSystem;
      var indicatorAxes = coordSys.getIndicatorAxes();
      var name = this.getData().getName(dataIndex);
      var nameToDisplay = name === "" ? this.name : name;
      var markerColor = retrieveVisualColorForTooltipMarker(this, dataIndex);
      return createTooltipMarkup("section", {
        header: nameToDisplay,
        sortBlocks: true,
        blocks: map2(indicatorAxes, function(axis) {
          var val = data.get(data.mapDimension(axis.dim), dataIndex);
          return createTooltipMarkup("nameValue", {
            markerType: "subItem",
            markerColor,
            name: axis.name,
            value: val,
            sortParam: val
          });
        })
      });
    };
    RadarSeriesModel2.prototype.getTooltipPosition = function(dataIndex) {
      if (dataIndex != null) {
        var data_1 = this.getData();
        var coordSys = this.coordinateSystem;
        var values = data_1.getValues(map2(coordSys.dimensions, function(dim) {
          return data_1.mapDimension(dim);
        }), dataIndex);
        for (var i2 = 0, len2 = values.length; i2 < len2; i2++) {
          if (!isNaN(values[i2])) {
            var indicatorAxes = coordSys.getIndicatorAxes();
            return coordSys.coordToPoint(indicatorAxes[i2].dataToCoord(values[i2]), i2);
          }
        }
      }
    };
    RadarSeriesModel2.type = "series." + SERIES_TYPE_RADAR;
    RadarSeriesModel2.dependencies = ["radar"];
    RadarSeriesModel2.defaultOption = {
      // zlevel: 0,
      z: 2,
      colorBy: "data",
      coordinateSystem: "radar",
      legendHoverLink: true,
      radarIndex: 0,
      lineStyle: {
        width: 2,
        type: "solid",
        join: "round"
      },
      label: {
        position: "top"
      },
      // areaStyle: {
      // },
      // itemStyle: {}
      symbolSize: 8
      // symbolRotate: null
    };
    return RadarSeriesModel2;
  })(Series_default)
);
var RadarSeries_default = RadarSeriesModel;

// node_modules/echarts/lib/chart/radar/RadarView.js
var RadarView = (
  /** @class */
  (function(_super) {
    __extends(RadarView3, _super);
    function RadarView3() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = SERIES_TYPE_RADAR;
      return _this;
    }
    RadarView3.prototype.render = function(seriesModel, ecModel, api) {
      var polar = seriesModel.coordinateSystem;
      var group2 = this.group;
      var data = seriesModel.getData();
      var oldData = this._data;
      function createSymbol3(data2, idx) {
        var symbolType = data2.getItemVisual(idx, "symbol") || "circle";
        if (symbolType === "none") {
          return;
        }
        var symbolSize = normalizeSymbolSize(data2.getItemVisual(idx, "symbolSize"));
        var symbolPath = createSymbol(symbolType, -1, -1, 2, 2);
        var symbolRotate = data2.getItemVisual(idx, "symbolRotate") || 0;
        symbolPath.attr({
          style: {
            strokeNoScale: true
          },
          z2: 100,
          scaleX: symbolSize[0] / 2,
          scaleY: symbolSize[1] / 2,
          rotation: symbolRotate * Math.PI / 180 || 0
        });
        return symbolPath;
      }
      function updateSymbols(oldPoints, newPoints, symbolGroup, data2, idx, isInit) {
        symbolGroup.removeAll();
        for (var i2 = 0; i2 < newPoints.length - 1; i2++) {
          var symbolPath = createSymbol3(data2, idx);
          if (symbolPath) {
            symbolPath.__dimIdx = i2;
            if (oldPoints[i2]) {
              symbolPath.setPosition(oldPoints[i2]);
              graphic_exports[isInit ? "initProps" : "updateProps"](symbolPath, {
                x: newPoints[i2][0],
                y: newPoints[i2][1]
              }, seriesModel, idx);
            } else {
              symbolPath.setPosition(newPoints[i2]);
            }
            symbolGroup.add(symbolPath);
          }
        }
      }
      function getInitialPoints(points4) {
        return map2(points4, function(pt) {
          return [polar.cx, polar.cy];
        });
      }
      data.diff(oldData).add(function(idx) {
        var points4 = data.getItemLayout(idx);
        if (!points4) {
          return;
        }
        var polygon = new Polygon_default();
        var polyline = new Polyline_default();
        var target = {
          shape: {
            points: points4
          }
        };
        polygon.shape.points = getInitialPoints(points4);
        polyline.shape.points = getInitialPoints(points4);
        initProps(polygon, target, seriesModel, idx);
        initProps(polyline, target, seriesModel, idx);
        var itemGroup = new Group_default();
        var symbolGroup = new Group_default();
        itemGroup.add(polyline);
        itemGroup.add(polygon);
        itemGroup.add(symbolGroup);
        updateSymbols(polyline.shape.points, points4, symbolGroup, data, idx, true);
        data.setItemGraphicEl(idx, itemGroup);
      }).update(function(newIdx, oldIdx) {
        var itemGroup = oldData.getItemGraphicEl(oldIdx);
        var polyline = itemGroup.childAt(0);
        var polygon = itemGroup.childAt(1);
        var symbolGroup = itemGroup.childAt(2);
        var target = {
          shape: {
            points: data.getItemLayout(newIdx)
          }
        };
        if (!target.shape.points) {
          return;
        }
        updateSymbols(polyline.shape.points, target.shape.points, symbolGroup, data, newIdx, false);
        saveOldStyle(polygon);
        saveOldStyle(polyline);
        updateProps(polyline, target, seriesModel);
        updateProps(polygon, target, seriesModel);
        data.setItemGraphicEl(newIdx, itemGroup);
      }).remove(function(idx) {
        group2.remove(oldData.getItemGraphicEl(idx));
      }).execute();
      data.eachItemGraphicEl(function(itemGroup, idx) {
        var itemModel = data.getItemModel(idx);
        var polyline = itemGroup.childAt(0);
        var polygon = itemGroup.childAt(1);
        var symbolGroup = itemGroup.childAt(2);
        var itemStyle = data.getItemVisual(idx, "style");
        var color3 = itemStyle.fill;
        group2.add(itemGroup);
        polyline.useStyle(defaults(itemModel.getModel("lineStyle").getLineStyle(), {
          fill: "none",
          stroke: color3
        }));
        setStatesStylesFromModel(polyline, itemModel, "lineStyle");
        setStatesStylesFromModel(polygon, itemModel, "areaStyle");
        var areaStyleModel = itemModel.getModel("areaStyle");
        var polygonIgnore = areaStyleModel.isEmpty() && areaStyleModel.parentModel.isEmpty();
        polygon.ignore = polygonIgnore;
        each(["emphasis", "select", "blur"], function(stateName) {
          var stateModel = itemModel.getModel([stateName, "areaStyle"]);
          var stateIgnore = stateModel.isEmpty() && stateModel.parentModel.isEmpty();
          polygon.ensureState(stateName).ignore = stateIgnore && polygonIgnore;
          var lineStyle = itemModel.getModel([stateName, "lineStyle"]).getLineStyle();
          polyline.ensureState(stateName).style = lineStyle;
          var areaStyle = stateModel.getAreaStyle();
          polygon.ensureState(stateName).style = areaStyle;
          var itemStateStyle = itemModel.getModel([stateName, "itemStyle"]).getItemStyle();
          symbolGroup.eachChild(function(symbolPath) {
            symbolPath.ensureState(stateName).style = clone(itemStateStyle);
          });
        });
        polygon.useStyle(defaults(itemModel.getModel("areaStyle").getAreaStyle(), {
          fill: color3,
          opacity: 0.7,
          decal: itemStyle.decal
        }));
        var emphasisModel = itemModel.getModel("emphasis");
        symbolGroup.eachChild(function(symbolPath) {
          if (symbolPath instanceof Image_default) {
            var pathStyle = symbolPath.style;
            symbolPath.useStyle(extend({
              // TODO other properties like x, y ?
              image: pathStyle.image,
              x: pathStyle.x,
              y: pathStyle.y,
              width: pathStyle.width,
              height: pathStyle.height
            }, itemStyle));
          } else {
            symbolPath.useStyle(itemStyle);
            symbolPath.setColor(color3);
            symbolPath.style.strokeNoScale = true;
          }
          var defaultText = data.getStore().get(data.getDimensionIndex(symbolPath.__dimIdx), idx);
          (defaultText == null || isNaN(defaultText)) && (defaultText = "");
          setLabelStyle(symbolPath, getLabelStatesModels(itemModel), {
            labelFetcher: data.hostModel,
            labelDataIndex: idx,
            labelDimIndex: symbolPath.__dimIdx,
            defaultText,
            inheritColor: color3,
            defaultOpacity: itemStyle.opacity
          });
        });
        toggleHoverEmphasis(itemGroup, emphasisModel.get("focus"), emphasisModel.get("blurScope"), emphasisModel.get("disabled"));
      });
      this._data = data;
    };
    RadarView3.prototype.remove = function() {
      this.group.removeAll();
      this._data = null;
    };
    RadarView3.type = SERIES_TYPE_RADAR;
    return RadarView3;
  })(Chart_default)
);
var RadarView_default = RadarView;

// node_modules/echarts/lib/coord/radar/RadarModel.js
var valueAxisDefault = axisDefault_default.value;
var COORD_SYS_TYPE_RADAR = "radar";
var COMPONENT_TYPE_RADAR = COORD_SYS_TYPE_RADAR;
var SERIES_TYPE_RADAR2 = COORD_SYS_TYPE_RADAR;
var RADAR_DEFAULT_SPLIT_NUMBER = 5;
function defaultsShow(opt, show) {
  return defaults({
    show
  }, opt);
}
var RadarModel = (
  /** @class */
  (function(_super) {
    __extends(RadarModel2, _super);
    function RadarModel2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = RadarModel2.type;
      return _this;
    }
    RadarModel2.prototype.optionUpdated = function() {
      var boundaryGap = this.get("boundaryGap");
      var splitNumber = this.get("splitNumber");
      var clockwise = this.get("clockwise");
      var scale3 = this.get("scale");
      var axisLine = this.get("axisLine");
      var axisTick = this.get("axisTick");
      var axisLabel = this.get("axisLabel");
      var nameTextStyle = this.get("axisName");
      var showName = this.get(["axisName", "show"]);
      var nameFormatter = this.get(["axisName", "formatter"]);
      var nameGap = this.get("axisNameGap");
      var triggerEvent = this.get("triggerEvent");
      var indicatorModels = map2(this.get("indicator") || [], function(indicatorOpt) {
        if (indicatorOpt.max != null && indicatorOpt.max > 0 && !indicatorOpt.min) {
          indicatorOpt.min = 0;
        } else if (indicatorOpt.min != null && indicatorOpt.min < 0 && !indicatorOpt.max) {
          indicatorOpt.max = 0;
        }
        var iNameTextStyle = nameTextStyle;
        if (indicatorOpt.color != null) {
          iNameTextStyle = defaults({
            color: indicatorOpt.color
          }, nameTextStyle);
        }
        var innerIndicatorOpt = merge2(clone(indicatorOpt), {
          boundaryGap,
          splitNumber,
          clockwise,
          scale: scale3,
          axisLine,
          axisTick,
          // axisType: axisType,
          axisLabel,
          // Compatible with 2 and use text
          name: indicatorOpt.text,
          showName,
          nameLocation: "end",
          nameGap,
          // min: 0,
          nameTextStyle: iNameTextStyle,
          triggerEvent
        }, false);
        if (isString(nameFormatter)) {
          var indName = innerIndicatorOpt.name;
          innerIndicatorOpt.name = nameFormatter.replace("{value}", indName != null ? indName : "");
        } else if (isFunction(nameFormatter)) {
          innerIndicatorOpt.name = nameFormatter(innerIndicatorOpt.name, innerIndicatorOpt);
        }
        var model2 = new Model_default(innerIndicatorOpt, null, this.ecModel);
        mixin(model2, AxisModelCommonMixin.prototype);
        model2.mainType = "radar";
        model2.componentIndex = this.componentIndex;
        model2.uid = getUID("ec_radar");
        return model2;
      }, this);
      this._indicatorModels = indicatorModels;
    };
    RadarModel2.prototype.getIndicatorModels = function() {
      return this._indicatorModels;
    };
    RadarModel2.type = COMPONENT_TYPE_RADAR;
    RadarModel2.defaultOption = {
      // zlevel: 0,
      z: 0,
      center: ["50%", "50%"],
      radius: "50%",
      startAngle: 90,
      clockwise: false,
      axisName: {
        show: true,
        color: tokens_default.color.axisLabel
        // formatter: null
        // textStyle: {}
      },
      boundaryGap: [0, 0],
      splitNumber: RADAR_DEFAULT_SPLIT_NUMBER,
      axisNameGap: 15,
      scale: false,
      // Polygon or circle
      shape: "polygon",
      axisLine: merge2({
        lineStyle: {
          color: tokens_default.color.neutral20
        }
      }, valueAxisDefault.axisLine),
      axisLabel: defaultsShow(valueAxisDefault.axisLabel, false),
      axisTick: defaultsShow(valueAxisDefault.axisTick, false),
      // axisType: 'value',
      splitLine: defaultsShow(valueAxisDefault.splitLine, true),
      splitArea: defaultsShow(valueAxisDefault.splitArea, true),
      // {text, min, max}
      indicator: []
    };
    return RadarModel2;
  })(Component_default)
);
var RadarModel_default = RadarModel;

// node_modules/echarts/lib/component/radar/RadarView.js
var RadarView2 = (
  /** @class */
  (function(_super) {
    __extends(RadarView3, _super);
    function RadarView3() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = RadarView3.type;
      return _this;
    }
    RadarView3.prototype.render = function(radarModel, ecModel, api) {
      var group2 = this.group;
      group2.removeAll();
      this._buildAxes(radarModel, api);
      this._buildSplitLineAndArea(radarModel);
    };
    RadarView3.prototype._buildAxes = function(radarModel, api) {
      var radar = radarModel.coordinateSystem;
      var indicatorAxes = radar.getIndicatorAxes();
      var axisBuilders = map2(indicatorAxes, function(indicatorAxis) {
        var axisName = indicatorAxis.model.get("showName") ? indicatorAxis.name : "";
        var axisBuilder = new AxisBuilder_default(indicatorAxis.model, api, {
          axisName,
          position: [radar.cx, radar.cy],
          rotation: indicatorAxis.angle,
          labelDirection: -1,
          tickDirection: -1,
          nameDirection: 1
        });
        return axisBuilder;
      });
      each(axisBuilders, function(axisBuilder) {
        axisBuilder.build();
        this.group.add(axisBuilder.group);
      }, this);
    };
    RadarView3.prototype._buildSplitLineAndArea = function(radarModel) {
      var radar = radarModel.coordinateSystem;
      var indicatorAxes = radar.getIndicatorAxes();
      if (!indicatorAxes.length) {
        return;
      }
      var shape = radarModel.get("shape");
      var splitLineModel = radarModel.getModel("splitLine");
      var splitAreaModel = radarModel.getModel("splitArea");
      var lineStyleModel = splitLineModel.getModel("lineStyle");
      var areaStyleModel = splitAreaModel.getModel("areaStyle");
      var showSplitLine = splitLineModel.get("show");
      var showSplitArea = splitAreaModel.get("show");
      var splitLineColors = lineStyleModel.get("color");
      var splitAreaColors = areaStyleModel.get("color");
      var splitLineColorsArr = isArray(splitLineColors) ? splitLineColors : [splitLineColors];
      var splitAreaColorsArr = isArray(splitAreaColors) ? splitAreaColors : [splitAreaColors];
      var splitLines = [];
      var splitAreas = [];
      function getColorIndex(areaOrLine, areaOrLineColorList, idx) {
        var colorIndex2 = idx % areaOrLineColorList.length;
        areaOrLine[colorIndex2] = areaOrLine[colorIndex2] || [];
        return colorIndex2;
      }
      if (shape === "circle") {
        var ticksRadius = indicatorAxes[0].getTicksCoords();
        var cx = radar.cx;
        var cy = radar.cy;
        for (var i2 = 0; i2 < ticksRadius.length; i2++) {
          if (showSplitLine) {
            var colorIndex = getColorIndex(splitLines, splitLineColorsArr, i2);
            splitLines[colorIndex].push(new Circle_default({
              shape: {
                cx,
                cy,
                r: ticksRadius[i2].coord
              }
            }));
          }
          if (showSplitArea && i2 < ticksRadius.length - 1) {
            var colorIndex = getColorIndex(splitAreas, splitAreaColorsArr, i2);
            splitAreas[colorIndex].push(new Ring_default({
              shape: {
                cx,
                cy,
                r0: ticksRadius[i2].coord,
                r: ticksRadius[i2 + 1].coord
              }
            }));
          }
        }
      } else {
        var realSplitNumber_1;
        var axesTicksPoints = map2(indicatorAxes, function(indicatorAxis, idx) {
          var ticksCoords = indicatorAxis.getTicksCoords();
          realSplitNumber_1 = realSplitNumber_1 == null ? ticksCoords.length - 1 : Math.min(ticksCoords.length - 1, realSplitNumber_1);
          return map2(ticksCoords, function(tickCoord) {
            return radar.coordToPoint(tickCoord.coord, idx);
          });
        });
        var prevPoints = [];
        for (var i2 = 0; i2 <= realSplitNumber_1; i2++) {
          var points4 = [];
          for (var j = 0; j < indicatorAxes.length; j++) {
            points4.push(axesTicksPoints[j][i2]);
          }
          if (points4[0]) {
            points4.push(points4[0].slice());
          } else {
            if (true) {
              console.error("Can't draw value axis " + i2);
            }
          }
          if (showSplitLine) {
            var colorIndex = getColorIndex(splitLines, splitLineColorsArr, i2);
            splitLines[colorIndex].push(new Polyline_default({
              shape: {
                points: points4
              }
            }));
          }
          if (showSplitArea && prevPoints) {
            var colorIndex = getColorIndex(splitAreas, splitAreaColorsArr, i2 - 1);
            splitAreas[colorIndex].push(new Polygon_default({
              shape: {
                points: points4.concat(prevPoints)
              }
            }));
          }
          prevPoints = points4.slice().reverse();
        }
      }
      var lineStyle = lineStyleModel.getLineStyle();
      var areaStyle = areaStyleModel.getAreaStyle();
      each(splitAreas, function(splitAreas2, idx) {
        this.group.add(mergePath2(splitAreas2, {
          style: defaults({
            stroke: "none",
            fill: splitAreaColorsArr[idx % splitAreaColorsArr.length]
          }, areaStyle),
          silent: true
        }));
      }, this);
      each(splitLines, function(splitLines2, idx) {
        this.group.add(mergePath2(splitLines2, {
          style: defaults({
            fill: "none",
            stroke: splitLineColorsArr[idx % splitLineColorsArr.length]
          }, lineStyle),
          silent: true
        }));
      }, this);
    };
    RadarView3.type = "radar";
    return RadarView3;
  })(Component_default2)
);
var RadarView_default2 = RadarView2;

// node_modules/echarts/lib/coord/radar/IndicatorAxis.js
var IndicatorAxis = (
  /** @class */
  (function(_super) {
    __extends(IndicatorAxis2, _super);
    function IndicatorAxis2(dim, scale3, radiusExtent) {
      var _this = _super.call(this, dim, scale3, radiusExtent) || this;
      _this.type = "value";
      _this.angle = 0;
      _this.name = "";
      return _this;
    }
    return IndicatorAxis2;
  })(Axis_default)
);
var IndicatorAxis_default = IndicatorAxis;

// node_modules/echarts/lib/coord/radar/Radar.js
var Radar = (
  /** @class */
  (function() {
    function Radar2(radarModel, ecModel, api) {
      this.type = COORD_SYS_TYPE_RADAR;
      this.dimensions = [];
      this._model = radarModel;
      this._indicatorAxes = map2(radarModel.getIndicatorModels(), function(indicatorModel, idx) {
        var dim = "indicator_" + idx;
        var indicatorAxis = new IndicatorAxis_default(
          dim,
          new Interval_default()
          // (indicatorModel.get('axisType') === 'log') ? new LogScale() : new IntervalScale()
        );
        indicatorAxis.name = indicatorModel.get("name");
        indicatorAxis.model = indicatorModel;
        indicatorModel.axis = indicatorAxis;
        this.dimensions.push(dim);
        return indicatorAxis;
      }, this);
      this.resize(radarModel, api);
    }
    Radar2.prototype.getIndicatorAxes = function() {
      return this._indicatorAxes;
    };
    Radar2.prototype.dataToPoint = function(value, indicatorIndex) {
      var indicatorAxis = this._indicatorAxes[indicatorIndex];
      return this.coordToPoint(indicatorAxis.dataToCoord(value), indicatorIndex);
    };
    Radar2.prototype.coordToPoint = function(coord, indicatorIndex) {
      var indicatorAxis = this._indicatorAxes[indicatorIndex];
      var angle = indicatorAxis.angle;
      var x = this.cx + coord * Math.cos(angle);
      var y = this.cy - coord * Math.sin(angle);
      return [x, y];
    };
    Radar2.prototype.pointToData = function(pt) {
      var dx = pt[0] - this.cx;
      var dy = pt[1] - this.cy;
      var radius = Math.sqrt(dx * dx + dy * dy);
      dx /= radius;
      dy /= radius;
      var radian = Math.atan2(-dy, dx);
      var minRadianDiff = Infinity;
      var closestAxis;
      var closestAxisIdx = -1;
      for (var i2 = 0; i2 < this._indicatorAxes.length; i2++) {
        var indicatorAxis = this._indicatorAxes[i2];
        var diff = Math.abs(radian - indicatorAxis.angle);
        if (diff < minRadianDiff) {
          closestAxis = indicatorAxis;
          closestAxisIdx = i2;
          minRadianDiff = diff;
        }
      }
      return [closestAxisIdx, +(closestAxis && closestAxis.coordToData(radius))];
    };
    Radar2.prototype.resize = function(radarModel, api) {
      var refContainer = createBoxLayoutReference(radarModel, api).refContainer;
      var center3 = radarModel.get("center");
      var clockwise = radarModel.get("clockwise") || false;
      var viewSize = Math.min(refContainer.width, refContainer.height) / 2;
      this.cx = parsePercent2(center3[0], refContainer.width) + refContainer.x;
      this.cy = parsePercent2(center3[1], refContainer.height) + refContainer.y;
      this.startAngle = radarModel.get("startAngle") * Math.PI / 180;
      var radius = radarModel.get("radius");
      if (isString(radius) || isNumber(radius)) {
        radius = [0, radius];
      }
      this.r0 = parsePercent2(radius[0], viewSize);
      this.r = parsePercent2(radius[1], viewSize);
      var sign = clockwise ? -1 : 1;
      each(this._indicatorAxes, function(indicatorAxis, idx) {
        indicatorAxis.setExtent(this.r0, this.r);
        var angle = this.startAngle + sign * idx * Math.PI * 2 / this._indicatorAxes.length;
        angle = Math.atan2(Math.sin(angle), Math.cos(angle));
        indicatorAxis.angle = angle;
      }, this);
    };
    Radar2.prototype.update = function(ecModel, api) {
      var indicatorAxes = this._indicatorAxes;
      var radarModel = this._model;
      var splitNumber = ensureValidSplitNumber(radarModel.get("splitNumber"), RADAR_DEFAULT_SPLIT_NUMBER);
      var dummyScale = new Interval_default();
      dummyScale.setExtent(0, splitNumber);
      dummyScale.setConfig({
        interval: 1
      });
      each(indicatorAxes, function(indicatorAxis) {
        scaleRawExtentInfoCreate(indicatorAxis, AXIS_EXTENT_INFO_BUILD_FROM_COORD_SYS_UPDATE);
        scaleCalcAlign(indicatorAxis, dummyScale);
      });
    };
    Radar2.prototype.convertToPixel = function(ecModel, finder, value) {
      console.warn("Not implemented.");
      return null;
    };
    Radar2.prototype.convertFromPixel = function(ecModel, finder, pixel) {
      console.warn("Not implemented.");
      return null;
    };
    Radar2.prototype.containPoint = function(point) {
      console.warn("Not implemented.");
      return false;
    };
    Radar2.create = function(ecModel, api) {
      var radarList = [];
      ecModel.eachComponent(COMPONENT_TYPE_RADAR, function(radarModel) {
        var radar = new Radar2(radarModel, ecModel, api);
        radarList.push(radar);
        radarModel.coordinateSystem = radar;
      });
      ecModel.eachSeriesByType(SERIES_TYPE_RADAR2, function(radarSeries) {
        if (radarSeries.get("coordinateSystem") === COORD_SYS_TYPE_RADAR) {
          var radar = radarSeries.coordinateSystem = radarList[radarSeries.get("radarIndex") || 0];
          if (radar) {
            each(radar.getIndicatorAxes(), function(indicatorAxis) {
              associateSeriesWithAxis(indicatorAxis, radarSeries, COORD_SYS_TYPE_RADAR);
            });
          }
        }
      });
      return radarList;
    };
    Radar2.dimensions = [];
    return Radar2;
  })()
);
var Radar_default = Radar;

// node_modules/echarts/lib/component/radar/install.js
function install5(registers) {
  registers.registerCoordinateSystem("radar", Radar_default);
  registers.registerComponentModel(RadarModel_default);
  registers.registerComponentView(RadarView_default2);
  registers.registerVisual({
    seriesType: "radar",
    reset: function(seriesModel) {
      var data = seriesModel.getData();
      data.each(function(idx) {
        data.setItemVisual(idx, "legendIcon", "roundRect");
      });
      data.setVisual("legendIcon", "roundRect");
    }
  });
}

// node_modules/echarts/lib/chart/radar/radarLayout.js
var radarLayoutStageHandler = createSimpleOverallStageHandler(SERIES_TYPE_RADAR, radarLayout);
function radarLayout(ecModel) {
  ecModel.eachSeriesByType(SERIES_TYPE_RADAR, function(seriesModel) {
    var data = seriesModel.getData();
    var points4 = [];
    var coordSys = seriesModel.coordinateSystem;
    if (!coordSys) {
      return;
    }
    var axes = coordSys.getIndicatorAxes();
    each(axes, function(axis, axisIndex) {
      data.each(data.mapDimension(axes[axisIndex].dim), function(val, dataIndex) {
        points4[dataIndex] = points4[dataIndex] || [];
        var point = coordSys.dataToPoint(val, axisIndex);
        points4[dataIndex][axisIndex] = isValidPoint(point) ? point : getValueMissingPoint(coordSys);
      });
    });
    data.each(function(idx) {
      var firstPoint = find(points4[idx], function(point) {
        return isValidPoint(point);
      }) || getValueMissingPoint(coordSys);
      points4[idx].push(firstPoint.slice());
      data.setItemLayout(idx, points4[idx]);
    });
  });
}
function isValidPoint(point) {
  return !isNaN(point[0]) && !isNaN(point[1]);
}
function getValueMissingPoint(coordSys) {
  return [coordSys.cx, coordSys.cy];
}

// node_modules/echarts/lib/chart/radar/install.js
function install6(registers) {
  use(install5);
  registers.registerChartView(RadarView_default);
  registers.registerSeriesModel(RadarSeries_default);
  registers.registerLayout(radarLayoutStageHandler);
  registers.registerProcessor(dataFilter("radar"));
  registers.registerPreprocessor(radarBackwardCompat);
}

// node_modules/echarts/lib/component/helper/interactionMutex.js
var inner8 = makeInner();
function take2(zr, resourceKey, userKey) {
  inner8(zr)[resourceKey] = userKey;
}
function release(zr, resourceKey, userKey) {
  var store = inner8(zr);
  var uKey = store[resourceKey];
  if (uKey === userKey) {
    store[resourceKey] = null;
  }
}
function isTaken(zr, resourceKey) {
  return !!inner8(zr)[resourceKey];
}
registerAction({
  type: "takeGlobalCursor",
  event: "globalCursorTaken",
  update: "update"
}, noop);

// node_modules/echarts/lib/component/helper/cursorHelper.js
var IRRELEVANT_EXCLUDES = {
  "axisPointer": 1,
  "tooltip": 1,
  "brush": 1
};
function onIrrelevantElement(e2, api, targetComponent) {
  var eventElComponent = api.getComponentByElement(e2.topTarget);
  if (!eventElComponent || eventElComponent === targetComponent || IRRELEVANT_EXCLUDES.hasOwnProperty(eventElComponent.mainType)) {
    return false;
  }
  var eventElCoordSys = eventElComponent.coordinateSystem;
  if (!eventElCoordSys || eventElCoordSys.model === targetComponent) {
    return false;
  }
  var eventElCmptZInfo = retrieveZInfo(eventElComponent);
  var targetCmptZInfo = retrieveZInfo(targetComponent);
  if ((eventElCmptZInfo.zlevel - targetCmptZInfo.zlevel || eventElCmptZInfo.z - targetCmptZInfo.z) <= 0) {
    return false;
  }
  return true;
}

// node_modules/echarts/lib/component/helper/RoamController.js
var RoamController = (
  /** @class */
  (function(_super) {
    __extends(RoamController2, _super);
    function RoamController2(zr) {
      var _this = _super.call(this) || this;
      _this._zr = zr;
      var mousedownHandler = bind(_this._mousedownHandler, _this);
      var mousemoveHandler = bind(_this._mousemoveHandler, _this);
      var mouseupHandler = bind(_this._mouseupHandler, _this);
      var mousewheelHandler = bind(_this._mousewheelHandler, _this);
      var pinchHandler = bind(_this._pinchHandler, _this);
      _this.enable = function(controlType, rawOpt) {
        var zInfo = rawOpt.zInfo;
        var _a2 = retrieveZInfo(zInfo.component), z = _a2.z, zlevel = _a2.zlevel;
        var zInfoParsed = {
          component: zInfo.component,
          z,
          zlevel,
          // By default roam controller is the lowest z2 comparing to other elememts in a component.
          z2: retrieve2(zInfo.z2, -Infinity)
        };
        var triggerInfo = extend({}, rawOpt.triggerInfo);
        this._opt = defaults(extend({}, rawOpt), {
          zoomOnMouseWheel: true,
          moveOnMouseMove: true,
          // By default, wheel do not trigger move.
          moveOnMouseWheel: false,
          preventDefaultMouseMove: true,
          zInfoParsed,
          triggerInfo,
          cursorGrab: "grab",
          cursorGrabbing: "grabbing"
        });
        if (controlType == null) {
          controlType = true;
        }
        if (!this._enabled || this._controlType !== controlType) {
          this.disable();
          this._enabled = true;
          if (controlType === true || controlType === "move" || controlType === "pan") {
            addRoamZrListener(zr, "mousedown", mousedownHandler, zInfoParsed);
            addRoamZrListener(zr, "mousemove", mousemoveHandler, zInfoParsed);
            addRoamZrListener(zr, "mouseup", mouseupHandler, zInfoParsed);
          }
          if (controlType === true || controlType === "scale" || controlType === "zoom") {
            addRoamZrListener(zr, "mousewheel", mousewheelHandler, zInfoParsed);
            addRoamZrListener(zr, "pinch", pinchHandler, zInfoParsed);
          }
        }
      };
      _this.disable = function() {
        if (this._enabled) {
          this._enabled = false;
          removeRoamZrListener(zr, "mousedown", mousedownHandler);
          removeRoamZrListener(zr, "mousemove", mousemoveHandler);
          removeRoamZrListener(zr, "mouseup", mouseupHandler);
          removeRoamZrListener(zr, "mousewheel", mousewheelHandler);
          removeRoamZrListener(zr, "pinch", pinchHandler);
        }
      };
      return _this;
    }
    RoamController2.prototype.isDragging = function() {
      return this._dragging;
    };
    RoamController2.prototype.isPinching = function() {
      return this._pinching;
    };
    RoamController2.prototype._checkPointer = function(e2, x, y) {
      var opt = this._opt;
      var zInfoParsed = opt.zInfoParsed;
      if (onIrrelevantElement(e2, opt.api, zInfoParsed.component)) {
        return false;
      }
      ;
      var triggerInfo = opt.triggerInfo;
      var roamTrigger = triggerInfo.roamTrigger;
      var inArea = false;
      if (roamTrigger === "global") {
        inArea = true;
      }
      if (!inArea) {
        inArea = triggerInfo.isInSelf(e2, x, y);
      }
      if (inArea && triggerInfo.isInClip && !triggerInfo.isInClip(e2, x, y)) {
        inArea = false;
      }
      return inArea;
    };
    RoamController2.prototype._decideCursorStyle = function(e2, x, y, forReverse) {
      var target = e2.target;
      if (!target && this._checkPointer(e2, x, y)) {
        return this._opt.cursorGrab;
      }
      if (forReverse) {
        return target && target.cursor || "default";
      }
    };
    RoamController2.prototype.dispose = function() {
      this.disable();
    };
    RoamController2.prototype._mousedownHandler = function(e2) {
      if (isMiddleOrRightButtonOnMouseUpDown(e2) || eventConsumed(e2)) {
        return;
      }
      var el = e2.target;
      while (el) {
        if (el.draggable) {
          return;
        }
        el = el.__hostTarget || el.parent;
      }
      var x = e2.offsetX;
      var y = e2.offsetY;
      if (this._checkPointer(e2, x, y)) {
        this._x = x;
        this._y = y;
        this._dragging = true;
      }
    };
    RoamController2.prototype._mousemoveHandler = function(e2) {
      var zr = this._zr;
      if (e2.gestureEvent === "pinch" || isTaken(zr, "globalPan") || eventConsumed(e2)) {
        return;
      }
      var x = e2.offsetX;
      var y = e2.offsetY;
      if (!this._dragging || !isAvailableBehavior("moveOnMouseMove", e2, this._opt)) {
        var cursorStyle = this._decideCursorStyle(e2, x, y, false);
        if (cursorStyle) {
          zr.setCursorStyle(cursorStyle);
        }
        return;
      }
      zr.setCursorStyle(this._opt.cursorGrabbing);
      var oldX = this._x;
      var oldY = this._y;
      var dx = x - oldX;
      var dy = y - oldY;
      this._x = x;
      this._y = y;
      if (this._opt.preventDefaultMouseMove) {
        stop(e2.event);
      }
      e2.__ecRoamConsumed = true;
      trigger2(this, "pan", "moveOnMouseMove", e2, {
        dx,
        dy,
        oldX,
        oldY,
        newX: x,
        newY: y,
        isAvailableBehavior: null
      });
    };
    RoamController2.prototype._mouseupHandler = function(e2) {
      if (eventConsumed(e2)) {
        return;
      }
      var zr = this._zr;
      if (!isMiddleOrRightButtonOnMouseUpDown(e2)) {
        this._dragging = false;
        var cursorStyle = this._decideCursorStyle(e2, e2.offsetX, e2.offsetY, true);
        if (cursorStyle) {
          zr.setCursorStyle(cursorStyle);
        }
      }
    };
    RoamController2.prototype._mousewheelHandler = function(e2) {
      if (eventConsumed(e2)) {
        return;
      }
      var shouldZoom = isAvailableBehavior("zoomOnMouseWheel", e2, this._opt);
      var shouldMove = isAvailableBehavior("moveOnMouseWheel", e2, this._opt);
      var wheelDelta = e2.wheelDelta;
      var absWheelDeltaDelta = Math.abs(wheelDelta);
      var originX = e2.offsetX;
      var originY = e2.offsetY;
      if (wheelDelta === 0 || !shouldZoom && !shouldMove) {
        return;
      }
      if (shouldZoom) {
        var factor = absWheelDeltaDelta > 3 ? 1.4 : absWheelDeltaDelta > 1 ? 1.2 : 1.1;
        var scale3 = wheelDelta > 0 ? factor : 1 / factor;
        this._checkTriggerMoveZoom(this, "zoom", "zoomOnMouseWheel", e2, {
          scale: scale3,
          originX,
          originY,
          isAvailableBehavior: null
        });
      }
      if (shouldMove) {
        var absDelta = Math.abs(wheelDelta);
        var scrollDelta = (wheelDelta > 0 ? 1 : -1) * (absDelta > 3 ? 0.4 : absDelta > 1 ? 0.15 : 0.05);
        this._checkTriggerMoveZoom(this, "scrollMove", "moveOnMouseWheel", e2, {
          scrollDelta,
          originX,
          originY,
          isAvailableBehavior: null
        });
      }
    };
    RoamController2.prototype._pinchHandler = function(e2) {
      if (isTaken(this._zr, "globalPan") || eventConsumed(e2)) {
        return;
      }
      var scale3 = e2.pinchScale > 1 ? 1.1 : 1 / 1.1;
      this._checkTriggerMoveZoom(this, "zoom", null, e2, {
        scale: scale3,
        originX: e2.pinchX,
        originY: e2.pinchY,
        isAvailableBehavior: null
      });
    };
    RoamController2.prototype._checkTriggerMoveZoom = function(controller, eventName, behaviorToCheck, e2, contollerEvent) {
      if (controller._checkPointer(e2, contollerEvent.originX, contollerEvent.originY)) {
        stop(e2.event);
        e2.__ecRoamConsumed = true;
        trigger2(controller, eventName, behaviorToCheck, e2, contollerEvent);
      }
    };
    return RoamController2;
  })(Eventful_default)
);
function eventConsumed(e2) {
  return e2.__ecRoamConsumed;
}
var innerZrStore = makeInner();
function ensureZrStore(zr) {
  var store = innerZrStore(zr);
  store.roam = store.roam || {};
  store.uniform = store.uniform || {};
  return store;
}
function addRoamZrListener(zr, eventType, listener, zInfoParsed) {
  var store = ensureZrStore(zr);
  var roam = store.roam;
  var listenerList = roam[eventType] = roam[eventType] || [];
  var idx = 0;
  for (; idx < listenerList.length; idx++) {
    var currZInfo = listenerList[idx].zInfoParsed;
    if ((currZInfo.zlevel - zInfoParsed.zlevel || currZInfo.z - zInfoParsed.z || currZInfo.z2 - zInfoParsed.z2) <= 0) {
      break;
    }
  }
  listenerList.splice(idx, 0, {
    listener,
    zInfoParsed
  });
  ensureUniformListener(zr, eventType);
}
function removeRoamZrListener(zr, eventType, listener) {
  var store = ensureZrStore(zr);
  var listenerList = store.roam[eventType] || [];
  for (var idx = 0; idx < listenerList.length; idx++) {
    if (listenerList[idx].listener === listener) {
      listenerList.splice(idx, 1);
      if (!listenerList.length) {
        removeUniformListener(zr, eventType);
      }
      return;
    }
  }
}
function ensureUniformListener(zr, eventType) {
  var store = ensureZrStore(zr);
  if (!store.uniform[eventType]) {
    zr.on(eventType, store.uniform[eventType] = function(event) {
      var listenerList = store.roam[eventType];
      if (listenerList) {
        for (var i2 = 0; i2 < listenerList.length; i2++) {
          listenerList[i2].listener(event);
        }
      }
    });
  }
}
function removeUniformListener(zr, eventType) {
  var store = ensureZrStore(zr);
  var uniform = store.uniform;
  if (uniform[eventType]) {
    zr.off(eventType, uniform[eventType]);
    uniform[eventType] = null;
  }
}
function trigger2(controller, eventName, behaviorToCheck, e2, contollerEvent) {
  contollerEvent.isAvailableBehavior = bind(isAvailableBehavior, null, behaviorToCheck, e2);
  controller.trigger(eventName, contollerEvent);
}
function isAvailableBehavior(behaviorToCheck, e2, settings) {
  var setting = settings[behaviorToCheck];
  return !behaviorToCheck || setting && (!isString(setting) || e2.event[setting + "Key"]);
}
var RoamController_default = RoamController;

// node_modules/zrender/lib/tool/parseXML.js
function parseXML(svg) {
  if (isString(svg)) {
    var parser = new DOMParser();
    svg = parser.parseFromString(svg, "text/xml");
  }
  var svgNode = svg;
  if (svgNode.nodeType === 9) {
    svgNode = svgNode.firstChild;
  }
  while (svgNode.nodeName.toLowerCase() !== "svg" || svgNode.nodeType !== 1) {
    svgNode = svgNode.nextSibling;
  }
  return svgNode;
}

// node_modules/zrender/lib/tool/parseSVG.js
var nodeParsers;
var INHERITABLE_STYLE_ATTRIBUTES_MAP = {
  "fill": "fill",
  "stroke": "stroke",
  "stroke-width": "lineWidth",
  "opacity": "opacity",
  "fill-opacity": "fillOpacity",
  "stroke-opacity": "strokeOpacity",
  "stroke-dasharray": "lineDash",
  "stroke-dashoffset": "lineDashOffset",
  "stroke-linecap": "lineCap",
  "stroke-linejoin": "lineJoin",
  "stroke-miterlimit": "miterLimit",
  "font-family": "fontFamily",
  "font-size": "fontSize",
  "font-style": "fontStyle",
  "font-weight": "fontWeight",
  "text-anchor": "textAlign",
  "visibility": "visibility",
  "display": "display"
};
var INHERITABLE_STYLE_ATTRIBUTES_MAP_KEYS = keys(INHERITABLE_STYLE_ATTRIBUTES_MAP);
var SELF_STYLE_ATTRIBUTES_MAP = {
  "alignment-baseline": "textBaseline",
  "stop-color": "stopColor"
};
var SELF_STYLE_ATTRIBUTES_MAP_KEYS = keys(SELF_STYLE_ATTRIBUTES_MAP);
var SVGParser = (function() {
  function SVGParser2() {
    this._defs = {};
    this._root = null;
  }
  SVGParser2.prototype.parse = function(xml, opt) {
    opt = opt || {};
    var svg = parseXML(xml);
    if (true) {
      if (!svg) {
        throw new Error("Illegal svg");
      }
    }
    this._defsUsePending = [];
    var root = new Group_default();
    this._root = root;
    var named = [];
    var viewBox = svg.getAttribute("viewBox") || "";
    var width = parseFloat(svg.getAttribute("width") || opt.width);
    var height = parseFloat(svg.getAttribute("height") || opt.height);
    isNaN(width) && (width = null);
    isNaN(height) && (height = null);
    parseAttributes(svg, root, null, true, false);
    var child = svg.firstChild;
    while (child) {
      this._parseNode(child, root, named, null, false, false);
      child = child.nextSibling;
    }
    applyDefs(this._defs, this._defsUsePending);
    this._defsUsePending = [];
    var viewBoxRect;
    var viewBoxTransform;
    if (viewBox) {
      var viewBoxArr = splitNumberSequence(viewBox);
      if (viewBoxArr.length >= 4) {
        viewBoxRect = {
          x: parseFloat(viewBoxArr[0] || 0),
          y: parseFloat(viewBoxArr[1] || 0),
          width: parseFloat(viewBoxArr[2]),
          height: parseFloat(viewBoxArr[3])
        };
      }
    }
    if (viewBoxRect && width != null && height != null) {
      viewBoxTransform = makeViewBoxTransform(viewBoxRect, { x: 0, y: 0, width, height });
      if (!opt.ignoreViewBox) {
        var elRoot = root;
        root = new Group_default();
        root.add(elRoot);
        elRoot.scaleX = elRoot.scaleY = viewBoxTransform.scale;
        elRoot.x = viewBoxTransform.x;
        elRoot.y = viewBoxTransform.y;
      }
    }
    if (!opt.ignoreRootClip && width != null && height != null) {
      root.setClipPath(new Rect_default({
        shape: { x: 0, y: 0, width, height }
      }));
    }
    return {
      root,
      width,
      height,
      viewBoxRect,
      viewBoxTransform,
      named
    };
  };
  SVGParser2.prototype._parseNode = function(xmlNode, parentGroup, named, namedFrom, isInDefs, isInText) {
    var nodeName = xmlNode.nodeName.toLowerCase();
    var el;
    var namedFromForSub = namedFrom;
    if (nodeName === "defs") {
      isInDefs = true;
    }
    if (nodeName === "text") {
      isInText = true;
    }
    if (nodeName === "defs" || nodeName === "switch") {
      el = parentGroup;
    } else {
      if (!isInDefs) {
        var parser_1 = nodeParsers[nodeName];
        if (parser_1 && hasOwn(nodeParsers, nodeName)) {
          el = parser_1.call(this, xmlNode, parentGroup);
          var nameAttr = xmlNode.getAttribute("name");
          if (nameAttr) {
            var newNamed = {
              name: nameAttr,
              namedFrom: null,
              svgNodeTagLower: nodeName,
              el
            };
            named.push(newNamed);
            if (nodeName === "g") {
              namedFromForSub = newNamed;
            }
          } else if (namedFrom) {
            named.push({
              name: namedFrom.name,
              namedFrom,
              svgNodeTagLower: nodeName,
              el
            });
          }
          parentGroup.add(el);
        }
      }
      var parser = paintServerParsers[nodeName];
      if (parser && hasOwn(paintServerParsers, nodeName)) {
        var def = parser.call(this, xmlNode);
        var id = xmlNode.getAttribute("id");
        if (id) {
          this._defs[id] = def;
        }
      }
    }
    if (el && el.isGroup) {
      var child = xmlNode.firstChild;
      while (child) {
        if (child.nodeType === 1) {
          this._parseNode(child, el, named, namedFromForSub, isInDefs, isInText);
        } else if (child.nodeType === 3 && isInText) {
          this._parseText(child, el);
        }
        child = child.nextSibling;
      }
    }
  };
  SVGParser2.prototype._parseText = function(xmlNode, parentGroup) {
    var text = new TSpan_default({
      style: {
        text: xmlNode.textContent
      },
      silent: true,
      x: this._textX || 0,
      y: this._textY || 0
    });
    inheritStyle(parentGroup, text);
    parseAttributes(xmlNode, text, this._defsUsePending, false, false);
    applyTextAlignment(text, parentGroup);
    var textStyle = text.style;
    var fontSize = textStyle.fontSize;
    if (fontSize && fontSize < 9) {
      textStyle.fontSize = 9;
      text.scaleX *= fontSize / 9;
      text.scaleY *= fontSize / 9;
    }
    var font = (textStyle.fontSize || textStyle.fontFamily) && [
      textStyle.fontStyle,
      textStyle.fontWeight,
      (textStyle.fontSize || 12) + "px",
      textStyle.fontFamily || "sans-serif"
    ].join(" ");
    textStyle.font = font;
    var rect = text.getBoundingRect();
    this._textX += rect.width;
    parentGroup.add(text);
    return text;
  };
  SVGParser2.internalField = (function() {
    nodeParsers = {
      "g": function(xmlNode, parentGroup) {
        var g = new Group_default();
        inheritStyle(parentGroup, g);
        parseAttributes(xmlNode, g, this._defsUsePending, false, false);
        return g;
      },
      "rect": function(xmlNode, parentGroup) {
        var rect = new Rect_default();
        inheritStyle(parentGroup, rect);
        parseAttributes(xmlNode, rect, this._defsUsePending, false, false);
        rect.setShape({
          x: parseFloat(xmlNode.getAttribute("x") || "0"),
          y: parseFloat(xmlNode.getAttribute("y") || "0"),
          width: parseFloat(xmlNode.getAttribute("width") || "0"),
          height: parseFloat(xmlNode.getAttribute("height") || "0")
        });
        rect.silent = true;
        return rect;
      },
      "circle": function(xmlNode, parentGroup) {
        var circle = new Circle_default();
        inheritStyle(parentGroup, circle);
        parseAttributes(xmlNode, circle, this._defsUsePending, false, false);
        circle.setShape({
          cx: parseFloat(xmlNode.getAttribute("cx") || "0"),
          cy: parseFloat(xmlNode.getAttribute("cy") || "0"),
          r: parseFloat(xmlNode.getAttribute("r") || "0")
        });
        circle.silent = true;
        return circle;
      },
      "line": function(xmlNode, parentGroup) {
        var line = new Line_default();
        inheritStyle(parentGroup, line);
        parseAttributes(xmlNode, line, this._defsUsePending, false, false);
        line.setShape({
          x1: parseFloat(xmlNode.getAttribute("x1") || "0"),
          y1: parseFloat(xmlNode.getAttribute("y1") || "0"),
          x2: parseFloat(xmlNode.getAttribute("x2") || "0"),
          y2: parseFloat(xmlNode.getAttribute("y2") || "0")
        });
        line.silent = true;
        return line;
      },
      "ellipse": function(xmlNode, parentGroup) {
        var ellipse = new Ellipse_default();
        inheritStyle(parentGroup, ellipse);
        parseAttributes(xmlNode, ellipse, this._defsUsePending, false, false);
        ellipse.setShape({
          cx: parseFloat(xmlNode.getAttribute("cx") || "0"),
          cy: parseFloat(xmlNode.getAttribute("cy") || "0"),
          rx: parseFloat(xmlNode.getAttribute("rx") || "0"),
          ry: parseFloat(xmlNode.getAttribute("ry") || "0")
        });
        ellipse.silent = true;
        return ellipse;
      },
      "polygon": function(xmlNode, parentGroup) {
        var pointsStr = xmlNode.getAttribute("points");
        var pointsArr;
        if (pointsStr) {
          pointsArr = parsePoints(pointsStr);
        }
        var polygon = new Polygon_default({
          shape: {
            points: pointsArr || []
          },
          silent: true
        });
        inheritStyle(parentGroup, polygon);
        parseAttributes(xmlNode, polygon, this._defsUsePending, false, false);
        return polygon;
      },
      "polyline": function(xmlNode, parentGroup) {
        var pointsStr = xmlNode.getAttribute("points");
        var pointsArr;
        if (pointsStr) {
          pointsArr = parsePoints(pointsStr);
        }
        var polyline = new Polyline_default({
          shape: {
            points: pointsArr || []
          },
          silent: true
        });
        inheritStyle(parentGroup, polyline);
        parseAttributes(xmlNode, polyline, this._defsUsePending, false, false);
        return polyline;
      },
      "image": function(xmlNode, parentGroup) {
        var img = new Image_default();
        inheritStyle(parentGroup, img);
        parseAttributes(xmlNode, img, this._defsUsePending, false, false);
        img.setStyle({
          image: xmlNode.getAttribute("xlink:href") || xmlNode.getAttribute("href"),
          x: +xmlNode.getAttribute("x"),
          y: +xmlNode.getAttribute("y"),
          width: +xmlNode.getAttribute("width"),
          height: +xmlNode.getAttribute("height")
        });
        img.silent = true;
        return img;
      },
      "text": function(xmlNode, parentGroup) {
        var x = xmlNode.getAttribute("x") || "0";
        var y = xmlNode.getAttribute("y") || "0";
        var dx = xmlNode.getAttribute("dx") || "0";
        var dy = xmlNode.getAttribute("dy") || "0";
        this._textX = parseFloat(x) + parseFloat(dx);
        this._textY = parseFloat(y) + parseFloat(dy);
        var g = new Group_default();
        inheritStyle(parentGroup, g);
        parseAttributes(xmlNode, g, this._defsUsePending, false, true);
        return g;
      },
      "tspan": function(xmlNode, parentGroup) {
        var x = xmlNode.getAttribute("x");
        var y = xmlNode.getAttribute("y");
        if (x != null) {
          this._textX = parseFloat(x);
        }
        if (y != null) {
          this._textY = parseFloat(y);
        }
        var dx = xmlNode.getAttribute("dx") || "0";
        var dy = xmlNode.getAttribute("dy") || "0";
        var g = new Group_default();
        inheritStyle(parentGroup, g);
        parseAttributes(xmlNode, g, this._defsUsePending, false, true);
        this._textX += parseFloat(dx);
        this._textY += parseFloat(dy);
        return g;
      },
      "path": function(xmlNode, parentGroup) {
        var d = xmlNode.getAttribute("d") || "";
        var path = createFromString(d);
        inheritStyle(parentGroup, path);
        parseAttributes(xmlNode, path, this._defsUsePending, false, false);
        path.silent = true;
        return path;
      }
    };
  })();
  return SVGParser2;
})();
var paintServerParsers = {
  "lineargradient": function(xmlNode) {
    var x1 = parseInt(xmlNode.getAttribute("x1") || "0", 10);
    var y1 = parseInt(xmlNode.getAttribute("y1") || "0", 10);
    var x2 = parseInt(xmlNode.getAttribute("x2") || "10", 10);
    var y2 = parseInt(xmlNode.getAttribute("y2") || "0", 10);
    var gradient = new LinearGradient_default(x1, y1, x2, y2);
    parsePaintServerUnit(xmlNode, gradient);
    parseGradientColorStops(xmlNode, gradient);
    return gradient;
  },
  "radialgradient": function(xmlNode) {
    var cx = parseInt(xmlNode.getAttribute("cx") || "0", 10);
    var cy = parseInt(xmlNode.getAttribute("cy") || "0", 10);
    var r = parseInt(xmlNode.getAttribute("r") || "0", 10);
    var gradient = new RadialGradient_default(cx, cy, r);
    parsePaintServerUnit(xmlNode, gradient);
    parseGradientColorStops(xmlNode, gradient);
    return gradient;
  }
};
function parsePaintServerUnit(xmlNode, gradient) {
  var gradientUnits = xmlNode.getAttribute("gradientUnits");
  if (gradientUnits === "userSpaceOnUse") {
    gradient.global = true;
  }
}
function parseGradientColorStops(xmlNode, gradient) {
  var stop2 = xmlNode.firstChild;
  while (stop2) {
    if (stop2.nodeType === 1 && stop2.nodeName.toLocaleLowerCase() === "stop") {
      var offsetStr = stop2.getAttribute("offset");
      var offset = void 0;
      if (offsetStr && offsetStr.indexOf("%") > 0) {
        offset = parseInt(offsetStr, 10) / 100;
      } else if (offsetStr) {
        offset = parseFloat(offsetStr);
      } else {
        offset = 0;
      }
      var styleVals = {};
      parseInlineStyle(stop2, styleVals, styleVals);
      var stopColor = styleVals.stopColor || stop2.getAttribute("stop-color") || "#000000";
      var stopOpacity = styleVals.stopOpacity || stop2.getAttribute("stop-opacity");
      if (stopOpacity) {
        var rgba = parse(stopColor);
        var stopColorOpacity = rgba && rgba[3];
        if (stopColorOpacity) {
          rgba[3] *= parseCssFloat(stopOpacity);
          stopColor = stringify(rgba, "rgba");
        }
      }
      gradient.colorStops.push({
        offset,
        color: stopColor
      });
    }
    stop2 = stop2.nextSibling;
  }
}
function inheritStyle(parent, child) {
  if (parent && parent.__inheritedStyle) {
    if (!child.__inheritedStyle) {
      child.__inheritedStyle = {};
    }
    defaults(child.__inheritedStyle, parent.__inheritedStyle);
  }
}
function parsePoints(pointsString) {
  var list = splitNumberSequence(pointsString);
  var points4 = [];
  for (var i2 = 0; i2 < list.length; i2 += 2) {
    var x = parseFloat(list[i2]);
    var y = parseFloat(list[i2 + 1]);
    points4.push([x, y]);
  }
  return points4;
}
function parseAttributes(xmlNode, el, defsUsePending, onlyInlineStyle, isTextGroup) {
  var disp = el;
  var inheritedStyle = disp.__inheritedStyle = disp.__inheritedStyle || {};
  var selfStyle = {};
  if (xmlNode.nodeType === 1) {
    parseTransformAttribute(xmlNode, el);
    parseInlineStyle(xmlNode, inheritedStyle, selfStyle);
    if (!onlyInlineStyle) {
      parseAttributeStyle(xmlNode, inheritedStyle, selfStyle);
    }
  }
  disp.style = disp.style || {};
  if (inheritedStyle.fill != null) {
    disp.style.fill = getFillStrokeStyle(disp, "fill", inheritedStyle.fill, defsUsePending);
  }
  if (inheritedStyle.stroke != null) {
    disp.style.stroke = getFillStrokeStyle(disp, "stroke", inheritedStyle.stroke, defsUsePending);
  }
  each([
    "lineWidth",
    "opacity",
    "fillOpacity",
    "strokeOpacity",
    "miterLimit",
    "fontSize"
  ], function(propName) {
    if (inheritedStyle[propName] != null) {
      disp.style[propName] = parseFloat(inheritedStyle[propName]);
    }
  });
  each([
    "lineDashOffset",
    "lineCap",
    "lineJoin",
    "fontWeight",
    "fontFamily",
    "fontStyle",
    "textAlign"
  ], function(propName) {
    if (inheritedStyle[propName] != null) {
      disp.style[propName] = inheritedStyle[propName];
    }
  });
  if (isTextGroup) {
    disp.__selfStyle = selfStyle;
  }
  if (inheritedStyle.lineDash) {
    disp.style.lineDash = map2(splitNumberSequence(inheritedStyle.lineDash), function(str) {
      return parseFloat(str);
    });
  }
  if (inheritedStyle.visibility === "hidden" || inheritedStyle.visibility === "collapse") {
    disp.invisible = true;
  }
  if (inheritedStyle.display === "none") {
    disp.ignore = true;
  }
}
function applyTextAlignment(text, parentGroup) {
  var parentSelfStyle = parentGroup.__selfStyle;
  if (parentSelfStyle) {
    var textBaseline = parentSelfStyle.textBaseline;
    var zrTextBaseline = textBaseline;
    if (!textBaseline || textBaseline === "auto") {
      zrTextBaseline = "alphabetic";
    } else if (textBaseline === "baseline") {
      zrTextBaseline = "alphabetic";
    } else if (textBaseline === "before-edge" || textBaseline === "text-before-edge") {
      zrTextBaseline = "top";
    } else if (textBaseline === "after-edge" || textBaseline === "text-after-edge") {
      zrTextBaseline = "bottom";
    } else if (textBaseline === "central" || textBaseline === "mathematical") {
      zrTextBaseline = "middle";
    }
    text.style.textBaseline = zrTextBaseline;
  }
  var parentInheritedStyle = parentGroup.__inheritedStyle;
  if (parentInheritedStyle) {
    var textAlign = parentInheritedStyle.textAlign;
    var zrTextAlign = textAlign;
    if (textAlign) {
      if (textAlign === "middle") {
        zrTextAlign = "center";
      }
      text.style.textAlign = zrTextAlign;
    }
  }
}
var urlRegex = /^url\(\s*#(.*?)\)/;
function getFillStrokeStyle(el, method, str, defsUsePending) {
  var urlMatch = str && str.match(urlRegex);
  if (urlMatch) {
    var url = trim(urlMatch[1]);
    defsUsePending.push([el, method, url]);
    return;
  }
  if (str === "none") {
    str = null;
  }
  return str;
}
function applyDefs(defs, defsUsePending) {
  for (var i2 = 0; i2 < defsUsePending.length; i2++) {
    var item = defsUsePending[i2];
    item[0].style[item[1]] = defs[item[2]];
  }
}
var numberReg2 = /-?([0-9]*\.)?[0-9]+([eE]-?[0-9]+)?/g;
function splitNumberSequence(rawStr) {
  return rawStr.match(numberReg2) || [];
}
var transformRegex = /(translate|scale|rotate|skewX|skewY|matrix)\(([\-\s0-9\.eE,]*)\)/g;
var DEGREE_TO_ANGLE = Math.PI / 180;
function parseTransformAttribute(xmlNode, node) {
  var transform = xmlNode.getAttribute("transform");
  if (transform) {
    transform = transform.replace(/,/g, " ");
    var transformOps_1 = [];
    var mt = null;
    transform.replace(transformRegex, function(str, type2, value2) {
      transformOps_1.push(type2, value2);
      return "";
    });
    for (var i2 = transformOps_1.length - 1; i2 > 0; i2 -= 2) {
      var value = transformOps_1[i2];
      var type = transformOps_1[i2 - 1];
      var valueArr = splitNumberSequence(value);
      mt = mt || create();
      switch (type) {
        case "translate":
          translate(mt, mt, [parseFloat(valueArr[0]), parseFloat(valueArr[1] || "0")]);
          break;
        case "scale":
          scale(mt, mt, [parseFloat(valueArr[0]), parseFloat(valueArr[1] || valueArr[0])]);
          break;
        case "rotate":
          rotate(mt, mt, -parseFloat(valueArr[0]) * DEGREE_TO_ANGLE, [
            parseFloat(valueArr[1] || "0"),
            parseFloat(valueArr[2] || "0")
          ]);
          break;
        case "skewX":
          var sx = Math.tan(parseFloat(valueArr[0]) * DEGREE_TO_ANGLE);
          mul(mt, [1, 0, sx, 1, 0, 0], mt);
          break;
        case "skewY":
          var sy = Math.tan(parseFloat(valueArr[0]) * DEGREE_TO_ANGLE);
          mul(mt, [1, sy, 0, 1, 0, 0], mt);
          break;
        case "matrix":
          mt[0] = parseFloat(valueArr[0]);
          mt[1] = parseFloat(valueArr[1]);
          mt[2] = parseFloat(valueArr[2]);
          mt[3] = parseFloat(valueArr[3]);
          mt[4] = parseFloat(valueArr[4]);
          mt[5] = parseFloat(valueArr[5]);
          break;
      }
    }
    node.setLocalTransform(mt);
  }
}
var styleRegex = /([^\s:;]+)\s*:\s*([^:;]+)/g;
function parseInlineStyle(xmlNode, inheritableStyleResult, selfStyleResult) {
  var style2 = xmlNode.getAttribute("style");
  if (!style2) {
    return;
  }
  styleRegex.lastIndex = 0;
  var styleRegResult;
  while ((styleRegResult = styleRegex.exec(style2)) != null) {
    var svgStlAttr = styleRegResult[1];
    var zrInheritableStlAttr = hasOwn(INHERITABLE_STYLE_ATTRIBUTES_MAP, svgStlAttr) ? INHERITABLE_STYLE_ATTRIBUTES_MAP[svgStlAttr] : null;
    if (zrInheritableStlAttr) {
      inheritableStyleResult[zrInheritableStlAttr] = styleRegResult[2];
    }
    var zrSelfStlAttr = hasOwn(SELF_STYLE_ATTRIBUTES_MAP, svgStlAttr) ? SELF_STYLE_ATTRIBUTES_MAP[svgStlAttr] : null;
    if (zrSelfStlAttr) {
      selfStyleResult[zrSelfStlAttr] = styleRegResult[2];
    }
  }
}
function parseAttributeStyle(xmlNode, inheritableStyleResult, selfStyleResult) {
  for (var i2 = 0; i2 < INHERITABLE_STYLE_ATTRIBUTES_MAP_KEYS.length; i2++) {
    var svgAttrName = INHERITABLE_STYLE_ATTRIBUTES_MAP_KEYS[i2];
    var attrValue = xmlNode.getAttribute(svgAttrName);
    if (attrValue != null) {
      inheritableStyleResult[INHERITABLE_STYLE_ATTRIBUTES_MAP[svgAttrName]] = attrValue;
    }
  }
  for (var i2 = 0; i2 < SELF_STYLE_ATTRIBUTES_MAP_KEYS.length; i2++) {
    var svgAttrName = SELF_STYLE_ATTRIBUTES_MAP_KEYS[i2];
    var attrValue = xmlNode.getAttribute(svgAttrName);
    if (attrValue != null) {
      selfStyleResult[SELF_STYLE_ATTRIBUTES_MAP[svgAttrName]] = attrValue;
    }
  }
}
function makeViewBoxTransform(viewBoxRect, boundingRect) {
  var scaleX = boundingRect.width / viewBoxRect.width;
  var scaleY = boundingRect.height / viewBoxRect.height;
  var scale3 = Math.min(scaleX, scaleY);
  return {
    scale: scale3,
    x: -(viewBoxRect.x + viewBoxRect.width / 2) * scale3 + (boundingRect.x + boundingRect.width / 2),
    y: -(viewBoxRect.y + viewBoxRect.height / 2) * scale3 + (boundingRect.y + boundingRect.height / 2)
  };
}
function parseSVG(xml, opt) {
  var parser = new SVGParser();
  return parser.parse(xml, opt);
}

// node_modules/zrender/lib/contain/polygon.js
var EPSILON6 = 1e-8;
function isAroundEqual2(a, b) {
  return Math.abs(a - b) < EPSILON6;
}
function contain2(points4, x, y) {
  var w = 0;
  var p = points4[0];
  if (!p) {
    return false;
  }
  for (var i2 = 1; i2 < points4.length; i2++) {
    var p2 = points4[i2];
    w += windingLine(p[0], p[1], p2[0], p2[1], x, y);
    p = p2;
  }
  var p0 = points4[0];
  if (!isAroundEqual2(p[0], p0[0]) || !isAroundEqual2(p[1], p0[1])) {
    w += windingLine(p[0], p[1], p0[0], p0[1], x, y);
  }
  return w !== 0;
}

// node_modules/echarts/lib/coord/geo/Region.js
var TMP_TRANSFORM = [];
function transformPoints(points4, transform) {
  for (var p = 0; p < points4.length; p++) {
    applyTransform(points4[p], points4[p], transform);
  }
}
function updateBBoxFromPoints(points4, min3, max3, projection) {
  for (var i2 = 0; i2 < points4.length; i2++) {
    var p = points4[i2];
    if (projection) {
      p = projection.project(p);
    }
    if (p && isFinite(p[0]) && isFinite(p[1])) {
      min(min3, min3, p);
      max(max3, max3, p);
    }
  }
}
function centroid(points4) {
  var signedArea = 0;
  var cx = 0;
  var cy = 0;
  var len2 = points4.length;
  var x0 = points4[len2 - 1][0];
  var y0 = points4[len2 - 1][1];
  for (var i2 = 0; i2 < len2; i2++) {
    var x1 = points4[i2][0];
    var y1 = points4[i2][1];
    var a = x0 * y1 - x1 * y0;
    signedArea += a;
    cx += (x0 + x1) * a;
    cy += (y0 + y1) * a;
    x0 = x1;
    y0 = y1;
  }
  return signedArea ? [cx / signedArea / 3, cy / signedArea / 3, signedArea] : [points4[0][0] || 0, points4[0][1] || 0];
}
var Region = (
  /** @class */
  (function() {
    function Region2(name) {
      this.name = name;
    }
    Region2.prototype.setCenter = function(center3) {
      this._center = center3;
    };
    Region2.prototype.getCenter = function() {
      var center3 = this._center;
      if (!center3) {
        center3 = this._center = this.calcCenter();
      }
      return center3;
    };
    return Region2;
  })()
);
var GeoJSONPolygonGeometry = (
  /** @class */
  /* @__PURE__ */ (function() {
    function GeoJSONPolygonGeometry2(exterior, interiors) {
      this.type = "polygon";
      this.exterior = exterior;
      this.interiors = interiors;
    }
    return GeoJSONPolygonGeometry2;
  })()
);
var GeoJSONLineStringGeometry = (
  /** @class */
  /* @__PURE__ */ (function() {
    function GeoJSONLineStringGeometry2(points4) {
      this.type = "linestring";
      this.points = points4;
    }
    return GeoJSONLineStringGeometry2;
  })()
);
var GeoJSONRegion = (
  /** @class */
  (function(_super) {
    __extends(GeoJSONRegion2, _super);
    function GeoJSONRegion2(name, geometries, cp) {
      var _this = _super.call(this, name) || this;
      _this.type = "geoJSON";
      _this.geometries = geometries;
      _this._center = cp && [cp[0], cp[1]];
      return _this;
    }
    GeoJSONRegion2.prototype.calcCenter = function() {
      var geometries = this.geometries;
      var largestGeo;
      var largestGeoSize = 0;
      for (var i2 = 0; i2 < geometries.length; i2++) {
        var geo = geometries[i2];
        var exterior = geo.exterior;
        var size = exterior && exterior.length;
        if (size > largestGeoSize) {
          largestGeo = geo;
          largestGeoSize = size;
        }
      }
      if (largestGeo) {
        return centroid(largestGeo.exterior);
      }
      var rect = this.getBoundingRect();
      return [rect.x + rect.width / 2, rect.y + rect.height / 2];
    };
    GeoJSONRegion2.prototype.getBoundingRect = function(projection) {
      var rect = this._rect;
      if (rect && !projection) {
        return rect;
      }
      var min3 = [Infinity, Infinity];
      var max3 = [-Infinity, -Infinity];
      var geometries = this.geometries;
      each(geometries, function(geo) {
        if (geo.type === "polygon") {
          updateBBoxFromPoints(geo.exterior, min3, max3, projection);
        } else {
          each(geo.points, function(points4) {
            updateBBoxFromPoints(points4, min3, max3, projection);
          });
        }
      });
      if (!(isFinite(min3[0]) && isFinite(min3[1]) && isFinite(max3[0]) && isFinite(max3[1]))) {
        min3[0] = min3[1] = max3[0] = max3[1] = 0;
      }
      rect = new BoundingRect_default(min3[0], min3[1], max3[0] - min3[0], max3[1] - min3[1]);
      if (!projection) {
        this._rect = rect;
      }
      return rect;
    };
    GeoJSONRegion2.prototype.contain = function(coord) {
      var rect = this.getBoundingRect();
      var geometries = this.geometries;
      if (!rect.contain(coord[0], coord[1])) {
        return false;
      }
      loopGeo: for (var i2 = 0, len2 = geometries.length; i2 < len2; i2++) {
        var geo = geometries[i2];
        if (geo.type !== "polygon") {
          continue;
        }
        var exterior = geo.exterior;
        var interiors = geo.interiors;
        if (contain2(exterior, coord[0], coord[1])) {
          for (var k2 = 0; k2 < (interiors ? interiors.length : 0); k2++) {
            if (contain2(interiors[k2], coord[0], coord[1])) {
              continue loopGeo;
            }
          }
          return true;
        }
      }
      return false;
    };
    GeoJSONRegion2.prototype.transformTo = function(x, y, width, height) {
      var rect = this.getBoundingRect();
      var aspect = rect.width / rect.height;
      if (!width) {
        width = aspect * height;
      } else if (!height) {
        height = width / aspect;
      }
      var target = new BoundingRect_default(x, y, width, height);
      var transform = rect.calculateTransform(target);
      var geometries = this.geometries;
      for (var i2 = 0; i2 < geometries.length; i2++) {
        var geo = geometries[i2];
        if (geo.type === "polygon") {
          transformPoints(geo.exterior, transform);
          each(geo.interiors, function(interior) {
            transformPoints(interior, transform);
          });
        } else {
          each(geo.points, function(points4) {
            transformPoints(points4, transform);
          });
        }
      }
      rect = this._rect;
      rect.copy(target);
      this._center = [rect.x + rect.width / 2, rect.y + rect.height / 2];
    };
    GeoJSONRegion2.prototype.cloneShallow = function(name) {
      name == null && (name = this.name);
      var newRegion = new GeoJSONRegion2(name, this.geometries, this._center);
      newRegion._rect = this._rect;
      newRegion.transformTo = null;
      return newRegion;
    };
    return GeoJSONRegion2;
  })(Region)
);
var GeoSVGRegion = (
  /** @class */
  (function(_super) {
    __extends(GeoSVGRegion2, _super);
    function GeoSVGRegion2(name, elOnlyForCalculate) {
      var _this = _super.call(this, name) || this;
      _this.type = "geoSVG";
      _this._elOnlyForCalculate = elOnlyForCalculate;
      return _this;
    }
    GeoSVGRegion2.prototype.calcCenter = function() {
      var el = this._elOnlyForCalculate;
      var rect = el.getBoundingRect();
      var center3 = [rect.x + rect.width / 2, rect.y + rect.height / 2];
      var mat = identity(TMP_TRANSFORM);
      var target = el;
      while (target && !target.isGeoSVGGraphicRoot) {
        mul(mat, target.getLocalTransform(), mat);
        target = target.parent;
      }
      invert(mat, mat);
      applyTransform(center3, center3, mat);
      return center3;
    };
    return GeoSVGRegion2;
  })(Region)
);

// node_modules/echarts/lib/coord/geo/GeoSVGResource.js
var REGION_AVAILABLE_SVG_TAG_MAP = createHashMap([
  "rect",
  "circle",
  "line",
  "ellipse",
  "polygon",
  "polyline",
  "path",
  // <text> <tspan> are also enabled because some SVG might paint text itself,
  // but still need to trigger events or tooltip.
  "text",
  "tspan",
  // <g> is also enabled because this case: if multiple tags share one name
  // and need label displayed, every tags will display the name, which is not
  // expected. So we can put them into a <g name="xxx">. Thereby only one label
  // displayed and located based on the bounding rect of the <g>.
  "g"
]);
var GeoSVGResource = (
  /** @class */
  (function() {
    function GeoSVGResource2(mapName, svg) {
      this.type = "geoSVG";
      this._usedGraphicMap = createHashMap();
      this._freedGraphics = [];
      this._mapName = mapName;
      this._parsedXML = parseXML(svg);
    }
    GeoSVGResource2.prototype.load = function() {
      var firstGraphic = this._firstGraphic;
      if (!firstGraphic) {
        firstGraphic = this._firstGraphic = this._buildGraphic(this._parsedXML);
        this._freedGraphics.push(firstGraphic);
        this._boundingRect = this._firstGraphic.boundingRect.clone();
        var _a2 = createRegions(firstGraphic.named), regions = _a2.regions, regionsMap = _a2.regionsMap;
        this._regions = regions;
        this._regionsMap = regionsMap;
      }
      return {
        boundingRect: this._boundingRect,
        regions: this._regions,
        regionsMap: this._regionsMap
      };
    };
    GeoSVGResource2.prototype._buildGraphic = function(svgXML) {
      var result;
      var rootFromParse;
      try {
        result = svgXML && parseSVG(svgXML, {
          ignoreViewBox: true,
          ignoreRootClip: true
        }) || {};
        rootFromParse = result.root;
        assert(rootFromParse != null);
      } catch (e2) {
        throw new Error("Invalid svg format\n" + e2.message);
      }
      var root = new Group_default();
      root.add(rootFromParse);
      root.isGeoSVGGraphicRoot = true;
      var svgWidth = result.width;
      var svgHeight = result.height;
      var viewBoxRect = result.viewBoxRect;
      var boundingRect = this._boundingRect;
      if (!boundingRect) {
        var bRectX = void 0;
        var bRectY = void 0;
        var bRectWidth = void 0;
        var bRectHeight = void 0;
        if (svgWidth != null) {
          bRectX = 0;
          bRectWidth = svgWidth;
        } else if (viewBoxRect) {
          bRectX = viewBoxRect.x;
          bRectWidth = viewBoxRect.width;
        }
        if (svgHeight != null) {
          bRectY = 0;
          bRectHeight = svgHeight;
        } else if (viewBoxRect) {
          bRectY = viewBoxRect.y;
          bRectHeight = viewBoxRect.height;
        }
        if (bRectX == null || bRectY == null) {
          var calculatedBoundingRect = rootFromParse.getBoundingRect();
          if (bRectX == null) {
            bRectX = calculatedBoundingRect.x;
            bRectWidth = calculatedBoundingRect.width;
          }
          if (bRectY == null) {
            bRectY = calculatedBoundingRect.y;
            bRectHeight = calculatedBoundingRect.height;
          }
        }
        boundingRect = this._boundingRect = new BoundingRect_default(bRectX, bRectY, bRectWidth, bRectHeight);
      }
      if (viewBoxRect) {
        var viewBoxTransform = makeViewBoxTransform(viewBoxRect, boundingRect);
        rootFromParse.scaleX = rootFromParse.scaleY = viewBoxTransform.scale;
        rootFromParse.x = viewBoxTransform.x;
        rootFromParse.y = viewBoxTransform.y;
      }
      root.setClipPath(new Rect_default({
        shape: boundingRect.plain()
      }));
      var named = [];
      each(result.named, function(namedItem) {
        if (REGION_AVAILABLE_SVG_TAG_MAP.get(namedItem.svgNodeTagLower) != null) {
          named.push(namedItem);
          setSilent(namedItem.el);
        }
      });
      return {
        root,
        boundingRect,
        named
      };
    };
    GeoSVGResource2.prototype.useGraphic = function(hostKey) {
      var usedRootMap = this._usedGraphicMap;
      var svgGraphic = usedRootMap.get(hostKey);
      if (svgGraphic) {
        return svgGraphic;
      }
      svgGraphic = this._freedGraphics.pop() || this._buildGraphic(this._parsedXML);
      usedRootMap.set(hostKey, svgGraphic);
      return svgGraphic;
    };
    GeoSVGResource2.prototype.freeGraphic = function(hostKey) {
      var usedRootMap = this._usedGraphicMap;
      var svgGraphic = usedRootMap.get(hostKey);
      if (svgGraphic) {
        usedRootMap.removeKey(hostKey);
        this._freedGraphics.push(svgGraphic);
      }
    };
    return GeoSVGResource2;
  })()
);
function setSilent(el) {
  el.silent = false;
  if (el.isGroup) {
    el.traverse(function(child) {
      child.silent = false;
    });
  }
}
function createRegions(named) {
  var regions = [];
  var regionsMap = createHashMap();
  each(named, function(namedItem) {
    if (namedItem.namedFrom != null) {
      return;
    }
    var region = new GeoSVGRegion(namedItem.name, namedItem.el);
    regions.push(region);
    regionsMap.set(namedItem.name, region);
  });
  return {
    regions,
    regionsMap
  };
}

// node_modules/echarts/lib/coord/geo/parseGeoJson.js
function decode(json) {
  if (!json.UTF8Encoding) {
    return json;
  }
  var jsonCompressed = json;
  var encodeScale = jsonCompressed.UTF8Scale;
  if (encodeScale == null) {
    encodeScale = 1024;
  }
  var features2 = jsonCompressed.features;
  each(features2, function(feature) {
    var geometry = feature.geometry;
    var encodeOffsets = geometry.encodeOffsets;
    var coordinates = geometry.coordinates;
    if (!encodeOffsets) {
      return;
    }
    switch (geometry.type) {
      case "LineString":
        geometry.coordinates = decodeRing(coordinates, encodeOffsets, encodeScale);
        break;
      case "Polygon":
        decodeRings(coordinates, encodeOffsets, encodeScale);
        break;
      case "MultiLineString":
        decodeRings(coordinates, encodeOffsets, encodeScale);
        break;
      case "MultiPolygon":
        each(coordinates, function(rings, idx) {
          return decodeRings(rings, encodeOffsets[idx], encodeScale);
        });
    }
  });
  jsonCompressed.UTF8Encoding = false;
  return jsonCompressed;
}
function decodeRings(rings, encodeOffsets, encodeScale) {
  for (var c = 0; c < rings.length; c++) {
    rings[c] = decodeRing(rings[c], encodeOffsets[c], encodeScale);
  }
}
function decodeRing(coordinate, encodeOffsets, encodeScale) {
  var result = [];
  var prevX = encodeOffsets[0];
  var prevY = encodeOffsets[1];
  for (var i2 = 0; i2 < coordinate.length; i2 += 2) {
    var x = coordinate.charCodeAt(i2) - 64;
    var y = coordinate.charCodeAt(i2 + 1) - 64;
    x = x >> 1 ^ -(x & 1);
    y = y >> 1 ^ -(y & 1);
    x += prevX;
    y += prevY;
    prevX = x;
    prevY = y;
    result.push([x / encodeScale, y / encodeScale]);
  }
  return result;
}
function parseGeoJSON(geoJson, nameProperty) {
  geoJson = decode(geoJson);
  return map2(filter2(geoJson.features, function(featureObj) {
    return featureObj.geometry && featureObj.properties && featureObj.geometry.coordinates.length > 0;
  }), function(featureObj) {
    var properties = featureObj.properties;
    var geo = featureObj.geometry;
    var geometries = [];
    switch (geo.type) {
      case "Polygon":
        var coordinates = geo.coordinates;
        geometries.push(new GeoJSONPolygonGeometry(coordinates[0], coordinates.slice(1)));
        break;
      case "MultiPolygon":
        each(geo.coordinates, function(item) {
          if (item[0]) {
            geometries.push(new GeoJSONPolygonGeometry(item[0], item.slice(1)));
          }
        });
        break;
      case "LineString":
        geometries.push(new GeoJSONLineStringGeometry([geo.coordinates]));
        break;
      case "MultiLineString":
        geometries.push(new GeoJSONLineStringGeometry(geo.coordinates));
    }
    var region = new GeoJSONRegion(properties[nameProperty || "name"], geometries, properties.cp);
    region.properties = properties;
    return region;
  });
}

// node_modules/echarts/lib/coord/geo/fix/nanhai.js
var geoCoord = [126, 25];
var nanhaiName = "南海诸岛";
var points2 = [[[0, 3.5], [7, 11.2], [15, 11.9], [30, 7], [42, 0.7], [52, 0.7], [56, 7.7], [59, 0.7], [64, 0.7], [64, 0], [5, 0], [0, 3.5]], [[13, 16.1], [19, 14.7], [16, 21.7], [11, 23.1], [13, 16.1]], [[12, 32.2], [14, 38.5], [15, 38.5], [13, 32.2], [12, 32.2]], [[16, 47.6], [12, 53.2], [13, 53.2], [18, 47.6], [16, 47.6]], [[6, 64.4], [8, 70], [9, 70], [8, 64.4], [6, 64.4]], [[23, 82.6], [29, 79.8], [30, 79.8], [25, 82.6], [23, 82.6]], [[37, 70.7], [43, 62.3], [44, 62.3], [39, 70.7], [37, 70.7]], [[48, 51.1], [51, 45.5], [53, 45.5], [50, 51.1], [48, 51.1]], [[51, 35], [51, 28.7], [53, 28.7], [53, 35], [51, 35]], [[52, 22.4], [55, 17.5], [56, 17.5], [53, 22.4], [52, 22.4]], [[58, 12.6], [62, 7], [63, 7], [60, 12.6], [58, 12.6]], [[0, 3.5], [0, 93.1], [64, 93.1], [64, 0], [63, 0], [63, 92.4], [1, 92.4], [1, 3.5], [0, 3.5]]];
for (i = 0; i < points2.length; i++) {
  for (k = 0; k < points2[i].length; k++) {
    points2[i][k][0] /= 10.5;
    points2[i][k][1] /= -10.5 / 0.75;
    points2[i][k][0] += geoCoord[0];
    points2[i][k][1] += geoCoord[1];
  }
}
var k;
var i;
function fixNanhai(mapType, regions) {
  if (mapType === "china") {
    for (var i2 = 0; i2 < regions.length; i2++) {
      if (regions[i2].name === nanhaiName) {
        return;
      }
    }
    regions.push(new GeoJSONRegion(nanhaiName, map2(points2, function(exterior) {
      return {
        type: "polygon",
        exterior
      };
    }), geoCoord));
  }
}

// node_modules/echarts/lib/coord/geo/fix/textCoord.js
var coordsOffsetMap = {
  "南海诸岛": [32, 80],
  // 全国
  "广东": [0, -10],
  "香港": [10, 5],
  "澳门": [-10, 10],
  // '北京': [-10, 0],
  "天津": [5, 5]
};
function fixTextCoords(mapType, region) {
  if (mapType === "china") {
    var coordFix = coordsOffsetMap[region.name];
    if (coordFix) {
      var cp = region.getCenter();
      cp[0] += coordFix[0] / 10.5;
      cp[1] += -coordFix[1] / (10.5 / 0.75);
      region.setCenter(cp);
    }
  }
}

// node_modules/echarts/lib/coord/geo/fix/diaoyuIsland.js
var points3 = [[[123.45165252685547, 25.73527164402261], [123.49731445312499, 25.73527164402261], [123.49731445312499, 25.750734064600884], [123.45165252685547, 25.750734064600884], [123.45165252685547, 25.73527164402261]]];
function fixDiaoyuIsland(mapType, region) {
  if (mapType === "china" && region.name === "台湾") {
    region.geometries.push({
      type: "polygon",
      exterior: points3[0]
    });
  }
}

// node_modules/echarts/lib/coord/geo/GeoJSONResource.js
var DEFAULT_NAME_PROPERTY = "name";
var GeoJSONResource = (
  /** @class */
  (function() {
    function GeoJSONResource2(mapName, geoJSON, specialAreas) {
      this.type = "geoJSON";
      this._parsedMap = createHashMap();
      this._mapName = mapName;
      this._specialAreas = specialAreas;
      this._geoJSON = parseInput(geoJSON);
    }
    GeoJSONResource2.prototype.load = function(nameMap, nameProperty) {
      nameProperty = nameProperty || DEFAULT_NAME_PROPERTY;
      var parsed = this._parsedMap.get(nameProperty);
      if (!parsed) {
        var rawRegions = this._parseToRegions(nameProperty);
        parsed = this._parsedMap.set(nameProperty, {
          regions: rawRegions,
          boundingRect: calculateBoundingRect(rawRegions)
        });
      }
      var regionsMap = createHashMap();
      var finalRegions = [];
      each(parsed.regions, function(region) {
        var regionName = region.name;
        if (nameMap && hasOwn(nameMap, regionName)) {
          region = region.cloneShallow(regionName = nameMap[regionName]);
        }
        finalRegions.push(region);
        regionsMap.set(regionName, region);
      });
      return {
        regions: finalRegions,
        boundingRect: parsed.boundingRect || new BoundingRect_default(0, 0, 0, 0),
        regionsMap
      };
    };
    GeoJSONResource2.prototype._parseToRegions = function(nameProperty) {
      var mapName = this._mapName;
      var geoJSON = this._geoJSON;
      var rawRegions;
      try {
        rawRegions = geoJSON ? parseGeoJSON(geoJSON, nameProperty) : [];
      } catch (e2) {
        throw new Error("Invalid geoJson format\n" + e2.message);
      }
      fixNanhai(mapName, rawRegions);
      each(rawRegions, function(region) {
        var regionName = region.name;
        fixTextCoords(mapName, region);
        fixDiaoyuIsland(mapName, region);
        var specialArea = this._specialAreas && this._specialAreas[regionName];
        if (specialArea) {
          region.transformTo(specialArea.left, specialArea.top, specialArea.width, specialArea.height);
        }
      }, this);
      return rawRegions;
    };
    GeoJSONResource2.prototype.getMapForUser = function() {
      return {
        // For backward compatibility, use geoJson
        // PENDING: it has been returning them without clone.
        // do we need to avoid outsite modification?
        geoJson: this._geoJSON,
        geoJSON: this._geoJSON,
        specialAreas: this._specialAreas
      };
    };
    return GeoJSONResource2;
  })()
);
function calculateBoundingRect(regions) {
  var rect;
  for (var i2 = 0; i2 < regions.length; i2++) {
    var regionRect = regions[i2].getBoundingRect();
    rect = rect || regionRect.clone();
    rect.union(regionRect);
  }
  return rect;
}
function parseInput(source) {
  return !isString(source) ? source : typeof JSON !== "undefined" && JSON.parse ? JSON.parse(source) : new Function("return (" + source + ");")();
}

// node_modules/echarts/lib/coord/geo/geoSourceManager.js
var storage = createHashMap();
var geoSourceManager_default = {
  /**
   * Compatible with previous `echarts.registerMap`.
   *
   * @usage
   * ```js
   *
   * echarts.registerMap('USA', geoJson, specialAreas);
   *
   * echarts.registerMap('USA', {
   *     geoJson: geoJson,
   *     specialAreas: {...}
   * });
   * echarts.registerMap('USA', {
   *     geoJSON: geoJson,
   *     specialAreas: {...}
   * });
   *
   * echarts.registerMap('airport', {
   *     svg: svg
   * }
   * ```
   *
   * Note:
   * Do not support that register multiple geoJSON or SVG
   * one map name. Because different geoJSON and SVG have
   * different unit. It's not easy to make sure how those
   * units are mapping/normalize.
   * If intending to use multiple geoJSON or SVG, we can
   * use multiple geo coordinate system.
   */
  registerMap: function(mapName, rawDef, rawSpecialAreas) {
    if (rawDef.svg) {
      var resource = new GeoSVGResource(mapName, rawDef.svg);
      storage.set(mapName, resource);
    } else {
      var geoJSON = rawDef.geoJson || rawDef.geoJSON;
      if (geoJSON && !rawDef.features) {
        rawSpecialAreas = rawDef.specialAreas;
      } else {
        geoJSON = rawDef;
      }
      var resource = new GeoJSONResource(mapName, geoJSON, rawSpecialAreas);
      storage.set(mapName, resource);
    }
  },
  getGeoResource: function(mapName) {
    return storage.get(mapName);
  },
  /**
   * Only for exporting to users.
   * **MUST NOT** used internally.
   */
  getMapForUser: function(mapName) {
    var resource = storage.get(mapName);
    return resource && resource.type === "geoJSON" && resource.getMapForUser();
  },
  load: function(mapName, nameMap, nameProperty) {
    var resource = storage.get(mapName);
    if (!resource) {
      if (true) {
        console.error("Map " + mapName + " not exists. The GeoJSON of the map must be provided.");
      }
      return;
    }
    return resource.load(nameMap, nameProperty);
  }
};

// node_modules/echarts/lib/coord/View.js
var VIEW_COORD_SYS_TRANS_RAW = 0;
var VIEW_COORD_SYS_TRANS_ROAM = 1;
var VIEW_COORD_SYS_TRANS_OVERALL = 2;
function inner9(viewCoordSys) {
  return viewCoordSys;
}
var VIEW_COORD_SYS_TYPE = "view";
var View = (
  /** @class */
  (function(_super) {
    __extends(View2, _super);
    function View2(invertY, legacyCenterBase, legacyGeo) {
      var _this = _super.call(this) || this;
      _this.type = VIEW_COORD_SYS_TYPE;
      _this.dimensions = ["x", "y"];
      var viewInner = inner9(_this);
      viewInner.invertY = invertY;
      viewInner.lgCt = legacyCenterBase;
      viewInner.lgGeo = legacyGeo;
      var trans = viewInner.trans = [];
      trans[VIEW_COORD_SYS_TRANS_RAW] = transformableCreate();
      trans[VIEW_COORD_SYS_TRANS_ROAM] = transformableCreate();
      trans[VIEW_COORD_SYS_TRANS_OVERALL] = transformableCreate();
      viewInner.mtRaw = create();
      viewInner.mtRawInv = create();
      viewInner.mtOverall = create();
      viewInner.mtOverallInv = create();
      viewInner.zoom = 1;
      return _this;
    }
    View2.prototype.getBoundingRect = function() {
      return viewCoordSysCopyBoundingRect(null, this);
    };
    View2.prototype.getViewRect = function() {
      return viewCoordSysCopyViewRect(null, this);
    };
    View2.prototype.getRoamTransform = function() {
      return transformableGetLocalTransform(inner9(this).trans[VIEW_COORD_SYS_TRANS_ROAM]);
    };
    View2.prototype.dataToPoint = function(data, noRoam, out2) {
      var transform = noRoam ? inner9(this).mtRaw : inner9(this).mtOverall;
      out2 = out2 || [];
      return transform ? applyTransform(out2, data, transform) : copy2(out2, data);
    };
    View2.prototype.pointToData = function(point, reserved, out2) {
      out2 = out2 || [];
      var invTransform = inner9(this).mtOverallInv;
      return invTransform ? applyTransform(out2, point, invTransform) : copy2(out2, point);
    };
    View2.prototype.convertToPixel = function(ecModel, finder, value) {
      var coordSys = getCoordSys(finder);
      return coordSys === this ? coordSys.dataToPoint(value) : null;
    };
    View2.prototype.convertFromPixel = function(ecModel, finder, pixel) {
      var coordSys = getCoordSys(finder);
      return coordSys === this ? coordSys.pointToData(pixel) : null;
    };
    View2.prototype.containPoint = function(point) {
      var viewInner = inner9(this);
      boundingRectCopy(tmpPixelRectForContain, viewInner.dataRect);
      boundingRectApplyTransform(tmpPixelRectForContain, tmpPixelRectForContain, viewInner.mtOverall);
      return boundingRectContain(tmpPixelRectForContain, point[0], point[1]);
    };
    View2.dimensions = ["x", "y"];
    return View2;
  })(Transformable_default)
);
var tmpPixelRectForContain = boundingRectCreate();
function viewCoordSysCopyOverallMatrix(out2, viewCoordSys) {
  return copy(out2 || [], inner9(viewCoordSys).mtOverall);
}
function viewCoordSysGetZoomOption(viewCoordSys) {
  return inner9(viewCoordSys).zoom;
}
function viewCoordSysCopyBoundingRect(out2, viewCoordSys) {
  return boundingRectCopy(out2 || boundingRectCreate(), inner9(viewCoordSys).dataRect);
}
function viewCoordSysCopyViewRect(out2, viewCoordSys) {
  return boundingRectCopy(out2 || boundingRectCreate(), inner9(viewCoordSys).viewRect);
}
function viewCoordSysCopyTrans(out2, viewCoordSys, transKind) {
  return copyTransform(out2 || transformableCreate(), inner9(viewCoordSys).trans[transKind]);
}
function viewCoordSysIsInputReady(viewInner) {
  return !!(viewInner.dataRect && viewInner.viewRect);
}
var tmpMtRTO = create();
function viewCoordSysSetRoamOptionFromModel(viewCoordSys, hostModel) {
  var viewInner = inner9(viewCoordSys);
  viewInner.centerOption = hostModel.getShallow("center");
  var zoomLimit = viewInner.zoomLimit = hostModel.getShallow("scaleLimit");
  var zoomOption = hostModel.getShallow("zoom");
  viewInner.zoom = clampByZoomLimit(zoomOption || 1, zoomLimit) || 1;
  if (viewCoordSysIsInputReady(viewInner)) {
    viewCoordSysUpdateTransform(viewInner);
  }
}
function viewCoordSysSetBoundingRect(viewCoordSys, x, y, width, height) {
  var viewInner = inner9(viewCoordSys);
  viewInner.dataRect = new BoundingRect_default(x, y, width, height);
  if (viewCoordSysIsInputReady(viewInner)) {
    viewCoordSysUpdateTransform(viewInner);
  }
}
function viewCoordSysSetViewRect(viewCoordSys, x, y, width, height) {
  var viewInner = inner9(viewCoordSys);
  viewInner.viewRect = new BoundingRect_default(x, y, width, height);
  if (viewCoordSysIsInputReady(viewInner)) {
    viewCoordSysUpdateTransform(viewInner);
  }
}
function viewCoordSysUpdateTransform(viewInner) {
  viewCoordSysUpdateRawTrans(viewInner);
  viewCoordSysUpdateRoamTrans(viewInner);
  viewCoordSysUpdateOverallTrans(viewInner);
}
function viewCoordSysUpdateRawTrans(viewInner) {
  var dataRect = viewInner.dataRect;
  var viewRect2 = viewInner.viewRect;
  var rawTrans = viewInner.trans[VIEW_COORD_SYS_TRANS_RAW];
  var invertY = viewInner.invertY;
  if (invertY) {
    dataRect = boundingRectCopy(tmpRectURT, dataRect);
    dataRect.y = -dataRect.y - dataRect.height;
  }
  boundingRectCalculateTransform(tmpMtURT, dataRect, viewRect2);
  decomposeTransform(rawTrans, tmpMtURT);
  if (invertY) {
    rawTrans.scaleY = -rawTrans.scaleY;
  }
  var mtRaw = transformableGetLocalTransform(rawTrans, viewInner.mtRaw);
  invert(viewInner.mtRawInv, mtRaw);
}
var tmpMtURT = create();
var tmpRectURT = boundingRectCreate();
function viewCoordSysUpdateRoamTrans(viewInner) {
  var viewRectCenter = viewCoordSysGetViewRectCenter(viewInner);
  var roamViewCenter = parseCenterOption(tmpCenterURT, viewInner, viewInner.centerOption) ? applyTransform(tmpCenterURT, tmpCenterURT, viewInner.mtRaw) : viewRectCenter;
  var zoom = viewInner.zoom;
  var roamTrans = viewInner.trans[VIEW_COORD_SYS_TRANS_ROAM];
  roamTrans.x = viewRectCenter[0] - zoom * roamViewCenter[0];
  roamTrans.y = viewRectCenter[1] - zoom * roamViewCenter[1];
  roamTrans.scaleX = roamTrans.scaleY = zoom;
}
var tmpCenterURT = [];
function viewCoordSysUpdateOverallTrans(viewInner) {
  var trans = viewInner.trans;
  var roamTrans = trans[VIEW_COORD_SYS_TRANS_ROAM];
  var rawTrans = trans[VIEW_COORD_SYS_TRANS_RAW];
  var overallTrans = trans[VIEW_COORD_SYS_TRANS_OVERALL];
  calcOverallTrans(overallTrans, rawTrans, roamTrans);
  var mtOverall = transformableGetLocalTransform(overallTrans, viewInner.mtOverall);
  var mtOverallInv = invert(viewInner.mtOverallInv, mtOverall);
  legacyCopyOverallTrans(viewInner, overallTrans, mtOverall, mtOverallInv);
  legacyCopyOverallTrans(viewInner.lgGeo, overallTrans, mtOverall, mtOverallInv);
}
function legacyCopyOverallTrans(target, overallTrans, mtOverall, mtOverallInv) {
  if (target) {
    copyTransform(target, overallTrans);
    copy(target.transform || (target.transform = []), mtOverall);
    copy(target.invTransform || (target.invTransform = []), mtOverallInv);
  }
}
function calcOverallTrans(out2, rawTrans, roamTrans) {
  transformableGetLocalTransform(rawTrans, tmpMtCOT1);
  transformableGetLocalTransform(roamTrans, tmpMtCOT2);
  mul(tmpMtCOT2, tmpMtCOT2, tmpMtCOT1);
  decomposeTransform(out2, tmpMtCOT2);
}
var tmpMtCOT1 = create();
var tmpMtCOT2 = create();
function getCoordSys(finder) {
  var seriesModel = finder.seriesModel;
  return seriesModel ? seriesModel.coordinateSystem : null;
}
function viewCoordSysGetViewRectCenter(viewInner) {
  var viewRect2 = viewInner.viewRect;
  tmpViewRectCenter[0] = viewRect2.x + viewRect2.width / 2;
  tmpViewRectCenter[1] = viewRect2.y + viewRect2.height / 2;
  return tmpViewRectCenter;
}
var tmpViewRectCenter = [];
function isViewCoordSys(coordSys) {
  return coordSys && coordSys.type === "view";
}
function applyViewCoordSysTransToElement(syncBackEl, syncBackType, viewCoordSys, animatableModel) {
  var viewInner = inner9(viewCoordSys);
  viewInner.syncBackEl = syncBackEl;
  viewInner.syncBackType = syncBackType;
  if (!animatableModel) {
    viewCoordSysCopyTrans(syncBackEl, viewCoordSys, syncBackType);
    syncBackEl.dirty();
  } else {
    updateProps(syncBackEl, viewCoordSysCopyTrans(null, viewCoordSys, syncBackType), animatableModel);
  }
}
var tmpTransSB1 = transformableCreate();
var tmpTransSB2 = transformableCreate();
function getOwnRoamViewCoordSys(hostModel) {
  return hostModel.__ownRoamView ? hostModel.__ownRoamView() : null;
}
function parseCenterOption(out2, viewInner, centerOption) {
  if (true) {
    assert(viewCoordSysIsInputReady(viewInner));
  }
  var dataRect = viewInner.dataRect;
  if (!centerOption) {
    return false;
  }
  var lgCt = viewInner.lgCt;
  if (lgCt) {
    set(out2, parsePercent2(centerOption[0], lgCt.w), parsePercent2(centerOption[1], lgCt.h));
  } else if (dataRect) {
    set(out2, parsePercent2(centerOption[0], dataRect.width, dataRect.x), parsePercent2(centerOption[1], dataRect.height, dataRect.y));
  }
  return true;
}
function useLegacyViewCoordSysCenterBase(ecModel, api) {
  return api && ecModel && ecModel.getShallow("legacyViewCoordSysCenterBase") ? {
    w: api.getWidth(),
    h: api.getHeight()
  } : null;
}
function clampByZoomLimit(zoom, zoomLimit) {
  if (zoomLimit) {
    var zoomMin = zoomLimit.min || 0;
    var zoomMax = zoomLimit.max || Infinity;
    zoom = Math.max(Math.min(zoomMax, zoom), zoomMin);
  }
  return zoom;
}
function calcCompensationScaleToPreserveNodeSize(viewCoordSys, model2) {
  var nodeScaleRatio = model2.getShallow("nodeScaleRatio", true) || 1;
  var viewInner = inner9(viewCoordSys);
  return ((viewInner.zoom - 1) * nodeScaleRatio + 1) / (viewInner.trans[VIEW_COORD_SYS_TRANS_OVERALL].scaleX || 1);
}
var View_default = View;

// node_modules/echarts/lib/component/helper/roamHelper.js
function updateRoamControllerSimply(componentOrSeries, api, controller, isInSelf, clipRect, extraOnRoam, roamTypeDefault, geoBackwardCompat) {
  var coordSys = getOwnRoamViewCoordSys(componentOrSeries);
  if (!coordSys) {
    controller.disable();
    return;
  }
  controller.enable(
    // NOTE:
    //  If roamTypeDefault is null/undefined, roamType will be set as true.
    // PENDING:
    //  In MapSeries case, it has long been retrieving `roam` option from the first MapSeries
    //  that are not filtered out by a legend. But that requires `roam: true` to be set in all
    //  MapSeries option, otherwise if a legend hides the `roam: true` MapSeries, the map can
    //  not be roamed unexpectedly.
    retrieve2(componentOrSeries.get("roam"), roamTypeDefault),
    {
      api,
      zInfo: {
        component: componentOrSeries
      },
      triggerInfo: {
        roamTrigger: componentOrSeries.get("roamTrigger"),
        isInSelf,
        isInClip: function(e2, x, y) {
          return !clipRect || clipRect.contain(x, y);
        }
      }
    }
  );
  function dispatchAction3(extra) {
    var mainType = componentOrSeries.mainType;
    var payload = payloadDisableAnimation(defaults({
      type: makeViewCoordSysActionType(mainType, componentOrSeries.subType, ROAM_ACTION_TYPE_SUFFIX)
    }, extra));
    if (geoBackwardCompat) {
      payload.componentType = mainType;
    }
    payload[mainType + "Id"] = componentOrSeries.id;
    api.dispatchAction(payload);
  }
  controller.off("pan").off("zoom").on("pan", function(event) {
    extraOnRoam && extraOnRoam("pan");
    dispatchAction3({
      dx: event.dx,
      dy: event.dy
    });
  }).on("zoom", function(event) {
    extraOnRoam && extraOnRoam("zoom");
    dispatchAction3({
      zoom: event.scale,
      originX: event.originX,
      originY: event.originY
    });
  });
}
function createIsInSelfByPointerCheckerEl(pointerCheckerEl) {
  return function(e2, x, y) {
    tmpRectCII.copy(pointerCheckerEl.getBoundingRect());
    tmpRectCII.applyTransform(pointerCheckerEl.getComputedTransform());
    return tmpRectCII.contain(x, y);
  };
}
var tmpRectCII = new BoundingRect_default(0, 0, 0, 0);
function makeViewCoordSysActionType(mainType, subType, suffix) {
  return (mainType !== COMPONENT_MAIN_TYPE_SERIES ? mainType : subType === "map" ? "geo" : subType) + suffix;
}
function isRoamPayloadHasZoom(payload) {
  return payload.zoom != null;
}
function createViewCoordSysSimply(componentOrSeries, api, x, y, width, height, viewRect2) {
  var viewCoordSys = new View_default(null, useLegacyViewCoordSysCenterBase(componentOrSeries.ecModel, api));
  viewCoordSysSetBoundingRect(viewCoordSys, x, y, width, height);
  viewRect2 ? viewCoordSysSetViewRect(viewCoordSys, viewRect2.x, viewRect2.y, viewRect2.width, viewRect2.height) : viewCoordSysSetViewRect(viewCoordSys, x, y, width, height);
  viewCoordSysSetRoamOptionFromModel(viewCoordSys, componentOrSeries);
  return viewCoordSys;
}

// node_modules/echarts/lib/component/helper/MapDraw.js
var OPTION_STYLE_ENABLED_TAGS = ["rect", "circle", "line", "ellipse", "polygon", "polyline", "path"];
var OPTION_STYLE_ENABLED_TAG_MAP = createHashMap(OPTION_STYLE_ENABLED_TAGS);
var STATE_TRIGGER_TAG_MAP = createHashMap(OPTION_STYLE_ENABLED_TAGS.concat(["g"]));
var LABEL_HOST_MAP = createHashMap(OPTION_STYLE_ENABLED_TAGS.concat(["g"]));
var mapLabelRaw = makeInner();
function getFixedItemStyle(model2) {
  var itemStyle = model2.getItemStyle();
  var areaColor = model2.get("areaColor");
  if (areaColor != null) {
    itemStyle.fill = areaColor;
  }
  return itemStyle;
}
function fixLineStyle(styleHost) {
  var style2 = styleHost.style;
  if (style2) {
    style2.stroke = style2.stroke || style2.fill;
    style2.fill = null;
  }
}
var MapDraw = (
  /** @class */
  (function() {
    function MapDraw2(api) {
      var group2 = this.group = new Group_default();
      var transformGroup = this._transformGroup = new Group_default();
      group2.add(transformGroup);
      this.uid = getUID("ec_map_draw");
      this._controller = new RoamController_default(api.getZr());
      transformGroup.add(this._regionsGroup = new Group_default());
      transformGroup.add(this._svgGroup = new Group_default());
    }
    MapDraw2.prototype.draw = function(mapOrGeoModel, ecModel, api, fromView, payload) {
      var mapDraw = this;
      var data = mapOrGeoModel.getData && mapOrGeoModel.getData();
      if (isGeoModel(mapOrGeoModel)) {
        ecModel.eachComponent({
          mainType: "series",
          subType: "map"
        }, function(mapSeries) {
          if (!data && mapSeries.getHostGeoModel() === mapOrGeoModel) {
            data = mapSeries.getData();
          }
        });
      }
      var geo = mapOrGeoModel.coordinateSystem;
      var viewCoordSys = geo.view;
      var regionsGroup = this._regionsGroup;
      var transformGroup = this._transformGroup;
      var isFirstDraw = !regionsGroup.childAt(0) || payload;
      var clipRect;
      if (geo.shouldClip()) {
        clipRect = viewCoordSysCopyViewRect(null, viewCoordSys);
        this.group.setClipPath(new Rect_default({
          shape: clipRect.clone()
        }));
      } else {
        this.group.removeClipPath();
      }
      applyViewCoordSysTransToElement(transformGroup, VIEW_COORD_SYS_TRANS_ROAM, viewCoordSys, isFirstDraw ? null : mapOrGeoModel);
      var isVisualEncodedByVisualMap = data && data.getVisual("visualMeta") && data.getVisual("visualMeta").length > 0;
      if (geo.resourceType === "geoJSON") {
        this._buildGeoJSON(viewCoordSys, api, geo, mapOrGeoModel, data, isVisualEncodedByVisualMap);
      } else if (geo.resourceType === "geoSVG") {
        this._buildSVG(viewCoordSys, api, geo, mapOrGeoModel, data, isVisualEncodedByVisualMap);
      }
      updateRoamControllerSimply(
        mapOrGeoModel,
        api,
        this._controller,
        function(e2, x, y) {
          return mapOrGeoModel.coordinateSystem.containPoint([x, y]);
        },
        clipRect,
        function() {
          mapDraw._mouseDownFlag = false;
        },
        false,
        // Default roam type.
        true
      );
      this._updateMapSelectHandler(mapOrGeoModel, regionsGroup, api, fromView);
    };
    MapDraw2.prototype.__updateOnOwnRoam = function(mapOrGeoModel) {
      applyViewCoordSysTransToElement(this._transformGroup, VIEW_COORD_SYS_TRANS_ROAM, mapOrGeoModel.coordinateSystem.view, null);
    };
    MapDraw2.prototype._buildGeoJSON = function(viewCoordSys, api, geo, mapOrGeoModel, data, isVisualEncodedByVisualMap) {
      var regionsGroupByName = this._regionsGroupByName = createHashMap();
      var regionsInfoByName = createHashMap();
      var regionsGroup = this._regionsGroup;
      var projection = geo.projection;
      var projectionStream = projection && projection.stream;
      var transMt = transformableGetLocalTransform(viewCoordSysCopyTrans(null, viewCoordSys, VIEW_COORD_SYS_TRANS_RAW));
      function transformPoint(point, project) {
        if (project) {
          point = project(point);
        }
        return point && applyTransform([], point, transMt);
      }
      ;
      function transformPolygonPoints(inPoints) {
        var outPoints = [];
        var project = !projectionStream && projection && projection.project;
        for (var i2 = 0; i2 < inPoints.length; ++i2) {
          var newPt = transformPoint(inPoints[i2], project);
          newPt && outPoints.push(newPt);
        }
        return outPoints;
      }
      function getPolyShape(points4) {
        return {
          shape: {
            points: transformPolygonPoints(points4)
          }
        };
      }
      regionsGroup.removeAll();
      each(geo.regions, function(region) {
        var regionName = region.name;
        var regionGroup = regionsGroupByName.get(regionName);
        var _a2 = regionsInfoByName.get(regionName) || {}, dataIdx = _a2.dataIdx, regionModel = _a2.regionModel;
        if (!regionGroup) {
          regionGroup = regionsGroupByName.set(regionName, new Group_default());
          regionsGroup.add(regionGroup);
          dataIdx = data ? data.indexOfName(regionName) : null;
          regionModel = isGeoModel(mapOrGeoModel) ? mapOrGeoModel.getRegionModel(regionName) : data ? data.getItemModel(dataIdx) : null;
          var silent = regionModel.get("silent", true);
          silent != null && (regionGroup.silent = silent);
          regionsInfoByName.set(regionName, {
            dataIdx,
            regionModel
          });
        }
        var polygonSubpaths = [];
        var polylineSubpaths = [];
        each(region.geometries, function(geometry) {
          if (geometry.type === "polygon") {
            var polys = [geometry.exterior].concat(geometry.interiors || []);
            if (projectionStream) {
              polys = projectPolys(polys, projectionStream);
            }
            each(polys, function(poly) {
              polygonSubpaths.push(new Polygon_default(getPolyShape(poly)));
            });
          } else {
            var points4 = geometry.points;
            if (projectionStream) {
              points4 = projectPolys(points4, projectionStream, true);
            }
            each(points4, function(points5) {
              polylineSubpaths.push(new Polyline_default(getPolyShape(points5)));
            });
          }
        });
        var centerPt = transformPoint(region.getCenter(), projection && projection.project);
        function createCompoundPath(subpaths, isLine) {
          if (!subpaths.length) {
            return;
          }
          var compoundPath = new CompoundPath_default({
            culling: true,
            segmentIgnoreThreshold: 1,
            shape: {
              paths: subpaths
            }
          });
          regionGroup.add(compoundPath);
          applyOptionStyleForRegion(api, data, isVisualEncodedByVisualMap, compoundPath, dataIdx, regionModel);
          resetLabelForRegion(mapOrGeoModel, data, compoundPath, regionName, regionModel, dataIdx, centerPt);
          if (isLine) {
            fixLineStyle(compoundPath);
            each(compoundPath.states, fixLineStyle);
          }
        }
        createCompoundPath(polygonSubpaths);
        createCompoundPath(polylineSubpaths, true);
      });
      regionsGroupByName.each(function(regionGroup, regionName) {
        var _a2 = regionsInfoByName.get(regionName), dataIdx = _a2.dataIdx, regionModel = _a2.regionModel;
        resetEventTriggerForRegion(mapOrGeoModel, data, regionGroup, regionName, regionModel, dataIdx);
        resetTooltipForRegion(mapOrGeoModel, data, regionGroup, regionName, regionModel);
        resetStateTriggerForRegion(mapOrGeoModel, regionGroup, regionName, regionModel);
      }, this);
    };
    MapDraw2.prototype._buildSVG = function(viewCoordSys, api, geo, mapOrGeoModel, data, isVisualEncodedByVisualMap) {
      var mapName = geo.map;
      viewCoordSysCopyTrans(this._svgGroup, viewCoordSys, VIEW_COORD_SYS_TRANS_RAW);
      if (this._svgResourceChanged(mapName)) {
        this._freeSVG();
        this._useSVG(mapName);
      }
      var svgDispatcherMap = this._svgDispatcherMap = createHashMap();
      var focusSelf = false;
      each(this._svgGraphicRecord.named, function(namedItem) {
        var regionName = namedItem.name;
        var svgNodeTagLower = namedItem.svgNodeTagLower;
        var el = namedItem.el;
        var dataIdx = data ? data.indexOfName(regionName) : null;
        var regionModel = mapOrGeoModel.getRegionModel(regionName);
        if (OPTION_STYLE_ENABLED_TAG_MAP.get(svgNodeTagLower) != null && el instanceof Displayable_default) {
          applyOptionStyleForRegion(api, data, isVisualEncodedByVisualMap, el, dataIdx, regionModel);
        }
        if (el instanceof Displayable_default) {
          el.culling = true;
        }
        var silent = regionModel.get("silent", true);
        silent != null && (el.silent = silent);
        el.z2EmphasisLift = 0;
        if (!namedItem.namedFrom) {
          if (LABEL_HOST_MAP.get(svgNodeTagLower) != null) {
            resetLabelForRegion(mapOrGeoModel, data, el, regionName, regionModel, dataIdx, null);
          }
          resetEventTriggerForRegion(mapOrGeoModel, data, el, regionName, regionModel, dataIdx);
          resetTooltipForRegion(mapOrGeoModel, data, el, regionName, regionModel);
          if (STATE_TRIGGER_TAG_MAP.get(svgNodeTagLower) != null) {
            var focus_1 = resetStateTriggerForRegion(mapOrGeoModel, el, regionName, regionModel);
            if (focus_1 === "self") {
              focusSelf = true;
            }
            var els = svgDispatcherMap.get(regionName) || svgDispatcherMap.set(regionName, []);
            els.push(el);
          }
        }
      }, this);
      this._enableBlurEntireSVG(focusSelf, mapOrGeoModel);
    };
    MapDraw2.prototype._enableBlurEntireSVG = function(focusSelf, mapOrGeoModel) {
      if (focusSelf && isGeoModel(mapOrGeoModel)) {
        var blurStyle = mapOrGeoModel.getModel(["blur", "itemStyle"]).getItemStyle();
        var opacity_1 = blurStyle.opacity;
        this._svgGraphicRecord.root.traverse(function(el) {
          if (!el.isGroup) {
            setDefaultStateProxy(el);
            var style2 = el.ensureState("blur").style || {};
            if (style2.opacity == null && opacity_1 != null) {
              style2.opacity = opacity_1;
            }
            el.ensureState("emphasis");
          }
        });
      }
    };
    MapDraw2.prototype.remove = function() {
      this._regionsGroup.removeAll();
      this._regionsGroupByName = null;
      this._svgGroup.removeAll();
      this._freeSVG();
      this._controller.disable();
    };
    MapDraw2.prototype.findHighDownDispatchers = function(name, geoModel) {
      if (name == null) {
        return [];
      }
      var geo = geoModel.coordinateSystem;
      if (geo.resourceType === "geoJSON") {
        var regionsGroupByName = this._regionsGroupByName;
        if (regionsGroupByName) {
          var regionGroup = regionsGroupByName.get(name);
          return regionGroup ? [regionGroup] : [];
        }
      } else if (geo.resourceType === "geoSVG") {
        return this._svgDispatcherMap && this._svgDispatcherMap.get(name) || [];
      }
    };
    MapDraw2.prototype._svgResourceChanged = function(mapName) {
      return this._svgMapName !== mapName;
    };
    MapDraw2.prototype._useSVG = function(mapName) {
      var resource = geoSourceManager_default.getGeoResource(mapName);
      if (resource && resource.type === "geoSVG") {
        var svgGraphic = resource.useGraphic(this.uid);
        this._svgGroup.add(svgGraphic.root);
        this._svgGraphicRecord = svgGraphic;
        this._svgMapName = mapName;
      }
    };
    MapDraw2.prototype._freeSVG = function() {
      var mapName = this._svgMapName;
      if (mapName == null) {
        return;
      }
      var resource = geoSourceManager_default.getGeoResource(mapName);
      if (resource && resource.type === "geoSVG") {
        resource.freeGraphic(this.uid);
      }
      this._svgGraphicRecord = null;
      this._svgDispatcherMap = null;
      this._svgGroup.removeAll();
      this._svgMapName = null;
    };
    MapDraw2.prototype.resetForLabelLayout = function() {
      this.group.traverse(function(el) {
        var label = el.getTextContent();
        if (label) {
          label.ignore = mapLabelRaw(label).ignore;
        }
      });
    };
    MapDraw2.prototype._updateMapSelectHandler = function(mapOrGeoModel, regionsGroup, api, fromView) {
      var mapDraw = this;
      regionsGroup.off("mousedown");
      regionsGroup.off("click");
      if (mapOrGeoModel.get("selectedMode")) {
        regionsGroup.on("mousedown", function() {
          mapDraw._mouseDownFlag = true;
        });
        regionsGroup.on("click", function(e2) {
          if (!mapDraw._mouseDownFlag) {
            return;
          }
          mapDraw._mouseDownFlag = false;
        });
      }
    };
    return MapDraw2;
  })()
);
function applyOptionStyleForRegion(api, data, isVisualEncodedByVisualMap, el, dataIndex, regionModel) {
  var normalStyleModel = regionModel.getModel("itemStyle");
  var emphasisStyleModel = regionModel.getModel(["emphasis", "itemStyle"]);
  var blurStyleModel = regionModel.getModel(["blur", "itemStyle"]);
  var selectStyleModel = regionModel.getModel(["select", "itemStyle"]);
  var normalStyle = getFixedItemStyle(normalStyleModel);
  var emphasisStyle = getFixedItemStyle(emphasisStyleModel);
  var selectStyle = getFixedItemStyle(selectStyleModel);
  var blurStyle = getFixedItemStyle(blurStyleModel);
  if (data) {
    var style2 = data.getItemVisual(dataIndex, "style");
    var decal = data.getItemVisual(dataIndex, "decal");
    if (isVisualEncodedByVisualMap && style2.fill) {
      normalStyle.fill = style2.fill;
    }
    if (decal) {
      normalStyle.decal = createOrUpdatePatternFromDecal(decal, api);
    }
  }
  el.setStyle(normalStyle);
  el.style.strokeNoScale = true;
  el.ensureState("emphasis").style = emphasisStyle;
  el.ensureState("select").style = selectStyle;
  el.ensureState("blur").style = blurStyle;
  setDefaultStateProxy(el);
}
function resetLabelForRegion(mapOrGeoModel, data, el, regionName, regionModel, dataIdx, labelXY) {
  var isDataNaN = data && isNaN(data.get(data.mapDimension("value"), dataIdx));
  var itemLayout = data && data.getItemLayout(dataIdx);
  if (isGeoModel(mapOrGeoModel) || isDataNaN || itemLayout && itemLayout.showLabel) {
    var query2 = !isGeoModel(mapOrGeoModel) ? dataIdx : regionName;
    var labelFetcher = void 0;
    if (!data || dataIdx >= 0) {
      labelFetcher = mapOrGeoModel;
    }
    var specifiedTextOpt = labelXY ? {
      normal: {
        align: "center",
        verticalAlign: "middle"
      }
    } : null;
    setLabelStyle(el, getLabelStatesModels(regionModel), {
      labelFetcher,
      labelDataIndex: query2,
      defaultText: regionName
    }, specifiedTextOpt);
    var textEl = el.getTextContent();
    if (textEl) {
      mapLabelRaw(textEl).ignore = textEl.ignore;
      if (el.textConfig && labelXY) {
        var rect = el.getBoundingRect().clone();
        el.textConfig.layoutRect = rect;
        el.textConfig.position = [(labelXY[0] - rect.x) / rect.width * 100 + "%", (labelXY[1] - rect.y) / rect.height * 100 + "%"];
      }
    }
    el.disableLabelAnimation = true;
  } else {
    el.removeTextContent();
    el.removeTextConfig();
    el.disableLabelAnimation = null;
  }
}
function resetEventTriggerForRegion(mapOrGeoModel, data, eventTrigger, regionName, regionModel, dataIdx) {
  if (data) {
    data.setItemGraphicEl(dataIdx, eventTrigger);
  } else {
    getECData(eventTrigger).eventData = {
      componentType: "geo",
      componentIndex: mapOrGeoModel.componentIndex,
      geoIndex: mapOrGeoModel.componentIndex,
      name: regionName,
      region: regionModel && regionModel.option || {}
    };
  }
}
function resetTooltipForRegion(mapOrGeoModel, data, el, regionName, regionModel) {
  if (!data) {
    setTooltipConfig({
      el,
      componentModel: mapOrGeoModel,
      itemName: regionName,
      // @ts-ignore FIXME:TS fix the "compatible with each other"?
      itemTooltipOption: regionModel.get("tooltip")
    });
  }
}
function resetStateTriggerForRegion(mapOrGeoModel, el, regionName, regionModel) {
  el.highDownSilentOnTouch = !!mapOrGeoModel.get("selectedMode");
  var emphasisModel = regionModel.getModel("emphasis");
  var focus = emphasisModel.get("focus");
  toggleHoverEmphasis(el, focus, emphasisModel.get("blurScope"), emphasisModel.get("disabled"));
  if (isGeoModel(mapOrGeoModel)) {
    enableComponentHighDownFeatures(el, mapOrGeoModel, regionName);
  }
  return focus;
}
function projectPolys(rings, createStream, isLine) {
  var polygons = [];
  var curPoly;
  function startPolygon() {
    curPoly = [];
  }
  function endPolygon() {
    if (curPoly.length) {
      polygons.push(curPoly);
      curPoly = [];
    }
  }
  var stream = createStream({
    polygonStart: startPolygon,
    polygonEnd: endPolygon,
    lineStart: startPolygon,
    lineEnd: endPolygon,
    point: function(x, y) {
      if (isFinite(x) && isFinite(y)) {
        curPoly.push([x, y]);
      }
    },
    sphere: function() {
    }
  });
  !isLine && stream.polygonStart();
  each(rings, function(ring) {
    stream.lineStart();
    for (var i2 = 0; i2 < ring.length; i2++) {
      stream.point(ring[i2][0], ring[i2][1]);
    }
    stream.lineEnd();
  });
  !isLine && stream.polygonEnd();
  return polygons;
}
function isGeoModel(mapOrGeoModel) {
  return mapOrGeoModel.mainType === "geo";
}
var MapDraw_default = MapDraw;

// node_modules/echarts/lib/chart/map/MapSeries.js
var SERIES_TYPE_MAP = "map";
var MapSeries = (
  /** @class */
  (function(_super) {
    __extends(MapSeries2, _super);
    function MapSeries2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = MapSeries2.type;
      _this.getTooltipPosition = function(dataIndex) {
        if (dataIndex != null) {
          var name_1 = this.getData().getName(dataIndex);
          var geo = this.coordinateSystem;
          var region = geo.getRegion(name_1);
          return region && geo.dataToPoint(region.getCenter());
        }
      };
      return _this;
    }
    MapSeries2.prototype.getInitialData = function(option) {
      var data = createSeriesDataSimply(this, {
        coordDimensions: ["value"],
        encodeDefaulter: curry(makeSeriesEncodeForNameBased, this)
      });
      var dataNameIndexMap = createHashMap();
      var toAppendItems = [];
      for (var i2 = 0, len2 = data.count(); i2 < len2; i2++) {
        var name_2 = data.getName(i2);
        dataNameIndexMap.set(name_2, i2);
      }
      var geoSource = geoSourceManager_default.load(this.getMapType(), this.option.nameMap, this.option.nameProperty);
      each(geoSource.regions, function(region) {
        var name = region.name;
        var dataNameIdx = dataNameIndexMap.get(name);
        var specifiedGeoJSONRegionStyle = region.properties && region.properties.echartsStyle;
        var dataItem;
        if (dataNameIdx == null) {
          dataItem = {
            name
          };
          toAppendItems.push(dataItem);
        } else {
          dataItem = data.getRawDataItem(dataNameIdx);
        }
        specifiedGeoJSONRegionStyle && merge2(dataItem, specifiedGeoJSONRegionStyle);
      });
      data.appendData(toAppendItems);
      return data;
    };
    MapSeries2.prototype.getHostGeoModel = function() {
      if (decideCoordSysUsageKind(this).kind === COORD_SYS_USAGE_KIND_BOX) {
        return;
      }
      return this.getReferringComponents("geo", {
        useDefault: false,
        enableAll: false,
        enableNone: false
      }).models[0];
    };
    MapSeries2.prototype.getMapType = function() {
      return (this.getHostGeoModel() || this).option.map;
    };
    MapSeries2.prototype.getRawValue = function(dataIndex) {
      var data = this.getData();
      return data.get(data.mapDimension("value"), dataIndex);
    };
    MapSeries2.prototype.getRegionModel = function(regionName) {
      var data = this.getData();
      return data.getItemModel(data.indexOfName(regionName));
    };
    MapSeries2.prototype.formatTooltip = function(dataIndex, multipleSeries, dataType) {
      var data = this.getData();
      var value = this.getRawValue(dataIndex);
      var name = data.getName(dataIndex);
      var seriesNames = [];
      each(this.seriesGroup.f, function(mapSeries) {
        var otherIndex = mapSeries.originalData.indexOfName(name);
        var valueDim = data.mapDimension("value");
        if (!isNaN(mapSeries.originalData.get(valueDim, otherIndex))) {
          seriesNames.push(mapSeries.name);
        }
      });
      return createTooltipMarkup("section", {
        header: seriesNames.join(", "),
        noHeader: !seriesNames.length,
        blocks: [createTooltipMarkup("nameValue", {
          name,
          value
        })]
      });
    };
    MapSeries2.prototype.getLegendIcon = function(opt) {
      var iconType = opt.icon || "roundRect";
      var icon = createSymbol(iconType, 0, 0, opt.itemWidth, opt.itemHeight, opt.itemStyle.fill);
      icon.setStyle(opt.itemStyle);
      icon.style.stroke = "none";
      if (iconType.indexOf("empty") > -1) {
        icon.style.stroke = icon.style.fill;
        icon.style.fill = tokens_default.color.neutral00;
        icon.style.lineWidth = 2;
      }
      return icon;
    };
    MapSeries2.prototype.__ownRoamView = function() {
      return mapSeriesNeedsDrawMap(this) ? this.coordinateSystem.view : null;
    };
    MapSeries2.type = "series." + SERIES_TYPE_MAP;
    MapSeries2.dependencies = ["geo"];
    MapSeries2.layoutMode = "box";
    MapSeries2.defaultOption = {
      // 一级层叠
      // zlevel: 0,
      // 二级层叠
      z: 2,
      coordinateSystem: "geo",
      // map should be explicitly specified since ec3.
      map: "",
      // If `geoIndex` is not specified, a exclusive geo will be
      // created. Otherwise use the specified geo component, and
      // `map` and `mapType` are ignored.
      // geoIndex: 0,
      // 'center' | 'left' | 'right' | 'x%' | {number}
      left: "center",
      // 'center' | 'top' | 'bottom' | 'x%' | {number}
      top: "center",
      // right
      // bottom
      // width:
      // height
      // Aspect is width / height. Inited to be geoJson bbox aspect
      // This parameter is used for scale this aspect
      // Default value:
      // for geoSVG source: 1,
      // for geoJSON source: 0.75.
      aspectScale: null,
      // Layout with center and size
      // If you want to put map in a fixed size box with right aspect ratio
      // This two properties may be more convenient.
      // layoutCenter: [50%, 50%]
      // layoutSize: 100
      showLegendSymbol: true,
      // Define left-top, right-bottom coords to control view
      // For example, [ [180, 90], [-180, -90] ],
      // higher priority than center and zoom
      boundingCoords: null,
      // Default on center of map
      center: null,
      zoom: 1,
      scaleLimit: null,
      selectedMode: true,
      label: {
        show: false,
        color: tokens_default.color.tertiary
      },
      // scaleLimit: null,
      itemStyle: {
        borderWidth: 0.5,
        borderColor: tokens_default.color.border,
        areaColor: tokens_default.color.background
      },
      emphasis: {
        label: {
          show: true,
          color: tokens_default.color.primary
        },
        itemStyle: {
          areaColor: tokens_default.color.highlight
        }
      },
      select: {
        label: {
          show: true,
          color: tokens_default.color.primary
        },
        itemStyle: {
          color: tokens_default.color.highlight
        }
      },
      nameProperty: "name"
    };
    return MapSeries2;
  })(Series_default)
);
function mapSeriesGroupHasOwnGeo(groupKey) {
  return groupKey.indexOf("i") === 0;
}
function mapSeriesNeedsDrawMap(mapSeries) {
  return getMainMapSeries(mapSeries.seriesGroup) === mapSeries && !mapSeries.getHostGeoModel();
}
function getMainMapSeries(mapSeriesGroup) {
  return mapSeriesGroup.f[0];
}
function buildAllMapSeriesGroups(ecModel, beforeSeriesFiltering) {
  var allMapSeriesGroups = {};
  ecModel.eachRawSeriesByType(SERIES_TYPE_MAP, function(seriesModel) {
    var hostGeoModel = seriesModel.getHostGeoModel();
    var key2 = hostGeoModel ? "o" + hostGeoModel.id : "i" + seriesModel.getMapType();
    var group2 = allMapSeriesGroups[key2] = allMapSeriesGroups[key2] || {
      f: [],
      r: []
    };
    if (!ecModel.isSeriesFiltered(seriesModel) && !beforeSeriesFiltering) {
      group2.f.push(seriesModel);
    }
    group2.r.push(seriesModel);
  });
  return allMapSeriesGroups;
}

// node_modules/echarts/lib/chart/map/MapView.js
var MapView = (
  /** @class */
  (function(_super) {
    __extends(MapView2, _super);
    function MapView2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = SERIES_TYPE_MAP;
      return _this;
    }
    MapView2.prototype.render = function(mapModel, ecModel, api, payload) {
      if (payload && payload.type === "mapToggleSelect" && payload.from === this.uid) {
        return;
      }
      var group2 = this.group;
      group2.removeAll();
      if (mapModel.getHostGeoModel()) {
        return;
      }
      var mapDraw = this._mapDraw;
      if (mapDraw && payload && payload.type === "geoRoam") {
        mapDraw.resetForLabelLayout();
      }
      if (!(payload && payload.type === "geoRoam" && payload.componentType === "series" && payload.seriesId === mapModel.id)) {
        if (mapSeriesNeedsDrawMap(mapModel)) {
          mapDraw = mapDraw || (this._mapDraw = new MapDraw_default(api));
          group2.add(mapDraw.group);
          mapDraw.draw(mapModel, ecModel, api, this, payload);
        } else {
          this._clearMapDraw();
        }
      } else {
        mapDraw && group2.add(mapDraw.group);
      }
      mapModel.get("showLegendSymbol") && ecModel.getComponent("legend") && this._renderSymbols(mapModel);
    };
    MapView2.prototype.__updateOnOwnRoam = function(payload, model2, api) {
      var mapDraw = this._mapDraw;
      if (mapSeriesNeedsDrawMap(model2) && mapDraw) {
        mapDraw.__updateOnOwnRoam(model2);
      }
    };
    MapView2.prototype.remove = function() {
      this._clearMapDraw();
      this.group.removeAll();
    };
    MapView2.prototype.dispose = function() {
      this._clearMapDraw();
    };
    MapView2.prototype._clearMapDraw = function() {
      this._mapDraw && this._mapDraw.remove();
      this._mapDraw = null;
    };
    MapView2.prototype._renderSymbols = function(mapModel) {
      var originalData = mapModel.originalData;
      var group2 = this.group;
      originalData.each(originalData.mapDimension("value"), function(value, originalDataIndex) {
        if (isNaN(value)) {
          return;
        }
        var layout3 = originalData.getItemLayout(originalDataIndex);
        if (!layout3 || !layout3.point) {
          return;
        }
        var point = layout3.point;
        var offset = layout3.offset;
        var circle = new Circle_default({
          style: {
            // Because the special of map draw.
            // Which needs statistic of multiple series and draw on one map.
            // And each series also need a symbol with legend color
            //
            // Layout and visual are put one the different data
            // TODO
            fill: mapModel.getData().getVisual("style").fill
          },
          shape: {
            cx: point[0] + offset * 9,
            cy: point[1],
            r: 3
          },
          silent: true,
          // Do not overlap the first series, on which labels are displayed.
          z2: 8 + (!offset ? Z2_EMPHASIS_LIFT + 1 : 0)
        });
        if (!offset) {
          var fullData = getMainMapSeries(mapModel.seriesGroup).getData();
          var name_1 = originalData.getName(originalDataIndex);
          var fullIndex_1 = fullData.indexOfName(name_1);
          var itemModel = originalData.getItemModel(originalDataIndex);
          var labelModel = itemModel.getModel("label");
          var regionGroup = fullData.getItemGraphicEl(fullIndex_1);
          setLabelStyle(circle, getLabelStatesModels(itemModel), {
            labelFetcher: {
              getFormattedLabel: function(idx, state2) {
                return mapModel.getFormattedLabel(fullIndex_1, state2);
              }
            },
            defaultText: name_1
          });
          circle.disableLabelAnimation = true;
          if (!labelModel.get("position")) {
            circle.setTextConfig({
              position: "bottom"
            });
          }
          regionGroup.onHoverStateChange = function(toState) {
            setStatesFlag(circle, toState);
          };
        }
        group2.add(circle);
      });
    };
    MapView2.type = SERIES_TYPE_MAP;
    return MapView2;
  })(Chart_default)
);

// node_modules/echarts/lib/coord/geo/Geo.js
var GEO_DEFAULT_PARAMS = {
  "geoJSON": {
    aspectScale: 0.75,
    invertLongitute: true
  },
  "geoSVG": {
    aspectScale: 1,
    invertLongitute: false
  }
};
var geo2DDimensions = ["lng", "lat"];
var Geo = (
  /** @class */
  (function(_super) {
    __extends(Geo2, _super);
    function Geo2(name, map4, opt) {
      var _this = _super.call(this) || this;
      _this.dimensions = geo2DDimensions;
      _this.type = "geo";
      _this._nameCoordMap = createHashMap();
      _this.name = name;
      var projection = opt.projection;
      var source = geoSourceManager_default.load(map4, opt.nameMap, opt.nameProperty);
      var resource = geoSourceManager_default.getGeoResource(map4);
      var resourceType = _this.resourceType = resource ? resource.type : null;
      var regions = _this.regions = source.regions;
      var defaultParams = GEO_DEFAULT_PARAMS[resource.type];
      _this._clip = opt.clip;
      var invertLongitute = projection ? false : defaultParams.invertLongitute;
      _this.view = new View_default(invertLongitute, useLegacyViewCoordSysCenterBase(opt.ecModel, opt.api), _this);
      _this.map = map4;
      _this._regionsMap = source.regionsMap;
      _this.regions = source.regions;
      if (projection) {
        if (resourceType === "geoSVG") {
          if (true) {
            warn("Map " + map4 + " with SVG source can't use projection. Only GeoJSON source supports projection.");
          }
          projection = null;
        }
        if (!(projection.project && projection.unproject)) {
          if (true) {
            warn("project and unproject must be both provided in the projeciton.");
          }
          projection = null;
        }
      }
      _this.projection = projection;
      var boundingRect;
      if (projection) {
        for (var i2 = 0; i2 < regions.length; i2++) {
          var regionRect = regions[i2].getBoundingRect(projection);
          boundingRect = boundingRect || regionRect.clone();
          boundingRect.union(regionRect);
        }
      } else {
        boundingRect = source.boundingRect;
      }
      viewCoordSysSetBoundingRect(_this.view, boundingRect.x, boundingRect.y, boundingRect.width, boundingRect.height);
      _this.aspectScale = projection ? 1 : retrieve2(opt.aspectScale, defaultParams.aspectScale);
      return _this;
    }
    Geo2.prototype.getRegion = function(name) {
      return this._regionsMap.get(name);
    };
    Geo2.prototype.getRegionByCoord = function(coord) {
      var regions = this.regions;
      for (var i2 = 0; i2 < regions.length; i2++) {
        var region = regions[i2];
        if (region.type === "geoJSON" && region.contain(coord)) {
          return regions[i2];
        }
      }
    };
    Geo2.prototype.addGeoCoord = function(name, geoCoord2) {
      this._nameCoordMap.set(name, geoCoord2);
    };
    Geo2.prototype.getGeoCoord = function(name) {
      var region = this._regionsMap.get(name);
      return this._nameCoordMap.get(name) || region && region.getCenter();
    };
    Geo2.prototype.dataToPoint = function(data, noRoam, out2) {
      if (isString(data)) {
        data = this.getGeoCoord(data);
      }
      if (data) {
        var projection = this.projection;
        if (projection) {
          data = projection.project(data);
        }
        return data && this.view.dataToPoint(data, noRoam, out2);
      }
    };
    Geo2.prototype.pointToData = function(point, reserved, out2) {
      var projection = this.projection;
      if (projection) {
        point = projection.unproject(point);
      }
      return point && this.view.pointToData(point, out2);
    };
    Geo2.prototype.convertToPixel = function(ecModel, finder, value) {
      var coordSys = getCoordSys2(finder);
      return coordSys === this ? coordSys.dataToPoint(value) : null;
    };
    Geo2.prototype.convertFromPixel = function(ecModel, finder, pixel) {
      var coordSys = getCoordSys2(finder);
      return coordSys === this ? coordSys.pointToData(pixel) : null;
    };
    Geo2.prototype.containPoint = function(point) {
      return this.view.containPoint(point);
    };
    Geo2.prototype.getArea = function(tolerance) {
      tolerance = tolerance || 0;
      var rect = viewCoordSysCopyViewRect(null, this.view);
      rect.x -= tolerance;
      rect.y -= tolerance;
      rect.width += 2 * tolerance;
      rect.height += 2 * tolerance;
      return rect;
    };
    Geo2.prototype.shouldClip = function() {
      return this._clip;
    };
    Geo2.prototype.getBoundingRect = function() {
      return this.view.getBoundingRect();
    };
    Geo2.prototype.getViewRect = function() {
      return this.view.getViewRect();
    };
    Geo2.prototype.getRoamTransform = function() {
      return this.view.getRoamTransform();
    };
    return Geo2;
  })(
    Transformable_default
    // See VIEW_COORD_SYS_TRANS_OVERALL_BACKWARD_COMPATIBILITY
  )
);
function getCoordSys2(finder) {
  var geoModel = finder.geoModel;
  var seriesModel = finder.seriesModel;
  return geoModel ? geoModel.coordinateSystem : seriesModel ? seriesModel.coordinateSystem || (seriesModel.getReferringComponents("geo", SINGLE_REFERRING).models[0] || {}).coordinateSystem : null;
}
var Geo_default = Geo;

// node_modules/echarts/lib/coord/geo/geoCreator.js
function resizeGeo(geoModel, api) {
  var viewCoordSys = this.view;
  var boundingCoords = geoModel.get("boundingCoords");
  if (boundingCoords != null) {
    var leftTop_1 = boundingCoords[0];
    var rightBottom_1 = boundingCoords[1];
    if (!(isFinite(leftTop_1[0]) && isFinite(leftTop_1[1]) && isFinite(rightBottom_1[0]) && isFinite(rightBottom_1[1]))) {
      if (true) {
        console.error("Invalid boundingCoords");
      }
    } else {
      var projection_1 = this.projection;
      if (projection_1) {
        var xMin = leftTop_1[0];
        var yMin = leftTop_1[1];
        var xMax = rightBottom_1[0];
        var yMax = rightBottom_1[1];
        leftTop_1 = [Infinity, Infinity];
        rightBottom_1 = [-Infinity, -Infinity];
        var sampleLine = function(x0, y0, x1, y1) {
          var dx = x1 - x0;
          var dy = y1 - y0;
          for (var i2 = 0; i2 <= 100; i2++) {
            var p = i2 / 100;
            var pt = projection_1.project([x0 + dx * p, y0 + dy * p]);
            min(leftTop_1, leftTop_1, pt);
            max(rightBottom_1, rightBottom_1, pt);
          }
        };
        sampleLine(xMin, yMin, xMax, yMin);
        sampleLine(xMax, yMin, xMax, yMax);
        sampleLine(xMax, yMax, xMin, yMax);
        sampleLine(xMin, yMax, xMax, yMin);
      }
      viewCoordSysSetBoundingRect(viewCoordSys, leftTop_1[0], leftTop_1[1], rightBottom_1[0] - leftTop_1[0], rightBottom_1[1] - leftTop_1[1]);
    }
  }
  var rect = viewCoordSys.getBoundingRect();
  var centerOption = geoModel.get("layoutCenter");
  var sizeOption = geoModel.get("layoutSize");
  var refContainer = createBoxLayoutReference(geoModel, api).refContainer;
  var aspect = rect.width / rect.height * this.aspectScale;
  var useCenterAndSize = false;
  var center3;
  var size;
  if (centerOption && sizeOption) {
    center3 = [parsePercent2(centerOption[0], refContainer.width) + refContainer.x, parsePercent2(centerOption[1], refContainer.height) + refContainer.y];
    size = parsePercent2(sizeOption, Math.min(refContainer.width, refContainer.height));
    if (!isNaN(center3[0]) && !isNaN(center3[1]) && !isNaN(size)) {
      useCenterAndSize = true;
    } else {
      if (true) {
        console.warn("Given layoutCenter or layoutSize data are invalid. Use left/top/width/height instead.");
      }
    }
  }
  var viewRect2;
  if (useCenterAndSize) {
    viewRect2 = {};
    if (aspect > 1) {
      viewRect2.width = size;
      viewRect2.height = size / aspect;
    } else {
      viewRect2.height = size;
      viewRect2.width = size * aspect;
    }
    viewRect2.y = center3[1] - viewRect2.height / 2;
    viewRect2.x = center3[0] - viewRect2.width / 2;
  } else {
    var boxLayoutOption = geoModel.getBoxLayoutParams();
    boxLayoutOption.aspect = aspect;
    viewRect2 = getLayoutRect(boxLayoutOption, refContainer);
    viewRect2 = applyPreserveAspect(geoModel, viewRect2, aspect);
  }
  viewCoordSysSetViewRect(viewCoordSys, viewRect2.x, viewRect2.y, viewRect2.width, viewRect2.height);
  viewCoordSysSetRoamOptionFromModel(viewCoordSys, geoModel);
}
function setGeoCoords(geo, model2) {
  each(model2.get("geoCoord"), function(geoCoord2, name) {
    geo.addGeoCoord(name, geoCoord2);
  });
}
var GeoCreator = (
  /** @class */
  (function() {
    function GeoCreator2() {
      this.dimensions = geo2DDimensions;
    }
    GeoCreator2.prototype.create = function(ecModel, api) {
      var geoList = [];
      function getCommonGeoProperties(model2) {
        return {
          nameProperty: model2.get("nameProperty"),
          aspectScale: model2.get("aspectScale"),
          projection: model2.get("projection"),
          clip: model2.getShallow("clip", true)
        };
      }
      ecModel.eachComponent("geo", function(geoModel, idx) {
        var mapName = geoModel.get("map");
        var geo = new Geo_default(mapName + idx, mapName, extend({
          nameMap: geoModel.get("nameMap"),
          api,
          ecModel
        }, getCommonGeoProperties(geoModel)));
        geoList.push(geo);
        geoModel.coordinateSystem = geo;
        geo.model = geoModel;
        geo.resize = resizeGeo;
        geo.resize(geoModel, api);
      });
      ecModel.eachSeries(function(seriesModel) {
        injectCoordSysByOption({
          targetModel: seriesModel,
          coordSysType: "geo",
          coordSysProvider: function() {
            var geoModel = seriesModel.subType === SERIES_TYPE_MAP ? seriesModel.getHostGeoModel() : seriesModel.getReferringComponents("geo", SINGLE_REFERRING).models[0];
            return geoModel && geoModel.coordinateSystem;
          },
          allowNotFound: true
        });
      });
      each(buildAllMapSeriesGroups(ecModel, true), function(mapSeriesGroup, groupKey) {
        if (!mapSeriesGroupHasOwnGeo(groupKey)) {
          return;
        }
        var firstDeclaredMapSeries = mapSeriesGroup.r[0];
        var nameMapList = [];
        each(mapSeriesGroup.r, function(mapSeries) {
          nameMapList.push(mapSeries.get("nameMap"));
          mapSeries.seriesGroup = null;
        });
        var mapType = groupKey.slice(1);
        var geo = new Geo_default(mapType, mapType, extend({
          nameMap: mergeAll(nameMapList),
          api,
          ecModel
        }, getCommonGeoProperties(firstDeclaredMapSeries)));
        var scaleLimit;
        each(mapSeriesGroup.r, function(mapSeries) {
          scaleLimit = retrieve2(scaleLimit, mapSeries.get("scaleLimit"));
        });
        geoList.push(geo);
        geo.resize = resizeGeo;
        geo.resize(firstDeclaredMapSeries, api);
        each(mapSeriesGroup.r, function(mapSeries) {
          mapSeries.coordinateSystem = geo;
          setGeoCoords(geo, mapSeries);
        });
      });
      return geoList;
    };
    GeoCreator2.prototype.getFilledRegions = function(originRegionArr, mapName, nameMap, nameProperty) {
      var regionsArr = (originRegionArr || []).slice();
      var dataNameMap = createHashMap();
      for (var i2 = 0; i2 < regionsArr.length; i2++) {
        dataNameMap.set(regionsArr[i2].name, regionsArr[i2]);
      }
      var source = geoSourceManager_default.load(mapName, nameMap, nameProperty);
      each(source.regions, function(region) {
        var name = region.name;
        var regionOption = dataNameMap.get(name);
        var specifiedGeoJSONRegionStyle = region.properties && region.properties.echartsStyle;
        if (!regionOption) {
          regionOption = {
            name
          };
          regionsArr.push(regionOption);
        }
        specifiedGeoJSONRegionStyle && merge2(regionOption, specifiedGeoJSONRegionStyle);
      });
      return regionsArr;
    };
    return GeoCreator2;
  })()
);
var geoCreator = new GeoCreator();
var geoCreator_default = geoCreator;

// node_modules/echarts/lib/coord/geo/GeoModel.js
var GeoModel = (
  /** @class */
  (function(_super) {
    __extends(GeoModel2, _super);
    function GeoModel2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = GeoModel2.type;
      return _this;
    }
    GeoModel2.prototype.init = function(option, parentModel, ecModel) {
      this.mergeDefaultAndTheme(option, ecModel);
      var source = geoSourceManager_default.getGeoResource(option.map);
      if (source && source.type === "geoJSON") {
        var itemStyle = option.itemStyle = option.itemStyle || {};
        if (!("color" in itemStyle)) {
          itemStyle.color = option.defaultItemStyleColor || tokens_default.color.backgroundTint;
        }
      }
      defaultEmphasis(option, "label", ["show"]);
    };
    GeoModel2.prototype.optionUpdated = function() {
      var _this = this;
      var option = this.option;
      option.regions = geoCreator_default.getFilledRegions(option.regions, option.map, option.nameMap, option.nameProperty);
      var selectedMap = {};
      this._optionModelMap = reduce(option.regions || [], function(optionModelMap, regionOpt) {
        var regionName = regionOpt.name;
        if (regionName) {
          optionModelMap.set(regionName, new Model_default(regionOpt, _this, _this.ecModel));
          if (regionOpt.selected) {
            selectedMap[regionName] = true;
          }
        }
        return optionModelMap;
      }, createHashMap());
      if (!option.selectedMap) {
        option.selectedMap = selectedMap;
      }
    };
    GeoModel2.prototype.getRegionModel = function(name) {
      return this._optionModelMap.get(name) || new Model_default(null, this, this.ecModel);
    };
    GeoModel2.prototype.getFormattedLabel = function(name, status) {
      var regionModel = this.getRegionModel(name);
      var formatter = status === "normal" ? regionModel.get(["label", "formatter"]) : regionModel.get(["emphasis", "label", "formatter"]);
      var params = {
        name
      };
      if (isFunction(formatter)) {
        params.status = status;
        return formatter(params);
      } else if (isString(formatter)) {
        return formatter.replace("{a}", name != null ? name : "");
      }
    };
    GeoModel2.prototype.select = function(name) {
      var option = this.option;
      var selectedMode = option.selectedMode;
      if (!selectedMode) {
        return;
      }
      if (selectedMode !== "multiple") {
        option.selectedMap = null;
      }
      var selectedMap = option.selectedMap || (option.selectedMap = {});
      selectedMap[name] = true;
    };
    GeoModel2.prototype.unSelect = function(name) {
      var selectedMap = this.option.selectedMap;
      if (selectedMap) {
        selectedMap[name] = false;
      }
    };
    GeoModel2.prototype.toggleSelected = function(name) {
      this[this.isSelected(name) ? "unSelect" : "select"](name);
    };
    GeoModel2.prototype.isSelected = function(name) {
      var selectedMap = this.option.selectedMap;
      return !!(selectedMap && selectedMap[name]);
    };
    GeoModel2.prototype.__ownRoamView = function() {
      return this.coordinateSystem.view;
    };
    GeoModel2.type = "geo";
    GeoModel2.layoutMode = "box";
    GeoModel2.defaultOption = {
      // zlevel: 0,
      z: 0,
      show: true,
      left: "center",
      top: "center",
      // Default value:
      // for geoSVG source: 1,
      // for geoJSON source: 0.75.
      aspectScale: null,
      // /// Layout with center and size
      // If you want to put map in a fixed size box with right aspect ratio
      // This two properties may be more convenient
      // layoutCenter: [50%, 50%]
      // layoutSize: 100
      silent: false,
      // Map type
      map: "",
      // Define left-top, right-bottom coords to control view
      // For example, [ [180, 90], [-180, -90] ]
      boundingCoords: null,
      // Default on center of map
      center: null,
      zoom: 1,
      scaleLimit: null,
      // selectedMode: false
      label: {
        show: false,
        color: tokens_default.color.tertiary
      },
      itemStyle: {
        borderWidth: 0.5,
        borderColor: tokens_default.color.border
      },
      emphasis: {
        label: {
          show: true,
          color: tokens_default.color.primary
        },
        itemStyle: {
          color: tokens_default.color.highlight
        }
      },
      select: {
        label: {
          show: true,
          color: tokens_default.color.primary
        },
        itemStyle: {
          color: tokens_default.color.highlight
        }
      },
      regions: []
      // tooltip: {
      //     show: false
      // }
    };
    return GeoModel2;
  })(Component_default)
);

// node_modules/echarts/lib/component/geo/GeoView.js
var GeoView = (
  /** @class */
  (function(_super) {
    __extends(GeoView2, _super);
    function GeoView2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = GeoView2.type;
      _this.focusBlurEnabled = true;
      return _this;
    }
    GeoView2.prototype.init = function(ecModel, api) {
      this._api = api;
    };
    GeoView2.prototype.render = function(geoModel, ecModel, api, payload) {
      this._model = geoModel;
      if (!geoModel.get("show")) {
        this._mapDraw && this._mapDraw.remove();
        this._mapDraw = null;
        return;
      }
      if (!this._mapDraw) {
        this._mapDraw = new MapDraw_default(api);
      }
      var mapDraw = this._mapDraw;
      mapDraw.draw(geoModel, ecModel, api, this, payload);
      mapDraw.group.on("click", this._handleRegionClick, this);
      mapDraw.group.silent = geoModel.get("silent");
      this.group.add(mapDraw.group);
      this.updateSelectStatus(geoModel, ecModel, api);
    };
    GeoView2.prototype.__updateOnOwnRoam = function(payload, model2, api) {
      var mapDraw = this._mapDraw;
      if (mapDraw) {
        mapDraw.__updateOnOwnRoam(model2);
      }
    };
    GeoView2.prototype._handleRegionClick = function(e2) {
      var eventData;
      findEventDispatcher(e2.target, function(current) {
        return (eventData = getECData(current).eventData) != null;
      }, true);
      if (eventData) {
        this._api.dispatchAction({
          type: "geoToggleSelect",
          geoId: this._model.id,
          name: eventData.name
        });
      }
    };
    GeoView2.prototype.updateSelectStatus = function(model2, ecModel, api) {
      var _this = this;
      this._mapDraw.group.traverse(function(node) {
        var eventData = getECData(node).eventData;
        if (eventData) {
          _this._model.isSelected(eventData.name) ? api.enterSelect(node) : api.leaveSelect(node);
          return true;
        }
      });
    };
    GeoView2.prototype.findHighDownDispatchers = function(name) {
      return this._mapDraw && this._mapDraw.findHighDownDispatchers(name, this._model);
    };
    GeoView2.prototype.dispose = function() {
      this._mapDraw && this._mapDraw.remove();
    };
    GeoView2.type = "geo";
    return GeoView2;
  })(Component_default2)
);

// node_modules/echarts/lib/chart/map/mapSymbolLayout.js
var mapSymbolLayoutStageHandler = createSimpleOverallStageHandler(SERIES_TYPE_MAP, mapSymbolLayout);
function mapSymbolLayout(ecModel) {
  each(buildAllMapSeriesGroups(ecModel), function(mapSeriesGroup, groupKey) {
    if (!getMainMapSeries(mapSeriesGroup) || !mapSeriesGroupHasOwnGeo(groupKey)) {
      return;
    }
    var mapSymbolOffsets = {};
    each(mapSeriesGroup.f, function(subMapSeries) {
      var geo = subMapSeries.coordinateSystem;
      var data2 = subMapSeries.originalData;
      if (subMapSeries.get("showLegendSymbol") && ecModel.getComponent("legend")) {
        data2.each(data2.mapDimension("value"), function(value, idx) {
          var name = data2.getName(idx);
          var region = geo.getRegion(name);
          if (!region || isNaN(value)) {
            return;
          }
          var offset = mapSymbolOffsets[name] || 0;
          var point = geo.dataToPoint(region.getCenter());
          mapSymbolOffsets[name] = offset + 1;
          data2.setItemLayout(idx, {
            point,
            offset
          });
        });
      }
    });
    var data = getMainMapSeries(mapSeriesGroup).getData();
    data.each(function(idx) {
      var name = data.getName(idx);
      var layout3 = data.getItemLayout(idx) || {};
      layout3.showLabel = !mapSymbolOffsets[name];
      data.setItemLayout(idx, layout3);
    });
  });
}

// node_modules/echarts/lib/chart/map/mapDataStatistic.js
function dataStatistics(datas, statisticType) {
  var dataNameMap = {};
  each(datas, function(data) {
    data.each(data.mapDimension("value"), function(value, idx) {
      var mapKey = "ec-" + data.getName(idx);
      dataNameMap[mapKey] = dataNameMap[mapKey] || [];
      if (!isNaN(value)) {
        dataNameMap[mapKey].push(value);
      }
    });
  });
  return datas[0].map(datas[0].mapDimension("value"), function(value, idx) {
    var mapKey = "ec-" + datas[0].getName(idx);
    var sum2 = 0;
    var min3 = Infinity;
    var max3 = -Infinity;
    var len2 = dataNameMap[mapKey].length;
    for (var i2 = 0; i2 < len2; i2++) {
      min3 = Math.min(min3, dataNameMap[mapKey][i2]);
      max3 = Math.max(max3, dataNameMap[mapKey][i2]);
      sum2 += dataNameMap[mapKey][i2];
    }
    var result;
    if (statisticType === "min") {
      result = min3;
    } else if (statisticType === "max") {
      result = max3;
    } else if (statisticType === "average") {
      result = sum2 / len2;
    } else {
      result = sum2;
    }
    return len2 === 0 ? NaN : result;
  });
}
var mapDataStatisticStageHandler = createSimpleOverallStageHandler(SERIES_TYPE_MAP, mapDataStatistic);
function mapDataStatistic(ecModel) {
  each(buildAllMapSeriesGroups(ecModel), function(seriesGroup) {
    var mainSeries = getMainMapSeries(seriesGroup);
    if (!mainSeries) {
      return;
    }
    var data = dataStatistics(
      map2(seriesGroup.f, function(seriesModel) {
        return seriesModel.getData();
      }),
      // PENDING: It has long been using `seriesGroup[0]` here, but if the first map series
      // is filtered out by legend, `seriesGroup[0]` is the second series in ec option, therefore,
      // this definition is not reasonable enough for users.
      mainSeries.get("mapValueCalculation")
    );
    each(seriesGroup.f, function(series) {
      series.seriesGroup = seriesGroup;
      series.originalData = series.getData();
      series.setData(data.cloneShallow());
    });
  });
}

// node_modules/echarts/lib/chart/tree/layoutHelper.js
function radialCoordinate(rad, r) {
  rad -= Math.PI / 2;
  return {
    x: r * Math.cos(rad),
    y: r * Math.sin(rad)
  };
}

// node_modules/echarts/lib/data/helper/linkSeriesData.js
var inner10 = makeInner();
function linkSeriesData(opt) {
  var mainData = opt.mainData;
  var datas = opt.datas;
  if (!datas) {
    datas = {
      main: mainData
    };
    opt.datasAttr = {
      main: "data"
    };
  }
  opt.datas = opt.mainData = null;
  linkAll(mainData, datas, opt);
  each(datas, function(data) {
    each(mainData.TRANSFERABLE_METHODS, function(methodName) {
      data.wrapMethod(methodName, curry(transferInjection, opt));
    });
  });
  mainData.wrapMethod("cloneShallow", curry(cloneShallowInjection, opt));
  each(mainData.CHANGABLE_METHODS, function(methodName) {
    mainData.wrapMethod(methodName, curry(changeInjection, opt));
  });
  assert(datas[mainData.dataType] === mainData);
}
function transferInjection(opt, res) {
  if (isMainData(this)) {
    var datas = extend({}, inner10(this).datas);
    datas[this.dataType] = res;
    linkAll(res, datas, opt);
  } else {
    linkSingle(res, this.dataType, inner10(this).mainData, opt);
  }
  return res;
}
function changeInjection(opt, res) {
  opt.struct && opt.struct.update();
  return res;
}
function cloneShallowInjection(opt, res) {
  each(inner10(res).datas, function(data, dataType) {
    data !== res && linkSingle(data.cloneShallow(), dataType, res, opt);
  });
  return res;
}
function getLinkedData(dataType) {
  var mainData = inner10(this).mainData;
  return dataType == null || mainData == null ? mainData : inner10(mainData).datas[dataType];
}
function getLinkedDataAll() {
  var mainData = inner10(this).mainData;
  return mainData == null ? [{
    data: mainData
  }] : map2(keys(inner10(mainData).datas), function(type) {
    return {
      type,
      data: inner10(mainData).datas[type]
    };
  });
}
function isMainData(data) {
  return inner10(data).mainData === data;
}
function linkAll(mainData, datas, opt) {
  inner10(mainData).datas = {};
  each(datas, function(data, dataType) {
    linkSingle(data, dataType, mainData, opt);
  });
}
function linkSingle(data, dataType, mainData, opt) {
  inner10(mainData).datas[dataType] = data;
  inner10(data).mainData = mainData;
  data.dataType = dataType;
  if (opt.struct) {
    data[opt.structAttr] = opt.struct;
    opt.struct[opt.datasAttr[dataType]] = data;
  }
  data.getLinkedData = getLinkedData;
  data.getLinkedDataAll = getLinkedDataAll;
}
var linkSeriesData_default = linkSeriesData;

// node_modules/echarts/lib/data/Tree.js
var TreeNode = (
  /** @class */
  (function() {
    function TreeNode2(name, hostTree) {
      this.depth = 0;
      this.height = 0;
      this.dataIndex = -1;
      this.children = [];
      this.viewChildren = [];
      this.isExpand = false;
      this.name = name || "";
      this.hostTree = hostTree;
    }
    TreeNode2.prototype.isRemoved = function() {
      return this.dataIndex < 0;
    };
    TreeNode2.prototype.eachNode = function(options, cb, context) {
      if (isFunction(options)) {
        context = cb;
        cb = options;
        options = null;
      }
      options = options || {};
      if (isString(options)) {
        options = {
          order: options
        };
      }
      var order = options.order || "preorder";
      var children = this[options.attr || "children"];
      var suppressVisitSub;
      order === "preorder" && (suppressVisitSub = cb.call(context, this));
      for (var i2 = 0; !suppressVisitSub && i2 < children.length; i2++) {
        children[i2].eachNode(options, cb, context);
      }
      order === "postorder" && cb.call(context, this);
    };
    TreeNode2.prototype.updateDepthAndHeight = function(depth) {
      var height = 0;
      this.depth = depth;
      for (var i2 = 0; i2 < this.children.length; i2++) {
        var child = this.children[i2];
        child.updateDepthAndHeight(depth + 1);
        if (child.height > height) {
          height = child.height;
        }
      }
      this.height = height + 1;
    };
    TreeNode2.prototype.getNodeById = function(id) {
      if (this.getId() === id) {
        return this;
      }
      for (var i2 = 0, children = this.children, len2 = children.length; i2 < len2; i2++) {
        var res = children[i2].getNodeById(id);
        if (res) {
          return res;
        }
      }
    };
    TreeNode2.prototype.contains = function(node) {
      if (node === this) {
        return true;
      }
      for (var i2 = 0, children = this.children, len2 = children.length; i2 < len2; i2++) {
        var res = children[i2].contains(node);
        if (res) {
          return res;
        }
      }
    };
    TreeNode2.prototype.getAncestors = function(includeSelf) {
      var ancestors = [];
      var node = includeSelf ? this : this.parentNode;
      while (node) {
        ancestors.push(node);
        node = node.parentNode;
      }
      ancestors.reverse();
      return ancestors;
    };
    TreeNode2.prototype.getAncestorsIndices = function() {
      var indices = [];
      var currNode = this;
      while (currNode) {
        indices.push(currNode.dataIndex);
        currNode = currNode.parentNode;
      }
      indices.reverse();
      return indices;
    };
    TreeNode2.prototype.getDescendantIndices = function() {
      var indices = [];
      this.eachNode(function(childNode) {
        indices.push(childNode.dataIndex);
      });
      return indices;
    };
    TreeNode2.prototype.getValue = function(dimension) {
      var data = this.hostTree.data;
      return data.getStore().get(data.getDimensionIndex(dimension || "value"), this.dataIndex);
    };
    TreeNode2.prototype.setLayout = function(layout3, merge3) {
      this.dataIndex >= 0 && this.hostTree.data.setItemLayout(this.dataIndex, layout3, merge3);
    };
    TreeNode2.prototype.getLayout = function() {
      return this.hostTree.data.getItemLayout(this.dataIndex);
    };
    TreeNode2.prototype.getModel = function(path) {
      if (this.dataIndex < 0) {
        return;
      }
      var hostTree = this.hostTree;
      var itemModel = hostTree.data.getItemModel(this.dataIndex);
      return itemModel.getModel(path);
    };
    TreeNode2.prototype.getLevelModel = function() {
      return (this.hostTree.levelModels || [])[this.depth];
    };
    TreeNode2.prototype.setVisual = function(key2, value) {
      this.dataIndex >= 0 && this.hostTree.data.setItemVisual(this.dataIndex, key2, value);
    };
    TreeNode2.prototype.getVisual = function(key2) {
      return this.hostTree.data.getItemVisual(this.dataIndex, key2);
    };
    TreeNode2.prototype.getRawIndex = function() {
      return this.hostTree.data.getRawIndex(this.dataIndex);
    };
    TreeNode2.prototype.getId = function() {
      return this.hostTree.data.getId(this.dataIndex);
    };
    TreeNode2.prototype.getChildIndex = function() {
      if (this.parentNode) {
        var children = this.parentNode.children;
        for (var i2 = 0; i2 < children.length; ++i2) {
          if (children[i2] === this) {
            return i2;
          }
        }
        return -1;
      }
      return -1;
    };
    TreeNode2.prototype.isAncestorOf = function(node) {
      var parent = node.parentNode;
      while (parent) {
        if (parent === this) {
          return true;
        }
        parent = parent.parentNode;
      }
      return false;
    };
    TreeNode2.prototype.isDescendantOf = function(node) {
      return node !== this && node.isAncestorOf(this);
    };
    return TreeNode2;
  })()
);
var Tree = (
  /** @class */
  (function() {
    function Tree2(hostModel) {
      this.type = "tree";
      this._nodes = [];
      this.hostModel = hostModel;
    }
    Tree2.prototype.eachNode = function(options, cb, context) {
      this.root.eachNode(options, cb, context);
    };
    Tree2.prototype.getNodeByDataIndex = function(dataIndex) {
      var rawIndex = this.data.getRawIndex(dataIndex);
      return this._nodes[rawIndex];
    };
    Tree2.prototype.getNodeById = function(name) {
      return this.root.getNodeById(name);
    };
    Tree2.prototype.update = function() {
      var data = this.data;
      var nodes = this._nodes;
      for (var i2 = 0, len2 = nodes.length; i2 < len2; i2++) {
        nodes[i2].dataIndex = -1;
      }
      for (var i2 = 0, len2 = data.count(); i2 < len2; i2++) {
        nodes[data.getRawIndex(i2)].dataIndex = i2;
      }
    };
    Tree2.prototype.clearLayouts = function() {
      this.data.clearItemLayouts();
    };
    Tree2.createTree = function(dataRoot, hostModel, beforeLink) {
      var tree = new Tree2(hostModel);
      var listData = [];
      var dimMax = 1;
      buildHierarchy(dataRoot);
      function buildHierarchy(dataNode, parentNode2) {
        var value = dataNode.value;
        dimMax = Math.max(dimMax, isArray(value) ? value.length : 1);
        listData.push(dataNode);
        var node = new TreeNode(convertOptionIdName(dataNode.name, ""), tree);
        parentNode2 ? addChild(node, parentNode2) : tree.root = node;
        tree._nodes.push(node);
        var children = dataNode.children;
        if (children) {
          for (var i2 = 0; i2 < children.length; i2++) {
            buildHierarchy(children[i2], node);
          }
        }
      }
      tree.root.updateDepthAndHeight(0);
      var dimensions = prepareSeriesDataSchema(listData, {
        coordDimensions: ["value"],
        dimensionsCount: dimMax
      }).dimensions;
      var list = new SeriesData_default(dimensions, hostModel);
      list.initData(listData);
      beforeLink && beforeLink(list);
      linkSeriesData_default({
        mainData: list,
        struct: tree,
        structAttr: "tree"
      });
      tree.update();
      return tree;
    };
    return Tree2;
  })()
);
function addChild(child, node) {
  var children = node.children;
  if (child.parentNode === node) {
    return;
  }
  children.push(child);
  child.parentNode = node;
}
var Tree_default = Tree;

// node_modules/echarts/lib/chart/helper/treeHelper.js
function retrieveTargetInfo(payload, validPayloadTypes, seriesModel) {
  if (payload && indexOf(validPayloadTypes, payload.type) >= 0) {
    var root = seriesModel.getData().tree.root;
    var targetNode = payload.targetNode;
    if (isString(targetNode)) {
      targetNode = root.getNodeById(targetNode);
    }
    if (targetNode && root.contains(targetNode)) {
      return {
        node: targetNode
      };
    }
    var targetNodeId = payload.targetNodeId;
    if (targetNodeId != null && (targetNode = root.getNodeById(targetNodeId))) {
      return {
        node: targetNode
      };
    }
  }
}
function getPathToRoot(node) {
  var path = [];
  while (node) {
    node = node.parentNode;
    node && path.push(node);
  }
  return path.reverse();
}
function aboveViewRoot(viewRoot, node) {
  var viewPath = getPathToRoot(viewRoot);
  return indexOf(viewPath, node) >= 0;
}
function wrapTreePathInfo(node, seriesModel) {
  var treePathInfo = [];
  while (node) {
    var nodeDataIndex = node.dataIndex;
    treePathInfo.push({
      name: node.name,
      dataIndex: nodeDataIndex,
      value: seriesModel.getRawValue(nodeDataIndex)
    });
    node = node.parentNode;
  }
  treePathInfo.reverse();
  return treePathInfo;
}

// node_modules/echarts/lib/chart/tree/TreeSeries.js
var SERIES_TYPE_TREE = "tree";
var TreeSeriesModel = (
  /** @class */
  (function(_super) {
    __extends(TreeSeriesModel2, _super);
    function TreeSeriesModel2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = TreeSeriesModel2.type;
      _this.hasSymbolVisual = true;
      _this.ignoreStyleOnData = true;
      return _this;
    }
    TreeSeriesModel2.prototype.getInitialData = function(option) {
      var root = {
        name: option.name,
        children: option.data
      };
      var leaves = option.leaves || {};
      var leavesModel = new Model_default(leaves, this, this.ecModel);
      var tree = Tree_default.createTree(root, this, beforeLink);
      function beforeLink(nodeData) {
        nodeData.wrapMethod("getItemModel", function(model2, idx) {
          var node = tree.getNodeByDataIndex(idx);
          if (!(node && node.children.length && node.isExpand)) {
            model2.parentModel = leavesModel;
          }
          return model2;
        });
      }
      var treeDepth = 0;
      tree.eachNode("preorder", function(node) {
        if (node.depth > treeDepth) {
          treeDepth = node.depth;
        }
      });
      var expandAndCollapse = option.expandAndCollapse;
      var expandTreeDepth = expandAndCollapse && option.initialTreeDepth >= 0 ? option.initialTreeDepth : treeDepth;
      tree.root.eachNode("preorder", function(node) {
        var item = node.hostTree.data.getRawDataItem(node.dataIndex);
        node.isExpand = item && item.collapsed != null ? !item.collapsed : node.depth <= expandTreeDepth;
      });
      return tree.data;
    };
    TreeSeriesModel2.prototype.getOrient = function() {
      var orient = this.get("orient");
      if (orient === "horizontal") {
        orient = "LR";
      } else if (orient === "vertical") {
        orient = "TB";
      }
      return orient;
    };
    TreeSeriesModel2.prototype.formatTooltip = function(dataIndex, multipleSeries, dataType) {
      var tree = this.getData().tree;
      var realRoot = tree.root.children[0];
      var node = tree.getNodeByDataIndex(dataIndex);
      var value = node.getValue();
      var name = node.name;
      while (node && node !== realRoot) {
        name = node.parentNode.name + "." + name;
        node = node.parentNode;
      }
      return createTooltipMarkup("nameValue", {
        name,
        value,
        noValue: isNaN(value) || value == null
      });
    };
    TreeSeriesModel2.prototype.getDataParams = function(dataIndex) {
      var params = _super.prototype.getDataParams.apply(this, arguments);
      var node = this.getData().tree.getNodeByDataIndex(dataIndex);
      params.treeAncestors = wrapTreePathInfo(node, this);
      params.collapsed = !node.isExpand;
      return params;
    };
    TreeSeriesModel2.prototype.__ownRoamView = function() {
      return this.coordinateSystem;
    };
    TreeSeriesModel2.type = "series." + SERIES_TYPE_TREE;
    TreeSeriesModel2.layoutMode = "box";
    TreeSeriesModel2.defaultOption = {
      // zlevel: 0,
      z: 2,
      // `coordinateSystem` can be declared as 'matrix', 'calendar',
      //  which provides box layout container.
      coordinateSystemUsage: "box",
      // the position of the whole view
      left: "12%",
      top: "12%",
      right: "12%",
      bottom: "12%",
      // the layout of the tree, two value can be selected, 'orthogonal' or 'radial'
      layout: "orthogonal",
      // value can be 'polyline'
      edgeShape: "curve",
      edgeForkPosition: "50%",
      // true | false | 'move' | 'scale', see module:component/helper/RoamController.
      roam: false,
      roamTrigger: "global",
      // Symbol size scale ratio in roam
      nodeScaleRatio: 0.4,
      // Default on center of graph
      center: null,
      zoom: 1,
      orient: "LR",
      symbol: "emptyCircle",
      symbolSize: 7,
      expandAndCollapse: true,
      initialTreeDepth: 2,
      lineStyle: {
        color: tokens_default.color.borderTint,
        width: 1.5,
        curveness: 0.5
      },
      itemStyle: {
        color: "lightsteelblue",
        // borderColor: '#c23531',
        borderWidth: 1.5
      },
      label: {
        show: true
      },
      animationEasing: "linear",
      animationDuration: 700,
      animationDurationUpdate: 500
    };
    return TreeSeriesModel2;
  })(Series_default)
);

// node_modules/echarts/lib/chart/tree/TreeView.js
var TreeEdgeShape = (
  /** @class */
  /* @__PURE__ */ (function() {
    function TreeEdgeShape2() {
      this.parentPoint = [];
      this.childPoints = [];
    }
    return TreeEdgeShape2;
  })()
);
var TreePath = (
  /** @class */
  (function(_super) {
    __extends(TreePath2, _super);
    function TreePath2(opts) {
      return _super.call(this, opts) || this;
    }
    TreePath2.prototype.getDefaultStyle = function() {
      return {
        stroke: tokens_default.color.neutral99,
        fill: null
      };
    };
    TreePath2.prototype.getDefaultShape = function() {
      return new TreeEdgeShape();
    };
    TreePath2.prototype.buildPath = function(ctx, shape) {
      var childPoints = shape.childPoints;
      var childLen = childPoints.length;
      var parentPoint = shape.parentPoint;
      var firstChildPos = childPoints[0];
      var lastChildPos = childPoints[childLen - 1];
      if (childLen === 1) {
        ctx.moveTo(parentPoint[0], parentPoint[1]);
        ctx.lineTo(firstChildPos[0], firstChildPos[1]);
        return;
      }
      var orient = shape.orient;
      var forkDim = orient === "TB" || orient === "BT" ? 0 : 1;
      var otherDim = 1 - forkDim;
      var forkPosition = parsePercent2(shape.forkPosition, 1);
      var tmpPoint = [];
      tmpPoint[forkDim] = parentPoint[forkDim];
      tmpPoint[otherDim] = parentPoint[otherDim] + (lastChildPos[otherDim] - parentPoint[otherDim]) * forkPosition;
      ctx.moveTo(parentPoint[0], parentPoint[1]);
      ctx.lineTo(tmpPoint[0], tmpPoint[1]);
      ctx.moveTo(firstChildPos[0], firstChildPos[1]);
      tmpPoint[forkDim] = firstChildPos[forkDim];
      ctx.lineTo(tmpPoint[0], tmpPoint[1]);
      tmpPoint[forkDim] = lastChildPos[forkDim];
      ctx.lineTo(tmpPoint[0], tmpPoint[1]);
      ctx.lineTo(lastChildPos[0], lastChildPos[1]);
      for (var i2 = 1; i2 < childLen - 1; i2++) {
        var point = childPoints[i2];
        ctx.moveTo(point[0], point[1]);
        tmpPoint[forkDim] = point[forkDim];
        ctx.lineTo(tmpPoint[0], tmpPoint[1]);
      }
    };
    return TreePath2;
  })(Path_default)
);
var TreeView = (
  /** @class */
  (function(_super) {
    __extends(TreeView2, _super);
    function TreeView2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = SERIES_TYPE_TREE;
      _this._mainGroup = new Group_default();
      return _this;
    }
    TreeView2.prototype.init = function(ecModel, api) {
      this._controller = new RoamController_default(api.getZr());
      this.group.add(this._mainGroup);
      this._firstRender = true;
    };
    TreeView2.prototype.render = function(seriesModel, ecModel, api) {
      var data = seriesModel.getData();
      var layoutInfo = seriesModel.layoutInfo;
      var group2 = this._mainGroup;
      var layout3 = seriesModel.get("layout");
      if (layout3 === "radial") {
        group2.x = layoutInfo.x + layoutInfo.width / 2;
        group2.y = layoutInfo.y + layoutInfo.height / 2;
      } else {
        group2.x = layoutInfo.x;
        group2.y = layoutInfo.y;
      }
      this._updateViewCoordSys(seriesModel, api);
      updateRoamControllerSimply(seriesModel, api, this._controller, createIsInSelfByPointerCheckerEl(this.group), null);
      var oldData = this._data;
      data.diff(oldData).add(function(newIdx) {
        if (symbolNeedsDraw2(data, newIdx)) {
          updateNode(data, newIdx, null, group2, seriesModel);
        }
      }).update(function(newIdx, oldIdx) {
        var symbolEl = oldData.getItemGraphicEl(oldIdx);
        if (!symbolNeedsDraw2(data, newIdx)) {
          symbolEl && removeNode(oldData, oldIdx, symbolEl, group2, seriesModel);
          return;
        }
        updateNode(data, newIdx, symbolEl, group2, seriesModel);
      }).remove(function(oldIdx) {
        var symbolEl = oldData.getItemGraphicEl(oldIdx);
        if (symbolEl) {
          removeNode(oldData, oldIdx, symbolEl, group2, seriesModel);
        }
      }).execute();
      this._updateNodeAndLinkScale(seriesModel);
      if (seriesModel.get("expandAndCollapse") === true) {
        data.eachItemGraphicEl(function(el, dataIndex) {
          el.off("click").on("click", function() {
            api.dispatchAction({
              type: "treeExpandAndCollapse",
              seriesId: seriesModel.id,
              dataIndex
            });
          });
        });
      }
      this._data = data;
      this._firstRender = false;
    };
    TreeView2.prototype.__updateOnOwnRoam = function(payload, seriesModel, api) {
      applyViewCoordSysTransToElement(this.group, VIEW_COORD_SYS_TRANS_OVERALL, seriesModel.coordinateSystem, null);
      if (isRoamPayloadHasZoom(payload)) {
        this._updateNodeAndLinkScale(seriesModel);
      }
    };
    TreeView2.prototype._updateViewCoordSys = function(seriesModel, api) {
      var data = seriesModel.getData();
      var points4 = [];
      data.each(function(idx) {
        var layout3 = data.getItemLayout(idx);
        if (layout3 && !isNaN(layout3.x) && !isNaN(layout3.y)) {
          points4.push([+layout3.x, +layout3.y]);
        }
      });
      var min3 = [];
      var max3 = [];
      fromPoints(points4, min3, max3);
      var oldMin = this._min;
      var oldMax = this._max;
      if (max3[0] - min3[0] === 0) {
        min3[0] = oldMin ? oldMin[0] : min3[0] - 1;
        max3[0] = oldMax ? oldMax[0] : max3[0] + 1;
      }
      if (max3[1] - min3[1] === 0) {
        min3[1] = oldMin ? oldMin[1] : min3[1] - 1;
        max3[1] = oldMax ? oldMax[1] : max3[1] + 1;
      }
      var ownCoordSys = seriesModel.coordinateSystem = createViewCoordSysSimply(seriesModel, api, min3[0], min3[1], max3[0] - min3[0], max3[1] - min3[1]);
      applyViewCoordSysTransToElement(this.group, VIEW_COORD_SYS_TRANS_OVERALL, ownCoordSys, this._firstRender ? null : seriesModel);
      this._min = min3;
      this._max = max3;
    };
    TreeView2.prototype._updateNodeAndLinkScale = function(seriesModel) {
      var data = seriesModel.getData();
      var nodeScale = calcCompensationScaleToPreserveNodeSize(seriesModel.coordinateSystem, seriesModel);
      data.eachItemGraphicEl(function(el, idx) {
        el.setSymbolScale(nodeScale);
      });
    };
    TreeView2.prototype.dispose = function() {
      this._controller && this._controller.dispose();
    };
    TreeView2.prototype.remove = function() {
      this._mainGroup.removeAll();
      this._data = null;
    };
    TreeView2.type = SERIES_TYPE_TREE;
    return TreeView2;
  })(Chart_default)
);
function symbolNeedsDraw2(data, dataIndex) {
  var layout3 = data.getItemLayout(dataIndex);
  return layout3 && !isNaN(layout3.x) && !isNaN(layout3.y);
}
function updateNode(data, dataIndex, symbolEl, group2, seriesModel) {
  var isInit = !symbolEl;
  var node = data.tree.getNodeByDataIndex(dataIndex);
  var itemModel = node.getModel();
  var visualColor = node.getVisual("style").fill;
  var symbolInnerColor = node.isExpand === false && node.children.length !== 0 ? visualColor : tokens_default.color.neutral00;
  var virtualRoot = data.tree.root;
  var source = node.parentNode === virtualRoot ? node : node.parentNode || node;
  var sourceSymbolEl = data.getItemGraphicEl(source.dataIndex);
  var sourceLayout = source.getLayout();
  var sourceOldLayout = sourceSymbolEl ? {
    x: sourceSymbolEl.__oldX,
    y: sourceSymbolEl.__oldY,
    rawX: sourceSymbolEl.__radialOldRawX,
    rawY: sourceSymbolEl.__radialOldRawY
  } : sourceLayout;
  var targetLayout = node.getLayout();
  if (isInit) {
    symbolEl = new Symbol_default(data, dataIndex, null, {
      symbolInnerColor,
      useNameLabel: true
    });
    symbolEl.x = sourceOldLayout.x;
    symbolEl.y = sourceOldLayout.y;
  } else {
    symbolEl.updateData(data, dataIndex, null, {
      symbolInnerColor,
      useNameLabel: true
    });
  }
  symbolEl.__radialOldRawX = symbolEl.__radialRawX;
  symbolEl.__radialOldRawY = symbolEl.__radialRawY;
  symbolEl.__radialRawX = targetLayout.rawX;
  symbolEl.__radialRawY = targetLayout.rawY;
  group2.add(symbolEl);
  data.setItemGraphicEl(dataIndex, symbolEl);
  symbolEl.__oldX = symbolEl.x;
  symbolEl.__oldY = symbolEl.y;
  updateProps(symbolEl, {
    x: targetLayout.x,
    y: targetLayout.y
  }, seriesModel);
  var symbolPath = symbolEl.getSymbolPath();
  if (seriesModel.get("layout") === "radial") {
    var realRoot = virtualRoot.children[0];
    var rootLayout = realRoot.getLayout();
    var length_1 = realRoot.children.length;
    var rad = void 0;
    var isLeft = void 0;
    if (targetLayout.x === rootLayout.x && node.isExpand === true && realRoot.children.length) {
      var center3 = {
        x: (realRoot.children[0].getLayout().x + realRoot.children[length_1 - 1].getLayout().x) / 2,
        y: (realRoot.children[0].getLayout().y + realRoot.children[length_1 - 1].getLayout().y) / 2
      };
      rad = Math.atan2(center3.y - rootLayout.y, center3.x - rootLayout.x);
      if (rad < 0) {
        rad = Math.PI * 2 + rad;
      }
      isLeft = center3.x < rootLayout.x;
      if (isLeft) {
        rad = rad - Math.PI;
      }
    } else {
      rad = Math.atan2(targetLayout.y - rootLayout.y, targetLayout.x - rootLayout.x);
      if (rad < 0) {
        rad = Math.PI * 2 + rad;
      }
      if (node.children.length === 0 || node.children.length !== 0 && node.isExpand === false) {
        isLeft = targetLayout.x < rootLayout.x;
        if (isLeft) {
          rad = rad - Math.PI;
        }
      } else {
        isLeft = targetLayout.x > rootLayout.x;
        if (!isLeft) {
          rad = rad - Math.PI;
        }
      }
    }
    var textPosition = isLeft ? "left" : "right";
    var normalLabelModel = itemModel.getModel("label");
    var rotate2 = normalLabelModel.get("rotate");
    var labelRotateRadian = rotate2 * (Math.PI / 180);
    var textContent = symbolPath.getTextContent();
    if (textContent) {
      symbolPath.setTextConfig({
        position: normalLabelModel.get("position") || textPosition,
        rotation: rotate2 == null ? -rad : labelRotateRadian,
        origin: "center"
      });
      textContent.setStyle("verticalAlign", "middle");
    }
  }
  var focus = itemModel.get(["emphasis", "focus"]);
  var focusDataIndices = focus === "relative" ? concatArray(node.getAncestorsIndices(), node.getDescendantIndices()) : focus === "ancestor" ? node.getAncestorsIndices() : focus === "descendant" ? node.getDescendantIndices() : null;
  if (focusDataIndices) {
    getECData(symbolEl).focus = focusDataIndices;
  }
  drawEdge(seriesModel, node, virtualRoot, symbolEl, sourceOldLayout, sourceLayout, targetLayout, group2);
  if (symbolEl.__edge) {
    symbolEl.onHoverStateChange = function(toState) {
      if (toState !== "blur") {
        var parentEl = node.parentNode && data.getItemGraphicEl(node.parentNode.dataIndex);
        if (!(parentEl && parentEl.hoverState === HOVER_STATE_BLUR)) {
          setStatesFlag(symbolEl.__edge, toState);
        }
      }
    };
  }
}
function drawEdge(seriesModel, node, virtualRoot, symbolEl, sourceOldLayout, sourceLayout, targetLayout, group2) {
  var itemModel = node.getModel();
  var edgeShape = seriesModel.get("edgeShape");
  var layout3 = seriesModel.get("layout");
  var orient = seriesModel.getOrient();
  var curvature = seriesModel.get(["lineStyle", "curveness"]);
  var edgeForkPosition = seriesModel.get("edgeForkPosition");
  var lineStyle = itemModel.getModel("lineStyle").getLineStyle();
  var edge = symbolEl.__edge;
  if (edgeShape === "curve") {
    if (node.parentNode && node.parentNode !== virtualRoot) {
      if (!edge) {
        edge = symbolEl.__edge = new BezierCurve_default({
          shape: getEdgeShape(layout3, orient, curvature, sourceOldLayout, sourceOldLayout)
        });
      }
      updateProps(edge, {
        shape: getEdgeShape(layout3, orient, curvature, sourceLayout, targetLayout)
      }, seriesModel);
    }
  } else if (edgeShape === "polyline") {
    if (layout3 === "orthogonal") {
      if (node !== virtualRoot && node.children && node.children.length !== 0 && node.isExpand === true) {
        var children = node.children;
        var childPoints = [];
        for (var i2 = 0; i2 < children.length; i2++) {
          var childLayout = children[i2].getLayout();
          childPoints.push([childLayout.x, childLayout.y]);
        }
        if (!edge) {
          edge = symbolEl.__edge = new TreePath({
            shape: {
              parentPoint: [targetLayout.x, targetLayout.y],
              childPoints: [[targetLayout.x, targetLayout.y]],
              orient,
              forkPosition: edgeForkPosition
            }
          });
        }
        updateProps(edge, {
          shape: {
            parentPoint: [targetLayout.x, targetLayout.y],
            childPoints
          }
        }, seriesModel);
      }
    } else {
      if (true) {
        throw new Error("The polyline edgeShape can only be used in orthogonal layout");
      }
    }
  }
  if (edge && !(edgeShape === "polyline" && !node.isExpand)) {
    edge.useStyle(defaults({
      strokeNoScale: true,
      fill: null
    }, lineStyle));
    setStatesStylesFromModel(edge, itemModel, "lineStyle");
    setDefaultStateProxy(edge);
    group2.add(edge);
  }
}
function removeNodeEdge(node, data, group2, seriesModel, removeAnimationOpt) {
  var virtualRoot = data.tree.root;
  var _a2 = getSourceNode(virtualRoot, node), source = _a2.source, sourceLayout = _a2.sourceLayout;
  var symbolEl = data.getItemGraphicEl(node.dataIndex);
  if (!symbolEl) {
    return;
  }
  var sourceSymbolEl = data.getItemGraphicEl(source.dataIndex);
  var sourceEdge = sourceSymbolEl.__edge;
  var edge = symbolEl.__edge || (source.isExpand === false || source.children.length === 1 ? sourceEdge : void 0);
  var edgeShape = seriesModel.get("edgeShape");
  var layoutOpt = seriesModel.get("layout");
  var orient = seriesModel.get("orient");
  var curvature = seriesModel.get(["lineStyle", "curveness"]);
  if (edge) {
    if (edgeShape === "curve") {
      removeElement(edge, {
        shape: getEdgeShape(layoutOpt, orient, curvature, sourceLayout, sourceLayout),
        style: {
          opacity: 0
        }
      }, seriesModel, {
        cb: function() {
          group2.remove(edge);
        },
        removeOpt: removeAnimationOpt
      });
    } else if (edgeShape === "polyline" && seriesModel.get("layout") === "orthogonal") {
      removeElement(edge, {
        shape: {
          parentPoint: [sourceLayout.x, sourceLayout.y],
          childPoints: [[sourceLayout.x, sourceLayout.y]]
        },
        style: {
          opacity: 0
        }
      }, seriesModel, {
        cb: function() {
          group2.remove(edge);
        },
        removeOpt: removeAnimationOpt
      });
    }
  }
}
function getSourceNode(virtualRoot, node) {
  var source = node.parentNode === virtualRoot ? node : node.parentNode || node;
  var sourceLayout;
  while (sourceLayout = source.getLayout(), sourceLayout == null) {
    source = source.parentNode === virtualRoot ? source : source.parentNode || source;
  }
  return {
    source,
    sourceLayout
  };
}
function removeNode(data, dataIndex, symbolEl, group2, seriesModel) {
  var node = data.tree.getNodeByDataIndex(dataIndex);
  var virtualRoot = data.tree.root;
  var sourceLayout = getSourceNode(virtualRoot, node).sourceLayout;
  var removeAnimationOpt = {
    duration: seriesModel.get("animationDurationUpdate"),
    easing: seriesModel.get("animationEasingUpdate")
  };
  removeElement(symbolEl, {
    x: sourceLayout.x + 1,
    y: sourceLayout.y + 1
  }, seriesModel, {
    cb: function() {
      group2.remove(symbolEl);
      data.setItemGraphicEl(dataIndex, null);
    },
    removeOpt: removeAnimationOpt
  });
  symbolEl.fadeOut(null, data.hostModel, {
    fadeLabel: true,
    animation: removeAnimationOpt
  });
  each(node.children, function(childNode) {
    removeNodeEdge(childNode, data, group2, seriesModel, removeAnimationOpt);
  });
  removeNodeEdge(node, data, group2, seriesModel, removeAnimationOpt);
}
function getEdgeShape(layoutOpt, orient, curvature, sourceLayout, targetLayout) {
  var cpx1;
  var cpy1;
  var cpx2;
  var cpy2;
  var x1;
  var x2;
  var y1;
  var y2;
  if (layoutOpt === "radial") {
    x1 = sourceLayout.rawX;
    y1 = sourceLayout.rawY;
    x2 = targetLayout.rawX;
    y2 = targetLayout.rawY;
    var radialCoor1 = radialCoordinate(x1, y1);
    var radialCoor2 = radialCoordinate(x1, y1 + (y2 - y1) * curvature);
    var radialCoor3 = radialCoordinate(x2, y2 + (y1 - y2) * curvature);
    var radialCoor4 = radialCoordinate(x2, y2);
    return {
      x1: radialCoor1.x || 0,
      y1: radialCoor1.y || 0,
      x2: radialCoor4.x || 0,
      y2: radialCoor4.y || 0,
      cpx1: radialCoor2.x || 0,
      cpy1: radialCoor2.y || 0,
      cpx2: radialCoor3.x || 0,
      cpy2: radialCoor3.y || 0
    };
  } else {
    x1 = sourceLayout.x;
    y1 = sourceLayout.y;
    x2 = targetLayout.x;
    y2 = targetLayout.y;
    if (orient === "LR" || orient === "RL") {
      cpx1 = x1 + (x2 - x1) * curvature;
      cpy1 = y1;
      cpx2 = x2 + (x1 - x2) * curvature;
      cpy2 = y2;
    }
    if (orient === "TB" || orient === "BT") {
      cpx1 = x1;
      cpy1 = y1 + (y2 - y1) * curvature;
      cpx2 = x2;
      cpy2 = y2 + (y1 - y2) * curvature;
    }
  }
  return {
    x1,
    y1,
    x2,
    y2,
    cpx1,
    cpy1,
    cpx2,
    cpy2
  };
}

// node_modules/echarts/lib/chart/tree/treeVisual.js
var treeVisualStageHandler = createSimpleOverallStageHandler(SERIES_TYPE_TREE, treeVisual);
function treeVisual(ecModel) {
  ecModel.eachSeriesByType(SERIES_TYPE_TREE, function(seriesModel) {
    var data = seriesModel.getData();
    var tree = data.tree;
    tree.eachNode(function(node) {
      var model2 = node.getModel();
      var style2 = model2.getModel("itemStyle").getItemStyle();
      var existsStyle = data.ensureUniqueItemVisual(node.dataIndex, "style");
      extend(existsStyle, style2);
    });
  });
}

// node_modules/echarts/lib/chart/helper/enableAriaDecalForTree.js
function enableAriaDecalForTree(seriesModel) {
  var data = seriesModel.getData();
  var tree = data.tree;
  var decalPaletteScope = {};
  tree.eachNode(function(node) {
    var current = node;
    while (current && current.depth > 1) {
      current = current.parentNode;
    }
    var decal = getDecalFromPalette(seriesModel.ecModel, current.name || current.dataIndex + "", decalPaletteScope);
    node.setVisual("decal", decal);
  });
}

// node_modules/echarts/lib/chart/treemap/TreemapSeries.js
var TreemapSeriesModel = (
  /** @class */
  (function(_super) {
    __extends(TreemapSeriesModel2, _super);
    function TreemapSeriesModel2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = TreemapSeriesModel2.type;
      _this.preventUsingHoverLayer = true;
      return _this;
    }
    TreemapSeriesModel2.prototype.getInitialData = function(option, ecModel) {
      var root = {
        name: option.name,
        children: option.data
      };
      completeTreeValue(root);
      var levels = option.levels || [];
      var designatedVisualItemStyle = this.designatedVisualItemStyle = {};
      var designatedVisualModel = new Model_default({
        itemStyle: designatedVisualItemStyle
      }, this, ecModel);
      levels = option.levels = setDefault(levels, ecModel);
      var levelModels = map2(levels || [], function(levelDefine) {
        return new Model_default(levelDefine, designatedVisualModel, ecModel);
      }, this);
      var tree = Tree_default.createTree(root, this, beforeLink);
      function beforeLink(nodeData) {
        nodeData.wrapMethod("getItemModel", function(model2, idx) {
          var node = tree.getNodeByDataIndex(idx);
          var levelModel = node ? levelModels[node.depth] : null;
          model2.parentModel = levelModel || designatedVisualModel;
          return model2;
        });
      }
      return tree.data;
    };
    TreemapSeriesModel2.prototype.optionUpdated = function() {
      this.resetViewRoot();
    };
    TreemapSeriesModel2.prototype.formatTooltip = function(dataIndex, multipleSeries, dataType) {
      var data = this.getData();
      var value = this.getRawValue(dataIndex);
      var name = data.getName(dataIndex);
      return createTooltipMarkup("nameValue", {
        name,
        value
      });
    };
    TreemapSeriesModel2.prototype.getDataParams = function(dataIndex) {
      var params = _super.prototype.getDataParams.apply(this, arguments);
      var node = this.getData().tree.getNodeByDataIndex(dataIndex);
      params.treeAncestors = wrapTreePathInfo(node, this);
      params.treePathInfo = params.treeAncestors;
      return params;
    };
    TreemapSeriesModel2.prototype.setLayoutInfo = function(layoutInfo) {
      this.layoutInfo = this.layoutInfo || {};
      extend(this.layoutInfo, layoutInfo);
    };
    TreemapSeriesModel2.prototype.mapIdToIndex = function(id) {
      var idIndexMap = this._idIndexMap;
      if (!idIndexMap) {
        idIndexMap = this._idIndexMap = createHashMap();
        this._idIndexMapCount = 0;
      }
      var index = idIndexMap.get(id);
      if (index == null) {
        idIndexMap.set(id, index = this._idIndexMapCount++);
      }
      return index;
    };
    TreemapSeriesModel2.prototype.getViewRoot = function() {
      return this._viewRoot;
    };
    TreemapSeriesModel2.prototype.resetViewRoot = function(viewRoot) {
      viewRoot ? this._viewRoot = viewRoot : viewRoot = this._viewRoot;
      var root = this.getRawData().tree.root;
      if (!viewRoot || viewRoot !== root && !root.contains(viewRoot)) {
        this._viewRoot = root;
      }
    };
    TreemapSeriesModel2.prototype.enableAriaDecal = function() {
      enableAriaDecalForTree(this);
    };
    TreemapSeriesModel2.type = "series.treemap";
    TreemapSeriesModel2.layoutMode = "box";
    TreemapSeriesModel2.defaultOption = {
      // Disable progressive rendering
      progressive: 0,
      // size: ['80%', '80%'],            // deprecated, compatible with ec2.
      // `coordinateSystem` can be declared as 'matrix', 'calendar',
      //  which provides box layout container.
      coordinateSystemUsage: "box",
      left: tokens_default.size.l,
      top: tokens_default.size.xxxl,
      right: tokens_default.size.l,
      bottom: tokens_default.size.xxxl,
      sort: true,
      clipWindow: "origin",
      squareRatio: 0.5 * (1 + Math.sqrt(5)),
      leafDepth: null,
      drillDownIcon: "▶",
      // to align specialized icon. ▷▶❒❐▼✚
      zoomToNodeRatio: 0.32 * 0.32,
      scaleLimit: {
        max: 5,
        min: 0.2
      },
      roam: true,
      roamTrigger: "global",
      nodeClick: "zoomToNode",
      animation: true,
      animationDurationUpdate: 900,
      animationEasing: "quinticInOut",
      breadcrumb: {
        show: true,
        height: 22,
        left: "center",
        bottom: tokens_default.size.m,
        // right
        // bottom
        emptyItemWidth: 25,
        itemStyle: {
          color: tokens_default.color.backgroundShade,
          textStyle: {
            color: tokens_default.color.secondary
          }
        },
        emphasis: {
          itemStyle: {
            color: tokens_default.color.background
          }
        }
      },
      label: {
        show: true,
        // Do not use textDistance, for ellipsis rect just the same as treemap node rect.
        distance: 0,
        padding: 5,
        position: "inside",
        // formatter: null,
        color: tokens_default.color.neutral00,
        overflow: "truncate"
        // align
        // verticalAlign
      },
      upperLabel: {
        show: false,
        position: [0, "50%"],
        height: 20,
        // formatter: null,
        // color: '#fff',
        overflow: "truncate",
        // align: null,
        verticalAlign: "middle"
      },
      itemStyle: {
        color: null,
        colorAlpha: null,
        colorSaturation: null,
        borderWidth: 0,
        gapWidth: 0,
        borderColor: tokens_default.color.neutral00,
        borderColorSaturation: null
        // If specified, borderColor will be ineffective, and the
        // border color is evaluated by color of current node and
        // borderColorSaturation.
      },
      emphasis: {
        upperLabel: {
          show: true,
          position: [0, "50%"],
          overflow: "truncate",
          verticalAlign: "middle"
        }
      },
      visualDimension: 0,
      visualMin: null,
      visualMax: null,
      color: [],
      // level[n].color (if necessary).
      // + Specify color list of each level. level[0].color would be global
      // color list if not specified. (see method `setDefault`).
      // + But set as a empty array to forbid fetch color from global palette
      // when using nodeModel.get('color'), otherwise nodes on deep level
      // will always has color palette set and are not able to inherit color
      // from parent node.
      // + TreemapSeries.color can not be set as 'none', otherwise effect
      // legend color fetching (see seriesColor.js).
      colorAlpha: null,
      colorSaturation: null,
      colorMappingBy: "index",
      visibleMin: 10,
      // be rendered. Only works when sort is 'asc' or 'desc'.
      childrenVisibleMin: null,
      // grandchildren will not show.
      // Why grandchildren? If not grandchildren but children,
      // some siblings show children and some not,
      // the appearance may be mess and not consistent,
      levels: []
      // Each item: {
      //     visibleMin, itemStyle, visualDimension, label
      // }
    };
    return TreemapSeriesModel2;
  })(Series_default)
);
function completeTreeValue(dataNode) {
  var sum2 = 0;
  each(dataNode.children, function(child) {
    completeTreeValue(child);
    var childValue = child.value;
    isArray(childValue) && (childValue = childValue[0]);
    sum2 += childValue;
  });
  var thisValue = dataNode.value;
  if (isArray(thisValue)) {
    thisValue = thisValue[0];
  }
  if (thisValue == null || isNaN(thisValue)) {
    thisValue = sum2;
  }
  if (thisValue < 0) {
    thisValue = 0;
  }
  isArray(dataNode.value) ? dataNode.value[0] = thisValue : dataNode.value = thisValue;
}
function setDefault(levels, ecModel) {
  var globalColorList = normalizeToArray(ecModel.get("color"));
  var globalDecalList = normalizeToArray(ecModel.get(["aria", "decal", "decals"]));
  if (!globalColorList) {
    return;
  }
  levels = levels || [];
  var hasColorDefine;
  var hasDecalDefine;
  each(levels, function(levelDefine) {
    var model2 = new Model_default(levelDefine);
    var modelColor = model2.get("color");
    var modelDecal = model2.get("decal");
    if (model2.get(["itemStyle", "color"]) || modelColor && modelColor !== "none") {
      hasColorDefine = true;
    }
    if (model2.get(["itemStyle", "decal"]) || modelDecal && modelDecal !== "none") {
      hasDecalDefine = true;
    }
  });
  var level0 = levels[0] || (levels[0] = {});
  if (!hasColorDefine) {
    level0.color = globalColorList.slice();
  }
  if (!hasDecalDefine && globalDecalList) {
    level0.decal = globalDecalList.slice();
  }
  return levels;
}

// node_modules/echarts/lib/chart/treemap/Breadcrumb.js
var TEXT_PADDING = 8;
var ITEM_GAP = 8;
var ARRAY_LENGTH = 5;
var Breadcrumb = (
  /** @class */
  (function() {
    function Breadcrumb2(containerGroup) {
      this.group = new Group_default();
      containerGroup.add(this.group);
    }
    Breadcrumb2.prototype.render = function(seriesModel, api, targetNode, onSelect) {
      var model2 = seriesModel.getModel("breadcrumb");
      var thisGroup = this.group;
      thisGroup.removeAll();
      if (!model2.get("show") || !targetNode) {
        return;
      }
      var normalStyleModel = model2.getModel("itemStyle");
      var emphasisModel = model2.getModel("emphasis");
      var textStyleModel = normalStyleModel.getModel("textStyle");
      var emphasisTextStyleModel = emphasisModel.getModel(["itemStyle", "textStyle"]);
      var refContainer = createBoxLayoutReference(seriesModel, api).refContainer;
      var boxLayoutParams = {
        left: model2.get("left"),
        right: model2.get("right"),
        top: model2.get("top"),
        bottom: model2.get("bottom")
      };
      var layoutParam = {
        emptyItemWidth: model2.get("emptyItemWidth"),
        totalWidth: 0,
        renderList: []
      };
      var availableSize = getLayoutRect(boxLayoutParams, refContainer);
      this._prepare(targetNode, layoutParam, textStyleModel);
      this._renderContent(seriesModel, layoutParam, availableSize, normalStyleModel, emphasisModel, textStyleModel, emphasisTextStyleModel, onSelect);
      positionElement(thisGroup, boxLayoutParams, refContainer);
    };
    Breadcrumb2.prototype._prepare = function(targetNode, layoutParam, textStyleModel) {
      for (var node = targetNode; node; node = node.parentNode) {
        var text = convertOptionIdName(node.getModel().get("name"), "");
        var textRect = textStyleModel.getTextRect(text);
        var itemWidth = Math.max(textRect.width + TEXT_PADDING * 2, layoutParam.emptyItemWidth);
        layoutParam.totalWidth += itemWidth + ITEM_GAP;
        layoutParam.renderList.push({
          node,
          text,
          width: itemWidth
        });
      }
    };
    Breadcrumb2.prototype._renderContent = function(seriesModel, layoutParam, availableSize, normalStyleModel, emphasisModel, textStyleModel, emphasisTextStyleModel, onSelect) {
      var lastX = 0;
      var emptyItemWidth = layoutParam.emptyItemWidth;
      var height = seriesModel.get(["breadcrumb", "height"]);
      var totalWidth = layoutParam.totalWidth;
      var renderList = layoutParam.renderList;
      var emphasisItemStyle = emphasisModel.getModel("itemStyle").getItemStyle();
      for (var i2 = renderList.length - 1; i2 >= 0; i2--) {
        var item = renderList[i2];
        var itemNode = item.node;
        var itemWidth = item.width;
        var text = item.text;
        if (totalWidth > availableSize.width) {
          totalWidth -= itemWidth - emptyItemWidth;
          itemWidth = emptyItemWidth;
          text = null;
        }
        var el = new Polygon_default({
          shape: {
            points: makeItemPoints(lastX, 0, itemWidth, height, i2 === renderList.length - 1, i2 === 0)
          },
          style: defaults(normalStyleModel.getItemStyle(), {
            lineJoin: "bevel"
          }),
          textContent: new Text_default({
            style: createTextStyle(textStyleModel, {
              text
            })
          }),
          textConfig: {
            position: "inside"
          },
          z2: Z2_EMPHASIS_LIFT * 1e4,
          onclick: curry(onSelect, itemNode)
        });
        el.disableLabelAnimation = true;
        el.getTextContent().ensureState("emphasis").style = createTextStyle(emphasisTextStyleModel, {
          text
        });
        el.ensureState("emphasis").style = emphasisItemStyle;
        toggleHoverEmphasis(el, emphasisModel.get("focus"), emphasisModel.get("blurScope"), emphasisModel.get("disabled"));
        this.group.add(el);
        packEventData(el, seriesModel, itemNode);
        lastX += itemWidth + ITEM_GAP;
      }
    };
    Breadcrumb2.prototype.remove = function() {
      this.group.removeAll();
    };
    return Breadcrumb2;
  })()
);
function makeItemPoints(x, y, itemWidth, itemHeight, head, tail) {
  var points4 = [[head ? x : x - ARRAY_LENGTH, y], [x + itemWidth, y], [x + itemWidth, y + itemHeight], [head ? x : x - ARRAY_LENGTH, y + itemHeight]];
  !tail && points4.splice(2, 0, [x + itemWidth + ARRAY_LENGTH, y + itemHeight / 2]);
  !head && points4.push([x, y + itemHeight / 2]);
  return points4;
}
function packEventData(el, seriesModel, itemNode) {
  getECData(el).eventData = {
    componentType: "series",
    componentSubType: "treemap",
    componentIndex: seriesModel.componentIndex,
    seriesIndex: seriesModel.seriesIndex,
    seriesName: seriesModel.name,
    seriesType: "treemap",
    selfType: "breadcrumb",
    nodeData: {
      dataIndex: itemNode && itemNode.dataIndex,
      name: itemNode && itemNode.name
    },
    treePathInfo: itemNode && wrapTreePathInfo(itemNode, seriesModel)
  };
}
var Breadcrumb_default = Breadcrumb;

// node_modules/echarts/lib/util/animation.js
var AnimationWrap = (
  /** @class */
  (function() {
    function AnimationWrap2() {
      this._storage = [];
      this._elExistsMap = {};
    }
    AnimationWrap2.prototype.add = function(el, target, duration, delay2, easing) {
      if (this._elExistsMap[el.id]) {
        return false;
      }
      this._elExistsMap[el.id] = true;
      this._storage.push({
        el,
        target,
        duration,
        delay: delay2,
        easing
      });
      return true;
    };
    AnimationWrap2.prototype.finished = function(callback) {
      this._finishedCallback = callback;
      return this;
    };
    AnimationWrap2.prototype.start = function() {
      var _this = this;
      var count2 = this._storage.length;
      var checkTerminate = function() {
        count2--;
        if (count2 <= 0) {
          _this._storage.length = 0;
          _this._elExistsMap = {};
          _this._finishedCallback && _this._finishedCallback();
        }
      };
      for (var i2 = 0, len2 = this._storage.length; i2 < len2; i2++) {
        var item = this._storage[i2];
        item.el.animateTo(item.target, {
          duration: item.duration,
          delay: item.delay,
          easing: item.easing,
          setToFinal: true,
          done: checkTerminate,
          aborted: checkTerminate
        });
      }
      return this;
    };
    return AnimationWrap2;
  })()
);
function createWrap() {
  return new AnimationWrap();
}

// node_modules/echarts/lib/chart/treemap/treemapLayout.js
function calculateCurrentZoom(baseSize, currSize) {
  return currSize.width / baseSize.width || currSize.height / baseSize.height || 1;
}
function treemapClampZoom(zoom, seriesModel) {
  return clampByZoomLimit(zoom, seriesModel.get("scaleLimit", true));
}

// node_modules/echarts/lib/chart/treemap/TreemapView.js
var Group2 = Group_default;
var Rect2 = Rect_default;
var DRAG_THRESHOLD = 3;
var PATH_LABEL_NOAMAL = "label";
var PATH_UPPERLABEL_NORMAL = "upperLabel";
var Z2_BASE = Z2_EMPHASIS_LIFT * 10;
var Z2_BG = Z2_EMPHASIS_LIFT * 2;
var Z2_CONTENT = Z2_EMPHASIS_LIFT * 3;
var getStateItemStyle = makeStyleMapper([
  ["fill", "color"],
  // `borderColor` and `borderWidth` has been occupied,
  // so use `stroke` to indicate the stroke of the rect.
  ["stroke", "strokeColor"],
  ["lineWidth", "strokeWidth"],
  ["shadowBlur"],
  ["shadowOffsetX"],
  ["shadowOffsetY"],
  ["shadowColor"]
  // Option decal is in `DecalObject` but style.decal is in `PatternObject`.
  // So do not transfer decal directly.
]);
var getItemStyleNormal = function(model2) {
  var itemStyle = getStateItemStyle(model2);
  itemStyle.stroke = itemStyle.fill = itemStyle.lineWidth = null;
  return itemStyle;
};
var inner11 = makeInner();
var TreemapView = (
  /** @class */
  (function(_super) {
    __extends(TreemapView2, _super);
    function TreemapView2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = TreemapView2.type;
      _this._state = "ready";
      _this._storage = createStorage();
      return _this;
    }
    TreemapView2.prototype.render = function(seriesModel, ecModel, api, payload) {
      var models = ecModel.findComponents({
        mainType: "series",
        subType: "treemap",
        query: payload
      });
      if (indexOf(models, seriesModel) < 0) {
        return;
      }
      this.seriesModel = seriesModel;
      this.api = api;
      this.ecModel = ecModel;
      var types = ["treemapZoomToNode", "treemapRootToNode"];
      var targetInfo = retrieveTargetInfo(payload, types, seriesModel);
      var payloadType = payload && payload.type;
      var layoutInfo = seriesModel.layoutInfo;
      var isInit = !this._oldTree;
      var thisStorage = this._storage;
      var reRoot = payloadType === "treemapRootToNode" && targetInfo && thisStorage ? {
        rootNodeGroup: thisStorage.nodeGroup[targetInfo.node.getRawIndex()],
        direction: payload.direction
      } : null;
      var containerGroup = this._giveContainerGroup(layoutInfo);
      var hasAnimation = seriesModel.get("animation");
      var renderResult = this._doRender(containerGroup, seriesModel, reRoot);
      hasAnimation && !isInit && (!payloadType || payloadType === "treemapZoomToNode" || payloadType === "treemapRootToNode") ? this._doAnimation(containerGroup, renderResult, seriesModel, reRoot) : renderResult.renderFinally();
      this._resetController(api);
      this._renderBreadcrumb(seriesModel, api, targetInfo);
    };
    TreemapView2.prototype._giveContainerGroup = function(layoutInfo) {
      var containerGroup = this._containerGroup;
      if (!containerGroup) {
        containerGroup = this._containerGroup = new Group2();
        this._initEvents(containerGroup);
        this.group.add(containerGroup);
      }
      containerGroup.x = layoutInfo.x;
      containerGroup.y = layoutInfo.y;
      return containerGroup;
    };
    TreemapView2.prototype._doRender = function(containerGroup, seriesModel, reRoot) {
      var thisTree = seriesModel.getData().tree;
      var oldTree = this._oldTree;
      var lastsForAnimation = createStorage();
      var thisStorage = createStorage();
      var oldStorage = this._storage;
      var willInvisibleEls = [];
      function doRenderNode(thisNode, oldNode, parentGroup, depth) {
        return renderNode(seriesModel, thisStorage, oldStorage, reRoot, lastsForAnimation, willInvisibleEls, thisNode, oldNode, parentGroup, depth);
      }
      dualTravel(thisTree.root ? [thisTree.root] : [], oldTree && oldTree.root ? [oldTree.root] : [], containerGroup, thisTree === oldTree || !oldTree, 0);
      var willDeleteEls = clearStorage(oldStorage);
      this._oldTree = thisTree;
      this._storage = thisStorage;
      return {
        lastsForAnimation,
        willDeleteEls,
        renderFinally
      };
      function dualTravel(thisViewChildren, oldViewChildren, parentGroup, sameTree, depth) {
        if (sameTree) {
          oldViewChildren = thisViewChildren;
          each(thisViewChildren, function(child, index) {
            !child.isRemoved() && processNode(index, index);
          });
        } else {
          new DataDiffer_default(oldViewChildren, thisViewChildren, getKey2, getKey2).add(processNode).update(processNode).remove(curry(processNode, null)).execute();
        }
        function getKey2(node) {
          return node.getId();
        }
        function processNode(newIndex, oldIndex) {
          var thisNode = newIndex != null ? thisViewChildren[newIndex] : null;
          var oldNode = oldIndex != null ? oldViewChildren[oldIndex] : null;
          var group2 = doRenderNode(thisNode, oldNode, parentGroup, depth);
          group2 && dualTravel(thisNode && thisNode.viewChildren || [], oldNode && oldNode.viewChildren || [], group2, sameTree, depth + 1);
        }
      }
      function clearStorage(storage2) {
        var willDeleteEls2 = createStorage();
        storage2 && each(storage2, function(store, storageName) {
          var delEls = willDeleteEls2[storageName];
          each(store, function(el) {
            el && (delEls.push(el), inner11(el).willDelete = true);
          });
        });
        return willDeleteEls2;
      }
      function renderFinally() {
        each(willDeleteEls, function(els) {
          each(els, function(el) {
            el.parent && el.parent.remove(el);
          });
        });
        each(willInvisibleEls, function(el) {
          el.invisible = true;
          el.dirty();
        });
      }
    };
    TreemapView2.prototype._doAnimation = function(containerGroup, renderResult, seriesModel, reRoot) {
      var durationOption = seriesModel.get("animationDurationUpdate");
      var easingOption = seriesModel.get("animationEasing");
      var duration = (isFunction(durationOption) ? 0 : durationOption) || 0;
      var easing = (isFunction(easingOption) ? null : easingOption) || "cubicOut";
      var animationWrap = createWrap();
      each(renderResult.willDeleteEls, function(store, storageName) {
        each(store, function(el, rawIndex) {
          if (el.invisible) {
            return;
          }
          var parent = el.parent;
          var target;
          var innerStore = inner11(parent);
          if (reRoot && reRoot.direction === "drillDown") {
            target = parent === reRoot.rootNodeGroup ? {
              shape: {
                x: 0,
                y: 0,
                width: innerStore.nodeWidth,
                height: innerStore.nodeHeight
              },
              style: {
                opacity: 0
              }
            } : {
              style: {
                opacity: 0
              }
            };
          } else {
            var targetX = 0;
            var targetY = 0;
            if (!innerStore.willDelete) {
              targetX = innerStore.nodeWidth / 2;
              targetY = innerStore.nodeHeight / 2;
            }
            target = storageName === "nodeGroup" ? {
              x: targetX,
              y: targetY,
              style: {
                opacity: 0
              }
            } : {
              shape: {
                x: targetX,
                y: targetY,
                width: 0,
                height: 0
              },
              style: {
                opacity: 0
              }
            };
          }
          target && animationWrap.add(el, target, duration, 0, easing);
        });
      });
      each(this._storage, function(store, storageName) {
        each(store, function(el, rawIndex) {
          var last = renderResult.lastsForAnimation[storageName][rawIndex];
          var target = {};
          if (!last) {
            return;
          }
          if (el instanceof Group_default) {
            if (last.oldX != null) {
              target.x = el.x;
              target.y = el.y;
              el.x = last.oldX;
              el.y = last.oldY;
            }
          } else {
            if (last.oldShape) {
              target.shape = extend({}, el.shape);
              el.setShape(last.oldShape);
            }
            if (last.fadein) {
              el.setStyle("opacity", 0);
              target.style = {
                opacity: 1
              };
            } else if (el.style.opacity !== 1) {
              target.style = {
                opacity: 1
              };
            }
          }
          animationWrap.add(el, target, duration, 0, easing);
        });
      }, this);
      this._state = "animating";
      animationWrap.finished(bind(function() {
        this._state = "ready";
        renderResult.renderFinally();
      }, this)).start();
    };
    TreemapView2.prototype._resetController = function(api) {
      var _this = this;
      var seriesModel = this.seriesModel;
      var controller = this._controller;
      if (!controller) {
        controller = this._controller = new RoamController_default(api.getZr());
        controller.on("pan", bind(this._onPan, this));
        controller.on("zoom", bind(this._onZoom, this));
      }
      controller.enable(seriesModel.get("roam"), {
        api,
        zInfo: {
          component: seriesModel
        },
        triggerInfo: {
          roamTrigger: seriesModel.get("roamTrigger"),
          isInSelf: function(e2, x, y) {
            var containerGroup = _this._containerGroup;
            return containerGroup ? containerGroup.getBoundingRect().contain(x - containerGroup.x, y - containerGroup.y) : false;
          }
        }
      });
    };
    TreemapView2.prototype._clearController = function() {
      var controller = this._controller;
      if (controller) {
        controller.dispose();
        controller = null;
      }
    };
    TreemapView2.prototype._onPan = function(e2) {
      if (this._state !== "animating" && (Math.abs(e2.dx) > DRAG_THRESHOLD || Math.abs(e2.dy) > DRAG_THRESHOLD)) {
        var root = this.seriesModel.getData().tree.root;
        if (!root) {
          return;
        }
        var rootLayout = root.getLayout();
        if (!rootLayout) {
          return;
        }
        this.api.dispatchAction({
          type: "treemapMove",
          from: this.uid,
          seriesId: this.seriesModel.id,
          rootRect: {
            x: rootLayout.x + e2.dx,
            y: rootLayout.y + e2.dy,
            width: rootLayout.width,
            height: rootLayout.height
          }
        });
      }
    };
    TreemapView2.prototype._onZoom = function(e2) {
      var mouseX = e2.originX;
      var mouseY = e2.originY;
      var zoomDelta = e2.scale;
      var seriesModel = this.seriesModel;
      if (this._state !== "animating") {
        var root = seriesModel.getData().tree.root;
        if (!root) {
          return;
        }
        var rootLayout = root.getLayout();
        if (!rootLayout) {
          return;
        }
        var rect = new BoundingRect_default(rootLayout.x, rootLayout.y, rootLayout.width, rootLayout.height);
        var layoutInfo = seriesModel.layoutInfo;
        var currZoom = calculateCurrentZoom(layoutInfo, rootLayout);
        var newZoom = currZoom * zoomDelta;
        newZoom = treemapClampZoom(newZoom, seriesModel);
        var zoomScale = newZoom / currZoom;
        mouseX -= layoutInfo.x;
        mouseY -= layoutInfo.y;
        var m2 = create();
        translate(m2, m2, [-mouseX, -mouseY]);
        scale(m2, m2, [zoomScale, zoomScale]);
        translate(m2, m2, [mouseX, mouseY]);
        rect.applyTransform(m2);
        this.api.dispatchAction({
          type: "treemapRender",
          from: this.uid,
          seriesId: this.seriesModel.id,
          rootRect: {
            x: rect.x,
            y: rect.y,
            width: rect.width,
            height: rect.height
          }
        });
      }
    };
    TreemapView2.prototype._initEvents = function(containerGroup) {
      var _this = this;
      containerGroup.on("click", function(e2) {
        if (_this._state !== "ready") {
          return;
        }
        var nodeClick = _this.seriesModel.get("nodeClick", true);
        if (!nodeClick) {
          return;
        }
        var targetInfo = _this.findTarget(e2.offsetX, e2.offsetY);
        if (!targetInfo) {
          return;
        }
        var node = targetInfo.node;
        if (node.getLayout().isLeafRoot) {
          _this._rootToNode(targetInfo);
        } else {
          if (nodeClick === "zoomToNode") {
            _this._zoomToNode(targetInfo);
          } else if (nodeClick === "link") {
            var itemModel = node.hostTree.data.getItemModel(node.dataIndex);
            var link = itemModel.get("link", true);
            var linkTarget = itemModel.get("target", true) || "blank";
            link && windowOpen(link, linkTarget);
          }
        }
      }, this);
    };
    TreemapView2.prototype._renderBreadcrumb = function(seriesModel, api, targetInfo) {
      var _this = this;
      if (!targetInfo) {
        targetInfo = seriesModel.get("leafDepth", true) != null ? {
          node: seriesModel.getViewRoot()
        } : this.findTarget(api.getWidth() / 2, api.getHeight() / 2);
        if (!targetInfo) {
          targetInfo = {
            node: seriesModel.getData().tree.root
          };
        }
      }
      (this._breadcrumb || (this._breadcrumb = new Breadcrumb_default(this.group))).render(seriesModel, api, targetInfo.node, function(node) {
        if (_this._state !== "animating") {
          aboveViewRoot(seriesModel.getViewRoot(), node) ? _this._rootToNode({
            node
          }) : _this._zoomToNode({
            node
          });
        }
      });
    };
    TreemapView2.prototype.remove = function() {
      this._clearController();
      this._containerGroup && this._containerGroup.removeAll();
      this._storage = createStorage();
      this._state = "ready";
      this._breadcrumb && this._breadcrumb.remove();
    };
    TreemapView2.prototype.dispose = function() {
      this._clearController();
    };
    TreemapView2.prototype._zoomToNode = function(targetInfo) {
      this.api.dispatchAction({
        type: "treemapZoomToNode",
        from: this.uid,
        seriesId: this.seriesModel.id,
        targetNode: targetInfo.node
      });
    };
    TreemapView2.prototype._rootToNode = function(targetInfo) {
      this.api.dispatchAction({
        type: "treemapRootToNode",
        from: this.uid,
        seriesId: this.seriesModel.id,
        targetNode: targetInfo.node
      });
    };
    TreemapView2.prototype.findTarget = function(x, y) {
      var targetInfo;
      var viewRoot = this.seriesModel.getViewRoot();
      viewRoot.eachNode({
        attr: "viewChildren",
        order: "preorder"
      }, function(node) {
        var bgEl = this._storage.background[node.getRawIndex()];
        if (bgEl) {
          var point = bgEl.transformCoordToLocal(x, y);
          var shape = bgEl.shape;
          if (shape.x <= point[0] && point[0] <= shape.x + shape.width && shape.y <= point[1] && point[1] <= shape.y + shape.height) {
            targetInfo = {
              node,
              offsetX: point[0],
              offsetY: point[1]
            };
          } else {
            return false;
          }
        }
      }, this);
      return targetInfo;
    };
    TreemapView2.type = "treemap";
    return TreemapView2;
  })(Chart_default)
);
function createStorage() {
  return {
    nodeGroup: [],
    background: [],
    content: []
  };
}
function renderNode(seriesModel, thisStorage, oldStorage, reRoot, lastsForAnimation, willInvisibleEls, thisNode, oldNode, parentGroup, depth) {
  if (!thisNode) {
    return;
  }
  var thisLayout = thisNode.getLayout();
  var data = seriesModel.getData();
  var nodeModel = thisNode.getModel();
  data.setItemGraphicEl(thisNode.dataIndex, null);
  if (!thisLayout || !thisLayout.isInView) {
    return;
  }
  var thisWidth = thisLayout.width;
  var thisHeight = thisLayout.height;
  var borderWidth = thisLayout.borderWidth;
  var thisInvisible = thisLayout.invisible;
  var thisRawIndex = thisNode.getRawIndex();
  var oldRawIndex = oldNode && oldNode.getRawIndex();
  var thisViewChildren = thisNode.viewChildren;
  var upperHeight = thisLayout.upperHeight;
  var isParent = thisViewChildren && thisViewChildren.length;
  var itemStyleNormalModel = nodeModel.getModel("itemStyle");
  var itemStyleEmphasisModel = nodeModel.getModel(["emphasis", "itemStyle"]);
  var itemStyleBlurModel = nodeModel.getModel(["blur", "itemStyle"]);
  var itemStyleSelectModel = nodeModel.getModel(["select", "itemStyle"]);
  var borderRadius = itemStyleNormalModel.get("borderRadius") || 0;
  var group2 = giveGraphic("nodeGroup", Group2);
  if (!group2) {
    return;
  }
  parentGroup.add(group2);
  group2.x = thisLayout.x || 0;
  group2.y = thisLayout.y || 0;
  group2.markRedraw();
  inner11(group2).nodeWidth = thisWidth;
  inner11(group2).nodeHeight = thisHeight;
  if (thisLayout.isAboveViewRoot) {
    return group2;
  }
  var bg = giveGraphic("background", Rect2, depth, Z2_BG);
  bg && renderBackground(group2, bg, isParent && thisLayout.upperLabelHeight);
  var emphasisModel = nodeModel.getModel("emphasis");
  var focus = emphasisModel.get("focus");
  var blurScope = emphasisModel.get("blurScope");
  var isDisabled = emphasisModel.get("disabled");
  var focusOrIndices = focus === "ancestor" ? thisNode.getAncestorsIndices() : focus === "descendant" ? thisNode.getDescendantIndices() : focus;
  if (isParent) {
    if (isHighDownDispatcher(group2)) {
      setAsHighDownDispatcher(group2, false);
    }
    if (bg) {
      setAsHighDownDispatcher(bg, !isDisabled);
      data.setItemGraphicEl(thisNode.dataIndex, bg);
      enableHoverFocus(bg, focusOrIndices, blurScope);
    }
  } else {
    var content = giveGraphic("content", Rect2, depth, Z2_CONTENT);
    content && renderContent(group2, content);
    bg.disableMorphing = true;
    if (bg && isHighDownDispatcher(bg)) {
      setAsHighDownDispatcher(bg, false);
    }
    setAsHighDownDispatcher(group2, !isDisabled);
    data.setItemGraphicEl(thisNode.dataIndex, group2);
    var cursorStyle = nodeModel.getShallow("cursor");
    cursorStyle && content.attr("cursor", cursorStyle);
    enableHoverFocus(group2, focusOrIndices, blurScope);
  }
  return group2;
  function renderBackground(group3, bg2, useUpperLabel) {
    var ecData = getECData(bg2);
    ecData.dataIndex = thisNode.dataIndex;
    ecData.seriesIndex = seriesModel.seriesIndex;
    bg2.setShape({
      x: 0,
      y: 0,
      width: thisWidth,
      height: thisHeight,
      r: borderRadius
    });
    if (thisInvisible) {
      processInvisible(bg2);
    } else {
      bg2.invisible = false;
      var style2 = thisNode.getVisual("style");
      var visualBorderColor = style2.stroke;
      var normalStyle = getItemStyleNormal(itemStyleNormalModel);
      normalStyle.fill = visualBorderColor;
      var emphasisStyle = getStateItemStyle(itemStyleEmphasisModel);
      emphasisStyle.fill = itemStyleEmphasisModel.get("borderColor");
      var blurStyle = getStateItemStyle(itemStyleBlurModel);
      blurStyle.fill = itemStyleBlurModel.get("borderColor");
      var selectStyle = getStateItemStyle(itemStyleSelectModel);
      selectStyle.fill = itemStyleSelectModel.get("borderColor");
      if (useUpperLabel) {
        var upperLabelWidth = thisWidth - 2 * borderWidth;
        prepareText(
          // PENDING: convert ZRColor to ColorString for text.
          bg2,
          visualBorderColor,
          style2.opacity,
          {
            x: borderWidth,
            y: 0,
            width: upperLabelWidth,
            height: upperHeight
          }
        );
      } else {
        bg2.removeTextContent();
      }
      bg2.setStyle(normalStyle);
      bg2.ensureState("emphasis").style = emphasisStyle;
      bg2.ensureState("blur").style = blurStyle;
      bg2.ensureState("select").style = selectStyle;
      setDefaultStateProxy(bg2);
    }
    group3.add(bg2);
  }
  function renderContent(group3, content2) {
    var ecData = getECData(content2);
    ecData.dataIndex = thisNode.dataIndex;
    ecData.seriesIndex = seriesModel.seriesIndex;
    var contentWidth = Math.max(thisWidth - 2 * borderWidth, 0);
    var contentHeight = Math.max(thisHeight - 2 * borderWidth, 0);
    content2.culling = true;
    content2.setShape({
      x: borderWidth,
      y: borderWidth,
      width: contentWidth,
      height: contentHeight,
      r: borderRadius
    });
    if (thisInvisible) {
      processInvisible(content2);
    } else {
      content2.invisible = false;
      var nodeStyle = thisNode.getVisual("style");
      var visualColor = nodeStyle.fill;
      var normalStyle = getItemStyleNormal(itemStyleNormalModel);
      normalStyle.fill = visualColor;
      normalStyle.decal = nodeStyle.decal;
      var emphasisStyle = getStateItemStyle(itemStyleEmphasisModel);
      var blurStyle = getStateItemStyle(itemStyleBlurModel);
      var selectStyle = getStateItemStyle(itemStyleSelectModel);
      prepareText(content2, visualColor, nodeStyle.opacity, null);
      content2.setStyle(normalStyle);
      content2.ensureState("emphasis").style = emphasisStyle;
      content2.ensureState("blur").style = blurStyle;
      content2.ensureState("select").style = selectStyle;
      setDefaultStateProxy(content2);
    }
    group3.add(content2);
  }
  function processInvisible(element) {
    !element.invisible && willInvisibleEls.push(element);
  }
  function prepareText(rectEl, visualColor, visualOpacity, upperLabelRect) {
    var normalLabelModel = nodeModel.getModel(upperLabelRect ? PATH_UPPERLABEL_NORMAL : PATH_LABEL_NOAMAL);
    var defaultText = convertOptionIdName(nodeModel.get("name"), null);
    var isShow = normalLabelModel.getShallow("show");
    setLabelStyle(rectEl, getLabelStatesModels(nodeModel, upperLabelRect ? PATH_UPPERLABEL_NORMAL : PATH_LABEL_NOAMAL), {
      defaultText: isShow ? defaultText : null,
      inheritColor: visualColor,
      defaultOpacity: visualOpacity,
      labelFetcher: seriesModel,
      labelDataIndex: thisNode.dataIndex
    });
    var textEl = rectEl.getTextContent();
    if (!textEl) {
      return;
    }
    var textStyle = textEl.style;
    var textPadding = normalizeCssArray(textStyle.padding || 0);
    if (upperLabelRect) {
      rectEl.setTextConfig({
        layoutRect: upperLabelRect
      });
      textEl.disableLabelLayout = true;
    }
    textEl.beforeUpdate = function() {
      var width = Math.max((upperLabelRect ? upperLabelRect.width : rectEl.shape.width) - textPadding[1] - textPadding[3], 0);
      var height = Math.max((upperLabelRect ? upperLabelRect.height : rectEl.shape.height) - textPadding[0] - textPadding[2], 0);
      if (textStyle.width !== width || textStyle.height !== height) {
        textEl.setStyle({
          width,
          height
        });
      }
    };
    textStyle.truncateMinChar = 2;
    textStyle.lineOverflow = "truncate";
    addDrillDownIcon(textStyle, upperLabelRect, thisLayout);
    var textEmphasisState = textEl.getState("emphasis");
    addDrillDownIcon(textEmphasisState ? textEmphasisState.style : null, upperLabelRect, thisLayout);
  }
  function addDrillDownIcon(style2, upperLabelRect, thisLayout2) {
    var text = style2 ? style2.text : null;
    if (!upperLabelRect && thisLayout2.isLeafRoot && text != null) {
      var iconChar = seriesModel.get("drillDownIcon", true);
      style2.text = iconChar ? iconChar + " " + text : text;
    }
  }
  function giveGraphic(storageName, Ctor, depth2, z) {
    var element = oldRawIndex != null && oldStorage[storageName][oldRawIndex];
    var lasts = lastsForAnimation[storageName];
    if (element) {
      oldStorage[storageName][oldRawIndex] = null;
      prepareAnimationWhenHasOld(lasts, element);
    } else if (!thisInvisible) {
      element = new Ctor();
      if (element instanceof Displayable_default) {
        element.z2 = calculateZ2(depth2, z);
      }
      prepareAnimationWhenNoOld(lasts, element);
    }
    return thisStorage[storageName][thisRawIndex] = element;
  }
  function prepareAnimationWhenHasOld(lasts, element) {
    var lastCfg = lasts[thisRawIndex] = {};
    if (element instanceof Group2) {
      lastCfg.oldX = element.x;
      lastCfg.oldY = element.y;
    } else {
      lastCfg.oldShape = extend({}, element.shape);
    }
  }
  function prepareAnimationWhenNoOld(lasts, element) {
    var lastCfg = lasts[thisRawIndex] = {};
    var parentNode2 = thisNode.parentNode;
    var isGroup = element instanceof Group_default;
    if (parentNode2 && (!reRoot || reRoot.direction === "drillDown")) {
      var parentOldX = 0;
      var parentOldY = 0;
      var parentOldBg = lastsForAnimation.background[parentNode2.getRawIndex()];
      if (!reRoot && parentOldBg && parentOldBg.oldShape) {
        parentOldX = parentOldBg.oldShape.width;
        parentOldY = parentOldBg.oldShape.height;
      }
      if (isGroup) {
        lastCfg.oldX = 0;
        lastCfg.oldY = parentOldY;
      } else {
        lastCfg.oldShape = {
          x: parentOldX,
          y: parentOldY,
          width: 0,
          height: 0
        };
      }
    }
    lastCfg.fadein = !isGroup;
  }
}
function calculateZ2(depth, z2InLevel) {
  return depth * Z2_BASE + z2InLevel;
}

// node_modules/echarts/lib/visual/VisualMapping.js
var each4 = each;
var isObject4 = isObject;
var CATEGORY_DEFAULT_VISUAL_INDEX = -1;
var VisualMapping = (
  /** @class */
  (function() {
    function VisualMapping2(option) {
      var mappingMethod = option.mappingMethod;
      var visualType = option.type;
      var thisOption = this.option = clone(option);
      this.type = visualType;
      this.mappingMethod = mappingMethod;
      this._normalizeData = normalizers[mappingMethod];
      var visualHandler = VisualMapping2.visualHandlers[visualType];
      this.applyVisual = visualHandler.applyVisual;
      this.getColorMapper = visualHandler.getColorMapper;
      this._normalizedToVisual = visualHandler._normalizedToVisual[mappingMethod];
      if (mappingMethod === "piecewise") {
        normalizeVisualRange(thisOption);
        preprocessForPiecewise(thisOption);
      } else if (mappingMethod === "category") {
        thisOption.categories ? preprocessForSpecifiedCategory(thisOption) : normalizeVisualRange(thisOption, true);
      } else {
        assert(mappingMethod !== "linear" || thisOption.dataExtent);
        normalizeVisualRange(thisOption);
      }
    }
    VisualMapping2.prototype.mapValueToVisual = function(value) {
      var normalized = this._normalizeData(value);
      return this._normalizedToVisual(normalized, value);
    };
    VisualMapping2.prototype.getNormalizer = function() {
      return bind(this._normalizeData, this);
    };
    VisualMapping2.listVisualTypes = function() {
      return keys(VisualMapping2.visualHandlers);
    };
    VisualMapping2.isValidType = function(visualType) {
      return VisualMapping2.visualHandlers.hasOwnProperty(visualType);
    };
    VisualMapping2.eachVisual = function(visual, callback, context) {
      if (isObject(visual)) {
        each(visual, callback, context);
      } else {
        callback.call(context, visual);
      }
    };
    VisualMapping2.mapVisual = function(visual, callback, context) {
      var isPrimary;
      var newVisual = isArray(visual) ? [] : isObject(visual) ? {} : (isPrimary = true, null);
      VisualMapping2.eachVisual(visual, function(v, key2) {
        var newVal = callback.call(context, v, key2);
        isPrimary ? newVisual = newVal : newVisual[key2] = newVal;
      });
      return newVisual;
    };
    VisualMapping2.retrieveVisuals = function(obj) {
      var ret = {};
      var hasVisual;
      obj && each4(VisualMapping2.visualHandlers, function(h, visualType) {
        if (obj.hasOwnProperty(visualType)) {
          ret[visualType] = obj[visualType];
          hasVisual = true;
        }
      });
      return hasVisual ? ret : null;
    };
    VisualMapping2.prepareVisualTypes = function(visualTypes) {
      if (isArray(visualTypes)) {
        visualTypes = visualTypes.slice();
      } else if (isObject4(visualTypes)) {
        var types_1 = [];
        each4(visualTypes, function(item, type) {
          types_1.push(type);
        });
        visualTypes = types_1;
      } else {
        return [];
      }
      visualTypes.sort(function(type1, type2) {
        return type2 === "color" && type1 !== "color" && type1.indexOf("color") === 0 ? 1 : -1;
      });
      return visualTypes;
    };
    VisualMapping2.dependsOn = function(visualType1, visualType2) {
      return visualType2 === "color" ? !!(visualType1 && visualType1.indexOf(visualType2) === 0) : visualType1 === visualType2;
    };
    VisualMapping2.findPieceIndex = function(value, pieceList, findClosestWhenOutside) {
      var possibleI;
      var abs2 = Infinity;
      for (var i2 = 0, len2 = pieceList.length; i2 < len2; i2++) {
        var pieceValue = pieceList[i2].value;
        if (pieceValue != null) {
          if (pieceValue === value || isString(pieceValue) && pieceValue === value + "") {
            return i2;
          }
          findClosestWhenOutside && updatePossible(pieceValue, i2);
        }
      }
      for (var i2 = 0, len2 = pieceList.length; i2 < len2; i2++) {
        var piece = pieceList[i2];
        var interval2 = piece.interval;
        var close_1 = piece.close;
        if (interval2) {
          if (interval2[0] === -Infinity) {
            if (littleThan(close_1[1], value, interval2[1])) {
              return i2;
            }
          } else if (interval2[1] === Infinity) {
            if (littleThan(close_1[0], interval2[0], value)) {
              return i2;
            }
          } else if (littleThan(close_1[0], interval2[0], value) && littleThan(close_1[1], value, interval2[1])) {
            return i2;
          }
          findClosestWhenOutside && updatePossible(interval2[0], i2);
          findClosestWhenOutside && updatePossible(interval2[1], i2);
        }
      }
      if (findClosestWhenOutside) {
        return value === Infinity ? pieceList.length - 1 : value === -Infinity ? 0 : possibleI;
      }
      function updatePossible(val, index) {
        var newAbs = Math.abs(val - value);
        if (newAbs < abs2) {
          abs2 = newAbs;
          possibleI = index;
        }
      }
    };
    VisualMapping2.visualHandlers = {
      color: {
        applyVisual: makeApplyVisual("color"),
        getColorMapper: function() {
          var thisOption = this.option;
          return bind(thisOption.mappingMethod === "category" ? function(value, isNormalized) {
            !isNormalized && (value = this._normalizeData(value));
            return doMapCategory.call(this, value);
          } : function(value, isNormalized, out2) {
            var returnRGBArray = !!out2;
            !isNormalized && (value = this._normalizeData(value));
            out2 = fastLerp(value, thisOption.parsedVisual, out2);
            return returnRGBArray ? out2 : stringify(out2, "rgba");
          }, this);
        },
        _normalizedToVisual: {
          linear: function(normalized) {
            return stringify(fastLerp(normalized, this.option.parsedVisual), "rgba");
          },
          category: doMapCategory,
          piecewise: function(normalized, value) {
            var result = getSpecifiedVisual.call(this, value);
            if (result == null) {
              result = stringify(fastLerp(normalized, this.option.parsedVisual), "rgba");
            }
            return result;
          },
          fixed: doMapFixed
        }
      },
      colorHue: makePartialColorVisualHandler(function(color3, value) {
        return modifyHSL(color3, value);
      }),
      colorSaturation: makePartialColorVisualHandler(function(color3, value) {
        return modifyHSL(color3, null, value);
      }),
      colorLightness: makePartialColorVisualHandler(function(color3, value) {
        return modifyHSL(color3, null, null, value);
      }),
      colorAlpha: makePartialColorVisualHandler(function(color3, value) {
        return modifyAlpha(color3, value);
      }),
      decal: {
        applyVisual: makeApplyVisual("decal"),
        _normalizedToVisual: {
          linear: null,
          category: doMapCategory,
          piecewise: null,
          fixed: null
        }
      },
      opacity: {
        applyVisual: makeApplyVisual("opacity"),
        _normalizedToVisual: createNormalizedToNumericVisual([0, 1])
      },
      liftZ: {
        applyVisual: makeApplyVisual("liftZ"),
        _normalizedToVisual: {
          linear: doMapFixed,
          category: doMapFixed,
          piecewise: doMapFixed,
          fixed: doMapFixed
        }
      },
      symbol: {
        applyVisual: function(value, getter2, setter2) {
          var symbolCfg = this.mapValueToVisual(value);
          setter2("symbol", symbolCfg);
        },
        _normalizedToVisual: {
          linear: doMapToArray,
          category: doMapCategory,
          piecewise: function(normalized, value) {
            var result = getSpecifiedVisual.call(this, value);
            if (result == null) {
              result = doMapToArray.call(this, normalized);
            }
            return result;
          },
          fixed: doMapFixed
        }
      },
      symbolSize: {
        applyVisual: makeApplyVisual("symbolSize"),
        _normalizedToVisual: createNormalizedToNumericVisual([0, 1])
      }
    };
    return VisualMapping2;
  })()
);
function preprocessForPiecewise(thisOption) {
  var pieceList = thisOption.pieceList;
  thisOption.hasSpecialVisual = false;
  each(pieceList, function(piece, index) {
    piece.originIndex = index;
    if (piece.visual != null) {
      thisOption.hasSpecialVisual = true;
    }
  });
}
function preprocessForSpecifiedCategory(thisOption) {
  var categories = thisOption.categories;
  var categoryMap = thisOption.categoryMap = {};
  var visual = thisOption.visual;
  each4(categories, function(cate, index) {
    categoryMap[cate] = index;
  });
  if (!isArray(visual)) {
    var visualArr_1 = [];
    if (isObject(visual)) {
      each4(visual, function(v, cate) {
        var index = categoryMap[cate];
        visualArr_1[index != null ? index : CATEGORY_DEFAULT_VISUAL_INDEX] = v;
      });
    } else {
      visualArr_1[CATEGORY_DEFAULT_VISUAL_INDEX] = visual;
    }
    visual = setVisualToOption(thisOption, visualArr_1);
  }
  for (var i2 = categories.length - 1; i2 >= 0; i2--) {
    if (visual[i2] == null) {
      delete categoryMap[categories[i2]];
      categories.pop();
    }
  }
}
function normalizeVisualRange(thisOption, isCategory2) {
  var visual = thisOption.visual;
  var visualArr = [];
  if (isObject(visual)) {
    each4(visual, function(v) {
      visualArr.push(v);
    });
  } else if (visual != null) {
    visualArr.push(visual);
  }
  var doNotNeedPair = {
    color: 1,
    symbol: 1
  };
  if (!isCategory2 && visualArr.length === 1 && !doNotNeedPair.hasOwnProperty(thisOption.type)) {
    visualArr[1] = visualArr[0];
  }
  setVisualToOption(thisOption, visualArr);
}
function makePartialColorVisualHandler(applyValue) {
  return {
    applyVisual: function(value, getter2, setter2) {
      var colorChannel = this.mapValueToVisual(value);
      setter2("color", applyValue(getter2("color"), colorChannel));
    },
    _normalizedToVisual: createNormalizedToNumericVisual([0, 1])
  };
}
function doMapToArray(normalized) {
  var visual = this.option.visual;
  return visual[Math.round(linearMap(normalized, [0, 1], [0, visual.length - 1], true))] || {};
}
function makeApplyVisual(visualType) {
  return function(value, getter2, setter2) {
    setter2(visualType, this.mapValueToVisual(value));
  };
}
function doMapCategory(normalized) {
  var visual = this.option.visual;
  return visual[this.option.loop && normalized !== CATEGORY_DEFAULT_VISUAL_INDEX ? normalized % visual.length : normalized];
}
function doMapFixed() {
  return this.option.visual[0];
}
function createNormalizedToNumericVisual(sourceExtent) {
  return {
    linear: function(normalized) {
      return linearMap(normalized, sourceExtent, this.option.visual, true);
    },
    category: doMapCategory,
    piecewise: function(normalized, value) {
      var result = getSpecifiedVisual.call(this, value);
      if (result == null) {
        result = linearMap(normalized, sourceExtent, this.option.visual, true);
      }
      return result;
    },
    fixed: doMapFixed
  };
}
function getSpecifiedVisual(value) {
  var thisOption = this.option;
  var pieceList = thisOption.pieceList;
  if (thisOption.hasSpecialVisual) {
    var pieceIndex = VisualMapping.findPieceIndex(value, pieceList);
    var piece = pieceList[pieceIndex];
    if (piece && piece.visual) {
      return piece.visual[this.type];
    }
  }
}
function setVisualToOption(thisOption, visualArr) {
  thisOption.visual = visualArr;
  if (thisOption.type === "color") {
    thisOption.parsedVisual = map2(visualArr, function(item) {
      var color3 = parse(item);
      if (!color3 && true) {
        warn("'" + item + "' is an illegal color, fallback to '#000000'", true);
      }
      return color3 || [0, 0, 0, 1];
    });
  }
  return visualArr;
}
var normalizers = {
  linear: function(value) {
    return linearMap(value, this.option.dataExtent, [0, 1], true);
  },
  piecewise: function(value) {
    var pieceList = this.option.pieceList;
    var pieceIndex = VisualMapping.findPieceIndex(value, pieceList, true);
    if (pieceIndex != null) {
      return linearMap(pieceIndex, [0, pieceList.length - 1], [0, 1], true);
    }
  },
  category: function(value) {
    var index = this.option.categories ? this.option.categoryMap[value] : value;
    return index == null ? CATEGORY_DEFAULT_VISUAL_INDEX : index;
  },
  fixed: noop
};
function littleThan(close, a, b) {
  return close ? a <= b : a < b;
}
var VisualMapping_default = VisualMapping;

// node_modules/echarts/lib/chart/treemap/treemapVisual.js
var inner12 = makeInner();

// node_modules/echarts/lib/data/Graph.js
function generateNodeKey(id) {
  return "_EC_" + id;
}
var Graph = (
  /** @class */
  (function() {
    function Graph2(directed) {
      this.type = "graph";
      this.nodes = [];
      this.edges = [];
      this._nodesMap = {};
      this._edgesMap = {};
      this._directed = directed || false;
    }
    Graph2.prototype.isDirected = function() {
      return this._directed;
    };
    ;
    Graph2.prototype.addNode = function(id, dataIndex) {
      id = id == null ? "" + dataIndex : "" + id;
      var nodesMap = this._nodesMap;
      if (nodesMap[generateNodeKey(id)]) {
        if (true) {
          console.error("Graph nodes have duplicate name or id");
        }
        return;
      }
      var node = new GraphNode(id, dataIndex);
      node.hostGraph = this;
      this.nodes.push(node);
      nodesMap[generateNodeKey(id)] = node;
      return node;
    };
    ;
    Graph2.prototype.getNodeByIndex = function(dataIndex) {
      var rawIdx = this.data.getRawIndex(dataIndex);
      return this.nodes[rawIdx];
    };
    ;
    Graph2.prototype.getNodeById = function(id) {
      return this._nodesMap[generateNodeKey(id)];
    };
    ;
    Graph2.prototype.addEdge = function(n1, n2, dataIndex) {
      var nodesMap = this._nodesMap;
      var edgesMap = this._edgesMap;
      if (isNumber(n1)) {
        n1 = this.nodes[n1];
      }
      if (isNumber(n2)) {
        n2 = this.nodes[n2];
      }
      if (!(n1 instanceof GraphNode)) {
        n1 = nodesMap[generateNodeKey(n1)];
      }
      if (!(n2 instanceof GraphNode)) {
        n2 = nodesMap[generateNodeKey(n2)];
      }
      if (!n1 || !n2) {
        return;
      }
      var key2 = n1.id + "-" + n2.id;
      var edge = new GraphEdge(n1, n2, dataIndex);
      edge.hostGraph = this;
      if (this._directed) {
        n1.outEdges.push(edge);
        n2.inEdges.push(edge);
      }
      n1.edges.push(edge);
      if (n1 !== n2) {
        n2.edges.push(edge);
      }
      this.edges.push(edge);
      edgesMap[key2] = edge;
      return edge;
    };
    ;
    Graph2.prototype.getEdgeByIndex = function(dataIndex) {
      var rawIdx = this.edgeData.getRawIndex(dataIndex);
      return this.edges[rawIdx];
    };
    ;
    Graph2.prototype.getEdge = function(n1, n2) {
      if (n1 instanceof GraphNode) {
        n1 = n1.id;
      }
      if (n2 instanceof GraphNode) {
        n2 = n2.id;
      }
      var edgesMap = this._edgesMap;
      if (this._directed) {
        return edgesMap[n1 + "-" + n2];
      } else {
        return edgesMap[n1 + "-" + n2] || edgesMap[n2 + "-" + n1];
      }
    };
    ;
    Graph2.prototype.eachNode = function(cb, context) {
      var nodes = this.nodes;
      var len2 = nodes.length;
      for (var i2 = 0; i2 < len2; i2++) {
        if (nodes[i2].dataIndex >= 0) {
          cb.call(context, nodes[i2], i2);
        }
      }
    };
    ;
    Graph2.prototype.eachEdge = function(cb, context) {
      var edges = this.edges;
      var len2 = edges.length;
      for (var i2 = 0; i2 < len2; i2++) {
        if (edges[i2].dataIndex >= 0 && edges[i2].node1.dataIndex >= 0 && edges[i2].node2.dataIndex >= 0) {
          cb.call(context, edges[i2], i2);
        }
      }
    };
    ;
    Graph2.prototype.breadthFirstTraverse = function(cb, startNode, direction, context) {
      if (!(startNode instanceof GraphNode)) {
        startNode = this._nodesMap[generateNodeKey(startNode)];
      }
      if (!startNode) {
        return;
      }
      var edgeType = direction === "out" ? "outEdges" : direction === "in" ? "inEdges" : "edges";
      for (var i2 = 0; i2 < this.nodes.length; i2++) {
        this.nodes[i2].__visited = false;
      }
      if (cb.call(context, startNode, null)) {
        return;
      }
      var queue = [startNode];
      while (queue.length) {
        var currentNode = queue.shift();
        var edges = currentNode[edgeType];
        for (var i2 = 0; i2 < edges.length; i2++) {
          var e2 = edges[i2];
          var otherNode = e2.node1 === currentNode ? e2.node2 : e2.node1;
          if (!otherNode.__visited) {
            if (cb.call(context, otherNode, currentNode)) {
              return;
            }
            queue.push(otherNode);
            otherNode.__visited = true;
          }
        }
      }
    };
    ;
    Graph2.prototype.update = function() {
      var data = this.data;
      var edgeData = this.edgeData;
      var nodes = this.nodes;
      var edges = this.edges;
      for (var i2 = 0, len2 = nodes.length; i2 < len2; i2++) {
        nodes[i2].dataIndex = -1;
      }
      for (var i2 = 0, len2 = data.count(); i2 < len2; i2++) {
        nodes[data.getRawIndex(i2)].dataIndex = i2;
      }
      edgeData.filterSelf(function(idx) {
        var edge = edges[edgeData.getRawIndex(idx)];
        return edge.node1.dataIndex >= 0 && edge.node2.dataIndex >= 0;
      });
      for (var i2 = 0, len2 = edges.length; i2 < len2; i2++) {
        edges[i2].dataIndex = -1;
      }
      for (var i2 = 0, len2 = edgeData.count(); i2 < len2; i2++) {
        edges[edgeData.getRawIndex(i2)].dataIndex = i2;
      }
    };
    ;
    Graph2.prototype.clone = function() {
      var graph = new Graph2(this._directed);
      var nodes = this.nodes;
      var edges = this.edges;
      for (var i2 = 0; i2 < nodes.length; i2++) {
        graph.addNode(nodes[i2].id, nodes[i2].dataIndex);
      }
      for (var i2 = 0; i2 < edges.length; i2++) {
        var e2 = edges[i2];
        graph.addEdge(e2.node1.id, e2.node2.id, e2.dataIndex);
      }
      return graph;
    };
    ;
    return Graph2;
  })()
);
var GraphNode = (
  /** @class */
  (function() {
    function GraphNode2(id, dataIndex) {
      this.inEdges = [];
      this.outEdges = [];
      this.edges = [];
      this.dataIndex = -1;
      this.id = id == null ? "" : id;
      this.dataIndex = dataIndex == null ? -1 : dataIndex;
    }
    GraphNode2.prototype.degree = function() {
      return this.edges.length;
    };
    GraphNode2.prototype.inDegree = function() {
      return this.inEdges.length;
    };
    GraphNode2.prototype.outDegree = function() {
      return this.outEdges.length;
    };
    GraphNode2.prototype.getModel = function(path) {
      if (this.dataIndex < 0) {
        return;
      }
      var graph = this.hostGraph;
      var itemModel = graph.data.getItemModel(this.dataIndex);
      return itemModel.getModel(path);
    };
    GraphNode2.prototype.getAdjacentDataIndices = function() {
      var dataIndices = {
        edge: [],
        node: []
      };
      for (var i2 = 0; i2 < this.edges.length; i2++) {
        var adjacentEdge = this.edges[i2];
        if (adjacentEdge.dataIndex < 0) {
          continue;
        }
        dataIndices.edge.push(adjacentEdge.dataIndex);
        dataIndices.node.push(adjacentEdge.node1.dataIndex, adjacentEdge.node2.dataIndex);
      }
      return dataIndices;
    };
    GraphNode2.prototype.getTrajectoryDataIndices = function() {
      var connectedEdgesMap = createHashMap();
      var connectedNodesMap = createHashMap();
      for (var i2 = 0, len2 = this.edges.length; i2 < len2; i2++) {
        var adjacentEdge = this.edges[i2];
        if (adjacentEdge.dataIndex < 0) {
          continue;
        }
        connectedEdgesMap.set(adjacentEdge.dataIndex, true);
        var sourceNodesQueue = [adjacentEdge.node1];
        var targetNodesQueue = [adjacentEdge.node2];
        var nodeIteratorIndex = 0;
        while (nodeIteratorIndex < sourceNodesQueue.length) {
          var sourceNode = sourceNodesQueue[nodeIteratorIndex];
          nodeIteratorIndex++;
          connectedNodesMap.set(sourceNode.dataIndex, true);
          var sourceNodeInEdges = sourceNode.inEdges;
          for (var j = 0, len_1 = sourceNodeInEdges.length, inEdge = void 0, inEdgeDataIndex = void 0; j < len_1; j++) {
            inEdge = sourceNodeInEdges[j];
            inEdgeDataIndex = inEdge.dataIndex;
            if (inEdgeDataIndex >= 0 && !connectedEdgesMap.hasKey(inEdgeDataIndex)) {
              connectedEdgesMap.set(inEdgeDataIndex, true);
              sourceNodesQueue.push(inEdge.node1);
            }
          }
        }
        nodeIteratorIndex = 0;
        while (nodeIteratorIndex < targetNodesQueue.length) {
          var targetNode = targetNodesQueue[nodeIteratorIndex];
          nodeIteratorIndex++;
          connectedNodesMap.set(targetNode.dataIndex, true);
          var targetNodeOutEdges = targetNode.outEdges;
          for (var j = 0, len_2 = targetNodeOutEdges.length, outEdge = void 0, outEdgeDataIndex = void 0; j < len_2; j++) {
            outEdge = targetNodeOutEdges[j];
            outEdgeDataIndex = outEdge.dataIndex;
            if (outEdgeDataIndex >= 0 && !connectedEdgesMap.hasKey(outEdgeDataIndex)) {
              connectedEdgesMap.set(outEdgeDataIndex, true);
              targetNodesQueue.push(outEdge.node2);
            }
          }
        }
      }
      return {
        edge: connectedEdgesMap.keys(),
        node: connectedNodesMap.keys()
      };
    };
    return GraphNode2;
  })()
);
var GraphEdge = (
  /** @class */
  (function() {
    function GraphEdge2(n1, n2, dataIndex) {
      this.dataIndex = -1;
      this.node1 = n1;
      this.node2 = n2;
      this.dataIndex = dataIndex == null ? -1 : dataIndex;
    }
    GraphEdge2.prototype.getModel = function(path) {
      if (this.dataIndex < 0) {
        return;
      }
      var graph = this.hostGraph;
      var itemModel = graph.edgeData.getItemModel(this.dataIndex);
      return itemModel.getModel(path);
    };
    GraphEdge2.prototype.getAdjacentDataIndices = function() {
      return {
        edge: [this.dataIndex],
        node: [this.node1.dataIndex, this.node2.dataIndex]
      };
    };
    GraphEdge2.prototype.getTrajectoryDataIndices = function() {
      var connectedEdgesMap = createHashMap();
      var connectedNodesMap = createHashMap();
      connectedEdgesMap.set(this.dataIndex, true);
      var sourceNodes = [this.node1];
      var targetNodes = [this.node2];
      var nodeIteratorIndex = 0;
      while (nodeIteratorIndex < sourceNodes.length) {
        var sourceNode = sourceNodes[nodeIteratorIndex];
        nodeIteratorIndex++;
        connectedNodesMap.set(sourceNode.dataIndex, true);
        var sourceNodeInEdges = sourceNode.inEdges;
        for (var j = 0, len2 = sourceNodeInEdges.length, inEdge = void 0, inEdgeDataIndex = void 0; j < len2; j++) {
          inEdge = sourceNode.inEdges[j];
          inEdgeDataIndex = inEdge.dataIndex;
          if (inEdgeDataIndex >= 0 && !connectedEdgesMap.hasKey(inEdgeDataIndex)) {
            connectedEdgesMap.set(inEdgeDataIndex, true);
            sourceNodes.push(inEdge.node1);
          }
        }
      }
      nodeIteratorIndex = 0;
      while (nodeIteratorIndex < targetNodes.length) {
        var targetNode = targetNodes[nodeIteratorIndex];
        nodeIteratorIndex++;
        connectedNodesMap.set(targetNode.dataIndex, true);
        var targetNodeOutEdges = targetNode.outEdges;
        for (var j = 0, len2 = targetNodeOutEdges.length, outEdge = void 0, outEdgeDataIndex = void 0; j < len2; j++) {
          outEdge = targetNode.outEdges[j];
          outEdgeDataIndex = outEdge.dataIndex;
          if (outEdgeDataIndex >= 0 && !connectedEdgesMap.hasKey(outEdgeDataIndex)) {
            connectedEdgesMap.set(outEdgeDataIndex, true);
            targetNodes.push(outEdge.node2);
          }
        }
      }
      return {
        edge: connectedEdgesMap.keys(),
        node: connectedNodesMap.keys()
      };
    };
    return GraphEdge2;
  })()
);
function createGraphDataProxyMixin(hostName, dataName) {
  return {
    /**
     * @param Default 'value'. can be 'a', 'b', 'c', 'd', 'e'.
     */
    getValue: function(dimension) {
      var data = this[hostName][dataName];
      return data.getStore().get(data.getDimensionIndex(dimension || "value"), this.dataIndex);
    },
    // TODO: TYPE stricter type.
    setVisual: function(key2, value) {
      this.dataIndex >= 0 && this[hostName][dataName].setItemVisual(this.dataIndex, key2, value);
    },
    getVisual: function(key2) {
      return this[hostName][dataName].getItemVisual(this.dataIndex, key2);
    },
    setLayout: function(layout3, merge3) {
      this.dataIndex >= 0 && this[hostName][dataName].setItemLayout(this.dataIndex, layout3, merge3);
    },
    getLayout: function() {
      return this[hostName][dataName].getItemLayout(this.dataIndex);
    },
    getGraphicEl: function() {
      return this[hostName][dataName].getItemGraphicEl(this.dataIndex);
    },
    getRawIndex: function() {
      return this[hostName][dataName].getRawIndex(this.dataIndex);
    }
  };
}
mixin(GraphNode, createGraphDataProxyMixin("hostGraph", "data"));
mixin(GraphEdge, createGraphDataProxyMixin("hostGraph", "edgeData"));
var Graph_default = Graph;

// node_modules/echarts/lib/chart/helper/createGraphFromNodeEdge.js
function createGraphFromNodeEdge(nodes, edges, seriesModel, directed, beforeLink) {
  var graph = new Graph_default(directed);
  for (var i2 = 0; i2 < nodes.length; i2++) {
    graph.addNode(retrieve(
      // Id, name, dataIndex
      nodes[i2].id,
      nodes[i2].name,
      i2
    ), i2);
  }
  var linkNameList = [];
  var validEdges = [];
  var linkCount = 0;
  for (var i2 = 0; i2 < edges.length; i2++) {
    var link = edges[i2];
    var source = link.source;
    var target = link.target;
    if (graph.addEdge(source, target, linkCount)) {
      validEdges.push(link);
      linkNameList.push(retrieve(convertOptionIdName(link.id, null), source + " > " + target));
      linkCount++;
    }
  }
  var coordSys = seriesModel.get("coordinateSystem");
  var nodeData;
  if (coordSys === "cartesian2d" || coordSys === "polar" || coordSys === "matrix") {
    nodeData = createSeriesData_default(nodes, seriesModel);
  } else {
    var coordSysCtor = CoordinateSystem_default.get(coordSys);
    var coordDimensions = coordSysCtor ? coordSysCtor.dimensions || [] : [];
    if (indexOf(coordDimensions, "value") < 0) {
      coordDimensions.concat(["value"]);
    }
    var dimensions = prepareSeriesDataSchema(nodes, {
      coordDimensions,
      encodeDefine: seriesModel.getEncode()
    }).dimensions;
    nodeData = new SeriesData_default(dimensions, seriesModel);
    nodeData.initData(nodes);
  }
  var edgeData = new SeriesData_default(["value"], seriesModel);
  edgeData.initData(validEdges, linkNameList);
  beforeLink && beforeLink(nodeData, edgeData);
  linkSeriesData_default({
    mainData: nodeData,
    struct: graph,
    structAttr: "graph",
    datas: {
      node: nodeData,
      edge: edgeData
    },
    datasAttr: {
      node: "data",
      edge: "edgeData"
    }
  });
  graph.update();
  return graph;
}

// node_modules/echarts/lib/chart/helper/multipleGraphEdgeHelper.js
var KEY_DELIMITER = "-->";
var getAutoCurvenessParams = function(seriesModel) {
  return seriesModel.get("autoCurveness") || null;
};
var createCurveness = function(seriesModel, appendLength) {
  var autoCurvenessParmas = getAutoCurvenessParams(seriesModel);
  var length = 20;
  var curvenessList = [];
  if (isNumber(autoCurvenessParmas)) {
    length = autoCurvenessParmas;
  } else if (isArray(autoCurvenessParmas)) {
    seriesModel.__curvenessList = autoCurvenessParmas;
    return;
  }
  if (appendLength > length) {
    length = appendLength;
  }
  var len2 = length % 2 ? length + 2 : length + 3;
  curvenessList = [];
  for (var i2 = 0; i2 < len2; i2++) {
    curvenessList.push((i2 % 2 ? i2 + 1 : i2) / 10 * (i2 % 2 ? -1 : 1));
  }
  seriesModel.__curvenessList = curvenessList;
};
var getKeyOfEdges = function(n1, n2, seriesModel) {
  var source = [n1.id, n1.dataIndex].join(".");
  var target = [n2.id, n2.dataIndex].join(".");
  return [seriesModel.uid, source, target].join(KEY_DELIMITER);
};
var getOppositeKey = function(key2) {
  var keys2 = key2.split(KEY_DELIMITER);
  return [keys2[0], keys2[2], keys2[1]].join(KEY_DELIMITER);
};
var getEdgeFromMap = function(edge, seriesModel) {
  var key2 = getKeyOfEdges(edge.node1, edge.node2, seriesModel);
  return seriesModel.__edgeMap[key2];
};
var getTotalLengthBetweenNodes = function(edge, seriesModel) {
  var len2 = getEdgeMapLengthWithKey(getKeyOfEdges(edge.node1, edge.node2, seriesModel), seriesModel);
  var lenV = getEdgeMapLengthWithKey(getKeyOfEdges(edge.node2, edge.node1, seriesModel), seriesModel);
  return len2 + lenV;
};
var getEdgeMapLengthWithKey = function(key2, seriesModel) {
  var edgeMap = seriesModel.__edgeMap;
  return edgeMap[key2] ? edgeMap[key2].length : 0;
};
function initCurvenessList(seriesModel) {
  if (!getAutoCurvenessParams(seriesModel)) {
    return;
  }
  seriesModel.__curvenessList = [];
  seriesModel.__edgeMap = {};
  createCurveness(seriesModel);
}
function createEdgeMapForCurveness(n1, n2, seriesModel, index) {
  if (!getAutoCurvenessParams(seriesModel)) {
    return;
  }
  var key2 = getKeyOfEdges(n1, n2, seriesModel);
  var edgeMap = seriesModel.__edgeMap;
  var oppositeEdges = edgeMap[getOppositeKey(key2)];
  if (edgeMap[key2] && !oppositeEdges) {
    edgeMap[key2].isForward = true;
  } else if (oppositeEdges && edgeMap[key2]) {
    oppositeEdges.isForward = true;
    edgeMap[key2].isForward = false;
  }
  edgeMap[key2] = edgeMap[key2] || [];
  edgeMap[key2].push(index);
}
function getCurvenessForEdge(edge, seriesModel, index, needReverse) {
  var autoCurvenessParams = getAutoCurvenessParams(seriesModel);
  var isArrayParam = isArray(autoCurvenessParams);
  if (!autoCurvenessParams) {
    return null;
  }
  var edgeArray = getEdgeFromMap(edge, seriesModel);
  if (!edgeArray) {
    return null;
  }
  var edgeIndex = -1;
  for (var i2 = 0; i2 < edgeArray.length; i2++) {
    if (edgeArray[i2] === index) {
      edgeIndex = i2;
      break;
    }
  }
  var totalLen = getTotalLengthBetweenNodes(edge, seriesModel);
  createCurveness(seriesModel, totalLen);
  edge.lineStyle = edge.lineStyle || {};
  var curKey = getKeyOfEdges(edge.node1, edge.node2, seriesModel);
  var curvenessList = seriesModel.__curvenessList;
  var parityCorrection = isArrayParam ? 0 : totalLen % 2 ? 0 : 1;
  if (!edgeArray.isForward) {
    var oppositeKey = getOppositeKey(curKey);
    var len2 = getEdgeMapLengthWithKey(oppositeKey, seriesModel);
    var resValue = curvenessList[edgeIndex + len2 + parityCorrection];
    if (needReverse) {
      if (isArrayParam) {
        if (autoCurvenessParams && autoCurvenessParams[0] === 0) {
          return (len2 + parityCorrection) % 2 ? resValue : -resValue;
        } else {
          return ((len2 % 2 ? 0 : 1) + parityCorrection) % 2 ? resValue : -resValue;
        }
      } else {
        return (len2 + parityCorrection) % 2 ? resValue : -resValue;
      }
    } else {
      return curvenessList[edgeIndex + len2 + parityCorrection];
    }
  } else {
    return curvenessList[parityCorrection + edgeIndex];
  }
}

// node_modules/echarts/lib/chart/graph/GraphSeries.js
var SERIES_TYPE_GRAPH = "graph";
var GraphSeriesModel = (
  /** @class */
  (function(_super) {
    __extends(GraphSeriesModel2, _super);
    function GraphSeriesModel2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = GraphSeriesModel2.type;
      _this.hasSymbolVisual = true;
      return _this;
    }
    GraphSeriesModel2.prototype.init = function(option) {
      _super.prototype.init.apply(this, arguments);
      var self2 = this;
      function getCategoriesData() {
        return self2._categoriesData;
      }
      this.legendVisualProvider = new LegendVisualProvider_default(getCategoriesData, getCategoriesData);
      this.fillDataTextStyle(option.edges || option.links);
      this._updateCategoriesData();
    };
    GraphSeriesModel2.prototype.mergeOption = function(option) {
      _super.prototype.mergeOption.apply(this, arguments);
      this.fillDataTextStyle(option.edges || option.links);
      this._updateCategoriesData();
    };
    GraphSeriesModel2.prototype.mergeDefaultAndTheme = function(option) {
      _super.prototype.mergeDefaultAndTheme.apply(this, arguments);
      defaultEmphasis(option, "edgeLabel", ["show"]);
    };
    GraphSeriesModel2.prototype.getInitialData = function(option, ecModel) {
      var edges = option.edges || option.links || [];
      var nodes = option.data || option.nodes || [];
      var self2 = this;
      if (nodes && edges) {
        initCurvenessList(this);
        var graph = createGraphFromNodeEdge(nodes, edges, this, true, beforeLink);
        each(graph.edges, function(edge) {
          createEdgeMapForCurveness(edge.node1, edge.node2, this, edge.dataIndex);
        }, this);
        return graph.data;
      }
      function beforeLink(nodeData, edgeData) {
        nodeData.wrapMethod("getItemModel", function(model2) {
          var categoriesModels = self2._categoriesModels;
          var categoryIdx = model2.getShallow("category");
          var categoryModel = categoriesModels[categoryIdx];
          if (categoryModel) {
            categoryModel.parentModel = model2.parentModel;
            model2.parentModel = categoryModel;
          }
          return model2;
        });
        var oldGetModel = Model_default.prototype.getModel;
        function newGetModel(path, parentModel) {
          var model2 = oldGetModel.call(this, path, parentModel);
          model2.resolveParentPath = resolveParentPath;
          return model2;
        }
        edgeData.wrapMethod("getItemModel", function(model2) {
          model2.resolveParentPath = resolveParentPath;
          model2.getModel = newGetModel;
          return model2;
        });
        function resolveParentPath(pathArr) {
          if (pathArr && (pathArr[0] === "label" || pathArr[1] === "label")) {
            var newPathArr = pathArr.slice();
            if (pathArr[0] === "label") {
              newPathArr[0] = "edgeLabel";
            } else if (pathArr[1] === "label") {
              newPathArr[1] = "edgeLabel";
            }
            return newPathArr;
          }
          return pathArr;
        }
      }
    };
    GraphSeriesModel2.prototype.getGraph = function() {
      return this.getData().graph;
    };
    GraphSeriesModel2.prototype.getEdgeData = function() {
      return this.getGraph().edgeData;
    };
    GraphSeriesModel2.prototype.getCategoriesData = function() {
      return this._categoriesData;
    };
    GraphSeriesModel2.prototype.formatTooltip = function(dataIndex, multipleSeries, dataType) {
      if (dataType === "edge") {
        var nodeData = this.getData();
        var params = this.getDataParams(dataIndex, dataType);
        var edge = nodeData.graph.getEdgeByIndex(dataIndex);
        var sourceName = nodeData.getName(edge.node1.dataIndex);
        var targetName = nodeData.getName(edge.node2.dataIndex);
        var nameArr = [];
        sourceName != null && nameArr.push(sourceName);
        targetName != null && nameArr.push(targetName);
        return createTooltipMarkup("nameValue", {
          name: nameArr.join(" > "),
          value: params.value,
          noValue: params.value == null
        });
      }
      var nodeMarkup = defaultSeriesFormatTooltip({
        series: this,
        dataIndex,
        multipleSeries
      });
      return nodeMarkup;
    };
    GraphSeriesModel2.prototype._updateCategoriesData = function() {
      var categories = map2(this.option.categories || [], function(category) {
        return category.value != null ? category : extend({
          value: 0
        }, category);
      });
      var categoriesData = new SeriesData_default(["value"], this);
      categoriesData.initData(categories);
      this._categoriesData = categoriesData;
      this._categoriesModels = categoriesData.mapArray(function(idx) {
        return categoriesData.getItemModel(idx);
      });
    };
    GraphSeriesModel2.prototype.isAnimationEnabled = function() {
      return _super.prototype.isAnimationEnabled.call(this) && !(this.get("layout") === "force" && this.get(["force", "layoutAnimation"]));
    };
    GraphSeriesModel2.prototype.__ownRoamView = function() {
      var coordSys = this.coordinateSystem;
      return isViewCoordSys(coordSys) && coordSys;
    };
    GraphSeriesModel2.type = "series." + SERIES_TYPE_GRAPH;
    GraphSeriesModel2.dependencies = ["grid", "polar", "geo", "singleAxis", "calendar"];
    GraphSeriesModel2.defaultOption = {
      // zlevel: 0,
      z: 2,
      coordinateSystem: "view",
      // Default option for all coordinate systems
      // xAxisIndex: 0,
      // yAxisIndex: 0,
      // polarIndex: 0,
      // geoIndex: 0,
      legendHoverLink: true,
      layout: null,
      // Configuration of circular layout
      circular: {
        rotateLabel: false
      },
      // Configuration of force directed layout
      force: {
        initLayout: null,
        // Node repulsion. Can be an array to represent range.
        repulsion: [0, 50],
        gravity: 0.1,
        // Initial friction
        friction: 0.6,
        // Edge length. Can be an array to represent range.
        edgeLength: 30,
        layoutAnimation: true
      },
      left: "center",
      top: "center",
      // right: null,
      // bottom: null,
      // width: '80%',
      // height: '80%',
      symbol: "circle",
      symbolSize: 10,
      edgeSymbol: ["none", "none"],
      edgeSymbolSize: 10,
      edgeLabel: {
        position: "middle",
        distance: 5
      },
      draggable: false,
      roam: false,
      // Default on center of graph
      center: null,
      zoom: 1,
      // Symbol size scale ratio in roam
      nodeScaleRatio: 0.6,
      // cursor: null,
      // categories: [],
      // data: []
      // Or
      // nodes: []
      //
      // links: []
      // Or
      // edges: []
      label: {
        show: false,
        formatter: "{b}"
      },
      itemStyle: {},
      lineStyle: {
        // Don't use tokens.color.border because of the opacity
        color: tokens_default.color.neutral50,
        width: 1,
        opacity: 0.5
      },
      emphasis: {
        scale: true,
        label: {
          show: true
        }
      },
      select: {
        itemStyle: {
          borderColor: tokens_default.color.primary
        }
      }
    };
    return GraphSeriesModel2;
  })(Series_default)
);

// node_modules/echarts/lib/chart/graph/edgeVisual.js
function normalize2(a) {
  if (!(a instanceof Array)) {
    a = [a, a];
  }
  return a;
}
var graphEdgeVisualStageHandler = createSimpleOverallStageHandler(SERIES_TYPE_GRAPH, graphEdgeVisual);
function graphEdgeVisual(ecModel) {
  ecModel.eachSeriesByType(SERIES_TYPE_GRAPH, function(seriesModel) {
    var graph = seriesModel.getGraph();
    var edgeData = seriesModel.getEdgeData();
    var symbolType = normalize2(seriesModel.get("edgeSymbol"));
    var symbolSize = normalize2(seriesModel.get("edgeSymbolSize"));
    edgeData.setVisual("fromSymbol", symbolType && symbolType[0]);
    edgeData.setVisual("toSymbol", symbolType && symbolType[1]);
    edgeData.setVisual("fromSymbolSize", symbolSize && symbolSize[0]);
    edgeData.setVisual("toSymbolSize", symbolSize && symbolSize[1]);
    edgeData.setVisual("style", seriesModel.getModel("lineStyle").getLineStyle());
    edgeData.each(function(idx) {
      var itemModel = edgeData.getItemModel(idx);
      var edge = graph.getEdgeByIndex(idx);
      var symbolType2 = normalize2(itemModel.getShallow("symbol", true));
      var symbolSize2 = normalize2(itemModel.getShallow("symbolSize", true));
      var style2 = itemModel.getModel("lineStyle").getLineStyle();
      var existsStyle = edgeData.ensureUniqueItemVisual(idx, "style");
      extend(existsStyle, style2);
      switch (existsStyle.stroke) {
        case "source": {
          var nodeStyle = edge.node1.getVisual("style");
          existsStyle.stroke = nodeStyle && nodeStyle.fill;
          break;
        }
        case "target": {
          var nodeStyle = edge.node2.getVisual("style");
          existsStyle.stroke = nodeStyle && nodeStyle.fill;
          break;
        }
      }
      symbolType2[0] && edge.setVisual("fromSymbol", symbolType2[0]);
      symbolType2[1] && edge.setVisual("toSymbol", symbolType2[1]);
      symbolSize2[0] && edge.setVisual("fromSymbolSize", symbolSize2[0]);
      symbolSize2[1] && edge.setVisual("toSymbolSize", symbolSize2[1]);
    });
  });
}

// node_modules/echarts/lib/chart/graph/simpleLayoutHelper.js
function simpleLayout(seriesModel) {
  var coordSys = seriesModel.coordinateSystem;
  if (coordSys && coordSys.type !== "view") {
    return;
  }
  var graph = seriesModel.getGraph();
  graph.eachNode(function(node) {
    var model2 = node.getModel();
    node.setLayout([+model2.get("x"), +model2.get("y")]);
  });
  simpleLayoutEdge(graph, seriesModel);
}
function simpleLayoutEdge(graph, seriesModel) {
  graph.eachEdge(function(edge, index) {
    var curveness = retrieve3(edge.getModel().get(["lineStyle", "curveness"]), -getCurvenessForEdge(edge, seriesModel, index, true), 0);
    var p1 = clone3(edge.node1.getLayout());
    var p2 = clone3(edge.node2.getLayout());
    var points4 = [p1, p2];
    if (+curveness) {
      points4.push([(p1[0] + p2[0]) / 2 - (p1[1] - p2[1]) * curveness, (p1[1] + p2[1]) / 2 - (p2[0] - p1[0]) * curveness]);
    }
    edge.setLayout(points4);
  });
}

// node_modules/echarts/lib/chart/graph/simpleLayout.js
var graphSimpleLayoutStageHandler = createSimpleOverallStageHandler(SERIES_TYPE_GRAPH, graphSimpleLayout);
function graphSimpleLayout(ecModel, api) {
  ecModel.eachSeriesByType(SERIES_TYPE_GRAPH, function(seriesModel) {
    var layout3 = seriesModel.get("layout");
    var coordSys = seriesModel.coordinateSystem;
    if (coordSys && coordSys.type !== "view") {
      var data_1 = seriesModel.getData();
      var dimensions_1 = [];
      each(coordSys.dimensions, function(coordDim) {
        dimensions_1 = dimensions_1.concat(data_1.mapDimensionsAll(coordDim));
      });
      for (var dataIndex = 0; dataIndex < data_1.count(); dataIndex++) {
        var value = [];
        var hasValue = false;
        for (var i2 = 0; i2 < dimensions_1.length; i2++) {
          var val = data_1.get(dimensions_1[i2], dataIndex);
          if (!isNaN(val)) {
            hasValue = true;
          }
          value.push(val);
        }
        if (hasValue) {
          data_1.setItemLayout(dataIndex, coordSys.dataToPoint(value));
        } else {
          data_1.setItemLayout(dataIndex, [NaN, NaN]);
        }
      }
      simpleLayoutEdge(data_1.graph, seriesModel);
    } else if (!layout3 || layout3 === "none") {
      simpleLayout(seriesModel);
    }
  });
}

// node_modules/echarts/lib/chart/graph/graphHelper.js
function getNodeGlobalScale(seriesModel) {
  var coordSys = seriesModel.coordinateSystem;
  return isViewCoordSys(coordSys) ? calcCompensationScaleToPreserveNodeSize(coordSys, seriesModel) : 1;
}
function getSymbolSize(node) {
  var symbolSize = node.getVisual("symbolSize");
  if (symbolSize instanceof Array) {
    symbolSize = (symbolSize[0] + symbolSize[1]) / 2;
  }
  return +symbolSize;
}

// node_modules/echarts/lib/chart/graph/circularLayoutHelper.js
var PI7 = Math.PI;
var _symbolRadiansHalf = [];
function circularLayout(seriesModel, basedOn, draggingNode, pointer) {
  var coordSys = seriesModel.coordinateSystem;
  if (coordSys && coordSys.type !== "view") {
    return;
  }
  var rect = coordSys.getBoundingRect();
  var nodeData = seriesModel.getData();
  var graph = nodeData.graph;
  var cx = rect.width / 2 + rect.x;
  var cy = rect.height / 2 + rect.y;
  var r = Math.min(rect.width, rect.height) / 2;
  var count2 = nodeData.count();
  nodeData.setLayout({
    cx,
    cy
  });
  if (!count2) {
    return;
  }
  if (draggingNode) {
    var _a2 = coordSys.pointToData(pointer), tempX = _a2[0], tempY = _a2[1];
    var v = [tempX - cx, tempY - cy];
    normalize(v, v);
    scale2(v, v, r);
    draggingNode.setLayout([cx + v[0], cy + v[1]], true);
    var circularRotateLabel = seriesModel.get(["circular", "rotateLabel"]);
    rotateNodeLabel(draggingNode, circularRotateLabel, cx, cy);
  }
  _layoutNodesBasedOn[basedOn](seriesModel, graph, nodeData, r, cx, cy, count2);
  graph.eachEdge(function(edge, index) {
    var curveness = retrieve3(edge.getModel().get(["lineStyle", "curveness"]), getCurvenessForEdge(edge, seriesModel, index), 0);
    var p1 = clone3(edge.node1.getLayout());
    var p2 = clone3(edge.node2.getLayout());
    var cp1;
    var x12 = (p1[0] + p2[0]) / 2;
    var y12 = (p1[1] + p2[1]) / 2;
    if (+curveness) {
      curveness *= 3;
      cp1 = [cx * curveness + x12 * (1 - curveness), cy * curveness + y12 * (1 - curveness)];
    }
    edge.setLayout([p1, p2, cp1]);
  });
}
var _layoutNodesBasedOn = {
  value: function(seriesModel, graph, nodeData, r, cx, cy, count2) {
    var angle = 0;
    var sum2 = nodeData.getSum("value");
    var unitAngle = Math.PI * 2 / (sum2 || count2);
    graph.eachNode(function(node) {
      var value = node.getValue("value");
      var radianHalf = unitAngle * (sum2 ? value : 1) / 2;
      angle += radianHalf;
      node.setLayout([r * Math.cos(angle) + cx, r * Math.sin(angle) + cy]);
      angle += radianHalf;
    });
  },
  symbolSize: function(seriesModel, graph, nodeData, r, cx, cy, count2) {
    var sumRadian = 0;
    _symbolRadiansHalf.length = count2;
    var nodeScale = getNodeGlobalScale(seriesModel);
    graph.eachNode(function(node) {
      var symbolSize = getSymbolSize(node);
      isNaN(symbolSize) && (symbolSize = 2);
      symbolSize < 0 && (symbolSize = 0);
      symbolSize *= nodeScale;
      var symbolRadianHalf = Math.asin(symbolSize / 2 / r);
      isNaN(symbolRadianHalf) && (symbolRadianHalf = PI7 / 2);
      _symbolRadiansHalf[node.dataIndex] = symbolRadianHalf;
      sumRadian += symbolRadianHalf * 2;
    });
    var halfRemainRadian = (2 * PI7 - sumRadian) / count2 / 2;
    var angle = 0;
    graph.eachNode(function(node) {
      var radianHalf = halfRemainRadian + _symbolRadiansHalf[node.dataIndex];
      angle += radianHalf;
      (!node.getLayout() || !node.getLayout().fixed) && node.setLayout([r * Math.cos(angle) + cx, r * Math.sin(angle) + cy]);
      angle += radianHalf;
    });
  }
};
function rotateNodeLabel(node, circularRotateLabel, cx, cy) {
  var el = node.getGraphicEl();
  if (!el) {
    return;
  }
  var nodeModel = node.getModel();
  var labelRotate = nodeModel.get(["label", "rotate"]) || 0;
  var symbolPath = el.getSymbolPath();
  if (circularRotateLabel) {
    var pos = node.getLayout();
    var rad = Math.atan2(pos[1] - cy, pos[0] - cx);
    if (rad < 0) {
      rad = Math.PI * 2 + rad;
    }
    var isLeft = pos[0] < cx;
    if (isLeft) {
      rad = rad - Math.PI;
    }
    var textPosition = isLeft ? "left" : "right";
    symbolPath.setTextConfig({
      rotation: -rad,
      position: textPosition,
      origin: "center"
    });
    var emphasisState = symbolPath.ensureState("emphasis");
    extend(emphasisState.textConfig || (emphasisState.textConfig = {}), {
      position: textPosition
    });
  } else {
    symbolPath.setTextConfig({
      rotation: labelRotate *= Math.PI / 180
    });
  }
}

// node_modules/echarts/lib/chart/graph/circularLayout.js
var graphCircularLayoutStageHandler = createSimpleOverallStageHandler(SERIES_TYPE_GRAPH, graphCircularLayout);
function graphCircularLayout(ecModel) {
  ecModel.eachSeriesByType("graph", function(seriesModel) {
    if (seriesModel.get("layout") === "circular") {
      circularLayout(seriesModel, "symbolSize");
    }
  });
}

// node_modules/echarts/lib/chart/graph/forceHelper.js
var scaleAndAdd2 = scaleAndAdd;
function forceLayout(inNodes, inEdges, opts) {
  var nodes = inNodes;
  var edges = inEdges;
  var rect = opts.rect;
  var width = rect.width;
  var height = rect.height;
  var center3 = [rect.x + width / 2, rect.y + height / 2];
  var gravity = opts.gravity == null ? 0.1 : opts.gravity;
  for (var i2 = 0; i2 < nodes.length; i2++) {
    var n = nodes[i2];
    if (!n.p) {
      n.p = create2(width * (Math.random() - 0.5) + center3[0], height * (Math.random() - 0.5) + center3[1]);
    }
    n.pp = clone3(n.p);
    n.edges = null;
  }
  var initialFriction = opts.friction == null ? 0.6 : opts.friction;
  var friction = initialFriction;
  var beforeStepCallback;
  var afterStepCallback;
  return {
    warmUp: function() {
      friction = initialFriction * 0.8;
    },
    setFixed: function(idx) {
      nodes[idx].fixed = true;
    },
    setUnfixed: function(idx) {
      nodes[idx].fixed = false;
    },
    /**
     * Before step hook
     */
    beforeStep: function(cb) {
      beforeStepCallback = cb;
    },
    /**
     * After step hook
     */
    afterStep: function(cb) {
      afterStepCallback = cb;
    },
    /**
     * Some formulas were originally copied from "d3.js"
     * https://github.com/d3/d3/blob/b516d77fb8566b576088e73410437494717ada26/src/layout/force.js
     * with some modifications made for this project.
     * See the license statement at the head of this file.
     */
    step: function(cb) {
      beforeStepCallback && beforeStepCallback(nodes, edges);
      var v12 = [];
      var nLen = nodes.length;
      for (var i3 = 0; i3 < edges.length; i3++) {
        var e2 = edges[i3];
        if (e2.ignoreForceLayout) {
          continue;
        }
        var n1 = e2.n1;
        var n2 = e2.n2;
        sub(v12, n2.p, n1.p);
        var d = len(v12) - e2.d;
        var w = n2.w / (n1.w + n2.w);
        if (isNaN(w)) {
          w = 0;
        }
        normalize(v12, v12);
        !n1.fixed && scaleAndAdd2(n1.p, n1.p, v12, w * d * friction);
        !n2.fixed && scaleAndAdd2(n2.p, n2.p, v12, -(1 - w) * d * friction);
      }
      for (var i3 = 0; i3 < nLen; i3++) {
        var n3 = nodes[i3];
        if (!n3.fixed) {
          sub(v12, center3, n3.p);
          scaleAndAdd2(n3.p, n3.p, v12, gravity * friction);
        }
      }
      for (var i3 = 0; i3 < nLen; i3++) {
        var n1 = nodes[i3];
        for (var j = i3 + 1; j < nLen; j++) {
          var n2 = nodes[j];
          sub(v12, n2.p, n1.p);
          var d = len(v12);
          if (d === 0) {
            set(v12, Math.random() - 0.5, Math.random() - 0.5);
            d = 1;
          }
          var repFact = (n1.rep + n2.rep) / d / d;
          !n1.fixed && scaleAndAdd2(n1.pp, n1.pp, v12, repFact);
          !n2.fixed && scaleAndAdd2(n2.pp, n2.pp, v12, -repFact);
        }
      }
      var v = [];
      for (var i3 = 0; i3 < nLen; i3++) {
        var n3 = nodes[i3];
        if (!n3.fixed) {
          sub(v, n3.p, n3.pp);
          scaleAndAdd2(n3.p, n3.p, v, friction);
          copy2(n3.pp, n3.p);
        }
      }
      friction = friction * 0.992;
      var finished = friction < 0.01;
      afterStepCallback && afterStepCallback(nodes, edges, finished);
      cb && cb(finished);
    }
  };
}

// node_modules/echarts/lib/chart/graph/forceLayout.js
var graphForceLayoutStageHandler = createSimpleOverallStageHandler(SERIES_TYPE_GRAPH, graphForceLayout);
function graphForceLayout(ecModel) {
  ecModel.eachSeriesByType(SERIES_TYPE_GRAPH, function(graphSeries) {
    var coordSys = graphSeries.coordinateSystem;
    if (coordSys && coordSys.type !== "view") {
      return;
    }
    if (graphSeries.get("layout") === "force") {
      var preservedPoints_1 = graphSeries.preservedPoints || {};
      var graph_1 = graphSeries.getGraph();
      var nodeData_1 = graph_1.data;
      var edgeData = graph_1.edgeData;
      var forceModel = graphSeries.getModel("force");
      var initLayout = forceModel.get("initLayout");
      if (graphSeries.preservedPoints) {
        nodeData_1.each(function(idx) {
          var id = nodeData_1.getId(idx);
          nodeData_1.setItemLayout(idx, preservedPoints_1[id] || [NaN, NaN]);
        });
      } else if (!initLayout || initLayout === "none") {
        simpleLayout(graphSeries);
      } else if (initLayout === "circular") {
        circularLayout(graphSeries, "value");
      }
      var nodeDataExtent_1 = nodeData_1.getDataExtent("value");
      var edgeDataExtent_1 = edgeData.getDataExtent("value");
      var repulsion = forceModel.get("repulsion");
      var edgeLength = forceModel.get("edgeLength");
      var repulsionArr_1 = isArray(repulsion) ? repulsion : [repulsion, repulsion];
      var edgeLengthArr_1 = isArray(edgeLength) ? edgeLength : [edgeLength, edgeLength];
      edgeLengthArr_1 = [edgeLengthArr_1[1], edgeLengthArr_1[0]];
      var nodes_1 = nodeData_1.mapArray("value", function(value, idx) {
        var point = nodeData_1.getItemLayout(idx);
        var rep = linearMap(value, nodeDataExtent_1, repulsionArr_1);
        if (isNaN(rep)) {
          rep = (repulsionArr_1[0] + repulsionArr_1[1]) / 2;
        }
        return {
          w: rep,
          rep,
          fixed: nodeData_1.getItemModel(idx).get("fixed"),
          p: !point || isNaN(point[0]) || isNaN(point[1]) ? null : point
        };
      });
      var edges = edgeData.mapArray("value", function(value, idx) {
        var edge = graph_1.getEdgeByIndex(idx);
        var d = linearMap(value, edgeDataExtent_1, edgeLengthArr_1);
        if (isNaN(d)) {
          d = (edgeLengthArr_1[0] + edgeLengthArr_1[1]) / 2;
        }
        var edgeModel = edge.getModel();
        var curveness = retrieve3(edge.getModel().get(["lineStyle", "curveness"]), -getCurvenessForEdge(edge, graphSeries, idx, true), 0);
        return {
          n1: nodes_1[edge.node1.dataIndex],
          n2: nodes_1[edge.node2.dataIndex],
          d,
          curveness,
          ignoreForceLayout: edgeModel.get("ignoreForceLayout")
        };
      });
      var rect = coordSys.getBoundingRect();
      var forceInstance = forceLayout(nodes_1, edges, {
        rect,
        gravity: forceModel.get("gravity"),
        friction: forceModel.get("friction")
      });
      forceInstance.beforeStep(function(nodes, edges2) {
        for (var i2 = 0, l = nodes.length; i2 < l; i2++) {
          if (nodes[i2].fixed) {
            copy2(nodes[i2].p, graph_1.getNodeByIndex(i2).getLayout());
          }
        }
      });
      forceInstance.afterStep(function(nodes, edges2, stopped) {
        for (var i2 = 0, l = nodes.length; i2 < l; i2++) {
          if (!nodes[i2].fixed) {
            graph_1.getNodeByIndex(i2).setLayout(nodes[i2].p);
          }
          preservedPoints_1[nodeData_1.getId(i2)] = nodes[i2].p;
        }
        for (var i2 = 0, l = edges2.length; i2 < l; i2++) {
          var e2 = edges2[i2];
          var edge = graph_1.getEdgeByIndex(i2);
          var p1 = e2.n1.p;
          var p2 = e2.n2.p;
          var points4 = edge.getLayout();
          points4 = points4 ? points4.slice() : [];
          points4[0] = points4[0] || [];
          points4[1] = points4[1] || [];
          copy2(points4[0], p1);
          copy2(points4[1], p2);
          if (+e2.curveness) {
            points4[2] = [(p1[0] + p2[0]) / 2 - (p1[1] - p2[1]) * e2.curveness, (p1[1] + p2[1]) / 2 - (p2[0] - p1[0]) * e2.curveness];
          }
          edge.setLayout(points4);
        }
      });
      graphSeries.forceLayout = forceInstance;
      graphSeries.preservedPoints = preservedPoints_1;
      forceInstance.step();
    } else {
      graphSeries.forceLayout = null;
    }
  });
}

// node_modules/echarts/lib/chart/helper/LinePath.js
var straightLineProto = Line_default.prototype;
var bezierCurveProto = BezierCurve_default.prototype;
var StraightLineShape = (
  /** @class */
  /* @__PURE__ */ (function() {
    function StraightLineShape2() {
      this.x1 = 0;
      this.y1 = 0;
      this.x2 = 0;
      this.y2 = 0;
      this.percent = 1;
    }
    return StraightLineShape2;
  })()
);
var CurveShape = (
  /** @class */
  (function(_super) {
    __extends(CurveShape2, _super);
    function CurveShape2() {
      return _super !== null && _super.apply(this, arguments) || this;
    }
    return CurveShape2;
  })(StraightLineShape)
);
function isStraightLine(shape) {
  return isNaN(+shape.cpx1) || isNaN(+shape.cpy1);
}
var ECLinePath = (
  /** @class */
  (function(_super) {
    __extends(ECLinePath2, _super);
    function ECLinePath2(opts) {
      var _this = _super.call(this, opts) || this;
      _this.type = "ec-line";
      return _this;
    }
    ECLinePath2.prototype.getDefaultStyle = function() {
      return {
        stroke: tokens_default.color.neutral99,
        fill: null
      };
    };
    ECLinePath2.prototype.getDefaultShape = function() {
      return new StraightLineShape();
    };
    ECLinePath2.prototype.buildPath = function(ctx, shape) {
      if (isStraightLine(shape)) {
        straightLineProto.buildPath.call(this, ctx, shape);
      } else {
        bezierCurveProto.buildPath.call(this, ctx, shape);
      }
    };
    ECLinePath2.prototype.pointAt = function(t) {
      if (isStraightLine(this.shape)) {
        return straightLineProto.pointAt.call(this, t);
      } else {
        return bezierCurveProto.pointAt.call(this, t);
      }
    };
    ECLinePath2.prototype.tangentAt = function(t) {
      var shape = this.shape;
      var p = isStraightLine(shape) ? [shape.x2 - shape.x1, shape.y2 - shape.y1] : bezierCurveProto.tangentAt.call(this, t);
      return normalize(p, p);
    };
    return ECLinePath2;
  })(Path_default)
);
var LinePath_default = ECLinePath;

// node_modules/echarts/lib/chart/helper/Line.js
var SYMBOL_CATEGORIES = ["fromSymbol", "toSymbol"];
function makeSymbolTypeKey(symbolCategory) {
  return "_" + symbolCategory + "Type";
}
function makeSymbolTypeValue(name, lineData, idx) {
  var symbolType = lineData.getItemVisual(idx, name);
  if (!symbolType || symbolType === "none") {
    return symbolType;
  }
  var symbolSize = lineData.getItemVisual(idx, name + "Size");
  var symbolRotate = lineData.getItemVisual(idx, name + "Rotate");
  var symbolOffset = lineData.getItemVisual(idx, name + "Offset");
  var symbolKeepAspect = lineData.getItemVisual(idx, name + "KeepAspect");
  var symbolSizeArr = normalizeSymbolSize(symbolSize);
  var symbolOffsetArr = normalizeSymbolOffset(symbolOffset || 0, symbolSizeArr);
  return symbolType + symbolSizeArr + symbolOffsetArr + (symbolRotate || "") + (symbolKeepAspect || "");
}
function createSymbol2(name, lineData, idx) {
  var symbolType = lineData.getItemVisual(idx, name);
  if (!symbolType || symbolType === "none") {
    return;
  }
  var symbolSize = lineData.getItemVisual(idx, name + "Size");
  var symbolRotate = lineData.getItemVisual(idx, name + "Rotate");
  var symbolOffset = lineData.getItemVisual(idx, name + "Offset");
  var symbolKeepAspect = lineData.getItemVisual(idx, name + "KeepAspect");
  var symbolSizeArr = normalizeSymbolSize(symbolSize);
  var symbolOffsetArr = normalizeSymbolOffset(symbolOffset || 0, symbolSizeArr);
  var symbolPath = createSymbol(symbolType, -symbolSizeArr[0] / 2 + symbolOffsetArr[0], -symbolSizeArr[1] / 2 + symbolOffsetArr[1], symbolSizeArr[0], symbolSizeArr[1], null, symbolKeepAspect);
  symbolPath.__specifiedRotation = symbolRotate == null || isNaN(symbolRotate) ? void 0 : +symbolRotate * Math.PI / 180 || 0;
  symbolPath.name = name;
  return symbolPath;
}
function createLine(points4) {
  var line = new LinePath_default({
    name: "line",
    subPixelOptimize: true
  });
  setLinePoints(line.shape, points4);
  return line;
}
function setLinePoints(targetShape, points4) {
  targetShape.x1 = points4[0][0];
  targetShape.y1 = points4[0][1];
  targetShape.x2 = points4[1][0];
  targetShape.y2 = points4[1][1];
  targetShape.percent = 1;
  var cp1 = points4[2];
  if (cp1) {
    targetShape.cpx1 = cp1[0];
    targetShape.cpy1 = cp1[1];
  } else {
    targetShape.cpx1 = NaN;
    targetShape.cpy1 = NaN;
  }
}
var Line2 = (
  /** @class */
  (function(_super) {
    __extends(Line3, _super);
    function Line3(lineData, idx, seriesScope) {
      var _this = _super.call(this) || this;
      _this._createLine(lineData, idx, seriesScope);
      return _this;
    }
    Line3.prototype._createLine = function(lineData, idx, seriesScope) {
      var seriesModel = lineData.hostModel;
      var linePoints = lineData.getItemLayout(idx);
      var z2 = lineData.getItemVisual(idx, "z2");
      var line = createLine(linePoints);
      line.shape.percent = 0;
      initProps(line, {
        z2: retrieve2(z2, 0),
        shape: {
          percent: 1
        }
      }, seriesModel, idx);
      this.add(line);
      each(SYMBOL_CATEGORIES, function(symbolCategory) {
        var symbol = createSymbol2(symbolCategory, lineData, idx);
        this.add(symbol);
        this[makeSymbolTypeKey(symbolCategory)] = makeSymbolTypeValue(symbolCategory, lineData, idx);
      }, this);
      this._updateCommonStl(lineData, idx, seriesScope);
    };
    Line3.prototype.updateData = function(lineData, idx, seriesScope) {
      var seriesModel = lineData.hostModel;
      var line = this.childOfName("line");
      var linePoints = lineData.getItemLayout(idx);
      var target = {
        shape: {}
      };
      setLinePoints(target.shape, linePoints);
      updateProps(line, target, seriesModel, idx);
      each(SYMBOL_CATEGORIES, function(symbolCategory) {
        var symbolType = makeSymbolTypeValue(symbolCategory, lineData, idx);
        var key2 = makeSymbolTypeKey(symbolCategory);
        if (this[key2] !== symbolType) {
          this.remove(this.childOfName(symbolCategory));
          var symbol = createSymbol2(symbolCategory, lineData, idx);
          this.add(symbol);
        }
        this[key2] = symbolType;
      }, this);
      this._updateCommonStl(lineData, idx, seriesScope);
    };
    ;
    Line3.prototype.getLinePath = function() {
      return this.childAt(0);
    };
    Line3.prototype._updateCommonStl = function(lineData, idx, seriesScope) {
      var seriesModel = lineData.hostModel;
      var line = this.childOfName("line");
      var emphasisLineStyle = seriesScope && seriesScope.emphasisLineStyle;
      var blurLineStyle = seriesScope && seriesScope.blurLineStyle;
      var selectLineStyle = seriesScope && seriesScope.selectLineStyle;
      var labelStatesModels = seriesScope && seriesScope.labelStatesModels;
      var emphasisDisabled = seriesScope && seriesScope.emphasisDisabled;
      var focus = seriesScope && seriesScope.focus;
      var blurScope = seriesScope && seriesScope.blurScope;
      if (!seriesScope || lineData.hasItemOption) {
        var itemModel = lineData.getItemModel(idx);
        var emphasisModel = itemModel.getModel("emphasis");
        emphasisLineStyle = emphasisModel.getModel("lineStyle").getLineStyle();
        blurLineStyle = itemModel.getModel(["blur", "lineStyle"]).getLineStyle();
        selectLineStyle = itemModel.getModel(["select", "lineStyle"]).getLineStyle();
        emphasisDisabled = emphasisModel.get("disabled");
        focus = emphasisModel.get("focus");
        blurScope = emphasisModel.get("blurScope");
        labelStatesModels = getLabelStatesModels(itemModel);
      }
      var lineStyle = lineData.getItemVisual(idx, "style");
      var visualColor = lineStyle.stroke;
      line.useStyle(lineStyle);
      line.style.fill = null;
      line.style.strokeNoScale = true;
      line.ensureState("emphasis").style = emphasisLineStyle;
      line.ensureState("blur").style = blurLineStyle;
      line.ensureState("select").style = selectLineStyle;
      each(SYMBOL_CATEGORIES, function(symbolCategory) {
        var symbol = this.childOfName(symbolCategory);
        if (symbol) {
          symbol.setColor(visualColor);
          symbol.style.opacity = lineStyle.opacity;
          for (var i2 = 0; i2 < SPECIAL_STATES.length; i2++) {
            var stateName = SPECIAL_STATES[i2];
            var lineState = line.getState(stateName);
            if (lineState) {
              var lineStateStyle = lineState.style || {};
              var state2 = symbol.ensureState(stateName);
              var stateStyle = state2.style || (state2.style = {});
              if (lineStateStyle.stroke != null) {
                stateStyle[symbol.__isEmptyBrush ? "stroke" : "fill"] = lineStateStyle.stroke;
              }
              if (lineStateStyle.opacity != null) {
                stateStyle.opacity = lineStateStyle.opacity;
              }
            }
          }
          symbol.markRedraw();
        }
      }, this);
      var rawVal = seriesModel.getRawValue(idx);
      setLabelStyle(this, labelStatesModels, {
        labelDataIndex: idx,
        labelFetcher: {
          getFormattedLabel: function(dataIndex, stateName) {
            return seriesModel.getFormattedLabel(dataIndex, stateName, lineData.dataType);
          }
        },
        inheritColor: visualColor || tokens_default.color.neutral99,
        defaultOpacity: lineStyle.opacity,
        defaultText: (rawVal == null ? lineData.getName(idx) : isFinite(rawVal) ? round2(rawVal, 10) : rawVal) + ""
      });
      var label = this.getTextContent();
      if (label) {
        var labelNormalModel = labelStatesModels.normal;
        label.__align = label.style.align;
        label.__verticalAlign = label.style.verticalAlign;
        label.__position = labelNormalModel.get("position") || "middle";
        var distance2 = labelNormalModel.get("distance");
        if (!isArray(distance2)) {
          distance2 = [distance2, distance2];
        }
        label.__labelDistance = distance2;
      }
      this.setTextConfig({
        position: null,
        local: true,
        inside: false
        // Can't be inside for stroke element.
      });
      toggleHoverEmphasis(this, focus, blurScope, emphasisDisabled);
    };
    Line3.prototype.highlight = function() {
      enterEmphasis(this);
    };
    Line3.prototype.downplay = function() {
      leaveEmphasis(this);
    };
    Line3.prototype.updateLayout = function(lineData, idx) {
      this.childOfName("line").stopAnimation();
      this.setLinePoints(lineData.getItemLayout(idx));
    };
    Line3.prototype.setLinePoints = function(points4) {
      var linePath = this.childOfName("line");
      setLinePoints(linePath.shape, points4);
      linePath.dirty();
    };
    Line3.prototype.beforeUpdate = function() {
      var lineGroup = this;
      var symbolFrom = lineGroup.childOfName("fromSymbol");
      var symbolTo = lineGroup.childOfName("toSymbol");
      var label = lineGroup.getTextContent();
      if (!symbolFrom && !symbolTo && (!label || label.ignore)) {
        return;
      }
      var invScale = 1;
      var parentNode2 = this.parent;
      while (parentNode2) {
        if (parentNode2.scaleX) {
          invScale /= parentNode2.scaleX;
        }
        parentNode2 = parentNode2.parent;
      }
      var line = lineGroup.childOfName("line");
      if (!this.__dirty && !line.__dirty) {
        return;
      }
      var percent = line.shape.percent;
      var fromPos = line.pointAt(0);
      var toPos = line.pointAt(percent);
      var d = sub([], toPos, fromPos);
      normalize(d, d);
      function setSymbolRotation(symbol, percent2) {
        var specifiedRotation = symbol.__specifiedRotation;
        if (specifiedRotation == null) {
          var tangent2 = line.tangentAt(percent2);
          symbol.attr("rotation", (percent2 === 1 ? -1 : 1) * Math.PI / 2 - Math.atan2(tangent2[1], tangent2[0]));
        } else {
          symbol.attr("rotation", specifiedRotation);
        }
      }
      if (symbolFrom) {
        symbolFrom.setPosition(fromPos);
        setSymbolRotation(symbolFrom, 0);
        symbolFrom.scaleX = symbolFrom.scaleY = invScale * percent;
        symbolFrom.markRedraw();
      }
      if (symbolTo) {
        symbolTo.setPosition(toPos);
        setSymbolRotation(symbolTo, 1);
        symbolTo.scaleX = symbolTo.scaleY = invScale * percent;
        symbolTo.markRedraw();
      }
      if (label && !label.ignore) {
        label.x = label.y = 0;
        label.originX = label.originY = 0;
        var textAlign = void 0;
        var textVerticalAlign = void 0;
        var distance2 = label.__labelDistance;
        var distanceX = distance2[0] * invScale;
        var distanceY = distance2[1] * invScale;
        var halfPercent = percent / 2;
        var tangent = line.tangentAt(halfPercent);
        var n = [tangent[1], -tangent[0]];
        var cp = line.pointAt(halfPercent);
        if (n[1] > 0) {
          n[0] = -n[0];
          n[1] = -n[1];
        }
        var dir3 = tangent[0] < 0 ? -1 : 1;
        if (label.__position !== "start" && label.__position !== "end") {
          var rotation = -Math.atan2(tangent[1], tangent[0]);
          if (toPos[0] < fromPos[0]) {
            rotation = Math.PI + rotation;
          }
          label.rotation = rotation;
        }
        var dy = void 0;
        switch (label.__position) {
          case "insideStartTop":
          case "insideMiddleTop":
          case "insideEndTop":
          case "middle":
            dy = -distanceY;
            textVerticalAlign = "bottom";
            break;
          case "insideStartBottom":
          case "insideMiddleBottom":
          case "insideEndBottom":
            dy = distanceY;
            textVerticalAlign = "top";
            break;
          default:
            dy = 0;
            textVerticalAlign = "middle";
        }
        switch (label.__position) {
          case "end":
            label.x = d[0] * distanceX + toPos[0];
            label.y = d[1] * distanceY + toPos[1];
            textAlign = d[0] > 0.8 ? "left" : d[0] < -0.8 ? "right" : "center";
            textVerticalAlign = d[1] > 0.8 ? "top" : d[1] < -0.8 ? "bottom" : "middle";
            break;
          case "start":
            label.x = -d[0] * distanceX + fromPos[0];
            label.y = -d[1] * distanceY + fromPos[1];
            textAlign = d[0] > 0.8 ? "right" : d[0] < -0.8 ? "left" : "center";
            textVerticalAlign = d[1] > 0.8 ? "bottom" : d[1] < -0.8 ? "top" : "middle";
            break;
          case "insideStartTop":
          case "insideStart":
          case "insideStartBottom":
            label.x = distanceX * dir3 + fromPos[0];
            label.y = fromPos[1] + dy;
            textAlign = tangent[0] < 0 ? "right" : "left";
            label.originX = -distanceX * dir3;
            label.originY = -dy;
            break;
          case "insideMiddleTop":
          case "insideMiddle":
          case "insideMiddleBottom":
          case "middle":
            label.x = cp[0];
            label.y = cp[1] + dy;
            textAlign = "center";
            label.originY = -dy;
            break;
          case "insideEndTop":
          case "insideEnd":
          case "insideEndBottom":
            label.x = -distanceX * dir3 + toPos[0];
            label.y = toPos[1] + dy;
            textAlign = tangent[0] >= 0 ? "right" : "left";
            label.originX = distanceX * dir3;
            label.originY = -dy;
            break;
        }
        label.scaleX = label.scaleY = invScale;
        label.setStyle({
          // Use the user specified text align and baseline first
          verticalAlign: label.__verticalAlign || textVerticalAlign,
          align: label.__align || textAlign
        });
      }
    };
    return Line3;
  })(Group_default)
);
var Line_default2 = Line2;

// node_modules/echarts/lib/chart/helper/LineDraw.js
var LineDraw = (
  /** @class */
  (function() {
    function LineDraw2(LineCtor) {
      this.group = new Group_default();
      this._LineCtor = LineCtor || Line_default2;
    }
    LineDraw2.prototype.updateData = function(lineData) {
      var _this = this;
      this._progressiveEls = null;
      var lineDraw = this;
      var group2 = lineDraw.group;
      var oldLineData = lineDraw._lineData;
      lineDraw._lineData = lineData;
      if (!oldLineData) {
        group2.removeAll();
      }
      var seriesScope = makeSeriesScope2(lineData);
      lineData.diff(oldLineData).add(function(idx) {
        _this._doAdd(lineData, idx, seriesScope);
      }).update(function(newIdx, oldIdx) {
        _this._doUpdate(oldLineData, lineData, oldIdx, newIdx, seriesScope);
      }).remove(function(idx) {
        group2.remove(oldLineData.getItemGraphicEl(idx));
      }).execute();
    };
    ;
    LineDraw2.prototype.updateLayout = function() {
      var lineData = this._lineData;
      if (!lineData) {
        return;
      }
      lineData.eachItemGraphicEl(function(el, idx) {
        el.updateLayout(lineData, idx);
      }, this);
    };
    ;
    LineDraw2.prototype.incrementalPrepareUpdate = function(lineData) {
      this._seriesScope = makeSeriesScope2(lineData);
      this._lineData = null;
      this.group.removeAll();
    };
    ;
    LineDraw2.prototype.incrementalUpdate = function(taskParams, lineData, incrementalId) {
      this._progressiveEls = [];
      function updateIncrementalAndHover(el2) {
        if (!el2.isGroup && !isEffectObject(el2)) {
          el2.incremental = incrementalId;
          el2.ensureState("emphasis").hoverLayer = HOVER_LAYER_FOR_INCREMENTAL;
        }
      }
      for (var idx = taskParams.start; idx < taskParams.end; idx++) {
        var itemLayout = lineData.getItemLayout(idx);
        if (lineNeedsDraw(itemLayout)) {
          var el = new this._LineCtor(lineData, idx, this._seriesScope);
          el.traverse(updateIncrementalAndHover);
          this.group.add(el);
          lineData.setItemGraphicEl(idx, el);
          this._progressiveEls.push(el);
        }
      }
    };
    ;
    LineDraw2.prototype.remove = function() {
      this.group.removeAll();
    };
    ;
    LineDraw2.prototype.eachRendered = function(cb) {
      traverseElements(this._progressiveEls || this.group, cb);
    };
    LineDraw2.prototype._doAdd = function(lineData, idx, seriesScope) {
      var itemLayout = lineData.getItemLayout(idx);
      if (!lineNeedsDraw(itemLayout)) {
        return;
      }
      var el = new this._LineCtor(lineData, idx, seriesScope);
      lineData.setItemGraphicEl(idx, el);
      this.group.add(el);
    };
    LineDraw2.prototype._doUpdate = function(oldLineData, newLineData, oldIdx, newIdx, seriesScope) {
      var itemEl = oldLineData.getItemGraphicEl(oldIdx);
      if (!lineNeedsDraw(newLineData.getItemLayout(newIdx))) {
        this.group.remove(itemEl);
        return;
      }
      if (!itemEl) {
        itemEl = new this._LineCtor(newLineData, newIdx, seriesScope);
      } else {
        itemEl.updateData(newLineData, newIdx, seriesScope);
      }
      newLineData.setItemGraphicEl(newIdx, itemEl);
      this.group.add(itemEl);
    };
    return LineDraw2;
  })()
);
function isEffectObject(el) {
  return el.animators && el.animators.length > 0;
}
function makeSeriesScope2(lineData) {
  var hostModel = lineData.hostModel;
  var emphasisModel = hostModel.getModel("emphasis");
  return {
    lineStyle: hostModel.getModel("lineStyle").getLineStyle(),
    emphasisLineStyle: emphasisModel.getModel(["lineStyle"]).getLineStyle(),
    blurLineStyle: hostModel.getModel(["blur", "lineStyle"]).getLineStyle(),
    selectLineStyle: hostModel.getModel(["select", "lineStyle"]).getLineStyle(),
    emphasisDisabled: emphasisModel.get("disabled"),
    blurScope: emphasisModel.get("blurScope"),
    focus: emphasisModel.get("focus"),
    labelStatesModels: getLabelStatesModels(hostModel)
  };
}
function isPointNaN(pt) {
  return isNaN(pt[0]) || isNaN(pt[1]);
}
function lineNeedsDraw(pts) {
  return pts && !isPointNaN(pts[0]) && !isPointNaN(pts[1]);
}
var LineDraw_default = LineDraw;

// node_modules/echarts/lib/chart/graph/adjustEdge.js
var v1 = [];
var v2 = [];
var v3 = [];
var quadraticAt2 = quadraticAt;
var v2DistSquare = distSquare;
var mathAbs6 = Math.abs;
function intersectCurveCircle(curvePoints, center3, radius) {
  var p0 = curvePoints[0];
  var p1 = curvePoints[1];
  var p2 = curvePoints[2];
  var d = Infinity;
  var t;
  var radiusSquare = radius * radius;
  var interval2 = 0.1;
  for (var _t = 0.1; _t <= 0.9; _t += 0.1) {
    v1[0] = quadraticAt2(p0[0], p1[0], p2[0], _t);
    v1[1] = quadraticAt2(p0[1], p1[1], p2[1], _t);
    var diff = mathAbs6(v2DistSquare(v1, center3) - radiusSquare);
    if (diff < d) {
      d = diff;
      t = _t;
    }
  }
  for (var i2 = 0; i2 < 32; i2++) {
    var next = t + interval2;
    v2[0] = quadraticAt2(p0[0], p1[0], p2[0], t);
    v2[1] = quadraticAt2(p0[1], p1[1], p2[1], t);
    v3[0] = quadraticAt2(p0[0], p1[0], p2[0], next);
    v3[1] = quadraticAt2(p0[1], p1[1], p2[1], next);
    var diff = v2DistSquare(v2, center3) - radiusSquare;
    if (mathAbs6(diff) < 0.01) {
      break;
    }
    var nextDiff = v2DistSquare(v3, center3) - radiusSquare;
    interval2 /= 2;
    if (diff < 0) {
      if (nextDiff >= 0) {
        t = t + interval2;
      } else {
        t = t - interval2;
      }
    } else {
      if (nextDiff >= 0) {
        t = t - interval2;
      } else {
        t = t + interval2;
      }
    }
  }
  return t;
}
function adjustEdge(graph, scale3) {
  var tmp0 = [];
  var quadraticSubdivide2 = quadraticSubdivide;
  var pts = [[], [], []];
  var pts2 = [[], []];
  var v = [];
  scale3 /= 2;
  graph.eachEdge(function(edge, idx) {
    var linePoints = edge.getLayout();
    var fromSymbol = edge.getVisual("fromSymbol");
    var toSymbol = edge.getVisual("toSymbol");
    if (!linePoints.__original) {
      linePoints.__original = [clone3(linePoints[0]), clone3(linePoints[1])];
      if (linePoints[2]) {
        linePoints.__original.push(clone3(linePoints[2]));
      }
    }
    var originalPoints = linePoints.__original;
    if (linePoints[2] != null) {
      copy2(pts[0], originalPoints[0]);
      copy2(pts[1], originalPoints[2]);
      copy2(pts[2], originalPoints[1]);
      if (fromSymbol && fromSymbol !== "none") {
        var symbolSize = getSymbolSize(edge.node1);
        var t = intersectCurveCircle(pts, originalPoints[0], symbolSize * scale3);
        quadraticSubdivide2(pts[0][0], pts[1][0], pts[2][0], t, tmp0);
        pts[0][0] = tmp0[3];
        pts[1][0] = tmp0[4];
        quadraticSubdivide2(pts[0][1], pts[1][1], pts[2][1], t, tmp0);
        pts[0][1] = tmp0[3];
        pts[1][1] = tmp0[4];
      }
      if (toSymbol && toSymbol !== "none") {
        var symbolSize = getSymbolSize(edge.node2);
        var t = intersectCurveCircle(pts, originalPoints[1], symbolSize * scale3);
        quadraticSubdivide2(pts[0][0], pts[1][0], pts[2][0], t, tmp0);
        pts[1][0] = tmp0[1];
        pts[2][0] = tmp0[2];
        quadraticSubdivide2(pts[0][1], pts[1][1], pts[2][1], t, tmp0);
        pts[1][1] = tmp0[1];
        pts[2][1] = tmp0[2];
      }
      copy2(linePoints[0], pts[0]);
      copy2(linePoints[1], pts[2]);
      copy2(linePoints[2], pts[1]);
    } else {
      copy2(pts2[0], originalPoints[0]);
      copy2(pts2[1], originalPoints[1]);
      sub(v, pts2[1], pts2[0]);
      normalize(v, v);
      if (fromSymbol && fromSymbol !== "none") {
        var symbolSize = getSymbolSize(edge.node1);
        scaleAndAdd(pts2[0], pts2[0], v, symbolSize * scale3);
      }
      if (toSymbol && toSymbol !== "none") {
        var symbolSize = getSymbolSize(edge.node2);
        scaleAndAdd(pts2[1], pts2[1], v, -symbolSize * scale3);
      }
      copy2(linePoints[0], pts2[0]);
      copy2(linePoints[1], pts2[1]);
    }
  });
}

// node_modules/echarts/lib/component/helper/thumbnailBridge.js
var inner13 = makeInner();
function getThumbnailBridge(model2) {
  if (model2) {
    return inner13(model2).bridge;
  }
}
function injectThumbnailBridge(model2, thumbnailBridge) {
  if (model2) {
    inner13(model2).bridge = thumbnailBridge;
  }
}

// node_modules/echarts/lib/chart/graph/GraphView.js
var GraphView = (
  /** @class */
  (function(_super) {
    __extends(GraphView2, _super);
    function GraphView2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = SERIES_TYPE_GRAPH;
      return _this;
    }
    GraphView2.prototype.init = function(ecModel, api) {
      var symbolDraw = new SymbolDraw_default();
      var lineDraw = new LineDraw_default();
      var group2 = this.group;
      var mainGroup = new Group_default();
      this._controller = new RoamController_default(api.getZr());
      mainGroup.add(symbolDraw.group);
      mainGroup.add(lineDraw.group);
      group2.add(mainGroup);
      this._symbolDraw = symbolDraw;
      this._lineDraw = lineDraw;
      this._mainGroup = mainGroup;
      this._firstRender = true;
    };
    GraphView2.prototype.render = function(seriesModel, ecModel, api) {
      var _this = this;
      var ownCoordSys = getOwnRoamViewCoordSys(seriesModel);
      var isForceLayout = false;
      this._model = seriesModel;
      this._api = api;
      this._active = true;
      var mainGroup = this._mainGroup;
      var thumbnailInfo = this._getThumbnailInfo();
      if (thumbnailInfo) {
        thumbnailInfo.bridge.reset(api);
      }
      var symbolDraw = this._symbolDraw;
      var lineDraw = this._lineDraw;
      if (ownCoordSys) {
        applyViewCoordSysTransToElement(mainGroup, VIEW_COORD_SYS_TRANS_OVERALL, ownCoordSys, this._firstRender ? null : seriesModel);
      }
      adjustEdge(seriesModel.getGraph(), getNodeGlobalScale(seriesModel));
      var data = seriesModel.getData();
      symbolDraw.updateData(data);
      var edgeData = seriesModel.getEdgeData();
      lineDraw.updateData(edgeData);
      this._updateNodeAndLinkScale();
      if (ownCoordSys) {
        updateRoamControllerSimply(seriesModel, api, this._controller, function(e2, x, y) {
          return seriesModel.coordinateSystem.containPoint([x, y]);
        }, null);
      }
      clearTimeout(this._layoutTimeout);
      var forceLayout2 = seriesModel.forceLayout;
      var layoutAnimation = seriesModel.get(["force", "layoutAnimation"]);
      if (forceLayout2) {
        isForceLayout = true;
        this._startForceLayoutIteration(forceLayout2, api, layoutAnimation);
      }
      var layout3 = seriesModel.get("layout");
      data.graph.eachNode(function(node) {
        var idx = node.dataIndex;
        var el = node.getGraphicEl();
        var itemModel = node.getModel();
        if (!el) {
          return;
        }
        el.off("drag").off("dragend");
        var draggable = itemModel.get("draggable");
        if (draggable) {
          el.on("drag", function(e2) {
            switch (layout3) {
              case "force":
                forceLayout2.warmUp();
                !_this._layouting && _this._startForceLayoutIteration(forceLayout2, api, layoutAnimation);
                forceLayout2.setFixed(idx);
                data.setItemLayout(idx, [el.x, el.y]);
                break;
              case "circular":
                data.setItemLayout(idx, [el.x, el.y]);
                node.setLayout({
                  fixed: true
                }, true);
                circularLayout(seriesModel, "symbolSize", node, [e2.offsetX, e2.offsetY]);
                _this.updateLayout(seriesModel);
                break;
              case "none":
              default:
                data.setItemLayout(idx, [el.x, el.y]);
                simpleLayoutEdge(seriesModel.getGraph(), seriesModel);
                _this.updateLayout(seriesModel);
                break;
            }
          }).on("dragend", function() {
            if (forceLayout2) {
              forceLayout2.setUnfixed(idx);
            }
          });
        }
        el.setDraggable(draggable, !!itemModel.get("cursor"));
        var focus = itemModel.get(["emphasis", "focus"]);
        if (focus === "adjacency") {
          getECData(el).focus = node.getAdjacentDataIndices();
        }
      });
      data.graph.eachEdge(function(edge) {
        var el = edge.getGraphicEl();
        var focus = edge.getModel().get(["emphasis", "focus"]);
        if (!el) {
          return;
        }
        if (focus === "adjacency") {
          getECData(el).focus = {
            edge: [edge.dataIndex],
            node: [edge.node1.dataIndex, edge.node2.dataIndex]
          };
        }
      });
      var circularRotateLabel = seriesModel.get("layout") === "circular" && seriesModel.get(["circular", "rotateLabel"]);
      var cx = data.getLayout("cx");
      var cy = data.getLayout("cy");
      data.graph.eachNode(function(node) {
        rotateNodeLabel(node, circularRotateLabel, cx, cy);
      });
      this._firstRender = false;
      if (!isForceLayout) {
        this._renderThumbnail(seriesModel, api, this._symbolDraw, this._lineDraw);
      }
    };
    GraphView2.prototype.dispose = function() {
      this.remove();
      this._controller && this._controller.dispose();
    };
    GraphView2.prototype._startForceLayoutIteration = function(forceLayout2, api, layoutAnimation) {
      var self2 = this;
      var firstRendered = false;
      (function step() {
        forceLayout2.step(function(stopped) {
          self2.updateLayout(self2._model);
          if (stopped || !firstRendered) {
            firstRendered = true;
            self2._renderThumbnail(self2._model, api, self2._symbolDraw, self2._lineDraw);
          }
          (self2._layouting = !stopped) && (layoutAnimation ? self2._layoutTimeout = setTimeout(step, 16) : step());
        });
      })();
    };
    GraphView2.prototype.__updateOnOwnRoam = function(payload, seriesModel, api) {
      var ownCoordSys = getOwnRoamViewCoordSys(seriesModel);
      if (!this._active || !ownCoordSys) {
        return;
      }
      applyViewCoordSysTransToElement(this._mainGroup, VIEW_COORD_SYS_TRANS_OVERALL, ownCoordSys, null);
      if (isRoamPayloadHasZoom(payload)) {
        this._updateNodeAndLinkScale();
        adjustEdge(seriesModel.getGraph(), getNodeGlobalScale(seriesModel));
        this._lineDraw.updateLayout();
        api.updateLabelLayout();
      }
      this._updateThumbnailWindow();
    };
    GraphView2.prototype._updateNodeAndLinkScale = function() {
      var seriesModel = this._model;
      var data = seriesModel.getData();
      var nodeScale = getNodeGlobalScale(seriesModel);
      data.eachItemGraphicEl(function(el, idx) {
        el && el.setSymbolScale(nodeScale);
      });
    };
    GraphView2.prototype.updateLayout = function(seriesModel) {
      if (!this._active) {
        return;
      }
      adjustEdge(seriesModel.getGraph(), getNodeGlobalScale(seriesModel));
      this._symbolDraw.updateLayout();
      this._lineDraw.updateLayout();
    };
    GraphView2.prototype.remove = function() {
      this._active = false;
      clearTimeout(this._layoutTimeout);
      this._layouting = false;
      this._layoutTimeout = null;
      this._symbolDraw && this._symbolDraw.remove();
      this._lineDraw && this._lineDraw.remove();
      this._controller && this._controller.disable();
    };
    GraphView2.prototype._getThumbnailInfo = function() {
      var model2 = this._model;
      var coordSys = model2.coordinateSystem;
      if (coordSys.type !== "view") {
        return;
      }
      var bridge = getThumbnailBridge(model2);
      if (!bridge) {
        return;
      }
      return {
        bridge,
        coordSys
      };
    };
    GraphView2.prototype._updateThumbnailWindow = function() {
      var info = this._getThumbnailInfo();
      if (info) {
        info.bridge.updateWindow(viewCoordSysCopyOverallMatrix(null, info.coordSys), this._api);
      }
    };
    GraphView2.prototype._renderThumbnail = function(seriesModel, api, symbolDraw, lineDraw) {
      var info = this._getThumbnailInfo();
      if (!info) {
        return;
      }
      var bridgeGroup = new Group_default();
      var symbolNodes = symbolDraw.group.children();
      var lineNodes = lineDraw.group.children();
      var lineGroup = new Group_default();
      var symbolGroup = new Group_default();
      bridgeGroup.add(symbolGroup);
      bridgeGroup.add(lineGroup);
      for (var i2 = 0; i2 < symbolNodes.length; i2++) {
        var node = symbolNodes[i2];
        var sub2 = node.children()[0];
        var x = node.x;
        var y = node.y;
        var subShape = clone(sub2.shape);
        var shape = extend(subShape, {
          width: sub2.scaleX,
          height: sub2.scaleY,
          x: x - sub2.scaleX / 2,
          y: y - sub2.scaleY / 2
        });
        var style2 = clone(sub2.style);
        var subThumbnail = new sub2.constructor({
          shape,
          style: style2,
          z2: 151
        });
        symbolGroup.add(subThumbnail);
      }
      for (var i2 = 0; i2 < lineNodes.length; i2++) {
        var node = lineNodes[i2];
        var line = node.children()[0];
        var style2 = clone(line.style);
        var shape = clone(line.shape);
        var lineThumbnail = new LinePath_default({
          style: style2,
          shape,
          z2: 151
        });
        lineGroup.add(lineThumbnail);
      }
      info.bridge.renderContent({
        api,
        roamType: seriesModel.get("roam"),
        viewportRect: null,
        group: bridgeGroup,
        targetTrans: viewCoordSysCopyOverallMatrix(null, info.coordSys)
      });
    };
    GraphView2.type = SERIES_TYPE_GRAPH;
    return GraphView2;
  })(Chart_default)
);

// node_modules/echarts/lib/chart/graph/categoryFilter.js
var graphCategoryFilterStageHandler = createSimpleOverallStageHandler(SERIES_TYPE_GRAPH, categoryFilter);
function categoryFilter(ecModel) {
  var legendModels = ecModel.findComponents({
    mainType: "legend"
  });
  if (!legendModels || !legendModels.length) {
    return;
  }
  ecModel.eachSeriesByType(SERIES_TYPE_GRAPH, function(graphSeries) {
    var categoriesData = graphSeries.getCategoriesData();
    var graph = graphSeries.getGraph();
    var data = graph.data;
    var categoryNames = categoriesData.mapArray(categoriesData.getName);
    data.filterSelf(function(idx) {
      var model2 = data.getItemModel(idx);
      var category = model2.getShallow("category");
      if (category != null) {
        if (isNumber(category)) {
          category = categoryNames[category];
        }
        for (var i2 = 0; i2 < legendModels.length; i2++) {
          if (!legendModels[i2].isSelected(category)) {
            return false;
          }
        }
      }
      return true;
    });
  });
}

// node_modules/echarts/lib/chart/graph/categoryVisual.js
var graphCategoryVisualStageHandler = createSimpleOverallStageHandler(SERIES_TYPE_GRAPH, categoryVisual);
function categoryVisual(ecModel) {
  var paletteScope = {};
  ecModel.eachSeriesByType(SERIES_TYPE_GRAPH, function(seriesModel) {
    var categoriesData = seriesModel.getCategoriesData();
    var data = seriesModel.getData();
    var categoryNameIdxMap = {};
    categoriesData.each(function(idx) {
      var name = categoriesData.getName(idx);
      categoryNameIdxMap["ec-" + name] = idx;
      var itemModel = categoriesData.getItemModel(idx);
      var style2 = itemModel.getModel("itemStyle").getItemStyle();
      if (!style2.fill) {
        style2.fill = seriesModel.getColorFromPalette(name, paletteScope);
      }
      categoriesData.setItemVisual(idx, "style", style2);
      var symbolVisualList = ["symbol", "symbolSize", "symbolKeepAspect"];
      for (var i2 = 0; i2 < symbolVisualList.length; i2++) {
        var symbolVisual = itemModel.getShallow(symbolVisualList[i2], true);
        if (symbolVisual != null) {
          categoriesData.setItemVisual(idx, symbolVisualList[i2], symbolVisual);
        }
      }
    });
    if (categoriesData.count()) {
      data.each(function(idx) {
        var model2 = data.getItemModel(idx);
        var categoryIdx = model2.getShallow("category");
        if (categoryIdx != null) {
          if (isString(categoryIdx)) {
            categoryIdx = categoryNameIdxMap["ec-" + categoryIdx];
          }
          var categoryStyle = categoriesData.getItemVisual(categoryIdx, "style");
          var style2 = data.ensureUniqueItemVisual(idx, "style");
          extend(style2, categoryStyle);
          var visualList = ["symbol", "symbolSize", "symbolKeepAspect"];
          for (var i2 = 0; i2 < visualList.length; i2++) {
            data.setItemVisual(idx, visualList[i2], categoriesData.getItemVisual(categoryIdx, visualList[i2]));
          }
        }
      });
    }
  });
}

// node_modules/echarts/lib/chart/chord/ChordSeries.js
var SERIES_TYPE_CHORD = "chord";
var ChordSeriesModel = (
  /** @class */
  (function(_super) {
    __extends(ChordSeriesModel2, _super);
    function ChordSeriesModel2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = ChordSeriesModel2.type;
      return _this;
    }
    ChordSeriesModel2.prototype.init = function(option) {
      _super.prototype.init.apply(this, arguments);
      this.fillDataTextStyle(option.edges || option.links);
      this.legendVisualProvider = new LegendVisualProvider_default(bind(this.getData, this), bind(this.getRawData, this));
    };
    ChordSeriesModel2.prototype.mergeOption = function(option) {
      _super.prototype.mergeOption.apply(this, arguments);
      this.fillDataTextStyle(option.edges || option.links);
    };
    ChordSeriesModel2.prototype.getInitialData = function(option, ecModel) {
      var edges = option.edges || option.links || [];
      var nodes = option.data || option.nodes || [];
      if (nodes && edges) {
        var graph = createGraphFromNodeEdge(nodes, edges, this, true, beforeLink);
        return graph.data;
      }
      function beforeLink(nodeData, edgeData) {
        var oldGetModel = Model_default.prototype.getModel;
        function newGetModel(path, parentModel) {
          var model2 = oldGetModel.call(this, path, parentModel);
          model2.resolveParentPath = resolveParentPath;
          return model2;
        }
        edgeData.wrapMethod("getItemModel", function(model2) {
          model2.resolveParentPath = resolveParentPath;
          model2.getModel = newGetModel;
          return model2;
        });
        function resolveParentPath(pathArr) {
          if (pathArr && (pathArr[0] === "label" || pathArr[1] === "label")) {
            var newPathArr = pathArr.slice();
            if (pathArr[0] === "label") {
              newPathArr[0] = "edgeLabel";
            } else if (pathArr[1] === "label") {
              newPathArr[1] = "edgeLabel";
            }
            return newPathArr;
          }
          return pathArr;
        }
      }
    };
    ChordSeriesModel2.prototype.getGraph = function() {
      return this.getData().graph;
    };
    ChordSeriesModel2.prototype.getEdgeData = function() {
      return this.getGraph().edgeData;
    };
    ChordSeriesModel2.prototype.formatTooltip = function(dataIndex, multipleSeries, dataType) {
      var params = this.getDataParams(dataIndex, dataType);
      if (dataType === "edge") {
        var nodeData = this.getData();
        var edge = nodeData.graph.getEdgeByIndex(dataIndex);
        var sourceName = nodeData.getName(edge.node1.dataIndex);
        var targetName = nodeData.getName(edge.node2.dataIndex);
        var nameArr = [];
        sourceName != null && nameArr.push(sourceName);
        targetName != null && nameArr.push(targetName);
        return createTooltipMarkup("nameValue", {
          name: nameArr.join(" > "),
          value: params.value,
          noValue: params.value == null
        });
      }
      return createTooltipMarkup("nameValue", {
        name: params.name,
        value: params.value,
        noValue: params.value == null
      });
    };
    ChordSeriesModel2.prototype.getDataParams = function(dataIndex, dataType) {
      var params = _super.prototype.getDataParams.call(this, dataIndex, dataType);
      if (dataType === "node") {
        var nodeData = this.getData();
        var node = this.getGraph().getNodeByIndex(dataIndex);
        if (params.name == null) {
          params.name = nodeData.getName(dataIndex);
        }
        if (params.value == null) {
          var nodeValue = node.getLayout().value;
          params.value = nodeValue;
        }
      }
      return params;
    };
    ChordSeriesModel2.type = "series." + SERIES_TYPE_CHORD;
    ChordSeriesModel2.defaultOption = {
      // zlevel: 0,
      z: 2,
      coordinateSystem: "none",
      legendHoverLink: true,
      colorBy: "data",
      left: 0,
      top: 0,
      right: 0,
      bottom: 0,
      width: null,
      height: null,
      center: ["50%", "50%"],
      radius: ["70%", "80%"],
      clockwise: true,
      startAngle: 90,
      endAngle: "auto",
      minAngle: 0,
      padAngle: 3,
      itemStyle: {
        borderRadius: [0, 0, 5, 5]
      },
      lineStyle: {
        width: 0,
        color: "source",
        opacity: 0.2
      },
      label: {
        show: true,
        position: "outside",
        distance: 5
      },
      emphasis: {
        focus: "adjacency",
        lineStyle: {
          opacity: 0.5
        }
      }
    };
    return ChordSeriesModel2;
  })(Series_default)
);

// node_modules/echarts/lib/chart/chord/ChordPiece.js
var ChordPiece = (
  /** @class */
  (function(_super) {
    __extends(ChordPiece2, _super);
    function ChordPiece2(data, idx, startAngle) {
      var _this = _super.call(this) || this;
      getECData(_this).dataType = "node";
      _this.z2 = 2;
      var text = new Text_default();
      _this.setTextContent(text);
      _this.updateData(data, idx, startAngle, true);
      return _this;
    }
    ChordPiece2.prototype.updateData = function(data, idx, startAngle, firstCreate) {
      var sector = this;
      var node = data.graph.getNodeByIndex(idx);
      var seriesModel = data.hostModel;
      var itemModel = node.getModel();
      var emphasisModel = itemModel.getModel("emphasis");
      var layout3 = data.getItemLayout(idx);
      var shape = extend(getSectorCornerRadius(itemModel.getModel("itemStyle"), layout3, true), layout3);
      var el = this;
      if (isNaN(shape.startAngle)) {
        el.setShape(shape);
        return;
      }
      if (firstCreate) {
        el.setShape(shape);
      } else {
        updateProps(el, {
          shape
        }, seriesModel, idx);
      }
      var sectorShape = extend(getSectorCornerRadius(itemModel.getModel("itemStyle"), layout3, true), layout3);
      sector.setShape(sectorShape);
      sector.useStyle(data.getItemVisual(idx, "style"));
      setStatesStylesFromModel(sector, itemModel);
      this._updateLabel(seriesModel, itemModel, node);
      data.setItemGraphicEl(idx, el);
      setStatesStylesFromModel(el, itemModel, "itemStyle");
      var focus = emphasisModel.get("focus");
      toggleHoverEmphasis(this, focus === "adjacency" ? node.getAdjacentDataIndices() : focus, emphasisModel.get("blurScope"), emphasisModel.get("disabled"));
    };
    ChordPiece2.prototype._updateLabel = function(seriesModel, itemModel, node) {
      var label = this.getTextContent();
      var layout3 = node.getLayout();
      var midAngle = (layout3.startAngle + layout3.endAngle) / 2;
      var dx = Math.cos(midAngle);
      var dy = Math.sin(midAngle);
      var normalLabelModel = itemModel.getModel("label");
      label.ignore = !normalLabelModel.get("show");
      var labelStateModels = getLabelStatesModels(itemModel);
      var style2 = node.getVisual("style");
      setLabelStyle(label, labelStateModels, {
        labelFetcher: {
          getFormattedLabel: function(dataIndex, stateName, dataType, labelDimIndex, formatter, extendParams) {
            return seriesModel.getFormattedLabel(
              dataIndex,
              stateName,
              "node",
              labelDimIndex,
              // ensure edgeLabel formatter is provided
              // to prevent the inheritance from `label.formatter` of the series
              retrieve3(formatter, labelStateModels.normal && labelStateModels.normal.get("formatter"), itemModel.get("name")),
              extendParams
            );
          }
        },
        labelDataIndex: node.dataIndex,
        defaultText: node.dataIndex + "",
        inheritColor: style2.fill,
        defaultOpacity: style2.opacity,
        defaultOutsidePosition: "startArc"
      });
      var labelPosition = normalLabelModel.get("position") || "outside";
      var labelPadding = normalLabelModel.get("distance") || 0;
      var r;
      if (labelPosition === "outside") {
        r = layout3.r + labelPadding;
      } else {
        r = (layout3.r + layout3.r0) / 2;
      }
      this.textConfig = {
        inside: labelPosition !== "outside"
      };
      var align = labelPosition !== "outside" ? normalLabelModel.get("align") || "center" : dx > 0 ? "left" : "right";
      var verticalAlign = labelPosition !== "outside" ? normalLabelModel.get("verticalAlign") || "middle" : dy > 0 ? "top" : "bottom";
      label.attr({
        x: dx * r + layout3.cx,
        y: dy * r + layout3.cy,
        rotation: 0,
        style: {
          align,
          verticalAlign
        }
      });
    };
    return ChordPiece2;
  })(Sector_default)
);
var ChordPiece_default = ChordPiece;

// node_modules/echarts/lib/chart/chord/ChordEdge.js
var ChordEdge = (
  /** @class */
  (function(_super) {
    __extends(ChordEdge2, _super);
    function ChordEdge2(nodeData, edgeData, edgeIdx, startAngle) {
      var _this = _super.call(this) || this;
      getECData(_this).dataType = "edge";
      _this.updateData(nodeData, edgeData, edgeIdx, startAngle, true);
      return _this;
    }
    ChordEdge2.prototype.buildPath = function(ctx, shape) {
      ctx.moveTo(shape.s1[0], shape.s1[1]);
      var ratio = 0.7;
      var clockwise = shape.clockwise;
      ctx.arc(shape.cx, shape.cy, shape.r, shape.sStartAngle, shape.sEndAngle, !clockwise);
      ctx.bezierCurveTo((shape.cx - shape.s2[0]) * ratio + shape.s2[0], (shape.cy - shape.s2[1]) * ratio + shape.s2[1], (shape.cx - shape.t1[0]) * ratio + shape.t1[0], (shape.cy - shape.t1[1]) * ratio + shape.t1[1], shape.t1[0], shape.t1[1]);
      ctx.arc(shape.cx, shape.cy, shape.r, shape.tStartAngle, shape.tEndAngle, !clockwise);
      ctx.bezierCurveTo((shape.cx - shape.t2[0]) * ratio + shape.t2[0], (shape.cy - shape.t2[1]) * ratio + shape.t2[1], (shape.cx - shape.s1[0]) * ratio + shape.s1[0], (shape.cy - shape.s1[1]) * ratio + shape.s1[1], shape.s1[0], shape.s1[1]);
      ctx.closePath();
    };
    ChordEdge2.prototype.updateData = function(nodeData, edgeData, edgeIdx, startAngle, firstCreate) {
      var seriesModel = nodeData.hostModel;
      var edge = edgeData.graph.getEdgeByIndex(edgeIdx);
      var layout3 = edge.getLayout();
      var itemModel = edge.node1.getModel();
      var edgeModel = edgeData.getItemModel(edge.dataIndex);
      var lineStyle = edgeModel.getModel("lineStyle");
      var emphasisModel = edgeModel.getModel("emphasis");
      var focus = emphasisModel.get("focus");
      var shape = extend(getSectorCornerRadius(itemModel.getModel("itemStyle"), layout3, true), layout3);
      var el = this;
      if (isNaN(shape.sStartAngle) || isNaN(shape.tStartAngle)) {
        el.setShape(shape);
        return;
      }
      if (firstCreate) {
        el.setShape(shape);
        applyEdgeFill(el, edge, nodeData, lineStyle);
      } else {
        saveOldStyle(el);
        applyEdgeFill(el, edge, nodeData, lineStyle);
        updateProps(el, {
          shape
        }, seriesModel, edgeIdx);
      }
      toggleHoverEmphasis(this, focus === "adjacency" ? edge.getAdjacentDataIndices() : focus, emphasisModel.get("blurScope"), emphasisModel.get("disabled"));
      setStatesStylesFromModel(el, edgeModel, "lineStyle");
      edgeData.setItemGraphicEl(edge.dataIndex, el);
    };
    return ChordEdge2;
  })(Path_default)
);
function applyEdgeFill(edgeShape, edge, nodeData, lineStyleModel) {
  var node1 = edge.node1;
  var node2 = edge.node2;
  var edgeStyle = edgeShape.style;
  edgeShape.setStyle(lineStyleModel.getLineStyle());
  var color3 = lineStyleModel.get("color");
  switch (color3) {
    case "source":
      edgeStyle.fill = nodeData.getItemVisual(node1.dataIndex, "style").fill;
      edgeStyle.decal = node1.getVisual("style").decal;
      break;
    case "target":
      edgeStyle.fill = nodeData.getItemVisual(node2.dataIndex, "style").fill;
      edgeStyle.decal = node2.getVisual("style").decal;
      break;
    case "gradient":
      var sourceColor = nodeData.getItemVisual(node1.dataIndex, "style").fill;
      var targetColor = nodeData.getItemVisual(node2.dataIndex, "style").fill;
      if (isString(sourceColor) && isString(targetColor)) {
        var shape = edgeShape.shape;
        var sMidX = (shape.s1[0] + shape.s2[0]) / 2;
        var sMidY = (shape.s1[1] + shape.s2[1]) / 2;
        var tMidX = (shape.t1[0] + shape.t2[0]) / 2;
        var tMidY = (shape.t1[1] + shape.t2[1]) / 2;
        edgeStyle.fill = new LinearGradient_default(sMidX, sMidY, tMidX, tMidY, [{
          offset: 0,
          color: sourceColor
        }, {
          offset: 1,
          color: targetColor
        }], true);
      }
      break;
  }
}

// node_modules/echarts/lib/chart/chord/ChordView.js
var RADIAN3 = Math.PI / 180;
var ChordView = (
  /** @class */
  (function(_super) {
    __extends(ChordView2, _super);
    function ChordView2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = SERIES_TYPE_CHORD;
      return _this;
    }
    ChordView2.prototype.init = function(ecModel, api) {
    };
    ChordView2.prototype.render = function(seriesModel, ecModel, api) {
      var data = seriesModel.getData();
      var oldData = this._data;
      var group2 = this.group;
      var startAngle = -seriesModel.get("startAngle") * RADIAN3;
      data.diff(oldData).add(function(newIdx) {
        var layout3 = data.getItemLayout(newIdx);
        if (layout3) {
          var el = new ChordPiece_default(data, newIdx, startAngle);
          getECData(el).dataIndex = newIdx;
          group2.add(el);
        }
      }).update(function(newIdx, oldIdx) {
        var el = oldData.getItemGraphicEl(oldIdx);
        var layout3 = data.getItemLayout(newIdx);
        if (!layout3) {
          el && removeElementWithFadeOut(el, seriesModel, oldIdx);
          return;
        }
        if (!el) {
          el = new ChordPiece_default(data, newIdx, startAngle);
        } else {
          el.updateData(data, newIdx, startAngle);
        }
        group2.add(el);
      }).remove(function(oldIdx) {
        var el = oldData.getItemGraphicEl(oldIdx);
        el && removeElementWithFadeOut(el, seriesModel, oldIdx);
      }).execute();
      if (!oldData) {
        var center3 = seriesModel.get("center");
        this.group.scaleX = 0.01;
        this.group.scaleY = 0.01;
        this.group.originX = parsePercent2(center3[0], api.getWidth());
        this.group.originY = parsePercent2(center3[1], api.getHeight());
        initProps(this.group, {
          scaleX: 1,
          scaleY: 1
        }, seriesModel);
      }
      this._data = data;
      this.renderEdges(seriesModel, startAngle);
    };
    ChordView2.prototype.renderEdges = function(seriesModel, startAngle) {
      var nodeData = seriesModel.getData();
      var edgeData = seriesModel.getEdgeData();
      var oldData = this._edgeData;
      var group2 = this.group;
      edgeData.diff(oldData).add(function(newIdx) {
        var el = new ChordEdge(nodeData, edgeData, newIdx, startAngle);
        getECData(el).dataIndex = newIdx;
        group2.add(el);
      }).update(function(newIdx, oldIdx) {
        var el = oldData.getItemGraphicEl(oldIdx);
        el.updateData(nodeData, edgeData, newIdx, startAngle);
        group2.add(el);
      }).remove(function(oldIdx) {
        var el = oldData.getItemGraphicEl(oldIdx);
        el && removeElementWithFadeOut(el, seriesModel, oldIdx);
      }).execute();
      this._edgeData = edgeData;
    };
    ChordView2.prototype.dispose = function() {
    };
    ChordView2.type = SERIES_TYPE_CHORD;
    return ChordView2;
  })(Chart_default)
);

// node_modules/echarts/lib/chart/chord/chordLayout.js
var RADIAN4 = Math.PI / 180;
var chordCircularLayoutStageHandler = createSimpleOverallStageHandler(SERIES_TYPE_CHORD, chordCircularLayout);
function chordCircularLayout(ecModel, api) {
  ecModel.eachSeriesByType(SERIES_TYPE_CHORD, function(seriesModel) {
    chordLayout(seriesModel, api);
  });
}
function chordLayout(seriesModel, api) {
  var nodeData = seriesModel.getData();
  var nodeGraph = nodeData.graph;
  var edgeData = seriesModel.getEdgeData();
  var edgeCount = edgeData.count();
  if (!edgeCount) {
    return;
  }
  var _a2 = getCircleLayout(seriesModel, api), cx = _a2.cx, cy = _a2.cy, r = _a2.r, r0 = _a2.r0;
  var padAngle = Math.max((seriesModel.get("padAngle") || 0) * RADIAN4, 0);
  var minAngle = Math.max((seriesModel.get("minAngle") || 0) * RADIAN4, 0);
  var startAngle = -seriesModel.get("startAngle") * RADIAN4;
  var endAngle = startAngle + Math.PI * 2;
  var clockwise = seriesModel.get("clockwise");
  var dir3 = clockwise ? 1 : -1;
  var angles = [startAngle, endAngle];
  normalizeArcAngles(angles, !clockwise);
  var normalizedStartAngle = angles[0], normalizedEndAngle = angles[1];
  var totalAngle = normalizedEndAngle - normalizedStartAngle;
  var allZero = nodeData.getSum("value") === 0 && edgeData.getSum("value") === 0;
  var nodeValues = [];
  var renderedNodeCount = 0;
  nodeGraph.eachEdge(function(edge) {
    var value = allZero ? 1 : edge.getValue("value");
    if (allZero && (value > 0 || minAngle)) {
      renderedNodeCount += 2;
    }
    var node1Index = edge.node1.dataIndex;
    var node2Index = edge.node2.dataIndex;
    nodeValues[node1Index] = (nodeValues[node1Index] || 0) + value;
    nodeValues[node2Index] = (nodeValues[node2Index] || 0) + value;
  });
  var nodeValueSum = 0;
  nodeGraph.eachNode(function(node) {
    var dataValue = node.getValue("value");
    if (!isNaN(dataValue)) {
      nodeValues[node.dataIndex] = Math.max(dataValue, nodeValues[node.dataIndex] || 0);
    }
    if (!allZero && (nodeValues[node.dataIndex] > 0 || minAngle)) {
      renderedNodeCount++;
    }
    nodeValueSum += nodeValues[node.dataIndex] || 0;
  });
  if (renderedNodeCount === 0 || nodeValueSum === 0) {
    return;
  }
  if (padAngle * renderedNodeCount >= Math.abs(totalAngle)) {
    padAngle = Math.max(0, (Math.abs(totalAngle) - minAngle * renderedNodeCount) / renderedNodeCount);
  }
  if ((padAngle + minAngle) * renderedNodeCount >= Math.abs(totalAngle)) {
    minAngle = (Math.abs(totalAngle) - padAngle * renderedNodeCount) / renderedNodeCount;
  }
  var unitAngle = (totalAngle - padAngle * renderedNodeCount * dir3) / nodeValueSum;
  var totalDeficit = 0;
  var totalSurplus = 0;
  var totalSurplusSpan = 0;
  var minSurplus = Infinity;
  nodeGraph.eachNode(function(node) {
    var value = nodeValues[node.dataIndex] || 0;
    var spanAngle = unitAngle * (nodeValueSum ? value : 1) * dir3;
    if (Math.abs(spanAngle) < minAngle) {
      totalDeficit += minAngle - Math.abs(spanAngle);
    } else {
      minSurplus = Math.min(minSurplus, Math.abs(spanAngle) - minAngle);
      totalSurplus += Math.abs(spanAngle) - minAngle;
      totalSurplusSpan += Math.abs(spanAngle);
    }
    node.setLayout({
      angle: spanAngle,
      value
    });
  });
  var surplusAsMuchAsPossible = false;
  if (totalDeficit > totalSurplus) {
    var scale_1 = totalDeficit / totalSurplus;
    nodeGraph.eachNode(function(node) {
      var spanAngle = node.getLayout().angle;
      if (Math.abs(spanAngle) >= minAngle) {
        node.setLayout({
          angle: spanAngle * scale_1,
          ratio: scale_1
        }, true);
      } else {
        node.setLayout({
          angle: minAngle,
          ratio: minAngle === 0 ? 1 : spanAngle / minAngle
        }, true);
      }
    });
  } else {
    nodeGraph.eachNode(function(node) {
      if (surplusAsMuchAsPossible) {
        return;
      }
      var spanAngle = node.getLayout().angle;
      var borrowRatio = Math.min(spanAngle / totalSurplusSpan, 1);
      var borrowAngle = borrowRatio * totalDeficit;
      if (spanAngle - borrowAngle < minAngle) {
        surplusAsMuchAsPossible = true;
      }
    });
  }
  var restDeficit = totalDeficit;
  nodeGraph.eachNode(function(node) {
    if (restDeficit <= 0) {
      return;
    }
    var spanAngle = node.getLayout().angle;
    if (spanAngle > minAngle && minAngle > 0) {
      var borrowRatio = surplusAsMuchAsPossible ? 1 : Math.min(spanAngle / totalSurplusSpan, 1);
      var maxBorrowAngle = spanAngle - minAngle;
      var borrowAngle = Math.min(maxBorrowAngle, Math.min(restDeficit, totalDeficit * borrowRatio));
      restDeficit -= borrowAngle;
      node.setLayout({
        angle: spanAngle - borrowAngle,
        ratio: (spanAngle - borrowAngle) / spanAngle
      }, true);
    } else if (minAngle > 0) {
      node.setLayout({
        angle: minAngle,
        ratio: spanAngle === 0 ? 1 : minAngle / spanAngle
      }, true);
    }
  });
  var angle = normalizedStartAngle;
  var edgeAccAngle = [];
  nodeGraph.eachNode(function(node) {
    var spanAngle = Math.max(node.getLayout().angle, minAngle);
    node.setLayout({
      cx,
      cy,
      r0,
      r,
      startAngle: angle,
      endAngle: angle + spanAngle * dir3,
      clockwise
    }, true);
    edgeAccAngle[node.dataIndex] = angle;
    angle += (spanAngle + padAngle) * dir3;
  });
  nodeGraph.eachEdge(function(edge) {
    var value = allZero ? 1 : edge.getValue("value");
    var spanAngle = unitAngle * (nodeValueSum ? value : 1) * dir3;
    var node1Index = edge.node1.dataIndex;
    var sStartAngle = edgeAccAngle[node1Index] || 0;
    var sSpan = Math.abs((edge.node1.getLayout().ratio || 1) * spanAngle);
    var sEndAngle = sStartAngle + sSpan * dir3;
    var s1 = [cx + r0 * Math.cos(sStartAngle), cy + r0 * Math.sin(sStartAngle)];
    var s2 = [cx + r0 * Math.cos(sEndAngle), cy + r0 * Math.sin(sEndAngle)];
    var node2Index = edge.node2.dataIndex;
    var tStartAngle = edgeAccAngle[node2Index] || 0;
    var tSpan = Math.abs((edge.node2.getLayout().ratio || 1) * spanAngle);
    var tEndAngle = tStartAngle + tSpan * dir3;
    var t1 = [cx + r0 * Math.cos(tStartAngle), cy + r0 * Math.sin(tStartAngle)];
    var t2 = [cx + r0 * Math.cos(tEndAngle), cy + r0 * Math.sin(tEndAngle)];
    edge.setLayout({
      s1,
      s2,
      sStartAngle,
      sEndAngle,
      t1,
      t2,
      tStartAngle,
      tEndAngle,
      cx,
      cy,
      r: r0,
      value,
      clockwise
    });
    edgeAccAngle[node1Index] = sEndAngle;
    edgeAccAngle[node2Index] = tEndAngle;
  });
}

// node_modules/echarts/lib/chart/gauge/PointerPath.js
var PointerShape = (
  /** @class */
  /* @__PURE__ */ (function() {
    function PointerShape2() {
      this.angle = 0;
      this.width = 10;
      this.r = 10;
      this.x = 0;
      this.y = 0;
    }
    return PointerShape2;
  })()
);
var PointerPath = (
  /** @class */
  (function(_super) {
    __extends(PointerPath2, _super);
    function PointerPath2(opts) {
      var _this = _super.call(this, opts) || this;
      _this.type = "pointer";
      return _this;
    }
    PointerPath2.prototype.getDefaultShape = function() {
      return new PointerShape();
    };
    PointerPath2.prototype.buildPath = function(ctx, shape) {
      var mathCos6 = Math.cos;
      var mathSin6 = Math.sin;
      var r = shape.r;
      var width = shape.width;
      var angle = shape.angle;
      var x = shape.x - mathCos6(angle) * width * (width >= r / 3 ? 1 : 2);
      var y = shape.y - mathSin6(angle) * width * (width >= r / 3 ? 1 : 2);
      angle = shape.angle - Math.PI / 2;
      ctx.moveTo(x, y);
      ctx.lineTo(shape.x + mathCos6(angle) * width, shape.y + mathSin6(angle) * width);
      ctx.lineTo(shape.x + mathCos6(shape.angle) * r, shape.y + mathSin6(shape.angle) * r);
      ctx.lineTo(shape.x - mathCos6(angle) * width, shape.y - mathSin6(angle) * width);
      ctx.lineTo(x, y);
    };
    return PointerPath2;
  })(Path_default)
);
var PointerPath_default = PointerPath;

// node_modules/echarts/lib/chart/gauge/GaugeView.js
function parsePosition(seriesModel, api) {
  var center3 = seriesModel.get("center");
  var width = api.getWidth();
  var height = api.getHeight();
  var size = Math.min(width, height);
  var cx = parsePercent2(center3[0], api.getWidth());
  var cy = parsePercent2(center3[1], api.getHeight());
  var r = parsePercent2(seriesModel.get("radius"), size / 2);
  return {
    cx,
    cy,
    r
  };
}
function formatLabel(value, labelFormatter) {
  var label = value == null ? "" : value + "";
  if (labelFormatter) {
    if (isString(labelFormatter)) {
      label = labelFormatter.replace("{value}", label);
    } else if (isFunction(labelFormatter)) {
      label = labelFormatter(value);
    }
  }
  return label;
}
var GaugeView = (
  /** @class */
  (function(_super) {
    __extends(GaugeView2, _super);
    function GaugeView2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = GaugeView2.type;
      return _this;
    }
    GaugeView2.prototype.render = function(seriesModel, ecModel, api) {
      this.group.removeAll();
      var colorList = seriesModel.get(["axisLine", "lineStyle", "color"]);
      var posInfo = parsePosition(seriesModel, api);
      this._renderMain(seriesModel, ecModel, api, colorList, posInfo);
      this._data = seriesModel.getData();
    };
    GaugeView2.prototype.dispose = function() {
    };
    GaugeView2.prototype._renderMain = function(seriesModel, ecModel, api, colorList, posInfo) {
      var group2 = this.group;
      var clockwise = seriesModel.get("clockwise");
      var startAngle = -seriesModel.get("startAngle") / 180 * Math.PI;
      var endAngle = -seriesModel.get("endAngle") / 180 * Math.PI;
      var axisLineModel = seriesModel.getModel("axisLine");
      var roundCap = axisLineModel.get("roundCap");
      var MainPath = roundCap ? sausage_default : Sector_default;
      var showAxis = axisLineModel.get("show");
      var lineStyleModel = axisLineModel.getModel("lineStyle");
      var axisLineWidth = lineStyleModel.get("width");
      var angles = [startAngle, endAngle];
      normalizeArcAngles(angles, !clockwise);
      startAngle = angles[0];
      endAngle = angles[1];
      var angleRangeSpan = endAngle - startAngle;
      var prevEndAngle = startAngle;
      var sectors = [];
      for (var i2 = 0; showAxis && i2 < colorList.length; i2++) {
        var percent = Math.min(Math.max(colorList[i2][0], 0), 1);
        endAngle = startAngle + angleRangeSpan * percent;
        var sector = new MainPath({
          shape: {
            startAngle: prevEndAngle,
            endAngle,
            cx: posInfo.cx,
            cy: posInfo.cy,
            clockwise,
            r0: posInfo.r - axisLineWidth,
            r: posInfo.r
          },
          silent: true
        });
        sector.setStyle({
          fill: colorList[i2][1]
        });
        sector.setStyle(lineStyleModel.getLineStyle(
          // Because we use sector to simulate arc
          // so the properties for stroking are useless
          ["color", "width"]
        ));
        sectors.push(sector);
        prevEndAngle = endAngle;
      }
      sectors.reverse();
      each(sectors, function(sector2) {
        return group2.add(sector2);
      });
      var getColor2 = function(percent2) {
        if (percent2 <= 0) {
          return colorList[0][1];
        }
        var i3;
        for (i3 = 0; i3 < colorList.length; i3++) {
          if (colorList[i3][0] >= percent2 && (i3 === 0 ? 0 : colorList[i3 - 1][0]) < percent2) {
            return colorList[i3][1];
          }
        }
        return colorList[i3 - 1][1];
      };
      this._renderTicks(seriesModel, ecModel, api, getColor2, posInfo, startAngle, endAngle, clockwise, axisLineWidth);
      this._renderTitleAndDetail(seriesModel, ecModel, api, getColor2, posInfo);
      this._renderAnchor(seriesModel, posInfo);
      this._renderPointer(seriesModel, ecModel, api, getColor2, posInfo, startAngle, endAngle, clockwise, axisLineWidth);
    };
    GaugeView2.prototype._renderTicks = function(seriesModel, ecModel, api, getColor2, posInfo, startAngle, endAngle, clockwise, axisLineWidth) {
      var group2 = this.group;
      var cx = posInfo.cx;
      var cy = posInfo.cy;
      var r = posInfo.r;
      var minVal = +seriesModel.get("min");
      var maxVal = +seriesModel.get("max");
      var splitLineModel = seriesModel.getModel("splitLine");
      var tickModel = seriesModel.getModel("axisTick");
      var labelModel = seriesModel.getModel("axisLabel");
      var splitNumber = seriesModel.get("splitNumber");
      var subSplitNumber = tickModel.get("splitNumber");
      var splitLineLen = parsePercent2(splitLineModel.get("length"), r);
      var tickLen = parsePercent2(tickModel.get("length"), r);
      var angle = startAngle;
      var step = (endAngle - startAngle) / splitNumber;
      var subStep = step / subSplitNumber;
      var splitLineStyle = splitLineModel.getModel("lineStyle").getLineStyle();
      var tickLineStyle = tickModel.getModel("lineStyle").getLineStyle();
      var splitLineDistance = splitLineModel.get("distance");
      var unitX;
      var unitY;
      for (var i2 = 0; i2 <= splitNumber; i2++) {
        unitX = Math.cos(angle);
        unitY = Math.sin(angle);
        if (splitLineModel.get("show")) {
          var distance2 = splitLineDistance ? splitLineDistance + axisLineWidth : axisLineWidth;
          var splitLine = new Line_default({
            shape: {
              x1: unitX * (r - distance2) + cx,
              y1: unitY * (r - distance2) + cy,
              x2: unitX * (r - splitLineLen - distance2) + cx,
              y2: unitY * (r - splitLineLen - distance2) + cy
            },
            style: splitLineStyle,
            silent: true
          });
          if (splitLineStyle.stroke === "auto") {
            splitLine.setStyle({
              stroke: getColor2(i2 / splitNumber)
            });
          }
          group2.add(splitLine);
        }
        if (labelModel.get("show")) {
          var distance2 = labelModel.get("distance") + splitLineDistance;
          var label = formatLabel(
            // multiply firstly to avoid rounding error.
            round2(i2 * (maxVal - minVal) / splitNumber + minVal, DEFAULT_PRECISION_FOR_ROUNDING_ERROR),
            labelModel.get("formatter")
          );
          var autoColor = getColor2(i2 / splitNumber);
          var textStyleX = unitX * (r - splitLineLen - distance2) + cx;
          var textStyleY = unitY * (r - splitLineLen - distance2) + cy;
          var rotateType = labelModel.get("rotate");
          var rotate2 = 0;
          if (rotateType === "radial") {
            rotate2 = -angle + 2 * Math.PI;
            if (rotate2 > Math.PI / 2) {
              rotate2 += Math.PI;
            }
          } else if (rotateType === "tangential") {
            rotate2 = -angle - Math.PI / 2;
          } else if (isNumber(rotateType)) {
            rotate2 = rotateType * Math.PI / 180;
          }
          if (rotate2 === 0) {
            group2.add(new Text_default({
              style: createTextStyle(labelModel, {
                text: label,
                x: textStyleX,
                y: textStyleY,
                verticalAlign: unitY < -0.8 ? "top" : unitY > 0.8 ? "bottom" : "middle",
                align: unitX < -0.4 ? "left" : unitX > 0.4 ? "right" : "center"
              }, {
                inheritColor: autoColor
              }),
              silent: true
            }));
          } else {
            group2.add(new Text_default({
              style: createTextStyle(labelModel, {
                text: label,
                x: textStyleX,
                y: textStyleY,
                verticalAlign: "middle",
                align: "center"
              }, {
                inheritColor: autoColor
              }),
              silent: true,
              originX: textStyleX,
              originY: textStyleY,
              rotation: rotate2
            }));
          }
        }
        if (tickModel.get("show") && i2 !== splitNumber) {
          var distance2 = tickModel.get("distance");
          distance2 = distance2 ? distance2 + axisLineWidth : axisLineWidth;
          for (var j = 0; j <= subSplitNumber; j++) {
            unitX = Math.cos(angle);
            unitY = Math.sin(angle);
            var tickLine = new Line_default({
              shape: {
                x1: unitX * (r - distance2) + cx,
                y1: unitY * (r - distance2) + cy,
                x2: unitX * (r - tickLen - distance2) + cx,
                y2: unitY * (r - tickLen - distance2) + cy
              },
              silent: true,
              style: tickLineStyle
            });
            if (tickLineStyle.stroke === "auto") {
              tickLine.setStyle({
                stroke: getColor2((i2 + j / subSplitNumber) / splitNumber)
              });
            }
            group2.add(tickLine);
            angle += subStep;
          }
          angle -= subStep;
        } else {
          angle += step;
        }
      }
    };
    GaugeView2.prototype._renderPointer = function(seriesModel, ecModel, api, getColor2, posInfo, startAngle, endAngle, clockwise, axisLineWidth) {
      var group2 = this.group;
      var oldData = this._data;
      var oldProgressData = this._progressEls;
      var progressList = [];
      var showPointer2 = seriesModel.get(["pointer", "show"]);
      var progressModel = seriesModel.getModel("progress");
      var showProgress = progressModel.get("show");
      var data = seriesModel.getData();
      var valueDim = data.mapDimension("value");
      var minVal = +seriesModel.get("min");
      var maxVal = +seriesModel.get("max");
      var valueExtent = [minVal, maxVal];
      var angleExtent = [startAngle, endAngle];
      function createPointer(idx, angle) {
        var itemModel = data.getItemModel(idx);
        var pointerModel = itemModel.getModel("pointer");
        var pointerWidth = parsePercent2(pointerModel.get("width"), posInfo.r);
        var pointerLength = parsePercent2(pointerModel.get("length"), posInfo.r);
        var pointerStr = seriesModel.get(["pointer", "icon"]);
        var pointerOffset = pointerModel.get("offsetCenter");
        var pointerOffsetX = parsePercent2(pointerOffset[0], posInfo.r);
        var pointerOffsetY = parsePercent2(pointerOffset[1], posInfo.r);
        var pointerKeepAspect = pointerModel.get("keepAspect");
        var pointer;
        if (pointerStr) {
          pointer = createSymbol(pointerStr, pointerOffsetX - pointerWidth / 2, pointerOffsetY - pointerLength, pointerWidth, pointerLength, null, pointerKeepAspect);
        } else {
          pointer = new PointerPath_default({
            shape: {
              angle: -Math.PI / 2,
              width: pointerWidth,
              r: pointerLength,
              x: pointerOffsetX,
              y: pointerOffsetY
            }
          });
        }
        pointer.rotation = -(angle + Math.PI / 2);
        pointer.x = posInfo.cx;
        pointer.y = posInfo.cy;
        return pointer;
      }
      function createProgress(idx, endAngle2) {
        var roundCap = progressModel.get("roundCap");
        var ProgressPath = roundCap ? sausage_default : Sector_default;
        var isOverlap = progressModel.get("overlap");
        var progressWidth = isOverlap ? progressModel.get("width") : axisLineWidth / data.count();
        var r0 = isOverlap ? posInfo.r - progressWidth : posInfo.r - (idx + 1) * progressWidth;
        var r = isOverlap ? posInfo.r : posInfo.r - idx * progressWidth;
        var progress = new ProgressPath({
          shape: {
            startAngle,
            endAngle: endAngle2,
            cx: posInfo.cx,
            cy: posInfo.cy,
            clockwise,
            r0,
            r
          }
        });
        isOverlap && (progress.z2 = linearMap(data.get(valueDim, idx), [minVal, maxVal], [100, 0], true));
        return progress;
      }
      if (showProgress || showPointer2) {
        data.diff(oldData).add(function(idx) {
          var val = data.get(valueDim, idx);
          if (showPointer2) {
            var pointer = createPointer(idx, startAngle);
            initProps(pointer, {
              rotation: -((isNaN(+val) ? angleExtent[0] : linearMap(val, valueExtent, angleExtent, true)) + Math.PI / 2)
            }, seriesModel);
            group2.add(pointer);
            data.setItemGraphicEl(idx, pointer);
          }
          if (showProgress) {
            var progress = createProgress(idx, startAngle);
            var isClip = progressModel.get("clip");
            initProps(progress, {
              shape: {
                endAngle: linearMap(val, valueExtent, angleExtent, isClip)
              }
            }, seriesModel);
            group2.add(progress);
            setCommonECData(seriesModel.seriesIndex, data.dataType, idx, progress);
            progressList[idx] = progress;
          }
        }).update(function(newIdx, oldIdx) {
          var val = data.get(valueDim, newIdx);
          if (showPointer2) {
            var previousPointer = oldData.getItemGraphicEl(oldIdx);
            var previousRotate = previousPointer ? previousPointer.rotation : startAngle;
            var pointer = createPointer(newIdx, previousRotate);
            pointer.rotation = previousRotate;
            updateProps(pointer, {
              rotation: -((isNaN(+val) ? angleExtent[0] : linearMap(val, valueExtent, angleExtent, true)) + Math.PI / 2)
            }, seriesModel);
            group2.add(pointer);
            data.setItemGraphicEl(newIdx, pointer);
          }
          if (showProgress) {
            var previousProgress = oldProgressData[oldIdx];
            var previousEndAngle = previousProgress ? previousProgress.shape.endAngle : startAngle;
            var progress = createProgress(newIdx, previousEndAngle);
            var isClip = progressModel.get("clip");
            updateProps(progress, {
              shape: {
                endAngle: linearMap(val, valueExtent, angleExtent, isClip)
              }
            }, seriesModel);
            group2.add(progress);
            setCommonECData(seriesModel.seriesIndex, data.dataType, newIdx, progress);
            progressList[newIdx] = progress;
          }
        }).execute();
        data.each(function(idx) {
          var itemModel = data.getItemModel(idx);
          var emphasisModel = itemModel.getModel("emphasis");
          var focus = emphasisModel.get("focus");
          var blurScope = emphasisModel.get("blurScope");
          var emphasisDisabled = emphasisModel.get("disabled");
          var autoColor = getColor2(linearMap(data.get(valueDim, idx), valueExtent, [0, 1], true));
          if (showPointer2) {
            var pointer = data.getItemGraphicEl(idx);
            var symbolStyle = data.getItemVisual(idx, "style");
            var visualColor = symbolStyle.fill;
            if (pointer instanceof Image_default) {
              var pathStyle = pointer.style;
              pointer.useStyle(extend({
                image: pathStyle.image,
                x: pathStyle.x,
                y: pathStyle.y,
                width: pathStyle.width,
                height: pathStyle.height
              }, symbolStyle));
            } else {
              pointer.useStyle(symbolStyle);
              pointer.type !== "pointer" && pointer.setColor(visualColor);
            }
            pointer.setStyle(itemModel.getModel(["pointer", "itemStyle"]).getItemStyle());
            if (pointer.style.fill === "auto") {
              pointer.setStyle("fill", autoColor);
            }
            pointer.z2EmphasisLift = 0;
            setStatesStylesFromModel(pointer, itemModel);
            toggleHoverEmphasis(pointer, focus, blurScope, emphasisDisabled);
          }
          if (showProgress) {
            var progress = progressList[idx];
            progress.useStyle(data.getItemVisual(idx, "style"));
            progress.setStyle(itemModel.getModel(["progress", "itemStyle"]).getItemStyle());
            if (progress.style.fill === "auto") {
              progress.setStyle("fill", autoColor);
            }
            progress.z2EmphasisLift = 0;
            setStatesStylesFromModel(progress, itemModel);
            toggleHoverEmphasis(progress, focus, blurScope, emphasisDisabled);
          }
        });
        this._progressEls = progressList;
      }
    };
    GaugeView2.prototype._renderAnchor = function(seriesModel, posInfo) {
      var anchorModel = seriesModel.getModel("anchor");
      var showAnchor = anchorModel.get("show");
      if (showAnchor) {
        var anchorSize = anchorModel.get("size");
        var anchorType = anchorModel.get("icon");
        var offsetCenter = anchorModel.get("offsetCenter");
        var anchorKeepAspect = anchorModel.get("keepAspect");
        var anchor = createSymbol(anchorType, posInfo.cx - anchorSize / 2 + parsePercent2(offsetCenter[0], posInfo.r), posInfo.cy - anchorSize / 2 + parsePercent2(offsetCenter[1], posInfo.r), anchorSize, anchorSize, null, anchorKeepAspect);
        anchor.z2 = anchorModel.get("showAbove") ? 1 : 0;
        anchor.setStyle(anchorModel.getModel("itemStyle").getItemStyle());
        this.group.add(anchor);
      }
    };
    GaugeView2.prototype._renderTitleAndDetail = function(seriesModel, ecModel, api, getColor2, posInfo) {
      var _this = this;
      var data = seriesModel.getData();
      var valueDim = data.mapDimension("value");
      var minVal = +seriesModel.get("min");
      var maxVal = +seriesModel.get("max");
      var contentGroup = new Group_default();
      var newTitleEls = [];
      var newDetailEls = [];
      var hasAnimation = seriesModel.isAnimationEnabled();
      var showPointerAbove = seriesModel.get(["pointer", "showAbove"]);
      data.diff(this._data).add(function(idx) {
        newTitleEls[idx] = new Text_default({
          silent: true
        });
        newDetailEls[idx] = new Text_default({
          silent: true
        });
      }).update(function(idx, oldIdx) {
        newTitleEls[idx] = _this._titleEls[oldIdx];
        newDetailEls[idx] = _this._detailEls[oldIdx];
      }).execute();
      data.each(function(idx) {
        var itemModel = data.getItemModel(idx);
        var value = data.get(valueDim, idx);
        var itemGroup = new Group_default();
        var autoColor = getColor2(linearMap(value, [minVal, maxVal], [0, 1], true));
        var itemTitleModel = itemModel.getModel("title");
        if (itemTitleModel.get("show")) {
          var titleOffsetCenter = itemTitleModel.get("offsetCenter");
          var titleX = posInfo.cx + parsePercent2(titleOffsetCenter[0], posInfo.r);
          var titleY = posInfo.cy + parsePercent2(titleOffsetCenter[1], posInfo.r);
          var labelEl = newTitleEls[idx];
          labelEl.attr({
            z2: showPointerAbove ? 0 : 2,
            style: createTextStyle(itemTitleModel, {
              x: titleX,
              y: titleY,
              text: data.getName(idx),
              align: "center",
              verticalAlign: "middle"
            }, {
              inheritColor: autoColor
            })
          });
          itemGroup.add(labelEl);
        }
        var itemDetailModel = itemModel.getModel("detail");
        if (itemDetailModel.get("show")) {
          var detailOffsetCenter = itemDetailModel.get("offsetCenter");
          var detailX = posInfo.cx + parsePercent2(detailOffsetCenter[0], posInfo.r);
          var detailY = posInfo.cy + parsePercent2(detailOffsetCenter[1], posInfo.r);
          var width = parsePercent2(itemDetailModel.get("width"), posInfo.r);
          var height = parsePercent2(itemDetailModel.get("height"), posInfo.r);
          var detailColor = seriesModel.get(["progress", "show"]) ? data.getItemVisual(idx, "style").fill : autoColor;
          var labelEl = newDetailEls[idx];
          var formatter_1 = itemDetailModel.get("formatter");
          labelEl.attr({
            z2: showPointerAbove ? 0 : 2,
            style: createTextStyle(itemDetailModel, {
              x: detailX,
              y: detailY,
              text: formatLabel(value, formatter_1),
              width: isNaN(width) ? null : width,
              height: isNaN(height) ? null : height,
              align: "center",
              verticalAlign: "middle"
            }, {
              inheritColor: detailColor
            })
          });
          setLabelValueAnimation(labelEl, {
            normal: itemDetailModel
          }, value, function(value2) {
            return formatLabel(value2, formatter_1);
          });
          hasAnimation && animateLabelValue(labelEl, idx, data, seriesModel, {
            getFormattedLabel: function(labelDataIndex, status, dataType, labelDimIndex, fmt, extendParams) {
              return formatLabel(extendParams ? extendParams.interpolatedValue : value, formatter_1);
            }
          });
          itemGroup.add(labelEl);
        }
        contentGroup.add(itemGroup);
      });
      this.group.add(contentGroup);
      this._titleEls = newTitleEls;
      this._detailEls = newDetailEls;
    };
    GaugeView2.type = "gauge";
    return GaugeView2;
  })(Chart_default)
);
var GaugeView_default = GaugeView;

// node_modules/echarts/lib/chart/gauge/GaugeSeries.js
var GaugeSeriesModel = (
  /** @class */
  (function(_super) {
    __extends(GaugeSeriesModel2, _super);
    function GaugeSeriesModel2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = GaugeSeriesModel2.type;
      _this.visualStyleAccessPath = "itemStyle";
      return _this;
    }
    GaugeSeriesModel2.prototype.getInitialData = function(option, ecModel) {
      return createSeriesDataSimply(this, ["value"]);
    };
    GaugeSeriesModel2.type = "series.gauge";
    GaugeSeriesModel2.defaultOption = {
      // zlevel: 0,
      z: 2,
      colorBy: "data",
      // 默认全局居中
      center: ["50%", "50%"],
      legendHoverLink: true,
      radius: "75%",
      startAngle: 225,
      endAngle: -45,
      clockwise: true,
      // 最小值
      min: 0,
      // 最大值
      max: 100,
      // 分割段数，默认为10
      splitNumber: 10,
      // 坐标轴线
      axisLine: {
        // 默认显示，属性show控制显示与否
        show: true,
        roundCap: false,
        lineStyle: {
          color: [[1, tokens_default.color.neutral10]],
          width: 10
        }
      },
      // 坐标轴线
      progress: {
        // 默认显示，属性show控制显示与否
        show: false,
        overlap: true,
        width: 10,
        roundCap: false,
        clip: true
      },
      // 分隔线
      splitLine: {
        // 默认显示，属性show控制显示与否
        show: true,
        // 属性length控制线长
        length: 10,
        distance: 10,
        // 属性lineStyle（详见lineStyle）控制线条样式
        lineStyle: {
          color: tokens_default.color.axisTick,
          width: 3,
          type: "solid"
        }
      },
      // 坐标轴小标记
      axisTick: {
        // 属性show控制显示与否，默认不显示
        show: true,
        // 每份split细分多少段
        splitNumber: 5,
        // 属性length控制线长
        length: 6,
        distance: 10,
        // 属性lineStyle控制线条样式
        lineStyle: {
          color: tokens_default.color.axisTickMinor,
          width: 1,
          type: "solid"
        }
      },
      axisLabel: {
        show: true,
        distance: 15,
        // formatter: null,
        color: tokens_default.color.axisLabel,
        fontSize: 12,
        rotate: 0
      },
      pointer: {
        icon: null,
        offsetCenter: [0, 0],
        show: true,
        showAbove: true,
        length: "60%",
        width: 6,
        keepAspect: false
      },
      anchor: {
        show: false,
        showAbove: false,
        size: 6,
        icon: "circle",
        offsetCenter: [0, 0],
        keepAspect: false,
        itemStyle: {
          color: tokens_default.color.neutral00,
          borderWidth: 0,
          borderColor: tokens_default.color.theme[0]
        }
      },
      title: {
        show: true,
        // x, y，单位px
        offsetCenter: [0, "20%"],
        // 其余属性默认使用全局文本样式，详见TEXTSTYLE
        color: tokens_default.color.secondary,
        fontSize: 16,
        valueAnimation: false
      },
      detail: {
        show: true,
        backgroundColor: tokens_default.color.transparent,
        borderWidth: 0,
        borderColor: tokens_default.color.neutral40,
        width: 100,
        height: null,
        padding: [5, 10],
        // x, y，单位px
        offsetCenter: [0, "40%"],
        // formatter: null,
        // 其余属性默认使用全局文本样式，详见TEXTSTYLE
        color: tokens_default.color.primary,
        fontSize: 30,
        fontWeight: "bold",
        lineHeight: 30,
        valueAnimation: false
      }
    };
    return GaugeSeriesModel2;
  })(Series_default)
);
var GaugeSeries_default = GaugeSeriesModel;

// node_modules/echarts/lib/chart/gauge/install.js
function install8(registers) {
  registers.registerChartView(GaugeView_default);
  registers.registerSeriesModel(GaugeSeries_default);
}

// node_modules/echarts/lib/chart/funnel/FunnelSeries.js
var SERIES_TYPE_FUNNEL = "funnel";
var FunnelSeriesModel = (
  /** @class */
  (function(_super) {
    __extends(FunnelSeriesModel2, _super);
    function FunnelSeriesModel2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = FunnelSeriesModel2.type;
      return _this;
    }
    FunnelSeriesModel2.prototype.init = function(option) {
      _super.prototype.init.apply(this, arguments);
      this.legendVisualProvider = new LegendVisualProvider_default(bind(this.getData, this), bind(this.getRawData, this));
      this._defaultLabelLine(option);
    };
    FunnelSeriesModel2.prototype.getInitialData = function(option, ecModel) {
      return createSeriesDataSimply(this, {
        coordDimensions: ["value"],
        encodeDefaulter: curry(makeSeriesEncodeForNameBased, this)
      });
    };
    FunnelSeriesModel2.prototype._defaultLabelLine = function(option) {
      defaultEmphasis(option, "labelLine", ["show"]);
      var labelLineNormalOpt = option.labelLine;
      var labelLineEmphasisOpt = option.emphasis.labelLine;
      labelLineNormalOpt.show = labelLineNormalOpt.show && option.label.show;
      labelLineEmphasisOpt.show = labelLineEmphasisOpt.show && option.emphasis.label.show;
    };
    FunnelSeriesModel2.prototype.getDataParams = function(dataIndex) {
      var data = this.getData();
      var params = _super.prototype.getDataParams.call(this, dataIndex);
      var valueDim = data.mapDimension("value");
      var sum2 = data.getSum(valueDim);
      params.percent = !sum2 ? 0 : +(data.get(valueDim, dataIndex) / sum2 * 100).toFixed(2);
      params.$vars.push("percent");
      return params;
    };
    FunnelSeriesModel2.type = "series." + SERIES_TYPE_FUNNEL;
    FunnelSeriesModel2.defaultOption = {
      coordinateSystemUsage: "box",
      // zlevel: 0,                  // 一级层叠
      z: 2,
      legendHoverLink: true,
      colorBy: "data",
      left: 80,
      top: 60,
      right: 80,
      bottom: 65,
      // width: {totalWidth} - left - right,
      // height: {totalHeight} - top - bottom,
      // 默认取数据最小最大值
      // min: 0,
      // max: 100,
      minSize: "0%",
      maxSize: "100%",
      sort: "descending",
      orient: "vertical",
      gap: 0,
      funnelAlign: "center",
      label: {
        show: true,
        position: "outer"
        // formatter: 标签文本格式器，同Tooltip.formatter，不支持异步回调
      },
      labelLine: {
        show: true,
        length: 20,
        lineStyle: {
          // color: 各异,
          width: 1
        }
      },
      itemStyle: {
        // color: 各异,
        borderColor: tokens_default.color.neutral00,
        borderWidth: 1
      },
      emphasis: {
        label: {
          show: true
        }
      },
      select: {
        itemStyle: {
          borderColor: tokens_default.color.primary
        }
      }
    };
    return FunnelSeriesModel2;
  })(Series_default)
);

// node_modules/echarts/lib/chart/funnel/FunnelView.js
var opacityAccessPath = ["itemStyle", "opacity"];
var FunnelPiece = (
  /** @class */
  (function(_super) {
    __extends(FunnelPiece2, _super);
    function FunnelPiece2(data, idx) {
      var _this = _super.call(this) || this;
      var polygon = _this;
      var labelLine = new Polyline_default();
      var text = new Text_default();
      polygon.setTextContent(text);
      _this.setTextGuideLine(labelLine);
      _this.updateData(data, idx, true);
      return _this;
    }
    FunnelPiece2.prototype.updateData = function(data, idx, firstCreate) {
      var polygon = this;
      var seriesModel = data.hostModel;
      var itemModel = data.getItemModel(idx);
      var layout3 = data.getItemLayout(idx);
      var emphasisModel = itemModel.getModel("emphasis");
      var opacity = itemModel.get(opacityAccessPath);
      opacity = opacity == null ? 1 : opacity;
      if (!firstCreate) {
        saveOldStyle(polygon);
      }
      polygon.useStyle(data.getItemVisual(idx, "style"));
      polygon.style.lineJoin = "round";
      if (firstCreate) {
        polygon.setShape({
          points: layout3.points
        });
        polygon.style.opacity = 0;
        initProps(polygon, {
          style: {
            opacity
          }
        }, seriesModel, idx);
      } else {
        updateProps(polygon, {
          style: {
            opacity
          },
          shape: {
            points: layout3.points
          }
        }, seriesModel, idx);
      }
      setStatesStylesFromModel(polygon, itemModel);
      this._updateLabel(data, idx);
      toggleHoverEmphasis(this, emphasisModel.get("focus"), emphasisModel.get("blurScope"), emphasisModel.get("disabled"));
    };
    FunnelPiece2.prototype._updateLabel = function(data, idx) {
      var polygon = this;
      var labelLine = this.getTextGuideLine();
      var labelText = polygon.getTextContent();
      var seriesModel = data.hostModel;
      var itemModel = data.getItemModel(idx);
      var layout3 = data.getItemLayout(idx);
      var labelLayout2 = layout3.label;
      var style2 = data.getItemVisual(idx, "style");
      var visualColor = style2.fill;
      setLabelStyle(
        // position will not be used in setLabelStyle
        labelText,
        getLabelStatesModels(itemModel),
        {
          labelFetcher: data.hostModel,
          labelDataIndex: idx,
          defaultOpacity: style2.opacity,
          defaultText: data.getName(idx)
        },
        {
          normal: {
            align: labelLayout2.textAlign,
            verticalAlign: labelLayout2.verticalAlign
          }
        }
      );
      var labelModel = itemModel.getModel("label");
      var labelColor = labelModel.get("color");
      var overrideColor = labelColor === "inherit" ? visualColor : null;
      polygon.setTextConfig({
        local: true,
        inside: !!labelLayout2.inside,
        insideStroke: overrideColor,
        outsideFill: overrideColor
      });
      var linePoints = labelLayout2.linePoints;
      labelLine.setShape({
        points: linePoints
      });
      polygon.textGuideLineConfig = {
        anchor: linePoints ? new Point_default(linePoints[0][0], linePoints[0][1]) : null
      };
      updateProps(labelText, {
        style: {
          x: labelLayout2.x,
          y: labelLayout2.y
        }
      }, seriesModel, idx);
      labelText.attr({
        rotation: labelLayout2.rotation,
        originX: labelLayout2.x,
        originY: labelLayout2.y,
        z2: 10
      });
      setLabelLineStyle(polygon, getLabelLineStatesModels(itemModel), {
        // Default use item visual color
        stroke: visualColor
      });
    };
    return FunnelPiece2;
  })(Polygon_default)
);
var FunnelView = (
  /** @class */
  (function(_super) {
    __extends(FunnelView2, _super);
    function FunnelView2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = SERIES_TYPE_FUNNEL;
      _this.ignoreLabelLineUpdate = true;
      return _this;
    }
    FunnelView2.prototype.render = function(seriesModel, ecModel, api) {
      var data = seriesModel.getData();
      var oldData = this._data;
      var group2 = this.group;
      data.diff(oldData).add(function(idx) {
        var funnelPiece = new FunnelPiece(data, idx);
        data.setItemGraphicEl(idx, funnelPiece);
        group2.add(funnelPiece);
      }).update(function(newIdx, oldIdx) {
        var piece = oldData.getItemGraphicEl(oldIdx);
        piece.updateData(data, newIdx);
        group2.add(piece);
        data.setItemGraphicEl(newIdx, piece);
      }).remove(function(idx) {
        var piece = oldData.getItemGraphicEl(idx);
        removeElementWithFadeOut(piece, seriesModel, idx);
      }).execute();
      this._data = data;
    };
    FunnelView2.prototype.remove = function() {
      this.group.removeAll();
      this._data = null;
    };
    FunnelView2.prototype.dispose = function() {
    };
    FunnelView2.type = SERIES_TYPE_FUNNEL;
    return FunnelView2;
  })(Chart_default)
);

// node_modules/echarts/lib/chart/funnel/funnelLayout.js
function getSortedIndices(data, sort3) {
  var valueDim = data.mapDimension("value");
  var valueArr = data.mapArray(valueDim, function(val) {
    return val;
  });
  var indices = [];
  var isAscending = sort3 === "ascending";
  for (var i2 = 0, len2 = data.count(); i2 < len2; i2++) {
    indices[i2] = i2;
  }
  if (isFunction(sort3)) {
    indices.sort(sort3);
  } else if (sort3 !== "none") {
    indices.sort(function(a, b) {
      return isAscending ? valueArr[a] - valueArr[b] : valueArr[b] - valueArr[a];
    });
  }
  return indices;
}
function labelLayout(data) {
  var seriesModel = data.hostModel;
  var isHorizontal = isOrientHorizontal(seriesModel);
  data.each(function(idx) {
    var itemModel = data.getItemModel(idx);
    var labelModel = itemModel.getModel("label");
    var labelPosition = labelModel.get("position");
    var labelLineModel = itemModel.getModel("labelLine");
    var layout3 = data.getItemLayout(idx);
    var points4 = layout3.points;
    var isLabelInside = labelPosition === "inner" || labelPosition === "inside" || labelPosition === "center" || labelPosition === "insideLeft" || labelPosition === "insideRight";
    var textAlign;
    var textX;
    var textY;
    var linePoints;
    if (isLabelInside) {
      if (labelPosition === "insideLeft") {
        textX = (points4[0][0] + points4[3][0]) / 2 + 5;
        textY = (points4[0][1] + points4[3][1]) / 2;
        textAlign = "left";
      } else if (labelPosition === "insideRight") {
        textX = (points4[1][0] + points4[2][0]) / 2 - 5;
        textY = (points4[1][1] + points4[2][1]) / 2;
        textAlign = "right";
      } else {
        textX = (points4[0][0] + points4[1][0] + points4[2][0] + points4[3][0]) / 4;
        textY = (points4[0][1] + points4[1][1] + points4[2][1] + points4[3][1]) / 4;
        textAlign = "center";
      }
      linePoints = [[textX, textY], [textX, textY]];
    } else {
      var x1 = void 0;
      var y1 = void 0;
      var x2 = void 0;
      var y2 = void 0;
      var labelLineLen = labelLineModel.get("length");
      if (isString(labelPosition)) {
        if (!isHorizontal && indexOf(["top", "bottom"], labelPosition) > -1) {
          labelPosition = "left";
          if (true) {
            console.warn("Position error: Funnel chart on vertical orient dose not support top and bottom.");
          }
        }
        if (isHorizontal && indexOf(["left", "right"], labelPosition) > -1) {
          labelPosition = "bottom";
          if (true) {
            console.warn("Position error: Funnel chart on horizontal orient dose not support left and right.");
          }
        }
      }
      if (labelPosition === "left") {
        x1 = (points4[3][0] + points4[0][0]) / 2;
        y1 = (points4[3][1] + points4[0][1]) / 2;
        x2 = x1 - labelLineLen;
        textX = x2 - 5;
        textAlign = "right";
      } else if (labelPosition === "right") {
        x1 = (points4[1][0] + points4[2][0]) / 2;
        y1 = (points4[1][1] + points4[2][1]) / 2;
        x2 = x1 + labelLineLen;
        textX = x2 + 5;
        textAlign = "left";
      } else if (labelPosition === "top") {
        x1 = (points4[3][0] + points4[0][0]) / 2;
        y1 = (points4[3][1] + points4[0][1]) / 2;
        y2 = y1 - labelLineLen;
        textY = y2 - 5;
        textAlign = "center";
      } else if (labelPosition === "bottom") {
        x1 = (points4[1][0] + points4[2][0]) / 2;
        y1 = (points4[1][1] + points4[2][1]) / 2;
        y2 = y1 + labelLineLen;
        textY = y2 + 5;
        textAlign = "center";
      } else if (labelPosition === "rightTop") {
        x1 = isHorizontal ? points4[3][0] : points4[1][0];
        y1 = isHorizontal ? points4[3][1] : points4[1][1];
        if (isHorizontal) {
          y2 = y1 - labelLineLen;
          textY = y2 - 5;
          textAlign = "center";
        } else {
          x2 = x1 + labelLineLen;
          textX = x2 + 5;
          textAlign = "top";
        }
      } else if (labelPosition === "rightBottom") {
        x1 = points4[2][0];
        y1 = points4[2][1];
        if (isHorizontal) {
          y2 = y1 + labelLineLen;
          textY = y2 + 5;
          textAlign = "center";
        } else {
          x2 = x1 + labelLineLen;
          textX = x2 + 5;
          textAlign = "bottom";
        }
      } else if (labelPosition === "leftTop") {
        x1 = points4[0][0];
        y1 = isHorizontal ? points4[0][1] : points4[1][1];
        if (isHorizontal) {
          y2 = y1 - labelLineLen;
          textY = y2 - 5;
          textAlign = "center";
        } else {
          x2 = x1 - labelLineLen;
          textX = x2 - 5;
          textAlign = "right";
        }
      } else if (labelPosition === "leftBottom") {
        x1 = isHorizontal ? points4[1][0] : points4[3][0];
        y1 = isHorizontal ? points4[1][1] : points4[2][1];
        if (isHorizontal) {
          y2 = y1 + labelLineLen;
          textY = y2 + 5;
          textAlign = "center";
        } else {
          x2 = x1 - labelLineLen;
          textX = x2 - 5;
          textAlign = "right";
        }
      } else {
        x1 = (points4[1][0] + points4[2][0]) / 2;
        y1 = (points4[1][1] + points4[2][1]) / 2;
        if (isHorizontal) {
          y2 = y1 + labelLineLen;
          textY = y2 + 5;
          textAlign = "center";
        } else {
          x2 = x1 + labelLineLen;
          textX = x2 + 5;
          textAlign = "left";
        }
      }
      if (isHorizontal) {
        x2 = x1;
        textX = x2;
      } else {
        y2 = y1;
        textY = y2;
      }
      linePoints = [[x1, y1], [x2, y2]];
    }
    layout3.label = {
      linePoints,
      x: textX,
      y: textY,
      verticalAlign: "middle",
      textAlign,
      inside: isLabelInside
    };
  });
}
var funnelLayoutStageHandler = createSimpleOverallStageHandler(SERIES_TYPE_FUNNEL, funnelLayout);
function funnelLayout(ecModel, api) {
  ecModel.eachSeriesByType(SERIES_TYPE_FUNNEL, function(seriesModel) {
    var data = seriesModel.getData();
    var valueDim = data.mapDimension("value");
    var sort3 = seriesModel.get("sort");
    var layoutRef = createBoxLayoutReference(seriesModel, api);
    var viewRect2 = getLayoutRect(seriesModel.getBoxLayoutParams(), layoutRef.refContainer);
    var isHorizontal = isOrientHorizontal(seriesModel);
    var viewWidth = viewRect2.width;
    var viewHeight = viewRect2.height;
    var indices = getSortedIndices(data, sort3);
    var x = viewRect2.x;
    var y = viewRect2.y;
    var sizeExtent = isHorizontal ? [parsePercent2(seriesModel.get("minSize"), viewHeight), parsePercent2(seriesModel.get("maxSize"), viewHeight)] : [parsePercent2(seriesModel.get("minSize"), viewWidth), parsePercent2(seriesModel.get("maxSize"), viewWidth)];
    var dataExtent = data.getDataExtent(valueDim);
    var min3 = seriesModel.get("min");
    var max3 = seriesModel.get("max");
    if (min3 == null) {
      min3 = Math.min(dataExtent[0], 0);
    }
    if (max3 == null) {
      max3 = dataExtent[1];
    }
    var funnelAlign = seriesModel.get("funnelAlign");
    var gap = seriesModel.get("gap");
    var viewSize = isHorizontal ? viewWidth : viewHeight;
    var itemSize = (viewSize - gap * (data.count() - 1)) / data.count();
    var getLinePoints = function(idx2, offset) {
      if (isHorizontal) {
        var val_1 = data.get(valueDim, idx2) || 0;
        var itemHeight = linearMap(val_1, [min3, max3], sizeExtent, true);
        var y0 = void 0;
        switch (funnelAlign) {
          case "top":
            y0 = y;
            break;
          case "center":
            y0 = y + (viewHeight - itemHeight) / 2;
            break;
          case "bottom":
            y0 = y + (viewHeight - itemHeight);
            break;
        }
        return [[offset, y0], [offset, y0 + itemHeight]];
      }
      var val = data.get(valueDim, idx2) || 0;
      var itemWidth = linearMap(val, [min3, max3], sizeExtent, true);
      var x0;
      switch (funnelAlign) {
        case "left":
          x0 = x;
          break;
        case "center":
          x0 = x + (viewWidth - itemWidth) / 2;
          break;
        case "right":
          x0 = x + viewWidth - itemWidth;
          break;
      }
      return [[x0, offset], [x0 + itemWidth, offset]];
    };
    if (sort3 === "ascending") {
      itemSize = -itemSize;
      gap = -gap;
      if (isHorizontal) {
        x += viewWidth;
      } else {
        y += viewHeight;
      }
      indices = indices.reverse();
    }
    for (var i2 = 0; i2 < indices.length; i2++) {
      var idx = indices[i2];
      var nextIdx = indices[i2 + 1];
      var itemModel = data.getItemModel(idx);
      if (isHorizontal) {
        var width = itemModel.get(["itemStyle", "width"]);
        if (width == null) {
          width = itemSize;
        } else {
          width = parsePercent2(width, viewWidth);
          if (sort3 === "ascending") {
            width = -width;
          }
        }
        var start2 = getLinePoints(idx, x);
        var end2 = getLinePoints(nextIdx, x + width);
        x += width + gap;
        data.setItemLayout(idx, {
          points: start2.concat(end2.slice().reverse())
        });
      } else {
        var height = itemModel.get(["itemStyle", "height"]);
        if (height == null) {
          height = itemSize;
        } else {
          height = parsePercent2(height, viewHeight);
          if (sort3 === "ascending") {
            height = -height;
          }
        }
        var start2 = getLinePoints(idx, y);
        var end2 = getLinePoints(nextIdx, y + height);
        y += height + gap;
        data.setItemLayout(idx, {
          points: start2.concat(end2.slice().reverse())
        });
      }
    }
    labelLayout(data);
  });
}
function isOrientHorizontal(seriesModel) {
  return seriesModel.get("orient") === "horizontal";
}

// node_modules/echarts/lib/chart/parallel/ParallelView.js
var DEFAULT_SMOOTH = 0.3;
var ParallelView = (
  /** @class */
  (function(_super) {
    __extends(ParallelView3, _super);
    function ParallelView3() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = ParallelView3.type;
      _this._dataGroup = new Group_default();
      _this._initialized = false;
      return _this;
    }
    ParallelView3.prototype.init = function() {
      this.group.add(this._dataGroup);
    };
    ParallelView3.prototype.render = function(seriesModel, ecModel, api, payload) {
      var dataGroup = this._dataGroup;
      this._progressiveEls = [];
      dataGroup.removeAll();
      var data = seriesModel.getData();
      var oldData = this._data;
      var coordSys = seriesModel.coordinateSystem;
      var dimensions = coordSys.dimensions;
      var seriesScope = makeSeriesScope3(seriesModel);
      data.diff(oldData).add(add2).update(update).remove(remove).execute();
      function add2(newDataIndex) {
        var line = addEl(data, dataGroup, newDataIndex, dimensions, coordSys);
        updateElCommon(line, data, newDataIndex, seriesScope);
      }
      function update(newDataIndex, oldDataIndex) {
        var line = oldData.getItemGraphicEl(oldDataIndex);
        dataGroup.add(line);
        var points4 = createLinePoints(data, newDataIndex, dimensions, coordSys);
        data.setItemGraphicEl(newDataIndex, line);
        updateProps(line, {
          shape: {
            points: points4
          }
        }, seriesModel, newDataIndex);
        saveOldStyle(line);
        updateElCommon(line, data, newDataIndex, seriesScope);
      }
      function remove(oldDataIndex) {
        var line = oldData.getItemGraphicEl(oldDataIndex);
        dataGroup.remove(line);
      }
      if (!this._initialized) {
        this._initialized = true;
        var clipPath = createGridClipShape(coordSys, seriesModel, function() {
          setTimeout(function() {
            dataGroup.removeClipPath();
          });
        });
        dataGroup.setClipPath(clipPath);
      }
      this._data = data;
    };
    ParallelView3.prototype.incrementalPrepareRender = function(seriesModel, ecModel, api) {
      this._initialized = true;
      this._data = null;
      this._dataGroup.removeAll();
    };
    ParallelView3.prototype.incrementalRender = function(taskParams, seriesModel, ecModel) {
      var data = seriesModel.getData();
      var coordSys = seriesModel.coordinateSystem;
      var progressiveEls = this._progressiveEls = [];
      for (var dataIndex = taskParams.start; dataIndex < taskParams.end; dataIndex++) {
        var line = addEl(data, this._dataGroup, dataIndex, coordSys.dimensions, coordSys);
        line.incremental = getIncrementalId(seriesModel);
        updateElCommon(line, data, dataIndex, makeSeriesScope3(seriesModel));
        progressiveEls.push(line);
      }
    };
    ParallelView3.prototype.remove = function() {
      this._dataGroup && this._dataGroup.removeAll();
      this._data = null;
    };
    ParallelView3.type = "parallel";
    return ParallelView3;
  })(Chart_default)
);
function createGridClipShape(coordSys, seriesModel, cb) {
  var parallelModel = coordSys.model;
  var rect = coordSys.getRect();
  var rectEl = new Rect_default({
    shape: {
      x: rect.x,
      y: rect.y,
      width: rect.width,
      height: rect.height
    }
  });
  var dim = parallelModel.get("layout") === "horizontal" ? "width" : "height";
  rectEl.setShape(dim, 0);
  initProps(rectEl, {
    shape: {
      width: rect.width,
      height: rect.height
    }
  }, seriesModel, cb);
  return rectEl;
}
function createLinePoints(data, dataIndex, dimensions, coordSys) {
  var points4 = [];
  for (var i2 = 0; i2 < dimensions.length; i2++) {
    var dimName = dimensions[i2];
    var value = data.get(data.mapDimension(dimName), dataIndex);
    if (!isEmptyValue(value, coordSys.getAxis(dimName).type)) {
      points4.push(coordSys.dataToPoint(value, dimName));
    }
  }
  return points4;
}
function addEl(data, dataGroup, dataIndex, dimensions, coordSys) {
  var points4 = createLinePoints(data, dataIndex, dimensions, coordSys);
  var line = new Polyline_default({
    shape: {
      points: points4
    },
    // silent: true,
    z2: 10
  });
  dataGroup.add(line);
  data.setItemGraphicEl(dataIndex, line);
  return line;
}
function makeSeriesScope3(seriesModel) {
  var smooth = seriesModel.get("smooth", true);
  smooth === true && (smooth = DEFAULT_SMOOTH);
  smooth = numericToNumber(smooth);
  eqNaN(smooth) && (smooth = 0);
  return {
    smooth
  };
}
function updateElCommon(el, data, dataIndex, seriesScope) {
  el.useStyle(data.getItemVisual(dataIndex, "style"));
  el.style.fill = null;
  el.setShape("smooth", seriesScope.smooth);
  var itemModel = data.getItemModel(dataIndex);
  var emphasisModel = itemModel.getModel("emphasis");
  setStatesStylesFromModel(el, itemModel, "lineStyle");
  toggleHoverEmphasis(el, emphasisModel.get("focus"), emphasisModel.get("blurScope"), emphasisModel.get("disabled"));
}
function isEmptyValue(val, axisType) {
  return axisType === "category" ? val == null : val == null || isNaN(val);
}

// node_modules/echarts/lib/chart/parallel/ParallelSeries.js
var ParallelSeriesModel = (
  /** @class */
  (function(_super) {
    __extends(ParallelSeriesModel2, _super);
    function ParallelSeriesModel2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = ParallelSeriesModel2.type;
      _this.visualStyleAccessPath = "lineStyle";
      _this.visualDrawType = "stroke";
      return _this;
    }
    ParallelSeriesModel2.prototype.getInitialData = function(option, ecModel) {
      return createSeriesData_default(null, this, {
        useEncodeDefaulter: bind(makeDefaultEncode, null, this)
      });
    };
    ParallelSeriesModel2.prototype.getRawIndicesByActiveState = function(activeState) {
      var coordSys = this.coordinateSystem;
      var data = this.getData();
      var indices = [];
      coordSys.eachActiveState(data, function(theActiveState, dataIndex) {
        if (activeState === theActiveState) {
          indices.push(data.getRawIndex(dataIndex));
        }
      });
      return indices;
    };
    ParallelSeriesModel2.type = "series.parallel";
    ParallelSeriesModel2.dependencies = ["parallel"];
    ParallelSeriesModel2.defaultOption = {
      // zlevel: 0,
      z: 2,
      coordinateSystem: "parallel",
      parallelIndex: 0,
      label: {
        show: false
      },
      inactiveOpacity: 0.05,
      activeOpacity: 1,
      lineStyle: {
        width: 1,
        opacity: 0.45,
        type: "solid"
      },
      emphasis: {
        label: {
          show: false
        }
      },
      progressive: 300,
      smooth: false,
      animationEasing: "linear"
    };
    return ParallelSeriesModel2;
  })(Series_default)
);
function makeDefaultEncode(seriesModel) {
  var parallelModel = seriesModel.ecModel.getComponent("parallel", seriesModel.get("parallelIndex"));
  if (!parallelModel) {
    return;
  }
  var encodeDefine = {};
  each(parallelModel.dimensions, function(axisDim) {
    var dataDimIndex = convertDimNameToNumber(axisDim);
    encodeDefine[axisDim] = dataDimIndex;
  });
  return encodeDefine;
}
function convertDimNameToNumber(dimName) {
  return +dimName.replace("dim", "");
}

// node_modules/echarts/lib/component/parallel/ParallelView.js
var CLICK_THRESHOLD = 5;
var ParallelView2 = (
  /** @class */
  (function(_super) {
    __extends(ParallelView3, _super);
    function ParallelView3() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = ParallelView3.type;
      return _this;
    }
    ParallelView3.prototype.render = function(parallelModel, ecModel, api) {
      this._model = parallelModel;
      this._api = api;
      if (!this._handlers) {
        this._handlers = {};
        each(handlers, function(handler, eventName) {
          api.getZr().on(eventName, this._handlers[eventName] = bind(handler, this));
        }, this);
      }
      createOrUpdate(this, "_throttledDispatchExpand", parallelModel.get("axisExpandRate"), "fixRate");
    };
    ParallelView3.prototype.dispose = function(ecModel, api) {
      clear(this, "_throttledDispatchExpand");
      each(this._handlers, function(handler, eventName) {
        api.getZr().off(eventName, handler);
      });
      this._handlers = null;
    };
    ParallelView3.prototype._throttledDispatchExpand = function(opt) {
      this._dispatchExpand(opt);
    };
    ParallelView3.prototype._dispatchExpand = function(opt) {
      opt && this._api.dispatchAction(extend({
        type: "parallelAxisExpand"
      }, opt));
    };
    ParallelView3.type = "parallel";
    return ParallelView3;
  })(Component_default2)
);
var handlers = {
  mousedown: function(e2) {
    if (checkTrigger(this, "click")) {
      this._mouseDownPoint = [e2.offsetX, e2.offsetY];
    }
  },
  mouseup: function(e2) {
    var mouseDownPoint = this._mouseDownPoint;
    if (checkTrigger(this, "click") && mouseDownPoint) {
      var point = [e2.offsetX, e2.offsetY];
      var dist3 = Math.pow(mouseDownPoint[0] - point[0], 2) + Math.pow(mouseDownPoint[1] - point[1], 2);
      if (dist3 > CLICK_THRESHOLD) {
        return;
      }
      var result = this._model.coordinateSystem.getSlidedAxisExpandWindow([e2.offsetX, e2.offsetY]);
      result.behavior !== "none" && this._dispatchExpand({
        axisExpandWindow: result.axisExpandWindow
      });
    }
    this._mouseDownPoint = null;
  },
  mousemove: function(e2) {
    if (this._mouseDownPoint || !checkTrigger(this, "mousemove")) {
      return;
    }
    var model2 = this._model;
    var result = model2.coordinateSystem.getSlidedAxisExpandWindow([e2.offsetX, e2.offsetY]);
    var behavior = result.behavior;
    behavior === "jump" && this._throttledDispatchExpand.debounceNextCall(model2.get("axisExpandDebounce"));
    this._throttledDispatchExpand(behavior === "none" ? null : {
      axisExpandWindow: result.axisExpandWindow,
      // Jumping uses animation, and sliding suppresses animation.
      animation: behavior === "jump" ? null : {
        duration: 0
        // Disable animation.
      }
    });
  }
};
function checkTrigger(view, triggerOn) {
  var model2 = view._model;
  return model2.get("axisExpandable") && model2.get("axisExpandTriggerOn") === triggerOn;
}

// node_modules/echarts/lib/coord/parallel/ParallelModel.js
var COORD_SYS_TYPE_PARALLEL = "parallel";
var COMPONENT_TYPE_PARALLEL = COORD_SYS_TYPE_PARALLEL;
var ParallelModel = (
  /** @class */
  (function(_super) {
    __extends(ParallelModel2, _super);
    function ParallelModel2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = ParallelModel2.type;
      return _this;
    }
    ParallelModel2.prototype.init = function() {
      _super.prototype.init.apply(this, arguments);
      this.mergeOption({});
    };
    ParallelModel2.prototype.mergeOption = function(newOption) {
      var thisOption = this.option;
      newOption && merge2(thisOption, newOption, true);
      this._initDimensions();
    };
    ParallelModel2.prototype.contains = function(model2, ecModel) {
      var parallelIndex = model2.get("parallelIndex");
      return parallelIndex != null && ecModel.getComponent("parallel", parallelIndex) === this;
    };
    ParallelModel2.prototype.setAxisExpand = function(opt) {
      each(["axisExpandable", "axisExpandCenter", "axisExpandCount", "axisExpandWidth", "axisExpandWindow"], function(name) {
        if (opt.hasOwnProperty(name)) {
          this.option[name] = opt[name];
        }
      }, this);
    };
    ParallelModel2.prototype._initDimensions = function() {
      var dimensions = this.dimensions = [];
      var parallelAxisIndex = this.parallelAxisIndex = [];
      var axisModels = filter2(this.ecModel.queryComponents({
        mainType: "parallelAxis"
      }), function(axisModel) {
        return (axisModel.get("parallelIndex") || 0) === this.componentIndex;
      }, this);
      each(axisModels, function(axisModel) {
        dimensions.push("dim" + axisModel.get("dim"));
        parallelAxisIndex.push(axisModel.componentIndex);
      });
    };
    ParallelModel2.type = COMPONENT_TYPE_PARALLEL;
    ParallelModel2.dependencies = ["parallelAxis"];
    ParallelModel2.layoutMode = "box";
    ParallelModel2.defaultOption = {
      // zlevel: 0,
      z: 0,
      left: 80,
      top: 60,
      right: 80,
      bottom: 60,
      // width: {totalWidth} - left - right,
      // height: {totalHeight} - top - bottom,
      layout: "horizontal",
      // FIXME
      // naming?
      axisExpandable: false,
      axisExpandCenter: null,
      axisExpandCount: 0,
      axisExpandWidth: 50,
      axisExpandRate: 17,
      axisExpandDebounce: 50,
      // [out, in, jumpTarget]. In percentage. If use [null, 0.05], null means full.
      // Do not doc to user until necessary.
      axisExpandSlideTriggerArea: [-0.15, 0.05, 0.4],
      axisExpandTriggerOn: "click",
      parallelAxisDefault: null
    };
    return ParallelModel2;
  })(Component_default)
);

// node_modules/echarts/lib/coord/parallel/ParallelAxis.js
var ParallelAxis = (
  /** @class */
  (function(_super) {
    __extends(ParallelAxis2, _super);
    function ParallelAxis2(dim, scale3, coordExtent, axisType, axisIndex) {
      var _this = _super.call(this, dim, scale3, coordExtent) || this;
      _this.type = axisType || "value";
      _this.axisIndex = axisIndex;
      return _this;
    }
    ParallelAxis2.prototype.isHorizontal = function() {
      return this.coordinateSystem.getModel().get("layout") !== "horizontal";
    };
    return ParallelAxis2;
  })(Axis_default)
);
var ParallelAxis_default = ParallelAxis;

// node_modules/echarts/lib/component/helper/sliderMove.js
function sliderMove(delta, handleEnds, extent, handleIndex, minSpan, maxSpan) {
  delta = delta || 0;
  var extentSpan = addSafe(extent[1], -extent[0]);
  if (minSpan != null) {
    minSpan = restrict(minSpan, [0, extentSpan]);
  }
  if (maxSpan != null) {
    maxSpan = Math.max(maxSpan, minSpan != null ? minSpan : 0);
  }
  if (handleIndex === "all") {
    var handleSpan = Math.abs(addSafe(handleEnds[1], -handleEnds[0]));
    handleSpan = restrict(handleSpan, [0, extentSpan]);
    minSpan = maxSpan = restrict(handleSpan, [minSpan, maxSpan]);
    handleIndex = 0;
  }
  handleEnds[0] = restrict(handleEnds[0], extent);
  handleEnds[1] = restrict(handleEnds[1], extent);
  var originalDistSign = getSpanSign(handleEnds, handleIndex);
  handleEnds[handleIndex] += delta;
  var extentMinSpan = minSpan || 0;
  var realExtent = extent.slice();
  originalDistSign.sign < 0 ? realExtent[0] = addSafe(realExtent[0], extentMinSpan) : realExtent[1] = addSafe(realExtent[1], -extentMinSpan);
  handleEnds[handleIndex] = restrict(handleEnds[handleIndex], realExtent);
  var currDistSign;
  currDistSign = getSpanSign(handleEnds, handleIndex);
  if (minSpan != null && (currDistSign.sign !== originalDistSign.sign || currDistSign.span < minSpan)) {
    handleEnds[1 - handleIndex] = addSafe(handleEnds[handleIndex], originalDistSign.sign * minSpan);
  }
  currDistSign = getSpanSign(handleEnds, handleIndex);
  if (maxSpan != null && currDistSign.span > maxSpan) {
    handleEnds[1 - handleIndex] = addSafe(handleEnds[handleIndex], currDistSign.sign * maxSpan);
  }
  return handleEnds;
}
function getSpanSign(handleEnds, handleIndex) {
  var dist3 = handleEnds[handleIndex] - handleEnds[1 - handleIndex];
  return {
    span: Math.abs(dist3),
    sign: dist3 > 0 ? -1 : dist3 < 0 ? 1 : handleIndex ? -1 : 1
  };
}
function restrict(value, extend2) {
  return Math.min(extend2[1] != null ? extend2[1] : Infinity, Math.max(extend2[0] != null ? extend2[0] : -Infinity, value));
}

// node_modules/echarts/lib/coord/parallel/Parallel.js
var Parallel = (
  /** @class */
  (function() {
    function Parallel2(parallelModel, ecModel, api) {
      this.type = COORD_SYS_TYPE_PARALLEL;
      this._axesMap = createHashMap();
      this._axesLayout = {};
      this.dimensions = parallelModel.dimensions;
      this._model = parallelModel;
      this._init(parallelModel, ecModel, api);
    }
    Parallel2.prototype._init = function(parallelModel, ecModel, api) {
      var dimensions = parallelModel.dimensions;
      var parallelAxisIndex = parallelModel.parallelAxisIndex;
      each(dimensions, function(dim, idx) {
        var axisIndex = parallelAxisIndex[idx];
        var axisModel = ecModel.getComponent("parallelAxis", axisIndex);
        var axisType = determineAxisType(axisModel);
        var axis = this._axesMap.set(dim, new ParallelAxis_default(dim, createScaleByModel(axisModel, axisType, false), [0, 0], axisType, axisIndex));
        axis.onBand = isAxisOnBand(axis.scale, axisModel);
        axis.inverse = axisModel.get("inverse");
        axisModel.axis = axis;
        axis.model = axisModel;
        axis.coordinateSystem = axisModel.coordinateSystem = this;
      }, this);
    };
    Parallel2.prototype.update = function(ecModel, api) {
      each(this.dimensions, function(dim) {
        var axis = this._axesMap.get(dim);
        scaleRawExtentInfoCreate(axis, AXIS_EXTENT_INFO_BUILD_FROM_COORD_SYS_UPDATE);
        scaleCalcNice(axis);
      }, this);
    };
    Parallel2.prototype.containPoint = function(point) {
      var layoutInfo = this._makeLayoutInfo();
      var axisBase = layoutInfo.axisBase;
      var layoutBase = layoutInfo.layoutBase;
      var pixelDimIndex = layoutInfo.pixelDimIndex;
      var pAxis = point[1 - pixelDimIndex];
      var pLayout = point[pixelDimIndex];
      return pAxis >= axisBase && pAxis <= axisBase + layoutInfo.axisLength && pLayout >= layoutBase && pLayout <= layoutBase + layoutInfo.layoutLength;
    };
    Parallel2.prototype.getModel = function() {
      return this._model;
    };
    Parallel2.prototype.resize = function(parallelModel, api) {
      var refContainer = createBoxLayoutReference(parallelModel, api).refContainer;
      this._rect = getLayoutRect(parallelModel.getBoxLayoutParams(), refContainer);
      this._layoutAxes();
    };
    Parallel2.prototype.getRect = function() {
      return this._rect;
    };
    Parallel2.prototype._makeLayoutInfo = function() {
      var parallelModel = this._model;
      var rect = this._rect;
      var xy = ["x", "y"];
      var wh = ["width", "height"];
      var layout3 = parallelModel.get("layout");
      var pixelDimIndex = layout3 === "horizontal" ? 0 : 1;
      var layoutLength = rect[wh[pixelDimIndex]];
      var layoutExtent = [0, layoutLength];
      var axisCount = this.dimensions.length;
      var axisExpandWidth = restrict2(parallelModel.get("axisExpandWidth"), layoutExtent);
      var axisExpandCount = restrict2(parallelModel.get("axisExpandCount") || 0, [0, axisCount]);
      var axisExpandable = parallelModel.get("axisExpandable") && axisCount > 3 && axisCount > axisExpandCount && axisExpandCount > 1 && axisExpandWidth > 0 && layoutLength > 0;
      var axisExpandWindow = parallelModel.get("axisExpandWindow");
      var winSize;
      if (!axisExpandWindow) {
        winSize = restrict2(axisExpandWidth * (axisExpandCount - 1), layoutExtent);
        var axisExpandCenter = parallelModel.get("axisExpandCenter") || mathFloor(axisCount / 2);
        axisExpandWindow = [axisExpandWidth * axisExpandCenter - winSize / 2];
        axisExpandWindow[1] = axisExpandWindow[0] + winSize;
      } else {
        winSize = restrict2(axisExpandWindow[1] - axisExpandWindow[0], layoutExtent);
        axisExpandWindow[1] = axisExpandWindow[0] + winSize;
      }
      var axisCollapseWidth = (layoutLength - winSize) / (axisCount - axisExpandCount);
      axisCollapseWidth < 3 && (axisCollapseWidth = 0);
      var winInnerIndices = [mathFloor(round2(axisExpandWindow[0] / axisExpandWidth, 1)) + 1, mathCeil(round2(axisExpandWindow[1] / axisExpandWidth, 1)) - 1];
      var axisExpandWindow0Pos = axisCollapseWidth / axisExpandWidth * axisExpandWindow[0];
      return {
        layout: layout3,
        pixelDimIndex,
        layoutBase: rect[xy[pixelDimIndex]],
        layoutLength,
        axisBase: rect[xy[1 - pixelDimIndex]],
        axisLength: rect[wh[1 - pixelDimIndex]],
        axisExpandable,
        axisExpandWidth,
        axisCollapseWidth,
        axisExpandWindow,
        axisCount,
        winInnerIndices,
        axisExpandWindow0Pos
      };
    };
    Parallel2.prototype._layoutAxes = function() {
      var rect = this._rect;
      var axes = this._axesMap;
      var dimensions = this.dimensions;
      var layoutInfo = this._makeLayoutInfo();
      var layout3 = layoutInfo.layout;
      axes.each(function(axis) {
        var axisExtent = [0, layoutInfo.axisLength];
        var idx = axis.inverse ? 1 : 0;
        axis.setExtent(axisExtent[idx], axisExtent[1 - idx]);
      });
      each(dimensions, function(dim, idx) {
        var posInfo = (layoutInfo.axisExpandable ? layoutAxisWithExpand : layoutAxisWithoutExpand)(idx, layoutInfo);
        var positionTable = {
          horizontal: {
            x: posInfo.position,
            y: layoutInfo.axisLength
          },
          vertical: {
            x: 0,
            y: posInfo.position
          }
        };
        var rotationTable = {
          horizontal: mathPI / 2,
          vertical: 0
        };
        var position = [positionTable[layout3].x + rect.x, positionTable[layout3].y + rect.y];
        var rotation = rotationTable[layout3];
        var transform = create();
        rotate(transform, transform, rotation);
        translate(transform, transform, position);
        this._axesLayout[dim] = {
          position,
          rotation,
          transform,
          axisNameAvailableWidth: posInfo.axisNameAvailableWidth,
          axisLabelShow: posInfo.axisLabelShow,
          nameTruncateMaxWidth: posInfo.nameTruncateMaxWidth,
          tickDirection: 1,
          labelDirection: 1
        };
      }, this);
    };
    Parallel2.prototype.getAxis = function(dim) {
      return this._axesMap.get(dim);
    };
    Parallel2.prototype.dataToPoint = function(value, dim) {
      return this.axisCoordToPoint(this._axesMap.get(dim).dataToCoord(value), dim);
    };
    Parallel2.prototype.eachActiveState = function(data, callback, start2, end2) {
      start2 == null && (start2 = 0);
      end2 == null && (end2 = data.count());
      var axesMap = this._axesMap;
      var dimensions = this.dimensions;
      var dataDimensions = [];
      var axisModels = [];
      each(dimensions, function(axisDim) {
        dataDimensions.push(data.mapDimension(axisDim));
        axisModels.push(axesMap.get(axisDim).model);
      });
      var hasActiveSet = this.hasAxisBrushed();
      for (var dataIndex = start2; dataIndex < end2; dataIndex++) {
        var activeState = void 0;
        if (!hasActiveSet) {
          activeState = "normal";
        } else {
          activeState = "active";
          var values = data.getValues(dataDimensions, dataIndex);
          for (var j = 0, lenj = dimensions.length; j < lenj; j++) {
            var state2 = axisModels[j].getActiveState(values[j]);
            if (state2 === "inactive") {
              activeState = "inactive";
              break;
            }
          }
        }
        callback(activeState, dataIndex);
      }
    };
    Parallel2.prototype.hasAxisBrushed = function() {
      var dimensions = this.dimensions;
      var axesMap = this._axesMap;
      var hasActiveSet = false;
      for (var j = 0, lenj = dimensions.length; j < lenj; j++) {
        if (axesMap.get(dimensions[j]).model.getActiveState() !== "normal") {
          hasActiveSet = true;
        }
      }
      return hasActiveSet;
    };
    Parallel2.prototype.axisCoordToPoint = function(coord, dim) {
      var axisLayout = this._axesLayout[dim];
      return applyTransform2([coord, 0], axisLayout.transform);
    };
    Parallel2.prototype.getAxisLayout = function(dim) {
      return clone(this._axesLayout[dim]);
    };
    Parallel2.prototype.getSlidedAxisExpandWindow = function(point) {
      var layoutInfo = this._makeLayoutInfo();
      var pixelDimIndex = layoutInfo.pixelDimIndex;
      var axisExpandWindow = layoutInfo.axisExpandWindow.slice();
      var winSize = axisExpandWindow[1] - axisExpandWindow[0];
      var extent = [0, layoutInfo.axisExpandWidth * (layoutInfo.axisCount - 1)];
      if (!this.containPoint(point)) {
        return {
          behavior: "none",
          axisExpandWindow
        };
      }
      var pointCoord = point[pixelDimIndex] - layoutInfo.layoutBase - layoutInfo.axisExpandWindow0Pos;
      var delta;
      var behavior = "slide";
      var axisCollapseWidth = layoutInfo.axisCollapseWidth;
      var triggerArea = this._model.get("axisExpandSlideTriggerArea");
      var useJump = triggerArea[0] != null;
      if (axisCollapseWidth) {
        if (useJump && axisCollapseWidth && pointCoord < winSize * triggerArea[0]) {
          behavior = "jump";
          delta = pointCoord - winSize * triggerArea[2];
        } else if (useJump && axisCollapseWidth && pointCoord > winSize * (1 - triggerArea[0])) {
          behavior = "jump";
          delta = pointCoord - winSize * (1 - triggerArea[2]);
        } else {
          (delta = pointCoord - winSize * triggerArea[1]) >= 0 && (delta = pointCoord - winSize * (1 - triggerArea[1])) <= 0 && (delta = 0);
        }
        delta *= layoutInfo.axisExpandWidth / axisCollapseWidth;
        delta ? sliderMove(delta, axisExpandWindow, extent, "all") : behavior = "none";
      } else {
        var winSize2 = axisExpandWindow[1] - axisExpandWindow[0];
        var pos = extent[1] * pointCoord / winSize2;
        axisExpandWindow = [mathMax4(0, pos - winSize2 / 2)];
        axisExpandWindow[1] = mathMin4(extent[1], axisExpandWindow[0] + winSize2);
        axisExpandWindow[0] = axisExpandWindow[1] - winSize2;
      }
      return {
        axisExpandWindow,
        behavior
      };
    };
    return Parallel2;
  })()
);
function restrict2(len2, extent) {
  return mathMin4(mathMax4(len2, extent[0]), extent[1]);
}
function layoutAxisWithoutExpand(axisIndex, layoutInfo) {
  var step = layoutInfo.layoutLength / (layoutInfo.axisCount - 1);
  return {
    position: step * axisIndex,
    axisNameAvailableWidth: step,
    axisLabelShow: true
  };
}
function layoutAxisWithExpand(axisIndex, layoutInfo) {
  var layoutLength = layoutInfo.layoutLength;
  var axisExpandWidth = layoutInfo.axisExpandWidth;
  var axisCount = layoutInfo.axisCount;
  var axisCollapseWidth = layoutInfo.axisCollapseWidth;
  var winInnerIndices = layoutInfo.winInnerIndices;
  var position;
  var axisNameAvailableWidth = axisCollapseWidth;
  var axisLabelShow = false;
  var nameTruncateMaxWidth;
  if (axisIndex < winInnerIndices[0]) {
    position = axisIndex * axisCollapseWidth;
    nameTruncateMaxWidth = axisCollapseWidth;
  } else if (axisIndex <= winInnerIndices[1]) {
    position = layoutInfo.axisExpandWindow0Pos + axisIndex * axisExpandWidth - layoutInfo.axisExpandWindow[0];
    axisNameAvailableWidth = axisExpandWidth;
    axisLabelShow = true;
  } else {
    position = layoutLength - (axisCount - 1 - axisIndex) * axisCollapseWidth;
    nameTruncateMaxWidth = axisCollapseWidth;
  }
  return {
    position,
    axisNameAvailableWidth,
    axisLabelShow,
    nameTruncateMaxWidth
  };
}

// node_modules/echarts/lib/coord/parallel/AxisModel.js
var ParallelAxisModel = (
  /** @class */
  (function(_super) {
    __extends(ParallelAxisModel2, _super);
    function ParallelAxisModel2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = ParallelAxisModel2.type;
      _this.activeIntervals = [];
      return _this;
    }
    ParallelAxisModel2.prototype.getAreaSelectStyle = function() {
      return makeStyleMapper([
        ["fill", "color"],
        ["lineWidth", "borderWidth"],
        ["stroke", "borderColor"],
        ["width", "width"],
        ["opacity", "opacity"]
        // Option decal is in `DecalObject` but style.decal is in `PatternObject`.
        // So do not transfer decal directly.
      ])(this.getModel("areaSelectStyle"));
    };
    ParallelAxisModel2.prototype.setActiveIntervals = function(intervals) {
      var activeIntervals = this.activeIntervals = clone(intervals);
      if (activeIntervals) {
        for (var i2 = activeIntervals.length - 1; i2 >= 0; i2--) {
          asc(activeIntervals[i2]);
        }
      }
    };
    ParallelAxisModel2.prototype.getActiveState = function(value) {
      var activeIntervals = this.activeIntervals;
      if (!activeIntervals.length) {
        return "normal";
      }
      if (value == null || isNaN(+value)) {
        return "inactive";
      }
      if (activeIntervals.length === 1) {
        var interval2 = activeIntervals[0];
        if (interval2[0] <= value && value <= interval2[1]) {
          return "active";
        }
      } else {
        for (var i2 = 0, len2 = activeIntervals.length; i2 < len2; i2++) {
          if (activeIntervals[i2][0] <= value && value <= activeIntervals[i2][1]) {
            return "active";
          }
        }
      }
      return "inactive";
    };
    return ParallelAxisModel2;
  })(Component_default)
);
mixin(ParallelAxisModel, AxisModelCommonMixin);

// node_modules/echarts/lib/component/helper/BrushController.js
var BRUSH_PANEL_GLOBAL = true;
var mathMin9 = Math.min;
var mathMax9 = Math.max;
var mathPow3 = Math.pow;
var COVER_Z = 1e4;
var UNSELECT_THRESHOLD = 6;
var MIN_RESIZE_LINE_WIDTH = 6;
var MUTEX_RESOURCE_KEY = "globalPan";
var DIRECTION_MAP = {
  w: [0, 0],
  e: [0, 1],
  n: [1, 0],
  s: [1, 1]
};
var CURSOR_MAP = {
  w: "ew",
  e: "ew",
  n: "ns",
  s: "ns",
  ne: "nesw",
  sw: "nesw",
  nw: "nwse",
  se: "nwse"
};
var DEFAULT_BRUSH_OPT = {
  brushStyle: {
    lineWidth: 2,
    stroke: tokens_default.color.backgroundTint,
    fill: tokens_default.color.borderTint
  },
  transformable: true,
  brushMode: "single",
  removeOnClick: false
};
var baseUID = 0;
var BrushController = (
  /** @class */
  (function(_super) {
    __extends(BrushController2, _super);
    function BrushController2(zr) {
      var _this = _super.call(this) || this;
      _this._track = [];
      _this._covers = [];
      _this._handlers = {};
      if (true) {
        assert(zr);
      }
      _this._zr = zr;
      _this.group = new Group_default();
      _this._uid = "brushController_" + baseUID++;
      each(pointerHandlers, function(handler, eventName) {
        this._handlers[eventName] = bind(handler, this);
      }, _this);
      return _this;
    }
    BrushController2.prototype.enableBrush = function(brushOption) {
      if (true) {
        assert(this._mounted);
      }
      this._brushType && this._doDisableBrush();
      brushOption.brushType && this._doEnableBrush(brushOption);
      return this;
    };
    BrushController2.prototype._doEnableBrush = function(brushOption) {
      var zr = this._zr;
      if (!this._enableGlobalPan) {
        take2(zr, MUTEX_RESOURCE_KEY, this._uid);
      }
      each(this._handlers, function(handler, eventName) {
        zr.on(eventName, handler);
      });
      this._brushType = brushOption.brushType;
      this._brushOption = merge2(clone(DEFAULT_BRUSH_OPT), brushOption, true);
    };
    BrushController2.prototype._doDisableBrush = function() {
      var zr = this._zr;
      release(zr, MUTEX_RESOURCE_KEY, this._uid);
      each(this._handlers, function(handler, eventName) {
        zr.off(eventName, handler);
      });
      this._brushType = this._brushOption = null;
    };
    BrushController2.prototype.setPanels = function(panelOpts) {
      if (panelOpts && panelOpts.length) {
        var panels_1 = this._panels = {};
        each(panelOpts, function(panelOpts2) {
          panels_1[panelOpts2.panelId] = clone(panelOpts2);
        });
      } else {
        this._panels = null;
      }
      return this;
    };
    BrushController2.prototype.mount = function(opt) {
      opt = opt || {};
      if (true) {
        this._mounted = true;
      }
      this._enableGlobalPan = opt.enableGlobalPan;
      var thisGroup = this.group;
      this._zr.add(thisGroup);
      thisGroup.attr({
        x: opt.x || 0,
        y: opt.y || 0,
        rotation: opt.rotation || 0,
        scaleX: opt.scaleX || 1,
        scaleY: opt.scaleY || 1
      });
      this._transform = thisGroup.getLocalTransform();
      return this;
    };
    BrushController2.prototype.updateCovers = function(coverConfigList) {
      if (true) {
        assert(this._mounted);
      }
      coverConfigList = map2(coverConfigList, function(coverConfig) {
        return merge2(clone(DEFAULT_BRUSH_OPT), coverConfig, true);
      });
      var tmpIdPrefix = "\0-brush-index-";
      var oldCovers = this._covers;
      var newCovers = this._covers = [];
      var controller = this;
      var creatingCover = this._creatingCover;
      new DataDiffer_default(oldCovers, coverConfigList, oldGetKey, getKey2).add(addOrUpdate).update(addOrUpdate).remove(remove).execute();
      return this;
      function getKey2(brushOption, index) {
        return (brushOption.id != null ? brushOption.id : tmpIdPrefix + index) + "-" + brushOption.brushType;
      }
      function oldGetKey(cover, index) {
        return getKey2(cover.__brushOption, index);
      }
      function addOrUpdate(newIndex, oldIndex) {
        var newBrushInternal = coverConfigList[newIndex];
        if (oldIndex != null && oldCovers[oldIndex] === creatingCover) {
          newCovers[newIndex] = oldCovers[oldIndex];
        } else {
          var cover = newCovers[newIndex] = oldIndex != null ? (oldCovers[oldIndex].__brushOption = newBrushInternal, oldCovers[oldIndex]) : endCreating(controller, createCover(controller, newBrushInternal));
          updateCoverAfterCreation(controller, cover);
        }
      }
      function remove(oldIndex) {
        if (oldCovers[oldIndex] !== creatingCover) {
          controller.group.remove(oldCovers[oldIndex]);
        }
      }
    };
    BrushController2.prototype.unmount = function() {
      if (true) {
        if (!this._mounted) {
          return;
        }
      }
      this.enableBrush(false);
      clearCovers(this);
      this._zr.remove(this.group);
      if (true) {
        this._mounted = false;
      }
      return this;
    };
    BrushController2.prototype.dispose = function() {
      this.unmount();
      this.off();
    };
    return BrushController2;
  })(Eventful_default)
);
function createCover(controller, brushOption) {
  var cover = coverRenderers[brushOption.brushType].createCover(controller, brushOption);
  cover.__brushOption = brushOption;
  updateZ(cover, brushOption);
  controller.group.add(cover);
  return cover;
}
function endCreating(controller, creatingCover) {
  var coverRenderer = getCoverRenderer(creatingCover);
  if (coverRenderer.endCreating) {
    coverRenderer.endCreating(controller, creatingCover);
    updateZ(creatingCover, creatingCover.__brushOption);
  }
  return creatingCover;
}
function updateCoverShape(controller, cover) {
  var brushOption = cover.__brushOption;
  getCoverRenderer(cover).updateCoverShape(controller, cover, brushOption.range, brushOption);
}
function updateZ(cover, brushOption) {
  var z = brushOption.z;
  z == null && (z = COVER_Z);
  cover.traverse(function(el) {
    el.z = z;
    el.z2 = z;
  });
}
function updateCoverAfterCreation(controller, cover) {
  getCoverRenderer(cover).updateCommon(controller, cover);
  updateCoverShape(controller, cover);
}
function getCoverRenderer(cover) {
  return coverRenderers[cover.__brushOption.brushType];
}
function getPanelByPoint(controller, e2, localCursorPoint) {
  var panels = controller._panels;
  if (!panels) {
    return BRUSH_PANEL_GLOBAL;
  }
  var panel;
  var transform = controller._transform;
  each(panels, function(pn) {
    pn.isTargetByCursor(e2, localCursorPoint, transform) && (panel = pn);
  });
  return panel;
}
function getPanelByCover(controller, cover) {
  var panels = controller._panels;
  if (!panels) {
    return BRUSH_PANEL_GLOBAL;
  }
  var panelId = cover.__brushOption.panelId;
  return panelId != null ? panels[panelId] : BRUSH_PANEL_GLOBAL;
}
function clearCovers(controller) {
  var covers = controller._covers;
  var originalLength = covers.length;
  each(covers, function(cover) {
    controller.group.remove(cover);
  }, controller);
  covers.length = 0;
  return !!originalLength;
}
function trigger3(controller, opt) {
  var areas = map2(controller._covers, function(cover) {
    var brushOption = cover.__brushOption;
    var range = clone(brushOption.range);
    return {
      brushType: brushOption.brushType,
      panelId: brushOption.panelId,
      range
    };
  });
  controller.trigger("brush", {
    areas,
    isEnd: !!opt.isEnd,
    removeOnClick: !!opt.removeOnClick
  });
}
function shouldShowCover(controller) {
  var track = controller._track;
  if (!track.length) {
    return false;
  }
  var p2 = track[track.length - 1];
  var p1 = track[0];
  var dx = p2[0] - p1[0];
  var dy = p2[1] - p1[1];
  var dist3 = mathPow3(dx * dx + dy * dy, 0.5);
  return dist3 > UNSELECT_THRESHOLD;
}
function getTrackEnds(track) {
  var tail = track.length - 1;
  tail < 0 && (tail = 0);
  return [track[0], track[tail]];
}
function createBaseRectCover(rectRangeConverter, controller, brushOption, edgeNameSequences) {
  var cover = new Group_default();
  cover.add(new Rect_default({
    name: "main",
    style: makeStyle(brushOption),
    silent: true,
    draggable: true,
    cursor: "move",
    drift: curry(driftRect, rectRangeConverter, controller, cover, ["n", "s", "w", "e"]),
    ondragend: curry(trigger3, controller, {
      isEnd: true
    })
  }));
  each(edgeNameSequences, function(nameSequence) {
    cover.add(new Rect_default({
      name: nameSequence.join(""),
      style: {
        opacity: 0
      },
      draggable: true,
      silent: true,
      invisible: true,
      drift: curry(driftRect, rectRangeConverter, controller, cover, nameSequence),
      ondragend: curry(trigger3, controller, {
        isEnd: true
      })
    }));
  });
  return cover;
}
function updateBaseRect(controller, cover, localRange, brushOption) {
  var lineWidth = brushOption.brushStyle.lineWidth || 0;
  var handleSize = mathMax9(lineWidth, MIN_RESIZE_LINE_WIDTH);
  var x = localRange[0][0];
  var y = localRange[1][0];
  var xa = x - lineWidth / 2;
  var ya = y - lineWidth / 2;
  var x2 = localRange[0][1];
  var y2 = localRange[1][1];
  var x2a = x2 - handleSize + lineWidth / 2;
  var y2a = y2 - handleSize + lineWidth / 2;
  var width = x2 - x;
  var height = y2 - y;
  var widtha = width + lineWidth;
  var heighta = height + lineWidth;
  updateRectShape(controller, cover, "main", x, y, width, height);
  if (brushOption.transformable) {
    updateRectShape(controller, cover, "w", xa, ya, handleSize, heighta);
    updateRectShape(controller, cover, "e", x2a, ya, handleSize, heighta);
    updateRectShape(controller, cover, "n", xa, ya, widtha, handleSize);
    updateRectShape(controller, cover, "s", xa, y2a, widtha, handleSize);
    updateRectShape(controller, cover, "nw", xa, ya, handleSize, handleSize);
    updateRectShape(controller, cover, "ne", x2a, ya, handleSize, handleSize);
    updateRectShape(controller, cover, "sw", xa, y2a, handleSize, handleSize);
    updateRectShape(controller, cover, "se", x2a, y2a, handleSize, handleSize);
  }
}
function updateCommon(controller, cover) {
  var brushOption = cover.__brushOption;
  var transformable = brushOption.transformable;
  var mainEl = cover.childAt(0);
  mainEl.useStyle(makeStyle(brushOption));
  mainEl.attr({
    silent: !transformable,
    cursor: transformable ? "move" : "default"
  });
  each([["w"], ["e"], ["n"], ["s"], ["s", "e"], ["s", "w"], ["n", "e"], ["n", "w"]], function(nameSequence) {
    var el = cover.childOfName(nameSequence.join(""));
    var globalDir = nameSequence.length === 1 ? getGlobalDirection1(controller, nameSequence[0]) : getGlobalDirection2(controller, nameSequence);
    el && el.attr({
      silent: !transformable,
      invisible: !transformable,
      cursor: transformable ? CURSOR_MAP[globalDir] + "-resize" : null
    });
  });
}
function updateRectShape(controller, cover, name, x, y, w, h) {
  var el = cover.childOfName(name);
  el && el.setShape(pointsToRect(clipByPanel(controller, cover, [[x, y], [x + w, y + h]])));
}
function makeStyle(brushOption) {
  return defaults({
    strokeNoScale: true
  }, brushOption.brushStyle);
}
function formatRectRange(x, y, x2, y2) {
  var min3 = [mathMin9(x, x2), mathMin9(y, y2)];
  var max3 = [mathMax9(x, x2), mathMax9(y, y2)];
  return [
    [min3[0], max3[0]],
    [min3[1], max3[1]]
    // y range
  ];
}
function getTransform3(controller) {
  return getTransform2(controller.group);
}
function getGlobalDirection1(controller, localDirName) {
  var map4 = {
    w: "left",
    e: "right",
    n: "top",
    s: "bottom"
  };
  var inverseMap = {
    left: "w",
    right: "e",
    top: "n",
    bottom: "s"
  };
  var dir3 = transformDirection(map4[localDirName], getTransform3(controller));
  return inverseMap[dir3];
}
function getGlobalDirection2(controller, localDirNameSeq) {
  var globalDir = [getGlobalDirection1(controller, localDirNameSeq[0]), getGlobalDirection1(controller, localDirNameSeq[1])];
  (globalDir[0] === "e" || globalDir[0] === "w") && globalDir.reverse();
  return globalDir.join("");
}
function driftRect(rectRangeConverter, controller, cover, dirNameSequence, dx, dy) {
  var brushOption = cover.__brushOption;
  var rectRange = rectRangeConverter.toRectRange(brushOption.range);
  var localDelta = toLocalDelta(controller, dx, dy);
  each(dirNameSequence, function(dirName) {
    var ind = DIRECTION_MAP[dirName];
    rectRange[ind[0]][ind[1]] += localDelta[ind[0]];
  });
  brushOption.range = rectRangeConverter.fromRectRange(formatRectRange(rectRange[0][0], rectRange[1][0], rectRange[0][1], rectRange[1][1]));
  updateCoverAfterCreation(controller, cover);
  trigger3(controller, {
    isEnd: false
  });
}
function driftPolygon(controller, cover, dx, dy) {
  var range = cover.__brushOption.range;
  var localDelta = toLocalDelta(controller, dx, dy);
  each(range, function(point) {
    point[0] += localDelta[0];
    point[1] += localDelta[1];
  });
  updateCoverAfterCreation(controller, cover);
  trigger3(controller, {
    isEnd: false
  });
}
function toLocalDelta(controller, dx, dy) {
  var thisGroup = controller.group;
  var localD = thisGroup.transformCoordToLocal(dx, dy);
  var localZero = thisGroup.transformCoordToLocal(0, 0);
  return [localD[0] - localZero[0], localD[1] - localZero[1]];
}
function clipByPanel(controller, cover, data) {
  var panel = getPanelByCover(controller, cover);
  return panel && panel !== BRUSH_PANEL_GLOBAL ? panel.clipPath(data, controller._transform) : clone(data);
}
function pointsToRect(points4) {
  var xmin = mathMin9(points4[0][0], points4[1][0]);
  var ymin = mathMin9(points4[0][1], points4[1][1]);
  var xmax = mathMax9(points4[0][0], points4[1][0]);
  var ymax = mathMax9(points4[0][1], points4[1][1]);
  return {
    x: xmin,
    y: ymin,
    width: xmax - xmin,
    height: ymax - ymin
  };
}
function resetCursor(controller, e2, localCursorPoint) {
  if (
    // Check active
    !controller._brushType || isOutsideZrArea(controller, e2.offsetX, e2.offsetY)
  ) {
    return;
  }
  var zr = controller._zr;
  var covers = controller._covers;
  var currPanel = getPanelByPoint(controller, e2, localCursorPoint);
  if (!controller._dragging) {
    for (var i2 = 0; i2 < covers.length; i2++) {
      var brushOption = covers[i2].__brushOption;
      if (currPanel && (currPanel === BRUSH_PANEL_GLOBAL || brushOption.panelId === currPanel.panelId) && coverRenderers[brushOption.brushType].contain(covers[i2], localCursorPoint[0], localCursorPoint[1])) {
        return;
      }
    }
  }
  currPanel && zr.setCursorStyle("crosshair");
}
function preventDefault(e2) {
  var rawE = e2.event;
  rawE.preventDefault && rawE.preventDefault();
}
function mainShapeContain(cover, x, y) {
  return cover.childOfName("main").contain(x, y);
}
function updateCoverByMouse(controller, e2, localCursorPoint, isEnd) {
  var creatingCover = controller._creatingCover;
  var panel = controller._creatingPanel;
  var thisBrushOption = controller._brushOption;
  var eventParams;
  controller._track.push(localCursorPoint.slice());
  if (shouldShowCover(controller) || creatingCover) {
    if (panel && !creatingCover) {
      thisBrushOption.brushMode === "single" && clearCovers(controller);
      var brushOption = clone(thisBrushOption);
      brushOption.brushType = determineBrushType(brushOption.brushType, panel);
      brushOption.panelId = panel === BRUSH_PANEL_GLOBAL ? null : panel.panelId;
      creatingCover = controller._creatingCover = createCover(controller, brushOption);
      controller._covers.push(creatingCover);
    }
    if (creatingCover) {
      var coverRenderer = coverRenderers[determineBrushType(controller._brushType, panel)];
      var coverBrushOption = creatingCover.__brushOption;
      coverBrushOption.range = coverRenderer.getCreatingRange(clipByPanel(controller, creatingCover, controller._track));
      if (isEnd) {
        endCreating(controller, creatingCover);
        coverRenderer.updateCommon(controller, creatingCover);
      }
      updateCoverShape(controller, creatingCover);
      eventParams = {
        isEnd
      };
    }
  } else if (isEnd && thisBrushOption.brushMode === "single" && thisBrushOption.removeOnClick) {
    if (getPanelByPoint(controller, e2, localCursorPoint) && clearCovers(controller)) {
      eventParams = {
        isEnd,
        removeOnClick: true
      };
    }
  }
  return eventParams;
}
function determineBrushType(brushType, panel) {
  if (brushType === "auto") {
    if (true) {
      assert(panel && panel.defaultBrushType, 'MUST have defaultBrushType when brushType is "atuo"');
    }
    return panel.defaultBrushType;
  }
  return brushType;
}
var pointerHandlers = {
  mousedown: function(e2) {
    if (this._dragging) {
      handleDragEnd(this, e2);
    } else if (!e2.target || !e2.target.draggable) {
      preventDefault(e2);
      var localCursorPoint = this.group.transformCoordToLocal(e2.offsetX, e2.offsetY);
      this._creatingCover = null;
      var panel = this._creatingPanel = getPanelByPoint(this, e2, localCursorPoint);
      if (panel) {
        this._dragging = true;
        this._track = [localCursorPoint.slice()];
      }
    }
  },
  mousemove: function(e2) {
    var x = e2.offsetX;
    var y = e2.offsetY;
    var localCursorPoint = this.group.transformCoordToLocal(x, y);
    resetCursor(this, e2, localCursorPoint);
    if (this._dragging) {
      preventDefault(e2);
      var eventParams = updateCoverByMouse(this, e2, localCursorPoint, false);
      eventParams && trigger3(this, eventParams);
    }
  },
  mouseup: function(e2) {
    handleDragEnd(this, e2);
  }
};
function handleDragEnd(controller, e2) {
  if (controller._dragging) {
    preventDefault(e2);
    var x = e2.offsetX;
    var y = e2.offsetY;
    var localCursorPoint = controller.group.transformCoordToLocal(x, y);
    var eventParams = updateCoverByMouse(controller, e2, localCursorPoint, true);
    controller._dragging = false;
    controller._track = [];
    controller._creatingCover = null;
    eventParams && trigger3(controller, eventParams);
  }
}
function isOutsideZrArea(controller, x, y) {
  var zr = controller._zr;
  return x < 0 || x > zr.getWidth() || y < 0 || y > zr.getHeight();
}
var coverRenderers = {
  lineX: getLineRenderer(0),
  lineY: getLineRenderer(1),
  rect: {
    createCover: function(controller, brushOption) {
      function returnInput(range) {
        return range;
      }
      return createBaseRectCover({
        toRectRange: returnInput,
        fromRectRange: returnInput
      }, controller, brushOption, [["w"], ["e"], ["n"], ["s"], ["s", "e"], ["s", "w"], ["n", "e"], ["n", "w"]]);
    },
    getCreatingRange: function(localTrack) {
      var ends = getTrackEnds(localTrack);
      return formatRectRange(ends[1][0], ends[1][1], ends[0][0], ends[0][1]);
    },
    updateCoverShape: function(controller, cover, localRange, brushOption) {
      updateBaseRect(controller, cover, localRange, brushOption);
    },
    updateCommon,
    contain: mainShapeContain
  },
  polygon: {
    createCover: function(controller, brushOption) {
      var cover = new Group_default();
      cover.add(new Polyline_default({
        name: "main",
        style: makeStyle(brushOption),
        silent: true
      }));
      return cover;
    },
    getCreatingRange: function(localTrack) {
      return localTrack;
    },
    endCreating: function(controller, cover) {
      cover.remove(cover.childAt(0));
      cover.add(new Polygon_default({
        name: "main",
        draggable: true,
        drift: curry(driftPolygon, controller, cover),
        ondragend: curry(trigger3, controller, {
          isEnd: true
        })
      }));
    },
    updateCoverShape: function(controller, cover, localRange, brushOption) {
      cover.childAt(0).setShape({
        points: clipByPanel(controller, cover, localRange)
      });
    },
    updateCommon,
    contain: mainShapeContain
  }
};
function getLineRenderer(xyIndex) {
  return {
    createCover: function(controller, brushOption) {
      return createBaseRectCover({
        toRectRange: function(range) {
          var rectRange = [range, [0, 100]];
          xyIndex && rectRange.reverse();
          return rectRange;
        },
        fromRectRange: function(rectRange) {
          return rectRange[xyIndex];
        }
      }, controller, brushOption, [[["w"], ["e"]], [["n"], ["s"]]][xyIndex]);
    },
    getCreatingRange: function(localTrack) {
      var ends = getTrackEnds(localTrack);
      var min3 = mathMin9(ends[0][xyIndex], ends[1][xyIndex]);
      var max3 = mathMax9(ends[0][xyIndex], ends[1][xyIndex]);
      return [min3, max3];
    },
    updateCoverShape: function(controller, cover, localRange, brushOption) {
      var otherExtent;
      var panel = getPanelByCover(controller, cover);
      if (panel !== BRUSH_PANEL_GLOBAL && panel.getLinearBrushOtherExtent) {
        otherExtent = panel.getLinearBrushOtherExtent(xyIndex);
      } else {
        var zr = controller._zr;
        otherExtent = [0, [zr.getWidth(), zr.getHeight()][1 - xyIndex]];
      }
      var rectRange = [localRange, otherExtent];
      xyIndex && rectRange.reverse();
      updateBaseRect(controller, cover, rectRange, brushOption);
    },
    updateCommon,
    contain: mainShapeContain
  };
}
var BrushController_default = BrushController;

// node_modules/echarts/lib/component/helper/brushHelper.js
function makeRectPanelClipPath(rect) {
  rect = normalizeRect(rect);
  return function(localPoints) {
    return clipPointsByRect(localPoints, rect);
  };
}
function makeLinearBrushOtherExtent(rect, specifiedXYIndex) {
  rect = normalizeRect(rect);
  return function(xyIndex) {
    var idx = specifiedXYIndex != null ? specifiedXYIndex : xyIndex;
    var brushWidth = idx ? rect.width : rect.height;
    var base2 = idx ? rect.x : rect.y;
    return [base2, base2 + (brushWidth || 0)];
  };
}
function makeRectIsTargetByCursor(rect, api, targetModel) {
  var boundingRect = normalizeRect(rect);
  return function(e2, localCursorPoint) {
    return boundingRect.contain(localCursorPoint[0], localCursorPoint[1]) && !onIrrelevantElement(e2, api, targetModel);
  };
}
function normalizeRect(rect) {
  return BoundingRect_default.create(rect);
}

// node_modules/echarts/lib/component/axis/ParallelAxisView.js
var ParallelAxisView = (
  /** @class */
  (function(_super) {
    __extends(ParallelAxisView2, _super);
    function ParallelAxisView2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = ParallelAxisView2.type;
      return _this;
    }
    ParallelAxisView2.prototype.init = function(ecModel, api) {
      _super.prototype.init.apply(this, arguments);
      (this._brushController = new BrushController_default(api.getZr())).on("brush", bind(this._onBrush, this));
    };
    ParallelAxisView2.prototype.render = function(axisModel, ecModel, api, payload) {
      if (fromAxisAreaSelect(axisModel, ecModel, payload)) {
        return;
      }
      this.axisModel = axisModel;
      this.api = api;
      this.group.removeAll();
      var oldAxisGroup = this._axisGroup;
      this._axisGroup = new Group_default();
      this.group.add(this._axisGroup);
      if (!axisModel.get("show")) {
        return;
      }
      var coordSysModel = getCoordSysModel(axisModel, ecModel);
      var coordSys = coordSysModel.coordinateSystem;
      var areaSelectStyle = axisModel.getAreaSelectStyle();
      var areaWidth = areaSelectStyle.width;
      var dim = axisModel.axis.dim;
      var axisLayout = coordSys.getAxisLayout(dim);
      var builderOpt = extend({
        strokeContainThreshold: areaWidth
      }, axisLayout);
      var axisBuilder = new AxisBuilder_default(axisModel, api, builderOpt);
      axisBuilder.build();
      this._axisGroup.add(axisBuilder.group);
      this._refreshBrushController(builderOpt, areaSelectStyle, axisModel, coordSysModel, areaWidth, api);
      groupTransition(oldAxisGroup, this._axisGroup, axisModel);
    };
    ParallelAxisView2.prototype._refreshBrushController = function(builderOpt, areaSelectStyle, axisModel, coordSysModel, areaWidth, api) {
      var extent = axisModel.axis.getExtent();
      var extentLen = extent[1] - extent[0];
      var extra = Math.min(30, Math.abs(extentLen) * 0.1);
      var rect = BoundingRect_default.create({
        x: extent[0],
        y: -areaWidth / 2,
        width: extentLen,
        height: areaWidth
      });
      rect.x -= extra;
      rect.width += 2 * extra;
      this._brushController.mount({
        enableGlobalPan: true,
        rotation: builderOpt.rotation,
        x: builderOpt.position[0],
        y: builderOpt.position[1]
      }).setPanels([{
        panelId: "pl",
        clipPath: makeRectPanelClipPath(rect),
        isTargetByCursor: makeRectIsTargetByCursor(rect, api, coordSysModel),
        getLinearBrushOtherExtent: makeLinearBrushOtherExtent(rect, 0)
      }]).enableBrush({
        brushType: "lineX",
        brushStyle: areaSelectStyle,
        removeOnClick: true
      }).updateCovers(getCoverInfoList(axisModel));
    };
    ParallelAxisView2.prototype._onBrush = function(eventParam) {
      var coverInfoList = eventParam.areas;
      var axisModel = this.axisModel;
      var axis = axisModel.axis;
      var intervals = map2(coverInfoList, function(coverInfo) {
        return [axis.coordToData(coverInfo.range[0], true), axis.coordToData(coverInfo.range[1], true)];
      });
      if (!axisModel.option.realtime === eventParam.isEnd || eventParam.removeOnClick) {
        this.api.dispatchAction({
          type: "axisAreaSelect",
          parallelAxisId: axisModel.id,
          intervals
        });
      }
    };
    ParallelAxisView2.prototype.dispose = function() {
      this._brushController.dispose();
    };
    ParallelAxisView2.type = "parallelAxis";
    return ParallelAxisView2;
  })(Component_default2)
);
function fromAxisAreaSelect(axisModel, ecModel, payload) {
  return payload && payload.type === "axisAreaSelect" && ecModel.findComponents({
    mainType: "parallelAxis",
    query: payload
  })[0] === axisModel;
}
function getCoverInfoList(axisModel) {
  var axis = axisModel.axis;
  return map2(axisModel.activeIntervals, function(interval2) {
    return {
      brushType: "lineX",
      panelId: "pl",
      range: [axis.dataToCoord(interval2[0], true), axis.dataToCoord(interval2[1], true)]
    };
  });
}
function getCoordSysModel(axisModel, ecModel) {
  return ecModel.getComponent("parallel", axisModel.get("parallelIndex"));
}

// node_modules/echarts/lib/chart/sankey/SankeySeries.js
var SERIES_TYPE_SANKEY = "sankey";
var SankeySeriesModel = (
  /** @class */
  (function(_super) {
    __extends(SankeySeriesModel2, _super);
    function SankeySeriesModel2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = SankeySeriesModel2.type;
      return _this;
    }
    SankeySeriesModel2.prototype.getInitialData = function(option, ecModel) {
      var links = option.edges || option.links || [];
      var nodes = option.data || option.nodes || [];
      var levels = option.levels || [];
      this.levelModels = [];
      var levelModels = this.levelModels;
      for (var i2 = 0; i2 < levels.length; i2++) {
        if (levels[i2].depth != null && levels[i2].depth >= 0) {
          levelModels[levels[i2].depth] = new Model_default(levels[i2], this, ecModel);
        } else {
          if (true) {
            throw new Error("levels[i].depth is mandatory and should be natural number");
          }
        }
      }
      var graph = createGraphFromNodeEdge(nodes, links, this, true, beforeLink);
      return graph.data;
      function beforeLink(nodeData, edgeData) {
        nodeData.wrapMethod("getItemModel", function(model2, idx) {
          var seriesModel = model2.parentModel;
          var layout3 = seriesModel.getData().getItemLayout(idx);
          if (layout3) {
            var nodeDepth = layout3.depth;
            var levelModel = seriesModel.levelModels[nodeDepth];
            if (levelModel) {
              model2.parentModel = levelModel;
            }
          }
          return model2;
        });
        edgeData.wrapMethod("getItemModel", function(model2, idx) {
          var seriesModel = model2.parentModel;
          var edge = seriesModel.getGraph().getEdgeByIndex(idx);
          var layout3 = edge.node1.getLayout();
          if (layout3) {
            var depth = layout3.depth;
            var levelModel = seriesModel.levelModels[depth];
            if (levelModel) {
              model2.parentModel = levelModel;
            }
          }
          return model2;
        });
      }
    };
    SankeySeriesModel2.prototype.setNodePosition = function(dataIndex, localPosition) {
      var nodes = this.option.data || this.option.nodes;
      var dataItem = nodes[dataIndex];
      dataItem.localX = localPosition[0];
      dataItem.localY = localPosition[1];
    };
    SankeySeriesModel2.prototype.getGraph = function() {
      return this.getData().graph;
    };
    SankeySeriesModel2.prototype.getEdgeData = function() {
      return this.getGraph().edgeData;
    };
    SankeySeriesModel2.prototype.formatTooltip = function(dataIndex, multipleSeries, dataType) {
      function noValue(val) {
        return isNaN(val) || val == null;
      }
      if (dataType === "edge") {
        var params = this.getDataParams(dataIndex, dataType);
        var rawDataOpt = params.data;
        var edgeValue = params.value;
        var edgeName = rawDataOpt.source + " -- " + rawDataOpt.target;
        return createTooltipMarkup("nameValue", {
          name: edgeName,
          value: edgeValue,
          noValue: noValue(edgeValue)
        });
      } else {
        var node = this.getGraph().getNodeByIndex(dataIndex);
        var value = node.getLayout().value;
        var name_1 = this.getDataParams(dataIndex, dataType).data.name;
        return createTooltipMarkup("nameValue", {
          name: name_1 != null ? name_1 + "" : null,
          value,
          noValue: noValue(value)
        });
      }
    };
    SankeySeriesModel2.prototype.optionUpdated = function() {
    };
    SankeySeriesModel2.prototype.getDataParams = function(dataIndex, dataType) {
      var params = _super.prototype.getDataParams.call(this, dataIndex, dataType);
      if (params.value == null && dataType === "node") {
        var node = this.getGraph().getNodeByIndex(dataIndex);
        var nodeValue = node.getLayout().value;
        params.value = nodeValue;
      }
      return params;
    };
    SankeySeriesModel2.prototype.__ownRoamView = function() {
      return this.coordinateSystem;
    };
    SankeySeriesModel2.type = "series." + SERIES_TYPE_SANKEY;
    SankeySeriesModel2.layoutMode = "box";
    SankeySeriesModel2.defaultOption = {
      // zlevel: 0,
      z: 2,
      // `coordinateSystem` can be declared as 'matrix', 'calendar',
      //  which provides box layout container.
      coordinateSystemUsage: "box",
      left: "5%",
      top: "5%",
      right: "20%",
      bottom: "5%",
      orient: "horizontal",
      nodeWidth: 20,
      nodeGap: 8,
      draggable: true,
      layoutIterations: 32,
      // true | false | 'move' | 'scale', see module:component/helper/RoamController.
      roam: false,
      roamTrigger: "global",
      center: null,
      zoom: 1,
      label: {
        show: true,
        position: "right",
        fontSize: 12
      },
      edgeLabel: {
        show: false,
        fontSize: 12
      },
      levels: [],
      nodeAlign: "justify",
      lineStyle: {
        color: tokens_default.color.neutral50,
        opacity: 0.2,
        curveness: 0.5
      },
      emphasis: {
        label: {
          show: true
        },
        lineStyle: {
          opacity: 0.5
        }
      },
      select: {
        itemStyle: {
          borderColor: tokens_default.color.primary
        }
      },
      animationEasing: "linear",
      animationDuration: 1e3
    };
    return SankeySeriesModel2;
  })(Series_default)
);

// node_modules/echarts/lib/chart/sankey/SankeyView.js
var SankeyPathShape = (
  /** @class */
  /* @__PURE__ */ (function() {
    function SankeyPathShape2() {
      this.x1 = 0;
      this.y1 = 0;
      this.x2 = 0;
      this.y2 = 0;
      this.cpx1 = 0;
      this.cpy1 = 0;
      this.cpx2 = 0;
      this.cpy2 = 0;
      this.extent = 0;
    }
    return SankeyPathShape2;
  })()
);
var SankeyPath = (
  /** @class */
  (function(_super) {
    __extends(SankeyPath2, _super);
    function SankeyPath2(opts) {
      return _super.call(this, opts) || this;
    }
    SankeyPath2.prototype.getDefaultShape = function() {
      return new SankeyPathShape();
    };
    SankeyPath2.prototype.buildPath = function(ctx, shape) {
      var extent = shape.extent;
      ctx.moveTo(shape.x1, shape.y1);
      ctx.bezierCurveTo(shape.cpx1, shape.cpy1, shape.cpx2, shape.cpy2, shape.x2, shape.y2);
      if (shape.orient === "vertical") {
        ctx.lineTo(shape.x2 + extent, shape.y2);
        ctx.bezierCurveTo(shape.cpx2 + extent, shape.cpy2, shape.cpx1 + extent, shape.cpy1, shape.x1 + extent, shape.y1);
      } else {
        ctx.lineTo(shape.x2, shape.y2 + extent);
        ctx.bezierCurveTo(shape.cpx2, shape.cpy2 + extent, shape.cpx1, shape.cpy1 + extent, shape.x1, shape.y1 + extent);
      }
      ctx.closePath();
    };
    SankeyPath2.prototype.highlight = function() {
      enterEmphasis(this);
    };
    SankeyPath2.prototype.downplay = function() {
      leaveEmphasis(this);
    };
    return SankeyPath2;
  })(Path_default)
);
var SankeyView = (
  /** @class */
  (function(_super) {
    __extends(SankeyView2, _super);
    function SankeyView2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = SERIES_TYPE_SANKEY;
      _this._mainGroup = new Group_default();
      return _this;
    }
    SankeyView2.prototype.init = function(ecModel, api) {
      this._controller = new RoamController_default(api.getZr());
      this.group.add(this._mainGroup);
      this._firstRender = true;
    };
    SankeyView2.prototype.render = function(seriesModel, ecModel, api) {
      var graph = seriesModel.getGraph();
      var mainGroup = this._mainGroup;
      var layoutInfo = seriesModel.layoutInfo;
      var width = layoutInfo.width;
      var height = layoutInfo.height;
      var nodeData = seriesModel.getData();
      var edgeData = seriesModel.getData("edge");
      var orient = seriesModel.get("orient");
      mainGroup.removeAll();
      mainGroup.x = layoutInfo.x;
      mainGroup.y = layoutInfo.y;
      this._updateViewCoordSys(seriesModel, api);
      updateRoamControllerSimply(seriesModel, api, this._controller, createIsInSelfByPointerCheckerEl(mainGroup), null);
      graph.eachEdge(function(edge) {
        var curve = new SankeyPath();
        var ecData = getECData(curve);
        ecData.dataIndex = edge.dataIndex;
        ecData.seriesIndex = seriesModel.seriesIndex;
        ecData.dataType = "edge";
        var edgeModel = edge.getModel();
        var lineStyleModel = edgeModel.getModel("lineStyle");
        var curvature = lineStyleModel.get("curveness");
        var n1Layout = edge.node1.getLayout();
        var node1Model = edge.node1.getModel();
        var dragX1 = node1Model.get("localX");
        var dragY1 = node1Model.get("localY");
        var n2Layout = edge.node2.getLayout();
        var node2Model = edge.node2.getModel();
        var dragX2 = node2Model.get("localX");
        var dragY2 = node2Model.get("localY");
        var edgeLayout = edge.getLayout();
        var x1;
        var y1;
        var x2;
        var y2;
        var cpx1;
        var cpy1;
        var cpx2;
        var cpy2;
        curve.shape.extent = Math.max(1, edgeLayout.dy);
        curve.shape.orient = orient;
        if (orient === "vertical") {
          x1 = (dragX1 != null ? dragX1 * width : n1Layout.x) + edgeLayout.sy;
          y1 = (dragY1 != null ? dragY1 * height : n1Layout.y) + n1Layout.dy;
          x2 = (dragX2 != null ? dragX2 * width : n2Layout.x) + edgeLayout.ty;
          y2 = dragY2 != null ? dragY2 * height : n2Layout.y;
          cpx1 = x1;
          cpy1 = y1 * (1 - curvature) + y2 * curvature;
          cpx2 = x2;
          cpy2 = y1 * curvature + y2 * (1 - curvature);
        } else {
          x1 = (dragX1 != null ? dragX1 * width : n1Layout.x) + n1Layout.dx;
          y1 = (dragY1 != null ? dragY1 * height : n1Layout.y) + edgeLayout.sy;
          x2 = dragX2 != null ? dragX2 * width : n2Layout.x;
          y2 = (dragY2 != null ? dragY2 * height : n2Layout.y) + edgeLayout.ty;
          cpx1 = x1 * (1 - curvature) + x2 * curvature;
          cpy1 = y1;
          cpx2 = x1 * curvature + x2 * (1 - curvature);
          cpy2 = y2;
        }
        curve.setShape({
          x1,
          y1,
          x2,
          y2,
          cpx1,
          cpy1,
          cpx2,
          cpy2
        });
        curve.useStyle(lineStyleModel.getItemStyle());
        applyCurveStyle(curve.style, orient, edge);
        var defaultEdgeLabelText = "" + edgeModel.get("value");
        var edgeLabelStateModels = getLabelStatesModels(edgeModel, "edgeLabel");
        setLabelStyle(curve, edgeLabelStateModels, {
          labelFetcher: {
            getFormattedLabel: function(dataIndex, stateName, dataType, labelDimIndex, formatter, extendParams) {
              return seriesModel.getFormattedLabel(
                dataIndex,
                stateName,
                "edge",
                labelDimIndex,
                // ensure edgeLabel formatter is provided
                // to prevent the inheritance from `label.formatter` of the series
                retrieve3(formatter, edgeLabelStateModels.normal && edgeLabelStateModels.normal.get("formatter"), defaultEdgeLabelText),
                extendParams
              );
            }
          },
          labelDataIndex: edge.dataIndex,
          defaultText: defaultEdgeLabelText
        });
        curve.setTextConfig({
          position: "inside"
        });
        var emphasisModel = edgeModel.getModel("emphasis");
        setStatesStylesFromModel(curve, edgeModel, "lineStyle", function(model2) {
          var style2 = model2.getItemStyle();
          applyCurveStyle(style2, orient, edge);
          return style2;
        });
        mainGroup.add(curve);
        edgeData.setItemGraphicEl(edge.dataIndex, curve);
        var focus = emphasisModel.get("focus");
        toggleHoverEmphasis(curve, focus === "adjacency" ? edge.getAdjacentDataIndices() : focus === "trajectory" ? edge.getTrajectoryDataIndices() : focus, emphasisModel.get("blurScope"), emphasisModel.get("disabled"));
      });
      graph.eachNode(function(node) {
        var layout3 = node.getLayout();
        var itemModel = node.getModel();
        var dragX = itemModel.get("localX");
        var dragY = itemModel.get("localY");
        var emphasisModel = itemModel.getModel("emphasis");
        var borderRadius = itemModel.get(["itemStyle", "borderRadius"]) || 0;
        var rect = new Rect_default({
          shape: {
            x: dragX != null ? dragX * width : layout3.x,
            y: dragY != null ? dragY * height : layout3.y,
            width: layout3.dx,
            height: layout3.dy,
            r: borderRadius
          },
          style: itemModel.getModel("itemStyle").getItemStyle(),
          z2: 10
        });
        setLabelStyle(rect, getLabelStatesModels(itemModel), {
          labelFetcher: {
            getFormattedLabel: function(dataIndex, stateName) {
              return seriesModel.getFormattedLabel(dataIndex, stateName, "node");
            }
          },
          labelDataIndex: node.dataIndex,
          defaultText: node.id
        });
        rect.disableLabelAnimation = true;
        rect.setStyle("fill", node.getVisual("color"));
        rect.setStyle("decal", node.getVisual("style").decal);
        setStatesStylesFromModel(rect, itemModel);
        mainGroup.add(rect);
        nodeData.setItemGraphicEl(node.dataIndex, rect);
        getECData(rect).dataType = "node";
        var focus = emphasisModel.get("focus");
        toggleHoverEmphasis(rect, focus === "adjacency" ? node.getAdjacentDataIndices() : focus === "trajectory" ? node.getTrajectoryDataIndices() : focus, emphasisModel.get("blurScope"), emphasisModel.get("disabled"));
      });
      nodeData.eachItemGraphicEl(function(el, dataIndex) {
        var itemModel = nodeData.getItemModel(dataIndex);
        if (itemModel.get("draggable")) {
          el.drift = function(dx, dy) {
            this.shape.x += dx;
            this.shape.y += dy;
            this.dirty();
            api.dispatchAction({
              type: "dragNode",
              seriesId: seriesModel.id,
              dataIndex: nodeData.getRawIndex(dataIndex),
              localX: this.shape.x / width,
              localY: this.shape.y / height
            });
          };
          el.draggable = true;
          el.cursor = "move";
        }
      });
      if (!this._data && seriesModel.isAnimationEnabled()) {
        mainGroup.setClipPath(createGridClipShape2(mainGroup.getBoundingRect(), seriesModel, function() {
          mainGroup.removeClipPath();
        }));
      }
      this._data = seriesModel.getData();
      this._firstRender = false;
    };
    SankeyView2.prototype.__updateOnOwnRoam = function(payload, seriesModel, api) {
      applyViewCoordSysTransToElement(this.group, VIEW_COORD_SYS_TRANS_OVERALL, seriesModel.coordinateSystem, null);
    };
    SankeyView2.prototype.dispose = function() {
      this._controller && this._controller.dispose();
    };
    SankeyView2.prototype._updateViewCoordSys = function(seriesModel, api) {
      var layoutInfo = seriesModel.layoutInfo;
      var ownCoordSys = seriesModel.coordinateSystem = createViewCoordSysSimply(
        seriesModel,
        api,
        layoutInfo.x,
        layoutInfo.y,
        layoutInfo.width,
        layoutInfo.height
        // 0, 0, layoutInfo.width, layoutInfo.height
      );
      applyViewCoordSysTransToElement(this.group, VIEW_COORD_SYS_TRANS_OVERALL, ownCoordSys, this._firstRender ? null : seriesModel);
    };
    SankeyView2.type = SERIES_TYPE_SANKEY;
    return SankeyView2;
  })(Chart_default)
);
function applyCurveStyle(curveProps, orient, edge) {
  switch (curveProps.fill) {
    case "source":
      curveProps.fill = edge.node1.getVisual("color");
      curveProps.decal = edge.node1.getVisual("style").decal;
      break;
    case "target":
      curveProps.fill = edge.node2.getVisual("color");
      curveProps.decal = edge.node2.getVisual("style").decal;
      break;
    case "gradient":
      var sourceColor = edge.node1.getVisual("color");
      var targetColor = edge.node2.getVisual("color");
      if (isString(sourceColor) && isString(targetColor)) {
        curveProps.fill = new LinearGradient_default(0, 0, +(orient === "horizontal"), +(orient === "vertical"), [{
          color: sourceColor,
          offset: 0
        }, {
          color: targetColor,
          offset: 1
        }]);
      }
  }
}
function createGridClipShape2(rect, seriesModel, cb) {
  var rectEl = new Rect_default({
    shape: {
      x: rect.x - 10,
      y: rect.y - 10,
      width: 0,
      height: rect.height + 20
    }
  });
  initProps(rectEl, {
    shape: {
      width: rect.width + 20
    }
  }, seriesModel, cb);
  return rectEl;
}

// node_modules/echarts/lib/chart/sankey/sankeyLayout.js
var sankeyLayoutStageHandler = createSimpleOverallStageHandler(SERIES_TYPE_SANKEY, sankeyLayout);
function sankeyLayout(ecModel, api) {
  ecModel.eachSeriesByType(SERIES_TYPE_SANKEY, function(seriesModel) {
    var nodeWidth = seriesModel.get("nodeWidth");
    var nodeGap = seriesModel.get("nodeGap");
    var refContainer = createBoxLayoutReference(seriesModel, api).refContainer;
    var layoutInfo = getLayoutRect(seriesModel.getBoxLayoutParams(), refContainer);
    seriesModel.layoutInfo = layoutInfo;
    var width = layoutInfo.width;
    var height = layoutInfo.height;
    var graph = seriesModel.getGraph();
    var nodes = graph.nodes;
    var edges = graph.edges;
    computeNodeValues(nodes);
    var filteredNodes = filter2(nodes, function(node) {
      return node.getLayout().value === 0;
    });
    var iterations = filteredNodes.length !== 0 ? 0 : seriesModel.get("layoutIterations");
    var orient = seriesModel.get("orient");
    var nodeAlign = seriesModel.get("nodeAlign");
    layoutSankey(nodes, edges, nodeWidth, nodeGap, width, height, iterations, orient, nodeAlign);
  });
}
function layoutSankey(nodes, edges, nodeWidth, nodeGap, width, height, iterations, orient, nodeAlign) {
  computeNodeBreadths(nodes, edges, nodeWidth, width, height, orient, nodeAlign);
  computeNodeDepths(nodes, edges, height, width, nodeGap, iterations, orient);
  computeEdgeDepths(nodes, orient);
}
function computeNodeValues(nodes) {
  each(nodes, function(node) {
    var value1 = sum(node.outEdges, getEdgeValue);
    var value2 = sum(node.inEdges, getEdgeValue);
    var nodeRawValue = node.getValue() || 0;
    var value = Math.max(value1, value2, nodeRawValue);
    node.setLayout({
      value
    }, true);
  });
}
function computeNodeBreadths(nodes, edges, nodeWidth, width, height, orient, nodeAlign) {
  var remainEdges = [];
  var indegreeArr = [];
  var zeroIndegrees = [];
  var nextTargetNode = [];
  var x = 0;
  for (var i2 = 0; i2 < edges.length; i2++) {
    remainEdges[i2] = 1;
  }
  for (var i2 = 0; i2 < nodes.length; i2++) {
    indegreeArr[i2] = nodes[i2].inEdges.length;
    if (indegreeArr[i2] === 0) {
      zeroIndegrees.push(nodes[i2]);
    }
  }
  var maxNodeDepth = -1;
  while (zeroIndegrees.length) {
    for (var idx = 0; idx < zeroIndegrees.length; idx++) {
      var node = zeroIndegrees[idx];
      var item = node.hostGraph.data.getRawDataItem(node.dataIndex);
      var isItemDepth = item.depth != null && item.depth >= 0;
      if (isItemDepth && item.depth > maxNodeDepth) {
        maxNodeDepth = item.depth;
      }
      node.setLayout({
        depth: isItemDepth ? item.depth : x
      }, true);
      orient === "vertical" ? node.setLayout({
        dy: nodeWidth
      }, true) : node.setLayout({
        dx: nodeWidth
      }, true);
      for (var edgeIdx = 0; edgeIdx < node.outEdges.length; edgeIdx++) {
        var edge = node.outEdges[edgeIdx];
        var indexEdge = indexOf(edges, edge);
        remainEdges[indexEdge] = 0;
        var targetNode = edge.node2;
        var nodeIndex = indexOf(nodes, targetNode);
        if (--indegreeArr[nodeIndex] === 0 && indexOf(nextTargetNode, targetNode) < 0) {
          nextTargetNode.push(targetNode);
        }
      }
    }
    ++x;
    zeroIndegrees = nextTargetNode;
    nextTargetNode = [];
  }
  for (var i2 = 0; i2 < remainEdges.length; i2++) {
    if (remainEdges[i2] === 1) {
      throw new Error("Sankey is a DAG, the original data has cycle!");
    }
  }
  var maxDepth = maxNodeDepth > x - 1 ? maxNodeDepth : x - 1;
  if (nodeAlign && nodeAlign !== "left") {
    adjustNodeWithNodeAlign(nodes, nodeAlign, orient, maxDepth);
  }
  var kx = orient === "vertical" ? (height - nodeWidth) / maxDepth : (width - nodeWidth) / maxDepth;
  scaleNodeBreadths(nodes, kx, orient);
}
function isNodeDepth(node) {
  var item = node.hostGraph.data.getRawDataItem(node.dataIndex);
  return item.depth != null && item.depth >= 0;
}
function adjustNodeWithNodeAlign(nodes, nodeAlign, orient, maxDepth) {
  if (nodeAlign === "right") {
    var nextSourceNode = [];
    var remainNodes = nodes;
    var nodeHeight = 0;
    while (remainNodes.length) {
      for (var i2 = 0; i2 < remainNodes.length; i2++) {
        var node = remainNodes[i2];
        node.setLayout({
          skNodeHeight: nodeHeight
        }, true);
        for (var j = 0; j < node.inEdges.length; j++) {
          var edge = node.inEdges[j];
          if (indexOf(nextSourceNode, edge.node1) < 0) {
            nextSourceNode.push(edge.node1);
          }
        }
      }
      remainNodes = nextSourceNode;
      nextSourceNode = [];
      ++nodeHeight;
    }
    each(nodes, function(node2) {
      if (!isNodeDepth(node2)) {
        node2.setLayout({
          depth: Math.max(0, maxDepth - node2.getLayout().skNodeHeight)
        }, true);
      }
    });
  } else if (nodeAlign === "justify") {
    moveSinksRight(nodes, maxDepth);
  }
}
function moveSinksRight(nodes, maxDepth) {
  each(nodes, function(node) {
    if (!isNodeDepth(node) && !node.outEdges.length) {
      node.setLayout({
        depth: maxDepth
      }, true);
    }
  });
}
function scaleNodeBreadths(nodes, kx, orient) {
  each(nodes, function(node) {
    var nodeDepth = node.getLayout().depth * kx;
    orient === "vertical" ? node.setLayout({
      y: nodeDepth
    }, true) : node.setLayout({
      x: nodeDepth
    }, true);
  });
}
function computeNodeDepths(nodes, edges, height, width, nodeGap, iterations, orient) {
  var nodesByBreadth = prepareNodesByBreadth(nodes, orient);
  initializeNodeDepth(nodesByBreadth, edges, height, width, nodeGap, orient);
  resolveCollisions(nodesByBreadth, nodeGap, height, width, orient);
  for (var alpha = 1; iterations > 0; iterations--) {
    alpha *= 0.99;
    relaxRightToLeft(nodesByBreadth, alpha, orient);
    resolveCollisions(nodesByBreadth, nodeGap, height, width, orient);
    relaxLeftToRight(nodesByBreadth, alpha, orient);
    resolveCollisions(nodesByBreadth, nodeGap, height, width, orient);
  }
}
function prepareNodesByBreadth(nodes, orient) {
  var nodesByBreadth = [];
  var keyAttr = orient === "vertical" ? "y" : "x";
  var groupResult = groupData(nodes, function(node) {
    return node.getLayout()[keyAttr];
  });
  asc(groupResult.keys);
  each(groupResult.keys, function(key2) {
    nodesByBreadth.push(groupResult.buckets.get(key2));
  });
  return nodesByBreadth;
}
function initializeNodeDepth(nodesByBreadth, edges, height, width, nodeGap, orient) {
  var minKy = Infinity;
  each(nodesByBreadth, function(nodes) {
    var n = nodes.length;
    var sum2 = 0;
    each(nodes, function(node) {
      sum2 += node.getLayout().value;
    });
    var ky = orient === "vertical" ? (width - (n - 1) * nodeGap) / sum2 : (height - (n - 1) * nodeGap) / sum2;
    if (ky < minKy) {
      minKy = ky;
    }
  });
  each(nodesByBreadth, function(nodes) {
    each(nodes, function(node, i2) {
      var nodeDy = node.getLayout().value * minKy;
      if (orient === "vertical") {
        node.setLayout({
          x: i2
        }, true);
        node.setLayout({
          dx: nodeDy
        }, true);
      } else {
        node.setLayout({
          y: i2
        }, true);
        node.setLayout({
          dy: nodeDy
        }, true);
      }
    });
  });
  each(edges, function(edge) {
    var edgeDy = +edge.getValue() * minKy;
    edge.setLayout({
      dy: edgeDy
    }, true);
  });
}
function resolveCollisions(nodesByBreadth, nodeGap, height, width, orient) {
  var keyAttr = orient === "vertical" ? "x" : "y";
  each(nodesByBreadth, function(nodes) {
    nodes.sort(function(a, b) {
      return a.getLayout()[keyAttr] - b.getLayout()[keyAttr];
    });
    var nodeX;
    var node;
    var dy;
    var y0 = 0;
    var n = nodes.length;
    var nodeDyAttr = orient === "vertical" ? "dx" : "dy";
    for (var i2 = 0; i2 < n; i2++) {
      node = nodes[i2];
      dy = y0 - node.getLayout()[keyAttr];
      if (dy > 0) {
        nodeX = node.getLayout()[keyAttr] + dy;
        orient === "vertical" ? node.setLayout({
          x: nodeX
        }, true) : node.setLayout({
          y: nodeX
        }, true);
      }
      y0 = node.getLayout()[keyAttr] + node.getLayout()[nodeDyAttr] + nodeGap;
    }
    var viewWidth = orient === "vertical" ? width : height;
    dy = y0 - nodeGap - viewWidth;
    if (dy > 0) {
      nodeX = node.getLayout()[keyAttr] - dy;
      orient === "vertical" ? node.setLayout({
        x: nodeX
      }, true) : node.setLayout({
        y: nodeX
      }, true);
      y0 = nodeX;
      for (var i2 = n - 2; i2 >= 0; --i2) {
        node = nodes[i2];
        dy = node.getLayout()[keyAttr] + node.getLayout()[nodeDyAttr] + nodeGap - y0;
        if (dy > 0) {
          nodeX = node.getLayout()[keyAttr] - dy;
          orient === "vertical" ? node.setLayout({
            x: nodeX
          }, true) : node.setLayout({
            y: nodeX
          }, true);
        }
        y0 = node.getLayout()[keyAttr];
      }
    }
  });
}
function relaxRightToLeft(nodesByBreadth, alpha, orient) {
  each(nodesByBreadth.slice().reverse(), function(nodes) {
    each(nodes, function(node) {
      if (node.outEdges.length) {
        var y = sum(node.outEdges, weightedTarget, orient) / sum(node.outEdges, getEdgeValue);
        if (isNaN(y)) {
          var len2 = node.outEdges.length;
          y = len2 ? sum(node.outEdges, centerTarget, orient) / len2 : 0;
        }
        if (orient === "vertical") {
          var nodeX = node.getLayout().x + (y - center2(node, orient)) * alpha;
          node.setLayout({
            x: nodeX
          }, true);
        } else {
          var nodeY = node.getLayout().y + (y - center2(node, orient)) * alpha;
          node.setLayout({
            y: nodeY
          }, true);
        }
      }
    });
  });
}
function weightedTarget(edge, orient) {
  return center2(edge.node2, orient) * edge.getValue();
}
function centerTarget(edge, orient) {
  return center2(edge.node2, orient);
}
function weightedSource(edge, orient) {
  return center2(edge.node1, orient) * edge.getValue();
}
function centerSource(edge, orient) {
  return center2(edge.node1, orient);
}
function center2(node, orient) {
  return orient === "vertical" ? node.getLayout().x + node.getLayout().dx / 2 : node.getLayout().y + node.getLayout().dy / 2;
}
function getEdgeValue(edge) {
  return edge.getValue();
}
function sum(array, cb, orient) {
  var sum2 = 0;
  var len2 = array.length;
  var i2 = -1;
  while (++i2 < len2) {
    var value = +cb(array[i2], orient);
    if (!isNaN(value)) {
      sum2 += value;
    }
  }
  return sum2;
}
function relaxLeftToRight(nodesByBreadth, alpha, orient) {
  each(nodesByBreadth, function(nodes) {
    each(nodes, function(node) {
      if (node.inEdges.length) {
        var y = sum(node.inEdges, weightedSource, orient) / sum(node.inEdges, getEdgeValue);
        if (isNaN(y)) {
          var len2 = node.inEdges.length;
          y = len2 ? sum(node.inEdges, centerSource, orient) / len2 : 0;
        }
        if (orient === "vertical") {
          var nodeX = node.getLayout().x + (y - center2(node, orient)) * alpha;
          node.setLayout({
            x: nodeX
          }, true);
        } else {
          var nodeY = node.getLayout().y + (y - center2(node, orient)) * alpha;
          node.setLayout({
            y: nodeY
          }, true);
        }
      }
    });
  });
}
function computeEdgeDepths(nodes, orient) {
  var keyAttr = orient === "vertical" ? "x" : "y";
  each(nodes, function(node) {
    node.outEdges.sort(function(a, b) {
      return a.node2.getLayout()[keyAttr] - b.node2.getLayout()[keyAttr];
    });
    node.inEdges.sort(function(a, b) {
      return a.node1.getLayout()[keyAttr] - b.node1.getLayout()[keyAttr];
    });
  });
  each(nodes, function(node) {
    var sy = 0;
    var ty = 0;
    each(node.outEdges, function(edge) {
      edge.setLayout({
        sy
      }, true);
      sy += edge.getLayout().dy;
    });
    each(node.inEdges, function(edge) {
      edge.setLayout({
        ty
      }, true);
      ty += edge.getLayout().dy;
    });
  });
}

// node_modules/echarts/lib/chart/sankey/sankeyVisual.js
var sankeyVisualStageHandler = createSimpleOverallStageHandler(SERIES_TYPE_SANKEY, sankeyVisual);
function sankeyVisual(ecModel) {
  ecModel.eachSeriesByType(SERIES_TYPE_SANKEY, function(seriesModel) {
    var graph = seriesModel.getGraph();
    var nodes = graph.nodes;
    var edges = graph.edges;
    if (nodes.length) {
      var minValue_1 = Infinity;
      var maxValue_1 = -Infinity;
      each(nodes, function(node) {
        var nodeValue = node.getLayout().value;
        if (nodeValue < minValue_1) {
          minValue_1 = nodeValue;
        }
        if (nodeValue > maxValue_1) {
          maxValue_1 = nodeValue;
        }
      });
      each(nodes, function(node) {
        var mapping = new VisualMapping_default({
          type: "color",
          mappingMethod: "linear",
          dataExtent: [minValue_1, maxValue_1],
          visual: seriesModel.get("color")
        });
        var mapValueToColor = mapping.mapValueToVisual(node.getLayout().value);
        var customColor = node.getModel().get(["itemStyle", "color"]);
        if (customColor != null) {
          node.setVisual("color", customColor);
          node.setVisual("style", {
            fill: customColor
          });
        } else {
          node.setVisual("color", mapValueToColor);
          node.setVisual("style", {
            fill: mapValueToColor
          });
        }
      });
    }
    if (edges.length) {
      each(edges, function(edge) {
        var edgeStyle = edge.getModel().get("lineStyle");
        edge.setVisual("style", edgeStyle);
      });
    }
  });
}

// node_modules/echarts/lib/chart/helper/whiskerBoxCommon.js
var WhiskerBoxCommonMixin = (
  /** @class */
  (function() {
    function WhiskerBoxCommonMixin2() {
    }
    WhiskerBoxCommonMixin2.prototype._hasEncodeRule = function(key2) {
      var encodeRules = this.getEncode();
      return encodeRules && encodeRules.get(key2) != null;
    };
    WhiskerBoxCommonMixin2.prototype.getInitialData = function(option, ecModel) {
      var ordinalMeta;
      var xAxisModel = ecModel.getComponent("xAxis", this.get("xAxisIndex"));
      var yAxisModel = ecModel.getComponent("yAxis", this.get("yAxisIndex"));
      var xAxisType = xAxisModel.get("type");
      var yAxisType = yAxisModel.get("type");
      var addOrdinal;
      var layout3 = option.layout;
      if (xAxisType === "category") {
        layout3 = "horizontal";
        ordinalMeta = xAxisModel.getOrdinalMeta();
        addOrdinal = !this._hasEncodeRule("x");
      } else if (yAxisType === "category") {
        layout3 = "vertical";
        ordinalMeta = yAxisModel.getOrdinalMeta();
        addOrdinal = !this._hasEncodeRule("y");
      }
      if (!layout3) {
        layout3 = yAxisType === "time" ? "vertical" : "horizontal";
      }
      this._layout = layout3;
      var coordDims = ["x", "y"];
      var baseAxisDimIndex = layout3 === "horizontal" ? 0 : 1;
      var baseAxisDim = this._baseAxisDim = coordDims[baseAxisDimIndex];
      var otherAxisDim = coordDims[1 - baseAxisDimIndex];
      var axisModels = [xAxisModel, yAxisModel];
      var baseAxisType = axisModels[baseAxisDimIndex].get("type");
      var otherAxisType = axisModels[1 - baseAxisDimIndex].get("type");
      var data = option.data;
      if (data && addOrdinal) {
        var newOptionData_1 = [];
        each(data, function(item, index) {
          var newItem;
          if (isArray(item)) {
            newItem = item.slice();
            item.unshift(index);
          } else if (isArray(item.value)) {
            newItem = extend({}, item);
            newItem.value = newItem.value.slice();
            item.value.unshift(index);
          } else {
            newItem = item;
          }
          newOptionData_1.push(newItem);
        });
        option.data = newOptionData_1;
      }
      var defaultValueDimensions = this.defaultValueDimensions;
      var coordDimensions = [{
        name: baseAxisDim,
        type: getDimensionTypeByAxis(baseAxisType),
        ordinalMeta,
        otherDims: {
          tooltip: false,
          itemName: 0
        },
        dimsDef: ["base"]
      }, {
        name: otherAxisDim,
        type: getDimensionTypeByAxis(otherAxisType),
        dimsDef: defaultValueDimensions.slice()
      }];
      return createSeriesDataSimply(this, {
        coordDimensions,
        dimensionsCount: defaultValueDimensions.length + 1,
        encodeDefaulter: curry(makeSeriesEncodeForAxisCoordSys, coordDimensions, this)
      });
    };
    WhiskerBoxCommonMixin2.prototype.getBaseAxis = function() {
      var dim = this._baseAxisDim;
      return this.ecModel.getComponent(dim + "Axis", this.get(dim + "AxisIndex")).axis;
    };
    WhiskerBoxCommonMixin2.prototype.getWhiskerBoxesLayout = function() {
      return this._layout;
    };
    return WhiskerBoxCommonMixin2;
  })()
);
function resolveNormalBoxClipping(clipArea, itemLayout) {
  var count2 = itemLayout.ends.length;
  var containCount = 0;
  for (var i2 = 0; i2 < count2; i2++) {
    if (clipArea.contain(itemLayout.ends[i2][0], itemLayout.ends[i2][1])) {
      containCount++;
    }
  }
  return !containCount ? SHAPE_CLIP_KIND_FULLY_CLIPPED : containCount < count2 ? SHAPE_CLIP_KIND_PARTIALLY_CLIPPED : SHAPE_CLIP_KIND_NOT_CLIPPED;
}

// node_modules/echarts/lib/chart/boxplot/BoxplotSeries.js
var SERIES_TYPE_BOXPLOT = "boxplot";
var BoxplotSeriesModel = (
  /** @class */
  (function(_super) {
    __extends(BoxplotSeriesModel2, _super);
    function BoxplotSeriesModel2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = BoxplotSeriesModel2.type;
      _this.defaultValueDimensions = [{
        name: "min",
        defaultTooltip: true
      }, {
        name: "Q1",
        defaultTooltip: true
      }, {
        name: "median",
        defaultTooltip: true
      }, {
        name: "Q3",
        defaultTooltip: true
      }, {
        name: "max",
        defaultTooltip: true
      }];
      _this.visualDrawType = "stroke";
      return _this;
    }
    BoxplotSeriesModel2.type = "series." + SERIES_TYPE_BOXPLOT;
    BoxplotSeriesModel2.dependencies = ["xAxis", "yAxis", "grid"];
    BoxplotSeriesModel2.defaultOption = {
      // zlevel: 0,
      z: 2,
      coordinateSystem: "cartesian2d",
      legendHoverLink: true,
      layout: null,
      clip: true,
      boxWidth: [7, 50],
      itemStyle: {
        color: tokens_default.color.neutral00,
        borderWidth: 1
      },
      emphasis: {
        scale: true,
        itemStyle: {
          borderWidth: 2,
          shadowBlur: 5,
          shadowOffsetX: 1,
          shadowOffsetY: 1,
          shadowColor: tokens_default.color.shadow
        }
      },
      animationDuration: 800
    };
    return BoxplotSeriesModel2;
  })(Series_default)
);
mixin(BoxplotSeriesModel, WhiskerBoxCommonMixin, true);

// node_modules/echarts/lib/chart/boxplot/BoxplotView.js
var BoxplotView = (
  /** @class */
  (function(_super) {
    __extends(BoxplotView2, _super);
    function BoxplotView2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = SERIES_TYPE_BOXPLOT;
      return _this;
    }
    BoxplotView2.prototype.render = function(seriesModel, ecModel, api) {
      var data = seriesModel.getData();
      var group2 = this.group;
      var oldData = this._data;
      if (!this._data) {
        group2.removeAll();
      }
      var constDim = seriesModel.getWhiskerBoxesLayout() === "horizontal" ? 1 : 0;
      var needClip = seriesModel.get("clip", true);
      var coordSys = seriesModel.coordinateSystem;
      var clipArea = coordSys.getArea && coordSys.getArea();
      var clipPath = needClip && createClipPath(coordSys, false, seriesModel);
      data.diff(oldData).add(function(newIdx) {
        if (data.hasValue(newIdx)) {
          var itemLayout = data.getItemLayout(newIdx);
          var clipKind = needClip ? resolveNormalBoxClipping(clipArea, itemLayout) : SHAPE_CLIP_KIND_NOT_CLIPPED;
          if (clipKind === SHAPE_CLIP_KIND_FULLY_CLIPPED) {
            return;
          }
          var symbolEl = createNormalBox(itemLayout, data, newIdx, constDim, true);
          updateClipPath(clipKind === SHAPE_CLIP_KIND_PARTIALLY_CLIPPED, symbolEl, clipPath);
          data.setItemGraphicEl(newIdx, symbolEl);
          group2.add(symbolEl);
        }
      }).update(function(newIdx, oldIdx) {
        var symbolEl = oldData.getItemGraphicEl(oldIdx);
        if (!data.hasValue(newIdx)) {
          group2.remove(symbolEl);
          return;
        }
        var itemLayout = data.getItemLayout(newIdx);
        var clipKind = needClip ? resolveNormalBoxClipping(clipArea, itemLayout) : SHAPE_CLIP_KIND_NOT_CLIPPED;
        if (clipKind === SHAPE_CLIP_KIND_FULLY_CLIPPED) {
          group2.remove(symbolEl);
          return;
        }
        if (!symbolEl) {
          symbolEl = createNormalBox(itemLayout, data, newIdx, constDim);
        } else {
          saveOldStyle(symbolEl);
          updateNormalBoxData(itemLayout, symbolEl, data, newIdx);
        }
        updateClipPath(clipKind === SHAPE_CLIP_KIND_PARTIALLY_CLIPPED, symbolEl, clipPath);
        group2.add(symbolEl);
        data.setItemGraphicEl(newIdx, symbolEl);
      }).remove(function(oldIdx) {
        var el = oldData.getItemGraphicEl(oldIdx);
        el && group2.remove(el);
      }).execute();
      this._data = data;
    };
    BoxplotView2.prototype.remove = function(ecModel) {
      var group2 = this.group;
      var data = this._data;
      this._data = null;
      data && data.eachItemGraphicEl(function(el) {
        el && group2.remove(el);
      });
    };
    BoxplotView2.type = SERIES_TYPE_BOXPLOT;
    return BoxplotView2;
  })(Chart_default)
);
var BoxPathShape = (
  /** @class */
  /* @__PURE__ */ (function() {
    function BoxPathShape2() {
    }
    return BoxPathShape2;
  })()
);
var BoxPath = (
  /** @class */
  (function(_super) {
    __extends(BoxPath2, _super);
    function BoxPath2(opts) {
      var _this = _super.call(this, opts) || this;
      _this.type = "boxplotBoxPath";
      return _this;
    }
    BoxPath2.prototype.getDefaultShape = function() {
      return new BoxPathShape();
    };
    BoxPath2.prototype.buildPath = function(ctx, shape) {
      var ends = shape.points;
      var i2 = 0;
      ctx.moveTo(ends[i2][0], ends[i2][1]);
      i2++;
      for (; i2 < 4; i2++) {
        ctx.lineTo(ends[i2][0], ends[i2][1]);
      }
      ctx.closePath();
      for (; i2 < ends.length; i2++) {
        ctx.moveTo(ends[i2][0], ends[i2][1]);
        i2++;
        ctx.lineTo(ends[i2][0], ends[i2][1]);
      }
    };
    return BoxPath2;
  })(Path_default)
);
function createNormalBox(itemLayout, data, dataIndex, constDim, isInit) {
  var ends = itemLayout.ends;
  var el = new BoxPath({
    shape: {
      points: isInit ? transInit(ends, constDim, itemLayout) : ends
    }
  });
  updateNormalBoxData(itemLayout, el, data, dataIndex, isInit);
  return el;
}
function updateNormalBoxData(itemLayout, el, data, dataIndex, isInit) {
  var seriesModel = data.hostModel;
  var updateMethod = graphic_exports[isInit ? "initProps" : "updateProps"];
  updateMethod(el, {
    shape: {
      points: itemLayout.ends
    }
  }, seriesModel, dataIndex);
  el.useStyle(data.getItemVisual(dataIndex, "style"));
  el.style.strokeNoScale = true;
  el.z2 = 100;
  var itemModel = data.getItemModel(dataIndex);
  var emphasisModel = itemModel.getModel("emphasis");
  setStatesStylesFromModel(el, itemModel);
  toggleHoverEmphasis(el, emphasisModel.get("focus"), emphasisModel.get("blurScope"), emphasisModel.get("disabled"));
}
function transInit(points4, dim, itemLayout) {
  return map2(points4, function(point) {
    point = point.slice();
    point[dim] = itemLayout.initBaseline;
    return point;
  });
}

// node_modules/echarts/lib/chart/boxplot/boxplotLayout.js
var callOnlyOnce3 = makeCallOnlyOnce();
var boxplotLayoutStageHandler = createSimpleOverallStageHandler(SERIES_TYPE_BOXPLOT, boxplotLayout);
function boxplotLayout(ecModel) {
  var axisStatKey = makeAxisStatKey(SERIES_TYPE_BOXPLOT);
  eachAxisOnKey(ecModel, axisStatKey, function(axis) {
    var seriesCount = countSeriesOnAxisOnKey(axis, axisStatKey);
    if (!seriesCount) {
      return;
    }
    var baseResult = calculateBase(axis, seriesCount);
    eachSeriesOnAxisOnKey(axis, axisStatKey, function(seriesModel) {
      var seriesIndex = seriesModel.seriesIndex;
      layoutSingleSeries(seriesModel, baseResult.boxOffsetList[seriesIndex], baseResult.boxWidthList[seriesIndex]);
    });
  });
}
function calculateBase(baseAxis, seriesCount) {
  var boxWidthList = [];
  var boxOffsetList = [];
  var boundList = [];
  var bandWidth = calcBandWidth(baseAxis, {
    fromStat: {
      key: makeAxisStatKey(SERIES_TYPE_BOXPLOT)
    },
    min: 1
  }).w;
  eachSeriesOnAxisOnKey(baseAxis, makeAxisStatKey(SERIES_TYPE_BOXPLOT), function(seriesModel) {
    var boxWidthBound = seriesModel.get("boxWidth");
    if (!isArray(boxWidthBound)) {
      boxWidthBound = [boxWidthBound, boxWidthBound];
    }
    boundList[seriesModel.seriesIndex] = [parsePercent2(boxWidthBound[0], bandWidth) || 0, parsePercent2(boxWidthBound[1], bandWidth) || 0];
  });
  var availableWidth = bandWidth * 0.8 - 2;
  var boxGap = availableWidth / seriesCount * 0.3;
  var boxWidth = (availableWidth - boxGap * (seriesCount - 1)) / seriesCount;
  var base2 = boxWidth / 2 - availableWidth / 2;
  eachSeriesOnAxisOnKey(baseAxis, makeAxisStatKey(SERIES_TYPE_BOXPLOT), function(seriesModel) {
    var seriesIndex = seriesModel.seriesIndex;
    boxOffsetList[seriesIndex] = base2;
    base2 += boxGap + boxWidth;
    boxWidthList[seriesIndex] = mathMin4(mathMax4(boxWidth, boundList[seriesIndex][0]), boundList[seriesIndex][1]);
  });
  return {
    boxOffsetList,
    boxWidthList
  };
}
function layoutSingleSeries(seriesModel, offset, boxWidth) {
  var coordSys = seriesModel.coordinateSystem;
  var data = seriesModel.getData();
  var halfWidth = boxWidth / 2;
  var cDimIdx = seriesModel.getWhiskerBoxesLayout() === "horizontal" ? 0 : 1;
  var vDimIdx = 1 - cDimIdx;
  var coordDims = ["x", "y"];
  var cDim = data.mapDimension(coordDims[cDimIdx]);
  var vDims = data.mapDimensionsAll(coordDims[vDimIdx]);
  if (cDim == null || vDims.length < 5) {
    return;
  }
  for (var dataIndex = 0; dataIndex < data.count(); dataIndex++) {
    var axisDimVal = data.get(cDim, dataIndex);
    var median = getPoint(axisDimVal, vDims[2], dataIndex);
    var end1 = getPoint(axisDimVal, vDims[0], dataIndex);
    var end2 = getPoint(axisDimVal, vDims[1], dataIndex);
    var end4 = getPoint(axisDimVal, vDims[3], dataIndex);
    var end5 = getPoint(axisDimVal, vDims[4], dataIndex);
    var ends = [];
    addBodyEnd(ends, end2, false);
    addBodyEnd(ends, end4, true);
    ends.push(end1, end2, end5, end4);
    layEndLine(ends, end1);
    layEndLine(ends, end5);
    layEndLine(ends, median);
    data.setItemLayout(dataIndex, {
      initBaseline: median[vDimIdx],
      ends
    });
  }
  function getPoint(axisDimVal2, dim, dataIndex2) {
    var val = data.get(dim, dataIndex2);
    var p = [];
    p[cDimIdx] = axisDimVal2;
    p[vDimIdx] = val;
    var point;
    if (isNaN(axisDimVal2) || isNaN(val)) {
      point = [NaN, NaN];
    } else {
      point = coordSys.dataToPoint(p);
      point[cDimIdx] += offset;
    }
    return point;
  }
  function addBodyEnd(ends2, point, start2) {
    var point1 = point.slice();
    var point2 = point.slice();
    point1[cDimIdx] += halfWidth;
    point2[cDimIdx] -= halfWidth;
    start2 ? ends2.push(point1, point2) : ends2.push(point2, point1);
  }
  function layEndLine(ends2, endCenter) {
    var from = endCenter.slice();
    var to = endCenter.slice();
    from[cDimIdx] -= halfWidth;
    to[cDimIdx] += halfWidth;
    ends2.push(from, to);
  }
}

// node_modules/echarts/lib/chart/candlestick/CandlestickSeries.js
var SERIES_TYPE_CANDLESTICK = "candlestick";
var CandlestickSeriesModel = (
  /** @class */
  (function(_super) {
    __extends(CandlestickSeriesModel2, _super);
    function CandlestickSeriesModel2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = CandlestickSeriesModel2.type;
      _this.defaultValueDimensions = [{
        name: "open",
        defaultTooltip: true
      }, {
        name: "close",
        defaultTooltip: true
      }, {
        name: "lowest",
        defaultTooltip: true
      }, {
        name: "highest",
        defaultTooltip: true
      }];
      return _this;
    }
    CandlestickSeriesModel2.prototype.getShadowDim = function() {
      return "open";
    };
    CandlestickSeriesModel2.prototype.brushSelector = function(dataIndex, data, selectors) {
      var itemLayout = data.getItemLayout(dataIndex);
      return itemLayout && selectors.rect(itemLayout.brushRect);
    };
    CandlestickSeriesModel2.type = "series." + SERIES_TYPE_CANDLESTICK;
    CandlestickSeriesModel2.dependencies = ["xAxis", "yAxis", "grid"];
    CandlestickSeriesModel2.defaultOption = {
      // zlevel: 0,
      z: 2,
      coordinateSystem: "cartesian2d",
      legendHoverLink: true,
      // xAxisIndex: 0,
      // yAxisIndex: 0,
      layout: null,
      clip: true,
      itemStyle: {
        color: "#eb5454",
        color0: "#47b262",
        borderColor: "#eb5454",
        borderColor0: "#47b262",
        borderColorDoji: null,
        // borderColor: '#d24040',
        // borderColor0: '#398f4f',
        borderWidth: 1
      },
      emphasis: {
        itemStyle: {
          borderWidth: 2
        }
      },
      barMaxWidth: null,
      barMinWidth: null,
      barWidth: null,
      large: true,
      largeThreshold: 600,
      progressive: 3e3,
      progressiveThreshold: 1e4,
      progressiveChunkMode: "mod",
      animationEasing: "linear",
      animationDuration: 300
    };
    return CandlestickSeriesModel2;
  })(Series_default)
);
mixin(CandlestickSeriesModel, WhiskerBoxCommonMixin, true);

// node_modules/echarts/lib/chart/candlestick/candlestickVisual.js
var positiveBorderColorQuery = ["itemStyle", "borderColor"];
var negativeBorderColorQuery = ["itemStyle", "borderColor0"];
var dojiBorderColorQuery = ["itemStyle", "borderColorDoji"];
var positiveColorQuery = ["itemStyle", "color"];
var negativeColorQuery = ["itemStyle", "color0"];
function getColor(sign, model2) {
  return model2.get(sign > 0 ? positiveColorQuery : negativeColorQuery);
}
function getBorderColor(sign, model2) {
  return model2.get(sign === 0 ? dojiBorderColorQuery : sign > 0 ? positiveBorderColorQuery : negativeBorderColorQuery);
}
var candlestickVisual = {
  seriesType: SERIES_TYPE_CANDLESTICK,
  plan: createRenderPlanner(),
  // For legend.
  performRawSeries: true,
  reset: function(seriesModel, ecModel) {
    if (ecModel.isSeriesFiltered(seriesModel)) {
      return;
    }
    var isLargeRender = seriesModel.pipelineContext.large;
    return !isLargeRender && {
      progress: function(params, data) {
        var dataIndex;
        while ((dataIndex = params.next()) != null) {
          var itemModel = data.getItemModel(dataIndex);
          var sign = data.getItemLayout(dataIndex).sign;
          var style2 = itemModel.getItemStyle();
          style2.fill = getColor(sign, itemModel);
          style2.stroke = getBorderColor(sign, itemModel) || style2.fill;
          var existsStyle = data.ensureUniqueItemVisual(dataIndex, "style");
          extend(existsStyle, style2);
        }
      }
    };
  }
};

// node_modules/echarts/lib/chart/candlestick/CandlestickView.js
var SKIP_PROPS = ["color", "borderColor"];
var CandlestickView = (
  /** @class */
  (function(_super) {
    __extends(CandlestickView2, _super);
    function CandlestickView2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = CandlestickView2.type;
      return _this;
    }
    CandlestickView2.prototype.render = function(seriesModel, ecModel, api) {
      this.group.removeClipPath();
      this._progressiveEls = null;
      this._updateDrawMode(seriesModel);
      this._isLargeDraw ? this._renderLarge(seriesModel) : this._renderNormal(seriesModel);
    };
    CandlestickView2.prototype.incrementalPrepareRender = function(seriesModel, ecModel, api) {
      this._clear();
      this._updateDrawMode(seriesModel);
    };
    CandlestickView2.prototype.incrementalRender = function(params, seriesModel, ecModel, api) {
      this._progressiveEls = [];
      this._isLargeDraw ? this._incrementalRenderLarge(params, seriesModel) : this._incrementalRenderNormal(params, seriesModel);
    };
    CandlestickView2.prototype.eachRendered = function(cb) {
      traverseElements(this._progressiveEls || this.group, cb);
    };
    CandlestickView2.prototype._updateDrawMode = function(seriesModel) {
      var isLargeDraw = seriesModel.pipelineContext.large;
      if (this._isLargeDraw == null || isLargeDraw !== this._isLargeDraw) {
        this._isLargeDraw = isLargeDraw;
        this._clear();
      }
    };
    CandlestickView2.prototype._renderNormal = function(seriesModel) {
      var data = seriesModel.getData();
      var oldData = this._data;
      var group2 = this.group;
      var isSimpleBox = data.getLayout("isSimpleBox");
      var needClip = seriesModel.get("clip", true);
      var coordSys = seriesModel.coordinateSystem;
      var clipArea = coordSys.getArea && coordSys.getArea();
      var clipPath = needClip && createClipPath(coordSys, false, seriesModel);
      if (!this._data) {
        group2.removeAll();
      }
      var transPointDim = getTransPointDimension(seriesModel);
      data.diff(oldData).add(function(newIdx) {
        if (data.hasValue(newIdx)) {
          var itemLayout = data.getItemLayout(newIdx);
          var clipKind = needClip ? resolveNormalBoxClipping(clipArea, itemLayout) : SHAPE_CLIP_KIND_NOT_CLIPPED;
          if (clipKind === SHAPE_CLIP_KIND_FULLY_CLIPPED) {
            return;
          }
          var el = createNormalBox2(itemLayout, newIdx, transPointDim, true);
          initProps(el, {
            shape: {
              points: itemLayout.ends
            }
          }, seriesModel, newIdx);
          updateClipPath(clipKind === SHAPE_CLIP_KIND_PARTIALLY_CLIPPED, el, clipPath);
          setBoxCommon(el, data, newIdx, isSimpleBox);
          group2.add(el);
          data.setItemGraphicEl(newIdx, el);
        }
      }).update(function(newIdx, oldIdx) {
        var el = oldData.getItemGraphicEl(oldIdx);
        if (!data.hasValue(newIdx)) {
          group2.remove(el);
          return;
        }
        var itemLayout = data.getItemLayout(newIdx);
        var clipKind = needClip ? resolveNormalBoxClipping(clipArea, itemLayout) : SHAPE_CLIP_KIND_NOT_CLIPPED;
        if (clipKind === SHAPE_CLIP_KIND_FULLY_CLIPPED) {
          group2.remove(el);
          return;
        }
        if (!el) {
          el = createNormalBox2(itemLayout, newIdx, transPointDim);
        } else {
          updateProps(el, {
            shape: {
              points: itemLayout.ends
            }
          }, seriesModel, newIdx);
          saveOldStyle(el);
        }
        setBoxCommon(el, data, newIdx, isSimpleBox);
        updateClipPath(clipKind === SHAPE_CLIP_KIND_PARTIALLY_CLIPPED, el, clipPath);
        group2.add(el);
        data.setItemGraphicEl(newIdx, el);
      }).remove(function(oldIdx) {
        var el = oldData.getItemGraphicEl(oldIdx);
        el && group2.remove(el);
      }).execute();
      this._data = data;
    };
    CandlestickView2.prototype._renderLarge = function(seriesModel) {
      this._clear();
      createLarge2(seriesModel, this.group);
      var clipPath = seriesModel.get("clip", true) ? createClipPath(seriesModel.coordinateSystem, false, seriesModel) : null;
      updateClipPath(!!clipPath, this.group, clipPath);
    };
    CandlestickView2.prototype._incrementalRenderNormal = function(params, seriesModel) {
      var data = seriesModel.getData();
      var isSimpleBox = data.getLayout("isSimpleBox");
      var transPointDim = getTransPointDimension(seriesModel);
      var dataIndex;
      while ((dataIndex = params.next()) != null) {
        var itemLayout = data.getItemLayout(dataIndex);
        var el = createNormalBox2(itemLayout, dataIndex, transPointDim);
        setBoxCommon(el, data, dataIndex, isSimpleBox);
        el.incremental = getIncrementalId(seriesModel);
        this.group.add(el);
        this._progressiveEls.push(el);
      }
    };
    CandlestickView2.prototype._incrementalRenderLarge = function(params, seriesModel) {
      createLarge2(seriesModel, this.group, this._progressiveEls, true);
    };
    CandlestickView2.prototype.remove = function(ecModel) {
      this._clear();
    };
    CandlestickView2.prototype._clear = function() {
      this.group.removeAll();
      updateClipPath(false, this.group, null);
      this._data = null;
    };
    CandlestickView2.type = SERIES_TYPE_CANDLESTICK;
    return CandlestickView2;
  })(Chart_default)
);
var NormalBoxPathShape = (
  /** @class */
  /* @__PURE__ */ (function() {
    function NormalBoxPathShape2() {
    }
    return NormalBoxPathShape2;
  })()
);
var NormalBoxPath = (
  /** @class */
  (function(_super) {
    __extends(NormalBoxPath2, _super);
    function NormalBoxPath2(opts) {
      var _this = _super.call(this, opts) || this;
      _this.type = "normalCandlestickBox";
      return _this;
    }
    NormalBoxPath2.prototype.getDefaultShape = function() {
      return new NormalBoxPathShape();
    };
    NormalBoxPath2.prototype.buildPath = function(ctx, shape) {
      var ends = shape.points;
      if (this.__simpleBox) {
        ctx.moveTo(ends[4][0], ends[4][1]);
        ctx.lineTo(ends[6][0], ends[6][1]);
      } else {
        ctx.moveTo(ends[0][0], ends[0][1]);
        ctx.lineTo(ends[1][0], ends[1][1]);
        ctx.lineTo(ends[2][0], ends[2][1]);
        ctx.lineTo(ends[3][0], ends[3][1]);
        ctx.closePath();
        ctx.moveTo(ends[4][0], ends[4][1]);
        ctx.lineTo(ends[5][0], ends[5][1]);
        ctx.moveTo(ends[6][0], ends[6][1]);
        ctx.lineTo(ends[7][0], ends[7][1]);
      }
    };
    return NormalBoxPath2;
  })(Path_default)
);
function createNormalBox2(itemLayout, dataIndex, constDim, isInit) {
  var ends = itemLayout.ends;
  return new NormalBoxPath({
    shape: {
      points: isInit ? transInit2(ends, constDim, itemLayout) : ends
    },
    z2: 100
  });
}
function setBoxCommon(el, data, dataIndex, isSimpleBox) {
  var itemModel = data.getItemModel(dataIndex);
  el.useStyle(data.getItemVisual(dataIndex, "style"));
  el.style.strokeNoScale = true;
  var cursorStyle = itemModel.getShallow("cursor");
  cursorStyle && el.attr("cursor", cursorStyle);
  el.__simpleBox = isSimpleBox;
  setStatesStylesFromModel(el, itemModel);
  var sign = data.getItemLayout(dataIndex).sign;
  each(el.states, function(state2, stateName) {
    var stateModel = itemModel.getModel(stateName);
    var color3 = getColor(sign, stateModel);
    var borderColor = getBorderColor(sign, stateModel) || color3;
    var stateStyle = state2.style || (state2.style = {});
    color3 && (stateStyle.fill = color3);
    borderColor && (stateStyle.stroke = borderColor);
  });
  var emphasisModel = itemModel.getModel("emphasis");
  toggleHoverEmphasis(el, emphasisModel.get("focus"), emphasisModel.get("blurScope"), emphasisModel.get("disabled"));
}
function transInit2(points4, dim, itemLayout) {
  return map2(points4, function(point) {
    point = point.slice();
    point[dim] = itemLayout.initBaseline;
    return point;
  });
}
function getTransPointDimension(seriesModel) {
  return seriesModel.getWhiskerBoxesLayout() === "horizontal" ? 1 : 0;
}
var LargeBoxPathShape = (
  /** @class */
  /* @__PURE__ */ (function() {
    function LargeBoxPathShape2() {
    }
    return LargeBoxPathShape2;
  })()
);
var LargeBoxPath = (
  /** @class */
  (function(_super) {
    __extends(LargeBoxPath2, _super);
    function LargeBoxPath2(opts) {
      var _this = _super.call(this, opts) || this;
      _this.type = "largeCandlestickBox";
      return _this;
    }
    LargeBoxPath2.prototype.getDefaultShape = function() {
      return new LargeBoxPathShape();
    };
    LargeBoxPath2.prototype.buildPath = function(ctx, shape) {
      var points4 = shape.points;
      for (var i2 = 0; i2 < points4.length; ) {
        if (this.__sign === points4[i2++]) {
          var x = points4[i2++];
          ctx.moveTo(x, points4[i2++]);
          ctx.lineTo(x, points4[i2++]);
        } else {
          i2 += 3;
        }
      }
    };
    return LargeBoxPath2;
  })(Path_default)
);
function createLarge2(seriesModel, group2, progressiveEls, incremental) {
  var data = seriesModel.getData();
  var largePoints = data.getLayout("largePoints");
  var elP = new LargeBoxPath({
    shape: {
      points: largePoints
    },
    __sign: 1,
    ignoreCoarsePointer: true
  });
  group2.add(elP);
  var elN = new LargeBoxPath({
    shape: {
      points: largePoints
    },
    __sign: -1,
    ignoreCoarsePointer: true
  });
  group2.add(elN);
  var elDoji = new LargeBoxPath({
    shape: {
      points: largePoints
    },
    __sign: 0,
    ignoreCoarsePointer: true
  });
  group2.add(elDoji);
  setLargeStyle(1, elP, seriesModel, data);
  setLargeStyle(-1, elN, seriesModel, data);
  setLargeStyle(0, elDoji, seriesModel, data);
  if (incremental) {
    elP.incremental = getIncrementalId(seriesModel);
    elN.incremental = getIncrementalId(seriesModel);
    elDoji.incremental = getIncrementalId(seriesModel);
  }
  if (progressiveEls) {
    progressiveEls.push(elP, elN, elDoji);
  }
}
function setLargeStyle(sign, el, seriesModel, data) {
  var borderColor = getBorderColor(sign, seriesModel) || getColor(sign, seriesModel);
  var itemStyle = seriesModel.getModel("itemStyle").getItemStyle(SKIP_PROPS);
  el.useStyle(itemStyle);
  el.style.fill = null;
  el.style.stroke = borderColor;
  var cursorStyle = seriesModel.get("cursor");
  cursorStyle && el.attr("cursor", cursorStyle);
}

// node_modules/echarts/lib/chart/candlestick/candlestickLayout.js
var callOnlyOnce4 = makeCallOnlyOnce();
var candlestickLayout = {
  seriesType: SERIES_TYPE_CANDLESTICK,
  plan: createRenderPlanner(),
  reset: function(seriesModel) {
    var coordSys = seriesModel.coordinateSystem;
    var data = seriesModel.getData();
    var candleWidth = calculateCandleWidth(seriesModel, data);
    var cDimIdx = seriesModel.getWhiskerBoxesLayout() === "horizontal" ? 0 : 1;
    var vDimIdx = 1 - cDimIdx;
    var coordDims = ["x", "y"];
    var cDimI = data.getDimensionIndex(data.mapDimension(coordDims[cDimIdx]));
    var vDimsI = map2(data.mapDimensionsAll(coordDims[vDimIdx]), data.getDimensionIndex, data);
    var openDimI = vDimsI[0];
    var closeDimI = vDimsI[1];
    var lowestDimI = vDimsI[2];
    var highestDimI = vDimsI[3];
    data.setLayout({
      candleWidth,
      // The value is experimented visually.
      isSimpleBox: candleWidth <= 1.3
    });
    if (cDimI < 0 || vDimsI.length < 4) {
      return;
    }
    return {
      progress: seriesModel.pipelineContext.large ? largeProgress : normalProgress
    };
    function normalProgress(params, data2) {
      var dataIndex;
      var store = data2.getStore();
      while ((dataIndex = params.next()) != null) {
        var axisDimVal = store.get(cDimI, dataIndex);
        var openVal = store.get(openDimI, dataIndex);
        var closeVal = store.get(closeDimI, dataIndex);
        var lowestVal = store.get(lowestDimI, dataIndex);
        var highestVal = store.get(highestDimI, dataIndex);
        var ocLow = mathMin4(openVal, closeVal);
        var ocHigh = mathMax4(openVal, closeVal);
        var ocLowPoint = getPoint(ocLow, axisDimVal);
        var ocHighPoint = getPoint(ocHigh, axisDimVal);
        var lowestPoint = getPoint(lowestVal, axisDimVal);
        var highestPoint = getPoint(highestVal, axisDimVal);
        var ends = [];
        addBodyEnd(ends, ocHighPoint, 0);
        addBodyEnd(ends, ocLowPoint, 1);
        ends.push(subPixelOptimizePoint(highestPoint), subPixelOptimizePoint(ocHighPoint), subPixelOptimizePoint(lowestPoint), subPixelOptimizePoint(ocLowPoint));
        var itemModel = data2.getItemModel(dataIndex);
        var hasDojiColor = !!itemModel.get(["itemStyle", "borderColorDoji"]);
        data2.setItemLayout(dataIndex, {
          sign: getSign(store, dataIndex, openVal, closeVal, closeDimI, hasDojiColor),
          initBaseline: openVal > closeVal ? ocHighPoint[vDimIdx] : ocLowPoint[vDimIdx],
          ends,
          brushRect: makeBrushRect(lowestVal, highestVal, axisDimVal)
        });
      }
      function getPoint(val, axisDimVal2) {
        var p = [];
        p[cDimIdx] = axisDimVal2;
        p[vDimIdx] = val;
        return isNaN(axisDimVal2) || isNaN(val) ? [NaN, NaN] : coordSys.dataToPoint(p);
      }
      function addBodyEnd(ends2, point, start2) {
        var point1 = point.slice();
        var point2 = point.slice();
        point1[cDimIdx] = subPixelOptimize2(point1[cDimIdx] + candleWidth / 2, 1, false);
        point2[cDimIdx] = subPixelOptimize2(point2[cDimIdx] - candleWidth / 2, 1, true);
        start2 ? ends2.push(point1, point2) : ends2.push(point2, point1);
      }
      function makeBrushRect(lowestVal2, highestVal2, axisDimVal2) {
        var pmin = getPoint(lowestVal2, axisDimVal2);
        var pmax = getPoint(highestVal2, axisDimVal2);
        pmin[cDimIdx] -= candleWidth / 2;
        pmax[cDimIdx] -= candleWidth / 2;
        return {
          x: pmin[0],
          y: pmin[1],
          width: vDimIdx ? candleWidth : pmax[0] - pmin[0],
          height: vDimIdx ? pmax[1] - pmin[1] : candleWidth
        };
      }
      function subPixelOptimizePoint(point) {
        point[cDimIdx] = subPixelOptimize2(point[cDimIdx], 1);
        return point;
      }
    }
    function largeProgress(params, data2) {
      var points4 = createFloat32Array(params.count * 4);
      var offset = 0;
      var point;
      var tmpIn = [];
      var tmpOut = [];
      var dataIndex;
      var store = data2.getStore();
      var hasDojiColor = !!seriesModel.get(["itemStyle", "borderColorDoji"]);
      while ((dataIndex = params.next()) != null) {
        var axisDimVal = store.get(cDimI, dataIndex);
        var openVal = store.get(openDimI, dataIndex);
        var closeVal = store.get(closeDimI, dataIndex);
        var lowestVal = store.get(lowestDimI, dataIndex);
        var highestVal = store.get(highestDimI, dataIndex);
        if (isNaN(axisDimVal) || isNaN(lowestVal) || isNaN(highestVal)) {
          points4[offset++] = NaN;
          offset += 3;
          continue;
        }
        points4[offset++] = getSign(store, dataIndex, openVal, closeVal, closeDimI, hasDojiColor);
        tmpIn[cDimIdx] = axisDimVal;
        tmpIn[vDimIdx] = lowestVal;
        point = coordSys.dataToPoint(tmpIn, null, tmpOut);
        points4[offset++] = point ? point[0] : NaN;
        points4[offset++] = point ? point[1] : NaN;
        tmpIn[vDimIdx] = highestVal;
        point = coordSys.dataToPoint(tmpIn, null, tmpOut);
        points4[offset++] = point ? point[1] : NaN;
      }
      data2.setLayout("largePoints", points4);
    }
  }
};
function getSign(store, dataIndex, openVal, closeVal, closeDimI, hasDojiColor) {
  var sign;
  if (openVal > closeVal) {
    sign = -1;
  } else if (openVal < closeVal) {
    sign = 1;
  } else {
    sign = hasDojiColor ? 0 : dataIndex > 0 ? store.get(closeDimI, dataIndex - 1) <= closeVal ? 1 : -1 : 1;
  }
  return sign;
}
function calculateCandleWidth(seriesModel, data) {
  var baseAxis = seriesModel.getBaseAxis();
  var bandWidth = calcBandWidth(baseAxis, {
    fromStat: {
      key: makeAxisStatKey(SERIES_TYPE_CANDLESTICK)
    },
    min: 1
  }).w;
  var barMaxWidth = parsePercent2(retrieve2(seriesModel.get("barMaxWidth"), bandWidth), bandWidth);
  var barMinWidth = parsePercent2(retrieve2(seriesModel.get("barMinWidth"), 1), bandWidth);
  var barWidth = seriesModel.get("barWidth");
  return barWidth != null ? parsePercent2(barWidth, bandWidth) : mathMax4(mathMin4(bandWidth / 2, barMaxWidth), barMinWidth);
}

// node_modules/echarts/lib/chart/helper/EffectSymbol.js
function updateRipplePath(rippleGroup, effectCfg) {
  var color3 = effectCfg.rippleEffectColor || effectCfg.color;
  rippleGroup.eachChild(function(ripplePath) {
    ripplePath.attr({
      z: effectCfg.z,
      zlevel: effectCfg.zlevel,
      style: {
        stroke: effectCfg.brushType === "stroke" ? color3 : null,
        fill: effectCfg.brushType === "fill" ? color3 : null
      }
    });
  });
}
var EffectSymbol = (
  /** @class */
  (function(_super) {
    __extends(EffectSymbol2, _super);
    function EffectSymbol2(data, idx) {
      var _this = _super.call(this) || this;
      var symbol = new Symbol_default(data, idx);
      var rippleGroup = new Group_default();
      _this.add(symbol);
      _this.add(rippleGroup);
      _this.updateData(data, idx);
      return _this;
    }
    EffectSymbol2.prototype.stopEffectAnimation = function() {
      this.childAt(1).removeAll();
    };
    EffectSymbol2.prototype.startEffectAnimation = function(effectCfg) {
      var symbolType = effectCfg.symbolType;
      var color3 = effectCfg.color;
      var rippleNumber = effectCfg.rippleNumber;
      var rippleGroup = this.childAt(1);
      for (var i2 = 0; i2 < rippleNumber; i2++) {
        var ripplePath = createSymbol(symbolType, -1, -1, 2, 2, color3);
        ripplePath.attr({
          style: {
            strokeNoScale: true
          },
          z2: 99,
          silent: true,
          scaleX: 0.5,
          scaleY: 0.5
        });
        var delay2 = -i2 / rippleNumber * effectCfg.period + effectCfg.effectOffset;
        ripplePath.animate("", true).when(effectCfg.period, {
          scaleX: effectCfg.rippleScale / 2,
          scaleY: effectCfg.rippleScale / 2
        }).delay(delay2).start();
        ripplePath.animateStyle(true).when(effectCfg.period, {
          opacity: 0
        }).delay(delay2).start();
        rippleGroup.add(ripplePath);
      }
      updateRipplePath(rippleGroup, effectCfg);
    };
    EffectSymbol2.prototype.updateEffectAnimation = function(effectCfg) {
      var oldEffectCfg = this._effectCfg;
      var rippleGroup = this.childAt(1);
      var DIFFICULT_PROPS = ["symbolType", "period", "rippleScale", "rippleNumber"];
      for (var i2 = 0; i2 < DIFFICULT_PROPS.length; i2++) {
        var propName = DIFFICULT_PROPS[i2];
        if (oldEffectCfg[propName] !== effectCfg[propName]) {
          this.stopEffectAnimation();
          this.startEffectAnimation(effectCfg);
          return;
        }
      }
      updateRipplePath(rippleGroup, effectCfg);
    };
    EffectSymbol2.prototype.highlight = function() {
      enterEmphasis(this);
    };
    EffectSymbol2.prototype.downplay = function() {
      leaveEmphasis(this);
    };
    EffectSymbol2.prototype.getSymbolType = function() {
      var symbol = this.childAt(0);
      return symbol && symbol.getSymbolType();
    };
    EffectSymbol2.prototype.updateData = function(data, idx) {
      var _this = this;
      var seriesModel = data.hostModel;
      this.childAt(0).updateData(data, idx);
      var rippleGroup = this.childAt(1);
      var itemModel = data.getItemModel(idx);
      var symbolType = data.getItemVisual(idx, "symbol");
      var symbolSize = normalizeSymbolSize(data.getItemVisual(idx, "symbolSize"));
      var symbolStyle = data.getItemVisual(idx, "style");
      var color3 = symbolStyle && symbolStyle.fill;
      var emphasisModel = itemModel.getModel("emphasis");
      rippleGroup.setScale(symbolSize);
      rippleGroup.traverse(function(ripplePath) {
        ripplePath.setStyle("fill", color3);
      });
      var symbolOffset = normalizeSymbolOffset(data.getItemVisual(idx, "symbolOffset"), symbolSize);
      if (symbolOffset) {
        rippleGroup.x = symbolOffset[0];
        rippleGroup.y = symbolOffset[1];
      }
      var symbolRotate = data.getItemVisual(idx, "symbolRotate");
      rippleGroup.rotation = (symbolRotate || 0) * Math.PI / 180 || 0;
      var effectCfg = {};
      effectCfg.showEffectOn = seriesModel.get("showEffectOn");
      effectCfg.rippleScale = itemModel.get(["rippleEffect", "scale"]);
      effectCfg.brushType = itemModel.get(["rippleEffect", "brushType"]);
      effectCfg.period = itemModel.get(["rippleEffect", "period"]) * 1e3;
      effectCfg.effectOffset = idx / data.count();
      effectCfg.z = seriesModel.getShallow("z") || 0;
      effectCfg.zlevel = seriesModel.getShallow("zlevel") || 0;
      effectCfg.symbolType = symbolType;
      effectCfg.color = color3;
      effectCfg.rippleEffectColor = itemModel.get(["rippleEffect", "color"]);
      effectCfg.rippleNumber = itemModel.get(["rippleEffect", "number"]);
      if (effectCfg.showEffectOn === "render") {
        this._effectCfg ? this.updateEffectAnimation(effectCfg) : this.startEffectAnimation(effectCfg);
        this._effectCfg = effectCfg;
      } else {
        this._effectCfg = null;
        this.stopEffectAnimation();
        this.onHoverStateChange = function(toState) {
          if (toState === "emphasis") {
            if (effectCfg.showEffectOn !== "render") {
              _this.startEffectAnimation(effectCfg);
            }
          } else if (toState === "normal") {
            if (effectCfg.showEffectOn !== "render") {
              _this.stopEffectAnimation();
            }
          }
        };
      }
      this._effectCfg = effectCfg;
      toggleHoverEmphasis(this, emphasisModel.get("focus"), emphasisModel.get("blurScope"), emphasisModel.get("disabled"));
    };
    ;
    EffectSymbol2.prototype.fadeOut = function(cb) {
      cb && cb();
    };
    ;
    return EffectSymbol2;
  })(Group_default)
);
var EffectSymbol_default = EffectSymbol;

// node_modules/echarts/lib/chart/effectScatter/EffectScatterView.js
var EffectScatterView = (
  /** @class */
  (function(_super) {
    __extends(EffectScatterView2, _super);
    function EffectScatterView2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = EffectScatterView2.type;
      return _this;
    }
    EffectScatterView2.prototype.init = function() {
      this._symbolDraw = new SymbolDraw_default(EffectSymbol_default);
    };
    EffectScatterView2.prototype.render = function(seriesModel, ecModel, api) {
      var data = seriesModel.getData();
      var effectSymbolDraw = this._symbolDraw;
      effectSymbolDraw.updateData(data, createSymbolDrawOpt2(seriesModel));
      this.group.add(effectSymbolDraw.group);
    };
    EffectScatterView2.prototype.updateTransform = function(seriesModel, ecModel, api) {
      var data = seriesModel.getData();
      this.group.dirty();
      var res = pointsLayout("").reset(seriesModel, ecModel, api);
      if (res.progress) {
        res.progress({
          start: 0,
          end: data.count(),
          count: data.count()
        }, data);
      }
      this._symbolDraw.updateLayout(createSymbolDrawOpt2(seriesModel));
    };
    EffectScatterView2.prototype._updateGroupTransform = function(seriesModel) {
      var coordSys = seriesModel.coordinateSystem;
      if (coordSys && coordSys.getRoamTransform) {
        this.group.transform = clone2(coordSys.getRoamTransform());
        this.group.decomposeTransform();
      }
    };
    EffectScatterView2.prototype.remove = function(ecModel, api) {
      this._symbolDraw && this._symbolDraw.remove(true);
    };
    EffectScatterView2.type = "effectScatter";
    return EffectScatterView2;
  })(Chart_default)
);
function createSymbolDrawOpt2(seriesModel) {
  return {
    clipShape: createCoordSysClipAreaSimply(seriesModel)
  };
}

// node_modules/echarts/lib/chart/effectScatter/EffectScatterSeries.js
var EffectScatterSeriesModel = (
  /** @class */
  (function(_super) {
    __extends(EffectScatterSeriesModel2, _super);
    function EffectScatterSeriesModel2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = EffectScatterSeriesModel2.type;
      _this.hasSymbolVisual = true;
      return _this;
    }
    EffectScatterSeriesModel2.prototype.getInitialData = function(option, ecModel) {
      return createSeriesData_default(null, this, {
        useEncodeDefaulter: true
      });
    };
    EffectScatterSeriesModel2.prototype.brushSelector = function(dataIndex, data, selectors) {
      return selectors.point(data.getItemLayout(dataIndex));
    };
    EffectScatterSeriesModel2.type = "series.effectScatter";
    EffectScatterSeriesModel2.dependencies = ["grid", "polar"];
    EffectScatterSeriesModel2.defaultOption = {
      coordinateSystem: "cartesian2d",
      // zlevel: 0,
      z: 2,
      legendHoverLink: true,
      effectType: "ripple",
      progressive: 0,
      // When to show the effect, option: 'render'|'emphasis'
      showEffectOn: "render",
      clip: true,
      // Ripple effect config
      rippleEffect: {
        period: 4,
        // Scale of ripple
        scale: 2.5,
        // Brush type can be fill or stroke
        brushType: "fill",
        // Ripple number
        number: 3
      },
      universalTransition: {
        divideShape: "clone"
      },
      // Cartesian coordinate system
      // xAxisIndex: 0,
      // yAxisIndex: 0,
      // Polar coordinate system
      // polarIndex: 0,
      // Geo coordinate system
      // geoIndex: 0,
      // symbol: null,        // 图形类型
      symbolSize: 10
      // 图形大小，半宽（半径）参数，当图形为方向或菱形则总宽度为symbolSize * 2
      // symbolRotate: null,  // 图形旋转控制
      // itemStyle: {
      //     opacity: 1
      // }
    };
    return EffectScatterSeriesModel2;
  })(Series_default)
);

// node_modules/echarts/lib/chart/helper/EffectLine.js
var EffectLine = (
  /** @class */
  (function(_super) {
    __extends(EffectLine2, _super);
    function EffectLine2(lineData, idx, seriesScope) {
      var _this = _super.call(this) || this;
      _this.add(_this.createLine(lineData, idx, seriesScope));
      _this._updateEffectSymbol(lineData, idx);
      return _this;
    }
    EffectLine2.prototype.createLine = function(lineData, idx, seriesScope) {
      return new Line_default2(lineData, idx, seriesScope);
    };
    EffectLine2.prototype._updateEffectSymbol = function(lineData, idx) {
      var itemModel = lineData.getItemModel(idx);
      var effectModel = itemModel.getModel("effect");
      var size = effectModel.get("symbolSize");
      var symbolType = effectModel.get("symbol");
      if (!isArray(size)) {
        size = [size, size];
      }
      var lineStyle = lineData.getItemVisual(idx, "style");
      var color3 = effectModel.get("color") || lineStyle && lineStyle.stroke;
      var symbol = this.childAt(1);
      if (this._symbolType !== symbolType) {
        this.remove(symbol);
        symbol = createSymbol(symbolType, -0.5, -0.5, 1, 1, color3);
        symbol.z2 = 100;
        symbol.culling = true;
        this.add(symbol);
      }
      if (!symbol) {
        return;
      }
      symbol.setStyle("shadowColor", color3);
      symbol.setStyle(effectModel.getItemStyle(["color"]));
      symbol.scaleX = size[0];
      symbol.scaleY = size[1];
      symbol.setColor(color3);
      this._symbolType = symbolType;
      this._symbolScale = size;
      this._updateEffectAnimation(lineData, effectModel, idx);
    };
    EffectLine2.prototype._updateEffectAnimation = function(lineData, effectModel, idx) {
      var symbol = this.childAt(1);
      if (!symbol) {
        return;
      }
      var points4 = lineData.getItemLayout(idx);
      var period = effectModel.get("period") * 1e3;
      var loop = effectModel.get("loop");
      var roundTrip = effectModel.get("roundTrip");
      var constantSpeed = effectModel.get("constantSpeed");
      var delayExpr = retrieve(effectModel.get("delay"), function(idx2) {
        return idx2 / lineData.count() * period / 3;
      });
      symbol.ignore = true;
      this._updateAnimationPoints(symbol, points4);
      if (constantSpeed > 0) {
        period = this._getLineLength(symbol) / constantSpeed * 1e3;
      }
      if (period !== this._period || loop !== this._loop || roundTrip !== this._roundTrip) {
        symbol.stopAnimation();
        var delayNum = void 0;
        if (isFunction(delayExpr)) {
          delayNum = delayExpr(idx);
        } else {
          delayNum = delayExpr;
        }
        if (symbol.__t > 0) {
          delayNum = -period * symbol.__t;
        }
        this._animateSymbol(symbol, period, delayNum, loop, roundTrip);
      }
      this._period = period;
      this._loop = loop;
      this._roundTrip = roundTrip;
    };
    EffectLine2.prototype._animateSymbol = function(symbol, period, delayNum, loop, roundTrip) {
      if (period > 0) {
        symbol.__t = 0;
        var self_1 = this;
        var animator = symbol.animate("", loop).when(roundTrip ? period * 2 : period, {
          __t: roundTrip ? 2 : 1
        }).delay(delayNum).during(function() {
          self_1._updateSymbolPosition(symbol);
        });
        if (!loop) {
          animator.done(function() {
            self_1.remove(symbol);
          });
        }
        animator.start();
      }
    };
    EffectLine2.prototype._getLineLength = function(symbol) {
      return dist(symbol.__p1, symbol.__cp1) + dist(symbol.__cp1, symbol.__p2);
    };
    EffectLine2.prototype._updateAnimationPoints = function(symbol, points4) {
      symbol.__p1 = points4[0];
      symbol.__p2 = points4[1];
      symbol.__cp1 = points4[2] || [(points4[0][0] + points4[1][0]) / 2, (points4[0][1] + points4[1][1]) / 2];
    };
    EffectLine2.prototype.updateData = function(lineData, idx, seriesScope) {
      this.childAt(0).updateData(lineData, idx, seriesScope);
      this._updateEffectSymbol(lineData, idx);
    };
    EffectLine2.prototype._updateSymbolPosition = function(symbol) {
      var p1 = symbol.__p1;
      var p2 = symbol.__p2;
      var cp1 = symbol.__cp1;
      var t = symbol.__t <= 1 ? symbol.__t : 2 - symbol.__t;
      var pos = [symbol.x, symbol.y];
      var lastPos = pos.slice();
      var quadraticAt3 = quadraticAt;
      var quadraticDerivativeAt2 = quadraticDerivativeAt;
      pos[0] = quadraticAt3(p1[0], cp1[0], p2[0], t);
      pos[1] = quadraticAt3(p1[1], cp1[1], p2[1], t);
      var tx = symbol.__t <= 1 ? quadraticDerivativeAt2(p1[0], cp1[0], p2[0], t) : quadraticDerivativeAt2(p2[0], cp1[0], p1[0], 1 - t);
      var ty = symbol.__t <= 1 ? quadraticDerivativeAt2(p1[1], cp1[1], p2[1], t) : quadraticDerivativeAt2(p2[1], cp1[1], p1[1], 1 - t);
      symbol.rotation = -Math.atan2(ty, tx) - Math.PI / 2;
      if (this._symbolType === "line" || this._symbolType === "rect" || this._symbolType === "roundRect") {
        if (symbol.__lastT !== void 0 && symbol.__lastT < symbol.__t) {
          symbol.scaleY = dist(lastPos, pos) * 1.05;
          if (t === 1) {
            pos[0] = lastPos[0] + (pos[0] - lastPos[0]) / 2;
            pos[1] = lastPos[1] + (pos[1] - lastPos[1]) / 2;
          }
        } else if (symbol.__lastT === 1) {
          symbol.scaleY = 2 * dist(p1, pos);
        } else {
          symbol.scaleY = this._symbolScale[1];
        }
      }
      symbol.__lastT = symbol.__t;
      symbol.ignore = false;
      symbol.x = pos[0];
      symbol.y = pos[1];
    };
    EffectLine2.prototype.updateLayout = function(lineData, idx) {
      this.childAt(0).updateLayout(lineData, idx);
      var effectModel = lineData.getItemModel(idx).getModel("effect");
      this._updateEffectAnimation(lineData, effectModel, idx);
    };
    return EffectLine2;
  })(Group_default)
);
var EffectLine_default = EffectLine;

// node_modules/echarts/lib/chart/helper/Polyline.js
var Polyline2 = (
  /** @class */
  (function(_super) {
    __extends(Polyline3, _super);
    function Polyline3(lineData, idx, seriesScope) {
      var _this = _super.call(this) || this;
      _this._createPolyline(lineData, idx, seriesScope);
      return _this;
    }
    Polyline3.prototype._createPolyline = function(lineData, idx, seriesScope) {
      var points4 = lineData.getItemLayout(idx);
      var line = new Polyline_default({
        shape: {
          points: points4
        }
      });
      this.add(line);
      this._updateCommonStl(lineData, idx, seriesScope);
    };
    ;
    Polyline3.prototype.updateData = function(lineData, idx, seriesScope) {
      var seriesModel = lineData.hostModel;
      var line = this.childAt(0);
      var target = {
        shape: {
          points: lineData.getItemLayout(idx)
        }
      };
      updateProps(line, target, seriesModel, idx);
      this._updateCommonStl(lineData, idx, seriesScope);
    };
    ;
    Polyline3.prototype._updateCommonStl = function(lineData, idx, seriesScope) {
      var line = this.childAt(0);
      var itemModel = lineData.getItemModel(idx);
      var emphasisLineStyle = seriesScope && seriesScope.emphasisLineStyle;
      var focus = seriesScope && seriesScope.focus;
      var blurScope = seriesScope && seriesScope.blurScope;
      var emphasisDisabled = seriesScope && seriesScope.emphasisDisabled;
      if (!seriesScope || lineData.hasItemOption) {
        var emphasisModel = itemModel.getModel("emphasis");
        emphasisLineStyle = emphasisModel.getModel("lineStyle").getLineStyle();
        emphasisDisabled = emphasisModel.get("disabled");
        focus = emphasisModel.get("focus");
        blurScope = emphasisModel.get("blurScope");
      }
      line.useStyle(lineData.getItemVisual(idx, "style"));
      line.style.fill = null;
      line.style.strokeNoScale = true;
      var lineEmphasisState = line.ensureState("emphasis");
      lineEmphasisState.style = emphasisLineStyle;
      toggleHoverEmphasis(this, focus, blurScope, emphasisDisabled);
    };
    ;
    Polyline3.prototype.updateLayout = function(lineData, idx) {
      var polyline = this.childAt(0);
      polyline.stopAnimation();
      polyline.setShape("points", lineData.getItemLayout(idx));
    };
    ;
    return Polyline3;
  })(Group_default)
);
var Polyline_default2 = Polyline2;

// node_modules/echarts/lib/chart/helper/EffectPolyline.js
var EffectPolyline = (
  /** @class */
  (function(_super) {
    __extends(EffectPolyline2, _super);
    function EffectPolyline2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this._lastFrame = 0;
      _this._lastFramePercent = 0;
      return _this;
    }
    EffectPolyline2.prototype.createLine = function(lineData, idx, seriesScope) {
      return new Polyline_default2(lineData, idx, seriesScope);
    };
    ;
    EffectPolyline2.prototype._updateAnimationPoints = function(symbol, points4) {
      this._points = points4;
      var accLenArr = [0];
      var len2 = 0;
      for (var i2 = 1; i2 < points4.length; i2++) {
        var p1 = points4[i2 - 1];
        var p2 = points4[i2];
        len2 += dist(p1, p2);
        accLenArr.push(len2);
      }
      if (len2 === 0) {
        this._length = 0;
        return;
      }
      for (var i2 = 0; i2 < accLenArr.length; i2++) {
        accLenArr[i2] /= len2;
      }
      this._offsets = accLenArr;
      this._length = len2;
    };
    ;
    EffectPolyline2.prototype._getLineLength = function() {
      return this._length;
    };
    ;
    EffectPolyline2.prototype._updateSymbolPosition = function(symbol) {
      var t = symbol.__t <= 1 ? symbol.__t : 2 - symbol.__t;
      var points4 = this._points;
      var offsets = this._offsets;
      var len2 = points4.length;
      if (!offsets) {
        return;
      }
      var lastFrame = this._lastFrame;
      var frame;
      if (t < this._lastFramePercent) {
        var start2 = Math.min(lastFrame + 1, len2 - 1);
        for (frame = start2; frame >= 0; frame--) {
          if (offsets[frame] <= t) {
            break;
          }
        }
        frame = Math.min(frame, len2 - 2);
      } else {
        for (frame = lastFrame; frame < len2; frame++) {
          if (offsets[frame] > t) {
            break;
          }
        }
        frame = Math.min(frame - 1, len2 - 2);
      }
      var p = (t - offsets[frame]) / (offsets[frame + 1] - offsets[frame]);
      var p0 = points4[frame];
      var p1 = points4[frame + 1];
      symbol.x = p0[0] * (1 - p) + p * p1[0];
      symbol.y = p0[1] * (1 - p) + p * p1[1];
      var tx = symbol.__t <= 1 ? p1[0] - p0[0] : p0[0] - p1[0];
      var ty = symbol.__t <= 1 ? p1[1] - p0[1] : p0[1] - p1[1];
      symbol.rotation = -Math.atan2(ty, tx) - Math.PI / 2;
      this._lastFrame = frame;
      this._lastFramePercent = t;
      symbol.ignore = false;
    };
    ;
    return EffectPolyline2;
  })(EffectLine_default)
);
var EffectPolyline_default = EffectPolyline;

// node_modules/echarts/lib/chart/helper/LargeLineDraw.js
var LargeLinesPathShape = (
  /** @class */
  /* @__PURE__ */ (function() {
    function LargeLinesPathShape2() {
      this.polyline = false;
      this.curveness = 0;
      this.segs = [];
    }
    return LargeLinesPathShape2;
  })()
);
var LargeLinesPath = (
  /** @class */
  (function(_super) {
    __extends(LargeLinesPath2, _super);
    function LargeLinesPath2(opts) {
      var _this = _super.call(this, opts) || this;
      _this._off = 0;
      _this.hoverDataIdx = -1;
      return _this;
    }
    LargeLinesPath2.prototype.reset = function() {
      this.notClear = false;
      this._off = 0;
    };
    LargeLinesPath2.prototype.beforeBrush = function(param) {
      if (param && !param.contentRetained) {
        this.reset();
      }
    };
    LargeLinesPath2.prototype.getDefaultStyle = function() {
      return {
        stroke: tokens_default.color.neutral99,
        fill: null
      };
    };
    LargeLinesPath2.prototype.getDefaultShape = function() {
      return new LargeLinesPathShape();
    };
    LargeLinesPath2.prototype.buildPath = function(ctx, shape) {
      var segs = shape.segs;
      var curveness = shape.curveness;
      var i2;
      if (shape.polyline) {
        for (i2 = this._off; i2 < segs.length; ) {
          var count2 = segs[i2++];
          if (count2 > 0) {
            ctx.moveTo(segs[i2++], segs[i2++]);
            for (var k2 = 1; k2 < count2; k2++) {
              ctx.lineTo(segs[i2++], segs[i2++]);
            }
          }
        }
      } else {
        for (i2 = this._off; i2 < segs.length; ) {
          var x0 = segs[i2++];
          var y0 = segs[i2++];
          var x1 = segs[i2++];
          var y1 = segs[i2++];
          ctx.moveTo(x0, y0);
          if (curveness > 0) {
            var x2 = (x0 + x1) / 2 - (y0 - y1) * curveness;
            var y2 = (y0 + y1) / 2 - (x1 - x0) * curveness;
            ctx.quadraticCurveTo(x2, y2, x1, y1);
          } else {
            ctx.lineTo(x1, y1);
          }
        }
      }
      if (this.incremental) {
        this._off = i2;
        this.notClear = true;
      }
    };
    LargeLinesPath2.prototype.findDataIndex = function(x, y) {
      var shape = this.shape;
      var segs = shape.segs;
      var curveness = shape.curveness;
      var lineWidth = this.style.lineWidth;
      if (shape.polyline) {
        var dataIndex = 0;
        for (var i2 = 0; i2 < segs.length; ) {
          var count2 = segs[i2++];
          if (count2 > 0) {
            var x0 = segs[i2++];
            var y0 = segs[i2++];
            for (var k2 = 1; k2 < count2; k2++) {
              var x1 = segs[i2++];
              var y1 = segs[i2++];
              if (containStroke(x0, y0, x1, y1, lineWidth, x, y)) {
                return dataIndex;
              }
            }
          }
          dataIndex++;
        }
      } else {
        var dataIndex = 0;
        for (var i2 = 0; i2 < segs.length; ) {
          var x0 = segs[i2++];
          var y0 = segs[i2++];
          var x1 = segs[i2++];
          var y1 = segs[i2++];
          if (curveness > 0) {
            var x2 = (x0 + x1) / 2 - (y0 - y1) * curveness;
            var y2 = (y0 + y1) / 2 - (x1 - x0) * curveness;
            if (containStroke3(x0, y0, x2, y2, x1, y1, lineWidth, x, y)) {
              return dataIndex;
            }
          } else {
            if (containStroke(x0, y0, x1, y1, lineWidth, x, y)) {
              return dataIndex;
            }
          }
          dataIndex++;
        }
      }
      return -1;
    };
    LargeLinesPath2.prototype.contain = function(x, y) {
      var localPos = this.transformCoordToLocal(x, y);
      var rect = this.getBoundingRect();
      x = localPos[0];
      y = localPos[1];
      if (rect.contain(x, y)) {
        var dataIdx = this.hoverDataIdx = this.findDataIndex(x, y);
        return dataIdx >= 0;
      }
      this.hoverDataIdx = -1;
      return false;
    };
    LargeLinesPath2.prototype.getBoundingRect = function() {
      var rect = this._rect;
      if (!rect) {
        var shape = this.shape;
        var points4 = shape.segs;
        var minX = Infinity;
        var minY = Infinity;
        var maxX = -Infinity;
        var maxY = -Infinity;
        for (var i2 = 0; i2 < points4.length; ) {
          var x = points4[i2++];
          var y = points4[i2++];
          minX = Math.min(x, minX);
          maxX = Math.max(x, maxX);
          minY = Math.min(y, minY);
          maxY = Math.max(y, maxY);
        }
        rect = this._rect = new BoundingRect_default(minX, minY, maxX, maxY);
      }
      return rect;
    };
    return LargeLinesPath2;
  })(Path_default)
);
var LargeLineDraw = (
  /** @class */
  (function() {
    function LargeLineDraw2() {
      this.group = new Group_default();
    }
    LargeLineDraw2.prototype.updateData = function(data) {
      this._clear();
      var lineEl = this._create();
      lineEl.setShape({
        segs: data.getLayout("linesPoints")
      });
      this._setCommon(lineEl, data);
    };
    ;
    LargeLineDraw2.prototype.incrementalPrepareUpdate = function(data) {
      this.group.removeAll();
      this._clear();
    };
    ;
    LargeLineDraw2.prototype.incrementalUpdate = function(taskParams, data, incrementalId) {
      var lastAdded = this._newAdded[0];
      var linePoints = data.getLayout("linesPoints");
      var oldSegs = lastAdded && lastAdded.shape.segs;
      if (oldSegs && oldSegs.length < 2e4) {
        var oldLen = oldSegs.length;
        var newSegs = new Float32Array(oldLen + linePoints.length);
        newSegs.set(oldSegs);
        newSegs.set(linePoints, oldLen);
        lastAdded.setShape({
          segs: newSegs
        });
      } else {
        this._newAdded = [];
        var lineEl = this._create();
        lineEl.incremental = incrementalId;
        lineEl.setShape({
          segs: linePoints
        });
        this._setCommon(lineEl, data);
        lineEl.__startIndex = taskParams.start;
      }
    };
    LargeLineDraw2.prototype.remove = function() {
      this._clear();
    };
    LargeLineDraw2.prototype.eachRendered = function(cb) {
      this._newAdded[0] && cb(this._newAdded[0]);
    };
    LargeLineDraw2.prototype._create = function() {
      var lineEl = new LargeLinesPath({
        cursor: "default",
        ignoreCoarsePointer: true
      });
      this._newAdded.push(lineEl);
      this.group.add(lineEl);
      return lineEl;
    };
    LargeLineDraw2.prototype._setCommon = function(lineEl, data, isIncremental) {
      var hostModel = data.hostModel;
      lineEl.setShape({
        polyline: hostModel.get("polyline"),
        curveness: hostModel.get(["lineStyle", "curveness"])
      });
      lineEl.useStyle(hostModel.getModel("lineStyle").getLineStyle());
      lineEl.style.strokeNoScale = true;
      var style2 = data.getVisual("style");
      if (style2 && style2.stroke) {
        lineEl.setStyle("stroke", style2.stroke);
      }
      lineEl.setStyle("fill", null);
      var ecData = getECData(lineEl);
      ecData.seriesIndex = hostModel.seriesIndex;
      lineEl.on("mousemove", function(e2) {
        ecData.dataIndex = null;
        var dataIndex = lineEl.hoverDataIdx;
        if (dataIndex > 0) {
          ecData.dataIndex = dataIndex + lineEl.__startIndex;
        }
      });
    };
    ;
    LargeLineDraw2.prototype._clear = function() {
      this._newAdded = [];
      this.group.removeAll();
    };
    ;
    return LargeLineDraw2;
  })()
);
var LargeLineDraw_default = LargeLineDraw;

// node_modules/echarts/lib/chart/lines/linesLayout.js
var linesLayout = {
  seriesType: "lines",
  plan: createRenderPlanner(),
  reset: function(seriesModel) {
    var coordSys = seriesModel.coordinateSystem;
    if (!coordSys) {
      if (true) {
        error("The lines series must have a coordinate system.");
      }
      return;
    }
    var isPolyline = seriesModel.get("polyline");
    var isLarge = seriesModel.pipelineContext.large;
    return {
      progress: function(params, lineData) {
        var lineCoords = [];
        if (isLarge) {
          var points4 = void 0;
          var segCount = params.end - params.start;
          if (isPolyline) {
            var totalCoordsCount = 0;
            for (var i2 = params.start; i2 < params.end; i2++) {
              totalCoordsCount += seriesModel.getLineCoordsCount(i2);
            }
            points4 = new Float32Array(segCount + totalCoordsCount * 2);
          } else {
            points4 = new Float32Array(segCount * 4);
          }
          var offset = 0;
          var pt = [];
          for (var i2 = params.start; i2 < params.end; i2++) {
            var len2 = seriesModel.getLineCoords(i2, lineCoords);
            if (isPolyline) {
              points4[offset++] = len2;
            }
            for (var k2 = 0; k2 < len2; k2++) {
              pt = coordSys.dataToPoint(lineCoords[k2], false, pt);
              points4[offset++] = pt[0];
              points4[offset++] = pt[1];
            }
          }
          lineData.setLayout("linesPoints", points4);
        } else {
          for (var i2 = params.start; i2 < params.end; i2++) {
            var itemModel = lineData.getItemModel(i2);
            var len2 = seriesModel.getLineCoords(i2, lineCoords);
            var pts = [];
            if (isPolyline) {
              for (var j = 0; j < len2; j++) {
                pts.push(coordSys.dataToPoint(lineCoords[j]));
              }
            } else {
              pts[0] = coordSys.dataToPoint(lineCoords[0]);
              pts[1] = coordSys.dataToPoint(lineCoords[1]);
              var curveness = itemModel.get(["lineStyle", "curveness"]);
              if (+curveness) {
                pts[2] = [(pts[0][0] + pts[1][0]) / 2 - (pts[0][1] - pts[1][1]) * curveness, (pts[0][1] + pts[1][1]) / 2 - (pts[1][0] - pts[0][0]) * curveness];
              }
            }
            lineData.setItemLayout(i2, pts);
          }
        }
      }
    };
  }
};
var linesLayout_default = linesLayout;

// node_modules/echarts/lib/chart/lines/LinesView.js
var LinesView = (
  /** @class */
  (function(_super) {
    __extends(LinesView2, _super);
    function LinesView2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = LinesView2.type;
      return _this;
    }
    LinesView2.prototype.render = function(seriesModel, ecModel, api) {
      var data = seriesModel.getData();
      var lineDraw = this._updateLineDraw(data, seriesModel);
      var zlevel = seriesModel.get("zlevel");
      var trailLength = seriesModel.get(["effect", "trailLength"]);
      var zr = api.getZr();
      var isCanvas = !!getCurrentCanvasPainter(api);
      if (isCanvas) {
        zr.painter.getLayer(zlevel).clear(true);
      }
      if (this._lastZlevel != null && isCanvas) {
        zr.configLayer(this._lastZlevel, {
          motionBlur: false
        });
      }
      if (this._showEffect(seriesModel) && trailLength > 0) {
        if (isCanvas) {
          zr.configLayer(zlevel, {
            motionBlur: true,
            lastFrameAlpha: Math.max(Math.min(trailLength / 10 + 0.9, 1), 0)
          });
        } else if (true) {
          console.warn("SVG render mode doesn't support lines with trail effect");
        }
      }
      lineDraw.updateData(data);
      var clipPath = seriesModel.get("clip", true) && createClipPath(seriesModel.coordinateSystem, false, seriesModel);
      if (clipPath) {
        this.group.setClipPath(clipPath);
      } else {
        this.group.removeClipPath();
      }
      this._lastZlevel = zlevel;
      this._finished = true;
    };
    LinesView2.prototype.incrementalPrepareRender = function(seriesModel, ecModel, api) {
      var data = seriesModel.getData();
      var lineDraw = this._updateLineDraw(data, seriesModel);
      lineDraw.incrementalPrepareUpdate(data);
      this._clearLayer(api);
      this._finished = false;
    };
    LinesView2.prototype.incrementalRender = function(taskParams, seriesModel, ecModel) {
      this._lineDraw.incrementalUpdate(taskParams, seriesModel.getData(), getIncrementalId(seriesModel));
      this._finished = taskParams.end === seriesModel.getData().count();
    };
    LinesView2.prototype.eachRendered = function(cb) {
      this._lineDraw && this._lineDraw.eachRendered(cb);
    };
    LinesView2.prototype.updateTransform = function(seriesModel, ecModel, api) {
      var data = seriesModel.getData();
      var lineDraw = this._lineDraw;
      if (!this._finished || !lineDraw || !lineDraw.updateLayout) {
        return {
          update: true
        };
      } else {
        var res = linesLayout_default.reset(seriesModel, ecModel, api);
        if (res.progress) {
          res.progress({
            start: 0,
            end: data.count(),
            count: data.count()
          }, data);
        }
        lineDraw.updateLayout();
        this._clearLayer(api);
      }
    };
    LinesView2.prototype._updateLineDraw = function(data, seriesModel) {
      var lineDraw = this._lineDraw;
      var hasEffect = this._showEffect(seriesModel);
      var isPolyline = !!seriesModel.get("polyline");
      var pipelineContext = seriesModel.pipelineContext;
      var isLargeDraw = pipelineContext.large;
      if (true) {
        if (hasEffect && isLargeDraw) {
          console.warn("Large lines not support effect");
        }
      }
      if (!lineDraw || hasEffect !== this._hasEffet || isPolyline !== this._isPolyline || isLargeDraw !== this._isLargeDraw) {
        if (lineDraw) {
          lineDraw.remove();
        }
        lineDraw = this._lineDraw = isLargeDraw ? new LargeLineDraw_default() : new LineDraw_default(isPolyline ? hasEffect ? EffectPolyline_default : Polyline_default2 : hasEffect ? EffectLine_default : Line_default2);
        this._hasEffet = hasEffect;
        this._isPolyline = isPolyline;
        this._isLargeDraw = isLargeDraw;
      }
      this.group.add(lineDraw.group);
      return lineDraw;
    };
    LinesView2.prototype._showEffect = function(seriesModel) {
      return !!seriesModel.get(["effect", "show"]);
    };
    LinesView2.prototype._clearLayer = function(api) {
      var painter = getCurrentCanvasPainter(api);
      if (painter && this._lastZlevel != null) {
        painter.getLayer(this._lastZlevel).clear(true);
      }
    };
    LinesView2.prototype.remove = function(ecModel, api) {
      this._lineDraw && this._lineDraw.remove();
      this._lineDraw = null;
      this._clearLayer(api);
    };
    LinesView2.prototype.dispose = function(ecModel, api) {
      this.remove(ecModel, api);
    };
    LinesView2.type = "lines";
    return LinesView2;
  })(Chart_default)
);

// node_modules/echarts/lib/chart/lines/LinesSeries.js
var Uint32Arr = typeof Uint32Array === "undefined" ? Array : Uint32Array;
var Float64Arr = typeof Float64Array === "undefined" ? Array : Float64Array;
function compatEc2(seriesOpt) {
  var data = seriesOpt.data;
  if (data && data[0] && data[0][0] && data[0][0].coord) {
    if (true) {
      console.warn("Lines data configuration has been changed to { coords:[[1,2],[2,3]] }");
    }
    seriesOpt.data = map2(data, function(itemOpt) {
      var coords = [itemOpt[0].coord, itemOpt[1].coord];
      var target = {
        coords
      };
      if (itemOpt[0].name) {
        target.fromName = itemOpt[0].name;
      }
      if (itemOpt[1].name) {
        target.toName = itemOpt[1].name;
      }
      return mergeAll([target, itemOpt[0], itemOpt[1]]);
    });
  }
}
var LinesSeriesModel = (
  /** @class */
  (function(_super) {
    __extends(LinesSeriesModel2, _super);
    function LinesSeriesModel2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = LinesSeriesModel2.type;
      _this.visualStyleAccessPath = "lineStyle";
      _this.visualDrawType = "stroke";
      return _this;
    }
    LinesSeriesModel2.prototype.init = function(option) {
      option.data = option.data || [];
      compatEc2(option);
      var result = this._processFlatCoordsArray(option.data);
      this._flatCoords = result.flatCoords;
      this._flatCoordsOffset = result.flatCoordsOffset;
      if (result.flatCoords) {
        option.data = new Float32Array(result.count);
      }
      _super.prototype.init.apply(this, arguments);
    };
    LinesSeriesModel2.prototype.mergeOption = function(option) {
      compatEc2(option);
      if (option.data) {
        var result = this._processFlatCoordsArray(option.data);
        this._flatCoords = result.flatCoords;
        this._flatCoordsOffset = result.flatCoordsOffset;
        if (result.flatCoords) {
          option.data = new Float32Array(result.count);
        }
      }
      _super.prototype.mergeOption.apply(this, arguments);
    };
    LinesSeriesModel2.prototype.appendData = function(params) {
      var result = this._processFlatCoordsArray(params.data);
      if (result.flatCoords) {
        if (!this._flatCoords) {
          this._flatCoords = result.flatCoords;
          this._flatCoordsOffset = result.flatCoordsOffset;
        } else {
          this._flatCoords = concatArray(this._flatCoords, result.flatCoords);
          this._flatCoordsOffset = concatArray(this._flatCoordsOffset, result.flatCoordsOffset);
        }
        params.data = new Float32Array(result.count);
      }
      this.getRawData().appendData(params.data);
    };
    LinesSeriesModel2.prototype._getCoordsFromItemModel = function(idx) {
      var itemModel = this.getData().getItemModel(idx);
      var coords = itemModel.option instanceof Array ? itemModel.option : itemModel.getShallow("coords");
      if (true) {
        if (!(coords instanceof Array && coords.length > 0 && coords[0] instanceof Array)) {
          throw new Error("Invalid coords " + JSON.stringify(coords) + ". Lines must have 2d coords array in data item.");
        }
      }
      return coords;
    };
    LinesSeriesModel2.prototype.getLineCoordsCount = function(idx) {
      if (this._flatCoordsOffset) {
        return this._flatCoordsOffset[idx * 2 + 1];
      } else {
        return this._getCoordsFromItemModel(idx).length;
      }
    };
    LinesSeriesModel2.prototype.getLineCoords = function(idx, out2) {
      if (this._flatCoordsOffset) {
        var offset = this._flatCoordsOffset[idx * 2];
        var len2 = this._flatCoordsOffset[idx * 2 + 1];
        for (var i2 = 0; i2 < len2; i2++) {
          out2[i2] = out2[i2] || [];
          out2[i2][0] = this._flatCoords[offset + i2 * 2];
          out2[i2][1] = this._flatCoords[offset + i2 * 2 + 1];
        }
        return len2;
      } else {
        var coords = this._getCoordsFromItemModel(idx);
        for (var i2 = 0; i2 < coords.length; i2++) {
          out2[i2] = out2[i2] || [];
          out2[i2][0] = coords[i2][0];
          out2[i2][1] = coords[i2][1];
        }
        return coords.length;
      }
    };
    LinesSeriesModel2.prototype._processFlatCoordsArray = function(data) {
      var startOffset = 0;
      if (this._flatCoords) {
        startOffset = this._flatCoords.length;
      }
      if (isNumber(data[0])) {
        var len2 = data.length;
        var coordsOffsetAndLenStorage = new Uint32Arr(len2);
        var coordsStorage = new Float64Arr(len2);
        var coordsCursor = 0;
        var offsetCursor = 0;
        var dataCount = 0;
        for (var i2 = 0; i2 < len2; ) {
          dataCount++;
          var count2 = data[i2++];
          coordsOffsetAndLenStorage[offsetCursor++] = coordsCursor + startOffset;
          coordsOffsetAndLenStorage[offsetCursor++] = count2;
          for (var k2 = 0; k2 < count2; k2++) {
            var x = data[i2++];
            var y = data[i2++];
            coordsStorage[coordsCursor++] = x;
            coordsStorage[coordsCursor++] = y;
            if (i2 > len2) {
              if (true) {
                throw new Error("Invalid data format.");
              }
            }
          }
        }
        return {
          flatCoordsOffset: new Uint32Array(coordsOffsetAndLenStorage.buffer, 0, offsetCursor),
          flatCoords: coordsStorage,
          count: dataCount
        };
      }
      return {
        flatCoordsOffset: null,
        flatCoords: null,
        count: data.length
      };
    };
    LinesSeriesModel2.prototype.getInitialData = function(option, ecModel) {
      if (true) {
        var CoordSys = CoordinateSystem_default.get(option.coordinateSystem);
        if (!CoordSys) {
          throw new Error("Unknown coordinate system " + option.coordinateSystem);
        }
      }
      var lineData = new SeriesData_default(["value"], this);
      lineData.hasItemOption = false;
      lineData.initData(option.data, [], function(dataItem, dimName, dataIndex, dimIndex) {
        if (dataItem instanceof Array) {
          return NaN;
        } else {
          lineData.hasItemOption = true;
          var value = dataItem.value;
          if (value != null) {
            return value instanceof Array ? value[dimIndex] : value;
          }
        }
      });
      return lineData;
    };
    LinesSeriesModel2.prototype.formatTooltip = function(dataIndex, multipleSeries, dataType) {
      var data = this.getData();
      var value = this.getRawValue(dataIndex);
      var itemModel = data.getItemModel(dataIndex);
      var itemName = itemModel.get("name");
      if (!itemName) {
        var fromName = itemModel.get("fromName");
        var toName = itemModel.get("toName");
        var nameArr = [];
        fromName != null && nameArr.push(fromName);
        toName != null && nameArr.push(toName);
        itemName = nameArr.join(" > ");
      }
      return createTooltipMarkup("nameValue", {
        name: itemName,
        value,
        // NOTE: here `value` may be `coords` (an 2D-array) if ec option is like
        //  series: {
        //      type: 'lines',
        //      data: [
        //          [[2.122232, 100.1133], [5.122232, 104.1133]],
        //          [[22.122232, 0.1133], [25.122232, 4.1133]],
        //      ]
        //  }
        // Do not display `value` in that case.
        noValue: value == null || isNaN(value)
      });
    };
    LinesSeriesModel2.prototype.preventIncremental = function() {
      return !!this.get(["effect", "show"]);
    };
    LinesSeriesModel2.prototype.getProgressive = function() {
      var progressive = this.option.progressive;
      if (progressive == null) {
        return this.option.large ? 1e4 : this.get("progressive");
      }
      return progressive;
    };
    LinesSeriesModel2.prototype.getProgressiveThreshold = function() {
      var progressiveThreshold = this.option.progressiveThreshold;
      if (progressiveThreshold == null) {
        return this.option.large ? 2e4 : this.get("progressiveThreshold");
      }
      return progressiveThreshold;
    };
    LinesSeriesModel2.prototype.getZLevelKey = function() {
      var effectModel = this.getModel("effect");
      var trailLength = effectModel.get("trailLength");
      return this.getData().count() > this.getProgressiveThreshold() ? this.id : effectModel.get("show") && trailLength > 0 ? trailLength + "" : "";
    };
    LinesSeriesModel2.type = "series.lines";
    LinesSeriesModel2.dependencies = ["grid", "polar", "geo", "calendar"];
    LinesSeriesModel2.defaultOption = {
      coordinateSystem: "geo",
      // zlevel: 0,
      z: 2,
      legendHoverLink: true,
      // Cartesian coordinate system
      xAxisIndex: 0,
      yAxisIndex: 0,
      symbol: ["none", "none"],
      symbolSize: [10, 10],
      // Geo coordinate system
      geoIndex: 0,
      effect: {
        show: false,
        period: 4,
        constantSpeed: 0,
        symbol: "circle",
        symbolSize: 3,
        loop: true,
        trailLength: 0.2
      },
      large: false,
      // Available when large is true
      largeThreshold: 2e3,
      polyline: false,
      clip: true,
      label: {
        show: false,
        position: "end"
        // distance: 5,
        // formatter: 标签文本格式器，同Tooltip.formatter，不支持异步回调
      },
      lineStyle: {
        opacity: 0.5
      }
    };
    return LinesSeriesModel2;
  })(Series_default)
);

// node_modules/echarts/lib/chart/heatmap/HeatmapLayer.js
var GRADIENT_LEVELS = 256;
var HeatmapLayer = (
  /** @class */
  (function() {
    function HeatmapLayer2() {
      this.blurSize = 30;
      this.pointSize = 20;
      this.maxOpacity = 1;
      this.minOpacity = 0;
      this._gradientPixels = {
        inRange: null,
        outOfRange: null
      };
      var canvas = platformApi.createCanvas();
      this.canvas = canvas;
    }
    HeatmapLayer2.prototype.update = function(data, width, height, normalize3, colorFunc, isInRange) {
      var brush3 = this._getBrush();
      var gradientInRange = this._getGradient(colorFunc, "inRange");
      var gradientOutOfRange = this._getGradient(colorFunc, "outOfRange");
      var r = this.pointSize + this.blurSize;
      var canvas = this.canvas;
      var ctx = canvas.getContext("2d");
      var len2 = data.length;
      canvas.width = width;
      canvas.height = height;
      for (var i2 = 0; i2 < len2; ++i2) {
        var p = data[i2];
        var x = p[0];
        var y = p[1];
        var value = p[2];
        var alpha = normalize3(value);
        ctx.globalAlpha = alpha;
        ctx.drawImage(brush3, x - r, y - r);
      }
      if (!canvas.width || !canvas.height) {
        return canvas;
      }
      var imageData = ctx.getImageData(0, 0, canvas.width, canvas.height);
      var pixels = imageData.data;
      var offset = 0;
      var pixelLen = pixels.length;
      var minOpacity = this.minOpacity;
      var maxOpacity = this.maxOpacity;
      var diffOpacity = maxOpacity - minOpacity;
      while (offset < pixelLen) {
        var alpha = pixels[offset + 3] / 256;
        var gradientOffset = Math.floor(alpha * (GRADIENT_LEVELS - 1)) * 4;
        if (alpha > 0) {
          var gradient = isInRange(alpha) ? gradientInRange : gradientOutOfRange;
          alpha > 0 && (alpha = alpha * diffOpacity + minOpacity);
          pixels[offset++] = gradient[gradientOffset];
          pixels[offset++] = gradient[gradientOffset + 1];
          pixels[offset++] = gradient[gradientOffset + 2];
          pixels[offset++] = gradient[gradientOffset + 3] * alpha * 256;
        } else {
          offset += 4;
        }
      }
      ctx.putImageData(imageData, 0, 0);
      return canvas;
    };
    HeatmapLayer2.prototype._getBrush = function() {
      var brushCanvas = this._brushCanvas || (this._brushCanvas = platformApi.createCanvas());
      var r = this.pointSize + this.blurSize;
      var d = r * 2;
      brushCanvas.width = d;
      brushCanvas.height = d;
      var ctx = brushCanvas.getContext("2d");
      ctx.clearRect(0, 0, d, d);
      ctx.shadowOffsetX = d;
      ctx.shadowBlur = this.blurSize;
      ctx.shadowColor = tokens_default.color.neutral99;
      ctx.beginPath();
      ctx.arc(-r, r, this.pointSize, 0, Math.PI * 2, true);
      ctx.closePath();
      ctx.fill();
      return brushCanvas;
    };
    HeatmapLayer2.prototype._getGradient = function(colorFunc, state2) {
      var gradientPixels = this._gradientPixels;
      var pixelsSingleState = gradientPixels[state2] || (gradientPixels[state2] = new Uint8ClampedArray(256 * 4));
      var color3 = [0, 0, 0, 0];
      var off = 0;
      for (var i2 = 0; i2 < 256; i2++) {
        colorFunc[state2](i2 / 255, true, color3);
        pixelsSingleState[off++] = color3[0];
        pixelsSingleState[off++] = color3[1];
        pixelsSingleState[off++] = color3[2];
        pixelsSingleState[off++] = color3[3];
      }
      return pixelsSingleState;
    };
    return HeatmapLayer2;
  })()
);
var HeatmapLayer_default = HeatmapLayer;

// node_modules/echarts/lib/chart/heatmap/HeatmapView.js
function getIsInPiecewiseRange(dataExtent, pieceList, selected) {
  var dataSpan = dataExtent[1] - dataExtent[0];
  pieceList = map2(pieceList, function(piece) {
    return {
      interval: [(piece.interval[0] - dataExtent[0]) / dataSpan, (piece.interval[1] - dataExtent[0]) / dataSpan]
    };
  });
  var len2 = pieceList.length;
  var lastIndex = 0;
  return function(val) {
    var i2;
    for (i2 = lastIndex; i2 < len2; i2++) {
      var interval2 = pieceList[i2].interval;
      if (interval2[0] <= val && val <= interval2[1]) {
        lastIndex = i2;
        break;
      }
    }
    if (i2 === len2) {
      for (i2 = lastIndex - 1; i2 >= 0; i2--) {
        var interval2 = pieceList[i2].interval;
        if (interval2[0] <= val && val <= interval2[1]) {
          lastIndex = i2;
          break;
        }
      }
    }
    return i2 >= 0 && i2 < len2 && selected[i2];
  };
}
function getIsInContinuousRange(dataExtent, range) {
  var dataSpan = dataExtent[1] - dataExtent[0];
  range = [(range[0] - dataExtent[0]) / dataSpan, (range[1] - dataExtent[0]) / dataSpan];
  return function(val) {
    return val >= range[0] && val <= range[1];
  };
}
var HeatmapView = (
  /** @class */
  (function(_super) {
    __extends(HeatmapView2, _super);
    function HeatmapView2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = HeatmapView2.type;
      return _this;
    }
    HeatmapView2.prototype.render = function(seriesModel, ecModel, api) {
      var visualMapOfThisSeries;
      ecModel.eachComponent("visualMap", function(visualMap) {
        visualMap.eachTargetSeries(function(targetSeries) {
          if (targetSeries === seriesModel) {
            visualMapOfThisSeries = visualMap;
          }
        });
      });
      if (true) {
        if (!visualMapOfThisSeries) {
          throw new Error("Heatmap must use with visualMap");
        }
      }
      this._progressiveEls = null;
      this.group.removeAll();
      var coordSys = seriesModel.coordinateSystem;
      if (coordSys.type === "cartesian2d" || coordSys.type === "calendar" || coordSys.type === "matrix") {
        this._renderOnGridLike(seriesModel, api, 0, seriesModel.getData().count());
      } else if (isGeoLikeCoordSys(coordSys)) {
        this._renderOnGeo(coordSys, seriesModel, visualMapOfThisSeries, api);
      }
    };
    HeatmapView2.prototype.incrementalPrepareRender = function(seriesModel, ecModel, api) {
      this.group.removeAll();
    };
    HeatmapView2.prototype.incrementalRender = function(params, seriesModel, ecModel, api) {
      var coordSys = seriesModel.coordinateSystem;
      if (coordSys) {
        if (isGeoLikeCoordSys(coordSys)) {
          this.render(seriesModel, ecModel, api);
        } else {
          this._progressiveEls = [];
          this._renderOnGridLike(seriesModel, api, params.start, params.end, true);
        }
      }
    };
    HeatmapView2.prototype.eachRendered = function(cb) {
      traverseElements(this._progressiveEls || this.group, cb);
    };
    HeatmapView2.prototype._renderOnGridLike = function(seriesModel, api, start2, end2, useIncremental) {
      var coordSys = seriesModel.coordinateSystem;
      var isCartesian2d = isCoordinateSystemType(coordSys, "cartesian2d");
      var isMatrix = isCoordinateSystemType(coordSys, "matrix");
      var width;
      var height;
      var xAxisExtent;
      var yAxisExtent;
      if (isCartesian2d) {
        var xAxis = coordSys.getAxis("x");
        var yAxis = coordSys.getAxis("y");
        if (true) {
          if (!(xAxis.type === "category" && yAxis.type === "category")) {
            throw new Error("Heatmap on cartesian must have two category axes");
          }
          if (!(xAxis.onBand && yAxis.onBand)) {
            throw new Error("Heatmap on cartesian must have two axes with boundaryGap true");
          }
        }
        width = calcBandWidth(xAxis).w + 0.5;
        height = calcBandWidth(yAxis).w + 0.5;
        xAxisExtent = xAxis.scale.getExtent();
        yAxisExtent = yAxis.scale.getExtent();
      }
      var group2 = this.group;
      var data = seriesModel.getData();
      var emphasisStyle = seriesModel.getModel(["emphasis", "itemStyle"]).getItemStyle();
      var blurStyle = seriesModel.getModel(["blur", "itemStyle"]).getItemStyle();
      var selectStyle = seriesModel.getModel(["select", "itemStyle"]).getItemStyle();
      var borderRadius = seriesModel.get(["itemStyle", "borderRadius"]);
      var labelStatesModels = getLabelStatesModels(seriesModel);
      var emphasisModel = seriesModel.getModel("emphasis");
      var focus = emphasisModel.get("focus");
      var blurScope = emphasisModel.get("blurScope");
      var emphasisDisabled = emphasisModel.get("disabled");
      var dataDims = isCartesian2d || isMatrix ? [data.mapDimension("x"), data.mapDimension("y"), data.mapDimension("value")] : [data.mapDimension("time"), data.mapDimension("value")];
      for (var idx = start2; idx < end2; idx++) {
        var rect = void 0;
        var style2 = data.getItemVisual(idx, "style");
        if (isCartesian2d) {
          var dataDimX = data.get(dataDims[0], idx);
          var dataDimY = data.get(dataDims[1], idx);
          if (isNaN(data.get(dataDims[2], idx)) || isNaN(dataDimX) || isNaN(dataDimY) || dataDimX < xAxisExtent[0] || dataDimX > xAxisExtent[1] || dataDimY < yAxisExtent[0] || dataDimY > yAxisExtent[1]) {
            continue;
          }
          var point = coordSys.dataToPoint([dataDimX, dataDimY]);
          rect = new Rect_default({
            shape: {
              x: point[0] - width / 2,
              y: point[1] - height / 2,
              width,
              height
            },
            style: style2
          });
        } else if (isMatrix) {
          var shape = coordSys.dataToLayout([data.get(dataDims[0], idx), data.get(dataDims[1], idx)]).rect;
          if (eqNaN(shape.x)) {
            continue;
          }
          rect = new Rect_default({
            z2: 1,
            shape,
            style: style2
          });
        } else {
          if (isNaN(data.get(dataDims[1], idx))) {
            continue;
          }
          var layout3 = coordSys.dataToLayout([data.get(dataDims[0], idx)]);
          var shape = layout3.contentRect || layout3.rect;
          if (eqNaN(shape.x) || eqNaN(shape.y)) {
            continue;
          }
          rect = new Rect_default({
            z2: 1,
            shape,
            style: style2
          });
        }
        if (data.hasItemOption) {
          var itemModel = data.getItemModel(idx);
          var emphasisModel_1 = itemModel.getModel("emphasis");
          emphasisStyle = emphasisModel_1.getModel("itemStyle").getItemStyle();
          blurStyle = itemModel.getModel(["blur", "itemStyle"]).getItemStyle();
          selectStyle = itemModel.getModel(["select", "itemStyle"]).getItemStyle();
          borderRadius = itemModel.get(["itemStyle", "borderRadius"]);
          focus = emphasisModel_1.get("focus");
          blurScope = emphasisModel_1.get("blurScope");
          emphasisDisabled = emphasisModel_1.get("disabled");
          labelStatesModels = getLabelStatesModels(itemModel);
        }
        rect.shape.r = borderRadius;
        var rawValue = seriesModel.getRawValue(idx);
        var defaultText = "-";
        if (rawValue && rawValue[2] != null) {
          defaultText = rawValue[2] + "";
        }
        setLabelStyle(rect, labelStatesModels, {
          labelFetcher: seriesModel,
          labelDataIndex: idx,
          defaultOpacity: style2.opacity,
          defaultText
        });
        rect.ensureState("emphasis").style = emphasisStyle;
        rect.ensureState("blur").style = blurStyle;
        rect.ensureState("select").style = selectStyle;
        toggleHoverEmphasis(rect, focus, blurScope, emphasisDisabled);
        rect.incremental = getIncrementalId(seriesModel, useIncremental);
        if (useIncremental) {
          rect.states.emphasis.hoverLayer = HOVER_LAYER_FOR_INCREMENTAL;
        }
        group2.add(rect);
        data.setItemGraphicEl(idx, rect);
        if (this._progressiveEls) {
          this._progressiveEls.push(rect);
        }
      }
    };
    HeatmapView2.prototype._renderOnGeo = function(geo, seriesModel, visualMapModel, api) {
      var inRangeVisuals = visualMapModel.targetVisuals.inRange;
      var outOfRangeVisuals = visualMapModel.targetVisuals.outOfRange;
      var data = seriesModel.getData();
      var hmLayer = this._hmLayer || this._hmLayer || new HeatmapLayer_default();
      hmLayer.blurSize = seriesModel.get("blurSize");
      hmLayer.pointSize = seriesModel.get("pointSize");
      hmLayer.minOpacity = seriesModel.get("minOpacity");
      hmLayer.maxOpacity = seriesModel.get("maxOpacity");
      var rect = geo.getViewRect().clone();
      var roamTransform = geo.getRoamTransform();
      rect.applyTransform(roamTransform);
      var x = Math.max(rect.x, 0);
      var y = Math.max(rect.y, 0);
      var x2 = Math.min(rect.width + rect.x, api.getWidth());
      var y2 = Math.min(rect.height + rect.y, api.getHeight());
      var width = x2 - x;
      var height = y2 - y;
      var dims = [data.mapDimension("lng"), data.mapDimension("lat"), data.mapDimension("value")];
      var points4 = data.mapArray(dims, function(lng, lat, value) {
        var pt = geo.dataToPoint([lng, lat]);
        pt[0] -= x;
        pt[1] -= y;
        pt.push(value);
        return pt;
      });
      var dataExtent = visualMapModel.getExtent();
      var isInRange = visualMapModel.type === "visualMap.continuous" ? getIsInContinuousRange(dataExtent, visualMapModel.option.range) : getIsInPiecewiseRange(dataExtent, visualMapModel.getPieceList(), visualMapModel.option.selected);
      hmLayer.update(points4, width, height, inRangeVisuals.color.getNormalizer(), {
        inRange: inRangeVisuals.color.getColorMapper(),
        outOfRange: outOfRangeVisuals.color.getColorMapper()
      }, isInRange);
      var img = new Image_default({
        style: {
          width,
          height,
          x,
          y,
          image: hmLayer.canvas
        },
        silent: true
      });
      this.group.add(img);
    };
    HeatmapView2.type = "heatmap";
    return HeatmapView2;
  })(Chart_default)
);

// node_modules/echarts/lib/chart/heatmap/HeatmapSeries.js
var HeatmapSeriesModel = (
  /** @class */
  (function(_super) {
    __extends(HeatmapSeriesModel2, _super);
    function HeatmapSeriesModel2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = HeatmapSeriesModel2.type;
      return _this;
    }
    HeatmapSeriesModel2.prototype.getInitialData = function(option, ecModel) {
      return createSeriesData_default(null, this, {
        generateCoord: "value"
      });
    };
    HeatmapSeriesModel2.prototype.preventIncremental = function() {
      var coordSysCreator = CoordinateSystem_default.get(this.get("coordinateSystem"));
      if (coordSysCreator && coordSysCreator.dimensions) {
        return coordSysCreator.dimensions[0] === "lng" && coordSysCreator.dimensions[1] === "lat";
      }
    };
    HeatmapSeriesModel2.type = "series.heatmap";
    HeatmapSeriesModel2.dependencies = ["grid", "geo", "calendar", "matrix"];
    HeatmapSeriesModel2.defaultOption = {
      coordinateSystem: "cartesian2d",
      // zlevel: 0,
      z: 2,
      // Cartesian coordinate system
      // xAxisIndex: 0,
      // yAxisIndex: 0,
      // Geo coordinate system
      geoIndex: 0,
      blurSize: 30,
      pointSize: 20,
      maxOpacity: 1,
      minOpacity: 0,
      select: {
        itemStyle: {
          borderColor: tokens_default.color.primary
        }
      }
    };
    return HeatmapSeriesModel2;
  })(Series_default)
);

// node_modules/echarts/lib/chart/bar/PictorialBarView.js
var BAR_BORDER_WIDTH_QUERY = ["itemStyle", "borderWidth"];
var LAYOUT_ATTRS = [{
  xy: "x",
  wh: "width",
  index: 0,
  posDesc: ["left", "right"]
}, {
  xy: "y",
  wh: "height",
  index: 1,
  posDesc: ["top", "bottom"]
}];
var pathForLineWidth = new Circle_default();
var PictorialBarView = (
  /** @class */
  (function(_super) {
    __extends(PictorialBarView2, _super);
    function PictorialBarView2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = SERIES_TYPE_PICTORIAL_BAR;
      return _this;
    }
    PictorialBarView2.prototype.render = function(seriesModel, ecModel, api) {
      var group2 = this.group;
      var data = seriesModel.getData();
      var oldData = this._data;
      var cartesian = seriesModel.coordinateSystem;
      var baseAxis = cartesian.getBaseAxis();
      var isHorizontal = baseAxis.isHorizontal();
      var coordSysRect = cartesian.master.getRect();
      var opt = {
        ecSize: {
          width: api.getWidth(),
          height: api.getHeight()
        },
        seriesModel,
        coordSys: cartesian,
        coordSysExtent: [[coordSysRect.x, coordSysRect.x + coordSysRect.width], [coordSysRect.y, coordSysRect.y + coordSysRect.height]],
        isHorizontal,
        valueDim: LAYOUT_ATTRS[+isHorizontal],
        categoryDim: LAYOUT_ATTRS[1 - +isHorizontal]
      };
      data.diff(oldData).add(function(dataIndex) {
        if (!data.hasValue(dataIndex)) {
          return;
        }
        var itemModel = getItemModel(data, dataIndex);
        var symbolMeta = getSymbolMeta(data, dataIndex, itemModel, opt);
        var bar = createBar(data, opt, symbolMeta);
        data.setItemGraphicEl(dataIndex, bar);
        group2.add(bar);
        updateCommon2(bar, opt, symbolMeta);
      }).update(function(newIndex, oldIndex) {
        var bar = oldData.getItemGraphicEl(oldIndex);
        if (!data.hasValue(newIndex)) {
          group2.remove(bar);
          return;
        }
        var itemModel = getItemModel(data, newIndex);
        var symbolMeta = getSymbolMeta(data, newIndex, itemModel, opt);
        var pictorialShapeStr = getShapeStr(data, symbolMeta);
        if (bar && pictorialShapeStr !== bar.__pictorialShapeStr) {
          group2.remove(bar);
          data.setItemGraphicEl(newIndex, null);
          bar = null;
        }
        if (bar) {
          updateBar(bar, opt, symbolMeta);
        } else {
          bar = createBar(data, opt, symbolMeta, true);
        }
        data.setItemGraphicEl(newIndex, bar);
        bar.__pictorialSymbolMeta = symbolMeta;
        group2.add(bar);
        updateCommon2(bar, opt, symbolMeta);
      }).remove(function(dataIndex) {
        var bar = oldData.getItemGraphicEl(dataIndex);
        bar && removeBar(oldData, dataIndex, bar.__pictorialSymbolMeta.animationModel, bar);
      }).execute();
      var clipPath = seriesModel.get("clip", true) ? createClipPath(seriesModel.coordinateSystem, false, seriesModel) : null;
      if (clipPath) {
        group2.setClipPath(clipPath);
      } else {
        group2.removeClipPath();
      }
      this._data = data;
      return this.group;
    };
    PictorialBarView2.prototype.remove = function(ecModel, api) {
      var group2 = this.group;
      var data = this._data;
      if (ecModel.get("animation")) {
        if (data) {
          data.eachItemGraphicEl(function(bar) {
            removeBar(data, getECData(bar).dataIndex, ecModel, bar);
          });
        }
      } else {
        group2.removeAll();
      }
    };
    PictorialBarView2.type = SERIES_TYPE_PICTORIAL_BAR;
    return PictorialBarView2;
  })(Chart_default)
);
function getSymbolMeta(data, dataIndex, itemModel, opt) {
  var layout3 = data.getItemLayout(dataIndex);
  var symbolRepeat = itemModel.get("symbolRepeat");
  var symbolClip = itemModel.get("symbolClip");
  var symbolPosition = itemModel.get("symbolPosition") || "start";
  var symbolRotate = itemModel.get("symbolRotate");
  var rotation = (symbolRotate || 0) * Math.PI / 180 || 0;
  var symbolPatternSize = itemModel.get("symbolPatternSize") || 2;
  var isAnimationEnabled2 = itemModel.isAnimationEnabled();
  var symbolMeta = {
    dataIndex,
    layout: layout3,
    itemModel,
    symbolType: data.getItemVisual(dataIndex, "symbol") || "circle",
    style: data.getItemVisual(dataIndex, "style"),
    symbolClip,
    symbolRepeat,
    symbolRepeatDirection: itemModel.get("symbolRepeatDirection"),
    symbolPatternSize,
    rotation,
    animationModel: isAnimationEnabled2 ? itemModel : null,
    hoverScale: isAnimationEnabled2 && itemModel.get(["emphasis", "scale"]),
    z2: itemModel.getShallow("z", true) || 0
  };
  prepareBarLength(itemModel, symbolRepeat, layout3, opt, symbolMeta);
  prepareSymbolSize(data, dataIndex, layout3, symbolRepeat, symbolClip, symbolMeta.boundingLength, symbolMeta.pxSign, symbolPatternSize, opt, symbolMeta);
  prepareLineWidth(itemModel, symbolMeta.symbolScale, rotation, opt, symbolMeta);
  var symbolSize = symbolMeta.symbolSize;
  var symbolOffset = normalizeSymbolOffset(itemModel.get("symbolOffset"), symbolSize);
  prepareLayoutInfo(itemModel, symbolSize, layout3, symbolRepeat, symbolClip, symbolOffset, symbolPosition, symbolMeta.valueLineWidth, symbolMeta.boundingLength, symbolMeta.repeatCutLength, opt, symbolMeta);
  return symbolMeta;
}
function prepareBarLength(itemModel, symbolRepeat, layout3, opt, outputSymbolMeta) {
  var valueDim = opt.valueDim;
  var symbolBoundingData = itemModel.get("symbolBoundingData");
  var valueAxis2 = opt.coordSys.getOtherAxis(opt.coordSys.getBaseAxis());
  var zeroPx = valueAxis2.toGlobalCoord(valueAxis2.dataToCoord(0));
  var pxSignIdx = 1 - +(layout3[valueDim.wh] <= 0);
  var boundingLength;
  if (isArray(symbolBoundingData)) {
    var symbolBoundingExtent = [convertToCoordOnAxis(valueAxis2, symbolBoundingData[0]) - zeroPx, convertToCoordOnAxis(valueAxis2, symbolBoundingData[1]) - zeroPx];
    symbolBoundingExtent[1] < symbolBoundingExtent[0] && symbolBoundingExtent.reverse();
    boundingLength = symbolBoundingExtent[pxSignIdx];
  } else if (symbolBoundingData != null) {
    boundingLength = convertToCoordOnAxis(valueAxis2, symbolBoundingData) - zeroPx;
  } else if (symbolRepeat) {
    boundingLength = opt.coordSysExtent[valueDim.index][pxSignIdx] - zeroPx;
  } else {
    boundingLength = layout3[valueDim.wh];
  }
  outputSymbolMeta.boundingLength = boundingLength;
  if (symbolRepeat) {
    outputSymbolMeta.repeatCutLength = layout3[valueDim.wh];
  }
  var isXAxis = valueDim.xy === "x";
  var isInverse = valueAxis2.inverse;
  outputSymbolMeta.pxSign = isXAxis && !isInverse || !isXAxis && isInverse ? boundingLength >= 0 ? 1 : -1 : boundingLength > 0 ? 1 : -1;
}
function convertToCoordOnAxis(axis, value) {
  return axis.toGlobalCoord(axis.dataToCoord(axis.scale.parse(value)));
}
function prepareSymbolSize(data, dataIndex, layout3, symbolRepeat, symbolClip, boundingLength, pxSign, symbolPatternSize, opt, outputSymbolMeta) {
  var valueDim = opt.valueDim;
  var categoryDim = opt.categoryDim;
  var categorySize = Math.abs(layout3[categoryDim.wh]);
  var symbolSize = data.getItemVisual(dataIndex, "symbolSize");
  var parsedSymbolSize;
  if (isArray(symbolSize)) {
    parsedSymbolSize = symbolSize.slice();
  } else {
    if (symbolSize == null) {
      parsedSymbolSize = ["100%", "100%"];
    } else {
      parsedSymbolSize = [symbolSize, symbolSize];
    }
  }
  parsedSymbolSize[categoryDim.index] = parsePercent2(parsedSymbolSize[categoryDim.index], categorySize);
  parsedSymbolSize[valueDim.index] = parsePercent2(parsedSymbolSize[valueDim.index], symbolRepeat ? categorySize : Math.abs(boundingLength));
  outputSymbolMeta.symbolSize = parsedSymbolSize;
  var symbolScale = outputSymbolMeta.symbolScale = [parsedSymbolSize[0] / symbolPatternSize, parsedSymbolSize[1] / symbolPatternSize];
  symbolScale[valueDim.index] *= (opt.isHorizontal ? -1 : 1) * pxSign;
}
function prepareLineWidth(itemModel, symbolScale, rotation, opt, outputSymbolMeta) {
  var valueLineWidth = itemModel.get(BAR_BORDER_WIDTH_QUERY) || 0;
  if (valueLineWidth) {
    pathForLineWidth.attr({
      scaleX: symbolScale[0],
      scaleY: symbolScale[1],
      rotation
    });
    pathForLineWidth.updateTransform();
    valueLineWidth /= pathForLineWidth.getLineScale();
    valueLineWidth *= symbolScale[opt.valueDim.index];
  }
  outputSymbolMeta.valueLineWidth = valueLineWidth || 0;
}
function prepareLayoutInfo(itemModel, symbolSize, layout3, symbolRepeat, symbolClip, symbolOffset, symbolPosition, valueLineWidth, boundingLength, repeatCutLength, opt, outputSymbolMeta) {
  var categoryDim = opt.categoryDim;
  var valueDim = opt.valueDim;
  var pxSign = outputSymbolMeta.pxSign;
  var unitLength = Math.max(symbolSize[valueDim.index] + valueLineWidth, 0);
  var pathLen = unitLength;
  if (symbolRepeat) {
    var absBoundingLength = Math.abs(boundingLength);
    var symbolMargin = retrieve(itemModel.get("symbolMargin"), "15%") + "";
    var hasEndGap = false;
    if (symbolMargin.lastIndexOf("!") === symbolMargin.length - 1) {
      hasEndGap = true;
      symbolMargin = symbolMargin.slice(0, symbolMargin.length - 1);
    }
    var symbolMarginNumeric = parsePercent2(symbolMargin, symbolSize[valueDim.index]);
    var uLenWithMargin = Math.max(unitLength + symbolMarginNumeric * 2, 0);
    var endFix = hasEndGap ? 0 : symbolMarginNumeric * 2;
    var repeatSpecified = isNumeric(symbolRepeat);
    var repeatTimes = repeatSpecified ? symbolRepeat : toIntTimes((absBoundingLength + endFix) / uLenWithMargin);
    var mDiff = absBoundingLength - repeatTimes * unitLength;
    symbolMarginNumeric = mDiff / 2 / (hasEndGap ? repeatTimes : Math.max(repeatTimes - 1, 1));
    uLenWithMargin = unitLength + symbolMarginNumeric * 2;
    endFix = hasEndGap ? 0 : symbolMarginNumeric * 2;
    if (!repeatSpecified && symbolRepeat !== "fixed") {
      repeatTimes = repeatCutLength ? toIntTimes((Math.abs(repeatCutLength) + endFix) / uLenWithMargin) : 0;
    }
    pathLen = repeatTimes * uLenWithMargin - endFix;
    outputSymbolMeta.repeatTimes = repeatTimes;
    outputSymbolMeta.symbolMargin = symbolMarginNumeric;
  }
  var sizeFix = pxSign * (pathLen / 2);
  var pathPosition = outputSymbolMeta.pathPosition = [];
  pathPosition[categoryDim.index] = layout3[categoryDim.wh] / 2;
  pathPosition[valueDim.index] = symbolPosition === "start" ? sizeFix : symbolPosition === "end" ? boundingLength - sizeFix : boundingLength / 2;
  if (symbolOffset) {
    pathPosition[0] += symbolOffset[0];
    pathPosition[1] += symbolOffset[1];
  }
  var bundlePosition = outputSymbolMeta.bundlePosition = [];
  bundlePosition[categoryDim.index] = layout3[categoryDim.xy];
  bundlePosition[valueDim.index] = layout3[valueDim.xy];
  var barRectShape = outputSymbolMeta.barRectShape = extend({}, layout3);
  barRectShape[valueDim.wh] = pxSign * Math.max(Math.abs(layout3[valueDim.wh]), Math.abs(pathPosition[valueDim.index] + sizeFix));
  barRectShape[categoryDim.wh] = layout3[categoryDim.wh];
  var clipShape = outputSymbolMeta.clipShape = {};
  clipShape[categoryDim.xy] = -layout3[categoryDim.xy];
  clipShape[categoryDim.wh] = opt.ecSize[categoryDim.wh];
  clipShape[valueDim.xy] = 0;
  clipShape[valueDim.wh] = layout3[valueDim.wh];
}
function createPath(symbolMeta) {
  var symbolPatternSize = symbolMeta.symbolPatternSize;
  var path = createSymbol(
    // Consider texture img, make a big size.
    symbolMeta.symbolType,
    -symbolPatternSize / 2,
    -symbolPatternSize / 2,
    symbolPatternSize,
    symbolPatternSize
  );
  path.attr({
    culling: true
  });
  path.type !== "image" && path.setStyle({
    strokeNoScale: true
  });
  return path;
}
function createOrUpdateRepeatSymbols(bar, opt, symbolMeta, isUpdate) {
  var bundle = bar.__pictorialBundle;
  var symbolSize = symbolMeta.symbolSize;
  var valueLineWidth = symbolMeta.valueLineWidth;
  var pathPosition = symbolMeta.pathPosition;
  var valueDim = opt.valueDim;
  var repeatTimes = symbolMeta.repeatTimes || 0;
  var index = 0;
  var unit = symbolSize[opt.valueDim.index] + valueLineWidth + symbolMeta.symbolMargin * 2;
  eachPath(bar, function(path2) {
    path2.__pictorialAnimationIndex = index;
    path2.__pictorialRepeatTimes = repeatTimes;
    if (index < repeatTimes) {
      updateAttr(path2, null, makeTarget(index), symbolMeta, isUpdate);
    } else {
      updateAttr(path2, null, {
        scaleX: 0,
        scaleY: 0
      }, symbolMeta, isUpdate, function() {
        bundle.remove(path2);
      });
    }
    index++;
  });
  for (; index < repeatTimes; index++) {
    var path = createPath(symbolMeta);
    path.__pictorialAnimationIndex = index;
    path.__pictorialRepeatTimes = repeatTimes;
    bundle.add(path);
    var target = makeTarget(index);
    updateAttr(path, {
      x: target.x,
      y: target.y,
      scaleX: 0,
      scaleY: 0
    }, {
      scaleX: target.scaleX,
      scaleY: target.scaleY,
      rotation: target.rotation
    }, symbolMeta, isUpdate);
  }
  function makeTarget(index2) {
    var position = pathPosition.slice();
    var pxSign = symbolMeta.pxSign;
    var i2 = index2;
    if (symbolMeta.symbolRepeatDirection === "start" ? pxSign > 0 : pxSign < 0) {
      i2 = repeatTimes - 1 - index2;
    }
    position[valueDim.index] = unit * (i2 - repeatTimes / 2 + 0.5) + pathPosition[valueDim.index];
    return {
      x: position[0],
      y: position[1],
      scaleX: symbolMeta.symbolScale[0],
      scaleY: symbolMeta.symbolScale[1],
      rotation: symbolMeta.rotation
    };
  }
}
function createOrUpdateSingleSymbol(bar, opt, symbolMeta, isUpdate) {
  var bundle = bar.__pictorialBundle;
  var mainPath = bar.__pictorialMainPath;
  if (!mainPath) {
    mainPath = bar.__pictorialMainPath = createPath(symbolMeta);
    bundle.add(mainPath);
    updateAttr(mainPath, {
      x: symbolMeta.pathPosition[0],
      y: symbolMeta.pathPosition[1],
      scaleX: 0,
      scaleY: 0,
      rotation: symbolMeta.rotation
    }, {
      scaleX: symbolMeta.symbolScale[0],
      scaleY: symbolMeta.symbolScale[1]
    }, symbolMeta, isUpdate);
  } else {
    updateAttr(mainPath, null, {
      x: symbolMeta.pathPosition[0],
      y: symbolMeta.pathPosition[1],
      scaleX: symbolMeta.symbolScale[0],
      scaleY: symbolMeta.symbolScale[1],
      rotation: symbolMeta.rotation
    }, symbolMeta, isUpdate);
  }
}
function createOrUpdateBarRect(bar, symbolMeta, isUpdate) {
  var rectShape = extend({}, symbolMeta.barRectShape);
  var barRect = bar.__pictorialBarRect;
  if (!barRect) {
    barRect = bar.__pictorialBarRect = new Rect_default({
      z2: 2,
      shape: rectShape,
      silent: true,
      style: {
        stroke: "transparent",
        fill: "transparent",
        lineWidth: 0
      }
    });
    barRect.disableMorphing = true;
    bar.add(barRect);
  } else {
    updateAttr(barRect, null, {
      shape: rectShape
    }, symbolMeta, isUpdate);
  }
}
function createOrUpdateClip(bar, opt, symbolMeta, isUpdate) {
  if (symbolMeta.symbolClip) {
    var clipPath = bar.__pictorialClipPath;
    var clipShape = extend({}, symbolMeta.clipShape);
    var valueDim = opt.valueDim;
    var animationModel = symbolMeta.animationModel;
    var dataIndex = symbolMeta.dataIndex;
    if (clipPath) {
      updateProps(clipPath, {
        shape: clipShape
      }, animationModel, dataIndex);
    } else {
      clipShape[valueDim.wh] = 0;
      clipPath = new Rect_default({
        shape: clipShape
      });
      bar.__pictorialBundle.setClipPath(clipPath);
      bar.__pictorialClipPath = clipPath;
      var target = {};
      target[valueDim.wh] = symbolMeta.clipShape[valueDim.wh];
      graphic_exports[isUpdate ? "updateProps" : "initProps"](clipPath, {
        shape: target
      }, animationModel, dataIndex);
    }
  }
}
function getItemModel(data, dataIndex) {
  var itemModel = data.getItemModel(dataIndex);
  itemModel.getAnimationDelayParams = getAnimationDelayParams;
  itemModel.isAnimationEnabled = isAnimationEnabled;
  return itemModel;
}
function getAnimationDelayParams(path) {
  return {
    index: path.__pictorialAnimationIndex,
    count: path.__pictorialRepeatTimes
  };
}
function isAnimationEnabled() {
  return this.parentModel.isAnimationEnabled() && !!this.getShallow("animation");
}
function createBar(data, opt, symbolMeta, isUpdate) {
  var bar = new Group_default();
  var bundle = new Group_default();
  bar.add(bundle);
  bar.__pictorialBundle = bundle;
  bundle.x = symbolMeta.bundlePosition[0];
  bundle.y = symbolMeta.bundlePosition[1];
  if (symbolMeta.symbolRepeat) {
    createOrUpdateRepeatSymbols(bar, opt, symbolMeta);
  } else {
    createOrUpdateSingleSymbol(bar, opt, symbolMeta);
  }
  createOrUpdateBarRect(bar, symbolMeta, isUpdate);
  createOrUpdateClip(bar, opt, symbolMeta, isUpdate);
  bar.__pictorialShapeStr = getShapeStr(data, symbolMeta);
  bar.__pictorialSymbolMeta = symbolMeta;
  return bar;
}
function updateBar(bar, opt, symbolMeta) {
  var animationModel = symbolMeta.animationModel;
  var dataIndex = symbolMeta.dataIndex;
  var bundle = bar.__pictorialBundle;
  updateProps(bundle, {
    x: symbolMeta.bundlePosition[0],
    y: symbolMeta.bundlePosition[1]
  }, animationModel, dataIndex);
  if (symbolMeta.symbolRepeat) {
    createOrUpdateRepeatSymbols(bar, opt, symbolMeta, true);
  } else {
    createOrUpdateSingleSymbol(bar, opt, symbolMeta, true);
  }
  createOrUpdateBarRect(bar, symbolMeta, true);
  createOrUpdateClip(bar, opt, symbolMeta, true);
}
function removeBar(data, dataIndex, animationModel, bar) {
  var labelRect = bar.__pictorialBarRect;
  labelRect && labelRect.removeTextContent();
  var paths = [];
  eachPath(bar, function(path) {
    paths.push(path);
  });
  bar.__pictorialMainPath && paths.push(bar.__pictorialMainPath);
  bar.__pictorialClipPath && (animationModel = null);
  each(paths, function(path) {
    removeElement(path, {
      scaleX: 0,
      scaleY: 0
    }, animationModel, dataIndex, function() {
      bar.parent && bar.parent.remove(bar);
    });
  });
  data.setItemGraphicEl(dataIndex, null);
}
function getShapeStr(data, symbolMeta) {
  return [data.getItemVisual(symbolMeta.dataIndex, "symbol") || "none", !!symbolMeta.symbolRepeat, !!symbolMeta.symbolClip].join(":");
}
function eachPath(bar, cb, context) {
  each(bar.__pictorialBundle.children(), function(el) {
    el !== bar.__pictorialBarRect && cb.call(context, el);
  });
}
function updateAttr(el, immediateAttrs, animationAttrs, symbolMeta, isUpdate, cb) {
  immediateAttrs && el.attr(immediateAttrs);
  if (symbolMeta.symbolClip && !isUpdate) {
    animationAttrs && el.attr(animationAttrs);
  } else {
    animationAttrs && graphic_exports[isUpdate ? "updateProps" : "initProps"](el, animationAttrs, symbolMeta.animationModel, symbolMeta.dataIndex, cb);
  }
}
function updateCommon2(bar, opt, symbolMeta) {
  var dataIndex = symbolMeta.dataIndex;
  var itemModel = symbolMeta.itemModel;
  var emphasisModel = itemModel.getModel("emphasis");
  var emphasisStyle = emphasisModel.getModel("itemStyle").getItemStyle();
  var blurStyle = itemModel.getModel(["blur", "itemStyle"]).getItemStyle();
  var selectStyle = itemModel.getModel(["select", "itemStyle"]).getItemStyle();
  var cursorStyle = itemModel.getShallow("cursor");
  var focus = emphasisModel.get("focus");
  var blurScope = emphasisModel.get("blurScope");
  var hoverScale = emphasisModel.get("scale");
  eachPath(bar, function(path) {
    if (path instanceof Image_default) {
      var pathStyle = path.style;
      path.useStyle(extend({
        // TODO other properties like dx, dy ?
        image: pathStyle.image,
        x: pathStyle.x,
        y: pathStyle.y,
        width: pathStyle.width,
        height: pathStyle.height
      }, symbolMeta.style));
    } else {
      path.useStyle(symbolMeta.style);
    }
    var emphasisState = path.ensureState("emphasis");
    emphasisState.style = emphasisStyle;
    if (hoverScale) {
      emphasisState.scaleX = path.scaleX * 1.1;
      emphasisState.scaleY = path.scaleY * 1.1;
    }
    path.ensureState("blur").style = blurStyle;
    path.ensureState("select").style = selectStyle;
    cursorStyle && (path.cursor = cursorStyle);
    path.z2 = symbolMeta.z2;
  });
  var barPositionOutside = opt.valueDim.posDesc[+(symbolMeta.boundingLength > 0)];
  var barRect = bar.__pictorialBarRect;
  barRect.ignoreClip = true;
  setLabelStyle(barRect, getLabelStatesModels(itemModel), {
    labelFetcher: opt.seriesModel,
    labelDataIndex: dataIndex,
    defaultText: getDefaultLabel(opt.seriesModel.getData(), dataIndex),
    inheritColor: symbolMeta.style.fill,
    defaultOpacity: symbolMeta.style.opacity,
    defaultOutsidePosition: barPositionOutside
  });
  toggleHoverEmphasis(bar, focus, blurScope, emphasisModel.get("disabled"));
}
function toIntTimes(times) {
  var roundedTimes = Math.round(times);
  return Math.abs(times - roundedTimes) < 1e-4 ? roundedTimes : Math.ceil(times);
}

// node_modules/echarts/lib/chart/bar/PictorialBarSeries.js
var PictorialBarSeriesModel = (
  /** @class */
  (function(_super) {
    __extends(PictorialBarSeriesModel2, _super);
    function PictorialBarSeriesModel2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = PictorialBarSeriesModel2.type;
      _this.hasSymbolVisual = true;
      _this.defaultSymbol = "roundRect";
      return _this;
    }
    PictorialBarSeriesModel2.prototype.getInitialData = function(option) {
      option.stack = null;
      return _super.prototype.getInitialData.apply(this, arguments);
    };
    PictorialBarSeriesModel2.type = "series." + SERIES_TYPE_PICTORIAL_BAR;
    PictorialBarSeriesModel2.dependencies = ["grid"];
    PictorialBarSeriesModel2.defaultOption = inheritDefaultOption(BaseBarSeries_default.defaultOption, {
      symbol: "circle",
      symbolSize: null,
      symbolRotate: null,
      symbolPosition: null,
      symbolOffset: null,
      symbolMargin: null,
      symbolRepeat: false,
      symbolRepeatDirection: "end",
      symbolClip: false,
      symbolBoundingData: null,
      symbolPatternSize: 400,
      barGap: "-100%",
      // Pictorial bar do not clip by default because in many cases
      // xAxis and yAxis are not displayed and it's expected not to clip
      clip: false,
      // z can be set in data item, which is z2 actually.
      // Disable progressive
      progressive: 0,
      emphasis: {
        // By default pictorialBar do not hover scale. Hover scale is not suitable
        // for the case that both has foreground and background.
        scale: false
      },
      select: {
        itemStyle: {
          borderColor: tokens_default.color.primary
        }
      }
    });
    return PictorialBarSeriesModel2;
  })(BaseBarSeries_default)
);

// node_modules/echarts/lib/chart/themeRiver/ThemeRiverSeries.js
var DATA_NAME_INDEX = 2;
var SERIES_TYPE_THEME_RIVER = "themeRiver";
var ThemeRiverSeriesModel = (
  /** @class */
  (function(_super) {
    __extends(ThemeRiverSeriesModel2, _super);
    function ThemeRiverSeriesModel2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = ThemeRiverSeriesModel2.type;
      return _this;
    }
    ThemeRiverSeriesModel2.prototype.init = function(option) {
      _super.prototype.init.apply(this, arguments);
      this.legendVisualProvider = new LegendVisualProvider_default(bind(this.getData, this), bind(this.getRawData, this));
    };
    ThemeRiverSeriesModel2.prototype.fixData = function(data) {
      var rawDataLength = data.length;
      var timeValueKeys = {};
      var groupResult = groupData(data, function(item) {
        if (!timeValueKeys.hasOwnProperty(item[0] + "")) {
          timeValueKeys[item[0] + ""] = -1;
        }
        return item[2];
      });
      var layerData = [];
      groupResult.buckets.each(function(items, key2) {
        layerData.push({
          name: key2,
          dataList: items
        });
      });
      var layerNum = layerData.length;
      for (var k2 = 0; k2 < layerNum; ++k2) {
        var name_1 = layerData[k2].name;
        for (var j = 0; j < layerData[k2].dataList.length; ++j) {
          var timeValue = layerData[k2].dataList[j][0] + "";
          timeValueKeys[timeValue] = k2;
        }
        for (var timeValue in timeValueKeys) {
          if (timeValueKeys.hasOwnProperty(timeValue) && timeValueKeys[timeValue] !== k2) {
            timeValueKeys[timeValue] = k2;
            data[rawDataLength] = [timeValue, 0, name_1];
            rawDataLength++;
          }
        }
      }
      return data;
    };
    ThemeRiverSeriesModel2.prototype.getInitialData = function(option, ecModel) {
      var singleAxisModel = this.getReferringComponents("singleAxis", SINGLE_REFERRING).models[0];
      var axisType = singleAxisModel.get("type");
      var filterData = filter2(option.data, function(dataItem) {
        return dataItem[2] !== void 0;
      });
      var data = this.fixData(filterData || []);
      var nameList = [];
      var nameMap = this.nameMap = createHashMap();
      var count2 = 0;
      for (var i2 = 0; i2 < data.length; ++i2) {
        nameList.push(data[i2][DATA_NAME_INDEX]);
        if (!nameMap.get(data[i2][DATA_NAME_INDEX])) {
          nameMap.set(data[i2][DATA_NAME_INDEX], count2);
          count2++;
        }
      }
      var dimensions = prepareSeriesDataSchema(data, {
        coordDimensions: ["single"],
        dimensionsDefine: [{
          name: "time",
          type: getDimensionTypeByAxis(axisType)
        }, {
          name: "value",
          type: "float"
        }, {
          name: "name",
          type: "ordinal"
        }],
        encodeDefine: {
          single: 0,
          value: 1,
          itemName: 2
        }
      }).dimensions;
      var list = new SeriesData_default(dimensions, this);
      list.initData(data);
      return list;
    };
    ThemeRiverSeriesModel2.prototype.getLayerSeries = function() {
      var data = this.getData();
      var lenCount = data.count();
      var indexArr = [];
      for (var i2 = 0; i2 < lenCount; ++i2) {
        indexArr[i2] = i2;
      }
      var timeDim = data.mapDimension("single");
      var groupResult = groupData(indexArr, function(index) {
        return data.get("name", index);
      });
      var layerSeries = [];
      groupResult.buckets.each(function(items, key2) {
        items.sort(function(index1, index2) {
          return data.get(timeDim, index1) - data.get(timeDim, index2);
        });
        layerSeries.push({
          name: key2,
          indices: items
        });
      });
      return layerSeries;
    };
    ThemeRiverSeriesModel2.prototype.getAxisTooltipData = function(dim, value, baseAxis) {
      if (!isArray(dim)) {
        dim = dim ? [dim] : [];
      }
      var data = this.getData();
      var layerSeries = this.getLayerSeries();
      var indices = [];
      var layerNum = layerSeries.length;
      var nestestValue;
      for (var i2 = 0; i2 < layerNum; ++i2) {
        var minDist = Number.MAX_VALUE;
        var nearestIdx = -1;
        var pointNum = layerSeries[i2].indices.length;
        for (var j = 0; j < pointNum; ++j) {
          var theValue = data.get(dim[0], layerSeries[i2].indices[j]);
          var dist3 = Math.abs(theValue - value);
          if (dist3 <= minDist) {
            nestestValue = theValue;
            minDist = dist3;
            nearestIdx = layerSeries[i2].indices[j];
          }
        }
        indices.push(nearestIdx);
      }
      return {
        dataIndices: indices,
        nestestValue
      };
    };
    ThemeRiverSeriesModel2.prototype.formatTooltip = function(dataIndex, multipleSeries, dataType) {
      var data = this.getData();
      var name = data.getName(dataIndex);
      var value = data.get(data.mapDimension("value"), dataIndex);
      return createTooltipMarkup("nameValue", {
        name,
        value
      });
    };
    ThemeRiverSeriesModel2.type = "series." + SERIES_TYPE_THEME_RIVER;
    ThemeRiverSeriesModel2.dependencies = ["singleAxis"];
    ThemeRiverSeriesModel2.defaultOption = {
      // zlevel: 0,
      z: 2,
      colorBy: "data",
      coordinateSystem: "singleAxis",
      // gap in axis's orthogonal orientation
      boundaryGap: ["10%", "10%"],
      // legendHoverLink: true,
      singleAxisIndex: 0,
      animationEasing: "linear",
      label: {
        margin: 4,
        show: true,
        position: "left",
        fontSize: 11
      },
      emphasis: {
        label: {
          show: true
        }
      }
    };
    return ThemeRiverSeriesModel2;
  })(Series_default)
);

// node_modules/echarts/lib/chart/themeRiver/ThemeRiverView.js
var ThemeRiverView = (
  /** @class */
  (function(_super) {
    __extends(ThemeRiverView2, _super);
    function ThemeRiverView2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = SERIES_TYPE_THEME_RIVER;
      _this._layers = [];
      return _this;
    }
    ThemeRiverView2.prototype.render = function(seriesModel, ecModel, api) {
      var data = seriesModel.getData();
      var self2 = this;
      var group2 = this.group;
      var layersSeries = seriesModel.getLayerSeries();
      var layoutInfo = data.getLayout("layoutInfo");
      var rect = layoutInfo.rect;
      var boundaryGap = layoutInfo.boundaryGap;
      group2.x = 0;
      group2.y = rect.y + boundaryGap[0];
      function keyGetter(item) {
        return item.name;
      }
      var dataDiffer = new DataDiffer_default(this._layersSeries || [], layersSeries, keyGetter, keyGetter);
      var newLayersGroups = [];
      dataDiffer.add(bind(process2, this, "add")).update(bind(process2, this, "update")).remove(bind(process2, this, "remove")).execute();
      function process2(status, idx, oldIdx) {
        var oldLayersGroups = self2._layers;
        if (status === "remove") {
          group2.remove(oldLayersGroups[idx]);
          return;
        }
        var points0 = [];
        var points1 = [];
        var style2;
        var indices = layersSeries[idx].indices;
        var j = 0;
        for (; j < indices.length; j++) {
          var layout3 = data.getItemLayout(indices[j]);
          var x = layout3.x;
          var y0 = layout3.y0;
          var y = layout3.y;
          points0.push(x, y0);
          points1.push(x, y0 + y);
          style2 = data.getItemVisual(indices[j], "style");
        }
        var polygon;
        var textLayout = data.getItemLayout(indices[0]);
        var labelModel = seriesModel.getModel("label");
        var margin = labelModel.get("margin");
        var emphasisModel = seriesModel.getModel("emphasis");
        if (status === "add") {
          var layerGroup = newLayersGroups[idx] = new Group_default();
          polygon = new ECPolygon({
            shape: {
              points: points0,
              stackedOnPoints: points1,
              smooth: 0.4,
              stackedOnSmooth: 0.4,
              smoothConstraint: false
            },
            z2: 0
          });
          layerGroup.add(polygon);
          group2.add(layerGroup);
          if (seriesModel.isAnimationEnabled()) {
            polygon.setClipPath(createGridClipShape3(polygon.getBoundingRect(), seriesModel, function() {
              polygon.removeClipPath();
            }));
          }
        } else {
          var layerGroup = oldLayersGroups[oldIdx];
          polygon = layerGroup.childAt(0);
          group2.add(layerGroup);
          newLayersGroups[idx] = layerGroup;
          updateProps(polygon, {
            shape: {
              points: points0,
              stackedOnPoints: points1
            }
          }, seriesModel);
          saveOldStyle(polygon);
        }
        setLabelStyle(polygon, getLabelStatesModels(seriesModel), {
          labelDataIndex: indices[j - 1],
          defaultText: data.getName(indices[j - 1]),
          inheritColor: style2.fill
        }, {
          normal: {
            verticalAlign: "middle"
            // align: 'right'
          }
        });
        polygon.setTextConfig({
          position: null,
          local: true
        });
        var labelEl = polygon.getTextContent();
        if (labelEl) {
          labelEl.x = textLayout.x - margin;
          labelEl.y = textLayout.y0 + textLayout.y / 2;
        }
        polygon.useStyle(style2);
        data.setItemGraphicEl(idx, polygon);
        setStatesStylesFromModel(polygon, seriesModel);
        toggleHoverEmphasis(polygon, emphasisModel.get("focus"), emphasisModel.get("blurScope"), emphasisModel.get("disabled"));
      }
      this._layersSeries = layersSeries;
      this._layers = newLayersGroups;
    };
    ThemeRiverView2.type = SERIES_TYPE_THEME_RIVER;
    return ThemeRiverView2;
  })(Chart_default)
);
function createGridClipShape3(rect, seriesModel, cb) {
  var rectEl = new Rect_default({
    shape: {
      x: rect.x - 10,
      y: rect.y - 10,
      width: 0,
      height: rect.height + 20
    }
  });
  initProps(rectEl, {
    shape: {
      x: rect.x - 50,
      width: rect.width + 100,
      height: rect.height + 20
    }
  }, seriesModel, cb);
  return rectEl;
}

// node_modules/echarts/lib/chart/themeRiver/themeRiverLayout.js
var themeRiverLayoutStageHandler = createSimpleOverallStageHandler(SERIES_TYPE_THEME_RIVER, themeRiverLayout);
function themeRiverLayout(ecModel, api) {
  ecModel.eachSeriesByType(SERIES_TYPE_THEME_RIVER, function(seriesModel) {
    var data = seriesModel.getData();
    var single = seriesModel.coordinateSystem;
    var layoutInfo = {};
    var rect = single.getRect();
    layoutInfo.rect = rect;
    var boundaryGap = seriesModel.get("boundaryGap");
    var axis = single.getAxis();
    layoutInfo.boundaryGap = boundaryGap;
    if (axis.orient === "horizontal") {
      boundaryGap[0] = parsePercent2(boundaryGap[0], rect.height);
      boundaryGap[1] = parsePercent2(boundaryGap[1], rect.height);
      var height = rect.height - boundaryGap[0] - boundaryGap[1];
      doThemeRiverLayout(data, seriesModel, height);
    } else {
      boundaryGap[0] = parsePercent2(boundaryGap[0], rect.width);
      boundaryGap[1] = parsePercent2(boundaryGap[1], rect.width);
      var width = rect.width - boundaryGap[0] - boundaryGap[1];
      doThemeRiverLayout(data, seriesModel, width);
    }
    data.setLayout("layoutInfo", layoutInfo);
  });
}
function doThemeRiverLayout(data, seriesModel, height) {
  if (!data.count()) {
    return;
  }
  var coordSys = seriesModel.coordinateSystem;
  var layerSeries = seriesModel.getLayerSeries();
  var timeDim = data.mapDimension("single");
  var valueDim = data.mapDimension("value");
  var layerPoints = map2(layerSeries, function(singleLayer) {
    return map2(singleLayer.indices, function(idx) {
      var pt = coordSys.dataToPoint(data.get(timeDim, idx));
      pt[1] = data.get(valueDim, idx);
      return pt;
    });
  });
  var base2 = computeBaseline(layerPoints);
  var baseLine = base2.y0;
  var ky = height / base2.max;
  var n = layerSeries.length;
  var m2 = layerSeries[0].indices.length;
  var baseY0;
  for (var j = 0; j < m2; ++j) {
    baseY0 = baseLine[j] * ky;
    data.setItemLayout(layerSeries[0].indices[j], {
      layerIndex: 0,
      x: layerPoints[0][j][0],
      y0: baseY0,
      y: layerPoints[0][j][1] * ky
    });
    for (var i2 = 1; i2 < n; ++i2) {
      baseY0 += layerPoints[i2 - 1][j][1] * ky;
      data.setItemLayout(layerSeries[i2].indices[j], {
        layerIndex: i2,
        x: layerPoints[i2][j][0],
        y0: baseY0,
        y: layerPoints[i2][j][1] * ky
      });
    }
  }
}
function computeBaseline(data) {
  var layerNum = data.length;
  var pointNum = data[0].length;
  var sums = [];
  var y0 = [];
  var max3 = 0;
  for (var i2 = 0; i2 < pointNum; ++i2) {
    var temp = 0;
    for (var j = 0; j < layerNum; ++j) {
      temp += data[j][i2][1];
    }
    if (temp > max3) {
      max3 = temp;
    }
    sums.push(temp);
  }
  for (var k2 = 0; k2 < pointNum; ++k2) {
    y0[k2] = (max3 - sums[k2]) / 2;
  }
  max3 = 0;
  for (var l = 0; l < pointNum; ++l) {
    var sum2 = sums[l] + y0[l];
    if (sum2 > max3) {
      max3 = sum2;
    }
  }
  return {
    y0,
    max: max3
  };
}

// node_modules/echarts/lib/chart/sunburst/SunburstPiece.js
var DEFAULT_SECTOR_Z = 2;
var DEFAULT_TEXT_Z = 4;
var SunburstPiece = (
  /** @class */
  (function(_super) {
    __extends(SunburstPiece2, _super);
    function SunburstPiece2(node, seriesModel, ecModel, api) {
      var _this = _super.call(this) || this;
      _this.z2 = DEFAULT_SECTOR_Z;
      _this.textConfig = {
        inside: true
      };
      getECData(_this).seriesIndex = seriesModel.seriesIndex;
      var text = new Text_default({
        z2: DEFAULT_TEXT_Z,
        silent: node.getModel().get(["label", "silent"])
      });
      _this.setTextContent(text);
      _this.updateData(true, node, seriesModel, ecModel, api);
      return _this;
    }
    SunburstPiece2.prototype.updateData = function(firstCreate, node, seriesModel, ecModel, api) {
      this.node = node;
      node.piece = this;
      seriesModel = seriesModel || this._seriesModel;
      ecModel = ecModel || this._ecModel;
      var sector = this;
      getECData(sector).dataIndex = node.dataIndex;
      var itemModel = node.getModel();
      var emphasisModel = itemModel.getModel("emphasis");
      var layout3 = node.getLayout();
      var sectorShape = extend({}, layout3);
      sectorShape.label = null;
      var normalStyle = node.getVisual("style");
      normalStyle.lineJoin = "bevel";
      var decal = node.getVisual("decal");
      if (decal) {
        normalStyle.decal = createOrUpdatePatternFromDecal(decal, api);
      }
      var cornerRadius = getSectorCornerRadius(itemModel.getModel("itemStyle"), sectorShape, true);
      extend(sectorShape, cornerRadius);
      each(SPECIAL_STATES, function(stateName) {
        var state2 = sector.ensureState(stateName);
        var itemStyleModel = itemModel.getModel([stateName, "itemStyle"]);
        state2.style = itemStyleModel.getItemStyle();
        var cornerRadius2 = getSectorCornerRadius(itemStyleModel, sectorShape);
        if (cornerRadius2) {
          state2.shape = cornerRadius2;
        }
      });
      if (firstCreate) {
        sector.setShape(sectorShape);
        sector.shape.r = layout3.r0;
        initProps(sector, {
          shape: {
            r: layout3.r
          }
        }, seriesModel, node.dataIndex);
      } else {
        updateProps(sector, {
          shape: sectorShape
        }, seriesModel);
        saveOldStyle(sector);
      }
      sector.useStyle(normalStyle);
      this._updateLabel(seriesModel);
      var cursorStyle = itemModel.getShallow("cursor");
      cursorStyle && sector.attr("cursor", cursorStyle);
      this._seriesModel = seriesModel || this._seriesModel;
      this._ecModel = ecModel || this._ecModel;
      var focus = emphasisModel.get("focus");
      var focusOrIndices = focus === "relative" ? concatArray(node.getAncestorsIndices(), node.getDescendantIndices()) : focus === "ancestor" ? node.getAncestorsIndices() : focus === "descendant" ? node.getDescendantIndices() : focus;
      toggleHoverEmphasis(this, focusOrIndices, emphasisModel.get("blurScope"), emphasisModel.get("disabled"));
    };
    SunburstPiece2.prototype._updateLabel = function(seriesModel) {
      var _this = this;
      var itemModel = this.node.getModel();
      var normalLabelModel = itemModel.getModel("label");
      var layout3 = this.node.getLayout();
      var angle = layout3.endAngle - layout3.startAngle;
      var midAngle = (layout3.startAngle + layout3.endAngle) / 2;
      var dx = Math.cos(midAngle);
      var dy = Math.sin(midAngle);
      var sector = this;
      var label = sector.getTextContent();
      var dataIndex = this.node.dataIndex;
      var labelMinAngle = normalLabelModel.get("minAngle") / 180 * Math.PI;
      var isNormalShown = normalLabelModel.get("show") && !(labelMinAngle != null && Math.abs(angle) < labelMinAngle);
      label.ignore = !isNormalShown;
      each(DISPLAY_STATES, function(stateName) {
        var labelStateModel = stateName === "normal" ? itemModel.getModel("label") : itemModel.getModel([stateName, "label"]);
        var isNormal = stateName === "normal";
        var state2 = isNormal ? label : label.ensureState(stateName);
        var text = seriesModel.getFormattedLabel(dataIndex, stateName);
        if (isNormal) {
          text = text || _this.node.name;
        }
        state2.style = createTextStyle(labelStateModel, {}, null, stateName !== "normal", true);
        if (text) {
          state2.style.text = text;
        }
        var isShown = labelStateModel.get("show");
        if (isShown != null && !isNormal) {
          state2.ignore = !isShown;
        }
        var labelPosition = getLabelAttr(labelStateModel, "position");
        var sectorState = isNormal ? sector : sector.states[stateName];
        var labelColor = sectorState.style.fill;
        sectorState.textConfig = {
          outsideFill: labelStateModel.get("color") === "inherit" ? labelColor : null,
          inside: labelPosition !== "outside"
        };
        var r;
        var labelPadding = getLabelAttr(labelStateModel, "distance") || 0;
        var textAlign = getLabelAttr(labelStateModel, "align");
        var rotateType = getLabelAttr(labelStateModel, "rotate");
        var flipStartAngle = Math.PI * 0.5;
        var flipEndAngle = Math.PI * 1.5;
        var midAngleNormal = normalizeRadian(rotateType === "tangential" ? Math.PI / 2 - midAngle : midAngle);
        var needsFlip = midAngleNormal > flipStartAngle && !isRadianAroundZero(midAngleNormal - flipStartAngle) && midAngleNormal < flipEndAngle;
        if (labelPosition === "outside") {
          r = layout3.r + labelPadding;
          textAlign = needsFlip ? "right" : "left";
        } else {
          if (!textAlign || textAlign === "center") {
            if (layout3.r0 === 0 && isRadianAroundZero(angle - 2 * Math.PI)) {
              r = 0;
            } else {
              r = (layout3.r + layout3.r0) / 2;
            }
            textAlign = "center";
          } else if (textAlign === "left") {
            r = layout3.r0 + labelPadding;
            textAlign = needsFlip ? "right" : "left";
          } else if (textAlign === "right") {
            r = layout3.r - labelPadding;
            textAlign = needsFlip ? "left" : "right";
          }
        }
        state2.style.align = textAlign;
        state2.style.verticalAlign = getLabelAttr(labelStateModel, "verticalAlign") || "middle";
        state2.x = r * dx + layout3.cx;
        state2.y = r * dy + layout3.cy;
        var rotate2 = 0;
        if (rotateType === "radial") {
          rotate2 = normalizeRadian(-midAngle) + (needsFlip ? Math.PI : 0);
        } else if (rotateType === "tangential") {
          rotate2 = normalizeRadian(Math.PI / 2 - midAngle) + (needsFlip ? Math.PI : 0);
        } else if (isNumber(rotateType)) {
          rotate2 = rotateType * Math.PI / 180;
        }
        state2.rotation = normalizeRadian(rotate2);
      });
      function getLabelAttr(model2, name) {
        var stateAttr = model2.get(name);
        if (stateAttr == null) {
          return normalLabelModel.get(name);
        }
        return stateAttr;
      }
      label.dirtyStyle();
    };
    return SunburstPiece2;
  })(Sector_default)
);
var SunburstPiece_default = SunburstPiece;

// node_modules/echarts/lib/chart/sunburst/SunburstSeries.js
var SERIES_TYPE_SUNBURST = "sunburst";
var SunburstSeriesModel = (
  /** @class */
  (function(_super) {
    __extends(SunburstSeriesModel2, _super);
    function SunburstSeriesModel2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = SunburstSeriesModel2.type;
      _this.ignoreStyleOnData = true;
      return _this;
    }
    SunburstSeriesModel2.prototype.getInitialData = function(option, ecModel) {
      var root = {
        name: option.name,
        children: option.data
      };
      completeTreeValue2(root);
      var levelModels = this._levelModels = map2(option.levels || [], function(levelDefine) {
        return new Model_default(levelDefine, this, ecModel);
      }, this);
      var tree = Tree_default.createTree(root, this, beforeLink);
      function beforeLink(nodeData) {
        nodeData.wrapMethod("getItemModel", function(model2, idx) {
          var node = tree.getNodeByDataIndex(idx);
          var levelModel = levelModels[node.depth];
          levelModel && (model2.parentModel = levelModel);
          return model2;
        });
      }
      return tree.data;
    };
    SunburstSeriesModel2.prototype.optionUpdated = function() {
      this.resetViewRoot();
    };
    SunburstSeriesModel2.prototype.getDataParams = function(dataIndex) {
      var params = _super.prototype.getDataParams.apply(this, arguments);
      var node = this.getData().tree.getNodeByDataIndex(dataIndex);
      params.treePathInfo = wrapTreePathInfo(node, this);
      return params;
    };
    SunburstSeriesModel2.prototype.getLevelModel = function(node) {
      return this._levelModels && this._levelModels[node.depth];
    };
    SunburstSeriesModel2.prototype.getViewRoot = function() {
      return this._viewRoot;
    };
    SunburstSeriesModel2.prototype.resetViewRoot = function(viewRoot) {
      viewRoot ? this._viewRoot = viewRoot : viewRoot = this._viewRoot;
      var root = this.getRawData().tree.root;
      if (!viewRoot || viewRoot !== root && !root.contains(viewRoot)) {
        this._viewRoot = root;
      }
    };
    SunburstSeriesModel2.prototype.enableAriaDecal = function() {
      enableAriaDecalForTree(this);
    };
    SunburstSeriesModel2.type = "series." + SERIES_TYPE_SUNBURST;
    SunburstSeriesModel2.defaultOption = {
      // zlevel: 0,
      z: 2,
      // 默认全局居中
      center: ["50%", "50%"],
      radius: [0, "75%"],
      // 默认顺时针
      clockwise: true,
      startAngle: 90,
      // 最小角度改为0
      minAngle: 0,
      // If still show when all data zero.
      stillShowZeroSum: true,
      // 'rootToNode', 'link', or false
      nodeClick: "rootToNode",
      renderLabelForZeroData: false,
      label: {
        // could be: 'radial', 'tangential', or 'none'
        rotate: "radial",
        show: true,
        opacity: 1,
        // 'left' is for inner side of inside, and 'right' is for outer
        // side for inside
        align: "center",
        position: "inside",
        distance: 5,
        silent: true
      },
      itemStyle: {
        borderWidth: 1,
        borderColor: "white",
        borderType: "solid",
        shadowBlur: 0,
        shadowColor: "rgba(0, 0, 0, 0.2)",
        shadowOffsetX: 0,
        shadowOffsetY: 0,
        opacity: 1
      },
      emphasis: {
        focus: "descendant"
      },
      blur: {
        itemStyle: {
          opacity: 0.2
        },
        label: {
          opacity: 0.1
        }
      },
      // Animation type can be expansion, scale.
      animationType: "expansion",
      animationDuration: 1e3,
      animationDurationUpdate: 500,
      data: [],
      /**
       * Sort order.
       *
       * Valid values: 'desc', 'asc', null, or callback function.
       * 'desc' and 'asc' for descend and ascendant order;
       * null for not sorting;
       * example of callback function:
       * function(nodeA, nodeB) {
       *     return nodeA.getValue() - nodeB.getValue();
       * }
       */
      sort: "desc"
    };
    return SunburstSeriesModel2;
  })(Series_default)
);
function completeTreeValue2(dataNode) {
  var sum2 = 0;
  each(dataNode.children, function(child) {
    completeTreeValue2(child);
    var childValue = child.value;
    isArray(childValue) && (childValue = childValue[0]);
    sum2 += childValue;
  });
  var thisValue = dataNode.value;
  if (isArray(thisValue)) {
    thisValue = thisValue[0];
  }
  if (thisValue == null || isNaN(thisValue)) {
    thisValue = sum2;
  }
  if (thisValue < 0) {
    thisValue = 0;
  }
  isArray(dataNode.value) ? dataNode.value[0] = thisValue : dataNode.value = thisValue;
}

// node_modules/echarts/lib/chart/sunburst/sunburstAction.js
var ROOT_TO_NODE_ACTION = "sunburstRootToNode";

// node_modules/echarts/lib/chart/sunburst/SunburstView.js
var SunburstView = (
  /** @class */
  (function(_super) {
    __extends(SunburstView2, _super);
    function SunburstView2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = SERIES_TYPE_SUNBURST;
      return _this;
    }
    SunburstView2.prototype.render = function(seriesModel, ecModel, api, payload) {
      var self2 = this;
      this.seriesModel = seriesModel;
      this.api = api;
      this.ecModel = ecModel;
      var data = seriesModel.getData();
      var virtualRoot = data.tree.root;
      var newRoot = seriesModel.getViewRoot();
      var group2 = this.group;
      var renderLabelForZeroData = seriesModel.get("renderLabelForZeroData");
      var newChildren = [];
      newRoot.eachNode(function(node) {
        newChildren.push(node);
      });
      var oldChildren = this._oldChildren || [];
      dualTravel(newChildren, oldChildren);
      renderRollUp(virtualRoot, newRoot);
      this._initEvents();
      this._oldChildren = newChildren;
      function dualTravel(newChildren2, oldChildren2) {
        if (newChildren2.length === 0 && oldChildren2.length === 0) {
          return;
        }
        new DataDiffer_default(oldChildren2, newChildren2, getKey2, getKey2).add(processNode).update(processNode).remove(curry(processNode, null)).execute();
        function getKey2(node) {
          return node.getId();
        }
        function processNode(newIdx, oldIdx) {
          var newNode = newIdx == null ? null : newChildren2[newIdx];
          var oldNode = oldIdx == null ? null : oldChildren2[oldIdx];
          doRenderNode(newNode, oldNode);
        }
      }
      function doRenderNode(newNode, oldNode) {
        if (!renderLabelForZeroData && newNode && !newNode.getValue()) {
          newNode = null;
        }
        if (newNode !== virtualRoot && oldNode !== virtualRoot) {
          if (oldNode && oldNode.piece) {
            if (newNode) {
              oldNode.piece.updateData(false, newNode, seriesModel, ecModel, api);
              data.setItemGraphicEl(newNode.dataIndex, oldNode.piece);
            } else {
              removeNode2(oldNode);
            }
          } else if (newNode) {
            var piece = new SunburstPiece_default(newNode, seriesModel, ecModel, api);
            group2.add(piece);
            data.setItemGraphicEl(newNode.dataIndex, piece);
          }
        }
      }
      function removeNode2(node) {
        if (!node) {
          return;
        }
        if (node.piece) {
          group2.remove(node.piece);
          node.piece = null;
        }
      }
      function renderRollUp(virtualRoot2, viewRoot) {
        if (viewRoot.depth > 0) {
          if (self2.virtualPiece) {
            self2.virtualPiece.updateData(false, virtualRoot2, seriesModel, ecModel, api);
          } else {
            self2.virtualPiece = new SunburstPiece_default(virtualRoot2, seriesModel, ecModel, api);
            group2.add(self2.virtualPiece);
          }
          viewRoot.piece.off("click");
          self2.virtualPiece.on("click", function(e2) {
            self2._rootToNode(viewRoot.parentNode);
          });
        } else if (self2.virtualPiece) {
          group2.remove(self2.virtualPiece);
          self2.virtualPiece = null;
        }
      }
    };
    SunburstView2.prototype._initEvents = function() {
      var _this = this;
      this.group.off("click");
      this.group.on("click", function(e2) {
        var targetFound = false;
        var viewRoot = _this.seriesModel.getViewRoot();
        viewRoot.eachNode(function(node) {
          if (!targetFound && node.piece && node.piece === e2.target) {
            var nodeClick = node.getModel().get("nodeClick");
            if (nodeClick === "rootToNode") {
              _this._rootToNode(node);
            } else if (nodeClick === "link") {
              var itemModel = node.getModel();
              var link = itemModel.get("link");
              if (link) {
                var linkTarget = itemModel.get("target", true) || "_blank";
                windowOpen(link, linkTarget);
              }
            }
            targetFound = true;
          }
        });
      });
    };
    SunburstView2.prototype._rootToNode = function(node) {
      if (node !== this.seriesModel.getViewRoot()) {
        this.api.dispatchAction({
          type: ROOT_TO_NODE_ACTION,
          from: this.uid,
          seriesId: this.seriesModel.id,
          targetNode: node
        });
      }
    };
    SunburstView2.prototype.containPoint = function(point, seriesModel) {
      var treeRoot = seriesModel.getData();
      var itemLayout = treeRoot.getItemLayout(0);
      if (itemLayout) {
        var dx = point[0] - itemLayout.cx;
        var dy = point[1] - itemLayout.cy;
        var radius = Math.sqrt(dx * dx + dy * dy);
        return radius <= itemLayout.r && radius >= itemLayout.r0;
      }
    };
    SunburstView2.type = SERIES_TYPE_SUNBURST;
    return SunburstView2;
  })(Chart_default)
);

// node_modules/echarts/lib/chart/sunburst/sunburstVisual.js
var sunburstVisualStageHandler = createSimpleOverallStageHandler(SERIES_TYPE_SUNBURST, sunburstVisual);
function sunburstVisual(ecModel) {
  var paletteScope = {};
  function pickColor(node, seriesModel, treeHeight) {
    if (node.depth === 0) {
      return tokens_default.color.neutral50;
    }
    var current = node;
    while (current && current.depth > 1) {
      current = current.parentNode;
    }
    var color3 = seriesModel.getColorFromPalette(current.name || current.dataIndex + "", paletteScope);
    if (node.depth > 1 && isString(color3)) {
      color3 = lift(color3, (node.depth - 1) / (treeHeight - 1) * 0.5);
    }
    return color3;
  }
  ecModel.eachSeriesByType(SERIES_TYPE_SUNBURST, function(seriesModel) {
    var data = seriesModel.getData();
    var tree = data.tree;
    tree.eachNode(function(node) {
      var model2 = node.getModel();
      var style2 = model2.getModel("itemStyle").getItemStyle();
      if (!style2.fill) {
        style2.fill = pickColor(node, seriesModel, tree.root.height);
      }
      var existsStyle = data.ensureUniqueItemVisual(node.dataIndex, "style");
      extend(existsStyle, style2);
    });
  });
}

// node_modules/echarts/lib/chart/sunburst/sunburstLayout.js
var RADIAN5 = Math.PI / 180;
var sunburstLayoutStageHandler = createSimpleOverallStageHandler(SERIES_TYPE_SUNBURST, sunburstLayout);
function sunburstLayout(ecModel, api) {
  ecModel.eachSeriesByType(SERIES_TYPE_SUNBURST, function(seriesModel) {
    var center3 = seriesModel.get("center");
    var radius = seriesModel.get("radius");
    if (!isArray(radius)) {
      radius = [0, radius];
    }
    if (!isArray(center3)) {
      center3 = [center3, center3];
    }
    var width = api.getWidth();
    var height = api.getHeight();
    var size = Math.min(width, height);
    var cx = parsePercent2(center3[0], width);
    var cy = parsePercent2(center3[1], height);
    var r0 = parsePercent2(radius[0], size / 2);
    var r = parsePercent2(radius[1], size / 2);
    var startAngle = -seriesModel.get("startAngle") * RADIAN5;
    var minAngle = seriesModel.get("minAngle") * RADIAN5;
    var virtualRoot = seriesModel.getData().tree.root;
    var treeRoot = seriesModel.getViewRoot();
    var rootDepth = treeRoot.depth;
    var sort3 = seriesModel.get("sort");
    if (sort3 != null) {
      initChildren(treeRoot, sort3);
    }
    var validDataCount = 0;
    each(treeRoot.children, function(child) {
      !isNaN(child.getValue()) && validDataCount++;
    });
    var sum2 = treeRoot.getValue();
    var unitRadian = Math.PI / (sum2 || validDataCount) * 2;
    var renderRollupNode = treeRoot.depth > 0;
    var levels = treeRoot.height - (renderRollupNode ? -1 : 1);
    var rPerLevel = (r - r0) / (levels || 1);
    var clockwise = seriesModel.get("clockwise");
    var stillShowZeroSum = seriesModel.get("stillShowZeroSum");
    var dir3 = clockwise ? 1 : -1;
    var renderNode2 = function(node, startAngle2) {
      if (!node) {
        return;
      }
      var endAngle = startAngle2;
      if (node !== virtualRoot) {
        var value = node.getValue();
        var angle2 = sum2 === 0 && stillShowZeroSum ? unitRadian : value * unitRadian;
        if (angle2 < minAngle) {
          angle2 = minAngle;
        }
        endAngle = startAngle2 + dir3 * angle2;
        var depth = node.depth - rootDepth - (renderRollupNode ? -1 : 1);
        var rStart2 = r0 + rPerLevel * depth;
        var rEnd2 = r0 + rPerLevel * (depth + 1);
        var levelModel = seriesModel.getLevelModel(node);
        if (levelModel) {
          var r0_1 = levelModel.get("r0", true);
          var r_1 = levelModel.get("r", true);
          var radius_1 = levelModel.get("radius", true);
          if (radius_1 != null) {
            r0_1 = radius_1[0];
            r_1 = radius_1[1];
          }
          r0_1 != null && (rStart2 = parsePercent2(r0_1, size / 2));
          r_1 != null && (rEnd2 = parsePercent2(r_1, size / 2));
        }
        node.setLayout({
          angle: angle2,
          startAngle: startAngle2,
          endAngle,
          clockwise,
          cx,
          cy,
          r0: rStart2,
          r: rEnd2
        });
      }
      if (node.children && node.children.length) {
        var siblingAngle_1 = 0;
        each(node.children, function(node2) {
          siblingAngle_1 += renderNode2(node2, startAngle2 + siblingAngle_1);
        });
      }
      return endAngle - startAngle2;
    };
    if (renderRollupNode) {
      var rStart = r0;
      var rEnd = r0 + rPerLevel;
      var angle = Math.PI * 2;
      virtualRoot.setLayout({
        angle,
        startAngle,
        endAngle: startAngle + angle,
        clockwise,
        cx,
        cy,
        r0: rStart,
        r: rEnd
      });
    }
    renderNode2(treeRoot, startAngle);
  });
}
function initChildren(node, sortOrder) {
  var children = node.children || [];
  node.children = sort2(children, sortOrder);
  if (children.length) {
    each(node.children, function(child) {
      initChildren(child, sortOrder);
    });
  }
}
function sort2(children, sortOrder) {
  if (isFunction(sortOrder)) {
    var sortTargets = map2(children, function(child, idx) {
      var value = child.getValue();
      return {
        params: {
          depth: child.depth,
          height: child.height,
          dataIndex: child.dataIndex,
          getValue: function() {
            return value;
          }
        },
        index: idx
      };
    });
    sortTargets.sort(function(a, b) {
      return sortOrder(a.params, b.params);
    });
    return map2(sortTargets, function(target) {
      return children[target.index];
    });
  } else {
    var isAsc_1 = sortOrder === "asc";
    return children.sort(function(a, b) {
      var diff = (a.getValue() - b.getValue()) * (isAsc_1 ? 1 : -1);
      return diff === 0 ? (a.dataIndex - b.dataIndex) * (isAsc_1 ? -1 : 1) : diff;
    });
  }
}

// node_modules/echarts/lib/chart/custom/CustomSeries.js
var STYLE_VISUAL_TYPE = {
  color: "fill",
  borderColor: "stroke"
};
var NON_STYLE_VISUAL_PROPS = {
  symbol: 1,
  symbolSize: 1,
  symbolKeepAspect: 1,
  legendIcon: 1,
  visualMeta: 1,
  liftZ: 1,
  decal: 1
};
var customInnerStore = makeInner();
var CustomSeriesModel = (
  /** @class */
  (function(_super) {
    __extends(CustomSeriesModel2, _super);
    function CustomSeriesModel2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = CustomSeriesModel2.type;
      return _this;
    }
    CustomSeriesModel2.prototype.optionUpdated = function() {
      this.currentZLevel = this.get("zlevel", true);
      this.currentZ = this.get("z", true);
    };
    CustomSeriesModel2.prototype.getInitialData = function(option, ecModel) {
      return createSeriesData_default(null, this);
    };
    CustomSeriesModel2.prototype.getDataParams = function(dataIndex, dataType, el) {
      var params = _super.prototype.getDataParams.call(this, dataIndex, dataType);
      el && (params.info = customInnerStore(el).info);
      return params;
    };
    CustomSeriesModel2.type = "series.custom";
    CustomSeriesModel2.dependencies = ["grid", "polar", "geo", "singleAxis", "calendar", "matrix"];
    CustomSeriesModel2.defaultOption = {
      coordinateSystem: "cartesian2d",
      // zlevel: 0,
      z: 2,
      legendHoverLink: true,
      // Custom series will not clip by default.
      // Some case will use custom series to draw label
      // For example https://echarts.apache.org/examples/en/editor.html?c=custom-gantt-flight
      clip: false
      // Cartesian coordinate system
      // xAxisIndex: 0,
      // yAxisIndex: 0,
      // Polar coordinate system
      // polarIndex: 0,
      // Geo coordinate system
      // geoIndex: 0,
    };
    return CustomSeriesModel2;
  })(Series_default)
);
var CustomSeries_default = CustomSeriesModel;

// node_modules/echarts/lib/coord/cartesian/prepareCustom.js
function dataToCoordSize(dataSize, dataItem) {
  dataItem = dataItem || [0, 0];
  return map2(["x", "y"], function(dim, dimIdx) {
    var axis = this.getAxis(dim);
    var val = dataItem[dimIdx];
    var halfSize = dataSize[dimIdx] / 2;
    return axis.type === "category" ? calcBandWidth(axis).w : Math.abs(axis.dataToCoord(val - halfSize) - axis.dataToCoord(val + halfSize));
  }, this);
}
function cartesianPrepareCustom(coordSys) {
  var rect = coordSys.master.getRect();
  return {
    coordSys: {
      // The name exposed to user is always 'cartesian2d' but not 'grid'.
      type: "cartesian2d",
      x: rect.x,
      y: rect.y,
      width: rect.width,
      height: rect.height
    },
    api: {
      coord: function(data) {
        return coordSys.dataToPoint(data);
      },
      size: bind(dataToCoordSize, coordSys)
    }
  };
}

// node_modules/echarts/lib/coord/geo/prepareCustom.js
function dataToCoordSize2(dataSize, dataItem) {
  dataItem = dataItem || [0, 0];
  return map2([0, 1], function(dimIdx) {
    var val = dataItem[dimIdx];
    var halfSize = dataSize[dimIdx] / 2;
    var p1 = [];
    var p2 = [];
    p1[dimIdx] = val - halfSize;
    p2[dimIdx] = val + halfSize;
    p1[1 - dimIdx] = p2[1 - dimIdx] = dataItem[1 - dimIdx];
    return Math.abs(this.dataToPoint(p1)[dimIdx] - this.dataToPoint(p2)[dimIdx]);
  }, this);
}
function geoPrepareCustom(coordSys) {
  var viewCoordSys = coordSys.view;
  var rect = viewCoordSys.getBoundingRect();
  return {
    coordSys: {
      type: "geo",
      x: rect.x,
      y: rect.y,
      width: rect.width,
      height: rect.height,
      zoom: viewCoordSysGetZoomOption(viewCoordSys)
    },
    api: {
      coord: function(data) {
        return coordSys.dataToPoint(data);
      },
      size: bind(dataToCoordSize2, coordSys)
    }
  };
}

// node_modules/echarts/lib/coord/single/prepareCustom.js
function dataToCoordSize3(dataSize, dataItem) {
  var axis = this.getAxis();
  var val = dataItem instanceof Array ? dataItem[0] : dataItem;
  var halfSize = (dataSize instanceof Array ? dataSize[0] : dataSize) / 2;
  return axis.type === "category" ? calcBandWidth(axis).w : Math.abs(axis.dataToCoord(val - halfSize) - axis.dataToCoord(val + halfSize));
}
function singlePrepareCustom(coordSys) {
  var rect = coordSys.getRect();
  return {
    coordSys: {
      type: "singleAxis",
      x: rect.x,
      y: rect.y,
      width: rect.width,
      height: rect.height
    },
    api: {
      coord: function(val) {
        return coordSys.dataToPoint(val);
      },
      size: bind(dataToCoordSize3, coordSys)
    }
  };
}

// node_modules/echarts/lib/coord/polar/prepareCustom.js
function dataToCoordSize4(dataSize, dataItem) {
  dataItem = dataItem || [0, 0];
  return map2(["Radius", "Angle"], function(dim, dimIdx) {
    var getterName = "get" + dim + "Axis";
    var axis = this[getterName]();
    var val = dataItem[dimIdx];
    var halfSize = dataSize[dimIdx] / 2;
    var result = axis.type === "category" ? calcBandWidth(axis).w : Math.abs(axis.dataToCoord(val - halfSize) - axis.dataToCoord(val + halfSize));
    if (dim === "Angle") {
      result = result * Math.PI / 180;
    }
    return result;
  }, this);
}
function polarPrepareCustom(coordSys) {
  var radiusAxis = coordSys.getRadiusAxis();
  var angleAxis = coordSys.getAngleAxis();
  var radius = radiusAxis.getExtent();
  radius[0] > radius[1] && radius.reverse();
  return {
    coordSys: {
      type: "polar",
      cx: coordSys.cx,
      cy: coordSys.cy,
      r: radius[1],
      r0: radius[0]
    },
    api: {
      coord: function(data) {
        var radius2 = radiusAxis.dataToRadius(data[0]);
        var angle = angleAxis.dataToAngle(data[1]);
        var coord = coordSys.coordToPoint([radius2, angle]);
        coord.push(radius2, angle * Math.PI / 180);
        return coord;
      },
      size: bind(dataToCoordSize4, coordSys)
    }
  };
}

// node_modules/echarts/lib/coord/calendar/prepareCustom.js
function calendarPrepareCustom(coordSys) {
  var rect = coordSys.getRect();
  var rangeInfo = coordSys.getRangeInfo();
  return {
    coordSys: {
      type: "calendar",
      x: rect.x,
      y: rect.y,
      width: rect.width,
      height: rect.height,
      cellWidth: coordSys.getCellWidth(),
      cellHeight: coordSys.getCellHeight(),
      rangeInfo: {
        start: rangeInfo.start,
        end: rangeInfo.end,
        weeks: rangeInfo.weeks,
        dayCount: rangeInfo.allDay
      }
    },
    api: {
      coord: function(data, clamp2) {
        return coordSys.dataToPoint(data, clamp2);
      },
      layout: function(data, clamp2) {
        return coordSys.dataToLayout(data, clamp2);
      }
    }
  };
}

// node_modules/echarts/lib/coord/matrix/prepareCustom.js
function matrixPrepareCustom(coordSys) {
  var rect = coordSys.getRect();
  return {
    coordSys: {
      type: "matrix",
      x: rect.x,
      y: rect.y,
      width: rect.width,
      height: rect.height
    },
    api: {
      coord: function(data, opt) {
        return coordSys.dataToPoint(data, opt);
      },
      layout: function(data, opt) {
        return coordSys.dataToLayout(data, opt);
      }
    }
  };
}

// node_modules/echarts/lib/animation/customGraphicTransition.js
var LEGACY_TRANSFORM_PROPS_MAP = {
  position: ["x", "y"],
  scale: ["scaleX", "scaleY"],
  origin: ["originX", "originY"]
};
var LEGACY_TRANSFORM_PROPS = keys(LEGACY_TRANSFORM_PROPS_MAP);
var TRANSFORM_PROPS_MAP = reduce(TRANSFORMABLE_PROPS, function(obj, key2) {
  obj[key2] = 1;
  return obj;
}, {});
var transformPropNamesStr = TRANSFORMABLE_PROPS.join(", ");
var ELEMENT_ANIMATABLE_PROPS = ["", "style", "shape", "extra"];
var transitionInnerStore = makeInner();
function getElementAnimationConfig(animationType, el, elOption, parentModel, dataIndex) {
  var animationProp = animationType + "Animation";
  var config = getAnimationConfig(animationType, parentModel, dataIndex) || {};
  var userDuring = transitionInnerStore(el).userDuring;
  if (config.duration > 0) {
    config.during = userDuring ? bind(duringCall, {
      el,
      userDuring
    }) : null;
    config.setToFinal = true;
    config.scope = animationType;
  }
  extend(config, elOption[animationProp]);
  return config;
}
function applyUpdateTransition(el, elOption, animatableModel, opts) {
  opts = opts || {};
  var dataIndex = opts.dataIndex, isInit = opts.isInit, clearStyle = opts.clearStyle;
  var hasAnimation = animatableModel.isAnimationEnabled();
  var store = transitionInnerStore(el);
  var styleOpt = elOption.style;
  store.userDuring = elOption.during;
  var transFromProps = {};
  var propsToSet = {};
  prepareTransformAllPropsFinal(el, elOption, propsToSet);
  if (el.type === "compound") {
    var paths = el.shape.paths;
    var optionPaths = elOption.shape.paths;
    for (var i2 = 0; i2 < optionPaths.length; i2++) {
      var path = optionPaths[i2];
      prepareShapeOrExtraAllPropsFinal("shape", path, paths[i2]);
    }
  } else {
    prepareShapeOrExtraAllPropsFinal("shape", elOption, propsToSet);
    prepareShapeOrExtraAllPropsFinal("extra", elOption, propsToSet);
  }
  if (!isInit && hasAnimation) {
    prepareTransformTransitionFrom(el, elOption, transFromProps);
    prepareShapeOrExtraTransitionFrom("shape", el, elOption, transFromProps);
    prepareShapeOrExtraTransitionFrom("extra", el, elOption, transFromProps);
    prepareStyleTransitionFrom(el, elOption, styleOpt, transFromProps);
  }
  propsToSet.style = styleOpt;
  applyPropsDirectly(el, propsToSet, clearStyle);
  applyMiscProps(el, elOption);
  if (hasAnimation) {
    if (isInit) {
      var enterFromProps_1 = {};
      each(ELEMENT_ANIMATABLE_PROPS, function(propName) {
        var prop = propName ? elOption[propName] : elOption;
        if (prop && prop.enterFrom) {
          if (propName) {
            enterFromProps_1[propName] = enterFromProps_1[propName] || {};
          }
          extend(propName ? enterFromProps_1[propName] : enterFromProps_1, prop.enterFrom);
        }
      });
      var config = getElementAnimationConfig("enter", el, elOption, animatableModel, dataIndex);
      if (config.duration > 0) {
        el.animateFrom(enterFromProps_1, config);
      }
    } else {
      applyPropsTransition(el, elOption, dataIndex || 0, animatableModel, transFromProps);
    }
  }
  updateLeaveTo(el, elOption);
  styleOpt ? el.dirty() : el.markRedraw();
}
function updateLeaveTo(el, elOption) {
  var leaveToProps = transitionInnerStore(el).leaveToProps;
  for (var i2 = 0; i2 < ELEMENT_ANIMATABLE_PROPS.length; i2++) {
    var propName = ELEMENT_ANIMATABLE_PROPS[i2];
    var prop = propName ? elOption[propName] : elOption;
    if (prop && prop.leaveTo) {
      if (!leaveToProps) {
        leaveToProps = transitionInnerStore(el).leaveToProps = {};
      }
      if (propName) {
        leaveToProps[propName] = leaveToProps[propName] || {};
      }
      extend(propName ? leaveToProps[propName] : leaveToProps, prop.leaveTo);
    }
  }
}
function applyLeaveTransition(el, elOption, animatableModel, onRemove) {
  if (el) {
    var parent_1 = el.parent;
    var leaveToProps = transitionInnerStore(el).leaveToProps;
    if (leaveToProps) {
      var config = getElementAnimationConfig("update", el, elOption, animatableModel, 0);
      config.done = function() {
        parent_1 && parent_1.remove(el);
        onRemove && onRemove();
      };
      el.animateTo(leaveToProps, config);
    } else {
      parent_1 && parent_1.remove(el);
      onRemove && onRemove();
    }
  }
}
function isTransitionAll(transition2) {
  return transition2 === "all";
}
function applyPropsDirectly(el, allPropsFinal, clearStyle) {
  var styleOpt = allPropsFinal.style;
  if (!el.isGroup && styleOpt) {
    if (clearStyle) {
      el.useStyle({});
      var animators = el.animators;
      for (var i2 = 0; i2 < animators.length; i2++) {
        var animator = animators[i2];
        if (animator.targetName === "style") {
          animator.changeTarget(el.style);
        }
      }
    }
    el.setStyle(styleOpt);
  }
  if (allPropsFinal) {
    allPropsFinal.style = null;
    allPropsFinal && el.attr(allPropsFinal);
    allPropsFinal.style = styleOpt;
  }
}
function applyPropsTransition(el, elOption, dataIndex, model2, transFromProps) {
  if (transFromProps) {
    var config = getElementAnimationConfig("update", el, elOption, model2, dataIndex);
    if (config.duration > 0) {
      el.animateFrom(transFromProps, config);
    }
  }
}
function applyMiscProps(el, elOption) {
  hasOwn(elOption, "silent") && (el.silent = elOption.silent);
  hasOwn(elOption, "ignore") && (el.ignore = elOption.ignore);
  if (el instanceof Displayable_default) {
    hasOwn(elOption, "invisible") && (el.invisible = elOption.invisible);
  }
  if (el instanceof Path_default) {
    hasOwn(elOption, "autoBatch") && (el.autoBatch = elOption.autoBatch);
  }
}
var tmpDuringScope = {};
var transitionDuringAPI = {
  // Usually other props do not need to be changed in animation during.
  setTransform: function(key2, val) {
    if (true) {
      assert(hasOwn(TRANSFORM_PROPS_MAP, key2), "Only " + transformPropNamesStr + " available in `setTransform`.");
    }
    tmpDuringScope.el[key2] = val;
    return this;
  },
  getTransform: function(key2) {
    if (true) {
      assert(hasOwn(TRANSFORM_PROPS_MAP, key2), "Only " + transformPropNamesStr + " available in `getTransform`.");
    }
    return tmpDuringScope.el[key2];
  },
  setShape: function(key2, val) {
    if (true) {
      assertNotReserved(key2);
    }
    var el = tmpDuringScope.el;
    var shape = el.shape || (el.shape = {});
    shape[key2] = val;
    el.dirtyShape && el.dirtyShape();
    return this;
  },
  getShape: function(key2) {
    if (true) {
      assertNotReserved(key2);
    }
    var shape = tmpDuringScope.el.shape;
    if (shape) {
      return shape[key2];
    }
  },
  setStyle: function(key2, val) {
    if (true) {
      assertNotReserved(key2);
    }
    var el = tmpDuringScope.el;
    var style2 = el.style;
    if (style2) {
      if (true) {
        if (eqNaN(val)) {
          warn("style." + key2 + " must not be assigned with NaN.");
        }
      }
      style2[key2] = val;
      el.dirtyStyle && el.dirtyStyle();
    }
    return this;
  },
  getStyle: function(key2) {
    if (true) {
      assertNotReserved(key2);
    }
    var style2 = tmpDuringScope.el.style;
    if (style2) {
      return style2[key2];
    }
  },
  setExtra: function(key2, val) {
    if (true) {
      assertNotReserved(key2);
    }
    var extra = tmpDuringScope.el.extra || (tmpDuringScope.el.extra = {});
    extra[key2] = val;
    return this;
  },
  getExtra: function(key2) {
    if (true) {
      assertNotReserved(key2);
    }
    var extra = tmpDuringScope.el.extra;
    if (extra) {
      return extra[key2];
    }
  }
};
function assertNotReserved(key2) {
  if (true) {
    if (key2 === "transition" || key2 === "enterFrom" || key2 === "leaveTo") {
      throw new Error('key must not be "' + key2 + '"');
    }
  }
}
function duringCall() {
  var scope = this;
  var el = scope.el;
  if (!el) {
    return;
  }
  var latestUserDuring = transitionInnerStore(el).userDuring;
  var scopeUserDuring = scope.userDuring;
  if (latestUserDuring !== scopeUserDuring) {
    scope.el = scope.userDuring = null;
    return;
  }
  tmpDuringScope.el = el;
  scopeUserDuring(transitionDuringAPI);
}
function prepareShapeOrExtraTransitionFrom(mainAttr, fromEl, elOption, transFromProps) {
  var attrOpt = elOption[mainAttr];
  if (!attrOpt) {
    return;
  }
  var elPropsInAttr = fromEl[mainAttr];
  var transFromPropsInAttr;
  if (elPropsInAttr) {
    var transition2 = elOption.transition;
    var attrTransition = attrOpt.transition;
    if (attrTransition) {
      !transFromPropsInAttr && (transFromPropsInAttr = transFromProps[mainAttr] = {});
      if (isTransitionAll(attrTransition)) {
        extend(transFromPropsInAttr, elPropsInAttr);
      } else {
        var transitionKeys = normalizeToArray(attrTransition);
        for (var i2 = 0; i2 < transitionKeys.length; i2++) {
          var key2 = transitionKeys[i2];
          var elVal = elPropsInAttr[key2];
          transFromPropsInAttr[key2] = elVal;
        }
      }
    } else if (isTransitionAll(transition2) || indexOf(transition2, mainAttr) >= 0) {
      !transFromPropsInAttr && (transFromPropsInAttr = transFromProps[mainAttr] = {});
      var elPropsInAttrKeys = keys(elPropsInAttr);
      for (var i2 = 0; i2 < elPropsInAttrKeys.length; i2++) {
        var key2 = elPropsInAttrKeys[i2];
        var elVal = elPropsInAttr[key2];
        if (isNonStyleTransitionEnabled(attrOpt[key2], elVal)) {
          transFromPropsInAttr[key2] = elVal;
        }
      }
    }
  }
}
function prepareShapeOrExtraAllPropsFinal(mainAttr, elOption, allProps) {
  var attrOpt = elOption[mainAttr];
  if (!attrOpt) {
    return;
  }
  var allPropsInAttr = allProps[mainAttr] = {};
  var keysInAttr = keys(attrOpt);
  for (var i2 = 0; i2 < keysInAttr.length; i2++) {
    var key2 = keysInAttr[i2];
    allPropsInAttr[key2] = cloneValue(attrOpt[key2]);
  }
}
function prepareTransformTransitionFrom(el, elOption, transFromProps) {
  var transition2 = elOption.transition;
  var transitionKeys = isTransitionAll(transition2) ? TRANSFORMABLE_PROPS : normalizeToArray(transition2 || []);
  for (var i2 = 0; i2 < transitionKeys.length; i2++) {
    var key2 = transitionKeys[i2];
    if (key2 === "style" || key2 === "shape" || key2 === "extra") {
      continue;
    }
    var elVal = el[key2];
    if (true) {
      checkTransformPropRefer(key2, "el.transition");
    }
    transFromProps[key2] = elVal;
  }
}
function prepareTransformAllPropsFinal(el, elOption, allProps) {
  for (var i2 = 0; i2 < LEGACY_TRANSFORM_PROPS.length; i2++) {
    var legacyName = LEGACY_TRANSFORM_PROPS[i2];
    var xyName = LEGACY_TRANSFORM_PROPS_MAP[legacyName];
    var legacyArr = elOption[legacyName];
    if (legacyArr) {
      allProps[xyName[0]] = legacyArr[0];
      allProps[xyName[1]] = legacyArr[1];
    }
  }
  for (var i2 = 0; i2 < TRANSFORMABLE_PROPS.length; i2++) {
    var key2 = TRANSFORMABLE_PROPS[i2];
    if (elOption[key2] != null) {
      allProps[key2] = elOption[key2];
    }
  }
}
function prepareStyleTransitionFrom(fromEl, elOption, styleOpt, transFromProps) {
  if (!styleOpt) {
    return;
  }
  var fromElStyle = fromEl.style;
  var transFromStyleProps;
  if (fromElStyle) {
    var styleTransition = styleOpt.transition;
    var elTransition = elOption.transition;
    if (styleTransition && !isTransitionAll(styleTransition)) {
      var transitionKeys = normalizeToArray(styleTransition);
      !transFromStyleProps && (transFromStyleProps = transFromProps.style = {});
      for (var i2 = 0; i2 < transitionKeys.length; i2++) {
        var key2 = transitionKeys[i2];
        var elVal = fromElStyle[key2];
        transFromStyleProps[key2] = elVal;
      }
    } else if (fromEl.getAnimationStyleProps && (isTransitionAll(elTransition) || isTransitionAll(styleTransition) || indexOf(elTransition, "style") >= 0)) {
      var animationProps = fromEl.getAnimationStyleProps();
      var animationStyleProps = animationProps ? animationProps.style : null;
      if (animationStyleProps) {
        !transFromStyleProps && (transFromStyleProps = transFromProps.style = {});
        var styleKeys = keys(styleOpt);
        for (var i2 = 0; i2 < styleKeys.length; i2++) {
          var key2 = styleKeys[i2];
          if (animationStyleProps[key2]) {
            var elVal = fromElStyle[key2];
            transFromStyleProps[key2] = elVal;
          }
        }
      }
    }
  }
}
function isNonStyleTransitionEnabled(optVal, elVal) {
  return !isArrayLike(optVal) ? optVal != null && isFinite(optVal) : optVal !== elVal;
}
var checkTransformPropRefer;
if (true) {
  checkTransformPropRefer = function(key2, usedIn) {
    if (!hasOwn(TRANSFORM_PROPS_MAP, key2)) {
      warn("Prop `" + key2 + "` is not a permitted in `" + usedIn + "`. Only `" + keys(TRANSFORM_PROPS_MAP).join("`, `") + "` are permitted.");
    }
  };
}

// node_modules/echarts/lib/animation/customGraphicKeyframeAnimation.js
var getStateToRestore = makeInner();
var KEYFRAME_EXCLUDE_KEYS = ["percent", "easing", "shape", "style", "extra"];
function stopPreviousKeyframeAnimationAndRestore(el) {
  el.stopAnimation("keyframe");
  el.attr(getStateToRestore(el));
}
function applyKeyframeAnimation(el, animationOpts, animatableModel) {
  if (!animatableModel.isAnimationEnabled() || !animationOpts) {
    return;
  }
  if (isArray(animationOpts)) {
    each(animationOpts, function(singleAnimationOpts) {
      applyKeyframeAnimation(el, singleAnimationOpts, animatableModel);
    });
    return;
  }
  var keyframes2 = animationOpts.keyframes;
  var duration = animationOpts.duration;
  if (animatableModel && duration == null) {
    var config = getAnimationConfig("enter", animatableModel, 0);
    duration = config && config.duration;
  }
  if (!keyframes2 || !duration) {
    return;
  }
  var stateToRestore = getStateToRestore(el);
  each(ELEMENT_ANIMATABLE_PROPS, function(targetPropName) {
    if (targetPropName && !el[targetPropName]) {
      return;
    }
    var animator;
    var endFrameIsSet = false;
    keyframes2.sort(function(a, b) {
      return a.percent - b.percent;
    });
    each(keyframes2, function(kf) {
      var animators = el.animators;
      var kfValues = targetPropName ? kf[targetPropName] : kf;
      if (true) {
        if (kf.percent >= 1) {
          endFrameIsSet = true;
        }
      }
      if (!kfValues) {
        return;
      }
      var propKeys = keys(kfValues);
      if (!targetPropName) {
        propKeys = filter2(propKeys, function(key2) {
          return indexOf(KEYFRAME_EXCLUDE_KEYS, key2) < 0;
        });
      }
      if (!propKeys.length) {
        return;
      }
      if (!animator) {
        animator = el.animate(targetPropName, animationOpts.loop, true);
        animator.scope = "keyframe";
      }
      for (var i2 = 0; i2 < animators.length; i2++) {
        if (animators[i2] !== animator && animators[i2].targetName === animator.targetName) {
          animators[i2].stopTracks(propKeys);
        }
      }
      targetPropName && (stateToRestore[targetPropName] = stateToRestore[targetPropName] || {});
      var savedTarget = targetPropName ? stateToRestore[targetPropName] : stateToRestore;
      each(propKeys, function(key2) {
        savedTarget[key2] = ((targetPropName ? el[targetPropName] : el) || {})[key2];
      });
      animator.whenWithKeys(duration * kf.percent, kfValues, propKeys, kf.easing);
    });
    if (!animator) {
      return;
    }
    if (true) {
      if (!endFrameIsSet) {
        warn("End frame with percent: 1 is missing in the keyframeAnimation.", true);
      }
    }
    animator.delay(animationOpts.delay || 0).duration(duration).start(animationOpts.easing);
  });
}

// node_modules/echarts/lib/chart/custom/CustomView.js
var EMPHASIS = "emphasis";
var NORMAL = "normal";
var BLUR = "blur";
var SELECT = "select";
var STATES = [NORMAL, EMPHASIS, BLUR, SELECT];
var PATH_ITEM_STYLE = {
  normal: ["itemStyle"],
  emphasis: [EMPHASIS, "itemStyle"],
  blur: [BLUR, "itemStyle"],
  select: [SELECT, "itemStyle"]
};
var PATH_LABEL = {
  normal: ["label"],
  emphasis: [EMPHASIS, "label"],
  blur: [BLUR, "label"],
  select: [SELECT, "label"]
};
var DEFAULT_TRANSITION = ["x", "y"];
var GROUP_DIFF_PREFIX = "e\0\0";
var attachedTxInfoTmp = {
  normal: {},
  emphasis: {},
  blur: {},
  select: {}
};
var prepareCustoms = {
  cartesian2d: cartesianPrepareCustom,
  geo: geoPrepareCustom,
  single: singlePrepareCustom,
  polar: polarPrepareCustom,
  calendar: calendarPrepareCustom,
  matrix: matrixPrepareCustom
};
function isPath2(el) {
  return el instanceof Path_default;
}
function isDisplayable(el) {
  return el instanceof Displayable_default;
}
function copyElement(sourceEl, targetEl) {
  targetEl.copyTransform(sourceEl);
  if (isDisplayable(targetEl) && isDisplayable(sourceEl)) {
    targetEl.setStyle(sourceEl.style);
    targetEl.z = sourceEl.z;
    targetEl.z2 = sourceEl.z2;
    targetEl.zlevel = sourceEl.zlevel;
    targetEl.invisible = sourceEl.invisible;
    targetEl.ignore = sourceEl.ignore;
    if (isPath2(targetEl) && isPath2(sourceEl)) {
      targetEl.setShape(sourceEl.shape);
    }
  }
}
var CustomChartView = (
  /** @class */
  (function(_super) {
    __extends(CustomChartView2, _super);
    function CustomChartView2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = CustomChartView2.type;
      return _this;
    }
    CustomChartView2.prototype.render = function(customSeries, ecModel, api, payload) {
      this._progressiveEls = null;
      var oldData = this._data;
      var data = customSeries.getData();
      var group2 = this.group;
      var renderItem = makeRenderItem(customSeries, data, ecModel, api);
      if (!oldData) {
        group2.removeAll();
      }
      data.diff(oldData).add(function(newIdx) {
        createOrUpdateItem(api, null, newIdx, renderItem(newIdx, payload), customSeries, group2, data);
      }).remove(function(oldIdx) {
        var el = oldData.getItemGraphicEl(oldIdx);
        el && applyLeaveTransition(el, customInnerStore(el).option, customSeries);
      }).update(function(newIdx, oldIdx) {
        var oldEl = oldData.getItemGraphicEl(oldIdx);
        createOrUpdateItem(api, oldEl, newIdx, renderItem(newIdx, payload), customSeries, group2, data);
      }).execute();
      var clipPath = customSeries.get("clip", true) ? createClipPath(customSeries.coordinateSystem, false, customSeries) : null;
      if (clipPath) {
        group2.setClipPath(clipPath);
      } else {
        group2.removeClipPath();
      }
      this._data = data;
    };
    CustomChartView2.prototype.incrementalPrepareRender = function(customSeries, ecModel, api) {
      this.group.removeAll();
      this._data = null;
    };
    CustomChartView2.prototype.incrementalRender = function(params, customSeries, ecModel, api, payload) {
      var data = customSeries.getData();
      var renderItem = makeRenderItem(customSeries, data, ecModel, api);
      var progressiveEls = this._progressiveEls = [];
      function setIncrementalAndHoverLayer(el2) {
        if (!el2.isGroup) {
          el2.incremental = getIncrementalId(customSeries);
          el2.ensureState("emphasis").hoverLayer = HOVER_LAYER_FOR_INCREMENTAL;
        }
      }
      for (var idx = params.start; idx < params.end; idx++) {
        var el = createOrUpdateItem(null, null, idx, renderItem(idx, payload), customSeries, this.group, data);
        if (el) {
          el.traverse(setIncrementalAndHoverLayer);
          progressiveEls.push(el);
        }
      }
    };
    CustomChartView2.prototype.eachRendered = function(cb) {
      traverseElements(this._progressiveEls || this.group, cb);
    };
    CustomChartView2.prototype.filterForExposedEvent = function(eventType, query2, targetEl, packedEvent) {
      var elementName = query2.element;
      if (elementName == null || targetEl.name === elementName) {
        return true;
      }
      while ((targetEl = targetEl.__hostTarget || targetEl.parent) && targetEl !== this.group) {
        if (targetEl.name === elementName) {
          return true;
        }
      }
      return false;
    };
    CustomChartView2.type = "custom";
    return CustomChartView2;
  })(Chart_default)
);
var CustomView_default = CustomChartView;
function createEl2(elOption) {
  var graphicType = elOption.type;
  var el;
  if (graphicType === "path") {
    var shape = elOption.shape;
    var pathRect = shape.width != null && shape.height != null ? {
      x: shape.x || 0,
      y: shape.y || 0,
      width: shape.width,
      height: shape.height
    } : null;
    var pathData = getPathData(shape);
    el = makePath(pathData, null, pathRect, shape.layout || "center");
    customInnerStore(el).customPathData = pathData;
  } else if (graphicType === "image") {
    el = new Image_default({});
    customInnerStore(el).customImagePath = elOption.style.image;
  } else if (graphicType === "text") {
    el = new Text_default({});
  } else if (graphicType === "group") {
    el = new Group_default();
  } else if (graphicType === "compoundPath") {
    var shape = elOption.shape;
    if (!shape || !shape.paths) {
      var errMsg = "";
      if (true) {
        errMsg = "shape.paths must be specified in compoundPath";
      }
      throwError2(errMsg);
    }
    var paths = map2(shape.paths, function(path) {
      if (path.type === "path") {
        return makePath(path.shape.pathData, path, null);
      }
      var Clz2 = getShapeClass(path.type);
      if (!Clz2) {
        var errMsg2 = "";
        if (true) {
          errMsg2 = 'graphic type "' + graphicType + '" can not be found.';
        }
        throwError2(errMsg2);
      }
      return new Clz2();
    });
    el = new CompoundPath_default({
      shape: {
        paths
      }
    });
  } else {
    var Clz = getShapeClass(graphicType);
    if (!Clz) {
      var errMsg = "";
      if (true) {
        errMsg = 'graphic type "' + graphicType + '" can not be found.';
      }
      throwError2(errMsg);
    }
    el = new Clz();
  }
  customInnerStore(el).customGraphicType = graphicType;
  el.name = elOption.name;
  el.z2EmphasisLift = 1;
  el.z2SelectLift = 1;
  return el;
}
function updateElNormal(api, el, dataIndex, elOption, attachedTxInfo, seriesModel, isInit) {
  stopPreviousKeyframeAnimationAndRestore(el);
  var txCfgOpt = attachedTxInfo && attachedTxInfo.normal.cfg;
  if (txCfgOpt) {
    el.setTextConfig(txCfgOpt);
  }
  if (elOption && elOption.transition == null) {
    elOption.transition = DEFAULT_TRANSITION;
  }
  var styleOpt = elOption && elOption.style;
  if (styleOpt) {
    if (el.type === "text") {
      var textOptionStyle = styleOpt;
      hasOwn(textOptionStyle, "textFill") && (textOptionStyle.fill = textOptionStyle.textFill);
      hasOwn(textOptionStyle, "textStroke") && (textOptionStyle.stroke = textOptionStyle.textStroke);
    }
    var decalPattern = void 0;
    var decalObj = isPath2(el) ? styleOpt.decal : null;
    if (api && decalObj) {
      decalObj.dirty = true;
      decalPattern = createOrUpdatePatternFromDecal(decalObj, api);
    }
    styleOpt.__decalPattern = decalPattern;
  }
  if (isDisplayable(el)) {
    if (styleOpt) {
      var decalPattern = styleOpt.__decalPattern;
      if (decalPattern) {
        styleOpt.decal = decalPattern;
      }
    }
  }
  applyUpdateTransition(el, elOption, seriesModel, {
    dataIndex,
    isInit,
    clearStyle: true
  });
  applyKeyframeAnimation(el, elOption.keyframeAnimation, seriesModel);
}
function updateElOnState(state2, el, elStateOpt, styleOpt, attachedTxInfo) {
  var elDisplayable = el.isGroup ? null : el;
  var txCfgOpt = attachedTxInfo && attachedTxInfo[state2].cfg;
  if (elDisplayable) {
    var stateObj = elDisplayable.ensureState(state2);
    if (styleOpt === false) {
      var existingEmphasisState = elDisplayable.getState(state2);
      if (existingEmphasisState) {
        existingEmphasisState.style = null;
      }
    } else {
      stateObj.style = styleOpt || null;
    }
    if (txCfgOpt) {
      stateObj.textConfig = txCfgOpt;
    }
    setDefaultStateProxy(elDisplayable);
  }
}
function updateZ2(el, elOption, seriesModel) {
  if (el.isGroup) {
    return;
  }
  var elDisplayable = el;
  var currentZ = seriesModel.currentZ;
  var currentZLevel = seriesModel.currentZLevel;
  elDisplayable.z = currentZ;
  elDisplayable.zlevel = currentZLevel;
  var optZ2 = elOption.z2;
  optZ2 != null && (elDisplayable.z2 = optZ2 || 0);
  for (var i2 = 0; i2 < STATES.length; i2++) {
    updateZForEachState(elDisplayable, elOption, STATES[i2]);
  }
}
function updateZForEachState(elDisplayable, elOption, state2) {
  var isNormal = state2 === NORMAL;
  var elStateOpt = isNormal ? elOption : retrieveStateOption(elOption, state2);
  var optZ2 = elStateOpt ? elStateOpt.z2 : null;
  var stateObj;
  if (optZ2 != null) {
    stateObj = isNormal ? elDisplayable : elDisplayable.ensureState(state2);
    stateObj.z2 = optZ2 || 0;
  }
}
function makeRenderItem(customSeries, data, ecModel, api) {
  var renderItem = customSeries.get("renderItem");
  if (typeof renderItem === "string") {
    var registeredRenderItem = getCustomSeries(renderItem);
    if (registeredRenderItem) {
      renderItem = registeredRenderItem;
    } else if (true) {
      console.warn("Custom series renderItem '" + renderItem + "' not found.\n                Call 'echarts.registerCustomSeries' to register it.");
    }
  }
  var coordSys = customSeries.coordinateSystem;
  var prepareResult2 = {};
  if (coordSys) {
    if (true) {
      assert(renderItem, "series.render is required.");
      assert(coordSys.prepareCustoms || prepareCustoms[coordSys.type], "This coordSys does not support custom series.");
    }
    prepareResult2 = coordSys.prepareCustoms ? coordSys.prepareCustoms(coordSys) : prepareCustoms[coordSys.type](coordSys);
  }
  var userAPI = defaults({
    getWidth: api.getWidth,
    getHeight: api.getHeight,
    getZr: api.getZr,
    getDevicePixelRatio: api.getDevicePixelRatio,
    value,
    style: style2,
    ordinalRawValue,
    styleEmphasis,
    visual,
    barLayout,
    currentSeriesIndices,
    font
  }, prepareResult2.api || {});
  var userParams = {
    // The life cycle of context: current round of rendering.
    // The global life cycle is probably not necessary, because
    // user can store global status by themselves.
    context: {},
    seriesId: customSeries.id,
    seriesName: customSeries.name,
    seriesIndex: customSeries.seriesIndex,
    coordSys: prepareResult2.coordSys,
    dataInsideLength: data.count(),
    encode: wrapEncodeDef(customSeries.getData()),
    itemPayload: customSeries.get("itemPayload") || {}
  };
  var currDataIndexInside;
  var currItemModel;
  var currItemStyleModels = {};
  var currLabelModels = {};
  var seriesItemStyleModels = {};
  var seriesLabelModels = {};
  for (var i2 = 0; i2 < STATES.length; i2++) {
    var stateName = STATES[i2];
    seriesItemStyleModels[stateName] = customSeries.getModel(PATH_ITEM_STYLE[stateName]);
    seriesLabelModels[stateName] = customSeries.getModel(PATH_LABEL[stateName]);
  }
  function getItemModel2(dataIndexInside) {
    return dataIndexInside === currDataIndexInside ? currItemModel || (currItemModel = data.getItemModel(dataIndexInside)) : data.getItemModel(dataIndexInside);
  }
  function getItemStyleModel(dataIndexInside, state2) {
    return !data.hasItemOption ? seriesItemStyleModels[state2] : dataIndexInside === currDataIndexInside ? currItemStyleModels[state2] || (currItemStyleModels[state2] = getItemModel2(dataIndexInside).getModel(PATH_ITEM_STYLE[state2])) : getItemModel2(dataIndexInside).getModel(PATH_ITEM_STYLE[state2]);
  }
  function getLabelModel(dataIndexInside, state2) {
    return !data.hasItemOption ? seriesLabelModels[state2] : dataIndexInside === currDataIndexInside ? currLabelModels[state2] || (currLabelModels[state2] = getItemModel2(dataIndexInside).getModel(PATH_LABEL[state2])) : getItemModel2(dataIndexInside).getModel(PATH_LABEL[state2]);
  }
  return function(dataIndexInside, payload) {
    currDataIndexInside = dataIndexInside;
    currItemModel = null;
    currItemStyleModels = {};
    currLabelModels = {};
    return renderItem && renderItem(defaults({
      dataIndexInside,
      dataIndex: data.getRawIndex(dataIndexInside),
      // Can be used for optimization when zoom or roam.
      actionType: payload ? payload.type : null
    }, userParams), userAPI);
  };
  function value(dim, dataIndexInside) {
    dataIndexInside == null && (dataIndexInside = currDataIndexInside);
    return data.getStore().get(data.getDimensionIndex(dim || 0), dataIndexInside);
  }
  function ordinalRawValue(dim, dataIndexInside) {
    dataIndexInside == null && (dataIndexInside = currDataIndexInside);
    dim = dim || 0;
    var dimInfo = data.getDimensionInfo(dim);
    if (!dimInfo) {
      var dimIndex = data.getDimensionIndex(dim);
      return dimIndex >= 0 ? data.getStore().get(dimIndex, dataIndexInside) : void 0;
    }
    var val = data.get(dimInfo.name, dataIndexInside);
    var ordinalMeta = dimInfo && dimInfo.ordinalMeta;
    return ordinalMeta ? ordinalMeta.categories[val] : val;
  }
  function style2(userProps, dataIndexInside) {
    if (true) {
      warnDeprecated("api.style", "Please write literal style directly instead.");
    }
    dataIndexInside == null && (dataIndexInside = currDataIndexInside);
    var style3 = data.getItemVisual(dataIndexInside, "style");
    var visualColor = style3 && style3.fill;
    var opacity = style3 && style3.opacity;
    var itemStyle = getItemStyleModel(dataIndexInside, NORMAL).getItemStyle();
    visualColor != null && (itemStyle.fill = visualColor);
    opacity != null && (itemStyle.opacity = opacity);
    var opt = {
      inheritColor: isString(visualColor) ? visualColor : tokens_default.color.neutral99
    };
    var labelModel = getLabelModel(dataIndexInside, NORMAL);
    var textStyle = createTextStyle(labelModel, null, opt, false, true);
    textStyle.text = labelModel.getShallow("show") ? retrieve2(customSeries.getFormattedLabel(dataIndexInside, NORMAL), getDefaultLabel(data, dataIndexInside)) : null;
    var textConfig = createTextConfig(labelModel, opt, false);
    preFetchFromExtra(userProps, itemStyle);
    itemStyle = convertToEC4StyleForCustomSerise(itemStyle, textStyle, textConfig);
    userProps && applyUserPropsAfter(itemStyle, userProps);
    itemStyle.legacy = true;
    return itemStyle;
  }
  function styleEmphasis(userProps, dataIndexInside) {
    if (true) {
      warnDeprecated("api.styleEmphasis", "Please write literal style directly instead.");
    }
    dataIndexInside == null && (dataIndexInside = currDataIndexInside);
    var itemStyle = getItemStyleModel(dataIndexInside, EMPHASIS).getItemStyle();
    var labelModel = getLabelModel(dataIndexInside, EMPHASIS);
    var textStyle = createTextStyle(labelModel, null, null, true, true);
    textStyle.text = labelModel.getShallow("show") ? retrieve3(customSeries.getFormattedLabel(dataIndexInside, EMPHASIS), customSeries.getFormattedLabel(dataIndexInside, NORMAL), getDefaultLabel(data, dataIndexInside)) : null;
    var textConfig = createTextConfig(labelModel, null, true);
    preFetchFromExtra(userProps, itemStyle);
    itemStyle = convertToEC4StyleForCustomSerise(itemStyle, textStyle, textConfig);
    userProps && applyUserPropsAfter(itemStyle, userProps);
    itemStyle.legacy = true;
    return itemStyle;
  }
  function applyUserPropsAfter(itemStyle, extra) {
    for (var key2 in extra) {
      if (hasOwn(extra, key2)) {
        itemStyle[key2] = extra[key2];
      }
    }
  }
  function preFetchFromExtra(extra, itemStyle) {
    if (extra) {
      extra.textFill && (itemStyle.textFill = extra.textFill);
      extra.textPosition && (itemStyle.textPosition = extra.textPosition);
    }
  }
  function visual(visualType, dataIndexInside) {
    dataIndexInside == null && (dataIndexInside = currDataIndexInside);
    if (hasOwn(STYLE_VISUAL_TYPE, visualType)) {
      var style_1 = data.getItemVisual(dataIndexInside, "style");
      return style_1 ? style_1[STYLE_VISUAL_TYPE[visualType]] : null;
    }
    if (hasOwn(NON_STYLE_VISUAL_PROPS, visualType)) {
      return data.getItemVisual(dataIndexInside, visualType);
    }
  }
  function barLayout(opt) {
    if (coordSys.type === "cartesian2d") {
      var baseAxis = coordSys.getBaseAxis();
      return computeBarLayoutForCustomSeries(defaults({
        axis: baseAxis
      }, opt));
    }
  }
  function currentSeriesIndices() {
    return ecModel.getCurrentSeriesIndices();
  }
  function font(opt) {
    return getFont(opt, ecModel);
  }
}
function wrapEncodeDef(data) {
  var encodeDef = {};
  each(data.dimensions, function(dimName) {
    var dimInfo = data.getDimensionInfo(dimName);
    if (!dimInfo.isExtraCoord) {
      var coordDim = dimInfo.coordDim;
      var dataDims = encodeDef[coordDim] = encodeDef[coordDim] || [];
      dataDims[dimInfo.coordDimIndex] = data.getDimensionIndex(dimName);
    }
  });
  return encodeDef;
}
function createOrUpdateItem(api, existsEl, dataIndex, elOption, seriesModel, group2, data) {
  if (!elOption) {
    group2.remove(existsEl);
    return;
  }
  var el = doCreateOrUpdateEl(api, existsEl, dataIndex, elOption, seriesModel, group2);
  el && data.setItemGraphicEl(dataIndex, el);
  el && toggleHoverEmphasis(el, elOption.focus, elOption.blurScope, elOption.emphasisDisabled);
  return el;
}
function doCreateOrUpdateEl(api, existsEl, dataIndex, elOption, seriesModel, group2) {
  if (true) {
    assert(elOption, "should not have an null/undefined element setting");
  }
  var toBeReplacedIdx = -1;
  var oldEl = existsEl;
  if (existsEl && doesElNeedRecreate(existsEl, elOption, seriesModel)) {
    toBeReplacedIdx = indexOf(group2.childrenRef(), existsEl);
    existsEl = null;
  }
  var isInit = !existsEl;
  var el = existsEl;
  if (!el) {
    el = createEl2(elOption);
    if (oldEl) {
      copyElement(oldEl, el);
    }
  } else {
    el.clearStates();
  }
  if (elOption.morph === false) {
    el.disableMorphing = true;
  } else if (el.disableMorphing) {
    el.disableMorphing = false;
  }
  if (elOption.tooltipDisabled) {
    el.tooltipDisabled = true;
  }
  attachedTxInfoTmp.normal.cfg = attachedTxInfoTmp.normal.conOpt = attachedTxInfoTmp.emphasis.cfg = attachedTxInfoTmp.emphasis.conOpt = attachedTxInfoTmp.blur.cfg = attachedTxInfoTmp.blur.conOpt = attachedTxInfoTmp.select.cfg = attachedTxInfoTmp.select.conOpt = null;
  attachedTxInfoTmp.isLegacy = false;
  doCreateOrUpdateAttachedTx(el, dataIndex, elOption, seriesModel, isInit, attachedTxInfoTmp);
  doCreateOrUpdateClipPath(el, dataIndex, elOption, seriesModel, isInit);
  updateElNormal(api, el, dataIndex, elOption, attachedTxInfoTmp, seriesModel, isInit);
  hasOwn(elOption, "info") && (customInnerStore(el).info = elOption.info);
  for (var i2 = 0; i2 < STATES.length; i2++) {
    var stateName = STATES[i2];
    if (stateName !== NORMAL) {
      var otherStateOpt = retrieveStateOption(elOption, stateName);
      var otherStyleOpt = retrieveStyleOptionOnState(elOption, otherStateOpt, stateName);
      updateElOnState(stateName, el, otherStateOpt, otherStyleOpt, attachedTxInfoTmp);
    }
  }
  updateZ2(el, elOption, seriesModel);
  if (elOption.type === "group") {
    mergeChildren(api, el, dataIndex, elOption, seriesModel);
  }
  if (toBeReplacedIdx >= 0) {
    group2.replaceAt(el, toBeReplacedIdx);
  } else {
    group2.add(el);
  }
  return el;
}
function doesElNeedRecreate(el, elOption, seriesModel) {
  var elInner = customInnerStore(el);
  var elOptionType = elOption.type;
  var elOptionShape = elOption.shape;
  var elOptionStyle = elOption.style;
  return (
    // Always create new if universal transition is enabled.
    // Because we do transition after render. It needs to know what old element is. Replacement will loose it.
    seriesModel.isUniversalTransitionEnabled() || elOptionType != null && elOptionType !== elInner.customGraphicType || elOptionType === "path" && hasOwnPathData(elOptionShape) && getPathData(elOptionShape) !== elInner.customPathData || elOptionType === "image" && hasOwn(elOptionStyle, "image") && elOptionStyle.image !== elInner.customImagePath
  );
}
function doCreateOrUpdateClipPath(el, dataIndex, elOption, seriesModel, isInit) {
  var clipPathOpt = elOption.clipPath;
  if (clipPathOpt === false) {
    if (el && el.getClipPath()) {
      el.removeClipPath();
    }
  } else if (clipPathOpt) {
    var clipPath = el.getClipPath();
    if (clipPath && doesElNeedRecreate(clipPath, clipPathOpt, seriesModel)) {
      clipPath = null;
    }
    if (!clipPath) {
      clipPath = createEl2(clipPathOpt);
      if (true) {
        assert(isPath2(clipPath), "Only any type of `path` can be used in `clipPath`, rather than " + clipPath.type + ".");
      }
      el.setClipPath(clipPath);
    }
    updateElNormal(null, clipPath, dataIndex, clipPathOpt, null, seriesModel, isInit);
  }
}
function doCreateOrUpdateAttachedTx(el, dataIndex, elOption, seriesModel, isInit, attachedTxInfo) {
  if (el.isGroup || el.type === "compoundPath") {
    return;
  }
  processTxInfo(elOption, null, attachedTxInfo);
  processTxInfo(elOption, EMPHASIS, attachedTxInfo);
  var txConOptNormal = attachedTxInfo.normal.conOpt;
  var txConOptEmphasis = attachedTxInfo.emphasis.conOpt;
  var txConOptBlur = attachedTxInfo.blur.conOpt;
  var txConOptSelect = attachedTxInfo.select.conOpt;
  if (txConOptNormal != null || txConOptEmphasis != null || txConOptSelect != null || txConOptBlur != null) {
    var textContent = el.getTextContent();
    if (txConOptNormal === false) {
      textContent && el.removeTextContent();
    } else {
      txConOptNormal = attachedTxInfo.normal.conOpt = txConOptNormal || {
        type: "text"
      };
      if (!textContent) {
        textContent = createEl2(txConOptNormal);
        el.setTextContent(textContent);
      } else {
        textContent.clearStates();
      }
      updateElNormal(null, textContent, dataIndex, txConOptNormal, null, seriesModel, isInit);
      var txConStlOptNormal = txConOptNormal && txConOptNormal.style;
      for (var i2 = 0; i2 < STATES.length; i2++) {
        var stateName = STATES[i2];
        if (stateName !== NORMAL) {
          var txConOptOtherState = attachedTxInfo[stateName].conOpt;
          updateElOnState(stateName, textContent, txConOptOtherState, retrieveStyleOptionOnState(txConOptNormal, txConOptOtherState, stateName), null);
        }
      }
      txConStlOptNormal ? textContent.dirty() : textContent.markRedraw();
    }
  }
}
function processTxInfo(elOption, state2, attachedTxInfo) {
  var stateOpt = !state2 ? elOption : retrieveStateOption(elOption, state2);
  var styleOpt = !state2 ? elOption.style : retrieveStyleOptionOnState(elOption, stateOpt, EMPHASIS);
  var elType = elOption.type;
  var txCfg = stateOpt ? stateOpt.textConfig : null;
  var txConOptNormal = elOption.textContent;
  var txConOpt = !txConOptNormal ? null : !state2 ? txConOptNormal : retrieveStateOption(txConOptNormal, state2);
  if (styleOpt && // Because emphasis style has little info to detect legacy,
  // if normal is legacy, emphasis is trade as legacy.
  (attachedTxInfo.isLegacy || isEC4CompatibleStyle(styleOpt, elType, !!txCfg, !!txConOpt))) {
    attachedTxInfo.isLegacy = true;
    var convertResult = convertFromEC4CompatibleStyle(styleOpt, elType, !state2);
    if (!txCfg && convertResult.textConfig) {
      txCfg = convertResult.textConfig;
    }
    if (!txConOpt && convertResult.textContent) {
      txConOpt = convertResult.textContent;
    }
  }
  if (!state2 && txConOpt) {
    var txConOptNormal_1 = txConOpt;
    !txConOptNormal_1.type && (txConOptNormal_1.type = "text");
    if (true) {
      assert(txConOptNormal_1.type === "text", 'textContent.type must be "text"');
    }
  }
  var info = !state2 ? attachedTxInfo.normal : attachedTxInfo[state2];
  info.cfg = txCfg;
  info.conOpt = txConOpt;
}
function retrieveStateOption(elOption, state2) {
  return !state2 ? elOption : elOption ? elOption[state2] : null;
}
function retrieveStyleOptionOnState(stateOptionNormal, stateOption, state2) {
  var style2 = stateOption && stateOption.style;
  if (style2 == null && state2 === EMPHASIS && stateOptionNormal) {
    style2 = stateOptionNormal.styleEmphasis;
  }
  return style2;
}
function mergeChildren(api, el, dataIndex, elOption, seriesModel) {
  var newChildren = elOption.children;
  var newLen = newChildren ? newChildren.length : 0;
  var mergeChildren2 = elOption.$mergeChildren;
  var byName = mergeChildren2 === "byName" || elOption.diffChildrenByName;
  var notMerge = mergeChildren2 === false;
  if (!newLen && !byName && !notMerge) {
    return;
  }
  if (byName) {
    diffGroupChildren({
      api,
      oldChildren: el.children() || [],
      newChildren: newChildren || [],
      dataIndex,
      seriesModel,
      group: el
    });
    return;
  }
  notMerge && el.removeAll();
  var index = 0;
  for (; index < newLen; index++) {
    var newChild = newChildren[index];
    var oldChild = el.childAt(index);
    if (newChild) {
      if (newChild.ignore == null) {
        newChild.ignore = false;
      }
      doCreateOrUpdateEl(api, oldChild, dataIndex, newChild, seriesModel, el);
    } else {
      if (true) {
        assert(oldChild, "renderItem should not return a group containing elements as null/undefined/{} if they do not exist before.");
      }
      oldChild.ignore = true;
    }
  }
  for (var i2 = el.childCount() - 1; i2 >= index; i2--) {
    var child = el.childAt(i2);
    removeChildFromGroup(el, child, seriesModel);
  }
}
function removeChildFromGroup(group2, child, seriesModel) {
  child && applyLeaveTransition(child, customInnerStore(group2).option, seriesModel);
}
function diffGroupChildren(context) {
  new DataDiffer_default(context.oldChildren, context.newChildren, getKey, getKey, context).add(processAddUpdate).update(processAddUpdate).remove(processRemove).execute();
}
function getKey(item, idx) {
  var name = item && item.name;
  return name != null ? name : GROUP_DIFF_PREFIX + idx;
}
function processAddUpdate(newIndex, oldIndex) {
  var context = this.context;
  var childOption = newIndex != null ? context.newChildren[newIndex] : null;
  var child = oldIndex != null ? context.oldChildren[oldIndex] : null;
  doCreateOrUpdateEl(context.api, child, context.dataIndex, childOption, context.seriesModel, context.group);
}
function processRemove(oldIndex) {
  var context = this.context;
  var child = context.oldChildren[oldIndex];
  child && applyLeaveTransition(child, customInnerStore(child).option, context.seriesModel);
}
function getPathData(shape) {
  return shape && (shape.pathData || shape.d);
}
function hasOwnPathData(shape) {
  return shape && (hasOwn(shape, "pathData") || hasOwn(shape, "d"));
}

// node_modules/echarts/lib/chart/custom/install.js
function install10(registers) {
  registers.registerChartView(CustomView_default);
  registers.registerSeriesModel(CustomSeries_default);
}

// node_modules/echarts/lib/component/axisPointer/BaseAxisPointer.js
var inner14 = makeInner();
var clone4 = clone;
var bind2 = bind;
var BaseAxisPointer = (
  /** @class */
  (function() {
    function BaseAxisPointer2() {
      this._dragging = false;
      this.animationThreshold = 15;
    }
    BaseAxisPointer2.prototype.render = function(axisModel, axisPointerModel, api, forceRender) {
      var value = axisPointerModel.get("value");
      var status = axisPointerModel.get("status");
      this._axisModel = axisModel;
      this._axisPointerModel = axisPointerModel;
      this._api = api;
      if (!forceRender && this._lastValue === value && this._lastStatus === status) {
        return;
      }
      this._lastValue = value;
      this._lastStatus = status;
      var group2 = this._group;
      var handle = this._handle;
      if (!status || status === "hide") {
        group2 && group2.hide();
        handle && handle.hide();
        return;
      }
      group2 && group2.show();
      handle && handle.show();
      var elOption = {};
      this.makeElOption(elOption, value, axisModel, axisPointerModel, api);
      var graphicKey = elOption.graphicKey;
      if (graphicKey !== this._lastGraphicKey) {
        this.clear(api);
      }
      this._lastGraphicKey = graphicKey;
      var moveAnimation = this._moveAnimation = this.determineAnimation(axisModel, axisPointerModel);
      if (!group2) {
        group2 = this._group = new Group_default();
        this.createPointerEl(group2, elOption, axisModel, axisPointerModel);
        this.createLabelEl(group2, elOption, axisModel, axisPointerModel);
        api.getZr().add(group2);
      } else {
        var doUpdateProps = curry(updateProps2, axisPointerModel, moveAnimation);
        this.updatePointerEl(group2, elOption, doUpdateProps);
        this.updateLabelEl(group2, elOption, doUpdateProps, axisPointerModel);
      }
      updateMandatoryProps(group2, axisPointerModel, true);
      this._renderHandle(value);
    };
    BaseAxisPointer2.prototype.remove = function(api) {
      this.clear(api);
    };
    BaseAxisPointer2.prototype.dispose = function(api) {
      this.clear(api);
    };
    BaseAxisPointer2.prototype.determineAnimation = function(axisModel, axisPointerModel) {
      var animation2 = axisPointerModel.get("animation");
      var axis = axisModel.axis;
      var isCategoryAxis = axis.type === "category";
      var useSnap = axisPointerModel.get("snap");
      if (!useSnap && !isCategoryAxis) {
        return false;
      }
      if (animation2 === "auto" || animation2 == null) {
        var animationThreshold = this.animationThreshold;
        if (isCategoryAxis && calcBandWidth(axis).w > animationThreshold) {
          return true;
        }
        if (useSnap) {
          var seriesDataCount = getAxisInfo(axisModel).seriesDataCount;
          var axisExtent = axis.getExtent();
          return Math.abs(axisExtent[0] - axisExtent[1]) / seriesDataCount > animationThreshold;
        }
        return false;
      }
      return animation2 === true;
    };
    BaseAxisPointer2.prototype.makeElOption = function(elOption, value, axisModel, axisPointerModel, api) {
    };
    BaseAxisPointer2.prototype.createPointerEl = function(group2, elOption, axisModel, axisPointerModel) {
      var pointerOption = elOption.pointer;
      if (pointerOption) {
        var pointerEl = inner14(group2).pointerEl = new graphic_exports[pointerOption.type](clone4(elOption.pointer));
        group2.add(pointerEl);
      }
    };
    BaseAxisPointer2.prototype.createLabelEl = function(group2, elOption, axisModel, axisPointerModel) {
      if (elOption.label) {
        var labelEl = inner14(group2).labelEl = new Text_default(clone4(elOption.label));
        group2.add(labelEl);
        updateLabelShowHide(labelEl, axisPointerModel);
      }
    };
    BaseAxisPointer2.prototype.updatePointerEl = function(group2, elOption, updateProps3) {
      var pointerEl = inner14(group2).pointerEl;
      if (pointerEl && elOption.pointer) {
        pointerEl.setStyle(elOption.pointer.style);
        updateProps3(pointerEl, {
          shape: elOption.pointer.shape
        });
      }
    };
    BaseAxisPointer2.prototype.updateLabelEl = function(group2, elOption, updateProps3, axisPointerModel) {
      var labelEl = inner14(group2).labelEl;
      if (labelEl) {
        labelEl.setStyle(elOption.label.style);
        updateProps3(labelEl, {
          // Consider text length change in vertical axis, animation should
          // be used on shape, otherwise the effect will be weird.
          // TODOTODO
          // shape: elOption.label.shape,
          x: elOption.label.x,
          y: elOption.label.y
        });
        updateLabelShowHide(labelEl, axisPointerModel);
      }
    };
    BaseAxisPointer2.prototype._renderHandle = function(value) {
      if (this._dragging || !this.updateHandleTransform) {
        return;
      }
      var axisPointerModel = this._axisPointerModel;
      var zr = this._api.getZr();
      var handle = this._handle;
      var handleModel = axisPointerModel.getModel("handle");
      var status = axisPointerModel.get("status");
      if (!handleModel.get("show") || !status || status === "hide") {
        handle && zr.remove(handle);
        this._handle = null;
        return;
      }
      var isInit;
      if (!this._handle) {
        isInit = true;
        handle = this._handle = createIcon(handleModel.get("icon"), {
          cursor: "move",
          draggable: true,
          onmousemove: function(e2) {
            stop(e2.event);
          },
          onmousedown: bind2(this._onHandleDragMove, this, 0, 0),
          drift: bind2(this._onHandleDragMove, this),
          ondragend: bind2(this._onHandleDragEnd, this)
        });
        zr.add(handle);
      }
      updateMandatoryProps(handle, axisPointerModel, false);
      handle.setStyle(handleModel.getItemStyle(null, ["color", "borderColor", "borderWidth", "opacity", "shadowColor", "shadowBlur", "shadowOffsetX", "shadowOffsetY"]));
      var handleSize = handleModel.get("size");
      if (!isArray(handleSize)) {
        handleSize = [handleSize, handleSize];
      }
      handle.scaleX = handleSize[0] / 2;
      handle.scaleY = handleSize[1] / 2;
      createOrUpdate(this, "_doDispatchAxisPointer", handleModel.get("throttle") || 0, "fixRate");
      this._moveHandleToValue(value, isInit);
    };
    BaseAxisPointer2.prototype._moveHandleToValue = function(value, isInit) {
      updateProps2(this._axisPointerModel, !isInit && this._moveAnimation, this._handle, getHandleTransProps(this.getHandleTransform(value, this._axisModel, this._axisPointerModel)));
    };
    BaseAxisPointer2.prototype._onHandleDragMove = function(dx, dy) {
      var handle = this._handle;
      if (!handle) {
        return;
      }
      this._dragging = true;
      var trans = this.updateHandleTransform(getHandleTransProps(handle), [dx, dy], this._axisModel, this._axisPointerModel);
      this._payloadInfo = trans;
      handle.stopAnimation();
      handle.attr(getHandleTransProps(trans));
      inner14(handle).lastProp = null;
      this._doDispatchAxisPointer();
    };
    BaseAxisPointer2.prototype._doDispatchAxisPointer = function() {
      var handle = this._handle;
      if (!handle) {
        return;
      }
      var payloadInfo = this._payloadInfo;
      var axisModel = this._axisModel;
      this._api.dispatchAction({
        type: "updateAxisPointer",
        x: payloadInfo.cursorPoint[0],
        y: payloadInfo.cursorPoint[1],
        tooltipOption: payloadInfo.tooltipOption,
        axesInfo: [{
          axisDim: axisModel.axis.dim,
          axisIndex: axisModel.componentIndex
        }]
      });
    };
    BaseAxisPointer2.prototype._onHandleDragEnd = function() {
      this._dragging = false;
      var handle = this._handle;
      if (!handle) {
        return;
      }
      var value = this._axisPointerModel.get("value");
      this._moveHandleToValue(value);
      this._api.dispatchAction({
        type: "hideTip"
      });
    };
    BaseAxisPointer2.prototype.clear = function(api) {
      this._lastValue = null;
      this._lastStatus = null;
      var zr = api.getZr();
      var group2 = this._group;
      var handle = this._handle;
      if (zr && group2) {
        this._lastGraphicKey = null;
        group2 && zr.remove(group2);
        handle && zr.remove(handle);
        this._group = null;
        this._handle = null;
        this._payloadInfo = null;
      }
      clear(this, "_doDispatchAxisPointer");
    };
    BaseAxisPointer2.prototype.doClear = function() {
    };
    BaseAxisPointer2.prototype.buildLabel = function(xy, wh, xDimIndex) {
      xDimIndex = xDimIndex || 0;
      return {
        x: xy[xDimIndex],
        y: xy[1 - xDimIndex],
        width: wh[xDimIndex],
        height: wh[1 - xDimIndex]
      };
    };
    return BaseAxisPointer2;
  })()
);
function updateProps2(animationModel, moveAnimation, el, props) {
  if (!propsEqual(inner14(el).lastProp, props)) {
    inner14(el).lastProp = props;
    moveAnimation ? updateProps(el, props, animationModel) : (el.stopAnimation(), el.attr(props));
  }
}
function propsEqual(lastProps, newProps) {
  if (isObject(lastProps) && isObject(newProps)) {
    var equals_1 = true;
    each(newProps, function(item, key2) {
      equals_1 = equals_1 && propsEqual(lastProps[key2], item);
    });
    return !!equals_1;
  } else {
    return lastProps === newProps;
  }
}
function updateLabelShowHide(labelEl, axisPointerModel) {
  labelEl[axisPointerModel.get(["label", "show"]) ? "show" : "hide"]();
}
function getHandleTransProps(trans) {
  return {
    x: trans.x || 0,
    y: trans.y || 0,
    rotation: trans.rotation || 0
  };
}
function updateMandatoryProps(group2, axisPointerModel, silent) {
  var z = axisPointerModel.get("z");
  var zlevel = axisPointerModel.get("zlevel");
  group2 && group2.traverse(function(el) {
    if (el.type !== "group") {
      z != null && (el.z = z);
      zlevel != null && (el.zlevel = zlevel);
      el.silent = silent;
    }
  });
}
var BaseAxisPointer_default = BaseAxisPointer;

// node_modules/echarts/lib/component/axisPointer/viewHelper.js
function buildElStyle(axisPointerModel) {
  var axisPointerType = axisPointerModel.get("type");
  var styleModel = axisPointerModel.getModel(axisPointerType + "Style");
  var style2;
  if (axisPointerType === "line") {
    style2 = styleModel.getLineStyle();
    style2.fill = null;
  } else if (axisPointerType === "shadow") {
    style2 = styleModel.getAreaStyle();
    style2.stroke = null;
  }
  return style2;
}
function buildLabelElOption(elOption, axisModel, axisPointerModel, api, labelPos) {
  var value = axisPointerModel.get("value");
  var text = getValueLabel(value, axisModel.axis, axisModel.ecModel, axisPointerModel.get("seriesDataIndices"), {
    precision: axisPointerModel.get(["label", "precision"]),
    formatter: axisPointerModel.get(["label", "formatter"])
  });
  var labelModel = axisPointerModel.getModel("label");
  var paddings = normalizeCssArray2(labelModel.get("padding") || 0);
  var font = labelModel.getFont();
  var textRect = getBoundingRect(text, font);
  var position = labelPos.position;
  var width = textRect.width + paddings[1] + paddings[3];
  var height = textRect.height + paddings[0] + paddings[2];
  var align = labelPos.align;
  align === "right" && (position[0] -= width);
  align === "center" && (position[0] -= width / 2);
  var verticalAlign = labelPos.verticalAlign;
  verticalAlign === "bottom" && (position[1] -= height);
  verticalAlign === "middle" && (position[1] -= height / 2);
  confineInContainer(position, width, height, api);
  var bgColor = labelModel.get("backgroundColor");
  if (!bgColor || bgColor === "auto") {
    bgColor = axisModel.get(["axisLine", "lineStyle", "color"]);
  }
  elOption.label = {
    // shape: {x: 0, y: 0, width: width, height: height, r: labelModel.get('borderRadius')},
    x: position[0],
    y: position[1],
    style: createTextStyle(labelModel, {
      text,
      font,
      fill: labelModel.getTextColor(),
      padding: paddings,
      backgroundColor: bgColor
    }),
    // Label should be over axisPointer.
    z2: 10
  };
}
function confineInContainer(position, width, height, api) {
  var viewWidth = api.getWidth();
  var viewHeight = api.getHeight();
  position[0] = Math.min(position[0] + width, viewWidth) - width;
  position[1] = Math.min(position[1] + height, viewHeight) - height;
  position[0] = Math.max(position[0], 0);
  position[1] = Math.max(position[1], 0);
}
function getValueLabel(value, axis, ecModel, seriesDataIndices, opt) {
  value = axis.scale.parse(value);
  var text = axis.scale.getLabel({
    value
  }, {
    // If `precision` is set, width can be fixed (like '12.00500'), which
    // helps to debounce when when moving label.
    precision: opt.precision
  });
  var formatter = opt.formatter;
  if (formatter) {
    var params_1 = {
      value: getAxisRawValue(axis, {
        value
      }),
      axisDimension: axis.dim,
      axisIndex: axis.index,
      seriesData: []
    };
    each(seriesDataIndices, function(idxItem) {
      var series = ecModel.getSeriesByIndex(idxItem.seriesIndex);
      var dataIndex = idxItem.dataIndexInside;
      var dataParams = series && series.getDataParams(dataIndex);
      dataParams && params_1.seriesData.push(dataParams);
    });
    if (isString(formatter)) {
      text = formatter.replace("{value}", text);
    } else if (isFunction(formatter)) {
      text = formatter(params_1);
    }
  }
  return text;
}
function getTransformedPosition(axis, value, layoutInfo) {
  var transform = create();
  rotate(transform, transform, layoutInfo.rotation);
  translate(transform, transform, layoutInfo.position);
  return applyTransform2([axis.dataToCoord(value), (layoutInfo.labelOffset || 0) + (layoutInfo.labelDirection || 1) * (layoutInfo.labelMargin || 0)], transform);
}
function buildCartesianSingleLabelElOption(value, elOption, layoutInfo, axisModel, axisPointerModel, api) {
  var textLayout = AxisBuilder_default.innerTextLayout(layoutInfo.rotation, 0, layoutInfo.labelDirection);
  layoutInfo.labelMargin = axisPointerModel.get(["label", "margin"]);
  buildLabelElOption(elOption, axisModel, axisPointerModel, api, {
    position: getTransformedPosition(axisModel.axis, value, layoutInfo),
    align: textLayout.textAlign,
    verticalAlign: textLayout.textVerticalAlign
  });
}
function makeLineShape(p1, p2, xDimIndex) {
  xDimIndex = xDimIndex || 0;
  return {
    x1: p1[xDimIndex],
    y1: p1[1 - xDimIndex],
    x2: p2[xDimIndex],
    y2: p2[1 - xDimIndex]
  };
}
function makeRectShape(xy, wh, xDimIndex) {
  xDimIndex = xDimIndex || 0;
  return {
    x: xy[xDimIndex],
    y: xy[1 - xDimIndex],
    width: wh[xDimIndex],
    height: wh[1 - xDimIndex]
  };
}
function makeSectorShape(cx, cy, r0, r, startAngle, endAngle) {
  return {
    cx,
    cy,
    r0,
    r,
    startAngle,
    endAngle,
    clockwise: true
  };
}
function calcAxisPointerShadowBandWidth(axis, seriesDataIndices, ecModel) {
  return calcBandWidth(axis, {
    fromStat: {
      sers: map2(seriesDataIndices, function(item) {
        return ecModel.getSeriesByIndex(item.seriesIndex);
      })
    },
    min: 1
  }).w;
}
function calcAxisPointerShadowEnds(val, axisExtent, bandWidth) {
  return [mathMax4(mathMin4(axisExtent[0], axisExtent[1]), val - bandWidth / 2), mathMin4(val + bandWidth / 2, mathMax4(axisExtent[0], axisExtent[1]))];
}

// node_modules/echarts/lib/component/axisPointer/CartesianAxisPointer.js
var CartesianAxisPointer = (
  /** @class */
  (function(_super) {
    __extends(CartesianAxisPointer2, _super);
    function CartesianAxisPointer2() {
      return _super !== null && _super.apply(this, arguments) || this;
    }
    CartesianAxisPointer2.prototype.makeElOption = function(elOption, value, axisModel, axisPointerModel, api) {
      var axis = axisModel.axis;
      var grid = axis.grid;
      var axisPointerType = axisPointerModel.get("type");
      var thisExtent = axis.getGlobalExtent();
      var otherExtent = getCartesian(grid, axis).getOtherAxis(axis).getGlobalExtent();
      var pixelValue = axis.toGlobalCoord(axis.dataToCoord(value, true));
      if (axisPointerType && axisPointerType !== "none") {
        var elStyle = buildElStyle(axisPointerModel);
        var pointerOption = pointerShapeBuilder[axisPointerType](axis, pixelValue, thisExtent, otherExtent, axisPointerModel.get("seriesDataIndices"), axisPointerModel.ecModel);
        pointerOption.style = elStyle;
        elOption.graphicKey = pointerOption.type;
        elOption.pointer = pointerOption;
      }
      var layoutInfo = layout(grid.getRect(), axisModel);
      buildCartesianSingleLabelElOption(value, elOption, layoutInfo, axisModel, axisPointerModel, api);
    };
    CartesianAxisPointer2.prototype.getHandleTransform = function(value, axisModel, axisPointerModel) {
      var layoutInfo = layout(axisModel.axis.grid.getRect(), axisModel, {
        labelInside: false
      });
      layoutInfo.labelMargin = axisPointerModel.get(["handle", "margin"]);
      var pos = getTransformedPosition(axisModel.axis, value, layoutInfo);
      return {
        x: pos[0],
        y: pos[1],
        rotation: layoutInfo.rotation + (layoutInfo.labelDirection < 0 ? Math.PI : 0)
      };
    };
    CartesianAxisPointer2.prototype.updateHandleTransform = function(transform, delta, axisModel, axisPointerModel) {
      var axis = axisModel.axis;
      var grid = axis.grid;
      var axisExtent = axis.getGlobalExtent(true);
      var otherExtent = getCartesian(grid, axis).getOtherAxis(axis).getGlobalExtent();
      var dimIndex = axis.dim === "x" ? 0 : 1;
      var currPosition = [transform.x, transform.y];
      currPosition[dimIndex] += delta[dimIndex];
      currPosition[dimIndex] = mathMin4(axisExtent[1], currPosition[dimIndex]);
      currPosition[dimIndex] = mathMax4(axisExtent[0], currPosition[dimIndex]);
      var cursorOtherValue = (otherExtent[1] + otherExtent[0]) / 2;
      var cursorPoint = [cursorOtherValue, cursorOtherValue];
      cursorPoint[dimIndex] = currPosition[dimIndex];
      var tooltipOptions = [{
        verticalAlign: "middle"
      }, {
        align: "center"
      }];
      return {
        x: currPosition[0],
        y: currPosition[1],
        rotation: transform.rotation,
        cursorPoint,
        tooltipOption: tooltipOptions[dimIndex]
      };
    };
    return CartesianAxisPointer2;
  })(BaseAxisPointer_default)
);
function getCartesian(grid, axis) {
  var opt = {};
  opt[axis.dim + "AxisIndex"] = axis.index;
  return grid.getCartesian(opt);
}
var pointerShapeBuilder = {
  line: function(axis, pixelValue, thisExtent, otherExtent) {
    var targetShape = makeLineShape([pixelValue, otherExtent[0]], [pixelValue, otherExtent[1]], getAxisDimIndex(axis));
    return {
      type: "Line",
      subPixelOptimize: true,
      shape: targetShape
    };
  },
  shadow: function(axis, pixelValue, thisExtent, otherExtent, seriesDataIndices, ecModel) {
    var bandWidth = calcAxisPointerShadowBandWidth(axis, seriesDataIndices, ecModel);
    var otherSpan = otherExtent[1] - otherExtent[0];
    var _a2 = calcAxisPointerShadowEnds(pixelValue, thisExtent, bandWidth), min3 = _a2[0], max3 = _a2[1];
    return {
      type: "Rect",
      shape: makeRectShape([min3, otherExtent[0]], [max3 - min3, otherSpan], getAxisDimIndex(axis))
    };
  }
};
function getAxisDimIndex(axis) {
  return axis.dim === "x" ? 0 : 1;
}
var CartesianAxisPointer_default = CartesianAxisPointer;

// node_modules/echarts/lib/component/axisPointer/AxisPointerModel.js
var AxisPointerModel = (
  /** @class */
  (function(_super) {
    __extends(AxisPointerModel2, _super);
    function AxisPointerModel2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = AxisPointerModel2.type;
      return _this;
    }
    AxisPointerModel2.type = "axisPointer";
    AxisPointerModel2.defaultOption = {
      // 'auto' means that show when triggered by tooltip or handle.
      show: "auto",
      // zlevel: 0,
      z: 50,
      type: "line",
      // axispointer triggered by tootip determine snap automatically,
      // see `modelHelper`.
      snap: false,
      triggerTooltip: true,
      triggerEmphasis: true,
      value: null,
      status: null,
      link: [],
      // Do not set 'auto' here, otherwise global animation: false
      // will not effect at this axispointer.
      animation: null,
      animationDurationUpdate: 200,
      lineStyle: {
        color: tokens_default.color.border,
        width: 1,
        type: "dashed"
      },
      shadowStyle: {
        color: tokens_default.color.shadowTint
      },
      label: {
        show: true,
        formatter: null,
        precision: "auto",
        margin: 3,
        color: tokens_default.color.neutral00,
        padding: [5, 7, 5, 7],
        backgroundColor: tokens_default.color.accent60,
        borderColor: null,
        borderWidth: 0,
        borderRadius: 3
      },
      handle: {
        show: false,
        // eslint-disable-next-line
        icon: "M10.7,11.9v-1.3H9.3v1.3c-4.9,0.3-8.8,4.4-8.8,9.4c0,5,3.9,9.1,8.8,9.4h1.3c4.9-0.3,8.8-4.4,8.8-9.4C19.5,16.3,15.6,12.2,10.7,11.9z M13.3,24.4H6.7v-1.2h6.6z M13.3,22H6.7v-1.2h6.6z M13.3,19.6H6.7v-1.2h6.6z",
        size: 45,
        // handle margin is from symbol center to axis, which is stable when circular move.
        margin: 50,
        // color: '#1b8bbd'
        // color: '#2f4554'
        color: tokens_default.color.accent40,
        // For mobile performance
        throttle: 40
      }
    };
    return AxisPointerModel2;
  })(Component_default)
);
var AxisPointerModel_default = AxisPointerModel;

// node_modules/echarts/lib/component/axisPointer/globalListener.js
var inner15 = makeInner();
var each5 = each;
function register(key2, api, handler) {
  if (env_default.node) {
    return;
  }
  var zr = api.getZr();
  inner15(zr).records || (inner15(zr).records = {});
  initGlobalListeners(zr, api);
  var record = inner15(zr).records[key2] || (inner15(zr).records[key2] = {});
  record.handler = handler;
}
function initGlobalListeners(zr, api) {
  if (inner15(zr).initialized) {
    return;
  }
  inner15(zr).initialized = true;
  useHandler("click", curry(doEnter, "click"));
  useHandler("mousemove", curry(doEnter, "mousemove"));
  useHandler("mousewheel", curry(doEnter, "mousewheel"));
  useHandler("globalout", onLeave);
  function useHandler(eventType, cb) {
    zr.on(eventType, function(e2) {
      var dis = makeDispatchAction(api);
      each5(inner15(zr).records, function(record) {
        record && cb(record, e2, dis.dispatchAction);
      });
      dispatchTooltipFinally(dis.pendings, api);
    });
  }
}
function dispatchTooltipFinally(pendings, api) {
  var showLen = pendings.showTip.length;
  var hideLen = pendings.hideTip.length;
  var actuallyPayload;
  if (showLen) {
    actuallyPayload = pendings.showTip[showLen - 1];
  } else if (hideLen) {
    actuallyPayload = pendings.hideTip[hideLen - 1];
  }
  if (actuallyPayload) {
    actuallyPayload.dispatchAction = null;
    api.dispatchAction(actuallyPayload);
  }
}
function onLeave(record, e2, dispatchAction3) {
  record.handler("leave", null, dispatchAction3);
}
function doEnter(currTrigger, record, e2, dispatchAction3) {
  record.handler(currTrigger, e2, dispatchAction3);
}
function makeDispatchAction(api) {
  var pendings = {
    showTip: [],
    hideTip: []
  };
  var dispatchAction3 = function(payload) {
    var pendingList = pendings[payload.type];
    if (pendingList) {
      pendingList.push(payload);
    } else {
      payload.dispatchAction = dispatchAction3;
      api.dispatchAction(payload);
    }
  };
  return {
    dispatchAction: dispatchAction3,
    pendings
  };
}
function unregister(key2, api) {
  if (env_default.node) {
    return;
  }
  var zr = api.getZr();
  var record = (inner15(zr).records || {})[key2];
  if (record) {
    inner15(zr).records[key2] = null;
  }
}

// node_modules/echarts/lib/component/axisPointer/AxisPointerView.js
var AxisPointerView = (
  /** @class */
  (function(_super) {
    __extends(AxisPointerView2, _super);
    function AxisPointerView2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = AxisPointerView2.type;
      return _this;
    }
    AxisPointerView2.prototype.render = function(globalAxisPointerModel, ecModel, api) {
      var globalTooltipModel = ecModel.getComponent("tooltip");
      var triggerOn = globalAxisPointerModel.get("triggerOn") || globalTooltipModel && globalTooltipModel.get("triggerOn") || "mousemove|click|mousewheel";
      register("axisPointer", api, function(currTrigger, e2, dispatchAction3) {
        if (triggerOn !== "none" && (currTrigger === "leave" || triggerOn.indexOf(currTrigger) >= 0)) {
          dispatchAction3({
            type: "updateAxisPointer",
            currTrigger,
            x: e2 && e2.offsetX,
            y: e2 && e2.offsetY
          });
        }
      });
    };
    AxisPointerView2.prototype.remove = function(ecModel, api) {
      unregister("axisPointer", api);
    };
    AxisPointerView2.prototype.dispose = function(ecModel, api) {
      unregister("axisPointer", api);
    };
    AxisPointerView2.type = "axisPointer";
    return AxisPointerView2;
  })(Component_default2)
);
var AxisPointerView_default = AxisPointerView;

// node_modules/echarts/lib/component/axisPointer/findPointFromSeries.js
function findPointFromSeries(finder, ecModel) {
  var point = [];
  var seriesIndex = finder.seriesIndex;
  var seriesModel;
  if (seriesIndex == null || !(seriesModel = ecModel.getSeriesByIndex(seriesIndex))) {
    return {
      point: []
    };
  }
  var data = seriesModel.getData();
  var dataIndex = queryDataIndex(data, finder);
  if (dataIndex == null || dataIndex < 0 || isArray(dataIndex)) {
    return {
      point: []
    };
  }
  var el = data.getItemGraphicEl(dataIndex);
  var coordSys = seriesModel.coordinateSystem;
  if (seriesModel.getTooltipPosition) {
    point = seriesModel.getTooltipPosition(dataIndex) || [];
  } else if (coordSys && coordSys.dataToPoint) {
    if (finder.isStacked) {
      var baseAxis = coordSys.getBaseAxis();
      var valueAxis2 = coordSys.getOtherAxis(baseAxis);
      var valueAxisDim = valueAxis2.dim;
      var baseAxisDim = baseAxis.dim;
      var baseDataOffset = valueAxisDim === "x" || valueAxisDim === "radius" ? 1 : 0;
      var baseDim = data.mapDimension(baseAxisDim);
      var stackedData = [];
      stackedData[baseDataOffset] = data.get(baseDim, dataIndex);
      stackedData[1 - baseDataOffset] = data.get(data.getCalculationInfo("stackResultDimension"), dataIndex);
      point = coordSys.dataToPoint(stackedData) || [];
    } else {
      point = coordSys.dataToPoint(data.getValues(map2(coordSys.dimensions, function(dim) {
        return data.mapDimension(dim);
      }), dataIndex)) || [];
    }
  } else if (el) {
    var rect = el.getBoundingRect().clone();
    rect.applyTransform(el.transform);
    point = [rect.x + rect.width / 2, rect.y + rect.height / 2];
  }
  return {
    point,
    el
  };
}

// node_modules/echarts/lib/component/axisPointer/axisTrigger.js
var inner16 = makeInner();
function axisTrigger(payload, ecModel, api) {
  var currTrigger = payload.currTrigger;
  var point = [payload.x, payload.y];
  var finder = payload;
  var dispatchAction3 = payload.dispatchAction || bind(api.dispatchAction, api);
  var coordSysAxesInfo = ecModel.getComponent("axisPointer").coordSysAxesInfo;
  if (!coordSysAxesInfo) {
    return;
  }
  if (illegalPoint(point)) {
    point = findPointFromSeries({
      seriesIndex: finder.seriesIndex,
      // Do not use dataIndexInside from other ec instance.
      // FIXME: auto detect it?
      dataIndex: finder.dataIndex
    }, ecModel).point;
  }
  var isIllegalPoint = illegalPoint(point);
  var inputAxesInfo = finder.axesInfo;
  var axesInfo = coordSysAxesInfo.axesInfo;
  var shouldHide = currTrigger === "leave" || illegalPoint(point);
  var outputPayload = {};
  var showValueMap = {};
  var dataByCoordSys = {
    list: [],
    map: {}
  };
  var updaters = {
    showPointer: curry(showPointer, showValueMap),
    showTooltip: curry(showTooltip, dataByCoordSys)
  };
  each(coordSysAxesInfo.coordSysMap, function(coordSys, coordSysKey) {
    var coordSysContainsPoint = isIllegalPoint || coordSys.containPoint(point);
    each(coordSysAxesInfo.coordSysAxesInfo[coordSysKey], function(axisInfo, key2) {
      var axis = axisInfo.axis;
      var inputAxisInfo = findInputAxisInfo(inputAxesInfo, axisInfo);
      if (!shouldHide && coordSysContainsPoint && (!inputAxesInfo || inputAxisInfo)) {
        var val = inputAxisInfo && inputAxisInfo.value;
        if (val == null && !isIllegalPoint) {
          val = axis.pointToData(point);
        }
        val != null && processOnAxis(axisInfo, val, updaters, false, outputPayload);
      }
    });
  });
  var linkTriggers = {};
  each(axesInfo, function(tarAxisInfo, tarKey) {
    var linkGroup = tarAxisInfo.linkGroup;
    if (linkGroup && !showValueMap[tarKey]) {
      each(linkGroup.axesInfo, function(srcAxisInfo, srcKey) {
        var srcValItem = showValueMap[srcKey];
        if (srcAxisInfo !== tarAxisInfo && srcValItem) {
          var val = srcValItem.value;
          linkGroup.mapper && (val = tarAxisInfo.axis.scale.parse(linkGroup.mapper(val, makeMapperParam(srcAxisInfo), makeMapperParam(tarAxisInfo))));
          linkTriggers[tarAxisInfo.key] = val;
        }
      });
    }
  });
  each(linkTriggers, function(val, tarKey) {
    processOnAxis(axesInfo[tarKey], val, updaters, true, outputPayload);
  });
  updateModelActually(showValueMap, axesInfo, outputPayload);
  dispatchTooltipActually(dataByCoordSys, point, payload, dispatchAction3);
  dispatchHighDownActually(axesInfo, dispatchAction3, api);
  return outputPayload;
}
function processOnAxis(axisInfo, newValue, updaters, noSnap, outputFinder) {
  var axis = axisInfo.axis;
  if (axis.scale.isBlank() || !axis.containData(newValue)) {
    return;
  }
  if (!axisInfo.involveSeries) {
    updaters.showPointer(axisInfo, newValue);
    return;
  }
  var payloadInfo = buildPayloadsBySeries(newValue, axisInfo);
  var payloadBatch = payloadInfo.payloadBatch;
  var snapToValue = payloadInfo.snapToValue;
  if (payloadBatch[0] && outputFinder.seriesIndex == null) {
    extend(outputFinder, payloadBatch[0]);
  }
  if (!noSnap && axisInfo.snap) {
    if (axis.containData(snapToValue) && snapToValue != null) {
      newValue = snapToValue;
    }
  }
  updaters.showPointer(axisInfo, newValue, payloadBatch);
  updaters.showTooltip(axisInfo, payloadInfo, snapToValue);
}
function buildPayloadsBySeries(value, axisInfo) {
  var axis = axisInfo.axis;
  var dim = axis.dim;
  var snapToValue = value;
  var payloadBatch = [];
  var minDist = Number.MAX_VALUE;
  var minDiff = -1;
  each(axisInfo.seriesModels, function(series, idx) {
    var dataDim = series.getData().mapDimensionsAll(dim);
    var seriesNestestValue;
    var dataIndices;
    if (series.getAxisTooltipData) {
      var result = series.getAxisTooltipData(dataDim, value, axis);
      dataIndices = result.dataIndices;
      seriesNestestValue = result.nestestValue;
    } else {
      dataIndices = series.indicesOfNearest(
        dim,
        dataDim[0],
        value,
        // Add a threshold to avoid find the wrong dataIndex
        // when data length is not same.
        // false,
        axis.type === "category" ? 0.5 : null
      );
      if (!dataIndices.length) {
        return;
      }
      seriesNestestValue = series.getData().get(dataDim[0], dataIndices[0]);
    }
    if (!isNullableNumberFinite(seriesNestestValue)) {
      return;
    }
    var diff = value - seriesNestestValue;
    var dist3 = Math.abs(diff);
    if (dist3 <= minDist) {
      if (dist3 < minDist || diff >= 0 && minDiff < 0) {
        minDist = dist3;
        minDiff = diff;
        snapToValue = seriesNestestValue;
        payloadBatch.length = 0;
      }
      each(dataIndices, function(dataIndex) {
        payloadBatch.push({
          seriesIndex: series.seriesIndex,
          dataIndexInside: dataIndex,
          dataIndex: series.getData().getRawIndex(dataIndex)
        });
      });
    }
  });
  return {
    payloadBatch,
    snapToValue
  };
}
function showPointer(showValueMap, axisInfo, value, payloadBatch) {
  showValueMap[axisInfo.key] = {
    value,
    payloadBatch
  };
}
function showTooltip(dataByCoordSys, axisInfo, payloadInfo, value) {
  var payloadBatch = payloadInfo.payloadBatch;
  var axis = axisInfo.axis;
  var axisModel = axis.model;
  var axisPointerModel = axisInfo.axisPointerModel;
  if (!axisInfo.triggerTooltip || !payloadBatch.length) {
    return;
  }
  var coordSysModel = axisInfo.coordSys.model;
  var coordSysKey = makeKey(coordSysModel);
  var coordSysItem = dataByCoordSys.map[coordSysKey];
  if (!coordSysItem) {
    coordSysItem = dataByCoordSys.map[coordSysKey] = {
      coordSysId: coordSysModel.id,
      coordSysIndex: coordSysModel.componentIndex,
      coordSysType: coordSysModel.type,
      coordSysMainType: coordSysModel.mainType,
      dataByAxis: []
    };
    dataByCoordSys.list.push(coordSysItem);
  }
  coordSysItem.dataByAxis.push({
    axisDim: axis.dim,
    axisIndex: axisModel.componentIndex,
    axisType: axisModel.type,
    axisId: axisModel.id,
    value,
    // Caution: viewHelper.getValueLabel is actually on "view stage", which
    // depends that all models have been updated. So it should not be performed
    // here. Considering axisPointerModel used here is volatile, which is hard
    // to be retrieve in TooltipView, we prepare parameters here.
    valueLabelOpt: {
      precision: axisPointerModel.get(["label", "precision"]),
      formatter: axisPointerModel.get(["label", "formatter"])
    },
    seriesDataIndices: payloadBatch.slice()
  });
}
function updateModelActually(showValueMap, axesInfo, outputPayload) {
  var outputAxesInfo = outputPayload.axesInfo = [];
  each(axesInfo, function(axisInfo, key2) {
    var option = axisInfo.axisPointerModel.option;
    var valItem = showValueMap[key2];
    if (valItem) {
      !axisInfo.useHandle && (option.status = "show");
      option.value = valItem.value;
      option.seriesDataIndices = (valItem.payloadBatch || []).slice();
    } else {
      !axisInfo.useHandle && (option.status = "hide");
    }
    option.status === "show" && outputAxesInfo.push({
      axisDim: axisInfo.axis.dim,
      axisIndex: axisInfo.axis.model.componentIndex,
      value: option.value
    });
  });
}
function dispatchTooltipActually(dataByCoordSys, point, payload, dispatchAction3) {
  if (illegalPoint(point) || !dataByCoordSys.list.length) {
    dispatchAction3({
      type: "hideTip"
    });
    return;
  }
  var sampleItem = ((dataByCoordSys.list[0].dataByAxis[0] || {}).seriesDataIndices || [])[0] || {};
  dispatchAction3({
    type: "showTip",
    escapeConnect: true,
    x: point[0],
    y: point[1],
    tooltipOption: payload.tooltipOption,
    position: payload.position,
    dataIndexInside: sampleItem.dataIndexInside,
    dataIndex: sampleItem.dataIndex,
    seriesIndex: sampleItem.seriesIndex,
    dataByCoordSys: dataByCoordSys.list
  });
}
function dispatchHighDownActually(axesInfo, dispatchAction3, api) {
  var zr = api.getZr();
  var highDownKey = "axisPointerLastHighlights";
  var lastHighlights = inner16(zr)[highDownKey] || {};
  var newHighlights = inner16(zr)[highDownKey] = {};
  each(axesInfo, function(axisInfo, key2) {
    var option = axisInfo.axisPointerModel.option;
    option.status === "show" && axisInfo.triggerEmphasis && each(option.seriesDataIndices, function(batchItem) {
      newHighlights[batchItem.seriesIndex + "|" + batchItem.dataIndex] = batchItem;
    });
  });
  var toHighlight = [];
  var toDownplay = [];
  function makeHighDownItem(batchItem) {
    return {
      seriesIndex: batchItem.seriesIndex,
      dataIndex: batchItem.dataIndex
    };
  }
  each(lastHighlights, function(batchItem, key2) {
    !newHighlights[key2] && toDownplay.push(makeHighDownItem(batchItem));
  });
  each(newHighlights, function(batchItem, key2) {
    !lastHighlights[key2] && toHighlight.push(makeHighDownItem(batchItem));
  });
  toDownplay.length && api.dispatchAction({
    type: "downplay",
    escapeConnect: true,
    // Not blur others when highlight in axisPointer.
    notBlur: true,
    batch: toDownplay
  });
  toHighlight.length && api.dispatchAction({
    type: "highlight",
    escapeConnect: true,
    // Not blur others when highlight in axisPointer.
    notBlur: true,
    batch: toHighlight
  });
}
function findInputAxisInfo(inputAxesInfo, axisInfo) {
  for (var i2 = 0; i2 < (inputAxesInfo || []).length; i2++) {
    var inputAxisInfo = inputAxesInfo[i2];
    if (axisInfo.axis.dim === inputAxisInfo.axisDim && axisInfo.axis.model.componentIndex === inputAxisInfo.axisIndex) {
      return inputAxisInfo;
    }
  }
}
function makeMapperParam(axisInfo) {
  var axisModel = axisInfo.axis.model;
  var item = {};
  var dim = item.axisDim = axisInfo.axis.dim;
  item.axisIndex = item[dim + "AxisIndex"] = axisModel.componentIndex;
  item.axisName = item[dim + "AxisName"] = axisModel.name;
  item.axisId = item[dim + "AxisId"] = axisModel.id;
  return item;
}
function illegalPoint(point) {
  return !point || point[0] == null || isNaN(point[0]) || point[1] == null || isNaN(point[1]);
}

// node_modules/echarts/lib/component/axisPointer/install.js
function install28(registers) {
  AxisView_default.registerAxisPointerClass("CartesianAxisPointer", CartesianAxisPointer_default);
  registers.registerComponentModel(AxisPointerModel_default);
  registers.registerComponentView(AxisPointerView_default);
  registers.registerPreprocessor(function(option) {
    if (option) {
      (!option.axisPointer || option.axisPointer.length === 0) && (option.axisPointer = {});
      var link = option.axisPointer.link;
      if (link && !isArray(link)) {
        option.axisPointer.link = [link];
      }
    }
  });
  registers.registerProcessor(registers.PRIORITY.PROCESSOR.STATISTIC, {
    overallReset: function(ecModel, api) {
      ecModel.getComponent("axisPointer").coordSysAxesInfo = collect(ecModel, api);
    }
  });
  registers.registerAction({
    type: "updateAxisPointer",
    event: "updateAxisPointer",
    update: ":updateAxisPointer"
  }, axisTrigger);
}

// node_modules/echarts/lib/component/grid/install.js
function install29(registers) {
  use(install4);
  use(install28);
}

// node_modules/echarts/lib/component/axisPointer/PolarAxisPointer.js
var PolarAxisPointer = (
  /** @class */
  (function(_super) {
    __extends(PolarAxisPointer2, _super);
    function PolarAxisPointer2() {
      return _super !== null && _super.apply(this, arguments) || this;
    }
    PolarAxisPointer2.prototype.makeElOption = function(elOption, value, axisModel, axisPointerModel, api) {
      var axis = axisModel.axis;
      if (axis.dim === "angle") {
        this.animationThreshold = Math.PI / 18;
      }
      var polar = axis.polar;
      var thisExtent = axis.getExtent();
      var otherExtent = polar.getOtherAxis(axis).getExtent();
      var coordValue = axis.dataToCoord(value);
      var axisPointerType = axisPointerModel.get("type");
      if (axisPointerType && axisPointerType !== "none") {
        var elStyle = buildElStyle(axisPointerModel);
        var pointerOption = pointerShapeBuilder2[axisPointerType](axis, polar, coordValue, thisExtent, otherExtent, axisPointerModel.get("seriesDataIndices"), axisPointerModel.ecModel);
        pointerOption.style = elStyle;
        elOption.graphicKey = pointerOption.type;
        elOption.pointer = pointerOption;
      }
      var labelMargin = axisPointerModel.get(["label", "margin"]);
      var labelPos = getLabelPosition(value, axisModel, axisPointerModel, polar, labelMargin);
      buildLabelElOption(elOption, axisModel, axisPointerModel, api, labelPos);
    };
    return PolarAxisPointer2;
  })(BaseAxisPointer_default)
);
function getLabelPosition(value, axisModel, axisPointerModel, polar, labelMargin) {
  var axis = axisModel.axis;
  var coord = axis.dataToCoord(value);
  var axisAngle = polar.getAngleAxis().getExtent()[0];
  axisAngle = axisAngle / 180 * Math.PI;
  var radiusExtent = polar.getRadiusAxis().getExtent();
  var position;
  var align;
  var verticalAlign;
  if (axis.dim === "radius") {
    var transform = create();
    rotate(transform, transform, axisAngle);
    translate(transform, transform, [polar.cx, polar.cy]);
    position = applyTransform2([coord, -labelMargin], transform);
    var labelRotation = axisModel.getModel("axisLabel").get("rotate") || 0;
    var labelLayout2 = AxisBuilder_default.innerTextLayout(axisAngle, labelRotation * Math.PI / 180, -1);
    align = labelLayout2.textAlign;
    verticalAlign = labelLayout2.textVerticalAlign;
  } else {
    var r = radiusExtent[1];
    position = polar.coordToPoint([r + labelMargin, coord]);
    var cx = polar.cx;
    var cy = polar.cy;
    align = Math.abs(position[0] - cx) / r < 0.3 ? "center" : position[0] > cx ? "left" : "right";
    verticalAlign = Math.abs(position[1] - cy) / r < 0.3 ? "middle" : position[1] > cy ? "top" : "bottom";
  }
  return {
    position,
    align,
    verticalAlign
  };
}
var pointerShapeBuilder2 = {
  line: function(axis, polar, coordValue, thisExtent, otherExtent) {
    return axis.dim === "angle" ? {
      type: "Line",
      shape: makeLineShape(polar.coordToPoint([otherExtent[0], coordValue]), polar.coordToPoint([otherExtent[1], coordValue]))
    } : {
      type: "Circle",
      shape: {
        cx: polar.cx,
        cy: polar.cy,
        r: coordValue
      }
    };
  },
  shadow: function(axis, polar, coordValue, thisExtent, otherExtent, seriesDataIndices, ecModel) {
    var radian = Math.PI / 180;
    var bandWidth = calcAxisPointerShadowBandWidth(axis, seriesDataIndices, ecModel);
    var shape;
    if (axis.dim === "angle") {
      shape = makeSectorShape(
        polar.cx,
        polar.cy,
        otherExtent[0],
        otherExtent[1],
        // In ECharts the screen y is negative if angle is positive,
        // opposite to zrender shape.
        // No need clamp.
        (-coordValue - bandWidth / 2) * radian,
        (-coordValue + bandWidth / 2) * radian
      );
    } else {
      var _a2 = calcAxisPointerShadowEnds(coordValue, thisExtent, bandWidth), min3 = _a2[0], max3 = _a2[1];
      shape = makeSectorShape(polar.cx, polar.cy, min3, max3, 0, Math.PI * 2);
    }
    return {
      type: "Sector",
      shape
    };
  }
};

// node_modules/echarts/lib/coord/polar/PolarModel.js
var COORD_SYS_TYPE_POLAR = "polar";
var PolarModel = (
  /** @class */
  (function(_super) {
    __extends(PolarModel2, _super);
    function PolarModel2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = PolarModel2.type;
      return _this;
    }
    PolarModel2.prototype.findAxisModel = function(axisType) {
      var foundAxisModel;
      var ecModel = this.ecModel;
      ecModel.eachComponent(axisType, function(axisModel) {
        if (axisModel.getCoordSysModel() === this) {
          foundAxisModel = axisModel;
        }
      }, this);
      return foundAxisModel;
    };
    PolarModel2.type = COORD_SYS_TYPE_POLAR;
    PolarModel2.dependencies = ["radiusAxis", "angleAxis"];
    PolarModel2.defaultOption = {
      // zlevel: 0,
      z: 0,
      center: ["50%", "50%"],
      radius: "80%"
    };
    return PolarModel2;
  })(Component_default)
);

// node_modules/echarts/lib/coord/polar/AxisModel.js
var PolarAxisModel = (
  /** @class */
  (function(_super) {
    __extends(PolarAxisModel2, _super);
    function PolarAxisModel2() {
      return _super !== null && _super.apply(this, arguments) || this;
    }
    PolarAxisModel2.prototype.getCoordSysModel = function() {
      return this.getReferringComponents("polar", SINGLE_REFERRING).models[0];
    };
    PolarAxisModel2.type = "polarAxis";
    return PolarAxisModel2;
  })(Component_default)
);
mixin(PolarAxisModel, AxisModelCommonMixin);
var AngleAxisModel = (
  /** @class */
  (function(_super) {
    __extends(AngleAxisModel2, _super);
    function AngleAxisModel2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = AngleAxisModel2.type;
      return _this;
    }
    AngleAxisModel2.type = "angleAxis";
    return AngleAxisModel2;
  })(PolarAxisModel)
);
var RadiusAxisModel = (
  /** @class */
  (function(_super) {
    __extends(RadiusAxisModel2, _super);
    function RadiusAxisModel2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = RadiusAxisModel2.type;
      return _this;
    }
    RadiusAxisModel2.type = "radiusAxis";
    return RadiusAxisModel2;
  })(PolarAxisModel)
);

// node_modules/echarts/lib/coord/polar/RadiusAxis.js
var RadiusAxis = (
  /** @class */
  (function(_super) {
    __extends(RadiusAxis2, _super);
    function RadiusAxis2(scale3, radiusExtent) {
      return _super.call(this, "radius", scale3, radiusExtent) || this;
    }
    RadiusAxis2.prototype.pointToData = function(point, clamp2) {
      return this.polar.pointToData(point, clamp2)[this.dim === "radius" ? 0 : 1];
    };
    return RadiusAxis2;
  })(Axis_default)
);
RadiusAxis.prototype.dataToRadius = Axis_default.prototype.dataToCoord;
RadiusAxis.prototype.radiusToData = Axis_default.prototype.coordToData;
var RadiusAxis_default = RadiusAxis;

// node_modules/echarts/lib/coord/polar/AngleAxis.js
var inner17 = makeInner();
var AngleAxis = (
  /** @class */
  (function(_super) {
    __extends(AngleAxis2, _super);
    function AngleAxis2(scale3, angleExtent) {
      return _super.call(this, "angle", scale3, angleExtent || [0, 360]) || this;
    }
    AngleAxis2.prototype.pointToData = function(point, clamp2) {
      return this.polar.pointToData(point, clamp2)[this.dim === "radius" ? 0 : 1];
    };
    AngleAxis2.prototype.calculateCategoryInterval = function() {
      var axis = this;
      var labelModel = axis.getLabelModel();
      var ordinalScale = axis.scale;
      var ordinalExtent = ordinalScale.getExtent();
      var tickCount = ordinalScale.count();
      if (ordinalExtent[1] - ordinalExtent[0] < 1) {
        return 0;
      }
      var tickValue = ordinalExtent[0];
      var unitSpan = axis.dataToCoord(tickValue + 1) - axis.dataToCoord(tickValue);
      var unitH = Math.abs(unitSpan);
      var rect = getBoundingRect(tickValue == null ? "" : tickValue + "", labelModel.getFont(), "center", "top");
      var maxH = Math.max(rect.height, 7);
      var dh = maxH / unitH;
      isNaN(dh) && (dh = Infinity);
      var interval2 = Math.max(0, Math.floor(dh));
      var cache = inner17(axis.model);
      var lastAutoInterval = cache.lastAutoInterval;
      var lastTickCount = cache.lastTickCount;
      if (lastAutoInterval != null && lastTickCount != null && Math.abs(lastAutoInterval - interval2) <= 1 && Math.abs(lastTickCount - tickCount) <= 1 && lastAutoInterval > interval2) {
        interval2 = lastAutoInterval;
      } else {
        cache.lastTickCount = tickCount;
        cache.lastAutoInterval = interval2;
      }
      return interval2;
    };
    return AngleAxis2;
  })(Axis_default)
);
AngleAxis.prototype.dataToAngle = Axis_default.prototype.dataToCoord;
AngleAxis.prototype.angleToData = Axis_default.prototype.coordToData;
var AngleAxis_default = AngleAxis;

// node_modules/echarts/lib/coord/polar/Polar.js
var polarDimensions = ["radius", "angle"];
var Polar = (
  /** @class */
  (function() {
    function Polar2(name) {
      this.dimensions = polarDimensions;
      this.type = COORD_SYS_TYPE_POLAR;
      this.cx = 0;
      this.cy = 0;
      this._radiusAxis = new RadiusAxis_default();
      this._angleAxis = new AngleAxis_default();
      this.axisPointerEnabled = true;
      this.name = name || "";
      this._radiusAxis.polar = this._angleAxis.polar = this;
    }
    Polar2.prototype.containPoint = function(point) {
      var coord = this.pointToCoord(point);
      return this._radiusAxis.contain(coord[0]) && this._angleAxis.contain(coord[1]);
    };
    Polar2.prototype.containData = function(data) {
      return this._radiusAxis.containData(data[0]) && this._angleAxis.containData(data[1]);
    };
    Polar2.prototype.getAxis = function(dim) {
      var key2 = "_" + dim + "Axis";
      return this[key2];
    };
    Polar2.prototype.getAxes = function() {
      return [this._radiusAxis, this._angleAxis];
    };
    Polar2.prototype.getAxesByScale = function(scaleType) {
      var axes = [];
      var angleAxis = this._angleAxis;
      var radiusAxis = this._radiusAxis;
      angleAxis.scale.type === scaleType && axes.push(angleAxis);
      radiusAxis.scale.type === scaleType && axes.push(radiusAxis);
      return axes;
    };
    Polar2.prototype.getAngleAxis = function() {
      return this._angleAxis;
    };
    Polar2.prototype.getRadiusAxis = function() {
      return this._radiusAxis;
    };
    Polar2.prototype.getOtherAxis = function(axis) {
      var angleAxis = this._angleAxis;
      return axis === angleAxis ? this._radiusAxis : angleAxis;
    };
    Polar2.prototype.getBaseAxis = function() {
      return this.getAxesByScale("ordinal")[0] || this.getAxesByScale("time")[0] || this.getAngleAxis();
    };
    Polar2.prototype.getTooltipAxes = function(dim) {
      var baseAxis = dim != null && dim !== "auto" ? this.getAxis(dim) : this.getBaseAxis();
      return {
        baseAxes: [baseAxis],
        otherAxes: [this.getOtherAxis(baseAxis)]
      };
    };
    Polar2.prototype.dataToPoint = function(data, clamp2, out2) {
      return this.coordToPoint([
        // Must be the same order as polarDimensions
        this._radiusAxis.dataToRadius(data[0], clamp2),
        this._angleAxis.dataToAngle(data[1], clamp2)
      ], out2);
    };
    Polar2.prototype.pointToData = function(point, clamp2, out2) {
      out2 = out2 || [];
      var coord = this.pointToCoord(point);
      out2[0] = this._radiusAxis.radiusToData(coord[0], clamp2);
      out2[1] = this._angleAxis.angleToData(coord[1], clamp2);
      return out2;
    };
    Polar2.prototype.pointToCoord = function(point) {
      var dx = point[0] - this.cx;
      var dy = point[1] - this.cy;
      var angleAxis = this.getAngleAxis();
      var extent = angleAxis.getExtent();
      var minAngle = Math.min(extent[0], extent[1]);
      var maxAngle = Math.max(extent[0], extent[1]);
      angleAxis.inverse ? minAngle = maxAngle - 360 : maxAngle = minAngle + 360;
      var radius = Math.sqrt(dx * dx + dy * dy);
      dx /= radius;
      dy /= radius;
      var radian = Math.atan2(-dy, dx) / Math.PI * 180;
      var dir3 = radian < minAngle ? 1 : -1;
      while (radian < minAngle || radian > maxAngle) {
        radian += dir3 * 360;
      }
      return [radius, radian];
    };
    Polar2.prototype.coordToPoint = function(coord, out2) {
      out2 = out2 || [];
      var radius = coord[0];
      var radian = coord[1] / 180 * Math.PI;
      out2[0] = Math.cos(radian) * radius + this.cx;
      out2[1] = -Math.sin(radian) * radius + this.cy;
      return out2;
    };
    Polar2.prototype.getArea = function() {
      var angleAxis = this.getAngleAxis();
      var radiusAxis = this.getRadiusAxis();
      var radiusExtent = radiusAxis.getExtent().slice();
      radiusExtent[0] > radiusExtent[1] && radiusExtent.reverse();
      var angleExtent = angleAxis.getExtent();
      var RADIAN6 = Math.PI / 180;
      var EPSILON7 = 1e-4;
      return {
        cx: this.cx,
        cy: this.cy,
        r0: radiusExtent[0],
        r: radiusExtent[1],
        startAngle: -angleExtent[0] * RADIAN6,
        endAngle: -angleExtent[1] * RADIAN6,
        clockwise: angleAxis.inverse,
        contain: function(x, y) {
          var dx = x - this.cx;
          var dy = y - this.cy;
          var d2 = dx * dx + dy * dy;
          var r = this.r;
          var r0 = this.r0;
          return r !== r0 && d2 - EPSILON7 <= r * r && d2 + EPSILON7 >= r0 * r0;
        },
        // As the bounding box
        x: this.cx - radiusExtent[1],
        y: this.cy - radiusExtent[1],
        width: radiusExtent[1] * 2,
        height: radiusExtent[1] * 2
      };
    };
    Polar2.prototype.convertToPixel = function(ecModel, finder, value) {
      var coordSys = getCoordSys3(finder);
      return coordSys === this ? this.dataToPoint(value) : null;
    };
    Polar2.prototype.convertFromPixel = function(ecModel, finder, pixel) {
      var coordSys = getCoordSys3(finder);
      return coordSys === this ? this.pointToData(pixel) : null;
    };
    return Polar2;
  })()
);
function getCoordSys3(finder) {
  var seriesModel = finder.seriesModel;
  var polarModel = finder.polarModel;
  return polarModel && polarModel.coordinateSystem || seriesModel && seriesModel.coordinateSystem;
}

// node_modules/echarts/lib/component/axis/AngleAxisView.js
var elementList = ["axisLine", "axisLabel", "axisTick", "minorTick", "splitLine", "minorSplitLine", "splitArea"];
function getAxisLineShape(polar, rExtent, angle) {
  rExtent[1] > rExtent[0] && (rExtent = rExtent.slice().reverse());
  var start2 = polar.coordToPoint([rExtent[0], angle]);
  var end2 = polar.coordToPoint([rExtent[1], angle]);
  return {
    x1: start2[0],
    y1: start2[1],
    x2: end2[0],
    y2: end2[1]
  };
}
function getRadiusIdx(polar) {
  var radiusAxis = polar.getRadiusAxis();
  return radiusAxis.inverse ? 0 : 1;
}
function fixAngleOverlap(list) {
  var firstItem = list[0];
  var lastItem = list[list.length - 1];
  if (firstItem && lastItem && Math.abs(Math.abs(firstItem.coord - lastItem.coord) - 360) < 1e-4) {
    list.pop();
  }
}
var AngleAxisView = (
  /** @class */
  (function(_super) {
    __extends(AngleAxisView2, _super);
    function AngleAxisView2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = AngleAxisView2.type;
      _this.axisPointerClass = "PolarAxisPointer";
      return _this;
    }
    AngleAxisView2.prototype.render = function(angleAxisModel, ecModel) {
      this.group.removeAll();
      if (!angleAxisModel.get("show")) {
        return;
      }
      var angleAxis = angleAxisModel.axis;
      var polar = angleAxis.polar;
      var radiusExtent = polar.getRadiusAxis().getExtent();
      var ticksAngles = angleAxis.getTicksCoords({
        breakTicks: "none"
      });
      var minorTickAngles = angleAxis.getMinorTicksCoords();
      var labels = [];
      each(angleAxis.getViewLabels(), function(labelItem) {
        if (labelItem.tick.offInterval) {
          return;
        }
        labelItem = clone(labelItem);
        var scale3 = angleAxis.scale;
        labelItem.coord = angleAxis.dataToCoord(getTickValueOutermost(scale3, labelItem.tick));
        labels.push(labelItem);
      });
      fixAngleOverlap(labels);
      fixAngleOverlap(ticksAngles);
      each(elementList, function(name) {
        if (angleAxisModel.get([name, "show"]) && (!angleAxis.scale.isBlank() || name === "axisLine")) {
          angelAxisElementsBuilders[name](this.group, angleAxisModel, polar, ticksAngles, minorTickAngles, radiusExtent, labels);
        }
      }, this);
    };
    AngleAxisView2.type = "angleAxis";
    return AngleAxisView2;
  })(AxisView_default)
);
var angelAxisElementsBuilders = {
  axisLine: function(group2, angleAxisModel, polar, ticksAngles, minorTickAngles, radiusExtent) {
    var lineStyleModel = angleAxisModel.getModel(["axisLine", "lineStyle"]);
    var angleAxis = polar.getAngleAxis();
    var RADIAN6 = Math.PI / 180;
    var angleExtent = angleAxis.getExtent();
    var rId = getRadiusIdx(polar);
    var r0Id = rId ? 0 : 1;
    var shape;
    var shapeType = Math.abs(angleExtent[1] - angleExtent[0]) === 360 ? "Circle" : "Arc";
    if (radiusExtent[r0Id] === 0) {
      shape = new graphic_exports[shapeType]({
        shape: {
          cx: polar.cx,
          cy: polar.cy,
          r: radiusExtent[rId],
          startAngle: -angleExtent[0] * RADIAN6,
          endAngle: -angleExtent[1] * RADIAN6,
          clockwise: angleAxis.inverse
        },
        style: lineStyleModel.getLineStyle(),
        z2: 1,
        silent: true
      });
    } else {
      shape = new Ring_default({
        shape: {
          cx: polar.cx,
          cy: polar.cy,
          r: radiusExtent[rId],
          r0: radiusExtent[r0Id]
        },
        style: lineStyleModel.getLineStyle(),
        z2: 1,
        silent: true
      });
    }
    shape.style.fill = null;
    group2.add(shape);
  },
  axisTick: function(group2, angleAxisModel, polar, ticksAngles, minorTickAngles, radiusExtent) {
    var tickModel = angleAxisModel.getModel("axisTick");
    var tickLen = (tickModel.get("inside") ? -1 : 1) * tickModel.get("length");
    var radius = radiusExtent[getRadiusIdx(polar)];
    var lines = map2(ticksAngles, function(tickAngleItem) {
      return new Line_default({
        shape: getAxisLineShape(polar, [radius, radius + tickLen], tickAngleItem.coord)
      });
    });
    group2.add(mergePath2(lines, {
      style: defaults(tickModel.getModel("lineStyle").getLineStyle(), {
        stroke: angleAxisModel.get(["axisLine", "lineStyle", "color"])
      })
    }));
  },
  minorTick: function(group2, angleAxisModel, polar, tickAngles, minorTickAngles, radiusExtent) {
    if (!minorTickAngles.length) {
      return;
    }
    var tickModel = angleAxisModel.getModel("axisTick");
    var minorTickModel = angleAxisModel.getModel("minorTick");
    var tickLen = (tickModel.get("inside") ? -1 : 1) * minorTickModel.get("length");
    var radius = radiusExtent[getRadiusIdx(polar)];
    var lines = [];
    for (var i2 = 0; i2 < minorTickAngles.length; i2++) {
      for (var k2 = 0; k2 < minorTickAngles[i2].length; k2++) {
        lines.push(new Line_default({
          shape: getAxisLineShape(polar, [radius, radius + tickLen], minorTickAngles[i2][k2].coord)
        }));
      }
    }
    group2.add(mergePath2(lines, {
      style: defaults(minorTickModel.getModel("lineStyle").getLineStyle(), defaults(tickModel.getLineStyle(), {
        stroke: angleAxisModel.get(["axisLine", "lineStyle", "color"])
      }))
    }));
  },
  axisLabel: function(group2, angleAxisModel, polar, ticksAngles, minorTickAngles, radiusExtent, labels) {
    var rawCategoryData = angleAxisModel.getCategories(true);
    var commonLabelModel = angleAxisModel.getModel("axisLabel");
    var labelMargin = commonLabelModel.get("margin");
    var triggerEvent = angleAxisModel.get("triggerEvent");
    each(labels, function(labelItem, idx) {
      var labelModel = commonLabelModel;
      var tickValue = labelItem.tick.value;
      var r = radiusExtent[getRadiusIdx(polar)];
      var p = polar.coordToPoint([r + labelMargin, labelItem.coord]);
      var cx = polar.cx;
      var cy = polar.cy;
      var labelTextAlign = Math.abs(p[0] - cx) / r < 0.3 ? "center" : p[0] > cx ? "left" : "right";
      var labelTextVerticalAlign = Math.abs(p[1] - cy) / r < 0.3 ? "middle" : p[1] > cy ? "top" : "bottom";
      if (rawCategoryData && rawCategoryData[tickValue]) {
        var rawCategoryItem = rawCategoryData[tickValue];
        if (isObject(rawCategoryItem) && rawCategoryItem.textStyle) {
          labelModel = new Model_default(rawCategoryItem.textStyle, commonLabelModel, commonLabelModel.ecModel);
        }
      }
      var textEl = new Text_default({
        silent: AxisBuilder_default.isLabelSilent(angleAxisModel),
        style: createTextStyle(labelModel, {
          x: p[0],
          y: p[1],
          fill: labelModel.getTextColor() || angleAxisModel.get(["axisLine", "lineStyle", "color"]),
          text: labelItem.formattedLabel,
          align: labelTextAlign,
          verticalAlign: labelTextVerticalAlign
        })
      });
      group2.add(textEl);
      setTooltipConfig({
        el: textEl,
        componentModel: angleAxisModel,
        itemName: labelItem.formattedLabel,
        formatterParamsExtra: {
          isTruncated: function() {
            return textEl.isTruncated;
          },
          value: labelItem.rawLabel,
          tickIndex: idx
        }
      });
      if (triggerEvent) {
        var eventData = AxisBuilder_default.makeAxisEventDataBase(angleAxisModel);
        eventData.targetType = "axisLabel";
        eventData.value = labelItem.rawLabel;
        getECData(textEl).eventData = eventData;
      }
    }, this);
  },
  splitLine: function(group2, angleAxisModel, polar, ticksAngles, minorTickAngles, radiusExtent) {
    var splitLineModel = angleAxisModel.getModel("splitLine");
    var lineStyleModel = splitLineModel.getModel("lineStyle");
    var lineColors = lineStyleModel.get("color");
    var lineCount = 0;
    lineColors = lineColors instanceof Array ? lineColors : [lineColors];
    var splitLines = [];
    for (var i2 = 0; i2 < ticksAngles.length; i2++) {
      var colorIndex = lineCount++ % lineColors.length;
      splitLines[colorIndex] = splitLines[colorIndex] || [];
      splitLines[colorIndex].push(new Line_default({
        shape: getAxisLineShape(polar, radiusExtent, ticksAngles[i2].coord)
      }));
    }
    for (var i2 = 0; i2 < splitLines.length; i2++) {
      group2.add(mergePath2(splitLines[i2], {
        style: defaults({
          stroke: lineColors[i2 % lineColors.length]
        }, lineStyleModel.getLineStyle()),
        silent: true,
        z: angleAxisModel.get("z")
      }));
    }
  },
  minorSplitLine: function(group2, angleAxisModel, polar, ticksAngles, minorTickAngles, radiusExtent) {
    if (!minorTickAngles.length) {
      return;
    }
    var minorSplitLineModel = angleAxisModel.getModel("minorSplitLine");
    var lineStyleModel = minorSplitLineModel.getModel("lineStyle");
    var lines = [];
    for (var i2 = 0; i2 < minorTickAngles.length; i2++) {
      for (var k2 = 0; k2 < minorTickAngles[i2].length; k2++) {
        lines.push(new Line_default({
          shape: getAxisLineShape(polar, radiusExtent, minorTickAngles[i2][k2].coord)
        }));
      }
    }
    group2.add(mergePath2(lines, {
      style: lineStyleModel.getLineStyle(),
      silent: true,
      z: angleAxisModel.get("z")
    }));
  },
  splitArea: function(group2, angleAxisModel, polar, ticksAngles, minorTickAngles, radiusExtent) {
    if (!ticksAngles.length) {
      return;
    }
    var splitAreaModel = angleAxisModel.getModel("splitArea");
    var areaStyleModel = splitAreaModel.getModel("areaStyle");
    var areaColors = areaStyleModel.get("color");
    var lineCount = 0;
    areaColors = areaColors instanceof Array ? areaColors : [areaColors];
    var splitAreas = [];
    var RADIAN6 = Math.PI / 180;
    var prevAngle = -ticksAngles[0].coord * RADIAN6;
    var r0 = Math.min(radiusExtent[0], radiusExtent[1]);
    var r1 = Math.max(radiusExtent[0], radiusExtent[1]);
    var clockwise = angleAxisModel.get("clockwise");
    for (var i2 = 1, len2 = ticksAngles.length; i2 <= len2; i2++) {
      var coord = i2 === len2 ? ticksAngles[0].coord : ticksAngles[i2].coord;
      var colorIndex = lineCount++ % areaColors.length;
      splitAreas[colorIndex] = splitAreas[colorIndex] || [];
      splitAreas[colorIndex].push(new Sector_default({
        shape: {
          cx: polar.cx,
          cy: polar.cy,
          r0,
          r: r1,
          startAngle: prevAngle,
          endAngle: -coord * RADIAN6,
          clockwise
        },
        silent: true
      }));
      prevAngle = -coord * RADIAN6;
    }
    for (var i2 = 0; i2 < splitAreas.length; i2++) {
      group2.add(mergePath2(splitAreas[i2], {
        style: defaults({
          fill: areaColors[i2 % areaColors.length]
        }, areaStyleModel.getAreaStyle()),
        silent: true
      }));
    }
  }
};

// node_modules/echarts/lib/component/axis/RadiusAxisView.js
var selfBuilderAttrs2 = ["splitLine", "splitArea", "minorSplitLine"];
var RadiusAxisView = (
  /** @class */
  (function(_super) {
    __extends(RadiusAxisView2, _super);
    function RadiusAxisView2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = RadiusAxisView2.type;
      _this.axisPointerClass = "PolarAxisPointer";
      return _this;
    }
    RadiusAxisView2.prototype.render = function(radiusAxisModel, ecModel, api) {
      this.group.removeAll();
      if (!radiusAxisModel.get("show")) {
        return;
      }
      var oldAxisGroup = this._axisGroup;
      var newAxisGroup = this._axisGroup = new Group_default();
      this.group.add(newAxisGroup);
      var radiusAxis = radiusAxisModel.axis;
      var polar = radiusAxis.polar;
      var angleAxis = polar.getAngleAxis();
      var ticksCoords = radiusAxis.getTicksCoords();
      var minorTicksCoords = radiusAxis.getMinorTicksCoords();
      var axisAngle = angleAxis.getExtent()[0];
      var radiusExtent = radiusAxis.getExtent();
      var layout3 = layoutAxis(polar, radiusAxisModel, axisAngle);
      var axisBuilder = new AxisBuilder_default(radiusAxisModel, api, layout3);
      axisBuilder.build();
      newAxisGroup.add(axisBuilder.group);
      groupTransition(oldAxisGroup, newAxisGroup, radiusAxisModel);
      each(selfBuilderAttrs2, function(name) {
        if (radiusAxisModel.get([name, "show"]) && !radiusAxis.scale.isBlank()) {
          axisElementBuilders2[name](this.group, radiusAxisModel, polar, axisAngle, radiusExtent, ticksCoords, minorTicksCoords);
        }
      }, this);
    };
    RadiusAxisView2.type = "radiusAxis";
    return RadiusAxisView2;
  })(AxisView_default)
);
var axisElementBuilders2 = {
  splitLine: function(group2, radiusAxisModel, polar, axisAngle, radiusExtent, ticksCoords) {
    var splitLineModel = radiusAxisModel.getModel("splitLine");
    var lineStyleModel = splitLineModel.getModel("lineStyle");
    var lineColors = lineStyleModel.get("color");
    var lineCount = 0;
    var angleAxis = polar.getAngleAxis();
    var RADIAN6 = Math.PI / 180;
    var angleExtent = angleAxis.getExtent();
    var shapeType = Math.abs(angleExtent[1] - angleExtent[0]) === 360 ? "Circle" : "Arc";
    lineColors = lineColors instanceof Array ? lineColors : [lineColors];
    var splitLines = [];
    for (var i2 = 0; i2 < ticksCoords.length; i2++) {
      var colorIndex = lineCount++ % lineColors.length;
      splitLines[colorIndex] = splitLines[colorIndex] || [];
      splitLines[colorIndex].push(new graphic_exports[shapeType]({
        shape: {
          cx: polar.cx,
          cy: polar.cy,
          // ensure circle radius >= 0
          r: Math.max(ticksCoords[i2].coord, 0),
          startAngle: -angleExtent[0] * RADIAN6,
          endAngle: -angleExtent[1] * RADIAN6,
          clockwise: angleAxis.inverse
        }
      }));
    }
    for (var i2 = 0; i2 < splitLines.length; i2++) {
      group2.add(mergePath2(splitLines[i2], {
        style: defaults({
          stroke: lineColors[i2 % lineColors.length],
          fill: null
        }, lineStyleModel.getLineStyle()),
        silent: true
      }));
    }
  },
  minorSplitLine: function(group2, radiusAxisModel, polar, axisAngle, radiusExtent, ticksCoords, minorTicksCoords) {
    if (!minorTicksCoords.length) {
      return;
    }
    var minorSplitLineModel = radiusAxisModel.getModel("minorSplitLine");
    var lineStyleModel = minorSplitLineModel.getModel("lineStyle");
    var lines = [];
    for (var i2 = 0; i2 < minorTicksCoords.length; i2++) {
      for (var k2 = 0; k2 < minorTicksCoords[i2].length; k2++) {
        lines.push(new Circle_default({
          shape: {
            cx: polar.cx,
            cy: polar.cy,
            r: minorTicksCoords[i2][k2].coord
          }
        }));
      }
    }
    group2.add(mergePath2(lines, {
      style: defaults({
        fill: null
      }, lineStyleModel.getLineStyle()),
      silent: true
    }));
  },
  splitArea: function(group2, radiusAxisModel, polar, axisAngle, radiusExtent, ticksCoords) {
    if (!ticksCoords.length) {
      return;
    }
    var splitAreaModel = radiusAxisModel.getModel("splitArea");
    var areaStyleModel = splitAreaModel.getModel("areaStyle");
    var areaColors = areaStyleModel.get("color");
    var lineCount = 0;
    areaColors = areaColors instanceof Array ? areaColors : [areaColors];
    var splitAreas = [];
    var prevRadius = ticksCoords[0].coord;
    for (var i2 = 1; i2 < ticksCoords.length; i2++) {
      var colorIndex = lineCount++ % areaColors.length;
      splitAreas[colorIndex] = splitAreas[colorIndex] || [];
      splitAreas[colorIndex].push(new Sector_default({
        shape: {
          cx: polar.cx,
          cy: polar.cy,
          r0: prevRadius,
          r: ticksCoords[i2].coord,
          startAngle: 0,
          endAngle: Math.PI * 2
        },
        silent: true
      }));
      prevRadius = ticksCoords[i2].coord;
    }
    for (var i2 = 0; i2 < splitAreas.length; i2++) {
      group2.add(mergePath2(splitAreas[i2], {
        style: defaults({
          fill: areaColors[i2 % areaColors.length]
        }, areaStyleModel.getAreaStyle()),
        silent: true
      }));
    }
  }
};
function layoutAxis(polar, radiusAxisModel, axisAngle) {
  return {
    position: [polar.cx, polar.cy],
    rotation: axisAngle / 180 * Math.PI,
    labelDirection: -1,
    tickDirection: -1,
    nameDirection: 1,
    labelRotate: radiusAxisModel.getModel("axisLabel").get("rotate"),
    // Over splitLine and splitArea
    z2: 1
  };
}

// node_modules/echarts/lib/layout/barPolar.js
var callOnlyOnce5 = makeCallOnlyOnce();
function getSeriesStackId2(seriesModel) {
  return seriesModel.get("stack") || "__ec_stack_" + seriesModel.seriesIndex;
}
var barLayoutPolarStageHandler = createSimpleOverallStageHandler(SERIES_TYPE_BAR, barLayoutPolar);
function barLayoutPolar(ecModel) {
  var axisStatKey = makeAxisStatKey2(SERIES_TYPE_BAR, COORD_SYS_TYPE_POLAR);
  eachAxisOnKey(ecModel, axisStatKey, function(axis) {
    if (true) {
      assert(axis instanceof AngleAxis_default || axis instanceof RadiusAxis_default);
    }
    var barWidthAndOffset = calcRadialBar(axis, SERIES_TYPE_BAR);
    var lastStackCoords = {};
    eachSeriesOnAxisOnKey(axis, axisStatKey, function(seriesModel) {
      layoutPerAxisPerSeries(axis, seriesModel, barWidthAndOffset, lastStackCoords);
    });
  });
}
function layoutPerAxisPerSeries(baseAxis, seriesModel, barWidthAndOffset, lastStackCoords) {
  var data = seriesModel.getData();
  var stackId = getSeriesStackId2(seriesModel);
  var columnLayoutInfo = barWidthAndOffset[stackId];
  var columnOffset = columnLayoutInfo.offset;
  var columnWidth = columnLayoutInfo.width;
  var polar = seriesModel.coordinateSystem;
  if (true) {
    assert(polar.type === COORD_SYS_TYPE_POLAR);
  }
  var valueAxis2 = polar.getOtherAxis(baseAxis);
  var cx = polar.cx;
  var cy = polar.cy;
  var barMinHeight = seriesModel.get("barMinHeight") || 0;
  var barMinAngle = seriesModel.get("barMinAngle") || 0;
  lastStackCoords[stackId] = lastStackCoords[stackId] || [];
  var valueDim = data.mapDimension(valueAxis2.dim);
  var baseDim = data.mapDimension(baseAxis.dim);
  var stacked = isDimensionStacked(
    data,
    valueDim
    /* , baseDim */
  );
  var clampLayout = baseAxis.dim !== "radius" || !seriesModel.get("roundCap", true);
  var valueAxisStart = valueAxis2.dataToCoord(getStartValue(valueAxis2));
  for (var idx = 0, len2 = data.count(); idx < len2; idx++) {
    var value = data.get(valueDim, idx);
    var baseValue = data.get(baseDim, idx);
    var sign = value >= 0 ? "p" : "n";
    var baseCoord = valueAxisStart;
    if (stacked) {
      if (!lastStackCoords[stackId][baseValue]) {
        lastStackCoords[stackId][baseValue] = {
          p: valueAxisStart,
          n: valueAxisStart
          // Negative stack
        };
      }
      baseCoord = lastStackCoords[stackId][baseValue][sign];
    }
    var r0 = void 0;
    var r = void 0;
    var startAngle = void 0;
    var endAngle = void 0;
    if (valueAxis2.dim === "radius") {
      var radiusSpan = valueAxis2.dataToCoord(value) - valueAxisStart;
      var angle = baseAxis.dataToCoord(baseValue);
      if (mathAbs3(radiusSpan) < barMinHeight) {
        radiusSpan = (radiusSpan < 0 ? -1 : 1) * barMinHeight;
      }
      r0 = baseCoord;
      r = baseCoord + radiusSpan;
      startAngle = angle - columnOffset;
      endAngle = startAngle - columnWidth;
      stacked && (lastStackCoords[stackId][baseValue][sign] = r);
    } else {
      var angleSpan = valueAxis2.dataToCoord(value, clampLayout) - valueAxisStart;
      var radius = baseAxis.dataToCoord(baseValue);
      if (mathAbs3(angleSpan) < barMinAngle) {
        angleSpan = (angleSpan < 0 ? -1 : 1) * barMinAngle;
      }
      r0 = radius + columnOffset;
      r = r0 + columnWidth;
      startAngle = baseCoord;
      endAngle = baseCoord + angleSpan;
      stacked && (lastStackCoords[stackId][baseValue][sign] = endAngle);
    }
    data.setItemLayout(idx, {
      cx,
      cy,
      r0,
      r,
      // Consider that positive angle is anti-clockwise,
      // while positive radian of sector is clockwise
      startAngle: -startAngle * mathPI / 180,
      endAngle: -endAngle * mathPI / 180,
      /**
       * Keep the same logic with bar in catesion: use end value to
       * control direction. Notice that if clockwise is true (by
       * default), the sector will always draw clockwisely, no matter
       * whether endAngle is greater or less than startAngle.
       */
      clockwise: startAngle >= endAngle
    });
  }
}
function calcRadialBar(axis, seriesType2) {
  var axisStatKey = makeAxisStatKey2(seriesType2, COORD_SYS_TYPE_POLAR);
  var bandWidth = calcBandWidth(axis, {
    fromStat: {
      key: axisStatKey
    },
    min: 1
  }).w;
  var remainedWidth = bandWidth;
  var autoWidthCount = 0;
  var categoryGapOption = "20%";
  var gapOption = "30%";
  var stacks = {};
  eachSeriesOnAxisOnKey(axis, axisStatKey, function(seriesModel) {
    var stackId = getSeriesStackId2(seriesModel);
    if (!stacks[stackId]) {
      autoWidthCount++;
    }
    stacks[stackId] = stacks[stackId] || {
      width: 0,
      maxWidth: 0
    };
    var barWidth = parsePercent2(seriesModel.get("barWidth"), bandWidth);
    var barMaxWidth = parsePercent2(seriesModel.get("barMaxWidth"), bandWidth);
    var barGapOption = seriesModel.get("barGap");
    var barCategoryGapOption = seriesModel.get("barCategoryGap");
    if (barWidth && !stacks[stackId].width) {
      barWidth = mathMin4(remainedWidth, barWidth);
      stacks[stackId].width = barWidth;
      remainedWidth -= barWidth;
    }
    barMaxWidth && (stacks[stackId].maxWidth = barMaxWidth);
    barGapOption != null && (gapOption = barGapOption);
    barCategoryGapOption != null && (categoryGapOption = barCategoryGapOption);
  });
  var result = {};
  var categoryGap = parsePercent2(categoryGapOption, bandWidth);
  var barGapPercent = parsePercent2(gapOption, 1);
  var autoWidth = (remainedWidth - categoryGap) / (autoWidthCount + (autoWidthCount - 1) * barGapPercent);
  autoWidth = mathMax4(autoWidth, 0);
  each(stacks, function(column, stack) {
    var maxWidth = column.maxWidth;
    if (maxWidth && maxWidth < autoWidth) {
      maxWidth = mathMin4(maxWidth, remainedWidth);
      if (column.width) {
        maxWidth = mathMin4(maxWidth, column.width);
      }
      remainedWidth -= maxWidth;
      column.width = maxWidth;
      autoWidthCount--;
    }
  });
  autoWidth = (remainedWidth - categoryGap) / (autoWidthCount + (autoWidthCount - 1) * barGapPercent);
  autoWidth = mathMax4(autoWidth, 0);
  var widthSum = 0;
  var lastColumn;
  each(stacks, function(column, idx) {
    if (!column.width) {
      column.width = autoWidth;
    }
    lastColumn = column;
    widthSum += column.width * (1 + barGapPercent);
  });
  if (lastColumn) {
    widthSum -= lastColumn.width * barGapPercent;
  }
  var offset = -widthSum / 2;
  each(stacks, function(column, stackId) {
    result[stackId] = result[stackId] || {
      offset,
      width: column.width
    };
    offset += column.width * (1 + barGapPercent);
  });
  return result;
}

// node_modules/echarts/lib/component/polar/install.js
var PolarView = (
  /** @class */
  (function(_super) {
    __extends(PolarView2, _super);
    function PolarView2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = PolarView2.type;
      return _this;
    }
    PolarView2.type = "polar";
    return PolarView2;
  })(Component_default2)
);

// node_modules/echarts/lib/coord/single/singleAxisHelper.js
function layout2(axisModel, opt) {
  opt = opt || {};
  var single = axisModel.coordinateSystem;
  var axis = axisModel.axis;
  var layout3 = {};
  var axisPosition = axis.position;
  var orient = axis.orient;
  var rect = single.getRect();
  var rectBound = [rect.x, rect.x + rect.width, rect.y, rect.y + rect.height];
  var positionMap = {
    horizontal: {
      top: rectBound[2],
      bottom: rectBound[3]
    },
    vertical: {
      left: rectBound[0],
      right: rectBound[1]
    }
  };
  layout3.position = [orient === "vertical" ? positionMap.vertical[axisPosition] : rectBound[0], orient === "horizontal" ? positionMap.horizontal[axisPosition] : rectBound[3]];
  var r = {
    horizontal: 0,
    vertical: 1
  };
  layout3.rotation = Math.PI / 2 * r[orient];
  var directionMap = {
    top: -1,
    bottom: 1,
    right: 1,
    left: -1
  };
  layout3.labelDirection = layout3.tickDirection = layout3.nameDirection = directionMap[axisPosition];
  if (axisModel.get(["axisTick", "inside"])) {
    layout3.tickDirection = -layout3.tickDirection;
  }
  if (retrieve(opt.labelInside, axisModel.get(["axisLabel", "inside"]))) {
    layout3.labelDirection = -layout3.labelDirection;
  }
  var labelRotate = axisModel.get(["axisLabel", "rotate"]);
  layout3.labelRotate = axisPosition === "top" ? -labelRotate : labelRotate;
  layout3.z2 = 1;
  return layout3;
}

// node_modules/echarts/lib/component/axis/SingleAxisView.js
var selfBuilderAttrs3 = ["splitArea", "splitLine", "breakArea"];
var SingleAxisView = (
  /** @class */
  (function(_super) {
    __extends(SingleAxisView2, _super);
    function SingleAxisView2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = SingleAxisView2.type;
      _this.axisPointerClass = "SingleAxisPointer";
      return _this;
    }
    SingleAxisView2.prototype.render = function(axisModel, ecModel, api, payload) {
      var group2 = this.group;
      group2.removeAll();
      var oldAxisGroup = this._axisGroup;
      this._axisGroup = new Group_default();
      var layout3 = layout2(axisModel);
      var axisBuilder = new AxisBuilder_default(axisModel, api, layout3);
      axisBuilder.build();
      group2.add(this._axisGroup);
      group2.add(axisBuilder.group);
      each(selfBuilderAttrs3, function(name) {
        if (axisModel.get([name, "show"])) {
          axisElementBuilders3[name](this, this.group, this._axisGroup, axisModel, api);
        }
      }, this);
      groupTransition(oldAxisGroup, this._axisGroup, axisModel);
      _super.prototype.render.call(this, axisModel, ecModel, api, payload);
    };
    SingleAxisView2.prototype.remove = function() {
      rectCoordAxisHandleRemove(this);
    };
    SingleAxisView2.type = "singleAxis";
    return SingleAxisView2;
  })(AxisView_default)
);
var axisElementBuilders3 = {
  splitLine: function(axisView, group2, axisGroup, axisModel, api) {
    var axis = axisModel.axis;
    if (axis.scale.isBlank()) {
      return;
    }
    var splitLineModel = axisModel.getModel("splitLine");
    var lineStyleModel = splitLineModel.getModel("lineStyle");
    var lineColors = lineStyleModel.get("color");
    lineColors = lineColors instanceof Array ? lineColors : [lineColors];
    var lineWidth = lineStyleModel.get("width");
    var gridRect = axisModel.coordinateSystem.getRect();
    var isHorizontal = axis.isHorizontal();
    var splitLines = [];
    var lineCount = 0;
    var ticksCoords = axis.getTicksCoords({
      tickModel: splitLineModel,
      breakTicks: "none",
      pruneByBreak: "preserve_extent_bound"
    });
    var p1 = [];
    var p2 = [];
    for (var i2 = 0; i2 < ticksCoords.length; ++i2) {
      var tickCoord = axis.toGlobalCoord(ticksCoords[i2].coord);
      if (isHorizontal) {
        p1[0] = tickCoord;
        p1[1] = gridRect.y;
        p2[0] = tickCoord;
        p2[1] = gridRect.y + gridRect.height;
      } else {
        p1[0] = gridRect.x;
        p1[1] = tickCoord;
        p2[0] = gridRect.x + gridRect.width;
        p2[1] = tickCoord;
      }
      var line = new Line_default({
        shape: {
          x1: p1[0],
          y1: p1[1],
          x2: p2[0],
          y2: p2[1]
        },
        silent: true
      });
      subPixelOptimizeLine2(line.shape, lineWidth);
      var colorIndex = lineCount++ % lineColors.length;
      splitLines[colorIndex] = splitLines[colorIndex] || [];
      splitLines[colorIndex].push(line);
    }
    var lineStyle = lineStyleModel.getLineStyle(["color"]);
    for (var i2 = 0; i2 < splitLines.length; ++i2) {
      group2.add(mergePath2(splitLines[i2], {
        style: defaults({
          stroke: lineColors[i2 % lineColors.length]
        }, lineStyle),
        silent: true
      }));
    }
  },
  splitArea: function(axisView, group2, axisGroup, axisModel, api) {
    rectCoordAxisBuildSplitArea(axisView, axisGroup, axisModel, axisModel);
  },
  breakArea: function(axisView, group2, axisGroup, axisModel, api) {
    var axisBreakHelper = getAxisBreakHelper();
    var scale3 = axisModel.axis.scale;
    if (axisBreakHelper && scale3.type !== "ordinal") {
      axisBreakHelper.rectCoordBuildBreakAxis(group2, axisView, axisModel, axisModel.coordinateSystem.getRect(), api);
    }
  }
};

// node_modules/echarts/lib/coord/single/SingleAxis.js
var SingleAxis = (
  /** @class */
  (function(_super) {
    __extends(SingleAxis2, _super);
    function SingleAxis2(dim, scale3, coordExtent, axisType, position) {
      var _this = _super.call(this, dim, scale3, coordExtent) || this;
      _this.type = axisType || "value";
      _this.position = position || "bottom";
      return _this;
    }
    SingleAxis2.prototype.isHorizontal = function() {
      var position = this.position;
      return position === "top" || position === "bottom";
    };
    SingleAxis2.prototype.pointToData = function(point, clamp2) {
      return this.coordinateSystem.pointToData(point)[0];
    };
    return SingleAxis2;
  })(Axis_default)
);
var SingleAxis_default = SingleAxis;

// node_modules/echarts/lib/coord/single/Single.js
var singleDimensions = ["single"];
var Single = (
  /** @class */
  (function() {
    function Single2(axisModel, ecModel, api) {
      this.type = COORD_SYS_TYPE_SINGLE;
      this.dimension = "single";
      this.dimensions = singleDimensions;
      this.axisPointerEnabled = true;
      this.model = axisModel;
      this._init(axisModel, ecModel, api);
    }
    Single2.prototype._init = function(axisModel, ecModel, api) {
      var dim = this.dimension;
      var axisType = determineAxisType(axisModel);
      var axis = new SingleAxis_default(dim, createScaleByModel(axisModel, axisType, true), [0, 0], axisType, axisModel.get("position"));
      axis.onBand = isAxisOnBand(axis.scale, axisModel);
      axis.inverse = axisModel.get("inverse");
      axis.orient = axisModel.get("orient");
      axisModel.axis = axis;
      axis.model = axisModel;
      axis.coordinateSystem = this;
      this._axis = axis;
    };
    Single2.prototype.update = function(ecModel, api) {
      var axis = this._axis;
      scaleRawExtentInfoCreate(axis, AXIS_EXTENT_INFO_BUILD_FROM_COORD_SYS_UPDATE);
      scaleCalcNice(axis);
    };
    Single2.prototype.resize = function(axisModel, api) {
      var refContainer = createBoxLayoutReference(axisModel, api).refContainer;
      this._rect = getLayoutRect(axisModel.getBoxLayoutParams(), refContainer);
      this._adjustAxis();
    };
    Single2.prototype.getRect = function() {
      return this._rect;
    };
    Single2.prototype._adjustAxis = function() {
      var rect = this._rect;
      var axis = this._axis;
      var isHorizontal = axis.isHorizontal();
      var extent = isHorizontal ? [0, rect.width] : [0, rect.height];
      var idx = axis.inverse ? 1 : 0;
      axis.setExtent(extent[idx], extent[1 - idx]);
      this._updateAxisTransform(axis, isHorizontal ? rect.x : rect.y);
    };
    Single2.prototype._updateAxisTransform = function(axis, coordBase) {
      var axisExtent = axis.getExtent();
      var extentSum = axisExtent[0] + axisExtent[1];
      var isHorizontal = axis.isHorizontal();
      axis.toGlobalCoord = isHorizontal ? function(coord) {
        return coord + coordBase;
      } : function(coord) {
        return extentSum - coord + coordBase;
      };
      axis.toLocalCoord = isHorizontal ? function(coord) {
        return coord - coordBase;
      } : function(coord) {
        return extentSum - coord + coordBase;
      };
    };
    Single2.prototype.getAxis = function() {
      return this._axis;
    };
    Single2.prototype.getBaseAxis = function() {
      return this._axis;
    };
    Single2.prototype.getAxes = function() {
      return [this._axis];
    };
    Single2.prototype.getTooltipAxes = function() {
      return {
        baseAxes: [this.getAxis()],
        // Empty otherAxes
        otherAxes: []
      };
    };
    Single2.prototype.containPoint = function(point) {
      var rect = this.getRect();
      var axis = this.getAxis();
      var orient = axis.orient;
      if (orient === "horizontal") {
        return axis.contain(axis.toLocalCoord(point[0])) && point[1] >= rect.y && point[1] <= rect.y + rect.height;
      } else {
        return axis.contain(axis.toLocalCoord(point[1])) && point[0] >= rect.y && point[0] <= rect.y + rect.height;
      }
    };
    Single2.prototype.pointToData = function(point, reserved, out2) {
      out2 = out2 || [];
      var axis = this.getAxis();
      out2[0] = axis.coordToData(axis.toLocalCoord(point[axis.orient === "horizontal" ? 0 : 1]));
      return out2;
    };
    Single2.prototype.dataToPoint = function(val, reserved, out2) {
      var axis = this.getAxis();
      var rect = this.getRect();
      out2 = out2 || [];
      var idx = axis.orient === "horizontal" ? 0 : 1;
      if (val instanceof Array) {
        val = val[0];
      }
      out2[idx] = axis.toGlobalCoord(axis.dataToCoord(+val));
      out2[1 - idx] = idx === 0 ? rect.y + rect.height / 2 : rect.x + rect.width / 2;
      return out2;
    };
    Single2.prototype.convertToPixel = function(ecModel, finder, value) {
      var coordSys = getCoordSys4(finder);
      return coordSys === this ? this.dataToPoint(value) : null;
    };
    Single2.prototype.convertFromPixel = function(ecModel, finder, pixel) {
      var coordSys = getCoordSys4(finder);
      return coordSys === this ? this.pointToData(pixel) : null;
    };
    return Single2;
  })()
);
function getCoordSys4(finder) {
  var seriesModel = finder.seriesModel;
  var singleModel = finder.singleAxisModel;
  return singleModel && singleModel.coordinateSystem || seriesModel && seriesModel.coordinateSystem;
}

// node_modules/echarts/lib/component/axisPointer/SingleAxisPointer.js
var XY3 = ["x", "y"];
var WH3 = ["width", "height"];
var SingleAxisPointer = (
  /** @class */
  (function(_super) {
    __extends(SingleAxisPointer2, _super);
    function SingleAxisPointer2() {
      return _super !== null && _super.apply(this, arguments) || this;
    }
    SingleAxisPointer2.prototype.makeElOption = function(elOption, value, axisModel, axisPointerModel, api) {
      var axis = axisModel.axis;
      var coordSys = axis.coordinateSystem;
      var pointDimIndex = getPointDimIndex(axis);
      var thisExtent = getGlobalExtent(coordSys, pointDimIndex);
      var otherExtent = getGlobalExtent(coordSys, 1 - pointDimIndex);
      var pixelValue = coordSys.dataToPoint(value)[0];
      var axisPointerType = axisPointerModel.get("type");
      if (axisPointerType && axisPointerType !== "none") {
        var elStyle = buildElStyle(axisPointerModel);
        var pointerOption = pointerShapeBuilder3[axisPointerType](axis, pixelValue, thisExtent, otherExtent, axisPointerModel.get("seriesDataIndices"), axisPointerModel.ecModel);
        pointerOption.style = elStyle;
        elOption.graphicKey = pointerOption.type;
        elOption.pointer = pointerOption;
      }
      var layoutInfo = layout2(axisModel);
      buildCartesianSingleLabelElOption(value, elOption, layoutInfo, axisModel, axisPointerModel, api);
    };
    SingleAxisPointer2.prototype.getHandleTransform = function(value, axisModel, axisPointerModel) {
      var layoutInfo = layout2(axisModel, {
        labelInside: false
      });
      layoutInfo.labelMargin = axisPointerModel.get(["handle", "margin"]);
      var position = getTransformedPosition(axisModel.axis, value, layoutInfo);
      return {
        x: position[0],
        y: position[1],
        rotation: layoutInfo.rotation + (layoutInfo.labelDirection < 0 ? Math.PI : 0)
      };
    };
    SingleAxisPointer2.prototype.updateHandleTransform = function(transform, delta, axisModel, axisPointerModel) {
      var axis = axisModel.axis;
      var coordSys = axis.coordinateSystem;
      var dimIndex = getPointDimIndex(axis);
      var axisExtent = getGlobalExtent(coordSys, dimIndex);
      var currPosition = [transform.x, transform.y];
      currPosition[dimIndex] += delta[dimIndex];
      currPosition[dimIndex] = Math.min(axisExtent[1], currPosition[dimIndex]);
      currPosition[dimIndex] = Math.max(axisExtent[0], currPosition[dimIndex]);
      var otherExtent = getGlobalExtent(coordSys, 1 - dimIndex);
      var cursorOtherValue = (otherExtent[1] + otherExtent[0]) / 2;
      var cursorPoint = [cursorOtherValue, cursorOtherValue];
      cursorPoint[dimIndex] = currPosition[dimIndex];
      return {
        x: currPosition[0],
        y: currPosition[1],
        rotation: transform.rotation,
        cursorPoint,
        tooltipOption: {
          verticalAlign: "middle"
        }
      };
    };
    return SingleAxisPointer2;
  })(BaseAxisPointer_default)
);
var pointerShapeBuilder3 = {
  line: function(axis, pixelValue, thisExtent, otherExtent) {
    var targetShape = makeLineShape([pixelValue, otherExtent[0]], [pixelValue, otherExtent[1]], getPointDimIndex(axis));
    return {
      type: "Line",
      subPixelOptimize: true,
      shape: targetShape
    };
  },
  shadow: function(axis, pixelValue, thisExtent, otherExtent, seriesDataIndices, ecModel) {
    var bandWidth = calcAxisPointerShadowBandWidth(axis, seriesDataIndices, ecModel);
    var span = otherExtent[1] - otherExtent[0];
    var _a2 = calcAxisPointerShadowEnds(pixelValue, thisExtent, bandWidth), min3 = _a2[0], max3 = _a2[1];
    return {
      type: "Rect",
      shape: makeRectShape([min3, otherExtent[0]], [max3 - min3, span], getPointDimIndex(axis))
    };
  }
};
function getPointDimIndex(axis) {
  return axis.isHorizontal() ? 0 : 1;
}
function getGlobalExtent(coordSys, dimIndex) {
  var rect = coordSys.getRect();
  return [rect[XY3[dimIndex]], rect[XY3[dimIndex]] + rect[WH3[dimIndex]]];
}

// node_modules/echarts/lib/component/singleAxis/install.js
var SingleView = (
  /** @class */
  (function(_super) {
    __extends(SingleView2, _super);
    function SingleView2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = SingleView2.type;
      return _this;
    }
    SingleView2.type = "single";
    return SingleView2;
  })(Component_default2)
);

// node_modules/echarts/lib/coord/calendar/CalendarModel.js
var CalendarModel = (
  /** @class */
  (function(_super) {
    __extends(CalendarModel2, _super);
    function CalendarModel2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = CalendarModel2.type;
      return _this;
    }
    CalendarModel2.prototype.init = function(option, parentModel, ecModel) {
      var inputPositionParams = getLayoutParams(option);
      _super.prototype.init.apply(this, arguments);
      mergeAndNormalizeLayoutParams(option, inputPositionParams);
    };
    CalendarModel2.prototype.mergeOption = function(option) {
      _super.prototype.mergeOption.apply(this, arguments);
      mergeAndNormalizeLayoutParams(this.option, option);
    };
    CalendarModel2.prototype.getCellSize = function() {
      return this.option.cellSize;
    };
    CalendarModel2.type = "calendar";
    CalendarModel2.layoutMode = "box";
    CalendarModel2.defaultOption = {
      // zlevel: 0,
      // TODO: theoretically, the z of the calendar should be lower
      // than series, but we don't want the series to be displayed
      // on top of the borders like month split line. To align with
      // the effect of previous versions, we set the z to 2 for now
      // until better solution is found.
      z: 2,
      left: 80,
      top: 60,
      cellSize: 20,
      // horizontal vertical
      orient: "horizontal",
      // month separate line style
      splitLine: {
        show: true,
        lineStyle: {
          color: tokens_default.color.axisLine,
          width: 1,
          type: "solid"
        }
      },
      // rect style  temporarily unused emphasis
      itemStyle: {
        color: tokens_default.color.neutral00,
        borderWidth: 1,
        borderColor: tokens_default.color.neutral10
      },
      // week text style
      dayLabel: {
        show: true,
        firstDay: 0,
        // start end
        position: "start",
        margin: tokens_default.size.s,
        color: tokens_default.color.secondary
      },
      // month text style
      monthLabel: {
        show: true,
        // start end
        position: "start",
        margin: tokens_default.size.s,
        // center or left
        align: "center",
        formatter: null,
        color: tokens_default.color.secondary
      },
      // year text style
      yearLabel: {
        show: true,
        // top bottom left right
        position: null,
        margin: tokens_default.size.xl,
        formatter: null,
        color: tokens_default.color.quaternary,
        fontFamily: "sans-serif",
        fontWeight: "bolder",
        fontSize: 20
      }
    };
    return CalendarModel2;
  })(Component_default)
);
function mergeAndNormalizeLayoutParams(target, raw) {
  var cellSize = target.cellSize;
  var cellSizeArr;
  if (!isArray(cellSize)) {
    cellSizeArr = target.cellSize = [cellSize, cellSize];
  } else {
    cellSizeArr = cellSize;
  }
  if (cellSizeArr.length === 1) {
    cellSizeArr[1] = cellSizeArr[0];
  }
  var ignoreSize = map2([0, 1], function(hvIdx) {
    if (sizeCalculable(raw, hvIdx)) {
      cellSizeArr[hvIdx] = "auto";
    }
    return cellSizeArr[hvIdx] != null && cellSizeArr[hvIdx] !== "auto";
  });
  mergeLayoutParam(target, raw, {
    type: "box",
    ignoreSize
  });
}

// node_modules/echarts/lib/component/calendar/CalendarView.js
var CalendarView = (
  /** @class */
  (function(_super) {
    __extends(CalendarView2, _super);
    function CalendarView2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = CalendarView2.type;
      return _this;
    }
    CalendarView2.prototype.render = function(calendarModel, ecModel, api) {
      var group2 = this.group;
      group2.removeAll();
      var coordSys = calendarModel.coordinateSystem;
      var rangeData = coordSys.getRangeInfo();
      var orient = coordSys.getOrient();
      var localeModel = ecModel.getLocaleModel();
      this._renderDayRect(calendarModel, rangeData, group2);
      this._renderLines(calendarModel, rangeData, orient, group2);
      this._renderYearText(calendarModel, rangeData, orient, group2);
      this._renderMonthText(calendarModel, localeModel, orient, group2);
      this._renderWeekText(calendarModel, localeModel, rangeData, orient, group2);
    };
    CalendarView2.prototype._renderDayRect = function(calendarModel, rangeData, group2) {
      var coordSys = calendarModel.coordinateSystem;
      var itemRectStyleModel = calendarModel.getModel("itemStyle").getItemStyle();
      var sw = coordSys.getCellWidth();
      var sh = coordSys.getCellHeight();
      for (var i2 = rangeData.start.time; i2 <= rangeData.end.time; i2 = coordSys.getNextNDay(i2, 1).time) {
        var point = coordSys.dataToCalendarLayout([i2], false).tl;
        var rect = new Rect_default({
          shape: {
            x: point[0],
            y: point[1],
            width: sw,
            height: sh
          },
          cursor: "default",
          style: itemRectStyleModel
        });
        group2.add(rect);
      }
    };
    CalendarView2.prototype._renderLines = function(calendarModel, rangeData, orient, group2) {
      var self2 = this;
      var coordSys = calendarModel.coordinateSystem;
      var lineStyleModel = calendarModel.getModel(["splitLine", "lineStyle"]).getLineStyle();
      var show = calendarModel.get(["splitLine", "show"]);
      var lineWidth = lineStyleModel.lineWidth;
      this._tlpoints = [];
      this._blpoints = [];
      this._firstDayOfMonth = [];
      this._firstDayPoints = [];
      var firstDay = rangeData.start;
      for (var i2 = 0; firstDay.time <= rangeData.end.time; i2++) {
        addPoints(firstDay.formatedDate);
        if (i2 === 0) {
          firstDay = coordSys.getDateInfo(rangeData.start.y + "-" + rangeData.start.m);
        }
        var date = firstDay.date;
        date.setMonth(date.getMonth() + 1);
        firstDay = coordSys.getDateInfo(date);
      }
      addPoints(coordSys.getNextNDay(rangeData.end.time, 1).formatedDate);
      function addPoints(date2) {
        self2._firstDayOfMonth.push(coordSys.getDateInfo(date2));
        self2._firstDayPoints.push(coordSys.dataToCalendarLayout([date2], false).tl);
        var points4 = self2._getLinePointsOfOneWeek(calendarModel, date2, orient);
        self2._tlpoints.push(points4[0]);
        self2._blpoints.push(points4[points4.length - 1]);
        show && self2._drawSplitline(points4, lineStyleModel, group2);
      }
      show && this._drawSplitline(self2._getEdgesPoints(self2._tlpoints, lineWidth, orient), lineStyleModel, group2);
      show && this._drawSplitline(self2._getEdgesPoints(self2._blpoints, lineWidth, orient), lineStyleModel, group2);
    };
    CalendarView2.prototype._getEdgesPoints = function(points4, lineWidth, orient) {
      var rs = [points4[0].slice(), points4[points4.length - 1].slice()];
      var idx = orient === "horizontal" ? 0 : 1;
      rs[0][idx] = rs[0][idx] - lineWidth / 2;
      rs[1][idx] = rs[1][idx] + lineWidth / 2;
      return rs;
    };
    CalendarView2.prototype._drawSplitline = function(points4, lineStyle, group2) {
      var poyline = new Polyline_default({
        z2: 20,
        shape: {
          points: points4
        },
        style: lineStyle
      });
      group2.add(poyline);
    };
    CalendarView2.prototype._getLinePointsOfOneWeek = function(calendarModel, date, orient) {
      var coordSys = calendarModel.coordinateSystem;
      var parsedDate = coordSys.getDateInfo(date);
      var points4 = [];
      for (var i2 = 0; i2 < 7; i2++) {
        var tmpD = coordSys.getNextNDay(parsedDate.time, i2);
        var point = coordSys.dataToCalendarLayout([tmpD.time], false);
        points4[2 * tmpD.day] = point.tl;
        points4[2 * tmpD.day + 1] = point[orient === "horizontal" ? "bl" : "tr"];
      }
      return points4;
    };
    CalendarView2.prototype._formatterLabel = function(formatter, params) {
      if (isString(formatter) && formatter) {
        return formatTplSimple(formatter, params);
      }
      if (isFunction(formatter)) {
        return formatter(params);
      }
      return params.nameMap;
    };
    CalendarView2.prototype._yearTextPositionControl = function(textEl, point, orient, position, margin) {
      var x = point[0];
      var y = point[1];
      var aligns = ["center", "bottom"];
      if (position === "bottom") {
        y += margin;
        aligns = ["center", "top"];
      } else if (position === "left") {
        x -= margin;
      } else if (position === "right") {
        x += margin;
        aligns = ["center", "top"];
      } else {
        y -= margin;
      }
      var rotate2 = 0;
      if (position === "left" || position === "right") {
        rotate2 = Math.PI / 2;
      }
      return {
        rotation: rotate2,
        x,
        y,
        style: {
          align: aligns[0],
          verticalAlign: aligns[1]
        }
      };
    };
    CalendarView2.prototype._renderYearText = function(calendarModel, rangeData, orient, group2) {
      var yearLabel = calendarModel.getModel("yearLabel");
      if (!yearLabel.get("show")) {
        return;
      }
      var margin = yearLabel.get("margin");
      var pos = yearLabel.get("position");
      if (!pos) {
        pos = orient !== "horizontal" ? "top" : "left";
      }
      var points4 = [this._tlpoints[this._tlpoints.length - 1], this._blpoints[0]];
      var xc = (points4[0][0] + points4[1][0]) / 2;
      var yc = (points4[0][1] + points4[1][1]) / 2;
      var idx = orient === "horizontal" ? 0 : 1;
      var posPoints = {
        top: [xc, points4[idx][1]],
        bottom: [xc, points4[1 - idx][1]],
        left: [points4[1 - idx][0], yc],
        right: [points4[idx][0], yc]
      };
      var name = rangeData.start.y;
      if (+rangeData.end.y > +rangeData.start.y) {
        name = name + "-" + rangeData.end.y;
      }
      var formatter = yearLabel.get("formatter");
      var params = {
        start: rangeData.start.y,
        end: rangeData.end.y,
        nameMap: name
      };
      var content = this._formatterLabel(formatter, params);
      var yearText = new Text_default({
        z2: 30,
        style: createTextStyle(yearLabel, {
          text: content
        }),
        silent: yearLabel.get("silent")
      });
      yearText.attr(this._yearTextPositionControl(yearText, posPoints[pos], orient, pos, margin));
      group2.add(yearText);
    };
    CalendarView2.prototype._monthTextPositionControl = function(point, isCenter, orient, position, margin) {
      var align = "left";
      var vAlign = "top";
      var x = point[0];
      var y = point[1];
      if (orient === "horizontal") {
        y = y + margin;
        if (isCenter) {
          align = "center";
        }
        if (position === "start") {
          vAlign = "bottom";
        }
      } else {
        x = x + margin;
        if (isCenter) {
          vAlign = "middle";
        }
        if (position === "start") {
          align = "right";
        }
      }
      return {
        x,
        y,
        align,
        verticalAlign: vAlign
      };
    };
    CalendarView2.prototype._renderMonthText = function(calendarModel, localeModel, orient, group2) {
      var monthLabel = calendarModel.getModel("monthLabel");
      if (!monthLabel.get("show")) {
        return;
      }
      var nameMap = monthLabel.get("nameMap");
      var margin = monthLabel.get("margin");
      var pos = monthLabel.get("position");
      var align = monthLabel.get("align");
      var termPoints = [this._tlpoints, this._blpoints];
      if (!nameMap || isString(nameMap)) {
        if (nameMap) {
          localeModel = getLocaleModel(nameMap) || localeModel;
        }
        nameMap = localeModel.get(["time", "monthAbbr"]) || [];
      }
      var idx = pos === "start" ? 0 : 1;
      var axis = orient === "horizontal" ? 0 : 1;
      margin = pos === "start" ? -margin : margin;
      var isCenter = align === "center";
      var labelSilent = monthLabel.get("silent");
      for (var i2 = 0; i2 < termPoints[idx].length - 1; i2++) {
        var tmp = termPoints[idx][i2].slice();
        var firstDay = this._firstDayOfMonth[i2];
        if (isCenter) {
          var firstDayPoints = this._firstDayPoints[i2];
          tmp[axis] = (firstDayPoints[axis] + termPoints[0][i2 + 1][axis]) / 2;
        }
        var formatter = monthLabel.get("formatter");
        var name_1 = nameMap[+firstDay.m - 1];
        var params = {
          yyyy: firstDay.y,
          yy: (firstDay.y + "").slice(2),
          MM: firstDay.m,
          M: +firstDay.m,
          nameMap: name_1
        };
        var content = this._formatterLabel(formatter, params);
        var monthText = new Text_default({
          z2: 30,
          style: extend(createTextStyle(monthLabel, {
            text: content
          }), this._monthTextPositionControl(tmp, isCenter, orient, pos, margin)),
          silent: labelSilent
        });
        group2.add(monthText);
      }
    };
    CalendarView2.prototype._weekTextPositionControl = function(point, orient, position, margin, cellSize) {
      var align = "center";
      var vAlign = "middle";
      var x = point[0];
      var y = point[1];
      var isStart = position === "start";
      if (orient === "horizontal") {
        x = x + margin + (isStart ? 1 : -1) * cellSize[0] / 2;
        align = isStart ? "right" : "left";
      } else {
        y = y + margin + (isStart ? 1 : -1) * cellSize[1] / 2;
        vAlign = isStart ? "bottom" : "top";
      }
      return {
        x,
        y,
        align,
        verticalAlign: vAlign
      };
    };
    CalendarView2.prototype._renderWeekText = function(calendarModel, localeModel, rangeData, orient, group2) {
      var dayLabel = calendarModel.getModel("dayLabel");
      if (!dayLabel.get("show")) {
        return;
      }
      var coordSys = calendarModel.coordinateSystem;
      var pos = dayLabel.get("position");
      var nameMap = dayLabel.get("nameMap");
      var margin = dayLabel.get("margin");
      var firstDayOfWeek = coordSys.getFirstDayOfWeek();
      if (!nameMap || isString(nameMap)) {
        if (nameMap) {
          localeModel = getLocaleModel(nameMap) || localeModel;
        }
        var dayOfWeekShort = localeModel.get(["time", "dayOfWeekShort"]);
        nameMap = dayOfWeekShort || map2(localeModel.get(["time", "dayOfWeekAbbr"]), function(val) {
          return val[0];
        });
      }
      var start2 = coordSys.getNextNDay(rangeData.end.time, 7 - rangeData.lweek).time;
      var cellSize = [coordSys.getCellWidth(), coordSys.getCellHeight()];
      margin = parsePercent2(margin, Math.min(cellSize[1], cellSize[0]));
      if (pos === "start") {
        start2 = coordSys.getNextNDay(rangeData.start.time, -(7 + rangeData.fweek)).time;
        margin = -margin;
      }
      var labelSilent = dayLabel.get("silent");
      for (var i2 = 0; i2 < 7; i2++) {
        var tmpD = coordSys.getNextNDay(start2, i2);
        var point = coordSys.dataToCalendarLayout([tmpD.time], false).center;
        var day = i2;
        day = Math.abs((i2 + firstDayOfWeek) % 7);
        var weekText = new Text_default({
          z2: 30,
          style: extend(createTextStyle(dayLabel, {
            text: nameMap[day]
          }), this._weekTextPositionControl(point, orient, pos, margin, cellSize)),
          silent: labelSilent
        });
        group2.add(weekText);
      }
    };
    CalendarView2.type = "calendar";
    return CalendarView2;
  })(Component_default2)
);

// node_modules/echarts/lib/coord/calendar/Calendar.js
var PROXIMATE_ONE_DAY = 864e5;
var Calendar = (
  /** @class */
  (function() {
    function Calendar2(calendarModel, ecModel, api) {
      this.type = "calendar";
      this.dimensions = Calendar2.dimensions;
      this.getDimensionsInfo = Calendar2.getDimensionsInfo;
      this._model = calendarModel;
      this._update(ecModel, api);
    }
    Calendar2.getDimensionsInfo = function() {
      return [{
        name: "time",
        type: "time"
      }, "value"];
    };
    Calendar2.prototype.getRangeInfo = function() {
      return this._rangeInfo;
    };
    Calendar2.prototype.getModel = function() {
      return this._model;
    };
    Calendar2.prototype.getRect = function() {
      return this._rect;
    };
    Calendar2.prototype.getCellWidth = function() {
      return this._sw;
    };
    Calendar2.prototype.getCellHeight = function() {
      return this._sh;
    };
    Calendar2.prototype.getOrient = function() {
      return this._orient;
    };
    Calendar2.prototype.getFirstDayOfWeek = function() {
      return this._firstDayOfWeek;
    };
    Calendar2.prototype.getDateInfo = function(date) {
      date = parseDate(date);
      var y = date.getFullYear();
      var m2 = date.getMonth() + 1;
      var mStr = m2 < 10 ? "0" + m2 : "" + m2;
      var d = date.getDate();
      var dStr = d < 10 ? "0" + d : "" + d;
      var day = date.getDay();
      day = Math.abs((day + 7 - this.getFirstDayOfWeek()) % 7);
      return {
        y: y + "",
        m: mStr,
        d: dStr,
        day,
        time: date.getTime(),
        formatedDate: y + "-" + mStr + "-" + dStr,
        date
      };
    };
    Calendar2.prototype.getNextNDay = function(date, n) {
      n = n || 0;
      if (n === 0) {
        return this.getDateInfo(date);
      }
      date = new Date(this.getDateInfo(date).time);
      date.setDate(date.getDate() + n);
      return this.getDateInfo(date);
    };
    Calendar2.prototype._update = function(ecModel, api) {
      this._firstDayOfWeek = +this._model.getModel("dayLabel").get("firstDay");
      this._orient = this._model.get("orient");
      this._lineWidth = this._model.getModel("itemStyle").getItemStyle().lineWidth || 0;
      this._rangeInfo = this._getRangeInfo(this._initRangeOption());
      var weeks = this._rangeInfo.weeks || 1;
      var whNames = ["width", "height"];
      var cellSize = this._model.getCellSize().slice();
      var layoutParams = this._model.getBoxLayoutParams();
      var cellNumbers = this._orient === "horizontal" ? [weeks, 7] : [7, weeks];
      each([0, 1], function(idx) {
        if (cellSizeSpecified(cellSize, idx)) {
          layoutParams[whNames[idx]] = cellSize[idx] * cellNumbers[idx];
        }
      });
      var whGlobal = {
        width: api.getWidth(),
        height: api.getHeight()
      };
      var calendarRect = this._rect = getLayoutRect(layoutParams, whGlobal);
      each([0, 1], function(idx) {
        if (!cellSizeSpecified(cellSize, idx)) {
          cellSize[idx] = calendarRect[whNames[idx]] / cellNumbers[idx];
        }
      });
      function cellSizeSpecified(cellSize2, idx) {
        return cellSize2[idx] != null && cellSize2[idx] !== "auto";
      }
      this._sw = cellSize[0];
      this._sh = cellSize[1];
    };
    Calendar2.prototype.dataToPoint = function(data, clamp2, out2) {
      out2 = out2 || [];
      isArray(data) && (data = data[0]);
      clamp2 == null && (clamp2 = true);
      var dayInfo = this.getDateInfo(data);
      var range = this._rangeInfo;
      var date = dayInfo.formatedDate;
      if (clamp2 && !(dayInfo.time >= range.start.time && dayInfo.time < range.end.time + PROXIMATE_ONE_DAY)) {
        out2[0] = out2[1] = NaN;
        return out2;
      }
      var week = dayInfo.day;
      var nthWeek = this._getRangeInfo([range.start.time, date]).nthWeek;
      if (this._orient === "vertical") {
        out2[0] = this._rect.x + week * this._sw + this._sw / 2;
        out2[1] = this._rect.y + nthWeek * this._sh + this._sh / 2;
      } else {
        out2[0] = this._rect.x + nthWeek * this._sw + this._sw / 2;
        out2[1] = this._rect.y + week * this._sh + this._sh / 2;
      }
      return out2;
    };
    Calendar2.prototype.pointToData = function(point) {
      var date = this.pointToDate(point);
      return date && date.time;
    };
    Calendar2.prototype.dataToLayout = function(data, clamp2, out2) {
      out2 = out2 || {};
      var rect = out2.rect = out2.rect || {};
      var contentRect = out2.contentRect = out2.contentRect || {};
      var point = this.dataToPoint(data, clamp2);
      rect.x = point[0] - this._sw / 2;
      rect.y = point[1] - this._sh / 2;
      rect.width = this._sw;
      rect.height = this._sh;
      BoundingRect_default.copy(contentRect, rect);
      expandOrShrinkRect(contentRect, this._lineWidth / 2, true, true);
      return out2;
    };
    Calendar2.prototype.dataToCalendarLayout = function(data, clamp2) {
      var point = this.dataToPoint(data, clamp2);
      return {
        center: point,
        tl: [point[0] - this._sw / 2, point[1] - this._sh / 2],
        tr: [point[0] + this._sw / 2, point[1] - this._sh / 2],
        br: [point[0] + this._sw / 2, point[1] + this._sh / 2],
        bl: [point[0] - this._sw / 2, point[1] + this._sh / 2]
      };
    };
    Calendar2.prototype.pointToDate = function(point) {
      var nthX = Math.floor((point[0] - this._rect.x) / this._sw) + 1;
      var nthY = Math.floor((point[1] - this._rect.y) / this._sh) + 1;
      var range = this._rangeInfo.range;
      if (this._orient === "vertical") {
        return this._getDateByWeeksAndDay(nthY, nthX - 1, range);
      }
      return this._getDateByWeeksAndDay(nthX, nthY - 1, range);
    };
    Calendar2.prototype.convertToPixel = function(ecModel, finder, value) {
      var coordSys = getCoordSys5(finder);
      return coordSys === this ? coordSys.dataToPoint(value) : null;
    };
    Calendar2.prototype.convertToLayout = function(ecModel, finder, value) {
      var coordSys = getCoordSys5(finder);
      return coordSys === this ? coordSys.dataToLayout(value) : null;
    };
    Calendar2.prototype.convertFromPixel = function(ecModel, finder, pixel) {
      var coordSys = getCoordSys5(finder);
      return coordSys === this ? coordSys.pointToData(pixel) : null;
    };
    Calendar2.prototype.containPoint = function(point) {
      console.warn("Not implemented.");
      return false;
    };
    Calendar2.prototype._initRangeOption = function() {
      var range = this._model.get("range");
      var normalizedRange;
      if (isArray(range) && range.length === 1) {
        range = range[0];
      }
      if (!isArray(range)) {
        var rangeStr = range.toString();
        if (/^\d{4}$/.test(rangeStr)) {
          normalizedRange = [rangeStr + "-01-01", rangeStr + "-12-31"];
        }
        if (/^\d{4}[\/|-]\d{1,2}$/.test(rangeStr)) {
          var start2 = this.getDateInfo(rangeStr);
          var firstDay = start2.date;
          firstDay.setMonth(firstDay.getMonth() + 1);
          var end2 = this.getNextNDay(firstDay, -1);
          normalizedRange = [start2.formatedDate, end2.formatedDate];
        }
        if (/^\d{4}[\/|-]\d{1,2}[\/|-]\d{1,2}$/.test(rangeStr)) {
          normalizedRange = [rangeStr, rangeStr];
        }
      } else {
        normalizedRange = range;
      }
      if (!normalizedRange) {
        if (true) {
          logError("Invalid date range.");
        }
        return range;
      }
      var tmp = this._getRangeInfo(normalizedRange);
      if (tmp.start.time > tmp.end.time) {
        normalizedRange.reverse();
      }
      return normalizedRange;
    };
    Calendar2.prototype._getRangeInfo = function(range) {
      var parsedRange = [this.getDateInfo(range[0]), this.getDateInfo(range[1])];
      var reversed;
      if (parsedRange[0].time > parsedRange[1].time) {
        reversed = true;
        parsedRange.reverse();
      }
      var allDay = Math.floor(parsedRange[1].time / PROXIMATE_ONE_DAY) - Math.floor(parsedRange[0].time / PROXIMATE_ONE_DAY) + 1;
      var date = new Date(parsedRange[0].time);
      var startDateNum = date.getDate();
      var endDateNum = parsedRange[1].date.getDate();
      date.setDate(startDateNum + allDay - 1);
      var dateNum = date.getDate();
      if (dateNum !== endDateNum) {
        var sign = date.getTime() - parsedRange[1].time > 0 ? 1 : -1;
        while ((dateNum = date.getDate()) !== endDateNum && (date.getTime() - parsedRange[1].time) * sign > 0) {
          allDay -= sign;
          date.setDate(dateNum - sign);
        }
      }
      var weeks = Math.floor((allDay + parsedRange[0].day + 6) / 7);
      var nthWeek = reversed ? -weeks + 1 : weeks - 1;
      reversed && parsedRange.reverse();
      return {
        range: [parsedRange[0].formatedDate, parsedRange[1].formatedDate],
        start: parsedRange[0],
        end: parsedRange[1],
        allDay,
        weeks,
        // From 0.
        nthWeek,
        fweek: parsedRange[0].day,
        lweek: parsedRange[1].day
      };
    };
    Calendar2.prototype._getDateByWeeksAndDay = function(nthWeek, day, range) {
      var rangeInfo = this._getRangeInfo(range);
      if (nthWeek > rangeInfo.weeks || nthWeek === 0 && day < rangeInfo.fweek || nthWeek === rangeInfo.weeks && day > rangeInfo.lweek) {
        return null;
      }
      var nthDay = (nthWeek - 1) * 7 - rangeInfo.fweek + day;
      var date = new Date(rangeInfo.start.time);
      date.setDate(+rangeInfo.start.d + nthDay);
      return this.getDateInfo(date);
    };
    Calendar2.create = function(ecModel, api) {
      var calendarList = [];
      ecModel.eachComponent("calendar", function(calendarModel) {
        var calendar = new Calendar2(calendarModel, ecModel, api);
        calendarList.push(calendar);
        calendarModel.coordinateSystem = calendar;
      });
      ecModel.eachComponent(function(mainType, componentModel) {
        injectCoordSysByOption({
          targetModel: componentModel,
          coordSysType: "calendar",
          coordSysProvider: simpleCoordSysInjectionProvider
        });
      });
      return calendarList;
    };
    Calendar2.dimensions = ["time", "value"];
    return Calendar2;
  })()
);
function getCoordSys5(finder) {
  var calendarModel = finder.calendarModel;
  var seriesModel = finder.seriesModel;
  var coordSys = calendarModel ? calendarModel.coordinateSystem : seriesModel ? seriesModel.coordinateSystem : null;
  return coordSys;
}

// node_modules/echarts/lib/coord/matrix/matrixCoordHelper.js
var MatrixCellLayoutInfoType = {
  level: 1,
  leaf: 2,
  nonLeaf: 3
};
var MatrixClampOption = {
  // No clamp, be falsy, equals to null/undefined. It means if the input part is
  // null/undefined/NaN/outOfBoundary, the result part is NaN, rather than clamp to
  // the boundary of the matrix.
  none: 0,
  // Clamp, where null/undefined/NaN/outOfBoundary can be used to cover the entire row/column.
  all: 1,
  body: 2,
  corner: 3
};
function coordDataToAllCellLevelLayout(coordValue, dims, thisDimIdx) {
  var result = dims[XY2[thisDimIdx]].getCell(coordValue);
  if (!result && isNumber(coordValue) && coordValue < 0) {
    result = dims[XY2[1 - thisDimIdx]].getUnitLayoutInfo(thisDimIdx, Math.round(coordValue));
  }
  return result;
}
function resetXYLocatorRange(out2) {
  var rg = out2 || [];
  rg[0] = rg[0] || [];
  rg[1] = rg[1] || [];
  rg[0][0] = rg[0][1] = rg[1][0] = rg[1][1] = NaN;
  return rg;
}
function parseCoordRangeOption(locOut, reasonOut, data, dims, clamp2) {
  parseCoordRangeOptionOnOneDim(locOut[0], reasonOut, clamp2, data, dims, 0);
  parseCoordRangeOptionOnOneDim(locOut[1], reasonOut, clamp2, data, dims, 1);
}
function parseCoordRangeOptionOnOneDim(locDimOut, reasonOut, clamp2, data, dims, dimIdx) {
  locDimOut[0] = Infinity;
  locDimOut[1] = -Infinity;
  var dataOnDim = data[dimIdx];
  var coordValArr = isArray(dataOnDim) ? dataOnDim : [dataOnDim];
  var len2 = coordValArr.length;
  var hasClamp = !!clamp2;
  if (len2 >= 1) {
    parseCoordRangeOptionOnOneDimOnePart(locDimOut, reasonOut, coordValArr, hasClamp, dims, dimIdx, 0);
    if (len2 > 1) {
      parseCoordRangeOptionOnOneDimOnePart(locDimOut, reasonOut, coordValArr, hasClamp, dims, dimIdx, len2 - 1);
    }
  } else {
    if (true) {
      if (reasonOut) {
        reasonOut.push('Should be like [["x1", "x2"], ["y1", "y2"]], or ["x1", "y1"], rather than empty.');
      }
    }
    locDimOut[0] = locDimOut[1] = NaN;
  }
  if (hasClamp) {
    var locLowerBound = -dims[XY2[1 - dimIdx]].getLocatorCount(dimIdx);
    var locUpperBound = dims[XY2[dimIdx]].getLocatorCount(dimIdx) - 1;
    if (clamp2 === MatrixClampOption.body) {
      locLowerBound = mathMax4(0, locLowerBound);
    } else if (clamp2 === MatrixClampOption.corner) {
      locUpperBound = mathMin4(-1, locUpperBound);
    }
    if (locUpperBound < locLowerBound) {
      locLowerBound = locUpperBound = NaN;
    }
    if (eqNaN(locDimOut[0])) {
      locDimOut[0] = locLowerBound;
    }
    if (eqNaN(locDimOut[1])) {
      locDimOut[1] = locUpperBound;
    }
    locDimOut[0] = mathMax4(mathMin4(locDimOut[0], locUpperBound), locLowerBound);
    locDimOut[1] = mathMax4(mathMin4(locDimOut[1], locUpperBound), locLowerBound);
  }
}
function parseCoordRangeOptionOnOneDimOnePart(locDimOut, reasonOut, coordValArr, hasClamp, dims, dimIdx, partIdx) {
  var layout3 = coordDataToAllCellLevelLayout(coordValArr[partIdx], dims, dimIdx);
  if (!layout3) {
    if (true) {
      if (!hasClamp && reasonOut) {
        reasonOut.push("Can not find cell by coord[" + dimIdx + "][" + partIdx + "].");
      }
    }
    locDimOut[0] = locDimOut[1] = NaN;
    return;
  }
  var locatorA = layout3.id[XY2[dimIdx]];
  var locatorB = locatorA;
  var dimCell = cellLayoutInfoToDimCell(layout3);
  if (dimCell) {
    locatorB += dimCell.span[XY2[dimIdx]] - 1;
  }
  locDimOut[0] = mathMin4(locDimOut[0], locatorA, locatorB);
  locDimOut[1] = mathMax4(locDimOut[1], locatorA, locatorB);
}
function isXYLocatorRangeInvalidOnDim(locatorRange, dimIdx) {
  return eqNaN(locatorRange[dimIdx][0]) || eqNaN(locatorRange[dimIdx][1]);
}
function resolveXYLocatorRangeByCellMerge(inOutLocatorRange, outMergedMarkList, mergeDefList, mergeDefListTravelLen) {
  outMergedMarkList = outMergedMarkList || _tmpOutMergedMarkList;
  for (var idx = 0; idx < mergeDefListTravelLen; idx++) {
    outMergedMarkList[idx] = false;
  }
  while (true) {
    var expanded = false;
    for (var idx = 0; idx < mergeDefListTravelLen; idx++) {
      var mergeDef = mergeDefList[idx];
      if (!outMergedMarkList[idx] && mergeDef.cellMergeOwner && expandXYLocatorRangeIfIntersect(inOutLocatorRange, mergeDef.locatorRange)) {
        outMergedMarkList[idx] = true;
        expanded = true;
      }
    }
    if (!expanded) {
      break;
    }
  }
}
var _tmpOutMergedMarkList = [];
function expandXYLocatorRangeIfIntersect(thisLocRange, otherLocRange) {
  if (!locatorRangeIntersectOneDim(thisLocRange[0], otherLocRange[0]) || !locatorRangeIntersectOneDim(thisLocRange[1], otherLocRange[1])) {
    return false;
  }
  thisLocRange[0][0] = mathMin4(thisLocRange[0][0], otherLocRange[0][0]);
  thisLocRange[0][1] = mathMax4(thisLocRange[0][1], otherLocRange[0][1]);
  thisLocRange[1][0] = mathMin4(thisLocRange[1][0], otherLocRange[1][0]);
  thisLocRange[1][1] = mathMax4(thisLocRange[1][1], otherLocRange[1][1]);
  return true;
}
function locatorRangeIntersectOneDim(locRange1OneDim, locRange2OneDim) {
  return locRange1OneDim[1] >= locRange2OneDim[0] && locRange1OneDim[0] <= locRange2OneDim[1];
}
function fillIdSpanFromLocatorRange(owner, locatorRange) {
  owner.id.set(locatorRange[0][0], locatorRange[1][0]);
  owner.span.set(locatorRange[0][1] - owner.id.x + 1, locatorRange[1][1] - owner.id.y + 1);
}
function cloneXYLocatorRange(target, source) {
  target[0][0] = source[0][0];
  target[0][1] = source[0][1];
  target[1][0] = source[1][0];
  target[1][1] = source[1][1];
}
function xyLocatorRangeToRectOneDim(oneDimOut, locRange, dims, dimIdx) {
  var layoutMin = coordDataToAllCellLevelLayout(locRange[dimIdx][0], dims, dimIdx);
  var layoutMax = coordDataToAllCellLevelLayout(locRange[dimIdx][1], dims, dimIdx);
  oneDimOut[XY2[dimIdx]] = oneDimOut[WH2[dimIdx]] = NaN;
  if (layoutMin && layoutMax) {
    oneDimOut[XY2[dimIdx]] = layoutMin.xy;
    oneDimOut[WH2[dimIdx]] = layoutMax.xy + layoutMax.wh - layoutMin.xy;
  }
}
function setDimXYValue(out2, dimIdx, valueOnThisDim, valueOnOtherDim) {
  out2[XY2[dimIdx]] = valueOnThisDim;
  out2[XY2[1 - dimIdx]] = valueOnOtherDim;
  return out2;
}
function cellLayoutInfoToDimCell(cellLayoutInfo) {
  return cellLayoutInfo && (cellLayoutInfo.type === MatrixCellLayoutInfoType.leaf || cellLayoutInfo.type === MatrixCellLayoutInfoType.nonLeaf) ? cellLayoutInfo : null;
}
function createNaNRectLike() {
  return {
    x: NaN,
    y: NaN,
    width: NaN,
    height: NaN
  };
}

// node_modules/echarts/lib/coord/matrix/MatrixDim.js
var MatrixDim = (
  /** @class */
  (function() {
    function MatrixDim2(dim, dimModel) {
      this._cells = [];
      this._levels = [];
      this.dim = dim;
      this.dimIdx = dim === "x" ? 0 : 1;
      this._model = dimModel;
      this._uniqueValueGen = createUniqueValueGenerator(dim);
      var dimModelData = dimModel.get("data", true);
      var length = dimModel.get("length", true);
      if (dimModelData != null && !isArray(dimModelData)) {
        if (true) {
          error("Illegal echarts option - matrix." + this.dim + ".data must be an array if specified.");
        }
        dimModelData = [];
      }
      if (dimModelData) {
        this._initByDimModelData(dimModelData);
      } else if (length != null) {
        dimModelData = Array(length);
        for (var i2 = 0; i2 < length; i2++) {
          dimModelData[i2] = null;
        }
        this._initByDimModelData(dimModelData);
      } else {
        this._initBySeriesData();
      }
    }
    MatrixDim2.prototype._initByDimModelData = function(dimModelData) {
      var self2 = this;
      var _cells = self2._cells;
      var _levels = self2._levels;
      var sameLocatorCellsLists = [];
      var _cellCount = 0;
      self2._leavesCount = traverseInitCells(dimModelData, 0, 0);
      postInitCells();
      return;
      function traverseInitCells(dimModelData2, firstLeafLocator, level) {
        var totalSpan = 0;
        if (!dimModelData2) {
          return totalSpan;
        }
        each(dimModelData2, function(option, optionIdx) {
          var invalidOption = false;
          var cellOption;
          if (isString(option)) {
            cellOption = {
              value: option
            };
          } else if (isObject(option)) {
            cellOption = option;
            if (option.value != null && !isString(option.value)) {
              invalidOption = true;
              cellOption = {
                value: null
              };
            }
          } else {
            cellOption = {
              value: null
            };
            if (option != null) {
              invalidOption = true;
            }
          }
          if (invalidOption) {
            if (true) {
              error("Illegal echarts option - matrix." + self2.dim + ".data[" + optionIdx + "] must be `string | {value: string}`.");
            }
          }
          var cell = {
            type: MatrixCellLayoutInfoType.nonLeaf,
            ordinal: NaN,
            level,
            firstLeafLocator,
            id: new Point_default(),
            span: setDimXYValue(new Point_default(), self2.dimIdx, 1, 1),
            option: cellOption,
            xy: NaN,
            wh: NaN,
            dim: self2,
            rect: createNaNRectLike()
          };
          _cellCount++;
          (sameLocatorCellsLists[firstLeafLocator] || (sameLocatorCellsLists[firstLeafLocator] = [])).push(cell);
          if (!_levels[level]) {
            _levels[level] = {
              type: MatrixCellLayoutInfoType.level,
              xy: NaN,
              wh: NaN,
              option: null,
              id: new Point_default(),
              dim: self2
            };
          }
          var childrenSpan = traverseInitCells(cellOption.children, firstLeafLocator, level + 1);
          var subSpan = Math.max(1, childrenSpan);
          cell.span[XY2[self2.dimIdx]] = subSpan;
          totalSpan += subSpan;
          firstLeafLocator += subSpan;
        });
        return totalSpan;
      }
      function postInitCells() {
        var categories = [];
        while (_cells.length < _cellCount) {
          for (var locator = 0; locator < sameLocatorCellsLists.length; locator++) {
            var cell = sameLocatorCellsLists[locator].pop();
            if (cell) {
              cell.ordinal = categories.length;
              var val = cell.option.value;
              categories.push(val);
              _cells.push(cell);
              self2._uniqueValueGen.calcDupBase(val);
            }
          }
        }
        self2._uniqueValueGen.ensureValueUnique(categories, _cells);
        var ordinalMeta = self2._ordinalMeta = new OrdinalMeta_default({
          categories,
          needCollect: false,
          deduplication: false
        });
        self2._scale = new Ordinal_default({
          ordinalMeta
        });
        for (var idx = 0; idx < self2._leavesCount; idx++) {
          var leaf = self2._cells[idx];
          leaf.type = MatrixCellLayoutInfoType.leaf;
          leaf.span[XY2[1 - self2.dimIdx]] = self2._levels.length - leaf.level;
        }
        self2._initCellsId();
        self2._initLevelIdOptions();
      }
    };
    MatrixDim2.prototype._initBySeriesData = function() {
      var self2 = this;
      self2._leavesCount = 0;
      self2._levels = [{
        type: MatrixCellLayoutInfoType.level,
        xy: NaN,
        wh: NaN,
        option: null,
        id: new Point_default(),
        dim: self2
      }];
      self2._initLevelIdOptions();
      var ordinalMeta = self2._ordinalMeta = new OrdinalMeta_default({
        needCollect: true,
        deduplication: true,
        onCollect: function(value, ordinalNumber) {
          var cell = self2._cells[ordinalNumber] = {
            type: MatrixCellLayoutInfoType.leaf,
            ordinal: ordinalNumber,
            level: 0,
            firstLeafLocator: ordinalNumber,
            id: new Point_default(),
            span: setDimXYValue(new Point_default(), self2.dimIdx, 1, 1),
            // Theoretically `value` is from `dataset` or `series.data`, so it may be any type.
            // Do not restrict this case for user's convenience, and here simply convert it to
            // string for display.
            option: {
              value: value + ""
            },
            xy: NaN,
            wh: NaN,
            dim: self2,
            rect: createNaNRectLike()
          };
          self2._leavesCount++;
          self2._setCellId(cell);
        }
      });
      self2._scale = new Ordinal_default({
        ordinalMeta
      });
    };
    MatrixDim2.prototype._setCellId = function(cell) {
      var levelsLen = this._levels.length;
      var dimIdx = this.dimIdx;
      setDimXYValue(cell.id, dimIdx, cell.firstLeafLocator, cell.level - levelsLen);
    };
    MatrixDim2.prototype._initCellsId = function() {
      var levelsLen = this._levels.length;
      var dimIdx = this.dimIdx;
      each(this._cells, function(cell) {
        setDimXYValue(cell.id, dimIdx, cell.firstLeafLocator, cell.level - levelsLen);
      });
    };
    MatrixDim2.prototype._initLevelIdOptions = function() {
      var levelsLen = this._levels.length;
      var dimIdx = this.dimIdx;
      var levelOptionList = this._model.get("levels", true);
      levelOptionList = isArray(levelOptionList) ? levelOptionList : [];
      each(this._levels, function(levelCfg, level) {
        setDimXYValue(levelCfg.id, dimIdx, 0, level - levelsLen);
        levelCfg.option = levelOptionList[level];
      });
    };
    MatrixDim2.prototype.shouldShow = function() {
      return !!this._model.getShallow("show", true);
    };
    MatrixDim2.prototype.resetLayoutIterator = function(it, dimIdx, startLocator, count2) {
      it = it || new ListIterator();
      if (dimIdx === this.dimIdx) {
        var len2 = this._leavesCount;
        var startIdx = startLocator != null ? Math.max(0, startLocator) : 0;
        count2 = count2 != null ? Math.min(count2, len2) : len2;
        it.reset(this._cells, startIdx, startIdx + count2);
      } else {
        var len2 = this._levels.length;
        var startIdx = startLocator != null ? Math.max(0, startLocator + len2) : 0;
        count2 = count2 != null ? Math.min(count2, len2) : len2;
        it.reset(this._levels, startIdx, startIdx + count2);
      }
      return it;
    };
    MatrixDim2.prototype.resetCellIterator = function(it) {
      return (it || new ListIterator()).reset(this._cells, 0);
    };
    MatrixDim2.prototype.resetLevelIterator = function(it) {
      return (it || new ListIterator()).reset(this._levels, 0);
    };
    MatrixDim2.prototype.getLayout = function(outRect, dimIdx, locator) {
      var layout3 = this.getUnitLayoutInfo(dimIdx, locator);
      outRect[XY2[dimIdx]] = layout3 ? layout3.xy : NaN;
      outRect[WH2[dimIdx]] = layout3 ? layout3.wh : NaN;
    };
    MatrixDim2.prototype.getUnitLayoutInfo = function(dimIdx, locator) {
      return dimIdx === this.dimIdx ? locator < this._leavesCount ? this._cells[locator] : void 0 : this._levels[locator + this._levels.length];
    };
    MatrixDim2.prototype.getCell = function(value) {
      var ordinal = this._scale.parse(value);
      return eqNaN(ordinal) ? void 0 : this._cells[ordinal];
    };
    MatrixDim2.prototype.getLocatorCount = function(dimIdx) {
      return dimIdx === this.dimIdx ? this._leavesCount : this._levels.length;
    };
    MatrixDim2.prototype.getOrdinalMeta = function() {
      return this._ordinalMeta;
    };
    return MatrixDim2;
  })()
);
function createUniqueValueGenerator(dim) {
  var dimUpper = dim.toUpperCase();
  var defaultValReg = new RegExp("^" + dimUpper + "([0-9]+)$");
  var dupBase = 0;
  function calcDupBase(val) {
    var matchResult;
    if (val != null && (matchResult = val.match(defaultValReg))) {
      dupBase = mathMax4(dupBase, +matchResult[1] + 1);
    }
  }
  function makeUniqueValue() {
    return "" + dimUpper + dupBase++;
  }
  function ensureValueUnique(categories, cells) {
    var cateMap = createHashMap();
    for (var idx = 0; idx < categories.length; idx++) {
      var value = categories[idx];
      if (value == null || cateMap.get(value) != null) {
        categories[idx] = value = makeUniqueValue();
        cells[idx].option = defaults({
          value
        }, cells[idx].option);
      }
      cateMap.set(value, true);
    }
  }
  return {
    calcDupBase,
    ensureValueUnique
  };
}

// node_modules/echarts/lib/coord/matrix/MatrixBodyCorner.js
var MatrixBodyCorner = (
  /** @class */
  (function() {
    function MatrixBodyCorner2(kind, bodyOrCornerModel, dims) {
      this._model = bodyOrCornerModel;
      this._dims = dims;
      this._kind = kind;
      this._cellMergeOwnerList = [];
    }
    MatrixBodyCorner2.prototype._ensureCellMap = function() {
      var self2 = this;
      var _cellMap = self2._cellMap;
      if (!_cellMap) {
        _cellMap = self2._cellMap = createHashMap();
        fillCellMap();
      }
      return _cellMap;
      function fillCellMap() {
        var parsedList = [];
        var cellOptionList = self2._model.getShallow("data");
        if (cellOptionList && !isArray(cellOptionList)) {
          if (true) {
            error("matrix." + cellOptionList + ".data must be an array if specified.");
          }
          cellOptionList = null;
        }
        each(cellOptionList, function(option, idx2) {
          if (!isObject(option) || !isArray(option.coord)) {
            if (true) {
              error("Illegal matrix." + self2._kind + ".data[" + idx2 + "], must be a {coord: [...], ...}");
            }
            return;
          }
          var locatorRange2 = resetXYLocatorRange([]);
          var reasonArr = null;
          if (true) {
            reasonArr = [];
          }
          parseCoordRangeOption(locatorRange2, reasonArr, option.coord, self2._dims, option.coordClamp ? MatrixClampOption[self2._kind] : MatrixClampOption.none);
          if (isXYLocatorRangeInvalidOnDim(locatorRange2, 0) || isXYLocatorRangeInvalidOnDim(locatorRange2, 1)) {
            if (true) {
              error("Can not determine cells by option matrix." + self2._kind + ".data[" + idx2 + "]: " + ("" + reasonArr.join(" ")));
            }
            return;
          }
          var cellMergeOwner = option && option.mergeCells;
          var parsed2 = {
            id: new Point_default(),
            span: new Point_default(),
            locatorRange: locatorRange2,
            option,
            cellMergeOwner
          };
          fillIdSpanFromLocatorRange(parsed2, locatorRange2);
          parsedList.push(parsed2);
        });
        var mergedMarkList = [];
        for (var parsedIdx = 0; parsedIdx < parsedList.length; parsedIdx++) {
          var parsed = parsedList[parsedIdx];
          if (!parsed.cellMergeOwner) {
            continue;
          }
          var locatorRange = parsed.locatorRange;
          resolveXYLocatorRangeByCellMerge(locatorRange, mergedMarkList, parsedList, parsedIdx);
          for (var idx = 0; idx < parsedIdx; idx++) {
            if (mergedMarkList[idx]) {
              parsedList[idx].cellMergeOwner = false;
            }
          }
          if (locatorRange[0][0] !== parsed.id.x || locatorRange[1][0] !== parsed.id.y) {
            parsed.cellMergeOwner = false;
            var newOption = extend({}, parsed.option);
            newOption.coord = null;
            var newParsed = {
              id: new Point_default(),
              span: new Point_default(),
              locatorRange,
              option: newOption,
              cellMergeOwner: true
            };
            fillIdSpanFromLocatorRange(newParsed, locatorRange);
            parsedList.push(newParsed);
          }
        }
        each(parsedList, function(parsed2) {
          var topLeftCell = ensureBodyOrCornerCell(parsed2.id.x, parsed2.id.y);
          if (parsed2.cellMergeOwner) {
            topLeftCell.cellMergeOwner = true;
            topLeftCell.span = parsed2.span;
            topLeftCell.locatorRange = parsed2.locatorRange;
            topLeftCell.spanRect = createNaNRectLike();
            self2._cellMergeOwnerList.push(topLeftCell);
          }
          if (!parsed2.cellMergeOwner && !parsed2.option) {
            return;
          }
          for (var yidx = 0; yidx < parsed2.span.y; yidx++) {
            for (var xidx = 0; xidx < parsed2.span.x; xidx++) {
              var cell = ensureBodyOrCornerCell(parsed2.id.x + xidx, parsed2.id.y + yidx);
              cell.option = parsed2.option;
              if (parsed2.cellMergeOwner) {
                cell.inSpanOf = topLeftCell;
              }
            }
          }
        });
      }
      function ensureBodyOrCornerCell(x, y) {
        var key2 = makeCellMapKey(x, y);
        var cell = _cellMap.get(key2);
        if (!cell) {
          cell = _cellMap.set(key2, {
            id: new Point_default(x, y),
            option: null,
            inSpanOf: null,
            span: null,
            spanRect: null,
            locatorRange: null,
            cellMergeOwner: false
          });
        }
        return cell;
      }
    };
    MatrixBodyCorner2.prototype.getCell = function(xy) {
      return this._ensureCellMap().get(makeCellMapKey(xy[0], xy[1]));
    };
    MatrixBodyCorner2.prototype.travelExistingCells = function(cb) {
      this._ensureCellMap().each(cb);
    };
    MatrixBodyCorner2.prototype.expandRangeByCellMerge = function(locatorRange) {
      if (!isXYLocatorRangeInvalidOnDim(locatorRange, 0) && !isXYLocatorRangeInvalidOnDim(locatorRange, 1) && locatorRange[0][0] === locatorRange[0][1] && locatorRange[1][0] === locatorRange[1][1]) {
        _tmpERBCMLocator[0] = locatorRange[0][0];
        _tmpERBCMLocator[1] = locatorRange[1][0];
        var cell = this.getCell(_tmpERBCMLocator);
        var inSpanOf = cell && cell.inSpanOf;
        if (inSpanOf) {
          cloneXYLocatorRange(locatorRange, inSpanOf.locatorRange);
          return;
        }
      }
      var list = this._cellMergeOwnerList;
      resolveXYLocatorRangeByCellMerge(locatorRange, null, list, list.length);
    };
    return MatrixBodyCorner2;
  })()
);
var _tmpERBCMLocator = [];
function makeCellMapKey(x, y) {
  return x + "|" + y;
}

// node_modules/echarts/lib/coord/matrix/MatrixModel.js
var defaultLabelOption = {
  show: true,
  color: tokens_default.color.secondary,
  // overflow: 'truncate',
  overflow: "break",
  lineOverflow: "truncate",
  padding: [2, 3, 2, 3],
  // Prefer to use `padding`, rather than distance.
  distance: 0
};
function makeDefaultCellItemStyleOption(isCorner) {
  return {
    color: "none",
    borderWidth: 1,
    borderColor: isCorner ? "none" : tokens_default.color.borderTint
  };
}
var defaultDimOption = {
  show: true,
  label: defaultLabelOption,
  itemStyle: makeDefaultCellItemStyleOption(false),
  silent: void 0,
  dividerLineStyle: {
    width: 1,
    color: tokens_default.color.border
  }
};
var defaultBodyOption = {
  label: defaultLabelOption,
  itemStyle: makeDefaultCellItemStyleOption(false),
  silent: void 0
};
var defaultCornerOption = {
  label: defaultLabelOption,
  itemStyle: makeDefaultCellItemStyleOption(true),
  silent: void 0
};
var defaultMatrixOption = {
  // As a most basic coord sys, `z` should be lower than
  // other series and coord sys, such as, grid.
  z: -50,
  left: "10%",
  top: "10%",
  right: "10%",
  bottom: "10%",
  x: defaultDimOption,
  y: defaultDimOption,
  body: defaultBodyOption,
  corner: defaultCornerOption,
  backgroundStyle: {
    color: "none",
    borderColor: tokens_default.color.axisLine,
    borderWidth: 1
  },
  triggerEvent: false
};
var MatrixModel = (
  /** @class */
  (function(_super) {
    __extends(MatrixModel2, _super);
    function MatrixModel2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = MatrixModel2.type;
      return _this;
    }
    MatrixModel2.prototype.optionUpdated = function() {
      var dimModels = this._dimModels = {
        // Do not use matrixModel as the parent model, for preventing from cascade-fetching options to it.
        x: new MatrixDimensionModel(this.get("x", true) || {}),
        y: new MatrixDimensionModel(this.get("y", true) || {})
      };
      dimModels.x.option.type = dimModels.y.option.type = "category";
      var xDim2 = dimModels.x.dim = new MatrixDim("x", dimModels.x);
      var yDim2 = dimModels.y.dim = new MatrixDim("y", dimModels.y);
      var dims = {
        x: xDim2,
        y: yDim2
      };
      this._body = new MatrixBodyCorner("body", new Model_default(this.getShallow("body")), dims);
      this._corner = new MatrixBodyCorner("corner", new Model_default(this.getShallow("corner")), dims);
    };
    MatrixModel2.prototype.getDimensionModel = function(dim) {
      return this._dimModels[dim];
    };
    MatrixModel2.prototype.getBody = function() {
      return this._body;
    };
    MatrixModel2.prototype.getCorner = function() {
      return this._corner;
    };
    MatrixModel2.type = "matrix";
    MatrixModel2.layoutMode = "box";
    MatrixModel2.defaultOption = defaultMatrixOption;
    return MatrixModel2;
  })(Component_default)
);
var MatrixDimensionModel = (
  /** @class */
  (function(_super) {
    __extends(MatrixDimensionModel2, _super);
    function MatrixDimensionModel2() {
      return _super !== null && _super.apply(this, arguments) || this;
    }
    MatrixDimensionModel2.prototype.getOrdinalMeta = function() {
      return this.dim.getOrdinalMeta();
    };
    return MatrixDimensionModel2;
  })(Model_default)
);

// node_modules/echarts/lib/component/matrix/MatrixView.js
var round5 = Math.round;
var Z2_BACKGROUND = 0;
var Z2_OUTER_BORDER = 99;
var Z2_BODY_CORNER_CELL_DEFAULT = {
  normal: 25,
  special: 100
};
var Z2_DIMENSION_CELL_DEFAULT = {
  normal: 50,
  special: 125
};
var MatrixView = (
  /** @class */
  (function(_super) {
    __extends(MatrixView2, _super);
    function MatrixView2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = MatrixView2.type;
      return _this;
    }
    MatrixView2.prototype.render = function(matrixModel, ecModel) {
      this.group.removeAll();
      var group2 = this.group;
      var coordSys = matrixModel.coordinateSystem;
      var rect = coordSys.getRect();
      var xDimModel = matrixModel.getDimensionModel("x");
      var yDimModel = matrixModel.getDimensionModel("y");
      var xDim2 = xDimModel.dim;
      var yDim2 = yDimModel.dim;
      renderDimensionCells(group2, matrixModel, ecModel);
      createBodyAndCorner(group2, matrixModel, xDim2, yDim2, ecModel);
      var borderZ2Option = matrixModel.getShallow("borderZ2", true);
      var outerBorderZ2 = retrieve2(borderZ2Option, Z2_OUTER_BORDER);
      var dividerLineZ2 = outerBorderZ2 - 1;
      var bgStyle = matrixModel.getModel("backgroundStyle").getItemStyle(["borderWidth"]);
      bgStyle.lineWidth = 0;
      var borderStyle = matrixModel.getModel("backgroundStyle").getItemStyle(["color", "decal", "shadowColor", "shadowBlur", "shadowOffsetX", "shadowOffsetY"]);
      borderStyle.fill = "none";
      var bgRect = createMatrixRect(rect.clone(), bgStyle, Z2_BACKGROUND);
      var borderRect = createMatrixRect(rect.clone(), borderStyle, outerBorderZ2);
      bgRect.silent = true;
      borderRect.silent = true;
      group2.add(bgRect);
      group2.add(borderRect);
      var xDimCell0 = xDim2.getUnitLayoutInfo(0, 0);
      var yDimCell0 = yDim2.getUnitLayoutInfo(1, 0);
      if (xDimCell0 && yDimCell0) {
        if (xDim2.shouldShow()) {
          group2.add(createMatrixLine({
            x1: rect.x,
            y1: yDimCell0.xy,
            x2: rect.x + rect.width,
            y2: yDimCell0.xy
          }, xDimModel.getModel("dividerLineStyle").getLineStyle(), dividerLineZ2));
        }
        if (yDim2.shouldShow()) {
          group2.add(createMatrixLine({
            x1: xDimCell0.xy,
            y1: rect.y,
            x2: xDimCell0.xy,
            y2: rect.y + rect.height
          }, yDimModel.getModel("dividerLineStyle").getLineStyle(), dividerLineZ2));
        }
      }
    };
    MatrixView2.type = "matrix";
    return MatrixView2;
  })(Component_default2)
);
function renderDimensionCells(group2, matrixModel, ecModel) {
  renderOnDimension(0);
  renderOnDimension(1);
  function renderOnDimension(dimIdx) {
    var thisDimModel = matrixModel.getDimensionModel(XY2[dimIdx]);
    var thisDim = thisDimModel.dim;
    if (!thisDim.shouldShow()) {
      return;
    }
    var thisDimBgStyleModel = thisDimModel.getModel("itemStyle");
    var thisDimLabelModel = thisDimModel.getModel("label");
    var tooltipOption = matrixModel.getShallow("tooltip", true);
    var xyLocator = [];
    for (var it_1 = thisDim.resetCellIterator(); it_1.next(); ) {
      var dimCell = it_1.item;
      var shape = {};
      BoundingRect_default.copy(shape, dimCell.rect);
      set(xyLocator, dimCell.id.x, dimCell.id.y);
      createMatrixCell(xyLocator, matrixModel, group2, ecModel, dimCell.option, thisDimBgStyleModel, thisDimLabelModel, thisDimModel, shape, dimCell.option.value, Z2_DIMENSION_CELL_DEFAULT, tooltipOption, dimIdx === 0 ? "x" : "y");
    }
  }
}
function createBodyAndCorner(group2, matrixModel, xDim2, yDim2, ecModel) {
  createBodyOrCornerCells("body", matrixModel.getBody(), xDim2, yDim2);
  if (xDim2.shouldShow() && yDim2.shouldShow()) {
    createBodyOrCornerCells("corner", matrixModel.getCorner(), yDim2, xDim2);
  }
  function createBodyOrCornerCells(bodyCornerOptionRoot, bodyOrCorner, dimForCoordX, dimForCoordY) {
    var parentCellModel = new Model_default(matrixModel.getShallow(bodyCornerOptionRoot, true));
    var parentItemStyleModel = parentCellModel.getModel("itemStyle");
    var parentLabelModel = parentCellModel.getModel("label");
    var itx = new ListIterator();
    var ity = new ListIterator();
    var xyLocator = [];
    var tooltipOption = matrixModel.getShallow("tooltip", true);
    for (dimForCoordY.resetLayoutIterator(ity, 1); ity.next(); ) {
      for (dimForCoordX.resetLayoutIterator(itx, 0); itx.next(); ) {
        var xLayout = itx.item;
        var yLayout = ity.item;
        set(xyLocator, xLayout.id.x, yLayout.id.y);
        var bodyCornerCell = bodyOrCorner.getCell(xyLocator);
        if (bodyCornerCell && bodyCornerCell.inSpanOf && bodyCornerCell.inSpanOf !== bodyCornerCell) {
          continue;
        }
        var shape = {};
        if (bodyCornerCell && bodyCornerCell.span) {
          BoundingRect_default.copy(shape, bodyCornerCell.spanRect);
        } else {
          xLayout.dim.getLayout(shape, 0, xyLocator[0]);
          yLayout.dim.getLayout(shape, 1, xyLocator[1]);
        }
        var bodyCornerCellOption = bodyCornerCell ? bodyCornerCell.option : null;
        createMatrixCell(xyLocator, matrixModel, group2, ecModel, bodyCornerCellOption, parentItemStyleModel, parentLabelModel, parentCellModel, shape, bodyCornerCellOption ? bodyCornerCellOption.value : null, Z2_BODY_CORNER_CELL_DEFAULT, tooltipOption, bodyCornerOptionRoot);
      }
    }
  }
}
function createMatrixCell(xyLocator, matrixModel, group2, ecModel, cellOption, parentItemStyleModel, parentLabelModel, parentCellModel, shape, textValue, zrCellDefault, tooltipOption, targetType) {
  var _a2;
  _tmpCellItemStyleModel.option = cellOption ? cellOption.itemStyle : null;
  _tmpCellItemStyleModel.parentModel = parentItemStyleModel;
  _tmpCellModel.option = cellOption;
  _tmpCellModel.parentModel = parentCellModel;
  var z2 = retrieve2(_tmpCellModel.getShallow("z2"), cellOption && cellOption.itemStyle ? zrCellDefault.special : zrCellDefault.normal);
  var tooltipOptionShow = tooltipOption && tooltipOption.show;
  var cellRect = createMatrixRect(shape, _tmpCellItemStyleModel.getItemStyle(), z2);
  group2.add(cellRect);
  var cursorOption = _tmpCellModel.get("cursor");
  if (cursorOption != null) {
    cellRect.attr("cursor", cursorOption);
  }
  var cellText;
  if (textValue != null) {
    var text = textValue + "";
    _tmpCellLabelModel.option = cellOption ? cellOption.label : null;
    _tmpCellLabelModel.parentModel = parentLabelModel;
    _tmpCellLabelModel.ecModel = ecModel;
    var formatter = _tmpCellLabelModel.getShallow("formatter");
    if (formatter) {
      var params = {
        componentType: "matrix",
        componentIndex: matrixModel.componentIndex,
        name: text,
        value: textValue,
        coord: xyLocator.slice(),
        $vars: ["name", "value", "coord"]
      };
      if (isString(formatter)) {
        text = formatTplSimple(formatter, params);
      } else if (isFunction(formatter)) {
        var formattedText = formatter(params);
        if (formattedText != null) {
          text = formattedText + "";
        }
      }
    }
    setLabelStyle(
      cellRect,
      // Currently do not support other states (`emphasis`, `select`, `blur`)
      {
        normal: _tmpCellLabelModel
      },
      {
        defaultText: text,
        autoOverflowArea: true,
        // By default based on boundingRect. But boundingRect contains borderWidth,
        // and borderWidth is half outside the cell. Thus specific `layoutRect` explicitly.
        layoutRect: clone(cellRect.shape)
      }
    );
    cellText = cellRect.getTextContent();
    if (cellText) {
      cellText.z2 = z2 + 1;
      var style2 = cellText.style;
      if (style2 && style2.overflow && style2.overflow !== "none" && style2.lineOverflow) {
        var clipShape = {};
        BoundingRect_default.copy(clipShape, shape);
        expandOrShrinkRect(clipShape, (((_a2 = cellRect.style) === null || _a2 === void 0 ? void 0 : _a2.lineWidth) || 0) / 2, true, true);
        cellRect.updateInnerText();
        cellText.getLocalTransform(_tmpInnerTextTrans);
        invert(_tmpInnerTextTrans, _tmpInnerTextTrans);
        BoundingRect_default.applyTransform(clipShape, clipShape, _tmpInnerTextTrans);
        cellText.setClipPath(new Rect_default({
          shape: clipShape
        }));
      }
    }
    setTooltipConfig({
      el: cellRect,
      componentModel: matrixModel,
      itemName: text,
      itemTooltipOption: tooltipOption,
      formatterParamsExtra: {
        xyLocator: xyLocator.slice()
      }
    });
  }
  var triggerEvent = matrixModel.get("triggerEvent", true);
  if (cellText) {
    var labelSilent = _tmpCellLabelModel.get("silent");
    if (labelSilent == null) {
      labelSilent = !(triggerEvent || tooltipOptionShow);
    }
    cellText.silent = labelSilent;
    cellText.ignoreHostSilent = true;
  }
  var rectSilent = _tmpCellModel.get("silent");
  if (rectSilent == null) {
    rectSilent = // If no background color in cell, set `rect.silent: false` will cause that only
    // the border response to mouse hovering, which is probably weird.
    // So we deliberately make rect non-interactive if `silent` is not explicitly specified,
    // even if `triggerEvent` is set as `true`.
    !cellRect.style || cellRect.style.fill === "none" || !cellRect.style.fill;
  }
  cellRect.silent = rectSilent;
  if (triggerEvent && cellRect) {
    var eventData = {
      componentType: "matrix",
      componentIndex: matrixModel.componentIndex,
      matrixIndex: matrixModel.componentIndex,
      targetType,
      name: cellText && cellText.style ? cellText.style.text : void 0,
      value: textValue,
      coord: xyLocator.slice()
    };
    getECData(cellRect).eventData = eventData;
  }
  clearTmpModel(_tmpCellModel);
  clearTmpModel(_tmpCellItemStyleModel);
  clearTmpModel(_tmpCellLabelModel);
}
var _tmpCellModel = new Model_default();
var _tmpCellItemStyleModel = new Model_default();
var _tmpCellLabelModel = new Model_default();
var _tmpInnerTextTrans = [];
function createMatrixRect(shape, style2, z2) {
  var lineWidth = style2.lineWidth;
  if (lineWidth) {
    var x2Original = shape.x + shape.width;
    var y2Original = shape.y + shape.height;
    shape.x = subPixelOptimize(shape.x, lineWidth, true);
    shape.y = subPixelOptimize(shape.y, lineWidth, true);
    shape.width = subPixelOptimize(x2Original, lineWidth, true) - shape.x;
    shape.height = subPixelOptimize(y2Original, lineWidth, true) - shape.y;
  }
  return new Rect_default({
    shape,
    style: style2,
    z2
  });
}
function createMatrixLine(shape, style2, z2) {
  var lineWidth = style2.lineWidth;
  if (lineWidth) {
    if (round5(shape.x1 * 2) === round5(shape.x2 * 2)) {
      shape.x1 = shape.x2 = subPixelOptimize(shape.x1, lineWidth, true);
    }
    if (round5(shape.y1 * 2) === round5(shape.y2 * 2)) {
      shape.y1 = shape.y2 = subPixelOptimize(shape.y1, lineWidth, true);
    }
  }
  return new Line_default({
    shape,
    style: style2,
    silent: true,
    z2
  });
}

// node_modules/echarts/lib/coord/matrix/Matrix.js
var Matrix = (
  /** @class */
  (function() {
    function Matrix2(matrixModel, ecModel, api) {
      this.dimensions = Matrix2.dimensions;
      this.type = "matrix";
      this._model = matrixModel;
      var models = this._dimModels = {
        x: matrixModel.getDimensionModel("x"),
        y: matrixModel.getDimensionModel("y")
      };
      this._dims = {
        x: models.x.dim,
        y: models.y.dim
      };
      this._resize(matrixModel, api);
    }
    Matrix2.getDimensionsInfo = function() {
      return [{
        name: "x",
        type: "ordinal"
      }, {
        name: "y",
        type: "ordinal"
      }, {
        name: "value"
      }];
    };
    Matrix2.create = function(ecModel, api) {
      var matrixList = [];
      ecModel.eachComponent("matrix", function(matrixModel) {
        var matrix = new Matrix2(matrixModel, ecModel, api);
        matrixList.push(matrix);
        matrixModel.coordinateSystem = matrix;
      });
      ecModel.eachComponent(function(mainType, componentModel) {
        injectCoordSysByOption({
          targetModel: componentModel,
          coordSysType: "matrix",
          coordSysProvider: simpleCoordSysInjectionProvider
        });
      });
      return matrixList;
    };
    Matrix2.prototype.getRect = function() {
      return this._rect;
    };
    Matrix2.prototype._resize = function(matrixModel, api) {
      var dims = this._dims;
      var dimModels = this._dimModels;
      var rect = this._rect = getLayoutRect(matrixModel.getBoxLayoutParams(), {
        width: api.getWidth(),
        height: api.getHeight()
      });
      layOutUnitsOnDimension(dimModels, dims, rect, 0);
      layOutUnitsOnDimension(dimModels, dims, rect, 1);
      layOutDimCellsRestInfoByUnit(0, dims);
      layOutDimCellsRestInfoByUnit(1, dims);
      layOutBodyCornerCellMerge(this._model.getBody(), dims);
      layOutBodyCornerCellMerge(this._model.getCorner(), dims);
    };
    Matrix2.prototype.dataToPoint = function(data, opt, out2) {
      out2 = out2 || [];
      this.dataToLayout(data, opt, _dtpOutDataToLayout);
      out2[0] = _dtpOutDataToLayout.rect.x + _dtpOutDataToLayout.rect.width / 2;
      out2[1] = _dtpOutDataToLayout.rect.y + _dtpOutDataToLayout.rect.height / 2;
      return out2;
    };
    Matrix2.prototype.dataToLayout = function(data, opt, out2) {
      var dims = this._dims;
      out2 = out2 || {};
      var outRect = out2.rect = out2.rect || {};
      outRect.x = outRect.y = outRect.width = outRect.height = NaN;
      var outLocRange = out2.matrixXYLocatorRange = resetXYLocatorRange(out2.matrixXYLocatorRange);
      if (!isArray(data)) {
        if (true) {
          error("Input data must be an array in `convertToLayout`, `convertToPixel`");
        }
        return out2;
      }
      parseCoordRangeOption(outLocRange, null, data, dims, retrieve2(opt && opt.clamp, MatrixClampOption.none));
      if (!opt || !opt.ignoreMergeCells) {
        if (!opt || opt.clamp !== MatrixClampOption.corner) {
          this._model.getBody().expandRangeByCellMerge(outLocRange);
        }
        if (!opt || opt.clamp !== MatrixClampOption.body) {
          this._model.getCorner().expandRangeByCellMerge(outLocRange);
        }
      }
      xyLocatorRangeToRectOneDim(outRect, outLocRange, dims, 0);
      xyLocatorRangeToRectOneDim(outRect, outLocRange, dims, 1);
      return out2;
    };
    Matrix2.prototype.pointToData = function(point, opt, out2) {
      var dims = this._dims;
      pointToDataOneDimPrepareCtx(_tmpCtxPointToData, 0, dims, point, opt && opt.clamp);
      pointToDataOneDimPrepareCtx(_tmpCtxPointToData, 1, dims, point, opt && opt.clamp);
      out2 = out2 || [];
      out2[0] = out2[1] = NaN;
      if (_tmpCtxPointToData.y === CtxPointToDataAreaType.inCorner && _tmpCtxPointToData.x === CtxPointToDataAreaType.inBody) {
        pointToDataOnlyHeaderFillOut(_tmpCtxPointToData, out2, 0, dims);
      } else if (_tmpCtxPointToData.x === CtxPointToDataAreaType.inCorner && _tmpCtxPointToData.y === CtxPointToDataAreaType.inBody) {
        pointToDataOnlyHeaderFillOut(_tmpCtxPointToData, out2, 1, dims);
      } else {
        pointToDataBodyCornerFillOut(_tmpCtxPointToData, out2, 0, dims);
        pointToDataBodyCornerFillOut(_tmpCtxPointToData, out2, 1, dims);
      }
      return out2;
    };
    Matrix2.prototype.convertToPixel = function(ecModel, finder, value, opt) {
      var coordSys = getCoordSys6(finder);
      return coordSys === this ? coordSys.dataToPoint(value, opt) : void 0;
    };
    Matrix2.prototype.convertToLayout = function(ecModel, finder, value, opt) {
      var coordSys = getCoordSys6(finder);
      return coordSys === this ? coordSys.dataToLayout(value, opt) : void 0;
    };
    Matrix2.prototype.convertFromPixel = function(ecModel, finder, pixel, opt) {
      var coordSys = getCoordSys6(finder);
      return coordSys === this ? coordSys.pointToData(pixel, opt) : void 0;
    };
    Matrix2.prototype.containPoint = function(point) {
      return this._rect.contain(point[0], point[1]);
    };
    Matrix2.dimensions = ["x", "y", "value"];
    return Matrix2;
  })()
);
var _dtpOutDataToLayout = {
  rect: createNaNRectLike()
};
var _ptdLevelIt = new ListIterator();
var _ptdDimCellIt = new ListIterator();
function layOutUnitsOnDimension(dimModels, dims, matrixRect, dimIdx) {
  var otherDimIdx = 1 - dimIdx;
  var thisDim = dims[XY2[dimIdx]];
  var otherDim = dims[XY2[otherDimIdx]];
  var otherDimShow = otherDim.shouldShow();
  for (var it_1 = thisDim.resetCellIterator(); it_1.next(); ) {
    it_1.item.wh = it_1.item.xy = NaN;
  }
  for (var it_2 = otherDim.resetLayoutIterator(null, dimIdx); it_2.next(); ) {
    it_2.item.wh = it_2.item.xy = NaN;
  }
  var restSize = matrixRect[WH2[dimIdx]];
  var restCellsCount = thisDim.getLocatorCount(dimIdx) + otherDim.getLocatorCount(dimIdx);
  var tmpLevelModel = new Model_default();
  for (var it_3 = otherDim.resetLevelIterator(); it_3.next(); ) {
    tmpLevelModel.option = it_3.item.option;
    tmpLevelModel.parentModel = dimModels[XY2[otherDimIdx]];
    layOutSpecified(it_3.item, otherDimShow ? tmpLevelModel.get("levelSize") : 0);
  }
  var tmpCellModel = new Model_default();
  for (var it_4 = thisDim.resetCellIterator(); it_4.next(); ) {
    if (it_4.item.type === MatrixCellLayoutInfoType.leaf) {
      tmpCellModel.option = it_4.item.option;
      tmpCellModel.parentModel = void 0;
      layOutSpecified(it_4.item, tmpCellModel.get("size"));
    }
  }
  function layOutSpecified(item, sizeOption) {
    var size = parseSizeOption(sizeOption, dimIdx, matrixRect);
    if (!eqNaN(size)) {
      item.wh = confineSize(size, restSize);
      restSize = confineSize(restSize - item.wh);
      restCellsCount--;
    }
  }
  var computedCellWH = restCellsCount ? restSize / restCellsCount : 0;
  var notAlignToBigmost = !restCellsCount && restSize >= 1;
  var currXY = matrixRect[XY2[dimIdx]];
  var maxLocator = thisDim.getLocatorCount(dimIdx) - 1;
  var it = new ListIterator();
  for (otherDim.resetLayoutIterator(it, dimIdx); it.next(); ) {
    layOutUnspecified(it.item);
  }
  for (thisDim.resetLayoutIterator(it, dimIdx); it.next(); ) {
    layOutUnspecified(it.item);
  }
  function layOutUnspecified(item) {
    if (eqNaN(item.wh)) {
      item.wh = computedCellWH;
    }
    item.xy = currXY;
    if (item.id[XY2[dimIdx]] === maxLocator && !notAlignToBigmost) {
      item.wh = matrixRect[XY2[dimIdx]] + matrixRect[WH2[dimIdx]] - item.xy;
    }
    currXY += item.wh;
  }
}
function layOutDimCellsRestInfoByUnit(dimIdx, dims) {
  for (var it_5 = dims[XY2[dimIdx]].resetCellIterator(); it_5.next(); ) {
    var dimCell = it_5.item;
    layOutRectOneDimBasedOnUnit(dimCell.rect, dimIdx, dimCell.id, dimCell.span, dims);
    layOutRectOneDimBasedOnUnit(dimCell.rect, 1 - dimIdx, dimCell.id, dimCell.span, dims);
    if (dimCell.type === MatrixCellLayoutInfoType.nonLeaf) {
      dimCell.xy = dimCell.rect[XY2[dimIdx]];
      dimCell.wh = dimCell.rect[WH2[dimIdx]];
    }
  }
}
function layOutBodyCornerCellMerge(bodyOrCorner, dims) {
  bodyOrCorner.travelExistingCells(function(cell) {
    var computedSpan = cell.span;
    if (computedSpan) {
      var layoutRect = cell.spanRect;
      var id = cell.id;
      layOutRectOneDimBasedOnUnit(layoutRect, 0, id, computedSpan, dims);
      layOutRectOneDimBasedOnUnit(layoutRect, 1, id, computedSpan, dims);
    }
  });
}
function layOutRectOneDimBasedOnUnit(outRect, dimIdx, id, span, dims) {
  outRect[WH2[dimIdx]] = 0;
  var locator = id[XY2[dimIdx]];
  var dim = locator < 0 ? dims[XY2[1 - dimIdx]] : dims[XY2[dimIdx]];
  var layoutUnit = dim.getUnitLayoutInfo(dimIdx, id[XY2[dimIdx]]);
  outRect[XY2[dimIdx]] = layoutUnit.xy;
  outRect[WH2[dimIdx]] = layoutUnit.wh;
  if (span[XY2[dimIdx]] > 1) {
    var layoutUnit2 = dim.getUnitLayoutInfo(dimIdx, id[XY2[dimIdx]] + span[XY2[dimIdx]] - 1);
    outRect[WH2[dimIdx]] = layoutUnit2.xy + layoutUnit2.wh - layoutUnit.xy;
  }
}
function parseSizeOption(sizeOption, dimIdx, matrixRect) {
  var sizeNum = parsePositionSizeOption(sizeOption, matrixRect[WH2[dimIdx]]);
  return confineSize(sizeNum, matrixRect[WH2[dimIdx]]);
}
function confineSize(sizeNum, sizeLimit) {
  return Math.max(Math.min(sizeNum, retrieve2(sizeLimit, Infinity)), 0);
}
function getCoordSys6(finder) {
  var matrixModel = finder.matrixModel;
  var seriesModel = finder.seriesModel;
  var coordSys = matrixModel ? matrixModel.coordinateSystem : seriesModel ? seriesModel.coordinateSystem : null;
  return coordSys;
}
var CtxPointToDataAreaType = {
  inBody: 1,
  inCorner: 2,
  outside: 3
};
var _tmpCtxPointToData = {
  x: null,
  y: null,
  point: []
};
function pointToDataOneDimPrepareCtx(ctx, dimIdx, dims, point, clamp2) {
  var thisDim = dims[XY2[dimIdx]];
  var otherDim = dims[XY2[1 - dimIdx]];
  var bodyMaxUnit = thisDim.getUnitLayoutInfo(dimIdx, thisDim.getLocatorCount(dimIdx) - 1);
  var body0Unit = thisDim.getUnitLayoutInfo(dimIdx, 0);
  var cornerMinUnit = otherDim.getUnitLayoutInfo(dimIdx, -otherDim.getLocatorCount(dimIdx));
  var cornerMinus1Unit = otherDim.shouldShow() ? otherDim.getUnitLayoutInfo(dimIdx, -1) : null;
  var coord = ctx.point[dimIdx] = point[dimIdx];
  if (!body0Unit && !cornerMinus1Unit) {
    ctx[XY2[dimIdx]] = CtxPointToDataAreaType.outside;
    return;
  }
  if (clamp2 === MatrixClampOption.body) {
    if (body0Unit) {
      ctx[XY2[dimIdx]] = CtxPointToDataAreaType.inBody;
      coord = mathMin4(bodyMaxUnit.xy + bodyMaxUnit.wh, mathMax4(body0Unit.xy, coord));
      ctx.point[dimIdx] = coord;
    } else {
      ctx[XY2[dimIdx]] = CtxPointToDataAreaType.outside;
    }
    return;
  } else if (clamp2 === MatrixClampOption.corner) {
    if (cornerMinus1Unit) {
      ctx[XY2[dimIdx]] = CtxPointToDataAreaType.inCorner;
      coord = mathMin4(cornerMinus1Unit.xy + cornerMinus1Unit.wh, mathMax4(cornerMinUnit.xy, coord));
      ctx.point[dimIdx] = coord;
    } else {
      ctx[XY2[dimIdx]] = CtxPointToDataAreaType.outside;
    }
    return;
  }
  var pxLoc0 = body0Unit ? body0Unit.xy : cornerMinus1Unit ? cornerMinus1Unit.xy + cornerMinus1Unit.wh : NaN;
  var pxMin = cornerMinUnit ? cornerMinUnit.xy : pxLoc0;
  var pxMax = bodyMaxUnit ? bodyMaxUnit.xy + bodyMaxUnit.wh : pxLoc0;
  if (coord < pxMin) {
    if (!clamp2) {
      ctx[XY2[dimIdx]] = CtxPointToDataAreaType.outside;
      return;
    }
    coord = pxMin;
  } else if (coord > pxMax) {
    if (!clamp2) {
      ctx[XY2[dimIdx]] = CtxPointToDataAreaType.outside;
      return;
    }
    coord = pxMax;
  }
  ctx.point[dimIdx] = coord;
  ctx[XY2[dimIdx]] = pxLoc0 <= coord && coord <= pxMax ? CtxPointToDataAreaType.inBody : pxMin <= coord && coord <= pxLoc0 ? CtxPointToDataAreaType.inCorner : CtxPointToDataAreaType.outside;
}
function pointToDataOnlyHeaderFillOut(ctx, partialOut, dimIdx, dims) {
  var otherDimIdx = 1 - dimIdx;
  if (ctx[XY2[dimIdx]] === CtxPointToDataAreaType.outside) {
    return;
  }
  for (dims[XY2[dimIdx]].resetCellIterator(_ptdDimCellIt); _ptdDimCellIt.next(); ) {
    var cell = _ptdDimCellIt.item;
    if (isCoordInRect(ctx.point[dimIdx], cell.rect, dimIdx) && isCoordInRect(ctx.point[otherDimIdx], cell.rect, otherDimIdx)) {
      partialOut[dimIdx] = cell.ordinal;
      partialOut[otherDimIdx] = cell.id[XY2[otherDimIdx]];
      return;
    }
  }
}
function pointToDataBodyCornerFillOut(ctx, partialOut, dimIdx, dims) {
  if (ctx[XY2[dimIdx]] === CtxPointToDataAreaType.outside) {
    return;
  }
  var dim = ctx[XY2[dimIdx]] === CtxPointToDataAreaType.inCorner ? dims[XY2[1 - dimIdx]] : dims[XY2[dimIdx]];
  for (dim.resetLayoutIterator(_ptdLevelIt, dimIdx); _ptdLevelIt.next(); ) {
    if (isCoordInLayoutInfo(ctx.point[dimIdx], _ptdLevelIt.item)) {
      partialOut[dimIdx] = _ptdLevelIt.item.id[XY2[dimIdx]];
      return;
    }
  }
}
function isCoordInLayoutInfo(coord, cell) {
  return cell.xy <= coord && coord <= cell.xy + cell.wh;
}
function isCoordInRect(coord, rect, dimIdx) {
  return rect[XY2[dimIdx]] <= coord && coord <= rect[XY2[dimIdx]] + rect[WH2[dimIdx]];
}

// node_modules/echarts/lib/component/graphic/GraphicModel.js
function setKeyInfoToNewElOption(resultItem, newElOption) {
  var existElOption = resultItem.existing;
  newElOption.id = resultItem.keyInfo.id;
  !newElOption.type && existElOption && (newElOption.type = existElOption.type);
  if (newElOption.parentId == null) {
    var newElParentOption = newElOption.parentOption;
    if (newElParentOption) {
      newElOption.parentId = newElParentOption.id;
    } else if (existElOption) {
      newElOption.parentId = existElOption.parentId;
    }
  }
  newElOption.parentOption = null;
}
function isSetLoc(obj, props) {
  var isSet;
  each(props, function(prop) {
    obj[prop] != null && obj[prop] !== "auto" && (isSet = true);
  });
  return isSet;
}
function mergeNewElOptionToExist(existList, index, newElOption) {
  var newElOptCopy = extend({}, newElOption);
  var existElOption = existList[index];
  var $action = newElOption.$action || "merge";
  if ($action === "merge") {
    if (existElOption) {
      if (true) {
        var newType = newElOption.type;
        assert(!newType || existElOption.type === newType, 'Please set $action: "replace" to change `type`');
      }
      merge2(existElOption, newElOptCopy, true);
      mergeLayoutParam(existElOption, newElOptCopy, {
        ignoreSize: true
      });
      copyLayoutParams(newElOption, existElOption);
      copyTransitionInfo(newElOption, existElOption);
      copyTransitionInfo(newElOption, existElOption, "shape");
      copyTransitionInfo(newElOption, existElOption, "style");
      copyTransitionInfo(newElOption, existElOption, "extra");
      newElOption.clipPath = existElOption.clipPath;
    } else {
      existList[index] = newElOptCopy;
    }
  } else if ($action === "replace") {
    existList[index] = newElOptCopy;
  } else if ($action === "remove") {
    existElOption && (existList[index] = null);
  }
}
var TRANSITION_PROPS_TO_COPY = ["transition", "enterFrom", "leaveTo"];
var ROOT_TRANSITION_PROPS_TO_COPY = TRANSITION_PROPS_TO_COPY.concat(["enterAnimation", "updateAnimation", "leaveAnimation"]);
function copyTransitionInfo(target, source, targetProp) {
  if (targetProp) {
    if (!target[targetProp] && source[targetProp]) {
      target[targetProp] = {};
    }
    target = target[targetProp];
    source = source[targetProp];
  }
  if (!target || !source) {
    return;
  }
  var props = targetProp ? TRANSITION_PROPS_TO_COPY : ROOT_TRANSITION_PROPS_TO_COPY;
  for (var i2 = 0; i2 < props.length; i2++) {
    var prop = props[i2];
    if (target[prop] == null && source[prop] != null) {
      target[prop] = source[prop];
    }
  }
}
function setLayoutInfoToExist(existItem, newElOption) {
  if (!existItem) {
    return;
  }
  existItem.hv = newElOption.hv = [
    // Rigid body, don't care about `width`.
    isSetLoc(newElOption, ["left", "right"]),
    // Rigid body, don't care about `height`.
    isSetLoc(newElOption, ["top", "bottom"])
  ];
  if (existItem.type === "group") {
    var existingGroupOpt = existItem;
    var newGroupOpt = newElOption;
    existingGroupOpt.width == null && (existingGroupOpt.width = newGroupOpt.width = 0);
    existingGroupOpt.height == null && (existingGroupOpt.height = newGroupOpt.height = 0);
  }
}
var GraphicComponentModel = (
  /** @class */
  (function(_super) {
    __extends(GraphicComponentModel2, _super);
    function GraphicComponentModel2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = GraphicComponentModel2.type;
      _this.preventAutoZ = true;
      return _this;
    }
    GraphicComponentModel2.prototype.mergeOption = function(option, ecModel) {
      var elements = this.option.elements;
      this.option.elements = null;
      _super.prototype.mergeOption.call(this, option, ecModel);
      this.option.elements = elements;
    };
    GraphicComponentModel2.prototype.optionUpdated = function(newOption, isInit) {
      var thisOption = this.option;
      var newList = (isInit ? thisOption : newOption).elements;
      var existList = thisOption.elements = isInit ? [] : thisOption.elements;
      var flattenedList = [];
      this._flatten(newList, flattenedList, null);
      var mappingResult = mappingToExists(existList, flattenedList, "normalMerge");
      var elOptionsToUpdate = this._elOptionsToUpdate = [];
      each(mappingResult, function(resultItem, index) {
        var newElOption = resultItem.newOption;
        if (true) {
          assert(isObject(newElOption) || resultItem.existing, "Empty graphic option definition");
        }
        if (!newElOption) {
          return;
        }
        elOptionsToUpdate.push(newElOption);
        setKeyInfoToNewElOption(resultItem, newElOption);
        mergeNewElOptionToExist(existList, index, newElOption);
        setLayoutInfoToExist(existList[index], newElOption);
      }, this);
      thisOption.elements = filter2(existList, function(item) {
        item && delete item.$action;
        return item != null;
      });
    };
    GraphicComponentModel2.prototype._flatten = function(optionList, result, parentOption) {
      each(optionList, function(option) {
        if (!option) {
          return;
        }
        if (parentOption) {
          option.parentOption = parentOption;
        }
        result.push(option);
        var children = option.children;
        if (children && children.length) {
          this._flatten(children, result, option);
        }
        delete option.children;
      }, this);
    };
    GraphicComponentModel2.prototype.useElOptionsToUpdate = function() {
      var els = this._elOptionsToUpdate;
      this._elOptionsToUpdate = null;
      return els;
    };
    GraphicComponentModel2.type = "graphic";
    GraphicComponentModel2.defaultOption = {
      elements: []
      // parentId: null
    };
    return GraphicComponentModel2;
  })(Component_default)
);

// node_modules/echarts/lib/component/graphic/GraphicView.js
var nonShapeGraphicElements = {
  // Reserved but not supported in graphic component.
  path: null,
  compoundPath: null,
  // Supported in graphic component.
  group: Group_default,
  image: Image_default,
  text: Text_default
};
var inner18 = makeInner();
var GraphicComponentView = (
  /** @class */
  (function(_super) {
    __extends(GraphicComponentView2, _super);
    function GraphicComponentView2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = GraphicComponentView2.type;
      return _this;
    }
    GraphicComponentView2.prototype.init = function() {
      this._elMap = createHashMap();
    };
    GraphicComponentView2.prototype.render = function(graphicModel, ecModel, api) {
      if (graphicModel !== this._lastGraphicModel) {
        this._clear();
      }
      this._lastGraphicModel = graphicModel;
      this._updateElements(graphicModel);
      this._relocate(graphicModel, api);
    };
    GraphicComponentView2.prototype._updateElements = function(graphicModel) {
      var elOptionsToUpdate = graphicModel.useElOptionsToUpdate();
      if (!elOptionsToUpdate) {
        return;
      }
      var elMap = this._elMap;
      var rootGroup = this.group;
      var globalZ = graphicModel.get("z");
      var globalZLevel = graphicModel.get("zlevel");
      each(elOptionsToUpdate, function(elOption) {
        var id = convertOptionIdName(elOption.id, null);
        var elExisting = id != null ? elMap.get(id) : null;
        var parentId = convertOptionIdName(elOption.parentId, null);
        var targetElParent = parentId != null ? elMap.get(parentId) : rootGroup;
        var elType = elOption.type;
        var elOptionStyle = elOption.style;
        if (elType === "text" && elOptionStyle) {
          if (elOption.hv && elOption.hv[1]) {
            elOptionStyle.textVerticalAlign = elOptionStyle.textBaseline = elOptionStyle.verticalAlign = elOptionStyle.align = null;
          }
        }
        var textContentOption = elOption.textContent;
        var textConfig = elOption.textConfig;
        if (elOptionStyle && isEC4CompatibleStyle(elOptionStyle, elType, !!textConfig, !!textContentOption)) {
          var convertResult = convertFromEC4CompatibleStyle(elOptionStyle, elType, true);
          if (!textConfig && convertResult.textConfig) {
            textConfig = elOption.textConfig = convertResult.textConfig;
          }
          if (!textContentOption && convertResult.textContent) {
            textContentOption = convertResult.textContent;
          }
        }
        var elOptionCleaned = getCleanedElOption(elOption);
        if (true) {
          elExisting && assert(targetElParent === elExisting.parent, "Changing parent is not supported.");
        }
        var $action = elOption.$action || "merge";
        var isMerge = $action === "merge";
        var isReplace = $action === "replace";
        if (isMerge) {
          var isInit = !elExisting;
          var el_1 = elExisting;
          if (isInit) {
            el_1 = createEl3(id, targetElParent, elOption.type, elMap);
          } else {
            el_1 && (inner18(el_1).isNew = false);
            stopPreviousKeyframeAnimationAndRestore(el_1);
          }
          if (el_1) {
            applyUpdateTransition(el_1, elOptionCleaned, graphicModel, {
              isInit
            });
            updateCommonAttrs(el_1, elOption, globalZ, globalZLevel);
          }
        } else if (isReplace) {
          removeEl(elExisting, elOption, elMap, graphicModel);
          var el_2 = createEl3(id, targetElParent, elOption.type, elMap);
          if (el_2) {
            applyUpdateTransition(el_2, elOptionCleaned, graphicModel, {
              isInit: true
            });
            updateCommonAttrs(el_2, elOption, globalZ, globalZLevel);
          }
        } else if ($action === "remove") {
          updateLeaveTo(elExisting, elOption);
          removeEl(elExisting, elOption, elMap, graphicModel);
        }
        var el = elMap.get(id);
        if (el && textContentOption) {
          if (isMerge) {
            var textContentExisting = el.getTextContent();
            textContentExisting ? textContentExisting.attr(textContentOption) : el.setTextContent(new Text_default(textContentOption));
          } else if (isReplace) {
            el.setTextContent(new Text_default(textContentOption));
          }
        }
        if (el) {
          var clipPathOption = elOption.clipPath;
          if (clipPathOption) {
            var clipPathType = clipPathOption.type;
            var clipPath = void 0;
            var isInit = false;
            if (isMerge) {
              var oldClipPath = el.getClipPath();
              isInit = !oldClipPath || inner18(oldClipPath).type !== clipPathType;
              clipPath = isInit ? newEl(clipPathType) : oldClipPath;
            } else if (isReplace) {
              isInit = true;
              clipPath = newEl(clipPathType);
            }
            el.setClipPath(clipPath);
            applyUpdateTransition(clipPath, clipPathOption, graphicModel, {
              isInit
            });
            applyKeyframeAnimation(clipPath, clipPathOption.keyframeAnimation, graphicModel);
          }
          var elInner = inner18(el);
          el.setTextConfig(textConfig);
          elInner.option = elOption;
          setEventData(el, graphicModel, elOption);
          setTooltipConfig({
            el,
            componentModel: graphicModel,
            itemName: el.name,
            itemTooltipOption: elOption.tooltip
          });
          applyKeyframeAnimation(el, elOption.keyframeAnimation, graphicModel);
        }
      });
    };
    GraphicComponentView2.prototype._relocate = function(graphicModel, api) {
      var elOptions = graphicModel.option.elements;
      var rootGroup = this.group;
      var elMap = this._elMap;
      var apiWidth = api.getWidth();
      var apiHeight = api.getHeight();
      var xy = ["x", "y"];
      for (var i2 = 0; i2 < elOptions.length; i2++) {
        var elOption = elOptions[i2];
        var id = convertOptionIdName(elOption.id, null);
        var el = id != null ? elMap.get(id) : null;
        if (!el || !el.isGroup) {
          continue;
        }
        var parentEl = el.parent;
        var isParentRoot = parentEl === rootGroup;
        var elInner = inner18(el);
        var parentElInner = inner18(parentEl);
        elInner.width = parsePercent2(elInner.option.width, isParentRoot ? apiWidth : parentElInner.width) || 0;
        elInner.height = parsePercent2(elInner.option.height, isParentRoot ? apiHeight : parentElInner.height) || 0;
      }
      for (var i2 = elOptions.length - 1; i2 >= 0; i2--) {
        var elOption = elOptions[i2];
        var id = convertOptionIdName(elOption.id, null);
        var el = id != null ? elMap.get(id) : null;
        if (!el) {
          continue;
        }
        var parentEl = el.parent;
        var parentElInner = inner18(parentEl);
        var containerInfo = parentEl === rootGroup ? {
          width: apiWidth,
          height: apiHeight
        } : {
          width: parentElInner.width,
          height: parentElInner.height
        };
        var layoutPos = {};
        var layouted = positionElement(el, elOption, containerInfo, null, {
          hv: elOption.hv,
          boundingMode: elOption.bounding
        }, layoutPos);
        if (!inner18(el).isNew && layouted) {
          var transition2 = elOption.transition;
          var animatePos = {};
          for (var k2 = 0; k2 < xy.length; k2++) {
            var key2 = xy[k2];
            var val = layoutPos[key2];
            if (transition2 && (isTransitionAll(transition2) || indexOf(transition2, key2) >= 0)) {
              animatePos[key2] = val;
            } else {
              el[key2] = val;
            }
          }
          updateProps(el, animatePos, graphicModel, 0);
        } else {
          el.attr(layoutPos);
        }
      }
    };
    GraphicComponentView2.prototype._clear = function() {
      var _this = this;
      var elMap = this._elMap;
      elMap.each(function(el) {
        removeEl(el, inner18(el).option, elMap, _this._lastGraphicModel);
      });
      this._elMap = createHashMap();
    };
    GraphicComponentView2.prototype.dispose = function() {
      this._clear();
    };
    GraphicComponentView2.type = "graphic";
    return GraphicComponentView2;
  })(Component_default2)
);
function newEl(graphicType) {
  if (true) {
    assert(graphicType, "graphic type MUST be set");
  }
  var Clz = hasOwn(nonShapeGraphicElements, graphicType) ? nonShapeGraphicElements[graphicType] : getShapeClass(graphicType);
  if (true) {
    assert(Clz, "graphic type " + graphicType + " can not be found");
  }
  var el = new Clz({});
  inner18(el).type = graphicType;
  return el;
}
function createEl3(id, targetElParent, graphicType, elMap) {
  var el = newEl(graphicType);
  targetElParent.add(el);
  elMap.set(id, el);
  inner18(el).id = id;
  inner18(el).isNew = true;
  return el;
}
function removeEl(elExisting, elOption, elMap, graphicModel) {
  var existElParent = elExisting && elExisting.parent;
  if (existElParent) {
    elExisting.type === "group" && elExisting.traverse(function(el) {
      removeEl(el, elOption, elMap, graphicModel);
    });
    applyLeaveTransition(elExisting, elOption, graphicModel);
    elMap.removeKey(inner18(elExisting).id);
  }
}
function updateCommonAttrs(el, elOption, defaultZ, defaultZlevel) {
  if (!el.isGroup) {
    each([
      ["cursor", Displayable_default.prototype.cursor],
      // We should not support configure z and zlevel in the element level.
      // But seems we didn't limit it previously. So here still use it to avoid breaking.
      ["zlevel", defaultZlevel || 0],
      ["z", defaultZ || 0],
      // z2 must not be null/undefined, otherwise sort error may occur.
      ["z2", 0]
    ], function(item) {
      var prop = item[0];
      if (hasOwn(elOption, prop)) {
        el[prop] = retrieve2(elOption[prop], item[1]);
      } else if (el[prop] == null) {
        el[prop] = item[1];
      }
    });
  }
  each(keys(elOption), function(key2) {
    if (key2.indexOf("on") === 0) {
      var val = elOption[key2];
      el[key2] = isFunction(val) ? val : null;
    }
  });
  if (hasOwn(elOption, "draggable")) {
    el.draggable = elOption.draggable;
  }
  elOption.name != null && (el.name = elOption.name);
  elOption.id != null && (el.id = elOption.id);
}
function getCleanedElOption(elOption) {
  elOption = extend({}, elOption);
  each(["id", "parentId", "$action", "hv", "bounding", "textContent", "clipPath"].concat(LOCATION_PARAMS), function(name) {
    delete elOption[name];
  });
  return elOption;
}
function setEventData(el, graphicModel, elOption) {
  var eventData = getECData(el).eventData;
  if (!el.silent && !el.ignore && !eventData) {
    eventData = getECData(el).eventData = {
      componentType: "graphic",
      componentIndex: graphicModel.componentIndex,
      name: el.name
    };
  }
  if (eventData) {
    eventData.info = elOption.info;
  }
}

// node_modules/echarts/lib/component/graphic/install.js
function install30(registers) {
  registers.registerComponentModel(GraphicComponentModel);
  registers.registerComponentView(GraphicComponentView);
  registers.registerPreprocessor(function(option) {
    var graphicOption = option.graphic;
    if (isArray(graphicOption)) {
      if (!graphicOption[0] || !graphicOption[0].elements) {
        option.graphic = [{
          elements: graphicOption
        }];
      } else {
        option.graphic = [option.graphic[0]];
      }
    } else if (graphicOption && !graphicOption.elements) {
      option.graphic = [{
        elements: [graphicOption]
      }];
    }
  });
}

// node_modules/echarts/lib/component/dataZoom/helper.js
var DATA_ZOOM_AXIS_DIMENSIONS = ["x", "y", "radius", "angle", "single"];
var ecModelCacheInner2 = makeInner();
var SERIES_COORDS = ["cartesian2d", "polar", "singleAxis"];
function isCoordSupported(seriesModel) {
  var coordType = seriesModel.get("coordinateSystem");
  return indexOf(SERIES_COORDS, coordType) >= 0;
}
function getAxisMainType(axisDim) {
  if (true) {
    assert(axisDim);
  }
  return axisDim + "Axis";
}
function findEffectedDataZooms(ecModel, payload) {
  var axisRecords = createHashMap();
  var effectedModels = [];
  var effectedModelMap = createHashMap();
  ecModel.eachComponent({
    mainType: "dataZoom",
    query: payload
  }, function(dataZoomModel) {
    if (!effectedModelMap.get(dataZoomModel.uid)) {
      addToEffected(dataZoomModel);
    }
  });
  var foundNewLink;
  do {
    foundNewLink = false;
    ecModel.eachComponent("dataZoom", processSingle);
  } while (foundNewLink);
  function processSingle(dataZoomModel) {
    if (!effectedModelMap.get(dataZoomModel.uid) && isLinked(dataZoomModel)) {
      addToEffected(dataZoomModel);
      foundNewLink = true;
    }
  }
  function addToEffected(dataZoom) {
    effectedModelMap.set(dataZoom.uid, true);
    effectedModels.push(dataZoom);
    markAxisControlled(dataZoom);
  }
  function isLinked(dataZoomModel) {
    var isLink = false;
    dataZoomModel.eachTargetAxis(function(axisDim, axisIndex) {
      var axisIdxArr = axisRecords.get(axisDim);
      if (axisIdxArr && axisIdxArr[axisIndex]) {
        isLink = true;
      }
    });
    return isLink;
  }
  function markAxisControlled(dataZoomModel) {
    dataZoomModel.eachTargetAxis(function(axisDim, axisIndex) {
      (axisRecords.get(axisDim) || axisRecords.set(axisDim, []))[axisIndex] = true;
    });
  }
  return effectedModels;
}
function collectReferCoordSysModelInfo(dataZoomModel) {
  var ecModel = dataZoomModel.ecModel;
  var coordSysInfoWrap = {
    infoList: [],
    infoMap: createHashMap()
  };
  dataZoomModel.eachTargetAxis(function(axisDim, axisIndex) {
    var axisModel = ecModel.getComponent(getAxisMainType(axisDim), axisIndex);
    if (!axisModel) {
      return;
    }
    var coordSysModel = axisModel.getCoordSysModel();
    if (!coordSysModel) {
      return;
    }
    var coordSysUid = coordSysModel.uid;
    var coordSysInfo = coordSysInfoWrap.infoMap.get(coordSysUid);
    if (!coordSysInfo) {
      coordSysInfo = {
        model: coordSysModel,
        axisModels: []
      };
      coordSysInfoWrap.infoList.push(coordSysInfo);
      coordSysInfoWrap.infoMap.set(coordSysUid, coordSysInfo);
    }
    coordSysInfo.axisModels.push(axisModel);
  });
  return coordSysInfoWrap;
}
function ensureAxisProxyMap(ecModel) {
  var store = ecModelCacheInner2(getCachePerECPrepare(ecModel));
  return store.axisProxyMap || (store.axisProxyMap = createHashMap());
}
function getAxisProxyFromModel(axisModel) {
  if (!axisModel) {
    return;
  }
  if (true) {
    assert(axisModel.ecModel);
  }
  return ensureAxisProxyMap(axisModel.ecModel).get(axisModel.uid);
}
function setAxisProxyToModel(axisModel, axisProxy) {
  if (true) {
    assert(axisModel.ecModel);
  }
  ensureAxisProxyMap(axisModel.ecModel).set(axisModel.uid, axisProxy);
}
function getAlignTo(dataZoomModel, axisProxy) {
  var alignToAxis = axisProxy.getAxisModel().axis.__alignTo;
  return alignToAxis && dataZoomModel.getAxisProxy(alignToAxis.dim, alignToAxis.model.componentIndex) ? getAxisProxyFromModel(alignToAxis.model) : null;
}

// node_modules/echarts/lib/component/dataZoom/DataZoomModel.js
var DataZoomAxisInfo = (
  /** @class */
  (function() {
    function DataZoomAxisInfo2() {
      this.indexList = [];
      this.indexMap = [];
    }
    DataZoomAxisInfo2.prototype.add = function(axisCmptIdx) {
      if (!this.indexMap[axisCmptIdx]) {
        this.indexList.push(axisCmptIdx);
        this.indexMap[axisCmptIdx] = true;
      }
    };
    return DataZoomAxisInfo2;
  })()
);
var DataZoomModel = (
  /** @class */
  (function(_super) {
    __extends(DataZoomModel2, _super);
    function DataZoomModel2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = DataZoomModel2.type;
      _this._autoThrottle = true;
      _this._noTarget = true;
      _this._rangePropMode = ["percent", "percent"];
      return _this;
    }
    DataZoomModel2.prototype.init = function(option, parentModel, ecModel) {
      var inputRawOption = retrieveRawOption(option);
      this.settledOption = inputRawOption;
      this.mergeDefaultAndTheme(option, ecModel);
      this._doInit(inputRawOption);
    };
    DataZoomModel2.prototype.mergeOption = function(newOption) {
      var inputRawOption = retrieveRawOption(newOption);
      merge2(this.option, newOption, true);
      merge2(this.settledOption, inputRawOption, true);
      this._doInit(inputRawOption);
    };
    DataZoomModel2.prototype._doInit = function(inputRawOption) {
      var thisOption = this.option;
      this._setDefaultThrottle(inputRawOption);
      this._updateRangeUse(inputRawOption);
      var settledOption = this.settledOption;
      each([["start", "startValue"], ["end", "endValue"]], function(names, index) {
        if (this._rangePropMode[index] === "value") {
          thisOption[names[0]] = settledOption[names[0]] = null;
        }
      }, this);
      this._resetTarget();
    };
    DataZoomModel2.prototype._resetTarget = function() {
      var optionOrient = this.get("orient", true);
      var targetAxisIndexMap = this._targetAxisInfoMap = createHashMap();
      var hasAxisSpecified = this._fillSpecifiedTargetAxis(targetAxisIndexMap);
      if (hasAxisSpecified) {
        this._orient = optionOrient || this._makeAutoOrientByTargetAxis();
      } else {
        this._orient = optionOrient || "horizontal";
        this._fillAutoTargetAxisByOrient(targetAxisIndexMap, this._orient);
      }
      this._noTarget = true;
      targetAxisIndexMap.each(function(axisInfo) {
        if (axisInfo.indexList.length) {
          this._noTarget = false;
        }
      }, this);
    };
    DataZoomModel2.prototype._fillSpecifiedTargetAxis = function(targetAxisIndexMap) {
      var hasAxisSpecified = false;
      each(DATA_ZOOM_AXIS_DIMENSIONS, function(axisDim) {
        var refering = this.getReferringComponents(getAxisMainType(axisDim), MULTIPLE_REFERRING);
        if (!refering.specified) {
          return;
        }
        hasAxisSpecified = true;
        var axisInfo = new DataZoomAxisInfo();
        each(refering.models, function(axisModel) {
          axisInfo.add(axisModel.componentIndex);
        });
        targetAxisIndexMap.set(axisDim, axisInfo);
      }, this);
      return hasAxisSpecified;
    };
    DataZoomModel2.prototype._fillAutoTargetAxisByOrient = function(targetAxisIndexMap, orient) {
      var ecModel = this.ecModel;
      var needAuto = true;
      if (needAuto) {
        var axisDim = orient === "vertical" ? "y" : "x";
        var axisModels = ecModel.findComponents({
          mainType: axisDim + "Axis"
        });
        setParallelAxis(axisModels, axisDim);
      }
      if (needAuto) {
        var axisModels = ecModel.findComponents({
          mainType: "singleAxis",
          filter: function(axisModel) {
            return axisModel.get("orient", true) === orient;
          }
        });
        setParallelAxis(axisModels, "single");
      }
      function setParallelAxis(axisModels2, axisDim2) {
        var axisModel = axisModels2[0];
        if (!axisModel) {
          return;
        }
        var axisInfo = new DataZoomAxisInfo();
        axisInfo.add(axisModel.componentIndex);
        targetAxisIndexMap.set(axisDim2, axisInfo);
        needAuto = false;
        if (axisDim2 === "x" || axisDim2 === "y") {
          var gridModel_1 = axisModel.getReferringComponents("grid", SINGLE_REFERRING).models[0];
          gridModel_1 && each(axisModels2, function(axModel) {
            if (axisModel.componentIndex !== axModel.componentIndex && gridModel_1 === axModel.getReferringComponents("grid", SINGLE_REFERRING).models[0]) {
              axisInfo.add(axModel.componentIndex);
            }
          });
        }
      }
      if (needAuto) {
        each(DATA_ZOOM_AXIS_DIMENSIONS, function(axisDim2) {
          if (!needAuto) {
            return;
          }
          var axisModels2 = ecModel.findComponents({
            mainType: getAxisMainType(axisDim2),
            filter: function(axisModel) {
              return axisModel.get("type", true) === "category";
            }
          });
          if (axisModels2[0]) {
            var axisInfo = new DataZoomAxisInfo();
            axisInfo.add(axisModels2[0].componentIndex);
            targetAxisIndexMap.set(axisDim2, axisInfo);
            needAuto = false;
          }
        }, this);
      }
    };
    DataZoomModel2.prototype._makeAutoOrientByTargetAxis = function() {
      var dim;
      this.eachTargetAxis(function(axisDim) {
        !dim && (dim = axisDim);
      }, this);
      return dim === "y" ? "vertical" : "horizontal";
    };
    DataZoomModel2.prototype._setDefaultThrottle = function(inputRawOption) {
      if (inputRawOption.hasOwnProperty("throttle")) {
        this._autoThrottle = false;
      }
      if (this._autoThrottle) {
        var globalOption = this.ecModel.option;
        this.option.throttle = globalOption.animation && globalOption.animationDurationUpdate > 0 ? 100 : 20;
      }
    };
    DataZoomModel2.prototype._updateRangeUse = function(inputRawOption) {
      var rangePropMode = this._rangePropMode;
      var rangeModeInOption = this.get("rangeMode");
      each([["start", "startValue"], ["end", "endValue"]], function(names, index) {
        var percentSpecified = inputRawOption[names[0]] != null;
        var valueSpecified = inputRawOption[names[1]] != null;
        if (percentSpecified && !valueSpecified) {
          rangePropMode[index] = "percent";
        } else if (!percentSpecified && valueSpecified) {
          rangePropMode[index] = "value";
        } else if (rangeModeInOption) {
          rangePropMode[index] = rangeModeInOption[index];
        } else if (percentSpecified) {
          rangePropMode[index] = "percent";
        }
      });
    };
    DataZoomModel2.prototype.noTarget = function() {
      return this._noTarget;
    };
    DataZoomModel2.prototype.getFirstTargetAxisModel = function() {
      var firstAxisModel;
      this.eachTargetAxis(function(axisDim, axisIndex) {
        if (firstAxisModel == null) {
          firstAxisModel = this.ecModel.getComponent(getAxisMainType(axisDim), axisIndex);
        }
      }, this);
      return firstAxisModel;
    };
    DataZoomModel2.prototype.eachTargetAxis = function(callback, context) {
      this._targetAxisInfoMap.each(function(axisInfo, axisDim) {
        each(axisInfo.indexList, function(axisIndex) {
          callback.call(context, axisDim, axisIndex);
        });
      });
    };
    DataZoomModel2.prototype.getAxisProxy = function(axisDim, axisIndex) {
      return getAxisProxyFromModel(this.getAxisModel(axisDim, axisIndex));
    };
    DataZoomModel2.prototype.getAxisModel = function(axisDim, axisIndex) {
      if (true) {
        assert(axisDim && axisIndex != null);
      }
      var axisInfo = this._targetAxisInfoMap.get(axisDim);
      if (axisInfo && axisInfo.indexMap[axisIndex]) {
        return this.ecModel.getComponent(getAxisMainType(axisDim), axisIndex);
      }
    };
    DataZoomModel2.prototype.setRawRange = function(opt) {
      var thisOption = this.option;
      var settledOption = this.settledOption;
      each([["start", "startValue"], ["end", "endValue"]], function(names) {
        if (opt[names[0]] != null || opt[names[1]] != null) {
          thisOption[names[0]] = settledOption[names[0]] = opt[names[0]];
          thisOption[names[1]] = settledOption[names[1]] = opt[names[1]];
        }
      }, this);
      this._updateRangeUse(opt);
    };
    DataZoomModel2.prototype.setCalculatedRange = function(opt) {
      var option = this.option;
      each(["start", "startValue", "end", "endValue"], function(name) {
        option[name] = opt[name];
      });
    };
    DataZoomModel2.prototype.getPercentRange = function() {
      var axisProxy = this.findRepresentativeAxisProxy();
      if (axisProxy) {
        return axisProxy.getWindow().percent;
      }
    };
    DataZoomModel2.prototype.getValueRange = function(axisDim, axisIndex) {
      if (axisDim == null && axisIndex == null) {
        var axisProxy = this.findRepresentativeAxisProxy();
        if (axisProxy) {
          return axisProxy.getWindow().value;
        }
      } else {
        return this.getAxisProxy(axisDim, axisIndex).getWindow().value;
      }
    };
    DataZoomModel2.prototype.findRepresentativeAxisProxy = function(axisModel) {
      if (axisModel) {
        return getAxisProxyFromModel(axisModel);
      }
      var firstProxy;
      var axisDimList = this._targetAxisInfoMap.keys();
      for (var i2 = 0; i2 < axisDimList.length; i2++) {
        var axisDim = axisDimList[i2];
        var axisInfo = this._targetAxisInfoMap.get(axisDim);
        for (var j = 0; j < axisInfo.indexList.length; j++) {
          var proxy = this.getAxisProxy(axisDim, axisInfo.indexList[j]);
          if (proxy.hostedBy(this)) {
            return proxy;
          }
          if (!firstProxy) {
            firstProxy = proxy;
          }
        }
      }
      return firstProxy;
    };
    DataZoomModel2.prototype.getRangePropMode = function() {
      return this._rangePropMode.slice();
    };
    DataZoomModel2.prototype.getOrient = function() {
      if (true) {
        assert(this._orient);
      }
      return this._orient;
    };
    DataZoomModel2.type = "dataZoom";
    DataZoomModel2.dependencies = ["xAxis", "yAxis", "radiusAxis", "angleAxis", "singleAxis", "series", "toolbox"];
    DataZoomModel2.defaultOption = {
      // zlevel: 0,
      z: 4,
      filterMode: "filter",
      start: 0,
      end: 100
    };
    return DataZoomModel2;
  })(Component_default)
);
function retrieveRawOption(option) {
  var ret = {};
  each(["start", "end", "startValue", "endValue", "throttle"], function(name) {
    option.hasOwnProperty(name) && (ret[name] = option[name]);
  });
  return ret;
}
var DataZoomModel_default = DataZoomModel;

// node_modules/echarts/lib/component/dataZoom/SelectZoomModel.js
var SelectDataZoomModel = (
  /** @class */
  (function(_super) {
    __extends(SelectDataZoomModel2, _super);
    function SelectDataZoomModel2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = SelectDataZoomModel2.type;
      return _this;
    }
    SelectDataZoomModel2.type = "dataZoom.select";
    return SelectDataZoomModel2;
  })(DataZoomModel_default)
);
var SelectZoomModel_default = SelectDataZoomModel;

// node_modules/echarts/lib/component/dataZoom/DataZoomView.js
var DataZoomView = (
  /** @class */
  (function(_super) {
    __extends(DataZoomView2, _super);
    function DataZoomView2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = DataZoomView2.type;
      return _this;
    }
    DataZoomView2.prototype.render = function(dataZoomModel, ecModel, api, payload) {
      this.dataZoomModel = dataZoomModel;
      this.ecModel = ecModel;
      this.api = api;
    };
    DataZoomView2.type = "dataZoom";
    return DataZoomView2;
  })(Component_default2)
);
var DataZoomView_default = DataZoomView;

// node_modules/echarts/lib/component/dataZoom/SelectZoomView.js
var SelectDataZoomView = (
  /** @class */
  (function(_super) {
    __extends(SelectDataZoomView2, _super);
    function SelectDataZoomView2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = SelectDataZoomView2.type;
      return _this;
    }
    SelectDataZoomView2.type = "dataZoom.select";
    return SelectDataZoomView2;
  })(DataZoomView_default)
);
var SelectZoomView_default = SelectDataZoomView;

// node_modules/echarts/lib/component/dataZoom/AxisProxy.js
var AxisProxy = (
  /** @class */
  (function() {
    function AxisProxy2(dimName, axisIndex, dataZoomModel, ecModel) {
      this._dimName = dimName;
      this._axisIndex = axisIndex;
      this.ecModel = ecModel;
      this._dataZoomModel = dataZoomModel;
    }
    AxisProxy2.prototype.hostedBy = function(dataZoomModel) {
      return this._dataZoomModel === dataZoomModel;
    };
    AxisProxy2.prototype.getWindow = function() {
      return clone(this._window);
    };
    AxisProxy2.prototype.getTargetSeriesModels = function() {
      var seriesModels = [];
      this.ecModel.eachSeries(function(seriesModel) {
        if (isCoordSupported(seriesModel)) {
          var axisMainType = getAxisMainType(this._dimName);
          var axisModel = seriesModel.getReferringComponents(axisMainType, SINGLE_REFERRING).models[0];
          if (axisModel && this._axisIndex === axisModel.componentIndex) {
            seriesModels.push(seriesModel);
          }
        }
      }, this);
      return seriesModels;
    };
    AxisProxy2.prototype.getAxisModel = function() {
      return this.ecModel.getComponent(this._dimName + "Axis", this._axisIndex);
    };
    AxisProxy2.prototype.getMinMaxSpan = function() {
      return clone(this._minMaxSpan);
    };
    AxisProxy2.prototype.calculateDataWindow = function(opt) {
      var dataExtent = this._extent;
      var axis = this.getAxisModel().axis;
      var scale3 = axis.scale;
      var dataZoomModel = this._dataZoomModel;
      var rangePropMode = dataZoomModel.getRangePropMode();
      var percentExtent = [0, 100];
      var percentWindow = [];
      var valueWindow = [];
      var hasPropModeValue;
      var needRound = [false, false];
      each(["start", "end"], function(prop, idx) {
        var boundPercent = opt[prop];
        var boundValue = opt[prop + "Value"];
        if (rangePropMode[idx] === "percent") {
          boundPercent == null && (boundPercent = percentExtent[idx]);
          boundValue = linearMap(boundPercent, percentExtent, dataExtent);
          needRound[idx] = true;
        } else {
          hasPropModeValue = true;
          if (boundValue == null) {
            boundValue = dataExtent[idx];
          } else {
            boundValue = scale3.parse(boundValue);
            if (scale3.sanitize) {
              boundValue = scale3.sanitize(boundValue, dataExtent);
            }
          }
          boundPercent = linearMap(boundValue, dataExtent, percentExtent);
        }
        valueWindow[idx] = boundValue == null || isNaN(boundValue) ? dataExtent[idx] : boundValue;
        percentWindow[idx] = boundPercent == null || isNaN(boundPercent) ? percentExtent[idx] : boundPercent;
      });
      asc(valueWindow);
      asc(percentWindow);
      var spans = this._minMaxSpan;
      hasPropModeValue ? restrictSet(valueWindow, percentWindow, dataExtent, percentExtent, false) : restrictSet(percentWindow, valueWindow, percentExtent, dataExtent, true);
      function restrictSet(fromWindow, toWindow, fromExtent, toExtent, toValue) {
        var suffix = toValue ? "Span" : "ValueSpan";
        sliderMove(0, fromWindow, fromExtent, "all", spans["min" + suffix], spans["max" + suffix]);
        for (var i2 = 0; i2 < 2; i2++) {
          toWindow[i2] = linearMap(fromWindow[i2], fromExtent, toExtent, true);
          if (toValue) {
            toWindow[i2] = toWindow[i2];
            needRound[i2] = true;
          }
        }
        ensureExtentAscSimply(toWindow);
      }
      var isScaleOrdinalOrTime = isOrdinalScale(scale3) || isTimeScale(scale3);
      var pxExtent = axis.getExtent();
      var pxSpan = mathAbs3(pxExtent[1] - pxExtent[0]);
      var precision = isScaleOrdinalOrTime ? 0 : getAcceptableTickPrecision(valueWindow, pxSpan, 0.5);
      each([[0, mathCeil], [1, mathFloor]], function(_a2) {
        var idx = _a2[0], ceilOrFloor = _a2[1];
        if (!needRound[idx] || !isFinite(precision)) {
          return;
        }
        valueWindow[idx] = round2(valueWindow[idx], precision);
        valueWindow[idx] = mathMin4(dataExtent[1], mathMax4(dataExtent[0], valueWindow[idx]));
        if (percentWindow[idx] === percentExtent[idx]) {
          valueWindow[idx] = dataExtent[idx];
          if (isScaleOrdinalOrTime) {
            valueWindow[idx] = ceilOrFloor(valueWindow[idx]);
          }
        }
      });
      ensureExtentAscSimply(valueWindow);
      var percentInvertedWindow = [linearMap(valueWindow[0], dataExtent, percentExtent, true), linearMap(valueWindow[1], dataExtent, percentExtent, true)];
      ensureExtentAscSimply(percentInvertedWindow);
      return {
        value: valueWindow,
        percent: percentWindow,
        percentInverted: percentInvertedWindow,
        valuePrecision: precision
      };
    };
    AxisProxy2.prototype.reset = function(dataZoomModel, alignToPercentInverted) {
      if (!this.hostedBy(dataZoomModel)) {
        return;
      }
      var axis = this.getAxisModel().axis;
      scaleRawExtentInfoCreate(axis, AXIS_EXTENT_INFO_BUILD_FROM_DATA_ZOOM);
      var rawExtentInfo = axis.scale.rawExtentInfo;
      this._extent = rawExtentInfo.makeNoZoom();
      this._updateMinMaxSpan();
      var opt = dataZoomModel.settledOption;
      if (alignToPercentInverted) {
        opt = defaults({
          start: alignToPercentInverted[0],
          end: alignToPercentInverted[1]
        }, opt);
      }
      var _a2 = this._window = this.calculateDataWindow(opt), percent = _a2.percent, value = _a2.value;
      if (percent[0] !== 0) {
        rawExtentInfo.setZoomMM(0, value[0]);
      }
      if (percent[1] !== 100) {
        rawExtentInfo.setZoomMM(1, value[1]);
      }
    };
    AxisProxy2.prototype.filterData = function(dataZoomModel, api) {
      if (!this.hostedBy(dataZoomModel)) {
        return;
      }
      var axisDim = this._dimName;
      var seriesModels = this.getTargetSeriesModels();
      var filterMode = dataZoomModel.get("filterMode");
      var valueWindow = this._window.value;
      if (filterMode === "none") {
        return;
      }
      each(seriesModels, function(seriesModel) {
        var seriesData = seriesModel.getData();
        var dataDims = seriesData.mapDimensionsAll(axisDim);
        if (!dataDims.length) {
          return;
        }
        if (filterMode === "weakFilter") {
          var store_1 = seriesData.getStore();
          var dataDimIndices_1 = map2(dataDims, function(dim) {
            return seriesData.getDimensionIndex(dim);
          }, seriesData);
          seriesData.filterSelf(function(dataIndex) {
            var leftOut;
            var rightOut;
            var hasValue;
            for (var i2 = 0; i2 < dataDims.length; i2++) {
              var value = store_1.get(dataDimIndices_1[i2], dataIndex);
              var thisHasValue = !isNaN(value);
              var thisLeftOut = value < valueWindow[0];
              var thisRightOut = value > valueWindow[1];
              if (thisHasValue && !thisLeftOut && !thisRightOut) {
                return true;
              }
              thisHasValue && (hasValue = true);
              thisLeftOut && (leftOut = true);
              thisRightOut && (rightOut = true);
            }
            return hasValue && leftOut && rightOut;
          });
        } else {
          each(dataDims, function(dim) {
            if (filterMode === "empty") {
              seriesModel.setData(seriesData = seriesData.map(dim, function(value) {
                return !isInWindow(value) ? NaN : value;
              }));
            } else {
              var range = {};
              range[dim] = valueWindow;
              seriesData.selectRange(range);
            }
          });
        }
        each(dataDims, function(dim) {
          seriesData.setApproximateExtent(valueWindow, dim);
        });
      });
      function isInWindow(value) {
        return value >= valueWindow[0] && value <= valueWindow[1];
      }
    };
    AxisProxy2.prototype._updateMinMaxSpan = function() {
      var minMaxSpan = this._minMaxSpan = {};
      var dataZoomModel = this._dataZoomModel;
      var dataExtent = this._extent;
      each(["min", "max"], function(minMax) {
        var percentSpan = dataZoomModel.get(minMax + "Span");
        var valueSpan = dataZoomModel.get(minMax + "ValueSpan");
        valueSpan != null && (valueSpan = this.getAxisModel().axis.scale.parse(valueSpan));
        if (valueSpan != null) {
          percentSpan = linearMap(dataExtent[0] + valueSpan, dataExtent, [0, 100], true);
        } else if (percentSpan != null) {
          valueSpan = linearMap(percentSpan, [0, 100], dataExtent, true) - dataExtent[0];
        }
        minMaxSpan[minMax + "Span"] = percentSpan;
        minMaxSpan[minMax + "ValueSpan"] = valueSpan;
      }, this);
    };
    return AxisProxy2;
  })()
);
var AxisProxy_default = AxisProxy;

// node_modules/echarts/lib/component/dataZoom/dataZoomProcessor.js
var dataZoomProcessor = {
  dirtyOnOverallProgress: true,
  // `dataZoomProcessor` will only be performed in needed series. Consider if
  // there is a line series and a pie series, it is better not to update the
  // line series if only pie series is needed to be updated.
  getTargetSeries: function(ecModel) {
    function eachAxisModel(cb) {
      ecModel.eachComponent("dataZoom", function(dataZoomModel) {
        dataZoomModel.eachTargetAxis(function(axisDim, axisIndex) {
          var axisModel = ecModel.getComponent(getAxisMainType(axisDim), axisIndex);
          cb(axisDim, axisIndex, axisModel, dataZoomModel);
        });
      });
    }
    var proxyList = [];
    eachAxisModel(function(axisDim, axisIndex, axisModel, dataZoomModel) {
      if (!getAxisProxyFromModel(axisModel)) {
        var axisProxy = new AxisProxy_default(axisDim, axisIndex, dataZoomModel, ecModel);
        proxyList.push(axisProxy);
        setAxisProxyToModel(axisModel, axisProxy);
      }
    });
    var seriesModelMap = createHashMap();
    each(proxyList, function(axisProxy) {
      each(axisProxy.getTargetSeriesModels(), function(seriesModel) {
        seriesModelMap.set(seriesModel.uid, seriesModel);
      });
    });
    return seriesModelMap;
  },
  // Consider appendData, where filter should be performed. Because data process is
  // in block mode currently, it is not need to worry about that the overallProgress
  // execute every frame.
  overallReset: function(ecModel, api) {
    ecModel.eachComponent("dataZoom", function(dataZoomModel) {
      var axisProxyNeedAlign = [];
      dataZoomModel.eachTargetAxis(function(axisDim, axisIndex) {
        var axisProxy = dataZoomModel.getAxisProxy(axisDim, axisIndex);
        var alignToAxisProxy = getAlignTo(dataZoomModel, axisProxy);
        if (alignToAxisProxy) {
          axisProxyNeedAlign.push([axisProxy, alignToAxisProxy]);
        } else {
          axisProxy.reset(dataZoomModel, null);
        }
      });
      each(axisProxyNeedAlign, function(item) {
        item[0].reset(dataZoomModel, item[1].getWindow().percentInverted);
      });
      dataZoomModel.eachTargetAxis(function(axisDim, axisIndex) {
        dataZoomModel.getAxisProxy(axisDim, axisIndex).filterData(dataZoomModel, api);
      });
    });
    ecModel.eachComponent("dataZoom", function(dataZoomModel) {
      var axisProxy = dataZoomModel.findRepresentativeAxisProxy();
      if (axisProxy) {
        var _a2 = axisProxy.getWindow(), percent = _a2.percent, value = _a2.value;
        dataZoomModel.setCalculatedRange({
          start: percent[0],
          end: percent[1],
          startValue: value[0],
          endValue: value[1]
        });
      }
    });
  }
};
var dataZoomProcessor_default = dataZoomProcessor;

// node_modules/echarts/lib/component/dataZoom/dataZoomAction.js
function installDataZoomAction(registers) {
  registers.registerAction("dataZoom", function(payload, ecModel) {
    var effectedModels = findEffectedDataZooms(ecModel, payload);
    each(effectedModels, function(dataZoomModel) {
      dataZoomModel.setRawRange({
        start: payload.start,
        end: payload.end,
        startValue: payload.startValue,
        endValue: payload.endValue
      });
    });
  });
}

// node_modules/echarts/lib/component/dataZoom/installCommon.js
var callOnlyOnce6 = makeCallOnlyOnce();
function installCommon(registers) {
  callOnlyOnce6(registers, function() {
    registers.registerProcessor(registers.PRIORITY.PROCESSOR.FILTER, dataZoomProcessor_default);
    installDataZoomAction(registers);
    registers.registerSubTypeDefaulter("dataZoom", function() {
      return "slider";
    });
  });
}

// node_modules/echarts/lib/component/dataZoom/installDataZoomSelect.js
function install31(registers) {
  registers.registerComponentModel(SelectZoomModel_default);
  registers.registerComponentView(SelectZoomView_default);
  installCommon(registers);
}

// node_modules/echarts/lib/component/toolbox/featureManager.js
var ToolboxFeature = (
  /** @class */
  /* @__PURE__ */ (function() {
    function ToolboxFeature2() {
    }
    return ToolboxFeature2;
  })()
);
var features = {};
function registerFeature(name, ctor) {
  features[name] = ctor;
}
function getFeature(name) {
  return features[name];
}

// node_modules/echarts/lib/component/toolbox/ToolboxModel.js
var ToolboxModel = (
  /** @class */
  (function(_super) {
    __extends(ToolboxModel2, _super);
    function ToolboxModel2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = ToolboxModel2.type;
      return _this;
    }
    ToolboxModel2.prototype.init = function(option, parentModel, ecModel) {
      var toolboxOptionInTheme = ecModel.getTheme().get("toolbox");
      var themeFeatureOption = toolboxOptionInTheme ? toolboxOptionInTheme.feature : null;
      if (themeFeatureOption) {
        this._themeFeatureOption = extend({}, themeFeatureOption);
        toolboxOptionInTheme.feature = {};
      }
      _super.prototype.init.call(this, option, parentModel, ecModel);
      if (themeFeatureOption) {
        toolboxOptionInTheme.feature = themeFeatureOption;
      }
    };
    ToolboxModel2.prototype.optionUpdated = function() {
      each(this.option.feature, function(featureOpt, featureName) {
        var themeFeatureOption = this._themeFeatureOption;
        var Feature = getFeature(featureName);
        if (Feature) {
          if (Feature.getDefaultOption) {
            Feature.defaultOption = Feature.getDefaultOption(this.ecModel);
          }
          if (themeFeatureOption && themeFeatureOption[featureName]) {
            merge2(featureOpt, themeFeatureOption[featureName]);
            themeFeatureOption[featureName] = null;
          }
          merge2(featureOpt, Feature.defaultOption);
        }
      }, this);
    };
    ToolboxModel2.type = "toolbox";
    ToolboxModel2.layoutMode = {
      type: "box",
      ignoreSize: true
    };
    ToolboxModel2.defaultOption = {
      show: true,
      z: 6,
      // zlevel: 0,
      orient: "horizontal",
      left: "right",
      top: "top",
      // right
      // bottom
      backgroundColor: "transparent",
      borderColor: tokens_default.color.border,
      borderRadius: 0,
      borderWidth: 0,
      padding: tokens_default.size.m,
      itemSize: 15,
      itemGap: tokens_default.size.s,
      showTitle: true,
      iconStyle: {
        borderColor: tokens_default.color.accent50,
        color: "none"
      },
      emphasis: {
        iconStyle: {
          borderColor: tokens_default.color.accent70
        }
      },
      // textStyle: {},
      // feature
      tooltip: {
        show: false,
        position: "bottom"
      }
    };
    return ToolboxModel2;
  })(Component_default)
);
var ToolboxModel_default = ToolboxModel;

// node_modules/echarts/lib/component/helper/listComponent.js
function makeBackground(rect, componentModel) {
  var padding = normalizeCssArray2(componentModel.get("padding"));
  var style2 = componentModel.getItemStyle(["color", "opacity"]);
  style2.fill = componentModel.get("backgroundColor");
  var bgRect = new Rect_default({
    shape: {
      x: rect.x - padding[3],
      y: rect.y - padding[0],
      width: rect.width + padding[1] + padding[3],
      height: rect.height + padding[0] + padding[2],
      r: componentModel.get("borderRadius")
    },
    style: style2,
    silent: true,
    z2: -1
  });
  return bgRect;
}

// node_modules/echarts/lib/component/toolbox/ToolboxView.js
var ToolboxView = (
  /** @class */
  (function(_super) {
    __extends(ToolboxView2, _super);
    function ToolboxView2() {
      return _super !== null && _super.apply(this, arguments) || this;
    }
    ToolboxView2.prototype.render = function(toolboxModel, ecModel, api, payload) {
      var group2 = this.group;
      group2.removeAll();
      if (!toolboxModel.get("show")) {
        return;
      }
      var itemSize = +toolboxModel.get("itemSize");
      var isVertical = toolboxModel.get("orient") === "vertical";
      var featureOpts = toolboxModel.get("feature") || {};
      var features2 = this._features || (this._features = createHashMap());
      var newFeatureNames = [];
      each(featureOpts, function(opt, name) {
        newFeatureNames.push(name);
      });
      new DataDiffer_default(this._featureNames || [], newFeatureNames).add(processFeature).update(processFeature).remove(curry(processFeature, null)).execute();
      this._featureNames = filter2(newFeatureNames, function(name) {
        return features2.hasKey(name);
      });
      function processFeature(newIndex, oldIndex) {
        var isDiffAdd = newIndex != null && oldIndex == null;
        var isDiffUpdate = newIndex != null && oldIndex != null;
        var isDiffRemove = newIndex == null;
        var featureName = isDiffAdd || isDiffUpdate ? newFeatureNames[newIndex] : newFeatureNames[oldIndex];
        var featureOpt = featureOpts[featureName];
        var featureModel = isDiffAdd || isDiffUpdate ? new Model_default(featureOpt, toolboxModel, ecModel) : null;
        var isFeatureShow = featureModel && featureModel.get("show");
        var feature;
        if (isDiffAdd) {
          if (!isFeatureShow) {
            return;
          }
          if (isUserFeatureName(featureName)) {
            feature = {
              onclick: featureModel.option.onclick,
              featureName
            };
          } else {
            var Feature = getFeature(featureName);
            if (!Feature) {
              return;
            }
            feature = new Feature();
          }
          features2.set(featureName, feature);
        } else {
          feature = features2.get(featureName);
        }
        if (isDiffRemove || !isFeatureShow) {
          if (isTooltipFeature(feature) && feature.dispose) {
            feature.dispose(ecModel, api);
          }
          features2.removeKey(featureName);
          return;
        }
        if (payload && payload.newTitle != null && payload.featureName === featureName) {
          featureOpt.title = payload.newTitle;
        }
        if (isDiffAdd) {
          feature.uid = getUID("toolbox-feature");
        }
        feature.model = featureModel;
        feature.ecModel = ecModel;
        feature.api = api;
        createIconPaths(featureModel, feature, featureName);
        featureModel.setIconStatus = function(iconName, status) {
          var option = this.option;
          var iconPaths = this.iconPaths;
          option.iconStatus = option.iconStatus || {};
          option.iconStatus[iconName] = status;
          if (iconPaths[iconName]) {
            (status === "emphasis" ? enterEmphasis : leaveEmphasis)(iconPaths[iconName]);
          }
        };
        if (isTooltipFeature(feature) && feature.render) {
          feature.render(featureModel, ecModel, api, payload);
        }
      }
      function createIconPaths(featureModel, feature, featureName) {
        var iconStyleModel = featureModel.getModel("iconStyle");
        var iconStyleEmphasisModel = featureModel.getModel(["emphasis", "iconStyle"]);
        var icons = feature instanceof ToolboxFeature && feature.getIcons ? feature.getIcons() : featureModel.get("icon");
        var titles = featureModel.get("title") || {};
        var iconsMap;
        var titlesMap;
        if (isString(icons)) {
          iconsMap = {};
          iconsMap[featureName] = icons;
        } else {
          iconsMap = icons;
        }
        if (isString(titles)) {
          titlesMap = {};
          titlesMap[featureName] = titles;
        } else {
          titlesMap = titles;
        }
        var iconPaths = featureModel.iconPaths = {};
        each(iconsMap, function(iconStr, iconName) {
          var path = createIcon(iconStr, {}, {
            x: -itemSize / 2,
            y: -itemSize / 2,
            width: itemSize,
            height: itemSize
          });
          path.setStyle(iconStyleModel.getItemStyle());
          var pathEmphasisState = path.ensureState("emphasis");
          pathEmphasisState.style = iconStyleEmphasisModel.getItemStyle();
          var textContent = new Text_default({
            style: {
              text: titlesMap[iconName],
              align: iconStyleEmphasisModel.get("textAlign"),
              borderRadius: iconStyleEmphasisModel.get("textBorderRadius"),
              padding: iconStyleEmphasisModel.get("textPadding"),
              fill: null,
              font: getFont({
                fontStyle: iconStyleEmphasisModel.get("textFontStyle"),
                fontFamily: iconStyleEmphasisModel.get("textFontFamily"),
                fontSize: iconStyleEmphasisModel.get("textFontSize"),
                fontWeight: iconStyleEmphasisModel.get("textFontWeight")
              }, ecModel)
            },
            ignore: true
          });
          path.setTextContent(textContent);
          setTooltipConfig({
            el: path,
            componentModel: toolboxModel,
            itemName: iconName,
            formatterParamsExtra: {
              title: titlesMap[iconName]
            }
          });
          path.__title = titlesMap[iconName];
          path.on("mouseover", function() {
            var hoverStyle = iconStyleEmphasisModel.getItemStyle();
            var defaultTextPosition = isVertical ? toolboxModel.get("right") == null && toolboxModel.get("left") !== "right" ? "right" : "left" : toolboxModel.get("bottom") == null && toolboxModel.get("top") !== "bottom" ? "bottom" : "top";
            textContent.setStyle({
              fill: iconStyleEmphasisModel.get("textFill") || hoverStyle.fill || hoverStyle.stroke || tokens_default.color.neutral99,
              backgroundColor: iconStyleEmphasisModel.get("textBackgroundColor")
            });
            path.setTextConfig({
              position: iconStyleEmphasisModel.get("textPosition") || defaultTextPosition
            });
            textContent.ignore = !toolboxModel.get("showTitle");
            api.enterEmphasis(this);
          }).on("mouseout", function() {
            if (featureModel.get(["iconStatus", iconName]) !== "emphasis") {
              api.leaveEmphasis(this);
            }
            textContent.hide();
          });
          (featureModel.get(["iconStatus", iconName]) === "emphasis" ? enterEmphasis : leaveEmphasis)(path);
          group2.add(path);
          path.on("click", bind(feature.onclick, feature, ecModel, api, iconName));
          iconPaths[iconName] = path;
        });
      }
      var refContainer = createBoxLayoutReference(toolboxModel, api).refContainer;
      var boxLayoutParams = toolboxModel.getBoxLayoutParams();
      var padding = toolboxModel.get("padding");
      var viewRect2 = getLayoutRect(boxLayoutParams, refContainer, padding);
      box(toolboxModel.get("orient"), group2, toolboxModel.get("itemGap"), viewRect2.width, viewRect2.height);
      positionElement(group2, boxLayoutParams, refContainer, padding);
      group2.add(makeBackground(group2.getBoundingRect(), toolboxModel));
      isVertical || group2.eachChild(function(icon) {
        var titleText = icon.__title;
        var emphasisState = icon.ensureState("emphasis");
        var emphasisTextConfig = emphasisState.textConfig || (emphasisState.textConfig = {});
        var textContent = icon.getTextContent();
        var emphasisTextState = textContent && textContent.ensureState("emphasis");
        if (emphasisTextState && !isFunction(emphasisTextState) && titleText) {
          var emphasisTextStyle = emphasisTextState.style || (emphasisTextState.style = {});
          var rect = getBoundingRect(titleText, Text_default.makeFont(emphasisTextStyle));
          var offsetX = icon.x + group2.x;
          var offsetY = icon.y + group2.y + itemSize;
          var needPutOnTop = false;
          if (offsetY + rect.height > api.getHeight()) {
            emphasisTextConfig.position = "top";
            needPutOnTop = true;
          }
          var topOffset = needPutOnTop ? -5 - rect.height : itemSize + 10;
          if (offsetX + rect.width / 2 > api.getWidth()) {
            emphasisTextConfig.position = ["100%", topOffset];
            emphasisTextStyle.align = "right";
          } else if (offsetX - rect.width / 2 < 0) {
            emphasisTextConfig.position = [0, topOffset];
            emphasisTextStyle.align = "left";
          }
        }
      });
    };
    ToolboxView2.prototype.updateView = function(toolboxModel, ecModel, api, payload) {
      each(this._features, function(feature) {
        feature && feature instanceof ToolboxFeature && feature.updateView && feature.updateView(feature.model, ecModel, api, payload);
      });
    };
    ToolboxView2.prototype.dispose = function(ecModel, api) {
      each(this._features, function(feature) {
        feature && feature instanceof ToolboxFeature && feature.dispose && feature.dispose(ecModel, api);
      });
    };
    ToolboxView2.type = "toolbox";
    return ToolboxView2;
  })(Component_default2)
);
function isUserFeatureName(featureName) {
  return featureName.indexOf("my") === 0;
}
function isTooltipFeature(feature) {
  return feature instanceof ToolboxFeature;
}
var ToolboxView_default = ToolboxView;

// node_modules/echarts/lib/component/toolbox/feature/SaveAsImage.js
var SaveAsImage = (
  /** @class */
  (function(_super) {
    __extends(SaveAsImage2, _super);
    function SaveAsImage2() {
      return _super !== null && _super.apply(this, arguments) || this;
    }
    SaveAsImage2.prototype.onclick = function(ecModel, api) {
      var model2 = this.model;
      var title = model2.get("name") || ecModel.get("title.0.text") || "echarts";
      var isSvg = api.getZr().painter.getType() === "svg";
      var type = isSvg ? "svg" : model2.get("type", true) || "png";
      var url = api.getConnectedDataURL({
        type,
        backgroundColor: model2.get("backgroundColor", true) || ecModel.get("backgroundColor") || tokens_default.color.neutral00,
        connectedBackgroundColor: model2.get("connectedBackgroundColor"),
        excludeComponents: model2.get("excludeComponents"),
        pixelRatio: model2.get("pixelRatio")
      });
      var browser = env_default.browser;
      if (typeof MouseEvent === "function" && (browser.newEdge || !browser.ie && !browser.edge)) {
        var $a = document.createElement("a");
        $a.download = title + "." + type;
        $a.target = "_blank";
        $a.href = url;
        var evt = new MouseEvent("click", {
          // some micro front-end framework， window maybe is a Proxy
          view: document.defaultView,
          bubbles: true,
          cancelable: false
        });
        $a.dispatchEvent(evt);
      } else {
        if (window.navigator.msSaveOrOpenBlob || isSvg) {
          var parts = url.split(",");
          var base64Encoded = parts[0].indexOf("base64") > -1;
          var bstr = isSvg ? decodeURIComponent(parts[1]) : parts[1];
          base64Encoded && (bstr = window.atob(bstr));
          var filename = title + "." + type;
          if (window.navigator.msSaveOrOpenBlob) {
            var n = bstr.length;
            var u8arr = new Uint8Array(n);
            while (n--) {
              u8arr[n] = bstr.charCodeAt(n);
            }
            var blob = new Blob([u8arr]);
            window.navigator.msSaveOrOpenBlob(blob, filename);
          } else {
            var frame = document.createElement("iframe");
            document.body.appendChild(frame);
            var cw = frame.contentWindow;
            var doc = cw.document;
            doc.open("image/svg+xml", "replace");
            doc.write(bstr);
            doc.close();
            cw.focus();
            doc.execCommand("SaveAs", true, filename);
            document.body.removeChild(frame);
          }
        } else {
          var lang = model2.get("lang");
          var html = '<body style="margin:0;"><img src="' + url + '" style="max-width:100%;" title="' + (lang && lang[0] || "") + '" /></body>';
          var tab = window.open();
          tab.document.write(html);
          tab.document.title = title;
        }
      }
    };
    SaveAsImage2.getDefaultOption = function(ecModel) {
      var defaultOption3 = {
        show: true,
        icon: "M4.7,22.9L29.3,45.5L54.7,23.4M4.6,43.6L4.6,58L53.8,58L53.8,43.6M29.2,45.1L29.2,0",
        title: ecModel.getLocaleModel().get(["toolbox", "saveAsImage", "title"]),
        type: "png",
        // Default use option.backgroundColor
        // backgroundColor: '#fff',
        connectedBackgroundColor: tokens_default.color.neutral00,
        name: "",
        excludeComponents: ["toolbox"],
        // use current pixel ratio of device by default
        // pixelRatio: 1,
        lang: ecModel.getLocaleModel().get(["toolbox", "saveAsImage", "lang"])
      };
      return defaultOption3;
    };
    return SaveAsImage2;
  })(ToolboxFeature)
);
var SaveAsImage_default = SaveAsImage;

// node_modules/echarts/lib/component/toolbox/feature/MagicType.js
var INNER_STACK_KEYWORD = "__ec_magicType_stack__";
var radioTypes = [["line", "bar"], ["stack"]];
var MagicType = (
  /** @class */
  (function(_super) {
    __extends(MagicType2, _super);
    function MagicType2() {
      return _super !== null && _super.apply(this, arguments) || this;
    }
    MagicType2.prototype.getIcons = function() {
      var model2 = this.model;
      var availableIcons = model2.get("icon");
      var icons = {};
      each(model2.get("type"), function(type) {
        if (availableIcons[type]) {
          icons[type] = availableIcons[type];
        }
      });
      return icons;
    };
    MagicType2.getDefaultOption = function(ecModel) {
      var defaultOption3 = {
        show: true,
        type: [],
        // Icon group
        icon: {
          line: "M4.1,28.9h7.1l9.3-22l7.4,38l9.7-19.7l3,12.8h14.9M4.1,58h51.4",
          bar: "M6.7,22.9h10V48h-10V22.9zM24.9,13h10v35h-10V13zM43.2,2h10v46h-10V2zM3.1,58h53.7",
          // eslint-disable-next-line
          stack: "M8.2,38.4l-8.4,4.1l30.6,15.3L60,42.5l-8.1-4.1l-21.5,11L8.2,38.4z M51.9,30l-8.1,4.2l-13.4,6.9l-13.9-6.9L8.2,30l-8.4,4.2l8.4,4.2l22.2,11l21.5-11l8.1-4.2L51.9,30z M51.9,21.7l-8.1,4.2L35.7,30l-5.3,2.8L24.9,30l-8.4-4.1l-8.3-4.2l-8.4,4.2L8.2,30l8.3,4.2l13.9,6.9l13.4-6.9l8.1-4.2l8.1-4.1L51.9,21.7zM30.4,2.2L-0.2,17.5l8.4,4.1l8.3,4.2l8.4,4.2l5.5,2.7l5.3-2.7l8.1-4.2l8.1-4.2l8.1-4.1L30.4,2.2z"
          // jshint ignore:line
        },
        // `line`, `bar`, `stack`, `tiled`
        title: ecModel.getLocaleModel().get(["toolbox", "magicType", "title"]),
        option: {},
        seriesIndex: {}
      };
      return defaultOption3;
    };
    MagicType2.prototype.onclick = function(ecModel, api, type) {
      var model2 = this.model;
      var seriesIndex = model2.get(["seriesIndex", type]);
      if (!seriesOptGenreator[type]) {
        return;
      }
      var newOption = {
        series: []
      };
      var generateNewSeriesTypes = function(seriesModel) {
        var seriesType2 = seriesModel.subType;
        var seriesId = seriesModel.id;
        var newSeriesOpt = seriesOptGenreator[type](seriesType2, seriesId, seriesModel, model2);
        if (newSeriesOpt) {
          defaults(newSeriesOpt, seriesModel.option);
          newOption.series.push(newSeriesOpt);
        }
        var coordSys = seriesModel.coordinateSystem;
        if (coordSys && coordSys.type === "cartesian2d" && (type === "line" || type === "bar")) {
          var categoryAxis2 = coordSys.getAxesByScale("ordinal")[0];
          if (categoryAxis2) {
            var axisDim = categoryAxis2.dim;
            var axisType = axisDim + "Axis";
            var axisModel = seriesModel.getReferringComponents(axisType, SINGLE_REFERRING).models[0];
            var axisIndex = axisModel.componentIndex;
            newOption[axisType] = newOption[axisType] || [];
            for (var i2 = 0; i2 <= axisIndex; i2++) {
              newOption[axisType][axisIndex] = newOption[axisType][axisIndex] || {};
            }
            newOption[axisType][axisIndex].boundaryGap = type === "bar";
          }
        }
      };
      each(radioTypes, function(radio) {
        if (indexOf(radio, type) >= 0) {
          each(radio, function(item) {
            model2.setIconStatus(item, "normal");
          });
        }
      });
      model2.setIconStatus(type, "emphasis");
      ecModel.eachComponent({
        mainType: "series",
        query: seriesIndex == null ? null : {
          seriesIndex
        }
      }, generateNewSeriesTypes);
      var newTitle;
      var currentType = type;
      if (type === "stack") {
        newTitle = merge2({
          stack: model2.option.title.tiled,
          tiled: model2.option.title.stack
        }, model2.option.title);
        if (model2.get(["iconStatus", type]) !== "emphasis") {
          currentType = "tiled";
        }
      }
      api.dispatchAction({
        type: "changeMagicType",
        currentType,
        newOption,
        newTitle,
        featureName: "magicType"
      });
    };
    return MagicType2;
  })(ToolboxFeature)
);
var seriesOptGenreator = {
  "line": function(seriesType2, seriesId, seriesModel, model2) {
    if (seriesType2 === "bar") {
      return merge2({
        id: seriesId,
        type: "line",
        // Preserve data related option
        data: seriesModel.get("data"),
        stack: seriesModel.get("stack"),
        markPoint: seriesModel.get("markPoint"),
        markLine: seriesModel.get("markLine")
      }, model2.get(["option", "line"]) || {}, true);
    }
  },
  "bar": function(seriesType2, seriesId, seriesModel, model2) {
    if (seriesType2 === "line") {
      return merge2({
        id: seriesId,
        type: "bar",
        // Preserve data related option
        data: seriesModel.get("data"),
        stack: seriesModel.get("stack"),
        markPoint: seriesModel.get("markPoint"),
        markLine: seriesModel.get("markLine")
      }, model2.get(["option", "bar"]) || {}, true);
    }
  },
  "stack": function(seriesType2, seriesId, seriesModel, model2) {
    var isStack = seriesModel.get("stack") === INNER_STACK_KEYWORD;
    if (seriesType2 === "line" || seriesType2 === "bar") {
      model2.setIconStatus("stack", isStack ? "normal" : "emphasis");
      return merge2({
        id: seriesId,
        stack: isStack ? "" : INNER_STACK_KEYWORD
      }, model2.get(["option", "stack"]) || {}, true);
    }
  }
};
registerAction({
  type: "changeMagicType",
  event: "magicTypeChanged",
  update: "prepareAndUpdate"
}, function(payload, ecModel) {
  ecModel.mergeOption(payload.newOption);
});
var MagicType_default = MagicType;

// node_modules/echarts/lib/component/toolbox/feature/DataView.js
var BLOCK_SPLITER = new Array(60).join("-");
var ITEM_SPLITER = "	";
function groupSeries(ecModel) {
  var seriesGroupByCategoryAxis = {};
  var otherSeries = [];
  var meta = [];
  ecModel.eachRawSeries(function(seriesModel) {
    var coordSys = seriesModel.coordinateSystem;
    if (coordSys && (coordSys.type === "cartesian2d" || coordSys.type === "polar")) {
      var baseAxis = coordSys.getBaseAxis();
      if (baseAxis.type === "category") {
        var key2 = getCartesianAxisHashKey(baseAxis);
        if (!seriesGroupByCategoryAxis[key2]) {
          seriesGroupByCategoryAxis[key2] = {
            categoryAxis: baseAxis,
            valueAxis: coordSys.getOtherAxis(baseAxis),
            series: []
          };
          meta.push({
            axisDim: baseAxis.dim,
            axisIndex: baseAxis.index
          });
        }
        seriesGroupByCategoryAxis[key2].series.push(seriesModel);
      } else {
        otherSeries.push(seriesModel);
      }
    } else {
      otherSeries.push(seriesModel);
    }
  });
  return {
    seriesGroupByCategoryAxis,
    other: otherSeries,
    meta
  };
}
function assembleSeriesWithCategoryAxis(groups) {
  var tables = [];
  each(groups, function(group2, key2) {
    var categoryAxis2 = group2.categoryAxis;
    var valueAxis2 = group2.valueAxis;
    var valueAxisDim = valueAxis2.dim;
    var headers = [" "].concat(map2(group2.series, function(series) {
      return series.name;
    }));
    var columns = [categoryAxis2.model.getCategories()];
    each(group2.series, function(series) {
      var rawData = series.getRawData();
      columns.push(series.getRawData().mapArray(rawData.mapDimension(valueAxisDim), function(val) {
        return val;
      }));
    });
    var lines = [headers.join(ITEM_SPLITER)];
    for (var i2 = 0; i2 < columns[0].length; i2++) {
      var items = [];
      for (var j = 0; j < columns.length; j++) {
        items.push(columns[j][i2]);
      }
      lines.push(items.join(ITEM_SPLITER));
    }
    tables.push(lines.join("\n"));
  });
  return tables.join("\n\n" + BLOCK_SPLITER + "\n\n");
}
function assembleOtherSeries(series) {
  return map2(series, function(series2) {
    var data = series2.getRawData();
    var lines = [series2.name];
    var vals = [];
    data.each(data.dimensions, function() {
      var argLen = arguments.length;
      var dataIndex = arguments[argLen - 1];
      var name = data.getName(dataIndex);
      for (var i2 = 0; i2 < argLen - 1; i2++) {
        vals[i2] = arguments[i2];
      }
      lines.push((name ? name + ITEM_SPLITER : "") + vals.join(ITEM_SPLITER));
    });
    return lines.join("\n");
  }).join("\n\n" + BLOCK_SPLITER + "\n\n");
}
function getContentFromModel(ecModel) {
  var result = groupSeries(ecModel);
  return {
    value: filter2([assembleSeriesWithCategoryAxis(result.seriesGroupByCategoryAxis), assembleOtherSeries(result.other)], function(str) {
      return !!str.replace(/[\n\t\s]/g, "");
    }).join("\n\n" + BLOCK_SPLITER + "\n\n"),
    meta: result.meta
  };
}
function trim2(str) {
  return str.replace(/^\s\s*/, "").replace(/\s\s*$/, "");
}
function isTSVFormat(block) {
  var firstLine = block.slice(0, block.indexOf("\n"));
  if (firstLine.indexOf(ITEM_SPLITER) >= 0) {
    return true;
  }
}
var itemSplitRegex = new RegExp("[" + ITEM_SPLITER + "]+", "g");
function parseTSVContents(tsv) {
  var tsvLines = tsv.split(/\n+/g);
  var headers = trim2(tsvLines.shift()).split(itemSplitRegex);
  var categories = [];
  var series = map2(headers, function(header) {
    return {
      name: header,
      data: []
    };
  });
  for (var i2 = 0; i2 < tsvLines.length; i2++) {
    var items = trim2(tsvLines[i2]).split(itemSplitRegex);
    categories.push(items.shift());
    for (var j = 0; j < items.length; j++) {
      series[j] && (series[j].data[i2] = items[j]);
    }
  }
  return {
    series,
    categories
  };
}
function parseListContents(str) {
  var lines = str.split(/\n+/g);
  var seriesName = trim2(lines.shift());
  var data = [];
  for (var i2 = 0; i2 < lines.length; i2++) {
    var line = trim2(lines[i2]);
    if (!line) {
      continue;
    }
    var items = line.split(itemSplitRegex);
    var name_1 = "";
    var value = void 0;
    var hasName = false;
    if (isNaN(items[0])) {
      hasName = true;
      name_1 = items[0];
      items = items.slice(1);
      data[i2] = {
        name: name_1,
        value: []
      };
      value = data[i2].value;
    } else {
      value = data[i2] = [];
    }
    for (var j = 0; j < items.length; j++) {
      value.push(+items[j]);
    }
    if (value.length === 1) {
      hasName ? data[i2].value = value[0] : data[i2] = value[0];
    }
  }
  return {
    name: seriesName,
    data
  };
}
function parseContents(str, blockMetaList) {
  var blocks = str.split(new RegExp("\n*" + BLOCK_SPLITER + "\n*", "g"));
  var newOption = {
    series: []
  };
  each(blocks, function(block, idx) {
    if (isTSVFormat(block)) {
      var result = parseTSVContents(block);
      var blockMeta = blockMetaList[idx];
      var axisKey = blockMeta.axisDim + "Axis";
      if (blockMeta) {
        newOption[axisKey] = newOption[axisKey] || [];
        newOption[axisKey][blockMeta.axisIndex] = {
          data: result.categories
        };
        newOption.series = newOption.series.concat(result.series);
      }
    } else {
      var result = parseListContents(block);
      newOption.series.push(result);
    }
  });
  return newOption;
}
var DataView = (
  /** @class */
  (function(_super) {
    __extends(DataView2, _super);
    function DataView2() {
      return _super !== null && _super.apply(this, arguments) || this;
    }
    DataView2.prototype.onclick = function(ecModel, api) {
      setTimeout(function() {
        api.dispatchAction({
          type: "hideTip"
        });
      });
      var container = api.getDom();
      var model2 = this.model;
      if (this._dom) {
        container.removeChild(this._dom);
      }
      var root = document.createElement("div");
      root.style.cssText = "position:absolute;top:0;bottom:0;left:0;right:0;padding:5px";
      root.style.backgroundColor = model2.get("backgroundColor") || tokens_default.color.neutral00;
      var header = document.createElement("h4");
      var lang = model2.get("lang") || [];
      header.innerHTML = lang[0] || model2.get("title");
      header.style.cssText = "margin:10px 20px";
      header.style.color = model2.get("textColor");
      var viewMain = document.createElement("div");
      var textarea = document.createElement("textarea");
      viewMain.style.cssText = "overflow:auto";
      var optionToContent = model2.get("optionToContent");
      var contentToOption = model2.get("contentToOption");
      var result = getContentFromModel(ecModel);
      if (isFunction(optionToContent)) {
        var htmlOrDom = optionToContent(api.getOption());
        if (isString(htmlOrDom)) {
          viewMain.innerHTML = htmlOrDom;
        } else if (isDom(htmlOrDom)) {
          viewMain.appendChild(htmlOrDom);
        }
      } else {
        textarea.readOnly = model2.get("readOnly");
        var style2 = textarea.style;
        style2.cssText = "display:block;width:100%;height:100%;font-family:monospace;font-size:14px;line-height:1.6rem;resize:none;box-sizing:border-box;outline:none";
        style2.color = model2.get("textColor");
        style2.borderColor = model2.get("textareaBorderColor");
        style2.backgroundColor = model2.get("textareaColor");
        textarea.value = result.value;
        viewMain.appendChild(textarea);
      }
      var blockMetaList = result.meta;
      var buttonContainer = document.createElement("div");
      buttonContainer.style.cssText = "position:absolute;bottom:5px;left:0;right:0";
      var buttonStyle = "float:right;margin-right:20px;border:none;cursor:pointer;padding:2px 5px;font-size:12px;border-radius:3px";
      var closeButton = document.createElement("div");
      var refreshButton = document.createElement("div");
      buttonStyle += ";background-color:" + model2.get("buttonColor");
      buttonStyle += ";color:" + model2.get("buttonTextColor");
      var self2 = this;
      function close() {
        container.removeChild(root);
        self2._dom = null;
      }
      addEventListener(closeButton, "click", close);
      addEventListener(refreshButton, "click", function() {
        if (contentToOption == null && optionToContent != null || contentToOption != null && optionToContent == null) {
          if (true) {
            warn("It seems you have just provided one of `contentToOption` and `optionToContent` functions but missed the other one. Data change is ignored.");
          }
          close();
          return;
        }
        var newOption;
        try {
          if (isFunction(contentToOption)) {
            newOption = contentToOption(viewMain, api.getOption());
          } else {
            newOption = parseContents(textarea.value, blockMetaList);
          }
        } catch (e2) {
          close();
          throw new Error("Data view format error " + e2);
        }
        if (newOption) {
          api.dispatchAction({
            type: "changeDataView",
            newOption
          });
        }
        close();
      });
      closeButton.innerHTML = lang[1];
      refreshButton.innerHTML = lang[2];
      refreshButton.style.cssText = closeButton.style.cssText = buttonStyle;
      !model2.get("readOnly") && buttonContainer.appendChild(refreshButton);
      buttonContainer.appendChild(closeButton);
      root.appendChild(header);
      root.appendChild(viewMain);
      root.appendChild(buttonContainer);
      viewMain.style.height = container.clientHeight - 80 + "px";
      container.appendChild(root);
      this._dom = root;
    };
    DataView2.prototype.dispose = function(ecModel, api) {
      this._dom && api.getDom().removeChild(this._dom);
    };
    DataView2.getDefaultOption = function(ecModel) {
      var defaultOption3 = {
        show: true,
        readOnly: false,
        optionToContent: null,
        contentToOption: null,
        // eslint-disable-next-line
        icon: "M17.5,17.3H33 M17.5,17.3H33 M45.4,29.5h-28 M11.5,2v56H51V14.8L38.4,2H11.5z M38.4,2.2v12.7H51 M45.4,41.7h-28",
        title: ecModel.getLocaleModel().get(["toolbox", "dataView", "title"]),
        lang: ecModel.getLocaleModel().get(["toolbox", "dataView", "lang"]),
        backgroundColor: tokens_default.color.background,
        textColor: tokens_default.color.primary,
        textareaColor: tokens_default.color.background,
        textareaBorderColor: tokens_default.color.border,
        buttonColor: tokens_default.color.accent50,
        buttonTextColor: tokens_default.color.neutral00
      };
      return defaultOption3;
    };
    return DataView2;
  })(ToolboxFeature)
);
function tryMergeDataOption(newData, originalData) {
  return map2(newData, function(newVal, idx) {
    var original = originalData && originalData[idx];
    if (isObject(original) && !isArray(original)) {
      var newValIsObject = isObject(newVal) && !isArray(newVal);
      if (!newValIsObject) {
        newVal = {
          value: newVal
        };
      }
      var shouldDeleteName = original.name != null && newVal.name == null;
      newVal = defaults(newVal, original);
      shouldDeleteName && delete newVal.name;
      return newVal;
    } else {
      return newVal;
    }
  });
}
registerAction({
  type: "changeDataView",
  event: "dataViewChanged",
  update: "prepareAndUpdate"
}, function(payload, ecModel) {
  var newSeriesOptList = [];
  each(payload.newOption.series, function(seriesOpt) {
    var seriesModel = ecModel.getSeriesByName(seriesOpt.name)[0];
    if (!seriesModel) {
      newSeriesOptList.push(extend({
        // Default is scatter
        type: "scatter"
      }, seriesOpt));
    } else {
      var originalData = seriesModel.get("data");
      newSeriesOptList.push({
        name: seriesOpt.name,
        data: tryMergeDataOption(seriesOpt.data, originalData)
      });
    }
  });
  ecModel.mergeOption(defaults({
    series: newSeriesOptList
  }, payload.newOption));
});
var DataView_default = DataView;

// node_modules/echarts/lib/component/dataZoom/history.js
var each6 = each;
var inner19 = makeInner();
function push(ecModel, newSnapshot) {
  var storedSnapshots = getStoreSnapshots(ecModel);
  each6(newSnapshot, function(batchItem, dataZoomId) {
    var i2 = storedSnapshots.length - 1;
    for (; i2 >= 0; i2--) {
      var snapshot = storedSnapshots[i2];
      if (snapshot[dataZoomId]) {
        break;
      }
    }
    if (i2 < 0) {
      var dataZoomModel = ecModel.queryComponents({
        mainType: "dataZoom",
        subType: "select",
        id: dataZoomId
      })[0];
      if (dataZoomModel) {
        var percentRange = dataZoomModel.getPercentRange();
        storedSnapshots[0][dataZoomId] = {
          dataZoomId,
          start: percentRange[0],
          end: percentRange[1]
        };
      }
    }
  });
  storedSnapshots.push(newSnapshot);
}
function pop(ecModel) {
  var storedSnapshots = getStoreSnapshots(ecModel);
  var head = storedSnapshots[storedSnapshots.length - 1];
  storedSnapshots.length > 1 && storedSnapshots.pop();
  var snapshot = {};
  each6(head, function(batchItem, dataZoomId) {
    for (var i2 = storedSnapshots.length - 1; i2 >= 0; i2--) {
      batchItem = storedSnapshots[i2][dataZoomId];
      if (batchItem) {
        snapshot[dataZoomId] = batchItem;
        break;
      }
    }
  });
  return snapshot;
}
function clear2(ecModel) {
  inner19(ecModel).snapshots = null;
}
function count(ecModel) {
  return getStoreSnapshots(ecModel).length;
}
function getStoreSnapshots(ecModel) {
  var store = inner19(ecModel);
  if (!store.snapshots) {
    store.snapshots = [{}];
  }
  return store.snapshots;
}

// node_modules/echarts/lib/component/toolbox/feature/Restore.js
var RestoreOption = (
  /** @class */
  (function(_super) {
    __extends(RestoreOption2, _super);
    function RestoreOption2() {
      return _super !== null && _super.apply(this, arguments) || this;
    }
    RestoreOption2.prototype.onclick = function(ecModel, api) {
      clear2(ecModel);
      api.dispatchAction({
        type: "restore",
        from: this.uid
      });
    };
    RestoreOption2.getDefaultOption = function(ecModel) {
      var defaultOption3 = {
        show: true,
        // eslint-disable-next-line
        icon: "M3.8,33.4 M47,18.9h9.8V8.7 M56.3,20.1 C52.1,9,40.5,0.6,26.8,2.1C12.6,3.7,1.6,16.2,2.1,30.6 M13,41.1H3.1v10.2 M3.7,39.9c4.2,11.1,15.8,19.5,29.5,18 c14.2-1.6,25.2-14.1,24.7-28.5",
        title: ecModel.getLocaleModel().get(["toolbox", "restore", "title"])
      };
      return defaultOption3;
    };
    return RestoreOption2;
  })(ToolboxFeature)
);
registerAction({
  type: "restore",
  event: "restore",
  update: "prepareAndUpdate"
}, function(payload, ecModel) {
  ecModel.resetOption("recreate");
});
var Restore_default = RestoreOption;

// node_modules/echarts/lib/component/helper/BrushTargetManager.js
var INCLUDE_FINDER_MAIN_TYPES = ["grid", "xAxis", "yAxis", "geo", "graph", "polar", "radiusAxis", "angleAxis", "bmap"];
var BrushTargetManager = (
  /** @class */
  (function() {
    function BrushTargetManager2(finder, ecModel, opt) {
      var _this = this;
      this._targetInfoList = [];
      var foundCpts = parseFinder2(ecModel, finder);
      each(targetInfoBuilders, function(builder, type) {
        if (!opt || !opt.include || indexOf(opt.include, type) >= 0) {
          builder(foundCpts, _this._targetInfoList);
        }
      });
    }
    BrushTargetManager2.prototype.setOutputRanges = function(areas, ecModel) {
      this.matchOutputRanges(areas, ecModel, function(area, coordRange, coordSys) {
        (area.coordRanges || (area.coordRanges = [])).push(coordRange);
        if (!area.coordRange) {
          area.coordRange = coordRange;
          var result = coordConvert[area.brushType](0, coordSys, coordRange);
          area.__rangeOffset = {
            offset: diffProcessor[area.brushType](result.values, area.range, [1, 1]),
            xyMinMax: result.xyMinMax
          };
        }
      });
      return areas;
    };
    BrushTargetManager2.prototype.matchOutputRanges = function(areas, ecModel, cb) {
      each(areas, function(area) {
        var targetInfo = this.findTargetInfo(area, ecModel);
        if (targetInfo && targetInfo !== true) {
          each(targetInfo.coordSyses, function(coordSys) {
            var result = coordConvert[area.brushType](1, coordSys, area.range, true);
            cb(area, result.values, coordSys, ecModel);
          });
        }
      }, this);
    };
    BrushTargetManager2.prototype.setInputRanges = function(areas, ecModel) {
      each(areas, function(area) {
        var targetInfo = this.findTargetInfo(area, ecModel);
        if (true) {
          assert(!targetInfo || targetInfo === true || area.coordRange, "coordRange must be specified when coord index specified.");
          assert(!targetInfo || targetInfo !== true || area.range, "range must be specified in global brush.");
        }
        area.range = area.range || [];
        if (targetInfo && targetInfo !== true) {
          area.panelId = targetInfo.panelId;
          var result = coordConvert[area.brushType](0, targetInfo.coordSys, area.coordRange);
          var rangeOffset = area.__rangeOffset;
          area.range = rangeOffset ? diffProcessor[area.brushType](result.values, rangeOffset.offset, getScales(result.xyMinMax, rangeOffset.xyMinMax)) : result.values;
        }
      }, this);
    };
    BrushTargetManager2.prototype.makePanelOpts = function(api, getDefaultBrushType) {
      return map2(this._targetInfoList, function(targetInfo) {
        var rect = targetInfo.getPanelRect();
        return {
          panelId: targetInfo.panelId,
          defaultBrushType: getDefaultBrushType ? getDefaultBrushType(targetInfo) : null,
          clipPath: makeRectPanelClipPath(rect),
          isTargetByCursor: makeRectIsTargetByCursor(rect, api, targetInfo.coordSysModel),
          getLinearBrushOtherExtent: makeLinearBrushOtherExtent(rect)
        };
      });
    };
    BrushTargetManager2.prototype.controlSeries = function(area, seriesModel, ecModel) {
      var targetInfo = this.findTargetInfo(area, ecModel);
      return targetInfo === true || targetInfo && indexOf(targetInfo.coordSyses, seriesModel.coordinateSystem) >= 0;
    };
    BrushTargetManager2.prototype.findTargetInfo = function(area, ecModel) {
      var targetInfoList = this._targetInfoList;
      var foundCpts = parseFinder2(ecModel, area);
      for (var i2 = 0; i2 < targetInfoList.length; i2++) {
        var targetInfo = targetInfoList[i2];
        var areaPanelId = area.panelId;
        if (areaPanelId) {
          if (targetInfo.panelId === areaPanelId) {
            return targetInfo;
          }
        } else {
          for (var j = 0; j < targetInfoMatchers.length; j++) {
            if (targetInfoMatchers[j](foundCpts, targetInfo)) {
              return targetInfo;
            }
          }
        }
      }
      return true;
    };
    return BrushTargetManager2;
  })()
);
function formatMinMax(minMax) {
  minMax[0] > minMax[1] && minMax.reverse();
  return minMax;
}
function parseFinder2(ecModel, finder) {
  return parseFinder(ecModel, finder, {
    includeMainTypes: INCLUDE_FINDER_MAIN_TYPES
  });
}
var targetInfoBuilders = {
  grid: function(foundCpts, targetInfoList) {
    var xAxisModels = foundCpts.xAxisModels;
    var yAxisModels = foundCpts.yAxisModels;
    var gridModels = foundCpts.gridModels;
    var gridModelMap = createHashMap();
    var xAxesHas = {};
    var yAxesHas = {};
    if (!xAxisModels && !yAxisModels && !gridModels) {
      return;
    }
    each(xAxisModels, function(axisModel) {
      var gridModel = axisModel.axis.grid.model;
      gridModelMap.set(gridModel.id, gridModel);
      xAxesHas[gridModel.id] = true;
    });
    each(yAxisModels, function(axisModel) {
      var gridModel = axisModel.axis.grid.model;
      gridModelMap.set(gridModel.id, gridModel);
      yAxesHas[gridModel.id] = true;
    });
    each(gridModels, function(gridModel) {
      gridModelMap.set(gridModel.id, gridModel);
      xAxesHas[gridModel.id] = true;
      yAxesHas[gridModel.id] = true;
    });
    gridModelMap.each(function(gridModel) {
      var grid = gridModel.coordinateSystem;
      var cartesians = [];
      each(grid.getCartesians(), function(cartesian, index) {
        if (indexOf(xAxisModels, cartesian.getAxis("x").model) >= 0 || indexOf(yAxisModels, cartesian.getAxis("y").model) >= 0) {
          cartesians.push(cartesian);
        }
      });
      targetInfoList.push({
        panelId: "grid--" + gridModel.id,
        gridModel,
        coordSysModel: gridModel,
        // Use the first one as the representitive coordSys.
        coordSys: cartesians[0],
        coordSyses: cartesians,
        getPanelRect: panelRectBuilders.grid,
        xAxisDeclared: xAxesHas[gridModel.id],
        yAxisDeclared: yAxesHas[gridModel.id]
      });
    });
  },
  geo: function(foundCpts, targetInfoList) {
    each(foundCpts.geoModels, function(geoModel) {
      var coordSys = geoModel.coordinateSystem;
      targetInfoList.push({
        panelId: "geo--" + geoModel.id,
        geoModel,
        coordSysModel: geoModel,
        coordSys,
        coordSyses: [coordSys],
        getPanelRect: panelRectBuilders.geo
      });
    });
  }
};
var targetInfoMatchers = [
  // grid
  function(foundCpts, targetInfo) {
    var xAxisModel = foundCpts.xAxisModel;
    var yAxisModel = foundCpts.yAxisModel;
    var gridModel = foundCpts.gridModel;
    !gridModel && xAxisModel && (gridModel = xAxisModel.axis.grid.model);
    !gridModel && yAxisModel && (gridModel = yAxisModel.axis.grid.model);
    return gridModel && gridModel === targetInfo.gridModel;
  },
  // geo
  function(foundCpts, targetInfo) {
    var geoModel = foundCpts.geoModel;
    return geoModel && geoModel === targetInfo.geoModel;
  }
];
var panelRectBuilders = {
  grid: function() {
    return this.coordSys.master.getRect().clone();
  },
  geo: function() {
    var viewCoordSys = this.coordSys.view;
    var rect = viewCoordSysCopyBoundingRect(null, viewCoordSys);
    boundingRectApplyTransform(rect, rect, viewCoordSysCopyOverallMatrix(null, viewCoordSys));
    return rect;
  }
};
var coordConvert = {
  lineX: curry(axisConvert, 0),
  lineY: curry(axisConvert, 1),
  rect: function(to, coordSys, rangeOrCoordRange, clamp2) {
    var xminymin = to ? coordSys.pointToData([rangeOrCoordRange[0][0], rangeOrCoordRange[1][0]], clamp2) : coordSys.dataToPoint([rangeOrCoordRange[0][0], rangeOrCoordRange[1][0]], clamp2);
    var xmaxymax = to ? coordSys.pointToData([rangeOrCoordRange[0][1], rangeOrCoordRange[1][1]], clamp2) : coordSys.dataToPoint([rangeOrCoordRange[0][1], rangeOrCoordRange[1][1]], clamp2);
    var values = [formatMinMax([xminymin[0], xmaxymax[0]]), formatMinMax([xminymin[1], xmaxymax[1]])];
    return {
      values,
      xyMinMax: values
    };
  },
  polygon: function(to, coordSys, rangeOrCoordRange, clamp2) {
    var xyMinMax = [initExtentForUnion(), initExtentForUnion()];
    var values = map2(rangeOrCoordRange, function(item) {
      var p = to ? coordSys.pointToData(item, clamp2) : coordSys.dataToPoint(item, clamp2);
      xyMinMax[0][0] = Math.min(xyMinMax[0][0], p[0]);
      xyMinMax[1][0] = Math.min(xyMinMax[1][0], p[1]);
      xyMinMax[0][1] = Math.max(xyMinMax[0][1], p[0]);
      xyMinMax[1][1] = Math.max(xyMinMax[1][1], p[1]);
      return p;
    });
    return {
      values,
      xyMinMax
    };
  }
};
function axisConvert(axisNameIndex, to, coordSys, rangeOrCoordRange) {
  if (true) {
    assert(coordSys.type === "cartesian2d", "lineX/lineY brush is available only in cartesian2d.");
  }
  var axis = coordSys.getAxis(["x", "y"][axisNameIndex]);
  var values = formatMinMax(map2([0, 1], function(i2) {
    return to ? axis.coordToData(axis.toLocalCoord(rangeOrCoordRange[i2]), true) : axis.toGlobalCoord(axis.dataToCoord(rangeOrCoordRange[i2]));
  }));
  var xyMinMax = [];
  xyMinMax[axisNameIndex] = values;
  xyMinMax[1 - axisNameIndex] = [NaN, NaN];
  return {
    values,
    xyMinMax
  };
}
var diffProcessor = {
  lineX: curry(axisDiffProcessor, 0),
  lineY: curry(axisDiffProcessor, 1),
  rect: function(values, refer, scales) {
    return [[values[0][0] - scales[0] * refer[0][0], values[0][1] - scales[0] * refer[0][1]], [values[1][0] - scales[1] * refer[1][0], values[1][1] - scales[1] * refer[1][1]]];
  },
  polygon: function(values, refer, scales) {
    return map2(values, function(item, idx) {
      return [item[0] - scales[0] * refer[idx][0], item[1] - scales[1] * refer[idx][1]];
    });
  }
};
function axisDiffProcessor(axisNameIndex, values, refer, scales) {
  return [values[0] - scales[axisNameIndex] * refer[0], values[1] - scales[axisNameIndex] * refer[1]];
}
function getScales(xyMinMaxCurr, xyMinMaxOrigin) {
  var sizeCurr = getSize2(xyMinMaxCurr);
  var sizeOrigin = getSize2(xyMinMaxOrigin);
  var scales = [sizeCurr[0] / sizeOrigin[0], sizeCurr[1] / sizeOrigin[1]];
  isNaN(scales[0]) && (scales[0] = 1);
  isNaN(scales[1]) && (scales[1] = 1);
  return scales;
}
function getSize2(xyMinMax) {
  return xyMinMax ? [xyMinMax[0][1] - xyMinMax[0][0], xyMinMax[1][1] - xyMinMax[1][0]] : [NaN, NaN];
}
var BrushTargetManager_default = BrushTargetManager;

// node_modules/echarts/lib/component/toolbox/feature/DataZoom.js
var each7 = each;
var DATA_ZOOM_ID_BASE = makeInternalComponentId("toolbox-dataZoom_");
var XY2WH = {
  x: "width",
  y: "height"
};
var DataZoomFeature = (
  /** @class */
  (function(_super) {
    __extends(DataZoomFeature2, _super);
    function DataZoomFeature2() {
      return _super !== null && _super.apply(this, arguments) || this;
    }
    DataZoomFeature2.prototype.render = function(featureModel, ecModel, api, payload) {
      if (!this._brushController) {
        this._brushController = new BrushController_default(api.getZr());
        this._brushController.on("brush", bind(this._onBrush, this)).mount();
      }
      updateZoomBtnStatus(featureModel, ecModel, this, payload, api);
      updateBackBtnStatus(featureModel, ecModel);
    };
    DataZoomFeature2.prototype.onclick = function(ecModel, api, type) {
      handlers2[type].call(this);
    };
    DataZoomFeature2.prototype.dispose = function(ecModel, api) {
      this._brushController && this._brushController.dispose();
    };
    DataZoomFeature2.prototype._onBrush = function(eventParam) {
      var areas = eventParam.areas;
      if (!eventParam.isEnd || !areas.length) {
        return;
      }
      var snapshot = {};
      var ecModel = this.ecModel;
      this._brushController.updateCovers([]);
      var brushTargetManager = new BrushTargetManager_default(makeAxisFinder(this.model), ecModel, {
        include: ["grid"]
      });
      brushTargetManager.matchOutputRanges(areas, ecModel, function(area, coordRange, coordSys) {
        if (coordSys.type !== "cartesian2d") {
          return;
        }
        var coordSysRect = coordSys.master.getRect().clone();
        var brushType = area.brushType;
        if (brushType === "rect") {
          setBatch("x", coordSys, coordSysRect, coordRange[0]);
          setBatch("y", coordSys, coordSysRect, coordRange[1]);
        } else {
          setBatch({
            lineX: "x",
            lineY: "y"
          }[brushType], coordSys, coordSysRect, coordRange);
        }
      });
      push(ecModel, snapshot);
      this._dispatchZoomAction(snapshot);
      function setBatch(dimName, coordSys, coordSysRect, minMax) {
        var axis = coordSys.getAxis(dimName);
        var axisModel = axis.model;
        var dataZoomModel = findDataZoom(dimName, axisModel, ecModel);
        var minMaxSpan = dataZoomModel.findRepresentativeAxisProxy(axisModel).getMinMaxSpan();
        var scaleExtent = axis.scale.getExtent();
        if (minMaxSpan.minValueSpan != null || minMaxSpan.maxValueSpan != null) {
          minMax = sliderMove(0, minMax.slice(), scaleExtent, 0, minMaxSpan.minValueSpan, minMaxSpan.maxValueSpan);
        }
        var precision = getAcceptableTickPrecision(scaleExtent, coordSysRect[XY2WH[dimName]], 0.5);
        dataZoomModel && (snapshot[dataZoomModel.id] = {
          dataZoomId: dataZoomModel.id,
          startValue: isFinite(precision) ? round2(minMax[0], precision) : minMax[0],
          endValue: isFinite(precision) ? round2(minMax[1], precision) : minMax[1]
        });
      }
      function findDataZoom(dimName, axisModel, ecModel2) {
        var found;
        ecModel2.eachComponent({
          mainType: "dataZoom",
          subType: "select"
        }, function(dzModel) {
          var has = dzModel.getAxisModel(dimName, axisModel.componentIndex);
          has && (found = dzModel);
        });
        return found;
      }
    };
    ;
    DataZoomFeature2.prototype._dispatchZoomAction = function(snapshot) {
      var batch = [];
      each7(snapshot, function(batchItem, dataZoomId) {
        batch.push(clone(batchItem));
      });
      batch.length && this.api.dispatchAction({
        type: "dataZoom",
        from: this.uid,
        batch
      });
    };
    DataZoomFeature2.getDefaultOption = function(ecModel) {
      var defaultOption3 = {
        show: true,
        filterMode: "filter",
        // Icon group
        icon: {
          zoom: "M0,13.5h26.9 M13.5,26.9V0 M32.1,13.5H58V58H13.5 V32.1",
          back: "M22,1.4L9.9,13.5l12.3,12.3 M10.3,13.5H54.9v44.6 H10.3v-26"
        },
        // `zoom`, `back`
        title: ecModel.getLocaleModel().get(["toolbox", "dataZoom", "title"]),
        brushStyle: {
          borderWidth: 0,
          color: tokens_default.color.backgroundTint
        }
      };
      return defaultOption3;
    };
    return DataZoomFeature2;
  })(ToolboxFeature)
);
var handlers2 = {
  zoom: function() {
    var nextActive = !this._isZoomActive;
    this.api.dispatchAction({
      type: "takeGlobalCursor",
      key: "dataZoomSelect",
      dataZoomSelectActive: nextActive
    });
  },
  back: function() {
    this._dispatchZoomAction(pop(this.ecModel));
  }
};
function makeAxisFinder(dzFeatureModel) {
  var setting = {
    xAxisIndex: dzFeatureModel.get("xAxisIndex", true),
    yAxisIndex: dzFeatureModel.get("yAxisIndex", true),
    xAxisId: dzFeatureModel.get("xAxisId", true),
    yAxisId: dzFeatureModel.get("yAxisId", true)
  };
  if (setting.xAxisIndex == null && setting.xAxisId == null) {
    setting.xAxisIndex = "all";
  }
  if (setting.yAxisIndex == null && setting.yAxisId == null) {
    setting.yAxisIndex = "all";
  }
  return setting;
}
function updateBackBtnStatus(featureModel, ecModel) {
  featureModel.setIconStatus("back", count(ecModel) > 1 ? "emphasis" : "normal");
}
function updateZoomBtnStatus(featureModel, ecModel, view, payload, api) {
  var zoomActive = view._isZoomActive;
  if (payload && payload.type === "takeGlobalCursor") {
    zoomActive = payload.key === "dataZoomSelect" ? payload.dataZoomSelectActive : false;
  }
  view._isZoomActive = zoomActive;
  featureModel.setIconStatus("zoom", zoomActive ? "emphasis" : "normal");
  var brushTargetManager = new BrushTargetManager_default(makeAxisFinder(featureModel), ecModel, {
    include: ["grid"]
  });
  var panels = brushTargetManager.makePanelOpts(api, function(targetInfo) {
    return targetInfo.xAxisDeclared && !targetInfo.yAxisDeclared ? "lineX" : !targetInfo.xAxisDeclared && targetInfo.yAxisDeclared ? "lineY" : "rect";
  });
  view._brushController.setPanels(panels).enableBrush(zoomActive && panels.length ? {
    brushType: "auto",
    brushStyle: featureModel.getModel("brushStyle").getItemStyle()
  } : false);
}
registerInternalOptionCreator("dataZoom", function(ecModel) {
  var toolboxModel = ecModel.getComponent("toolbox", 0);
  var featureDataZoomPath = ["feature", "dataZoom"];
  if (!toolboxModel || toolboxModel.get(featureDataZoomPath) == null) {
    return;
  }
  var dzFeatureModel = toolboxModel.getModel(featureDataZoomPath);
  var dzOptions = [];
  var finder = makeAxisFinder(dzFeatureModel);
  var finderResult = parseFinder(ecModel, finder);
  each7(finderResult.xAxisModels, function(axisModel) {
    return buildInternalOptions(axisModel, "xAxis", "xAxisIndex");
  });
  each7(finderResult.yAxisModels, function(axisModel) {
    return buildInternalOptions(axisModel, "yAxis", "yAxisIndex");
  });
  function buildInternalOptions(axisModel, axisMainType, axisIndexPropName) {
    var axisIndex = axisModel.componentIndex;
    var newOpt = {
      type: "select",
      $fromToolbox: true,
      // Default to be filter
      filterMode: dzFeatureModel.get("filterMode", true) || "filter",
      // Id for merge mapping.
      id: DATA_ZOOM_ID_BASE + axisMainType + axisIndex
    };
    newOpt[axisIndexPropName] = axisIndex;
    dzOptions.push(newOpt);
  }
  return dzOptions;
});
var DataZoom_default = DataZoomFeature;

// node_modules/echarts/lib/component/toolbox/install.js
function install32(registers) {
  registers.registerComponentModel(ToolboxModel_default);
  registers.registerComponentView(ToolboxView_default);
  registerFeature("saveAsImage", SaveAsImage_default);
  registerFeature("magicType", MagicType_default);
  registerFeature("dataView", DataView_default);
  registerFeature("dataZoom", DataZoom_default);
  registerFeature("restore", Restore_default);
  use(install31);
}

// node_modules/echarts/lib/component/tooltip/TooltipModel.js
var TooltipModel = (
  /** @class */
  (function(_super) {
    __extends(TooltipModel2, _super);
    function TooltipModel2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = TooltipModel2.type;
      return _this;
    }
    TooltipModel2.type = "tooltip";
    TooltipModel2.dependencies = ["axisPointer"];
    TooltipModel2.defaultOption = {
      // zlevel: 0,
      z: 60,
      show: true,
      // tooltip main content
      showContent: true,
      // 'trigger' only works on coordinate system.
      // 'item' | 'axis' | 'none'
      trigger: "item",
      // 'click' | 'mousemove' | 'none'
      triggerOn: "mousemove|click|mousewheel",
      alwaysShowContent: false,
      renderMode: "auto",
      // whether restraint content inside viewRect.
      // If renderMode: 'richText', default true.
      // If renderMode: 'html', defaults to `false` (for backward compat).
      confine: null,
      showDelay: 0,
      hideDelay: 100,
      // Animation transition time, unit is second
      transitionDuration: 0.4,
      displayTransition: true,
      enterable: false,
      backgroundColor: tokens_default.color.neutral00,
      // box shadow
      shadowBlur: 10,
      shadowColor: "rgba(0, 0, 0, .2)",
      shadowOffsetX: 1,
      shadowOffsetY: 2,
      // tooltip border radius, unit is px, default is 4
      borderRadius: 4,
      // tooltip border width, unit is px, default is 0 (no border)
      borderWidth: 1,
      defaultBorderColor: tokens_default.color.border,
      // Tooltip inside padding, default is 5 for all direction
      // Array is allowed to set up, right, bottom, left, same with css
      // The default value: See `tooltip/tooltipMarkup.ts#getPaddingFromTooltipModel`.
      padding: null,
      // Extra css text
      extraCssText: "",
      // axis indicator, trigger by axis
      axisPointer: {
        // default is line
        // legal values: 'line' | 'shadow' | 'cross'
        type: "line",
        // Valid when type is line, appoint tooltip line locate on which line. Optional
        // legal values: 'x' | 'y' | 'angle' | 'radius' | 'auto'
        // default is 'auto', chose the axis which type is category.
        // for multiply y axis, cartesian coord chose x axis, polar chose angle axis
        axis: "auto",
        animation: "auto",
        animationDurationUpdate: 200,
        animationEasingUpdate: "exponentialOut",
        crossStyle: {
          color: tokens_default.color.borderShade,
          width: 1,
          type: "dashed",
          // TODO formatter
          textStyle: {}
        }
        // lineStyle and shadowStyle should not be specified here,
        // otherwise it will always override those styles on option.axisPointer.
      },
      textStyle: {
        color: tokens_default.color.tertiary,
        fontSize: 14
      }
    };
    return TooltipModel2;
  })(Component_default)
);
var TooltipModel_default = TooltipModel;

// node_modules/echarts/lib/component/tooltip/helper.js
function shouldTooltipConfine(tooltipModel) {
  var confineOption = tooltipModel.get("confine");
  return confineOption != null ? !!confineOption : tooltipModel.get("renderMode") === "richText";
}
function testStyle(styleProps) {
  if (!env_default.domSupported) {
    return;
  }
  var style2 = document.documentElement.style;
  for (var i2 = 0, len2 = styleProps.length; i2 < len2; i2++) {
    if (styleProps[i2] in style2) {
      return styleProps[i2];
    }
  }
}
var TRANSFORM_VENDOR = testStyle(["transform", "webkitTransform", "OTransform", "MozTransform", "msTransform"]);
var TRANSITION_VENDOR = testStyle(["webkitTransition", "transition", "OTransition", "MozTransition", "msTransition"]);
function toCSSVendorPrefix(styleVendor, styleProp) {
  if (!styleVendor) {
    return styleProp;
  }
  styleProp = toCamelCase(styleProp, true);
  var idx = styleVendor.indexOf(styleProp);
  styleVendor = idx === -1 ? styleProp : "-" + styleVendor.slice(0, idx) + "-" + styleProp;
  return styleVendor.toLowerCase();
}
function getComputedStyle2(el, style2) {
  var stl = el.currentStyle || document.defaultView && document.defaultView.getComputedStyle(el);
  return stl ? style2 ? stl[style2] : stl : null;
}

// node_modules/echarts/lib/component/tooltip/TooltipHTMLContent.js
var CSS_TRANSITION_VENDOR = toCSSVendorPrefix(TRANSITION_VENDOR, "transition");
var CSS_TRANSFORM_VENDOR = toCSSVendorPrefix(TRANSFORM_VENDOR, "transform");
var gCssText = "position:absolute;display:block;border-style:solid;white-space:nowrap;z-index:9999999;" + (env_default.transform3dSupported ? "will-change:transform;" : "");
function mirrorPos(pos) {
  pos = pos === "left" ? "right" : pos === "right" ? "left" : pos === "top" ? "bottom" : "top";
  return pos;
}
function assembleArrow(tooltipModel, borderColor, arrowPosition) {
  if (!isString(arrowPosition) || arrowPosition === "inside") {
    return "";
  }
  var backgroundColor2 = tooltipModel.get("backgroundColor");
  var borderWidth = tooltipModel.get("borderWidth");
  borderColor = convertToColorString(borderColor);
  var arrowPos = mirrorPos(arrowPosition);
  var arrowSize = Math.max(Math.round(borderWidth) * 1.5, 6);
  var positionStyle = "";
  var transformStyle = CSS_TRANSFORM_VENDOR + ":";
  var rotateDeg;
  if (indexOf(["left", "right"], arrowPos) > -1) {
    positionStyle += "top:50%";
    transformStyle += "translateY(-50%) rotate(" + (rotateDeg = arrowPos === "left" ? -225 : -45) + "deg)";
  } else {
    positionStyle += "left:50%";
    transformStyle += "translateX(-50%) rotate(" + (rotateDeg = arrowPos === "top" ? 225 : 45) + "deg)";
  }
  var rotateRadian = rotateDeg * Math.PI / 180;
  var arrowWH = arrowSize + borderWidth;
  var rotatedWH = arrowWH * Math.abs(Math.cos(rotateRadian)) + arrowWH * Math.abs(Math.sin(rotateRadian));
  var arrowOffset = Math.round(((rotatedWH - Math.SQRT2 * borderWidth) / 2 + Math.SQRT2 * borderWidth - (rotatedWH - arrowWH) / 2) * 100) / 100;
  positionStyle += ";" + arrowPos + ":-" + arrowOffset + "px";
  var borderStyle = borderColor + " solid " + borderWidth + "px;";
  var styleCss = ["position:absolute;width:" + arrowSize + "px;height:" + arrowSize + "px;z-index:-1;", positionStyle + ";" + transformStyle + ";", "border-bottom:" + borderStyle, "border-right:" + borderStyle, "background-color:" + backgroundColor2 + ";"];
  return '<div style="' + styleCss.join("") + '"></div>';
}
function assembleTransition(duration, onlyFadeTransition, enableDisplayTransition) {
  var transitionCurve = "cubic-bezier(0.23,1,0.32,1)";
  var transitionOption = "";
  var transitionText = "";
  if (enableDisplayTransition) {
    transitionOption = " " + duration / 2 + "s " + transitionCurve;
    transitionText = "opacity" + transitionOption + ",visibility" + transitionOption;
  }
  if (!onlyFadeTransition) {
    transitionOption = " " + duration + "s " + transitionCurve;
    transitionText += (transitionText.length ? "," : "") + (env_default.transformSupported ? "" + CSS_TRANSFORM_VENDOR + transitionOption : ",left" + transitionOption + ",top" + transitionOption);
  }
  return CSS_TRANSITION_VENDOR + ":" + transitionText;
}
function assembleTransform(x, y, toString) {
  var x0 = x.toFixed(0) + "px";
  var y0 = y.toFixed(0) + "px";
  if (!env_default.transformSupported) {
    return toString ? "top:" + y0 + ";left:" + x0 + ";" : [["top", y0], ["left", x0]];
  }
  var is3d = env_default.transform3dSupported;
  var translate2 = "translate" + (is3d ? "3d" : "") + "(" + x0 + "," + y0 + (is3d ? ",0" : "") + ")";
  return toString ? "top:0;left:0;" + CSS_TRANSFORM_VENDOR + ":" + translate2 + ";" : [["top", 0], ["left", 0], [TRANSFORM_VENDOR, translate2]];
}
function assembleFont(textStyleModel) {
  var cssText = [];
  var fontSize = textStyleModel.get("fontSize");
  var color3 = textStyleModel.getTextColor();
  color3 && cssText.push("color:" + color3);
  cssText.push("font:" + textStyleModel.getFont());
  var lineHeight = retrieve2(textStyleModel.get("lineHeight"), Math.round(fontSize * 3 / 2));
  fontSize && cssText.push("line-height:" + lineHeight + "px");
  var shadowColor = textStyleModel.get("textShadowColor");
  var shadowBlur = textStyleModel.get("textShadowBlur") || 0;
  var shadowOffsetX = textStyleModel.get("textShadowOffsetX") || 0;
  var shadowOffsetY = textStyleModel.get("textShadowOffsetY") || 0;
  shadowColor && shadowBlur && cssText.push("text-shadow:" + shadowOffsetX + "px " + shadowOffsetY + "px " + shadowBlur + "px " + shadowColor);
  each(["decoration", "align"], function(name) {
    var val = textStyleModel.get(name);
    val && cssText.push("text-" + name + ":" + val);
  });
  return cssText.join(";");
}
function assembleCssText(tooltipModel, enableTransition, onlyFadeTransition, enableDisplayTransition) {
  var cssText = [];
  var transitionDuration = tooltipModel.get("transitionDuration");
  var backgroundColor2 = tooltipModel.get("backgroundColor");
  var shadowBlur = tooltipModel.get("shadowBlur");
  var shadowColor = tooltipModel.get("shadowColor");
  var shadowOffsetX = tooltipModel.get("shadowOffsetX");
  var shadowOffsetY = tooltipModel.get("shadowOffsetY");
  var textStyleModel = tooltipModel.getModel("textStyle");
  var padding = getPaddingFromTooltipModel(tooltipModel, "html");
  var boxShadow = shadowOffsetX + "px " + shadowOffsetY + "px " + shadowBlur + "px " + shadowColor;
  cssText.push("box-shadow:" + boxShadow);
  enableTransition && transitionDuration > 0 && cssText.push(assembleTransition(transitionDuration, onlyFadeTransition, enableDisplayTransition));
  if (backgroundColor2) {
    cssText.push("background-color:" + backgroundColor2);
  }
  each(["width", "color", "radius"], function(name) {
    var borderName = "border-" + name;
    var camelCase = toCamelCase(borderName);
    var val = tooltipModel.get(camelCase);
    val != null && cssText.push(borderName + ":" + val + (name === "color" ? "" : "px"));
  });
  cssText.push(assembleFont(textStyleModel));
  if (padding != null) {
    cssText.push("padding:" + normalizeCssArray2(padding).join("px ") + "px");
  }
  return cssText.join(";") + ";";
}
function makeStyleCoord(out2, zr, container, zrX, zrY) {
  var zrPainter = zr && zr.painter;
  if (container) {
    var zrViewportRoot = zrPainter && zrPainter.getViewportRoot();
    if (zrViewportRoot) {
      transformLocalCoord(out2, zrViewportRoot, container, zrX, zrY);
    }
  } else {
    out2[0] = zrX;
    out2[1] = zrY;
    var viewportRootOffset = zrPainter && zrPainter.getViewportRootOffset();
    if (viewportRootOffset) {
      out2[0] += viewportRootOffset.offsetLeft;
      out2[1] += viewportRootOffset.offsetTop;
    }
  }
  out2[2] = out2[0] / zr.getWidth();
  out2[3] = out2[1] / zr.getHeight();
}
var TooltipHTMLContent = (
  /** @class */
  (function() {
    function TooltipHTMLContent2(api, opt) {
      this._show = false;
      this._styleCoord = [0, 0, 0, 0];
      this._enterable = true;
      this._alwaysShowContent = false;
      this._firstShow = true;
      this._longHide = true;
      if (env_default.wxa) {
        return null;
      }
      var el = document.createElement("div");
      el.domBelongToZr = true;
      this.el = el;
      var zr = this._zr = api.getZr();
      var appendTo = opt.appendTo;
      var container = appendTo && (isString(appendTo) ? document.querySelector(appendTo) : isDom(appendTo) ? appendTo : isFunction(appendTo) && appendTo(api.getDom()));
      makeStyleCoord(this._styleCoord, zr, container, api.getWidth() / 2, api.getHeight() / 2);
      (container || api.getDom()).appendChild(el);
      this._api = api;
      this._container = container;
      var self2 = this;
      el.onmouseenter = function() {
        if (self2._enterable) {
          clearTimeout(self2._hideTimeout);
          self2._show = true;
        }
        self2._inContent = true;
      };
      el.onmousemove = function(e2) {
        e2 = e2 || window.event;
        if (!self2._enterable) {
          var handler = zr.handler;
          var zrViewportRoot = zr.painter.getViewportRoot();
          normalizeEvent(zrViewportRoot, e2, true);
          handler.dispatch("mousemove", e2);
        }
      };
      el.onmouseleave = function() {
        self2._inContent = false;
        if (self2._enterable) {
          if (self2._show) {
            self2.hideLater(self2._hideDelay);
          }
        }
      };
    }
    TooltipHTMLContent2.prototype.update = function(tooltipModel) {
      if (!this._container) {
        var container = this._api.getDom();
        var position = getComputedStyle2(container, "position");
        var domStyle = container.style;
        if (domStyle.position !== "absolute" && position !== "absolute") {
          domStyle.position = "relative";
        }
      }
      var alwaysShowContent = tooltipModel.get("alwaysShowContent");
      alwaysShowContent && this._moveIfResized();
      this._alwaysShowContent = alwaysShowContent;
      this._enableDisplayTransition = tooltipModel.get("displayTransition") && tooltipModel.get("transitionDuration") > 0;
      this.el.className = tooltipModel.get("className") || "";
    };
    TooltipHTMLContent2.prototype.show = function(tooltipModel, nearPointColor) {
      clearTimeout(this._hideTimeout);
      clearTimeout(this._longHideTimeout);
      var el = this.el;
      var style2 = el.style;
      var styleCoord = this._styleCoord;
      if (!el.innerHTML) {
        style2.display = "none";
      } else {
        style2.cssText = gCssText + assembleCssText(tooltipModel, !this._firstShow, this._longHide, this._enableDisplayTransition) + assembleTransform(styleCoord[0], styleCoord[1], true) + ("border-color:" + convertToColorString(nearPointColor) + ";") + (tooltipModel.get("extraCssText") || "") + (";pointer-events:" + (this._enterable ? "auto" : "none"));
      }
      this._show = true;
      this._firstShow = false;
      this._longHide = false;
    };
    TooltipHTMLContent2.prototype.setContent = function(content, markers, tooltipModel, borderColor, arrowPosition) {
      var el = this.el;
      if (content == null) {
        el.innerHTML = "";
        return;
      }
      var arrow = "";
      if (isString(arrowPosition) && tooltipModel.get("trigger") === "item" && !shouldTooltipConfine(tooltipModel)) {
        arrow = assembleArrow(tooltipModel, borderColor, arrowPosition);
      }
      if (isString(content)) {
        el.innerHTML = content + arrow;
      } else if (content) {
        el.innerHTML = "";
        if (!isArray(content)) {
          content = [content];
        }
        for (var i2 = 0; i2 < content.length; i2++) {
          if (isDom(content[i2]) && content[i2].parentNode !== el) {
            el.appendChild(content[i2]);
          }
        }
        if (arrow && el.childNodes.length) {
          var arrowEl = document.createElement("div");
          arrowEl.innerHTML = arrow;
          el.appendChild(arrowEl);
        }
      }
    };
    TooltipHTMLContent2.prototype.setEnterable = function(enterable) {
      this._enterable = enterable;
    };
    TooltipHTMLContent2.prototype.getSize = function() {
      var el = this.el;
      return el ? [el.offsetWidth, el.offsetHeight] : [0, 0];
    };
    TooltipHTMLContent2.prototype.moveTo = function(zrX, zrY) {
      if (!this.el) {
        return;
      }
      var styleCoord = this._styleCoord;
      makeStyleCoord(styleCoord, this._zr, this._container, zrX, zrY);
      if (styleCoord[0] != null && styleCoord[1] != null) {
        var style_1 = this.el.style;
        var transforms = assembleTransform(styleCoord[0], styleCoord[1]);
        each(transforms, function(transform) {
          style_1[transform[0]] = transform[1];
        });
      }
    };
    TooltipHTMLContent2.prototype._moveIfResized = function() {
      var ratioX = this._styleCoord[2];
      var ratioY = this._styleCoord[3];
      this.moveTo(ratioX * this._zr.getWidth(), ratioY * this._zr.getHeight());
    };
    TooltipHTMLContent2.prototype.hide = function() {
      var _this = this;
      var style2 = this.el.style;
      if (this._enableDisplayTransition) {
        style2.visibility = "hidden";
        style2.opacity = "0";
      } else {
        style2.display = "none";
      }
      env_default.transform3dSupported && (style2.willChange = "");
      this._show = false;
      this._longHideTimeout = setTimeout(function() {
        return _this._longHide = true;
      }, 500);
    };
    TooltipHTMLContent2.prototype.hideLater = function(time) {
      if (this._show && !(this._inContent && this._enterable) && !this._alwaysShowContent) {
        if (time) {
          this._hideDelay = time;
          this._show = false;
          this._hideTimeout = setTimeout(bind(this.hide, this), time);
        } else {
          this.hide();
        }
      }
    };
    TooltipHTMLContent2.prototype.isShow = function() {
      return this._show;
    };
    TooltipHTMLContent2.prototype.dispose = function() {
      clearTimeout(this._hideTimeout);
      clearTimeout(this._longHideTimeout);
      var zr = this._zr;
      transformLocalCoordClear(zr && zr.painter && zr.painter.getViewportRoot(), this._container);
      var el = this.el;
      if (el) {
        el.onmouseenter = el.onmousemove = el.onmouseleave = null;
        var parentNode2 = el.parentNode;
        parentNode2 && parentNode2.removeChild(el);
      }
      this.el = this._container = null;
    };
    return TooltipHTMLContent2;
  })()
);
var TooltipHTMLContent_default = TooltipHTMLContent;

// node_modules/echarts/lib/component/tooltip/TooltipRichContent.js
var TooltipRichContent = (
  /** @class */
  (function() {
    function TooltipRichContent2(api) {
      this._show = false;
      this._styleCoord = [0, 0, 0, 0];
      this._alwaysShowContent = false;
      this._enterable = true;
      this._zr = api.getZr();
      makeStyleCoord2(this._styleCoord, this._zr, api.getWidth() / 2, api.getHeight() / 2);
    }
    TooltipRichContent2.prototype.update = function(tooltipModel) {
      var alwaysShowContent = tooltipModel.get("alwaysShowContent");
      alwaysShowContent && this._moveIfResized();
      this._alwaysShowContent = alwaysShowContent;
    };
    TooltipRichContent2.prototype.show = function() {
      if (this._hideTimeout) {
        clearTimeout(this._hideTimeout);
      }
      this.el.show();
      this._show = true;
    };
    TooltipRichContent2.prototype.setContent = function(content, markupStyleCreator, tooltipModel, borderColor, arrowPosition) {
      var _this = this;
      if (isObject(content)) {
        throwError2(true ? "Passing DOM nodes as content is not supported in richText tooltip!" : "");
      }
      if (this.el) {
        this._zr.remove(this.el);
      }
      var textStyleModel = tooltipModel.getModel("textStyle");
      this.el = new Text_default({
        style: {
          rich: markupStyleCreator.richTextStyles,
          text: content,
          lineHeight: 22,
          borderWidth: 1,
          borderColor,
          textShadowColor: textStyleModel.get("textShadowColor"),
          fill: tooltipModel.get(["textStyle", "color"]),
          padding: getPaddingFromTooltipModel(tooltipModel, "richText"),
          verticalAlign: "top",
          align: "left"
        },
        z: tooltipModel.get("z")
      });
      each(["backgroundColor", "borderRadius", "shadowColor", "shadowBlur", "shadowOffsetX", "shadowOffsetY"], function(propName) {
        _this.el.style[propName] = tooltipModel.get(propName);
      });
      each(["textShadowBlur", "textShadowOffsetX", "textShadowOffsetY"], function(propName) {
        _this.el.style[propName] = textStyleModel.get(propName) || 0;
      });
      this._zr.add(this.el);
      var self2 = this;
      this.el.on("mouseover", function() {
        if (self2._enterable) {
          clearTimeout(self2._hideTimeout);
          self2._show = true;
        }
        self2._inContent = true;
      });
      this.el.on("mouseout", function() {
        if (self2._enterable) {
          if (self2._show) {
            self2.hideLater(self2._hideDelay);
          }
        }
        self2._inContent = false;
      });
    };
    TooltipRichContent2.prototype.setEnterable = function(enterable) {
      this._enterable = enterable;
    };
    TooltipRichContent2.prototype.getSize = function() {
      var el = this.el;
      var bounding = this.el.getBoundingRect();
      var shadowOuterSize = calcShadowOuterSize(el.style);
      return [bounding.width + shadowOuterSize.left + shadowOuterSize.right, bounding.height + shadowOuterSize.top + shadowOuterSize.bottom];
    };
    TooltipRichContent2.prototype.moveTo = function(x, y) {
      var el = this.el;
      if (el) {
        var styleCoord = this._styleCoord;
        makeStyleCoord2(styleCoord, this._zr, x, y);
        x = styleCoord[0];
        y = styleCoord[1];
        var style2 = el.style;
        var borderWidth = mathMaxWith0(style2.borderWidth || 0);
        var shadowOuterSize = calcShadowOuterSize(style2);
        el.x = x + borderWidth + shadowOuterSize.left;
        el.y = y + borderWidth + shadowOuterSize.top;
        el.markRedraw();
      }
    };
    TooltipRichContent2.prototype._moveIfResized = function() {
      var ratioX = this._styleCoord[2];
      var ratioY = this._styleCoord[3];
      this.moveTo(ratioX * this._zr.getWidth(), ratioY * this._zr.getHeight());
    };
    TooltipRichContent2.prototype.hide = function() {
      if (this.el) {
        this.el.hide();
      }
      this._show = false;
    };
    TooltipRichContent2.prototype.hideLater = function(time) {
      if (this._show && !(this._inContent && this._enterable) && !this._alwaysShowContent) {
        if (time) {
          this._hideDelay = time;
          this._show = false;
          this._hideTimeout = setTimeout(bind(this.hide, this), time);
        } else {
          this.hide();
        }
      }
    };
    TooltipRichContent2.prototype.isShow = function() {
      return this._show;
    };
    TooltipRichContent2.prototype.dispose = function() {
      this._zr.remove(this.el);
    };
    return TooltipRichContent2;
  })()
);
function mathMaxWith0(val) {
  return Math.max(0, val);
}
function calcShadowOuterSize(style2) {
  var shadowBlur = mathMaxWith0(style2.shadowBlur || 0);
  var shadowOffsetX = mathMaxWith0(style2.shadowOffsetX || 0);
  var shadowOffsetY = mathMaxWith0(style2.shadowOffsetY || 0);
  return {
    left: mathMaxWith0(shadowBlur - shadowOffsetX),
    right: mathMaxWith0(shadowBlur + shadowOffsetX),
    top: mathMaxWith0(shadowBlur - shadowOffsetY),
    bottom: mathMaxWith0(shadowBlur + shadowOffsetY)
  };
}
function makeStyleCoord2(out2, zr, zrX, zrY) {
  out2[0] = zrX;
  out2[1] = zrY;
  out2[2] = out2[0] / zr.getWidth();
  out2[3] = out2[1] / zr.getHeight();
}
var TooltipRichContent_default = TooltipRichContent;

// node_modules/echarts/lib/component/tooltip/TooltipView.js
var proxyRect = new Rect_default({
  shape: {
    x: -1,
    y: -1,
    width: 2,
    height: 2
  }
});
var TooltipView = (
  /** @class */
  (function(_super) {
    __extends(TooltipView2, _super);
    function TooltipView2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = TooltipView2.type;
      return _this;
    }
    TooltipView2.prototype.init = function(ecModel, api) {
      if (env_default.node || !api.getDom()) {
        return;
      }
      var tooltipModel = ecModel.getComponent("tooltip");
      var renderMode = this._renderMode = getTooltipRenderMode(tooltipModel.get("renderMode"));
      this._tooltipContent = renderMode === "richText" ? new TooltipRichContent_default(api) : new TooltipHTMLContent_default(api, {
        appendTo: tooltipModel.get("appendToBody", true) ? "body" : tooltipModel.get("appendTo", true)
      });
    };
    TooltipView2.prototype.render = function(tooltipModel, ecModel, api) {
      if (env_default.node || !api.getDom()) {
        return;
      }
      this.group.removeAll();
      this._tooltipModel = tooltipModel;
      this._ecModel = ecModel;
      this._api = api;
      var tooltipContent = this._tooltipContent;
      tooltipContent.update(tooltipModel);
      tooltipContent.setEnterable(tooltipModel.get("enterable"));
      this._initGlobalListener();
      this._keepShow();
      if (this._renderMode !== "richText" && tooltipModel.get("transitionDuration")) {
        createOrUpdate(this, "_updatePosition", 50, "fixRate");
      } else {
        clear(this, "_updatePosition");
      }
    };
    TooltipView2.prototype._initGlobalListener = function() {
      var tooltipModel = this._tooltipModel;
      var triggerOn = tooltipModel.get("triggerOn");
      register("itemTooltip", this._api, bind(function(currTrigger, e2, dispatchAction3) {
        if (triggerOn !== "none") {
          if (triggerOn.indexOf(currTrigger) >= 0) {
            this._tryShow(e2, dispatchAction3);
          } else if (currTrigger === "leave") {
            this._hide(dispatchAction3);
          }
        }
      }, this));
    };
    TooltipView2.prototype._keepShow = function() {
      var tooltipModel = this._tooltipModel;
      var ecModel = this._ecModel;
      var api = this._api;
      var triggerOn = tooltipModel.get("triggerOn");
      if (tooltipModel.get("trigger") !== "axis") {
        this._lastDataByCoordSys = null;
        this._cbParamsList = null;
      }
      if (this._lastX != null && this._lastY != null && triggerOn !== "none" && triggerOn !== "click") {
        var self_1 = this;
        clearTimeout(this._refreshUpdateTimeout);
        this._refreshUpdateTimeout = setTimeout(function() {
          !api.isDisposed() && self_1.manuallyShowTip(tooltipModel, ecModel, api, {
            x: self_1._lastX,
            y: self_1._lastY,
            dataByCoordSys: self_1._lastDataByCoordSys
          });
        });
      }
    };
    TooltipView2.prototype.manuallyShowTip = function(tooltipModel, ecModel, api, payload) {
      if (payload.from === this.uid || env_default.node || !api.getDom()) {
        return;
      }
      var dispatchAction3 = makeDispatchAction2(payload, api);
      this._ticket = "";
      var dataByCoordSys = payload.dataByCoordSys;
      var cmptRef = findComponentReference(payload, ecModel, api);
      if (cmptRef) {
        var rect = cmptRef.el.getBoundingRect().clone();
        rect.applyTransform(cmptRef.el.transform);
        this._tryShow({
          offsetX: rect.x + rect.width / 2,
          offsetY: rect.y + rect.height / 2,
          target: cmptRef.el,
          position: payload.position,
          // When manully trigger, the mouse is not on the el, so we'd better to
          // position tooltip on the bottom of the el and display arrow is possible.
          positionDefault: "bottom"
        }, dispatchAction3);
      } else if (payload.tooltip && payload.x != null && payload.y != null) {
        var el = proxyRect;
        el.x = payload.x;
        el.y = payload.y;
        el.update();
        getECData(el).tooltipConfig = {
          name: null,
          option: payload.tooltip
        };
        this._tryShow({
          offsetX: payload.x,
          offsetY: payload.y,
          target: el
        }, dispatchAction3);
      } else if (dataByCoordSys) {
        this._tryShow({
          offsetX: payload.x,
          offsetY: payload.y,
          position: payload.position,
          dataByCoordSys,
          tooltipOption: payload.tooltipOption
        }, dispatchAction3);
      } else if (payload.seriesIndex != null) {
        if (this._manuallyAxisShowTip(tooltipModel, ecModel, api, payload)) {
          return;
        }
        var pointInfo = findPointFromSeries(payload, ecModel);
        var cx = pointInfo.point[0];
        var cy = pointInfo.point[1];
        if (cx != null && cy != null) {
          this._tryShow({
            offsetX: cx,
            offsetY: cy,
            target: pointInfo.el,
            position: payload.position,
            // When manully trigger, the mouse is not on the el, so we'd better to
            // position tooltip on the bottom of the el and display arrow is possible.
            positionDefault: "bottom"
          }, dispatchAction3);
        }
      } else if (payload.x != null && payload.y != null) {
        api.dispatchAction({
          type: "updateAxisPointer",
          x: payload.x,
          y: payload.y
        });
        this._tryShow({
          offsetX: payload.x,
          offsetY: payload.y,
          position: payload.position,
          target: api.getZr().findHover(payload.x, payload.y).target
        }, dispatchAction3);
      }
    };
    TooltipView2.prototype.manuallyHideTip = function(tooltipModel, ecModel, api, payload) {
      var tooltipContent = this._tooltipContent;
      if (this._tooltipModel) {
        tooltipContent.hideLater(this._tooltipModel.get("hideDelay"));
      }
      this._lastX = this._lastY = this._lastDataByCoordSys = null;
      this._cbParamsList = null;
      if (payload.from !== this.uid) {
        this._hide(makeDispatchAction2(payload, api));
      }
    };
    TooltipView2.prototype._manuallyAxisShowTip = function(tooltipModel, ecModel, api, payload) {
      var seriesIndex = payload.seriesIndex;
      var dataIndex = payload.dataIndex;
      var coordSysAxesInfo = ecModel.getComponent("axisPointer").coordSysAxesInfo;
      if (seriesIndex == null || dataIndex == null || coordSysAxesInfo == null) {
        return;
      }
      var seriesModel = ecModel.getSeriesByIndex(seriesIndex);
      if (!seriesModel) {
        return;
      }
      var data = seriesModel.getData();
      var tooltipCascadedModel = buildTooltipModel([data.getItemModel(dataIndex), seriesModel, (seriesModel.coordinateSystem || {}).model], this._tooltipModel);
      if (tooltipCascadedModel.get("trigger") !== "axis") {
        return;
      }
      api.dispatchAction({
        type: "updateAxisPointer",
        seriesIndex,
        dataIndex,
        position: payload.position
      });
      return true;
    };
    TooltipView2.prototype._tryShow = function(e2, dispatchAction3) {
      var el = e2.target;
      var tooltipModel = this._tooltipModel;
      if (!tooltipModel) {
        return;
      }
      this._lastX = e2.offsetX;
      this._lastY = e2.offsetY;
      var dataByCoordSys = e2.dataByCoordSys;
      if (dataByCoordSys && dataByCoordSys.length) {
        this._showAxisTooltip(dataByCoordSys, e2);
      } else if (el) {
        var ecData = getECData(el);
        if (ecData.ssrType === "legend") {
          return;
        }
        this._lastDataByCoordSys = null;
        this._cbParamsList = null;
        var seriesDispatcher_1;
        var cmptDispatcher_1;
        findEventDispatcher(el, function(target) {
          if (target.tooltipDisabled) {
            seriesDispatcher_1 = cmptDispatcher_1 = null;
            return true;
          }
          if (seriesDispatcher_1 || cmptDispatcher_1) {
            return;
          }
          if (getECData(target).dataIndex != null) {
            seriesDispatcher_1 = target;
          } else if (getECData(target).tooltipConfig != null) {
            cmptDispatcher_1 = target;
          }
        }, true);
        if (seriesDispatcher_1) {
          this._showSeriesItemTooltip(e2, seriesDispatcher_1, dispatchAction3);
        } else if (cmptDispatcher_1) {
          this._showComponentItemTooltip(e2, cmptDispatcher_1, dispatchAction3);
        } else {
          this._hide(dispatchAction3);
        }
      } else {
        this._lastDataByCoordSys = null;
        this._cbParamsList = null;
        this._hide(dispatchAction3);
      }
    };
    TooltipView2.prototype._showOrMove = function(tooltipModel, cb) {
      var delay2 = tooltipModel.get("showDelay");
      cb = bind(cb, this);
      clearTimeout(this._showTimout);
      delay2 > 0 ? this._showTimout = setTimeout(cb, delay2) : cb();
    };
    TooltipView2.prototype._showAxisTooltip = function(dataByCoordSys, e2) {
      var ecModel = this._ecModel;
      var globalTooltipModel = this._tooltipModel;
      var point = [e2.offsetX, e2.offsetY];
      var singleTooltipModel = buildTooltipModel([e2.tooltipOption], globalTooltipModel);
      var renderMode = this._renderMode;
      var cbParamsList = [];
      var articleMarkup = createTooltipMarkup("section", {
        blocks: [],
        noHeader: true
      });
      var markupTextArrLegacy = [];
      var markupStyleCreator = new TooltipMarkupStyleCreator();
      each(dataByCoordSys, function(itemCoordSys) {
        each(itemCoordSys.dataByAxis, function(axisItem) {
          var axisModel = ecModel.getComponent(axisItem.axisDim + "Axis", axisItem.axisIndex);
          var axisValue = axisItem.value;
          var axis = axisModel.axis;
          var axisValueParsed = axis.scale.parse(axisValue);
          if (!axisModel || axisValue == null) {
            return;
          }
          var axisValueLabel = getValueLabel(axisValue, axis, ecModel, axisItem.seriesDataIndices, axisItem.valueLabelOpt);
          var axisSectionMarkup = createTooltipMarkup("section", {
            header: axisValueLabel,
            noHeader: !trim(axisValueLabel),
            sortBlocks: true,
            blocks: []
          });
          articleMarkup.blocks.push(axisSectionMarkup);
          each(axisItem.seriesDataIndices, function(idxItem) {
            var series = ecModel.getSeriesByIndex(idxItem.seriesIndex);
            var dataIndex = idxItem.dataIndexInside;
            var cbParams = series.getDataParams(dataIndex);
            if (cbParams.dataIndex < 0) {
              return;
            }
            cbParams.axisDim = axisItem.axisDim;
            cbParams.axisIndex = axisItem.axisIndex;
            cbParams.axisType = axisItem.axisType;
            cbParams.axisId = axisItem.axisId;
            cbParams.axisValue = getAxisRawValue(axisModel.axis, {
              value: axisValueParsed
            });
            cbParams.axisValueLabel = axisValueLabel;
            cbParams.marker = markupStyleCreator.makeTooltipMarker("item", convertToColorString(cbParams.color), renderMode);
            var seriesTooltipResult = normalizeTooltipFormatResult(series.formatTooltip(dataIndex, true, null));
            var frag = seriesTooltipResult.frag;
            if (frag) {
              var valueFormatter = buildTooltipModel([series], globalTooltipModel).get("valueFormatter");
              axisSectionMarkup.blocks.push(valueFormatter ? extend({
                valueFormatter
              }, frag) : frag);
            }
            if (seriesTooltipResult.text) {
              markupTextArrLegacy.push(seriesTooltipResult.text);
            }
            cbParamsList.push(cbParams);
          });
        });
      });
      articleMarkup.blocks.reverse();
      markupTextArrLegacy.reverse();
      var positionExpr = e2.position;
      var orderMode = singleTooltipModel.get("order");
      var builtMarkupText = buildTooltipMarkup(articleMarkup, markupStyleCreator, renderMode, orderMode, ecModel.get("useUTC"), singleTooltipModel.get("textStyle"));
      builtMarkupText && markupTextArrLegacy.unshift(builtMarkupText);
      var blockBreak = renderMode === "richText" ? "\n\n" : "<br/>";
      var allMarkupText = markupTextArrLegacy.join(blockBreak);
      this._showOrMove(singleTooltipModel, function() {
        if (this._updateContentNotChangedOnAxis(dataByCoordSys, cbParamsList)) {
          this._updatePosition(singleTooltipModel, positionExpr, point[0], point[1], this._tooltipContent, cbParamsList);
        } else {
          this._showTooltipContent(singleTooltipModel, allMarkupText, cbParamsList, Math.random() + "", point[0], point[1], positionExpr, null, markupStyleCreator);
        }
      });
    };
    TooltipView2.prototype._showSeriesItemTooltip = function(e2, dispatcher, dispatchAction3) {
      var ecModel = this._ecModel;
      var ecData = getECData(dispatcher);
      var seriesIndex = ecData.seriesIndex;
      var seriesModel = ecModel.getSeriesByIndex(seriesIndex);
      var dataModel = ecData.dataModel || seriesModel;
      var dataIndex = ecData.dataIndex;
      var dataType = ecData.dataType;
      var data = dataModel.getData(dataType);
      var renderMode = this._renderMode;
      var positionDefault = e2.positionDefault;
      var tooltipModel = buildTooltipModel([data.getItemModel(dataIndex), dataModel, seriesModel && (seriesModel.coordinateSystem || {}).model], this._tooltipModel, positionDefault ? {
        position: positionDefault
      } : null);
      var tooltipTrigger = tooltipModel.get("trigger");
      if (tooltipTrigger != null && tooltipTrigger !== "item") {
        return;
      }
      var params = dataModel.getDataParams(dataIndex, dataType);
      var markupStyleCreator = new TooltipMarkupStyleCreator();
      params.marker = markupStyleCreator.makeTooltipMarker("item", convertToColorString(params.color), renderMode);
      var seriesTooltipResult = normalizeTooltipFormatResult(dataModel.formatTooltip(dataIndex, false, dataType));
      var orderMode = tooltipModel.get("order");
      var valueFormatter = tooltipModel.get("valueFormatter");
      var frag = seriesTooltipResult.frag;
      var markupText = frag ? buildTooltipMarkup(valueFormatter ? extend({
        valueFormatter
      }, frag) : frag, markupStyleCreator, renderMode, orderMode, ecModel.get("useUTC"), tooltipModel.get("textStyle")) : seriesTooltipResult.text;
      var asyncTicket = "item_" + dataModel.name + "_" + dataIndex;
      this._showOrMove(tooltipModel, function() {
        this._showTooltipContent(tooltipModel, markupText, params, asyncTicket, e2.offsetX, e2.offsetY, e2.position, e2.target, markupStyleCreator);
      });
      dispatchAction3({
        type: "showTip",
        dataIndexInside: dataIndex,
        dataIndex: data.getRawIndex(dataIndex),
        seriesIndex,
        from: this.uid
      });
    };
    TooltipView2.prototype._showComponentItemTooltip = function(e2, el, dispatchAction3) {
      var isHTMLRenderMode = this._renderMode === "html";
      var ecData = getECData(el);
      var tooltipConfig = ecData.tooltipConfig;
      var tooltipOpt = tooltipConfig.option || {};
      var encodeHTMLContent = tooltipOpt.encodeHTMLContent;
      if (isString(tooltipOpt)) {
        var content = tooltipOpt;
        tooltipOpt = {
          content,
          // Fixed formatter
          formatter: content
        };
        encodeHTMLContent = true;
      }
      if (encodeHTMLContent && isHTMLRenderMode && tooltipOpt.content) {
        tooltipOpt = clone(tooltipOpt);
        tooltipOpt.content = encodeHTML(tooltipOpt.content);
      }
      var tooltipModelCascade = [tooltipOpt];
      var cmpt = this._ecModel.getComponent(ecData.componentMainType, ecData.componentIndex);
      if (cmpt) {
        tooltipModelCascade.push(cmpt);
      }
      tooltipModelCascade.push({
        formatter: tooltipOpt.content
      });
      var positionDefault = e2.positionDefault;
      var subTooltipModel = buildTooltipModel(tooltipModelCascade, this._tooltipModel, positionDefault ? {
        position: positionDefault
      } : null);
      var defaultHtml = subTooltipModel.get("content");
      var asyncTicket = Math.random() + "";
      var markupStyleCreator = new TooltipMarkupStyleCreator();
      this._showOrMove(subTooltipModel, function() {
        var formatterParams = clone(subTooltipModel.get("formatterParams") || {});
        this._showTooltipContent(subTooltipModel, defaultHtml, formatterParams, asyncTicket, e2.offsetX, e2.offsetY, e2.position, el, markupStyleCreator);
      });
      dispatchAction3({
        type: "showTip",
        from: this.uid
      });
    };
    TooltipView2.prototype._showTooltipContent = function(tooltipModel, defaultHtml, params, asyncTicket, x, y, positionExpr, el, markupStyleCreator) {
      this._ticket = "";
      if (!tooltipModel.get("showContent") || !tooltipModel.get("show")) {
        return;
      }
      var tooltipContent = this._tooltipContent;
      tooltipContent.setEnterable(tooltipModel.get("enterable"));
      var formatter = tooltipModel.get("formatter");
      positionExpr = positionExpr || tooltipModel.get("position");
      var html = defaultHtml;
      var nearPoint = this._getNearestPoint([x, y], params, tooltipModel.get("trigger"), tooltipModel.get("borderColor"), tooltipModel.get("defaultBorderColor", true));
      var nearPointColor = nearPoint.color;
      if (formatter) {
        if (isString(formatter)) {
          var useUTC = tooltipModel.ecModel.get("useUTC");
          var params0 = isArray(params) ? params[0] : params;
          var isTimeAxis = params0 && params0.axisType && params0.axisType.indexOf("time") >= 0;
          html = formatter;
          if (isTimeAxis) {
            html = format(params0.axisValue, html, useUTC);
          }
          html = formatTpl(html, params, true);
        } else if (isFunction(formatter)) {
          var callback = bind(function(cbTicket, html2) {
            if (cbTicket === this._ticket) {
              tooltipContent.setContent(html2, markupStyleCreator, tooltipModel, nearPointColor, positionExpr);
              this._updatePosition(tooltipModel, positionExpr, x, y, tooltipContent, params, el);
            }
          }, this);
          this._ticket = asyncTicket;
          html = formatter(params, asyncTicket, callback);
        } else {
          html = formatter;
        }
      }
      tooltipContent.setContent(html, markupStyleCreator, tooltipModel, nearPointColor, positionExpr);
      tooltipContent.show(tooltipModel, nearPointColor);
      this._updatePosition(tooltipModel, positionExpr, x, y, tooltipContent, params, el);
    };
    TooltipView2.prototype._getNearestPoint = function(point, tooltipDataParams, trigger4, borderColor, defaultBorderColor) {
      if (trigger4 === "axis" || isArray(tooltipDataParams)) {
        return {
          color: borderColor || defaultBorderColor
        };
      }
      if (!isArray(tooltipDataParams)) {
        return {
          color: borderColor || tooltipDataParams.color || tooltipDataParams.borderColor
        };
      }
    };
    TooltipView2.prototype._updatePosition = function(tooltipModel, positionExpr, x, y, content, params, el) {
      var viewWidth = this._api.getWidth();
      var viewHeight = this._api.getHeight();
      positionExpr = positionExpr || tooltipModel.get("position");
      var contentSize = content.getSize();
      var align = tooltipModel.get("align");
      var vAlign = tooltipModel.get("verticalAlign");
      var rect = el && el.getBoundingRect().clone();
      el && rect.applyTransform(el.transform);
      if (isFunction(positionExpr)) {
        positionExpr = positionExpr([x, y], params, content.el, rect, {
          viewSize: [viewWidth, viewHeight],
          contentSize: contentSize.slice()
        });
      }
      if (isArray(positionExpr)) {
        x = parsePercent2(positionExpr[0], viewWidth);
        y = parsePercent2(positionExpr[1], viewHeight);
      } else if (isObject(positionExpr)) {
        var boxLayoutPosition = positionExpr;
        boxLayoutPosition.width = contentSize[0];
        boxLayoutPosition.height = contentSize[1];
        var layoutRect = getLayoutRect(boxLayoutPosition, {
          width: viewWidth,
          height: viewHeight
        });
        x = layoutRect.x;
        y = layoutRect.y;
        align = null;
        vAlign = null;
      } else if (isString(positionExpr) && el) {
        var pos = calcTooltipPosition(positionExpr, rect, contentSize, tooltipModel.get("borderWidth"));
        x = pos[0];
        y = pos[1];
      } else {
        var pos = refixTooltipPosition(x, y, content, viewWidth, viewHeight, align ? null : 20, vAlign ? null : 20);
        x = pos[0];
        y = pos[1];
      }
      align && (x -= isCenterAlign(align) ? contentSize[0] / 2 : align === "right" ? contentSize[0] : 0);
      vAlign && (y -= isCenterAlign(vAlign) ? contentSize[1] / 2 : vAlign === "bottom" ? contentSize[1] : 0);
      if (shouldTooltipConfine(tooltipModel)) {
        var pos = confineTooltipPosition(x, y, content, viewWidth, viewHeight);
        x = pos[0];
        y = pos[1];
      }
      content.moveTo(x, y);
    };
    TooltipView2.prototype._updateContentNotChangedOnAxis = function(dataByCoordSys, cbParamsList) {
      var lastCoordSys = this._lastDataByCoordSys;
      var lastCbParamsList = this._cbParamsList;
      var contentNotChanged = !!lastCoordSys && lastCoordSys.length === dataByCoordSys.length;
      contentNotChanged && each(lastCoordSys, function(lastItemCoordSys, indexCoordSys) {
        var lastDataByAxis = lastItemCoordSys.dataByAxis || [];
        var thisItemCoordSys = dataByCoordSys[indexCoordSys] || {};
        var thisDataByAxis = thisItemCoordSys.dataByAxis || [];
        contentNotChanged = contentNotChanged && lastDataByAxis.length === thisDataByAxis.length;
        contentNotChanged && each(lastDataByAxis, function(lastItem, indexAxis) {
          var thisItem = thisDataByAxis[indexAxis] || {};
          var lastIndices = lastItem.seriesDataIndices || [];
          var newIndices = thisItem.seriesDataIndices || [];
          contentNotChanged = contentNotChanged && lastItem.value === thisItem.value && lastItem.axisType === thisItem.axisType && lastItem.axisId === thisItem.axisId && lastIndices.length === newIndices.length;
          contentNotChanged && each(lastIndices, function(lastIdxItem, j) {
            var newIdxItem = newIndices[j];
            contentNotChanged = contentNotChanged && lastIdxItem.seriesIndex === newIdxItem.seriesIndex && lastIdxItem.dataIndex === newIdxItem.dataIndex;
          });
          lastCbParamsList && each(lastItem.seriesDataIndices, function(idxItem) {
            var seriesIdx = idxItem.seriesIndex;
            var cbParams = cbParamsList[seriesIdx];
            var lastCbParams = lastCbParamsList[seriesIdx];
            if (cbParams && lastCbParams && lastCbParams.data !== cbParams.data) {
              contentNotChanged = false;
            }
          });
        });
      });
      this._lastDataByCoordSys = dataByCoordSys;
      this._cbParamsList = cbParamsList;
      return !!contentNotChanged;
    };
    TooltipView2.prototype._hide = function(dispatchAction3) {
      this._lastDataByCoordSys = null;
      this._cbParamsList = null;
      dispatchAction3({
        type: "hideTip",
        from: this.uid
      });
    };
    TooltipView2.prototype.dispose = function(ecModel, api) {
      if (env_default.node || !api.getDom()) {
        return;
      }
      clear(this, "_updatePosition");
      this._tooltipContent.dispose();
      unregister("itemTooltip", api);
      this._tooltipContent = null;
      this._tooltipModel = null;
      this._lastDataByCoordSys = null;
      this._cbParamsList = null;
    };
    TooltipView2.type = "tooltip";
    return TooltipView2;
  })(Component_default2)
);
function buildTooltipModel(modelCascade, globalTooltipModel, defaultTooltipOption) {
  var ecModel = globalTooltipModel.ecModel;
  var resultModel;
  if (defaultTooltipOption) {
    resultModel = new Model_default(defaultTooltipOption, ecModel, ecModel);
    resultModel = new Model_default(globalTooltipModel.option, resultModel, ecModel);
  } else {
    resultModel = globalTooltipModel;
  }
  for (var i2 = modelCascade.length - 1; i2 >= 0; i2--) {
    var tooltipOpt = modelCascade[i2];
    if (tooltipOpt) {
      if (tooltipOpt instanceof Model_default) {
        tooltipOpt = tooltipOpt.get("tooltip", true);
      }
      if (isString(tooltipOpt)) {
        tooltipOpt = {
          formatter: tooltipOpt
        };
      }
      if (tooltipOpt) {
        resultModel = new Model_default(tooltipOpt, resultModel, ecModel);
      }
    }
  }
  return resultModel;
}
function makeDispatchAction2(payload, api) {
  return payload.dispatchAction || bind(api.dispatchAction, api);
}
function refixTooltipPosition(x, y, content, viewWidth, viewHeight, gapH, gapV) {
  var size = content.getSize();
  var width = size[0];
  var height = size[1];
  if (gapH != null) {
    if (x + width + gapH + 2 > viewWidth) {
      x -= width + gapH;
    } else {
      x += gapH;
    }
  }
  if (gapV != null) {
    if (y + height + gapV > viewHeight) {
      y -= height + gapV;
    } else {
      y += gapV;
    }
  }
  return [x, y];
}
function confineTooltipPosition(x, y, content, viewWidth, viewHeight) {
  var size = content.getSize();
  var width = size[0];
  var height = size[1];
  x = Math.min(x + width, viewWidth) - width;
  y = Math.min(y + height, viewHeight) - height;
  x = Math.max(x, 0);
  y = Math.max(y, 0);
  return [x, y];
}
function calcTooltipPosition(position, rect, contentSize, borderWidth) {
  var domWidth = contentSize[0];
  var domHeight = contentSize[1];
  var offset = Math.ceil(Math.SQRT2 * borderWidth) + 8;
  var x = 0;
  var y = 0;
  var rectWidth = rect.width;
  var rectHeight = rect.height;
  switch (position) {
    case "inside":
      x = rect.x + rectWidth / 2 - domWidth / 2;
      y = rect.y + rectHeight / 2 - domHeight / 2;
      break;
    case "top":
      x = rect.x + rectWidth / 2 - domWidth / 2;
      y = rect.y - domHeight - offset;
      break;
    case "bottom":
      x = rect.x + rectWidth / 2 - domWidth / 2;
      y = rect.y + rectHeight + offset;
      break;
    case "left":
      x = rect.x - domWidth - offset;
      y = rect.y + rectHeight / 2 - domHeight / 2;
      break;
    case "right":
      x = rect.x + rectWidth + offset;
      y = rect.y + rectHeight / 2 - domHeight / 2;
  }
  return [x, y];
}
function isCenterAlign(align) {
  return align === "center" || align === "middle";
}
function findComponentReference(payload, ecModel, api) {
  var queryOptionMap = preParseFinder(payload).queryOptionMap;
  var componentMainType = queryOptionMap.keys()[0];
  if (!componentMainType || componentMainType === "series") {
    return;
  }
  var queryResult = queryReferringComponents(ecModel, componentMainType, queryOptionMap.get(componentMainType), {
    useDefault: false,
    enableAll: false,
    enableNone: false
  });
  var model2 = queryResult.models[0];
  if (!model2) {
    return;
  }
  var view = api.getViewOfComponentModel(model2);
  var el;
  view.group.traverse(function(subEl) {
    var tooltipConfig = getECData(subEl).tooltipConfig;
    if (tooltipConfig && tooltipConfig.name === payload.name) {
      el = subEl;
      return true;
    }
  });
  if (el) {
    return {
      componentMainType,
      componentIndex: model2.componentIndex,
      el
    };
  }
}
var TooltipView_default = TooltipView;

// node_modules/echarts/lib/component/tooltip/install.js
function install33(registers) {
  use(install28);
  registers.registerComponentModel(TooltipModel_default);
  registers.registerComponentView(TooltipView_default);
  registers.registerAction({
    type: "showTip",
    event: "showTip",
    update: "tooltip:manuallyShowTip"
  }, noop);
  registers.registerAction({
    type: "hideTip",
    event: "hideTip",
    update: "tooltip:manuallyHideTip"
  }, noop);
}

// node_modules/echarts/lib/component/brush/preprocessor.js
var DEFAULT_TOOLBOX_BTNS = ["rect", "polygon", "keep", "clear"];
function brushPreprocessor(option, isNew) {
  var brushComponents = normalizeToArray(option ? option.brush : []);
  if (!brushComponents.length) {
    return;
  }
  var brushComponentSpecifiedBtns = [];
  each(brushComponents, function(brushOpt) {
    var tbs = brushOpt.hasOwnProperty("toolbox") ? brushOpt.toolbox : [];
    if (tbs instanceof Array) {
      brushComponentSpecifiedBtns = brushComponentSpecifiedBtns.concat(tbs);
    }
  });
  var toolbox = option && option.toolbox;
  if (isArray(toolbox)) {
    toolbox = toolbox[0];
  }
  if (!toolbox) {
    toolbox = {
      feature: {}
    };
    option.toolbox = [toolbox];
  }
  var toolboxFeature = toolbox.feature || (toolbox.feature = {});
  var toolboxBrush = toolboxFeature.brush || (toolboxFeature.brush = {});
  var brushTypes = toolboxBrush.type || (toolboxBrush.type = []);
  brushTypes.push.apply(brushTypes, brushComponentSpecifiedBtns);
  removeDuplicates(brushTypes, function(item) {
    return item + "";
  }, null);
  if (isNew && !brushTypes.length) {
    brushTypes.push.apply(brushTypes, DEFAULT_TOOLBOX_BTNS);
  }
}

// node_modules/echarts/lib/visual/visualSolution.js
var each8 = each;
function hasKeys(obj) {
  if (obj) {
    for (var name_1 in obj) {
      if (obj.hasOwnProperty(name_1)) {
        return true;
      }
    }
  }
}
function createVisualMappings(option, stateList, supplementVisualOption) {
  var visualMappings = {};
  each8(stateList, function(state2) {
    var mappings = visualMappings[state2] = createMappings();
    each8(option[state2], function(visualData, visualType) {
      if (!VisualMapping_default.isValidType(visualType)) {
        return;
      }
      var mappingOption = {
        type: visualType,
        visual: visualData
      };
      supplementVisualOption && supplementVisualOption(mappingOption, state2);
      mappings[visualType] = new VisualMapping_default(mappingOption);
      if (visualType === "opacity") {
        mappingOption = clone(mappingOption);
        mappingOption.type = "colorAlpha";
        mappings.__hidden.__alphaForOpacity = new VisualMapping_default(mappingOption);
      }
    });
  });
  return visualMappings;
  function createMappings() {
    var Creater = function() {
    };
    Creater.prototype.__hidden = Creater.prototype;
    var obj = new Creater();
    return obj;
  }
}
function replaceVisualOption(thisOption, newOption, keys2) {
  var has;
  each(keys2, function(key2) {
    if (newOption.hasOwnProperty(key2) && hasKeys(newOption[key2])) {
      has = true;
    }
  });
  has && each(keys2, function(key2) {
    if (newOption.hasOwnProperty(key2) && hasKeys(newOption[key2])) {
      thisOption[key2] = clone(newOption[key2]);
    } else {
      delete thisOption[key2];
    }
  });
}
function applyVisual(stateList, visualMappings, data, getValueState, scope, dimension) {
  var visualTypesMap = {};
  each(stateList, function(state2) {
    var visualTypes = VisualMapping_default.prepareVisualTypes(visualMappings[state2]);
    visualTypesMap[state2] = visualTypes;
  });
  var dataIndex;
  function getVisual(key2) {
    return getItemVisualFromData(data, dataIndex, key2);
  }
  function setVisual(key2, value) {
    setItemVisualFromData(data, dataIndex, key2, value);
  }
  if (dimension == null) {
    data.each(eachItem);
  } else {
    data.each([dimension], eachItem);
  }
  function eachItem(valueOrIndex, index) {
    dataIndex = dimension == null ? valueOrIndex : index;
    var rawDataItem = data.getRawDataItem(dataIndex);
    if (rawDataItem && rawDataItem.visualMap === false) {
      return;
    }
    var valueState = getValueState.call(scope, valueOrIndex);
    var mappings = visualMappings[valueState];
    var visualTypes = visualTypesMap[valueState];
    for (var i2 = 0, len2 = visualTypes.length; i2 < len2; i2++) {
      var type = visualTypes[i2];
      mappings[type] && mappings[type].applyVisual(valueOrIndex, getVisual, setVisual);
    }
  }
}

// node_modules/echarts/lib/component/brush/selector.js
function makeBrushCommonSelectorForSeries(area) {
  var brushType = area.brushType;
  var selectors = {
    point: function(itemLayout) {
      return selector[brushType].point(itemLayout, selectors, area);
    },
    rect: function(itemLayout) {
      return selector[brushType].rect(itemLayout, selectors, area);
    }
  };
  return selectors;
}
var selector = {
  lineX: getLineSelectors(0),
  lineY: getLineSelectors(1),
  rect: {
    point: function(itemLayout, selectors, area) {
      return itemLayout && area.boundingRect.contain(itemLayout[0], itemLayout[1]);
    },
    rect: function(itemLayout, selectors, area) {
      return itemLayout && area.boundingRect.intersect(itemLayout);
    }
  },
  polygon: {
    point: function(itemLayout, selectors, area) {
      return itemLayout && area.boundingRect.contain(itemLayout[0], itemLayout[1]) && contain2(area.range, itemLayout[0], itemLayout[1]);
    },
    rect: function(itemLayout, selectors, area) {
      var points4 = area.range;
      if (!itemLayout || points4.length <= 1) {
        return false;
      }
      var x = itemLayout.x;
      var y = itemLayout.y;
      var width = itemLayout.width;
      var height = itemLayout.height;
      var p = points4[0];
      if (contain2(points4, x, y) || contain2(points4, x + width, y) || contain2(points4, x, y + height) || contain2(points4, x + width, y + height) || BoundingRect_default.create(itemLayout).contain(p[0], p[1]) || linePolygonIntersect(x, y, x + width, y, points4) || linePolygonIntersect(x, y, x, y + height, points4) || linePolygonIntersect(x + width, y, x + width, y + height, points4) || linePolygonIntersect(x, y + height, x + width, y + height, points4)) {
        return true;
      }
    }
  }
};
function getLineSelectors(xyIndex) {
  var xy = ["x", "y"];
  var wh = ["width", "height"];
  return {
    point: function(itemLayout, selectors, area) {
      if (itemLayout) {
        var range = area.range;
        var p = itemLayout[xyIndex];
        return inLineRange(p, range);
      }
    },
    rect: function(itemLayout, selectors, area) {
      if (itemLayout) {
        var range = area.range;
        var layoutRange = [itemLayout[xy[xyIndex]], itemLayout[xy[xyIndex]] + itemLayout[wh[xyIndex]]];
        layoutRange[1] < layoutRange[0] && layoutRange.reverse();
        return inLineRange(layoutRange[0], range) || inLineRange(layoutRange[1], range) || inLineRange(range[0], layoutRange) || inLineRange(range[1], layoutRange);
      }
    }
  };
}
function inLineRange(p, range) {
  return range[0] <= p && p <= range[1];
}

// node_modules/echarts/lib/component/brush/visualEncoding.js
var STATE_LIST = ["inBrush", "outOfBrush"];
var DISPATCH_METHOD = "__ecBrushSelect";
var DISPATCH_FLAG = "__ecInBrushSelectEvent";
function layoutCovers(ecModel) {
  ecModel.eachComponent({
    mainType: "brush"
  }, function(brushModel) {
    var brushTargetManager = brushModel.brushTargetManager = new BrushTargetManager_default(brushModel.option, ecModel);
    brushTargetManager.setInputRanges(brushModel.areas, ecModel);
  });
}
var brushVisualStageHandler = createSimpleOverallStageHandler2(brushVisual);
function brushVisual(ecModel, api, payload) {
  var brushSelected = [];
  var throttleType;
  var throttleDelay;
  ecModel.eachComponent({
    mainType: "brush"
  }, function(brushModel) {
    payload && payload.type === "takeGlobalCursor" && brushModel.setBrushOption(payload.key === "brush" ? payload.brushOption : {
      brushType: false
    });
  });
  layoutCovers(ecModel);
  ecModel.eachComponent({
    mainType: "brush"
  }, function(brushModel, brushIndex) {
    var thisBrushSelected = {
      brushId: brushModel.id,
      brushIndex,
      brushName: brushModel.name,
      areas: clone(brushModel.areas),
      selected: []
    };
    brushSelected.push(thisBrushSelected);
    var brushOption = brushModel.option;
    var brushLink = brushOption.brushLink;
    var linkedSeriesMap = [];
    var selectedDataIndexForLink = [];
    var rangeInfoBySeries = [];
    var hasBrushExists = false;
    if (!brushIndex) {
      throttleType = brushOption.throttleType;
      throttleDelay = brushOption.throttleDelay;
    }
    var areas = map2(brushModel.areas, function(area) {
      var builder = boundingRectBuilders[area.brushType];
      var selectableArea = defaults({
        boundingRect: builder ? builder(area) : void 0
      }, area);
      selectableArea.selectors = makeBrushCommonSelectorForSeries(selectableArea);
      return selectableArea;
    });
    var visualMappings = createVisualMappings(brushModel.option, STATE_LIST, function(mappingOption) {
      mappingOption.mappingMethod = "fixed";
    });
    isArray(brushLink) && each(brushLink, function(seriesIndex) {
      linkedSeriesMap[seriesIndex] = 1;
    });
    function linkOthers(seriesIndex) {
      return brushLink === "all" || !!linkedSeriesMap[seriesIndex];
    }
    function brushed(rangeInfoList) {
      return !!rangeInfoList.length;
    }
    ecModel.eachSeries(function(seriesModel, seriesIndex) {
      var rangeInfoList = rangeInfoBySeries[seriesIndex] = [];
      seriesModel.subType === "parallel" ? stepAParallel(seriesModel, seriesIndex) : stepAOthers(seriesModel, seriesIndex, rangeInfoList);
    });
    function stepAParallel(seriesModel, seriesIndex) {
      var coordSys = seriesModel.coordinateSystem;
      hasBrushExists = hasBrushExists || coordSys.hasAxisBrushed();
      linkOthers(seriesIndex) && coordSys.eachActiveState(seriesModel.getData(), function(activeState, dataIndex) {
        activeState === "active" && (selectedDataIndexForLink[dataIndex] = 1);
      });
    }
    function stepAOthers(seriesModel, seriesIndex, rangeInfoList) {
      if (!seriesModel.brushSelector || brushModelNotControll(brushModel, seriesIndex)) {
        return;
      }
      each(areas, function(area) {
        if (brushModel.brushTargetManager.controlSeries(area, seriesModel, ecModel)) {
          rangeInfoList.push(area);
        }
        hasBrushExists = hasBrushExists || brushed(rangeInfoList);
      });
      if (linkOthers(seriesIndex) && brushed(rangeInfoList)) {
        var data_1 = seriesModel.getData();
        data_1.each(function(dataIndex) {
          if (checkInRange(seriesModel, rangeInfoList, data_1, dataIndex)) {
            selectedDataIndexForLink[dataIndex] = 1;
          }
        });
      }
    }
    ecModel.eachSeries(function(seriesModel, seriesIndex) {
      var seriesBrushSelected = {
        seriesId: seriesModel.id,
        seriesIndex,
        seriesName: seriesModel.name,
        dataIndex: []
      };
      thisBrushSelected.selected.push(seriesBrushSelected);
      var rangeInfoList = rangeInfoBySeries[seriesIndex];
      var data = seriesModel.getData();
      var getValueState = linkOthers(seriesIndex) ? function(dataIndex) {
        return selectedDataIndexForLink[dataIndex] ? (seriesBrushSelected.dataIndex.push(data.getRawIndex(dataIndex)), "inBrush") : "outOfBrush";
      } : function(dataIndex) {
        return checkInRange(seriesModel, rangeInfoList, data, dataIndex) ? (seriesBrushSelected.dataIndex.push(data.getRawIndex(dataIndex)), "inBrush") : "outOfBrush";
      };
      (linkOthers(seriesIndex) ? hasBrushExists : brushed(rangeInfoList)) && applyVisual(STATE_LIST, visualMappings, data, getValueState);
    });
  });
  dispatchAction(api, throttleType, throttleDelay, brushSelected, payload);
}
function dispatchAction(api, throttleType, throttleDelay, brushSelected, payload) {
  if (!payload) {
    return;
  }
  var zr = api.getZr();
  if (zr[DISPATCH_FLAG]) {
    return;
  }
  if (!zr[DISPATCH_METHOD]) {
    zr[DISPATCH_METHOD] = doDispatch;
  }
  var fn = createOrUpdate(zr, DISPATCH_METHOD, throttleDelay, throttleType);
  fn(api, brushSelected);
}
function doDispatch(api, brushSelected) {
  if (!api.isDisposed()) {
    var zr = api.getZr();
    zr[DISPATCH_FLAG] = true;
    api.dispatchAction({
      type: "brushSelect",
      batch: brushSelected
    });
    zr[DISPATCH_FLAG] = false;
  }
}
function checkInRange(seriesModel, rangeInfoList, data, dataIndex) {
  for (var i2 = 0, len2 = rangeInfoList.length; i2 < len2; i2++) {
    var area = rangeInfoList[i2];
    if (seriesModel.brushSelector(dataIndex, data, area.selectors, area)) {
      return true;
    }
  }
}
function brushModelNotControll(brushModel, seriesIndex) {
  var seriesIndices = brushModel.option.seriesIndex;
  return seriesIndices != null && seriesIndices !== "all" && (isArray(seriesIndices) ? indexOf(seriesIndices, seriesIndex) < 0 : seriesIndex !== seriesIndices);
}
var boundingRectBuilders = {
  rect: function(area) {
    return getBoundingRectFromMinMax(area.range);
  },
  polygon: function(area) {
    var minMax;
    var range = area.range;
    for (var i2 = 0, len2 = range.length; i2 < len2; i2++) {
      minMax = minMax || [initExtentForUnion(), initExtentForUnion()];
      var rg = range[i2];
      rg[0] < minMax[0][0] && (minMax[0][0] = rg[0]);
      rg[0] > minMax[0][1] && (minMax[0][1] = rg[0]);
      rg[1] < minMax[1][0] && (minMax[1][0] = rg[1]);
      rg[1] > minMax[1][1] && (minMax[1][1] = rg[1]);
    }
    return minMax && getBoundingRectFromMinMax(minMax);
  }
};
function getBoundingRectFromMinMax(minMax) {
  return new BoundingRect_default(minMax[0][0], minMax[1][0], minMax[0][1] - minMax[0][0], minMax[1][1] - minMax[1][0]);
}

// node_modules/echarts/lib/component/brush/BrushView.js
var BrushView = (
  /** @class */
  (function(_super) {
    __extends(BrushView2, _super);
    function BrushView2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = BrushView2.type;
      return _this;
    }
    BrushView2.prototype.init = function(ecModel, api) {
      this.ecModel = ecModel;
      this.api = api;
      this.model;
      (this._brushController = new BrushController_default(api.getZr())).on("brush", bind(this._onBrush, this)).mount();
    };
    BrushView2.prototype.render = function(brushModel, ecModel, api, payload) {
      this.model = brushModel;
      this._updateController(brushModel, ecModel, api, payload);
    };
    BrushView2.prototype.updateTransform = function(brushModel, ecModel, api, payload) {
      layoutCovers(ecModel);
      this._updateController(brushModel, ecModel, api, payload);
    };
    BrushView2.prototype.updateVisual = function(brushModel, ecModel, api, payload) {
      this.updateTransform(brushModel, ecModel, api, payload);
    };
    BrushView2.prototype.updateView = function(brushModel, ecModel, api, payload) {
      this._updateController(brushModel, ecModel, api, payload);
    };
    BrushView2.prototype._updateController = function(brushModel, ecModel, api, payload) {
      (!payload || payload.$from !== brushModel.id) && this._brushController.setPanels(brushModel.brushTargetManager.makePanelOpts(api)).enableBrush(brushModel.brushOption).updateCovers(brushModel.areas.slice());
    };
    BrushView2.prototype.dispose = function() {
      this._brushController.dispose();
    };
    BrushView2.prototype._onBrush = function(eventParam) {
      var modelId = this.model.id;
      var areas = this.model.brushTargetManager.setOutputRanges(eventParam.areas, this.ecModel);
      (!eventParam.isEnd || eventParam.removeOnClick) && this.api.dispatchAction({
        type: "brush",
        brushId: modelId,
        areas: clone(areas),
        $from: modelId
      });
      eventParam.isEnd && this.api.dispatchAction({
        type: "brushEnd",
        brushId: modelId,
        areas: clone(areas),
        $from: modelId
      });
    };
    BrushView2.type = "brush";
    return BrushView2;
  })(Component_default2)
);
var BrushView_default = BrushView;

// node_modules/echarts/lib/component/brush/BrushModel.js
var BrushModel = (
  /** @class */
  (function(_super) {
    __extends(BrushModel2, _super);
    function BrushModel2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = BrushModel2.type;
      _this.areas = [];
      _this.brushOption = {};
      return _this;
    }
    BrushModel2.prototype.optionUpdated = function(newOption, isInit) {
      var thisOption = this.option;
      !isInit && replaceVisualOption(thisOption, newOption, ["inBrush", "outOfBrush"]);
      var inBrush = thisOption.inBrush = thisOption.inBrush || {};
      thisOption.outOfBrush = thisOption.outOfBrush || {
        color: this.option.defaultOutOfBrushColor
      };
      if (!inBrush.hasOwnProperty("liftZ")) {
        inBrush.liftZ = 5;
      }
    };
    BrushModel2.prototype.setAreas = function(areas) {
      if (true) {
        assert(isArray(areas));
        each(areas, function(area) {
          assert(area.brushType, "Illegal areas");
        });
      }
      if (!areas) {
        return;
      }
      this.areas = map2(areas, function(area) {
        return generateBrushOption(this.option, area);
      }, this);
    };
    BrushModel2.prototype.setBrushOption = function(brushOption) {
      this.brushOption = generateBrushOption(this.option, brushOption);
      this.brushType = this.brushOption.brushType;
    };
    BrushModel2.type = "brush";
    BrushModel2.dependencies = ["geo", "grid", "xAxis", "yAxis", "parallel", "series"];
    BrushModel2.defaultOption = {
      seriesIndex: "all",
      brushType: "rect",
      brushMode: "single",
      transformable: true,
      brushStyle: {
        borderWidth: 1,
        color: tokens_default.color.backgroundTint,
        borderColor: tokens_default.color.borderTint
      },
      throttleType: "fixRate",
      throttleDelay: 0,
      removeOnClick: true,
      z: 1e4,
      defaultOutOfBrushColor: tokens_default.color.disabled
    };
    return BrushModel2;
  })(Component_default)
);
function generateBrushOption(option, brushOption) {
  return merge2({
    brushType: option.brushType,
    brushMode: option.brushMode,
    transformable: option.transformable,
    brushStyle: new Model_default(option.brushStyle).getItemStyle(),
    removeOnClick: option.removeOnClick,
    z: option.z
  }, brushOption, true);
}
var BrushModel_default = BrushModel;

// node_modules/echarts/lib/component/toolbox/feature/Brush.js
var ICON_TYPES = ["rect", "polygon", "lineX", "lineY", "keep", "clear"];
var BrushFeature = (
  /** @class */
  (function(_super) {
    __extends(BrushFeature2, _super);
    function BrushFeature2() {
      return _super !== null && _super.apply(this, arguments) || this;
    }
    BrushFeature2.prototype.render = function(featureModel, ecModel, api) {
      var brushType;
      var brushMode;
      var isBrushed;
      ecModel.eachComponent({
        mainType: "brush"
      }, function(brushModel) {
        brushType = brushModel.brushType;
        brushMode = brushModel.brushOption.brushMode || "single";
        isBrushed = isBrushed || !!brushModel.areas.length;
      });
      this._brushType = brushType;
      this._brushMode = brushMode;
      each(featureModel.get("type", true), function(type) {
        featureModel.setIconStatus(type, (type === "keep" ? brushMode === "multiple" : type === "clear" ? isBrushed : type === brushType) ? "emphasis" : "normal");
      });
    };
    BrushFeature2.prototype.updateView = function(featureModel, ecModel, api) {
      this.render(featureModel, ecModel, api);
    };
    BrushFeature2.prototype.getIcons = function() {
      var model2 = this.model;
      var availableIcons = model2.get("icon", true);
      var icons = {};
      each(model2.get("type", true), function(type) {
        if (availableIcons[type]) {
          icons[type] = availableIcons[type];
        }
      });
      return icons;
    };
    ;
    BrushFeature2.prototype.onclick = function(ecModel, api, type) {
      var brushType = this._brushType;
      var brushMode = this._brushMode;
      if (type === "clear") {
        api.dispatchAction({
          type: "axisAreaSelect",
          intervals: []
        });
        api.dispatchAction({
          type: "brush",
          command: "clear",
          // Clear all areas of all brush components.
          areas: []
        });
      } else {
        api.dispatchAction({
          type: "takeGlobalCursor",
          key: "brush",
          brushOption: {
            brushType: type === "keep" ? brushType : brushType === type ? false : type,
            brushMode: type === "keep" ? brushMode === "multiple" ? "single" : "multiple" : brushMode
          }
        });
      }
    };
    ;
    BrushFeature2.getDefaultOption = function(ecModel) {
      var defaultOption3 = {
        show: true,
        type: ICON_TYPES.slice(),
        icon: {
          /* eslint-disable */
          rect: "M7.3,34.7 M0.4,10V-0.2h9.8 M89.6,10V-0.2h-9.8 M0.4,60v10.2h9.8 M89.6,60v10.2h-9.8 M12.3,22.4V10.5h13.1 M33.6,10.5h7.8 M49.1,10.5h7.8 M77.5,22.4V10.5h-13 M12.3,31.1v8.2 M77.7,31.1v8.2 M12.3,47.6v11.9h13.1 M33.6,59.5h7.6 M49.1,59.5 h7.7 M77.5,47.6v11.9h-13",
          polygon: "M55.2,34.9c1.7,0,3.1,1.4,3.1,3.1s-1.4,3.1-3.1,3.1 s-3.1-1.4-3.1-3.1S53.5,34.9,55.2,34.9z M50.4,51c1.7,0,3.1,1.4,3.1,3.1c0,1.7-1.4,3.1-3.1,3.1c-1.7,0-3.1-1.4-3.1-3.1 C47.3,52.4,48.7,51,50.4,51z M55.6,37.1l1.5-7.8 M60.1,13.5l1.6-8.7l-7.8,4 M59,19l-1,5.3 M24,16.1l6.4,4.9l6.4-3.3 M48.5,11.6 l-5.9,3.1 M19.1,12.8L9.7,5.1l1.1,7.7 M13.4,29.8l1,7.3l6.6,1.6 M11.6,18.4l1,6.1 M32.8,41.9 M26.6,40.4 M27.3,40.2l6.1,1.6 M49.9,52.1l-5.6-7.6l-4.9-1.2",
          lineX: "M15.2,30 M19.7,15.6V1.9H29 M34.8,1.9H40.4 M55.3,15.6V1.9H45.9 M19.7,44.4V58.1H29 M34.8,58.1H40.4 M55.3,44.4 V58.1H45.9 M12.5,20.3l-9.4,9.6l9.6,9.8 M3.1,29.9h16.5 M62.5,20.3l9.4,9.6L62.3,39.7 M71.9,29.9H55.4",
          lineY: "M38.8,7.7 M52.7,12h13.2v9 M65.9,26.6V32 M52.7,46.3h13.2v-9 M24.9,12H11.8v9 M11.8,26.6V32 M24.9,46.3H11.8v-9 M48.2,5.1l-9.3-9l-9.4,9.2 M38.9-3.9V12 M48.2,53.3l-9.3,9l-9.4-9.2 M38.9,62.3V46.4",
          keep: "M4,10.5V1h10.3 M20.7,1h6.1 M33,1h6.1 M55.4,10.5V1H45.2 M4,17.3v6.6 M55.6,17.3v6.6 M4,30.5V40h10.3 M20.7,40 h6.1 M33,40h6.1 M55.4,30.5V40H45.2 M21,18.9h62.9v48.6H21V18.9z",
          clear: "M22,14.7l30.9,31 M52.9,14.7L22,45.7 M4.7,16.8V4.2h13.1 M26,4.2h7.8 M41.6,4.2h7.8 M70.3,16.8V4.2H57.2 M4.7,25.9v8.6 M70.3,25.9v8.6 M4.7,43.2v12.6h13.1 M26,55.8h7.8 M41.6,55.8h7.8 M70.3,43.2v12.6H57.2"
          // jshint ignore:line
          /* eslint-enable */
        },
        // `rect`, `polygon`, `lineX`, `lineY`, `keep`, `clear`
        title: ecModel.getLocaleModel().get(["toolbox", "brush", "title"])
      };
      return defaultOption3;
    };
    return BrushFeature2;
  })(ToolboxFeature)
);
var Brush_default = BrushFeature;

// node_modules/echarts/lib/component/brush/install.js
function install34(registers) {
  registers.registerComponentView(BrushView_default);
  registers.registerComponentModel(BrushModel_default);
  registers.registerPreprocessor(brushPreprocessor);
  registers.registerVisual(registers.PRIORITY.VISUAL.BRUSH, brushVisualStageHandler);
  registers.registerAction({
    type: "brush",
    event: "brush",
    update: "updateVisual"
  }, function(payload, ecModel) {
    ecModel.eachComponent({
      mainType: "brush",
      query: payload
    }, function(brushModel) {
      brushModel.setAreas(payload.areas);
    });
  });
  registers.registerAction({
    type: "brushSelect",
    event: "brushSelected",
    update: "none"
  }, noop);
  registers.registerAction({
    type: "brushEnd",
    event: "brushEnd",
    update: "none"
  }, noop);
  registerFeature("brush", Brush_default);
}

// node_modules/echarts/lib/component/title/install.js
var TitleModel = (
  /** @class */
  (function(_super) {
    __extends(TitleModel2, _super);
    function TitleModel2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = TitleModel2.type;
      _this.layoutMode = {
        type: "box",
        ignoreSize: true
      };
      return _this;
    }
    TitleModel2.type = "title";
    TitleModel2.defaultOption = {
      // zlevel: 0,
      z: 6,
      show: true,
      text: "",
      target: "blank",
      subtext: "",
      subtarget: "blank",
      left: "center",
      top: tokens_default.size.m,
      backgroundColor: tokens_default.color.transparent,
      borderColor: tokens_default.color.primary,
      borderWidth: 0,
      padding: 5,
      itemGap: 10,
      textStyle: {
        fontSize: 18,
        fontWeight: "bold",
        color: tokens_default.color.primary
      },
      subtextStyle: {
        fontSize: 12,
        color: tokens_default.color.quaternary
      }
    };
    return TitleModel2;
  })(Component_default)
);
var TitleView = (
  /** @class */
  (function(_super) {
    __extends(TitleView2, _super);
    function TitleView2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = TitleView2.type;
      return _this;
    }
    TitleView2.prototype.render = function(titleModel, ecModel, api) {
      this.group.removeAll();
      if (!titleModel.get("show")) {
        return;
      }
      var group2 = this.group;
      var textStyleModel = titleModel.getModel("textStyle");
      var subtextStyleModel = titleModel.getModel("subtextStyle");
      var textAlign = titleModel.get("textAlign");
      var textVerticalAlign = retrieve2(titleModel.get("textBaseline"), titleModel.get("textVerticalAlign"));
      var textEl = new Text_default({
        style: createTextStyle(textStyleModel, {
          text: titleModel.get("text"),
          fill: textStyleModel.getTextColor()
        }, {
          disableBox: true
        }),
        z2: 10
      });
      var textRect = textEl.getBoundingRect();
      var subText = titleModel.get("subtext");
      var subTextEl = new Text_default({
        style: createTextStyle(subtextStyleModel, {
          text: subText,
          fill: subtextStyleModel.getTextColor(),
          y: textRect.height + titleModel.get("itemGap"),
          verticalAlign: "top"
        }, {
          disableBox: true
        }),
        z2: 10
      });
      var link = titleModel.get("link");
      var sublink = titleModel.get("sublink");
      var triggerEvent = titleModel.get("triggerEvent", true);
      textEl.silent = !link && !triggerEvent;
      subTextEl.silent = !sublink && !triggerEvent;
      if (link) {
        textEl.on("click", function() {
          windowOpen(link, "_" + titleModel.get("target"));
        });
      }
      if (sublink) {
        subTextEl.on("click", function() {
          windowOpen(sublink, "_" + titleModel.get("subtarget"));
        });
      }
      getECData(textEl).eventData = getECData(subTextEl).eventData = triggerEvent ? {
        componentType: "title",
        componentIndex: titleModel.componentIndex
      } : null;
      group2.add(textEl);
      subText && group2.add(subTextEl);
      var groupRect = group2.getBoundingRect();
      var layoutOption = titleModel.getBoxLayoutParams();
      layoutOption.width = groupRect.width;
      layoutOption.height = groupRect.height;
      var layoutRef = createBoxLayoutReference(titleModel, api);
      var layoutRect = getLayoutRect(layoutOption, layoutRef.refContainer, titleModel.get("padding"));
      if (!textAlign) {
        textAlign = titleModel.get("left") || titleModel.get("right");
        if (textAlign === "middle") {
          textAlign = "center";
        }
        if (textAlign === "right") {
          layoutRect.x += layoutRect.width;
        } else if (textAlign === "center") {
          layoutRect.x += layoutRect.width / 2;
        }
      }
      if (!textVerticalAlign) {
        textVerticalAlign = titleModel.get("top") || titleModel.get("bottom");
        if (textVerticalAlign === "center") {
          textVerticalAlign = "middle";
        }
        if (textVerticalAlign === "bottom") {
          layoutRect.y += layoutRect.height;
        } else if (textVerticalAlign === "middle") {
          layoutRect.y += layoutRect.height / 2;
        }
        textVerticalAlign = textVerticalAlign || "top";
      }
      group2.x = layoutRect.x;
      group2.y = layoutRect.y;
      group2.markRedraw();
      var alignStyle = {
        align: textAlign,
        verticalAlign: textVerticalAlign
      };
      textEl.setStyle(alignStyle);
      subTextEl.setStyle(alignStyle);
      groupRect = group2.getBoundingRect();
      var padding = layoutRect.margin;
      var style2 = titleModel.getItemStyle(["color", "opacity"]);
      style2.fill = titleModel.get("backgroundColor");
      var rect = new Rect_default({
        shape: {
          x: groupRect.x - padding[3],
          y: groupRect.y - padding[0],
          width: groupRect.width + padding[1] + padding[3],
          height: groupRect.height + padding[0] + padding[2],
          r: titleModel.get("borderRadius")
        },
        style: style2,
        subPixelOptimize: true,
        silent: true
      });
      group2.add(rect);
    };
    TitleView2.type = "title";
    return TitleView2;
  })(Component_default2)
);

// node_modules/echarts/lib/component/timeline/TimelineModel.js
var TimelineModel = (
  /** @class */
  (function(_super) {
    __extends(TimelineModel2, _super);
    function TimelineModel2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = TimelineModel2.type;
      _this.layoutMode = "box";
      return _this;
    }
    TimelineModel2.prototype.init = function(option, parentModel, ecModel) {
      this.mergeDefaultAndTheme(option, ecModel);
      this._initData();
    };
    TimelineModel2.prototype.mergeOption = function(option) {
      _super.prototype.mergeOption.apply(this, arguments);
      this._initData();
    };
    TimelineModel2.prototype.setCurrentIndex = function(currentIndex) {
      if (currentIndex == null) {
        currentIndex = this.option.currentIndex;
      }
      var count2 = this._data.count();
      if (this.option.loop) {
        currentIndex = (currentIndex % count2 + count2) % count2;
      } else {
        currentIndex >= count2 && (currentIndex = count2 - 1);
        currentIndex < 0 && (currentIndex = 0);
      }
      this.option.currentIndex = currentIndex;
    };
    TimelineModel2.prototype.getCurrentIndex = function() {
      return this.option.currentIndex;
    };
    TimelineModel2.prototype.isIndexMax = function() {
      return this.getCurrentIndex() >= this._data.count() - 1;
    };
    TimelineModel2.prototype.setPlayState = function(state2) {
      this.option.autoPlay = !!state2;
    };
    TimelineModel2.prototype.getPlayState = function() {
      return !!this.option.autoPlay;
    };
    TimelineModel2.prototype._initData = function() {
      var thisOption = this.option;
      var dataArr = thisOption.data || [];
      var axisType = thisOption.axisType;
      var names = this._names = [];
      var processedDataArr;
      if (axisType === "category") {
        processedDataArr = [];
        each(dataArr, function(item, index) {
          var value = convertOptionIdName(getDataItemValue(item), "");
          var newItem;
          if (isObject(item)) {
            newItem = clone(item);
            newItem.value = index;
          } else {
            newItem = index;
          }
          processedDataArr.push(newItem);
          names.push(value);
        });
      } else {
        processedDataArr = dataArr;
      }
      var dimType = {
        category: "ordinal",
        time: "time",
        value: "number"
      }[axisType] || "number";
      var data = this._data = new SeriesData_default([{
        name: "value",
        type: dimType
      }], this);
      data.initData(processedDataArr, names);
    };
    TimelineModel2.prototype.getData = function() {
      return this._data;
    };
    TimelineModel2.prototype.getCategories = function() {
      if (this.get("axisType") === "category") {
        return this._names.slice();
      }
    };
    TimelineModel2.type = "timeline";
    TimelineModel2.defaultOption = {
      // zlevel: 0,                  // 一级层叠
      z: 4,
      show: true,
      axisType: "time",
      realtime: true,
      left: "20%",
      top: null,
      right: "20%",
      bottom: 0,
      width: null,
      height: 40,
      padding: tokens_default.size.m,
      controlPosition: "left",
      autoPlay: false,
      rewind: false,
      loop: true,
      playInterval: 2e3,
      currentIndex: 0,
      itemStyle: {},
      label: {
        color: tokens_default.color.secondary
      },
      data: []
    };
    return TimelineModel2;
  })(Component_default)
);
var TimelineModel_default = TimelineModel;

// node_modules/echarts/lib/component/timeline/SliderTimelineModel.js
var SliderTimelineModel = (
  /** @class */
  (function(_super) {
    __extends(SliderTimelineModel2, _super);
    function SliderTimelineModel2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = SliderTimelineModel2.type;
      return _this;
    }
    SliderTimelineModel2.type = "timeline.slider";
    SliderTimelineModel2.defaultOption = inheritDefaultOption(TimelineModel_default.defaultOption, {
      backgroundColor: "rgba(0,0,0,0)",
      borderColor: tokens_default.color.border,
      borderWidth: 0,
      orient: "horizontal",
      inverse: false,
      tooltip: {
        trigger: "item"
        // data item may also have tootip attr.
      },
      symbol: "circle",
      symbolSize: 12,
      lineStyle: {
        show: true,
        width: 2,
        color: tokens_default.color.accent10
      },
      label: {
        position: "auto",
        // When using number, label position is not
        // restricted by viewRect.
        // positive: right/bottom, negative: left/top
        show: true,
        interval: "auto",
        rotate: 0,
        // formatter: null,
        // 其余属性默认使用全局文本样式，详见TEXTSTYLE
        color: tokens_default.color.tertiary
      },
      itemStyle: {
        color: tokens_default.color.accent20,
        borderWidth: 0
      },
      checkpointStyle: {
        symbol: "circle",
        symbolSize: 15,
        color: tokens_default.color.accent50,
        borderColor: tokens_default.color.accent50,
        borderWidth: 0,
        shadowBlur: 0,
        shadowOffsetX: 0,
        shadowOffsetY: 0,
        shadowColor: "rgba(0, 0, 0, 0)",
        // borderColor: 'rgba(194,53,49, 0.5)',
        animation: true,
        animationDuration: 300,
        animationEasing: "quinticInOut"
      },
      controlStyle: {
        show: true,
        showPlayBtn: true,
        showPrevBtn: true,
        showNextBtn: true,
        itemSize: 24,
        itemGap: 12,
        position: "left",
        playIcon: "path://M15 0C23.2843 0 30 6.71573 30 15C30 23.2843 23.2843 30 15 30C6.71573 30 0 23.2843 0 15C0 6.71573 6.71573 0 15 0ZM15 3C8.37258 3 3 8.37258 3 15C3 21.6274 8.37258 27 15 27C21.6274 27 27 21.6274 27 15C27 8.37258 21.6274 3 15 3ZM11.5 10.6699C11.5 9.90014 12.3333 9.41887 13 9.80371L20.5 14.1338C21.1667 14.5187 21.1667 15.4813 20.5 15.8662L13 20.1963C12.3333 20.5811 11.5 20.0999 11.5 19.3301V10.6699Z",
        stopIcon: "path://M15 0C23.2843 0 30 6.71573 30 15C30 23.2843 23.2843 30 15 30C6.71573 30 0 23.2843 0 15C0 6.71573 6.71573 0 15 0ZM15 3C8.37258 3 3 8.37258 3 15C3 21.6274 8.37258 27 15 27C21.6274 27 27 21.6274 27 15C27 8.37258 21.6274 3 15 3ZM11.5 10C12.3284 10 13 10.6716 13 11.5V18.5C13 19.3284 12.3284 20 11.5 20C10.6716 20 10 19.3284 10 18.5V11.5C10 10.6716 10.6716 10 11.5 10ZM18.5 10C19.3284 10 20 10.6716 20 11.5V18.5C20 19.3284 19.3284 20 18.5 20C17.6716 20 17 19.3284 17 18.5V11.5C17 10.6716 17.6716 10 18.5 10Z",
        // eslint-disable-next-line max-len
        nextIcon: "path://M0.838834 18.7383C0.253048 18.1525 0.253048 17.2028 0.838834 16.617L7.55635 9.89949L0.838834 3.18198C0.253048 2.59619 0.253048 1.64645 0.838834 1.06066C1.42462 0.474874 2.37437 0.474874 2.96015 1.06066L10.7383 8.83883L10.8412 8.95277C11.2897 9.50267 11.2897 10.2963 10.8412 10.8462L10.7383 10.9602L2.96015 18.7383C2.37437 19.3241 1.42462 19.3241 0.838834 18.7383Z",
        // eslint-disable-next-line max-len
        prevIcon: "path://M10.9602 1.06066C11.5459 1.64645 11.5459 2.59619 10.9602 3.18198L4.24264 9.89949L10.9602 16.617C11.5459 17.2028 11.5459 18.1525 10.9602 18.7383C10.3744 19.3241 9.42462 19.3241 8.83883 18.7383L1.06066 10.9602L0.957771 10.8462C0.509245 10.2963 0.509245 9.50267 0.957771 8.95277L1.06066 8.83883L8.83883 1.06066C9.42462 0.474874 10.3744 0.474874 10.9602 1.06066Z",
        prevBtnSize: 18,
        nextBtnSize: 18,
        color: tokens_default.color.accent50,
        borderColor: tokens_default.color.accent50,
        borderWidth: 0
      },
      emphasis: {
        label: {
          show: true,
          // 其余属性默认使用全局文本样式，详见TEXTSTYLE
          color: tokens_default.color.accent60
        },
        itemStyle: {
          color: tokens_default.color.accent60,
          borderColor: tokens_default.color.accent60
        },
        controlStyle: {
          color: tokens_default.color.accent70,
          borderColor: tokens_default.color.accent70
        }
      },
      progress: {
        lineStyle: {
          color: tokens_default.color.accent30
        },
        itemStyle: {
          color: tokens_default.color.accent40
        }
      },
      data: []
    });
    return SliderTimelineModel2;
  })(TimelineModel_default)
);
mixin(SliderTimelineModel, DataFormatMixin.prototype);

// node_modules/echarts/lib/component/timeline/TimelineView.js
var TimelineView = (
  /** @class */
  (function(_super) {
    __extends(TimelineView2, _super);
    function TimelineView2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = TimelineView2.type;
      return _this;
    }
    TimelineView2.type = "timeline";
    return TimelineView2;
  })(Component_default2)
);
var TimelineView_default = TimelineView;

// node_modules/echarts/lib/component/timeline/TimelineAxis.js
var TimelineAxis = (
  /** @class */
  (function(_super) {
    __extends(TimelineAxis2, _super);
    function TimelineAxis2(dim, scale3, coordExtent, axisType) {
      var _this = _super.call(this, dim, scale3, coordExtent) || this;
      _this.type = axisType || "value";
      return _this;
    }
    TimelineAxis2.prototype.getLabelModel = function() {
      return this.model.getModel("label");
    };
    TimelineAxis2.prototype.isHorizontal = function() {
      return this.model.get("orient") === "horizontal";
    };
    return TimelineAxis2;
  })(Axis_default)
);
var TimelineAxis_default = TimelineAxis;

// node_modules/echarts/lib/component/timeline/SliderTimelineView.js
var PI8 = Math.PI;
var labelDataIndexStore = makeInner();
var SliderTimelineView = (
  /** @class */
  (function(_super) {
    __extends(SliderTimelineView2, _super);
    function SliderTimelineView2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = SliderTimelineView2.type;
      return _this;
    }
    SliderTimelineView2.prototype.init = function(ecModel, api) {
      this.api = api;
    };
    SliderTimelineView2.prototype.render = function(timelineModel, ecModel, api) {
      this.model = timelineModel;
      this.api = api;
      this.ecModel = ecModel;
      this.group.removeAll();
      if (timelineModel.get("show", true)) {
        var layoutInfo_1 = this._layout(timelineModel, api);
        var mainGroup_1 = this._createGroup("_mainGroup");
        var labelGroup = this._createGroup("_labelGroup");
        var axis_1 = this._axis = this._createAxis(layoutInfo_1, timelineModel);
        timelineModel.formatTooltip = function(dataIndex) {
          var name = axis_1.scale.getLabel({
            value: dataIndex
          });
          return createTooltipMarkup("nameValue", {
            noName: true,
            value: name
          });
        };
        each(["AxisLine", "AxisTick", "Control", "CurrentPointer"], function(name) {
          this["_render" + name](layoutInfo_1, mainGroup_1, axis_1, timelineModel);
        }, this);
        this._renderAxisLabel(layoutInfo_1, labelGroup, axis_1, timelineModel);
        this._position(layoutInfo_1, timelineModel);
      }
      this._doPlayStop();
      this._updateTicksStatus();
    };
    SliderTimelineView2.prototype.remove = function() {
      this._clearTimer();
      this.group.removeAll();
    };
    SliderTimelineView2.prototype.dispose = function() {
      this._clearTimer();
    };
    SliderTimelineView2.prototype._layout = function(timelineModel, api) {
      var labelPosOpt = timelineModel.get(["label", "position"]);
      var orient = timelineModel.get("orient");
      var viewRect2 = getViewRect(timelineModel, api);
      var parsedLabelPos;
      if (labelPosOpt == null || labelPosOpt === "auto") {
        parsedLabelPos = orient === "horizontal" ? viewRect2.y + viewRect2.height / 2 < api.getHeight() / 2 ? "-" : "+" : viewRect2.x + viewRect2.width / 2 < api.getWidth() / 2 ? "+" : "-";
      } else if (isString(labelPosOpt)) {
        parsedLabelPos = {
          horizontal: {
            top: "-",
            bottom: "+"
          },
          vertical: {
            left: "-",
            right: "+"
          }
        }[orient][labelPosOpt];
      } else {
        parsedLabelPos = labelPosOpt;
      }
      var labelAlignMap = {
        horizontal: "center",
        vertical: parsedLabelPos >= 0 || parsedLabelPos === "+" ? "left" : "right"
      };
      var labelBaselineMap = {
        horizontal: parsedLabelPos >= 0 || parsedLabelPos === "+" ? "top" : "bottom",
        vertical: "middle"
      };
      var rotationMap = {
        horizontal: 0,
        vertical: PI8 / 2
      };
      var mainLength = orient === "vertical" ? viewRect2.height : viewRect2.width;
      var controlModel = timelineModel.getModel("controlStyle");
      var showControl = controlModel.get("show", true);
      var controlSize = showControl ? controlModel.get("itemSize") : 0;
      var controlGap = showControl ? controlModel.get("itemGap") : 0;
      var sizePlusGap = controlSize + controlGap;
      var labelRotation = timelineModel.get(["label", "rotate"]) || 0;
      labelRotation = labelRotation * PI8 / 180;
      var playPosition;
      var prevBtnPosition;
      var nextBtnPosition;
      var controlPosition = controlModel.get("position", true);
      var showPlayBtn = showControl && controlModel.get("showPlayBtn", true);
      var showPrevBtn = showControl && controlModel.get("showPrevBtn", true);
      var showNextBtn = showControl && controlModel.get("showNextBtn", true);
      var xLeft = 0;
      var xRight = mainLength;
      if (controlPosition === "left" || controlPosition === "bottom") {
        showPlayBtn && (playPosition = [0, 0], xLeft += sizePlusGap);
        showPrevBtn && (prevBtnPosition = [xLeft, 0], xLeft += sizePlusGap);
        showNextBtn && (nextBtnPosition = [xRight - controlSize, 0], xRight -= sizePlusGap);
      } else {
        showPlayBtn && (playPosition = [xRight - controlSize, 0], xRight -= sizePlusGap);
        showPrevBtn && (prevBtnPosition = [0, 0], xLeft += sizePlusGap);
        showNextBtn && (nextBtnPosition = [xRight - controlSize, 0], xRight -= sizePlusGap);
      }
      var axisExtent = [xLeft, xRight];
      if (timelineModel.get("inverse")) {
        axisExtent.reverse();
      }
      return {
        viewRect: viewRect2,
        mainLength,
        orient,
        rotation: rotationMap[orient],
        labelRotation,
        labelPosOpt: parsedLabelPos,
        labelAlign: timelineModel.get(["label", "align"]) || labelAlignMap[orient],
        labelBaseline: timelineModel.get(["label", "verticalAlign"]) || timelineModel.get(["label", "baseline"]) || labelBaselineMap[orient],
        // Based on mainGroup.
        playPosition,
        prevBtnPosition,
        nextBtnPosition,
        axisExtent,
        controlSize,
        controlGap
      };
    };
    SliderTimelineView2.prototype._position = function(layoutInfo, timelineModel) {
      var mainGroup = this._mainGroup;
      var labelGroup = this._labelGroup;
      var viewRect2 = layoutInfo.viewRect;
      if (layoutInfo.orient === "vertical") {
        var m2 = create();
        var rotateOriginX = viewRect2.x;
        var rotateOriginY = viewRect2.y + viewRect2.height;
        translate(m2, m2, [-rotateOriginX, -rotateOriginY]);
        rotate(m2, m2, -PI8 / 2);
        translate(m2, m2, [rotateOriginX, rotateOriginY]);
        viewRect2 = viewRect2.clone();
        viewRect2.applyTransform(m2);
      }
      var viewBound = getBound(viewRect2);
      var mainBound = getBound(mainGroup.getBoundingRect());
      var labelBound = getBound(labelGroup.getBoundingRect());
      var mainPosition = [mainGroup.x, mainGroup.y];
      var labelsPosition = [labelGroup.x, labelGroup.y];
      labelsPosition[0] = mainPosition[0] = viewBound[0][0];
      var labelPosOpt = layoutInfo.labelPosOpt;
      if (labelPosOpt == null || isString(labelPosOpt)) {
        var mainBoundIdx = labelPosOpt === "+" ? 0 : 1;
        toBound(mainPosition, mainBound, viewBound, 1, mainBoundIdx);
        toBound(labelsPosition, labelBound, viewBound, 1, 1 - mainBoundIdx);
      } else {
        var mainBoundIdx = labelPosOpt >= 0 ? 0 : 1;
        toBound(mainPosition, mainBound, viewBound, 1, mainBoundIdx);
        labelsPosition[1] = mainPosition[1] + labelPosOpt;
      }
      mainGroup.setPosition(mainPosition);
      labelGroup.setPosition(labelsPosition);
      mainGroup.rotation = labelGroup.rotation = layoutInfo.rotation;
      setOrigin(mainGroup);
      setOrigin(labelGroup);
      function setOrigin(targetGroup) {
        targetGroup.originX = viewBound[0][0] - targetGroup.x;
        targetGroup.originY = viewBound[1][0] - targetGroup.y;
      }
      function getBound(rect) {
        return [[rect.x, rect.x + rect.width], [rect.y, rect.y + rect.height]];
      }
      function toBound(fromPos, from, to, dimIdx, boundIdx) {
        fromPos[dimIdx] += to[dimIdx][boundIdx] - from[dimIdx][boundIdx];
      }
    };
    SliderTimelineView2.prototype._createAxis = function(layoutInfo, timelineModel) {
      var data = timelineModel.getData();
      var axisType = timelineModel.get("axisType") || timelineModel.get("type");
      if (axisType !== "category" && axisType !== "time") {
        axisType = "value";
      }
      var scale3 = createScaleByModel(timelineModel, axisType, false);
      scale3.getTicks = function() {
        return data.mapArray(["value"], function(value) {
          return {
            value
          };
        });
      };
      var dataExtent = data.getDataExtent("value");
      scale3.setExtent(dataExtent[0], dataExtent[1]);
      scaleCalcNiceDirectly(scale3, {
        fixMinMax: [true, true]
      });
      var axis = new TimelineAxis_default("value", scale3, layoutInfo.axisExtent, axisType);
      axis.model = timelineModel;
      return axis;
    };
    SliderTimelineView2.prototype._createGroup = function(key2) {
      var newGroup = this[key2] = new Group_default();
      this.group.add(newGroup);
      return newGroup;
    };
    SliderTimelineView2.prototype._renderAxisLine = function(layoutInfo, group2, axis, timelineModel) {
      var axisExtent = axis.getExtent();
      if (!timelineModel.get(["lineStyle", "show"])) {
        return;
      }
      var line = new Line_default({
        shape: {
          x1: axisExtent[0],
          y1: 0,
          x2: axisExtent[1],
          y2: 0
        },
        style: extend({
          lineCap: "round"
        }, timelineModel.getModel("lineStyle").getLineStyle()),
        silent: true,
        z2: 1
      });
      group2.add(line);
      var progressLine = this._progressLine = new Line_default({
        shape: {
          x1: axisExtent[0],
          x2: this._currentPointer ? this._currentPointer.x : axisExtent[0],
          y1: 0,
          y2: 0
        },
        style: defaults({
          lineCap: "round",
          lineWidth: line.style.lineWidth
        }, timelineModel.getModel(["progress", "lineStyle"]).getLineStyle()),
        silent: true,
        z2: 1
      });
      group2.add(progressLine);
    };
    SliderTimelineView2.prototype._renderAxisTick = function(layoutInfo, group2, axis, timelineModel) {
      var _this = this;
      var data = timelineModel.getData();
      var ticks = axis.scale.getTicks();
      this._tickSymbols = [];
      each(ticks, function(tick) {
        var tickCoord = axis.dataToCoord(tick.value);
        var itemModel = data.getItemModel(tick.value);
        var itemStyleModel = itemModel.getModel("itemStyle");
        var hoverStyleModel = itemModel.getModel(["emphasis", "itemStyle"]);
        var progressStyleModel = itemModel.getModel(["progress", "itemStyle"]);
        var symbolOpt = {
          x: tickCoord,
          y: 0,
          onclick: bind(_this._changeTimeline, _this, tick.value)
        };
        var el = giveSymbol(itemModel, itemStyleModel, group2, symbolOpt);
        el.ensureState("emphasis").style = hoverStyleModel.getItemStyle();
        el.ensureState("progress").style = progressStyleModel.getItemStyle();
        enableHoverEmphasis(el);
        var ecData = getECData(el);
        if (itemModel.get("tooltip")) {
          ecData.dataIndex = tick.value;
          ecData.dataModel = timelineModel;
        } else {
          ecData.dataIndex = ecData.dataModel = null;
        }
        _this._tickSymbols.push(el);
      });
    };
    SliderTimelineView2.prototype._renderAxisLabel = function(layoutInfo, group2, axis, timelineModel) {
      var _this = this;
      var labelModel = axis.getLabelModel();
      if (!labelModel.get("show")) {
        return;
      }
      var data = timelineModel.getData();
      var labels = axis.getViewLabels();
      this._tickLabels = [];
      each(labels, function(labelItem) {
        if (labelItem.tick.offInterval) {
          return;
        }
        var dataIndex = labelItem.tick.value;
        var itemModel = data.getItemModel(dataIndex);
        var normalLabelModel = itemModel.getModel("label");
        var hoverLabelModel = itemModel.getModel(["emphasis", "label"]);
        var progressLabelModel = itemModel.getModel(["progress", "label"]);
        var tickCoord = axis.dataToCoord(dataIndex);
        var textEl = new Text_default({
          x: tickCoord,
          y: 0,
          rotation: layoutInfo.labelRotation - layoutInfo.rotation,
          onclick: bind(_this._changeTimeline, _this, dataIndex),
          silent: false,
          style: createTextStyle(normalLabelModel, {
            text: labelItem.formattedLabel,
            align: layoutInfo.labelAlign,
            verticalAlign: layoutInfo.labelBaseline
          })
        });
        textEl.ensureState("emphasis").style = createTextStyle(hoverLabelModel);
        textEl.ensureState("progress").style = createTextStyle(progressLabelModel);
        group2.add(textEl);
        enableHoverEmphasis(textEl);
        labelDataIndexStore(textEl).dataIndex = dataIndex;
        _this._tickLabels.push(textEl);
      });
    };
    SliderTimelineView2.prototype._renderControl = function(layoutInfo, group2, axis, timelineModel) {
      var controlSize = layoutInfo.controlSize;
      var rotation = layoutInfo.rotation;
      var itemStyle = timelineModel.getModel("controlStyle").getItemStyle();
      var hoverStyle = timelineModel.getModel(["emphasis", "controlStyle"]).getItemStyle();
      var playState = timelineModel.getPlayState();
      var inverse = timelineModel.get("inverse", true);
      makeBtn(layoutInfo.nextBtnPosition, "next", bind(this._changeTimeline, this, inverse ? "-" : "+"));
      makeBtn(layoutInfo.prevBtnPosition, "prev", bind(this._changeTimeline, this, inverse ? "+" : "-"));
      makeBtn(layoutInfo.playPosition, playState ? "stop" : "play", bind(this._handlePlayClick, this, !playState), true);
      function makeBtn(position, iconName, onclick, willRotate) {
        if (!position) {
          return;
        }
        var iconSize = parsePercent(retrieve2(timelineModel.get(["controlStyle", iconName + "BtnSize"]), controlSize), controlSize);
        var rect = [0, -iconSize / 2, iconSize, iconSize];
        var btn = makeControlIcon(timelineModel, iconName + "Icon", rect, {
          x: position[0],
          y: position[1],
          originX: controlSize / 2,
          originY: 0,
          rotation: willRotate ? -rotation : 0,
          rectHover: true,
          style: itemStyle,
          onclick
        });
        btn.ensureState("emphasis").style = hoverStyle;
        group2.add(btn);
        enableHoverEmphasis(btn);
      }
    };
    SliderTimelineView2.prototype._renderCurrentPointer = function(layoutInfo, group2, axis, timelineModel) {
      var data = timelineModel.getData();
      var currentIndex = timelineModel.getCurrentIndex();
      var pointerModel = data.getItemModel(currentIndex).getModel("checkpointStyle");
      var me = this;
      var callback = {
        onCreate: function(pointer) {
          pointer.draggable = true;
          pointer.drift = bind(me._handlePointerDrag, me);
          pointer.ondragend = bind(me._handlePointerDragend, me);
          pointerMoveTo(pointer, me._progressLine, currentIndex, axis, timelineModel, true);
        },
        onUpdate: function(pointer) {
          pointerMoveTo(pointer, me._progressLine, currentIndex, axis, timelineModel);
        }
      };
      this._currentPointer = giveSymbol(pointerModel, pointerModel, this._mainGroup, {}, this._currentPointer, callback);
    };
    SliderTimelineView2.prototype._handlePlayClick = function(nextState) {
      this._clearTimer();
      this.api.dispatchAction({
        type: "timelinePlayChange",
        playState: nextState,
        from: this.uid
      });
    };
    SliderTimelineView2.prototype._handlePointerDrag = function(dx, dy, e2) {
      this._clearTimer();
      this._pointerChangeTimeline([e2.offsetX, e2.offsetY]);
    };
    SliderTimelineView2.prototype._handlePointerDragend = function(e2) {
      this._pointerChangeTimeline([e2.offsetX, e2.offsetY], true);
    };
    SliderTimelineView2.prototype._pointerChangeTimeline = function(mousePos, trigger4) {
      var toCoord = this._toAxisCoord(mousePos)[0];
      var axis = this._axis;
      var axisExtent = asc(axis.getExtent().slice());
      toCoord > axisExtent[1] && (toCoord = axisExtent[1]);
      toCoord < axisExtent[0] && (toCoord = axisExtent[0]);
      this._currentPointer.x = toCoord;
      this._currentPointer.markRedraw();
      var progressLine = this._progressLine;
      if (progressLine) {
        progressLine.shape.x2 = toCoord;
        progressLine.dirty();
      }
      var targetDataIndex = this._findNearestTick(toCoord);
      var timelineModel = this.model;
      if (trigger4 || targetDataIndex !== timelineModel.getCurrentIndex() && timelineModel.get("realtime")) {
        this._changeTimeline(targetDataIndex);
      }
    };
    SliderTimelineView2.prototype._doPlayStop = function() {
      var _this = this;
      this._clearTimer();
      if (this.model.getPlayState()) {
        this._timer = setTimeout(function() {
          var timelineModel = _this.model;
          _this._changeTimeline(timelineModel.getCurrentIndex() + (timelineModel.get("rewind", true) ? -1 : 1));
        }, this.model.get("playInterval"));
      }
    };
    SliderTimelineView2.prototype._toAxisCoord = function(vertex) {
      var trans = this._mainGroup.getLocalTransform();
      return applyTransform2(vertex, trans, true);
    };
    SliderTimelineView2.prototype._findNearestTick = function(axisCoord) {
      var data = this.model.getData();
      var dist3 = Infinity;
      var targetDataIndex;
      var axis = this._axis;
      data.each(["value"], function(value, dataIndex) {
        var coord = axis.dataToCoord(value);
        var d = Math.abs(coord - axisCoord);
        if (d < dist3) {
          dist3 = d;
          targetDataIndex = dataIndex;
        }
      });
      return targetDataIndex;
    };
    SliderTimelineView2.prototype._clearTimer = function() {
      if (this._timer) {
        clearTimeout(this._timer);
        this._timer = null;
      }
    };
    SliderTimelineView2.prototype._changeTimeline = function(nextIndex) {
      var currentIndex = this.model.getCurrentIndex();
      if (nextIndex === "+") {
        nextIndex = currentIndex + 1;
      } else if (nextIndex === "-") {
        nextIndex = currentIndex - 1;
      }
      this.api.dispatchAction({
        type: "timelineChange",
        currentIndex: nextIndex,
        from: this.uid
      });
    };
    SliderTimelineView2.prototype._updateTicksStatus = function() {
      var currentIndex = this.model.getCurrentIndex();
      var tickSymbols = this._tickSymbols;
      var tickLabels = this._tickLabels;
      if (tickSymbols) {
        for (var i2 = 0; i2 < tickSymbols.length; i2++) {
          tickSymbols && tickSymbols[i2] && tickSymbols[i2].toggleState("progress", i2 < currentIndex);
        }
      }
      if (tickLabels) {
        for (var i2 = 0; i2 < tickLabels.length; i2++) {
          tickLabels && tickLabels[i2] && tickLabels[i2].toggleState("progress", labelDataIndexStore(tickLabels[i2]).dataIndex <= currentIndex);
        }
      }
    };
    SliderTimelineView2.type = "timeline.slider";
    return SliderTimelineView2;
  })(TimelineView_default)
);
function getViewRect(model2, api) {
  return getLayoutRect(model2.getBoxLayoutParams(), createBoxLayoutReference(model2, api).refContainer, model2.get("padding"));
}
function makeControlIcon(timelineModel, objPath, rect, opts) {
  var style2 = opts.style;
  var icon = createIcon(timelineModel.get(["controlStyle", objPath]), opts || {}, new BoundingRect_default(rect[0], rect[1], rect[2], rect[3]));
  if (style2) {
    icon.setStyle(style2);
  }
  return icon;
}
function giveSymbol(hostModel, itemStyleModel, group2, opt, symbol, callback) {
  var color3 = itemStyleModel.get("color");
  if (!symbol) {
    var symbolType = hostModel.get("symbol");
    symbol = createSymbol(symbolType, -1, -1, 2, 2, color3);
    symbol.setStyle("strokeNoScale", true);
    group2.add(symbol);
    callback && callback.onCreate(symbol);
  } else {
    symbol.setColor(color3);
    group2.add(symbol);
    callback && callback.onUpdate(symbol);
  }
  var itemStyle = itemStyleModel.getItemStyle(["color"]);
  symbol.setStyle(itemStyle);
  opt = merge2({
    rectHover: true,
    z2: 100
  }, opt, true);
  var symbolSize = normalizeSymbolSize(hostModel.get("symbolSize"));
  opt.scaleX = symbolSize[0] / 2;
  opt.scaleY = symbolSize[1] / 2;
  var symbolOffset = normalizeSymbolOffset(hostModel.get("symbolOffset"), symbolSize);
  if (symbolOffset) {
    opt.x = (opt.x || 0) + symbolOffset[0];
    opt.y = (opt.y || 0) + symbolOffset[1];
  }
  var symbolRotate = hostModel.get("symbolRotate");
  opt.rotation = (symbolRotate || 0) * Math.PI / 180 || 0;
  symbol.attr(opt);
  symbol.updateTransform();
  return symbol;
}
function pointerMoveTo(pointer, progressLine, dataIndex, axis, timelineModel, noAnimation) {
  if (pointer.dragging) {
    return;
  }
  var pointerModel = timelineModel.getModel("checkpointStyle");
  var toCoord = axis.dataToCoord(timelineModel.getData().get("value", dataIndex));
  if (noAnimation || !pointerModel.get("animation", true)) {
    pointer.attr({
      x: toCoord,
      y: 0
    });
    progressLine && progressLine.attr({
      shape: {
        x2: toCoord
      }
    });
  } else {
    var animationCfg = {
      duration: pointerModel.get("animationDuration", true),
      easing: pointerModel.get("animationEasing", true)
    };
    pointer.stopAnimation(null, true);
    pointer.animateTo({
      x: toCoord,
      y: 0
    }, animationCfg);
    progressLine && progressLine.animateTo({
      shape: {
        x2: toCoord
      }
    }, animationCfg);
  }
}

// node_modules/echarts/lib/component/marker/checkMarkerInSeries.js
function checkMarkerInSeries(seriesOpts, markerType) {
  if (!seriesOpts) {
    return false;
  }
  var seriesOptArr = isArray(seriesOpts) ? seriesOpts : [seriesOpts];
  for (var idx = 0; idx < seriesOptArr.length; idx++) {
    if (seriesOptArr[idx] && seriesOptArr[idx][markerType]) {
      return true;
    }
  }
  return false;
}

// node_modules/echarts/lib/component/marker/MarkerModel.js
function fillLabel(opt) {
  defaultEmphasis(opt, "label", ["show"]);
}
var inner20 = makeInner();
var MarkerModel = (
  /** @class */
  (function(_super) {
    __extends(MarkerModel2, _super);
    function MarkerModel2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = MarkerModel2.type;
      _this.createdBySelf = false;
      _this.preventAutoZ = true;
      return _this;
    }
    MarkerModel2.prototype.init = function(option, parentModel, ecModel) {
      if (true) {
        if (this.type === "marker") {
          throw new Error("Marker component is abstract component. Use markLine, markPoint, markArea instead.");
        }
      }
      this.mergeDefaultAndTheme(option, ecModel);
      this._mergeOption(option, ecModel, false, true);
    };
    MarkerModel2.prototype.isAnimationEnabled = function() {
      if (env_default.node) {
        return false;
      }
      var hostSeries = this.__hostSeries;
      return this.getShallow("animation") && hostSeries && hostSeries.isAnimationEnabled();
    };
    MarkerModel2.prototype.mergeOption = function(newOpt, ecModel) {
      this._mergeOption(newOpt, ecModel, false, false);
    };
    MarkerModel2.prototype._mergeOption = function(newOpt, ecModel, createdBySelf, isInit) {
      var componentType = this.mainType;
      if (!createdBySelf) {
        ecModel.eachSeries(function(seriesModel) {
          var markerOpt = seriesModel.get(this.mainType, true);
          var markerModel = inner20(seriesModel)[componentType];
          if (!markerOpt || !markerOpt.data) {
            inner20(seriesModel)[componentType] = null;
            return;
          }
          if (!markerModel) {
            if (isInit) {
              fillLabel(markerOpt);
            }
            each(markerOpt.data, function(item) {
              if (item instanceof Array) {
                fillLabel(item[0]);
                fillLabel(item[1]);
              } else {
                fillLabel(item);
              }
            });
            markerModel = this.createMarkerModelFromSeries(markerOpt, this, ecModel);
            extend(markerModel, {
              mainType: this.mainType,
              // Use the same series index and name
              seriesIndex: seriesModel.seriesIndex,
              name: seriesModel.name,
              createdBySelf: true
            });
            markerModel.__hostSeries = seriesModel;
          } else {
            markerModel._mergeOption(markerOpt, ecModel, true);
          }
          inner20(seriesModel)[componentType] = markerModel;
        }, this);
      }
    };
    MarkerModel2.prototype.formatTooltip = function(dataIndex, multipleSeries, dataType) {
      var data = this.getData();
      var value = this.getRawValue(dataIndex);
      var itemName = data.getName(dataIndex);
      return createTooltipMarkup("section", {
        header: this.name,
        blocks: [createTooltipMarkup("nameValue", {
          name: itemName,
          value,
          noName: !itemName,
          noValue: value == null
        })]
      });
    };
    MarkerModel2.prototype.getData = function() {
      return this._data;
    };
    MarkerModel2.prototype.setData = function(data) {
      this._data = data;
    };
    MarkerModel2.prototype.getDataParams = function(dataIndex, dataType) {
      var params = DataFormatMixin.prototype.getDataParams.call(this, dataIndex, dataType);
      var hostSeries = this.__hostSeries;
      if (hostSeries) {
        params.seriesId = hostSeries.id;
        params.seriesName = hostSeries.name;
        params.seriesType = hostSeries.subType;
      }
      return params;
    };
    MarkerModel2.getMarkerModelFromSeries = function(seriesModel, componentType) {
      return inner20(seriesModel)[componentType];
    };
    MarkerModel2.type = "marker";
    MarkerModel2.dependencies = ["series", "grid", "polar", "geo"];
    return MarkerModel2;
  })(Component_default)
);
mixin(MarkerModel, DataFormatMixin.prototype);
var MarkerModel_default = MarkerModel;

// node_modules/echarts/lib/component/marker/MarkPointModel.js
var MarkPointModel = (
  /** @class */
  (function(_super) {
    __extends(MarkPointModel2, _super);
    function MarkPointModel2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = MarkPointModel2.type;
      return _this;
    }
    MarkPointModel2.prototype.createMarkerModelFromSeries = function(markerOpt, masterMarkerModel, ecModel) {
      return new MarkPointModel2(markerOpt, masterMarkerModel, ecModel);
    };
    MarkPointModel2.type = "markPoint";
    MarkPointModel2.defaultOption = {
      // zlevel: 0,
      z: 5,
      symbol: "pin",
      symbolSize: 50,
      // symbolRotate: 0,
      // symbolOffset: [0, 0]
      tooltip: {
        trigger: "item"
      },
      label: {
        show: true,
        position: "inside"
      },
      itemStyle: {
        borderWidth: 2
      },
      emphasis: {
        label: {
          show: true
        }
      }
    };
    return MarkPointModel2;
  })(MarkerModel_default)
);

// node_modules/echarts/lib/component/marker/markerHelper.js
function hasXOrY(item) {
  return !(isNaN(parseFloat(item.x)) && isNaN(parseFloat(item.y)));
}
function hasXAndY(item) {
  return !isNaN(parseFloat(item.x)) && !isNaN(parseFloat(item.y));
}
function markerTypeCalculatorWithExtent(markerType, data, axisDim, otherDataDim, targetDataDim, otherCoordIndex, targetCoordIndex) {
  var coordArr = [];
  var stacked = isDimensionStacked(
    data,
    targetDataDim
    /* , otherDataDim */
  );
  var calcDataDim = stacked ? data.getCalculationInfo("stackResultDimension") : targetDataDim;
  var value = numCalculate(data, calcDataDim, markerType);
  var seriesModel = data.hostModel;
  var dataIndex = seriesModel.indicesOfNearest(axisDim, calcDataDim, value)[0];
  coordArr[otherCoordIndex] = data.get(otherDataDim, dataIndex);
  coordArr[targetCoordIndex] = data.get(calcDataDim, dataIndex);
  var coordArrValue = data.get(targetDataDim, dataIndex);
  var precision = getPrecision(data.get(targetDataDim, dataIndex));
  precision = Math.min(precision, 20);
  if (precision >= 0) {
    coordArr[targetCoordIndex] = +coordArr[targetCoordIndex].toFixed(precision);
  }
  return [coordArr, coordArrValue];
}
var markerTypeCalculator = {
  min: curry(markerTypeCalculatorWithExtent, "min"),
  max: curry(markerTypeCalculatorWithExtent, "max"),
  average: curry(markerTypeCalculatorWithExtent, "average"),
  median: curry(markerTypeCalculatorWithExtent, "median")
};
function dataTransform(seriesModel, item) {
  if (!item) {
    return;
  }
  var data = seriesModel.getData();
  var coordSys = seriesModel.coordinateSystem;
  var dims = coordSys && coordSys.dimensions;
  if (!hasXAndY(item) && !isArray(item.coord) && isArray(dims)) {
    var axisInfo = getAxisInfo2(item, data, coordSys, seriesModel);
    item = clone(item);
    if (item.type && markerTypeCalculator[item.type] && axisInfo.baseAxis && axisInfo.valueAxis) {
      var otherCoordIndex = indexOf(dims, axisInfo.baseAxis.dim);
      var targetCoordIndex = indexOf(dims, axisInfo.valueAxis.dim);
      var coordInfo = markerTypeCalculator[item.type](data, axisInfo.valueAxis.dim, axisInfo.baseDataDim, axisInfo.valueDataDim, otherCoordIndex, targetCoordIndex);
      item.coord = coordInfo[0];
      item.value = coordInfo[1];
    } else {
      item.coord = [item.xAxis != null ? item.xAxis : item.radiusAxis, item.yAxis != null ? item.yAxis : item.angleAxis];
    }
  }
  if (item.coord == null || !isArray(dims)) {
    item.coord = [];
    var baseAxis = seriesModel.getBaseAxis();
    if (baseAxis && item.type && markerTypeCalculator[item.type]) {
      var otherAxis = coordSys.getOtherAxis(baseAxis);
      if (otherAxis) {
        item.value = numCalculate(data, data.mapDimension(otherAxis.dim), item.type);
      }
    }
  } else {
    var coord = item.coord;
    for (var i2 = 0; i2 < 2; i2++) {
      if (markerTypeCalculator[coord[i2]]) {
        coord[i2] = numCalculate(data, data.mapDimension(dims[i2]), coord[i2]);
      }
    }
  }
  return item;
}
function getAxisInfo2(item, data, coordSys, seriesModel) {
  var ret = {};
  if (item.valueIndex != null || item.valueDim != null) {
    ret.valueDataDim = item.valueIndex != null ? data.getDimension(item.valueIndex) : item.valueDim;
    ret.valueAxis = coordSys.getAxis(dataDimToCoordDim(seriesModel, ret.valueDataDim));
    ret.baseAxis = coordSys.getOtherAxis(ret.valueAxis);
    ret.baseDataDim = data.mapDimension(ret.baseAxis.dim);
  } else {
    ret.baseAxis = seriesModel.getBaseAxis();
    ret.valueAxis = coordSys.getOtherAxis(ret.baseAxis);
    ret.baseDataDim = data.mapDimension(ret.baseAxis.dim);
    ret.valueDataDim = data.mapDimension(ret.valueAxis.dim);
  }
  return ret;
}
function dataDimToCoordDim(seriesModel, dataDim) {
  var dimItem = seriesModel.getData().getDimensionInfo(dataDim);
  return dimItem && dimItem.coordDim;
}
function dataFilter2(coordSys, item) {
  return coordSys && coordSys.containData && item.coord && !hasXOrY(item) ? coordSys.containData(item.coord) : true;
}
function zoneFilter(coordSys, item1, item2) {
  return coordSys && coordSys.containZone && item1.coord && item2.coord && !hasXOrY(item1) && !hasXOrY(item2) ? coordSys.containZone(item1.coord, item2.coord) : true;
}
function createMarkerDimValueGetter(inCoordSys, dims) {
  return inCoordSys ? function(item, dimName, dataIndex, dimIndex) {
    var rawVal = dimIndex < 2 ? item.coord && item.coord[dimIndex] : item.value;
    return parseDataValue(rawVal, dims[dimIndex]);
  } : function(item, dimName, dataIndex, dimIndex) {
    return parseDataValue(item.value, dims[dimIndex]);
  };
}
function numCalculate(data, valueDataDim, type) {
  if (type === "average") {
    var sum_1 = 0;
    var count_1 = 0;
    data.each(valueDataDim, function(val, idx) {
      if (!isNaN(val)) {
        sum_1 += val;
        count_1++;
      }
    });
    return sum_1 / count_1;
  } else if (type === "median") {
    return data.getMedian(valueDataDim);
  } else {
    return data.getDataExtent(valueDataDim)[type === "max" ? 1 : 0];
  }
}

// node_modules/echarts/lib/component/marker/MarkerView.js
var inner21 = makeInner();
var MarkerView = (
  /** @class */
  (function(_super) {
    __extends(MarkerView2, _super);
    function MarkerView2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = MarkerView2.type;
      return _this;
    }
    MarkerView2.prototype.init = function() {
      this.markerGroupMap = createHashMap();
    };
    MarkerView2.prototype.render = function(markerModel, ecModel, api) {
      var _this = this;
      var markerGroupMap = this.markerGroupMap;
      markerGroupMap.each(function(item) {
        inner21(item).keep = false;
      });
      ecModel.eachSeries(function(seriesModel) {
        var markerModel2 = MarkerModel_default.getMarkerModelFromSeries(seriesModel, _this.type);
        markerModel2 && _this.renderSeries(seriesModel, markerModel2, ecModel, api);
      });
      markerGroupMap.each(function(item) {
        !inner21(item).keep && _this.group.remove(item.group);
      });
      updateZ3(ecModel, markerGroupMap, this.type);
    };
    MarkerView2.prototype.markKeep = function(drawGroup) {
      inner21(drawGroup).keep = true;
    };
    MarkerView2.prototype.toggleBlurSeries = function(seriesModelList, isBlur) {
      var _this = this;
      each(seriesModelList, function(seriesModel) {
        var markerModel = MarkerModel_default.getMarkerModelFromSeries(seriesModel, _this.type);
        if (markerModel) {
          var data = markerModel.getData();
          data.eachItemGraphicEl(function(el) {
            if (el) {
              isBlur ? enterBlur(el) : leaveBlur(el);
            }
          });
        }
      });
    };
    MarkerView2.type = "marker";
    return MarkerView2;
  })(Component_default2)
);
function updateZ3(ecModel, markerGroupMap, type) {
  ecModel.eachSeries(function(seriesModel) {
    var markerModel = MarkerModel_default.getMarkerModelFromSeries(seriesModel, type);
    var markerDraw = markerGroupMap.get(seriesModel.id);
    if (markerModel && markerDraw && markerDraw.group) {
      var _a2 = retrieveZInfo(markerModel), z = _a2.z, zlevel = _a2.zlevel;
      traverseUpdateZ(markerDraw.group, z, zlevel);
    }
  });
}
var MarkerView_default = MarkerView;

// node_modules/echarts/lib/component/marker/MarkPointView.js
function updateMarkerLayout(mpData, seriesModel, api) {
  var coordSys = seriesModel.coordinateSystem;
  var apiWidth = api.getWidth();
  var apiHeight = api.getHeight();
  var coordRect = coordSys && coordSys.getArea && coordSys.getArea();
  mpData.each(function(idx) {
    var itemModel = mpData.getItemModel(idx);
    var isRelativeToCoordinate = itemModel.get("relativeTo") === "coordinate";
    var width = isRelativeToCoordinate ? coordRect ? coordRect.width : 0 : apiWidth;
    var height = isRelativeToCoordinate ? coordRect ? coordRect.height : 0 : apiHeight;
    var left = isRelativeToCoordinate && coordRect ? coordRect.x : 0;
    var top = isRelativeToCoordinate && coordRect ? coordRect.y : 0;
    var point;
    var xPx = parsePercent2(itemModel.get("x"), width) + left;
    var yPx = parsePercent2(itemModel.get("y"), height) + top;
    if (!isNaN(xPx) && !isNaN(yPx)) {
      point = [xPx, yPx];
    } else if (seriesModel.getMarkerPosition) {
      point = seriesModel.getMarkerPosition(mpData.getValues(mpData.dimensions, idx));
    } else if (coordSys) {
      var x = mpData.get(coordSys.dimensions[0], idx);
      var y = mpData.get(coordSys.dimensions[1], idx);
      point = coordSys.dataToPoint([x, y]);
    }
    if (!isNaN(xPx)) {
      point[0] = xPx;
    }
    if (!isNaN(yPx)) {
      point[1] = yPx;
    }
    mpData.setItemLayout(idx, point);
  });
}
var MarkPointView = (
  /** @class */
  (function(_super) {
    __extends(MarkPointView2, _super);
    function MarkPointView2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = MarkPointView2.type;
      return _this;
    }
    MarkPointView2.prototype.updateTransform = function(markPointModel, ecModel, api) {
      ecModel.eachSeries(function(seriesModel) {
        var mpModel = MarkerModel_default.getMarkerModelFromSeries(seriesModel, "markPoint");
        if (mpModel) {
          updateMarkerLayout(mpModel.getData(), seriesModel, api);
          this.markerGroupMap.get(seriesModel.id).updateLayout();
        }
      }, this);
    };
    MarkPointView2.prototype.renderSeries = function(seriesModel, mpModel, ecModel, api) {
      var coordSys = seriesModel.coordinateSystem;
      var seriesId = seriesModel.id;
      var seriesData = seriesModel.getData();
      var symbolDrawMap = this.markerGroupMap;
      var symbolDraw = symbolDrawMap.get(seriesId) || symbolDrawMap.set(seriesId, new SymbolDraw_default());
      var mpData = createData(coordSys, seriesModel, mpModel);
      mpModel.setData(mpData);
      updateMarkerLayout(mpModel.getData(), seriesModel, api);
      mpData.each(function(idx) {
        var itemModel = mpData.getItemModel(idx);
        var symbol = itemModel.getShallow("symbol");
        var symbolSize = itemModel.getShallow("symbolSize");
        var symbolRotate = itemModel.getShallow("symbolRotate");
        var symbolOffset = itemModel.getShallow("symbolOffset");
        var symbolKeepAspect = itemModel.getShallow("symbolKeepAspect");
        if (isFunction(symbol) || isFunction(symbolSize) || isFunction(symbolRotate) || isFunction(symbolOffset)) {
          var rawIdx = mpModel.getRawValue(idx);
          var dataParams = mpModel.getDataParams(idx);
          if (isFunction(symbol)) {
            symbol = symbol(rawIdx, dataParams);
          }
          if (isFunction(symbolSize)) {
            symbolSize = symbolSize(rawIdx, dataParams);
          }
          if (isFunction(symbolRotate)) {
            symbolRotate = symbolRotate(rawIdx, dataParams);
          }
          if (isFunction(symbolOffset)) {
            symbolOffset = symbolOffset(rawIdx, dataParams);
          }
        }
        var style2 = itemModel.getModel("itemStyle").getItemStyle();
        var z2 = itemModel.get("z2");
        var color3 = getVisualFromData(seriesData, "color");
        if (!style2.fill) {
          style2.fill = color3;
        }
        mpData.setItemVisual(idx, {
          z2: retrieve2(z2, 0),
          symbol,
          symbolSize,
          symbolRotate,
          symbolOffset,
          symbolKeepAspect,
          style: style2
        });
      });
      symbolDraw.updateData(mpData);
      this.group.add(symbolDraw.group);
      mpData.eachItemGraphicEl(function(el) {
        el.traverse(function(child) {
          getECData(child).dataModel = mpModel;
        });
      });
      this.markKeep(symbolDraw);
      symbolDraw.group.silent = mpModel.get("silent") || seriesModel.get("silent");
    };
    MarkPointView2.type = "markPoint";
    return MarkPointView2;
  })(MarkerView_default)
);
function createData(coordSys, seriesModel, mpModel) {
  var coordDimsInfos;
  if (coordSys) {
    coordDimsInfos = map2(coordSys && coordSys.dimensions, function(coordDim) {
      var data = seriesModel.getData();
      var info = data.getDimensionInfo(data.mapDimension(coordDim)) || {};
      return extend(extend({}, info), {
        name: coordDim,
        // DON'T use ordinalMeta to parse and collect ordinal.
        ordinalMeta: null
      });
    });
  } else {
    coordDimsInfos = [{
      name: "value",
      type: "float"
    }];
  }
  var mpData = new SeriesData_default(coordDimsInfos, mpModel);
  var dataOpt = map2(mpModel.get("data"), curry(dataTransform, seriesModel));
  if (coordSys) {
    dataOpt = filter2(dataOpt, curry(dataFilter2, coordSys));
  }
  var dimValueGetter = createMarkerDimValueGetter(!!coordSys, coordDimsInfos);
  mpData.initData(dataOpt, null, dimValueGetter);
  return mpData;
}

// node_modules/echarts/lib/component/marker/MarkLineModel.js
var MarkLineModel = (
  /** @class */
  (function(_super) {
    __extends(MarkLineModel2, _super);
    function MarkLineModel2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = MarkLineModel2.type;
      return _this;
    }
    MarkLineModel2.prototype.createMarkerModelFromSeries = function(markerOpt, masterMarkerModel, ecModel) {
      return new MarkLineModel2(markerOpt, masterMarkerModel, ecModel);
    };
    MarkLineModel2.type = "markLine";
    MarkLineModel2.defaultOption = {
      // zlevel: 0,
      z: 5,
      symbol: ["circle", "arrow"],
      symbolSize: [8, 16],
      // symbolRotate: 0,
      symbolOffset: 0,
      precision: 2,
      tooltip: {
        trigger: "item"
      },
      label: {
        show: true,
        position: "end",
        distance: 5
      },
      lineStyle: {
        type: "dashed"
      },
      emphasis: {
        label: {
          show: true
        },
        lineStyle: {
          width: 3
        }
      },
      animationEasing: "linear"
    };
    return MarkLineModel2;
  })(MarkerModel_default)
);
var MarkLineModel_default = MarkLineModel;

// node_modules/echarts/lib/component/marker/MarkLineView.js
var inner22 = makeInner();
var markLineTransform = function(seriesModel, coordSys, mlModel, item) {
  var data = seriesModel.getData();
  var itemArray;
  if (!isArray(item)) {
    var mlType = item.type;
    if (mlType === "min" || mlType === "max" || mlType === "average" || mlType === "median" || item.xAxis != null || item.yAxis != null) {
      var valueAxis2 = void 0;
      var value = void 0;
      if (item.yAxis != null || item.xAxis != null) {
        valueAxis2 = coordSys.getAxis(item.yAxis != null ? "y" : "x");
        value = retrieve(item.yAxis, item.xAxis);
      } else {
        var axisInfo = getAxisInfo2(item, data, coordSys, seriesModel);
        valueAxis2 = axisInfo.valueAxis;
        var valueDataDim = getStackedDimension(data, axisInfo.valueDataDim);
        value = numCalculate(data, valueDataDim, mlType);
      }
      var valueIndex = valueAxis2.dim === "x" ? 0 : 1;
      var baseIndex = 1 - valueIndex;
      var mlFrom = clone(item);
      var mlTo = {
        coord: []
      };
      mlFrom.type = null;
      mlFrom.coord = [];
      mlFrom.coord[baseIndex] = -Infinity;
      mlTo.coord[baseIndex] = Infinity;
      var precision = mlModel.get("precision");
      if (precision >= 0 && isNumber(value)) {
        value = +value.toFixed(Math.min(precision, 20));
      }
      mlFrom.coord[valueIndex] = mlTo.coord[valueIndex] = value;
      itemArray = [mlFrom, mlTo, {
        type: mlType,
        valueIndex: item.valueIndex,
        // Force to use the value of calculated value.
        value
      }];
    } else {
      if (true) {
        logError("Invalid markLine data.");
      }
      itemArray = [];
    }
  } else {
    itemArray = item;
  }
  var normalizedItem = [dataTransform(seriesModel, itemArray[0]), dataTransform(seriesModel, itemArray[1]), extend({}, itemArray[2])];
  normalizedItem[2].type = normalizedItem[2].type || null;
  merge2(normalizedItem[2], normalizedItem[0]);
  merge2(normalizedItem[2], normalizedItem[1]);
  return normalizedItem;
};
function isInfinity(val) {
  return !isNaN(val) && !isFinite(val);
}
function ifMarkLineHasOnlyDim(dimIndex, fromCoord, toCoord, coordSys) {
  var otherDimIndex = 1 - dimIndex;
  var dimName = coordSys.dimensions[dimIndex];
  return isInfinity(fromCoord[otherDimIndex]) && isInfinity(toCoord[otherDimIndex]) && fromCoord[dimIndex] === toCoord[dimIndex] && coordSys.getAxis(dimName).containData(fromCoord[dimIndex]);
}
function markLineFilter(coordSys, item) {
  if (coordSys.type === "cartesian2d") {
    var fromCoord = item[0].coord;
    var toCoord = item[1].coord;
    if (fromCoord && toCoord && (ifMarkLineHasOnlyDim(1, fromCoord, toCoord, coordSys) || ifMarkLineHasOnlyDim(0, fromCoord, toCoord, coordSys))) {
      return true;
    }
  }
  return dataFilter2(coordSys, item[0]) && dataFilter2(coordSys, item[1]);
}
function updateSingleMarkerEndLayout(data, idx, isFrom, seriesModel, api) {
  var coordSys = seriesModel.coordinateSystem;
  var itemModel = data.getItemModel(idx);
  var point;
  var xPx = parsePercent2(itemModel.get("x"), api.getWidth());
  var yPx = parsePercent2(itemModel.get("y"), api.getHeight());
  if (!isNaN(xPx) && !isNaN(yPx)) {
    point = [xPx, yPx];
  } else {
    if (seriesModel.getMarkerPosition) {
      point = seriesModel.getMarkerPosition(data.getValues(data.dimensions, idx));
    } else {
      var dims = coordSys.dimensions;
      var x = data.get(dims[0], idx);
      var y = data.get(dims[1], idx);
      point = coordSys.dataToPoint([x, y]);
    }
    if (isCoordinateSystemType(coordSys, "cartesian2d")) {
      var xAxis = coordSys.getAxis("x");
      var yAxis = coordSys.getAxis("y");
      var dims = coordSys.dimensions;
      if (isInfinity(data.get(dims[0], idx))) {
        point[0] = xAxis.toGlobalCoord(xAxis.getExtent()[isFrom ? 0 : 1]);
      } else if (isInfinity(data.get(dims[1], idx))) {
        point[1] = yAxis.toGlobalCoord(yAxis.getExtent()[isFrom ? 0 : 1]);
      }
    }
    if (!isNaN(xPx)) {
      point[0] = xPx;
    }
    if (!isNaN(yPx)) {
      point[1] = yPx;
    }
  }
  data.setItemLayout(idx, point);
}
var MarkLineView = (
  /** @class */
  (function(_super) {
    __extends(MarkLineView2, _super);
    function MarkLineView2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = MarkLineView2.type;
      return _this;
    }
    MarkLineView2.prototype.updateTransform = function(markLineModel, ecModel, api) {
      ecModel.eachSeries(function(seriesModel) {
        var mlModel = MarkerModel_default.getMarkerModelFromSeries(seriesModel, "markLine");
        if (mlModel) {
          var mlData_1 = mlModel.getData();
          var fromData_1 = inner22(mlModel).from;
          var toData_1 = inner22(mlModel).to;
          fromData_1.each(function(idx) {
            updateSingleMarkerEndLayout(fromData_1, idx, true, seriesModel, api);
            updateSingleMarkerEndLayout(toData_1, idx, false, seriesModel, api);
          });
          mlData_1.each(function(idx) {
            mlData_1.setItemLayout(idx, [fromData_1.getItemLayout(idx), toData_1.getItemLayout(idx)]);
          });
          this.markerGroupMap.get(seriesModel.id).updateLayout();
        }
      }, this);
    };
    MarkLineView2.prototype.renderSeries = function(seriesModel, mlModel, ecModel, api) {
      var coordSys = seriesModel.coordinateSystem;
      var seriesId = seriesModel.id;
      var seriesData = seriesModel.getData();
      var lineDrawMap = this.markerGroupMap;
      var lineDraw = lineDrawMap.get(seriesId) || lineDrawMap.set(seriesId, new LineDraw_default());
      this.group.add(lineDraw.group);
      var mlData = createList(coordSys, seriesModel, mlModel);
      var fromData = mlData.from;
      var toData = mlData.to;
      var lineData = mlData.line;
      inner22(mlModel).from = fromData;
      inner22(mlModel).to = toData;
      mlModel.setData(lineData);
      var symbolType = mlModel.get("symbol");
      var symbolSize = mlModel.get("symbolSize");
      var symbolRotate = mlModel.get("symbolRotate");
      var symbolOffset = mlModel.get("symbolOffset");
      if (!isArray(symbolType)) {
        symbolType = [symbolType, symbolType];
      }
      if (!isArray(symbolSize)) {
        symbolSize = [symbolSize, symbolSize];
      }
      if (!isArray(symbolRotate)) {
        symbolRotate = [symbolRotate, symbolRotate];
      }
      if (!isArray(symbolOffset)) {
        symbolOffset = [symbolOffset, symbolOffset];
      }
      mlData.from.each(function(idx) {
        updateDataVisualAndLayout(fromData, idx, true);
        updateDataVisualAndLayout(toData, idx, false);
      });
      lineData.each(function(idx) {
        var itemModel = lineData.getItemModel(idx);
        var lineStyle = itemModel.getModel("lineStyle").getLineStyle();
        lineData.setItemLayout(idx, [fromData.getItemLayout(idx), toData.getItemLayout(idx)]);
        var z2 = itemModel.get("z2");
        if (lineStyle.stroke == null) {
          lineStyle.stroke = fromData.getItemVisual(idx, "style").fill;
        }
        lineData.setItemVisual(idx, {
          z2: retrieve2(z2, 0),
          fromSymbolKeepAspect: fromData.getItemVisual(idx, "symbolKeepAspect"),
          fromSymbolOffset: fromData.getItemVisual(idx, "symbolOffset"),
          fromSymbolRotate: fromData.getItemVisual(idx, "symbolRotate"),
          fromSymbolSize: fromData.getItemVisual(idx, "symbolSize"),
          fromSymbol: fromData.getItemVisual(idx, "symbol"),
          toSymbolKeepAspect: toData.getItemVisual(idx, "symbolKeepAspect"),
          toSymbolOffset: toData.getItemVisual(idx, "symbolOffset"),
          toSymbolRotate: toData.getItemVisual(idx, "symbolRotate"),
          toSymbolSize: toData.getItemVisual(idx, "symbolSize"),
          toSymbol: toData.getItemVisual(idx, "symbol"),
          style: lineStyle
        });
      });
      lineDraw.updateData(lineData);
      mlData.line.eachItemGraphicEl(function(el) {
        getECData(el).dataModel = mlModel;
        el.traverse(function(child) {
          getECData(child).dataModel = mlModel;
        });
      });
      function updateDataVisualAndLayout(data, idx, isFrom) {
        var itemModel = data.getItemModel(idx);
        updateSingleMarkerEndLayout(data, idx, isFrom, seriesModel, api);
        var style2 = itemModel.getModel("itemStyle").getItemStyle();
        if (style2.fill == null) {
          style2.fill = getVisualFromData(seriesData, "color");
        }
        data.setItemVisual(idx, {
          symbolKeepAspect: itemModel.get("symbolKeepAspect"),
          // `0` should be considered as a valid value, so use `retrieve2` instead of `||`
          symbolOffset: retrieve2(itemModel.get("symbolOffset", true), symbolOffset[isFrom ? 0 : 1]),
          symbolRotate: retrieve2(itemModel.get("symbolRotate", true), symbolRotate[isFrom ? 0 : 1]),
          // TODO: when 2d array is supported, it should ignore parent
          symbolSize: retrieve2(itemModel.get("symbolSize"), symbolSize[isFrom ? 0 : 1]),
          symbol: retrieve2(itemModel.get("symbol", true), symbolType[isFrom ? 0 : 1]),
          style: style2
        });
      }
      this.markKeep(lineDraw);
      lineDraw.group.silent = mlModel.get("silent") || seriesModel.get("silent");
    };
    MarkLineView2.type = "markLine";
    return MarkLineView2;
  })(MarkerView_default)
);
function createList(coordSys, seriesModel, mlModel) {
  var coordDimsInfos;
  if (coordSys) {
    coordDimsInfos = map2(coordSys && coordSys.dimensions, function(coordDim) {
      var data = seriesModel.getData();
      var info = data.getDimensionInfo(data.mapDimension(coordDim)) || {};
      return extend(extend({}, info), {
        name: coordDim,
        // DON'T use ordinalMeta to parse and collect ordinal.
        ordinalMeta: null
      });
    });
  } else {
    coordDimsInfos = [{
      name: "value",
      type: "float"
    }];
  }
  var fromData = new SeriesData_default(coordDimsInfos, mlModel);
  var toData = new SeriesData_default(coordDimsInfos, mlModel);
  var lineData = new SeriesData_default([], mlModel);
  var optData = map2(mlModel.get("data"), curry(markLineTransform, seriesModel, coordSys, mlModel));
  if (coordSys) {
    optData = filter2(optData, curry(markLineFilter, coordSys));
  }
  var dimValueGetter = createMarkerDimValueGetter(!!coordSys, coordDimsInfos);
  fromData.initData(map2(optData, function(item) {
    return item[0];
  }), null, dimValueGetter);
  toData.initData(map2(optData, function(item) {
    return item[1];
  }), null, dimValueGetter);
  lineData.initData(map2(optData, function(item) {
    return item[2];
  }));
  lineData.hasItemOption = true;
  return {
    from: fromData,
    to: toData,
    line: lineData
  };
}
var MarkLineView_default = MarkLineView;

// node_modules/echarts/lib/component/marker/installMarkLine.js
function install35(registers) {
  registers.registerComponentModel(MarkLineModel_default);
  registers.registerComponentView(MarkLineView_default);
  registers.registerPreprocessor(function(opt) {
    if (checkMarkerInSeries(opt.series, "markLine")) {
      opt.markLine = opt.markLine || {};
    }
  });
}

// node_modules/echarts/lib/component/marker/MarkAreaModel.js
var MarkAreaModel = (
  /** @class */
  (function(_super) {
    __extends(MarkAreaModel2, _super);
    function MarkAreaModel2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = MarkAreaModel2.type;
      return _this;
    }
    MarkAreaModel2.prototype.createMarkerModelFromSeries = function(markerOpt, masterMarkerModel, ecModel) {
      return new MarkAreaModel2(markerOpt, masterMarkerModel, ecModel);
    };
    MarkAreaModel2.type = "markArea";
    MarkAreaModel2.defaultOption = {
      // zlevel: 0,
      // PENDING
      z: 1,
      tooltip: {
        trigger: "item"
      },
      // markArea should fixed on the coordinate system
      animation: false,
      label: {
        show: true,
        position: "top"
      },
      itemStyle: {
        // color and borderColor default to use color from series
        // color: 'auto'
        // borderColor: 'auto'
        borderWidth: 0
      },
      emphasis: {
        label: {
          show: true,
          position: "top"
        }
      }
    };
    return MarkAreaModel2;
  })(MarkerModel_default)
);

// node_modules/echarts/lib/component/marker/MarkAreaView.js
var inner23 = makeInner();
var markAreaTransform = function(seriesModel, coordSys, maModel, item) {
  var item0 = item[0];
  var item1 = item[1];
  if (!item0 || !item1) {
    return;
  }
  var lt2 = dataTransform(seriesModel, item0);
  var rb2 = dataTransform(seriesModel, item1);
  var ltCoord = lt2.coord;
  var rbCoord = rb2.coord;
  ltCoord[0] = retrieve(ltCoord[0], -Infinity);
  ltCoord[1] = retrieve(ltCoord[1], -Infinity);
  rbCoord[0] = retrieve(rbCoord[0], Infinity);
  rbCoord[1] = retrieve(rbCoord[1], Infinity);
  var result = mergeAll([{}, lt2, rb2]);
  result.coord = [lt2.coord, rb2.coord];
  result.x0 = lt2.x;
  result.y0 = lt2.y;
  result.x1 = rb2.x;
  result.y1 = rb2.y;
  return result;
};
function isInfinity2(val) {
  return !isNaN(val) && !isFinite(val);
}
function ifMarkAreaHasOnlyDim(dimIndex, fromCoord, toCoord, coordSys) {
  var otherDimIndex = 1 - dimIndex;
  return isInfinity2(fromCoord[otherDimIndex]) && isInfinity2(toCoord[otherDimIndex]);
}
function markAreaFilter(coordSys, item) {
  var fromCoord = item.coord[0];
  var toCoord = item.coord[1];
  var item0 = {
    coord: fromCoord,
    x: item.x0,
    y: item.y0
  };
  var item1 = {
    coord: toCoord,
    x: item.x1,
    y: item.y1
  };
  if (isCoordinateSystemType(coordSys, "cartesian2d")) {
    if (fromCoord && toCoord && (ifMarkAreaHasOnlyDim(1, fromCoord, toCoord, coordSys) || ifMarkAreaHasOnlyDim(0, fromCoord, toCoord, coordSys))) {
      return true;
    }
    return zoneFilter(coordSys, item0, item1);
  }
  return dataFilter2(coordSys, item0) || dataFilter2(coordSys, item1);
}
function getSingleMarkerEndPoint(data, idx, dims, seriesModel, api) {
  var coordSys = seriesModel.coordinateSystem;
  var itemModel = data.getItemModel(idx);
  var point;
  var xPx = parsePercent2(itemModel.get(dims[0]), api.getWidth());
  var yPx = parsePercent2(itemModel.get(dims[1]), api.getHeight());
  if (!isNaN(xPx) && !isNaN(yPx)) {
    point = [xPx, yPx];
  } else {
    if (seriesModel.getMarkerPosition) {
      var pointValue0 = data.getValues(["x0", "y0"], idx);
      var pointValue1 = data.getValues(["x1", "y1"], idx);
      var clampPointValue0 = coordSys.clampData(pointValue0);
      var clampPointValue1 = coordSys.clampData(pointValue1);
      var pointValue = [];
      if (dims[0] === "x0") {
        pointValue[0] = clampPointValue0[0] > clampPointValue1[0] ? pointValue1[0] : pointValue0[0];
      } else {
        pointValue[0] = clampPointValue0[0] > clampPointValue1[0] ? pointValue0[0] : pointValue1[0];
      }
      if (dims[1] === "y0") {
        pointValue[1] = clampPointValue0[1] > clampPointValue1[1] ? pointValue1[1] : pointValue0[1];
      } else {
        pointValue[1] = clampPointValue0[1] > clampPointValue1[1] ? pointValue0[1] : pointValue1[1];
      }
      point = seriesModel.getMarkerPosition(pointValue, dims, true);
    } else {
      var x = data.get(dims[0], idx);
      var y = data.get(dims[1], idx);
      var pt = [x, y];
      coordSys.clampData && coordSys.clampData(pt, pt);
      point = coordSys.dataToPoint(pt, true);
    }
    if (isCoordinateSystemType(coordSys, "cartesian2d")) {
      var xAxis = coordSys.getAxis("x");
      var yAxis = coordSys.getAxis("y");
      var x = data.get(dims[0], idx);
      var y = data.get(dims[1], idx);
      if (isInfinity2(x)) {
        point[0] = xAxis.toGlobalCoord(xAxis.getExtent()[dims[0] === "x0" ? 0 : 1]);
      } else if (isInfinity2(y)) {
        point[1] = yAxis.toGlobalCoord(yAxis.getExtent()[dims[1] === "y0" ? 0 : 1]);
      }
    }
    if (!isNaN(xPx)) {
      point[0] = xPx;
    }
    if (!isNaN(yPx)) {
      point[1] = yPx;
    }
  }
  return point;
}
var dimPermutations = [["x0", "y0"], ["x1", "y0"], ["x1", "y1"], ["x0", "y1"]];
var MarkAreaView = (
  /** @class */
  (function(_super) {
    __extends(MarkAreaView2, _super);
    function MarkAreaView2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = MarkAreaView2.type;
      return _this;
    }
    MarkAreaView2.prototype.updateTransform = function(markAreaModel, ecModel, api) {
      ecModel.eachSeries(function(seriesModel) {
        var maModel = MarkerModel_default.getMarkerModelFromSeries(seriesModel, "markArea");
        if (maModel) {
          var areaData_1 = maModel.getData();
          areaData_1.each(function(idx) {
            var points4 = map2(dimPermutations, function(dim) {
              return getSingleMarkerEndPoint(areaData_1, idx, dim, seriesModel, api);
            });
            areaData_1.setItemLayout(idx, points4);
            var el = areaData_1.getItemGraphicEl(idx);
            el.setShape("points", points4);
          });
        }
      }, this);
    };
    MarkAreaView2.prototype.renderSeries = function(seriesModel, maModel, ecModel, api) {
      var coordSys = seriesModel.coordinateSystem;
      var seriesId = seriesModel.id;
      var seriesData = seriesModel.getData();
      var areaGroupMap = this.markerGroupMap;
      var polygonGroup = areaGroupMap.get(seriesId) || areaGroupMap.set(seriesId, {
        group: new Group_default()
      });
      this.group.add(polygonGroup.group);
      this.markKeep(polygonGroup);
      var areaData = createList2(coordSys, seriesModel, maModel);
      maModel.setData(areaData);
      areaData.each(function(idx) {
        var points4 = map2(dimPermutations, function(dim) {
          return getSingleMarkerEndPoint(areaData, idx, dim, seriesModel, api);
        });
        var xAxisScale = coordSys.getAxis("x").scale;
        var yAxisScale = coordSys.getAxis("y").scale;
        var xAxisExtent = xAxisScale.getExtent();
        var yAxisExtent = yAxisScale.getExtent();
        var xPointExtent = [xAxisScale.parse(areaData.get("x0", idx)), xAxisScale.parse(areaData.get("x1", idx))];
        var yPointExtent = [yAxisScale.parse(areaData.get("y0", idx)), yAxisScale.parse(areaData.get("y1", idx))];
        asc(xPointExtent);
        asc(yPointExtent);
        var overlapped = !(xAxisExtent[0] > xPointExtent[1] || xAxisExtent[1] < xPointExtent[0] || yAxisExtent[0] > yPointExtent[1] || yAxisExtent[1] < yPointExtent[0]);
        var allClipped = !overlapped;
        areaData.setItemLayout(idx, {
          points: points4,
          allClipped
        });
        var itemModel = areaData.getItemModel(idx);
        var style2 = itemModel.getModel("itemStyle").getItemStyle();
        var z2 = itemModel.get("z2");
        var color3 = getVisualFromData(seriesData, "color");
        if (!style2.fill) {
          style2.fill = color3;
          if (isString(style2.fill)) {
            style2.fill = modifyAlpha(style2.fill, 0.4);
          }
        }
        if (!style2.stroke) {
          style2.stroke = color3;
        }
        areaData.setItemVisual(idx, "style", style2);
        areaData.setItemVisual(idx, "z2", retrieve2(z2, 0));
      });
      areaData.diff(inner23(polygonGroup).data).add(function(idx) {
        var layout3 = areaData.getItemLayout(idx);
        var z2 = areaData.getItemVisual(idx, "z2");
        if (!layout3.allClipped) {
          var polygon = new Polygon_default({
            z2: retrieve2(z2, 0),
            shape: {
              points: layout3.points
            }
          });
          areaData.setItemGraphicEl(idx, polygon);
          polygonGroup.group.add(polygon);
        }
      }).update(function(newIdx, oldIdx) {
        var polygon = inner23(polygonGroup).data.getItemGraphicEl(oldIdx);
        var layout3 = areaData.getItemLayout(newIdx);
        var z2 = areaData.getItemVisual(newIdx, "z2");
        if (!layout3.allClipped) {
          if (polygon) {
            updateProps(polygon, {
              z2: retrieve2(z2, 0),
              shape: {
                points: layout3.points
              }
            }, maModel, newIdx);
          } else {
            polygon = new Polygon_default({
              shape: {
                points: layout3.points
              }
            });
          }
          areaData.setItemGraphicEl(newIdx, polygon);
          polygonGroup.group.add(polygon);
        } else if (polygon) {
          polygonGroup.group.remove(polygon);
        }
      }).remove(function(idx) {
        var polygon = inner23(polygonGroup).data.getItemGraphicEl(idx);
        polygonGroup.group.remove(polygon);
      }).execute();
      areaData.eachItemGraphicEl(function(polygon, idx) {
        var itemModel = areaData.getItemModel(idx);
        var style2 = areaData.getItemVisual(idx, "style");
        polygon.useStyle(areaData.getItemVisual(idx, "style"));
        setLabelStyle(polygon, getLabelStatesModels(itemModel), {
          labelFetcher: maModel,
          labelDataIndex: idx,
          defaultText: areaData.getName(idx) || "",
          inheritColor: isString(style2.fill) ? modifyAlpha(style2.fill, 1) : tokens_default.color.neutral99
        });
        setStatesStylesFromModel(polygon, itemModel);
        toggleHoverEmphasis(polygon, null, null, itemModel.get(["emphasis", "disabled"]));
        getECData(polygon).dataModel = maModel;
      });
      inner23(polygonGroup).data = areaData;
      polygonGroup.group.silent = maModel.get("silent") || seriesModel.get("silent");
    };
    MarkAreaView2.type = "markArea";
    return MarkAreaView2;
  })(MarkerView_default)
);
function createList2(coordSys, seriesModel, maModel) {
  var areaData;
  var dataDims;
  var dims = ["x0", "y0", "x1", "y1"];
  if (coordSys) {
    var coordDimsInfos_1 = map2(coordSys && coordSys.dimensions, function(coordDim) {
      var data = seriesModel.getData();
      var info = data.getDimensionInfo(data.mapDimension(coordDim)) || {};
      return extend(extend({}, info), {
        name: coordDim,
        // DON'T use ordinalMeta to parse and collect ordinal.
        ordinalMeta: null
      });
    });
    dataDims = map2(dims, function(dim, idx) {
      return {
        name: dim,
        type: coordDimsInfos_1[idx % 2].type
      };
    });
    areaData = new SeriesData_default(dataDims, maModel);
  } else {
    dataDims = [{
      name: "value",
      type: "float"
    }];
    areaData = new SeriesData_default(dataDims, maModel);
  }
  var optData = map2(maModel.get("data"), curry(markAreaTransform, seriesModel, coordSys, maModel));
  if (coordSys) {
    optData = filter2(optData, curry(markAreaFilter, coordSys));
  }
  var dimValueGetter = coordSys ? function(item, dimName, dataIndex, dimIndex) {
    var rawVal = item.coord[Math.floor(dimIndex / 2)][dimIndex % 2];
    return parseDataValue(rawVal, dataDims[dimIndex]);
  } : function(item, dimName, dataIndex, dimIndex) {
    return parseDataValue(item.value, dataDims[dimIndex]);
  };
  areaData.initData(optData, null, dimValueGetter);
  areaData.hasItemOption = true;
  return areaData;
}

// node_modules/echarts/lib/component/legend/LegendModel.js
var getDefaultSelectorOptions = function(ecModel, type) {
  if (type === "all") {
    return {
      type: "all",
      title: ecModel.getLocaleModel().get(["legend", "selector", "all"])
    };
  } else if (type === "inverse") {
    return {
      type: "inverse",
      title: ecModel.getLocaleModel().get(["legend", "selector", "inverse"])
    };
  }
};
var LegendModel = (
  /** @class */
  (function(_super) {
    __extends(LegendModel2, _super);
    function LegendModel2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = LegendModel2.type;
      _this.layoutMode = {
        type: "box",
        // legend.width/height are maxWidth/maxHeight actually,
        // whereas real width/height is calculated by its content.
        // (Setting {left: 10, right: 10} does not make sense).
        // So consider the case:
        // `setOption({legend: {left: 10});`
        // then `setOption({legend: {right: 10});`
        // The previous `left` should be cleared by setting `ignoreSize`.
        ignoreSize: true
      };
      return _this;
    }
    LegendModel2.prototype.init = function(option, parentModel, ecModel) {
      this.mergeDefaultAndTheme(option, ecModel);
      option.selected = option.selected || {};
      this._updateSelector(option);
    };
    LegendModel2.prototype.mergeOption = function(option, ecModel) {
      _super.prototype.mergeOption.call(this, option, ecModel);
      this._updateSelector(option);
    };
    LegendModel2.prototype._updateSelector = function(option) {
      var selector2 = option.selector;
      var ecModel = this.ecModel;
      if (selector2 === true) {
        selector2 = option.selector = ["all", "inverse"];
      }
      if (isArray(selector2)) {
        each(selector2, function(item, index) {
          isString(item) && (item = {
            type: item
          });
          selector2[index] = merge2(item, getDefaultSelectorOptions(ecModel, item.type));
        });
      }
    };
    LegendModel2.prototype.optionUpdated = function() {
      this._updateData(this.ecModel);
      var legendData = this._data;
      if (legendData[0] && this.get("selectedMode") === "single") {
        var hasSelected = false;
        for (var i2 = 0; i2 < legendData.length; i2++) {
          var name_1 = legendData[i2].get("name");
          if (this.isSelected(name_1)) {
            this.select(name_1);
            hasSelected = true;
            break;
          }
        }
        !hasSelected && this.select(legendData[0].get("name"));
      }
    };
    LegendModel2.prototype._updateData = function(ecModel) {
      var potentialData = [];
      var availableNames = [];
      ecModel.eachRawSeries(function(seriesModel) {
        var seriesName = seriesModel.name;
        availableNames.push(seriesName);
        var isPotential;
        if (seriesModel.legendVisualProvider) {
          var provider = seriesModel.legendVisualProvider;
          var names = provider.getAllNames();
          if (!ecModel.isSeriesFiltered(seriesModel)) {
            availableNames = availableNames.concat(names);
          }
          if (names.length) {
            potentialData = potentialData.concat(names);
          } else {
            isPotential = true;
          }
        } else {
          isPotential = true;
        }
        if (isPotential && isNameSpecified(seriesModel)) {
          potentialData.push(seriesModel.name);
        }
      });
      this._availableNames = availableNames;
      var rawData = this.get("data") || potentialData;
      var legendNameMap = createHashMap();
      var legendData = map2(rawData, function(dataItem) {
        if (isString(dataItem) || isNumber(dataItem)) {
          dataItem = {
            name: dataItem
          };
        }
        if (legendNameMap.get(dataItem.name)) {
          return null;
        }
        legendNameMap.set(dataItem.name, true);
        return new Model_default(dataItem, this, this.ecModel);
      }, this);
      this._data = filter2(legendData, function(item) {
        return !!item;
      });
    };
    LegendModel2.prototype.getData = function() {
      return this._data;
    };
    LegendModel2.prototype.select = function(name) {
      var selected = this.option.selected;
      var selectedMode = this.get("selectedMode");
      if (selectedMode === "single") {
        var data = this._data;
        each(data, function(dataItem) {
          selected[dataItem.get("name")] = false;
        });
      }
      selected[name] = true;
    };
    LegendModel2.prototype.unSelect = function(name) {
      if (this.get("selectedMode") !== "single") {
        this.option.selected[name] = false;
      }
    };
    LegendModel2.prototype.toggleSelected = function(name) {
      var selected = this.option.selected;
      if (!selected.hasOwnProperty(name)) {
        selected[name] = true;
      }
      this[selected[name] ? "unSelect" : "select"](name);
    };
    LegendModel2.prototype.allSelect = function() {
      var data = this._data;
      var selected = this.option.selected;
      each(data, function(dataItem) {
        selected[dataItem.get("name", true)] = true;
      });
    };
    LegendModel2.prototype.inverseSelect = function() {
      var data = this._data;
      var selected = this.option.selected;
      each(data, function(dataItem) {
        var name = dataItem.get("name", true);
        if (!selected.hasOwnProperty(name)) {
          selected[name] = true;
        }
        selected[name] = !selected[name];
      });
    };
    LegendModel2.prototype.isSelected = function(name) {
      var selected = this.option.selected;
      return !(selected.hasOwnProperty(name) && !selected[name]) && indexOf(this._availableNames, name) >= 0;
    };
    LegendModel2.prototype.getOrient = function() {
      return this.get("orient") === "vertical" ? {
        index: 1,
        name: "vertical"
      } : {
        index: 0,
        name: "horizontal"
      };
    };
    LegendModel2.type = "legend.plain";
    LegendModel2.dependencies = ["series"];
    LegendModel2.defaultOption = {
      // zlevel: 0,
      z: 4,
      show: true,
      orient: "horizontal",
      left: "center",
      // right: 'center',
      // top: 0,
      bottom: tokens_default.size.m,
      align: "auto",
      backgroundColor: tokens_default.color.transparent,
      borderColor: tokens_default.color.border,
      borderRadius: 0,
      borderWidth: 0,
      padding: 5,
      itemGap: 8,
      itemWidth: 25,
      itemHeight: 14,
      symbolRotate: "inherit",
      symbolKeepAspect: true,
      inactiveColor: tokens_default.color.disabled,
      inactiveBorderColor: tokens_default.color.disabled,
      inactiveBorderWidth: "auto",
      itemStyle: {
        color: "inherit",
        opacity: "inherit",
        borderColor: "inherit",
        borderWidth: "auto",
        borderCap: "inherit",
        borderJoin: "inherit",
        borderDashOffset: "inherit",
        borderMiterLimit: "inherit"
      },
      lineStyle: {
        width: "auto",
        color: "inherit",
        inactiveColor: tokens_default.color.disabled,
        inactiveWidth: 2,
        opacity: "inherit",
        type: "inherit",
        cap: "inherit",
        join: "inherit",
        dashOffset: "inherit",
        miterLimit: "inherit"
      },
      textStyle: {
        color: tokens_default.color.secondary
      },
      selectedMode: true,
      selector: false,
      selectorLabel: {
        show: true,
        borderRadius: 10,
        padding: [3, 5, 3, 5],
        fontSize: 12,
        fontFamily: "sans-serif",
        color: tokens_default.color.tertiary,
        borderWidth: 1,
        borderColor: tokens_default.color.border
      },
      emphasis: {
        selectorLabel: {
          show: true,
          color: tokens_default.color.quaternary
        }
      },
      selectorPosition: "auto",
      selectorItemGap: 7,
      selectorButtonGap: 10,
      tooltip: {
        show: false
      },
      triggerEvent: false
    };
    return LegendModel2;
  })(Component_default)
);
var LegendModel_default = LegendModel;

// node_modules/echarts/lib/component/legend/LegendView.js
var curry2 = curry;
var each9 = each;
var Group3 = Group_default;
var LegendView = (
  /** @class */
  (function(_super) {
    __extends(LegendView2, _super);
    function LegendView2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = LegendView2.type;
      _this.newlineDisabled = false;
      return _this;
    }
    LegendView2.prototype.init = function() {
      this.group.add(this._contentGroup = new Group3());
      this.group.add(this._selectorGroup = new Group3());
      this._isFirstRender = true;
    };
    LegendView2.prototype.getContentGroup = function() {
      return this._contentGroup;
    };
    LegendView2.prototype.getSelectorGroup = function() {
      return this._selectorGroup;
    };
    LegendView2.prototype.render = function(legendModel, ecModel, api) {
      var isFirstRender = this._isFirstRender;
      this._isFirstRender = false;
      this.resetInner();
      if (!legendModel.get("show", true)) {
        return;
      }
      var itemAlign = legendModel.get("align");
      var orient = legendModel.get("orient");
      if (!itemAlign || itemAlign === "auto") {
        itemAlign = legendModel.get("left") === "right" && orient === "vertical" ? "right" : "left";
      }
      var selector2 = legendModel.get("selector", true);
      var selectorPosition = legendModel.get("selectorPosition", true);
      if (selector2 && (!selectorPosition || selectorPosition === "auto")) {
        selectorPosition = orient === "horizontal" ? "end" : "start";
      }
      this.renderInner(itemAlign, legendModel, ecModel, api, selector2, orient, selectorPosition);
      var refContainer = createBoxLayoutReference(legendModel, api).refContainer;
      var positionInfo = legendModel.getBoxLayoutParams();
      var padding = legendModel.get("padding");
      var maxSize = getLayoutRect(positionInfo, refContainer, padding);
      var mainRect = this.layoutInner(legendModel, itemAlign, maxSize, isFirstRender, selector2, selectorPosition);
      var layoutRect = getLayoutRect(defaults({
        width: mainRect.width,
        height: mainRect.height
      }, positionInfo), refContainer, padding);
      this.group.x = layoutRect.x - mainRect.x;
      this.group.y = layoutRect.y - mainRect.y;
      this.group.markRedraw();
      this.group.add(this._backgroundEl = makeBackground(
        mainRect,
        // FXIME: most itemStyle options does not work in background because inherit is not handled yet.
        legendModel
      ));
    };
    LegendView2.prototype.resetInner = function() {
      this.getContentGroup().removeAll();
      this._backgroundEl && this.group.remove(this._backgroundEl);
      this.getSelectorGroup().removeAll();
    };
    LegendView2.prototype.renderInner = function(itemAlign, legendModel, ecModel, api, selector2, orient, selectorPosition) {
      var contentGroup = this.getContentGroup();
      var legendDrawnMap = createHashMap();
      var selectMode = legendModel.get("selectedMode");
      var triggerEvent = legendModel.get("triggerEvent");
      var excludeSeriesId = [];
      ecModel.eachRawSeries(function(seriesModel) {
        !seriesModel.get("legendHoverLink") && excludeSeriesId.push(seriesModel.id);
      });
      each9(legendModel.getData(), function(legendItemModel, dataIndex) {
        var _this = this;
        var name = legendItemModel.get("name");
        if (!this.newlineDisabled && (name === "" || name === "\n")) {
          var g = new Group3();
          g.newline = true;
          contentGroup.add(g);
          return;
        }
        var seriesModel = ecModel.getSeriesByName(name)[0];
        if (legendDrawnMap.get(name)) {
          return;
        }
        if (seriesModel) {
          var data = seriesModel.getData();
          var lineVisualStyle = data.getVisual("legendLineStyle") || {};
          var legendIcon = data.getVisual("legendIcon");
          var style2 = data.getVisual("style");
          var itemGroup = this._createItem(seriesModel, name, dataIndex, legendItemModel, legendModel, itemAlign, lineVisualStyle, style2, legendIcon, selectMode, api);
          itemGroup.on("click", curry2(dispatchSelectAction, name, null, api, excludeSeriesId)).on("mouseover", curry2(dispatchHighlightAction, seriesModel.name, null, api, excludeSeriesId)).on("mouseout", curry2(dispatchDownplayAction, seriesModel.name, null, api, excludeSeriesId));
          if (ecModel.ssr) {
            itemGroup.eachChild(function(child) {
              var ecData = getECData(child);
              ecData.seriesIndex = seriesModel.seriesIndex;
              ecData.dataIndex = dataIndex;
              ecData.ssrType = "legend";
            });
          }
          if (triggerEvent) {
            itemGroup.eachChild(function(child) {
              _this.packEventData(child, legendModel, seriesModel, dataIndex, name);
            });
          }
          legendDrawnMap.set(name, true);
        } else {
          ecModel.eachRawSeries(function(seriesModel2) {
            var _this2 = this;
            if (legendDrawnMap.get(name)) {
              return;
            }
            if (seriesModel2.legendVisualProvider) {
              var provider = seriesModel2.legendVisualProvider;
              if (!provider.containName(name)) {
                return;
              }
              var idx = provider.indexOfName(name);
              var style3 = provider.getItemVisual(idx, "style");
              var legendIcon2 = provider.getItemVisual(idx, "legendIcon");
              var colorArr = parse(style3.fill);
              if (colorArr && colorArr[3] === 0) {
                colorArr[3] = 0.2;
                style3 = extend(extend({}, style3), {
                  fill: stringify(colorArr, "rgba")
                });
              }
              var itemGroup2 = this._createItem(seriesModel2, name, dataIndex, legendItemModel, legendModel, itemAlign, {}, style3, legendIcon2, selectMode, api);
              itemGroup2.on("click", curry2(dispatchSelectAction, null, name, api, excludeSeriesId)).on("mouseover", curry2(dispatchHighlightAction, null, name, api, excludeSeriesId)).on("mouseout", curry2(dispatchDownplayAction, null, name, api, excludeSeriesId));
              if (ecModel.ssr) {
                itemGroup2.eachChild(function(child) {
                  var ecData = getECData(child);
                  ecData.seriesIndex = seriesModel2.seriesIndex;
                  ecData.dataIndex = dataIndex;
                  ecData.ssrType = "legend";
                });
              }
              if (triggerEvent) {
                itemGroup2.eachChild(function(child) {
                  _this2.packEventData(child, legendModel, seriesModel2, dataIndex, name);
                });
              }
              legendDrawnMap.set(name, true);
            }
          }, this);
        }
        if (true) {
          if (!legendDrawnMap.get(name)) {
            console.warn(name + " series not exists. Legend data should be same with series name or data name.");
          }
        }
      }, this);
      if (selector2) {
        this._createSelector(selector2, legendModel, api, orient, selectorPosition);
      }
    };
    LegendView2.prototype.packEventData = function(el, legendModel, seriesModel, dataIndex, name) {
      var eventData = {
        componentType: "legend",
        componentIndex: legendModel.componentIndex,
        dataIndex,
        value: name,
        seriesIndex: seriesModel.seriesIndex
      };
      getECData(el).eventData = eventData;
    };
    ;
    LegendView2.prototype._createSelector = function(selector2, legendModel, api, orient, selectorPosition) {
      var selectorGroup = this.getSelectorGroup();
      each9(selector2, function createSelectorButton(selectorItem) {
        var type = selectorItem.type;
        var labelText = new Text_default({
          style: {
            x: 0,
            y: 0,
            align: "center",
            verticalAlign: "middle"
          },
          onclick: function() {
            api.dispatchAction({
              type: type === "all" ? "legendAllSelect" : "legendInverseSelect",
              legendId: legendModel.id
            });
          }
        });
        selectorGroup.add(labelText);
        var labelModel = legendModel.getModel("selectorLabel");
        var emphasisLabelModel = legendModel.getModel(["emphasis", "selectorLabel"]);
        setLabelStyle(labelText, {
          normal: labelModel,
          emphasis: emphasisLabelModel
        }, {
          defaultText: selectorItem.title
        });
        enableHoverEmphasis(labelText);
      });
    };
    LegendView2.prototype._createItem = function(seriesModel, name, dataIndex, legendItemModel, legendModel, itemAlign, lineVisualStyle, itemVisualStyle, legendIcon, selectMode, api) {
      var drawType = seriesModel.visualDrawType;
      var itemWidth = legendModel.get("itemWidth");
      var itemHeight = legendModel.get("itemHeight");
      var isSelected = legendModel.isSelected(name);
      var iconRotate = legendItemModel.get("symbolRotate");
      var symbolKeepAspect = legendItemModel.get("symbolKeepAspect");
      var legendIconType = legendItemModel.get("icon");
      legendIcon = legendIconType || legendIcon || "roundRect";
      var style2 = getLegendStyle(legendIcon, legendItemModel, lineVisualStyle, itemVisualStyle, drawType, isSelected, api);
      var itemGroup = new Group3();
      var textStyleModel = legendItemModel.getModel("textStyle");
      if (isFunction(seriesModel.getLegendIcon) && (!legendIconType || legendIconType === "inherit")) {
        itemGroup.add(seriesModel.getLegendIcon({
          itemWidth,
          itemHeight,
          icon: legendIcon,
          iconRotate,
          itemStyle: style2.itemStyle,
          lineStyle: style2.lineStyle,
          symbolKeepAspect
        }));
      } else {
        var rotate2 = legendIconType === "inherit" && seriesModel.getData().getVisual("symbol") ? iconRotate === "inherit" ? seriesModel.getData().getVisual("symbolRotate") : iconRotate : 0;
        itemGroup.add(getDefaultLegendIcon({
          itemWidth,
          itemHeight,
          icon: legendIcon,
          iconRotate: rotate2,
          itemStyle: style2.itemStyle,
          lineStyle: style2.lineStyle,
          symbolKeepAspect
        }));
      }
      var textX = itemAlign === "left" ? itemWidth + 5 : -5;
      var textAlign = itemAlign;
      var formatter = legendModel.get("formatter");
      var content = name;
      if (isString(formatter) && formatter) {
        content = formatter.replace("{name}", name != null ? name : "");
      } else if (isFunction(formatter)) {
        content = formatter(name);
      }
      var textColor = isSelected ? textStyleModel.getTextColor() : legendItemModel.get("inactiveColor");
      itemGroup.add(new Text_default({
        style: createTextStyle(textStyleModel, {
          text: content,
          x: textX,
          y: itemHeight / 2,
          fill: textColor,
          align: textAlign,
          verticalAlign: "middle"
        }, {
          inheritColor: textColor
        })
      }));
      var hitRect = new Rect_default({
        shape: itemGroup.getBoundingRect(),
        style: {
          // Cannot use 'invisible' because SVG SSR will miss the node
          fill: "transparent"
        }
      });
      var tooltipModel = legendItemModel.getModel("tooltip");
      if (tooltipModel.get("show")) {
        setTooltipConfig({
          el: hitRect,
          componentModel: legendModel,
          itemName: name,
          itemTooltipOption: tooltipModel.option
        });
      }
      itemGroup.add(hitRect);
      itemGroup.eachChild(function(child) {
        child.silent = true;
      });
      hitRect.silent = !selectMode;
      this.getContentGroup().add(itemGroup);
      enableHoverEmphasis(itemGroup);
      itemGroup.__legendDataIndex = dataIndex;
      return itemGroup;
    };
    LegendView2.prototype.layoutInner = function(legendModel, itemAlign, maxSize, isFirstRender, selector2, selectorPosition) {
      var contentGroup = this.getContentGroup();
      var selectorGroup = this.getSelectorGroup();
      box(legendModel.get("orient"), contentGroup, legendModel.get("itemGap"), maxSize.width, maxSize.height);
      var contentRect = contentGroup.getBoundingRect();
      var contentPos = [-contentRect.x, -contentRect.y];
      selectorGroup.markRedraw();
      contentGroup.markRedraw();
      if (selector2) {
        box(
          // Buttons in selectorGroup always layout horizontally
          "horizontal",
          selectorGroup,
          legendModel.get("selectorItemGap", true)
        );
        var selectorRect = selectorGroup.getBoundingRect();
        var selectorPos = [-selectorRect.x, -selectorRect.y];
        var selectorButtonGap = legendModel.get("selectorButtonGap", true);
        var orientIdx = legendModel.getOrient().index;
        var wh = orientIdx === 0 ? "width" : "height";
        var hw = orientIdx === 0 ? "height" : "width";
        var yx = orientIdx === 0 ? "y" : "x";
        if (selectorPosition === "end") {
          selectorPos[orientIdx] += contentRect[wh] + selectorButtonGap;
        } else {
          contentPos[orientIdx] += selectorRect[wh] + selectorButtonGap;
        }
        selectorPos[1 - orientIdx] += contentRect[hw] / 2 - selectorRect[hw] / 2;
        selectorGroup.x = selectorPos[0];
        selectorGroup.y = selectorPos[1];
        contentGroup.x = contentPos[0];
        contentGroup.y = contentPos[1];
        var mainRect = {
          x: 0,
          y: 0
        };
        mainRect[wh] = contentRect[wh] + selectorButtonGap + selectorRect[wh];
        mainRect[hw] = Math.max(contentRect[hw], selectorRect[hw]);
        mainRect[yx] = Math.min(0, selectorRect[yx] + selectorPos[1 - orientIdx]);
        return mainRect;
      } else {
        contentGroup.x = contentPos[0];
        contentGroup.y = contentPos[1];
        return this.group.getBoundingRect();
      }
    };
    LegendView2.prototype.remove = function() {
      this.getContentGroup().removeAll();
      this._isFirstRender = true;
    };
    LegendView2.type = "legend.plain";
    return LegendView2;
  })(Component_default2)
);
function getLegendStyle(iconType, legendItemModel, lineVisualStyle, itemVisualStyle, drawType, isSelected, api) {
  function handleCommonProps(style2, visualStyle) {
    if (style2.lineWidth === "auto") {
      style2.lineWidth = visualStyle.lineWidth > 0 ? 2 : 0;
    }
    each9(style2, function(propVal, propName) {
      style2[propName] === "inherit" && (style2[propName] = visualStyle[propName]);
    });
  }
  var itemStyleModel = legendItemModel.getModel("itemStyle");
  var itemStyle = itemStyleModel.getItemStyle();
  var iconBrushType = iconType.lastIndexOf("empty", 0) === 0 ? "fill" : "stroke";
  var decalStyle = itemStyleModel.getShallow("decal");
  itemStyle.decal = !decalStyle || decalStyle === "inherit" ? itemVisualStyle.decal : createOrUpdatePatternFromDecal(decalStyle, api);
  if (itemStyle.fill === "inherit") {
    itemStyle.fill = itemVisualStyle[drawType];
  }
  if (itemStyle.stroke === "inherit") {
    itemStyle.stroke = itemVisualStyle[iconBrushType];
  }
  if (itemStyle.opacity === "inherit") {
    itemStyle.opacity = (drawType === "fill" ? itemVisualStyle : lineVisualStyle).opacity;
  }
  handleCommonProps(itemStyle, itemVisualStyle);
  var legendLineModel = legendItemModel.getModel("lineStyle");
  var lineStyle = legendLineModel.getLineStyle();
  handleCommonProps(lineStyle, lineVisualStyle);
  itemStyle.fill === "auto" && (itemStyle.fill = itemVisualStyle.fill);
  itemStyle.stroke === "auto" && (itemStyle.stroke = itemVisualStyle.fill);
  lineStyle.stroke === "auto" && (lineStyle.stroke = itemVisualStyle.fill);
  if (!isSelected) {
    var borderWidth = legendItemModel.get("inactiveBorderWidth");
    var visualHasBorder = itemStyle[iconBrushType];
    itemStyle.lineWidth = borderWidth === "auto" ? itemVisualStyle.lineWidth > 0 && visualHasBorder ? 2 : 0 : itemStyle.lineWidth;
    itemStyle.fill = legendItemModel.get("inactiveColor");
    itemStyle.stroke = legendItemModel.get("inactiveBorderColor");
    lineStyle.stroke = legendLineModel.get("inactiveColor");
    lineStyle.lineWidth = legendLineModel.get("inactiveWidth");
  }
  return {
    itemStyle,
    lineStyle
  };
}
function getDefaultLegendIcon(opt) {
  var symboType = opt.icon || "roundRect";
  var icon = createSymbol(symboType, 0, 0, opt.itemWidth, opt.itemHeight, opt.itemStyle.fill, opt.symbolKeepAspect);
  icon.setStyle(opt.itemStyle);
  icon.rotation = (opt.iconRotate || 0) * Math.PI / 180;
  icon.setOrigin([opt.itemWidth / 2, opt.itemHeight / 2]);
  if (symboType.indexOf("empty") > -1) {
    icon.style.stroke = icon.style.fill;
    icon.style.fill = tokens_default.color.neutral00;
    icon.style.lineWidth = 2;
  }
  return icon;
}
function dispatchSelectAction(seriesName, dataName, api, excludeSeriesId) {
  dispatchDownplayAction(seriesName, dataName, api, excludeSeriesId);
  api.dispatchAction({
    type: "legendToggleSelect",
    name: seriesName != null ? seriesName : dataName
  });
  dispatchHighlightAction(seriesName, dataName, api, excludeSeriesId);
}
function dispatchHighlightAction(seriesName, dataName, api, excludeSeriesId) {
  if (!api.usingTHL()) {
    api.dispatchAction({
      type: "highlight",
      seriesName,
      name: dataName,
      excludeSeriesId
    });
  }
}
function dispatchDownplayAction(seriesName, dataName, api, excludeSeriesId) {
  if (!api.usingTHL()) {
    api.dispatchAction({
      type: "downplay",
      seriesName,
      name: dataName,
      excludeSeriesId
    });
  }
}
var LegendView_default = LegendView;

// node_modules/echarts/lib/component/legend/legendAction.js
function legendSelectActionHandler(methodName, payload, ecModel) {
  var isAllSelect = methodName === "allSelect" || methodName === "inverseSelect";
  var selectedMap = {};
  var actionLegendIndices = [];
  ecModel.eachComponent({
    mainType: "legend",
    query: payload
  }, function(legendModel) {
    if (isAllSelect) {
      legendModel[methodName]();
    } else {
      legendModel[methodName](payload.name);
    }
    makeSelectedMap(legendModel, selectedMap);
    actionLegendIndices.push(legendModel.componentIndex);
  });
  var allSelectedMap = {};
  ecModel.eachComponent("legend", function(legendModel) {
    each(selectedMap, function(isSelected, name) {
      legendModel[isSelected ? "select" : "unSelect"](name);
    });
    makeSelectedMap(legendModel, allSelectedMap);
  });
  return isAllSelect ? {
    selected: allSelectedMap,
    // return legendIndex array to tell the developers which legends are allSelect / inverseSelect
    legendIndex: actionLegendIndices
  } : {
    name: payload.name,
    selected: allSelectedMap
  };
}
function makeSelectedMap(legendModel, out2) {
  var selectedMap = out2 || {};
  each(legendModel.getData(), function(model2) {
    var name = model2.get("name");
    if (name === "\n" || name === "") {
      return;
    }
    var isItemSelected = legendModel.isSelected(name);
    if (hasOwn(selectedMap, name)) {
      selectedMap[name] = selectedMap[name] && isItemSelected;
    } else {
      selectedMap[name] = isItemSelected;
    }
  });
  return selectedMap;
}
function installLegendAction(registers) {
  registers.registerAction("legendToggleSelect", "legendselectchanged", curry(legendSelectActionHandler, "toggleSelected"));
  registers.registerAction("legendAllSelect", "legendselectall", curry(legendSelectActionHandler, "allSelect"));
  registers.registerAction("legendInverseSelect", "legendinverseselect", curry(legendSelectActionHandler, "inverseSelect"));
  registers.registerAction("legendSelect", "legendselected", curry(legendSelectActionHandler, "select"));
  registers.registerAction("legendUnSelect", "legendunselected", curry(legendSelectActionHandler, "unSelect"));
}

// node_modules/echarts/lib/component/legend/legendFilter.js
var legendFilterStageHandler = createSimpleOverallStageHandler2(legendFilter);
function legendFilter(ecModel) {
  var legendModels = ecModel.findComponents({
    mainType: "legend"
  });
  if (legendModels && legendModels.length) {
    ecModel.filterSeries(function(series) {
      for (var i2 = 0; i2 < legendModels.length; i2++) {
        if (!legendModels[i2].isSelected(series.name)) {
          return false;
        }
      }
      return true;
    });
  }
}

// node_modules/echarts/lib/component/legend/installLegendPlain.js
function install36(registers) {
  registers.registerComponentModel(LegendModel_default);
  registers.registerComponentView(LegendView_default);
  registers.registerProcessor(registers.PRIORITY.PROCESSOR.SERIES_FILTER, legendFilterStageHandler);
  registers.registerSubTypeDefaulter("legend", function() {
    return "plain";
  });
  installLegendAction(registers);
}

// node_modules/echarts/lib/component/legend/ScrollableLegendModel.js
var ScrollableLegendModel = (
  /** @class */
  (function(_super) {
    __extends(ScrollableLegendModel2, _super);
    function ScrollableLegendModel2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = ScrollableLegendModel2.type;
      return _this;
    }
    ScrollableLegendModel2.prototype.setScrollDataIndex = function(scrollDataIndex) {
      this.option.scrollDataIndex = scrollDataIndex;
    };
    ScrollableLegendModel2.prototype.init = function(option, parentModel, ecModel) {
      var inputPositionParams = getLayoutParams(option);
      _super.prototype.init.call(this, option, parentModel, ecModel);
      mergeAndNormalizeLayoutParams2(this, option, inputPositionParams);
    };
    ScrollableLegendModel2.prototype.mergeOption = function(option, ecModel) {
      _super.prototype.mergeOption.call(this, option, ecModel);
      mergeAndNormalizeLayoutParams2(this, this.option, option);
    };
    ScrollableLegendModel2.type = "legend.scroll";
    ScrollableLegendModel2.defaultOption = inheritDefaultOption(LegendModel_default.defaultOption, {
      scrollDataIndex: 0,
      pageButtonItemGap: 5,
      pageButtonGap: null,
      pageButtonPosition: "end",
      pageFormatter: "{current}/{total}",
      pageIcons: {
        horizontal: ["M0,0L12,-10L12,10z", "M0,0L-12,-10L-12,10z"],
        vertical: ["M0,0L20,0L10,-20z", "M0,0L20,0L10,20z"]
      },
      pageIconColor: tokens_default.color.accent50,
      pageIconInactiveColor: tokens_default.color.accent10,
      pageIconSize: 15,
      pageTextStyle: {
        color: tokens_default.color.tertiary
      },
      animationDurationUpdate: 800
    });
    return ScrollableLegendModel2;
  })(LegendModel_default)
);
function mergeAndNormalizeLayoutParams2(legendModel, target, raw) {
  var orient = legendModel.getOrient();
  var ignoreSize = [1, 1];
  ignoreSize[orient.index] = 0;
  mergeLayoutParam(target, raw, {
    type: "box",
    ignoreSize: !!ignoreSize
  });
}
var ScrollableLegendModel_default = ScrollableLegendModel;

// node_modules/echarts/lib/component/legend/ScrollableLegendView.js
var Group4 = Group_default;
var WH4 = ["width", "height"];
var XY4 = ["x", "y"];
var ScrollableLegendView = (
  /** @class */
  (function(_super) {
    __extends(ScrollableLegendView2, _super);
    function ScrollableLegendView2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = ScrollableLegendView2.type;
      _this.newlineDisabled = true;
      _this._currentIndex = 0;
      return _this;
    }
    ScrollableLegendView2.prototype.init = function() {
      _super.prototype.init.call(this);
      this.group.add(this._containerGroup = new Group4());
      this._containerGroup.add(this.getContentGroup());
      this.group.add(this._controllerGroup = new Group4());
    };
    ScrollableLegendView2.prototype.resetInner = function() {
      _super.prototype.resetInner.call(this);
      this._controllerGroup.removeAll();
      this._containerGroup.removeClipPath();
      this._containerGroup.__rectSize = null;
    };
    ScrollableLegendView2.prototype.renderInner = function(itemAlign, legendModel, ecModel, api, selector2, orient, selectorPosition) {
      var self2 = this;
      _super.prototype.renderInner.call(this, itemAlign, legendModel, ecModel, api, selector2, orient, selectorPosition);
      var controllerGroup = this._controllerGroup;
      var pageIconSize = legendModel.get("pageIconSize", true);
      var pageIconSizeArr = isArray(pageIconSize) ? pageIconSize : [pageIconSize, pageIconSize];
      createPageButton("pagePrev", 0);
      var pageTextStyleModel = legendModel.getModel("pageTextStyle");
      controllerGroup.add(new Text_default({
        name: "pageText",
        style: {
          // Placeholder to calculate a proper layout.
          text: "xx/xx",
          fill: pageTextStyleModel.getTextColor(),
          font: pageTextStyleModel.getFont(),
          verticalAlign: "middle",
          align: "center"
        },
        silent: true
      }));
      createPageButton("pageNext", 1);
      function createPageButton(name, iconIdx) {
        var pageDataIndexName = name + "DataIndex";
        var icon = createIcon(legendModel.get("pageIcons", true)[legendModel.getOrient().name][iconIdx], {
          // Buttons will be created in each render, so we do not need
          // to worry about avoiding using legendModel kept in scope.
          onclick: bind(self2._pageGo, self2, pageDataIndexName, legendModel, api)
        }, {
          x: -pageIconSizeArr[0] / 2,
          y: -pageIconSizeArr[1] / 2,
          width: pageIconSizeArr[0],
          height: pageIconSizeArr[1]
        });
        icon.name = name;
        controllerGroup.add(icon);
      }
    };
    ScrollableLegendView2.prototype.layoutInner = function(legendModel, itemAlign, maxSize, isFirstRender, selector2, selectorPosition) {
      var selectorGroup = this.getSelectorGroup();
      var orientIdx = legendModel.getOrient().index;
      var wh = WH4[orientIdx];
      var xy = XY4[orientIdx];
      var hw = WH4[1 - orientIdx];
      var yx = XY4[1 - orientIdx];
      selector2 && box(
        // Buttons in selectorGroup always layout horizontally
        "horizontal",
        selectorGroup,
        legendModel.get("selectorItemGap", true)
      );
      var selectorButtonGap = legendModel.get("selectorButtonGap", true);
      var selectorRect = selectorGroup.getBoundingRect();
      var selectorPos = [-selectorRect.x, -selectorRect.y];
      var processMaxSize = clone(maxSize);
      selector2 && (processMaxSize[wh] = maxSize[wh] - selectorRect[wh] - selectorButtonGap);
      var mainRect = this._layoutContentAndController(legendModel, isFirstRender, processMaxSize, orientIdx, wh, hw, yx, xy);
      if (selector2) {
        if (selectorPosition === "end") {
          selectorPos[orientIdx] += mainRect[wh] + selectorButtonGap;
        } else {
          var offset = selectorRect[wh] + selectorButtonGap;
          selectorPos[orientIdx] -= offset;
          mainRect[xy] -= offset;
        }
        mainRect[wh] += selectorRect[wh] + selectorButtonGap;
        selectorPos[1 - orientIdx] += mainRect[yx] + mainRect[hw] / 2 - selectorRect[hw] / 2;
        mainRect[hw] = Math.max(mainRect[hw], selectorRect[hw]);
        mainRect[yx] = Math.min(mainRect[yx], selectorRect[yx] + selectorPos[1 - orientIdx]);
        selectorGroup.x = selectorPos[0];
        selectorGroup.y = selectorPos[1];
        selectorGroup.markRedraw();
      }
      return mainRect;
    };
    ScrollableLegendView2.prototype._layoutContentAndController = function(legendModel, isFirstRender, maxSize, orientIdx, wh, hw, yx, xy) {
      var contentGroup = this.getContentGroup();
      var containerGroup = this._containerGroup;
      var controllerGroup = this._controllerGroup;
      box(legendModel.get("orient"), contentGroup, legendModel.get("itemGap"), !orientIdx ? null : maxSize.width, orientIdx ? null : maxSize.height);
      box(
        // Buttons in controller are layout always horizontally.
        "horizontal",
        controllerGroup,
        legendModel.get("pageButtonItemGap", true)
      );
      var contentRect = contentGroup.getBoundingRect();
      var controllerRect = controllerGroup.getBoundingRect();
      var showController = this._showController = contentRect[wh] > maxSize[wh];
      var contentPos = [-contentRect.x, -contentRect.y];
      if (!isFirstRender) {
        contentPos[orientIdx] = contentGroup[xy];
      }
      var containerPos = [0, 0];
      var controllerPos = [-controllerRect.x, -controllerRect.y];
      var pageButtonGap = retrieve2(legendModel.get("pageButtonGap", true), legendModel.get("itemGap", true));
      if (showController) {
        var pageButtonPosition = legendModel.get("pageButtonPosition", true);
        if (pageButtonPosition === "end") {
          controllerPos[orientIdx] += maxSize[wh] - controllerRect[wh];
        } else {
          containerPos[orientIdx] += controllerRect[wh] + pageButtonGap;
        }
      }
      controllerPos[1 - orientIdx] += contentRect[hw] / 2 - controllerRect[hw] / 2;
      contentGroup.setPosition(contentPos);
      containerGroup.setPosition(containerPos);
      controllerGroup.setPosition(controllerPos);
      var mainRect = {
        x: 0,
        y: 0
      };
      mainRect[wh] = showController ? maxSize[wh] : contentRect[wh];
      mainRect[hw] = Math.max(contentRect[hw], controllerRect[hw]);
      mainRect[yx] = Math.min(0, controllerRect[yx] + controllerPos[1 - orientIdx]);
      containerGroup.__rectSize = maxSize[wh];
      if (showController) {
        var clipShape = {
          x: 0,
          y: 0
        };
        clipShape[wh] = Math.max(maxSize[wh] - controllerRect[wh] - pageButtonGap, 0);
        clipShape[hw] = mainRect[hw];
        containerGroup.setClipPath(new Rect_default({
          shape: clipShape
        }));
        containerGroup.__rectSize = clipShape[wh];
      } else {
        controllerGroup.eachChild(function(child) {
          child.attr({
            invisible: true,
            silent: true
          });
        });
      }
      var pageInfo = this._getPageInfo(legendModel);
      pageInfo.pageIndex != null && updateProps(
        contentGroup,
        {
          x: pageInfo.contentPosition[0],
          y: pageInfo.contentPosition[1]
        },
        // When switch from "show controller" to "not show controller", view should be
        // updated immediately without animation, otherwise causes weird effect.
        showController ? legendModel : null
      );
      this._updatePageInfoView(legendModel, pageInfo);
      return mainRect;
    };
    ScrollableLegendView2.prototype._pageGo = function(to, legendModel, api) {
      var scrollDataIndex = this._getPageInfo(legendModel)[to];
      scrollDataIndex != null && api.dispatchAction({
        type: "legendScroll",
        scrollDataIndex,
        legendId: legendModel.id
      });
    };
    ScrollableLegendView2.prototype._updatePageInfoView = function(legendModel, pageInfo) {
      var controllerGroup = this._controllerGroup;
      each(["pagePrev", "pageNext"], function(name) {
        var key2 = name + "DataIndex";
        var canJump = pageInfo[key2] != null;
        var icon = controllerGroup.childOfName(name);
        if (icon) {
          icon.setStyle("fill", canJump ? legendModel.get("pageIconColor", true) : legendModel.get("pageIconInactiveColor", true));
          icon.cursor = canJump ? "pointer" : "default";
        }
      });
      var pageText = controllerGroup.childOfName("pageText");
      var pageFormatter = legendModel.get("pageFormatter");
      var pageIndex = pageInfo.pageIndex;
      var current = pageIndex != null ? pageIndex + 1 : 0;
      var total = pageInfo.pageCount;
      pageText && pageFormatter && pageText.setStyle("text", isString(pageFormatter) ? pageFormatter.replace("{current}", current == null ? "" : current + "").replace("{total}", total == null ? "" : total + "") : pageFormatter({
        current,
        total
      }));
    };
    ScrollableLegendView2.prototype._getPageInfo = function(legendModel) {
      var scrollDataIndex = legendModel.get("scrollDataIndex", true);
      var contentGroup = this.getContentGroup();
      var containerRectSize = this._containerGroup.__rectSize;
      var orientIdx = legendModel.getOrient().index;
      var wh = WH4[orientIdx];
      var xy = XY4[orientIdx];
      var targetItemIndex = this._findTargetItemIndex(scrollDataIndex);
      var children = contentGroup.children();
      var targetItem = children[targetItemIndex];
      var itemCount = children.length;
      var pCount = !itemCount ? 0 : 1;
      var result = {
        contentPosition: [contentGroup.x, contentGroup.y],
        pageCount: pCount,
        pageIndex: pCount - 1,
        pagePrevDataIndex: null,
        pageNextDataIndex: null
      };
      if (!targetItem) {
        return result;
      }
      var targetItemInfo = getItemInfo(targetItem);
      result.contentPosition[orientIdx] = -targetItemInfo.s;
      for (var i2 = targetItemIndex + 1, winStartItemInfo = targetItemInfo, winEndItemInfo = targetItemInfo, currItemInfo = null; i2 <= itemCount; ++i2) {
        currItemInfo = getItemInfo(children[i2]);
        if (
          // Half of the last item is out of the window.
          !currItemInfo && winEndItemInfo.e > winStartItemInfo.s + containerRectSize || currItemInfo && !intersect2(currItemInfo, winStartItemInfo.s)
        ) {
          if (winEndItemInfo.i > winStartItemInfo.i) {
            winStartItemInfo = winEndItemInfo;
          } else {
            winStartItemInfo = currItemInfo;
          }
          if (winStartItemInfo) {
            if (result.pageNextDataIndex == null) {
              result.pageNextDataIndex = winStartItemInfo.i;
            }
            ++result.pageCount;
          }
        }
        winEndItemInfo = currItemInfo;
      }
      for (var i2 = targetItemIndex - 1, winStartItemInfo = targetItemInfo, winEndItemInfo = targetItemInfo, currItemInfo = null; i2 >= -1; --i2) {
        currItemInfo = getItemInfo(children[i2]);
        if (
          // If the the end item does not intersect with the window started
          // from the current item, a page can be settled.
          (!currItemInfo || !intersect2(winEndItemInfo, currItemInfo.s)) && winStartItemInfo.i < winEndItemInfo.i
        ) {
          winEndItemInfo = winStartItemInfo;
          if (result.pagePrevDataIndex == null) {
            result.pagePrevDataIndex = winStartItemInfo.i;
          }
          ++result.pageCount;
          ++result.pageIndex;
        }
        winStartItemInfo = currItemInfo;
      }
      return result;
      function getItemInfo(el) {
        if (el) {
          var itemRect = el.getBoundingRect();
          var start2 = itemRect[xy] + el[xy];
          return {
            s: start2,
            e: start2 + itemRect[wh],
            i: el.__legendDataIndex
          };
        }
      }
      function intersect2(itemInfo, winStart) {
        return itemInfo.e >= winStart && itemInfo.s <= winStart + containerRectSize;
      }
    };
    ScrollableLegendView2.prototype._findTargetItemIndex = function(targetDataIndex) {
      if (!this._showController) {
        return 0;
      }
      var index;
      var contentGroup = this.getContentGroup();
      var defaultIndex;
      contentGroup.eachChild(function(child, idx) {
        var legendDataIdx = child.__legendDataIndex;
        if (defaultIndex == null && legendDataIdx != null) {
          defaultIndex = idx;
        }
        if (legendDataIdx === targetDataIndex) {
          index = idx;
        }
      });
      return index != null ? index : defaultIndex;
    };
    ScrollableLegendView2.type = "legend.scroll";
    return ScrollableLegendView2;
  })(LegendView_default)
);
var ScrollableLegendView_default = ScrollableLegendView;

// node_modules/echarts/lib/component/legend/scrollableLegendAction.js
function installScrollableLegendAction(registers) {
  registers.registerAction("legendScroll", "legendscroll", function(payload, ecModel) {
    var scrollDataIndex = payload.scrollDataIndex;
    scrollDataIndex != null && ecModel.eachComponent({
      mainType: "legend",
      subType: "scroll",
      query: payload
    }, function(legendModel) {
      legendModel.setScrollDataIndex(scrollDataIndex);
    });
  });
}

// node_modules/echarts/lib/component/legend/installLegendScroll.js
function install37(registers) {
  use(install36);
  registers.registerComponentModel(ScrollableLegendModel_default);
  registers.registerComponentView(ScrollableLegendView_default);
  installScrollableLegendAction(registers);
}

// node_modules/echarts/lib/component/legend/install.js
function install38(registers) {
  use(install36);
  use(install37);
}

// node_modules/echarts/lib/component/dataZoom/InsideZoomModel.js
var InsideZoomModel = (
  /** @class */
  (function(_super) {
    __extends(InsideZoomModel2, _super);
    function InsideZoomModel2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = InsideZoomModel2.type;
      return _this;
    }
    InsideZoomModel2.type = "dataZoom.inside";
    InsideZoomModel2.defaultOption = inheritDefaultOption(DataZoomModel_default.defaultOption, {
      disabled: false,
      zoomLock: false,
      zoomOnMouseWheel: true,
      moveOnMouseMove: true,
      moveOnMouseWheel: false,
      preventDefaultMouseMove: true
    });
    return InsideZoomModel2;
  })(DataZoomModel_default)
);
var InsideZoomModel_default = InsideZoomModel;

// node_modules/echarts/lib/component/dataZoom/roams.js
var inner24 = makeInner();
function setViewInfoToCoordSysRecord(api, dataZoomModel, getRange) {
  inner24(api).coordSysRecordMap.each(function(coordSysRecord) {
    var dzInfo = coordSysRecord.dataZoomInfoMap.get(dataZoomModel.uid);
    if (dzInfo) {
      dzInfo.getRange = getRange;
    }
  });
}
function disposeCoordSysRecordIfNeeded(api, dataZoomModel) {
  var coordSysRecordMap = inner24(api).coordSysRecordMap;
  var coordSysKeyArr = coordSysRecordMap.keys();
  for (var i2 = 0; i2 < coordSysKeyArr.length; i2++) {
    var coordSysKey = coordSysKeyArr[i2];
    var coordSysRecord = coordSysRecordMap.get(coordSysKey);
    var dataZoomInfoMap = coordSysRecord.dataZoomInfoMap;
    if (dataZoomInfoMap) {
      var dzUid = dataZoomModel.uid;
      var dzInfo = dataZoomInfoMap.get(dzUid);
      if (dzInfo) {
        dataZoomInfoMap.removeKey(dzUid);
        if (!dataZoomInfoMap.keys().length) {
          disposeCoordSysRecord(coordSysRecordMap, coordSysRecord);
        }
      }
    }
  }
}
function disposeCoordSysRecord(coordSysRecordMap, coordSysRecord) {
  if (coordSysRecord) {
    coordSysRecordMap.removeKey(coordSysRecord.model.uid);
    var controller = coordSysRecord.controller;
    controller && controller.dispose();
  }
}
function createCoordSysRecord(api, coordSysModel) {
  var coordSysRecord = {
    model: coordSysModel,
    containsPoint: curry(containsPoint, coordSysModel),
    dispatchAction: curry(dispatchAction2, api),
    dataZoomInfoMap: null,
    controller: null
  };
  var controller = coordSysRecord.controller = new RoamController_default(api.getZr());
  each(["pan", "zoom", "scrollMove"], function(eventName) {
    controller.on(eventName, function(event) {
      var batch = [];
      coordSysRecord.dataZoomInfoMap.each(function(dzInfo) {
        if (!event.isAvailableBehavior(dzInfo.model.option)) {
          return;
        }
        var method = (dzInfo.getRange || {})[eventName];
        var range = method && method(dzInfo.dzReferCoordSysInfo, coordSysRecord.model.mainType, coordSysRecord.controller, event);
        !dzInfo.model.get("disabled", true) && range && batch.push({
          dataZoomId: dzInfo.model.id,
          start: range[0],
          end: range[1]
        });
      });
      batch.length && coordSysRecord.dispatchAction(batch);
    });
  });
  return coordSysRecord;
}
function dispatchAction2(api, batch) {
  if (!api.isDisposed()) {
    api.dispatchAction({
      type: "dataZoom",
      animation: {
        easing: "cubicOut",
        duration: 100
      },
      batch
    });
  }
}
function containsPoint(coordSysModel, e2, x, y) {
  return coordSysModel.coordinateSystem.containPoint([x, y]);
}
function mergeControllerParams(dataZoomInfoMap, coordSysRecord, api) {
  var controlType;
  var prefix = "type_";
  var typePriority = {
    "type_true": 2,
    "type_move": 1,
    "type_false": 0,
    "type_undefined": -1
  };
  var preventDefaultMouseMove = true;
  var cursorGrab;
  var cursorGrabbing;
  dataZoomInfoMap.each(function(dataZoomInfo) {
    var dataZoomModel = dataZoomInfo.model;
    var oneType = dataZoomModel.get("disabled", true) ? false : dataZoomModel.get("zoomLock", true) ? "move" : true;
    if (typePriority[prefix + oneType] > typePriority[prefix + controlType]) {
      controlType = oneType;
    }
    preventDefaultMouseMove = preventDefaultMouseMove && dataZoomModel.get("preventDefaultMouseMove", true);
    cursorGrab = retrieve2(dataZoomModel.get("cursorGrab", true), cursorGrab);
    cursorGrabbing = retrieve2(dataZoomModel.get("cursorGrabbing", true), cursorGrabbing);
  });
  return {
    controlType,
    opt: {
      // RoamController will enable all of these functionalities,
      // and the final behavior is determined by its event listener
      // provided by each inside zoom.
      zoomOnMouseWheel: true,
      moveOnMouseMove: true,
      moveOnMouseWheel: true,
      preventDefaultMouseMove: !!preventDefaultMouseMove,
      api,
      zInfo: {
        component: coordSysRecord.model
      },
      triggerInfo: {
        roamTrigger: null,
        isInSelf: coordSysRecord.containsPoint
      },
      cursorGrab,
      cursorGrabbing
    }
  };
}
function installDataZoomRoamProcessor(registers) {
  registers.registerUpdateLifecycle("coordsys:aftercreate", function(ecModel, api) {
    var apiInner = inner24(api);
    var coordSysRecordMap = apiInner.coordSysRecordMap || (apiInner.coordSysRecordMap = createHashMap());
    coordSysRecordMap.each(function(coordSysRecord) {
      coordSysRecord.dataZoomInfoMap = null;
    });
    ecModel.eachComponent({
      mainType: "dataZoom",
      subType: "inside"
    }, function(dataZoomModel) {
      var dzReferCoordSysWrap = collectReferCoordSysModelInfo(dataZoomModel);
      each(dzReferCoordSysWrap.infoList, function(dzCoordSysInfo) {
        var coordSysUid = dzCoordSysInfo.model.uid;
        var coordSysRecord = coordSysRecordMap.get(coordSysUid) || coordSysRecordMap.set(coordSysUid, createCoordSysRecord(api, dzCoordSysInfo.model));
        var dataZoomInfoMap = coordSysRecord.dataZoomInfoMap || (coordSysRecord.dataZoomInfoMap = createHashMap());
        dataZoomInfoMap.set(dataZoomModel.uid, {
          dzReferCoordSysInfo: dzCoordSysInfo,
          model: dataZoomModel,
          getRange: null
        });
      });
    });
    coordSysRecordMap.each(function(coordSysRecord) {
      var controller = coordSysRecord.controller;
      var firstDzInfo;
      var dataZoomInfoMap = coordSysRecord.dataZoomInfoMap;
      if (dataZoomInfoMap) {
        var firstDzKey = dataZoomInfoMap.keys()[0];
        if (firstDzKey != null) {
          firstDzInfo = dataZoomInfoMap.get(firstDzKey);
        }
      }
      if (!firstDzInfo) {
        disposeCoordSysRecord(coordSysRecordMap, coordSysRecord);
        return;
      }
      var controllerParams = mergeControllerParams(dataZoomInfoMap, coordSysRecord, api);
      controller.enable(controllerParams.controlType, controllerParams.opt);
      createOrUpdate(coordSysRecord, "dispatchAction", firstDzInfo.model.get("throttle", true), "fixRate");
    });
  });
}

// node_modules/echarts/lib/component/dataZoom/InsideZoomView.js
var InsideZoomView = (
  /** @class */
  (function(_super) {
    __extends(InsideZoomView2, _super);
    function InsideZoomView2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = "dataZoom.inside";
      return _this;
    }
    InsideZoomView2.prototype.render = function(dataZoomModel, ecModel, api) {
      _super.prototype.render.apply(this, arguments);
      if (dataZoomModel.noTarget()) {
        this._clear();
        return;
      }
      this.range = dataZoomModel.getPercentRange();
      setViewInfoToCoordSysRecord(api, dataZoomModel, {
        pan: bind(getRangeHandlers.pan, this),
        zoom: bind(getRangeHandlers.zoom, this),
        scrollMove: bind(getRangeHandlers.scrollMove, this)
      });
    };
    InsideZoomView2.prototype.dispose = function() {
      this._clear();
      _super.prototype.dispose.apply(this, arguments);
    };
    InsideZoomView2.prototype._clear = function() {
      disposeCoordSysRecordIfNeeded(this.api, this.dataZoomModel);
      this.range = null;
    };
    InsideZoomView2.type = "dataZoom.inside";
    return InsideZoomView2;
  })(DataZoomView_default)
);
var getRangeHandlers = {
  zoom: function(coordSysInfo, coordSysMainType, controller, e2) {
    var lastRange = this.range;
    var range = lastRange.slice();
    var axisModel = coordSysInfo.axisModels[0];
    if (!axisModel) {
      return;
    }
    var directionInfo = getDirectionInfo[coordSysMainType](null, [e2.originX, e2.originY], axisModel, controller, coordSysInfo);
    var percentPoint = (directionInfo.signal > 0 ? directionInfo.pixelStart + directionInfo.pixelLength - directionInfo.pixel : directionInfo.pixel - directionInfo.pixelStart) / directionInfo.pixelLength * (range[1] - range[0]) + range[0];
    var scale3 = Math.max(1 / e2.scale, 0);
    range[0] = (range[0] - percentPoint) * scale3 + percentPoint;
    range[1] = (range[1] - percentPoint) * scale3 + percentPoint;
    var minMaxSpan = this.dataZoomModel.findRepresentativeAxisProxy().getMinMaxSpan();
    sliderMove(0, range, [0, 100], 0, minMaxSpan.minSpan, minMaxSpan.maxSpan);
    this.range = range;
    if (lastRange[0] !== range[0] || lastRange[1] !== range[1]) {
      return range;
    }
  },
  pan: makeMover(function(range, axisModel, coordSysInfo, coordSysMainType, controller, e2) {
    var directionInfo = getDirectionInfo[coordSysMainType]([e2.oldX, e2.oldY], [e2.newX, e2.newY], axisModel, controller, coordSysInfo);
    return directionInfo.signal * (range[1] - range[0]) * directionInfo.pixel / directionInfo.pixelLength;
  }),
  scrollMove: makeMover(function(range, axisModel, coordSysInfo, coordSysMainType, controller, e2) {
    var directionInfo = getDirectionInfo[coordSysMainType]([0, 0], [e2.scrollDelta, e2.scrollDelta], axisModel, controller, coordSysInfo);
    return directionInfo.signal * (range[1] - range[0]) * e2.scrollDelta;
  })
};
function makeMover(getPercentDelta) {
  return function(coordSysInfo, coordSysMainType, controller, e2) {
    var lastRange = this.range;
    var range = lastRange.slice();
    var axisModel = coordSysInfo.axisModels[0];
    if (!axisModel) {
      return;
    }
    var percentDelta = getPercentDelta(range, axisModel, coordSysInfo, coordSysMainType, controller, e2);
    sliderMove(percentDelta, range, [0, 100], "all");
    this.range = range;
    if (lastRange[0] !== range[0] || lastRange[1] !== range[1]) {
      return range;
    }
  };
}
var getDirectionInfo = {
  grid: function(oldPoint, newPoint, axisModel, controller, coordSysInfo) {
    var axis = axisModel.axis;
    var ret = {};
    var rect = coordSysInfo.model.coordinateSystem.getRect();
    oldPoint = oldPoint || [0, 0];
    if (axis.dim === "x") {
      ret.pixel = newPoint[0] - oldPoint[0];
      ret.pixelLength = rect.width;
      ret.pixelStart = rect.x;
      ret.signal = axis.inverse ? 1 : -1;
    } else {
      ret.pixel = newPoint[1] - oldPoint[1];
      ret.pixelLength = rect.height;
      ret.pixelStart = rect.y;
      ret.signal = axis.inverse ? -1 : 1;
    }
    return ret;
  },
  polar: function(oldPoint, newPoint, axisModel, controller, coordSysInfo) {
    var axis = axisModel.axis;
    var ret = {};
    var polar = coordSysInfo.model.coordinateSystem;
    var radiusExtent = polar.getRadiusAxis().getExtent();
    var angleExtent = polar.getAngleAxis().getExtent();
    oldPoint = oldPoint ? polar.pointToCoord(oldPoint) : [0, 0];
    newPoint = polar.pointToCoord(newPoint);
    if (axisModel.mainType === "radiusAxis") {
      ret.pixel = newPoint[0] - oldPoint[0];
      ret.pixelLength = radiusExtent[1] - radiusExtent[0];
      ret.pixelStart = radiusExtent[0];
      ret.signal = axis.inverse ? 1 : -1;
    } else {
      ret.pixel = newPoint[1] - oldPoint[1];
      ret.pixelLength = angleExtent[1] - angleExtent[0];
      ret.pixelStart = angleExtent[0];
      ret.signal = axis.inverse ? -1 : 1;
    }
    return ret;
  },
  singleAxis: function(oldPoint, newPoint, axisModel, controller, coordSysInfo) {
    var axis = axisModel.axis;
    var rect = coordSysInfo.model.coordinateSystem.getRect();
    var ret = {};
    oldPoint = oldPoint || [0, 0];
    if (axis.orient === "horizontal") {
      ret.pixel = newPoint[0] - oldPoint[0];
      ret.pixelLength = rect.width;
      ret.pixelStart = rect.x;
      ret.signal = axis.inverse ? 1 : -1;
    } else {
      ret.pixel = newPoint[1] - oldPoint[1];
      ret.pixelLength = rect.height;
      ret.pixelStart = rect.y;
      ret.signal = axis.inverse ? -1 : 1;
    }
    return ret;
  }
};
var InsideZoomView_default = InsideZoomView;

// node_modules/echarts/lib/component/dataZoom/installDataZoomInside.js
function install39(registers) {
  installCommon(registers);
  registers.registerComponentModel(InsideZoomModel_default);
  registers.registerComponentView(InsideZoomView_default);
  installDataZoomRoamProcessor(registers);
}

// node_modules/echarts/lib/component/dataZoom/SliderZoomModel.js
var SliderZoomModel = (
  /** @class */
  (function(_super) {
    __extends(SliderZoomModel2, _super);
    function SliderZoomModel2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = SliderZoomModel2.type;
      return _this;
    }
    SliderZoomModel2.type = "dataZoom.slider";
    SliderZoomModel2.layoutMode = "box";
    SliderZoomModel2.defaultOption = inheritDefaultOption(DataZoomModel_default.defaultOption, {
      show: true,
      // deault value can only be drived in view stage.
      right: "ph",
      top: "ph",
      width: "ph",
      height: "ph",
      left: null,
      bottom: null,
      borderColor: tokens_default.color.accent10,
      borderRadius: 0,
      backgroundColor: tokens_default.color.transparent,
      // dataBackgroundColor: '#ddd',
      dataBackground: {
        lineStyle: {
          color: tokens_default.color.accent30,
          width: 0.5
        },
        areaStyle: {
          color: tokens_default.color.accent20,
          opacity: 0.2
        }
      },
      selectedDataBackground: {
        lineStyle: {
          color: tokens_default.color.accent40,
          width: 0.5
        },
        areaStyle: {
          color: tokens_default.color.accent20,
          opacity: 0.3
        }
      },
      // Color of selected window.
      fillerColor: "rgba(135,175,274,0.2)",
      handleIcon: "path://M-9.35,34.56V42m0-40V9.5m-2,0h4a2,2,0,0,1,2,2v21a2,2,0,0,1-2,2h-4a2,2,0,0,1-2-2v-21A2,2,0,0,1-11.35,9.5Z",
      // Percent of the slider height
      handleSize: "100%",
      handleStyle: {
        color: tokens_default.color.neutral00,
        borderColor: tokens_default.color.accent20
      },
      moveHandleSize: 7,
      moveHandleIcon: "path://M-320.9-50L-320.9-50c18.1,0,27.1,9,27.1,27.1V85.7c0,18.1-9,27.1-27.1,27.1l0,0c-18.1,0-27.1-9-27.1-27.1V-22.9C-348-41-339-50-320.9-50z M-212.3-50L-212.3-50c18.1,0,27.1,9,27.1,27.1V85.7c0,18.1-9,27.1-27.1,27.1l0,0c-18.1,0-27.1-9-27.1-27.1V-22.9C-239.4-41-230.4-50-212.3-50z M-103.7-50L-103.7-50c18.1,0,27.1,9,27.1,27.1V85.7c0,18.1-9,27.1-27.1,27.1l0,0c-18.1,0-27.1-9-27.1-27.1V-22.9C-130.9-41-121.8-50-103.7-50z",
      moveHandleStyle: {
        color: tokens_default.color.accent40,
        opacity: 0.5
      },
      showDetail: true,
      showDataShadow: "auto",
      realtime: true,
      zoomLock: false,
      textStyle: {
        color: tokens_default.color.tertiary
      },
      brushSelect: true,
      brushStyle: {
        color: tokens_default.color.accent30,
        opacity: 0.3
      },
      emphasis: {
        handleLabel: {
          show: true
        },
        handleStyle: {
          borderColor: tokens_default.color.accent40
        },
        moveHandleStyle: {
          opacity: 0.8
        }
      },
      defaultLocationEdgeGap: 15
    });
    return SliderZoomModel2;
  })(DataZoomModel_default)
);
var SliderZoomModel_default = SliderZoomModel;

// node_modules/echarts/lib/component/dataZoom/SliderZoomView.js
var Rect3 = Rect_default;
var DEFAULT_FRAME_BORDER_WIDTH = 1;
var DEFAULT_FILLER_SIZE = 30;
var DEFAULT_MOVE_HANDLE_SIZE = 7;
var HORIZONTAL = "horizontal";
var VERTICAL = "vertical";
var LABEL_GAP = 5;
var SHOW_DATA_SHADOW_SERIES_TYPE = ["line", "bar", "candlestick", "scatter"];
var REALTIME_ANIMATION_CONFIG = {
  easing: "cubicOut",
  duration: 100,
  delay: 0
};
var SliderZoomView = (
  /** @class */
  (function(_super) {
    __extends(SliderZoomView2, _super);
    function SliderZoomView2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = SliderZoomView2.type;
      _this._displayables = {};
      return _this;
    }
    SliderZoomView2.prototype.init = function(ecModel, api) {
      this.api = api;
      this._onBrush = bind(this._onBrush, this);
      this._onBrushEnd = bind(this._onBrushEnd, this);
    };
    SliderZoomView2.prototype.render = function(dataZoomModel, ecModel, api, payload) {
      _super.prototype.render.apply(this, arguments);
      createOrUpdate(this, "_dispatchZoomAction", dataZoomModel.get("throttle"), "fixRate");
      this._orient = dataZoomModel.getOrient();
      if (dataZoomModel.get("show") === false) {
        this.group.removeAll();
        return;
      }
      if (dataZoomModel.noTarget()) {
        this._clear();
        this.group.removeAll();
        return;
      }
      if (!payload || payload.type !== "dataZoom" || payload.from !== this.uid) {
        this._buildView();
      }
      this._updateView();
    };
    SliderZoomView2.prototype.dispose = function() {
      this._clear();
      _super.prototype.dispose.apply(this, arguments);
    };
    SliderZoomView2.prototype._clear = function() {
      clear(this, "_dispatchZoomAction");
      var zr = this.api.getZr();
      zr.off("mousemove", this._onBrush);
      zr.off("mouseup", this._onBrushEnd);
    };
    SliderZoomView2.prototype._buildView = function() {
      var thisGroup = this.group;
      thisGroup.removeAll();
      this._brushing = false;
      this._displayables.brushRect = null;
      this._resetLocation();
      this._resetInterval();
      var barGroup = this._displayables.sliderGroup = new Group_default();
      this._renderBackground();
      this._renderHandle();
      this._renderDataShadow();
      thisGroup.add(barGroup);
      this._positionGroup();
    };
    SliderZoomView2.prototype._resetLocation = function() {
      var dataZoomModel = this.dataZoomModel;
      var api = this.api;
      var showMoveHandle = dataZoomModel.get("brushSelect");
      var moveHandleSize = showMoveHandle ? DEFAULT_MOVE_HANDLE_SIZE : 0;
      var refContainer = createBoxLayoutReference(dataZoomModel, api).refContainer;
      var coordRect = this._findCoordRect();
      var edgeGap = dataZoomModel.get("defaultLocationEdgeGap", true) || 0;
      var positionInfo = this._orient === HORIZONTAL ? {
        // Why using 'right', because right should be used in vertical,
        // and it is better to be consistent for dealing with position param merge.
        right: refContainer.width - coordRect.x - coordRect.width,
        top: refContainer.height - DEFAULT_FILLER_SIZE - edgeGap - moveHandleSize,
        width: coordRect.width,
        height: DEFAULT_FILLER_SIZE
      } : {
        right: edgeGap,
        top: coordRect.y,
        width: DEFAULT_FILLER_SIZE,
        height: coordRect.height
      };
      var layoutParams = getLayoutParams(dataZoomModel.option);
      each(["right", "top", "width", "height"], function(name) {
        if (layoutParams[name] === "ph") {
          layoutParams[name] = positionInfo[name];
        }
      });
      var layoutRect = getLayoutRect(layoutParams, refContainer);
      this._location = {
        x: layoutRect.x,
        y: layoutRect.y
      };
      this._size = [layoutRect.width, layoutRect.height];
      this._orient === VERTICAL && this._size.reverse();
    };
    SliderZoomView2.prototype._positionGroup = function() {
      var thisGroup = this.group;
      var location = this._location;
      var orient = this._orient;
      var targetAxisModel = this.dataZoomModel.getFirstTargetAxisModel();
      var inverse = targetAxisModel && targetAxisModel.get("inverse");
      var sliderGroup = this._displayables.sliderGroup;
      var otherAxisInverse = (this._dataShadowInfo || {}).otherAxisInverse;
      sliderGroup.attr(orient === HORIZONTAL && !inverse ? {
        scaleY: otherAxisInverse ? 1 : -1,
        scaleX: 1
      } : orient === HORIZONTAL && inverse ? {
        scaleY: otherAxisInverse ? 1 : -1,
        scaleX: -1
      } : orient === VERTICAL && !inverse ? {
        scaleY: otherAxisInverse ? -1 : 1,
        scaleX: 1,
        rotation: Math.PI / 2
      } : {
        scaleY: otherAxisInverse ? -1 : 1,
        scaleX: -1,
        rotation: Math.PI / 2
      });
      var rect = thisGroup.getBoundingRect([sliderGroup]);
      var rectX = isNaN(rect.x) ? 0 : rect.x;
      var rectY = isNaN(rect.y) ? 0 : rect.y;
      thisGroup.x = location.x - rectX;
      thisGroup.y = location.y - rectY;
      thisGroup.markRedraw();
    };
    SliderZoomView2.prototype._getViewExtent = function() {
      return [0, this._size[0]];
    };
    SliderZoomView2.prototype._renderBackground = function() {
      var dataZoomModel = this.dataZoomModel;
      var size = this._size;
      var barGroup = this._displayables.sliderGroup;
      var brushSelect = dataZoomModel.get("brushSelect");
      barGroup.add(new Rect3({
        silent: true,
        shape: {
          x: 0,
          y: 0,
          width: size[0],
          height: size[1]
        },
        style: {
          fill: dataZoomModel.get("backgroundColor")
        },
        z2: -40
      }));
      var clickPanel = new Rect3({
        shape: {
          x: 0,
          y: 0,
          width: size[0],
          height: size[1]
        },
        style: {
          fill: "transparent"
        },
        z2: 0,
        onclick: bind(this._onClickPanel, this)
      });
      var zr = this.api.getZr();
      if (brushSelect) {
        clickPanel.on("mousedown", this._onBrushStart, this);
        clickPanel.cursor = "crosshair";
        zr.on("mousemove", this._onBrush);
        zr.on("mouseup", this._onBrushEnd);
      } else {
        zr.off("mousemove", this._onBrush);
        zr.off("mouseup", this._onBrushEnd);
      }
      barGroup.add(clickPanel);
    };
    SliderZoomView2.prototype._renderDataShadow = function() {
      var info = this._dataShadowInfo = this._prepareDataShadowInfo();
      this._displayables.dataShadowSegs = [];
      if (!info) {
        return;
      }
      var size = this._size;
      var oldSize = this._shadowSize || [];
      var seriesModel = info.series;
      var data = seriesModel.getRawData();
      var candlestickDim = seriesModel.getShadowDim && seriesModel.getShadowDim();
      var otherDim = candlestickDim && data.getDimensionInfo(candlestickDim) ? seriesModel.getShadowDim() : info.otherDim;
      if (otherDim == null) {
        return;
      }
      var polygonPts = this._shadowPolygonPts;
      var polylinePts = this._shadowPolylinePts;
      if (data !== this._shadowData || otherDim !== this._shadowDim || size[0] !== oldSize[0] || size[1] !== oldSize[1]) {
        var thisDataExtent_1 = data.getDataExtent(info.thisDim);
        var otherDataExtent_1 = data.getDataExtent(otherDim);
        var otherOffset = (otherDataExtent_1[1] - otherDataExtent_1[0]) * 0.3;
        otherDataExtent_1 = [otherDataExtent_1[0] - otherOffset, otherDataExtent_1[1] + otherOffset];
        var otherShadowExtent_1 = [0, size[1]];
        var thisShadowExtent = [0, size[0]];
        var areaPoints_1 = [[size[0], 0], [0, 0]];
        var linePoints_1 = [];
        var step_1 = thisShadowExtent[1] / Math.max(1, data.count() - 1);
        var normalizationConstant_1 = size[0] / (thisDataExtent_1[1] - thisDataExtent_1[0]);
        var isTimeAxis_1 = info.thisAxis.type === "time";
        var thisCoord_1 = -step_1;
        var stride_1 = Math.round(data.count() / size[0]);
        var lastIsEmpty_1;
        data.each([info.thisDim, otherDim], function(thisValue, otherValue, index) {
          if (stride_1 > 0 && index % stride_1) {
            if (!isTimeAxis_1) {
              thisCoord_1 += step_1;
            }
            return;
          }
          thisCoord_1 = isTimeAxis_1 ? (+thisValue - thisDataExtent_1[0]) * normalizationConstant_1 : thisCoord_1 + step_1;
          var isEmpty = otherValue == null || isNaN(otherValue) || otherValue === "";
          var otherCoord = isEmpty ? 0 : linearMap(otherValue, otherDataExtent_1, otherShadowExtent_1, true);
          if (isEmpty && !lastIsEmpty_1 && index) {
            areaPoints_1.push([areaPoints_1[areaPoints_1.length - 1][0], 0]);
            linePoints_1.push([linePoints_1[linePoints_1.length - 1][0], 0]);
          } else if (!isEmpty && lastIsEmpty_1) {
            areaPoints_1.push([thisCoord_1, 0]);
            linePoints_1.push([thisCoord_1, 0]);
          }
          if (!isEmpty) {
            areaPoints_1.push([thisCoord_1, otherCoord]);
            linePoints_1.push([thisCoord_1, otherCoord]);
          }
          lastIsEmpty_1 = isEmpty;
        });
        polygonPts = this._shadowPolygonPts = areaPoints_1;
        polylinePts = this._shadowPolylinePts = linePoints_1;
      }
      this._shadowData = data;
      this._shadowDim = otherDim;
      this._shadowSize = [size[0], size[1]];
      var dataZoomModel = this.dataZoomModel;
      function createDataShadowGroup(isSelectedArea) {
        var model2 = dataZoomModel.getModel(isSelectedArea ? "selectedDataBackground" : "dataBackground");
        var group3 = new Group_default();
        var polygon = new Polygon_default({
          shape: {
            points: polygonPts
          },
          segmentIgnoreThreshold: 1,
          style: model2.getModel("areaStyle").getAreaStyle(),
          silent: true,
          z2: -20
        });
        var polyline = new Polyline_default({
          shape: {
            points: polylinePts
          },
          segmentIgnoreThreshold: 1,
          style: model2.getModel("lineStyle").getLineStyle(),
          silent: true,
          z2: -19
        });
        group3.add(polygon);
        group3.add(polyline);
        return group3;
      }
      for (var i2 = 0; i2 < 3; i2++) {
        var group2 = createDataShadowGroup(i2 === 1);
        this._displayables.sliderGroup.add(group2);
        this._displayables.dataShadowSegs.push(group2);
      }
    };
    SliderZoomView2.prototype._prepareDataShadowInfo = function() {
      var dataZoomModel = this.dataZoomModel;
      var showDataShadow = dataZoomModel.get("showDataShadow");
      if (showDataShadow === false) {
        return;
      }
      var result;
      var ecModel = this.ecModel;
      dataZoomModel.eachTargetAxis(function(axisDim, axisIndex) {
        var seriesModels = dataZoomModel.getAxisProxy(axisDim, axisIndex).getTargetSeriesModels();
        each(seriesModels, function(seriesModel) {
          if (result) {
            return;
          }
          if (showDataShadow !== true && indexOf(SHOW_DATA_SHADOW_SERIES_TYPE, seriesModel.get("type")) < 0) {
            return;
          }
          var thisAxis = ecModel.getComponent(getAxisMainType(axisDim), axisIndex).axis;
          var otherDim = getOtherDim(axisDim);
          var otherAxisInverse;
          var coordSys = seriesModel.coordinateSystem;
          if (otherDim != null && coordSys.getOtherAxis) {
            otherAxisInverse = coordSys.getOtherAxis(thisAxis).inverse;
          }
          otherDim = seriesModel.getData().mapDimension(otherDim);
          var thisDim = seriesModel.getData().mapDimension(axisDim);
          result = {
            thisAxis,
            series: seriesModel,
            thisDim,
            otherDim,
            otherAxisInverse
          };
        }, this);
      }, this);
      return result;
    };
    SliderZoomView2.prototype._renderHandle = function() {
      var thisGroup = this.group;
      var displayables = this._displayables;
      var handles = displayables.handles = [null, null];
      var handleLabels = displayables.handleLabels = [null, null];
      var sliderGroup = this._displayables.sliderGroup;
      var size = this._size;
      var dataZoomModel = this.dataZoomModel;
      var api = this.api;
      var borderRadius = dataZoomModel.get("borderRadius") || 0;
      var brushSelect = dataZoomModel.get("brushSelect");
      var filler = displayables.filler = new Rect3({
        silent: brushSelect,
        style: {
          fill: dataZoomModel.get("fillerColor")
        },
        textConfig: {
          position: "inside"
        }
      });
      sliderGroup.add(filler);
      sliderGroup.add(new Rect3({
        silent: true,
        subPixelOptimize: true,
        shape: {
          x: 0,
          y: 0,
          width: size[0],
          height: size[1],
          r: borderRadius
        },
        style: {
          // deprecated option
          stroke: dataZoomModel.get("dataBackgroundColor") || dataZoomModel.get("borderColor"),
          lineWidth: DEFAULT_FRAME_BORDER_WIDTH,
          fill: tokens_default.color.transparent
        }
      }));
      each([0, 1], function(handleIndex) {
        var iconStr = dataZoomModel.get("handleIcon");
        if (!symbolBuildProxies[iconStr] && iconStr.indexOf("path://") < 0 && iconStr.indexOf("image://") < 0) {
          iconStr = "path://" + iconStr;
          if (true) {
            deprecateLog("handleIcon now needs 'path://' prefix when using a path string");
          }
        }
        var path = createSymbol(iconStr, -1, 0, 2, 2, null, true);
        path.attr({
          cursor: getCursor(this._orient),
          draggable: true,
          drift: bind(this._onDragMove, this, handleIndex),
          ondragend: bind(this._onDragEnd, this),
          onmouseover: bind(this._onOverDataInfoTriggerArea, this, true),
          onmouseout: bind(this._onOverDataInfoTriggerArea, this, false),
          z2: 5
        });
        var bRect = path.getBoundingRect();
        var handleSize = dataZoomModel.get("handleSize");
        this._handleHeight = parsePercent2(handleSize, this._size[1]);
        this._handleWidth = bRect.width / bRect.height * this._handleHeight;
        path.setStyle(dataZoomModel.getModel("handleStyle").getItemStyle());
        path.style.strokeNoScale = true;
        path.rectHover = true;
        path.ensureState("emphasis").style = dataZoomModel.getModel(["emphasis", "handleStyle"]).getItemStyle();
        enableHoverEmphasis(path);
        var handleColor = dataZoomModel.get("handleColor");
        if (handleColor != null) {
          path.style.fill = handleColor;
        }
        sliderGroup.add(handles[handleIndex] = path);
        var textStyleModel = dataZoomModel.getModel("textStyle");
        var handleLabel = dataZoomModel.get("handleLabel") || {};
        var handleLabelShow = handleLabel.show || false;
        thisGroup.add(handleLabels[handleIndex] = new Text_default({
          silent: true,
          invisible: !handleLabelShow,
          style: createTextStyle(textStyleModel, {
            x: 0,
            y: 0,
            text: "",
            verticalAlign: "middle",
            align: "center",
            fill: textStyleModel.getTextColor(),
            font: textStyleModel.getFont()
          }),
          z2: 10
        }));
      }, this);
      var actualMoveZone = filler;
      if (brushSelect) {
        var moveHandleHeight = parsePercent2(dataZoomModel.get("moveHandleSize"), size[1]);
        var moveHandle_1 = displayables.moveHandle = new Rect_default({
          style: dataZoomModel.getModel("moveHandleStyle").getItemStyle(),
          silent: true,
          shape: {
            r: [0, 0, 2, 2],
            y: size[1] - 0.5,
            height: moveHandleHeight
          }
        });
        var iconSize = moveHandleHeight * 0.8;
        var moveHandleIcon = displayables.moveHandleIcon = createSymbol(dataZoomModel.get("moveHandleIcon"), -iconSize / 2, -iconSize / 2, iconSize, iconSize, tokens_default.color.neutral00, true);
        moveHandleIcon.silent = true;
        moveHandleIcon.y = size[1] + moveHandleHeight / 2 - 0.5;
        moveHandle_1.ensureState("emphasis").style = dataZoomModel.getModel(["emphasis", "moveHandleStyle"]).getItemStyle();
        var moveZoneExpandSize = Math.min(size[1] / 2, Math.max(moveHandleHeight, 10));
        actualMoveZone = displayables.moveZone = new Rect_default({
          invisible: true,
          shape: {
            y: size[1] - moveZoneExpandSize,
            height: moveHandleHeight + moveZoneExpandSize
          }
        });
        actualMoveZone.on("mouseover", function() {
          api.enterEmphasis(moveHandle_1);
        }).on("mouseout", function() {
          api.leaveEmphasis(moveHandle_1);
        });
        sliderGroup.add(moveHandle_1);
        sliderGroup.add(moveHandleIcon);
        sliderGroup.add(actualMoveZone);
      }
      actualMoveZone.attr({
        draggable: true,
        cursor: "grab",
        drift: bind(this._onActualMoveZoneDrift, this),
        ondragstart: bind(this._onActualMoveZoneDragStart, this),
        ondragend: bind(this._onActualMoveZoneDragEnd, this),
        onmouseover: bind(this._onOverDataInfoTriggerArea, this, true),
        onmouseout: bind(this._onOverDataInfoTriggerArea, this, false)
      });
    };
    SliderZoomView2.prototype._resetInterval = function() {
      var range = this._range = this.dataZoomModel.getPercentRange();
      var viewExtent = this._getViewExtent();
      this._handleEnds = [linearMap(range[0], [0, 100], viewExtent, true), linearMap(range[1], [0, 100], viewExtent, true)];
    };
    SliderZoomView2.prototype._updateInterval = function(handleIndex, delta) {
      var dataZoomModel = this.dataZoomModel;
      var handleEnds = this._handleEnds;
      var viewExtend = this._getViewExtent();
      var minMaxSpan = dataZoomModel.findRepresentativeAxisProxy().getMinMaxSpan();
      var percentExtent = [0, 100];
      sliderMove(delta, handleEnds, viewExtend, dataZoomModel.get("zoomLock") ? "all" : handleIndex, minMaxSpan.minSpan != null ? linearMap(minMaxSpan.minSpan, percentExtent, viewExtend, true) : null, minMaxSpan.maxSpan != null ? linearMap(minMaxSpan.maxSpan, percentExtent, viewExtend, true) : null);
      var lastRange = this._range;
      var range = this._range = asc([linearMap(handleEnds[0], viewExtend, percentExtent, true), linearMap(handleEnds[1], viewExtend, percentExtent, true)]);
      return !lastRange || lastRange[0] !== range[0] || lastRange[1] !== range[1];
    };
    SliderZoomView2.prototype._updateView = function(nonRealtime) {
      var displaybles = this._displayables;
      var handleEnds = this._handleEnds;
      var handleInterval = asc(handleEnds.slice());
      var size = this._size;
      each([0, 1], function(handleIndex) {
        var handle = displaybles.handles[handleIndex];
        var handleHeight = this._handleHeight;
        handle.attr({
          scaleX: handleHeight / 2,
          scaleY: handleHeight / 2,
          // This is a trick, by adding an extra tiny offset to let the default handle's end point align to the drag window.
          // NOTE: It may affect some custom shapes a bit. But we prefer to have better result by default.
          x: handleEnds[handleIndex] + (handleIndex ? -1 : 1),
          y: size[1] / 2 - handleHeight / 2
        });
      }, this);
      displaybles.filler.setShape({
        x: handleInterval[0],
        y: 0,
        width: handleInterval[1] - handleInterval[0],
        height: size[1]
      });
      var viewExtent = {
        x: handleInterval[0],
        width: handleInterval[1] - handleInterval[0]
      };
      if (displaybles.moveHandle) {
        displaybles.moveHandle.setShape(viewExtent);
        displaybles.moveZone.setShape(viewExtent);
        displaybles.moveZone.getBoundingRect();
        displaybles.moveHandleIcon && displaybles.moveHandleIcon.attr("x", viewExtent.x + viewExtent.width / 2);
      }
      var dataShadowSegs = displaybles.dataShadowSegs;
      var segIntervals = [0, handleInterval[0], handleInterval[1], size[0]];
      for (var i2 = 0; i2 < dataShadowSegs.length; i2++) {
        var segGroup = dataShadowSegs[i2];
        var clipPath = segGroup.getClipPath();
        if (!clipPath) {
          clipPath = new Rect_default();
          segGroup.setClipPath(clipPath);
        }
        clipPath.setShape({
          x: segIntervals[i2],
          y: 0,
          width: segIntervals[i2 + 1] - segIntervals[i2],
          height: size[1]
        });
      }
      this._updateDataInfo(nonRealtime);
    };
    SliderZoomView2.prototype._updateDataInfo = function(nonRealtime) {
      var dataZoomModel = this.dataZoomModel;
      var displayables = this._displayables;
      var handleLabels = displayables.handleLabels;
      var orient = this._orient;
      var labelTexts = ["", ""];
      if (dataZoomModel.get("showDetail")) {
        var axisProxy = dataZoomModel.findRepresentativeAxisProxy();
        var scale3 = axisProxy.getAxisModel().axis.scale;
        if (axisProxy) {
          var range = this._range;
          var window_1;
          if (nonRealtime) {
            var calcWinInput = {
              start: range[0],
              end: range[1]
            };
            var alignTo = getAlignTo(dataZoomModel, axisProxy);
            if (alignTo) {
              var alignToWindow = alignTo.calculateDataWindow(calcWinInput).percentInverted;
              calcWinInput = {
                start: alignToWindow[0],
                end: alignToWindow[1]
              };
            }
            window_1 = axisProxy.calculateDataWindow(calcWinInput);
          } else {
            window_1 = axisProxy.getWindow();
          }
          labelTexts = [formatLabel2(dataZoomModel, 0, window_1, scale3), formatLabel2(dataZoomModel, 1, window_1, scale3)];
        }
      }
      var orderedHandleEnds = asc(this._handleEnds.slice());
      setLabel.call(this, 0);
      setLabel.call(this, 1);
      function setLabel(handleIndex) {
        var barTransform = getTransform2(displayables.handles[handleIndex].parent, this.group);
        var direction = transformDirection(handleIndex === 0 ? "right" : "left", barTransform);
        var offset = this._handleWidth / 2 + LABEL_GAP;
        var textPoint = applyTransform2([orderedHandleEnds[handleIndex] + (handleIndex === 0 ? -offset : offset), this._size[1] / 2], barTransform);
        handleLabels[handleIndex].setStyle({
          x: textPoint[0],
          y: textPoint[1],
          verticalAlign: orient === HORIZONTAL ? "middle" : direction,
          align: orient === HORIZONTAL ? direction : "center",
          text: labelTexts[handleIndex]
        });
      }
    };
    SliderZoomView2.prototype._onOverDataInfoTriggerArea = function(isOver) {
      this._isOverDataInfoTriggerArea = isOver;
      this._showDataInfo(isOver);
    };
    SliderZoomView2.prototype._showDataInfo = function(isEmphasis) {
      var handleLabel = this.dataZoomModel.get("handleLabel") || {};
      var normalShow = handleLabel.show || false;
      var emphasisHandleLabel = this.dataZoomModel.getModel(["emphasis", "handleLabel"]);
      var emphasisShow = emphasisHandleLabel.get("show") || false;
      var toShow = isEmphasis || this._dragging ? emphasisShow : normalShow;
      var displayables = this._displayables;
      var handleLabels = displayables.handleLabels;
      handleLabels[0].attr("invisible", !toShow);
      handleLabels[1].attr("invisible", !toShow);
      displayables.moveHandle && this.api[toShow ? "enterEmphasis" : "leaveEmphasis"](displayables.moveHandle, 1);
    };
    SliderZoomView2.prototype._onActualMoveZoneDrift = function(dx, dy, event) {
      this.api.getZr().setCursorStyle("grabbing");
      this._onDragMove("all", dx, dy, event);
    };
    SliderZoomView2.prototype._onActualMoveZoneDragStart = function(event) {
      event.target.attr("cursor", "grabbing");
      this._showDataInfo(true);
    };
    SliderZoomView2.prototype._onActualMoveZoneDragEnd = function(event) {
      event.target.attr("cursor", "grab");
      this._onDragEnd();
    };
    SliderZoomView2.prototype._onDragMove = function(handleIndex, dx, dy, event) {
      this._dragging = true;
      stop(event.event);
      var barTransform = this._displayables.sliderGroup.getLocalTransform();
      var vertex = applyTransform2([dx, dy], barTransform, true);
      var changed = this._updateInterval(handleIndex, vertex[0]);
      var realtime = this.dataZoomModel.get("realtime");
      this._updateView(!realtime);
      changed && realtime && this._dispatchZoomAction(true);
    };
    SliderZoomView2.prototype._onDragEnd = function() {
      this._dragging = false;
      if (!this._isOverDataInfoTriggerArea) {
        this._showDataInfo(false);
      }
      var realtime = this.dataZoomModel.get("realtime");
      !realtime && this._dispatchZoomAction(false);
    };
    SliderZoomView2.prototype._onClickPanel = function(e2) {
      var size = this._size;
      var localPoint = this._displayables.sliderGroup.transformCoordToLocal(e2.offsetX, e2.offsetY);
      if (localPoint[0] < 0 || localPoint[0] > size[0] || localPoint[1] < 0 || localPoint[1] > size[1]) {
        return;
      }
      var handleEnds = this._handleEnds;
      var center3 = (handleEnds[0] + handleEnds[1]) / 2;
      var changed = this._updateInterval("all", localPoint[0] - center3);
      this._updateView();
      changed && this._dispatchZoomAction(false);
    };
    SliderZoomView2.prototype._onBrushStart = function(e2) {
      var x = e2.offsetX;
      var y = e2.offsetY;
      this._brushStart = new Point_default(x, y);
      this._brushing = true;
      this._brushStartTime = +/* @__PURE__ */ new Date();
    };
    SliderZoomView2.prototype._onBrushEnd = function(e2) {
      if (!this._brushing) {
        return;
      }
      var brushRect = this._displayables.brushRect;
      this._brushing = false;
      if (!brushRect) {
        return;
      }
      brushRect.attr("ignore", true);
      var brushShape = brushRect.shape;
      var brushEndTime = +/* @__PURE__ */ new Date();
      if (brushEndTime - this._brushStartTime < 200 && Math.abs(brushShape.width) < 5) {
        return;
      }
      var viewExtend = this._getViewExtent();
      var percentExtent = [0, 100];
      var handleEnds = this._handleEnds = [brushShape.x, brushShape.x + brushShape.width];
      var minMaxSpan = this.dataZoomModel.findRepresentativeAxisProxy().getMinMaxSpan();
      sliderMove(0, handleEnds, viewExtend, 0, minMaxSpan.minSpan != null ? linearMap(minMaxSpan.minSpan, percentExtent, viewExtend, true) : null, minMaxSpan.maxSpan != null ? linearMap(minMaxSpan.maxSpan, percentExtent, viewExtend, true) : null);
      this._range = asc([linearMap(handleEnds[0], viewExtend, percentExtent, true), linearMap(handleEnds[1], viewExtend, percentExtent, true)]);
      this._updateView();
      this._dispatchZoomAction(false);
    };
    SliderZoomView2.prototype._onBrush = function(e2) {
      if (this._brushing) {
        stop(e2.event);
        this._updateBrushRect(e2.offsetX, e2.offsetY);
      }
    };
    SliderZoomView2.prototype._updateBrushRect = function(mouseX, mouseY) {
      var displayables = this._displayables;
      var dataZoomModel = this.dataZoomModel;
      var brushRect = displayables.brushRect;
      if (!brushRect) {
        brushRect = displayables.brushRect = new Rect3({
          silent: true,
          style: dataZoomModel.getModel("brushStyle").getItemStyle()
        });
        displayables.sliderGroup.add(brushRect);
      }
      brushRect.attr("ignore", false);
      var brushStart = this._brushStart;
      var sliderGroup = this._displayables.sliderGroup;
      var endPoint = sliderGroup.transformCoordToLocal(mouseX, mouseY);
      var startPoint = sliderGroup.transformCoordToLocal(brushStart.x, brushStart.y);
      var size = this._size;
      endPoint[0] = Math.max(Math.min(size[0], endPoint[0]), 0);
      brushRect.setShape({
        x: startPoint[0],
        y: 0,
        width: endPoint[0] - startPoint[0],
        height: size[1]
      });
    };
    SliderZoomView2.prototype._dispatchZoomAction = function(realtime) {
      var range = this._range;
      this.api.dispatchAction({
        type: "dataZoom",
        from: this.uid,
        dataZoomId: this.dataZoomModel.id,
        animation: realtime ? REALTIME_ANIMATION_CONFIG : null,
        start: range[0],
        end: range[1]
      });
    };
    SliderZoomView2.prototype._findCoordRect = function() {
      var rect;
      var coordSysInfoList = collectReferCoordSysModelInfo(this.dataZoomModel).infoList;
      if (!rect && coordSysInfoList.length) {
        var coordSys = coordSysInfoList[0].model.coordinateSystem;
        rect = coordSys.getRect && coordSys.getRect();
      }
      if (!rect) {
        var width = this.api.getWidth();
        var height = this.api.getHeight();
        rect = {
          x: width * 0.2,
          y: height * 0.2,
          width: width * 0.6,
          height: height * 0.6
        };
      }
      return rect;
    };
    SliderZoomView2.type = "dataZoom.slider";
    return SliderZoomView2;
  })(DataZoomView_default)
);
function formatLabel2(dataZoomModel, extentIdx, window2, scale3) {
  var labelFormatter = dataZoomModel.get("labelFormatter");
  var labelPrecision = dataZoomModel.get("labelPrecision");
  if (labelPrecision == null || labelPrecision === "auto") {
    labelPrecision = window2.valuePrecision;
  }
  var value = window2.value[extentIdx];
  var valueStr = value == null || isNaN(value) ? "" : isOrdinalScale(scale3) || isTimeScale(scale3) ? scale3.getLabel({
    value: Math.round(value)
  }) : isFinite(labelPrecision) ? round2(value, labelPrecision, true) : value + "";
  return isFunction(labelFormatter) ? labelFormatter(value, valueStr) : isString(labelFormatter) ? labelFormatter.replace("{value}", valueStr) : valueStr;
}
function getOtherDim(thisDim) {
  var map4 = {
    x: "y",
    y: "x",
    radius: "angle",
    angle: "radius"
  };
  return map4[thisDim];
}
function getCursor(orient) {
  return orient === "vertical" ? "ns-resize" : "ew-resize";
}
var SliderZoomView_default = SliderZoomView;

// node_modules/echarts/lib/component/dataZoom/installDataZoomSlider.js
function install40(registers) {
  registers.registerComponentModel(SliderZoomModel_default);
  registers.registerComponentView(SliderZoomView_default);
  installCommon(registers);
}

// node_modules/echarts/lib/component/dataZoom/install.js
function install41(registers) {
  use(install39);
  use(install40);
}

// node_modules/echarts/lib/visual/visualDefault.js
var visualDefault = {
  /**
   * @public
   */
  get: function(visualType, key2, isCategory2) {
    var value = clone((defaultOption2[visualType] || {})[key2]);
    return isCategory2 ? isArray(value) ? value[value.length - 1] : value : value;
  }
};
var defaultOption2 = {
  color: {
    active: ["#006edd", "#e0ffff"],
    inactive: [tokens_default.color.transparent]
  },
  colorHue: {
    active: [0, 360],
    inactive: [0, 0]
  },
  colorSaturation: {
    active: [0.3, 1],
    inactive: [0, 0]
  },
  colorLightness: {
    active: [0.9, 0.5],
    inactive: [0, 0]
  },
  colorAlpha: {
    active: [0.3, 1],
    inactive: [0, 0]
  },
  opacity: {
    active: [0.3, 1],
    inactive: [0, 0]
  },
  symbol: {
    active: ["circle", "roundRect", "diamond"],
    inactive: ["none"]
  },
  symbolSize: {
    active: [10, 50],
    inactive: [0, 0]
  }
};
var visualDefault_default = visualDefault;

// node_modules/echarts/lib/component/visualMap/VisualMapModel.js
var mapVisual = VisualMapping_default.mapVisual;
var eachVisual = VisualMapping_default.eachVisual;
var isArray2 = isArray;
var each10 = each;
var asc2 = asc;
var linearMap2 = linearMap;
var VisualMapModel = (
  /** @class */
  (function(_super) {
    __extends(VisualMapModel2, _super);
    function VisualMapModel2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = VisualMapModel2.type;
      _this.stateList = ["inRange", "outOfRange"];
      _this.replacableOptionKeys = ["inRange", "outOfRange", "target", "controller", "color"];
      _this.layoutMode = {
        type: "box",
        ignoreSize: true
      };
      _this.dataBound = [-Infinity, Infinity];
      _this.targetVisuals = {};
      _this.controllerVisuals = {};
      return _this;
    }
    VisualMapModel2.prototype.init = function(option, parentModel, ecModel) {
      this.mergeDefaultAndTheme(option, ecModel);
    };
    VisualMapModel2.prototype.optionUpdated = function(newOption, isInit) {
      var thisOption = this.option;
      !isInit && replaceVisualOption(thisOption, newOption, this.replacableOptionKeys);
      this.textStyleModel = this.getModel("textStyle");
      this.resetItemSize();
      this.completeVisualOption();
    };
    VisualMapModel2.prototype.resetVisual = function(supplementVisualOption) {
      var stateList = this.stateList;
      supplementVisualOption = bind(supplementVisualOption, this);
      this.controllerVisuals = createVisualMappings(this.option.controller, stateList, supplementVisualOption);
      this.targetVisuals = createVisualMappings(this.option.target, stateList, supplementVisualOption);
    };
    VisualMapModel2.prototype.getItemSymbol = function() {
      return null;
    };
    VisualMapModel2.prototype.getTargetSeriesIndices = function() {
      var _this = this;
      var seriesTargets = this.option.seriesTargets;
      if (seriesTargets) {
        var indices_1 = [];
        each10(seriesTargets, function(target) {
          if (target.seriesIndex != null) {
            indices_1.push(target.seriesIndex);
          } else if (target.seriesId != null) {
            var seriesModel_1;
            _this.ecModel.eachSeries(function(series) {
              if (series.id === target.seriesId) {
                seriesModel_1 = series;
              }
            });
            if (seriesModel_1) {
              indices_1.push(seriesModel_1.componentIndex);
            }
          }
        });
        return indices_1;
      }
      var optionSeriesId = this.option.seriesId;
      var optionSeriesIndex = this.option.seriesIndex;
      if (optionSeriesIndex == null && optionSeriesId == null) {
        optionSeriesIndex = "all";
      }
      var seriesModels = queryReferringComponents(this.ecModel, "series", {
        index: optionSeriesIndex,
        id: optionSeriesId
      }, {
        useDefault: false,
        enableAll: true,
        enableNone: false
      }).models;
      return map2(seriesModels, function(seriesModel) {
        return seriesModel.componentIndex;
      });
    };
    VisualMapModel2.prototype.eachTargetSeries = function(callback, context) {
      each(this.getTargetSeriesIndices(), function(seriesIndex) {
        var seriesModel = this.ecModel.getSeriesByIndex(seriesIndex);
        if (seriesModel) {
          callback.call(context, seriesModel);
        }
      }, this);
    };
    VisualMapModel2.prototype.isTargetSeries = function(seriesModel) {
      var is = false;
      this.eachTargetSeries(function(model2) {
        model2 === seriesModel && (is = true);
      });
      return is;
    };
    VisualMapModel2.prototype.formatValueText = function(value, isCategory2, edgeSymbols) {
      var option = this.option;
      var precision = option.precision;
      var dataBound = this.dataBound;
      var formatter = option.formatter;
      var isMinMax;
      edgeSymbols = edgeSymbols || ["<", ">"];
      if (isArray(value)) {
        value = value.slice();
        isMinMax = true;
      }
      var textValue = isCategory2 ? value : isMinMax ? [toFixed(value[0]), toFixed(value[1])] : toFixed(value);
      if (isString(formatter)) {
        return formatter.replace("{value}", isMinMax ? textValue[0] : textValue).replace("{value2}", isMinMax ? textValue[1] : textValue);
      } else if (isFunction(formatter)) {
        return isMinMax ? formatter(value[0], value[1]) : formatter(value);
      }
      if (isMinMax) {
        if (value[0] === dataBound[0]) {
          return edgeSymbols[0] + " " + textValue[1];
        } else if (value[1] === dataBound[1]) {
          return edgeSymbols[1] + " " + textValue[0];
        } else {
          return textValue[0] + " - " + textValue[1];
        }
      } else {
        return textValue;
      }
      function toFixed(val) {
        return val === dataBound[0] ? "min" : val === dataBound[1] ? "max" : (+val).toFixed(Math.min(precision, 20));
      }
    };
    VisualMapModel2.prototype.resetExtent = function() {
      var thisOption = this.option;
      var extent = asc2([thisOption.min, thisOption.max]);
      this._dataExtent = extent;
    };
    VisualMapModel2.prototype.getDimension = function(seriesIndex) {
      var _this = this;
      var seriesTargets = this.option.seriesTargets;
      if (seriesTargets) {
        var target = find(seriesTargets, function(target2) {
          return target2.seriesIndex != null && target2.seriesIndex === seriesIndex || target2.seriesId != null && target2.seriesId === _this.ecModel.getSeriesByIndex(seriesIndex).id;
        });
        if (target) {
          return target.dimension;
        }
      }
      return this.option.dimension;
    };
    VisualMapModel2.prototype.getDataDimensionIndex = function(data) {
      var seriesIndex = data.hostModel.seriesIndex;
      var optDim = this.getDimension(seriesIndex);
      if (optDim != null) {
        return data.getDimensionIndex(optDim);
      }
      var dimNames = data.dimensions;
      for (var i2 = dimNames.length - 1; i2 >= 0; i2--) {
        var dimName = dimNames[i2];
        var dimInfo = data.getDimensionInfo(dimName);
        if (!dimInfo.isCalculationCoord) {
          return dimInfo.storeDimIndex;
        }
      }
    };
    VisualMapModel2.prototype.getExtent = function() {
      return this._dataExtent.slice();
    };
    VisualMapModel2.prototype.completeVisualOption = function() {
      var ecModel = this.ecModel;
      var thisOption = this.option;
      var base2 = {
        inRange: thisOption.inRange,
        outOfRange: thisOption.outOfRange
      };
      var target = thisOption.target || (thisOption.target = {});
      var controller = thisOption.controller || (thisOption.controller = {});
      merge2(target, base2);
      merge2(controller, base2);
      var isCategory2 = this.isCategory();
      completeSingle.call(this, target);
      completeSingle.call(this, controller);
      completeInactive.call(this, target, "inRange", "outOfRange");
      completeController.call(this, controller);
      function completeSingle(base3) {
        if (isArray2(thisOption.color) && !base3.inRange) {
          base3.inRange = {
            color: thisOption.color.slice().reverse()
          };
        }
        base3.inRange = base3.inRange || {
          color: ecModel.get("gradientColor")
        };
      }
      function completeInactive(base3, stateExist, stateAbsent) {
        var optExist = base3[stateExist];
        var optAbsent = base3[stateAbsent];
        if (optExist && !optAbsent) {
          optAbsent = base3[stateAbsent] = {};
          each10(optExist, function(visualData, visualType) {
            if (!VisualMapping_default.isValidType(visualType)) {
              return;
            }
            var defa = visualDefault_default.get(visualType, "inactive", isCategory2);
            if (defa != null) {
              optAbsent[visualType] = defa;
              if (visualType === "color" && !optAbsent.hasOwnProperty("opacity") && !optAbsent.hasOwnProperty("colorAlpha")) {
                optAbsent.opacity = [0, 0];
              }
            }
          });
        }
      }
      function completeController(controller2) {
        var symbolExists = (controller2.inRange || {}).symbol || (controller2.outOfRange || {}).symbol;
        var symbolSizeExists = (controller2.inRange || {}).symbolSize || (controller2.outOfRange || {}).symbolSize;
        var inactiveColor = this.get("inactiveColor");
        var itemSymbol = this.getItemSymbol();
        var defaultSymbol = itemSymbol || "roundRect";
        each10(this.stateList, function(state2) {
          var itemSize = this.itemSize;
          var visuals = controller2[state2];
          if (!visuals) {
            visuals = controller2[state2] = {
              color: isCategory2 ? inactiveColor : [inactiveColor]
            };
          }
          if (visuals.symbol == null) {
            visuals.symbol = symbolExists && clone(symbolExists) || (isCategory2 ? defaultSymbol : [defaultSymbol]);
          }
          if (visuals.symbolSize == null) {
            visuals.symbolSize = symbolSizeExists && clone(symbolSizeExists) || (isCategory2 ? itemSize[0] : [itemSize[0], itemSize[0]]);
          }
          visuals.symbol = mapVisual(visuals.symbol, function(symbol) {
            return symbol === "none" ? defaultSymbol : symbol;
          });
          var symbolSize = visuals.symbolSize;
          if (symbolSize != null) {
            var max_1 = -Infinity;
            eachVisual(symbolSize, function(value) {
              value > max_1 && (max_1 = value);
            });
            visuals.symbolSize = mapVisual(symbolSize, function(value) {
              return linearMap2(value, [0, max_1], [0, itemSize[0]], true);
            });
          }
        }, this);
      }
    };
    VisualMapModel2.prototype.resetItemSize = function() {
      this.itemSize = [parseFloat(this.get("itemWidth")), parseFloat(this.get("itemHeight"))];
    };
    VisualMapModel2.prototype.isCategory = function() {
      return !!this.option.categories;
    };
    VisualMapModel2.prototype.setSelected = function(selected) {
    };
    VisualMapModel2.prototype.getSelected = function() {
      return null;
    };
    VisualMapModel2.prototype.getValueState = function(value) {
      return null;
    };
    VisualMapModel2.prototype.getVisualMeta = function(getColorVisual) {
      return null;
    };
    VisualMapModel2.type = "visualMap";
    VisualMapModel2.dependencies = ["series"];
    VisualMapModel2.defaultOption = {
      show: true,
      // zlevel: 0,
      z: 4,
      // seriesIndex: 'all',
      min: 0,
      max: 200,
      left: 0,
      right: null,
      top: null,
      bottom: 0,
      itemWidth: null,
      itemHeight: null,
      inverse: false,
      orient: "vertical",
      backgroundColor: tokens_default.color.transparent,
      borderColor: tokens_default.color.borderTint,
      contentColor: tokens_default.color.theme[0],
      inactiveColor: tokens_default.color.disabled,
      borderWidth: 0,
      padding: tokens_default.size.m,
      // 接受数组分别设定上右下左边距，同css
      textGap: 10,
      precision: 0,
      textStyle: {
        color: tokens_default.color.secondary
        // 值域文字颜色
      }
    };
    return VisualMapModel2;
  })(Component_default)
);
var VisualMapModel_default = VisualMapModel;

// node_modules/echarts/lib/component/visualMap/ContinuousModel.js
var DEFAULT_BAR_BOUND = [20, 140];
var ContinuousModel = (
  /** @class */
  (function(_super) {
    __extends(ContinuousModel2, _super);
    function ContinuousModel2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = ContinuousModel2.type;
      return _this;
    }
    ContinuousModel2.prototype.optionUpdated = function(newOption, isInit) {
      _super.prototype.optionUpdated.apply(this, arguments);
      this.resetExtent();
      this.resetVisual(function(mappingOption) {
        mappingOption.mappingMethod = "linear";
        mappingOption.dataExtent = this.getExtent();
      });
      this._resetRange();
    };
    ContinuousModel2.prototype.resetItemSize = function() {
      _super.prototype.resetItemSize.apply(this, arguments);
      var itemSize = this.itemSize;
      (itemSize[0] == null || isNaN(itemSize[0])) && (itemSize[0] = DEFAULT_BAR_BOUND[0]);
      (itemSize[1] == null || isNaN(itemSize[1])) && (itemSize[1] = DEFAULT_BAR_BOUND[1]);
    };
    ContinuousModel2.prototype._resetRange = function() {
      var dataExtent = this.getExtent();
      var range = this.option.range;
      if (!range || range.auto) {
        dataExtent.auto = 1;
        this.option.range = dataExtent;
      } else if (isArray(range)) {
        if (range[0] > range[1]) {
          range.reverse();
        }
        range[0] = Math.max(range[0], dataExtent[0]);
        range[1] = Math.min(range[1], dataExtent[1]);
      }
    };
    ContinuousModel2.prototype.completeVisualOption = function() {
      _super.prototype.completeVisualOption.apply(this, arguments);
      each(this.stateList, function(state2) {
        var symbolSize = this.option.controller[state2].symbolSize;
        if (symbolSize && symbolSize[0] !== symbolSize[1]) {
          symbolSize[0] = symbolSize[1] / 3;
        }
      }, this);
    };
    ContinuousModel2.prototype.setSelected = function(selected) {
      this.option.range = selected.slice();
      this._resetRange();
    };
    ContinuousModel2.prototype.getSelected = function() {
      var dataExtent = this.getExtent();
      var dataInterval = asc((this.get("range") || []).slice());
      dataInterval[0] > dataExtent[1] && (dataInterval[0] = dataExtent[1]);
      dataInterval[1] > dataExtent[1] && (dataInterval[1] = dataExtent[1]);
      dataInterval[0] < dataExtent[0] && (dataInterval[0] = dataExtent[0]);
      dataInterval[1] < dataExtent[0] && (dataInterval[1] = dataExtent[0]);
      return dataInterval;
    };
    ContinuousModel2.prototype.getValueState = function(value) {
      var range = this.option.range;
      var dataExtent = this.getExtent();
      var unboundedRange = retrieve2(this.option.unboundedRange, true);
      return (unboundedRange && range[0] <= dataExtent[0] || range[0] <= value) && (unboundedRange && range[1] >= dataExtent[1] || value <= range[1]) ? "inRange" : "outOfRange";
    };
    ContinuousModel2.prototype.findTargetDataIndices = function(range) {
      var result = [];
      this.eachTargetSeries(function(seriesModel) {
        var dataIndices = [];
        var data = seriesModel.getData();
        data.each(this.getDataDimensionIndex(data), function(value, dataIndex) {
          range[0] <= value && value <= range[1] && dataIndices.push(dataIndex);
        }, this);
        result.push({
          seriesId: seriesModel.id,
          dataIndex: dataIndices
        });
      }, this);
      return result;
    };
    ContinuousModel2.prototype.getVisualMeta = function(getColorVisual) {
      var oVals = getColorStopValues(this, "outOfRange", this.getExtent());
      var iVals = getColorStopValues(this, "inRange", this.option.range.slice());
      var stops = [];
      function setStop(value, valueState) {
        stops.push({
          value,
          color: getColorVisual(value, valueState)
        });
      }
      var iIdx = 0;
      var oIdx = 0;
      var iLen = iVals.length;
      var oLen = oVals.length;
      for (; oIdx < oLen && (!iVals.length || oVals[oIdx] <= iVals[0]); oIdx++) {
        if (oVals[oIdx] < iVals[iIdx]) {
          setStop(oVals[oIdx], "outOfRange");
        }
      }
      for (var first = 1; iIdx < iLen; iIdx++, first = 0) {
        first && stops.length && setStop(iVals[iIdx], "outOfRange");
        setStop(iVals[iIdx], "inRange");
      }
      for (var first = 1; oIdx < oLen; oIdx++) {
        if (!iVals.length || iVals[iVals.length - 1] < oVals[oIdx]) {
          if (first) {
            stops.length && setStop(stops[stops.length - 1].value, "outOfRange");
            first = 0;
          }
          setStop(oVals[oIdx], "outOfRange");
        }
      }
      var stopsLen = stops.length;
      return {
        stops,
        outerColors: [stopsLen ? stops[0].color : "transparent", stopsLen ? stops[stopsLen - 1].color : "transparent"]
      };
    };
    ContinuousModel2.type = "visualMap.continuous";
    ContinuousModel2.defaultOption = inheritDefaultOption(VisualMapModel_default.defaultOption, {
      align: "auto",
      calculable: false,
      hoverLink: true,
      realtime: true,
      handleIcon: "path://M-11.39,9.77h0a3.5,3.5,0,0,1-3.5,3.5h-22a3.5,3.5,0,0,1-3.5-3.5h0a3.5,3.5,0,0,1,3.5-3.5h22A3.5,3.5,0,0,1-11.39,9.77Z",
      handleSize: "120%",
      handleStyle: {
        borderColor: tokens_default.color.neutral00,
        borderWidth: 1
      },
      indicatorIcon: "circle",
      indicatorSize: "50%",
      indicatorStyle: {
        borderColor: tokens_default.color.neutral00,
        borderWidth: 2,
        shadowBlur: 2,
        shadowOffsetX: 1,
        shadowOffsetY: 1,
        shadowColor: tokens_default.color.shadow
      }
      // emphasis: {
      //     handleStyle: {
      //         shadowBlur: 3,
      //         shadowOffsetX: 1,
      //         shadowOffsetY: 1,
      //         shadowColor: tokens.color.shadow
      //     }
      // }
    });
    return ContinuousModel2;
  })(VisualMapModel_default)
);
function getColorStopValues(visualMapModel, valueState, dataExtent) {
  if (dataExtent[0] === dataExtent[1]) {
    return dataExtent.slice();
  }
  var count2 = 200;
  var step = (dataExtent[1] - dataExtent[0]) / count2;
  var value = dataExtent[0];
  var stopValues = [];
  for (var i2 = 0; i2 <= count2 && value < dataExtent[1]; i2++) {
    stopValues.push(value);
    value += step;
  }
  stopValues.push(dataExtent[1]);
  return stopValues;
}

// node_modules/echarts/lib/component/visualMap/VisualMapView.js
var VisualMapView = (
  /** @class */
  (function(_super) {
    __extends(VisualMapView2, _super);
    function VisualMapView2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = VisualMapView2.type;
      _this.autoPositionValues = {
        left: 1,
        right: 1,
        top: 1,
        bottom: 1
      };
      return _this;
    }
    VisualMapView2.prototype.init = function(ecModel, api) {
      this.ecModel = ecModel;
      this.api = api;
    };
    VisualMapView2.prototype.render = function(visualMapModel, ecModel, api, payload) {
      this.visualMapModel = visualMapModel;
      if (visualMapModel.get("show") === false) {
        this.group.removeAll();
        return;
      }
      this.doRender(visualMapModel, ecModel, api, payload);
    };
    VisualMapView2.prototype.renderBackground = function(group2) {
      var visualMapModel = this.visualMapModel;
      var padding = normalizeCssArray2(visualMapModel.get("padding") || 0);
      var rect = group2.getBoundingRect();
      group2.add(new Rect_default({
        z2: -1,
        silent: true,
        shape: {
          x: rect.x - padding[3],
          y: rect.y - padding[0],
          width: rect.width + padding[3] + padding[1],
          height: rect.height + padding[0] + padding[2]
        },
        style: {
          fill: visualMapModel.get("backgroundColor"),
          stroke: visualMapModel.get("borderColor"),
          lineWidth: visualMapModel.get("borderWidth")
        }
      }));
    };
    VisualMapView2.prototype.getControllerVisual = function(targetValue, visualCluster, opts) {
      opts = opts || {};
      var forceState = opts.forceState;
      var visualMapModel = this.visualMapModel;
      var visualObj = {};
      if (visualCluster === "color") {
        var defaultColor = visualMapModel.get("contentColor");
        visualObj.color = defaultColor;
      }
      function getter2(key2) {
        return visualObj[key2];
      }
      function setter2(key2, value) {
        visualObj[key2] = value;
      }
      var mappings = visualMapModel.controllerVisuals[forceState || visualMapModel.getValueState(targetValue)];
      var visualTypes = VisualMapping_default.prepareVisualTypes(mappings);
      each(visualTypes, function(type) {
        var visualMapping = mappings[type];
        if (opts.convertOpacityToAlpha && type === "opacity") {
          type = "colorAlpha";
          visualMapping = mappings.__alphaForOpacity;
        }
        if (VisualMapping_default.dependsOn(type, visualCluster)) {
          visualMapping && visualMapping.applyVisual(targetValue, getter2, setter2);
        }
      });
      return visualObj[visualCluster];
    };
    VisualMapView2.prototype.positionGroup = function(group2) {
      var model2 = this.visualMapModel;
      var api = this.api;
      var refContainer = createBoxLayoutReference(model2, api).refContainer;
      positionElement(group2, model2.getBoxLayoutParams(), refContainer);
    };
    VisualMapView2.prototype.doRender = function(visualMapModel, ecModel, api, payload) {
    };
    VisualMapView2.type = "visualMap";
    return VisualMapView2;
  })(Component_default2)
);
var VisualMapView_default = VisualMapView;

// node_modules/echarts/lib/component/visualMap/helper.js
var paramsSet = [["left", "right", "width"], ["top", "bottom", "height"]];
function getItemAlign(visualMapModel, api, itemSize) {
  var modelOption = visualMapModel.option;
  var itemAlign = modelOption.align;
  if (itemAlign != null && itemAlign !== "auto") {
    return itemAlign;
  }
  var ecSize = {
    width: api.getWidth(),
    height: api.getHeight()
  };
  var realIndex = modelOption.orient === "horizontal" ? 1 : 0;
  var reals = paramsSet[realIndex];
  var fakeValue = [0, null, 10];
  var layoutInput = {};
  for (var i2 = 0; i2 < 3; i2++) {
    layoutInput[paramsSet[1 - realIndex][i2]] = fakeValue[i2];
    layoutInput[reals[i2]] = i2 === 2 ? itemSize[0] : modelOption[reals[i2]];
  }
  var rParam = [["x", "width", 3], ["y", "height", 0]][realIndex];
  var rect = getLayoutRect(layoutInput, ecSize, modelOption.padding);
  return reals[(rect.margin[rParam[2]] || 0) + rect[rParam[0]] + rect[rParam[1]] * 0.5 < ecSize[rParam[1]] * 0.5 ? 0 : 1];
}
function makeHighDownBatch(batch, visualMapModel) {
  each(batch || [], function(batchItem) {
    if (batchItem.dataIndex != null) {
      batchItem.dataIndexInside = batchItem.dataIndex;
      batchItem.dataIndex = null;
    }
    batchItem.highlightKey = "visualMap" + (visualMapModel ? visualMapModel.componentIndex : "");
  });
  return batch;
}

// node_modules/echarts/lib/component/visualMap/ContinuousView.js
var linearMap3 = linearMap;
var each11 = each;
var mathMin10 = Math.min;
var mathMax10 = Math.max;
var HOVER_LINK_SIZE = 12;
var HOVER_LINK_OUT = 6;
var ContinuousView = (
  /** @class */
  (function(_super) {
    __extends(ContinuousView2, _super);
    function ContinuousView2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = ContinuousView2.type;
      _this._shapes = {};
      _this._dataInterval = [];
      _this._handleEnds = [];
      _this._hoverLinkDataIndices = [];
      return _this;
    }
    ContinuousView2.prototype.init = function(ecModel, api) {
      _super.prototype.init.call(this, ecModel, api);
      this._hoverLinkFromSeriesMouseOver = bind(this._hoverLinkFromSeriesMouseOver, this);
      this._hideIndicator = bind(this._hideIndicator, this);
    };
    ContinuousView2.prototype.doRender = function(visualMapModel, ecModel, api, payload) {
      if (!payload || payload.type !== "selectDataRange" || payload.from !== this.uid) {
        this._buildView();
      }
    };
    ContinuousView2.prototype._buildView = function() {
      this.group.removeAll();
      var visualMapModel = this.visualMapModel;
      var thisGroup = this.group;
      this._orient = visualMapModel.get("orient");
      this._useHandle = visualMapModel.get("calculable");
      this._resetInterval();
      this._renderBar(thisGroup);
      var dataRangeText = visualMapModel.get("text");
      this._renderEndsText(thisGroup, dataRangeText, 0);
      this._renderEndsText(thisGroup, dataRangeText, 1);
      this._updateView(true);
      this.renderBackground(thisGroup);
      this._updateView();
      this._enableHoverLinkToSeries();
      this._enableHoverLinkFromSeries();
      this.positionGroup(thisGroup);
    };
    ContinuousView2.prototype._renderEndsText = function(group2, dataRangeText, endsIndex) {
      if (!dataRangeText) {
        return;
      }
      var text = dataRangeText[1 - endsIndex];
      text = text != null ? text + "" : "";
      var visualMapModel = this.visualMapModel;
      var textGap = visualMapModel.get("textGap");
      var itemSize = visualMapModel.itemSize;
      var barGroup = this._shapes.mainGroup;
      var position = this._applyTransform([itemSize[0] / 2, endsIndex === 0 ? -textGap : itemSize[1] + textGap], barGroup);
      var align = this._applyTransform(endsIndex === 0 ? "bottom" : "top", barGroup);
      var orient = this._orient;
      var textStyleModel = this.visualMapModel.textStyleModel;
      this.group.add(new Text_default({
        style: createTextStyle(textStyleModel, {
          x: position[0],
          y: position[1],
          verticalAlign: textStyleModel.get("verticalAlign") || (orient === "horizontal" ? "middle" : align),
          align: textStyleModel.get("align") || (orient === "horizontal" ? align : "center"),
          text
        })
      }));
    };
    ContinuousView2.prototype._renderBar = function(targetGroup) {
      var visualMapModel = this.visualMapModel;
      var shapes = this._shapes;
      var itemSize = visualMapModel.itemSize;
      var orient = this._orient;
      var useHandle = this._useHandle;
      var itemAlign = getItemAlign(visualMapModel, this.api, itemSize);
      var mainGroup = shapes.mainGroup = this._createBarGroup(itemAlign);
      var gradientBarGroup = new Group_default();
      mainGroup.add(gradientBarGroup);
      gradientBarGroup.add(shapes.outOfRange = createPolygon());
      gradientBarGroup.add(shapes.inRange = createPolygon(null, useHandle ? getCursor2(this._orient) : null, bind(this._dragHandle, this, "all", false), bind(this._dragHandle, this, "all", true)));
      gradientBarGroup.setClipPath(new Rect_default({
        shape: {
          x: 0,
          y: 0,
          width: itemSize[0],
          height: itemSize[1],
          r: 3
        }
      }));
      var textRect = visualMapModel.textStyleModel.getTextRect("国");
      var textSize = mathMax10(textRect.width, textRect.height);
      if (useHandle) {
        shapes.handleThumbs = [];
        shapes.handleLabels = [];
        shapes.handleLabelPoints = [];
        this._createHandle(visualMapModel, mainGroup, 0, itemSize, textSize, orient);
        this._createHandle(visualMapModel, mainGroup, 1, itemSize, textSize, orient);
      }
      this._createIndicator(visualMapModel, mainGroup, itemSize, textSize, orient);
      targetGroup.add(mainGroup);
    };
    ContinuousView2.prototype._createHandle = function(visualMapModel, mainGroup, handleIndex, itemSize, textSize, orient) {
      var onDrift = bind(this._dragHandle, this, handleIndex, false);
      var onDragEnd = bind(this._dragHandle, this, handleIndex, true);
      var handleSize = parsePercent(visualMapModel.get("handleSize"), itemSize[0]);
      var handleThumb = createSymbol(visualMapModel.get("handleIcon"), -handleSize / 2, -handleSize / 2, handleSize, handleSize, null, true);
      var cursor = getCursor2(this._orient);
      handleThumb.attr({
        cursor,
        draggable: true,
        drift: onDrift,
        ondragend: onDragEnd,
        onmousemove: function(e2) {
          stop(e2.event);
        }
      });
      handleThumb.x = itemSize[0] / 2;
      handleThumb.useStyle(visualMapModel.getModel("handleStyle").getItemStyle());
      handleThumb.setStyle({
        strokeNoScale: true,
        strokeFirst: true
      });
      handleThumb.style.lineWidth *= 2;
      handleThumb.ensureState("emphasis").style = visualMapModel.getModel(["emphasis", "handleStyle"]).getItemStyle();
      setAsHighDownDispatcher(handleThumb, true);
      mainGroup.add(handleThumb);
      var textStyleModel = this.visualMapModel.textStyleModel;
      var handleLabel = new Text_default({
        cursor,
        draggable: true,
        drift: onDrift,
        onmousemove: function(e2) {
          stop(e2.event);
        },
        ondragend: onDragEnd,
        style: createTextStyle(textStyleModel, {
          x: 0,
          y: 0,
          text: ""
        })
      });
      handleLabel.ensureState("blur").style = {
        opacity: 0.1
      };
      handleLabel.stateTransition = {
        duration: 200
      };
      this.group.add(handleLabel);
      var handleLabelPoint = [handleSize, 0];
      var shapes = this._shapes;
      shapes.handleThumbs[handleIndex] = handleThumb;
      shapes.handleLabelPoints[handleIndex] = handleLabelPoint;
      shapes.handleLabels[handleIndex] = handleLabel;
    };
    ContinuousView2.prototype._createIndicator = function(visualMapModel, mainGroup, itemSize, textSize, orient) {
      var scale3 = parsePercent(visualMapModel.get("indicatorSize"), itemSize[0]);
      var indicator = createSymbol(visualMapModel.get("indicatorIcon"), -scale3 / 2, -scale3 / 2, scale3, scale3, null, true);
      indicator.attr({
        cursor: "move",
        invisible: true,
        silent: true,
        x: itemSize[0] / 2
      });
      var indicatorStyle = visualMapModel.getModel("indicatorStyle").getItemStyle();
      if (indicator instanceof Image_default) {
        var pathStyle = indicator.style;
        indicator.useStyle(extend({
          // TODO other properties like x, y ?
          image: pathStyle.image,
          x: pathStyle.x,
          y: pathStyle.y,
          width: pathStyle.width,
          height: pathStyle.height
        }, indicatorStyle));
      } else {
        indicator.useStyle(indicatorStyle);
      }
      mainGroup.add(indicator);
      var textStyleModel = this.visualMapModel.textStyleModel;
      var indicatorLabel = new Text_default({
        silent: true,
        invisible: true,
        style: createTextStyle(textStyleModel, {
          x: 0,
          y: 0,
          text: ""
        })
      });
      this.group.add(indicatorLabel);
      var indicatorLabelPoint = [(orient === "horizontal" ? textSize / 2 : HOVER_LINK_OUT) + itemSize[0] / 2, 0];
      var shapes = this._shapes;
      shapes.indicator = indicator;
      shapes.indicatorLabel = indicatorLabel;
      shapes.indicatorLabelPoint = indicatorLabelPoint;
      this._firstShowIndicator = true;
    };
    ContinuousView2.prototype._dragHandle = function(handleIndex, isEnd, dx, dy) {
      if (!this._useHandle) {
        return;
      }
      this._dragging = !isEnd;
      if (!isEnd) {
        var vertex = this._applyTransform([dx, dy], this._shapes.mainGroup, true);
        this._updateInterval(handleIndex, vertex[1]);
        this._hideIndicator();
        this._updateView();
      }
      if (isEnd === !this.visualMapModel.get("realtime")) {
        this.api.dispatchAction({
          type: "selectDataRange",
          from: this.uid,
          visualMapId: this.visualMapModel.id,
          selected: this._dataInterval.slice()
        });
      }
      if (isEnd) {
        !this._hovering && this._clearHoverLinkToSeries();
      } else if (useHoverLinkOnHandle(this.visualMapModel)) {
        this._doHoverLinkToSeries(this._handleEnds[handleIndex], false);
      }
    };
    ContinuousView2.prototype._resetInterval = function() {
      var visualMapModel = this.visualMapModel;
      var dataInterval = this._dataInterval = visualMapModel.getSelected();
      var dataExtent = visualMapModel.getExtent();
      var sizeExtent = [0, visualMapModel.itemSize[1]];
      this._handleEnds = [linearMap3(dataInterval[0], dataExtent, sizeExtent, true), linearMap3(dataInterval[1], dataExtent, sizeExtent, true)];
    };
    ContinuousView2.prototype._updateInterval = function(handleIndex, delta) {
      delta = delta || 0;
      var visualMapModel = this.visualMapModel;
      var handleEnds = this._handleEnds;
      var sizeExtent = [0, visualMapModel.itemSize[1]];
      sliderMove(
        delta,
        handleEnds,
        sizeExtent,
        handleIndex,
        // cross is forbidden
        0
      );
      var dataExtent = visualMapModel.getExtent();
      this._dataInterval = [linearMap3(handleEnds[0], sizeExtent, dataExtent, true), linearMap3(handleEnds[1], sizeExtent, dataExtent, true)];
    };
    ContinuousView2.prototype._updateView = function(forSketch) {
      var visualMapModel = this.visualMapModel;
      var dataExtent = visualMapModel.getExtent();
      var shapes = this._shapes;
      var outOfRangeHandleEnds = [0, visualMapModel.itemSize[1]];
      var inRangeHandleEnds = forSketch ? outOfRangeHandleEnds : this._handleEnds;
      var visualInRange = this._createBarVisual(this._dataInterval, dataExtent, inRangeHandleEnds, "inRange");
      var visualOutOfRange = this._createBarVisual(dataExtent, dataExtent, outOfRangeHandleEnds, "outOfRange");
      shapes.inRange.setStyle({
        fill: visualInRange.barColor
        // opacity: visualInRange.opacity
      }).setShape("points", visualInRange.barPoints);
      shapes.outOfRange.setStyle({
        fill: visualOutOfRange.barColor
        // opacity: visualOutOfRange.opacity
      }).setShape("points", visualOutOfRange.barPoints);
      this._updateHandle(inRangeHandleEnds, visualInRange);
    };
    ContinuousView2.prototype._createBarVisual = function(dataInterval, dataExtent, handleEnds, forceState) {
      var opts = {
        forceState,
        convertOpacityToAlpha: true
      };
      var colorStops = this._makeColorGradient(dataInterval, opts);
      var symbolSizes = [this.getControllerVisual(dataInterval[0], "symbolSize", opts), this.getControllerVisual(dataInterval[1], "symbolSize", opts)];
      var barPoints = this._createBarPoints(handleEnds, symbolSizes);
      return {
        barColor: new LinearGradient_default(0, 0, 0, 1, colorStops),
        barPoints,
        handlesColor: [colorStops[0].color, colorStops[colorStops.length - 1].color]
      };
    };
    ContinuousView2.prototype._makeColorGradient = function(dataInterval, opts) {
      var sampleNumber = 100;
      var colorStops = [];
      var step = (dataInterval[1] - dataInterval[0]) / sampleNumber;
      colorStops.push({
        color: this.getControllerVisual(dataInterval[0], "color", opts),
        offset: 0
      });
      for (var i2 = 1; i2 < sampleNumber; i2++) {
        var currValue = dataInterval[0] + step * i2;
        if (currValue > dataInterval[1]) {
          break;
        }
        colorStops.push({
          color: this.getControllerVisual(currValue, "color", opts),
          offset: i2 / sampleNumber
        });
      }
      colorStops.push({
        color: this.getControllerVisual(dataInterval[1], "color", opts),
        offset: 1
      });
      return colorStops;
    };
    ContinuousView2.prototype._createBarPoints = function(handleEnds, symbolSizes) {
      var itemSize = this.visualMapModel.itemSize;
      return [[itemSize[0] - symbolSizes[0], handleEnds[0]], [itemSize[0], handleEnds[0]], [itemSize[0], handleEnds[1]], [itemSize[0] - symbolSizes[1], handleEnds[1]]];
    };
    ContinuousView2.prototype._createBarGroup = function(itemAlign) {
      var orient = this._orient;
      var inverse = this.visualMapModel.get("inverse");
      return new Group_default(orient === "horizontal" && !inverse ? {
        scaleX: itemAlign === "bottom" ? 1 : -1,
        rotation: Math.PI / 2
      } : orient === "horizontal" && inverse ? {
        scaleX: itemAlign === "bottom" ? -1 : 1,
        rotation: -Math.PI / 2
      } : orient === "vertical" && !inverse ? {
        scaleX: itemAlign === "left" ? 1 : -1,
        scaleY: -1
      } : {
        scaleX: itemAlign === "left" ? 1 : -1
      });
    };
    ContinuousView2.prototype._updateHandle = function(handleEnds, visualInRange) {
      if (!this._useHandle) {
        return;
      }
      var shapes = this._shapes;
      var visualMapModel = this.visualMapModel;
      var handleThumbs = shapes.handleThumbs;
      var handleLabels = shapes.handleLabels;
      var itemSize = visualMapModel.itemSize;
      var dataExtent = visualMapModel.getExtent();
      var align = this._applyTransform("left", shapes.mainGroup);
      each11([0, 1], function(handleIndex) {
        var handleThumb = handleThumbs[handleIndex];
        handleThumb.setStyle("fill", visualInRange.handlesColor[handleIndex]);
        handleThumb.y = handleEnds[handleIndex];
        var val = linearMap3(handleEnds[handleIndex], [0, itemSize[1]], dataExtent, true);
        var symbolSize = this.getControllerVisual(val, "symbolSize");
        handleThumb.scaleX = handleThumb.scaleY = symbolSize / itemSize[0];
        handleThumb.x = itemSize[0] - symbolSize / 2;
        var textPoint = applyTransform2(shapes.handleLabelPoints[handleIndex], getTransform2(handleThumb, this.group));
        if (this._orient === "horizontal") {
          var minimumOffset = align === "left" || align === "top" ? (itemSize[0] - symbolSize) / 2 : (itemSize[0] - symbolSize) / -2;
          textPoint[1] += minimumOffset;
        }
        handleLabels[handleIndex].setStyle({
          x: textPoint[0],
          y: textPoint[1],
          text: visualMapModel.formatValueText(this._dataInterval[handleIndex]),
          verticalAlign: "middle",
          align: this._orient === "vertical" ? this._applyTransform("left", shapes.mainGroup) : "center"
        });
      }, this);
    };
    ContinuousView2.prototype._showIndicator = function(cursorValue, textValue, rangeSymbol, halfHoverLinkSize) {
      var visualMapModel = this.visualMapModel;
      var dataExtent = visualMapModel.getExtent();
      var itemSize = visualMapModel.itemSize;
      var sizeExtent = [0, itemSize[1]];
      var shapes = this._shapes;
      var indicator = shapes.indicator;
      if (!indicator) {
        return;
      }
      indicator.attr("invisible", false);
      var opts = {
        convertOpacityToAlpha: true
      };
      var color3 = this.getControllerVisual(cursorValue, "color", opts);
      var symbolSize = this.getControllerVisual(cursorValue, "symbolSize");
      var y = linearMap3(cursorValue, dataExtent, sizeExtent, true);
      var x = itemSize[0] - symbolSize / 2;
      var oldIndicatorPos = {
        x: indicator.x,
        y: indicator.y
      };
      indicator.y = y;
      indicator.x = x;
      var textPoint = applyTransform2(shapes.indicatorLabelPoint, getTransform2(indicator, this.group));
      var indicatorLabel = shapes.indicatorLabel;
      indicatorLabel.attr("invisible", false);
      var align = this._applyTransform("left", shapes.mainGroup);
      var orient = this._orient;
      var isHorizontal = orient === "horizontal";
      indicatorLabel.setStyle({
        text: (rangeSymbol ? rangeSymbol : "") + visualMapModel.formatValueText(textValue),
        verticalAlign: isHorizontal ? align : "middle",
        align: isHorizontal ? "center" : align
      });
      var indicatorNewProps = {
        x,
        y,
        style: {
          fill: color3
        }
      };
      var labelNewProps = {
        style: {
          x: textPoint[0],
          y: textPoint[1]
        }
      };
      if (visualMapModel.ecModel.isAnimationEnabled() && !this._firstShowIndicator) {
        var animationCfg = {
          duration: 100,
          easing: "cubicInOut",
          additive: true
        };
        indicator.x = oldIndicatorPos.x;
        indicator.y = oldIndicatorPos.y;
        indicator.animateTo(indicatorNewProps, animationCfg);
        indicatorLabel.animateTo(labelNewProps, animationCfg);
      } else {
        indicator.attr(indicatorNewProps);
        indicatorLabel.attr(labelNewProps);
      }
      this._firstShowIndicator = false;
      var handleLabels = this._shapes.handleLabels;
      if (handleLabels) {
        for (var i2 = 0; i2 < handleLabels.length; i2++) {
          this.api.enterBlur(handleLabels[i2]);
        }
      }
    };
    ContinuousView2.prototype._enableHoverLinkToSeries = function() {
      var self2 = this;
      this._shapes.mainGroup.on("mousemove", function(e2) {
        self2._hovering = true;
        if (!self2._dragging) {
          var itemSize = self2.visualMapModel.itemSize;
          var pos = self2._applyTransform([e2.offsetX, e2.offsetY], self2._shapes.mainGroup, true, true);
          pos[1] = mathMin10(mathMax10(0, pos[1]), itemSize[1]);
          self2._doHoverLinkToSeries(pos[1], 0 <= pos[0] && pos[0] <= itemSize[0]);
        }
      }).on("mouseout", function() {
        self2._hovering = false;
        !self2._dragging && self2._clearHoverLinkToSeries();
      });
    };
    ContinuousView2.prototype._enableHoverLinkFromSeries = function() {
      var zr = this.api.getZr();
      if (this.visualMapModel.option.hoverLink) {
        zr.on("mouseover", this._hoverLinkFromSeriesMouseOver, this);
        zr.on("mouseout", this._hideIndicator, this);
      } else {
        this._clearHoverLinkFromSeries();
      }
    };
    ContinuousView2.prototype._doHoverLinkToSeries = function(cursorPos, hoverOnBar) {
      var visualMapModel = this.visualMapModel;
      var itemSize = visualMapModel.itemSize;
      if (!visualMapModel.option.hoverLink) {
        return;
      }
      var sizeExtent = [0, itemSize[1]];
      var dataExtent = visualMapModel.getExtent();
      cursorPos = mathMin10(mathMax10(sizeExtent[0], cursorPos), sizeExtent[1]);
      var halfHoverLinkSize = getHalfHoverLinkSize(visualMapModel, dataExtent, sizeExtent);
      var hoverRange = [cursorPos - halfHoverLinkSize, cursorPos + halfHoverLinkSize];
      var cursorValue = linearMap3(cursorPos, sizeExtent, dataExtent, true);
      var valueRange = [linearMap3(hoverRange[0], sizeExtent, dataExtent, true), linearMap3(hoverRange[1], sizeExtent, dataExtent, true)];
      hoverRange[0] < sizeExtent[0] && (valueRange[0] = -Infinity);
      hoverRange[1] > sizeExtent[1] && (valueRange[1] = Infinity);
      if (hoverOnBar) {
        if (valueRange[0] === -Infinity) {
          this._showIndicator(cursorValue, valueRange[1], "< ", halfHoverLinkSize);
        } else if (valueRange[1] === Infinity) {
          this._showIndicator(cursorValue, valueRange[0], "> ", halfHoverLinkSize);
        } else {
          this._showIndicator(cursorValue, cursorValue, "≈ ", halfHoverLinkSize);
        }
      }
      var oldBatch = this._hoverLinkDataIndices;
      var newBatch = [];
      if (hoverOnBar || useHoverLinkOnHandle(visualMapModel)) {
        newBatch = this._hoverLinkDataIndices = visualMapModel.findTargetDataIndices(valueRange);
      }
      var resultBatches = compressBatches(oldBatch, newBatch);
      this._dispatchHighDown("downplay", makeHighDownBatch(resultBatches[0], visualMapModel));
      this._dispatchHighDown("highlight", makeHighDownBatch(resultBatches[1], visualMapModel));
    };
    ContinuousView2.prototype._hoverLinkFromSeriesMouseOver = function(e2) {
      var ecData;
      findEventDispatcher(e2.target, function(target) {
        var currECData = getECData(target);
        if (currECData.dataIndex != null) {
          ecData = currECData;
          return true;
        }
      }, true);
      if (!ecData) {
        return;
      }
      var dataModel = this.ecModel.getSeriesByIndex(ecData.seriesIndex);
      var visualMapModel = this.visualMapModel;
      if (!visualMapModel.isTargetSeries(dataModel)) {
        return;
      }
      var data = dataModel.getData(ecData.dataType);
      var value = data.getStore().get(visualMapModel.getDataDimensionIndex(data), ecData.dataIndex);
      if (!isNaN(value)) {
        this._showIndicator(value, value);
      }
    };
    ContinuousView2.prototype._hideIndicator = function() {
      var shapes = this._shapes;
      shapes.indicator && shapes.indicator.attr("invisible", true);
      shapes.indicatorLabel && shapes.indicatorLabel.attr("invisible", true);
      var handleLabels = this._shapes.handleLabels;
      if (handleLabels) {
        for (var i2 = 0; i2 < handleLabels.length; i2++) {
          this.api.leaveBlur(handleLabels[i2]);
        }
      }
    };
    ContinuousView2.prototype._clearHoverLinkToSeries = function() {
      this._hideIndicator();
      var indices = this._hoverLinkDataIndices;
      this._dispatchHighDown("downplay", makeHighDownBatch(indices, this.visualMapModel));
      indices.length = 0;
    };
    ContinuousView2.prototype._clearHoverLinkFromSeries = function() {
      this._hideIndicator();
      var zr = this.api.getZr();
      zr.off("mouseover", this._hoverLinkFromSeriesMouseOver);
      zr.off("mouseout", this._hideIndicator);
    };
    ContinuousView2.prototype._applyTransform = function(vertex, element, inverse, global2) {
      var transform = getTransform2(element, global2 ? null : this.group);
      return isArray(vertex) ? applyTransform2(vertex, transform, inverse) : transformDirection(vertex, transform, inverse);
    };
    ContinuousView2.prototype._dispatchHighDown = function(type, batch) {
      batch && batch.length && this.api.dispatchAction({
        type,
        batch
      });
    };
    ContinuousView2.prototype.dispose = function() {
      this._clearHoverLinkFromSeries();
      this._clearHoverLinkToSeries();
    };
    ContinuousView2.type = "visualMap.continuous";
    return ContinuousView2;
  })(VisualMapView_default)
);
function createPolygon(points4, cursor, onDrift, onDragEnd) {
  return new Polygon_default({
    shape: {
      points: points4
    },
    draggable: !!onDrift,
    cursor,
    drift: onDrift,
    onmousemove: function(e2) {
      stop(e2.event);
    },
    ondragend: onDragEnd
  });
}
function getHalfHoverLinkSize(visualMapModel, dataExtent, sizeExtent) {
  var halfHoverLinkSize = HOVER_LINK_SIZE / 2;
  var hoverLinkDataSize = visualMapModel.get("hoverLinkDataSize");
  if (hoverLinkDataSize) {
    halfHoverLinkSize = linearMap3(hoverLinkDataSize, dataExtent, sizeExtent, true) / 2;
  }
  return halfHoverLinkSize;
}
function useHoverLinkOnHandle(visualMapModel) {
  var hoverLinkOnHandle = visualMapModel.get("hoverLinkOnHandle");
  return !!(hoverLinkOnHandle == null ? visualMapModel.get("realtime") : hoverLinkOnHandle);
}
function getCursor2(orient) {
  return orient === "vertical" ? "ns-resize" : "ew-resize";
}

// node_modules/echarts/lib/component/visualMap/PiecewiseModel.js
var PiecewiseModel = (
  /** @class */
  (function(_super) {
    __extends(PiecewiseModel2, _super);
    function PiecewiseModel2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = PiecewiseModel2.type;
      _this._pieceList = [];
      return _this;
    }
    PiecewiseModel2.prototype.optionUpdated = function(newOption, isInit) {
      _super.prototype.optionUpdated.apply(this, arguments);
      this.resetExtent();
      var mode = this._mode = this._determineMode();
      this._pieceList = [];
      resetMethods[this._mode].call(this, this._pieceList);
      this._resetSelected(newOption, isInit);
      var categories = this.option.categories;
      this.resetVisual(function(mappingOption, state2) {
        if (mode === "categories") {
          mappingOption.mappingMethod = "category";
          mappingOption.categories = clone(categories);
        } else {
          mappingOption.dataExtent = this.getExtent();
          mappingOption.mappingMethod = "piecewise";
          mappingOption.pieceList = map2(this._pieceList, function(piece) {
            piece = clone(piece);
            if (state2 !== "inRange") {
              piece.visual = null;
            }
            return piece;
          });
        }
      });
    };
    PiecewiseModel2.prototype.completeVisualOption = function() {
      var option = this.option;
      var visualTypesInPieces = {};
      var visualTypes = VisualMapping_default.listVisualTypes();
      var isCategory2 = this.isCategory();
      each(option.pieces, function(piece) {
        each(visualTypes, function(visualType) {
          if (piece.hasOwnProperty(visualType)) {
            visualTypesInPieces[visualType] = 1;
          }
        });
      });
      each(visualTypesInPieces, function(v, visualType) {
        var exists = false;
        each(this.stateList, function(state2) {
          exists = exists || has(option, state2, visualType) || has(option.target, state2, visualType);
        }, this);
        !exists && each(this.stateList, function(state2) {
          (option[state2] || (option[state2] = {}))[visualType] = visualDefault_default.get(visualType, state2 === "inRange" ? "active" : "inactive", isCategory2);
        });
      }, this);
      function has(obj, state2, visualType) {
        return obj && obj[state2] && obj[state2].hasOwnProperty(visualType);
      }
      _super.prototype.completeVisualOption.apply(this, arguments);
    };
    PiecewiseModel2.prototype._resetSelected = function(newOption, isInit) {
      var thisOption = this.option;
      var pieceList = this._pieceList;
      var selected = (isInit ? thisOption : newOption).selected || {};
      thisOption.selected = selected;
      each(pieceList, function(piece, index) {
        var key2 = this.getSelectedMapKey(piece);
        if (!selected.hasOwnProperty(key2)) {
          selected[key2] = true;
        }
      }, this);
      if (thisOption.selectedMode === "single") {
        var hasSel_1 = false;
        each(pieceList, function(piece, index) {
          var key2 = this.getSelectedMapKey(piece);
          if (selected[key2]) {
            hasSel_1 ? selected[key2] = false : hasSel_1 = true;
          }
        }, this);
      }
    };
    PiecewiseModel2.prototype.getItemSymbol = function() {
      return this.get("itemSymbol");
    };
    PiecewiseModel2.prototype.getSelectedMapKey = function(piece) {
      return this._mode === "categories" ? piece.value + "" : piece.index + "";
    };
    PiecewiseModel2.prototype.getPieceList = function() {
      return this._pieceList;
    };
    PiecewiseModel2.prototype._determineMode = function() {
      var option = this.option;
      return option.pieces && option.pieces.length > 0 ? "pieces" : this.option.categories ? "categories" : "splitNumber";
    };
    PiecewiseModel2.prototype.setSelected = function(selected) {
      this.option.selected = clone(selected);
    };
    PiecewiseModel2.prototype.getValueState = function(value) {
      var index = VisualMapping_default.findPieceIndex(value, this._pieceList);
      return index != null ? this.option.selected[this.getSelectedMapKey(this._pieceList[index])] ? "inRange" : "outOfRange" : "outOfRange";
    };
    PiecewiseModel2.prototype.findTargetDataIndices = function(pieceIndex) {
      var result = [];
      var pieceList = this._pieceList;
      this.eachTargetSeries(function(seriesModel) {
        var dataIndices = [];
        var data = seriesModel.getData();
        data.each(this.getDataDimensionIndex(data), function(value, dataIndex) {
          var pIdx = VisualMapping_default.findPieceIndex(value, pieceList);
          pIdx === pieceIndex && dataIndices.push(dataIndex);
        }, this);
        result.push({
          seriesId: seriesModel.id,
          dataIndex: dataIndices
        });
      }, this);
      return result;
    };
    PiecewiseModel2.prototype.getRepresentValue = function(piece) {
      var representValue;
      if (this.isCategory()) {
        representValue = piece.value;
      } else {
        if (piece.value != null) {
          representValue = piece.value;
        } else {
          var pieceInterval = piece.interval || [];
          representValue = pieceInterval[0] === -Infinity && pieceInterval[1] === Infinity ? 0 : (pieceInterval[0] + pieceInterval[1]) / 2;
        }
      }
      return representValue;
    };
    PiecewiseModel2.prototype.getVisualMeta = function(getColorVisual) {
      if (this.isCategory()) {
        return;
      }
      var stops = [];
      var outerColors = ["", ""];
      var visualMapModel = this;
      function setStop(interval2, valueState) {
        var representValue = visualMapModel.getRepresentValue({
          interval: interval2
        });
        if (!valueState) {
          valueState = visualMapModel.getValueState(representValue);
        }
        var color3 = getColorVisual(representValue, valueState);
        if (interval2[0] === -Infinity) {
          outerColors[0] = color3;
        } else if (interval2[1] === Infinity) {
          outerColors[1] = color3;
        } else {
          stops.push({
            value: interval2[0],
            color: color3
          }, {
            value: interval2[1],
            color: color3
          });
        }
      }
      var pieceList = this._pieceList.slice();
      if (!pieceList.length) {
        pieceList.push({
          interval: [-Infinity, Infinity]
        });
      } else {
        var edge = pieceList[0].interval[0];
        edge !== -Infinity && pieceList.unshift({
          interval: [-Infinity, edge]
        });
        edge = pieceList[pieceList.length - 1].interval[1];
        edge !== Infinity && pieceList.push({
          interval: [edge, Infinity]
        });
      }
      var curr = -Infinity;
      each(pieceList, function(piece) {
        var interval2 = piece.interval;
        if (interval2) {
          interval2[0] > curr && setStop([curr, interval2[0]], "outOfRange");
          setStop(interval2.slice());
          curr = interval2[1];
        }
      }, this);
      return {
        stops,
        outerColors
      };
    };
    PiecewiseModel2.type = "visualMap.piecewise";
    PiecewiseModel2.defaultOption = inheritDefaultOption(VisualMapModel_default.defaultOption, {
      selected: null,
      minOpen: false,
      maxOpen: false,
      align: "auto",
      itemWidth: 20,
      itemHeight: 14,
      itemSymbol: "roundRect",
      pieces: null,
      categories: null,
      splitNumber: 5,
      selectedMode: "multiple",
      itemGap: 10,
      hoverLink: true
      // Enable hover highlight.
    });
    return PiecewiseModel2;
  })(VisualMapModel_default)
);
var resetMethods = {
  splitNumber: function(outPieceList) {
    var thisOption = this.option;
    var precision = Math.min(thisOption.precision, 20);
    var dataExtent = this.getExtent();
    var splitNumber = thisOption.splitNumber;
    splitNumber = Math.max(parseInt(splitNumber, 10), 1);
    thisOption.splitNumber = splitNumber;
    var splitStep = (dataExtent[1] - dataExtent[0]) / splitNumber;
    while (+splitStep.toFixed(precision) !== splitStep && precision < 5) {
      precision++;
    }
    thisOption.precision = precision;
    splitStep = +splitStep.toFixed(precision);
    if (thisOption.minOpen) {
      outPieceList.push({
        interval: [-Infinity, dataExtent[0]],
        close: [0, 0]
      });
    }
    for (var index = 0, curr = dataExtent[0]; index < splitNumber; curr += splitStep, index++) {
      var max3 = index === splitNumber - 1 ? dataExtent[1] : curr + splitStep;
      outPieceList.push({
        interval: [curr, max3],
        close: [1, 1]
      });
    }
    if (thisOption.maxOpen) {
      outPieceList.push({
        interval: [dataExtent[1], Infinity],
        close: [0, 0]
      });
    }
    reformIntervals(outPieceList);
    each(outPieceList, function(piece, index2) {
      piece.index = index2;
      piece.text = this.formatValueText(piece.interval);
    }, this);
  },
  categories: function(outPieceList) {
    var thisOption = this.option;
    each(thisOption.categories, function(cate) {
      outPieceList.push({
        text: this.formatValueText(cate, true),
        value: cate
      });
    }, this);
    normalizeReverse(thisOption, outPieceList);
  },
  pieces: function(outPieceList) {
    var thisOption = this.option;
    each(thisOption.pieces, function(pieceListItem, index) {
      if (!isObject(pieceListItem)) {
        pieceListItem = {
          value: pieceListItem
        };
      }
      var item = {
        text: "",
        index
      };
      if (pieceListItem.label != null) {
        item.text = pieceListItem.label;
      }
      if (pieceListItem.hasOwnProperty("value")) {
        var value = item.value = pieceListItem.value;
        item.interval = [value, value];
        item.close = [1, 1];
      } else {
        var interval2 = item.interval = [];
        var close_1 = item.close = [0, 0];
        var closeList = [1, 0, 1];
        var infinityList = [-Infinity, Infinity];
        var useMinMax = [];
        for (var lg = 0; lg < 2; lg++) {
          var names = [["gte", "gt", "min"], ["lte", "lt", "max"]][lg];
          for (var i2 = 0; i2 < 3 && interval2[lg] == null; i2++) {
            interval2[lg] = pieceListItem[names[i2]];
            close_1[lg] = closeList[i2];
            useMinMax[lg] = i2 === 2;
          }
          interval2[lg] == null && (interval2[lg] = infinityList[lg]);
        }
        useMinMax[0] && interval2[1] === Infinity && (close_1[0] = 0);
        useMinMax[1] && interval2[0] === -Infinity && (close_1[1] = 0);
        if (true) {
          if (interval2[0] > interval2[1]) {
            console.warn("Piece " + index + "is illegal: " + interval2 + " lower bound should not greater then uppper bound.");
          }
        }
        if (interval2[0] === interval2[1] && close_1[0] && close_1[1]) {
          item.value = interval2[0];
        }
      }
      item.visual = VisualMapping_default.retrieveVisuals(pieceListItem);
      outPieceList.push(item);
    }, this);
    normalizeReverse(thisOption, outPieceList);
    reformIntervals(outPieceList);
    each(outPieceList, function(piece) {
      var close = piece.close;
      var edgeSymbols = [["<", "≤"][close[1]], [">", "≥"][close[0]]];
      piece.text = piece.text || this.formatValueText(piece.value != null ? piece.value : piece.interval, false, edgeSymbols);
    }, this);
  }
};
function normalizeReverse(thisOption, pieceList) {
  var inverse = thisOption.inverse;
  if (thisOption.orient === "vertical" ? !inverse : inverse) {
    pieceList.reverse();
  }
}

// node_modules/echarts/lib/component/visualMap/PiecewiseView.js
var PiecewiseVisualMapView = (
  /** @class */
  (function(_super) {
    __extends(PiecewiseVisualMapView2, _super);
    function PiecewiseVisualMapView2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = PiecewiseVisualMapView2.type;
      return _this;
    }
    PiecewiseVisualMapView2.prototype.doRender = function() {
      var thisGroup = this.group;
      thisGroup.removeAll();
      var visualMapModel = this.visualMapModel;
      var textGap = visualMapModel.get("textGap");
      var textStyleModel = visualMapModel.textStyleModel;
      var itemAlign = this._getItemAlign();
      var itemSize = visualMapModel.itemSize;
      var viewData = this._getViewData();
      var endsText = viewData.endsText;
      var showLabel = retrieve(visualMapModel.get("showLabel", true), !endsText);
      var silent = !visualMapModel.get("selectedMode");
      endsText && this._renderEndsText(thisGroup, endsText[0], itemSize, showLabel, itemAlign);
      each(viewData.viewPieceList, function(item) {
        var piece = item.piece;
        var itemGroup = new Group_default();
        itemGroup.onclick = bind(this._onItemClick, this, piece);
        this._enableHoverLink(itemGroup, item.indexInModelPieceList);
        var representValue = visualMapModel.getRepresentValue(piece);
        this._createItemSymbol(itemGroup, representValue, [0, 0, itemSize[0], itemSize[1]], silent);
        if (showLabel) {
          var visualState = this.visualMapModel.getValueState(representValue);
          var align = textStyleModel.get("align") || itemAlign;
          itemGroup.add(new Text_default({
            style: createTextStyle(textStyleModel, {
              x: align === "right" ? -textGap : itemSize[0] + textGap,
              y: itemSize[1] / 2,
              text: piece.text,
              verticalAlign: textStyleModel.get("verticalAlign") || "middle",
              align,
              opacity: retrieve2(textStyleModel.get("opacity"), visualState === "outOfRange" ? 0.5 : 1)
            }),
            silent
          }));
        }
        thisGroup.add(itemGroup);
      }, this);
      endsText && this._renderEndsText(thisGroup, endsText[1], itemSize, showLabel, itemAlign);
      box(visualMapModel.get("orient"), thisGroup, visualMapModel.get("itemGap"));
      this.renderBackground(thisGroup);
      this.positionGroup(thisGroup);
    };
    PiecewiseVisualMapView2.prototype._enableHoverLink = function(itemGroup, pieceIndex) {
      var _this = this;
      itemGroup.on("mouseover", function() {
        return onHoverLink("highlight");
      }).on("mouseout", function() {
        return onHoverLink("downplay");
      });
      var onHoverLink = function(method) {
        var visualMapModel = _this.visualMapModel;
        visualMapModel.option.hoverLink && _this.api.dispatchAction({
          type: method,
          batch: makeHighDownBatch(visualMapModel.findTargetDataIndices(pieceIndex), visualMapModel)
        });
      };
    };
    PiecewiseVisualMapView2.prototype._getItemAlign = function() {
      var visualMapModel = this.visualMapModel;
      var modelOption = visualMapModel.option;
      if (modelOption.orient === "vertical") {
        return getItemAlign(visualMapModel, this.api, visualMapModel.itemSize);
      } else {
        var align = modelOption.align;
        if (!align || align === "auto") {
          align = "left";
        }
        return align;
      }
    };
    PiecewiseVisualMapView2.prototype._renderEndsText = function(group2, text, itemSize, showLabel, itemAlign) {
      if (!text) {
        return;
      }
      var itemGroup = new Group_default();
      var textStyleModel = this.visualMapModel.textStyleModel;
      itemGroup.add(new Text_default({
        style: createTextStyle(textStyleModel, {
          x: showLabel ? itemAlign === "right" ? itemSize[0] : 0 : itemSize[0] / 2,
          y: itemSize[1] / 2,
          verticalAlign: "middle",
          align: showLabel ? itemAlign : "center",
          text
        })
      }));
      group2.add(itemGroup);
    };
    PiecewiseVisualMapView2.prototype._getViewData = function() {
      var visualMapModel = this.visualMapModel;
      var viewPieceList = map2(visualMapModel.getPieceList(), function(piece, index) {
        return {
          piece,
          indexInModelPieceList: index
        };
      });
      var endsText = visualMapModel.get("text");
      var orient = visualMapModel.get("orient");
      var inverse = visualMapModel.get("inverse");
      if (orient === "horizontal" ? inverse : !inverse) {
        viewPieceList.reverse();
      } else if (endsText) {
        endsText = endsText.slice().reverse();
      }
      return {
        viewPieceList,
        endsText
      };
    };
    PiecewiseVisualMapView2.prototype._createItemSymbol = function(group2, representValue, shapeParam, silent) {
      var itemSymbol = createSymbol(
        // symbol will be string
        this.getControllerVisual(representValue, "symbol"),
        shapeParam[0],
        shapeParam[1],
        shapeParam[2],
        shapeParam[3],
        // color will be string
        this.getControllerVisual(representValue, "color")
      );
      itemSymbol.silent = silent;
      group2.add(itemSymbol);
    };
    PiecewiseVisualMapView2.prototype._onItemClick = function(piece) {
      var visualMapModel = this.visualMapModel;
      var option = visualMapModel.option;
      var selectedMode = option.selectedMode;
      if (!selectedMode) {
        return;
      }
      var selected = clone(option.selected);
      var newKey = visualMapModel.getSelectedMapKey(piece);
      if (selectedMode === "single" || selectedMode === true) {
        selected[newKey] = true;
        each(selected, function(o, key2) {
          selected[key2] = key2 === newKey;
        });
      } else {
        selected[newKey] = !selected[newKey];
      }
      this.api.dispatchAction({
        type: "selectDataRange",
        from: this.uid,
        visualMapId: this.visualMapModel.id,
        selected
      });
    };
    PiecewiseVisualMapView2.type = "visualMap.piecewise";
    return PiecewiseVisualMapView2;
  })(VisualMapView_default)
);

// node_modules/echarts/lib/component/thumbnail/ThumbnailBridgeImpl.js
var ThumbnailBridgeImpl = (
  /** @class */
  (function() {
    function ThumbnailBridgeImpl2(thumbnailModel) {
      this._thumbnailModel = thumbnailModel;
    }
    ThumbnailBridgeImpl2.prototype.reset = function(api) {
      this._renderVersion = api.getECUpdateCycleVersion();
    };
    ThumbnailBridgeImpl2.prototype.renderContent = function(opt) {
      var thumbnailView = opt.api.getViewOfComponentModel(this._thumbnailModel);
      if (!thumbnailView) {
        return;
      }
      opt.group.silent = true;
      thumbnailView.renderContent({
        group: opt.group,
        targetTrans: opt.targetTrans,
        z2Range: calcZ2Range(opt.group),
        roamType: opt.roamType,
        viewportRect: opt.viewportRect,
        renderVersion: this._renderVersion
      });
    };
    ThumbnailBridgeImpl2.prototype.updateWindow = function(targetTrans, api) {
      var thumbnailView = api.getViewOfComponentModel(this._thumbnailModel);
      if (!thumbnailView) {
        return;
      }
      thumbnailView.updateWindow({
        targetTrans,
        renderVersion: this._renderVersion
      });
    };
    return ThumbnailBridgeImpl2;
  })()
);

// node_modules/echarts/lib/component/thumbnail/ThumbnailModel.js
var ThumbnailModel = (
  /** @class */
  (function(_super) {
    __extends(ThumbnailModel2, _super);
    function ThumbnailModel2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = ThumbnailModel2.type;
      _this.preventAutoZ = true;
      return _this;
    }
    ThumbnailModel2.prototype.optionUpdated = function(newCptOption, isInit) {
      this._updateBridge();
    };
    ThumbnailModel2.prototype._updateBridge = function() {
      var bridge = this._birdge = this._birdge || new ThumbnailBridgeImpl(this);
      this._target = null;
      this.ecModel.eachSeries(function(series) {
        injectThumbnailBridge(series, null);
      });
      if (this.shouldShow()) {
        var target = this.getTarget();
        injectThumbnailBridge(target.baseMapProvider, bridge);
      }
    };
    ThumbnailModel2.prototype.shouldShow = function() {
      return this.getShallow("show", true);
    };
    ThumbnailModel2.prototype.getBridge = function() {
      return this._birdge;
    };
    ThumbnailModel2.prototype.getTarget = function() {
      if (this._target) {
        return this._target;
      }
      var series = this.getReferringComponents("series", {
        useDefault: false,
        enableAll: false,
        enableNone: false
      }).models[0];
      if (series) {
        if (series.subType !== "graph") {
          series = null;
          if (true) {
            error("series." + series.subType + " is not supported in thumbnail.", true);
          }
        }
      } else {
        series = this.ecModel.queryComponents({
          mainType: "series",
          subType: "graph"
        })[0];
      }
      this._target = {
        baseMapProvider: series
      };
      return this._target;
    };
    ThumbnailModel2.type = "thumbnail";
    ThumbnailModel2.layoutMode = "box";
    ThumbnailModel2.dependencies = ["series", "geo"];
    ThumbnailModel2.defaultOption = {
      show: true,
      right: 1,
      bottom: 1,
      height: "25%",
      width: "25%",
      itemStyle: {
        // Use echarts option.backgorundColor by default.
        borderColor: tokens_default.color.border,
        borderWidth: 2
      },
      windowStyle: {
        borderWidth: 1,
        color: tokens_default.color.neutral30,
        borderColor: tokens_default.color.neutral40,
        opacity: 0.3
      },
      z: 10
    };
    return ThumbnailModel2;
  })(Component_default)
);

// node_modules/echarts/lib/component/thumbnail/ThumbnailView.js
var ThumbnailView = (
  /** @class */
  (function(_super) {
    __extends(ThumbnailView2, _super);
    function ThumbnailView2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = ThumbnailView2.type;
      return _this;
    }
    ThumbnailView2.prototype.render = function(thumbnailModel, ecModel, api) {
      this._api = api;
      this._model = thumbnailModel;
      if (!this._coordSys) {
        this._coordSys = new View_default();
      }
      if (!this._isEnabled()) {
        this._clear();
        return;
      }
      this._renderVersion = api.getECUpdateCycleVersion();
      var group2 = this.group;
      group2.removeAll();
      var itemStyleModel = thumbnailModel.getModel("itemStyle");
      var itemStyle = itemStyleModel.getItemStyle();
      if (itemStyle.fill == null) {
        itemStyle.fill = ecModel.get("backgroundColor") || tokens_default.color.neutral00;
      }
      var refContainer = createBoxLayoutReference(thumbnailModel, api).refContainer;
      var boxRect = getLayoutRect(getBoxLayoutParams(thumbnailModel, true), refContainer);
      var boxBorderWidth = itemStyle.lineWidth || 0;
      var contentRect = this._contentRect = expandOrShrinkRect(boxRect.clone(), boxBorderWidth / 2, true, true);
      var contentGroup = new Group_default();
      group2.add(contentGroup);
      contentGroup.setClipPath(new Rect_default({
        shape: contentRect.plain()
      }));
      var targetGroup = this._targetGroup = new Group_default();
      contentGroup.add(targetGroup);
      var borderShape = boxRect.plain();
      borderShape.r = itemStyleModel.getShallow("borderRadius", true);
      group2.add(this._bgRect = new Rect_default({
        style: itemStyle,
        shape: borderShape,
        silent: false,
        cursor: "grab"
      }));
      var windowStyleModel = thumbnailModel.getModel("windowStyle");
      var windowR = windowStyleModel.getShallow("borderRadius", true);
      contentGroup.add(this._windowRect = new Rect_default({
        shape: {
          x: 0,
          y: 0,
          width: 0,
          height: 0,
          r: windowR
        },
        style: windowStyleModel.getItemStyle(),
        silent: false,
        cursor: "grab"
      }));
      this._dealRenderContent();
      this._dealUpdateWindow();
      updateZ4(thumbnailModel, this);
    };
    ThumbnailView2.prototype.renderContent = function(bridgeRendered) {
      this._bridgeRendered = bridgeRendered;
      if (this._isEnabled()) {
        this._dealRenderContent();
        this._dealUpdateWindow();
        updateZ4(this._model, this);
      }
    };
    ThumbnailView2.prototype._dealRenderContent = function() {
      var bridgeRendered = this._bridgeRendered;
      if (!bridgeRendered || bridgeRendered.renderVersion !== this._renderVersion) {
        return;
      }
      var targetGroup = this._targetGroup;
      var coordSys = this._coordSys;
      var contentRect = this._contentRect;
      targetGroup.removeAll();
      if (!bridgeRendered) {
        return;
      }
      var bridgeGroup = bridgeRendered.group;
      var bridgeRect = bridgeGroup.getBoundingRect();
      targetGroup.add(bridgeGroup);
      this._bgRect.z2 = bridgeRendered.z2Range.min - 10;
      viewCoordSysSetBoundingRect(coordSys, bridgeRect.x, bridgeRect.y, bridgeRect.width, bridgeRect.height);
      var viewRect2 = getLayoutRect({
        left: "center",
        top: "center",
        aspect: bridgeRect.width / bridgeRect.height
      }, contentRect);
      viewCoordSysSetViewRect(coordSys, viewRect2.x, viewRect2.y, viewRect2.width, viewRect2.height);
      viewCoordSysCopyTrans(bridgeGroup, coordSys, VIEW_COORD_SYS_TRANS_RAW);
      bridgeGroup.dirty();
      this._windowRect.z2 = bridgeRendered.z2Range.max + 10;
      this._resetRoamController(bridgeRendered.roamType);
    };
    ThumbnailView2.prototype.updateWindow = function(param) {
      var bridgeRendered = this._bridgeRendered;
      if (bridgeRendered && bridgeRendered.renderVersion === param.renderVersion) {
        bridgeRendered.targetTrans = param.targetTrans;
      }
      if (this._isEnabled()) {
        this._dealUpdateWindow();
      }
    };
    ThumbnailView2.prototype._dealUpdateWindow = function() {
      var bridgeRendered = this._bridgeRendered;
      if (!bridgeRendered || bridgeRendered.renderVersion !== this._renderVersion) {
        return;
      }
      var invTargetTrans = invert([], bridgeRendered.targetTrans);
      var transTargetToThis = mul([], viewCoordSysCopyOverallMatrix(null, this._coordSys), invTargetTrans);
      this._transThisToTarget = invert([], transTargetToThis);
      var viewportRect = bridgeRendered.viewportRect;
      if (!viewportRect) {
        viewportRect = new BoundingRect_default(0, 0, this._api.getWidth(), this._api.getHeight());
      } else {
        viewportRect = viewportRect.clone();
      }
      viewportRect.applyTransform(transTargetToThis);
      var windowRect = this._windowRect;
      var r = windowRect.shape.r;
      windowRect.setShape(defaults({
        r
      }, viewportRect));
    };
    ThumbnailView2.prototype._resetRoamController = function(roamType) {
      var _this = this;
      var api = this._api;
      var roamController = this._roamController;
      if (!roamController) {
        roamController = this._roamController = new RoamController_default(api.getZr());
      }
      if (!roamType || !this._isEnabled()) {
        roamController.disable();
        return;
      }
      roamController.enable(roamType, {
        api,
        zInfo: {
          component: this._model
        },
        triggerInfo: {
          roamTrigger: null,
          isInSelf: function(e2, x, y) {
            return _this._contentRect.contain(x, y);
          }
        }
      });
      roamController.off("pan").off("zoom").on("pan", bind(this._onPan, this)).on("zoom", bind(this._onZoom, this));
    };
    ThumbnailView2.prototype._onPan = function(event) {
      var trans = this._transThisToTarget;
      if (!this._isEnabled() || !trans) {
        return;
      }
      var oldOffset = applyTransform([], [event.oldX, event.oldY], trans);
      var newOffset = applyTransform([], [event.oldX - event.dx, event.oldY - event.dy], trans);
      this._api.dispatchAction(makeRoamPayload(this._model.getTarget().baseMapProvider, {
        dx: newOffset[0] - oldOffset[0],
        dy: newOffset[1] - oldOffset[1]
      }));
    };
    ThumbnailView2.prototype._onZoom = function(event) {
      var trans = this._transThisToTarget;
      if (!this._isEnabled() || !trans) {
        return;
      }
      var offset = applyTransform([], [event.originX, event.originY], trans);
      this._api.dispatchAction(makeRoamPayload(this._model.getTarget().baseMapProvider, {
        zoom: 1 / event.scale,
        originX: offset[0],
        originY: offset[1]
      }));
    };
    ThumbnailView2.prototype._isEnabled = function() {
      var thumbnailModel = this._model;
      if (!thumbnailModel || !thumbnailModel.shouldShow()) {
        return false;
      }
      var baseMapProvider = thumbnailModel.getTarget().baseMapProvider;
      if (!baseMapProvider) {
        return false;
      }
      return true;
    };
    ThumbnailView2.prototype._clear = function() {
      this.group.removeAll();
      this._bridgeRendered = null;
      if (this._roamController) {
        this._roamController.disable();
      }
    };
    ThumbnailView2.prototype.remove = function() {
      this._clear();
    };
    ThumbnailView2.prototype.dispose = function() {
      this._clear();
    };
    ThumbnailView2.type = "thumbnail";
    return ThumbnailView2;
  })(Component_default2)
);
function makeRoamPayload(baseMapProvider, params) {
  var type = baseMapProvider.mainType === "series" ? baseMapProvider.subType + "Roam" : baseMapProvider.mainType + "Roam";
  var payload = {
    type
  };
  payload[baseMapProvider.mainType + "Id"] = baseMapProvider.id;
  extend(payload, params);
  return payload;
}
function updateZ4(thumbnailModel, thumbnailView) {
  var zInfo = retrieveZInfo(thumbnailModel);
  traverseUpdateZ(thumbnailView.group, zInfo.z, zInfo.zlevel);
}

// node_modules/echarts/lib/visual/aria.js
var DEFAULT_OPTION = {
  label: {
    enabled: true
  },
  decal: {
    show: false
  }
};
var innerSeries = makeInner();
var innerGlobal = makeInner();
var ariaVisualStageHandler = createSimpleOverallStageHandler2(ariaVisual);
function ariaVisual(ecModel, api) {
  var ariaModel = ecModel.getModel("aria");
  if (!ariaModel.get("enabled")) {
    return;
  }
  var decalPaletteScope = innerGlobal(ecModel).scope || (innerGlobal(ecModel).scope = {});
  var defaultOption3 = clone(DEFAULT_OPTION);
  merge2(defaultOption3.label, ecModel.getLocaleModel().get("aria"), false);
  merge2(ariaModel.option, defaultOption3, false);
  setDecal();
  setLabel();
  function setDecal() {
    var decalModel = ariaModel.getModel("decal");
    var useDecal = decalModel.get("show");
    if (useDecal) {
      var paletteScopeGroupByType_1 = createHashMap();
      ecModel.eachSeries(function(seriesModel) {
        if (!seriesModel.isColorBySeries()) {
          innerSeries(seriesModel).scope = paletteScopeGroupByType_1.get(seriesModel.type) || paletteScopeGroupByType_1.set(seriesModel.type, {});
        }
      });
      ecModel.eachSeries(function(seriesModel) {
        if (isFunction(seriesModel.enableAriaDecal)) {
          seriesModel.enableAriaDecal();
          return;
        }
        var data = seriesModel.getData();
        if (!seriesModel.isColorBySeries()) {
          var dataAll_1 = seriesModel.getRawData();
          var idxMap_1 = {};
          var decalScope_1 = innerSeries(seriesModel).scope;
          data.each(function(idx) {
            var rawIdx = data.getRawIndex(idx);
            idxMap_1[rawIdx] = idx;
          });
          var dataCount_1 = dataAll_1.count();
          dataAll_1.each(function(rawIdx) {
            var idx = idxMap_1[rawIdx];
            var name = dataAll_1.getName(rawIdx) || rawIdx + "";
            var paletteDecal2 = getDecalFromPalette(seriesModel.ecModel, name, decalScope_1, dataCount_1);
            var specifiedDecal2 = data.getItemVisual(idx, "decal");
            data.setItemVisual(idx, "decal", mergeDecal(specifiedDecal2, paletteDecal2));
          });
        } else {
          var paletteDecal = getDecalFromPalette(seriesModel.ecModel, seriesModel.name, decalPaletteScope, ecModel.getSeriesCount());
          var specifiedDecal = data.getVisual("decal");
          data.setVisual("decal", mergeDecal(specifiedDecal, paletteDecal));
        }
        function mergeDecal(specifiedDecal2, paletteDecal2) {
          var resultDecal = specifiedDecal2 ? extend(extend({}, paletteDecal2), specifiedDecal2) : paletteDecal2;
          resultDecal.dirty = true;
          return resultDecal;
        }
      });
    }
  }
  function setLabel() {
    var dom = api.getZr().dom;
    if (!dom) {
      return;
    }
    var labelLocale = ecModel.getLocaleModel().get("aria");
    var labelModel = ariaModel.getModel("label");
    labelModel.option = defaults(labelModel.option, labelLocale);
    if (!labelModel.get("enabled")) {
      return;
    }
    dom.setAttribute("role", "img");
    if (labelModel.get("description")) {
      dom.setAttribute("aria-label", labelModel.get("description"));
      return;
    }
    var seriesCnt = ecModel.getSeriesCount();
    var maxDataCnt = labelModel.get(["data", "maxCount"]) || 10;
    var maxSeriesCnt = labelModel.get(["series", "maxCount"]) || 10;
    var displaySeriesCnt = Math.min(seriesCnt, maxSeriesCnt);
    var ariaLabel;
    if (seriesCnt < 1) {
      return;
    } else {
      var title = getTitle();
      if (title) {
        var withTitle = labelModel.get(["general", "withTitle"]);
        ariaLabel = replace(withTitle, {
          title
        });
      } else {
        ariaLabel = labelModel.get(["general", "withoutTitle"]);
      }
      var seriesLabels_1 = [];
      var prefix = seriesCnt > 1 ? labelModel.get(["series", "multiple", "prefix"]) : labelModel.get(["series", "single", "prefix"]);
      ariaLabel += replace(prefix, {
        seriesCount: seriesCnt
      });
      ecModel.eachSeries(function(seriesModel, idx) {
        if (idx < displaySeriesCnt) {
          var seriesLabel = void 0;
          var seriesName = seriesModel.get("name");
          var withName = seriesName ? "withName" : "withoutName";
          seriesLabel = seriesCnt > 1 ? labelModel.get(["series", "multiple", withName]) : labelModel.get(["series", "single", withName]);
          seriesLabel = replace(seriesLabel, {
            seriesId: seriesModel.seriesIndex,
            seriesName: seriesModel.get("name"),
            seriesType: getSeriesTypeName(seriesModel.subType)
          });
          var data = seriesModel.getData();
          if (data.count() > maxDataCnt) {
            var partialLabel = labelModel.get(["data", "partialData"]);
            seriesLabel += replace(partialLabel, {
              displayCnt: maxDataCnt
            });
          } else {
            seriesLabel += labelModel.get(["data", "allData"]);
          }
          var middleSeparator_1 = labelModel.get(["data", "separator", "middle"]);
          var endSeparator_1 = labelModel.get(["data", "separator", "end"]);
          var excludeDimensionId_1 = labelModel.get(["data", "excludeDimensionId"]);
          var dataLabels = [];
          for (var i2 = 0; i2 < data.count(); i2++) {
            if (i2 < maxDataCnt) {
              var name_1 = data.getName(i2);
              var value = !excludeDimensionId_1 ? data.getValues(i2) : filter2(data.getValues(i2), function(v, j) {
                return indexOf(excludeDimensionId_1, j) === -1;
              });
              var dataLabel = labelModel.get(["data", name_1 ? "withName" : "withoutName"]);
              dataLabels.push(replace(dataLabel, {
                name: name_1,
                value: value.join(middleSeparator_1)
              }));
            }
          }
          seriesLabel += dataLabels.join(middleSeparator_1) + endSeparator_1;
          seriesLabels_1.push(seriesLabel);
        }
      });
      var separatorModel = labelModel.getModel(["series", "multiple", "separator"]);
      var middleSeparator = separatorModel.get("middle");
      var endSeparator = separatorModel.get("end");
      ariaLabel += seriesLabels_1.join(middleSeparator) + endSeparator;
      dom.setAttribute("aria-label", ariaLabel);
    }
  }
  function replace(str, keyValues) {
    if (!isString(str)) {
      return str;
    }
    var result = str;
    each(keyValues, function(value, key2) {
      result = result.replace(new RegExp("\\{\\s*" + key2 + "\\s*\\}", "g"), value);
    });
    return result;
  }
  function getTitle() {
    var title = ecModel.get("title");
    if (title && title.length) {
      title = title[0];
    }
    return title && title.text;
  }
  function getSeriesTypeName(type) {
    var typeNames = ecModel.getLocaleModel().get(["series", "typeNames"]);
    return typeNames[type] || typeNames.chart;
  }
}

// node_modules/echarts/lib/util/conditionalExpression.js
var RELATIONAL_EXPRESSION_OP_ALIAS_MAP = {
  value: "eq",
  // PENDING: not good for literal semantic?
  "<": "lt",
  "<=": "lte",
  ">": "gt",
  ">=": "gte",
  "=": "eq",
  "!=": "ne",
  "<>": "ne"
  // Might be misleading for sake of the difference between '==' and '===',
  // so don't support them.
  // '==': 'eq',
  // '===': 'seq',
  // '!==': 'sne'
  // PENDING: Whether support some common alias "ge", "le", "neq"?
  // ge: 'gte',
  // le: 'lte',
  // neq: 'ne',
};
var RegExpEvaluator = (
  /** @class */
  (function() {
    function RegExpEvaluator2(rVal) {
      var condValue = this._condVal = isString(rVal) ? new RegExp(rVal) : isRegExp(rVal) ? rVal : null;
      if (condValue == null) {
        var errMsg = "";
        if (true) {
          errMsg = makePrintable("Illegal regexp", rVal, "in");
        }
        throwError2(errMsg);
      }
    }
    RegExpEvaluator2.prototype.evaluate = function(lVal) {
      var type = typeof lVal;
      return isString(type) ? this._condVal.test(lVal) : isNumber(type) ? this._condVal.test(lVal + "") : false;
    };
    return RegExpEvaluator2;
  })()
);
var ConstConditionInternal = (
  /** @class */
  (function() {
    function ConstConditionInternal2() {
    }
    ConstConditionInternal2.prototype.evaluate = function() {
      return this.value;
    };
    return ConstConditionInternal2;
  })()
);
var AndConditionInternal = (
  /** @class */
  (function() {
    function AndConditionInternal2() {
    }
    AndConditionInternal2.prototype.evaluate = function() {
      var children = this.children;
      for (var i2 = 0; i2 < children.length; i2++) {
        if (!children[i2].evaluate()) {
          return false;
        }
      }
      return true;
    };
    return AndConditionInternal2;
  })()
);
var OrConditionInternal = (
  /** @class */
  (function() {
    function OrConditionInternal2() {
    }
    OrConditionInternal2.prototype.evaluate = function() {
      var children = this.children;
      for (var i2 = 0; i2 < children.length; i2++) {
        if (children[i2].evaluate()) {
          return true;
        }
      }
      return false;
    };
    return OrConditionInternal2;
  })()
);
var NotConditionInternal = (
  /** @class */
  (function() {
    function NotConditionInternal2() {
    }
    NotConditionInternal2.prototype.evaluate = function() {
      return !this.child.evaluate();
    };
    return NotConditionInternal2;
  })()
);
var RelationalConditionInternal = (
  /** @class */
  (function() {
    function RelationalConditionInternal2() {
    }
    RelationalConditionInternal2.prototype.evaluate = function() {
      var needParse = !!this.valueParser;
      var getValue = this.getValue;
      var tarValRaw = getValue(this.valueGetterParam);
      var tarValParsed = needParse ? this.valueParser(tarValRaw) : null;
      for (var i2 = 0; i2 < this.subCondList.length; i2++) {
        if (!this.subCondList[i2].evaluate(needParse ? tarValParsed : tarValRaw)) {
          return false;
        }
      }
      return true;
    };
    return RelationalConditionInternal2;
  })()
);
function parseOption(exprOption, getters) {
  if (exprOption === true || exprOption === false) {
    var cond = new ConstConditionInternal();
    cond.value = exprOption;
    return cond;
  }
  var errMsg = "";
  if (!isObjectNotArray(exprOption)) {
    if (true) {
      errMsg = makePrintable("Illegal config. Expect a plain object but actually", exprOption);
    }
    throwError2(errMsg);
  }
  if (exprOption.and) {
    return parseAndOrOption("and", exprOption, getters);
  } else if (exprOption.or) {
    return parseAndOrOption("or", exprOption, getters);
  } else if (exprOption.not) {
    return parseNotOption(exprOption, getters);
  }
  return parseRelationalOption(exprOption, getters);
}
function parseAndOrOption(op, exprOption, getters) {
  var subOptionArr = exprOption[op];
  var errMsg = "";
  if (true) {
    errMsg = makePrintable('"and"/"or" condition should only be `' + op + ": [...]` and must not be empty array.", "Illegal condition:", exprOption);
  }
  if (!isArray(subOptionArr)) {
    throwError2(errMsg);
  }
  if (!subOptionArr.length) {
    throwError2(errMsg);
  }
  var cond = op === "and" ? new AndConditionInternal() : new OrConditionInternal();
  cond.children = map2(subOptionArr, function(subOption) {
    return parseOption(subOption, getters);
  });
  if (!cond.children.length) {
    throwError2(errMsg);
  }
  return cond;
}
function parseNotOption(exprOption, getters) {
  var subOption = exprOption.not;
  var errMsg = "";
  if (true) {
    errMsg = makePrintable('"not" condition should only be `not: {}`.', "Illegal condition:", exprOption);
  }
  if (!isObjectNotArray(subOption)) {
    throwError2(errMsg);
  }
  var cond = new NotConditionInternal();
  cond.child = parseOption(subOption, getters);
  if (!cond.child) {
    throwError2(errMsg);
  }
  return cond;
}
function parseRelationalOption(exprOption, getters) {
  var errMsg = "";
  var valueGetterParam = getters.prepareGetValue(exprOption);
  var subCondList = [];
  var exprKeys = keys(exprOption);
  var parserName = exprOption.parser;
  var valueParser = parserName ? getRawValueParser(parserName) : null;
  for (var i2 = 0; i2 < exprKeys.length; i2++) {
    var keyRaw = exprKeys[i2];
    if (keyRaw === "parser" || getters.valueGetterAttrMap.get(keyRaw)) {
      continue;
    }
    var op = hasOwn(RELATIONAL_EXPRESSION_OP_ALIAS_MAP, keyRaw) ? RELATIONAL_EXPRESSION_OP_ALIAS_MAP[keyRaw] : keyRaw;
    var condValueRaw = exprOption[keyRaw];
    var condValueParsed = valueParser ? valueParser(condValueRaw) : condValueRaw;
    var evaluator = createFilterComparator(op, condValueParsed) || op === "reg" && new RegExpEvaluator(condValueParsed);
    if (!evaluator) {
      if (true) {
        errMsg = makePrintable('Illegal relational operation: "' + keyRaw + '" in condition:', exprOption);
      }
      throwError2(errMsg);
    }
    subCondList.push(evaluator);
  }
  if (!subCondList.length) {
    if (true) {
      errMsg = makePrintable("Relational condition must have at least one operator.", "Illegal condition:", exprOption);
    }
    throwError2(errMsg);
  }
  var cond = new RelationalConditionInternal();
  cond.valueGetterParam = valueGetterParam;
  cond.valueParser = valueParser;
  cond.getValue = getters.getValue;
  cond.subCondList = subCondList;
  return cond;
}
function isObjectNotArray(val) {
  return isObject(val) && !isArrayLike(val);
}
var ConditionalExpressionParsed = (
  /** @class */
  (function() {
    function ConditionalExpressionParsed2(exprOption, getters) {
      this._cond = parseOption(exprOption, getters);
    }
    ConditionalExpressionParsed2.prototype.evaluate = function() {
      return this._cond.evaluate();
    };
    return ConditionalExpressionParsed2;
  })()
);

// node_modules/echarts/lib/component/transform/sortTransform.js
var sampleLog = "";
if (true) {
  sampleLog = ["Valid config is like:", '{ dimension: "age", order: "asc" }', 'or [{ dimension: "age", order: "asc"], { dimension: "date", order: "desc" }]'].join(" ");
}

// node_modules/echarts/lib/component/dataset/install.js
var DatasetModel = (
  /** @class */
  (function(_super) {
    __extends(DatasetModel2, _super);
    function DatasetModel2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = "dataset";
      return _this;
    }
    DatasetModel2.prototype.init = function(option, parentModel, ecModel) {
      _super.prototype.init.call(this, option, parentModel, ecModel);
      this._sourceManager = new SourceManager(this);
      disableTransformOptionMerge(this);
    };
    DatasetModel2.prototype.mergeOption = function(newOption, ecModel) {
      _super.prototype.mergeOption.call(this, newOption, ecModel);
      disableTransformOptionMerge(this);
    };
    DatasetModel2.prototype.optionUpdated = function() {
      this._sourceManager.dirty();
    };
    DatasetModel2.prototype.getSourceManager = function() {
      return this._sourceManager;
    };
    DatasetModel2.type = "dataset";
    DatasetModel2.defaultOption = {
      seriesLayoutBy: SERIES_LAYOUT_BY_COLUMN
    };
    return DatasetModel2;
  })(Component_default)
);
var DatasetView = (
  /** @class */
  (function(_super) {
    __extends(DatasetView2, _super);
    function DatasetView2() {
      var _this = _super !== null && _super.apply(this, arguments) || this;
      _this.type = "dataset";
      return _this;
    }
    DatasetView2.type = "dataset";
    return DatasetView2;
  })(Component_default2)
);

// node_modules/echarts/lib/label/LabelManager.js
function cloneArr(points4) {
  if (points4) {
    var newPoints = [];
    for (var i2 = 0; i2 < points4.length; i2++) {
      newPoints.push(points4[i2].slice());
    }
    return newPoints;
  }
}
function prepareLayoutCallbackParams(labelItem, hostEl) {
  var label = labelItem.label;
  var labelLine = hostEl && hostEl.getTextGuideLine();
  return {
    dataIndex: labelItem.dataIndex,
    dataType: labelItem.dataType,
    seriesIndex: labelItem.seriesModel.seriesIndex,
    text: labelItem.label.style.text,
    rect: labelItem.hostRect,
    labelRect: labelItem.rect,
    // x: labelAttr.x,
    // y: labelAttr.y,
    align: label.style.align,
    verticalAlign: label.style.verticalAlign,
    labelLinePoints: cloneArr(labelLine && labelLine.shape.points)
  };
}
var LABEL_OPTION_TO_STYLE_KEYS = ["align", "verticalAlign", "width", "height", "fontSize"];
var dummyTransformable = new Transformable_default();
var labelLayoutInnerStore = makeInner();
var labelLineAnimationStore = makeInner();
function extendWithKeys(target, source, keys2) {
  for (var i2 = 0; i2 < keys2.length; i2++) {
    var key2 = keys2[i2];
    if (source[key2] != null) {
      target[key2] = source[key2];
    }
  }
}
var LABEL_LAYOUT_PROPS = ["x", "y", "rotation"];
var LabelManager = (
  /** @class */
  (function() {
    function LabelManager2() {
      this._labelList = [];
      this._chartViewList = [];
    }
    LabelManager2.prototype.clearLabels = function() {
      this._labelList = [];
      this._chartViewList = [];
    };
    LabelManager2.prototype._addLabel = function(dataIndex, dataType, seriesModel, label, layoutOptionOrCb) {
      var labelStyle = label.style;
      var hostEl = label.__hostTarget;
      var textConfig = hostEl.textConfig || {};
      var labelTransform = label.getComputedTransform();
      var labelRect = label.getBoundingRect().plain();
      BoundingRect_default.applyTransform(labelRect, labelRect, labelTransform);
      if (labelTransform) {
        dummyTransformable.setLocalTransform(labelTransform);
      } else {
        dummyTransformable.x = dummyTransformable.y = dummyTransformable.rotation = dummyTransformable.originX = dummyTransformable.originY = 0;
        dummyTransformable.scaleX = dummyTransformable.scaleY = 1;
      }
      dummyTransformable.rotation = normalizeRadian(dummyTransformable.rotation);
      var host = label.__hostTarget;
      var hostRect;
      if (host) {
        hostRect = host.getBoundingRect().plain();
        var transform = host.getComputedTransform();
        BoundingRect_default.applyTransform(hostRect, hostRect, transform);
      }
      var labelGuide = hostRect && host.getTextGuideLine();
      this._labelList.push({
        label,
        labelLine: labelGuide,
        seriesModel,
        dataIndex,
        dataType,
        layoutOptionOrCb,
        layoutOption: null,
        rect: labelRect,
        hostRect,
        // Label with lower priority will be hidden when overlapped
        // Use rect size as default priority
        priority: hostRect ? hostRect.width * hostRect.height : 0,
        // Save default label attributes.
        // For restore if developers want get back to default value in callback.
        defaultAttr: {
          ignore: label.ignore,
          labelGuideIgnore: labelGuide && labelGuide.ignore,
          x: dummyTransformable.x,
          y: dummyTransformable.y,
          scaleX: dummyTransformable.scaleX,
          scaleY: dummyTransformable.scaleY,
          rotation: dummyTransformable.rotation,
          style: {
            x: labelStyle.x,
            y: labelStyle.y,
            align: labelStyle.align,
            verticalAlign: labelStyle.verticalAlign,
            width: labelStyle.width,
            height: labelStyle.height,
            fontSize: labelStyle.fontSize
          },
          cursor: label.cursor,
          attachedPos: textConfig.position,
          attachedRot: textConfig.rotation
        }
      });
    };
    LabelManager2.prototype.addLabelsOfSeries = function(chartView) {
      var _this = this;
      this._chartViewList.push(chartView);
      var seriesModel = chartView.__model;
      var layoutOption = seriesModel.get("labelLayout");
      if (!(isFunction(layoutOption) || keys(layoutOption).length)) {
        return;
      }
      chartView.group.traverse(function(child) {
        if (child.ignore) {
          return true;
        }
        var textEl = child.getTextContent();
        var ecData = getECData(child);
        if (textEl && !textEl.disableLabelLayout) {
          _this._addLabel(ecData.dataIndex, ecData.dataType, seriesModel, textEl, layoutOption);
        }
      });
    };
    LabelManager2.prototype.updateLayoutConfig = function(api) {
      var width = api.getWidth();
      var height = api.getHeight();
      function createDragHandler(el, labelLineModel) {
        return function() {
          updateLabelLinePoints(el, labelLineModel);
        };
      }
      for (var i2 = 0; i2 < this._labelList.length; i2++) {
        var labelItem = this._labelList[i2];
        var label = labelItem.label;
        var hostEl = label.__hostTarget;
        var defaultLabelAttr = labelItem.defaultAttr;
        var layoutOption = void 0;
        if (isFunction(labelItem.layoutOptionOrCb)) {
          layoutOption = labelItem.layoutOptionOrCb(prepareLayoutCallbackParams(labelItem, hostEl));
        } else {
          layoutOption = labelItem.layoutOptionOrCb;
        }
        layoutOption = layoutOption || {};
        labelItem.layoutOption = layoutOption;
        var degreeToRadian = Math.PI / 180;
        if (hostEl) {
          hostEl.setTextConfig({
            // Force to set local false.
            local: false,
            // Ignore position and rotation config on the host el if x or y is changed.
            position: layoutOption.x != null || layoutOption.y != null ? null : defaultLabelAttr.attachedPos,
            // Ignore rotation config on the host el if rotation is changed.
            rotation: layoutOption.rotate != null ? layoutOption.rotate * degreeToRadian : defaultLabelAttr.attachedRot,
            offset: [layoutOption.dx || 0, layoutOption.dy || 0]
          });
        }
        var needsUpdateLabelLine = false;
        if (layoutOption.x != null) {
          label.x = parsePercent2(layoutOption.x, width);
          label.setStyle("x", 0);
          needsUpdateLabelLine = true;
        } else {
          label.x = defaultLabelAttr.x;
          label.setStyle("x", defaultLabelAttr.style.x);
        }
        if (layoutOption.y != null) {
          label.y = parsePercent2(layoutOption.y, height);
          label.setStyle("y", 0);
          needsUpdateLabelLine = true;
        } else {
          label.y = defaultLabelAttr.y;
          label.setStyle("y", defaultLabelAttr.style.y);
        }
        if (layoutOption.labelLinePoints) {
          var guideLine = hostEl.getTextGuideLine();
          if (guideLine) {
            guideLine.setShape({
              points: layoutOption.labelLinePoints
            });
            needsUpdateLabelLine = false;
          }
        }
        var labelLayoutStore = labelLayoutInnerStore(label);
        labelLayoutStore.needsUpdateLabelLine = needsUpdateLabelLine;
        label.rotation = layoutOption.rotate != null ? layoutOption.rotate * degreeToRadian : defaultLabelAttr.rotation;
        label.scaleX = defaultLabelAttr.scaleX;
        label.scaleY = defaultLabelAttr.scaleY;
        for (var k2 = 0; k2 < LABEL_OPTION_TO_STYLE_KEYS.length; k2++) {
          var key2 = LABEL_OPTION_TO_STYLE_KEYS[k2];
          label.setStyle(key2, layoutOption[key2] != null ? layoutOption[key2] : defaultLabelAttr.style[key2]);
        }
        if (layoutOption.draggable) {
          label.draggable = true;
          label.cursor = "move";
          if (hostEl) {
            var hostModel = labelItem.seriesModel;
            if (labelItem.dataIndex != null) {
              var data = labelItem.seriesModel.getData(labelItem.dataType);
              hostModel = data.getItemModel(labelItem.dataIndex);
            }
            label.on("drag", createDragHandler(hostEl, hostModel.getModel("labelLine")));
          }
        } else {
          label.off("drag");
          label.cursor = defaultLabelAttr.cursor;
        }
      }
    };
    LabelManager2.prototype.layout = function(api) {
      var width = api.getWidth();
      var height = api.getHeight();
      var labelList = [];
      each(this._labelList, function(inputItem) {
        if (!inputItem.defaultAttr.ignore) {
          labelList.push(newLabelLayoutWithGeometry({}, inputItem));
        }
      });
      var labelsNeedsAdjustOnX = filter2(labelList, function(item) {
        return item.layoutOption.moveOverlap === "shiftX";
      });
      var labelsNeedsAdjustOnY = filter2(labelList, function(item) {
        return item.layoutOption.moveOverlap === "shiftY";
      });
      shiftLayoutOnXY(labelsNeedsAdjustOnX, 0, 0, width);
      shiftLayoutOnXY(labelsNeedsAdjustOnY, 1, 0, height);
      var labelsNeedsHideOverlap = filter2(labelList, function(item) {
        return item.layoutOption.hideOverlap;
      });
      restoreIgnore(labelsNeedsHideOverlap);
      hideOverlap(labelsNeedsHideOverlap);
    };
    LabelManager2.prototype.processLabelsOverall = function() {
      var _this = this;
      each(this._chartViewList, function(chartView) {
        var seriesModel = chartView.__model;
        var ignoreLabelLineUpdate = chartView.ignoreLabelLineUpdate;
        var animationEnabled = seriesModel.isAnimationEnabled();
        chartView.group.traverse(function(child) {
          if (child.ignore && !child.forceLabelAnimation) {
            return true;
          }
          var needsUpdateLabelLine = !ignoreLabelLineUpdate;
          var label = child.getTextContent();
          if (!needsUpdateLabelLine && label) {
            needsUpdateLabelLine = labelLayoutInnerStore(label).needsUpdateLabelLine;
          }
          if (needsUpdateLabelLine) {
            _this._updateLabelLine(child, seriesModel);
          }
          if (animationEnabled) {
            _this._animateLabels(child, seriesModel);
          }
        });
      });
    };
    LabelManager2.prototype._updateLabelLine = function(el, seriesModel) {
      var textEl = el.getTextContent();
      var ecData = getECData(el);
      var dataIndex = ecData.dataIndex;
      if (textEl && dataIndex != null) {
        var data = seriesModel.getData(ecData.dataType);
        var itemModel = data.getItemModel(dataIndex);
        var defaultStyle = {};
        var visualStyle = data.getItemVisual(dataIndex, "style");
        if (visualStyle) {
          var visualType = data.getVisual("drawType");
          defaultStyle.stroke = visualStyle[visualType];
        }
        var labelLineModel = itemModel.getModel("labelLine");
        setLabelLineStyle(el, getLabelLineStatesModels(itemModel), defaultStyle);
        updateLabelLinePoints(el, labelLineModel);
      }
    };
    LabelManager2.prototype._animateLabels = function(el, seriesModel) {
      var textEl = el.getTextContent();
      var guideLine = el.getTextGuideLine();
      if (textEl && (el.forceLabelAnimation || !textEl.ignore && !textEl.invisible && !el.disableLabelAnimation && !isElementRemoved(el))) {
        var layoutStore = labelLayoutInnerStore(textEl);
        var oldLayout = layoutStore.oldLayout;
        var ecData = getECData(el);
        var dataIndex = ecData.dataIndex;
        var newProps = {
          x: textEl.x,
          y: textEl.y,
          rotation: textEl.rotation
        };
        var data = seriesModel.getData(ecData.dataType);
        if (!oldLayout) {
          textEl.attr(newProps);
          if (!labelInner(textEl).valueAnimation) {
            var oldOpacity = retrieve2(textEl.style.opacity, 1);
            textEl.style.opacity = 0;
            initProps(textEl, {
              style: {
                opacity: oldOpacity
              }
            }, seriesModel, dataIndex);
          }
        } else {
          textEl.attr(oldLayout);
          var prevStates = el.prevStates;
          if (prevStates) {
            if (indexOf(prevStates, "select") >= 0) {
              textEl.attr(layoutStore.oldLayoutSelect);
            }
            if (indexOf(prevStates, "emphasis") >= 0) {
              textEl.attr(layoutStore.oldLayoutEmphasis);
            }
          }
          updateProps(textEl, newProps, seriesModel, dataIndex);
        }
        layoutStore.oldLayout = newProps;
        if (textEl.states.select) {
          var layoutSelect = layoutStore.oldLayoutSelect = {};
          extendWithKeys(layoutSelect, newProps, LABEL_LAYOUT_PROPS);
          extendWithKeys(layoutSelect, textEl.states.select, LABEL_LAYOUT_PROPS);
        }
        if (textEl.states.emphasis) {
          var layoutEmphasis = layoutStore.oldLayoutEmphasis = {};
          extendWithKeys(layoutEmphasis, newProps, LABEL_LAYOUT_PROPS);
          extendWithKeys(layoutEmphasis, textEl.states.emphasis, LABEL_LAYOUT_PROPS);
        }
        animateLabelValue(textEl, dataIndex, data, seriesModel, seriesModel);
      }
      if (guideLine && !guideLine.ignore && !guideLine.invisible) {
        var layoutStore = labelLineAnimationStore(guideLine);
        var oldLayout = layoutStore.oldLayout;
        var newLayout = {
          points: guideLine.shape.points
        };
        if (!oldLayout) {
          guideLine.setShape(newLayout);
          guideLine.style.strokePercent = 0;
          initProps(guideLine, {
            style: {
              strokePercent: 1
            }
          }, seriesModel);
        } else {
          guideLine.attr({
            shape: oldLayout
          });
          updateProps(guideLine, {
            shape: newLayout
          }, seriesModel);
        }
        layoutStore.oldLayout = newLayout;
      }
    };
    return LabelManager2;
  })()
);
var LabelManager_default = LabelManager;

// node_modules/echarts/lib/label/installLabelLayout.js
var getLabelManager = makeInner();
function installLabelLayout(registers) {
  registers.registerUpdateLifecycle("series:beforeupdate", function(ecModel, api, params) {
    var labelManager = getLabelManager(api).labelManager;
    if (!labelManager) {
      labelManager = getLabelManager(api).labelManager = new LabelManager_default();
    }
    labelManager.clearLabels();
  });
  registers.registerUpdateLifecycle("series:layoutlabels", function(ecModel, api, params) {
    var labelManager = getLabelManager(api).labelManager;
    each(params.updatedSeries, function(series) {
      labelManager.addLabelsOfSeries(api.getViewOfSeriesModel(series));
    });
    labelManager.updateLayoutConfig(api);
    labelManager.layout(api);
    labelManager.processLabelsOverall();
  });
}

// node_modules/echarts/lib/export/core.js
use(installLabelLayout);

// node_modules/zrender/lib/svg/SVGPathRebuilder.js
var mathSin5 = Math.sin;
var mathCos5 = Math.cos;
var PI9 = Math.PI;
var PI29 = Math.PI * 2;
var degree = 180 / PI9;
var SVGPathRebuilder = (function() {
  function SVGPathRebuilder2() {
  }
  SVGPathRebuilder2.prototype.reset = function(precision) {
    this._start = true;
    this._d = [];
    this._str = "";
    this._p = Math.pow(10, precision || 4);
  };
  SVGPathRebuilder2.prototype.moveTo = function(x, y) {
    this._add("M", x, y);
  };
  SVGPathRebuilder2.prototype.lineTo = function(x, y) {
    this._add("L", x, y);
  };
  SVGPathRebuilder2.prototype.bezierCurveTo = function(x, y, x2, y2, x3, y3) {
    this._add("C", x, y, x2, y2, x3, y3);
  };
  SVGPathRebuilder2.prototype.quadraticCurveTo = function(x, y, x2, y2) {
    this._add("Q", x, y, x2, y2);
  };
  SVGPathRebuilder2.prototype.arc = function(cx, cy, r, startAngle, endAngle, anticlockwise) {
    this.ellipse(cx, cy, r, r, 0, startAngle, endAngle, anticlockwise);
  };
  SVGPathRebuilder2.prototype.ellipse = function(cx, cy, rx, ry, psi, startAngle, endAngle, anticlockwise) {
    var dTheta = endAngle - startAngle;
    var clockwise = !anticlockwise;
    var dThetaPositive = Math.abs(dTheta);
    var isCircle = isAroundZero2(dThetaPositive - PI29) || (clockwise ? dTheta >= PI29 : -dTheta >= PI29);
    var unifiedTheta = dTheta > 0 ? dTheta % PI29 : dTheta % PI29 + PI29;
    var large = false;
    if (isCircle) {
      large = true;
    } else if (isAroundZero2(dThetaPositive)) {
      large = false;
    } else {
      large = unifiedTheta >= PI9 === !!clockwise;
    }
    var x0 = cx + rx * mathCos5(startAngle);
    var y0 = cy + ry * mathSin5(startAngle);
    if (this._start) {
      this._add("M", x0, y0);
    }
    var xRot = Math.round(psi * degree);
    if (isCircle) {
      var p = 1 / this._p;
      var dTheta_1 = (clockwise ? 1 : -1) * (PI29 - p);
      this._add("A", rx, ry, xRot, 1, +clockwise, cx + rx * mathCos5(startAngle + dTheta_1), cy + ry * mathSin5(startAngle + dTheta_1));
      if (p > 0.01) {
        this._add("A", rx, ry, xRot, 0, +clockwise, x0, y0);
      }
    } else {
      var x = cx + rx * mathCos5(endAngle);
      var y = cy + ry * mathSin5(endAngle);
      this._add("A", rx, ry, xRot, +large, +clockwise, x, y);
    }
  };
  SVGPathRebuilder2.prototype.rect = function(x, y, w, h) {
    this._add("M", x, y);
    this._add("l", w, 0);
    this._add("l", 0, h);
    this._add("l", -w, 0);
    this._add("Z");
  };
  SVGPathRebuilder2.prototype.closePath = function() {
    if (this._d.length > 0) {
      this._add("Z");
    }
  };
  SVGPathRebuilder2.prototype._add = function(cmd, a, b, c, d, e2, f, g, h) {
    var vals = [];
    var p = this._p;
    for (var i2 = 1; i2 < arguments.length; i2++) {
      var val = arguments[i2];
      if (isNaN(val)) {
        this._invalid = true;
        return;
      }
      vals.push(Math.round(val * p) / p);
    }
    this._d.push(cmd + vals.join(" "));
    this._start = cmd === "Z";
  };
  SVGPathRebuilder2.prototype.generateStr = function() {
    this._str = this._invalid ? "" : this._d.join("");
    this._d = [];
  };
  SVGPathRebuilder2.prototype.getStr = function() {
    return this._str;
  };
  return SVGPathRebuilder2;
})();
var SVGPathRebuilder_default = SVGPathRebuilder;

// node_modules/zrender/lib/svg/mapStyleToAttrs.js
var NONE = "none";
var mathRound3 = Math.round;
function pathHasFill(style2) {
  var fill = style2.fill;
  return fill != null && fill !== NONE;
}
function pathHasStroke(style2) {
  var stroke = style2.stroke;
  return stroke != null && stroke !== NONE;
}
var strokeProps = ["lineCap", "miterLimit", "lineJoin"];
var svgStrokeProps = map2(strokeProps, function(prop) {
  return "stroke-" + prop.toLowerCase();
});
function mapStyleToAttrs(updateAttr2, style2, el, forceUpdate) {
  var opacity = style2.opacity == null ? 1 : style2.opacity;
  if (el instanceof Image_default) {
    updateAttr2("opacity", opacity);
    return;
  }
  if (pathHasFill(style2)) {
    var fill = normalizeColor(style2.fill);
    updateAttr2("fill", fill.color);
    var fillOpacity = style2.fillOpacity != null ? style2.fillOpacity * fill.opacity * opacity : fill.opacity * opacity;
    if (forceUpdate || fillOpacity < 1) {
      updateAttr2("fill-opacity", fillOpacity);
    }
  } else {
    updateAttr2("fill", NONE);
  }
  if (pathHasStroke(style2)) {
    var stroke = normalizeColor(style2.stroke);
    updateAttr2("stroke", stroke.color);
    var strokeScale = style2.strokeNoScale ? el.getLineScale() : 1;
    var strokeWidth = strokeScale ? (style2.lineWidth || 0) / strokeScale : 0;
    var strokeOpacity = style2.strokeOpacity != null ? style2.strokeOpacity * stroke.opacity * opacity : stroke.opacity * opacity;
    var strokeFirst = style2.strokeFirst;
    if (forceUpdate || strokeWidth !== 1) {
      updateAttr2("stroke-width", strokeWidth);
    }
    if (forceUpdate || strokeFirst) {
      updateAttr2("paint-order", strokeFirst ? "stroke" : "fill");
    }
    if (forceUpdate || strokeOpacity < 1) {
      updateAttr2("stroke-opacity", strokeOpacity);
    }
    if (style2.lineDash) {
      var _a2 = getLineDash(el), lineDash = _a2[0], lineDashOffset = _a2[1];
      if (lineDash) {
        lineDashOffset = mathRound3(lineDashOffset || 0);
        updateAttr2("stroke-dasharray", lineDash.join(","));
        if (lineDashOffset || forceUpdate) {
          updateAttr2("stroke-dashoffset", lineDashOffset);
        }
      }
    } else if (forceUpdate) {
      updateAttr2("stroke-dasharray", NONE);
    }
    for (var i2 = 0; i2 < strokeProps.length; i2++) {
      var propName = strokeProps[i2];
      if (forceUpdate || style2[propName] !== DEFAULT_PATH_STYLE[propName]) {
        var val = style2[propName] || DEFAULT_PATH_STYLE[propName];
        val && updateAttr2(svgStrokeProps[i2], val);
      }
    }
  } else if (forceUpdate) {
    updateAttr2("stroke", NONE);
  }
}

// node_modules/zrender/lib/svg/core.js
var SVGNS = "http://www.w3.org/2000/svg";
var XLINKNS = "http://www.w3.org/1999/xlink";
var XMLNS = "http://www.w3.org/2000/xmlns/";
var XML_NAMESPACE = "http://www.w3.org/XML/1998/namespace";
var META_DATA_PREFIX = "ecmeta_";
function createElement(name) {
  return document.createElementNS(SVGNS, name);
}
function createVNode(tag, key2, attrs, children, text) {
  return {
    tag,
    attrs: attrs || {},
    children,
    text,
    key: key2
  };
}
function createElementOpen(name, attrs) {
  var attrsStr = [];
  if (attrs) {
    for (var key2 in attrs) {
      var val = attrs[key2];
      var part = key2;
      if (val === false) {
        continue;
      } else if (val !== true && val != null) {
        part += '="' + val + '"';
      }
      attrsStr.push(part);
    }
  }
  return "<" + name + " " + attrsStr.join(" ") + ">";
}
function createElementClose(name) {
  return "</" + name + ">";
}
function vNodeToString(el, opts) {
  opts = opts || {};
  var S2 = opts.newline ? "\n" : "";
  function convertElToString(el2) {
    var children = el2.children, tag = el2.tag, attrs = el2.attrs, text = el2.text;
    return createElementOpen(tag, attrs) + (tag !== "style" ? encodeHTML(text) : text || "") + (children ? "" + S2 + map2(children, function(child) {
      return convertElToString(child);
    }).join(S2) + S2 : "") + createElementClose(tag);
  }
  return convertElToString(el);
}
function getCssString(selectorNodes, animationNodes, opts) {
  opts = opts || {};
  var S2 = opts.newline ? "\n" : "";
  var bracketBegin = " {" + S2;
  var bracketEnd = S2 + "}";
  var selectors = map2(keys(selectorNodes), function(className) {
    return className + bracketBegin + map2(keys(selectorNodes[className]), function(attrName) {
      return attrName + ":" + selectorNodes[className][attrName] + ";";
    }).join(S2) + bracketEnd;
  }).join(S2);
  var animations = map2(keys(animationNodes), function(animationName) {
    return "@keyframes " + animationName + bracketBegin + map2(keys(animationNodes[animationName]), function(percent) {
      return percent + bracketBegin + map2(keys(animationNodes[animationName][percent]), function(attrName) {
        var val = animationNodes[animationName][percent][attrName];
        if (attrName === "d") {
          val = 'path("' + val + '")';
        }
        return attrName + ":" + val + ";";
      }).join(S2) + bracketEnd;
    }).join(S2) + bracketEnd;
  }).join(S2);
  if (!selectors && !animations) {
    return "";
  }
  return ["<![CDATA[", selectors, animations, "]]>"].join(S2);
}
function createBrushScope(zrId) {
  return {
    zrId,
    shadowCache: {},
    patternCache: {},
    gradientCache: {},
    clipPathCache: {},
    defs: {},
    cssNodes: {},
    cssAnims: {},
    cssStyleCache: {},
    cssAnimIdx: 0,
    shadowIdx: 0,
    gradientIdx: 0,
    patternIdx: 0,
    clipPathIdx: 0
  };
}
function createSVGVNode(width, height, children, useViewBox) {
  return createVNode("svg", "root", {
    "width": width,
    "height": height,
    "xmlns": SVGNS,
    "xmlns:xlink": XLINKNS,
    "version": "1.1",
    "baseProfile": "full",
    "viewBox": useViewBox ? "0 0 " + width + " " + height : false
  }, children);
}

// node_modules/zrender/lib/svg/cssClassId.js
var cssClassIdx = 0;
function getClassId() {
  return cssClassIdx++;
}

// node_modules/zrender/lib/svg/cssAnimation.js
var EASING_MAP = {
  cubicIn: "0.32,0,0.67,0",
  cubicOut: "0.33,1,0.68,1",
  cubicInOut: "0.65,0,0.35,1",
  quadraticIn: "0.11,0,0.5,0",
  quadraticOut: "0.5,1,0.89,1",
  quadraticInOut: "0.45,0,0.55,1",
  quarticIn: "0.5,0,0.75,0",
  quarticOut: "0.25,1,0.5,1",
  quarticInOut: "0.76,0,0.24,1",
  quinticIn: "0.64,0,0.78,0",
  quinticOut: "0.22,1,0.36,1",
  quinticInOut: "0.83,0,0.17,1",
  sinusoidalIn: "0.12,0,0.39,0",
  sinusoidalOut: "0.61,1,0.88,1",
  sinusoidalInOut: "0.37,0,0.63,1",
  exponentialIn: "0.7,0,0.84,0",
  exponentialOut: "0.16,1,0.3,1",
  exponentialInOut: "0.87,0,0.13,1",
  circularIn: "0.55,0,1,0.45",
  circularOut: "0,0.55,0.45,1",
  circularInOut: "0.85,0,0.15,1"
};
var transformOriginKey = "transform-origin";
function buildPathString(el, kfShape, path) {
  var shape = extend({}, el.shape);
  extend(shape, kfShape);
  el.buildPath(path, shape);
  var svgPathBuilder = new SVGPathRebuilder_default();
  svgPathBuilder.reset(getPathPrecision(el));
  path.rebuildPath(svgPathBuilder, 1);
  svgPathBuilder.generateStr();
  return svgPathBuilder.getStr();
}
function setTransformOrigin(target, transform) {
  var originX = transform.originX, originY = transform.originY;
  if (originX || originY) {
    target[transformOriginKey] = originX + "px " + originY + "px";
  }
}
var ANIMATE_STYLE_MAP = {
  fill: "fill",
  opacity: "opacity",
  lineWidth: "stroke-width",
  lineDashOffset: "stroke-dashoffset"
};
function addAnimation(cssAnim, scope) {
  var animationName = scope.zrId + "-ani-" + scope.cssAnimIdx++;
  scope.cssAnims[animationName] = cssAnim;
  return animationName;
}
function createCompoundPathCSSAnimation(el, attrs, scope) {
  var paths = el.shape.paths;
  var composedAnim = {};
  var cssAnimationCfg;
  var cssAnimationName;
  each(paths, function(path) {
    var subScope = createBrushScope(scope.zrId);
    subScope.animation = true;
    createCSSAnimation(path, {}, subScope, true);
    var cssAnims = subScope.cssAnims;
    var cssNodes = subScope.cssNodes;
    var animNames = keys(cssAnims);
    var len2 = animNames.length;
    if (!len2) {
      return;
    }
    cssAnimationName = animNames[len2 - 1];
    var lastAnim = cssAnims[cssAnimationName];
    for (var percent in lastAnim) {
      var kf = lastAnim[percent];
      composedAnim[percent] = composedAnim[percent] || { d: "" };
      composedAnim[percent].d += kf.d || "";
    }
    for (var className in cssNodes) {
      var val = cssNodes[className].animation;
      if (val.indexOf(cssAnimationName) >= 0) {
        cssAnimationCfg = val;
      }
    }
  });
  if (!cssAnimationCfg) {
    return;
  }
  attrs.d = false;
  var animationName = addAnimation(composedAnim, scope);
  return cssAnimationCfg.replace(cssAnimationName, animationName);
}
function getEasingFunc(easing) {
  return isString(easing) ? EASING_MAP[easing] ? "cubic-bezier(" + EASING_MAP[easing] + ")" : createCubicEasingFunc(easing) ? easing : "" : "";
}
function createCSSAnimation(el, attrs, scope, onlyShape) {
  var animators = el.animators;
  var len2 = animators.length;
  var cssAnimations = [];
  if (el instanceof CompoundPath_default) {
    var animationCfg = createCompoundPathCSSAnimation(el, attrs, scope);
    if (animationCfg) {
      cssAnimations.push(animationCfg);
    } else if (!len2) {
      return;
    }
  } else if (!len2) {
    return;
  }
  var groupAnimators = {};
  for (var i2 = 0; i2 < len2; i2++) {
    var animator = animators[i2];
    var cfgArr = [animator.getMaxTime() / 1e3 + "s"];
    var easing = getEasingFunc(animator.getClip().easing);
    var delay2 = animator.getDelay();
    if (easing) {
      cfgArr.push(easing);
    } else {
      cfgArr.push("linear");
    }
    if (delay2) {
      cfgArr.push(delay2 / 1e3 + "s");
    }
    if (animator.getLoop()) {
      cfgArr.push("infinite");
    }
    var cfg = cfgArr.join(" ");
    groupAnimators[cfg] = groupAnimators[cfg] || [cfg, []];
    groupAnimators[cfg][1].push(animator);
  }
  function createSingleCSSAnimation(groupAnimator) {
    var animators2 = groupAnimator[1];
    var len3 = animators2.length;
    var transformKfs = {};
    var shapeKfs = {};
    var finalKfs = {};
    var animationTimingFunctionAttrName = "animation-timing-function";
    function saveAnimatorTrackToCssKfs(animator3, cssKfs, toCssAttrName) {
      var tracks = animator3.getTracks();
      var maxTime = animator3.getMaxTime();
      for (var k2 = 0; k2 < tracks.length; k2++) {
        var track = tracks[k2];
        if (track.needsAnimate()) {
          var kfs = track.keyframes;
          var attrName = track.propName;
          toCssAttrName && (attrName = toCssAttrName(attrName));
          if (attrName) {
            for (var i4 = 0; i4 < kfs.length; i4++) {
              var kf = kfs[i4];
              var percent2 = Math.round(kf.time / maxTime * 100) + "%";
              var kfEasing = getEasingFunc(kf.easing);
              var rawValue = kf.rawValue;
              if (isString(rawValue) || isNumber(rawValue)) {
                cssKfs[percent2] = cssKfs[percent2] || {};
                cssKfs[percent2][attrName] = kf.rawValue;
                if (kfEasing) {
                  cssKfs[percent2][animationTimingFunctionAttrName] = kfEasing;
                }
              }
            }
          }
        }
      }
    }
    for (var i3 = 0; i3 < len3; i3++) {
      var animator2 = animators2[i3];
      var targetProp = animator2.targetName;
      if (!targetProp) {
        !onlyShape && saveAnimatorTrackToCssKfs(animator2, transformKfs);
      } else if (targetProp === "shape") {
        saveAnimatorTrackToCssKfs(animator2, shapeKfs);
      }
    }
    for (var percent in transformKfs) {
      var transform = {};
      copyTransform(transform, el);
      extend(transform, transformKfs[percent]);
      var str = getSRTTransformString(transform);
      var timingFunction = transformKfs[percent][animationTimingFunctionAttrName];
      finalKfs[percent] = str ? {
        transform: str
      } : {};
      setTransformOrigin(finalKfs[percent], transform);
      if (timingFunction) {
        finalKfs[percent][animationTimingFunctionAttrName] = timingFunction;
      }
    }
    ;
    var path;
    var canAnimateShape = true;
    for (var percent in shapeKfs) {
      finalKfs[percent] = finalKfs[percent] || {};
      var isFirst = !path;
      var timingFunction = shapeKfs[percent][animationTimingFunctionAttrName];
      if (isFirst) {
        path = new PathProxy_default();
      }
      var len_1 = path.len();
      path.reset();
      finalKfs[percent].d = buildPathString(el, shapeKfs[percent], path);
      var newLen = path.len();
      if (!isFirst && len_1 !== newLen) {
        canAnimateShape = false;
        break;
      }
      if (timingFunction) {
        finalKfs[percent][animationTimingFunctionAttrName] = timingFunction;
      }
    }
    ;
    if (!canAnimateShape) {
      for (var percent in finalKfs) {
        delete finalKfs[percent].d;
      }
    }
    if (!onlyShape) {
      for (var i3 = 0; i3 < len3; i3++) {
        var animator2 = animators2[i3];
        var targetProp = animator2.targetName;
        if (targetProp === "style") {
          saveAnimatorTrackToCssKfs(animator2, finalKfs, function(propName) {
            return ANIMATE_STYLE_MAP[propName];
          });
        }
      }
    }
    var percents = keys(finalKfs);
    var allTransformOriginSame = true;
    var transformOrigin;
    for (var i3 = 1; i3 < percents.length; i3++) {
      var p0 = percents[i3 - 1];
      var p1 = percents[i3];
      if (finalKfs[p0][transformOriginKey] !== finalKfs[p1][transformOriginKey]) {
        allTransformOriginSame = false;
        break;
      }
      transformOrigin = finalKfs[p0][transformOriginKey];
    }
    if (allTransformOriginSame && transformOrigin) {
      for (var percent in finalKfs) {
        if (finalKfs[percent][transformOriginKey]) {
          delete finalKfs[percent][transformOriginKey];
        }
      }
      attrs[transformOriginKey] = transformOrigin;
    }
    if (filter2(percents, function(percent2) {
      return keys(finalKfs[percent2]).length > 0;
    }).length) {
      var animationName = addAnimation(finalKfs, scope);
      return animationName + " " + groupAnimator[0] + " both";
    }
  }
  for (var key2 in groupAnimators) {
    var animationCfg = createSingleCSSAnimation(groupAnimators[key2]);
    if (animationCfg) {
      cssAnimations.push(animationCfg);
    }
  }
  if (cssAnimations.length) {
    var className = scope.zrId + "-cls-" + getClassId();
    scope.cssNodes["." + className] = {
      animation: cssAnimations.join(",")
    };
    attrs["class"] = className;
  }
}

// node_modules/zrender/lib/svg/cssEmphasis.js
function createCSSEmphasis(el, attrs, scope) {
  if (!el.ignore) {
    if (el.isSilent()) {
      var style2 = {
        "pointer-events": "none"
      };
      setClassAttribute(style2, attrs, scope, true);
    } else {
      var emphasisStyle = el.states.emphasis && el.states.emphasis.style ? el.states.emphasis.style : {};
      var fill = emphasisStyle.fill;
      if (!fill) {
        var normalFill = el.style && el.style.fill;
        var selectFill = el.states.select && el.states.select.style && el.states.select.style.fill;
        var fromFill = el.currentStates.indexOf("select") >= 0 ? selectFill || normalFill : normalFill;
        if (fromFill) {
          fill = liftColor(fromFill);
        }
      }
      var lineWidth = emphasisStyle.lineWidth;
      if (lineWidth) {
        var scaleX = !emphasisStyle.strokeNoScale && el.transform ? el.transform[0] : 1;
        lineWidth = lineWidth / scaleX;
      }
      var style2 = {
        cursor: "pointer"
      };
      if (fill) {
        style2.fill = fill;
      }
      if (emphasisStyle.stroke) {
        style2.stroke = emphasisStyle.stroke;
      }
      if (lineWidth) {
        style2["stroke-width"] = lineWidth;
      }
      setClassAttribute(style2, attrs, scope, true);
    }
  }
}
function setClassAttribute(style2, attrs, scope, withHover) {
  var styleKey = JSON.stringify(style2);
  var className = scope.cssStyleCache[styleKey];
  if (!className) {
    className = scope.zrId + "-cls-" + getClassId();
    scope.cssStyleCache[styleKey] = className;
    scope.cssNodes["." + className + (withHover ? ":hover" : "")] = style2;
  }
  attrs["class"] = attrs["class"] ? attrs["class"] + " " + className : className;
}

// node_modules/zrender/lib/svg/graphic.js
var round6 = Math.round;
function isImageLike2(val) {
  return val && isString(val.src);
}
function isCanvasLike(val) {
  return val && isFunction(val.toDataURL);
}
function setStyleAttrs(attrs, style2, el, scope) {
  mapStyleToAttrs(function(key2, val) {
    var isFillStroke = key2 === "fill" || key2 === "stroke";
    if (isFillStroke && isGradient(val)) {
      setGradient(style2, attrs, key2, scope);
    } else if (isFillStroke && isPattern(val)) {
      setPattern(el, attrs, key2, scope);
    } else {
      attrs[key2] = val;
    }
    if (isFillStroke && scope.ssr && val === "none") {
      attrs["pointer-events"] = "visible";
    }
  }, style2, el, false);
  setShadow(el, attrs, scope);
}
function setMetaData(attrs, el) {
  var metaData = getElementSSRData(el);
  if (metaData) {
    metaData.each(function(val, key2) {
      val != null && (attrs[(META_DATA_PREFIX + key2).toLowerCase()] = val + "");
    });
    if (el.isSilent()) {
      attrs[META_DATA_PREFIX + "silent"] = "true";
    }
  }
}
function noRotateScale(m2) {
  return isAroundZero2(m2[0] - 1) && isAroundZero2(m2[1]) && isAroundZero2(m2[2]) && isAroundZero2(m2[3] - 1);
}
function noTranslate(m2) {
  return isAroundZero2(m2[4]) && isAroundZero2(m2[5]);
}
function setTransform(attrs, m2, compress) {
  if (m2 && !(noTranslate(m2) && noRotateScale(m2))) {
    var mul2 = compress ? 10 : 1e4;
    attrs.transform = noRotateScale(m2) ? "translate(" + round6(m2[4] * mul2) / mul2 + " " + round6(m2[5] * mul2) / mul2 + ")" : getMatrixStr(m2);
  }
}
function convertPolyShape(shape, attrs, mul2) {
  var points4 = shape.points;
  var strArr = [];
  for (var i2 = 0; i2 < points4.length; i2++) {
    strArr.push(round6(points4[i2][0] * mul2) / mul2);
    strArr.push(round6(points4[i2][1] * mul2) / mul2);
  }
  attrs.points = strArr.join(" ");
}
function validatePolyShape(shape) {
  return !shape.smooth;
}
function createAttrsConvert(desc) {
  var normalizedDesc = map2(desc, function(item) {
    return typeof item === "string" ? [item, item] : item;
  });
  return function(shape, attrs, mul2) {
    for (var i2 = 0; i2 < normalizedDesc.length; i2++) {
      var item = normalizedDesc[i2];
      var val = shape[item[0]];
      if (val != null) {
        attrs[item[1]] = round6(val * mul2) / mul2;
      }
    }
  };
}
var builtinShapesDef = {
  circle: [createAttrsConvert(["cx", "cy", "r"])],
  polyline: [convertPolyShape, validatePolyShape],
  polygon: [convertPolyShape, validatePolyShape]
};
function hasShapeAnimation(el) {
  var animators = el.animators;
  for (var i2 = 0; i2 < animators.length; i2++) {
    if (animators[i2].targetName === "shape") {
      return true;
    }
  }
  return false;
}
function brushSVGPath(el, scope) {
  var style2 = el.style;
  var shape = el.shape;
  var builtinShpDef = builtinShapesDef[el.type];
  var attrs = {};
  var needsAnimate = scope.animation;
  var svgElType = "path";
  var strokePercent = el.style.strokePercent;
  var precision = scope.compress && getPathPrecision(el) || 4;
  if (builtinShpDef && !scope.willUpdate && !(builtinShpDef[1] && !builtinShpDef[1](shape)) && !(needsAnimate && hasShapeAnimation(el)) && !(strokePercent < 1)) {
    svgElType = el.type;
    var mul2 = Math.pow(10, precision);
    builtinShpDef[0](shape, attrs, mul2);
  } else {
    var needBuildPath = !el.path || el.shapeChanged();
    if (!el.path) {
      el.createPathProxy();
    }
    var path = el.path;
    if (needBuildPath) {
      path.beginPath();
      el.buildPath(path, el.shape);
      el.pathUpdated();
    }
    var pathVersion = path.getVersion();
    var elExt = el;
    var svgPathBuilder = elExt.__svgPathBuilder;
    if (elExt.__svgPathVersion !== pathVersion || !svgPathBuilder || strokePercent !== elExt.__svgPathStrokePercent) {
      if (!svgPathBuilder) {
        svgPathBuilder = elExt.__svgPathBuilder = new SVGPathRebuilder_default();
      }
      svgPathBuilder.reset(precision);
      path.rebuildPath(svgPathBuilder, strokePercent);
      svgPathBuilder.generateStr();
      elExt.__svgPathVersion = pathVersion;
      elExt.__svgPathStrokePercent = strokePercent;
    }
    attrs.d = svgPathBuilder.getStr();
  }
  setTransform(attrs, el.transform);
  setStyleAttrs(attrs, style2, el, scope);
  setMetaData(attrs, el);
  scope.animation && createCSSAnimation(el, attrs, scope);
  scope.emphasis && createCSSEmphasis(el, attrs, scope);
  return createVNode(svgElType, el.id + "", attrs);
}
function brushSVGImage(el, scope) {
  var style2 = el.style;
  var image = style2.image;
  if (image && !isString(image)) {
    if (isImageLike2(image)) {
      image = image.src;
    } else if (isCanvasLike(image)) {
      image = image.toDataURL();
    }
  }
  if (!image) {
    return;
  }
  var x = style2.x || 0;
  var y = style2.y || 0;
  var dw = style2.width;
  var dh = style2.height;
  var attrs = {
    href: image,
    width: dw,
    height: dh
  };
  if (x) {
    attrs.x = x;
  }
  if (y) {
    attrs.y = y;
  }
  setTransform(attrs, el.transform);
  setStyleAttrs(attrs, style2, el, scope);
  setMetaData(attrs, el);
  scope.animation && createCSSAnimation(el, attrs, scope);
  return createVNode("image", el.id + "", attrs);
}
function brushSVGTSpan(el, scope) {
  var style2 = el.style;
  var text = style2.text;
  text != null && (text += "");
  if (!text || isNaN(style2.x) || isNaN(style2.y)) {
    return;
  }
  var font = style2.font || DEFAULT_FONT;
  var x = style2.x || 0;
  var y = adjustTextY2(style2.y || 0, getLineHeight(font), style2.textBaseline);
  var textAlign = TEXT_ALIGN_TO_ANCHOR[style2.textAlign] || style2.textAlign;
  var attrs = {
    "dominant-baseline": "central",
    "text-anchor": textAlign
  };
  if (hasSeparateFont(style2)) {
    var separatedFontStr = "";
    var fontStyle = style2.fontStyle;
    var fontSize = parseFontSize(style2.fontSize);
    if (!parseFloat(fontSize)) {
      return;
    }
    var fontFamily = style2.fontFamily || DEFAULT_FONT_FAMILY;
    var fontWeight = style2.fontWeight;
    separatedFontStr += "font-size:" + fontSize + ";font-family:" + fontFamily + ";";
    if (fontStyle && fontStyle !== "normal") {
      separatedFontStr += "font-style:" + fontStyle + ";";
    }
    if (fontWeight && fontWeight !== "normal") {
      separatedFontStr += "font-weight:" + fontWeight + ";";
    }
    attrs.style = separatedFontStr;
  } else {
    attrs.style = "font: " + font;
  }
  if (text.match(/\s/)) {
    attrs["xml:space"] = "preserve";
  }
  if (x) {
    attrs.x = x;
  }
  if (y) {
    attrs.y = y;
  }
  setTransform(attrs, el.transform);
  setStyleAttrs(attrs, style2, el, scope);
  setMetaData(attrs, el);
  scope.animation && createCSSAnimation(el, attrs, scope);
  return createVNode("text", el.id + "", attrs, void 0, text);
}
function brush2(el, scope) {
  if (el instanceof Path_default) {
    return brushSVGPath(el, scope);
  } else if (el instanceof Image_default) {
    return brushSVGImage(el, scope);
  } else if (el instanceof TSpan_default) {
    return brushSVGTSpan(el, scope);
  }
}
function setShadow(el, attrs, scope) {
  var style2 = el.style;
  if (hasShadow(style2)) {
    var shadowKey = getShadowKey(el);
    var shadowCache = scope.shadowCache;
    var shadowId = shadowCache[shadowKey];
    if (!shadowId) {
      var globalScale = el.getGlobalScale();
      var scaleX = globalScale[0];
      var scaleY = globalScale[1];
      if (!scaleX || !scaleY) {
        return;
      }
      var offsetX = style2.shadowOffsetX || 0;
      var offsetY = style2.shadowOffsetY || 0;
      var blur_1 = style2.shadowBlur;
      var _a2 = normalizeColor(style2.shadowColor), opacity = _a2.opacity, color3 = _a2.color;
      var stdDx = blur_1 / 2 / scaleX;
      var stdDy = blur_1 / 2 / scaleY;
      var stdDeviation = stdDx + " " + stdDy;
      shadowId = scope.zrId + "-s" + scope.shadowIdx++;
      scope.defs[shadowId] = createVNode("filter", shadowId, {
        "id": shadowId,
        "x": "-100%",
        "y": "-100%",
        "width": "300%",
        "height": "300%"
      }, [
        createVNode("feDropShadow", "", {
          "dx": offsetX / scaleX,
          "dy": offsetY / scaleY,
          "stdDeviation": stdDeviation,
          "flood-color": color3,
          "flood-opacity": opacity
        })
      ]);
      shadowCache[shadowKey] = shadowId;
    }
    attrs.filter = getIdURL(shadowId);
  }
}
function setGradient(style2, attrs, target, scope) {
  var val = style2[target];
  var gradientTag;
  var gradientAttrs = {
    "gradientUnits": val.global ? "userSpaceOnUse" : "objectBoundingBox"
  };
  if (isLinearGradient(val)) {
    gradientTag = "linearGradient";
    gradientAttrs.x1 = val.x;
    gradientAttrs.y1 = val.y;
    gradientAttrs.x2 = val.x2;
    gradientAttrs.y2 = val.y2;
  } else if (isRadialGradient(val)) {
    gradientTag = "radialGradient";
    gradientAttrs.cx = retrieve2(val.x, 0.5);
    gradientAttrs.cy = retrieve2(val.y, 0.5);
    gradientAttrs.r = retrieve2(val.r, 0.5);
  } else {
    if (true) {
      logError("Illegal gradient type.");
    }
    return;
  }
  var colors2 = val.colorStops;
  var colorStops = [];
  for (var i2 = 0, len2 = colors2.length; i2 < len2; ++i2) {
    var offset = round4(colors2[i2].offset) * 100 + "%";
    var stopColor = colors2[i2].color;
    var _a2 = normalizeColor(stopColor), color3 = _a2.color, opacity = _a2.opacity;
    var stopsAttrs = {
      "offset": offset
    };
    stopsAttrs["stop-color"] = color3;
    if (opacity < 1) {
      stopsAttrs["stop-opacity"] = opacity;
    }
    colorStops.push(createVNode("stop", i2 + "", stopsAttrs));
  }
  var gradientVNode = createVNode(gradientTag, "", gradientAttrs, colorStops);
  var gradientKey = vNodeToString(gradientVNode);
  var gradientCache = scope.gradientCache;
  var gradientId = gradientCache[gradientKey];
  if (!gradientId) {
    gradientId = scope.zrId + "-g" + scope.gradientIdx++;
    gradientCache[gradientKey] = gradientId;
    gradientAttrs.id = gradientId;
    scope.defs[gradientId] = createVNode(gradientTag, gradientId, gradientAttrs, colorStops);
  }
  attrs[target] = getIdURL(gradientId);
}
function setPattern(el, attrs, target, scope) {
  var val = el.style[target];
  var boundingRect = el.getBoundingRect();
  var patternAttrs = {};
  var repeat = val.repeat;
  var noRepeat = repeat === "no-repeat";
  var repeatX = repeat === "repeat-x";
  var repeatY = repeat === "repeat-y";
  var child;
  if (isImagePattern(val)) {
    var imageWidth_1 = val.imageWidth;
    var imageHeight_1 = val.imageHeight;
    var imageSrc = void 0;
    var patternImage = val.image;
    if (isString(patternImage)) {
      imageSrc = patternImage;
    } else if (isImageLike2(patternImage)) {
      imageSrc = patternImage.src;
    } else if (isCanvasLike(patternImage)) {
      imageSrc = patternImage.toDataURL();
    }
    if (typeof Image === "undefined") {
      var errMsg = "Image width/height must been given explictly in svg-ssr renderer.";
      assert(imageWidth_1, errMsg);
      assert(imageHeight_1, errMsg);
    } else if (imageWidth_1 == null || imageHeight_1 == null) {
      var setSizeToVNode_1 = function(vNode, img) {
        if (vNode) {
          var svgEl = vNode.elm;
          var width = imageWidth_1 || img.width;
          var height = imageHeight_1 || img.height;
          if (vNode.tag === "pattern") {
            if (repeatX) {
              height = 1;
              width /= boundingRect.width;
            } else if (repeatY) {
              width = 1;
              height /= boundingRect.height;
            }
          }
          vNode.attrs.width = width;
          vNode.attrs.height = height;
          if (svgEl) {
            svgEl.setAttribute("width", width);
            svgEl.setAttribute("height", height);
          }
        }
      };
      var createdImage = createOrUpdateImage(imageSrc, null, el, function(img) {
        noRepeat || setSizeToVNode_1(patternVNode, img);
        setSizeToVNode_1(child, img);
      });
      if (createdImage && createdImage.width && createdImage.height) {
        imageWidth_1 = imageWidth_1 || createdImage.width;
        imageHeight_1 = imageHeight_1 || createdImage.height;
      }
    }
    child = createVNode("image", "img", {
      href: imageSrc,
      width: imageWidth_1,
      height: imageHeight_1
    });
    patternAttrs.width = imageWidth_1;
    patternAttrs.height = imageHeight_1;
  } else if (val.svgElement) {
    child = clone(val.svgElement);
    patternAttrs.width = val.svgWidth;
    patternAttrs.height = val.svgHeight;
  }
  if (!child) {
    return;
  }
  var patternWidth;
  var patternHeight;
  if (noRepeat) {
    patternWidth = patternHeight = 1;
  } else if (repeatX) {
    patternHeight = 1;
    patternWidth = patternAttrs.width / boundingRect.width;
  } else if (repeatY) {
    patternWidth = 1;
    patternHeight = patternAttrs.height / boundingRect.height;
  } else {
    patternAttrs.patternUnits = "userSpaceOnUse";
  }
  if (patternWidth != null && !isNaN(patternWidth)) {
    patternAttrs.width = patternWidth;
  }
  if (patternHeight != null && !isNaN(patternHeight)) {
    patternAttrs.height = patternHeight;
  }
  var patternTransform = getSRTTransformString(val);
  patternTransform && (patternAttrs.patternTransform = patternTransform);
  var patternVNode = createVNode("pattern", "", patternAttrs, [child]);
  var patternKey = vNodeToString(patternVNode);
  var patternCache = scope.patternCache;
  var patternId = patternCache[patternKey];
  if (!patternId) {
    patternId = scope.zrId + "-p" + scope.patternIdx++;
    patternCache[patternKey] = patternId;
    patternAttrs.id = patternId;
    patternVNode = scope.defs[patternId] = createVNode("pattern", patternId, patternAttrs, [child]);
  }
  attrs[target] = getIdURL(patternId);
}
function setClipPath(clipPath, attrs, scope) {
  var clipPathCache = scope.clipPathCache, defs = scope.defs;
  var clipPathId = clipPathCache[clipPath.id];
  if (!clipPathId) {
    clipPathId = scope.zrId + "-c" + scope.clipPathIdx++;
    var clipPathAttrs = {
      id: clipPathId
    };
    clipPathCache[clipPath.id] = clipPathId;
    defs[clipPathId] = createVNode("clipPath", clipPathId, clipPathAttrs, [brushSVGPath(clipPath, scope)]);
  }
  attrs["clip-path"] = getIdURL(clipPathId);
}

// node_modules/zrender/lib/svg/domapi.js
function createTextNode(text) {
  return document.createTextNode(text);
}
function insertBefore(parentNode2, newNode, referenceNode) {
  parentNode2.insertBefore(newNode, referenceNode);
}
function removeChild(node, child) {
  node.removeChild(child);
}
function appendChild(node, child) {
  node.appendChild(child);
}
function parentNode(node) {
  return node.parentNode;
}
function nextSibling(node) {
  return node.nextSibling;
}
function setTextContent(node, text) {
  node.textContent = text;
}

// node_modules/zrender/lib/svg/patch.js
var colonChar = 58;
var xChar = 120;
var emptyNode = createVNode("", "");
function isUndef(s) {
  return s === void 0;
}
function isDef(s) {
  return s !== void 0;
}
function createKeyToOldIdx(children, beginIdx, endIdx) {
  var map4 = {};
  for (var i2 = beginIdx; i2 <= endIdx; ++i2) {
    var key2 = children[i2].key;
    if (key2 !== void 0) {
      if (true) {
        if (map4[key2] != null) {
          console.error("Duplicate key " + key2);
        }
      }
      map4[key2] = i2;
    }
  }
  return map4;
}
function sameVnode(vnode1, vnode2) {
  var isSameKey = vnode1.key === vnode2.key;
  var isSameTag = vnode1.tag === vnode2.tag;
  return isSameTag && isSameKey;
}
function createElm(vnode) {
  var i2;
  var children = vnode.children;
  var tag = vnode.tag;
  if (isDef(tag)) {
    var elm = vnode.elm = createElement(tag);
    updateAttrs(emptyNode, vnode);
    if (isArray(children)) {
      for (i2 = 0; i2 < children.length; ++i2) {
        var ch = children[i2];
        if (ch != null) {
          appendChild(elm, createElm(ch));
        }
      }
    } else if (isDef(vnode.text) && !isObject(vnode.text)) {
      appendChild(elm, createTextNode(vnode.text));
    }
  } else {
    vnode.elm = createTextNode(vnode.text);
  }
  return vnode.elm;
}
function addVnodes(parentElm, before, vnodes, startIdx, endIdx) {
  for (; startIdx <= endIdx; ++startIdx) {
    var ch = vnodes[startIdx];
    if (ch != null) {
      insertBefore(parentElm, createElm(ch), before);
    }
  }
}
function removeVnodes(parentElm, vnodes, startIdx, endIdx) {
  for (; startIdx <= endIdx; ++startIdx) {
    var ch = vnodes[startIdx];
    if (ch != null) {
      if (isDef(ch.tag)) {
        var parent_1 = parentNode(ch.elm);
        removeChild(parent_1, ch.elm);
      } else {
        removeChild(parentElm, ch.elm);
      }
    }
  }
}
function updateAttrs(oldVnode, vnode) {
  var key2;
  var elm = vnode.elm;
  var oldAttrs = oldVnode && oldVnode.attrs || {};
  var attrs = vnode.attrs || {};
  if (oldAttrs === attrs) {
    return;
  }
  for (key2 in attrs) {
    var cur = attrs[key2];
    var old = oldAttrs[key2];
    if (old !== cur) {
      if (cur === true) {
        elm.setAttribute(key2, "");
      } else if (cur === false) {
        elm.removeAttribute(key2);
      } else {
        if (key2 === "style") {
          elm.style.cssText = cur;
        } else if (key2.charCodeAt(0) !== xChar) {
          elm.setAttribute(key2, cur);
        } else if (key2 === "xmlns:xlink" || key2 === "xmlns") {
          elm.setAttributeNS(XMLNS, key2, cur);
        } else if (key2.charCodeAt(3) === colonChar) {
          elm.setAttributeNS(XML_NAMESPACE, key2, cur);
        } else if (key2.charCodeAt(5) === colonChar) {
          elm.setAttributeNS(XLINKNS, key2, cur);
        } else {
          elm.setAttribute(key2, cur);
        }
      }
    }
  }
  for (key2 in oldAttrs) {
    if (!(key2 in attrs)) {
      elm.removeAttribute(key2);
    }
  }
}
function updateChildren(parentElm, oldCh, newCh) {
  var oldStartIdx = 0;
  var newStartIdx = 0;
  var oldEndIdx = oldCh.length - 1;
  var oldStartVnode = oldCh[0];
  var oldEndVnode = oldCh[oldEndIdx];
  var newEndIdx = newCh.length - 1;
  var newStartVnode = newCh[0];
  var newEndVnode = newCh[newEndIdx];
  var oldKeyToIdx;
  var idxInOld;
  var elmToMove;
  var before;
  while (oldStartIdx <= oldEndIdx && newStartIdx <= newEndIdx) {
    if (oldStartVnode == null) {
      oldStartVnode = oldCh[++oldStartIdx];
    } else if (oldEndVnode == null) {
      oldEndVnode = oldCh[--oldEndIdx];
    } else if (newStartVnode == null) {
      newStartVnode = newCh[++newStartIdx];
    } else if (newEndVnode == null) {
      newEndVnode = newCh[--newEndIdx];
    } else if (sameVnode(oldStartVnode, newStartVnode)) {
      patchVnode(oldStartVnode, newStartVnode);
      oldStartVnode = oldCh[++oldStartIdx];
      newStartVnode = newCh[++newStartIdx];
    } else if (sameVnode(oldEndVnode, newEndVnode)) {
      patchVnode(oldEndVnode, newEndVnode);
      oldEndVnode = oldCh[--oldEndIdx];
      newEndVnode = newCh[--newEndIdx];
    } else if (sameVnode(oldStartVnode, newEndVnode)) {
      patchVnode(oldStartVnode, newEndVnode);
      insertBefore(parentElm, oldStartVnode.elm, nextSibling(oldEndVnode.elm));
      oldStartVnode = oldCh[++oldStartIdx];
      newEndVnode = newCh[--newEndIdx];
    } else if (sameVnode(oldEndVnode, newStartVnode)) {
      patchVnode(oldEndVnode, newStartVnode);
      insertBefore(parentElm, oldEndVnode.elm, oldStartVnode.elm);
      oldEndVnode = oldCh[--oldEndIdx];
      newStartVnode = newCh[++newStartIdx];
    } else {
      if (isUndef(oldKeyToIdx)) {
        oldKeyToIdx = createKeyToOldIdx(oldCh, oldStartIdx, oldEndIdx);
      }
      idxInOld = oldKeyToIdx[newStartVnode.key];
      if (isUndef(idxInOld)) {
        insertBefore(parentElm, createElm(newStartVnode), oldStartVnode.elm);
      } else {
        elmToMove = oldCh[idxInOld];
        if (elmToMove.tag !== newStartVnode.tag) {
          insertBefore(parentElm, createElm(newStartVnode), oldStartVnode.elm);
        } else {
          patchVnode(elmToMove, newStartVnode);
          oldCh[idxInOld] = void 0;
          insertBefore(parentElm, elmToMove.elm, oldStartVnode.elm);
        }
      }
      newStartVnode = newCh[++newStartIdx];
    }
  }
  if (oldStartIdx <= oldEndIdx || newStartIdx <= newEndIdx) {
    if (oldStartIdx > oldEndIdx) {
      before = newCh[newEndIdx + 1] == null ? null : newCh[newEndIdx + 1].elm;
      addVnodes(parentElm, before, newCh, newStartIdx, newEndIdx);
    } else {
      removeVnodes(parentElm, oldCh, oldStartIdx, oldEndIdx);
    }
  }
}
function patchVnode(oldVnode, vnode) {
  var elm = vnode.elm = oldVnode.elm;
  var oldCh = oldVnode.children;
  var ch = vnode.children;
  if (oldVnode === vnode) {
    return;
  }
  updateAttrs(oldVnode, vnode);
  if (isUndef(vnode.text)) {
    if (isDef(oldCh) && isDef(ch)) {
      if (oldCh !== ch) {
        updateChildren(elm, oldCh, ch);
      }
    } else if (isDef(ch)) {
      if (isDef(oldVnode.text)) {
        setTextContent(elm, "");
      }
      addVnodes(elm, null, ch, 0, ch.length - 1);
    } else if (isDef(oldCh)) {
      removeVnodes(elm, oldCh, 0, oldCh.length - 1);
    } else if (isDef(oldVnode.text)) {
      setTextContent(elm, "");
    }
  } else if (oldVnode.text !== vnode.text) {
    if (isDef(oldCh)) {
      removeVnodes(elm, oldCh, 0, oldCh.length - 1);
    }
    setTextContent(elm, vnode.text);
  }
}
function patch(oldVnode, vnode) {
  if (sameVnode(oldVnode, vnode)) {
    patchVnode(oldVnode, vnode);
  } else {
    var elm = oldVnode.elm;
    var parent_2 = parentNode(elm);
    createElm(vnode);
    if (parent_2 !== null) {
      insertBefore(parent_2, vnode.elm, nextSibling(elm));
      removeVnodes(parent_2, [oldVnode], 0, 0);
    }
  }
  return vnode;
}

// node_modules/zrender/lib/svg/Painter.js
var svgId = 0;
var SVGPainter = (function() {
  function SVGPainter2(root, storage2, opts) {
    this.type = "svg";
    this.configLayer = createMethodNotSupport("configLayer");
    this.storage = storage2;
    this._opts = opts = extend({}, opts);
    this.root = root;
    this._id = "zr" + svgId++;
    this._oldVNode = createSVGVNode(opts.width, opts.height);
    if (root && !opts.ssr) {
      var viewport = this._viewport = document.createElement("div");
      viewport.style.cssText = "position:relative;overflow:hidden";
      var svgDom = this._svgDom = this._oldVNode.elm = createElement("svg");
      updateAttrs(null, this._oldVNode);
      viewport.appendChild(svgDom);
      root.appendChild(viewport);
    }
    this.resize(opts.width, opts.height);
  }
  SVGPainter2.prototype.getType = function() {
    return this.type;
  };
  SVGPainter2.prototype.getViewportRoot = function() {
    return this._viewport;
  };
  SVGPainter2.prototype.getViewportRootOffset = function() {
    var viewportRoot = this.getViewportRoot();
    if (viewportRoot) {
      return {
        offsetLeft: viewportRoot.offsetLeft || 0,
        offsetTop: viewportRoot.offsetTop || 0
      };
    }
  };
  SVGPainter2.prototype.getSvgDom = function() {
    return this._svgDom;
  };
  SVGPainter2.prototype.refresh = function() {
    if (this.root) {
      var vnode = this.renderToVNode({
        willUpdate: true
      });
      vnode.attrs.style = "position:absolute;left:0;top:0;user-select:none";
      patch(this._oldVNode, vnode);
      this._oldVNode = vnode;
    }
  };
  SVGPainter2.prototype.renderOneToVNode = function(el) {
    return brush2(el, createBrushScope(this._id));
  };
  SVGPainter2.prototype.renderToVNode = function(opts) {
    opts = opts || {};
    var list = this.storage.getDisplayList(true);
    var width = this._width;
    var height = this._height;
    var scope = createBrushScope(this._id);
    scope.animation = opts.animation;
    scope.willUpdate = opts.willUpdate;
    scope.compress = opts.compress;
    scope.emphasis = opts.emphasis;
    scope.ssr = this._opts.ssr;
    var children = [];
    var bgVNode = this._bgVNode = createBackgroundVNode(width, height, this._backgroundColor, scope);
    bgVNode && children.push(bgVNode);
    var mainVNode = !opts.compress ? this._mainVNode = createVNode("g", "main", {}, []) : null;
    this._paintList(list, scope, mainVNode ? mainVNode.children : children);
    mainVNode && children.push(mainVNode);
    var defs = map2(keys(scope.defs), function(id) {
      return scope.defs[id];
    });
    if (defs.length) {
      children.push(createVNode("defs", "defs", {}, defs));
    }
    if (opts.animation) {
      var animationCssStr = getCssString(scope.cssNodes, scope.cssAnims, { newline: true });
      if (animationCssStr) {
        var styleNode = createVNode("style", "stl", {}, [], animationCssStr);
        children.push(styleNode);
      }
    }
    return createSVGVNode(width, height, children, opts.useViewBox);
  };
  SVGPainter2.prototype.renderToString = function(opts) {
    opts = opts || {};
    return vNodeToString(this.renderToVNode({
      animation: retrieve2(opts.cssAnimation, true),
      emphasis: retrieve2(opts.cssEmphasis, true),
      willUpdate: false,
      compress: true,
      useViewBox: retrieve2(opts.useViewBox, true)
    }), { newline: true });
  };
  SVGPainter2.prototype.setBackgroundColor = function(backgroundColor2) {
    this._backgroundColor = backgroundColor2;
  };
  SVGPainter2.prototype.getSvgRoot = function() {
    return this._mainVNode && this._mainVNode.elm;
  };
  SVGPainter2.prototype._paintList = function(list, scope, out2) {
    var listLen = list.length;
    var clipPathsGroupsStack = [];
    var clipPathsGroupsStackDepth = 0;
    var currentClipPathGroup;
    var prevClipPaths;
    var clipGroupNodeIdx = 0;
    for (var i2 = 0; i2 < listLen; i2++) {
      var displayable = list[i2];
      if (!displayable.invisible) {
        var clipPaths = displayable.__clipPaths;
        var len2 = clipPaths && clipPaths.length || 0;
        var prevLen = prevClipPaths && prevClipPaths.length || 0;
        var lca = void 0;
        for (lca = Math.max(len2 - 1, prevLen - 1); lca >= 0; lca--) {
          if (clipPaths && prevClipPaths && clipPaths[lca] === prevClipPaths[lca]) {
            break;
          }
        }
        for (var i_1 = prevLen - 1; i_1 > lca; i_1--) {
          clipPathsGroupsStackDepth--;
          currentClipPathGroup = clipPathsGroupsStack[clipPathsGroupsStackDepth - 1];
        }
        for (var i_2 = lca + 1; i_2 < len2; i_2++) {
          var groupAttrs = {};
          setClipPath(clipPaths[i_2], groupAttrs, scope);
          var g = createVNode("g", "clip-g-" + clipGroupNodeIdx++, groupAttrs, []);
          (currentClipPathGroup ? currentClipPathGroup.children : out2).push(g);
          clipPathsGroupsStack[clipPathsGroupsStackDepth++] = g;
          currentClipPathGroup = g;
        }
        prevClipPaths = clipPaths;
        var ret = brush2(displayable, scope);
        if (ret) {
          (currentClipPathGroup ? currentClipPathGroup.children : out2).push(ret);
        }
      }
    }
  };
  SVGPainter2.prototype.resize = function(width, height) {
    var opts = this._opts;
    var root = this.root;
    var viewport = this._viewport;
    width != null && (opts.width = width);
    height != null && (opts.height = height);
    if (root && viewport) {
      viewport.style.display = "none";
      width = getSize(root, 0, opts);
      height = getSize(root, 1, opts);
      viewport.style.display = "";
    }
    if (this._width !== width || this._height !== height) {
      this._width = width;
      this._height = height;
      if (viewport) {
        var viewportStyle = viewport.style;
        viewportStyle.width = width + "px";
        viewportStyle.height = height + "px";
      }
      if (!isPattern(this._backgroundColor)) {
        var svgDom = this._svgDom;
        if (svgDom) {
          svgDom.setAttribute("width", width);
          svgDom.setAttribute("height", height);
        }
        var bgEl = this._bgVNode && this._bgVNode.elm;
        if (bgEl) {
          bgEl.setAttribute("width", width);
          bgEl.setAttribute("height", height);
        }
      } else {
        this.refresh();
      }
    }
  };
  SVGPainter2.prototype.getWidth = function() {
    return this._width;
  };
  SVGPainter2.prototype.getHeight = function() {
    return this._height;
  };
  SVGPainter2.prototype.dispose = function() {
    if (this.root) {
      this.root.innerHTML = "";
    }
    this._svgDom = this._viewport = this.storage = this._oldVNode = this._bgVNode = this._mainVNode = null;
  };
  SVGPainter2.prototype.clear = function() {
    if (this._svgDom) {
      this._svgDom.innerHTML = null;
    }
    this._oldVNode = null;
  };
  SVGPainter2.prototype.toDataURL = function(base64) {
    var str = this.renderToString();
    var prefix = "data:image/svg+xml;";
    if (base64) {
      str = encodeBase64(str);
      return str && prefix + "base64," + str;
    }
    return prefix + "charset=UTF-8," + encodeURIComponent(str);
  };
  return SVGPainter2;
})();
function createMethodNotSupport(method) {
  return function() {
    if (true) {
      logError('In SVG mode painter not support method "' + method + '"');
    }
  };
}
function createBackgroundVNode(width, height, backgroundColor2, scope) {
  var bgVNode;
  if (backgroundColor2 && backgroundColor2 !== "none") {
    bgVNode = createVNode("rect", "bg", {
      width,
      height,
      x: "0",
      y: "0"
    });
    if (isGradient(backgroundColor2)) {
      setGradient({ fill: backgroundColor2 }, bgVNode.attrs, "fill", scope);
    } else if (isPattern(backgroundColor2)) {
      setPattern({
        style: {
          fill: backgroundColor2
        },
        dirty: noop,
        getBoundingRect: function() {
          return { width, height };
        }
      }, bgVNode.attrs, "fill", scope);
    } else {
      var _a2 = normalizeColor(backgroundColor2), color3 = _a2.color, opacity = _a2.opacity;
      bgVNode.attrs.fill = color3;
      opacity < 1 && (bgVNode.attrs["fill-opacity"] = opacity);
    }
  }
  return bgVNode;
}
var Painter_default = SVGPainter;

// node_modules/echarts/lib/renderer/installSVGRenderer.js
function install57(registers) {
  registers.registerPainter("svg", Painter_default);
}

// node_modules/zrender/lib/canvas/Layer.js
function createDom(id, painter, dpr2) {
  var newDom = platformApi.createCanvas();
  var width = painter.getWidth();
  var height = painter.getHeight();
  var newDomStyle = newDom.style;
  if (newDomStyle) {
    newDomStyle.position = "absolute";
    newDomStyle.left = "0";
    newDomStyle.top = "0";
    newDomStyle.width = width + "px";
    newDomStyle.height = height + "px";
    newDom.setAttribute("data-zr-dom-id", id);
  }
  newDom.width = width * dpr2;
  newDom.height = height * dpr2;
  return newDom;
}
function isIncrementalLayer(layer) {
  return !layer.__cursors.get(INCREMENTAL_ID_FALSE);
}
function getStartEndFromCursor(layer) {
  var cursor = layer.__cursors.get(INCREMENTAL_ID_FALSE);
  return {
    startIdx: cursor ? cursor.startIdx : 0,
    endIdx: cursor ? cursor.endIdx : 0
  };
}
var Layer = (function(_super) {
  __extends2(Layer2, _super);
  function Layer2(id, painter, dpr2) {
    var _this = _super.call(this) || this;
    _this.motionBlur = false;
    _this.lastFrameAlpha = 0.7;
    _this.dpr = 1;
    _this.virtual = false;
    _this.config = {};
    _this.zlevel = 0;
    _this.zlevel2 = ZLEVEL2_NORMAL_BELOW;
    _this.maxRepaintRectCount = 5;
    _this.__dirty = true;
    _this.__firstTimePaint = true;
    _this.__prevIdx = { startIdx: 0, endIdx: 0 };
    var dom;
    dpr2 = dpr2 || devicePixelRatio;
    if (typeof id === "string") {
      dom = createDom(id, painter, dpr2);
    } else if (isObject(id)) {
      dom = id;
      id = dom.id;
    }
    _this.id = id;
    _this.dom = dom;
    var domStyle = dom.style;
    if (domStyle) {
      disableUserSelect(dom);
      dom.onselectstart = function() {
        return false;
      };
      domStyle.padding = "0";
      domStyle.margin = "0";
      domStyle.borderWidth = "0";
    }
    _this.painter = painter;
    _this.dpr = dpr2;
    return _this;
  }
  Layer2.prototype.afterBrush = function() {
    this.__prevIdx = getStartEndFromCursor(this);
  };
  Layer2.prototype.initContext = function() {
    this.ctx = this.dom.getContext("2d");
    this.ctx.dpr = this.dpr;
  };
  Layer2.prototype.setUnpainted = function() {
    this.__firstTimePaint = true;
  };
  Layer2.prototype.createBackBuffer = function() {
    var dpr2 = this.dpr;
    this.domBack = createDom("back-" + this.id, this.painter, dpr2);
    this.ctxBack = this.domBack.getContext("2d");
    if (dpr2 !== 1) {
      this.ctxBack.scale(dpr2, dpr2);
    }
  };
  Layer2.prototype.createRepaintRects = function(displayList, prevList, viewWidth, viewHeight) {
    if (this.__firstTimePaint) {
      this.__firstTimePaint = false;
      return null;
    }
    var mergedRepaintRects = [];
    var maxRepaintRectCount = this.maxRepaintRectCount;
    var full = false;
    var pendingRect = new BoundingRect_default(0, 0, 0, 0);
    function addRectToMergePool(rect) {
      if (!rect.isFinite() || rect.isZero()) {
        return;
      }
      if (mergedRepaintRects.length === 0) {
        var boundingRect = new BoundingRect_default(0, 0, 0, 0);
        boundingRect.copy(rect);
        mergedRepaintRects.push(boundingRect);
      } else {
        var isMerged = false;
        var minDeltaArea = Infinity;
        var bestRectToMergeIdx = 0;
        for (var i3 = 0; i3 < mergedRepaintRects.length; ++i3) {
          var mergedRect = mergedRepaintRects[i3];
          if (mergedRect.intersect(rect)) {
            var pendingRect_1 = new BoundingRect_default(0, 0, 0, 0);
            pendingRect_1.copy(mergedRect);
            pendingRect_1.union(rect);
            mergedRepaintRects[i3] = pendingRect_1;
            isMerged = true;
            break;
          } else if (full) {
            pendingRect.copy(rect);
            pendingRect.union(mergedRect);
            var aArea = rect.width * rect.height;
            var bArea = mergedRect.width * mergedRect.height;
            var pendingArea = pendingRect.width * pendingRect.height;
            var deltaArea = pendingArea - aArea - bArea;
            if (deltaArea < minDeltaArea) {
              minDeltaArea = deltaArea;
              bestRectToMergeIdx = i3;
            }
          }
        }
        if (full) {
          mergedRepaintRects[bestRectToMergeIdx].union(rect);
          isMerged = true;
        }
        if (!isMerged) {
          var boundingRect = new BoundingRect_default(0, 0, 0, 0);
          boundingRect.copy(rect);
          mergedRepaintRects.push(boundingRect);
        }
        if (!full) {
          full = mergedRepaintRects.length >= maxRepaintRectCount;
        }
      }
    }
    var se = getStartEndFromCursor(this);
    for (var i2 = se.startIdx; i2 < se.endIdx; ++i2) {
      var el = displayList[i2];
      if (el) {
        var shouldPaint = el.shouldBePainted(viewWidth, viewHeight, true, true);
        var prevRect = el.__isRendered && (el.__dirty & REDRAW_BIT || !shouldPaint) ? el.getPrevPaintRect() : null;
        if (prevRect) {
          addRectToMergePool(prevRect);
        }
        var curRect = shouldPaint && (el.__dirty & REDRAW_BIT || !el.__isRendered) ? el.getPaintRect() : null;
        if (curRect) {
          addRectToMergePool(curRect);
        }
      }
    }
    var prevIdx = this.__prevIdx;
    for (var i2 = prevIdx.startIdx; i2 < prevIdx.endIdx; ++i2) {
      var el = prevList[i2];
      var shouldPaint = el && el.shouldBePainted(viewWidth, viewHeight, true, true);
      if (el && (!shouldPaint || !el.__zr) && el.__isRendered) {
        var prevRect = el.getPrevPaintRect();
        if (prevRect) {
          addRectToMergePool(prevRect);
        }
      }
    }
    var hasIntersections;
    do {
      hasIntersections = false;
      for (var i2 = 0; i2 < mergedRepaintRects.length; ) {
        if (mergedRepaintRects[i2].isZero()) {
          mergedRepaintRects.splice(i2, 1);
          continue;
        }
        for (var j = i2 + 1; j < mergedRepaintRects.length; ) {
          if (mergedRepaintRects[i2].intersect(mergedRepaintRects[j])) {
            hasIntersections = true;
            mergedRepaintRects[i2].union(mergedRepaintRects[j]);
            mergedRepaintRects.splice(j, 1);
          } else {
            j++;
          }
        }
        i2++;
      }
    } while (hasIntersections);
    this._paintRects = mergedRepaintRects;
    return mergedRepaintRects;
  };
  Layer2.prototype.debugGetPaintRects = function() {
    return (this._paintRects || []).slice();
  };
  Layer2.prototype.resize = function(width, height) {
    var dpr2 = this.dpr;
    var dom = this.dom;
    var domStyle = dom.style;
    var domBack = this.domBack;
    if (domStyle) {
      domStyle.width = width + "px";
      domStyle.height = height + "px";
    }
    dom.width = width * dpr2;
    dom.height = height * dpr2;
    if (domBack) {
      domBack.width = width * dpr2;
      domBack.height = height * dpr2;
      if (dpr2 !== 1) {
        this.ctxBack.scale(dpr2, dpr2);
      }
    }
  };
  Layer2.prototype.clear = function(clearAll, clearColor, repaintRects) {
    var dom = this.dom;
    var ctx = this.ctx;
    var width = dom.width;
    var height = dom.height;
    clearColor = clearColor || this.clearColor;
    var haveMotionBLur = this.motionBlur && !clearAll;
    var lastFrameAlpha = this.lastFrameAlpha;
    var dpr2 = this.dpr;
    var self2 = this;
    if (haveMotionBLur) {
      if (!this.domBack) {
        this.createBackBuffer();
      }
      this.ctxBack.globalCompositeOperation = "copy";
      this.ctxBack.drawImage(dom, 0, 0, width / dpr2, height / dpr2);
    }
    var domBack = this.domBack;
    function doClear(x, y, width2, height2) {
      ctx.clearRect(x, y, width2, height2);
      if (clearColor && clearColor !== "transparent") {
        var clearColorGradientOrPattern = void 0;
        if (isGradientObject(clearColor)) {
          var shouldCache = clearColor.global || clearColor.__width === width2 && clearColor.__height === height2;
          clearColorGradientOrPattern = shouldCache && clearColor.__canvasGradient || getCanvasGradient(ctx, clearColor, {
            x: 0,
            y: 0,
            width: width2,
            height: height2
          });
          clearColor.__canvasGradient = clearColorGradientOrPattern;
          clearColor.__width = width2;
          clearColor.__height = height2;
        } else if (isImagePatternObject(clearColor)) {
          clearColor.scaleX = clearColor.scaleX || dpr2;
          clearColor.scaleY = clearColor.scaleY || dpr2;
          clearColorGradientOrPattern = createCanvasPattern(ctx, clearColor, {
            dirty: function() {
              self2.setUnpainted();
              self2.painter.refresh();
            }
          });
        }
        ctx.save();
        ctx.fillStyle = clearColorGradientOrPattern || clearColor;
        ctx.fillRect(x, y, width2, height2);
        ctx.restore();
      }
      if (haveMotionBLur) {
        ctx.save();
        ctx.globalAlpha = lastFrameAlpha;
        ctx.drawImage(domBack, x, y, width2, height2);
        ctx.restore();
      }
    }
    ;
    if (!repaintRects || haveMotionBLur) {
      doClear(0, 0, width, height);
    } else if (repaintRects.length) {
      each(repaintRects, function(rect) {
        doClear(rect.x * dpr2, rect.y * dpr2, rect.width * dpr2, rect.height * dpr2);
      });
    }
  };
  return Layer2;
})(Eventful_default);
var Layer_default = Layer;

// node_modules/zrender/lib/canvas/Painter.js
var HOVER_LAYER_ZLEVEL = 1e5;
var CANVAS_ZLEVEL = 314159;
var HOVER_LAYER_DIRTY_NO = void 0;
var HOVER_LAYER_DIRTY_REPAINT_IF_EXISTING = 1;
var HOVER_LAYER_DIRTY_REPAINT = 2;
function isLayerValid(layer) {
  if (!layer) {
    return false;
  }
  if (layer.__builtin__) {
    return true;
  }
  if (typeof layer.resize !== "function" || typeof layer.refresh !== "function") {
    return false;
  }
  return true;
}
function createRoot(width, height) {
  var domRoot = document.createElement("div");
  domRoot.style.cssText = [
    "position:relative",
    "width:" + width + "px",
    "height:" + height + "px",
    "padding:0",
    "margin:0",
    "border-width:0"
  ].join(";") + ";";
  return domRoot;
}
function createBuiltinLayer(id, painter, zlevel, zlevel2) {
  var layer = new Layer_default(id, painter, painter.dpr);
  layer.zlevel = zlevel;
  layer.zlevel2 = zlevel2;
  layer.__builtin__ = true;
  resetLayerDrawCursors(layer);
  return layer;
}
function resetLayerDrawCursors(layer) {
  layer.__cursorStack = [];
  layer.__cursors = createHashMap();
}
function resetLayerDrawCursor(cursor) {
  cursor.startIdx = cursor.drawIdx = cursor.endIdx = cursor.endIdxNew = 0;
  cursor.used = false;
  cursor.first = cursor.last = NaN;
  cursor.notClearIdx = -1;
  return cursor;
}
function ensureLayerDrawCursor(layer, incrementalCompat) {
  var cursors = layer.__cursors;
  var incremental = +incrementalCompat;
  return cursors.get(incremental) || (layer.__cursorStack.push(incremental), cursors.set(incremental, resetLayerDrawCursor({ key: incremental })));
}
function eachCursorInLayer(layer, cb) {
  var cursorStack = layer.__cursorStack;
  for (var i2 = 0; i2 < cursorStack.length; i2++) {
    cb(layer.__cursors.get(cursorStack[i2]));
  }
}
function ensureLayerListInZLevel(internal, zlevel) {
  var layers = internal.layers;
  return layers[zlevel] || (layers[zlevel] = new Array(3));
}
function eachLayer(internal, cb, filter3) {
  var layerStack = internal.layerStack;
  for (var i2 = 0; i2 < layerStack.length; i2++) {
    var zlevel = layerStack[i2].zl;
    var zlevel2 = layerStack[i2].zl2;
    var layer = internal.layers[zlevel][zlevel2];
    if (!filter3 || (!(filter3 & EACH_LAYER_BUILTIN) || layer.__builtin__) && (!(filter3 & EACH_LAYER_NOT_BUILTIN) || !layer.__builtin__) && (!(filter3 & EACH_LAYER_NOT_HOVER) || layer !== internal.hoverlayer)) {
      cb(layer, zlevel, zlevel2, i2);
    }
  }
}
var EACH_LAYER_BUILTIN = 1;
var EACH_LAYER_NOT_BUILTIN = 2;
var EACH_LAYER_NOT_HOVER = 4;
var EACH_LAYER_BUILTIN_NOT_HOVER = EACH_LAYER_BUILTIN | EACH_LAYER_NOT_HOVER;
var CanvasPainter = (function() {
  function CanvasPainter2(root, storage2, opts, id) {
    this.type = "canvas";
    this._prevDisplayList = [];
    this._layerConfig = {};
    this._needsManuallyCompositing = false;
    this.type = "canvas";
    this._i = {
      layerStack: [],
      layers: []
    };
    var singleCanvas = !root.nodeName || root.nodeName.toUpperCase() === "CANVAS";
    this._opts = opts = extend({}, opts || {});
    this.dpr = opts.devicePixelRatio || devicePixelRatio;
    this._singleCanvas = singleCanvas;
    this.root = root;
    var rootStyle = root.style;
    if (rootStyle) {
      disableUserSelect(root);
      root.innerHTML = "";
    }
    this.storage = storage2;
    this._prevDisplayList = [];
    if (!singleCanvas) {
      this._width = getSize(root, 0, opts);
      this._height = getSize(root, 1, opts);
      var domRoot = this._domRoot = createRoot(this._width, this._height);
      root.appendChild(domRoot);
    } else {
      var rootCanvas = root;
      var width = rootCanvas.width;
      var height = rootCanvas.height;
      if (opts.width != null) {
        width = opts.width;
      }
      if (opts.height != null) {
        height = opts.height;
      }
      this.dpr = opts.devicePixelRatio || 1;
      rootCanvas.width = width * this.dpr;
      rootCanvas.height = height * this.dpr;
      this._width = width;
      this._height = height;
      var singleLayer = createBuiltinLayer(rootCanvas, this, CANVAS_ZLEVEL, ZLEVEL2_NORMAL_BELOW);
      singleLayer.initContext();
      this._insertLayer(singleLayer, CANVAS_ZLEVEL, ZLEVEL2_NORMAL_BELOW, true);
      this._domRoot = root;
    }
  }
  CanvasPainter2.prototype.getType = function() {
    return "canvas";
  };
  CanvasPainter2.prototype.isSingleCanvas = function() {
    return this._singleCanvas;
  };
  CanvasPainter2.prototype.getViewportRoot = function() {
    return this._domRoot;
  };
  CanvasPainter2.prototype.getViewportRootOffset = function() {
    var viewportRoot = this.getViewportRoot();
    if (viewportRoot) {
      return {
        offsetLeft: viewportRoot.offsetLeft || 0,
        offsetTop: viewportRoot.offsetTop || 0
      };
    }
  };
  CanvasPainter2.prototype.refresh = function(optOrPaintAll) {
    var opt;
    if (optOrPaintAll && !isObject(optOrPaintAll)) {
      opt = { paintAll: !!optOrPaintAll };
    } else {
      opt = optOrPaintAll || {};
    }
    var refresh = retrieve2(opt.refresh, true);
    var refreshHover = retrieve2(opt.refreshHover, false);
    if (refreshHover) {
      this._hoverLayerDirty = HOVER_LAYER_DIRTY_REPAINT;
    }
    if (!refresh) {
      if (refreshHover) {
        this._paintHoverList(this.storage.getDisplayList(false));
      }
      return this;
    }
    var list = this.storage.getDisplayList(true);
    this._updateLayerStatus(list, opt.paintAll);
    this._redrawId = Math.random();
    var prevList = this._prevDisplayList;
    this._paintList(list, prevList, this._redrawId);
    var bgColor = this._backgroundColor;
    eachLayer(this._i, function(layer, zlevel, zlevel2, idx) {
      if (layer.refresh) {
        layer.refresh(idx === 0 ? bgColor : null);
      }
    }, EACH_LAYER_NOT_BUILTIN);
    if (this._opts.useDirtyRect) {
      this._prevDisplayList = list.slice();
    }
    return this;
  };
  CanvasPainter2.prototype._paintHoverList = function(list) {
    var hoverLayer = this._i.hoverlayer;
    var hoverLayerDirty = this._hoverLayerDirty;
    this._hoverLayerDirty = HOVER_LAYER_DIRTY_NO;
    if (hoverLayerDirty === HOVER_LAYER_DIRTY_NO) {
      return;
    }
    if (!hoverLayer && hoverLayerDirty === HOVER_LAYER_DIRTY_REPAINT) {
      hoverLayer = this._i.hoverlayer = this._ensureLayer(HOVER_LAYER_ZLEVEL);
    }
    if (!hoverLayer) {
      return;
    }
    hoverLayer.clear();
    var scope = {
      inHover: true,
      viewWidth: this._width,
      viewHeight: this._height,
      beforeBrushParam: {}
    };
    var ctx;
    for (var i2 = 0, len2 = list.length; i2 < len2; i2++) {
      var el = list[i2];
      if (!el.__inHover) {
        continue;
      }
      if (!ctx) {
        ctx = hoverLayer.ctx;
        ctx.save();
      }
      var hoverStyle = el.__hoverStyle;
      var originalStyle = void 0;
      if (hoverStyle) {
        originalStyle = el.style;
        el.style = hoverStyle;
      }
      brush(ctx, el, scope);
      if (hoverStyle) {
        el.style = originalStyle;
      }
    }
    if (ctx) {
      brushLoopFinalize(ctx, scope);
      ctx.restore();
    }
  };
  CanvasPainter2.prototype.getHoverLayer = function() {
    return this._ensureLayer(HOVER_LAYER_ZLEVEL);
  };
  CanvasPainter2.prototype.paintOne = function(ctx, el) {
    brushSingle(ctx, el);
  };
  CanvasPainter2.prototype._paintList = function(list, prevList, redrawId) {
    if (this._redrawId !== redrawId) {
      return;
    }
    var finished = this._doPaintList(list, prevList);
    if (this._needsManuallyCompositing) {
      this._compositeManually();
    }
    if (!finished) {
      var self_1 = this;
      requestAnimationFrame_default(function() {
        self_1._paintList(list, prevList, redrawId);
      });
    } else {
      eachLayer(this._i, function(layer) {
        layer.afterBrush && layer.afterBrush();
      }, EACH_LAYER_BUILTIN_NOT_HOVER);
      this._paintHoverList(list);
    }
  };
  CanvasPainter2.prototype._compositeManually = function() {
    var ctx = this._ensureLayer(CANVAS_ZLEVEL).ctx;
    var width = this._domRoot.width;
    var height = this._domRoot.height;
    ctx.clearRect(0, 0, width, height);
    eachLayer(this._i, function(layer) {
      if (layer.virtual) {
        ctx.drawImage(layer.dom, 0, 0, width, height);
      }
    }, EACH_LAYER_BUILTIN);
  };
  CanvasPainter2.prototype._doPaintList = function(list, prevList) {
    var painter = this;
    var finished = true;
    eachLayer(this._i, function(layer) {
      var needDraw = false;
      eachCursorInLayer(layer, function(cursor) {
        if (cursor.drawIdx < cursor.endIdx || cursor.notClearIdx >= 0) {
          needDraw = true;
        }
      });
      if (!needDraw && !layer.__dirty) {
        return;
      }
      var repaintRects = painter._opts.useDirtyRect && !isIncrementalLayer(layer) ? layer.createRepaintRects(list, prevList, painter._width, painter._height) : null;
      var firstLayerKey = painter._i.layerStack[0];
      var contentRetained = true;
      if (layer.__dirty) {
        contentRetained = false;
        layer.__dirty = false;
        var clearColor = layer.zlevel === firstLayerKey.zl && layer.zlevel2 === firstLayerKey.zl2 ? painter._backgroundColor : null;
        layer.clear(false, clearColor, repaintRects);
      }
      eachCursorInLayer(layer, function(cursor) {
        var cursorFinished = painter._paintPerCursor(layer, cursor, list, repaintRects, contentRetained);
        finished = finished && cursorFinished;
      });
    }, EACH_LAYER_BUILTIN_NOT_HOVER);
    if (env_default.wxa) {
      eachLayer(this._i, function(layer) {
        if (layer && layer.ctx && layer.ctx.draw) {
          layer.ctx.draw();
        }
      });
    }
    return finished;
  };
  CanvasPainter2.prototype._paintPerCursor = function(layer, layerCursor, list, repaintRects, contentRetained) {
    var ctx = layer.ctx;
    if (repaintRects) {
      if (!repaintRects.length) {
        layerCursor.drawIdx = layerCursor.endIdx;
      } else {
        var dpr2 = this.dpr;
        for (var r = 0; r < repaintRects.length; ++r) {
          var rect = repaintRects[r];
          ctx.save();
          ctx.beginPath();
          ctx.rect(rect.x * dpr2, rect.y * dpr2, rect.width * dpr2, rect.height * dpr2);
          ctx.clip();
          this._paintPerCursorInRect(layer, layerCursor, list, rect, contentRetained);
          ctx.restore();
        }
      }
    } else {
      ctx.save();
      this._paintPerCursorInRect(layer, layerCursor, list, null, contentRetained);
      ctx.restore();
    }
    return layerCursor.drawIdx >= layerCursor.endIdx;
  };
  CanvasPainter2.prototype._paintPerCursorInRect = function(layer, layerCursor, list, repaintRect, contentRetained) {
    var scope = {
      inHover: false,
      allClipped: false,
      prevEl: null,
      viewWidth: this._width,
      viewHeight: this._height,
      beforeBrushParam: { contentRetained }
    };
    var ctx = layer.ctx;
    var useTimer = isIncrementalLayer(layer);
    var startTime = useTimer && platformApi.getTime();
    var drawIdxBegin = layerCursor.drawIdx;
    var notClearIdx = layerCursor.notClearIdx;
    var idx = notClearIdx >= 0 ? Math.min(notClearIdx, drawIdxBegin) : drawIdxBegin;
    for (; idx < layerCursor.endIdx; idx++) {
      var el = list[idx];
      if (idx < drawIdxBegin && !el.notClear) {
        continue;
      }
      if (el.__inHover) {
        this._hoverLayerDirty = HOVER_LAYER_DIRTY_REPAINT;
      }
      if (repaintRect != null) {
        var paintRect = el.getPaintRect();
        if (paintRect && paintRect.intersect(repaintRect)) {
          brush(ctx, el, scope);
          el.setPrevPaintRect(paintRect);
        }
      } else {
        brush(ctx, el, scope);
      }
      if (useTimer) {
        var dTime = platformApi.getTime() - startTime;
        if (dTime > 15) {
          idx++;
          break;
        }
      }
    }
    brushLoopFinalize(ctx, scope);
    layerCursor.drawIdx = Math.max(idx, drawIdxBegin);
  };
  CanvasPainter2.prototype.getLayer = function(zlevel, virtual) {
    return this._ensureLayer(zlevel, 0, virtual);
  };
  CanvasPainter2.prototype._ensureLayer = function(zlevel, zlevel2, virtual) {
    zlevel2 = zlevel2 || 0;
    var singleCanvas = this._singleCanvas;
    if (singleCanvas && !this._needsManuallyCompositing) {
      zlevel = CANVAS_ZLEVEL;
      zlevel2 = 0;
    }
    var layer = ensureLayerListInZLevel(this._i, zlevel)[zlevel2];
    if (!layer) {
      layer = createBuiltinLayer("zr_" + zlevel + "." + zlevel2, this, zlevel, zlevel2);
      if (this._layerConfig[zlevel]) {
        merge2(layer, this._layerConfig[zlevel], true);
      }
      if (virtual || singleCanvas && zlevel !== CANVAS_ZLEVEL) {
        layer.virtual = true;
      }
      this._insertLayer(layer, zlevel, zlevel2, false);
      layer.initContext();
    }
    return layer;
  };
  CanvasPainter2.prototype.insertLayer = function(zlevel, layer) {
    this._insertLayer(layer, zlevel, 0, false);
  };
  CanvasPainter2.prototype._insertLayer = function(layer, zlevel, zlevel2, suppressDOMInsert) {
    var internal = this._i;
    var layersMap = internal.layers;
    var layerStack = internal.layerStack;
    var domRoot = this._domRoot;
    var prevLayer = null;
    if (layersMap[zlevel] && layersMap[zlevel][zlevel2]) {
      if (true) {
        logError("ZLevel " + zlevel + "." + zlevel2 + " has been used already");
      }
      return;
    }
    if (!isLayerValid(layer)) {
      if (true) {
        logError("Layer of zlevel " + zlevel + " is not valid");
      }
      return;
    }
    var len2 = layerStack.length;
    var i2 = 0;
    while (i2 < len2 && (layerStack[i2].zl < zlevel || layerStack[i2].zl === zlevel && layerStack[i2].zl2 < zlevel2)) {
      i2++;
    }
    if (i2 > 0) {
      prevLayer = ensureLayerListInZLevel(internal, layerStack[i2 - 1].zl)[layerStack[i2 - 1].zl2];
    }
    layerStack.splice(i2, 0, { zl: zlevel, zl2: zlevel2 });
    ensureLayerListInZLevel(internal, zlevel)[zlevel2] = layer;
    if (!suppressDOMInsert && !layer.virtual) {
      if (prevLayer) {
        var prevDom = prevLayer.dom;
        if (prevDom.nextSibling) {
          domRoot.insertBefore(layer.dom, prevDom.nextSibling);
        } else {
          domRoot.appendChild(layer.dom);
        }
      } else {
        if (domRoot.firstChild) {
          domRoot.insertBefore(layer.dom, domRoot.firstChild);
        } else {
          domRoot.appendChild(layer.dom);
        }
      }
    }
    layer.painter || (layer.painter = this);
  };
  CanvasPainter2.prototype.eachLayer = function(cb, context) {
    return eachLayer(this._i, function(layer, zlevel) {
      cb.call(context, layer, zlevel);
    });
  };
  CanvasPainter2.prototype.eachBuiltinLayer = function(cb, context) {
    return eachLayer(this._i, function(layer, zlevel) {
      cb.call(context, layer, zlevel);
    }, EACH_LAYER_BUILTIN);
  };
  CanvasPainter2.prototype.eachOtherLayer = function(cb, context) {
    return eachLayer(this._i, function(layer, zlevel) {
      cb.call(context, layer, zlevel);
    }, EACH_LAYER_NOT_BUILTIN);
  };
  CanvasPainter2.prototype.getLayers = function() {
    var layers = {};
    eachLayer(this._i, function(layer, zlevel, zlevel2) {
      layers[layer.id] = layer;
    });
    return layers;
  };
  CanvasPainter2.prototype._updateLayerStatus = function(list, paintAll) {
    var painter = this;
    if (painter._singleCanvas) {
      for (var i2 = 1; i2 < list.length; i2++) {
        var el = list[i2];
        if (el.zlevel !== list[i2 - 1].zlevel || el.incremental) {
          painter._needsManuallyCompositing = true;
          break;
        }
      }
    }
    eachLayer(painter._i, function(layer) {
      layer.__dirty = false;
      eachCursorInLayer(layer, function(cursor) {
        cursor.used = false;
        cursor.endIdxNew = 0;
        cursor.notClearIdx = -1;
      });
    }, EACH_LAYER_BUILTIN_NOT_HOVER);
    var prevZLevel;
    var currLayer = null;
    var currCursor = null;
    var aboveIncrementalInCurrZLevel = false;
    for (var idx = 0, len2 = list.length; idx < len2; idx++) {
      var el = list[idx];
      var zlevel = el.zlevel;
      var elIncremental = el.incremental;
      var zlevel2 = void 0;
      if (prevZLevel !== zlevel) {
        prevZLevel = zlevel;
        aboveIncrementalInCurrZLevel = false;
      }
      if (elIncremental) {
        aboveIncrementalInCurrZLevel = true;
        zlevel2 = ZLEVEL2_INCREMENTAL;
      } else {
        zlevel2 = aboveIncrementalInCurrZLevel ? ZLEVEL2_NORMAL_ABOVE : ZLEVEL2_NORMAL_BELOW;
      }
      if (!currLayer || zlevel !== currLayer.zlevel || zlevel2 !== currLayer.zlevel2) {
        currLayer = painter._ensureLayer(zlevel, zlevel2);
        currCursor = null;
        if (!currLayer.__builtin__) {
          logError("ZLevel " + zlevel + " has been used by unknown layer " + currLayer.id);
          continue;
        }
      }
      if (!currCursor || elIncremental !== currCursor.key) {
        currCursor = ensureLayerDrawCursor(currLayer, elIncremental);
        if (!currCursor.used) {
          currCursor.used = true;
          if (!paintAll && currCursor.first === el.id) {
            var idxShift = idx - currCursor.startIdx;
            currCursor.startIdx = idx;
            currCursor.drawIdx += idxShift;
            currCursor.endIdx += idxShift;
          } else {
            currLayer.__dirty = true;
            currCursor.first = el.id;
            currCursor.startIdx = currCursor.drawIdx = idx;
            currCursor.endIdx = idx + 1;
          }
        }
      }
      currCursor.endIdxNew = idx + 1;
      if (el.__dirty & REDRAW_BIT && !el.__inHover) {
        if (!elIncremental || !el.notClear && idx < currCursor.drawIdx) {
          currLayer.__dirty = true;
        }
        if (elIncremental && el.notClear && currCursor.notClearIdx < 0) {
          currCursor.notClearIdx = idx;
        }
      }
    }
    eachLayer(painter._i, function(layer) {
      var cursorStack = layer.__cursorStack;
      var cursors = layer.__cursors;
      for (var i3 = cursorStack.length - 1; i3 >= 0; i3--) {
        var cursor = cursors.get(cursorStack[i3]);
        if (!cursor.used) {
          layer.__dirty = true;
          cursors.removeKey(cursorStack[i3]);
          cursorStack.splice(i3, 1);
        } else {
          var endIdxNew = cursor.endIdxNew;
          if (isIncrementalLayer(layer) ? endIdxNew < cursor.drawIdx : endIdxNew !== cursor.endIdx || !endIdxNew || list[endIdxNew - 1].id !== cursor.last) {
            layer.__dirty = true;
          }
          cursor.endIdx = cursor.endIdxNew;
          cursor.last = endIdxNew ? list[endIdxNew - 1].id : NaN;
        }
      }
      if (layer.__dirty) {
        eachCursorInLayer(layer, function(cursor2) {
          cursor2.drawIdx = cursor2.startIdx;
        });
        if (painter._hoverLayerDirty === HOVER_LAYER_DIRTY_NO) {
          painter._hoverLayerDirty = HOVER_LAYER_DIRTY_REPAINT_IF_EXISTING;
        }
      }
    }, EACH_LAYER_BUILTIN_NOT_HOVER);
  };
  CanvasPainter2.prototype.clear = function() {
    eachLayer(this._i, function(layer) {
      layer.clear();
      resetLayerDrawCursors(layer);
    }, EACH_LAYER_BUILTIN);
    return this;
  };
  CanvasPainter2.prototype.setBackgroundColor = function(backgroundColor2) {
    this._backgroundColor = backgroundColor2;
    eachLayer(this._i, function(layer) {
      layer.setUnpainted();
    });
  };
  CanvasPainter2.prototype.configLayer = function(zlevel, config) {
    if (config) {
      var layerConfig_1 = this._layerConfig;
      if (!layerConfig_1[zlevel]) {
        layerConfig_1[zlevel] = config;
      } else {
        merge2(layerConfig_1[zlevel], config, true);
      }
      eachLayer(this._i, function(layer, zlevel2) {
        merge2(layer, layerConfig_1[zlevel2], true);
      });
    }
  };
  CanvasPainter2.prototype.delLayer = function(zlevel) {
    var layerStack = this._i.layerStack;
    var layersMap = this._i.layers;
    for (var i2 = layerStack.length - 1; i2 >= 0; i2--) {
      var key2 = layerStack[i2];
      if (key2.zl === zlevel) {
        var layer = layersMap[zlevel][key2.zl2];
        if (layer.__builtin__) {
          continue;
        }
        layerStack.splice(i2, 1);
        layersMap[zlevel][key2.zl2] = void 0;
        if (!layer.virtual) {
          var parentNode2 = layer.dom.parentNode;
          parentNode2 && parentNode2.removeChild(layer.dom);
        }
      }
    }
  };
  CanvasPainter2.prototype.resize = function(width, height) {
    if (!this._domRoot.style) {
      if (width == null || height == null) {
        return;
      }
      this._width = width;
      this._height = height;
      this._ensureLayer(CANVAS_ZLEVEL).resize(width, height);
    } else {
      var domRoot = this._domRoot;
      domRoot.style.display = "none";
      var opts = this._opts;
      var root = this.root;
      width != null && (opts.width = width);
      height != null && (opts.height = height);
      width = getSize(root, 0, opts);
      height = getSize(root, 1, opts);
      domRoot.style.display = "";
      if (this._width !== width || height !== this._height) {
        domRoot.style.width = width + "px";
        domRoot.style.height = height + "px";
        eachLayer(this._i, function(layer) {
          layer.resize(width, height);
        });
        this.refresh({ paintAll: true });
      }
      this._width = width;
      this._height = height;
    }
    return this;
  };
  CanvasPainter2.prototype.clearLayer = function(zlevel) {
    each(this._i.layers[zlevel], function(layer) {
      if (layer && !layer.__builtin__) {
        layer.clear();
      }
    });
  };
  CanvasPainter2.prototype.dispose = function() {
    this.root.innerHTML = "";
    this.root = this.storage = this._domRoot = this._i = null;
  };
  CanvasPainter2.prototype.getRenderedCanvas = function(opts) {
    opts = opts || {};
    if (this._singleCanvas && !this._compositeManually) {
      return this._i.layers[CANVAS_ZLEVEL][0].dom;
    }
    var imageLayer = new Layer_default("image", this, opts.pixelRatio || this.dpr);
    imageLayer.initContext();
    imageLayer.clear(false, opts.backgroundColor || this._backgroundColor);
    var ctx = imageLayer.ctx;
    if (opts.pixelRatio <= this.dpr) {
      this.refresh();
      var width_1 = imageLayer.dom.width;
      var height_1 = imageLayer.dom.height;
      eachLayer(this._i, function(layer) {
        if (layer.__builtin__) {
          ctx.drawImage(layer.dom, 0, 0, width_1, height_1);
        } else if (layer.renderToCanvas) {
          ctx.save();
          layer.renderToCanvas(ctx);
          ctx.restore();
        }
      });
    } else {
      var scope = {
        inHover: false,
        viewWidth: this._width,
        viewHeight: this._height,
        beforeBrushParam: {}
      };
      var displayList = this.storage.getDisplayList(true);
      for (var i2 = 0, len2 = displayList.length; i2 < len2; i2++) {
        var el = displayList[i2];
        brush(ctx, el, scope);
      }
      brushLoopFinalize(ctx, scope);
    }
    return imageLayer.dom;
  };
  CanvasPainter2.prototype.getWidth = function() {
    return this._width;
  };
  CanvasPainter2.prototype.getHeight = function() {
    return this._height;
  };
  return CanvasPainter2;
})();
var Painter_default2 = CanvasPainter;

// node_modules/echarts/lib/renderer/installCanvasRenderer.js
function install58(registers) {
  registers.registerPainter("canvas", Painter_default2);
}

// node_modules/@po-ui/ng-components/fesm2022/po-ui-ng-components.mjs
function PoDividerComponent_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 1);
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵtextInterpolate(ctx_r0.label);
  }
}
var _c02 = ["iconElement"];
var _c12 = ["*"];
function PoIconComponent_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "i", 3, 0);
    ɵɵprojection(2);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵclassMap(ctx_r0.class);
  }
}
function PoIconComponent_Conditional_1_ng_template_0_Template(rf, ctx) {
}
function PoIconComponent_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, PoIconComponent_Conditional_1_ng_template_0_Template, 0, 0, "ng-template", 2);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("ngTemplateOutlet", ctx_r0.icon);
  }
}
var _c2 = ["tagContainer"];
var _c3 = ["tagClose"];
var _c4 = ["poTag"];
var _c5 = (a0) => ({
  "color": a0
});
function PoTagComponent_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 5)(1, "span", 13);
    ɵɵtext(2);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance(2);
    ɵɵtextInterpolate(ctx_r0.tagOrientation ? ctx_r0.label + ":" : ctx_r0.label);
  }
}
function PoTagComponent_Conditional_7_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-icon", 9);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("p-icon", !ctx_r0.type || ctx_r0.forceIcon ? ctx_r0.icon : ctx_r0.iconFromType)("ngStyle", !ctx_r0.type && ctx_r0.customTextColor && !ctx_r0.isCaptionTag ? ɵɵpureFunction1(2, _c5, ctx_r0.customTextColor) : "");
  }
}
function PoTagComponent_Conditional_12_Template(rf, ctx) {
  if (rf & 1) {
    const _r2 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-icon", 14, 3);
    ɵɵlistener("click", function PoTagComponent_Conditional_12_Template_po_icon_click_0_listener() {
      ɵɵrestoreView(_r2);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.onClose());
    })("keydown.enter", function PoTagComponent_Conditional_12_Template_po_icon_keydown_enter_0_listener() {
      ɵɵrestoreView(_r2);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.onClose("enter"));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵclassProp("po-clickable", !ctx_r0.disabled);
    ɵɵproperty("p-tooltip", ctx_r0.literals.remove)("p-append-in-body", ctx_r0.appendInBody)("tabindex", !ctx_r0.disabled ? 0 : -1);
    ɵɵattribute("aria-label", ctx_r0.setAriaLabel())("role", !ctx_r0.disabled ? "button" : "");
  }
}
function PoAccordionItemBodyComponent_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 0)(1, "div", 1);
    ɵɵprojection(2);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    ɵɵproperty("@toggleBody", void 0);
  }
}
var _c6 = ["accordionElement"];
var _c7 = ["accordionHeaderElement"];
function PoAccordionItemHeaderComponent_Conditional_7_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-tag", 6);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("p-value", ctx_r0.labelTag)("p-type", ctx_r0.typeTag);
  }
}
function PoAccordionItemComponent_ng_template_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵprojection(0);
  }
}
var _c8 = ["accordionHeaderButtonManagerElement"];
var _c9 = ["accordionHeaderManagerElement"];
function PoAccordionComponent_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 1)(1, "po-accordion-manager", 3);
    ɵɵlistener("p-click", function PoAccordionComponent_Conditional_1_Template_po_accordion_manager_p_click_1_listener() {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.changeVisibleAllItems(ctx_r1.expandedAllItems));
    });
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵproperty("p-expanded-all-items", ctx_r1.expandedAllItems)("p-literals", ctx_r1.literals);
  }
}
function PoAccordionComponent_For_4_ng_container_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoAccordionComponent_For_4_Template(rf, ctx) {
  if (rf & 1) {
    const _r3 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "li", 4)(1, "po-accordion-item-header", 5);
    ɵɵlistener("p-toggle", function PoAccordionComponent_For_4_Template_po_accordion_item_header_p_toggle_1_listener($event) {
      const poAccordionItem_r4 = ɵɵrestoreView(_r3).$implicit;
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.headerToggle($event, poAccordionItem_r4));
    });
    ɵɵelementEnd();
    ɵɵelementStart(2, "po-accordion-item-body", 6);
    ɵɵtemplate(3, PoAccordionComponent_For_4_ng_container_3_Template, 1, 0, "ng-container", 7);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const poAccordionItem_r4 = ctx.$implicit;
    ɵɵclassProp("po-accordion-item-active", poAccordionItem_r4.expanded);
    ɵɵadvance();
    ɵɵproperty("p-disabled", poAccordionItem_r4.disabledItem)("p-expanded", poAccordionItem_r4.expanded)("p-label", poAccordionItem_r4.label)("p-label-tag", poAccordionItem_r4.labelTag)("p-type-tag", poAccordionItem_r4.typeTag);
    ɵɵadvance();
    ɵɵproperty("p-expanded", poAccordionItem_r4.expanded);
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", poAccordionItem_r4.templateRef);
  }
}
function PoAvatarComponent_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "img", 3);
    ɵɵlistener("error", function PoAvatarComponent_Conditional_1_Template_img_error_0_listener() {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onError());
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("src", ctx_r1.src, ɵɵsanitizeUrl);
    ɵɵattribute("loading", ctx_r1.loading);
  }
}
function PoAvatarComponent_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-icon", 2);
  }
}
var _c10 = (a0) => ({
  "background-color": a0
});
var _c11 = () => ({});
function PoBadgeComponent_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-icon", 1);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("p-icon", ctx_r0.badgeIcon);
  }
}
function PoBadgeComponent_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span", 2);
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵtextInterpolate(ctx_r0.badgeValue);
  }
}
function PoLinkComponent_Case_0_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "button", 4);
    ɵɵlistener("click", function PoLinkComponent_Case_0_Template_button_click_0_listener() {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onClick());
    });
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵtextInterpolate(ctx_r1.label);
  }
}
function PoLinkComponent_Case_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "a", 1);
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("href", ctx_r1.url, ɵɵsanitizeUrl)("target", ctx_r1.openNewTab ? "_blank" : "_self");
    ɵɵadvance();
    ɵɵtextInterpolate(ctx_r1.label);
  }
}
function PoLinkComponent_Case_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "a", 2);
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("routerLink", ctx_r1.url);
    ɵɵadvance();
    ɵɵtextInterpolate(ctx_r1.label);
  }
}
function PoLinkComponent_Case_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "a", 3);
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("href", ctx_r1.url, ɵɵsanitizeUrl);
    ɵɵadvance();
    ɵɵtextInterpolate(ctx_r1.label);
  }
}
var _c122 = ["loadingContainer"];
function PoLoadingIconComponent_Conditional_0_ng_template_0_Template(rf, ctx) {
}
function PoLoadingIconComponent_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, PoLoadingIconComponent_Conditional_0_ng_template_0_Template, 0, 0, "ng-template", null, 0, ɵɵtemplateRefExtractor);
  }
}
function PoLoadingIconComponent_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div");
    ɵɵnamespaceSVG();
    ɵɵelementStart(1, "svg", 2)(2, "defs")(3, "linearGradient", 3);
    ɵɵelement(4, "stop", 4)(5, "stop", 5);
    ɵɵelementEnd();
    ɵɵelementStart(6, "linearGradient", 3);
    ɵɵelement(7, "stop", 6)(8, "stop", 5);
    ɵɵelementEnd()();
    ɵɵelementStart(9, "g", 7);
    ɵɵelement(10, "path", 8)(11, "path", 9)(12, "path", 10);
    ɵɵelementEnd()()();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵclassMap(ɵɵinterpolate1("po-loading-icon-container po-loading-svg-", ctx_r0.size));
    ɵɵadvance();
    ɵɵproperty("ngClass", ctx_r0.neutralColor ? "po-loading-icon-neutral" : "po-loading-icon");
    ɵɵadvance(2);
    ɵɵproperty("id", "first" + ctx_r0.id);
    ɵɵadvance(3);
    ɵɵproperty("id", "second" + ctx_r0.id);
    ɵɵadvance(4);
    ɵɵattribute("stroke", "url(#first" + ctx_r0.id + ")");
    ɵɵadvance();
    ɵɵattribute("stroke", "url(#second" + ctx_r0.id + ")");
  }
}
function PoLoadingComponent_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span", 1);
    ɵɵtext(1);
    ɵɵelementStart(2, "div", 2);
    ɵɵtext(3, ".");
    ɵɵelementEnd();
    ɵɵelementStart(4, "div", 2);
    ɵɵtext(5, ".");
    ɵɵelementEnd();
    ɵɵelementStart(6, "div", 2);
    ɵɵtext(7, ".");
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵtextInterpolate1("", ctx_r0.text, " ");
  }
}
var _c13 = ["button"];
function PoButtonComponent_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 2);
    ɵɵelement(1, "po-loading-icon", 6);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵproperty("p-size", ctx_r0.mapSizeToIcon(ctx_r0.size));
  }
}
function PoButtonComponent_Conditional_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-icon", 4);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("p-icon", ctx_r0.icon());
  }
}
function PoButtonComponent_Conditional_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span", 5);
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵtextInterpolate(ctx_r0.label());
  }
}
var _c14 = ["popoverElement"];
function PoPopoverComponent_Conditional_0_ng_template_0_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoPopoverComponent_Conditional_0_ng_template_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, PoPopoverComponent_Conditional_0_ng_template_0_ng_container_0_Template, 1, 0, "ng-container", 3);
  }
  if (rf & 2) {
    ɵɵnextContext(2);
    const sharedPopoverContent_r1 = ɵɵreference(3);
    ɵɵproperty("ngTemplateOutlet", sharedPopoverContent_r1);
  }
}
function PoPopoverComponent_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, PoPopoverComponent_Conditional_0_ng_template_0_Template, 1, 1, "ng-template", 2);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("cdkConnectedOverlayOrigin", ctx_r1.target)("cdkConnectedOverlayOpen", true)("cdkConnectedOverlayDisableClose", true);
  }
}
function PoPopoverComponent_Conditional_1_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoPopoverComponent_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, PoPopoverComponent_Conditional_1_ng_container_0_Template, 1, 0, "ng-container", 3);
  }
  if (rf & 2) {
    ɵɵnextContext();
    const sharedPopoverContent_r1 = ɵɵreference(3);
    ɵɵproperty("ngTemplateOutlet", sharedPopoverContent_r1);
  }
}
function PoPopoverComponent_ng_template_2_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "div");
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵclassMap(ɵɵinterpolate1("po-popover-arrow po-arrow-", ctx_r1.arrowDirection));
  }
}
function PoPopoverComponent_ng_template_2_Conditional_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span", 7);
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵadvance();
    ɵɵtextInterpolate(ctx_r1.title);
  }
}
function PoPopoverComponent_ng_template_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 4, 1);
    ɵɵconditionalCreate(2, PoPopoverComponent_ng_template_2_Conditional_2_Template, 1, 3, "div", 5);
    ɵɵelementStart(3, "div", 6);
    ɵɵconditionalCreate(4, PoPopoverComponent_ng_template_2_Conditional_4_Template, 2, 1, "span", 7);
    ɵɵprojection(5);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵstyleProp("width", ctx_r1.width ?? ctx_r1.widthPopover, "px");
    ɵɵproperty("hidden", ctx_r1.isHidden)("ngClass", ctx_r1.customClasses());
    ɵɵadvance(2);
    ɵɵconditional(!ctx_r1.hideArrow ? 2 : -1);
    ɵɵadvance(2);
    ɵɵconditional(ctx_r1.title ? 4 : -1);
  }
}
var _c15 = ["target"];
var _c16 = ["popover"];
function PoHelperComponent_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 4);
    ɵɵlistener("keydown.enter", function PoHelperComponent_Conditional_0_Template_div_keydown_enter_0_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.executeCustomAction($event));
    })("keydown.space", function PoHelperComponent_Conditional_0_Template_div_keydown_space_0_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.executeCustomAction($event));
    })("click", function PoHelperComponent_Conditional_0_Template_div_click_0_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.executeCustomAction($event));
    })("animationend", function PoHelperComponent_Conditional_0_Template_div_animationend_0_listener() {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onRefreshAnimationEnd());
    });
    ɵɵelement(1, "po-icon", 5);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵclassProp("po-helper-refresh-animating", ctx_r1.refreshAnimating);
    ɵɵattribute("tabindex", 0)("aria-label", ctx_r1.customAction().ariaLabel || "");
    ɵɵadvance();
    ɵɵproperty("p-icon", ctx_r1.customAction().icon);
  }
}
function PoHelperComponent_Conditional_1_For_8_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span");
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const fragment_r4 = ctx.$implicit;
    ɵɵclassProp("po-text-bold", fragment_r4.bold)("po-text-italic", fragment_r4.italic)("po-text-underline", fragment_r4.underline);
    ɵɵadvance();
    ɵɵtextInterpolate(fragment_r4.text);
  }
}
function PoHelperComponent_Conditional_1_Conditional_9_Template(rf, ctx) {
  if (rf & 1) {
    const _r5 = ɵɵgetCurrentView();
    ɵɵelement(0, "po-divider");
    ɵɵelementStart(1, "po-link", 11);
    ɵɵlistener("keydown.enter", function PoHelperComponent_Conditional_1_Conditional_9_Template_po_link_keydown_enter_1_listener($event) {
      return $event.stopPropagation();
    })("keydown.space", function PoHelperComponent_Conditional_1_Conditional_9_Template_po_link_keydown_space_1_listener($event) {
      return $event.stopPropagation();
    })("p-action", function PoHelperComponent_Conditional_1_Conditional_9_Template_po_link_p_action_1_listener() {
      let tmp_5_0;
      ɵɵrestoreView(_r5);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView((tmp_5_0 = ctx_r1.helper()) == null ? null : tmp_5_0.footerAction == null ? null : tmp_5_0.footerAction.action());
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    let tmp_4_0;
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵadvance();
    ɵɵproperty("p-label", (tmp_4_0 = ctx_r1.helper()) == null ? null : tmp_4_0.footerAction == null ? null : tmp_4_0.footerAction.label);
  }
}
function PoHelperComponent_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r3 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 6, 0);
    ɵɵlistener("keydown", function PoHelperComponent_Conditional_1_Template_div_keydown_0_listener($event) {
      ɵɵrestoreView(_r3);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onKeyDown($event));
    })("click", function PoHelperComponent_Conditional_1_Template_div_click_0_listener($event) {
      ɵɵrestoreView(_r3);
      const ctx_r1 = ɵɵnextContext();
      ctx_r1.openHelperPopover();
      return ɵɵresetView(ctx_r1.emitClick($event));
    });
    ɵɵelement(2, "po-icon", 5);
    ɵɵelementStart(3, "po-popover", 7, 1);
    ɵɵlistener("p-close", function PoHelperComponent_Conditional_1_Template_po_popover_p_close_3_listener() {
      ɵɵrestoreView(_r3);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.handleClose());
    })("p-open", function PoHelperComponent_Conditional_1_Template_po_popover_p_open_3_listener() {
      ɵɵrestoreView(_r3);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.handleOpen());
    });
    ɵɵelementStart(5, "div", 8)(6, "span", 9);
    ɵɵrepeaterCreate(7, PoHelperComponent_Conditional_1_For_8_Template, 2, 7, "span", 10, ɵɵrepeaterTrackByIndex);
    ɵɵelementEnd();
    ɵɵconditionalCreate(9, PoHelperComponent_Conditional_1_Conditional_9_Template, 2, 1);
    ɵɵelementEnd()()();
  }
  if (rf & 2) {
    let tmp_10_0;
    let tmp_13_0;
    let tmp_18_0;
    const target_r6 = ɵɵreference(1);
    const popover_r7 = ɵɵreference(4);
    const ctx_r1 = ɵɵnextContext();
    ɵɵclassProp("po-helper-disabled", ctx_r1.disabled());
    ɵɵattribute("tabindex", ctx_r1.disabled() ? null : 0)("aria-label", ctx_r1.ariaLabel())("aria-haspopup", "dialog")("aria-expanded", !popover_r7.isHidden)("aria-controls", "popover-content-" + ctx_r1.id)("aria-describedby", !popover_r7.isHidden ? "popover-content-" + ctx_r1.id : null);
    ɵɵadvance(2);
    ɵɵproperty("p-icon", ((tmp_10_0 = ctx_r1.helper()) == null ? null : tmp_10_0.type) === "info" ? "ICON_INFO" : "ICON_HELP");
    ɵɵadvance();
    ɵɵproperty("p-position", ctx_r1.popoverPosition)("p-target", target_r6)("p-title", (tmp_13_0 = ctx_r1.helper()) == null ? null : tmp_13_0.title)("p-append-in-body", ctx_r1.appendBox())("p-offset", 8);
    ɵɵadvance(2);
    ɵɵproperty("id", "popover-content-" + ctx_r1.id);
    ɵɵadvance(2);
    ɵɵrepeater(ctx_r1.contentFragments());
    ɵɵadvance(2);
    ɵɵconditional(((tmp_18_0 = ctx_r1.helper()) == null ? null : tmp_18_0.type) === "help" && ((tmp_18_0 = ctx_r1.helper()) == null ? null : tmp_18_0.footerAction) ? 9 : -1);
  }
}
function PoLabelComponent_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "label", 2);
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵclassProp("po-label-wrap", ctx_r0.textWrap())("po-label-disabled", ctx_r0.disabled);
    ɵɵattribute("for", ctx_r0.field && ctx_r0.for ? ctx_r0.for : void 0)("p-field", ctx_r0.field);
    ɵɵadvance();
    ɵɵtextInterpolate(ctx_r0.label);
  }
}
function PoLabelComponent_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span", 3);
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵclassProp("po-label-required", ctx_r0.isRequiredLiteral(ctx_r0.requirement));
    ɵɵadvance();
    ɵɵtextInterpolate(ctx_r0.requirement);
  }
}
var _c17 = ["helperEl"];
function PoFieldContainerBottomComponent_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 2);
    ɵɵelement(1, "po-icon", 5);
    ɵɵelementStart(2, "span", 6);
    ɵɵtext(3);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance(2);
    ɵɵclassProp("po-field-error-limit", ctx_r0.errorLimit);
    ɵɵproperty("p-tooltip", ctx_r0.errorLimit ? ctx_r0.errorPattern : null)("p-append-in-body", ctx_r0.appendBox);
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ctx_r0.errorPattern, " ");
  }
}
function PoFieldContainerBottomComponent_Conditional_2_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span");
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ctx_r0.help, " ");
  }
}
function PoFieldContainerBottomComponent_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 3);
    ɵɵconditionalCreate(1, PoFieldContainerBottomComponent_Conditional_2_Conditional_1_Template, 2, 1, "span");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵconditional(ctx_r0.help ? 1 : -1);
  }
}
function PoFieldContainerBottomComponent_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-helper", 4, 0);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("p-size", ctx_r0.size)("p-helper", ctx_r0.poHelperComponent())("p-disabled", ctx_r0.disabled)("p-append-in-body", ctx_r0.appendBox);
  }
}
var _c18 = ["labelEl"];
function PoFieldContainerComponent_Conditional_2_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-helper", 6, 1);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵproperty("p-disabled", ctx_r0.disabled)("p-helper", ctx_r0.poHelperComponent())("p-size", ctx_r0.poHelperComponent().size ? ctx_r0.poHelperComponent().size : null);
  }
}
function PoFieldContainerComponent_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-label", 5, 0);
    ɵɵconditionalCreate(2, PoFieldContainerComponent_Conditional_2_Conditional_2_Template, 2, 3, "po-helper", 6);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("p-disabled", ctx_r0.disabled)("p-for", ctx_r0.id)("p-label", ctx_r0.label)("p-requirement", ctx_r0.requirement)("p-text-wrap", ctx_r0.textWrap())("p-tooltip", ctx_r0.showTip ? ctx_r0.label : null);
    ɵɵadvance(2);
    ɵɵconditional(ctx_r0.poHelperComponent() ? 2 : -1);
  }
}
function PoFieldContainerComponent_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 4);
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵtextInterpolate(ctx_r0.help);
  }
}
var _c19 = ["checkboxLabel"];
function PoCheckboxComponent_Conditional_1_ng_container_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoCheckboxComponent_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "po-field-container", 4)(1, "div", 5);
    ɵɵtemplate(2, PoCheckboxComponent_Conditional_1_ng_container_2_Template, 1, 0, "ng-container", 6);
    ɵɵelementEnd();
    ɵɵelement(3, "po-field-container-bottom", 7);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    const checkboxContent_r2 = ɵɵreference(4);
    ɵɵproperty("p-disabled", ctx_r0.disabled)("p-size", ctx_r0.size)("p-compact-label", ctx_r0.compactLabel());
    ɵɵadvance(2);
    ɵɵproperty("ngTemplateOutlet", checkboxContent_r2);
    ɵɵadvance();
    ɵɵproperty("p-help", ctx_r0.help)("p-size", ctx_r0.size);
  }
}
function PoCheckboxComponent_Conditional_2_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoCheckboxComponent_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, PoCheckboxComponent_Conditional_2_ng_container_0_Template, 1, 0, "ng-container", 6);
  }
  if (rf & 2) {
    ɵɵnextContext();
    const checkboxContent_r2 = ɵɵreference(4);
    ɵɵproperty("ngTemplateOutlet", checkboxContent_r2);
  }
}
function PoCheckboxComponent_ng_template_3_Conditional_4_Template(rf, ctx) {
  if (rf & 1) {
    const _r4 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-label", 13, 2);
    ɵɵlistener("mouseenter", function PoCheckboxComponent_ng_template_3_Conditional_4_Template_po_label_mouseenter_0_listener() {
      ɵɵrestoreView(_r4);
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.handleLabelTooltip());
    })("focus", function PoCheckboxComponent_ng_template_3_Conditional_4_Template_po_label_focus_0_listener() {
      ɵɵrestoreView(_r4);
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.handleLabelTooltip());
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵproperty("p-disabled", ctx_r0.disabled)("p-for", ctx_r0.id)("p-label", ctx_r0.label)("p-text-wrap", ctx_r0.labelTextWrap())("p-tooltip", ctx_r0.showTip ? ctx_r0.label : null);
  }
}
function PoCheckboxComponent_ng_template_3_Conditional_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-helper", 12, 3);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(2);
    const poHelper_r5 = ɵɵreadContextLet(0);
    ɵɵproperty("p-helper", poHelper_r5.helperSettings)("p-disabled", ctx_r0.disabled)("p-append-in-body", ctx_r0.appendBox)("p-size", ctx_r0.size === "large" ? "medium" : ctx_r0.size);
  }
}
function PoCheckboxComponent_ng_template_3_Template(rf, ctx) {
  if (rf & 1) {
    const _r3 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 8);
    ɵɵlistener("click", function PoCheckboxComponent_ng_template_3_Template_div_click_0_listener($event) {
      ɵɵrestoreView(_r3);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.checkOption($event, ctx_r0.checkboxValue));
    })("keydown", function PoCheckboxComponent_ng_template_3_Template_div_keydown_0_listener($event) {
      ɵɵrestoreView(_r3);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.onKeyDown($event, ctx_r0.checkboxValue));
    });
    ɵɵelementStart(1, "div", 9, 1);
    ɵɵlistener("blur", function PoCheckboxComponent_ng_template_3_Template_div_blur_1_listener() {
      ɵɵrestoreView(_r3);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.onBlur());
    });
    ɵɵelement(3, "span", 10);
    ɵɵconditionalCreate(4, PoCheckboxComponent_ng_template_3_Conditional_4_Template, 2, 5, "po-label", 11);
    ɵɵconditionalCreate(5, PoCheckboxComponent_ng_template_3_Conditional_5_Template, 2, 4, "po-helper", 12);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    const poHelper_r5 = ɵɵreadContextLet(0);
    ɵɵattribute("checked", ctx_r0.checkboxValue);
    ɵɵadvance();
    ɵɵproperty("tabindex", ctx_r0.disabled || ctx_r0.disabladTabindex ? -1 : 0);
    ɵɵattribute("aria-checked", ctx_r0.checkboxValue)("data-focused-element", !ctx_r0.disabled)("data-inactive-component", ctx_r0.disabled);
    ɵɵadvance(2);
    ɵɵproperty("id", ctx_r0.id);
    ɵɵattribute("aria-checked", ctx_r0.checkboxValue)("aria-disabled", ctx_r0.disabled)("required", ctx_r0.checkBoxRequired);
    ɵɵadvance();
    ɵɵconditional(ctx_r0.label ? 4 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r0.poHelperComponent() || poHelper_r5.hideAdditionalHelp ? 5 : -1);
  }
}
var _c20 = ["itemList"];
var _c21 = (a0) => ({
  $implicit: a0
});
function PoItemListComponent_Case_1_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-icon", 9);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵclassProp("po-field-icon-aa", ctx_r0.size === "small");
    ɵɵproperty("p-icon", ctx_r0.icon);
  }
}
function PoItemListComponent_Case_1_Conditional_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-icon", 8);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵproperty("p-icon", ctx_r0.iconArrowRight);
  }
}
function PoItemListComponent_Case_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 5, 0);
    ɵɵconditionalCreate(2, PoItemListComponent_Case_1_Conditional_2_Template, 1, 3, "po-icon", 6);
    ɵɵelementStart(3, "span", 7);
    ɵɵtext(4);
    ɵɵelementEnd();
    ɵɵconditionalCreate(5, PoItemListComponent_Case_1_Conditional_5_Template, 1, 1, "po-icon", 8);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵclassProp("po-item-list__separator", ctx_r0.separator)("po-item-list__disabled", ctx_r0.disabled || !ctx_r0.visible)("po-item-list__selected", (ctx_r0.selected || ctx_r0.activeTabs) && !ctx_r0.disabled)("po-item-list__hidden", ctx_r0.tabHide)("po-item-list__danger", ctx_r0.danger);
    ɵɵattribute("aria-disabled", ctx_r0.disabled || !ctx_r0.visible)("aria-label", ctx_r0.label);
    ɵɵadvance(2);
    ɵɵconditional(ctx_r0.icon ? 2 : -1);
    ɵɵadvance(2);
    ɵɵtextInterpolate(ctx_r0.label);
    ɵɵadvance();
    ɵɵconditional(ctx_r0.iconArrowRight ? 5 : -1);
  }
}
function PoItemListComponent_Case_2_Conditional_2_Case_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "span", 12);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(3);
    ɵɵproperty("innerHTML", ctx_r0.getLabelFormatted(ctx_r0.label), ɵɵsanitizeHtml);
  }
}
function PoItemListComponent_Case_2_Conditional_2_Case_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span", 7);
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(3);
    ɵɵadvance();
    ɵɵtextInterpolate(ctx_r0.item[ctx_r0.keysLabel[0]]);
  }
}
function PoItemListComponent_Case_2_Conditional_2_Case_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 13)(1, "span", 7);
    ɵɵtext(2);
    ɵɵelementEnd();
    ɵɵelementStart(3, "span", 14);
    ɵɵtext(4);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(3);
    ɵɵadvance(2);
    ɵɵtextInterpolate(ctx_r0.item[ctx_r0.keysLabel[0]]);
    ɵɵadvance(2);
    ɵɵtextInterpolate(ctx_r0.item[ctx_r0.keysLabel[1]]);
  }
}
function PoItemListComponent_Case_2_Conditional_2_Case_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 13)(1, "span", 7);
    ɵɵtext(2);
    ɵɵelementEnd();
    ɵɵelementStart(3, "span", 14);
    ɵɵtext(4);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(3);
    ɵɵadvance(2);
    ɵɵtextInterpolate(ctx_r0.item[ctx_r0.keysLabel[0]]);
    ɵɵadvance(2);
    ɵɵtextInterpolate2("", ctx_r0.item[ctx_r0.keysLabel[1]], " - ", ctx_r0.item[ctx_r0.keysLabel[2]]);
  }
}
function PoItemListComponent_Case_2_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵconditionalCreate(0, PoItemListComponent_Case_2_Conditional_2_Case_0_Template, 1, 1, "span", 12)(1, PoItemListComponent_Case_2_Conditional_2_Case_1_Template, 2, 1, "span", 7)(2, PoItemListComponent_Case_2_Conditional_2_Case_2_Template, 5, 2, "div", 13)(3, PoItemListComponent_Case_2_Conditional_2_Case_3_Template, 5, 3, "div", 13);
  }
  if (rf & 2) {
    let tmp_3_0;
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵconditional((tmp_3_0 = ctx_r0.keysLabel.length) === 0 ? 0 : tmp_3_0 === 1 ? 1 : tmp_3_0 === 2 ? 2 : 3);
  }
}
function PoItemListComponent_Case_2_ng_template_3_Template(rf, ctx) {
}
function PoItemListComponent_Case_2_Template(rf, ctx) {
  if (rf & 1) {
    const _r2 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 10, 0);
    ɵɵlistener("click", function PoItemListComponent_Case_2_Template_div_click_0_listener($event) {
      ɵɵrestoreView(_r2);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.onComboItem(ctx_r0.item, $event));
    });
    ɵɵconditionalCreate(2, PoItemListComponent_Case_2_Conditional_2_Template, 4, 1);
    ɵɵtemplate(3, PoItemListComponent_Case_2_ng_template_3_Template, 0, 0, "ng-template", 11);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵclassProp("po-item-list__separator", ctx_r0.separator)("po-item-list__selected", ctx_r0.selected && !ctx_r0.disabled);
    ɵɵattribute("aria-disabled", ctx_r0.disabled)("aria-label", ctx_r0.label);
    ɵɵadvance(2);
    ɵɵconditional(!ctx_r0.template ? 2 : -1);
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", ctx_r0.template == null ? null : ctx_r0.template.templateRef)("ngTemplateOutletContext", ɵɵpureFunction1(9, _c21, ctx_r0.templateContext));
  }
}
function PoItemListComponent_Case_3_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span", 17);
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵadvance();
    ɵɵtextInterpolate(ctx_r0.label);
  }
}
function PoItemListComponent_Case_3_ng_template_3_Template(rf, ctx) {
}
function PoItemListComponent_Case_3_Template(rf, ctx) {
  if (rf & 1) {
    const _r3 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 15);
    ɵɵlistener("click", function PoItemListComponent_Case_3_Template_div_click_0_listener() {
      ɵɵrestoreView(_r3);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.onCheckboxItem());
    })("keydown", function PoItemListComponent_Case_3_Template_div_keydown_0_listener($event) {
      ɵɵrestoreView(_r3);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.onCheckboxItemEmit($event));
    })("keyup", function PoItemListComponent_Case_3_Template_div_keyup_0_listener($event) {
      ɵɵrestoreView(_r3);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.onCheckboxItemEmit($event));
    });
    ɵɵelement(1, "po-checkbox", 16);
    ɵɵconditionalCreate(2, PoItemListComponent_Case_3_Conditional_2_Template, 2, 1, "span", 17);
    ɵɵtemplate(3, PoItemListComponent_Case_3_ng_template_3_Template, 0, 0, "ng-template", 11);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵclassProp("po-item-list__separator", ctx_r0.separator)("po-item-list__selected", ctx_r0.selected && !ctx_r0.disabled)("po-item-list__disabled", ctx_r0.disabled);
    ɵɵattribute("aria-disabled", ctx_r0.disabled)("aria-label", ctx_r0.label);
    ɵɵadvance();
    ɵɵproperty("p-disabled-tabindex", true)("p-checkboxValue", ctx_r0.checkboxValue === null ? "mixed" : ctx_r0.checkboxValue)("p-disabled", ctx_r0.disabled)("p-size", ctx_r0.size);
    ɵɵadvance();
    ɵɵconditional(!ctx_r0.template ? 2 : -1);
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", ctx_r0.template == null ? null : ctx_r0.template.templateRef)("ngTemplateOutletContext", ɵɵpureFunction1(15, _c21, ctx_r0.templateContext));
  }
}
var _c22 = ["inputElement"];
var _c23 = ["listbox"];
var _c24 = ["listboxItemList"];
var _c25 = ["listboxGroupHeader"];
var _c26 = ["searchElement"];
var _c27 = ["popupHeaderContainer"];
var _c28 = ["listboxItem"];
var _c29 = [[["", "p-popup-header-template", ""]]];
var _c30 = ["[p-popup-header-template]"];
var _forTrack0 = ($index, $item) => $item.label;
function PoListBoxComponent_Conditional_5_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-search-list", 11, 2);
    ɵɵlistener("p-change", function PoListBoxComponent_Conditional_5_Template_po_search_list_p_change_0_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.callChangeSearch($event));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("p-literals", ctx_r1.literalSearch)("p-field-value", ctx_r1.fieldValueSearch)("p-placeholder", ctx_r1.placeholderSearch)("p-size", ctx_r1.size);
  }
}
function PoListBoxComponent_Conditional_6_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    const _r4 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "li", 15);
    ɵɵlistener("click", function PoListBoxComponent_Conditional_6_Conditional_2_Template_li_click_0_listener() {
      ɵɵrestoreView(_r4);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.changeAll.emit());
    })("keydown", function PoListBoxComponent_Conditional_6_Conditional_2_Template_li_keydown_0_listener($event) {
      ɵɵrestoreView(_r4);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.changeAllEmit($event));
    })("keydown", function PoListBoxComponent_Conditional_6_Conditional_2_Template_li_keydown_0_listener($event) {
      ɵɵrestoreView(_r4);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.onSelectAllCheckboxKeyDown($event));
    });
    ɵɵelement(1, "po-item-list", 16);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵattribute("aria-checked", ctx_r1.checkboxAllValue === null ? "mixed" : ctx_r1.checkboxAllValue);
    ɵɵadvance();
    ɵɵproperty("p-selected", ctx_r1.checkboxAllValue)("p-label", ctx_r1.literals.selectAll)("p-checkbox-value", ctx_r1.checkboxAllValue)("p-size", ctx_r1.size);
  }
}
function PoListBoxComponent_Conditional_6_For_4_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "label", 18);
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const item_r6 = ɵɵnextContext().$implicit;
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", item_r6.label, " ");
  }
}
function PoListBoxComponent_Conditional_6_For_4_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    const _r7 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-item-list", 20);
    ɵɵlistener("p-selectcheckbox-item", function PoListBoxComponent_Conditional_6_For_4_Conditional_3_Template_po_item_list_p_selectcheckbox_item_0_listener($event) {
      ɵɵrestoreView(_r7);
      const ctx_r1 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r1.checkboxClicked($event));
    })("p-selectcombo-item", function PoListBoxComponent_Conditional_6_For_4_Conditional_3_Template_po_item_list_p_selectcombo_item_0_listener() {
      ɵɵrestoreView(_r7);
      const item_r6 = ɵɵnextContext().$implicit;
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.optionClicked(item_r6));
    })("p-emit-item-tabs", function PoListBoxComponent_Conditional_6_For_4_Conditional_3_Template_po_item_list_p_emit_item_tabs_0_listener($event) {
      ɵɵrestoreView(_r7);
      const ctx_r1 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r1.onSelectTabs($event));
    })("p-activated-tabs", function PoListBoxComponent_Conditional_6_For_4_Conditional_3_Template_po_item_list_p_activated_tabs_0_listener($event) {
      ɵɵrestoreView(_r7);
      const ctx_r1 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r1.onActivatedTabs($event));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const item_r6 = ɵɵnextContext().$implicit;
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵproperty("p-disabled", ctx_r1.returnBooleanValue(item_r6, "disabled"))("p-visible", ctx_r1.returnBooleanValue(item_r6, "visible"))("p-checkbox-value", ctx_r1.isSelectedItem(item_r6))("p-label", item_r6[ctx_r1.fieldLabel])("p-value", item_r6[ctx_r1.fieldValue])("p-selected", ctx_r1.isSelectedItem(item_r6) || item_r6.selected)("p-active-tabs", item_r6.active)("p-separator", item_r6.separator || ctx_r1.separator)("p-danger", item_r6.danger || item_r6.type === "danger")("p-icon", item_r6.icon)("p-type", ctx_r1.type)("p-item", item_r6)("p-tab-hide", item_r6.hide)("p-is-tabs", ctx_r1.isTabs)("p-field-value", ctx_r1.fieldValue)("p-field-label", ctx_r1.fieldLabel)("p-template", ctx_r1.template)("p-template-context", item_r6)("p-search-value", ctx_r1.searchValue)("p-filter-mode", ctx_r1.filterMode)("p-filtering", ctx_r1.isFiltering)("p-size", ctx_r1.size)("p-should-mark-letter", ctx_r1.shouldMarkLetters)("p-compare-cache", ctx_r1.compareCache)("p-combo-service", ctx_r1.comboService)("p-keys-label", ctx_r1.keysLabel);
    ɵɵattribute("data-item-list", ctx_r1.formatItemList(item_r6));
  }
}
function PoListBoxComponent_Conditional_6_For_4_Template(rf, ctx) {
  if (rf & 1) {
    const _r5 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "li", 17, 4);
    ɵɵlistener("click", function PoListBoxComponent_Conditional_6_For_4_Template_li_click_0_listener() {
      const item_r6 = ɵɵrestoreView(_r5).$implicit;
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.onSelectItem(item_r6));
    })("keydown", function PoListBoxComponent_Conditional_6_For_4_Template_li_keydown_0_listener($event) {
      const item_r6 = ɵɵrestoreView(_r5).$implicit;
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.onKeyDown(item_r6, $event));
    });
    ɵɵconditionalCreate(2, PoListBoxComponent_Conditional_6_For_4_Conditional_2_Template, 2, 1, "label", 18);
    ɵɵconditionalCreate(3, PoListBoxComponent_Conditional_6_For_4_Conditional_3_Template, 1, 27, "po-item-list", 19);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const item_r6 = ctx.$implicit;
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵclassProp("po-listbox-item", ctx_r1.visible())("po-listbox-item-type-check", ctx_r1.type === "check");
    ɵɵproperty("cdkOption", item_r6[ctx_r1.fieldLabel])("cdkOptionDisabled", ctx_r1.returnBooleanValue(item_r6, "disabled") || ctx_r1.returnBooleanValue(item_r6, "visible") === false || item_r6.options);
    ɵɵattribute("aria-selected", ctx_r1.isSelectedItem(item_r6) || item_r6.selected);
    ɵɵadvance(2);
    ɵɵconditional(item_r6.options ? 2 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r1.returnBooleanValue(item_r6, "visible") !== false ? 3 : -1);
  }
}
function PoListBoxComponent_Conditional_6_Conditional_5_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 21)(1, "span");
    ɵɵtext(2);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵadvance(2);
    ɵɵtextInterpolate(ctx_r1.placeholderListbox);
  }
}
function PoListBoxComponent_Conditional_6_Conditional_5_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 21)(1, "span");
    ɵɵtext(2);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵadvance(2);
    ɵɵtextInterpolate(ctx_r1.literals.noItems);
  }
}
function PoListBoxComponent_Conditional_6_Conditional_5_Template(rf, ctx) {
  if (rf & 1) {
    const _r8 = ɵɵgetCurrentView();
    ɵɵconditionalCreate(0, PoListBoxComponent_Conditional_6_Conditional_5_Conditional_0_Template, 3, 1, "div", 21);
    ɵɵconditionalCreate(1, PoListBoxComponent_Conditional_6_Conditional_5_Conditional_1_Template, 3, 1, "div", 21);
    ɵɵelementStart(2, "li", 22);
    ɵɵlistener("click", function PoListBoxComponent_Conditional_6_Conditional_5_Template_li_click_2_listener() {
      ɵɵrestoreView(_r8);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.handleFooterActionListbox());
    })("keydown", function PoListBoxComponent_Conditional_6_Conditional_5_Template_li_keydown_2_listener($event) {
      ɵɵrestoreView(_r8);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.onKeyDown({
        type: "footerAction"
      }, $event));
    });
    ɵɵelement(3, "po-item-list", 23);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵconditional(ctx_r1.placeholderListbox ? 0 : -1);
    ɵɵadvance();
    ɵɵconditional(!ctx_r1.placeholderListbox && !ctx_r1.items.length ? 1 : -1);
    ɵɵadvance(2);
    ɵɵproperty("p-label", ctx_r1.literals.footerActionListbox)("p-size", ctx_r1.size);
  }
}
function PoListBoxComponent_Conditional_6_Template(rf, ctx) {
  if (rf & 1) {
    const _r3 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "ul", 12, 3);
    ɵɵlistener("focusin", function PoListBoxComponent_Conditional_6_Template_ul_focusin_0_listener($event) {
      ɵɵrestoreView(_r3);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onListboxFocusIn($event));
    });
    ɵɵconditionalCreate(2, PoListBoxComponent_Conditional_6_Conditional_2_Template, 2, 5, "li", 13);
    ɵɵrepeaterCreate(3, PoListBoxComponent_Conditional_6_For_4_Template, 4, 9, "li", 14, ɵɵrepeaterTrackByIdentity);
    ɵɵconditionalCreate(5, PoListBoxComponent_Conditional_6_Conditional_5_Template, 4, 4);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("cdkListboxMultiple", ctx_r1.type === "check");
    ɵɵadvance(2);
    ɵɵconditional(ctx_r1.type === "check" && ctx_r1.items.length && !(ctx_r1.searchElement == null ? null : ctx_r1.searchElement.inputValue) && !ctx_r1.hideSelectAll ? 2 : -1);
    ɵɵadvance();
    ɵɵrepeater(ctx_r1.items);
    ɵɵadvance(2);
    ɵɵconditional(ctx_r1.footerActionListbox ? 5 : -1);
  }
}
function PoListBoxComponent_Conditional_7_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 24)(1, "span");
    ɵɵtext(2);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵadvance(2);
    ɵɵtextInterpolate1(" ", ctx_r1.literals.noItems);
  }
}
function PoListBoxComponent_Conditional_7_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵconditionalCreate(0, PoListBoxComponent_Conditional_7_Conditional_0_Template, 3, 1, "div", 24);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵconditional(!ctx_r1.items.length && !ctx_r1.isServerSearching && ctx_r1.type !== "action" && !ctx_r1.footerActionListbox ? 0 : -1);
  }
}
function PoListBoxComponent_Conditional_8_Conditional_1_For_1_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-item-list", 26);
  }
  if (rf & 2) {
    const item_r10 = ɵɵnextContext().$implicit;
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵproperty("p-label", item_r10.label)("p-item", item_r10)("p-separator", item_r10.separator || ctx_r1.separator)("p-icon", item_r10.icon)("p-icon-arrow-right", "ICON_ARROW_RIGHT")("p-selected", ctx_r1.isSelectedItem(item_r10) || item_r10.selected);
  }
}
function PoListBoxComponent_Conditional_8_Conditional_1_For_1_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-item-list", 27);
  }
  if (rf & 2) {
    const item_r10 = ɵɵnextContext().$implicit;
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵproperty("p-disabled", ctx_r1.returnBooleanValue(item_r10, "disabled"))("p-visible", ctx_r1.returnBooleanValue(item_r10, "visible"))("p-label", item_r10[ctx_r1.fieldLabel])("p-item", item_r10)("p-icon", item_r10.icon)("p-separator", item_r10.separator || ctx_r1.separator)("p-danger", item_r10.type === "danger")("p-selected", ctx_r1.isSelectedItem(item_r10) || item_r10.selected);
  }
}
function PoListBoxComponent_Conditional_8_Conditional_1_For_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r9 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "li", 17, 4);
    ɵɵlistener("click", function PoListBoxComponent_Conditional_8_Conditional_1_For_1_Template_li_click_0_listener($event) {
      const item_r10 = ɵɵrestoreView(_r9).$implicit;
      const ctx_r1 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r1.onSelectItem(item_r10, $event));
    })("keydown", function PoListBoxComponent_Conditional_8_Conditional_1_For_1_Template_li_keydown_0_listener($event) {
      const item_r10 = ɵɵrestoreView(_r9).$implicit;
      const ctx_r1 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r1.onKeyDown(item_r10, $event));
    });
    ɵɵconditionalCreate(2, PoListBoxComponent_Conditional_8_Conditional_1_For_1_Conditional_2_Template, 1, 6, "po-item-list", 26)(3, PoListBoxComponent_Conditional_8_Conditional_1_For_1_Conditional_3_Template, 1, 8, "po-item-list", 27);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const item_r10 = ctx.$implicit;
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵclassProp("po-listbox-item", ctx_r1.visible());
    ɵɵproperty("cdkOption", item_r10[ctx_r1.fieldLabel])("cdkOptionDisabled", ctx_r1.returnBooleanValue(item_r10, "disabled") || ctx_r1.returnBooleanValue(item_r10, "visible") === false);
    ɵɵattribute("aria-selected", ctx_r1.isSelectedItem(item_r10) || item_r10.selected);
    ɵɵadvance(2);
    ɵɵconditional((item_r10.subItems == null ? null : item_r10.subItems.length) || item_r10.$subItemTemplate ? 2 : !(item_r10.subItems == null ? null : item_r10.subItems.length) && ctx_r1.returnBooleanValue(item_r10, "visible") !== false ? 3 : -1);
  }
}
function PoListBoxComponent_Conditional_8_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵrepeaterCreate(0, PoListBoxComponent_Conditional_8_Conditional_1_For_1_Template, 4, 6, "li", 25, ɵɵrepeaterTrackByIdentity);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵrepeater(ctx_r1.currentItems);
  }
}
function PoListBoxComponent_Conditional_8_Conditional_2_Conditional_4_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoListBoxComponent_Conditional_8_Conditional_2_Conditional_4_Template(rf, ctx) {
  if (rf & 1) {
    const _r12 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 31);
    ɵɵlistener("click", function PoListBoxComponent_Conditional_8_Conditional_2_Conditional_4_Template_div_click_0_listener($event) {
      return $event.stopPropagation();
    })("keydown", function PoListBoxComponent_Conditional_8_Conditional_2_Conditional_4_Template_div_keydown_0_listener($event) {
      ɵɵrestoreView(_r12);
      const ctx_r1 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r1.onKeydownTemplate($event));
    })("keydown.space", function PoListBoxComponent_Conditional_8_Conditional_2_Conditional_4_Template_div_keydown_space_0_listener($event) {
      return $event.stopPropagation();
    });
    ɵɵtemplate(1, PoListBoxComponent_Conditional_8_Conditional_2_Conditional_4_ng_container_1_Template, 1, 0, "ng-container", 32);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", ctx_r1.currentGroup.$subItemTemplate)("ngTemplateOutletContext", ɵɵpureFunction1(2, _c21, ctx_r1.currentGroup));
  }
}
function PoListBoxComponent_Conditional_8_Conditional_2_Conditional_5_For_1_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    const _r13 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "li", 34);
    ɵɵlistener("click", function PoListBoxComponent_Conditional_8_Conditional_2_Conditional_5_For_1_Conditional_0_Template_li_click_0_listener($event) {
      ɵɵrestoreView(_r13);
      const subItem_r14 = ɵɵnextContext().$implicit;
      const ctx_r1 = ɵɵnextContext(4);
      return ɵɵresetView(ctx_r1.onSelectItem(subItem_r14, $event));
    })("keydown", function PoListBoxComponent_Conditional_8_Conditional_2_Conditional_5_For_1_Conditional_0_Template_li_keydown_0_listener($event) {
      ɵɵrestoreView(_r13);
      const subItem_r14 = ɵɵnextContext().$implicit;
      const ctx_r1 = ɵɵnextContext(4);
      return ɵɵresetView(ctx_r1.onKeyDown(subItem_r14, $event));
    });
    ɵɵelement(1, "po-item-list", 35);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const subItem_r14 = ɵɵnextContext().$implicit;
    const ctx_r1 = ɵɵnextContext(4);
    ɵɵproperty("cdkOption", subItem_r14[ctx_r1.fieldLabel]);
    ɵɵadvance();
    ɵɵproperty("p-label", subItem_r14.label)("p-item", subItem_r14)("p-separator", subItem_r14.separator || ctx_r1.separator)("p-icon", "ICON_ARROW_RIGHT")("p-icon-position", "right");
  }
}
function PoListBoxComponent_Conditional_8_Conditional_2_Conditional_5_For_1_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r15 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "li", 34);
    ɵɵlistener("click", function PoListBoxComponent_Conditional_8_Conditional_2_Conditional_5_For_1_Conditional_1_Template_li_click_0_listener($event) {
      ɵɵrestoreView(_r15);
      const subItem_r14 = ɵɵnextContext().$implicit;
      const ctx_r1 = ɵɵnextContext(4);
      return ɵɵresetView(ctx_r1.onSelectItem(subItem_r14, $event));
    })("keydown", function PoListBoxComponent_Conditional_8_Conditional_2_Conditional_5_For_1_Conditional_1_Template_li_keydown_0_listener($event) {
      ɵɵrestoreView(_r15);
      const subItem_r14 = ɵɵnextContext().$implicit;
      const ctx_r1 = ɵɵnextContext(4);
      return ɵɵresetView(ctx_r1.onKeyDown(subItem_r14, $event));
    });
    ɵɵelement(1, "po-item-list", 27);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const subItem_r14 = ɵɵnextContext().$implicit;
    const ctx_r1 = ɵɵnextContext(4);
    ɵɵproperty("cdkOption", subItem_r14[ctx_r1.fieldLabel]);
    ɵɵadvance();
    ɵɵproperty("p-disabled", ctx_r1.returnBooleanValue(subItem_r14, "disabled"))("p-visible", ctx_r1.returnBooleanValue(subItem_r14, "visible"))("p-label", subItem_r14[ctx_r1.fieldLabel])("p-item", subItem_r14)("p-icon", subItem_r14.icon)("p-separator", subItem_r14.separator || ctx_r1.separator)("p-danger", subItem_r14.type === "danger")("p-selected", ctx_r1.isSelectedItem(subItem_r14) || subItem_r14.selected);
  }
}
function PoListBoxComponent_Conditional_8_Conditional_2_Conditional_5_For_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵconditionalCreate(0, PoListBoxComponent_Conditional_8_Conditional_2_Conditional_5_For_1_Conditional_0_Template, 2, 6, "li", 33)(1, PoListBoxComponent_Conditional_8_Conditional_2_Conditional_5_For_1_Conditional_1_Template, 2, 9, "li", 33);
  }
  if (rf & 2) {
    const subItem_r14 = ctx.$implicit;
    const ctx_r1 = ɵɵnextContext(4);
    ɵɵconditional((subItem_r14.subItems == null ? null : subItem_r14.subItems.length) ? 0 : !(subItem_r14.subItems == null ? null : subItem_r14.subItems.length) && ctx_r1.returnBooleanValue(subItem_r14, "visible") !== false ? 1 : -1);
  }
}
function PoListBoxComponent_Conditional_8_Conditional_2_Conditional_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵrepeaterCreate(0, PoListBoxComponent_Conditional_8_Conditional_2_Conditional_5_For_1_Template, 2, 1, null, null, _forTrack0);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵrepeater(ctx_r1.currentItems);
  }
}
function PoListBoxComponent_Conditional_8_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    const _r11 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "li", 28, 5);
    ɵɵlistener("click", function PoListBoxComponent_Conditional_8_Conditional_2_Template_li_click_0_listener($event) {
      ɵɵrestoreView(_r11);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.goBack($event));
    })("keydown", function PoListBoxComponent_Conditional_8_Conditional_2_Template_li_keydown_0_listener($event) {
      ɵɵrestoreView(_r11);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.onKeydownGoBack($event, ctx_r1.currentGroup));
    });
    ɵɵelement(2, "po-icon", 29)(3, "po-tag", 30);
    ɵɵelementEnd();
    ɵɵconditionalCreate(4, PoListBoxComponent_Conditional_8_Conditional_2_Conditional_4_Template, 2, 4, "div")(5, PoListBoxComponent_Conditional_8_Conditional_2_Conditional_5_Template, 2, 0);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵproperty("cdkOption", "back-option");
    ɵɵattribute("aria-label", ctx_r1.literals == null ? null : ctx_r1.literals.backToPreviousGroup);
    ɵɵadvance(2);
    ɵɵproperty("p-icon", "ICON_ARROW_LEFT");
    ɵɵadvance();
    ɵɵproperty("p-value", ctx_r1.currentGroup.label);
    ɵɵadvance();
    ɵɵconditional(ctx_r1.currentGroup.$subItemTemplate ? 4 : 5);
  }
}
function PoListBoxComponent_Conditional_8_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "ul", 9);
    ɵɵconditionalCreate(1, PoListBoxComponent_Conditional_8_Conditional_1_Template, 2, 0);
    ɵɵconditionalCreate(2, PoListBoxComponent_Conditional_8_Conditional_2_Template, 6, 5);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵconditional(!ctx_r1.currentGroup ? 1 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r1.currentGroup ? 2 : -1);
  }
}
function PoListBoxComponent_Conditional_9_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div");
    ɵɵelement(1, "po-loading-overlay", 36);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵclassProp("po-listbox-container-loading-default", !ctx_r1.infiniteLoading);
    ɵɵadvance();
    ɵɵproperty("p-size", ctx_r1.getSizeLoading())("p-text", ctx_r1.getTextLoading());
  }
}
var _c31 = ["popupRef"];
var _c32 = ["poListBoxRef"];
var _c33 = ["p-popup-header-template", ""];
function PoPopupComponent_Conditional_0_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "div");
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵclassMap(ɵɵinterpolate1("po-popup-arrow po-arrow-", ctx_r1.arrowDirection));
  }
}
function PoPopupComponent_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 2, 0);
    ɵɵconditionalCreate(2, PoPopupComponent_Conditional_0_Conditional_2_Template, 1, 3, "div", 3);
    ɵɵelementStart(3, "div", 4)(4, "po-listbox", 5, 1);
    ɵɵlistener("p-close", function PoPopupComponent_Conditional_0_Template_po_listbox_p_close_4_listener() {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.close());
    })("p-click-item", function PoPopupComponent_Conditional_0_Template_po_listbox_p_click_item_4_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onClickItem($event));
    });
    ɵɵprojection(7, 0, _c33);
    ɵɵelementEnd()()();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance(2);
    ɵɵconditional(!ctx_r1.hideArrow ? 2 : -1);
    ɵɵadvance(2);
    ɵɵproperty("p-items", ctx_r1.actions)("p-param", ctx_r1.param)("p-size", ctx_r1.size)("p-listbox-subitems", ctx_r1.computedListboxSubitems);
  }
}
function PoBreadcrumbFavoriteComponent_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span", 2);
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵtextInterpolate(ctx_r0.literals == null ? null : ctx_r0.literals.unfavorite);
  }
}
function PoBreadcrumbFavoriteComponent_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span", 2);
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵtextInterpolate(ctx_r0.literals == null ? null : ctx_r0.literals.favorite);
  }
}
var _c34 = ["breadcrumb"];
var _c35 = ["dropdownIcon"];
var _c36 = ["popup"];
function PoBreadcrumbComponent_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "ul", 6, 1)(2, "li")(3, "po-link", 9);
    ɵɵlistener("p-action", function PoBreadcrumbComponent_Conditional_3_Template_po_link_p_action_3_listener() {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.emitAction(ctx_r1.itemsView[0]));
    });
    ɵɵelementEnd()();
    ɵɵelement(4, "po-icon", 10);
    ɵɵelementStart(5, "li", 11, 2);
    ɵɵlistener("click", function PoBreadcrumbComponent_Conditional_3_Template_li_click_5_listener() {
      ɵɵrestoreView(_r1);
      const popup_r3 = ɵɵreference(16);
      return ɵɵresetView(popup_r3.toggle());
    })("keyup", function PoBreadcrumbComponent_Conditional_3_Template_li_keyup_5_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.openPopup($event));
    });
    ɵɵelement(7, "po-icon", 12);
    ɵɵelementEnd();
    ɵɵelement(8, "po-icon", 10);
    ɵɵelementStart(9, "li")(10, "po-link", 9);
    ɵɵlistener("p-action", function PoBreadcrumbComponent_Conditional_3_Template_po_link_p_action_10_listener() {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.emitAction(ctx_r1.itemsView[ctx_r1.itemsView.length - 2]));
    });
    ɵɵelementEnd()();
    ɵɵelement(11, "po-icon", 10);
    ɵɵelementStart(12, "li")(13, "span", 13);
    ɵɵtext(14);
    ɵɵelementEnd()();
    ɵɵelementStart(15, "po-popup", 14, 3);
    ɵɵlistener("p-close", function PoBreadcrumbComponent_Conditional_3_Template_po_popup_p_close_15_listener() {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.closePopUp());
    });
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const target_r4 = ɵɵreference(6);
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance(3);
    ɵɵproperty("p-label", ctx_r1.itemsView[0].label)("p-url", ctx_r1.itemsView[0].link);
    ɵɵadvance(2);
    ɵɵattribute("aria-label", ctx_r1.literals.literalButtonPopup);
    ɵɵadvance(5);
    ɵɵproperty("p-label", ctx_r1.itemsView[ctx_r1.itemsView.length - 2].label)("p-url", ctx_r1.itemsView[ctx_r1.itemsView.length - 2].link);
    ɵɵadvance(3);
    ɵɵattribute("aria-current", ctx_r1.itemsView[ctx_r1.itemsView.length - 1].label);
    ɵɵadvance();
    ɵɵtextInterpolate(ctx_r1.itemsView[ctx_r1.itemsView.length - 1].label);
    ɵɵadvance();
    ɵɵproperty("p-hide-arrow", true)("p-actions", ctx_r1.itemsViewPopup)("p-size", ctx_r1.size)("p-target", target_r4);
  }
}
function PoBreadcrumbComponent_Conditional_4_For_2_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r5 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-link", 9);
    ɵɵlistener("p-action", function PoBreadcrumbComponent_Conditional_4_For_2_Conditional_1_Template_po_link_p_action_0_listener() {
      ɵɵrestoreView(_r5);
      const item_r6 = ɵɵnextContext().$implicit;
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.emitAction(item_r6));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const item_r6 = ɵɵnextContext().$implicit;
    ɵɵproperty("p-label", item_r6.label)("p-url", item_r6.link);
  }
}
function PoBreadcrumbComponent_Conditional_4_For_2_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span", 13);
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const item_r6 = ɵɵnextContext().$implicit;
    ɵɵattribute("aria-current", item_r6.label);
    ɵɵadvance();
    ɵɵtextInterpolate(item_r6.label);
  }
}
function PoBreadcrumbComponent_Conditional_4_For_2_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-icon", 10);
  }
}
function PoBreadcrumbComponent_Conditional_4_For_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "li", 15);
    ɵɵconditionalCreate(1, PoBreadcrumbComponent_Conditional_4_For_2_Conditional_1_Template, 1, 2, "po-link", 16);
    ɵɵconditionalCreate(2, PoBreadcrumbComponent_Conditional_4_For_2_Conditional_2_Template, 2, 2, "span", 13);
    ɵɵconditionalCreate(3, PoBreadcrumbComponent_Conditional_4_For_2_Conditional_3_Template, 1, 0, "po-icon", 10);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ɵ$index_36_r7 = ctx.$index;
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵadvance();
    ɵɵconditional(!(ɵ$index_36_r7 === ctx_r1.itemsView.length - 1) ? 1 : -1);
    ɵɵadvance();
    ɵɵconditional(ɵ$index_36_r7 === ctx_r1.itemsView.length - 1 ? 2 : -1);
    ɵɵadvance();
    ɵɵconditional(!(ɵ$index_36_r7 === ctx_r1.itemsView.length - 1) ? 3 : -1);
  }
}
function PoBreadcrumbComponent_Conditional_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "ul", 7);
    ɵɵrepeaterCreate(1, PoBreadcrumbComponent_Conditional_4_For_2_Template, 4, 3, "li", 15, ɵɵrepeaterTrackByIdentity);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵrepeater(ctx_r1.itemsView);
  }
}
function PoBreadcrumbComponent_Conditional_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-breadcrumb-favorite", 8);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("p-favorite-service", ctx_r1.favoriteService)("p-item-active", ctx_r1.items[ctx_r1.items.length - 1])("p-params-service", ctx_r1.paramsService)("p-hidden-literal", ctx_r1.hiddenLiteralFavorite);
  }
}
function PoButtonGroupComponent_For_2_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-button", 2);
    ɵɵlistener("p-click", function PoButtonGroupComponent_For_2_Template_po_button_p_click_0_listener() {
      const ctx_r1 = ɵɵrestoreView(_r1);
      const button_r3 = ctx_r1.$implicit;
      const ɵ$index_3_r4 = ctx_r1.$index;
      const ctx_r4 = ɵɵnextContext();
      ctx_r4.onButtonClick(button_r3, ɵ$index_3_r4);
      return ɵɵresetView(button_r3.action(button_r3));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const button_r3 = ctx.$implicit;
    const ctx_r4 = ɵɵnextContext();
    ɵɵclassProp("po-button-group-button-selected", button_r3.selected)("po-button-group-disabled", button_r3.disabled);
    ɵɵproperty("p-disabled", button_r3.disabled)("p-icon", button_r3.icon)("p-label", button_r3.label)("p-size", ctx_r4.size)("p-tooltip", !button_r3.disabled ? button_r3.tooltip : void 0)("p-append-in-body", true);
  }
}
var _c37 = ["hourCell"];
var _c38 = ["minuteCell"];
var _c39 = ["secondCell"];
var _c40 = ["periodCell"];
var _c41 = ["hourItems"];
var _c42 = ["minuteItems"];
var _c43 = ["secondItems"];
function PoTimerComponent_For_9_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-button", 22, 6);
    ɵɵlistener("focus", function PoTimerComponent_For_9_Template_po_button_focus_0_listener() {
      const ɵ$index_11_r2 = ɵɵrestoreView(_r1).$index;
      const ctx_r2 = ɵɵnextContext();
      return ɵɵresetView(ctx_r2.onCellFocus("hour", ɵ$index_11_r2));
    })("keydown", function PoTimerComponent_For_9_Template_po_button_keydown_0_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r2 = ɵɵnextContext();
      return ɵɵresetView(ctx_r2.onCellKeydown($event, "hour"));
    })("p-click", function PoTimerComponent_For_9_Template_po_button_p_click_0_listener() {
      const ctx_r3 = ɵɵrestoreView(_r1);
      const hour_r5 = ctx_r3.$implicit;
      const ɵ$index_11_r2 = ctx_r3.$index;
      const ctx_r2 = ɵɵnextContext();
      ctx_r2.onSelectHour(hour_r5);
      return ɵɵresetView(ctx_r2.onCellFocus("hour", ɵ$index_11_r2));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const hour_r5 = ctx.$implicit;
    const ɵ$index_11_r2 = ctx.$index;
    const ctx_r2 = ɵɵnextContext();
    ɵɵclassProp("po-timer-display-selected", ctx_r2.selectedHour === hour_r5);
    ɵɵproperty("p-tabindex", ctx_r2.getCellTabIndex("hour", ɵ$index_11_r2))("p-label", ctx_r2.formatValue(hour_r5))("p-aria-label", ctx_r2.formatValue(hour_r5) + " " + ctx_r2.literals.hours)("p-disabled", ctx_r2.isHourDisabled(hour_r5))("p-size", ctx_r2.size);
    ɵɵattribute("id", "po-timer-hour-" + ɵ$index_11_r2)("data-aria-role", "option")("data-aria-selected", ctx_r2.selectedHour === hour_r5 ? "true" : null)("data-aria-setsize", ctx_r2.hours.length)("data-aria-posinset", ɵ$index_11_r2 % ctx_r2.hours.length + 1);
  }
}
function PoTimerComponent_For_17_Template(rf, ctx) {
  if (rf & 1) {
    const _r6 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-button", 22, 7);
    ɵɵlistener("focus", function PoTimerComponent_For_17_Template_po_button_focus_0_listener() {
      const ɵ$index_20_r7 = ɵɵrestoreView(_r6).$index;
      const ctx_r2 = ɵɵnextContext();
      return ɵɵresetView(ctx_r2.onCellFocus("minute", ɵ$index_20_r7));
    })("keydown", function PoTimerComponent_For_17_Template_po_button_keydown_0_listener($event) {
      ɵɵrestoreView(_r6);
      const ctx_r2 = ɵɵnextContext();
      return ɵɵresetView(ctx_r2.onCellKeydown($event, "minute"));
    })("p-click", function PoTimerComponent_For_17_Template_po_button_p_click_0_listener() {
      const ctx_r7 = ɵɵrestoreView(_r6);
      const minute_r9 = ctx_r7.$implicit;
      const ɵ$index_20_r7 = ctx_r7.$index;
      const ctx_r2 = ɵɵnextContext();
      ctx_r2.onSelectMinute(minute_r9);
      return ɵɵresetView(ctx_r2.onCellFocus("minute", ɵ$index_20_r7));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const minute_r9 = ctx.$implicit;
    const ɵ$index_20_r7 = ctx.$index;
    const ctx_r2 = ɵɵnextContext();
    ɵɵclassProp("po-timer-display-selected", ctx_r2.selectedMinute === minute_r9);
    ɵɵproperty("p-tabindex", ctx_r2.getCellTabIndex("minute", ɵ$index_20_r7))("p-label", ctx_r2.formatValue(minute_r9))("p-aria-label", ctx_r2.formatValue(minute_r9) + " " + ctx_r2.literals.minutes)("p-disabled", ctx_r2.disabledMinuteCache.has(minute_r9))("p-size", ctx_r2.size);
    ɵɵattribute("id", "po-timer-minute-" + ɵ$index_20_r7)("data-aria-role", "option")("data-aria-selected", ctx_r2.selectedMinute === minute_r9 ? "true" : null)("data-aria-setsize", ctx_r2.minutes.length)("data-aria-posinset", ɵ$index_20_r7 % ctx_r2.minutes.length + 1);
  }
}
function PoTimerComponent_Conditional_18_For_7_Template(rf, ctx) {
  if (rf & 1) {
    const _r11 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-button", 22, 11);
    ɵɵlistener("focus", function PoTimerComponent_Conditional_18_For_7_Template_po_button_focus_0_listener() {
      const ɵ$index_30_r12 = ɵɵrestoreView(_r11).$index;
      const ctx_r2 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r2.onCellFocus("second", ɵ$index_30_r12));
    })("keydown", function PoTimerComponent_Conditional_18_For_7_Template_po_button_keydown_0_listener($event) {
      ɵɵrestoreView(_r11);
      const ctx_r2 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r2.onCellKeydown($event, "second"));
    })("p-click", function PoTimerComponent_Conditional_18_For_7_Template_po_button_p_click_0_listener() {
      const ctx_r12 = ɵɵrestoreView(_r11);
      const second_r14 = ctx_r12.$implicit;
      const ɵ$index_30_r12 = ctx_r12.$index;
      const ctx_r2 = ɵɵnextContext(2);
      ctx_r2.onSelectSecond(second_r14);
      return ɵɵresetView(ctx_r2.onCellFocus("second", ɵ$index_30_r12));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const second_r14 = ctx.$implicit;
    const ɵ$index_30_r12 = ctx.$index;
    const ctx_r2 = ɵɵnextContext(2);
    ɵɵclassProp("po-timer-display-selected", ctx_r2.selectedSecond === second_r14);
    ɵɵproperty("p-tabindex", ctx_r2.getCellTabIndex("second", ɵ$index_30_r12))("p-label", ctx_r2.formatValue(second_r14))("p-aria-label", ctx_r2.formatValue(second_r14) + " " + ctx_r2.literals.seconds)("p-disabled", ctx_r2.disabledSecondCache.has(second_r14))("p-size", ctx_r2.size);
    ɵɵattribute("id", "po-timer-second-" + ɵ$index_30_r12)("data-aria-role", "option")("data-aria-selected", ctx_r2.selectedSecond === second_r14 ? "true" : null)("data-aria-setsize", ctx_r2.seconds.length)("data-aria-posinset", ɵ$index_30_r12 % ctx_r2.seconds.length + 1);
  }
}
function PoTimerComponent_Conditional_18_Template(rf, ctx) {
  if (rf & 1) {
    const _r10 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 16, 8);
    ɵɵlistener("wheel", function PoTimerComponent_Conditional_18_Template_div_wheel_0_listener($event) {
      ɵɵrestoreView(_r10);
      const ctx_r2 = ɵɵnextContext();
      return ɵɵresetView(ctx_r2.onColumnWheel($event, "second"));
    });
    ɵɵelementStart(2, "div", 17, 9)(4, "div", 18, 10);
    ɵɵrepeaterCreate(6, PoTimerComponent_Conditional_18_For_7_Template, 2, 12, "po-button", 19, ɵɵcomponentInstance().trackByIndex, true);
    ɵɵelementEnd()()();
  }
  if (rf & 2) {
    const ctx_r2 = ɵɵnextContext();
    ɵɵattribute("aria-label", ctx_r2.literals.seconds)("aria-activedescendant", ctx_r2.activeDescendantIds.second);
    ɵɵadvance(6);
    ɵɵrepeater(ctx_r2.displaySeconds);
  }
}
function PoTimerComponent_Conditional_19_Template(rf, ctx) {
  if (rf & 1) {
    const _r15 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 21, 12)(2, "div", 23)(3, "po-button", 24, 13);
    ɵɵlistener("focus", function PoTimerComponent_Conditional_19_Template_po_button_focus_3_listener() {
      ɵɵrestoreView(_r15);
      const ctx_r2 = ɵɵnextContext();
      return ɵɵresetView(ctx_r2.onCellFocus("period", 0));
    })("p-click", function PoTimerComponent_Conditional_19_Template_po_button_p_click_3_listener() {
      ɵɵrestoreView(_r15);
      const ctx_r2 = ɵɵnextContext();
      ctx_r2.onSelectPeriod("AM");
      return ɵɵresetView(ctx_r2.onCellFocus("period", 0));
    })("keydown", function PoTimerComponent_Conditional_19_Template_po_button_keydown_3_listener($event) {
      ɵɵrestoreView(_r15);
      const ctx_r2 = ɵɵnextContext();
      return ɵɵresetView(ctx_r2.onPeriodKeydown($event));
    });
    ɵɵelementEnd();
    ɵɵelementStart(5, "po-button", 25, 13);
    ɵɵlistener("focus", function PoTimerComponent_Conditional_19_Template_po_button_focus_5_listener() {
      ɵɵrestoreView(_r15);
      const ctx_r2 = ɵɵnextContext();
      return ɵɵresetView(ctx_r2.onCellFocus("period", 1));
    })("p-click", function PoTimerComponent_Conditional_19_Template_po_button_p_click_5_listener() {
      ɵɵrestoreView(_r15);
      const ctx_r2 = ɵɵnextContext();
      ctx_r2.onSelectPeriod("PM");
      return ɵɵresetView(ctx_r2.onCellFocus("period", 1));
    })("keydown", function PoTimerComponent_Conditional_19_Template_po_button_keydown_5_listener($event) {
      ɵɵrestoreView(_r15);
      const ctx_r2 = ɵɵnextContext();
      return ɵɵresetView(ctx_r2.onPeriodKeydown($event));
    });
    ɵɵelementEnd()()();
  }
  if (rf & 2) {
    const ctx_r2 = ɵɵnextContext();
    ɵɵadvance(3);
    ɵɵclassProp("po-timer-display-selected", ctx_r2.period === "AM");
    ɵɵproperty("p-size", ctx_r2.size)("p-tabindex", ctx_r2.getCellTabIndex("period", 0))("p-disabled", ctx_r2.isPeriodDisabled("AM"));
    ɵɵattribute("role", "radio")("aria-checked", ctx_r2.period === "AM");
    ɵɵadvance(2);
    ɵɵclassProp("po-timer-display-selected", ctx_r2.period === "PM");
    ɵɵproperty("p-size", ctx_r2.size)("p-tabindex", ctx_r2.getCellTabIndex("period", 1))("p-disabled", ctx_r2.isPeriodDisabled("PM"));
    ɵɵattribute("role", "radio")("aria-checked", ctx_r2.period === "PM");
  }
}
function PoCalendarFooterComponent_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-button", 3);
    ɵɵlistener("click", function PoCalendarFooterComponent_Conditional_2_Template_po_button_click_0_listener() {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onSelectToday());
    })("keydown", function PoCalendarFooterComponent_Conditional_2_Template_po_button_keydown_0_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onTodayKeydown($event));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("p-label", ctx_r1.labelToday)("p-disabled", ctx_r1.todayDisabled)("p-size", ctx_r1.size);
  }
}
function PoCleanComponent_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-icon", 1);
    ɵɵlistener("click", function PoCleanComponent_Conditional_0_Template_po_icon_click_0_listener() {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.clear());
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵclassProp("po-field-icon-aa", ctx_r1.size === "small")("po-field-icon-large", ctx_r1.size === "large");
    ɵɵproperty("p-icon", ctx_r1.icon || "ICON_CLEAR_CONTENT");
  }
}
var _c44 = ["outerContainer"];
var _c45 = ["containerElement"];
var _c46 = ["contentElement"];
var _c47 = ["fieldContainerInput"];
var _c48 = ["iconArrow"];
var _c49 = ["inp"];
var _c50 = ["poListbox"];
function PoComboComponent_Conditional_10_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 13);
    ɵɵelement(1, "po-icon", 22);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵclassProp("po-field-icon-disabled", ctx_r0.isDisabled)("po-field-icon-aa", ctx_r0.size === "small");
    ɵɵproperty("p-icon", ctx_r0.icon);
  }
}
function PoComboComponent_Conditional_14_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-loading-icon", 16);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("p-size", ctx_r0.mapSizeToIcon(ctx_r0.size));
  }
}
function PoComboComponent_Conditional_15_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    const _r2 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-clean", 24);
    ɵɵlistener("p-change-event", function PoComboComponent_Conditional_15_Conditional_0_Template_po_clean_p_change_event_0_listener($event) {
      ɵɵrestoreView(_r2);
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.clear($event));
    })("click", function PoComboComponent_Conditional_15_Conditional_0_Template_po_clean_click_0_listener($event) {
      ɵɵrestoreView(_r2);
      const ctx_r0 = ɵɵnextContext(2);
      ctx_r0.clear(null);
      return ɵɵresetView($event.preventDefault());
    })("keydown.enter", function PoComboComponent_Conditional_15_Conditional_0_Template_po_clean_keydown_enter_0_listener($event) {
      ɵɵrestoreView(_r2);
      const ctx_r0 = ɵɵnextContext(2);
      ctx_r0.clearAndFocus();
      return ɵɵresetView($event.preventDefault());
    })("keydown.tab", function PoComboComponent_Conditional_15_Conditional_0_Template_po_clean_keydown_tab_0_listener($event) {
      ɵɵrestoreView(_r2);
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.handleCleanKeyboardTab($event));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵclassProp("po-combo-clean-aa", ctx_r0.size === "small");
    ɵɵproperty("p-element-ref", ctx_r0.inputEl)("p-size", ctx_r0.size);
    ɵɵattribute("aria-label", ctx_r0.literals.clean);
  }
}
function PoComboComponent_Conditional_15_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵconditionalCreate(0, PoComboComponent_Conditional_15_Conditional_0_Template, 1, 5, "po-clean", 23);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵconditional(!ctx_r0.loading && ctx_r0.isCleanVisible() ? 0 : -1);
  }
}
function PoComboComponent_Conditional_19_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-helper", 19, 6);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    const poHelper_r3 = ɵɵreadContextLet(0);
    ɵɵproperty("p-size", ctx_r0.size)("p-helper", poHelper_r3.helperSettings)("p-disabled", ctx_r0.isDisabled)("p-append-in-body", ctx_r0.appendBox);
  }
}
function PoComboComponent_Conditional_20_ng_template_0_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoComboComponent_Conditional_20_ng_template_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, PoComboComponent_Conditional_20_ng_template_0_ng_container_0_Template, 1, 0, "ng-container", 25);
  }
  if (rf & 2) {
    ɵɵnextContext(2);
    const dropdownListbox_r4 = ɵɵreference(24);
    ɵɵproperty("ngTemplateOutlet", dropdownListbox_r4);
  }
}
function PoComboComponent_Conditional_20_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, PoComboComponent_Conditional_20_ng_template_0_Template, 1, 1, "ng-template", 20);
  }
  if (rf & 2) {
    ɵɵnextContext();
    const trigger_r5 = ɵɵreference(6);
    ɵɵproperty("cdkConnectedOverlayOrigin", trigger_r5)("cdkConnectedOverlayOpen", true)("cdkConnectedOverlayDisableClose", true);
  }
}
function PoComboComponent_Conditional_21_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoComboComponent_Conditional_21_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, PoComboComponent_Conditional_21_ng_container_0_Template, 1, 0, "ng-container", 25);
  }
  if (rf & 2) {
    ɵɵnextContext();
    const dropdownListbox_r4 = ɵɵreference(24);
    ɵɵproperty("ngTemplateOutlet", dropdownListbox_r4);
  }
}
function PoComboComponent_ng_template_23_Template(rf, ctx) {
  if (rf & 1) {
    const _r6 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 26, 7)(2, "po-listbox", 27, 8);
    ɵɵlistener("p-selectcombo-item", function PoComboComponent_ng_template_23_Template_po_listbox_p_selectcombo_item_2_listener($event) {
      ɵɵrestoreView(_r6);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.onOptionClick($event, $event.event));
    })("p-update-infinite-scroll", function PoComboComponent_ng_template_23_Template_po_listbox_p_update_infinite_scroll_2_listener() {
      ɵɵrestoreView(_r6);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.showMoreInfiniteScroll());
    })("p-close", function PoComboComponent_ng_template_23_Template_po_listbox_p_close_2_listener() {
      ɵɵrestoreView(_r6);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.onCloseCombo());
    })("keydown", function PoComboComponent_ng_template_23_Template_po_listbox_keydown_2_listener($event) {
      ɵɵrestoreView(_r6);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.onListboxKeyDown($event));
    });
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("hidden", !ctx_r0.comboOpen && !ctx_r0.isServerSearching);
    ɵɵadvance(2);
    ɵɵproperty("p-items", ctx_r0.visibleOptions)("p-field-value", ctx_r0.dynamicValue)("p-field-label", ctx_r0.dynamicLabel)("p-template", ctx_r0.comboOptionTemplate)("p-search-value", ctx_r0.getInputValue())("p-infinite-loading", ctx_r0.infiniteLoading)("p-infinite-scroll", ctx_r0.infiniteScroll)("p-filtering", ctx_r0.isFiltering)("p-cache", ctx_r0.cache)("p-filter-mode", ctx_r0.filterMode)("p-visible", ctx_r0.comboOpen)("p-is-searching", ctx_r0.isServerSearching)("p-should-mark-letter", ctx_r0.shouldMarkLetters)("p-compare-cache", ctx_r0.compareObjects(ctx_r0.cacheOptions, ctx_r0.visibleOptions))("p-combo-service", ctx_r0.service)("p-infinite-scroll-distance", ctx_r0.infiniteScrollDistance)("p-size", ctx_r0.size)("p-container-width", ctx_r0.containerWidth);
  }
}
function PoCalendarHeaderComponent_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0, 1);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("ngTemplateOutlet", ctx_r0.headerTemplate)("ngTemplateOutletContext", ɵɵpureFunction1(2, _c21, ctx_r0.templateContext));
  }
}
function PoCalendarHeaderComponent_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    const _r2 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-button", 4);
    ɵɵlistener("click", function PoCalendarHeaderComponent_Conditional_2_Template_po_button_click_0_listener() {
      ɵɵrestoreView(_r2);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.previous.emit());
    })("keydown.enter", function PoCalendarHeaderComponent_Conditional_2_Template_po_button_keydown_enter_0_listener($event) {
      ɵɵrestoreView(_r2);
      const ctx_r0 = ɵɵnextContext();
      ctx_r0.previous.emit();
      return ɵɵresetView($event.preventDefault());
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("p-aria-label", ctx_r0.previousMonthLabel)("p-size", ctx_r0.size);
  }
}
function PoCalendarHeaderComponent_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    const _r3 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-button", 5);
    ɵɵlistener("click", function PoCalendarHeaderComponent_Conditional_3_Template_po_button_click_0_listener() {
      ɵɵrestoreView(_r3);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.next.emit());
    })("keydown.enter", function PoCalendarHeaderComponent_Conditional_3_Template_po_button_keydown_enter_0_listener($event) {
      ɵɵrestoreView(_r3);
      const ctx_r0 = ɵɵnextContext();
      ctx_r0.next.emit();
      return ɵɵresetView($event.preventDefault());
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("p-aria-label", ctx_r0.nextMonthLabel)("p-size", ctx_r0.size);
  }
}
function PoCalendarWrapperComponent_Conditional_1_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-calendar-header", 11);
    ɵɵlistener("p-previous", function PoCalendarWrapperComponent_Conditional_1_Conditional_1_Template_po_calendar_header_p_previous_0_listener() {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.onPreviousMonth());
    })("p-next", function PoCalendarWrapperComponent_Conditional_1_Conditional_1_Template_po_calendar_header_p_next_0_listener() {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.onNextMonth());
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    const internalComboTemplate_r3 = ɵɵreference(6);
    ɵɵproperty("p-display-month", ctx_r1.displayMonth)("p-display-year", ctx_r1.displayYear)("p-template-context", ctx_r1.templateContext)("p-header-template", ctx_r1.headerTemplate || internalComboTemplate_r3)("p-size", ctx_r1.size)("p-locale", ctx_r1.locale);
  }
}
function PoCalendarWrapperComponent_Conditional_1_For_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 8);
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const weekDay_r4 = ctx.$implicit;
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", weekDay_r4, " ");
  }
}
function PoCalendarWrapperComponent_Conditional_1_For_8_Template(rf, ctx) {
  if (rf & 1) {
    const _r5 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 12);
    ɵɵlistener("click", function PoCalendarWrapperComponent_Conditional_1_For_8_Template_div_click_0_listener() {
      const day_r6 = ɵɵrestoreView(_r5).$implicit;
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.onSelectDate(day_r6));
    })("mouseenter", function PoCalendarWrapperComponent_Conditional_1_For_8_Template_div_mouseenter_0_listener() {
      const day_r6 = ɵɵrestoreView(_r5).$implicit;
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.onMouseEnter(day_r6));
    })("mouseleave", function PoCalendarWrapperComponent_Conditional_1_For_8_Template_div_mouseleave_0_listener() {
      ɵɵrestoreView(_r5);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.onMouseLeave());
    })("keydown", function PoCalendarWrapperComponent_Conditional_1_For_8_Template_div_keydown_0_listener($event) {
      const ctx_r6 = ɵɵrestoreView(_r5);
      const day_r6 = ctx_r6.$implicit;
      const ɵ$index_19_r8 = ctx_r6.$index;
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.onDayKeydown($event, day_r6, ɵ$index_19_r8));
    });
    ɵɵelementStart(1, "span", 13);
    ɵɵtext(2);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const day_r6 = ctx.$implicit;
    const ɵ$index_19_r8 = ctx.$index;
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵproperty("ngClass", ctx_r1.getDayBackgroundColor(day_r6))("tabindex", ctx_r1.getDayTabIndex(day_r6, ɵ$index_19_r8));
    ɵɵattribute("data-other-month", day_r6.getMonth() !== ctx_r1.displayMonthNumber)("data-day-index", ɵ$index_19_r8);
    ɵɵadvance();
    ɵɵproperty("ngClass", ctx_r1.getDayForegroundColor(day_r6));
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", day_r6.getDate(), " ");
  }
}
function PoCalendarWrapperComponent_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵconditionalCreate(1, PoCalendarWrapperComponent_Conditional_1_Conditional_1_Template, 1, 6, "po-calendar-header", 5);
    ɵɵelementStart(2, "div", 6)(3, "div", 7);
    ɵɵrepeaterCreate(4, PoCalendarWrapperComponent_Conditional_1_For_5_Template, 2, 1, "div", 8, ɵɵcomponentInstance().trackByWeekDay, true);
    ɵɵelementEnd();
    ɵɵelementStart(6, "div", 9);
    ɵɵrepeaterCreate(7, PoCalendarWrapperComponent_Conditional_1_For_8_Template, 3, 6, "div", 10, ɵɵcomponentInstance().trackByDay, true);
    ɵɵelementEnd()();
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵconditional(ctx_r1.templateContext ? 1 : -1);
    ɵɵadvance(3);
    ɵɵrepeater(ctx_r1.displayWeekDays);
    ɵɵadvance(3);
    ɵɵrepeater(ctx_r1.displayDays);
  }
}
function PoCalendarWrapperComponent_Conditional_2_For_8_Template(rf, ctx) {
  if (rf & 1) {
    const _r10 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 17);
    ɵɵlistener("click", function PoCalendarWrapperComponent_Conditional_2_For_8_Template_div_click_0_listener() {
      const ɵ$index_39_r11 = ɵɵrestoreView(_r10).$index;
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.onSelectMonth(ctx_r1.displayYear, ɵ$index_39_r11));
    });
    ɵɵelementStart(1, "span", 13);
    ɵɵtext(2);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const month_r12 = ctx.$implicit;
    const ɵ$index_39_r11 = ctx.$index;
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵproperty("ngClass", ctx_r1.getBackgroundColor(ɵ$index_39_r11, ctx_r1.displayMonthNumber));
    ɵɵadvance();
    ɵɵproperty("ngClass", ctx_r1.getForegroundColor(ɵ$index_39_r11, ctx_r1.displayMonthNumber));
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", month_r12, " ");
  }
}
function PoCalendarWrapperComponent_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    const _r9 = ɵɵgetCurrentView();
    ɵɵelementContainerStart(0);
    ɵɵelementStart(1, "po-calendar-header", 14);
    ɵɵlistener("p-previous", function PoCalendarWrapperComponent_Conditional_2_Template_po_calendar_header_p_previous_1_listener() {
      ɵɵrestoreView(_r9);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.updateYear(-1));
    })("p-next", function PoCalendarWrapperComponent_Conditional_2_Template_po_calendar_header_p_next_1_listener() {
      ɵɵrestoreView(_r9);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.updateYear(1));
    });
    ɵɵelementEnd();
    ɵɵelementStart(2, "div", 6)(3, "div", 7)(4, "div", 8);
    ɵɵtext(5);
    ɵɵelementEnd()();
    ɵɵelementStart(6, "div", 15);
    ɵɵrepeaterCreate(7, PoCalendarWrapperComponent_Conditional_2_For_8_Template, 3, 3, "div", 16, ɵɵcomponentInstance().trackByMonth, true);
    ɵɵelementEnd()();
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    const internalComboTemplate_r3 = ɵɵreference(6);
    ɵɵadvance();
    ɵɵproperty("p-template-context", ctx_r1.templateContext)("p-header-template", ctx_r1.headerTemplate || internalComboTemplate_r3)("p-size", ctx_r1.size)("p-locale", ctx_r1.locale);
    ɵɵadvance(4);
    ɵɵtextInterpolate1(" ", ctx_r1.monthLabel, " ");
    ɵɵadvance(2);
    ɵɵrepeater(ctx_r1.displayMonths);
  }
}
function PoCalendarWrapperComponent_Conditional_3_For_8_Template(rf, ctx) {
  if (rf & 1) {
    const _r14 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 20);
    ɵɵlistener("click", function PoCalendarWrapperComponent_Conditional_3_For_8_Template_div_click_0_listener() {
      const year_r15 = ɵɵrestoreView(_r14).$implicit;
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.onSelectYear(year_r15, ctx_r1.displayMonthNumber));
    });
    ɵɵelementStart(1, "span", 13);
    ɵɵtext(2);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const year_r15 = ctx.$implicit;
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵproperty("ngClass", ctx_r1.getBackgroundColor(year_r15, ctx_r1.currentYear));
    ɵɵadvance();
    ɵɵproperty("ngClass", ctx_r1.getForegroundColor(year_r15, ctx_r1.currentYear));
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", year_r15, " ");
  }
}
function PoCalendarWrapperComponent_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    const _r13 = ɵɵgetCurrentView();
    ɵɵelementContainerStart(0);
    ɵɵelementStart(1, "po-calendar-header", 14);
    ɵɵlistener("p-previous", function PoCalendarWrapperComponent_Conditional_3_Template_po_calendar_header_p_previous_1_listener() {
      ɵɵrestoreView(_r13);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.updateYear(-10));
    })("p-next", function PoCalendarWrapperComponent_Conditional_3_Template_po_calendar_header_p_next_1_listener() {
      ɵɵrestoreView(_r13);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.updateYear(10));
    });
    ɵɵelementEnd();
    ɵɵelementStart(2, "div", 6)(3, "div", 7)(4, "div", 8);
    ɵɵtext(5);
    ɵɵelementEnd()();
    ɵɵelementStart(6, "div", 18);
    ɵɵrepeaterCreate(7, PoCalendarWrapperComponent_Conditional_3_For_8_Template, 3, 3, "div", 19, ɵɵcomponentInstance().trackByYear, true);
    ɵɵelementEnd()();
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    const internalComboTemplate_r3 = ɵɵreference(6);
    ɵɵadvance();
    ɵɵproperty("p-template-context", ctx_r1.templateContext)("p-header-template", ctx_r1.headerTemplate || internalComboTemplate_r3)("p-size", ctx_r1.size)("p-locale", ctx_r1.locale);
    ɵɵadvance(4);
    ɵɵtextInterpolate1(" ", ctx_r1.yearLabel, " ");
    ɵɵadvance(2);
    ɵɵrepeater(ctx_r1.displayDecade);
  }
}
function PoCalendarWrapperComponent_Conditional_4_Template(rf, ctx) {
  if (rf & 1) {
    const _r16 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-calendar-footer", 21);
    ɵɵlistener("p-clear", function PoCalendarWrapperComponent_Conditional_4_Template_po_calendar_footer_p_clear_0_listener() {
      ɵɵrestoreView(_r16);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onClear());
    })("p-select-today", function PoCalendarWrapperComponent_Conditional_4_Template_po_calendar_footer_p_select_today_0_listener() {
      ɵɵrestoreView(_r16);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onSelectToday());
    })("p-close-calendar", function PoCalendarWrapperComponent_Conditional_4_Template_po_calendar_footer_p_close_calendar_0_listener() {
      ɵɵrestoreView(_r16);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onFooterCloseCalendar());
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("p-label-clear", ctx_r1.displayToClean)("p-label-today", ctx_r1.displayToday)("p-hide-today-button", ctx_r1.hideTodayButton)("p-size", ctx_r1.size)("p-responsive", ctx_r1.responsive)("p-today-disabled", ctx_r1.isTodayUnavailable());
  }
}
function PoCalendarWrapperComponent_ng_template_5_Template(rf, ctx) {
  if (rf & 1) {
    const _r17 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-combo", 22, 1);
    ɵɵlistener("p-change", function PoCalendarWrapperComponent_ng_template_5_Template_po_combo_p_change_0_listener($event) {
      const combo_r18 = ɵɵrestoreView(_r17).$implicit;
      return ɵɵresetView(combo_r18.updateDate(combo_r18.year, $event));
    })("p-blur", function PoCalendarWrapperComponent_ng_template_5_Template_po_combo_p_blur_0_listener() {
      const combo_r18 = ɵɵrestoreView(_r17).$implicit;
      const monthCombo_r19 = ɵɵreference(1);
      return ɵɵresetView(combo_r18.onComboBlur(monthCombo_r19));
    });
    ɵɵelementEnd();
    ɵɵelementStart(2, "po-combo", 23, 2);
    ɵɵlistener("p-change", function PoCalendarWrapperComponent_ng_template_5_Template_po_combo_p_change_2_listener($event) {
      const combo_r18 = ɵɵrestoreView(_r17).$implicit;
      return ɵɵresetView(combo_r18.updateDate($event, combo_r18.monthIndex));
    })("p-blur", function PoCalendarWrapperComponent_ng_template_5_Template_po_combo_p_blur_2_listener() {
      const combo_r18 = ɵɵrestoreView(_r17).$implicit;
      const yearCombo_r20 = ɵɵreference(3);
      return ɵɵresetView(combo_r18.onComboBlur(yearCombo_r20));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const combo_r18 = ctx.$implicit;
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("name", "month-" + ctx_r1.comboKey)("p-clean", false)("p-remove-initial-filter", true)("ngModel", combo_r18.monthIndex)("p-options", combo_r18.monthsOptions)("p-size", ctx_r1.size);
    ɵɵadvance(2);
    ɵɵproperty("name", "year-" + ctx_r1.comboKey)("p-clean", false)("p-remove-initial-filter", true)("ngModel", combo_r18.year)("p-options", combo_r18.yearsOptions)("p-size", ctx_r1.size);
  }
}
var _c51 = ["presetButton"];
function PoCalendarPresetListComponent_For_2_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-button", 3, 0);
    ɵɵlistener("click", function PoCalendarPresetListComponent_For_2_Template_po_button_click_0_listener() {
      const preset_r2 = ɵɵrestoreView(_r1).$implicit;
      const ctx_r2 = ɵɵnextContext();
      return ɵɵresetView(ctx_r2.onPresetClick(preset_r2));
    })("keydown", function PoCalendarPresetListComponent_For_2_Template_po_button_keydown_0_listener($event) {
      const ɵ$index_3_r4 = ɵɵrestoreView(_r1).$index;
      const ctx_r2 = ɵɵnextContext();
      return ɵɵresetView(ctx_r2.onKeydown($event, ɵ$index_3_r4));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const preset_r2 = ctx.$implicit;
    const ɵ$index_3_r4 = ctx.$index;
    const ctx_r2 = ɵɵnextContext();
    ɵɵclassProp("po-calendar-preset-item-selected", ctx_r2.isSelected(preset_r2));
    ɵɵproperty("p-size", ctx_r2.size)("p-label", ctx_r2.getDisplayLabel(preset_r2))("p-disabled", ctx_r2.isPresetDisabled(preset_r2))("p-tabindex", ɵ$index_3_r4 === ctx_r2.focusedIndex ? 0 : -1);
    ɵɵattribute("aria-selected", ctx_r2.isSelected(preset_r2));
  }
}
var _c52 = ["timer"];
var _c53 = ["yearOption"];
var _c54 = ["monthOption"];
var _c55 = () => ({
  partType: "start"
});
var _c56 = () => ({
  partType: "single"
});
function PoCalendarComponent_Conditional_0_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoCalendarComponent_Conditional_0_ng_container_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoCalendarComponent_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 7);
    ɵɵtemplate(1, PoCalendarComponent_Conditional_0_ng_container_1_Template, 1, 0, "ng-container", 11)(2, PoCalendarComponent_Conditional_0_ng_container_2_Template, 1, 0, "ng-container", 11);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    ɵɵnextContext();
    const calendarMonth_r1 = ɵɵreference(7);
    const calendarYear_r2 = ɵɵreference(9);
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", calendarMonth_r1);
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", calendarYear_r2);
  }
}
function PoCalendarComponent_Conditional_1_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoCalendarComponent_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 8);
    ɵɵtemplate(1, PoCalendarComponent_Conditional_1_ng_container_1_Template, 1, 0, "ng-container", 11);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    ɵɵnextContext();
    const calendarYear_r2 = ɵɵreference(9);
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", calendarYear_r2);
  }
}
function PoCalendarComponent_Conditional_2_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r3 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-calendar-preset-list", 15);
    ɵɵlistener("p-select-preset", function PoCalendarComponent_Conditional_2_Conditional_1_Template_po_calendar_preset_list_p_select_preset_0_listener($event) {
      ɵɵrestoreView(_r3);
      const ctx_r3 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r3.onPresetSelected($event));
    })("p-close-calendar", function PoCalendarComponent_Conditional_2_Conditional_1_Template_po_calendar_preset_list_p_close_calendar_0_listener() {
      ɵɵrestoreView(_r3);
      const ctx_r3 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r3.onCloseCalendar());
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r3 = ɵɵnextContext(2);
    ɵɵproperty("p-presets", ctx_r3.effectivePresets())("p-locale", ctx_r3.locale)("p-selected-preset", ctx_r3.selectedPresetLabel)("p-size", ctx_r3.size);
  }
}
function PoCalendarComponent_Conditional_2_ng_container_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoCalendarComponent_Conditional_2_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    const _r5 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-calendar-preset-list", 16);
    ɵɵlistener("p-select-preset", function PoCalendarComponent_Conditional_2_Conditional_3_Template_po_calendar_preset_list_p_select_preset_0_listener($event) {
      ɵɵrestoreView(_r5);
      const ctx_r3 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r3.onPresetSelected($event));
    })("p-close-calendar", function PoCalendarComponent_Conditional_2_Conditional_3_Template_po_calendar_preset_list_p_close_calendar_0_listener() {
      ɵɵrestoreView(_r5);
      const ctx_r3 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r3.onCloseCalendar());
    });
    ɵɵelementEnd();
    ɵɵelementStart(1, "po-calendar-footer", 17);
    ɵɵlistener("p-clear", function PoCalendarComponent_Conditional_2_Conditional_3_Template_po_calendar_footer_p_clear_1_listener($event) {
      ɵɵrestoreView(_r5);
      const ctx_r3 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r3.onSelectDate($event));
    })("p-close-calendar", function PoCalendarComponent_Conditional_2_Conditional_3_Template_po_calendar_footer_p_close_calendar_1_listener() {
      ɵɵrestoreView(_r5);
      const ctx_r3 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r3.onCloseCalendar());
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r3 = ɵɵnextContext(2);
    ɵɵproperty("p-presets", ctx_r3.effectivePresets())("p-locale", ctx_r3.locale)("p-selected-preset", ctx_r3.selectedPresetLabel)("p-size", ctx_r3.size)("p-responsive", true);
    ɵɵadvance();
    ɵɵproperty("p-size", ctx_r3.size)("p-responsive", ctx_r3.isResponsive)("p-hide-today-button", true)("p-label-clear", ctx_r3.displayToClean);
  }
}
function PoCalendarComponent_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 12);
    ɵɵconditionalCreate(1, PoCalendarComponent_Conditional_2_Conditional_1_Template, 1, 4, "po-calendar-preset-list", 13);
    ɵɵtemplate(2, PoCalendarComponent_Conditional_2_ng_container_2_Template, 1, 0, "ng-container", 14);
    ɵɵconditionalCreate(3, PoCalendarComponent_Conditional_2_Conditional_3_Template, 2, 9);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r3 = ɵɵnextContext();
    const calendarWrapper_r6 = ɵɵreference(5);
    ɵɵclassProp("po-calendar-with-presets", ctx_r3.effectivePresets().length > 0)("po-calendar-mobile", ctx_r3.isResponsive || ctx_r3.widthWithPresets)("po-calendar-responsive", !ctx_r3.isResponsive && ctx_r3.widthWithPresets);
    ɵɵadvance();
    ɵɵconditional(ctx_r3.effectivePresets().length > 0 && !ctx_r3.isResponsive && !ctx_r3.widthWithPresets ? 1 : -1);
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", calendarWrapper_r6)("ngTemplateOutletContext", ɵɵpureFunction0(10, _c55));
    ɵɵadvance();
    ɵɵconditional(ctx_r3.effectivePresets().length > 0 && (ctx_r3.isResponsive || ctx_r3.widthWithPresets) ? 3 : -1);
  }
}
function PoCalendarComponent_Conditional_3_ng_container_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoCalendarComponent_Conditional_3_Conditional_3_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoCalendarComponent_Conditional_3_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, PoCalendarComponent_Conditional_3_Conditional_3_ng_container_0_Template, 1, 0, "ng-container", 11);
  }
  if (rf & 2) {
    ɵɵnextContext(2);
    const calendarTimer_r7 = ɵɵreference(11);
    ɵɵproperty("ngTemplateOutlet", calendarTimer_r7);
  }
}
function PoCalendarComponent_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div")(1, "div", 12);
    ɵɵtemplate(2, PoCalendarComponent_Conditional_3_ng_container_2_Template, 1, 0, "ng-container", 14);
    ɵɵelementEnd();
    ɵɵconditionalCreate(3, PoCalendarComponent_Conditional_3_Conditional_3_Template, 1, 1, "ng-container");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r3 = ɵɵnextContext();
    const calendarWrapper_r6 = ɵɵreference(5);
    ɵɵclassProp("po-calendar-date-time", ctx_r3.isDateTime)("po-calendar-three-columns", ctx_r3.showSeconds || ctx_r3.format === "12")("po-calendar-four-columns", ctx_r3.showSeconds && ctx_r3.format === "12");
    ɵɵadvance(2);
    ɵɵproperty("ngTemplateOutlet", calendarWrapper_r6)("ngTemplateOutletContext", ɵɵpureFunction0(9, _c56));
    ɵɵadvance();
    ɵɵconditional(ctx_r3.isDateTime ? 3 : -1);
  }
}
function PoCalendarComponent_ng_template_4_Template(rf, ctx) {
  if (rf & 1) {
    const _r8 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-calendar-wrapper", 18);
    ɵɵlistener("p-header-change", function PoCalendarComponent_ng_template_4_Template_po_calendar_wrapper_p_header_change_0_listener($event) {
      const partType_r9 = ɵɵrestoreView(_r8).partType;
      const ctx_r3 = ɵɵnextContext();
      return ɵɵresetView(ctx_r3.onHeaderChange($event, partType_r9));
    })("p-select-date", function PoCalendarComponent_ng_template_4_Template_po_calendar_wrapper_p_select_date_0_listener($event) {
      const partType_r9 = ɵɵrestoreView(_r8).partType;
      const ctx_r3 = ɵɵnextContext();
      return ɵɵresetView(ctx_r3.onSelectDate($event, partType_r9));
    })("p-select-today", function PoCalendarComponent_ng_template_4_Template_po_calendar_wrapper_p_select_today_0_listener() {
      ɵɵrestoreView(_r8);
      const ctx_r3 = ɵɵnextContext();
      return ɵɵresetView(ctx_r3.onSelectTodayPreset());
    })("p-hover-date", function PoCalendarComponent_ng_template_4_Template_po_calendar_wrapper_p_hover_date_0_listener($event) {
      ɵɵrestoreView(_r8);
      const ctx_r3 = ɵɵnextContext();
      return ɵɵresetView(ctx_r3.onHoverDate($event));
    })("p-close-calendar", function PoCalendarComponent_ng_template_4_Template_po_calendar_wrapper_p_close_calendar_0_listener() {
      ɵɵrestoreView(_r8);
      const ctx_r3 = ɵɵnextContext();
      return ɵɵresetView(ctx_r3.onCloseCalendar());
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const partType_r9 = ctx.partType;
    const ctx_r3 = ɵɵnextContext();
    ɵɵproperty("p-value", ctx_r3.getValue(partType_r9))("p-activate-date", ctx_r3.getActivateDate(partType_r9))("p-locale", ctx_r3.locale)("p-min-date", ctx_r3.minDate)("p-presets", ctx_r3.effectivePresets().length > 0)("p-max-date", ctx_r3.maxDate)("p-part-type", partType_r9)("p-range", ctx_r3.isRange)("p-responsive", ctx_r3.isResponsive || ctx_r3.widthWithPresets)("p-selected-value", ctx_r3.value)("p-hover-value", ctx_r3.hoverValue)("p-hide-today-button", ctx_r3.effectivePresets().length > 0)("p-header-template", ctx_r3.headerTemplate)("p-size", ctx_r3.size);
  }
}
function PoCalendarComponent_ng_template_6_For_2_Template(rf, ctx) {
  if (rf & 1) {
    const _r10 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "li", 20)(1, "po-button", 21, 4);
    ɵɵlistener("p-click", function PoCalendarComponent_ng_template_6_For_2_Template_po_button_p_click_1_listener($event) {
      const ɵ$index_56_r11 = ɵɵrestoreView(_r10).$index;
      const ctx_r3 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r3.selectMonth(ɵ$index_56_r11, $event, true));
    })("keydown", function PoCalendarComponent_ng_template_6_For_2_Template_po_button_keydown_1_listener($event) {
      const ɵ$index_56_r11 = ɵɵrestoreView(_r10).$index;
      const ctx_r3 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r3.onKeydownMonth($event, ɵ$index_56_r11));
    });
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const month_r12 = ctx.$implicit;
    const ɵ$index_56_r11 = ctx.$index;
    const ctx_r3 = ɵɵnextContext(2);
    ɵɵadvance();
    ɵɵclassProp("po-button-selected", ɵ$index_56_r11 === ctx_r3.selectedIndexMonth);
    ɵɵproperty("p-label", ɵɵinterpolate(month_r12))("p-tabindex", ɵ$index_56_r11 === ctx_r3.focusedIndex ? 0 : -1)("p-size", ctx_r3.size)("p-disabled", ctx_r3.isMonthDisabled(ɵ$index_56_r11));
    ɵɵattribute("aria-selected", ɵ$index_56_r11 === ctx_r3.selectedIndexMonth);
  }
}
function PoCalendarComponent_ng_template_6_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "ul", 19);
    ɵɵrepeaterCreate(1, PoCalendarComponent_ng_template_6_For_2_Template, 3, 8, "li", 20, ɵɵrepeaterTrackByIdentity);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r3 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵrepeater(ctx_r3.displayMonths);
  }
}
function PoCalendarComponent_ng_template_8_For_2_Template(rf, ctx) {
  if (rf & 1) {
    const _r13 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "li", 23)(1, "po-button", 21, 5);
    ɵɵlistener("p-click", function PoCalendarComponent_ng_template_8_For_2_Template_po_button_p_click_1_listener($event) {
      const ctx_r13 = ɵɵrestoreView(_r13);
      const year_r15 = ctx_r13.$implicit;
      const ɵ$index_65_r16 = ctx_r13.$index;
      const ctx_r3 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r3.selectYear(ɵ$index_65_r16, $event, true, year_r15));
    })("keydown", function PoCalendarComponent_ng_template_8_For_2_Template_po_button_keydown_1_listener($event) {
      const ɵ$index_65_r16 = ɵɵrestoreView(_r13).$index;
      const ctx_r3 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r3.onKeydownYear($event, ɵ$index_65_r16));
    });
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const year_r15 = ctx.$implicit;
    const ɵ$index_65_r16 = ctx.$index;
    const ctx_r3 = ɵɵnextContext(2);
    ɵɵadvance();
    ɵɵclassProp("po-button-selected", ɵ$index_65_r16 === ctx_r3.selectedIndexYear);
    ɵɵproperty("p-label", ɵɵinterpolate(year_r15))("p-tabindex", ɵ$index_65_r16 === ctx_r3.focusedIndex ? 0 : -1)("p-size", ctx_r3.size)("p-disabled", ctx_r3.isYearDisabled(year_r15));
    ɵɵattribute("aria-selected", ɵ$index_65_r16 === ctx_r3.selectedIndexYear);
  }
}
function PoCalendarComponent_ng_template_8_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "ul", 22);
    ɵɵrepeaterCreate(1, PoCalendarComponent_ng_template_8_For_2_Template, 3, 8, "li", 23, ɵɵrepeaterTrackByIdentity);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r3 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵrepeater(ctx_r3.displayYears);
  }
}
function PoCalendarComponent_ng_template_10_Template(rf, ctx) {
  if (rf & 1) {
    const _r17 = ɵɵgetCurrentView();
    ɵɵelement(0, "div", 24);
    ɵɵelementStart(1, "po-timer", 25, 6);
    ɵɵlistener("p-change", function PoCalendarComponent_ng_template_10_Template_po_timer_p_change_1_listener($event) {
      ɵɵrestoreView(_r17);
      const ctx_r3 = ɵɵnextContext();
      return ɵɵresetView(ctx_r3.onTimeChange($event));
    })("p-boundary-tab", function PoCalendarComponent_ng_template_10_Template_po_timer_p_boundary_tab_1_listener($event) {
      ɵɵrestoreView(_r17);
      const ctx_r3 = ɵɵnextContext();
      return ɵɵresetView(ctx_r3.onTimerBoundaryTab($event));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r3 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵproperty("p-format", ctx_r3.format)("p-locale", ctx_r3.locale)("p-min-time", ctx_r3.minTime)("p-max-time", ctx_r3.maxTime)("p-minute-interval", ctx_r3.minuteInterval)("p-second-interval", ctx_r3.secondInterval)("p-show-seconds", ctx_r3.showSeconds)("p-size", ctx_r3.size);
  }
}
var _c57 = ["targetPopup"];
var _c58 = ["chartContainer"];
var _c59 = (a0, a1) => ({
  height: a0,
  "margin-top": a1
});
function PoChartComponent_Conditional_0_Conditional_1_Conditional_3_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r3 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-button", 16);
    ɵɵlistener("p-click", function PoChartComponent_Conditional_0_Conditional_1_Conditional_3_Conditional_1_Template_po_button_p_click_0_listener() {
      ɵɵrestoreView(_r3);
      const ctx_r1 = ɵɵnextContext(4);
      return ɵɵresetView(ctx_r1.openModal());
    });
    ɵɵelementEnd();
  }
}
function PoChartComponent_Conditional_0_Conditional_1_Conditional_3_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    const _r4 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-button", 17);
    ɵɵlistener("p-click", function PoChartComponent_Conditional_0_Conditional_1_Conditional_3_Conditional_2_Template_po_button_p_click_0_listener() {
      ɵɵrestoreView(_r4);
      const ctx_r1 = ɵɵnextContext(4);
      return ɵɵresetView(ctx_r1.toggleExpand());
    });
    ɵɵelementEnd();
  }
}
function PoChartComponent_Conditional_0_Conditional_1_Conditional_3_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    const _r5 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-button", 18, 3);
    ɵɵlistener("p-click", function PoChartComponent_Conditional_0_Conditional_1_Conditional_3_Conditional_3_Template_po_button_p_click_0_listener() {
      ɵɵrestoreView(_r5);
      ɵɵnextContext(3);
      const popup_r6 = ɵɵreference(8);
      return ɵɵresetView(popup_r6.toggle());
    });
    ɵɵelementEnd();
  }
}
function PoChartComponent_Conditional_0_Conditional_1_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 12);
    ɵɵconditionalCreate(1, PoChartComponent_Conditional_0_Conditional_1_Conditional_3_Conditional_1_Template, 1, 0, "po-button", 13);
    ɵɵconditionalCreate(2, PoChartComponent_Conditional_0_Conditional_1_Conditional_3_Conditional_2_Template, 1, 0, "po-button", 14);
    ɵɵconditionalCreate(3, PoChartComponent_Conditional_0_Conditional_1_Conditional_3_Conditional_3_Template, 2, 0, "po-button", 15);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵadvance();
    ɵɵconditional(!(ctx_r1.options == null ? null : ctx_r1.options.header == null ? null : ctx_r1.options.header.hideTableDetails) ? 1 : -1);
    ɵɵadvance();
    ɵɵconditional(!(ctx_r1.options == null ? null : ctx_r1.options.header == null ? null : ctx_r1.options.header.hideExpand) ? 2 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r1.showPopup ? 3 : -1);
  }
}
function PoChartComponent_Conditional_0_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 6)(1, "div", 11);
    ɵɵlistener("mouseover", function PoChartComponent_Conditional_0_Conditional_1_Template_div_mouseover_1_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.showTooltipTitle($event));
    })("focus", function PoChartComponent_Conditional_0_Conditional_1_Template_div_focus_1_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.showTooltipTitle($event));
    });
    ɵɵtext(2);
    ɵɵelementEnd();
    ɵɵconditionalCreate(3, PoChartComponent_Conditional_0_Conditional_1_Conditional_3_Template, 4, 3, "div", 12);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵadvance();
    ɵɵproperty("ngClass", !ctx_r1.showPopup && (ctx_r1.options == null ? null : ctx_r1.options.header == null ? null : ctx_r1.options.header.hideTableDetails) && (ctx_r1.options == null ? null : ctx_r1.options.header == null ? null : ctx_r1.options.header.hideExpand) ? "po-md-12" : "po-lg-9 po-md-8 po-sm-6")("p-tooltip", ctx_r1.tooltipTitle);
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ctx_r1.title, " ");
    ɵɵadvance();
    ɵɵconditional(ctx_r1.showPopup || !(ctx_r1.options == null ? null : ctx_r1.options.header == null ? null : ctx_r1.options.header.hideTableDetails) || !(ctx_r1.options == null ? null : ctx_r1.options.header == null ? null : ctx_r1.options.header.hideExpand) ? 3 : -1);
  }
}
function PoChartComponent_Conditional_0_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    const _r7 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 7)(1, "div", 19);
    ɵɵlistener("mouseover", function PoChartComponent_Conditional_0_Conditional_2_Template_div_mouseover_1_listener($event) {
      ɵɵrestoreView(_r7);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.showTooltipDescription($event));
    })("focus", function PoChartComponent_Conditional_0_Conditional_2_Template_div_focus_1_listener($event) {
      ɵɵrestoreView(_r7);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.showTooltipDescription($event));
    });
    ɵɵtext(2);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵadvance();
    ɵɵproperty("p-tooltip", ctx_r1.tooltipDescriptionGauge);
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ctx_r1.options.descriptionChart, " ");
  }
}
function PoChartComponent_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 5);
    ɵɵconditionalCreate(1, PoChartComponent_Conditional_0_Conditional_1_Template, 4, 4, "div", 6);
    ɵɵconditionalCreate(2, PoChartComponent_Conditional_0_Conditional_2_Template, 3, 2, "div", 7);
    ɵɵelement(3, "div", 8, 0)(5, "div", 9, 1)(7, "po-popup", 10, 2);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵclassProp("expanded", ctx_r1.isExpanded)("po-chart-container-gauge", ctx_r1.isTypeGauge && !ctx_r1.isExpanded && (ctx_r1.options == null ? null : ctx_r1.options.showContainerGauge));
    ɵɵadvance();
    ɵɵconditional(ctx_r1.showHeader ? 1 : -1);
    ɵɵadvance();
    ɵɵconditional((ctx_r1.options == null ? null : ctx_r1.options.descriptionChart) ? 2 : -1);
    ɵɵadvance();
    ɵɵproperty("ngStyle", ctx_r1.showHeader ? ɵɵpureFunction2(13, _c59, ctx_r1.height - ctx_r1.headerHeight - ctx_r1.paddingContainerGauge - ctx_r1.descriptionHeight + "px", ctx_r1.chartMarginTop) : ɵɵpureFunction2(16, _c59, ctx_r1.height - ctx_r1.paddingContainerGauge - ctx_r1.descriptionHeight + "px", ctx_r1.chartMarginTop));
    ɵɵadvance(2);
    ɵɵproperty("p-tooltip", ctx_r1.tooltipText)("p-hide-arrow", true)("p-inner-html", true)("p-tooltip-position", ctx_r1.positionTooltip);
    ɵɵadvance(2);
    ɵɵproperty("p-actions", ctx_r1.popupActions)("p-target", ctx_r1.targetRef);
  }
}
function PoContainerComponent_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 1);
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ctx_r0.title, " ");
  }
}
function PoContextMenuComponent_Conditional_4_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "span", 11, 0);
    ɵɵlistener("mouseenter", function PoContextMenuComponent_Conditional_4_Conditional_1_Template_span_mouseenter_0_listener() {
      ɵɵrestoreView(_r1);
      const contextTitleRef_r2 = ɵɵreference(1);
      const ctx_r2 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r2.handlerContextTitleTooltip(contextTitleRef_r2));
    });
    ɵɵtext(2);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r2 = ɵɵnextContext(2);
    ɵɵproperty("p-tooltip", ctx_r2.contextTitleTooltip());
    ɵɵadvance(2);
    ɵɵtextInterpolate1(" ", ctx_r2.contextTitle(), " ");
  }
}
function PoContextMenuComponent_Conditional_4_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    const _r4 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "span", 12, 1);
    ɵɵlistener("mouseenter", function PoContextMenuComponent_Conditional_4_Conditional_2_Template_span_mouseenter_0_listener() {
      ɵɵrestoreView(_r4);
      const titleRef_r5 = ɵɵreference(1);
      const ctx_r2 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r2.handlerTitleTooltip(titleRef_r5));
    });
    ɵɵtext(2);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r2 = ɵɵnextContext(2);
    ɵɵproperty("p-tooltip", ctx_r2.titleTooltip());
    ɵɵadvance(2);
    ɵɵtextInterpolate1(" ", ctx_r2.title(), " ");
  }
}
function PoContextMenuComponent_Conditional_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 7);
    ɵɵconditionalCreate(1, PoContextMenuComponent_Conditional_4_Conditional_1_Template, 3, 2, "span", 9);
    ɵɵconditionalCreate(2, PoContextMenuComponent_Conditional_4_Conditional_2_Template, 3, 2, "span", 10);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r2 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵconditional(ctx_r2.contextTitle() ? 1 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r2.title() ? 2 : -1);
  }
}
function PoContextMenuComponent_Conditional_5_For_3_Template(rf, ctx) {
  if (rf & 1) {
    const _r6 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "li", 15, 2);
    ɵɵlistener("click", function PoContextMenuComponent_Conditional_5_For_3_Template_li_click_0_listener() {
      const item_r7 = ɵɵrestoreView(_r6).$implicit;
      const ctx_r2 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r2.selectItem(item_r7));
    })("keydown.enter", function PoContextMenuComponent_Conditional_5_For_3_Template_li_keydown_enter_0_listener() {
      const item_r7 = ɵɵrestoreView(_r6).$implicit;
      const ctx_r2 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r2.selectItem(item_r7));
    })("keydown.space", function PoContextMenuComponent_Conditional_5_For_3_Template_li_keydown_space_0_listener() {
      const item_r7 = ɵɵrestoreView(_r6).$implicit;
      const ctx_r2 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r2.selectItem(item_r7));
    })("mouseenter", function PoContextMenuComponent_Conditional_5_For_3_Template_li_mouseenter_0_listener() {
      const item_r7 = ɵɵrestoreView(_r6).$implicit;
      const itemRef_r8 = ɵɵreference(1);
      const ctx_r2 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r2.handlerItemTooltip(item_r7, itemRef_r8));
    });
    ɵɵelementStart(2, "span", 16);
    ɵɵtext(3);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const item_r7 = ctx.$implicit;
    ɵɵclassProp("po-context-menu-item-selected", item_r7.selected);
    ɵɵproperty("p-tooltip", item_r7.tooltip);
    ɵɵattribute("aria-label", item_r7.label);
    ɵɵadvance(3);
    ɵɵtextInterpolate(item_r7.label);
  }
}
function PoContextMenuComponent_Conditional_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "nav", 8)(1, "ul", 13);
    ɵɵrepeaterCreate(2, PoContextMenuComponent_Conditional_5_For_3_Template, 4, 5, "li", 14, ɵɵrepeaterTrackByIndex);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r2 = ɵɵnextContext();
    ɵɵadvance(2);
    ɵɵrepeater(ctx_r2._items());
  }
}
var _c60 = ["disclaimerContainer"];
function PoDisclaimerComponent_Conditional_2_Conditional_4_Template(rf, ctx) {
  if (rf & 1) {
    const _r3 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-icon", 7);
    ɵɵlistener("click", function PoDisclaimerComponent_Conditional_2_Conditional_4_Template_po_icon_click_0_listener() {
      ɵɵrestoreView(_r3);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.close());
    })("keydown", function PoDisclaimerComponent_Conditional_2_Conditional_4_Template_po_icon_keydown_0_listener($event) {
      ɵɵrestoreView(_r3);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.onKeyPress($event));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵclassProp("po-disclaimer-remove-danger", ctx_r1.type === "danger");
    ɵɵattribute("aria-label", ctx_r1.setAriaLabel())("role", !ctx_r1.hideClose ? "button" : "");
  }
}
function PoDisclaimerComponent_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 3);
    ɵɵlistener("click", function PoDisclaimerComponent_Conditional_2_Template_div_click_0_listener() {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.emitLastDisclaimer(ctx_r1.lastDisclaimer));
    })("keydown.enter", function PoDisclaimerComponent_Conditional_2_Template_div_keydown_enter_0_listener() {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.emitLastDisclaimer(ctx_r1.lastDisclaimer));
    });
    ɵɵelementStart(1, "div", 4)(2, "span", 5);
    ɵɵtext(3);
    ɵɵelementEnd()();
    ɵɵconditionalCreate(4, PoDisclaimerComponent_Conditional_2_Conditional_4_Template, 1, 4, "po-icon", 6);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵclassProp("po-disclaimer-remove-all", ctx_r1.hideClose)("po-disclaimer-danger", ctx_r1.type === "danger");
    ɵɵproperty("p-tooltip", ctx_r1.getWidthDisclaimer() ? ctx_r1.getLabel() : "")("p-append-in-body", ctx_r1.appendInBody)("p-tooltip-position", ctx_r1.tooltipPosition)("tabindex", ctx_r1.hideClose ? "0" : "-1");
    ɵɵadvance();
    ɵɵclassProp("show-close", !ctx_r1.hideClose);
    ɵɵadvance(2);
    ɵɵtextInterpolate1(" ", ctx_r1.getLabel(), " ");
    ɵɵadvance();
    ɵɵconditional(ctx_r1.hideClose === false ? 4 : -1);
  }
}
function PoDisclaimerGroupComponent_Conditional_0_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 1);
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵadvance();
    ɵɵtextInterpolate(ctx_r0.title);
  }
}
function PoDisclaimerGroupComponent_Conditional_0_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    const _r2 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-disclaimer-remove", 4);
    ɵɵlistener("keydown", function PoDisclaimerGroupComponent_Conditional_0_Conditional_2_Template_po_disclaimer_remove_keydown_0_listener($event) {
      ɵɵrestoreView(_r2);
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.onKeyPress($event));
    })("p-remove-all-action", function PoDisclaimerGroupComponent_Conditional_0_Conditional_2_Template_po_disclaimer_remove_p_remove_all_action_0_listener() {
      ɵɵrestoreView(_r2);
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.removeAllItems());
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵproperty("p-label", ctx_r0.literals.removeAll);
  }
}
function PoDisclaimerGroupComponent_Conditional_0_For_4_Template(rf, ctx) {
  if (rf & 1) {
    const _r3 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-tag", 5);
    ɵɵlistener("p-close", function PoDisclaimerGroupComponent_Conditional_0_For_4_Template_po_tag_p_close_0_listener($event) {
      const disclaimer_r4 = ɵɵrestoreView(_r3).$implicit;
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.onCloseAction(disclaimer_r4, $event));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const disclaimer_r4 = ctx.$implicit;
    ɵɵproperty("p-value", disclaimer_r4.label || disclaimer_r4.value)("p-removable", !disclaimer_r4.hideClose);
  }
}
function PoDisclaimerGroupComponent_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 0);
    ɵɵconditionalCreate(1, PoDisclaimerGroupComponent_Conditional_0_Conditional_1_Template, 2, 1, "div", 1);
    ɵɵconditionalCreate(2, PoDisclaimerGroupComponent_Conditional_0_Conditional_2_Template, 1, 1, "po-disclaimer-remove", 2);
    ɵɵrepeaterCreate(3, PoDisclaimerGroupComponent_Conditional_0_For_4_Template, 1, 2, "po-tag", 3, ɵɵrepeaterTrackByIdentity);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵconditional(ctx_r0.title ? 1 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r0.isRemoveAll() ? 2 : -1);
    ɵɵadvance();
    ɵɵrepeater(ctx_r0.disclaimers);
  }
}
var _c61 = ["dropdownRef"];
var _c62 = (a0, a1) => ({
  "po-dropdown-button-disabled": a0,
  "po-dropdown-button-open": a1
});
function PoCheckboxGroupComponent_For_5_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "li")(1, "po-checkbox", 7, 0);
    ɵɵlistener("p-blur", function PoCheckboxGroupComponent_For_5_Template_po_checkbox_p_blur_1_listener() {
      ɵɵrestoreView(_r1);
      const checkboxLabel_r2 = ɵɵreference(2);
      const ctx_r2 = ɵɵnextContext();
      return ɵɵresetView(ctx_r2.onBlur(checkboxLabel_r2));
    })("click", function PoCheckboxGroupComponent_For_5_Template_po_checkbox_click_1_listener() {
      const option_r4 = ɵɵrestoreView(_r1).$implicit;
      const ctx_r2 = ɵɵnextContext();
      return ɵɵresetView(ctx_r2.checkOption(option_r4));
    })("keydown", function PoCheckboxGroupComponent_For_5_Template_po_checkbox_keydown_1_listener($event) {
      const option_r4 = ɵɵrestoreView(_r1).$implicit;
      const checkboxLabel_r2 = ɵɵreference(2);
      const ctx_r2 = ɵɵnextContext();
      return ɵɵresetView(ctx_r2.onKeyDown($event, option_r4, checkboxLabel_r2));
    });
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const option_r4 = ctx.$implicit;
    const ctx_r2 = ɵɵnextContext();
    ɵɵclassMap(ɵɵinterpolate2("po-checkbox-group-item po-md-", ctx_r2.mdColumns, " po-lg-", ctx_r2.columns));
    ɵɵclassProp("po-checkbox-group-item-disabled", option_r4.disabled || ctx_r2.disabled);
    ɵɵadvance();
    ɵɵproperty("p-label", option_r4.label)("p-disabled", option_r4.disabled || ctx_r2.disabled)("p-checkboxValue", ctx_r2.checkedOptions[option_r4.value] === null ? "mixed" : ctx_r2.checkedOptions[option_r4.value])("p-required", ctx_r2.required)("p-label-text-wrap", true)("p-size", ctx_r2.size);
  }
}
var _c63 = ["radio"];
var _c64 = ["radioInput"];
function PoRadioComponent_Conditional_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-label", 5);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("p-text-wrap", ctx_r0.labelTextWrap)("p-disabled", ctx_r0.disabled)("p-label", ctx_r0.label);
  }
}
var _c65 = ["inputRadio"];
function PoRadioGroupComponent_For_6_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div")(1, "po-radio", 8, 1);
    ɵɵlistener("p-blur", function PoRadioGroupComponent_For_6_Template_po_radio_p_blur_1_listener() {
      ɵɵrestoreView(_r1);
      const inputRadio_r2 = ɵɵreference(2);
      const ctx_r2 = ɵɵnextContext();
      return ɵɵresetView(ctx_r2.onBlur(inputRadio_r2));
    })("keydown", function PoRadioGroupComponent_For_6_Template_po_radio_keydown_1_listener($event) {
      ɵɵrestoreView(_r1);
      const inputRadio_r2 = ɵɵreference(2);
      const ctx_r2 = ɵɵnextContext();
      return ɵɵresetView(ctx_r2.onKeyDown($event, inputRadio_r2));
    })("keyup", function PoRadioGroupComponent_For_6_Template_po_radio_keyup_1_listener($event) {
      const option_r4 = ɵɵrestoreView(_r1).$implicit;
      const ctx_r2 = ɵɵnextContext();
      return ɵɵresetView(ctx_r2.onKeyUp($event, option_r4.value));
    })("click", function PoRadioGroupComponent_For_6_Template_po_radio_click_1_listener() {
      const option_r4 = ɵɵrestoreView(_r1).$implicit;
      const ctx_r2 = ɵɵnextContext();
      return ɵɵresetView(ctx_r2.eventClick(option_r4.value, option_r4.disabled === true || ctx_r2.disabled));
    });
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const option_r4 = ctx.$implicit;
    const ctx_r2 = ɵɵnextContext();
    ɵɵclassMap(ɵɵinterpolate2("po-radio-group-item po-md-", ctx_r2.mdColumns, " po-lg-", ctx_r2.columns));
    ɵɵadvance();
    ɵɵproperty("name", ctx_r2.name)("p-checked", ctx_r2.value === option_r4.value)("p-disabled", option_r4.disabled === true || ctx_r2.disabled)("p-label", option_r4.label)("p-label-text-wrap", true)("p-required", ctx_r2.required)("p-size", ctx_r2.size)("p-value", option_r4.value);
  }
}
var _c66 = ["calendar"];
var _c67 = ["dialogPicker"];
var _c68 = ["iconDatepicker"];
var _c69 = ["iconClean"];
function PoDatepickerComponent_Conditional_9_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-loading-icon", 13);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("p-size", ctx_r0.mapSizeToIcon(ctx_r0.size));
  }
}
function PoDatepickerComponent_Conditional_10_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r3 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-clean", 19, 5);
    ɵɵlistener("p-change-event", function PoDatepickerComponent_Conditional_10_Conditional_1_Template_po_clean_p_change_event_0_listener() {
      ɵɵrestoreView(_r3);
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.clear());
    })("keydown", function PoDatepickerComponent_Conditional_10_Conditional_1_Template_po_clean_keydown_0_listener($event) {
      ɵɵrestoreView(_r3);
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.handleCleanKeyboardTab($event));
    })("keydown.enter", function PoDatepickerComponent_Conditional_10_Conditional_1_Template_po_clean_keydown_enter_0_listener($event) {
      ɵɵrestoreView(_r3);
      const ctx_r0 = ɵɵnextContext(2);
      ctx_r0.clearAndFocus();
      return ɵɵresetView($event.preventDefault());
    })("keydown.space", function PoDatepickerComponent_Conditional_10_Conditional_1_Template_po_clean_keydown_space_0_listener($event) {
      ɵɵrestoreView(_r3);
      const ctx_r0 = ɵɵnextContext(2);
      ctx_r0.clearAndFocus();
      return ɵɵresetView($event.preventDefault());
    });
    ɵɵelementEnd();
    ɵɵelement(2, "div", 20);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵproperty("p-element-ref", ctx_r0.inputEl)("p-size", ctx_r0.size);
    ɵɵattribute("aria-label", ctx_r0.literals.clean);
  }
}
function PoDatepickerComponent_Conditional_10_Template(rf, ctx) {
  if (rf & 1) {
    const _r2 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 14);
    ɵɵconditionalCreate(1, PoDatepickerComponent_Conditional_10_Conditional_1_Template, 3, 3);
    ɵɵelementStart(2, "po-button", 18, 4);
    ɵɵlistener("keydown", function PoDatepickerComponent_Conditional_10_Template_po_button_keydown_2_listener($event) {
      ɵɵrestoreView(_r2);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.onKeyPress($event));
    })("keydown.tab", function PoDatepickerComponent_Conditional_10_Template_po_button_keydown_tab_2_listener($event) {
      ɵɵrestoreView(_r2);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.handleCleanKeyboardTab($event));
    })("p-click", function PoDatepickerComponent_Conditional_10_Template_po_button_p_click_2_listener() {
      ɵɵrestoreView(_r2);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.togglePicker(false));
    });
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵconditional(ctx_r0.clean && ctx_r0.dateInputValue && !ctx_r0.isDisabled && !ctx_r0.readonly ? 1 : -1);
    ɵɵadvance();
    ɵɵclassProp("po-button-tertiary-danger", ctx_r0.getErrorPattern());
    ɵɵproperty("p-disabled", ctx_r0.isDisabled || ctx_r0.readonly)("p-size", ctx_r0.size);
  }
}
function PoDatepickerComponent_Conditional_11_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-helper", 15, 6);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    const poHelper_r4 = ɵɵreadContextLet(0);
    ɵɵproperty("p-size", ctx_r0.size)("p-helper", poHelper_r4.helperSettings)("p-disabled", ctx_r0.isDisabled)("p-append-in-body", ctx_r0.appendBox);
  }
}
function PoDatepickerComponent_Conditional_12_ng_template_0_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoDatepickerComponent_Conditional_12_ng_template_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, PoDatepickerComponent_Conditional_12_ng_template_0_ng_container_0_Template, 1, 0, "ng-container", 21);
  }
  if (rf & 2) {
    ɵɵnextContext(2);
    const sharedCalendarContent_r5 = ɵɵreference(15);
    ɵɵproperty("ngTemplateOutlet", sharedCalendarContent_r5);
  }
}
function PoDatepickerComponent_Conditional_12_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, PoDatepickerComponent_Conditional_12_ng_template_0_Template, 1, 1, "ng-template", 16);
  }
  if (rf & 2) {
    ɵɵnextContext();
    const trigger_r6 = ɵɵreference(5);
    ɵɵproperty("cdkConnectedOverlayOrigin", trigger_r6)("cdkConnectedOverlayOpen", true)("cdkConnectedOverlayDisableClose", true);
  }
}
function PoDatepickerComponent_Conditional_13_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoDatepickerComponent_Conditional_13_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, PoDatepickerComponent_Conditional_13_ng_container_0_Template, 1, 0, "ng-container", 21);
  }
  if (rf & 2) {
    ɵɵnextContext();
    const sharedCalendarContent_r5 = ɵɵreference(15);
    ɵɵproperty("ngTemplateOutlet", sharedCalendarContent_r5);
  }
}
function PoDatepickerComponent_ng_template_14_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "div", 23);
  }
}
function PoDatepickerComponent_ng_template_14_Template(rf, ctx) {
  if (rf & 1) {
    const _r7 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 22, 7);
    ɵɵconditionalCreate(2, PoDatepickerComponent_ng_template_14_Conditional_2_Template, 1, 0, "div", 23);
    ɵɵelementStart(3, "po-calendar", 24, 8);
    ɵɵtwoWayListener("ngModelChange", function PoDatepickerComponent_ng_template_14_Template_po_calendar_ngModelChange_3_listener($event) {
      ɵɵrestoreView(_r7);
      const ctx_r0 = ɵɵnextContext();
      ɵɵtwoWayBindingSet(ctx_r0.date, $event) || (ctx_r0.date = $event);
      return ɵɵresetView($event);
    });
    ɵɵlistener("p-change", function PoDatepickerComponent_ng_template_14_Template_po_calendar_p_change_3_listener($event) {
      ɵɵrestoreView(_r7);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.dateSelected($event));
    })("p-change-month-year", function PoDatepickerComponent_ng_template_14_Template_po_calendar_p_change_month_year_3_listener() {
      ɵɵrestoreView(_r7);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.adjustCalendarPosition());
    })("keydown", function PoDatepickerComponent_ng_template_14_Template_po_calendar_keydown_3_listener($event) {
      ɵɵrestoreView(_r7);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.onCalendarKeyDown($event));
    })("p-close", function PoDatepickerComponent_ng_template_14_Template_po_calendar_p_close_3_listener() {
      ɵɵrestoreView(_r7);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.closeCalendar(false));
    });
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵclassProp("po-datepicker-popup-calendar", !ctx_r0.verifyMobile());
    ɵɵproperty("hidden", !ctx_r0.visible);
    ɵɵadvance(2);
    ɵɵconditional(ctx_r0.verifyMobile() ? 2 : -1);
    ɵɵadvance();
    ɵɵclassProp("po-datepicker-calendar-mobile", ctx_r0.verifyMobile());
    ɵɵtwoWayProperty("ngModel", ctx_r0.date);
    ɵɵproperty("p-max-date", ctx_r0.maxDate)("p-min-date", ctx_r0.minDate)("p-locale", ctx_r0.locale)("p-size", ctx_r0.size)("p-mode", ctx_r0.mode)("p-year-range-limit", ctx_r0.yearRangeLimit);
  }
}
function PoDatepickerComponent_Conditional_16_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-field-container-bottom", 17);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("p-append-in-body", ctx_r0.appendBox)("p-help", ctx_r0.help)("p-disabled", ctx_r0.isDisabled)("p-error-pattern", ctx_r0.getErrorPattern())("p-error-limit", ctx_r0.errorLimit)("p-size", ctx_r0.size);
  }
}
var _c70 = ["modalContent"];
var _c71 = ["modalContainer"];
var _c72 = ["*", [["po-modal-footer"]]];
var _c73 = ["*", "po-modal-footer"];
function PoModalComponent_Conditional_0_Conditional_9_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-icon", 10);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵproperty("p-icon", ctx_r1.icon);
  }
}
function PoModalComponent_Conditional_0_Conditional_12_Template(rf, ctx) {
  if (rf & 1) {
    const _r3 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-button", 14);
    ɵɵlistener("p-click", function PoModalComponent_Conditional_0_Conditional_12_Template_po_button_p_click_0_listener() {
      ɵɵrestoreView(_r3);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.close(true));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵproperty("p-aria-label", ctx_r1.literals.close)("p-size", ctx_r1.componentsSize);
  }
}
function PoModalComponent_Conditional_0_Conditional_15_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵprojection(0, 1);
  }
}
function PoModalComponent_Conditional_0_Conditional_16_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    const _r5 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-button", 18);
    ɵɵlistener("p-click", function PoModalComponent_Conditional_0_Conditional_16_Conditional_2_Template_po_button_p_click_0_listener() {
      ɵɵrestoreView(_r5);
      const ctx_r1 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r1.secondaryAction.action());
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵproperty("p-danger", ctx_r1.getSecondaryActionButtonDanger())("p-disabled", ctx_r1.secondaryAction.disabled)("p-icon", ctx_r1.secondaryAction.icon)("p-label", ctx_r1.secondaryAction.label)("p-loading", ctx_r1.secondaryAction.loading)("p-size", ctx_r1.componentsSize);
  }
}
function PoModalComponent_Conditional_0_Conditional_16_Template(rf, ctx) {
  if (rf & 1) {
    const _r4 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-modal-footer")(1, "div", 15);
    ɵɵconditionalCreate(2, PoModalComponent_Conditional_0_Conditional_16_Conditional_2_Template, 1, 6, "po-button", 16);
    ɵɵelementStart(3, "po-button", 17);
    ɵɵlistener("p-click", function PoModalComponent_Conditional_0_Conditional_16_Template_po_button_p_click_3_listener() {
      ɵɵrestoreView(_r4);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.primaryAction.action());
    });
    ɵɵelementEnd()()();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵadvance(2);
    ɵɵconditional(ctx_r1.secondaryAction ? 2 : -1);
    ɵɵadvance();
    ɵɵproperty("p-danger", ctx_r1.primaryAction.danger)("p-disabled", ctx_r1.primaryAction.disabled)("p-icon", ctx_r1.primaryAction.icon)("p-label", ctx_r1.primaryAction.label)("p-loading", ctx_r1.primaryAction.loading)("p-size", ctx_r1.componentsSize);
  }
}
function PoModalComponent_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 3, 0);
    ɵɵlistener("keydown.esc", function PoModalComponent_Conditional_0_Template_div_keydown_esc_0_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.closeModalOnEscapeKey($event));
    });
    ɵɵelementStart(2, "div", 4);
    ɵɵlistener("mousedown", function PoModalComponent_Conditional_0_Template_div_mousedown_2_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onClickOut($event));
    });
    ɵɵelementEnd();
    ɵɵelementStart(3, "div", 5)(4, "div", 6)(5, "div", 7, 1)(7, "div", 8)(8, "div", 9);
    ɵɵconditionalCreate(9, PoModalComponent_Conditional_0_Conditional_9_Template, 1, 1, "po-icon", 10);
    ɵɵelementStart(10, "div", 11);
    ɵɵtext(11);
    ɵɵelementEnd()();
    ɵɵconditionalCreate(12, PoModalComponent_Conditional_0_Conditional_12_Template, 1, 2, "po-button", 12);
    ɵɵelementEnd();
    ɵɵelementStart(13, "div", 13);
    ɵɵprojection(14);
    ɵɵelementEnd();
    ɵɵconditionalCreate(15, PoModalComponent_Conditional_0_Conditional_15_Template, 1, 0)(16, PoModalComponent_Conditional_0_Conditional_16_Template, 4, 7, "po-modal-footer");
    ɵɵelementEnd()()()();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance(5);
    ɵɵclassMap(ɵɵinterpolate1("po-modal-content po-modal-", ctx_r1.size));
    ɵɵadvance(4);
    ɵɵconditional(ctx_r1.icon ? 9 : -1);
    ɵɵadvance(2);
    ɵɵtextInterpolate1(" ", ctx_r1.title, " ");
    ɵɵadvance();
    ɵɵconditional(!ctx_r1.hideClose ? 12 : -1);
    ɵɵadvance(3);
    ɵɵconditional(ctx_r1.modalFooter ? 15 : 16);
  }
}
var _c74 = ["toaster"];
var _c75 = ["buttonClose"];
function PoToasterComponent_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-icon", 4);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("p-icon", ctx_r0.getIcon());
  }
}
function PoToasterComponent_Conditional_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-icon", 5);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("p-icon", ctx_r0.getIcon());
  }
}
function PoToasterComponent_Conditional_8_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 8);
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵtextInterpolate(ctx_r0.supportMessage);
  }
}
function PoToasterComponent_Conditional_10_Template(rf, ctx) {
  if (rf & 1) {
    const _r2 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-button", 12);
    ɵɵlistener("p-click", function PoToasterComponent_Conditional_10_Template_po_button_p_click_0_listener($event) {
      ɵɵrestoreView(_r2);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.poToasterAction($event));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("p-label", ctx_r0.actionLabel)("p-size", ctx_r0.sizeActions);
  }
}
function PoToasterComponent_Conditional_11_Template(rf, ctx) {
  if (rf & 1) {
    const _r3 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 11);
    ɵɵelement(1, "div", 13);
    ɵɵelementStart(2, "po-button", 14, 1);
    ɵɵlistener("p-click", function PoToasterComponent_Conditional_11_Template_po_button_p_click_2_listener($event) {
      ɵɵrestoreView(_r3);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.onButtonClose($event));
    });
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance(2);
    ɵɵproperty("p-size", ctx_r0.sizeActions);
  }
}
var _c76 = ["iconCalendar"];
var _c77 = ["datetimepickerField"];
function PoDatetimepickerComponent_Conditional_12_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 16)(1, "input", 22);
    ɵɵlistener("keydown", function PoDatetimepickerComponent_Conditional_12_Template_input_keydown_1_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onPeriodKeydown($event));
    })("click", function PoDatetimepickerComponent_Conditional_12_Template_input_click_1_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onPeriodClick($event));
    })("focus", function PoDatetimepickerComponent_Conditional_12_Template_input_focus_1_listener() {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onInputFocus());
    })("blur", function PoDatetimepickerComponent_Conditional_12_Template_input_blur_1_listener() {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onPeriodBlur());
    });
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵattribute("data-value", ctx_r1.currentPeriod);
    ɵɵadvance();
    ɵɵproperty("value", ctx_r1.currentPeriod)("disabled", ctx_r1.isDisabled);
    ɵɵattribute("tabindex", ctx_r1.isDisabled || ctx_r1.isReadonly ? -1 : 0)("aria-label", ctx_r1.currentPeriod);
  }
}
function PoDatetimepickerComponent_Conditional_13_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-loading-icon", 17);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("p-size", ctx_r1.mapSizeToIcon(ctx_r1.hostSize));
  }
}
function PoDatetimepickerComponent_Conditional_14_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r4 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-clean", 24, 6);
    ɵɵlistener("p-change-event", function PoDatetimepickerComponent_Conditional_14_Conditional_1_Template_po_clean_p_change_event_0_listener() {
      ɵɵrestoreView(_r4);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.clear());
    })("keydown.enter", function PoDatetimepickerComponent_Conditional_14_Conditional_1_Template_po_clean_keydown_enter_0_listener($event) {
      ɵɵrestoreView(_r4);
      const ctx_r1 = ɵɵnextContext(2);
      ctx_r1.clear(true);
      return ɵɵresetView($event.preventDefault());
    })("keydown.space", function PoDatetimepickerComponent_Conditional_14_Conditional_1_Template_po_clean_keydown_space_0_listener($event) {
      ɵɵrestoreView(_r4);
      const ctx_r1 = ɵɵnextContext(2);
      ctx_r1.clear(true);
      return ɵɵresetView($event.preventDefault());
    });
    ɵɵelementEnd();
    ɵɵelement(2, "div", 25);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵproperty("p-element-ref", ctx_r1.inputEl)("p-size", ctx_r1.hostSize);
    ɵɵattribute("aria-label", ctx_r1.literals.clean);
  }
}
function PoDatetimepickerComponent_Conditional_14_Template(rf, ctx) {
  if (rf & 1) {
    const _r3 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 18);
    ɵɵconditionalCreate(1, PoDatetimepickerComponent_Conditional_14_Conditional_1_Template, 3, 3);
    ɵɵelementStart(2, "po-button", 23, 5);
    ɵɵlistener("p-click", function PoDatetimepickerComponent_Conditional_14_Template_po_button_p_click_2_listener() {
      ɵɵrestoreView(_r3);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.togglePicker(false));
    })("keydown", function PoDatetimepickerComponent_Conditional_14_Template_po_button_keydown_2_listener($event) {
      ɵɵrestoreView(_r3);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.eventOnButtonKeydown($event));
    });
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵconditional(ctx_r1.isClean && ctx_r1.dateTimeInputValue && !ctx_r1.isDisabled && !ctx_r1.isReadonly ? 1 : -1);
    ɵɵadvance();
    ɵɵclassProp("po-button-tertiary-danger", ctx_r1.getErrorPattern());
    ɵɵproperty("p-disabled", ctx_r1.isDisabled || ctx_r1.isReadonly)("p-size", ctx_r1.hostSize);
  }
}
function PoDatetimepickerComponent_Conditional_15_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-helper", 19, 7);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    const poHelper_r5 = ɵɵreadContextLet(0);
    ɵɵproperty("p-size", ctx_r1.hostSize)("p-helper", poHelper_r5.helperSettings)("p-disabled", ctx_r1.isDisabled)("p-append-in-body", ctx_r1.appendBox());
  }
}
function PoDatetimepickerComponent_Conditional_16_ng_template_0_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoDatetimepickerComponent_Conditional_16_ng_template_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, PoDatetimepickerComponent_Conditional_16_ng_template_0_ng_container_0_Template, 1, 0, "ng-container", 26);
  }
  if (rf & 2) {
    ɵɵnextContext(2);
    const sharedCalendarContent_r6 = ɵɵreference(20);
    ɵɵproperty("ngTemplateOutlet", sharedCalendarContent_r6);
  }
}
function PoDatetimepickerComponent_Conditional_16_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, PoDatetimepickerComponent_Conditional_16_ng_template_0_Template, 1, 1, "ng-template", 20);
  }
  if (rf & 2) {
    ɵɵnextContext();
    const trigger_r7 = ɵɵreference(5);
    ɵɵproperty("cdkConnectedOverlayOrigin", trigger_r7)("cdkConnectedOverlayOpen", true)("cdkConnectedOverlayDisableClose", true);
  }
}
function PoDatetimepickerComponent_Conditional_17_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoDatetimepickerComponent_Conditional_17_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, PoDatetimepickerComponent_Conditional_17_ng_container_0_Template, 1, 0, "ng-container", 26);
  }
  if (rf & 2) {
    ɵɵnextContext();
    const sharedCalendarContent_r6 = ɵɵreference(20);
    ɵɵproperty("ngTemplateOutlet", sharedCalendarContent_r6);
  }
}
function PoDatetimepickerComponent_Conditional_18_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-field-container-bottom", 21);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("p-append-in-body", ctx_r1.appendBox())("p-help", ctx_r1.help())("p-disabled", ctx_r1.isDisabled)("p-error-pattern", ctx_r1.getErrorPattern())("p-error-limit", ctx_r1.errorLimit())("p-size", ctx_r1.hostSize);
  }
}
function PoDatetimepickerComponent_ng_template_19_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "div", 28);
  }
}
function PoDatetimepickerComponent_ng_template_19_Template(rf, ctx) {
  if (rf & 1) {
    const _r8 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 27, 8);
    ɵɵconditionalCreate(2, PoDatetimepickerComponent_ng_template_19_Conditional_2_Template, 1, 0, "div", 28);
    ɵɵelementStart(3, "po-calendar", 29, 9);
    ɵɵlistener("p-change", function PoDatetimepickerComponent_ng_template_19_Template_po_calendar_p_change_3_listener($event) {
      ɵɵrestoreView(_r8);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onDateChange($event));
    })("p-change-time", function PoDatetimepickerComponent_ng_template_19_Template_po_calendar_p_change_time_3_listener($event) {
      ɵɵrestoreView(_r8);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onTimeChange($event));
    })("p-close", function PoDatetimepickerComponent_ng_template_19_Template_po_calendar_p_close_3_listener() {
      ɵɵrestoreView(_r8);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onCalendarClose());
    })("p-change-month-year", function PoDatetimepickerComponent_ng_template_19_Template_po_calendar_p_change_month_year_3_listener() {
      ɵɵrestoreView(_r8);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.adjustCalendarPosition());
    })("p-timer-boundary-tab", function PoDatetimepickerComponent_ng_template_19_Template_po_calendar_p_timer_boundary_tab_3_listener($event) {
      ɵɵrestoreView(_r8);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onTimerBoundaryTab($event));
    })("keydown", function PoDatetimepickerComponent_ng_template_19_Template_po_calendar_keydown_3_listener($event) {
      ɵɵrestoreView(_r8);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.eventOnCalendarKeydown($event));
    });
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵclassProp("po-datetimepicker-popup-calendar", !ctx_r1.verifyMobile());
    ɵɵproperty("hidden", !ctx_r1.visible);
    ɵɵadvance(2);
    ɵɵconditional(ctx_r1.verifyMobile() ? 2 : -1);
    ɵɵadvance();
    ɵɵclassProp("po-datetimepicker-calendar-mobile", ctx_r1.verifyMobile());
    ɵɵproperty("p-size", ctx_r1.hostSize)("p-locale", ctx_r1.locale)("p-format", ctx_r1.resolvedTimerFormat)("p-min-date", ctx_r1.resolvedMinDate)("p-max-date", ctx_r1.resolvedMaxDate)("p-max-time", ctx_r1.maxTime())("p-min-time", ctx_r1.minTime())("p-minute-interval", ctx_r1.minuteInterval())("p-second-interval", ctx_r1.secondInterval())("p-show-seconds", ctx_r1.showSeconds());
  }
}
var _c78 = ["iconTimepicker"];
var _c79 = ["minuteInput"];
var _c80 = ["secondInput"];
var _c81 = ["timepickerField"];
var _c82 = ["periodInput"];
function PoTimepickerComponent_Conditional_17_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 19);
    ɵɵtext(1, " : ");
    ɵɵelementEnd();
    ɵɵelementStart(2, "div", 17)(3, "input", 20, 6);
    ɵɵlistener("input", function PoTimepickerComponent_Conditional_17_Template_input_input_3_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onSegmentInput($event, "second"));
    })("blur", function PoTimepickerComponent_Conditional_17_Template_input_blur_3_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onSegmentBlur($event));
    })("focus", function PoTimepickerComponent_Conditional_17_Template_input_focus_3_listener() {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onSegmentFocus());
    })("click", function PoTimepickerComponent_Conditional_17_Template_input_click_3_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.eventOnClick($event));
    })("keydown", function PoTimepickerComponent_Conditional_17_Template_input_keydown_3_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onSegmentKeydown($event, "second"));
    });
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵclassProp("po-timepicker-field-separator-hidden", !ctx_r1.showSeparator);
    ɵɵadvance(2);
    ɵɵattribute("data-value", ctx_r1.secondDisplay || ctx_r1.secondPlaceholder || "  ");
    ɵɵadvance();
    ɵɵproperty("disabled", ctx_r1.isDisabled)("readonly", ctx_r1.readonly)("placeholder", ctx_r1.isDisabled ? "" : ctx_r1.secondPlaceholder);
    ɵɵattribute("aria-label", ctx_r1.literals.second)("aria-valuenow", ctx_r1.secondDisplay ? +ctx_r1.secondDisplay : null)("aria-valuetext", ctx_r1.secondDisplay || null);
  }
}
function PoTimepickerComponent_Conditional_18_Template(rf, ctx) {
  if (rf & 1) {
    const _r3 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 21)(1, "input", 28, 7);
    ɵɵlistener("keydown", function PoTimepickerComponent_Conditional_18_Template_input_keydown_1_listener($event) {
      ɵɵrestoreView(_r3);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onPeriodSegmentKeydown($event));
    })("click", function PoTimepickerComponent_Conditional_18_Template_input_click_1_listener($event) {
      ɵɵrestoreView(_r3);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onPeriodSegmentClick($event));
    })("blur", function PoTimepickerComponent_Conditional_18_Template_input_blur_1_listener($event) {
      ɵɵrestoreView(_r3);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onSegmentBlur($event));
    })("focus", function PoTimepickerComponent_Conditional_18_Template_input_focus_1_listener() {
      ɵɵrestoreView(_r3);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onSegmentFocus());
    });
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵattribute("data-value", ctx_r1.periodDisplay || "AM");
    ɵɵadvance();
    ɵɵproperty("value", ctx_r1.periodDisplay || "AM")("disabled", ctx_r1.isDisabled);
    ɵɵattribute("tabindex", ctx_r1.isDisabled || ctx_r1.readonly ? -1 : 0)("aria-label", ctx_r1.periodDisplay || "AM");
  }
}
function PoTimepickerComponent_Conditional_19_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-loading-icon", 22);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("p-size", ctx_r1.mapSizeToIcon(ctx_r1.size));
  }
}
function PoTimepickerComponent_Conditional_20_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r5 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-clean", 30, 9);
    ɵɵlistener("p-change-event", function PoTimepickerComponent_Conditional_20_Conditional_1_Template_po_clean_p_change_event_0_listener() {
      ɵɵrestoreView(_r5);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.clear());
    })("keydown", function PoTimepickerComponent_Conditional_20_Conditional_1_Template_po_clean_keydown_0_listener($event) {
      ɵɵrestoreView(_r5);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.handleCleanKeyboardTab($event));
    })("keydown.enter", function PoTimepickerComponent_Conditional_20_Conditional_1_Template_po_clean_keydown_enter_0_listener($event) {
      ɵɵrestoreView(_r5);
      const ctx_r1 = ɵɵnextContext(2);
      ctx_r1.clearAndFocus();
      return ɵɵresetView($event.preventDefault());
    })("keydown.space", function PoTimepickerComponent_Conditional_20_Conditional_1_Template_po_clean_keydown_space_0_listener($event) {
      ɵɵrestoreView(_r5);
      const ctx_r1 = ɵɵnextContext(2);
      ctx_r1.clearAndFocus();
      return ɵɵresetView($event.preventDefault());
    });
    ɵɵelementEnd();
    ɵɵelement(2, "div", 31);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵproperty("p-element-ref", ctx_r1.cleanElementRef)("p-size", ctx_r1.size);
    ɵɵattribute("aria-label", ctx_r1.literals.clean);
  }
}
function PoTimepickerComponent_Conditional_20_Template(rf, ctx) {
  if (rf & 1) {
    const _r4 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 23);
    ɵɵconditionalCreate(1, PoTimepickerComponent_Conditional_20_Conditional_1_Template, 3, 3);
    ɵɵelementStart(2, "po-button", 29, 8);
    ɵɵlistener("keydown", function PoTimepickerComponent_Conditional_20_Template_po_button_keydown_2_listener($event) {
      ɵɵrestoreView(_r4);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onKeyPress($event));
    })("keydown.tab", function PoTimepickerComponent_Conditional_20_Template_po_button_keydown_tab_2_listener($event) {
      ɵɵrestoreView(_r4);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.handleTimepickerButtonKeyboardTab($event));
    })("p-click", function PoTimepickerComponent_Conditional_20_Template_po_button_p_click_2_listener() {
      ɵɵrestoreView(_r4);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.togglePicker(false));
    });
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵconditional(ctx_r1.clean && ctx_r1.hasValue() && !ctx_r1.isDisabled && !ctx_r1.readonly ? 1 : -1);
    ɵɵadvance();
    ɵɵclassProp("po-button-tertiary-danger", ctx_r1.getErrorPattern());
    ɵɵproperty("p-aria-label", ctx_r1.literals.open)("p-disabled", ctx_r1.isDisabled || ctx_r1.readonly)("p-size", ctx_r1.size);
  }
}
function PoTimepickerComponent_Conditional_21_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-helper", 24, 10);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    const poHelper_r6 = ɵɵreadContextLet(0);
    ɵɵproperty("p-size", ctx_r1.size)("p-helper", poHelper_r6.helperSettings)("p-disabled", ctx_r1.isDisabled)("p-append-in-body", ctx_r1.appendBox);
  }
}
function PoTimepickerComponent_Conditional_22_ng_template_0_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoTimepickerComponent_Conditional_22_ng_template_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, PoTimepickerComponent_Conditional_22_ng_template_0_ng_container_0_Template, 1, 0, "ng-container", 32);
  }
  if (rf & 2) {
    ɵɵnextContext(2);
    const sharedTimerContent_r7 = ɵɵreference(25);
    ɵɵproperty("ngTemplateOutlet", sharedTimerContent_r7);
  }
}
function PoTimepickerComponent_Conditional_22_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, PoTimepickerComponent_Conditional_22_ng_template_0_Template, 1, 1, "ng-template", 25);
  }
  if (rf & 2) {
    ɵɵnextContext();
    const trigger_r8 = ɵɵreference(5);
    ɵɵproperty("cdkConnectedOverlayOrigin", trigger_r8)("cdkConnectedOverlayOpen", true)("cdkConnectedOverlayDisableClose", true);
  }
}
function PoTimepickerComponent_Conditional_23_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoTimepickerComponent_Conditional_23_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, PoTimepickerComponent_Conditional_23_ng_container_0_Template, 1, 0, "ng-container", 32);
  }
  if (rf & 2) {
    ɵɵnextContext();
    const sharedTimerContent_r7 = ɵɵreference(25);
    ɵɵproperty("ngTemplateOutlet", sharedTimerContent_r7);
  }
}
function PoTimepickerComponent_ng_template_24_Conditional_0_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "div", 35);
  }
}
function PoTimepickerComponent_ng_template_24_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    const _r9 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 34, 11);
    ɵɵconditionalCreate(2, PoTimepickerComponent_ng_template_24_Conditional_0_Conditional_2_Template, 1, 0, "div", 35);
    ɵɵelementStart(3, "po-timer", 36, 12);
    ɵɵlistener("p-change", function PoTimepickerComponent_ng_template_24_Conditional_0_Template_po_timer_p_change_3_listener($event) {
      ɵɵrestoreView(_r9);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.timerSelected($event));
    })("p-boundary-tab", function PoTimepickerComponent_ng_template_24_Conditional_0_Template_po_timer_p_boundary_tab_3_listener($event) {
      ɵɵrestoreView(_r9);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.onTimerBoundaryTab($event));
    })("keydown", function PoTimepickerComponent_ng_template_24_Conditional_0_Template_po_timer_keydown_3_listener($event) {
      ɵɵrestoreView(_r9);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.onTimerKeyDown($event));
    })("focusout", function PoTimepickerComponent_ng_template_24_Conditional_0_Template_po_timer_focusout_3_listener($event) {
      ɵɵrestoreView(_r9);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.onTimerFocusOut($event));
    });
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵclassProp("po-timepicker-popup-timer", !ctx_r1.verifyMobile());
    ɵɵadvance(2);
    ɵɵconditional(ctx_r1.verifyMobile() ? 2 : -1);
    ɵɵadvance();
    ɵɵclassProp("po-timepicker-timer-mobile", ctx_r1.verifyMobile());
    ɵɵproperty("p-format", ctx_r1.format)("p-locale", ctx_r1.locale)("p-show-seconds", ctx_r1.showSeconds)("p-minute-interval", ctx_r1.minuteInterval)("p-second-interval", ctx_r1.secondInterval)("p-min-time", ctx_r1.minTime)("p-max-time", ctx_r1.maxTime)("p-size", ctx_r1.size)("p-value", ctx_r1.timeValue);
  }
}
function PoTimepickerComponent_ng_template_24_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵconditionalCreate(0, PoTimepickerComponent_ng_template_24_Conditional_0_Template, 5, 14, "div", 33);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵconditional(ctx_r1.visible ? 0 : -1);
  }
}
function PoTimepickerComponent_Conditional_28_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-field-container-bottom", 27);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("p-append-in-body", ctx_r1.appendBox)("p-help", ctx_r1.help)("p-disabled", ctx_r1.isDisabled)("p-error-pattern", ctx_r1.getErrorPattern())("p-error-limit", ctx_r1.errorLimit)("p-size", ctx_r1.size);
  }
}
function PoProgressBarComponent_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 0);
    ɵɵelement(1, "div", 2)(2, "div", 3);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵattribute("aria-label", ctx_r0.ariaLabel)("aria-valuenow", ctx_r0.value);
    ɵɵadvance(2);
    ɵɵstyleProp("left", "-" + (100 - ctx_r0.value) + "%");
  }
}
function PoProgressBarComponent_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 1);
    ɵɵelement(1, "div", 4);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵattribute("aria-label", ctx_r0.ariaLabel);
  }
}
function PoProgressCircleComponent_Conditional_0_Conditional_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵdomElementStart(0, "span", 6);
    ɵɵtext(1);
    ɵɵdomElementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵadvance();
    ɵɵtextInterpolate1("", ctx_r0.value(), "%");
  }
}
function PoProgressCircleComponent_Conditional_0_Conditional_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵdomElementStart(0, "span");
    ɵɵdomElement(1, "span", 8);
    ɵɵdomElementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵclassMap(ɵɵinterpolate1("po-progress-circle-info po-progress-circle-info-error po-progress-circle-info-error-", ctx_r0.size()));
  }
}
function PoProgressCircleComponent_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵdomElementStart(0, "div", 2);
    ɵɵnamespaceSVG();
    ɵɵdomElementStart(1, "svg", 3);
    ɵɵdomElement(2, "circle", 4)(3, "circle", 5);
    ɵɵdomElementEnd();
    ɵɵconditionalCreate(4, PoProgressCircleComponent_Conditional_0_Conditional_4_Template, 2, 1, "span", 6);
    ɵɵconditionalCreate(5, PoProgressCircleComponent_Conditional_0_Conditional_5_Template, 2, 3, "span", 7);
    ɵɵdomElementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵstyleProp("max-height", ctx_r0.naturalSize())("max-width", ctx_r0.naturalSize());
    ɵɵattribute("aria-valuenow", ctx_r0.value())("aria-label", ctx_r0.ariaLabel());
    ɵɵadvance();
    ɵɵattribute("viewBox", "0 0 " + ctx_r0.viewBoxSize() + " " + ctx_r0.viewBoxSize())("width", ctx_r0.viewBoxSize())("height", ctx_r0.viewBoxSize());
    ɵɵadvance();
    ɵɵattribute("cx", ctx_r0.center())("cy", ctx_r0.center())("r", ctx_r0.effectiveRadius());
    ɵɵadvance();
    ɵɵattribute("cx", ctx_r0.center())("cy", ctx_r0.center())("r", ctx_r0.effectiveRadius())("stroke-dasharray", ctx_r0.circumference())("stroke-dashoffset", ctx_r0.dashOffset());
    ɵɵadvance();
    ɵɵconditional(ctx_r0.showPercentage() && ctx_r0.status() !== "error" ? 4 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r0.status() === "error" ? 5 : -1);
  }
}
function PoProgressCircleComponent_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵdomElementStart(0, "div", 1)(1, "div", 9);
    ɵɵnamespaceSVG();
    ɵɵdomElementStart(2, "svg", 3);
    ɵɵdomElement(3, "circle", 4)(4, "circle", 5);
    ɵɵdomElementEnd()()();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵattribute("aria-label", ctx_r0.ariaLabel());
    ɵɵadvance();
    ɵɵstyleProp("max-width", ctx_r0.naturalSize())("max-height", ctx_r0.naturalSize());
    ɵɵadvance();
    ɵɵattribute("viewBox", "0 0 " + ctx_r0.viewBoxSize() + " " + ctx_r0.viewBoxSize())("width", ctx_r0.viewBoxSize())("height", ctx_r0.viewBoxSize());
    ɵɵadvance();
    ɵɵattribute("cx", ctx_r0.center())("cy", ctx_r0.center())("r", ctx_r0.effectiveRadius());
    ɵɵadvance();
    ɵɵattribute("cx", ctx_r0.center())("cy", ctx_r0.center())("r", ctx_r0.effectiveRadius())("pathLength", 100);
  }
}
function PoProgressComponent_Conditional_1_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 2);
    ɵɵelement(1, "po-label", 13);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵadvance();
    ɵɵproperty("p-label", ctx_r0.text);
  }
}
function PoProgressComponent_Conditional_1_Conditional_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-icon", 14);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵclassProp("po-progress-info-icon-error", ctx_r0.status === "error");
    ɵɵproperty("p-icon", ctx_r0.infoIcon);
  }
}
function PoProgressComponent_Conditional_1_Conditional_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-icon", 7);
  }
}
function PoProgressComponent_Conditional_1_Conditional_6_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span", 15);
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵclassProp("po-progress-info-text-error", ctx_r0.status === "error");
    ɵɵadvance();
    ɵɵtextInterpolate(ctx_r0.info);
  }
}
function PoProgressComponent_Conditional_1_Conditional_8_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span");
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵadvance();
    ɵɵtextInterpolate1("", ctx_r0.value, "%");
  }
}
function PoProgressComponent_Conditional_1_Conditional_9_Template(rf, ctx) {
  if (rf & 1) {
    const _r2 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-button", 16);
    ɵɵlistener("p-click", function PoProgressComponent_Conditional_1_Conditional_9_Template_po_button_p_click_0_listener() {
      ɵɵrestoreView(_r2);
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.emitRetry());
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵproperty("p-aria-label", ctx_r0.literals.retry)("p-size", ctx_r0.sizeActions);
  }
}
function PoProgressComponent_Conditional_1_Conditional_10_Template(rf, ctx) {
  if (rf & 1) {
    const _r3 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-button", 17);
    ɵɵlistener("p-click", function PoProgressComponent_Conditional_1_Conditional_10_Template_po_button_p_click_0_listener() {
      ɵɵrestoreView(_r3);
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.callAction());
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵproperty("p-danger", ctx_r0.customAction.type === "danger")("p-disabled", ctx_r0.actionIsDisabled(ctx_r0.customAction))("p-label", ctx_r0.customAction.label || "")("p-icon", ctx_r0.customAction.icon)("p-size", ctx_r0.sizeActions);
  }
}
function PoProgressComponent_Conditional_1_Conditional_11_Template(rf, ctx) {
  if (rf & 1) {
    const _r4 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-button", 18);
    ɵɵlistener("p-click", function PoProgressComponent_Conditional_1_Conditional_11_Template_po_button_p_click_0_listener() {
      ɵɵrestoreView(_r4);
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.emitCancellation());
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵproperty("p-disabled", ctx_r0.disabledCancel)("p-aria-label", ctx_r0.literals.cancel)("p-danger", true)("p-size", ctx_r0.sizeActions);
  }
}
function PoProgressComponent_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵconditionalCreate(0, PoProgressComponent_Conditional_1_Conditional_0_Template, 2, 1, "div", 2);
    ɵɵelement(1, "po-progress-bar", 3);
    ɵɵelementStart(2, "div", 4)(3, "div", 5);
    ɵɵconditionalCreate(4, PoProgressComponent_Conditional_1_Conditional_4_Template, 1, 3, "po-icon", 6);
    ɵɵconditionalCreate(5, PoProgressComponent_Conditional_1_Conditional_5_Template, 1, 0, "po-icon", 7);
    ɵɵconditionalCreate(6, PoProgressComponent_Conditional_1_Conditional_6_Template, 2, 3, "span", 8);
    ɵɵelementEnd();
    ɵɵelementStart(7, "div", 9);
    ɵɵconditionalCreate(8, PoProgressComponent_Conditional_1_Conditional_8_Template, 2, 1, "span");
    ɵɵconditionalCreate(9, PoProgressComponent_Conditional_1_Conditional_9_Template, 1, 2, "po-button", 10);
    ɵɵconditionalCreate(10, PoProgressComponent_Conditional_1_Conditional_10_Template, 1, 5, "po-button", 11);
    ɵɵconditionalCreate(11, PoProgressComponent_Conditional_1_Conditional_11_Template, 1, 4, "po-button", 12);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵconditional(ctx_r0.text ? 0 : -1);
    ɵɵadvance();
    ɵɵclassMap(ɵɵinterpolate1("po-progress-bar po-progress-bar-", ctx_r0.size));
    ɵɵproperty("p-aria-label", ctx_r0.ariaLabel || ctx_r0.text)("p-indeterminate", ctx_r0.indeterminate)("p-value", ctx_r0.value);
    ɵɵadvance(3);
    ɵɵconditional(ctx_r0.infoIcon ? 4 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r0.isAllowInfoError ? 5 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r0.info ? 6 : -1);
    ɵɵadvance(2);
    ɵɵconditional(ctx_r0.showPercentage && !ctx_r0.indeterminate ? 8 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r0.isAllowRetry ? 9 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r0.isActionVisible(ctx_r0.customAction) ? 10 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r0.isAllowCancel ? 11 : -1);
  }
}
function PoProgressComponent_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-progress-circle", 19);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵclassMap(ɵɵinterpolate1("po-progress-circle po-progress-circle-", ctx_r0.size));
    ɵɵproperty("p-aria-label", ctx_r0.ariaLabel || ctx_r0.text)("p-indeterminate", ctx_r0.indeterminate)("p-value", ctx_r0.value)("p-size", ctx_r0.size)("p-status", ctx_r0.status)("p-show-percentage", ctx_r0.showPercentage)("p-radius", ctx_r0.radius())("p-parent-size", ctx_r0.parentSize());
  }
}
var _c83 = ["pageContent"];
var _c84 = ["*", [["po-page-slide-footer"]]];
var _c85 = ["*", "po-page-slide-footer"];
var _c86 = (a0) => ({
  "width": a0
});
function PoPageSlideComponent_Conditional_0_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r3 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 12);
    ɵɵlistener("mousedown", function PoPageSlideComponent_Conditional_0_Conditional_1_Template_div_mousedown_0_listener($event) {
      ɵɵrestoreView(_r3);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.onClickOut($event));
    });
    ɵɵelementEnd();
  }
}
function PoPageSlideComponent_Conditional_0_Conditional_9_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 8);
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵadvance();
    ɵɵtextInterpolate(ctx_r1.subtitle);
  }
}
function PoPageSlideComponent_Conditional_0_Conditional_10_Template(rf, ctx) {
  if (rf & 1) {
    const _r4 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-button", 13);
    ɵɵlistener("p-click", function PoPageSlideComponent_Conditional_0_Conditional_10_Template_po_button_p_click_0_listener() {
      ɵɵrestoreView(_r4);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.close());
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵproperty("p-aria-label", ctx_r1.buttonAriaLabel)("p-size", ctx_r1.componentsSize);
  }
}
function PoPageSlideComponent_Conditional_0_Conditional_13_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 11);
    ɵɵelement(1, "po-divider");
    ɵɵprojection(2, 1);
    ɵɵelementEnd();
  }
}
function PoPageSlideComponent_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 2);
    ɵɵlistener("keydown.esc", function PoPageSlideComponent_Conditional_0_Template_div_keydown_esc_0_listener() {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.close());
    });
    ɵɵconditionalCreate(1, PoPageSlideComponent_Conditional_0_Conditional_1_Template, 1, 0, "div", 3);
    ɵɵelementStart(2, "div", 4)(3, "div", 5, 0)(5, "div", 6)(6, "div", 7)(7, "span");
    ɵɵtext(8);
    ɵɵelementEnd();
    ɵɵconditionalCreate(9, PoPageSlideComponent_Conditional_0_Conditional_9_Template, 2, 1, "div", 8);
    ɵɵelementEnd();
    ɵɵconditionalCreate(10, PoPageSlideComponent_Conditional_0_Conditional_10_Template, 1, 2, "po-button", 9);
    ɵɵelementEnd();
    ɵɵelementStart(11, "div", 10);
    ɵɵprojection(12);
    ɵɵelementEnd();
    ɵɵconditionalCreate(13, PoPageSlideComponent_Conditional_0_Conditional_13_Template, 3, 0, "div", 11);
    ɵɵelementEnd()()();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("@fade", ctx_r1.fadeParams);
    ɵɵadvance();
    ɵɵconditional(ctx_r1.size !== "full" ? 1 : -1);
    ɵɵadvance();
    ɵɵclassMap(ɵɵinterpolate1("po-page-slide-container po-page-slide-right po-page-slide-", ctx_r1.size));
    ɵɵproperty("@slide", ctx_r1.slideParams)("ngStyle", ɵɵpureFunction1(14, _c86, ctx_r1.flexibleWidth ? "" : ctx_r1.size === "auto" ? "auto" : ""));
    ɵɵattribute("aria-labelledby", ctx_r1.title ? ctx_r1.titleId : null)("aria-label", ctx_r1.title ? null : ctx_r1.defaultDialogLabel);
    ɵɵadvance(5);
    ɵɵattribute("id", ctx_r1.titleId);
    ɵɵadvance();
    ɵɵtextInterpolate(ctx_r1.title);
    ɵɵadvance();
    ɵɵconditional(ctx_r1.subtitle ? 9 : -1);
    ɵɵadvance();
    ɵɵconditional(!ctx_r1.hideClose ? 10 : -1);
    ɵɵadvance(3);
    ɵɵconditional(ctx_r1.pageSlideFooter ? 13 : -1);
  }
}
var _c87 = (a0, a1) => ({
  "po-clickable": a0,
  "po-table-icon-disabled": a1
});
function PoTableColumnIconComponent_po_table_icon_0_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-table-icon", 1);
    ɵɵlistener("p-click", function PoTableColumnIconComponent_po_table_icon_0_Template_po_table_icon_p_click_0_listener($event) {
      const column_r2 = ɵɵrestoreView(_r1).$implicit;
      const ctx_r2 = ɵɵnextContext();
      return ɵɵresetView(ctx_r2.click(column_r2, $event));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const column_r2 = ctx.$implicit;
    const ctx_r2 = ɵɵnextContext();
    ɵɵproperty("p-clickable", ctx_r2.isClickable(column_r2))("p-color", ctx_r2.getColor(column_r2))("p-disabled", ctx_r2.isDisabled(column_r2))("p-icon", ctx_r2.getIcon(column_r2))("p-icon-tooltip", column_r2.tooltip);
  }
}
function PoTableColumnLabelComponent_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-tag", 0);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("p-color", ctx_r0.value == null ? null : ctx_r0.value.color)("p-value", ctx_r0.value == null ? null : ctx_r0.value.label)("p-text-color", ctx_r0.value == null ? null : ctx_r0.value.textColor)("p-icon", ctx_r0.value == null ? null : ctx_r0.value.icon)("p-type", ctx_r0.value == null ? null : ctx_r0.value.type);
  }
}
var _c88 = (a0) => ({
  "po-table-link-disabled": a0
});
function PoTableColumnLinkComponent_Case_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-link", 3);
    ɵɵlistener("p-action", function PoTableColumnLinkComponent_Case_1_Template_po_link_p_action_0_listener() {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.action == null ? null : ctx_r1.action(ctx_r1.value, ctx_r1.row));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("p-label", ctx_r1.value)("p-open-new-tab", ctx_r1.openNewTab);
  }
}
function PoTableColumnLinkComponent_Case_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-link", 2);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("p-label", ctx_r1.value)("p-open-new-tab", ctx_r1.openNewTab)("p-url", ctx_r1.link);
  }
}
function PoTableColumnLinkComponent_Case_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-link", 2);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("p-label", ctx_r1.value)("p-open-new-tab", ctx_r1.openNewTab)("p-url", ctx_r1.link);
  }
}
var _c89 = ["switchContainer"];
function PoSwitchComponent_Conditional_6_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 6);
    ɵɵelement(1, "po-loading-icon", 11);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵproperty("p-size", ctx_r0.mapSizeToIcon(ctx_r0.size));
  }
}
function PoSwitchComponent_Conditional_7_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-icon", 13);
  }
}
function PoSwitchComponent_Conditional_7_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 7)(1, "div", 12);
    ɵɵconditionalCreate(2, PoSwitchComponent_Conditional_7_Conditional_2_Template, 1, 0, "po-icon", 13);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵattribute("aria-label", ctx_r0.value === true ? ctx_r0.labelOn : ctx_r0.labelOff);
    ɵɵadvance();
    ɵɵconditional(ctx_r0.value === true ? 2 : -1);
  }
}
function PoSwitchComponent_Conditional_8_Template(rf, ctx) {
  if (rf & 1) {
    const _r2 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-label", 14);
    ɵɵlistener("click", function PoSwitchComponent_Conditional_8_Template_po_label_click_0_listener() {
      ɵɵrestoreView(_r2);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.eventClick());
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("p-disabled", ctx_r0.isDisabled)("p-label", ctx_r0.value === true ? ctx_r0.labelOn : ctx_r0.labelOff);
  }
}
function PoSwitchComponent_Conditional_9_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-helper", 9, 1);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    const poHelper_r3 = ɵɵreadContextLet(0);
    ɵɵproperty("p-size", ctx_r0.size)("p-helper", poHelper_r3.helperSettings)("p-disabled", ctx_r0.isDisabled)("p-append-in-body", ctx_r0.appendBox);
  }
}
function PoTableListManagerComponent_Conditional_0_For_4_Conditional_0_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoTableListManagerComponent_Conditional_0_For_4_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "li", 4);
    ɵɵtemplate(1, PoTableListManagerComponent_Conditional_0_For_4_Conditional_0_ng_container_1_Template, 1, 0, "ng-container", 5);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const option_r1 = ɵɵnextContext().$implicit;
    const ctx_r1 = ɵɵnextContext(2);
    const listItemsManagerDefault_r3 = ɵɵreference(7);
    ɵɵclassProp("po-checkbox-group-item-disabled", option_r1.disabled || ctx_r1.disabled);
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", listItemsManagerDefault_r3)("ngTemplateOutletContext", ɵɵpureFunction1(4, _c21, option_r1));
  }
}
function PoTableListManagerComponent_Conditional_0_For_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵconditionalCreate(0, PoTableListManagerComponent_Conditional_0_For_4_Conditional_0_Template, 2, 6, "li", 3);
  }
  if (rf & 2) {
    const option_r1 = ctx.$implicit;
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵconditional(ctx_r1.isFixed(option_r1) ? 0 : -1);
  }
}
function PoTableListManagerComponent_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "section", 1)(1, "p", 2);
    ɵɵtext(2);
    ɵɵelementEnd();
    ɵɵrepeaterCreate(3, PoTableListManagerComponent_Conditional_0_For_4_Template, 1, 1, null, null, ɵɵcomponentInstance().trackByFn, true);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance(2);
    ɵɵtextInterpolate(ctx_r1.literals.fixedColumns);
    ɵɵadvance();
    ɵɵrepeater(ctx_r1.checkboxGroupOptionsView);
  }
}
function PoTableListManagerComponent_For_5_Conditional_0_Conditional_1_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoTableListManagerComponent_For_5_Conditional_0_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, PoTableListManagerComponent_For_5_Conditional_0_Conditional_1_ng_container_0_Template, 1, 0, "ng-container", 5);
  }
  if (rf & 2) {
    const option_r4 = ɵɵnextContext(2).$implicit;
    ɵɵnextContext();
    const listItemsManagerDefault_r3 = ɵɵreference(7);
    ɵɵproperty("ngTemplateOutlet", listItemsManagerDefault_r3)("ngTemplateOutletContext", ɵɵpureFunction1(2, _c21, option_r4));
  }
}
function PoTableListManagerComponent_For_5_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "li", 4);
    ɵɵconditionalCreate(1, PoTableListManagerComponent_For_5_Conditional_0_Conditional_1_Template, 1, 4, "ng-container");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const option_r4 = ɵɵnextContext().$implicit;
    const ctx_r1 = ɵɵnextContext();
    ɵɵclassProp("po-checkbox-group-item-disabled", option_r4.disabled || ctx_r1.disabled);
    ɵɵadvance();
    ɵɵconditional(!ctx_r1.isFixed(option_r4) ? 1 : -1);
  }
}
function PoTableListManagerComponent_For_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵconditionalCreate(0, PoTableListManagerComponent_For_5_Conditional_0_Template, 2, 3, "li", 3);
  }
  if (rf & 2) {
    const option_r4 = ctx.$implicit;
    const ctx_r1 = ɵɵnextContext();
    ɵɵconditional(!ctx_r1.isFixed(option_r4) ? 0 : -1);
  }
}
function PoTableListManagerComponent_ng_template_6_Conditional_6_Template(rf, ctx) {
  if (rf & 1) {
    const _r7 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-button", 14);
    ɵɵlistener("p-click", function PoTableListManagerComponent_ng_template_6_Conditional_6_Template_po_button_p_click_0_listener() {
      ɵɵrestoreView(_r7);
      const option_r6 = ɵɵnextContext().$implicit;
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.emitFixed(option_r6));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const option_r6 = ɵɵnextContext().$implicit;
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("p-icon", ctx_r1.isFixed(option_r6) ? "ICON_PUSH_PIN_SLASH" : "ICON_PUSH_PIN")("p-disabled", !option_r6.visible || ctx_r1.checksIfHasFiveFixed(option_r6))("p-size", ctx_r1.componentsSize);
  }
}
function PoTableListManagerComponent_ng_template_6_Template(rf, ctx) {
  if (rf & 1) {
    const _r5 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 6)(1, "div", 7)(2, "po-switch", 8);
    ɵɵlistener("p-change", function PoTableListManagerComponent_ng_template_6_Template_po_switch_p_change_2_listener() {
      const option_r6 = ɵɵrestoreView(_r5).$implicit;
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.clickSwitch(option_r6));
    })("keydown", function PoTableListManagerComponent_ng_template_6_Template_po_switch_keydown_2_listener($event) {
      const option_r6 = ɵɵrestoreView(_r5).$implicit;
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onKeyDown($event, option_r6));
    });
    ɵɵelementEnd();
    ɵɵelementStart(3, "span", 9);
    ɵɵtext(4);
    ɵɵelementEnd()();
    ɵɵelementStart(5, "div", 10);
    ɵɵconditionalCreate(6, PoTableListManagerComponent_ng_template_6_Conditional_6_Template, 1, 3, "po-button", 11);
    ɵɵelementStart(7, "po-button", 12);
    ɵɵlistener("p-click", function PoTableListManagerComponent_ng_template_6_Template_po_button_p_click_7_listener() {
      const option_r6 = ɵɵrestoreView(_r5).$implicit;
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.emitChangePosition(option_r6, "up"));
    });
    ɵɵelementEnd();
    ɵɵelementStart(8, "po-button", 13);
    ɵɵlistener("p-click", function PoTableListManagerComponent_ng_template_6_Template_po_button_p_click_8_listener() {
      const option_r6 = ɵɵrestoreView(_r5).$implicit;
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.emitChangePosition(option_r6, "down"));
    });
    ɵɵelementEnd()()();
  }
  if (rf & 2) {
    const option_r6 = ctx.$implicit;
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance(2);
    ɵɵproperty("p-disabled", option_r6.disabled || ctx_r1.disabled || ctx_r1.isFixed(option_r6))("p-value", ctx_r1.checkedOptions[option_r6.value])("p-hide-label-status", true)("p-size", ctx_r1.componentsSize);
    ɵɵadvance();
    ɵɵclassProp("po-table-list-manager-item-text-aa", ctx_r1.componentsSize === "small");
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", option_r6.label, " ");
    ɵɵadvance(2);
    ɵɵconditional(!ctx_r1.hideActionFixedColumns ? 6 : -1);
    ɵɵadvance();
    ɵɵproperty("p-tooltip", ctx_r1.literals.up)("p-disabled", ctx_r1.verifyArrowDisabled(option_r6, "up") || ctx_r1.isFixed(option_r6))("p-size", ctx_r1.componentsSize);
    ɵɵadvance();
    ɵɵproperty("p-tooltip", ctx_r1.literals.down)("p-disabled", ctx_r1.verifyArrowDisabled(option_r6, "down") || ctx_r1.isFixed(option_r6))("p-size", ctx_r1.componentsSize);
  }
}
var _c90 = ["pageSlideColumnsManager"];
var _c91 = ["fieldContainer"];
var _c92 = ["iconSearchAi"];
function PoSearchAiComponent_Conditional_6_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "div", 9);
  }
}
function PoSearchAiComponent_Conditional_11_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 13);
    ɵɵelement(1, "po-loading-icon", 16);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵproperty("p-size", ctx_r0.mapSizeToIcon(ctx_r0.size));
  }
}
function PoSearchAiComponent_Conditional_12_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 13);
    ɵɵelement(1, "po-loading-icon", 17);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵproperty("p-size", ctx_r0.mapSizeToIcon(ctx_r0.size));
  }
}
function PoSearchAiComponent_Conditional_13_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r3 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-clean", 19, 3);
    ɵɵlistener("p-change-event", function PoSearchAiComponent_Conditional_13_Conditional_1_Template_po_clean_p_change_event_0_listener() {
      ɵɵrestoreView(_r3);
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.clearSearch());
    })("keydown", function PoSearchAiComponent_Conditional_13_Conditional_1_Template_po_clean_keydown_0_listener($event) {
      ɵɵrestoreView(_r3);
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.handleCleanKeydown($event));
    })("keydown.enter", function PoSearchAiComponent_Conditional_13_Conditional_1_Template_po_clean_keydown_enter_0_listener($event) {
      ɵɵrestoreView(_r3);
      const ctx_r0 = ɵɵnextContext(2);
      ctx_r0.clearAndFocus();
      return ɵɵresetView($event.preventDefault());
    })("keydown.space", function PoSearchAiComponent_Conditional_13_Conditional_1_Template_po_clean_keydown_space_0_listener($event) {
      ɵɵrestoreView(_r3);
      const ctx_r0 = ɵɵnextContext(2);
      ctx_r0.clearAndFocus();
      return ɵɵresetView($event.preventDefault());
    });
    ɵɵelementEnd();
    ɵɵelement(2, "div", 20);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵproperty("p-element-ref", ctx_r0.inputEl)("p-size", ctx_r0.size);
    ɵɵattribute("aria-label", ctx_r0.effectiveLiterals().clean);
  }
}
function PoSearchAiComponent_Conditional_13_Template(rf, ctx) {
  if (rf & 1) {
    const _r2 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 13);
    ɵɵconditionalCreate(1, PoSearchAiComponent_Conditional_13_Conditional_1_Template, 3, 3);
    ɵɵelementStart(2, "po-button", 18, 2);
    ɵɵlistener("p-click", function PoSearchAiComponent_Conditional_13_Template_po_button_p_click_2_listener() {
      ɵɵrestoreView(_r2);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.search());
    });
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵconditional(ctx_r0.clean && ctx_r0.hasValue() && !ctx_r0.isDisabled && !ctx_r0.readonly ? 1 : -1);
    ɵɵadvance();
    ɵɵproperty("p-disabled", ctx_r0.isDisabled || !!ctx_r0.readonly)("p-size", ctx_r0.size);
  }
}
function PoSearchAiComponent_Conditional_14_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-helper", 14, 4);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    const poHelper_r4 = ɵɵreadContextLet(0);
    ɵɵproperty("p-size", ctx_r0.size)("p-helper", poHelper_r4.helperSettings)("p-disabled", ctx_r0.isDisabled)("p-append-in-body", ctx_r0.appendBox);
  }
}
function PoSearchAiComponent_Conditional_15_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-field-container-bottom", 15);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("p-append-in-body", ctx_r0.appendBox)("p-help", ctx_r0.help)("p-disabled", ctx_r0.isDisabled)("p-error-pattern", ctx_r0.getErrorPattern())("p-error-limit", ctx_r0.errorLimit)("p-size", ctx_r0.size);
  }
}
var _c93 = ["locateCounter"];
var _c94 = ["poSearchInput"];
var _c95 = ["poListboxContainerElement"];
var _c96 = ["poListboxElement"];
function PoSearchComponent_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 5);
    ɵɵelement(1, "po-dropdown", 16);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵproperty("p-label", ctx_r0.searchFilterSelectLabel)("p-disabled", ctx_r0.isDisabled)("p-actions", ctx_r0.searchFilterSelectActions)("p-size", ctx_r0.size);
  }
}
function PoSearchComponent_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 6);
    ɵɵelement(1, "po-icon", 17);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵproperty("p-icon", ctx_r0.icon ? ctx_r0.icon : "ICON_SEARCH");
  }
}
function PoSearchComponent_Conditional_6_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span", 9, 1);
    ɵɵtext(2);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵattribute("aria-label", ctx_r0.literals.result + " " + ((ctx_r0.locateSummary == null ? null : ctx_r0.locateSummary.currentIndex) ?? 0) + " " + ctx_r0.literals.of + " " + ((ctx_r0.locateSummary == null ? null : ctx_r0.locateSummary.total) ?? 0));
    ɵɵadvance(2);
    ɵɵtextInterpolate2(" ", (ctx_r0.locateSummary == null ? null : ctx_r0.locateSummary.currentIndex) ?? 0, "/", (ctx_r0.locateSummary == null ? null : ctx_r0.locateSummary.total) ?? 0, " ");
  }
}
function PoSearchComponent_Conditional_8_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 11);
    ɵɵelement(1, "po-loading-icon", 18);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵproperty("p-size", ctx_r0.mapSizeToIcon(ctx_r0.size));
  }
}
function PoSearchComponent_Conditional_9_Template(rf, ctx) {
  if (rf & 1) {
    const _r2 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "button", 19);
    ɵɵlistener("click", function PoSearchComponent_Conditional_9_Template_button_click_0_listener() {
      ɵɵrestoreView(_r2);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.clearSearch());
    })("keydown.enter", function PoSearchComponent_Conditional_9_Template_button_keydown_enter_0_listener() {
      ɵɵrestoreView(_r2);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.clearSearch());
    })("keydown", function PoSearchComponent_Conditional_9_Template_button_keydown_0_listener($event) {
      ɵɵrestoreView(_r2);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.onCleanKeydown($event));
    });
    ɵɵelement(1, "po-clean", 20);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("ariaLabel", ctx_r0.literals.clean);
    ɵɵadvance();
    ɵɵproperty("p-size", ctx_r0.size);
  }
}
function PoSearchComponent_Conditional_10_Template(rf, ctx) {
  if (rf & 1) {
    const _r3 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "button", 21);
    ɵɵlistener("click", function PoSearchComponent_Conditional_10_Template_button_click_0_listener() {
      ɵɵrestoreView(_r3);
      const ctx_r0 = ɵɵnextContext();
      const poSearchInput_r4 = ɵɵreference(4);
      return ɵɵresetView(ctx_r0.onSearchChange(poSearchInput_r4.value, true, true));
    })("keydown.enter", function PoSearchComponent_Conditional_10_Template_button_keydown_enter_0_listener() {
      ɵɵrestoreView(_r3);
      const ctx_r0 = ɵɵnextContext();
      const poSearchInput_r4 = ɵɵreference(4);
      return ɵɵresetView(ctx_r0.onSearchChange(poSearchInput_r4.value, true, true));
    });
    ɵɵelement(1, "po-icon", 17);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("ariaLabel", ctx_r0.literals.search)("disabled", ctx_r0.isDisabled);
    ɵɵadvance();
    ɵɵproperty("p-icon", ctx_r0.icon ? ctx_r0.icon : "ICON_SEARCH");
  }
}
function PoSearchComponent_Conditional_11_Template(rf, ctx) {
  if (rf & 1) {
    const _r5 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 14);
    ɵɵelement(1, "div", 22);
    ɵɵelementStart(2, "button", 23);
    ɵɵlistener("click", function PoSearchComponent_Conditional_11_Template_button_click_2_listener() {
      ɵɵrestoreView(_r5);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.locateNext.emit());
    });
    ɵɵelement(3, "po-icon", 24);
    ɵɵelementEnd();
    ɵɵelementStart(4, "button", 25);
    ɵɵlistener("click", function PoSearchComponent_Conditional_11_Template_button_click_4_listener() {
      ɵɵrestoreView(_r5);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.locatePrevious.emit());
    });
    ɵɵelement(5, "po-icon", 26);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance(2);
    ɵɵproperty("ariaLabel", ctx_r0.literals.next)("disabled", ctx_r0.disabled || !(ctx_r0.locateSummary == null ? null : ctx_r0.locateSummary.total));
    ɵɵadvance(2);
    ɵɵproperty("ariaLabel", ctx_r0.literals.previous)("disabled", ctx_r0.disabled || !(ctx_r0.locateSummary == null ? null : ctx_r0.locateSummary.total));
  }
}
function PoSearchComponent_Conditional_12_Template(rf, ctx) {
  if (rf & 1) {
    const _r6 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 15, 2)(2, "po-listbox", 27, 3);
    ɵɵlistener("p-selectcombo-item", function PoSearchComponent_Conditional_12_Template_po_listbox_p_selectcombo_item_2_listener($event) {
      ɵɵrestoreView(_r6);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.onListboxClick($event, $event.event));
    })("p-close", function PoSearchComponent_Conditional_12_Template_po_listbox_p_close_2_listener() {
      ɵɵrestoreView(_r6);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.onCloseListbox());
    })("p-footer-action-listbox-event", function PoSearchComponent_Conditional_12_Template_po_listbox_p_footer_action_listbox_event_2_listener() {
      ɵɵrestoreView(_r6);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.handlerFooterActionListbox());
    });
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("hidden", !ctx_r0.listboxOpen);
    ɵɵadvance(2);
    ɵɵproperty("p-items", ctx_r0.listboxFilteredItems)("p-visible", ctx_r0.listboxOpen)("p-filter-mode", ctx_r0.filterType)("p-should-mark-letter", ctx_r0.shouldMarkLetters)("p-filtering", ctx_r0.isFiltering)("p-search-value", ctx_r0.getInputValue())("p-size", ctx_r0.size)("p-literals", ctx_r0.literals)("p-separator", ctx_r0.showSeparator)("p-keys-label", ctx_r0.keysLabel)("p-footer-action-listbox", ctx_r0.showFooterActionListbox)("p-placeholder-listbox", ctx_r0.placeholderListbox);
  }
}
function PoTableDetailComponent_Conditional_1_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "th", 2);
  }
}
function PoTableDetailComponent_Conditional_1_For_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "th", 4)(1, "div", 5);
    ɵɵtext(2);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const detail_r1 = ctx.$implicit;
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵadvance();
    ɵɵclassProp("po-table-header-flex-right", detail_r1.type === "currency" || detail_r1.type === "number");
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ctx_r1.getColumnTitleLabel(detail_r1), " ");
  }
}
function PoTableDetailComponent_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "thead")(1, "tr");
    ɵɵconditionalCreate(2, PoTableDetailComponent_Conditional_1_Conditional_2_Template, 1, 0, "th", 2);
    ɵɵelement(3, "th", 3);
    ɵɵrepeaterCreate(4, PoTableDetailComponent_Conditional_1_For_5_Template, 3, 3, "th", 4, ɵɵrepeaterTrackByIndex);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance(2);
    ɵɵconditional(ctx_r1.isSelectable ? 2 : -1);
    ɵɵadvance(2);
    ɵɵrepeater(ctx_r1.detail.columns);
  }
}
function PoTableDetailComponent_For_4_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r3 = ɵɵgetCurrentView();
    ɵɵelement(0, "td", 9);
    ɵɵelementStart(1, "td", 10)(2, "po-checkbox", 11);
    ɵɵlistener("p-change", function PoTableDetailComponent_For_4_Conditional_1_Template_po_checkbox_p_change_2_listener() {
      ɵɵrestoreView(_r3);
      const item_r4 = ɵɵnextContext().$implicit;
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onSelectRow(item_r4));
    });
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const item_r4 = ɵɵnextContext().$implicit;
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance(2);
    ɵɵproperty("p-checkboxValue", item_r4.$selected)("p-size", ctx_r1.componentsSize);
  }
}
function PoTableDetailComponent_For_4_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "td", 7);
  }
}
function PoTableDetailComponent_For_4_For_4_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "strong");
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const detail_r6 = ɵɵnextContext().$implicit;
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ctx_r1.getColumnTitleLabel(detail_r6), ": ");
  }
}
function PoTableDetailComponent_For_4_For_4_Case_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span");
    ɵɵtext(1);
    ɵɵpipe(2, "currency");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const detail_r6 = ɵɵnextContext().$implicit;
    const item_r4 = ɵɵnextContext().$implicit;
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵtextInterpolate(ɵɵpipeBind4(2, 1, ctx_r1.getDetailData(item_r4, detail_r6), detail_r6.format, "symbol", "1.2-2"));
  }
}
function PoTableDetailComponent_For_4_For_4_Case_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span");
    ɵɵtext(1);
    ɵɵpipe(2, "date");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const detail_r6 = ɵɵnextContext().$implicit;
    const item_r4 = ɵɵnextContext().$implicit;
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵtextInterpolate(ɵɵpipeBind2(2, 1, ctx_r1.getDetailData(item_r4, detail_r6), detail_r6.format || "dd/MM/yyyy"));
  }
}
function PoTableDetailComponent_For_4_For_4_Case_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span");
    ɵɵtext(1);
    ɵɵpipe(2, "po_time");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const detail_r6 = ɵɵnextContext().$implicit;
    const item_r4 = ɵɵnextContext().$implicit;
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵtextInterpolate(ɵɵpipeBind2(2, 1, ctx_r1.getDetailData(item_r4, detail_r6), detail_r6.format || "HH:mm:ss.ffffff"));
  }
}
function PoTableDetailComponent_For_4_For_4_Case_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span");
    ɵɵtext(1);
    ɵɵpipe(2, "date");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const detail_r6 = ɵɵnextContext().$implicit;
    const item_r4 = ɵɵnextContext().$implicit;
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ɵɵpipeBind2(2, 1, ctx_r1.getDetailData(item_r4, detail_r6), detail_r6.format || "dd/MM/yyyy HH:mm:ss"), " ");
  }
}
function PoTableDetailComponent_For_4_For_4_Case_6_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span");
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const detail_r6 = ɵɵnextContext().$implicit;
    const item_r4 = ɵɵnextContext().$implicit;
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵtextInterpolate(ctx_r1.formatNumberDetail(ctx_r1.getDetailData(item_r4, detail_r6), detail_r6.format));
  }
}
function PoTableDetailComponent_For_4_For_4_Case_7_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span");
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const detail_r6 = ɵɵnextContext().$implicit;
    const item_r4 = ɵɵnextContext().$implicit;
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵtextInterpolate(ctx_r1.getDetailData(item_r4, detail_r6));
  }
}
function PoTableDetailComponent_For_4_For_4_Template(rf, ctx) {
  if (rf & 1) {
    const _r5 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "td", 12);
    ɵɵlistener("click", function PoTableDetailComponent_For_4_For_4_Template_td_click_0_listener() {
      ɵɵrestoreView(_r5);
      const item_r4 = ɵɵnextContext().$implicit;
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.isSelectable ? ctx_r1.onSelectRow(item_r4) : "javascript:;");
    });
    ɵɵconditionalCreate(1, PoTableDetailComponent_For_4_For_4_Conditional_1_Template, 2, 1, "strong");
    ɵɵconditionalCreate(2, PoTableDetailComponent_For_4_For_4_Case_2_Template, 3, 6, "span")(3, PoTableDetailComponent_For_4_For_4_Case_3_Template, 3, 4, "span")(4, PoTableDetailComponent_For_4_For_4_Case_4_Template, 3, 4, "span")(5, PoTableDetailComponent_For_4_For_4_Case_5_Template, 3, 4, "span")(6, PoTableDetailComponent_For_4_For_4_Case_6_Template, 2, 1, "span")(7, PoTableDetailComponent_For_4_For_4_Case_7_Template, 2, 1, "span");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    let tmp_22_0;
    const detail_r6 = ctx.$implicit;
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵclassProp("po-table-column-right", ctx_r1.typeHeaderTop && (detail_r6.type === "currency" || detail_r6.type === "number"));
    ɵɵadvance();
    ɵɵconditional(ctx_r1.typeHeaderInline ? 1 : -1);
    ɵɵadvance();
    ɵɵconditional((tmp_22_0 = detail_r6.type) === "currency" ? 2 : tmp_22_0 === "date" ? 3 : tmp_22_0 === "time" ? 4 : tmp_22_0 === "dateTime" ? 5 : tmp_22_0 === "number" ? 6 : 7);
  }
}
function PoTableDetailComponent_For_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "tr", 6);
    ɵɵconditionalCreate(1, PoTableDetailComponent_For_4_Conditional_1_Template, 3, 2)(2, PoTableDetailComponent_For_4_Conditional_2_Template, 1, 0, "td", 7);
    ɵɵrepeaterCreate(3, PoTableDetailComponent_For_4_For_4_Template, 8, 4, "td", 8, ɵɵrepeaterTrackByIndex);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const item_r4 = ctx.$implicit;
    const ctx_r1 = ɵɵnextContext();
    ɵɵclassProp("po-table-row-active", item_r4.$selected && ctx_r1.isSelectable);
    ɵɵadvance();
    ɵɵconditional(ctx_r1.isSelectable ? 1 : 2);
    ɵɵadvance(2);
    ɵɵrepeater(ctx_r1.detailColumns);
  }
}
function PoTableShowSubtitleComponent_For_7_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 5);
    ɵɵelement(1, "po-table-subtitle-circle", 6);
    ɵɵelementStart(2, "span", 7);
    ɵɵtext(3);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const subtitle_r3 = ctx.$implicit;
    ɵɵadvance();
    ɵɵproperty("p-subtitle", subtitle_r3);
    ɵɵadvance(2);
    ɵɵtextInterpolate(subtitle_r3.label);
  }
}
function PoTableSubtitleFooterComponent_For_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 1);
    ɵɵelement(1, "po-table-subtitle-circle", 3);
    ɵɵelementStart(2, "span", 4);
    ɵɵtext(3);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const subtitle_r1 = ctx.$implicit;
    ɵɵadvance();
    ɵɵproperty("p-subtitle", subtitle_r1);
    ɵɵadvance(2);
    ɵɵtextInterpolate(subtitle_r1.label);
  }
}
function PoTableSubtitleFooterComponent_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-table-show-subtitle", 2);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("p-components-size", ctx_r1.componentsSize)("p-literals", ctx_r1.literals)("p-subtitles", ctx_r1.subtitles);
  }
}
var _c97 = ["noColumnsHeader"];
var _c98 = ["tableFooter"];
var _c99 = ["tableWrapper"];
var _c100 = ["tableTemplate"];
var _c101 = ["tableVirtualScroll"];
var _c102 = ["tableScrollable"];
var _c103 = ["columnManager"];
var _c104 = ["columnBatchActions"];
var _c105 = ["columnActionLeft"];
var _c106 = ["filterInput"];
var _c107 = ["columnManagerTarget"];
var _c108 = ["columnManagerTargetFixed"];
var _c109 = ["actionsIconElement"];
var _c110 = ["actionsElement"];
var _c111 = (a0, a1, a2, a3, a4) => ({
  "po-table-interactive": a0,
  "po-table-selectable": a1,
  "po-table-striped": a2,
  "po-table-data-fixed-columns": a3,
  "po-table-text-wrap-enabled": a4
});
var _c112 = (a0, a1) => ({
  "no-hover": a0,
  "po-table-column-drag": a1
});
var _c113 = (a0) => ({
  "po-table-header-sorted": a0
});
var _c114 = (a0, a1) => ({
  row: a0,
  rowIndex: a1
});
var _c115 = (a0, a1) => ({
  row: a0,
  column: a1
});
var _c116 = (a0, a1) => ({
  $implicit: a0,
  rowIndex: a1
});
function PoTableComponent_Conditional_1_ng_template_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtext(0);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵtextInterpolate(ctx_r1.literals.noItem);
  }
}
function PoTableComponent_Conditional_1_ng_template_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtext(0);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵtextInterpolate(ctx_r1.literals.oneItem);
  }
}
function PoTableComponent_Conditional_1_ng_template_6_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtext(0);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵtextInterpolate2("", ctx_r1.itemsSelected.length, " ", ctx_r1.literals.multipleItems);
  }
}
function PoTableComponent_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 23, 10)(2, "div", 32)(3, "strong");
    ɵɵtemplate(4, PoTableComponent_Conditional_1_ng_template_4_Template, 1, 1, "ng-template", 33)(5, PoTableComponent_Conditional_1_ng_template_5_Template, 1, 1, "ng-template", 34)(6, PoTableComponent_Conditional_1_ng_template_6_Template, 1, 2, "ng-template", 35);
    ɵɵelementEnd()();
    ɵɵelementStart(7, "div", 36)(8, "po-button", 37);
    ɵɵlistener("p-click", function PoTableComponent_Conditional_1_Template_po_button_p_click_8_listener() {
      ɵɵrestoreView(_r1);
      ɵɵnextContext();
      const modalDelete_r3 = ɵɵreference(28);
      return ɵɵresetView(modalDelete_r3.open());
    });
    ɵɵelementEnd()()();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance(2);
    ɵɵproperty("ngPlural", ctx_r1.itemsSelected.length);
    ɵɵadvance(6);
    ɵɵproperty("p-danger", true)("p-disabled", ctx_r1.itemsSelected.length > 1 && ctx_r1.serviceDeleteApi !== void 0)("p-label", ctx_r1.literals.delete)("p-size", ctx_r1.componentsSize);
  }
}
function PoTableComponent_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    const _r4 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 24, 11)(2, "po-button", 38, 12);
    ɵɵlistener("p-click", function PoTableComponent_Conditional_2_Template_po_button_p_click_2_listener() {
      ɵɵrestoreView(_r4);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onOpenColumnManager());
    });
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance(2);
    ɵɵproperty("p-aria-label", ctx_r1.literals.columnsManager)("p-tooltip", ctx_r1.literals.columnsManager)("p-size", ctx_r1.componentsSize);
  }
}
function PoTableComponent_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    const _r5 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 25)(1, "po-search-ai", 39);
    ɵɵlistener("p-result", function PoTableComponent_Conditional_3_Template_po_search_ai_p_result_1_listener($event) {
      ɵɵrestoreView(_r5);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onAiResult($event));
    })("p-low-confidence", function PoTableComponent_Conditional_3_Template_po_search_ai_p_low_confidence_1_listener($event) {
      ɵɵrestoreView(_r5);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onAiLowConfidence($event));
    })("p-error", function PoTableComponent_Conditional_3_Template_po_search_ai_p_error_1_listener($event) {
      ɵɵrestoreView(_r5);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onAiError($event));
    })("p-clear", function PoTableComponent_Conditional_3_Template_po_search_ai_p_clear_1_listener() {
      ɵɵrestoreView(_r5);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onAiClear());
    });
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵproperty("p-url", ctx_r1.searchAiField().url)("p-clean", true)("p-columns", ctx_r1.searchAiColumns)("p-min-confidence", ctx_r1.searchAiField().minConfidence)("p-timeout", ctx_r1.searchAiField().timeout)("p-placeholder", ctx_r1.searchAiPlaceholder)("p-literals", ctx_r1.searchAiField().literals)("p-size", ctx_r1.componentsSize);
  }
}
function PoTableComponent_Conditional_4_Template(rf, ctx) {
  if (rf & 1) {
    const _r6 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 25)(1, "po-search", 40);
    ɵɵlistener("p-filtered-items-change", function PoTableComponent_Conditional_4_Template_po_search_p_filtered_items_change_1_listener($event) {
      ɵɵrestoreView(_r6);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onFilteredItemsChange($event));
    });
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵproperty("p-items", ctx_r1.items)("p-filter-keys", ctx_r1.filteredColumns)("p-filter-type", ctx_r1.filterType)("p-size", ctx_r1.componentsSize);
  }
}
function PoTableComponent_Conditional_5_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoTableComponent_Conditional_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "po-container", 26);
    ɵɵtemplate(1, PoTableComponent_Conditional_5_ng_container_1_Template, 1, 0, "ng-container", 41);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    ɵɵnextContext();
    const tableContainerTemplate_r7 = ɵɵreference(8);
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", tableContainerTemplate_r7);
  }
}
function PoTableComponent_Conditional_6_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-loading-overlay", 42);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵproperty("p-text", ctx_r1.literals.loadingData)("p-size", ctx_r1.sizeLoading);
  }
}
function PoTableComponent_Conditional_6_Conditional_5_Conditional_3_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoTableComponent_Conditional_6_Conditional_5_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div");
    ɵɵtemplate(1, PoTableComponent_Conditional_6_Conditional_5_Conditional_3_ng_container_1_Template, 1, 0, "ng-container", 41);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    ɵɵnextContext(3);
    const tableVirtualScrollTemplate_r8 = ɵɵreference(13);
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", tableVirtualScrollTemplate_r8);
  }
}
function PoTableComponent_Conditional_6_Conditional_5_Conditional_4_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoTableComponent_Conditional_6_Conditional_5_Conditional_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 49);
    ɵɵtemplate(1, PoTableComponent_Conditional_6_Conditional_5_Conditional_4_ng_container_1_Template, 1, 0, "ng-container", 41);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(3);
    const tableTemplate_r9 = ɵɵreference(11);
    ɵɵstyleProp("height", ctx_r1.heightTableContainer, "px");
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", tableTemplate_r9);
  }
}
function PoTableComponent_Conditional_6_Conditional_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 47)(1, "div", null, 14);
    ɵɵconditionalCreate(3, PoTableComponent_Conditional_6_Conditional_5_Conditional_3_Template, 2, 1, "div");
    ɵɵconditionalCreate(4, PoTableComponent_Conditional_6_Conditional_5_Conditional_4_Template, 2, 3, "div", 48);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵstyleProp("height", ctx_r1.heightTableContainer, "px");
    ɵɵadvance();
    ɵɵclassProp("po-table-container-fixed-inner", ctx_r1.virtualScroll);
    ɵɵadvance(2);
    ɵɵconditional(ctx_r1.virtualScroll ? 3 : -1);
    ɵɵadvance();
    ɵɵconditional(!ctx_r1.virtualScroll ? 4 : -1);
  }
}
function PoTableComponent_Conditional_6_Conditional_6_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoTableComponent_Conditional_6_Conditional_6_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div");
    ɵɵtemplate(1, PoTableComponent_Conditional_6_Conditional_6_ng_container_1_Template, 1, 0, "ng-container", 41);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    ɵɵnextContext(2);
    const tableTemplate_r9 = ɵɵreference(11);
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", tableTemplate_r9);
  }
}
function PoTableComponent_Conditional_6_Conditional_7_For_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-table-subtitle-footer", 50);
  }
  if (rf & 2) {
    const column_r10 = ctx.$implicit;
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵproperty("p-components-size", ctx_r1.componentsSize)("p-literals", ctx_r1.literals)("p-subtitles", column_r10.subtitles);
  }
}
function PoTableComponent_Conditional_6_Conditional_7_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 46);
    ɵɵrepeaterCreate(1, PoTableComponent_Conditional_6_Conditional_7_For_2_Template, 1, 3, "po-table-subtitle-footer", 50, ɵɵcomponentInstance().trackBy, true);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵadvance();
    ɵɵrepeater(ctx_r1.subtitleColumns);
  }
}
function PoTableComponent_Conditional_6_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div");
    ɵɵconditionalCreate(1, PoTableComponent_Conditional_6_Conditional_1_Template, 1, 2, "po-loading-overlay", 42);
    ɵɵelementStart(2, "div", 43)(3, "div", 44, 13);
    ɵɵconditionalCreate(5, PoTableComponent_Conditional_6_Conditional_5_Template, 5, 6, "div", 45);
    ɵɵconditionalCreate(6, PoTableComponent_Conditional_6_Conditional_6_Template, 2, 1, "div");
    ɵɵelementEnd()()();
    ɵɵconditionalCreate(7, PoTableComponent_Conditional_6_Conditional_7_Template, 3, 0, "div", 46);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵclassProp("po-table-container-sticky", ctx_r1.loading);
    ɵɵadvance();
    ɵɵconditional(ctx_r1.loading ? 1 : -1);
    ɵɵadvance(2);
    ɵɵstyleProp("opacity", ctx_r1.tableOpacity);
    ɵɵclassProp("po-table-header-fixed-columns-pixels", ctx_r1.allColumnsWidthPixels);
    ɵɵadvance(2);
    ɵɵconditional(ctx_r1.height ? 5 : -1);
    ɵɵadvance();
    ɵɵconditional(!ctx_r1.height ? 6 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r1.hasFooter ? 7 : -1);
  }
}
function PoTableComponent_ng_template_7_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-loading-overlay", 42);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵproperty("p-text", ctx_r1.literals.loadingData)("p-size", ctx_r1.sizeLoading);
  }
}
function PoTableComponent_ng_template_7_Conditional_5_Conditional_3_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoTableComponent_ng_template_7_Conditional_5_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div");
    ɵɵtemplate(1, PoTableComponent_ng_template_7_Conditional_5_Conditional_3_ng_container_1_Template, 1, 0, "ng-container", 41);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    ɵɵnextContext(3);
    const tableVirtualScrollTemplate_r8 = ɵɵreference(13);
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", tableVirtualScrollTemplate_r8);
  }
}
function PoTableComponent_ng_template_7_Conditional_5_Conditional_4_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoTableComponent_ng_template_7_Conditional_5_Conditional_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 49);
    ɵɵtemplate(1, PoTableComponent_ng_template_7_Conditional_5_Conditional_4_ng_container_1_Template, 1, 0, "ng-container", 41);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(3);
    const tableTemplate_r9 = ɵɵreference(11);
    ɵɵstyleProp("height", ctx_r1.heightTableContainer, "px");
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", tableTemplate_r9);
  }
}
function PoTableComponent_ng_template_7_Conditional_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 47)(1, "div", null, 14);
    ɵɵconditionalCreate(3, PoTableComponent_ng_template_7_Conditional_5_Conditional_3_Template, 2, 1, "div");
    ɵɵconditionalCreate(4, PoTableComponent_ng_template_7_Conditional_5_Conditional_4_Template, 2, 3, "div", 48);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵstyleProp("height", ctx_r1.heightTableContainer, "px");
    ɵɵadvance();
    ɵɵclassProp("po-table-container-fixed-inner", ctx_r1.virtualScroll);
    ɵɵadvance(2);
    ɵɵconditional(ctx_r1.virtualScroll ? 3 : -1);
    ɵɵadvance();
    ɵɵconditional(!ctx_r1.virtualScroll ? 4 : -1);
  }
}
function PoTableComponent_ng_template_7_Conditional_6_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoTableComponent_ng_template_7_Conditional_6_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div");
    ɵɵtemplate(1, PoTableComponent_ng_template_7_Conditional_6_ng_container_1_Template, 1, 0, "ng-container", 41);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    ɵɵnextContext(2);
    const tableTemplate_r9 = ɵɵreference(11);
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", tableTemplate_r9);
  }
}
function PoTableComponent_ng_template_7_Conditional_7_For_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-table-subtitle-footer", 50);
  }
  if (rf & 2) {
    const column_r11 = ctx.$implicit;
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵproperty("p-components-size", ctx_r1.componentsSize)("p-literals", ctx_r1.literals)("p-subtitles", column_r11.subtitles);
  }
}
function PoTableComponent_ng_template_7_Conditional_7_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 46);
    ɵɵrepeaterCreate(1, PoTableComponent_ng_template_7_Conditional_7_For_2_Template, 1, 3, "po-table-subtitle-footer", 50, ɵɵcomponentInstance().trackBy, true);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵadvance();
    ɵɵrepeater(ctx_r1.subtitleColumns);
  }
}
function PoTableComponent_ng_template_7_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div");
    ɵɵconditionalCreate(1, PoTableComponent_ng_template_7_Conditional_1_Template, 1, 2, "po-loading-overlay", 42);
    ɵɵelementStart(2, "div", 43)(3, "div", 44, 13);
    ɵɵconditionalCreate(5, PoTableComponent_ng_template_7_Conditional_5_Template, 5, 6, "div", 45);
    ɵɵconditionalCreate(6, PoTableComponent_ng_template_7_Conditional_6_Template, 2, 1, "div");
    ɵɵelementEnd()()();
    ɵɵconditionalCreate(7, PoTableComponent_ng_template_7_Conditional_7_Template, 3, 0, "div", 46);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵclassProp("po-table-container-sticky", ctx_r1.loading);
    ɵɵadvance();
    ɵɵconditional(ctx_r1.loading ? 1 : -1);
    ɵɵadvance(2);
    ɵɵstyleProp("opacity", ctx_r1.tableOpacity);
    ɵɵclassProp("po-table-header-fixed-columns-pixels", ctx_r1.allColumnsWidthPixels);
    ɵɵadvance(2);
    ɵɵconditional(ctx_r1.height ? 5 : -1);
    ɵɵadvance();
    ɵɵconditional(!ctx_r1.height ? 6 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r1.hasFooter ? 7 : -1);
  }
}
function PoTableComponent_Conditional_9_Template(rf, ctx) {
  if (rf & 1) {
    const _r12 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 51, 15)(2, "po-button", 52);
    ɵɵlistener("p-click", function PoTableComponent_Conditional_9_Template_po_button_p_click_2_listener() {
      ɵɵrestoreView(_r12);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onShowMore());
    });
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵclassProp("po-invisible", ctx_r1.showMore.observers.length === 0 && !ctx_r1.hasService);
    ɵɵadvance(2);
    ɵɵproperty("p-disabled", ctx_r1.showMoreDisabled)("p-label", ctx_r1.literals.loadMoreData)("p-loading", ctx_r1.loadingShowMore)("p-size", ctx_r1.componentsSize);
  }
}
function PoTableComponent_ng_template_10_Conditional_4_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    const _r14 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-checkbox", 63);
    ɵɵlistener("p-change", function PoTableComponent_ng_template_10_Conditional_4_Conditional_2_Template_po_checkbox_p_change_0_listener() {
      ɵɵrestoreView(_r14);
      const ctx_r1 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r1.selectAllRows());
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵproperty("p-size", ctx_r1.componentsSize)("p-checkboxValue", ctx_r1.selectAll === null ? "mixed" : ctx_r1.selectAll);
  }
}
function PoTableComponent_ng_template_10_Conditional_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "th", 61)(1, "div");
    ɵɵconditionalCreate(2, PoTableComponent_ng_template_10_Conditional_4_Conditional_2_Template, 1, 2, "po-checkbox", 62);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵstyleProp("pointer-events", ctx_r1.hideSelectAll ? "none" : "auto");
    ɵɵadvance();
    ɵɵclassProp("po-table-header-fixed-inner", ctx_r1.height);
    ɵɵadvance();
    ɵɵconditional(!ctx_r1.hideSelectAll ? 2 : -1);
  }
}
function PoTableComponent_ng_template_10_Conditional_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "th", 56);
  }
}
function PoTableComponent_ng_template_10_Conditional_6_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "th", null, 17);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵclassProp("po-table-header-master-detail", !ctx_r1.isSingleAction)("po-table-header-single-action", ctx_r1.isSingleAction);
  }
}
function PoTableComponent_ng_template_10_Conditional_7_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 65);
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵstyleProp("width", ctx_r1.hasValidColumns ? ctx_r1.headerWidth : null, "px");
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ctx_r1.hasValidColumns ? ctx_r1.literals.noVisibleColumn : ctx_r1.literals.noColumns, " ");
  }
}
function PoTableComponent_ng_template_10_Conditional_7_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtext(0);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵtextInterpolate1(" ", ctx_r1.hasValidColumns ? ctx_r1.literals.noVisibleColumn : ctx_r1.literals.noColumns, " ");
  }
}
function PoTableComponent_ng_template_10_Conditional_7_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "th", 58, 18);
    ɵɵconditionalCreate(2, PoTableComponent_ng_template_10_Conditional_7_Conditional_2_Template, 2, 3, "div", 64)(3, PoTableComponent_ng_template_10_Conditional_7_Conditional_3_Template, 1, 1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵattribute("colspan", ctx_r1.columnCount);
    ɵɵadvance(2);
    ɵɵconditional(ctx_r1.height ? 2 : 3);
  }
}
function PoTableComponent_ng_template_10_Conditional_8_For_1_Conditional_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-icon", 69);
  }
}
function PoTableComponent_ng_template_10_Conditional_8_For_1_ng_container_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoTableComponent_ng_template_10_Conditional_8_For_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r15 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "th", 67);
    ɵɵpipe(1, "titlecase");
    ɵɵpipe(2, "lowercase");
    ɵɵlistener("click", function PoTableComponent_ng_template_10_Conditional_8_For_1_Template_th_click_0_listener() {
      const column_r16 = ɵɵrestoreView(_r15).$implicit;
      const ctx_r1 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r1.sortColumn(column_r16));
    });
    ɵɵelementStart(3, "div", 68);
    ɵɵconditionalCreate(4, PoTableComponent_ng_template_10_Conditional_8_For_1_Conditional_4_Template, 1, 0, "po-icon", 69);
    ɵɵtemplate(5, PoTableComponent_ng_template_10_Conditional_8_For_1_ng_container_5_Template, 1, 0, "ng-container", 70);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const column_r16 = ctx.$implicit;
    const ctx_r1 = ɵɵnextContext(3);
    const contentHeaderTemplate_r17 = ɵɵreference(23);
    ɵɵstyleProp("width", column_r16.width)("max-width", column_r16.width)("min-width", column_r16.width);
    ɵɵclassProp("po-clickable", ctx_r1.sort && column_r16.sortable !== false || ctx_r1.hasService)("po-table-header-subtitle", column_r16.type === "subtitle")("po-table-column-drag-box", ctx_r1.isDraggable);
    ɵɵproperty("ngClass", ɵɵpureFunction1(30, _c113, ctx_r1.sort && ctx_r1.JSON.stringify(ctx_r1.sortedColumn == null ? null : ctx_r1.sortedColumn.property) === ctx_r1.JSON.stringify(column_r16) && (ctx_r1.sortedColumn.ascending || !ctx_r1.sortedColumn.ascending)))("ngStyle", ɵɵpureFunction1(32, _c86, ctx_r1.height > 0 && !ctx_r1.virtualScroll ? !ctx_r1.hasItems ? "100%" : ctx_r1.applyFixedColumns() ? column_r16.width : "auto" : ""))("cdkDragDisabled", column_r16.fixed ? "true" : "false")("pFrozenColumn", column_r16.fixed);
    ɵɵattribute("data-po-table-column-name", ɵɵpipeBind1(2, 28, column_r16.label || ɵɵpipeBind1(1, 26, column_r16.property)));
    ɵɵadvance(3);
    ɵɵclassProp("po-table-header-fixed-inner", ctx_r1.height)("po-table-header-flex-right", column_r16.type === "currency" || column_r16.type === "number")("po-table-header-flex-center", column_r16.type === "subtitle");
    ɵɵadvance();
    ɵɵconditional(ctx_r1.isDraggable && !column_r16.fixed ? 4 : -1);
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", contentHeaderTemplate_r17)("ngTemplateOutletContext", ɵɵpureFunction1(34, _c21, column_r16));
  }
}
function PoTableComponent_ng_template_10_Conditional_8_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵrepeaterCreate(0, PoTableComponent_ng_template_10_Conditional_8_For_1_Template, 6, 36, "th", 66, ɵɵcomponentInstance().trackBy, true);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵrepeater(ctx_r1.mainColumns);
  }
}
function PoTableComponent_ng_template_10_Conditional_9_For_1_ng_container_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoTableComponent_ng_template_10_Conditional_9_For_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r18 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "th", 72);
    ɵɵpipe(1, "titlecase");
    ɵɵpipe(2, "lowercase");
    ɵɵlistener("click", function PoTableComponent_ng_template_10_Conditional_9_For_1_Template_th_click_0_listener() {
      const column_r19 = ɵɵrestoreView(_r18).$implicit;
      const ctx_r1 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r1.sortColumn(column_r19));
    });
    ɵɵelementStart(3, "div", 68);
    ɵɵtemplate(4, PoTableComponent_ng_template_10_Conditional_9_For_1_ng_container_4_Template, 1, 0, "ng-container", 70);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const column_r19 = ctx.$implicit;
    const ctx_r1 = ɵɵnextContext(3);
    const contentHeaderTemplate_r17 = ɵɵreference(23);
    ɵɵstyleProp("width", column_r19.width)("max-width", column_r19.width)("min-width", column_r19.width);
    ɵɵclassProp("po-clickable", ctx_r1.sort && column_r19.sortable !== false || ctx_r1.hasService)("po-table-header-subtitle", column_r19.type === "subtitle");
    ɵɵproperty("ngClass", ɵɵpureFunction1(26, _c113, ctx_r1.sort && ctx_r1.JSON.stringify(ctx_r1.sortedColumn == null ? null : ctx_r1.sortedColumn.property) === ctx_r1.JSON.stringify(column_r19) && (ctx_r1.sortedColumn.ascending || !ctx_r1.sortedColumn.ascending)))("ngStyle", ɵɵpureFunction1(28, _c86, ctx_r1.height > 0 && !ctx_r1.virtualScroll ? !ctx_r1.hasItems ? "100%" : ctx_r1.applyFixedColumns() ? column_r19.width : "auto" : ""))("pFrozenColumn", column_r19.fixed);
    ɵɵattribute("data-po-table-column-name", ɵɵpipeBind1(2, 24, column_r19.label || ɵɵpipeBind1(1, 22, column_r19.property)));
    ɵɵadvance(3);
    ɵɵclassProp("po-table-header-fixed-inner", ctx_r1.height)("po-table-header-flex-right", column_r19.type === "currency" || column_r19.type === "number")("po-table-header-flex-center", column_r19.type === "subtitle");
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", contentHeaderTemplate_r17)("ngTemplateOutletContext", ɵɵpureFunction1(30, _c21, column_r19));
  }
}
function PoTableComponent_ng_template_10_Conditional_9_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵrepeaterCreate(0, PoTableComponent_ng_template_10_Conditional_9_For_1_Template, 5, 32, "th", 71, ɵɵcomponentInstance().trackBy, true);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵrepeater(ctx_r1.mainColumns);
  }
}
function PoTableComponent_ng_template_10_Conditional_10_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "th", 56);
  }
}
function PoTableComponent_ng_template_10_Conditional_11_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "th");
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵclassProp("po-table-header-single-action", ctx_r1.isSingleAction)("po-table-header-actions", !ctx_r1.isSingleAction);
  }
}
function PoTableComponent_ng_template_10_Conditional_12_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "tbody", 60)(1, "tr", 73)(2, "td", 74)(3, "span");
    ɵɵtext(4);
    ɵɵelementEnd()()()();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵadvance(2);
    ɵɵproperty("colSpan", ctx_r1.columnCount);
    ɵɵadvance(2);
    ɵɵtextInterpolate1(" ", ctx_r1.literals.noData, " ");
  }
}
function PoTableComponent_ng_template_10_Conditional_13_For_1_Conditional_2_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoTableComponent_ng_template_10_Conditional_13_For_1_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "td", 61);
    ɵɵtemplate(1, PoTableComponent_ng_template_10_Conditional_13_For_1_Conditional_2_ng_container_1_Template, 1, 0, "ng-container", 70);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const row_r20 = ɵɵnextContext().$implicit;
    const ctx_r1 = ɵɵnextContext(3);
    const inputRadio_r21 = ɵɵreference(19);
    const inputCheckbox_r22 = ɵɵreference(21);
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", ctx_r1.singleSelect ? inputRadio_r21 : inputCheckbox_r22)("ngTemplateOutletContext", ɵɵpureFunction1(2, _c21, row_r20));
  }
}
function PoTableComponent_ng_template_10_Conditional_13_For_1_Conditional_3_ng_template_1_Template(rf, ctx) {
}
function PoTableComponent_ng_template_10_Conditional_13_For_1_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    const _r23 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "td", 79);
    ɵɵlistener("click", function PoTableComponent_ng_template_10_Conditional_13_For_1_Conditional_3_Template_td_click_0_listener() {
      ɵɵrestoreView(_r23);
      const row_r20 = ɵɵnextContext().$implicit;
      const ctx_r1 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r1.toggleDetail(row_r20));
    });
    ɵɵtemplate(1, PoTableComponent_ng_template_10_Conditional_13_For_1_Conditional_3_ng_template_1_Template, 0, 0, "ng-template", 77);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r23 = ɵɵnextContext();
    const row_r20 = ctx_r23.$implicit;
    const ɵ$index_210_r25 = ctx_r23.$index;
    ɵɵnextContext(3);
    const poTableColumnDetail_r26 = ɵɵreference(17);
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", poTableColumnDetail_r26)("ngTemplateOutletContext", ɵɵpureFunction2(2, _c114, row_r20, ɵ$index_210_r25));
  }
}
function PoTableComponent_ng_template_10_Conditional_13_For_1_Conditional_4_ng_template_0_Template(rf, ctx) {
}
function PoTableComponent_ng_template_10_Conditional_13_For_1_Conditional_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, PoTableComponent_ng_template_10_Conditional_13_For_1_Conditional_4_ng_template_0_Template, 0, 0, "ng-template", 77);
  }
  if (rf & 2) {
    const ctx_r23 = ɵɵnextContext();
    const row_r20 = ctx_r23.$implicit;
    const ɵ$index_210_r25 = ctx_r23.$index;
    ɵɵnextContext(3);
    const ActionsColumnTemplate_r27 = ɵɵreference(25);
    ɵɵproperty("ngTemplateOutlet", ActionsColumnTemplate_r27)("ngTemplateOutletContext", ɵɵpureFunction2(2, _c114, row_r20, ɵ$index_210_r25));
  }
}
function PoTableComponent_ng_template_10_Conditional_13_For_1_For_6_Case_2_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoTableComponent_ng_template_10_Conditional_13_For_1_For_6_Case_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span");
    ɵɵtemplate(1, PoTableComponent_ng_template_10_Conditional_13_For_1_For_6_Case_2_ng_container_1_Template, 1, 0, "ng-container", 70);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const column_r29 = ɵɵnextContext().$implicit;
    const row_r20 = ɵɵnextContext().$implicit;
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", ctx_r1.getTemplate(column_r29))("ngTemplateOutletContext", ɵɵpureFunction1(2, _c21, ctx_r1.getCellData(row_r20, column_r29)));
  }
}
function PoTableComponent_ng_template_10_Conditional_13_For_1_For_6_Case_3_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoTableComponent_ng_template_10_Conditional_13_For_1_For_6_Case_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span");
    ɵɵtemplate(1, PoTableComponent_ng_template_10_Conditional_13_For_1_For_6_Case_3_ng_container_1_Template, 1, 0, "ng-container", 70);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const column_r29 = ɵɵnextContext().$implicit;
    const row_r20 = ɵɵnextContext().$implicit;
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", ctx_r1.tableCellTemplate == null ? null : ctx_r1.tableCellTemplate.templateRef)("ngTemplateOutletContext", ɵɵpureFunction2(2, _c115, row_r20, column_r29));
  }
}
function PoTableComponent_ng_template_10_Conditional_13_For_1_For_6_Case_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span");
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const column_r29 = ɵɵnextContext().$implicit;
    const row_r20 = ɵɵnextContext().$implicit;
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ctx_r1.getBooleanLabel(ctx_r1.getCellData(row_r20, column_r29), column_r29), " ");
  }
}
function PoTableComponent_ng_template_10_Conditional_13_For_1_For_6_Case_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span");
    ɵɵtext(1);
    ɵɵpipe(2, "currency");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const column_r29 = ɵɵnextContext().$implicit;
    const row_r20 = ɵɵnextContext().$implicit;
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ɵɵpipeBind4(2, 1, ctx_r1.getCellData(row_r20, column_r29), column_r29.format, "symbol", "1.2-2"), " ");
  }
}
function PoTableComponent_ng_template_10_Conditional_13_For_1_For_6_Case_6_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span");
    ɵɵtext(1);
    ɵɵpipe(2, "date");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const column_r29 = ɵɵnextContext().$implicit;
    const row_r20 = ɵɵnextContext().$implicit;
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ɵɵpipeBind2(2, 1, ctx_r1.getCellData(row_r20, column_r29), column_r29.format || "dd/MM/yyyy"), " ");
  }
}
function PoTableComponent_ng_template_10_Conditional_13_For_1_For_6_Case_7_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span");
    ɵɵtext(1);
    ɵɵpipe(2, "po_time");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const column_r29 = ɵɵnextContext().$implicit;
    const row_r20 = ɵɵnextContext().$implicit;
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ɵɵpipeBind2(2, 1, ctx_r1.getCellData(row_r20, column_r29), column_r29.format || "HH:mm:ss.ffffff"), " ");
  }
}
function PoTableComponent_ng_template_10_Conditional_13_For_1_For_6_Case_8_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span");
    ɵɵtext(1);
    ɵɵpipe(2, "date");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const column_r29 = ɵɵnextContext().$implicit;
    const row_r20 = ɵɵnextContext().$implicit;
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ɵɵpipeBind2(2, 1, ctx_r1.getCellData(row_r20, column_r29), column_r29.format || "dd/MM/yyyy HH:mm:ss"), " ");
  }
}
function PoTableComponent_ng_template_10_Conditional_13_For_1_For_6_Case_9_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span");
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const column_r29 = ɵɵnextContext().$implicit;
    const row_r20 = ɵɵnextContext().$implicit;
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ctx_r1.formatNumber(ctx_r1.getCellData(row_r20, column_r29), column_r29.format), " ");
  }
}
function PoTableComponent_ng_template_10_Conditional_13_For_1_For_6_Case_10_Template(rf, ctx) {
  if (rf & 1) {
    const _r30 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-table-column-link", 84);
    ɵɵlistener("click", function PoTableComponent_ng_template_10_Conditional_13_For_1_For_6_Case_10_Template_po_table_column_link_click_0_listener($event) {
      ɵɵrestoreView(_r30);
      const column_r29 = ɵɵnextContext().$implicit;
      const row_r20 = ɵɵnextContext().$implicit;
      const ctx_r1 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r1.onClickLink($event, row_r20, column_r29));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const column_r29 = ɵɵnextContext().$implicit;
    const row_r20 = ɵɵnextContext().$implicit;
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵproperty("p-action", column_r29.action)("p-disabled", ctx_r1.checkDisabled(row_r20, column_r29))("p-link", row_r20[column_r29.link])("p-row", row_r20)("p-value", ctx_r1.getCellData(row_r20, column_r29));
  }
}
function PoTableComponent_ng_template_10_Conditional_13_For_1_For_6_Case_11_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-table-column-icon", 83);
  }
  if (rf & 2) {
    const column_r29 = ɵɵnextContext().$implicit;
    const row_r20 = ɵɵnextContext().$implicit;
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵproperty("p-column", column_r29)("p-icons", ctx_r1.getColumnIcons(row_r20, column_r29))("p-row", row_r20);
  }
}
function PoTableComponent_ng_template_10_Conditional_13_For_1_For_6_Case_12_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span");
    ɵɵelement(1, "po-table-subtitle-circle", 85);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const column_r29 = ɵɵnextContext().$implicit;
    const row_r20 = ɵɵnextContext().$implicit;
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵadvance();
    ɵɵproperty("p-subtitle", ctx_r1.getSubtitleColumn(row_r20, column_r29));
  }
}
function PoTableComponent_ng_template_10_Conditional_13_For_1_For_6_Case_13_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span");
    ɵɵelement(1, "po-table-column-label", 86);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const column_r29 = ɵɵnextContext().$implicit;
    const row_r20 = ɵɵnextContext().$implicit;
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵadvance();
    ɵɵproperty("p-value", ctx_r1.getColumnLabel(row_r20, column_r29));
  }
}
function PoTableComponent_ng_template_10_Conditional_13_For_1_For_6_Case_14_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span");
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const column_r29 = ɵɵnextContext().$implicit;
    const row_r20 = ɵɵnextContext().$implicit;
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵadvance();
    ɵɵtextInterpolate(column_r29.mask ? ctx_r1.formatWithMask(ctx_r1.getCellData(row_r20, column_r29), column_r29.mask) : ctx_r1.getCellData(row_r20, column_r29));
  }
}
function PoTableComponent_ng_template_10_Conditional_13_For_1_For_6_Template(rf, ctx) {
  if (rf & 1) {
    const _r28 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "td", 80);
    ɵɵlistener("click", function PoTableComponent_ng_template_10_Conditional_13_For_1_For_6_Template_td_click_0_listener() {
      ɵɵrestoreView(_r28);
      const row_r20 = ɵɵnextContext().$implicit;
      const ctx_r1 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r1.hasSelectableRow() ? ctx_r1.selectRow(row_r20) : "javascript:;");
    });
    ɵɵelementStart(1, "div", 81);
    ɵɵlistener("mouseenter", function PoTableComponent_ng_template_10_Conditional_13_For_1_For_6_Template_div_mouseenter_1_listener($event) {
      const column_r29 = ɵɵrestoreView(_r28).$implicit;
      const row_r20 = ɵɵnextContext().$implicit;
      const ctx_r1 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r1.tooltipMouseEnter($event, column_r29, row_r20));
    })("mouseleave", function PoTableComponent_ng_template_10_Conditional_13_For_1_For_6_Template_div_mouseleave_1_listener() {
      ɵɵrestoreView(_r28);
      const ctx_r1 = ɵɵnextContext(4);
      return ɵɵresetView(ctx_r1.tooltipMouseLeave());
    });
    ɵɵconditionalCreate(2, PoTableComponent_ng_template_10_Conditional_13_For_1_For_6_Case_2_Template, 2, 4, "span")(3, PoTableComponent_ng_template_10_Conditional_13_For_1_For_6_Case_3_Template, 2, 5, "span")(4, PoTableComponent_ng_template_10_Conditional_13_For_1_For_6_Case_4_Template, 2, 1, "span")(5, PoTableComponent_ng_template_10_Conditional_13_For_1_For_6_Case_5_Template, 3, 6, "span")(6, PoTableComponent_ng_template_10_Conditional_13_For_1_For_6_Case_6_Template, 3, 4, "span")(7, PoTableComponent_ng_template_10_Conditional_13_For_1_For_6_Case_7_Template, 3, 4, "span")(8, PoTableComponent_ng_template_10_Conditional_13_For_1_For_6_Case_8_Template, 3, 4, "span")(9, PoTableComponent_ng_template_10_Conditional_13_For_1_For_6_Case_9_Template, 2, 1, "span")(10, PoTableComponent_ng_template_10_Conditional_13_For_1_For_6_Case_10_Template, 1, 5, "po-table-column-link", 82)(11, PoTableComponent_ng_template_10_Conditional_13_For_1_For_6_Case_11_Template, 1, 3, "po-table-column-icon", 83)(12, PoTableComponent_ng_template_10_Conditional_13_For_1_For_6_Case_12_Template, 2, 1, "span")(13, PoTableComponent_ng_template_10_Conditional_13_For_1_For_6_Case_13_Template, 2, 1, "span")(14, PoTableComponent_ng_template_10_Conditional_13_For_1_For_6_Case_14_Template, 2, 1, "span");
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    let tmp_46_0;
    const column_r29 = ctx.$implicit;
    const row_r20 = ɵɵnextContext().$implicit;
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵstyleProp("width", column_r29.width)("max-width", column_r29.width)("min-width", column_r29.width);
    ɵɵclassProp("po-table-column", column_r29.type !== "icon")("po-table-column-right", column_r29.type === "currency" || column_r29.type === "number")("po-table-column-center", column_r29.type === "subtitle")("po-table-column-icons", column_r29.type === "icon");
    ɵɵproperty("pFrozenColumn", column_r29.fixed)("ngClass", ctx_r1.getClassColor(row_r20, column_r29));
    ɵɵadvance();
    ɵɵproperty("p-tooltip", ctx_r1.tooltipText)("p-append-in-body", true);
    ɵɵadvance();
    ɵɵconditional((tmp_46_0 = column_r29.type) === "columnTemplate" ? 2 : tmp_46_0 === "cellTemplate" ? 3 : tmp_46_0 === "boolean" ? 4 : tmp_46_0 === "currency" ? 5 : tmp_46_0 === "date" ? 6 : tmp_46_0 === "time" ? 7 : tmp_46_0 === "dateTime" ? 8 : tmp_46_0 === "number" ? 9 : tmp_46_0 === "link" ? 10 : tmp_46_0 === "icon" ? 11 : tmp_46_0 === "subtitle" ? 12 : tmp_46_0 === "label" ? 13 : 14);
  }
}
function PoTableComponent_ng_template_10_Conditional_13_For_1_Conditional_7_ng_template_1_Template(rf, ctx) {
}
function PoTableComponent_ng_template_10_Conditional_13_For_1_Conditional_7_Template(rf, ctx) {
  if (rf & 1) {
    const _r31 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "td", 79);
    ɵɵlistener("click", function PoTableComponent_ng_template_10_Conditional_13_For_1_Conditional_7_Template_td_click_0_listener() {
      ɵɵrestoreView(_r31);
      const row_r20 = ɵɵnextContext().$implicit;
      const ctx_r1 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r1.toggleDetail(row_r20));
    });
    ɵɵtemplate(1, PoTableComponent_ng_template_10_Conditional_13_For_1_Conditional_7_ng_template_1_Template, 0, 0, "ng-template", 77);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r23 = ɵɵnextContext();
    const row_r20 = ctx_r23.$implicit;
    const ɵ$index_210_r25 = ctx_r23.$index;
    ɵɵnextContext(3);
    const poTableColumnDetail_r26 = ɵɵreference(17);
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", poTableColumnDetail_r26)("ngTemplateOutletContext", ɵɵpureFunction2(2, _c114, row_r20, ɵ$index_210_r25));
  }
}
function PoTableComponent_ng_template_10_Conditional_13_For_1_Conditional_8_ng_template_0_Template(rf, ctx) {
}
function PoTableComponent_ng_template_10_Conditional_13_For_1_Conditional_8_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, PoTableComponent_ng_template_10_Conditional_13_For_1_Conditional_8_ng_template_0_Template, 0, 0, "ng-template", 77);
  }
  if (rf & 2) {
    const ctx_r23 = ɵɵnextContext();
    const row_r20 = ctx_r23.$implicit;
    const ɵ$index_210_r25 = ctx_r23.$index;
    ɵɵnextContext(3);
    const ActionsColumnTemplate_r27 = ɵɵreference(25);
    ɵɵproperty("ngTemplateOutlet", ActionsColumnTemplate_r27)("ngTemplateOutletContext", ɵɵpureFunction2(2, _c114, row_r20, ɵ$index_210_r25));
  }
}
function PoTableComponent_ng_template_10_Conditional_13_For_1_Conditional_9_ng_template_2_Template(rf, ctx) {
}
function PoTableComponent_ng_template_10_Conditional_13_For_1_Conditional_9_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "tr")(1, "td", 87);
    ɵɵtemplate(2, PoTableComponent_ng_template_10_Conditional_13_For_1_Conditional_9_ng_template_2_Template, 0, 0, "ng-template", 77);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r23 = ɵɵnextContext();
    const row_r20 = ctx_r23.$implicit;
    const ɵ$index_210_r25 = ctx_r23.$index;
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵadvance();
    ɵɵproperty("colSpan", ctx_r1.columnCountForMasterDetail);
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", ctx_r1.tableRowTemplate.templateRef)("ngTemplateOutletContext", ɵɵpureFunction2(3, _c116, row_r20, ɵ$index_210_r25));
  }
}
function PoTableComponent_ng_template_10_Conditional_13_For_1_Conditional_10_Template(rf, ctx) {
  if (rf & 1) {
    const _r32 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "tr")(1, "td", 88)(2, "po-table-detail", 89);
    ɵɵlistener("p-select-row", function PoTableComponent_ng_template_10_Conditional_13_For_1_Conditional_10_Template_po_table_detail_p_select_row_2_listener($event) {
      ɵɵrestoreView(_r32);
      const ctx_r1 = ɵɵnextContext(4);
      return ɵɵresetView(ctx_r1.selectDetailRow($event));
    });
    ɵɵelementEnd()()();
  }
  if (rf & 2) {
    const row_r20 = ɵɵnextContext().$implicit;
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵadvance();
    ɵɵproperty("colSpan", ctx_r1.columnCountForMasterDetail);
    ɵɵadvance();
    ɵɵproperty("p-selectable", ctx_r1.selectable && !ctx_r1.detailHideSelect)("p-detail", ctx_r1.columnMasterDetail.detail)("p-items", row_r20[ctx_r1.nameColumnDetail])("p-parent-row", row_r20)("p-components-size", ctx_r1.componentsSize);
  }
}
function PoTableComponent_ng_template_10_Conditional_13_For_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "tbody", 60)(1, "tr", 75);
    ɵɵconditionalCreate(2, PoTableComponent_ng_template_10_Conditional_13_For_1_Conditional_2_Template, 2, 4, "td", 61);
    ɵɵconditionalCreate(3, PoTableComponent_ng_template_10_Conditional_13_For_1_Conditional_3_Template, 2, 5, "td", 76);
    ɵɵconditionalCreate(4, PoTableComponent_ng_template_10_Conditional_13_For_1_Conditional_4_Template, 1, 5, null, 77);
    ɵɵrepeaterCreate(5, PoTableComponent_ng_template_10_Conditional_13_For_1_For_6_Template, 15, 19, "td", 78, ɵɵcomponentInstance().trackBy, true);
    ɵɵconditionalCreate(7, PoTableComponent_ng_template_10_Conditional_13_For_1_Conditional_7_Template, 2, 5, "td", 76);
    ɵɵconditionalCreate(8, PoTableComponent_ng_template_10_Conditional_13_For_1_Conditional_8_Template, 1, 5, null, 77);
    ɵɵelementEnd();
    ɵɵconditionalCreate(9, PoTableComponent_ng_template_10_Conditional_13_For_1_Conditional_9_Template, 3, 6, "tr");
    ɵɵconditionalCreate(10, PoTableComponent_ng_template_10_Conditional_13_For_1_Conditional_10_Template, 3, 6, "tr");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const row_r20 = ctx.$implicit;
    const ɵ$index_210_r25 = ctx.$index;
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵadvance();
    ɵɵclassProp("po-table-row-active", row_r20.$selected || row_r20.$selected === null && ctx_r1.selectable);
    ɵɵadvance();
    ɵɵconditional(ctx_r1.selectable ? 2 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r1.columnMasterDetail && !ctx_r1.hideDetail && !ctx_r1.hasRowTemplate || ctx_r1.hasRowTemplate && !ctx_r1.hasRowTemplateWithArrowDirectionRight ? 3 : -1);
    ɵɵadvance();
    ɵɵconditional(!ctx_r1.actionRight && (ctx_r1.visibleActions.length > 1 || ctx_r1.isSingleAction) ? 4 : -1);
    ɵɵadvance();
    ɵɵrepeater(ctx_r1.mainColumns);
    ɵɵadvance(2);
    ɵɵconditional(ctx_r1.hasRowTemplateWithArrowDirectionRight ? 7 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r1.actionRight ? 8 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r1.hasMainColumns && ctx_r1.hasRowTemplate && row_r20.$showDetail && ctx_r1.isShowRowTemplate(row_r20, ɵ$index_210_r25) ? 9 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r1.hasMainColumns && ctx_r1.isShowMasterDetail(row_r20) ? 10 : -1);
  }
}
function PoTableComponent_ng_template_10_Conditional_13_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵrepeaterCreate(0, PoTableComponent_ng_template_10_Conditional_13_For_1_Template, 11, 9, "tbody", 60, ɵɵcomponentInstance().trackBy, true);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵrepeater(ctx_r1.filteredItems);
  }
}
function PoTableComponent_ng_template_10_Template(rf, ctx) {
  if (rf & 1) {
    const _r13 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "table", 53, 16)(2, "thead")(3, "tr", 54);
    ɵɵlistener("cdkDropListDropped", function PoTableComponent_ng_template_10_Template_tr_cdkDropListDropped_3_listener($event) {
      ɵɵrestoreView(_r13);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.drop($event));
    });
    ɵɵconditionalCreate(4, PoTableComponent_ng_template_10_Conditional_4_Template, 3, 5, "th", 55);
    ɵɵconditionalCreate(5, PoTableComponent_ng_template_10_Conditional_5_Template, 1, 0, "th", 56);
    ɵɵconditionalCreate(6, PoTableComponent_ng_template_10_Conditional_6_Template, 2, 4, "th", 57);
    ɵɵconditionalCreate(7, PoTableComponent_ng_template_10_Conditional_7_Template, 4, 2, "th", 58);
    ɵɵconditionalCreate(8, PoTableComponent_ng_template_10_Conditional_8_Template, 2, 0)(9, PoTableComponent_ng_template_10_Conditional_9_Template, 2, 0);
    ɵɵconditionalCreate(10, PoTableComponent_ng_template_10_Conditional_10_Template, 1, 0, "th", 56);
    ɵɵconditionalCreate(11, PoTableComponent_ng_template_10_Conditional_11_Template, 1, 4, "th", 59);
    ɵɵelementEnd()();
    ɵɵconditionalCreate(12, PoTableComponent_ng_template_10_Conditional_12_Template, 5, 2, "tbody", 60);
    ɵɵconditionalCreate(13, PoTableComponent_ng_template_10_Conditional_13_Template, 2, 0);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("ngClass", ɵɵpureFunction5(17, _c111, ctx_r1.selectable || ctx_r1.sort, ctx_r1.selectable, ctx_r1.striped, ctx_r1.applyFixedColumns(), ctx_r1.textWrap));
    ɵɵattribute("p-spacing", ctx_r1.spacing);
    ɵɵadvance(2);
    ɵɵclassProp("po-table-header-sticky", ctx_r1.height > 0 && !ctx_r1.virtualScroll);
    ɵɵattribute("p-spacing", ctx_r1.spacing ?? (ctx_r1.componentsSize === "small" ? "extraSmall" : "medium"));
    ɵɵadvance();
    ɵɵclassProp("po-table-header", !ctx_r1.height);
    ɵɵproperty("ngClass", !ctx_r1.height ? ɵɵpureFunction2(23, _c112, ctx_r1.hideSelectAll, ctx_r1.isDraggable) : "");
    ɵɵadvance();
    ɵɵconditional(ctx_r1.hasSelectableColumn ? 4 : -1);
    ɵɵadvance();
    ɵɵconditional((ctx_r1.hasMasterDetailColumn || ctx_r1.hasRowTemplate) && ctx_r1.hasMainColumns && !ctx_r1.hasRowTemplateWithArrowDirectionRight ? 5 : -1);
    ɵɵadvance();
    ɵɵconditional(!ctx_r1.actionRight && ctx_r1.hasItems && ctx_r1.hasMainColumns && (ctx_r1.visibleActions.length > 1 || ctx_r1.isSingleAction) ? 6 : -1);
    ɵɵadvance();
    ɵɵconditional(!ctx_r1.hasMainColumns ? 7 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r1.isDraggable || ctx_r1.hasSomeFixed() ? 8 : 9);
    ɵɵadvance(2);
    ɵɵconditional(ctx_r1.hasRowTemplateWithArrowDirectionRight && (ctx_r1.hasVisibleActions || ctx_r1.hideColumnsManager) && ctx_r1.hasMainColumns ? 10 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r1.hasVisibleActions && ctx_r1.actionRight && ctx_r1.hasItems && ctx_r1.hasMainColumns && (ctx_r1.visibleActions.length > 1 || ctx_r1.isSingleAction) ? 11 : -1);
    ɵɵadvance();
    ɵɵconditional(!ctx_r1.hasItems || !ctx_r1.hasMainColumns ? 12 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r1.hasMainColumns ? 13 : -1);
  }
}
function PoTableComponent_ng_template_12_Conditional_5_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    const _r34 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-checkbox", 94);
    ɵɵlistener("p-change", function PoTableComponent_ng_template_12_Conditional_5_Conditional_2_Template_po_checkbox_p_change_0_listener() {
      ɵɵrestoreView(_r34);
      const ctx_r1 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r1.selectAllRows());
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵproperty("p-checkboxValue", ctx_r1.selectAll === null ? "mixed" : ctx_r1.selectAll)("p-size", ctx_r1.componentsSize);
  }
}
function PoTableComponent_ng_template_12_Conditional_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "th", 61)(1, "div");
    ɵɵconditionalCreate(2, PoTableComponent_ng_template_12_Conditional_5_Conditional_2_Template, 1, 2, "po-checkbox", 93);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵstyleProp("pointer-events", ctx_r1.hideSelectAll ? "none" : "auto");
    ɵɵadvance();
    ɵɵclassProp("po-table-header-fixed-inner", ctx_r1.height);
    ɵɵadvance();
    ɵɵconditional(!ctx_r1.hideSelectAll ? 2 : -1);
  }
}
function PoTableComponent_ng_template_12_Conditional_6_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "th", 56);
  }
}
function PoTableComponent_ng_template_12_Conditional_7_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "th", null, 17);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵclassProp("po-table-header-master-detail", !ctx_r1.isSingleAction)("po-table-header-single-action", ctx_r1.isSingleAction);
  }
}
function PoTableComponent_ng_template_12_Conditional_8_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 65);
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵstyleProp("width", ctx_r1.hasValidColumns ? ctx_r1.headerWidth : null, "px");
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ctx_r1.hasValidColumns ? ctx_r1.literals.noVisibleColumn : ctx_r1.literals.noColumns, " ");
  }
}
function PoTableComponent_ng_template_12_Conditional_8_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtext(0);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵtextInterpolate1(" ", ctx_r1.hasValidColumns ? ctx_r1.literals.noVisibleColumn : ctx_r1.literals.noColumns, " ");
  }
}
function PoTableComponent_ng_template_12_Conditional_8_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "th", 58, 18);
    ɵɵconditionalCreate(2, PoTableComponent_ng_template_12_Conditional_8_Conditional_2_Template, 2, 3, "div", 64)(3, PoTableComponent_ng_template_12_Conditional_8_Conditional_3_Template, 1, 1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵattribute("colspan", ctx_r1.columnCount);
    ɵɵadvance(2);
    ɵɵconditional(ctx_r1.height ? 2 : 3);
  }
}
function PoTableComponent_ng_template_12_Conditional_9_For_1_Conditional_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-icon", 69);
  }
}
function PoTableComponent_ng_template_12_Conditional_9_For_1_ng_container_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoTableComponent_ng_template_12_Conditional_9_For_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r35 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "th", 67);
    ɵɵpipe(1, "titlecase");
    ɵɵpipe(2, "lowercase");
    ɵɵlistener("click", function PoTableComponent_ng_template_12_Conditional_9_For_1_Template_th_click_0_listener() {
      const column_r36 = ɵɵrestoreView(_r35).$implicit;
      const ctx_r1 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r1.sortColumn(column_r36));
    });
    ɵɵelementStart(3, "div", 68);
    ɵɵconditionalCreate(4, PoTableComponent_ng_template_12_Conditional_9_For_1_Conditional_4_Template, 1, 0, "po-icon", 69);
    ɵɵtemplate(5, PoTableComponent_ng_template_12_Conditional_9_For_1_ng_container_5_Template, 1, 0, "ng-container", 70);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const column_r36 = ctx.$implicit;
    const ctx_r1 = ɵɵnextContext(3);
    const contentHeaderTemplate_r17 = ɵɵreference(23);
    ɵɵstyleProp("width", column_r36.width)("max-width", column_r36.width)("min-width", column_r36.width);
    ɵɵclassProp("po-clickable", ctx_r1.sort && column_r36.sortable !== false || ctx_r1.hasService)("po-table-header-subtitle", column_r36.type === "subtitle")("po-table-column-drag-box", ctx_r1.isDraggable);
    ɵɵproperty("ngClass", ɵɵpureFunction1(30, _c113, ctx_r1.sort && ctx_r1.JSON.stringify(ctx_r1.sortedColumn == null ? null : ctx_r1.sortedColumn.property) === ctx_r1.JSON.stringify(column_r36) && (ctx_r1.sortedColumn.ascending || !ctx_r1.sortedColumn.ascending)))("ngStyle", ɵɵpureFunction1(32, _c86, !ctx_r1.hasItems ? "100%" : ctx_r1.applyFixedColumns() ? column_r36.width : "auto"))("cdkDragDisabled", column_r36.fixed ? "true" : "false")("pFrozenColumn", column_r36.fixed);
    ɵɵattribute("data-po-table-column-name", ɵɵpipeBind1(2, 28, column_r36.label || ɵɵpipeBind1(1, 26, column_r36.property)));
    ɵɵadvance(3);
    ɵɵclassProp("po-table-header-fixed-inner", ctx_r1.height)("po-table-header-flex-right", column_r36.type === "currency" || column_r36.type === "number")("po-table-header-flex-center", column_r36.type === "subtitle");
    ɵɵadvance();
    ɵɵconditional(ctx_r1.isDraggable && !column_r36.fixed ? 4 : -1);
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", contentHeaderTemplate_r17)("ngTemplateOutletContext", ɵɵpureFunction1(34, _c21, column_r36));
  }
}
function PoTableComponent_ng_template_12_Conditional_9_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵrepeaterCreate(0, PoTableComponent_ng_template_12_Conditional_9_For_1_Template, 6, 36, "th", 66, ɵɵcomponentInstance().trackBy, true);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵrepeater(ctx_r1.mainColumns);
  }
}
function PoTableComponent_ng_template_12_Conditional_10_For_1_ng_container_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoTableComponent_ng_template_12_Conditional_10_For_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r37 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "th", 96);
    ɵɵpipe(1, "titlecase");
    ɵɵpipe(2, "lowercase");
    ɵɵlistener("click", function PoTableComponent_ng_template_12_Conditional_10_For_1_Template_th_click_0_listener() {
      const column_r38 = ɵɵrestoreView(_r37).$implicit;
      const ctx_r1 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r1.sortColumn(column_r38));
    });
    ɵɵelementStart(3, "div", 68);
    ɵɵtemplate(4, PoTableComponent_ng_template_12_Conditional_10_For_1_ng_container_4_Template, 1, 0, "ng-container", 70);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const column_r38 = ctx.$implicit;
    const ctx_r1 = ɵɵnextContext(3);
    const contentHeaderTemplate_r17 = ɵɵreference(23);
    ɵɵstyleProp("width", column_r38.width)("max-width", column_r38.width)("min-width", column_r38.width);
    ɵɵclassProp("po-clickable", ctx_r1.sort && column_r38.sortable !== false || ctx_r1.hasService)("po-table-header-subtitle", column_r38.type === "subtitle");
    ɵɵproperty("ngClass", ɵɵpureFunction1(26, _c113, ctx_r1.sort && ctx_r1.JSON.stringify(ctx_r1.sortedColumn == null ? null : ctx_r1.sortedColumn.property) === ctx_r1.JSON.stringify(column_r38) && (ctx_r1.sortedColumn.ascending || !ctx_r1.sortedColumn.ascending)))("ngStyle", ɵɵpureFunction1(28, _c86, !ctx_r1.hasItems ? "100%" : ctx_r1.applyFixedColumns() ? column_r38.width : "auto"))("pFrozenColumn", column_r38.fixed);
    ɵɵattribute("data-po-table-column-name", ɵɵpipeBind1(2, 24, column_r38.label || ɵɵpipeBind1(1, 22, column_r38.property)));
    ɵɵadvance(3);
    ɵɵclassProp("po-table-header-fixed-inner", ctx_r1.height)("po-table-header-flex-right", column_r38.type === "currency" || column_r38.type === "number")("po-table-header-flex-center", column_r38.type === "subtitle");
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", contentHeaderTemplate_r17)("ngTemplateOutletContext", ɵɵpureFunction1(30, _c21, column_r38));
  }
}
function PoTableComponent_ng_template_12_Conditional_10_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵrepeaterCreate(0, PoTableComponent_ng_template_12_Conditional_10_For_1_Template, 5, 32, "th", 95, ɵɵcomponentInstance().trackBy, true);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵrepeater(ctx_r1.mainColumns);
  }
}
function PoTableComponent_ng_template_12_Conditional_11_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "th", 56);
  }
}
function PoTableComponent_ng_template_12_Conditional_12_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "th");
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵclassProp("po-table-header-single-action", ctx_r1.isSingleAction)("po-table-header-actions", !ctx_r1.isSingleAction);
  }
}
function PoTableComponent_ng_template_12_Conditional_13_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "tbody", 60)(1, "tr", 73)(2, "td", 74)(3, "span");
    ɵɵtext(4);
    ɵɵelementEnd()()()();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵadvance(2);
    ɵɵproperty("colSpan", ctx_r1.columnCount);
    ɵɵadvance(2);
    ɵɵtextInterpolate1(" ", ctx_r1.literals.noData, " ");
  }
}
function PoTableComponent_ng_template_12_Conditional_14_tbody_0_Conditional_2_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoTableComponent_ng_template_12_Conditional_14_tbody_0_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "td", 61);
    ɵɵtemplate(1, PoTableComponent_ng_template_12_Conditional_14_tbody_0_Conditional_2_ng_container_1_Template, 1, 0, "ng-container", 70);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const row_r39 = ɵɵnextContext().$implicit;
    const ctx_r1 = ɵɵnextContext(3);
    const inputRadio_r21 = ɵɵreference(19);
    const inputCheckbox_r22 = ɵɵreference(21);
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", ctx_r1.singleSelect ? inputRadio_r21 : inputCheckbox_r22)("ngTemplateOutletContext", ɵɵpureFunction1(2, _c21, row_r39));
  }
}
function PoTableComponent_ng_template_12_Conditional_14_tbody_0_Conditional_3_ng_template_1_Template(rf, ctx) {
}
function PoTableComponent_ng_template_12_Conditional_14_tbody_0_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    const _r40 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "td", 79);
    ɵɵlistener("click", function PoTableComponent_ng_template_12_Conditional_14_tbody_0_Conditional_3_Template_td_click_0_listener() {
      ɵɵrestoreView(_r40);
      const row_r39 = ɵɵnextContext().$implicit;
      const ctx_r1 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r1.toggleDetail(row_r39));
    });
    ɵɵtemplate(1, PoTableComponent_ng_template_12_Conditional_14_tbody_0_Conditional_3_ng_template_1_Template, 0, 0, "ng-template", 77);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r40 = ɵɵnextContext();
    const row_r39 = ctx_r40.$implicit;
    const rowIndex_r42 = ctx_r40.index;
    ɵɵnextContext(3);
    const poTableColumnDetail_r26 = ɵɵreference(17);
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", poTableColumnDetail_r26)("ngTemplateOutletContext", ɵɵpureFunction2(2, _c114, row_r39, rowIndex_r42));
  }
}
function PoTableComponent_ng_template_12_Conditional_14_tbody_0_Conditional_4_ng_template_0_Template(rf, ctx) {
}
function PoTableComponent_ng_template_12_Conditional_14_tbody_0_Conditional_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, PoTableComponent_ng_template_12_Conditional_14_tbody_0_Conditional_4_ng_template_0_Template, 0, 0, "ng-template", 77);
  }
  if (rf & 2) {
    const ctx_r40 = ɵɵnextContext();
    const row_r39 = ctx_r40.$implicit;
    const rowIndex_r42 = ctx_r40.index;
    ɵɵnextContext(3);
    const ActionsColumnTemplate_r27 = ɵɵreference(25);
    ɵɵproperty("ngTemplateOutlet", ActionsColumnTemplate_r27)("ngTemplateOutletContext", ɵɵpureFunction2(2, _c114, row_r39, rowIndex_r42));
  }
}
function PoTableComponent_ng_template_12_Conditional_14_tbody_0_For_6_Case_2_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoTableComponent_ng_template_12_Conditional_14_tbody_0_For_6_Case_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span");
    ɵɵtemplate(1, PoTableComponent_ng_template_12_Conditional_14_tbody_0_For_6_Case_2_ng_container_1_Template, 1, 0, "ng-container", 70);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const column_r44 = ɵɵnextContext().$implicit;
    const row_r39 = ɵɵnextContext().$implicit;
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", ctx_r1.getTemplate(column_r44))("ngTemplateOutletContext", ɵɵpureFunction1(2, _c21, ctx_r1.getCellData(row_r39, column_r44)));
  }
}
function PoTableComponent_ng_template_12_Conditional_14_tbody_0_For_6_Case_3_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoTableComponent_ng_template_12_Conditional_14_tbody_0_For_6_Case_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span");
    ɵɵtemplate(1, PoTableComponent_ng_template_12_Conditional_14_tbody_0_For_6_Case_3_ng_container_1_Template, 1, 0, "ng-container", 70);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const column_r44 = ɵɵnextContext().$implicit;
    const row_r39 = ɵɵnextContext().$implicit;
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", ctx_r1.tableCellTemplate == null ? null : ctx_r1.tableCellTemplate.templateRef)("ngTemplateOutletContext", ɵɵpureFunction2(2, _c115, row_r39, column_r44));
  }
}
function PoTableComponent_ng_template_12_Conditional_14_tbody_0_For_6_Case_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span");
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const column_r44 = ɵɵnextContext().$implicit;
    const row_r39 = ɵɵnextContext().$implicit;
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ctx_r1.getBooleanLabel(ctx_r1.getCellData(row_r39, column_r44), column_r44), " ");
  }
}
function PoTableComponent_ng_template_12_Conditional_14_tbody_0_For_6_Case_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span");
    ɵɵtext(1);
    ɵɵpipe(2, "currency");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const column_r44 = ɵɵnextContext().$implicit;
    const row_r39 = ɵɵnextContext().$implicit;
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ɵɵpipeBind4(2, 1, ctx_r1.getCellData(row_r39, column_r44), column_r44.format, "symbol", "1.2-2"), " ");
  }
}
function PoTableComponent_ng_template_12_Conditional_14_tbody_0_For_6_Case_6_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span");
    ɵɵtext(1);
    ɵɵpipe(2, "date");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const column_r44 = ɵɵnextContext().$implicit;
    const row_r39 = ɵɵnextContext().$implicit;
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ɵɵpipeBind2(2, 1, ctx_r1.getCellData(row_r39, column_r44), column_r44.format || "dd/MM/yyyy"), " ");
  }
}
function PoTableComponent_ng_template_12_Conditional_14_tbody_0_For_6_Case_7_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span");
    ɵɵtext(1);
    ɵɵpipe(2, "po_time");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const column_r44 = ɵɵnextContext().$implicit;
    const row_r39 = ɵɵnextContext().$implicit;
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ɵɵpipeBind2(2, 1, ctx_r1.getCellData(row_r39, column_r44), column_r44.format || "HH:mm:ss.ffffff"), " ");
  }
}
function PoTableComponent_ng_template_12_Conditional_14_tbody_0_For_6_Case_8_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span");
    ɵɵtext(1);
    ɵɵpipe(2, "date");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const column_r44 = ɵɵnextContext().$implicit;
    const row_r39 = ɵɵnextContext().$implicit;
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ɵɵpipeBind2(2, 1, ctx_r1.getCellData(row_r39, column_r44), column_r44.format || "dd/MM/yyyy HH:mm:ss"), " ");
  }
}
function PoTableComponent_ng_template_12_Conditional_14_tbody_0_For_6_Case_9_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span");
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const column_r44 = ɵɵnextContext().$implicit;
    const row_r39 = ɵɵnextContext().$implicit;
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ctx_r1.formatNumber(ctx_r1.getCellData(row_r39, column_r44), column_r44.format), " ");
  }
}
function PoTableComponent_ng_template_12_Conditional_14_tbody_0_For_6_Case_10_Template(rf, ctx) {
  if (rf & 1) {
    const _r45 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-table-column-link", 84);
    ɵɵlistener("click", function PoTableComponent_ng_template_12_Conditional_14_tbody_0_For_6_Case_10_Template_po_table_column_link_click_0_listener($event) {
      ɵɵrestoreView(_r45);
      const column_r44 = ɵɵnextContext().$implicit;
      const row_r39 = ɵɵnextContext().$implicit;
      const ctx_r1 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r1.onClickLink($event, row_r39, column_r44));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const column_r44 = ɵɵnextContext().$implicit;
    const row_r39 = ɵɵnextContext().$implicit;
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵproperty("p-action", column_r44.action)("p-disabled", ctx_r1.checkDisabled(row_r39, column_r44))("p-link", row_r39[column_r44.link])("p-row", row_r39)("p-value", ctx_r1.getCellData(row_r39, column_r44));
  }
}
function PoTableComponent_ng_template_12_Conditional_14_tbody_0_For_6_Case_11_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-table-column-icon", 83);
  }
  if (rf & 2) {
    const column_r44 = ɵɵnextContext().$implicit;
    const row_r39 = ɵɵnextContext().$implicit;
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵproperty("p-column", column_r44)("p-icons", ctx_r1.getColumnIcons(row_r39, column_r44))("p-row", row_r39);
  }
}
function PoTableComponent_ng_template_12_Conditional_14_tbody_0_For_6_Case_12_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span");
    ɵɵelement(1, "po-table-subtitle-circle", 85);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const column_r44 = ɵɵnextContext().$implicit;
    const row_r39 = ɵɵnextContext().$implicit;
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵadvance();
    ɵɵproperty("p-subtitle", ctx_r1.getSubtitleColumn(row_r39, column_r44));
  }
}
function PoTableComponent_ng_template_12_Conditional_14_tbody_0_For_6_Case_13_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span");
    ɵɵelement(1, "po-table-column-label", 86);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const column_r44 = ɵɵnextContext().$implicit;
    const row_r39 = ɵɵnextContext().$implicit;
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵadvance();
    ɵɵproperty("p-value", ctx_r1.getColumnLabel(row_r39, column_r44));
  }
}
function PoTableComponent_ng_template_12_Conditional_14_tbody_0_For_6_Case_14_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span");
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const column_r44 = ɵɵnextContext().$implicit;
    const row_r39 = ɵɵnextContext().$implicit;
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵadvance();
    ɵɵtextInterpolate(column_r44.mask ? ctx_r1.formatWithMask(ctx_r1.getCellData(row_r39, column_r44), column_r44.mask) : ctx_r1.getCellData(row_r39, column_r44));
  }
}
function PoTableComponent_ng_template_12_Conditional_14_tbody_0_For_6_Template(rf, ctx) {
  if (rf & 1) {
    const _r43 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "td", 99);
    ɵɵlistener("click", function PoTableComponent_ng_template_12_Conditional_14_tbody_0_For_6_Template_td_click_0_listener() {
      ɵɵrestoreView(_r43);
      const row_r39 = ɵɵnextContext().$implicit;
      const ctx_r1 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r1.hasSelectableRow() ? ctx_r1.selectRow(row_r39) : "javascript:;");
    });
    ɵɵelementStart(1, "div", 81);
    ɵɵlistener("mouseenter", function PoTableComponent_ng_template_12_Conditional_14_tbody_0_For_6_Template_div_mouseenter_1_listener($event) {
      const column_r44 = ɵɵrestoreView(_r43).$implicit;
      const row_r39 = ɵɵnextContext().$implicit;
      const ctx_r1 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r1.tooltipMouseEnter($event, column_r44, row_r39));
    })("mouseleave", function PoTableComponent_ng_template_12_Conditional_14_tbody_0_For_6_Template_div_mouseleave_1_listener() {
      ɵɵrestoreView(_r43);
      const ctx_r1 = ɵɵnextContext(4);
      return ɵɵresetView(ctx_r1.tooltipMouseLeave());
    });
    ɵɵconditionalCreate(2, PoTableComponent_ng_template_12_Conditional_14_tbody_0_For_6_Case_2_Template, 2, 4, "span")(3, PoTableComponent_ng_template_12_Conditional_14_tbody_0_For_6_Case_3_Template, 2, 5, "span")(4, PoTableComponent_ng_template_12_Conditional_14_tbody_0_For_6_Case_4_Template, 2, 1, "span")(5, PoTableComponent_ng_template_12_Conditional_14_tbody_0_For_6_Case_5_Template, 3, 6, "span")(6, PoTableComponent_ng_template_12_Conditional_14_tbody_0_For_6_Case_6_Template, 3, 4, "span")(7, PoTableComponent_ng_template_12_Conditional_14_tbody_0_For_6_Case_7_Template, 3, 4, "span")(8, PoTableComponent_ng_template_12_Conditional_14_tbody_0_For_6_Case_8_Template, 3, 4, "span")(9, PoTableComponent_ng_template_12_Conditional_14_tbody_0_For_6_Case_9_Template, 2, 1, "span")(10, PoTableComponent_ng_template_12_Conditional_14_tbody_0_For_6_Case_10_Template, 1, 5, "po-table-column-link", 82)(11, PoTableComponent_ng_template_12_Conditional_14_tbody_0_For_6_Case_11_Template, 1, 3, "po-table-column-icon", 83)(12, PoTableComponent_ng_template_12_Conditional_14_tbody_0_For_6_Case_12_Template, 2, 1, "span")(13, PoTableComponent_ng_template_12_Conditional_14_tbody_0_For_6_Case_13_Template, 2, 1, "span")(14, PoTableComponent_ng_template_12_Conditional_14_tbody_0_For_6_Case_14_Template, 2, 1, "span");
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    let tmp_38_0;
    const column_r44 = ctx.$implicit;
    const row_r39 = ɵɵnextContext().$implicit;
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵstyleProp("width", column_r44.width)("max-width", column_r44.width)("min-width", column_r44.width);
    ɵɵclassProp("po-table-column", column_r44.type !== "icon")("po-table-column-right", column_r44.type === "currency" || column_r44.type === "number")("po-table-column-center", column_r44.type === "subtitle")("po-table-column-icons", column_r44.type === "icon");
    ɵɵproperty("ngClass", ctx_r1.getClassColor(row_r39, column_r44))("pFrozenColumn", column_r44.fixed);
    ɵɵadvance();
    ɵɵproperty("p-tooltip", ctx_r1.tooltipText)("p-append-in-body", true);
    ɵɵadvance();
    ɵɵconditional((tmp_38_0 = column_r44.type) === "columnTemplate" ? 2 : tmp_38_0 === "cellTemplate" ? 3 : tmp_38_0 === "boolean" ? 4 : tmp_38_0 === "currency" ? 5 : tmp_38_0 === "date" ? 6 : tmp_38_0 === "time" ? 7 : tmp_38_0 === "dateTime" ? 8 : tmp_38_0 === "number" ? 9 : tmp_38_0 === "link" ? 10 : tmp_38_0 === "icon" ? 11 : tmp_38_0 === "subtitle" ? 12 : tmp_38_0 === "label" ? 13 : 14);
  }
}
function PoTableComponent_ng_template_12_Conditional_14_tbody_0_Conditional_7_ng_template_1_Template(rf, ctx) {
}
function PoTableComponent_ng_template_12_Conditional_14_tbody_0_Conditional_7_Template(rf, ctx) {
  if (rf & 1) {
    const _r46 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "td", 79);
    ɵɵlistener("click", function PoTableComponent_ng_template_12_Conditional_14_tbody_0_Conditional_7_Template_td_click_0_listener() {
      ɵɵrestoreView(_r46);
      const row_r39 = ɵɵnextContext().$implicit;
      const ctx_r1 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r1.toggleDetail(row_r39));
    });
    ɵɵtemplate(1, PoTableComponent_ng_template_12_Conditional_14_tbody_0_Conditional_7_ng_template_1_Template, 0, 0, "ng-template", 77);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r40 = ɵɵnextContext();
    const row_r39 = ctx_r40.$implicit;
    const rowIndex_r42 = ctx_r40.index;
    ɵɵnextContext(3);
    const poTableColumnDetail_r26 = ɵɵreference(17);
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", poTableColumnDetail_r26)("ngTemplateOutletContext", ɵɵpureFunction2(2, _c114, row_r39, rowIndex_r42));
  }
}
function PoTableComponent_ng_template_12_Conditional_14_tbody_0_Conditional_8_ng_template_0_Template(rf, ctx) {
}
function PoTableComponent_ng_template_12_Conditional_14_tbody_0_Conditional_8_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, PoTableComponent_ng_template_12_Conditional_14_tbody_0_Conditional_8_ng_template_0_Template, 0, 0, "ng-template", 77);
  }
  if (rf & 2) {
    const ctx_r40 = ɵɵnextContext();
    const row_r39 = ctx_r40.$implicit;
    const rowIndex_r42 = ctx_r40.index;
    ɵɵnextContext(3);
    const ActionsColumnTemplate_r27 = ɵɵreference(25);
    ɵɵproperty("ngTemplateOutlet", ActionsColumnTemplate_r27)("ngTemplateOutletContext", ɵɵpureFunction2(2, _c114, row_r39, rowIndex_r42));
  }
}
function PoTableComponent_ng_template_12_Conditional_14_tbody_0_Conditional_9_ng_template_2_Template(rf, ctx) {
}
function PoTableComponent_ng_template_12_Conditional_14_tbody_0_Conditional_9_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "tr")(1, "td", 87);
    ɵɵtemplate(2, PoTableComponent_ng_template_12_Conditional_14_tbody_0_Conditional_9_ng_template_2_Template, 0, 0, "ng-template", 77);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r40 = ɵɵnextContext();
    const row_r39 = ctx_r40.$implicit;
    const rowIndex_r42 = ctx_r40.index;
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵadvance();
    ɵɵproperty("colSpan", ctx_r1.columnCountForMasterDetail);
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", ctx_r1.tableRowTemplate.templateRef)("ngTemplateOutletContext", ɵɵpureFunction2(3, _c116, row_r39, rowIndex_r42));
  }
}
function PoTableComponent_ng_template_12_Conditional_14_tbody_0_Conditional_10_Template(rf, ctx) {
  if (rf & 1) {
    const _r47 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "tr")(1, "td", 88)(2, "po-table-detail", 89);
    ɵɵlistener("p-select-row", function PoTableComponent_ng_template_12_Conditional_14_tbody_0_Conditional_10_Template_po_table_detail_p_select_row_2_listener($event) {
      ɵɵrestoreView(_r47);
      const ctx_r1 = ɵɵnextContext(4);
      return ɵɵresetView(ctx_r1.selectDetailRow($event));
    });
    ɵɵelementEnd()()();
  }
  if (rf & 2) {
    const row_r39 = ɵɵnextContext().$implicit;
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵadvance();
    ɵɵproperty("colSpan", ctx_r1.columnCountForMasterDetail);
    ɵɵadvance();
    ɵɵproperty("p-selectable", ctx_r1.selectable && !ctx_r1.detailHideSelect)("p-detail", ctx_r1.columnMasterDetail.detail)("p-items", row_r39[ctx_r1.nameColumnDetail])("p-parent-row", row_r39)("p-components-size", ctx_r1.componentsSize);
  }
}
function PoTableComponent_ng_template_12_Conditional_14_tbody_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "tbody", 60)(1, "tr", 75);
    ɵɵconditionalCreate(2, PoTableComponent_ng_template_12_Conditional_14_tbody_0_Conditional_2_Template, 2, 4, "td", 61);
    ɵɵconditionalCreate(3, PoTableComponent_ng_template_12_Conditional_14_tbody_0_Conditional_3_Template, 2, 5, "td", 76);
    ɵɵconditionalCreate(4, PoTableComponent_ng_template_12_Conditional_14_tbody_0_Conditional_4_Template, 1, 5, null, 77);
    ɵɵrepeaterCreate(5, PoTableComponent_ng_template_12_Conditional_14_tbody_0_For_6_Template, 15, 19, "td", 98, ɵɵcomponentInstance().trackBy, true);
    ɵɵconditionalCreate(7, PoTableComponent_ng_template_12_Conditional_14_tbody_0_Conditional_7_Template, 2, 5, "td", 76);
    ɵɵconditionalCreate(8, PoTableComponent_ng_template_12_Conditional_14_tbody_0_Conditional_8_Template, 1, 5, null, 77);
    ɵɵelementEnd();
    ɵɵconditionalCreate(9, PoTableComponent_ng_template_12_Conditional_14_tbody_0_Conditional_9_Template, 3, 6, "tr");
    ɵɵconditionalCreate(10, PoTableComponent_ng_template_12_Conditional_14_tbody_0_Conditional_10_Template, 3, 6, "tr");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const row_r39 = ctx.$implicit;
    const rowIndex_r42 = ctx.index;
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵadvance();
    ɵɵclassProp("po-table-row-active", row_r39.$selected || row_r39.$selected === null && ctx_r1.selectable);
    ɵɵadvance();
    ɵɵconditional(ctx_r1.selectable ? 2 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r1.columnMasterDetail && !ctx_r1.hideDetail && !ctx_r1.hasRowTemplate || ctx_r1.hasRowTemplate && !ctx_r1.hasRowTemplateWithArrowDirectionRight ? 3 : -1);
    ɵɵadvance();
    ɵɵconditional(!ctx_r1.actionRight && (ctx_r1.visibleActions.length > 1 || ctx_r1.isSingleAction) ? 4 : -1);
    ɵɵadvance();
    ɵɵrepeater(ctx_r1.mainColumns);
    ɵɵadvance(2);
    ɵɵconditional(ctx_r1.hasRowTemplateWithArrowDirectionRight ? 7 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r1.actionRight ? 8 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r1.hasMainColumns && ctx_r1.hasRowTemplate && row_r39.$showDetail && ctx_r1.isShowRowTemplate(row_r39, rowIndex_r42) ? 9 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r1.hasMainColumns && ctx_r1.isShowMasterDetail(row_r39) ? 10 : -1);
  }
}
function PoTableComponent_ng_template_12_Conditional_14_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, PoTableComponent_ng_template_12_Conditional_14_tbody_0_Template, 11, 9, "tbody", 97);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵproperty("cdkVirtualForOf", ctx_r1.filteredItems)("cdkVirtualForTrackBy", ctx_r1.trackBy);
  }
}
function PoTableComponent_ng_template_12_Template(rf, ctx) {
  if (rf & 1) {
    const _r33 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "cdk-virtual-scroll-viewport", 90, 19)(2, "table", 53)(3, "thead", 91)(4, "tr", 92);
    ɵɵlistener("cdkDropListDropped", function PoTableComponent_ng_template_12_Template_tr_cdkDropListDropped_4_listener($event) {
      ɵɵrestoreView(_r33);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.drop($event));
    });
    ɵɵconditionalCreate(5, PoTableComponent_ng_template_12_Conditional_5_Template, 3, 5, "th", 55);
    ɵɵconditionalCreate(6, PoTableComponent_ng_template_12_Conditional_6_Template, 1, 0, "th", 56);
    ɵɵconditionalCreate(7, PoTableComponent_ng_template_12_Conditional_7_Template, 2, 4, "th", 57);
    ɵɵconditionalCreate(8, PoTableComponent_ng_template_12_Conditional_8_Template, 4, 2, "th", 58);
    ɵɵconditionalCreate(9, PoTableComponent_ng_template_12_Conditional_9_Template, 2, 0)(10, PoTableComponent_ng_template_12_Conditional_10_Template, 2, 0);
    ɵɵconditionalCreate(11, PoTableComponent_ng_template_12_Conditional_11_Template, 1, 0, "th", 56);
    ɵɵconditionalCreate(12, PoTableComponent_ng_template_12_Conditional_12_Template, 1, 4, "th", 59);
    ɵɵelementEnd()();
    ɵɵconditionalCreate(13, PoTableComponent_ng_template_12_Conditional_13_Template, 5, 2, "tbody", 60);
    ɵɵconditionalCreate(14, PoTableComponent_ng_template_12_Conditional_14_Template, 1, 2, "tbody", 60);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵstyleProp("height", ctx_r1.heightTableContainer, "px");
    ɵɵproperty("itemSize", ctx_r1.itemSize)("minBufferPx", ctx_r1.heightTableContainer < 100 ? 100 : ctx_r1.heightTableContainer)("maxBufferPx", ctx_r1.heightTableContainer < 200 ? 200 : ctx_r1.heightTableContainer);
    ɵɵadvance(2);
    ɵɵproperty("ngClass", ɵɵpureFunction5(21, _c111, ctx_r1.selectable || ctx_r1.sort, ctx_r1.selectable, ctx_r1.striped, ctx_r1.applyFixedColumns(), ctx_r1.textWrap));
    ɵɵattribute("p-spacing", ctx_r1.spacing);
    ɵɵadvance();
    ɵɵstyleProp("top", ctx_r1.inverseOfTranslation);
    ɵɵattribute("p-spacing", ctx_r1.spacing ?? (ctx_r1.componentsSize === "small" ? "extraSmall" : "medium"));
    ɵɵadvance();
    ɵɵclassProp("po-table-header", !ctx_r1.height);
    ɵɵadvance();
    ɵɵconditional(ctx_r1.hasSelectableColumn ? 5 : -1);
    ɵɵadvance();
    ɵɵconditional((ctx_r1.hasMasterDetailColumn || ctx_r1.hasRowTemplate) && ctx_r1.hasMainColumns && !ctx_r1.hasRowTemplateWithArrowDirectionRight ? 6 : -1);
    ɵɵadvance();
    ɵɵconditional(!ctx_r1.actionRight && ctx_r1.hasItems && ctx_r1.hasMainColumns && (ctx_r1.visibleActions.length > 1 || ctx_r1.isSingleAction) ? 7 : -1);
    ɵɵadvance();
    ɵɵconditional(!ctx_r1.hasMainColumns ? 8 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r1.isDraggable || ctx_r1.hasSomeFixed() ? 9 : 10);
    ɵɵadvance(2);
    ɵɵconditional(ctx_r1.hasRowTemplateWithArrowDirectionRight && ctx_r1.hasMainColumns && (ctx_r1.hasVisibleActions || ctx_r1.hideColumnsManager) ? 11 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r1.hasVisibleActions && ctx_r1.actionRight && ctx_r1.hasItems && ctx_r1.hasMainColumns && (ctx_r1.visibleActions.length > 1 || ctx_r1.isSingleAction) ? 12 : -1);
    ɵɵadvance();
    ɵɵconditional(!ctx_r1.hasItems || !ctx_r1.hasMainColumns ? 13 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r1.hasMainColumns ? 14 : -1);
  }
}
function PoTableComponent_ng_template_16_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-icon", 100);
  }
  if (rf & 2) {
    const row_r48 = ɵɵnextContext().row;
    ɵɵproperty("p-icon", row_r48.$showDetail ? "ICON_ARROW_UP" : "ICON_ARROW_DOWN");
  }
}
function PoTableComponent_ng_template_16_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵconditionalCreate(0, PoTableComponent_ng_template_16_Conditional_0_Template, 1, 1, "po-icon", 100);
  }
  if (rf & 2) {
    const row_r48 = ctx.row;
    const rowIndex_r49 = ctx.rowIndex;
    const ctx_r1 = ɵɵnextContext();
    ɵɵconditional(ctx_r1.containsMasterDetail(row_r48) && !ctx_r1.hasRowTemplate || ctx_r1.isShowRowTemplate(row_r48, rowIndex_r49) && ctx_r1.hasRowTemplate ? 0 : -1);
  }
}
function PoTableComponent_ng_template_18_Template(rf, ctx) {
  if (rf & 1) {
    const _r50 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-radio", 101);
    ɵɵlistener("p-change-selected", function PoTableComponent_ng_template_18_Template_po_radio_p_change_selected_0_listener() {
      const row_r51 = ɵɵrestoreView(_r50).$implicit;
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.selectRow(row_r51));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const row_r51 = ctx.$implicit;
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("name", ctx_r1.idRadio)("p-checked", row_r51.$selected)("p-size", ctx_r1.componentsSize);
  }
}
function PoTableComponent_ng_template_20_Template(rf, ctx) {
  if (rf & 1) {
    const _r52 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-checkbox", 102);
    ɵɵlistener("p-change", function PoTableComponent_ng_template_20_Template_po_checkbox_p_change_0_listener() {
      const row_r53 = ɵɵrestoreView(_r52).$implicit;
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.selectable ? ctx_r1.selectRow(row_r53) : "javascript:;");
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const row_r53 = ctx.$implicit;
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("p-checkboxValue", row_r53.$selected === null ? "mixed" : row_r53.$selected)("p-size", ctx_r1.componentsSize);
  }
}
function PoTableComponent_ng_template_22_Conditional_4_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-icon", 105);
  }
}
function PoTableComponent_ng_template_22_Conditional_4_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-icon", 106);
  }
}
function PoTableComponent_ng_template_22_Conditional_4_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-icon", 107);
  }
}
function PoTableComponent_ng_template_22_Conditional_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span");
    ɵɵconditionalCreate(1, PoTableComponent_ng_template_22_Conditional_4_Conditional_1_Template, 1, 0, "po-icon", 105);
    ɵɵconditionalCreate(2, PoTableComponent_ng_template_22_Conditional_4_Conditional_2_Template, 1, 0, "po-icon", 106);
    ɵɵconditionalCreate(3, PoTableComponent_ng_template_22_Conditional_4_Conditional_3_Template, 1, 0, "po-icon", 107);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const column_r55 = ɵɵnextContext().$implicit;
    const ctx_r1 = ɵɵnextContext();
    ɵɵclassProp("po-table-header-icon-unselected", ctx_r1.JSON.stringify(ctx_r1.sortedColumn == null ? null : ctx_r1.sortedColumn.property) !== ctx_r1.JSON.stringify(column_r55))("po-table-header-icon-descending", ctx_r1.JSON.stringify(ctx_r1.sortedColumn == null ? null : ctx_r1.sortedColumn.property) === ctx_r1.JSON.stringify(column_r55) && ctx_r1.sortedColumn.ascending)("po-table-header-icon-ascending", ctx_r1.JSON.stringify(ctx_r1.sortedColumn == null ? null : ctx_r1.sortedColumn.property) === ctx_r1.JSON.stringify(column_r55) && !ctx_r1.sortedColumn.ascending);
    ɵɵadvance();
    ɵɵconditional(ctx_r1.JSON.stringify(ctx_r1.sortedColumn == null ? null : ctx_r1.sortedColumn.property) !== ctx_r1.JSON.stringify(column_r55) ? 1 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r1.JSON.stringify(ctx_r1.sortedColumn == null ? null : ctx_r1.sortedColumn.property) === ctx_r1.JSON.stringify(column_r55) && ctx_r1.sortedColumn.ascending ? 2 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r1.JSON.stringify(ctx_r1.sortedColumn == null ? null : ctx_r1.sortedColumn.property) === ctx_r1.JSON.stringify(column_r55) && !ctx_r1.sortedColumn.ascending ? 3 : -1);
  }
}
function PoTableComponent_ng_template_22_Template(rf, ctx) {
  if (rf & 1) {
    const _r54 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "span", 103, 20);
    ɵɵlistener("mouseenter", function PoTableComponent_ng_template_22_Template_span_mouseenter_0_listener($event) {
      ɵɵrestoreView(_r54);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.tooltipMouseEnter($event));
    })("mouseleave", function PoTableComponent_ng_template_22_Template_span_mouseleave_0_listener() {
      ɵɵrestoreView(_r54);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.tooltipMouseLeave());
    });
    ɵɵtext(2);
    ɵɵpipe(3, "titlecase");
    ɵɵelementEnd();
    ɵɵconditionalCreate(4, PoTableComponent_ng_template_22_Conditional_4_Template, 4, 9, "span", 104);
  }
  if (rf & 2) {
    const column_r55 = ctx.$implicit;
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("p-tooltip", ctx_r1.tooltipText)("p-append-in-body", true);
    ɵɵadvance(2);
    ɵɵtextInterpolate1(" ", column_r55.label || ɵɵpipeBind1(3, 4, column_r55.property), " ");
    ɵɵadvance(2);
    ɵɵconditional(ctx_r1.sort && column_r55.sortable !== false ? 4 : -1);
  }
}
function PoTableComponent_ng_template_24_Conditional_0_Conditional_1_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-icon", 113);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(4);
    ɵɵproperty("p-icon", ctx_r1.firstAction.icon);
  }
}
function PoTableComponent_ng_template_24_Conditional_0_Conditional_1_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span");
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(4);
    ɵɵadvance();
    ɵɵtextInterpolate(ctx_r1.firstAction.label);
  }
}
function PoTableComponent_ng_template_24_Conditional_0_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r56 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 112);
    ɵɵlistener("click", function PoTableComponent_ng_template_24_Conditional_0_Conditional_1_Template_div_click_0_listener() {
      ɵɵrestoreView(_r56);
      const row_r57 = ɵɵnextContext(2).row;
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.executeTableAction(row_r57, ctx_r1.firstAction));
    });
    ɵɵconditionalCreate(1, PoTableComponent_ng_template_24_Conditional_0_Conditional_1_Conditional_1_Template, 1, 1, "po-icon", 113);
    ɵɵconditionalCreate(2, PoTableComponent_ng_template_24_Conditional_0_Conditional_1_Conditional_2_Template, 2, 1, "span");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const row_r57 = ɵɵnextContext(2).row;
    const ctx_r1 = ɵɵnextContext();
    ɵɵclassProp("po-table-action-disabled", ctx_r1.firstAction.disabled ? ctx_r1.validateTableAction(row_r57, ctx_r1.firstAction) : false);
    ɵɵadvance();
    ɵɵconditional(ctx_r1.firstAction.icon ? 1 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r1.firstAction.label ? 2 : -1);
  }
}
function PoTableComponent_ng_template_24_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "td", 110);
    ɵɵconditionalCreate(1, PoTableComponent_ng_template_24_Conditional_0_Conditional_1_Template, 3, 4, "div", 111);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵstyleProp("width", ctx_r1.height && ctx_r1.actionRight ? ctx_r1.getWidthColumnManager() : "", "px")("max-width", ctx_r1.height && !ctx_r1.actionRight ? ctx_r1.getColumnWidthActionsLeft() : "", "px")("width", ctx_r1.height && !ctx_r1.actionRight ? ctx_r1.getColumnWidthActionsLeft() : "", "px");
    ɵɵadvance();
    ɵɵconditional(ctx_r1.firstAction.visible !== false ? 1 : -1);
  }
}
function PoTableComponent_ng_template_24_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r58 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "td", 109)(1, "div", 114, 21);
    ɵɵlistener("click", function PoTableComponent_ng_template_24_Conditional_1_Template_div_click_1_listener() {
      ɵɵrestoreView(_r58);
      const popupTarget_r59 = ɵɵreference(2);
      const row_r57 = ɵɵnextContext().row;
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.togglePopup(row_r57, popupTarget_r59));
    });
    ɵɵelement(3, "po-icon", 115);
    ɵɵelementEnd()();
  }
}
function PoTableComponent_ng_template_24_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵconditionalCreate(0, PoTableComponent_ng_template_24_Conditional_0_Template, 2, 7, "td", 108);
    ɵɵconditionalCreate(1, PoTableComponent_ng_template_24_Conditional_1_Template, 4, 0, "td", 109);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵconditional(ctx_r1.isSingleAction ? 0 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r1.visibleActions.length > 1 ? 1 : -1);
  }
}
function PoTableComponent_Conditional_26_Template(rf, ctx) {
  if (rf & 1) {
    const _r60 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-table-column-manager", 116);
    ɵɵlistener("p-visible-columns-change", function PoTableComponent_Conditional_26_Template_po_table_column_manager_p_visible_columns_change_0_listener($event) {
      ɵɵrestoreView(_r60);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onVisibleColumnsChange($event));
    })("p-change-visible-columns", function PoTableComponent_Conditional_26_Template_po_table_column_manager_p_change_visible_columns_0_listener($event) {
      ɵɵrestoreView(_r60);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onChangeVisibleColumns($event));
    })("p-change-fixed-columns", function PoTableComponent_Conditional_26_Template_po_table_column_manager_p_change_fixed_columns_0_listener($event) {
      ɵɵrestoreView(_r60);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onChangeFixedColumns($event));
    })("p-initial-columns", function PoTableComponent_Conditional_26_Template_po_table_column_manager_p_initial_columns_0_listener($event) {
      ɵɵrestoreView(_r60);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onColumnRestoreManager($event));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("p-columns", ctx_r1.columns)("p-max-columns", ctx_r1.maxColumns)("p-target", ctx_r1.columnManagerTarget)("p-last-visible-columns-selected", ctx_r1.lastVisibleColumnsSelected)("p-hide-action-fixed-columns", ctx_r1.hideActionFixedColumns)("p-columns-default", ctx_r1.initialColumns)("p-components-size", ctx_r1.componentsSize);
  }
}
var _c117 = ["dateRangeField"];
var _c118 = ["endDateInput"];
var _c119 = ["startDateInput"];
var _c120 = ["calendarPicker"];
function PoDatepickerRangeComponent_Conditional_15_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-loading-icon", 18);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("p-size", ctx_r0.mapSizeToIcon(ctx_r0.size));
  }
}
function PoDatepickerRangeComponent_Conditional_16_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r3 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-clean", 24, 6);
    ɵɵlistener("p-change-event", function PoDatepickerRangeComponent_Conditional_16_Conditional_1_Template_po_clean_p_change_event_0_listener() {
      ɵɵrestoreView(_r3);
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.clear());
    })("keydown.enter", function PoDatepickerRangeComponent_Conditional_16_Conditional_1_Template_po_clean_keydown_enter_0_listener($event) {
      ɵɵrestoreView(_r3);
      const ctx_r0 = ɵɵnextContext(2);
      ctx_r0.clearAndFocus();
      return ɵɵresetView($event.preventDefault());
    })("keydown.space", function PoDatepickerRangeComponent_Conditional_16_Conditional_1_Template_po_clean_keydown_space_0_listener($event) {
      ɵɵrestoreView(_r3);
      const ctx_r0 = ɵɵnextContext(2);
      ctx_r0.clearAndFocus();
      return ɵɵresetView($event.preventDefault());
    });
    ɵɵelementEnd();
    ɵɵelement(2, "div", 25);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵproperty("p-size", ctx_r0.size);
    ɵɵattribute("aria-label", ctx_r0.literals.clean);
  }
}
function PoDatepickerRangeComponent_Conditional_16_Template(rf, ctx) {
  if (rf & 1) {
    const _r2 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 19);
    ɵɵconditionalCreate(1, PoDatepickerRangeComponent_Conditional_16_Conditional_1_Template, 3, 2);
    ɵɵelementStart(2, "po-button", 23, 5);
    ɵɵlistener("keydown", function PoDatepickerRangeComponent_Conditional_16_Template_po_button_keydown_2_listener($event) {
      ɵɵrestoreView(_r2);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.onKeyPress($event));
    })("p-click", function PoDatepickerRangeComponent_Conditional_16_Template_po_button_p_click_2_listener() {
      ɵɵrestoreView(_r2);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.toggleCalendar());
    });
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵconditional(ctx_r0.enableCleaner ? 1 : -1);
    ɵɵadvance();
    ɵɵclassProp("po-button-tertiary-danger", ctx_r0.getErrorMessage);
    ɵɵproperty("p-disabled", ctx_r0.isDisabled || ctx_r0.readonly)("p-size", ctx_r0.size);
  }
}
function PoDatepickerRangeComponent_Conditional_17_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-helper", 20, 7);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    const poHelper_r4 = ɵɵreadContextLet(0);
    ɵɵproperty("p-size", ctx_r0.size)("p-helper", poHelper_r4.helperSettings)("p-disabled", ctx_r0.isDisabled)("p-append-in-body", ctx_r0.appendBox);
  }
}
function PoDatepickerRangeComponent_Conditional_18_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-field-container-bottom", 21);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("p-append-in-body", ctx_r0.appendBox)("p-help", ctx_r0.help)("p-disabled", ctx_r0.isDisabled)("p-error-pattern", ctx_r0.getErrorMessage)("p-error-limit", ctx_r0.errorLimit)("p-size", ctx_r0.size);
  }
}
function PoDatepickerRangeComponent_Conditional_19_ng_template_0_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoDatepickerRangeComponent_Conditional_19_ng_template_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, PoDatepickerRangeComponent_Conditional_19_ng_template_0_ng_container_0_Template, 1, 0, "ng-container", 26);
  }
  if (rf & 2) {
    ɵɵnextContext(2);
    const sharedCalendarContent_r5 = ɵɵreference(22);
    ɵɵproperty("ngTemplateOutlet", sharedCalendarContent_r5);
  }
}
function PoDatepickerRangeComponent_Conditional_19_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, PoDatepickerRangeComponent_Conditional_19_ng_template_0_Template, 1, 1, "ng-template", 22);
  }
  if (rf & 2) {
    ɵɵnextContext();
    const trigger_r6 = ɵɵreference(3);
    ɵɵproperty("cdkConnectedOverlayOrigin", trigger_r6)("cdkConnectedOverlayOpen", true)("cdkConnectedOverlayDisableClose", true);
  }
}
function PoDatepickerRangeComponent_Conditional_20_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoDatepickerRangeComponent_Conditional_20_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, PoDatepickerRangeComponent_Conditional_20_ng_container_0_Template, 1, 0, "ng-container", 26);
  }
  if (rf & 2) {
    ɵɵnextContext();
    const sharedCalendarContent_r5 = ɵɵreference(22);
    ɵɵproperty("ngTemplateOutlet", sharedCalendarContent_r5);
  }
}
function PoDatepickerRangeComponent_ng_template_21_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    const _r8 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 30);
    ɵɵlistener("click", function PoDatepickerRangeComponent_ng_template_21_Conditional_2_Template_div_click_0_listener() {
      ɵɵrestoreView(_r8);
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.closeCalendar());
    });
    ɵɵelementEnd();
  }
}
function PoDatepickerRangeComponent_ng_template_21_Template(rf, ctx) {
  if (rf & 1) {
    const _r7 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 27, 8);
    ɵɵconditionalCreate(2, PoDatepickerRangeComponent_ng_template_21_Conditional_2_Template, 1, 0, "div", 28);
    ɵɵelementStart(3, "po-calendar", 29);
    ɵɵlistener("ngModelChange", function PoDatepickerRangeComponent_ng_template_21_Template_po_calendar_ngModelChange_3_listener($event) {
      ɵɵrestoreView(_r7);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.onCalendarChange($event));
    })("p-change-month-year", function PoDatepickerRangeComponent_ng_template_21_Template_po_calendar_p_change_month_year_3_listener() {
      ɵɵrestoreView(_r7);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.setCalendarPosition());
    })("keydown", function PoDatepickerRangeComponent_ng_template_21_Template_po_calendar_keydown_3_listener($event) {
      ɵɵrestoreView(_r7);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.onCalendarKeyDown($event));
    })("p-close", function PoDatepickerRangeComponent_ng_template_21_Template_po_calendar_p_close_3_listener() {
      ɵɵrestoreView(_r7);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.closeCalendar());
    });
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵclassProp("po-calendar-range-picker", !ctx_r0.verifyMobile())("po-datepicker-range-calendar-mobile-container", ctx_r0.verifyMobile() && ctx_r0.isCalendarVisible);
    ɵɵproperty("hidden", !ctx_r0.isCalendarVisible);
    ɵɵadvance(2);
    ɵɵconditional(ctx_r0.verifyMobile() ? 2 : -1);
    ɵɵadvance();
    ɵɵclassProp("po-datepicker-range-calendar-mobile", ctx_r0.verifyMobile());
    ɵɵproperty("ngModel", ctx_r0.dateRange)("p-max-date", ctx_r0.maxDate)("p-min-date", ctx_r0.minDate)("p-locale", ctx_r0.locale)("p-size", ctx_r0.size)("p-range-presets", ctx_r0.rangePresets)("p-range-preset-options", ctx_r0.rangePresetOptions)("p-range-presets-order", ctx_r0.rangePresetsOrder)("p-width-with-presets", ctx_r0.widthWithPresets);
  }
}
var _c121 = (a0, a1, a2, a3) => ({
  "po-input-icon-left": a0,
  "po-input-icon-left-aa": a1,
  "po-input-icon-right": a2,
  "po-input-icon-right-aa": a3
});
function PoDecimalComponent_Conditional_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 6);
    ɵɵelement(1, "po-icon", 13);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵclassProp("po-field-icon-disabled", ctx_r0.isDisabled)("po-field-icon-aa", ctx_r0.size === "small");
    ɵɵproperty("p-icon", ctx_r0.icon);
  }
}
function PoDecimalComponent_Conditional_9_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-loading-icon", 9);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("p-size", ctx_r0.mapSizeToIcon(ctx_r0.size));
  }
}
function PoDecimalComponent_Conditional_10_Template(rf, ctx) {
  if (rf & 1) {
    const _r2 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-clean", 14);
    ɵɵlistener("p-change-event", function PoDecimalComponent_Conditional_10_Template_po_clean_p_change_event_0_listener($event) {
      ɵɵrestoreView(_r2);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.clear($event));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("p-element-ref", ctx_r0.inputEl)("p-size", ctx_r0.size);
  }
}
function PoDecimalComponent_Conditional_11_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-helper", 11, 1);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    const poHelper_r3 = ɵɵreadContextLet(0);
    ɵɵproperty("p-size", ctx_r0.size)("p-helper", poHelper_r3.helperSettings)("p-disabled", ctx_r0.isDisabled)("p-append-in-body", ctx_r0.appendBox);
  }
}
function PoDecimalComponent_Conditional_12_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-field-container-bottom", 12);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("p-append-in-body", ctx_r0.appendBox)("p-help", ctx_r0.help)("p-disabled", ctx_r0.isDisabled)("p-error-limit", ctx_r0.errorLimit)("p-error-pattern", ctx_r0.getErrorPatternMessage())("p-size", ctx_r0.size);
  }
}
function PoEmailComponent_Conditional_6_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 7);
    ɵɵelement(1, "po-icon", 14);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵclassMap(ɵɵinterpolate1("po-field-icon ", ctx_r0.disabled ? "po-icon-input-disabled" : "po-icon-input"));
    ɵɵclassProp("po-field-icon-disabled", ctx_r0.isDisabled)("po-field-icon-aa", ctx_r0.size === "small");
    ɵɵproperty("p-icon", ctx_r0.icon);
  }
}
function PoEmailComponent_Conditional_10_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-loading-icon", 10);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("p-size", ctx_r0.mapSizeToIcon(ctx_r0.size));
  }
}
function PoEmailComponent_Conditional_11_Template(rf, ctx) {
  if (rf & 1) {
    const _r2 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-clean", 15);
    ɵɵlistener("p-change-event", function PoEmailComponent_Conditional_11_Template_po_clean_p_change_event_0_listener($event) {
      ɵɵrestoreView(_r2);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.clear($event));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵclassMap(ctx_r0.disabled ? "po-icon-input-disabled" : "po-icon-input");
    ɵɵproperty("p-element-ref", ctx_r0.inputEl)("p-size", ctx_r0.size);
  }
}
function PoEmailComponent_Conditional_12_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-helper", 12, 2);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    const poHelper_r3 = ɵɵreadContextLet(0);
    ɵɵproperty("p-size", ctx_r0.size)("p-helper", poHelper_r3.helperSettings)("p-disabled", ctx_r0.isDisabled)("p-append-in-body", ctx_r0.appendBox);
  }
}
function PoEmailComponent_Conditional_13_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-field-container-bottom", 13);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("p-append-in-body", ctx_r0.appendBox)("p-help", ctx_r0.help)("p-disabled", ctx_r0.isDisabled)("p-error-pattern", ctx_r0.getErrorPattern())("p-error-limit", ctx_r0.errorLimit)("p-size", ctx_r0.size);
  }
}
function PoInputComponent_Conditional_6_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 7);
    ɵɵelement(1, "po-icon", 14);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵclassMap(ɵɵinterpolate1("po-field-icon ", ctx_r0.disabled ? "po-icon-input-disabled" : "po-icon-input"));
    ɵɵclassProp("po-field-icon-disabled", ctx_r0.isDisabled)("po-field-icon-aa", ctx_r0.size === "small");
    ɵɵproperty("p-icon", ctx_r0.icon);
  }
}
function PoInputComponent_Conditional_10_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-loading-icon", 10);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("p-size", ctx_r0.mapSizeToIcon(ctx_r0.size));
  }
}
function PoInputComponent_Conditional_11_Template(rf, ctx) {
  if (rf & 1) {
    const _r2 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-clean", 15);
    ɵɵlistener("p-change-event", function PoInputComponent_Conditional_11_Template_po_clean_p_change_event_0_listener($event) {
      ɵɵrestoreView(_r2);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.clear($event));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵclassMap(ctx_r0.disabled ? "po-icon-input-disabled" : "po-icon-input");
    ɵɵproperty("p-element-ref", ctx_r0.inputEl)("p-size", ctx_r0.size);
  }
}
function PoInputComponent_Conditional_12_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-helper", 12, 2);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    const poHelper_r3 = ɵɵreadContextLet(0);
    ɵɵproperty("p-size", ctx_r0.size)("p-helper", poHelper_r3.helperSettings)("p-disabled", ctx_r0.isDisabled)("p-append-in-body", ctx_r0.appendBox);
  }
}
function PoInputComponent_Conditional_13_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-field-container-bottom", 13);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("p-append-in-body", ctx_r0.appendBox)("p-help", ctx_r0.help)("p-disabled", ctx_r0.isDisabled)("p-error-pattern", ctx_r0.getErrorPattern())("p-error-limit", ctx_r0.errorLimit)("p-size", ctx_r0.size);
  }
}
function PoLoginComponent_Conditional_10_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-loading-icon", 10);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("p-size", ctx_r0.mapSizeToIcon(ctx_r0.size));
  }
}
function PoLoginComponent_Conditional_11_Template(rf, ctx) {
  if (rf & 1) {
    const _r2 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-clean", 14);
    ɵɵlistener("p-change-event", function PoLoginComponent_Conditional_11_Template_po_clean_p_change_event_0_listener($event) {
      ɵɵrestoreView(_r2);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.clear($event));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("p-element-ref", ctx_r0.inputEl)("p-size", ctx_r0.size);
  }
}
function PoLoginComponent_Conditional_12_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-helper", 12, 1);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    const poHelper_r3 = ɵɵreadContextLet(0);
    ɵɵproperty("p-size", ctx_r0.size)("p-helper", poHelper_r3.helperSettings)("p-disabled", ctx_r0.isDisabled)("p-append-in-body", ctx_r0.appendBox);
  }
}
function PoLoginComponent_Conditional_13_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-field-container-bottom", 13);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("p-help", ctx_r0.help)("p-disabled", ctx_r0.isDisabled)("p-error-pattern", ctx_r0.getErrorPattern())("p-error-limit", ctx_r0.errorLimit)("p-size", ctx_r0.size);
  }
}
var _c1222 = ["inpsearch"];
var _c123 = ["container"];
function PoLookupModalComponent_Conditional_9_Template(rf, ctx) {
  if (rf & 1) {
    const _r2 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 11)(1, "span", 16);
    ɵɵlistener("click", function PoLookupModalComponent_Conditional_9_Template_span_click_1_listener() {
      ɵɵrestoreView(_r2);
      const ctx_r2 = ɵɵnextContext();
      return ɵɵresetView(ctx_r2.onAdvancedFilter());
    })("keydown.enter", function PoLookupModalComponent_Conditional_9_Template_span_keydown_enter_1_listener() {
      ɵɵrestoreView(_r2);
      const ctx_r2 = ɵɵnextContext();
      return ɵɵresetView(ctx_r2.onAdvancedFilter());
    });
    ɵɵtext(2);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r2 = ɵɵnextContext();
    ɵɵadvance(2);
    ɵɵtextInterpolate1(" ", ctx_r2.literals.modalAdvancedSearch, " ");
  }
}
function PoLookupModalComponent_Conditional_10_Template(rf, ctx) {
  if (rf & 1) {
    const _r4 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-disclaimer-group", 17);
    ɵɵlistener("p-change", function PoLookupModalComponent_Conditional_10_Template_po_disclaimer_group_p_change_0_listener() {
      ɵɵrestoreView(_r4);
      const ctx_r2 = ɵɵnextContext();
      return ɵɵresetView(ctx_r2.onChangeDisclaimerGroup());
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r2 = ɵɵnextContext();
    ɵɵproperty("p-disclaimers", ctx_r2.disclaimerGroup == null ? null : ctx_r2.disclaimerGroup.disclaimers)("p-title", ctx_r2.disclaimerGroup == null ? null : ctx_r2.disclaimerGroup.title);
  }
}
function PoLookupModalComponent_Conditional_14_Template(rf, ctx) {
  if (rf & 1) {
    const _r5 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-disclaimer-group", 18);
    ɵɵlistener("p-remove", function PoLookupModalComponent_Conditional_14_Template_po_disclaimer_group_p_remove_0_listener($event) {
      ɵɵrestoreView(_r5);
      const ctx_r2 = ɵɵnextContext();
      return ɵɵresetView(ctx_r2.onUnselectFromDisclaimer($event.removedDisclaimer));
    })("p-remove-all", function PoLookupModalComponent_Conditional_14_Template_po_disclaimer_group_p_remove_all_0_listener($event) {
      ɵɵrestoreView(_r5);
      const ctx_r2 = ɵɵnextContext();
      return ɵɵresetView(ctx_r2.onAllUnselectedTag($event));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r2 = ɵɵnextContext();
    ɵɵproperty("p-disclaimers", ctx_r2.selecteds);
  }
}
function PoLookupComponent_ng_template_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-helper", 7, 1);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    const poHelper_r2 = ɵɵreadContextLet(0);
    ɵɵproperty("p-size", ctx_r0.size)("p-helper", poHelper_r2.helperSettings)("p-disabled", ctx_r0.isDisabled)("p-append-in-body", ctx_r0.appendBox);
  }
}
function PoLookupComponent_Conditional_5_Conditional_6_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 13);
    ɵɵelement(1, "po-loading-icon", 16);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵadvance();
    ɵɵproperty("p-size", ctx_r0.mapSizeToIcon(ctx_r0.size));
  }
}
function PoLookupComponent_Conditional_5_Conditional_7_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    const _r4 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-clean", 18);
    ɵɵlistener("p-change-event", function PoLookupComponent_Conditional_5_Conditional_7_Conditional_0_Template_po_clean_p_change_event_0_listener() {
      ɵɵrestoreView(_r4);
      const ctx_r0 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r0.cleanModel());
    })("click", function PoLookupComponent_Conditional_5_Conditional_7_Conditional_0_Template_po_clean_click_0_listener($event) {
      ɵɵrestoreView(_r4);
      const ctx_r0 = ɵɵnextContext(3);
      ctx_r0.cleanModel();
      return ɵɵresetView($event.preventDefault());
    })("keydown.enter", function PoLookupComponent_Conditional_5_Conditional_7_Conditional_0_Template_po_clean_keydown_enter_0_listener($event) {
      ɵɵrestoreView(_r4);
      const ctx_r0 = ɵɵnextContext(3);
      ctx_r0.cleanModel();
      return ɵɵresetView($event.preventDefault());
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    ɵɵnextContext(2);
    const inp_r5 = ɵɵreference(4);
    const ctx_r0 = ɵɵnextContext();
    ɵɵclassProp("po-lookup-button-aa", ctx_r0.size === "small");
    ɵɵproperty("p-element-ref", inp_r5)("p-size", ctx_r0.size);
    ɵɵattribute("aria-label", ctx_r0.literals.clean);
  }
}
function PoLookupComponent_Conditional_5_Conditional_7_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵconditionalCreate(0, PoLookupComponent_Conditional_5_Conditional_7_Conditional_0_Template, 1, 5, "po-clean", 17);
  }
  if (rf & 2) {
    ɵɵnextContext();
    const inp_r5 = ɵɵreference(4);
    const ctx_r0 = ɵɵnextContext();
    ɵɵconditional(ctx_r0.clean && !ctx_r0.isDisabled && !!inp_r5.value ? 0 : -1);
  }
}
function PoLookupComponent_Conditional_5_Conditional_11_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoLookupComponent_Conditional_5_Conditional_11_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, PoLookupComponent_Conditional_5_Conditional_11_ng_container_0_Template, 1, 0, "ng-container", 19);
  }
  if (rf & 2) {
    ɵɵnextContext(2);
    const poHelperTemplate_r6 = ɵɵreference(3);
    ɵɵproperty("ngTemplateOutlet", poHelperTemplate_r6);
  }
}
function PoLookupComponent_Conditional_5_Template(rf, ctx) {
  if (rf & 1) {
    const _r3 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 8)(1, "div", 9)(2, "div", 10)(3, "input", 11, 2);
    ɵɵlistener("blur", function PoLookupComponent_Conditional_5_Template_input_blur_3_listener() {
      ɵɵrestoreView(_r3);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.searchEvent());
    })("keydown", function PoLookupComponent_Conditional_5_Template_input_keydown_3_listener($event) {
      ɵɵrestoreView(_r3);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.onKeyDown($event));
    });
    ɵɵelementEnd();
    ɵɵelementStart(5, "div", 12);
    ɵɵconditionalCreate(6, PoLookupComponent_Conditional_5_Conditional_6_Template, 2, 1, "div", 13)(7, PoLookupComponent_Conditional_5_Conditional_7_Template, 1, 1);
    ɵɵelementStart(8, "div", 14, 3);
    ɵɵlistener("p-change-event", function PoLookupComponent_Conditional_5_Template_div_p_change_event_8_listener() {
      ɵɵrestoreView(_r3);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.openLookup());
    })("click", function PoLookupComponent_Conditional_5_Template_div_click_8_listener($event) {
      ɵɵrestoreView(_r3);
      const ctx_r0 = ɵɵnextContext();
      !ctx_r0.isDisabled && ctx_r0.openLookup();
      return ɵɵresetView($event.preventDefault());
    })("keydown.enter", function PoLookupComponent_Conditional_5_Template_div_keydown_enter_8_listener($event) {
      ɵɵrestoreView(_r3);
      const ctx_r0 = ɵɵnextContext();
      !ctx_r0.isDisabled && ctx_r0.openLookup();
      return ɵɵresetView($event.preventDefault());
    });
    ɵɵelement(10, "po-icon", 15);
    ɵɵelementEnd()()();
    ɵɵconditionalCreate(11, PoLookupComponent_Conditional_5_Conditional_11_Template, 1, 1, "ng-container");
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const inp_r5 = ɵɵreference(4);
    const ctx_r0 = ɵɵnextContext();
    const needsHelper_r7 = ɵɵreadContextLet(1);
    ɵɵclassProp("po-lookup-disabled", ctx_r0.disabled)("po-lookup-loading", ctx_r0.loading);
    ɵɵadvance(3);
    ɵɵproperty("ngClass", ctx_r0.clean && inp_r5.value ? "po-input-double-icon-right" : "po-input-icon-right")("id", ctx_r0.id)("autocomplete", ctx_r0.autocomplete)("disabled", ctx_r0.isDisabled)("placeholder", ctx_r0.placeholder)("required", ctx_r0.required);
    ɵɵadvance(3);
    ɵɵconditional(ctx_r0.loading ? 6 : 7);
    ɵɵadvance(2);
    ɵɵclassProp("po-field-icon-disabled", ctx_r0.isDisabled);
    ɵɵproperty("ariaLabel", ctx_r0.literals.search);
    ɵɵadvance(2);
    ɵɵclassProp("po-field-icon", !ctx_r0.isDisabled)("po-field-icon-aa", !ctx_r0.isDisabled && ctx_r0.size === "small");
    ɵɵadvance();
    ɵɵconditional(needsHelper_r7 ? 11 : -1);
  }
}
function PoLookupComponent_Conditional_6_Conditional_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span", 21);
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵadvance();
    ɵɵtextInterpolate(ctx_r0.placeholder);
  }
}
function PoLookupComponent_Conditional_6_For_7_Template(rf, ctx) {
  if (rf & 1) {
    const _r9 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-disclaimer", 23);
    ɵɵlistener("p-close-action", function PoLookupComponent_Conditional_6_For_7_Template_po_disclaimer_p_close_action_0_listener() {
      const disclaimer_r10 = ɵɵrestoreView(_r9).$implicit;
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.closeDisclaimer(disclaimer_r10.value));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const disclaimer_r10 = ctx.$implicit;
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵclassProp("po-clickable", disclaimer_r10.value === "" && !ctx_r0.isDisabled);
    ɵɵproperty("p-label", disclaimer_r10.label)("p-value", disclaimer_r10.value)("p-hide-close", disclaimer_r10.value === "" || ctx_r0.isDisabled);
  }
}
function PoLookupComponent_Conditional_6_Conditional_9_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 13);
    ɵɵelement(1, "po-loading-icon", 16);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵadvance();
    ɵɵproperty("p-size", ctx_r0.mapSizeToIcon(ctx_r0.size));
  }
}
function PoLookupComponent_Conditional_6_Conditional_10_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    const _r11 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-clean", 18);
    ɵɵlistener("p-change-event", function PoLookupComponent_Conditional_6_Conditional_10_Conditional_0_Template_po_clean_p_change_event_0_listener() {
      ɵɵrestoreView(_r11);
      const ctx_r0 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r0.cleanModel());
    })("click", function PoLookupComponent_Conditional_6_Conditional_10_Conditional_0_Template_po_clean_click_0_listener($event) {
      ɵɵrestoreView(_r11);
      const ctx_r0 = ɵɵnextContext(3);
      ctx_r0.cleanModel();
      return ɵɵresetView($event.preventDefault());
    })("keydown.enter", function PoLookupComponent_Conditional_6_Conditional_10_Conditional_0_Template_po_clean_keydown_enter_0_listener($event) {
      ɵɵrestoreView(_r11);
      const ctx_r0 = ɵɵnextContext(3);
      ctx_r0.cleanModel();
      return ɵɵresetView($event.preventDefault());
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    ɵɵnextContext(2);
    const inp_r12 = ɵɵreference(4);
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("p-element-ref", inp_r12)("p-size", ctx_r0.size);
    ɵɵattribute("aria-label", ctx_r0.literals.clean);
  }
}
function PoLookupComponent_Conditional_6_Conditional_10_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵconditionalCreate(0, PoLookupComponent_Conditional_6_Conditional_10_Conditional_0_Template, 1, 3, "po-clean", 24);
  }
  if (rf & 2) {
    ɵɵnextContext();
    const inp_r12 = ɵɵreference(4);
    const ctx_r0 = ɵɵnextContext();
    ɵɵconditional(ctx_r0.clean && !ctx_r0.isDisabled && !!inp_r12.value ? 0 : -1);
  }
}
function PoLookupComponent_Conditional_6_Conditional_14_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoLookupComponent_Conditional_6_Conditional_14_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, PoLookupComponent_Conditional_6_Conditional_14_ng_container_0_Template, 1, 0, "ng-container", 19);
  }
  if (rf & 2) {
    ɵɵnextContext(2);
    const poHelperTemplate_r6 = ɵɵreference(3);
    ɵɵproperty("ngTemplateOutlet", poHelperTemplate_r6);
  }
}
function PoLookupComponent_Conditional_6_Template(rf, ctx) {
  if (rf & 1) {
    const _r8 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 8)(1, "div", 9)(2, "div", 10)(3, "div", 20, 2);
    ɵɵlistener("blur", function PoLookupComponent_Conditional_6_Template_div_blur_3_listener() {
      ɵɵrestoreView(_r8);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.onBlur());
    })("keydown", function PoLookupComponent_Conditional_6_Template_div_keydown_3_listener($event) {
      ɵɵrestoreView(_r8);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.onKeyDown($event));
    });
    ɵɵconditionalCreate(5, PoLookupComponent_Conditional_6_Conditional_5_Template, 2, 1, "span", 21);
    ɵɵrepeaterCreate(6, PoLookupComponent_Conditional_6_For_7_Template, 1, 5, "po-disclaimer", 22, ɵɵrepeaterTrackByIndex);
    ɵɵelementEnd();
    ɵɵelementStart(8, "div", 12);
    ɵɵconditionalCreate(9, PoLookupComponent_Conditional_6_Conditional_9_Template, 2, 1, "div", 13)(10, PoLookupComponent_Conditional_6_Conditional_10_Template, 1, 1);
    ɵɵelementStart(11, "div", 14, 3);
    ɵɵlistener("p-change-event", function PoLookupComponent_Conditional_6_Template_div_p_change_event_11_listener() {
      ɵɵrestoreView(_r8);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.openLookup());
    })("click", function PoLookupComponent_Conditional_6_Template_div_click_11_listener($event) {
      ɵɵrestoreView(_r8);
      const ctx_r0 = ɵɵnextContext();
      !ctx_r0.isDisabled && ctx_r0.openLookup();
      return ɵɵresetView($event.preventDefault());
    })("keydown.enter", function PoLookupComponent_Conditional_6_Template_div_keydown_enter_11_listener($event) {
      ɵɵrestoreView(_r8);
      const ctx_r0 = ɵɵnextContext();
      !ctx_r0.isDisabled && ctx_r0.openLookup();
      return ɵɵresetView($event.preventDefault());
    });
    ɵɵelement(13, "po-icon", 15);
    ɵɵelementEnd()()();
    ɵɵconditionalCreate(14, PoLookupComponent_Conditional_6_Conditional_14_Template, 1, 1, "ng-container");
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const inp_r12 = ɵɵreference(4);
    const ctx_r0 = ɵɵnextContext();
    const needsHelper_r7 = ɵɵreadContextLet(1);
    ɵɵclassProp("po-lookup-disabled", ctx_r0.disabled)("po-lookup-loading", ctx_r0.loading);
    ɵɵadvance(3);
    ɵɵclassProp("po-lookup-input-auto", ctx_r0.autoHeight)("po-lookup-input-static", !ctx_r0.autoHeight)("po-lookup-input-padding-button-clean", ctx_r0.clean || ctx_r0.loading)("po-lookup-input-disabled", ctx_r0.isDisabled)("po-lookup-input-auto-dynamic-height", ctx_r0.isExpandedHeight);
    ɵɵproperty("tabindex", ctx_r0.isDisabled ? -1 : 0)("ngClass", ctx_r0.clean && inp_r12.value ? "po-input-double-icon-right" : "po-input-icon-right");
    ɵɵadvance(2);
    ɵɵconditional(ctx_r0.placeholder && !(ctx_r0.disclaimers == null ? null : ctx_r0.disclaimers.length) ? 5 : -1);
    ɵɵadvance();
    ɵɵrepeater(ctx_r0.visibleDisclaimers);
    ɵɵadvance(3);
    ɵɵconditional(ctx_r0.loading ? 9 : 10);
    ɵɵadvance(2);
    ɵɵclassProp("po-field-icon-disabled", ctx_r0.isDisabled);
    ɵɵproperty("ariaLabel", ctx_r0.literals.search);
    ɵɵadvance(2);
    ɵɵclassProp("po-field-icon", !ctx_r0.isDisabled)("po-field-icon-aa", !ctx_r0.isDisabled && ctx_r0.size === "small");
    ɵɵadvance();
    ɵɵconditional(needsHelper_r7 ? 14 : -1);
  }
}
var _c124 = ["divElement"];
var _c125 = ["dropdownElement"];
function PoMultiselectComponent_Conditional_10_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 12)(1, "span");
    ɵɵtext(2);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance(2);
    ɵɵtextInterpolate(ctx_r0.placeholder ? ctx_r0.placeholder : ctx_r0.literals.selectItem);
  }
}
function PoMultiselectComponent_For_12_Template(rf, ctx) {
  if (rf & 1) {
    const _r2 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-tag", 20);
    ɵɵlistener("p-close", function PoMultiselectComponent_For_12_Template_po_tag_p_close_0_listener($event) {
      const tag_r3 = ɵɵrestoreView(_r2).$implicit;
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.closeTag(tag_r3[ctx_r0.fieldValue], $event));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const tag_r3 = ctx.$implicit;
    const ɵ$index_21_r4 = ctx.$index;
    const ctx_r0 = ɵɵnextContext();
    ɵɵclassProp("po-clickable", tag_r3[ctx_r0.fieldValue] === "" && !ctx_r0.disabled);
    ɵɵproperty("p-value", tag_r3[ctx_r0.fieldLabel])("p-literals", ɵ$index_21_r4 + 1 === ctx_r0.visibleTags.length && ctx_r0.hasMoreTag ? ctx_r0.literalsTag : void 0)("p-removable", true)("p-disabled", ctx_r0.isDisabled)("p-append-in-body", ctx_r0.appendBox);
  }
}
function PoMultiselectComponent_Conditional_14_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-loading-icon", 15);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("p-size", ctx_r0.mapSizeToIcon(ctx_r0.size));
  }
}
function PoMultiselectComponent_Conditional_15_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-icon", 21, 4);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵclassMap(ɵɵinterpolate1("po-field-icon ", ctx_r0.disabled ? "po-icon-input-disabled" : "po-icon-input"));
    ɵɵclassProp("po-field-icon-aa", ctx_r0.size === "small");
    ɵɵproperty("p-icon", ɵɵinterpolate(ctx_r0.dropdownIcon))("ngClass", ctx_r0.disabled ? "po-field-icon-disabled" : "");
  }
}
function PoMultiselectComponent_Conditional_16_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-helper", 17, 5);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    const poHelper_r5 = ɵɵreadContextLet(0);
    ɵɵproperty("p-size", ctx_r0.size)("p-helper", poHelper_r5.helperSettings)("p-disabled", ctx_r0.isDisabled)("p-append-in-body", ctx_r0.appendBox);
  }
}
function PoMultiselectComponent_Conditional_17_ng_template_0_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoMultiselectComponent_Conditional_17_ng_template_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, PoMultiselectComponent_Conditional_17_ng_template_0_ng_container_0_Template, 1, 0, "ng-container", 22);
  }
  if (rf & 2) {
    ɵɵnextContext(2);
    const dropdownListbox_r6 = ɵɵreference(21);
    ɵɵproperty("ngTemplateOutlet", dropdownListbox_r6);
  }
}
function PoMultiselectComponent_Conditional_17_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, PoMultiselectComponent_Conditional_17_ng_template_0_Template, 1, 1, "ng-template", 18);
  }
  if (rf & 2) {
    ɵɵnextContext();
    const trigger_r7 = ɵɵreference(5);
    ɵɵproperty("cdkConnectedOverlayOrigin", trigger_r7)("cdkConnectedOverlayOpen", true)("cdkConnectedOverlayDisableClose", true);
  }
}
function PoMultiselectComponent_Conditional_18_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoMultiselectComponent_Conditional_18_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, PoMultiselectComponent_Conditional_18_ng_container_0_Template, 1, 0, "ng-container", 22);
  }
  if (rf & 2) {
    ɵɵnextContext();
    const dropdownListbox_r6 = ɵɵreference(21);
    ɵɵproperty("ngTemplateOutlet", dropdownListbox_r6);
  }
}
function PoMultiselectComponent_ng_template_20_Template(rf, ctx) {
  if (rf & 1) {
    const _r8 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-multiselect-dropdown", 23, 6);
    ɵɵlistener("p-change", function PoMultiselectComponent_ng_template_20_Template_po_multiselect_dropdown_p_change_0_listener($event) {
      ɵɵrestoreView(_r8);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.changeItems($event));
    })("p-change-search", function PoMultiselectComponent_ng_template_20_Template_po_multiselect_dropdown_p_change_search_0_listener($event) {
      ɵɵrestoreView(_r8);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.changeSearch($event));
    })("p-close-dropdown", function PoMultiselectComponent_ng_template_20_Template_po_multiselect_dropdown_p_close_dropdown_0_listener() {
      ɵɵrestoreView(_r8);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.controlDropdownVisibility(false));
    })("keydown", function PoMultiselectComponent_ng_template_20_Template_po_multiselect_dropdown_keydown_0_listener($event) {
      ɵɵrestoreView(_r8);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.onKeyDownDropdown($event, 0));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("p-searching", ctx_r0.isServerSearching)("p-hide-search", ctx_r0.hideSearch)("p-hide-select-all", ctx_r0.hideSelectAll)("p-literals", ctx_r0.literals)("p-options", ctx_r0.options)("p-visible-options", ctx_r0.visibleOptionsDropdown)("p-selected-options", ctx_r0.selectedOptions)("p-placeholder-search", ctx_r0.placeholderSearch)("p-field-value", ctx_r0.fieldValue)("p-field-label", ctx_r0.fieldLabel)("p-multiselect-template", ctx_r0.multiselectOptionTemplate)("p-size", ctx_r0.size)("p-container-width", ctx_r0.containerWidth);
  }
}
function PoNumberComponent_Conditional_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 6);
    ɵɵelement(1, "po-icon", 13);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵclassProp("po-field-icon-disabled", ctx_r0.isDisabled)("po-field-icon-aa", ctx_r0.size === "small");
    ɵɵproperty("p-icon", ctx_r0.icon);
  }
}
function PoNumberComponent_Conditional_9_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-loading-icon", 9);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("p-size", ctx_r0.mapSizeToIcon(ctx_r0.size));
  }
}
function PoNumberComponent_Conditional_10_Template(rf, ctx) {
  if (rf & 1) {
    const _r2 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-clean", 14);
    ɵɵlistener("p-change-event", function PoNumberComponent_Conditional_10_Template_po_clean_p_change_event_0_listener($event) {
      ɵɵrestoreView(_r2);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.clear($event));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("p-default-value", null)("p-element-ref", ctx_r0.inputEl)("p-size", ctx_r0.size);
  }
}
function PoNumberComponent_Conditional_11_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-helper", 11, 1);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    const poHelper_r3 = ɵɵreadContextLet(0);
    ɵɵproperty("p-size", ctx_r0.size)("p-helper", poHelper_r3.helperSettings)("p-disabled", ctx_r0.isDisabled)("p-append-in-body", ctx_r0.appendBox);
  }
}
function PoNumberComponent_Conditional_12_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-field-container-bottom", 12);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("p-append-in-body", ctx_r0.appendBox)("p-help", ctx_r0.help)("p-disabled", ctx_r0.isDisabled)("p-error-limit", ctx_r0.errorLimit)("p-error-pattern", ctx_r0.getErrorPatternMessage())("p-size", ctx_r0.size);
  }
}
function PoPasswordComponent_Conditional_10_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-loading-icon", 10);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("p-size", ctx_r0.mapSizeToIcon(ctx_r0.size));
  }
}
function PoPasswordComponent_Conditional_11_Template(rf, ctx) {
  if (rf & 1) {
    const _r2 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-clean", 15);
    ɵɵlistener("p-change-event", function PoPasswordComponent_Conditional_11_Template_po_clean_p_change_event_0_listener($event) {
      ɵɵrestoreView(_r2);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.clear($event));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("p-element-ref", ctx_r0.inputEl)("p-size", ctx_r0.size);
  }
}
function PoPasswordComponent_Conditional_12_Template(rf, ctx) {
  if (rf & 1) {
    const _r3 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-icon", 16);
    ɵɵlistener("click", function PoPasswordComponent_Conditional_12_Template_po_icon_click_0_listener() {
      ɵɵrestoreView(_r3);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.showPassword());
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵclassProp("po-field-icon-aa", ctx_r0.size === "small")("po-field-icon-disabled-aa", ctx_r0.size === "small");
    ɵɵproperty("p-icon", ctx_r0.visiblePassword ? "ICON_EYE" : "ICON_EYE_OFF")("ngClass", !ctx_r0.visiblePassword ? "po-field-icon-disabled" : "");
  }
}
function PoPasswordComponent_Conditional_13_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-helper", 13, 1);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    const poHelper_r4 = ɵɵreadContextLet(0);
    ɵɵproperty("p-size", ctx_r0.size)("p-helper", poHelper_r4.helperSettings)("p-disabled", ctx_r0.isDisabled)("p-append-in-body", ctx_r0.appendBox);
  }
}
function PoPasswordComponent_Conditional_14_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-field-container-bottom", 14);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("p-help", ctx_r0.help)("p-disabled", ctx_r0.isDisabled)("p-error-limit", ctx_r0.errorLimit)("p-error-pattern", ctx_r0.getErrorPattern())("p-size", ctx_r0.size);
  }
}
var _c126 = ["bodyElement"];
function PoRichTextBodyComponent_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 3);
    ɵɵelement(1, "po-loading-icon", 4);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵproperty("p-size", ctx_r0.mapSizeToIcon(ctx_r0.size));
  }
}
var _c127 = ["selectFilesLink"];
var _c128 = (a0) => ({
  "po-upload-drag-drop-area-disabled": a0
});
var _c129 = (a0) => ({
  "po-clickable": a0
});
function PoUploadDragDropAreaComponent_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-icon", 3);
    ɵɵelementStart(1, "div", 4);
    ɵɵtext(2);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance(2);
    ɵɵtextInterpolate1(" ", ctx_r0.directoryCompatible ? ctx_r0.literals == null ? null : ctx_r0.literals.dropFoldersHere : ctx_r0.literals == null ? null : ctx_r0.literals.dropFilesHere, " ");
  }
}
function PoUploadDragDropAreaComponent_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    const _r2 = ɵɵgetCurrentView();
    ɵɵelement(0, "po-icon", 5);
    ɵɵelementStart(1, "div", 6);
    ɵɵtext(2);
    ɵɵelementEnd();
    ɵɵelementStart(3, "button", 7, 0);
    ɵɵlistener("click", function PoUploadDragDropAreaComponent_Conditional_3_Template_button_click_3_listener() {
      ɵɵrestoreView(_r2);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.selectFiles.emit());
    });
    ɵɵtext(5);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance(2);
    ɵɵtextInterpolate1(" ", ctx_r0.directoryCompatible ? ctx_r0.literals == null ? null : ctx_r0.literals.dragFoldersHere : ctx_r0.literals == null ? null : ctx_r0.literals.dragFilesHere, " ");
    ɵɵadvance();
    ɵɵproperty("disabled", ctx_r0.disabled)("ngClass", ɵɵpureFunction1(4, _c129, !ctx_r0.disabled));
    ɵɵadvance(2);
    ɵɵtextInterpolate1(" ", ctx_r0.directoryCompatible ? ctx_r0.literals == null ? null : ctx_r0.literals.selectFolderOnComputer : ctx_r0.literals == null ? null : ctx_r0.literals.selectFilesOnComputer, " ");
  }
}
var _c130 = ["DragDropAreaFixed"];
var _c131 = ["dragDropOverlay"];
function PoUploadDragDropComponent_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-upload-drag-drop-area-overlay", 3, 0);
    ɵɵlistener("p-area-element", function PoUploadDragDropComponent_Conditional_0_Template_po_upload_drag_drop_area_overlay_p_area_element_0_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onAreaElement($event));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("p-directory-compatible", ctx_r1.directoryCompatible)("p-disabled", ctx_r1.disabled)("p-literals", ctx_r1.literals)("p-target", ctx_r1.dragDropAreaComponent.elementRef);
  }
}
var _c132 = (a0, a1) => [a0, a1];
function PoUploadFileRestrictionsComponent_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "p");
    ɵɵtext(1);
    ɵɵpipe(2, "poI18n");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ɵɵpipeBind2(2, 1, ctx_r0.literals == null ? null : ctx_r0.literals.numberOfFilesAllowed, ctx_r0.maxFiles), " ");
  }
}
function PoUploadFileRestrictionsComponent_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "p");
    ɵɵtext(1);
    ɵɵpipe(2, "poI18n");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ɵɵpipeBind2(2, 1, ctx_r0.literals == null ? null : ctx_r0.literals.allowedFormats, ctx_r0.allowedExtensions), " ");
  }
}
function PoUploadFileRestrictionsComponent_Conditional_2_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span");
    ɵɵtext(1);
    ɵɵpipe(2, "poI18n");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ɵɵpipeBind2(2, 1, ctx_r0.literals == null ? null : ctx_r0.literals.allowedFileSizeRange, ɵɵpureFunction2(4, _c132, ctx_r0.minFileSize, ctx_r0.maxFileSize)), " ");
  }
}
function PoUploadFileRestrictionsComponent_Conditional_2_Conditional_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span");
    ɵɵtext(1);
    ɵɵpipe(2, "poI18n");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ɵɵpipeBind2(2, 1, ctx_r0.literals == null ? null : ctx_r0.literals.minFileSizeAllowed, ctx_r0.minFileSize), " ");
  }
}
function PoUploadFileRestrictionsComponent_Conditional_2_Conditional_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span");
    ɵɵtext(1);
    ɵɵpipe(2, "poI18n");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ɵɵpipeBind2(2, 1, ctx_r0.literals == null ? null : ctx_r0.literals.maxFileSizeAllowed, ctx_r0.maxFileSize), " ");
  }
}
function PoUploadFileRestrictionsComponent_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "p")(1, "span");
    ɵɵtext(2);
    ɵɵelementEnd();
    ɵɵconditionalCreate(3, PoUploadFileRestrictionsComponent_Conditional_2_Conditional_3_Template, 3, 7, "span");
    ɵɵconditionalCreate(4, PoUploadFileRestrictionsComponent_Conditional_2_Conditional_4_Template, 3, 4, "span");
    ɵɵconditionalCreate(5, PoUploadFileRestrictionsComponent_Conditional_2_Conditional_5_Template, 3, 4, "span");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance(2);
    ɵɵtextInterpolate(ctx_r0.literals == null ? null : ctx_r0.literals.allowedSizes);
    ɵɵadvance();
    ɵɵconditional(ctx_r0.minFileSize && ctx_r0.maxFileSize ? 3 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r0.minFileSize && !ctx_r0.maxFileSize ? 4 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r0.maxFileSize && !ctx_r0.minFileSize ? 5 : -1);
  }
}
var _c133 = ["inputFile"];
var _c134 = ["uploadButton"];
var _c135 = ["modal"];
var _c136 = (a0) => ({
  "po-upload-file-restrictions-drag-drop": a0
});
function PoUploadComponent_Conditional_5_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-upload-drag-drop", 16);
    ɵɵlistener("p-file-change", function PoUploadComponent_Conditional_5_Template_po_upload_drag_drop_p_file_change_0_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onFileChangeDragDrop($event));
    })("p-select-files", function PoUploadComponent_Conditional_5_Template_po_upload_drag_drop_p_select_files_0_listener() {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.selectFiles());
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("p-directory-compatible", ctx_r1.canHandleDirectory)("p-disabled", ctx_r1.isDisabled)("p-drag-drop-height", ctx_r1.dragDropHeight)("p-literals", ctx_r1.literals);
  }
}
function PoUploadComponent_Conditional_6_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-upload-file-restrictions", 17);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵclassProp("po-pb-1", !ctx_r1.displayDragDrop);
    ɵɵproperty("ngClass", ɵɵpureFunction1(7, _c136, ctx_r1.displayDragDrop))("p-allowed-extensions", ctx_r1.fileRestrictions == null ? null : ctx_r1.fileRestrictions.allowedExtensions)("p-max-files", ctx_r1.maxFiles)("p-max-file-size", ctx_r1.fileRestrictions == null ? null : ctx_r1.fileRestrictions.maxFileSize)("p-min-file-size", ctx_r1.fileRestrictions == null ? null : ctx_r1.fileRestrictions.minFileSize);
  }
}
function PoUploadComponent_Conditional_7_Template(rf, ctx) {
  if (rf & 1) {
    const _r3 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-button", 18, 2);
    ɵɵlistener("p-click", function PoUploadComponent_Conditional_7_Template_po_button_p_click_0_listener() {
      ɵɵrestoreView(_r3);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.selectFiles());
    })("keydown", function PoUploadComponent_Conditional_7_Template_po_button_keydown_0_listener($event) {
      ɵɵrestoreView(_r3);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onKeyDown($event));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("p-disabled", ctx_r1.isSelectButtonDisabled)("p-loading", ctx_r1.loading)("p-label", ctx_r1.selectFileButtonLabel)("p-size", ctx_r1.size);
  }
}
function PoUploadComponent_Conditional_8_For_2_Conditional_5_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r7 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 32);
    ɵɵelement(1, "po-icon", 33);
    ɵɵelementEnd();
    ɵɵelementStart(2, "img", 34);
    ɵɵlistener("error", function PoUploadComponent_Conditional_8_For_2_Conditional_5_Conditional_1_Template_img_error_2_listener() {
      ɵɵrestoreView(_r7);
      const file_r6 = ɵɵnextContext(2).$implicit;
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.onImageError(file_r6));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const file_r6 = ɵɵnextContext(2).$implicit;
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵadvance(2);
    ɵɵproperty("src", file_r6.thumbnailUrl, ɵɵsanitizeUrl)("alt", ctx_r1.literals.thumbnail);
  }
}
function PoUploadComponent_Conditional_8_For_2_Conditional_5_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 15);
    ɵɵelement(1, "po-icon", 35);
    ɵɵelementEnd();
  }
}
function PoUploadComponent_Conditional_8_For_2_Conditional_5_Template(rf, ctx) {
  if (rf & 1) {
    const _r5 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 31);
    ɵɵlistener("click", function PoUploadComponent_Conditional_8_For_2_Conditional_5_Template_div_click_0_listener() {
      ɵɵrestoreView(_r5);
      const file_r6 = ɵɵnextContext().$implicit;
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.openModal(file_r6));
    })("keydown", function PoUploadComponent_Conditional_8_For_2_Conditional_5_Template_div_keydown_0_listener($event) {
      ɵɵrestoreView(_r5);
      const file_r6 = ɵɵnextContext().$implicit;
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.openModal(file_r6, $event));
    });
    ɵɵconditionalCreate(1, PoUploadComponent_Conditional_8_For_2_Conditional_5_Conditional_1_Template, 3, 2)(2, PoUploadComponent_Conditional_8_For_2_Conditional_5_Conditional_2_Template, 2, 0, "div", 15);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const file_r6 = ɵɵnextContext().$implicit;
    ɵɵadvance();
    ɵɵconditional(!file_r6.imageError ? 1 : 2);
  }
}
function PoUploadComponent_Conditional_8_For_2_Conditional_9_Template(rf, ctx) {
  if (rf & 1) {
    const _r8 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-button", 36);
    ɵɵlistener("p-click", function PoUploadComponent_Conditional_8_For_2_Conditional_9_Template_po_button_p_click_0_listener() {
      ɵɵrestoreView(_r8);
      const file_r6 = ɵɵnextContext().$implicit;
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.customClick(file_r6));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵproperty("p-danger", ctx_r1.customAction.type === "danger")("p-disabled", ctx_r1.actionIsDisabled(ctx_r1.customAction))("p-label", ctx_r1.customAction.label || "")("p-icon", ctx_r1.customAction.icon)("p-size", ctx_r1.size);
  }
}
function PoUploadComponent_Conditional_8_For_2_Conditional_10_Template(rf, ctx) {
  if (rf & 1) {
    const _r9 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 37);
    ɵɵlistener("keydown", function PoUploadComponent_Conditional_8_For_2_Conditional_10_Template_div_keydown_0_listener($event) {
      ɵɵrestoreView(_r9);
      const file_r6 = ɵɵnextContext().$implicit;
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.cancel(file_r6, $event));
    })("click", function PoUploadComponent_Conditional_8_For_2_Conditional_10_Template_div_click_0_listener() {
      ɵɵrestoreView(_r9);
      const file_r6 = ɵɵnextContext().$implicit;
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.cancel(file_r6));
    });
    ɵɵelement(1, "po-icon", 38);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const file_r6 = ɵɵnextContext().$implicit;
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵclassProp("po-upload-cancel-disabled", ctx_r1.disabledRemoveFile);
    ɵɵproperty("tabindex", ctx_r1.disabledRemoveFile ? -1 : 0);
    ɵɵattribute("aria-label", ctx_r1.literals.close);
    ɵɵadvance();
    ɵɵclassProp("po-upload-icon-remove", file_r6.status === 0);
    ɵɵproperty("p-icon", file_r6.status === 0 ? "ICON_DELETE" : "ICON_CLOSE");
  }
}
function PoUploadComponent_Conditional_8_For_2_Conditional_11_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span");
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(4);
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ctx_r1.literals.doneText, " ");
  }
}
function PoUploadComponent_Conditional_8_For_2_Conditional_11_Conditional_2_Conditional_4_Template(rf, ctx) {
  if (rf & 1) {
    const _r10 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-link", 45);
    ɵɵlistener("p-action", function PoUploadComponent_Conditional_8_For_2_Conditional_11_Conditional_2_Conditional_4_Template_po_link_p_action_0_listener() {
      ɵɵrestoreView(_r10);
      const file_r6 = ɵɵnextContext(3).$implicit;
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.uploadFiles([file_r6]));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(5);
    ɵɵproperty("p-label", ctx_r1.literals.tryAgain);
  }
}
function PoUploadComponent_Conditional_8_For_2_Conditional_11_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 40);
    ɵɵelement(1, "po-icon", 42);
    ɵɵelementStart(2, "span", 43);
    ɵɵtext(3);
    ɵɵelementEnd();
    ɵɵconditionalCreate(4, PoUploadComponent_Conditional_8_For_2_Conditional_11_Conditional_2_Conditional_4_Template, 1, 1, "po-link", 44);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const file_r6 = ɵɵnextContext(2).$implicit;
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵadvance(3);
    ɵɵtextInterpolate1(" ", (file_r6 == null ? null : file_r6.errorMessage) || (ctx_r1.literals == null ? null : ctx_r1.literals.errorOccurred), " ");
    ɵɵadvance();
    ɵɵconditional(!file_r6.errorMessage ? 4 : -1);
  }
}
function PoUploadComponent_Conditional_8_For_2_Conditional_11_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span");
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(4);
    ɵɵadvance();
    ɵɵtextInterpolate(ctx_r1.literals.uploadingText);
  }
}
function PoUploadComponent_Conditional_8_For_2_Conditional_11_Conditional_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span");
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(4);
    ɵɵadvance();
    ɵɵtextInterpolate(ctx_r1.literals.startSending);
  }
}
function PoUploadComponent_Conditional_8_For_2_Conditional_11_Conditional_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span", 41);
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const file_r6 = ɵɵnextContext(2).$implicit;
    ɵɵadvance();
    ɵɵtextInterpolate1("", file_r6.percent || 0, "%");
  }
}
function PoUploadComponent_Conditional_8_For_2_Conditional_11_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 39);
    ɵɵconditionalCreate(1, PoUploadComponent_Conditional_8_For_2_Conditional_11_Conditional_1_Template, 2, 1, "span")(2, PoUploadComponent_Conditional_8_For_2_Conditional_11_Conditional_2_Template, 5, 2, "div", 40)(3, PoUploadComponent_Conditional_8_For_2_Conditional_11_Conditional_3_Template, 2, 1, "span")(4, PoUploadComponent_Conditional_8_For_2_Conditional_11_Conditional_4_Template, 2, 1, "span");
    ɵɵconditionalCreate(5, PoUploadComponent_Conditional_8_For_2_Conditional_11_Conditional_5_Template, 2, 1, "span", 41);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const file_r6 = ɵɵnextContext().$implicit;
    ɵɵclassProp("fade-out", file_r6.status === 0);
    ɵɵadvance();
    ɵɵconditional(file_r6.status === 0 ? 1 : file_r6.status === 2 ? 2 : file_r6.status === 1 ? 3 : file_r6.status === 3 ? 4 : -1);
    ɵɵadvance(4);
    ɵɵconditional(file_r6.status !== 2 ? 5 : -1);
  }
}
function PoUploadComponent_Conditional_8_For_2_Conditional_12_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-progress", 46);
  }
  if (rf & 2) {
    const file_r6 = ɵɵnextContext().$implicit;
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵclassProp("fade-out", file_r6.status === 0);
    ɵɵproperty("p-disabled-cancel", ctx_r1.disabledRemoveFile)("p-value", file_r6.percent)("p-status", ctx_r1.progressStatusByFileStatus[file_r6.status])("p-size-actions", ctx_r1.size);
  }
}
function PoUploadComponent_Conditional_8_For_2_Template(rf, ctx) {
  if (rf & 1) {
    const _r4 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 20)(1, "po-container")(2, "div", 21)(3, "div", 22)(4, "div", 23);
    ɵɵconditionalCreate(5, PoUploadComponent_Conditional_8_For_2_Conditional_5_Template, 3, 1, "div", 24);
    ɵɵelementStart(6, "span", 25);
    ɵɵlistener("keydown", function PoUploadComponent_Conditional_8_For_2_Template_span_keydown_6_listener($event) {
      const file_r6 = ɵɵrestoreView(_r4).$implicit;
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.openModal(file_r6, $event));
    })("click", function PoUploadComponent_Conditional_8_For_2_Template_span_click_6_listener() {
      const file_r6 = ɵɵrestoreView(_r4).$implicit;
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.openModal(file_r6));
    })("mouseover", function PoUploadComponent_Conditional_8_For_2_Template_span_mouseover_6_listener($event) {
      const file_r6 = ɵɵrestoreView(_r4).$implicit;
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.showTooltipText($event, file_r6 == null ? null : file_r6.displayName));
    })("focus", function PoUploadComponent_Conditional_8_For_2_Template_span_focus_6_listener($event) {
      const file_r6 = ɵɵrestoreView(_r4).$implicit;
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.showTooltipText($event, file_r6 == null ? null : file_r6.displayName));
    });
    ɵɵtext(7);
    ɵɵelementEnd()();
    ɵɵelementStart(8, "div", 26);
    ɵɵconditionalCreate(9, PoUploadComponent_Conditional_8_For_2_Conditional_9_Template, 1, 5, "po-button", 27);
    ɵɵconditionalCreate(10, PoUploadComponent_Conditional_8_For_2_Conditional_10_Template, 2, 7, "div", 28);
    ɵɵelementEnd()();
    ɵɵconditionalCreate(11, PoUploadComponent_Conditional_8_For_2_Conditional_11_Template, 6, 4, "div", 29);
    ɵɵelementEnd();
    ɵɵconditionalCreate(12, PoUploadComponent_Conditional_8_For_2_Conditional_12_Template, 1, 6, "po-progress", 30);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const file_r6 = ctx.$implicit;
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵclassProp("po-upload-container-error", file_r6.status === 2)("po-upload-status-uploaded", file_r6.hideDoneContent);
    ɵɵadvance(2);
    ɵɵproperty("ngClass", ctx_r1.showThumbnail && file_r6.thumbnailUrl ? "po-upload-content-thumbnail" : "po-upload-content");
    ɵɵadvance(3);
    ɵɵconditional(ctx_r1.showThumbnail && file_r6.thumbnailUrl ? 5 : -1);
    ɵɵadvance();
    ɵɵclassProp("po-upload-name-interactive", ctx_r1.showThumbnail && file_r6.thumbnailUrl);
    ɵɵproperty("p-tooltip", ctx_r1.tooltipTitle);
    ɵɵadvance();
    ɵɵtextInterpolate(file_r6.displayName);
    ɵɵadvance(2);
    ɵɵconditional(ctx_r1.isActionVisible(ctx_r1.customAction) ? 9 : -1);
    ɵɵadvance();
    ɵɵconditional(file_r6.status !== 0 || (ctx_r1.onRemove == null ? null : ctx_r1.onRemove.observers == null ? null : ctx_r1.onRemove.observers.length) > 0 ? 10 : -1);
    ɵɵadvance();
    ɵɵconditional(!file_r6.hideDoneContent ? 11 : -1);
    ɵɵadvance();
    ɵɵconditional(file_r6.status !== 2 && !file_r6.hideDoneContent ? 12 : -1);
  }
}
function PoUploadComponent_Conditional_8_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 10);
    ɵɵrepeaterCreate(1, PoUploadComponent_Conditional_8_For_2_Template, 13, 14, "div", 19, ɵɵcomponentInstance().trackByFn, true);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵrepeater(ctx_r1.currentFiles);
  }
}
function PoUploadComponent_Conditional_9_Template(rf, ctx) {
  if (rf & 1) {
    const _r11 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-button", 47);
    ɵɵlistener("p-click", function PoUploadComponent_Conditional_9_Template_po_button_p_click_0_listener() {
      ɵɵrestoreView(_r11);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.uploadFiles(ctx_r1.currentFiles));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵclassProp("po-mt-3", ctx_r1.hasMoreThanFourItems);
    ɵɵproperty("p-disabled", ctx_r1.isFieldDisabled || ctx_r1.hasAnyFileUploading(ctx_r1.currentFiles))("p-label", ctx_r1.literals.startSending)("p-size", ctx_r1.size);
  }
}
function PoUploadComponent_Conditional_13_Template(rf, ctx) {
  if (rf & 1) {
    const _r12 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "img", 48);
    ɵɵlistener("error", function PoUploadComponent_Conditional_13_Template_img_error_0_listener() {
      ɵɵrestoreView(_r12);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.errorModalImage = true);
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("src", ctx_r1.modalImageUrl, ɵɵsanitizeUrl)("alt", ctx_r1.literals.thumbnail);
  }
}
function PoUploadComponent_Conditional_14_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 15);
    ɵɵelement(1, "po-icon", 35);
    ɵɵelementEnd();
  }
}
function PoUrlComponent_Conditional_6_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 7);
    ɵɵelement(1, "po-icon", 14);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵclassMap(ɵɵinterpolate1("po-field-icon ", ctx_r0.disabled ? "po-icon-input-disabled" : "po-icon-input"));
    ɵɵclassProp("po-field-icon-disabled", ctx_r0.isDisabled)("po-field-icon-aa", ctx_r0.size === "small");
    ɵɵproperty("p-icon", ctx_r0.icon);
  }
}
function PoUrlComponent_Conditional_10_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-loading-icon", 10);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("p-size", ctx_r0.mapSizeToIcon(ctx_r0.size));
  }
}
function PoUrlComponent_Conditional_11_Template(rf, ctx) {
  if (rf & 1) {
    const _r2 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-clean", 15);
    ɵɵlistener("p-change-event", function PoUrlComponent_Conditional_11_Template_po_clean_p_change_event_0_listener($event) {
      ɵɵrestoreView(_r2);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.clear($event));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵclassMap(ctx_r0.disabled ? "po-icon-input-disabled" : "po-icon-input");
    ɵɵproperty("p-element-ref", ctx_r0.inputEl)("p-size", ctx_r0.size);
  }
}
function PoUrlComponent_Conditional_12_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-helper", 12, 2);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    const poHelper_r3 = ɵɵreadContextLet(0);
    ɵɵproperty("p-size", ctx_r0.size)("p-helper", poHelper_r3.helperSettings)("p-disabled", ctx_r0.isDisabled)("p-append-in-body", ctx_r0.appendBox);
  }
}
function PoUrlComponent_Conditional_13_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-field-container-bottom", 13);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("p-append-in-body", ctx_r0.appendBox)("p-help", ctx_r0.help)("p-disabled", ctx_r0.isDisabled)("p-error-pattern", ctx_r0.getErrorPattern())("p-error-limit", ctx_r0.errorLimit)("p-size", ctx_r0.size);
  }
}
var _c137 = ["modalImageForm"];
var _c138 = ["upload"];
var _c139 = ["modalLinkForm"];
var _c140 = ["colorPickerInput"];
var _c141 = ["toolbarElement"];
function PoRichTextToolbarComponent_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 5);
    ɵɵelement(1, "po-button-group", 13);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵproperty("p-buttons", ctx_r0.formatButtons)("p-size", ctx_r0.size);
  }
}
function PoRichTextToolbarComponent_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    const _r2 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 6)(1, "div", 14)(2, "button", 15)(3, "input", 16, 3);
    ɵɵlistener("input", function PoRichTextToolbarComponent_Conditional_3_Template_input_input_3_listener($event) {
      ɵɵrestoreView(_r2);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.changeTextColor($event.target.value));
    });
    ɵɵelementEnd()()()();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance(2);
    ɵɵproperty("disabled", ctx_r0.readonly)("p-tooltip", ctx_r0.literals.textColor);
    ɵɵattribute("aria-label", ctx_r0.literals.textColor);
    ɵɵadvance();
    ɵɵproperty("disabled", ctx_r0.readonly);
    ɵɵattribute("aria-label", ctx_r0.literals.textColor);
  }
}
function PoRichTextToolbarComponent_Conditional_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 7);
    ɵɵelement(1, "po-button-group", 17);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵproperty("p-buttons", ctx_r0.alignButtons)("p-size", ctx_r0.size);
  }
}
function PoRichTextToolbarComponent_Conditional_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 8);
    ɵɵelement(1, "po-button-group", 17);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵproperty("p-buttons", ctx_r0.listButtons)("p-size", ctx_r0.size);
  }
}
function PoRichTextToolbarComponent_Conditional_6_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 9);
    ɵɵelement(1, "po-button-group", 18);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵproperty("p-buttons", ctx_r0.linkButtons)("p-size", ctx_r0.size);
  }
}
function PoRichTextToolbarComponent_Conditional_7_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 10);
    ɵɵelement(1, "po-button-group", 18);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵproperty("p-buttons", ctx_r0.mediaButtons)("p-size", ctx_r0.size);
  }
}
function PoRichTextComponent_Conditional_5_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-rich-text-toolbar", 7, 1);
    ɵɵlistener("p-link-editing", function PoRichTextComponent_Conditional_5_Template_po_rich_text_toolbar_p_link_editing_0_listener($event) {
      ɵɵrestoreView(_r1);
      ɵɵnextContext();
      const richTextBody_r2 = ɵɵreference(4);
      return ɵɵresetView(richTextBody_r2.linkEditing($event));
    })("p-command", function PoRichTextComponent_Conditional_5_Template_po_rich_text_toolbar_p_command_0_listener($event) {
      ɵɵrestoreView(_r1);
      ɵɵnextContext();
      const richTextBody_r2 = ɵɵreference(4);
      return ɵɵresetView(richTextBody_r2.executeCommand($event));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r2 = ɵɵnextContext();
    ɵɵproperty("p-readonly", ctx_r2.readonly || ctx_r2.isDisabled)("p-disabled", ctx_r2.isDisabled)("p-hide-toolbar-actions", ctx_r2.toolbarActions)("p-size", ctx_r2.size);
  }
}
function PoRichTextComponent_Conditional_6_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-field-container-bottom", 6);
  }
  if (rf & 2) {
    const ctx_r2 = ɵɵnextContext();
    const poHelper_r4 = ɵɵreadContextLet(0);
    ɵɵproperty("p-append-in-body", ctx_r2.appendBox)("p-help", ctx_r2.help)("p-disabled", ctx_r2.isDisabled)("p-error-limit", ctx_r2.errorLimit)("p-error-pattern", ctx_r2.errorMsg)("p-size", ctx_r2.size)("p-helper", !ctx_r2.label ? poHelper_r4.helperSettings : void 0)("p-show-helper", !ctx_r2.label ? ctx_r2.displayAdditionalHelp : void 0);
  }
}
var _c142 = ["select"];
function PoSelectComponent_Conditional_7_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "option", 10);
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵproperty("disabled", !!ctx_r0.placeholder)("hidden", !(ctx_r0.selectedValue == null ? null : ctx_r0.selectedValue.toString()) && !ctx_r0.placeholder)("selected", !(ctx_r0.selectedValue == null ? null : ctx_r0.selectedValue.toString()))("value", ctx_r0.placeholder ?? "");
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ctx_r0.placeholder, " ");
  }
}
function PoSelectComponent_Conditional_7_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵconditionalCreate(0, PoSelectComponent_Conditional_7_Conditional_0_Template, 2, 5, "option", 10);
    ɵɵelementStart(1, "option", 11);
    ɵɵtext(2);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵconditional(!(ctx_r0.selectedValue == null ? null : ctx_r0.selectedValue.toString()) || !!ctx_r0.placeholder ? 0 : -1);
    ɵɵadvance();
    ɵɵproperty("hidden", true);
    ɵɵadvance();
    ɵɵtextInterpolate(ctx_r0.displayValue);
  }
}
function PoSelectComponent_Conditional_8_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "option");
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵtextInterpolate(ctx_r0.displayValue || ctx_r0.placeholder);
  }
}
function PoSelectComponent_Conditional_9_For_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "option", 12);
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const item_r2 = ctx.$implicit;
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵproperty("disabled", ctx_r0.readonly)("value", item_r2 == null ? null : item_r2[ctx_r0.fieldValue]);
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", item_r2 == null ? null : item_r2[ctx_r0.fieldLabel], " ");
  }
}
function PoSelectComponent_Conditional_9_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵrepeaterCreate(0, PoSelectComponent_Conditional_9_For_1_Template, 2, 3, "option", 12, ɵɵrepeaterTrackByIdentity);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵrepeater(ctx_r0.optionWithoutGroup);
  }
}
function PoSelectComponent_Conditional_10_For_1_Conditional_0_For_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "option", 14);
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const subItem_r3 = ctx.$implicit;
    const ctx_r0 = ɵɵnextContext(4);
    ɵɵproperty("value", subItem_r3 == null ? null : subItem_r3[ctx_r0.fieldValue])("disabled", ctx_r0.readonly);
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", subItem_r3 == null ? null : subItem_r3[ctx_r0.fieldLabel], " ");
  }
}
function PoSelectComponent_Conditional_10_For_1_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "optgroup", 13);
    ɵɵrepeaterCreate(1, PoSelectComponent_Conditional_10_For_1_Conditional_0_For_2_Template, 2, 3, "option", 14, ɵɵrepeaterTrackByIdentity);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const item_r4 = ɵɵnextContext().$implicit;
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵproperty("label", ɵɵinterpolate(item_r4 == null ? null : item_r4.label))("disabled", ctx_r0.readonly);
    ɵɵadvance();
    ɵɵrepeater(item_r4.options);
  }
}
function PoSelectComponent_Conditional_10_For_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵconditionalCreate(0, PoSelectComponent_Conditional_10_For_1_Conditional_0_Template, 3, 3, "optgroup", 13);
  }
  if (rf & 2) {
    const item_r4 = ctx.$implicit;
    ɵɵconditional((item_r4 == null ? null : item_r4.options.length) > 0 ? 0 : -1);
  }
}
function PoSelectComponent_Conditional_10_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵrepeaterCreate(0, PoSelectComponent_Conditional_10_For_1_Template, 1, 1, null, null, ɵɵrepeaterTrackByIdentity);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵrepeater(ctx_r0.listGroupOptions);
  }
}
function PoSelectComponent_Conditional_11_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 7);
    ɵɵelement(1, "po-loading-icon", 15);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵproperty("p-size", ctx_r0.mapSizeToIcon(ctx_r0.size));
  }
}
function PoSelectComponent_Conditional_12_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-helper", 8, 1);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    const poHelper_r5 = ɵɵreadContextLet(0);
    ɵɵproperty("p-size", ctx_r0.size)("p-helper", poHelper_r5.helperSettings)("p-disabled", ctx_r0.isDisabled)("p-append-in-body", ctx_r0.appendBox);
  }
}
function PoSelectComponent_Conditional_13_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-field-container-bottom", 9);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("p-append-in-body", ctx_r0.appendBox)("p-help", ctx_r0.help)("p-disabled", ctx_r0.isDisabled)("p-error-pattern", ctx_r0.getErrorPattern())("p-error-limit", ctx_r0.errorLimit)("p-size", ctx_r0.size);
  }
}
var _c143 = ["textareaBody"];
function PoTextareaComponent_Conditional_7_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 6);
    ɵɵelement(1, "po-loading-icon", 8);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵproperty("p-size", ctx_r0.mapSizeToIcon(ctx_r0.size));
  }
}
function PoTextareaComponent_Conditional_8_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-field-container-bottom", 7);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    const poHelper_r2 = ɵɵreadContextLet(0);
    ɵɵproperty("p-append-in-body", ctx_r0.appendBox)("p-help", ctx_r0.help)("p-disabled", ctx_r0.isDisabled)("p-error-pattern", ctx_r0.getErrorPattern())("p-error-limit", ctx_r0.errorLimit)("p-size", ctx_r0.size)("p-helper", !ctx_r0.label ? poHelper_r2.helperSettings : void 0)("p-show-helper", !ctx_r0.label ? ctx_r0.displayAdditionalHelp : void 0);
  }
}
function PoInfoComponent_Conditional_5_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-link", 5);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵproperty("p-label", ctx_r0.value)("p-url", ctx_r0.url)("p-open-new-tab", true);
  }
}
function PoInfoComponent_Conditional_5_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-link", 5);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵproperty("p-label", ctx_r0.value)("p-url", ctx_r0.url)("p-open-new-tab", false);
  }
}
function PoInfoComponent_Conditional_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵconditionalCreate(0, PoInfoComponent_Conditional_5_Conditional_0_Template, 1, 3, "po-link", 5)(1, PoInfoComponent_Conditional_5_Conditional_1_Template, 1, 3, "po-link", 5);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵconditional(ctx_r0.isExternalLink ? 0 : 1);
  }
}
function PoInfoComponent_Conditional_6_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span", 4);
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵtextInterpolate(ctx_r0.value);
  }
}
function PoDynamicContainerComponent_For_2_ng_container_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoDynamicContainerComponent_For_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div")(1, "po-container", 1)(2, "div", 2);
    ɵɵtemplate(3, PoDynamicContainerComponent_For_2_ng_container_3_Template, 1, 0, "ng-container", 3);
    ɵɵelementEnd()()();
  }
  if (rf & 2) {
    const fieldsInContainer_r1 = ctx.$implicit;
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵproperty("p-title", fieldsInContainer_r1[0].container);
    ɵɵadvance(2);
    ɵɵproperty("ngTemplateOutlet", ctx_r1.content)("ngTemplateOutletContext", ɵɵpureFunction1(3, _c21, fieldsInContainer_r1));
  }
}
var _c144 = ["component"];
function PoDynamicFormFieldsComponent_Conditional_0_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoDynamicFormFieldsComponent_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 4);
    ɵɵtemplate(1, PoDynamicFormFieldsComponent_Conditional_0_ng_container_1_Template, 1, 0, "ng-container", 5);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    const poContent_r2 = ɵɵreference(2);
    const poContentContainer_r3 = ɵɵreference(4);
    ɵɵclassProp("po-dynamic", ctx_r0.hasContainers);
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", ctx_r0.hasContainers ? poContentContainer_r3 : poContent_r2)("ngTemplateOutletContext", ɵɵpureFunction1(4, _c21, ctx_r0.hasContainers ? ctx_r0.containerFields : ctx_r0.visibleFields));
  }
}
function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-divider", 6);
  }
  if (rf & 2) {
    const field_r4 = ɵɵnextContext().$implicit;
    ɵɵproperty("p-label", field_r4.divider);
  }
}
function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r5 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-datepicker", 25, 2);
    ɵɵtwoWayListener("ngModelChange", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_1_Template_po_datepicker_ngModelChange_0_listener($event) {
      ɵɵrestoreView(_r5);
      const field_r4 = ɵɵnextContext().$implicit;
      const ctx_r0 = ɵɵnextContext(2);
      ɵɵtwoWayBindingSet(ctx_r0.value[field_r4.property], $event) || (ctx_r0.value[field_r4.property] = $event);
      return ɵɵresetView($event);
    });
    ɵɵlistener("p-change", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_1_Template_po_datepicker_p_change_0_listener() {
      ɵɵrestoreView(_r5);
      const field_r4 = ɵɵnextContext().$implicit;
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.onChangeField(field_r4));
    })("p-change-model", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_1_Template_po_datepicker_p_change_model_0_listener() {
      ɵɵrestoreView(_r5);
      const field_r4 = ɵɵnextContext().$implicit;
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.onChangeFieldModel(field_r4));
    })("p-additional-help", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_1_Template_po_datepicker_p_additional_help_0_listener($event) {
      ɵɵrestoreView(_r5);
      const field_r4 = ɵɵnextContext().$implicit;
      return ɵɵresetView(field_r4.additionalHelp == null ? null : field_r4.additionalHelp($event));
    })("p-keydown", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_1_Template_po_datepicker_p_keydown_0_listener($event) {
      ɵɵrestoreView(_r5);
      const field_r4 = ɵɵnextContext().$implicit;
      return ɵɵresetView(field_r4.keydown == null ? null : field_r4.keydown($event));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const field_r4 = ɵɵnextContext().$implicit;
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵproperty("name", field_r4.property);
    ɵɵtwoWayProperty("ngModel", ctx_r0.value[field_r4.property]);
    ɵɵproperty("ngClass", field_r4.componentClass)("additionalHelpEventTrigger", field_r4.additionalHelp ? "event" : "noEvent")("p-additional-help-tooltip", field_r4.additionalHelpTooltip)("p-compact-label", field_r4.compactLabel)("p-clean", field_r4.clean)("p-disabled", ctx_r0.isDisabled(field_r4))("p-error-pattern", field_r4.errorMessage)("p-error-limit", field_r4.errorLimit)("p-error-async", field_r4.errorAsyncFunction)("p-auto-focus", field_r4.focus)("p-format", field_r4.format)("p-help", field_r4.help)("p-helper", field_r4.helper)("p-iso-format", field_r4.isoFormat)("p-label", field_r4.label)("p-loading", field_r4.loading)("p-locale", field_r4.locale)("p-max-date", field_r4.maxValue)("p-min-date", field_r4.minValue)("p-mode", field_r4.mode)("p-no-autocomplete", field_r4.noAutocomplete)("p-optional", field_r4.optional)("p-readonly", field_r4.readonly)("p-required", field_r4.required)("p-required-field-error-message", field_r4.requiredFieldErrorMessage)("p-size", field_r4.size || ctx_r0.componentsSize)("p-show-required", field_r4.showRequired)("p-year-range-limit", field_r4.yearRangeLimit)("p-placeholder", field_r4.placeholder);
  }
}
function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    const _r6 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-datetimepicker", 26, 2);
    ɵɵtwoWayListener("ngModelChange", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_2_Template_po_datetimepicker_ngModelChange_0_listener($event) {
      ɵɵrestoreView(_r6);
      const field_r4 = ɵɵnextContext().$implicit;
      const ctx_r0 = ɵɵnextContext(2);
      ɵɵtwoWayBindingSet(ctx_r0.value[field_r4.property], $event) || (ctx_r0.value[field_r4.property] = $event);
      return ɵɵresetView($event);
    });
    ɵɵlistener("p-change", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_2_Template_po_datetimepicker_p_change_0_listener() {
      ɵɵrestoreView(_r6);
      const field_r4 = ɵɵnextContext().$implicit;
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.onChangeField(field_r4));
    })("p-change-model", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_2_Template_po_datetimepicker_p_change_model_0_listener() {
      ɵɵrestoreView(_r6);
      const field_r4 = ɵɵnextContext().$implicit;
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.onChangeFieldModel(field_r4));
    })("p-keydown", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_2_Template_po_datetimepicker_p_keydown_0_listener($event) {
      ɵɵrestoreView(_r6);
      const field_r4 = ɵɵnextContext().$implicit;
      return ɵɵresetView(field_r4.keydown == null ? null : field_r4.keydown($event));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const field_r4 = ɵɵnextContext().$implicit;
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵproperty("name", field_r4.property);
    ɵɵtwoWayProperty("ngModel", ctx_r0.value[field_r4.property]);
    ɵɵproperty("ngClass", field_r4.componentClass)("p-append-in-body", field_r4.appendInBody)("p-clean", field_r4.clean)("p-compact-label", field_r4.compactLabel)("p-disabled", ctx_r0.isDisabled(field_r4))("p-error-pattern", field_r4.errorMessage)("p-error-limit", field_r4.errorLimit)("p-format-date", field_r4.format)("p-format-time", field_r4.formatTime)("p-help", field_r4.help)("p-helper", field_r4.helper)("p-label", field_r4.label)("p-loading", field_r4.loading)("p-locale", field_r4.locale)("p-max-date", field_r4.maxValue)("p-max-time", field_r4.maxTime)("p-min-date", field_r4.minValue)("p-min-time", field_r4.minTime)("p-minute-interval", field_r4.minuteInterval)("p-no-autocomplete", field_r4.noAutocomplete)("p-optional", field_r4.optional)("p-placeholder", field_r4.placeholder)("p-readonly", field_r4.readonly)("p-required", field_r4.required)("p-required-field-error-message", field_r4.requiredFieldErrorMessage)("p-second-interval", field_r4.secondInterval)("p-show-seconds", field_r4.showSeconds)("p-show-required", field_r4.showRequired)("p-size", field_r4.size || ctx_r0.componentsSize);
  }
}
function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    const _r7 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-datepicker-range", 27, 2);
    ɵɵtwoWayListener("ngModelChange", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_3_Template_po_datepicker_range_ngModelChange_0_listener($event) {
      ɵɵrestoreView(_r7);
      const field_r4 = ɵɵnextContext().$implicit;
      const ctx_r0 = ɵɵnextContext(2);
      ɵɵtwoWayBindingSet(ctx_r0.value[field_r4.property], $event) || (ctx_r0.value[field_r4.property] = $event);
      return ɵɵresetView($event);
    });
    ɵɵlistener("p-additional-help", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_3_Template_po_datepicker_range_p_additional_help_0_listener($event) {
      ɵɵrestoreView(_r7);
      const field_r4 = ɵɵnextContext().$implicit;
      return ɵɵresetView(field_r4.additionalHelp == null ? null : field_r4.additionalHelp($event));
    })("p-change", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_3_Template_po_datepicker_range_p_change_0_listener() {
      ɵɵrestoreView(_r7);
      const field_r4 = ɵɵnextContext().$implicit;
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.onChangeField(field_r4));
    })("p-change-model", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_3_Template_po_datepicker_range_p_change_model_0_listener() {
      ɵɵrestoreView(_r7);
      const field_r4 = ɵɵnextContext().$implicit;
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.onChangeFieldModel(field_r4));
    })("p-keydown", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_3_Template_po_datepicker_range_p_keydown_0_listener($event) {
      ɵɵrestoreView(_r7);
      const field_r4 = ɵɵnextContext().$implicit;
      return ɵɵresetView(field_r4.keydown == null ? null : field_r4.keydown($event));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const field_r4 = ɵɵnextContext().$implicit;
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵproperty("name", field_r4.property);
    ɵɵtwoWayProperty("ngModel", ctx_r0.value[field_r4.property]);
    ɵɵproperty("ngClass", field_r4.componentClass)("additionalHelpEventTrigger", field_r4.additionalHelp ? "event" : "noEvent")("p-additional-help-tooltip", field_r4.additionalHelpTooltip)("p-clean", field_r4.clean)("p-compact-label", field_r4.compactLabel)("p-disabled", ctx_r0.isDisabled(field_r4))("p-auto-focus", field_r4.focus)("p-help", field_r4.help)("p-helper", field_r4.helper)("p-label", field_r4.label)("p-literals", field_r4.literals)("p-loading", field_r4.loading)("p-max-date", field_r4.maxValue)("p-min-date", field_r4.minValue)("p-no-autocomplete", field_r4.noAutocomplete)("p-optional", field_r4.optional)("p-placeholder", field_r4.placeholder)("p-readonly", field_r4.readonly)("p-required", field_r4.required)("p-field-error-message", field_r4.errorMessage)("p-error-limit", field_r4.errorLimit)("p-show-required", field_r4.showRequired)("p-size", field_r4.size || ctx_r0.componentsSize)("p-range-presets", field_r4.rangePresets)("p-range-preset-options", field_r4.rangePresetOptions)("p-range-presets-order", field_r4.rangePresetsOrder);
  }
}
function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_4_Template(rf, ctx) {
  if (rf & 1) {
    const _r8 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-timepicker", 28, 2);
    ɵɵtwoWayListener("ngModelChange", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_4_Template_po_timepicker_ngModelChange_0_listener($event) {
      ɵɵrestoreView(_r8);
      const field_r4 = ɵɵnextContext().$implicit;
      const ctx_r0 = ɵɵnextContext(2);
      ɵɵtwoWayBindingSet(ctx_r0.value[field_r4.property], $event) || (ctx_r0.value[field_r4.property] = $event);
      return ɵɵresetView($event);
    });
    ɵɵlistener("p-change", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_4_Template_po_timepicker_p_change_0_listener() {
      ɵɵrestoreView(_r8);
      const field_r4 = ɵɵnextContext().$implicit;
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.onChangeField(field_r4));
    })("p-change-model", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_4_Template_po_timepicker_p_change_model_0_listener() {
      ɵɵrestoreView(_r8);
      const field_r4 = ɵɵnextContext().$implicit;
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.onChangeFieldModel(field_r4));
    })("p-keydown", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_4_Template_po_timepicker_p_keydown_0_listener($event) {
      ɵɵrestoreView(_r8);
      const field_r4 = ɵɵnextContext().$implicit;
      return ɵɵresetView(field_r4.keydown == null ? null : field_r4.keydown($event));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const field_r4 = ɵɵnextContext().$implicit;
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵproperty("name", field_r4.property);
    ɵɵtwoWayProperty("ngModel", ctx_r0.value[field_r4.property]);
    ɵɵproperty("ngClass", field_r4.componentClass)("p-append-in-body", field_r4.appendBox)("p-compact-label", field_r4.compactLabel)("p-clean", field_r4.clean)("p-disabled", ctx_r0.isDisabled(field_r4))("p-error-pattern", field_r4.errorMessage)("p-error-limit", field_r4.errorLimit)("p-auto-focus", field_r4.focus)("p-format", field_r4.format)("p-help", field_r4.help)("p-helper", field_r4.helper)("p-model-format", field_r4.modelFormat)("p-label", field_r4.label)("p-locale", field_r4.locale)("p-max-time", field_r4.maxValue)("p-min-time", field_r4.minValue)("p-minute-interval", field_r4.minuteInterval)("p-no-autocomplete", field_r4.noAutocomplete)("p-optional", field_r4.optional)("p-placeholder", field_r4.placeholder)("p-readonly", field_r4.readonly)("p-required", field_r4.required)("p-required-field-error-message", field_r4.requiredFieldErrorMessage)("p-second-interval", field_r4.secondInterval)("p-show-required", field_r4.showRequired)("p-show-seconds", field_r4.showSeconds)("p-size", field_r4.size || ctx_r0.componentsSize);
  }
}
function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_5_Template(rf, ctx) {
  if (rf & 1) {
    const _r9 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-input", 29, 2);
    ɵɵtwoWayListener("ngModelChange", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_5_Template_po_input_ngModelChange_0_listener($event) {
      ɵɵrestoreView(_r9);
      const field_r4 = ɵɵnextContext().$implicit;
      const ctx_r0 = ɵɵnextContext(2);
      ɵɵtwoWayBindingSet(ctx_r0.value[field_r4.property], $event) || (ctx_r0.value[field_r4.property] = $event);
      return ɵɵresetView($event);
    });
    ɵɵlistener("p-additional-help", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_5_Template_po_input_p_additional_help_0_listener($event) {
      ɵɵrestoreView(_r9);
      const field_r4 = ɵɵnextContext().$implicit;
      return ɵɵresetView(field_r4.additionalHelp == null ? null : field_r4.additionalHelp($event));
    })("p-change", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_5_Template_po_input_p_change_0_listener() {
      ɵɵrestoreView(_r9);
      const field_r4 = ɵɵnextContext().$implicit;
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.onChangeField(field_r4));
    })("p-change-model", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_5_Template_po_input_p_change_model_0_listener() {
      ɵɵrestoreView(_r9);
      const field_r4 = ɵɵnextContext().$implicit;
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.onChangeFieldModel(field_r4));
    })("p-keydown", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_5_Template_po_input_p_keydown_0_listener($event) {
      ɵɵrestoreView(_r9);
      const field_r4 = ɵɵnextContext().$implicit;
      return ɵɵresetView(field_r4.keydown == null ? null : field_r4.keydown($event));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const field_r4 = ɵɵnextContext().$implicit;
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵproperty("name", field_r4.property);
    ɵɵtwoWayProperty("ngModel", ctx_r0.value[field_r4.property]);
    ɵɵproperty("ngClass", field_r4.componentClass)("additionalHelpEventTrigger", field_r4.additionalHelp ? "event" : "noEvent")("p-additional-help-tooltip", field_r4.additionalHelpTooltip)("p-clean", field_r4.clean)("p-compact-label", field_r4.compactLabel)("p-disabled", ctx_r0.isDisabled(field_r4))("p-error-pattern", field_r4.errorMessage)("p-error-limit", field_r4.errorLimit)("p-error-async-properties", field_r4.errorAsyncProperties)("p-auto-focus", field_r4.focus)("p-help", field_r4.help)("p-helper", field_r4.helper)("p-label", field_r4.label)("p-loading", field_r4.loading)("p-mask-format-model", field_r4.maskFormatModel)("p-mask-no-length-validation", field_r4.maskNoLengthValidation)("p-mask", field_r4.mask)("p-maxlength", field_r4.maxLength)("p-minlength", field_r4.minLength)("p-no-autocomplete", field_r4.noAutocomplete)("p-optional", field_r4.optional)("p-pattern", field_r4.pattern)("p-required", field_r4.required)("p-required-field-error-message", field_r4.requiredFieldErrorMessage)("p-show-required", field_r4.showRequired)("p-size", field_r4.size || ctx_r0.componentsSize)("p-icon", field_r4.icon)("p-placeholder", field_r4.placeholder)("p-readonly", field_r4.readonly);
  }
}
function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_6_Template(rf, ctx) {
  if (rf & 1) {
    const _r10 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-number", 30, 2);
    ɵɵtwoWayListener("ngModelChange", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_6_Template_po_number_ngModelChange_0_listener($event) {
      ɵɵrestoreView(_r10);
      const field_r4 = ɵɵnextContext().$implicit;
      const ctx_r0 = ɵɵnextContext(2);
      ɵɵtwoWayBindingSet(ctx_r0.value[field_r4.property], $event) || (ctx_r0.value[field_r4.property] = $event);
      return ɵɵresetView($event);
    });
    ɵɵlistener("p-additional-help", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_6_Template_po_number_p_additional_help_0_listener($event) {
      ɵɵrestoreView(_r10);
      const field_r4 = ɵɵnextContext().$implicit;
      return ɵɵresetView(field_r4.additionalHelp == null ? null : field_r4.additionalHelp($event));
    })("p-change", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_6_Template_po_number_p_change_0_listener() {
      ɵɵrestoreView(_r10);
      const field_r4 = ɵɵnextContext().$implicit;
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.onChangeField(field_r4));
    })("p-change-model", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_6_Template_po_number_p_change_model_0_listener() {
      ɵɵrestoreView(_r10);
      const field_r4 = ɵɵnextContext().$implicit;
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.onChangeFieldModel(field_r4));
    })("p-keydown", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_6_Template_po_number_p_keydown_0_listener($event) {
      ɵɵrestoreView(_r10);
      const field_r4 = ɵɵnextContext().$implicit;
      return ɵɵresetView(field_r4.keydown == null ? null : field_r4.keydown($event));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const field_r4 = ɵɵnextContext().$implicit;
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵproperty("name", field_r4.property);
    ɵɵtwoWayProperty("ngModel", ctx_r0.value[field_r4.property]);
    ɵɵproperty("ngClass", field_r4.componentClass)("additionalHelpEventTrigger", field_r4.additionalHelp ? "event" : "noEvent")("p-additional-help-tooltip", field_r4.additionalHelpTooltip)("p-clean", field_r4.clean)("p-compact-label", field_r4.compactLabel)("p-disabled", ctx_r0.isDisabled(field_r4))("p-error-pattern", field_r4.errorMessage)("p-error-limit", field_r4.errorLimit)("p-error-async-properties", field_r4.errorAsyncProperties)("p-auto-focus", field_r4.focus)("p-help", field_r4.help)("p-helper", field_r4.helper)("p-label", field_r4.label)("p-loading", field_r4.loading)("p-min", field_r4.minValue)("p-max", field_r4.maxValue)("p-maxlength", field_r4.maxLength)("p-minlength", field_r4.minLength)("p-no-autocomplete", field_r4.noAutocomplete)("p-optional", field_r4.optional)("p-step", field_r4.step)("p-readonly", field_r4.readonly)("p-required", field_r4.required)("p-required-field-error-message", field_r4.requiredFieldErrorMessage)("p-show-required", field_r4.showRequired)("p-size", field_r4.size || ctx_r0.componentsSize)("p-icon", field_r4.icon)("p-placeholder", field_r4.placeholder);
  }
}
function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_7_Template(rf, ctx) {
  if (rf & 1) {
    const _r11 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-decimal", 31, 2);
    ɵɵtwoWayListener("ngModelChange", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_7_Template_po_decimal_ngModelChange_0_listener($event) {
      ɵɵrestoreView(_r11);
      const field_r4 = ɵɵnextContext().$implicit;
      const ctx_r0 = ɵɵnextContext(2);
      ɵɵtwoWayBindingSet(ctx_r0.value[field_r4.property], $event) || (ctx_r0.value[field_r4.property] = $event);
      return ɵɵresetView($event);
    });
    ɵɵlistener("p-additional-help", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_7_Template_po_decimal_p_additional_help_0_listener($event) {
      ɵɵrestoreView(_r11);
      const field_r4 = ɵɵnextContext().$implicit;
      return ɵɵresetView(field_r4.additionalHelp == null ? null : field_r4.additionalHelp($event));
    })("p-change", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_7_Template_po_decimal_p_change_0_listener() {
      ɵɵrestoreView(_r11);
      const field_r4 = ɵɵnextContext().$implicit;
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.onChangeField(field_r4));
    })("p-change-model", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_7_Template_po_decimal_p_change_model_0_listener() {
      ɵɵrestoreView(_r11);
      const field_r4 = ɵɵnextContext().$implicit;
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.onChangeFieldModel(field_r4));
    })("p-keydown", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_7_Template_po_decimal_p_keydown_0_listener($event) {
      ɵɵrestoreView(_r11);
      const field_r4 = ɵɵnextContext().$implicit;
      return ɵɵresetView(field_r4.keydown == null ? null : field_r4.keydown($event));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const field_r4 = ɵɵnextContext().$implicit;
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵproperty("name", field_r4.property);
    ɵɵtwoWayProperty("ngModel", ctx_r0.value[field_r4.property]);
    ɵɵproperty("ngClass", field_r4.componentClass)("additionalHelpEventTrigger", field_r4.additionalHelp ? "event" : "noEvent")("p-additional-help-tooltip", field_r4.additionalHelpTooltip)("p-clean", field_r4.clean)("p-compact-label", field_r4.compactLabel)("p-error-pattern", field_r4.errorMessage)("p-error-limit", field_r4.errorLimit)("p-error-async-properties", field_r4.errorAsyncProperties)("p-disabled", ctx_r0.isDisabled(field_r4))("p-decimals-length", field_r4.decimalsLength)("p-display-format", field_r4.displayFormat)("p-thousand-maxlength", field_r4.thousandMaxlength)("p-icon", field_r4.icon)("p-auto-focus", field_r4.focus)("p-help", field_r4.help)("p-helper", field_r4.helper)("p-label", field_r4.label)("p-loading", field_r4.loading)("p-locale", field_r4.locale)("p-mask-no-length-validation", field_r4.maskNoLengthValidation)("p-min", field_r4.minValue)("p-max", field_r4.maxValue)("p-maxlength", field_r4.maxLength)("p-minlength", field_r4.minLength)("p-no-autocomplete", field_r4.noAutocomplete)("p-optional", field_r4.optional)("p-readonly", field_r4.readonly)("p-required", field_r4.required)("p-required-field-error-message", field_r4.requiredFieldErrorMessage)("p-show-required", field_r4.showRequired)("p-size", field_r4.size || ctx_r0.componentsSize)("p-placeholder", field_r4.placeholder);
  }
}
function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_8_Template(rf, ctx) {
  if (rf & 1) {
    const _r12 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-select", 32, 2);
    ɵɵtwoWayListener("ngModelChange", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_8_Template_po_select_ngModelChange_0_listener($event) {
      ɵɵrestoreView(_r12);
      const field_r4 = ɵɵnextContext().$implicit;
      const ctx_r0 = ɵɵnextContext(2);
      ɵɵtwoWayBindingSet(ctx_r0.value[field_r4.property], $event) || (ctx_r0.value[field_r4.property] = $event);
      return ɵɵresetView($event);
    });
    ɵɵlistener("p-additional-help", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_8_Template_po_select_p_additional_help_0_listener($event) {
      ɵɵrestoreView(_r12);
      const field_r4 = ɵɵnextContext().$implicit;
      return ɵɵresetView(field_r4.additionalHelp == null ? null : field_r4.additionalHelp($event));
    })("p-change", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_8_Template_po_select_p_change_0_listener() {
      ɵɵrestoreView(_r12);
      const field_r4 = ɵɵnextContext().$implicit;
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.onChangeField(field_r4));
    })("p-change-model", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_8_Template_po_select_p_change_model_0_listener() {
      ɵɵrestoreView(_r12);
      const field_r4 = ɵɵnextContext().$implicit;
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.onChangeFieldModel(field_r4));
    })("p-keydown", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_8_Template_po_select_p_keydown_0_listener($event) {
      ɵɵrestoreView(_r12);
      const field_r4 = ɵɵnextContext().$implicit;
      return ɵɵresetView(field_r4.keydown == null ? null : field_r4.keydown($event));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const field_r4 = ɵɵnextContext().$implicit;
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵproperty("name", field_r4.property);
    ɵɵtwoWayProperty("ngModel", ctx_r0.value[field_r4.property]);
    ɵɵproperty("ngClass", field_r4.componentClass)("additionalHelpEventTrigger", field_r4.additionalHelp ? "event" : "noEvent")("p-additional-help-tooltip", field_r4.additionalHelpTooltip)("p-field-label", field_r4.fieldLabel)("p-field-value", field_r4.fieldValue)("p-compact-label", field_r4.compactLabel)("p-disabled", ctx_r0.isDisabled(field_r4))("p-help", field_r4.help)("p-helper", field_r4.helper)("p-label", field_r4.label)("p-loading", field_r4.loading)("p-optional", field_r4.optional)("p-options", field_r4.options)("p-required", field_r4.required)("p-field-error-message", field_r4.errorMessage)("p-error-limit", field_r4.errorLimit)("p-show-required", field_r4.showRequired)("p-size", field_r4.size || ctx_r0.componentsSize)("p-placeholder", field_r4.placeholder)("p-readonly", field_r4.readonly);
  }
}
function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_9_Template(rf, ctx) {
  if (rf & 1) {
    const _r13 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-radio-group", 33, 2);
    ɵɵtwoWayListener("ngModelChange", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_9_Template_po_radio_group_ngModelChange_0_listener($event) {
      ɵɵrestoreView(_r13);
      const field_r4 = ɵɵnextContext().$implicit;
      const ctx_r0 = ɵɵnextContext(2);
      ɵɵtwoWayBindingSet(ctx_r0.value[field_r4.property], $event) || (ctx_r0.value[field_r4.property] = $event);
      return ɵɵresetView($event);
    });
    ɵɵlistener("p-additional-help", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_9_Template_po_radio_group_p_additional_help_0_listener($event) {
      ɵɵrestoreView(_r13);
      const field_r4 = ɵɵnextContext().$implicit;
      return ɵɵresetView(field_r4.additionalHelp == null ? null : field_r4.additionalHelp($event));
    })("p-change", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_9_Template_po_radio_group_p_change_0_listener() {
      ɵɵrestoreView(_r13);
      const field_r4 = ɵɵnextContext().$implicit;
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.onChangeField(field_r4));
    })("p-change-model", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_9_Template_po_radio_group_p_change_model_0_listener() {
      ɵɵrestoreView(_r13);
      const field_r4 = ɵɵnextContext().$implicit;
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.onChangeFieldModel(field_r4));
    })("p-keydown", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_9_Template_po_radio_group_p_keydown_0_listener($event) {
      ɵɵrestoreView(_r13);
      const field_r4 = ɵɵnextContext().$implicit;
      return ɵɵresetView(field_r4.keydown == null ? null : field_r4.keydown($event));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const field_r4 = ɵɵnextContext().$implicit;
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵproperty("name", field_r4.property);
    ɵɵtwoWayProperty("ngModel", ctx_r0.value[field_r4.property]);
    ɵɵproperty("ngClass", field_r4.componentClass)("additionalHelpEventTrigger", field_r4.additionalHelp ? "event" : "noEvent")("p-additional-help-tooltip", field_r4.additionalHelpTooltip)("p-columns", field_r4.columns || 3)("p-compact-label", field_r4.compactLabel)("p-auto-focus", field_r4.focus)("p-disabled", ctx_r0.isDisabled(field_r4))("p-help", field_r4.help)("p-helper", field_r4.helper)("p-label", field_r4.label)("p-optional", field_r4.optional)("p-options", field_r4.options)("p-required", field_r4.required)("p-field-error-message", field_r4.errorMessage)("p-error-limit", field_r4.errorLimit)("p-show-required", field_r4.showRequired)("p-size", field_r4.size || ctx_r0.componentsSize);
  }
}
function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_10_Template(rf, ctx) {
  if (rf & 1) {
    const _r14 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-switch", 34, 2);
    ɵɵtwoWayListener("ngModelChange", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_10_Template_po_switch_ngModelChange_0_listener($event) {
      ɵɵrestoreView(_r14);
      const field_r4 = ɵɵnextContext().$implicit;
      const ctx_r0 = ɵɵnextContext(2);
      ɵɵtwoWayBindingSet(ctx_r0.value[field_r4.property], $event) || (ctx_r0.value[field_r4.property] = $event);
      return ɵɵresetView($event);
    });
    ɵɵlistener("p-additional-help", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_10_Template_po_switch_p_additional_help_0_listener($event) {
      ɵɵrestoreView(_r14);
      const field_r4 = ɵɵnextContext().$implicit;
      return ɵɵresetView(field_r4.additionalHelp == null ? null : field_r4.additionalHelp($event));
    })("p-change", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_10_Template_po_switch_p_change_0_listener() {
      ɵɵrestoreView(_r14);
      const field_r4 = ɵɵnextContext().$implicit;
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.onChangeField(field_r4));
    })("p-change-model", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_10_Template_po_switch_p_change_model_0_listener() {
      ɵɵrestoreView(_r14);
      const field_r4 = ɵɵnextContext().$implicit;
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.onChangeFieldModel(field_r4));
    })("p-keydown", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_10_Template_po_switch_p_keydown_0_listener($event) {
      ɵɵrestoreView(_r14);
      const field_r4 = ɵɵnextContext().$implicit;
      return ɵɵresetView(field_r4.keydown == null ? null : field_r4.keydown($event));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const field_r4 = ɵɵnextContext().$implicit;
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵproperty("name", field_r4.property);
    ɵɵtwoWayProperty("ngModel", ctx_r0.value[field_r4.property]);
    ɵɵproperty("ngClass", field_r4.componentClass)("additionalHelpEventTrigger", field_r4.additionalHelp ? "event" : "noEvent")("p-additional-help-tooltip", field_r4.additionalHelpTooltip)("p-compact-label", field_r4.compactLabel)("p-disabled", ctx_r0.isDisabled(field_r4))("p-error-limit", field_r4.errorLimit)("p-field-error-message", field_r4.errorMessage)("p-format-model", field_r4.formatModel)("p-help", field_r4.help)("p-helper", field_r4.helper)("p-label", field_r4.label)("p-label-off", field_r4.booleanFalse)("p-label-on", field_r4.booleanTrue)("p-label-position", field_r4.labelPosition)("p-loading", field_r4.loading)("p-hide-label-status", field_r4.hideLabelStatus)("p-invalid-value", field_r4.invalidValue)("p-size", field_r4.size || ctx_r0.componentsSize);
  }
}
function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_11_Template(rf, ctx) {
  if (rf & 1) {
    const _r15 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-checkbox", 35, 2);
    ɵɵtwoWayListener("ngModelChange", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_11_Template_po_checkbox_ngModelChange_0_listener($event) {
      ɵɵrestoreView(_r15);
      const field_r4 = ɵɵnextContext().$implicit;
      const ctx_r0 = ɵɵnextContext(2);
      ɵɵtwoWayBindingSet(ctx_r0.value[field_r4.property], $event) || (ctx_r0.value[field_r4.property] = $event);
      return ɵɵresetView($event);
    });
    ɵɵlistener("p-additional-help", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_11_Template_po_checkbox_p_additional_help_0_listener($event) {
      ɵɵrestoreView(_r15);
      const field_r4 = ɵɵnextContext().$implicit;
      return ɵɵresetView(field_r4.additionalHelp == null ? null : field_r4.additionalHelp($event));
    })("p-change", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_11_Template_po_checkbox_p_change_0_listener() {
      ɵɵrestoreView(_r15);
      const field_r4 = ɵɵnextContext().$implicit;
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.onChangeField(field_r4));
    })("p-change-model", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_11_Template_po_checkbox_p_change_model_0_listener() {
      ɵɵrestoreView(_r15);
      const field_r4 = ɵɵnextContext().$implicit;
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.onChangeFieldModel(field_r4));
    })("p-keydown", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_11_Template_po_checkbox_p_keydown_0_listener($event) {
      ɵɵrestoreView(_r15);
      const field_r4 = ɵɵnextContext().$implicit;
      return ɵɵresetView(field_r4.keydown == null ? null : field_r4.keydown($event));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const field_r4 = ɵɵnextContext().$implicit;
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵproperty("name", field_r4.property);
    ɵɵtwoWayProperty("ngModel", ctx_r0.value[field_r4.property]);
    ɵɵproperty("ngClass", field_r4.componentClass)("additionalHelpEventTrigger", field_r4.additionalHelp ? "event" : "noEvent")("p-additional-help-tooltip", field_r4.additionalHelpTooltip)("p-auto-focus", field_r4.focus)("p-compact-label", field_r4.compactLabel)("p-disabled", ctx_r0.isDisabled(field_r4))("p-help", field_r4.help)("p-helper", field_r4.helper)("p-label", field_r4.label)("p-size", field_r4.size || ctx_r0.componentsSize);
  }
}
function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_12_Template(rf, ctx) {
  if (rf & 1) {
    const _r16 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-combo", 36, 2);
    ɵɵtwoWayListener("ngModelChange", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_12_Template_po_combo_ngModelChange_0_listener($event) {
      ɵɵrestoreView(_r16);
      const field_r4 = ɵɵnextContext().$implicit;
      const ctx_r0 = ɵɵnextContext(2);
      ɵɵtwoWayBindingSet(ctx_r0.value[field_r4.property], $event) || (ctx_r0.value[field_r4.property] = $event);
      return ɵɵresetView($event);
    });
    ɵɵlistener("p-change", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_12_Template_po_combo_p_change_0_listener($event) {
      ɵɵrestoreView(_r16);
      const field_r4 = ɵɵnextContext().$implicit;
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.onChangeField(field_r4, $event));
    })("p-change-model", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_12_Template_po_combo_p_change_model_0_listener() {
      ɵɵrestoreView(_r16);
      const field_r4 = ɵɵnextContext().$implicit;
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.onChangeFieldModel(field_r4));
    })("p-keydown", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_12_Template_po_combo_p_keydown_0_listener($event) {
      ɵɵrestoreView(_r16);
      const field_r4 = ɵɵnextContext().$implicit;
      return ɵɵresetView(field_r4.keydown == null ? null : field_r4.keydown($event));
    })("p-additional-help", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_12_Template_po_combo_p_additional_help_0_listener($event) {
      ɵɵrestoreView(_r16);
      const field_r4 = ɵɵnextContext().$implicit;
      return ɵɵresetView(field_r4.additionalHelp == null ? null : field_r4.additionalHelp($event));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const field_r4 = ɵɵnextContext().$implicit;
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵproperty("name", field_r4.property);
    ɵɵtwoWayProperty("ngModel", ctx_r0.value[field_r4.property]);
    ɵɵproperty("ngClass", field_r4.componentClass)("additionalHelpEventTrigger", field_r4.additionalHelp ? "event" : "noEvent")("p-additional-help-tooltip", field_r4.additionalHelpTooltip)("p-auto-focus", field_r4.focus)("p-compact-label", field_r4.compactLabel)("p-clean", field_r4.clean)("p-disabled", ctx_r0.isDisabled(field_r4))("p-field-label", field_r4.fieldLabel)("p-field-value", field_r4.fieldValue)("p-filter-params", field_r4.params)("p-filter-service", field_r4.optionsService)("p-help", field_r4.help)("p-helper", field_r4.helper)("p-infinite-scroll", field_r4.infiniteScroll)("p-infinite-scroll-distance", field_r4.infiniteScrollDistance || 100)("p-label", field_r4.label)("p-literals", field_r4.literals)("p-loading", field_r4.loading)("p-options", field_r4.options)("p-optional", field_r4.optional)("p-sort", field_r4.sort)("p-required", field_r4.required)("p-field-error-message", field_r4.errorMessage)("p-error-limit", field_r4.errorLimit)("p-show-required", field_r4.showRequired)("p-icon", field_r4.icon)("p-placeholder", field_r4.placeholder)("p-filter-minlength", field_r4.filterMinlength)("p-filter-mode", field_r4.filterMode)("p-disabled-init-filter", field_r4.disabledInitFilter)("p-disabled-tab-filter", field_r4.disabledTabFilter)("p-debounce-time", field_r4.debounceTime)("p-change-on-enter", field_r4.changeOnEnter)("p-remove-initial-filter", field_r4.removeInitialFilter)("p-size", field_r4.size || ctx_r0.componentsSize)("p-listbox-control-position", field_r4.listboxControlPosition);
  }
}
function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_13_Template(rf, ctx) {
  if (rf & 1) {
    const _r17 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-lookup", 37, 2);
    ɵɵtwoWayListener("ngModelChange", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_13_Template_po_lookup_ngModelChange_0_listener($event) {
      ɵɵrestoreView(_r17);
      const field_r4 = ɵɵnextContext().$implicit;
      const ctx_r0 = ɵɵnextContext(2);
      ɵɵtwoWayBindingSet(ctx_r0.value[field_r4.property], $event) || (ctx_r0.value[field_r4.property] = $event);
      return ɵɵresetView($event);
    });
    ɵɵlistener("p-change", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_13_Template_po_lookup_p_change_0_listener() {
      ɵɵrestoreView(_r17);
      const field_r4 = ɵɵnextContext().$implicit;
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.onChangeField(field_r4));
    })("p-change-model", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_13_Template_po_lookup_p_change_model_0_listener() {
      ɵɵrestoreView(_r17);
      const field_r4 = ɵɵnextContext().$implicit;
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.onChangeFieldModel(field_r4));
    })("p-additional-help", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_13_Template_po_lookup_p_additional_help_0_listener($event) {
      ɵɵrestoreView(_r17);
      const field_r4 = ɵɵnextContext().$implicit;
      return ɵɵresetView(field_r4.additionalHelp == null ? null : field_r4.additionalHelp($event));
    })("p-change-visible-columns", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_13_Template_po_lookup_p_change_visible_columns_0_listener($event) {
      ɵɵrestoreView(_r17);
      const field_r4 = ɵɵnextContext().$implicit;
      return ɵɵresetView(field_r4.changeVisibleColumns == null ? null : field_r4.changeVisibleColumns($event));
    })("p-keydown", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_13_Template_po_lookup_p_keydown_0_listener($event) {
      ɵɵrestoreView(_r17);
      const field_r4 = ɵɵnextContext().$implicit;
      return ɵɵresetView(field_r4.keydown == null ? null : field_r4.keydown($event));
    })("p-restore-column-manager", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_13_Template_po_lookup_p_restore_column_manager_0_listener($event) {
      ɵɵrestoreView(_r17);
      const field_r4 = ɵɵnextContext().$implicit;
      return ɵɵresetView(field_r4.columnRestoreManager == null ? null : field_r4.columnRestoreManager($event));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const field_r4 = ɵɵnextContext().$implicit;
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵproperty("name", field_r4.property);
    ɵɵtwoWayProperty("ngModel", ctx_r0.value[field_r4.property]);
    ɵɵproperty("ngClass", field_r4.componentClass)("additionalHelpEventTrigger", field_r4.additionalHelp ? "event" : "noEvent")("p-additional-help-tooltip", field_r4.additionalHelpTooltip)("p-compact-label", field_r4.compactLabel)("p-clean", field_r4.clean)("p-columns", field_r4.columns)("p-disabled", ctx_r0.isDisabled(field_r4))("p-field-label", field_r4.fieldLabel || "label")("p-field-value", field_r4.fieldValue || "value")("p-filter-params", field_r4.params)("p-filter-service", field_r4.searchService)("p-auto-focus", field_r4.focus)("p-auto-height", field_r4.autoHeight)("p-help", field_r4.help)("p-helper", field_r4.helper)("p-field-format", field_r4.format)("p-infinite-scroll", field_r4.infiniteScroll)("p-label", field_r4.label)("p-literals", field_r4.literals)("p-loading", field_r4.loading)("p-multiple", field_r4.multiple)("p-no-autocomplete", field_r4.noAutocomplete)("p-optional", field_r4.optional)("p-required", field_r4.required)("p-field-error-message", field_r4.errorMessage)("p-error-limit", field_r4.errorLimit)("p-show-required", field_r4.showRequired)("p-size", field_r4.size || ctx_r0.componentsSize)("p-placeholder", field_r4.placeholder)("p-advanced-filters", field_r4.advancedFilters);
  }
}
function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_14_Template(rf, ctx) {
  if (rf & 1) {
    const _r18 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-checkbox-group", 38, 2);
    ɵɵtwoWayListener("ngModelChange", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_14_Template_po_checkbox_group_ngModelChange_0_listener($event) {
      ɵɵrestoreView(_r18);
      const field_r4 = ɵɵnextContext().$implicit;
      const ctx_r0 = ɵɵnextContext(2);
      ɵɵtwoWayBindingSet(ctx_r0.value[field_r4.property], $event) || (ctx_r0.value[field_r4.property] = $event);
      return ɵɵresetView($event);
    });
    ɵɵlistener("p-additional-help", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_14_Template_po_checkbox_group_p_additional_help_0_listener($event) {
      ɵɵrestoreView(_r18);
      const field_r4 = ɵɵnextContext().$implicit;
      return ɵɵresetView(field_r4.additionalHelp == null ? null : field_r4.additionalHelp($event));
    })("p-change", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_14_Template_po_checkbox_group_p_change_0_listener() {
      ɵɵrestoreView(_r18);
      const field_r4 = ɵɵnextContext().$implicit;
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.onChangeField(field_r4));
    })("p-change-model", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_14_Template_po_checkbox_group_p_change_model_0_listener() {
      ɵɵrestoreView(_r18);
      const field_r4 = ɵɵnextContext().$implicit;
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.onChangeFieldModel(field_r4));
    })("p-keydown", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_14_Template_po_checkbox_group_p_keydown_0_listener($event) {
      ɵɵrestoreView(_r18);
      const field_r4 = ɵɵnextContext().$implicit;
      return ɵɵresetView(field_r4.keydown == null ? null : field_r4.keydown($event));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const field_r4 = ɵɵnextContext().$implicit;
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵproperty("name", field_r4.property);
    ɵɵtwoWayProperty("ngModel", ctx_r0.value[field_r4.property]);
    ɵɵproperty("ngClass", field_r4.componentClass)("additionalHelpEventTrigger", field_r4.additionalHelp ? "event" : "noEvent")("p-additional-help-tooltip", field_r4.additionalHelpTooltip)("p-columns", field_r4.columns || 3)("p-compact-label", field_r4.compactLabel)("p-auto-focus", field_r4.focus)("p-disabled", ctx_r0.isDisabled(field_r4))("p-help", field_r4.help)("p-helper", field_r4.helper)("p-label", field_r4.label)("p-optional", field_r4.optional)("p-options", field_r4.options)("p-required", field_r4.required)("p-show-required", field_r4.showRequired)("p-field-error-message", field_r4.errorMessage)("p-error-limit", field_r4.errorLimit)("p-size", field_r4.size || ctx_r0.componentsSize);
  }
}
function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_15_Template(rf, ctx) {
  if (rf & 1) {
    const _r19 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-multiselect", 39, 2);
    ɵɵtwoWayListener("ngModelChange", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_15_Template_po_multiselect_ngModelChange_0_listener($event) {
      ɵɵrestoreView(_r19);
      const field_r4 = ɵɵnextContext().$implicit;
      const ctx_r0 = ɵɵnextContext(2);
      ɵɵtwoWayBindingSet(ctx_r0.value[field_r4.property], $event) || (ctx_r0.value[field_r4.property] = $event);
      return ɵɵresetView($event);
    });
    ɵɵlistener("p-change", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_15_Template_po_multiselect_p_change_0_listener() {
      ɵɵrestoreView(_r19);
      const field_r4 = ɵɵnextContext().$implicit;
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.onChangeField(field_r4));
    })("p-change-model", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_15_Template_po_multiselect_p_change_model_0_listener() {
      ɵɵrestoreView(_r19);
      const field_r4 = ɵɵnextContext().$implicit;
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.onChangeFieldModel(field_r4));
    })("p-additional-help", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_15_Template_po_multiselect_p_additional_help_0_listener($event) {
      ɵɵrestoreView(_r19);
      const field_r4 = ɵɵnextContext().$implicit;
      return ɵɵresetView(field_r4.additionalHelp == null ? null : field_r4.additionalHelp($event));
    })("p-keydown", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_15_Template_po_multiselect_p_keydown_0_listener($event) {
      ɵɵrestoreView(_r19);
      const field_r4 = ɵɵnextContext().$implicit;
      return ɵɵresetView(field_r4.keydown == null ? null : field_r4.keydown($event));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const field_r4 = ɵɵnextContext().$implicit;
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵproperty("name", field_r4.property);
    ɵɵtwoWayProperty("ngModel", ctx_r0.value[field_r4.property]);
    ɵɵproperty("ngClass", field_r4.componentClass)("additionalHelpEventTrigger", field_r4.additionalHelp ? "event" : "noEvent")("p-additional-help-tooltip", field_r4.additionalHelpTooltip)("p-compact-label", field_r4.compactLabel)("p-disabled", ctx_r0.isDisabled(field_r4))("p-auto-focus", field_r4.focus)("p-auto-height", field_r4.autoHeight)("p-help", field_r4.help)("p-helper", field_r4.helper)("p-label", field_r4.label)("p-literals", field_r4.literals)("p-loading", field_r4.loading)("p-optional", field_r4.optional)("p-options", field_r4.options)("p-required", field_r4.required)("p-field-error-message", field_r4.errorMessage)("p-error-limit", field_r4.errorLimit)("p-show-required", field_r4.showRequired)("p-placeholder", field_r4.placeholder)("p-field-label", field_r4.fieldLabel)("p-field-value", field_r4.fieldValue)("p-filter-service", field_r4.optionsService)("p-filter-mode", field_r4.filterMode)("p-debounce-time", field_r4.debounceTime)("p-sort", field_r4.sort)("p-placeholder-search", field_r4.placeholderSearch)("p-hide-search", field_r4.hideSearch)("p-hide-select-all", field_r4.hideSelectAll)("p-size", field_r4.size || ctx_r0.componentsSize)("p-listbox-control-position", field_r4.listboxControlPosition);
  }
}
function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_16_Template(rf, ctx) {
  if (rf & 1) {
    const _r20 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-textarea", 40, 2);
    ɵɵtwoWayListener("ngModelChange", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_16_Template_po_textarea_ngModelChange_0_listener($event) {
      ɵɵrestoreView(_r20);
      const field_r4 = ɵɵnextContext().$implicit;
      const ctx_r0 = ɵɵnextContext(2);
      ɵɵtwoWayBindingSet(ctx_r0.value[field_r4.property], $event) || (ctx_r0.value[field_r4.property] = $event);
      return ɵɵresetView($event);
    });
    ɵɵlistener("p-additional-help", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_16_Template_po_textarea_p_additional_help_0_listener($event) {
      ɵɵrestoreView(_r20);
      const field_r4 = ɵɵnextContext().$implicit;
      return ɵɵresetView(field_r4.additionalHelp == null ? null : field_r4.additionalHelp($event));
    })("p-change", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_16_Template_po_textarea_p_change_0_listener() {
      ɵɵrestoreView(_r20);
      const field_r4 = ɵɵnextContext().$implicit;
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.onChangeField(field_r4));
    })("p-change-model", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_16_Template_po_textarea_p_change_model_0_listener() {
      ɵɵrestoreView(_r20);
      const field_r4 = ɵɵnextContext().$implicit;
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.onChangeFieldModel(field_r4));
    })("p-keydown", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_16_Template_po_textarea_p_keydown_0_listener($event) {
      ɵɵrestoreView(_r20);
      const field_r4 = ɵɵnextContext().$implicit;
      return ɵɵresetView(field_r4.keydown == null ? null : field_r4.keydown($event));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const field_r4 = ɵɵnextContext().$implicit;
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵproperty("name", field_r4.property);
    ɵɵtwoWayProperty("ngModel", ctx_r0.value[field_r4.property]);
    ɵɵproperty("ngClass", field_r4.componentClass)("additionalHelpEventTrigger", field_r4.additionalHelp ? "event" : "noEvent")("p-additional-help-tooltip", field_r4.additionalHelpTooltip)("p-compact-label", field_r4.compactLabel)("p-disabled", ctx_r0.isDisabled(field_r4))("p-auto-focus", field_r4.focus)("p-help", field_r4.help)("p-helper", field_r4.helper)("p-label", field_r4.label)("p-loading", field_r4.loading)("p-maxlength", field_r4.maxLength)("p-minlength", field_r4.minLength)("p-optional", field_r4.optional)("p-readonly", field_r4.readonly)("p-required", field_r4.required)("p-field-error-message", field_r4.errorMessage)("p-error-limit", field_r4.errorLimit)("p-show-required", field_r4.showRequired)("p-rows", field_r4.rows)("p-size", field_r4.size || ctx_r0.componentsSize)("p-placeholder", field_r4.placeholder);
  }
}
function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_17_Template(rf, ctx) {
  if (rf & 1) {
    const _r21 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-password", 41, 2);
    ɵɵtwoWayListener("ngModelChange", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_17_Template_po_password_ngModelChange_0_listener($event) {
      ɵɵrestoreView(_r21);
      const field_r4 = ɵɵnextContext().$implicit;
      const ctx_r0 = ɵɵnextContext(2);
      ɵɵtwoWayBindingSet(ctx_r0.value[field_r4.property], $event) || (ctx_r0.value[field_r4.property] = $event);
      return ɵɵresetView($event);
    });
    ɵɵlistener("p-additional-help", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_17_Template_po_password_p_additional_help_0_listener($event) {
      ɵɵrestoreView(_r21);
      const field_r4 = ɵɵnextContext().$implicit;
      return ɵɵresetView(field_r4.additionalHelp == null ? null : field_r4.additionalHelp($event));
    })("p-change", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_17_Template_po_password_p_change_0_listener() {
      ɵɵrestoreView(_r21);
      const field_r4 = ɵɵnextContext().$implicit;
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.onChangeField(field_r4));
    })("p-change-model", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_17_Template_po_password_p_change_model_0_listener() {
      ɵɵrestoreView(_r21);
      const field_r4 = ɵɵnextContext().$implicit;
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.onChangeFieldModel(field_r4));
    })("p-keydown", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_17_Template_po_password_p_keydown_0_listener($event) {
      ɵɵrestoreView(_r21);
      const field_r4 = ɵɵnextContext().$implicit;
      return ɵɵresetView(field_r4.keydown == null ? null : field_r4.keydown($event));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const field_r4 = ɵɵnextContext().$implicit;
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵproperty("name", field_r4.property);
    ɵɵtwoWayProperty("ngModel", ctx_r0.value[field_r4.property]);
    ɵɵproperty("ngClass", field_r4.componentClass)("additionalHelpEventTrigger", field_r4.additionalHelp ? "event" : "noEvent")("p-additional-help-tooltip", field_r4.additionalHelpTooltip)("p-compact-label", field_r4.compactLabel)("p-clean", field_r4.clean)("p-disabled", ctx_r0.isDisabled(field_r4))("p-error-pattern", field_r4.errorMessage)("p-error-limit", field_r4.errorLimit)("p-error-async-properties", field_r4.errorAsyncProperties)("p-auto-focus", field_r4.focus)("p-help", field_r4.help)("p-helper", field_r4.helper)("p-hide-password-peek", field_r4.hidePasswordPeek)("p-icon", field_r4.icon)("p-label", field_r4.label)("p-loading", field_r4.loading)("p-maxlength", field_r4.maxLength)("p-minlength", field_r4.minLength)("p-no-autocomplete", field_r4.noAutocomplete)("p-optional", field_r4.optional)("p-pattern", field_r4.pattern)("p-readonly", field_r4.readonly)("p-required", field_r4.required)("p-required-field-error-message", field_r4.requiredFieldErrorMessage)("p-show-required", field_r4.showRequired)("p-size", field_r4.size || ctx_r0.componentsSize)("p-placeholder", field_r4.placeholder);
  }
}
function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_18_Template(rf, ctx) {
  if (rf & 1) {
    const _r22 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-upload", 42, 2);
    ɵɵtwoWayListener("ngModelChange", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_18_Template_po_upload_ngModelChange_0_listener($event) {
      ɵɵrestoreView(_r22);
      const field_r4 = ɵɵnextContext().$implicit;
      const ctx_r0 = ɵɵnextContext(2);
      ɵɵtwoWayBindingSet(ctx_r0.value[field_r4.property], $event) || (ctx_r0.value[field_r4.property] = $event);
      return ɵɵresetView($event);
    });
    ɵɵlistener("p-custom-action-click", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_18_Template_po_upload_p_custom_action_click_0_listener($event) {
      ɵɵrestoreView(_r22);
      const field_r4 = ɵɵnextContext().$implicit;
      return ɵɵresetView(field_r4.customActionClick($event));
    })("p-error", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_18_Template_po_upload_p_error_0_listener($event) {
      ɵɵrestoreView(_r22);
      const field_r4 = ɵɵnextContext().$implicit;
      return ɵɵresetView(field_r4.onError($event));
    })("p-success", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_18_Template_po_upload_p_success_0_listener($event) {
      ɵɵrestoreView(_r22);
      const field_r4 = ɵɵnextContext().$implicit;
      return ɵɵresetView(field_r4.onSuccess($event));
    })("p-upload", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_18_Template_po_upload_p_upload_0_listener($event) {
      ɵɵrestoreView(_r22);
      const field_r4 = ɵɵnextContext().$implicit;
      return ɵɵresetView(field_r4.onUpload($event));
    })("p-additional-help", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_18_Template_po_upload_p_additional_help_0_listener($event) {
      ɵɵrestoreView(_r22);
      const field_r4 = ɵɵnextContext().$implicit;
      return ɵɵresetView(field_r4.additionalHelp == null ? null : field_r4.additionalHelp($event));
    })("p-change", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_18_Template_po_upload_p_change_0_listener() {
      ɵɵrestoreView(_r22);
      const field_r4 = ɵɵnextContext().$implicit;
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.onChangeField(field_r4));
    })("p-change-model", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_18_Template_po_upload_p_change_model_0_listener() {
      ɵɵrestoreView(_r22);
      const field_r4 = ɵɵnextContext().$implicit;
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.onChangeFieldModel(field_r4));
    })("p-keydown", function PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_18_Template_po_upload_p_keydown_0_listener($event) {
      ɵɵrestoreView(_r22);
      const field_r4 = ɵɵnextContext().$implicit;
      return ɵɵresetView(field_r4.keydown == null ? null : field_r4.keydown($event));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const field_r4 = ɵɵnextContext().$implicit;
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵtwoWayProperty("ngModel", ctx_r0.value[field_r4.property]);
    ɵɵproperty("ngClass", field_r4.componentClass)("additionalHelpEventTrigger", field_r4.additionalHelp ? "event" : "noEvent")("p-additional-help-tooltip", field_r4.additionalHelpTooltip)("p-auto-upload", field_r4.autoUpload)("p-compact-label", field_r4.compactLabel)("p-directory", field_r4.directory)("p-disabled", ctx_r0.isDisabled(field_r4))("p-drag-drop", field_r4.dragDrop)("p-drag-drop-height", field_r4.dragDropHeight)("p-restrictions", field_r4.restrictions)("p-form-field", field_r4.formField)("p-headers", field_r4.headers)("p-help", field_r4.help)("p-helper", field_r4.helper)("p-hide-restrictions-info", field_r4.hideRestrictionsInfo)("p-hide-select-button", field_r4.hideSelectButton)("p-hide-send-button", field_r4.hideSendButton)("p-multiple", field_r4.multiple)("p-label", field_r4.label)("p-literals", field_r4.literals)("p-loading", field_r4.loading)("name", field_r4.property)("p-custom-action", field_r4.customAction)("p-optional", field_r4.optional)("p-required", field_r4.required)("p-show-required", field_r4.showRequired)("p-show-thumbnail", field_r4.showThumbnail ?? true)("p-size", field_r4.size || ctx_r0.componentsSize)("p-url", field_r4.url);
  }
}
function PoDynamicFormFieldsComponent_ng_template_1_For_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵconditionalCreate(0, PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_0_Template, 1, 1, "po-divider", 6);
    ɵɵconditionalCreate(1, PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_1_Template, 2, 31, "po-datepicker", 7);
    ɵɵconditionalCreate(2, PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_2_Template, 2, 31, "po-datetimepicker", 8);
    ɵɵconditionalCreate(3, PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_3_Template, 2, 28, "po-datepicker-range", 9);
    ɵɵconditionalCreate(4, PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_4_Template, 2, 29, "po-timepicker", 10);
    ɵɵconditionalCreate(5, PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_5_Template, 2, 31, "po-input", 11);
    ɵɵconditionalCreate(6, PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_6_Template, 2, 30, "po-number", 12);
    ɵɵconditionalCreate(7, PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_7_Template, 2, 34, "po-decimal", 13);
    ɵɵconditionalCreate(8, PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_8_Template, 2, 22, "po-select", 14);
    ɵɵconditionalCreate(9, PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_9_Template, 2, 19, "po-radio-group", 15);
    ɵɵconditionalCreate(10, PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_10_Template, 2, 20, "po-switch", 16);
    ɵɵconditionalCreate(11, PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_11_Template, 2, 12, "po-checkbox", 17);
    ɵɵconditionalCreate(12, PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_12_Template, 2, 38, "po-combo", 18);
    ɵɵconditionalCreate(13, PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_13_Template, 2, 32, "po-lookup", 19);
    ɵɵconditionalCreate(14, PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_14_Template, 2, 19, "po-checkbox-group", 20);
    ɵɵconditionalCreate(15, PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_15_Template, 2, 32, "po-multiselect", 21);
    ɵɵconditionalCreate(16, PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_16_Template, 2, 23, "po-textarea", 22);
    ɵɵconditionalCreate(17, PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_17_Template, 2, 29, "po-password", 23);
    ɵɵconditionalCreate(18, PoDynamicFormFieldsComponent_ng_template_1_For_1_Conditional_18_Template, 2, 30, "po-upload", 24);
  }
  if (rf & 2) {
    const field_r4 = ctx.$implicit;
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵconditional((field_r4 == null ? null : field_r4.divider == null ? null : field_r4.divider.trim()) ? 0 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r0.compareTo(field_r4.control, "datepicker") ? 1 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r0.compareTo(field_r4.control, "datetimepicker") ? 2 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r0.compareTo(field_r4.control, "datepickerrange") ? 3 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r0.compareTo(field_r4.control, "timepicker") ? 4 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r0.compareTo(field_r4.control, "input") ? 5 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r0.compareTo(field_r4.control, "number") ? 6 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r0.compareTo(field_r4.control, "decimal") ? 7 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r0.compareTo(field_r4.control, "select") ? 8 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r0.compareTo(field_r4.control, "radioGroup") ? 9 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r0.compareTo(field_r4.control, "switch") ? 10 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r0.compareTo(field_r4.control, "checkbox") ? 11 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r0.compareTo(field_r4.control, "combo") ? 12 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r0.compareTo(field_r4.control, "lookup") ? 13 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r0.compareTo(field_r4.control, "checkboxGroup") ? 14 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r0.compareTo(field_r4.control, "multiselect") ? 15 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r0.compareTo(field_r4.control, "textarea") ? 16 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r0.compareTo(field_r4.control, "password") ? 17 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r0.compareTo(field_r4.control, "upload") ? 18 : -1);
  }
}
function PoDynamicFormFieldsComponent_ng_template_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵrepeaterCreate(0, PoDynamicFormFieldsComponent_ng_template_1_For_1_Template, 19, 19, null, null, ɵɵcomponentInstance().trackBy, true);
  }
  if (rf & 2) {
    const fields_r23 = ctx.$implicit;
    ɵɵrepeater(fields_r23);
  }
}
function PoDynamicFormFieldsComponent_ng_template_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-dynamic-container", 43);
  }
  if (rf & 2) {
    const fields_r24 = ctx.$implicit;
    ɵɵnextContext();
    const poContent_r2 = ɵɵreference(2);
    ɵɵproperty("p-fields", fields_r24)("p-content", poContent_r2);
  }
}
var _c145 = ["fieldsComponent"];
var _c146 = ["dynamicForm"];
function PoDynamicFormComponent_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-dynamic-form-fields", 2, 0);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("p-auto-focus", ctx_r0.autoFocus)("p-components-size", ctx_r0.componentsSize)("p-fields", ctx_r0.fields)("p-value", ctx_r0.value);
  }
}
function PoDynamicFormComponent_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r2 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "form", null, 1)(2, "po-dynamic-form-fields", 3, 0);
    ɵɵtwoWayListener("p-fieldsChange", function PoDynamicFormComponent_Conditional_1_Template_po_dynamic_form_fields_p_fieldsChange_2_listener($event) {
      ɵɵrestoreView(_r2);
      const ctx_r0 = ɵɵnextContext();
      ɵɵtwoWayBindingSet(ctx_r0.fields, $event) || (ctx_r0.fields = $event);
      return ɵɵresetView($event);
    });
    ɵɵlistener("p-object-value", function PoDynamicFormComponent_Conditional_1_Template_po_dynamic_form_fields_p_object_value_2_listener($event) {
      ɵɵrestoreView(_r2);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.sendObjectValue($event));
    })("p-form-validate", function PoDynamicFormComponent_Conditional_1_Template_po_dynamic_form_fields_p_form_validate_2_listener($event) {
      ɵɵrestoreView(_r2);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.validateForm($event));
    });
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance(2);
    ɵɵtwoWayProperty("p-fields", ctx_r0.fields);
    ɵɵproperty("p-auto-focus", ctx_r0.autoFocus)("p-disabled-form", ctx_r0.disabledForm)("p-components-size", ctx_r0.componentsSize)("p-validate", ctx_r0.validate)("p-validate-fields", ctx_r0.validateFields)("p-validate-on-input", ctx_r0.validateOnInput)("p-value", ctx_r0.value);
  }
}
function PoImageComponent_Conditional_0_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "img", 1);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵproperty("priority", ctx_r0.priority)("loading", ctx_r0.loading)("fill", true)("ngSrc", ctx_r0.source);
    ɵɵattribute("alt", ctx_r0.alternate);
  }
}
function PoImageComponent_Conditional_0_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "img", 2);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵproperty("loading", ctx_r0.loading)("src", ctx_r0.source, ɵɵsanitizeUrl);
    ɵɵattribute("alt", ctx_r0.alternate);
  }
}
function PoImageComponent_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "po-container", 0);
    ɵɵconditionalCreate(1, PoImageComponent_Conditional_0_Conditional_1_Template, 1, 5, "img", 1)(2, PoImageComponent_Conditional_0_Conditional_2_Template, 1, 3, "img", 2);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("p-height", ctx_r0.height);
    ɵɵadvance();
    ɵɵconditional(!ctx_r0.isBase64 ? 1 : 2);
  }
}
function PoDynamicViewComponent_Conditional_0_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoDynamicViewComponent_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 6);
    ɵɵtemplate(1, PoDynamicViewComponent_Conditional_0_ng_container_1_Template, 1, 0, "ng-container", 7);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    const poContent_r2 = ɵɵreference(2);
    const poContentContainer_r3 = ɵɵreference(4);
    ɵɵclassProp("po-dynamic", ctx_r0.hasContainers);
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", ctx_r0.hasContainers ? poContentContainer_r3 : poContent_r2)("ngTemplateOutletContext", ɵɵpureFunction1(4, _c21, ctx_r0.hasContainers ? ctx_r0.containerFields : ctx_r0.visibleFields));
  }
}
function PoDynamicViewComponent_ng_template_1_For_1_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-divider", 8);
  }
  if (rf & 2) {
    const field_r4 = ɵɵnextContext().$implicit;
    ɵɵproperty("p-label", field_r4.divider);
  }
}
function PoDynamicViewComponent_ng_template_1_For_1_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoDynamicViewComponent_ng_template_1_For_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵconditionalCreate(0, PoDynamicViewComponent_ng_template_1_For_1_Conditional_0_Template, 1, 1, "po-divider", 8);
    ɵɵtemplate(1, PoDynamicViewComponent_ng_template_1_For_1_ng_container_1_Template, 1, 0, "ng-container", 7);
  }
  if (rf & 2) {
    const field_r4 = ctx.$implicit;
    ɵɵnextContext(2);
    const poInfo_r5 = ɵɵreference(6);
    const poTag_r6 = ɵɵreference(8);
    const poImage_r7 = ɵɵreference(10);
    ɵɵconditional((field_r4 == null ? null : field_r4.divider == null ? null : field_r4.divider.trim()) ? 0 : -1);
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", !field_r4.tag && !field_r4.image ? poInfo_r5 : !field_r4.image ? poTag_r6 : poImage_r7)("ngTemplateOutletContext", ɵɵpureFunction1(3, _c21, field_r4));
  }
}
function PoDynamicViewComponent_ng_template_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵrepeaterCreate(0, PoDynamicViewComponent_ng_template_1_For_1_Template, 2, 5, null, null, ɵɵrepeaterTrackByIdentity);
  }
  if (rf & 2) {
    const fields_r8 = ctx.$implicit;
    ɵɵrepeater(fields_r8);
  }
}
function PoDynamicViewComponent_ng_template_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-dynamic-container", 9);
  }
  if (rf & 2) {
    const fields_r9 = ctx.$implicit;
    ɵɵnextContext();
    const poContent_r2 = ɵɵreference(2);
    ɵɵproperty("p-fields", fields_r9)("p-content", poContent_r2);
  }
}
function PoDynamicViewComponent_ng_template_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-info", 10);
  }
  if (rf & 2) {
    const field_r10 = ctx.$implicit;
    const ctx_r0 = ɵɵnextContext();
    ɵɵclassProp("po-info-value-pre", ctx_r0.textWrap && ctx_r0.containsLineBreak(ctx_r0.setFieldValue(field_r10)));
    ɵɵproperty("ngClass", field_r10.cssClass)("p-label", field_r10.label)("p-size", ctx_r0.componentsSize)("p-value", ctx_r0.setFieldValue(field_r10));
  }
}
function PoDynamicViewComponent_ng_template_7_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-tag", 11);
  }
  if (rf & 2) {
    const field_r11 = ctx.$implicit;
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("ngClass", field_r11.cssClass)("p-color", field_r11.color)("p-icon", field_r11.icon)("p-label", field_r11.label)("p-text-color", field_r11.textColor)("p-value", ctx_r0.setFieldValue(field_r11));
  }
}
function PoDynamicViewComponent_ng_template_9_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-image", 12);
  }
  if (rf & 2) {
    const field_r12 = ctx.$implicit;
    ɵɵproperty("p-alt", field_r12.alt)("p-height", field_r12.height)("p-src", field_r12.value);
  }
}
function PoFilterChipComponent_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-icon", 1);
  }
}
function PoGridCellComponent_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "input", 4, 0);
    ɵɵtwoWayListener("ngModelChange", function PoGridCellComponent_Conditional_1_Template_input_ngModelChange_0_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      ɵɵtwoWayBindingSet(ctx_r1.editValue, $event) || (ctx_r1.editValue = $event);
      return ɵɵresetView($event);
    });
    ɵɵlistener("keydown.arrowup", function PoGridCellComponent_Conditional_1_Template_input_keydown_arrowup_0_listener($event) {
      return $event.stopPropagation();
    })("keydown.arrowdown", function PoGridCellComponent_Conditional_1_Template_input_keydown_arrowdown_0_listener($event) {
      return $event.stopPropagation();
    })("keydown.arrowleft", function PoGridCellComponent_Conditional_1_Template_input_keydown_arrowleft_0_listener($event) {
      return $event.stopPropagation();
    })("keydown.arrowright", function PoGridCellComponent_Conditional_1_Template_input_keydown_arrowright_0_listener($event) {
      return $event.stopPropagation();
    })("keydown", function PoGridCellComponent_Conditional_1_Template_input_keydown_0_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onKeyDownInput($event));
    })("blur", function PoGridCellComponent_Conditional_1_Template_input_blur_0_listener() {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onBlurInput());
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("id", ctx_r1.position);
    ɵɵtwoWayProperty("ngModel", ctx_r1.editValue);
  }
}
function PoGridCellComponent_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    const _r3 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 5, 1);
    ɵɵlistener("keydown", function PoGridCellComponent_Conditional_2_Template_div_keydown_0_listener($event) {
      ɵɵrestoreView(_r3);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onKeyDownContent($event));
    })("dblclick", function PoGridCellComponent_Conditional_2_Template_div_dblclick_0_listener($event) {
      ɵɵrestoreView(_r3);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.dblclick($event));
    });
    ɵɵelementStart(2, "span");
    ɵɵtext(3);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵclassMap(ɵɵinterpolate2("po-grid-cell-content po-grid-cell-", ctx_r1.align, " ", !ctx_r1.value && ctx_r1.required ? "po-grid-cell-required" : ""));
    ɵɵproperty("tabindex", ɵɵinterpolate(ctx_r1.readonly ? -1 : 0))("id", ctx_r1.position);
    ɵɵadvance(3);
    ɵɵtextInterpolate(ctx_r1.value || " ");
  }
}
var _c147 = ["table"];
var _c148 = ["wrapper"];
function PoGridComponent_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div");
    ɵɵtext(1);
    ɵɵelement(2, "br");
    ɵɵtext(3);
    ɵɵelement(4, "br");
    ɵɵtext(5);
    ɵɵelement(6, "br")(7, "hr");
    ɵɵtext(8);
    ɵɵpipe(9, "json");
    ɵɵelement(10, "hr");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵtextInterpolate2(" lastCell: ", ctx_r0.lastCell, " | currencyCell: ", ctx_r0.currencyCell, " ");
    ɵɵadvance(2);
    ɵɵtextInterpolate2(" lastRow: ", ctx_r0.lastRow, " | currencyRow: ", ctx_r0.currencyRow, " ");
    ɵɵadvance(2);
    ɵɵtextInterpolate2(" lastColumn: ", ctx_r0.lastColumn, " | currencyColumn: ", ctx_r0.currencyColumn, " ");
    ɵɵadvance(3);
    ɵɵtextInterpolate1(" currencyObj: ", ɵɵpipeBind1(9, 7, ctx_r0.currencyObj), " ");
  }
}
function PoGridComponent_Conditional_7_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoGridComponent_Conditional_7_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 9);
    ɵɵtemplate(1, PoGridComponent_Conditional_7_ng_container_1_Template, 1, 0, "ng-container", 11);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    ɵɵnextContext();
    const divFreezeColumns_r2 = ɵɵreference(12);
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", divFreezeColumns_r2);
  }
}
function PoGridComponent_ng_container_9_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoGridComponent_Conditional_10_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoGridComponent_Conditional_10_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 12);
    ɵɵtemplate(1, PoGridComponent_Conditional_10_ng_container_1_Template, 1, 0, "ng-container", 11);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    ɵɵnextContext();
    const divActionColumns_r3 = ɵɵreference(16);
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", divActionColumns_r3);
  }
}
function PoGridComponent_ng_template_11_For_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-grid-head", 17);
  }
  if (rf & 2) {
    const column_r4 = ctx.$implicit;
    const ɵ$index_55_r5 = ctx.$index;
    ɵɵstyleProp("width", column_r4.cssWidth)("max-width", column_r4.cssWidth)("min-width", column_r4.cssWidth);
    ɵɵproperty("p-position", ɵɵinterpolate("0-" + ɵ$index_55_r5))("p-width", column_r4.width)("p-align", column_r4.align)("p-title", column_r4.label);
  }
}
function PoGridComponent_ng_template_11_For_7_For_2_Template(rf, ctx) {
  if (rf & 1) {
    const _r8 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-grid-cell", 20);
    ɵɵtwoWayListener("p-valueChange", function PoGridComponent_ng_template_11_For_7_For_2_Template_po_grid_cell_p_valueChange_0_listener($event) {
      const column_r9 = ɵɵrestoreView(_r8).$implicit;
      const row_r7 = ɵɵnextContext().$implicit;
      ɵɵtwoWayBindingSet(row_r7[column_r9.property], $event) || (row_r7[column_r9.property] = $event);
      return ɵɵresetView($event);
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const column_r9 = ctx.$implicit;
    const ɵ$index_63_r10 = ctx.$index;
    const ctx_r10 = ɵɵnextContext();
    const row_r7 = ctx_r10.$implicit;
    const ɵ$index_60_r12 = ctx_r10.$index;
    ɵɵstyleProp("width", column_r9.cssWidth)("max-width", column_r9.cssWidth)("min-width", column_r9.cssWidth);
    ɵɵproperty("p-position", ɵɵinterpolate(ɵ$index_60_r12 + 1 + "-" + ɵ$index_63_r10))("p-align", column_r9.align)("p-readonly", column_r9.readonly)("p-required", column_r9.required)("p-width", column_r9.width);
    ɵɵtwoWayProperty("p-value", row_r7[column_r9.property]);
  }
}
function PoGridComponent_ng_template_11_For_7_Template(rf, ctx) {
  if (rf & 1) {
    const _r6 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 18);
    ɵɵlistener("keydown.control.delete", function PoGridComponent_ng_template_11_For_7_Template_div_keydown_control_delete_0_listener($event) {
      const row_r7 = ɵɵrestoreView(_r6).$implicit;
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.removeRow($event, row_r7));
    })("keydown.esc", function PoGridComponent_ng_template_11_For_7_Template_div_keydown_esc_0_listener($event) {
      const row_r7 = ɵɵrestoreView(_r6).$implicit;
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.cancelRow($event, row_r7));
    });
    ɵɵrepeaterCreate(1, PoGridComponent_ng_template_11_For_7_For_2_Template, 1, 13, "po-grid-cell", 19, ɵɵrepeaterTrackByIdentity);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵadvance();
    ɵɵrepeater(ctx_r0.freezeColumns);
  }
}
function PoGridComponent_ng_template_11_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 13)(1, "div", 14)(2, "div", 15);
    ɵɵrepeaterCreate(3, PoGridComponent_ng_template_11_For_4_Template, 1, 11, "po-grid-head", 16, ɵɵrepeaterTrackByIdentity);
    ɵɵelementEnd()();
    ɵɵelementStart(5, "div", 7);
    ɵɵrepeaterCreate(6, PoGridComponent_ng_template_11_For_7_Template, 3, 0, "div", 15, ɵɵrepeaterTrackByIdentity);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance(3);
    ɵɵrepeater(ctx_r0.freezeColumns);
    ɵɵadvance(3);
    ɵɵrepeater(ctx_r0.data);
  }
}
function PoGridComponent_ng_template_13_For_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-grid-head", 23);
  }
  if (rf & 2) {
    const column_r13 = ctx.$implicit;
    const ɵ$index_74_r14 = ctx.$index;
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵstyleProp("width", column_r13.cssWidth)("max-width", column_r13.cssWidth)("min-width", column_r13.cssWidth);
    ɵɵproperty("p-position", ɵɵinterpolate("0-" + (ɵ$index_74_r14 + ctx_r0.freezeColumns.length)))("p-align", column_r13.align)("p-title", column_r13.label)("p-width", column_r13.width);
  }
}
function PoGridComponent_ng_template_13_For_7_For_2_Template(rf, ctx) {
  if (rf & 1) {
    const _r17 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-grid-cell", 25);
    ɵɵtwoWayListener("p-valueChange", function PoGridComponent_ng_template_13_For_7_For_2_Template_po_grid_cell_p_valueChange_0_listener($event) {
      const column_r18 = ɵɵrestoreView(_r17).$implicit;
      const row_r16 = ɵɵnextContext().$implicit;
      ɵɵtwoWayBindingSet(row_r16[column_r18.property], $event) || (row_r16[column_r18.property] = $event);
      return ɵɵresetView($event);
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const column_r18 = ctx.$implicit;
    const ɵ$index_82_r19 = ctx.$index;
    const ctx_r19 = ɵɵnextContext();
    const row_r16 = ctx_r19.$implicit;
    const ɵ$index_79_r21 = ctx_r19.$index;
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵstyleProp("width", column_r18.cssWidth)("max-width", column_r18.cssWidth)("min-width", column_r18.cssWidth);
    ɵɵproperty("p-position", ɵɵinterpolate(ɵ$index_79_r21 + 1 + "-" + (ɵ$index_82_r19 + ctx_r0.freezeColumns.length)))("p-align", column_r18.align)("p-readonly", column_r18.readonly)("p-required", column_r18.required);
    ɵɵtwoWayProperty("p-value", row_r16[column_r18.property]);
    ɵɵproperty("p-width", column_r18.width);
  }
}
function PoGridComponent_ng_template_13_For_7_Template(rf, ctx) {
  if (rf & 1) {
    const _r15 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 18);
    ɵɵlistener("keydown.control.delete", function PoGridComponent_ng_template_13_For_7_Template_div_keydown_control_delete_0_listener($event) {
      const row_r16 = ɵɵrestoreView(_r15).$implicit;
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.removeRow($event, row_r16));
    })("keydown.esc", function PoGridComponent_ng_template_13_For_7_Template_div_keydown_esc_0_listener($event) {
      const row_r16 = ɵɵrestoreView(_r15).$implicit;
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.cancelRow($event, row_r16));
    });
    ɵɵrepeaterCreate(1, PoGridComponent_ng_template_13_For_7_For_2_Template, 1, 13, "po-grid-cell", 24, ɵɵrepeaterTrackByIdentity);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵadvance();
    ɵɵrepeater(ctx_r0.columns);
  }
}
function PoGridComponent_ng_template_13_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 21)(1, "div", 14)(2, "div", 15);
    ɵɵrepeaterCreate(3, PoGridComponent_ng_template_13_For_4_Template, 1, 11, "po-grid-head", 22, ɵɵrepeaterTrackByIdentity);
    ɵɵelementEnd()();
    ɵɵelementStart(5, "div", 7);
    ɵɵrepeaterCreate(6, PoGridComponent_ng_template_13_For_7_Template, 3, 0, "div", 15, ɵɵrepeaterTrackByIdentity);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance(3);
    ɵɵrepeater(ctx_r0.columns);
    ɵɵadvance(3);
    ɵɵrepeater(ctx_r0.data);
  }
}
function PoGridComponent_ng_template_15_For_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-grid-head", 27);
  }
  if (rf & 2) {
    const column_r22 = ctx.$implicit;
    const ɵ$index_93_r23 = ctx.$index;
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵproperty("p-position", ɵɵinterpolate("0-" + (ɵ$index_93_r23 + ctx_r0.freezeColumns.length + ctx_r0.columns.length)))("p-title", column_r22.label)("p-align", column_r22.align);
  }
}
function PoGridComponent_ng_template_15_For_7_For_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-grid-cell-action", 28);
  }
  if (rf & 2) {
    const column_r26 = ctx.$implicit;
    const ɵ$index_101_r27 = ctx.$index;
    const ctx_r27 = ɵɵnextContext();
    const row_r25 = ctx_r27.$implicit;
    const ɵ$index_98_r29 = ctx_r27.$index;
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵproperty("p-position", ɵɵinterpolate(ɵ$index_98_r29 + 1 + "-" + (ɵ$index_101_r27 + ctx_r0.freezeColumns.length + ctx_r0.columns.length)))("p-value", row_r25[column_r26.property]);
  }
}
function PoGridComponent_ng_template_15_For_7_Template(rf, ctx) {
  if (rf & 1) {
    const _r24 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 18);
    ɵɵlistener("keydown.control.delete", function PoGridComponent_ng_template_15_For_7_Template_div_keydown_control_delete_0_listener($event) {
      const row_r25 = ɵɵrestoreView(_r24).$implicit;
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.removeRow($event, row_r25));
    })("keydown.esc", function PoGridComponent_ng_template_15_For_7_Template_div_keydown_esc_0_listener($event) {
      const row_r25 = ɵɵrestoreView(_r24).$implicit;
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.cancelRow($event, row_r25));
    });
    ɵɵrepeaterCreate(1, PoGridComponent_ng_template_15_For_7_For_2_Template, 1, 3, "po-grid-cell-action", 28, ɵɵrepeaterTrackByIdentity);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵadvance();
    ɵɵrepeater(ctx_r0.actionColumns);
  }
}
function PoGridComponent_ng_template_15_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 26)(1, "div", 14)(2, "div", 15);
    ɵɵrepeaterCreate(3, PoGridComponent_ng_template_15_For_4_Template, 1, 4, "po-grid-head", 27, ɵɵrepeaterTrackByIdentity);
    ɵɵelementEnd()();
    ɵɵelementStart(5, "div", 7);
    ɵɵrepeaterCreate(6, PoGridComponent_ng_template_15_For_7_Template, 3, 0, "div", 15, ɵɵrepeaterTrackByIdentity);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance(3);
    ɵɵrepeater(ctx_r0.actionColumns);
    ɵɵadvance(3);
    ɵɵrepeater(ctx_r0.data);
  }
}
var _c149 = (a0, a1) => ({
  $implicit: a0,
  index: a1
});
function PoListViewComponent_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 2)(1, "div", 7)(2, "div", 8)(3, "po-checkbox", 9);
    ɵɵtwoWayListener("ngModelChange", function PoListViewComponent_Conditional_1_Template_po_checkbox_ngModelChange_3_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      ɵɵtwoWayBindingSet(ctx_r1.selectAll, $event) || (ctx_r1.selectAll = $event);
      return ɵɵresetView($event);
    });
    ɵɵlistener("p-change", function PoListViewComponent_Conditional_1_Template_po_checkbox_p_change_3_listener() {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.selectAllListItems());
    })("click", function PoListViewComponent_Conditional_1_Template_po_checkbox_click_3_listener() {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.selectAllListItems());
    });
    ɵɵelementEnd()()()();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance(3);
    ɵɵtwoWayProperty("ngModel", ctx_r1.selectAll);
    ɵɵproperty("p-label", ctx_r1.literals.selectAll)("p-size", ctx_r1.componentsSize);
  }
}
function PoListViewComponent_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "po-container", 4)(1, "div", 10)(2, "span");
    ɵɵtext(3);
    ɵɵelementEnd()()();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("p-no-padding", true);
    ɵɵadvance(3);
    ɵɵtextInterpolate1(" ", ctx_r1.literals.noData, " ");
  }
}
function PoListViewComponent_For_5_Conditional_5_Template(rf, ctx) {
  if (rf & 1) {
    const _r3 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 14)(1, "po-checkbox", 23);
    ɵɵtwoWayListener("ngModelChange", function PoListViewComponent_For_5_Conditional_5_Template_po_checkbox_ngModelChange_1_listener($event) {
      ɵɵrestoreView(_r3);
      const item_r4 = ɵɵnextContext().$implicit;
      ɵɵtwoWayBindingSet(item_r4.$selected, $event) || (item_r4.$selected = $event);
      return ɵɵresetView($event);
    });
    ɵɵlistener("p-change", function PoListViewComponent_For_5_Conditional_5_Template_po_checkbox_p_change_1_listener() {
      ɵɵrestoreView(_r3);
      const item_r4 = ɵɵnextContext().$implicit;
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.selectListItem(item_r4));
    })("click", function PoListViewComponent_For_5_Conditional_5_Template_po_checkbox_click_1_listener() {
      ɵɵrestoreView(_r3);
      const item_r4 = ɵɵnextContext().$implicit;
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.selectListItem(item_r4));
    });
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const item_r4 = ɵɵnextContext().$implicit;
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵtwoWayProperty("ngModel", item_r4.$selected);
    ɵɵproperty("p-size", ctx_r1.componentsSize);
  }
}
function PoListViewComponent_For_5_a_6_Template(rf, ctx) {
  if (rf & 1) {
    const _r5 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "a", 24);
    ɵɵlistener("click", function PoListViewComponent_For_5_a_6_Template_a_click_0_listener() {
      ɵɵrestoreView(_r5);
      const item_r4 = ɵɵnextContext().$implicit;
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.runTitleAction(item_r4));
    });
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const item_r4 = ɵɵnextContext().$implicit;
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("href", item_r4[ctx_r1.propertyLink], ɵɵsanitizeUrl);
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ctx_r1.getItemTitle(item_r4), " ");
  }
}
function PoListViewComponent_For_5_a_7_Template(rf, ctx) {
  if (rf & 1) {
    const _r6 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "a", 25);
    ɵɵlistener("click", function PoListViewComponent_For_5_a_7_Template_a_click_0_listener() {
      ɵɵrestoreView(_r6);
      const item_r4 = ɵɵnextContext().$implicit;
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.runTitleAction(item_r4));
    });
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const item_r4 = ɵɵnextContext().$implicit;
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("routerLink", item_r4[ctx_r1.propertyLink]);
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ctx_r1.getItemTitle(item_r4), " ");
  }
}
function PoListViewComponent_For_5_span_8_Template(rf, ctx) {
  if (rf & 1) {
    const _r7 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "span", 26);
    ɵɵlistener("click", function PoListViewComponent_For_5_span_8_Template_span_click_0_listener() {
      ɵɵrestoreView(_r7);
      const item_r4 = ɵɵnextContext().$implicit;
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.titleHasAction && ctx_r1.runTitleAction(item_r4));
    });
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const item_r4 = ɵɵnextContext().$implicit;
    const ctx_r1 = ɵɵnextContext();
    ɵɵclassProp("po-list-view-title-link", ctx_r1.titleHasAction);
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ctx_r1.getItemTitle(item_r4), " ");
  }
}
function PoListViewComponent_For_5_Conditional_9_For_2_Template(rf, ctx) {
  if (rf & 1) {
    const _r8 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-button", 28);
    ɵɵlistener("p-click", function PoListViewComponent_For_5_Conditional_9_For_2_Template_po_button_p_click_0_listener() {
      const action_r9 = ɵɵrestoreView(_r8).$implicit;
      const item_r4 = ɵɵnextContext(2).$implicit;
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onClickAction(action_r9, item_r4));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const action_r9 = ctx.$implicit;
    const item_r4 = ɵɵnextContext(2).$implicit;
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("p-disabled", ctx_r1.returnBooleanValue(action_r9, item_r4, "disabled"))("p-icon", action_r9.icon)("p-label", action_r9.label)("p-danger", action_r9.type === "danger")("p-size", ctx_r1.componentsSize);
  }
}
function PoListViewComponent_For_5_Conditional_9_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 18);
    ɵɵrepeaterCreate(1, PoListViewComponent_For_5_Conditional_9_For_2_Template, 1, 5, "po-button", 27, ɵɵrepeaterTrackByIdentity);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    ɵɵnextContext();
    const visibleActionsForItem_r10 = ɵɵreadContextLet(0);
    ɵɵadvance();
    ɵɵrepeater(visibleActionsForItem_r10);
  }
}
function PoListViewComponent_For_5_Conditional_10_Template(rf, ctx) {
  if (rf & 1) {
    const _r11 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 19)(1, "div", 29, 1);
    ɵɵlistener("click", function PoListViewComponent_For_5_Conditional_10_Template_div_click_1_listener() {
      ɵɵrestoreView(_r11);
      const popupTarget_r12 = ɵɵreference(2);
      const item_r4 = ɵɵnextContext().$implicit;
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.togglePopup(item_r4, popupTarget_r12));
    });
    ɵɵelement(3, "po-icon", 30);
    ɵɵelementEnd()();
  }
}
function PoListViewComponent_For_5_Conditional_11_ng_template_1_Template(rf, ctx) {
}
function PoListViewComponent_For_5_Conditional_11_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 20);
    ɵɵtemplate(1, PoListViewComponent_For_5_Conditional_11_ng_template_1_Template, 0, 0, "ng-template", 31);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r12 = ɵɵnextContext();
    const item_r4 = ctx_r12.$implicit;
    const ɵ$index_22_r14 = ctx_r12.$index;
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", ctx_r1.listViewContentTemplate.templateRef)("ngTemplateOutletContext", ɵɵpureFunction2(2, _c149, item_r4, ɵ$index_22_r14));
  }
}
function PoListViewComponent_For_5_Conditional_12_ng_template_1_Template(rf, ctx) {
}
function PoListViewComponent_For_5_Conditional_12_Template(rf, ctx) {
  if (rf & 1) {
    const _r15 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 21);
    ɵɵlistener("@showHideDetail.start", function PoListViewComponent_For_5_Conditional_12_Template_div_animation_showHideDetail_start_0_listener($event) {
      ɵɵrestoreView(_r15);
      const item_r4 = ɵɵnextContext().$implicit;
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(item_r4.$showDetail ? ctx_r1.onAnimationEvent($event, item_r4) : "undefined");
    });
    ɵɵtemplate(1, PoListViewComponent_For_5_Conditional_12_ng_template_1_Template, 0, 0, "ng-template", 31);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r12 = ɵɵnextContext();
    const item_r4 = ctx_r12.$implicit;
    const ɵ$index_22_r14 = ctx_r12.$index;
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("@showHideDetail", void 0);
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", ctx_r1.listViewDetailTemplate.templateRef)("ngTemplateOutletContext", ɵɵpureFunction2(3, _c149, item_r4, ɵ$index_22_r14));
  }
}
function PoListViewComponent_For_5_Conditional_13_Template(rf, ctx) {
  if (rf & 1) {
    const _r16 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 22)(1, "po-button", 32);
    ɵɵlistener("p-click", function PoListViewComponent_For_5_Conditional_13_Template_po_button_p_click_1_listener() {
      ɵɵrestoreView(_r16);
      const item_r4 = ɵɵnextContext().$implicit;
      return ɵɵresetView(item_r4.$showDetail = !item_r4.$showDetail);
    });
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const item_r4 = ɵɵnextContext().$implicit;
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵproperty("p-label", item_r4.$showDetail ? ctx_r1.literals.hideDetails : ctx_r1.literals.showDetails)("p-size", ctx_r1.componentsSize);
  }
}
function PoListViewComponent_For_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵdeclareLet(0);
    ɵɵelementStart(1, "po-container", 4)(2, "div", 11)(3, "div", 12)(4, "div", 13);
    ɵɵconditionalCreate(5, PoListViewComponent_For_5_Conditional_5_Template, 2, 2, "div", 14);
    ɵɵtemplate(6, PoListViewComponent_For_5_a_6_Template, 2, 2, "a", 15)(7, PoListViewComponent_For_5_a_7_Template, 2, 2, "a", 16)(8, PoListViewComponent_For_5_span_8_Template, 2, 3, "span", 17);
    ɵɵelementEnd();
    ɵɵconditionalCreate(9, PoListViewComponent_For_5_Conditional_9_Template, 3, 0, "div", 18);
    ɵɵconditionalCreate(10, PoListViewComponent_For_5_Conditional_10_Template, 4, 0, "div", 19);
    ɵɵelementEnd();
    ɵɵconditionalCreate(11, PoListViewComponent_For_5_Conditional_11_Template, 2, 5, "div", 20);
    ɵɵconditionalCreate(12, PoListViewComponent_For_5_Conditional_12_Template, 2, 6, "div", 21);
    ɵɵelementEnd();
    ɵɵconditionalCreate(13, PoListViewComponent_For_5_Conditional_13_Template, 2, 2, "div", 22);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const item_r4 = ctx.$implicit;
    const ctx_r1 = ɵɵnextContext();
    const visibleActionsForItem_r17 = ɵɵstoreLet(ctx_r1.getVisibleActions(item_r4));
    ɵɵadvance();
    ɵɵproperty("p-no-padding", true);
    ɵɵadvance(3);
    ɵɵproperty("ngSwitch", ctx_r1.checkTitleType(item_r4));
    ɵɵadvance();
    ɵɵconditional(ctx_r1.select ? 5 : -1);
    ɵɵadvance();
    ɵɵproperty("ngSwitchCase", "externalLink");
    ɵɵadvance();
    ɵɵproperty("ngSwitchCase", "internalLink");
    ɵɵadvance();
    ɵɵproperty("ngSwitchCase", "noLink");
    ɵɵadvance();
    ɵɵconditional(visibleActionsForItem_r17.length > 0 && visibleActionsForItem_r17.length <= 2 ? 9 : -1);
    ɵɵadvance();
    ɵɵconditional(visibleActionsForItem_r17.length > 2 ? 10 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r1.hasContentTemplate ? 11 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r1.hasDetailTemplate && item_r4.$showDetail ? 12 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r1.hasDetailTemplate ? 13 : -1);
  }
}
function PoListViewComponent_Conditional_6_Template(rf, ctx) {
  if (rf & 1) {
    const _r18 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 5)(1, "po-button", 33);
    ɵɵlistener("p-click", function PoListViewComponent_Conditional_6_Template_po_button_p_click_1_listener() {
      ɵɵrestoreView(_r18);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onShowMore());
    });
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵproperty("p-disabled", ctx_r1.showMoreDisabled)("p-label", ctx_r1.literals.loadMoreData)("p-size", ctx_r1.componentsSize);
  }
}
function PoLogoComponent_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "a", 0);
    ɵɵelement(1, "img", 2);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("href", ctx_r0.link === true ? "./" : ctx_r0.link, ɵɵsanitizeUrl);
    ɵɵadvance();
    ɵɵclassMap(ctx_r0.className);
    ɵɵproperty("alt", ctx_r0.logoAlt)("src", ctx_r0.logo, ɵɵsanitizeUrl)("title", ctx_r0.logoAlt);
  }
}
function PoLogoComponent_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "img", 2);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵclassMap(ctx_r0.className);
    ɵɵproperty("alt", ctx_r0.logoAlt)("src", ctx_r0.logo, ɵɵsanitizeUrl)("title", ctx_r0.logoAlt);
  }
}
function PoMenuPanelItemComponent_Conditional_0_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoMenuPanelItemComponent_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "a", 1);
    ɵɵtemplate(1, PoMenuPanelItemComponent_Conditional_0_ng_container_1_Template, 1, 0, "ng-container", 4);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    const menuItemTemplate_r2 = ɵɵreference(4);
    ɵɵproperty("routerLink", ctx_r0.menuItemInternal.link);
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", menuItemTemplate_r2);
  }
}
function PoMenuPanelItemComponent_Conditional_1_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoMenuPanelItemComponent_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "a", 2);
    ɵɵtemplate(1, PoMenuPanelItemComponent_Conditional_1_ng_container_1_Template, 1, 0, "ng-container", 4);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    const menuItemTemplate_r2 = ɵɵreference(4);
    ɵɵproperty("href", ctx_r0.menuItemInternal.link, ɵɵsanitizeUrl);
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", menuItemTemplate_r2);
  }
}
function PoMenuPanelItemComponent_Conditional_2_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoMenuPanelItemComponent_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "a", 3);
    ɵɵtemplate(1, PoMenuPanelItemComponent_Conditional_2_ng_container_1_Template, 1, 0, "ng-container", 4);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    ɵɵnextContext();
    const menuItemTemplate_r2 = ɵɵreference(4);
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", menuItemTemplate_r2);
  }
}
function PoMenuPanelItemComponent_ng_template_3_Template(rf, ctx) {
  if (rf & 1) {
    const _r3 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 5);
    ɵɵlistener("click", function PoMenuPanelItemComponent_ng_template_3_Template_div_click_0_listener($event) {
      ɵɵrestoreView(_r3);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.clickMenuItem($event));
    });
    ɵɵelement(1, "span");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵclassProp("po-menu-panel-item-selected", ctx_r0.menuItemInternal.isSelected);
    ɵɵproperty("p-tooltip", ctx_r0.menuItemInternal.label);
    ɵɵadvance();
    ɵɵclassMap(ctx_r0.menuItemInternal.icon);
  }
}
function PoMenuPanelComponent_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-logo", 2);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("p-logo", ctx_r0.logo)("p-logo-alt", ctx_r0.logoAlt);
  }
}
function PoMenuPanelComponent_For_6_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 5);
    ɵɵelement(1, "po-menu-panel-item", 6);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const menu_r2 = ctx.$implicit;
    const ɵ$index_12_r3 = ctx.$index;
    ɵɵadvance();
    ɵɵclassProp("po-menu-panel-item-first", ɵ$index_12_r3 === 0);
    ɵɵproperty("p-menu-item-internal", menu_r2);
  }
}
var _c150 = ["inputFilter"];
function PoMenuFilterComponent_Conditional_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-icon", 8);
  }
  if (rf & 2) {
    const ctx_r2 = ɵɵnextContext();
    ɵɵclassProp("po-field-icon-aa", ctx_r2.componentsSize === "small");
  }
}
function PoMenuFilterComponent_Conditional_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-loading-icon", 5);
  }
  if (rf & 2) {
    const ctx_r2 = ɵɵnextContext();
    ɵɵproperty("p-size", ctx_r2.componentsSize === "small" ? "xs" : "sm");
  }
}
var _c151 = ["menuSubItems"];
function PoMenuItemComponent_Conditional_0_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoMenuItemComponent_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "a", 7);
    ɵɵlistener("keydown.enter", function PoMenuItemComponent_Conditional_0_Template_a_keydown_enter_0_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.clickMenuItem($event));
    })("keydown.space", function PoMenuItemComponent_Conditional_0_Template_a_keydown_space_0_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.clickMenuItem($event));
    });
    ɵɵtemplate(1, PoMenuItemComponent_Conditional_0_ng_container_1_Template, 1, 0, "ng-container", 8);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    const menuItemTemplate_r3 = ɵɵreference(6);
    ɵɵproperty("routerLink", ctx_r1.link)("p-tooltip", ctx_r1.collapsedMenu && ctx_r1.shortLabel ? ctx_r1.shortLabel : void 0)("p-append-in-body", true);
    ɵɵattribute("aria-label", ctx_r1.label);
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", menuItemTemplate_r3);
  }
}
function PoMenuItemComponent_Conditional_1_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoMenuItemComponent_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r4 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "a", 9);
    ɵɵlistener("keydown.enter", function PoMenuItemComponent_Conditional_1_Template_a_keydown_enter_0_listener($event) {
      ɵɵrestoreView(_r4);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.clickMenuItem($event));
    })("keydown.space", function PoMenuItemComponent_Conditional_1_Template_a_keydown_space_0_listener($event) {
      ɵɵrestoreView(_r4);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.clickMenuItem($event));
    });
    ɵɵtemplate(1, PoMenuItemComponent_Conditional_1_ng_container_1_Template, 1, 0, "ng-container", 8);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    const menuItemTemplate_r3 = ɵɵreference(6);
    ɵɵproperty("href", ctx_r1.link, ɵɵsanitizeUrl)("p-tooltip", ctx_r1.collapsedMenu && ctx_r1.shortLabel ? ctx_r1.shortLabel : void 0)("p-append-in-body", true);
    ɵɵattribute("aria-label", ctx_r1.label);
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", menuItemTemplate_r3);
  }
}
function PoMenuItemComponent_Conditional_2_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoMenuItemComponent_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    const _r5 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "a", 10);
    ɵɵlistener("keydown.enter", function PoMenuItemComponent_Conditional_2_Template_a_keydown_enter_0_listener($event) {
      ɵɵrestoreView(_r5);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.clickMenuItem($event));
    })("keydown.space", function PoMenuItemComponent_Conditional_2_Template_a_keydown_space_0_listener($event) {
      ɵɵrestoreView(_r5);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.clickMenuItem($event));
    });
    ɵɵtemplate(1, PoMenuItemComponent_Conditional_2_ng_container_1_Template, 1, 0, "ng-container", 8);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    const menuItemTemplate_r3 = ɵɵreference(6);
    ɵɵproperty("p-tooltip", ctx_r1.collapsedMenu && ctx_r1.shortLabel ? ctx_r1.shortLabel : void 0)("p-append-in-body", true);
    ɵɵattribute("aria-label", ctx_r1.label);
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", menuItemTemplate_r3);
  }
}
function PoMenuItemComponent_Conditional_3_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoMenuItemComponent_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 5);
    ɵɵtemplate(1, PoMenuItemComponent_Conditional_3_ng_container_1_Template, 1, 0, "ng-container", 8);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    ɵɵnextContext();
    const menuItemTemplate_r3 = ɵɵreference(6);
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", menuItemTemplate_r3);
  }
}
function PoMenuItemComponent_Conditional_4_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoMenuItemComponent_Conditional_4_For_6_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "li", 13);
    ɵɵelement(1, "po-menu-item", 14);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const subItem_r6 = ctx.$implicit;
    ɵɵattribute("aria-level", subItem_r6.level);
    ɵɵadvance();
    ɵɵproperty("p-action", subItem_r6.action)("p-badge-alert", subItem_r6.badgeAlert)("p-badge-color", subItem_r6.badge ? subItem_r6.badge.color : void 0)("p-badge-value", subItem_r6.badge ? subItem_r6.badge.value : void 0)("p-id", subItem_r6.id)("p-label", subItem_r6.label)("p-level", subItem_r6.level)("p-link", subItem_r6.link)("p-sub-items", subItem_r6.subItems)("p-type", subItem_r6.type);
  }
}
function PoMenuItemComponent_Conditional_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 6);
    ɵɵtemplate(1, PoMenuItemComponent_Conditional_4_ng_container_1_Template, 1, 0, "ng-container", 8);
    ɵɵelementStart(2, "div", 11, 1)(4, "ul", 12);
    ɵɵrepeaterCreate(5, PoMenuItemComponent_Conditional_4_For_6_Template, 2, 11, "li", 13, ɵɵrepeaterTrackByIndex);
    ɵɵelementEnd()()();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    const menuItemTemplate_r3 = ɵɵreference(6);
    ɵɵattribute("aria-expanded", ctx_r1.isOpened ? true : false);
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", menuItemTemplate_r3);
    ɵɵadvance();
    ɵɵstyleProp("max-height", ctx_r1.maxHeight, "px");
    ɵɵproperty("hidden", ctx_r1.collapsedMenu || !ctx_r1.isOpened);
    ɵɵattribute("aria-expanded", ctx_r1.isOpened ? true : false)("aria-hidden", !ctx_r1.isOpened ? true : false);
    ɵɵadvance(3);
    ɵɵrepeater(ctx_r1.subItems);
  }
}
function PoMenuItemComponent_ng_template_5_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-icon", 16);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵproperty("p-icon", ctx_r1.icon);
  }
}
function PoMenuItemComponent_ng_template_5_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-icon", 17);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵproperty("p-icon", ctx_r1.isOpened ? "ICON_ARROW_UP po-field-icon" : "ICON_ARROW_DOWN po-field-icon");
  }
}
function PoMenuItemComponent_ng_template_5_Conditional_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-badge", 18);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵproperty("ngClass", !ctx_r1.collapsedMenu ? "po-menu-badge-align" : "po-menu-badge-align-collapsed")("p-color", ctx_r1.badgeColor)("p-value", ctx_r1.badgeValue);
  }
}
function PoMenuItemComponent_ng_template_5_Conditional_6_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "span", 19);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵproperty("ngClass", !ctx_r1.collapsedMenu ? "po-menu-badge-alert" : "po-menu-badge-alert-collapsed");
  }
}
function PoMenuItemComponent_ng_template_5_Template(rf, ctx) {
  if (rf & 1) {
    const _r7 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 15);
    ɵɵlistener("click", function PoMenuItemComponent_ng_template_5_Template_div_click_0_listener($event) {
      ɵɵrestoreView(_r7);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.clickMenuItem($event));
    })("keydown.enter", function PoMenuItemComponent_ng_template_5_Template_div_keydown_enter_0_listener($event) {
      ɵɵrestoreView(_r7);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.clickMenuItem($event));
    })("keydown.space", function PoMenuItemComponent_ng_template_5_Template_div_keydown_space_0_listener($event) {
      ɵɵrestoreView(_r7);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.clickMenuItem($event));
    });
    ɵɵconditionalCreate(1, PoMenuItemComponent_ng_template_5_Conditional_1_Template, 1, 1, "po-icon", 16);
    ɵɵconditionalCreate(2, PoMenuItemComponent_ng_template_5_Conditional_2_Template, 1, 1, "po-icon", 17);
    ɵɵelementStart(3, "span");
    ɵɵtext(4);
    ɵɵelementEnd();
    ɵɵconditionalCreate(5, PoMenuItemComponent_ng_template_5_Conditional_5_Template, 1, 3, "po-badge", 18);
    ɵɵconditionalCreate(6, PoMenuItemComponent_ng_template_5_Conditional_6_Template, 1, 1, "span", 19);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵclassProp("po-menu-icon-container", ctx_r1.level === 1 && ctx_r1.icon)("po-menu-item-selected", ctx_r1.isSelected)("po-menu-item-level-two", ctx_r1.level === 2)("po-menu-item-level-three", ctx_r1.level === 3)("po-menu-item-level-four", ctx_r1.level === 4)("po-menu-item-grouper-up", ctx_r1.type === "subItems" && ctx_r1.isOpened)("po-menu-item-grouper-down", ctx_r1.type === "subItems" && !ctx_r1.isOpened)("po-menu-item-no-data", ctx_r1.type === "noData")("po-menu-sub-item-selected", ctx_r1.isSelectedSubItem);
    ɵɵproperty("p-tooltip", ctx_r1.type === "subItems" && ctx_r1.collapsedMenu && ctx_r1.shortLabel ? ctx_r1.shortLabel : void 0)("p-append-in-body", true)("tabindex", ctx_r1.type === "subItems" ? 0 : -1);
    ɵɵattribute("aria-label", ctx_r1.label)("role", ctx_r1.type === "subItems" ? "menuitem" : void 0);
    ɵɵadvance();
    ɵɵconditional(ctx_r1.level === 1 && ctx_r1.icon ? 1 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r1.type === "subItems" && !ctx_r1.collapsedMenu ? 2 : -1);
    ɵɵadvance();
    ɵɵclassProp("po-menu-icon-label", ctx_r1.level === 1 && ctx_r1.icon);
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ctx_r1.label, " ");
    ɵɵadvance();
    ɵɵconditional(ctx_r1.canShowBadge ? 5 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r1.badgeAlert ? 6 : -1);
  }
}
var _c152 = () => ({
  showFilter: true,
  showTemplate: true
});
var _c153 = (a0) => ({
  "po-menu-animation": a0
});
var _c154 = () => ({
  showFilter: false,
  showTemplate: false
});
function PoMenuComponent_Conditional_0_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoMenuComponent_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, PoMenuComponent_Conditional_0_ng_container_0_Template, 1, 0, "ng-container", 3);
  }
  if (rf & 2) {
    ɵɵnextContext();
    const menuItemsTemplate_r1 = ɵɵreference(7);
    ɵɵproperty("ngTemplateOutlet", menuItemsTemplate_r1)("ngTemplateOutletContext", ɵɵpureFunction0(2, _c152));
  }
}
function PoMenuComponent_Conditional_1_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    const _r3 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 17);
    ɵɵlistener("click", function PoMenuComponent_Conditional_1_Conditional_0_Template_div_click_0_listener() {
      ɵɵrestoreView(_r3);
      const ctx_r3 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r3.toggleMenuMobile());
    });
    ɵɵelementEnd();
  }
}
function PoMenuComponent_Conditional_1_Conditional_7_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 10);
    ɵɵelement(1, "po-logo", 18);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r3 = ɵɵnextContext(2);
    ɵɵadvance();
    ɵɵproperty("p-class", ctx_r3.enableCollapse ? "po-menu-short-logo" : "po-menu-logo")("p-logo", ctx_r3.enableCollapse ? ctx_r3.shortLogo || ctx_r3.logo : ctx_r3.logo)("p-logo-alt", ctx_r3.logoAlt)("p-link", ctx_r3.logoLink);
  }
}
function PoMenuComponent_Conditional_1_Conditional_8_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoMenuComponent_Conditional_1_Conditional_8_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 11);
    ɵɵtemplate(1, PoMenuComponent_Conditional_1_Conditional_8_ng_container_1_Template, 1, 0, "ng-container", 19);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    ɵɵnextContext(2);
    const menuCustomTemplate_r5 = ɵɵreference(3);
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", menuCustomTemplate_r5);
  }
}
function PoMenuComponent_Conditional_1_Conditional_9_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoMenuComponent_Conditional_1_Conditional_9_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, PoMenuComponent_Conditional_1_Conditional_9_ng_container_0_Template, 1, 0, "ng-container", 19);
  }
  if (rf & 2) {
    ɵɵnextContext(2);
    const menuFilterTemplate_r6 = ɵɵreference(5);
    ɵɵproperty("ngTemplateOutlet", menuFilterTemplate_r6);
  }
}
function PoMenuComponent_Conditional_1_ng_container_14_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoMenuComponent_Conditional_1_Conditional_15_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span", 21);
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r3 = ɵɵnextContext(3);
    ɵɵadvance();
    ɵɵtextInterpolate(ctx_r3.literals.close);
  }
}
function PoMenuComponent_Conditional_1_Conditional_15_Template(rf, ctx) {
  if (rf & 1) {
    const _r7 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 16)(1, "button", 20);
    ɵɵlistener("click", function PoMenuComponent_Conditional_1_Conditional_15_Template_button_click_1_listener() {
      ɵɵrestoreView(_r7);
      const ctx_r3 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r3.toggle());
    });
    ɵɵconditionalCreate(2, PoMenuComponent_Conditional_1_Conditional_15_Conditional_2_Template, 2, 1, "span", 21);
    ɵɵelement(3, "po-icon", 22);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r3 = ɵɵnextContext(2);
    ɵɵadvance();
    ɵɵattribute("aria-label", ctx_r3.enableCollapseButton ? ctx_r3.literals.close : ctx_r3.literals.open);
    ɵɵadvance();
    ɵɵconditional(ctx_r3.enableCollapseButton ? 2 : -1);
    ɵɵadvance();
    ɵɵproperty("p-icon", ctx_r3.enableCollapse ? "ICON_MENU_OPEN" : "ICON_MENU_CLOSE");
  }
}
function PoMenuComponent_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r2 = ɵɵgetCurrentView();
    ɵɵconditionalCreate(0, PoMenuComponent_Conditional_1_Conditional_0_Template, 1, 0, "div", 4);
    ɵɵelementStart(1, "div")(2, "div", 5);
    ɵɵlistener("click", function PoMenuComponent_Conditional_1_Template_div_click_2_listener() {
      ɵɵrestoreView(_r2);
      const ctx_r3 = ɵɵnextContext();
      return ɵɵresetView(ctx_r3.toggleMenuMobile());
    });
    ɵɵelement(3, "po-icon", 6);
    ɵɵelementEnd();
    ɵɵelementStart(4, "div", 7)(5, "div", 8);
    ɵɵlistener("mouseenter", function PoMenuComponent_Conditional_1_Template_div_mouseenter_5_listener() {
      ɵɵrestoreView(_r2);
      const ctx_r3 = ɵɵnextContext();
      return ɵɵresetView(ctx_r3.onMouseEnter());
    })("mouseleave", function PoMenuComponent_Conditional_1_Template_div_mouseleave_5_listener() {
      ɵɵrestoreView(_r2);
      const ctx_r3 = ɵɵnextContext();
      return ɵɵresetView(ctx_r3.onMouseLeave());
    });
    ɵɵelementStart(6, "div", 9);
    ɵɵconditionalCreate(7, PoMenuComponent_Conditional_1_Conditional_7_Template, 2, 4, "div", 10);
    ɵɵconditionalCreate(8, PoMenuComponent_Conditional_1_Conditional_8_Template, 2, 1, "div", 11);
    ɵɵconditionalCreate(9, PoMenuComponent_Conditional_1_Conditional_9_Template, 1, 1, "ng-container");
    ɵɵelementEnd();
    ɵɵelementStart(10, "div", 12)(11, "div", 13)(12, "div", 14)(13, "nav", 15);
    ɵɵtemplate(14, PoMenuComponent_Conditional_1_ng_container_14_Template, 1, 0, "ng-container", 3);
    ɵɵelementEnd()()()()();
    ɵɵconditionalCreate(15, PoMenuComponent_Conditional_1_Conditional_15_Template, 4, 3, "div", 16);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r3 = ɵɵnextContext();
    const menuItemsTemplate_r1 = ɵɵreference(7);
    ɵɵconditional(ctx_r3.mobileOpened ? 0 : -1);
    ɵɵadvance(4);
    ɵɵproperty("ngClass", ɵɵpureFunction1(9, _c153, ctx_r3.mobileOpened));
    ɵɵattribute("aria-expanded", !ctx_r3.collapsed);
    ɵɵadvance(3);
    ɵɵconditional(ctx_r3.logo || ctx_r3.shortLogo && ctx_r3.enableCollapse ? 7 : -1);
    ɵɵadvance();
    ɵɵconditional(!ctx_r3.enableCollapse && ctx_r3.menuHeaderTemplate ? 8 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r3.filter ? 9 : -1);
    ɵɵadvance(5);
    ɵɵproperty("ngTemplateOutlet", menuItemsTemplate_r1)("ngTemplateOutletContext", ɵɵpureFunction0(11, _c154));
    ɵɵadvance();
    ɵɵconditional(ctx_r3.hasFooter ? 15 : -1);
  }
}
function PoMenuComponent_ng_template_2_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoMenuComponent_ng_template_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, PoMenuComponent_ng_template_2_ng_container_0_Template, 1, 0, "ng-container", 19);
  }
  if (rf & 2) {
    const ctx_r3 = ɵɵnextContext();
    ɵɵproperty("ngTemplateOutlet", ctx_r3.menuHeaderTemplate.templateRef);
  }
}
function PoMenuComponent_ng_template_4_Template(rf, ctx) {
  if (rf & 1) {
    const _r8 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-menu-filter", 23);
    ɵɵlistener("p-filter", function PoMenuComponent_ng_template_4_Template_po_menu_filter_p_filter_0_listener($event) {
      ɵɵrestoreView(_r8);
      const ctx_r3 = ɵɵnextContext();
      return ɵɵresetView(ctx_r3.debounceFilter($event));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r3 = ɵɵnextContext();
    ɵɵproperty("p-enable-collapse", ctx_r3.enableCollapse)("p-loading", ctx_r3.filterLoading)("p-components-size", ctx_r3.componentsSize);
  }
}
function PoMenuComponent_ng_template_6_Conditional_0_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoMenuComponent_ng_template_6_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, PoMenuComponent_ng_template_6_Conditional_0_ng_container_0_Template, 1, 0, "ng-container", 19);
  }
  if (rf & 2) {
    ɵɵnextContext(2);
    const menuCustomTemplate_r5 = ɵɵreference(3);
    ɵɵproperty("ngTemplateOutlet", menuCustomTemplate_r5);
  }
}
function PoMenuComponent_ng_template_6_Conditional_1_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoMenuComponent_ng_template_6_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, PoMenuComponent_ng_template_6_Conditional_1_ng_container_0_Template, 1, 0, "ng-container", 19);
  }
  if (rf & 2) {
    ɵɵnextContext(2);
    const menuFilterTemplate_r6 = ɵɵreference(5);
    ɵɵproperty("ngTemplateOutlet", menuFilterTemplate_r6);
  }
}
function PoMenuComponent_ng_template_6_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "li", 25);
    ɵɵelement(1, "po-menu-item", 27);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r3 = ɵɵnextContext(2);
    ɵɵadvance();
    ɵɵproperty("p-level", 1)("p-label", ctx_r3.literals.itemNotFound)("p-short-label", ctx_r3.literals.itemNotFound);
  }
}
function PoMenuComponent_ng_template_6_For_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "li", 25);
    ɵɵelement(1, "po-menu-item", 28);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const menu_r9 = ctx.$implicit;
    const ɵ$index_91_r10 = ctx.$index;
    const ctx_r3 = ɵɵnextContext(2);
    ɵɵclassProp("po-menu-item-first", ɵ$index_91_r10 === 0);
    ɵɵattribute("aria-level", menu_r9.level);
    ɵɵadvance();
    ɵɵproperty("p-action", menu_r9.action)("p-badge-alert", menu_r9.badgeAlert)("p-badge-color", menu_r9.badge == null ? null : menu_r9.badge.color)("p-badge-value", menu_r9.badge == null ? null : menu_r9.badge.value)("p-collapsed-menu", ctx_r3.enableCollapse)("p-icon", ctx_r3.allowIcons ? menu_r9.icon : null)("p-id", menu_r9.id)("p-label", menu_r9.label)("p-level", menu_r9.level)("p-link", menu_r9.link)("p-short-label", menu_r9.shortLabel)("p-sub-items", menu_r9.subItems)("p-type", menu_r9.type);
  }
}
function PoMenuComponent_ng_template_6_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵconditionalCreate(0, PoMenuComponent_ng_template_6_Conditional_0_Template, 1, 1, "ng-container");
    ɵɵconditionalCreate(1, PoMenuComponent_ng_template_6_Conditional_1_Template, 1, 1, "ng-container");
    ɵɵelementStart(2, "ul", 24);
    ɵɵconditionalCreate(3, PoMenuComponent_ng_template_6_Conditional_3_Template, 2, 3, "li", 25);
    ɵɵrepeaterCreate(4, PoMenuComponent_ng_template_6_For_5_Template, 2, 16, "li", 26, ɵɵrepeaterTrackByIdentity);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const showFilter_r11 = ctx.showFilter;
    const showTemplate_r12 = ctx.showTemplate;
    const ctx_r3 = ɵɵnextContext();
    ɵɵconditional(ctx_r3.menuHeaderTemplate && showTemplate_r12 ? 0 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r3.filter && showFilter_r11 ? 1 : -1);
    ɵɵadvance(2);
    ɵɵconditional(ctx_r3.noData ? 3 : -1);
    ɵɵadvance();
    ɵɵrepeater(ctx_r3.filteredItems);
  }
}
function PoNavbarActionsComponent_For_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-navbar-action", 0);
  }
  if (rf & 2) {
    const iconAction_r1 = ctx.$implicit;
    ɵɵproperty("p-action", iconAction_r1.action)("p-icon", iconAction_r1.icon)("p-label", iconAction_r1.label)("p-link", iconAction_r1.link)("p-tooltip-text", iconAction_r1.tooltip);
  }
}
var _c155 = (a0, a1) => ({
  "po-navbar-item-navigation-icon-disabled": a0,
  "po-clickable": a1
});
function PoNavbarItemComponent_Case_0_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "a", 2);
    ɵɵlistener("click", function PoNavbarItemComponent_Case_0_Template_a_click_0_listener() {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.itemClick());
    });
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵclassProp("po-clickable", ctx_r1.clickable);
    ɵɵproperty("href", ctx_r1.link, ɵɵsanitizeUrl);
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ctx_r1.label, " ");
  }
}
function PoNavbarItemComponent_Case_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r3 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "a", 3);
    ɵɵlistener("click", function PoNavbarItemComponent_Case_1_Template_a_click_0_listener() {
      ɵɵrestoreView(_r3);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.itemClick(ctx_r1.label, ctx_r1.link));
    });
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵclassProp("po-clickable", ctx_r1.clickable);
    ɵɵproperty("routerLink", ctx_r1.link);
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ctx_r1.label, " ");
  }
}
var _c156 = ["navbarItemsContainer"];
function PoNavbarItemsComponent_For_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "li", 3);
    ɵɵelement(1, "po-navbar-item", 4);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const item_r1 = ctx.$implicit;
    const ctx_r1 = ɵɵnextContext();
    ɵɵclassProp("po-navbar-item-selected", ctx_r1.selectedItem === item_r1);
    ɵɵadvance();
    ɵɵproperty("p-action", item_r1.action)("p-clickable", ctx_r1.selectedItem !== item_r1)("p-label", item_r1.label)("p-link", item_r1.link);
  }
}
function PoNavbarLogoComponent_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-logo", 0);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("p-logo", ctx_r0.logo)("p-logo-alt", ctx_r0.logoAlt);
  }
}
var _c157 = (a0) => ({
  "po-navbar-shadow": a0
});
var _c158 = (a0, a1) => ({
  "po-navbar-logo-menu": a0,
  "po-navbar-no-logo": a1
});
function PoNavbarComponent_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-navbar-item-navigation", 6);
    ɵɵlistener("p-click", function PoNavbarComponent_Conditional_3_Template_po_navbar_item_navigation_p_click_0_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.navigateItems($event));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("p-disable-left", ctx_r1.navbarItemNavigationDisableLeft)("p-disable-right", ctx_r1.navbarItemNavigationDisableRight);
  }
}
function PoNavbarComponent_Conditional_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-menu", 5);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("p-menus", ctx_r1.items);
  }
}
var _c159 = [[["", "po-page-header-navigation", ""]], "*"];
var _c160 = ["[po-page-header-navigation]", "*"];
function PoPageHeaderComponent_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 1);
    ɵɵelement(1, "po-breadcrumb", 4);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵproperty("p-favorite-service", ctx_r0.breadcrumb.favorite)("p-items", ctx_r0.breadcrumb.items)("p-params-service", ctx_r0.breadcrumb.params)("p-size", ctx_r0.size);
  }
}
function PoPageHeaderComponent_Conditional_4_Conditional_1_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "h3", 6);
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(3);
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ctx_r0.title, " ");
  }
}
function PoPageHeaderComponent_Conditional_4_Conditional_1_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "h2", 6);
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(3);
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ctx_r0.title, " ");
  }
}
function PoPageHeaderComponent_Conditional_4_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵconditionalCreate(0, PoPageHeaderComponent_Conditional_4_Conditional_1_Conditional_0_Template, 2, 1, "h3", 6)(1, PoPageHeaderComponent_Conditional_4_Conditional_1_Conditional_1_Template, 2, 1, "h2", 6);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵconditional(ctx_r0.type === "secondary" || ctx_r0.type === "tertiary" ? 0 : 1);
  }
}
function PoPageHeaderComponent_Conditional_4_Conditional_2_For_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span");
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const fragment_r2 = ctx.$implicit;
    ɵɵclassProp("po-text-bold", fragment_r2.bold)("po-text-italic", fragment_r2.italic)("po-text-underline", fragment_r2.underline);
    ɵɵadvance();
    ɵɵtextInterpolate(fragment_r2.text);
  }
}
function PoPageHeaderComponent_Conditional_4_Conditional_2_Conditional_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-helper", 9);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(3);
    ɵɵproperty("p-custom-action", ctx_r0.customAction())("p-size", ctx_r0.size);
  }
}
function PoPageHeaderComponent_Conditional_4_Conditional_2_Conditional_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-helper", 10);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(3);
    ɵɵproperty("p-helper", ctx_r0.helper())("p-size", ctx_r0.size);
  }
}
function PoPageHeaderComponent_Conditional_4_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 5)(1, "span", 7);
    ɵɵrepeaterCreate(2, PoPageHeaderComponent_Conditional_4_Conditional_2_For_3_Template, 2, 7, "span", 8, ɵɵrepeaterTrackByIndex);
    ɵɵelementEnd();
    ɵɵconditionalCreate(4, PoPageHeaderComponent_Conditional_4_Conditional_2_Conditional_4_Template, 1, 2, "po-helper", 9)(5, PoPageHeaderComponent_Conditional_4_Conditional_2_Conditional_5_Template, 1, 2, "po-helper", 10);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵadvance(2);
    ɵɵrepeater(ctx_r0.subtitleFragments);
    ɵɵadvance(2);
    ɵɵconditional(ctx_r0.customAction() ? 4 : ctx_r0.helper() ? 5 : -1);
  }
}
function PoPageHeaderComponent_Conditional_4_Conditional_3_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 5);
    ɵɵelement(1, "po-helper", 9);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(3);
    ɵɵadvance();
    ɵɵproperty("p-custom-action", ctx_r0.customAction())("p-size", ctx_r0.size);
  }
}
function PoPageHeaderComponent_Conditional_4_Conditional_3_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 5);
    ɵɵelement(1, "po-helper", 10);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(3);
    ɵɵadvance();
    ɵɵproperty("p-helper", ctx_r0.helper())("p-size", ctx_r0.size);
  }
}
function PoPageHeaderComponent_Conditional_4_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵconditionalCreate(0, PoPageHeaderComponent_Conditional_4_Conditional_3_Conditional_0_Template, 2, 2, "div", 5)(1, PoPageHeaderComponent_Conditional_4_Conditional_3_Conditional_1_Template, 2, 2, "div", 5);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵconditional(ctx_r0.customAction() ? 0 : ctx_r0.helper() ? 1 : -1);
  }
}
function PoPageHeaderComponent_Conditional_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 3);
    ɵɵconditionalCreate(1, PoPageHeaderComponent_Conditional_4_Conditional_1_Template, 2, 1);
    ɵɵconditionalCreate(2, PoPageHeaderComponent_Conditional_4_Conditional_2_Template, 6, 1, "div", 5);
    ɵɵconditionalCreate(3, PoPageHeaderComponent_Conditional_4_Conditional_3_Template, 2, 1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵconditional(ctx_r0.title ? 1 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r0.title && ctx_r0.subtitle ? 2 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r0.title && !ctx_r0.subtitle ? 3 : -1);
  }
}
var _c161 = () => ({
  kindFallback: "secondary"
});
var _c162 = () => ({
  kindFallback: "primary"
});
function PoPageDefaultComponent_ng_template_1_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-dropdown", 3);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵproperty("p-label", ctx_r0.literals.otherActions)("p-actions", ctx_r0.visibleActions)("p-size", ctx_r0.componentsSize);
  }
}
function PoPageDefaultComponent_ng_template_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵconditionalCreate(0, PoPageDefaultComponent_ng_template_1_Conditional_0_Template, 1, 3, "po-dropdown", 3);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵconditional(ctx_r0.visibleActions.length ? 0 : -1);
  }
}
function PoPageDefaultComponent_ng_template_3_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-dropdown", 4);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵproperty("p-label", ctx_r0.literals.otherActions)("p-actions", ctx_r0.dropdownActions)("p-size", ctx_r0.componentsSize);
  }
}
function PoPageDefaultComponent_ng_template_3_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r2 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-button", 6);
    ɵɵlistener("p-click", function PoPageDefaultComponent_ng_template_3_Conditional_1_Template_po_button_p_click_0_listener() {
      ɵɵrestoreView(_r2);
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.callAction(ctx_r0.visibleActions[0]));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const kindFallback_r3 = ɵɵnextContext().kindFallback;
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("p-kind", ctx_r0.getActionKind(ctx_r0.visibleActions[0], kindFallback_r3))("p-danger", ctx_r0.visibleActions[0].type === "danger")("p-disabled", ctx_r0.actionIsDisabled(ctx_r0.visibleActions[0]))("p-icon", ctx_r0.visibleActions[0].icon)("p-label", ctx_r0.isMobile && ctx_r0.visibleActions[0].icon ? "" : ctx_r0.visibleActions[0].label)("p-aria-label", ctx_r0.visibleActions[0].label)("p-size", ctx_r0.componentsSize);
  }
}
function PoPageDefaultComponent_ng_template_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵconditionalCreate(0, PoPageDefaultComponent_ng_template_3_Conditional_0_Template, 1, 3, "po-dropdown", 4);
    ɵɵconditionalCreate(1, PoPageDefaultComponent_ng_template_3_Conditional_1_Template, 1, 7, "po-button", 5);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵconditional(ctx_r0.visibleActions.length > 1 ? 0 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r0.visibleActions[0] ? 1 : -1);
  }
}
function PoPageDefaultComponent_ng_template_5_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-dropdown", 4);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵproperty("p-label", ctx_r0.literals.otherActions)("p-actions", ctx_r0.dropdownActions)("p-size", ctx_r0.componentsSize);
  }
}
function PoPageDefaultComponent_ng_template_5_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r4 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-button", 9);
    ɵɵlistener("p-click", function PoPageDefaultComponent_ng_template_5_Conditional_1_Template_po_button_p_click_0_listener() {
      ɵɵrestoreView(_r4);
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.callAction(ctx_r0.visibleActions[2]));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵproperty("p-kind", ctx_r0.getActionKind(ctx_r0.visibleActions[2], "secondary"))("p-danger", ctx_r0.visibleActions[2].type === "danger")("p-disabled", ctx_r0.actionIsDisabled(ctx_r0.visibleActions[2]))("p-icon", ctx_r0.visibleActions[2].icon)("p-label", ctx_r0.visibleActions[2].label)("p-size", ctx_r0.componentsSize);
  }
}
function PoPageDefaultComponent_ng_template_5_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    const _r5 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-button", 10);
    ɵɵlistener("p-click", function PoPageDefaultComponent_ng_template_5_Conditional_2_Template_po_button_p_click_0_listener() {
      ɵɵrestoreView(_r5);
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.callAction(ctx_r0.visibleActions[1]));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵproperty("p-kind", ctx_r0.getActionKind(ctx_r0.visibleActions[1], "secondary"))("p-disabled", ctx_r0.actionIsDisabled(ctx_r0.visibleActions[1]))("p-icon", ctx_r0.visibleActions[1].icon)("p-label", ctx_r0.visibleActions[1].label)("p-danger", ctx_r0.visibleActions[1].type === "danger")("p-size", ctx_r0.componentsSize);
  }
}
function PoPageDefaultComponent_ng_template_5_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    const _r6 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-button", 6);
    ɵɵlistener("p-click", function PoPageDefaultComponent_ng_template_5_Conditional_3_Template_po_button_p_click_0_listener() {
      ɵɵrestoreView(_r6);
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.callAction(ctx_r0.visibleActions[0]));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵproperty("p-kind", ctx_r0.getActionKind(ctx_r0.visibleActions[0], "primary"))("p-danger", ctx_r0.visibleActions[0].type === "danger")("p-disabled", ctx_r0.actionIsDisabled(ctx_r0.visibleActions[0]))("p-icon", ctx_r0.visibleActions[0].icon)("p-label", ctx_r0.isMobile && ctx_r0.visibleActions[0].icon ? "" : ctx_r0.visibleActions[0].label)("p-aria-label", ctx_r0.visibleActions[0].label)("p-size", ctx_r0.componentsSize);
  }
}
function PoPageDefaultComponent_ng_template_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵconditionalCreate(0, PoPageDefaultComponent_ng_template_5_Conditional_0_Template, 1, 3, "po-dropdown", 4);
    ɵɵconditionalCreate(1, PoPageDefaultComponent_ng_template_5_Conditional_1_Template, 1, 6, "po-button", 7);
    ɵɵconditionalCreate(2, PoPageDefaultComponent_ng_template_5_Conditional_2_Template, 1, 6, "po-button", 8);
    ɵɵconditionalCreate(3, PoPageDefaultComponent_ng_template_5_Conditional_3_Template, 1, 7, "po-button", 5);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵconditional(ctx_r0.visibleActions.length > ctx_r0.limitPrimaryActions ? 0 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r0.visibleActions.length === 3 && ctx_r0.visibleActions[2] && !ctx_r0.isMobile ? 1 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r0.visibleActions[1] && (ctx_r0.visibleActions.length === 2 || !ctx_r0.isMobile) ? 2 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r0.visibleActions[0] ? 3 : -1);
  }
}
function PoPageDefaultComponent_Conditional_7_Case_0_Case_3_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoPageDefaultComponent_Conditional_7_Case_0_Case_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, PoPageDefaultComponent_Conditional_7_Case_0_Case_3_ng_container_0_Template, 1, 0, "ng-container", 15);
  }
  if (rf & 2) {
    ɵɵnextContext(3);
    const actionsDropdown_r8 = ɵɵreference(2);
    ɵɵproperty("ngTemplateOutlet", actionsDropdown_r8);
  }
}
function PoPageDefaultComponent_Conditional_7_Case_0_Case_4_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoPageDefaultComponent_Conditional_7_Case_0_Case_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, PoPageDefaultComponent_Conditional_7_Case_0_Case_4_ng_container_0_Template, 1, 0, "ng-container", 16);
  }
  if (rf & 2) {
    ɵɵnextContext(3);
    const actionsMixed_r9 = ɵɵreference(4);
    ɵɵproperty("ngTemplateOutlet", actionsMixed_r9)("ngTemplateOutletContext", ɵɵpureFunction0(2, _c161));
  }
}
function PoPageDefaultComponent_Conditional_7_Case_0_Case_5_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoPageDefaultComponent_Conditional_7_Case_0_Case_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, PoPageDefaultComponent_Conditional_7_Case_0_Case_5_ng_container_0_Template, 1, 0, "ng-container", 15);
  }
  if (rf & 2) {
    ɵɵnextContext(3);
    const actionsDefault_r10 = ɵɵreference(6);
    ɵɵproperty("ngTemplateOutlet", actionsDefault_r10);
  }
}
function PoPageDefaultComponent_Conditional_7_Case_0_Template(rf, ctx) {
  if (rf & 1) {
    const _r7 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-page-header", 11)(1, "po-button", 13);
    ɵɵlistener("p-click", function PoPageDefaultComponent_Conditional_7_Case_0_Template_po_button_p_click_1_listener() {
      ɵɵrestoreView(_r7);
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.back.emit());
    });
    ɵɵelementEnd();
    ɵɵelementStart(2, "div", 14);
    ɵɵconditionalCreate(3, PoPageDefaultComponent_Conditional_7_Case_0_Case_3_Template, 1, 1, "ng-container")(4, PoPageDefaultComponent_Conditional_7_Case_0_Case_4_Template, 1, 3, "ng-container")(5, PoPageDefaultComponent_Conditional_7_Case_0_Case_5_Template, 1, 1, "ng-container");
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    let tmp_14_0;
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵproperty("p-type", "secondary")("p-helper", ctx_r0.refreshCustomAction() ? void 0 : ctx_r0.helper())("p-custom-action", ctx_r0.refreshCustomAction())("p-size", ctx_r0.componentsSize)("p-subtitle", ctx_r0.subtitle)("p-title", ctx_r0.title);
    ɵɵadvance();
    ɵɵproperty("p-icon", ctx_r0.backIcon)("p-size", ctx_r0.componentsSize)("p-aria-label", ctx_r0.backNavigationLabel);
    ɵɵadvance(2);
    ɵɵconditional((tmp_14_0 = ctx_r0.pageActionsLayout) === "dropdown" ? 3 : tmp_14_0 === "mixed" ? 4 : 5);
  }
}
function PoPageDefaultComponent_Conditional_7_Case_1_Case_2_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoPageDefaultComponent_Conditional_7_Case_1_Case_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, PoPageDefaultComponent_Conditional_7_Case_1_Case_2_ng_container_0_Template, 1, 0, "ng-container", 15);
  }
  if (rf & 2) {
    ɵɵnextContext(3);
    const actionsDropdown_r8 = ɵɵreference(2);
    ɵɵproperty("ngTemplateOutlet", actionsDropdown_r8);
  }
}
function PoPageDefaultComponent_Conditional_7_Case_1_Case_3_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoPageDefaultComponent_Conditional_7_Case_1_Case_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, PoPageDefaultComponent_Conditional_7_Case_1_Case_3_ng_container_0_Template, 1, 0, "ng-container", 16);
  }
  if (rf & 2) {
    ɵɵnextContext(3);
    const actionsMixed_r9 = ɵɵreference(4);
    ɵɵproperty("ngTemplateOutlet", actionsMixed_r9)("ngTemplateOutletContext", ɵɵpureFunction0(2, _c161));
  }
}
function PoPageDefaultComponent_Conditional_7_Case_1_Case_4_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoPageDefaultComponent_Conditional_7_Case_1_Case_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, PoPageDefaultComponent_Conditional_7_Case_1_Case_4_ng_container_0_Template, 1, 0, "ng-container", 15);
  }
  if (rf & 2) {
    ɵɵnextContext(3);
    const actionsDefault_r10 = ɵɵreference(6);
    ɵɵproperty("ngTemplateOutlet", actionsDefault_r10);
  }
}
function PoPageDefaultComponent_Conditional_7_Case_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "po-page-header", 11)(1, "div", 14);
    ɵɵconditionalCreate(2, PoPageDefaultComponent_Conditional_7_Case_1_Case_2_Template, 1, 1, "ng-container")(3, PoPageDefaultComponent_Conditional_7_Case_1_Case_3_Template, 1, 3, "ng-container")(4, PoPageDefaultComponent_Conditional_7_Case_1_Case_4_Template, 1, 1, "ng-container");
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    let tmp_11_0;
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵproperty("p-type", "tertiary")("p-helper", ctx_r0.refreshCustomAction() ? void 0 : ctx_r0.helper())("p-custom-action", ctx_r0.refreshCustomAction())("p-size", ctx_r0.componentsSize)("p-subtitle", ctx_r0.subtitle)("p-title", ctx_r0.title);
    ɵɵadvance(2);
    ɵɵconditional((tmp_11_0 = ctx_r0.pageActionsLayout) === "dropdown" ? 2 : tmp_11_0 === "mixed" ? 3 : 4);
  }
}
function PoPageDefaultComponent_Conditional_7_Case_2_Case_2_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoPageDefaultComponent_Conditional_7_Case_2_Case_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, PoPageDefaultComponent_Conditional_7_Case_2_Case_2_ng_container_0_Template, 1, 0, "ng-container", 15);
  }
  if (rf & 2) {
    ɵɵnextContext(3);
    const actionsDropdown_r8 = ɵɵreference(2);
    ɵɵproperty("ngTemplateOutlet", actionsDropdown_r8);
  }
}
function PoPageDefaultComponent_Conditional_7_Case_2_Case_3_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoPageDefaultComponent_Conditional_7_Case_2_Case_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, PoPageDefaultComponent_Conditional_7_Case_2_Case_3_ng_container_0_Template, 1, 0, "ng-container", 16);
  }
  if (rf & 2) {
    ɵɵnextContext(3);
    const actionsMixed_r9 = ɵɵreference(4);
    ɵɵproperty("ngTemplateOutlet", actionsMixed_r9)("ngTemplateOutletContext", ɵɵpureFunction0(2, _c162));
  }
}
function PoPageDefaultComponent_Conditional_7_Case_2_Case_4_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoPageDefaultComponent_Conditional_7_Case_2_Case_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, PoPageDefaultComponent_Conditional_7_Case_2_Case_4_ng_container_0_Template, 1, 0, "ng-container", 15);
  }
  if (rf & 2) {
    ɵɵnextContext(3);
    const actionsDefault_r10 = ɵɵreference(6);
    ɵɵproperty("ngTemplateOutlet", actionsDefault_r10);
  }
}
function PoPageDefaultComponent_Conditional_7_Case_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "po-page-header", 12)(1, "div", 14);
    ɵɵconditionalCreate(2, PoPageDefaultComponent_Conditional_7_Case_2_Case_2_Template, 1, 1, "ng-container")(3, PoPageDefaultComponent_Conditional_7_Case_2_Case_3_Template, 1, 3, "ng-container")(4, PoPageDefaultComponent_Conditional_7_Case_2_Case_4_Template, 1, 1, "ng-container");
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    let tmp_11_0;
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵproperty("p-breadcrumb", ctx_r0.breadcrumb)("p-helper", ctx_r0.refreshCustomAction() ? void 0 : ctx_r0.helper())("p-custom-action", ctx_r0.refreshCustomAction())("p-size", ctx_r0.componentsSize)("p-subtitle", ctx_r0.subtitle)("p-title", ctx_r0.title);
    ɵɵadvance(2);
    ɵɵconditional((tmp_11_0 = ctx_r0.pageActionsLayout) === "dropdown" ? 2 : tmp_11_0 === "mixed" ? 3 : 4);
  }
}
function PoPageDefaultComponent_Conditional_7_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵconditionalCreate(0, PoPageDefaultComponent_Conditional_7_Case_0_Template, 6, 10, "po-page-header", 11)(1, PoPageDefaultComponent_Conditional_7_Case_1_Template, 5, 7, "po-page-header", 11)(2, PoPageDefaultComponent_Conditional_7_Case_2_Template, 5, 7, "po-page-header", 12);
  }
  if (rf & 2) {
    let tmp_4_0;
    const ctx_r0 = ɵɵnextContext();
    ɵɵconditional((tmp_4_0 = ctx_r0.pageHeaderType) === "secondary" ? 0 : tmp_4_0 === "tertiary" ? 1 : 2);
  }
}
function PoPageDetailComponent_Conditional_1_Conditional_1_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-button", 5);
    ɵɵlistener("p-click", function PoPageDetailComponent_Conditional_1_Conditional_1_Conditional_1_Template_po_button_p_click_0_listener() {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r1.back.emit());
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵproperty("p-icon", ctx_r1.hasEditOrRemoveFn("icon"))("p-label", ctx_r1.literals.back)("p-kind", ctx_r1.hasEditOrRemoveFn("type"))("p-size", ctx_r1.componentsSize);
  }
}
function PoPageDetailComponent_Conditional_1_Conditional_1_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    const _r3 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-button", 6);
    ɵɵlistener("p-click", function PoPageDetailComponent_Conditional_1_Conditional_1_Conditional_2_Template_po_button_p_click_0_listener() {
      ɵɵrestoreView(_r3);
      const ctx_r1 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r1.remove.emit());
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵproperty("p-icon", ctx_r1.hasEditFn("icon"))("p-label", ctx_r1.literals.remove)("p-kind", ctx_r1.hasEditFn("type"))("p-size", ctx_r1.componentsSize);
  }
}
function PoPageDetailComponent_Conditional_1_Conditional_1_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    const _r4 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-button", 7);
    ɵɵlistener("p-click", function PoPageDetailComponent_Conditional_1_Conditional_1_Conditional_3_Template_po_button_p_click_0_listener() {
      ɵɵrestoreView(_r4);
      const ctx_r1 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r1.edit.emit());
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵproperty("p-label", ctx_r1.literals.edit)("p-size", ctx_r1.componentsSize);
  }
}
function PoPageDetailComponent_Conditional_1_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 1);
    ɵɵconditionalCreate(1, PoPageDetailComponent_Conditional_1_Conditional_1_Conditional_1_Template, 1, 4, "po-button", 2);
    ɵɵconditionalCreate(2, PoPageDetailComponent_Conditional_1_Conditional_1_Conditional_2_Template, 1, 4, "po-button", 3);
    ɵɵconditionalCreate(3, PoPageDetailComponent_Conditional_1_Conditional_1_Conditional_3_Template, 1, 2, "po-button", 4);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵadvance();
    ɵɵconditional(ctx_r1.hasEvent("back") ? 1 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r1.hasEvent("remove") ? 2 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r1.hasEvent("edit") ? 3 : -1);
  }
}
function PoPageDetailComponent_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "po-page-header", 0);
    ɵɵconditionalCreate(1, PoPageDetailComponent_Conditional_1_Conditional_1_Template, 4, 3, "div", 1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("p-breadcrumb", ctx_r1.breadcrumb)("p-size", ctx_r1.componentsSize)("p-subtitle", ctx_r1.subtitle)("p-title", ctx_r1.title);
    ɵɵadvance();
    ɵɵconditional(ctx_r1.hasAnyAction() ? 1 : -1);
  }
}
function PoPageEditComponent_Conditional_1_Conditional_1_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-button", 5);
    ɵɵlistener("p-click", function PoPageEditComponent_Conditional_1_Conditional_1_Conditional_1_Template_po_button_p_click_0_listener() {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r1.cancel.emit());
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵproperty("p-icon", ctx_r1.getIcon("cancel"))("p-label", ctx_r1.literals.cancel)("p-kind", ctx_r1.getType("cancel"))("p-size", ctx_r1.componentsSize);
  }
}
function PoPageEditComponent_Conditional_1_Conditional_1_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    const _r3 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-button", 6);
    ɵɵlistener("p-click", function PoPageEditComponent_Conditional_1_Conditional_1_Conditional_2_Template_po_button_p_click_0_listener() {
      ɵɵrestoreView(_r3);
      const ctx_r1 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r1.saveNew.emit());
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵproperty("p-disabled", ctx_r1.disableSubmit)("p-icon", ctx_r1.getIcon("saveNew"))("p-label", ctx_r1.literals.saveNew)("p-kind", ctx_r1.getType("saveNew"))("p-size", ctx_r1.componentsSize);
  }
}
function PoPageEditComponent_Conditional_1_Conditional_1_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    const _r4 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-button", 7);
    ɵɵlistener("p-click", function PoPageEditComponent_Conditional_1_Conditional_1_Conditional_3_Template_po_button_p_click_0_listener() {
      ɵɵrestoreView(_r4);
      const ctx_r1 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r1.save.emit());
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵproperty("p-disabled", ctx_r1.disableSubmit)("p-label", ctx_r1.literals.save)("p-size", ctx_r1.componentsSize);
  }
}
function PoPageEditComponent_Conditional_1_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 1);
    ɵɵconditionalCreate(1, PoPageEditComponent_Conditional_1_Conditional_1_Conditional_1_Template, 1, 4, "po-button", 2);
    ɵɵconditionalCreate(2, PoPageEditComponent_Conditional_1_Conditional_1_Conditional_2_Template, 1, 5, "po-button", 3);
    ɵɵconditionalCreate(3, PoPageEditComponent_Conditional_1_Conditional_1_Conditional_3_Template, 1, 3, "po-button", 4);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵadvance();
    ɵɵconditional(ctx_r1.hasEvent("cancel") ? 1 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r1.hasEvent("saveNew") ? 2 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r1.hasEvent("save") ? 3 : -1);
  }
}
function PoPageEditComponent_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "po-page-header", 0);
    ɵɵconditionalCreate(1, PoPageEditComponent_Conditional_1_Conditional_1_Template, 4, 3, "div", 1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("p-breadcrumb", ctx_r1.breadcrumb)("p-size", ctx_r1.componentsSize)("p-subtitle", ctx_r1.subtitle)("p-title", ctx_r1.title);
    ɵɵadvance();
    ɵɵconditional(ctx_r1.hasAnyAction() ? 1 : -1);
  }
}
var _c163 = (a0) => [a0];
function PoPageListComponent_Conditional_1_Conditional_4_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-button", 11);
    ɵɵlistener("p-click", function PoPageListComponent_Conditional_1_Conditional_4_Template_po_button_p_click_0_listener() {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.callAction(ctx_r1.visibleActions[0]));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵproperty("p-danger", ctx_r1.visibleActions[0].type === "danger")("p-disabled", ctx_r1.actionIsDisabled(ctx_r1.actions[0]))("p-icon", ctx_r1.visibleActions[0].icon)("p-label", ctx_r1.visibleActions[0].label)("p-size", ctx_r1.componentsSize);
  }
}
function PoPageListComponent_Conditional_1_Conditional_5_Template(rf, ctx) {
  if (rf & 1) {
    const _r3 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-button", 12);
    ɵɵlistener("p-click", function PoPageListComponent_Conditional_1_Conditional_5_Template_po_button_p_click_0_listener() {
      ɵɵrestoreView(_r3);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.callAction(ctx_r1.visibleActions[1]));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵproperty("p-danger", ctx_r1.visibleActions[1].type === "danger")("p-disabled", ctx_r1.actionIsDisabled(ctx_r1.actions[1]))("p-label", ctx_r1.visibleActions[1].label)("p-size", ctx_r1.componentsSize);
  }
}
function PoPageListComponent_Conditional_1_Conditional_6_Template(rf, ctx) {
  if (rf & 1) {
    const _r4 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-button", 12);
    ɵɵlistener("p-click", function PoPageListComponent_Conditional_1_Conditional_6_Template_po_button_p_click_0_listener() {
      ɵɵrestoreView(_r4);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.callAction(ctx_r1.visibleActions[2]));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵproperty("p-danger", ctx_r1.visibleActions[2].type === "danger")("p-disabled", ctx_r1.actionIsDisabled(ctx_r1.visibleActions[2]))("p-label", ctx_r1.visibleActions[2].label)("p-size", ctx_r1.componentsSize);
  }
}
function PoPageListComponent_Conditional_1_Conditional_7_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-dropdown", 8);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵproperty("p-actions", ctx_r1.dropdownActions)("p-label", ctx_r1.literals.otherActions)("p-size", ctx_r1.componentsSize);
  }
}
function PoPageListComponent_Conditional_1_Conditional_8_Conditional_6_Template(rf, ctx) {
  if (rf & 1) {
    const _r6 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 17)(1, "span", 18);
    ɵɵlistener("click", function PoPageListComponent_Conditional_1_Conditional_8_Conditional_6_Template_span_click_1_listener() {
      ɵɵrestoreView(_r6);
      const ctx_r1 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r1.callActionFilter("advancedAction"));
    })("keydown.enter", function PoPageListComponent_Conditional_1_Conditional_8_Conditional_6_Template_span_keydown_enter_1_listener() {
      ɵɵrestoreView(_r6);
      const ctx_r1 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r1.callActionFilter("advancedAction"));
    });
    ɵɵtext(2);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵadvance(2);
    ɵɵtextInterpolate1(" ", ctx_r1.advancedSearch, " ");
  }
}
function PoPageListComponent_Conditional_1_Conditional_8_Template(rf, ctx) {
  if (rf & 1) {
    const _r5 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 9)(1, "div", 13)(2, "div", 14)(3, "po-icon", 15);
    ɵɵlistener("click", function PoPageListComponent_Conditional_1_Conditional_8_Template_po_icon_click_3_listener() {
      ɵɵrestoreView(_r5);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.callActionFilter("action"));
    });
    ɵɵelementEnd()();
    ɵɵelementStart(4, "input", 16, 0);
    ɵɵlistener("keypress", function PoPageListComponent_Conditional_1_Conditional_8_Template_input_keypress_4_listener($event) {
      ɵɵrestoreView(_r5);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.onkeypress($event.keyCode));
    });
    ɵɵelementEnd()();
    ɵɵconditionalCreate(6, PoPageListComponent_Conditional_1_Conditional_8_Conditional_6_Template, 3, 1, "div", 17);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵproperty("ngClass", ɵɵpureFunction2(10, _c132, ctx_r1.hasCustomFilterSize() ? ctx_r1.filterSizeClass(ctx_r1.filter.width) : "", ((ctx_r1.visibleActions == null ? null : ctx_r1.visibleActions.length) > 0 || ctx_r1.subtitle) && ctx_r1.title ? "po-page-list-filter-extra-margin-top" : ""));
    ɵɵadvance();
    ɵɵproperty("ngClass", ɵɵpureFunction1(13, _c163, ctx_r1.hasCustomFilterSize() ? ctx_r1.filter.advancedAction ? "po-page-filter-input-variable-size" : "po-page-filter-input-variable-size-wo-adv-search" : ""));
    ɵɵadvance(2);
    ɵɵclassProp("po-field-icon-aa", ctx_r1.componentsSize === "small");
    ɵɵadvance();
    ɵɵclassProp("po-input-icon-right-aa", ctx_r1.componentsSize === "small");
    ɵɵproperty("placeholder", ctx_r1.filter.placeholder || "")("value", ctx_r1.quickSearchValue || "");
    ɵɵattribute("p-size", ctx_r1.componentsSize);
    ɵɵadvance(2);
    ɵɵconditional(ctx_r1.filter.advancedAction ? 6 : -1);
  }
}
function PoPageListComponent_Conditional_1_Conditional_9_Template(rf, ctx) {
  if (rf & 1) {
    const _r7 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-disclaimer-group", 19);
    ɵɵlistener("p-change", function PoPageListComponent_Conditional_1_Conditional_9_Template_po_disclaimer_group_p_change_0_listener($event) {
      ɵɵrestoreView(_r7);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.onChangeDisclaimerGroup($event));
    })("p-remove", function PoPageListComponent_Conditional_1_Conditional_9_Template_po_disclaimer_group_p_remove_0_listener($event) {
      ɵɵrestoreView(_r7);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.onRemoveDisclaimer($event));
    })("p-remove-all", function PoPageListComponent_Conditional_1_Conditional_9_Template_po_disclaimer_group_p_remove_all_0_listener($event) {
      ɵɵrestoreView(_r7);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.onRemoveAllDisclaimers($event));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵclassProp("po-page-list-disclaimer-group", !!(ctx_r1.disclaimerGroup == null ? null : ctx_r1.disclaimerGroup.disclaimers == null ? null : ctx_r1.disclaimerGroup.disclaimers.length));
    ɵɵproperty("p-disclaimers", ctx_r1.disclaimerGroup == null ? null : ctx_r1.disclaimerGroup.disclaimers)("p-hide-remove-all", ctx_r1.disclaimerGroup == null ? null : ctx_r1.disclaimerGroup.hideRemoveAll)("p-title", ctx_r1.disclaimerGroup == null ? null : ctx_r1.disclaimerGroup.title);
  }
}
function PoPageListComponent_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "po-page-header", 2)(1, "div", 3)(2, "div", 4)(3, "div", 5);
    ɵɵconditionalCreate(4, PoPageListComponent_Conditional_1_Conditional_4_Template, 1, 5, "po-button", 6);
    ɵɵconditionalCreate(5, PoPageListComponent_Conditional_1_Conditional_5_Template, 1, 4, "po-button", 7);
    ɵɵconditionalCreate(6, PoPageListComponent_Conditional_1_Conditional_6_Template, 1, 4, "po-button", 7);
    ɵɵconditionalCreate(7, PoPageListComponent_Conditional_1_Conditional_7_Template, 1, 3, "po-dropdown", 8);
    ɵɵelementEnd();
    ɵɵconditionalCreate(8, PoPageListComponent_Conditional_1_Conditional_8_Template, 7, 15, "div", 9);
    ɵɵelementEnd();
    ɵɵconditionalCreate(9, PoPageListComponent_Conditional_1_Conditional_9_Template, 1, 5, "po-disclaimer-group", 10);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵclassProp("po-page-list-header-padding", ctx_r1.filter && !ctx_r1.visibleActions.length);
    ɵɵproperty("p-breadcrumb", ctx_r1.breadcrumb)("p-title", ctx_r1.title)("p-size", ctx_r1.componentsSize)("p-subtitle", ctx_r1.subtitle);
    ɵɵadvance(3);
    ɵɵclassProp("po-page-list-actions-padding", ctx_r1.filter);
    ɵɵadvance();
    ɵɵconditional(ctx_r1.visibleActions[0] ? 4 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r1.visibleActions[1] && (ctx_r1.visibleActions.length === 2 || !ctx_r1.isMobile) ? 5 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r1.visibleActions.length === 3 && ctx_r1.visibleActions[2] && !ctx_r1.isMobile ? 6 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r1.visibleActions.length > ctx_r1.limitPrimaryActions ? 7 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r1.filter ? 8 : -1);
    ɵɵadvance();
    ɵɵconditional(!!ctx_r1.disclaimerGroup ? 9 : -1);
  }
}
function PoSkeletonComponent_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span", 1);
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵtextInterpolate(ctx_r0.ariaLabel());
  }
}
function PoSlideCirclesComponent_For_2_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 2);
    ɵɵlistener("click", function PoSlideCirclesComponent_For_2_Template_div_click_0_listener() {
      const ɵ$index_3_r2 = ɵɵrestoreView(_r1).$index;
      const ctx_r2 = ɵɵnextContext();
      return ɵɵresetView(ctx_r2.click.emit(ɵ$index_3_r2));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ɵ$index_3_r2 = ctx.$index;
    const ctx_r2 = ɵɵnextContext();
    ɵɵclassProp("po-slide-active-circle", ɵ$index_3_r2 === ctx_r2.currentSlideIndex);
  }
}
var _c164 = ["slideItem"];
var _c165 = (a0, a1) => ({
  alt: a0,
  image: a1
});
var _c166 = (a0) => ({
  item: a0
});
var _c167 = (a0, a1, a2) => ({
  alt: a0,
  image: a1,
  data: a2
});
function PoSlideItemComponent_Case_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "a", 4);
    ɵɵelementContainer(1, 8);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    const slideItemImage_r2 = ɵɵreference(8);
    ɵɵproperty("routerLink", ctx_r0.link);
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", slideItemImage_r2)("ngTemplateOutletContext", ɵɵpureFunction1(6, _c166, ɵɵpureFunction2(3, _c165, ctx_r0.alt, ctx_r0.image)));
  }
}
function PoSlideItemComponent_Case_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "a", 5);
    ɵɵelementContainer(1, 8);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    const slideItemImage_r2 = ɵɵreference(8);
    ɵɵproperty("href", ctx_r0.link, ɵɵsanitizeUrl);
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", slideItemImage_r2)("ngTemplateOutletContext", ɵɵpureFunction1(6, _c166, ɵɵpureFunction2(3, _c165, ctx_r0.alt, ctx_r0.image)));
  }
}
function PoSlideItemComponent_Case_4_Template(rf, ctx) {
  if (rf & 1) {
    const _r3 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "a", 9);
    ɵɵlistener("click", function PoSlideItemComponent_Case_4_Template_a_click_0_listener() {
      ɵɵrestoreView(_r3);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.action ? ctx_r0.action(ctx_r0.data) : void 0);
    });
    ɵɵelementContainer(1, 8);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    const slideItemTemplate_r4 = ɵɵreference(6);
    const slideItemImage_r2 = ɵɵreference(8);
    ɵɵproperty("ngClass", ctx_r0.action ? "po-slide-item-link" : "po-slide-item-no-link");
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", ctx_r0.template ? slideItemTemplate_r4 : slideItemImage_r2)("ngTemplateOutletContext", ɵɵpureFunction1(7, _c166, ɵɵpureFunction3(3, _c167, ctx_r0.alt, ctx_r0.image, ctx_r0.data)));
  }
}
function PoSlideItemComponent_ng_template_5_Conditional_0_ng_template_1_Template(rf, ctx) {
}
function PoSlideItemComponent_ng_template_5_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 10);
    ɵɵtemplate(1, PoSlideItemComponent_ng_template_5_Conditional_0_ng_template_1_Template, 0, 0, "ng-template", 8);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r4 = ɵɵnextContext();
    const item_r6 = ctx_r4.item;
    const index_r7 = ctx_r4.index;
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", ctx_r0.template.templateRef)("ngTemplateOutletContext", ɵɵpureFunction2(2, _c149, item_r6.data, index_r7));
  }
}
function PoSlideItemComponent_ng_template_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵconditionalCreate(0, PoSlideItemComponent_ng_template_5_Conditional_0_Template, 2, 5, "div", 10);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵconditional(ctx_r0.template ? 0 : -1);
  }
}
function PoSlideItemComponent_ng_template_7_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "img", 12);
  }
  if (rf & 2) {
    const item_r8 = ɵɵnextContext().item;
    const ctx_r0 = ɵɵnextContext();
    ɵɵstyleProp("height", ctx_r0.imageHeight, "px");
    ɵɵproperty("alt", item_r8.alt)("src", item_r8.image, ɵɵsanitizeUrl);
  }
}
function PoSlideItemComponent_ng_template_7_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵconditionalCreate(0, PoSlideItemComponent_ng_template_7_Conditional_0_Template, 1, 4, "img", 11);
  }
  if (rf & 2) {
    const item_r8 = ctx.item;
    const ctx_r0 = ɵɵnextContext();
    ɵɵconditional(item_r8.image && !ctx_r0.isIEOrEdge ? 0 : -1);
  }
}
var _c168 = ["slide"];
function PoSlideComponent_For_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-slide-item", 4);
  }
  if (rf & 2) {
    const item_r1 = ctx.$implicit;
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("p-action", item_r1.action)("p-data", item_r1)("p-image", item_r1.image)("p-image-height", ctx_r1.imageHeight)("p-template", ctx_r1.slideContentTemplate)("p-alt", item_r1.alt)("p-link", item_r1.link);
  }
}
function PoSlideComponent_Conditional_6_Template(rf, ctx) {
  if (rf & 1) {
    const _r3 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-slide-control", 8);
    ɵɵlistener("p-click", function PoSlideComponent_Conditional_6_Template_po_slide_control_p_click_0_listener() {
      ɵɵrestoreView(_r3);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.previousControl());
    });
    ɵɵelementEnd();
  }
}
function PoSlideComponent_Conditional_7_Template(rf, ctx) {
  if (rf & 1) {
    const _r4 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-slide-control", 9);
    ɵɵlistener("p-click", function PoSlideComponent_Conditional_7_Template_po_slide_control_p_click_0_listener() {
      ɵɵrestoreView(_r4);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.nextControl());
    });
    ɵɵelementEnd();
  }
}
function PoSlideComponent_Conditional_8_Template(rf, ctx) {
  if (rf & 1) {
    const _r5 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-slide-circles", 10);
    ɵɵlistener("p-click", function PoSlideComponent_Conditional_8_Template_po_slide_circles_p_click_0_listener($event) {
      ɵɵrestoreView(_r5);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.goToItem($event));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("p-current-slide-index", ctx_r1.currentSlideIndex)("p-items", ctx_r1.slideItems);
  }
}
var _c169 = (a0, a1) => ({
  "po-stepper-circle-border": a0,
  "po-stepper-circle-done": a1
});
function PoStepperCircleComponent_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 1);
    ɵɵelement(1, "po-icon", 2);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵclassProp("po-stepper-circle-content-lg", ctx_r0.isLargeStep)("po-stepper-circle-content-md", ctx_r0.isMediumStep);
    ɵɵproperty("p-icon", ctx_r0.iconActive ? ctx_r0.iconActive : "ICON_EDIT");
  }
}
function PoStepperCircleComponent_Conditional_2_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "po-icon", 2);
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵclassProp("po-stepper-circle-content-lg", ctx_r0.isLargeStep)("po-stepper-circle-content-md", ctx_r0.isMediumStep);
    ɵɵproperty("p-icon", ctx_r0.isDone ? ctx_r0.iconDone || "ICON_OK" : ctx_r0.iconDefault ? ctx_r0.iconDefault : ctx_r0.icons ? "ICON_INFO" : "");
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", !ctx_r0.icons && !ctx_r0.isDone && !ctx_r0.iconDefault ? ctx_r0.content : "", " ");
  }
}
function PoStepperCircleComponent_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵconditionalCreate(0, PoStepperCircleComponent_Conditional_2_Conditional_0_Template, 2, 6, "po-icon", 3);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵconditional(!ctx_r0.isActive ? 0 : -1);
  }
}
var _c170 = ["labelElement"];
function PoStepperStepComponent_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-stepper-label", 3);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("p-align-center", ctx_r0.alignCenter)("p-content", ctx_r0.label)("p-status", ctx_r0.status)("p-vertical-orientation", ctx_r0.isVerticalOrientation)("p-disable-click", ctx_r0.disabledClick);
    ɵɵattribute("aria-label", ctx_r0.label);
  }
}
function PoStepperComponent_ng_container_2_div_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "div");
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    const step_r3 = ctx_r1.$implicit;
    const index_r4 = ctx_r1.index;
    const ctx_r4 = ɵɵnextContext();
    ɵɵstyleProp("left", ctx_r4.isVerticalOrientation ? !ctx_r4.stepSizeCircle || ctx_r4.stepSizeCircle === 24 ? 16 : ctx_r4.stepSizeCircle / 2 : null, "px");
    ɵɵclassProp("po-stepper-step-bar-top", ctx_r4.alignCenter && ctx_r4.isVerticalOrientation)("po-stepper-step-dashed-border-vertical", ctx_r4.alignCenter && ctx_r4.isVerticalOrientation && ctx_r4.isDashedBorderTop(step_r3, index_r4));
  }
}
function PoStepperComponent_ng_container_2_div_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "div");
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    const step_r3 = ctx_r1.$implicit;
    const index_r4 = ctx_r1.index;
    const ctx_r4 = ɵɵnextContext();
    ɵɵstyleProp("top", !ctx_r4.isVerticalOrientation ? ctx_r4.stepSizeCircle === 24 ? 16 : ctx_r4.stepSizeCircle / 2 : null, "px")("left", ctx_r4.isVerticalOrientation ? !ctx_r4.stepSizeCircle || ctx_r4.stepSizeCircle === 24 ? 16 : ctx_r4.stepSizeCircle / 2 : null, "px");
    ɵɵclassProp("po-stepper-step-bar-bottom", ctx_r4.isVerticalOrientation)("po-stepper-step-bar-bottom-center", ctx_r4.alignCenter && ctx_r4.isVerticalOrientation)("po-stepper-step-bar-right", !ctx_r4.isVerticalOrientation)("po-stepper-step-bar-center", ctx_r4.alignCenter && !ctx_r4.isVerticalOrientation)("po-stepper-step-dashed-border", !ctx_r4.isVerticalOrientation && ctx_r4.isDashedBorder(step_r3, index_r4))("po-stepper-step-dashed-border-vertical", ctx_r4.isVerticalOrientation && ctx_r4.isDashedBorder(step_r3, index_r4));
  }
}
function PoStepperComponent_ng_container_2_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementContainerStart(0);
    ɵɵelementStart(1, "div", 3);
    ɵɵtemplate(2, PoStepperComponent_ng_container_2_div_2_Template, 1, 6, "div", 4);
    ɵɵelementStart(3, "po-stepper-step", 5);
    ɵɵlistener("p-activated", function PoStepperComponent_ng_container_2_Template_po_stepper_step_p_activated_3_listener() {
      const step_r3 = ɵɵrestoreView(_r1).$implicit;
      const ctx_r4 = ɵɵnextContext();
      return ɵɵresetView(ctx_r4.onStepActive(step_r3));
    })("p-click", function PoStepperComponent_ng_container_2_Template_po_stepper_step_p_click_3_listener() {
      const ctx_r5 = ɵɵrestoreView(_r1);
      const step_r3 = ctx_r5.$implicit;
      const index_r4 = ctx_r5.index;
      const ctx_r4 = ɵɵnextContext();
      return ɵɵresetView(ctx_r4.changeStep(index_r4, step_r3));
    })("p-enter", function PoStepperComponent_ng_container_2_Template_po_stepper_step_p_enter_3_listener() {
      const ctx_r6 = ɵɵrestoreView(_r1);
      const step_r3 = ctx_r6.$implicit;
      const index_r4 = ctx_r6.index;
      const ctx_r4 = ɵɵnextContext();
      return ɵɵresetView(ctx_r4.changeStep(index_r4, step_r3));
    });
    ɵɵelementEnd();
    ɵɵtemplate(4, PoStepperComponent_ng_container_2_div_4_Template, 1, 16, "div", 6);
    ɵɵelementEnd();
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    let tmp_12_0;
    const step_r3 = ctx.$implicit;
    const index_r4 = ctx.index;
    const ctx_r4 = ɵɵnextContext();
    ɵɵadvance(2);
    ɵɵproperty("ngIf", index_r4 > 0);
    ɵɵadvance();
    ɵɵclassProp("po-stepper-step-position-center", ctx_r4.alignCenter);
    ɵɵproperty("p-align-center", ctx_r4.alignCenter)("p-circle-content", index_r4 + 1)("p-label", step_r3.label)("p-orientation", ctx_r4.orientation)("p-status", step_r3.status)("p-step-icons", ctx_r4.stepIcons)("p-step-size", ctx_r4.stepSize)("p-next-status", (tmp_12_0 = ctx_r4.poSteps.get(index_r4 + 1)) == null ? null : tmp_12_0.status)("p-icon-default", step_r3.iconDefault)("p-step-icon-active", ctx_r4.iconActive)("p-step-icon-done", ctx_r4.iconDone)("p-vertical-orientation", ctx_r4.isVerticalOrientation)("p-disable-click", ctx_r4.disabledClick);
    ɵɵadvance();
    ɵɵproperty("ngIf", index_r4 < ctx_r4.stepList.length - 1);
  }
}
function PoStepperComponent_div_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 7);
    ɵɵprojection(1);
    ɵɵelementEnd();
  }
}
var _c171 = ["tabButtom"];
var _c172 = (a0) => ({
  "po-tab-focusable": a0
});
var _c173 = (a0, a1) => ({
  "po-tab-border-active": a0,
  "po-tab-border-disabled": a1
});
function PoTabDropdownComponent_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 4)(1, "po-listbox", 5, 1);
    ɵɵlistener("p-activated-tabs", function PoTabDropdownComponent_Conditional_3_Template_po_listbox_p_activated_tabs_1_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.activated.emit($event));
    })("p-change-state-tabs", function PoTabDropdownComponent_Conditional_3_Template_po_listbox_p_change_state_tabs_1_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.changeState.emit($event));
    })("p-click-tabs", function PoTabDropdownComponent_Conditional_3_Template_po_listbox_p_click_tabs_1_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      ctx_r1.closeDropdown();
      return ɵɵresetView(ctx_r1.click.emit($event));
    })("p-close", function PoTabDropdownComponent_Conditional_3_Template_po_listbox_p_close_1_listener() {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.closeAndReturnToButtom());
    });
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("ngStyle", ctx_r1.dropdownStyles);
    ɵɵadvance();
    ɵɵproperty("p-items", ctx_r1.tabs)("p-is-tabs", true)("p-size", ctx_r1.size);
  }
}
var _c174 = ["tabDropdown"];
var _c175 = ["containerTabs"];
var _c176 = ["tabButton"];
function PoTabsComponent_For_5_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-tab-button", 9, 1);
    ɵɵlistener("keyup.enter", function PoTabsComponent_For_5_Template_po_tab_button_keyup_enter_0_listener() {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.closeListbox());
    })("p-activated", function PoTabsComponent_For_5_Template_po_tab_button_p_activated_0_listener() {
      const tab_r3 = ɵɵrestoreView(_r1).$implicit;
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onTabActive(tab_r3));
    })("p-change-state", function PoTabsComponent_For_5_Template_po_tab_button_p_change_state_0_listener() {
      const tab_r3 = ɵɵrestoreView(_r1).$implicit;
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onTabChangeState(tab_r3));
    })("p-click", function PoTabsComponent_For_5_Template_po_tab_button_p_click_0_listener() {
      const tab_r3 = ɵɵrestoreView(_r1).$implicit;
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.selectedTab(tab_r3));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const tab_r3 = ctx.$implicit;
    ɵɵproperty("p-active", tab_r3.active)("p-disabled", tab_r3.disabled)("p-hide", tab_r3.hide)("p-id", tab_r3.id)("hidden", tab_r3.hide)("p-label", tab_r3.label)("id", tab_r3.id);
    ɵɵattribute("disabled", tab_r3.disabled);
  }
}
function PoTabsComponent_Conditional_6_Template(rf, ctx) {
  if (rf & 1) {
    const _r4 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-tab-dropdown", 10, 2);
    ɵɵlistener("p-change-state", function PoTabsComponent_Conditional_6_Template_po_tab_dropdown_p_change_state_0_listener($event) {
      ɵɵrestoreView(_r4);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onTabChangeState($event));
    })("p-click", function PoTabsComponent_Conditional_6_Template_po_tab_dropdown_p_click_0_listener($event) {
      ɵɵrestoreView(_r4);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onTabActiveByDropdown($event));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵclassProp("po-tab-dropdown-device", !(ctx_r1.tabsDefault == null ? null : ctx_r1.tabsDefault.length));
    ɵɵproperty("p-label", ctx_r1.literals.moreTabs)("p-size", ctx_r1.size)("p-tabs", ctx_r1.overflowedTabs);
  }
}
function PoContextTabButtonComponent_Conditional_5_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 6);
    ɵɵlistener("click", function PoContextTabButtonComponent_Conditional_5_Template_div_click_0_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.closeTab($event));
    })("keydown", function PoContextTabButtonComponent_Conditional_5_Template_div_keydown_0_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.closeTab($event));
    });
    ɵɵelement(1, "po-icon", 7);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵproperty("tabIndex", ctx_r1.activeCloseIcon ? 0 : -1);
    ɵɵattribute("aria-label", ctx_r1.literals.close);
  }
}
function PoContextTabsComponent_ng_container_4_po_context_tab_button_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-context-tab-button", 10, 1);
    ɵɵlistener("keyup.enter", function PoContextTabsComponent_ng_container_4_po_context_tab_button_1_Template_po_context_tab_button_keyup_enter_0_listener() {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.closeListbox());
    })("p-activated", function PoContextTabsComponent_ng_container_4_po_context_tab_button_1_Template_po_context_tab_button_p_activated_0_listener() {
      ɵɵrestoreView(_r1);
      const tab_r3 = ɵɵnextContext().$implicit;
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onTabActive(tab_r3));
    })("p-change-state", function PoContextTabsComponent_ng_container_4_po_context_tab_button_1_Template_po_context_tab_button_p_change_state_0_listener() {
      ɵɵrestoreView(_r1);
      const tab_r3 = ɵɵnextContext().$implicit;
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onTabChangeState(tab_r3));
    })("p-change-visible", function PoContextTabsComponent_ng_container_4_po_context_tab_button_1_Template_po_context_tab_button_p_change_visible_0_listener() {
      ɵɵrestoreView(_r1);
      const tab_r3 = ɵɵnextContext().$implicit;
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onChangeVisibilityTab(tab_r3));
    })("p-click", function PoContextTabsComponent_ng_container_4_po_context_tab_button_1_Template_po_context_tab_button_p_click_0_listener() {
      ɵɵrestoreView(_r1);
      const tab_r3 = ɵɵnextContext().$implicit;
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.selectedTab(tab_r3));
    })("p-close", function PoContextTabsComponent_ng_container_4_po_context_tab_button_1_Template_po_context_tab_button_p_close_0_listener() {
      ɵɵrestoreView(_r1);
      const tab_r3 = ɵɵnextContext().$implicit;
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.closeTab(tab_r3));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const tab_r3 = ɵɵnextContext().$implicit;
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("p-active", tab_r3.active)("p-disabled", tab_r3.disabled)("p-show-tooltip", tab_r3.showTooltip)("p-hide", tab_r3.hide)("p-id", tab_r3.id)("hidden", tab_r3.hide)("p-hide-close", tab_r3.hideClose)("p-label", tab_r3.label)("p-literals", ctx_r1.literals)("id", tab_r3.id);
    ɵɵattribute("disabled", tab_r3.disabled);
  }
}
function PoContextTabsComponent_ng_container_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainerStart(0);
    ɵɵtemplate(1, PoContextTabsComponent_ng_container_4_po_context_tab_button_1_Template, 2, 11, "po-context-tab-button", 9);
    ɵɵelementContainerEnd();
  }
  if (rf & 2) {
    const tab_r3 = ctx.$implicit;
    ɵɵadvance();
    ɵɵproperty("ngIf", !tab_r3.removed);
  }
}
function PoContextTabsComponent_po_tab_dropdown_5_Template(rf, ctx) {
  if (rf & 1) {
    const _r4 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-tab-dropdown", 11, 2);
    ɵɵlistener("p-change-state", function PoContextTabsComponent_po_tab_dropdown_5_Template_po_tab_dropdown_p_change_state_0_listener($event) {
      ɵɵrestoreView(_r4);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onTabChangeState($event));
    })("p-click", function PoContextTabsComponent_po_tab_dropdown_5_Template_po_tab_dropdown_p_click_0_listener($event) {
      ɵɵrestoreView(_r4);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onTabActiveByDropdown($event));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵclassProp("po-tab-dropdown-device", !(ctx_r1.tabsDefault == null ? null : ctx_r1.tabsDefault.length));
    ɵɵproperty("p-label", ctx_r1.literals.moreTabs)("p-size", ctx_r1.size)("p-tabs", ctx_r1.overflowedTabs);
  }
}
var _c177 = ["notification"];
function PoToolbarNotificationComponent_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 4);
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r2 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ctx_r2.notificationNumber, " ");
  }
}
function PoToolbarProfileComponent_Conditional_5_Conditional_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 8);
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r2 = ɵɵnextContext(2);
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ctx_r2.profile.subtitle, " ");
  }
}
function PoToolbarProfileComponent_Conditional_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 5);
    ɵɵelement(1, "po-avatar", 6);
    ɵɵelementStart(2, "div")(3, "div", 7);
    ɵɵtext(4);
    ɵɵelementEnd();
    ɵɵconditionalCreate(5, PoToolbarProfileComponent_Conditional_5_Conditional_5_Template, 2, 1, "div", 8);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r2 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵproperty("p-src", ctx_r2.profileAvatar);
    ɵɵadvance(3);
    ɵɵtextInterpolate1(" ", ctx_r2.profile.title, " ");
    ɵɵadvance();
    ɵɵconditional(ctx_r2.profile.subtitle ? 5 : -1);
  }
}
function PoToolbarComponent_Conditional_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-toolbar-actions", 3);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("p-actions", ctx_r0.actions)("p-actions-icon", ctx_r0.actionsIcon);
  }
}
function PoToolbarComponent_Conditional_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-toolbar-notification", 4);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("p-notification-actions", ctx_r0.notificationActions)("p-notification-number", ctx_r0.notificationNumber);
  }
}
function PoToolbarComponent_Conditional_6_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-toolbar-profile", 5);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("p-profile", ctx_r0.profile)("p-profile-actions", ctx_r0.profileActions);
  }
}
var _c178 = ["inputCheckbox"];
function PoTreeViewItemHeaderComponent_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "button", 4);
    ɵɵlistener("click", function PoTreeViewItemHeaderComponent_Conditional_1_Template_button_click_0_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.expanded.emit($event));
    });
    ɵɵelement(1, "po-icon", 5);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵclassProp("po-tree-view-item-header-button-icon-transform", ctx_r1.item.expanded);
  }
}
function PoTreeViewItemHeaderComponent_Conditional_2_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    const _r3 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-radio", 8, 0);
    ɵɵtwoWayListener("ngModelChange", function PoTreeViewItemHeaderComponent_Conditional_2_Conditional_0_Template_po_radio_ngModelChange_0_listener($event) {
      ɵɵrestoreView(_r3);
      const ctx_r1 = ɵɵnextContext(2);
      ɵɵtwoWayBindingSet(ctx_r1.item.selected, $event) || (ctx_r1.item.selected = $event);
      return ɵɵresetView($event);
    });
    ɵɵlistener("p-change-selected", function PoTreeViewItemHeaderComponent_Conditional_2_Conditional_0_Template_po_radio_p_change_selected_0_listener() {
      ɵɵrestoreView(_r3);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.selected.emit(ctx_r1.item));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵclassProp("po-tree-view-item-header-padding", !ctx_r1.hasSubItems);
    ɵɵproperty("name", ctx_r1.idRadio);
    ɵɵtwoWayProperty("ngModel", ctx_r1.item.selected);
    ɵɵproperty("p-label", ctx_r1.item.label)("p-value", ctx_r1.item.value)("p-checked", ctx_r1.item.value === ctx_r1.selectedValue)("p-disabled", ctx_r1.item.isSelectable === false)("p-size", ctx_r1.componentsSize);
  }
}
function PoTreeViewItemHeaderComponent_Conditional_2_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r4 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-checkbox", 9);
    ɵɵtwoWayListener("ngModelChange", function PoTreeViewItemHeaderComponent_Conditional_2_Conditional_1_Template_po_checkbox_ngModelChange_0_listener($event) {
      ɵɵrestoreView(_r4);
      const ctx_r1 = ɵɵnextContext(2);
      ɵɵtwoWayBindingSet(ctx_r1.item.selected, $event) || (ctx_r1.item.selected = $event);
      return ɵɵresetView($event);
    });
    ɵɵlistener("p-change", function PoTreeViewItemHeaderComponent_Conditional_2_Conditional_1_Template_po_checkbox_p_change_0_listener() {
      ɵɵrestoreView(_r4);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.selected.emit(ctx_r1.item));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵclassProp("po-tree-view-item-header-padding", !ctx_r1.hasSubItems);
    ɵɵproperty("p-label", ctx_r1.item.label);
    ɵɵtwoWayProperty("ngModel", ctx_r1.item.selected);
    ɵɵproperty("p-disabled", ctx_r1.item.isSelectable === false)("p-size", ctx_r1.componentsSize);
  }
}
function PoTreeViewItemHeaderComponent_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵconditionalCreate(0, PoTreeViewItemHeaderComponent_Conditional_2_Conditional_0_Template, 2, 9, "po-radio", 6)(1, PoTreeViewItemHeaderComponent_Conditional_2_Conditional_1_Template, 1, 6, "po-checkbox", 7);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵconditional(ctx_r1.singleSelect ? 0 : 1);
  }
}
function PoTreeViewItemHeaderComponent_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span", 10);
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵclassProp("po-tree-view-item-header-padding", !ctx_r1.hasSubItems);
    ɵɵadvance();
    ɵɵtextInterpolate1(" ", ctx_r1.item.label, " ");
  }
}
function PoTreeViewItemComponent_Conditional_2_Conditional_1_For_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-tree-view-item", 3);
  }
  if (rf & 2) {
    const subItem_r1 = ctx.$implicit;
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵproperty("p-components-size", ctx_r1.componentsSize)("p-item", subItem_r1)("p-selectable", ctx_r1.selectable)("p-single-select", ctx_r1.singleSelect)("p-selected-value", ctx_r1.selectedValue);
  }
}
function PoTreeViewItemComponent_Conditional_2_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div");
    ɵɵrepeaterCreate(1, PoTreeViewItemComponent_Conditional_2_Conditional_1_For_2_Template, 1, 5, "po-tree-view-item", 3, ɵɵcomponentInstance().trackByFunction, true);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵproperty("@toggleBody", void 0);
    ɵɵadvance();
    ɵɵrepeater(ctx_r1.item.subItems);
  }
}
function PoTreeViewItemComponent_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "ul", 2);
    ɵɵconditionalCreate(1, PoTreeViewItemComponent_Conditional_2_Conditional_1_Template, 3, 1, "div");
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵconditional(ctx_r1.item.expanded ? 1 : -1);
  }
}
function PoTreeViewComponent_Conditional_0_For_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-tree-view-item", 2);
  }
  if (rf & 2) {
    const item_r1 = ctx.$implicit;
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵproperty("p-components-size", ctx_r1.componentsSize)("p-item", item_r1)("p-selectable", ctx_r1.selectable)("p-single-select", ctx_r1.singleSelect)("p-selected-value", ctx_r1.selectedValue);
  }
}
function PoTreeViewComponent_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "po-container", 0)(1, "ul", 1);
    ɵɵrepeaterCreate(2, PoTreeViewComponent_Conditional_0_For_3_Template, 1, 5, "po-tree-view-item", 2, ɵɵcomponentInstance().trackByFunction, true);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance(2);
    ɵɵrepeater(ctx_r1.items);
  }
}
var _c179 = ["buttonPopUp"];
var _c180 = ["wrapperInfo"];
var _c181 = ["wrapperTitle"];
var _c182 = ["tagElement"];
var _c183 = ["contentContainer"];
var _c184 = ["#refContent", ""];
function PoWidgetComponent_Conditional_1_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 18);
    ɵɵelement(1, "po-avatar", 20);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵadvance();
    ɵɵproperty("p-size", ctx_r0.avatarSize())("p-src", ctx_r0.avatarSrc());
  }
}
function PoWidgetComponent_Conditional_1_Conditional_2_ng_template_1_Template(rf, ctx) {
}
function PoWidgetComponent_Conditional_1_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 19);
    ɵɵtemplate(1, PoWidgetComponent_Conditional_1_Conditional_2_ng_template_1_Template, 0, 0, "ng-template", 21);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", ctx_r0.avatarCustomTemplate());
  }
}
function PoWidgetComponent_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 17);
    ɵɵconditionalCreate(1, PoWidgetComponent_Conditional_1_Conditional_1_Template, 2, 2, "div", 18);
    ɵɵconditionalCreate(2, PoWidgetComponent_Conditional_1_Conditional_2_Template, 2, 1, "div", 19);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵstyleProp("width", ctx_r0.avatarWidthCustomTemplate())("height", `${ctx_r0.height - 2}px`);
    ɵɵclassProp("po-widget-container__avatar_pb", ctx_r0.primaryLabel);
    ɵɵadvance();
    ɵɵconditional(!ctx_r0.avatarCustomTemplate() && ctx_r0.avatarSrc() ? 1 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r0.avatarCustomTemplate() ? 2 : -1);
  }
}
function PoWidgetComponent_Conditional_3_Conditional_1_ng_template_1_Template(rf, ctx) {
}
function PoWidgetComponent_Conditional_3_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 23);
    ɵɵtemplate(1, PoWidgetComponent_Conditional_3_Conditional_1_ng_template_1_Template, 0, 0, "ng-template", 21);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    ɵɵnextContext(2);
    const tag_r2 = ɵɵreference(13);
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", tag_r2);
  }
}
function PoWidgetComponent_Conditional_3_Conditional_5_Template(rf, ctx) {
  if (rf & 1) {
    const _r3 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "span", 30, 4);
    ɵɵlistener("click", function PoWidgetComponent_Conditional_3_Conditional_5_Template_span_click_0_listener($event) {
      ɵɵrestoreView(_r3);
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.runTitleAction($event));
    });
    ɵɵtext(2);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵproperty("p-tooltip", ctx_r0.title && ctx_r0.showTooltip() ? ctx_r0.title : "");
    ɵɵadvance(2);
    ɵɵtextInterpolate1(" ", ctx_r0.title, " ");
  }
}
function PoWidgetComponent_Conditional_3_Conditional_6_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "p", 27, 4);
    ɵɵtext(2);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵproperty("p-tooltip", ctx_r0.title && ctx_r0.showTooltip() ? ctx_r0.title : "");
    ɵɵadvance(2);
    ɵɵtextInterpolate1(" ", ctx_r0.title, " ");
  }
}
function PoWidgetComponent_Conditional_3_Conditional_7_ng_template_0_Template(rf, ctx) {
}
function PoWidgetComponent_Conditional_3_Conditional_7_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, PoWidgetComponent_Conditional_3_Conditional_7_ng_template_0_Template, 0, 0, "ng-template", 21);
  }
  if (rf & 2) {
    ɵɵnextContext(2);
    const tag_r2 = ɵɵreference(13);
    ɵɵproperty("ngTemplateOutlet", tag_r2);
  }
}
function PoWidgetComponent_Conditional_3_Conditional_8_Template(rf, ctx) {
  if (rf & 1) {
    const _r4 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 31, 5);
    ɵɵlistener("click", function PoWidgetComponent_Conditional_3_Conditional_8_Template_div_click_0_listener($event) {
      ɵɵrestoreView(_r4);
      const popupTarget_r5 = ɵɵreference(1);
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.togglePopup(popupTarget_r5, $event));
    })("keydown", function PoWidgetComponent_Conditional_3_Conditional_8_Template_div_keydown_0_listener($event) {
      return $event.stopPropagation();
    });
    ɵɵelement(2, "po-button", 32, 6);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵadvance(2);
    ɵɵproperty("p-size", ctx_r0.size)("p-disabled", ctx_r0.disabled);
  }
}
function PoWidgetComponent_Conditional_3_Conditional_9_ng_template_1_Template(rf, ctx) {
}
function PoWidgetComponent_Conditional_3_Conditional_9_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 29);
    ɵɵtemplate(1, PoWidgetComponent_Conditional_3_Conditional_9_ng_template_1_Template, 0, 0, "ng-template", 21);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    ɵɵnextContext(2);
    const tag_r2 = ɵɵreference(13);
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", tag_r2);
  }
}
function PoWidgetComponent_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 22);
    ɵɵconditionalCreate(1, PoWidgetComponent_Conditional_3_Conditional_1_Template, 2, 1, "div", 23);
    ɵɵelementStart(2, "div", 24)(3, "div", 25, 3);
    ɵɵconditionalCreate(5, PoWidgetComponent_Conditional_3_Conditional_5_Template, 3, 2, "span", 26)(6, PoWidgetComponent_Conditional_3_Conditional_6_Template, 3, 2, "p", 27);
    ɵɵconditionalCreate(7, PoWidgetComponent_Conditional_3_Conditional_7_Template, 1, 1, null, 21);
    ɵɵelementEnd();
    ɵɵconditionalCreate(8, PoWidgetComponent_Conditional_3_Conditional_8_Template, 4, 2, "div", 28);
    ɵɵelementEnd();
    ɵɵconditionalCreate(9, PoWidgetComponent_Conditional_3_Conditional_9_Template, 2, 1, "div", 29);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵclassProp("po-widget-header__tag-top-actions", ctx_r0.tagLabel && ctx_r0.tagPosition() === "top" && (ctx_r0.actions == null ? null : ctx_r0.actions.length));
    ɵɵadvance();
    ɵɵconditional(ctx_r0.tagLabel && ctx_r0.tagPosition() === "top" ? 1 : -1);
    ɵɵadvance(4);
    ɵɵconditional(ctx_r0.showTitleAction ? 5 : 6);
    ɵɵadvance(2);
    ɵɵconditional(ctx_r0.tagLabel && ctx_r0.tagPosition() === "right" ? 7 : -1);
    ɵɵadvance();
    ɵɵconditional((ctx_r0.actions == null ? null : ctx_r0.actions.length) ? 8 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r0.tagLabel && ctx_r0.tagPosition() === "bottom" ? 9 : -1);
  }
}
function PoWidgetComponent_Conditional_8_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span", 14);
    ɵɵtext(1, ".");
    ɵɵelementEnd();
  }
}
function PoWidgetComponent_Conditional_9_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    const _r7 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 33);
    ɵɵlistener("click", function PoWidgetComponent_Conditional_9_Conditional_3_Template_div_click_0_listener($event) {
      return $event.stopPropagation();
    })("keydown", function PoWidgetComponent_Conditional_9_Conditional_3_Template_div_keydown_0_listener($event) {
      return $event.stopPropagation();
    });
    ɵɵelementStart(1, "po-button", 34);
    ɵɵlistener("p-click", function PoWidgetComponent_Conditional_9_Conditional_3_Template_po_button_p_click_1_listener() {
      ɵɵrestoreView(_r7);
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.runSecondaryAction());
    });
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵadvance();
    ɵɵproperty("id", ɵɵinterpolate1("secondaryAct-", ctx_r0.id))("p-label", ctx_r0.secondaryLabel)("p-danger", ctx_r0.dangerSecondaryAction)("p-kind", ctx_r0.kindSecondaryAction || "tertiary")("p-disabled", ctx_r0.disabled)("p-size", ctx_r0.size);
  }
}
function PoWidgetComponent_Conditional_9_Template(rf, ctx) {
  if (rf & 1) {
    const _r6 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 15)(1, "div", 33);
    ɵɵlistener("click", function PoWidgetComponent_Conditional_9_Template_div_click_1_listener($event) {
      return $event.stopPropagation();
    })("keydown", function PoWidgetComponent_Conditional_9_Template_div_keydown_1_listener($event) {
      return $event.stopPropagation();
    });
    ɵɵelementStart(2, "po-button", 34);
    ɵɵlistener("p-click", function PoWidgetComponent_Conditional_9_Template_po_button_p_click_2_listener() {
      ɵɵrestoreView(_r6);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.runPrimaryAction());
    });
    ɵɵelementEnd()();
    ɵɵconditionalCreate(3, PoWidgetComponent_Conditional_9_Conditional_3_Template, 2, 7, "div", 35);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance(2);
    ɵɵproperty("id", ɵɵinterpolate1("primaryAct-", ctx_r0.id))("p-label", ctx_r0.primaryLabel)("p-danger", ctx_r0.dangerPrimaryAction)("p-kind", ctx_r0.kindPrimaryAction || "tertiary")("p-disabled", ctx_r0.disabled)("p-size", ctx_r0.size);
    ɵɵadvance();
    ɵɵconditional(ctx_r0.secondaryLabel ? 3 : -1);
  }
}
function PoWidgetComponent_ng_template_12_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-tag", 36, 7);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("p-value", ctx_r0.tagLabel)("p-icon", ctx_r0.tagIcon)("p-type", ctx_r0.disabled ? null : ctx_r0.tagType ? ctx_r0.tagType : "success")("p-color", ctx_r0.disabled ? "#ECECEE" : null)("p-text-color", ctx_r0.disabled ? "#36364A" : null);
  }
}
var _c185 = ["titleBrand"];
function PoHeaderbrandComponent_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-button", 8, 1);
    ɵɵlistener("p-click", function PoHeaderbrandComponent_Conditional_1_Template_po_button_p_click_0_listener() {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.clickMenu.emit());
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("p-size", ctx_r1.size());
  }
}
function PoHeaderbrandComponent_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    const _r3 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-button", 9, 1);
    ɵɵlistener("p-click", function PoHeaderbrandComponent_Conditional_2_Template_po_button_p_click_0_listener() {
      ɵɵrestoreView(_r3);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.clickMenu.emit());
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("p-size", ctx_r1.size());
  }
}
function PoHeaderbrandComponent_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    const _r4 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 10);
    ɵɵlistener("click", function PoHeaderbrandComponent_Conditional_3_Template_div_click_0_listener() {
      ɵɵrestoreView(_r4);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onClickLogo());
    });
    ɵɵelement(1, "img", 11);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵproperty("src", ctx_r1.brand.logo, ɵɵsanitizeUrl);
  }
}
function PoHeaderbrandComponent_Conditional_4_Template(rf, ctx) {
  if (rf & 1) {
    const _r5 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 12);
    ɵɵlistener("click", function PoHeaderbrandComponent_Conditional_4_Template_div_click_0_listener() {
      ɵɵrestoreView(_r5);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onClickLogo());
    });
    ɵɵelement(1, "img", 11);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵproperty("src", (ctx_r1.brand == null ? null : ctx_r1.brand.smallLogo) || (ctx_r1.brand == null ? null : ctx_r1.brand.logo), ɵɵsanitizeUrl);
  }
}
var _c186 = ["buttonMoreElement"];
function PoHeaderMenuItemComponent_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "li", 6, 0);
    ɵɵlistener("click", function PoHeaderMenuItemComponent_Conditional_0_Template_li_click_0_listener() {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.openListButtonMore());
    })("keydown", function PoHeaderMenuItemComponent_Conditional_0_Template_li_keydown_0_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onKeyDownButtonMore($event));
    });
    ɵɵelementStart(3, "div", 7)(4, "span");
    ɵɵtext(5);
    ɵɵelementEnd();
    ɵɵelement(6, "po-icon", 8);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance(3);
    ɵɵclassProp("open", ctx_r1.poPopupElement == null ? null : ctx_r1.poPopupElement.showPopup);
    ɵɵattribute("aria-expanded", ctx_r1.poPopupElement == null ? null : ctx_r1.poPopupElement.showPopup);
    ɵɵadvance(2);
    ɵɵtextInterpolate(ctx_r1.literals.more);
    ɵɵadvance();
    ɵɵproperty("p-icon", (ctx_r1.poPopupElement == null ? null : ctx_r1.poPopupElement.showPopup) ? "ICON_ARROW_UP" : "ICON_ARROW_DOWN");
  }
}
function PoHeaderMenuItemComponent_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r3 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "li", 9, 1);
    ɵɵlistener("click", function PoHeaderMenuItemComponent_Conditional_1_Template_li_click_0_listener() {
      ɵɵrestoreView(_r3);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onAction(ctx_r1.item));
    })("keydown", function PoHeaderMenuItemComponent_Conditional_1_Template_li_keydown_0_listener($event) {
      ɵɵrestoreView(_r3);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onKeyDownButtonList($event, ctx_r1.item));
    });
    ɵɵelementStart(2, "div", 10)(3, "span");
    ɵɵtext(4);
    ɵɵelementEnd()()();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("p-tooltip", ctx_r1.item == null ? null : ctx_r1.item.label);
    ɵɵattribute("aria-current", ctx_r1.selectedItem);
    ɵɵadvance(2);
    ɵɵclassProp("submenu-item-selected", ctx_r1.selectedItem);
    ɵɵattribute("aria-selected", ctx_r1.item == null ? null : ctx_r1.item.$selected);
    ɵɵadvance(2);
    ɵɵtextInterpolate(ctx_r1.item == null ? null : ctx_r1.item.label);
  }
}
function PoHeaderMenuItemComponent_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    const _r4 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-popup", 11, 2);
    ɵɵlistener("p-click-item", function PoHeaderMenuItemComponent_Conditional_2_Template_po_popup_p_click_item_0_listener($event) {
      ɵɵrestoreView(_r4);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onAction($event, false));
    })("p-close", function PoHeaderMenuItemComponent_Conditional_2_Template_po_popup_p_close_0_listener() {
      ɵɵrestoreView(_r4);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onClosePopup());
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("p-size", ctx_r1.size())("p-actions", ctx_r1.itemOverFlow)("p-target", ctx_r1.buttonMoreElement)("p-hide-arrow", true);
  }
}
var _c187 = ["buttonActionElement"];
var _c188 = ["poPopupAction"];
var _c189 = ["poPopoverAction"];
var _forTrack1 = ($index, $item) => $item == null ? null : $item.label;
function PoHeaderToolsComponent_Conditional_1_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoHeaderToolsComponent_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, PoHeaderToolsComponent_Conditional_1_ng_container_0_Template, 1, 0, "ng-container", 6);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("ngTemplateOutlet", ctx_r0.headerTemplate);
  }
}
function PoHeaderToolsComponent_For_3_ng_template_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "i", 10);
    ɵɵelement(1, "po-badge", 11);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const action_r4 = ɵɵnextContext().$implicit;
    ɵɵclassMap((action_r4 == null ? null : action_r4.icon) || "an an-bell");
    ɵɵadvance();
    ɵɵproperty("p-value", action_r4 == null ? null : action_r4.badge);
  }
}
function PoHeaderToolsComponent_For_3_Conditional_7_ng_container_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoHeaderToolsComponent_For_3_Conditional_7_Template(rf, ctx) {
  if (rf & 1) {
    const _r6 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-popover", 12, 3);
    ɵɵlistener("p-close", function PoHeaderToolsComponent_For_3_Conditional_7_Template_po_popover_p_close_0_listener() {
      ɵɵrestoreView(_r6);
      const action_r4 = ɵɵnextContext().$implicit;
      return ɵɵresetView(action_r4 == null ? null : action_r4.onClose == null ? null : action_r4.onClose(action_r4 == null ? null : action_r4.label));
    })("p-open", function PoHeaderToolsComponent_For_3_Conditional_7_Template_po_popover_p_open_0_listener() {
      ɵɵrestoreView(_r6);
      const action_r4 = ɵɵnextContext().$implicit;
      return ɵɵresetView(action_r4 == null ? null : action_r4.onOpen == null ? null : action_r4.onOpen(action_r4 == null ? null : action_r4.label));
    });
    ɵɵtemplate(2, PoHeaderToolsComponent_For_3_Conditional_7_ng_container_2_Template, 1, 0, "ng-container", 6);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r6 = ɵɵnextContext();
    const action_r4 = ctx_r6.$implicit;
    const ɵ$index_8_r3 = ctx_r6.$index;
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("p-width", action_r4.popover.width)("p-target", ctx_r0.buttonActionElements == null ? null : ctx_r0.buttonActionElements.get(ɵ$index_8_r3))("p-hide-arrow", true)("p-corner-aligned", true);
    ɵɵadvance(2);
    ɵɵproperty("ngTemplateOutlet", action_r4.popover == null ? null : action_r4.popover.content);
  }
}
function PoHeaderToolsComponent_For_3_Template(rf, ctx) {
  if (rf & 1) {
    const _r2 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "li")(1, "po-button", 7, 0);
    ɵɵlistener("p-click", function PoHeaderToolsComponent_For_3_Template_po_button_p_click_1_listener() {
      const ɵ$index_8_r3 = ɵɵrestoreView(_r2).$index;
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.onClickAction(ɵ$index_8_r3));
    });
    ɵɵelementEnd();
    ɵɵtemplate(3, PoHeaderToolsComponent_For_3_ng_template_3_Template, 2, 3, "ng-template", null, 1, ɵɵtemplateRefExtractor);
    ɵɵelementStart(5, "po-popup", 8, 2);
    ɵɵlistener("p-close", function PoHeaderToolsComponent_For_3_Template_po_popup_p_close_5_listener() {
      const ctx_r4 = ɵɵrestoreView(_r2);
      const action_r4 = ctx_r4.$implicit;
      const ɵ$index_8_r3 = ctx_r4.$index;
      const ctx_r0 = ɵɵnextContext();
      ctx_r0.onClosePopup(ɵ$index_8_r3);
      return ɵɵresetView(action_r4 == null ? null : action_r4.onClose == null ? null : action_r4.onClose(action_r4 == null ? null : action_r4.label));
    })("p-open", function PoHeaderToolsComponent_For_3_Template_po_popup_p_open_5_listener() {
      const action_r4 = ɵɵrestoreView(_r2).$implicit;
      return ɵɵresetView(action_r4 == null ? null : action_r4.onOpen == null ? null : action_r4.onOpen(action_r4 == null ? null : action_r4.label));
    });
    ɵɵelementEnd();
    ɵɵconditionalCreate(7, PoHeaderToolsComponent_For_3_Conditional_7_Template, 3, 5, "po-popover", 9);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const action_r4 = ctx.$implicit;
    const ɵ$index_8_r3 = ctx.$index;
    const badge_r8 = ɵɵreference(4);
    const ctx_r0 = ɵɵnextContext();
    ɵɵclassProp("po-header-nav-item-small", ɵ$index_8_r3 !== ctx_r0.actionTools.length - 1);
    ɵɵadvance();
    ɵɵclassProp("po-header-selected-tool", ctx_r0.checkSelected(ɵ$index_8_r3));
    ɵɵproperty("p-tooltip", action_r4 == null ? null : action_r4.tooltip)("p-icon", (action_r4 == null ? null : action_r4.badge) ? badge_r8 : (action_r4 == null ? null : action_r4.icon) || "an an-dots-nine")("p-aria-expanded", ctx_r0.checkSelected(ɵ$index_8_r3))("p-aria-label", ctx_r0.getAriaLabel(action_r4))("p-size", ctx_r0.size());
    ɵɵadvance(4);
    ɵɵproperty("p-actions", action_r4 == null ? null : action_r4.items)("p-target", ctx_r0.buttonActionElements == null ? null : ctx_r0.buttonActionElements.get(ɵ$index_8_r3))("p-hide-arrow", true)("p-size", ctx_r0.size());
    ɵɵadvance(2);
    ɵɵconditional((action_r4 == null ? null : action_r4.popover) ? 7 : -1);
  }
}
function PoHeaderCustomerComponent_Conditional_9_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-badge", 11);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("p-status", ctx_r0.headerUser.status);
  }
}
function PoHeaderCustomerComponent_Conditional_13_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-badge", 11);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵproperty("p-status", ctx_r0.headerUser.status);
  }
}
function PoHeaderCustomerComponent_Conditional_14_Template(rf, ctx) {
  if (rf & 1) {
    const _r2 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-popup", 15, 1);
    ɵɵlistener("p-close", function PoHeaderCustomerComponent_Conditional_14_Template_po_popup_p_close_0_listener() {
      ɵɵrestoreView(_r2);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.headerUser == null ? null : ctx_r0.headerUser.onClose == null ? null : ctx_r0.headerUser.onClose());
    })("p-open", function PoHeaderCustomerComponent_Conditional_14_Template_po_popup_p_open_0_listener() {
      ɵɵrestoreView(_r2);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.headerUser == null ? null : ctx_r0.headerUser.onOpen == null ? null : ctx_r0.headerUser.onOpen());
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    const targetRef_r3 = ɵɵreference(2);
    ɵɵproperty("p-actions", ctx_r0.headerUser.items)("p-target", targetRef_r3)("p-hide-arrow", true)("p-size", ctx_r0.size());
  }
}
function PoHeaderCustomerComponent_Conditional_15_ng_container_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function PoHeaderCustomerComponent_Conditional_15_Template(rf, ctx) {
  if (rf & 1) {
    const _r4 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-popover", 16, 2);
    ɵɵlistener("p-close", function PoHeaderCustomerComponent_Conditional_15_Template_po_popover_p_close_0_listener() {
      ɵɵrestoreView(_r4);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.headerUser == null ? null : ctx_r0.headerUser.onClose == null ? null : ctx_r0.headerUser.onClose());
    })("p-open", function PoHeaderCustomerComponent_Conditional_15_Template_po_popover_p_open_0_listener() {
      ɵɵrestoreView(_r4);
      const ctx_r0 = ɵɵnextContext();
      return ɵɵresetView(ctx_r0.headerUser == null ? null : ctx_r0.headerUser.onOpen == null ? null : ctx_r0.headerUser.onOpen());
    });
    ɵɵtemplate(2, PoHeaderCustomerComponent_Conditional_15_ng_container_2_Template, 1, 0, "ng-container", 17);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    const targetRef_r3 = ɵɵreference(2);
    ɵɵproperty("p-width", ctx_r0.headerUser.popover.width)("p-target", targetRef_r3)("p-hide-arrow", true)("p-corner-aligned", true);
    ɵɵadvance(2);
    ɵɵproperty("ngTemplateOutlet", ctx_r0.headerUser.popover.content);
  }
}
var _c190 = ["buttonFirstAction"];
var _c191 = ["navElement"];
var _c192 = ["menuWrapperBrand"];
var _c193 = ["menuWrapperTools"];
var _c194 = ["overflowButton"];
var _c195 = ["menusubmenuitem"];
var _forTrack2 = ($index, $item) => $item.id;
function PoHeaderComponent_For_8_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-header-menu-item", 14, 4);
    ɵɵlistener("p-item-click", function PoHeaderComponent_For_8_Template_po_header_menu_item_p_item_click_0_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onSelected($event));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const item_r3 = ctx.$implicit;
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("p-item", item_r3)("p-size", ctx_r1.size());
  }
}
function PoHeaderComponent_Conditional_9_Template(rf, ctx) {
  if (rf & 1) {
    const _r4 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "po-header-menu-item", 15, 5);
    ɵɵlistener("p-item-click", function PoHeaderComponent_Conditional_9_Template_po_header_menu_item_p_item_click_0_listener($event) {
      ɵɵrestoreView(_r4);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onSelected($event));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("p-button-overflow", true)("p-item-overflow", ctx_r1.overflowItems)("p-size", ctx_r1.size());
  }
}
function PoHeaderComponent_Conditional_13_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "po-header-customer", 13);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("p-header-user", ctx_r1.headerUser)("p-size", ctx_r1.size());
  }
}
function PoHeaderComponent_Conditional_14_div_4_1_ng_template_0_Template(rf, ctx) {
}
function PoHeaderComponent_Conditional_14_div_4_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, PoHeaderComponent_Conditional_14_div_4_1_ng_template_0_Template, 0, 0, "ng-template");
  }
}
function PoHeaderComponent_Conditional_14_div_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div");
    ɵɵtemplate(1, PoHeaderComponent_Conditional_14_div_4_1_Template, 1, 0, null, 21);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", ctx_r1.menuHeaderTemplate == null ? null : ctx_r1.menuHeaderTemplate.templateRef);
  }
}
function PoHeaderComponent_Conditional_14_Template(rf, ctx) {
  if (rf & 1) {
    const _r5 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 16);
    ɵɵlistener("click", function PoHeaderComponent_Conditional_14_Template_div_click_0_listener() {
      ɵɵrestoreView(_r5);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onCloseMenu());
    });
    ɵɵelementEnd();
    ɵɵelementStart(1, "aside", 17)(2, "div", 18)(3, "po-menu", 19);
    ɵɵtemplate(4, PoHeaderComponent_Conditional_14_div_4_Template, 2, 1, "div", 20);
    ɵɵelementEnd()()();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵclassProp("show", ctx_r1.showMenu);
    ɵɵadvance();
    ɵɵclassProp("open", ctx_r1.showMenu);
    ɵɵadvance(2);
    ɵɵproperty("p-only-menu-item", true)("p-menus", ctx_r1.menuCollapseJoin)("p-filter", ctx_r1.filterMenu);
  }
}
var _c196 = (a0) => ({
  detail: a0
});
function PoHttpInterceptorDetailComponent_Conditional_2_For_2_ng_template_1_Template(rf, ctx) {
}
function PoHttpInterceptorDetailComponent_Conditional_2_For_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "po-accordion-item", 5);
    ɵɵtemplate(1, PoHttpInterceptorDetailComponent_Conditional_2_For_2_ng_template_1_Template, 0, 0, "ng-template", 4);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const detail_r1 = ctx.$implicit;
    const ctx_r1 = ɵɵnextContext(2);
    const body_r3 = ɵɵreference(5);
    ɵɵproperty("p-label", ctx_r1.formatDetailItemTitle(detail_r1));
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", body_r3)("ngTemplateOutletContext", ɵɵpureFunction1(3, _c196, detail_r1));
  }
}
function PoHttpInterceptorDetailComponent_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "po-accordion", 3);
    ɵɵrepeaterCreate(1, PoHttpInterceptorDetailComponent_Conditional_2_For_2_Template, 2, 5, "po-accordion-item", 5, ɵɵrepeaterTrackByIdentity);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵrepeater(ctx_r1.details);
  }
}
function PoHttpInterceptorDetailComponent_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0, 4);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    const body_r3 = ɵɵreference(5);
    ɵɵproperty("ngTemplateOutlet", body_r3)("ngTemplateOutletContext", ɵɵpureFunction1(3, _c196, ctx_r1.details[0] || ɵɵpureFunction0(2, _c11)));
  }
}
function PoHttpInterceptorDetailComponent_ng_template_4_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 6);
    ɵɵelement(1, "po-tag", 7);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const detail_r4 = ɵɵnextContext().detail;
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵproperty("p-color", ctx_r1.typeColor(detail_r4.type))("p-value", ctx_r1.typeValue(detail_r4.type));
  }
}
function PoHttpInterceptorDetailComponent_ng_template_4_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "p")(1, "strong");
    ɵɵtext(2);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const detail_r4 = ɵɵnextContext().detail;
    ɵɵadvance(2);
    ɵɵtextInterpolate(detail_r4.code);
  }
}
function PoHttpInterceptorDetailComponent_ng_template_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵconditionalCreate(0, PoHttpInterceptorDetailComponent_ng_template_4_Conditional_0_Template, 2, 2, "div", 6);
    ɵɵconditionalCreate(1, PoHttpInterceptorDetailComponent_ng_template_4_Conditional_1_Template, 3, 1, "p");
    ɵɵelementStart(2, "div", 2)(3, "p");
    ɵɵtext(4);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const detail_r4 = ctx.detail;
    ɵɵconditional(detail_r4.type ? 0 : -1);
    ɵɵadvance();
    ɵɵconditional(detail_r4.detailTitle ? 1 : -1);
    ɵɵadvance(3);
    ɵɵtextInterpolate(detail_r4.detailedMessage);
  }
}
var PoDividerSize;
(function(PoDividerSize2) {
  PoDividerSize2["small"] = "small";
  PoDividerSize2["medium"] = "medium";
  PoDividerSize2["large"] = "large";
})(PoDividerSize || (PoDividerSize = {}));
var PoDividerBaseComponent = class _PoDividerBaseComponent {
  coordinateX1;
  coordinateX2;
  _borderWidth = PoDividerSize.small;
  /** Valor do rótulo a ser exibido. */
  label;
  /**
   * @optional
   *
   * @description
   *
   * Define a espessura da linha.
   *
   * Valores válidos:
   * - small
   * - medium
   * - large
   *
   * @default `small`
   */
  set borderWidth(value) {
    this._borderWidth = PoDividerSize[value] ? PoDividerSize[value] : PoDividerSize.small;
    this.getCoordinates();
  }
  get borderWidth() {
    return this._borderWidth;
  }
  ngOnInit() {
    this.getCoordinates();
  }
  getCoordinates() {
    if (this.borderWidth === PoDividerSize.small) {
      this.coordinateX1 = "0.1%";
      this.coordinateX2 = "99.9%";
    } else if (this.borderWidth === PoDividerSize.medium) {
      this.coordinateX1 = "0.2%";
      this.coordinateX2 = "99.8%";
    } else {
      this.coordinateX1 = "0.3%";
      this.coordinateX2 = "99.7%";
    }
  }
  static ɵfac = function PoDividerBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoDividerBaseComponent)();
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoDividerBaseComponent,
    inputs: {
      label: [0, "p-label", "label"],
      borderWidth: [0, "p-border-width", "borderWidth"]
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoDividerBaseComponent, [{
    type: Directive
  }], null, {
    label: [{
      type: Input,
      args: ["p-label"]
    }],
    borderWidth: [{
      type: Input,
      args: ["p-border-width"]
    }]
  });
})();
var PoDividerComponent = class _PoDividerComponent extends PoDividerBaseComponent {
  static ɵfac = /* @__PURE__ */ (() => {
    let ɵPoDividerComponent_BaseFactory;
    return function PoDividerComponent_Factory(__ngFactoryType__) {
      return (ɵPoDividerComponent_BaseFactory || (ɵPoDividerComponent_BaseFactory = ɵɵgetInheritedFactory(_PoDividerComponent)))(__ngFactoryType__ || _PoDividerComponent);
    };
  })();
  static ɵcmp = ɵɵdefineComponent({
    type: _PoDividerComponent,
    selectors: [["po-divider"]],
    standalone: false,
    features: [ɵɵInheritDefinitionFeature],
    decls: 4,
    vars: 4,
    consts: [[1, "po-divider"], [1, "po-divider-label"], ["xmlns", "http://www.w3.org/2000/svg"], ["y1", "50%", "y2", "50%"]],
    template: function PoDividerComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "div", 0);
        ɵɵconditionalCreate(1, PoDividerComponent_Conditional_1_Template, 2, 1, "div", 1);
        ɵɵnamespaceSVG();
        ɵɵelementStart(2, "svg", 2);
        ɵɵelement(3, "line", 3);
        ɵɵelementEnd()();
      }
      if (rf & 2) {
        ɵɵattribute("p-size", ctx.borderWidth);
        ɵɵadvance();
        ɵɵconditional(ctx.label ? 1 : -1);
        ɵɵadvance(2);
        ɵɵattribute("x1", ctx.coordinateX1)("x2", ctx.coordinateX2);
      }
    },
    encapsulation: 2,
    changeDetection: 0
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoDividerComponent, [{
    type: Component,
    args: [{
      selector: "po-divider",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: '<div class="po-divider" [attr.p-size]="borderWidth">\n  @if (label) {\n    <div class="po-divider-label">{{ label }}</div>\n  }\n  <svg xmlns="http://www.w3.org/2000/svg">\n    <line [attr.x1]="coordinateX1" y1="50%" [attr.x2]="coordinateX2" y2="50%" />\n  </svg>\n</div>\n'
    }]
  }], null, null);
})();
var PoDividerModule = class _PoDividerModule {
  static ɵfac = function PoDividerModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoDividerModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoDividerModule,
    declarations: [PoDividerComponent],
    imports: [CommonModule],
    exports: [PoDividerComponent]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [CommonModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoDividerModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule],
      declarations: [PoDividerComponent],
      exports: [PoDividerComponent]
    }]
  }], null, null);
})();
var AnimaliaIconDictionary = {
  NAME_LIB: "AnimaliaIcon",
  ICON_AGRO_BUSINESS: "an an-tractor",
  ICON_ALIGN_CENTER: "an an-text-align-center",
  ICON_ALIGN_JUSTIFY: "an an-text-align-justify",
  ICON_ALIGN_LEFT: "an an-text-align-left",
  ICON_ALIGN_RIGHT: "an an-text-align-right",
  ICON_ARROW_ARC_LEFT: "an an-arrow-arc-left",
  ICON_ARROW_BACK: "an an-arrow-left",
  ICON_ARROW_DOWN: "an an-caret-down",
  ICON_OTHER_ARROW_DOWN: "an an-arrow-down",
  ICON_ARROW_LEFT: "an an-caret-left",
  ICON_ARROW_RIGHT: "an an-caret-right",
  ICON_ARROW_UP: "an an-caret-up",
  ICON_OTHER_ARROW_UP: "an an-arrow-up",
  ICON_CALENDAR: "an an-calendar-blank",
  ICON_CALENDAR_DOTS: "an an-calendar-dots",
  ICON_CART: "an an-shopping-cart",
  ICON_CLEAR_CONTENT: "an an-x-circle",
  ICON_FILL_CLEAR_CONTENT: "an-fill an-x-circle",
  ICON_CLOCK: "an an-clock",
  ICON_CLOSE: "an an-x",
  ICON_COPY: "an an-copy",
  ICON_DELETE: "an an-trash",
  ICON_DRAG: "an an-dots-six-vertical",
  ICON_EDIT: "an an-pencil-simple",
  ICON_EXCLAMATION: "an an-warning-circle",
  ICON_EXIT: "an an-sign-out",
  ICON_EXPAND: "an an-corners-out",
  ICON_EYE: "an an-eye",
  ICON_EYE_OFF: "an an-eye-closed",
  ICON_FILTER: "an an-funnel",
  ICON_FILL_FILTER: "an-fill an-funnel",
  ICON_FUNNEL: "an an-funnel-simple",
  ICON_FUNNEL_X: "an an-funnel-x",
  ICON_GAS_PUMP: "an an-gas-pump",
  ICON_HELP: "an an-question",
  ICON_INFO: "an an-info",
  ICON_LAST_PAGE: "an an-caret-double-right",
  ICON_LINK: "an an-link",
  ICON_LIST: "an an-list-dashes",
  ICON_LIST_BULLETS: "an an-list-bullets",
  ICON_LOCK: "an an-lock",
  ICON_MAGIC_WAND: "an an-magic-wand",
  ICON_MAIL: "an an-envelope-simple",
  ICON_MENU: "an an-list",
  ICON_MENU_CLOSE: "an an-caret-circle-left",
  ICON_MENU_OPEN: "an an-caret-circle-right",
  ICON_MINUS: "an an-minus",
  ICON_MORE: "an an-dots-three",
  ICON_MORE_VERT: "an an-dots-three-vertical",
  ICON_FILL_MORE_OUTLINE_VERT: "an-fill an-dots-three-outline-vertical",
  ICON_MORE_OUT_VERT_FILL: "an-fill an-dots-three-outline-vertical",
  ICON_NEWSPAPER: "an an-newspaper",
  ICON_NOTIFICATION: "an an-bell",
  ICON_OK: "an an-check",
  ICON_PARAMETERS: "an an-sliders-horizontal",
  ICON_PICTURE: "an an-image",
  ICON_PICTURE_BROKEN: "an an-image-broken",
  ICON_PLUS: "an an-plus",
  ICON_PROHIBIT: "an an-prohibit",
  ICON_PUSH_PIN: "an an-push-pin",
  ICON_PUSH_PIN_SLASH: "an an-push-pin-slash",
  ICON_REFRESH: "an an-arrow-clockwise",
  ICON_SEARCH: "an an-magnifying-glass",
  ICON_SETTINGS: "an an-gear-six",
  ICON_SIDEBAR: "an an-sidebar",
  ICON_SIDEBAR_SIMPLES: "an an-sidebar-simple",
  ICON_SORT: "an an-arrows-down-up",
  ICON_SORT_ASC: "an an-arrow-up",
  ICON_SORT_ASCENDING: "an an-sort-ascending",
  ICON_FILL_SORT_ASCENDING: "an-fill an-sort-ascending",
  ICON_SORT_DESC: "an an-arrow-down",
  ICON_SORT_DESCENDING: "an an-sort-descending",
  ICON_FILL_SORT_DESCENDING: "an-fill an-sort-descending",
  ICON_STAR: "an an-star",
  ICON_TELEPHONE: "an an-phone",
  ICON_TEXT_BOLD: "an an-text-b",
  ICON_TEXT_ITALIC: "an an-text-italic",
  ICON_TEXT_UNDERLINE: "an an-text-underline",
  ICON_UPLOAD_CLOUD: "an an-cloud-arrow-up",
  ICON_USER: "an an-user",
  ICON_WARNING: "an an-exclamation-mark",
  ICON_WORLD: "an an-globe-simple"
};
var PoIconComponent = class _PoIconComponent {
  iconElement;
  class;
  _icon;
  _iconToken;
  hostPIcon = null;
  constructor() {
    this._iconToken = AnimaliaIconDictionary;
  }
  /**
   * Define o ícone a ser exibido.
   *
   * É possível usar qualquer um dos ícones da [Biblioteca de ícones](https://po-ui.io/icons). conforme exemplo abaixo:
   * ```
   * <po-button p-icon="an an-user" p-label="PO button"></po-button>
   * ```
   * Também é possível utilizar outras fontes de ícones, por exemplo a biblioteca *Font Awesome*, da seguinte forma:
   * ```
   * <po-button p-icon="fa fa-podcast" p-label="PO button"></po-button>
   * ```
   * Outra opção seria a customização do ícone através do `TemplateRef`, conforme exemplo abaixo:
   * ```
   * <po-button [p-icon]="template" p-label="button template ionic"></po-button>
   *
   * <ng-template #template>
   *  <ion-icon style="font-size: inherit" name="heart"></ion-icon>
   * </ng-template>
   * ```
   * > Para o ícone enquadrar corretamente, deve-se utilizar `font-size: inherit` caso o ícone utilizado não aplique-o.
   */
  set icon(value) {
    if (typeof value === "string") {
      this.hostPIcon = value;
      this.processIcon(value);
    } else if (value instanceof TemplateRef) {
      this._icon = value;
      this.hostPIcon = null;
    }
  }
  get icon() {
    return this._icon;
  }
  addClasses(value, iconToken = false) {
    this.class = iconToken ? value : value.startsWith("po-icon-") ? this.class = `po-icon ${value}` : this.class = `po-fonts-icon ${value}`;
  }
  getIcon(iconName) {
    return this._iconToken.hasOwnProperty(iconName) ? this._iconToken[iconName].startsWith("po-icon ") ? this._iconToken[iconName] : "po-fonts-icon " + this._iconToken[iconName] : "";
  }
  processIcon(icon) {
    const iconToken = this.processIconTokens(icon);
    if (iconToken !== "") {
      this.addClasses(iconToken, true);
    } else {
      this.addClasses(icon);
    }
  }
  processIconTokens(value) {
    const iconTokens = this.splitIconNames(value);
    let icon = "";
    if (Array.isArray(iconTokens)) {
      iconTokens.map((iconName) => {
        icon += this.getIcon(iconName) !== "" ? " " + this.getIcon(iconName) : icon !== "" ? " " + iconName : iconName;
      });
    } else {
      icon = this.getIcon(iconTokens);
    }
    return icon.trim();
  }
  splitIconNames(iconName) {
    return iconName.includes(" ") ? iconName.split(" ") : iconName;
  }
  static ɵfac = function PoIconComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoIconComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoIconComponent,
    selectors: [["po-icon"]],
    viewQuery: function PoIconComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c02, 5);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.iconElement = _t.first);
      }
    },
    hostVars: 1,
    hostBindings: function PoIconComponent_HostBindings(rf, ctx) {
      if (rf & 2) {
        ɵɵattribute("p-icon", ctx.hostPIcon);
      }
    },
    inputs: {
      icon: [0, "p-icon", "icon"]
    },
    standalone: false,
    ngContentSelectors: _c12,
    decls: 2,
    vars: 1,
    consts: [["iconElement", ""], ["aria-hidden", "true", 3, "class"], [3, "ngTemplateOutlet"], ["aria-hidden", "true"]],
    template: function PoIconComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵprojectionDef();
        ɵɵconditionalCreate(0, PoIconComponent_Conditional_0_Template, 3, 2, "i", 1)(1, PoIconComponent_Conditional_1_Template, 1, 1, null, 2);
      }
      if (rf & 2) {
        ɵɵconditional(ctx.class ? 0 : 1);
      }
    },
    dependencies: [NgTemplateOutlet],
    encapsulation: 2,
    changeDetection: 0
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoIconComponent, [{
    type: Component,
    args: [{
      selector: "po-icon",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: '@if (class) {\n  <i #iconElement [class]="class" aria-hidden="true">\n    <ng-content></ng-content>\n  </i>\n} @else {\n  <ng-template [ngTemplateOutlet]="icon"></ng-template>\n}\n'
    }]
  }], () => [], {
    iconElement: [{
      type: ViewChild,
      args: ["iconElement", {
        static: false
      }]
    }],
    hostPIcon: [{
      type: HostBinding,
      args: ["attr.p-icon"]
    }],
    icon: [{
      type: Input,
      args: ["p-icon"]
    }]
  });
})();
var PoIconModule = class _PoIconModule {
  static ɵfac = function PoIconModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoIconModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoIconModule,
    declarations: [PoIconComponent],
    imports: [CommonModule],
    exports: [PoIconComponent]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [CommonModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoIconModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule],
      declarations: [PoIconComponent],
      exports: [PoIconComponent]
    }]
  }], null, null);
})();
var PoTagOrientation;
(function(PoTagOrientation2) {
  PoTagOrientation2["Horizontal"] = "horizontal";
  PoTagOrientation2["Vertical"] = "vertical";
})(PoTagOrientation || (PoTagOrientation = {}));
var PoTagType;
(function(PoTagType2) {
  PoTagType2["Danger"] = "danger";
  PoTagType2["Info"] = "info";
  PoTagType2["Success"] = "success";
  PoTagType2["Warning"] = "warning";
  PoTagType2["Neutral"] = "neutral";
})(PoTagType || (PoTagType = {}));
var PoDensityMode;
(function(PoDensityMode2) {
  PoDensityMode2["Small"] = "small";
  PoDensityMode2["Medium"] = "medium";
})(PoDensityMode || (PoDensityMode = {}));
var PoFieldSize;
(function(PoFieldSize2) {
  PoFieldSize2["Small"] = "small";
  PoFieldSize2["Medium"] = "medium";
})(PoFieldSize || (PoFieldSize = {}));
var poLanguageDefault = [{
  description: "English",
  language: "en"
}, {
  description: "Español",
  language: "es"
}, {
  description: "Português",
  language: "pt"
}, {
  description: "Pусский",
  language: "ru"
}];
var poLocales = poLanguageDefault.map((language) => language.language);
var poLocaleDefault = "pt";
var poLocaleDecimalSeparatorList = [{
  separator: ".",
  language: "en"
}, {
  separator: ",",
  language: "es"
}, {
  separator: ",",
  language: "pt"
}, {
  separator: ",",
  language: "ru"
}];
var poLocaleThousandSeparatorList = [{
  separator: ",",
  language: "en"
}, {
  separator: ".",
  language: "es"
}, {
  separator: ".",
  language: "pt"
}, {
  separator: " ",
  language: "ru"
}];
var poLocaleDateSeparatorList = [{
  separator: "/",
  locale: "en"
}, {
  separator: "/",
  locale: "es"
}, {
  separator: "/",
  locale: "pt"
}, {
  separator: ".",
  locale: "ru"
}];
var PoThemeA11yEnum;
(function(PoThemeA11yEnum2) {
  PoThemeA11yEnum2["AA"] = "AA";
  PoThemeA11yEnum2["AAA"] = "AAA";
})(PoThemeA11yEnum || (PoThemeA11yEnum = {}));
var PoTableColumnSpacing;
(function(PoTableColumnSpacing2) {
  PoTableColumnSpacing2["ExtraSmall"] = "extraSmall";
  PoTableColumnSpacing2["Small"] = "small";
  PoTableColumnSpacing2["Medium"] = "medium";
  PoTableColumnSpacing2["Large"] = "large";
})(PoTableColumnSpacing || (PoTableColumnSpacing = {}));
function formatBytes(bytes, decimals = 2) {
  if (!bytes) {
    return void 0;
  }
  const multiplier = 1024;
  const sizes = ["Bytes", "KB", "MB", "GB", "TB", "PB", "EB", "ZB", "YB"];
  const result = Math.floor(Math.log(bytes) / Math.log(multiplier));
  decimals = decimals < 0 ? 0 : decimals;
  return `${parseFloat((bytes / Math.pow(multiplier, result)).toFixed(decimals))} ${sizes[result]}`;
}
function getBrowserLanguage() {
  const language = navigator.language || navigator["userLanguage"];
  const shortLanguage = getShortLanguage(language);
  return poLocales.includes(shortLanguage) ? language : poLocaleDefault;
}
function getDefaultSizeFn(sizeEnum) {
  return getA11yDefaultSize() === "small" ? sizeEnum["Small"] : sizeEnum["Medium"];
}
function getShortBrowserLanguage() {
  return getShortLanguage(getBrowserLanguage());
}
function getShortLanguage(language) {
  return (language || poLocaleDefault).toLowerCase().substring(0, 2);
}
function isLanguage(value) {
  const languageRegex = new RegExp("^[a-z]{2}(-[a-z]{2})?$", "i");
  return languageRegex.test(value);
}
function reloadCurrentPage() {
  const currentUrl = window.location.origin + window.location.pathname;
  if (isValidUrl(currentUrl)) {
    window.location.assign(currentUrl);
  }
}
function isValidUrl(url, location = window.location) {
  return url === location.origin + location.pathname;
}
function convertToBoolean(val) {
  if (typeof val === "string") {
    val = val.toLowerCase().trim();
    return val === "true" || val === "on" || val === "";
  }
  if (typeof val === "number") {
    return val === 1;
  }
  return !!val;
}
function convertToInt(value, valueDefault) {
  const validNumber = parseInt(value, 10);
  const validDefaultValue = parseInt(valueDefault, 10);
  const defaultValue = validDefaultValue || validDefaultValue === 0 ? validDefaultValue : void 0;
  return validNumber || validNumber === 0 ? validNumber : defaultValue;
}
function isTypeof(object, type) {
  return typeof object === type;
}
function callFunction(fn, context, param) {
  if (isTypeof(fn, "function")) {
    fn.call(context, param);
  } else {
    context[fn](param);
  }
}
function convertIsoToDate(value, start2, end2) {
  if (value) {
    const day = parseInt(value.substring(8, 10), 10);
    const month = parseInt(value.substring(5, 7), 10);
    const year = parseInt(value.substring(0, 4), 10);
    if (start2) {
      const date = new Date(year, month - 1, day, 0, 0, 0);
      setYearFrom0To100(date, year);
      return date;
    } else if (end2) {
      const date = new Date(year, month - 1, day, 23, 59, 59);
      setYearFrom0To100(date, year);
      return date;
    } else {
      return new Date(year, month - 1, day);
    }
  }
}
function convertDateToISODate(date) {
  if (date) {
    const getMonth = date.getMonth() + 1;
    const day = date.getDate() < 10 ? "0" + date.getDate() : date.getDate();
    const month = getMonth < 10 ? "0" + getMonth : getMonth;
    const year = formatYear(date.getFullYear());
    return year + "-" + month + "-" + day;
  } else {
    return null;
  }
}
function convertDateToISOExtended(date, time) {
  if (date) {
    const getMonth = date.getMonth() + 1;
    const day = date.getDate() < 10 ? "0" + date.getDate() : date.getDate();
    const month = getMonth < 10 ? "0" + getMonth : getMonth;
    const year = formatYear(date.getFullYear());
    const dateString = date.toString();
    if (time !== null) {
      return year + "-" + month + "-" + day + time;
    } else {
      return year + "-" + month + "-" + day + "T" + dateString.substring(16, 24) + dateString.substring(28, 31) + ":" + dateString.substring(31, 33);
    }
  } else {
    return null;
  }
}
function formatYear(year) {
  if (year >= 1e3) {
    return year.toString();
  }
  if (year > 99 && year < 1e3) {
    return `0${year}`;
  }
  if (year > 9 && year < 100) {
    return `00${year}`;
  }
  if (year >= 0 && year < 10) {
    return `000${year}`;
  }
}
function isIEOrEdge() {
  const userAgent = window.navigator.userAgent;
  return /msie\s|trident\/|edge\//i.test(userAgent);
}
function isIE() {
  const userAgent = window.navigator.userAgent;
  return /msie\s|trident/i.test(userAgent);
}
function isFirefox() {
  const userAgent = window.navigator.userAgent;
  return userAgent.toLowerCase().indexOf("firefox") > -1;
}
function isSafari() {
  const userAgent = window.navigator.userAgent;
  return /^(?!.*chrome).*safari.*$/.test(userAgent.toLocaleLowerCase());
}
function isMobile() {
  const userAgent = window.navigator.userAgent;
  return userAgent.match(/Android|webOS|iPhone|iPad|iPod|BlackBerry|Windows Phone/i);
}
function isEquals(value, comparedValue) {
  return JSON.stringify(value) === JSON.stringify(comparedValue);
}
function isKeyCodeEnter(event) {
  return event.keyCode === 13 || event.which === 13;
}
function setYearFrom0To100(date, year) {
  if (year >= 0 && year < 100) {
    date.setFullYear(year);
  }
}
function sortOptionsByProperty(options, property) {
  options.sort((optionA, optionB) => {
    optionA = optionA[property].toString().toLowerCase();
    optionB = optionB[property].toString().toLowerCase();
    if (optionA < optionB) {
      return -1;
    }
    if (optionA > optionB) {
      return 1;
    }
    return 0;
  });
}
function sortFields(fields = [], defaultOrdering = -1) {
  const resultClassification = {
    fieldAComesFirst: -1,
    fieldAComesAfter: 1,
    keepPositions: 0
  };
  const isOrderValid = (order) => isTypeof(order, "number") && order > 0;
  const applyDefaultOrdering = (order) => isOrderValid(order) ? order : defaultOrdering;
  return fields.sort((fieldA, fieldB) => {
    const orderA = applyDefaultOrdering(fieldA.order);
    const orderB = applyDefaultOrdering(fieldB.order);
    if (orderA === orderB) {
      return resultClassification.keepPositions;
    }
    if (orderA === defaultOrdering) {
      return resultClassification.fieldAComesAfter;
    }
    if (orderB === defaultOrdering) {
      return resultClassification.fieldAComesFirst;
    }
    return orderA - orderB;
  });
}
function removeDuplicatedOptions(list) {
  for (let i2 = 0; i2 < list.length; i2++) {
    if (i2 === 0) {
      continue;
    }
    if (list.findIndex((op) => op.value === list[i2].value) !== i2) {
      list.splice(i2, 1);
      i2--;
    }
  }
}
function removeDuplicatedOptionsWithFieldValue(list, newValue) {
  for (let i2 = 0; i2 < list.length; i2++) {
    if (i2 === 0) {
      continue;
    }
    if (list.findIndex((op) => op[newValue] === list[i2][newValue]) !== i2) {
      list.splice(i2, 1);
      i2--;
    }
  }
}
function removeUndefinedAndNullOptions(list) {
  for (let i2 = 0; i2 < list.length; i2++) {
    if (list[i2].value === void 0 || list[i2].value === null) {
      list.splice(i2, 1);
      i2--;
    }
  }
}
function removeUndefinedAndNullOptionsWithFieldValue(list, newValue) {
  for (let i2 = 0; i2 < list.length; i2++) {
    if (list[i2][newValue] === void 0 || list[i2][newValue] === null) {
      list.splice(i2, 1);
      i2--;
    }
  }
}
function validValue(value) {
  return value !== null && value !== void 0 && value !== "" || value === false;
}
function isExternalLink(url) {
  return url ? url.startsWith("http") : false;
}
function openExternalLink(url) {
  window.open(url, "_blank");
}
function getFormattedLink(link) {
  let formattedLink = "";
  if (link) {
    formattedLink = link.replace(/^(\.)+/g, "");
  }
  if (!formattedLink.startsWith("/")) {
    formattedLink = "/".concat(formattedLink);
  }
  return formattedLink;
}
function sortValues(leftSide, rightSide, ascending = true) {
  const left = isTypeof(leftSide, "string") ? leftSide.toLowerCase() : leftSide;
  const right = isTypeof(rightSide, "string") ? rightSide.toLowerCase() : rightSide;
  const leftIsInvalid = left === null || left === void 0 || Number.isNaN(left);
  const rightIsInvalid = right === null || right === void 0 || Number.isNaN(right);
  if (ascending) {
    if (left < right || leftIsInvalid) {
      return -1;
    } else if (left > right || rightIsInvalid) {
      return 1;
    }
  } else if (ascending === false) {
    if (left < right || leftIsInvalid) {
      return 1;
    } else if (left > right || rightIsInvalid) {
      return -1;
    }
  }
  return 0;
}
function validateDateRange(date, dateStart, dateEnd) {
  if (dateStart && dateEnd) {
    return date >= dateStart && date <= dateEnd;
  } else if (dateStart && !dateEnd) {
    return date >= dateStart;
  } else if (!dateStart && dateEnd) {
    return date <= dateEnd;
  } else {
    return true;
  }
}
function uuid() {
  function hex4() {
    return Math.floor((1 + Math.random()) * 65536).toString(16).substring(1);
  }
  return hex4() + hex4() + "-" + hex4() + "-" + hex4() + "-" + hex4() + "-" + hex4() + hex4() + hex4();
}
function capitalizeFirstLetter(text) {
  return `${text.charAt(0).toUpperCase()}${text.slice(1)}`;
}
function convertImageToBase64(file) {
  return new Promise((resolve, reject) => {
    const reader = new FileReader();
    reader.readAsDataURL(file);
    reader.onload = () => resolve(reader.result);
    reader.onerror = (error2) => reject(error2);
  });
}
function isValidImageBase64(value) {
  try {
    return !!value.match(/^data:image\/([a-zA-Z]*);base64,([^\"]*)$/);
  } catch (err) {
    return false;
  }
}
function validateObjectType(value) {
  return isTypeof(value, "object") && !Array.isArray(value) ? value : void 0;
}
function getFocusableElements(parentElement) {
  const focusableElements = 'button:not([disabled]), [href], input, select, textarea, [tabindex]:not([tabindex="-1"]';
  return parentElement.querySelectorAll(focusableElements);
}
function replaceFormatSeparator(format2, separator) {
  let newFormat = format2;
  if (separator !== "/") {
    newFormat = newFormat.replace(/\//g, separator);
  }
  return newFormat;
}
function sortArrayOfObjects(items, key2, isAscendingOrder) {
  if (items) {
    return items.sort((a, b) => {
      const valueA = a[key2];
      const valueB = b[key2];
      if (typeof valueA === "number" && typeof valueB === "number") {
        return isAscendingOrder ? valueA - valueB : valueB - valueA;
      } else {
        const compareResult = valueA.toString().localeCompare(valueB.toString());
        return isAscendingOrder ? compareResult : -compareResult;
      }
    });
  }
}
function validateSizeFn(value, sizeEnum) {
  const validSizes = Object.values(sizeEnum);
  if (value && validSizes.includes(value)) {
    if (value === sizeEnum["Small"] && getA11yLevel() === PoThemeA11yEnum.AAA) {
      return sizeEnum["Medium"];
    }
    return value;
  }
  return getDefaultSizeFn(sizeEnum);
}
function getA11yDefaultSize() {
  const defaultSize = document.documentElement.getAttribute("data-default-size") || localStorage.getItem("po-default-size");
  const a11yLevel = document.documentElement.getAttribute("data-a11y");
  return defaultSize === PoFieldSize.Small && a11yLevel === PoThemeA11yEnum.AA ? PoFieldSize.Small : PoFieldSize.Medium;
}
function getA11yLevel() {
  const a11yLevel = document.documentElement.getAttribute("data-a11y");
  if (a11yLevel === PoThemeA11yEnum.AA) {
    return PoThemeA11yEnum.AA;
  }
  localStorage.setItem("po-default-size", PoFieldSize.Medium);
  return PoThemeA11yEnum.AAA;
}
function getDensityMode() {
  const densityMode = document.documentElement.getAttribute("po-density-mode");
  if (densityMode === PoDensityMode.Small) {
    return PoDensityMode.Small;
  }
  return PoDensityMode.Medium;
}
function setHelperSettings(label, additionalHelpTooltip, poHelperComponent, size, onClick) {
  const baseSize = size ?? poHelperComponent?.size ?? "medium";
  const resolvedSize = baseSize === "large" ? "medium" : baseSize;
  const defaulWithoutLabel = {
    hideAdditionalHelp: false,
    helperSettings: null
  };
  if (onClick) {
    return {
      hideAdditionalHelp: true,
      helperSettings: {
        type: "help",
        eventOnClick: onClick,
        size: resolvedSize
      }
    };
  }
  if (additionalHelpTooltip && !poHelperComponent) {
    return {
      hideAdditionalHelp: true,
      helperSettings: {
        content: additionalHelpTooltip,
        type: "help",
        size: resolvedSize
      }
    };
  }
  if (poHelperComponent) {
    if (poHelperComponent.eventOnClick) {
      return {
        hideAdditionalHelp: true,
        helperSettings: {
          type: poHelperComponent.type || "help",
          eventOnClick: poHelperComponent.eventOnClick,
          size: resolvedSize
        }
      };
    }
    if (typeof poHelperComponent === "string" && poHelperComponent.trim() !== "") {
      return {
        hideAdditionalHelp: true,
        helperSettings: {
          content: poHelperComponent,
          size: resolvedSize
        }
      };
    }
    return {
      hideAdditionalHelp: true,
      helperSettings: __spreadProps(__spreadValues({}, poHelperComponent), {
        size: resolvedSize
      })
    };
  }
  return defaulWithoutLabel;
}
function updateTooltip(isTooltipActive, labelElement) {
  const el = getMeasurableEl(labelElement);
  if (!el) return false;
  const isEllipsed = el.scrollWidth > el.clientWidth;
  if (isTooltipActive !== isEllipsed) {
    return isEllipsed;
  } else {
    return isTooltipActive;
  }
}
function getMeasurableEl(labelElement) {
  const host = labelElement?.nativeElement;
  if (!host) return null;
  const inner25 = host.querySelector(".po-label, label, .po-label-title, .po-field-title .po-checkbox-label");
  return inner25 ?? host;
}
function getTextColor(type) {
  const isLightTheme = !document.documentElement.className.includes("-dark-");
  let token = "";
  if (type === "lightest") {
    token = isLightTheme ? "--color-neutral-light-00" : "--color-neutral-dark-95";
  } else {
    token = isLightTheme ? "--color-neutral-dark-95" : "--color-neutral-light-00";
  }
  return getComputedStyle(document.documentElement).getPropertyValue(token);
}
function getTextColorFromBackgroundColor(backgroundColor2) {
  const rgbValues = backgroundColor2.replaceAll(/(?:^rgba?\(|\s+|\)$)/g, "").split(",").map((value) => Number.parseInt(value, 10));
  const r = rgbValues[0];
  const g = rgbValues[1];
  const b = rgbValues[2];
  const yiq = (r * 299 + g * 587 + b * 114) / 1e3;
  return yiq >= 128 ? getTextColor("darkest") : getTextColor("lightest");
}
function mapInputSizeToLoadingIcon(size) {
  const sizeMap = {
    small: "xs",
    medium: "sm"
  };
  return sizeMap[size ?? ""] || "sm";
}
var PoUtils = {
  capitalizeFirstLetter,
  convertDateToISODate,
  convertDateToISOExtended,
  convertImageToBase64,
  convertToBoolean,
  convertToInt,
  formatYear,
  getA11yLevel,
  getBrowserLanguage,
  getDensityMode,
  getShortBrowserLanguage,
  isExternalLink,
  isFirefox,
  isIE,
  isIEOrEdge,
  isKeyCodeEnter,
  isLanguage,
  isMobile,
  isTypeof,
  openExternalLink,
  reloadCurrentPage,
  removeDuplicatedOptions,
  removeDuplicatedOptionsWithFieldValue,
  removeUndefinedAndNullOptions,
  removeUndefinedAndNullOptionsWithFieldValue,
  replaceFormatSeparator,
  setYearFrom0To100,
  sortOptionsByProperty,
  sortValues,
  uuid,
  validateDateRange
};
var PO_TABLE_ROW_HEIGHT_BY_SPACING = {
  [PoTableColumnSpacing.ExtraSmall]: 32,
  [PoTableColumnSpacing.Small]: 40,
  [PoTableColumnSpacing.Medium]: 48,
  [PoTableColumnSpacing.Large]: 56
};
var poDefaultLanguage = "PO_DEFAULT_LANGUAGE";
var poLocaleKey = "PO_USER_LOCALE";
var PoLanguageService = class _PoLanguageService {
  constructor() {
  }
  set languageDefault(language) {
    if (language && PoUtils.isLanguage(language)) {
      localStorage.setItem(poDefaultLanguage, language);
    }
  }
  get languageDefault() {
    return localStorage.getItem(poDefaultLanguage);
  }
  /**
   * @description
   *
   * Método responsável por retornar o idioma ativo.
   *
   * A busca do idioma será feita na seguinte ordem:
   *
   *   1 - o idioma que foi armazenado no *localStorage*, através do método `setLanguage()` utilizado pelo i18n.
   *
   *   2 - o valor inserido nas configurações do módulo do i18n através do parâmetro `config`, sendo o idioma inserido
   * na propriedade `language` da interface `PoI18nConfigDefault`.
   *
   *   3 - o idioma do navegador utilizado.
   *
   * > Caso o idioma do navegador não seja suportado pelo PO (`pt`, `en`, `es` ou `ru`), será retornado valor `pt`.
   *
   * **Retorno:** `string` com a sigla do idioma ativo.
   */
  getLanguage() {
    const language = localStorage.getItem(poLocaleKey) || this.languageDefault || PoUtils.getBrowserLanguage();
    return language && language.toLowerCase();
  }
  /**
   * @description
   *
   * Método responsável por retornar o idioma *default* da aplicação definido nas configurações do módulo do i18n através
   * do parâmetro `config`.
   *
   * **Retorno:** `string` com a sigla do idioma *default*.
   */
  getLanguageDefault() {
    return this.languageDefault;
  }
  /**
   * @description
   *
   * Método responsável por retornar a abreviação do idioma ativo na aplicação.
   *
   * @default `pt`
   *
   * **Retorno:** `string` com a sigla abreviada do idioma ativo.
   */
  getShortLanguage() {
    const language = this.getLanguage();
    const shortLanguage = getShortLanguage(language);
    return poLocales.includes(shortLanguage) ? shortLanguage : poLocaleDefault;
  }
  /**
   * @description
   *
   * Método para salvar o idioma da aplicação no *storage*, utilizado pelo serviço do i18n.
   *
   * > Ao definir um idioma por este método, todos os módulos da aplicação utilizarão o idioma definido.
   *
   * @param language sigla do idioma.
   *
   * Esta sigla deve ser composta por duas letras representando o idioma,
   * podendo ser adicionado outras duas letras representando o país, por exemplo: `pt`, `pt-BR`, `pt-br`, `en` ou `en-US`.
   *
   * > Caso seja informado um valor diferente deste padrão, o mesmo será ignorado.
   */
  setLanguage(language) {
    if (!PoUtils.isLanguage(language)) {
      return;
    }
    localStorage.setItem(poLocaleKey, language.toLowerCase());
  }
  /**
   * @description
   *
   * Método que define o idioma configurado a partir do parâmetro `config` utilizado pelo módulo do i18n.
   *
   * > Ao definir um idioma por este serviço, apenas o módulo do i18n referente a esta configuração utilizará o idioma definido.
   *
   * @param language sigla do idioma.
   *
   * Esta sigla deve ser composta por duas letras representando o idioma,
   * podendo ser adicionado outras duas letras representando o país, por exemplo: `pt`, `pt-BR`, `pt-br`, `en` ou `en-US`.
   *
   * > Caso seja informado um valor diferente deste padrão, o mesmo será ignorado.
   */
  setLanguageDefault(language) {
    this.languageDefault = language;
  }
  /**
   * @description
   *
   * Método que retorna o separador
   *
   * @param language sigla do idioma.
   *
   * Esta sigla deve ser composta por duas letras representando o idioma
   *
   * > Caso seja informado um valor diferente deste padrão, o mesmo será ignorado.
   */
  getNumberSeparators(language) {
    language = language || this.getShortLanguage();
    const decimal = poLocaleDecimalSeparatorList.find((separator) => separator.language === language) ?? {};
    const thousand = poLocaleThousandSeparatorList.find((separator) => separator.language === language) ?? {};
    const decimalSeparator = decimal.separator ?? ",";
    const thousandSeparator = thousand.separator ?? ".";
    return {
      decimalSeparator,
      thousandSeparator
    };
  }
  /**
   * @description
   *
   * Método que retorna o separador de datas
   *
   * @param language sigla do idioma.
   *
   * Esta sigla deve ser composta por duas letras representando o idioma
   *
   * > Caso seja informado um valor diferente deste padrão, o mesmo será ignorado.
   */
  getDateSeparator(language) {
    language = language || this.getShortLanguage();
    const separatorChar = poLocaleDateSeparatorList.find((separator) => separator.locale === language) ?? {};
    return separatorChar.separator ?? "/";
  }
  static ɵfac = function PoLanguageService_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoLanguageService)();
  };
  static ɵprov = ɵɵdefineInjectable({
    token: _PoLanguageService,
    factory: _PoLanguageService.ɵfac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoLanguageService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [], null);
})();
var PoTagIcon;
(function(PoTagIcon2) {
  PoTagIcon2["Danger"] = "ICON_CLOSE";
  PoTagIcon2["Info"] = "ICON_INFO";
  PoTagIcon2["Success"] = "ICON_OK";
  PoTagIcon2["Warning"] = "ICON_WARNING";
})(PoTagIcon || (PoTagIcon = {}));
var PoCaptionTagColorEnum;
(function(PoCaptionTagColorEnum2) {
  PoCaptionTagColorEnum2["CaptionTag01"] = "caption-tag-01";
  PoCaptionTagColorEnum2["CaptionTag02"] = "caption-tag-02";
  PoCaptionTagColorEnum2["CaptionTag03"] = "caption-tag-03";
  PoCaptionTagColorEnum2["CaptionTag04"] = "caption-tag-04";
  PoCaptionTagColorEnum2["CaptionTag05"] = "caption-tag-05";
  PoCaptionTagColorEnum2["CaptionTag06"] = "caption-tag-06";
  PoCaptionTagColorEnum2["CaptionTag07"] = "caption-tag-07";
  PoCaptionTagColorEnum2["CaptionTag08"] = "caption-tag-08";
  PoCaptionTagColorEnum2["CaptionTag09"] = "caption-tag-09";
  PoCaptionTagColorEnum2["CaptionTag10"] = "caption-tag-10";
  PoCaptionTagColorEnum2["CaptionTag11"] = "caption-tag-11";
  PoCaptionTagColorEnum2["CaptionTag12"] = "caption-tag-12";
  PoCaptionTagColorEnum2["CaptionTag13"] = "caption-tag-13";
  PoCaptionTagColorEnum2["CaptionTag14"] = "caption-tag-14";
  PoCaptionTagColorEnum2["CaptionTag15"] = "caption-tag-15";
  PoCaptionTagColorEnum2["CaptionTag16"] = "caption-tag-16";
  PoCaptionTagColorEnum2["CaptionTag17"] = "caption-tag-17";
  PoCaptionTagColorEnum2["CaptionTag18"] = "caption-tag-18";
  PoCaptionTagColorEnum2["CaptionTag19"] = "caption-tag-19";
  PoCaptionTagColorEnum2["CaptionTag20"] = "caption-tag-20";
  PoCaptionTagColorEnum2["CaptionTag21"] = "caption-tag-21";
  PoCaptionTagColorEnum2["CaptionTag22"] = "caption-tag-22";
  PoCaptionTagColorEnum2["CaptionTag23"] = "caption-tag-23";
  PoCaptionTagColorEnum2["CaptionTag24"] = "caption-tag-24";
  PoCaptionTagColorEnum2["CaptionTag25"] = "caption-tag-25";
  PoCaptionTagColorEnum2["CaptionTag26"] = "caption-tag-26";
  PoCaptionTagColorEnum2["CaptionTag27"] = "caption-tag-27";
  PoCaptionTagColorEnum2["CaptionTag28"] = "caption-tag-28";
  PoCaptionTagColorEnum2["CaptionTag29"] = "caption-tag-29";
  PoCaptionTagColorEnum2["CaptionTag30"] = "caption-tag-30";
  PoCaptionTagColorEnum2["CaptionTag31"] = "caption-tag-31";
  PoCaptionTagColorEnum2["CaptionTag32"] = "caption-tag-32";
  PoCaptionTagColorEnum2["CaptionTag33"] = "caption-tag-33";
  PoCaptionTagColorEnum2["CaptionTag34"] = "caption-tag-34";
  PoCaptionTagColorEnum2["CaptionTag35"] = "caption-tag-35";
})(PoCaptionTagColorEnum || (PoCaptionTagColorEnum = {}));
var PoColorPaletteEnum;
(function(PoColorPaletteEnum2) {
  PoColorPaletteEnum2["Color01"] = "color-01";
  PoColorPaletteEnum2["Color02"] = "color-02";
  PoColorPaletteEnum2["Color03"] = "color-03";
  PoColorPaletteEnum2["Color04"] = "color-04";
  PoColorPaletteEnum2["Color05"] = "color-05";
  PoColorPaletteEnum2["Color06"] = "color-06";
  PoColorPaletteEnum2["Color07"] = "color-07";
  PoColorPaletteEnum2["Color08"] = "color-08";
  PoColorPaletteEnum2["Color09"] = "color-09";
  PoColorPaletteEnum2["Color10"] = "color-10";
  PoColorPaletteEnum2["Color11"] = "color-11";
  PoColorPaletteEnum2["Color12"] = "color-12";
})(PoColorPaletteEnum || (PoColorPaletteEnum = {}));
var poTagColors = [...Object.values(PoColorPaletteEnum), ...Object.values(PoCaptionTagColorEnum)];
var poTagTextColors = Object.values(PoColorPaletteEnum);
var poTagOrientationDefault = PoTagOrientation.Vertical;
var PoTagLiteralsDefault = {
  en: {
    remove: "Clear"
  },
  es: {
    remove: "Eliminar"
  },
  pt: {
    remove: "Remover"
  },
  ru: {
    remove: "удалять"
  }
};
var PoTagBaseComponent = class _PoTagBaseComponent {
  /**
   * @optional
   *
   * @description
   *
   * Define uma legenda que será exibida acima ou ao lado da *tag*, de acordo com a `p-orientation`.
   */
  label;
  /**
   * @optional
   *
   * @description
   *
   * Habilita a opção de remover a tag
   *
   * @default `false`
   */
  removable = false;
  /**
   * @optional
   *
   * @description
   *
   * Desabilita o `po-tag` e não permite que o usuário interaja com o mesmo.
   * > A propriedade `p-disabled` somente terá efeito caso a propriedade `p-removable` esteja definida como `true`.
   *
   * @default `false`
   */
  disabled = false;
  /** Texto da tag. */
  value;
  //input interno para o multiselect
  appendInBody;
  /**
   * @optional
   *
   * @description
   *
   * Ação que será executada ao clicar sobre o `po-tag` e que receberá como parâmetro um objeto contendo o seu valor e tipo.
   *
   * O evento de click só funciona se a tag não for removível.
   */
  click = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Ação que sera executada quando clicar sobre o ícone de remover no `po-tag`
   */
  remove = new EventEmitter();
  poTagOrientation = PoTagOrientation;
  customColor;
  customTextColor;
  _color;
  _textColor;
  _icon;
  _orientation = poTagOrientationDefault;
  _type;
  _literals;
  language;
  /**
   * @optional
   *
   * @description
   *
   * Determina a cor da tag. As maneiras de customizar as cores são:
   * - Hexadeximal, por exemplo `#c64840`;
   * - RGB, como `rgb(0, 0, 165)`;
   * - O nome da cor, por exemplo `blue`;
   * - Usando uma das cores do tema do PO:
   * Valores válidos:
   *   - <span class="dot po-color-01"></span> `color-01`
   *   - <span class="dot po-color-02"></span> `color-02`
   *   - <span class="dot po-color-03"></span> `color-03`
   *   - <span class="dot po-color-04"></span> `color-04`
   *   - <span class="dot po-color-05"></span> `color-05`
   *   - <span class="dot po-color-06"></span> `color-06`
   *   - <span class="dot po-color-07"></span> `color-07`
   *   - <span class="dot po-color-08"></span> `color-08`
   *   - <span class="dot po-color-09"></span> `color-09`
   *   - <span class="dot po-color-10"></span> `color-10`
   *   - <span class="dot po-color-11"></span> `color-11`
   *   - <span class="dot po-color-12"></span> `color-12`
   *
   * > Também é possível utilizar as 35 cores da paleta **Caption Tag Colors**:
   *
   *   - <span class="dot po-caption-tag-01"></span> `caption-tag-01` <span class="dot po-caption-tag-02"></span> `caption-tag-02` <span class="dot po-caption-tag-03"></span> `caption-tag-03` <span class="dot po-caption-tag-04"></span> `caption-tag-04` <span class="dot po-caption-tag-05"></span> `caption-tag-05`
   *   - <span class="dot po-caption-tag-06"></span> `caption-tag-06` <span class="dot po-caption-tag-07"></span> `caption-tag-07` <span class="dot po-caption-tag-08"></span> `caption-tag-08` <span class="dot po-caption-tag-09"></span> `caption-tag-09` <span class="dot po-caption-tag-10"></span> `caption-tag-10`
   *   - <span class="dot po-caption-tag-11"></span> `caption-tag-11` <span class="dot po-caption-tag-12"></span> `caption-tag-12` <span class="dot po-caption-tag-13"></span> `caption-tag-13` <span class="dot po-caption-tag-14"></span> `caption-tag-14` <span class="dot po-caption-tag-15"></span> `caption-tag-15`
   *   - <span class="dot po-caption-tag-16"></span> `caption-tag-16` <span class="dot po-caption-tag-17"></span> `caption-tag-17` <span class="dot po-caption-tag-18"></span> `caption-tag-18` <span class="dot po-caption-tag-19"></span> `caption-tag-19` <span class="dot po-caption-tag-20"></span> `caption-tag-20`
   *   - <span class="dot po-caption-tag-21"></span> `caption-tag-21` <span class="dot po-caption-tag-22"></span> `caption-tag-22` <span class="dot po-caption-tag-23"></span> `caption-tag-23` <span class="dot po-caption-tag-24"></span> `caption-tag-24` <span class="dot po-caption-tag-25"></span> `caption-tag-25`
   *   - <span class="dot po-caption-tag-26"></span> `caption-tag-26` <span class="dot po-caption-tag-27"></span> `caption-tag-27` <span class="dot po-caption-tag-28"></span> `caption-tag-28` <span class="dot po-caption-tag-29"></span> `caption-tag-29` <span class="dot po-caption-tag-30"></span> `caption-tag-30`
   *   - <span class="dot po-caption-tag-31"></span> `caption-tag-31` <span class="dot po-caption-tag-32"></span> `caption-tag-32` <span class="dot po-caption-tag-33"></span> `caption-tag-33` <span class="dot po-caption-tag-34"></span> `caption-tag-34` <span class="dot po-caption-tag-35"></span> `caption-tag-35`
   *
   * Exemplo de uso:
   * ```
   * <po-tag p-color="caption-tag-15" p-value="Status"></po-tag>
   * ```
   *
   * - Para uma melhor acessibilidade no uso do componente é recomendável utilizar cores com um melhor contraste em relação ao background;
   * - Para as cores legacy (`color-01` a `color-12`) e cores customizadas, o componente ajusta automaticamente a cor do texto para garantir legibilidade.
   * - Para as cores **Caption Tag Colors** (`caption-tag-01` a `caption-tag-35`), a cor do texto é fixa e definida via token CSS, não sendo possível alterá-la via `p-text-color`.
   *
   * > **Atenção:** A propriedade `p-type` sobrepõe esta definição.
   */
  set color(value) {
    this._color = poTagColors.includes(value) ? value : void 0;
    if (this._color === void 0) {
      CSS.supports("color", value) ? this.customColor = value : this.customColor = void 0;
    }
  }
  get color() {
    return this._color;
  }
  /**
   * @optional
   *
   * @description
   *
   * Determina a cor do texto da tag. As maneiras de customizar as cores são:
   * - Hexadeximal, por exemplo `#c64840`;
   * - RGB, como `rgb(0, 0, 165)`;
   * - O nome da cor, por exemplo `blue`;
   * - Usando uma das cores do tema do PO:
   * Valores válidos:
   *   - <span class="dot po-color-01"></span> `color-01`
   *   - <span class="dot po-color-02"></span> `color-02`
   *   - <span class="dot po-color-03"></span> `color-03`
   *   - <span class="dot po-color-04"></span> `color-04`
   *   - <span class="dot po-color-05"></span> `color-05`
   *   - <span class="dot po-color-06"></span> `color-06`
   *   - <span class="dot po-color-07"></span> `color-07`
   *   - <span class="dot po-color-08"></span> `color-08`
   *   - <span class="dot po-color-09"></span> `color-09`
   *   - <span class="dot po-color-10"></span> `color-10`
   *   - <span class="dot po-color-11"></span> `color-11`
   *   - <span class="dot po-color-12"></span> `color-12`
   *
   * - Para uma melhor acessibilidade no uso do componente é recomendável utilizar cores com um melhor contraste em relação ao background.
   *
   * > **Atenção:** A propriedade `p-type` sobrepõe esta definição.
   *
   * > **Atenção:** As cores da paleta **Caption Tag Colors** (`caption-tag-01` a `caption-tag-35`) não são aceitas nesta propriedade,
   * pois possuem cor de texto fixa definida via token CSS.
   */
  set textColor(value) {
    this._textColor = poTagTextColors.includes(value) ? value : void 0;
    if (this._textColor === void 0) {
      CSS.supports("color", value) ? this.customTextColor = value : this.customTextColor = void 0;
    }
  }
  get textColor() {
    return this._textColor;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define ou ativa um ícone que será exibido ao lado do valor da *tag*.
   *
   * Quando `p-type` estiver definida, basta informar um valor igual a `true` para que o ícone seja exibido conforme descrições abaixo:
   * - <span class="an an-check"></span> - `success`
   * - <span class="an an-warning-circle"></span> - `warning`
   * - <span class="an an-x"></span> - `danger`
   * - <span class="an an-info"></span> - `info`
   *
   * Também É possível usar qualquer um dos ícones da [Biblioteca de ícones](https://po-ui.io/icons). conforme exemplo abaixo:
   * ```
   * <po-tag p-icon="an an-user" p-value="PO Tag"></po-tag>
   * ```
   * como também utilizar outras fontes de ícones, por exemplo a biblioteca *Font Awesome*, da seguinte forma:
   * ```
   * <po-tag p-icon="fa fa-podcast" p-value="PO Tag"></po-button>
   * ```
   * Outra opção seria a customização do ícone através do `TemplateRef`, conforme exemplo abaixo:
   * ```
   * <po-tag [p-icon]="template" p-value="Tag template ionic"></po-button>
   *
   * <ng-template #template>
   *  <ion-icon style="font-size: inherit" name="heart"></ion-icon>
   * </ng-template>
   * ```
   * > Para o ícone enquadrar corretamente, deve-se utilizar `font-size: inherit` caso o ícone utilizado não aplique-o.
   *
   * @default `false`
   */
  set icon(value) {
    if (this.type && !this.forceIcon) {
      this._icon = convertToBoolean(value);
    } else {
      this._icon = value;
    }
  }
  get icon() {
    return this._icon;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define o *layout* de exibição.
   *
   * @default `vertical`
   */
  set orientation(value) {
    this._orientation = Object.values(PoTagOrientation).includes(value) ? value : poTagOrientationDefault;
  }
  get orientation() {
    return this._orientation;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define o tipo da *tag*.
   *
   * Valores válidos:
   *  - `success`: cor verde utilizada para simbolizar sucesso ou êxito.
   *  - `warning`: cor amarela que representa aviso ou advertência.
   *  - `danger`: cor vermelha para erro ou aviso crítico.
   *  - `info`: cor azul claro que caracteriza conteúdo informativo.
   *  - `neutral`: cor cinza claro para uso geral.
   *
   * > Quando esta propriedade for definida, irá sobrepor a definição de `p-color` e `p-icon` somente será exibido caso seja `true`.
   *
   * @default `info`
   */
  set type(value) {
    if (!this.removable) {
      this._type = Object.values(PoTagType).includes(value) ? value : void 0;
    }
  }
  get type() {
    return this._type;
  }
  //propriedade interna para forçar icone mesmo com tipo definido
  forceIcon = false;
  /**
   * @optional
   *
   * @description
   *
   * Objeto com as literais usadas no `po-tag`.
   *
   *
   * Para utilizar, basta passar a literal customizada:
   *
   * ```
   *  const customLiterals: PoTagLiterals = {
   *    remove: 'Remover itens'
   *  };
   * ```
   *
   * E para carregar as literais customizadas, basta apenas passar o objeto para o componente:
   *
   * ```
   * <po-tag
   *   [p-literals]="customLiterals">
   * </po-tag>
   * ```
   *
   * > O objeto padrão de literais será traduzido de acordo com o idioma do
   * [`PoI18nService`](/documentation/po-i18n) ou do browser.
   */
  set literals(value) {
    if (value instanceof Object && !(value instanceof Array)) {
      this._literals = __spreadValues(__spreadValues(__spreadValues({}, PoTagLiteralsDefault[poLocaleDefault]), PoTagLiteralsDefault[this.language]), value);
    } else {
      this._literals = PoTagLiteralsDefault[this.language];
    }
  }
  get literals() {
    return this._literals || PoTagLiteralsDefault[this.language];
  }
  constructor(languageService) {
    this.language = languageService.getShortLanguage();
  }
  static ɵfac = function PoTagBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoTagBaseComponent)(ɵɵdirectiveInject(PoLanguageService));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoTagBaseComponent,
    inputs: {
      label: [0, "p-label", "label"],
      removable: [2, "p-removable", "removable", convertToBoolean],
      disabled: [2, "p-disabled", "disabled", convertToBoolean],
      value: [0, "p-value", "value"],
      appendInBody: [0, "p-append-in-body", "appendInBody"],
      color: [0, "p-color", "color"],
      textColor: [0, "p-text-color", "textColor"],
      icon: [0, "p-icon", "icon"],
      orientation: [0, "p-orientation", "orientation"],
      type: [0, "p-type", "type"],
      forceIcon: [2, "p-force-icon", "forceIcon", convertToBoolean],
      literals: [0, "p-literals", "literals"]
    },
    outputs: {
      click: "p-click",
      remove: "p-close"
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoTagBaseComponent, [{
    type: Directive
  }], () => [{
    type: PoLanguageService
  }], {
    label: [{
      type: Input,
      args: ["p-label"]
    }],
    removable: [{
      type: Input,
      args: [{
        alias: "p-removable",
        transform: convertToBoolean
      }]
    }],
    disabled: [{
      type: Input,
      args: [{
        alias: "p-disabled",
        transform: convertToBoolean
      }]
    }],
    value: [{
      type: Input,
      args: ["p-value"]
    }],
    appendInBody: [{
      type: Input,
      args: ["p-append-in-body"]
    }],
    click: [{
      type: Output,
      args: ["p-click"]
    }],
    remove: [{
      type: Output,
      args: ["p-close"]
    }],
    color: [{
      type: Input,
      args: ["p-color"]
    }],
    textColor: [{
      type: Input,
      args: ["p-text-color"]
    }],
    icon: [{
      type: Input,
      args: ["p-icon"]
    }],
    orientation: [{
      type: Input,
      args: ["p-orientation"]
    }],
    type: [{
      type: Input,
      args: ["p-type"]
    }],
    forceIcon: [{
      type: Input,
      args: [{
        alias: "p-force-icon",
        transform: convertToBoolean
      }]
    }],
    literals: [{
      type: Input,
      args: ["p-literals"]
    }]
  });
})();
var PO_CONTROL_POSITIONS = ["right", "right-top", "right-bottom", "top", "top-left", "top-right", "left", "left-top", "left-bottom", "bottom", "bottom-left", "bottom-right"];
var PO_TOOLTIP_POSITION_DEFAULT = "bottom";
var PoTooltipBaseDirective = class _PoTooltipBaseDirective {
  /**
   * @optional
   *
   * @description
   *
   * Define que o po-tooltip será incluido no body e não dentro do elemento ao qual o tooltip foi especificado.
   * Opção necessária para o caso de uso de tooltip em um elemento SVG.
   *
   * @default `false`
   */
  appendInBody = false;
  /**
   * @optional
   *
   * @description
   *
   * Controla a exibição da seta de indicação da tooltip.
   *
   * Quando `true`, a seta que aponta para o elemento alvo será ocultada.
   * Quando `false`, a seta será exibida normalmente.
   *
   * Essa propriedade é útil em cenários onde a seta não é necessária ou pode interferir no layout da aplicação.
   *
   * @default `false`
   */
  hideArrow = false;
  /**
   * @optional
   *
   * @description
   *
   * Permite a renderização de conteúdo HTML dentro da tooltip.
   *
   * Quando `true`, o valor da propriedade `tooltip` será interpretado como HTML,
   * possibilitando a utilização de tags e elementos HTML dentro da tooltip.
   * Caso `false`, o conteúdo será tratado como texto puro.
   *
   * @default `false`
   */
  innerHtml = false;
  _tooltipPosition = "bottom";
  tooltipContent;
  _displayTooltip = false;
  _tooltip = "";
  /**
   * @description
   *
   * Habilita e atribui um texto ao po-tooltip.
   *
   * **Como boa prática, indica-se utilizar um texto com até 140 caracteres.**
   */
  set tooltip(tooltip) {
    this._tooltip = tooltip;
  }
  get tooltip() {
    return this._tooltip;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define a posição que o po-tooltip abrirá em relação ao componente alvo. Sugere-se que seja
   * usada a orientação "bottom" (abaixo), porém o mesmo é flexível e será rotacionado
   * automaticamente para se adequar a tela, caso necessário.
   *
   * Posições válidas:
   * - `right`: Posiciona o po-tooltip no lado direito do componente alvo.
   * - `right-bottom`: Posiciona o po-tooltip no lado direito inferior do componente alvo.
   * - `right-top`: Posiciona o po-tooltip no lado direito superior do componente alvo.
   * - `bottom`: Posiciona o po-tooltip abaixo do componente alvo.
   * - `bottom-left`: Posiciona o po-tooltip abaixo e à esquerda do componente alvo.
   * - `bottom-right`: Posiciona o po-tooltip abaixo e à direita do componente alvo.
   * - `left`: Posiciona o po-tooltip no lado esquerdo do componente alvo.
   * - `left-top`: Posiciona o po-tooltip no lado esquerdo superior do componente alvo.
   * - `left-bottom`: Posiciona o po-tooltip no lado esquerdo inferior do componente alvo.
   * - `top`: Posiciona o po-tooltip acima do componente alvo.
   * - `top-right`: Posiciona o po-tooltip acima e à direita do componente alvo.
   * - `top-left`: Posiciona o po-tooltip acima e à esquerda do componente alvo.
   *
   * @default bottom
   */
  set tooltipPosition(position) {
    this._tooltipPosition = PO_CONTROL_POSITIONS.includes(position) ? position : PO_TOOLTIP_POSITION_DEFAULT;
  }
  get tooltipPosition() {
    return this._tooltipPosition;
  }
  set displayTooltip(value) {
    this._displayTooltip = value;
    this._displayTooltip ? this.addTooltipAction() : this.removeTooltipAction();
  }
  get displayTooltip() {
    return this._displayTooltip;
  }
  static ɵfac = function PoTooltipBaseDirective_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoTooltipBaseDirective)();
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoTooltipBaseDirective,
    inputs: {
      appendInBody: [2, "p-append-in-body", "appendInBody", convertToBoolean],
      hideArrow: [2, "p-hide-arrow", "hideArrow", convertToBoolean],
      innerHtml: [2, "p-inner-html", "innerHtml", convertToBoolean],
      tooltip: [0, "p-tooltip", "tooltip"],
      tooltipPosition: [0, "p-tooltip-position", "tooltipPosition"],
      displayTooltip: [2, "p-display-tooltip", "displayTooltip", convertToBoolean]
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoTooltipBaseDirective, [{
    type: Directive
  }], null, {
    appendInBody: [{
      type: Input,
      args: [{
        alias: "p-append-in-body",
        transform: convertToBoolean
      }]
    }],
    hideArrow: [{
      type: Input,
      args: [{
        alias: "p-hide-arrow",
        transform: convertToBoolean
      }]
    }],
    innerHtml: [{
      type: Input,
      args: [{
        alias: "p-inner-html",
        transform: convertToBoolean
      }]
    }],
    tooltip: [{
      type: Input,
      args: ["p-tooltip"]
    }],
    tooltipPosition: [{
      type: Input,
      args: ["p-tooltip-position"]
    }],
    displayTooltip: [{
      type: Input,
      args: [{
        alias: "p-display-tooltip",
        transform: convertToBoolean
      }]
    }]
  });
})();
var poControlPositionSidesDefault = ["bottom", "bottom-left", "bottom-right", "left", "left-top", "left-bottom", "top", "top-right", "top-left", "right", "right-bottom", "right-top"];
var PoControlPositionService = class _PoControlPositionService {
  arrowDirection;
  customPositions;
  differenceDiagonalToWidthArrow = 3;
  element;
  elementOffset;
  isCornerAligned;
  isSetElementWidth = false;
  // offset da seta em relação ao canto do componente
  offsetArrow = 12;
  targetElement;
  /**
   * @description Ajusta a posição do elemento, caso não couber em tela irá para próxima posição.
   *
   * @param value posição para exibição do elemento
   */
  adjustPosition(value) {
    const position = value || "bottom";
    this.elementPosition(position);
    this.customPositions && this.customPositions.length ? this.adjustCustomPosition(position) : this.adjustDefaultPosition(position);
  }
  /**
   * @description Retorna a direção da seta, conforme a posição do elemento.
   */
  getArrowDirection() {
    return this.arrowDirection;
  }
  /**
   * @description Método responsável por definir as propriedades utilizadas para exibir o elemento na posição correta.
   *
   * @param element elemento que será exibido
   * @param elementOffset offSet do elemento
   * @param targetElement elemento de onde deve partir a exibição
   * @param customPositions posições que sobrescreve as posições padrões
   * @param isSetElementWidth indica se deve definir o tamanho do elemento a ser exibido, caso for verdadeiro será igual do targetElement
   * @param isCornerAligned indica se o elemento filho será alinhado nos cantos do elemento pai.
   */
  setElements(element, elementOffset, targetElement, customPositions, isSetElementWidth = false, isCornerAligned = false) {
    this.element = element instanceof ElementRef ? element.nativeElement : element;
    this.targetElement = targetElement instanceof ElementRef ? targetElement.nativeElement : targetElement;
    this.elementOffset = elementOffset;
    this.customPositions = customPositions;
    this.isSetElementWidth = isSetElementWidth;
    this.isCornerAligned = isCornerAligned;
  }
  adjustCustomPosition(position) {
    const positionLength = this.customPositions.length;
    let sidesCount = 0;
    while (sidesCount++ < positionLength) {
      if (this.overflowAllSides(position)) {
        position = this.nextPosition(position, this.customPositions);
        this.elementPosition(position);
      }
    }
  }
  adjustDefaultPosition(position) {
    const mainPositions = this.getMainPositions();
    const mainPositionLength = mainPositions.length;
    let mainSidesCount = 0;
    while (mainSidesCount++ < mainPositionLength) {
      const mainPosition = this.getMainPosition(position);
      this.elementPosition(position);
      if (this.overflowMain(mainPosition)) {
        position = this.nextPosition(mainPosition, mainPositions);
        continue;
      } else if (this.overflowAllSides(position)) {
        this.verifySubPositions(position);
        return;
      }
      return;
    }
  }
  elementPosition(position) {
    this.isCornerAligned ? this.setAlignedElementPosition(position) : this.setElementPosition(position);
  }
  getMainPosition(position) {
    return position.indexOf("-") > -1 ? position.substring(0, position.indexOf("-")) : position;
  }
  getMainPositions() {
    const defaultMainPositions = ["top", "right", "bottom", "left"];
    return this.customPositions && this.customPositions.length ? this.getMainPositionsByCustomPositions(this.customPositions) : defaultMainPositions;
  }
  getMainPositionsByCustomPositions(customPositions) {
    const mainPositions = [];
    const customPositionsLength = customPositions.length;
    for (let i2 = 0; i2 < customPositionsLength; i2++) {
      const position = customPositions[i2];
      const mainPosition = this.getMainPosition(position);
      if (!mainPositions.includes(mainPosition)) {
        mainPositions.push(mainPosition);
      }
    }
    return mainPositions;
  }
  getOverflows() {
    const sizesAndPositions = this.getSizesAndPositions();
    return {
      right: sizesAndPositions.element.right > sizesAndPositions.window.innerWidth,
      top: sizesAndPositions.element.top <= 0,
      left: sizesAndPositions.element.left <= 0,
      bottom: sizesAndPositions.element.bottom > sizesAndPositions.window.innerHeight
    };
  }
  getSizesAndPositions() {
    return {
      window: {
        scrollY: window.scrollY,
        scrollX: window.scrollX,
        innerWidth: window.innerWidth,
        innerHeight: window.innerHeight
      },
      element: this.element?.getBoundingClientRect(),
      target: this.targetElement ? this.targetElement.getBoundingClientRect() : {
        top: 0,
        bottom: 0,
        right: 0,
        left: 0
      }
    };
  }
  nextPosition(position, positions = []) {
    const index = positions.indexOf(position);
    const nextIndex = index + 1;
    if (index > -1 && nextIndex < positions.length) {
      return positions[nextIndex];
    } else {
      return positions[0];
    }
  }
  overflowAllSides(position) {
    const overflows = this.getOverflows();
    switch (position) {
      case "top":
        return overflows.top || overflows.right || overflows.left;
      case "top-right":
        return overflows.top || overflows.right || overflows.left;
      case "top-left":
        return overflows.top || overflows.left || overflows.right;
      case "right":
        return overflows.right || overflows.top || overflows.bottom;
      case "right-top":
        return overflows.right || overflows.top;
      case "right-bottom":
        return overflows.right || overflows.bottom;
      case "bottom":
        return overflows.bottom || overflows.right || overflows.left;
      case "bottom-right":
        return overflows.bottom || overflows.right || overflows.left;
      case "bottom-left":
        return overflows.bottom || overflows.left || overflows.right;
      case "left":
        return overflows.left || overflows.top || overflows.bottom;
      case "left-top":
        return overflows.left || overflows.top;
      case "left-bottom":
        return overflows.left || overflows.bottom;
    }
  }
  overflowMain(position) {
    const overflows = this.getOverflows();
    switch (position) {
      case "top":
        return overflows.top;
      case "right":
        return overflows.right;
      case "bottom":
        return overflows.bottom;
      case "left":
        return overflows.left;
    }
  }
  setAlignedArrowDirection(elementPosition) {
    switch (elementPosition) {
      case "top-left":
        return "bottom-left";
      case "top-right":
        return "bottom-right";
      case "bottom-right":
        return "top-right";
      case "bottom-left":
        return "top-left";
    }
  }
  setAlignedBottomPositions(displacement, sizesAndPositions) {
    this.element.style.top = sizesAndPositions.target.bottom + this.elementOffset + "px";
    this.element.style.left = sizesAndPositions.target.left - displacement + "px";
  }
  setAlignedElementPosition(position) {
    this.setElementWidth();
    const sizesAndPositions = this.getSizesAndPositions();
    switch (position) {
      case "bottom-left":
        this.setAlignedBottomPositions(0, sizesAndPositions);
        break;
      case "bottom-right":
        this.setAlignedBottomPositions(sizesAndPositions.element.width - sizesAndPositions.target.width, sizesAndPositions);
        break;
      case "top-left":
        this.setAlignedTopPositions(0, sizesAndPositions);
        break;
      case "top-right":
        this.setAlignedTopPositions(sizesAndPositions.element.width - sizesAndPositions.target.width, sizesAndPositions);
        break;
    }
    this.arrowDirection = this.setAlignedArrowDirection(position);
  }
  setAlignedTopPositions(displacement, sizesAndPositions) {
    this.element.style.top = sizesAndPositions.target.top - sizesAndPositions.element.height - this.elementOffset + "px";
    this.element.style.left = sizesAndPositions.target.left - displacement + "px";
  }
  setArrowDirection(elementPosition) {
    switch (elementPosition) {
      case "top":
        return "bottom";
      case "top-left":
        return "bottom-right";
      case "top-right":
        return "bottom-left";
      case "right":
        return "left";
      case "right-top":
        return "left-bottom";
      case "right-bottom":
        return "left-top";
      case "bottom":
        return "top";
      case "bottom-right":
        return "top-left";
      case "bottom-left":
        return "top-right";
      case "left":
        return "right";
      case "left-bottom":
        return "right-top";
      case "left-top":
        return "right-bottom";
    }
  }
  setBottomPositions(displacement, sizesAndPositions) {
    this.element.style.top = sizesAndPositions.target.bottom + this.elementOffset + "px";
    this.element.style.left = sizesAndPositions.target.left + sizesAndPositions.target.width / 2 - displacement + "px";
  }
  setElementPosition(position) {
    this.setElementWidth();
    const sizesAndPositions = this.getSizesAndPositions();
    switch (position) {
      case "top":
        this.setTopPositions(sizesAndPositions.element.width / 2, sizesAndPositions);
        break;
      case "top-left":
        this.setTopPositions(sizesAndPositions.element.width - this.offsetArrow - this.differenceDiagonalToWidthArrow, sizesAndPositions);
        break;
      case "top-right":
        this.setTopPositions(this.offsetArrow + this.differenceDiagonalToWidthArrow, sizesAndPositions);
        break;
      case "right":
        this.setRightPositions(sizesAndPositions.element.height / 2, sizesAndPositions);
        break;
      case "right-top":
        this.setRightPositions(sizesAndPositions.element.height - this.offsetArrow - this.differenceDiagonalToWidthArrow, sizesAndPositions);
        break;
      case "right-bottom":
        this.setRightPositions(this.offsetArrow, sizesAndPositions);
        break;
      case "bottom":
        this.setBottomPositions(sizesAndPositions.element.width / 2, sizesAndPositions);
        break;
      case "bottom-right":
        this.setBottomPositions(this.offsetArrow + this.differenceDiagonalToWidthArrow, sizesAndPositions);
        break;
      case "bottom-left":
        this.setBottomPositions(sizesAndPositions.element.width - this.offsetArrow - this.differenceDiagonalToWidthArrow, sizesAndPositions);
        break;
      case "left":
        this.setLeftPositions(sizesAndPositions.element.height / 2, sizesAndPositions);
        break;
      case "left-bottom":
        this.setLeftPositions(this.offsetArrow, sizesAndPositions);
        break;
      case "left-top":
        this.setLeftPositions(sizesAndPositions.element.height - this.offsetArrow, sizesAndPositions);
        break;
    }
    this.arrowDirection = this.setArrowDirection(position);
  }
  setElementWidth() {
    if (this.isSetElementWidth && this.targetElement) {
      this.element.style.width = `${this.targetElement.clientWidth}px`;
    }
  }
  setLeftPositions(displacement, sizesAndPositions) {
    this.element.style.top = sizesAndPositions.target.top - displacement + sizesAndPositions.target.height / 2 + "px";
    this.element.style.left = sizesAndPositions.target.left - sizesAndPositions.element.width - this.elementOffset + "px";
  }
  setRightPositions(displacement, sizesAndPositions) {
    this.element.style.top = sizesAndPositions.target.top - displacement + sizesAndPositions.target.height / 2 + "px";
    this.element.style.left = sizesAndPositions.target.right + this.elementOffset + "px";
  }
  setTopPositions(displacement, sizesAndPositions) {
    this.element.style.top = sizesAndPositions.target.top - sizesAndPositions.element.height - this.elementOffset + "px";
    this.element.style.left = sizesAndPositions.target.left + sizesAndPositions.target.width / 2 - displacement + "px";
  }
  verifySubPositions(position) {
    if (position.indexOf("-") > -1) {
      position = position.substring(0, position.indexOf("-"));
      this.elementPosition(position);
    }
    let align = 0;
    while (align++ < 3) {
      if (this.overflowAllSides(position)) {
        position = this.nextPosition(position, poControlPositionSidesDefault);
        this.elementPosition(position);
      } else {
        return null;
      }
    }
  }
  static ɵfac = function PoControlPositionService_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoControlPositionService)();
  };
  static ɵprov = ɵɵdefineInjectable({
    token: _PoControlPositionService,
    factory: _PoControlPositionService.ɵfac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoControlPositionService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
var PoTooltipControlPositionService = class _PoTooltipControlPositionService extends PoControlPositionService {
  static ɵfac = /* @__PURE__ */ (() => {
    let ɵPoTooltipControlPositionService_BaseFactory;
    return function PoTooltipControlPositionService_Factory(__ngFactoryType__) {
      return (ɵPoTooltipControlPositionService_BaseFactory || (ɵPoTooltipControlPositionService_BaseFactory = ɵɵgetInheritedFactory(_PoTooltipControlPositionService)))(__ngFactoryType__ || _PoTooltipControlPositionService);
    };
  })();
  static ɵprov = ɵɵdefineInjectable({
    token: _PoTooltipControlPositionService,
    factory: _PoTooltipControlPositionService.ɵfac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoTooltipControlPositionService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
var nativeElements = ["input", "button"];
var PoTooltipDirective = class _PoTooltipDirective extends PoTooltipBaseDirective {
  elementRef;
  renderer;
  poControlPosition;
  sanitizer;
  arrowDirection;
  divArrow;
  divContent;
  isHidden;
  lastTooltipText;
  textContent;
  tooltipOffset = 8;
  eventListenerFunction;
  constructor(elementRef, renderer, poControlPosition, sanitizer) {
    super();
    this.elementRef = elementRef;
    this.renderer = renderer;
    this.poControlPosition = poControlPosition;
    this.sanitizer = sanitizer;
  }
  ngOnDestroy() {
    this.hideTooltip();
  }
  ngOnInit() {
    this.initScrollEventListenerFunction();
  }
  onMouseEnter() {
    if (!this.displayTooltip) {
      this.addTooltipAction();
    }
  }
  onMouseLeave() {
    if (!this.displayTooltip) {
      this.removeTooltipAction();
    }
  }
  onMouseClick() {
    if (!this.displayTooltip) {
      this.removeTooltipAction();
    }
  }
  onFocusOut() {
    if (!this.displayTooltip) {
      this.removeTooltipAction();
    }
  }
  onFocusIn() {
    if (!this.displayTooltip) {
      this.addTooltipAction();
    }
  }
  onKeyDown(event) {
    if (!this.displayTooltip && (event.code === "Escape" || event.keyCode === 27)) {
      this.removeTooltipAction();
    }
  }
  // Controla a visibilidade do tooltip, criado para auxiliar a propriedade `p-additional-help-tooltip`.
  toggleTooltipVisibility(show) {
    if (!this.displayTooltip) {
      show ? this.addTooltipAction() : this.removeTooltipAction();
    }
  }
  addTooltipAction() {
    setTimeout(() => {
      if (this.tooltip) {
        this.tooltipContent ? this.showTooltip() : this.createTooltip();
        this.removeArrow(this.arrowDirection);
        this.poControlPosition.adjustPosition(this.tooltipPosition);
        this.arrowDirection = this.poControlPosition.getArrowDirection();
        this.addArrow(this.arrowDirection);
        this.lastTooltipText = this.tooltip;
      }
    });
  }
  removeTooltipAction() {
    setTimeout(() => {
      if (this.appendInBody && this.tooltipContent) {
        this.renderer.removeChild(document.body, this.tooltipContent);
        this.tooltipContent = void 0;
      } else {
        this.hideTooltip();
      }
    });
  }
  addArrow(arrowDirection) {
    this.renderer.addClass(this.divArrow, `po-arrow-${arrowDirection}`);
  }
  addScrollEventListener() {
    window.addEventListener("scroll", this.eventListenerFunction, true);
  }
  // Monta a estrutura do tooltip
  createTooltip() {
    this.tooltipContent = this.renderer.createElement("div");
    this.renderer.addClass(this.tooltipContent, "po-tooltip");
    this.renderer.setStyle(this.tooltipContent, "transition", "visibility .3s, opacity .3s linear");
    this.renderer.setStyle(this.tooltipContent, "opacity", 0.9);
    this.insertAriaLabelTooltip();
    this.divArrow = this.renderer.createElement("div");
    this.renderer.addClass(this.divArrow, "po-tooltip-arrow");
    if (this.hideArrow) {
      this.renderer.setStyle(this.divArrow, "display", "none");
    }
    this.divContent = this.renderer.createElement("div");
    this.renderer.addClass(this.divContent, "po-tooltip-content");
    this.textContent = this.renderer.createText(this.tooltip);
    if (this.innerHtml) {
      this.textContent = this.renderer.createText("");
      const securityContent = this.sanitizer.sanitize(SecurityContext.HTML, this.tooltip) || "";
      this.renderer.setProperty(this.divContent, "innerHTML", securityContent);
    }
    this.renderer.appendChild(this.divContent, this.textContent);
    this.renderer.appendChild(this.tooltipContent, this.divArrow);
    this.renderer.appendChild(this.tooltipContent, this.divContent);
    const parentTarget = this.appendInBody ? document.body : this.elementRef.nativeElement;
    this.renderer.appendChild(parentTarget, this.tooltipContent);
    this.poControlPosition.setElements(this.tooltipContent, this.tooltipOffset, this.elementRef);
    this.addScrollEventListener();
  }
  initScrollEventListenerFunction() {
    this.eventListenerFunction = () => {
      if (!this.isHidden) {
        setTimeout(() => {
          this.poControlPosition.adjustPosition(this.tooltipPosition);
        });
      }
    };
  }
  hideTooltip() {
    if (this.tooltipContent) {
      try {
        this.renderer.setStyle(this.tooltipContent, "opacity", 0);
        this.renderer.setStyle(this.tooltipContent, "visibility", "hidden");
      } catch (e2) {
      }
      this.isHidden = true;
      this.removeScrollEventListener();
    }
  }
  removeArrow(arrowDirection) {
    if (this.elementRef.nativeElement.querySelector(`.po-arrow-${arrowDirection}`)) {
      this.renderer.removeClass(this.divArrow, `po-arrow-${arrowDirection}`);
    }
  }
  removeScrollEventListener() {
    window.removeEventListener("scroll", this.eventListenerFunction, true);
  }
  showTooltip() {
    this.renderer.setStyle(this.tooltipContent, "opacity", 0.9);
    this.renderer.setStyle(this.tooltipContent, "visibility", "visible");
    this.updateTextContent();
    this.isHidden = false;
    this.addScrollEventListener();
  }
  updateTextContent() {
    const checkRepeatedText = this.tooltip.split("\n");
    if (this.lastTooltipText !== this.tooltip && checkRepeatedText[0] !== this.lastTooltipText) {
      this.renderer.removeChild(this.divContent, this.textContent);
      this.textContent = this.renderer.createText(this.tooltip);
      this.renderer.appendChild(this.divContent, this.textContent);
      if (this.innerHtml) {
        this.textContent = this.renderer.createText("");
        const securityContent = this.sanitizer.sanitize(SecurityContext.HTML, this.tooltip) || "";
        this.renderer.setProperty(this.divContent, "innerHTML", securityContent);
      }
    }
  }
  insertAriaLabelTooltip() {
    const nativeTextContent = this.elementRef.nativeElement.textContent;
    let targetElement = "";
    nativeElements.forEach((el) => {
      if (this.elementRef.nativeElement.getElementsByTagName(el)[0] !== void 0) {
        targetElement = el;
      }
    });
    if (this.elementRef.nativeElement.getElementsByTagName(targetElement)[0] && this.tooltip) {
      this.renderer.setAttribute(this.elementRef.nativeElement.getElementsByTagName(targetElement)[0], "aria-label", nativeTextContent + " " + this.tooltip);
    }
  }
  static ɵfac = function PoTooltipDirective_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoTooltipDirective)(ɵɵdirectiveInject(ElementRef), ɵɵdirectiveInject(Renderer2), ɵɵdirectiveInject(PoTooltipControlPositionService), ɵɵdirectiveInject(DomSanitizer));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoTooltipDirective,
    selectors: [["", "p-tooltip", ""]],
    hostBindings: function PoTooltipDirective_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("mouseenter", function PoTooltipDirective_mouseenter_HostBindingHandler() {
          return ctx.onMouseEnter();
        })("mouseleave", function PoTooltipDirective_mouseleave_HostBindingHandler() {
          return ctx.onMouseLeave();
        })("click", function PoTooltipDirective_click_HostBindingHandler() {
          return ctx.onMouseClick();
        })("focusout", function PoTooltipDirective_focusout_HostBindingHandler() {
          return ctx.onFocusOut();
        })("focusin", function PoTooltipDirective_focusin_HostBindingHandler() {
          return ctx.onFocusIn();
        })("keydown", function PoTooltipDirective_keydown_HostBindingHandler($event) {
          return ctx.onKeyDown($event);
        });
      }
    },
    standalone: false,
    features: [ɵɵProvidersFeature([PoTooltipControlPositionService]), ɵɵInheritDefinitionFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoTooltipDirective, [{
    type: Directive,
    args: [{
      selector: "[p-tooltip]",
      providers: [PoTooltipControlPositionService],
      standalone: false
    }]
  }], () => [{
    type: ElementRef
  }, {
    type: Renderer2
  }, {
    type: PoTooltipControlPositionService
  }, {
    type: DomSanitizer
  }], {
    onMouseEnter: [{
      type: HostListener,
      args: ["mouseenter"]
    }],
    onMouseLeave: [{
      type: HostListener,
      args: ["mouseleave"]
    }],
    onMouseClick: [{
      type: HostListener,
      args: ["click"]
    }],
    onFocusOut: [{
      type: HostListener,
      args: ["focusout"]
    }],
    onFocusIn: [{
      type: HostListener,
      args: ["focusin"]
    }],
    onKeyDown: [{
      type: HostListener,
      args: ["keydown", ["$event"]]
    }]
  });
})();
var poTagTypeDefault = "po-tag-" + PoTagType.Info;
var PoTagComponent = class _PoTagComponent extends PoTagBaseComponent {
  el = inject(ElementRef);
  tagContainer;
  tagClose;
  poTag;
  isClickable;
  constructor() {
    const languageService = inject(PoLanguageService);
    super(languageService);
  }
  ngOnInit() {
    this.isClickable = this.click.observers.length > 0;
  }
  get iconFromType() {
    switch (this.type) {
      case PoTagType.Danger:
        return PoTagIcon.Danger;
      case PoTagType.Info:
        return PoTagIcon.Info;
      case PoTagType.Success:
        return PoTagIcon.Success;
      case PoTagType.Warning:
        return PoTagIcon.Warning;
    }
  }
  get isCaptionTag() {
    return this.tagColor?.startsWith("po-caption-tag-");
  }
  get tagColor() {
    if (this.type && !this.removable) {
      return `po-tag-${this.type}`;
    }
    if (this.color && !this.removable) {
      return `po-${this.color}`;
    }
    if (!this.customColor && !this.removable) {
      return poTagTypeDefault;
    }
  }
  get tagOrientation() {
    return this.orientation === this.poTagOrientation.Horizontal;
  }
  onClick(event = "click") {
    if (!this.removable && !this.disabled) {
      const submittedTagItem = {
        value: this.value,
        type: this.type,
        event
      };
      this.click.emit(submittedTagItem);
      if (this.poTag && this.poTag.nativeElement) {
        this.poTag.nativeElement.focus();
      }
    }
  }
  onClose(event = "click") {
    if (!this.disabled) {
      this.onRemove();
      this.remove.emit(event);
    }
  }
  onKeyPressed(event) {
    event.preventDefault();
    event.stopPropagation();
    this.onClick("enter");
  }
  styleTag() {
    requestAnimationFrame(this.applyTextColorByContrast);
    if (!this.tagColor && !this.removable) {
      return {
        "background-color": this.customColor
      };
    } else {
      return {};
    }
  }
  applyTextColorByContrast = () => {
    const computedStyle = getComputedStyle(this.poTag.nativeElement);
    if (this.tagColor?.startsWith("po-caption-tag-")) {
      const captionIndex = this.tagColor.replace("po-caption-tag-", "");
      this.poTag.nativeElement.style.color = `var(--color-caption-tag-text-${captionIndex})`;
    } else if ((!this.tagColor || this.tagColor?.startsWith("po-color-")) && !this.removable && !this.textColor && computedStyle?.backgroundColor) {
      const textColor = getTextColorFromBackgroundColor(computedStyle.backgroundColor);
      this.poTag.nativeElement.style.color = textColor;
    } else {
      this.poTag.nativeElement.style.color = "";
    }
  };
  getWidthTag() {
    return this.tagContainer.nativeElement.offsetWidth > 155;
  }
  setAriaLabel() {
    return this.label ? this.label + " " + this.literals.remove : this.value + " " + this.literals.remove;
  }
  onRemove() {
    if (!this.disabled) {
      this.el.nativeElement.remove();
    }
  }
  static ɵfac = function PoTagComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoTagComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoTagComponent,
    selectors: [["po-tag"]],
    viewQuery: function PoTagComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c2, 7)(_c3, 7)(_c4, 7);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.tagContainer = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.tagClose = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.poTag = _t.first);
      }
    },
    standalone: false,
    features: [ɵɵInheritDefinitionFeature],
    decls: 13,
    vars: 23,
    consts: [["poTagWrapper", ""], ["poTag", ""], ["tagContainer", ""], ["tagClose", ""], [1, "po-tag-container"], [1, "po-tag-title", "po-text-nowrap"], [1, "po-tag-sub-container"], [1, "po-tag-wrapper", 3, "keydown.enter", "keydown.space", "keyup.space", "click", "tabindex"], [1, "po-tag", 3, "ngClass", "ngStyle"], [1, "po-tag-icon", 3, "p-icon", "ngStyle"], ["p-tooltip-position", "top", 1, "po-tag-value", 3, "p-append-in-body", "p-tooltip"], [3, "ngStyle"], ["p-icon", "ICON_CLOSE", "p-tooltip-position", "top", 1, "po-tag-remove", 3, "p-tooltip", "p-append-in-body", "po-clickable", "tabindex"], [1, "po-tag-label"], ["p-icon", "ICON_CLOSE", "p-tooltip-position", "top", 1, "po-tag-remove", 3, "click", "keydown.enter", "p-tooltip", "p-append-in-body", "tabindex"]],
    template: function PoTagComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "div", 4);
        ɵɵconditionalCreate(1, PoTagComponent_Conditional_1_Template, 3, 1, "div", 5);
        ɵɵelementStart(2, "div", 6)(3, "div", 7, 0);
        ɵɵlistener("keydown.enter", function PoTagComponent_Template_div_keydown_enter_3_listener($event) {
          return ctx.onKeyPressed($event);
        })("keydown.space", function PoTagComponent_Template_div_keydown_space_3_listener($event) {
          return $event.preventDefault();
        })("keyup.space", function PoTagComponent_Template_div_keyup_space_3_listener($event) {
          return ctx.onKeyPressed($event);
        })("click", function PoTagComponent_Template_div_click_3_listener() {
          return ctx.onClick();
        });
        ɵɵelementStart(5, "div", 8, 1);
        ɵɵconditionalCreate(7, PoTagComponent_Conditional_7_Template, 1, 4, "po-icon", 9);
        ɵɵelementStart(8, "div", 10, 2)(10, "span", 11);
        ɵɵtext(11);
        ɵɵelementEnd()();
        ɵɵconditionalCreate(12, PoTagComponent_Conditional_12_Template, 2, 7, "po-icon", 12);
        ɵɵelementEnd()()()();
      }
      if (rf & 2) {
        ɵɵclassProp("po-tag-container-horizontal", ctx.tagOrientation);
        ɵɵadvance();
        ɵɵconditional(ctx.label ? 1 : -1);
        ɵɵadvance(2);
        ɵɵclassProp("po-clickable", ctx.isClickable && !ctx.disabled && !ctx.removable);
        ɵɵproperty("tabindex", ctx.isClickable && !ctx.removable ? 0 : -1);
        ɵɵattribute("role", ctx.isClickable && !ctx.disabled && !ctx.removable ? "button" : "");
        ɵɵadvance(2);
        ɵɵclassProp("po-clickable", ctx.isClickable && !ctx.disabled && !ctx.removable)("po-tag-removable", ctx.removable)("po-tag-disabled", ctx.disabled && ctx.removable);
        ɵɵproperty("ngClass", ctx.tagColor)("ngStyle", ctx.styleTag());
        ɵɵadvance(2);
        ɵɵconditional(ctx.icon && !ctx.removable ? 7 : -1);
        ɵɵadvance();
        ɵɵproperty("p-append-in-body", ctx.appendInBody)("p-tooltip", ctx.getWidthTag() ? ctx.value : "");
        ɵɵadvance(2);
        ɵɵproperty("ngStyle", !ctx.type && ctx.customTextColor && !ctx.removable && !ctx.isCaptionTag ? ɵɵpureFunction1(21, _c5, ctx.customTextColor) : "");
        ɵɵadvance();
        ɵɵtextInterpolate(ctx.value);
        ɵɵadvance();
        ɵɵconditional(ctx.removable ? 12 : -1);
      }
    },
    dependencies: [NgClass, NgStyle, PoIconComponent, PoTooltipDirective],
    encapsulation: 2,
    changeDetection: 0
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoTagComponent, [{
    type: Component,
    args: [{
      selector: "po-tag",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: `<div class="po-tag-container" [class.po-tag-container-horizontal]="tagOrientation">
  @if (label) {
    <div class="po-tag-title po-text-nowrap">
      <span class="po-tag-label">{{ tagOrientation ? label + ':' : label }}</span>
    </div>
  }

  <div class="po-tag-sub-container">
    <div
      #poTagWrapper
      class="po-tag-wrapper"
      [class.po-clickable]="isClickable && !disabled && !removable"
      [attr.role]="isClickable && !disabled && !removable ? 'button' : ''"
      (keydown.enter)="onKeyPressed($event)"
      (keydown.space)="$event.preventDefault()"
      (keyup.space)="onKeyPressed($event)"
      (click)="onClick()"
      [tabindex]="isClickable && !removable ? 0 : -1"
    >
      <div
        #poTag
        class="po-tag"
        [class.po-clickable]="isClickable && !disabled && !removable"
        [class.po-tag-removable]="removable"
        [class.po-tag-disabled]="disabled && removable"
        [ngClass]="tagColor"
        [ngStyle]="styleTag()"
      >
        @if (icon && !removable) {
          <po-icon
            class="po-tag-icon"
            [p-icon]="!type || forceIcon ? icon : iconFromType"
            [ngStyle]="!type && customTextColor && !isCaptionTag ? { 'color': customTextColor } : ''"
          >
          </po-icon>
        }

        <div
          #tagContainer
          class="po-tag-value"
          [p-append-in-body]="appendInBody"
          [p-tooltip]="getWidthTag() ? value : ''"
          p-tooltip-position="top"
        >
          <span
            [ngStyle]="!type && customTextColor && !removable && !isCaptionTag ? { 'color': customTextColor } : ''"
            >{{ value }}</span
          >
        </div>

        @if (removable) {
          <po-icon
            #tagClose
            p-icon="ICON_CLOSE"
            p-tooltip-position="top"
            [p-tooltip]="literals.remove"
            [p-append-in-body]="appendInBody"
            [attr.aria-label]="setAriaLabel()"
            class="po-tag-remove"
            [class.po-clickable]="!disabled"
            [tabindex]="!disabled ? 0 : -1"
            [attr.role]="!disabled ? 'button' : ''"
            (click)="onClose()"
            (keydown.enter)="onClose('enter')"
          >
          </po-icon>
        }
      </div>
    </div>
  </div>
</div>
`
    }]
  }], () => [], {
    tagContainer: [{
      type: ViewChild,
      args: ["tagContainer", {
        static: true
      }]
    }],
    tagClose: [{
      type: ViewChild,
      args: ["tagClose", {
        static: true
      }]
    }],
    poTag: [{
      type: ViewChild,
      args: ["poTag", {
        static: true
      }]
    }]
  });
})();
var PoTooltipModule = class _PoTooltipModule {
  static ɵfac = function PoTooltipModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoTooltipModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoTooltipModule,
    declarations: [PoTooltipDirective],
    imports: [CommonModule],
    exports: [PoTooltipDirective]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [CommonModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoTooltipModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule],
      declarations: [PoTooltipDirective],
      exports: [PoTooltipDirective]
    }]
  }], null, null);
})();
var PoTagModule = class _PoTagModule {
  static ɵfac = function PoTagModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoTagModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoTagModule,
    declarations: [PoTagComponent],
    imports: [CommonModule, PoIconModule, PoTooltipModule],
    exports: [PoTagComponent]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [CommonModule, PoIconModule, PoTooltipModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoTagModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, PoIconModule, PoTooltipModule],
      declarations: [PoTagComponent],
      exports: [PoTagComponent],
      providers: [],
      schemas: []
    }]
  }], null, null);
})();
var PoAccordionItemBodyComponent = class _PoAccordionItemBodyComponent {
  expanded = false;
  static ɵfac = function PoAccordionItemBodyComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoAccordionItemBodyComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoAccordionItemBodyComponent,
    selectors: [["po-accordion-item-body"]],
    inputs: {
      expanded: [0, "p-expanded", "expanded"]
    },
    standalone: false,
    ngContentSelectors: _c12,
    decls: 1,
    vars: 1,
    consts: [[1, "po-accordion-item-body"], [1, "po-accordion-item-body-content"]],
    template: function PoAccordionItemBodyComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵprojectionDef();
        ɵɵconditionalCreate(0, PoAccordionItemBodyComponent_Conditional_0_Template, 3, 1, "div", 0);
      }
      if (rf & 2) {
        ɵɵconditional(ctx.expanded ? 0 : -1);
      }
    },
    encapsulation: 2,
    data: {
      animation: [trigger("toggleBody", [transition("* => void", [style({
        height: "*"
      }), animate(200, style({
        height: 0
      }))]), transition("void => *", [style({
        height: "0"
      }), animate(200, style({
        height: "*"
      }))])])]
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoAccordionItemBodyComponent, [{
    type: Component,
    args: [{
      selector: "po-accordion-item-body",
      animations: [trigger("toggleBody", [transition("* => void", [style({
        height: "*"
      }), animate(200, style({
        height: 0
      }))]), transition("void => *", [style({
        height: "0"
      }), animate(200, style({
        height: "*"
      }))])])],
      standalone: false,
      template: '@if (expanded) {\n  <div class="po-accordion-item-body" @toggleBody>\n    <div class="po-accordion-item-body-content">\n      <ng-content></ng-content>\n    </div>\n  </div>\n}\n'
    }]
  }], null, {
    expanded: [{
      type: Input,
      args: ["p-expanded"]
    }]
  });
})();
var PoLanguageModule = class _PoLanguageModule {
  static ɵfac = function PoLanguageModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoLanguageModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoLanguageModule
  });
  static ɵinj = ɵɵdefineInjector({
    providers: [PoLanguageService]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoLanguageModule, [{
    type: NgModule,
    args: [{
      providers: [PoLanguageService]
    }]
  }], null, null);
})();
var PoAccordionItemHeaderComponent = class _PoAccordionItemHeaderComponent {
  language = poLocaleDefault;
  accordionElement;
  accordionHeaderElement;
  expanded = false;
  label;
  labelTag;
  typeTag;
  disabledItem;
  toggle = new EventEmitter();
  constructor() {
    const languageService = inject(PoLanguageService);
    this.language = languageService.getShortLanguage();
  }
  onClick() {
    this.expanded = !this.expanded;
    this.toggle.emit(this.expanded);
  }
  getTooltip() {
    const widthContainer = this.accordionElement.nativeElement.offsetWidth - 56;
    const widthHeaderElement = this.accordionHeaderElement.nativeElement.offsetWidth;
    if (widthHeaderElement >= widthContainer) {
      return this.label;
    }
    return null;
  }
  static ɵfac = function PoAccordionItemHeaderComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoAccordionItemHeaderComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoAccordionItemHeaderComponent,
    selectors: [["po-accordion-item-header"]],
    viewQuery: function PoAccordionItemHeaderComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c6, 7, ElementRef)(_c7, 7, ElementRef);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.accordionElement = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.accordionHeaderElement = _t.first);
      }
    },
    inputs: {
      expanded: [0, "p-expanded", "expanded"],
      label: [0, "p-label", "label"],
      labelTag: [0, "p-label-tag", "labelTag"],
      typeTag: [0, "p-type-tag", "typeTag"],
      disabledItem: [0, "p-disabled", "disabledItem"]
    },
    outputs: {
      toggle: "p-toggle"
    },
    standalone: false,
    decls: 9,
    vars: 6,
    consts: [["accordionElement", ""], ["accordionHeaderElement", ""], [1, "po-accordion-item-header"], ["type", "button", 1, "po-accordion-item-header-button", "po-clickable", 3, "click", "disabled"], [1, "po-accordion-item-header-button-content", 3, "p-tooltip"], [1, "po-text-ellipsis", "po-accordion-item-header-title"], [1, "po-accordion-item-header-tag", 3, "p-value", "p-type"], ["p-icon", "ICON_ARROW_DOWN", 1, "po-accordion-item-header-icon"]],
    template: function PoAccordionItemHeaderComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "div", 2)(1, "button", 3, 0);
        ɵɵlistener("click", function PoAccordionItemHeaderComponent_Template_button_click_1_listener() {
          return ctx.onClick();
        });
        ɵɵelementStart(3, "div", 4)(4, "div", 5, 1);
        ɵɵtext(6);
        ɵɵelementEnd();
        ɵɵconditionalCreate(7, PoAccordionItemHeaderComponent_Conditional_7_Template, 1, 2, "po-tag", 6);
        ɵɵelementEnd();
        ɵɵelement(8, "po-icon", 7);
        ɵɵelementEnd()();
      }
      if (rf & 2) {
        ɵɵadvance();
        ɵɵproperty("disabled", ctx.disabledItem);
        ɵɵattribute("aria-label", ctx.label)("aria-expanded", ctx.expanded || false);
        ɵɵadvance(2);
        ɵɵproperty("p-tooltip", ctx.getTooltip());
        ɵɵadvance(3);
        ɵɵtextInterpolate(ctx.label);
        ɵɵadvance();
        ɵɵconditional(ctx.labelTag ? 7 : -1);
      }
    },
    dependencies: [PoTagComponent, PoIconComponent, PoTooltipDirective],
    encapsulation: 2,
    changeDetection: 0
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoAccordionItemHeaderComponent, [{
    type: Component,
    args: [{
      selector: "po-accordion-item-header",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: '<div class="po-accordion-item-header">\n  <button\n    #accordionElement\n    [disabled]="disabledItem"\n    [attr.aria-label]="label"\n    [attr.aria-expanded]="expanded || false"\n    class="po-accordion-item-header-button po-clickable"\n    type="button"\n    (click)="onClick()"\n  >\n    <div class="po-accordion-item-header-button-content" [p-tooltip]="getTooltip()">\n      <div #accordionHeaderElement class="po-text-ellipsis po-accordion-item-header-title">{{ label }}</div>\n      @if (labelTag) {\n        <po-tag class="po-accordion-item-header-tag" [p-value]="labelTag" [p-type]="typeTag"> </po-tag>\n      }\n    </div>\n    <po-icon p-icon="ICON_ARROW_DOWN" class="po-accordion-item-header-icon"></po-icon>\n  </button>\n</div>\n'
    }]
  }], () => [], {
    accordionElement: [{
      type: ViewChild,
      args: ["accordionElement", {
        read: ElementRef,
        static: true
      }]
    }],
    accordionHeaderElement: [{
      type: ViewChild,
      args: ["accordionHeaderElement", {
        read: ElementRef,
        static: true
      }]
    }],
    expanded: [{
      type: Input,
      args: ["p-expanded"]
    }],
    label: [{
      type: Input,
      args: ["p-label"]
    }],
    labelTag: [{
      type: Input,
      args: ["p-label-tag"]
    }],
    typeTag: [{
      type: Input,
      args: ["p-type-tag"]
    }],
    disabledItem: [{
      type: Input,
      args: ["p-disabled"]
    }],
    toggle: [{
      type: Output,
      args: ["p-toggle"]
    }]
  });
})();
var PoAccordionService = class _PoAccordionService {
  subjectChild = new Subject();
  // Recebe o accordionItem
  receiveFromChildAccordionClicked() {
    return this.subjectChild.asObservable();
  }
  // Envia accordionItem colapsado/expadido do accordion
  sendToParentAccordionItemClicked(accordionItem) {
    this.subjectChild.next(accordionItem);
  }
  static ɵfac = function PoAccordionService_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoAccordionService)();
  };
  static ɵprov = ɵɵdefineInjectable({
    token: _PoAccordionService,
    factory: _PoAccordionService.ɵfac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoAccordionService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
var PoAccordionItemComponent = class _PoAccordionItemComponent {
  accordionService = inject(PoAccordionService);
  _type;
  /** Título do item. */
  label;
  /**
   * @optional
   *
   * @description
   *
   * Label da Tag.
   *
   */
  labelTag;
  /**
   * @optional
   *
   * @description
   *
   * Desabilita item.
   *
   * @default `false`
   */
  disabledItem = false;
  /**
   * @optional
   *
   * @description
   *
   * Define o tipo da *tag* caso ela esteja sendo exibida.
   *
   * Valores válidos:
   *  - `success`: cor verde utilizada para simbolizar sucesso ou êxito.
   *  - `warning`: cor amarela que representa aviso ou advertência.
   *  - `danger`: cor vermelha para erro ou aviso crítico.
   *  - `info`: cor cinza escuro que caracteriza conteúdo informativo.
   *
   *
   * @default `info`
   */
  set typeTag(value) {
    this._type = Object.values(PoTagType).includes(value) ? value : void 0;
  }
  get typeTag() {
    return this._type;
  }
  /** Evento disparado ao expandir o item, seja manualmente ou programaticamente. */
  expandEvent = new EventEmitter();
  /** Evento disparado ao retrair o item, seja manualmente ou programaticamente. */
  collapseEvent = new EventEmitter();
  templateRef;
  expanded;
  expandSubscription;
  collapseSubscription;
  constructor() {
    this.expandSubscription = this.accordionService.receiveFromChildAccordionClicked().pipe(filter((poAccordionItem) => poAccordionItem === this && poAccordionItem.expanded)).subscribe(() => {
      this.expandEvent.emit();
    });
    this.collapseSubscription = this.accordionService.receiveFromChildAccordionClicked().pipe(filter((poAccordionItem) => poAccordionItem === this && !poAccordionItem.expanded)).subscribe(() => {
      this.collapseEvent.emit();
    });
  }
  ngOnDestroy() {
    this.expandSubscription.unsubscribe();
    this.collapseSubscription.unsubscribe();
  }
  /**
   * Método para colapsar o `po-accordion-item`.
   */
  collapse() {
    if (!this.disabledItem) {
      this.expanded = false;
      this.accordionService.sendToParentAccordionItemClicked(this);
    }
  }
  /**
   * Método para expandir o `po-accordion-item`.
   */
  expand() {
    if (!this.disabledItem) {
      this.expanded = true;
      this.accordionService.sendToParentAccordionItemClicked(this);
    }
  }
  static ɵfac = function PoAccordionItemComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoAccordionItemComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoAccordionItemComponent,
    selectors: [["po-accordion-item"]],
    viewQuery: function PoAccordionItemComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(TemplateRef, 7);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.templateRef = _t.first);
      }
    },
    inputs: {
      label: [0, "p-label", "label"],
      labelTag: [0, "p-label-tag", "labelTag"],
      disabledItem: [2, "p-disabled", "disabledItem", convertToBoolean],
      typeTag: [0, "p-type-tag", "typeTag"]
    },
    outputs: {
      expandEvent: "p-expand",
      collapseEvent: "p-collapse"
    },
    standalone: false,
    ngContentSelectors: _c12,
    decls: 1,
    vars: 0,
    template: function PoAccordionItemComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵprojectionDef();
        ɵɵtemplate(0, PoAccordionItemComponent_ng_template_0_Template, 1, 0, "ng-template");
      }
    },
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoAccordionItemComponent, [{
    type: Component,
    args: [{
      selector: "po-accordion-item",
      standalone: false,
      template: "<ng-template>\n  <ng-content></ng-content>\n</ng-template>\n"
    }]
  }], () => [], {
    label: [{
      type: Input,
      args: ["p-label"]
    }],
    labelTag: [{
      type: Input,
      args: ["p-label-tag"]
    }],
    disabledItem: [{
      type: Input,
      args: [{
        alias: "p-disabled",
        transform: convertToBoolean
      }]
    }],
    typeTag: [{
      type: Input,
      args: ["p-type-tag"]
    }],
    expandEvent: [{
      type: Output,
      args: ["p-expand"]
    }],
    collapseEvent: [{
      type: Output,
      args: ["p-collapse"]
    }],
    templateRef: [{
      type: ViewChild,
      args: [TemplateRef, {
        static: true
      }]
    }]
  });
})();
var PoAccordionManagerComponent = class _PoAccordionManagerComponent {
  labelValue = "";
  changeDetector = inject(ChangeDetectorRef);
  accordionElement;
  accordionHeaderElement;
  expandedAllItems = false;
  literals;
  clickManager = new EventEmitter();
  ngOnChanges(changes) {
    this.labelValue = changes.expandedAllItems.currentValue ? this.literals.closeAllItems : this.literals.expandAllItems;
    this.changeDetector.detectChanges();
  }
  onClick() {
    this.clickManager.emit();
  }
  getTooltip() {
    const widthContainer = this.accordionElement.nativeElement.offsetWidth - 69;
    const widthHeaderElement = this.accordionHeaderElement.nativeElement.offsetWidth;
    if (widthHeaderElement >= widthContainer) {
      return this.labelValue;
    }
    return null;
  }
  static ɵfac = function PoAccordionManagerComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoAccordionManagerComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoAccordionManagerComponent,
    selectors: [["po-accordion-manager"]],
    viewQuery: function PoAccordionManagerComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c8, 7, ElementRef)(_c9, 7, ElementRef);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.accordionElement = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.accordionHeaderElement = _t.first);
      }
    },
    inputs: {
      expandedAllItems: [0, "p-expanded-all-items", "expandedAllItems"],
      literals: [0, "p-literals", "literals"]
    },
    outputs: {
      clickManager: "p-click"
    },
    standalone: false,
    features: [ɵɵNgOnChangesFeature],
    decls: 7,
    vars: 4,
    consts: [["accordionHeaderButtonManagerElement", ""], ["accordionHeaderManagerElement", ""], [1, "po-accordion-manager-header"], ["type", "button", 1, "po-accordion-manager-button", "po-clickable", 3, "click", "p-tooltip"], [1, "po-text-ellipsis", "po-accordion-manager-button-content"], ["p-icon", "ICON_LAST_PAGE", 1, "po-accordion-manager-icon", 3, "ngClass"]],
    template: function PoAccordionManagerComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "div", 2)(1, "button", 3, 0);
        ɵɵlistener("click", function PoAccordionManagerComponent_Template_button_click_1_listener() {
          return ctx.onClick();
        });
        ɵɵelementStart(3, "div", 4, 1);
        ɵɵtext(5);
        ɵɵelementEnd();
        ɵɵelement(6, "po-icon", 5);
        ɵɵelementEnd()();
      }
      if (rf & 2) {
        ɵɵadvance();
        ɵɵproperty("p-tooltip", ctx.getTooltip());
        ɵɵattribute("aria-expanded", ctx.expandedAllItems);
        ɵɵadvance(4);
        ɵɵtextInterpolate1(" ", ctx.expandedAllItems ? ctx.literals.closeAllItems : ctx.literals.expandAllItems, " ");
        ɵɵadvance();
        ɵɵproperty("ngClass", ctx.expandedAllItems ? "icon-down" : "icon-up");
      }
    },
    dependencies: [NgClass, PoIconComponent, PoTooltipDirective],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoAccordionManagerComponent, [{
    type: Component,
    args: [{
      selector: "po-accordion-manager",
      standalone: false,
      template: `<div class="po-accordion-manager-header">
  <button
    #accordionHeaderButtonManagerElement
    class="po-accordion-manager-button po-clickable"
    type="button"
    (click)="onClick()"
    [attr.aria-expanded]="expandedAllItems"
    [p-tooltip]="getTooltip()"
  >
    <div #accordionHeaderManagerElement class="po-text-ellipsis po-accordion-manager-button-content">
      {{ expandedAllItems ? literals.closeAllItems : literals.expandAllItems }}
    </div>
    <po-icon
      p-icon="ICON_LAST_PAGE"
      [ngClass]="expandedAllItems ? 'icon-down' : 'icon-up'"
      class="po-accordion-manager-icon"
    >
    </po-icon>
  </button>
</div>
`
    }]
  }], null, {
    accordionElement: [{
      type: ViewChild,
      args: ["accordionHeaderButtonManagerElement", {
        read: ElementRef,
        static: true
      }]
    }],
    accordionHeaderElement: [{
      type: ViewChild,
      args: ["accordionHeaderManagerElement", {
        read: ElementRef,
        static: true
      }]
    }],
    expandedAllItems: [{
      type: Input,
      args: ["p-expanded-all-items"]
    }],
    literals: [{
      type: Input,
      args: ["p-literals"]
    }],
    clickManager: [{
      type: Output,
      args: ["p-click"]
    }]
  });
})();
var poAccordionLiteralsDefault = {
  en: {
    closeAllItems: "Close all items",
    expandAllItems: "Open all items"
  },
  es: {
    closeAllItems: "Cerrar todos los elementos",
    expandAllItems: "Abrir todos los elementos"
  },
  pt: {
    closeAllItems: "Fechar todos os itens",
    expandAllItems: "Abrir todos os itens"
  },
  ru: {
    closeAllItems: "Закрыть все элементы",
    expandAllItems: "Открыть все элементы"
  }
};
var PoAccordionBaseComponent = class _PoAccordionBaseComponent {
  language = poLocaleDefault;
  _literals;
  _size = void 0;
  _initialSize = void 0;
  /**
   * @optional
   *
   * @description
   *
   * Objeto com as literais usadas no `po-accordion`.
   *
   * Existem duas maneiras de customizar o componente, passando um objeto com todas as literais disponíveis:
   *
   * ```
   *  const customLiterals: PoAccordionLiterals = {
   *    closeAllItems: 'Fechar todos os itens',
   *    expandAllItems: 'Expandir todos os itens'
   *  };
   * ```
   *
   * Ou passando apenas as literais que deseja customizar:
   *
   * ```
   *  const customLiterals: PoAccordionLiterals = {
   *    expandAllItems: 'Expandir todos os itens'
   *  };
   * ```
   *
   * E para carregar as literais customizadas, basta apenas passar o objeto para o componente.
   *
   * ```
   * <po-accordion
   *   [p-literals]="customLiterals">
   * </po-accordion>
   * ```
   *
   * > O objeto padrão de literais será traduzido de acordo com o idioma do
   * [`PoI18nService`](/documentation/po-i18n) ou do browser.
   */
  set literals(value) {
    if (value instanceof Object && !(value instanceof Array)) {
      this._literals = __spreadValues(__spreadValues(__spreadValues({}, poAccordionLiteralsDefault[poLocaleDefault]), poAccordionLiteralsDefault[this.language]), value);
    } else {
      this._literals = poAccordionLiteralsDefault[this.language];
    }
  }
  get literals() {
    return this._literals || poAccordionLiteralsDefault[this.language];
  }
  /**
   * @optional
   *
   * @description
   *
   * Exibe o Gerenciador de Accordion.
   *
   * @default `false`
   */
  showManagerAccordion = false;
  /**
   * @optional
   *
   * @description
   *
   * Permite expandir mais de um `<po-accordion-item></po-accordion-item>` ao mesmo tempo.
   * Sempre habilitada caso a propriedade `p-show-manager-accordion` esteja como `true`.
   *
   * @default `false`
   */
  allowExpandItems = false;
  /**
   * @optional
   *
   * @description
   *
   * Define o tamanho do componente:
   * - `small`: altura de 32px (disponível apenas para acessibilidade AA).
   * - `medium`: altura de 44px.
   *
   * > Caso a acessibilidade AA não esteja configurada, o tamanho `medium` será mantido.
   * Para mais detalhes, consulte a documentação do [po-theme](https://po-ui.io/documentation/po-theme).
   *
   * @default `medium`
   */
  set size(value) {
    this._initialSize = value;
    this.applySizeBasedOnA11y();
  }
  get size() {
    return this._size ?? getDefaultSizeFn(PoFieldSize);
  }
  /**
   * @optional
   *
   * @description
   *
   * Evento disparado ao expandir o gerenciador de accordion, seja manualmente ou programaticamente.
   *
   */
  expandAllEvent = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Evento disparado ao retrair o gerenciador de accordion, seja manualmente ou programaticamente.
   *
   */
  collapseAllEvent = new EventEmitter();
  constructor(languageService) {
    this.language = languageService.getShortLanguage();
  }
  onThemeChange() {
    this.applySizeBasedOnA11y();
  }
  applySizeBasedOnA11y() {
    const size = validateSizeFn(this._initialSize, PoFieldSize);
    this._size = size;
  }
  static ɵfac = function PoAccordionBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoAccordionBaseComponent)(ɵɵdirectiveInject(PoLanguageService));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoAccordionBaseComponent,
    hostVars: 1,
    hostBindings: function PoAccordionBaseComponent_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("PoUiThemeChange", function PoAccordionBaseComponent_PoUiThemeChange_HostBindingHandler() {
          return ctx.onThemeChange();
        }, ɵɵresolveWindow);
      }
      if (rf & 2) {
        ɵɵattribute("p-size", ctx.size);
      }
    },
    inputs: {
      literals: [0, "p-literals", "literals"],
      showManagerAccordion: [2, "p-show-manager-accordion", "showManagerAccordion", convertToBoolean],
      allowExpandItems: [2, "p-allow-expand-all-items", "allowExpandItems", convertToBoolean],
      size: [0, "p-size", "size"]
    },
    outputs: {
      expandAllEvent: "p-expand-all",
      collapseAllEvent: "p-collapse-all"
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoAccordionBaseComponent, [{
    type: Directive
  }], () => [{
    type: PoLanguageService
  }], {
    literals: [{
      type: Input,
      args: ["p-literals"]
    }],
    showManagerAccordion: [{
      type: Input,
      args: [{
        alias: "p-show-manager-accordion",
        transform: convertToBoolean
      }]
    }],
    allowExpandItems: [{
      type: Input,
      args: [{
        alias: "p-allow-expand-all-items",
        transform: convertToBoolean
      }]
    }],
    size: [{
      type: Input,
      args: ["p-size"]
    }, {
      type: HostBinding,
      args: ["attr.p-size"]
    }],
    expandAllEvent: [{
      type: Output,
      args: ["p-expand-all"]
    }],
    collapseAllEvent: [{
      type: Output,
      args: ["p-collapse-all"]
    }],
    onThemeChange: [{
      type: HostListener,
      args: ["window:PoUiThemeChange"]
    }]
  });
})();
var PoAccordionComponent = class _PoAccordionComponent extends PoAccordionBaseComponent {
  accordionService = inject(PoAccordionService);
  accordionsHeader;
  poAccordionItems;
  expandedAllItems = false;
  accordionServiceSubscription;
  expandedActiveAccordionItem;
  constructor() {
    const languageService = inject(PoLanguageService);
    super(languageService);
    this.receiveFromChildAccordionSubscription();
  }
  ngOnDestroy() {
    this.accordionServiceSubscription.unsubscribe();
  }
  changeVisibleAllItems(event) {
    this.expandedAllItems = !event;
    this.poAccordionItems.forEach((item) => {
      if (!item.disabledItem) {
        item.expanded = this.expandedAllItems;
        this.toggle(item, false);
      }
    });
    if (this.expandedAllItems) {
      this.expandAllEvent.emit();
    } else {
      this.collapseAllEvent.emit();
    }
  }
  /**
   * Método para colapsar todos os itens.
   * Só pode ser utilizado quando a propriedade `p-show-manager-accordion` estiver como `true`.
   */
  collapseAllItems() {
    if (this.showManagerAccordion) {
      this.changeVisibleAllItems(true);
    }
  }
  /**
   * Método para expandir todos os itens.
   * Só pode ser utilizado quando a propriedade `p-show-manager-accordion` estiver como `true`.
   */
  expandAllItems() {
    if (this.showManagerAccordion) {
      this.changeVisibleAllItems(false);
    }
  }
  headerToggle(event, poAccordionItem) {
    poAccordionItem.expanded = event;
    this.accordionService.sendToParentAccordionItemClicked(poAccordionItem);
  }
  checkVisibleAllItems(event) {
    if (this.showManagerAccordion) {
      const accordionList = this.poAccordionItems.toArray();
      const accordionsValids = accordionList.filter((item) => !item.disabledItem);
      const allItemsExpanded = accordionsValids.every((item) => item.expanded === true);
      if (allItemsExpanded) {
        this.expandedAllItems = event;
      } else {
        this.expandedAllItems = false;
      }
    }
  }
  receiveFromChildAccordionSubscription() {
    this.accordionServiceSubscription = this.accordionService.receiveFromChildAccordionClicked().subscribe((poAccordionItem) => this.toggle(poAccordionItem));
  }
  toggle(poAccordionItem, checkAllItems = true) {
    const isCurrentAccordionCollapsed = !poAccordionItem.expanded;
    if (checkAllItems) {
      this.checkVisibleAllItems(poAccordionItem.expanded);
    }
    if (isCurrentAccordionCollapsed) {
      this.expandedActiveAccordionItem = null;
      return;
    }
    if (!this.showManagerAccordion && !this.allowExpandItems && this.expandedActiveAccordionItem) {
      this.expandedActiveAccordionItem.collapse();
    }
    this.expandedActiveAccordionItem = poAccordionItem;
  }
  static ɵfac = function PoAccordionComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoAccordionComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoAccordionComponent,
    selectors: [["po-accordion"]],
    contentQueries: function PoAccordionComponent_ContentQueries(rf, ctx, dirIndex) {
      if (rf & 1) {
        ɵɵcontentQuery(dirIndex, PoAccordionItemComponent, 4);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.poAccordionItems = _t);
      }
    },
    viewQuery: function PoAccordionComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(PoAccordionItemHeaderComponent, 5);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.accordionsHeader = _t);
      }
    },
    standalone: false,
    features: [ɵɵProvidersFeature([PoAccordionService]), ɵɵInheritDefinitionFeature],
    decls: 5,
    vars: 1,
    consts: [[1, "po-accordion"], [1, "po-accordion-manager"], [1, "po-accordion-item", 3, "po-accordion-item-active"], [3, "p-click", "p-expanded-all-items", "p-literals"], [1, "po-accordion-item"], [3, "p-toggle", "p-disabled", "p-expanded", "p-label", "p-label-tag", "p-type-tag"], [3, "p-expanded"], [4, "ngTemplateOutlet"]],
    template: function PoAccordionComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "div", 0);
        ɵɵconditionalCreate(1, PoAccordionComponent_Conditional_1_Template, 2, 2, "div", 1);
        ɵɵelementStart(2, "ul");
        ɵɵrepeaterCreate(3, PoAccordionComponent_For_4_Template, 4, 9, "li", 2, ɵɵrepeaterTrackByIdentity);
        ɵɵelementEnd()();
      }
      if (rf & 2) {
        ɵɵadvance();
        ɵɵconditional(ctx.showManagerAccordion && ctx.poAccordionItems.length > 1 ? 1 : -1);
        ɵɵadvance(2);
        ɵɵrepeater(ctx.poAccordionItems);
      }
    },
    dependencies: [NgTemplateOutlet, PoAccordionItemBodyComponent, PoAccordionItemHeaderComponent, PoAccordionManagerComponent],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoAccordionComponent, [{
    type: Component,
    args: [{
      selector: "po-accordion",
      providers: [PoAccordionService],
      standalone: false,
      template: '<div class="po-accordion">\n  @if (showManagerAccordion && poAccordionItems.length > 1) {\n    <div class="po-accordion-manager">\n      <po-accordion-manager\n        [p-expanded-all-items]="expandedAllItems"\n        [p-literals]="literals"\n        (p-click)="changeVisibleAllItems(expandedAllItems)"\n      ></po-accordion-manager>\n    </div>\n  }\n  <ul>\n    @for (poAccordionItem of poAccordionItems; track poAccordionItem) {\n      <li class="po-accordion-item" [class.po-accordion-item-active]="poAccordionItem.expanded">\n        <po-accordion-item-header\n          [p-disabled]="poAccordionItem.disabledItem"\n          [p-expanded]="poAccordionItem.expanded"\n          [p-label]="poAccordionItem.label"\n          [p-label-tag]="poAccordionItem.labelTag"\n          [p-type-tag]="poAccordionItem.typeTag"\n          (p-toggle)="headerToggle($event, poAccordionItem)"\n        >\n        </po-accordion-item-header>\n        <po-accordion-item-body [p-expanded]="poAccordionItem.expanded">\n          <ng-container *ngTemplateOutlet="poAccordionItem.templateRef"></ng-container>\n        </po-accordion-item-body>\n      </li>\n    }\n  </ul>\n</div>\n'
    }]
  }], () => [], {
    accordionsHeader: [{
      type: ViewChildren,
      args: [PoAccordionItemHeaderComponent]
    }],
    poAccordionItems: [{
      type: ContentChildren,
      args: [PoAccordionItemComponent]
    }]
  });
})();
var PoAccordionModule = class _PoAccordionModule {
  static ɵfac = function PoAccordionModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoAccordionModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoAccordionModule,
    declarations: [PoAccordionComponent, PoAccordionItemBodyComponent, PoAccordionItemComponent, PoAccordionItemHeaderComponent, PoAccordionManagerComponent],
    imports: [CommonModule, PoTagModule, PoIconModule, PoDividerModule, PoTooltipModule],
    exports: [PoAccordionComponent, PoAccordionItemComponent]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [CommonModule, PoTagModule, PoIconModule, PoDividerModule, PoTooltipModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoAccordionModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, PoTagModule, PoIconModule, PoDividerModule, PoTooltipModule],
      declarations: [PoAccordionComponent, PoAccordionItemBodyComponent, PoAccordionItemComponent, PoAccordionItemHeaderComponent, PoAccordionManagerComponent],
      exports: [PoAccordionComponent, PoAccordionItemComponent]
    }]
  }], null, null);
})();
var PO_AVATAR_SIZES = ["xs", "sm", "md", "lg", "xl"];
var PO_AVATAR_SIZE_DEFAULT = "md";
var validateAvatarSize = (value) => PO_AVATAR_SIZES.includes(value) ? value : PO_AVATAR_SIZE_DEFAULT;
var PoAvatarBaseComponent = class _PoAvatarBaseComponent {
  /**
   * Fonte da imagem que pode ser um caminho local (`./assets/images/logo-black-small.png`)
   * ou um servidor externo (`https://po-ui.io/assets/images/logo-black-small.png`).
   */
  src;
  /**
   * @optional
   *
   * @description
   *
   * Indica como o navegador deve carregar a imagem.
   *
   * Valores válidos:
   *  - `eager` (a imagem é carregada imediatamente, independente de estar visível ou não)
   *  - `lazy` (a imagem só é carregada quando estiver próxima de ser renderizada)
   *
   * @default `eager`
   */
  loading = "eager";
  /** Evento disparado ao clicar na imagem do *avatar*. */
  click = new EventEmitter();
  _size = "md";
  /**
   * @optional
   *
   * @description
   *
   * Tamanho de exibição do componente.
   *
   * Valores válidos:
   *  - `xs` (24x24)
   *  - `sm` (32x32)
   *  - `md` (64x64)
   *  - `lg` (96x96)
   *  - `xl` (144x144)
   *
   * @default `md`
   */
  set size(value) {
    this._size = validateAvatarSize(value);
  }
  get size() {
    return this._size;
  }
  get hasClickEvent() {
    return !!this.click.observers.length;
  }
  static ɵfac = function PoAvatarBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoAvatarBaseComponent)();
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoAvatarBaseComponent,
    inputs: {
      src: [0, "p-src", "src"],
      loading: [0, "p-loading", "loading"],
      size: [0, "p-size", "size"]
    },
    outputs: {
      click: "p-click"
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoAvatarBaseComponent, [{
    type: Directive
  }], null, {
    src: [{
      type: Input,
      args: ["p-src"]
    }],
    loading: [{
      type: Input,
      args: ["p-loading"]
    }],
    click: [{
      type: Output,
      args: ["p-click"]
    }],
    size: [{
      type: Input,
      args: ["p-size"]
    }]
  });
})();
var PoAvatarComponent = class _PoAvatarComponent extends PoAvatarBaseComponent {
  ngOnInit() {
    if (!this.src) {
      this.src = void 0;
    }
  }
  onError() {
    this.src = void 0;
  }
  static ɵfac = /* @__PURE__ */ (() => {
    let ɵPoAvatarComponent_BaseFactory;
    return function PoAvatarComponent_Factory(__ngFactoryType__) {
      return (ɵPoAvatarComponent_BaseFactory || (ɵPoAvatarComponent_BaseFactory = ɵɵgetInheritedFactory(_PoAvatarComponent)))(__ngFactoryType__ || _PoAvatarComponent);
    };
  })();
  static ɵcmp = ɵɵdefineComponent({
    type: _PoAvatarComponent,
    selectors: [["po-avatar"]],
    standalone: false,
    features: [ɵɵInheritDefinitionFeature],
    decls: 3,
    vars: 6,
    consts: [[3, "click"], ["alt", "", 1, "po-avatar-image", 3, "src"], ["p-icon", "ICON_USER", 1, "po-avatar-default-icon"], ["alt", "", 1, "po-avatar-image", 3, "error", "src"]],
    template: function PoAvatarComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "div", 0);
        ɵɵlistener("click", function PoAvatarComponent_Template_div_click_0_listener() {
          return ctx.click.emit();
        });
        ɵɵconditionalCreate(1, PoAvatarComponent_Conditional_1_Template, 1, 2, "img", 1)(2, PoAvatarComponent_Conditional_2_Template, 1, 0, "po-icon", 2);
        ɵɵelementEnd();
      }
      if (rf & 2) {
        ɵɵclassMap(ɵɵinterpolate1("po-avatar po-avatar-", ctx.size));
        ɵɵclassProp("po-clickable", ctx.hasClickEvent);
        ɵɵadvance();
        ɵɵconditional(ctx.src ? 1 : 2);
      }
    },
    dependencies: [PoIconComponent],
    encapsulation: 2,
    changeDetection: 0
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoAvatarComponent, [{
    type: Component,
    args: [{
      selector: "po-avatar",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: '<div class="po-avatar po-avatar-{{ size }}" [class.po-clickable]="hasClickEvent" (click)="click.emit()">\n  @if (src) {\n    <img class="po-avatar-image" [src]="src" alt="" [attr.loading]="loading" (error)="onError()" />\n  } @else {\n    <po-icon p-icon="ICON_USER" class="po-avatar-default-icon"></po-icon>\n  }\n</div>\n'
    }]
  }], null, null);
})();
var PoAvatarModule = class _PoAvatarModule {
  static ɵfac = function PoAvatarModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoAvatarModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoAvatarModule,
    declarations: [PoAvatarComponent],
    imports: [CommonModule, PoIconModule],
    exports: [PoAvatarComponent]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [CommonModule, PoIconModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoAvatarModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, PoIconModule],
      declarations: [PoAvatarComponent],
      exports: [PoAvatarComponent]
    }]
  }], null, null);
})();
var poBadgeColors = [...Object.values(PoColorPaletteEnum), ...Object.values(PoCaptionTagColorEnum)];
var PO_BADGE_COLOR_DEFAULT = "color-07";
var PoBadgeBaseComponent = class _PoBadgeBaseComponent {
  badgeValue;
  customColor;
  _color = PO_BADGE_COLOR_DEFAULT;
  _value;
  _status;
  _ariaLabel;
  /**
   * @description
   *
   * Define um `aria-label` para o `po-badge`
   */
  set ariaLabel(value) {
    if (value === void 0) {
      this._ariaLabel = "";
    }
    this._ariaLabel = value;
  }
  get ariaLabel() {
    return this._ariaLabel;
  }
  /**
   * @optional
   *
   * @description
   *
   * Determina a cor do `po-badge`. As maneiras de customizar as cores são:
   * - Hexadeximal, por exemplo `#c64840`;
   * - RGB, como `rgb(0, 0, 165)`;
   * - O nome da cor, por exemplo `blue`;
   * - Usando uma das cores do tema do PO:
   * Valores válidos:
   *   - <span class="dot po-color-01"></span> `color-01`
   *   - <span class="dot po-color-02"></span> `color-02`
   *   - <span class="dot po-color-03"></span> `color-03`
   *   - <span class="dot po-color-04"></span> `color-04`
   *   - <span class="dot po-color-05"></span> `color-05`
   *   - <span class="dot po-color-06"></span> `color-06`
   *   - <span class="dot po-color-07"></span> `color-07`
   *   - <span class="dot po-color-08"></span> `color-08`
   *   - <span class="dot po-color-09"></span> `color-09`
   *   - <span class="dot po-color-10"></span> `color-10`
   *   - <span class="dot po-color-11"></span> `color-11`
   *   - <span class="dot po-color-12"></span> `color-12`
   *
   * > Também é possível utilizar as 35 cores da paleta **Caption Tag Colors**:
   *
   *   - <span class="dot po-caption-tag-01"></span> `caption-tag-01` <span class="dot po-caption-tag-02"></span> `caption-tag-02` <span class="dot po-caption-tag-03"></span> `caption-tag-03` <span class="dot po-caption-tag-04"></span> `caption-tag-04` <span class="dot po-caption-tag-05"></span> `caption-tag-05`
   *   - <span class="dot po-caption-tag-06"></span> `caption-tag-06` <span class="dot po-caption-tag-07"></span> `caption-tag-07` <span class="dot po-caption-tag-08"></span> `caption-tag-08` <span class="dot po-caption-tag-09"></span> `caption-tag-09` <span class="dot po-caption-tag-10"></span> `caption-tag-10`
   *   - <span class="dot po-caption-tag-11"></span> `caption-tag-11` <span class="dot po-caption-tag-12"></span> `caption-tag-12` <span class="dot po-caption-tag-13"></span> `caption-tag-13` <span class="dot po-caption-tag-14"></span> `caption-tag-14` <span class="dot po-caption-tag-15"></span> `caption-tag-15`
   *   - <span class="dot po-caption-tag-16"></span> `caption-tag-16` <span class="dot po-caption-tag-17"></span> `caption-tag-17` <span class="dot po-caption-tag-18"></span> `caption-tag-18` <span class="dot po-caption-tag-19"></span> `caption-tag-19` <span class="dot po-caption-tag-20"></span> `caption-tag-20`
   *   - <span class="dot po-caption-tag-21"></span> `caption-tag-21` <span class="dot po-caption-tag-22"></span> `caption-tag-22` <span class="dot po-caption-tag-23"></span> `caption-tag-23` <span class="dot po-caption-tag-24"></span> `caption-tag-24` <span class="dot po-caption-tag-25"></span> `caption-tag-25`
   *   - <span class="dot po-caption-tag-26"></span> `caption-tag-26` <span class="dot po-caption-tag-27"></span> `caption-tag-27` <span class="dot po-caption-tag-28"></span> `caption-tag-28` <span class="dot po-caption-tag-29"></span> `caption-tag-29` <span class="dot po-caption-tag-30"></span> `caption-tag-30`
   *   - <span class="dot po-caption-tag-31"></span> `caption-tag-31` <span class="dot po-caption-tag-32"></span> `caption-tag-32` <span class="dot po-caption-tag-33"></span> `caption-tag-33` <span class="dot po-caption-tag-34"></span> `caption-tag-34` <span class="dot po-caption-tag-35"></span> `caption-tag-35`
   *
   * Exemplo de uso:
   * ```
   * <po-badge p-color="caption-tag-13" p-value="5"></po-badge>
   * ```
   *
   * @default `color-07`
   */
  set color(value) {
    if (value !== void 0 && (value.includes("color") || value.startsWith("caption-tag-"))) {
      this._color = poBadgeColors.includes(value) ? value : PO_BADGE_COLOR_DEFAULT;
    } else {
      CSS.supports("background-color", value) ? this.customColor = value : this.customColor = void 0;
    }
  }
  get color() {
    return this._color;
  }
  /**
   * @optional
   *
   * @description
   * Ícone exibido no `po-badge`.
   *
   * Para exibir icone do status atual declare a propriedade `p-icon`. conforme exemplo abaixo:
   * ```
   * <po-badge [p-icon]="true"></po-badge>
   * ```
   * É possível usar qualquer um dos ícones da [Biblioteca de ícones](https://po-ui.io/icons). conforme exemplo abaixo:
   * ```
   * <po-badge p-icon="an an-user"></po-badge>
   * ```
   * Também é possível utilizar outras fontes de ícones, por exemplo a biblioteca *Font Awesome*, da seguinte forma:
   * ```
   * <po-badge p-icon="fa fa-podcast"></po-badge>
   * ```
   * Outra opção seria a customização do ícone através do `TemplateRef`, conforme exemplo abaixo:
   * ```
   * <po-badge [p-icon]="template"></po-badge>
   *
   * <ng-template #template>
   *  <ion-icon style="font-size: inherit" name="heart"></ion-icon>
   * </ng-template>
   * ```
   */
  icon;
  /**
   * @description
   *
   * Define o estado do `po-badge`
   *
   * Valores válidos:
   * - `positive`: Define a cor do `po-badge` com a cor de feedback positivo.;
   * - `negative`: Define a cor do `po-badge` com a cor de feedback negative.;
   * - `warning`: Define a cor do `po-badge` com a cor de feedback warning.;
   * - `disabled`: Define a cor do `po-badge` com a cor de feedback disabled;
   *
   */
  set status(value) {
    this._status = ["positive", "negative", "warning", "disabled"].includes(value) ? value : void 0;
  }
  get status() {
    return this._status;
  }
  /**
   * @description
   *
   * Define o tamanho do `po-badge`
   *
   * Valores válidos:
   * - `small`: o `po-badge` fica do tamanho padrão, com 8px de altura.;
   * - `medium`: o `po-badge` fica do tamanho padrão, com 16px de altura.;
   * - `large`: o `po-badge` fica do tamanho padrão, com 24px de altura.;
   *
   * @default `medium`
   */
  size = "medium";
  /**
   * @description
   *
   * Exibe uma borda para o `po-badge`
   *
   * > Pode personalizar cor da bordar com a propriedade `p-color-border`
   */
  showBorder = false;
  /**
   * @description
   *
   * Número exibido no componente, caso o mesmo seja maior que 9 o valor exibido será 9+.
   */
  set value(value) {
    this._value = value <= 0 ? 0 : convertToInt(value);
  }
  get value() {
    return this._value;
  }
  static ɵfac = function PoBadgeBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoBadgeBaseComponent)();
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoBadgeBaseComponent,
    hostVars: 1,
    hostBindings: function PoBadgeBaseComponent_HostBindings(rf, ctx) {
      if (rf & 2) {
        ɵɵattribute("p-status", ctx.status);
      }
    },
    inputs: {
      ariaLabel: [0, "p-aria-label", "ariaLabel"],
      color: [0, "p-color", "color"],
      icon: [0, "p-icon", "icon"],
      status: [0, "p-status", "status"],
      size: [0, "p-size", "size"],
      showBorder: [2, "p-show-border", "showBorder", convertToBoolean],
      value: [0, "p-value", "value"]
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoBadgeBaseComponent, [{
    type: Directive
  }], null, {
    ariaLabel: [{
      type: Input,
      args: ["p-aria-label"]
    }],
    color: [{
      type: Input,
      args: ["p-color"]
    }],
    icon: [{
      type: Input,
      args: ["p-icon"]
    }],
    status: [{
      type: HostBinding,
      args: ["attr.p-status"]
    }, {
      type: Input,
      args: ["p-status"]
    }],
    size: [{
      type: Input,
      args: ["p-size"]
    }],
    showBorder: [{
      type: Input,
      args: [{
        alias: "p-show-border",
        transform: convertToBoolean
      }]
    }],
    value: [{
      type: Input,
      args: ["p-value"]
    }]
  });
})();
var PoBadgeLiteralsDefault = {
  en: {
    notification: "new notification",
    notifications: "new notifications"
  },
  es: {
    notification: "nueva notificación",
    notifications: "nuevas notificaciones"
  },
  pt: {
    notification: "nova notificação",
    notifications: "nova notificaçoes"
  },
  ru: {
    notification: "новое уведомление",
    notifications: "новые уведомления"
  }
};
var PO_BADGE_MAX_NOTIFICATIONS = 9;
var PoBadgeComponent = class _PoBadgeComponent extends PoBadgeBaseComponent {
  isNotification = false;
  notificationLabel = "";
  literals;
  badgeIcon = "";
  isValidValue = false;
  poLanguageService = inject(PoLanguageService);
  ngOnInit() {
    this.literals = PoBadgeLiteralsDefault[this.poLanguageService.getShortLanguage()];
    this.initialize();
  }
  ngOnChanges(changes) {
    if (changes["status"] || changes["icon"]) {
      this.setStatus();
    }
    if (changes["value"]) {
      this.setBadgeValue();
      this.setBadgeNotification(changes["value"].currentValue);
      this.setLiterals();
    }
  }
  initialize() {
    this.setStatus();
    this.setLiterals();
    this.setBadgeValue();
    this.setBadgeNotification(this.value);
  }
  getChangeStyle() {
    if (this.color === "color-07" && !this.customColor || this.status) {
      return "po-badge-default";
    } else if (this.color) {
      return `po-${this.color}`;
    }
  }
  setLiterals() {
    if (this.value) {
      this.notificationLabel = this.value > 1 ? `${this.ariaLabel ?? ""} ${this.value} ${this.literals?.notifications}` : `${this.ariaLabel ?? ""} ${this.value} ${this.literals?.notification}`;
    } else {
      this.notificationLabel = `${this.ariaLabel ?? ""} ${this.literals?.notification}`;
    }
  }
  setStatus() {
    this.isNotification = false;
    this.badgeValue = null;
    this.switchIconStatus();
  }
  switchIconStatus() {
    if (typeof this.icon === "boolean" && this.icon) {
      this.badgeIcon = "";
      if (["positive", "negative", "warning", "disabled"].includes(this.status)) {
        switch (this.status) {
          case "positive":
            this.badgeIcon = "ICON_OK";
            break;
          case "negative":
            this.badgeIcon = "ICON_MINUS";
            break;
          case "warning":
            this.badgeIcon = "ICON_WARNING";
            break;
        }
      }
    } else {
      this.badgeIcon = this.icon;
    }
  }
  setBadgeNotification(value) {
    if (value > 1 && !this.status) {
      this.isNotification = true;
      return true;
    } else {
      this.isNotification = false;
      this.badgeValue = null;
      return false;
    }
  }
  setBadgeValue() {
    if (this.value) {
      this.checkBadgeValue(this.value);
    }
  }
  checkBadgeValue(value) {
    this.isValidValue = Number.isInteger(value) && value >= 1;
    this.badgeValue = this.isValidValue ? this.formatBadgeValue(value) : "";
  }
  formatBadgeValue(value) {
    return value > PO_BADGE_MAX_NOTIFICATIONS ? "9+" : value.toString();
  }
  static ɵfac = /* @__PURE__ */ (() => {
    let ɵPoBadgeComponent_BaseFactory;
    return function PoBadgeComponent_Factory(__ngFactoryType__) {
      return (ɵPoBadgeComponent_BaseFactory || (ɵPoBadgeComponent_BaseFactory = ɵɵgetInheritedFactory(_PoBadgeComponent)))(__ngFactoryType__ || _PoBadgeComponent);
    };
  })();
  static ɵcmp = ɵɵdefineComponent({
    type: _PoBadgeComponent,
    selectors: [["po-badge"]],
    standalone: false,
    features: [ɵɵInheritDefinitionFeature, ɵɵNgOnChangesFeature],
    decls: 3,
    vars: 16,
    consts: [[3, "ariaLabel", "ngStyle"], [3, "p-icon"], ["aria-hidden", "true", 1, "po-badge-value"]],
    template: function PoBadgeComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "div", 0);
        ɵɵconditionalCreate(1, PoBadgeComponent_Conditional_1_Template, 1, 1, "po-icon", 1);
        ɵɵconditionalCreate(2, PoBadgeComponent_Conditional_2_Template, 2, 1, "span", 2);
        ɵɵelementEnd();
      }
      if (rf & 2) {
        ɵɵclassMap(ɵɵinterpolate2("", ctx.getChangeStyle(), " po-badge po-badge-", ctx.size ? ctx.size : "medium"));
        ɵɵclassProp("po-badge-notification", ctx.isNotification && !ctx.status)("po-badge-border", ctx.showBorder);
        ɵɵproperty("ariaLabel", ctx.notificationLabel)("ngStyle", ctx.customColor && !ctx.status ? ɵɵpureFunction1(13, _c10, ctx.customColor) : ɵɵpureFunction0(15, _c11));
        ɵɵattribute("data-status", ctx.status);
        ɵɵadvance();
        ɵɵconditional(!ctx.isNotification && ctx.size !== "small" && ctx.badgeIcon && ctx.status !== "disabled" ? 1 : -1);
        ɵɵadvance();
        ɵɵconditional(ctx.value > 1 ? 2 : -1);
      }
    },
    dependencies: [NgStyle, PoIconComponent],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoBadgeComponent, [{
    type: Component,
    args: [{
      selector: "po-badge",
      standalone: false,
      template: `<div
  class="{{ getChangeStyle() }} po-badge po-badge-{{ size ? size : 'medium' }}"
  [class.po-badge-notification]="isNotification && !status"
  [class.po-badge-border]="showBorder"
  [attr.data-status]="status"
  [ariaLabel]="notificationLabel"
  [ngStyle]="customColor && !status ? { 'background-color': customColor } : {}"
>
  @if (!isNotification && size !== 'small' && badgeIcon && status !== 'disabled') {
    <po-icon [p-icon]="badgeIcon"></po-icon>
  }
  @if (value > 1) {
    <span class="po-badge-value" aria-hidden="true">{{ badgeValue }}</span>
  }
</div>
`
    }]
  }], null, null);
})();
var PoBadgeModule = class _PoBadgeModule {
  static ɵfac = function PoBadgeModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoBadgeModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoBadgeModule,
    declarations: [PoBadgeComponent],
    imports: [CommonModule, PoIconModule],
    exports: [PoBadgeComponent]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [CommonModule, PoIconModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoBadgeModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, PoIconModule],
      declarations: [PoBadgeComponent],
      exports: [PoBadgeComponent]
    }]
  }], null, null);
})();
var PoBreadcrumbBaseComponent = class _PoBreadcrumbBaseComponent {
  /**
   * @optional
   *
   * @description
   *
   * Permite definir uma URL no componente `po-breadcrumb` para favoritar ou desfavoritar.
   * > Para utilizar esta propriedade, o último `PoBreadcrumbItem` da lista de items da propriedade `p-items` deve ter um link informado.
   *
   * > A API deve estar preparada para retornar um objeto no formato `{ isFavorite: boolean }`.
   *
   * Ao iniciar, o `po-breadcrumb` faz um GET na URL definida na propriedade `p-favorite-service` e deve retornar a propriedade
   * `{ isFavorite: boolean }` do último `PoBreadcrumbItem` definido na lista de itens da propriedade `p-items`.
   *
   * Ao clicar em favoritar ou desfavoritar o `po-breadcrumb` faz um POST com o link e a propriedade `{ isFavorite: boolean }`
   * definidos no último item da propriedade `p-items`.
   *
   * > Caso algum parâmetro seja definido na propriedade `p-params-service`, o mesmo será enviado para a API e retornará
   * após fazer um GET ou POST.
   *
   * Exemplo de URL contendo o serviço de favoritar ou desfavoritar:
   *
   * ```
   * https://po-ui.io/sample/api/favorite
   * ```
   *
   * Ao fazer o GET o `po-breadcrumb` concatena o link com a URL de serviço. Exemplo:
   *
   * ```
   * GET http://<domain>/api/favorite?url=/example
   * ```
   *
   * ```
   * GET http://po.com.br/sample/api/favorite?url=/example
   * ```
   *
   * ```
   * POST
   * payload: { isFavorite: true, url: '/example' }
   * ```
   *
   * Caso possua parâmetros definidos na propriedade `p-params-service`:
   *
   * ```
   * POST
   * payload: { isFavorite: true, url: "/example", params: "{ id: 14, user: 'dev.po' }" }
   * ```
   *
   * Exemplos de retorno:
   *
   * ```
   * { isFavorite: true, url: "/example" }
   * ```
   *
   * ```
   * { isFavorite: false, url: "/example" }
   * ```
   *
   * ```
   * { isFavorite: false, url: "/example", params: "{ id: 14, user: 'dev.po' }" }
   * ```
   */
  favoriteService;
  /**
   * @optional
   *
   * @description
   *
   * Objeto que possibilita o envio de parâmetros adicionais à requisição.
   */
  paramsService;
  itemsView = [];
  itemsViewPopup = [];
  clickoutListener;
  resizeListener;
  _items = [];
  _size = void 0;
  _initialSize = void 0;
  /**
   * @description
   *
   * Lista de itens do _breadcrumb_.
   *
   * **Exemplo:**
   * ```
   * { label: 'Po Portal', link: 'portal' }
   * ```
   */
  set items(items) {
    this._items = items;
    this.itemsView = [].concat(items);
    if (this.itemsView.length >= 4) {
      this.transformToArrayPopup(items);
    }
  }
  get items() {
    return this._items;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define o tamanho do componente entre `small` ou `medium`.
   *
   * > Caso a acessibilidade AA não esteja configurada, o tamanho `medium` será mantido.
   * Para mais detalhes, consulte a documentação do [po-theme](https://po-ui.io/documentation/po-theme).
   *
   * @default `medium`
   */
  set size(value) {
    this._initialSize = value;
    this.applySizeBasedOnA11y();
  }
  get size() {
    return this._size ?? getDefaultSizeFn(PoFieldSize);
  }
  onThemeChange() {
    this.applySizeBasedOnA11y();
  }
  applySizeBasedOnA11y() {
    const size = validateSizeFn(this._initialSize, PoFieldSize);
    this._size = size;
  }
  transformToArrayPopup(items) {
    const itemsCopy = items.map((obj) => __spreadValues({}, obj));
    itemsCopy.shift();
    itemsCopy.splice(-2, 1);
    itemsCopy.pop();
    this.itemsViewPopup = this.transformArrayToActionPopUp(itemsCopy);
  }
  transformArrayToActionPopUp(items) {
    return items.map((obj) => {
      if (obj.hasOwnProperty("link")) {
        obj["url"] = obj.link;
        delete obj.link;
        if (obj.hasOwnProperty("action")) {
          delete obj.action;
        }
      }
      return obj;
    });
  }
  static ɵfac = function PoBreadcrumbBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoBreadcrumbBaseComponent)();
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoBreadcrumbBaseComponent,
    hostVars: 1,
    hostBindings: function PoBreadcrumbBaseComponent_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("PoUiThemeChange", function PoBreadcrumbBaseComponent_PoUiThemeChange_HostBindingHandler() {
          return ctx.onThemeChange();
        }, ɵɵresolveWindow);
      }
      if (rf & 2) {
        ɵɵattribute("p-size", ctx.size);
      }
    },
    inputs: {
      favoriteService: [0, "p-favorite-service", "favoriteService"],
      paramsService: [0, "p-params-service", "paramsService"],
      items: [0, "p-items", "items"],
      size: [0, "p-size", "size"]
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoBreadcrumbBaseComponent, [{
    type: Directive
  }], null, {
    favoriteService: [{
      type: Input,
      args: ["p-favorite-service"]
    }],
    paramsService: [{
      type: Input,
      args: ["p-params-service"]
    }],
    items: [{
      type: Input,
      args: ["p-items"]
    }],
    size: [{
      type: Input,
      args: ["p-size"]
    }, {
      type: HostBinding,
      args: ["attr.p-size"]
    }],
    onThemeChange: [{
      type: HostListener,
      args: ["window:PoUiThemeChange"]
    }]
  });
})();
var PoLinkBaseComponent = class _PoLinkBaseComponent {
  /** Valor do rótulo a ser exibido. */
  label;
  /** Indica se o link deverá ser aberto em uma nova aba. Sempre que utilizar essa propriedade, é importante informar isso ao usuário através da label. */
  openNewTab = false;
  /** Url que será aberta ao clicar no link. */
  url;
  get type() {
    if (!this.url && this.action.observed) {
      return "action";
    }
    return isExternalLink(this.url) ? "externalLink" : "internalLink";
  }
  /** Ação que será executada quando o usuário clicar sobre o `po-link`.
   * > Ao utilizar junto da propriedade `p-url` a ação será ignorada.
   */
  action = new EventEmitter();
  static ɵfac = function PoLinkBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoLinkBaseComponent)();
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoLinkBaseComponent,
    inputs: {
      label: [0, "p-label", "label"],
      openNewTab: [2, "p-open-new-tab", "openNewTab", convertToBoolean],
      url: [0, "p-url", "url"]
    },
    outputs: {
      action: "p-action"
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoLinkBaseComponent, [{
    type: Directive
  }], null, {
    label: [{
      type: Input,
      args: ["p-label"]
    }],
    openNewTab: [{
      type: Input,
      args: [{
        alias: "p-open-new-tab",
        transform: convertToBoolean
      }]
    }],
    url: [{
      type: Input,
      args: ["p-url"]
    }],
    action: [{
      type: Output,
      args: ["p-action"]
    }]
  });
})();
var PoLinkComponent = class _PoLinkComponent extends PoLinkBaseComponent {
  onClick() {
    if (this.url) {
      return;
    } else {
      this.action.emit(null);
    }
  }
  static ɵfac = /* @__PURE__ */ (() => {
    let ɵPoLinkComponent_BaseFactory;
    return function PoLinkComponent_Factory(__ngFactoryType__) {
      return (ɵPoLinkComponent_BaseFactory || (ɵPoLinkComponent_BaseFactory = ɵɵgetInheritedFactory(_PoLinkComponent)))(__ngFactoryType__ || _PoLinkComponent);
    };
  })();
  static ɵcmp = ɵɵdefineComponent({
    type: _PoLinkComponent,
    selectors: [["po-link"]],
    standalone: false,
    features: [ɵɵInheritDefinitionFeature],
    decls: 4,
    vars: 1,
    consts: [["type", "button", 1, "po-link"], [1, "po-link", 3, "href", "target"], [1, "po-link", 3, "routerLink"], [1, "po-link", 3, "href"], ["type", "button", 1, "po-link", 3, "click"]],
    template: function PoLinkComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵconditionalCreate(0, PoLinkComponent_Case_0_Template, 2, 1, "button", 0)(1, PoLinkComponent_Case_1_Template, 2, 3, "a", 1)(2, PoLinkComponent_Case_2_Template, 2, 2, "a", 2)(3, PoLinkComponent_Case_3_Template, 2, 2, "a", 3);
      }
      if (rf & 2) {
        let tmp_0_0;
        ɵɵconditional((tmp_0_0 = ctx.type) === "action" ? 0 : tmp_0_0 === "externalLink" ? 1 : tmp_0_0 === "internalLink" ? 2 : 3);
      }
    },
    dependencies: [RouterLink],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoLinkComponent, [{
    type: Component,
    args: [{
      selector: "po-link",
      standalone: false,
      template: `@switch (type) {
  @case ('action') {
    <button class="po-link" type="button" (click)="onClick()">{{ label }}</button>
  }
  @case ('externalLink') {
    <a class="po-link" [href]="url" [target]="openNewTab ? '_blank' : '_self'">{{ label }}</a>
  }
  @case ('internalLink') {
    <a class="po-link" [routerLink]="url">{{ label }}</a>
  }
  @default {
    <a class="po-link" [href]="url">{{ label }}</a>
  }
}
`
    }]
  }], null, null);
})();
var poPopupDefaultPosition = "bottom-left";
var PoPopupBaseComponent = class _PoPopupBaseComponent {
  arrowDirection = "top-right";
  showPopup = false;
  oldTarget;
  param;
  clickoutListener;
  resizeListener;
  _actions;
  _customPositions;
  _hideArrow = false;
  _isCornerAlign = false;
  _position = poPopupDefaultPosition;
  _size = void 0;
  _target;
  // Determina se o modo de subníveis deve ser ativado.
  // Retorna true se alguma ação possuir subItems não vazio ou $subItemTemplate.
  get computedListboxSubitems() {
    if (!this._actions || this._actions.length === 0) {
      return false;
    }
    return this._actions.some((action) => action.subItems && action.subItems.length > 0 || !!action.$subItemTemplate);
  }
  // template-icon
  templateIcon = false;
  /** Lista de ações que serão exibidas no componente. */
  set actions(value) {
    this._actions = Array.isArray(value) ? value : [];
  }
  get actions() {
    return this._actions;
  }
  /**
   * @optional
   *
   * @description
   *
   * Oculta a seta do componente *popup*.
   *
   * @default `false`
   */
  set hideArrow(value) {
    this._hideArrow = convertToBoolean(value);
  }
  get hideArrow() {
    return this._hideArrow;
  }
  // Define se o `po-popup` será alinhado pelos cantos do elemento target.
  set isCornerAlign(value) {
    this._isCornerAlign = convertToBoolean(value);
  }
  get isCornerAlign() {
    return this._isCornerAlign;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define a posição inicial que o `po-popup` abrirá em relação ao componente alvo. Sugere-se que seja
   * usada a orientação `bottom-left` (abaixo e a esquerda), porém o mesmo é flexível e será rotacionado
   * automaticamente para se adequar a tela, caso necessário.
   *
   * > Caso seja definido um `p-custom-positions` o componente irá abrir na posição definida na propriedade `p-position`
   * e caso não caiba na posição inicial ele irá rotacionar seguindo a ordem de posições definidas no `p-custom-positions`.
   *
   * Posições válidas:
   * - `right`: Posiciona o po-popup no lado direito do componente alvo.
   * - `right-bottom`: Posiciona o po-popup no lado direito inferior do componente alvo.
   * - `right-top`: Posiciona o po-popup no lado direito superior do componente alvo.
   * - `bottom`: Posiciona o po-popup abaixo do componente alvo.
   * - `bottom-left`: Posiciona o po-popup abaixo e à esquerda do componente alvo.
   * - `bottom-right`: Posiciona o po-popup abaixo e à direita do componente alvo.
   * - `left`: Posiciona o po-popup no lado esquerdo do componente alvo.
   * - `left-top`: Posiciona o po-popup no lado esquerdo superior do componente alvo.
   * - `left-bottom`: Posiciona o po-popup no lado esquerdo inferior do componente alvo.
   * - `top`: Posiciona o po-popup acima do componente alvo.
   * - `top-right`: Posiciona o po-popup acima e à direita do componente alvo.
   * - `top-left`: Posiciona o po-popup acima e à esquerda do componente alvo.
   *
   * @default `bottom-left`
   */
  set position(value) {
    this._position = PO_CONTROL_POSITIONS.includes(value) ? value : poPopupDefaultPosition;
  }
  get position() {
    return this._position;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define as posições e a sequência que o `po-popup` poderá rotacionar. A sequência será definida pela ordem passada
   * no *array*. Caso não seja definido, o `po-popup` irá rotacionar em todas as posições válidas.
   *
   * > O componente sempre irá abrir na posição definida no `p-position` e caso não caiba na posição definida o mesmo
   * irá rotacionar seguindo a ordem definida pelo `p-custom-position`.
   *
   * Posições válidas:
   * - `right`: Posiciona o po-popup no lado direito do componente alvo.
   * - `right-bottom`: Posiciona o po-popup no lado direito inferior do componente alvo.
   * - `right-top`: Posiciona o po-popup no lado direito superior do componente alvo.
   * - `bottom`: Posiciona o po-popup abaixo do componente alvo.
   * - `bottom-left`: Posiciona o po-popup abaixo e à esquerda do componente alvo.
   * - `bottom-right`: Posiciona o po-popup abaixo e à direita do componente alvo.
   * - `left`: Posiciona o po-popup no lado esquerdo do componente alvo.
   * - `left-top`: Posiciona o po-popup no lado esquerdo superior do componente alvo.
   * - `left-bottom`: Posiciona o po-popup no lado esquerdo inferior do componente alvo.
   * - `top`: Posiciona o po-popup acima do componente alvo.
   * - `top-right`: Posiciona o po-popup acima e à direita do componente alvo.
   * - `top-left`: Posiciona o po-popup acima e à esquerda do componente alvo.
   */
  set customPositions(value) {
    this._customPositions = Array.isArray(value) ? value : [];
  }
  get customPositions() {
    return this._customPositions;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define o tamanho dos componentes de formulário no template:
   * - `small`: aplica a medida small de cada componente (disponível apenas para acessibilidade AA).
   * - `medium`: aplica a medida medium de cada componente.
   *
   * > Caso a acessibilidade AA não esteja configurada, o tamanho `medium` será mantido.
   * Para mais detalhes, consulte a documentação do [po-theme](https://po-ui.io/documentation/po-theme).
   *
   * @default `medium`
   */
  set size(value) {
    this._size = validateSizeFn(value, PoFieldSize);
  }
  get size() {
    return this._size ?? getDefaultSizeFn(PoFieldSize);
  }
  /**
   * @description
   *
   * Para utilizar o `po-popup` deve-se colocar uma variável local no componente que disparará o evento
   * de abertura no mesmo e com isso, invocará a função `toggle`, por exemplo:
   *
   * ```
   * <span #icon class="an an-credit-card" (click)="popup.toggle()">
   *   Credit Actions
   * </span>
   *
   * <po-popup #popup
   *   [p-actions]="actions"
   *   [p-target]="icon">
   * </po-popup>
   * ```
   *
   * Caso o elemento alvo for um componente, será preciso obter o `ElementRef` do mesmo e passá-lo à propriedade, por exemplo:
   *
   * ```
   * // component.html
   *
   * <po-button #poButton
   *   p-label="Open Popover"
   *   (p-click)="popup.toggle()">
   * </po-button>
   *
   * <po-popup #popup
   *   [p-actions]="actions"
   *   [p-target]="poButtonRef">
   * </po-popup>
   *
   * // component.ts
   *
   * @ViewChild('poButton', { read: ElementRef }) poButtonRef: ElementRef;
   * ```
   */
  set target(value) {
    this._target = value instanceof ElementRef ? value.nativeElement : value;
  }
  get target() {
    return this._target;
  }
  closeEvent = new EventEmitter();
  clickItem = new EventEmitter();
  openEvent = new EventEmitter();
  static ɵfac = function PoPopupBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoPopupBaseComponent)();
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoPopupBaseComponent,
    inputs: {
      templateIcon: [0, "p-template-icon", "templateIcon"],
      actions: [0, "p-actions", "actions"],
      hideArrow: [0, "p-hide-arrow", "hideArrow"],
      isCornerAlign: [0, "p-is-corner-align", "isCornerAlign"],
      position: [0, "p-position", "position"],
      customPositions: [0, "p-custom-positions", "customPositions"],
      size: [0, "p-size", "size"],
      target: [0, "p-target", "target"]
    },
    outputs: {
      closeEvent: "p-close",
      clickItem: "p-click-item",
      openEvent: "p-open"
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoPopupBaseComponent, [{
    type: Directive
  }], null, {
    templateIcon: [{
      type: Input,
      args: ["p-template-icon"]
    }],
    actions: [{
      type: Input,
      args: ["p-actions"]
    }],
    hideArrow: [{
      type: Input,
      args: ["p-hide-arrow"]
    }],
    isCornerAlign: [{
      type: Input,
      args: ["p-is-corner-align"]
    }],
    position: [{
      type: Input,
      args: ["p-position"]
    }],
    customPositions: [{
      type: Input,
      args: ["p-custom-positions"]
    }],
    size: [{
      type: Input,
      args: ["p-size"]
    }],
    target: [{
      type: Input,
      args: ["p-target"]
    }],
    closeEvent: [{
      type: Output,
      args: ["p-close"]
    }],
    clickItem: [{
      type: Output,
      args: ["p-click-item"]
    }],
    openEvent: [{
      type: Output,
      args: ["p-open"]
    }]
  });
})();
var PoItemListType;
(function(PoItemListType2) {
  PoItemListType2["action"] = "action";
  PoItemListType2["check"] = "check";
  PoItemListType2["option"] = "option";
  PoItemListType2["danger"] = "danger";
})(PoItemListType || (PoItemListType = {}));
var PoItemListFilterMode;
(function(PoItemListFilterMode2) {
  PoItemListFilterMode2[PoItemListFilterMode2["startsWith"] = 0] = "startsWith";
  PoItemListFilterMode2[PoItemListFilterMode2["contains"] = 1] = "contains";
  PoItemListFilterMode2[PoItemListFilterMode2["endsWith"] = 2] = "endsWith";
})(PoItemListFilterMode || (PoItemListFilterMode = {}));
var poListBoxLiteralsDefault = {
  en: {
    backToPreviousGroup: "Go back to the previous list",
    noItems: "No items found"
  },
  es: {
    backToPreviousGroup: "Volver a la lista anterior",
    noItems: "No se encontraron artículos"
  },
  pt: {
    backToPreviousGroup: "Voltar para a lista anterior",
    noItems: "Nenhum item encontrado"
  },
  ru: {
    backToPreviousGroup: "Вернуться к предыдущему списку",
    noItems: "ничего не найдено"
  }
};
var PoListBoxBaseComponent = class _PoListBoxBaseComponent {
  _items = [];
  _type;
  _literals;
  language = poLocaleDefault;
  _size = void 0;
  _initialSize = void 0;
  listboxSubitems = false;
  visible = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "visible"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-visible",
    transform: convertToBoolean
  }));
  set type(value) {
    this._type = PoItemListType[value] ?? "action";
  }
  get type() {
    return this._type;
  }
  set items(items) {
    this._items = Array.isArray(items) ? items : [];
  }
  get items() {
    return this._items;
  }
  set literals(value) {
    if (value instanceof Object && !(value instanceof Array)) {
      this._literals = __spreadValues(__spreadValues(__spreadValues({}, poListBoxLiteralsDefault[poLocaleDefault]), poListBoxLiteralsDefault[this.language]), value);
    } else {
      this._literals = poListBoxLiteralsDefault[this.language];
    }
  }
  get literals() {
    return this._literals || poListBoxLiteralsDefault[this.language];
  }
  get isItemListGroup() {
    return this.items.length && this.items[0].hasOwnProperty("options");
  }
  isTabs = false;
  // parâmetro que pode ser passado para o popup ao clicar em um item
  param;
  //valor do checkbox de selecionar todos
  checkboxAllValue;
  // Propriedade que recebe a lista de opções selecionadas.
  selectedOptions = [];
  // Propriedade que recebe um item selecionado.
  selectedOption;
  fieldValue = "value";
  fieldLabel = "label";
  // Propriedade que recebe as literais definidas no componente `po-multiselect`.
  literalSearch;
  // Propriedade que recebe o valor de comparação de pesquisa
  fieldValueSearch = "value";
  // Propriedade que indica se o campo de pesquisa deverá ser escondido.
  hideSearch = false;
  // Propriedade que indica se o campo "Selecionar todos" deverá ser escondido.
  hideSelectAll = false;
  //Propriedades relacionados ao template customizado do multiselect
  multiselectTemplate;
  template;
  placeholderSearch;
  searchValue;
  isServerSearching = false;
  infiniteLoading = false;
  infiniteScroll = false;
  cache = false;
  infiniteScrollDistance = 100;
  filterMode = PoItemListFilterMode.contains;
  isFiltering = false;
  shouldMarkLetters = true;
  set size(value) {
    this._initialSize = value;
    this.applySizeBasedOnA11y();
  }
  get size() {
    return this._size ?? getDefaultSizeFn(PoFieldSize);
  }
  compareCache = false;
  comboService;
  containerWidth;
  // Define os nomes das propriedades do objeto que serão exibidos como rótulos (labels) no `listbox`
  keysLabel = [];
  // Habilita a ação no rodapé do list box
  footerActionListbox = false;
  // Cria um item no listbox que será utilizado como um "placeholder"
  placeholderListbox;
  // Define se haverá ou não um separador entre todos os itens do listbox
  separator = false;
  // Evento disparado a cada tecla digitada na pesquisa.
  changeSearch = new EventEmitter();
  selectItem = new EventEmitter();
  closeEvent = new EventEmitter();
  // MULTISELECT PROPERTIES
  //output para evento do checkbox
  change = new EventEmitter();
  //output para evento do checkbox
  selectCombo = new EventEmitter();
  //output para evento do checkbox de selecionar todos
  changeAll = new EventEmitter();
  UpdateInfiniteScroll = new EventEmitter();
  // Evento disparado quando uma tab é ativada
  activatedTab = new EventEmitter();
  // Evento disparado no click de uma aba do tabs
  clickTab = new EventEmitter();
  changeStateTabs = new EventEmitter();
  clickItem = new EventEmitter();
  // Evento disparado ao clicar na ação do rodapé do listbox
  footerActionListboxEvent = new EventEmitter();
  constructor(languageService) {
    this.language = languageService.getShortLanguage();
  }
  onThemeChange() {
    this.applySizeBasedOnA11y();
  }
  applySizeBasedOnA11y() {
    const size = validateSizeFn(this._initialSize, PoFieldSize);
    this._size = size;
  }
  static ɵfac = function PoListBoxBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoListBoxBaseComponent)(ɵɵdirectiveInject(PoLanguageService));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoListBoxBaseComponent,
    hostVars: 1,
    hostBindings: function PoListBoxBaseComponent_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("PoUiThemeChange", function PoListBoxBaseComponent_PoUiThemeChange_HostBindingHandler() {
          return ctx.onThemeChange();
        }, ɵɵresolveWindow);
      }
      if (rf & 2) {
        ɵɵattribute("p-size", ctx.size);
      }
    },
    inputs: {
      listboxSubitems: [0, "p-listbox-subitems", "listboxSubitems"],
      visible: [1, "p-visible", "visible"],
      type: [0, "p-type", "type"],
      items: [0, "p-items", "items"],
      literals: [0, "p-literals", "literals"],
      isTabs: [2, "p-is-tabs", "isTabs", convertToBoolean],
      param: [0, "p-param", "param"],
      checkboxAllValue: [0, "p-checkboxAllValue", "checkboxAllValue"],
      selectedOptions: [0, "p-selected-options", "selectedOptions"],
      selectedOption: [0, "p-selected-option", "selectedOption"],
      fieldValue: [0, "p-field-value", "fieldValue"],
      fieldLabel: [0, "p-field-label", "fieldLabel"],
      literalSearch: [0, "p-literal-search", "literalSearch"],
      fieldValueSearch: [0, "p-field-value-search", "fieldValueSearch"],
      hideSearch: [0, "p-hide-search", "hideSearch"],
      hideSelectAll: [0, "p-hide-select-all", "hideSelectAll"],
      multiselectTemplate: [0, "p-multiselect-template", "multiselectTemplate"],
      template: [0, "p-template", "template"],
      placeholderSearch: [0, "p-placeholder-search", "placeholderSearch"],
      searchValue: [0, "p-search-value", "searchValue"],
      isServerSearching: [2, "p-is-searching", "isServerSearching", convertToBoolean],
      infiniteLoading: [2, "p-infinite-loading", "infiniteLoading", convertToBoolean],
      infiniteScroll: [2, "p-infinite-scroll", "infiniteScroll", convertToBoolean],
      cache: [2, "p-cache", "cache", convertToBoolean],
      infiniteScrollDistance: [0, "p-infinite-scroll-distance", "infiniteScrollDistance"],
      filterMode: [0, "p-filter-mode", "filterMode"],
      isFiltering: [0, "p-filtering", "isFiltering"],
      shouldMarkLetters: [0, "p-should-mark-letter", "shouldMarkLetters"],
      size: [0, "p-size", "size"],
      compareCache: [0, "p-compare-cache", "compareCache"],
      comboService: [0, "p-combo-service", "comboService"],
      containerWidth: [0, "p-container-width", "containerWidth"],
      keysLabel: [0, "p-keys-label", "keysLabel"],
      footerActionListbox: [0, "p-footer-action-listbox", "footerActionListbox"],
      placeholderListbox: [0, "p-placeholder-listbox", "placeholderListbox"],
      separator: [0, "p-separator", "separator"]
    },
    outputs: {
      changeSearch: "p-change-search",
      selectItem: "p-select-item",
      closeEvent: "p-close",
      change: "p-change",
      selectCombo: "p-selectcombo-item",
      changeAll: "p-change-all",
      UpdateInfiniteScroll: "p-update-infinite-scroll",
      activatedTab: "p-activated-tabs",
      clickTab: "p-click-tabs",
      changeStateTabs: "p-change-state-tabs",
      clickItem: "p-click-item",
      footerActionListboxEvent: "p-footer-action-listbox-event"
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoListBoxBaseComponent, [{
    type: Directive
  }], () => [{
    type: PoLanguageService
  }], {
    listboxSubitems: [{
      type: Input,
      args: ["p-listbox-subitems"]
    }],
    visible: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-visible",
        required: false
      }]
    }],
    type: [{
      type: Input,
      args: ["p-type"]
    }],
    items: [{
      type: Input,
      args: ["p-items"]
    }],
    literals: [{
      type: Input,
      args: ["p-literals"]
    }],
    isTabs: [{
      type: Input,
      args: [{
        alias: "p-is-tabs",
        transform: convertToBoolean
      }]
    }],
    param: [{
      type: Input,
      args: ["p-param"]
    }],
    checkboxAllValue: [{
      type: Input,
      args: ["p-checkboxAllValue"]
    }],
    selectedOptions: [{
      type: Input,
      args: ["p-selected-options"]
    }],
    selectedOption: [{
      type: Input,
      args: ["p-selected-option"]
    }],
    fieldValue: [{
      type: Input,
      args: ["p-field-value"]
    }],
    fieldLabel: [{
      type: Input,
      args: ["p-field-label"]
    }],
    literalSearch: [{
      type: Input,
      args: ["p-literal-search"]
    }],
    fieldValueSearch: [{
      type: Input,
      args: ["p-field-value-search"]
    }],
    hideSearch: [{
      type: Input,
      args: ["p-hide-search"]
    }],
    hideSelectAll: [{
      type: Input,
      args: ["p-hide-select-all"]
    }],
    multiselectTemplate: [{
      type: Input,
      args: ["p-multiselect-template"]
    }],
    template: [{
      type: Input,
      args: ["p-template"]
    }],
    placeholderSearch: [{
      type: Input,
      args: ["p-placeholder-search"]
    }],
    searchValue: [{
      type: Input,
      args: ["p-search-value"]
    }],
    isServerSearching: [{
      type: Input,
      args: [{
        alias: "p-is-searching",
        transform: convertToBoolean
      }]
    }],
    infiniteLoading: [{
      type: Input,
      args: [{
        alias: "p-infinite-loading",
        transform: convertToBoolean
      }]
    }],
    infiniteScroll: [{
      type: Input,
      args: [{
        alias: "p-infinite-scroll",
        transform: convertToBoolean
      }]
    }],
    cache: [{
      type: Input,
      args: [{
        alias: "p-cache",
        transform: convertToBoolean
      }]
    }],
    infiniteScrollDistance: [{
      type: Input,
      args: ["p-infinite-scroll-distance"]
    }],
    filterMode: [{
      type: Input,
      args: ["p-filter-mode"]
    }],
    isFiltering: [{
      type: Input,
      args: ["p-filtering"]
    }],
    shouldMarkLetters: [{
      type: Input,
      args: ["p-should-mark-letter"]
    }],
    size: [{
      type: Input,
      args: ["p-size"]
    }, {
      type: HostBinding,
      args: ["attr.p-size"]
    }],
    compareCache: [{
      type: Input,
      args: ["p-compare-cache"]
    }],
    comboService: [{
      type: Input,
      args: ["p-combo-service"]
    }],
    containerWidth: [{
      type: Input,
      args: ["p-container-width"]
    }],
    keysLabel: [{
      type: Input,
      args: ["p-keys-label"]
    }],
    footerActionListbox: [{
      type: Input,
      args: ["p-footer-action-listbox"]
    }],
    placeholderListbox: [{
      type: Input,
      args: ["p-placeholder-listbox"]
    }],
    separator: [{
      type: Input,
      args: ["p-separator"]
    }],
    changeSearch: [{
      type: Output,
      args: ["p-change-search"]
    }],
    selectItem: [{
      type: Output,
      args: ["p-select-item"]
    }],
    closeEvent: [{
      type: Output,
      args: ["p-close"]
    }],
    change: [{
      type: Output,
      args: ["p-change"]
    }],
    selectCombo: [{
      type: Output,
      args: ["p-selectcombo-item"]
    }],
    changeAll: [{
      type: Output,
      args: ["p-change-all"]
    }],
    UpdateInfiniteScroll: [{
      type: Output,
      args: ["p-update-infinite-scroll"]
    }],
    activatedTab: [{
      type: Output,
      args: ["p-activated-tabs"]
    }],
    clickTab: [{
      type: Output,
      args: ["p-click-tabs"]
    }],
    changeStateTabs: [{
      type: Output,
      args: ["p-change-state-tabs"]
    }],
    clickItem: [{
      type: Output,
      args: ["p-click-item"]
    }],
    footerActionListboxEvent: [{
      type: Output,
      args: ["p-footer-action-listbox-event"]
    }],
    onThemeChange: [{
      type: HostListener,
      args: ["window:PoUiThemeChange"]
    }]
  });
})();
var poLoadingOverlayLiteralsDefault = {
  en: {
    loading: "Loading"
  },
  es: {
    loading: "Cargando"
  },
  pt: {
    loading: "Carregando"
  },
  ru: {
    loading: "Загрузка"
  }
};
var PoLoadingOverlayBaseComponent = class _PoLoadingOverlayBaseComponent {
  languageService;
  _screenLock = false;
  _text;
  _size;
  /**
   * @optional
   *
   * @description
   *
   * Define se o *overlay* será aplicado a um *container* ou à página inteira.
   *
   * Para utilizar o componente como um *container*, o elemento pai deverá receber uma posição relativa, por exemplo:
   *
   * ```
   * <div style="position: relative">
   *
   *  <po-chart [p-series]="[{ value: 10, category: 'Example' }]">
   *  </po-chart>
   *
   *  <po-loading-overlay>
   *  </po-loading-overlay>
   * </div>
   * ```
   *
   * @default `false`
   */
  set screenLock(screenLock2) {
    this._screenLock = convertToBoolean(screenLock2);
  }
  get screenLock() {
    return this._screenLock;
  }
  /**
   * @optional
   *
   * @description
   *
   * Texto a ser exibido no componente.
   *
   * > O valor padrão será traduzido de acordo com o idioma configurado no [**PoI18n**](/documentation/po-i18n) ou navegador.
   *
   * @default `Carregando`
   */
  set text(value) {
    this._text = value || this.getTextDefault();
  }
  get text() {
    return this._text;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define o tamanho do componente com base no tamanho do ícone de *loading*.
   *
   * Tamanhos disponíveis para o *loading*:
   * - `xs`: 1rem
   * - `sm`: 1.5rem
   * - `md`: 3rem
   * - `lg`: 5rem (valor padrão)
   *
   * @default `lg`
   */
  set size(value) {
    this._size = value === "" || !value ? "lg" : value;
  }
  get size() {
    return this._size;
  }
  constructor(languageService) {
    this.languageService = languageService;
    this.text = this.getTextDefault();
  }
  getTextDefault() {
    const language = this.languageService.getShortLanguage();
    return poLoadingOverlayLiteralsDefault[language].loading;
  }
  static ɵfac = function PoLoadingOverlayBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoLoadingOverlayBaseComponent)(ɵɵdirectiveInject(PoLanguageService));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoLoadingOverlayBaseComponent,
    inputs: {
      screenLock: [0, "p-screen-lock", "screenLock"],
      text: [0, "p-text", "text"],
      size: [0, "p-size", "size"]
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoLoadingOverlayBaseComponent, [{
    type: Directive
  }], () => [{
    type: PoLanguageService
  }], {
    screenLock: [{
      type: Input,
      args: ["p-screen-lock"]
    }],
    text: [{
      type: Input,
      args: ["p-text"]
    }],
    size: [{
      type: Input,
      args: ["p-size"]
    }]
  });
})();
var PoOverlayBaseComponent = class _PoOverlayBaseComponent {
  /**
   * @optional
   *
   * @description
   *
   * Define se o *overlay* será aplicado a um *container* ou a página inteira.
   *
   * Para utilizar o componente como um *container*, o elemento pai deverá receber uma posição relativa, por exemplo:
   *
   * ```
   * <div style="position: relative">
   *
   *  <po-chart [p-series]="[{ value: 10, category: 'Example' }]">
   *  </po-chart>
   *
   *  <po-overlay>
   *  </po-overlay>
   * </div>
   * ```
   *
   * @default `false`
   */
  screenLock = false;
  static ɵfac = function PoOverlayBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoOverlayBaseComponent)();
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoOverlayBaseComponent,
    inputs: {
      screenLock: [2, "p-screen-lock", "screenLock", convertToBoolean]
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoOverlayBaseComponent, [{
    type: Directive
  }], null, {
    screenLock: [{
      type: Input,
      args: [{
        alias: "p-screen-lock",
        transform: convertToBoolean
      }]
    }]
  });
})();
var PoOverlayComponent = class _PoOverlayComponent extends PoOverlayBaseComponent {
  static ɵfac = /* @__PURE__ */ (() => {
    let ɵPoOverlayComponent_BaseFactory;
    return function PoOverlayComponent_Factory(__ngFactoryType__) {
      return (ɵPoOverlayComponent_BaseFactory || (ɵPoOverlayComponent_BaseFactory = ɵɵgetInheritedFactory(_PoOverlayComponent)))(__ngFactoryType__ || _PoOverlayComponent);
    };
  })();
  static ɵcmp = ɵɵdefineComponent({
    type: _PoOverlayComponent,
    selectors: [["po-overlay"]],
    standalone: false,
    features: [ɵɵInheritDefinitionFeature],
    ngContentSelectors: _c12,
    decls: 3,
    vars: 2,
    consts: [["role", "alert", "aria-busy", "true", 1, "po-overlay"], [1, "po-overlay-content"]],
    template: function PoOverlayComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵprojectionDef();
        ɵɵelementStart(0, "div", 0)(1, "div", 1);
        ɵɵprojection(2);
        ɵɵelementEnd()();
      }
      if (rf & 2) {
        ɵɵclassProp("po-overlay-fixed", ctx.screenLock);
      }
    },
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoOverlayComponent, [{
    type: Component,
    args: [{
      selector: "po-overlay",
      standalone: false,
      template: '<div class="po-overlay" [class.po-overlay-fixed]="screenLock" role="alert" aria-busy="true">\n  <div class="po-overlay-content">\n    <ng-content></ng-content>\n  </div>\n</div>\n'
    }]
  }], null, null);
})();
var PoLoadingIconSize;
(function(PoLoadingIconSize2) {
  PoLoadingIconSize2["xs"] = "xs";
  PoLoadingIconSize2["sm"] = "sm";
  PoLoadingIconSize2["md"] = "md";
  PoLoadingIconSize2["lg"] = "lg";
})(PoLoadingIconSize || (PoLoadingIconSize = {}));
var poLoadingLiteralsDefault = {
  en: {
    loading: "Loading"
  },
  es: {
    loading: "Cargando"
  },
  pt: {
    loading: "Carregando"
  },
  ru: {
    loading: "Загрузка"
  }
};
var PoLoadingBaseComponent = class _PoLoadingBaseComponent {
  languageService;
  _text;
  _size;
  /**
   * Texto a ser exibido no componente.
   */
  set text(value) {
    this._text = value || (value === "" ? "" : this.getTextDefault());
  }
  get text() {
    return this._text;
  }
  /**
   * Define o tamanho do ícone.
   * Valores válidos:
   *  - `xs`: 16px
   *  - `sm`: 24px
   *  - `md`: 32px
   *  - `lg`: 80px
   *
   * @default `lg`
   */
  set size(value) {
    if (value && PoLoadingIconSize[value]) {
      this._size = PoLoadingIconSize[value];
    } else {
      this._size = PoLoadingIconSize.lg;
    }
  }
  get size() {
    return this._size;
  }
  inOverlay = false;
  constructor(languageService) {
    this.languageService = languageService;
    this.text = this.getTextDefault();
  }
  getTextDefault() {
    const language = this.languageService.getShortLanguage();
    return poLoadingLiteralsDefault[language].loading;
  }
  static ɵfac = function PoLoadingBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoLoadingBaseComponent)(ɵɵdirectiveInject(PoLanguageService));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoLoadingBaseComponent,
    inputs: {
      text: [0, "p-text", "text"],
      size: [0, "p-size", "size"],
      inOverlay: [0, "p-in-overlay", "inOverlay"]
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoLoadingBaseComponent, [{
    type: Directive
  }], () => [{
    type: PoLanguageService
  }], {
    text: [{
      type: Input,
      args: ["p-text"]
    }],
    size: [{
      type: Input,
      args: ["p-size"]
    }],
    inOverlay: [{
      type: Input,
      args: ["p-in-overlay"]
    }]
  });
})();
var LOADING_ICON_COMPONENT = new InjectionToken("LOADING_ICON_COMPONENT");
var PoLoadingIconComponent = class _PoLoadingIconComponent {
  loadingIconComponent;
  _neutralColor;
  _size = "md";
  createdRef;
  id = uuid();
  loadingContainer;
  /**
   * @optional
   *
   * @description
   *
   * Definição para cor neutra (cinza) para o ícone de carregamento.
   *
   * @default `false`
   */
  set neutralColor(value) {
    this._neutralColor = convertToBoolean(value);
  }
  get neutralColor() {
    return this._neutralColor;
  }
  /**
   * @optional
   *
   * @description
   *
   * Definição do tamanho do ícone.
   *
   * Valores válidos:
   *  - `xs`: tamanho `extra small`
   *  - `sm`: tamanho `small`
   *  - `md`: tamanho `medium`
   *  - `lg`: tamanho `large`
   *
   * @default `md`
   */
  set size(value) {
    this._size = PoLoadingIconSize[value] ? PoLoadingIconSize[value] : PoLoadingIconSize.md;
    if (this.createdRef) {
      this.createdRef.instance.size = this.size;
    }
  }
  get size() {
    return this._size;
  }
  inOverlay = false;
  constructor(loadingIconComponent) {
    this.loadingIconComponent = loadingIconComponent;
  }
  ngOnDestroy() {
    if (this.createdRef) {
      this.createdRef.destroy();
    }
  }
  ngAfterViewInit() {
    if (this.loadingIconComponent && this.loadingContainer && this.inOverlay) {
      this.loadingContainer.clear();
      this.createdRef = this.loadingContainer.createComponent(this.loadingIconComponent);
      this.createdRef.instance.size = this.size;
    }
  }
  static ɵfac = function PoLoadingIconComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoLoadingIconComponent)(ɵɵdirectiveInject(LOADING_ICON_COMPONENT, 8));
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoLoadingIconComponent,
    selectors: [["po-loading-icon"]],
    viewQuery: function PoLoadingIconComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c122, 5, ViewContainerRef);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.loadingContainer = _t.first);
      }
    },
    inputs: {
      neutralColor: [0, "p-neutral-color", "neutralColor"],
      size: [0, "p-size", "size"],
      inOverlay: [0, "p-in-overlay", "inOverlay"]
    },
    standalone: false,
    decls: 2,
    vars: 1,
    consts: [["loadingContainer", ""], [3, "class"], ["viewBox", "0 0 200 200", "fill", "none", "xmlns", "http://www.w3.org/2000/svg", 1, "po-loading-icon-animated", 3, "ngClass"], [3, "id"], ["offset", "0", "stop-opacity", "0", "stop-color", "currentColor"], ["offset", "1", "stop-opacity", "0.5", "stop-color", "currentColor"], ["offset", "0", "stop-opacity", "1", "stop-color", "currentColor"], ["stroke-width", "24", "transform", "matrix(0.876218, 0, 0, 0.87855, 12.377892, 12.144993)"], ["d", "M 4 100 A 96 96 0 0 1 196 100"], ["d", "M 196 100 A 96 96 0 0 1 4 100"], ["stroke", "currentColor", "d", "M 4 100 A 96 96 0 0 1 4 98"]],
    template: function PoLoadingIconComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵconditionalCreate(0, PoLoadingIconComponent_Conditional_0_Template, 2, 0)(1, PoLoadingIconComponent_Conditional_1_Template, 13, 8, "div", 1);
      }
      if (rf & 2) {
        ɵɵconditional(ctx.inOverlay && ctx.loadingIconComponent ? 0 : 1);
      }
    },
    dependencies: [NgClass],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoLoadingIconComponent, [{
    type: Component,
    args: [{
      selector: "po-loading-icon",
      standalone: false,
      template: `@if (inOverlay && loadingIconComponent) {
  <ng-template #loadingContainer></ng-template>
} @else {
  <div class="po-loading-icon-container po-loading-svg-{{ size }}">
    <svg
      [ngClass]="neutralColor ? 'po-loading-icon-neutral' : 'po-loading-icon'"
      class="po-loading-icon-animated"
      viewBox="0 0 200 200"
      fill="none"
      xmlns="http://www.w3.org/2000/svg"
    >
      <defs>
        <linearGradient [id]="'first' + id">
          <stop offset="0" stop-opacity="0" stop-color="currentColor" />
          <stop offset="1" stop-opacity="0.5" stop-color="currentColor" />
        </linearGradient>
        <linearGradient [id]="'second' + id">
          <stop offset="0" stop-opacity="1" stop-color="currentColor" />
          <stop offset="1" stop-opacity="0.5" stop-color="currentColor" />
        </linearGradient>
      </defs>
      <g stroke-width="24" transform="matrix(0.876218, 0, 0, 0.87855, 12.377892, 12.144993)">
        <path [attr.stroke]="'url(#' + 'first' + id + ')'" d="M 4 100 A 96 96 0 0 1 196 100" />
        <path [attr.stroke]="'url(#' + 'second' + id + ')'" d="M 196 100 A 96 96 0 0 1 4 100" />
        <path stroke="currentColor" d="M 4 100 A 96 96 0 0 1 4 98" />
      </g>
    </svg>
  </div>
}
`
    }]
  }], () => [{
    type: Type,
    decorators: [{
      type: Optional
    }, {
      type: Inject,
      args: [LOADING_ICON_COMPONENT]
    }]
  }], {
    loadingContainer: [{
      type: ViewChild,
      args: ["loadingContainer", {
        read: ViewContainerRef,
        static: false
      }]
    }],
    neutralColor: [{
      type: Input,
      args: ["p-neutral-color"]
    }],
    size: [{
      type: Input,
      args: ["p-size"]
    }],
    inOverlay: [{
      type: Input,
      args: ["p-in-overlay"]
    }]
  });
})();
var PoLoadingComponent = class _PoLoadingComponent extends PoLoadingBaseComponent {
  static ɵfac = /* @__PURE__ */ (() => {
    let ɵPoLoadingComponent_BaseFactory;
    return function PoLoadingComponent_Factory(__ngFactoryType__) {
      return (ɵPoLoadingComponent_BaseFactory || (ɵPoLoadingComponent_BaseFactory = ɵɵgetInheritedFactory(_PoLoadingComponent)))(__ngFactoryType__ || _PoLoadingComponent);
    };
  })();
  static ɵcmp = ɵɵdefineComponent({
    type: _PoLoadingComponent,
    selectors: [["po-loading"]],
    standalone: false,
    features: [ɵɵInheritDefinitionFeature],
    decls: 3,
    vars: 6,
    consts: [[3, "p-size", "p-in-overlay"], [1, "po-loading-label", "po-text-ellipsis"], ["aria-hidden", "true", 1, "po-loading-dot"]],
    template: function PoLoadingComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "div");
        ɵɵelement(1, "po-loading-icon", 0);
        ɵɵconditionalCreate(2, PoLoadingComponent_Conditional_2_Template, 8, 1, "span", 1);
        ɵɵelementEnd();
      }
      if (rf & 2) {
        ɵɵclassMap(ɵɵinterpolate1("po-loading po-loading-", ctx.size));
        ɵɵadvance();
        ɵɵproperty("p-size", ctx.size)("p-in-overlay", ctx.inOverlay);
        ɵɵadvance();
        ɵɵconditional(ctx.text ? 2 : -1);
      }
    },
    dependencies: [PoLoadingIconComponent],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoLoadingComponent, [{
    type: Component,
    args: [{
      selector: "po-loading",
      standalone: false,
      template: '<div class="po-loading po-loading-{{ size }}">\n  <po-loading-icon [p-size]="size" [p-in-overlay]="inOverlay"></po-loading-icon>\n\n  @if (text) {\n    <span class="po-loading-label po-text-ellipsis"\n      >{{ text }}\n      <div class="po-loading-dot" aria-hidden="true">.</div>\n      <div class="po-loading-dot" aria-hidden="true">.</div>\n      <div class="po-loading-dot" aria-hidden="true">.</div>\n    </span>\n  }\n</div>\n'
    }]
  }], null, null);
})();
var PoLoadingOverlayComponent = class _PoLoadingOverlayComponent extends PoLoadingOverlayBaseComponent {
  changeDetector = inject(ChangeDetectorRef);
  constructor() {
    const languageService = inject(PoLanguageService);
    super(languageService);
  }
  static ɵfac = function PoLoadingOverlayComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoLoadingOverlayComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoLoadingOverlayComponent,
    selectors: [["po-loading-overlay"]],
    standalone: false,
    features: [ɵɵInheritDefinitionFeature],
    decls: 2,
    vars: 4,
    consts: [[3, "p-screen-lock"], [3, "p-text", "p-size", "p-in-overlay"]],
    template: function PoLoadingOverlayComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "po-overlay", 0);
        ɵɵelement(1, "po-loading", 1);
        ɵɵelementEnd();
      }
      if (rf & 2) {
        ɵɵproperty("p-screen-lock", ctx.screenLock);
        ɵɵadvance();
        ɵɵproperty("p-text", ctx.text)("p-size", ctx.size)("p-in-overlay", true);
      }
    },
    dependencies: [PoOverlayComponent, PoLoadingComponent],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoLoadingOverlayComponent, [{
    type: Component,
    args: [{
      selector: "po-loading-overlay",
      standalone: false,
      template: '<po-overlay [p-screen-lock]="screenLock">\n  <po-loading [p-text]="text" [p-size]="size" [p-in-overlay]="true"></po-loading>\n</po-overlay>\n'
    }]
  }], () => [], null);
})();
var PoItemListBaseComponent = class _PoItemListBaseComponent {
  _label;
  _value;
  _type;
  _visible = true;
  _disabled = false;
  _activeTabs = false;
  set type(value) {
    this._type = PoItemListType[value] ?? "action";
  }
  get type() {
    return this._type;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define o estado como visível.
   *
   * @default `true`
   */
  set visible(value) {
    if (value === true || value === null || value === void 0) {
      this._visible = true;
    } else {
      this._visible = false;
    }
  }
  get visible() {
    return this._visible;
  }
  item;
  /** Texto de exibição do item. */
  label;
  /** Tamanho do texto exibido. */
  size;
  /** Valor do item. */
  value;
  danger = false;
  /**
   * @optional
   *
   * @description
   *
   * Define o estado como desabilitado.
   *
   * @default `false`
   */
  set disabled(value) {
    if (value === false || value === null || value === void 0) {
      this._disabled = false;
    } else {
      this._disabled = true;
    }
  }
  get disabled() {
    return this._disabled;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define se a ação está selecionada.
   *
   * @default `false`
   */
  selected = false;
  /**
   * @optional
   *
   * @description
   *
   * Atribui uma linha separadora acima do item.
   *
   * @default `false`
   */
  separator = false;
  /**
   * @optional
   *
   * @description
   *
   * Define um ícone que será exibido ao lado esquerdo do rótulo.
   */
  icon;
  /**
   * @optional
   *
   * @description
   *
   * Define se deve ser exibido o ícone indicando subnível.
   */
  iconArrowRight;
  // Define a posição do ícone: 'left' (padrão) ou 'right'.
  iconPosition = "left";
  /**
   * @optional
   *
   * @description
   *
   * Ação a ser realizada ao clicar no item do tipo `option`.
   */
  clickItem = new EventEmitter();
  // MULTISELECT PROPERTIES
  //emissao de evento do checkbox
  checkboxItem = new EventEmitter();
  comboItem = new EventEmitter();
  //valor do checkbox de selecionar todos
  checkboxValue;
  fieldValue = "value";
  fieldLabel = "label";
  template;
  templateContext;
  searchValue = "";
  filterMode = PoItemListFilterMode.contains;
  isFiltering = false;
  shouldMarkLetters = true;
  compareCache = false;
  comboService;
  // TABS PROPERTIES
  isTabs = false;
  tabHide = false;
  tabsItem = new EventEmitter();
  activatedTab = new EventEmitter();
  // Ativa o botão
  set activeTabs(value) {
    this._activeTabs = value;
    this.emitActiveTabs(this.item);
  }
  get activeTabs() {
    return this._activeTabs;
  }
  // Define os nomes das propriedades do objeto que serão exibidos como rótulos (labels)
  keysLabel = [];
  emitActiveTabs(tab) {
    if (tab?.active) {
      this.activatedTab.emit(tab);
    }
  }
  static ɵfac = function PoItemListBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoItemListBaseComponent)();
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoItemListBaseComponent,
    hostVars: 2,
    hostBindings: function PoItemListBaseComponent_HostBindings(rf, ctx) {
      if (rf & 2) {
        ɵɵattribute("p-type", ctx.type)("p-size", ctx.size);
      }
    },
    inputs: {
      type: [0, "p-type", "type"],
      visible: [0, "p-visible", "visible"],
      item: [0, "p-item", "item"],
      label: [0, "p-label", "label"],
      size: [0, "p-size", "size"],
      value: [0, "p-value", "value"],
      danger: [2, "p-danger", "danger", convertToBoolean],
      disabled: [0, "p-disabled", "disabled"],
      selected: [2, "p-selected", "selected", convertToBoolean],
      separator: [2, "p-separator", "separator", convertToBoolean],
      icon: [0, "p-icon", "icon"],
      iconArrowRight: [0, "p-icon-arrow-right", "iconArrowRight"],
      iconPosition: [0, "p-icon-position", "iconPosition"],
      checkboxValue: [0, "p-checkbox-value", "checkboxValue"],
      fieldValue: [0, "p-field-value", "fieldValue"],
      fieldLabel: [0, "p-field-label", "fieldLabel"],
      template: [0, "p-template", "template"],
      templateContext: [0, "p-template-context", "templateContext"],
      searchValue: [0, "p-search-value", "searchValue"],
      filterMode: [0, "p-filter-mode", "filterMode"],
      isFiltering: [0, "p-filtering", "isFiltering"],
      shouldMarkLetters: [0, "p-should-mark-letter", "shouldMarkLetters"],
      compareCache: [0, "p-compare-cache", "compareCache"],
      comboService: [0, "p-combo-service", "comboService"],
      isTabs: [0, "p-is-tabs", "isTabs"],
      tabHide: [0, "p-tab-hide", "tabHide"],
      activeTabs: [0, "p-active-tabs", "activeTabs"],
      keysLabel: [0, "p-keys-label", "keysLabel"]
    },
    outputs: {
      clickItem: "p-click-item",
      checkboxItem: "p-selectcheckbox-item",
      comboItem: "p-selectcombo-item",
      tabsItem: "p-emit-item-tabs",
      activatedTab: "p-activated-tabs"
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoItemListBaseComponent, [{
    type: Directive
  }], null, {
    type: [{
      type: HostBinding,
      args: ["attr.p-type"]
    }, {
      type: Input,
      args: ["p-type"]
    }],
    visible: [{
      type: Input,
      args: ["p-visible"]
    }],
    item: [{
      type: Input,
      args: ["p-item"]
    }],
    label: [{
      type: Input,
      args: ["p-label"]
    }],
    size: [{
      type: HostBinding,
      args: ["attr.p-size"]
    }, {
      type: Input,
      args: ["p-size"]
    }],
    value: [{
      type: Input,
      args: ["p-value"]
    }],
    danger: [{
      type: Input,
      args: [{
        alias: "p-danger",
        transform: convertToBoolean
      }]
    }],
    disabled: [{
      type: Input,
      args: ["p-disabled"]
    }],
    selected: [{
      type: Input,
      args: [{
        alias: "p-selected",
        transform: convertToBoolean
      }]
    }],
    separator: [{
      type: Input,
      args: [{
        alias: "p-separator",
        transform: convertToBoolean
      }]
    }],
    icon: [{
      type: Input,
      args: ["p-icon"]
    }],
    iconArrowRight: [{
      type: Input,
      args: ["p-icon-arrow-right"]
    }],
    iconPosition: [{
      type: Input,
      args: ["p-icon-position"]
    }],
    clickItem: [{
      type: Output,
      args: ["p-click-item"]
    }],
    checkboxItem: [{
      type: Output,
      args: ["p-selectcheckbox-item"]
    }],
    comboItem: [{
      type: Output,
      args: ["p-selectcombo-item"]
    }],
    checkboxValue: [{
      type: Input,
      args: ["p-checkbox-value"]
    }],
    fieldValue: [{
      type: Input,
      args: ["p-field-value"]
    }],
    fieldLabel: [{
      type: Input,
      args: ["p-field-label"]
    }],
    template: [{
      type: Input,
      args: ["p-template"]
    }],
    templateContext: [{
      type: Input,
      args: ["p-template-context"]
    }],
    searchValue: [{
      type: Input,
      args: ["p-search-value"]
    }],
    filterMode: [{
      type: Input,
      args: ["p-filter-mode"]
    }],
    isFiltering: [{
      type: Input,
      args: ["p-filtering"]
    }],
    shouldMarkLetters: [{
      type: Input,
      args: ["p-should-mark-letter"]
    }],
    compareCache: [{
      type: Input,
      args: ["p-compare-cache"]
    }],
    comboService: [{
      type: Input,
      args: ["p-combo-service"]
    }],
    isTabs: [{
      type: Input,
      args: ["p-is-tabs"]
    }],
    tabHide: [{
      type: Input,
      args: ["p-tab-hide"]
    }],
    tabsItem: [{
      type: Output,
      args: ["p-emit-item-tabs"]
    }],
    activatedTab: [{
      type: Output,
      args: ["p-activated-tabs"]
    }],
    activeTabs: [{
      type: Input,
      args: ["p-active-tabs"]
    }],
    keysLabel: [{
      type: Input,
      args: ["p-keys-label"]
    }]
  });
})();
var PoKeyCodeEnum;
(function(PoKeyCodeEnum2) {
  PoKeyCodeEnum2[PoKeyCodeEnum2["arrowDown"] = 40] = "arrowDown";
  PoKeyCodeEnum2[PoKeyCodeEnum2["arrowUp"] = 38] = "arrowUp";
  PoKeyCodeEnum2[PoKeyCodeEnum2["backspace"] = 8] = "backspace";
  PoKeyCodeEnum2[PoKeyCodeEnum2["delete"] = 46] = "delete";
  PoKeyCodeEnum2[PoKeyCodeEnum2["enter"] = 13] = "enter";
  PoKeyCodeEnum2[PoKeyCodeEnum2["esc"] = 27] = "esc";
  PoKeyCodeEnum2[PoKeyCodeEnum2["keyK"] = 75] = "keyK";
  PoKeyCodeEnum2[PoKeyCodeEnum2["keyL"] = 76] = "keyL";
  PoKeyCodeEnum2[PoKeyCodeEnum2["space"] = 32] = "space";
  PoKeyCodeEnum2[PoKeyCodeEnum2["tab"] = 9] = "tab";
  PoKeyCodeEnum2[PoKeyCodeEnum2["left"] = 37] = "left";
  PoKeyCodeEnum2[PoKeyCodeEnum2["right"] = 39] = "right";
})(PoKeyCodeEnum || (PoKeyCodeEnum = {}));
var PoCheckboxSize;
(function(PoCheckboxSize2) {
  PoCheckboxSize2["Small"] = "small";
  PoCheckboxSize2["Medium"] = "medium";
  PoCheckboxSize2["Large"] = "large";
})(PoCheckboxSize || (PoCheckboxSize = {}));
var PoCheckboxBaseComponent = class _PoCheckboxBaseComponent {
  cd;
  // Propriedade interna que define se o ícone de ajuda adicional terá cursor clicável (evento) ou padrão (tooltip).
  additionalHelpEventTrigger;
  /**
   *
   * @deprecated v23.x.x use `p-helper`
   *
   * @optional
   *
   * @description
   * Exibe um ícone de ajuda adicional, com o texto desta propriedade sendo passado para o popover do componente `po-helper`.
   * **Como boa prática, indica-se utilizar um texto com até 140 caracteres.**
   * > Requer um recuo mínimo de 8px se o componente estiver próximo à lateral da tela.
   *
   * > Essa propriedade está **depreciada** e será removida na versão `23.x.x`. Recomendamos utilizar a propriedade `p-helper` que oferece mais recursos e flexibilidade.
   */
  additionalHelpTooltip;
  /**
   * @optional
   *
   * @description
   *
   * Define que o popover (`p-helper`) será incluído no body da página e não dentro do componente. Essa
   * opção pode ser necessária em cenários com containers que possuem scroll ou overflow escondido, garantindo o
   * posicionamento correto do tooltip próximo ao elemento.
   *
   * > Quando utilizado com `p-helper`, leitores de tela como o NVDA podem não ler o conteúdo do popover.
   *
   * @default `false`
   */
  appendBox = false;
  /**
   * @optional
   *
   * @description
   * Define se o título do campo será exibido de forma compacta.
   *
   * Quando habilitado (`true`), o modo compacto afeta o conjunto composto por:
   * - `po-label`
   * - `p-requirement (showRequired)`
   * - `po-helper`
   *
   * Ou seja, todos os elementos relacionados ao título do campo
   * (rótulo, indicador de obrigatoriedade e componente auxiliar) passam
   * a seguir o comportamento de layout compacto.
   *
   * Também é possível definir esse comportamento de forma global,
   * uma única vez, na folha de estilo geral da aplicação, por meio
   * da customização dos tokens CSS:
   *
   * - `--field-container-title-justify`
   * - `--field-container-title-flex`
   *
   * Exemplo:
   *
   * ```
   * :root {
   *   --field-container-title-justify: flex-start;
   *   --field-container-title-flex: 0 1 auto;
   * }
   * ```
   *
   * Dessa forma, o layout compacto passa a ser o padrão da aplicação,
   * sem a necessidade de definir a propriedade individualmente em cada campo.
   *
   * @default `false`
   */
  compactLabel = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "compactLabel"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-compact-label",
    transform: convertToBoolean
  }));
  /**
   * @optional
   *
   * @description
   * Texto de apoio do campo */
  help;
  /** Define o nome do *checkbox*. */
  name;
  /**
   * @optional
   *
   * @description
   *
   * Aplica foco no elemento ao ser iniciado.
   *
   * > Caso mais de um elemento seja configurado com essa propriedade, apenas o último elemento declarado com ela terá o foco.
   *
   * @default `false`
   */
  autoFocus = false;
  /** Texto de exibição do *checkbox*. */
  label;
  /**
   *
   * @deprecated v23.x.x use `p-helper`
   *
   * @optional
   *
   * @description
   * Evento disparado ao clicar no ícone de ajuda adicional.
   *
   * > Essa propriedade está **depreciada** e será removida na versão `23.x.x`. Recomendamos utilizar a propriedade `p-helper` que oferece mais recursos e flexibilidade.
   */
  additionalHelp = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Evento disparado ao sair do campo.
   */
  blur = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Evento disparado quando o valor do *checkbox* for alterado.
   */
  change = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Evento disparado sempre que o valor do model é alterado, seja por interação do usuário
   * ou por atualização programática (ex: `setValue`, `patchValue`, carregamento assíncrono).
   *
   * Diferentemente do `p-change`, que é disparado apenas por interação do usuário,
   * o `p-change-model` cobre todos os cenários de alteração de valor.
   *
   * Não emite quando o novo valor é idêntico ao anterior (deduplicação automática).
   */
  changeModel = new EventEmitter();
  /**
   * @optional
   *
   * @description
   * Evento disparado quando uma tecla é pressionada enquanto o foco está no componente.
   * Retorna um objeto `KeyboardEvent` com informações sobre a tecla.
   */
  keydown = new EventEmitter();
  //propriedade interna recebida do checkbox-group para verificar se o checkbox está ativo, inativo ou indeterminate
  checkboxValue;
  //propriedade interna recebida do checkbox-group para verificar se o checkbox é required
  checkBoxRequired;
  //propriedade interna recebida para desabilitar o tabindex do checkbox na utilização dentro de um list-box
  disabladTabindex = false;
  displayAdditionalHelp = false;
  id = uuid();
  modelLastUpdate;
  propagateChange;
  onTouched;
  _disabled = false;
  _size = void 0;
  _initialSize = void 0;
  /**
   * @optional
   *
   * @description
   *
   * Define o estado do *checkbox* como desabilitado.
   *
   * @default `false`
   */
  set disabled(value) {
    this._disabled = value;
  }
  get disabled() {
    return this._disabled;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define o tamanho da caixa de seleção do componente:
   * - `small`: 16x16 (disponível apenas para acessibilidade AA).
   * - `medium`: 24x24.
   * - `large`: 32x32.
   *
   * > Caso a acessibilidade AA não esteja configurada, o tamanho `medium` será mantido.
   * Para mais detalhes, consulte a documentação do [po-theme](https://po-ui.io/documentation/po-theme).
   *
   * @default `medium`
   *
   */
  set size(value) {
    this._initialSize = value;
    this.applySizeBasedOnA11y();
  }
  get size() {
    return this._size ?? getDefaultSizeFn(PoCheckboxSize);
  }
  /**
   * @optional
   *
   * @description
   *
   * Define as opções do componente de ajuda (po-helper) que será exibido ao lado do label quando a propriedade `p-label` for definida, ou, ao lado do componente na ausência da propriedade `p-label`.
   * > Para mais informações acesse: https://po-ui.io/documentation/po-helper.
   *
   * > Ao configurar esta propriedade, o antigo ícone de ajuda adicional (`p-additional-help-tooltip` e `p-additional-help`) será ignorado.
   */
  poHelperComponent = input(void 0, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "poHelperComponent"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-helper"
  }));
  /**
   * @optional
   *
   * @description
   *
   * Habilita a quebra automática do texto da propriedade `p-label`. Quando `p-label-text-wrap` for verdadeiro, o texto que excede
   * o espaço disponível é transferido para a próxima linha em pontos apropriados para uma
   * leitura clara.
   *
   * @default `false`
   */
  labelTextWrap = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "labelTextWrap"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-label-text-wrap"
  }));
  constructor(cd) {
    this.cd = cd;
  }
  onThemeChange() {
    this.applySizeBasedOnA11y();
  }
  changeValue() {
    if (this.propagateChange) {
      this.propagateChange(this.checkboxValue);
    }
    this.change.emit(this.checkboxValue);
    this.controlChangeModelEmitter(this.checkboxValue);
  }
  checkOption(event, value) {
    const target = event.target;
    if (target.closest("po-helper")) {
      return;
    }
    if (!this.disabled) {
      value === "mixed" ? this.changeModelValue(true) : this.changeModelValue(!value);
      this.changeValue();
    }
  }
  // Função implementada do ControlValueAccessor
  // Usada para interceptar os estados de habilitado via forms api
  setDisabledState(isDisabled) {
    this.disabled = isDisabled;
    this.cd.markForCheck();
  }
  registerOnChange(fn) {
    this.propagateChange = fn;
  }
  registerOnTouched(fn) {
    this.onTouched = fn;
  }
  writeValue(value) {
    if (value !== this.checkboxValue) {
      this.changeModelValue(value);
    }
    if (value != null) {
      this.controlChangeModelEmitter(this.checkboxValue);
    }
  }
  controlChangeModelEmitter(value) {
    if (this.modelLastUpdate !== value) {
      this.changeModel.emit(value);
      this.modelLastUpdate = value;
    }
  }
  applySizeBasedOnA11y() {
    const size = validateSizeFn(this._initialSize, PoCheckboxSize);
    this._size = size;
  }
  static ɵfac = function PoCheckboxBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoCheckboxBaseComponent)(ɵɵdirectiveInject(ChangeDetectorRef));
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoCheckboxBaseComponent,
    selectors: [["po-checkbox-base"]],
    hostVars: 1,
    hostBindings: function PoCheckboxBaseComponent_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("PoUiThemeChange", function PoCheckboxBaseComponent_PoUiThemeChange_HostBindingHandler() {
          return ctx.onThemeChange();
        }, ɵɵresolveWindow);
      }
      if (rf & 2) {
        ɵɵattribute("p-size", ctx.size);
      }
    },
    inputs: {
      additionalHelpEventTrigger: "additionalHelpEventTrigger",
      additionalHelpTooltip: [0, "p-additional-help-tooltip", "additionalHelpTooltip"],
      appendBox: [2, "p-append-in-body", "appendBox", convertToBoolean],
      compactLabel: [1, "p-compact-label", "compactLabel"],
      help: [0, "p-help", "help"],
      name: "name",
      autoFocus: [2, "p-auto-focus", "autoFocus", convertToBoolean],
      label: [0, "p-label", "label"],
      checkboxValue: [0, "p-checkboxValue", "checkboxValue"],
      checkBoxRequired: [2, "p-required", "checkBoxRequired", convertToBoolean],
      disabladTabindex: [2, "p-disabled-tabindex", "disabladTabindex", convertToBoolean],
      disabled: [2, "p-disabled", "disabled", convertToBoolean],
      size: [0, "p-size", "size"],
      poHelperComponent: [1, "p-helper", "poHelperComponent"],
      labelTextWrap: [1, "p-label-text-wrap", "labelTextWrap"]
    },
    outputs: {
      additionalHelp: "p-additional-help",
      blur: "p-blur",
      change: "p-change",
      changeModel: "p-change-model",
      keydown: "p-keydown"
    },
    standalone: false,
    decls: 0,
    vars: 0,
    template: function PoCheckboxBaseComponent_Template(rf, ctx) {
    },
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoCheckboxBaseComponent, [{
    type: Component,
    args: [{
      selector: "po-checkbox-base",
      template: "",
      standalone: false
    }]
  }], () => [{
    type: ChangeDetectorRef
  }], {
    additionalHelpEventTrigger: [{
      type: Input
    }],
    additionalHelpTooltip: [{
      type: Input,
      args: ["p-additional-help-tooltip"]
    }],
    appendBox: [{
      type: Input,
      args: [{
        alias: "p-append-in-body",
        transform: convertToBoolean
      }]
    }],
    compactLabel: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-compact-label",
        required: false
      }]
    }],
    help: [{
      type: Input,
      args: ["p-help"]
    }],
    name: [{
      type: Input,
      args: ["name"]
    }],
    autoFocus: [{
      type: Input,
      args: [{
        alias: "p-auto-focus",
        transform: convertToBoolean
      }]
    }],
    label: [{
      type: Input,
      args: ["p-label"]
    }],
    additionalHelp: [{
      type: Output,
      args: ["p-additional-help"]
    }],
    blur: [{
      type: Output,
      args: ["p-blur"]
    }],
    change: [{
      type: Output,
      args: ["p-change"]
    }],
    changeModel: [{
      type: Output,
      args: ["p-change-model"]
    }],
    keydown: [{
      type: Output,
      args: ["p-keydown"]
    }],
    checkboxValue: [{
      type: Input,
      args: ["p-checkboxValue"]
    }],
    checkBoxRequired: [{
      type: Input,
      args: [{
        alias: "p-required",
        transform: convertToBoolean
      }]
    }],
    disabladTabindex: [{
      type: Input,
      args: [{
        alias: "p-disabled-tabindex",
        transform: convertToBoolean
      }]
    }],
    disabled: [{
      type: Input,
      args: [{
        alias: "p-disabled",
        transform: convertToBoolean
      }]
    }],
    size: [{
      type: Input,
      args: ["p-size"]
    }, {
      type: HostBinding,
      args: ["attr.p-size"]
    }],
    poHelperComponent: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-helper",
        required: false
      }]
    }],
    labelTextWrap: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-label-text-wrap",
        required: false
      }]
    }],
    onThemeChange: [{
      type: HostListener,
      args: ["window:PoUiThemeChange"]
    }]
  });
})();
var PoButtonKind;
(function(PoButtonKind2) {
  PoButtonKind2["primary"] = "primary";
  PoButtonKind2["secondary"] = "secondary";
  PoButtonKind2["tertiary"] = "tertiary";
})(PoButtonKind || (PoButtonKind = {}));
var PoButtonSize;
(function(PoButtonSize2) {
  PoButtonSize2["Small"] = "small";
  PoButtonSize2["Medium"] = "medium";
  PoButtonSize2["Large"] = "large";
})(PoButtonSize || (PoButtonSize = {}));
var PoButtonType;
(function(PoButtonType2) {
  PoButtonType2["Submit"] = "submit";
  PoButtonType2["Button"] = "button";
  PoButtonType2["Reset"] = "reset";
})(PoButtonType || (PoButtonType = {}));
var PoButtonBaseComponent = class _PoButtonBaseComponent {
  /**
   * @optional
   *
   * @description
   *
   * Label do botão.
   */
  label = input(void 0, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "label"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-label"
  }));
  /**
   * @optional
   *
   * @description
   * Ícone exibido ao lado esquerdo do label do botão.
   *
   * É possível usar qualquer um dos ícones da [Biblioteca de ícones](https://po-ui.io/icons), conforme exemplo:
   * ```
   * <po-button p-icon="an an-user" p-label="PO button"></po-button>
   * ```
   * Também é possível utilizar outras fontes de ícones, por exemplo a biblioteca *Font Awesome*, desde que a biblioteca
   * esteja carregada no projeto:
   * ```
   * <po-button p-icon="fa fa-podcast" p-label="PO button"></po-button>
   * ```
   * Outra opção seria a customização do ícone através do `TemplateRef`, conforme exemplo abaixo:
   * ```
   * <po-button [p-icon]="template" p-label="button template ionic"></po-button>
   *
   * <ng-template #template>
   *  <ion-icon style="font-size: inherit" name="heart"></ion-icon>
   * </ng-template>
   * ```
   * > Para o ícone enquadrar corretamente, deve-se utilizar `font-size: inherit` caso o ícone utilizado não aplique-o.
   */
  icon = input(void 0, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "icon"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-icon"
  }));
  /**
   * @optional
   *
   * @description
   * Define o tipo do botão.
   *
   * @default `PoButtonType.Button`
   */
  type = input(PoButtonType.Button, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "type"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-type"
  }));
  // Evento disparado ao sair do campo.
  blur = new EventEmitter();
  /** Ação que será executada quando o usuário clicar sobre o `po-button`. */
  click = new EventEmitter();
  _danger = false;
  _disabled = false;
  _loading = false;
  _kind = PoButtonKind.secondary;
  _size = void 0;
  _initialSize = void 0;
  hasSize = false;
  /**
   * @optional
   *
   * @description
   *
   * Exibe um ícone de carregamento à esquerda do _label_ do botão.
   *
   * > Quando esta propriedade estiver habilitada, desabilitará o botão.
   *
   * @default `false`
   */
  set loading(value) {
    this._loading = convertToBoolean(value);
  }
  get loading() {
    return this._loading;
  }
  /**
   * @optional
   *
   * @description
   *
   * Deve ser usado em ações irreversíveis que o usuário precisa ter cuidado ao executá-la, como a exclusão de um registro.
   *
   * > A propriedade `p-kind="tertiary"` será inativada ao utilizar esta propriedade.
   */
  set danger(value) {
    this._danger = this.kind !== PoButtonKind.tertiary ? value : false;
  }
  get danger() {
    return this._danger;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define o estilo visual do componente conforme valores especificados no enum `PoButtonKind`:
   *  - `primary`: destaca o botão, sendo recomendado para ações principais.
   *  - `secondary`: estilo padrão, ideal para ações secundárias.
   *  - `tertiary`: exibe o botão sem preenchimento no fundo, indicado para ações opcionais.
   *
   * @default `secondary`
   */
  set kind(value) {
    this._kind = PoButtonKind[value] ? PoButtonKind[value] : PoButtonKind.secondary;
  }
  get kind() {
    return this._kind;
  }
  /**
   * @optional
   *
   * @description
   *
   * Desabilita o `po-button` e não permite que o usuário interaja com o mesmo.
   *
   * @default `false`
   */
  set disabled(value) {
    this._disabled = value === "" ? true : convertToBoolean(value);
  }
  get disabled() {
    return this._disabled;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define um `aria-label` para o `po-button`.
   *
   * Caso esta propriedade não seja informada será considerada a label do botão.
   *
   * > Em caso de botões com apenas ícone a atribuição de valor à esta propriedade é muito importante para acessibilidade.
   */
  ariaLabel = input(void 0, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "ariaLabel"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-aria-label"
  }));
  ariaExpanded = null;
  /**
   * @optional
   *
   * @description
   *
   * Define o `tabindex` do elemento `<button>` nativo interno.
   *
   * Use `[tabindex]="-1"` para remover o botão da ordem de foco do teclado.
   */
  tabindex = null;
  /**
   * @optional
   *
   * @description
   *
   * Define o tamanho do componente:
   * - `small`: altura de 32px (disponível apenas para acessibilidade AA).
   * - `medium`: altura de 44px.
   * - `large`: altura de 56px.
   *
   * > Caso a acessibilidade AA não esteja configurada, o tamanho `medium` será mantido.
   * Para mais detalhes, consulte a documentação do [po-theme](https://po-ui.io/documentation/po-theme).
   *
   * @default `medium`
   */
  set size(value) {
    this._initialSize = value;
    this.applySizeBasedOnA11y();
  }
  get size() {
    return this._size ?? getDefaultSizeFn(PoButtonSize);
  }
  onThemeChange() {
    this.applySizeBasedOnA11y();
  }
  applySizeBasedOnA11y() {
    const size = validateSizeFn(this._initialSize, PoButtonSize);
    this._size = size;
  }
  static ɵfac = function PoButtonBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoButtonBaseComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoButtonBaseComponent,
    selectors: [["po-button-base"]],
    hostVars: 3,
    hostBindings: function PoButtonBaseComponent_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("PoUiThemeChange", function PoButtonBaseComponent_PoUiThemeChange_HostBindingHandler() {
          return ctx.onThemeChange();
        }, ɵɵresolveWindow);
      }
      if (rf & 2) {
        ɵɵattribute("p-danger", ctx.danger)("p-kind", ctx.kind)("p-size", ctx.size);
      }
    },
    inputs: {
      label: [1, "p-label", "label"],
      icon: [1, "p-icon", "icon"],
      type: [1, "p-type", "type"],
      loading: [0, "p-loading", "loading"],
      danger: [2, "p-danger", "danger", convertToBoolean],
      kind: [0, "p-kind", "kind"],
      disabled: [0, "p-disabled", "disabled"],
      ariaLabel: [1, "p-aria-label", "ariaLabel"],
      ariaExpanded: [0, "p-aria-expanded", "ariaExpanded"],
      tabindex: [0, "p-tabindex", "tabindex"],
      size: [0, "p-size", "size"]
    },
    outputs: {
      blur: "p-blur",
      click: "p-click"
    },
    standalone: false,
    decls: 0,
    vars: 0,
    template: function PoButtonBaseComponent_Template(rf, ctx) {
    },
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoButtonBaseComponent, [{
    type: Component,
    args: [{
      selector: "po-button-base",
      template: "",
      standalone: false
    }]
  }], null, {
    label: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-label",
        required: false
      }]
    }],
    icon: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-icon",
        required: false
      }]
    }],
    type: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-type",
        required: false
      }]
    }],
    blur: [{
      type: Output,
      args: ["p-blur"]
    }],
    click: [{
      type: Output,
      args: ["p-click"]
    }],
    loading: [{
      type: Input,
      args: ["p-loading"]
    }],
    danger: [{
      type: HostBinding,
      args: ["attr.p-danger"]
    }, {
      type: Input,
      args: [{
        alias: "p-danger",
        transform: convertToBoolean
      }]
    }],
    kind: [{
      type: HostBinding,
      args: ["attr.p-kind"]
    }, {
      type: Input,
      args: ["p-kind"]
    }],
    disabled: [{
      type: Input,
      args: ["p-disabled"]
    }],
    ariaLabel: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-aria-label",
        required: false
      }]
    }],
    ariaExpanded: [{
      type: Input,
      args: ["p-aria-expanded"]
    }],
    tabindex: [{
      type: Input,
      args: ["p-tabindex"]
    }],
    size: [{
      type: Input,
      args: ["p-size"]
    }, {
      type: HostBinding,
      args: ["attr.p-size"]
    }],
    onThemeChange: [{
      type: HostListener,
      args: ["window:PoUiThemeChange"]
    }]
  });
})();
var PoButtonComponent = class _PoButtonComponent extends PoButtonBaseComponent {
  elementRef;
  buttonElement;
  constructor(elementRef) {
    super();
    this.elementRef = elementRef;
  }
  onBlur() {
    this.blur.emit();
  }
  /**
   * Função que atribui foco ao componente.
   *
   * Para utilizá-la é necessário ter a instância do componente no DOM, podendo ser utilizado o ViewChild da seguinte forma:
   *
   * ```
   * import { PoButtonComponent } from '@po-ui/ng-components';
   *
   * ...
   *
   * @ViewChild(PoButtonComponent, { static: true }) button: PoButtonComponent;
   *
   * focusButton() {
   *   this.button.focus();
   * }
   * ```
   */
  focus() {
    if (!this.disabled) {
      this.buttonElement.nativeElement.focus();
    }
  }
  mapSizeToIcon(size) {
    const sizeMap = {
      small: "xs",
      medium: "sm",
      large: "sm"
    };
    return sizeMap[size] || "sm";
  }
  onClick() {
    this.click.emit(null);
  }
  static ɵfac = function PoButtonComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoButtonComponent)(ɵɵdirectiveInject(ElementRef));
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoButtonComponent,
    selectors: [["po-button"]],
    viewQuery: function PoButtonComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c13, 7);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.buttonElement = _t.first);
      }
    },
    standalone: false,
    features: [ɵɵInheritDefinitionFeature],
    decls: 6,
    vars: 10,
    consts: [["button", ""], [1, "po-button", 3, "blur", "click", "type", "disabled"], [1, "po-button-loading-icon"], [1, "po-button-container"], [1, "po-button-icon", 3, "p-icon"], [1, "po-button-label"], [3, "p-size"]],
    template: function PoButtonComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "button", 1, 0);
        ɵɵlistener("blur", function PoButtonComponent_Template_button_blur_0_listener() {
          return ctx.onBlur();
        })("click", function PoButtonComponent_Template_button_click_0_listener() {
          return ctx.onClick();
        });
        ɵɵconditionalCreate(2, PoButtonComponent_Conditional_2_Template, 2, 1, "div", 2);
        ɵɵelementStart(3, "div", 3);
        ɵɵconditionalCreate(4, PoButtonComponent_Conditional_4_Template, 1, 1, "po-icon", 4);
        ɵɵconditionalCreate(5, PoButtonComponent_Conditional_5_Template, 2, 1, "span", 5);
        ɵɵelementEnd()();
      }
      if (rf & 2) {
        ɵɵproperty("type", ctx.type())("disabled", ctx.disabled || ctx.loading);
        ɵɵattribute("p-kind", ctx.kind)("p-danger", ctx.danger)("aria-label", ctx.ariaLabel())("aria-expanded", ctx.ariaExpanded)("tabindex", ctx.tabindex);
        ɵɵadvance(2);
        ɵɵconditional(ctx.loading ? 2 : -1);
        ɵɵadvance(2);
        ɵɵconditional(ctx.icon() ? 4 : -1);
        ɵɵadvance();
        ɵɵconditional(ctx.label() ? 5 : -1);
      }
    },
    dependencies: [PoLoadingIconComponent, PoIconComponent],
    encapsulation: 2,
    changeDetection: 0
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoButtonComponent, [{
    type: Component,
    args: [{
      selector: "po-button",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: '<button\n  #button\n  class="po-button"\n  [type]="type()"\n  [attr.p-kind]="kind"\n  [attr.p-danger]="danger"\n  [disabled]="disabled || loading"\n  [attr.aria-label]="ariaLabel()"\n  [attr.aria-expanded]="ariaExpanded"\n  [attr.tabindex]="tabindex"\n  (blur)="onBlur()"\n  (click)="onClick()"\n>\n  @if (loading) {\n    <div class="po-button-loading-icon">\n      <po-loading-icon [p-size]="mapSizeToIcon(size)"></po-loading-icon>\n    </div>\n  }\n\n  <div class="po-button-container">\n    @if (icon()) {\n      <po-icon class="po-button-icon" [p-icon]="icon()"></po-icon>\n    }\n    @if (label()) {\n      <span class="po-button-label">{{ label() }}</span>\n    }\n  </div>\n</button>\n'
    }]
  }], () => [{
    type: ElementRef
  }], {
    buttonElement: [{
      type: ViewChild,
      args: ["button", {
        static: true
      }]
    }]
  });
})();
var PoOverlayModule = class _PoOverlayModule {
  static ɵfac = function PoOverlayModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoOverlayModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoOverlayModule,
    declarations: [PoOverlayComponent],
    imports: [CommonModule],
    exports: [PoOverlayComponent]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [CommonModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoOverlayModule, [{
    type: NgModule,
    args: [{
      declarations: [PoOverlayComponent],
      imports: [CommonModule],
      exports: [PoOverlayComponent]
    }]
  }], null, null);
})();
var PoLoadingModule = class _PoLoadingModule {
  static ɵfac = function PoLoadingModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoLoadingModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoLoadingModule,
    declarations: [PoLoadingComponent, PoLoadingIconComponent, PoLoadingOverlayComponent],
    imports: [CommonModule, PoLanguageModule, PoOverlayModule],
    exports: [PoLoadingComponent, PoLoadingIconComponent, PoLoadingOverlayComponent]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [CommonModule, PoLanguageModule, PoOverlayModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoLoadingModule, [{
    type: NgModule,
    args: [{
      declarations: [PoLoadingComponent, PoLoadingIconComponent, PoLoadingOverlayComponent],
      exports: [PoLoadingComponent, PoLoadingIconComponent, PoLoadingOverlayComponent],
      imports: [CommonModule, PoLanguageModule, PoOverlayModule]
    }]
  }], null, null);
})();
var PoButtonModule = class _PoButtonModule {
  static ɵfac = function PoButtonModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoButtonModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoButtonModule,
    declarations: [PoButtonComponent],
    imports: [CommonModule, PoLoadingModule, PoIconModule],
    exports: [PoButtonComponent]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [CommonModule, PoLoadingModule, PoIconModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoButtonModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, PoLoadingModule, PoIconModule],
      declarations: [PoButtonComponent],
      exports: [PoButtonComponent]
    }]
  }], null, null);
})();
var PoHelperBaseComponent = class _PoHelperBaseComponent {
  /**
   * @optional
   *
   * @description
   *
   * Define o conteúdo e as opções do popover de ajuda/informação.
   *
   * Aceita uma string simples (exibida como conteúdo) ou um objeto do tipo `PoHelperOptions` para configuração avançada:
   * - `title`: Título do popover.
   * - `content`: Conteúdo explicativo exibido no popover.
   * - `type`: Tipo do ícone (`help` ou `info`).
   * - `eventOnClick`: Função chamada ao clicar no ícone.
   * - `footerAction`: Objeto com `label` e `action` para ação customizada no rodapé do popover.
   *
   * Exemplo de uso:
   * ```html
   * <po-helper p-helper="Texto explicativo"></po-helper>
   * <po-helper [p-helper]="{ title: 'Ajuda', content: 'Texto', type: 'help' }"></po-helper>
   * ```
   *
   */
  helper = input(void 0, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "helper"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-helper",
    transform: this.transformHelper.bind(this)
  }));
  /** @docsPrivate */
  customAction = input(void 0, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "customAction"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-custom-action"
  }));
  /**
   * @optional
   *
   * @description
   *
   * Define o tamanho do componente:
   * - `small`: altura do ícone com seu valor de 16px (disponível apenas para acessibilidade AA).
   * - `medium`: altura do ícone com seu valor de 24px.
   *
   * > Caso a acessibilidade AA não esteja configurada, o tamanho `medium` será mantido.
   * Para mais detalhes, consulte a documentação do [po-theme](https://po-ui.io/documentation/po-theme).
   *
   * @default `medium`
   */
  size = input(void 0, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "size"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-size"
  }));
  get hostSize() {
    return validateSizeFn(this.size(), PoFieldSize);
  }
  /**
   * @optional
   *
   * @description
   *
   * Indica se o helper deve ser exibido no estado desativado, desabilitando interações do usuário.
   *
   * @default `false`
   */
  disabled = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "disabled"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-disabled"
  }));
  /**
   * @optional
   *
   * @description
   * Define que o popover será inserido no body da página em vez do elemento definido em `p-target`. Essa opção pode
   * ser necessária em cenários com containers que possuem scroll ou overflow escondido, garantindo o posicionamento
   * correto do conteúdo próximo ao elemento.
   */
  appendBox = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "appendBox"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-append-in-body"
  }));
  transformHelper(value) {
    if (!value) return;
    if (typeof value === "string") {
      return {
        title: "",
        content: value,
        type: "help"
      };
    }
    if (!value.type) {
      value.type = "help";
    }
    if (value.type === "info" && value.footerAction) {
      delete value.footerAction;
    }
    return value;
  }
  static ɵfac = function PoHelperBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoHelperBaseComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoHelperBaseComponent,
    selectors: [["po-helper-base"]],
    hostVars: 1,
    hostBindings: function PoHelperBaseComponent_HostBindings(rf, ctx) {
      if (rf & 2) {
        ɵɵattribute("p-size", ctx.hostSize);
      }
    },
    inputs: {
      helper: [1, "p-helper", "helper"],
      customAction: [1, "p-custom-action", "customAction"],
      size: [1, "p-size", "size"],
      disabled: [1, "p-disabled", "disabled"],
      appendBox: [1, "p-append-in-body", "appendBox"]
    },
    standalone: false,
    decls: 0,
    vars: 0,
    template: function PoHelperBaseComponent_Template(rf, ctx) {
    },
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoHelperBaseComponent, [{
    type: Component,
    args: [{
      selector: "po-helper-base",
      template: "",
      standalone: false
    }]
  }], null, {
    helper: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-helper",
        required: false
      }]
    }],
    customAction: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-custom-action",
        required: false
      }]
    }],
    size: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-size",
        required: false
      }]
    }],
    hostSize: [{
      type: HostBinding,
      args: ["attr.p-size"]
    }],
    disabled: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-disabled",
        required: false
      }]
    }],
    appendBox: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-append-in-body",
        required: false
      }]
    }]
  });
})();
var TAG_NORMALIZE_MAP = {
  strong: "b",
  em: "i"
};
var TAG_REGEX = /<(\/?)(\w+)(?:\s[^>]*)?\/?>/gi;
function sanitizeContent(input2, allowedTags) {
  return input2.replace(TAG_REGEX, (_match, closing, tagName) => {
    const normalized = tagName.toLowerCase();
    if (allowedTags.includes(normalized)) {
      const output2 = TAG_NORMALIZE_MAP[normalized] || normalized;
      return closing ? `</${output2}>` : `<${output2}>`;
    }
    return "";
  });
}
function parseSafeText(content, allowedTags) {
  if (!content) {
    return [];
  }
  const sanitized = sanitizeContent(content, allowedTags);
  const fragments = [];
  const counters2 = {
    b: 0,
    i: 0,
    u: 0
  };
  const parts = sanitized.split(/(<\/?[biu]>)/gi);
  for (const part of parts) {
    if (!part) {
      continue;
    }
    const tagMatch = /^<(\/?)([biu])>$/i.exec(part);
    if (tagMatch) {
      const tag = tagMatch[2].toLowerCase();
      if (tagMatch[1] === "/") {
        counters2[tag] = Math.max(0, counters2[tag] - 1);
      } else {
        counters2[tag]++;
      }
    } else {
      fragments.push({
        text: part,
        bold: counters2.b > 0,
        italic: counters2.i > 0,
        underline: counters2.u > 0
      });
    }
  }
  return fragments;
}
var PO_POPOVER_DEFAULT_POSITION = "right";
var PO_POPOVER_DEFAULT_TRIGGER = "click";
var PO_POPOVER_TRIGGERS = ["click", "hover", "function"];
var PoPopoverBaseComponent = class _PoPopoverBaseComponent {
  /**
   * @optional
   *
   * @description
   *
   * Define que o popover será inserido no body da página em vez do elemento definido em `p-target`. Essa opção pode
   * ser necessária em cenários com containers que possuem scroll ou overflow escondido, garantindo o posicionamento
   * correto do conteúdo próximo ao elemento.
   *
   * @default `false`
   */
  appendBox = false;
  // Indica se o elemento filho será alinhado nos cantos do elemento pai.
  cornerAligned = false;
  /**
   * @optional
   *
   * @description
   *
   * Define o espaçamento (em pixels) entre o componente alvo (`p-target`) e o `po-popover`.
   * Útil para componentes com targets pequenos (como ícones) onde o espaçamento padrão é
   * proporcionalmente muito grande e cria desalinhamento visual.
   *
   * @default `8`
   */
  offset = 8;
  /**
   * @description
   *
   * ElementRef do componente de origem responsável por abrir o popover.
   * Para utilizar o po-popover deve-se colocar uma variável no componente que vai disparar o evento
   * de abertura, exemplo:
   *
   * ```
   * <po-button
   *   p-label="Open Popover">
   * </po-button>
   *
   * <po-popover
   *   [p-target]="poButton"
   *   [p-title]="PO Popover">
   * </po-popover>
   * ```
   *
   * Também deve-se criar um ViewChild para cada popover, passando como referência o elemento do
   * HTML que irá disparar o evento. Exemplo:
   *
   * ```
   * @ViewChild(PoButtonComponent, {read: ElementRef}) poButton: PoButtonComponent;
   * ```
   *
   * Pode-se tambem informar diretamente o HTMLElement, para não ter que utilizar o ViewChild.
   * Para utilizar o po-popover deve-se colocar uma variável no componente que vai disparar o evento
   * de abertura, exemplo:
   *
   * ```
   * <button #target>
   *   Abrir popover
   * </button>
   *
   * <po-popover
   *     [p-target]="target"
   *     p-trigger="click" >
   * </po-popover>
   * ```
   *
   *
   *
   */
  target;
  /** Título do popover. */
  title;
  // Define o width do popover.
  width;
  /** Evento disparado ao fechar o popover. */
  closePopover = new EventEmitter();
  /** Evento disparado ao abrir o popover. */
  openPopover = new EventEmitter();
  // Controla se o popover fica oculto ou visível, por padrão é oculto.
  isHidden = true;
  clickoutListener;
  mouseEnterListener;
  mouseLeaveListener;
  resizeListener;
  _hideArrow = false;
  _position = PO_POPOVER_DEFAULT_POSITION;
  _trigger = PO_POPOVER_DEFAULT_TRIGGER;
  /**
   * @optional
   *
   * @description
   *
   * Desabilita a seta do componente *popover*.
   *
   * @default `false`
   */
  set hideArrow(value) {
    this._hideArrow = convertToBoolean(value);
  }
  get hideArrow() {
    return this._hideArrow;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define a posição que o po-popover abrirá em relação ao componente alvo. Sugere-se que seja
   * usada a orientação "right" (direita), porém o mesmo é flexível e será rotacionado
   * automaticamente para se adequar a tela, caso necessário.
   *
   * Posições válidas:
   * - `right`: Posiciona o po-popover no lado direito do componente alvo.
   * - `right-bottom`: Posiciona o po-popover no lado direito inferior do componente alvo.
   * - `right-top`: Posiciona o po-popover no lado direito superior do componente alvo.
   * - `bottom`: Posiciona o po-popover abaixo do componente alvo.
   * - `bottom-left`: Posiciona o po-popover abaixo e à esquerda do componente alvo.
   * - `bottom-right`: Posiciona o po-popover abaixo e à direita do componente alvo.
   * - `left`: Posiciona o po-popover no lado esquerdo do componente alvo.
   * - `left-top`: Posiciona o po-popover no lado esquerdo superior do componente alvo.
   * - `left-bottom`: Posiciona o po-popover no lado esquerdo inferior do componente alvo.
   * - `top`: Posiciona o po-popover acima do componente alvo.
   * - `top-right`: Posiciona o po-popover acima e à direita do componente alvo.
   * - `top-left`: Posiciona o po-popover acima e à esquerda do componente alvo.
   *
   *
   * @default right
   */
  set position(value) {
    this._position = PO_CONTROL_POSITIONS.includes(value) ? value : PO_POPOVER_DEFAULT_POSITION;
  }
  get position() {
    return this._position;
  }
  /**
   * @description
   *
   * Define o evento que abrirá o po-popover.
   *
   * Valores válidos:
   *  - `click`: Abre ao clicar no componente alvo.
   *  - `hover`: Abre ao passar o mouse sobre o componente alvo.
   *  - `function`: Abre através de funções públicas do componente.
   *
   * @default click
   * @optional
   */
  set trigger(value) {
    this._trigger = PO_POPOVER_TRIGGERS.includes(value) ? value : PO_POPOVER_DEFAULT_TRIGGER;
  }
  get trigger() {
    return this._trigger;
  }
  /**
   * @optional
   *
   * @description
   * Permite a inclusão de classes CSS customizadas ao componente.
   *
   * Exemplo: `p-custom-classes="minha-classe-1 minha-classe-2"`.
   *
   */
  customClasses = input(void 0, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "customClasses"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-custom-classes"
  }));
  static ɵfac = function PoPopoverBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoPopoverBaseComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoPopoverBaseComponent,
    selectors: [["po-popover-base"]],
    inputs: {
      appendBox: [2, "p-append-in-body", "appendBox", convertToBoolean],
      cornerAligned: [0, "p-corner-aligned", "cornerAligned"],
      offset: [0, "p-offset", "offset"],
      target: [0, "p-target", "target"],
      title: [0, "p-title", "title"],
      width: [0, "p-width", "width"],
      hideArrow: [0, "p-hide-arrow", "hideArrow"],
      position: [0, "p-position", "position"],
      trigger: [0, "p-trigger", "trigger"],
      customClasses: [1, "p-custom-classes", "customClasses"]
    },
    outputs: {
      closePopover: "p-close",
      openPopover: "p-open"
    },
    standalone: false,
    decls: 0,
    vars: 0,
    template: function PoPopoverBaseComponent_Template(rf, ctx) {
    },
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoPopoverBaseComponent, [{
    type: Component,
    args: [{
      selector: "po-popover-base",
      template: "",
      standalone: false
    }]
  }], null, {
    appendBox: [{
      type: Input,
      args: [{
        alias: "p-append-in-body",
        transform: convertToBoolean
      }]
    }],
    cornerAligned: [{
      type: Input,
      args: ["p-corner-aligned"]
    }],
    offset: [{
      type: Input,
      args: ["p-offset"]
    }],
    target: [{
      type: Input,
      args: ["p-target"]
    }],
    title: [{
      type: Input,
      args: ["p-title"]
    }],
    width: [{
      type: Input,
      args: ["p-width"]
    }],
    closePopover: [{
      type: Output,
      args: ["p-close"]
    }],
    openPopover: [{
      type: Output,
      args: ["p-open"]
    }],
    hideArrow: [{
      type: Input,
      args: ["p-hide-arrow"]
    }],
    position: [{
      type: Input,
      args: ["p-position"]
    }],
    trigger: [{
      type: Input,
      args: ["p-trigger"]
    }],
    customClasses: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-custom-classes",
        required: false
      }]
    }]
  });
})();
var PoPopoverComponent = class _PoPopoverComponent extends PoPopoverBaseComponent {
  renderer;
  poControlPosition;
  cd;
  popoverElement;
  arrowDirection = "left";
  timeoutResize;
  targetElement;
  afterViewInitWasCalled = false;
  widthPopover = void 0;
  keydownTargetListener;
  keydownPopoverListener;
  resizeObserver;
  eventListenerFunction;
  tabbableSelector = ['a[href]:not([tabindex="-1"])', 'area[href]:not([tabindex="-1"])', 'input:not([disabled]):not([type="hidden"]):not([tabindex="-1"])', 'select:not([disabled]):not([tabindex="-1"])', 'textarea:not([disabled]):not([tabindex="-1"])', 'button:not([disabled]):not([tabindex="-1"])', 'iframe:not([tabindex="-1"])', '[contenteditable="true"]:not([tabindex="-1"])', '[tabindex]:not([tabindex="-1"])'].join(",");
  constructor(renderer, poControlPosition, cd) {
    super();
    this.renderer = renderer;
    this.poControlPosition = poControlPosition;
    this.cd = cd;
  }
  onThemeChange() {
    requestAnimationFrame(() => {
      this.setPopoverPosition();
    });
  }
  ngOnChanges(changes) {
    if (changes["appendBox"]) {
      this.cd.detectChanges();
      this.attachPopoverKeydown();
    }
    if (this.afterViewInitWasCalled && changes["target"]) {
      this.removeListeners();
      this.initEvents();
    }
  }
  ngAfterViewInit() {
    this.initEvents();
    this.afterViewInitWasCalled = true;
  }
  initEvents() {
    this.targetElement = this.target instanceof ElementRef ? this.target.nativeElement : this.target;
    this.initEventListenerFunction();
    this.setElementsControlPosition();
    this.setRendererListenInit();
  }
  ngOnDestroy() {
    this.disconnectResizeObserver();
    this.removeListeners();
  }
  close() {
    this.isHidden = true;
    this.disconnectResizeObserver();
    this.widthPopover = void 0;
    this.closePopover.emit();
    if (this.trigger === "function" && this.clickoutListener) {
      this.clickoutListener();
    }
    this.cd.detectChanges();
  }
  debounceResize() {
    clearTimeout(this.timeoutResize);
    this.timeoutResize = setTimeout(() => {
      this.setPopoverPosition();
    }, 200);
  }
  open() {
    this.addScrollEventListener();
    this.setOpacity(0);
    this.isHidden = false;
    if (this.cornerAligned && !this.width && !this.widthPopover) {
      const el = this.popoverElement.nativeElement;
      el.style.visibility = "hidden";
      el.style.left = "-9999px";
      el.style.width = "";
      this.setOpacity(0);
      this.cd.detectChanges();
      requestAnimationFrame(() => {
        this.widthPopover = el.getBoundingClientRect().width;
        el.style.visibility = "";
        el.style.left = "";
        this.cd.detectChanges();
      });
    }
    this.showPopover();
    if (this.trigger === "function") {
      this.clickoutListener = this.renderer.listen("document", "click", (event) => {
        this.togglePopup(event);
      });
    }
    this.cd.detectChanges();
  }
  showPopover() {
    requestAnimationFrame(() => {
      this.stabilizePopoverWidth();
      this.setElementsControlPosition();
      this.setPopoverPosition();
      this.setOpacity(1);
      this.openPopover.emit();
      this.observeContentResize();
      this.cd.detectChanges();
    });
  }
  stabilizePopoverWidth() {
    if (this.width != null || this.widthPopover != null) return;
    const el = this.popoverElement?.nativeElement;
    const target = this.targetElement;
    if (!el || !target) return;
    el.style.left = "0px";
    el.style.top = "0px";
    const natural = el.getBoundingClientRect().width;
    const tRect = target.getBoundingClientRect();
    const cx = tRect.left + tRect.width / 2;
    const halfTarget = tRect.width / 2;
    const vp = window.innerWidth;
    const safety = 2;
    const candidates = [];
    if (cx + 15 <= vp) candidates.push(cx + 15 - safety);
    if (cx - 15 >= 0) candidates.push(vp - cx + 15 - safety);
    const centered = 2 * Math.min(cx, vp - cx) - safety;
    if (centered > 0) candidates.push(centered);
    const leftMax = cx - halfTarget - this.offset - safety;
    if (leftMax > 0) candidates.push(leftMax);
    const rightMax = vp - cx - halfTarget - this.offset - safety;
    if (rightMax > 0) candidates.push(rightMax);
    const minWidth = 240;
    const maxFit = candidates.length ? Math.max(...candidates) : natural;
    this.widthPopover = Math.max(minWidth, Math.min(natural, maxFit));
    this.cd.detectChanges();
  }
  ensurePopoverPosition() {
    setTimeout(() => {
      this.setElementsControlPosition();
      this.setPopoverPosition();
      this.cd.detectChanges();
    });
  }
  setOpacity(value) {
    this.popoverElement.nativeElement.style.opacity = value;
  }
  setPopoverPosition() {
    this.poControlPosition.adjustPosition(this.position);
    this.arrowDirection = this.poControlPosition.getArrowDirection();
  }
  setRendererListenInit() {
    this.resizeListener = this.renderer.listen("window", "resize", (event) => {
      if (!this.isHidden) {
        this.debounceResize();
      }
    });
    if (this.trigger === "hover") {
      this.mouseEnterListener = this.renderer.listen(this.targetElement, "mouseenter", (event) => {
        this.open();
      });
      this.mouseLeaveListener = this.renderer.listen(this.targetElement, "mouseleave", (event) => {
        this.close();
      });
    } else if (this.trigger === "click") {
      this.clickoutListener = this.renderer.listen("document", "click", (event) => {
        this.togglePopup(event);
      });
    }
    if (this.targetElement) {
      this.keydownTargetListener = this.renderer.listen(this.targetElement, "keydown", (event) => {
        if (this.appendBox && !this.isHidden && event.key === "Tab") {
          event.preventDefault();
          if (event.shiftKey) {
            this.focusPrevBeforeTarget();
          } else {
            this.focusOnFirstFocusable();
          }
        }
      });
    }
    this.attachPopoverKeydown();
  }
  togglePopup(event) {
    if (!this.isHidden && !this.popoverElement.nativeElement.contains(event.target) && !this.targetElement?.contains(event.target)) {
      if (!this.appendBox) {
        this.close();
      }
    } else if (this.targetElement?.contains(event.target) && this.trigger !== "function") {
      this.popoverElement.nativeElement.hidden ? this.open() : this.close();
    }
  }
  addScrollEventListener() {
    window.addEventListener("scroll", this.eventListenerFunction, true);
  }
  initEventListenerFunction() {
    this.eventListenerFunction = () => {
      this.setPopoverPosition();
    };
  }
  removeListeners() {
    if (this.clickoutListener) {
      this.clickoutListener();
    }
    if (this.mouseEnterListener) {
      this.mouseEnterListener();
    }
    if (this.mouseLeaveListener) {
      this.mouseLeaveListener();
    }
    this.resizeListener();
    window.removeEventListener("scroll", this.eventListenerFunction, true);
    this.keydownTargetListener?.();
    this.keydownPopoverListener?.();
  }
  setElementsControlPosition() {
    this.poControlPosition.setElements(this.popoverElement.nativeElement, this.offset, this.target, void 0, false, this.cornerAligned);
  }
  focusOnTarget() {
    const el = this.targetElement;
    el?.focus?.();
  }
  focusOnFirstFocusable() {
    const host = this.popoverElement?.nativeElement;
    if (!host) {
      this.focusOnTarget();
      return;
    }
    const action = host.querySelector(".po-helper-footer-action-link button");
    if (action) {
      action.focus();
      return;
    }
    const dialog = host.querySelector('[role="dialog"]');
    if (dialog) {
      this.close();
      this.focusNextAfterTarget();
      return;
    }
    const hadTabindex = host.hasAttribute("tabindex");
    if (!hadTabindex) host.setAttribute("tabindex", "-1");
    host.focus();
    if (!hadTabindex) {
      host?.addEventListener("blur", () => host.removeAttribute("tabindex"), {
        once: true
      });
    }
  }
  attachPopoverKeydown() {
    this.keydownPopoverListener?.();
    const host = this.popoverElement?.nativeElement;
    if (!host) return;
    this.keydownPopoverListener = this.renderer.listen(host, "keydown", (event) => {
      if (this.appendBox) {
        if (event.key !== "Tab") return;
        const innerTabbables = this.getTabbablesIn(host);
        const first = innerTabbables[0];
        const last = innerTabbables[innerTabbables.length - 1];
        const isShift = event.shiftKey;
        const active = document.activeElement;
        if (isShift && (!active || active === first)) {
          event.preventDefault();
          this.focusOnTarget();
          return;
        }
        if (!isShift && (!active || active === last) || active.id.includes("popover-content")) {
          event.preventDefault();
          this.focusNextAfterTarget();
          return;
        }
      }
    });
  }
  isVisible(element) {
    const cs = window.getComputedStyle(element);
    if (cs.visibility === "hidden" || cs.display === "none") return false;
    let currentElement = element;
    while (currentElement) {
      const style2 = window.getComputedStyle(currentElement);
      if (style2.display === "none" || style2.visibility === "hidden") return false;
      currentElement = currentElement.parentElement;
    }
    const rect = element.getBoundingClientRect();
    return !!(rect.width || rect.height || element.getClientRects().length);
  }
  getTabbablesIn(container) {
    return Array.from(container.querySelectorAll(this.tabbableSelector)).filter((el) => this.isVisible(el) && !el.hasAttribute("disabled"));
  }
  getDocumentTabbables() {
    const all = Array.from(document.querySelectorAll(this.tabbableSelector));
    return all.filter((el) => this.isVisible(el) && !el.hasAttribute("disabled"));
  }
  focusNextAfterTarget() {
    const docTabs = this.getDocumentTabbables();
    if (!docTabs.length) return;
    const target = this.targetElement;
    let startIndex = target ? docTabs.indexOf(target) : -1;
    if (startIndex < 0 && this.popoverElement?.nativeElement) {
      const inner25 = this.getTabbablesIn(this.popoverElement.nativeElement);
      if (inner25.length) {
        const idxByInner = docTabs.indexOf(inner25[inner25.length - 1]);
        if (idxByInner >= 0) startIndex = idxByInner;
      }
    }
    const next = docTabs[startIndex + 1] || docTabs[0];
    next?.focus?.();
  }
  focusPrevBeforeTarget() {
    const docTabs = this.getDocumentTabbables();
    if (!docTabs.length) return;
    const target = this.targetElement;
    const idx = target ? docTabs.indexOf(target) : -1;
    const prev = docTabs[idx - 1] || docTabs[docTabs.length - 1];
    prev?.focus?.();
  }
  observeContentResize() {
    this.disconnectResizeObserver();
    if (!this.popoverElement?.nativeElement) return;
    let baselineFired = false;
    this.resizeObserver = new ResizeObserver(() => {
      if (!baselineFired) {
        baselineFired = true;
        return;
      }
      this.setElementsControlPosition();
      this.setPopoverPosition();
      this.cd.detectChanges();
    });
    this.resizeObserver.observe(this.popoverElement.nativeElement);
  }
  disconnectResizeObserver() {
    this.resizeObserver?.disconnect();
    this.resizeObserver = null;
  }
  static ɵfac = function PoPopoverComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoPopoverComponent)(ɵɵdirectiveInject(Renderer2), ɵɵdirectiveInject(PoControlPositionService), ɵɵdirectiveInject(ChangeDetectorRef));
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoPopoverComponent,
    selectors: [["po-popover"]],
    viewQuery: function PoPopoverComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c14, 5, ElementRef);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.popoverElement = _t.first);
      }
    },
    hostBindings: function PoPopoverComponent_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("PoUiThemeChange", function PoPopoverComponent_PoUiThemeChange_HostBindingHandler() {
          return ctx.onThemeChange();
        }, ɵɵresolveWindow);
      }
    },
    standalone: false,
    features: [ɵɵProvidersFeature([PoControlPositionService]), ɵɵInheritDefinitionFeature, ɵɵNgOnChangesFeature],
    ngContentSelectors: _c12,
    decls: 4,
    vars: 1,
    consts: [["sharedPopoverContent", ""], ["popoverElement", ""], ["cdkConnectedOverlay", "", 3, "cdkConnectedOverlayOrigin", "cdkConnectedOverlayOpen", "cdkConnectedOverlayDisableClose"], [4, "ngTemplateOutlet"], [1, "po-popover", 3, "hidden", "ngClass"], [3, "class"], [1, "po-popover-content"], [1, "po-popover-title"]],
    template: function PoPopoverComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵprojectionDef();
        ɵɵconditionalCreate(0, PoPopoverComponent_Conditional_0_Template, 1, 3, null, 2)(1, PoPopoverComponent_Conditional_1_Template, 1, 1, "ng-container");
        ɵɵtemplate(2, PoPopoverComponent_ng_template_2_Template, 6, 6, "ng-template", null, 0, ɵɵtemplateRefExtractor);
      }
      if (rf & 2) {
        ɵɵconditional(ctx.appendBox ? 0 : 1);
      }
    },
    dependencies: [NgClass, NgTemplateOutlet, CdkConnectedOverlay],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoPopoverComponent, [{
    type: Component,
    args: [{
      selector: "po-popover",
      providers: [PoControlPositionService],
      standalone: false,
      template: '@if (appendBox) {\n  <ng-template\n    cdkConnectedOverlay\n    [cdkConnectedOverlayOrigin]="target"\n    [cdkConnectedOverlayOpen]="true"\n    [cdkConnectedOverlayDisableClose]="true"\n  >\n    <ng-container *ngTemplateOutlet="sharedPopoverContent"></ng-container>\n  </ng-template>\n} @else {\n  <ng-container *ngTemplateOutlet="sharedPopoverContent"></ng-container>\n}\n\n<ng-template #sharedPopoverContent>\n  <div\n    #popoverElement\n    [hidden]="isHidden"\n    class="po-popover"\n    [style.width.px]="width ?? widthPopover"\n    [ngClass]="customClasses()"\n  >\n    @if (!hideArrow) {\n      <div class="po-popover-arrow po-arrow-{{ arrowDirection }}"></div>\n    }\n\n    <div class="po-popover-content">\n      @if (title) {\n        <span class="po-popover-title">{{ title }}</span>\n      }\n      <ng-content></ng-content>\n    </div>\n  </div>\n</ng-template>\n'
    }]
  }], () => [{
    type: Renderer2
  }, {
    type: PoControlPositionService
  }, {
    type: ChangeDetectorRef
  }], {
    popoverElement: [{
      type: ViewChild,
      args: ["popoverElement", {
        read: ElementRef,
        static: false
      }]
    }],
    onThemeChange: [{
      type: HostListener,
      args: ["window:PoUiThemeChange"]
    }]
  });
})();
var PO_HELPER_ALLOWED_TAGS = ["b", "i", "u", "strong", "em"];
var PoHelperComponent = class _PoHelperComponent extends PoHelperBaseComponent {
  cdr;
  target;
  popover;
  poButton;
  refreshAnimating = false;
  static instances = [];
  static idCounter = 0;
  popoverPosition = "right";
  id;
  boundFocusIn;
  poHelperLiterals = {
    en: {
      info: "Additional information",
      help: "Additional help"
    },
    pt: {
      info: "Informação adicional",
      help: "Ajuda adicional"
    },
    es: {
      info: "Información adicional",
      help: "Ayuda adicional"
    },
    ru: {
      info: "Дополнительная информация",
      help: "Дополнительная помощь"
    }
  };
  contentFragments = computed(() => {
    const helperValue = this.helper();
    if (!helperValue) {
      return [];
    }
    return parseSafeText(helperValue.content, PO_HELPER_ALLOWED_TAGS);
  }, ...ngDevMode ? [{
    debugName: "contentFragments"
  }] : (
    /* istanbul ignore next */
    []
  ));
  constructor(cdr) {
    super();
    this.cdr = cdr;
    this.id = "po-helper-" + _PoHelperComponent.idCounter++;
  }
  ngAfterViewInit() {
    _PoHelperComponent.instances.push(this);
  }
  ngOnChanges(changes) {
    if (changes.size) {
      this.cdr.detectChanges();
    }
  }
  ngOnDestroy() {
    _PoHelperComponent.instances = _PoHelperComponent.instances.filter((i2) => i2 !== this);
    this.handleClose();
  }
  openHelperPopover() {
    requestAnimationFrame(() => {
      if (this.popover.isHidden && (this.helper()["content"] || typeof this.helper() === "string" || this.helper()["title"])) {
        this.popover.open();
      } else {
        this.popover.close();
      }
    });
  }
  helperIsVisible() {
    return this.popover && !this.popover.isHidden;
  }
  closeHelperPopover() {
    requestAnimationFrame(() => {
      if (!this.popover.isHidden) {
        this.popover.close();
      }
    });
  }
  emitClick(event) {
    if (this.disabled()) {
      event.preventDefault();
      return;
    }
    if (!this.helper() || typeof this.helper() === "string") {
      return;
    }
    this.handleEmitEvent(event);
  }
  onKeyDown(event) {
    if (this.disabled()) {
      event?.preventDefault?.();
      event?.stopPropagation?.();
      return;
    }
    if (event?.code === "Space" || event?.code === "Enter") {
      event.preventDefault();
      if (!this.popover) {
        return;
      }
      _PoHelperComponent.instances.forEach((instance) => {
        if (instance !== this && instance.popover && !instance.popover.isHidden) {
          instance.popover.close();
        }
      });
      if (typeof this.helper() !== "string") {
        const helperObj = this.helper();
        if (helperObj && typeof helperObj === "object" && "eventOnClick" in helperObj && typeof helperObj.eventOnClick !== "undefined") {
          this.handleEmitEvent(event);
          return;
        }
      }
      if (this.popover.isHidden && (this.helper()["content"] || this.helper()["title"])) {
        this.popover.open();
      } else {
        this.popover.close();
      }
      this.handleEmitEvent(event);
    }
  }
  handleEmitEvent(event) {
    const helper = this.helper();
    const onClick = helper.eventOnClick;
    if (typeof onClick === "function") {
      onClick(helper);
      event.preventDefault();
      event.stopPropagation();
      return;
    }
    if (onClick && typeof onClick.emit === "function") {
      onClick.emit(helper);
      event.preventDefault();
      event.stopPropagation();
    }
  }
  closePopoverOnFocusOut(event) {
    if (!this.popover || this.popover.isHidden) {
      return;
    }
    const targetEl = this.target?.nativeElement;
    const popEl = this.popover.popoverElement?.nativeElement;
    const focusNode = event.target;
    if (focusNode && !targetEl.contains(focusNode) && !popEl?.contains(focusNode)) {
      this.popover.close();
    }
  }
  ariaLabel() {
    const helper = this.helper();
    const type = typeof helper !== "string" && helper?.type === "info" ? "info" : "help";
    const lang = (navigator.language || "en").substring(0, 2).toLowerCase();
    const literals = this.poHelperLiterals[lang] || this.poHelperLiterals["en"];
    return literals[type];
  }
  handleOpen() {
    this.boundFocusIn = this.closePopoverOnFocusOut.bind(this);
    window.addEventListener("focusin", this.boundFocusIn, true);
  }
  handleClose() {
    if (this.boundFocusIn) {
      window.removeEventListener("focusin", this.boundFocusIn, true);
    }
  }
  executeCustomAction(event) {
    event.preventDefault();
    event.stopPropagation();
    const action = this.customAction();
    if (action && typeof action.action === "function") {
      if (this.refreshAnimating) {
        this.refreshAnimating = false;
        this.cdr.detectChanges();
      }
      this.refreshAnimating = true;
      action.action();
    }
  }
  onRefreshAnimationEnd() {
    this.refreshAnimating = false;
  }
  static ɵfac = function PoHelperComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoHelperComponent)(ɵɵdirectiveInject(ChangeDetectorRef));
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoHelperComponent,
    selectors: [["po-helper"]],
    viewQuery: function PoHelperComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c15, 7, ElementRef)(_c16, 5)(PoButtonComponent, 7, ElementRef);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.target = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.popover = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.poButton = _t.first);
      }
    },
    standalone: false,
    features: [ɵɵInheritDefinitionFeature, ɵɵNgOnChangesFeature],
    decls: 2,
    vars: 1,
    consts: [["target", ""], ["popover", ""], ["role", "button", 1, "po-helper-container", "po-helper-target", 3, "po-helper-refresh-animating"], ["role", "button", 1, "po-helper-container", "po-helper-target", 3, "po-helper-disabled"], ["role", "button", 1, "po-helper-container", "po-helper-target", 3, "keydown.enter", "keydown.space", "click", "animationend"], [3, "p-icon"], ["role", "button", 1, "po-helper-container", "po-helper-target", 3, "keydown", "click"], ["p-custom-classes", "po-helper-popover", "p-trigger", "function", 3, "p-close", "p-open", "p-position", "p-target", "p-title", "p-append-in-body", "p-offset"], ["role", "dialog", "aria-modal", "false", "tabindex", "-1", 3, "id"], [1, "po-helper-content-text"], [3, "po-text-bold", "po-text-italic", "po-text-underline"], [1, "po-helper-footer-action-link", 3, "keydown.enter", "keydown.space", "p-action", "p-label"]],
    template: function PoHelperComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵconditionalCreate(0, PoHelperComponent_Conditional_0_Template, 2, 5, "div", 2)(1, PoHelperComponent_Conditional_1_Template, 10, 16, "div", 3);
      }
      if (rf & 2) {
        ɵɵconditional(ctx.customAction() ? 0 : 1);
      }
    },
    dependencies: [PoIconComponent, PoPopoverComponent, PoLinkComponent, PoDividerComponent],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoHelperComponent, [{
    type: Component,
    args: [{
      selector: "po-helper",
      standalone: false,
      template: `@if (customAction()) {
  <div
    class="po-helper-container po-helper-target"
    [class.po-helper-refresh-animating]="refreshAnimating"
    [attr.tabindex]="0"
    role="button"
    [attr.aria-label]="customAction().ariaLabel || ''"
    (keydown.enter)="executeCustomAction($event)"
    (keydown.space)="executeCustomAction($event)"
    (click)="executeCustomAction($event)"
    (animationend)="onRefreshAnimationEnd()"
  >
    <po-icon [p-icon]="customAction().icon"></po-icon>
  </div>
} @else {
  <div
    class="po-helper-container po-helper-target"
    #target
    [attr.tabindex]="disabled() ? null : 0"
    role="button"
    [attr.aria-label]="ariaLabel()"
    [attr.aria-haspopup]="'dialog'"
    [attr.aria-expanded]="!popover.isHidden"
    [attr.aria-controls]="'popover-content-' + id"
    [attr.aria-describedby]="!popover.isHidden ? 'popover-content-' + id : null"
    [class.po-helper-disabled]="disabled()"
    (keydown)="onKeyDown($event)"
    (click)="openHelperPopover(); emitClick($event)"
  >
    <po-icon [p-icon]="helper()?.type === 'info' ? 'ICON_INFO' : 'ICON_HELP'"></po-icon>
    <po-popover
      #popover
      [p-position]="popoverPosition"
      [p-target]="target"
      [p-title]="helper()?.title"
      [p-append-in-body]="appendBox()"
      [p-offset]="8"
      p-custom-classes="po-helper-popover"
      p-trigger="function"
      (p-close)="handleClose()"
      (p-open)="handleOpen()"
    >
      <div [id]="'popover-content-' + id" role="dialog" aria-modal="false" tabindex="-1">
        <span class="po-helper-content-text">
          @for (fragment of contentFragments(); track $index) {
            <span
              [class.po-text-bold]="fragment.bold"
              [class.po-text-italic]="fragment.italic"
              [class.po-text-underline]="fragment.underline"
              >{{ fragment.text }}</span
            >
          }
        </span>
        @if (helper()?.type === 'help' && helper()?.footerAction) {
          <po-divider></po-divider>
          <po-link
            class="po-helper-footer-action-link"
            (keydown.enter)="$event.stopPropagation()"
            (keydown.space)="$event.stopPropagation()"
            (p-action)="helper()?.footerAction?.action()"
            [p-label]="helper()?.footerAction?.label"
          >
          </po-link>
        }
      </div>
    </po-popover>
  </div>
}
`
    }]
  }], () => [{
    type: ChangeDetectorRef
  }], {
    target: [{
      type: ViewChild,
      args: ["target", {
        read: ElementRef,
        static: true
      }]
    }],
    popover: [{
      type: ViewChild,
      args: ["popover", {
        static: false
      }]
    }],
    poButton: [{
      type: ViewChild,
      args: [PoButtonComponent, {
        read: ElementRef,
        static: true
      }]
    }]
  });
})();
var PoLinkModule = class _PoLinkModule {
  static ɵfac = function PoLinkModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoLinkModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoLinkModule,
    declarations: [PoLinkComponent],
    imports: [CommonModule, RouterModule],
    exports: [PoLinkComponent]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [CommonModule, RouterModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoLinkModule, [{
    type: NgModule,
    args: [{
      declarations: [PoLinkComponent],
      imports: [CommonModule, RouterModule],
      exports: [PoLinkComponent]
    }]
  }], null, null);
})();
var PoPopoverModule = class _PoPopoverModule {
  static ɵfac = function PoPopoverModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoPopoverModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoPopoverModule,
    declarations: [PoPopoverComponent],
    imports: [CommonModule, OverlayModule],
    exports: [PoPopoverComponent]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [CommonModule, OverlayModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoPopoverModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, OverlayModule],
      declarations: [PoPopoverComponent],
      exports: [PoPopoverComponent]
    }]
  }], null, null);
})();
var PoHelperModule = class _PoHelperModule {
  static ɵfac = function PoHelperModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoHelperModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoHelperModule,
    declarations: [PoHelperComponent],
    imports: [CommonModule, PoIconModule, PoPopoverModule, PoLinkModule, PoDividerModule],
    exports: [PoHelperComponent]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [CommonModule, PoIconModule, PoPopoverModule, PoLinkModule, PoDividerModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoHelperModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, PoIconModule, PoPopoverModule, PoLinkModule, PoDividerModule],
      declarations: [PoHelperComponent],
      exports: [PoHelperComponent]
    }]
  }], null, null);
})();
var poFieldContainerLiterals = {
  en: {
    optional: "Optional",
    required: "Required"
  },
  es: {
    optional: "Opcional",
    required: "Obligatorio"
  },
  pt: {
    optional: "Opcional",
    required: "Obrigatório"
  },
  ru: {
    optional: "Пожеланию",
    required: "Обязательный"
  }
};
var PoLabelComponent = class _PoLabelComponent {
  _disabled = false;
  _field = false;
  /**
   * @optional
   *
   * @description
   *
   * Indica se o label está desativado.
   *
   * @default `false`
   */
  set disabled(value) {
    this._disabled = value === "" ? true : convertToBoolean(value);
  }
  get disabled() {
    return this._disabled;
  }
  /**
   * @optional
   *
   * @description
   *
   * Indica se o label será de um field.
   *
   * @default `false`
   */
  set field(value) {
    this._field = value === "" ? true : convertToBoolean(value);
  }
  get field() {
    return this._field;
  }
  /** Indica o campo vinculado ao label */
  for;
  /** Label do campo. */
  label;
  /** Indica o tipo do campo vinculado ao label */
  requirement;
  /**
   * @optional
   *
   * @description
   *
   * Habilita a quebra automática do texto. Quando ativada, o texto que excede
   * o espaço disponível é transferido para a próxima linha em pontos apropriados para uma
   * leitura clara.
   *
   * @default `false`
   */
  textWrap = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "textWrap"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-text-wrap"
  }));
  isRequiredLiteral(value) {
    return Object.values(poFieldContainerLiterals).some((literals) => literals.required.toLowerCase() === value.toLowerCase());
  }
  static ɵfac = function PoLabelComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoLabelComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoLabelComponent,
    selectors: [["po-label"]],
    inputs: {
      disabled: [0, "p-disabled", "disabled"],
      field: [0, "p-field", "field"],
      for: [0, "p-for", "for"],
      label: [0, "p-label", "label"],
      requirement: [0, "p-requirement", "requirement"],
      textWrap: [1, "p-text-wrap", "textWrap"]
    },
    standalone: false,
    decls: 2,
    vars: 2,
    consts: [[1, "po-label", 3, "po-label-wrap", "po-label-disabled"], [1, "po-label-requirement", 3, "po-label-required"], [1, "po-label"], [1, "po-label-requirement"]],
    template: function PoLabelComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵconditionalCreate(0, PoLabelComponent_Conditional_0_Template, 2, 7, "label", 0);
        ɵɵconditionalCreate(1, PoLabelComponent_Conditional_1_Template, 2, 3, "span", 1);
      }
      if (rf & 2) {
        ɵɵconditional(ctx.label ? 0 : -1);
        ɵɵadvance();
        ɵɵconditional(ctx.field && ctx.requirement ? 1 : -1);
      }
    },
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoLabelComponent, [{
    type: Component,
    args: [{
      selector: "po-label",
      standalone: false,
      template: '@if (label) {\n  <label\n    class="po-label"\n    [class.po-label-wrap]="textWrap()"\n    [attr.for]="field && for ? for : undefined"\n    [attr.p-field]="field"\n    [class.po-label-disabled]="disabled"\n    >{{ label }}</label\n  >\n}\n@if (field && requirement) {\n  <span class="po-label-requirement" [class.po-label-required]="isRequiredLiteral(requirement)">{{ requirement }}</span>\n}\n'
    }]
  }], null, {
    disabled: [{
      type: Input,
      args: ["p-disabled"]
    }],
    field: [{
      type: Input,
      args: ["p-field"]
    }],
    for: [{
      type: Input,
      args: ["p-for"]
    }],
    label: [{
      type: Input,
      args: ["p-label"]
    }],
    requirement: [{
      type: Input,
      args: ["p-requirement"]
    }],
    textWrap: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-text-wrap",
        required: false
      }]
    }]
  });
})();
var PoDragBaseDirective = class _PoDragBaseDirective {
  /**
   * @description
   *
   * Dado associado ao item arrastável. O valor é emitido nos eventos
   * `p-drag-started` e `p-drag-ended`.
   */
  data = input(void 0, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "data"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-drag"
  }));
  /**
   * @optional
   *
   * @description
   *
   * Desabilita o arraste do item. Quando `true`, o usuário não pode iniciar
   * um gesto de arrastar neste elemento, mas o item continua participando do
   * cálculo de posições dos vizinhos dentro do `p-drop-list`.
   *
   * @default false
   */
  dragDisabled = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "dragDisabled"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-drag-disabled",
    transform: convertToBoolean
  }));
  /**
   * @optional
   *
   * @description
   *
   * Evento emitido quando o arraste do item é iniciado.
   * O valor emitido é o dado associado ao item (propriedade `p-drag`).
   */
  dragStarted = output({
    alias: "p-drag-started"
  });
  /**
   * @optional
   *
   * @description
   *
   * Evento emitido quando o arraste do item é encerrado.
   * O valor emitido é o dado associado ao item (propriedade `p-drag`).
   */
  dragEnded = output({
    alias: "p-drag-ended"
  });
  /**
   * @optional
   *
   * @description
   *
   * Evento emitido continuamente enquanto o item está sendo arrastado.
   * Contém a posição do ponteiro, a distância percorrida e a direção do movimento.
   *
   * > **Atenção:** Este evento é disparado em alta frequência (a cada frame de movimento).
   * > Evite operações custosas no handler ou aplique técnicas de throttle/debounce.
   */
  dragMoved = output({
    alias: "p-drag-moved"
  });
  static ɵfac = function PoDragBaseDirective_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoDragBaseDirective)();
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoDragBaseDirective,
    inputs: {
      data: [1, "p-drag", "data"],
      dragDisabled: [1, "p-drag-disabled", "dragDisabled"]
    },
    outputs: {
      dragStarted: "p-drag-started",
      dragEnded: "p-drag-ended",
      dragMoved: "p-drag-moved"
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoDragBaseDirective, [{
    type: Directive
  }], null, {
    data: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-drag",
        required: false
      }]
    }],
    dragDisabled: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-drag-disabled",
        required: false
      }]
    }],
    dragStarted: [{
      type: Output,
      args: ["p-drag-started"]
    }],
    dragEnded: [{
      type: Output,
      args: ["p-drag-ended"]
    }],
    dragMoved: [{
      type: Output,
      args: ["p-drag-moved"]
    }]
  });
})();
var PoDropListBaseDirective = class _PoDropListBaseDirective {
  /**
   * @description
   *
   * Lista de itens gerenciada pelo container. A diretiva muta este array
   * diretamente ao reordenar ou transferir itens entre containers.
   *
   * O array atualizado é refletido no evento `p-dropped`.
   */
  items = input([], __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "items"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-drop-list"
  }));
  /**
   * @optional
   *
   * @description
   *
   * Identificador único do container. Utilizado para conectar múltiplos
   * containers via `p-drop-list-connected-to`.
   *
   */
  dropListId = input("", __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "dropListId"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-drop-list-id"
  }));
  /**
   * @optional
   *
   * @description
   *
   * Desabilita o drag para os elementos dentro do container.
   * Quando `true`, seus elementos não poderão ser arrastados, porém elementos de outros containers poderão ser arrastados para dentro dele.
   *
   * @default false
   */
  dropListDisabled = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "dropListDisabled"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-drop-list-disabled",
    transform: convertToBoolean
  }));
  /**
   * @optional
   *
   * @description
   *
   * Lista de containers conectados. Aceita um array de ids (`string`). Quando configurado,
   * itens podem ser arrastados entre os containers listados.
   */
  dropListConnectedTo = input([], __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "dropListConnectedTo"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-drop-list-connected-to"
  }));
  /**
   * @optional
   *
   * @description
   *
   * Orientação dos itens dentro do container. Afeta a lógica de sombra de
   * posicionamento do Angular CDK durante o arraste.
   *
   * Valores aceitos: `'horizontal'` | `'vertical'` | `'mixed'`.
   *
   * @default `'vertical'`
   */
  dropListOrientation = input("vertical", __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "dropListOrientation"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-drop-list-orientation"
  }));
  /**
   * @optional
   *
   * @description
   *
   * Desabilita a reordenação automática dos itens durante o arraste.
   *
   * Quando `true`, permite arrastar o item sem alterar sua posição na lista de origem.
   * A transferência de itens entre containers conectados continua funcionando.
   *
   * Use esta propriedade quando a reordenação dos itens for realizada por uma lógica
   * personalizada do consumidor.
   *
   * @default false
   */
  dropSortingDisabled = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "dropSortingDisabled"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-drop-sorting-disabled",
    transform: convertToBoolean
  }));
  /**
   * @optional
   *
   * @description
   *
   * Evento emitido quando um item é solto dentro deste container.
   * Contém os índices anterior e atual, o item movido e os ids dos
   * containers de origem e destino.
   */
  dropped = output({
    alias: "p-dropped"
  });
  /**
   * @optional
   *
   * @description
   *
   * Evento emitido quando um item externo entra neste container durante o arraste.
   * Contém o item que entrou e o id do container de origem.
   */
  dragEntered = output({
    alias: "p-drag-entered"
  });
  static ɵfac = function PoDropListBaseDirective_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoDropListBaseDirective)();
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoDropListBaseDirective,
    inputs: {
      items: [1, "p-drop-list", "items"],
      dropListId: [1, "p-drop-list-id", "dropListId"],
      dropListDisabled: [1, "p-drop-list-disabled", "dropListDisabled"],
      dropListConnectedTo: [1, "p-drop-list-connected-to", "dropListConnectedTo"],
      dropListOrientation: [1, "p-drop-list-orientation", "dropListOrientation"],
      dropSortingDisabled: [1, "p-drop-sorting-disabled", "dropSortingDisabled"]
    },
    outputs: {
      dropped: "p-dropped",
      dragEntered: "p-drag-entered"
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoDropListBaseDirective, [{
    type: Directive
  }], null, {
    items: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-drop-list",
        required: false
      }]
    }],
    dropListId: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-drop-list-id",
        required: false
      }]
    }],
    dropListDisabled: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-drop-list-disabled",
        required: false
      }]
    }],
    dropListConnectedTo: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-drop-list-connected-to",
        required: false
      }]
    }],
    dropListOrientation: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-drop-list-orientation",
        required: false
      }]
    }],
    dropSortingDisabled: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-drop-sorting-disabled",
        required: false
      }]
    }],
    dropped: [{
      type: Output,
      args: ["p-dropped"]
    }],
    dragEntered: [{
      type: Output,
      args: ["p-drag-entered"]
    }]
  });
})();
var PoDropListDirective = class _PoDropListDirective extends PoDropListBaseDirective {
  cdkDropList = inject(CdkDropList);
  subscriptions = new Subscription();
  /** Último índice confirmado pelo evento sorted. Usado para corrigir o currentIndex
   *  do dropped, que pode ficar dessincronizado em listas horizontais com itens de
   *  tamanhos diferentes (ping-pong do CDK). */
  lastSortedIndex = null;
  constructor() {
    super();
    effect(() => {
      const disabled = this.dropListDisabled();
      this.cdkDropList.disabled = disabled;
    });
    effect(() => {
      const id = this.dropListId();
      if (id) {
        this.cdkDropList.id = id;
      }
    });
    effect(() => {
      this.cdkDropList.data = this.items();
    });
    effect(() => {
      this.cdkDropList.connectedTo = this.dropListConnectedTo();
    });
    effect(() => {
      this.cdkDropList.sortingDisabled = this.dropSortingDisabled();
    });
  }
  ngOnInit() {
    this.subscriptions.add(this.cdkDropList.sorted.subscribe((event) => {
      this.lastSortedIndex = event.currentIndex;
    }));
    this.subscriptions.add(this.cdkDropList.dropped.subscribe((event) => {
      this.handleDrop(event);
    }));
    this.subscriptions.add(this.cdkDropList.entered.subscribe((event) => {
      const draggedItem = event.item.data;
      this.dragEntered.emit({
        item: draggedItem,
        container: this.cdkDropList.id
      });
    }));
  }
  ngOnDestroy() {
    this.subscriptions.unsubscribe();
  }
  handleDrop(event) {
    const isSameContainer = event.previousContainer === event.container;
    const currentIndex = isSameContainer && this.lastSortedIndex !== null ? this.lastSortedIndex : event.currentIndex;
    this.lastSortedIndex = null;
    if (isSameContainer && event.previousIndex === currentIndex) {
      return;
    }
    const currentItems = [...this.items()];
    let reorderedItems;
    if (isSameContainer) {
      moveItemInArray(currentItems, event.previousIndex, currentIndex);
      reorderedItems = currentItems;
    } else {
      const previousItems = [...event.previousContainer.data];
      transferArrayItem(previousItems, currentItems, event.previousIndex, currentIndex);
      reorderedItems = currentItems;
    }
    const dropped = {
      previousIndex: event.previousIndex,
      currentIndex,
      item: reorderedItems[currentIndex],
      items: reorderedItems,
      container: this.cdkDropList.id,
      dropPoint: event.dropPoint
    };
    if (!isSameContainer) {
      dropped.previousContainer = event.previousContainer.id;
    }
    this.dropped.emit(dropped);
  }
  static ɵfac = function PoDropListDirective_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoDropListDirective)();
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoDropListDirective,
    selectors: [["", "p-drop-list", ""]],
    features: [ɵɵHostDirectivesFeature([{
      directive: CdkDropList,
      inputs: ["cdkDropListOrientation", "p-drop-list-orientation"]
    }]), ɵɵInheritDefinitionFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoDropListDirective, [{
    type: Directive,
    args: [{
      selector: "[p-drop-list]",
      standalone: true,
      hostDirectives: [{
        directive: CdkDropList,
        inputs: ["cdkDropListOrientation: p-drop-list-orientation"]
      }]
    }]
  }], () => [], null);
})();
var poDragHandleButtonLiteralsDefault = {
  en: {
    dragToReorder: "Drag to reorder"
  },
  es: {
    dragToReorder: "Arrastre para reordenar"
  },
  pt: {
    dragToReorder: "Arraste para reordenar"
  },
  ru: {
    dragToReorder: "Перетащите для изменения порядка"
  }
};
var PoDragHandleButtonComponent = class _PoDragHandleButtonComponent {
  sizeInput = input("", __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "sizeInput"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-size"
  }));
  literals = __spreadValues(__spreadValues({}, poDragHandleButtonLiteralsDefault[poLocaleDefault]), poDragHandleButtonLiteralsDefault[inject(PoLanguageService).getShortLanguage()]);
  themeChangeSignal = signal(0, ...ngDevMode ? [{
    debugName: "themeChangeSignal"
  }] : (
    /* istanbul ignore next */
    []
  ));
  resolvedSize = computed(() => {
    this.themeChangeSignal();
    const value = this.sizeInput();
    return value ? validateSizeFn(value, PoButtonSize) : getDefaultSizeFn(PoButtonSize);
  }, ...ngDevMode ? [{
    debugName: "resolvedSize"
  }] : (
    /* istanbul ignore next */
    []
  ));
  onThemeChange() {
    this.themeChangeSignal.update((v) => v + 1);
  }
  static ɵfac = function PoDragHandleButtonComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoDragHandleButtonComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoDragHandleButtonComponent,
    selectors: [["po-drag-handle-button"]],
    hostBindings: function PoDragHandleButtonComponent_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("PoUiThemeChange", function PoDragHandleButtonComponent_PoUiThemeChange_HostBindingHandler() {
          return ctx.onThemeChange();
        }, ɵɵresolveWindow);
      }
    },
    inputs: {
      sizeInput: [1, "p-size", "sizeInput"]
    },
    decls: 2,
    vars: 5,
    consts: [["cdkDragHandle", "", "type", "button", "tabindex", "-1", "p-tooltip-position", "top", 1, "po-drag-drop-handle", 3, "click", "p-tooltip", "p-append-in-body"], ["p-icon", "ICON_DRAG"]],
    template: function PoDragHandleButtonComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "button", 0);
        ɵɵlistener("click", function PoDragHandleButtonComponent_Template_button_click_0_listener($event) {
          return $event.stopPropagation();
        });
        ɵɵelement(1, "po-icon", 1);
        ɵɵelementEnd();
      }
      if (rf & 2) {
        ɵɵclassProp("size-small", ctx.resolvedSize() === "small");
        ɵɵproperty("p-tooltip", ctx.literals.dragToReorder)("p-append-in-body", true);
        ɵɵattribute("aria-label", ctx.literals.dragToReorder);
      }
    },
    dependencies: [CdkDragHandle, PoTooltipModule, PoTooltipDirective, PoIconModule, PoIconComponent],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoDragHandleButtonComponent, [{
    type: Component,
    args: [{
      selector: "po-drag-handle-button",
      standalone: true,
      imports: [CdkDragHandle, PoTooltipModule, PoIconModule],
      encapsulation: ViewEncapsulation.None,
      template: `
    <button
      cdkDragHandle
      class="po-drag-drop-handle"
      [class.size-small]="resolvedSize() === 'small'"
      type="button"
      tabindex="-1"
      [attr.aria-label]="literals.dragToReorder"
      [p-tooltip]="literals.dragToReorder"
      p-tooltip-position="top"
      [p-append-in-body]="true"
      (click)="$event.stopPropagation()"
    >
      <po-icon p-icon="ICON_DRAG"></po-icon>
    </button>
  `
    }]
  }], null, {
    sizeInput: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-size",
        required: false
      }]
    }],
    onThemeChange: [{
      type: HostListener,
      args: ["window:PoUiThemeChange"]
    }]
  });
})();
var PoDragDirective = class _PoDragDirective extends PoDragBaseDirective {
  cdkDrag = inject(CdkDrag);
  el = inject(ElementRef);
  renderer = inject(Renderer2);
  injector = inject(Injector);
  envInjector = inject(EnvironmentInjector);
  appRef = inject(ApplicationRef);
  parentDropList = inject(PoDropListDirective, {
    optional: true
  });
  handleRef = null;
  constructor() {
    super();
    effect(() => {
      this.cdkDrag.data = this.data();
    });
    effect(() => {
      if (this.dragDisabled() || this.parentDropList?.dropListDisabled()) {
        this.destroyHandle();
      } else {
        this.appendHandle();
      }
    }, {
      injector: this.injector
    });
  }
  ngOnInit() {
    this.cdkDrag.previewClass = "po-drag-drop-item-preview";
    this.appendItemHover();
    this.cdkDrag.started.subscribe(() => {
      this.removeTooltipOnDragStart();
      const placeholder = this.cdkDrag.getPlaceholderElement();
      if (placeholder) {
        this.renderer.addClass(placeholder, "po-drag-drop-item-placeholder");
      }
      this.dragStarted.emit(this.data());
    });
    this.cdkDrag.ended.subscribe(() => {
      this.dragEnded.emit(this.data());
    });
    this.cdkDrag.moved.subscribe((event) => {
      this.dragMoved.emit(event);
    });
  }
  ngOnDestroy() {
    this.destroyHandle();
  }
  appendHandle() {
    this.destroyHandle();
    const handleRef = createComponent(PoDragHandleButtonComponent, {
      environmentInjector: this.envInjector,
      elementInjector: this.injector
    });
    this.appRef.attachView(handleRef.hostView);
    handleRef.changeDetectorRef.detectChanges();
    this.renderer.appendChild(this.el.nativeElement, handleRef.location.nativeElement);
    this.handleRef = handleRef;
  }
  destroyHandle() {
    if (this.handleRef) {
      this.appRef.detachView(this.handleRef.hostView);
      this.renderer.removeChild(this.el.nativeElement, this.handleRef.location.nativeElement);
      this.handleRef.destroy();
      this.handleRef = null;
    }
  }
  removeTooltipOnDragStart() {
    const tooltips = this.el.nativeElement.querySelectorAll(".po-tooltip");
    tooltips.forEach((tooltip) => tooltip.remove());
  }
  appendItemHover() {
    effect(() => {
      if (this.dragDisabled() || this.parentDropList?.dropListDisabled()) {
        this.renderer.removeClass(this.el.nativeElement, "po-drag-drop-item");
      } else {
        this.renderer.addClass(this.el.nativeElement, "po-drag-drop-item");
      }
    }, {
      injector: this.injector
    });
  }
  static ɵfac = function PoDragDirective_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoDragDirective)();
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoDragDirective,
    selectors: [["", "p-drag", ""]],
    features: [ɵɵHostDirectivesFeature([{
      directive: CdkDrag,
      inputs: ["cdkDragDisabled", "p-drag-disabled", "cdkDragData", "p-drag"]
    }]), ɵɵInheritDefinitionFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoDragDirective, [{
    type: Directive,
    args: [{
      selector: "[p-drag]",
      standalone: true,
      hostDirectives: [{
        directive: CdkDrag,
        inputs: ["cdkDragDisabled: p-drag-disabled", "cdkDragData: p-drag"]
      }]
    }]
  }], () => [], null);
})();
var PoDragDropModule = class _PoDragDropModule {
  static ɵfac = function PoDragDropModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoDragDropModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoDragDropModule,
    imports: [PoDragDirective, PoDropListDirective],
    exports: [PoDragDirective, PoDropListDirective]
  });
  static ɵinj = ɵɵdefineInjector({});
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoDragDropModule, [{
    type: NgModule,
    args: [{
      imports: [PoDragDirective, PoDropListDirective],
      exports: [PoDragDirective, PoDropListDirective]
    }]
  }], null, null);
})();
var PoDirectivesModule = class _PoDirectivesModule {
  static ɵfac = function PoDirectivesModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoDirectivesModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoDirectivesModule,
    imports: [PoDragDropModule, PoTooltipModule],
    exports: [PoDragDropModule, PoTooltipModule]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [PoDragDropModule, PoTooltipModule, PoDragDropModule, PoTooltipModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoDirectivesModule, [{
    type: NgModule,
    args: [{
      declarations: [],
      imports: [PoDragDropModule, PoTooltipModule],
      exports: [PoDragDropModule, PoTooltipModule],
      providers: [],
      bootstrap: []
    }]
  }], null, null);
})();
var PoFieldContainerBottomComponent = class _PoFieldContainerBottomComponent {
  poTooltip;
  helperEl;
  /** Define se o tooltip será inserido no `body` em vez do componente. */
  appendBox = false;
  disabled = false;
  /**
   * Mensagem que será apresentada quando o pattern ou a máscara não for satisfeita.
   * Obs: Esta mensagem não é apresentada quando o campo estiver vazio, mesmo que ele seja requerido.
   */
  errorPattern = "";
  /**
   * Limita a exibição da mensagem de erro a duas linhas e exibe um tooltip com o texto completo.
   */
  errorLimit = false;
  /** Texto de apoio do campo. */
  help;
  /** Define o tamanho do componente. */
  size;
  /** Configurações do ícone de ajuda adicional vínculado ao label. */
  poHelperComponent = input(void 0, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "poHelperComponent"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-helper"
  }));
  /** Define se o componente helper estará visível através das ações customizadas */
  showHelperComponent = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "showHelperComponent"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-show-helper"
  }));
  isInitialChange = true;
  ngOnChanges(changes) {
    if (changes.showHelperComponent && this.showHelperComponent()) {
      if (typeof this.poHelperComponent()?.eventOnClick === "function") {
        this.poHelperComponent()?.eventOnClick();
        return;
      }
      this.helperEl?.openHelperPopover();
    } else if (changes.showHelperComponent && !this.showHelperComponent()) {
      this.helperEl?.closeHelperPopover();
    }
  }
  static ɵfac = function PoFieldContainerBottomComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoFieldContainerBottomComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoFieldContainerBottomComponent,
    selectors: [["po-field-container-bottom"]],
    viewQuery: function PoFieldContainerBottomComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(PoTooltipDirective, 5)(_c17, 5, PoHelperComponent);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.poTooltip = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.helperEl = _t.first);
      }
    },
    hostVars: 1,
    hostBindings: function PoFieldContainerBottomComponent_HostBindings(rf, ctx) {
      if (rf & 2) {
        ɵɵattribute("p-size", ctx.size);
      }
    },
    inputs: {
      appendBox: [2, "p-append-in-body", "appendBox", convertToBoolean],
      disabled: [0, "p-disabled", "disabled"],
      errorPattern: [0, "p-error-pattern", "errorPattern"],
      errorLimit: [0, "p-error-limit", "errorLimit"],
      help: [0, "p-help", "help"],
      size: [0, "p-size", "size"],
      poHelperComponent: [1, "p-helper", "poHelperComponent"],
      showHelperComponent: [1, "p-show-helper", "showHelperComponent"]
    },
    standalone: false,
    features: [ɵɵNgOnChangesFeature],
    decls: 4,
    vars: 6,
    consts: [["helperEl", ""], ["role", "alert", "aria-live", "assertive", "tabindex", "-1", 1, "po-field-container-bottom", "po-footer-group"], [1, "po-field-container-bottom-text-error"], [1, "po-field-container-bottom-help-text"], [3, "p-size", "p-helper", "p-disabled", "p-append-in-body"], ["p-icon", "ICON_EXCLAMATION"], ["p-tooltip-position", "top", 1, "po-field-error-message", 3, "p-tooltip", "p-append-in-body"]],
    template: function PoFieldContainerBottomComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "div", 1);
        ɵɵconditionalCreate(1, PoFieldContainerBottomComponent_Conditional_1_Template, 4, 5, "div", 2);
        ɵɵconditionalCreate(2, PoFieldContainerBottomComponent_Conditional_2_Template, 2, 1, "div", 3);
        ɵɵconditionalCreate(3, PoFieldContainerBottomComponent_Conditional_3_Template, 2, 4, "po-helper", 4);
        ɵɵelementEnd();
      }
      if (rf & 2) {
        ɵɵclassProp("po-field-container-bottom-disabled", ctx.disabled);
        ɵɵattribute("aria-label", ctx.errorPattern && ctx.errorLimit ? ctx.errorPattern : null);
        ɵɵadvance();
        ɵɵconditional(ctx.errorPattern ? 1 : -1);
        ɵɵadvance();
        ɵɵconditional(!ctx.errorPattern && ctx.help ? 2 : -1);
        ɵɵadvance();
        ɵɵconditional(ctx.poHelperComponent() ? 3 : -1);
      }
    },
    dependencies: [PoIconComponent, PoTooltipDirective, PoHelperComponent],
    encapsulation: 2,
    changeDetection: 0
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoFieldContainerBottomComponent, [{
    type: Component,
    args: [{
      selector: "po-field-container-bottom",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: '<div\n  class="po-field-container-bottom po-footer-group"\n  [class.po-field-container-bottom-disabled]="disabled"\n  [attr.aria-label]="errorPattern && errorLimit ? errorPattern : null"\n  role="alert"\n  aria-live="assertive"\n  tabindex="-1"\n>\n  @if (errorPattern) {\n    <div class="po-field-container-bottom-text-error">\n      <po-icon p-icon="ICON_EXCLAMATION"></po-icon>\n      <span\n        class="po-field-error-message"\n        [class.po-field-error-limit]="errorLimit"\n        [p-tooltip]="errorLimit ? errorPattern : null"\n        p-tooltip-position="top"\n        [p-append-in-body]="appendBox"\n      >\n        {{ errorPattern }}\n      </span>\n    </div>\n  }\n  @if (!errorPattern && help) {\n    <div class="po-field-container-bottom-help-text">\n      @if (help) {\n        <span>\n          {{ help }}\n        </span>\n      }\n    </div>\n  }\n  @if (poHelperComponent()) {\n    <po-helper\n      #helperEl\n      [p-size]="size"\n      [p-helper]="poHelperComponent()"\n      [p-disabled]="disabled"\n      [p-append-in-body]="appendBox"\n    >\n    </po-helper>\n  }\n</div>\n'
    }]
  }], null, {
    poTooltip: [{
      type: ViewChild,
      args: [PoTooltipDirective]
    }],
    helperEl: [{
      type: ViewChild,
      args: ["helperEl", {
        read: PoHelperComponent,
        static: false
      }]
    }],
    appendBox: [{
      type: Input,
      args: [{
        alias: "p-append-in-body",
        transform: convertToBoolean
      }]
    }],
    disabled: [{
      type: Input,
      args: ["p-disabled"]
    }],
    errorPattern: [{
      type: Input,
      args: ["p-error-pattern"]
    }],
    errorLimit: [{
      type: Input,
      args: ["p-error-limit"]
    }],
    help: [{
      type: Input,
      args: ["p-help"]
    }],
    size: [{
      type: HostBinding,
      args: ["attr.p-size"]
    }, {
      type: Input,
      args: ["p-size"]
    }],
    poHelperComponent: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-helper",
        required: false
      }]
    }],
    showHelperComponent: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-show-helper",
        required: false
      }]
    }]
  });
})();
var PoFieldContainerComponent = class _PoFieldContainerComponent {
  cdr;
  labelEl;
  helperEl;
  /** Indica se o campo será desabilitado. */
  disabled;
  /** Identificador do campo */
  id;
  /** Label do campo. */
  label;
  /** Texto de apoio do campo. */
  help;
  /** Configurações do ícone de ajuda adicional vínculado ao label. */
  poHelperComponent = input(void 0, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "poHelperComponent"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-helper"
  }));
  /** Define se o componente helper estará visível através das ações customizadas */
  showHelperComponent = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "showHelperComponent"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-show-helper"
  }));
  /**
   * @optional
   *
   * Habilita a quebra automática do texto do componente po-label. Quando ativada, o texto que excede
   * o espaço disponível é transferido para a próxima linha em pontos apropriados para uma
   * leitura clara.
   *
   * @default `false`
   */
  textWrap = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "textWrap"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-text-wrap"
  }));
  literals;
  requirement;
  showTip = false;
  _optional = false;
  _required = false;
  /** Indica se o campo será opcional. */
  set optional(value) {
    this._optional = value;
  }
  get optional() {
    return this._optional;
  }
  /** Indica se o campo será obrigatório. */
  set required(value) {
    this._required = value;
  }
  get required() {
    return this._required;
  }
  /** Define se a indicação de campo obrigatório será exibida. */
  showRequired = false;
  /** Define o tamanho do componente. */
  size;
  /**
   * Define se o título do campo será exibido de forma compacta.
   *
   * Quando habilitado (`true`), o modo compacto afeta o conjunto composto por:
   * - `po-label`
   * - `p-requirement (showRequired)`
   * - `po-helper`
   *
   * Ou seja, todos os elementos relacionados ao título do campo
   * (rótulo, indicador de obrigatoriedade e componente auxiliar) passam
   * a seguir o comportamento de layout compacto.
   */
  compactLabel = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "compactLabel"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-compact-label",
    transform: convertToBoolean
  }));
  constructor(cdr) {
    this.cdr = cdr;
    const languageService = inject(PoLanguageService);
    const language = languageService.getShortLanguage();
    this.literals = __spreadValues({}, poFieldContainerLiterals[language]);
  }
  ngOnInit() {
    this.setRequirement();
    this.handleLabelTooltip();
  }
  ngOnChanges(changes) {
    if (changes.help || changes.label || changes.optional || changes.required || changes.showRequired || changes.textWrap) {
      this.setRequirement();
      queueMicrotask(() => this.handleLabelTooltip());
      this.cdr.detectChanges();
    }
    if (changes.showHelperComponent && this.showHelperComponent()) {
      if (typeof this.poHelperComponent()?.eventOnClick === "function") {
        this.poHelperComponent()?.eventOnClick();
        return;
      }
      this.helperEl?.openHelperPopover();
    } else if (changes.showHelperComponent && !this.showHelperComponent()) {
      this.helperEl?.closeHelperPopover();
    }
  }
  setRequirement() {
    if (this.label || this.help) {
      if (!this.required && this.optional) {
        this.requirement = this.literals["optional"];
      } else if (this.required && this.showRequired) {
        this.requirement = this.literals["required"];
      } else {
        this.requirement = void 0;
      }
    }
  }
  handleLabelTooltip() {
    this.showTip = updateTooltip(this.showTip, this.labelEl);
    this.cdr.markForCheck();
  }
  static ɵfac = function PoFieldContainerComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoFieldContainerComponent)(ɵɵdirectiveInject(ChangeDetectorRef));
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoFieldContainerComponent,
    selectors: [["po-field-container"]],
    viewQuery: function PoFieldContainerComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c18, 5, ElementRef)(_c17, 5, PoHelperComponent);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.labelEl = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.helperEl = _t.first);
      }
    },
    hostVars: 1,
    hostBindings: function PoFieldContainerComponent_HostBindings(rf, ctx) {
      if (rf & 2) {
        ɵɵattribute("p-size", ctx.size);
      }
    },
    inputs: {
      disabled: [0, "p-disabled", "disabled"],
      id: [0, "p-id", "id"],
      label: [0, "p-label", "label"],
      help: [0, "p-help", "help"],
      poHelperComponent: [1, "p-helper", "poHelperComponent"],
      showHelperComponent: [1, "p-show-helper", "showHelperComponent"],
      textWrap: [1, "p-text-wrap", "textWrap"],
      optional: [2, "p-optional", "optional", convertToBoolean],
      required: [2, "p-required", "required", convertToBoolean],
      showRequired: [0, "p-show-required", "showRequired"],
      size: [0, "p-size", "size"],
      compactLabel: [1, "p-compact-label", "compactLabel"]
    },
    standalone: false,
    features: [ɵɵNgOnChangesFeature],
    ngContentSelectors: _c12,
    decls: 5,
    vars: 4,
    consts: [["labelEl", ""], ["helperEl", ""], [1, "po-field-container", 3, "mouseenter"], [1, "po-field-container-title"], [1, "po-field-help"], ["p-field", "true", 3, "p-disabled", "p-for", "p-label", "p-requirement", "p-text-wrap", "p-tooltip"], [3, "p-disabled", "p-helper", "p-size"]],
    template: function PoFieldContainerComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵprojectionDef();
        ɵɵelementStart(0, "div", 2);
        ɵɵlistener("mouseenter", function PoFieldContainerComponent_Template_div_mouseenter_0_listener() {
          return ctx.handleLabelTooltip();
        });
        ɵɵelementStart(1, "div", 3);
        ɵɵconditionalCreate(2, PoFieldContainerComponent_Conditional_2_Template, 3, 7);
        ɵɵelementEnd();
        ɵɵconditionalCreate(3, PoFieldContainerComponent_Conditional_3_Template, 2, 1, "div", 4);
        ɵɵprojection(4);
        ɵɵelementEnd();
      }
      if (rf & 2) {
        ɵɵadvance();
        ɵɵclassProp("po-field-container-compact-label", ctx.compactLabel());
        ɵɵadvance();
        ɵɵconditional(ctx.label || ctx.help ? 2 : -1);
        ɵɵadvance();
        ɵɵconditional(ctx.help ? 3 : -1);
      }
    },
    dependencies: [PoLabelComponent, PoTooltipDirective, PoHelperComponent],
    encapsulation: 2,
    changeDetection: 0
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoFieldContainerComponent, [{
    type: Component,
    args: [{
      selector: "po-field-container",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: '<div class="po-field-container" (mouseenter)="handleLabelTooltip()">\n  <div class="po-field-container-title" [class.po-field-container-compact-label]="compactLabel()">\n    @if (label || help) {\n      <po-label\n        #labelEl\n        [p-disabled]="disabled"\n        p-field="true"\n        [p-for]="id"\n        [p-label]="label"\n        [p-requirement]="requirement"\n        [p-text-wrap]="textWrap()"\n        [p-tooltip]="showTip ? label : null"\n      ></po-label>\n      @if (poHelperComponent()) {\n        <po-helper\n          #helperEl\n          [p-disabled]="disabled"\n          [p-helper]="poHelperComponent()"\n          [p-size]="poHelperComponent().size ? poHelperComponent().size : null"\n        ></po-helper>\n      }\n    }\n  </div>\n\n  @if (help) {\n    <div class="po-field-help">{{ help }}</div>\n  }\n\n  <ng-content></ng-content>\n</div>\n'
    }]
  }], () => [{
    type: ChangeDetectorRef
  }], {
    labelEl: [{
      type: ViewChild,
      args: ["labelEl", {
        read: ElementRef
      }]
    }],
    helperEl: [{
      type: ViewChild,
      args: ["helperEl", {
        read: PoHelperComponent,
        static: false
      }]
    }],
    disabled: [{
      type: Input,
      args: ["p-disabled"]
    }],
    id: [{
      type: Input,
      args: ["p-id"]
    }],
    label: [{
      type: Input,
      args: ["p-label"]
    }],
    help: [{
      type: Input,
      args: ["p-help"]
    }],
    poHelperComponent: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-helper",
        required: false
      }]
    }],
    showHelperComponent: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-show-helper",
        required: false
      }]
    }],
    textWrap: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-text-wrap",
        required: false
      }]
    }],
    optional: [{
      type: Input,
      args: [{
        alias: "p-optional",
        transform: convertToBoolean
      }]
    }],
    required: [{
      type: Input,
      args: [{
        alias: "p-required",
        transform: convertToBoolean
      }]
    }],
    showRequired: [{
      type: Input,
      args: ["p-show-required"]
    }],
    size: [{
      type: HostBinding,
      args: ["attr.p-size"]
    }, {
      type: Input,
      args: ["p-size"]
    }],
    compactLabel: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-compact-label",
        required: false
      }]
    }]
  });
})();
var PoCheckboxComponent = class _PoCheckboxComponent extends PoCheckboxBaseComponent {
  changeDetector = inject(ChangeDetectorRef);
  showTip = false;
  checkboxLabel;
  labelEl;
  helperEl;
  constructor() {
    const changeDetector = inject(ChangeDetectorRef);
    super(changeDetector);
    this.changeDetector = changeDetector;
  }
  /**
   * Função que atribui foco ao *checkbox*.
   *
   * Para utilizá-la é necessário capturar a referência do componente no DOM através do `ViewChild`, como por exemplo:
   *
   * ```
   * ...
   * import { ViewChild } from '@angular/core';
   * import { PoCheckboxComponent } from '@po-ui/ng-components';
   *
   * ...
   *
   * @ViewChild(PoCheckboxComponent, { static: true }) checkbox: PoCheckboxComponent;
   *
   * focusCheckbox() {
   *   this.checkbox.focus();
   * }
   * ```
   */
  focus() {
    if (this.checkboxLabel && !this.disabled) {
      this.checkboxLabel.nativeElement.focus();
    }
  }
  onBlur() {
    this.onTouched?.();
    this.blur.emit();
  }
  ngAfterViewInit() {
    this.handleLabelTooltip();
    if (this.autoFocus) {
      this.focus();
    }
  }
  ngOnChanges(changes) {
    if (changes.label || changes.additionalHelpTooltip || changes.helper || changes.size) {
      this.displayAdditionalHelp = false;
      queueMicrotask(() => this.handleLabelTooltip());
      this.changeDetector.detectChanges();
    }
  }
  ngOnInit() {
    this.handleLabelTooltip();
  }
  emitAdditionalHelp() {
    if (this.label && this.isAdditionalHelpEventTriggered()) {
      this.additionalHelp.emit();
    }
  }
  getAdditionalHelpTooltip() {
    return this.isAdditionalHelpEventTriggered() ? null : this.additionalHelpTooltip;
  }
  onKeyDown(event, value) {
    const isFieldFocused = document.activeElement === this.checkboxLabel.nativeElement;
    if (event.which === PoKeyCodeEnum.space || event.keyCode === PoKeyCodeEnum.space) {
      this.checkOption(event, value);
      event.preventDefault();
    }
    if (isFieldFocused) {
      this.keydown.emit(event);
    }
  }
  /**
   * Método que exibe `p-helper` ou executa a ação definida em `p-helper{eventOnClick}` ou em `p-additionalHelp`.
   * Para isso, será necessário configurar uma tecla de atalho utilizando o evento `p-keydown`.
   *
   * > Exibe ou oculta o conteúdo do componente `po-helper` quando o componente estiver com foco.
   *
   * ```
   * //Exemplo com label e p-helper
   * <po-checkbox
   *  #checkbox
   *  ...
   *  p-label="Label do checkbox"
   *  [p-helper]="helperOptions"
   *  (p-keydown)="onKeyDown($event, checkbox)"
   * ></po-checkbox>
   * ```
   * ```
   * ...
   * onKeyDown(event: KeyboardEvent, inp: PoCheckboxComponent): void {
   *  if (event.code === 'F9') {
   *    inp.showAdditionalHelp();
   *  }
   * }
   * ```
   */
  showAdditionalHelp() {
    this.displayAdditionalHelp = !this.displayAdditionalHelp;
    if (this.helperEl?.helperIsVisible()) {
      this.helperEl?.closeHelperPopover();
      return;
    }
    if (this.helperEl) {
      const helper = this.poHelperComponent();
      const isHelpEvt = this.isAdditionalHelpEventTriggered();
      if (isHelpEvt) {
        this.additionalHelp.emit();
        return;
      }
      if (helper && typeof helper !== "string" && helper.eventOnClick) {
        helper.eventOnClick();
        return;
      }
      this.helperEl?.openHelperPopover();
    }
    return this.displayAdditionalHelp;
  }
  setHelper(label, additionalHelpTooltip) {
    return setHelperSettings(label, additionalHelpTooltip, this.poHelperComponent(), this.size, this.isAdditionalHelpEventTriggered() ? this.additionalHelp : void 0);
  }
  changeModelValue(value) {
    if (value === null) {
      this.checkboxValue = "mixed";
    } else {
      this.checkboxValue = typeof value === "boolean" || value === null ? value : false;
    }
    this.changeDetector.detectChanges();
  }
  isAdditionalHelpEventTriggered() {
    return this.additionalHelpEventTrigger === "event" || this.additionalHelpEventTrigger === void 0 && this.additionalHelp.observed;
  }
  handleLabelTooltip() {
    this.showTip = updateTooltip(this.showTip, this.labelEl);
    this.changeDetector.markForCheck();
  }
  static ɵfac = function PoCheckboxComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoCheckboxComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoCheckboxComponent,
    selectors: [["po-checkbox"]],
    viewQuery: function PoCheckboxComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c19, 5)(_c18, 5, ElementRef)(_c17, 5, PoHelperComponent);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.checkboxLabel = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.labelEl = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.helperEl = _t.first);
      }
    },
    standalone: false,
    features: [ɵɵProvidersFeature([{
      provide: NG_VALUE_ACCESSOR,
      useExisting: forwardRef(() => _PoCheckboxComponent),
      multi: true
    }]), ɵɵInheritDefinitionFeature, ɵɵNgOnChangesFeature],
    decls: 5,
    vars: 2,
    consts: [["checkboxContent", ""], ["checkboxLabel", ""], ["labelEl", ""], ["helperEl", ""], [3, "p-disabled", "p-size", "p-compact-label"], [1, "po-field-container-content"], [4, "ngTemplateOutlet"], [3, "p-help", "p-size"], [1, "container-po-checkbox", 3, "click", "keydown"], ["role", "checkbox", 1, "po-checkbox-outline", 3, "blur", "tabindex"], ["aria-label", " ", 1, "po-checkbox", "po-checkbox-phosphor", 3, "id"], ["tabindex", "-1", 1, "po-checkbox-label", 3, "p-disabled", "p-for", "p-label", "p-text-wrap", "p-tooltip"], [1, "po-checkbox-helper", 3, "p-helper", "p-disabled", "p-append-in-body", "p-size"], ["tabindex", "-1", 1, "po-checkbox-label", 3, "mouseenter", "focus", "p-disabled", "p-for", "p-label", "p-text-wrap", "p-tooltip"]],
    template: function PoCheckboxComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵdeclareLet(0);
        ɵɵconditionalCreate(1, PoCheckboxComponent_Conditional_1_Template, 4, 6, "po-field-container", 4)(2, PoCheckboxComponent_Conditional_2_Template, 1, 1, "ng-container");
        ɵɵtemplate(3, PoCheckboxComponent_ng_template_3_Template, 6, 11, "ng-template", null, 0, ɵɵtemplateRefExtractor);
      }
      if (rf & 2) {
        ɵɵstoreLet(ctx.setHelper(ctx.label, ctx.additionalHelpTooltip));
        ɵɵadvance();
        ɵɵconditional(ctx.help ? 1 : 2);
      }
    },
    dependencies: [NgTemplateOutlet, PoLabelComponent, PoFieldContainerBottomComponent, PoFieldContainerComponent, PoHelperComponent, PoTooltipDirective],
    encapsulation: 2,
    changeDetection: 0
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoCheckboxComponent, [{
    type: Component,
    args: [{
      selector: "po-checkbox",
      changeDetection: ChangeDetectionStrategy.OnPush,
      providers: [{
        provide: NG_VALUE_ACCESSOR,
        useExisting: forwardRef(() => PoCheckboxComponent),
        multi: true
      }],
      standalone: false,
      template: `@let poHelper = setHelper(label, additionalHelpTooltip);
@if (help) {
  <po-field-container [p-disabled]="disabled" [p-size]="size" [p-compact-label]="compactLabel()">
    <div class="po-field-container-content">
      <ng-container *ngTemplateOutlet="checkboxContent"></ng-container>
    </div>
    <po-field-container-bottom [p-help]="help" [p-size]="size"></po-field-container-bottom>
  </po-field-container>
} @else {
  <ng-container *ngTemplateOutlet="checkboxContent"></ng-container>
}

<ng-template #checkboxContent>
  <div
    class="container-po-checkbox"
    [attr.checked]="checkboxValue"
    (click)="checkOption($event, checkboxValue)"
    (keydown)="onKeyDown($event, checkboxValue)"
  >
    <div
      #checkboxLabel
      role="checkbox"
      class="po-checkbox-outline"
      [tabindex]="disabled || disabladTabindex ? -1 : 0"
      [attr.aria-checked]="checkboxValue"
      [attr.data-focused-element]="!disabled"
      [attr.data-inactive-component]="disabled"
      (blur)="onBlur()"
    >
      <span
        [attr.aria-checked]="checkboxValue"
        aria-label=" "
        [id]="id"
        class="po-checkbox po-checkbox-phosphor"
        [attr.aria-disabled]="disabled"
        [attr.required]="checkBoxRequired"
      >
      </span>

      @if (label) {
        <po-label
          #labelEl
          class="po-checkbox-label"
          tabindex="-1"
          [p-disabled]="disabled"
          [p-for]="id"
          [p-label]="label"
          [p-text-wrap]="labelTextWrap()"
          [p-tooltip]="showTip ? label : null"
          (mouseenter)="handleLabelTooltip()"
          (focus)="handleLabelTooltip()"
        >
        </po-label>
      }
      @if (poHelperComponent() || poHelper.hideAdditionalHelp) {
        <po-helper
          #helperEl
          class="po-checkbox-helper"
          [p-helper]="poHelper.helperSettings"
          [p-disabled]="disabled"
          [p-append-in-body]="appendBox"
          [p-size]="size === 'large' ? 'medium' : size"
        >
        </po-helper>
      }
    </div>
  </div>
</ng-template>
`
    }]
  }], () => [], {
    checkboxLabel: [{
      type: ViewChild,
      args: ["checkboxLabel", {
        static: false
      }]
    }],
    labelEl: [{
      type: ViewChild,
      args: ["labelEl", {
        read: ElementRef
      }]
    }],
    helperEl: [{
      type: ViewChild,
      args: ["helperEl", {
        read: PoHelperComponent,
        static: false
      }]
    }]
  });
})();
var PoItemListComponent = class _PoItemListComponent extends PoItemListBaseComponent {
  sanitized = inject(DomSanitizer);
  itemList;
  selectedView;
  param;
  clickListener;
  shouldUpdateSelected(changes) {
    return !!(changes.searchValue && changes.label && changes.searchValue.currentValue === changes.label.currentValue);
  }
  ngOnChanges(changes) {
    if (this.shouldUpdateSelected(changes)) {
      this.selected = true;
    }
    if (this.isTabs) {
      if (changes.tabHide?.currentValue || changes.disabled?.currentValue) {
        this.tabsItem.emit(this.item);
      } else if (changes.activeTabs?.currentValue) {
        this.emitActiveTabs(this.item);
      }
    }
  }
  onCheckboxItem() {
    const option = {
      [this.fieldValue]: this.value,
      [this.fieldLabel]: this.label
    };
    const selected = !this.checkboxValue;
    this.checkboxItem.emit({
      option,
      selected
    });
  }
  onComboItem(options, event) {
    const option = {
      [this.fieldValue]: this.value,
      [this.fieldLabel]: this.label
    };
    this.selectedView = options;
    this.comboItem.emit(__spreadProps(__spreadValues({}, option), {
      event
    }));
  }
  compareObjects(obj1, obj2) {
    return JSON.stringify(obj1) === JSON.stringify(obj2);
  }
  onCheckboxItemEmit(event) {
    if (event && event.code === "Enter" || event.code === "Space") {
      this.onCheckboxItem();
    }
  }
  getLabelFormatted(label) {
    const sanitizedLabel = this.sanitizeTagHTML(label);
    let format2 = sanitizedLabel;
    if (this.isFiltering || this.validateForOptionsLabel()) {
      const labelInput = this.sanitizeTagHTML(this.searchValue.toString().toLowerCase());
      const labelLowerCase = sanitizedLabel.toLowerCase();
      const highlightClass = this.size === PoFieldSize.Small ? "po-font-text-bold" : "po-font-text-large-bold";
      const openTagBold = `<span class="${highlightClass}">`;
      const closeTagBold = "</span>";
      let startString;
      let middleString;
      let endString;
      switch (this.filterMode) {
        case PoItemListFilterMode.startsWith:
        case PoItemListFilterMode.contains: {
          const indexOfLabelInput = labelLowerCase.indexOf(labelInput);
          if (indexOfLabelInput > -1) {
            startString = sanitizedLabel.substring(0, indexOfLabelInput);
            middleString = sanitizedLabel.substring(indexOfLabelInput, indexOfLabelInput + labelInput.length);
            endString = sanitizedLabel.substring(indexOfLabelInput + labelInput.length);
            format2 = startString + openTagBold + middleString + closeTagBold + endString;
          }
          break;
        }
        case PoItemListFilterMode.endsWith: {
          const lastIndexOfLabelInput = labelLowerCase.lastIndexOf(labelInput);
          if (lastIndexOfLabelInput > -1) {
            startString = sanitizedLabel.substring(0, lastIndexOfLabelInput);
            middleString = sanitizedLabel.substring(lastIndexOfLabelInput);
            format2 = startString + openTagBold + middleString + closeTagBold;
          }
          break;
        }
      }
    }
    return this.safeHtml(format2);
  }
  validateForOptionsLabel() {
    return this.comboService && this.searchValue && !this.compareCache && this.shouldMarkLetters;
  }
  safeHtml(value) {
    return this.sanitized.bypassSecurityTrustHtml(value);
  }
  sanitizeTagHTML(value = "") {
    return value.replace(/</g, "&lt;").replace(/>/g, "&gt;");
  }
  static ɵfac = /* @__PURE__ */ (() => {
    let ɵPoItemListComponent_BaseFactory;
    return function PoItemListComponent_Factory(__ngFactoryType__) {
      return (ɵPoItemListComponent_BaseFactory || (ɵPoItemListComponent_BaseFactory = ɵɵgetInheritedFactory(_PoItemListComponent)))(__ngFactoryType__ || _PoItemListComponent);
    };
  })();
  static ɵcmp = ɵɵdefineComponent({
    type: _PoItemListComponent,
    selectors: [["po-item-list"]],
    viewQuery: function PoItemListComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c20, 7);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.itemList = _t.first);
      }
    },
    standalone: false,
    features: [ɵɵInheritDefinitionFeature, ɵɵNgOnChangesFeature],
    decls: 4,
    vars: 1,
    consts: [["itemList", ""], [3, "keydown"], [1, "po-item-list", "po-item-list__action", 3, "po-item-list__separator", "po-item-list__disabled", "po-item-list__selected", "po-item-list__hidden", "po-item-list__danger"], [1, "po-item-list", "po-item-list__option", 3, "po-item-list__separator", "po-item-list__selected"], [1, "po-item-list", "po-item-list__check", 3, "po-item-list__separator", "po-item-list__selected", "po-item-list__disabled"], [1, "po-item-list", "po-item-list__action"], [1, "po-popup-icon-item", "po-field-icon", 3, "po-field-icon-aa", "p-icon"], [1, "po-item-list-label"], [1, "po-popup-icon-item-right", "po-field-icon", 3, "p-icon"], [1, "po-popup-icon-item", "po-field-icon", 3, "p-icon"], [1, "po-item-list", "po-item-list__option", 3, "click"], [3, "ngTemplateOutlet", "ngTemplateOutletContext"], [1, "po-item-list-label", 3, "innerHTML"], [1, "po-item-list-label", "po-item-list-label-group"], [1, "po-item-list-sublabel"], [1, "po-item-list", "po-item-list__check", 3, "click", "keydown", "keyup"], [3, "p-disabled-tabindex", "p-checkboxValue", "p-disabled", "p-size"], [1, "po-item-list__label"]],
    template: function PoItemListComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementContainerStart(0, 1);
        ɵɵlistener("keydown", function PoItemListComponent_Template_ng_container_keydown_0_listener($event) {
          return ctx.onCheckboxItemEmit($event);
        });
        ɵɵconditionalCreate(1, PoItemListComponent_Case_1_Template, 6, 15, "div", 2)(2, PoItemListComponent_Case_2_Template, 4, 11, "div", 3)(3, PoItemListComponent_Case_3_Template, 4, 17, "div", 4);
        ɵɵelementContainerEnd();
      }
      if (rf & 2) {
        let tmp_0_0;
        ɵɵadvance();
        ɵɵconditional((tmp_0_0 = ctx.type) === "option" ? 2 : tmp_0_0 === "check" ? 3 : 1);
      }
    },
    dependencies: [NgTemplateOutlet, PoCheckboxComponent, PoIconComponent],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoItemListComponent, [{
    type: Component,
    args: [{
      selector: "po-item-list",
      standalone: false,
      template: `<ng-container (keydown)="onCheckboxItemEmit($event)">
  @switch (type) {
    @default {
      <div
        #itemList
        [class.po-item-list__separator]="separator"
        [class.po-item-list__disabled]="disabled || !visible"
        [class.po-item-list__selected]="(selected || activeTabs) && !disabled"
        [class.po-item-list__hidden]="tabHide"
        [attr.aria-disabled]="disabled || !visible"
        [attr.aria-label]="label"
        [class.po-item-list__danger]="danger"
        class="po-item-list po-item-list__action"
      >
        @if (icon) {
          <po-icon
            class="po-popup-icon-item po-field-icon"
            [class.po-field-icon-aa]="size === 'small'"
            [p-icon]="icon"
          ></po-icon>
        }
        <span class="po-item-list-label">{{ label }}</span>

        @if (iconArrowRight) {
          <po-icon class="po-popup-icon-item-right po-field-icon" [p-icon]="iconArrowRight"></po-icon>
        }
      </div>
    }
    @case ('option') {
      <div
        #itemList
        class="po-item-list po-item-list__option"
        [class.po-item-list__separator]="separator"
        [class.po-item-list__selected]="selected && !disabled"
        [attr.aria-disabled]="disabled"
        [attr.aria-label]="label"
        (click)="onComboItem(item, $event)"
      >
        @if (!template) {
          @switch (keysLabel.length) {
            @case (0) {
              <span class="po-item-list-label" [innerHTML]="getLabelFormatted(label)"></span>
            }

            @case (1) {
              <span class="po-item-list-label">{{ item[keysLabel[0]] }}</span>
            }

            @case (2) {
              <div class="po-item-list-label po-item-list-label-group">
                <span class="po-item-list-label">{{ item[keysLabel[0]] }}</span>
                <span class="po-item-list-sublabel">{{ item[keysLabel[1]] }}</span>
              </div>
            }

            @default {
              <div class="po-item-list-label po-item-list-label-group">
                <span class="po-item-list-label">{{ item[keysLabel[0]] }}</span>
                <span class="po-item-list-sublabel">{{ item[keysLabel[1]] }} - {{ item[keysLabel[2]] }}</span>
              </div>
            }
          }
        }

        <ng-template
          [ngTemplateOutlet]="template?.templateRef"
          [ngTemplateOutletContext]="{ $implicit: templateContext }"
        >
        </ng-template>
      </div>
    }
    @case ('check') {
      <div
        class="po-item-list po-item-list__check"
        [class.po-item-list__separator]="separator"
        [class.po-item-list__selected]="selected && !disabled"
        [class.po-item-list__disabled]="disabled"
        [attr.aria-disabled]="disabled"
        [attr.aria-label]="label"
        (click)="onCheckboxItem()"
        (keydown)="onCheckboxItemEmit($event)"
        (keyup)="onCheckboxItemEmit($event)"
      >
        <po-checkbox
          [p-disabled-tabindex]="true"
          [p-checkboxValue]="checkboxValue === null ? 'mixed' : checkboxValue"
          [p-disabled]="disabled"
          [p-size]="size"
        ></po-checkbox>
        @if (!template) {
          <span class="po-item-list__label">{{ label }}</span>
        }
        <ng-template
          [ngTemplateOutlet]="template?.templateRef"
          [ngTemplateOutletContext]="{ $implicit: templateContext }"
        >
        </ng-template>
      </div>
    }
  }
</ng-container>
`
    }]
  }], null, {
    itemList: [{
      type: ViewChild,
      args: ["itemList", {
        static: true
      }]
    }]
  });
})();
var PoSearchListComponent = class _PoSearchListComponent {
  cd = inject(ChangeDetectorRef);
  inputElement;
  /** Propriedade que recebe as literais definidas no `po-listbox`. */
  literals;
  fieldValue;
  /** Tamanho do texto de exibido do item. */
  size;
  /** Evento que será disparado a cada tecla digitada no campo de busca. */
  change = new EventEmitter();
  _placeholder;
  initUp = false;
  initDown = false;
  /**
   * @optional
   *
   * @description
   *
   * Placeholder do campo de pesquisa.
   *
   * > Caso o mesmo não seja informado, o valor padrão será traduzido com base no idioma do navegador (pt, es e en).
   *
   * @default `Buscar`
   */
  set placeholder(placeholder) {
    this._placeholder = placeholder && this.isTypeof(placeholder, "string") ? placeholder : this.literals.placeholderSearch;
  }
  get placeholder() {
    return this._placeholder || this.literals.placeholderSearch;
  }
  get inputValue() {
    return this.inputElement.nativeElement.value;
  }
  onChange(event) {
    if (event.key === "Tab") {
      event.preventDefault();
    }
    this.initUp = true;
    if (this.initDown === this.initUp) {
      this.change.emit({
        event,
        [this.fieldValue]: this.inputElement.nativeElement.value
      });
    }
  }
  setFocus() {
    this.inputElement.nativeElement.focus();
  }
  clean() {
    this.inputElement.nativeElement.value = "";
    this.cd.markForCheck();
  }
  resetKeys() {
    this.initUp = false;
    this.initDown = false;
  }
  isTypeof(object, type) {
    return typeof object === type;
  }
  changeInitSearch(event) {
    if (event.key === "Tab") {
      event.preventDefault();
    }
    this.initDown = true;
  }
  static ɵfac = function PoSearchListComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoSearchListComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoSearchListComponent,
    selectors: [["po-search-list"]],
    viewQuery: function PoSearchListComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c22, 7, ElementRef);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.inputElement = _t.first);
      }
    },
    hostVars: 1,
    hostBindings: function PoSearchListComponent_HostBindings(rf, ctx) {
      if (rf & 2) {
        ɵɵattribute("p-size", ctx.size);
      }
    },
    inputs: {
      literals: [0, "p-literals", "literals"],
      fieldValue: [0, "p-field-value", "fieldValue"],
      size: [0, "p-size", "size"],
      placeholder: [0, "p-placeholder", "placeholder"]
    },
    outputs: {
      change: "p-change"
    },
    standalone: false,
    decls: 5,
    vars: 6,
    consts: [["inputElement", ""], [1, "po-field-container-input", "po-listbox-container-search"], [1, "po-field-icon-container-left"], ["p-icon", "ICON_SEARCH", 1, "po-field-icon", "po-icon-input"], ["type", "text", 1, "po-input", "po-input-icon-left", "po-listbox-search-input", 3, "keyup", "keydown", "placeholder"]],
    template: function PoSearchListComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "div", 1)(1, "div", 2);
        ɵɵelement(2, "po-icon", 3);
        ɵɵelementEnd();
        ɵɵelementStart(3, "input", 4, 0);
        ɵɵlistener("keyup", function PoSearchListComponent_Template_input_keyup_3_listener($event) {
          return ctx.onChange($event);
        })("keydown", function PoSearchListComponent_Template_input_keydown_3_listener($event) {
          return ctx.changeInitSearch($event);
        });
        ɵɵelementEnd()();
      }
      if (rf & 2) {
        ɵɵadvance(2);
        ɵɵclassProp("po-field-icon-aa", ctx.size === "small");
        ɵɵadvance();
        ɵɵclassProp("po-input-icon-left-aa", ctx.size === "small");
        ɵɵproperty("placeholder", ctx.placeholder);
        ɵɵattribute("p-size", ctx.size);
      }
    },
    dependencies: [PoIconComponent],
    encapsulation: 2,
    changeDetection: 0
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoSearchListComponent, [{
    type: Component,
    args: [{
      selector: "po-search-list",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: `<div class="po-field-container-input po-listbox-container-search">
  <div class="po-field-icon-container-left">
    <po-icon
      p-icon="ICON_SEARCH"
      class="po-field-icon po-icon-input"
      [class.po-field-icon-aa]="size === 'small'"
    ></po-icon>
  </div>

  <input
    #inputElement
    class="po-input po-input-icon-left po-listbox-search-input"
    [class.po-input-icon-left-aa]="size === 'small'"
    [attr.p-size]="size"
    type="text"
    [placeholder]="placeholder"
    (keyup)="onChange($event)"
    (keydown)="changeInitSearch($event)"
  />
</div>
`
    }]
  }], null, {
    inputElement: [{
      type: ViewChild,
      args: ["inputElement", {
        read: ElementRef,
        static: true
      }]
    }],
    literals: [{
      type: Input,
      args: ["p-literals"]
    }],
    fieldValue: [{
      type: Input,
      args: ["p-field-value"]
    }],
    size: [{
      type: HostBinding,
      args: ["attr.p-size"]
    }, {
      type: Input,
      args: ["p-size"]
    }],
    change: [{
      type: Output,
      args: ["p-change"]
    }],
    placeholder: [{
      type: Input,
      args: ["p-placeholder"]
    }]
  });
})();
var PoListBoxComponent = class _PoListBoxComponent extends PoListBoxBaseComponent {
  element = inject(ElementRef);
  currentItems = [];
  currentGroup = null;
  navigationStack = [];
  renderer = inject(Renderer2);
  router = inject(Router);
  changeDetector = inject(ChangeDetectorRef);
  listbox;
  listboxItemList;
  listboxGroupHeader;
  searchElement;
  popupHeaderContainer;
  listboxItems;
  cdkListbox;
  cdkOptions;
  scrollEvent$;
  subscriptionScrollEvent;
  constructor() {
    const languageService = inject(PoLanguageService);
    super(languageService);
    effect(() => {
      const visible = this.visible();
      if (visible && this.type === "option" && !this.listboxSubitems) {
        this.scrollToAndSelectCurrentItem();
      }
    });
  }
  ngOnInit() {
    if (this.listboxSubitems) {
      this.currentItems = this.items;
    }
  }
  ngAfterViewInit() {
    this.setListBoxMaxHeight();
    this.setListBoxWidth();
    this.listboxItemList?.nativeElement.focus();
    if (this.listboxSubitems) {
      requestAnimationFrame(() => {
        const firstItem = this.listboxItems?.first.nativeElement;
        if (firstItem) {
          firstItem.focus();
          setTimeout(() => {
            firstItem.dispatchEvent(new FocusEvent("focus", {
              bubbles: true
            }));
          }, 0);
        }
      });
    }
    this.changeDetector.detectChanges();
  }
  ngOnChanges(changes) {
    if (changes?.items) {
      this.setListBoxMaxHeight();
      this.setListBoxWidth();
    }
    if (this.visible() && this.infiniteScroll) {
      this.checkInfiniteScroll();
    }
  }
  ngOnDestroy() {
    if (this.subscriptionScrollEvent?.unsubscribe) {
      this.subscriptionScrollEvent.unsubscribe();
    }
  }
  openGroup(group2, event) {
    event?.stopPropagation();
    this.navigationStack.push({
      group: this.currentGroup,
      items: this.currentItems
    });
    this.currentGroup = group2;
    this.currentItems = group2.subItems || [];
    requestAnimationFrame(() => {
      const firstItem = this.listboxGroupHeader?.nativeElement;
      if (firstItem) {
        firstItem.focus();
        setTimeout(() => {
          firstItem.dispatchEvent(new FocusEvent("focus", {
            bubbles: true
          }));
        }, 0);
      }
    });
  }
  goBack(event) {
    event?.stopPropagation();
    const previous = this.navigationStack.pop();
    if (previous) {
      this.currentGroup = previous.group;
      this.currentItems = previous.items;
    } else {
      this.currentGroup = null;
      this.currentItems = this.items;
    }
    this.clickItem.emit({
      goBack: true
    });
    requestAnimationFrame(() => {
      const firstItem = this.listboxItems?.first?.nativeElement;
      if (firstItem) {
        firstItem.focus();
        setTimeout(() => {
          firstItem.dispatchEvent(new FocusEvent("focus", {
            bubbles: true
          }));
        }, 0);
      }
    });
  }
  onKeydownGoBack(event, currentGroup) {
    if (event.key === "Enter") {
      this.goBack(event);
    }
    if (event?.code === "Escape" || event.code === "Tab") {
      if (event.code === "Tab" && !event.shiftKey && currentGroup?.$subItemTemplate) {
        return;
      }
      this.closeEvent.emit();
    }
  }
  onKeydownTemplate(event) {
    if (event.code === "Tab") {
      if (event.target?.closest(".po-listbox-dropdown")) {
        event.stopPropagation();
        return;
      }
      this.closeEvent.emit();
    }
  }
  onSelectItem(itemListAction, event) {
    const isDisabled = itemListAction.hasOwnProperty("disabled") && itemListAction.disabled !== null && itemListAction.disabled !== void 0 ? this.returnBooleanValue(itemListAction, "disabled") : false;
    const isVisible = itemListAction.hasOwnProperty("visible") && itemListAction.visible !== null && itemListAction.visible !== void 0 ? this.returnBooleanValue(itemListAction, "visible") : true;
    if (this.isTabs && !itemListAction.disabled && !itemListAction.hide) {
      this.onClickTabs(itemListAction);
    }
    if (itemListAction?.action && !isDisabled && isVisible) {
      itemListAction.action(this.param || itemListAction);
    }
    if (itemListAction?.url && !isDisabled && isVisible) {
      return this.openUrl(itemListAction.url);
    }
    if (itemListAction?.subItems?.length || itemListAction?.$subItemTemplate) {
      this.openGroup(itemListAction, event);
    } else if (this.listboxSubitems) {
      this.closeEvent.emit();
    }
    if (!isDisabled) {
      this.clickItem.emit(itemListAction);
    }
  }
  onSelectAllCheckboxKeyDown(event) {
    if (event.code === "Tab") {
      this.closeEvent.emit();
    }
  }
  onKeyDown(itemListAction, event) {
    event?.preventDefault();
    if (event && event.code === "Enter" || event.code === "Space") {
      if (itemListAction.type === "footerAction") {
        this.handleFooterActionListbox();
        return;
      }
      if (this.type === "option" && (itemListAction?.action || itemListAction?.url)) {
        this.onSelectItem(itemListAction);
        this.optionClicked(itemListAction);
        return;
      }
      switch (this.type) {
        case "check":
          this.onSelectCheckBoxItem(itemListAction);
          break;
        case "option":
          this.optionClicked(itemListAction);
          break;
        case "action":
          this.onSelectItem(itemListAction);
          break;
      }
    }
    if (event && event.code === "Escape" || event.code === "Tab") {
      this.closeEvent.emit();
    }
  }
  checkboxClicked({
    option,
    selected
  }) {
    if (this.type === "check") {
      this.change.emit({
        selected,
        option
      });
    }
  }
  optionClicked(option) {
    if (this.type === "option") {
      this.items.filter((item) => item[this.fieldValue] === option[this.fieldValue] ? item["selected"] = true : item["selected"] = false);
      this.cdkListbox.selectValue(option[this.fieldLabel]);
      this.selectCombo.emit(__spreadValues({}, option));
    }
  }
  onSelectCheckBoxItem(option) {
    const selected = !this.isSelectedItem(option);
    this.checkboxClicked({
      option,
      selected
    });
  }
  isSelectedItem(option) {
    return this.selectedOptions.some((selectedItem) => selectedItem[this.fieldValue] === option[this.fieldValue]);
  }
  changeAllEmit(event) {
    if (event && event.code === "Enter" || event.code === "Space") {
      this.changeAll.emit();
    }
  }
  onSelectTabs(tab) {
    if (this.isTabs && tab) {
      this.changeStateTabs.emit(tab);
    }
  }
  onActivatedTabs(tab) {
    this.activatedTab.emit(tab);
  }
  callChangeSearch(event) {
    this.changeSearch.emit(event);
  }
  showMoreInfiniteScroll({
    target
  }) {
    const scrollPosition = target.offsetHeight + target.scrollTop;
    if (scrollPosition >= target.scrollHeight * (this.infiniteScrollDistance / 110)) {
      this.UpdateInfiniteScroll.emit();
    }
  }
  scrollListener(componentListner) {
    return fromEvent(componentListner, "scroll").pipe(debounceTime(100));
  }
  setFocus() {
    this.listboxItemList?.nativeElement?.focus();
  }
  getSelectedItem() {
    if (!this.items?.length) {
      return void 0;
    }
    return this.items.find((item) => this.isSelectedItem(item) || item?.selected);
  }
  scrollToAndSelectCurrentItem() {
    const targetOption = this.listboxItems?.toArray() ?? [];
    const selectedOption = targetOption.find((item) => item.nativeElement.getAttribute("aria-selected") === "true");
    const selectedItem = this.getSelectedItem();
    selectedOption?.nativeElement.scrollIntoView({
      block: "nearest"
    });
    if (selectedItem) {
      this.cdkListbox?.selectValue(selectedItem[this.fieldLabel]);
    }
  }
  onListboxFocusIn(event) {
    if (this.type !== "option" || this.listboxSubitems || !this.cdkListbox) {
      return;
    }
    const focusedElement = event.target;
    const focusedOption = this.cdkOptions?.find((option) => option.element === focusedElement || option.element.contains(focusedElement));
    if (focusedOption && !this.cdkListbox.isActive(focusedOption)) {
      this.cdkListbox._setActiveOption(focusedOption);
    }
  }
  checkInfiniteScroll() {
    if (this.hasInfiniteScroll()) {
      this.includeInfiniteScroll();
    }
  }
  getSizeLoading() {
    const width = this.listbox.nativeElement.offsetWidth || this.containerWidth;
    if (width > 180 && this.size !== PoFieldSize.Small) {
      return "md";
    } else if (width >= 140 || this.size === PoFieldSize.Small) {
      return "sm";
    } else {
      return "xs";
    }
  }
  getTextLoading() {
    const width = this.listbox.nativeElement.offsetWidth || this.containerWidth;
    return width < 140 ? " " : "";
  }
  hasInfiniteScroll() {
    this.changeDetector.detectChanges();
    return this.infiniteScroll && this.listboxItemList?.nativeElement.scrollHeight;
  }
  checkTemplate() {
    if (this.cache || this.infiniteScroll) {
      return this.items.length;
    } else if (!this.items.length && this.footerActionListbox) {
      return true;
    } else {
      return !this.isServerSearching && this.items.length;
    }
  }
  includeInfiniteScroll() {
    this.scrollEvent$ = this.scrollListener(this.listboxItemList?.nativeElement);
    this.subscriptionScrollEvent = this.scrollEvent$.subscribe((event) => {
      this.showMoreInfiniteScroll(event);
    });
  }
  returnBooleanValue(itemListAction, property) {
    return isTypeof(itemListAction[property], "function") ? itemListAction[property](this.param || itemListAction) : itemListAction[property];
  }
  setListBoxMaxHeight() {
    const dropdownMaxHeight = 400;
    const itemsLength = this.items.length;
    const hasPopupHeaderContainer = this.popupHeaderContainer?.nativeElement?.children?.length > 0;
    if (!this.listboxSubitems && itemsLength > 6) {
      if (this.type === "check" && !this.hideSearch) {
        this.renderer.setStyle(this.listbox.nativeElement, "maxHeight", `${44 * 6 - 44 / 3 + 60}px`);
      } else if (hasPopupHeaderContainer) {
        this.renderer.setStyle(this.listbox.nativeElement.querySelector("ul[role=listbox]"), "maxHeight", `${44 * 6 - 44 / 3}px`);
        this.renderer.removeStyle(this.listbox.nativeElement, "maxHeight");
      } else {
        this.renderer.setStyle(this.listbox.nativeElement, "maxHeight", `${44 * 6 - 44 / 3}px`);
      }
    }
    if (this.listboxSubitems) {
      this.renderer.setStyle(this.listbox.nativeElement, "maxHeight", `${dropdownMaxHeight}px`);
    }
  }
  setListBoxWidth() {
    const dropdownMinWidth = 240;
    const dropdownMaxWidth = 340;
    if (this.listboxSubitems && this.items) {
      this.renderer.setStyle(this.listbox.nativeElement, "minWidth", `${dropdownMinWidth}px`);
      this.renderer.setStyle(this.listbox.nativeElement, "maxWidth", `${dropdownMaxWidth}px`);
    }
  }
  openUrl(url) {
    if (PoUtils.isExternalLink(url)) {
      return PoUtils.openExternalLink(url);
    }
    if (url) {
      return this.router.navigate([url]);
    }
  }
  onClickTabs(tab) {
    if (!tab.disabled) {
      this.clickTab.emit(tab);
    }
  }
  formatItemList(item) {
    if (this.isTabs) {
      return item.id;
    } else {
      try {
        return JSON.stringify(item);
      } catch (error2) {
        return item;
      }
    }
  }
  handleFooterActionListbox() {
    this.footerActionListboxEvent.emit();
    this.closeEvent.emit();
  }
  static ɵfac = function PoListBoxComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoListBoxComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoListBoxComponent,
    selectors: [["po-listbox"]],
    viewQuery: function PoListBoxComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c23, 7)(_c24, 5)(_c25, 5)(_c26, 5)(_c27, 5)(_c24, 5, CdkListbox)(_c28, 5)(CdkOption, 5);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.listbox = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.listboxItemList = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.listboxGroupHeader = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.searchElement = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.popupHeaderContainer = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.cdkListbox = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.listboxItems = _t);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.cdkOptions = _t);
      }
    },
    standalone: false,
    features: [ɵɵInheritDefinitionFeature, ɵɵNgOnChangesFeature],
    ngContentSelectors: _c30,
    decls: 10,
    vars: 8,
    consts: [["listbox", ""], ["popupHeaderContainer", ""], ["searchElement", ""], ["listboxItemList", ""], ["listboxItem", ""], ["listboxGroupHeader", ""], [1, "po-listbox", 3, "hidden"], [3, "p-literals", "p-field-value", "p-placeholder", "p-size"], ["cdkListbox", "", 1, "po-listbox-list", 3, "cdkListboxMultiple"], ["cdkListbox", "", 1, "po-listbox-list", "po-listbox-dropdown"], [3, "po-listbox-container-loading-default"], [3, "p-change", "p-literals", "p-field-value", "p-placeholder", "p-size"], ["cdkListbox", "", 1, "po-listbox-list", 3, "focusin", "cdkListboxMultiple"], ["cdkOption", "selectAll", 1, "po-listbox-item-type-check"], [3, "po-listbox-item", "po-listbox-item-type-check", "cdkOption", "cdkOptionDisabled"], ["cdkOption", "selectAll", 1, "po-listbox-item-type-check", 3, "click", "keydown"], ["p-type", "check", "p-value", "selectAll", 3, "p-selected", "p-label", "p-checkbox-value", "p-size"], [3, "click", "keydown", "cdkOption", "cdkOptionDisabled"], [1, "po-combo-item-title"], [3, "p-disabled", "p-visible", "p-checkbox-value", "p-label", "p-value", "p-selected", "p-active-tabs", "p-separator", "p-danger", "p-icon", "p-type", "p-item", "p-tab-hide", "p-is-tabs", "p-field-value", "p-field-label", "p-template", "p-template-context", "p-search-value", "p-filter-mode", "p-filtering", "p-size", "p-should-mark-letter", "p-compare-cache", "p-combo-service", "p-keys-label"], [3, "p-selectcheckbox-item", "p-selectcombo-item", "p-emit-item-tabs", "p-activated-tabs", "p-disabled", "p-visible", "p-checkbox-value", "p-label", "p-value", "p-selected", "p-active-tabs", "p-separator", "p-danger", "p-icon", "p-type", "p-item", "p-tab-hide", "p-is-tabs", "p-field-value", "p-field-label", "p-template", "p-template-context", "p-search-value", "p-filter-mode", "p-filtering", "p-size", "p-should-mark-letter", "p-compare-cache", "p-combo-service", "p-keys-label"], ["aria-hidden", "true", 1, "po-item-list"], ["cdkOption", "footerActionListbox", 1, "po-listbox-item", 3, "click", "keydown"], ["p-separator", "", 3, "p-label", "p-size"], [1, "po-listbox-container-no-data", "po-text-center"], [3, "po-listbox-item", "cdkOption", "cdkOptionDisabled"], [3, "p-label", "p-item", "p-separator", "p-icon", "p-icon-arrow-right", "p-selected"], [1, "po-listbox-item-sub", 3, "p-disabled", "p-visible", "p-label", "p-item", "p-icon", "p-separator", "p-danger", "p-selected"], [1, "po-listbox-group-header", 3, "click", "keydown", "cdkOption"], [1, "po-field-icon", 3, "p-icon"], [3, "p-value"], [3, "click", "keydown", "keydown.space"], [4, "ngTemplateOutlet", "ngTemplateOutletContext"], [3, "cdkOption"], [3, "click", "keydown", "cdkOption"], [3, "p-label", "p-item", "p-separator", "p-icon", "p-icon-position"], [3, "p-size", "p-text"]],
    template: function PoListBoxComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵprojectionDef(_c29);
        ɵɵelementStart(0, "div", 6, 0)(2, "div", null, 1);
        ɵɵprojection(4);
        ɵɵelementEnd();
        ɵɵconditionalCreate(5, PoListBoxComponent_Conditional_5_Template, 2, 4, "po-search-list", 7);
        ɵɵconditionalCreate(6, PoListBoxComponent_Conditional_6_Template, 6, 3, "ul", 8)(7, PoListBoxComponent_Conditional_7_Template, 1, 1);
        ɵɵconditionalCreate(8, PoListBoxComponent_Conditional_8_Template, 3, 2, "ul", 9);
        ɵɵconditionalCreate(9, PoListBoxComponent_Conditional_9_Template, 2, 4, "div", 10);
        ɵɵelementEnd();
      }
      if (rf & 2) {
        ɵɵclassProp("po-listbox-check", ctx.type === "check" || ctx.type === "option");
        ɵɵproperty("hidden", ctx.visible());
        ɵɵattribute("data-type", ctx.type);
        ɵɵadvance(5);
        ɵɵconditional(!ctx.hideSearch && ctx.type === "check" ? 5 : -1);
        ɵɵadvance();
        ɵɵconditional(ctx.checkTemplate() && !ctx.listboxSubitems ? 6 : 7);
        ɵɵadvance(2);
        ɵɵconditional(ctx.listboxSubitems ? 8 : -1);
        ɵɵadvance();
        ɵɵconditional(ctx.isServerSearching && ctx.type !== "action" ? 9 : -1);
      }
    },
    dependencies: [NgTemplateOutlet, PoIconComponent, PoLoadingOverlayComponent, PoTagComponent, CdkListbox, CdkOption, PoItemListComponent, PoSearchListComponent],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoListBoxComponent, [{
    type: Component,
    args: [{
      selector: "po-listbox",
      standalone: false,
      template: `<div
  #listbox
  class="po-listbox"
  [class.po-listbox-check]="type === 'check' || type === 'option'"
  [attr.data-type]="type"
  [hidden]="visible()"
>
  <div #popupHeaderContainer>
    <ng-content select="[p-popup-header-template]"></ng-content>
  </div>

  @if (!hideSearch && type === 'check') {
    <po-search-list
      #searchElement
      (p-change)="callChangeSearch($event)"
      [p-literals]="literalSearch"
      [p-field-value]="fieldValueSearch"
      [p-placeholder]="placeholderSearch"
      [p-size]="size"
    ></po-search-list>
  }

  @if (checkTemplate() && !listboxSubitems) {
    <ul
      #listboxItemList
      cdkListbox
      [cdkListboxMultiple]="type === 'check'"
      class="po-listbox-list"
      (focusin)="onListboxFocusIn($event)"
    >
      @if (type === 'check' && items.length && !searchElement?.inputValue && !hideSelectAll) {
        <li
          cdkOption="selectAll"
          [attr.aria-checked]="checkboxAllValue === null ? 'mixed' : checkboxAllValue"
          class="po-listbox-item-type-check"
          (click)="changeAll.emit()"
          (keydown)="changeAllEmit($event)"
          (keydown)="onSelectAllCheckboxKeyDown($event)"
        >
          <po-item-list
            [p-selected]="checkboxAllValue"
            p-type="check"
            [p-label]="literals.selectAll"
            p-value="selectAll"
            [p-checkbox-value]="checkboxAllValue"
            [p-size]="size"
          >
          </po-item-list>
        </li>
      }
      @for (item of items; track item) {
        <li
          #listboxItem
          [class.po-listbox-item]="visible()"
          [class.po-listbox-item-type-check]="type === 'check'"
          [cdkOption]="item[fieldLabel]"
          [cdkOptionDisabled]="
            returnBooleanValue(item, 'disabled') || returnBooleanValue(item, 'visible') === false || item.options
          "
          [attr.aria-selected]="isSelectedItem(item) || item.selected"
          (click)="onSelectItem(item)"
          (keydown)="onKeyDown(item, $event)"
        >
          @if (item.options) {
            <label class="po-combo-item-title">
              {{ item.label }}
            </label>
          }
          @if (returnBooleanValue(item, 'visible') !== false) {
            <po-item-list
              [p-disabled]="returnBooleanValue(item, 'disabled')"
              [p-visible]="returnBooleanValue(item, 'visible')"
              [p-checkbox-value]="isSelectedItem(item)"
              [attr.data-item-list]="formatItemList(item)"
              [p-label]="item[fieldLabel]"
              [p-value]="item[fieldValue]"
              [p-selected]="isSelectedItem(item) || item.selected"
              [p-active-tabs]="item.active"
              [p-separator]="item.separator || separator"
              [p-danger]="item.danger || item.type === 'danger'"
              [p-icon]="item.icon"
              [p-type]="type"
              [p-item]="item"
              [p-tab-hide]="item.hide"
              [p-is-tabs]="isTabs"
              [p-field-value]="fieldValue"
              [p-field-label]="fieldLabel"
              [p-template]="template"
              [p-template-context]="item"
              (p-selectcheckbox-item)="checkboxClicked($event)"
              (p-selectcombo-item)="optionClicked(item)"
              (p-emit-item-tabs)="onSelectTabs($event)"
              (p-activated-tabs)="onActivatedTabs($event)"
              [p-search-value]="searchValue"
              [p-filter-mode]="filterMode"
              [p-filtering]="isFiltering"
              [p-size]="size"
              [p-should-mark-letter]="shouldMarkLetters"
              [p-compare-cache]="compareCache"
              [p-combo-service]="comboService"
              [p-keys-label]="keysLabel"
            ></po-item-list>
          }
        </li>
      }

      @if (footerActionListbox) {
        @if (placeholderListbox) {
          <div class="po-item-list" aria-hidden="true">
            <span>{{ placeholderListbox }}</span>
          </div>
        }

        @if (!placeholderListbox && !items.length) {
          <div class="po-item-list" aria-hidden="true">
            <span>{{ literals.noItems }}</span>
          </div>
        }

        <li
          class="po-listbox-item"
          (click)="handleFooterActionListbox()"
          (keydown)="onKeyDown({ type: 'footerAction' }, $event)"
          cdkOption="footerActionListbox"
        >
          <po-item-list [p-label]="literals.footerActionListbox" p-separator [p-size]="size" />
        </li>
      }
    </ul>
  } @else {
    @if (!items.length && !isServerSearching && type !== 'action' && !footerActionListbox) {
      <div class="po-listbox-container-no-data po-text-center">
        <span> {{ literals.noItems }}</span>
      </div>
    }
  }

  @if (listboxSubitems) {
    <ul cdkListbox class="po-listbox-list po-listbox-dropdown">
      @if (!currentGroup) {
        @for (item of currentItems; track item) {
          <li
            #listboxItem
            [class.po-listbox-item]="visible()"
            [cdkOption]="item[fieldLabel]"
            [cdkOptionDisabled]="returnBooleanValue(item, 'disabled') || returnBooleanValue(item, 'visible') === false"
            [attr.aria-selected]="isSelectedItem(item) || item.selected"
            (click)="onSelectItem(item, $event)"
            (keydown)="onKeyDown(item, $event)"
          >
            @if (item.subItems?.length || item.$subItemTemplate) {
              <po-item-list
                [p-label]="item.label"
                [p-item]="item"
                [p-separator]="item.separator || separator"
                [p-icon]="item.icon"
                [p-icon-arrow-right]="'ICON_ARROW_RIGHT'"
                [p-selected]="isSelectedItem(item) || item.selected"
              >
              </po-item-list>
            } @else if (!item.subItems?.length && returnBooleanValue(item, 'visible') !== false) {
              <po-item-list
                class="po-listbox-item-sub"
                [p-disabled]="returnBooleanValue(item, 'disabled')"
                [p-visible]="returnBooleanValue(item, 'visible')"
                [p-label]="item[fieldLabel]"
                [p-item]="item"
                [p-icon]="item.icon"
                [p-separator]="item.separator || separator"
                [p-danger]="item.type === 'danger'"
                [p-selected]="isSelectedItem(item) || item.selected"
              >
              </po-item-list>
            }
          </li>
        }
      }

      @if (currentGroup) {
        <li
          #listboxGroupHeader
          class="po-listbox-group-header"
          [cdkOption]="'back-option'"
          [attr.aria-label]="literals?.backToPreviousGroup"
          (click)="goBack($event)"
          (keydown)="onKeydownGoBack($event, currentGroup)"
        >
          <po-icon class="po-field-icon" [p-icon]="'ICON_ARROW_LEFT'"></po-icon>
          <po-tag [p-value]="currentGroup.label"></po-tag>
        </li>

        @if (currentGroup.$subItemTemplate) {
          <div
            (click)="$event.stopPropagation()"
            (keydown)="onKeydownTemplate($event)"
            (keydown.space)="$event.stopPropagation()"
          >
            <ng-container
              *ngTemplateOutlet="currentGroup.$subItemTemplate; context: { $implicit: currentGroup }"
            ></ng-container>
          </div>
        } @else {
          @for (subItem of currentItems; track subItem.label) {
            @if (subItem.subItems?.length) {
              <li
                [cdkOption]="subItem[fieldLabel]"
                (click)="onSelectItem(subItem, $event)"
                (keydown)="onKeyDown(subItem, $event)"
              >
                <po-item-list
                  [p-label]="subItem.label"
                  [p-item]="subItem"
                  [p-separator]="subItem.separator || separator"
                  [p-icon]="'ICON_ARROW_RIGHT'"
                  [p-icon-position]="'right'"
                >
                </po-item-list>
              </li>
            } @else if (!subItem.subItems?.length && returnBooleanValue(subItem, 'visible') !== false) {
              <li
                [cdkOption]="subItem[fieldLabel]"
                (click)="onSelectItem(subItem, $event)"
                (keydown)="onKeyDown(subItem, $event)"
              >
                <po-item-list
                  class="po-listbox-item-sub"
                  [p-disabled]="returnBooleanValue(subItem, 'disabled')"
                  [p-visible]="returnBooleanValue(subItem, 'visible')"
                  [p-label]="subItem[fieldLabel]"
                  [p-item]="subItem"
                  [p-icon]="subItem.icon"
                  [p-separator]="subItem.separator || separator"
                  [p-danger]="subItem.type === 'danger'"
                  [p-selected]="isSelectedItem(subItem) || subItem.selected"
                >
                </po-item-list>
              </li>
            }
          }
        }
      }
    </ul>
  }

  @if (isServerSearching && type !== 'action') {
    <div [class.po-listbox-container-loading-default]="!infiniteLoading">
      <po-loading-overlay [p-size]="getSizeLoading()" [p-text]="getTextLoading()"></po-loading-overlay>
    </div>
  }
</div>
`
    }]
  }], () => [], {
    listbox: [{
      type: ViewChild,
      args: ["listbox", {
        static: true
      }]
    }],
    listboxItemList: [{
      type: ViewChild,
      args: ["listboxItemList", {
        static: false
      }]
    }],
    listboxGroupHeader: [{
      type: ViewChild,
      args: ["listboxGroupHeader"]
    }],
    searchElement: [{
      type: ViewChild,
      args: ["searchElement"]
    }],
    popupHeaderContainer: [{
      type: ViewChild,
      args: ["popupHeaderContainer"]
    }],
    listboxItems: [{
      type: ViewChildren,
      args: ["listboxItem"]
    }],
    cdkListbox: [{
      type: ViewChild,
      args: ["listboxItemList", {
        read: CdkListbox
      }]
    }],
    cdkOptions: [{
      type: ViewChildren,
      args: [CdkOption]
    }]
  });
})();
var PoPopupComponent = class _PoPopupComponent extends PoPopupBaseComponent {
  id = `po-popup[${uuid()}]`;
  renderer = inject(Renderer2);
  router = inject(Router);
  poControlPosition = inject(PoControlPositionService);
  changeDetector = inject(ChangeDetectorRef);
  popupRef;
  listbox;
  //utilizado apenas no theme builder
  poListBoxRef;
  ngAfterViewInit() {
    if (this.templateIcon && this.target) {
      this.target = this.target?.iconElement?.nativeElement;
    }
  }
  /**
   * Fecha o componente *popup*.
   *
   * > Por padrão, este comportamento é acionado somente ao clicar fora do componente ou em determinada ação / url.
   */
  close() {
    this.removeListeners();
    this.showPopup = false;
    this.closeEvent.emit();
  }
  onActionClick(popupAction) {
    const actionNoDisabled = popupAction && !this.returnBooleanValue(popupAction, "disabled");
    if (popupAction?.action && actionNoDisabled) {
      this.close();
      popupAction.action(this.param || popupAction);
    }
    if (popupAction?.url && actionNoDisabled) {
      this.close();
      return this.openUrl(popupAction.url);
    }
  }
  /**
   * Abre o componente *popup*.
   *
   * > É possível informar um parâmetro que será utilizado na execução da ação do item e na função de desabilitar.
   */
  open(param) {
    this.oldTarget = this.target;
    this.param = param;
    this.showPopup = true;
    this.changeDetector.detectChanges();
    this.validateInitialContent();
    this.openEvent.emit();
  }
  returnBooleanValue(popupAction, property) {
    return PoUtils.isTypeof(popupAction[property], "function") ? popupAction[property](this.param || popupAction) : popupAction[property];
  }
  /**
   * Responsável por abrir e fechar o *popup*.
   *
   * Quando disparado abrirá o *popup* e caso o mesmo já estiver aberto e possuir o mesmo `target` irá fecha-lo.
   *
   * É possível informar um parâmetro que será utilizado na execução da ação do item e na função de desabilitar.
   */
  toggle(param) {
    this.showPopup && this.oldTarget === this.target ? this.close() : this.open(param);
  }
  onClickItem(item) {
    if (!item.goBack) {
      this.clickItem.emit(item);
    }
    if (item.subItems || item.$subItemTemplate || item.goBack) {
      this.changeDetector.detectChanges();
      this.validateInitialContent();
    }
  }
  checkAllActionIsInvisible() {
    if (this.actions.every((item) => item.visible === false)) {
      return true;
    }
    return false;
  }
  clickedOutDisabledItem(event) {
    const containsItemDisabled = this.elementContains(event.target, "po-popup-item-disabled") || this.elementContains(event.target.parentElement, "po-popup-item-disabled");
    return !containsItemDisabled;
  }
  clickedOutHeaderTemplate(event) {
    const popupHeaderTemplate = this.popupRef && this.popupRef.nativeElement.querySelector("[p-popup-header-template]");
    return !(popupHeaderTemplate && popupHeaderTemplate.contains(event.target));
  }
  clickedOutTarget(event) {
    return this.target && !this.target.contains(event.target);
  }
  closePopupOnClickout(event) {
    if (this.clickedOutTarget(event) && this.clickedOutDisabledItem(event) && this.clickedOutHeaderTemplate(event)) {
      this.close();
    }
  }
  elementContains(element, className) {
    return element && element.classList.contains(className);
  }
  hasContentToShow() {
    return !!(this.popupRef?.nativeElement && this.listbox?.nativeElement);
  }
  initializeListeners() {
    this.resizeListener = this.renderer.listen("window", "resize", () => {
      this.close();
    });
    this.clickoutListener = this.renderer.listen("document", "click", (event) => {
      this.closePopupOnClickout(event);
    });
    window.addEventListener("scroll", this.onScroll, true);
  }
  onScroll = ({
    target
  }) => {
    const {
      showPopup,
      popupRef
    } = this;
    if (showPopup && popupRef?.nativeElement && target instanceof Node && !popupRef.nativeElement.contains(target)) {
      this.close();
    }
  };
  openUrl(url) {
    if (isExternalLink(url)) {
      return PoUtils.openExternalLink(url);
    }
    if (url) {
      return this.router.navigate([url]);
    }
  }
  removeListeners() {
    if (this.clickoutListener) {
      this.clickoutListener();
    }
    if (this.resizeListener) {
      this.resizeListener();
    }
    window.removeEventListener("scroll", this.onScroll, true);
  }
  setPosition() {
    if (this.listbox.nativeElement.querySelector(".po-listbox")) {
      this.poControlPosition.setElements(this.popupRef.nativeElement, 8, this.target, this.customPositions, false, this.isCornerAlign);
      this.poControlPosition.adjustPosition(this.position);
      this.arrowDirection = this.poControlPosition.getArrowDirection();
    }
  }
  validateInitialContent() {
    if (this.hasContentToShow()) {
      this.setPosition();
      this.initializeListeners();
    } else {
      this.close();
    }
  }
  static ɵfac = /* @__PURE__ */ (() => {
    let ɵPoPopupComponent_BaseFactory;
    return function PoPopupComponent_Factory(__ngFactoryType__) {
      return (ɵPoPopupComponent_BaseFactory || (ɵPoPopupComponent_BaseFactory = ɵɵgetInheritedFactory(_PoPopupComponent)))(__ngFactoryType__ || _PoPopupComponent);
    };
  })();
  static ɵcmp = ɵɵdefineComponent({
    type: _PoPopupComponent,
    selectors: [["po-popup"]],
    viewQuery: function PoPopupComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c31, 5, ElementRef)(_c23, 5, ElementRef)(_c32, 5);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.popupRef = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.listbox = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.poListBoxRef = _t.first);
      }
    },
    standalone: false,
    features: [ɵɵProvidersFeature([PoControlPositionService]), ɵɵInheritDefinitionFeature],
    ngContentSelectors: _c30,
    decls: 1,
    vars: 1,
    consts: [["popupRef", ""], ["listbox", "", "poListBoxRef", ""], [1, "po-popup"], [3, "class"], [1, "po-popup-container"], ["p-type", "action", 3, "p-close", "p-click-item", "p-items", "p-param", "p-size", "p-listbox-subitems"]],
    template: function PoPopupComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵprojectionDef(_c29);
        ɵɵconditionalCreate(0, PoPopupComponent_Conditional_0_Template, 8, 5, "div", 2);
      }
      if (rf & 2) {
        ɵɵconditional(ctx.showPopup && ctx.actions.length && !ctx.checkAllActionIsInvisible() ? 0 : -1);
      }
    },
    dependencies: [PoListBoxComponent],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoPopupComponent, [{
    type: Component,
    args: [{
      selector: "po-popup",
      providers: [PoControlPositionService],
      standalone: false,
      template: '@if (showPopup && actions.length && !checkAllActionIsInvisible()) {\n  <div #popupRef class="po-popup">\n    @if (!hideArrow) {\n      <div class="po-popup-arrow po-arrow-{{ arrowDirection }}"></div>\n    }\n    <div class="po-popup-container">\n      <po-listbox\n        #listbox\n        #poListBoxRef\n        p-type="action"\n        [p-items]="actions"\n        [p-param]="param"\n        [p-size]="size"\n        [p-listbox-subitems]="computedListboxSubitems"\n        (p-close)="close()"\n        (p-click-item)="onClickItem($event)"\n      >\n        <ng-content p-popup-header-template select="[p-popup-header-template]"></ng-content>\n      </po-listbox>\n    </div>\n  </div>\n}\n'
    }]
  }], null, {
    popupRef: [{
      type: ViewChild,
      args: ["popupRef", {
        read: ElementRef
      }]
    }],
    listbox: [{
      type: ViewChild,
      args: ["listbox", {
        read: ElementRef
      }]
    }],
    poListBoxRef: [{
      type: ViewChild,
      args: ["poListBoxRef"]
    }]
  });
})();
var PoBreadcrumbFavoriteService = class _PoBreadcrumbFavoriteService {
  http = inject(HttpClient);
  _url;
  _bodyParams;
  get url() {
    return this._url;
  }
  get bodyParams() {
    return this._bodyParams;
  }
  configService(url, params, item) {
    this._url = url;
    this._bodyParams = {
      isFavorite: false,
      url: item.link,
      params
    };
  }
  sendStatusFavorite(status) {
    this.bodyParams["isFavorite"] = status;
    return this.http.post(this.url, this.bodyParams);
  }
  getFavorite() {
    return this.http.get(this.url, {
      responseType: "json",
      params: {
        url: this.bodyParams["url"]
      }
    });
  }
  static ɵfac = function PoBreadcrumbFavoriteService_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoBreadcrumbFavoriteService)();
  };
  static ɵprov = ɵɵdefineInjectable({
    token: _PoBreadcrumbFavoriteService,
    factory: _PoBreadcrumbFavoriteService.ɵfac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoBreadcrumbFavoriteService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
var PoBreadcrumbLiterals = {
  en: {
    favorite: "Favorite",
    unfavorite: "Unfavorite"
  },
  es: {
    favorite: "Favor",
    unfavorite: "Desfavorecer"
  },
  pt: {
    favorite: "Favoritar",
    unfavorite: "Desfavoritar"
  },
  ru: {
    favorite: "Любимый",
    unfavorite: "Немилость"
  }
};
var PoBreadcrumbFavoriteComponent = class _PoBreadcrumbFavoriteComponent {
  service = inject(PoBreadcrumbFavoriteService);
  languageService = inject(PoLanguageService);
  // URL do serviço.
  favoriteService;
  // Item do breadcrumb ativo.
  itemActive;
  // Parâmetro que será enviado junto com o serviço de favoritar.
  paramsService;
  // Esconde literal e mantém apenas icone
  hiddenLiteral = false;
  favorite = false;
  literals;
  getSubscription;
  setSubscription;
  constructor() {
    const languageService = this.languageService;
    const language = languageService.getShortLanguage();
    this.literals = __spreadValues({}, PoBreadcrumbLiterals[language]);
  }
  ngOnInit() {
    this.service.configService(this.favoriteService, this.paramsService, this.itemActive);
    this.getStatusFavorite();
  }
  ngOnDestroy() {
    if (this.getSubscription) {
      this.getSubscription.unsubscribe();
    }
    if (this.setSubscription) {
      this.setSubscription.unsubscribe();
    }
  }
  toggleFavoriteAction() {
    this.favorite ? this.setStatusFavorite(false) : this.setStatusFavorite(true);
  }
  getStatusFavorite() {
    this.getSubscription = this.service.getFavorite().subscribe((result) => this.favorite = result.isFavorite);
  }
  setStatusFavorite(status) {
    this.setSubscription = this.service.sendStatusFavorite(status).subscribe((result) => this.favorite = result.isFavorite);
  }
  static ɵfac = function PoBreadcrumbFavoriteComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoBreadcrumbFavoriteComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoBreadcrumbFavoriteComponent,
    selectors: [["po-breadcrumb-favorite"]],
    inputs: {
      favoriteService: [0, "p-favorite-service", "favoriteService"],
      itemActive: [0, "p-item-active", "itemActive"],
      paramsService: [0, "p-params-service", "paramsService"],
      hiddenLiteral: [0, "p-hidden-literal", "hiddenLiteral"]
    },
    standalone: false,
    features: [ɵɵProvidersFeature([PoBreadcrumbFavoriteService])],
    decls: 4,
    vars: 4,
    consts: [["tabindex", "0", "role", "button", 1, "po-breadcrumb-favorite", "po-clickable", 3, "click", "keyup.enter"], ["p-icon", "ICON_STAR", 1, "po-breadcrumb-favorite-star", "po-clickable"], [1, "po-hidden-sm", "po-breadcrumb-favorite-label"]],
    template: function PoBreadcrumbFavoriteComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "div", 0);
        ɵɵlistener("click", function PoBreadcrumbFavoriteComponent_Template_div_click_0_listener() {
          return ctx.toggleFavoriteAction();
        })("keyup.enter", function PoBreadcrumbFavoriteComponent_Template_div_keyup_enter_0_listener() {
          return ctx.toggleFavoriteAction();
        });
        ɵɵelement(1, "po-icon", 1);
        ɵɵconditionalCreate(2, PoBreadcrumbFavoriteComponent_Conditional_2_Template, 2, 1, "span", 2);
        ɵɵconditionalCreate(3, PoBreadcrumbFavoriteComponent_Conditional_3_Template, 2, 1, "span", 2);
        ɵɵelementEnd();
      }
      if (rf & 2) {
        ɵɵadvance();
        ɵɵclassProp("po-breadcrumb-favorite-star-active", ctx.favorite);
        ɵɵadvance();
        ɵɵconditional(ctx.favorite && !ctx.hiddenLiteral ? 2 : -1);
        ɵɵadvance();
        ɵɵconditional(!ctx.favorite && !ctx.hiddenLiteral ? 3 : -1);
      }
    },
    dependencies: [PoIconComponent],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoBreadcrumbFavoriteComponent, [{
    type: Component,
    args: [{
      selector: "po-breadcrumb-favorite",
      providers: [PoBreadcrumbFavoriteService],
      standalone: false,
      template: '<div\n  tabindex="0"\n  role="button"\n  class="po-breadcrumb-favorite po-clickable"\n  (click)="toggleFavoriteAction()"\n  (keyup.enter)="toggleFavoriteAction()"\n>\n  <po-icon\n    p-icon="ICON_STAR"\n    class="po-breadcrumb-favorite-star po-clickable"\n    [class.po-breadcrumb-favorite-star-active]="favorite"\n  >\n  </po-icon>\n  @if (favorite && !hiddenLiteral) {\n    <span class="po-hidden-sm po-breadcrumb-favorite-label">{{ literals?.unfavorite }}</span>\n  }\n  @if (!favorite && !hiddenLiteral) {\n    <span class="po-hidden-sm po-breadcrumb-favorite-label">{{ literals?.favorite }}</span>\n  }\n</div>\n'
    }]
  }], () => [], {
    favoriteService: [{
      type: Input,
      args: ["p-favorite-service"]
    }],
    itemActive: [{
      type: Input,
      args: ["p-item-active"]
    }],
    paramsService: [{
      type: Input,
      args: ["p-params-service"]
    }],
    hiddenLiteral: [{
      type: Input,
      args: ["p-hidden-literal"]
    }]
  });
})();
var poBreadcrumbLiterals = {
  en: {
    literalButtonPopup: "Menu pop up collapsed"
  },
  es: {
    literalButtonPopup: "Menú pop up colapsado"
  },
  pt: {
    literalButtonPopup: "Menu pop up colapsado"
  },
  ru: {
    literalButtonPopup: "меню свернуто"
  }
};
var PoBreadcrumbComponent = class _PoBreadcrumbComponent extends PoBreadcrumbBaseComponent {
  element = inject(ElementRef);
  renderer = inject(Renderer2);
  languageService = inject(PoLanguageService);
  breadcrumbElement;
  dropdownIcon;
  svgTarget;
  popupContainer;
  showDropdown = false;
  showDropdownToggle = false;
  dropdownItems;
  literals;
  hiddenLiteralFavorite = false;
  _breadcrumbItemsLenght = 0;
  calculatedElement = false;
  differ;
  hiddenWithoutResize = false;
  initialized = false;
  timeoutResize;
  constructor() {
    const differs = inject(IterableDiffers);
    super();
    const languageService = this.languageService;
    this.differ = differs.find([]).create(null);
    const language = languageService.getShortLanguage();
    this.literals = __spreadValues({}, poBreadcrumbLiterals[language]);
  }
  ngAfterViewInit() {
    this.initialized = true;
    this.initializeResizeListener();
  }
  ngDoCheck() {
    const breadcrumbWidth = this.breadcrumbElement.nativeElement.offsetWidth;
    if (breadcrumbWidth && !this.calculatedElement && this.initialized) {
      this.initBreadcrumbSize();
    }
    if (this.hiddenWithoutResize) {
      this.debounceResize();
      this.hiddenWithoutResize = false;
    }
    this.checkChangeOnItems();
  }
  ngOnDestroy() {
    this.removeResizeListener();
  }
  emitAction(item) {
    if (item.action) {
      item.action();
    }
  }
  openPopup(event) {
    if (event.code === "Enter" || event.code === "Space") {
      this.popupContainer.open();
    }
  }
  closePopUp() {
    this.svgTarget.nativeElement.focus();
  }
  checkChangeOnItems() {
    if (this.differ) {
      const changes = this.differ.diff(this.items);
      if (changes) {
        this.calcBreadcrumbItemsWidth();
        this.calculatedElement = false;
      }
    }
  }
  calcBreadcrumb() {
    const breadcrumbFavorite = this.getBreadcrumbFavoriteWidth();
    const existLabel = this.existsFavoritelabel();
    const breadcrumb = this.getBreadcrumbWidth(breadcrumbFavorite, existLabel);
    const breadcrumbTooltip = this.getBreadcrumbTooltipWidth();
    if (breadcrumb <= this._breadcrumbItemsLenght) {
      this.enableBreadcrumbResponsive();
    } else {
      this.disableBreadcrumbResponsive();
    }
    if (breadcrumbTooltip && breadcrumb <= breadcrumbTooltip) {
      this.hiddenLiteralFavorite = true;
    } else {
      this.hiddenLiteralFavorite = false;
    }
  }
  getBreadcrumbFavoriteWidth() {
    return this.favoriteService ? this.element.nativeElement.querySelector(".po-breadcrumb-favorite").offsetWidth + 20 : 0;
  }
  getBreadcrumbTooltipWidth() {
    return this.favoriteService ? this.element.nativeElement.querySelector(".po-breadcrumb-tooltip")?.offsetWidth : 0;
  }
  existsFavoritelabel() {
    return !!this.element.nativeElement.querySelector(".po-breadcrumb-favorite-label");
  }
  getBreadcrumbWidth(breadcrumbFavorite, existLabel) {
    const widthSpan = !existLabel ? 95 : 0;
    return this.element.nativeElement.querySelector(".po-breadcrumb").offsetWidth - (breadcrumbFavorite + widthSpan);
  }
  calcBreadcrumbItemsWidth() {
    const breadcrumbItem = this.element.nativeElement.querySelectorAll(".po-breadcrumb-item");
    this._breadcrumbItemsLenght = Array.from(breadcrumbItem).map((breadcrumb) => breadcrumb["offsetWidth"]).reduce((a, b) => a + b, 16);
  }
  enableBreadcrumbResponsive() {
    this.showDropdownToggle = true;
  }
  disableBreadcrumbResponsive() {
    this.showDropdownToggle = false;
    this.itemsView = [].concat(this.items);
    this.showDropdown = false;
  }
  debounceResize() {
    clearTimeout(this.timeoutResize);
    this.timeoutResize = setTimeout(() => {
      if (this.calculatedElement && !this.hiddenWithoutResize && this.breadcrumbElement.nativeElement.offsetWidth === 0) {
        this.hiddenWithoutResize = true;
      } else {
        this.calcBreadcrumb();
      }
    }, 50);
  }
  initBreadcrumbSize() {
    this.calcBreadcrumbItemsWidth();
    this.calcBreadcrumb();
    this.calculatedElement = true;
  }
  initializeResizeListener() {
    this.resizeListener = this.renderer.listen("window", "resize", (event) => {
      this.debounceResize();
    });
  }
  removeResizeListener() {
    this.resizeListener();
  }
  static ɵfac = function PoBreadcrumbComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoBreadcrumbComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoBreadcrumbComponent,
    selectors: [["po-breadcrumb"]],
    viewQuery: function PoBreadcrumbComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c34, 7, ElementRef)(_c35, 5, ElementRef)(_c15, 5, ElementRef)(_c36, 5);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.breadcrumbElement = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.dropdownIcon = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.svgTarget = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.popupContainer = _t.first);
      }
    },
    standalone: false,
    features: [ɵɵInheritDefinitionFeature],
    decls: 6,
    vars: 3,
    consts: [["breadcrumb", ""], ["dropdownIcon", ""], ["target", ""], ["popup", ""], [1, "po-breadcrumb"], [1, "po-breadcrumb-items"], [1, "po-breadcrumb-item", "po-clickable", "po-breadcrumb-tooltip"], [1, "po-breadcrumb-item-container"], [3, "p-favorite-service", "p-item-active", "p-params-service", "p-hidden-literal"], [3, "p-action", "p-label", "p-url"], ["p-icon", "ICON_ARROW_RIGHT po-breadcrumb-icon-arrow"], ["role", "button", "tabindex", "0", 1, "po-breadcrumb-icon-more", 3, "click", "keyup"], ["p-icon", "ICON_MORE_VERT"], ["role", "link", "tabindex", "0", 1, "po-breadcrumb-item-activate"], ["p-position", "bottom-right", 3, "p-close", "p-hide-arrow", "p-actions", "p-size", "p-target"], [1, "po-breadcrumb-item"], [3, "p-label", "p-url"]],
    template: function PoBreadcrumbComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "nav", 4, 0)(2, "div", 5);
        ɵɵconditionalCreate(3, PoBreadcrumbComponent_Conditional_3_Template, 17, 11, "ul", 6);
        ɵɵconditionalCreate(4, PoBreadcrumbComponent_Conditional_4_Template, 3, 0, "ul", 7);
        ɵɵelementEnd();
        ɵɵconditionalCreate(5, PoBreadcrumbComponent_Conditional_5_Template, 1, 4, "po-breadcrumb-favorite", 8);
        ɵɵelementEnd();
      }
      if (rf & 2) {
        ɵɵadvance(3);
        ɵɵconditional(ctx.showDropdownToggle && ctx.itemsView.length >= 4 ? 3 : -1);
        ɵɵadvance();
        ɵɵconditional(!ctx.showDropdownToggle || ctx.itemsView.length < 4 ? 4 : -1);
        ɵɵadvance();
        ɵɵconditional(ctx.favoriteService ? 5 : -1);
      }
    },
    dependencies: [PoLinkComponent, PoPopupComponent, PoIconComponent, PoBreadcrumbFavoriteComponent],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoBreadcrumbComponent, [{
    type: Component,
    args: [{
      selector: "po-breadcrumb",
      standalone: false,
      template: '<nav class="po-breadcrumb" #breadcrumb>\n  <div class="po-breadcrumb-items">\n    @if (showDropdownToggle && itemsView.length >= 4) {\n      <ul #dropdownIcon class="po-breadcrumb-item po-clickable po-breadcrumb-tooltip">\n        <li>\n          <po-link\n            [p-label]="itemsView[0].label"\n            [p-url]="itemsView[0].link"\n            (p-action)="emitAction(itemsView[0])"\n          ></po-link>\n        </li>\n        <po-icon p-icon="ICON_ARROW_RIGHT po-breadcrumb-icon-arrow"></po-icon>\n        <li\n          #target\n          class="po-breadcrumb-icon-more"\n          [attr.aria-label]="literals.literalButtonPopup"\n          role="button"\n          tabindex="0"\n          (click)="popup.toggle()"\n          (keyup)="openPopup($event)"\n        >\n          <po-icon p-icon="ICON_MORE_VERT"></po-icon>\n        </li>\n        <po-icon p-icon="ICON_ARROW_RIGHT po-breadcrumb-icon-arrow"></po-icon>\n        <li>\n          <po-link\n            [p-label]="itemsView[itemsView.length - 2].label"\n            [p-url]="itemsView[itemsView.length - 2].link"\n            (p-action)="emitAction(itemsView[itemsView.length - 2])"\n          ></po-link>\n        </li>\n        <po-icon p-icon="ICON_ARROW_RIGHT po-breadcrumb-icon-arrow"></po-icon>\n        <li>\n          <span\n            class="po-breadcrumb-item-activate"\n            role="link"\n            tabindex="0"\n            [attr.aria-current]="itemsView[itemsView.length - 1].label"\n            >{{ itemsView[itemsView.length - 1].label }}</span\n          >\n        </li>\n        <po-popup\n          #popup\n          p-position="bottom-right"\n          (p-close)="closePopUp()"\n          [p-hide-arrow]="true"\n          [p-actions]="itemsViewPopup"\n          [p-size]="size"\n          [p-target]="target"\n        >\n        </po-popup>\n      </ul>\n    }\n\n    @if (!showDropdownToggle || itemsView.length < 4) {\n      <ul class="po-breadcrumb-item-container">\n        @for (item of itemsView; track item; let itemIndex = $index) {\n          <li class="po-breadcrumb-item">\n            @if (!(itemIndex === itemsView.length - 1)) {\n              <po-link [p-label]="item.label" [p-url]="item.link" (p-action)="emitAction(item)"></po-link>\n            }\n            @if (itemIndex === itemsView.length - 1) {\n              <span class="po-breadcrumb-item-activate" role="link" tabindex="0" [attr.aria-current]="item.label">{{\n                item.label\n              }}</span>\n            }\n            @if (!(itemIndex === itemsView.length - 1)) {\n              <po-icon p-icon="ICON_ARROW_RIGHT po-breadcrumb-icon-arrow"></po-icon>\n            }\n          </li>\n        }\n      </ul>\n    }\n  </div>\n\n  @if (favoriteService) {\n    <po-breadcrumb-favorite\n      [p-favorite-service]="favoriteService"\n      [p-item-active]="items[items.length - 1]"\n      [p-params-service]="paramsService"\n      [p-hidden-literal]="hiddenLiteralFavorite"\n    >\n    </po-breadcrumb-favorite>\n  }\n</nav>\n'
    }]
  }], () => [], {
    breadcrumbElement: [{
      type: ViewChild,
      args: ["breadcrumb", {
        read: ElementRef,
        static: true
      }]
    }],
    dropdownIcon: [{
      type: ViewChild,
      args: ["dropdownIcon", {
        read: ElementRef
      }]
    }],
    svgTarget: [{
      type: ViewChild,
      args: ["target", {
        read: ElementRef
      }]
    }],
    popupContainer: [{
      type: ViewChild,
      args: ["popup"]
    }]
  });
})();
var PoLabelModule = class _PoLabelModule {
  static ɵfac = function PoLabelModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoLabelModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoLabelModule,
    declarations: [PoLabelComponent],
    imports: [CommonModule, PoHelperModule],
    exports: [PoLabelComponent]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [CommonModule, PoHelperModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoLabelModule, [{
    type: NgModule,
    args: [{
      declarations: [PoLabelComponent],
      exports: [PoLabelComponent],
      imports: [CommonModule, PoHelperModule]
    }]
  }], null, null);
})();
var PoFieldContainerModule = class _PoFieldContainerModule {
  static ɵfac = function PoFieldContainerModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoFieldContainerModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoFieldContainerModule,
    declarations: [PoFieldContainerBottomComponent, PoFieldContainerComponent],
    imports: [CommonModule, PoLabelModule, PoIconModule, PoTooltipModule, PoHelperModule],
    exports: [PoFieldContainerBottomComponent, PoFieldContainerComponent]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [CommonModule, PoLabelModule, PoIconModule, PoTooltipModule, PoHelperModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoFieldContainerModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, PoLabelModule, PoIconModule, PoTooltipModule, PoHelperModule],
      exports: [PoFieldContainerBottomComponent, PoFieldContainerComponent],
      declarations: [PoFieldContainerBottomComponent, PoFieldContainerComponent]
    }]
  }], null, null);
})();
var PoCheckboxModule = class _PoCheckboxModule {
  static ɵfac = function PoCheckboxModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoCheckboxModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoCheckboxModule,
    declarations: [PoCheckboxComponent],
    imports: [CommonModule, FormsModule, PoLabelModule, PoFieldContainerModule, PoHelperModule, PoTooltipModule],
    exports: [PoCheckboxComponent]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [CommonModule, FormsModule, PoLabelModule, PoFieldContainerModule, PoHelperModule, PoTooltipModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoCheckboxModule, [{
    type: NgModule,
    args: [{
      declarations: [PoCheckboxComponent],
      exports: [PoCheckboxComponent],
      imports: [CommonModule, FormsModule, PoLabelModule, PoFieldContainerModule, PoHelperModule, PoTooltipModule]
    }]
  }], null, null);
})();
var PoListBoxModule = class _PoListBoxModule {
  static ɵfac = function PoListBoxModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoListBoxModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoListBoxModule,
    declarations: [PoListBoxComponent, PoItemListComponent, PoSearchListComponent],
    imports: [CommonModule, PoCheckboxModule, PoIconModule, PoLoadingModule, PoTagModule, CdkListboxModule],
    exports: [PoListBoxComponent]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [CommonModule, PoCheckboxModule, PoIconModule, PoLoadingModule, PoTagModule, CdkListboxModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoListBoxModule, [{
    type: NgModule,
    args: [{
      declarations: [PoListBoxComponent, PoItemListComponent, PoSearchListComponent],
      exports: [PoListBoxComponent],
      imports: [CommonModule, PoCheckboxModule, PoIconModule, PoLoadingModule, PoTagModule, CdkListboxModule]
    }]
  }], null, null);
})();
var PoPopupModule = class _PoPopupModule {
  static ɵfac = function PoPopupModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoPopupModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoPopupModule,
    declarations: [PoPopupComponent],
    imports: [CommonModule, PoListBoxModule],
    exports: [PoPopupComponent]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [CommonModule, PoListBoxModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoPopupModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, PoListBoxModule],
      declarations: [PoPopupComponent],
      exports: [PoPopupComponent],
      providers: [],
      schemas: []
    }]
  }], null, null);
})();
var PoBreadcrumbModule = class _PoBreadcrumbModule {
  static ɵfac = function PoBreadcrumbModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoBreadcrumbModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoBreadcrumbModule,
    declarations: [PoBreadcrumbComponent, PoBreadcrumbFavoriteComponent],
    imports: [CommonModule, RouterModule, PoLinkModule, PoPopupModule, PoIconModule],
    exports: [PoBreadcrumbComponent]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [CommonModule, RouterModule, PoLinkModule, PoPopupModule, PoIconModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoBreadcrumbModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, RouterModule, PoLinkModule, PoPopupModule, PoIconModule],
      declarations: [PoBreadcrumbComponent, PoBreadcrumbFavoriteComponent],
      exports: [PoBreadcrumbComponent]
    }]
  }], null, null);
})();
var PoButtonGroupToggle;
(function(PoButtonGroupToggle2) {
  PoButtonGroupToggle2["Multiple"] = "multiple";
  PoButtonGroupToggle2["None"] = "none";
  PoButtonGroupToggle2["Single"] = "single";
})(PoButtonGroupToggle || (PoButtonGroupToggle = {}));
var PO_TOGGLE_TYPE_DEFAULT = "none";
var PoButtonGroupBaseComponent = class _PoButtonGroupBaseComponent {
  /**
   * @description
   *
   * Lista de botões.
   */
  buttons = input([], __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "buttons"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-buttons"
  }));
  _size = void 0;
  _initialSize = void 0;
  _toggle = PO_TOGGLE_TYPE_DEFAULT;
  /**
   * @optional
   *
   * @description
   *
   * Define o modo de seleção dos botões no componente conforme valores especificados no enum `PoButtonGroupToggle`:
   *  - `multiple`: permite múltiplas seleções.
   *  - `none`: desativa a funcionalidade de seleção.
   *  - `single`: restringe a seleção a um único botão.
   *
   * @default `none`
   */
  set toggle(value) {
    this._toggle = Object.values(PoButtonGroupToggle).includes(value) ? value : PO_TOGGLE_TYPE_DEFAULT;
    this.checkSelecteds(this._toggle);
  }
  get toggle() {
    return this._toggle;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define o tamanho do componente:
   * - `small`: altura de 32px (disponível apenas para acessibilidade AA).
   * - `medium`: altura de 44px.
   *
   * > Caso a acessibilidade AA não esteja configurada, o tamanho `medium` será mantido.
   * Para mais detalhes, consulte a documentação do [po-theme](https://po-ui.io/documentation/po-theme).
   *
   * @default `medium`
   */
  set size(value) {
    this._initialSize = value;
    this.applySizeBasedOnA11y();
  }
  get size() {
    return this._size ?? getDefaultSizeFn(PoFieldSize);
  }
  onThemeChange() {
    this.applySizeBasedOnA11y();
  }
  applySizeBasedOnA11y() {
    const size = validateSizeFn(this._initialSize, PoFieldSize);
    this._size = size;
  }
  onButtonClick(buttonClicked, buttonIndex) {
    if (this.toggle === PoButtonGroupToggle.Single) {
      this.buttons().forEach((button, index) => button.selected = index === buttonIndex ? !buttonClicked.selected : false);
    } else if (this.toggle === PoButtonGroupToggle.Multiple) {
      buttonClicked.selected = !buttonClicked.selected;
    }
  }
  checkSelecteds(toggleMode) {
    if (toggleMode === PoButtonGroupToggle.None) {
      this.deselectAllButtons();
    } else if (toggleMode === PoButtonGroupToggle.Single) {
      const hasMoreOneSelected = this.buttons().filter((button) => button.selected).length > 1;
      if (hasMoreOneSelected) {
        this.deselectAllButtons();
      }
    }
  }
  deselectAllButtons() {
    this.buttons().forEach((button) => button.selected = false);
  }
  static ɵfac = function PoButtonGroupBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoButtonGroupBaseComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoButtonGroupBaseComponent,
    selectors: [["po-button-group-base"]],
    hostVars: 1,
    hostBindings: function PoButtonGroupBaseComponent_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("PoUiThemeChange", function PoButtonGroupBaseComponent_PoUiThemeChange_HostBindingHandler() {
          return ctx.onThemeChange();
        }, ɵɵresolveWindow);
      }
      if (rf & 2) {
        ɵɵattribute("p-size", ctx.size);
      }
    },
    inputs: {
      buttons: [1, "p-buttons", "buttons"],
      toggle: [0, "p-toggle", "toggle"],
      size: [0, "p-size", "size"]
    },
    standalone: false,
    decls: 0,
    vars: 0,
    template: function PoButtonGroupBaseComponent_Template(rf, ctx) {
    },
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoButtonGroupBaseComponent, [{
    type: Component,
    args: [{
      selector: "po-button-group-base",
      template: "",
      standalone: false
    }]
  }], null, {
    buttons: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-buttons",
        required: false
      }]
    }],
    toggle: [{
      type: Input,
      args: ["p-toggle"]
    }],
    size: [{
      type: Input,
      args: ["p-size"]
    }, {
      type: HostBinding,
      args: ["attr.p-size"]
    }],
    onThemeChange: [{
      type: HostListener,
      args: ["window:PoUiThemeChange"]
    }]
  });
})();
var PoButtonGroupComponent = class _PoButtonGroupComponent extends PoButtonGroupBaseComponent {
  static ɵfac = /* @__PURE__ */ (() => {
    let ɵPoButtonGroupComponent_BaseFactory;
    return function PoButtonGroupComponent_Factory(__ngFactoryType__) {
      return (ɵPoButtonGroupComponent_BaseFactory || (ɵPoButtonGroupComponent_BaseFactory = ɵɵgetInheritedFactory(_PoButtonGroupComponent)))(__ngFactoryType__ || _PoButtonGroupComponent);
    };
  })();
  static ɵcmp = ɵɵdefineComponent({
    type: _PoButtonGroupComponent,
    selectors: [["po-button-group"]],
    standalone: false,
    features: [ɵɵInheritDefinitionFeature],
    decls: 3,
    vars: 0,
    consts: [[1, "po-button-group-container"], ["p-tooltip-position", "bottom", 1, "po-sm-12", "po-button-group", 3, "po-button-group-button-selected", "po-button-group-disabled", "p-disabled", "p-icon", "p-label", "p-size", "p-tooltip", "p-append-in-body"], ["p-tooltip-position", "bottom", 1, "po-sm-12", "po-button-group", 3, "p-click", "p-disabled", "p-icon", "p-label", "p-size", "p-tooltip", "p-append-in-body"]],
    template: function PoButtonGroupComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "div", 0);
        ɵɵrepeaterCreate(1, PoButtonGroupComponent_For_2_Template, 1, 10, "po-button", 1, ɵɵrepeaterTrackByIdentity);
        ɵɵelementEnd();
      }
      if (rf & 2) {
        ɵɵadvance();
        ɵɵrepeater(ctx.buttons());
      }
    },
    dependencies: [PoButtonComponent, PoTooltipDirective],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoButtonGroupComponent, [{
    type: Component,
    args: [{
      selector: "po-button-group",
      standalone: false,
      template: '<div class="po-button-group-container">\n  @for (button of buttons(); track button; let i = $index) {\n    <po-button\n      class="po-sm-12 po-button-group"\n      [class.po-button-group-button-selected]="button.selected"\n      [class.po-button-group-disabled]="button.disabled"\n      [p-disabled]="button.disabled"\n      [p-icon]="button.icon"\n      [p-label]="button.label"\n      [p-size]="size"\n      [p-tooltip]="!button.disabled ? button.tooltip : undefined"\n      p-tooltip-position="bottom"\n      [p-append-in-body]="true"\n      (p-click)="onButtonClick(button, i); button.action(button)"\n    >\n    </po-button>\n  }\n</div>\n'
    }]
  }], null, null);
})();
var PoButtonGroupModule = class _PoButtonGroupModule {
  static ɵfac = function PoButtonGroupModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoButtonGroupModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoButtonGroupModule,
    declarations: [PoButtonGroupComponent],
    imports: [CommonModule, PoButtonModule, PoTooltipModule],
    exports: [PoButtonGroupComponent]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [CommonModule, PoButtonModule, PoTooltipModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoButtonGroupModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, PoButtonModule, PoTooltipModule],
      declarations: [PoButtonGroupComponent],
      exports: [PoButtonGroupComponent]
    }]
  }], null, null);
})();
var INFINITY_SCROLL_MIN_REPEAT = 3;
var INFINITY_SCROLL_MIN_ITEMS = 30;
var PoTimerScrollHelper = class {
  /**
   * Normaliza o offset para o intervalo [sectionHeight, 2 * sectionHeight).
   *
   * Matematica:
   *   mod = ((offset - sH) % sH + sH) % sH  →  resultado em [0, sH)
   *   retorno = mod + sH                     →  resultado em [sH, 2*sH)
   *
   * Funciona para qualquer valor de offset (positivo, negativo, multiplos).
   */
  static wrapOffset(offset, sectionHeight) {
    if (sectionHeight <= 0) {
      return offset;
    }
    const mod = ((offset - sectionHeight) % sectionHeight + sectionHeight) % sectionHeight;
    return mod + sectionHeight;
  }
  /**
   * Calcula o passo (em px) por item, incluindo o gap entre itens.
   *
   * Para N itens num flex-column com altura H, gap G e padding P:
   *   scrollHeight = N*H + (N-1)*G + 2*P
   *   passo = H + G = (scrollHeight - 2*P + G) / N
   */
  static getCellStep(itemsEl, displayCount) {
    if (!itemsEl || displayCount === 0) {
      return 40;
    }
    const style2 = getComputedStyle(itemsEl);
    const gap = parseFloat(style2.rowGap) || 0;
    const paddingTop = parseFloat(style2.paddingTop) || 0;
    const paddingBottom = parseFloat(style2.paddingBottom) || 0;
    return (itemsEl.scrollHeight - paddingTop - paddingBottom + gap) / displayCount;
  }
  /**
   * Calcula o indice no displayArray do item visivel no topo a partir do offset.
   * Usa Math.round para snap ao item mais proximo.
   */
  static computeTopDisplayIndex(offset, step, displayLength) {
    if (step <= 0 || displayLength === 0) {
      return 0;
    }
    const raw = Math.round(offset / step);
    return (raw % displayLength + displayLength) % displayLength;
  }
  /**
   * Repete o array fonte o numero necessario de vezes para manter
   * o infinity scroll com pelo menos INFINITY_SCROLL_MIN_ITEMS itens.
   *
   * Quando o array fonte tem menos de 6 itens, retorna uma copia simples
   * (sem repeticao), pois o infinity scroll nao e utilizado.
   */
  static repeatArray(source) {
    if (!source || source.length === 0) {
      return [];
    }
    if (source.length < 6) {
      return [...source];
    }
    const repeats = Math.max(INFINITY_SCROLL_MIN_REPEAT, Math.ceil(INFINITY_SCROLL_MIN_ITEMS / source.length));
    const result = [];
    for (let i2 = 0; i2 < repeats; i2++) {
      result.push(...source);
    }
    return result;
  }
};
var PoTimerFormat;
(function(PoTimerFormat2) {
  PoTimerFormat2["Format24"] = "24";
  PoTimerFormat2["Format12"] = "12";
})(PoTimerFormat || (PoTimerFormat = {}));
var poTimerLiterals = {
  en: {
    hours: "Hours",
    minutes: "Minutes",
    seconds: "Seconds"
  },
  es: {
    hours: "Horas",
    minutes: "Minutos",
    seconds: "Segundos"
  },
  pt: {
    hours: "Horas",
    minutes: "Minutos",
    seconds: "Segundos"
  },
  ru: {
    hours: "Часы",
    minutes: "Минуты",
    seconds: "Секунды"
  }
};
var PO_TIMER_DEFAULT_MINUTE_INTERVAL = 5;
var PO_TIMER_DEFAULT_SECOND_INTERVAL = 1;
var PoTimerBaseComponent = class _PoTimerBaseComponent {
  languageService;
  _format = PoTimerFormat.Format24;
  _locale;
  _maxTime;
  _minTime;
  _minuteInterval = PO_TIMER_DEFAULT_MINUTE_INTERVAL;
  _secondInterval = PO_TIMER_DEFAULT_SECOND_INTERVAL;
  _showSeconds = false;
  _size;
  _initialSize;
  shortLanguage;
  onChangePropagate = null;
  onTouched = null;
  /**
   * @optional
   *
   * @description
   *
   * Evento disparado ao selecionar um horário.
   * Retorna uma `string` no formato ISO 8601 (`HH:mm` ou `HH:mm:ss`).
   */
  change = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Define um valor inicial para o componente no formato ISO 8601 (`HH:mm` ou `HH:mm:ss`).
   */
  set value(value) {
    this.writeValue(value);
  }
  get value() {
    return this.buildTimeValue();
  }
  /**
   * @optional
   *
   * @description
   *
   * Define o formato de exibição do timer.
   *
   * Valores válidos:
   * - `24`: formato de 24 horas (padrão)
   * - `12`: formato de 12 horas com indicador AM/PM
   *
   * @default `24`
   */
  set format(value) {
    this._format = Object.values(PoTimerFormat).includes(value) ? value : PoTimerFormat.Format24;
    this.generateHours();
  }
  get format() {
    return this._format;
  }
  /**
   * @optional
   *
   * @description
   *
   * Idioma do componente.
   *
   * > O locale padrão será recuperado com base no [`PoI18nService`](/documentation/po-i18n) ou *browser*.
   */
  set locale(locale) {
    this._locale = poLocales.includes(locale) ? locale : this.shortLanguage;
    this.setLiterals();
  }
  get locale() {
    return this._locale;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define o horário máximo permitido para seleção.
   * Horários posteriores ao limite ficam desabilitados.
   *
   * Formato aceito: `HH:mm` ou `HH:mm:ss`.
   */
  set maxTime(value) {
    this._maxTime = this.isValidTimeString(value) ? value : void 0;
  }
  get maxTime() {
    return this._maxTime;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define o horário mínimo permitido para seleção.
   * Horários anteriores ao limite ficam desabilitados.
   *
   * Formato aceito: `HH:mm` ou `HH:mm:ss`.
   */
  set minTime(value) {
    this._minTime = this.isValidTimeString(value) ? value : void 0;
  }
  get minTime() {
    return this._minTime;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define o intervalo entre os minutos exibidos no painel.
   *
   * @default `5`
   */
  set minuteInterval(value) {
    const parsed = parseInt(value, 10);
    this._minuteInterval = parsed > 0 && parsed < 60 ? parsed : PO_TIMER_DEFAULT_MINUTE_INTERVAL;
    this.generateMinutes();
  }
  get minuteInterval() {
    return this._minuteInterval;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define o intervalo entre os segundos exibidos no painel.
   * Utilizado apenas quando `p-show-seconds` está ativo.
   *
   * @default `1`
   */
  set secondInterval(value) {
    const parsed = parseInt(value, 10);
    this._secondInterval = parsed > 0 && parsed < 60 ? parsed : PO_TIMER_DEFAULT_SECOND_INTERVAL;
    this.generateSeconds();
  }
  get secondInterval() {
    return this._secondInterval;
  }
  /**
   * @optional
   *
   * @description
   *
   * Exibe a coluna de segundos no painel de seleção.
   *
   * @default `false`
   */
  set showSeconds(value) {
    this._showSeconds = value === true || value === "true" || value === "";
    this.generateSeconds();
  }
  get showSeconds() {
    return this._showSeconds;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define o tamanho do componente.
   *
   * > Caso a acessibilidade AA não esteja configurada, o tamanho `medium` será mantido.
   * Para mais detalhes, consulte a documentação do [po-theme](https://po-ui.io/documentation/po-theme).
   *
   * @default `medium`
   */
  set size(value) {
    this._initialSize = value;
    this.applySizeBasedOnA11y();
  }
  get size() {
    return this._size ?? getDefaultSizeFn(PoFieldSize);
  }
  literals;
  hours = [];
  minutes = [];
  seconds = [];
  selectedHour = null;
  selectedMinute = null;
  selectedSecond = null;
  period = "AM";
  constructor(languageService) {
    this.languageService = languageService;
    this.shortLanguage = languageService.getShortLanguage();
    this._locale = this.shortLanguage;
    this.setLiterals();
  }
  get is12HourFormat() {
    return this._format === PoTimerFormat.Format12;
  }
  onThemeChange() {
    this.applySizeBasedOnA11y();
  }
  /** Gera a lista de horas disponíveis de acordo com o formato. */
  generateHours() {
    this.hours = [];
    if (this.is12HourFormat) {
      for (let i2 = 1; i2 <= 12; i2++) {
        this.hours.push(i2);
      }
    } else {
      for (let i2 = 0; i2 <= 23; i2++) {
        this.hours.push(i2);
      }
    }
  }
  /** Gera a lista de minutos de acordo com o intervalo configurado. */
  generateMinutes() {
    this.minutes = [];
    for (let i2 = 0; i2 < 60; i2 += this._minuteInterval) {
      this.minutes.push(i2);
    }
  }
  /** Gera a lista de segundos de acordo com o intervalo configurado. */
  generateSeconds() {
    this.seconds = [];
    if (this._showSeconds) {
      for (let i2 = 0; i2 < 60; i2 += this._secondInterval) {
        this.seconds.push(i2);
      }
    }
  }
  /** Formata um número com dois dígitos. */
  formatValue(value) {
    if (value == null) {
      return "--";
    }
    return value < 10 ? `0${value}` : `${value}`;
  }
  /** Verifica se uma hora está desabilitada com base nos limites min/max. */
  isHourDisabled(hour) {
    if (!this._minTime && !this._maxTime) {
      return false;
    }
    const hour24 = this.convertTo24Hour(hour);
    if (this._minTime) {
      const minHour = this.parseTimeComponent(this._minTime, "hour");
      if (hour24 < minHour) {
        return true;
      }
    }
    if (this._maxTime) {
      const maxHour = this.parseTimeComponent(this._maxTime, "hour");
      if (hour24 > maxHour) {
        return true;
      }
    }
    return false;
  }
  /**
   * Verifica se um período (AM/PM) está completamente desabilitado.
   * Retorna `true` quando todas as horas do período estão fora do range min/max.
   */
  isPeriodDisabled(targetPeriod) {
    if (!this.is12HourFormat || !this._minTime && !this._maxTime) {
      return false;
    }
    const currentPeriod = this.period;
    this.period = targetPeriod;
    const allDisabled = this.hours.every((hour) => this.isHourDisabled(hour));
    this.period = currentPeriod;
    return allDisabled;
  }
  /** Verifica se um minuto está desabilitado com base nos limites min/max e hora selecionada. */
  isMinuteDisabled(minute) {
    if (!this._minTime && !this._maxTime) {
      return false;
    }
    if (this.selectedHour != null) {
      return !this.isMinuteAllowedForHour(this.selectedHour, minute);
    }
    if (!this.hours?.length) {
      return false;
    }
    return !this.hours.some((hour) => !this.isHourDisabled(hour) && this.isMinuteAllowedForHour(hour, minute));
  }
  /** Verifica se um segundo está desabilitado com base nos limites min/max, hora e minuto selecionados. */
  isSecondDisabled(second) {
    if (!this._minTime && !this._maxTime) {
      return false;
    }
    if (this.selectedHour != null && this.selectedMinute != null) {
      return !this.isSecondAllowed(this.selectedHour, this.selectedMinute, second);
    }
    if (this.selectedHour != null) {
      if (!this.minutes?.length) {
        return false;
      }
      return !this.minutes.some((minute) => this.isMinuteAllowedForHour(this.selectedHour, minute) && this.isSecondAllowed(this.selectedHour, minute, second));
    }
    if (!this.hours?.length) {
      return false;
    }
    return !this.hours.some((hour) => !this.isHourDisabled(hour) && this.minutes.some((minute) => this.isMinuteAllowedForHour(hour, minute) && this.isSecondAllowed(hour, minute, second)));
  }
  isMinuteAllowedForHour(hour, minute) {
    const hour24 = this.convertTo24Hour(hour);
    if (this._minTime) {
      const minHour = this.parseTimeComponent(this._minTime, "hour");
      const minMinute = this.parseTimeComponent(this._minTime, "minute");
      if (hour24 < minHour || hour24 === minHour && minute < minMinute) {
        return false;
      }
    }
    if (this._maxTime) {
      const maxHour = this.parseTimeComponent(this._maxTime, "hour");
      const maxMinute = this.parseTimeComponent(this._maxTime, "minute");
      if (hour24 > maxHour || hour24 === maxHour && minute > maxMinute) {
        return false;
      }
    }
    return true;
  }
  isSecondAllowed(hour, minute, second) {
    const hour24 = this.convertTo24Hour(hour);
    if (this._minTime) {
      const minHour = this.parseTimeComponent(this._minTime, "hour");
      const minMinute = this.parseTimeComponent(this._minTime, "minute");
      const minSecond = this.parseTimeComponent(this._minTime, "second");
      if (hour24 < minHour || hour24 === minHour && minute < minMinute || hour24 === minHour && minute === minMinute && second < minSecond) {
        return false;
      }
    }
    if (this._maxTime) {
      const maxHour = this.parseTimeComponent(this._maxTime, "hour");
      const maxMinute = this.parseTimeComponent(this._maxTime, "minute");
      const maxSecond = this.parseTimeComponent(this._maxTime, "second");
      if (hour24 > maxHour || hour24 === maxHour && minute > maxMinute || hour24 === maxHour && minute === maxMinute && second > maxSecond) {
        return false;
      }
    }
    return true;
  }
  /** Gera o valor ISO 8601 com base na seleção atual. */
  buildTimeValue() {
    if (this.selectedHour == null || this.selectedMinute == null) {
      return "";
    }
    if (this._showSeconds && this.selectedSecond == null) {
      return "";
    }
    const hour24 = this.convertTo24Hour(this.selectedHour);
    const hourStr = this.formatValue(hour24);
    const minuteStr = this.formatValue(this.selectedMinute);
    if (this._showSeconds && this.selectedSecond != null) {
      const secondStr = this.formatValue(this.selectedSecond);
      return `${hourStr}:${minuteStr}:${secondStr}`;
    }
    return `${hourStr}:${minuteStr}`;
  }
  /** Define o horário a partir de uma string ISO. */
  setTimeFromString(time) {
    if (!time) {
      this.selectedHour = null;
      this.selectedMinute = null;
      this.selectedSecond = null;
      return;
    }
    const parts = time.split(":");
    if (parts.length >= 2) {
      let hour = parseInt(parts[0], 10);
      const minute = parseInt(parts[1], 10);
      const second = parts.length >= 3 ? parseInt(parts[2], 10) : null;
      if (this.is12HourFormat) {
        const converted = this.convertTo12HourDisplay(hour);
        this.period = converted.period;
        hour = converted.hour;
      }
      this.selectedHour = hour;
      this.selectedMinute = minute;
      this.selectedSecond = this._showSeconds ? second ?? null : null;
    }
  }
  /** Converte hora 24h para formato de exibicao 12h com periodo. */
  convertTo12HourDisplay(hour) {
    if (hour === 0) {
      return {
        hour: 12,
        period: "AM"
      };
    }
    if (hour === 12) {
      return {
        hour: 12,
        period: "PM"
      };
    }
    if (hour > 12) {
      return {
        hour: hour - 12,
        period: "PM"
      };
    }
    return {
      hour,
      period: "AM"
    };
  }
  writeValue(value) {
    this.setTimeFromString(value);
  }
  registerOnChange(fn) {
    this.onChangePropagate = fn;
  }
  registerOnTouched(fn) {
    this.onTouched = fn;
  }
  setDisabledState(_isDisabled) {
  }
  emitChange() {
    const value = this.buildTimeValue();
    if (value) {
      this.updateModel(value);
      this.change.emit(value);
    }
  }
  callOnTouched() {
    if (this.onTouched) {
      this.onTouched();
    }
  }
  updateModel(value) {
    if (this.onChangePropagate) {
      this.onChangePropagate(value);
    }
  }
  /** Converte hora no formato atual para formato de 24 horas. */
  convertTo24Hour(hour) {
    if (!this.is12HourFormat) {
      return hour;
    }
    if (this.period === "AM") {
      return hour === 12 ? 0 : hour;
    } else {
      return hour === 12 ? 12 : hour + 12;
    }
  }
  /** Extrai componente do tempo (hora, minuto ou segundo) de uma string. */
  parseTimeComponent(time, component) {
    if (!time) {
      return 0;
    }
    const parts = time.split(":");
    switch (component) {
      case "hour":
        return parseInt(parts[0], 10) || 0;
      case "minute":
        return parts.length >= 2 ? parseInt(parts[1], 10) || 0 : 0;
      case "second":
        return parts.length >= 3 ? parseInt(parts[2], 10) || 0 : 0;
      default:
        return 0;
    }
  }
  applySizeBasedOnA11y() {
    this._size = validateSizeFn(this._initialSize, PoFieldSize);
  }
  isValidTimeString(value) {
    if (!value || typeof value !== "string") {
      return false;
    }
    return /^\d{2}:\d{2}(:\d{2})?$/.test(value);
  }
  setLiterals() {
    this.literals = poTimerLiterals[this._locale] || poTimerLiterals[this.shortLanguage] || poTimerLiterals["en"];
  }
  static ɵfac = function PoTimerBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoTimerBaseComponent)(ɵɵdirectiveInject(PoLanguageService));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoTimerBaseComponent,
    hostVars: 1,
    hostBindings: function PoTimerBaseComponent_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("PoUiThemeChange", function PoTimerBaseComponent_PoUiThemeChange_HostBindingHandler() {
          return ctx.onThemeChange();
        }, ɵɵresolveWindow);
      }
      if (rf & 2) {
        ɵɵattribute("p-size", ctx.size);
      }
    },
    inputs: {
      value: [0, "p-value", "value"],
      format: [0, "p-format", "format"],
      locale: [0, "p-locale", "locale"],
      maxTime: [0, "p-max-time", "maxTime"],
      minTime: [0, "p-min-time", "minTime"],
      minuteInterval: [0, "p-minute-interval", "minuteInterval"],
      secondInterval: [0, "p-second-interval", "secondInterval"],
      showSeconds: [0, "p-show-seconds", "showSeconds"],
      size: [0, "p-size", "size"]
    },
    outputs: {
      change: "p-change"
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoTimerBaseComponent, [{
    type: Directive
  }], () => [{
    type: PoLanguageService
  }], {
    change: [{
      type: Output,
      args: ["p-change"]
    }],
    value: [{
      type: Input,
      args: ["p-value"]
    }],
    format: [{
      type: Input,
      args: ["p-format"]
    }],
    locale: [{
      type: Input,
      args: ["p-locale"]
    }],
    maxTime: [{
      type: Input,
      args: ["p-max-time"]
    }],
    minTime: [{
      type: Input,
      args: ["p-min-time"]
    }],
    minuteInterval: [{
      type: Input,
      args: ["p-minute-interval"]
    }],
    secondInterval: [{
      type: Input,
      args: ["p-second-interval"]
    }],
    showSeconds: [{
      type: Input,
      args: ["p-show-seconds"]
    }],
    size: [{
      type: HostBinding,
      args: ["attr.p-size"]
    }, {
      type: Input,
      args: ["p-size"]
    }],
    onThemeChange: [{
      type: HostListener,
      args: ["window:PoUiThemeChange"]
    }]
  });
})();
var VISIBLE_ITEMS_PER_COLUMN = 6;
var PoTimerComponent = class _PoTimerComponent extends PoTimerBaseComponent {
  // Refs dos botoes de cada coluna para gerenciamento de foco programatico.
  hourCells;
  minuteCells;
  secondCells;
  periodCells;
  // Containers de recorte (clipping) de cada coluna.
  hourItemsRefs;
  minuteItemsRefs;
  secondItemsRefs;
  // Arrays repetidos para o infinity scroll.
  displayHours = [];
  displayMinutes = [];
  displaySeconds = [];
  // Emite quando Tab/Shift+Tab atinge a borda do componente.
  boundaryTab = new EventEmitter();
  changeDetector = inject(ChangeDetectorRef);
  ngZone = inject(NgZone);
  domDocument = inject(DOCUMENT, {
    optional: true
  });
  hasViewInitialized = false;
  currentRenderedSize;
  /** ID do requestAnimationFrame pendente para throttle do wheel. */
  wheelRafId = null;
  /**
   * Offset atual (em px) de cada coluna, mantido em JS.
   * O container de itens e posicionado via translateY(-offset).
   * O offset e mantido sempre no intervalo [sectionHeight, 2*sectionHeight)
   * para que o salto de reposicionamento seja sempre invisivel (as secoes
   * sao identicas por serem copias do mesmo array fonte).
   */
  columnOffsets = {
    hour: 0,
    minute: 0,
    second: 0,
    period: 0
  };
  /**
   * Indice no displayArray do item focado em cada coluna.
   * Usado para redirecionar o foco ao botao correto ao entrar na coluna via Tab.
   */
  focusedDisplayIndex = {
    hour: 0,
    minute: 0,
    second: 0,
    period: 0
  };
  // IDs dos itens ativos em cada coluna para aria-activedescendant.
  // Aponta para o item focado no displayArray.
  activeDescendantIds = {
    hour: "",
    minute: "",
    second: "",
    period: ""
  };
  // Cache de minutos desabilitados para evitar recalculo a cada ciclo de change detection.
  disabledMinuteCache = /* @__PURE__ */ new Set();
  // Cache de segundos desabilitados para evitar recalculo a cada ciclo de change detection.
  disabledSecondCache = /* @__PURE__ */ new Set();
  ariaSyncNeeded = false;
  cachedCellPairs = {
    hour: [],
    minute: [],
    second: [],
    period: []
  };
  constructor() {
    const languageService = inject(PoLanguageService);
    super(languageService);
  }
  ngOnInit() {
    this.generateHours();
    this.generateMinutes();
    this.generateSeconds();
    this.buildDisplayArrays();
    this.rebuildDisabledCaches();
  }
  ngOnChanges(changes) {
    const rebuildKeys = ["format", "showSeconds", "minuteInterval", "secondInterval"];
    const needsRebuild = rebuildKeys.some((key2) => key2 in changes && !changes[key2].firstChange);
    if (needsRebuild) {
      this.buildDisplayArrays();
      this.rebuildDisabledCaches();
      this.changeDetector.markForCheck();
      if (this.hasViewInitialized) {
        this.realignColumnsToSelection();
      }
    }
    if ("minTime" in changes || "maxTime" in changes) {
      this.rebuildDisabledCaches();
      this.changeDetector.markForCheck();
    }
  }
  ngAfterViewInit() {
    this.hasViewInitialized = true;
    this.currentRenderedSize = this.size;
    this.updateAllCachedCellPairs();
    this.subscribeToCellChanges();
    this.ngZone.runOutsideAngular(() => {
      requestAnimationFrame(() => {
        this.initAllColumnOffsets();
        this.syncAriaToNativeButtons();
      });
    });
  }
  ngAfterViewChecked() {
    if (!this.hasViewInitialized) {
      return;
    }
    if (this.ariaSyncNeeded) {
      this.syncAriaToNativeButtons();
      this.ariaSyncNeeded = false;
    }
    const nextSize = this.size;
    if (nextSize !== this.currentRenderedSize) {
      this.currentRenderedSize = nextSize;
      this.ngZone.runOutsideAngular(() => {
        setTimeout(() => {
          this.initAllColumnOffsets();
        });
      });
    }
  }
  ngOnDestroy() {
    if (this.wheelRafId != null) {
      cancelAnimationFrame(this.wheelRafId);
    }
  }
  // Seleciona uma hora.
  onSelectHour(hour) {
    if (this.isHourDisabled(hour)) {
      return;
    }
    this.selectedHour = hour;
    this.rebuildDisabledCaches();
    if (this.selectedMinute != null && this.disabledMinuteCache.has(this.selectedMinute)) {
      this.selectedMinute = this.getFirstAvailableMinuteForCurrentHour();
      this.rebuildDisabledCaches();
    }
    if (this.showSeconds && this.selectedMinute != null && this.selectedSecond != null && this.disabledSecondCache.has(this.selectedSecond)) {
      this.selectedSecond = this.getFirstAvailableSecondForCurrentHourAndMinute();
    }
    this.emitChange();
    this.callOnTouched();
    this.changeDetector.markForCheck();
  }
  // Seleciona um minuto.
  onSelectMinute(minute) {
    if (this.isMinuteDisabled(minute)) {
      return;
    }
    this.selectedMinute = minute;
    this.rebuildDisabledCaches();
    if (this.showSeconds && this.selectedSecond != null && this.disabledSecondCache.has(this.selectedSecond)) {
      this.selectedSecond = this.getFirstAvailableSecondForCurrentHourAndMinute();
    }
    this.emitChange();
    this.callOnTouched();
    this.changeDetector.markForCheck();
  }
  // Seleciona um segundo.
  onSelectSecond(second) {
    if (this.disabledSecondCache.has(second)) {
      return;
    }
    this.selectedSecond = second;
    this.emitChange();
    this.callOnTouched();
    this.changeDetector.markForCheck();
  }
  // Alterna o periodo AM/PM.
  onSelectPeriod(newPeriod) {
    this.period = newPeriod;
    this.rebuildDisabledCaches();
    this.focusedDisplayIndex["period"] = newPeriod === "AM" ? 0 : 1;
    this.focusButtonAt("period", this.focusedDisplayIndex["period"]);
    this.emitChange();
    this.callOnTouched();
    this.changeDetector.markForCheck();
  }
  onCellFocus(type, displayIndex) {
    this.focusedDisplayIndex[type] = displayIndex;
    this.normalizeFocusedIndex(type);
    this.updateActiveDescendant(type, this.focusedDisplayIndex[type]);
    this.markAriaSyncNeeded();
  }
  getCellTabIndex(type, displayIndex) {
    const domFocusedIndex = this.getDomFocusedDisplayIndex(type);
    const normalizedIndex = domFocusedIndex ?? this.getCurrentFocusedDisplayIndex(type);
    return normalizedIndex === displayIndex ? 0 : -1;
  }
  // Trata navegacao via teclado na coluna focada.
  // ArrowUp/Down movem o foco para o proximo/anterior item habilitado e so
  // traduzem a lista quando o item focado sai da viewport.
  // Enter/Space selecionam o item focado.
  // Tab/Shift+Tab movem o foco para a proxima/anterior coluna visivel,
  // ou deixam o browser mover o foco naturalmente ao sair do componente.
  onCellKeydown(event, type) {
    switch (event.key) {
      case "ArrowUp":
        event.preventDefault();
        this.moveFocusByStep(type, -1);
        break;
      case "ArrowDown":
        event.preventDefault();
        this.moveFocusByStep(type, 1);
        break;
      case "Enter":
      case " ":
        event.preventDefault();
        this.selectFocusedItem(type);
        break;
      case "Tab":
        if (event.shiftKey) {
          this.focusPreviousColumn(event, type);
        } else {
          this.focusNextColumn(event, type);
        }
        break;
      default:
        break;
    }
  }
  // Trata navegacao via teclado na coluna AM/PM.
  onPeriodKeydown(event) {
    switch (event.key) {
      case "ArrowUp":
        event.preventDefault();
        this.movePeriodFocusByStep(-1);
        break;
      case "ArrowDown":
        event.preventDefault();
        this.movePeriodFocusByStep(1);
        break;
      case "Enter":
      case " ":
        event.preventDefault();
        this.selectFocusedPeriod();
        break;
      case "Tab":
        if (event.shiftKey) {
          this.focusPreviousColumn(event, "period");
        } else {
          this.focusNextColumn(event, "period");
        }
        break;
      default:
        break;
    }
  }
  // Trata o scroll da roda do mouse nas colunas.
  //
  // A posicao e controlada por JS via translateY, sem qualquer interacao
  // com o scrollTop do browser. Usa throttle via requestAnimationFrame
  // para agrupar multiplos ticks do wheel em um unico step por frame,
  // evitando acumulo em trackpads de alta resolucao.
  onColumnWheel(event, type) {
    event.preventDefault();
    if (this.wheelRafId != null) {
      return;
    }
    const direction = event.deltaY > 0 ? 1 : -1;
    this.ngZone.runOutsideAngular(() => {
      this.wheelRafId = requestAnimationFrame(() => {
        this.wheelRafId = null;
        this.scrollColumnByStep(type, direction);
      });
    });
  }
  // Define o horario a partir de um valor externo.
  writeValue(time) {
    if (this.hasViewInitialized && time === this.buildTimeValue()) {
      return;
    }
    super.writeValue(time);
    this.rebuildDisabledCaches();
    this.changeDetector.markForCheck();
    if (this.hasViewInitialized) {
      requestAnimationFrame(() => {
        this.ngZone.runOutsideAngular(() => {
          this.initAllColumnOffsets();
        });
      });
    }
  }
  // Track function para o @for do infinity scroll.
  trackByIndex(index, _item) {
    return index;
  }
  focusFirstVisibleCell() {
    const firstType = this.getVisibleColumnTypes()[0];
    if (firstType) {
      this.focusActiveButton(firstType);
    }
  }
  focusLastVisibleCell() {
    const columns = this.getVisibleColumnTypes();
    const lastType = columns[columns.length - 1];
    if (lastType) {
      this.focusActiveButton(lastType);
    }
  }
  initAllColumnOffsets() {
    this.initColumnOffset("hour");
    this.initColumnOffset("minute");
    this.initColumnOffset("second");
    if (this.is12HourFormat) {
      this.focusedDisplayIndex["period"] = this.period === "PM" ? 1 : 0;
    }
    this.refreshRovingTabIndex();
    this.syncAriaToNativeButtons();
  }
  /**
   * Posiciona o container de itens na secao do meio do array repetido,
   * alinhando o item selecionado ao topo da janela visivel.
   *
   * Estrutura do array repetido (exemplo sourceLength = 24, repeats = 3):
   *   [secao 0: itens 0-23] [secao 1: itens 0-23] [secao 2: itens 0-23]
   *                         ^--- usuario fica aqui (offset em [sH, 2*sH))
   *
   * Usar a secao do meio garante que qualquer deslize para cima ou para baixo
   * tem espaco antes de precisar fazer o wrap.
   */
  initColumnOffset(type) {
    const itemsEl = this.getItemsElement(type);
    if (!itemsEl) {
      return;
    }
    const sourceArray = this.getSourceArray(type);
    const displayArray = this.getDisplayArray(type);
    if (!sourceArray.length || !displayArray.length) {
      return;
    }
    const step = this.getCellStep(itemsEl, displayArray.length);
    const sectionHeight = sourceArray.length * step;
    const useInfinityScroll = sourceArray.length >= 6;
    const selectedValue = this.getSelectedValue(type);
    const selectedIndex = sourceArray.indexOf(selectedValue);
    const selectedValueIsValid = selectedIndex >= 0 && selectedValue != null && !this.isValueDisabledByType(type, sourceArray[selectedIndex]);
    const alignIndex = selectedValueIsValid ? selectedIndex : this.getFirstAvailableIndexByType(type, sourceArray);
    const offset = useInfinityScroll ? sectionHeight + alignIndex * step : 0;
    this.columnOffsets[type] = offset;
    itemsEl.style.transform = useInfinityScroll ? `translateY(${-offset}px)` : "";
    this.focusedDisplayIndex[type] = useInfinityScroll ? this.computeTopDisplayIndex(offset, step, displayArray.length) : alignIndex;
    this.normalizeFocusedIndex(type);
    this.updateActiveDescendant(type, this.focusedDisplayIndex[type]);
  }
  getFirstAvailableIndexByType(type, sourceArray) {
    if (!sourceArray?.length) {
      return 0;
    }
    switch (type) {
      case "hour": {
        const index = sourceArray.findIndex((hour) => !this.isHourDisabled(hour));
        return index >= 0 ? index : 0;
      }
      case "minute": {
        const referenceHour = this.getReferenceHourForConstraints();
        if (referenceHour == null) {
          return 0;
        }
        const index = sourceArray.findIndex((minute) => this.isMinuteAllowedForHour(referenceHour, minute));
        return index >= 0 ? index : 0;
      }
      case "second": {
        const referenceHour = this.getReferenceHourForConstraints();
        if (referenceHour == null) {
          return 0;
        }
        const referenceMinute = this.getReferenceMinuteForConstraints(referenceHour);
        if (referenceMinute == null) {
          return 0;
        }
        const index = sourceArray.findIndex((second) => this.isSecondAllowed(referenceHour, referenceMinute, second));
        return index >= 0 ? index : 0;
      }
      default:
        return 0;
    }
  }
  getReferenceHourForConstraints() {
    if (this.selectedHour != null && !this.isHourDisabled(this.selectedHour)) {
      return this.selectedHour;
    }
    const index = this.hours.findIndex((hour) => !this.isHourDisabled(hour));
    return index >= 0 ? this.hours[index] : null;
  }
  getReferenceMinuteForConstraints(referenceHour) {
    if (this.selectedMinute != null && this.isMinuteAllowedForHour(referenceHour, this.selectedMinute)) {
      return this.selectedMinute;
    }
    const index = this.minutes.findIndex((minute) => this.isMinuteAllowedForHour(referenceHour, minute));
    return index >= 0 ? this.minutes[index] : null;
  }
  isValueDisabledByType(type, value) {
    switch (type) {
      case "hour":
        return this.isHourDisabled(value);
      case "minute": {
        if (this.selectedHour == null) {
          return false;
        }
        return this.isMinuteDisabled(value);
      }
      case "second": {
        if (this.selectedHour == null || this.selectedMinute == null) {
          return false;
        }
        return this.isSecondDisabled(value);
      }
      default:
        return false;
    }
  }
  /**
   * Desloca a coluna pelo numero de passos indicado e aplica o wrap modular.
   *
   * O wrap mantem o offset em [sectionHeight, 2*sectionHeight), aproveitando
   * o fato de que as secoes sao identicas — o salto e impercetivel visualmente.
   */
  scrollColumnByStep(type, steps, syncFocusToTop = true) {
    const itemsEl = this.getItemsElement(type);
    if (!itemsEl) {
      return;
    }
    const sourceArray = this.getSourceArray(type);
    const displayArray = this.getDisplayArray(type);
    if (!sourceArray.length || !displayArray.length) {
      return;
    }
    const step = this.getCellStep(itemsEl, displayArray.length);
    const sectionHeight = sourceArray.length * step;
    const useInfinityScroll = sourceArray.length >= 6;
    if (!useInfinityScroll) {
      const newIndex = ((this.focusedDisplayIndex[type] + steps) % sourceArray.length + sourceArray.length) % sourceArray.length;
      if (syncFocusToTop) {
        this.focusedDisplayIndex[type] = newIndex;
        this.normalizeFocusedIndex(type);
        this.updateActiveDescendant(type, this.focusedDisplayIndex[type]);
      }
      return;
    }
    const rawOffset = this.columnOffsets[type] + steps * step;
    const wrappedOffset = this.wrapOffset(rawOffset, sectionHeight);
    this.columnOffsets[type] = wrappedOffset;
    itemsEl.style.transform = `translateY(${-wrappedOffset}px)`;
    if (syncFocusToTop) {
      this.focusedDisplayIndex[type] = this.computeTopDisplayIndex(wrappedOffset, step, displayArray.length);
      this.normalizeFocusedIndex(type);
      this.updateActiveDescendant(type, this.focusedDisplayIndex[type]);
    }
    this.syncAriaToNativeButtons();
  }
  wrapOffset(offset, sectionHeight) {
    return PoTimerScrollHelper.wrapOffset(offset, sectionHeight);
  }
  getCellStep(itemsEl, displayCount) {
    return PoTimerScrollHelper.getCellStep(itemsEl, displayCount);
  }
  /**
   * Foca o botao nativo (<button> interno ao po-button) no indice indicado
   * do displayArray da coluna. O po-button tem tabindex=-1 para nao aparecer
   * no fluxo natural do Tab, mas pode receber foco programatico.
   */
  focusButtonAt(type, displayIndex) {
    const cellPairs = this.cachedCellPairs[type];
    if (!cellPairs?.length) {
      return;
    }
    const startIndex = this.getFocusableDisplayIndex(type, displayIndex, cellPairs.length);
    for (let offset = 0; offset < cellPairs.length; offset++) {
      const index = (startIndex + offset) % cellPairs.length;
      const {
        nativeButton
      } = cellPairs[index];
      if (!nativeButton || nativeButton.disabled || nativeButton.getAttribute("aria-disabled") === "true") {
        continue;
      }
      this.focusedDisplayIndex[type] = index;
      this.syncAriaToNativeButtons();
      nativeButton.focus({
        preventScroll: true
      });
      this.refreshRovingTabIndex();
      return;
    }
  }
  focusActiveButton(type) {
    this.normalizeFocusedIndex(type);
    this.focusButtonAt(type, this.focusedDisplayIndex[type]);
    this.updateActiveDescendant(type, this.focusedDisplayIndex[type]);
  }
  computeTopDisplayIndex(offset, step, displayLength) {
    return PoTimerScrollHelper.computeTopDisplayIndex(offset, step, displayLength);
  }
  /**
   * Foca a proxima coluna visivel do componente.
   * Se a coluna atual for a ultima, nao cancela o evento e deixa o browser
   * mover o foco para o proximo elemento focavel apos o componente.
   */
  focusNextColumn(event, type) {
    const columns = this.getVisibleColumnTypes();
    const currentIdx = columns.indexOf(type);
    if (currentIdx < 0) {
      return;
    }
    const nextType = columns[currentIdx + 1];
    if (nextType) {
      event.preventDefault();
      this.focusActiveButton(nextType);
      return;
    }
    this.boundaryTab.emit({
      direction: "forward",
      event,
      column: type
    });
  }
  /**
   * Foca a coluna anterior visivel do componente.
   * Se a coluna atual for a primeira, nao cancela o evento e deixa o browser
   * mover o foco para o elemento focavel antes do componente.
   */
  focusPreviousColumn(event, type) {
    const columns = this.getVisibleColumnTypes();
    const currentIdx = columns.indexOf(type);
    if (currentIdx < 0) {
      return;
    }
    const prevType = columns[currentIdx - 1];
    if (prevType) {
      event.preventDefault();
      this.focusActiveButton(prevType);
      return;
    }
    this.boundaryTab.emit({
      direction: "backward",
      event,
      column: type
    });
  }
  /**
   * Retorna, em ordem DOM, os elementos focaveis de todas as colunas visiveis.
   * Inclui o div da coluna AM/PM (se visivel), cujos botoes internos recebem
   * o foco diretamente pois nao usam o padrao de roving focus.
   */
  getVisibleColumnTypes() {
    const columns = ["hour", "minute"];
    if (this.showSeconds) {
      columns.push("second");
    }
    if (this.is12HourFormat) {
      columns.push("period");
    }
    return columns;
  }
  updateAllCachedCellPairs() {
    this.updateCachedCellPairs("hour", this.hourCells);
    this.updateCachedCellPairs("minute", this.minuteCells);
    this.updateCachedCellPairs("second", this.secondCells);
    this.updateCachedCellPairs("period", this.periodCells);
    this.markAriaSyncNeeded();
  }
  subscribeToCellChanges() {
    this.hourCells?.changes?.subscribe(() => this.updateCachedCellPairs("hour", this.hourCells));
    this.minuteCells?.changes?.subscribe(() => this.updateCachedCellPairs("minute", this.minuteCells));
    this.secondCells?.changes?.subscribe(() => this.updateCachedCellPairs("second", this.secondCells));
    this.periodCells?.changes?.subscribe(() => this.updateCachedCellPairs("period", this.periodCells));
  }
  updateCachedCellPairs(type, cells) {
    const elements = cells?.toArray() ?? [];
    this.cachedCellPairs[type] = elements.map((component) => {
      const hostEl = component.elementRef.nativeElement;
      const nativeButton = component.buttonElement?.nativeElement || hostEl?.querySelector("button");
      return nativeButton && hostEl ? {
        hostEl,
        nativeButton
      } : null;
    }).filter((pair) => pair !== null);
    this.markAriaSyncNeeded();
  }
  markAriaSyncNeeded() {
    this.ariaSyncNeeded = true;
  }
  /** Atualiza aria-activedescendant para o indice focado no displayArray. */
  updateActiveDescendant(type, displayIndex) {
    const normalizedIndex = this.getNormalizedDisplayIndex(type, displayIndex);
    if (normalizedIndex < 0) {
      return;
    }
    this.activeDescendantIds[type] = `po-timer-${type}-${normalizedIndex}`;
  }
  /**
   * Move o foco para o proximo item habilitado na direcao indicada.
   * So aplica translate quando o item focado fica parcial ou totalmente fora da viewport.
   */
  moveFocusByStep(type, stepDirection) {
    const displayArray = this.getDisplayArray(type);
    if (!displayArray.length) {
      return;
    }
    const nextIndex = this.getNextEnabledDisplayIndex(type, this.focusedDisplayIndex[type], stepDirection);
    this.focusedDisplayIndex[type] = nextIndex;
    this.normalizeFocusedIndex(type);
    const normalizedFocusedIndex = this.focusedDisplayIndex[type];
    if (this.shouldTranslateToRevealFocusedItem(type, normalizedFocusedIndex)) {
      const stepsToReveal = this.getStepsToRevealFocusedItem(type, normalizedFocusedIndex, stepDirection);
      if (stepsToReveal !== 0) {
        this.scrollColumnByStep(type, stepsToReveal, false);
        this.focusedDisplayIndex[type] = normalizedFocusedIndex;
      }
    }
    this.focusButtonAt(type, this.focusedDisplayIndex[type]);
    this.updateActiveDescendant(type, this.focusedDisplayIndex[type]);
  }
  movePeriodFocusByStep(stepDirection) {
    const nextIndex = ((this.getCurrentFocusedDisplayIndex("period") + stepDirection) % 2 + 2) % 2;
    const targetPeriod = nextIndex === 0 ? "AM" : "PM";
    if (this.isPeriodDisabled(targetPeriod)) {
      return;
    }
    this.focusedDisplayIndex["period"] = nextIndex;
    this.focusButtonAt("period", nextIndex);
    this.updateActiveDescendant("period", nextIndex);
  }
  selectFocusedPeriod() {
    const focusedPeriodIndex = this.getCurrentFocusedDisplayIndex("period");
    const targetPeriod = focusedPeriodIndex === 0 ? "AM" : "PM";
    if (this.isPeriodDisabled(targetPeriod)) {
      return;
    }
    this.onSelectPeriod(targetPeriod);
  }
  getFocusableDisplayIndex(type, displayIndex, displayLength) {
    const normalizedIndex = (displayIndex % displayLength + displayLength) % displayLength;
    const sourceArray = this.getSourceArray(type);
    const sourceLength = sourceArray.length;
    if (sourceLength < VISIBLE_ITEMS_PER_COLUMN) {
      return normalizedIndex;
    }
    const itemsEl = this.getItemsElement(type);
    if (!itemsEl) {
      return normalizedIndex;
    }
    const step = this.getCellStep(itemsEl, displayLength);
    if (step <= 0) {
      return normalizedIndex;
    }
    const viewportHeight = this.getColumnViewportHeight(itemsEl, step);
    const sourceIndex = (normalizedIndex % sourceLength + sourceLength) % sourceLength;
    const tolerance = 0.5;
    let bestIndex = normalizedIndex;
    let bestScore = Number.POSITIVE_INFINITY;
    for (let candidate = sourceIndex; candidate < displayLength; candidate += sourceLength) {
      const itemTop = candidate * step - this.columnOffsets[type];
      const itemBottom = itemTop + step;
      const isFullyVisible = itemTop >= -tolerance && itemBottom <= viewportHeight + tolerance;
      let clippedPixels = 0;
      if (itemTop < 0) {
        clippedPixels = -itemTop;
      } else if (itemBottom > viewportHeight) {
        clippedPixels = itemBottom - viewportHeight;
      }
      const sectionDistance = Math.abs(candidate - normalizedIndex) / sourceLength;
      const score = (isFullyVisible ? 0 : 1e3) + clippedPixels + sectionDistance;
      if (score < bestScore) {
        bestScore = score;
        bestIndex = candidate;
      }
    }
    return bestIndex;
  }
  /** Seleciona o item atualmente focado na coluna. */
  selectFocusedItem(type) {
    const displayArray = this.getDisplayArray(type);
    if (!displayArray.length) {
      return;
    }
    const displayIndex = this.getNormalizedDisplayIndex(type, this.focusedDisplayIndex[type]);
    if (displayIndex < 0 || this.isDisplayIndexDisabled(type, displayIndex, displayArray)) {
      return;
    }
    const value = displayArray[displayIndex];
    switch (type) {
      case "hour":
        this.onSelectHour(value);
        break;
      case "minute":
        this.onSelectMinute(value);
        break;
      case "second":
        this.onSelectSecond(value);
        break;
      default:
        break;
    }
  }
  shouldTranslateToRevealFocusedItem(type, focusedIndex) {
    const itemsEl = this.getItemsElement(type);
    const sourceArray = this.getSourceArray(type);
    const displayArray = this.getDisplayArray(type);
    if (!itemsEl || !sourceArray.length || sourceArray.length < VISIBLE_ITEMS_PER_COLUMN || !displayArray.length) {
      return false;
    }
    const step = this.getCellStep(itemsEl, displayArray.length);
    if (step <= 0) {
      return false;
    }
    const viewportHeight = this.getColumnViewportHeight(itemsEl, step);
    const normalizedIndex = this.getNormalizedDisplayIndex(type, focusedIndex);
    const itemTop = normalizedIndex * step - this.columnOffsets[type];
    const itemBottom = itemTop + step;
    const tolerance = 0.5;
    return itemTop < -tolerance || itemBottom > viewportHeight + tolerance;
  }
  getStepsToRevealFocusedItem(type, focusedIndex, _stepDirection) {
    const itemsEl = this.getItemsElement(type);
    const displayArray = this.getDisplayArray(type);
    if (!itemsEl || !displayArray.length) {
      return 0;
    }
    const step = this.getCellStep(itemsEl, displayArray.length);
    if (step <= 0) {
      return 0;
    }
    const viewportHeight = this.getColumnViewportHeight(itemsEl, step);
    const normalizedIndex = this.getNormalizedDisplayIndex(type, focusedIndex);
    const itemTop = normalizedIndex * step - this.columnOffsets[type];
    const itemBottom = itemTop + step;
    const tolerance = 0.5;
    if (itemTop < -tolerance) {
      return -Math.max(1, Math.ceil(-itemTop / step));
    }
    if (itemBottom > viewportHeight + tolerance) {
      return Math.max(1, Math.ceil((itemBottom - viewportHeight) / step));
    }
    return 0;
  }
  getColumnViewportHeight(itemsEl, step) {
    const viewportEl = itemsEl.parentElement;
    if (viewportEl?.clientHeight) {
      return viewportEl.clientHeight;
    }
    return step * VISIBLE_ITEMS_PER_COLUMN;
  }
  getNextEnabledDisplayIndex(type, currentIndex, stepDirection) {
    const displayArray = this.getDisplayArray(type);
    const sourceArray = this.getSourceArray(type);
    const sourceLength = sourceArray.length;
    if (!displayArray.length) {
      return 0;
    }
    const startIndex = this.getNormalizedDisplayIndex(type, currentIndex);
    const maxIterations = sourceLength >= VISIBLE_ITEMS_PER_COLUMN ? sourceLength : displayArray.length;
    for (let offset = 1; offset <= maxIterations; offset++) {
      const candidateIndex = this.getNormalizedDisplayIndex(type, startIndex + offset * stepDirection);
      if (!this.isDisplayIndexDisabled(type, candidateIndex, displayArray)) {
        const sourceIndex = sourceLength > 0 ? candidateIndex % sourceLength : candidateIndex;
        return this.getDisplayIndexForSourceNearViewport(type, sourceIndex, stepDirection);
      }
    }
    return startIndex;
  }
  getForwardDistance(fromIndex, toIndex, length) {
    if (length <= 0) {
      return 0;
    }
    return ((toIndex - fromIndex) % length + length) % length;
  }
  getNormalizedDisplayIndex(type, displayIndex) {
    const displayArray = this.getDisplayArray(type);
    if (!displayArray.length) {
      return -1;
    }
    return (displayIndex % displayArray.length + displayArray.length) % displayArray.length;
  }
  getDisplayIndexForSourceNearViewport(type, sourceIndex, stepDirection) {
    const displayArray = this.getDisplayArray(type);
    const sourceArray = this.getSourceArray(type);
    const sourceLength = sourceArray.length;
    if (!displayArray.length || sourceLength === 0) {
      return this.getNormalizedDisplayIndex(type, sourceIndex);
    }
    if (sourceLength < VISIBLE_ITEMS_PER_COLUMN) {
      return this.getNormalizedDisplayIndex(type, sourceIndex);
    }
    const normalizedSourceIndex = (sourceIndex % sourceLength + sourceLength) % sourceLength;
    const itemsEl = this.getItemsElement(type);
    const visibleCount = Math.min(VISIBLE_ITEMS_PER_COLUMN, displayArray.length);
    if (!itemsEl) {
      return this.getNormalizedDisplayIndex(type, sourceLength + normalizedSourceIndex);
    }
    const step = this.getCellStep(itemsEl, displayArray.length);
    if (step <= 0) {
      return this.getNormalizedDisplayIndex(type, sourceLength + normalizedSourceIndex);
    }
    const topIndex = this.computeTopDisplayIndex(this.columnOffsets[type], step, displayArray.length);
    let bestIndex = this.getNormalizedDisplayIndex(type, sourceLength + normalizedSourceIndex);
    let bestScore = Number.POSITIVE_INFINITY;
    for (let candidate = normalizedSourceIndex; candidate < displayArray.length; candidate += sourceLength) {
      const isVisible = this.getForwardDistance(topIndex, candidate, displayArray.length) < visibleCount;
      const directionalDistance = stepDirection > 0 ? this.getForwardDistance(topIndex, candidate, displayArray.length) : this.getForwardDistance(candidate, topIndex, displayArray.length);
      const score = (isVisible ? 0 : displayArray.length) + directionalDistance;
      if (score < bestScore) {
        bestScore = score;
        bestIndex = candidate;
      }
    }
    return bestIndex;
  }
  getItemsElement(type) {
    switch (type) {
      case "hour":
        return this.hourItemsRefs?.first?.nativeElement;
      case "minute":
        return this.minuteItemsRefs?.first?.nativeElement;
      case "second":
        return this.secondItemsRefs?.first?.nativeElement;
      default:
        return null;
    }
  }
  normalizeFocusedIndex(type) {
    this.focusedDisplayIndex[type] = this.getNormalizedFocusedIndex(type);
  }
  getNormalizedFocusedIndex(type) {
    const displayArray = this.getDisplayArray(type);
    if (!displayArray.length) {
      return 0;
    }
    if (type === "period") {
      return (this.focusedDisplayIndex.period % 2 + 2) % 2;
    }
    const startIndex = (this.focusedDisplayIndex[type] % displayArray.length + displayArray.length) % displayArray.length;
    for (let offset = 0; offset < displayArray.length; offset++) {
      const index = (startIndex + offset) % displayArray.length;
      if (!this.isDisplayIndexDisabled(type, index, displayArray)) {
        return index;
      }
    }
    return startIndex;
  }
  getCurrentFocusedDisplayIndex(type) {
    if (type === "period") {
      return (this.focusedDisplayIndex.period % 2 + 2) % 2;
    }
    return this.getNormalizedDisplayIndex(type, this.focusedDisplayIndex[type]);
  }
  getDomFocusedDisplayIndex(type) {
    if (!this.domDocument) {
      return null;
    }
    const activeElement = this.domDocument.activeElement;
    const hostButton = activeElement?.closest("po-button.po-timer-display");
    const hostId = hostButton?.id;
    if (!hostId?.startsWith(`po-timer-${type}-`)) {
      return null;
    }
    const rawIndex = Number.parseInt(hostId.replace(`po-timer-${type}-`, ""), 10);
    return Number.isNaN(rawIndex) ? null : rawIndex;
  }
  isDisplayIndexDisabled(type, displayIndex, displayArray) {
    const value = displayArray[displayIndex];
    switch (type) {
      case "hour":
        return this.isHourDisabled(value);
      case "minute":
        return this.isMinuteDisabled(value);
      case "second":
        return this.isSecondDisabled(value);
      case "period":
        return this.isPeriodDisabled(displayIndex === 0 ? "AM" : "PM");
      default:
        return false;
    }
  }
  getSelectedValue(type) {
    switch (type) {
      case "hour":
        return this.selectedHour;
      case "minute":
        return this.selectedMinute;
      case "second":
        return this.selectedSecond;
      default:
        return 0;
    }
  }
  buildDisplayArrays() {
    this.displayHours = this.repeatArray(this.hours);
    this.displayMinutes = this.repeatArray(this.minutes);
    this.displaySeconds = this.repeatArray(this.seconds);
    this.markAriaSyncNeeded();
  }
  /** Reconstroi os caches de minutos e segundos desabilitados. */
  rebuildDisabledCaches() {
    this.disabledMinuteCache = new Set(this.minutes.filter((m2) => this.isMinuteDisabled(m2)));
    this.disabledSecondCache = new Set(this.seconds.filter((s) => this.isSecondDisabled(s)));
  }
  getFirstAvailableMinuteForCurrentHour() {
    const firstAvailableMinute = this.minutes.find((minute) => !this.isMinuteDisabled(minute));
    return firstAvailableMinute ?? null;
  }
  getFirstAvailableSecondForCurrentHourAndMinute() {
    const firstAvailableSecond = this.seconds.find((second) => !this.isSecondDisabled(second));
    return firstAvailableSecond ?? null;
  }
  realignColumnsToSelection() {
    this.ngZone.runOutsideAngular(() => {
      requestAnimationFrame(() => {
        requestAnimationFrame(() => {
          this.initAllColumnOffsets();
        });
      });
    });
  }
  refreshRovingTabIndex() {
    this.changeDetector.markForCheck();
  }
  repeatArray(source) {
    return PoTimerScrollHelper.repeatArray(source);
  }
  getSourceArray(type) {
    switch (type) {
      case "hour":
        return this.hours;
      case "minute":
        return this.minutes;
      case "second":
        return this.seconds;
      default:
        return this.hours;
    }
  }
  getDisplayArray(type) {
    switch (type) {
      case "hour":
        return this.displayHours;
      case "minute":
        return this.displayMinutes;
      case "second":
        return this.displaySeconds;
      default:
        return [];
    }
  }
  /**
   * Propaga atributos ARIA (role, aria-selected, aria-setsize, aria-posinset)
   * dos elementos host <po-button> para os elementos nativos <button> internos,
   * garantindo que leitores de tela como NVDA recebam a semantica correta.
   *
   * Usa a secao fixa do meio [sourceLength, 2*sourceLength) como canonica.
   * Quando o item focado esta fora dessa secao (na fronteira do infinity scroll),
   * ele substitui o item equivalente na secao fixa para manter exatamente
   * sourceLength itens com role="option".
   *
   * Todos os botoes nativos recebem os atributos ARIA corretos
   * (role, aria-selected, aria-setsize, aria-posinset) propagados
   * dos data-attributes do host <po-button>.
   *
   * Nao usa `inert` — o infinity scroll repete o array 3x e marcar
   * secoes como inert causava botoes desabilitados durante transicoes
   * de scroll. Em vez disso, duplicatas recebem role="none" e
   * aria-hidden="true" no <button> nativo para que NVDA nao as conte,
   * mas permanecem clicaveis visualmente.
   *
   * A secao canonica e determinada pela secao do item focado,
   * garantindo que exatamente sourceLength itens tenham role="option".
   */
  syncAriaToNativeButtons() {
    const columnTypes = ["hour", "minute", "second"];
    for (const type of columnTypes) {
      const cellPairs = this.cachedCellPairs[type];
      if (!cellPairs?.length) {
        continue;
      }
      const sourceLength = this.getSourceArray(type).length;
      const useInfinityScroll = sourceLength >= VISIBLE_ITEMS_PER_COLUMN;
      const focusedIdx = this.focusedDisplayIndex[type];
      const canonicalStart = useInfinityScroll && sourceLength > 0 ? Math.floor(focusedIdx / sourceLength) * sourceLength : 0;
      for (let i2 = 0; i2 < cellPairs.length; i2++) {
        const {
          hostEl,
          nativeButton
        } = cellPairs[i2];
        if (!hostEl || !nativeButton) {
          continue;
        }
        const isCanonical = this.isCanonicalDisplayItem(i2, useInfinityScroll, sourceLength, canonicalStart);
        if (isCanonical) {
          nativeButton.removeAttribute("aria-hidden");
          this.syncSingleButtonAria(hostEl, nativeButton, i2, sourceLength);
        } else {
          nativeButton.setAttribute("role", "none");
          nativeButton.setAttribute("aria-hidden", "true");
          nativeButton.removeAttribute("aria-setsize");
          nativeButton.removeAttribute("aria-posinset");
          nativeButton.removeAttribute("aria-selected");
        }
      }
    }
  }
  /**
   * Determina se um item do displayArray e canonico para fins de ARIA.
   *
   * - Sem infinity scroll: todos sao canonicos.
   * - Com infinity scroll: apenas itens na secao canonica (determinada
   *   pelo offset visivel) recebem role="option".
   */
  isCanonicalDisplayItem(index, useInfinityScroll, sourceLength, canonicalStart) {
    if (!useInfinityScroll) {
      return true;
    }
    if (sourceLength <= 0) {
      return false;
    }
    return index >= canonicalStart && index < canonicalStart + sourceLength;
  }
  /**
   * Sincroniza os atributos ARIA de um unico par host/nativeButton.
   * Chamado tanto por syncAriaToNativeButtons (batch) quanto por
   * focusButtonAt (antes do .focus()) para evitar leitura duplicada.
   */
  syncSingleButtonAria(hostEl, nativeButton, displayIndex, sourceLength) {
    const role = hostEl.getAttribute("data-aria-role");
    const selected = hostEl.getAttribute("data-aria-selected");
    if (role) {
      nativeButton.setAttribute("role", role);
    }
    if (selected === "true") {
      nativeButton.setAttribute("aria-selected", "true");
    } else {
      nativeButton.removeAttribute("aria-selected");
    }
    if (sourceLength > 0) {
      const normalizedPosInSet = (displayIndex % sourceLength + sourceLength) % sourceLength + 1;
      nativeButton.setAttribute("aria-setsize", String(sourceLength));
      nativeButton.setAttribute("aria-posinset", String(normalizedPosInSet));
    } else {
      nativeButton.removeAttribute("aria-setsize");
      nativeButton.removeAttribute("aria-posinset");
    }
  }
  static ɵfac = function PoTimerComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoTimerComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoTimerComponent,
    selectors: [["po-timer"]],
    viewQuery: function PoTimerComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c37, 5)(_c38, 5)(_c39, 5)(_c40, 5)(_c41, 5, ElementRef)(_c42, 5, ElementRef)(_c43, 5, ElementRef);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.hourCells = _t);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.minuteCells = _t);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.secondCells = _t);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.periodCells = _t);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.hourItemsRefs = _t);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.minuteItemsRefs = _t);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.secondItemsRefs = _t);
      }
    },
    outputs: {
      boundaryTab: "p-boundary-tab"
    },
    standalone: false,
    features: [ɵɵProvidersFeature([{
      provide: NG_VALUE_ACCESSOR,
      useExisting: forwardRef(() => _PoTimerComponent),
      multi: true
    }]), ɵɵInheritDefinitionFeature, ɵɵNgOnChangesFeature],
    decls: 20,
    vars: 6,
    consts: [["hourColumn", ""], ["hourScroll", ""], ["hourItems", ""], ["minuteColumn", ""], ["minuteScroll", ""], ["minuteItems", ""], ["hourCell", ""], ["minuteCell", ""], ["secondColumn", ""], ["secondScroll", ""], ["secondItems", ""], ["secondCell", ""], ["periodColumn", ""], ["periodCell", ""], [1, "po-timer-wrapper"], [1, "po-timer"], ["role", "listbox", "tabindex", "-1", 1, "po-timer-column", 3, "wheel"], [1, "po-timer-column-scroll"], [1, "po-timer-column-items"], ["p-kind", "tertiary", 1, "po-timer-display", 3, "p-tabindex", "p-label", "p-aria-label", "p-disabled", "p-size", "po-timer-display-selected"], ["role", "listbox", "tabindex", "-1", 1, "po-timer-column"], ["role", "radiogroup", "aria-label", "AM/PM", 1, "po-timer-column", "po-timer-column-period"], ["p-kind", "tertiary", 1, "po-timer-display", 3, "focus", "keydown", "p-click", "p-tabindex", "p-label", "p-aria-label", "p-disabled", "p-size"], [1, "po-timer-column-scroll", "po-timer-period-scroll", "po-timer-column-items"], ["p-kind", "tertiary", "p-label", "AM", 1, "po-timer-display", 3, "focus", "p-click", "keydown", "p-size", "p-tabindex", "p-disabled"], ["p-kind", "tertiary", "p-label", "PM", 1, "po-timer-display", 3, "focus", "p-click", "keydown", "p-size", "p-tabindex", "p-disabled"]],
    template: function PoTimerComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "div", 14)(1, "div", 15)(2, "div", 16, 0);
        ɵɵlistener("wheel", function PoTimerComponent_Template_div_wheel_2_listener($event) {
          return ctx.onColumnWheel($event, "hour");
        });
        ɵɵelementStart(4, "div", 17, 1)(6, "div", 18, 2);
        ɵɵrepeaterCreate(8, PoTimerComponent_For_9_Template, 2, 12, "po-button", 19, ctx.trackByIndex, true);
        ɵɵelementEnd()()();
        ɵɵelementStart(10, "div", 16, 3);
        ɵɵlistener("wheel", function PoTimerComponent_Template_div_wheel_10_listener($event) {
          return ctx.onColumnWheel($event, "minute");
        });
        ɵɵelementStart(12, "div", 17, 4)(14, "div", 18, 5);
        ɵɵrepeaterCreate(16, PoTimerComponent_For_17_Template, 2, 12, "po-button", 19, ctx.trackByIndex, true);
        ɵɵelementEnd()()();
        ɵɵconditionalCreate(18, PoTimerComponent_Conditional_18_Template, 8, 2, "div", 20);
        ɵɵconditionalCreate(19, PoTimerComponent_Conditional_19_Template, 7, 14, "div", 21);
        ɵɵelementEnd()();
      }
      if (rf & 2) {
        ɵɵadvance(2);
        ɵɵattribute("aria-label", ctx.literals.hours)("aria-activedescendant", ctx.activeDescendantIds.hour);
        ɵɵadvance(6);
        ɵɵrepeater(ctx.displayHours);
        ɵɵadvance(2);
        ɵɵattribute("aria-label", ctx.literals.minutes)("aria-activedescendant", ctx.activeDescendantIds.minute);
        ɵɵadvance(6);
        ɵɵrepeater(ctx.displayMinutes);
        ɵɵadvance(2);
        ɵɵconditional(ctx.showSeconds ? 18 : -1);
        ɵɵadvance();
        ɵɵconditional(ctx.is12HourFormat ? 19 : -1);
      }
    },
    dependencies: [PoButtonComponent],
    encapsulation: 2,
    changeDetection: 0
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoTimerComponent, [{
    type: Component,
    args: [{
      selector: "po-timer",
      changeDetection: ChangeDetectionStrategy.OnPush,
      providers: [{
        provide: NG_VALUE_ACCESSOR,
        useExisting: forwardRef(() => PoTimerComponent),
        multi: true
      }],
      standalone: false,
      template: `<div class="po-timer-wrapper">
  <div class="po-timer">
    <!-- Coluna de Horas -->
    <div
      #hourColumn
      class="po-timer-column"
      role="listbox"
      tabindex="-1"
      [attr.aria-label]="literals.hours"
      [attr.aria-activedescendant]="activeDescendantIds.hour"
      (wheel)="onColumnWheel($event, 'hour')"
    >
      <div class="po-timer-column-scroll" #hourScroll>
        <div class="po-timer-column-items" #hourItems>
          @for (hour of displayHours; track trackByIndex($index, hour); let i = $index) {
            <po-button
              #hourCell
              class="po-timer-display"
              p-kind="tertiary"
              [attr.id]="'po-timer-hour-' + i"
              [p-tabindex]="getCellTabIndex('hour', i)"
              [p-label]="formatValue(hour)"
              [p-aria-label]="formatValue(hour) + ' ' + literals.hours"
              [p-disabled]="isHourDisabled(hour)"
              [p-size]="size"
              [class.po-timer-display-selected]="selectedHour === hour"
              [attr.data-aria-role]="'option'"
              [attr.data-aria-selected]="selectedHour === hour ? 'true' : null"
              [attr.data-aria-setsize]="hours.length"
              [attr.data-aria-posinset]="(i % hours.length) + 1"
              (focus)="onCellFocus('hour', i)"
              (keydown)="onCellKeydown($event, 'hour')"
              (p-click)="onSelectHour(hour); onCellFocus('hour', i)"
            >
            </po-button>
          }
        </div>
      </div>
    </div>

    <!-- Coluna de Minutos -->
    <div
      #minuteColumn
      class="po-timer-column"
      role="listbox"
      tabindex="-1"
      [attr.aria-label]="literals.minutes"
      [attr.aria-activedescendant]="activeDescendantIds.minute"
      (wheel)="onColumnWheel($event, 'minute')"
    >
      <div class="po-timer-column-scroll" #minuteScroll>
        <div class="po-timer-column-items" #minuteItems>
          @for (minute of displayMinutes; track trackByIndex($index, minute); let i = $index) {
            <po-button
              #minuteCell
              class="po-timer-display"
              p-kind="tertiary"
              [attr.id]="'po-timer-minute-' + i"
              [p-tabindex]="getCellTabIndex('minute', i)"
              [p-label]="formatValue(minute)"
              [p-aria-label]="formatValue(minute) + ' ' + literals.minutes"
              [p-disabled]="disabledMinuteCache.has(minute)"
              [p-size]="size"
              [class.po-timer-display-selected]="selectedMinute === minute"
              [attr.data-aria-role]="'option'"
              [attr.data-aria-selected]="selectedMinute === minute ? 'true' : null"
              [attr.data-aria-setsize]="minutes.length"
              [attr.data-aria-posinset]="(i % minutes.length) + 1"
              (focus)="onCellFocus('minute', i)"
              (keydown)="onCellKeydown($event, 'minute')"
              (p-click)="onSelectMinute(minute); onCellFocus('minute', i)"
            >
            </po-button>
          }
        </div>
      </div>
    </div>

    <!-- Coluna de Segundos (condicional) -->
    @if (showSeconds) {
      <div
        #secondColumn
        class="po-timer-column"
        role="listbox"
        tabindex="-1"
        [attr.aria-label]="literals.seconds"
        [attr.aria-activedescendant]="activeDescendantIds.second"
        (wheel)="onColumnWheel($event, 'second')"
      >
        <div class="po-timer-column-scroll" #secondScroll>
          <div class="po-timer-column-items" #secondItems>
            @for (second of displaySeconds; track trackByIndex($index, second); let i = $index) {
              <po-button
                #secondCell
                class="po-timer-display"
                p-kind="tertiary"
                [attr.id]="'po-timer-second-' + i"
                [p-tabindex]="getCellTabIndex('second', i)"
                [p-label]="formatValue(second)"
                [p-aria-label]="formatValue(second) + ' ' + literals.seconds"
                [p-disabled]="disabledSecondCache.has(second)"
                [p-size]="size"
                [class.po-timer-display-selected]="selectedSecond === second"
                [attr.data-aria-role]="'option'"
                [attr.data-aria-selected]="selectedSecond === second ? 'true' : null"
                [attr.data-aria-setsize]="seconds.length"
                [attr.data-aria-posinset]="(i % seconds.length) + 1"
                (focus)="onCellFocus('second', i)"
                (keydown)="onCellKeydown($event, 'second')"
                (p-click)="onSelectSecond(second); onCellFocus('second', i)"
              >
              </po-button>
            }
          </div>
        </div>
      </div>
    }

    <!-- Coluna AM/PM (formato 12h) -->
    @if (is12HourFormat) {
      <div #periodColumn class="po-timer-column po-timer-column-period" role="radiogroup" aria-label="AM/PM">
        <div class="po-timer-column-scroll po-timer-period-scroll po-timer-column-items">
          <po-button
            #periodCell
            class="po-timer-display"
            p-kind="tertiary"
            p-label="AM"
            [p-size]="size"
            [p-tabindex]="getCellTabIndex('period', 0)"
            [p-disabled]="isPeriodDisabled('AM')"
            [class.po-timer-display-selected]="period === 'AM'"
            [attr.role]="'radio'"
            [attr.aria-checked]="period === 'AM'"
            (focus)="onCellFocus('period', 0)"
            (p-click)="onSelectPeriod('AM'); onCellFocus('period', 0)"
            (keydown)="onPeriodKeydown($event)"
          >
          </po-button>
          <po-button
            #periodCell
            class="po-timer-display"
            p-kind="tertiary"
            p-label="PM"
            [p-size]="size"
            [p-tabindex]="getCellTabIndex('period', 1)"
            [p-disabled]="isPeriodDisabled('PM')"
            [class.po-timer-display-selected]="period === 'PM'"
            [attr.role]="'radio'"
            [attr.aria-checked]="period === 'PM'"
            (focus)="onCellFocus('period', 1)"
            (p-click)="onSelectPeriod('PM'); onCellFocus('period', 1)"
            (keydown)="onPeriodKeydown($event)"
          >
          </po-button>
        </div>
      </div>
    }
  </div>
</div>
`
    }]
  }], () => [], {
    hourCells: [{
      type: ViewChildren,
      args: ["hourCell"]
    }],
    minuteCells: [{
      type: ViewChildren,
      args: ["minuteCell"]
    }],
    secondCells: [{
      type: ViewChildren,
      args: ["secondCell"]
    }],
    periodCells: [{
      type: ViewChildren,
      args: ["periodCell"]
    }],
    hourItemsRefs: [{
      type: ViewChildren,
      args: ["hourItems", {
        read: ElementRef
      }]
    }],
    minuteItemsRefs: [{
      type: ViewChildren,
      args: ["minuteItems", {
        read: ElementRef
      }]
    }],
    secondItemsRefs: [{
      type: ViewChildren,
      args: ["secondItems", {
        read: ElementRef
      }]
    }],
    boundaryTab: [{
      type: Output,
      args: ["p-boundary-tab"]
    }]
  });
})();
var PoTimerModule = class _PoTimerModule {
  static ɵfac = function PoTimerModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoTimerModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoTimerModule,
    declarations: [PoTimerComponent],
    imports: [CommonModule, PoButtonModule],
    exports: [PoTimerComponent]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [CommonModule, PoButtonModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoTimerModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, PoButtonModule],
      declarations: [PoTimerComponent],
      exports: [PoTimerComponent]
    }]
  }], null, null);
})();
var PoCalendarLangService = class _PoCalendarLangService {
  language = poLocaleDefault;
  months = [{
    pt: "Janeiro",
    en: "January",
    es: "Enero",
    ru: "Январь"
  }, {
    pt: "Fevereiro",
    en: "February",
    es: "Febrero",
    ru: "Февраль"
  }, {
    pt: "Março",
    en: "March",
    es: "Marzo",
    ru: "Март"
  }, {
    pt: "Abril",
    en: "April",
    es: "Abril",
    ru: "Апрель"
  }, {
    pt: "Maio",
    en: "May",
    es: "Mayo",
    ru: "Май"
  }, {
    pt: "Junho",
    en: "June",
    es: "Junio",
    ru: "Июнь"
  }, {
    pt: "Julho",
    en: "July",
    es: "Julio",
    ru: "Июль"
  }, {
    pt: "Agosto",
    en: "August",
    es: "Agosto",
    ru: "Август"
  }, {
    pt: "Setembro",
    en: "September",
    es: "Setiembre",
    ru: "Сентябрь"
  }, {
    pt: "Outubro",
    en: "October",
    es: "Octubre",
    ru: "Октябрь"
  }, {
    pt: "Novembro",
    en: "November",
    es: "Noviembre",
    ru: "Ноябрь"
  }, {
    pt: "Dezembro",
    en: "December",
    es: "Diciembre",
    ru: "Декабрь"
  }];
  shortWeekDays = [{
    pt: "Dom",
    en: "Sun",
    es: "Dom",
    ru: "Вс"
  }, {
    pt: "Seg",
    en: "Mon",
    es: "Lun",
    ru: "Пн"
  }, {
    pt: "Ter",
    en: "Tue",
    es: "Mar",
    ru: "Вт"
  }, {
    pt: "Qua",
    en: "Wed",
    es: "Mié",
    ru: "Ср"
  }, {
    pt: "Qui",
    en: "Thu",
    es: "Jue",
    ru: "Чт"
  }, {
    pt: "Sex",
    en: "Fri",
    es: "Vie",
    ru: "Пт"
  }, {
    pt: "Sáb",
    en: "Sat",
    es: "Sáb",
    ru: "Сб"
  }];
  monthLabel = {
    pt: "Mês",
    en: "Month",
    es: "Mes",
    ru: "Месяц"
  };
  yearLabel = {
    pt: "Ano",
    en: "Year",
    es: "Año",
    ru: "Год"
  };
  todayLabel = {
    pt: "Hoje",
    en: "Today",
    es: "Hoy",
    ru: "Сегодня"
  };
  previousMonthLabel = {
    pt: "Mês anterior",
    en: "Previous month",
    es: "Mes anterior",
    ru: "Предыдущий месяц"
  };
  nextMonthLabel = {
    pt: "Próximo mês",
    en: "Next month",
    es: "Próximo mes",
    ru: "Следующий месяц"
  };
  toCleanLabel = {
    pt: "Limpar",
    en: "To clean",
    es: "Para limpiar",
    ru: "Чистить"
  };
  presetLabels = {
    tomorrow: {
      pt: "Amanhã",
      en: "Tomorrow",
      es: "Mañana",
      ru: "Завтра"
    },
    today: {
      pt: "Hoje",
      en: "Today",
      es: "Hoy",
      ru: "Сегодня"
    },
    yesterday: {
      pt: "Ontem",
      en: "Yesterday",
      es: "Ayer",
      ru: "Вчера"
    },
    "7days": {
      pt: "Últimos 7 dias",
      en: "Last 7 days",
      es: "Últimos 7 días",
      ru: "Последние 7 дней"
    },
    "14days": {
      pt: "Últimos 14 dias",
      en: "Last 14 days",
      es: "Últimos 14 días",
      ru: "Последние 14 дней"
    },
    "30days": {
      pt: "Últimos 30 dias",
      en: "Last 30 days",
      es: "Últimos 30 días",
      ru: "Последние 30 дней"
    },
    "3months": {
      pt: "Últimos 3 meses",
      en: "Last 3 months",
      es: "Últimos 3 meses",
      ru: "Последние 3 месяцев"
    },
    "6months": {
      pt: "Últimos 6 meses",
      en: "Last 6 months",
      es: "Últimos 6 meses",
      ru: "Последние 6 месяцев"
    }
  };
  getMonth(month) {
    return this.months[month][this.language];
  }
  getMonthLabel() {
    return this.monthLabel[this.language];
  }
  getMonthsArray() {
    const arrMonths = Array();
    for (let i2 = 0; i2 < this.months.length; i2++) {
      arrMonths.push(this.months[i2][this.language]);
    }
    return arrMonths;
  }
  getWeekDays(day) {
    return this.shortWeekDays[day][this.language];
  }
  getWeekDaysArray() {
    const arrWeekDays = Array();
    for (let i2 = 0; i2 < this.shortWeekDays.length; i2++) {
      const weekDay = this.shortWeekDays[i2][this.language];
      arrWeekDays.push(weekDay.toLowerCase());
    }
    return arrWeekDays;
  }
  getYearLabel() {
    return this.yearLabel[this.language];
  }
  getTodayLabel() {
    return this.todayLabel[this.language];
  }
  getPreviousMonthLabel() {
    return this.previousMonthLabel[this.language];
  }
  getNextMonthLabel() {
    return this.nextMonthLabel[this.language];
  }
  getToCleanLabel() {
    return this.toCleanLabel[this.language];
  }
  getPresetLabel(key2) {
    const labels = this.presetLabels[key2];
    return labels ? labels[this.language] : key2;
  }
  setLanguage(language) {
    if (language && language.length >= 2) {
      language = language.toLowerCase().slice(0, 2);
      this.language = poLocales.includes(language) ? language : poLocaleDefault;
    }
  }
  static ɵfac = function PoCalendarLangService_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoCalendarLangService)();
  };
  static ɵprov = ɵɵdefineInjectable({
    token: _PoCalendarLangService,
    factory: _PoCalendarLangService.ɵfac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoCalendarLangService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
var poCalendarServiceFirstWeekDayDefault = 0;
var PoCalendarService = class _PoCalendarService {
  parseDate(dateValue) {
    if (!dateValue) return void 0;
    if (typeof dateValue === "string") {
      const dateOnlyRegex = /^(\d{4})-(\d{2})-(\d{2})$/;
      if (dateOnlyRegex.test(dateValue)) {
        const [, year, month, day] = dateValue.match(dateOnlyRegex).map(Number);
        return new Date(year, month - 1, day);
      }
      const parsed = new Date(dateValue);
      if (!isNaN(parsed.getTime())) {
        dateValue = parsed;
      } else {
        return void 0;
      }
    }
    if (dateValue instanceof Date) {
      if (dateValue.getHours() !== 0 && dateValue.getUTCHours() === 0 && dateValue.getUTCMinutes() === 0) {
        return new Date(dateValue.getUTCFullYear(), dateValue.getUTCMonth(), dateValue.getUTCDate());
      }
      return new Date(dateValue.getFullYear(), dateValue.getMonth(), dateValue.getDate());
    }
    return void 0;
  }
  getYearOptions(minDate, maxDate) {
    const currentYear = (/* @__PURE__ */ new Date()).getFullYear();
    let minYear = currentYear - 150;
    let maxYear = currentYear + 150;
    const parsedMinDate = this.parseDate(minDate);
    if (parsedMinDate) {
      minYear = parsedMinDate.getFullYear();
    }
    const parsedMaxDate = this.parseDate(maxDate);
    if (parsedMaxDate) {
      maxYear = parsedMaxDate.getFullYear();
    }
    const options = [];
    for (let i2 = minYear; i2 <= maxYear; i2++) {
      options.push({
        label: i2.toString(),
        value: i2
      });
    }
    return options;
  }
  monthDates(year, month, dayFormatter = null, weekFormatter = null) {
    if (typeof month !== "number" || month < 0 || month > 11) {
      throw Error("month must be a number (Jan is 0)");
    }
    const weeks = [];
    let week = [];
    let i2 = 0;
    let date = new Date(year, month, 1);
    if (year >= 0 && year < 100) {
      date.setFullYear(year);
    }
    date = this.weekStartDate(date);
    do {
      for (i2 = 0; i2 < 7; i2++) {
        week.push(dayFormatter ? dayFormatter(date) : date);
        date = new Date(date.getTime());
        date.setDate(date.getDate() + 1);
      }
      weeks.push(weekFormatter ? weekFormatter(week) : week);
      week = [];
    } while (date.getMonth() <= month && date.getFullYear() === year);
    return weeks;
  }
  monthDays(year, month) {
    const getDayObject = function(date) {
      return date;
    };
    return this.monthDates(year, month, getDayObject);
  }
  weekStartDate(date) {
    const startDate = new Date(date.getTime());
    while (startDate.getDay() !== poCalendarServiceFirstWeekDayDefault) {
      startDate.setDate(startDate.getDate() - 1);
    }
    return startDate;
  }
  isToday(date) {
    if (!date || !(date instanceof Date)) {
      return false;
    }
    const today = /* @__PURE__ */ new Date();
    return date.getDate() === today.getDate() && date.getMonth() === today.getMonth() && date.getFullYear() === today.getFullYear();
  }
  static ɵfac = function PoCalendarService_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoCalendarService)();
  };
  static ɵprov = ɵɵdefineInjectable({
    token: _PoCalendarService,
    factory: _PoCalendarService.ɵfac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoCalendarService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
var PoCalendarMode;
(function(PoCalendarMode2) {
  PoCalendarMode2["Range"] = "range";
  PoCalendarMode2["MonthYear"] = "month-year";
  PoCalendarMode2["Year"] = "year";
  PoCalendarMode2["DateTime"] = "date-time";
})(PoCalendarMode || (PoCalendarMode = {}));
var PoDateService = class _PoDateService {
  dateRegex = new RegExp("^(?:[0-9])\\d{1}(?:[0-9])\\d{1}-(?:0[1-9]|1[0-2])-(?:0[1-9]|[12]\\d|3[01])$");
  isoRegex = new RegExp("^(?:[0-9])\\d{1}(?:[0-9])\\d{1}-(?:0[1-9]|1[0-2])-(?:0[1-9]|[12]\\d|3[01])T(?:[01]\\d|2[0-3]):[0-5]\\d:[0-5]\\d(?:Z|-0[1-9]|-1\\d|-2[0-3]|-00:?(?:0[1-9]|[0-5]\\d)|\\+[01]\\d|\\+2[0-3])(?:|:?[0-5]\\d)$");
  /**
   * Método responsável por converter datas do formato `yyyy-mm-ddThh:mm:ss+|-hh:mm` para o formato `Date`.
   *
   * @param dateString Data no formato `yyyy-mm-ddThh:mm:ss+|-hh:mm`.
   * @param minDate Definir `true` caso seja `minDate`.
   * @param maxDate Definir `true` caso seja `maxDate`.
   */
  convertIsoToDate(dateString, minDate, maxDate) {
    if (dateString) {
      const {
        year,
        month,
        day
      } = this.getDateFromIso(dateString);
      if (minDate) {
        const date = new Date(year, month - 1, day, 0, 0, 0);
        this.setYearFrom0To100(date, year);
        return date;
      } else if (maxDate) {
        const date = new Date(year, month - 1, day, 23, 59, 59);
        this.setYearFrom0To100(date, year);
        return date;
      } else {
        const miliseconds = Date.parse(dateString);
        const timezone = (/* @__PURE__ */ new Date()).getTimezoneOffset() * 6e4;
        return new Date(miliseconds + timezone);
      }
    }
  }
  /**
   * Método responsável por converter data do formato `Date` para o formato `yyyy-mm-dd`.
   *
   * @param date Data no formato `Date`.
   */
  convertDateToISO(date) {
    if (date) {
      const fullYear = date.getFullYear();
      const getMonth = date.getMonth() + 1;
      const day = date.getDate() < 10 ? "0" + date.getDate() : date.getDate();
      const month = getMonth < 10 ? "0" + getMonth : getMonth;
      const year = this.formatYear(fullYear);
      return year + "-" + month + "-" + day;
    } else {
      return null;
    }
  }
  /**
   * Método responsável por adicionar zeros a esquerda do anos em formato string.
   *
   * @param year Ano a ser validado.
   */
  formatYear(year) {
    if (year > 999) {
      return year.toString();
    }
    if (year > 99 && year < 1e3) {
      return `0${year}`;
    }
    if (year > 9 && year < 100) {
      return `00${year}`;
    }
    if (year >= 0 && year < 10) {
      return `000${year}`;
    }
  }
  /**
   * Método responsável por retornar o dia, mês e ano separados em formato de objeto.
   *
   * @param isoDate Ano em formato string.
   */
  getDateFromIso(isoDate) {
    const day = parseInt(isoDate.substring(8, 10), 10);
    const month = parseInt(isoDate.substring(5, 7), 10);
    const year = parseInt(isoDate.substring(0, 4), 10);
    return {
      year,
      month,
      day
    };
  }
  /**
   * Método responsável por retornar a data com a hora definida para `00:00:00` caso `isMinDate` for igual a `true` ou `23:59:59`
   * caso `isMindate` seja igual a `false` .
   *
   * @param date Data no formato `Date` ou `yyyy-mm-ddThh:mm:ss+|-hh:mm`.
   * @param isMinDate Caso `true` aplica `00:00:00`, caso `false` aplica `23:59:59` a hora da data informada.
   */
  getDateForDateRange(date, isMinDate) {
    const lastHour = isMinDate ? [0, 0, 0] : [23, 59, 59];
    if (date instanceof Date) {
      const {
        year,
        month,
        day
      } = this.splitDate(date);
      const validDate = new Date(year, month, day, ...lastHour);
      this.setYearFrom0To100(validDate, year);
      return validDate;
    } else if (this.isValidIso(date)) {
      return this.convertIsoToDate(date, isMinDate, !isMinDate);
    }
  }
  /**
   * Retorna `true` caso o período seja válido, para isso a primeira data deve ser maior que a segunda data.
   *
   * @param dateA primeira data
   * @param dateB segunda data
   */
  isDateRangeValid(dateA = "", dateB = "") {
    const dateASplitted = dateA.split("-").map((item) => parseInt(item, 10));
    const dateBSplitted = dateB.split("-").map((item) => parseInt(item, 10));
    for (let index = 0; index <= dateASplitted.length; index++) {
      if (dateASplitted[index] > dateBSplitted[index]) {
        return true;
      } else if (dateASplitted[index] < dateBSplitted[index]) {
        return false;
      }
    }
    return true;
  }
  /**
   * Método responsável por validar se uma data está no formato `yyyy-mm-ddThh:mm:ss+|-hh:mm` ou `yyyy-mm-dd`.
   *
   * @param stringDate Data.
   */
  isValidIso(stringDate) {
    return this.dateRegex.test(stringDate) || this.isoRegex.test(stringDate);
  }
  /**
   * Método responsável por corrigir a data caso a mesma esteja entre os anos 0 e 99.
   *
   * @param date Data.
   * @param year .
   */
  setYearFrom0To100(date, year) {
    if (year >= 0 && year < 100) {
      date.setFullYear(year);
    }
  }
  /**
   * Método responsável por retornar o dia , mês e ano de uma data informada.
   *
   * @param date Valor da data.
   */
  splitDate(date) {
    const year = date.getFullYear();
    const month = date.getMonth();
    const day = date.getDate();
    return {
      year,
      month,
      day
    };
  }
  /**
   * Método responsável por validar se uma data está entre a `minDate` e `maxDate`.
   *
   * @param date Data a ser validada.
   * @param minDate Data inicial.
   * @param maxDate Data final.
   */
  validateDateRange(date, minDate, maxDate) {
    if (minDate && maxDate) {
      return date >= minDate && date <= maxDate;
    } else if (minDate && !maxDate) {
      return date >= minDate;
    } else if (!minDate && maxDate) {
      return date <= maxDate;
    } else {
      return true;
    }
  }
  /**
   * Método responsável por validar se a data foi informada nos padrões 'yyyy-mm-dd', 'yyyy-mm-ddThh:mm:ss+|-hh:mm' ou
   * 'Date' padrão do javascript.
   *
   * @param date Data que será validada.
   */
  validateDate(date) {
    const validDate = date instanceof Date ? this.convertDateToISO(date) : date;
    return this.isValidIso(validDate) ? validDate : void 0;
  }
  static ɵfac = function PoDateService_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoDateService)();
  };
  static ɵprov = ɵɵdefineInjectable({
    token: _PoDateService,
    factory: _PoDateService.ɵfac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoDateService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
var PoDateTimeModule = class _PoDateTimeModule {
  static ɵfac = function PoDateTimeModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoDateTimeModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoDateTimeModule
  });
  static ɵinj = ɵɵdefineInjector({
    providers: [PoDateService]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoDateTimeModule, [{
    type: NgModule,
    args: [{
      providers: [PoDateService],
      bootstrap: []
    }]
  }], null, null);
})();
var PoCalendarBaseComponent = class _PoCalendarBaseComponent {
  poDate;
  languageService;
  // --- Propriedades ---
  activateDate;
  selectedPresetLabel = null;
  value;
  onTouched = null;
  propagateChange = null;
  today = /* @__PURE__ */ new Date();
  shortLanguage;
  _locale;
  _maxDate;
  _minDate;
  _mode;
  _rangePresets = false;
  _size;
  _initialSize;
  // --- Inputs ---
  // Define o formato de exibição do timer quando o calendário está no modo `date-time`.
  format = PoTimerFormat.Format24;
  // Propriedade que permite integrar o po-combo no componente de calendar. Implementa o template de header com `PoCombo`.
  headerTemplate;
  /**
   * @optional
   *
   * @description
   *
   * Idioma do componente.
   *
   * > O locale padrão será recuperado com base no [`PoI18nService`](/documentation/po-i18n) ou *browser*.
   */
  set locale(locale) {
    this._locale = poLocales.includes(locale) ? locale : this.shortLanguage;
  }
  get locale() {
    return this._locale;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define a data máxima permitida para seleção. As datas posteriores ao limite definido permanecem visíveis, mas ficam
   * desabilitadas para clique.
   *
   * Aceita os seguintes formatos:
   *
   * - **Data e hora combinados (E8601DZw): yyyy-mm-ddThh:mm:ss+|-hh:mm**
   *   ```
   *   this.date = '2017-11-28T00:00:00-02:00';
   *   ```
   *
   * - **Data (E8601DAw.): yyyy-mm-dd**
   *   ```
   *   this.date = '2017-11-28';
   *   ```
   *
   * - **JavaScript Date Object:**
   *   ```
   *   this.date = new Date(2017, 10, 28);
   *   ```
   */
  set maxDate(maxDate) {
    this._maxDate = this.poDate.getDateForDateRange(maxDate, false);
  }
  get maxDate() {
    return this._maxDate;
  }
  // Define o horário máximo permitido para seleção no timer quando o calendário está no modo `date-time`.
  maxTime;
  /**
   * @optional
   *
   * @description
   *
   * Define a data mínima para seleção. As datas anteriores ao limite permanecem visíveis, mas desabilitadas para clique.
   *
   * Aceita os seguintes formatos:
   *
   * - **Data e hora combinados (E8601DZw): yyyy-mm-ddThh:mm:ss+|-hh:mm**
   *   ```
   *   this.date = '2017-11-28T00:00:00-02:00';
   *   ```
   *
   * - **Data (E8601DAw.): yyyy-mm-dd**
   *   ```
   *   this.date = '2017-11-28';
   *   ```
   *
   * - **JavaScript Date Object:**
   *   ```
   *   this.date = new Date(2017, 10, 28);
   *   ```
   */
  set minDate(minDate) {
    this._minDate = this.poDate.getDateForDateRange(minDate, true);
  }
  get minDate() {
    return this._minDate;
  }
  // Define o horário mínimo permitido para seleção no timer quando o calendário está no modo `date-time`.
  minTime;
  // Define o intervalo entre os minutos exibidos no painel do timer quando o calendário está no modo `date-time`.
  minuteInterval;
  /**
   * @optional
   *
   * @description
   *
   * Define o modo de exibição do calendário.
   * Caso não seja informado, o componente operará no modo padrão de seleção única.
   * Para permitir a seleção de um intervalo de datas, deve ser utilizado o valor `PoCalendarMode.Range`.
   */
  set mode(value) {
    this._mode = value;
    this.setActivateDate();
  }
  get mode() {
    return this._mode;
  }
  /**
   * @optional
   *
   * @description
   *
   * Lista de presets customizados de intervalos de data exibidos no painel lateral do calendário em modo *range*.
   *
   * Quando informado sem `p-range-presets`, exibe apenas os presets customizados.
   * Quando informado junto com `p-range-presets` habilitado, os presets customizados serão exibidos junto aos presets padrão,
   * ordenados automaticamente por temporalidade com base na data início (Futuro → Presente → Passado).
   *
   * Para utilizar presets customizados, informe um array de objetos que implementam a interface `PoCalendarRangePreset`.
   */
  rangePresetOptions;
  /**
   * @optional
   *
   * @description
   *
   * Habilita a exibição dos presets padrão de intervalos de data no painel lateral do calendário em modo *range*.
   *
   * Aceita os seguintes valores:
   * - `true`: exibe todos os presets padrão (Amanhã, Hoje, Ontem, Últimos 7 dias, Últimos 14 dias, Últimos 30 dias, Últimos 3 meses, Últimos 6 meses).
   * - `false`: não exibe os presets padrão.
   * - `Array<string>`: exibe apenas os presets padrão cujos labels estejam no array informado.
   *   O preset "today" é obrigatório e será incluído automaticamente mesmo que não esteja no array.
   *
   * Caso `p-range-preset-options` também seja informado, os presets customizados serão exibidos junto aos presets padrão,
   * ordenados automaticamente por temporalidade com base na data início (Futuro → Presente → Passado).
   *
   * @default `false`
   */
  set rangePresets(value) {
    if (Array.isArray(value)) {
      this._rangePresets = value;
    } else if (value === "" || value === true || value === "true") {
      this._rangePresets = true;
    } else {
      this._rangePresets = false;
    }
  }
  get rangePresets() {
    return this._rangePresets;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define a ordenação dos presets na lista.
   *
   * Valores aceitos:
   * - `'asc'` (padrão): presets mais próximos de hoje aparecem primeiro.
   * - `'desc'`: presets mais distantes de hoje aparecem primeiro.
   *
   * A ordenação é aplicada dentro de cada grupo de temporalidade com base na data início (Futuro → Presente → Passado).
   *
   * @default `asc`
   */
  rangePresetsOrder = "asc";
  // Define o intervalo entre os segundos exibidos no painel do timer quando o calendário está no modo `date-time`.
  // Utilizado apenas quando `p-show-seconds` está ativo.
  secondInterval;
  // Exibe a coluna de segundos no painel de seleção do timer quando o calendário está no modo `date-time`.
  showSeconds = false;
  /**
   * @optional
   *
   * @description
   *
   * Define o tamanho do componente.
   *
   * > Caso a acessibilidade AA não esteja configurada, o tamanho `medium` será mantido.
   * Para mais detalhes, consulte a documentação do [po-theme](https://po-ui.io/documentation/po-theme).
   *
   * @default `medium`
   */
  set size(value) {
    this._initialSize = value;
    this.applySizeBasedOnA11y();
  }
  get size() {
    return this._size ?? getDefaultSizeFn(PoFieldSize);
  }
  // Propriedade interna responsável por controlar a visibilidade do calendário
  // no po-datepicker-range, especialmente quando presets estão configurados.
  widthWithPresets;
  //  Propriedade interna utilizada pelo po-datepicker.
  //  Define o limite de anos exibidos nas variações `month-year` e `year`,
  //  considerando a data atual como referência.
  //
  //  O valor informado determina o intervalo de anos anterior e posterior
  //  à data corrente que será disponibilizado para seleção.
  yearRangeLimit = 150;
  // --- Outputs ---
  /**
   * @optional
   *
   * @description
   *
   * Evento disparado ao alterar o valor do model.
   * Em modo padrão, retorna uma `string` no formato ISO 8601 (`yyyy-mm-dd`).
   * Em modo *range*, retorna um objeto contendo as datas de início (`start`) e fim (`end`).
   */
  change = new EventEmitter();
  /**
   * @description
   *
   * Evento disparado ao alterar o mês ou o ano no cabeçalho do calendário.
   * Retorna um objeto contendo os valores numéricos:
   * - `month`: Mês selecionado (variando de 1 a 12).
   * - `year`: Ano selecionado.
   */
  changeMonthYear = new EventEmitter();
  // Evento disparado ao selecionar um horário no timer quando o calendário está no modo `date-time`.
  // Retorna uma `string` no formato ISO 8601 (`HH:mm` ou `HH:mm:ss`).
  changeTime = new EventEmitter();
  // Evento disparado ao fechar o calendário, seja por seleção de data ou por clique fora do componente.
  close = new EventEmitter();
  // Evento disparado quando Tab/ShiftTab atinge a última coluna do timer no modo date-time.
  timerBoundaryTab = new EventEmitter();
  // --- Getters derivados do mode ---
  get isDateTime() {
    return this.mode === PoCalendarMode.DateTime;
  }
  get isRange() {
    return this.mode === PoCalendarMode.Range;
  }
  get isMonthYear() {
    return this.mode === PoCalendarMode.MonthYear;
  }
  get isYear() {
    return this.mode === PoCalendarMode.Year;
  }
  // --- Constructor ---
  constructor(poDate, languageService) {
    this.poDate = poDate;
    this.languageService = languageService;
    this.shortLanguage = languageService.getShortLanguage();
    this._locale = this.languageService.getShortLanguage();
  }
  // --- Métodos ---
  onThemeChange() {
    this.applySizeBasedOnA11y();
  }
  setActivateDate(date) {
    let newData;
    if (typeof date !== "string") {
      const temporaryDate = new Date(date);
      const year = temporaryDate.getFullYear();
      const month = ("0" + (temporaryDate.getMonth() + 1)).slice(-2);
      const day = ("0" + temporaryDate.getDate()).slice(-2);
      const formattedDate = `${year}-${month}-${day}`;
      newData = formattedDate + "T00:00:00";
    } else {
      newData = date + "T00:00:00";
    }
    const activateDate = date ? date : this.verifyActivateDate();
    let checkedStart;
    let checkedEnd;
    if (this.isRange) {
      if (new Date(newData).getDate() > 28) {
        checkedStart = new Date(activateDate);
        checkedEnd = new Date(checkedStart.getFullYear(), checkedStart.getMonth() + 1, 0, 23, 59, 59, 999);
        checkedEnd.setMilliseconds(checkedEnd.getMilliseconds() + 1);
      } else {
        checkedStart = typeof activateDate === "string" ? this.poDate.convertIsoToDate(activateDate) : new Date(activateDate);
        checkedEnd = new Date(new Date(checkedStart).setMonth(checkedStart.getMonth() + 1));
      }
      this.activateDate = {
        start: checkedStart,
        end: checkedEnd
      };
    } else {
      this.activateDate = new Date(activateDate);
    }
  }
  verifyActivateDate() {
    let today = this.today;
    if (this.minDate && this.minDate > this.today) {
      today = this.minDate;
    } else if (this.maxDate && this.maxDate < this.today) {
      today = this.maxDate;
    }
    return today;
  }
  applySizeBasedOnA11y() {
    const size = validateSizeFn(this._initialSize, PoFieldSize);
    this._size = size;
  }
  static ɵfac = function PoCalendarBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoCalendarBaseComponent)(ɵɵdirectiveInject(PoDateService), ɵɵdirectiveInject(PoLanguageService));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoCalendarBaseComponent,
    hostVars: 1,
    hostBindings: function PoCalendarBaseComponent_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("PoUiThemeChange", function PoCalendarBaseComponent_PoUiThemeChange_HostBindingHandler() {
          return ctx.onThemeChange();
        }, ɵɵresolveWindow);
      }
      if (rf & 2) {
        ɵɵattribute("p-size", ctx.size);
      }
    },
    inputs: {
      format: [0, "p-format", "format"],
      headerTemplate: [0, "p-header-template", "headerTemplate"],
      locale: [0, "p-locale", "locale"],
      maxDate: [0, "p-max-date", "maxDate"],
      maxTime: [0, "p-max-time", "maxTime"],
      minDate: [0, "p-min-date", "minDate"],
      minTime: [0, "p-min-time", "minTime"],
      minuteInterval: [0, "p-minute-interval", "minuteInterval"],
      mode: [0, "p-mode", "mode"],
      rangePresetOptions: [0, "p-range-preset-options", "rangePresetOptions"],
      rangePresets: [0, "p-range-presets", "rangePresets"],
      rangePresetsOrder: [0, "p-range-presets-order", "rangePresetsOrder"],
      secondInterval: [0, "p-second-interval", "secondInterval"],
      showSeconds: [0, "p-show-seconds", "showSeconds"],
      size: [0, "p-size", "size"],
      widthWithPresets: [0, "p-width-with-presets", "widthWithPresets"],
      yearRangeLimit: [0, "p-year-range-limit", "yearRangeLimit"]
    },
    outputs: {
      change: "p-change",
      changeMonthYear: "p-change-month-year",
      changeTime: "p-change-time",
      close: "p-close",
      timerBoundaryTab: "p-timer-boundary-tab"
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoCalendarBaseComponent, [{
    type: Directive
  }], () => [{
    type: PoDateService
  }, {
    type: PoLanguageService
  }], {
    format: [{
      type: Input,
      args: ["p-format"]
    }],
    headerTemplate: [{
      type: Input,
      args: ["p-header-template"]
    }],
    locale: [{
      type: Input,
      args: ["p-locale"]
    }],
    maxDate: [{
      type: Input,
      args: ["p-max-date"]
    }],
    maxTime: [{
      type: Input,
      args: ["p-max-time"]
    }],
    minDate: [{
      type: Input,
      args: ["p-min-date"]
    }],
    minTime: [{
      type: Input,
      args: ["p-min-time"]
    }],
    minuteInterval: [{
      type: Input,
      args: ["p-minute-interval"]
    }],
    mode: [{
      type: Input,
      args: ["p-mode"]
    }],
    rangePresetOptions: [{
      type: Input,
      args: ["p-range-preset-options"]
    }],
    rangePresets: [{
      type: Input,
      args: ["p-range-presets"]
    }],
    rangePresetsOrder: [{
      type: Input,
      args: ["p-range-presets-order"]
    }],
    secondInterval: [{
      type: Input,
      args: ["p-second-interval"]
    }],
    showSeconds: [{
      type: Input,
      args: ["p-show-seconds"]
    }],
    size: [{
      type: Input,
      args: ["p-size"]
    }, {
      type: HostBinding,
      args: ["attr.p-size"]
    }],
    widthWithPresets: [{
      type: Input,
      args: ["p-width-with-presets"]
    }],
    yearRangeLimit: [{
      type: Input,
      args: ["p-year-range-limit"]
    }],
    change: [{
      type: Output,
      args: ["p-change"]
    }],
    changeMonthYear: [{
      type: Output,
      args: ["p-change-month-year"]
    }],
    changeTime: [{
      type: Output,
      args: ["p-change-time"]
    }],
    close: [{
      type: Output,
      args: ["p-close"]
    }],
    timerBoundaryTab: [{
      type: Output,
      args: ["p-timer-boundary-tab"]
    }],
    onThemeChange: [{
      type: HostListener,
      args: ["window:PoUiThemeChange"]
    }]
  });
})();
function startOfDay(date) {
  const d = new Date(date);
  d.setHours(0, 0, 0, 0);
  return d;
}
function endOfDay(date) {
  const d = new Date(date);
  d.setHours(23, 59, 59, 999);
  return d;
}
var PO_CALENDAR_DEFAULT_RANGE_PRESETS = [{
  label: "tomorrow",
  dateRange: (today) => {
    const tomorrow = new Date(today);
    tomorrow.setDate(tomorrow.getDate() + 1);
    return {
      start: startOfDay(tomorrow),
      end: endOfDay(tomorrow)
    };
  }
}, {
  label: "today",
  dateRange: (today) => ({
    start: startOfDay(today),
    end: endOfDay(today)
  })
}, {
  label: "yesterday",
  dateRange: (today) => {
    const yesterday = new Date(today);
    yesterday.setDate(yesterday.getDate() - 1);
    return {
      start: startOfDay(yesterday),
      end: endOfDay(yesterday)
    };
  }
}, {
  label: "7days",
  dateRange: (today) => {
    const start2 = new Date(today);
    start2.setDate(start2.getDate() - 6);
    return {
      start: startOfDay(start2),
      end: endOfDay(today)
    };
  }
}, {
  label: "14days",
  dateRange: (today) => {
    const start2 = new Date(today);
    start2.setDate(start2.getDate() - 13);
    return {
      start: startOfDay(start2),
      end: endOfDay(today)
    };
  }
}, {
  label: "30days",
  dateRange: (today) => {
    const start2 = new Date(today);
    start2.setDate(start2.getDate() - 29);
    return {
      start: startOfDay(start2),
      end: endOfDay(today)
    };
  }
}, {
  label: "3months",
  dateRange: (today) => {
    const start2 = new Date(today);
    start2.setMonth(start2.getMonth() - 3);
    return {
      start: startOfDay(start2),
      end: endOfDay(today)
    };
  }
}, {
  label: "6months",
  dateRange: (today) => {
    const start2 = new Date(today);
    start2.setMonth(start2.getMonth() - 6);
    return {
      start: startOfDay(start2),
      end: endOfDay(today)
    };
  }
}];
var PoCalendarFooterComponent = class _PoCalendarFooterComponent {
  size;
  labelClear;
  labelToday;
  responsive = false;
  todayDisabled = false;
  hideTodayButton = false;
  clear = new EventEmitter();
  selectToday = new EventEmitter();
  closeCalendar = new EventEmitter();
  onClear() {
    this.clear.emit();
  }
  onSelectToday() {
    this.selectToday.emit();
  }
  onClearKeydown(event) {
    if (this.hideTodayButton && event.key === "Tab" && !event.shiftKey) {
      this.closeCalendar.emit();
    }
  }
  onTodayKeydown(event) {
    if (event.key === "Tab" && !event.shiftKey) {
      this.closeCalendar.emit();
    }
  }
  static ɵfac = function PoCalendarFooterComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoCalendarFooterComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoCalendarFooterComponent,
    selectors: [["po-calendar-footer"]],
    inputs: {
      size: [0, "p-size", "size"],
      labelClear: [0, "p-label-clear", "labelClear"],
      labelToday: [0, "p-label-today", "labelToday"],
      responsive: [0, "p-responsive", "responsive"],
      todayDisabled: [0, "p-today-disabled", "todayDisabled"],
      hideTodayButton: [0, "p-hide-today-button", "hideTodayButton"]
    },
    outputs: {
      clear: "p-clear",
      selectToday: "p-select-today",
      closeCalendar: "p-close-calendar"
    },
    standalone: false,
    decls: 3,
    vars: 3,
    consts: [[1, "po-calendar-footer"], ["p-kind", "tertiary", "type", "button", 3, "click", "keydown.enter", "keydown.space", "keydown", "p-label", "p-size"], ["p-kind", "tertiary", "type", "button", 3, "p-label", "p-disabled", "p-size"], ["p-kind", "tertiary", "type", "button", 3, "click", "keydown", "p-label", "p-disabled", "p-size"]],
    template: function PoCalendarFooterComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "div", 0)(1, "po-button", 1);
        ɵɵlistener("click", function PoCalendarFooterComponent_Template_po_button_click_1_listener() {
          return ctx.onClear();
        })("keydown.enter", function PoCalendarFooterComponent_Template_po_button_keydown_enter_1_listener() {
          return ctx.onClear();
        })("keydown.space", function PoCalendarFooterComponent_Template_po_button_keydown_space_1_listener() {
          return ctx.onClear();
        })("keydown", function PoCalendarFooterComponent_Template_po_button_keydown_1_listener($event) {
          return ctx.onClearKeydown($event);
        });
        ɵɵelementEnd();
        ɵɵconditionalCreate(2, PoCalendarFooterComponent_Conditional_2_Template, 1, 3, "po-button", 2);
        ɵɵelementEnd();
      }
      if (rf & 2) {
        ɵɵadvance();
        ɵɵproperty("p-label", ctx.labelClear)("p-size", ctx.size);
        ɵɵadvance();
        ɵɵconditional(!ctx.hideTodayButton ? 2 : -1);
      }
    },
    dependencies: [PoButtonComponent],
    encapsulation: 2,
    changeDetection: 0
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoCalendarFooterComponent, [{
    type: Component,
    args: [{
      selector: "po-calendar-footer",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: '<div class="po-calendar-footer">\n  <po-button\n    p-kind="tertiary"\n    type="button"\n    [p-label]="labelClear"\n    [p-size]="size"\n    (click)="onClear()"\n    (keydown.enter)="onClear()"\n    (keydown.space)="onClear()"\n    (keydown)="onClearKeydown($event)"\n  >\n  </po-button>\n\n  @if (!hideTodayButton) {\n    <po-button\n      p-kind="tertiary"\n      type="button"\n      [p-label]="labelToday"\n      [p-disabled]="todayDisabled"\n      [p-size]="size"\n      (click)="onSelectToday()"\n      (keydown)="onTodayKeydown($event)"\n    >\n    </po-button>\n  }\n</div>\n'
    }]
  }], null, {
    size: [{
      type: Input,
      args: ["p-size"]
    }],
    labelClear: [{
      type: Input,
      args: ["p-label-clear"]
    }],
    labelToday: [{
      type: Input,
      args: ["p-label-today"]
    }],
    responsive: [{
      type: Input,
      args: ["p-responsive"]
    }],
    todayDisabled: [{
      type: Input,
      args: ["p-today-disabled"]
    }],
    hideTodayButton: [{
      type: Input,
      args: ["p-hide-today-button"]
    }],
    clear: [{
      type: Output,
      args: ["p-clear"]
    }],
    selectToday: [{
      type: Output,
      args: ["p-select-today"]
    }],
    closeCalendar: [{
      type: Output,
      args: ["p-close-calendar"]
    }]
  });
})();
function requiredFailed(required, disabled, value) {
  const valid = typeof value === "string" && value || typeof value === "object" && value && value.length || typeof value === "number" && (value || value === 0) || typeof value === "boolean" && value;
  return required && !disabled && !valid;
}
function maxlengpoailed(maxlength, value, maskNoLengthValidation = false) {
  return validateLength(maxlength, value, "max", maskNoLengthValidation);
}
function minlengpoailed(minlength, value, maskNoLengthValidation = false) {
  return validateLength(minlength, value, "min", maskNoLengthValidation);
}
function validateLength(limit, value, comparison, maskNoLengthValidation = false) {
  if (!limit && limit !== 0) {
    return false;
  }
  const validValue2 = (value || value === 0) && value.toString();
  if (!validValue2) {
    return false;
  }
  const processedValue = maskNoLengthValidation ? validValue2.replace(/[^\w]/g, "") : validValue2;
  if (comparison === "max") {
    return processedValue.length > Number(limit);
  } else if (comparison === "min") {
    return processedValue.length < Number(limit);
  }
  return false;
}
function patternFailed(pattern, value) {
  let reg;
  try {
    reg = new RegExp(pattern);
  } catch (e2) {
    return true;
  }
  return pattern && value && !reg.test(value);
}
function minFailed(min3, value) {
  const validValue2 = value || value === 0;
  const validMin = min3 || min3 === 0;
  return validValue2 && validMin && value < min3;
}
function maxFailed(max3, value) {
  const validValue2 = value || value === 0;
  const validMax = max3 || max3 === 0;
  return validValue2 && validMax && value > max3;
}
function dateFailed(value) {
  return value && isNaN(Date.parse(value));
}
var PoValidators = {
  maxlengpoailed,
  minlengpoailed,
  requiredFailed
};
var PoComboFilterMode;
(function(PoComboFilterMode2) {
  PoComboFilterMode2[PoComboFilterMode2["startsWith"] = 0] = "startsWith";
  PoComboFilterMode2[PoComboFilterMode2["contains"] = 1] = "contains";
  PoComboFilterMode2[PoComboFilterMode2["endsWith"] = 2] = "endsWith";
})(PoComboFilterMode || (PoComboFilterMode = {}));
var poComboLiteralsDefault = {
  en: {
    noData: "No data found",
    chooseOption: "Choose an option",
    clear: "Clear"
  },
  es: {
    noData: "Datos no encontrados",
    chooseOption: "Elija una opción",
    clear: "limpia"
  },
  pt: {
    noData: "Nenhum dado encontrado",
    chooseOption: "Escolha uma opção",
    clear: "Apagar"
  },
  ru: {
    noData: "Данные не найдены",
    chooseOption: "Выберите опцию",
    clear: "чистый"
  }
};
var PO_COMBO_DEBOUNCE_TIME_DEFAULT = 400;
var PO_COMBO_FIELD_LABEL_DEFAULT = "label";
var PO_COMBO_FIELD_VALUE_DEFAULT = "value";
var poMultiselectContainerPositionDefault$1 = "bottom";
var PoComboBaseComponent = class _PoComboBaseComponent {
  changeDetector;
  // Propriedade interna que define se o ícone de ajuda adicional terá cursor clicável (evento) ou padrão (tooltip).
  additionalHelpEventTrigger;
  /**
   *
   * @deprecated v23.x.x use `p-helper`
   *
   * @optional
   *
   * @description
   * Exibe um ícone de ajuda adicional, com o texto desta propriedade sendo passado para o popover do componente `po-helper`.
   * **Como boa prática, indica-se utilizar um texto com até 140 caracteres.**
   * > Requer um recuo mínimo de 8px se o componente estiver próximo à lateral da tela.
   *
   * > Essa propriedade está **depreciada** e será removida na versão `23.x.x`. Recomendamos utilizar a propriedade `p-helper` que oferece mais recursos e flexibilidade.
   */
  additionalHelpTooltip;
  /**
   * @optional
   *
   * @description
   *
   * Evento disparado ao sair do campo.
   */
  blur = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Aplica foco no elemento ao ser iniciado.
   *
   * > Caso mais de um elemento seja configurado com essa propriedade, apenas o último elemento declarado com ela terá o foco.
   *
   * @default `false`
   */
  autoFocus = false;
  /**
   * @optional
   *
   * @description
   * Define se o título do campo será exibido de forma compacta.
   *
   * Quando habilitado (`true`), o modo compacto afeta o conjunto composto por:
   * - `po-label`
   * - `p-requirement (showRequired)`
   * - `po-helper`
   *
   * Ou seja, todos os elementos relacionados ao título do campo
   * (rótulo, indicador de obrigatoriedade e componente auxiliar) passam
   * a seguir o comportamento de layout compacto.
   *
   * Também é possível definir esse comportamento de forma global,
   * uma única vez, na folha de estilo geral da aplicação, por meio
   * da customização dos tokens CSS:
   *
   * - `--field-container-title-justify`
   * - `--field-container-title-flex`
   *
   * Exemplo:
   *
   * ```
   * :root {
   *   --field-container-title-justify: flex-start;
   *   --field-container-title-flex: 0 1 auto;
   * }
   * ```
   *
   * Dessa forma, o layout compacto passa a ser o padrão da aplicação,
   * sem a necessidade de definir a propriedade individualmente em cada campo.
   *
   * @default `false`
   */
  compactLabel = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "compactLabel"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-compact-label",
    transform: convertToBoolean
  }));
  /** Label no componente. */
  label;
  /** Texto de apoio para o campo. */
  help;
  /** Nome do componente. */
  name;
  /**
   * @optional
   *
   * @description
   * Nesta propriedade deve ser informada a URL do serviço em que será realizado o filtro para carregamento da lista de
   * itens no componente.
   * Caso haja a necessidade de customização, então pode ser informado um serviço implementando a interface PoComboFilter.
   *
   * Caso utilizado uma URL, o serviço deve ser retornado no padrão API TOTVS e utiliza as propriedades
   * `p-field-label` e `p-field-value` para a construção da lista de itens.
   *
   * Quando utilizada uma URL de serviço, então será concatenada nesta URL o valor que deseja-se filtrar da seguinte forma:
   * ```
   * url + ?filter=Peter
   * ```
   *
   * Se for definida a propriedade `p-filter-params`, a mesma também será concatenada. Por exemplo, para o
   * parâmetro `{ age: 23 }` a URL ficaria:
   *
   * ```
   * url + ?page=1&pageSize=20&age=23&filter=Peter
   * ```
   */
  filterService;
  /**
   * @optional
   *
   * @description
   *
   * Se verdadeiro ativa a funcionalidade de scroll infinito para o combo, Ao chegar ao fim da tabela executará nova busca dos dados conforme paginação.
   *
   * @default `false`
   */
  set infiniteScroll(value) {
    this._infiniteScroll = value;
  }
  get infiniteScroll() {
    return this._infiniteScroll;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define o percentual necessário para disparar o evento `show-more`, que é responsável por carregar mais dados no combo. Caso o valor seja maior que 100 ou menor que 0, o valor padrão será 100%.
   *
   * **Exemplos**
   * - p-infinite-scroll-distance = 80: Quando atingir 80% do scroll do combo, o `show-more` será disparado.
   */
  set infiniteScrollDistance(value) {
    this._infiniteScrollDistance = value > 100 || value < 0 ? 100 : value;
  }
  get infiniteScrollDistance() {
    return this._infiniteScrollDistance;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define o ícone que será exibido no início do campo.
   *
   * É possível usar qualquer um dos ícones da [Biblioteca de ícones](https://po-ui.io/icons). conforme exemplo abaixo:
   * ```
   * <po-combo p-icon="an an-user" p-label="PO combo"></po-combo>
   * ```
   * Também é possível utilizar outras fontes de ícones, por exemplo a biblioteca *Font Awesome*, da seguinte forma:
   * ```
   * <po-combo p-icon="fa fa-podcast" p-label="PO combo"></po-combo>
   * ```
   * Outra opção seria a customização do ícone através do `TemplateRef`, conforme exemplo abaixo:
   * ```
   * <po-combo [p-icon]="template" p-label="combo template ionic"></po-combo>
   *
   * <ng-template #template>
   *  <ion-icon style="font-size: inherit" name="heart"></ion-icon>
   * </ng-template>
   * ```
   * > Para o ícone enquadrar corretamente, deve-se utilizar `font-size: inherit` caso o ícone utilizado não aplique-o.
   */
  icon;
  /**
   * @optional
   *
   * @description
   *
   * Define se a indicação de campo opcional será exibida.
   *
   * > Não será exibida a indicação se:
   * - O campo conter `p-required`;
   * - Não possuir `p-help` e/ou `p-label`.
   *
   * @default `false`
   */
  optional;
  /** Se verdadeiro, o campo receberá um botão para ser limpo. */
  clean;
  /**
   * @optional
   *
   * @description
   *
   * Se verdadeiro, o evento `p-change` receberá como argumento o `PoComboOption` referente à opção selecionada.
   *
   * @default `false`
   */
  emitObjectValue = false;
  /**
   * @optional
   *
   * @description
   *
   * Se verdadeiro, desabilitará a busca de um item via TAB.
   *
   * @default `false`
   */
  disabledTabFilter = false;
  /**
   * @optional
   *
   * @description
   *
   * Define se o filtro inicial será removido no primeiro clique do campo.
   *
   * Quando habilitado e o combo possui um valor padrão, o primeiro clique
   * exibirá todos os itens da lista ao invés de apenas o item inicializado.
   *
   * @default `false`
   */
  set removeInitialFilter(value) {
    this._removeInitialFilter = value;
    if (value) {
      this.isRemoveInitialFilterSetByInput = true;
    }
  }
  get removeInitialFilter() {
    return this._removeInitialFilter;
  }
  /**
   * @optional
   *
   * @description
   *
   * Exibe a mensagem setada se o campo estiver vazio e for requerido.
   *
   * > Necessário que a propriedade `p-required` esteja habilitada.
   *
   */
  fieldErrorMessage;
  /**
   *
   * @deprecated v23.x.x use `p-helper`
   *
   * @optional
   *
   * @description
   * Evento disparado ao clicar no ícone de ajuda adicional.
   *
   * > Essa propriedade está **depreciada** e será removida na versão `23.x.x`. Recomendamos utilizar a propriedade `p-helper` que oferece mais recursos e flexibilidade.
   */
  additionalHelp = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Limita a exibição da mensagem de erro a duas linhas e exibe um tooltip com o texto completo.
   *
   * > Caso essa propriedade seja definida como `true`, a mensagem de erro será limitada a duas linhas
   * e um tooltip será exibido ao passar o mouse sobre a mensagem para mostrar o conteúdo completo.
   *
   * @default `false`
   */
  errorLimit = false;
  /**
   * @optional
   *
   * @description
   *
   * Deve ser informada uma função que será disparada quando houver alterações no ngModel. A função receberá como argumento o model modificado.
   *
   * > Pode-se optar pelo recebimento do objeto selecionado ao invés do model através da propriedade `p-emit-object-value`.
   */
  change = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Evento disparado sempre que o valor do model é alterado, seja por interação do usuário
   * ou por atualização programática (ex: `setValue`, `patchValue`, carregamento assíncrono).
   *
   * Diferentemente do `p-change`, que é disparado apenas por interação do usuário,
   * o `p-change-model` cobre todos os cenários de alteração de valor.
   *
   * Não emite quando o novo valor é idêntico ao anterior (deduplicação automática).
   */
  changeModel = new EventEmitter();
  /**
   * @optional
   *
   * @description
   * Evento disparado quando uma tecla é pressionada enquanto o foco está no componente.
   * Retorna um objeto `KeyboardEvent` com informações sobre a tecla.
   */
  keydown = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Função para atualizar o ngModel do componente, necessário quando não for utilizado dentro da tag form.
   *
   * Na versão 12.2.0 do Angular a verificação `strictTemplates` vem true como default. Portanto, para utilizar
   * two-way binding no componente deve se utilizar da seguinte forma:
   *
   * ```
   * <po-combo ... [ngModel]="comboModel" (ngModelChange)="comboModel = $event"> </po-combo>
   * ```
   *
   */
  ngModelChange = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Deve ser informada uma função que será disparada quando houver alterações no Search input. A função receberá como argumento o input modificado.
   *
   */
  inputChange = new EventEmitter();
  /**
   * @docsPrivate
   *
   * Determinar se o valor do compo deve retorna objeto do tipo {value: any, label: any}
   */
  controlValueWithLabel = false;
  /**
   * @optional
   *
   * @description
   *
   * Define a direção preferida para exibição do `listbox` em relação ao campo (`top` ou `bottom`).
   * Útil em casos onde o posicionamento automático não se comporta como esperado, como quando o componente está próximo
   * ao final do formulário ou do container visível. Na maioria dos casos, essa direção será respeitada; no entanto,
   * pode ser ajustada automaticamente conforme o espaço disponível na tela.
   *
   * @default `bottom`
   */
  listboxControlPosition = poMultiselectContainerPositionDefault$1;
  /**
   * @optional
   *
   * @description
   *
   * Define as opções do componente de ajuda (po-helper) que será exibido ao lado do label quando a propriedade `p-label` for definida, ou, ao lado do componente na ausência da propriedade `p-label`.
   * > Para mais informações acesse: https://po-ui.io/documentation/po-helper.
   *
   * > Ao configurar esta propriedade, o antigo ícone de ajuda adicional (`p-additional-help-tooltip` e `p-additional-help`) será ignorado.
   */
  poHelperComponent = input(void 0, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "poHelperComponent"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-helper"
  }));
  /**
   * @optional
   *
   * @description
   *
   * Habilita a quebra automática do texto da propriedade `p-label`. Quando `p-label-text-wrap` for verdadeiro, o texto que excede
   * o espaço disponível é transferido para a próxima linha em pontos apropriados para uma
   * leitura clara.
   *
   * @default `false`
   */
  labelTextWrap = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "labelTextWrap"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-label-text-wrap"
  }));
  cacheOptions = [];
  defaultService;
  firstInWriteValue = true;
  isFirstFilter = true;
  isFiltering = false;
  keyupSubscribe;
  modelLastUpdate;
  onModelChange;
  previousSearchValue = "";
  selectedOption;
  selectedValue;
  selectedView;
  service;
  visibleOptions = [];
  page = 1;
  pageSize = 10;
  displayAdditionalHelp = false;
  dynamicLabel = "label";
  dynamicValue = "value";
  shouldApplyFocus = false;
  hasValidatorRequired = false;
  cacheStaticOptions = [];
  comboOptionsList = [];
  onModelTouched = null;
  _changeOnEnter = false;
  _debounceTime = 400;
  _disabled = false;
  _disabledInitFilter = false;
  _fieldLabel = "label";
  _fieldValue = "value";
  _filterMinlength = 0;
  _filterMode = PoComboFilterMode.startsWith;
  _filterParams;
  _literals;
  _loading = false;
  _options = [];
  _placeholder = "";
  _required = false;
  _size = void 0;
  _initialSize = void 0;
  _sort = false;
  language;
  _infiniteScrollDistance = 100;
  _infiniteScroll = false;
  _removeInitialFilter = false;
  isRemoveInitialFilterSetByInput = false;
  // utilizado para fazer o controle de atualizar o model.
  // não deve forçar a atualização se o gatilho for o writeValue para não deixar o campo dirty.
  fromWriteValue = false;
  validatorChange;
  /** Mensagem apresentada enquanto o campo estiver vazio. */
  set placeholder(value) {
    this._placeholder = value || this.literals.chooseOption;
  }
  get placeholder() {
    return this._placeholder;
  }
  /**
   * @optional
   *
   * @description
   * Esta propriedade define em quanto tempo (em milissegundos), aguarda para acionar o evento de filtro após cada pressionamento de tecla.
   * Será utilizada apenas quando houver serviço (`p-filter-service`).
   *
   * @default `400`
   */
  set debounceTime(value) {
    const parsedValue = parseInt(value, 10);
    this._debounceTime = !isNaN(parsedValue) && parsedValue > 0 ? parsedValue : PO_COMBO_DEBOUNCE_TIME_DEFAULT;
  }
  get debounceTime() {
    return this._debounceTime;
  }
  /**
   * @optional
   *
   * @description
   * Desabilita o filtro inicial no serviço, que é executado no primeiro clique no campo.
   *
   * @default `false`
   *
   */
  set disabledInitFilter(value) {
    this._disabledInitFilter = value;
  }
  get disabledInitFilter() {
    return this._disabledInitFilter;
  }
  /**
   * @optional
   *
   * @description
   * Deve ser informado o nome da propriedade do objeto que será utilizado para a conversão dos itens apresentados na lista do componente
   * (`p-options`), esta propriedade será responsável pelo valor de cada item da lista.
   *
   * Necessário quando informar o serviço como URL e o mesmo não estiver retornando uma lista de objetos no padrão da interface
   * PoComboOption.
   *
   * @default `value`
   */
  set fieldValue(value) {
    if (!this.service && !this.filterService) {
      this.dynamicValue = value;
    }
    this._fieldValue = value || PO_COMBO_FIELD_VALUE_DEFAULT;
    if (isTypeof(this.filterService, "string") && this.service) {
      this.service.fieldValue = this._fieldValue;
    }
  }
  get fieldValue() {
    return this._fieldValue;
  }
  /**
   * @optional
   *
   * @description
   * Deve ser informado o nome da propriedade do objeto que será utilizado para a conversão dos itens apresentados na lista do componente
   * (`p-options`), esta propriedade será responsável pelo texto de apresentação de cada item da lista.
   *
   * Necessário quando informar o serviço como URL e o mesmo não estiver retornando uma lista de objetos no padrão da interface
   * PoComboOption.
   *
   * @default `label`
   */
  set fieldLabel(value) {
    if (!this.service && !this.filterService) {
      this.dynamicLabel = value;
    }
    this._fieldLabel = value || PO_COMBO_FIELD_LABEL_DEFAULT;
    if (isTypeof(this.filterService, "string") && this.service) {
      this.service.fieldLabel = this._fieldLabel;
    }
  }
  get fieldLabel() {
    return this._fieldLabel;
  }
  /**
   * @optional
   *
   * @description
   * Valor mínimo de caracteres para realizar o filtro no serviço.
   *
   * @default `0`
   */
  set filterMinlength(value) {
    const parseValue = typeof value === "string" ? parseInt(value, 10) : value;
    this._filterMinlength = Number.isInteger(parseValue) ? parseValue : 0;
  }
  get filterMinlength() {
    return this._filterMinlength;
  }
  /**
   * @optional
   *
   * @description
   * Define que o campo será obrigatório.
   *
   * @default `false`
   */
  set required(required) {
    this._required = required;
    this.validateModel(this.selectedValue);
  }
  get required() {
    return this._required;
  }
  /**
   * Define se a indicação de campo obrigatório será exibida.
   *
   * > Não será exibida a indicação se:
   * - Não possuir `p-help` e/ou `p-label`.
   */
  showRequired = false;
  /**
   * @optional
   *
   * @description
   *
   * Define o tamanho do componente:
   * - `small`: altura do input como 32px (disponível apenas para acessibilidade AA).
   * - `medium`: altura do input como 44px.
   *
   * > Caso a acessibilidade AA não esteja configurada, o tamanho `medium` será mantido.
   * Para mais detalhes, consulte a documentação do [po-theme](https://po-ui.io/documentation/po-theme).
   *
   * @default `medium`
   */
  set size(value) {
    this._initialSize = value;
    this.applySizeBasedOnA11y();
  }
  get size() {
    return this._size ?? getDefaultSizeFn(PoFieldSize);
  }
  /**
   * @optional
   *
   * @description
   * Indica que o evento `p-change` só será disparado ao clicar ou pressionar a tecla "Enter" sobre uma opção selecionada.
   *
   * @default `false`
   */
  set changeOnEnter(changeOnEnter) {
    this._changeOnEnter = changeOnEnter;
  }
  get changeOnEnter() {
    return this._changeOnEnter;
  }
  /**
   * @optional
   *
   * @description
   * Indica que o campo será desabilitado.
   *
   * @default `false`
   */
  set disabled(disabled) {
    this._disabled = disabled;
    this.validateModel(this.selectedValue);
  }
  get disabled() {
    return this._disabled;
  }
  /**
   * @optional
   *
   * @description
   * Exibe um ícone de carregamento no lado direito do campo para sinalizar que uma operação está em andamento.
   *
   * @default `false`
   */
  set loading(value) {
    this._loading = value;
    this.changeDetector?.markForCheck();
  }
  get loading() {
    return this._loading;
  }
  get isDisabled() {
    return this._disabled || this._loading;
  }
  /** Indica que a lista definida na propriedade p-options será ordenada pela descrição. */
  set sort(sort3) {
    this._sort = sort3;
    this.comboListDefinitions();
  }
  get sort() {
    return this._sort;
  }
  /**
   * Nesta propriedade define a lista de opções do `po-combo`.
   *
   * > A lista pode ser definida utilizando um array com o valor representando o `value` e o `label` das seguintes formas:
   *
   * ```
   * <po-combo name="combo" p-label="PO Combo" [p-options]="[{value: 1, label: 'One'}, {value: 2, label: 'two'}]"> </po-combo>
   * ```
   *
   * ```
   * <po-combo name="combo" p-label="PO Combo" [p-options]="[{name: 'Roger', age: 28}, {name: 'Anne', age: 35}]" p-field-label="name" p-field-value="age"> </po-combo>
   * ```
   *
   * - Aconselha-se utilizar valores distintos no `label` e `value` dos itens.
   */
  set options(options) {
    this._options = Array.isArray(options) ? options : [];
    this.comboListDefinitions();
  }
  get options() {
    return this._options;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define o modo de pesquisa utilizado no filtro da lista de seleção: `startsWith`, `contains` ou `endsWith`.
   *
   * > Quando utilizar a propriedade `p-filter-service` esta propriedade será ignorada.
   *
   * @default `startsWith`
   */
  set filterMode(filterMode) {
    this._filterMode = filterMode in PoComboFilterMode ? filterMode : PoComboFilterMode.startsWith;
    switch (this._filterMode.toString()) {
      case "startsWith":
        this._filterMode = PoComboFilterMode.startsWith;
        break;
      case "contains":
        this._filterMode = PoComboFilterMode.contains;
        break;
      case "endsWith":
        this._filterMode = PoComboFilterMode.endsWith;
        break;
    }
  }
  get filterMode() {
    return this._filterMode;
  }
  /**
   * @optional
   *
   * @description
   *
   * Valor que será repassado como parâmetro para a URL ou aos métodos do serviço que implementam a interface *PoComboFilter*.
   *
   * > Caso a lista contenha agrupamentos, os mesmos só serão exibidos se houver no mínimo uma opção que corresponda à pesquisa.
   */
  set filterParams(filterParams) {
    this._filterParams = filterParams || filterParams === 0 || filterParams === false ? filterParams : void 0;
  }
  get filterParams() {
    return this._filterParams;
  }
  /**
   * @optional
   *
   * @description
   *
   * Objeto com as literais usadas no `po-combo`.
   *
   * Para utilizar basta passar a literal que deseja customizar:
   *
   * ```
   *  const customLiterals: PoComboLiterals = {
   *    noData: 'Nenhum valor'
   *  };
   * ```
   *
   * E para carregar a literal customizada, basta apenas passar o objeto para o componente.
   *
   * ```
   * <po-combo
   *   [p-literals]="customLiterals">
   * </po-combo>
   * ```
   *
   * > O objeto padrão de literais será traduzido de acordo com o idioma do
   * [`PoI18nService`](/documentation/po-i18n) ou do browser.
   */
  set literals(value) {
    if (value instanceof Object && !(value instanceof Array)) {
      this._literals = __spreadValues(__spreadValues(__spreadValues({}, poComboLiteralsDefault[poLocaleDefault]), poComboLiteralsDefault[this.language]), value);
    } else {
      this._literals = poComboLiteralsDefault[this.language];
    }
  }
  get literals() {
    return this._literals || poComboLiteralsDefault[this.language];
  }
  /**
   * @optional
   *
   * @description
   *
   * Define se o componente irá guardar o valor do model para evitar requisições repetidas.
   *
   * > Caso o valor seja `false`, o componente fará uma nova requisição mesmo que o valor procurado seja o mesmo do model.
   *
   * @default `true`
   */
  cache = true;
  /**
   * @optional
   *
   * @description
   *
   * Define que o `listbox` e/ou popover (`p-helper` e/ou `p-error-limit`) serão incluídos no body da
   * página e não dentro do componente. Essa opção pode ser necessária em cenários com containers que possuem scroll ou
   * overflow escondido,garantindo o posicionamento correto de ambos próximo ao elemento.
   *
   * > Quando utilizado com `p-helper`, leitores de tela como o NVDA podem não ler o conteúdo do popover.
   *
   * @default `false`
   */
  appendBox = false;
  constructor(languageService, changeDetector) {
    this.changeDetector = changeDetector;
    this.language = languageService.getShortLanguage();
  }
  get isOptionGroupList() {
    return this._options.length && this._options[0].hasOwnProperty("options");
  }
  ngOnInit() {
    this.dynamicValue = this.checkIfService("value");
    this.dynamicLabel = this.checkIfService("label");
    this.updateComboList();
  }
  onThemeChange() {
    this.applySizeBasedOnA11y();
  }
  onInitService() {
    if (this.filterService) {
      this.setService(this.filterService);
      this.initInputObservable();
    }
  }
  setService(service) {
    if (service) {
      if (isTypeof(service, "object")) {
        this.service = service;
      } else {
        this.service = this.defaultService;
        this.service.configProperties(service, this.fieldLabel, this.fieldValue);
      }
    }
  }
  compareMethod(search, option, filterMode) {
    switch (filterMode) {
      case PoComboFilterMode.startsWith:
        return this.startsWith(search, option);
      case PoComboFilterMode.contains:
        return this.contains(search, option);
      case PoComboFilterMode.endsWith:
        return this.endsWith(search, option);
    }
  }
  startsWith(search, option) {
    return option[this.dynamicLabel].toLowerCase().startsWith(search.toLowerCase());
  }
  contains(search, option) {
    return option[this.dynamicLabel].toLowerCase().indexOf(search.toLowerCase()) > -1;
  }
  endsWith(search, option) {
    return option[this.dynamicLabel].toLowerCase().endsWith(search.toLowerCase());
  }
  getOptionFromValue(value, options) {
    return options ? options.find((option) => this.isEqual(option[this.dynamicValue], value)) : null;
  }
  getOptionFromLabel(label, options) {
    if (options) {
      return options.find((option) => option[this.dynamicLabel].toString().toLowerCase() === label.toString().toLowerCase());
    } else {
      return null;
    }
  }
  updateSelectedValue(option, isUpdateModel = true) {
    const optionLabel = option && option[this.dynamicLabel] || "";
    const optionValue = option?.[this.dynamicValue] !== void 0 ? option[this.dynamicValue] : void 0;
    this.updateInternalVariables(option);
    this.updateOptionsSelectedState(option, optionValue);
    if (this.changeOnEnter && isUpdateModel) {
      this.setInputValue(optionLabel);
    } else if (!this.changeOnEnter) {
      this.setInputValue(optionLabel);
    }
    if (isUpdateModel) {
      this.updateModel(optionValue);
    }
  }
  isGroupTitle(option) {
    return option?.options === true;
  }
  updateOptionsSelectedState(selectedOption, value) {
    this.comboOptionsList.forEach((option) => {
      const isSelectedGroup = this.isGroupTitle(option) && this.isGroupTitle(selectedOption) && option[this.dynamicLabel] === selectedOption[this.dynamicLabel];
      const isSelectedOption = !this.isGroupTitle(option) && validValue(value) && this.isEqual(option[this.dynamicValue], value);
      option.selected = isSelectedGroup || isSelectedOption;
    });
  }
  callModelChange(value) {
    return this.onModelChange ? this.onModelChange(value) : this.ngModelChange.emit(value);
  }
  controlChangeModelEmitter(value) {
    if (this.modelLastUpdate !== value) {
      this.changeModel.emit(value);
      this.modelLastUpdate = value;
    }
  }
  isEqual(value, inputValue) {
    if ((value || value === 0) && inputValue) {
      return value.toString() === inputValue.toString();
    }
    if (value === null && inputValue !== null || value === void 0 && inputValue !== void 0) {
      value = `${value}`;
    }
    return value === inputValue;
  }
  //Transforma o tamanho do input para o tamanho do ícone de loading correspondente
  mapSizeToIcon(size) {
    return mapInputSizeToLoadingIcon(size);
  }
  searchForLabel(search, options, filterMode) {
    if (search && options && options.length) {
      const newOptions = [];
      let addedOptionsGroupTitle = false;
      let optionsGroupTitle;
      options.forEach((option) => {
        if ("options" in option) {
          addedOptionsGroupTitle = false;
          return optionsGroupTitle = option;
        }
        if (option[this.dynamicLabel] && (this.compareMethod(search, option, filterMode) || this.service)) {
          if (this.isOptionGroupList && !addedOptionsGroupTitle) {
            newOptions.push(optionsGroupTitle);
            addedOptionsGroupTitle = true;
          }
          newOptions.push(option);
        }
      });
      this.selectedView = newOptions[this.isOptionGroupList ? 1 : 0];
      this.updateComboList(newOptions);
    } else {
      this.updateComboList();
    }
  }
  updateComboList(options) {
    const copyOptions = options || [...this.comboOptionsList];
    let newOptions;
    if (this.removeInitialFilter) {
      newOptions = copyOptions;
    } else {
      newOptions = !options && !this.infiniteScroll && this.selectedValue ? [__spreadValues({}, this.selectedOption)] : copyOptions;
    }
    this.visibleOptions = newOptions;
    if (!this.selectedView && this.visibleOptions.length) {
      this.selectedView = copyOptions.find((option) => option[this.dynamicValue] !== void 0);
    }
  }
  getNextOption(value, options, reverse = false) {
    const optionsList = reverse ? options.slice(0).reverse() : options.slice(0);
    let optionFound = null;
    let found = false;
    for (const option of optionsList) {
      if (option[this.dynamicValue] && !optionFound) {
        optionFound = option;
      }
      if (option[this.dynamicValue] && found) {
        return option;
      }
      if (this.isEqual(option[this.dynamicValue], value)) {
        found = true;
      }
    }
    return optionFound;
  }
  getIndexSelectedView() {
    for (let i2 = 0; i2 < this.visibleOptions.length; i2++) {
      if (this.compareObjects(this.visibleOptions[i2], this.selectedView)) {
        return i2;
      }
    }
    return null;
  }
  compareObjects(obj1, obj2) {
    return JSON.stringify(obj1) === JSON.stringify(obj2);
  }
  verifyValidOption() {
    const inputValue = this.getInputValue();
    const optionFound = this.getOptionFromLabel(inputValue, this.comboOptionsList);
    if (!this.service) {
      this.updateComboList([...this.cacheStaticOptions]);
    }
    if (optionFound && optionFound[this.dynamicValue] !== this.selectedValue) {
      this.updateSelectedValue(optionFound);
      this.previousSearchValue = optionFound[this.dynamicLabel];
    } else if (this.selectedValue && this.selectedOption && this.selectedOption[this.dynamicLabel] !== inputValue) {
      this.updateSelectedValueWithOldOption();
      this.previousSearchValue = this.selectedOption[this.dynamicLabel];
      return;
    } else if (inputValue && !optionFound) {
      const isInputValueDiffSelectedLabel = !!(this.selectedOption && this.selectedOption[this.dynamicLabel] !== inputValue);
      this.updateSelectedValue(null, isInputValueDiffSelectedLabel || this.changeOnEnter);
      this.previousSearchValue = "";
      return;
    }
  }
  // Recebe as alterações do model
  writeValue(value) {
    const originalValue = value;
    value = this.getValueWrite(value);
    this.fromWriteValue = true;
    if (validValue(value) && !this.service && this.comboOptionsList && this.comboOptionsList.length) {
      const option = this.getOptionFromValue(value, this.comboOptionsList);
      this.updateSelectedValue(option);
      this.normalizeModelIfNeeded(originalValue, option);
      this.updateComboList();
      if (!this.isRemoveInitialFilterSetByInput) {
        this.removeInitialFilter = false;
      }
      return;
    }
    if (value && this.service) {
      return this.getObjectByValue(value);
    } else {
      this.updateSelectedValue(null);
      this.updateComboList();
      this.updateHasNext();
    }
  }
  // Função implementada do ControlValueAccessor
  // Usada para interceptar os estados de habilitado via forms api
  setDisabledState(isDisabled) {
    this.disabled = isDisabled;
    this.changeDetector.markForCheck();
  }
  registerOnChange(fn) {
    this.onModelChange = fn;
  }
  registerOnTouched(fn) {
    this.onModelTouched = fn;
  }
  registerOnValidatorChange(fn) {
    this.validatorChange = fn;
  }
  validate(abstractControl) {
    if (!this.hasValidatorRequired && this.fieldErrorMessage && abstractControl.hasValidator(Validators.required)) {
      this.hasValidatorRequired = true;
    }
    let valueToValidate = abstractControl.value;
    if (this.controlValueWithLabel && valueToValidate && typeof valueToValidate === "object") {
      valueToValidate = valueToValidate.value;
    }
    if (PoValidators.requiredFailed(this.required || this.hasValidatorRequired, this.disabled, valueToValidate)) {
      this.changeDetector.markForCheck();
      return {
        required: {
          valid: false
        }
      };
    }
    return {};
  }
  clear(value) {
    this.callModelChange(value);
    this.updateSelectedValue(null);
    this.updateComboList();
    this.initInputObservable();
    this.updateHasNext();
    if (this.service || this.filterService) {
      this.keyupSubscribe.unsubscribe();
    }
  }
  configAfterSetFilterService(service) {
    if (service) {
      this.comboOptionsList = [];
      this.unsubscribeKeyupObservable();
      this.onInitService();
    } else {
      this.service = void 0;
      this.comboOptionsList = this.cacheStaticOptions;
    }
    this.visibleOptions = [];
    this.isFirstFilter = true;
  }
  unsubscribeKeyupObservable() {
    if (this.keyupSubscribe) {
      this.keyupSubscribe.unsubscribe();
    }
  }
  validateModel(model2) {
    if (this.validatorChange) {
      this.validatorChange(model2);
    }
  }
  comboListDefinitions() {
    this.comboOptionsList = this.options.length > 0 ? this.listingComboOptions(this.options) : this.options;
    this.cacheStaticOptions = this.comboOptionsList;
    this.updateComboList();
  }
  checkIfService(dynamicValue) {
    if ((this.service || this.filterService) && dynamicValue === "label") {
      return PO_COMBO_FIELD_LABEL_DEFAULT;
    }
    if ((this.service || this.filterService) && dynamicValue === "value") {
      return PO_COMBO_FIELD_VALUE_DEFAULT;
    }
    if (!this.service && dynamicValue === "label") {
      return this.fieldLabel;
    }
    if (!this.service && dynamicValue === "value") {
      return this.fieldValue;
    }
  }
  compareOptions(dynamicLabel) {
    return function(optionA, optionB) {
      const labelA = optionA[dynamicLabel].toString().toLowerCase();
      const labelB = optionB[dynamicLabel].toString().toLowerCase();
      return labelA < labelB ? -1 : labelA > labelB ? 1 : 0;
    };
  }
  getValueUpdate(data, selectedOption) {
    const {
      [this.dynamicValue]: value,
      [this.dynamicLabel]: label
    } = selectedOption || {};
    if (this.controlValueWithLabel && value !== null && value !== void 0) {
      return {
        value,
        label
      };
    }
    return data;
  }
  getValueWrite(data) {
    if (this.controlValueWithLabel && data?.value !== null && data?.value !== void 0) {
      return data?.value;
    }
    return data;
  }
  normalizeModelIfNeeded(originalValue, option) {
    if (!this.controlValueWithLabel) {
      return;
    }
    const isPrimitive2 = typeof originalValue === "number" || typeof originalValue === "string";
    if (!isPrimitive2 || !option) {
      return;
    }
    const value = option[this.dynamicValue];
    const label = option[this.dynamicLabel];
    if (value === null) {
      return;
    }
    this.callModelChange({
      value,
      label
    });
  }
  hasDuplicatedOption(options, currentOption, accumulatedGroupOptions) {
    if (accumulatedGroupOptions) {
      return accumulatedGroupOptions.some((option) => option[this.dynamicLabel] === currentOption);
    } else {
      return options.some((option) => option[this.dynamicValue] === currentOption);
    }
  }
  listingComboOptions(comboOptions) {
    const comboOptionsList = comboOptions.concat();
    const verifiedComboOptionsList = this.verifyComboOptions(comboOptionsList);
    this.sortOptions(verifiedComboOptionsList);
    if (this.isOptionGroupList && verifiedComboOptionsList.length > 0) {
      return this.verifyComboOptionsGroup(verifiedComboOptionsList);
    }
    return verifiedComboOptionsList;
  }
  sortOptions(comboOptionsList) {
    if (comboOptionsList.length > 0 && this.sort) {
      return comboOptionsList.sort(this.compareOptions(this.dynamicLabel));
    }
  }
  validateValue(currentOption, verifyingOptionsGroup = false) {
    const {
      options
    } = currentOption;
    if (this.isOptionGroupList) {
      return validValue(currentOption[this.dynamicLabel]) && options && options.length > 0 || verifyingOptionsGroup === true && validValue(currentOption[this.dynamicValue]);
    }
    return validValue(currentOption[this.dynamicValue]) && !options;
  }
  verifyComboOptions(comboOptions, verifyingOptionsGroup = false, accumulatedGroupOptions) {
    return comboOptions.reduce((accumulatedOptions, currentOption) => {
      if (!this.verifyIfHasLabel(currentOption) || this.hasDuplicatedOption(accumulatedOptions, currentOption[this.dynamicValue] || currentOption[this.dynamicLabel], accumulatedGroupOptions) || !this.validateValue(currentOption, verifyingOptionsGroup)) {
        return accumulatedOptions;
      }
      accumulatedOptions.push(currentOption);
      return accumulatedOptions;
    }, []);
  }
  verifyComboOptionsGroup(comboOptionsList) {
    return comboOptionsList.reduce((accumulatedGroupOptions, currentOption) => {
      const {
        options
      } = currentOption;
      const verifiedComboOptionsGroupList = this.verifyComboOptions(options, true, accumulatedGroupOptions);
      if (verifiedComboOptionsGroupList.length > 0) {
        this.sortOptions(verifiedComboOptionsGroupList);
        accumulatedGroupOptions.push({
          label: currentOption[this.dynamicLabel],
          options: true
        }, ...verifiedComboOptionsGroupList);
      }
      return accumulatedGroupOptions;
    }, []);
  }
  verifyIfHasLabel(currentOption = {}) {
    const {
      options
    } = currentOption;
    if (this.isOptionGroupList && options && !currentOption[this.dynamicLabel] || !currentOption[this.dynamicLabel] && !currentOption[this.dynamicValue] || !this.isOptionGroupList && options) {
      return false;
    }
    if (!currentOption[this.dynamicLabel]) {
      currentOption[this.dynamicLabel] = currentOption[this.dynamicValue].toString();
      return true;
    }
    return true;
  }
  updateInternalVariables(option) {
    if (option) {
      this.selectedView = option;
      this.selectedOption = option;
    } else {
      this.selectedView = void 0;
      this.selectedOption = void 0;
    }
  }
  updateModel(value) {
    if (value !== this.selectedValue && !this.fromWriteValue) {
      this.callModelChange(this.getValueUpdate(value, this.selectedOption));
      this.change.emit(this.emitObjectValue ? this.selectedOption : value);
    }
    this.selectedValue = value;
    this.fromWriteValue = false;
    this.controlChangeModelEmitter(value);
  }
  updateSelectedValueWithOldOption() {
    const oldOption = this.getOptionFromValue(this.selectedValue, this.comboOptionsList);
    if (oldOption && oldOption[this.dynamicLabel]) {
      return this.updateSelectedValue(oldOption);
    }
  }
  updateHasNext() {
    if (this.service && this.infiniteScroll) {
      this.defaultService.hasNext = true;
    }
  }
  applySizeBasedOnA11y() {
    const size = validateSizeFn(this._initialSize, PoFieldSize);
    this._size = size;
  }
  static ɵfac = function PoComboBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoComboBaseComponent)(ɵɵdirectiveInject(PoLanguageService), ɵɵdirectiveInject(ChangeDetectorRef));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoComboBaseComponent,
    hostVars: 2,
    hostBindings: function PoComboBaseComponent_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("PoUiThemeChange", function PoComboBaseComponent_PoUiThemeChange_HostBindingHandler() {
          return ctx.onThemeChange();
        }, ɵɵresolveWindow);
      }
      if (rf & 2) {
        ɵɵattribute("p-size", ctx.size)("p-loading", ctx.loading);
      }
    },
    inputs: {
      additionalHelpEventTrigger: "additionalHelpEventTrigger",
      additionalHelpTooltip: [0, "p-additional-help-tooltip", "additionalHelpTooltip"],
      autoFocus: [2, "p-auto-focus", "autoFocus", convertToBoolean],
      compactLabel: [1, "p-compact-label", "compactLabel"],
      label: [0, "p-label", "label"],
      help: [0, "p-help", "help"],
      name: "name",
      filterService: [0, "p-filter-service", "filterService"],
      infiniteScroll: [2, "p-infinite-scroll", "infiniteScroll", convertToBoolean],
      infiniteScrollDistance: [0, "p-infinite-scroll-distance", "infiniteScrollDistance"],
      icon: [0, "p-icon", "icon"],
      optional: [0, "p-optional", "optional"],
      clean: [2, "p-clean", "clean", convertToBoolean],
      emitObjectValue: [2, "p-emit-object-value", "emitObjectValue", convertToBoolean],
      disabledTabFilter: [2, "p-disabled-tab-filter", "disabledTabFilter", convertToBoolean],
      removeInitialFilter: [2, "p-remove-initial-filter", "removeInitialFilter", convertToBoolean],
      fieldErrorMessage: [0, "p-field-error-message", "fieldErrorMessage"],
      errorLimit: [0, "p-error-limit", "errorLimit"],
      controlValueWithLabel: [2, "p-control-value-with-label", "controlValueWithLabel", convertToBoolean],
      listboxControlPosition: [0, "p-listbox-control-position", "listboxControlPosition"],
      poHelperComponent: [1, "p-helper", "poHelperComponent"],
      labelTextWrap: [1, "p-label-text-wrap", "labelTextWrap"],
      placeholder: [0, "p-placeholder", "placeholder"],
      debounceTime: [0, "p-debounce-time", "debounceTime"],
      disabledInitFilter: [2, "p-disabled-init-filter", "disabledInitFilter", convertToBoolean],
      fieldValue: [0, "p-field-value", "fieldValue"],
      fieldLabel: [0, "p-field-label", "fieldLabel"],
      filterMinlength: [0, "p-filter-minlength", "filterMinlength"],
      required: [2, "p-required", "required", convertToBoolean],
      showRequired: [0, "p-show-required", "showRequired"],
      size: [0, "p-size", "size"],
      changeOnEnter: [2, "p-change-on-enter", "changeOnEnter", convertToBoolean],
      disabled: [2, "p-disabled", "disabled", convertToBoolean],
      loading: [2, "p-loading", "loading", convertToBoolean],
      sort: [2, "p-sort", "sort", convertToBoolean],
      options: [0, "p-options", "options"],
      filterMode: [0, "p-filter-mode", "filterMode"],
      filterParams: [0, "p-filter-params", "filterParams"],
      literals: [0, "p-literals", "literals"],
      cache: [2, "p-cache", "cache", convertToBoolean],
      appendBox: [2, "p-append-in-body", "appendBox", convertToBoolean]
    },
    outputs: {
      blur: "p-blur",
      additionalHelp: "p-additional-help",
      change: "p-change",
      changeModel: "p-change-model",
      keydown: "p-keydown",
      ngModelChange: "ngModelChange",
      inputChange: "p-input-change"
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoComboBaseComponent, [{
    type: Directive
  }], () => [{
    type: PoLanguageService
  }, {
    type: ChangeDetectorRef
  }], {
    additionalHelpEventTrigger: [{
      type: Input
    }],
    additionalHelpTooltip: [{
      type: Input,
      args: ["p-additional-help-tooltip"]
    }],
    blur: [{
      type: Output,
      args: ["p-blur"]
    }],
    autoFocus: [{
      type: Input,
      args: [{
        alias: "p-auto-focus",
        transform: convertToBoolean
      }]
    }],
    compactLabel: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-compact-label",
        required: false
      }]
    }],
    label: [{
      type: Input,
      args: ["p-label"]
    }],
    help: [{
      type: Input,
      args: ["p-help"]
    }],
    name: [{
      type: Input,
      args: ["name"]
    }],
    filterService: [{
      type: Input,
      args: ["p-filter-service"]
    }],
    infiniteScroll: [{
      type: Input,
      args: [{
        alias: "p-infinite-scroll",
        transform: convertToBoolean
      }]
    }],
    infiniteScrollDistance: [{
      type: Input,
      args: ["p-infinite-scroll-distance"]
    }],
    icon: [{
      type: Input,
      args: ["p-icon"]
    }],
    optional: [{
      type: Input,
      args: ["p-optional"]
    }],
    clean: [{
      type: Input,
      args: [{
        alias: "p-clean",
        transform: convertToBoolean
      }]
    }],
    emitObjectValue: [{
      type: Input,
      args: [{
        alias: "p-emit-object-value",
        transform: convertToBoolean
      }]
    }],
    disabledTabFilter: [{
      type: Input,
      args: [{
        alias: "p-disabled-tab-filter",
        transform: convertToBoolean
      }]
    }],
    removeInitialFilter: [{
      type: Input,
      args: [{
        alias: "p-remove-initial-filter",
        transform: convertToBoolean
      }]
    }],
    fieldErrorMessage: [{
      type: Input,
      args: ["p-field-error-message"]
    }],
    additionalHelp: [{
      type: Output,
      args: ["p-additional-help"]
    }],
    errorLimit: [{
      type: Input,
      args: ["p-error-limit"]
    }],
    change: [{
      type: Output,
      args: ["p-change"]
    }],
    changeModel: [{
      type: Output,
      args: ["p-change-model"]
    }],
    keydown: [{
      type: Output,
      args: ["p-keydown"]
    }],
    ngModelChange: [{
      type: Output,
      args: ["ngModelChange"]
    }],
    inputChange: [{
      type: Output,
      args: ["p-input-change"]
    }],
    controlValueWithLabel: [{
      type: Input,
      args: [{
        alias: "p-control-value-with-label",
        transform: convertToBoolean
      }]
    }],
    listboxControlPosition: [{
      type: Input,
      args: ["p-listbox-control-position"]
    }],
    poHelperComponent: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-helper",
        required: false
      }]
    }],
    labelTextWrap: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-label-text-wrap",
        required: false
      }]
    }],
    placeholder: [{
      type: Input,
      args: ["p-placeholder"]
    }],
    debounceTime: [{
      type: Input,
      args: ["p-debounce-time"]
    }],
    disabledInitFilter: [{
      type: Input,
      args: [{
        alias: "p-disabled-init-filter",
        transform: convertToBoolean
      }]
    }],
    fieldValue: [{
      type: Input,
      args: ["p-field-value"]
    }],
    fieldLabel: [{
      type: Input,
      args: ["p-field-label"]
    }],
    filterMinlength: [{
      type: Input,
      args: ["p-filter-minlength"]
    }],
    required: [{
      type: Input,
      args: [{
        alias: "p-required",
        transform: convertToBoolean
      }]
    }],
    showRequired: [{
      type: Input,
      args: ["p-show-required"]
    }],
    size: [{
      type: Input,
      args: ["p-size"]
    }, {
      type: HostBinding,
      args: ["attr.p-size"]
    }],
    changeOnEnter: [{
      type: Input,
      args: [{
        alias: "p-change-on-enter",
        transform: convertToBoolean
      }]
    }],
    disabled: [{
      type: Input,
      args: [{
        alias: "p-disabled",
        transform: convertToBoolean
      }]
    }],
    loading: [{
      type: HostBinding,
      args: ["attr.p-loading"]
    }, {
      type: Input,
      args: [{
        alias: "p-loading",
        transform: convertToBoolean
      }]
    }],
    sort: [{
      type: Input,
      args: [{
        alias: "p-sort",
        transform: convertToBoolean
      }]
    }],
    options: [{
      type: Input,
      args: ["p-options"]
    }],
    filterMode: [{
      type: Input,
      args: ["p-filter-mode"]
    }],
    filterParams: [{
      type: Input,
      args: ["p-filter-params"]
    }],
    literals: [{
      type: Input,
      args: ["p-literals"]
    }],
    cache: [{
      type: Input,
      args: [{
        alias: "p-cache",
        transform: convertToBoolean
      }]
    }],
    appendBox: [{
      type: Input,
      args: [{
        alias: "p-append-in-body",
        transform: convertToBoolean
      }]
    }],
    onThemeChange: [{
      type: HostListener,
      args: ["window:PoUiThemeChange"]
    }]
  });
})();
var PoComboFilterService = class _PoComboFilterService {
  http = inject(HttpClient);
  fieldLabel = "label";
  fieldValue = "value";
  hasNext = true;
  headers = new HttpHeaders({
    "X-PO-No-Message": "true"
  });
  _url;
  messages = [];
  get url() {
    return this._url;
  }
  getFilteredData(param, filterParams) {
    const params = this.prepareParams(param, filterParams);
    return this.http.get(`${this.url}`, {
      responseType: "json",
      params,
      headers: this.headers
    }).pipe(tap((res) => this.hasNext = res["hasNext"]), map((response) => this.parseToArrayComboOption(response.items)));
  }
  getObjectByValue(value, filterParams) {
    const filterParamsValidated = validateObjectType(filterParams);
    return this.http.get(`${this.url}/${value}`, {
      params: filterParamsValidated,
      headers: this.headers
    }).pipe(map((item) => this.parseToComboOption(item)));
  }
  configProperties(url, fieldLabel, fieldValue) {
    this._url = url;
    this.fieldLabel = fieldLabel;
    this.fieldValue = fieldValue;
  }
  scrollListener(componentListner) {
    return fromEvent(componentListner, "scroll").pipe(debounceTime(100));
  }
  prepareParams(param, filterParams) {
    const {
      page,
      pageSize,
      value
    } = param;
    const filterParamsValidated = validateObjectType(filterParams);
    return page ? __spreadProps(__spreadValues({}, filterParamsValidated), {
      page,
      pageSize,
      filter: value
    }) : __spreadProps(__spreadValues({}, filterParamsValidated), {
      filter: value
    });
  }
  parseToArrayComboOption(items) {
    if (items && items.length > 0) {
      const parsedOptions = items.map((item) => this.parseToComboOption(item));
      this.displayMessages();
      return parsedOptions;
    }
    return [];
  }
  parseToComboOption(item) {
    if (!item?.[this.fieldValue]) {
      this.addMessage(item, this.fieldValue);
      return {
        value: ""
      };
    }
    const label = item[this.fieldLabel];
    const value = item[this.fieldValue];
    return {
      label,
      value
    };
  }
  addMessage(item, property) {
    this.messages.push(`Cannot read property "${property}" of ${JSON.stringify(item)},
      see [p-field-value] property at https://po-ui.io/documentation/po-combo`);
  }
  displayMessages() {
    if (this.messages.length) {
      this.messages.forEach((message) => console.error(message));
      this.messages = [];
    }
  }
  static ɵfac = function PoComboFilterService_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoComboFilterService)();
  };
  static ɵprov = ɵɵdefineInjectable({
    token: _PoComboFilterService,
    factory: _PoComboFilterService.ɵfac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoComboFilterService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
var PoComboOptionTemplateDirective = class _PoComboOptionTemplateDirective {
  templateRef;
  // Necessário manter templateRef para o funcionamento do row template.
  constructor(templateRef) {
    this.templateRef = templateRef;
  }
  static ɵfac = function PoComboOptionTemplateDirective_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoComboOptionTemplateDirective)(ɵɵdirectiveInject(TemplateRef));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoComboOptionTemplateDirective,
    selectors: [["", "p-combo-option-template", ""]],
    standalone: false
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoComboOptionTemplateDirective, [{
    type: Directive,
    args: [{
      selector: "[p-combo-option-template]",
      standalone: false
    }]
  }], () => [{
    type: TemplateRef
  }], null);
})();
var PoCleanBaseComponent = class _PoCleanBaseComponent {
  _size = void 0;
  /** Nesta propriedade deve-se informar o elementRef do campo de entrada que utilizará o po-clean. */
  inputRef;
  /** Valor que será atribuído ao campo quando for clicado no po-clean. */
  defaultValue = "";
  // Ícone customizado para o componente.
  icon;
  // Tamanho do componente.
  set size(value) {
    this._size = value;
  }
  get size() {
    return this._size;
  }
  /**
   * @optional
   *
   * @description
   *
   * Evento disparado quando executada ação do po-clean.
   * Este evento deve ser usado para avisar para o componente que está usando o po-clean, que o botão foi disparado,
   * e provavelmente será preciso emitir o evento para atualizar o model.
   */
  changeEvent = new EventEmitter();
  clear() {
    this.setInputValue(this.defaultValue);
    this.changeEvent.emit(this.defaultValue);
  }
  showIcon() {
    return this.defaultValue !== this.getInputValue();
  }
  static ɵfac = function PoCleanBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoCleanBaseComponent)();
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoCleanBaseComponent,
    hostVars: 1,
    hostBindings: function PoCleanBaseComponent_HostBindings(rf, ctx) {
      if (rf & 2) {
        ɵɵattribute("p-size", ctx.size);
      }
    },
    inputs: {
      inputRef: [0, "p-element-ref", "inputRef"],
      defaultValue: [0, "p-default-value", "defaultValue"],
      icon: [0, "p-icon", "icon"],
      size: [0, "p-size", "size"]
    },
    outputs: {
      changeEvent: "p-change-event"
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoCleanBaseComponent, [{
    type: Directive
  }], null, {
    inputRef: [{
      type: Input,
      args: ["p-element-ref"]
    }],
    defaultValue: [{
      type: Input,
      args: ["p-default-value"]
    }],
    icon: [{
      type: Input,
      args: ["p-icon"]
    }],
    size: [{
      type: Input,
      args: ["p-size"]
    }, {
      type: HostBinding,
      args: ["attr.p-size"]
    }],
    changeEvent: [{
      type: Output,
      args: ["p-change-event"]
    }]
  });
})();
var PoCleanComponent = class _PoCleanComponent extends PoCleanBaseComponent {
  setInputValue(value) {
    if (this.inputRef && this.inputRef.nativeElement) {
      this.inputRef.nativeElement.value = value;
    }
  }
  getInputValue() {
    if (this.inputRef && this.inputRef.nativeElement) {
      return this.inputRef.nativeElement.value;
    }
  }
  static ɵfac = /* @__PURE__ */ (() => {
    let ɵPoCleanComponent_BaseFactory;
    return function PoCleanComponent_Factory(__ngFactoryType__) {
      return (ɵPoCleanComponent_BaseFactory || (ɵPoCleanComponent_BaseFactory = ɵɵgetInheritedFactory(_PoCleanComponent)))(__ngFactoryType__ || _PoCleanComponent);
    };
  })();
  static ɵcmp = ɵɵdefineComponent({
    type: _PoCleanComponent,
    selectors: [["po-clean"]],
    standalone: false,
    features: [ɵɵInheritDefinitionFeature],
    decls: 1,
    vars: 1,
    consts: [[1, "po-field-icon", 3, "po-field-icon-aa", "po-field-icon-large", "p-icon"], [1, "po-field-icon", 3, "click", "p-icon"]],
    template: function PoCleanComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵconditionalCreate(0, PoCleanComponent_Conditional_0_Template, 1, 5, "po-icon", 0);
      }
      if (rf & 2) {
        ɵɵconditional(ctx.showIcon() ? 0 : -1);
      }
    },
    dependencies: [PoIconComponent],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoCleanComponent, [{
    type: Component,
    args: [{
      selector: "po-clean",
      standalone: false,
      template: `@if (showIcon()) {
  <po-icon
    class="po-field-icon"
    [class.po-field-icon-aa]="size === 'small'"
    [class.po-field-icon-large]="size === 'large'"
    [p-icon]="icon || 'ICON_CLEAR_CONTENT'"
    (click)="clear()"
  >
  </po-icon>
}
`
    }]
  }], null, null);
})();
var poComboContainerOffset = 8;
var PoComboComponent = class _PoComboComponent extends PoComboBaseComponent {
  element = inject(ElementRef);
  differs = inject(IterableDiffers);
  defaultService = inject(PoComboFilterService);
  renderer = inject(Renderer2);
  controlPosition = inject(PoControlPositionService);
  comboOptionTemplate;
  outerContainer;
  containerElement;
  contentElement;
  fieldContainerInput;
  iconElement;
  inputEl;
  poListbox;
  helperEl;
  comboIcon = "ICON_ARROW_DOWN";
  comboOpen = false;
  differ;
  id = `po-combo[${uuid()}]`;
  isProcessingValueByTab = false;
  scrollTop = 0;
  shouldMarkLetters = true;
  infiniteLoading = false;
  containerWidth;
  _isServerSearching = false;
  lastKey;
  clickoutListener;
  eventResizeListener;
  filterSubscription;
  getSubscription;
  subscriptionScrollEvent;
  constructor() {
    const changeDetector = inject(ChangeDetectorRef);
    const languageService = inject(PoLanguageService);
    super(languageService, changeDetector);
    const differs = this.differs;
    this.changeDetector = changeDetector;
    this.differ = differs.find([]).create(null);
  }
  set isServerSearching(value) {
    if (value) {
      this._isServerSearching = value;
      this.changeDetector.detectChanges();
      this.setContainerPosition();
      this.initializeListeners();
    } else {
      this._isServerSearching = value;
    }
  }
  get isServerSearching() {
    return this._isServerSearching;
  }
  ngAfterViewInit() {
    if (this.autoFocus) {
      this.focus();
    }
    this.setContainerWidth();
  }
  ngOnChanges(changes) {
    if (changes.debounceTime) {
      this.unsubscribeKeyupObservable();
      this.initInputObservable();
    }
    if (changes.filterService) {
      this.configAfterSetFilterService(this.filterService);
    }
    if (changes.label) {
      this.displayAdditionalHelp = false;
    }
  }
  ngOnDestroy() {
    this.removeListeners();
    if (this.filterSubscription) {
      this.filterSubscription.unsubscribe();
    }
    if (this.getSubscription) {
      this.getSubscription.unsubscribe();
    }
    if (this.infiniteScroll) {
      this.subscriptionScrollEvent?.unsubscribe();
    }
  }
  emitAdditionalHelp() {
    if (this.label && this.isAdditionalHelpEventTriggered()) {
      this.additionalHelp.emit();
    }
  }
  /**
   * Função que atribui foco ao componente.
   *
   * Para utilizá-la é necessário ter a instância do componente no DOM, podendo ser utilizado o ViewChild da seguinte forma:
   *
   * ```
   * import { PoComboComponent } from '@po-ui/ng-components';
   *
   * ...
   *
   * @ViewChild(PoComboComponent, { static: true }) combo: PoComboComponent;
   *
   * focusCombo() {
   *   this.combo.focus();
   * }
   * ```
   */
  focus() {
    if (!this.disabled) {
      this.inputEl.nativeElement.focus();
    }
  }
  getAdditionalHelpTooltip() {
    return this.isAdditionalHelpEventTriggered() ? null : this.additionalHelpTooltip;
  }
  onBlur(event) {
    this.onModelTouched?.();
    if (event.type === "blur") {
      this.blur.emit();
    }
  }
  onKeyDown(event) {
    const key2 = event?.keyCode;
    const inputValue = event?.target?.value;
    const isFieldFocused = document.activeElement === this.inputEl.nativeElement;
    if (event.shiftKey && key2 === PoKeyCodeEnum.tab) {
      this.controlComboVisibility(false);
      return;
    }
    if (this.service && key2 === PoKeyCodeEnum.tab && inputValue && !this.disabledTabFilter) {
      this.controlComboVisibility(false);
      return this.getObjectByValue(inputValue);
    }
    if (key2 === PoKeyCodeEnum.arrowDown) {
      event.preventDefault();
      if (this.visibleOptions.length) {
        this.focusItem();
      }
      if (this.comboOpen) {
        this.controlComboVisibility(true);
      } else {
        this.toggleComboVisibility();
      }
      this.isFiltering = this.changeOnEnter ? this.isFiltering : false;
    }
    if (key2 === PoKeyCodeEnum.enter && this.selectedView && this.comboOpen) {
      const isUpdateModel = this.selectedView.value !== this.selectedValue || inputValue !== this.selectedView.label;
      this.controlComboVisibility(false);
      this.updateSelectedValue(this.selectedView, isUpdateModel);
      this.isFiltering = false;
      if (!this.service) {
        this.updateComboList([...this.cacheStaticOptions]);
      }
      return;
    }
    if (key2 === PoKeyCodeEnum.enter) {
      event.preventDefault();
      this.toggleComboVisibility();
    }
    if (key2 === PoKeyCodeEnum.esc) {
      if (key2 === this.lastKey) {
        this.lastKey = "";
        if (this.selectedValue) {
          this.clearAndFocus();
        }
        return;
      } else {
        this.onCloseCombo();
      }
    }
    if (key2 === PoKeyCodeEnum.tab && this.shouldHandleTab(event) && !this.isCleanVisible()) {
      event.preventDefault();
      this.focusItem();
    }
    this.lastKey = event.keyCode;
    if (isFieldFocused) {
      this.keydown.emit(event);
    }
  }
  onKeyUp(event) {
    const key2 = event.keyCode || event.which;
    const inputValue = event.target.value;
    const isValidKey = key2 !== PoKeyCodeEnum.arrowUp && key2 !== PoKeyCodeEnum.arrowDown && key2 !== PoKeyCodeEnum.enter && key2 !== PoKeyCodeEnum.esc && key2 !== PoKeyCodeEnum.tab;
    if (isValidKey) {
      if (inputValue) {
        if (!this.service && this.previousSearchValue !== inputValue) {
          this.shouldMarkLetters = true;
          this.isFiltering = true;
          this.searchForLabel(inputValue, this.comboOptionsList, this.filterMode);
          this.inputChange.emit(inputValue);
        }
      } else {
        const useDefaultOptionsService = this.service && this.selectedValue && this.selectedOption.label === this.previousSearchValue;
        this.updateSelectedValue(null);
        if (!this.service) {
          this.updateComboList();
        } else if (useDefaultOptionsService) {
          this.updateComboList([...this.cacheOptions]);
        }
        this.isFiltering = false;
      }
      if (this.previousSearchValue !== inputValue) {
        this.changeDetector.detectChanges();
        this.controlComboVisibility(true);
      }
    }
    this.previousSearchValue = inputValue;
  }
  initInputObservable() {
    if (this.service) {
      const keyupObservable = fromEvent(this.inputEl.nativeElement, "keyup").pipe(filter((e2) => this.isValidCharacterToSearch(e2.keyCode)), map((e2) => e2.currentTarget.value), distinctUntilChanged(), tap(() => {
        this.shouldMarkLetters = false;
      }), debounceTime(this.debounceTime));
      this.keyupSubscribe = keyupObservable.subscribe((value) => {
        if (value.length >= this.filterMinlength || !value) {
          this.controlApplyFilter(value);
        }
      });
    }
  }
  controlApplyFilter(value, isArrowDown) {
    if (!this.isProcessingValueByTab && (!this.selectedOption || value !== this.selectedOption[this.dynamicLabel]) || !this.cache) {
      this.defaultService.hasNext = true;
      this.page = this.setPage();
      this.options = [];
      this.applyFilter(value, true, isArrowDown);
    }
    this.isProcessingValueByTab = false;
  }
  applyFilter(value, reset = false, isArrowDown) {
    if (this.removeInitialFilter) {
      this.defaultService.hasNext = true;
    }
    if (this.defaultService.hasNext) {
      this.controlComboVisibility(false, reset);
      this.isServerSearching = true;
      const param = this.infiniteScroll ? {
        property: this.fieldLabel,
        value,
        page: this.page,
        pageSize: this.pageSize
      } : {
        property: this.fieldLabel,
        value
      };
      this.filterSubscription = this.service.getFilteredData(param, this.filterParams).subscribe((items) => {
        this.setOptionsByApplyFilter(value, items, reset);
        if (isArrowDown) {
          this.focusItem();
        }
      }, (error2) => this.onErrorFilteredData());
    }
  }
  setOptionsByApplyFilter(value, items, reset = false) {
    this.shouldMarkLetters = true;
    this.isServerSearching = false;
    this.infiniteLoading = false;
    this.options = this.prepareOptions(items);
    this.searchForLabel(value, items, this.filterMode);
    this.changeDetector.detectChanges();
    this.controlComboVisibility(true, reset);
    if (this.isFirstFilter) {
      this.isFirstFilter = !this.isFirstFilter;
      this.updateCacheOptions();
    }
  }
  getErrorPattern() {
    return this.fieldErrorMessage && this.hasInvalidClass() ? this.fieldErrorMessage : "";
  }
  hasInvalidClass() {
    return this.element.nativeElement.classList.contains("ng-invalid") && this.element.nativeElement.classList.contains("ng-dirty");
  }
  getObjectByValue(value) {
    if (this.selectedValue !== value && this.selectedOption?.[this.dynamicLabel] !== value) {
      this.isProcessingValueByTab = true;
      this.getSubscription = this.service.getObjectByValue(value, this.filterParams).subscribe((item) => this.updateOptionByFilteredValue(item), (error2) => this.onErrorGetObjectByValue());
    }
  }
  updateOptionByFilteredValue(item) {
    if (item) {
      this.options = [item];
      this.onOptionClick(item);
    } else {
      this.updateSelectedValue(null);
    }
    setTimeout(() => {
      this.isProcessingValueByTab = false;
    }, this.debounceTime);
  }
  setShouldApplyFocus(value) {
    this.shouldApplyFocus = value;
  }
  toggleComboVisibility(isButton) {
    if (this.disabled) {
      return;
    }
    this.setShouldApplyFocus(true);
    if (this.service && !this.disabledInitFilter) {
      this.applyFilterInFirstClick();
    }
    this.controlComboVisibility(!this.comboOpen, false, isButton);
  }
  applyFilterInFirstClick() {
    const isEmptyFirstFilter = this.isFirstFilter && !this.selectedValue;
    if (this.removeInitialFilter || isEmptyFirstFilter) {
      this.options = [];
      const scrollingControl = this.setScrollingControl();
      this.applyFilter("", scrollingControl);
    }
  }
  controlComboVisibility(toOpen, reset = false, isButton) {
    toOpen ? this.open(reset, isButton) : this.close(reset);
  }
  onCloseCombo() {
    this.controlComboVisibility(false);
    this.inputEl.nativeElement.focus();
  }
  onOptionClick(option, event) {
    const inputValue = this.getInputValue();
    const isUpdateModel = option[this.dynamicValue] !== this.selectedValue || !!(this.selectedView && inputValue !== this.selectedView[this.dynamicLabel]);
    if (event) {
      event.stopPropagation();
    }
    this.updateSelectedValue(option, isUpdateModel);
    this.controlComboVisibility(false);
    if (!this.service) {
      this.updateComboList([...this.cacheStaticOptions]);
    }
    this.previousSearchValue = this.selectedView[this.dynamicLabel];
    if (this.shouldApplyFocus) {
      this.inputEl.nativeElement.focus();
    }
  }
  calculateScrollTop(selectedItem, index) {
    if (!selectedItem.length || index <= 1) {
      return 0;
    } else {
      return selectedItem[0].offsetTop;
    }
  }
  cleanListbox() {
    this.updateSelectedValue(null);
    this.options.map((option) => option.selected = false);
  }
  getInputValue() {
    return this.inputEl.nativeElement.value;
  }
  setInputValue(value) {
    this.inputEl.nativeElement.value = value;
    if (value === null) {
      this.cleanListbox();
    }
  }
  /**
   * Método que exibe `p-helper` ou executa a ação definida em `p-helper{eventOnClick}` ou em `p-additionalHelp`.
   * Para isso, será necessário configurar uma tecla de atalho utilizando o evento `p-keydown`.
   *
   * > Exibe ou oculta o conteúdo do componente `po-helper` quando o componente estiver com foco.
   *
   * ```
   * // Exemplo com p-label e p-helper
   * <po-combo
   *  #combo
   *  ...
   *  p-label="Label do combo"
   *  [p-helper]="helperOptions"
   *  (p-keydown)="onKeyDown($event, combo)"
   * ></po-combo>
   * ```
   * ```
   * ...
   * onKeyDown(event: KeyboardEvent, inp: PoComboComponent): void {
   *  if (event.code === 'F9') {
   *    inp.showAdditionalHelp();
   *  }
   * }
   * ```
   */
  showAdditionalHelp() {
    this.displayAdditionalHelp = !this.displayAdditionalHelp;
    const helper = this.poHelperComponent();
    const isHelpEvt = this.isAdditionalHelpEventTriggered();
    if (!this.label && (helper || this.additionalHelpTooltip || isHelpEvt)) {
      if (isHelpEvt) {
        this.additionalHelp.emit();
      }
      if (typeof helper !== "string" && typeof helper?.eventOnClick === "function") {
        helper.eventOnClick();
        return;
      }
      if (this.helperEl?.helperIsVisible()) {
        this.helperEl?.closeHelperPopover();
        return;
      }
      this.helperEl?.openHelperPopover();
      return;
    }
    return this.displayAdditionalHelp;
  }
  wasClickedOnToggle(event) {
    if (this.comboOpen && !this.inputEl.nativeElement.contains(event.target) && !this.iconElement.nativeElement.contains(event.target) && (!this.contentElement || !this.contentElement.nativeElement.contains(event.target))) {
      this.controlComboVisibility(false);
      this.verifyValidOption();
      this.selectedView = this.changeOnEnter && !this.selectedValue ? void 0 : this.selectedView;
    } else {
      if (this.service && !this.getInputValue() && !this.isFirstFilter) {
        const scrollingControl = this.setScrollingControl();
        this.applyFilter("", scrollingControl);
      }
    }
  }
  isValidCharacterToSearch(keyCode) {
    return keyCode !== 9 && // tab
    keyCode !== 13 && // entet
    keyCode !== 16 && // shift
    keyCode !== 17 && // ctrl
    keyCode !== 18 && // alt
    keyCode !== 20 && // capslock
    keyCode !== 27 && // esc
    keyCode !== 37 && // seta
    keyCode !== 38 && // seta
    keyCode !== 39 && // seta
    keyCode !== 40 && // seta
    keyCode !== 93;
  }
  searchOnEnterOrArrow(event, value) {
    if ((event.key === "ArrowDown" || event.key === "Enter") && this.service && !this.selectedView && value.length >= this.filterMinlength) {
      this.controlApplyFilter(value, event.key === "ArrowDown");
    }
  }
  showMoreInfiniteScroll() {
    if (this.defaultService.hasNext) {
      this.infiniteLoading = true;
    }
    this.page++;
    this.applyFilter("", true);
  }
  clearAndFocus() {
    this.clear(null);
    this.inputEl.nativeElement.focus();
  }
  updateCacheOptions() {
    this.cacheOptions = this.comboOptionsList.map((item) => item.value === this.selectedValue ? __spreadProps(__spreadValues({}, item), {
      selected: true
    }) : item);
  }
  isCleanVisible() {
    return this.clean && !this.disabled && !!this.inputEl.nativeElement.value;
  }
  handleCleanKeyboardTab(event) {
    if (this.shouldHandleTab(event)) {
      event.preventDefault();
      this.focusItem();
    }
  }
  onListboxKeyDown(event) {
    const key2 = event?.keyCode;
    if (key2 === PoKeyCodeEnum.tab && event.shiftKey) {
      this.focusInput();
      event.preventDefault();
    }
    if (key2 === PoKeyCodeEnum.esc) {
      event.stopPropagation();
    }
  }
  adjustContainerPosition() {
    this.controlPosition.adjustPosition(this.listboxControlPosition);
  }
  close(reset) {
    this.comboOpen = false;
    if (!reset) {
      if (!this.getInputValue()) {
        this.page = this.setPage();
        this.defaultService.hasNext = true;
      }
      if (this.infiniteScroll) {
        this.options = this.setOptions();
      }
    }
    this.changeDetector.detectChanges();
    this.comboIcon = "ICON_ARROW_DOWN";
    this.removeListeners();
    this.isFiltering = false;
    this.renderer.removeClass(this.inputEl.nativeElement, "po-combo-input-focus");
  }
  initializeListeners() {
    this.removeListeners();
    this.clickoutListener = this.renderer.listen("document", "click", (event) => {
      this.wasClickedOnToggle(event);
    });
    this.eventResizeListener = this.renderer.listen("window", "resize", () => {
      setTimeout(() => this.adjustContainerPosition(), 250);
    });
    window.addEventListener("scroll", this.onScroll, true);
  }
  isAdditionalHelpEventTriggered() {
    return this.additionalHelpEventTrigger === "event" || this.additionalHelpEventTrigger === void 0 && this.additionalHelp.observed;
  }
  onErrorGetObjectByValue() {
    this.updateOptionByFilteredValue(null);
  }
  onScroll = () => {
    this.adjustContainerPosition();
  };
  onErrorFilteredData() {
    this.isServerSearching = false;
    this.updateComboList([]);
    this.controlComboVisibility(true);
  }
  open(reset, isButton) {
    this.comboOpen = true;
    if (!reset && this.infiniteScroll) {
      if (!this.getInputValue()) {
        this.page = 1;
      }
      this.options = this.setOptions();
    }
    this.changeDetector.detectChanges();
    this.comboIcon = "ICON_ARROW_UP";
    this.initializeListeners();
    isButton ? this.renderer.addClass(this.inputEl.nativeElement, "po-combo-input-focus") : this.inputEl.nativeElement.focus();
    this.setContainerPosition();
    if (this.comboOpen) {
      this.setContainerWidth();
    }
  }
  removeListeners() {
    if (this.clickoutListener) {
      this.clickoutListener();
    }
    if (this.eventResizeListener) {
      this.eventResizeListener();
    }
    window.removeEventListener("scroll", this.onScroll, true);
  }
  setContainerPosition() {
    this.controlPosition?.setElements(this.containerElement?.nativeElement, poComboContainerOffset, this.fieldContainerInput, ["top", "bottom"], true);
    this.adjustContainerPosition();
  }
  setContainerWidth() {
    if (this.outerContainer) {
      this.containerWidth = this.outerContainer.nativeElement.offsetWidth;
    }
  }
  setOptions() {
    return this.getInputValue() ? this.options : [];
  }
  prepareOptions(items) {
    return this.infiniteScroll ? [...this.options, ...items] : items;
  }
  setPage() {
    return this.infiniteScroll ? 1 : void 0;
  }
  setScrollingControl() {
    return this.infiniteScroll ? true : false;
  }
  focusItem() {
    this.poListbox?.listboxItemList?.nativeElement.focus();
    setTimeout(() => {
      let item = null;
      if (this.selectedValue) {
        item = document.querySelector('.po-listbox-item[aria-selected="true"]');
      }
      if (!item) {
        const items = document.querySelectorAll(".po-listbox-item");
        item = items.length > 0 ? items[0] : null;
      }
      if (item) {
        item.focus();
      }
    });
  }
  // Define o foco no elemento input do combobox.
  focusInput(event) {
    event?.preventDefault();
    this.inputEl.nativeElement.focus();
  }
  shouldHandleTab(event) {
    return this.comboOpen && this.appendBox && !event.shiftKey;
  }
  setHelper(label, additionalHelpTooltip) {
    return setHelperSettings(label, additionalHelpTooltip, this.poHelperComponent(), this.size, this.isAdditionalHelpEventTriggered() ? this.additionalHelp : void 0);
  }
  static ɵfac = function PoComboComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoComboComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoComboComponent,
    selectors: [["po-combo"]],
    contentQueries: function PoComboComponent_ContentQueries(rf, ctx, dirIndex) {
      if (rf & 1) {
        ɵɵcontentQuery(dirIndex, PoComboOptionTemplateDirective, 7);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.comboOptionTemplate = _t.first);
      }
    },
    viewQuery: function PoComboComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c44, 5, ElementRef)(_c45, 5, ElementRef)(_c46, 5, ElementRef)(_c47, 5, ElementRef)(_c48, 7, ElementRef)(_c49, 7, ElementRef)(_c50, 5)(_c17, 5, PoHelperComponent);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.outerContainer = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.containerElement = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.contentElement = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.fieldContainerInput = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.iconElement = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.inputEl = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.poListbox = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.helperEl = _t.first);
      }
    },
    standalone: false,
    features: [ɵɵProvidersFeature([PoComboFilterService, PoControlPositionService, {
      provide: NG_VALUE_ACCESSOR,
      useExisting: forwardRef(() => _PoComboComponent),
      multi: true
    }, {
      provide: NG_VALIDATORS,
      useExisting: forwardRef(() => _PoComboComponent),
      multi: true
    }]), ɵɵInheritDefinitionFeature, ɵɵNgOnChangesFeature],
    decls: 25,
    vars: 49,
    consts: [["outerContainer", ""], ["searchContainer", "", "trigger", "cdkOverlayOrigin"], ["fieldContainerInput", ""], ["inp", ""], ["iconArrow", ""], ["dropdownListbox", ""], ["helperEl", ""], ["containerElement", ""], ["poListbox", "", "contentElement", ""], [3, "p-disabled", "p-id", "p-label", "p-optional", "p-required", "p-show-required", "p-helper", "p-show-helper", "p-size", "p-text-wrap", "p-compact-label"], ["cdkOverlayOrigin", "", 1, "po-field-container-content", "po-combo-container-content"], [1, "po-field-container-content-helper-wrapper"], [1, "po-field-container-input"], [1, "po-field-icon-container-left"], ["autocomplete", "off", "type", "text", 1, "po-combo-input", 3, "click", "keyup", "blur", "keydown", "ngClass", "disabled", "id", "placeholder", "required"], [1, "po-field-icon-container-right"], [3, "p-size"], [1, "po-combo-arrow", "po-field-icon", 3, "click"], [3, "p-icon"], [1, "po-field-helper-button", 3, "p-size", "p-helper", "p-disabled", "p-append-in-body"], ["cdkConnectedOverlay", "", 3, "cdkConnectedOverlayOrigin", "cdkConnectedOverlayOpen", "cdkConnectedOverlayDisableClose"], [3, "p-append-in-body", "p-help", "p-disabled", "p-error-pattern", "p-error-limit", "p-size"], [1, "po-field-icon", "po-icon-input", 3, "p-icon"], ["tabindex", "0", "role", "button", 1, "po-combo-clean", "po-icon-input", 3, "po-combo-clean-aa", "p-element-ref", "p-size"], ["tabindex", "0", "role", "button", 1, "po-combo-clean", "po-icon-input", 3, "p-change-event", "click", "keydown.enter", "keydown.tab", "p-element-ref", "p-size"], [4, "ngTemplateOutlet"], [1, "po-combo-container", 3, "hidden"], ["p-type", "option", 3, "p-selectcombo-item", "p-update-infinite-scroll", "p-close", "keydown", "p-items", "p-field-value", "p-field-label", "p-template", "p-search-value", "p-infinite-loading", "p-infinite-scroll", "p-filtering", "p-cache", "p-filter-mode", "p-visible", "p-is-searching", "p-should-mark-letter", "p-compare-cache", "p-combo-service", "p-infinite-scroll-distance", "p-size", "p-container-width"]],
    template: function PoComboComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵdeclareLet(0);
        ɵɵelementStart(1, "div", null, 0)(3, "po-field-container", 9)(4, "div", 10, 1)(7, "div", 11)(8, "div", 12, 2);
        ɵɵconditionalCreate(10, PoComboComponent_Conditional_10_Template, 2, 5, "div", 13);
        ɵɵelementStart(11, "input", 14, 3);
        ɵɵlistener("click", function PoComboComponent_Template_input_click_11_listener() {
          return !ctx.isDisabled && ctx.toggleComboVisibility();
        })("keyup", function PoComboComponent_Template_input_keyup_11_listener($event) {
          return ctx.onKeyUp($event);
        })("blur", function PoComboComponent_Template_input_blur_11_listener($event) {
          return ctx.onBlur($event);
        })("keyup", function PoComboComponent_Template_input_keyup_11_listener($event) {
          return ctx.searchOnEnterOrArrow($event, $event.target.value);
        })("keydown", function PoComboComponent_Template_input_keydown_11_listener($event) {
          return ctx.onKeyDown($event);
        });
        ɵɵelementEnd();
        ɵɵelementStart(13, "div", 15);
        ɵɵconditionalCreate(14, PoComboComponent_Conditional_14_Template, 1, 1, "po-loading-icon", 16)(15, PoComboComponent_Conditional_15_Template, 1, 1);
        ɵɵelementStart(16, "div", 17, 4);
        ɵɵlistener("click", function PoComboComponent_Template_div_click_16_listener() {
          return !ctx.isDisabled && ctx.toggleComboVisibility(true);
        });
        ɵɵelement(18, "po-icon", 18);
        ɵɵelementEnd()()();
        ɵɵconditionalCreate(19, PoComboComponent_Conditional_19_Template, 2, 4, "po-helper", 19);
        ɵɵelementEnd()();
        ɵɵconditionalCreate(20, PoComboComponent_Conditional_20_Template, 1, 3, null, 20)(21, PoComboComponent_Conditional_21_Template, 1, 1, "ng-container");
        ɵɵelement(22, "po-field-container-bottom", 21);
        ɵɵelementEnd();
        ɵɵtemplate(23, PoComboComponent_ng_template_23_Template, 5, 19, "ng-template", null, 5, ɵɵtemplateRefExtractor);
        ɵɵelementEnd();
      }
      if (rf & 2) {
        const inp_r7 = ɵɵreference(12);
        const poHelper_r8 = ɵɵstoreLet(ctx.setHelper(ctx.label, ctx.additionalHelpTooltip));
        !ctx.label && (ctx.poHelperComponent() || poHelper_r8.hideAdditionalHelp);
        ɵɵadvance(3);
        ɵɵproperty("p-disabled", ctx.isDisabled)("p-id", ctx.id)("p-label", ctx.label)("p-optional", ctx.optional)("p-required", ctx.required)("p-show-required", ctx.showRequired)("p-helper", ctx.label ? poHelper_r8.helperSettings : void 0)("p-show-helper", ctx.label ? ctx.displayAdditionalHelp : void 0)("p-size", ctx.size)("p-text-wrap", ctx.labelTextWrap())("p-compact-label", ctx.compactLabel());
        ɵɵadvance(7);
        ɵɵconditional(ctx.icon ? 10 : -1);
        ɵɵadvance();
        ɵɵclassProp("po-input-icon-left", ctx.icon)("po-input-icon-left-aa", ctx.icon && ctx.size === "small");
        ɵɵproperty("ngClass", ctx.clean && inp_r7.value ? ctx.size === "small" ? "po-input-double-icon-right-aa" : "po-input-double-icon-right" : ctx.size === "small" ? "po-input-icon-right-aa" : "po-input-icon-right")("disabled", ctx.isDisabled)("id", ctx.id)("placeholder", ctx.isDisabled ? "" : ctx.placeholder)("required", ctx.required);
        ɵɵattribute("data-focused-element", !ctx.isDisabled)("data-inactive-component", ctx.isDisabled)("name", ctx.name)("data-append-in-body", ctx.appendBox)("p-size", ctx.size);
        ɵɵadvance(3);
        ɵɵconditional(ctx.loading ? 14 : 15);
        ɵɵadvance(2);
        ɵɵclassProp("po-field-icon-disabled", ctx.isDisabled)("po-field-icon-disabled-aa", ctx.isDisabled && ctx.size === "small");
        ɵɵadvance(2);
        ɵɵclassProp("po-field-icon", !ctx.isDisabled)("po-field-icon-aa", ctx.size === "small")("po-combo-default-border", !ctx.isDisabled && inp_r7.value)("po-combo-background-arrow-up", !ctx.isDisabled && ctx.comboOpen);
        ɵɵproperty("p-icon", ctx.comboOpen ? "ICON_ARROW_UP po-icon-input" : "ICON_ARROW_DOWN po-icon-input");
        ɵɵadvance();
        ɵɵconditional(!ctx.label && ctx.poHelperComponent() || !ctx.label && poHelper_r8.hideAdditionalHelp ? 19 : -1);
        ɵɵadvance();
        ɵɵconditional(ctx.appendBox ? 20 : 21);
        ɵɵadvance(2);
        ɵɵproperty("p-append-in-body", ctx.appendBox)("p-help", ctx.help)("p-disabled", ctx.isDisabled)("p-error-pattern", ctx.getErrorPattern())("p-error-limit", ctx.errorLimit)("p-size", ctx.size);
      }
    },
    dependencies: [NgClass, NgTemplateOutlet, CdkConnectedOverlay, CdkOverlayOrigin, PoFieldContainerBottomComponent, PoFieldContainerComponent, PoListBoxComponent, PoHelperComponent, PoIconComponent, PoCleanComponent, PoLoadingIconComponent],
    encapsulation: 2,
    changeDetection: 0
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoComboComponent, [{
    type: Component,
    args: [{
      selector: "po-combo",
      changeDetection: ChangeDetectionStrategy.OnPush,
      providers: [PoComboFilterService, PoControlPositionService, {
        provide: NG_VALUE_ACCESSOR,
        useExisting: forwardRef(() => PoComboComponent),
        multi: true
      }, {
        provide: NG_VALIDATORS,
        useExisting: forwardRef(() => PoComboComponent),
        multi: true
      }],
      standalone: false,
      template: `@let poHelper = setHelper(label, additionalHelpTooltip);
@let needsHelper = !label && (poHelperComponent() || poHelper.hideAdditionalHelp);
<div #outerContainer>
  <po-field-container
    [p-disabled]="isDisabled"
    [p-id]="id"
    [p-label]="label"
    [p-optional]="optional"
    [p-required]="required"
    [p-show-required]="showRequired"
    [p-helper]="label ? poHelper.helperSettings : undefined"
    [p-show-helper]="label ? displayAdditionalHelp : undefined"
    [p-size]="size"
    [p-text-wrap]="labelTextWrap()"
    [p-compact-label]="compactLabel()"
  >
    <div
      #searchContainer
      cdkOverlayOrigin
      #trigger="cdkOverlayOrigin"
      class="po-field-container-content po-combo-container-content"
    >
      <div class="po-field-container-content-helper-wrapper">
        <div #fieldContainerInput class="po-field-container-input">
          @if (icon) {
            <div class="po-field-icon-container-left">
              <po-icon
                class="po-field-icon po-icon-input"
                [class.po-field-icon-disabled]="isDisabled"
                [class.po-field-icon-aa]="size === 'small'"
                [p-icon]="icon"
              ></po-icon>
            </div>
          }

          <input
            #inp
            class="po-combo-input"
            [ngClass]="
              clean && inp.value
                ? size === 'small'
                  ? 'po-input-double-icon-right-aa'
                  : 'po-input-double-icon-right'
                : size === 'small'
                  ? 'po-input-icon-right-aa'
                  : 'po-input-icon-right'
            "
            [class.po-input-icon-left]="icon"
            [class.po-input-icon-left-aa]="icon && size === 'small'"
            autocomplete="off"
            type="text"
            [attr.data-focused-element]="!isDisabled"
            [attr.data-inactive-component]="isDisabled"
            [attr.name]="name"
            [disabled]="isDisabled"
            [id]="id"
            [placeholder]="isDisabled ? '' : placeholder"
            [required]="required"
            [attr.data-append-in-body]="appendBox"
            [attr.p-size]="size"
            (click)="!isDisabled && toggleComboVisibility()"
            (keyup)="onKeyUp($event)"
            (blur)="onBlur($event)"
            (keyup)="searchOnEnterOrArrow($event, $event.target.value)"
            (keydown)="onKeyDown($event)"
          />

          <div class="po-field-icon-container-right">
            @if (loading) {
              <po-loading-icon [p-size]="mapSizeToIcon(size)"></po-loading-icon>
            } @else {
              @if (!loading && isCleanVisible()) {
                <po-clean
                  tabindex="0"
                  role="button"
                  [attr.aria-label]="literals.clean"
                  class="po-combo-clean po-icon-input"
                  [class.po-combo-clean-aa]="size === 'small'"
                  [p-element-ref]="inputEl"
                  [p-size]="size"
                  (p-change-event)="clear($event)"
                  (click)="clear(null); $event.preventDefault()"
                  (keydown.enter)="clearAndFocus(); $event.preventDefault()"
                  (keydown.tab)="handleCleanKeyboardTab($event)"
                >
                </po-clean>
              }
            }
            <div
              #iconArrow
              class="po-combo-arrow po-field-icon"
              [class.po-field-icon-disabled]="isDisabled"
              [class.po-field-icon-disabled-aa]="isDisabled && size === 'small'"
              (click)="!isDisabled && toggleComboVisibility(true)"
            >
              <po-icon
                [p-icon]="comboOpen ? 'ICON_ARROW_UP po-icon-input' : 'ICON_ARROW_DOWN po-icon-input'"
                [class.po-field-icon]="!isDisabled"
                [class.po-field-icon-aa]="size === 'small'"
                [class.po-combo-default-border]="!isDisabled && inp.value"
                [class.po-combo-background-arrow-up]="!isDisabled && comboOpen"
              ></po-icon>
            </div>
          </div>
        </div>
        @if ((!this.label && poHelperComponent()) || (!this.label && poHelper.hideAdditionalHelp)) {
          <po-helper
            #helperEl
            class="po-field-helper-button"
            [p-size]="size"
            [p-helper]="poHelper.helperSettings"
            [p-disabled]="isDisabled"
            [p-append-in-body]="appendBox"
          >
          </po-helper>
        }
      </div>
    </div>

    @if (appendBox) {
      <ng-template
        cdkConnectedOverlay
        [cdkConnectedOverlayOrigin]="trigger"
        [cdkConnectedOverlayOpen]="true"
        [cdkConnectedOverlayDisableClose]="true"
      >
        <ng-container *ngTemplateOutlet="dropdownListbox"></ng-container>
      </ng-template>
    } @else {
      <ng-container *ngTemplateOutlet="dropdownListbox"> </ng-container>
    }

    <po-field-container-bottom
      [p-append-in-body]="appendBox"
      [p-help]="help"
      [p-disabled]="isDisabled"
      [p-error-pattern]="getErrorPattern()"
      [p-error-limit]="errorLimit"
      [p-size]="size"
    ></po-field-container-bottom>
  </po-field-container>

  <ng-template #dropdownListbox>
    <div #containerElement class="po-combo-container" [hidden]="!comboOpen && !isServerSearching">
      <po-listbox
        #poListbox
        #contentElement
        p-type="option"
        [p-items]="visibleOptions"
        [p-field-value]="dynamicValue"
        [p-field-label]="dynamicLabel"
        [p-template]="comboOptionTemplate"
        [p-search-value]="getInputValue()"
        [p-infinite-loading]="infiniteLoading"
        [p-infinite-scroll]="infiniteScroll"
        [p-filtering]="isFiltering"
        [p-cache]="cache"
        [p-filter-mode]="filterMode"
        [p-visible]="comboOpen"
        [p-is-searching]="isServerSearching"
        [p-should-mark-letter]="shouldMarkLetters"
        [p-compare-cache]="compareObjects(cacheOptions, visibleOptions)"
        [p-combo-service]="service"
        [p-infinite-scroll-distance]="infiniteScrollDistance"
        [p-size]="size"
        [p-container-width]="containerWidth"
        (p-selectcombo-item)="onOptionClick($event, $event.event)"
        (p-update-infinite-scroll)="showMoreInfiniteScroll()"
        (p-close)="onCloseCombo()"
        (keydown)="onListboxKeyDown($event)"
      ></po-listbox>
    </div>
  </ng-template>
</div>
`
    }]
  }], () => [], {
    comboOptionTemplate: [{
      type: ContentChild,
      args: [PoComboOptionTemplateDirective, {
        static: true
      }]
    }],
    outerContainer: [{
      type: ViewChild,
      args: ["outerContainer ", {
        read: ElementRef
      }]
    }],
    containerElement: [{
      type: ViewChild,
      args: ["containerElement", {
        read: ElementRef
      }]
    }],
    contentElement: [{
      type: ViewChild,
      args: ["contentElement", {
        read: ElementRef
      }]
    }],
    fieldContainerInput: [{
      type: ViewChild,
      args: ["fieldContainerInput", {
        read: ElementRef
      }]
    }],
    iconElement: [{
      type: ViewChild,
      args: ["iconArrow", {
        read: ElementRef,
        static: true
      }]
    }],
    inputEl: [{
      type: ViewChild,
      args: ["inp", {
        read: ElementRef,
        static: true
      }]
    }],
    poListbox: [{
      type: ViewChild,
      args: ["poListbox"]
    }],
    helperEl: [{
      type: ViewChild,
      args: ["helperEl", {
        read: PoHelperComponent,
        static: false
      }]
    }]
  });
})();
var PoCalendarHeaderComponent = class _PoCalendarHeaderComponent {
  cdr = inject(ChangeDetectorRef);
  poCalendarLangService = inject(PoCalendarLangService);
  hidePrevious = false;
  hideNext = false;
  displayMonth;
  displayYear;
  displayMonthLabel;
  monthOptions;
  headerTemplate;
  templateContext;
  size;
  _locale;
  set locale(value) {
    if (this._locale !== value) {
      this._locale = value;
      this.setupLabels();
    }
  }
  get locale() {
    return this._locale;
  }
  previous = new EventEmitter();
  next = new EventEmitter();
  selectMonth = new EventEmitter();
  selectYear = new EventEmitter();
  previousMonthLabel;
  nextMonthLabel;
  labelsInitialized = false;
  ngOnInit() {
    if (!this.labelsInitialized) {
      this.setupLabels();
    }
  }
  setupLabels() {
    if (this._locale) {
      this.poCalendarLangService.setLanguage(this._locale);
    }
    this.previousMonthLabel = this.poCalendarLangService.getPreviousMonthLabel();
    this.nextMonthLabel = this.poCalendarLangService.getNextMonthLabel();
    this.labelsInitialized = true;
    this.cdr.markForCheck();
  }
  ngOnChanges(changes) {
    if (changes["templateContext"] || changes["monthOptions"] || changes["headerTemplate"]) {
      this.cdr.markForCheck();
      try {
        this.cdr.detectChanges();
      } catch (e2) {
      }
    }
  }
  static ɵfac = function PoCalendarHeaderComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoCalendarHeaderComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoCalendarHeaderComponent,
    selectors: [["po-calendar-header"]],
    inputs: {
      hidePrevious: [0, "p-hide-previous", "hidePrevious"],
      hideNext: [0, "p-hide-next", "hideNext"],
      displayMonth: [0, "p-display-month", "displayMonth"],
      displayYear: [0, "p-display-year", "displayYear"],
      displayMonthLabel: [0, "p-display-month-label", "displayMonthLabel"],
      monthOptions: [0, "p-month-options", "monthOptions"],
      headerTemplate: [0, "p-header-template", "headerTemplate"],
      templateContext: [0, "p-template-context", "templateContext"],
      size: [0, "p-size", "size"],
      locale: [0, "p-locale", "locale"]
    },
    outputs: {
      previous: "p-previous",
      next: "p-next",
      selectMonth: "p-select-month",
      selectYear: "p-select-year"
    },
    standalone: false,
    features: [ɵɵNgOnChangesFeature],
    decls: 4,
    vars: 3,
    consts: [[1, "po-calendar-header"], [3, "ngTemplateOutlet", "ngTemplateOutletContext"], ["p-kind", "tertiary", "p-icon", "ICON_ARROW_LEFT", 3, "p-aria-label", "p-size"], ["p-kind", "tertiary", "p-icon", "ICON_ARROW_RIGHT", 3, "p-aria-label", "p-size"], ["p-kind", "tertiary", "p-icon", "ICON_ARROW_LEFT", 3, "click", "keydown.enter", "p-aria-label", "p-size"], ["p-kind", "tertiary", "p-icon", "ICON_ARROW_RIGHT", 3, "click", "keydown.enter", "p-aria-label", "p-size"]],
    template: function PoCalendarHeaderComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "div", 0);
        ɵɵconditionalCreate(1, PoCalendarHeaderComponent_Conditional_1_Template, 1, 4, "ng-container", 1);
        ɵɵconditionalCreate(2, PoCalendarHeaderComponent_Conditional_2_Template, 1, 2, "po-button", 2);
        ɵɵconditionalCreate(3, PoCalendarHeaderComponent_Conditional_3_Template, 1, 2, "po-button", 3);
        ɵɵelementEnd();
      }
      if (rf & 2) {
        ɵɵadvance();
        ɵɵconditional(ctx.headerTemplate ? 1 : -1);
        ɵɵadvance();
        ɵɵconditional((ctx.previous == null ? null : ctx.previous.observers) && !ctx.hidePrevious ? 2 : -1);
        ɵɵadvance();
        ɵɵconditional((ctx.next == null ? null : ctx.next.observers.length) && !ctx.hideNext ? 3 : -1);
      }
    },
    dependencies: [NgTemplateOutlet, PoButtonComponent],
    encapsulation: 2,
    changeDetection: 0
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoCalendarHeaderComponent, [{
    type: Component,
    args: [{
      selector: "po-calendar-header",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: '<div class="po-calendar-header">\n  @if (headerTemplate) {\n    <ng-container [ngTemplateOutlet]="headerTemplate" [ngTemplateOutletContext]="{ $implicit: templateContext }">\n    </ng-container>\n  }\n\n  @if (previous?.observers && !hidePrevious) {\n    <po-button\n      p-kind="tertiary"\n      (click)="previous.emit()"\n      (keydown.enter)="previous.emit(); $event.preventDefault()"\n      [p-aria-label]="previousMonthLabel"\n      p-icon="ICON_ARROW_LEFT"\n      [p-size]="size"\n    ></po-button>\n  }\n\n  @if (next?.observers.length && !hideNext) {\n    <po-button\n      p-kind="tertiary"\n      (click)="next.emit()"\n      (keydown.enter)="next.emit(); $event.preventDefault()"\n      [p-aria-label]="nextMonthLabel"\n      p-icon="ICON_ARROW_RIGHT"\n      [p-size]="size"\n    ></po-button>\n  }\n</div>\n'
    }]
  }], null, {
    hidePrevious: [{
      type: Input,
      args: ["p-hide-previous"]
    }],
    hideNext: [{
      type: Input,
      args: ["p-hide-next"]
    }],
    displayMonth: [{
      type: Input,
      args: ["p-display-month"]
    }],
    displayYear: [{
      type: Input,
      args: ["p-display-year"]
    }],
    displayMonthLabel: [{
      type: Input,
      args: ["p-display-month-label"]
    }],
    monthOptions: [{
      type: Input,
      args: ["p-month-options"]
    }],
    headerTemplate: [{
      type: Input,
      args: ["p-header-template"]
    }],
    templateContext: [{
      type: Input,
      args: ["p-template-context"]
    }],
    size: [{
      type: Input,
      args: ["p-size"]
    }],
    locale: [{
      type: Input,
      args: ["p-locale"]
    }],
    previous: [{
      type: Output,
      args: ["p-previous"]
    }],
    next: [{
      type: Output,
      args: ["p-next"]
    }],
    selectMonth: [{
      type: Output,
      args: ["p-select-month"]
    }],
    selectYear: [{
      type: Output,
      args: ["p-select-year"]
    }]
  });
})();
var PoCalendarWrapperComponent = class _PoCalendarWrapperComponent {
  poCalendarService = inject(PoCalendarService);
  poCalendarLangService = inject(PoCalendarLangService);
  poDate = inject(PoDateService);
  cdr = inject(ChangeDetectorRef);
  elementRef = inject(ElementRef);
  value;
  mode = "day";
  responsive = false;
  partType;
  range = false;
  activateDate = /* @__PURE__ */ new Date();
  selectedValue;
  minDate;
  maxDate;
  hoverValue;
  size;
  hideTodayButton = false;
  hasPresets = false;
  headerTemplate;
  _locale;
  set locale(value) {
    this._locale = value;
    this.setupOptions();
  }
  get locale() {
    return this._locale;
  }
  headerChange = new EventEmitter();
  selectDate = new EventEmitter();
  selectTodayEvent = new EventEmitter();
  hoverDateSource = new Subject();
  hoverDate = this.hoverDateSource.pipe(debounceTime(100));
  closeCalendar = new EventEmitter();
  currentYear;
  displayYear;
  displayMonthNumber;
  displayMonth;
  displayDays = [];
  displayWeekDays = [];
  displayMonths = [];
  displayDecade = [];
  displayStartDecade;
  displayFinalDecade;
  displayToday;
  displayToClean;
  today = /* @__PURE__ */ new Date();
  comboMonthsOptions = [];
  comboYearsOptions = [];
  comboKey = 0;
  focusedDayIndex = 0;
  templateContext = {
    monthIndex: 0,
    monthsOptions: [],
    year: 0,
    yearsOptions: [],
    updateDate: (year, month, comboComponent) => this.updateDate(year, month, comboComponent),
    onComboBlur: () => this.onComboBlur()
  };
  currentMonthNumber;
  lastDisplay;
  get monthLabel() {
    return this.poCalendarLangService.getMonthLabel();
  }
  get yearLabel() {
    return this.poCalendarLangService.getYearLabel();
  }
  get isDayVisible() {
    return this.mode === "day";
  }
  get isMonthVisible() {
    return this.mode === "month";
  }
  get isYearVisible() {
    return this.mode === "year";
  }
  get isStartPart() {
    return this.partType === "start";
  }
  get isEndPart() {
    return this.partType === "end";
  }
  get date() {
    return this.value;
  }
  ngOnInit() {
    this.initializeData();
  }
  ngOnChanges(changes) {
    const {
      activateDate,
      minDate,
      maxDate,
      locale
    } = changes;
    if (minDate || maxDate) {
      this.comboYearsOptions = [...this.poCalendarService.getYearOptions(this.minDate, this.maxDate)];
      this.updateTemplateContext();
      this.ensureValidFocusedDay();
      this.cdr.markForCheck();
    }
    if (locale && !locale.firstChange && locale.previousValue !== locale.currentValue) {
      this.comboKey++;
      this.updateTemplateContext();
      this.cdr.detectChanges();
    }
    if (activateDate && !activateDate.firstChange) {
      const val = activateDate.currentValue;
      const dateToUse = this.getDateToUse(val);
      if (dateToUse.getFullYear() !== this.displayYear || dateToUse.getMonth() !== this.displayMonthNumber) {
        this.updateDisplay(dateToUse.getFullYear(), dateToUse.getMonth());
      }
    }
  }
  getDateToUse(value) {
    if (!value) return /* @__PURE__ */ new Date();
    let dateValue = value.start ?? value;
    if (typeof dateValue === "string") {
      const dateOnlyRegex = /^(\d{4})-(\d{2})-(\d{2})$/;
      if (dateOnlyRegex.test(dateValue)) {
        const [, year, month, day] = dateOnlyRegex.exec(dateValue).map(Number);
        return new Date(year, month - 1, day);
      }
      dateValue = new Date(dateValue);
    }
    if (dateValue instanceof Date && !Number.isNaN(dateValue.getTime())) {
      if (dateValue.getUTCHours() === 0 && dateValue.getUTCMinutes() === 0) {
        return new Date(dateValue.getUTCFullYear(), dateValue.getUTCMonth(), dateValue.getUTCDate());
      }
      return new Date(dateValue.getFullYear(), dateValue.getMonth(), dateValue.getDate());
    }
    return /* @__PURE__ */ new Date();
  }
  initializeData() {
    const date = this.getDateToUse(this.activateDate || this.value || /* @__PURE__ */ new Date());
    this.displayYear = date.getFullYear();
    this.displayMonthNumber = date.getMonth();
    this.setupOptions();
    this.updateDisplay(this.displayYear, this.displayMonthNumber);
  }
  setupOptions() {
    this.poCalendarLangService.setLanguage(this.locale);
    this.displayWeekDays = this.poCalendarLangService.getWeekDaysArray();
    this.displayMonths = this.poCalendarLangService.getMonthsArray();
    this.displayToday = this.poCalendarLangService.getTodayLabel();
    this.displayToClean = this.poCalendarLangService.getToCleanLabel();
    this.comboYearsOptions = [...this.poCalendarService.getYearOptions(this.minDate, this.maxDate)];
    this.comboMonthsOptions = [...this.displayMonths.map((label, index) => ({
      label,
      value: index
    }))];
    if (this.displayMonthNumber !== void 0) {
      this.displayMonth = this.displayMonths[this.displayMonthNumber];
    }
    this.cdr.markForCheck();
    this.updateTemplateContext();
  }
  onHeaderDateChange(event) {
    this.mode = "day";
    this.updateDisplay(event.year, event.month);
  }
  updateTemplateContext() {
    const yearsOptions = [...this.comboYearsOptions];
    if (this.displayYear !== void 0 && !yearsOptions.some((option) => option.value === this.displayYear)) {
      yearsOptions.push({
        label: this.displayYear.toString(),
        value: this.displayYear
      });
      yearsOptions.sort((a, b) => a.value - b.value);
    }
    this.templateContext = {
      monthIndex: this.displayMonthNumber,
      monthsOptions: [...this.comboMonthsOptions],
      year: this.displayYear,
      yearsOptions,
      updateDate: (year, month, comboComponent) => this.updateDate(year, month, comboComponent),
      onComboBlur: (comboComponent) => this.onComboBlur(comboComponent)
    };
    this.cdr.markForCheck();
  }
  updateDate(year, month, comboComponent) {
    const isInvalidYear = year === void 0 || year === null;
    const isInvalidMonth = month === void 0 || month === null;
    if (isInvalidYear || isInvalidMonth) {
      this.templateContext = __spreadProps(__spreadValues({}, this.templateContext), {
        year,
        monthIndex: month
      });
      return;
    }
    const hasChanged = this.displayYear !== year || this.displayMonthNumber !== month;
    this.updateDisplay(year, month);
    if (comboComponent && typeof comboComponent.focus === "function") {
      setTimeout(() => {
        comboComponent.focus();
      }, 0);
    }
    if (hasChanged) {
      this.headerChange.emit({
        month: month + 1,
        year
      });
    }
  }
  onComboBlur(comboComponent) {
    if (comboComponent) {
      setTimeout(() => this.handleComboBlur(comboComponent), 0);
    } else {
      this.ensureValidDisplay(this.templateContext.year, this.templateContext.monthIndex);
    }
  }
  handleComboBlur(comboComponent) {
    let currentYear = this.templateContext.year;
    let currentMonth = this.templateContext.monthIndex;
    const isMonthInvalid = this.isMonthInvalid(currentMonth);
    const isYearInvalid = this.isYearInvalid(currentYear);
    if (comboComponent?.selectedOption?.value !== void 0) {
      this.syncInputValue(comboComponent);
      if (isMonthInvalid || isYearInvalid) {
        ({
          currentYear,
          currentMonth
        } = this.applySelectedValue(comboComponent.selectedOption.value, currentYear, currentMonth));
      }
    }
    this.ensureValidDisplay(currentYear, currentMonth);
  }
  syncInputValue(comboComponent) {
    if (comboComponent?.inputEl?.nativeElement) {
      const currentInputValue = comboComponent.inputEl.nativeElement.value;
      const expectedValue = comboComponent.selectedOption.label || "";
      if (currentInputValue !== expectedValue) {
        comboComponent.inputEl.nativeElement.value = expectedValue;
      }
    }
  }
  applySelectedValue(value, currentYear, currentMonth) {
    if (value >= 0 && value <= 11) {
      currentMonth = value;
    } else if (typeof value === "number" && value > 11) {
      currentYear = value;
    }
    return {
      currentYear,
      currentMonth
    };
  }
  ensureValidDisplay(currentYear, currentMonth) {
    const isInvalidYear = currentYear === void 0 || currentYear === null;
    const isInvalidMonth = currentMonth === void 0 || currentMonth === null;
    if (isInvalidYear || isInvalidMonth) {
      const safeYear = isInvalidYear ? this.displayYear || this.today.getFullYear() : currentYear;
      const safeMonth = isInvalidMonth ? this.displayMonthNumber ?? this.today.getMonth() : currentMonth;
      this.updateDisplay(safeYear, safeMonth);
    }
  }
  isMonthInvalid(month) {
    return typeof month === "string" || month < 0 || month > 11 || Number.isNaN(month);
  }
  isYearInvalid(year) {
    return typeof year === "string" || Number.isNaN(year) || year && year < 1900 || year && year > 2100;
  }
  updateDisplay(year, month) {
    if (year === void 0 || month === void 0) return;
    this.displayYear = year;
    this.displayMonthNumber = month;
    this.displayMonth = this.displayMonths[month];
    const calendarArray = this.poCalendarService.monthDays(year, month);
    this.displayDays = [].concat(...calendarArray);
    this.getDecadeArray(year);
    this.updateTemplateContext();
    this.setInitialFocusedDay();
    this.ensureValidFocusedDay();
    this.cdr.detectChanges();
  }
  setInitialFocusedDay() {
    if (this.value) {
      const selectedDate = this.value instanceof Date ? this.value : this.value?.start;
      if (selectedDate instanceof Date) {
        const selectedIndex = this.displayDays.findIndex((day) => day && this.equalsDate(day, selectedDate) && day.getMonth() === this.displayMonthNumber && !this.isDayDisabled(day));
        if (selectedIndex !== -1) {
          this.focusedDayIndex = selectedIndex;
          return;
        }
      }
    }
    const firstAvailableIndex = this.displayDays.findIndex((day) => day instanceof Date && day.getMonth() === this.displayMonthNumber && !this.isDayDisabled(day));
    if (firstAvailableIndex !== -1) {
      this.focusedDayIndex = firstAvailableIndex;
    }
    this.cdr.markForCheck();
  }
  ensureValidFocusedDay() {
    const currentDay = this.displayDays[this.focusedDayIndex];
    if (currentDay instanceof Date && currentDay.getMonth() === this.displayMonthNumber && !this.isDayDisabled(currentDay)) {
      return;
    }
    const firstAvailableIndex = this.displayDays.findIndex((day) => day instanceof Date && day.getMonth() === this.displayMonthNumber && !this.isDayDisabled(day));
    if (firstAvailableIndex !== -1) {
      this.focusedDayIndex = firstAvailableIndex;
    }
  }
  getDayTabIndex(day, index) {
    if (!day || !(day instanceof Date)) {
      return -1;
    }
    if (day.getMonth() !== this.displayMonthNumber) {
      return -1;
    }
    if (this.isDayDisabled(day)) {
      return -1;
    }
    return index === this.focusedDayIndex ? 0 : -1;
  }
  onNextMonth() {
    const newMonth = this.displayMonthNumber < 11 ? this.displayMonthNumber + 1 : 0;
    const newYear = this.displayMonthNumber < 11 ? this.displayYear : this.displayYear + 1;
    this.updateDisplay(newYear, newMonth);
    this.headerChange.emit({
      month: newMonth + 1,
      year: newYear
    });
  }
  onPreviousMonth() {
    const newMonth = this.displayMonthNumber > 0 ? this.displayMonthNumber - 1 : 11;
    const newYear = this.displayMonthNumber > 0 ? this.displayYear : this.displayYear - 1;
    this.updateDisplay(newYear, newMonth);
    this.headerChange.emit({
      month: newMonth + 1,
      year: newYear
    });
  }
  updateYear(value) {
    const newYear = this.displayYear + value;
    this.updateDisplay(newYear, this.displayMonthNumber);
  }
  onSelectMonth(year, month) {
    this.selectDisplayMode("day");
    this.updateDisplay(year, month);
  }
  onSelectYear(year, month) {
    this.selectDisplayMode(this.lastDisplay === "month" ? "month" : "day");
    this.currentYear = year;
    this.updateDisplay(year, month);
  }
  selectDisplayMode(mode) {
    this.lastDisplay = this.mode;
    this.mode = mode;
    this.cdr.detectChanges();
  }
  onSelectDate(date) {
    if (!this.poDate.validateDateRange(date, this.minDate, this.maxDate)) {
      return;
    }
    this.selectDate.emit(date);
  }
  onMouseEnter(day) {
    this.hoverDateSource.next(day);
  }
  onMouseLeave() {
    this.hoverDateSource.next(null);
  }
  onSelectToday() {
    this.selectTodayEvent.emit();
    this.onSelectDate(this.today);
    if (this.displayMonthNumber !== this.today.getMonth() || this.displayYear !== this.today.getFullYear()) {
      this.updateDisplay(this.today.getFullYear(), this.today.getMonth());
    }
    const todayIndex = this.displayDays.findIndex((day) => day instanceof Date && this.equalsDate(day, this.today));
    if (todayIndex !== -1) {
      this.focusedDayIndex = todayIndex;
      this.cdr.detectChanges();
    }
  }
  onFooterCloseCalendar() {
    this.restoreOriginalDisplay();
    this.closeCalendar.emit();
  }
  onHostKeydown(event) {
    if (event.key === "Tab" && event.shiftKey) {
      setTimeout(() => {
        const activeElement = document.activeElement;
        if (!this.elementRef.nativeElement.contains(activeElement)) {
          setTimeout(() => {
            this.restoreOriginalDisplay();
          }, 200);
        }
      }, 0);
    }
  }
  restoreOriginalDisplay() {
    if (!this.value) return;
    const originalDate = this.getDateToUse(this.value);
    const originalYear = originalDate.getFullYear();
    const originalMonth = originalDate.getMonth();
    if (this.displayYear !== originalYear || this.displayMonthNumber !== originalMonth) {
      this.updateDisplay(originalYear, originalMonth);
    }
  }
  onDayKeydown(event, day, index) {
    const key2 = event.key;
    const dayOfMonth = day.getDate();
    if (this.isSelectionKey(key2)) {
      this.handleSelectKey(day, index);
      event.preventDefault();
    } else if (this.handleNavigationKey(key2, index)) {
      event.preventDefault();
    } else if (this.handlePageNavigation(key2, event.shiftKey, dayOfMonth, index)) {
      event.preventDefault();
    } else if (key2 === "Escape") {
      event.preventDefault();
    } else if (key2 === "Tab") {
      this.setInitialFocusedDay();
    }
  }
  isSelectionKey(key2) {
    return key2 === "Enter" || key2 === " ";
  }
  handleSelectKey(day, index) {
    if (this.isDayDisabled(day)) {
      return;
    }
    this.onSelectDate(day);
    this.focusElement(index);
  }
  handleNavigationKey(key2, index) {
    let newIndex = this.getNextNavigationIndex(key2, index);
    if (newIndex !== -1 && newIndex < this.displayDays.length) {
      let newDate = this.displayDays[newIndex];
      if (!newDate) return false;
      if (newDate.getMonth() !== this.displayMonthNumber || newDate.getFullYear() !== this.displayYear) {
        return false;
      }
      if (this.isDayDisabled(newDate)) {
        const direction = this.getNavigationDirection(key2);
        newIndex = this.findNextAvailableDay(newIndex, direction);
        if (newIndex === -1) {
          return false;
        }
        newDate = this.displayDays[newIndex];
        if (!newDate || this.isDayDisabled(newDate)) {
          return false;
        }
      }
      this.focusedDayIndex = newIndex;
      this.cdr.detectChanges();
      this.focusElement(newIndex);
      return true;
    }
    return false;
  }
  getNextNavigationIndex(key2, index) {
    switch (key2) {
      case "ArrowUp":
        return index - 7;
      case "ArrowDown":
        return index + 7;
      case "ArrowRight":
        return index + 1;
      case "ArrowLeft":
        return index - 1;
      case "Home":
        return this.getFirstAvailableDayInWeek(index);
      case "End":
        return this.getLastAvailableDayInWeek(index);
      default:
        return -1;
    }
  }
  getFirstAvailableDayInWeek(index) {
    const weekStart = Math.floor(index / 7) * 7;
    const weekEnd = Math.min(weekStart + 7, this.displayDays.length);
    for (let i2 = weekStart; i2 < weekEnd; i2++) {
      const date = this.displayDays[i2];
      if (date instanceof Date && date.getMonth() === this.displayMonthNumber && !this.isDayDisabled(date)) {
        return i2;
      }
    }
    return weekStart;
  }
  getLastAvailableDayInWeek(index) {
    const weekStart = Math.floor(index / 7) * 7;
    const weekEnd = Math.min(weekStart + 7, this.displayDays.length);
    for (let i2 = weekEnd - 1; i2 >= weekStart; i2--) {
      const date = this.displayDays[i2];
      if (date instanceof Date && date.getMonth() === this.displayMonthNumber && !this.isDayDisabled(date)) {
        return i2;
      }
    }
    return weekStart;
  }
  getNavigationDirection(key2) {
    return key2 === "ArrowRight" || key2 === "ArrowDown" || key2 === "End" ? "forward" : "backward";
  }
  findNextAvailableDay(startIndex, direction) {
    const step = direction === "forward" ? 1 : -1;
    let index = startIndex + step;
    for (let i2 = 0; i2 < 100; i2++) {
      if (index < 0 || index >= this.displayDays.length) {
        break;
      }
      const date = this.displayDays[index];
      if (date instanceof Date && date.getMonth() === this.displayMonthNumber && !this.isDayDisabled(date)) {
        return index;
      }
      index += step;
    }
    return -1;
  }
  handlePageNavigation(key2, isShiftKey, dayOfMonth, index) {
    const directionMap = {
      PageUp: "up",
      PageDown: "down"
    };
    const direction = directionMap[key2];
    if (!direction) {
      return false;
    }
    const step = direction === "up" ? -1 : 1;
    let targetMonth = this.displayMonthNumber;
    let targetYear = this.displayYear;
    if (isShiftKey) {
      targetYear += step;
    } else {
      targetMonth += step;
      if (targetMonth < 0) {
        targetMonth = 11;
        targetYear -= 1;
      } else if (targetMonth > 11) {
        targetMonth = 0;
        targetYear += 1;
      }
    }
    if (!this.hasAvailableDaysInMonth(targetYear, targetMonth)) {
      return false;
    }
    this.applyPageNavigation(direction, isShiftKey);
    this.focusOnSameDayAndWeek(dayOfMonth, index);
    return true;
  }
  hasAvailableDaysInMonth(year, month) {
    const calendarArray = this.poCalendarService.monthDays(year, month);
    const monthDays = calendarArray.flat();
    return monthDays.some((day) => day instanceof Date && day.getMonth() === month && !this.isDayDisabled(day));
  }
  applyPageNavigation(direction, isShiftKey) {
    const step = direction === "up" ? -1 : 1;
    if (isShiftKey) {
      this.displayYear += step;
    } else {
      this.displayMonthNumber += step;
      if (this.displayMonthNumber < 0) {
        this.displayMonthNumber = 11;
        this.displayYear -= 1;
      } else if (this.displayMonthNumber > 11) {
        this.displayMonthNumber = 0;
        this.displayYear += 1;
      }
    }
    this.updateDisplay(this.displayYear, this.displayMonthNumber);
  }
  focusElement(index) {
    this.focusedDayIndex = index;
    this.cdr.detectChanges();
    setTimeout(() => {
      const element = this.queryDayElement(index);
      if (element instanceof HTMLElement) {
        element.focus();
      }
    }, 0);
  }
  focusOnSameDayAndWeek(dayOfMonth, currentIndex) {
    const currentWeekRow = Math.floor(currentIndex / 7);
    const dayOfWeek = currentIndex % 7;
    setTimeout(() => {
      const focusIndex = this.findTargetDayIndex(dayOfMonth, dayOfWeek, currentWeekRow);
      if (focusIndex !== -1) {
        let targetIndex = focusIndex;
        const targetDate = this.displayDays[targetIndex];
        if (targetDate instanceof Date && this.isDayDisabled(targetDate)) {
          targetIndex = this.getFirstAvailableDayInWeek(targetIndex);
        }
        this.focusedDayIndex = targetIndex;
        this.cdr.detectChanges();
        const element = this.queryDayElement(targetIndex);
        if (element instanceof HTMLElement) {
          element.focus();
        }
      }
    }, 0);
  }
  queryDayElement(index) {
    return this.elementRef.nativeElement.querySelector(`[data-day-index="${index}"]`);
  }
  findTargetDayIndex(dayOfMonth, dayOfWeek, startWeekRow) {
    const totalRows = Math.ceil(this.displayDays.length / 7);
    for (let row = 0; row < totalRows; row++) {
      const checkIndex = row * 7 + dayOfWeek;
      if (this.isValidCalendarDate(checkIndex, dayOfMonth)) return checkIndex;
    }
    for (let row = startWeekRow - 1; row >= 0; row--) {
      const checkIndex = row * 7 + dayOfWeek;
      if (this.isValidCalendarDate(checkIndex)) return checkIndex;
    }
    for (let row = startWeekRow + 1; row < totalRows; row++) {
      const checkIndex = row * 7 + dayOfWeek;
      if (this.isValidCalendarDate(checkIndex)) return checkIndex;
    }
    return -1;
  }
  isValidCalendarDate(index, requiredDayOfMonth) {
    if (index >= this.displayDays.length) return false;
    const date = this.displayDays[index];
    if (!(date instanceof Date) || date.getMonth() !== this.displayMonthNumber) {
      return false;
    }
    return requiredDayOfMonth === void 0 || date.getDate() === requiredDayOfMonth;
  }
  onClear() {
    this.selectDate.emit(void 0);
    const firstAvailableIndex = this.displayDays.findIndex((day) => day instanceof Date && day.getMonth() === this.displayMonthNumber && !this.isDayDisabled(day));
    if (firstAvailableIndex !== -1) {
      this.focusedDayIndex = firstAvailableIndex;
    }
    this.cdr.detectChanges();
  }
  getDayBackgroundColor(date) {
    return this.getDayColor(date, "background");
  }
  getDayForegroundColor(date) {
    return this.getDayColor(date, "foreground");
  }
  getBackgroundColor(displayValue, propertyValue) {
    return displayValue === propertyValue ? "po-calendar-box-background-selected" : "po-calendar-box-background";
  }
  getForegroundColor(displayValue, propertyValue) {
    return displayValue === propertyValue ? "po-calendar-box-foreground-selected" : "po-calendar-box-foreground";
  }
  isTodayUnavailable() {
    return this.minDate > this.today || this.maxDate < this.today;
  }
  isDayDisabled(date) {
    return !this.poDate.validateDateRange(date, this.minDate, this.maxDate);
  }
  getDayColor(date, type) {
    if (!date) return "";
    const prefix = `po-calendar-box-${type}`;
    if (date.getMonth() !== this.displayMonthNumber) {
      const isDisabled = !this.poDate.validateDateRange(date, this.minDate, this.maxDate);
      return isDisabled ? `${prefix}-other-month-disabled` : `${prefix}-other-month`;
    }
    if (this.range && this.selectedValue) {
      const rangeColor = this.getRangeColor(date, prefix, type);
      if (rangeColor) return rangeColor;
    }
    if (!this.range && this.equalsDate(date, this.today) && this.equalsDate(date, this.date)) {
      const prefix2 = `po-calendar-box-${type}`;
      return `${prefix2}-today-selected`;
    }
    if (!this.range && this.equalsDate(date, this.date)) {
      return this.getColorForDate(date, type);
    }
    if (this.equalsDate(date, this.today)) {
      return this.getColorForToday(date, type);
    }
    return this.getColorForDefaultDate(date, type);
  }
  getRangeColor(date, prefix, type) {
    const {
      start: start2,
      end: end2
    } = this.selectedValue;
    if (start2 && this.equalsDate(date, start2) || end2 && this.equalsDate(date, end2)) {
      return this.getColorForDate(date, type);
    }
    if (!start2) return;
    if (end2 && date > start2 && date < end2) {
      return this.getColorForDateRange(date, type);
    }
    if (!end2 && date > start2 && date < this.hoverValue) {
      return `${prefix}-hover`;
    }
  }
  getColorForDate(date, type) {
    const prefix = `po-calendar-box-${type}`;
    return this.poDate.validateDateRange(date, this.minDate, this.maxDate) ? `${prefix}-selected` : `${prefix}-selected-disabled`;
  }
  getColorForDefaultDate(date, type) {
    const prefix = `po-calendar-box-${type}`;
    return this.poDate.validateDateRange(date, this.minDate, this.maxDate) ? prefix : `${prefix}-disabled`;
  }
  getColorForToday(date, type) {
    const prefix = `po-calendar-box-${type}`;
    return this.poDate.validateDateRange(date, this.minDate, this.maxDate) ? `${prefix}-today` : `${prefix}-today-disabled`;
  }
  getColorForDateRange(date, type) {
    const prefix = `po-calendar-box-${type}`;
    return this.poDate.validateDateRange(date, this.minDate, this.maxDate) ? `${prefix}-in-range` : `${prefix}-in-range-disabled`;
  }
  getColorState(date, prefix, state2) {
    const isValid = this.poDate.validateDateRange(date, this.minDate, this.maxDate);
    return isValid ? `${prefix}-${state2}` : `${prefix}-${state2}-disabled`;
  }
  equalsDate(d1, d2) {
    try {
      return !!d1 && !!d2 && d1.getFullYear() === d2.getFullYear() && d1.getMonth() === d2.getMonth() && d1.getDate() === d2.getDate();
    } catch (e2) {
      return false;
    }
  }
  getDecadeArray(year) {
    this.displayDecade = [];
    let startYear = year;
    if (year % 10 !== 0) {
      while (startYear % 10 !== 0) startYear--;
    }
    this.updateDecade(startYear);
  }
  updateDecade(startYear) {
    this.displayStartDecade = startYear;
    this.displayFinalDecade = startYear + 9;
    this.addAllYearsInDecade(startYear);
  }
  addAllYearsInDecade(startYear) {
    for (let i2 = startYear; i2 < startYear + 10; i2++) {
      this.displayDecade.push(i2);
    }
  }
  trackByYear(index, year) {
    return year;
  }
  trackByMonth(index, month) {
    return index;
  }
  trackByDay(index, day) {
    if (!day || !(day instanceof Date)) {
      return `invalid-${index}`;
    }
    return `${index}:${day.getFullYear()}-${day.getMonth()}-${day.getDate()}`;
  }
  trackByWeekDay(index, weekDay) {
    return `${index}:${weekDay}`;
  }
  static ɵfac = function PoCalendarWrapperComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoCalendarWrapperComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoCalendarWrapperComponent,
    selectors: [["po-calendar-wrapper"]],
    hostBindings: function PoCalendarWrapperComponent_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("keydown", function PoCalendarWrapperComponent_keydown_HostBindingHandler($event) {
          return ctx.onHostKeydown($event);
        });
      }
    },
    inputs: {
      value: [0, "p-value", "value"],
      mode: [0, "p-mode", "mode"],
      responsive: [0, "p-responsive", "responsive"],
      partType: [0, "p-part-type", "partType"],
      range: [0, "p-range", "range"],
      activateDate: [0, "p-activate-date", "activateDate"],
      selectedValue: [0, "p-selected-value", "selectedValue"],
      minDate: [0, "p-min-date", "minDate"],
      maxDate: [0, "p-max-date", "maxDate"],
      hoverValue: [0, "p-hover-value", "hoverValue"],
      size: [0, "p-size", "size"],
      hideTodayButton: [0, "p-hide-today-button", "hideTodayButton"],
      hasPresets: [0, "p-presets", "hasPresets"],
      headerTemplate: [0, "p-header-template", "headerTemplate"],
      locale: [0, "p-locale", "locale"]
    },
    outputs: {
      headerChange: "p-header-change",
      selectDate: "p-select-date",
      selectTodayEvent: "p-select-today",
      hoverDate: "p-hover-date",
      closeCalendar: "p-close-calendar"
    },
    standalone: false,
    features: [ɵɵProvidersFeature([PoCalendarService]), ɵɵNgOnChangesFeature],
    decls: 7,
    vars: 4,
    consts: [["internalComboTemplate", ""], ["monthCombo", ""], ["yearCombo", ""], [1, "po-calendar-wrapper"], [3, "p-label-clear", "p-label-today", "p-hide-today-button", "p-size", "p-responsive", "p-today-disabled"], [3, "p-display-month", "p-display-year", "p-template-context", "p-header-template", "p-size", "p-locale"], [1, "po-calendar-content"], [1, "po-calendar-labels"], [1, "po-calendar-label"], [1, "po-calendar-content-list-day"], ["attr-calendar", "", 1, "po-calendar-day", 3, "ngClass", "tabindex"], [3, "p-previous", "p-next", "p-display-month", "p-display-year", "p-template-context", "p-header-template", "p-size", "p-locale"], ["attr-calendar", "", 1, "po-calendar-day", 3, "click", "mouseenter", "mouseleave", "keydown", "ngClass", "tabindex"], [3, "ngClass"], [3, "p-previous", "p-next", "p-template-context", "p-header-template", "p-size", "p-locale"], [1, "po-calendar-content-list-month"], ["attr-calendar", "", 1, "po-calendar-month", 3, "ngClass"], ["attr-calendar", "", 1, "po-calendar-month", 3, "click", "ngClass"], [1, "po-calendar-content-list-year"], ["attr-calendar", "", 1, "po-calendar-year", 3, "ngClass"], ["attr-calendar", "", 1, "po-calendar-year", 3, "click", "ngClass"], [3, "p-clear", "p-select-today", "p-close-calendar", "p-label-clear", "p-label-today", "p-hide-today-button", "p-size", "p-responsive", "p-today-disabled"], [1, "po-combo-first", 3, "p-change", "p-blur", "name", "p-clean", "p-remove-initial-filter", "ngModel", "p-options", "p-size"], [1, "po-combo-last", 3, "p-change", "p-blur", "name", "p-clean", "p-remove-initial-filter", "ngModel", "p-options", "p-size"]],
    template: function PoCalendarWrapperComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "div", 3);
        ɵɵconditionalCreate(1, PoCalendarWrapperComponent_Conditional_1_Template, 9, 1, "ng-container");
        ɵɵconditionalCreate(2, PoCalendarWrapperComponent_Conditional_2_Template, 9, 5, "ng-container");
        ɵɵconditionalCreate(3, PoCalendarWrapperComponent_Conditional_3_Template, 9, 5, "ng-container");
        ɵɵconditionalCreate(4, PoCalendarWrapperComponent_Conditional_4_Template, 1, 6, "po-calendar-footer", 4);
        ɵɵelementEnd();
        ɵɵtemplate(5, PoCalendarWrapperComponent_ng_template_5_Template, 4, 12, "ng-template", null, 0, ɵɵtemplateRefExtractor);
      }
      if (rf & 2) {
        ɵɵadvance();
        ɵɵconditional(ctx.isDayVisible ? 1 : -1);
        ɵɵadvance();
        ɵɵconditional(ctx.isMonthVisible ? 2 : -1);
        ɵɵadvance();
        ɵɵconditional(ctx.isYearVisible ? 3 : -1);
        ɵɵadvance();
        ɵɵconditional(!ctx.responsive || ctx.responsive && !ctx.hasPresets ? 4 : -1);
      }
    },
    dependencies: [NgClass, NgControlStatus, NgModel, PoComboComponent, PoCalendarFooterComponent, PoCalendarHeaderComponent],
    encapsulation: 2,
    changeDetection: 0
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoCalendarWrapperComponent, [{
    type: Component,
    args: [{
      selector: "po-calendar-wrapper",
      providers: [PoCalendarService],
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: `<div class="po-calendar-wrapper">
  @if (isDayVisible) {
    <ng-container>
      @if (templateContext) {
        <po-calendar-header
          (p-previous)="onPreviousMonth()"
          (p-next)="onNextMonth()"
          [p-display-month]="displayMonth"
          [p-display-year]="displayYear"
          [p-template-context]="templateContext"
          [p-header-template]="headerTemplate || internalComboTemplate"
          [p-size]="size"
          [p-locale]="locale"
        >
        </po-calendar-header>
      }

      <div class="po-calendar-content">
        <div class="po-calendar-labels">
          @for (weekDay of displayWeekDays; track trackByWeekDay($index, weekDay)) {
            <div class="po-calendar-label">
              {{ weekDay }}
            </div>
          }
        </div>

        <div class="po-calendar-content-list-day">
          @for (day of displayDays; track trackByDay($index, day); let i = $index) {
            <div
              class="po-calendar-day"
              [ngClass]="getDayBackgroundColor(day)"
              (click)="onSelectDate(day)"
              (mouseenter)="onMouseEnter(day)"
              (mouseleave)="onMouseLeave()"
              (keydown)="onDayKeydown($event, day, i)"
              [tabindex]="getDayTabIndex(day, i)"
              attr-calendar
              [attr.data-other-month]="day.getMonth() !== displayMonthNumber"
              [attr.data-day-index]="i"
            >
              <span [ngClass]="getDayForegroundColor(day)">
                {{ day.getDate() }}
              </span>
            </div>
          }
        </div>
      </div>
    </ng-container>
  }

  @if (isMonthVisible) {
    <ng-container>
      <po-calendar-header
        (p-previous)="updateYear(-1)"
        (p-next)="updateYear(1)"
        [p-template-context]="templateContext"
        [p-header-template]="headerTemplate || internalComboTemplate"
        [p-size]="size"
        [p-locale]="locale"
      >
      </po-calendar-header>

      <div class="po-calendar-content">
        <div class="po-calendar-labels">
          <div class="po-calendar-label">
            {{ monthLabel }}
          </div>
        </div>
        <div class="po-calendar-content-list-month">
          @for (month of displayMonths; track trackByMonth($index, month); let i = $index) {
            <div
              class="po-calendar-month"
              [ngClass]="getBackgroundColor(i, displayMonthNumber)"
              (click)="onSelectMonth(displayYear, i)"
              attr-calendar
            >
              <span [ngClass]="getForegroundColor(i, displayMonthNumber)">
                {{ month }}
              </span>
            </div>
          }
        </div>
      </div>
    </ng-container>
  }

  @if (isYearVisible) {
    <ng-container>
      <po-calendar-header
        (p-previous)="updateYear(-10)"
        (p-next)="updateYear(10)"
        [p-template-context]="templateContext"
        [p-header-template]="headerTemplate || internalComboTemplate"
        [p-size]="size"
        [p-locale]="locale"
      >
      </po-calendar-header>

      <div class="po-calendar-content">
        <div class="po-calendar-labels">
          <div class="po-calendar-label">
            {{ yearLabel }}
          </div>
        </div>

        <div class="po-calendar-content-list-year">
          @for (year of displayDecade; track trackByYear($index, year)) {
            <div
              class="po-calendar-year"
              [ngClass]="getBackgroundColor(year, currentYear)"
              (click)="onSelectYear(year, displayMonthNumber)"
              attr-calendar
            >
              <span [ngClass]="getForegroundColor(year, currentYear)">
                {{ year }}
              </span>
            </div>
          }
        </div>
      </div>
    </ng-container>
  }
  @if (!responsive || (responsive && !hasPresets)) {
    <po-calendar-footer
      [p-label-clear]="displayToClean"
      [p-label-today]="displayToday"
      [p-hide-today-button]="hideTodayButton"
      [p-size]="size"
      [p-responsive]="responsive"
      [p-today-disabled]="isTodayUnavailable()"
      (p-clear)="onClear()"
      (p-select-today)="onSelectToday()"
      (p-close-calendar)="onFooterCloseCalendar()"
    ></po-calendar-footer>
  }
</div>

<ng-template #internalComboTemplate let-combo>
  <po-combo
    #monthCombo
    class="po-combo-first"
    [name]="'month-' + comboKey"
    [p-clean]="false"
    [p-remove-initial-filter]="true"
    [ngModel]="combo.monthIndex"
    [p-options]="combo.monthsOptions"
    (p-change)="combo.updateDate(combo.year, $event)"
    (p-blur)="combo.onComboBlur(monthCombo)"
    [p-size]="size"
  >
  </po-combo>

  <po-combo
    #yearCombo
    class="po-combo-last"
    [name]="'year-' + comboKey"
    [p-clean]="false"
    [p-remove-initial-filter]="true"
    [ngModel]="combo.year"
    [p-options]="combo.yearsOptions"
    (p-change)="combo.updateDate($event, combo.monthIndex)"
    (p-blur)="combo.onComboBlur(yearCombo)"
    [p-size]="size"
  >
  </po-combo>
</ng-template>
`
    }]
  }], null, {
    value: [{
      type: Input,
      args: ["p-value"]
    }],
    mode: [{
      type: Input,
      args: ["p-mode"]
    }],
    responsive: [{
      type: Input,
      args: ["p-responsive"]
    }],
    partType: [{
      type: Input,
      args: ["p-part-type"]
    }],
    range: [{
      type: Input,
      args: ["p-range"]
    }],
    activateDate: [{
      type: Input,
      args: ["p-activate-date"]
    }],
    selectedValue: [{
      type: Input,
      args: ["p-selected-value"]
    }],
    minDate: [{
      type: Input,
      args: ["p-min-date"]
    }],
    maxDate: [{
      type: Input,
      args: ["p-max-date"]
    }],
    hoverValue: [{
      type: Input,
      args: ["p-hover-value"]
    }],
    size: [{
      type: Input,
      args: ["p-size"]
    }],
    hideTodayButton: [{
      type: Input,
      args: ["p-hide-today-button"]
    }],
    hasPresets: [{
      type: Input,
      args: ["p-presets"]
    }],
    headerTemplate: [{
      type: Input,
      args: ["p-header-template"]
    }],
    locale: [{
      type: Input,
      args: ["p-locale"]
    }],
    headerChange: [{
      type: Output,
      args: ["p-header-change"]
    }],
    selectDate: [{
      type: Output,
      args: ["p-select-date"]
    }],
    selectTodayEvent: [{
      type: Output,
      args: ["p-select-today"]
    }],
    hoverDate: [{
      type: Output,
      args: ["p-hover-date"]
    }],
    closeCalendar: [{
      type: Output,
      args: ["p-close-calendar"]
    }],
    onHostKeydown: [{
      type: HostListener,
      args: ["keydown", ["$event"]]
    }]
  });
})();
var PoCalendarPresetListComponent = class _PoCalendarPresetListComponent {
  poCalendarLangService = inject(PoCalendarLangService);
  _locale;
  size;
  presets = [];
  selectedPreset = null;
  responsive = false;
  set locale(value) {
    this._locale = value;
    this.poCalendarLangService.setLanguage(value);
  }
  get locale() {
    return this._locale;
  }
  closeCalendar = new EventEmitter();
  selectPreset = new EventEmitter();
  presetButtons;
  focusedIndex = 0;
  getDisplayLabel(preset) {
    return this.poCalendarLangService.getPresetLabel(preset.label);
  }
  isSelected(preset) {
    return this.selectedPreset === preset.label;
  }
  onKeydown(event, index) {
    const buttons = this.getPresetButtons();
    if (event.key === "ArrowDown") {
      event.preventDefault();
      const nextIndex = index < this.presets.length - 1 ? index + 1 : index;
      this.focusPreset(buttons, nextIndex);
    } else if (event.key === "ArrowUp") {
      event.preventDefault();
      const prevIndex = index > 0 ? index - 1 : index;
      this.focusPreset(buttons, prevIndex);
    } else if (event.key === "Tab" && !event.shiftKey) {
      this.resetFocusToFirst();
    } else if (event.key === "Tab" && event.shiftKey && !this.responsive) {
      this.closeCalendar.emit();
    }
  }
  resetFocusToFirst() {
    this.focusedIndex = 0;
  }
  isPresetDisabled(preset) {
    return preset.isDisabled === true;
  }
  onPresetClick(preset) {
    if (this.isPresetDisabled(preset)) {
      return;
    }
    const {
      start: start2,
      end: end2
    } = preset.dateRange(/* @__PURE__ */ new Date());
    this.selectPreset.emit({
      label: preset.label,
      start: start2,
      end: end2
    });
  }
  focusPreset(buttons, index) {
    if (buttons[index]) {
      this.focusedIndex = index;
      buttons[index].focus();
    }
  }
  getPresetButtons() {
    return this.presetButtons.toArray();
  }
  static ɵfac = function PoCalendarPresetListComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoCalendarPresetListComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoCalendarPresetListComponent,
    selectors: [["po-calendar-preset-list"]],
    viewQuery: function PoCalendarPresetListComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c51, 5);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.presetButtons = _t);
      }
    },
    inputs: {
      size: [0, "p-size", "size"],
      presets: [0, "p-presets", "presets"],
      selectedPreset: [0, "p-selected-preset", "selectedPreset"],
      responsive: [0, "p-responsive", "responsive"],
      locale: [0, "p-locale", "locale"]
    },
    outputs: {
      closeCalendar: "p-close-calendar",
      selectPreset: "p-select-preset"
    },
    standalone: false,
    decls: 3,
    vars: 0,
    consts: [["presetButton", ""], ["role", "listbox", 1, "po-calendar-preset-list"], ["p-kind", "tertiary", "role", "option", 1, "po-calendar-preset-item", 3, "po-calendar-preset-item-selected", "p-size", "p-label", "p-disabled", "p-tabindex"], ["p-kind", "tertiary", "role", "option", 1, "po-calendar-preset-item", 3, "click", "keydown", "p-size", "p-label", "p-disabled", "p-tabindex"]],
    template: function PoCalendarPresetListComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "div", 1);
        ɵɵrepeaterCreate(1, PoCalendarPresetListComponent_For_2_Template, 2, 7, "po-button", 2, _forTrack0);
        ɵɵelementEnd();
      }
      if (rf & 2) {
        ɵɵadvance();
        ɵɵrepeater(ctx.presets);
      }
    },
    dependencies: [PoButtonComponent],
    encapsulation: 2,
    changeDetection: 0
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoCalendarPresetListComponent, [{
    type: Component,
    args: [{
      selector: "po-calendar-preset-list",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: '<div class="po-calendar-preset-list" role="listbox">\n  @for (preset of presets; track preset.label; let i = $index) {\n    <po-button\n      #presetButton\n      class="po-calendar-preset-item"\n      [class.po-calendar-preset-item-selected]="isSelected(preset)"\n      p-kind="tertiary"\n      role="option"\n      [p-size]="size"\n      [p-label]="getDisplayLabel(preset)"\n      [p-disabled]="isPresetDisabled(preset)"\n      [attr.aria-selected]="isSelected(preset)"\n      [p-tabindex]="i === focusedIndex ? 0 : -1"\n      (click)="onPresetClick(preset)"\n      (keydown)="onKeydown($event, i)"\n    >\n    </po-button>\n  }\n</div>\n'
    }]
  }], null, {
    size: [{
      type: Input,
      args: ["p-size"]
    }],
    presets: [{
      type: Input,
      args: ["p-presets"]
    }],
    selectedPreset: [{
      type: Input,
      args: ["p-selected-preset"]
    }],
    responsive: [{
      type: Input,
      args: ["p-responsive"]
    }],
    locale: [{
      type: Input,
      args: ["p-locale"]
    }],
    closeCalendar: [{
      type: Output,
      args: ["p-close-calendar"]
    }],
    selectPreset: [{
      type: Output,
      args: ["p-select-preset"]
    }],
    presetButtons: [{
      type: ViewChildren,
      args: ["presetButton"]
    }]
  });
})();
var providers$7 = [{
  provide: NG_VALUE_ACCESSOR,
  // eslint-disable-next-line
  useExisting: forwardRef(() => PoCalendarComponent),
  multi: true
}, {
  provide: NG_VALIDATORS,
  // eslint-disable-next-line
  useExisting: forwardRef(() => PoCalendarComponent),
  multi: true
}];
var PoCalendarComponent = class _PoCalendarComponent extends PoCalendarBaseComponent {
  timerComponent;
  yearOptions;
  monthOptions;
  changeDetector = inject(ChangeDetectorRef);
  poCalendarService = inject(PoCalendarService);
  poCalendarLangService = inject(PoCalendarLangService);
  hoverValue;
  displayToClean;
  displayMonths = [];
  displayYears = [];
  focusedIndex = 0;
  selectedIndexMonth = null;
  selectedIndexYear = null;
  selectedMonth;
  selectedYear;
  isTodayPresetSelection = false;
  _isRange = signal(false, ...ngDevMode ? [{
    debugName: "_isRange"
  }] : (
    /* istanbul ignore next */
    []
  ));
  _rangePresetsValue = signal(false, ...ngDevMode ? [{
    debugName: "_rangePresetsValue"
  }] : (
    /* istanbul ignore next */
    []
  ));
  _rangePresetOptionsValue = signal(void 0, ...ngDevMode ? [{
    debugName: "_rangePresetOptionsValue"
  }] : (
    /* istanbul ignore next */
    []
  ));
  _rangePresetsOrderValue = signal("asc", ...ngDevMode ? [{
    debugName: "_rangePresetsOrderValue"
  }] : (
    /* istanbul ignore next */
    []
  ));
  _minDateValue = signal(void 0, ...ngDevMode ? [{
    debugName: "_minDateValue"
  }] : (
    /* istanbul ignore next */
    []
  ));
  _maxDateValue = signal(void 0, ...ngDevMode ? [{
    debugName: "_maxDateValue"
  }] : (
    /* istanbul ignore next */
    []
  ));
  effectivePresets = computed(() => {
    const isRange = this._isRange();
    const rangePresets = this._rangePresetsValue();
    const rangePresetOptions = this._rangePresetOptionsValue();
    const rangePresetsOrder = this._rangePresetsOrderValue();
    const minDate = this._minDateValue();
    const maxDate = this._maxDateValue();
    if (!isRange) {
      return [];
    }
    const hasCustomPresets = rangePresetOptions !== void 0 && rangePresetOptions.length > 0;
    let defaultPresets = [];
    if (Array.isArray(rangePresets)) {
      const allowedKeys = new Set(rangePresets.map((k2) => k2.toLowerCase()));
      allowedKeys.add("today");
      defaultPresets = PO_CALENDAR_DEFAULT_RANGE_PRESETS.filter((p) => allowedKeys.has(p.label.toLowerCase()));
    } else if (rangePresets) {
      defaultPresets = [...PO_CALENDAR_DEFAULT_RANGE_PRESETS];
    }
    const customPresets = hasCustomPresets ? rangePresetOptions : [];
    const combined = [...defaultPresets, ...customPresets];
    if (combined.length > 0 && !combined.some((p) => p.label.toLowerCase() === "today")) {
      const todayPreset = PO_CALENDAR_DEFAULT_RANGE_PRESETS.find((p) => p.label === "today");
      if (todayPreset) {
        combined.unshift(todayPreset);
      }
    }
    if (combined.length === 0) {
      return [];
    }
    const sorted = this.sortPresetsByTemporality(combined, rangePresetsOrder);
    return this.enrichPresetsWithDisabledState(sorted, minDate, maxDate);
  }, ...ngDevMode ? [{
    debugName: "effectivePresets"
  }] : (
    /* istanbul ignore next */
    []
  ));
  constructor() {
    const poDate = inject(PoDateService);
    const languageService = inject(PoLanguageService);
    super(poDate, languageService);
  }
  get isResponsive() {
    return isMobile();
  }
  ngOnInit() {
    this.setActivateDate();
    this.displayToClean = this.poCalendarLangService.getToCleanLabel();
    if (this.mode === "month-year" || this.mode === "year") {
      this.initializeYearAndMonthMode();
    }
  }
  ngOnChanges(changes) {
    if (changes.minDate || changes.maxDate) {
      this.setActivateDate();
    }
  }
  ngDoCheck() {
    this._isRange.set(this.isRange);
    this._rangePresetsValue.set(this.rangePresets);
    this._rangePresetOptionsValue.set(this.rangePresetOptions);
    this._rangePresetsOrderValue.set(this.rangePresetsOrder);
    this._minDateValue.set(this.minDate);
    this._maxDateValue.set(this.maxDate);
  }
  isMonthDisabled(monthIndex) {
    if (!this.selectedYear) return false;
    const year = this.selectedYear;
    const month = monthIndex + 1;
    if (this.minDate) {
      const minYear = this.minDate.getFullYear();
      const minMonth = this.minDate.getMonth() + 1;
      if (year < minYear || year === minYear && month < minMonth) {
        return true;
      }
    }
    if (this.maxDate) {
      const maxYear = this.maxDate.getFullYear();
      const maxMonth = this.maxDate.getMonth() + 1;
      if (year > maxYear || year === maxYear && month > maxMonth) {
        return true;
      }
    }
    return false;
  }
  isYearDisabled(year) {
    const selectedMonth = this.selectedMonth;
    const minYear = this.minDate?.getFullYear();
    const minMonth = this.minDate?.getMonth() + 1;
    const maxYear = this.maxDate?.getFullYear();
    const maxMonth = this.maxDate?.getMonth() + 1;
    if (selectedMonth !== void 0 && selectedMonth !== null) {
      const month = selectedMonth;
      if (minYear && (year < minYear || year === minYear && month < minMonth)) {
        return true;
      }
      if (maxYear !== void 0 && (year > maxYear || year === maxYear && month > maxMonth)) {
        return true;
      }
      return false;
    }
    if (minYear !== void 0 && year < minYear) return true;
    if (maxYear !== void 0 && year > maxYear) return true;
    return false;
  }
  onKeydownMonth(event, index) {
    const monthOptions = this.getMonthOptions();
    const yearOptions = this.getYearOptions();
    if (event.key === "Enter" || event.code === "Space") {
      this.selectMonth(index, event);
    }
    if (event.key === "ArrowDown") {
      event.preventDefault();
      const nextIndex = index < monthOptions.length - 1 ? index + 1 : index;
      this.selectMonth(nextIndex, event);
    } else if (event.key === "ArrowUp") {
      event.preventDefault();
      const prevIndex = index > 0 ? index - 1 : index;
      this.selectMonth(prevIndex, event);
    } else if (event.key === "Tab" && !event.shiftKey) {
      event.preventDefault();
      const selected = yearOptions[this.selectedIndexYear];
      const enabledOptions = yearOptions.filter((btn) => !btn.disabled);
      (selected || enabledOptions[0])?.focus();
    } else if (event.key === "Tab" && event.shiftKey) {
      this.focusedIndex = 0;
      this.close.emit();
    }
  }
  onKeydownYear(event, index) {
    const monthOptions = this.getMonthOptions();
    const yearOptions = this.getYearOptions();
    if (event.key === "Enter" || event.code === "Space") {
      this.selectYear(index, event);
    }
    if (event.key === "ArrowDown") {
      event.preventDefault();
      const nextIndex = index < yearOptions.length - 1 ? index + 1 : index;
      this.selectYear(nextIndex, event);
    } else if (event.key === "ArrowUp") {
      event.preventDefault();
      const prevIndex = index > 0 ? index - 1 : index;
      this.selectYear(prevIndex, event);
    } else if (event.key === "Tab" && !event.shiftKey) {
      this.focusedIndex = 0;
      this.close.emit();
    } else if (event.key === "Tab" && event.shiftKey && this.mode === "month-year") {
      event.preventDefault();
      const selected = monthOptions[this.selectedIndexMonth];
      selected ? selected.focus() : monthOptions[0].focus();
    } else if (event.key === "Tab" && event.shiftKey && this.mode === "year") {
      this.close.emit();
    }
  }
  selectMonth(index, event, selected = false) {
    const monthOptions = this.getMonthOptions();
    const isSelectAction = event?.key === "Enter" || event?.code === "Space" || selected;
    if (isSelectAction) {
      this.selectedIndexMonth = index;
      this.selectedMonth = index + 1;
      this.updateModel(this.selectedMonth);
    }
    if (monthOptions[index]) {
      this.focusedIndex = index;
      monthOptions[index].focus();
    }
  }
  selectYear(index, event, selected = false, year) {
    const yearOptions = this.getYearOptions();
    const isSelectAction = event?.key === "Enter" || event?.code === "Space" || selected;
    if (isSelectAction) {
      this.selectedIndexYear = index;
      this.selectedYear = year;
      this.updateModel(this.selectedYear);
    }
    if (yearOptions[index]) {
      this.focusedIndex = index;
      yearOptions[index].focus();
    }
  }
  getMonthOptions() {
    return this.monthOptions.toArray();
  }
  getYearOptions() {
    return this.yearOptions.toArray();
  }
  initializeYearAndMonthMode() {
    this.displayMonths = this.poCalendarLangService.getMonthsArray();
    const currentYear = (/* @__PURE__ */ new Date()).getFullYear();
    this.displayYears = Array.from({
      length: this.yearRangeLimit * 2 + 1
    }, (_, i2) => currentYear - this.yearRangeLimit + i2);
  }
  getActivateDate(partType) {
    if (this.isRange && this.activateDate) {
      return this.activateDate.start;
    } else {
      return this.activateDate;
    }
  }
  getValue(partType) {
    if (this.isRange && this.value) {
      return this.value.start;
    } else {
      return this.value;
    }
  }
  onSelectDate(selectedDate, partType) {
    this.selectedPresetLabel = null;
    if (selectedDate === "" || selectedDate === void 0) {
      this.value = null;
      this.updateModel("");
      this.change.emit("");
      if (this.isDateTime && this.timerComponent) {
        this.timerComponent.writeValue(null);
        this.changeTime.emit("");
      }
      this.changeDetector.markForCheck();
      return;
    }
    let newValue;
    if (this.isRange) {
      newValue = this.getValueFromSelectedDate(selectedDate);
      this.activateDate = {
        start: newValue.start,
        end: newValue.end || newValue.start
      };
    } else {
      newValue = selectedDate;
      this.setActivateDate(selectedDate);
    }
    this.value = newValue;
    const newModel = this.convertDateToISO(this.value);
    this.updateModel(newModel);
    if (this.isDateTime && this.timerComponent && this.isTodayPresetSelection) {
      this.close.emit();
      this.change.emit(newModel);
      const now = /* @__PURE__ */ new Date();
      const hours = ("0" + now.getHours()).slice(-2);
      const minutes = ("0" + now.getMinutes()).slice(-2);
      const seconds = ("0" + now.getSeconds()).slice(-2);
      const currentTime = this.showSeconds ? `${hours}:${minutes}:${seconds}` : `${hours}:${minutes}`;
      this.timerComponent.writeValue(currentTime);
      this.changeTime.emit(currentTime);
      this.isTodayPresetSelection = false;
    } else {
      this.change.emit(newModel);
    }
  }
  onHoverDate(date) {
    this.hoverValue = date;
  }
  onSelectTodayPreset() {
    this.isTodayPresetSelection = true;
  }
  onHeaderChange({
    month,
    year
  }, partType) {
    this.updateActivateDateFromHeaderChange(month, year, partType);
    this.changeMonthYear.emit({
      month,
      year
    });
  }
  onCloseCalendar() {
    if (this.isDateTime) {
      return;
    }
    this.change.emit(this.value);
    this.close.emit();
  }
  onTimeChange(time) {
    this.changeTime.emit(time);
  }
  onTimerBoundaryTab(event) {
    this.timerBoundaryTab.emit(event);
  }
  registerOnChange(fn) {
    this.propagateChange = fn;
  }
  registerOnTouched(func) {
    this.onTouched = func;
  }
  validate(c) {
    return null;
  }
  writeValue(value) {
    if (value) {
      this.writeDate(value);
    } else {
      this.value = null;
      this.selectedMonth = null;
      this.selectedYear = null;
      this.selectedIndexMonth = null;
      this.selectedIndexYear = null;
    }
    const activateDate = this.getValidateStartDate(value);
    this.setActivateDate(activateDate);
    this.changeDetector.markForCheck();
  }
  onPresetSelected(event) {
    const start2 = this.clampDate(event.start, this.minDate, this.maxDate);
    const end2 = this.clampDate(event.end, this.minDate, this.maxDate);
    if (start2 > end2) {
      return;
    }
    this.selectedPresetLabel = event.label;
    this.value = {
      start: start2,
      end: end2
    };
    this.activateDate = {
      start: start2,
      end: end2
    };
    const newModel = this.convertDateToISO(this.value);
    this.updateModel(newModel);
    this.change.emit(newModel);
    this.changeDetector.markForCheck();
  }
  sortPresetsByTemporality(presets, order) {
    const today = /* @__PURE__ */ new Date();
    const todayStart = new Date(today.getFullYear(), today.getMonth(), today.getDate(), 0, 0, 0, 0);
    const todayTime = todayStart.getTime();
    const getGroup = (startDate) => {
      const startTime = new Date(startDate.getFullYear(), startDate.getMonth(), startDate.getDate(), 0, 0, 0, 0).getTime();
      if (startTime > todayTime) {
        return 0;
      }
      if (startTime < todayTime) {
        return 2;
      }
      return 1;
    };
    const sorted = [...presets].sort((a, b) => {
      const rangeA = a.dateRange(today);
      const rangeB = b.dateRange(today);
      const startA = new Date(rangeA.start);
      const startB = new Date(rangeB.start);
      const groupA = getGroup(startA);
      const groupB = getGroup(startB);
      if (groupA !== groupB) {
        return groupA - groupB;
      }
      const distA = Math.abs(startA.getTime() - todayTime);
      const distB = Math.abs(startB.getTime() - todayTime);
      return distA - distB;
    });
    const resolvedOrder = order ?? this.rangePresetsOrder;
    return resolvedOrder === "desc" ? sorted.reverse() : sorted;
  }
  normalizeDate(date) {
    return new Date(date.getFullYear(), date.getMonth(), date.getDate());
  }
  enrichPresetsWithDisabledState(presets, minDateInput, maxDateInput) {
    const today = /* @__PURE__ */ new Date();
    const resolvedMinDate = minDateInput ?? this.minDate;
    const resolvedMaxDate = maxDateInput ?? this.maxDate;
    const minDate = resolvedMinDate ? this.normalizeDate(resolvedMinDate) : void 0;
    const maxDate = resolvedMaxDate ? this.normalizeDate(resolvedMaxDate) : void 0;
    return presets.map((preset) => {
      const range = preset.dateRange(today);
      const start2 = this.normalizeDate(new Date(range.start));
      const end2 = this.normalizeDate(new Date(range.end));
      const isBeforeMin = minDate ? start2 < minDate : false;
      const isAfterMax = maxDate ? end2 > maxDate : false;
      const isDisabled = isBeforeMin || isAfterMax;
      return __spreadProps(__spreadValues({}, preset), {
        isDisabled
      });
    });
  }
  clampDate(date, min3, max3) {
    let result = new Date(date);
    if (min3 && result < min3) {
      result = new Date(min3);
    }
    if (max3 && result > max3) {
      result = new Date(max3);
    }
    return result;
  }
  getValidateStartDate(value) {
    if (this.isRange) {
      return value?.start || null;
    } else if (value instanceof Date || typeof value === "string") {
      return value;
    }
    return null;
  }
  getValueFromSelectedDate(selectedDate) {
    if (!this.value?.start || this.value.start > selectedDate || this.value.end && this.value.start) {
      return {
        start: new Date(selectedDate),
        end: null
      };
    }
    return {
      start: new Date(this.value.start),
      end: new Date(selectedDate)
    };
  }
  updateActivateDateFromHeaderChange(month, year, partType) {
    if (!this.isRange || !this.activateDate) {
      return;
    }
    if (partType === "start") {
      const currentStart = this.activateDate.start instanceof Date ? this.activateDate.start : /* @__PURE__ */ new Date();
      const newStart = this.buildDateWithMonthYear(currentStart, month - 1, year);
      this.activateDate = {
        start: newStart,
        end: this.activateDate.end
      };
      return;
    }
    if (partType === "end") {
      const currentEnd = this.activateDate.end instanceof Date ? this.activateDate.end : /* @__PURE__ */ new Date();
      const newEnd = this.buildDateWithMonthYear(currentEnd, month - 1, year);
      this.activateDate = {
        start: this.activateDate.start,
        end: newEnd
      };
    }
  }
  buildDateWithMonthYear(baseDate, month, year) {
    const day = baseDate instanceof Date ? baseDate.getDate() : 1;
    const daysInTargetMonth = new Date(year, month + 1, 0).getDate();
    return new Date(year, month, Math.min(day, daysInTargetMonth));
  }
  convertDateToISO(date) {
    if (this.isRange) {
      const start2 = date?.start instanceof Date ? this.poDate.convertDateToISO(date.start) : null;
      const end2 = date?.end instanceof Date ? this.poDate.convertDateToISO(date.end) : null;
      return {
        start: start2,
        end: end2
      };
    } else {
      return this.poDate.convertDateToISO(date);
    }
  }
  convertDateFromIso(stringDate) {
    if (stringDate && typeof stringDate === "string") {
      const {
        year,
        month,
        day
      } = this.poDate.getDateFromIso(stringDate);
      const date = new Date(year, month - 1, day);
      this.poDate.setYearFrom0To100(date, year);
      return date;
    }
    return null;
  }
  updateModel(value) {
    const MODE_YEAR = this.mode === "year";
    const MODE_MONTH_YEAR = this.mode === "month-year";
    let finalValue = value;
    if (MODE_MONTH_YEAR) {
      if (this.selectedMonth && this.selectedYear) {
        finalValue = new Date(this.selectedYear, this.selectedMonth - 1, 1);
        this.change.emit(finalValue);
      } else {
        return;
      }
    } else if (MODE_YEAR) {
      if (this.selectedYear) {
        finalValue = new Date(this.selectedYear, 0, 1);
        this.change.emit(finalValue);
      } else {
        return;
      }
    }
    if (this.propagateChange) {
      this.propagateChange(finalValue);
    }
  }
  setMonth(index) {
    this.selectedIndexMonth = index;
    this.selectedMonth = index + 1;
  }
  setYear(index, year) {
    this.selectedIndexYear = index;
    this.selectedYear = year;
  }
  writeDate(value) {
    if (this.mode === "month-year") {
      const date = new Date(value);
      const monthIndex = date.getMonth();
      const year = date.getFullYear();
      const yearOptions = this.getYearOptions();
      const yearIndex = yearOptions.findIndex((btn) => Number(btn.label()) === year);
      this.setMonth(monthIndex);
      this.setYear(yearIndex, year);
    } else if (this.mode === "year") {
      const year = new Date(value).getFullYear();
      const yearOptions = this.getYearOptions();
      const yearIndex = yearOptions.findIndex((btn) => Number(btn.label()) === year);
      this.setYear(yearIndex, year);
    } else if (this.isRange) {
      const start2 = value?.start;
      const end2 = value?.end;
      const newStart = start2 instanceof Date ? new Date(start2) : this.convertDateFromIso(start2);
      const newEnd = end2 instanceof Date ? new Date(end2) : this.convertDateFromIso(end2);
      this.value = {
        start: newStart,
        end: newEnd
      };
    } else {
      this.value = value instanceof Date ? new Date(value) : this.convertDateFromIso(value);
    }
  }
  static ɵfac = function PoCalendarComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoCalendarComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoCalendarComponent,
    selectors: [["po-calendar"]],
    viewQuery: function PoCalendarComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c52, 5)(_c53, 5)(_c54, 5);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.timerComponent = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.yearOptions = _t);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.monthOptions = _t);
      }
    },
    standalone: false,
    features: [ɵɵProvidersFeature(providers$7), ɵɵInheritDefinitionFeature, ɵɵNgOnChangesFeature],
    decls: 12,
    vars: 1,
    consts: [["calendarWrapper", ""], ["calendarMonth", ""], ["calendarYear", ""], ["calendarTimer", ""], ["monthOption", ""], ["yearOption", ""], ["timer", ""], [1, "po-calendar", "po-calendar-month-year"], [1, "po-calendar", "po-calendar-year"], [1, "po-calendar", 3, "po-calendar-with-presets", "po-calendar-mobile", "po-calendar-responsive"], [3, "po-calendar-date-time", "po-calendar-three-columns", "po-calendar-four-columns"], [4, "ngTemplateOutlet"], [1, "po-calendar"], [3, "p-presets", "p-locale", "p-selected-preset", "p-size"], [4, "ngTemplateOutlet", "ngTemplateOutletContext"], [3, "p-select-preset", "p-close-calendar", "p-presets", "p-locale", "p-selected-preset", "p-size"], [3, "p-select-preset", "p-close-calendar", "p-presets", "p-locale", "p-selected-preset", "p-size", "p-responsive"], [3, "p-clear", "p-close-calendar", "p-size", "p-responsive", "p-hide-today-button", "p-label-clear"], [3, "p-header-change", "p-select-date", "p-select-today", "p-hover-date", "p-close-calendar", "p-value", "p-activate-date", "p-locale", "p-min-date", "p-presets", "p-max-date", "p-part-type", "p-range", "p-responsive", "p-selected-value", "p-hover-value", "p-hide-today-button", "p-header-template", "p-size"], [1, "po-calendar-months"], [1, "po-calendar-month-item"], ["role", "option", "p-kind", "tertiary", 3, "p-click", "keydown", "p-label", "p-tabindex", "p-size", "p-disabled"], [1, "po-calendar-years"], [1, "po-calendar-year-item"], [1, "po-calendar-vertical-divider"], [3, "p-change", "p-boundary-tab", "p-format", "p-locale", "p-min-time", "p-max-time", "p-minute-interval", "p-second-interval", "p-show-seconds", "p-size"]],
    template: function PoCalendarComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵconditionalCreate(0, PoCalendarComponent_Conditional_0_Template, 3, 2, "div", 7)(1, PoCalendarComponent_Conditional_1_Template, 2, 1, "div", 8)(2, PoCalendarComponent_Conditional_2_Template, 4, 11, "div", 9)(3, PoCalendarComponent_Conditional_3_Template, 4, 10, "div", 10);
        ɵɵtemplate(4, PoCalendarComponent_ng_template_4_Template, 1, 14, "ng-template", null, 0, ɵɵtemplateRefExtractor)(6, PoCalendarComponent_ng_template_6_Template, 3, 0, "ng-template", null, 1, ɵɵtemplateRefExtractor)(8, PoCalendarComponent_ng_template_8_Template, 3, 0, "ng-template", null, 2, ɵɵtemplateRefExtractor)(10, PoCalendarComponent_ng_template_10_Template, 3, 8, "ng-template", null, 3, ɵɵtemplateRefExtractor);
      }
      if (rf & 2) {
        ɵɵconditional(ctx.isMonthYear ? 0 : ctx.isYear ? 1 : ctx.isRange ? 2 : 3);
      }
    },
    dependencies: [NgTemplateOutlet, PoButtonComponent, PoTimerComponent, PoCalendarFooterComponent, PoCalendarWrapperComponent, PoCalendarPresetListComponent],
    encapsulation: 2,
    changeDetection: 0
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoCalendarComponent, [{
    type: Component,
    args: [{
      selector: "po-calendar",
      changeDetection: ChangeDetectionStrategy.OnPush,
      providers: providers$7,
      standalone: false,
      template: `@if (isMonthYear) {
  <div class="po-calendar po-calendar-month-year">
    <ng-container *ngTemplateOutlet="calendarMonth"></ng-container>
    <ng-container *ngTemplateOutlet="calendarYear"></ng-container>
  </div>
} @else if (isYear) {
  <div class="po-calendar po-calendar-year">
    <ng-container *ngTemplateOutlet="calendarYear"></ng-container>
  </div>
} @else if (isRange) {
  <div
    class="po-calendar"
    [class.po-calendar-with-presets]="effectivePresets().length > 0"
    [class.po-calendar-mobile]="isResponsive || widthWithPresets"
    [class.po-calendar-responsive]="!isResponsive && widthWithPresets"
  >
    @if (effectivePresets().length > 0 && !isResponsive && !widthWithPresets) {
      <po-calendar-preset-list
        [p-presets]="effectivePresets()"
        [p-locale]="locale"
        [p-selected-preset]="selectedPresetLabel"
        [p-size]="size"
        (p-select-preset)="onPresetSelected($event)"
        (p-close-calendar)="onCloseCalendar()"
      ></po-calendar-preset-list>
    }
    <ng-container *ngTemplateOutlet="calendarWrapper; context: { partType: 'start' }"></ng-container>
    @if (effectivePresets().length > 0 && (isResponsive || widthWithPresets)) {
      <po-calendar-preset-list
        [p-presets]="effectivePresets()"
        [p-locale]="locale"
        [p-selected-preset]="selectedPresetLabel"
        [p-size]="size"
        [p-responsive]="true"
        (p-select-preset)="onPresetSelected($event)"
        (p-close-calendar)="onCloseCalendar()"
      ></po-calendar-preset-list>

      <po-calendar-footer
        [p-size]="size"
        [p-responsive]="isResponsive"
        [p-hide-today-button]="true"
        [p-label-clear]="displayToClean"
        (p-clear)="onSelectDate($event)"
        (p-close-calendar)="onCloseCalendar()"
      ></po-calendar-footer>
    }
  </div>
} @else {
  <div
    [class.po-calendar-date-time]="isDateTime"
    [class.po-calendar-three-columns]="showSeconds || format === '12'"
    [class.po-calendar-four-columns]="showSeconds && format === '12'"
  >
    <div class="po-calendar">
      <ng-container *ngTemplateOutlet="calendarWrapper; context: { partType: 'single' }"></ng-container>
    </div>
    @if (isDateTime) {
      <ng-container *ngTemplateOutlet="calendarTimer"></ng-container>
    }
  </div>
}

<ng-template #calendarWrapper let-partType="partType">
  <po-calendar-wrapper
    [p-value]="getValue(partType)"
    [p-activate-date]="getActivateDate(partType)"
    [p-locale]="locale"
    [p-min-date]="minDate"
    [p-presets]="effectivePresets().length > 0"
    [p-max-date]="maxDate"
    [p-part-type]="partType"
    [p-range]="isRange"
    [p-responsive]="isResponsive || widthWithPresets"
    [p-selected-value]="value"
    [p-hover-value]="hoverValue"
    [p-hide-today-button]="effectivePresets().length > 0"
    (p-header-change)="onHeaderChange($event, partType)"
    (p-select-date)="onSelectDate($event, partType)"
    (p-select-today)="onSelectTodayPreset()"
    (p-hover-date)="onHoverDate($event)"
    (p-close-calendar)="onCloseCalendar()"
    [p-header-template]="headerTemplate"
    [p-size]="size"
  >
  </po-calendar-wrapper>
</ng-template>

<ng-template #calendarMonth>
  <ul class="po-calendar-months">
    @for (month of displayMonths; track month; let i = $index) {
      <li class="po-calendar-month-item">
        <po-button
          #monthOption
          [class.po-button-selected]="i === selectedIndexMonth"
          role="option"
          p-kind="tertiary"
          p-label="{{ month }}"
          [attr.aria-selected]="i === selectedIndexMonth"
          [p-tabindex]="i === focusedIndex ? 0 : -1"
          [p-size]="size"
          [p-disabled]="isMonthDisabled(i)"
          (p-click)="selectMonth(i, $event, true)"
          (keydown)="onKeydownMonth($event, i)"
        ></po-button>
      </li>
    }
  </ul>
</ng-template>

<ng-template #calendarYear>
  <ul class="po-calendar-years">
    @for (year of displayYears; track year; let i = $index) {
      <li class="po-calendar-year-item">
        <po-button
          #yearOption
          [class.po-button-selected]="i === selectedIndexYear"
          role="option"
          p-kind="tertiary"
          p-label="{{ year }}"
          [attr.aria-selected]="i === selectedIndexYear"
          [p-tabindex]="i === focusedIndex ? 0 : -1"
          [p-size]="size"
          [p-disabled]="isYearDisabled(year)"
          (p-click)="selectYear(i, $event, true, year)"
          (keydown)="onKeydownYear($event, i)"
        >
        </po-button>
      </li>
    }
  </ul>
</ng-template>

<ng-template #calendarTimer>
  <div class="po-calendar-vertical-divider"></div>
  <po-timer
    #timer
    [p-format]="format"
    [p-locale]="locale"
    [p-min-time]="minTime"
    [p-max-time]="maxTime"
    [p-minute-interval]="minuteInterval"
    [p-second-interval]="secondInterval"
    [p-show-seconds]="showSeconds"
    [p-size]="size"
    (p-change)="onTimeChange($event)"
    (p-boundary-tab)="onTimerBoundaryTab($event)"
  ></po-timer>
</ng-template>
`
    }]
  }], () => [], {
    timerComponent: [{
      type: ViewChild,
      args: ["timer"]
    }],
    yearOptions: [{
      type: ViewChildren,
      args: ["yearOption"]
    }],
    monthOptions: [{
      type: ViewChildren,
      args: ["monthOption"]
    }]
  });
})();
var PoCleanModule = class _PoCleanModule {
  static ɵfac = function PoCleanModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoCleanModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoCleanModule,
    declarations: [PoCleanComponent],
    imports: [CommonModule, PoIconModule],
    exports: [PoCleanComponent]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [CommonModule, PoIconModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoCleanModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, PoIconModule],
      exports: [PoCleanComponent],
      declarations: [PoCleanComponent]
    }]
  }], null, null);
})();
var PoComboModule = class _PoComboModule {
  static ɵfac = function PoComboModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoComboModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoComboModule,
    declarations: [PoComboComponent, PoComboOptionTemplateDirective],
    imports: [CommonModule, FormsModule, OverlayModule, PoFieldContainerModule, PoListBoxModule, PoHelperModule, PoIconModule, PoCleanModule, PoLoadingModule],
    exports: [PoComboComponent, PoComboOptionTemplateDirective]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [CommonModule, FormsModule, OverlayModule, PoFieldContainerModule, PoListBoxModule, PoHelperModule, PoIconModule, PoCleanModule, PoLoadingModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoComboModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, FormsModule, OverlayModule, PoFieldContainerModule, PoListBoxModule, PoHelperModule, PoIconModule, PoCleanModule, PoLoadingModule],
      declarations: [PoComboComponent, PoComboOptionTemplateDirective],
      exports: [PoComboComponent, PoComboOptionTemplateDirective]
    }]
  }], null, null);
})();
var PoCalendarModule = class _PoCalendarModule {
  static ɵfac = function PoCalendarModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoCalendarModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoCalendarModule,
    declarations: [PoCalendarComponent, PoCalendarFooterComponent, PoCalendarHeaderComponent, PoCalendarWrapperComponent, PoCalendarPresetListComponent],
    imports: [CommonModule, FormsModule, PoIconModule, PoComboModule, PoButtonModule, PoTimerModule],
    exports: [PoCalendarComponent]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [CommonModule, FormsModule, PoIconModule, PoComboModule, PoButtonModule, PoTimerModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoCalendarModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, FormsModule, PoIconModule, PoComboModule, PoButtonModule, PoTimerModule],
      declarations: [PoCalendarComponent, PoCalendarFooterComponent, PoCalendarHeaderComponent, PoCalendarWrapperComponent, PoCalendarPresetListComponent],
      exports: [PoCalendarComponent]
    }]
  }], null, null);
})();
var PoDefaultColors = [["#29B6C5"], ["#0C6C94", "#29B6C5"], ["#0C6C94", "#29B6C5", "#C9ECF0"], ["#0C6C94", "#0B92B4", "#29B6C5", "#94DAE2"], ["#0C6C94", "#0B92B4", "#29B6C5", "#94DAE2", "#C9ECF0"], ["#0C6C94", "#00A5C5", "#0082A8", "#31628D", "#364473", "#32285C"], ["#0C6C94", "#00AACB", "#018DAE", "#277099", "#325783", "#35426E", "#32285D"], ["#0C6C94", "#00AFCE", "#0194B6", "#117FA2", "#0088AC", "#23789F", "#2B6992", "#32285C"], ["#0C6C94", "#00BAD5", "#00A7C7", "#0197B8", "#0088AC", "#23789F", "#2B6992", "#315C87", "#32285C"], ["#0C6C94", "#00BAD5", "#00A7C7", "#0197B8", "#0088AC", "#23789F", "#2B6992", "#315C87", "#364E7C", "#32285C"], ["#0C6C94", "#00BAD5", "#00A7C7", "#0197B8", "#0088AC", "#23789F", "#2B6992", "#315C87", "#364E7C", "#36436F", "#32285C"], ["#0C6C94", "#00BAD5", "#00A7C7", "#0197B8", "#0088AC", "#23789F", "#2B6992", "#315C87", "#364E7C", "#36436F", "#333565", "#32285C"]];
var poColorPalette = Object.values(PoColorPaletteEnum);
var PoColorService = class _PoColorService {
  defaultColors = [];
  overlayColors = [];
  colorBlack = "#000000";
  /**
   * Avalia a propriedade `color` na lista de items passada. Caso sim, trata se é decimal ou string `po-color`. Caso não haja, retorna a cor default.
   *
   * @param data
   */
  getColors(data, categoricalColors = false, overlayColors = false) {
    this.verifyIfHasColorProperty(data, categoricalColors, overlayColors);
    return data.map((dataItem, index) => {
      if (dataItem.color) {
        dataItem.color = this.verifyIfIsPoColorPalette(dataItem.color);
        if (overlayColors) {
          const overlayColor = this.verifyIfIsPoColorPalette(dataItem.color);
          return __spreadProps(__spreadValues({}, dataItem), {
            overlayColor,
            isNotTokenColor: true
          });
        }
        return dataItem;
      }
      const color3 = this.defaultColors[index] === void 0 ? this.colorBlack : this.defaultColors[index];
      if (overlayColors) {
        const overlayColor = this.overlayColors[index];
        return __spreadProps(__spreadValues({}, dataItem), {
          color: color3,
          overlayColor
        });
      }
      return __spreadProps(__spreadValues({}, dataItem), {
        color: color3
      });
    });
  }
  verifyIfHasColorProperty(data, categoricalColors = false, overlayColors = false) {
    const hasColorProperty = data.every((dataItem) => dataItem.hasOwnProperty("color") && dataItem.color?.length > 0);
    if (!hasColorProperty) {
      this.defaultColors = categoricalColors ? this.getDefaultCategoricalColors(data.length) : this.getDefaultColors(data.length);
      if (overlayColors) {
        this.overlayColors = this.getDefaultCategoricalColors(data.length, true);
      }
    }
  }
  verifyIfIsPoColorPalette(color3) {
    if (poColorPalette.includes(color3)) {
      return `po-${color3}`;
    }
    return color3;
  }
  getDefaultColors(length) {
    if (length === 1) {
      return PoDefaultColors[0];
    }
    const colorsLength = PoDefaultColors.length;
    if (length > colorsLength) {
      const quantityDuplicates = length / colorsLength;
      let colors2 = PoDefaultColors[colorsLength - 1];
      for (let i2 = 0; i2 <= quantityDuplicates; i2++) {
        colors2 = colors2.concat(PoDefaultColors[colorsLength]);
      }
      return colors2;
    }
    return PoDefaultColors[length - 1];
  }
  getDefaultCategoricalColors(length, isOverlay = false) {
    const maxTokens = 8;
    const colors2 = [];
    for (let i2 = 1; i2 <= Math.min(length, maxTokens); i2++) {
      isOverlay ? colors2.push(this.getCSSVariable(`--categorical-overlay-0${i2}`)) : colors2.push(this.getCSSVariable(`--categorical-0${i2}`));
    }
    while (colors2.length < length) {
      isOverlay ? colors2.push(this.defaultColors[length - 1]) : colors2.push(this.getRandomColor());
    }
    return colors2;
  }
  getRandomColor() {
    const array = new Uint32Array(1);
    crypto.getRandomValues(array);
    return `#${(array[0] & 16777215).toString(16).padStart(6, "0")}`;
  }
  getCSSVariable(variable) {
    return getComputedStyle(document.documentElement).getPropertyValue(variable).trim();
  }
  static ɵfac = function PoColorService_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoColorService)();
  };
  static ɵprov = ɵɵdefineInjectable({
    token: _PoColorService,
    factory: _PoColorService.ɵfac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoColorService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
var PoColorModule = class _PoColorModule {
  static ɵfac = function PoColorModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoColorModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoColorModule
  });
  static ɵinj = ɵɵdefineInjector({
    providers: [PoColorService]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoColorModule, [{
    type: NgModule,
    args: [{
      providers: [PoColorService],
      bootstrap: []
    }]
  }], null, null);
})();
var PoChartLabelFormat;
(function(PoChartLabelFormat2) {
  PoChartLabelFormat2["Number"] = "number";
  PoChartLabelFormat2["Currency"] = "currency";
})(PoChartLabelFormat || (PoChartLabelFormat = {}));
var poChartLiteralsDefault = {
  en: {
    downloadCSV: "Download CSV",
    exportCSV: "Export CSV",
    exportJPG: "Export JPG",
    exportPNG: "Export PNG",
    value: "Value",
    item: "Item",
    serie: "Series",
    category: "Category"
  },
  es: {
    downloadCSV: "Descargar CSV",
    exportCSV: "Exportar CSV",
    exportJPG: "Exportar JPG",
    exportPNG: "Exportar PNG",
    value: "Valor",
    item: "Artículo",
    serie: "Serie",
    category: "Categoría"
  },
  pt: {
    downloadCSV: "Baixar CSV",
    exportCSV: "Exportar CSV",
    exportJPG: "Exportar JPG",
    exportPNG: "Exportar PNG",
    value: "Valor",
    item: "Item",
    serie: "Série",
    category: "Categoria"
  },
  ru: {
    downloadCSV: "Скачать CSV",
    exportCSV: "Экспортировать CSV",
    exportJPG: "Экспортировать JPG",
    exportPNG: "Экспортировать PNG",
    value: "Ценить",
    item: "Пункт",
    serie: "Ряд",
    category: "Категория"
  }
};
var PoChartType;
(function(PoChartType2) {
  PoChartType2["Area"] = "area";
  PoChartType2["Donut"] = "donut";
  PoChartType2["Pie"] = "pie";
  PoChartType2["Line"] = "line";
  PoChartType2["Column"] = "column";
  PoChartType2["Bar"] = "bar";
  PoChartType2["Gauge"] = "gauge";
  PoChartType2["Radar"] = "radar";
})(PoChartType || (PoChartType = {}));
var poChartMinHeight = 200;
var poChartDefaultHeight = 400;
var PoChartBaseComponent = class _PoChartBaseComponent {
  _literals;
  setHeightGauge = false;
  language;
  id = "myChart";
  /** Define o título do gráfico. */
  title;
  /**
   * @description
   *
   * Define os elementos do gráfico que serão criados dinamicamente.
   */
  series;
  /**
   * @description
   *
   * Define o valor do gráfico do tipo `Gauge` quando utliza as propriedades `From` `To`.
   */
  valueGaugeMultiple;
  /**
   * @optional
   *
   * @description
   *
   * Define os valores utilizados na construção das categorias do gráfico.
   *
   * Para gráficos dos tipos *bar*, *area*, *column* e *line*, representa os nomes das categorias exibidas no eixo.
   *
   * Para gráficos do tipo *radar*, representa a configuração dos indicadores, formato (shape), áreas de divisão (splitArea)
   * e demais opções específicas do gráfico `Radar`.
   *
   * > Caso nenhum valor seja informado, será utilizado um hífen como categoria
   *   correspondente para cada série.
   *
   * > Gráficos do tipo bar dimensionam sua área considerando a largura do maior texto
   *   da categoria, sendo recomendável utilizar rótulos curtos para facilitar a leitura.
   */
  categories;
  /**
   * @optional
   *
   * @description
   *
   * Essa propriedade permite que o desenvolvedor adicione ações customizadas no popup do header, oferecendo mais flexibilidade e controle sobre as interações do componente.
   */
  customActions;
  /**
   * @optional
   *
   * @description
   *
   * Objeto com as configurações usadas no `po-chart`.
   *
   * É possível, por exemplo, definir as configurações de exibição das legendas,
   * configurar os eixos(*axis*) para os gráficos dos tipos `area`, `line`, `column`, `bar` e `radar` da seguinte forma:
   *
   * ```
   *  chartOptions: PoChartOptions = {
   *    legend: true,
   *    axis: {
   *      minRange: 0,
   *      maxRange: 100,
   *      gridLines: 5,
   *    },
   *  };
   * ```
   */
  options;
  /**
   * @optional
   *
   * @description
   *
   * Permite configurar as propriedades de exibição dos rótulos das séries no gráfico.
   *
   * Essa configuração possibilita fixar os valores das séries diretamente no gráfico, alterando o comportamento visual:
   * - Os valores das séries permanecem visíveis, sem a necessidade de hover.
   * - O *tooltip* não será exibido.
   * - Os marcadores (*bullets*) terão seu estilo ajustado.
   * - As outras séries ficarão com opacidade reduzida ao passar o mouse sobre a série ativa.
   *
   * > Disponível para gráficos do tipo `line` e `radar`.
   *
   * #### Exemplo de utilização:
   * ```typescript
   * dataLabel: PoChartDataLabel = {
   *   fixed: true,
   * };
   * ```
   */
  dataLabel;
  /**
   * @optional
   *
   * @description
   *
   * Define a altura do gráfico em px.
   *
   * > No caso do tipo `Gauge`, o valor padrão é `300` e esse é seu valor minimo aceito. Nos outros tipos, o valor mínimo aceito nesta propriedade é 200.
   *
   * @default `400`
   */
  set height(value) {
    let heightGauge = null;
    this.setHeightGauge = true;
    if (this.type === PoChartType.Gauge) {
      heightGauge = 300;
    }
    this._height = Math.max(value ?? heightGauge ?? poChartDefaultHeight, heightGauge ?? poChartMinHeight);
  }
  get height() {
    return this._height;
  }
  _height = poChartDefaultHeight;
  /**
   * @optional
   *
   * @description
   *
   * Define o tipo de gráfico.
   *
   * É possível também combinar gráficos dos tipos linha e coluna. Para isso, opte pela declaração de `type` conforme a interface `PoChartSerie`.
   *
   * > Note que, se houver declaração de tipo de gráfico tanto em `p-type` quanto em `PochartSerie.type`, o valor `{ type }` da primeira série anulará o valor definido em `p-type`.
   *
   * Se não passado valor, o padrão será relativo à primeira série passada em `p-series`:
   * - Se `p-series = [{ data: [1,2,3] }]`: será `PoChartType.Column`.
   * - Se `p-series = [{ data: 1 }]`: será `PoChartType.Pie`.
   *
   * > Veja os valores válidos no *enum* `PoChartType`.
   */
  type;
  /**
   * @optional
   *
   * @description
   *
   * Objeto com as literais usadas no `po-chart`.
   *
   * Para utilizar basta passar a literal que deseja customizar:
   *
   * ```
   *  const customLiterals: PoChartLiterals = {
   *    downloadCSV: 'Obter CSV',
   *  };
   * ```
   *
   * E para carregar a literal customizada, basta apenas passar o objeto para o componente.
   *
   * ```
   * <po-chart
   *   [p-literals]="customLiterals">
   * </po-chart>
   * ```
   *
   * > O objeto padrão de literais será traduzido de acordo com o idioma do
   * [`PoI18nService`](/documentation/po-i18n) ou do browser.
   */
  set literals(value) {
    if (value instanceof Object && !(value instanceof Array)) {
      this._literals = __spreadValues(__spreadValues(__spreadValues({}, poChartLiteralsDefault[poLocaleDefault]), poChartLiteralsDefault[this.language]), value);
    } else {
      this._literals = poChartLiteralsDefault[this.language];
    }
  }
  get literals() {
    return this._literals || poChartLiteralsDefault[this.language];
  }
  /**
   * @optional
   *
   * @description
   *
   * Evento executado quando o usuário clicar sobre um elemento do gráfico.
   *
   * O evento emitirá o seguinte parâmetro:
   * - *donut* e *pie*: um objeto contendo a categoria e valor da série.
   * - *radar*: um objeto contendo o nome da série e os valores.
   * - *area*, *line*, *column* e *bar*: um objeto contendo o nome da série, valor e categoria do eixo do gráfico.
   */
  seriesClick = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Evento executado quando o usuário passar o *mouse* sobre um elemento do gráfico.
   *
   * O evento emitirá o seguinte parâmetro de acordo com o tipo de gráfico:
   * - *donut* e *pie*: um objeto contendo a categoria e valor da série.
   * - *radar*: um objeto contendo o nome da série e os valores.
   * - *area*, *line*, *column* e *bar*: um objeto contendo a categoria, valor da série e categoria do eixo do gráfico.
   */
  seriesHover = new EventEmitter();
  constructor(languageService) {
    this.language = languageService.getShortLanguage();
  }
  ngOnInit() {
    if (this.type === PoChartType.Gauge && !this.setHeightGauge) {
      this._height = 300;
    }
  }
  static ɵfac = function PoChartBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoChartBaseComponent)(ɵɵdirectiveInject(PoLanguageService));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoChartBaseComponent,
    inputs: {
      id: [0, "t-id", "id"],
      title: [0, "p-title", "title"],
      series: [0, "p-series", "series"],
      valueGaugeMultiple: [0, "p-value-gauge-multiple", "valueGaugeMultiple"],
      categories: [0, "p-categories", "categories"],
      customActions: [0, "p-custom-actions", "customActions"],
      options: [0, "p-options", "options"],
      dataLabel: [0, "p-data-label", "dataLabel"],
      height: [0, "p-height", "height"],
      type: [0, "p-type", "type"],
      literals: [0, "p-literals", "literals"]
    },
    outputs: {
      seriesClick: "p-series-click",
      seriesHover: "p-series-hover"
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoChartBaseComponent, [{
    type: Directive
  }], () => [{
    type: PoLanguageService
  }], {
    id: [{
      type: Input,
      args: ["t-id"]
    }],
    title: [{
      type: Input,
      args: ["p-title"]
    }],
    series: [{
      type: Input,
      args: ["p-series"]
    }],
    valueGaugeMultiple: [{
      type: Input,
      args: ["p-value-gauge-multiple"]
    }],
    categories: [{
      type: Input,
      args: ["p-categories"]
    }],
    customActions: [{
      type: Input,
      args: ["p-custom-actions"]
    }],
    options: [{
      type: Input,
      args: ["p-options"]
    }],
    dataLabel: [{
      type: Input,
      args: ["p-data-label"]
    }],
    height: [{
      type: Input,
      args: ["p-height"]
    }],
    type: [{
      type: Input,
      args: ["p-type"]
    }],
    literals: [{
      type: Input,
      args: ["p-literals"]
    }],
    seriesClick: [{
      type: Output,
      args: ["p-series-click"]
    }],
    seriesHover: [{
      type: Output,
      args: ["p-series-hover"]
    }]
  });
})();
var gridPaddingValues = {
  paddingBottomWithTopLegend: 48,
  paddingBottomWithBottomLegend: 72,
  paddingBottomNoLegend: 0,
  paddingTopWithDataLabelFixed: 64,
  paddingTopDefaultWithTopLegend: 56,
  paddingTopWithDataLabelFixedBottomLegend: 32,
  paddingTopDefaultWithBottomLegend: 16,
  bottomDataZoomValueTopLegend: 8,
  bottomDataZoomValueBottomLegend: 32
};
var PoChartGridUtils = class {
  component;
  isTypeDonut = false;
  textCenterDonut = {};
  constructor(component) {
    this.component = component;
  }
  setGridOption(options) {
    const tokenBorderWidthSm = this.resolvePx("--border-width-sm");
    const paddingBottom = this.getPaddingBottomGrid();
    const paddingTop = this.getPaddingTopGrid();
    options.grid = {
      top: paddingTop,
      left: this.component.options?.axis?.paddingLeft || 16,
      right: this.component.options?.axis?.paddingRight || 32,
      bottom: this.component.options?.axis?.paddingBottom || paddingBottom,
      outerBoundsMode: "same",
      borderWidth: tokenBorderWidthSm
    };
  }
  setOptionsAxis(options) {
    const tokenFontSizeGrid = this.resolvePx("--font-size-grid", ".po-chart");
    const tokenBorderWidthSm = this.resolvePx("--border-width-sm");
    options.xAxis = {
      type: this.component.isTypeBar ? "value" : "category",
      axisLabel: {
        fontFamily: this.component.getCSSVariable("--font-family-grid", ".po-chart"),
        fontSize: tokenFontSizeGrid || 12,
        fontWeight: Number(this.component.getCSSVariable("--font-weight-grid", ".po-chart")),
        color: this.component.getCSSVariable("--text-color-grid", ".po-chart"),
        rotate: this.component.options?.axis?.rotateLegend,
        interval: 0,
        width: 72,
        overflow: "break"
      },
      splitLine: {
        show: this.component.isTypeBar ? this.component.options?.axis?.showXAxis ?? true : this.component.options?.axis?.showXAxis || false,
        lineStyle: {
          type: "solid",
          width: tokenBorderWidthSm,
          color: this.component.getCSSVariable("--color-grid", ".po-chart")
        }
      }
    };
    options.yAxis = {
      type: this.component.isTypeBar ? "category" : "value",
      splitNumber: 5,
      axisLabel: {
        margin: 10,
        fontFamily: this.component.getCSSVariable("--font-family-grid", ".po-chart"),
        fontSize: tokenFontSizeGrid || 12,
        color: this.component.getCSSVariable("--text-color-grid", ".po-chart"),
        fontWeight: Number(this.component.getCSSVariable("--font-weight-grid", ".po-chart"))
      },
      splitLine: {
        show: this.component.isTypeBar ? this.component.options?.axis?.showYAxis || false : this.component.options?.axis?.showYAxis ?? true,
        lineStyle: {
          color: this.component.getCSSVariable("--color-grid", ".po-chart")
        }
      }
    };
    if (this.component.isTypeRadar) {
      return;
    }
    if (this.component.isTypeBar) {
      options.yAxis.data = this.component.categories;
    } else {
      options.xAxis.data = this.component.categories;
      options.xAxis.boundaryGap = this.component.boundaryGap;
    }
  }
  setOptionDataZoom(options) {
    options.dataZoom = [{
      show: true,
      realtime: true,
      bottom: this.component.options?.bottomDataZoom || "calc(100%)",
      height: 25,
      xAxisIndex: [0]
    }, {
      type: "inside",
      realtime: true,
      xAxisIndex: [0]
    }];
  }
  setShowAxisDetails(options) {
    if (this.component.options?.axis?.showAxisDetails) {
      options.tooltip = {
        trigger: "none",
        axisPointer: {
          type: "cross",
          label: {
            backgroundColor: this.component.getCSSVariable("--color-neutral-dark-70")
          }
        }
      };
    }
  }
  setSerieTypeLine(serie, tokenBorderWidthMd, color3) {
    if (serie.type === "line") {
      serie.symbolSize = 8;
      serie.symbol = "circle";
      serie.itemStyle = {
        color: !this.component.options?.fillPoints ? this.component.getCSSVariable("--color-chart-line-point-fill", ".po-chart") : color3,
        borderColor: color3,
        borderWidth: tokenBorderWidthMd
      };
      serie.lineStyle = {
        color: color3,
        width: tokenBorderWidthMd
      };
    }
  }
  setSerieTypeArea(serie, index) {
    if (serie.isTypeArea) {
      let colorVariable;
      if (serie.color?.startsWith("var(--")) {
        colorVariable = getComputedStyle(document.documentElement).getPropertyValue(serie.color.replace(/^var\((--[^)]+)\)$/, "$1")).trim();
      } else if (serie.color?.includes("color")) {
        colorVariable = this.component.getCSSVariable(`--${serie.color.replace("po-", "")}`);
      } else {
        colorVariable = serie.overlayColor ?? serie.color;
      }
      serie.areaStyle = {
        color: colorVariable
      };
      if (index > 7 || serie.isNotTokenColor) {
        serie.areaStyle.opacity = 0.5;
      }
    }
  }
  setSerieTypeBarColumn(serie, color3) {
    if (serie.type === "bar") {
      serie.itemStyle = {
        borderRadius: this.resolvePx("--border-radius-bar", ".po-chart"),
        color: color3
      };
      serie.emphasis = {
        focus: "series"
      };
      serie.blur = {
        itemStyle: {
          opacity: 0.4
        }
      };
      this.component.boundaryGap = true;
      if (this.component.options?.stacked || serie.stackGroupName) {
        serie.stack = this.component.options?.stacked ? "total" : serie.stackGroupName;
        if (this.component.dataLabel?.fixed !== false) {
          this.component.dataLabel = {
            fixed: true
          };
          serie.label = {
            show: true
          };
        }
      }
    }
  }
  setSerieTypeDonutPie(serie, color3) {
    if (this.component.listTypePieDonut?.length) {
      const borderWidth = this.resolvePx("--border-width-sm");
      const borderColor = this.component.getCSSVariable("--border-color", ".po-chart");
      const seriePie = {
        name: serie.name,
        value: serie.data,
        itemStyle: {
          borderWidth,
          borderColor,
          color: color3,
          borderRadius: this.component.options?.borderRadius
        }
      };
      this.component.listTypePieDonut[0].data.push(seriePie);
    }
  }
  setListTypeDonutPie(type) {
    if (type === PoChartType.Donut) {
      this.isTypeDonut = true;
      this.component.itemsTypeDonut = this.normalizeToPercentage(this.component.series);
    }
    let radiusHorizontal = "80%";
    let radiusVertical = "55%";
    let positionHorizontal;
    if (this.component.options?.legend === false) {
      radiusHorizontal = "95%";
      radiusVertical = "65%";
      positionHorizontal = "50%";
    } else {
      positionHorizontal = this.component.options?.legendVerticalPosition === "top" ? "54%" : "46%";
    }
    if (this.component.options?.innerRadius) {
      radiusVertical = this.getAdjustedRadius(radiusVertical, this.component.options.innerRadius);
    }
    const radius = this.isTypeDonut ? [radiusVertical, radiusHorizontal] : radiusHorizontal;
    this.component.listTypePieDonut = [{
      type: "pie",
      center: ["50%", positionHorizontal],
      radius,
      roseType: this.component.options?.roseType ? "area" : void 0,
      label: {
        show: false
      },
      emphasis: {
        focus: "self"
      },
      data: [],
      blur: {
        itemStyle: {
          opacity: 0.4
        }
      }
    }];
    this.setTextCenterDonut();
  }
  setTextCenterDonut() {
    if (this.isTypeDonut && this.component.options?.textCenterGraph) {
      this.textCenterDonut = {
        type: "text",
        left: "center",
        top: this.component.options?.legendVerticalPosition === "top" ? "52%" : "44%",
        style: {
          text: this.component.options?.textCenterGraph,
          fontSize: this.resolvePx("--font-size-md"),
          fontWeight: Number(this.component.getCSSVariable("--font-weight-hightlight-value", ".po-chart")),
          fontFamily: this.component.getCSSVariable("--font-family-hightlight-value", ".po-chart"),
          fill: this.component.getCSSVariable("--color-hightlight-value", ".po-chart")
        },
        silent: true
      };
    }
  }
  getAdjustedRadius(radius, innerRadius) {
    const radiusValue = parseFloat(radius);
    if (innerRadius >= 100) {
      return radius;
    }
    const adjusted = radiusValue * (innerRadius / 100);
    return `${adjusted}%`;
  }
  normalizeToPercentage(series) {
    const total = series.map((item) => item.data).filter((value) => typeof value === "number").reduce((sum2, value) => sum2 + value, 0) || 1;
    return series.map((item) => ({
      label: item.label,
      data: item.data,
      valuePercentage: +(item.data / total * 100).toFixed(2)
    }));
  }
  resolvePx(size, selector2) {
    const token = this.component.getCSSVariable(size, selector2);
    if (token.endsWith("px")) {
      return parseFloat(token);
    } else if (token.endsWith("rem")) {
      return parseFloat(token) * 16;
    } else if (token.endsWith("em")) {
      const parentElement = selector2 ? document.querySelector(selector2) : document.documentElement;
      const parentFontSize = parentElement ? parseFloat(getComputedStyle(parentElement).fontSize) : 16;
      return parseFloat(token) * parentFontSize;
    }
  }
  getPaddingBottomGrid() {
    const options = this.component.options;
    if (options?.dataZoom && options.bottomDataZoom && (options.legend === false || options.legendVerticalPosition === "top")) {
      if (typeof options.bottomDataZoom === "boolean" && options.bottomDataZoom === true) {
        options.bottomDataZoom = gridPaddingValues.bottomDataZoomValueTopLegend;
      }
      return gridPaddingValues.paddingBottomWithTopLegend;
    } else if (options?.dataZoom && options.bottomDataZoom && options.legendVerticalPosition !== "top") {
      if (typeof options.bottomDataZoom === "boolean" && options.bottomDataZoom === true) {
        options.bottomDataZoom = gridPaddingValues.bottomDataZoomValueBottomLegend;
      }
      return gridPaddingValues.paddingBottomWithBottomLegend;
    } else if (options?.dataZoom && !options?.bottomDataZoom && options.legend === false || !options?.dataZoom && options?.legend === false || !options?.dataZoom && options?.legendVerticalPosition === "top" || this.component.isTypeRadar && options.legendVerticalPosition !== "top") {
      return gridPaddingValues.paddingBottomNoLegend;
    }
    return gridPaddingValues.paddingBottomWithTopLegend;
  }
  hasTopLegendOrNoZoom(options) {
    return options?.dataZoom && !options.bottomDataZoom || options?.dataZoom && options.bottomDataZoom && options.legendVerticalPosition === "top" || !options?.dataZoom && options?.legendVerticalPosition === "top";
  }
  hasBottomLegendWithZoom(options) {
    return options?.dataZoom && options.bottomDataZoom && options.legendVerticalPosition !== "top" || !options?.dataZoom && options?.legendVerticalPosition !== "top";
  }
  getPaddingTopGrid() {
    const options = this.component.options;
    if (this.hasTopLegendOrNoZoom(options)) {
      if (typeof options.bottomDataZoom === "boolean" && options.bottomDataZoom === true) {
        options.bottomDataZoom = gridPaddingValues.bottomDataZoomValueTopLegend;
      }
      const fixed = this.component.dataLabel?.fixed && !options?.axis?.maxRange;
      return fixed ? gridPaddingValues.paddingTopWithDataLabelFixed : gridPaddingValues.paddingTopDefaultWithTopLegend;
    } else if (this.hasBottomLegendWithZoom(options)) {
      const fixed = this.component.dataLabel?.fixed && !options?.axis?.maxRange;
      return fixed ? gridPaddingValues.paddingTopWithDataLabelFixedBottomLegend : gridPaddingValues.paddingTopDefaultWithBottomLegend;
    }
  }
  isRadarOptions(value) {
    return value && typeof value === "object" && !Array.isArray(value) && "indicator" in value;
  }
  convertRadarConfig(indicators) {
    return {
      shape: "polygon",
      splitArea: false,
      indicator: indicators.map((item) => ({
        name: item
      }))
    };
  }
  setListTypeRadar() {
    const radar = this.isRadarOptions(this.component.categories) ? this.component.categories : this.convertRadarConfig(this.component.categories);
    const radarConfig = {
      shape: radar?.shape ?? "polygon",
      radius: "60%",
      splitLine: {
        show: true,
        lineStyle: {
          color: [this.component.getCSSVariable("--border-color-radar", "po-chart .po-chart")],
          width: this.resolvePx("--border-width-sm")
        }
      },
      axisLine: {
        show: true,
        lineStyle: {
          color: this.component.getCSSVariable("--border-color-radar", "po-chart .po-chart"),
          width: this.resolvePx("--border-width-sm")
        }
      },
      splitArea: {
        show: radar?.splitArea ?? false,
        areaStyle: {
          color: [this.component.getCSSVariable("--color-background-line", "po-chart .po-chart"), this.component.getCSSVariable("--color-background-zebra", "po-chart .po-chart")]
        }
      },
      indicator: Array.isArray(radar?.indicator) ? radar.indicator.map((ind) => ({
        name: ind.name,
        max: ind.max ?? void 0,
        min: ind.min ?? 0,
        color: ind.color ?? this.component.getCSSVariable("--color-neutral-dark-90")
      })) : []
    };
    const currentOptions = this.component.options ?? {};
    const hasGlobalAreaStyle = this.component.options?.areaStyle === true;
    const hasSeriesAreaStyle = Array.isArray(this.component.series) && this.component.series.some((serie) => !!serie?.areaStyle);
    this.component.options = __spreadProps(__spreadValues({}, this.component.options), {
      fillPoints: hasGlobalAreaStyle || hasSeriesAreaStyle ? true : currentOptions.fillPoints ?? true,
      radar: radarConfig
    });
    return radarConfig;
  }
  setSerieTypeRadar(serie, tokenBorderWidthMd, color3) {
    if (serie.type === "radar") {
      serie.type = "radar";
      serie.symbol = "circle";
      serie.symbolSize = 6;
      if (this.component.dataLabel?.fixed) {
        serie.label = {
          show: true
        };
      }
      serie.itemStyle = {
        color: this.component.options?.fillPoints === false ? this.component.getCSSVariable("--color-chart-line-point-fill", "po-chart .po-chart") : color3,
        borderColor: color3,
        borderWidth: tokenBorderWidthMd
      };
      serie.lineStyle = {
        color: color3,
        width: tokenBorderWidthMd
      };
      return serie;
    }
  }
  setTooltipRadar(params) {
    const indicators = this.component.options.radar?.indicator ?? [];
    const values = params.value ?? [];
    values.map((val, i2) => {
      const indicatorName = indicators[i2]?.name ?? `Indicador ${i2 + 1}`;
      return `${indicatorName}: <b>${val}</b>`;
    });
  }
  buildRadarTooltip(params) {
    const indicators = this.component.options.radar?.indicator ?? [];
    const values = params.value ?? [];
    let tooltip = `<b>${params.name}</b><br>`;
    values.map((val, index) => {
      const indicatorName = indicators[index]?.name ?? `Indicator ${index + 1}`;
      tooltip += `${indicatorName}: <b>${val}</b><br>`;
    });
    return tooltip;
  }
  finalizeSerieTypeRadar(seriesUpdated) {
    const hasGlobalAreaStyle = this.component.options?.areaStyle === true;
    return [{
      type: "radar",
      data: seriesUpdated.map((item) => {
        const name = item?.name ?? "";
        const value = Array.isArray(item?.data) ? item.data : [];
        let areaStyle;
        if (hasGlobalAreaStyle || item.areaStyle === true) {
          areaStyle = {
            opacity: 0.5
          };
        } else {
          areaStyle = void 0;
        }
        return {
          name,
          value,
          areaStyle,
          symbol: item.symbol,
          symbolSize: item.symbolSize,
          itemStyle: item.itemStyle,
          lineStyle: item.lineStyle,
          label: {
            show: item?.label?.show
          }
        };
      })
    }];
  }
};
var PoChartGaugeUtils = class {
  component;
  constructor(component) {
    this.component = component;
  }
  setGaugeOptions(options, fontSizeText) {
    const series = this.component.series;
    if (this.component.isGaugeSingle || this.component.options?.legend === false || !series?.find((serie) => serie.label)) {
      return;
    }
    let currentX = 0;
    let currentY = 0;
    const lineHeight = 30;
    const gap = 20;
    const chartWidth = this.component.el.nativeElement.querySelector("#chart-id")?.clientWidth;
    const children = this.component.itemsTypeGauge.map((item, index) => {
      const label = item.label ?? "";
      const text = this.component.options?.showFromToLegend ? `${label} (${item.from}–${item.to})` : `${label}`;
      const estimatedTextWidth = text.length * (fontSizeText * 0.5) + 16;
      const itemWidth = estimatedTextWidth < 78 ? gap + estimatedTextWidth + 5 : gap + 90;
      if (currentX + itemWidth > chartWidth) {
        currentX = 0;
        currentY += lineHeight;
      }
      const itemGraphic = [{
        type: "rect",
        shape: {
          width: 14,
          height: 14
        },
        style: {
          fill: this.component.itemsColorTypeGauge[index]
        },
        left: currentX,
        top: currentY
      }, {
        type: "text",
        left: currentX + gap,
        top: currentY + 1,
        style: {
          text,
          fill: this.component.getCSSVariable("--color-legend", ".po-chart"),
          width: 85,
          overflow: "break",
          fontSize: fontSizeText,
          fontFamily: this.component.getCSSVariable("--font-family-grid", ".po-chart"),
          fontWeight: this.component.getCSSVariable("--font-weight-grid", ".po-chart")
        }
      }];
      currentX += itemWidth;
      return itemGraphic;
    }).flat();
    options.graphic = [{
      type: "group",
      left: "center",
      bottom: 0,
      layout: "horizontal",
      children
    }];
  }
  setListTypeGauge(serie, fontSizes) {
    const series = this.component.series;
    this.component.isGaugeSingle = series?.length === 1 && !!series[0]?.data;
    if (this.component.isGaugeSingle) {
      const item = series[0];
      this.component.itemsTypeGauge = [{
        label: item.label,
        from: 0,
        to: item.data,
        valuePercentage: item.data
      }];
    } else {
      this.component.itemsTypeGauge = this.normalizeToRelativePercentage(series);
    }
    const divWidth = this.component.el.nativeElement.querySelector("#chart-id")?.clientWidth ?? 0;
    const height = this.component.height;
    const properties = this.setPropertiesGauge(divWidth, height);
    const data = series[0];
    const fontSizeDetail = divWidth < 480 || height < 400 ? fontSizes.fontSizeMd : fontSizes.fontSizeLg;
    serie.type = "gauge";
    serie.startAngle = 180;
    serie.endAngle = 0;
    serie.center = properties.center;
    serie.radius = properties.radius;
    serie.axisTick = {
      show: false
    };
    serie.splitLine = {
      show: false
    };
    serie.axisLabel = {
      show: false
    };
    serie.pointer = {
      show: !this.component.isGaugeSingle && this.component.options?.pointer !== false,
      offsetCenter: ["0%", "-45%"],
      length: properties.lengthPointer,
      itemStyle: {
        color: this.component.getCSSVariable("--color-gauge-pointer-color", ".po-chart")
      }
    };
    serie.progress = {
      show: false
    };
    serie.detail = {
      show: true,
      offsetCenter: [0, "-30%"],
      formatter: (row) => {
        if (this.component.isGaugeSingle) {
          return `${data.data}%`;
        } else if (this.component.valueGaugeMultiple) {
          return `${this.component.valueGaugeMultiple}%`;
        }
        return "";
      },
      fontSize: fontSizeDetail,
      fontWeight: Number(this.component.getCSSVariable("--font-weight-hightlight-value", ".po-chart")),
      fontFamily: this.component.getCSSVariable("--font-family-hightlight-value", ".po-chart"),
      color: this.component.getCSSVariable("--color-hightlight-value", ".po-chart")
    };
    serie.title = {
      show: !!this.component.options?.subtitleGauge,
      offsetCenter: [0, "-10%"],
      fontSize: fontSizes.fontSizeSubtitle,
      fontWeight: Number(this.component.getCSSVariable("--font-weight-description-chart", "po-chart")),
      fontFamily: this.component.getCSSVariable("--font-family-description-chart", "po-chart"),
      color: this.component.getCSSVariable("--color-description-chart", "po-chart"),
      width: properties.widthSubtitle,
      overflow: "break"
    };
    serie.data = [{
      value: this.component.isGaugeSingle ? data.data : this.component.valueGaugeMultiple,
      name: this.component.isGaugeSingle ? this.component.options?.subtitleGauge || data.label : this.component.options?.subtitleGauge
    }];
    return serie;
  }
  setPropertiesGauge(divWidth, height) {
    let radius = "140%";
    let lengthPointer = "40%";
    let center3 = ["50%", "80%"];
    let widthSubtitle = height < 450 ? 400 : 500;
    const isSmallScreen = divWidth < 480;
    const isMediumScreen = divWidth >= 480 && divWidth < 960;
    const isLargeScreen = divWidth >= 960;
    if (isSmallScreen) {
      radius = height > 480 ? "100%" : "120%";
      lengthPointer = "25%";
      widthSubtitle = 300;
      center3 = ["50%", "75%"];
    } else if (isMediumScreen) {
      radius = height > 550 ? "100%" : "120%";
      widthSubtitle = height > 499 ? 450 : 320;
      lengthPointer = "35%";
    } else if (isLargeScreen && height > 750) {
      radius = "100%";
    }
    if (height < 400) {
      return this.setPropertiesDefaultHeight(center3);
    }
    return {
      radius,
      lengthPointer,
      center: center3,
      widthSubtitle
    };
  }
  setPropertiesDefaultHeight(center3) {
    const radius = "140%";
    const lengthPointer = "30%";
    const widthSubtitle = 210;
    return {
      radius,
      lengthPointer,
      center: center3,
      widthSubtitle
    };
  }
  setSerieTypeGauge(serie, color3) {
    if (serie.type === "gauge") {
      this.component.itemsColorTypeGauge = [...this.component.itemsColorTypeGauge, color3];
    }
  }
  normalizeToRelativePercentage(series) {
    const numericSeries = series.filter((item) => typeof item.to === "number");
    if (numericSeries.length === 0) return [];
    const sortedSeries = [...numericSeries].sort((a, b) => a.to - b.to);
    const max3 = sortedSeries[sortedSeries.length - 1].to;
    if (max3 === 0) {
      return sortedSeries.map((item) => ({
        label: item.label,
        from: item.from,
        to: item.to,
        valuePercentage: 100
      }));
    }
    return sortedSeries.map((item) => ({
      label: item.label,
      from: item.from,
      to: item.to,
      valuePercentage: +(item.to / max3 * 100).toFixed(2)
    }));
  }
  finalizeSerieTypeGauge(serie) {
    let colors2;
    if (this.component.isGaugeSingle) {
      colors2 = [[this.component.itemsTypeGauge[0].valuePercentage / 100, this.component.itemsColorTypeGauge[0]], [1, this.component.getCSSVariable("--color-base-gauge", ".po-chart")]];
    } else {
      colors2 = this.buildGaugeAxisLineColorsWithGaps(this.component.itemsTypeGauge, this.component.itemsColorTypeGauge, this.component.getCSSVariable("--color-base-gauge", ".po-chart"));
    }
    return [__spreadProps(__spreadValues({}, serie), {
      axisLine: {
        lineStyle: {
          width: 40,
          color: colors2
        }
      }
    })];
  }
  buildGaugeAxisLineColorsWithGaps(series, colorPalette, gapColor) {
    const sortedSeries = [...series].sort((a, b) => a.from - b.from);
    const maxTo = Math.max(...sortedSeries.map((s) => s.to));
    const colorSegments = [];
    let lastTo = 0;
    sortedSeries.forEach((segment, index) => {
      const {
        from,
        to
      } = segment;
      const color3 = colorPalette[index % colorPalette.length];
      if (from > lastTo) {
        colorSegments.push([+(from / maxTo).toFixed(4), gapColor]);
      }
      colorSegments.push([+(to / maxTo).toFixed(4), color3]);
      lastTo = to;
    });
    if (lastTo < maxTo) {
      colorSegments.push([1, gapColor]);
    }
    return colorSegments;
  }
};
use([install2, install10, install8, install30, install, install3, install6, install34, install41, install29, install38, install35, install32, install33, install58, install57]);
var PoChartComponent = class _PoChartComponent extends PoChartBaseComponent {
  el = inject(ElementRef);
  currencyPipe = inject(CurrencyPipe);
  decimalPipe = inject(DecimalPipe);
  colorService = inject(PoColorService);
  cdr = inject(ChangeDetectorRef);
  poTooltip;
  targetRef;
  chartContainer;
  tooltipText = ``;
  originalHeight;
  originalRadiusGauge;
  chartMarginTop = "0px";
  isTypeBar = false;
  isTypeRadar = false;
  boundaryGap = false;
  listTypePieDonut;
  itemsTypeDonut = [];
  isGaugeSingle;
  itemsTypeGauge = [];
  itemsColorTypeGauge = [];
  actionModal = {
    action: this.downloadCsv.bind(this),
    label: this.literals.downloadCSV
  };
  showPopup = true;
  itemsTable = [];
  columnsTable = [];
  isExpanded = false;
  legendData = [];
  headerHeight;
  paddingContainerGauge = 0;
  descriptionHeight = 0;
  positionTooltip = "top";
  tooltipTitle = void 0;
  tooltipDescriptionGauge = void 0;
  chartGridUtils;
  chartGaugeUtils;
  isTypeGauge = false;
  popupActions = [];
  chartInstance;
  currentRenderer;
  resizeObserver;
  intersectionObserver;
  hideDomEchartsDiv = false;
  vcr = inject(ViewContainerRef);
  get showHeader() {
    return this.title || !this.options?.header?.hideTableDetails || !this.options?.header?.hideExpand || this.showPopup;
  }
  constructor() {
    const languageService = inject(PoLanguageService);
    super(languageService);
  }
  changeTheme = (event) => {
    this.chartInstance?.dispose();
    this.chartInstance = void 0;
    this.initECharts();
    this.checkShowCEcharts();
  };
  ngOnInit() {
    this.chartGridUtils = new PoChartGridUtils(this);
    this.chartGaugeUtils = new PoChartGaugeUtils(this);
    super.ngOnInit();
  }
  ngOnChanges(changes) {
    if (changes["customActions"]?.currentValue) {
      this.setPopupActions();
    }
    if (changes["series"]?.firstChange) {
      return;
    }
    const {
      options,
      series,
      dataLabel,
      categories,
      height,
      valueGaugeMultiple
    } = changes;
    if (options || series || dataLabel || categories || valueGaugeMultiple) {
      if (!this.chartInstance || options?.currentValue?.rendererOption && this.currentRenderer !== options.currentValue.rendererOption) {
        this.chartInstance?.dispose();
        this.chartInstance = void 0;
        this.setInitialPopupActions();
        this.initECharts();
      } else {
        this.chartInstance?.clear();
        this.setInitialPopupActions();
        this.setChartsProperties();
      }
    }
    if (height) {
      setTimeout(() => {
        this.chartInstance?.resize();
      });
    }
  }
  ngAfterViewInit() {
    this.setInitialPopupActions();
    this.initECharts();
    this.checkShowCEcharts();
  }
  ngOnDestroy() {
    if (this.resizeObserver?.disconnect) {
      this.resizeObserver?.disconnect();
    }
    this.intersectionObserver?.disconnect();
  }
  showTooltipTitle(e2) {
    const element = e2.target;
    if (element.offsetWidth < element.scrollWidth) {
      this.tooltipTitle = this.title;
    } else {
      this.tooltipTitle = void 0;
    }
  }
  showTooltipDescription(e2) {
    const element = e2.target;
    if (element.offsetWidth < element.scrollWidth) {
      this.tooltipDescriptionGauge = this.options.descriptionChart;
    } else {
      this.tooltipDescriptionGauge = void 0;
    }
  }
  getCSSVariable(variable, selector2) {
    const element = selector2 ? document.querySelector(selector2) : document.documentElement;
    return element ? getComputedStyle(element).getPropertyValue(variable).trim() : "";
  }
  openModal() {
    return __async(this, null, function* () {
      if (!this.chartInstance) {
        return;
      }
      this.setTableProperties();
      this.vcr.clear();
      const {
        PoChartModalTableComponent
      } = yield import("./po-ui-ng-components-po-chart-modal-table.component-yOf0a_2R-LA3CYASM.js");
      const componentRef = this.vcr.createComponent(PoChartModalTableComponent);
      componentRef.setInput("title", this.title);
      componentRef.setInput("itemsTable", this.itemsTable);
      componentRef.setInput("columnsTable", this.columnsTable);
      componentRef.setInput("actionModal", this.actionModal);
      componentRef.instance.modalComponent.open();
    });
  }
  toggleExpand() {
    if (!this.isExpanded) {
      this.originalHeight = this.height;
      this.height = window.innerHeight;
      this.originalRadiusGauge = this.chartInstance?.getOption()?.series?.[0]?.radius;
    } else {
      this.height = this.originalHeight;
      this.chartMarginTop = "0px";
    }
    if (this.isTypeGauge && innerWidth < 1366) {
      this.chartInstance?.setOption({
        series: [{
          radius: this.isExpanded ? this.originalRadiusGauge : "100%"
        }]
      });
    }
    this.isExpanded = !this.isExpanded;
    setTimeout(() => {
      this.chartInstance?.resize();
    });
  }
  applyStringFormatter(template, context) {
    return template.replace("{name}", context.name).replace("{seriesName}", context.seriesName).replace("{value}", context.value);
  }
  observeContainerResize() {
    if (!this.series?.length) return;
    if (this.resizeObserver?.disconnect) {
      this.resizeObserver.disconnect();
      this.resizeObserver = void 0;
    }
    this.resizeObserver = new ResizeObserver(() => {
      if (this.chartInstance?.resize) {
        this.chartInstance.resize();
      }
    });
    this.resizeObserver.observe(this.chartContainer?.nativeElement);
  }
  checkShowCEcharts() {
    const chartElement = this.el.nativeElement.querySelector("#chart-id");
    if (!chartElement || !this.hideDomEchartsDiv) return;
    this.intersectionObserver = new IntersectionObserver((entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          this.setHeightProperties();
          this.initECharts();
          this.intersectionObserver.disconnect();
        }
      });
    }, {
      threshold: 0.1
    });
    this.intersectionObserver.observe(chartElement);
  }
  setInitialPopupActions() {
    const hideExportCsv = this.options?.header?.hideExportCsv;
    const hideExportImage = this.options?.header?.hideExportImage;
    this.showPopup = !(hideExportCsv && hideExportImage && !this.customActions?.length);
    this.cdr.detectChanges();
    this.paddingContainerGauge = this.options?.showContainerGauge ? 16 : 0;
    this.setHeightProperties();
    this.popupActions = [{
      label: this.literals.exportCSV,
      visible: !hideExportCsv,
      action: () => {
        this.setTableProperties();
        this.downloadCsv();
      }
    }, {
      label: this.literals.exportPNG,
      visible: !hideExportImage,
      action: this.exportImage.bind(this, "png")
    }, {
      label: this.literals.exportJPG,
      visible: !hideExportImage,
      action: this.exportImage.bind(this, "jpeg")
    }];
    this.actionModal = __spreadProps(__spreadValues({}, this.actionModal), {
      disabled: hideExportCsv
    });
    this.setPopupActions();
  }
  setHeightProperties() {
    const headerElement = this.el.nativeElement.querySelector(".po-chart-header");
    let descriptionHeight = 0;
    if (this.options?.descriptionChart) {
      const descriptionElement = this.el.nativeElement.querySelector(".description-chart");
      descriptionHeight = descriptionElement?.clientHeight;
    }
    if (this.headerHeight !== headerElement?.clientHeight || this.descriptionHeight !== descriptionHeight) {
      this.headerHeight = headerElement?.clientHeight ?? 0;
      this.descriptionHeight = descriptionHeight ?? 0;
      this.cdr.detectChanges();
      this.chartInstance?.resize();
    }
  }
  initECharts() {
    this.cdr.detectChanges();
    const echartsDiv = this.el.nativeElement.querySelector("#chart-id");
    if (!echartsDiv?.clientWidth) {
      this.hideDomEchartsDiv = true;
      return;
    }
    this.currentRenderer = this.options?.rendererOption || "canvas";
    this.chartInstance = init2(echartsDiv, null, {
      renderer: this.currentRenderer
    });
    this.observeContainerResize();
    this.setChartsProperties();
    this.initEChartsEvents();
  }
  initEChartsEvents() {
    this.chartInstance.on("click", (params) => {
      if (!params.value || params?.seriesType === "gauge") return;
      if (params.seriesName && !params.seriesName.includes("\x000")) {
        this.seriesClick.emit({
          label: params.seriesName,
          data: params.value,
          category: params.name
        });
      } else {
        this.seriesClick.emit({
          label: params.name,
          data: params.value
        });
      }
    });
    this.chartInstance.on("mouseover", (params) => {
      if (!params.value || params?.seriesType === "gauge") return;
      if (params.seriesType) {
        const divTooltipElement = this.el.nativeElement.querySelector("#custom-tooltip");
        if (divTooltipElement && this.dataLabel?.fixed !== true) {
          this.setTooltipProperties(divTooltipElement, params);
        }
      }
      if (params.seriesName && !params.seriesName.includes("\x000")) {
        this.seriesHover.emit({
          label: params.seriesName,
          data: params.value,
          category: params.name
        });
      } else {
        this.seriesHover.emit({
          label: params.name,
          data: params.value
        });
      }
    });
    this.chartInstance.on("mouseout", () => {
      this.poTooltip.last.toggleTooltipVisibility(false);
    });
  }
  parseTooltipText(text) {
    if (!text) return text;
    return text.replace(/\n/g, "<br>").replace(/\*\*(.*?)\*\*/g, "<b>$1</b>").replace(/__(.*?)__/g, "<i>$1</i>");
  }
  resolveCustomTooltip(params, name, seriesName, valueLabel) {
    let text;
    const serie = params.seriesType === "pie" || params.seriesType === "radar" ? this.series[params.dataIndex] : this.series[params.seriesIndex];
    if (serie?.tooltip) {
      if (typeof serie.tooltip === "function") {
        text = serie.tooltip(params);
      } else {
        text = this.applyStringFormatter(serie.tooltip, {
          name,
          seriesName,
          value: valueLabel
        });
      }
    }
    if (!text) {
      text = seriesName && !seriesName.includes("\x000") ? `<b>${name}</b><br>${seriesName}: <b>${valueLabel}</b>` : `${name}: <b>${valueLabel}</b>`;
      if (params.seriesType === "radar") {
        text = this.chartGridUtils.buildRadarTooltip(params);
      }
    }
    return this.parseTooltipText(text);
  }
  setTooltipProperties(divTooltipElement, params) {
    const chartElement = this.el.nativeElement.querySelector("#chart-id");
    let valueLabel = params.value;
    if (this.itemsTypeDonut?.length) {
      const findCurrentValue = this.itemsTypeDonut.find((item) => item.data === params.value && params.name === item.label);
      valueLabel = `${findCurrentValue?.valuePercentage ?? 0}%`;
    }
    const name = params.name === " " ? this.literals.item : params.name;
    const seriesName = params.seriesName === " " ? this.literals.item : params.seriesName;
    this.tooltipText = this.resolveCustomTooltip(params, name, seriesName, valueLabel);
    divTooltipElement.style.left = `${params.event.offsetX + chartElement.offsetLeft + 3}px`;
    divTooltipElement.style.top = `${chartElement.offsetTop + params.event.offsetY - 2}px`;
    this.poTooltip.last.toggleTooltipVisibility(true);
  }
  setChartsProperties() {
    this.isTypeBar = false;
    this.isTypeGauge = false;
    this.itemsColorTypeGauge = [];
    this.listTypePieDonut = [];
    this.itemsTypeDonut = [];
    let option = {};
    if (Array.isArray(this.categories) && !this.categories?.length && this.categories !== void 0) {
      this.categories = void 0;
    }
    if (!this.series?.length) {
      this.chartInstance?.dispose();
      this.chartInstance = void 0;
      return;
    }
    option = this.setOptions();
    this.chartInstance.setOption(option);
    this.cdr.detectChanges();
  }
  setOptions() {
    const newSeries = this.setSeries();
    const options = {
      backgroundColor: this.getCSSVariable("--background-color-grid", "po-chart"),
      series: newSeries
    };
    if (!this.listTypePieDonut?.length && !this.isTypeGauge) {
      this.chartGridUtils.setGridOption(options);
      this.chartGridUtils.setOptionsAxis(options);
      this.formatLabelOption(options);
      this.chartGridUtils.setShowAxisDetails(options);
      if (this.options?.dataZoom) {
        this.chartGridUtils.setOptionDataZoom(options);
      }
    } else if (this.chartGridUtils.isTypeDonut && this.options?.textCenterGraph) {
      options.graphic = this.chartGridUtils.textCenterDonut;
    }
    if (this.options?.legend !== false) {
      this.setOptionLegend(options);
    }
    if (this.isTypeGauge) {
      this.chartGaugeUtils.setGaugeOptions(options, this.chartGridUtils.resolvePx("--font-size-grid", ".po-chart"));
    }
    if (this.isTypeRadar) {
      options.radar = this.options?.radar;
    }
    return options;
  }
  formatLabelOption(options) {
    if (this.options?.axis && Object.keys(this.options.axis).length) {
      const currentAxis = this.isTypeBar ? "xAxis" : "yAxis";
      options[currentAxis]["splitNumber"] = this.options.axis.gridLines || 5;
      options[currentAxis]["min"] = this.options.axis.minRange;
      options[currentAxis]["max"] = this.options.axis.maxRange;
      if (this.options.axis.labelType) {
        options[currentAxis]["axisLabel"].formatter = this.options.axis.labelType === PoChartLabelFormat.Number ? (value) => this.decimalPipe.transform(value, "1.2-2") : (value) => this.currencyPipe.transform(value, null, "symbol", "1.2-2");
      }
    }
  }
  setOptionLegend(options) {
    options.legend = {
      type: this.options?.legendType || "plain",
      pageIconColor: this.options?.legendType === "scroll" ? this.getCSSVariable("--color-legend-scroll-icon-active", ".po-chart") : "",
      pageIconInactiveColor: this.options?.legendType === "scroll" ? this.getCSSVariable("--color-legend-scroll-icon-inactive", ".po-chart") : "",
      show: true,
      selectedMode: !this.isTypeGauge,
      orient: "horizontal",
      left: this.options?.legendType === "scroll" ? 32 : this.options?.legendPosition || "center",
      top: this.options?.legendVerticalPosition || "bottom",
      bottom: 0,
      padding: [0, 16, 0, 16],
      itemWidth: 16,
      itemHeight: 16,
      icon: "roundRect",
      textStyle: {
        color: this.getCSSVariable("--color-legend", ".po-chart"),
        fontSize: this.getCSSVariable("--font-size-grid", ".po-chart"),
        fontFamily: this.getCSSVariable("--font-family-grid", ".po-chart"),
        fontWeight: this.getCSSVariable("--font-weight-grid", ".po-chart")
      }
    };
  }
  setSeries() {
    const hasArea = this.type === "area" || this.series.some((serie) => serie.type === "area") || this.options?.areaStyle || this.series.some((serie) => serie.areaStyle === true);
    const newSeries = [...this.colorService.getColors(this.series, true, hasArea)];
    const tokenBorderWidthMd = this.chartGridUtils.resolvePx("--border-width-md");
    const findType = this.series.find((serie) => serie.type)?.type;
    let serieGauge = {};
    let typeDefault;
    if (!this.type) {
      typeDefault = Array.isArray(this.series[0].data) ? PoChartType.Column : PoChartType.Pie;
    }
    const verifyType = findType || this.type || typeDefault;
    if (verifyType === "donut" || verifyType === "pie") {
      this.chartGridUtils.setListTypeDonutPie(verifyType);
    } else if (verifyType === "gauge") {
      const serie = {};
      const fontSizes = {
        fontSizeMd: this.chartGridUtils.resolvePx("--font-size-md"),
        fontSizeLg: this.chartGridUtils.resolvePx("--font-size-lg"),
        fontSizeSubtitle: this.chartGridUtils.resolvePx("--font-size-description-chart", "po-chart")
      };
      serieGauge = this.chartGaugeUtils.setListTypeGauge(serie, fontSizes);
    } else if (verifyType === "radar") {
      this.chartGridUtils.setListTypeRadar();
    }
    const seriesUpdated = newSeries.map((serie, index) => {
      serie.name = serie.name || serie.label && typeof serie.label === "string" ? serie.name ?? serie.label : " ";
      !serie.type ? this.setTypeSerie(serie, this.type || typeDefault) : this.setTypeSerie(serie, serie.type);
      let colorVariable;
      if (serie.color?.startsWith("var(--")) {
        colorVariable = getComputedStyle(document.documentElement).getPropertyValue(serie.color.replace(/^var\((--[^)]+)\)$/, "$1")).trim();
      } else if (serie.color?.includes("color") || serie.color?.startsWith("caption-tag-")) {
        colorVariable = this.getCSSVariable(`--color-${serie.color.replace("po-", "").replace("color-", "")}`);
      } else {
        colorVariable = serie.color;
      }
      this.chartGridUtils.setSerieTypeDonutPie(serie, colorVariable);
      this.chartGaugeUtils.setSerieTypeGauge(serie, colorVariable);
      this.setSerieEmphasis(serie, colorVariable, tokenBorderWidthMd);
      this.chartGridUtils.setSerieTypeLine(serie, tokenBorderWidthMd, colorVariable);
      this.chartGridUtils.setSerieTypeArea(serie, index);
      this.chartGridUtils.setSerieTypeBarColumn(serie, colorVariable);
      this.chartGridUtils.setSerieTypeRadar(serie, tokenBorderWidthMd, colorVariable);
      return serie;
    });
    if (this.listTypePieDonut?.length) {
      return this.listTypePieDonut;
    } else if (verifyType === "gauge") {
      return this.chartGaugeUtils.finalizeSerieTypeGauge(serieGauge);
    } else if (verifyType === "radar") {
      return this.chartGridUtils.finalizeSerieTypeRadar(seriesUpdated);
    }
    return seriesUpdated;
  }
  setSerieEmphasis(serie, color3, tokenBorder) {
    serie.emphasis = {
      itemStyle: {
        color: color3,
        borderWidth: tokenBorder
      },
      scale: 1.5
    };
    if (this.dataLabel?.fixed) {
      serie.label = {
        show: true
      };
      serie.emphasis = {
        focus: "series",
        itemStyle: {
          color: color3,
          borderWidth: tokenBorder
        },
        scale: 1.5
      };
    }
  }
  setTypeSerie(serie, type) {
    switch (type) {
      case PoChartType.Area:
        serie.isTypeArea = true;
        serie.type = "line";
        break;
      case PoChartType.Bar:
        this.isTypeBar = true;
        serie.type = "bar";
        break;
      case PoChartType.Column:
        serie.isTypeColumn = true;
        serie.type = "bar";
        break;
      case PoChartType.Line:
        serie.type = "line";
        break;
      case PoChartType.Gauge:
        this.isTypeGauge = true;
        serie.type = "gauge";
        break;
      case PoChartType.Radar:
        this.isTypeRadar = true;
        serie.type = "radar";
        break;
    }
  }
  setTableProperties() {
    const option = this.chartInstance.getOption();
    let categories = this.isTypeBar ? option.yAxis[0].data : option.xAxis?.[0].data;
    if (!categories && !this.isTypeGauge) {
      categories = [];
      if (Array.isArray(this.series[0]?.data)) {
        this.series[0].data.forEach((data, index) => categories.push(String(index)));
      } else {
        let items = {
          [this.options?.firstColumnName || this.literals.serie]: "-"
        };
        option.series[0].data.forEach((data) => items = __spreadProps(__spreadValues({}, items), {
          [data.name]: data.value
        }));
        this.itemsTable = [items];
        return;
      }
    }
    const series = option.series;
    if (this.isTypeBar) {
      this.setTablePropertiesTypeBar(categories, series);
      return;
    } else if (this.isTypeGauge) {
      this.setTablePropertiesTypeGauge();
      return;
    } else if (this.isTypeRadar) {
      this.setTablePropertiesTypeRadar();
      return;
    }
    this.itemsTable = series.map((serie) => {
      const rowData = {
        serie: serie.name
      };
      categories.forEach((category, index) => {
        rowData[category] = serie.data[index];
      });
      return rowData;
    });
    this.setTableColumns(option, categories);
  }
  setTablePropertiesTypeBar(categories, series) {
    this.itemsTable = categories.map((category, index) => {
      const rowData = {
        categoria: category
      };
      series.forEach((serie) => {
        rowData[serie.name] = serie.data[index];
      });
      return rowData;
    });
    this.columnsTable = [{
      property: "categoria",
      label: this.options?.firstColumnName || this.literals.category
    }, ...series.map((serie) => ({
      property: serie.name,
      label: serie.name
    }))];
  }
  setTablePropertiesTypeGauge() {
    this.itemsTable = [{
      [this.literals.value]: this.isGaugeSingle ? this.series[0].data : this.valueGaugeMultiple || "-"
    }];
    if (!this.isGaugeSingle) {
      this.series.forEach((serie, index) => {
        const item = __spreadProps(__spreadValues({}, this.itemsTable[0]), {
          [serie.label || `${this.literals.item} ${index + 1}`]: `${serie.from} - ${serie.to}`
        });
        this.itemsTable = [__spreadValues({}, item)];
      });
    }
  }
  setTablePropertiesTypeRadar() {
    let indicators;
    if (!Array.isArray(this.categories) && this.categories?.indicator) {
      indicators = this.categories.indicator;
    } else if (Array.isArray(this.categories)) {
      indicators = this.categories.map((item) => ({
        name: item
      }));
    }
    const series = this.series;
    this.columnsTable = [{
      property: "serie",
      label: this.options?.firstColumnName || this.literals.serie
    }, ...indicators.map((indicator) => ({
      property: indicator.name,
      label: indicator.name
    }))];
    this.itemsTable = series.map((serie) => {
      const row = {
        serie: serie.label
      };
      indicators.forEach((indicator, idx) => {
        const value = serie.data[idx]?.value ?? serie.data[idx];
        row[indicator.name] = value;
      });
      return row;
    });
  }
  setTableColumns(option, categories) {
    this.columnsTable = [{
      property: "serie",
      label: this.options?.firstColumnName || this.literals.serie
    }, ...categories.map((category) => ({
      property: category,
      label: category
    }))];
  }
  downloadCsv() {
    const headers = Object.keys(this.itemsTable[0]);
    const columnNameDefault = this.isTypeBar ? "Categoria" : this.literals.serie;
    const firstColumnName = this.options?.firstColumnName || columnNameDefault;
    const orderedHeaders = this.columnsTable?.length ? [firstColumnName, ...headers.filter((header) => header !== "serie")] : [...headers.filter((header) => header !== "serie")];
    const csvContent = [orderedHeaders.join(";"), ...this.itemsTable.map((row) => orderedHeaders.map((header) => header === firstColumnName ? row["serie"] : row[header] ?? "").join(";"))].join("\n");
    const utf8Bom = "\uFEFF";
    const blob = new Blob([utf8Bom + csvContent], {
      type: "text/csv;charset=utf-8;"
    });
    const url = URL.createObjectURL(blob);
    const a = document.createElement("a");
    a.href = url;
    a.download = "dados_grafico.csv";
    document.body.appendChild(a);
    a.click();
    document.body.removeChild(a);
    URL.revokeObjectURL(url);
  }
  exportImage(type) {
    if (!this.chartInstance) {
      return;
    }
    if (this.currentRenderer === "svg") {
      this.exportSvgAsImage(type);
    } else {
      const chartImage = new Image();
      chartImage.src = this.chartInstance.getDataURL({
        type,
        pixelRatio: 2,
        backgroundColor: this.getCSSVariable("--color-neutral-light-00")
      });
      this.configureImageCanvas(type, chartImage);
    }
  }
  exportSvgAsImage(type) {
    const svgEl = this.el.nativeElement.querySelector("#chart-id svg");
    if (!svgEl) return;
    const serializer = new XMLSerializer();
    const svgString = serializer.serializeToString(svgEl);
    const svgBlob = new Blob([svgString], {
      type: "image/svg+xml;charset=utf-8"
    });
    const url = URL.createObjectURL(svgBlob);
    const image = new Image();
    this.configureImageCanvas(type, image);
    image.src = url;
  }
  configureImageCanvas(type, chartImage) {
    chartImage.onload = () => {
      const canvas = document.createElement("canvas");
      const ctx = canvas.getContext("2d");
      if (!ctx) return;
      const chartElement = this.el.nativeElement.querySelector("#chart-id");
      const headerElement = this.el.nativeElement.querySelector(".po-chart-header");
      const chartWidth = chartElement.clientWidth;
      const chartHeight = chartElement.clientHeight;
      const headerHeight = headerElement?.clientHeight || 40;
      canvas.width = chartWidth;
      canvas.height = headerHeight + chartHeight + 40;
      ctx.fillStyle = this.getCSSVariable("--color-neutral-light-00");
      ctx.fillRect(0, 0, canvas.width, canvas.height);
      this.setHeaderProperties(ctx, headerElement, chartWidth, headerHeight);
      ctx.drawImage(chartImage, 0, headerHeight, chartWidth, chartHeight);
      const link = document.createElement("a");
      link.href = canvas.toDataURL(`image/${type}`);
      link.download = `grafico-exportado.${type}`;
      document.body.appendChild(link);
      link.click();
      document.body.removeChild(link);
    };
  }
  setHeaderProperties(ctx, headerElement, chartWidth, headerHeight) {
    ctx.fillStyle = this.getCSSVariable("--color-neutral-light-00");
    ctx.fillRect(0, 0, chartWidth, headerHeight);
    const titleElement = headerElement.querySelector(".po-chart-header-title");
    const title = titleElement?.innerText ?? "Gráfico";
    ctx.fillStyle = this.getCSSVariable("--color-neutral-dark-70");
    ctx.font = "bold 16px Roboto-Bold";
    ctx.textAlign = "left";
    ctx.fillText(title, 20, headerHeight / 2 + 5);
  }
  setPopupActions() {
    this.popupActions = [...this.popupActions, ...this.customActions || []];
  }
  static ɵfac = function PoChartComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoChartComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoChartComponent,
    selectors: [["po-chart"]],
    viewQuery: function PoChartComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c57, 5, ElementRef)(_c58, 5)(PoTooltipDirective, 5);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.targetRef = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.chartContainer = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.poTooltip = _t);
      }
    },
    hostBindings: function PoChartComponent_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("PoUiThemeChange", function PoChartComponent_PoUiThemeChange_HostBindingHandler($event) {
          return ctx.changeTheme($event);
        }, ɵɵresolveWindow);
      }
    },
    standalone: false,
    features: [ɵɵInheritDefinitionFeature, ɵɵNgOnChangesFeature],
    decls: 1,
    vars: 1,
    consts: [["chartContainer", ""], ["tooltipElement", ""], ["popup", ""], ["targetPopup", ""], ["id", "chart-container", 3, "expanded", "po-chart-container-gauge"], ["id", "chart-container"], [1, "po-chart-header"], [1, "description-chart"], ["id", "chart-id", 1, "po-chart", 3, "ngStyle"], ["id", "custom-tooltip", 3, "p-tooltip", "p-hide-arrow", "p-inner-html", "p-tooltip-position"], ["p-position", "bottom", 3, "p-actions", "p-target"], [1, "po-chart-header-title", 3, "mouseover", "focus", "ngClass", "p-tooltip"], [1, "po-chart-header-actions", "po-lg-3", "po-md-4", "po-sm-6"], ["p-icon", "ICON_LIST_BULLETS", "p-kind", "tertiary"], ["p-icon", "ICON_EXPAND", "p-kind", "tertiary"], ["p-icon", "ICON_MORE_VERT", "p-kind", "tertiary"], ["p-icon", "ICON_LIST_BULLETS", "p-kind", "tertiary", 3, "p-click"], ["p-icon", "ICON_EXPAND", "p-kind", "tertiary", 3, "p-click"], ["p-icon", "ICON_MORE_VERT", "p-kind", "tertiary", 3, "p-click"], [1, "po-md-12", "description-text", 3, "mouseover", "focus", "p-tooltip"]],
    template: function PoChartComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵconditionalCreate(0, PoChartComponent_Conditional_0_Template, 9, 19, "div", 4);
      }
      if (rf & 2) {
        ɵɵconditional(ctx.series.length ? 0 : -1);
      }
    },
    dependencies: [NgClass, NgStyle, PoTooltipDirective, PoButtonComponent, PoPopupComponent],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoChartComponent, [{
    type: Component,
    args: [{
      selector: "po-chart",
      standalone: false,
      template: `@if (series.length) {
  <div
    [class.expanded]="isExpanded"
    [class.po-chart-container-gauge]="isTypeGauge && !isExpanded && options?.showContainerGauge"
    id="chart-container"
  >
    @if (showHeader) {
      <div class="po-chart-header">
        <div
          class="po-chart-header-title"
          [ngClass]="
            !showPopup && options?.header?.hideTableDetails && options?.header?.hideExpand
              ? 'po-md-12'
              : 'po-lg-9 po-md-8 po-sm-6'
          "
          [p-tooltip]="tooltipTitle"
          (mouseover)="showTooltipTitle($event)"
          (focus)="showTooltipTitle($event)"
        >
          {{ title }}
        </div>
        @if (showPopup || !options?.header?.hideTableDetails || !options?.header?.hideExpand) {
          <div class="po-chart-header-actions po-lg-3 po-md-4 po-sm-6">
            @if (!options?.header?.hideTableDetails) {
              <po-button p-icon="ICON_LIST_BULLETS" p-kind="tertiary" (p-click)="openModal()"></po-button>
            }
            @if (!options?.header?.hideExpand) {
              <po-button p-icon="ICON_EXPAND" p-kind="tertiary" (p-click)="toggleExpand()"></po-button>
            }
            @if (showPopup) {
              <po-button #targetPopup p-icon="ICON_MORE_VERT" p-kind="tertiary" (p-click)="popup.toggle()"></po-button>
            }
          </div>
        }
      </div>
    }
    @if (options?.descriptionChart) {
      <div class="description-chart">
        <div
          class="po-md-12 description-text"
          [p-tooltip]="tooltipDescriptionGauge"
          (mouseover)="showTooltipDescription($event)"
          (focus)="showTooltipDescription($event)"
        >
          {{ options.descriptionChart }}
        </div>
      </div>
    }
    <div
      #chartContainer
      id="chart-id"
      class="po-chart"
      [ngStyle]="
        showHeader
          ? {
              height: height - headerHeight - paddingContainerGauge - descriptionHeight + 'px',
              'margin-top': chartMarginTop
            }
          : { height: height - paddingContainerGauge - descriptionHeight + 'px', 'margin-top': chartMarginTop }
      "
    ></div>
    <div
      #tooltipElement
      id="custom-tooltip"
      [p-tooltip]="tooltipText"
      [p-hide-arrow]="true"
      [p-inner-html]="true"
      [p-tooltip-position]="positionTooltip"
    ></div>
    <po-popup #popup [p-actions]="popupActions" p-position="bottom" [p-target]="targetRef"> </po-popup>
  </div>
}
`
    }]
  }], () => [], {
    poTooltip: [{
      type: ViewChildren,
      args: [PoTooltipDirective]
    }],
    targetRef: [{
      type: ViewChild,
      args: ["targetPopup", {
        read: ElementRef,
        static: false
      }]
    }],
    chartContainer: [{
      type: ViewChild,
      args: ["chartContainer"]
    }],
    changeTheme: [{
      type: HostListener,
      args: ["window:PoUiThemeChange", ["$event"]]
    }]
  });
})();
var PoChartModule = class _PoChartModule {
  static ɵfac = function PoChartModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoChartModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoChartModule,
    declarations: [PoChartComponent],
    imports: [CommonModule, PoTooltipModule, PoButtonModule, PoPopupModule],
    exports: [PoChartComponent]
  });
  static ɵinj = ɵɵdefineInjector({
    providers: [DecimalPipe, CurrencyPipe],
    imports: [CommonModule, PoTooltipModule, PoButtonModule, PoPopupModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoChartModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, PoTooltipModule, PoButtonModule, PoPopupModule],
      declarations: [PoChartComponent],
      exports: [PoChartComponent],
      providers: [DecimalPipe, CurrencyPipe]
    }]
  }], null, null);
})();
var PoContainerBaseComponent = class _PoContainerBaseComponent {
  _height;
  _noBorder = false;
  _noPadding = false;
  /**
   * @optional
   *
   * @description
   *
   * Define a altura do `po-container`.
   *
   * > Caso não seja definido um valor, a altura se ajustará de acordo com o conteúdo.
   */
  set height(value) {
    this._height = convertToInt(value);
  }
  get height() {
    return this._height;
  }
  /**
   * @optional
   *
   * @description
   *
   * Desabilita a borda e a sombra em torno do `po-container`.
   *
   * @default `false`
   */
  set noBorder(value) {
    this._noBorder = convertToBoolean(value);
  }
  get noBorder() {
    return this._noBorder;
  }
  /**
   * @optional
   *
   * @description
   *
   * Desabilita o espaçamento interno do `po-container`.
   *
   * @default `false`
   */
  set noPadding(value) {
    this._noPadding = convertToBoolean(value);
  }
  get noPadding() {
    return this._noPadding;
  }
  /**
   * @optional
   *
   * @description
   *
   * Título do Container.
   */
  title;
  static ɵfac = function PoContainerBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoContainerBaseComponent)();
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoContainerBaseComponent,
    inputs: {
      height: [0, "p-height", "height"],
      noBorder: [0, "p-no-border", "noBorder"],
      noPadding: [0, "p-no-padding", "noPadding"],
      title: [0, "p-title", "title"]
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoContainerBaseComponent, [{
    type: Directive
  }], null, {
    height: [{
      type: Input,
      args: ["p-height"]
    }],
    noBorder: [{
      type: Input,
      args: ["p-no-border"]
    }],
    noPadding: [{
      type: Input,
      args: ["p-no-padding"]
    }],
    title: [{
      type: Input,
      args: ["p-title"]
    }]
  });
})();
var PoContainerComponent = class _PoContainerComponent extends PoContainerBaseComponent {
  static ɵfac = /* @__PURE__ */ (() => {
    let ɵPoContainerComponent_BaseFactory;
    return function PoContainerComponent_Factory(__ngFactoryType__) {
      return (ɵPoContainerComponent_BaseFactory || (ɵPoContainerComponent_BaseFactory = ɵɵgetInheritedFactory(_PoContainerComponent)))(__ngFactoryType__ || _PoContainerComponent);
    };
  })();
  static ɵcmp = ɵɵdefineComponent({
    type: _PoContainerComponent,
    selectors: [["po-container"]],
    standalone: false,
    features: [ɵɵInheritDefinitionFeature],
    ngContentSelectors: _c12,
    decls: 4,
    vars: 7,
    consts: [[1, "po-container"], [1, "po-container-title"], [1, "po-container-content"]],
    template: function PoContainerComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵprojectionDef();
        ɵɵelementStart(0, "div", 0);
        ɵɵconditionalCreate(1, PoContainerComponent_Conditional_1_Template, 2, 1, "div", 1);
        ɵɵelementStart(2, "div", 2);
        ɵɵprojection(3);
        ɵɵelementEnd()();
      }
      if (rf & 2) {
        ɵɵadvance();
        ɵɵconditional(ctx.title ? 1 : -1);
        ɵɵadvance();
        ɵɵstyleProp("height", ctx.height ? ctx.height + "px" : "auto");
        ɵɵclassProp("po-container-no-border", ctx.noBorder)("po-container-no-padding", ctx.noPadding);
      }
    },
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoContainerComponent, [{
    type: Component,
    args: [{
      selector: "po-container",
      standalone: false,
      template: `<div class="po-container">
  @if (title) {
    <div class="po-container-title">
      {{ title }}
    </div>
  }
  <div
    class="po-container-content"
    [class.po-container-no-border]="noBorder"
    [class.po-container-no-padding]="noPadding"
    [style.height]="height ? height + 'px' : 'auto'"
  >
    <ng-content></ng-content>
  </div>
</div>
`
    }]
  }], null, null);
})();
var PoContainerModule = class _PoContainerModule {
  static ɵfac = function PoContainerModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoContainerModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoContainerModule,
    declarations: [PoContainerComponent],
    imports: [CommonModule],
    exports: [PoContainerComponent]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [CommonModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoContainerModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule],
      exports: [PoContainerComponent],
      declarations: [PoContainerComponent]
    }]
  }], null, null);
})();
var PoContextMenuBaseComponent = class _PoContextMenuBaseComponent {
  /**
   * Título do contexto superior
   */
  contextTitle = input("", __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "contextTitle"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-context-title"
  }));
  /**
   * Título principal do menu
   */
  title = input("", __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "title"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-title"
  }));
  /**
   * Lista de itens para renderização.
   *
   * > Ao receber os itens, o componente valida que apenas um item pode ter `selected: true`.
   * > Se mais de um item estiver com `selected: true`, apenas o primeiro será mantido como selecionado.
   */
  items = input([], __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "items"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-items"
  }));
  /**
   * Define se o menu está aberto ou fechado.
   *
   * Suporta two-way binding:
   *
   * ```html
   * <po-context-menu
   *   [(p-expanded)]="expanded"
   * />
   * ```
   *
   * ou
   *
   * ```html
   * <po-context-menu
   *   [(p-expanded)]="expanded"
   *   (p-expandedChange)="handlerExpanded($event)"
   * />
   * ```
   *
   * @default `true`
   */
  expanded = model(true, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "expanded"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-expanded"
  }));
  /**
   * Evento emitido ao selecionar um item. Emite o item selecionado.
   */
  itemSelected = output({
    alias: "p-item-selected"
  });
  static ɵfac = function PoContextMenuBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoContextMenuBaseComponent)();
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoContextMenuBaseComponent,
    inputs: {
      contextTitle: [1, "p-context-title", "contextTitle"],
      title: [1, "p-title", "title"],
      items: [1, "p-items", "items"],
      expanded: [1, "p-expanded", "expanded"]
    },
    outputs: {
      expanded: "p-expandedChange",
      itemSelected: "p-item-selected"
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoContextMenuBaseComponent, [{
    type: Directive
  }], null, {
    contextTitle: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-context-title",
        required: false
      }]
    }],
    title: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-title",
        required: false
      }]
    }],
    items: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-items",
        required: false
      }]
    }],
    expanded: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-expanded",
        required: false
      }]
    }, {
      type: Output,
      args: ["p-expandedChange"]
    }],
    itemSelected: [{
      type: Output,
      args: ["p-item-selected"]
    }]
  });
})();
var poContextMenuLiteralsDefault = {
  en: {
    close: "Close menu",
    open: "Open menu"
  },
  es: {
    close: "Cerrar menú",
    open: "Abrir menú"
  },
  pt: {
    close: "Fechar menu",
    open: "Abrir menu"
  },
  ru: {
    close: "Закрыть меню",
    open: "Открыть меню"
  }
};
var PoContextMenuComponent = class _PoContextMenuComponent extends PoContextMenuBaseComponent {
  languageService = inject(PoLanguageService);
  literals;
  _items = signal([], ...ngDevMode ? [{
    debugName: "_items"
  }] : (
    /* istanbul ignore next */
    []
  ));
  _titleTooltipCalculated = false;
  _contextTitleTooltipCalculated = false;
  titleTooltip = signal(null, ...ngDevMode ? [{
    debugName: "titleTooltip"
  }] : (
    /* istanbul ignore next */
    []
  ));
  contextTitleTooltip = signal(null, ...ngDevMode ? [{
    debugName: "contextTitleTooltip"
  }] : (
    /* istanbul ignore next */
    []
  ));
  constructor() {
    super();
    this.setLiterals();
    effect(() => {
      const currentItems = this.items();
      if (this.hasMultipleSelected(currentItems)) {
        this._items.set(this.sanitizeSelection(currentItems));
      } else {
        this._items.set(currentItems);
      }
      if (this.title() !== this.titleTooltip()) {
        this.titleTooltip.set(null);
        this._titleTooltipCalculated = false;
      }
      if (this.contextTitle() !== this.contextTitleTooltip()) {
        this.contextTitleTooltip.set(null);
        this._contextTitleTooltipCalculated = false;
      }
    });
  }
  toggleExpand() {
    this.expanded.update((v) => !v);
  }
  selectItem(item) {
    let selectedItem;
    const updatedItems = this._items().map((i2) => {
      const updated = __spreadProps(__spreadValues({}, i2), {
        selected: i2 === item
      });
      if (updated.selected) {
        selectedItem = updated;
      }
      return updated;
    });
    this._items.set(updatedItems);
    this.itemSelected.emit(selectedItem);
    if (item.action) {
      item.action(selectedItem);
    }
  }
  handlerItemTooltip(item, value) {
    if (item.tooltip) {
      return;
    }
    const label = value.firstElementChild;
    if (label.scrollHeight > label.offsetHeight) {
      this._items.update((items) => items.map((i2) => i2 === item ? __spreadProps(__spreadValues({}, i2), {
        tooltip: item.label
      }) : i2));
    }
  }
  handlerTitleTooltip(value) {
    if (this._titleTooltipCalculated) {
      return;
    }
    this.applyTooltipIfOverflows(value, this.title(), this.titleTooltip);
    this._titleTooltipCalculated = true;
  }
  handlerContextTitleTooltip(value) {
    if (this._contextTitleTooltipCalculated) {
      return;
    }
    this.applyTooltipIfOverflows(value, this.contextTitle(), this.contextTitleTooltip);
    this._contextTitleTooltipCalculated = true;
  }
  setLiterals() {
    this.literals = __spreadValues(__spreadValues({}, poContextMenuLiteralsDefault[this.languageService?.getLanguageDefault()]), poContextMenuLiteralsDefault[this.languageService?.getShortLanguage()]);
  }
  hasMultipleSelected(items) {
    let count2 = 0;
    for (const item of items) {
      if (item.selected && ++count2 > 1) {
        return true;
      }
    }
    return false;
  }
  sanitizeSelection(items) {
    const firstIndex = items.findIndex((i2) => i2.selected);
    return items.map((item, index) => item.selected && index !== firstIndex ? __spreadProps(__spreadValues({}, item), {
      selected: false
    }) : item);
  }
  applyTooltipIfOverflows(element, textValue, storageSignal) {
    const hasOverflow = element.scrollHeight > element.offsetHeight;
    if (hasOverflow) {
      storageSignal.set(textValue);
    }
  }
  static ɵfac = function PoContextMenuComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoContextMenuComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoContextMenuComponent,
    selectors: [["po-context-menu"]],
    standalone: false,
    features: [ɵɵInheritDefinitionFeature],
    decls: 6,
    vars: 6,
    consts: [["contextTitleRef", ""], ["titleRef", ""], ["itemRef", ""], [1, "po-context-menu"], [1, "po-context-menu-header"], ["type", "button", 1, "po-context-menu-toggle", 3, "click"], [3, "p-icon"], [1, "po-context-menu-header-content"], [1, "po-context-menu-body"], ["p-tooltip-position", "right", 1, "po-context-menu-context-title", 3, "p-tooltip"], ["p-tooltip-position", "right", 1, "po-context-menu-title", 3, "p-tooltip"], ["p-tooltip-position", "right", 1, "po-context-menu-context-title", 3, "mouseenter", "p-tooltip"], ["p-tooltip-position", "right", 1, "po-context-menu-title", 3, "mouseenter", "p-tooltip"], ["role", "menu", 1, "po-context-menu-list"], ["role", "menuitem", "tabindex", "0", "p-tooltip-position", "right", 1, "po-context-menu-list-item", 3, "po-context-menu-item-selected", "p-tooltip"], ["role", "menuitem", "tabindex", "0", "p-tooltip-position", "right", 1, "po-context-menu-list-item", 3, "click", "keydown.enter", "keydown.space", "mouseenter", "p-tooltip"], [1, "po-context-menu-item-label"]],
    template: function PoContextMenuComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "div", 3)(1, "div", 4)(2, "button", 5);
        ɵɵlistener("click", function PoContextMenuComponent_Template_button_click_2_listener() {
          return ctx.toggleExpand();
        });
        ɵɵelement(3, "po-icon", 6);
        ɵɵelementEnd();
        ɵɵconditionalCreate(4, PoContextMenuComponent_Conditional_4_Template, 3, 2, "div", 7);
        ɵɵelementEnd();
        ɵɵconditionalCreate(5, PoContextMenuComponent_Conditional_5_Template, 4, 0, "nav", 8);
        ɵɵelementEnd();
      }
      if (rf & 2) {
        ɵɵclassProp("po-context-menu-collapsed", !ctx.expanded());
        ɵɵadvance(2);
        ɵɵattribute("aria-label", ctx.expanded() ? ctx.literals.close : ctx.literals.open);
        ɵɵadvance();
        ɵɵproperty("p-icon", ctx.expanded() ? "ICON_SIDEBAR" : "ICON_SIDEBAR_SIMPLES");
        ɵɵadvance();
        ɵɵconditional(ctx.expanded() ? 4 : -1);
        ɵɵadvance();
        ɵɵconditional(ctx.expanded() ? 5 : -1);
      }
    },
    dependencies: [PoIconComponent, PoTooltipDirective],
    encapsulation: 2,
    changeDetection: 0
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoContextMenuComponent, [{
    type: Component,
    args: [{
      selector: "po-context-menu",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: `<div class="po-context-menu" [class.po-context-menu-collapsed]="!expanded()">
  <div class="po-context-menu-header">
    <button
      class="po-context-menu-toggle"
      type="button"
      [attr.aria-label]="expanded() ? literals.close : literals.open"
      (click)="toggleExpand()"
    >
      <po-icon [p-icon]="expanded() ? 'ICON_SIDEBAR' : 'ICON_SIDEBAR_SIMPLES'" />
    </button>

    @if (expanded()) {
      <div class="po-context-menu-header-content">
        @if (contextTitle()) {
          <span
            #contextTitleRef
            class="po-context-menu-context-title"
            (mouseenter)="handlerContextTitleTooltip(contextTitleRef)"
            p-tooltip-position="right"
            [p-tooltip]="contextTitleTooltip()"
          >
            {{ contextTitle() }}
          </span>
        }
        @if (title()) {
          <span
            #titleRef
            class="po-context-menu-title"
            (mouseenter)="handlerTitleTooltip(titleRef)"
            p-tooltip-position="right"
            [p-tooltip]="titleTooltip()"
          >
            {{ title() }}
          </span>
        }
      </div>
    }
  </div>

  @if (expanded()) {
    <nav class="po-context-menu-body">
      <ul class="po-context-menu-list" role="menu">
        @for (item of _items(); track $index) {
          <li
            #itemRef
            class="po-context-menu-list-item"
            role="menuitem"
            tabindex="0"
            [attr.aria-label]="item.label"
            [class.po-context-menu-item-selected]="item.selected"
            (click)="selectItem(item)"
            (keydown.enter)="selectItem(item)"
            (keydown.space)="selectItem(item)"
            (mouseenter)="handlerItemTooltip(item, itemRef)"
            p-tooltip-position="right"
            [p-tooltip]="item.tooltip"
          >
            <span class="po-context-menu-item-label">{{ item.label }}</span>
          </li>
        }
      </ul>
    </nav>
  }
</div>
`
    }]
  }], () => [], null);
})();
var PoContextMenuModule = class _PoContextMenuModule {
  static ɵfac = function PoContextMenuModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoContextMenuModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoContextMenuModule,
    declarations: [PoContextMenuComponent],
    imports: [CommonModule, PoIconModule, PoTooltipModule],
    exports: [PoContextMenuComponent]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [CommonModule, PoIconModule, PoTooltipModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoContextMenuModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, PoIconModule, PoTooltipModule],
      declarations: [PoContextMenuComponent],
      exports: [PoContextMenuComponent]
    }]
  }], null, null);
})();
var PoDisclaimerLiterals = {
  en: {
    remove: "Remove"
  },
  es: {
    remove: "Eliminar"
  },
  pt: {
    remove: "Remover"
  },
  ru: {
    remove: "удалять"
  }
};
var PO_DISCLAIMER_TYPES = ["default", "danger"];
var PO_DISCLAIMER_DEFAULT_TYPE = "default";
var PoDisclaimerBaseComponent = class _PoDisclaimerBaseComponent {
  languageService;
  /**
   * Label que aparecerá dentro do po-disclaimer.
   * Quando não for definido um label será apresentada a propriedade p-value.
   */
  label;
  /** Valor do po-disclaimer. */
  value;
  /** Nome da propriedade vinculada à este po-disclaimer. */
  property;
  disclaimerCustomWidth = 201;
  lastDisclaimer = false;
  appendInBody = false;
  tooltipPosition = "bottom";
  /**
   * @optional
   *
   * @description
   *
   * Evento disparado ao fechar o disclaimer.
   * Para este evento será passado como parâmetro um objeto com value, label e property.
   */
  closeAction = new EventEmitter();
  clickNumber = new EventEmitter();
  literals;
  showDisclaimer = true;
  _type = "default";
  _hideClose = false;
  /**
   * @description
   *
   * Esta propriedade esconde o botão para fechamento do po-disclaimer, ao utilizar esta propriedade
   * sem passar valor a mesma é setada como false, onde o botão de fechamento está visível.
   *
   * @default false
   */
  set hideClose(value) {
    this._hideClose = value === "" ? true : convertToBoolean(value);
  }
  get hideClose() {
    return this._hideClose;
  }
  /**
   * @description
   *
   * Tipo do po-disclaimer. Pode ser 'default' ou 'danger'.
   *
   * @default default
   * @optional
   */
  set type(type) {
    this._type = PO_DISCLAIMER_TYPES.includes(type) ? type : PO_DISCLAIMER_DEFAULT_TYPE;
  }
  get type() {
    return this._type;
  }
  constructor(languageService) {
    this.languageService = languageService;
    const language = this.languageService?.getShortLanguage();
    this.literals = __spreadValues({}, PoDisclaimerLiterals[language]);
  }
  close() {
    this.showDisclaimer = false;
    this.closeAction.emit({
      value: this.value,
      label: this.label,
      property: this.property
    });
  }
  getLabel() {
    return this.label ? this.label : this.value;
  }
  setAriaLabel() {
    return this.label ? this.label + " " + this.literals.remove : this.value + " " + this.literals.remove;
  }
  static ɵfac = function PoDisclaimerBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoDisclaimerBaseComponent)(ɵɵdirectiveInject(PoLanguageService));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoDisclaimerBaseComponent,
    inputs: {
      label: [0, "p-label", "label"],
      value: [0, "p-value", "value"],
      property: [0, "p-property", "property"],
      disclaimerCustomWidth: [0, "p-disclaimer-custom-width", "disclaimerCustomWidth"],
      lastDisclaimer: [0, "p-last-disclaimer", "lastDisclaimer"],
      appendInBody: [0, "p-append-in-body", "appendInBody"],
      tooltipPosition: [0, "p-tooltip-position", "tooltipPosition"],
      hideClose: [0, "p-hide-close", "hideClose"],
      type: [0, "p-type", "type"]
    },
    outputs: {
      closeAction: "p-close-action",
      clickNumber: "p-click-number"
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoDisclaimerBaseComponent, [{
    type: Directive
  }], () => [{
    type: PoLanguageService
  }], {
    label: [{
      type: Input,
      args: ["p-label"]
    }],
    value: [{
      type: Input,
      args: ["p-value"]
    }],
    property: [{
      type: Input,
      args: ["p-property"]
    }],
    disclaimerCustomWidth: [{
      type: Input,
      args: ["p-disclaimer-custom-width"]
    }],
    lastDisclaimer: [{
      type: Input,
      args: ["p-last-disclaimer"]
    }],
    appendInBody: [{
      type: Input,
      args: ["p-append-in-body"]
    }],
    tooltipPosition: [{
      type: Input,
      args: ["p-tooltip-position"]
    }],
    closeAction: [{
      type: Output,
      args: ["p-close-action"]
    }],
    clickNumber: [{
      type: Output,
      args: ["p-click-number"]
    }],
    hideClose: [{
      type: Input,
      args: ["p-hide-close"]
    }],
    type: [{
      type: Input,
      args: ["p-type"]
    }]
  });
})();
var PoDisclaimerComponent = class _PoDisclaimerComponent extends PoDisclaimerBaseComponent {
  disclaimerContainer;
  onKeyPress(event) {
    if (PoUtils.isKeyCodeEnter(event)) {
      this.close();
    }
  }
  emitLastDisclaimer(isLast) {
    if (isLast) {
      this.clickNumber.emit();
    }
  }
  getWidthDisclaimer() {
    return this.disclaimerContainer.nativeElement.offsetWidth > this.disclaimerCustomWidth;
  }
  static ɵfac = /* @__PURE__ */ (() => {
    let ɵPoDisclaimerComponent_BaseFactory;
    return function PoDisclaimerComponent_Factory(__ngFactoryType__) {
      return (ɵPoDisclaimerComponent_BaseFactory || (ɵPoDisclaimerComponent_BaseFactory = ɵɵgetInheritedFactory(_PoDisclaimerComponent)))(__ngFactoryType__ || _PoDisclaimerComponent);
    };
  })();
  static ɵcmp = ɵɵdefineComponent({
    type: _PoDisclaimerComponent,
    selectors: [["po-disclaimer"]],
    viewQuery: function PoDisclaimerComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c60, 7);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.disclaimerContainer = _t.first);
      }
    },
    standalone: false,
    features: [ɵɵInheritDefinitionFeature],
    decls: 3,
    vars: 1,
    consts: [["disclaimerContainer", ""], [1, "disclaimer-container"], [1, "po-disclaimer", 3, "po-disclaimer-remove-all", "po-disclaimer-danger", "p-tooltip", "p-append-in-body", "p-tooltip-position", "tabindex"], [1, "po-disclaimer", 3, "click", "keydown.enter", "p-tooltip", "p-append-in-body", "p-tooltip-position", "tabindex"], [1, "po-disclaimer-label"], [1, "label"], ["p-icon", "ICON_CLOSE", "tabindex", "0", 1, "po-disclaimer-remove", "po-clickable", 3, "po-disclaimer-remove-danger"], ["p-icon", "ICON_CLOSE", "tabindex", "0", 1, "po-disclaimer-remove", "po-clickable", 3, "click", "keydown"]],
    template: function PoDisclaimerComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "div", 1, 0);
        ɵɵconditionalCreate(2, PoDisclaimerComponent_Conditional_2_Template, 5, 12, "div", 2);
        ɵɵelementEnd();
      }
      if (rf & 2) {
        ɵɵadvance(2);
        ɵɵconditional(ctx.showDisclaimer === true ? 2 : -1);
      }
    },
    dependencies: [PoIconComponent, PoTooltipDirective],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoDisclaimerComponent, [{
    type: Component,
    args: [{
      selector: "po-disclaimer",
      standalone: false,
      template: `<div #disclaimerContainer class="disclaimer-container">
  @if (showDisclaimer === true) {
    <div
      class="po-disclaimer"
      [class.po-disclaimer-remove-all]="hideClose"
      [class.po-disclaimer-danger]="type === 'danger'"
      [p-tooltip]="getWidthDisclaimer() ? getLabel() : ''"
      [p-append-in-body]="appendInBody"
      [p-tooltip-position]="tooltipPosition"
      [tabindex]="hideClose ? '0' : '-1'"
      (click)="emitLastDisclaimer(lastDisclaimer)"
      (keydown.enter)="emitLastDisclaimer(lastDisclaimer)"
    >
      <div class="po-disclaimer-label" [class.show-close]="!hideClose">
        <span class="label">
          {{ getLabel() }}
        </span>
      </div>
      @if (hideClose === false) {
        <po-icon
          p-icon="ICON_CLOSE"
          [attr.aria-label]="setAriaLabel()"
          [attr.role]="!hideClose ? 'button' : ''"
          class="po-disclaimer-remove po-clickable"
          tabindex="0"
          [class.po-disclaimer-remove-danger]="type === 'danger'"
          (click)="close()"
          (keydown)="onKeyPress($event)"
        ></po-icon>
      }
    </div>
  }
</div>
`
    }]
  }], null, {
    disclaimerContainer: [{
      type: ViewChild,
      args: ["disclaimerContainer", {
        static: true
      }]
    }]
  });
})();
var PoDisclaimerModule = class _PoDisclaimerModule {
  static ɵfac = function PoDisclaimerModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoDisclaimerModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoDisclaimerModule,
    declarations: [PoDisclaimerComponent],
    imports: [CommonModule, PoIconModule, PoTooltipModule],
    exports: [PoDisclaimerComponent]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [CommonModule, PoIconModule, PoTooltipModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoDisclaimerModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, PoIconModule, PoTooltipModule],
      declarations: [PoDisclaimerComponent],
      exports: [PoDisclaimerComponent]
    }]
  }], null, null);
})();
var poDisclaimerGroupLiteralsDefault = {
  en: {
    removeAll: "Remove all"
  },
  es: {
    removeAll: "Eliminar todos"
  },
  pt: {
    removeAll: "Remover todos"
  },
  ru: {
    removeAll: "Удалить все"
  }
};
var PoDisclaimerGroupBaseComponent = class _PoDisclaimerGroupBaseComponent {
  changeDetector;
  /** Título do grupo de *disclaimers*. */
  title;
  /**
   * @optional
   *
   * @description
   *
   * Função que será disparada quando a lista de *disclaimers* for modificada.
   */
  change = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Função que será disparada quando um *disclaimer* for removido da lista de *disclaimers* pelo usuário.
   *
   * Recebe como parâmetro um objeto conforme a interface `PoDisclaimerGroupRemoveAction`.
   */
  remove = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Função que será disparada quando todos os *disclaimers* forem removidos da lista de *disclaimers* pelo usuário,
   * utilizando o botão "remover todos".
   *
   * Recebe como parâmetro uma lista contendo todos os `disclaimers` removidos.
   */
  removeAll = new EventEmitter();
  literals;
  _disclaimers = [];
  _hideRemoveAll = false;
  differ;
  previousDisclaimers = [];
  /** Lista de *disclaimers*. */
  /**
   * @description
   *
   * Lista de *disclaimers*.
   *
   * Para que a lista de *disclaimers* seja atualizada dinamicamente deve-se passar uma nova referência do array de `PoDisclaimer`.
   *
   * Exemplo adicionando um *disclaimer* no array:
   *
   * ```
   * this.disclaimers = [...this.disclaimers, disclaimer];
   * ```
   *
   * ou
   *
   * ```
   * this.disclaimers = this.disclaimers.concat(disclaimer);
   * ```
   */
  set disclaimers(value) {
    this.previousDisclaimers = [...this.disclaimers];
    this._disclaimers = this.checkDisclaimers(value);
  }
  get disclaimers() {
    return this._disclaimers;
  }
  /**
   * @optional
   *
   * @description
   *
   * Oculta o botão para remover todos os *disclaimers* do grupo.
   *
   * > Por padrão, o mesmo é exibido à partir de dois ou mais *disclaimers* com a opção `hideClose` habilitada.
   *
   * @default `false`
   */
  set hideRemoveAll(value) {
    this._hideRemoveAll = value === "" ? true : convertToBoolean(value);
  }
  get hideRemoveAll() {
    return this._hideRemoveAll;
  }
  constructor(differs, languageService, changeDetector) {
    this.changeDetector = changeDetector;
    const language = languageService.getShortLanguage();
    this.differ = differs.find([]).create(null);
    this.literals = __spreadValues(__spreadValues({}, poDisclaimerGroupLiteralsDefault[poLocaleDefault]), poDisclaimerGroupLiteralsDefault[language]);
  }
  ngDoCheck() {
    this.checkChanges();
  }
  isRemoveAll() {
    return !this.hideRemoveAll && this.disclaimers.filter((c) => !c.hideClose).length > 1;
  }
  onKeyPress(event) {
    if (PoUtils.isKeyCodeEnter(event)) {
      this.removeAllItems();
    }
  }
  removeAllItems() {
    const removeItems = [];
    this.disclaimers.forEach((disclaimer) => {
      if (!disclaimer.hideClose) {
        removeItems.push(disclaimer);
      }
    });
    removeItems.forEach((disclaimer) => this.removeDisclaimer(disclaimer));
    this.emitChangeDisclaimers();
    this.removeAll.emit([...removeItems]);
  }
  removeDisclaimer(disclaimer) {
    const itemIndex = this.disclaimers.findIndex((d) => d["$id"] === disclaimer["$id"]);
    this.disclaimers.splice(itemIndex, 1);
  }
  emitChangeDisclaimers() {
    setTimeout(() => {
      this.change.emit(this.disclaimers);
    });
    this.previousDisclaimers = [...this._disclaimers];
    this.changeDetector?.detectChanges();
  }
  checkChanges() {
    if (this.differ) {
      const changes = this.differ.diff(this.disclaimers);
      if (changes && this.disclaimersAreChanged(this.disclaimers)) {
        this.emitChangeDisclaimers();
      }
    } else {
      this.changeDetector?.detectChanges();
    }
  }
  checkDisclaimers(disclaimers) {
    if (Array.isArray(disclaimers)) {
      for (let i2 = 0; i2 < disclaimers.length; i2++) {
        const disclaimer = disclaimers[i2];
        if (disclaimer.value || disclaimer.value === 0 || disclaimer.value === false) {
          disclaimer["$id"] = uuid();
        } else {
          disclaimers.splice(i2, 1);
          i2--;
        }
      }
      return disclaimers;
    }
    return [];
  }
  disclaimersAreChanged(disclaimers) {
    const currentValues = disclaimers;
    if (currentValues.length !== this.previousDisclaimers.length) {
      return true;
    }
    return currentValues.some((disclaimer, index) => disclaimer.value !== this.previousDisclaimers[index].value || disclaimer.property !== this.previousDisclaimers[index].property);
  }
  static ɵfac = function PoDisclaimerGroupBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoDisclaimerGroupBaseComponent)(ɵɵdirectiveInject(IterableDiffers), ɵɵdirectiveInject(PoLanguageService), ɵɵdirectiveInject(ChangeDetectorRef));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoDisclaimerGroupBaseComponent,
    inputs: {
      title: [0, "p-title", "title"],
      disclaimers: [0, "p-disclaimers", "disclaimers"],
      hideRemoveAll: [0, "p-hide-remove-all", "hideRemoveAll"]
    },
    outputs: {
      change: "p-change",
      remove: "p-remove",
      removeAll: "p-remove-all"
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoDisclaimerGroupBaseComponent, [{
    type: Directive
  }], () => [{
    type: IterableDiffers
  }, {
    type: PoLanguageService
  }, {
    type: ChangeDetectorRef
  }], {
    title: [{
      type: Input,
      args: ["p-title"]
    }],
    change: [{
      type: Output,
      args: ["p-change"]
    }],
    remove: [{
      type: Output,
      args: ["p-remove"]
    }],
    removeAll: [{
      type: Output,
      args: ["p-remove-all"]
    }],
    disclaimers: [{
      type: Input,
      args: ["p-disclaimers"]
    }],
    hideRemoveAll: [{
      type: Input,
      args: ["p-hide-remove-all"]
    }]
  });
})();
var PoDisclaimerRemoveComponent = class _PoDisclaimerRemoveComponent {
  /** Texto exibido.  */
  label;
  /** Ação para remover todos.  */
  removeAllAction = new EventEmitter();
  // Emite a ação de remover todos.
  removeAction() {
    this.removeAllAction.emit();
  }
  static ɵfac = function PoDisclaimerRemoveComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoDisclaimerRemoveComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoDisclaimerRemoveComponent,
    selectors: [["po-disclaimer-remove"]],
    inputs: {
      label: [0, "p-label", "label"]
    },
    outputs: {
      removeAllAction: "p-remove-all-action"
    },
    standalone: false,
    decls: 1,
    vars: 1,
    consts: [["p-type", "danger", 1, "po-clickable", 3, "p-click", "p-value"]],
    template: function PoDisclaimerRemoveComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "po-tag", 0);
        ɵɵlistener("p-click", function PoDisclaimerRemoveComponent_Template_po_tag_p_click_0_listener() {
          return ctx.removeAction();
        });
        ɵɵelementEnd();
      }
      if (rf & 2) {
        ɵɵproperty("p-value", ctx.label);
      }
    },
    dependencies: [PoTagComponent],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoDisclaimerRemoveComponent, [{
    type: Component,
    args: [{
      selector: "po-disclaimer-remove",
      standalone: false,
      template: '<po-tag class="po-clickable" p-type="danger" [p-value]="label" (p-click)="removeAction()"> </po-tag>\n'
    }]
  }], null, {
    label: [{
      type: Input,
      args: ["p-label"]
    }],
    removeAllAction: [{
      type: Output,
      args: ["p-remove-all-action"]
    }]
  });
})();
var PoDisclaimerGroupComponent = class _PoDisclaimerGroupComponent extends PoDisclaimerGroupBaseComponent {
  subscription = new Subscription();
  el = inject(ElementRef);
  constructor() {
    const differs = inject(IterableDiffers);
    const languageService = inject(PoLanguageService);
    const changeDetector = inject(ChangeDetectorRef);
    super(differs, languageService, changeDetector);
    this.changeDetector = changeDetector;
  }
  ngAfterViewInit() {
    this.handleKeyboardNavigationTag();
  }
  ngOnChanges(changes) {
    if (changes.disclaimers) {
      setTimeout(() => {
        this.handleKeyboardNavigationTag();
      });
    }
  }
  onCloseAction(disclaimer, event) {
    const index = this.disclaimers.findIndex((option) => option.value === disclaimer.value);
    this.removeDisclaimer(disclaimer);
    this.emitChangeDisclaimers();
    this.remove.emit({
      removedDisclaimer: __spreadValues({}, disclaimer),
      currentDisclaimers: [...this.disclaimers]
    });
    setTimeout(() => {
      this.focusOnNextTag(index, event);
    }, 300);
  }
  focusOnNextTag(indexClosed, clickOrEnter) {
    if (clickOrEnter === "enter") {
      const tagRemoveElements = this.el.nativeElement.querySelectorAll(".po-tag-remove");
      indexClosed = indexClosed || indexClosed === 0 ? indexClosed : tagRemoveElements.length;
      this.focusOnRemoveTag(tagRemoveElements, indexClosed);
    } else {
      indexClosed = 0;
    }
    this.handleKeyboardNavigationTag(indexClosed);
  }
  handleKeyboardNavigationTag(initialIndex = 0) {
    this.subscription.unsubscribe();
    this.subscription = new Subscription();
    const tagRemoveElements = this.el.nativeElement.querySelectorAll(".po-tag-remove");
    this.initializeTagRemoveElements(tagRemoveElements, initialIndex);
  }
  handleArrowLeft(tagRemoveElements, index) {
    if (index > 0) {
      this.setTabIndex(tagRemoveElements[index], -1);
      tagRemoveElements[index - 1].focus();
      this.setTabIndex(tagRemoveElements[index - 1], 0);
    }
  }
  handleArrowRight(tagRemoveElements, index) {
    if (index < tagRemoveElements.length - 1) {
      this.setTabIndex(tagRemoveElements[index], -1);
      tagRemoveElements[index + 1].focus();
      this.setTabIndex(tagRemoveElements[index + 1], 0);
    }
  }
  setTabIndex(element, tabIndex) {
    element.setAttribute("tabindex", tabIndex);
  }
  initializeTagRemoveElements(tagRemoveElements, initialIndex) {
    tagRemoveElements.forEach((tagRemoveElement, index) => {
      if (index === initialIndex) {
        this.setTabIndex(tagRemoveElements[initialIndex], 0);
      } else if (tagRemoveElements.length === initialIndex) {
        this.setTabIndex(tagRemoveElements[initialIndex - 1], 0);
      } else {
        this.setTabIndex(tagRemoveElement, -1);
      }
      this.subscription.add(fromEvent(tagRemoveElement, "keydown").subscribe((event) => {
        this.handleKeyDown(event, tagRemoveElements, index);
      }));
      if (index !== 0) {
        this.subscription.add(fromEvent(tagRemoveElements, "blur").subscribe(() => {
          this.setTabIndex(tagRemoveElements[index], -1);
          this.setTabIndex(tagRemoveElements[0], 0);
        }));
      }
    });
  }
  handleKeyDown(event, tagRemoveElements, index) {
    const KEY_SPACE = "Space";
    const KEY_ARROW_LEFT = "ArrowLeft";
    const KEY_ARROW_RIGHT = "ArrowRight";
    if (event.code === KEY_SPACE) {
      event.preventDefault();
      event.stopPropagation();
    }
    if (event.key === KEY_ARROW_LEFT) {
      this.handleArrowLeft(tagRemoveElements, index);
    } else if (event.key === KEY_ARROW_RIGHT) {
      this.handleArrowRight(tagRemoveElements, index);
    }
  }
  focusOnRemoveTag(tag, indexClosed) {
    if (tag.length === indexClosed) {
      tag[indexClosed - 1]?.focus();
    } else {
      tag[indexClosed]?.focus();
    }
  }
  static ɵfac = function PoDisclaimerGroupComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoDisclaimerGroupComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoDisclaimerGroupComponent,
    selectors: [["po-disclaimer-group"]],
    standalone: false,
    features: [ɵɵInheritDefinitionFeature, ɵɵNgOnChangesFeature],
    decls: 1,
    vars: 1,
    consts: [[1, "po-disclaimer-group"], [1, "po-disclaimer-group-title"], [1, "po-disclaimer-group-disclaimer-align", 3, "p-label"], [1, "po-disclaimer-group-disclaimer-align", 3, "p-value", "p-removable"], [1, "po-disclaimer-group-disclaimer-align", 3, "keydown", "p-remove-all-action", "p-label"], [1, "po-disclaimer-group-disclaimer-align", 3, "p-close", "p-value", "p-removable"]],
    template: function PoDisclaimerGroupComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵconditionalCreate(0, PoDisclaimerGroupComponent_Conditional_0_Template, 5, 2, "div", 0);
      }
      if (rf & 2) {
        ɵɵconditional(ctx.disclaimers.length > 0 ? 0 : -1);
      }
    },
    dependencies: [PoTagComponent, PoDisclaimerRemoveComponent],
    encapsulation: 2,
    changeDetection: 0
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoDisclaimerGroupComponent, [{
    type: Component,
    args: [{
      selector: "po-disclaimer-group",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: '@if (disclaimers.length > 0) {\n  <div class="po-disclaimer-group">\n    @if (title) {\n      <div class="po-disclaimer-group-title">{{ title }}</div>\n    }\n    @if (isRemoveAll()) {\n      <po-disclaimer-remove\n        class="po-disclaimer-group-disclaimer-align"\n        [p-label]="literals.removeAll"\n        (keydown)="onKeyPress($event)"\n        (p-remove-all-action)="removeAllItems()"\n      >\n      </po-disclaimer-remove>\n    }\n    @for (disclaimer of disclaimers; track disclaimer) {\n      <po-tag\n        class="po-disclaimer-group-disclaimer-align"\n        [p-value]="disclaimer.label || disclaimer.value"\n        [p-removable]="!disclaimer.hideClose"\n        (p-close)="onCloseAction(disclaimer, $event)"\n      >\n      </po-tag>\n    }\n  </div>\n}\n'
    }]
  }], () => [], null);
})();
var PoDisclaimerGroupModule = class _PoDisclaimerGroupModule {
  static ɵfac = function PoDisclaimerGroupModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoDisclaimerGroupModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoDisclaimerGroupModule,
    declarations: [PoDisclaimerGroupComponent, PoDisclaimerRemoveComponent],
    imports: [CommonModule, PoDisclaimerModule, PoTagModule],
    exports: [PoDisclaimerGroupComponent]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [CommonModule, PoDisclaimerModule, PoTagModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoDisclaimerGroupModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, PoDisclaimerModule, PoTagModule],
      declarations: [PoDisclaimerGroupComponent, PoDisclaimerRemoveComponent],
      exports: [PoDisclaimerGroupComponent]
    }]
  }], null, null);
})();
var poDropdownDefaultPosition = "bottom-left";
var PoDropdownBaseComponent = class _PoDropdownBaseComponent {
  /** Adiciona um rótulo ao `dropdown`. */
  label;
  icon = "ICON_ARROW_DOWN";
  open = false;
  _actions;
  _disabled = false;
  _size = void 0;
  _initialSize = void 0;
  _position = poDropdownDefaultPosition;
  /** Lista de ações que serão exibidas no componente. */
  set actions(value) {
    this._actions = Array.isArray(value) ? value : [];
  }
  get actions() {
    return this._actions;
  }
  /**
   * @optional
   *
   * @description
   *
   * Desabilita o campo.
   *
   * @default `false`
   */
  set disabled(value) {
    this._disabled = convertToBoolean(value);
  }
  get disabled() {
    return this._disabled;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define o tamanho do componente:
   * - `small`: altura do button como 32px (disponível apenas para acessibilidade AA).
   * - `medium`: altura do button como 44px.
   *
   * > Caso a acessibilidade AA não esteja configurada, o tamanho `medium` será mantido.
   * Para mais detalhes, consulte a documentação do [po-theme](https://po-ui.io/documentation/po-theme).
   *
   * @default `medium`
   */
  set size(value) {
    this._initialSize = value;
    this.applySizeBasedOnA11y();
  }
  get size() {
    return this._size ?? getDefaultSizeFn(PoFieldSize);
  }
  /**
   * @optional
   *
   * @description
   *
   * Define a posição preferencial de abertura do popup do dropdown em relação ao botão.
   *
   * Posições válidas:
   * - `right`: No lado direito.
   * - `right-bottom`: No lado direito inferior.
   * - `right-top`: No lado direito superior.
   * - `bottom`: Abaixo.
   * - `bottom-left`: Abaixo e à esquerda (padrão).
   * - `bottom-right`: Abaixo e à direita.
   * - `left`: No lado esquerdo.
   * - `left-top`: No lado esquerdo superior.
   * - `left-bottom`: No lado esquerdo inferior.
   * - `top`: Acima.
   * - `top-right`: Acima e à direita.
   * - `top-left`: Acima e à esquerda.
   *
   * > O popup será rotacionado automaticamente caso não caiba na posição definida.
   *
   * @default `bottom-left`
   */
  set position(value) {
    this._position = PO_CONTROL_POSITIONS.includes(value) ? value : poDropdownDefaultPosition;
  }
  get position() {
    return this._position;
  }
  get popupCustomPositions() {
    if (this._position === "bottom-right") {
      return ["bottom-right", "top-right"];
    }
    if (this._position === "top-left") {
      return ["top-left", "bottom-left"];
    }
    if (this._position === "top-right") {
      return ["top-right", "bottom-right"];
    }
    return ["bottom-left", "top-left"];
  }
  onThemeChange() {
    this.applySizeBasedOnA11y();
  }
  applySizeBasedOnA11y() {
    const size = validateSizeFn(this._initialSize, PoFieldSize);
    this._size = size;
  }
  static ɵfac = function PoDropdownBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoDropdownBaseComponent)();
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoDropdownBaseComponent,
    hostVars: 1,
    hostBindings: function PoDropdownBaseComponent_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("PoUiThemeChange", function PoDropdownBaseComponent_PoUiThemeChange_HostBindingHandler() {
          return ctx.onThemeChange();
        }, ɵɵresolveWindow);
      }
      if (rf & 2) {
        ɵɵattribute("p-size", ctx.size);
      }
    },
    inputs: {
      label: [0, "p-label", "label"],
      actions: [0, "p-actions", "actions"],
      disabled: [0, "p-disabled", "disabled"],
      size: [0, "p-size", "size"],
      position: [0, "p-position", "position"]
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoDropdownBaseComponent, [{
    type: Directive
  }], null, {
    label: [{
      type: Input,
      args: ["p-label"]
    }],
    actions: [{
      type: Input,
      args: ["p-actions"]
    }],
    disabled: [{
      type: Input,
      args: ["p-disabled"]
    }],
    size: [{
      type: Input,
      args: ["p-size"]
    }, {
      type: HostBinding,
      args: ["attr.p-size"]
    }],
    position: [{
      type: Input,
      args: ["p-position"]
    }],
    onThemeChange: [{
      type: HostListener,
      args: ["window:PoUiThemeChange"]
    }]
  });
})();
var PoDropdownComponent = class _PoDropdownComponent extends PoDropdownBaseComponent {
  renderer = inject(Renderer2);
  changeDetector = inject(ChangeDetectorRef);
  dropdownRef;
  popupRef;
  clickoutListener;
  resizeListener;
  onKeyDown(event) {
    if (PoUtils.isKeyCodeEnter(event)) {
      this.toggleDropdown();
    }
  }
  toggleDropdown() {
    this.dropdownRef && !this.open && !this.disabled ? this.showDropdown() : this.hideDropdown();
  }
  checkClickArea(event) {
    return this.dropdownRef?.nativeElement.contains(event.target);
  }
  hideDropdown() {
    this.icon = "ICON_ARROW_DOWN";
    this.removeListeners();
    this.popupRef.close();
    this.open = false;
    this.changeDetector.detectChanges();
  }
  initializeListeners() {
    this.clickoutListener = this.renderer.listen("document", "click", (event) => {
      this.wasClickedOnDropdown(event);
    });
    this.resizeListener = this.renderer.listen("window", "resize", () => {
      this.hideDropdown();
    });
    window.addEventListener("scroll", this.onScroll, true);
  }
  onScroll = ({
    target
  }) => {
    if (this.open && target.className !== "po-popup-container" && !this.isDropdownClosed()) {
      this.hideDropdown();
    }
  };
  isDropdownClosed() {
    const dropdownRect = this.dropdownRef.nativeElement.getBoundingClientRect();
    return dropdownRect.top >= 0 && dropdownRect.bottom <= window.innerHeight;
  }
  removeListeners() {
    if (this.clickoutListener) {
      this.clickoutListener();
    }
    if (this.resizeListener) {
      this.resizeListener();
    }
    window.removeEventListener("scroll", this.onScroll, true);
  }
  showDropdown() {
    this.icon = "ICON_ARROW_UP";
    this.initializeListeners();
    this.popupRef.open();
    this.open = true;
    this.changeDetector.detectChanges();
  }
  wasClickedOnDropdown(event) {
    const clickedOnDropdown = this.checkClickArea(event);
    if (!clickedOnDropdown) {
      this.hideDropdown();
    }
  }
  static ɵfac = /* @__PURE__ */ (() => {
    let ɵPoDropdownComponent_BaseFactory;
    return function PoDropdownComponent_Factory(__ngFactoryType__) {
      return (ɵPoDropdownComponent_BaseFactory || (ɵPoDropdownComponent_BaseFactory = ɵɵgetInheritedFactory(_PoDropdownComponent)))(__ngFactoryType__ || _PoDropdownComponent);
    };
  })();
  static ɵcmp = ɵɵdefineComponent({
    type: _PoDropdownComponent,
    selectors: [["po-dropdown"]],
    viewQuery: function PoDropdownComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c61, 7, ElementRef)(_c31, 5);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.dropdownRef = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.popupRef = _t.first);
      }
    },
    standalone: false,
    features: [ɵɵInheritDefinitionFeature],
    decls: 8,
    vars: 14,
    consts: [["dropdownRef", ""], ["popupRef", ""], [1, "po-dropdown", 3, "click", "keydown", "tabindex"], [1, "po-dropdown-button", 3, "ngClass"], [1, "po-dropdown-icon", 3, "p-icon"], ["p-hide-arrow", "", "p-is-corner-align", "", 3, "p-position", "p-actions", "p-custom-positions", "p-size", "p-target"]],
    template: function PoDropdownComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "div", 2, 0);
        ɵɵlistener("click", function PoDropdownComponent_Template_div_click_0_listener() {
          return ctx.toggleDropdown();
        })("keydown", function PoDropdownComponent_Template_div_keydown_0_listener($event) {
          return ctx.onKeyDown($event);
        });
        ɵɵelementStart(2, "div", 3)(3, "span");
        ɵɵtext(4);
        ɵɵelementEnd();
        ɵɵelement(5, "po-icon", 4);
        ɵɵelementEnd()();
        ɵɵelement(6, "po-popup", 5, 1);
      }
      if (rf & 2) {
        const dropdownRef_r1 = ɵɵreference(1);
        ɵɵproperty("tabindex", ɵɵinterpolate(ctx.disabled ? -1 : 0));
        ɵɵattribute("disabled", ctx.disabled);
        ɵɵadvance(2);
        ɵɵproperty("ngClass", ɵɵpureFunction2(11, _c62, ctx.disabled, ctx.open));
        ɵɵadvance(2);
        ɵɵtextInterpolate(ctx.label);
        ɵɵadvance();
        ɵɵproperty("p-icon", ctx.icon);
        ɵɵadvance();
        ɵɵproperty("p-position", ctx.position)("p-actions", ctx.actions)("p-custom-positions", ctx.popupCustomPositions)("p-size", ctx.size)("p-target", dropdownRef_r1);
      }
    },
    dependencies: [NgClass, PoPopupComponent, PoIconComponent],
    encapsulation: 2,
    changeDetection: 0
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoDropdownComponent, [{
    type: Component,
    args: [{
      selector: "po-dropdown",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: `<div
  #dropdownRef
  class="po-dropdown"
  [attr.disabled]="disabled"
  tabindex="{{ disabled ? -1 : 0 }}"
  (click)="toggleDropdown()"
  (keydown)="onKeyDown($event)"
>
  <div
    class="po-dropdown-button"
    [ngClass]="{ 'po-dropdown-button-disabled': disabled, 'po-dropdown-button-open': open }"
  >
    <span>{{ label }}</span>
    <po-icon class="po-dropdown-icon" [p-icon]="icon"></po-icon>
  </div>
</div>

<po-popup
  #popupRef
  p-hide-arrow
  p-is-corner-align
  [p-position]="position"
  [p-actions]="actions"
  [p-custom-positions]="popupCustomPositions"
  [p-size]="size"
  [p-target]="dropdownRef"
>
</po-popup>
`
    }]
  }], null, {
    dropdownRef: [{
      type: ViewChild,
      args: ["dropdownRef", {
        read: ElementRef,
        static: true
      }]
    }],
    popupRef: [{
      type: ViewChild,
      args: ["popupRef"]
    }]
  });
})();
var PoDropdownModule = class _PoDropdownModule {
  static ɵfac = function PoDropdownModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoDropdownModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoDropdownModule,
    declarations: [PoDropdownComponent],
    imports: [CommonModule, PoPopupModule, PoIconModule],
    exports: [PoDropdownComponent]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [CommonModule, PoPopupModule, PoIconModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoDropdownModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, PoPopupModule, PoIconModule],
      declarations: [PoDropdownComponent],
      exports: [PoDropdownComponent]
    }]
  }], null, null);
})();
var poCheckboxGroupColumnsDefaultLength = 6;
var poCheckboxGroupColumnsTotalLength = 12;
var PoCheckboxGroupBaseComponent = class _PoCheckboxGroupBaseComponent {
  // Propriedade interna que define se o ícone de ajuda adicional terá cursor clicável (evento) ou padrão (tooltip).
  additionalHelpEventTrigger;
  /**
   *
   * @deprecated v23.x.x use `p-helper`
   *
   * @optional
   *
   * @description
   * Exibe um ícone de ajuda adicional, com o texto desta propriedade sendo passado para o popover do componente `po-helper`.
   * **Como boa prática, indica-se utilizar um texto com até 140 caracteres.**
   * > Requer um recuo mínimo de 8px se o componente estiver próximo à lateral da tela.
   *
   * > Essa propriedade está **depreciada** e será removida na versão `23.x.x`. Recomendamos utilizar a propriedade `p-helper` que oferece mais recursos e flexibilidade.
   */
  additionalHelpTooltip;
  /**
   * @optional
   *
   * @description
   *
   * Define que o popover (`p-helper` e/ou `p-error-limit`) será incluído no body da página e não
   * dentro do componente. Essa opção pode ser necessária em cenários com containers que possuem scroll ou overflow
   * escondido, garantindo o posicionamento correto do tooltip próximo ao elemento.
   *
   * > Quando utilizado com `p-helper`, leitores de tela como o NVDA podem não ler o conteúdo do popover.
   *
   * @default `false`
   */
  appendBox = false;
  /**
   * @optional
   *
   * @description
   *
   * Aplica foco no elemento ao ser iniciado.
   *
   * > Caso mais de um elemento seja configurado com essa propriedade, apenas o último elemento declarado com ela terá o foco.
   *
   * @default `false`
   */
  autoFocus = false;
  /**
   * @optional
   *
   * @description
   * Define se o título do campo será exibido de forma compacta.
   *
   * Quando habilitado (`true`), o modo compacto afeta o conjunto composto por:
   * - `po-label`
   * - `p-requirement (showRequired)`
   * - `po-helper`
   *
   * Ou seja, todos os elementos relacionados ao título do campo
   * (rótulo, indicador de obrigatoriedade e componente auxiliar) passam
   * a seguir o comportamento de layout compacto.
   *
   * Também é possível definir esse comportamento de forma global,
   * uma única vez, na folha de estilo geral da aplicação, por meio
   * da customização dos tokens CSS:
   *
   * - `--field-container-title-justify`
   * - `--field-container-title-flex`
   *
   * Exemplo:
   *
   * ```
   * :root {
   *   --field-container-title-justify: flex-start;
   *   --field-container-title-flex: 0 1 auto;
   * }
   * ```
   *
   * Dessa forma, o layout compacto passa a ser o padrão da aplicação,
   * sem a necessidade de definir a propriedade individualmente em cada campo.
   *
   * @default `false`
   */
  compactLabel = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "compactLabel"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-compact-label",
    transform: convertToBoolean
  }));
  /** Nome dos checkboxes */
  name;
  /** Texto de apoio do campo */
  help;
  /** Label do campo */
  label;
  /**
   * @optional
   *
   * @description
   *
   * Define se a indicação de campo opcional será exibida.
   *
   * > Não será exibida a indicação se:
   * - O campo conter `p-required`;
   * - Não possuir `p-help` e/ou `p-label`.
   *
   * @default `false`
   */
  optional;
  /**
   * @optional
   *
   * @description
   *
   * Exibe a mensagem setada se o campo estiver vazio e for requerido.
   *
   * > Necessário que a propriedade `p-required` esteja habilitada.
   *
   */
  fieldErrorMessage;
  /**
   * @optional
   *
   * @description
   *
   * Limita a exibição da mensagem de erro a duas linhas e exibe um tooltip com o texto completo.
   *
   * > Caso essa propriedade seja definida como `true`, a mensagem de erro será limitada a duas linhas
   * e um tooltip será exibido ao passar o mouse sobre a mensagem para mostrar o conteúdo completo.
   *
   * @default `false`
   */
  errorLimit = false;
  /**
   * @optional
   *
   * @description
   *
   * Define as opções do componente de ajuda (po-helper) que será exibido ao lado do label quando a propriedade `p-label` for definida, ou, ao lado do componente na ausência da propriedade `p-label`.
   * > Para mais informações acesse: https://po-ui.io/documentation/po-helper.
   *
   * > Ao configurar esta propriedade, o antigo ícone de ajuda adicional (`p-additional-help-tooltip` e `p-additional-help`) será ignorado.
   */
  poHelperComponent = input(void 0, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "poHelperComponent"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-helper"
  }));
  /**
   * @optional
   *
   * @description
   * Habilita a quebra automática do texto da propriedade `p-label`. Quando `p-label-text-wrap` for verdadeiro, o texto que excede
   * o espaço disponível é transferido para a próxima linha em pontos apropriados para uma
   * leitura clara.
   *
   * @default `false`
   */
  labelTextWrap = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "labelTextWrap"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-label-text-wrap"
  }));
  /**
   * @optional
   *
   * @description
   *
   * Função para atualizar o `ngModel` do componente, necessário quando não for utilizado dentro da tag form.
   *
   * Na versão 12.2.0 do Angular a verificação `strictTemplates` vem true como default. Portanto, para utilizar
   * two-way binding no componente deve se utilizar da seguinte forma:
   *
   * ```
   * <po-checkbox-group ... [ngModel]="checkboxgroupModel" (ngModelChange)="checkboxgroupModel = $event"> </po-checkbox-group>
   * ```
   *
   */
  ngModelChange = new EventEmitter();
  /**
   * @deprecated v23.x.x use `p-helper`
   *
   * @optional
   *
   * @description
   * Evento disparado ao clicar no ícone de ajuda adicional.
   *
   * > Essa propriedade está **depreciada** e será removida na versão `23.x.x`. Recomendamos utilizar a propriedade `p-helper` que oferece mais recursos e flexibilidade.
   */
  additionalHelp = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Evento disparado ao alterar valor do campo
   */
  change = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Evento disparado sempre que o valor do model é alterado, seja por interação do usuário
   * ou por atualização programática (ex: `setValue`, `patchValue`, carregamento assíncrono).
   *
   * Diferentemente do `p-change`, que é disparado apenas por interação do usuário,
   * o `p-change-model` cobre todos os cenários de alteração de valor.
   *
   * Não emite quando o novo valor é idêntico ao anterior (deduplicação automática).
   */
  changeModel = new EventEmitter();
  /**
   * @optional
   *
   * @description
   * Evento disparado quando uma tecla é pressionada enquanto o foco está no componente.
   * Retorna um objeto `KeyboardEvent` com informações sobre a tecla.
   */
  keydown = new EventEmitter();
  checkboxGroupOptionsView;
  checkedOptions = {};
  checkedOptionsList = [];
  displayAdditionalHelp = false;
  mdColumns = poCheckboxGroupColumnsDefaultLength;
  modelLastUpdate;
  propagateChange;
  validatorChange;
  _columns = poCheckboxGroupColumnsDefaultLength;
  _disabled = false;
  _indeterminate = false;
  _options;
  _required = false;
  _size = void 0;
  _initialSize = void 0;
  /**
   * @optional
   *
   * @description
   *
   * Possibilita definir a quantidade de colunas para exibição dos itens do *checkbox*.
   * - É possível exibir as opções entre `1` e `4` colunas.
   * - Para resolução `sm` a colunagem invariavelmente passa para `1` coluna.
   * - Quando se trata de resolução `md` e o valor estabelecido para colunas for superior a `2`,
   * o *grid system* será composto por `2` colunas.
   * - Para evitar a quebra de linha, prefira a utilização de `1` coluna para opções com textos grandes.
   *
   * @default `2`
   *
   */
  set columns(value) {
    const columns = PoUtils.convertToInt(value, poCheckboxGroupColumnsDefaultLength);
    this._columns = this.getGridSystemColumns(columns, 4);
    this.mdColumns = this.getGridSystemColumns(columns, 2);
  }
  get columns() {
    return this._columns;
  }
  /**
   * @optional
   *
   * @description
   *
   * Desabilita todos os itens do checkbox.
   *
   * @default `false`
   */
  set disabled(value) {
    this._disabled = value;
    this.validateModel(this.checkIndeterminate());
  }
  get disabled() {
    return this._disabled;
  }
  /**
   * @optional
   *
   * @description
   *
   * Caso exista a necessidade de usar o valor indeterminado (`null`) dentro da lista de opções, é necessário setar
   * a propriedade `p-indeterminate` como `true`, por padrão essa propriedade vem desabilitada (`false`).
   *
   * Quando essa propriedade é setada como `true`, o *po-checkbox-group* passa a devolver um objeto completo para o
   * `ngModel`, diferente do array que contém apenas os valores selecionados.
   *
   * @default `false`
   */
  set indeterminate(indeterminate) {
    this._indeterminate = indeterminate;
  }
  get indeterminate() {
    return this._indeterminate;
  }
  /**
   * @optional
   *
   * @description
   * Lista de opções que serão exibidas
   * Nesta propriedade deve ser definido um array de objetos que implementam a interface PoCheckboxGroupOption
   */
  set options(value) {
    this._options = Array.isArray(value) ? value : [];
    this.removeDuplicatedOptions();
    this.setCheckboxGroupOptionsView(this.options);
  }
  get options() {
    return this._options;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define que o campo será obrigatório.
   *
   * @default `false`
   */
  set required(required) {
    this._required = required;
    this.validateModel(this.checkIndeterminate());
  }
  get required() {
    return this._required;
  }
  /**
   * Define se a indicação de campo obrigatório será exibida.
   *
   * > Não será exibida a indicação se:
   * - Não possuir `p-help` e/ou `p-label`.
   */
  showRequired = false;
  /**
   * @optional
   *
   * @description
   *
   * Define o tamanho dos checkboxes do componente:
   * - `small`: 16x16 (disponível apenas para acessibilidade AA).
   * - `medium`: 24x24.
   *
   * > Caso a acessibilidade AA não esteja configurada, o tamanho `medium` será mantido.
   * Para mais detalhes, consulte a documentação do [po-theme](https://po-ui.io/documentation/po-theme).
   *
   * @default `medium`
   *
   */
  set size(value) {
    this._initialSize = value;
    this.applySizeBasedOnA11y();
  }
  get size() {
    return this._size ?? getDefaultSizeFn(PoFieldSize);
  }
  onThemeChange() {
    this.applySizeBasedOnA11y();
  }
  changeValue() {
    const value = this.checkIndeterminate();
    if (this.propagateChange) {
      this.propagateChange(value);
    } else {
      this.ngModelChange.emit(value);
    }
    this.change.emit(value);
    this.controlChangeModelEmitter(value);
  }
  checkIndeterminate() {
    return this.indeterminate ? this.checkedOptions : this.checkedOptionsList;
  }
  checkOption(value) {
    if (!this._disabled && !value.disabled && !value.fixed) {
      this.checkOptionModel(value);
      this.changeValue();
    }
  }
  // Função implementada do ControlValueAccessor
  // Usada para interceptar os estados de habilitado via forms api
  setDisabledState(isDisabled) {
    this.disabled = isDisabled;
  }
  registerOnChange(fn) {
    this.propagateChange = fn;
  }
  registerOnTouched(fn) {
  }
  writeValue(optionsModel) {
    if (optionsModel && this.checkedOptions !== optionsModel) {
      this.generateCheckOptions(optionsModel);
    } else {
      this.checkedOptionsList = [];
      this.checkedOptions = {};
    }
    if (optionsModel != null) {
      this.controlChangeModelEmitter(this.checkIndeterminate());
    }
  }
  controlChangeModelEmitter(value) {
    const current = JSON.stringify(this.modelLastUpdate);
    const incoming = JSON.stringify(value);
    if (current !== incoming) {
      this.changeModel.emit(value);
      this.modelLastUpdate = Array.isArray(value) ? [...value] : value;
    }
  }
  registerOnValidatorChange(fn) {
    this.validatorChange = fn;
  }
  validate(abstractControl) {
    if (!this.indeterminate && PoValidators.requiredFailed(this.required, this.disabled, abstractControl.value) || this.isInvalidIndeterminate()) {
      return {
        required: {
          valid: false
        }
      };
    }
  }
  validateModel(model2) {
    if (this.validatorChange) {
      this.validatorChange(model2);
    }
  }
  checkColumnsRange(columns, maxColumns) {
    const minColumns = 1;
    return columns >= minColumns && columns <= maxColumns;
  }
  checkOptionModel(optionChecked) {
    this.checkedOptions[optionChecked.value] = !this.checkedOptions[optionChecked.value];
    if (!this.indeterminate && this.checkedOptionsList.includes(optionChecked.value)) {
      this.checkedOptionsList.splice(this.checkedOptionsList.indexOf(optionChecked.value), 1);
    } else if (!this.indeterminate) {
      this.checkedOptionsList.push(optionChecked.value);
    }
  }
  generateCheckOptions(optionsModel) {
    this.checkedOptions = {};
    if (optionsModel instanceof Array) {
      this.checkedOptionsList = optionsModel;
      this.options.forEach((option) => {
        this.checkedOptions[option.value] = optionsModel.includes(option.value);
      });
    } else {
      this.options.forEach((option) => {
        optionsModel[option.value] = optionsModel[option.value] === void 0 ? false : optionsModel[option.value];
        this.checkedOptions = optionsModel;
      });
    }
  }
  getGridSystemColumns(columns, maxColumns) {
    const gridSystemColumns = poCheckboxGroupColumnsTotalLength / columns;
    return this.checkColumnsRange(columns, maxColumns) ? gridSystemColumns : poCheckboxGroupColumnsDefaultLength;
  }
  isInvalidIndeterminate() {
    if (this.indeterminate && this.required && this.checkedOptions) {
      return Object.values(this.checkedOptions).every((value) => value === false);
    }
    return false;
  }
  removeDuplicatedOptions() {
    this.options.forEach((option, index) => {
      const duplicatedIndex = this.options.findIndex((optionFind) => optionFind.value === option.value) === index;
      if (!duplicatedIndex) {
        this.options.splice(this.options.indexOf(option), 1);
      }
    });
  }
  setCheckboxGroupOptionsView(optionsList) {
    this.checkboxGroupOptionsView = optionsList.map((option) => __spreadProps(__spreadValues({}, option), {
      id: PoUtils.uuid()
    }));
  }
  applySizeBasedOnA11y() {
    const size = validateSizeFn(this._initialSize, PoFieldSize);
    this._size = size;
  }
  static ɵfac = function PoCheckboxGroupBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoCheckboxGroupBaseComponent)();
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoCheckboxGroupBaseComponent,
    hostVars: 1,
    hostBindings: function PoCheckboxGroupBaseComponent_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("PoUiThemeChange", function PoCheckboxGroupBaseComponent_PoUiThemeChange_HostBindingHandler() {
          return ctx.onThemeChange();
        }, ɵɵresolveWindow);
      }
      if (rf & 2) {
        ɵɵattribute("p-size", ctx.size);
      }
    },
    inputs: {
      additionalHelpEventTrigger: "additionalHelpEventTrigger",
      additionalHelpTooltip: [0, "p-additional-help-tooltip", "additionalHelpTooltip"],
      appendBox: [2, "p-append-in-body", "appendBox", convertToBoolean],
      autoFocus: [2, "p-auto-focus", "autoFocus", convertToBoolean],
      compactLabel: [1, "p-compact-label", "compactLabel"],
      name: "name",
      help: [0, "p-help", "help"],
      label: [0, "p-label", "label"],
      optional: [0, "p-optional", "optional"],
      fieldErrorMessage: [0, "p-field-error-message", "fieldErrorMessage"],
      errorLimit: [0, "p-error-limit", "errorLimit"],
      poHelperComponent: [1, "p-helper", "poHelperComponent"],
      labelTextWrap: [1, "p-label-text-wrap", "labelTextWrap"],
      columns: [0, "p-columns", "columns"],
      disabled: [2, "p-disabled", "disabled", convertToBoolean],
      indeterminate: [2, "p-indeterminate", "indeterminate", convertToBoolean],
      options: [0, "p-options", "options"],
      required: [2, "p-required", "required", convertToBoolean],
      showRequired: [0, "p-show-required", "showRequired"],
      size: [0, "p-size", "size"]
    },
    outputs: {
      ngModelChange: "ngModelChange",
      additionalHelp: "p-additional-help",
      change: "p-change",
      changeModel: "p-change-model",
      keydown: "p-keydown"
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoCheckboxGroupBaseComponent, [{
    type: Directive
  }], null, {
    additionalHelpEventTrigger: [{
      type: Input
    }],
    additionalHelpTooltip: [{
      type: Input,
      args: ["p-additional-help-tooltip"]
    }],
    appendBox: [{
      type: Input,
      args: [{
        alias: "p-append-in-body",
        transform: convertToBoolean
      }]
    }],
    autoFocus: [{
      type: Input,
      args: [{
        alias: "p-auto-focus",
        transform: convertToBoolean
      }]
    }],
    compactLabel: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-compact-label",
        required: false
      }]
    }],
    name: [{
      type: Input,
      args: ["name"]
    }],
    help: [{
      type: Input,
      args: ["p-help"]
    }],
    label: [{
      type: Input,
      args: ["p-label"]
    }],
    optional: [{
      type: Input,
      args: ["p-optional"]
    }],
    fieldErrorMessage: [{
      type: Input,
      args: ["p-field-error-message"]
    }],
    errorLimit: [{
      type: Input,
      args: ["p-error-limit"]
    }],
    poHelperComponent: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-helper",
        required: false
      }]
    }],
    labelTextWrap: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-label-text-wrap",
        required: false
      }]
    }],
    ngModelChange: [{
      type: Output,
      args: ["ngModelChange"]
    }],
    additionalHelp: [{
      type: Output,
      args: ["p-additional-help"]
    }],
    change: [{
      type: Output,
      args: ["p-change"]
    }],
    changeModel: [{
      type: Output,
      args: ["p-change-model"]
    }],
    keydown: [{
      type: Output,
      args: ["p-keydown"]
    }],
    columns: [{
      type: Input,
      args: ["p-columns"]
    }],
    disabled: [{
      type: Input,
      args: [{
        alias: "p-disabled",
        transform: convertToBoolean
      }]
    }],
    indeterminate: [{
      type: Input,
      args: [{
        alias: "p-indeterminate",
        transform: convertToBoolean
      }]
    }],
    options: [{
      type: Input,
      args: ["p-options"]
    }],
    required: [{
      type: Input,
      args: [{
        alias: "p-required",
        transform: convertToBoolean
      }]
    }],
    showRequired: [{
      type: Input,
      args: ["p-show-required"]
    }],
    size: [{
      type: Input,
      args: ["p-size"]
    }, {
      type: HostBinding,
      args: ["attr.p-size"]
    }],
    onThemeChange: [{
      type: HostListener,
      args: ["window:PoUiThemeChange"]
    }]
  });
})();
var PoCheckboxGroupComponent = class _PoCheckboxGroupComponent extends PoCheckboxGroupBaseComponent {
  changeDetector = inject(ChangeDetectorRef);
  checkboxLabels;
  helperEl;
  el = inject(ElementRef);
  ngOnChanges(changes) {
    if (changes.label) {
      this.displayAdditionalHelp = false;
    }
  }
  ngAfterViewChecked() {
    this.changeDetector.detectChanges();
  }
  ngAfterViewInit() {
    if (this.autoFocus) {
      this.focus();
    }
  }
  onBlur(checkbox) {
    if (!this.isCheckboxOptionFocused(checkbox) && this.getAdditionalHelpTooltip() && this.displayAdditionalHelp) {
      this.showAdditionalHelp();
    }
  }
  emitAdditionalHelp() {
    if (this.label && this.isAdditionalHelpEventTriggered()) {
      this.additionalHelp.emit();
    }
  }
  /**
   * Função que atribui foco ao componente.
   *
   * Para utilizá-la é necessário ter a instância do componente no DOM, podendo ser utilizado o ViewChild da seguinte forma:
   *
   * ```
   * import { PoCheckboxGroupComponent } from '@po-ui/ng-components';
   *
   * ...
   *
   * @ViewChild(PoCheckboxGroupComponent, { static: true }) checkbox: PoCheckboxGroupComponent;
   *
   * focusCheckbox() {
   *   this.checkbox.focus();
   * }
   * ```
   */
  focus() {
    if (this.checkboxLabels && !this.disabled) {
      const checkboxLabel = this.checkboxLabels.find((_, index) => !this.options[index].disabled);
      if (checkboxLabel) {
        checkboxLabel.checkboxLabel.nativeElement.focus();
      }
    }
  }
  getAdditionalHelpTooltip() {
    return this.isAdditionalHelpEventTriggered() ? null : this.additionalHelpTooltip;
  }
  getErrorPattern() {
    return this.fieldErrorMessage && this.hasInvalidClass() ? this.fieldErrorMessage : "";
  }
  hasInvalidClass() {
    return this.el.nativeElement.classList.contains("ng-invalid") && this.el.nativeElement.classList.contains("ng-dirty");
  }
  onKeyDown(event, option, checkbox) {
    const spaceBar = 32;
    if (event.which === spaceBar || event.keyCode === spaceBar) {
      this.checkOption(option);
      event.preventDefault();
    }
    if (this.isCheckboxOptionFocused(checkbox)) {
      this.keydown.emit(event);
    }
  }
  /**
   * Método que exibe `p-helper` ou executa a ação definida em `p-helper{eventOnClick}` ou em `p-additionalHelp`.
   * Para isso, será necessário configurar uma tecla de atalho utilizando o evento `p-keydown`.
   *
   * > Exibe ou oculta o conteúdo do componente `po-helper` quando o componente estiver com foco.
   *
   * ```
   * //Exemplo com p-label e p-helper
   * <po-checkbox-group
   *  #checkboxGroup
   *  ...
   *  p-label="Label do checkbox"
   *  [p-helper]="helperOptions"
   *  (p-keydown)="onKeyDown($event, checkboxGroup)"
   * ></po-checkbox-group>
   * ```
   * ```
   * ...
   * onKeyDown(event: KeyboardEvent, inp: PoCheckboxGroupComponent): void {
   *  if (event.code === 'F9') {
   *    inp.showAdditionalHelp();
   *  }
   * }
   * ```
   */
  showAdditionalHelp() {
    this.displayAdditionalHelp = !this.displayAdditionalHelp;
    const helper = this.poHelperComponent();
    const isHelpEvt = this.isAdditionalHelpEventTriggered();
    if (!this.label && (helper || this.additionalHelpTooltip || isHelpEvt)) {
      if (isHelpEvt) {
        this.additionalHelp.emit();
      }
      if (typeof helper !== "string" && typeof helper?.eventOnClick === "function") {
        helper.eventOnClick();
        return;
      }
      if (this.helperEl?.helperIsVisible()) {
        this.helperEl?.closeHelperPopover();
        return;
      }
      this.helperEl?.openHelperPopover();
      return;
    }
    return this.displayAdditionalHelp;
  }
  trackByFn(index) {
    return index;
  }
  setHelper(label, additionalHelpTooltip) {
    return setHelperSettings(label, additionalHelpTooltip, this.poHelperComponent(), this.size, this.isAdditionalHelpEventTriggered() ? this.additionalHelp : void 0);
  }
  isAdditionalHelpEventTriggered() {
    return this.additionalHelpEventTrigger === "event" || this.additionalHelpEventTrigger === void 0 && this.additionalHelp.observed;
  }
  isCheckboxOptionFocused(checkbox) {
    return document.activeElement === checkbox.checkboxLabel.nativeElement;
  }
  static ɵfac = /* @__PURE__ */ (() => {
    let ɵPoCheckboxGroupComponent_BaseFactory;
    return function PoCheckboxGroupComponent_Factory(__ngFactoryType__) {
      return (ɵPoCheckboxGroupComponent_BaseFactory || (ɵPoCheckboxGroupComponent_BaseFactory = ɵɵgetInheritedFactory(_PoCheckboxGroupComponent)))(__ngFactoryType__ || _PoCheckboxGroupComponent);
    };
  })();
  static ɵcmp = ɵɵdefineComponent({
    type: _PoCheckboxGroupComponent,
    selectors: [["po-checkbox-group"]],
    viewQuery: function PoCheckboxGroupComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c17, 5, PoHelperComponent)(_c19, 5);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.helperEl = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.checkboxLabels = _t);
      }
    },
    standalone: false,
    features: [ɵɵProvidersFeature([{
      provide: NG_VALUE_ACCESSOR,
      useExisting: forwardRef(() => _PoCheckboxGroupComponent),
      multi: true
    }, {
      provide: NG_VALIDATORS,
      useExisting: forwardRef(() => _PoCheckboxGroupComponent),
      multi: true
    }]), ɵɵInheritDefinitionFeature, ɵɵNgOnChangesFeature],
    decls: 7,
    vars: 20,
    consts: [["checkboxLabel", ""], [3, "p-disabled", "p-label", "p-optional", "p-required", "p-show-required", "p-helper", "p-show-helper", "p-size", "p-text-wrap", "p-compact-label"], ["role", "group", 1, "po-checkbox-group-content"], [1, "po-field-container-content"], [1, "po-row"], [3, "class", "po-checkbox-group-item-disabled"], [3, "p-append-in-body", "p-help", "p-disabled", "p-error-limit", "p-error-pattern", "p-size", "p-helper", "p-show-helper"], [3, "p-blur", "click", "keydown", "p-label", "p-disabled", "p-checkboxValue", "p-required", "p-label-text-wrap", "p-size"]],
    template: function PoCheckboxGroupComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "po-field-container", 1)(1, "div", 2)(2, "div", 3)(3, "ul", 4);
        ɵɵrepeaterCreate(4, PoCheckboxGroupComponent_For_5_Template, 3, 12, "li", 5, ctx.trackByFn, true);
        ɵɵelementEnd()()();
        ɵɵelement(6, "po-field-container-bottom", 6);
        ɵɵelementEnd();
      }
      if (rf & 2) {
        const poHelper_r5 = ctx.setHelper(ctx.label, ctx.additionalHelpTooltip);
        ɵɵproperty("p-disabled", ctx.disabled)("p-label", ctx.label)("p-optional", ctx.optional)("p-required", ctx.required)("p-show-required", ctx.showRequired)("p-helper", ctx.label ? poHelper_r5.helperSettings : void 0)("p-show-helper", ctx.label ? ctx.displayAdditionalHelp : void 0)("p-size", ctx.size)("p-text-wrap", ctx.labelTextWrap())("p-compact-label", ctx.compactLabel());
        ɵɵadvance();
        ɵɵattribute("aria-label", ctx.label);
        ɵɵadvance();
        ɵɵattribute("name", ctx.name);
        ɵɵadvance(2);
        ɵɵrepeater(ctx.checkboxGroupOptionsView);
        ɵɵadvance(2);
        ɵɵproperty("p-append-in-body", ctx.appendBox)("p-help", ctx.help)("p-disabled", ctx.disabled)("p-error-limit", ctx.errorLimit)("p-error-pattern", ctx.getErrorPattern())("p-size", ctx.size)("p-helper", !ctx.label ? poHelper_r5.helperSettings : void 0)("p-show-helper", !ctx.label ? ctx.displayAdditionalHelp : void 0);
      }
    },
    dependencies: [PoFieldContainerBottomComponent, PoFieldContainerComponent, PoCheckboxComponent],
    encapsulation: 2,
    changeDetection: 0
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoCheckboxGroupComponent, [{
    type: Component,
    args: [{
      selector: "po-checkbox-group",
      changeDetection: ChangeDetectionStrategy.OnPush,
      providers: [{
        provide: NG_VALUE_ACCESSOR,
        useExisting: forwardRef(() => PoCheckboxGroupComponent),
        multi: true
      }, {
        provide: NG_VALIDATORS,
        useExisting: forwardRef(() => PoCheckboxGroupComponent),
        multi: true
      }],
      standalone: false,
      template: `@let poHelper = setHelper(label, additionalHelpTooltip);
<po-field-container
  [p-disabled]="disabled"
  [p-label]="label"
  [p-optional]="optional"
  [p-required]="required"
  [p-show-required]="showRequired"
  [p-helper]="label ? poHelper.helperSettings : undefined"
  [p-show-helper]="label ? displayAdditionalHelp : undefined"
  [p-size]="size"
  [p-text-wrap]="labelTextWrap()"
  [p-compact-label]="compactLabel()"
>
  <div role="group" class="po-checkbox-group-content" [attr.aria-label]="label">
    <div class="po-field-container-content" [attr.name]="name">
      <ul class="po-row">
        @for (option of checkboxGroupOptionsView; track trackByFn($index)) {
          <li
            class="po-checkbox-group-item po-md-{{ mdColumns }} po-lg-{{ columns }}"
            [class.po-checkbox-group-item-disabled]="option.disabled || disabled"
          >
            <po-checkbox
              #checkboxLabel
              [p-label]="option.label"
              [p-disabled]="option.disabled || disabled"
              (p-blur)="onBlur(checkboxLabel)"
              (click)="checkOption(option)"
              (keydown)="onKeyDown($event, option, checkboxLabel)"
              [p-checkboxValue]="checkedOptions[option.value] === null ? 'mixed' : checkedOptions[option.value]"
              [p-required]="required"
              [p-label-text-wrap]="true"
              [p-size]="size"
            >
            </po-checkbox>
          </li>
        }
      </ul>
    </div>
  </div>
  <po-field-container-bottom
    [p-append-in-body]="appendBox"
    [p-help]="help"
    [p-disabled]="disabled"
    [p-error-limit]="errorLimit"
    [p-error-pattern]="getErrorPattern()"
    [p-size]="size"
    [p-helper]="!label ? poHelper.helperSettings : undefined"
    [p-show-helper]="!label ? displayAdditionalHelp : undefined"
  ></po-field-container-bottom>
</po-field-container>
`
    }]
  }], null, {
    checkboxLabels: [{
      type: ViewChildren,
      args: ["checkboxLabel"]
    }],
    helperEl: [{
      type: ViewChild,
      args: ["helperEl", {
        read: PoHelperComponent,
        static: false
      }]
    }]
  });
})();
var PoCheckboxGroupModule = class _PoCheckboxGroupModule {
  static ɵfac = function PoCheckboxGroupModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoCheckboxGroupModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoCheckboxGroupModule,
    declarations: [PoCheckboxGroupComponent],
    imports: [CommonModule, FormsModule, PoFieldContainerModule, PoCheckboxModule, PoHelperModule],
    exports: [PoCheckboxGroupComponent]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [CommonModule, FormsModule, PoFieldContainerModule, PoCheckboxModule, PoHelperModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoCheckboxGroupModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, FormsModule, PoFieldContainerModule, PoCheckboxModule, PoHelperModule],
      exports: [PoCheckboxGroupComponent],
      declarations: [PoCheckboxGroupComponent]
    }]
  }], null, null);
})();
var PoRadioSize;
(function(PoRadioSize2) {
  PoRadioSize2["Small"] = "small";
  PoRadioSize2["Medium"] = "medium";
  PoRadioSize2["Large"] = "large";
})(PoRadioSize || (PoRadioSize = {}));
var poRadioGroupColumnsDefaultLength = 6;
var poRadioGroupColumnsTotalLength = 12;
var PoRadioGroupBaseComponent = class _PoRadioGroupBaseComponent {
  // Propriedade interna que define se o ícone de ajuda adicional terá cursor clicável (evento) ou padrão (tooltip).
  additionalHelpEventTrigger;
  /**
   *
   * @deprecated v23.x.x use `p-helper`
   *
   * @optional
   *
   * @description
   * Exibe um ícone de ajuda adicional, com o texto desta propriedade sendo passado para o popover do componente `po-helper`.
   * **Como boa prática, indica-se utilizar um texto com até 140 caracteres.**
   * > Requer um recuo mínimo de 8px se o componente estiver próximo à lateral da tela.
   *
   * > Essa propriedade está **depreciada** e será removida na versão `23.x.x`. Recomendamos utilizar a propriedade `p-helper` que oferece mais recursos e flexibilidade.
   */
  additionalHelpTooltip;
  /**
   * @optional
   *
   * @description
   *
   * Define que o popover (`p-helper` e/ou `p-error-limit`) será incluído no body da página e não
   * dentro do componente. Essa opção pode ser necessária em cenários com containers que possuem scroll ou overflow
   * escondido, garantindo o posicionamento correto do tooltip próximo ao elemento.
   *
   * > Quando utilizado com `p-helper`, leitores de tela como o NVDA podem não ler o conteúdo do popover.
   *
   * @default `false`
   */
  appendBox = false;
  /**
   * @optional
   *
   * @description
   *
   * Aplica foco no elemento ao ser iniciado.
   *
   * > Caso mais de um elemento seja configurado com essa propriedade, apenas o último elemento declarado com ela terá o foco.
   *
   * @default `false`
   */
  autoFocus = false;
  /**
   * @optional
   *
   * @description
   * Define se o título do campo será exibido de forma compacta.
   *
   * Quando habilitado (`true`), o modo compacto afeta o conjunto composto por:
   * - `po-label`
   * - `p-requirement (showRequired)`
   * - `po-helper`
   *
   * Ou seja, todos os elementos relacionados ao título do campo
   * (rótulo, indicador de obrigatoriedade e componente auxiliar) passam
   * a seguir o comportamento de layout compacto.
   *
   * Também é possível definir esse comportamento de forma global,
   * uma única vez, na folha de estilo geral da aplicação, por meio
   * da customização dos tokens CSS:
   *
   * - `--field-container-title-justify`
   * - `--field-container-title-flex`
   *
   * Exemplo:
   *
   * ```
   * :root {
   *   --field-container-title-justify: flex-start;
   *   --field-container-title-flex: 0 1 auto;
   * }
   * ```
   *
   * Dessa forma, o layout compacto passa a ser o padrão da aplicação,
   * sem a necessidade de definir a propriedade individualmente em cada campo.
   *
   * @default `false`
   */
  compactLabel = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "compactLabel"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-compact-label",
    transform: convertToBoolean
  }));
  /** Nome das opções. */
  name;
  /**
   * @optional
   *
   * @description
   *
   * Define se a indicação de campo opcional será exibida.
   *
   * > Não será exibida a indicação se:
   * - O campo conter `p-required`;
   * - Não possuir `p-help` e/ou `p-label`.
   *
   * @default `false`
   */
  optional;
  /**
   * @optional
   *
   * @description
   *
   * Exibe a mensagem setada se o campo estiver vazio e for requerido.
   *
   * > Necessário que a propriedade `p-required` esteja habilitada.
   *
   */
  fieldErrorMessage;
  /**
   * @optional
   *
   * @description
   *
   * Limita a exibição da mensagem de erro a duas linhas e exibe um tooltip com o texto completo.
   *
   * > Caso essa propriedade seja definida como `true`, a mensagem de erro será limitada a duas linhas
   * e um tooltip será exibido ao passar o mouse sobre a mensagem para mostrar o conteúdo completo.
   *
   * @default `false`
   */
  errorLimit = false;
  /**
   * @optional
   *
   * @description
   *
   * Define as opções do componente de ajuda (po-helper) que será exibido ao lado do label quando a propriedade `p-label` for definida, ou, ao lado do componente na ausência da propriedade `p-label`.
   * > Para mais informações acesse: https://po-ui.io/documentation/po-helper.
   *
   * > Ao configurar esta propriedade, o antigo ícone de ajuda adicional (`p-additional-help-tooltip` e `p-additional-help`) será ignorado.
   */
  poHelperComponent = input(void 0, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "poHelperComponent"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-helper"
  }));
  /**
   * @optional
   *
   * @description
   *
   * Habilita a quebra automática do texto da propriedade `p-label`. Quando `p-label-text-wrap` for verdadeiro, o texto que excede
   * o espaço disponível é transferido para a próxima linha em pontos apropriados para uma
   * leitura clara.
   *
   * @default `false`
   */
  labelTextWrap = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "labelTextWrap"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-label-text-wrap"
  }));
  /**
   *
   * @deprecated v23.x.x use `p-helper`
   *
   * @optional
   *
   * @description
   * Evento disparado ao clicar no ícone de ajuda adicional.
   *
   * > Essa propriedade está **depreciada** e será removida na versão `23.x.x`. Recomendamos utilizar a propriedade `p-helper` que oferece mais recursos e flexibilidade.
   */
  additionalHelp = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Evento ao alterar valor do campo.
   */
  change = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Evento disparado sempre que o valor do model é alterado, seja por interação do usuário
   * ou por atualização programática (ex: `setValue`, `patchValue`, carregamento assíncrono).
   *
   * Diferentemente do `p-change`, que é disparado apenas por interação do usuário,
   * o `p-change-model` cobre todos os cenários de alteração de valor.
   *
   * Não emite quando o novo valor é idêntico ao anterior (deduplicação automática).
   */
  changeModel = new EventEmitter();
  /**
   * @optional
   *
   * @description
   * Evento disparado quando uma tecla é pressionada enquanto o foco está no componente.
   * Retorna um objeto `KeyboardEvent` com informações sobre a tecla.
   */
  keydown = new EventEmitter();
  displayAdditionalHelp = false;
  mdColumns = poRadioGroupColumnsDefaultLength;
  modelLastUpdate;
  value;
  cd = inject(ChangeDetectorRef);
  onTouched = null;
  _columns = poRadioGroupColumnsDefaultLength;
  _disabled = false;
  _options;
  _required = false;
  _size = void 0;
  _initialSize = void 0;
  onChangePropagate = null;
  validatorChange;
  /**
   * @optional
   *
   * @description
   *
   * Define a quantidade de colunas para exibição das opções.
   *
   * **Considerações:**
   *  - É possível exibir as opções entre `1` e `4` colunas.
   *  - O número máximo de colunas é invariável nas seguintes resoluções:
   *    + `sm`: `1`
   *    + `md`: `2`
   *
   * @default `2`
   */
  set columns(value) {
    const columns = PoUtils.convertToInt(value, poRadioGroupColumnsDefaultLength);
    this._columns = this.getGridSystemColumns(columns, 4);
    this.mdColumns = this.getGridSystemColumns(columns, 2);
  }
  get columns() {
    return this._columns;
  }
  /**
   * @optional
   *
   * @description
   *
   * Indica que o campo será desabilitado.
   *
   * @default `false`
   */
  set disabled(disabled) {
    this._disabled = disabled;
    this.validateModel();
  }
  get disabled() {
    return this._disabled;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define que o campo será obrigatório.
   *
   * @default `false`
   */
  set required(required) {
    this._required = required;
    this.validateModel();
  }
  get required() {
    return this._required;
  }
  /**
   * Define se a indicação de campo obrigatório será exibida.
   *
   * > Não será exibida a indicação se:
   * - Não possuir `p-help` e/ou `p-label`.
   */
  showRequired = false;
  /**
   * Lista de opções que serão exibidas.
   * Nesta propriedade deve ser definido um array de objetos que implementam a interface PoRadioGroupOption.
   */
  set options(value) {
    this._options = value;
    removeDuplicatedOptions(this.options);
  }
  get options() {
    return this._options;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define o tamanho dos radios do componente:
   * - `small`: 16x16 (disponível apenas para acessibilidade AA).
   * - `medium`: 24x24.
   *
   * > Caso a acessibilidade AA não esteja configurada, o tamanho `medium` será mantido.
   * Para mais detalhes, consulte a documentação do [po-theme](https://po-ui.io/documentation/po-theme).
   *
   * @default `medium`
   *
   */
  set size(value) {
    this._initialSize = value;
    this.applySizeBasedOnA11y();
  }
  get size() {
    return this._size ?? getDefaultSizeFn(PoRadioSize);
  }
  onThemeChange() {
    this.applySizeBasedOnA11y();
  }
  controlChangeModelEmitter(value) {
    if (this.modelLastUpdate !== value) {
      this.changeModel.emit(value);
      this.modelLastUpdate = value;
    }
  }
  // Função que controla quando deve ser emitido onChange e atualiza o Model
  changeValue(changedValue) {
    if (this.onChangePropagate) {
      this.onChangePropagate(changedValue);
    }
    if (this.value !== changedValue) {
      this.change.emit(changedValue);
    }
    this.value = changedValue;
    this.controlChangeModelEmitter(changedValue);
  }
  // Função implementada do ControlValueAccessor
  // Usada para interceptar os estados de habilitado via forms api
  setDisabledState(isDisabled) {
    this.disabled = isDisabled;
  }
  registerOnChange(fn) {
    this.onChangePropagate = fn;
  }
  registerOnTouched(fn) {
    this.onTouched = fn;
  }
  registerOnValidatorChange(fn) {
    this.validatorChange = fn;
  }
  validate(abstractControl) {
    if (PoValidators.requiredFailed(this.required, this.disabled, abstractControl.value)) {
      return {
        required: {
          valid: false
        }
      };
    }
  }
  writeValue(modelValue) {
    this.value = modelValue;
    const element = this.getElementByValue(modelValue);
    if (!element && this.onChangePropagate) {
      this.value = void 0;
      this.onChangePropagate(this.value);
    }
    if (this.value != null) {
      this.controlChangeModelEmitter(this.value);
    }
  }
  checkColumnsRange(columns, maxColumns) {
    const minColumns = 1;
    return columns >= minColumns && columns <= maxColumns;
  }
  getGridSystemColumns(columns, maxColumns) {
    const gridSystemColumns = poRadioGroupColumnsTotalLength / columns;
    return this.checkColumnsRange(columns, maxColumns) ? gridSystemColumns : poRadioGroupColumnsDefaultLength;
  }
  validateModel() {
    if (this.validatorChange) {
      this.validatorChange();
    }
  }
  applySizeBasedOnA11y() {
    const size = validateSizeFn(this._initialSize, PoRadioSize);
    this._size = size;
  }
  static ɵfac = function PoRadioGroupBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoRadioGroupBaseComponent)();
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoRadioGroupBaseComponent,
    hostVars: 1,
    hostBindings: function PoRadioGroupBaseComponent_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("PoUiThemeChange", function PoRadioGroupBaseComponent_PoUiThemeChange_HostBindingHandler() {
          return ctx.onThemeChange();
        }, ɵɵresolveWindow);
      }
      if (rf & 2) {
        ɵɵattribute("p-size", ctx.size);
      }
    },
    inputs: {
      additionalHelpEventTrigger: "additionalHelpEventTrigger",
      additionalHelpTooltip: [0, "p-additional-help-tooltip", "additionalHelpTooltip"],
      appendBox: [2, "p-append-in-body", "appendBox", convertToBoolean],
      autoFocus: [2, "p-auto-focus", "autoFocus", convertToBoolean],
      compactLabel: [1, "p-compact-label", "compactLabel"],
      name: "name",
      optional: [0, "p-optional", "optional"],
      fieldErrorMessage: [0, "p-field-error-message", "fieldErrorMessage"],
      errorLimit: [0, "p-error-limit", "errorLimit"],
      poHelperComponent: [1, "p-helper", "poHelperComponent"],
      labelTextWrap: [1, "p-label-text-wrap", "labelTextWrap"],
      columns: [0, "p-columns", "columns"],
      disabled: [2, "p-disabled", "disabled", convertToBoolean],
      required: [2, "p-required", "required", convertToBoolean],
      showRequired: [0, "p-show-required", "showRequired"],
      options: [0, "p-options", "options"],
      size: [0, "p-size", "size"]
    },
    outputs: {
      additionalHelp: "p-additional-help",
      change: "p-change",
      changeModel: "p-change-model",
      keydown: "p-keydown"
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoRadioGroupBaseComponent, [{
    type: Directive
  }], null, {
    additionalHelpEventTrigger: [{
      type: Input
    }],
    additionalHelpTooltip: [{
      type: Input,
      args: ["p-additional-help-tooltip"]
    }],
    appendBox: [{
      type: Input,
      args: [{
        alias: "p-append-in-body",
        transform: convertToBoolean
      }]
    }],
    autoFocus: [{
      type: Input,
      args: [{
        alias: "p-auto-focus",
        transform: convertToBoolean
      }]
    }],
    compactLabel: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-compact-label",
        required: false
      }]
    }],
    name: [{
      type: Input,
      args: ["name"]
    }],
    optional: [{
      type: Input,
      args: ["p-optional"]
    }],
    fieldErrorMessage: [{
      type: Input,
      args: ["p-field-error-message"]
    }],
    errorLimit: [{
      type: Input,
      args: ["p-error-limit"]
    }],
    poHelperComponent: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-helper",
        required: false
      }]
    }],
    labelTextWrap: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-label-text-wrap",
        required: false
      }]
    }],
    additionalHelp: [{
      type: Output,
      args: ["p-additional-help"]
    }],
    change: [{
      type: Output,
      args: ["p-change"]
    }],
    changeModel: [{
      type: Output,
      args: ["p-change-model"]
    }],
    keydown: [{
      type: Output,
      args: ["p-keydown"]
    }],
    columns: [{
      type: Input,
      args: ["p-columns"]
    }],
    disabled: [{
      type: Input,
      args: [{
        alias: "p-disabled",
        transform: convertToBoolean
      }]
    }],
    required: [{
      type: Input,
      args: [{
        alias: "p-required",
        transform: convertToBoolean
      }]
    }],
    showRequired: [{
      type: Input,
      args: ["p-show-required"]
    }],
    options: [{
      type: Input,
      args: ["p-options"]
    }],
    size: [{
      type: Input,
      args: ["p-size"]
    }, {
      type: HostBinding,
      args: ["attr.p-size"]
    }],
    onThemeChange: [{
      type: HostListener,
      args: ["window:PoUiThemeChange"]
    }]
  });
})();
var PoFieldModel = class _PoFieldModel {
  cd;
  // Propriedade interna que define se o ícone de ajuda adicional terá cursor clicável (evento) ou padrão (tooltip).
  additionalHelpEventTrigger;
  /**
   *
   * @deprecated v23.x.x use `p-helper`
   *
   * @optional
   *
   * @description
   * Exibe um ícone de ajuda adicional, com o texto desta propriedade sendo passado para o popover do componente `po-helper`.
   * **Como boa prática, indica-se utilizar um texto com até 140 caracteres.**
   * > Requer um recuo mínimo de 8px se o componente estiver próximo à lateral da tela.
   *
   * > Essa propriedade está **depreciada** e será removida na versão `23.x.x`. Recomendamos utilizar a propriedade `p-helper` que oferece mais recursos e flexibilidade.
   */
  additionalHelpTooltip;
  /**
   * @optional
   *
   * @description
   *
   * Define que o popover (`p-helper` e/ou `p-error-limit`) será incluído no body da página e não
   * dentro do componente. Essa opção pode ser necessária em cenários com containers que possuem scroll ou overflow
   * escondido, garantindo o posicionamento correto do tooltip próximo ao elemento.
   *
   * > Quando utilizado com `p-helper`, leitores de tela como o NVDA podem não ler o conteúdo do popover.
   *
   * @default `false`
   */
  appendBox = false;
  /** Rótulo exibido pelo componente. */
  label;
  /** Nome do componente. */
  name;
  /** Texto de apoio para o campo. */
  help;
  /**
   * @optional
   *
   * @description
   *
   * Indica se o campo será desabilitado.
   *
   * @default `false`
   */
  disabled = false;
  /**
   *
   * @deprecated v23.x.x use `p-helper`
   *
   * @optional
   *
   * @description
   * Evento disparado ao clicar no ícone de ajuda adicional.
   *
   * > Essa propriedade está **depreciada** e será removida na versão `23.x.x`. Recomendamos utilizar a propriedade `p-helper` que oferece mais recursos e flexibilidade.
   */
  additionalHelp = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Evento disparado ao alterar valor do campo.
   */
  change = new EventEmitter();
  /**
   * @optional
   *
   * @description
   * Evento disparado quando uma tecla é pressionada enquanto o foco está no componente.
   * Retorna um objeto `KeyboardEvent` com informações sobre a tecla.
   */
  keydown = new EventEmitter();
  displayAdditionalHelp = false;
  value;
  onTouched;
  propagateChange;
  constructor(cd) {
    this.cd = cd;
  }
  // Função implementada do ControlValueAccessor
  // Usada para interceptar os estados de habilitado via forms api
  setDisabledState(isDisabled) {
    this.disabled = isDisabled;
    this.cd?.markForCheck();
  }
  registerOnChange(fn) {
    this.propagateChange = fn;
  }
  registerOnTouched(fn) {
    this.onTouched = fn;
  }
  writeValue(value) {
    this.onWriteValue(value);
  }
  emitAdditionalHelp() {
    if (this.label && this.isAdditionalHelpEventTriggered()) {
      this.additionalHelp.emit();
    }
  }
  emitChange(value) {
    this.change.emit(value);
  }
  getAdditionalHelpTooltip() {
    return this.isAdditionalHelpEventTriggered() ? null : this.additionalHelpTooltip;
  }
  /**
   * Método que exibe `p-helper` ou executa a ação definida em `p-helper{eventOnClick}` ou em `p-additionalHelp`.
   * Para isso, será necessário configurar uma tecla de atalho utilizando o evento `p-keydown`.
   *
   * > Exibe ou oculta o conteúdo do componente `po-helper` quando o componente estiver com foco.
   *
   * ```
   * // Exemplo com p-label e p-helper
   * <po-nome-component
   *  #component
   *  ...
   *  p-label="Label do component"
   *  [p-helper]="helperOptions"
   *  (p-keydown)="onKeyDown($event, component)"
   * ></po-nome-component>
   * ```
   * ```
   * ...
   * onKeyDown(event: KeyboardEvent, inp: PoNomeDoComponente): void {
   *  if (event.code === 'F9') {
   *    inp.showAdditionalHelp();
   *  }
   * }
   * ```
   */
  showAdditionalHelp(helperHtmlElement, poHelperComponent) {
    this.displayAdditionalHelp = !this.displayAdditionalHelp;
    const helper = poHelperComponent;
    const isHelpEvt = this.isAdditionalHelpEventTriggered();
    if (!this.label && (helper || this.additionalHelpTooltip || isHelpEvt || helperHtmlElement)) {
      if (isHelpEvt) {
        this.additionalHelp.emit();
      }
      if (typeof helper !== "string" && typeof helper?.eventOnClick === "function") {
        helper.eventOnClick();
        return;
      }
      if (helperHtmlElement?.helperIsVisible()) {
        helperHtmlElement?.closeHelperPopover();
        return;
      }
      helperHtmlElement?.openHelperPopover();
      return;
    }
    return this.displayAdditionalHelp;
  }
  isAdditionalHelpEventTriggered() {
    return this.additionalHelpEventTrigger === "event" || this.additionalHelpEventTrigger === void 0 && this.additionalHelp.observed;
  }
  updateModel(value) {
    if (this.propagateChange) {
      this.propagateChange(value);
    }
  }
  static ɵfac = function PoFieldModel_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoFieldModel)(ɵɵdirectiveInject(ChangeDetectorRef));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoFieldModel,
    inputs: {
      additionalHelpEventTrigger: "additionalHelpEventTrigger",
      additionalHelpTooltip: [0, "p-additional-help-tooltip", "additionalHelpTooltip"],
      appendBox: [2, "p-append-in-body", "appendBox", convertToBoolean],
      label: [0, "p-label", "label"],
      name: "name",
      help: [0, "p-help", "help"],
      disabled: [2, "p-disabled", "disabled", convertToBoolean]
    },
    outputs: {
      additionalHelp: "p-additional-help",
      change: "p-change",
      keydown: "p-keydown"
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoFieldModel, [{
    type: Directive
  }], () => [{
    type: ChangeDetectorRef
  }], {
    additionalHelpEventTrigger: [{
      type: Input
    }],
    additionalHelpTooltip: [{
      type: Input,
      args: ["p-additional-help-tooltip"]
    }],
    appendBox: [{
      type: Input,
      args: [{
        alias: "p-append-in-body",
        transform: convertToBoolean
      }]
    }],
    label: [{
      type: Input,
      args: ["p-label"]
    }],
    name: [{
      type: Input,
      args: ["name"]
    }],
    help: [{
      type: Input,
      args: ["p-help"]
    }],
    disabled: [{
      type: Input,
      args: [{
        alias: "p-disabled",
        transform: convertToBoolean
      }]
    }],
    additionalHelp: [{
      type: Output,
      args: ["p-additional-help"]
    }],
    change: [{
      type: Output,
      args: ["p-change"]
    }],
    keydown: [{
      type: Output,
      args: ["p-keydown"]
    }]
  });
})();
var PoRadioComponent = class _PoRadioComponent extends PoFieldModel {
  changeDetector = inject(ChangeDetectorRef);
  renderer = inject(Renderer2);
  radio;
  radioInput;
  value = false;
  _size = void 0;
  _initialSize = void 0;
  /** Define o valor do *radio* */
  radioValue;
  /** Define o tamanho do radio. */
  set size(value) {
    this._initialSize = value;
    this.applySizeBasedOnA11y();
  }
  get size() {
    return this._size ?? getDefaultSizeFn(PoRadioSize);
  }
  required;
  /**
   *
   * Habilita a quebra automática do texto da propriedade `p-label`. Quando `p-label-text-wrap` for verdadeiro, o texto que excede
   * o espaço disponível é transferido para a próxima linha em pontos apropriados para uma
   * leitura clara.
   *
   * @default `false`
   */
  labelTextWrap = false;
  /** Define o status do *radio* */
  checked = false;
  // Evento disparado ao sair do campo.
  blur = new EventEmitter();
  /** Emite evento para a tabela ao selecionar ou desselecionar */
  changeSelected = new EventEmitter();
  /**
   * Função que atribui foco ao *radio*.
   *
   * Para utilizá-la é necessário capturar a referência do componente no DOM através do `ViewChild`, como por exemplo:
   *
   * ```
   * import { ViewChild } from '@angular/core';
   * import { PoRadioComponent } from '@po-ui/ng-components';
   *
   * ...
   *
   * @ViewChild(PoRadioComponent, { static: true }) radio: PoRadioComponent;
   *
   * focusRadio() {
   * this.radio.focus();
   * }
   * ```
   *
   */
  focus() {
    if (!this.disabled) {
      this.radioInput.nativeElement.focus();
      this.onKeyup();
    }
  }
  onBlur() {
    this.onTouched?.();
    this.blur.emit();
  }
  onKeyDown(event) {
    if (event.which === PoKeyCodeEnum.space || event.keyCode === PoKeyCodeEnum.space) {
      this.eventClick();
    }
  }
  changeValue(value) {
    if (value) {
      this.value = value;
      this.updateModel(value);
      this.emitChange(this.value);
    }
  }
  eventClick() {
    if (!this.disabled) {
      this.changeValue(!this.value);
      this.changeDetector.detectChanges();
      this.changeSelected.emit(null);
    }
  }
  onWriteValue(value) {
    if (value !== this.value) {
      this.value = !!value;
      this.changeDetector.markForCheck();
    }
  }
  focusOut() {
    this.renderer.removeClass(this.radio.nativeElement, "po-radio-focus");
  }
  onKeyup() {
    this.renderer.addClass(this.radio.nativeElement, "po-radio-focus");
  }
  onKeydown() {
    this.renderer.addClass(this.radio.nativeElement, "po-radio-focus");
  }
  onThemeChange() {
    this.applySizeBasedOnA11y();
  }
  applySizeBasedOnA11y() {
    const size = validateSizeFn(this._initialSize, PoRadioSize);
    this._size = size;
  }
  static ɵfac = /* @__PURE__ */ (() => {
    let ɵPoRadioComponent_BaseFactory;
    return function PoRadioComponent_Factory(__ngFactoryType__) {
      return (ɵPoRadioComponent_BaseFactory || (ɵPoRadioComponent_BaseFactory = ɵɵgetInheritedFactory(_PoRadioComponent)))(__ngFactoryType__ || _PoRadioComponent);
    };
  })();
  static ɵcmp = ɵɵdefineComponent({
    type: _PoRadioComponent,
    selectors: [["po-radio"]],
    viewQuery: function PoRadioComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c63, 7)(_c64, 7);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.radio = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.radioInput = _t.first);
      }
    },
    hostVars: 1,
    hostBindings: function PoRadioComponent_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("focusout", function PoRadioComponent_focusout_HostBindingHandler() {
          return ctx.focusOut();
        })("keyup", function PoRadioComponent_keyup_HostBindingHandler() {
          return ctx.onKeyup();
        })("keydown", function PoRadioComponent_keydown_HostBindingHandler() {
          return ctx.onKeydown();
        })("PoUiThemeChange", function PoRadioComponent_PoUiThemeChange_HostBindingHandler() {
          return ctx.onThemeChange();
        }, ɵɵresolveWindow);
      }
      if (rf & 2) {
        ɵɵattribute("p-size", ctx.size);
      }
    },
    inputs: {
      radioValue: [0, "p-value", "radioValue"],
      size: [0, "p-size", "size"],
      required: [2, "p-required", "required", convertToBoolean],
      labelTextWrap: [2, "p-label-text-wrap", "labelTextWrap", convertToBoolean],
      checked: [0, "p-checked", "checked"]
    },
    outputs: {
      blur: "p-blur",
      changeSelected: "p-change-selected"
    },
    standalone: false,
    features: [ɵɵProvidersFeature([{
      provide: NG_VALUE_ACCESSOR,
      useExisting: forwardRef(() => _PoRadioComponent),
      multi: true
    }]), ɵɵInheritDefinitionFeature],
    decls: 6,
    vars: 6,
    consts: [["radio", ""], ["radioInput", ""], [1, "po-radio"], [3, "click", "keydown"], ["type", "radio", 3, "blur", "checked", "disabled", "name", "required", "value"], [3, "p-text-wrap", "p-disabled", "p-label"]],
    template: function PoRadioComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "div", 2, 0)(2, "label", 3);
        ɵɵlistener("click", function PoRadioComponent_Template_label_click_2_listener() {
          return ctx.eventClick();
        })("keydown", function PoRadioComponent_Template_label_keydown_2_listener($event) {
          return ctx.onKeyDown($event);
        });
        ɵɵelementStart(3, "input", 4, 1);
        ɵɵlistener("blur", function PoRadioComponent_Template_input_blur_3_listener() {
          return ctx.onBlur();
        });
        ɵɵelementEnd();
        ɵɵconditionalCreate(5, PoRadioComponent_Conditional_5_Template, 1, 3, "po-label", 5);
        ɵɵelementEnd()();
      }
      if (rf & 2) {
        ɵɵadvance(3);
        ɵɵproperty("checked", ctx.checked)("disabled", ctx.disabled)("name", ctx.name ?? "po-input-radio")("required", ctx.required)("value", ctx.radioValue ?? "");
        ɵɵadvance(2);
        ɵɵconditional(ctx.label ? 5 : -1);
      }
    },
    dependencies: [PoLabelComponent],
    encapsulation: 2,
    changeDetection: 0
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoRadioComponent, [{
    type: Component,
    args: [{
      selector: "po-radio",
      changeDetection: ChangeDetectionStrategy.OnPush,
      providers: [{
        provide: NG_VALUE_ACCESSOR,
        useExisting: forwardRef(() => PoRadioComponent),
        multi: true
      }],
      standalone: false,
      template: `<div #radio class="po-radio">
  <label (click)="eventClick()" (keydown)="onKeyDown($event)">
    <input
      #radioInput
      type="radio"
      [checked]="checked"
      [disabled]="disabled"
      [name]="name ?? 'po-input-radio'"
      [required]="required"
      [value]="radioValue ?? ''"
      (blur)="onBlur()"
    />
    @if (label) {
      <po-label [p-text-wrap]="labelTextWrap" [p-disabled]="disabled" [p-label]="label"></po-label>
    }
  </label>
</div>
`
    }]
  }], null, {
    radio: [{
      type: ViewChild,
      args: ["radio", {
        static: true
      }]
    }],
    radioInput: [{
      type: ViewChild,
      args: ["radioInput", {
        static: true
      }]
    }],
    radioValue: [{
      type: Input,
      args: ["p-value"]
    }],
    size: [{
      type: Input,
      args: ["p-size"]
    }, {
      type: HostBinding,
      args: ["attr.p-size"]
    }],
    required: [{
      type: Input,
      args: [{
        alias: "p-required",
        transform: convertToBoolean
      }]
    }],
    labelTextWrap: [{
      type: Input,
      args: [{
        alias: "p-label-text-wrap",
        transform: convertToBoolean
      }]
    }],
    checked: [{
      type: Input,
      args: ["p-checked"]
    }],
    blur: [{
      type: Output,
      args: ["p-blur"]
    }],
    changeSelected: [{
      type: Output,
      args: ["p-change-selected"]
    }],
    focusOut: [{
      type: HostListener,
      args: ["focusout"]
    }],
    onKeyup: [{
      type: HostListener,
      args: ["keyup"]
    }],
    onKeydown: [{
      type: HostListener,
      args: ["keydown"]
    }],
    onThemeChange: [{
      type: HostListener,
      args: ["window:PoUiThemeChange"]
    }]
  });
})();
var PoRadioGroupComponent = class _PoRadioGroupComponent extends PoRadioGroupBaseComponent {
  el = inject(ElementRef);
  /** Label do campo. */
  label;
  /** Texto de apoio do campo. */
  help;
  inputEl;
  radioLabels;
  helperEl;
  differ;
  constructor() {
    const differs = inject(IterableDiffers);
    super();
    this.differ = differs.find([]).create(null);
  }
  ngAfterViewInit() {
    if (this.autoFocus) {
      this.focus();
    }
  }
  ngDoCheck() {
    const change = this.differ.diff(this.options);
    if (change) {
      PoUtils.removeDuplicatedOptions(this.options);
    }
    this.cd.markForCheck();
  }
  ngOnChanges(changes) {
    if (changes.label) {
      this.displayAdditionalHelp = false;
    }
  }
  emitAdditionalHelp() {
    if (this.label && this.isAdditionalHelpEventTriggered()) {
      this.additionalHelp.emit();
    }
  }
  eventClick(value, disabled) {
    if (!disabled) {
      this.onTouched?.();
      this.changeValue(value);
    }
  }
  /**
   * Função que atribui foco ao componente.
   *
   * Para utilizá-la é necessário ter a instância do componente no DOM, podendo ser utilizado o ViewChild da seguinte forma:
   *
   * ```
   * import { PoRadioGroupComponent } from '@po-ui/ng-components';
   *
   * ...
   *
   * @ViewChild(PoRadioGroupComponent, { static: true }) radio: PoRadioGroupComponent;
   *
   * focusRadio() {
   *   this.radio.focus();
   * }
   * ```
   */
  focus() {
    if (this.radioLabels && !this.disabled) {
      const radioLabel = this.radioLabels.find((_, index) => !this.options[index].disabled);
      if (radioLabel) {
        radioLabel.focus();
      }
    }
  }
  getAdditionalHelpTooltip() {
    return this.isAdditionalHelpEventTriggered() ? null : this.additionalHelpTooltip;
  }
  getElementByValue(value) {
    return this.inputEl.nativeElement.querySelector(`input[value='${value}']`);
  }
  getErrorPattern() {
    return this.fieldErrorMessage && this.hasInvalidClass() ? this.fieldErrorMessage : "";
  }
  hasInvalidClass() {
    return this.el.nativeElement.classList.contains("ng-invalid") && this.el.nativeElement.classList.contains("ng-dirty");
  }
  onBlur(radio) {
    if (!this.isRadioOptionFocused(radio) && this.getAdditionalHelpTooltip() && this.displayAdditionalHelp) {
      this.showAdditionalHelp();
    }
  }
  onKeyDown(event, radio) {
    if (this.isRadioOptionFocused(radio)) {
      this.keydown.emit(event);
    }
  }
  onKeyUp(event, value) {
    const key2 = event.keyCode || event.which;
    if (this.isArrowKey(key2)) {
      this.changeValue(value);
    }
  }
  /**
   *
   * Método que exibe `p-helper` ou executa a ação definida em `p-helper{eventOnClick}` ou em `p-additionalHelp`.
   * Para isso, será necessário configurar uma tecla de atalho utilizando o evento `p-keydown`.
   *
   * > Exibe ou oculta o conteúdo do componente `po-helper` quando o componente estiver com foco.
   *
   * ```
   * // Exemplo com p-label e p-helper
   * <po-radio-group
   *  #radioGroup
   *  ...
   *  p-label="Label do radioGroup"
   *  [p-helper]="helperOptions"
   *  (p-keydown)="onKeyDown($event, radioGroup)"
   * ></po-radio-group>
   * ```
   * ```
   * ...
   * onKeyDown(event: KeyboardEvent, inp: PoRadioGroupComponent): void {
   *  if (event.code === 'F9') {
   *    inp.showAdditionalHelp();
   *  }
   * }
   * ```
   */
  showAdditionalHelp() {
    this.displayAdditionalHelp = !this.displayAdditionalHelp;
    const helper = this.poHelperComponent();
    const isHelpEvt = this.isAdditionalHelpEventTriggered();
    if (!this.label && (helper || this.additionalHelpTooltip || isHelpEvt)) {
      if (isHelpEvt) {
        this.additionalHelp.emit();
      }
      if (typeof helper !== "string" && typeof helper?.eventOnClick === "function") {
        helper.eventOnClick();
        return;
      }
      if (this.helperEl?.helperIsVisible()) {
        this.helperEl?.closeHelperPopover();
        return;
      }
      this.helperEl?.openHelperPopover();
      return;
    }
    return this.displayAdditionalHelp;
  }
  setHelper(label, additionalHelpTooltip) {
    return setHelperSettings(label, additionalHelpTooltip, this.poHelperComponent(), this.size, this.isAdditionalHelpEventTriggered() ? this.additionalHelp : void 0);
  }
  isAdditionalHelpEventTriggered() {
    return this.additionalHelpEventTrigger === "event" || this.additionalHelpEventTrigger === void 0 && this.additionalHelp.observed;
  }
  isArrowKey(key2) {
    return key2 >= 37 && key2 <= 40;
  }
  isRadioOptionFocused(radio) {
    return document.activeElement === radio.radioInput.nativeElement;
  }
  static ɵfac = function PoRadioGroupComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoRadioGroupComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoRadioGroupComponent,
    selectors: [["po-radio-group"]],
    viewQuery: function PoRadioGroupComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c49, 7, ElementRef)(_c17, 5, PoHelperComponent)(_c65, 5);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.inputEl = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.helperEl = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.radioLabels = _t);
      }
    },
    inputs: {
      label: [0, "p-label", "label"],
      help: [0, "p-help", "help"]
    },
    standalone: false,
    features: [ɵɵProvidersFeature([{
      provide: NG_VALUE_ACCESSOR,
      useExisting: forwardRef(() => _PoRadioGroupComponent),
      multi: true
    }, {
      provide: NG_VALIDATORS,
      useExisting: forwardRef(() => _PoRadioGroupComponent),
      multi: true
    }]), ɵɵInheritDefinitionFeature, ɵɵNgOnChangesFeature],
    decls: 8,
    vars: 21,
    consts: [["inp", ""], ["inputRadio", ""], [3, "p-disabled", "p-label", "p-optional", "p-required", "p-show-required", "p-helper", "p-show-helper", "p-size", "p-text-wrap", "p-compact-label"], ["role", "radiogroup"], [1, "po-field-container-content"], [1, "po-radio-group"], [3, "class"], [3, "p-append-in-body", "p-help", "p-disabled", "p-error-limit", "p-error-pattern", "p-size", "p-helper", "p-show-helper"], [3, "p-blur", "keydown", "keyup", "click", "name", "p-checked", "p-disabled", "p-label", "p-label-text-wrap", "p-required", "p-size", "p-value"]],
    template: function PoRadioGroupComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "po-field-container", 2)(1, "div", 3, 0)(3, "div", 4)(4, "div", 5);
        ɵɵrepeaterCreate(5, PoRadioGroupComponent_For_6_Template, 3, 12, "div", 6, ɵɵrepeaterTrackByIdentity);
        ɵɵelementEnd()()();
        ɵɵelement(7, "po-field-container-bottom", 7);
        ɵɵelementEnd();
      }
      if (rf & 2) {
        const poHelper_r5 = ctx.setHelper(ctx.label, ctx.additionalHelpTooltip);
        ɵɵproperty("p-disabled", ctx.disabled)("p-label", ctx.label)("p-optional", ctx.optional)("p-required", ctx.required)("p-show-required", ctx.showRequired)("p-helper", ctx.label ? poHelper_r5.helperSettings : void 0)("p-show-helper", ctx.label ? ctx.displayAdditionalHelp : void 0)("p-size", ctx.size)("p-text-wrap", ctx.labelTextWrap())("p-compact-label", ctx.compactLabel());
        ɵɵadvance();
        ɵɵattribute("aria-label", ctx.label)("data-options", ctx.options.length);
        ɵɵadvance(3);
        ɵɵattribute("name", ctx.name);
        ɵɵadvance();
        ɵɵrepeater(ctx.options);
        ɵɵadvance(2);
        ɵɵproperty("p-append-in-body", ctx.appendBox)("p-help", ctx.help)("p-disabled", ctx.disabled)("p-error-limit", ctx.errorLimit)("p-error-pattern", ctx.getErrorPattern())("p-size", ctx.size)("p-helper", !ctx.label ? poHelper_r5.helperSettings : void 0)("p-show-helper", !ctx.label ? ctx.displayAdditionalHelp : void 0);
      }
    },
    dependencies: [PoFieldContainerBottomComponent, PoFieldContainerComponent, PoRadioComponent],
    encapsulation: 2,
    changeDetection: 0
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoRadioGroupComponent, [{
    type: Component,
    args: [{
      selector: "po-radio-group",
      changeDetection: ChangeDetectionStrategy.OnPush,
      providers: [{
        provide: NG_VALUE_ACCESSOR,
        useExisting: forwardRef(() => PoRadioGroupComponent),
        multi: true
      }, {
        provide: NG_VALIDATORS,
        useExisting: forwardRef(() => PoRadioGroupComponent),
        multi: true
      }],
      standalone: false,
      template: '@let poHelper = setHelper(label, additionalHelpTooltip);\n<po-field-container\n  [p-disabled]="disabled"\n  [p-label]="label"\n  [p-optional]="optional"\n  [p-required]="required"\n  [p-show-required]="showRequired"\n  [p-helper]="label ? poHelper.helperSettings : undefined"\n  [p-show-helper]="label ? displayAdditionalHelp : undefined"\n  [p-size]="size"\n  [p-text-wrap]="labelTextWrap()"\n  [p-compact-label]="compactLabel()"\n>\n  <div #inp role="radiogroup" [attr.aria-label]="label" [attr.data-options]="options.length">\n    <div class="po-field-container-content">\n      <div class="po-radio-group" [attr.name]="name">\n        @for (option of options; track option) {\n          <div class="po-radio-group-item po-md-{{ mdColumns }} po-lg-{{ columns }}">\n            <po-radio\n              #inputRadio\n              [name]="name"\n              [p-checked]="value === option.value"\n              [p-disabled]="option.disabled === true || disabled"\n              [p-label]="option.label"\n              [p-label-text-wrap]="true"\n              [p-required]="required"\n              [p-size]="size"\n              [p-value]="option.value"\n              (p-blur)="onBlur(inputRadio)"\n              (keydown)="onKeyDown($event, inputRadio)"\n              (keyup)="onKeyUp($event, option.value)"\n              (click)="eventClick(option.value, option.disabled === true || disabled)"\n            >\n            </po-radio>\n          </div>\n        }\n      </div>\n    </div>\n  </div>\n  <po-field-container-bottom\n    [p-append-in-body]="appendBox"\n    [p-help]="help"\n    [p-disabled]="disabled"\n    [p-error-limit]="errorLimit"\n    [p-error-pattern]="getErrorPattern()"\n    [p-size]="size"\n    [p-helper]="!label ? poHelper.helperSettings : undefined"\n    [p-show-helper]="!label ? displayAdditionalHelp : undefined"\n  ></po-field-container-bottom>\n</po-field-container>\n'
    }]
  }], () => [], {
    label: [{
      type: Input,
      args: ["p-label"]
    }],
    help: [{
      type: Input,
      args: ["p-help"]
    }],
    inputEl: [{
      type: ViewChild,
      args: ["inp", {
        read: ElementRef,
        static: true
      }]
    }],
    radioLabels: [{
      type: ViewChildren,
      args: ["inputRadio"]
    }],
    helperEl: [{
      type: ViewChild,
      args: ["helperEl", {
        read: PoHelperComponent,
        static: false
      }]
    }]
  });
})();
var PoRadioModule = class _PoRadioModule {
  static ɵfac = function PoRadioModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoRadioModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoRadioModule,
    declarations: [PoRadioComponent],
    imports: [CommonModule, FormsModule, PoLabelModule],
    exports: [PoRadioComponent]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [CommonModule, FormsModule, PoLabelModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoRadioModule, [{
    type: NgModule,
    args: [{
      declarations: [PoRadioComponent],
      exports: [PoRadioComponent],
      imports: [CommonModule, FormsModule, PoLabelModule]
    }]
  }], null, null);
})();
var PoRadioGroupModule = class _PoRadioGroupModule {
  static ɵfac = function PoRadioGroupModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoRadioGroupModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoRadioGroupModule,
    declarations: [PoRadioGroupComponent],
    imports: [CommonModule, FormsModule, PoFieldContainerModule, PoRadioModule, PoHelperModule],
    exports: [PoRadioGroupComponent]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [CommonModule, FormsModule, PoFieldContainerModule, PoRadioModule, PoHelperModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoRadioGroupModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, FormsModule, PoFieldContainerModule, PoRadioModule, PoHelperModule],
      exports: [PoRadioGroupComponent],
      declarations: [PoRadioGroupComponent]
    }]
  }], null, null);
})();
var PoMask = class {
  mask = "";
  formatModel = false;
  // controle de posição
  initialPosition = 0;
  finalPosition = 0;
  pattern = "";
  get getPattern() {
    return this.pattern;
  }
  valueToInput;
  get getValueToInput() {
    return this.valueToInput;
  }
  set setValueToInput(value) {
    this.valueToInput = value;
  }
  valueToModel;
  get getValueToModel() {
    return this.valueToModel;
  }
  set setValueToModel(value) {
    this.valueToModel = value;
  }
  formattingEnds;
  constructor(mask, formatModel) {
    this.mask = mask;
    this.formatModel = formatModel;
    this.pattern = this.getRegexFromMask(mask);
  }
  keyup($event) {
    if (this.mask) {
      const value = $event.target.value;
      if ($event.keyCode === 17 || $event.keyCode === 91) {
        $event.target.value = this.controlFormatting(value);
        this.resetPositions($event);
      }
      $event.preventDefault();
      switch ($event.keyCode) {
        case 37:
          if (this.initialPosition > 0) {
            this.initialPosition--;
          }
          this.setPositionNotShiftKey($event);
          this.setSelectionRange($event);
          break;
        case 39:
          if (this.initialPosition < value.toString().length) {
            this.initialPosition++;
          }
          this.setPositionNotShiftKey($event);
          this.setSelectionRange($event);
          break;
        case 35:
          this.finalPosition = value.toString().length;
          if ($event.shiftKey) {
            this.setPositions($event);
          } else {
            this.initialPosition = this.finalPosition;
            this.setPositions($event);
          }
          break;
        case 36:
          if ($event.shiftKey) {
            this.finalPosition = this.initialPosition;
            this.initialPosition = 0;
            this.setPositions($event);
          } else {
            this.initialPosition = 0;
            this.finalPosition = 0;
            this.setPositions($event);
          }
          break;
      }
    }
  }
  setPositionNotShiftKey($event) {
    if (!$event.shiftKey) {
      this.finalPosition = this.initialPosition;
    }
  }
  setSelectionRange($event) {
    if (this.initialPosition > this.finalPosition) {
      $event.target.setSelectionRange?.(this.finalPosition, this.initialPosition);
    } else {
      $event.target.setSelectionRange?.(this.initialPosition, this.finalPosition);
    }
  }
  keydown($event) {
    if (this.mask) {
      let value = $event.target.value;
      if ($event.keyCode === 9) {
        return;
      }
      if (!$event.ctrlKey && !$event.metaKey) {
        $event.preventDefault();
      }
      if ($event.ctrlKey || $event.metaKey && $event.keyCode !== 86 || $event.keyCode >= 37 && $event.keyCode <= 40 || $event.keyCode === 16 || $event.keyCode === 9) {
        return;
      }
      if (this.isKeyValid($event.keyCode)) {
        if (this.finalPosition === null) {
          this.finalPosition = this.initialPosition;
        }
        this.revertPositions(this.initialPosition, this.finalPosition);
        switch ($event.keyCode) {
          case 8:
            this.getPosition($event);
            if (this.initialPosition < 0) {
              this.initialPosition = 0;
              this.setPositions($event);
            }
            if (this.initialPosition === this.finalPosition) {
              this.checkMaskBefore($event, -1);
              if (this.initialPosition !== 0) {
                value = value.slice(0, this.initialPosition - 1) + value.slice(this.finalPosition);
                value = this.controlFormatting(value);
                $event.target.value = value;
                this.changePosition($event, -1);
                this.checkMaskBefore($event, -1);
                this.setPositions($event);
                this.resetPositions($event);
              }
            } else {
              this.clearRangeSelection(value, $event, true);
            }
            this.setPositions($event);
            break;
          case 46:
            this.getPosition($event);
            if (this.initialPosition === this.finalPosition) {
              this.checkMaskAfter($event, 1);
              value = value.slice(0, this.initialPosition) + value.slice(this.finalPosition + 1);
              value = this.controlFormatting(value);
              $event.target.value = value;
              this.setPositions($event);
              this.resetPositions($event);
            } else {
              this.clearRangeSelection(value, $event, false);
            }
            this.setPositions($event);
            break;
          default:
            this.getPosition($event);
            value = value.slice(0, this.initialPosition) + $event.key + value.slice(this.finalPosition);
            value = this.controlFormatting(value);
            $event.target.value = value;
            this.changePosition($event, 1);
            this.checkMaskBefore($event, 1);
            this.setPositions($event);
            this.resetPositions($event);
            this.setPositions($event);
        }
      }
    }
  }
  clearRangeSelection(value, $event, isBackspace) {
    value = value.slice(0, this.initialPosition) + value.slice(this.finalPosition);
    value = this.controlFormatting(value);
    $event.target.value = value;
    if (isBackspace) {
      this.checkMaskBefore($event, -1);
    }
    this.setPositions($event);
    this.resetPositions($event);
  }
  // passa a posição do click para o controle de posição
  click($event) {
    this.initialPosition = $event.target.selectionStart;
    this.finalPosition = $event.target.selectionEnd;
  }
  blur($event) {
    if (this.mask) {
      let value = $event.target.value;
      value = this.controlFormatting(value);
      $event.target.value = value;
    }
  }
  revertPositions(initialPosition, finalPosition) {
    if (initialPosition > finalPosition) {
      const tempPosition = initialPosition;
      this.initialPosition = finalPosition;
      this.finalPosition = tempPosition;
    }
  }
  // reseta o controle de posição
  resetPositions($event) {
    this.initialPosition = $event.target.selectionStart;
    this.finalPosition = this.initialPosition;
  }
  // posiciona o cursor de acordo com o controle de posição
  setPositions($event) {
    $event.target.setSelectionRange?.(this.initialPosition, this.finalPosition);
  }
  // muda a posição do cursor e atualiza o controle de posição
  changePosition($event, value) {
    this.initialPosition = this.initialPosition + value;
    this.finalPosition = this.finalPosition + value;
    this.setPositions($event);
  }
  getPosition($event) {
    this.initialPosition = $event.target.selectionStart;
    this.finalPosition = $event.target.selectionEnd;
    this.setPositions($event);
  }
  // Método responsável por controlar a formatação e aplicar todas as máscara possíveis
  // quando houver valores opcionais (?)
  controlFormatting(value) {
    if (!value) {
      this.valueToInput = "";
      this.valueToModel = "";
      return "";
    }
    let valueProcessed;
    let maskTmp = this.mask;
    const arrMasks = [];
    let contMasks = 0;
    while (this.hasOptionalNumber(maskTmp)) {
      arrMasks.push(maskTmp);
      maskTmp = this.replaceOptionalNumber(maskTmp);
    }
    arrMasks.push(maskTmp);
    arrMasks.reverse();
    this.formattingEnds = false;
    while (!this.formattingEnds) {
      this.formattingEnds = true;
      if (!arrMasks[contMasks]) {
        break;
      }
      valueProcessed = this.formatValue(value, arrMasks[contMasks]);
      contMasks++;
    }
    return valueProcessed;
  }
  // Função que formata a máscara com o valor passado
  formatValue(value, mask) {
    mask = mask.replace(/\?/g, "");
    const guide = mask.replace(this.listValidKeys(), "_");
    let contGuide = 0;
    let valueProcessed = "";
    value = this.removeFormattingValue(value);
    for (let i2 = 0; i2 < value.length; i2++) {
      const charValue = value[i2];
      if (!guide[contGuide]) {
        this.formattingEnds = false;
        break;
      }
      while (this.isFixedCharacterGuide(guide[contGuide]) && guide[contGuide]) {
        valueProcessed += guide[contGuide];
        contGuide++;
      }
      if (this.isKeyValidMask(charValue, mask[contGuide])) {
        valueProcessed += charValue;
        contGuide++;
      } else {
        break;
      }
    }
    if (this.formatModel) {
      this.valueToInput = valueProcessed;
      this.valueToModel = valueProcessed;
    } else {
      this.valueToInput = valueProcessed;
      this.valueToModel = this.removeFormattingValue(valueProcessed);
    }
    return valueProcessed;
  }
  // verifica se tem algum caracter de mascara antes do cursor
  checkMaskBefore($event, position) {
    if (this.isFixedCharacterGuide($event.target.value.toString().charAt(this.initialPosition - 1))) {
      this.changePosition($event, position);
      this.checkMaskBefore($event, position);
    }
  }
  // verifica se tem algum caracter de mascara depois do cursor
  checkMaskAfter($event, position) {
    if (this.isFixedCharacterGuide($event.target.value.toString().charAt(this.initialPosition))) {
      this.changePosition($event, position);
      this.checkMaskAfter($event, position);
    }
  }
  // Retorna a máscara sem um valor opcional
  replaceOptionalNumber(mask) {
    let i2 = 9;
    while (i2 >= 0) {
      if (mask.indexOf(i2 + "?") > -1) {
        return mask.replace(i2 + "?", "");
      }
      i2--;
    }
    return mask;
  }
  // Verifica se contém caracteres permitidos somente na máscara \/() +-
  isFixedCharacterGuide(key2) {
    return this.testRegex(key2, this.getFixedCharacterGuide());
  }
  // Retorna caracteres permitidos somente na máscara \/() +-
  getFixedCharacterGuide() {
    return /[\\\/() +-.\:]/g;
  }
  // Caracteres permitidos de serem digitados
  listValidKeys() {
    return /[a-zA-Z0-9]/g;
  }
  // Se é um dígito válido
  isKeyValid(keyCode) {
    return this.isKeyCodeValid(keyCode);
  }
  // Verifica se a tecla digitada é permitida
  // Permite apenas números, letras, backspace e del
  isKeyCodeValid(keyCode) {
    return keyCode >= 48 && keyCode <= 57 || keyCode >= 65 && keyCode <= 90 || keyCode >= 96 && keyCode <= 105 || keyCode === 8 || keyCode === 9 || keyCode === 46;
  }
  // Se está de acordo com a máscara
  isKeyValidMask(key2, keyMask) {
    return this.testRegex(key2, this.replaceMask(keyMask));
  }
  // Retorna se a chave foi aprovada pela expressão regular
  testRegex(key2, regex) {
    return regex.test(key2);
  }
  // Remove a formatacão do valor
  // É possível ser melhorado para remover pontualmente os caracteres fixos de acordo com a máscara
  removeFormattingValue(value) {
    return value.replace(this.getFixedCharacterGuide(), "");
  }
  // Verifica se contém valor opcional na máscara 0-9?
  hasOptionalNumber(mask) {
    return mask.match(/\d\?/g);
  }
  // Retorna a expressão regular correspondente ao comando passado
  replaceMask(char) {
    let regex = /./;
    switch (char) {
      case "0":
        regex = /[0]/;
        break;
      case "1":
        regex = /[0-1]/;
        break;
      case "2":
        regex = /[0-2]/;
        break;
      case "3":
        regex = /[0-3]/;
        break;
      case "4":
        regex = /[0-4]/;
        break;
      case "5":
        regex = /[0-5]/;
        break;
      case "6":
        regex = /[0-6]/;
        break;
      case "7":
        regex = /[0-7]/;
        break;
      case "8":
        regex = /[0-8]/;
        break;
      case "9":
        regex = /[0-9]/;
        break;
      case " ":
        regex = /\s/;
        break;
      case "@":
        regex = /[a-zA-Z]/;
        break;
      case "w":
        regex = /[a-zA-Z0-9]/;
        break;
    }
    return regex;
  }
  getRegexFromMask(mask) {
    if (mask) {
      let pattern;
      if (this.formatModel) {
        pattern = mask.replace(/\\/g, "\\\\");
        pattern = pattern.replace(/\+/g, "\\+");
        pattern = pattern.replace(/\./g, "\\.");
        pattern = pattern.replace(/-/g, "-");
        pattern = pattern.replace(/\(/g, "\\(");
        pattern = pattern.replace(/\)/g, "\\)");
        pattern = pattern.replace(/\//g, "\\/");
        pattern = pattern.replace(/\s/g, "\\s");
        pattern = pattern.replace(/:/g, "\\:");
        pattern = pattern.replace(/\@(?!\s)/g, "\\w");
        pattern = pattern.replace(/\d/g, "\\w");
      } else {
        pattern = mask.replace(/\\/g, "");
        pattern = pattern.replace(/\+/g, "");
        pattern = pattern.replace(/\./g, "");
        pattern = pattern.replace(/-/g, "");
        pattern = pattern.replace(/\(/g, "");
        pattern = pattern.replace(/\)/g, "");
        pattern = pattern.replace(/\//g, "");
        pattern = pattern.replace(/\s/g, "");
        pattern = pattern.replace(/:/g, "");
        pattern = pattern.replace(/\@/g, "\\w");
        pattern = pattern.replace(/\d/g, "\\w");
      }
      return pattern;
    } else {
      return null;
    }
  }
};
var PoDatepickerIsoFormat;
(function(PoDatepickerIsoFormat2) {
  PoDatepickerIsoFormat2["Basic"] = "basic";
  PoDatepickerIsoFormat2["Extended"] = "extended";
})(PoDatepickerIsoFormat || (PoDatepickerIsoFormat = {}));
var poDatepickerFormatDefault$1 = "dd/mm/yyyy";
var PoDatepickerBaseComponent = class _PoDatepickerBaseComponent {
  languageService;
  cd;
  // Propriedade interna que define se o ícone de ajuda adicional terá cursor clicável (evento) ou padrão (tooltip).
  additionalHelpEventTrigger;
  /** Rótulo do campo. */
  label;
  /** Texto de apoio do campo. */
  help;
  /**
   * Define o modo de operação do datepicker.
   *
   * Permite configurar o componente para seleção de:
   * - Mês e ano (`month-year`);
   * - Apenas ano (`year`).
   */
  mode = void 0;
  /**
   * Define o limite de anos exibidos nas variações `month-year` e `year`,
   * considerando a data atual como referência.
   *
   * O valor informado determina o intervalo de anos anterior e posterior
   * à data corrente que será disponibilizado para seleção.
   *
   * @default 150
   */
  yearRangeLimit = 150;
  /**
   *
   * @deprecated v23.x.x use `p-helper`
   *
   * @optional
   *
   * @description
   * Exibe um ícone de ajuda adicional, com o texto desta propriedade sendo passado para o popover do componente `po-helper`.
   * **Como boa prática, indica-se utilizar um texto com até 140 caracteres.**
   * > Requer um recuo mínimo de 8px se o componente estiver próximo à lateral da tela.
   *
   * > Essa propriedade está **depreciada** e será removida na versão `23.x.x`. Recomendamos utilizar a propriedade `p-helper` que oferece mais recursos e flexibilidade.
   *
   */
  additionalHelpTooltip;
  /**
   * @optional
   *
   * @description
   *
   * Aplica foco no elemento ao ser iniciado.
   *
   * > Caso mais de um elemento seja configurado com essa propriedade, apenas o último elemento declarado com ela terá o foco.
   *
   * @default `false`
   */
  autoFocus = false;
  /**
   * @optional
   *
   * @description
   * Define se o título do campo será exibido de forma compacta.
   *
   * Quando habilitado (`true`), o modo compacto afeta o conjunto composto por:
   * - `po-label`
   * - `p-requirement (showRequired)`
   * - `po-helper`
   *
   * Ou seja, todos os elementos relacionados ao título do campo
   * (rótulo, indicador de obrigatoriedade e componente auxiliar) passam
   * a seguir o comportamento de layout compacto.
   *
   * Também é possível definir esse comportamento de forma global,
   * uma única vez, na folha de estilo geral da aplicação, por meio
   * da customização dos tokens CSS:
   *
   * - `--field-container-title-justify`
   * - `--field-container-title-flex`
   *
   * Exemplo:
   *
   * ```
   * :root {
   *   --field-container-title-justify: flex-start;
   *   --field-container-title-flex: 0 1 auto;
   * }
   * ```
   *
   * Dessa forma, o layout compacto passa a ser o padrão da aplicação,
   * sem a necessidade de definir a propriedade individualmente em cada campo.
   *
   * @default `false`
   */
  compactLabel = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "compactLabel"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-compact-label",
    transform: convertToBoolean
  }));
  /**
   * @optional
   *
   * @description
   *
   * Função executada para realizar a validação assíncrona personalizada.
   * Executada ao disparar o output `change`.
   *
   * @param value Valor atual preenchido no campo.
   *
   * @returns Retorna Observable com o valor `true` para sinalizar o erro `false` para indicar que não há erro.
   */
  errorAsync;
  /* Nome do componente datepicker. */
  name;
  /**
   * @optional
   *
   * @description
   *
   * Define se a indicação de campo opcional será exibida.
   *
   * > Não será exibida a indicação se:
   * - O campo conter `p-required`;
   * - Não possuir `p-help` e/ou `p-label`.
   *
   * @default `false`
   */
  optional;
  /**
   * Mensagem apresentada quando a data for inválida ou fora do período.
   *
   * > Por padrão, esta mensagem não é apresentada quando o campo estiver vazio, mesmo que ele seja requerido.
   * Para exibir a mensagem com o campo vazio, utilize a propriedade `p-required-field-error-message` em conjunto.
   */
  errorPattern = "";
  /**
   * @optional
   *
   * @description
   *
   * Limita a exibição da mensagem de erro a duas linhas e exibe um tooltip com o texto completo.
   *
   * > Caso essa propriedade seja definida como `true`, a mensagem de erro será limitada a duas linhas
   * e um tooltip será exibido ao passar o mouse sobre a mensagem para mostrar o conteúdo completo.
   *
   * @default `false`
   */
  errorLimit = false;
  /**
   * @optional
   *
   * @description
   *
   * Exibe a mensagem setada na propriedade `p-error-pattern` se o campo estiver vazio e for requerido.
   *
   * > Necessário que a propriedade `p-required` esteja habilitada.
   *
   * @default `false`
   */
  showErrorMessageRequired = false;
  /**
   * @deprecated v23.x.x use `p-helper`
   *
   * @optional
   *
   * @description
   * Evento disparado ao clicar no ícone de ajuda adicional.
   *
   * > Essa propriedade está **depreciada** e será removida na versão `23.x.x`. Recomendamos utilizar a propriedade `p-helper` que oferece mais recursos e flexibilidade.
   */
  additionalHelp = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Evento disparado sempre que o valor do model é alterado, seja por interação do usuário
   * ou por atualização programática (ex: `setValue`, `patchValue`, carregamento assíncrono).
   *
   * Diferentemente do `p-change`, que é disparado apenas por interação do usuário,
   * o `p-change-model` cobre todos os cenários de alteração de valor.
   *
   * Não emite quando o novo valor é idêntico ao anterior (deduplicação automática).
   */
  changeModel = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Evento disparado ao sair do campo.
   */
  onblur = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Evento disparado ao alterar valor do campo.
   */
  onchange = new EventEmitter();
  /**
   * @optional
   *
   * @description
   * Evento disparado quando uma tecla é pressionada enquanto o foco está no componente.
   * Retorna um objeto `KeyboardEvent` com informações sobre a tecla.
   */
  keydown = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Define as opções do componente de ajuda (po-helper) que será exibido ao lado do label quando a propriedade `p-label` for definida, ou, ao lado do componente na ausência da propriedade `p-label`.
   * > Para mais informações acesse: https://po-ui.io/documentation/po-helper.
   *
   * > Ao configurar esta propriedade, o antigo ícone de ajuda adicional (`p-additional-help-tooltip` e `p-additional-help`) será ignorado.
   */
  poHelperComponent = input(void 0, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "poHelperComponent"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-helper"
  }));
  /**
   * @optional
   *
   * @description
   *
   * Habilita a quebra automática do texto da propriedade `p-label`. Quando `p-label-text-wrap` for verdadeiro, o texto que excede
   * o espaço disponível é transferido para a próxima linha em pontos apropriados para uma
   * leitura clara.
   *
   * @default `false`
   */
  labelTextWrap = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "labelTextWrap"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-label-text-wrap"
  }));
  modelLastUpdate;
  offset;
  firstStart = true;
  hour = "T00:00:00-00:00";
  isExtendedISO = false;
  objMask;
  onChangeModel = null;
  validatorChange;
  onTouchedModel = null;
  shortLanguage;
  isInvalid;
  hasValidatorRequired;
  _format = poDatepickerFormatDefault$1;
  _isoFormat;
  _maxDate;
  _minDate;
  _noAutocomplete = false;
  _placeholder = "";
  _loading = false;
  previousValue;
  _size = void 0;
  _initialSize = void 0;
  subscription = new Subscription();
  _date;
  /**
   * @optional
   *
   * @description
   *
   * Define a propriedade nativa `autocomplete` do campo como `off`.
   *
   * @default `false`
   */
  set noAutocomplete(value) {
    this._noAutocomplete = value;
  }
  get noAutocomplete() {
    return this._noAutocomplete;
  }
  /**
   * @optional
   *
   * @description
   *
   * Mensagem que aparecerá enquanto o campo não estiver preenchido.
   */
  set placeholder(placeholder) {
    this._placeholder = isTypeof(placeholder, "string") ? placeholder : "";
  }
  get placeholder() {
    return this._placeholder;
  }
  /** Desabilita o campo. */
  // eslint-disable-next-line @typescript-eslint/member-ordering
  disabled = false;
  set setDisabled(disabled) {
    this.disabled = disabled;
    this.validateModel(PoUtils.convertDateToISOExtended(this.date, this.hour));
  }
  /** Torna o elemento somente leitura. */
  // eslint-disable-next-line @typescript-eslint/member-ordering
  readonly = false;
  set setReadonly(readonly) {
    this.readonly = readonly;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define que o campo será obrigatório.
   *
   * @default `false`
   */
  // eslint-disable-next-line @typescript-eslint/member-ordering
  required = false;
  set setRequired(required) {
    this.required = required;
    this.validateModel(PoUtils.convertDateToISOExtended(this.date, this.hour));
  }
  /**
   * Define se a indicação de campo obrigatório será exibida.
   *
   * > Não será exibida a indicação se:
   * - Não possuir `p-help` e/ou `p-label`.
   */
  showRequired = false;
  /**
   * @optional
   *
   * @description
   *
   * Define o tamanho do componente:
   * - `small`: altura do input como 32px (disponível apenas para acessibilidade AA).
   * - `medium`: altura do input como 44px.
   *
   * > Caso a acessibilidade AA não esteja configurada, o tamanho `medium` será mantido.
   * Para mais detalhes, consulte a documentação do [po-theme](https://po-ui.io/documentation/po-theme).
   *
   * @default `medium`
   */
  set size(value) {
    this._initialSize = value;
    this.applySizeBasedOnA11y();
  }
  get size() {
    return this._size ?? getDefaultSizeFn(PoFieldSize);
  }
  /** Habilita ação para limpar o campo. */
  // eslint-disable-next-line @typescript-eslint/member-ordering
  clean = false;
  set setClean(clean) {
    this.clean = clean;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define uma data mínima para o `po-datepicker`.
   */
  set minDate(value) {
    if (value instanceof Date) {
      const year = value.getFullYear();
      const date = new Date(year, value.getMonth(), value.getDate(), 0, 0, 0);
      PoUtils.setYearFrom0To100(date, year);
      this._minDate = date;
    } else {
      this._minDate = convertIsoToDate(value, true, false);
    }
    this.validateModel(PoUtils.convertDateToISOExtended(this.date, this.hour));
  }
  get minDate() {
    return this._minDate;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define uma data máxima para o `po-datepicker`.
   */
  set maxDate(value) {
    if (value instanceof Date) {
      const year = value.getFullYear();
      const date = new Date(year, value.getMonth(), value.getDate(), 23, 59, 59);
      PoUtils.setYearFrom0To100(date, year);
      this._maxDate = date;
    } else {
      this._maxDate = convertIsoToDate(value, false, true);
    }
    this.validateModel(PoUtils.convertDateToISOExtended(this.date, this.hour));
  }
  get maxDate() {
    return this._maxDate;
  }
  /**
   * @optional
   *
   * @description
   *
   * Formato de exibição da data.
   *
   * Valores válidos:
   *  - `dd/mm/yyyy`
   *  - `mm/dd/yyyy`
   *  - `yyyy/mm/dd`
   *
   * Propriedade incompatível com as variações month-year e year.
   *
   * @default `dd/mm/yyyy`
   */
  set format(value) {
    if (value) {
      value = value.toLowerCase();
      if (value.match(/dd/) && value.match(/mm/) && value.match(/yyyy/)) {
        this._format = value;
        this.objMask = this.buildMask(replaceFormatSeparator(this.format, this.languageService.getDateSeparator(this.locale)));
        this.refreshValue(this.date);
        return;
      }
    }
    this._format = poDatepickerFormatDefault$1;
    this.objMask = this.buildMask(replaceFormatSeparator(this.format, this.languageService.getDateSeparator(this.locale)));
  }
  get format() {
    return this._format;
  }
  /**
   * @optional
   *
   * @description
   *
   * Padrão de formatação para saída do *model*, independentemente do formato de entrada.
   *
   * > Veja os valores válidos no *enum* `PoDatepickerIsoFormat`.
   *
   * Propriedade incompatível com as variações month-year e year.
   */
  set isoFormat(value) {
    if (Object.values(PoDatepickerIsoFormat).includes(value)) {
      this._isoFormat = value;
      this.isExtendedISO = value === PoDatepickerIsoFormat.Extended;
    }
  }
  get isoFormat() {
    return this._isoFormat;
  }
  /**
   * @optional
   *
   * @description
   *
   * Idioma do Datepicker.
   *
   * > O locale padrão sera recuperado com base no [`PoI18nService`](/documentation/po-i18n) ou *browser*.
   */
  // eslint-disable-next-line @typescript-eslint/member-ordering
  _locale;
  set locale(value) {
    if (value) {
      this._locale = value.length >= 2 ? value : poLocaleDefault;
      this.objMask = this.buildMask(replaceFormatSeparator(this.format, this.languageService.getDateSeparator(this.locale)));
    } else {
      this._locale = this.shortLanguage;
      this.objMask = this.buildMask(replaceFormatSeparator(this.format, this.languageService.getDateSeparator(this.locale)));
    }
    this.refreshValue(this.date);
  }
  get locale() {
    return this._locale || this.shortLanguage;
  }
  /**
   * @optional
   *
   * @description
   * Exibe um ícone de carregamento no lado direito do campo para sinalizar que uma operação está em andamento.
   *
   * @default `false`
   */
  set loading(value) {
    this._loading = value;
    this.cd?.markForCheck();
  }
  get loading() {
    return this._loading;
  }
  get isDisabled() {
    return this.disabled || this.loading;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define que o `calendar` e/ou tooltip (`p-additional-help-tooltip` e/ou `p-error-limit`) serão incluídos no body da
   * página e não dentro do componente. Essa opção pode ser necessária em cenários com containers que possuem scroll ou
   * overflow escondido, garantindo o posicionamento correto de ambos próximo ao elemento.
   *
   * > Quando utilizado com `p-helper`, leitores de tela como o NVDA podem não ler o conteúdo do popover.
   *
   * @default `false`
   */
  appendBox = false;
  constructor(languageService, cd) {
    this.languageService = languageService;
    this.cd = cd;
  }
  set date(value) {
    if (!value) {
      this._date = value;
      return;
    }
    if (typeof value === "string") {
      if (this.mode === "month-year") {
        const [month, year] = value.split("/").map(Number);
        this._date = new Date(year, month - 1, 1);
      } else if (this.mode === "year") {
        this._date = new Date(Number(value), 0, 1);
      } else {
        this._date = convertIsoToDate(value, false, false);
      }
    } else {
      this._date = value;
    }
  }
  get date() {
    return this._date;
  }
  ngOnInit() {
    this.offset = (/* @__PURE__ */ new Date()).getTimezoneOffset();
    this.formatTimezoneAndHour(this.offset);
    if (this.mode === "month-year") {
      const separator = this.languageService.getDateSeparator(this.locale);
      this.objMask = new PoMask(`99${separator}9999`, true);
    } else if (this.mode === "year") {
      this.objMask = new PoMask("9999", true);
    } else {
      this.objMask = this.buildMask(replaceFormatSeparator(this.format, this.languageService.getDateSeparator(this.locale)));
    }
  }
  ngOnDestroy() {
    this.subscription?.unsubscribe();
  }
  onThemeChange() {
    this.applySizeBasedOnA11y();
  }
  // Converte um objeto string em Date
  getDateFromString(dateString) {
    const day = parseInt(dateString.substring(this.format.indexOf("d"), this.format.indexOf("d") + 2), 10);
    const month = parseInt(dateString.substring(this.format.indexOf("m"), this.format.indexOf("m") + 2), 10) - 1;
    const year = parseInt(dateString.substring(this.format.indexOf("y"), this.format.indexOf("y") + 4), 10);
    const date = new Date(year, month, day);
    PoUtils.setYearFrom0To100(date, year);
    return date.getFullYear() === year && date.getMonth() === month && date.getDate() === day ? date : null;
  }
  // Formata a data.
  formatToDate(value) {
    let dateFormatted = this.format;
    dateFormatted = dateFormatted.replace("dd", ("0" + value.getDate()).slice(-2));
    dateFormatted = dateFormatted.replace("mm", ("0" + (value.getMonth() + 1)).slice(-2));
    dateFormatted = dateFormatted.replace("yyyy", PoUtils.formatYear(value.getFullYear()));
    return dateFormatted;
  }
  // Método responsável por controlar o modelo.
  controlModel(date) {
    this.date = date;
    if (date && this.isExtendedISO) {
      this.callOnChange(PoUtils.convertDateToISOExtended(this.date, this.hour));
    } else if (date && !this.isExtendedISO) {
      this.callOnChange(PoUtils.convertDateToISODate(this.date));
    } else {
      date === void 0 ? this.callOnChange("") : this.callOnChange("Data inválida");
    }
  }
  // Executa a função onChange
  callOnChange(value, retry = true) {
    if (this.onChangeModel && value !== this.previousValue) {
      this.onChangeModel(value);
      this.previousValue = value;
      this.controlChangeModelEmitter(value);
    } else if (retry) {
      setTimeout(() => this.callOnChange(value, false));
    }
  }
  controlChangeModelEmitter(value) {
    if (this.modelLastUpdate !== value) {
      this.changeModel.emit(value);
      this.modelLastUpdate = value;
    }
  }
  //Transforma o tamanho do input para o tamanho do ícone de loading correspondente
  mapSizeToIcon(size) {
    return mapInputSizeToLoadingIcon(size);
  }
  // Função implementada do ControlValueAccessor
  // Usada para interceptar os estados de habilitado via forms api
  setDisabledState(isDisabled) {
    this.disabled = isDisabled;
    this.cd.markForCheck();
  }
  // Função implementada do ControlValueAccessor
  // Usada para interceptar as mudanças e não atualizar automaticamente o Model
  registerOnChange(func) {
    this.onChangeModel = func;
  }
  // Função implementada do ControlValueAccessor
  // Usada para interceptar as mudanças e não atualizar automaticamente o Model
  registerOnTouched(func) {
    this.onTouchedModel = func;
  }
  registerOnValidatorChange(fn) {
    this.validatorChange = fn;
  }
  validate(c) {
    this.errorPattern = this.errorPattern !== "Data inválida" && this.errorPattern !== "Data fora do período" ? this.errorPattern : "";
    if (!this.hasValidatorRequired && this.showErrorMessageRequired && c.hasValidator(Validators.required)) {
      this.hasValidatorRequired = true;
    }
    if (this.mode === "month-year" || this.mode === "year") {
      if (this.isMonthYearOrYearInvalid(c.value)) {
        this.errorPattern = this.errorPattern || "Data inválida";
        this.cd?.markForCheck();
        return {
          date: {
            valid: false
          }
        };
      }
    } else if (dateFailed(c.value)) {
      this.errorPattern = this.errorPattern || "Data inválida";
      this.cd?.markForCheck();
      return {
        date: {
          valid: false
        }
      };
    }
    if (PoValidators.requiredFailed(this.required, this.disabled, c.value)) {
      this.cd?.markForCheck();
      return {
        required: {
          valid: false
        }
      };
    }
    if (this.date && !this.isDateRangeValidForMode()) {
      this.errorPattern = this.errorPattern || "Data fora do período";
      this.cd?.markForCheck();
      return {
        date: {
          valid: false
        }
      };
    }
    if (this.errorPattern !== "") {
      this.subscription?.unsubscribe();
      this.subscription = c.statusChanges.pipe(switchMap((status) => {
        if (status === "INVALID") {
          this.cd?.markForCheck();
        }
        return [];
      })).subscribe();
    }
    return null;
  }
  // Valida se o valor é inválido para modos month-year ou year
  isMonthYearOrYearInvalid(value) {
    if (!value || value === "") {
      return false;
    }
    if (typeof value !== "string") {
      return true;
    }
    if (this.mode === "month-year") {
      const parts = value.split("/");
      if (parts.length !== 2) {
        return true;
      }
      const month = parseInt(parts[0], 10);
      const year = parseInt(parts[1], 10);
      return isNaN(month) || isNaN(year) || month < 1 || month > 12 || year <= 0;
    }
    if (this.mode === "year") {
      const year = parseInt(value, 10);
      return isNaN(year) || year <= 0;
    }
    return false;
  }
  // Valida range de data considerando o modo
  isDateRangeValidForMode() {
    if (this.mode === "month-year") {
      return this.validateMonthYearRange(this.date, this._minDate, this._maxDate);
    }
    if (this.mode === "year") {
      return this.validateYearRange(this.date, this._minDate, this._maxDate);
    }
    return PoUtils.validateDateRange(this.date, this._minDate, this._maxDate);
  }
  validateMonthYearRange(date, minDate, maxDate) {
    if (!date) {
      return true;
    }
    const dateMonthYear = date.getFullYear() * 12 + date.getMonth();
    if (minDate) {
      const minMonthYear = minDate.getFullYear() * 12 + minDate.getMonth();
      if (dateMonthYear < minMonthYear) {
        return false;
      }
    }
    if (maxDate) {
      const maxMonthYear = maxDate.getFullYear() * 12 + maxDate.getMonth();
      if (dateMonthYear > maxMonthYear) {
        return false;
      }
    }
    return true;
  }
  validateYearRange(date, minDate, maxDate) {
    if (!date) {
      return true;
    }
    if (minDate && date.getFullYear() < minDate.getFullYear()) {
      return false;
    }
    if (maxDate && date.getFullYear() > maxDate.getFullYear()) {
      return false;
    }
    return true;
  }
  validateModel(model2) {
    if (this.validatorChange) {
      this.validatorChange(model2);
    }
  }
  // Retorna um objeto do tipo PoMask com a mascara configurada.
  buildMask(format2 = this.format) {
    let mask = format2.toUpperCase();
    mask = mask.replace(/DD/g, "99");
    mask = mask.replace(/MM/g, "99");
    mask = mask.replace(/YYYY/g, "9999");
    return new PoMask(mask, true);
  }
  formatTimezoneAndHour(offset) {
    const offsetAbsolute = Math.abs(offset);
    const timezone = (offset < 0 ? "+" : "-") + ("00" + Math.floor(offsetAbsolute / 60)).slice(-2) + ":" + ("00" + offsetAbsolute % 60).slice(-2);
    this.hour = "T00:00:00" + timezone;
  }
  applySizeBasedOnA11y() {
    const size = validateSizeFn(this._initialSize, PoFieldSize);
    this._size = size;
  }
  static ɵfac = function PoDatepickerBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoDatepickerBaseComponent)(ɵɵdirectiveInject(PoLanguageService), ɵɵdirectiveInject(ChangeDetectorRef));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoDatepickerBaseComponent,
    hostVars: 1,
    hostBindings: function PoDatepickerBaseComponent_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("PoUiThemeChange", function PoDatepickerBaseComponent_PoUiThemeChange_HostBindingHandler() {
          return ctx.onThemeChange();
        }, ɵɵresolveWindow);
      }
      if (rf & 2) {
        ɵɵattribute("p-size", ctx.size);
      }
    },
    inputs: {
      additionalHelpEventTrigger: "additionalHelpEventTrigger",
      label: [0, "p-label", "label"],
      help: [0, "p-help", "help"],
      mode: [0, "p-mode", "mode"],
      yearRangeLimit: [0, "p-year-range-limit", "yearRangeLimit"],
      additionalHelpTooltip: [0, "p-additional-help-tooltip", "additionalHelpTooltip"],
      autoFocus: [2, "p-auto-focus", "autoFocus", convertToBoolean],
      compactLabel: [1, "p-compact-label", "compactLabel"],
      errorAsync: [0, "p-error-async", "errorAsync"],
      name: "name",
      optional: [0, "p-optional", "optional"],
      errorPattern: [0, "p-error-pattern", "errorPattern"],
      errorLimit: [0, "p-error-limit", "errorLimit"],
      showErrorMessageRequired: [0, "p-required-field-error-message", "showErrorMessageRequired"],
      poHelperComponent: [1, "p-helper", "poHelperComponent"],
      labelTextWrap: [1, "p-label-text-wrap", "labelTextWrap"],
      noAutocomplete: [2, "p-no-autocomplete", "noAutocomplete", convertToBoolean],
      placeholder: [0, "p-placeholder", "placeholder"],
      setDisabled: [2, "p-disabled", "setDisabled", convertToBoolean],
      setReadonly: [2, "p-readonly", "setReadonly", convertToBoolean],
      setRequired: [2, "p-required", "setRequired", convertToBoolean],
      showRequired: [0, "p-show-required", "showRequired"],
      size: [0, "p-size", "size"],
      setClean: [2, "p-clean", "setClean", convertToBoolean],
      minDate: [0, "p-min-date", "minDate"],
      maxDate: [0, "p-max-date", "maxDate"],
      format: [0, "p-format", "format"],
      isoFormat: [0, "p-iso-format", "isoFormat"],
      locale: [0, "p-locale", "locale"],
      loading: [2, "p-loading", "loading", convertToBoolean],
      appendBox: [2, "p-append-in-body", "appendBox", convertToBoolean]
    },
    outputs: {
      additionalHelp: "p-additional-help",
      changeModel: "p-change-model",
      onblur: "p-blur",
      onchange: "p-change",
      keydown: "p-keydown"
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoDatepickerBaseComponent, [{
    type: Directive
  }], () => [{
    type: PoLanguageService
  }, {
    type: ChangeDetectorRef
  }], {
    additionalHelpEventTrigger: [{
      type: Input
    }],
    label: [{
      type: Input,
      args: ["p-label"]
    }],
    help: [{
      type: Input,
      args: ["p-help"]
    }],
    mode: [{
      type: Input,
      args: ["p-mode"]
    }],
    yearRangeLimit: [{
      type: Input,
      args: ["p-year-range-limit"]
    }],
    additionalHelpTooltip: [{
      type: Input,
      args: ["p-additional-help-tooltip"]
    }],
    autoFocus: [{
      type: Input,
      args: [{
        alias: "p-auto-focus",
        transform: convertToBoolean
      }]
    }],
    compactLabel: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-compact-label",
        required: false
      }]
    }],
    errorAsync: [{
      type: Input,
      args: ["p-error-async"]
    }],
    name: [{
      type: Input,
      args: ["name"]
    }],
    optional: [{
      type: Input,
      args: ["p-optional"]
    }],
    errorPattern: [{
      type: Input,
      args: ["p-error-pattern"]
    }],
    errorLimit: [{
      type: Input,
      args: ["p-error-limit"]
    }],
    showErrorMessageRequired: [{
      type: Input,
      args: ["p-required-field-error-message"]
    }],
    additionalHelp: [{
      type: Output,
      args: ["p-additional-help"]
    }],
    changeModel: [{
      type: Output,
      args: ["p-change-model"]
    }],
    onblur: [{
      type: Output,
      args: ["p-blur"]
    }],
    onchange: [{
      type: Output,
      args: ["p-change"]
    }],
    keydown: [{
      type: Output,
      args: ["p-keydown"]
    }],
    poHelperComponent: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-helper",
        required: false
      }]
    }],
    labelTextWrap: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-label-text-wrap",
        required: false
      }]
    }],
    noAutocomplete: [{
      type: Input,
      args: [{
        alias: "p-no-autocomplete",
        transform: convertToBoolean
      }]
    }],
    placeholder: [{
      type: Input,
      args: ["p-placeholder"]
    }],
    setDisabled: [{
      type: Input,
      args: [{
        alias: "p-disabled",
        transform: convertToBoolean
      }]
    }],
    setReadonly: [{
      type: Input,
      args: [{
        alias: "p-readonly",
        transform: convertToBoolean
      }]
    }],
    setRequired: [{
      type: Input,
      args: [{
        alias: "p-required",
        transform: convertToBoolean
      }]
    }],
    showRequired: [{
      type: Input,
      args: ["p-show-required"]
    }],
    size: [{
      type: Input,
      args: ["p-size"]
    }, {
      type: HostBinding,
      args: ["attr.p-size"]
    }],
    setClean: [{
      type: Input,
      args: [{
        alias: "p-clean",
        transform: convertToBoolean
      }]
    }],
    minDate: [{
      type: Input,
      args: ["p-min-date"]
    }],
    maxDate: [{
      type: Input,
      args: ["p-max-date"]
    }],
    format: [{
      type: Input,
      args: ["p-format"]
    }],
    isoFormat: [{
      type: Input,
      args: ["p-iso-format"]
    }],
    locale: [{
      type: Input,
      args: ["p-locale"]
    }],
    loading: [{
      type: Input,
      args: [{
        alias: "p-loading",
        transform: convertToBoolean
      }]
    }],
    appendBox: [{
      type: Input,
      args: [{
        alias: "p-append-in-body",
        transform: convertToBoolean
      }]
    }],
    onThemeChange: [{
      type: HostListener,
      args: ["window:PoUiThemeChange"]
    }]
  });
})();
var PoDatepickerLiterals = {
  en: {
    open: "Open calendar",
    clean: "Clear field"
  },
  es: {
    open: "Calendario abierto",
    clean: "Limpiar campo"
  },
  pt: {
    open: "Abrir calendário",
    clean: "Limpar campo"
  },
  ru: {
    open: "открытый календарь",
    clean: "Очистить поле"
  }
};
var poCalendarContentOffset$2 = 8;
var poCalendarPositionDefault$2 = "bottom-left";
var PoDatepickerComponent = class _PoDatepickerComponent extends PoDatepickerBaseComponent {
  controlPosition = inject(PoControlPositionService);
  renderer = inject(Renderer2);
  calendar;
  dialogPicker;
  iconDatepicker;
  inputEl;
  iconClean;
  helperEl;
  displayAdditionalHelp = false;
  el;
  hour;
  id = `po-datepicker[${uuid()}]`;
  visible = false;
  literals;
  eventListenerFunction;
  eventResizeListener;
  clickListener;
  dateRegex = new RegExp("^(?:[0-9])\\d{1}(?:[0-9])\\d{1}-(?:0[1-9]|1[0-2])-(?:0[1-9]|[12]\\d|3[01])$");
  isoRegex = new RegExp("^(?:[0-9])\\d{1}(?:[0-9])\\d{1}-(?:0[1-9]|1[0-2])-(?:0[1-9]|[12]\\d|3[01])T(?:[01]\\d|2[0-3]):[0-5]\\d:[0-5]\\d(?:Z|-0[1-9]|-1\\d|-2[0-3]|-00:?(?:0[1-9]|[0-5]\\d)|\\+[01]\\d|\\+2[0-3])(?:|:?[0-5]\\d)$");
  timeoutChange;
  valueBeforeChange;
  subscriptionValidator = new Subscription();
  get autocomplete() {
    return this.noAutocomplete ? "off" : "on";
  }
  get dateInputValue() {
    return this.inputEl.nativeElement.value;
  }
  constructor() {
    const languageService = inject(PoLanguageService);
    const cd = inject(ChangeDetectorRef);
    const el = inject(ElementRef);
    super(languageService, cd);
    this.languageService = languageService;
    this.cd = cd;
    this.shortLanguage = this.languageService.getShortLanguage();
    this.el = el;
    const language = languageService.getShortLanguage();
    this.literals = __spreadValues({}, PoDatepickerLiterals[language]);
  }
  onKeyup($event) {
    if (this.readonly || $event?.target !== this.inputEl?.nativeElement) {
      return;
    }
    this.objMask?.keyup($event);
    if (this.mode === "month-year") {
      this.handleMonthYearKeyup();
      return;
    }
    if (this.mode === "year") {
      this.handleYearKeyup();
      return;
    }
    if (this.objMask?.valueToModel || this.objMask?.valueToModel === "") {
      if (this.objMask?.valueToModel.length >= 10) {
        this.controlModel(this.getDateFromString(this.inputEl.nativeElement.value));
        this.date = this.getDateFromString(this.inputEl.nativeElement.value);
      } else {
        this.date = void 0;
        this.controlModel(this.date);
      }
    } else {
      this.date = void 0;
    }
  }
  onKeydown($event) {
    if (this.readonly) {
      return;
    }
    if ($event.key === "Escape" && this.visible) {
      this.togglePicker(false);
      $event.preventDefault();
      $event.stopPropagation();
    }
    if ($event.key === "Tab" && $event.shiftKey && $event.target instanceof HTMLInputElement && this.visible) {
      this.togglePicker();
    }
    if ($event?.target === this.inputEl?.nativeElement) {
      this.objMask?.keydown($event);
    }
  }
  ngAfterViewInit() {
    this.setDialogPickerStyleDisplay("none");
    if (this.autoFocus) {
      this.focus();
    }
    if (this.iconDatepicker?.buttonElement?.nativeElement) {
      this.renderer.setAttribute(this.iconDatepicker.buttonElement.nativeElement, "aria-label", this.literals.open);
    }
  }
  ngOnChanges(changes) {
    if (changes.label) {
      this.displayAdditionalHelp = false;
    }
  }
  ngOnDestroy() {
    this.subscriptionValidator?.unsubscribe();
    this.removeListeners();
  }
  emitAdditionalHelp() {
    if (this.label && this.isAdditionalHelpEventTriggered()) {
      this.additionalHelp.emit();
    }
  }
  /**
   * Função que atribui foco ao componente.
   *
   * Para utilizá-la é necessário ter a instância do componente no DOM, podendo ser utilizado o ViewChild da seguinte forma:
   *
   * ```
   * import { PoDatepickerComponent } from '@po-ui/ng-components';
   *
   * ...
   *
   * @ViewChild(PoDatepickerComponent, { static: true }) datepicker: PoDatepickerComponent;
   *
   * focusDatepicker() {
   *   this.datepicker.focus();
   * }
   * ```
   */
  focus() {
    if (!this.disabled && this.inputEl?.nativeElement) {
      this.inputEl.nativeElement.focus();
    }
  }
  getAdditionalHelpTooltip() {
    return this.isAdditionalHelpEventTriggered() ? null : this.additionalHelpTooltip;
  }
  togglePicker(focusInput = true) {
    if (this.disabled || this.readonly || !this.iconDatepicker?.buttonElement?.nativeElement) {
      return;
    }
    if (!this.visible) {
      this.visible = true;
      this.setCalendarPosition();
      this.initializeListeners();
      this.renderer.setAttribute(this.inputEl.nativeElement, "aria-expanded", "true");
      this.renderer.setAttribute(this.iconDatepicker.buttonElement.nativeElement, "aria-expanded", "true");
    } else {
      this.inputEl.nativeElement.disabled = false;
      this.closeCalendar(focusInput);
      this.renderer.removeAttribute(this.inputEl.nativeElement, "aria-expanded");
      this.renderer.removeAttribute(this.iconDatepicker.buttonElement.nativeElement, "aria-expanded");
    }
  }
  closeCalendar(focusInput = true) {
    this.visible = false;
    this.removeListeners();
    this.setDialogPickerStyleDisplay("none");
    if (!this.verifyMobile() && focusInput) {
      this.focus();
    }
    if (!focusInput && this.clean && this.inputEl.nativeElement.value) {
      setTimeout(() => {
        this.iconDatepicker.focus();
      }, 0);
      return;
    }
    requestAnimationFrame(() => {
      this.iconDatepicker?.buttonElement?.nativeElement?.focus();
    });
  }
  dateSelected(event) {
    if (event === "") {
      this.clear();
      setTimeout(() => this.closeCalendar(), 200);
      this.onchange.emit(void 0);
      return;
    }
    this.onTouchedModel?.();
    if (!this.verifyMobile()) {
      this.focus();
    }
    if (this.mode === "month-year" && event instanceof Date) {
      this.date = event;
      const separator = this.languageService.getDateSeparator(this.locale);
      const displayValue = ("0" + (event.getMonth() + 1)).slice(-2) + separator + event.getFullYear();
      this.inputEl.nativeElement.value = displayValue;
      const emitString = ("0" + (event.getMonth() + 1)).slice(-2) + "/" + event.getFullYear();
      this.callOnChange(emitString);
      this.controlChangeEmitter();
      this.togglePicker();
      return;
    }
    if (this.mode === "year" && event instanceof Date) {
      this.date = event;
      this.inputEl.nativeElement.value = `${event.getFullYear()}`;
      this.callOnChange(`${event.getFullYear()}`);
      this.controlChangeEmitter();
      this.togglePicker();
      return;
    }
    this.inputEl.nativeElement.value = this.formatToDate(this.date) || "";
    this.controlModel(this.date);
    this.controlChangeEmitter();
    this.togglePicker();
  }
  // Esconde Picker quando for clicado fora
  wasClickedOnPicker(event) {
    if (!this.dialogPicker || !this.iconDatepicker) {
      return;
    }
    if ((!this.dialogPicker.nativeElement.contains(event.target) || this.hasOverlayClass(event.target)) && !this.iconDatepicker.buttonElement.nativeElement.contains(event.target) && !this.hasAttrCalendar(event.target)) {
      this.closeCalendar();
    }
  }
  hasInvalidClass() {
    return this.el.nativeElement.classList.contains("ng-invalid") && this.el.nativeElement.classList.contains("ng-dirty") && (this.inputEl.nativeElement.value !== "" || this.showErrorMessageRequired && (this.required || this.hasValidatorRequired));
  }
  getErrorPattern() {
    return this.errorPattern !== "" && this.hasInvalidClass() ? this.errorPattern : "";
  }
  clear() {
    this.valueBeforeChange = this.formatToDate(this.date);
    this.date = void 0;
    this.inputEl.nativeElement.value = "";
    this.controlModel(this.date);
    this.controlChangeEmitter();
  }
  clearAndFocus() {
    this.clear();
    setTimeout(() => {
      this.focus();
    }, 200);
  }
  eventOnBlur($event) {
    this.onTouchedModel?.();
    this.objMask?.blur($event);
    this.onblur.emit();
    if (this.mode === "month-year") {
      this.handleMonthYearBlur();
      this.controlChangeEmitter();
      return;
    }
    if (this.mode === "year") {
      this.handleYearBlur();
      this.controlChangeEmitter();
      return;
    }
    const date = this.inputEl.nativeElement.value;
    const newDate = date ? this.getDateFromString(date) : void 0;
    if (this.objMask?.valueToModel) {
      if (this.objMask.valueToModel.length >= 10) {
        this.controlModel(newDate);
        this.date = newDate;
      } else {
        this.date = null;
        this.controlModel(this.date);
      }
    } else {
      this.date = void 0;
      this.callOnChange(this.date);
    }
    this.controlChangeEmitter();
  }
  eventOnClick($event) {
    if (this.verifyMobile()) {
      $event.target.blur();
      setTimeout(() => this.togglePicker(), 0);
    } else {
      this.objMask?.click($event);
    }
  }
  onKeyDown(event) {
    const isFieldFocused = document.activeElement === this.inputEl.nativeElement;
    if (isFieldFocused) {
      this.keydown.emit(event);
    }
  }
  onKeyPress(event) {
    if (event.key === "Tab" && event.shiftKey && !this.visible && this.clean && this.inputEl.nativeElement.value) {
      this.iconClean.nativeElement?.focus();
      event.preventDefault();
      return;
    }
    if (event.key === "Tab" && event.shiftKey && !this.visible) {
      this.focus();
      event.preventDefault();
      event.stopPropagation();
    }
  }
  formatToDate(value) {
    if (!value) {
      return void 0;
    }
    if (this.mode === "month-year" && typeof value === "string") {
      return value;
    }
    if (this.mode === "year" && (typeof value === "string" || typeof value === "number")) {
      return `${value}`;
    }
    if (!(value instanceof Date)) {
      return void 0;
    }
    let dateFormatted = PoUtils.replaceFormatSeparator(this.format, this.languageService.getDateSeparator(this.locale));
    dateFormatted = dateFormatted.replace("dd", ("0" + value.getDate()).slice(-2));
    dateFormatted = dateFormatted.replace("mm", ("0" + (value.getMonth() + 1)).slice(-2));
    dateFormatted = dateFormatted.replace("yyyy", PoUtils.formatYear(value.getFullYear()));
    return dateFormatted;
  }
  refreshValue(value) {
    if (value) {
      this.inputEl.nativeElement.value = this.formatToDate(value);
    }
  }
  /**
   * Método que exibe `p-helper` ou executa a ação definida em `p-helper{eventOnClick}` ou em `p-additionalHelp`.
   * Para isso, será necessário configurar uma tecla de atalho utilizando o evento `p-keydown`.
   *
   * > Exibe ou oculta o conteúdo do componente `po-helper` quando o componente estiver com foco.
   *
   * ```
   * // Exemplo com p-label e p-helper
   * <po-datepicker
   *  #datepicker
   *  ...
   *  p-label="Label do datepicker"
   *  [p-helper]="helperOptions"
   *  (p-keydown)="onKeyDown($event, datepicker)"
   * ></po-datepicker>
   * ```
   * ```
   * ...
   * onKeyDown(event: KeyboardEvent, inp: PoDatepickerComponent): void {
   *  if (event.code === 'F9') {
   *    inp.showAdditionalHelp();
   *  }
   * }
   * ```
   */
  showAdditionalHelp() {
    this.displayAdditionalHelp = !this.displayAdditionalHelp;
    const helper = this.poHelperComponent();
    const isHelpEvt = this.isAdditionalHelpEventTriggered();
    if (!this.label && (helper || this.additionalHelpTooltip || isHelpEvt)) {
      if (isHelpEvt) {
        this.additionalHelp.emit();
      }
      if (typeof helper !== "string" && typeof helper?.eventOnClick === "function") {
        helper.eventOnClick();
        return;
      }
      if (this.helperEl?.helperIsVisible()) {
        this.helperEl?.closeHelperPopover();
        return;
      }
      this.helperEl?.openHelperPopover();
      return;
    }
    return this.displayAdditionalHelp;
  }
  handleSpecialModes(value) {
    if (this.mode === "month-year") {
      this.writeMonthYearValue(value);
      return true;
    }
    if (this.mode === "year") {
      this.writeYearValue(value);
      return true;
    }
    return false;
  }
  processValue(value) {
    if (value instanceof Date) {
      this.handleDateInstance(value);
      return;
    }
    if (this.isValidDateIso(value) || this.isValidExtendedIso(value)) {
      this.handleIsoString(value);
      return;
    }
    this.clearValue();
  }
  handleDateInstance(value) {
    const dateString = value.toString();
    this.hour = "T" + dateString.substring(16, 24) + dateString.substring(28, 31) + ":" + dateString.substring(31, 33);
    this.date = value;
    this.inputEl.nativeElement.value = this.formatToDate(value);
  }
  handleIsoString(value) {
    if (this.isValidExtendedIso(value)) {
      this.hour = value.substring(10, 25);
    }
    if (this.isoFormat === void 0) {
      this.isExtendedISO = this.isValidExtendedIso(value);
    }
    const day = parseInt(value.substring(8, 10), 10);
    const month = parseInt(value.substring(5, 7), 10) - 1;
    const year = parseInt(value.substring(0, 4), 10);
    const dateTemp = new Date(year, month, day);
    PoUtils.setYearFrom0To100(dateTemp, year);
    this.date = dateTemp;
    this.inputEl.nativeElement.value = this.formatToDate(dateTemp);
  }
  clearValue() {
    this.inputEl.nativeElement.value = "";
    this.date = void 0;
  }
  resetInput() {
    this.inputEl.nativeElement.value = "";
    this.date = void 0;
    this.callOnChange(this.date, false);
  }
  finalizeWrite() {
    this.controlModel(this.date);
    const dateModelFormatted = this.formatToDate(this.date);
    this.verifyErrorAsync(dateModelFormatted);
    this.valueBeforeChange = this.formatToDate(this.date);
  }
  // Função implementada do ControlValueAccessor
  writeValue(value) {
    if (!this.inputEl) {
      return;
    }
    if (!value) {
      this.resetInput();
      this.valueBeforeChange = this.formatToDate(this.date);
      return;
    }
    if (this.handleSpecialModes(value)) {
      return;
    }
    this.processValue(value);
    this.finalizeWrite();
  }
  isValidDateIso(value) {
    return this.dateRegex.test(value);
  }
  isValidExtendedIso(value) {
    return this.isoRegex.test(value);
  }
  hasOverlayClass(element) {
    return element.classList.contains("po-datepicker-calendar-overlay");
  }
  verifyErrorAsync(value) {
    if (this.errorPattern !== "" && this.errorAsync) {
      const errorAsync = this.errorAsync(value);
      if (isObservable(errorAsync)) {
        this.subscriptionValidator?.unsubscribe();
        this.subscriptionValidator = errorAsync.pipe(switchMap((error2) => {
          const element = this.el.nativeElement;
          if (error2) {
            element.classList.add("ng-invalid");
            element.classList.add("ng-dirty");
            this.cd.detectChanges();
          } else if (element.classList.contains("ng-invalid") && element.classList.contains("ng-dirty") && !this.isInvalid) {
            element.classList.remove("ng-invalid");
            this.cd.detectChanges();
          }
          return of("");
        })).subscribe();
      }
    }
  }
  /* istanbul ignore next */
  verifyMobile() {
    return isMobile();
  }
  handleMonthYearKeyup() {
    if (!(this.objMask?.valueToModel || this.objMask?.valueToModel === "")) {
      return;
    }
    const value = this.inputEl.nativeElement.value;
    if (value.length < 7) {
      this.callOnChange("");
      return;
    }
    this.applyMonthYearValue(value);
  }
  handleYearKeyup() {
    if (!(this.objMask?.valueToModel || this.objMask?.valueToModel === "")) {
      return;
    }
    const value = this.inputEl.nativeElement.value;
    if (value.length < 4) {
      this.callOnChange("");
      return;
    }
    this.applyYearValue(value);
  }
  handleMonthYearBlur() {
    const value = this.inputEl.nativeElement.value;
    if (!value) {
      this.callOnChange("");
      return;
    }
    this.applyMonthYearValue(value);
  }
  handleYearBlur() {
    const value = this.inputEl.nativeElement.value;
    if (!value) {
      this.callOnChange("");
      return;
    }
    this.applyYearValue(value, true);
  }
  applyMonthYearValue(value) {
    const separator = this.languageService.getDateSeparator(this.locale);
    const parsed = this.parseMonthYearInput(value, separator);
    if (parsed) {
      this.syncCalendarMonthYear(parsed);
      this.callOnChange(parsed);
    } else {
      this.callOnChange("Data inválida");
    }
  }
  applyYearValue(value, requireFullLength = false) {
    const year = parseInt(value, 10);
    if (!isNaN(year) && year > 0 && (!requireFullLength || value.length === 4)) {
      this.syncCalendarYear(year);
      this.callOnChange(`${year}`);
    } else {
      this.callOnChange("Data inválida");
    }
  }
  parseMonthYearInput(value, separator) {
    const parts = value.split(separator);
    if (parts.length !== 2) {
      return null;
    }
    const month = parseInt(parts[0], 10);
    const year = parseInt(parts[1], 10);
    if (isNaN(month) || isNaN(year) || month < 1 || month > 12 || year <= 0) {
      return null;
    }
    return ("0" + month).slice(-2) + "/" + year;
  }
  syncCalendarMonthYear(modelValue) {
    const parts = modelValue.split("/");
    if (parts.length === 2) {
      const month = parseInt(parts[0], 10);
      const year = parseInt(parts[1], 10);
      this.date = new Date(year, month - 1, 1);
    }
  }
  syncCalendarYear(year) {
    this.date = new Date(year, 0, 1);
  }
  writeMonthYearValue(value) {
    const separator = this.languageService.getDateSeparator(this.locale);
    let month;
    let year;
    if (value instanceof Date) {
      month = value.getMonth() + 1;
      year = value.getFullYear();
    } else if (typeof value === "string") {
      const normalizedValue = value.replace("-", "/");
      const parts = normalizedValue.split("/");
      if (parts.length === 2) {
        month = parseInt(parts[0], 10);
        year = parseInt(parts[1], 10);
      }
    }
    if (!isNaN(month) && !isNaN(year) && month >= 1 && month <= 12 && year > 0) {
      const displayValue = ("0" + month).slice(-2) + separator + year;
      this.inputEl.nativeElement.value = displayValue;
      const modelValue = ("0" + month).slice(-2) + "/" + year;
      this.syncCalendarMonthYear(modelValue);
      this.callOnChange(modelValue, false);
    } else {
      this.inputEl.nativeElement.value = "";
    }
    this.valueBeforeChange = this.inputEl.nativeElement.value;
  }
  writeYearValue(value) {
    const year = typeof value === "string" ? Number.parseInt(value, 10) : value;
    if (!Number.isNaN(year) && year > 0) {
      this.inputEl.nativeElement.value = `${year}`;
      this.syncCalendarYear(year);
      this.callOnChange(`${year}`, false);
    } else {
      this.inputEl.nativeElement.value = "";
    }
    this.valueBeforeChange = this.inputEl.nativeElement.value;
  }
  controlChangeEmitter() {
    if (this.mode === "month-year" || this.mode === "year") {
      const currentInputValue = this.inputEl?.nativeElement?.value || "";
      if (currentInputValue !== this.valueBeforeChange && this.isValidInputForMode(currentInputValue)) {
        this.valueBeforeChange = currentInputValue;
        clearTimeout(this.timeoutChange);
        this.timeoutChange = setTimeout(() => {
          this.onchange.emit(currentInputValue);
        }, 200);
      }
      return;
    }
    const dateModelFormatted = this.formatToDate(this.date);
    if (dateModelFormatted !== this.valueBeforeChange) {
      this.valueBeforeChange = dateModelFormatted;
      clearTimeout(this.timeoutChange);
      this.timeoutChange = setTimeout(() => {
        this.onchange.emit(dateModelFormatted);
        this.verifyErrorAsync(dateModelFormatted);
      }, 200);
    }
  }
  isValidInputForMode(value) {
    if (!value) {
      return true;
    }
    if (this.mode === "month-year") {
      const separator = this.languageService.getDateSeparator(this.locale);
      const parts = value.split(separator);
      if (parts.length !== 2) {
        return false;
      }
      const month = parseInt(parts[0], 10);
      const year = parseInt(parts[1], 10);
      return !isNaN(month) && !isNaN(year) && month >= 1 && month <= 12 && year > 0;
    }
    if (this.mode === "year") {
      const year = parseInt(value, 10);
      return !isNaN(year) && year > 0 && value.length === 4;
    }
    return true;
  }
  hasAttrCalendar(element) {
    const attrCalendar = "attr-calendar";
    return element && element.hasAttribute(attrCalendar) || element.parentElement && element.parentElement.hasAttribute(attrCalendar);
  }
  initializeListeners() {
    this.clickListener = this.renderer.listen("document", "click", (event) => {
      this.wasClickedOnPicker(event);
    });
    this.eventResizeListener = this.renderer.listen("window", "resize", () => {
      this.closeCalendar();
    });
    window.addEventListener("scroll", this.onScroll, true);
  }
  isAdditionalHelpEventTriggered() {
    return this.additionalHelpEventTrigger === "event" || this.additionalHelpEventTrigger === void 0 && this.additionalHelp.observed;
  }
  isFocusOnFirstCombo() {
    const first = this.dialogPicker.nativeElement.querySelector(".po-combo-first .po-combo-input");
    return first === document.activeElement;
  }
  isFocusOnLastCombo() {
    const last = this.dialogPicker.nativeElement.querySelector(".po-combo-last .po-combo-input");
    return last === document.activeElement;
  }
  onScroll = () => {
    this.controlPosition.adjustPosition(poCalendarPositionDefault$2);
  };
  removeListeners() {
    if (this.clickListener) {
      this.clickListener();
    }
    if (this.eventResizeListener) {
      this.eventResizeListener();
    }
    window.removeEventListener("scroll", this.onScroll, true);
  }
  setDialogPickerStyleDisplay(value) {
    if (this.dialogPicker && this.dialogPicker.nativeElement) {
      this.dialogPicker.nativeElement.style.display = value;
    }
  }
  setCalendarPosition() {
    this.setDialogPickerStyleDisplay("block");
    this.adjustCalendarPosition();
  }
  adjustCalendarPosition() {
    if (this?.dialogPicker.nativeElement && this.visible) {
      requestAnimationFrame(() => {
        const scrollHeight = this.dialogPicker.nativeElement.querySelector(".po-calendar")?.scrollHeight ?? this.dialogPicker.nativeElement.scrollHeight;
        const scrollWidth = this.dialogPicker.nativeElement.querySelector(".po-calendar")?.scrollWidth ?? this.dialogPicker.nativeElement.scrollWidth;
        this.dialogPicker.nativeElement.style.height = scrollHeight + "px";
        this.dialogPicker.nativeElement.style.width = scrollWidth + "px";
        this.dialogPicker.nativeElement.style.setProperty("--po-calendar-width", scrollWidth + "px");
        this.controlPosition.setElements(this.dialogPicker.nativeElement, poCalendarContentOffset$2, this.inputEl, ["top-left", "top-right", "bottom-left", "bottom-right"], false, true);
        this.controlPosition.adjustPosition(poCalendarPositionDefault$2);
      });
    }
  }
  handleCleanKeyboardTab(event) {
    if (this.shouldHandleTab(event)) {
      this.focusCalendar(event);
    }
  }
  // Determina se o tab deve abrir o listbox.
  shouldHandleTab(event) {
    return this.visible && !event.shiftKey;
  }
  focusCalendar(event) {
    if (!this.dialogPicker?.nativeElement) {
      return;
    }
    const firstCombo = this.dialogPicker.nativeElement.querySelector(".po-combo-first .po-combo-input");
    const monthOption = this.dialogPicker.nativeElement.querySelector(".po-calendar-months .po-button");
    const monthOptionSelected = this.dialogPicker.nativeElement.querySelector(".po-calendar-months .po-button-selected .po-button");
    const yearOptionSelected = this.dialogPicker.nativeElement.querySelector(".po-calendar-years .po-button-selected .po-button");
    const yearOptionEnabled = this.dialogPicker.nativeElement.querySelector(".po-calendar-years .po-button:not([disabled])");
    if (firstCombo) {
      event.preventDefault();
      firstCombo.focus();
    } else if (monthOptionSelected) {
      event.preventDefault();
      monthOptionSelected.focus();
    } else if (monthOption) {
      event.preventDefault();
      monthOption.focus();
    } else if (yearOptionSelected) {
      event.preventDefault();
      yearOptionSelected.focus();
    } else if (yearOptionEnabled) {
      event.preventDefault();
      yearOptionEnabled.focus();
    } else {
      this.togglePicker(false);
    }
  }
  onCalendarKeyDown(event) {
    if (!this.visible) return;
    if (event.key === "Escape" || event.key === "Tab" && event.shiftKey && this.isFocusOnFirstCombo()) {
      event.preventDefault();
      event.stopPropagation();
      this.iconDatepicker.buttonElement?.nativeElement.focus();
      this.closeCalendar(false);
    }
    if (event.key === "Tab" && event.shiftKey && this.isFocusOnLastCombo()) {
      const firstCombo = this.dialogPicker.nativeElement.querySelector(".po-combo-first .po-combo-input");
      firstCombo.focus();
      event.preventDefault();
      event.stopPropagation();
    }
  }
  setHelper(label, additionalHelpTooltip) {
    return setHelperSettings(label, additionalHelpTooltip, this.poHelperComponent(), this.size, this.isAdditionalHelpEventTriggered() ? this.additionalHelp : void 0);
  }
  static ɵfac = function PoDatepickerComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoDatepickerComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoDatepickerComponent,
    selectors: [["po-datepicker"]],
    viewQuery: function PoDatepickerComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c66, 7)(_c67, 5, ElementRef)(_c68, 5)(_c49, 7, ElementRef)(_c69, 5, ElementRef)(_c17, 5, PoHelperComponent);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.calendar = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.dialogPicker = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.iconDatepicker = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.inputEl = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.iconClean = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.helperEl = _t.first);
      }
    },
    hostBindings: function PoDatepickerComponent_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("keyup", function PoDatepickerComponent_keyup_HostBindingHandler($event) {
          return ctx.onKeyup($event);
        })("keydown", function PoDatepickerComponent_keydown_HostBindingHandler($event) {
          return ctx.onKeydown($event);
        });
      }
    },
    standalone: false,
    features: [ɵɵProvidersFeature([{
      provide: NG_VALUE_ACCESSOR,
      useExisting: forwardRef(() => _PoDatepickerComponent),
      multi: true
    }, {
      provide: NG_VALIDATORS,
      useExisting: forwardRef(() => _PoDatepickerComponent),
      multi: true
    }, PoControlPositionService]), ɵɵInheritDefinitionFeature, ɵɵNgOnChangesFeature],
    decls: 17,
    vars: 33,
    consts: [["outerContainer", ""], ["trigger", "cdkOverlayOrigin"], ["inp", ""], ["sharedCalendarContent", ""], ["iconDatepicker", ""], ["iconClean", ""], ["helperEl", ""], ["dialogPicker", ""], ["calendar", ""], [3, "p-disabled", "p-id", "p-label", "p-optional", "p-required", "p-show-required", "p-helper", "p-show-helper", "p-size", "p-text-wrap", "p-compact-label"], ["cdkOverlayOrigin", "", 1, "po-field-container-content", "po-field-container-content-datepicker"], [1, "po-field-container-input"], ["type", "text", 1, "po-input", "po-datepicker", 3, "blur", "click", "keydown", "keydown.tab", "autocomplete", "disabled", "id", "placeholder", "readonly", "required"], [3, "p-size"], [1, "po-field-icon-container-right"], [1, "po-field-helper-button", 3, "p-size", "p-helper", "p-disabled", "p-append-in-body"], ["cdkConnectedOverlay", "", 3, "cdkConnectedOverlayOrigin", "cdkConnectedOverlayOpen", "cdkConnectedOverlayDisableClose"], [3, "p-append-in-body", "p-help", "p-disabled", "p-error-pattern", "p-error-limit", "p-size"], ["p-icon", "ICON_CALENDAR_DOTS", "p-kind", "tertiary", 1, "po-datepicker-button", 3, "keydown", "keydown.tab", "p-click", "p-disabled", "p-size"], ["tabindex", "0", "role", "button", "p-icon", "ICON_FILL_CLEAR_CONTENT", 1, "po-icon-input", 3, "p-change-event", "keydown", "keydown.enter", "keydown.space", "p-element-ref", "p-size"], [1, "po-button-vertical-divider"], [4, "ngTemplateOutlet"], ["tabindex", "-1", 3, "hidden"], [1, "po-datepicker-calendar-overlay"], [3, "ngModelChange", "p-change", "p-change-month-year", "keydown", "p-close", "ngModel", "p-max-date", "p-min-date", "p-locale", "p-size", "p-mode", "p-year-range-limit"]],
    template: function PoDatepickerComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵdeclareLet(0);
        ɵɵelementStart(1, "po-field-container", 9)(2, "div", null, 0)(4, "div", 10, 1)(6, "div", 11)(7, "input", 12, 2);
        ɵɵlistener("blur", function PoDatepickerComponent_Template_input_blur_7_listener($event) {
          return ctx.eventOnBlur($event);
        })("click", function PoDatepickerComponent_Template_input_click_7_listener($event) {
          return ctx.eventOnClick($event);
        })("keydown", function PoDatepickerComponent_Template_input_keydown_7_listener($event) {
          return ctx.onKeyDown($event);
        })("keydown.tab", function PoDatepickerComponent_Template_input_keydown_tab_7_listener($event) {
          return ctx.handleCleanKeyboardTab($event);
        });
        ɵɵelementEnd();
        ɵɵconditionalCreate(9, PoDatepickerComponent_Conditional_9_Template, 1, 1, "po-loading-icon", 13)(10, PoDatepickerComponent_Conditional_10_Template, 4, 5, "div", 14);
        ɵɵelementEnd();
        ɵɵconditionalCreate(11, PoDatepickerComponent_Conditional_11_Template, 2, 4, "po-helper", 15);
        ɵɵelementEnd();
        ɵɵconditionalCreate(12, PoDatepickerComponent_Conditional_12_Template, 1, 3, null, 16)(13, PoDatepickerComponent_Conditional_13_Template, 1, 1, "ng-container");
        ɵɵtemplate(14, PoDatepickerComponent_ng_template_14_Template, 5, 13, "ng-template", null, 3, ɵɵtemplateRefExtractor);
        ɵɵelementEnd();
        ɵɵconditionalCreate(16, PoDatepickerComponent_Conditional_16_Template, 1, 6, "po-field-container-bottom", 17);
        ɵɵelementEnd();
      }
      if (rf & 2) {
        const inp_r8 = ɵɵreference(8);
        const poHelper_r9 = ɵɵstoreLet(ctx.setHelper(ctx.label, ctx.additionalHelpTooltip));
        ɵɵadvance();
        ɵɵclassProp("po-date-picker-container-disabled", ctx.isDisabled);
        ɵɵproperty("p-disabled", ctx.isDisabled)("p-id", ctx.id)("p-label", ctx.label)("p-optional", ctx.optional)("p-required", ctx.required)("p-show-required", ctx.showRequired)("p-helper", ctx.label ? poHelper_r9.helperSettings : void 0)("p-show-helper", ctx.label ? ctx.displayAdditionalHelp : void 0)("p-size", ctx.size)("p-text-wrap", ctx.labelTextWrap())("p-compact-label", ctx.compactLabel());
        ɵɵadvance(6);
        ɵɵclassProp("po-input-icon-right", ctx.clean && inp_r8.value && !ctx.isDisabled)("po-datepicker-field-disabled", ctx.isDisabled);
        ɵɵproperty("autocomplete", ctx.autocomplete)("disabled", ctx.isDisabled)("id", ctx.id)("placeholder", ctx.isDisabled ? "" : ctx.placeholder)("readonly", ctx.readonly)("required", ctx.required);
        ɵɵattribute("data-focused-element", !ctx.isDisabled)("data-inactive-component", ctx.isDisabled || ctx.readonly)("name", ctx.name)("p-size", ctx.size)("data-append-in-body", ctx.appendBox);
        ɵɵadvance(2);
        ɵɵconditional(ctx.loading ? 9 : 10);
        ɵɵadvance(2);
        ɵɵconditional(!ctx.label && ctx.poHelperComponent() || !ctx.label && poHelper_r9.hideAdditionalHelp ? 11 : -1);
        ɵɵadvance();
        ɵɵconditional(ctx.appendBox ? 12 : 13);
        ɵɵadvance(4);
        ɵɵconditional(!ctx.readonly ? 16 : -1);
      }
    },
    dependencies: [NgTemplateOutlet, NgControlStatus, NgModel, PoFieldContainerBottomComponent, PoFieldContainerComponent, CdkConnectedOverlay, CdkOverlayOrigin, PoCleanComponent, PoLoadingIconComponent, PoCalendarComponent, PoButtonComponent, PoHelperComponent],
    encapsulation: 2,
    changeDetection: 0
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoDatepickerComponent, [{
    type: Component,
    args: [{
      selector: "po-datepicker",
      providers: [{
        provide: NG_VALUE_ACCESSOR,
        useExisting: forwardRef(() => PoDatepickerComponent),
        multi: true
      }, {
        provide: NG_VALIDATORS,
        useExisting: forwardRef(() => PoDatepickerComponent),
        multi: true
      }, PoControlPositionService],
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: `@let poHelper = setHelper(label, additionalHelpTooltip);
<po-field-container
  [class.po-date-picker-container-disabled]="isDisabled"
  [p-disabled]="isDisabled"
  [p-id]="id"
  [p-label]="label"
  [p-optional]="optional"
  [p-required]="required"
  [p-show-required]="showRequired"
  [p-helper]="label ? poHelper.helperSettings : undefined"
  [p-show-helper]="label ? displayAdditionalHelp : undefined"
  [p-size]="size"
  [p-text-wrap]="labelTextWrap()"
  [p-compact-label]="compactLabel()"
>
  <div #outerContainer>
    <div
      cdkOverlayOrigin
      #trigger="cdkOverlayOrigin"
      class="po-field-container-content po-field-container-content-datepicker"
    >
      <div class="po-field-container-input">
        <input
          #inp
          type="text"
          class="po-input po-datepicker"
          [class.po-input-icon-right]="clean && inp.value && !isDisabled"
          [class.po-datepicker-field-disabled]="isDisabled"
          [attr.data-focused-element]="!isDisabled"
          [attr.data-inactive-component]="isDisabled || readonly"
          [attr.name]="name"
          [attr.p-size]="size"
          [autocomplete]="autocomplete"
          [disabled]="isDisabled"
          [id]="id"
          [placeholder]="isDisabled ? '' : placeholder"
          [readonly]="readonly"
          [required]="required"
          [attr.data-append-in-body]="appendBox"
          (blur)="eventOnBlur($event)"
          (click)="eventOnClick($event)"
          (keydown)="onKeyDown($event)"
          (keydown.tab)="handleCleanKeyboardTab($event)"
        />
        @if (loading) {
          <po-loading-icon [p-size]="mapSizeToIcon(size)"></po-loading-icon>
        } @else {
          <div class="po-field-icon-container-right">
            @if (clean && dateInputValue && !isDisabled && !readonly) {
              <po-clean
                #iconClean
                tabindex="0"
                role="button"
                [attr.aria-label]="literals.clean"
                class="po-icon-input"
                p-icon="ICON_FILL_CLEAR_CONTENT"
                [p-element-ref]="inputEl"
                [p-size]="size"
                (p-change-event)="clear()"
                (keydown)="handleCleanKeyboardTab($event)"
                (keydown.enter)="clearAndFocus(); $event.preventDefault()"
                (keydown.space)="clearAndFocus(); $event.preventDefault()"
              >
              </po-clean>
              <div class="po-button-vertical-divider"></div>
            }
            <po-button
              #iconDatepicker
              class="po-datepicker-button"
              [class.po-button-tertiary-danger]="getErrorPattern()"
              p-icon="ICON_CALENDAR_DOTS"
              p-kind="tertiary"
              [p-disabled]="isDisabled || readonly"
              [p-size]="size"
              (keydown)="onKeyPress($event)"
              (keydown.tab)="handleCleanKeyboardTab($event)"
              (p-click)="togglePicker(false)"
            >
            </po-button>
          </div>
        }
      </div>
      @if ((!this.label && poHelperComponent()) || (!this.label && poHelper.hideAdditionalHelp)) {
        <po-helper
          #helperEl
          class="po-field-helper-button"
          [p-size]="size"
          [p-helper]="poHelper.helperSettings"
          [p-disabled]="isDisabled"
          [p-append-in-body]="appendBox"
        >
        </po-helper>
      }
    </div>

    @if (appendBox) {
      <ng-template
        cdkConnectedOverlay
        [cdkConnectedOverlayOrigin]="trigger"
        [cdkConnectedOverlayOpen]="true"
        [cdkConnectedOverlayDisableClose]="true"
      >
        <ng-container *ngTemplateOutlet="sharedCalendarContent"></ng-container>
      </ng-template>
    } @else {
      <ng-container *ngTemplateOutlet="sharedCalendarContent"></ng-container>
    }

    <ng-template #sharedCalendarContent>
      <div #dialogPicker [class.po-datepicker-popup-calendar]="!verifyMobile()" tabindex="-1" [hidden]="!visible">
        @if (verifyMobile()) {
          <div class="po-datepicker-calendar-overlay"></div>
        }
        <po-calendar
          #calendar
          [class.po-datepicker-calendar-mobile]="verifyMobile()"
          [(ngModel)]="date"
          [p-max-date]="maxDate"
          [p-min-date]="minDate"
          [p-locale]="locale"
          [p-size]="size"
          [p-mode]="mode"
          [p-year-range-limit]="yearRangeLimit"
          (p-change)="dateSelected($event)"
          (p-change-month-year)="adjustCalendarPosition()"
          (keydown)="onCalendarKeyDown($event)"
          (p-close)="closeCalendar(false)"
        ></po-calendar>
      </div>
    </ng-template>
  </div>

  @if (!readonly) {
    <po-field-container-bottom
      [p-append-in-body]="appendBox"
      [p-help]="help"
      [p-disabled]="isDisabled"
      [p-error-pattern]="getErrorPattern()"
      [p-error-limit]="errorLimit"
      [p-size]="size"
    ></po-field-container-bottom>
  }
</po-field-container>
`
    }]
  }], () => [], {
    calendar: [{
      type: ViewChild,
      args: ["calendar", {
        static: true
      }]
    }],
    dialogPicker: [{
      type: ViewChild,
      args: ["dialogPicker", {
        read: ElementRef,
        static: false
      }]
    }],
    iconDatepicker: [{
      type: ViewChild,
      args: ["iconDatepicker"]
    }],
    inputEl: [{
      type: ViewChild,
      args: ["inp", {
        read: ElementRef,
        static: true
      }]
    }],
    iconClean: [{
      type: ViewChild,
      args: ["iconClean", {
        read: ElementRef
      }]
    }],
    helperEl: [{
      type: ViewChild,
      args: ["helperEl", {
        read: PoHelperComponent,
        static: false
      }]
    }],
    onKeyup: [{
      type: HostListener,
      args: ["keyup", ["$event"]]
    }],
    onKeydown: [{
      type: HostListener,
      args: ["keydown", ["$event"]]
    }]
  });
})();
var PoDatepickerModule = class _PoDatepickerModule {
  static ɵfac = function PoDatepickerModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoDatepickerModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoDatepickerModule,
    declarations: [PoDatepickerComponent],
    imports: [CommonModule, FormsModule, PoFieldContainerModule, OverlayModule, PoCleanModule, PoLoadingModule, PoCalendarModule, PoButtonModule, PoHelperModule],
    exports: [PoDatepickerComponent]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [CommonModule, FormsModule, PoFieldContainerModule, OverlayModule, PoCleanModule, PoLoadingModule, PoCalendarModule, PoButtonModule, PoHelperModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoDatepickerModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, FormsModule, PoFieldContainerModule, OverlayModule, PoCleanModule, PoLoadingModule, PoCalendarModule, PoButtonModule, PoHelperModule],
      exports: [PoDatepickerComponent],
      declarations: [PoDatepickerComponent]
    }]
  }], null, null);
})();
var PoColorPaletteService = class _PoColorPaletteService {
  getColor(value) {
    if (Object.values(PoColorPaletteEnum).includes(value.color) || Object.values(PoCaptionTagColorEnum).includes(value.color)) {
      return value.color;
    }
    return this.getColorFromType(value.type || value.color);
  }
  getColorFromType(type) {
    switch (type) {
      case "danger":
        return PoColorPaletteEnum.Color07;
      case "success":
        return PoColorPaletteEnum.Color11;
      case "warning":
        return PoColorPaletteEnum.Color08;
      default:
        return PoColorPaletteEnum.Color01;
    }
  }
  static ɵfac = function PoColorPaletteService_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoColorPaletteService)();
  };
  static ɵprov = ɵɵdefineInjectable({
    token: _PoColorPaletteService,
    factory: _PoColorPaletteService.ɵfac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoColorPaletteService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
var PoColorPaletteModule = class _PoColorPaletteModule {
  static ɵfac = function PoColorPaletteModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoColorPaletteModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoColorPaletteModule
  });
  static ɵinj = ɵɵdefineInjector({
    providers: [PoColorPaletteService]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoColorPaletteModule, [{
    type: NgModule,
    args: [{
      providers: [PoColorPaletteService],
      bootstrap: []
    }]
  }], null, null);
})();
var PoComponentInjectorService = class _PoComponentInjectorService {
  componentResolver;
  applicationRef;
  injector;
  constructor(componentResolver, applicationRef, injector) {
    this.componentResolver = componentResolver;
    this.applicationRef = applicationRef;
    this.injector = injector;
  }
  // Cria um dinamicamente no ApplicationRef
  createComponentInApplication(component) {
    const componentRef = this.componentResolver.resolveComponentFactory(component).create(this.injector);
    this.applicationRef.attachView(componentRef.hostView);
    const domElem = componentRef.hostView.rootNodes[0];
    document.body.appendChild(domElem);
    return componentRef;
  }
  // Destrói o componente e remove do ApplicationRef
  destroyComponentInApplication(componentRef) {
    this.applicationRef.detachView(componentRef.hostView);
    componentRef.destroy();
  }
  static ɵfac = function PoComponentInjectorService_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoComponentInjectorService)(ɵɵinject(ComponentFactoryResolver$1), ɵɵinject(ApplicationRef), ɵɵinject(Injector));
  };
  static ɵprov = ɵɵdefineInjectable({
    token: _PoComponentInjectorService,
    factory: _PoComponentInjectorService.ɵfac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoComponentInjectorService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: ComponentFactoryResolver$1
  }, {
    type: ApplicationRef
  }, {
    type: Injector
  }], null);
})();
var PoComponentInjectorModule = class _PoComponentInjectorModule {
  static ɵfac = function PoComponentInjectorModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoComponentInjectorModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoComponentInjectorModule
  });
  static ɵinj = ɵɵdefineInjector({
    providers: [PoComponentInjectorService]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoComponentInjectorModule, [{
    type: NgModule,
    args: [{
      declarations: [],
      imports: [],
      exports: [],
      providers: [PoComponentInjectorService],
      bootstrap: []
    }]
  }], null, null);
})();
var PoControlPositionModule = class _PoControlPositionModule {
  static ɵfac = function PoControlPositionModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoControlPositionModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoControlPositionModule
  });
  static ɵinj = ɵɵdefineInjector({
    providers: [PoControlPositionService]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoControlPositionModule, [{
    type: NgModule,
    args: [{
      providers: [PoControlPositionService]
    }]
  }], null, null);
})();
var poModalLiterals = {
  en: {
    close: "Close"
  },
  es: {
    close: "Cerrar"
  },
  pt: {
    close: "Fechar"
  },
  ru: {
    close: "Закрыть"
  }
};
var PoModalBaseComponent = class _PoModalBaseComponent {
  /** Título da modal. */
  title;
  /** Evento disparado ao fechar o modal. */
  closeModal = new EventEmitter();
  /**
   * Deve ser definido um objeto que implementa a interface `PoModalAction` contendo a label e a função da primeira ação.
   * Caso esta propriedade não seja definida ou esteja incompleta, automaticamente será adicionado um botão de ação com
   * a função de fechar a modal.
   */
  primaryAction;
  /** Deve ser definido um objeto que implementa a interface `PoModalAction` contendo a label e a função da segunda ação. */
  secondaryAction;
  language;
  literals;
  // Controla se a modal fica oculto ou visível, por padrão é oculto
  isHidden = true;
  // Event emmiter para quando a modal é fechada pelo 'X'.
  onXClosed = new EventEmitter();
  _componentsSize = void 0;
  _initialComponentsSize = void 0;
  _hideClose = false;
  _size = "md";
  /**
   * Define o tamanho da modal.
   *
   * Valores válidos:
   *  - `sm` (pequeno)
   *  - `md` (médio)
   *  - `lg` (grande)
   *  - `xl` (extra grande)
   *  - `auto` (automático)
   *
   * > Quando informado `auto` a modal calculará automaticamente seu tamanho baseado em seu conteúdo.
   * Caso não seja informado um valor, a modal terá o tamanho definido como `md`.
   *
   */
  set size(value) {
    const sizes = ["sm", "md", "lg", "xl", "auto"];
    this._size = sizes.indexOf(value) > -1 ? value : "md";
  }
  get size() {
    return this._size;
  }
  /**
   * Define o fechamento da modal ao clicar fora da mesma.
   * Informe o valor `true` para ativar o fechamento ao clicar fora da modal.
   */
  // eslint-disable-next-line @typescript-eslint/member-ordering
  clickOut = false;
  set setClickOut(value) {
    this.clickOut = value === "" ? false : convertToBoolean(value);
  }
  /**
   * @optional
   *
   * @description
   *
   * Define o tamanho dos componentes de formulário no modal:
   * - `small`: aplica a medida small de cada componente (disponível apenas para acessibilidade AA).
   * - `medium`: aplica a medida medium de cada componente.
   *
   * > Caso a acessibilidade AA não esteja configurada, o tamanho `medium` será mantido.
   * Para mais detalhes, consulte a documentação do [po-theme](https://po-ui.io/documentation/po-theme).
   *
   * @default `medium`
   */
  set componentsSize(value) {
    this._initialComponentsSize = value;
    this.applySizeBasedOnA11y();
  }
  get componentsSize() {
    return this._componentsSize ?? getDefaultSizeFn(PoFieldSize);
  }
  /**
   * @optional
   *
   * @description
   *
   * Oculta o ícone de fechar do cabeçalho da modal.
   *
   * > Caso a propriedade estiver habilitada, não será possível fechar a modal através da tecla *ESC*.
   *
   * @default `false`
   */
  set hideClose(value) {
    this._hideClose = convertToBoolean(value);
  }
  get hideClose() {
    return this._hideClose;
  }
  /**
   * @optional
   *
   * @description
   * Ícone exibido ao lado esquerdo do label do titúlo da modal.
   *
   * É possível usar qualquer um dos ícones da [Biblioteca de ícones](https://po-ui.io/icons). conforme exemplo abaixo:
   * ```
   * <po-modal p-icon="an an-user" p-title="PO Modal"></po-modal>
   * ```
   * Também é possível utilizar outras fontes de ícones, por exemplo a biblioteca *Font Awesome*, da seguinte forma:
   * ```
   * <po-modal p-icon="fa fa-podcast" p-title="PO Modal"></po-modal>
   * ```
   * Outra opção seria a customização do ícone através do `TemplateRef`, conforme exemplo abaixo:
   * ```
   * <po-modal [p-icon]="template" p-title="PO Modal"></po-modal>
   *
   * <ng-template #template>
   *  <ion-icon style="font-size: inherit" name="heart"></ion-icon>
   * </ng-template>
   * ```
   */
  icon;
  constructor(poLanguageService) {
    this.language = poLanguageService.getShortLanguage();
    this.literals = __spreadValues({}, poModalLiterals[this.language]);
  }
  /** Função para fechar a modal. */
  close(xClosed = false) {
    this.closeModal.emit();
    this.isHidden = true;
    if (xClosed) {
      this.onXClosed.emit(xClosed);
    }
  }
  /** Função para abrir a modal. */
  open() {
    this.validPrimaryAction();
    this.isHidden = false;
  }
  validPrimaryAction() {
    if (!this.primaryAction) {
      this.primaryAction = {
        action: () => this.close(),
        label: this.literals.close
      };
    }
    if (!this.primaryAction["action"]) {
      this.primaryAction["action"] = () => this.close();
    }
    if (!this.primaryAction["label"]) {
      this.primaryAction["label"] = this.literals.close;
    }
  }
  onThemeChange() {
    this.applySizeBasedOnA11y();
  }
  applySizeBasedOnA11y() {
    const size = validateSizeFn(this._initialComponentsSize, PoFieldSize);
    this._componentsSize = size;
  }
  static ɵfac = function PoModalBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoModalBaseComponent)(ɵɵdirectiveInject(PoLanguageService));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoModalBaseComponent,
    hostVars: 1,
    hostBindings: function PoModalBaseComponent_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("PoUiThemeChange", function PoModalBaseComponent_PoUiThemeChange_HostBindingHandler() {
          return ctx.onThemeChange();
        }, ɵɵresolveWindow);
      }
      if (rf & 2) {
        ɵɵattribute("p-components-size", ctx.componentsSize);
      }
    },
    inputs: {
      title: [0, "p-title", "title"],
      primaryAction: [0, "p-primary-action", "primaryAction"],
      secondaryAction: [0, "p-secondary-action", "secondaryAction"],
      size: [0, "p-size", "size"],
      setClickOut: [0, "p-click-out", "setClickOut"],
      componentsSize: [0, "p-components-size", "componentsSize"],
      hideClose: [0, "p-hide-close", "hideClose"],
      icon: [0, "p-icon", "icon"]
    },
    outputs: {
      closeModal: "p-close"
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoModalBaseComponent, [{
    type: Directive
  }], () => [{
    type: PoLanguageService
  }], {
    title: [{
      type: Input,
      args: ["p-title"]
    }],
    closeModal: [{
      type: Output,
      args: ["p-close"]
    }],
    primaryAction: [{
      type: Input,
      args: ["p-primary-action"]
    }],
    secondaryAction: [{
      type: Input,
      args: ["p-secondary-action"]
    }],
    size: [{
      type: Input,
      args: ["p-size"]
    }],
    setClickOut: [{
      type: Input,
      args: ["p-click-out"]
    }],
    componentsSize: [{
      type: Input,
      args: ["p-components-size"]
    }, {
      type: HostBinding,
      args: ["attr.p-components-size"]
    }],
    hideClose: [{
      type: Input,
      args: ["p-hide-close"]
    }],
    icon: [{
      type: Input,
      args: ["p-icon"]
    }],
    onThemeChange: [{
      type: HostListener,
      args: ["window:PoUiThemeChange"]
    }]
  });
})();
var PoModalFooterComponent = class _PoModalFooterComponent {
  /**
   * @optional
   *
   * @description
   *
   * Desabilita o alinhamento padrão, à direita, dos botões de ações que ficam no rodapé da [`PoModal`](/documentation/po-modal).
   *
   * > Caso a propriedade esteja habilitada, o alinhamento deverá ser a esquerda e pode ser personalizado.
   *
   * @default false
   */
  disabledAlign = false;
  static ɵfac = function PoModalFooterComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoModalFooterComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoModalFooterComponent,
    selectors: [["po-modal-footer"]],
    inputs: {
      disabledAlign: [0, "p-disabled-align", "disabledAlign"]
    },
    standalone: false,
    ngContentSelectors: _c12,
    decls: 2,
    vars: 2,
    consts: [[1, "po-modal-footer"]],
    template: function PoModalFooterComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵprojectionDef();
        ɵɵelementStart(0, "div", 0);
        ɵɵprojection(1);
        ɵɵelementEnd();
      }
      if (rf & 2) {
        ɵɵclassProp("po-modal-footer-align-right", !ctx.disabledAlign);
      }
    },
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoModalFooterComponent, [{
    type: Component,
    args: [{
      selector: "po-modal-footer",
      standalone: false,
      template: '<div class="po-modal-footer" [class.po-modal-footer-align-right]="!disabledAlign">\n  <ng-content></ng-content>\n</div>\n'
    }]
  }], null, {
    disabledAlign: [{
      type: Input,
      args: ["p-disabled-align"]
    }]
  });
})();
var PoActiveOverlayService = class _PoActiveOverlayService {
  activeOverlay = [];
  static ɵfac = function PoActiveOverlayService_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoActiveOverlayService)();
  };
  static ɵprov = ɵɵdefineInjectable({
    token: _PoActiveOverlayService,
    factory: _PoActiveOverlayService.ɵfac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoActiveOverlayService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
var PoModalComponent = class _PoModalComponent extends PoModalBaseComponent {
  poActiveOverlayService = inject(PoActiveOverlayService);
  modalContent;
  modalContainer;
  modalFooter;
  firstElement;
  focusFunction;
  focusableElements = "input, select, textarea, button:not([disabled]), a";
  id = uuid();
  sourceElement;
  constructor() {
    const poLanguageService = inject(PoLanguageService);
    super(poLanguageService);
  }
  close(xClosed = false) {
    if (this.poActiveOverlayService.activeOverlay.length > 0 && this.poActiveOverlayService.activeOverlay[this.poActiveOverlayService.activeOverlay.length - 1] === this.id) {
      this.poActiveOverlayService.activeOverlay.pop();
    }
    super.close(xClosed);
    this.removeEventListeners();
    if (this.sourceElement) {
      this.sourceElement.focus();
    }
  }
  closeModalOnEscapeKey(event) {
    if (!this.hideClose) {
      event.preventDefault();
      event.stopPropagation();
      this.close();
    }
  }
  getSecondaryActionButtonDanger() {
    return this.secondaryAction && this.secondaryAction.danger && !this.primaryAction.danger ? "true" : "false";
  }
  onClickOut(event) {
    if (this.clickOut && !this.modalContent.nativeElement.contains(event.target)) {
      this.close();
    }
  }
  open() {
    this.sourceElement = document.activeElement;
    super.open();
    this.handleFocus();
  }
  handleFocus() {
    this.poActiveOverlayService.activeOverlay.push(this.id);
    setTimeout(() => {
      if (this.modalContent) {
        this.initFocus();
        document.addEventListener("focus", this.focusFunction, true);
      }
    });
  }
  initFocus() {
    this.focusFunction = (event) => {
      const isCdkOverlayListbox = event.target.closest(".cdk-overlay-container") !== null;
      const modalElement = this.modalContent.nativeElement;
      if (!modalElement.contains(event.target) && !isCdkOverlayListbox && this.poActiveOverlayService.activeOverlay[this.poActiveOverlayService.activeOverlay.length - 1] === this.id) {
        event.stopPropagation();
        this.firstElement.focus();
      }
    };
    this.setFirstElement();
    if (this.hideClose) {
      this.firstElement.focus();
    } else {
      const firstFieldElement = this.modalContent.nativeElement.querySelectorAll(this.focusableElements)[1] || this.modalContent.nativeElement;
      firstFieldElement.focus();
    }
  }
  removeEventListeners() {
    document.removeEventListener("focus", this.focusFunction, true);
  }
  setFirstElement() {
    this.firstElement = this.modalContent.nativeElement.querySelector(this.focusableElements) || this.modalContent.nativeElement;
  }
  static ɵfac = function PoModalComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoModalComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoModalComponent,
    selectors: [["po-modal"]],
    contentQueries: function PoModalComponent_ContentQueries(rf, ctx, dirIndex) {
      if (rf & 1) {
        ɵɵcontentQuery(dirIndex, PoModalFooterComponent, 5);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.modalFooter = _t.first);
      }
    },
    viewQuery: function PoModalComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c70, 5, ElementRef)(_c71, 5, ElementRef);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.modalContent = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.modalContainer = _t.first);
      }
    },
    standalone: false,
    features: [ɵɵInheritDefinitionFeature],
    ngContentSelectors: _c73,
    decls: 1,
    vars: 1,
    consts: [["modalContainer", ""], ["modalContent", ""], ["tabindex", "0", 1, "po-modal"], ["tabindex", "0", 1, "po-modal", 3, "keydown.esc"], [1, "po-modal-overlay", 3, "mousedown"], [1, "po-modal-container", "po-pb-2", "po-pt-2"], [1, "po-modal-vertical-align"], ["tabindex", "-1"], [1, "po-modal-header"], [1, "po-modal-title", "po-text-ellipsis"], [3, "p-icon"], [1, "po-text-ellipsis"], ["p-icon", "ICON_CLOSE", "p-kind", "tertiary", 3, "p-aria-label", "p-size"], [1, "po-modal-body"], ["p-icon", "ICON_CLOSE", "p-kind", "tertiary", 3, "p-click", "p-aria-label", "p-size"], [1, "po-modal-footer-basic"], ["p-kind", "secondary", 3, "p-danger", "p-disabled", "p-icon", "p-label", "p-loading", "p-size"], ["p-kind", "primary", 1, "po-button-modal-first-action", 3, "p-click", "p-danger", "p-disabled", "p-icon", "p-label", "p-loading", "p-size"], ["p-kind", "secondary", 3, "p-click", "p-danger", "p-disabled", "p-icon", "p-label", "p-loading", "p-size"]],
    template: function PoModalComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵprojectionDef(_c72);
        ɵɵconditionalCreate(0, PoModalComponent_Conditional_0_Template, 17, 7, "div", 2);
      }
      if (rf & 2) {
        ɵɵconditional(!ctx.isHidden ? 0 : -1);
      }
    },
    dependencies: [PoButtonComponent, PoIconComponent, PoModalFooterComponent],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoModalComponent, [{
    type: Component,
    args: [{
      selector: "po-modal",
      standalone: false,
      template: '@if (!isHidden) {\n  <div #modalContainer class="po-modal" tabindex="0" (keydown.esc)="closeModalOnEscapeKey($event)">\n    <div class="po-modal-overlay" (mousedown)="onClickOut($event)"></div>\n    <div class="po-modal-container po-pb-2 po-pt-2">\n      <div class="po-modal-vertical-align">\n        <div #modalContent class="po-modal-content po-modal-{{ size }}" tabindex="-1">\n          <div class="po-modal-header">\n            <div class="po-modal-title po-text-ellipsis">\n              @if (icon) {\n                <po-icon [p-icon]="icon"></po-icon>\n              }\n              <div class="po-text-ellipsis">\n                {{ title }}\n              </div>\n            </div>\n            @if (!hideClose) {\n              <po-button\n                [p-aria-label]="literals.close"\n                p-icon="ICON_CLOSE"\n                (p-click)="close(true)"\n                p-kind="tertiary"\n                [p-size]="componentsSize"\n              >\n              </po-button>\n            }\n          </div>\n          <div class="po-modal-body">\n            <ng-content></ng-content>\n          </div>\n          @if (modalFooter) {\n            <ng-content select="po-modal-footer"></ng-content>\n          } @else {\n            <po-modal-footer>\n              <div class="po-modal-footer-basic">\n                @if (secondaryAction) {\n                  <po-button\n                    [p-danger]="getSecondaryActionButtonDanger()"\n                    [p-disabled]="secondaryAction.disabled"\n                    [p-icon]="secondaryAction.icon"\n                    [p-label]="secondaryAction.label"\n                    [p-loading]="secondaryAction.loading"\n                    p-kind="secondary"\n                    [p-size]="componentsSize"\n                    (p-click)="secondaryAction.action()"\n                  >\n                  </po-button>\n                }\n                <po-button\n                  class="po-button-modal-first-action"\n                  [p-danger]="primaryAction.danger"\n                  [p-disabled]="primaryAction.disabled"\n                  [p-icon]="primaryAction.icon"\n                  [p-label]="primaryAction.label"\n                  [p-loading]="primaryAction.loading"\n                  p-kind="primary"\n                  [p-size]="componentsSize"\n                  (p-click)="primaryAction.action()"\n                >\n                </po-button>\n              </div>\n            </po-modal-footer>\n          }\n        </div>\n      </div>\n    </div>\n  </div>\n}\n'
    }]
  }], () => [], {
    modalContent: [{
      type: ViewChild,
      args: ["modalContent", {
        read: ElementRef
      }]
    }],
    modalContainer: [{
      type: ViewChild,
      args: ["modalContainer", {
        read: ElementRef
      }]
    }],
    modalFooter: [{
      type: ContentChild,
      args: [PoModalFooterComponent]
    }]
  });
})();
var PoModalModule = class _PoModalModule {
  static ɵfac = function PoModalModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoModalModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoModalModule,
    declarations: [PoModalComponent, PoModalFooterComponent],
    imports: [CommonModule, PoButtonModule, PoIconModule],
    exports: [PoModalComponent, PoModalFooterComponent]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [CommonModule, PoButtonModule, PoIconModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoModalModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, PoButtonModule, PoIconModule],
      declarations: [PoModalComponent, PoModalFooterComponent],
      exports: [PoModalComponent, PoModalFooterComponent]
    }]
  }], null, null);
})();
var PoDialogType;
(function(PoDialogType2) {
  PoDialogType2[PoDialogType2["Alert"] = 0] = "Alert";
  PoDialogType2[PoDialogType2["Confirm"] = 1] = "Confirm";
})(PoDialogType || (PoDialogType = {}));
var poDialogAlertLiteralsDefault = {
  en: {
    ok: "Ok"
  },
  es: {
    ok: "Ok"
  },
  pt: {
    ok: "Ok"
  },
  ru: {
    ok: "Ок"
  }
};
var poDialogConfirmLiteralsDefault = {
  en: {
    cancel: "Cancel",
    confirm: "Confirm"
  },
  es: {
    cancel: "Cancelar",
    confirm: "Confirmar"
  },
  pt: {
    cancel: "Cancelar",
    confirm: "Confirmar"
  },
  ru: {
    cancel: "отменить",
    confirm: "подтвердить"
  }
};
var PoDialogComponent = class _PoDialogComponent {
  // ViewChild para o uso do po-modal.component
  poModal;
  // Title do poModal
  title;
  // Message do poModal
  message;
  // Objeto primary do poModal
  primaryAction = {
    label: "ok",
    action: () => {
    }
  };
  // Objeto secondary do poModal
  secondaryAction;
  // Callback executado ao fechar o poModal
  closeAction;
  // Literais usadas nos botão de alerta do poModal
  literalsAlert;
  // Literais usadas nos botões de confirmação do poModal
  literalsConfirm;
  // Atributo para armazenar a referencia do componente criado via serviço.
  componentRef;
  closeSubscription;
  _componentsSize = void 0;
  _initialComponentsSize = void 0;
  language;
  // Define o tamanho dos componentes de formulário no dialog.
  set componentsSize(value) {
    this._initialComponentsSize = value;
    this.applySizeBasedOnA11y();
  }
  get componentsSize() {
    return this._componentsSize ?? getDefaultSizeFn(PoFieldSize);
  }
  constructor(languageService) {
    this.language = languageService.getShortLanguage();
  }
  ngOnDestroy() {
    this.closeSubscription.unsubscribe();
  }
  ngOnInit() {
    this.closeSubscription = this.poModal.onXClosed.subscribe((close) => this.close(true));
  }
  onThemeChange() {
    this.applySizeBasedOnA11y();
  }
  // Fecha o poModal
  close(xClosed = false) {
    if (xClosed && this.closeAction) {
      this.closeAction();
    }
    this.poModal.close();
    this.destroy();
  }
  destroy() {
    if (this.componentRef) {
      this.componentRef.destroy();
    }
  }
  // Insere os valores recebidos de this.open para o poModal
  configDialog(primaryLabel, primaryAction, secondaryLabel, secondaryAction, closeAction) {
    this.primaryAction = {
      label: primaryLabel,
      action: () => {
        if (primaryAction) {
          primaryAction();
        }
        this.close();
      }
    };
    if (secondaryLabel) {
      this.secondaryAction = {
        label: secondaryLabel,
        action: () => {
          if (secondaryAction) {
            secondaryAction();
          }
          this.close();
        }
      };
    }
    this.closeAction = closeAction;
  }
  // Insere os valores recebidos de po-dialog.service de acordo com o tipo de diálago solicitado
  open(dialogOptions, dialogType, componentRef) {
    this.componentsSize = dialogOptions.componentsSize;
    this.title = dialogOptions.title;
    this.message = dialogOptions.message;
    this.componentRef = componentRef;
    this.setDialogLiterals(dialogOptions, dialogType);
    switch (dialogType) {
      case PoDialogType.Confirm: {
        this.configDialog(this.literalsConfirm.confirm, dialogOptions.confirm, this.literalsConfirm.cancel, dialogOptions.cancel, dialogOptions.close);
        break;
      }
      case PoDialogType.Alert: {
        this.configDialog(this.literalsAlert.ok, dialogOptions.ok);
        break;
      }
    }
    this.poModal.open();
  }
  setDialogLiterals(dialogOptions, dialogType) {
    const alertLiterals = poDialogAlertLiteralsDefault;
    const confirmLiterals = poDialogConfirmLiteralsDefault;
    const literals = dialogOptions.literals;
    if (dialogType === PoDialogType.Alert) {
      this.literalsAlert = __spreadValues(__spreadValues(__spreadValues({}, alertLiterals[poLocaleDefault]), alertLiterals[this.language]), literals);
    } else {
      this.literalsConfirm = __spreadValues(__spreadValues(__spreadValues({}, confirmLiterals[poLocaleDefault]), confirmLiterals[this.language]), literals);
    }
  }
  applySizeBasedOnA11y() {
    const size = validateSizeFn(this._initialComponentsSize, PoFieldSize);
    this._componentsSize = size;
  }
  static ɵfac = function PoDialogComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoDialogComponent)(ɵɵdirectiveInject(PoLanguageService));
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoDialogComponent,
    selectors: [["ng-component"]],
    viewQuery: function PoDialogComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(PoModalComponent, 7);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.poModal = _t.first);
      }
    },
    hostVars: 1,
    hostBindings: function PoDialogComponent_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("PoUiThemeChange", function PoDialogComponent_PoUiThemeChange_HostBindingHandler() {
          return ctx.onThemeChange();
        }, ɵɵresolveWindow);
      }
      if (rf & 2) {
        ɵɵattribute("p-components-size", ctx.componentsSize);
      }
    },
    inputs: {
      componentsSize: [0, "p-components-size", "componentsSize"]
    },
    standalone: false,
    decls: 2,
    vars: 5,
    consts: [[3, "p-title", "p-primary-action", "p-secondary-action", "p-components-size"], [3, "innerHtml"]],
    template: function PoDialogComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "po-modal", 0);
        ɵɵelement(1, "div", 1);
        ɵɵelementEnd();
      }
      if (rf & 2) {
        ɵɵproperty("p-title", ctx.title)("p-primary-action", ctx.primaryAction)("p-secondary-action", ctx.secondaryAction)("p-components-size", ctx.componentsSize);
        ɵɵadvance();
        ɵɵproperty("innerHtml", ctx.message, ɵɵsanitizeHtml);
      }
    },
    dependencies: [PoModalComponent],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoDialogComponent, [{
    type: Component,
    args: [{
      standalone: false,
      template: '<po-modal\n  [p-title]="title"\n  [p-primary-action]="primaryAction"\n  [p-secondary-action]="secondaryAction"\n  [p-components-size]="componentsSize"\n>\n  <div [innerHtml]="message"></div>\n</po-modal>\n'
    }]
  }], () => [{
    type: PoLanguageService
  }], {
    poModal: [{
      type: ViewChild,
      args: [PoModalComponent, {
        static: true
      }]
    }],
    componentsSize: [{
      type: Input,
      args: ["p-components-size"]
    }, {
      type: HostBinding,
      args: ["attr.p-components-size"]
    }],
    onThemeChange: [{
      type: HostListener,
      args: ["window:PoUiThemeChange"]
    }]
  });
})();
var PoDialogBaseService = class {
  /** Exibe um diálogo de confirmação, é possível definir ações para as opções de confirmação e cancelamento. */
  confirm(confirmOptions) {
    this.openDialog(PoDialogType.Confirm, confirmOptions);
  }
  /** Exibe um diálogo de alerta. */
  alert(alertOptions) {
    this.openDialog(PoDialogType.Alert, alertOptions);
  }
};
var PoDialogService = class _PoDialogService extends PoDialogBaseService {
  poComponentInjector;
  constructor(poComponentInjector) {
    super();
    this.poComponentInjector = poComponentInjector;
  }
  openDialog(dialogType, dialogOptions) {
    const componentRef = this.poComponentInjector.createComponentInApplication(PoDialogComponent);
    componentRef.instance.open(dialogOptions, dialogType, componentRef);
    componentRef.changeDetectorRef.detectChanges();
  }
  static ɵfac = function PoDialogService_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoDialogService)(ɵɵinject(PoComponentInjectorService));
  };
  static ɵprov = ɵɵdefineInjectable({
    token: _PoDialogService,
    factory: _PoDialogService.ɵfac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoDialogService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: PoComponentInjectorService
  }], null);
})();
var PoDialogModule = class _PoDialogModule {
  static ɵfac = function PoDialogModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoDialogModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoDialogModule,
    declarations: [PoDialogComponent],
    imports: [PoModalModule],
    exports: [PoDialogComponent]
  });
  static ɵinj = ɵɵdefineInjector({
    providers: [PoDialogService],
    imports: [PoModalModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoDialogModule, [{
    type: NgModule,
    args: [{
      declarations: [PoDialogComponent],
      imports: [PoModalModule],
      exports: [PoDialogComponent],
      providers: [PoDialogService]
    }]
  }], null, null);
})();
var PoI18nBasePipe = class {
  transform(value, args) {
    if (!value) {
      return "";
    }
    if (!(args instanceof Array)) {
      args = [args];
    }
    for (const arg of args) {
      value = value.replace(/(\{\w*\})+/, arg);
    }
    return value;
  }
};
var PoI18nPipe = class _PoI18nPipe extends PoI18nBasePipe {
  static ɵfac = /* @__PURE__ */ (() => {
    let ɵPoI18nPipe_BaseFactory;
    return function PoI18nPipe_Factory(__ngFactoryType__) {
      return (ɵPoI18nPipe_BaseFactory || (ɵPoI18nPipe_BaseFactory = ɵɵgetInheritedFactory(_PoI18nPipe)))(__ngFactoryType__ || _PoI18nPipe);
    };
  })();
  static ɵpipe = ɵɵdefinePipe({
    name: "poI18n",
    type: _PoI18nPipe,
    pure: true,
    standalone: false
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoI18nPipe, [{
    type: Pipe,
    args: [{
      name: "poI18n",
      standalone: false
    }]
  }], null, null);
})();
var PoMediaQueryService = class _PoMediaQueryService {
  renderer;
  constructor(rendererFactory) {
    this.renderer = rendererFactory.createRenderer(null, null);
  }
  /**
   * Método que replica as regras baseando-se pelos tokens de media query dentro das folhas de estilo do documento, permitindo a modificação dinâmica
   * dos valores CSS correspondentes aos tokens fornecidos.
   *
   * @param {PoMediaQueryTokens} tokens Objeto contendo os tokens que devem ser atualizados. Cada propriedade corresponde a uma variável CSS que será
   * dinamicamente modificada dentro das regras de media query.
   *
   */
  updateTokens(tokens2) {
    const styleSheets = document.styleSheets;
    const styleElement = this.createStyleElement();
    const dynamicSheet = styleElement.sheet;
    Array.from(styleSheets).forEach((sheet) => {
      if (sheet) {
        this.processStyleSheet(sheet, tokens2, dynamicSheet);
      }
    });
  }
  createStyleElement() {
    const styleElement = this.renderer.createElement("style");
    this.renderer.appendChild(document.head, styleElement);
    return styleElement;
  }
  processStyleSheet(sheet, tokens2, dynamicSheet) {
    const rules = sheet.cssRules;
    if (!rules) {
      console.warn("No rules found in stylesheet");
      return;
    }
    Array.from(rules).forEach((rule) => {
      if ("media" in rule) {
        this.updateTokensMediaRule(rule, tokens2, dynamicSheet);
      }
    });
  }
  updateTokensMediaRule(mediaRule, tokens2, dynamicSheet) {
    if (!dynamicSheet) {
      console.error("dynamicSheet is null or undefined. Cannot insert rule.");
      return;
    }
    const variablesInMediaRule = mediaRule.media.mediaText.match(/var\(--[^)]+\)/g) || [];
    if (variablesInMediaRule.length === 0) return;
    let updatedMediaQuery = "";
    variablesInMediaRule.forEach((variable) => {
      Object.entries(tokens2).forEach(([token, breakpoints]) => {
        if (breakpoints) {
          Object.entries(breakpoints).forEach(([breakpointVariable, value]) => {
            if (typeof value === "string") {
              const tokenToCompare = `var(--${breakpointVariable})`;
              if (tokenToCompare === variable) {
                updatedMediaQuery = this.buildMediaQuery(token, value, updatedMediaQuery);
              }
            }
          });
        }
      });
    });
    if (updatedMediaQuery) {
      const cssRules = Array.from(mediaRule.cssRules).map((rule) => rule.cssText).join(" ");
      dynamicSheet.insertRule(`${updatedMediaQuery} { ${cssRules} }`, dynamicSheet.cssRules.length);
    }
  }
  buildMediaQuery(token, value, existingQuery) {
    switch (true) {
      case (token.includes("sm") || token.includes("pull") || token.includes("offset")):
        return `@media (max-width: ${value})`;
      case token.includes("xl"):
        return `@media (min-width: ${value})`;
      case (token.includes("md") || token.includes("lg")):
        return existingQuery ? `${existingQuery} and (max-width: ${value})` : `@media (min-width: ${value})`;
      default:
        return existingQuery;
    }
  }
  static ɵfac = function PoMediaQueryService_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoMediaQueryService)(ɵɵinject(RendererFactory2));
  };
  static ɵprov = ɵɵdefineInjectable({
    token: _PoMediaQueryService,
    factory: _PoMediaQueryService.ɵfac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoMediaQueryService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: RendererFactory2
  }], null);
})();
var PoMediaQueryModule = class _PoMediaQueryModule {
  static ɵfac = function PoMediaQueryModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoMediaQueryModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoMediaQueryModule
  });
  static ɵinj = ɵɵdefineInjector({
    providers: [PoMediaQueryService]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoMediaQueryModule, [{
    type: NgModule,
    args: [{
      providers: [PoMediaQueryService],
      bootstrap: []
    }]
  }], null, null);
})();
var PoToasterMode;
(function(PoToasterMode2) {
  PoToasterMode2["Inline"] = "inline";
  PoToasterMode2["Alert"] = "alert";
})(PoToasterMode || (PoToasterMode = {}));
var PoToasterOrientation;
(function(PoToasterOrientation2) {
  PoToasterOrientation2[PoToasterOrientation2["Bottom"] = 0] = "Bottom";
  PoToasterOrientation2[PoToasterOrientation2["Top"] = 1] = "Top";
})(PoToasterOrientation || (PoToasterOrientation = {}));
var PoToasterType;
(function(PoToasterType2) {
  PoToasterType2["Error"] = "error";
  PoToasterType2["Information"] = "information";
  PoToasterType2["Success"] = "success";
  PoToasterType2["Warning"] = "warning";
})(PoToasterType || (PoToasterType = {}));
var poToasterLiterals = {
  en: {
    close: "Close"
  },
  es: {
    close: "Cerrar"
  },
  pt: {
    close: "Fechar"
  },
  ru: {
    close: "близко"
  }
};
var PoToasterBaseComponent = class _PoToasterBaseComponent {
  _isHide;
  _sizeActions = void 0;
  _initialSizeActions = void 0;
  /**
   * @optional
   *
   * @description
   *
   * Ação para a notificação.
   */
  action;
  /**
   * @optional
   *
   * @description
   *
   * Label do botão quando houver uma ação definida.
   */
  actionLabel;
  /**
   * @optional
   *
   * @description
   *
   * Mensagem a ser exibida na notificação.
   */
  message;
  /**
   * @optional
   *
   * @description
   *
   * Define se o Toaster esta invisivel.
   *
   * @default `false`
   */
  isHide = false;
  /**
   * @optional
   *
   * @description
   *
   * Evento emitido quando o valor de `isHide` é alterado.
   *
   */
  isHideChange = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Exibe botão de fechar no toaster modo inline.
   *
   * @default `true`
   */
  showClose = true;
  /**
   * @optional
   *
   * @description
   *
   * Define o tamanho das ações no componente:
   * - `small`: aplica a medida small de cada componente (disponível apenas para acessibilidade AA).
   * - `medium`: aplica a medida medium de cada componente.
   *
   * > Caso a acessibilidade AA não esteja configurada, o tamanho `medium` será mantido.
   * Para mais detalhes, consulte a documentação do [po-theme](https://po-ui.io/documentation/po-theme).
   *
   * @default `medium`
   */
  set sizeActions(value) {
    this._initialSizeActions = value;
    this.applySizeBasedOnA11y();
  }
  get sizeActions() {
    return this._sizeActions ?? getDefaultSizeFn(PoFieldSize);
  }
  /**
   * @optional
   *
   * @description
   *
   * Mensagem de suporte a ser exibida na notificação.
   */
  supportMessage;
  /**
   * @optional
   *
   * @description
   *
   * Determina o tipo de notificação.
   *
   * Valores aceitos: `error`, `information`, `success` e `warning`.
   * @see PoToasterType
   *
   * @default `PoToasterType.Information`
   */
  type = PoToasterType.Information;
  // Determina o modo do Toaster
  mode = PoToasterMode.Inline;
  // Orientação da notificação, a mesma pode ser exibida na parte superior ou inferior da página.
  orientation = PoToasterOrientation.Bottom;
  // ComponentRef
  componentRef;
  // Posição para notificação aparecer na tela.
  position;
  onThemeChange() {
    this.applySizeBasedOnA11y();
  }
  applySizeBasedOnA11y() {
    const size = validateSizeFn(this._initialSizeActions, PoFieldSize);
    this._sizeActions = size;
  }
  getInitialSizeActions() {
    return this._initialSizeActions;
  }
  static ɵfac = function PoToasterBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoToasterBaseComponent)();
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoToasterBaseComponent,
    hostVars: 1,
    hostBindings: function PoToasterBaseComponent_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("PoUiThemeChange", function PoToasterBaseComponent_PoUiThemeChange_HostBindingHandler() {
          return ctx.onThemeChange();
        }, ɵɵresolveWindow);
      }
      if (rf & 2) {
        ɵɵattribute("p-size-actions", ctx.sizeActions);
      }
    },
    inputs: {
      action: [0, "p-action", "action"],
      actionLabel: [0, "p-action-label", "actionLabel"],
      message: [0, "p-message", "message"],
      isHide: [2, "p-hide", "isHide", convertToBoolean],
      showClose: [2, "p-show-close", "showClose", convertToBoolean],
      sizeActions: [0, "p-size-actions", "sizeActions"],
      supportMessage: [0, "p-support-message", "supportMessage"],
      type: [0, "p-type", "type"],
      mode: [0, "p-mode", "mode"]
    },
    outputs: {
      isHideChange: "p-hide-change"
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoToasterBaseComponent, [{
    type: Directive
  }], null, {
    action: [{
      type: Input,
      args: ["p-action"]
    }],
    actionLabel: [{
      type: Input,
      args: ["p-action-label"]
    }],
    message: [{
      type: Input,
      args: ["p-message"]
    }],
    isHide: [{
      type: Input,
      args: [{
        alias: "p-hide",
        transform: convertToBoolean
      }]
    }],
    isHideChange: [{
      type: Output,
      args: ["p-hide-change"]
    }],
    showClose: [{
      type: Input,
      args: [{
        alias: "p-show-close",
        transform: convertToBoolean
      }]
    }],
    sizeActions: [{
      type: Input,
      args: ["p-size-actions"]
    }, {
      type: HostBinding,
      args: ["attr.p-size-actions"]
    }],
    supportMessage: [{
      type: Input,
      args: ["p-support-message"]
    }],
    type: [{
      type: Input,
      args: ["p-type"]
    }],
    mode: [{
      type: Input,
      args: ["p-mode"]
    }],
    onThemeChange: [{
      type: HostListener,
      args: ["window:PoUiThemeChange"]
    }]
  });
})();
var SPACE_BETWEEN_TOASTERS = 8;
var PoToasterComponent = class _PoToasterComponent extends PoToasterBaseComponent {
  changeDetector = inject(ChangeDetectorRef);
  renderer = inject(Renderer2);
  /* Componente toaster */
  toaster;
  buttonClose;
  alive = true;
  language;
  literals;
  /* Ícone do Toaster */
  icon;
  /* Margem do Toaster referênte à sua orientação e posição*/
  margin = 0;
  /* Observable para monitorar o Close to Toaster */
  observableOnClose = new Subject();
  /* Posição do Toaster*/
  toasterPosition = "po-toaster-bottom";
  /* Tipo do Toaster */
  toasterType;
  constructor() {
    const poLanguageService = inject(PoLanguageService);
    super();
    this.language = poLanguageService.getShortLanguage();
    this.literals = __spreadValues({}, poToasterLiterals[this.language]);
  }
  ngOnDestroy() {
    this.alive = false;
  }
  ngAfterViewInit() {
    setTimeout(() => {
      this.renderer.addClass(this.toaster.nativeElement, "po-toaster-visible");
      if (this.isInline()) {
        this.configToaster(this);
      }
    });
  }
  ngOnChanges(changes) {
    if (changes.isHide && changes.isHide.previousValue !== void 0) {
      if (changes.isHide.currentValue === true) {
        this.hide();
      } else {
        this.show();
      }
      this.changeDetector.detectChanges();
    }
  }
  /* Muda a posição do Toaster na tela*/
  changePosition(position) {
    setTimeout(() => {
      this.margin = SPACE_BETWEEN_TOASTERS;
      for (let i2 = 0; i2 < position; i2++) {
        this.margin += this.returnHeightToaster(i2) + SPACE_BETWEEN_TOASTERS;
      }
      if (this.orientation === PoToasterOrientation.Top) {
        this.toaster.nativeElement.style.top = this.margin + "px";
      } else {
        this.toaster.nativeElement.style.bottom = this.margin + "px";
      }
    });
  }
  /* Fecha o componente Toaster */
  close() {
    if (this.isAlert()) {
      this.observableOnClose.next(true);
    } else {
      this.hide();
    }
  }
  setFadeOut() {
    this.renderer.removeClass(this.toaster.nativeElement, "po-toaster-visible");
    this.renderer.addClass(this.toaster.nativeElement, "po-toaster-invisible");
  }
  setFadeIn() {
    this.renderer.removeClass(this.toaster.nativeElement, "po-toaster-invisible");
    this.renderer.addClass(this.toaster.nativeElement, "po-toaster-visible");
  }
  /* Configura o Toaster com os atributos passados para ele */
  configToaster(poToaster) {
    const sizeActions = poToaster === this ? this.getInitialSizeActions() : poToaster.sizeActions;
    this.type = poToaster.type;
    this.message = poToaster.message;
    this.orientation = poToaster.orientation;
    this.position = poToaster.position;
    this.action = poToaster.action;
    this.actionLabel = poToaster.actionLabel;
    this.componentRef = poToaster.componentRef;
    this.mode = poToaster.mode;
    this.sizeActions = sizeActions;
    this.showClose = poToaster.showClose;
    this.supportMessage = poToaster.supportMessage;
    if (this.orientation === PoToasterOrientation.Top) {
      this.toasterPosition = "po-toaster-top";
    }
    if (!this.mode) {
      this.mode = PoToasterMode.Inline;
    }
    if (!this.type || !Object.values(PoToasterType).includes(this.type)) {
      this.type = PoToasterType.Information;
    }
    this.changePosition(this.position);
    if (this.type === PoToasterType.Error) {
      this.toaster.nativeElement.setAttribute("role", "alert");
    } else if (this.action && this.actionLabel) {
      this.toaster.nativeElement.setAttribute("role", "alertdialog");
    } else {
      this.toaster.nativeElement.setAttribute("role", "status");
    }
    this.getToasterType();
    this.changeDetector.detectChanges();
    this.buttonClose?.buttonElement.nativeElement.setAttribute("aria-label", this.literals.close);
  }
  hasClose() {
    return this.isAlert() || this.showClose;
  }
  isAlert() {
    return this.mode === "alert";
  }
  isInline() {
    return this.mode === "inline";
  }
  getIcon() {
    return this.icon;
  }
  show() {
    this.isHide = true;
    this.setFadeIn();
    this.renderer.removeAttribute(this.toaster.nativeElement, "hidden");
  }
  hide() {
    this.isHide = true;
    this.setFadeOut();
    this.renderer.setAttribute(this.toaster.nativeElement, "hidden", "true");
  }
  getToasterPosition() {
    return this.toasterPosition;
  }
  getToasterType() {
    switch (this.type) {
      case PoToasterType.Error: {
        this.toasterType = "po-toaster-error";
        this.icon = "ICON_CLOSE";
        break;
      }
      case PoToasterType.Information: {
        this.toasterType = "po-toaster-info";
        this.icon = this.isAlert() ? "ICON_INFO" : "ICON_WARNING";
        break;
      }
      case PoToasterType.Success: {
        this.toasterType = "po-toaster-success";
        this.icon = "ICON_OK";
        break;
      }
      case PoToasterType.Warning: {
        this.toasterType = "po-toaster-warning";
        this.icon = this.isAlert() ? "ICON_EXCLAMATION" : "ICON_WARNING";
        break;
      }
    }
    return this.toasterType;
  }
  onButtonClose(event) {
    if (this.action && !this.actionLabel) {
      this.poToasterAction(event);
    } else {
      this.close();
    }
    if (this.isInline()) {
      this.isHideChange.emit(this.isHide);
    }
  }
  /* Chama a função passada pelo atributo `action` */
  poToasterAction(event) {
    this.action(this);
  }
  returnHeightToaster(position) {
    if (this.orientation === PoToasterOrientation.Top) {
      return document.querySelectorAll(".po-toaster-top")[position].offsetHeight;
    }
    return document.querySelectorAll(".po-toaster-bottom")[position].offsetHeight;
  }
  static ɵfac = function PoToasterComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoToasterComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoToasterComponent,
    selectors: [["po-toaster"]],
    viewQuery: function PoToasterComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c74, 5)(_c75, 5);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.toaster = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.buttonClose = _t.first);
      }
    },
    standalone: false,
    features: [ɵɵInheritDefinitionFeature, ɵɵNgOnChangesFeature],
    decls: 12,
    vars: 14,
    consts: [["toaster", ""], ["buttonClose", ""], [3, "hidden"], [1, "po-toaster-icon", 3, "ngClass"], [3, "p-icon"], [1, "po-toaster-inline-icon", 3, "p-icon"], [1, "po-toaster-actions"], [1, "po-toaster-message"], [1, "po-toaster-support-message"], [1, "po-toaster-action"], ["p-kind", "tertiary", 3, "p-label", "p-size"], [1, "po-toaster-close"], ["p-kind", "tertiary", 3, "p-click", "p-label", "p-size"], [1, "po-toaster-divider"], ["p-icon", "ICON_CLOSE", "p-kind", "tertiary", 1, "po-toaster-button-close", 3, "p-click", "p-size"]],
    template: function PoToasterComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "div", 2, 0)(2, "div", 3);
        ɵɵconditionalCreate(3, PoToasterComponent_Conditional_3_Template, 1, 1, "po-icon", 4);
        ɵɵelementEnd();
        ɵɵconditionalCreate(4, PoToasterComponent_Conditional_4_Template, 1, 1, "po-icon", 5);
        ɵɵelementStart(5, "div", 6)(6, "div", 7);
        ɵɵtext(7);
        ɵɵconditionalCreate(8, PoToasterComponent_Conditional_8_Template, 2, 1, "div", 8);
        ɵɵelementEnd();
        ɵɵelementStart(9, "div", 9);
        ɵɵconditionalCreate(10, PoToasterComponent_Conditional_10_Template, 1, 2, "po-button", 10);
        ɵɵelementEnd()();
        ɵɵconditionalCreate(11, PoToasterComponent_Conditional_11_Template, 4, 1, "div", 11);
        ɵɵelementEnd();
      }
      if (rf & 2) {
        ɵɵclassMap(ɵɵinterpolate2("\n    po-toaster \n    ", ctx.getToasterType(), " \n    ", ctx.getToasterPosition(), "\n  "));
        ɵɵclassProp("po-toaster-inline", ctx.isInline());
        ɵɵproperty("hidden", ctx.isHide);
        ɵɵadvance(2);
        ɵɵproperty("ngClass", ctx.isAlert() ? "po-toaster-icon-default" : "po-toaster-decoration");
        ɵɵadvance();
        ɵɵconditional(ctx.isAlert() ? 3 : -1);
        ɵɵadvance();
        ɵɵconditional(ctx.isInline() ? 4 : -1);
        ɵɵadvance(3);
        ɵɵtextInterpolate1(" ", ctx.message, " ");
        ɵɵadvance();
        ɵɵconditional(ctx.supportMessage ? 8 : -1);
        ɵɵadvance(2);
        ɵɵconditional(ctx.action && ctx.actionLabel ? 10 : -1);
        ɵɵadvance();
        ɵɵconditional(ctx.hasClose() ? 11 : -1);
      }
    },
    dependencies: [NgClass, PoIconComponent, PoButtonComponent],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoToasterComponent, [{
    type: Component,
    args: [{
      selector: "po-toaster",
      standalone: false,
      template: `<div
  #toaster
  class="
    po-toaster 
    {{ getToasterType() }} 
    {{ getToasterPosition() }}
  "
  [class.po-toaster-inline]="isInline()"
  [hidden]="isHide"
>
  <div class="po-toaster-icon" [ngClass]="isAlert() ? 'po-toaster-icon-default' : 'po-toaster-decoration'">
    @if (isAlert()) {
      <po-icon [p-icon]="getIcon()"></po-icon>
    }
  </div>

  @if (isInline()) {
    <po-icon class="po-toaster-inline-icon" [p-icon]="getIcon()"></po-icon>
  }

  <div class="po-toaster-actions">
    <div class="po-toaster-message">
      {{ message }}
      @if (supportMessage) {
        <div class="po-toaster-support-message">{{ supportMessage }}</div>
      }
    </div>

    <div class="po-toaster-action">
      @if (action && actionLabel) {
        <po-button
          (p-click)="poToasterAction($event)"
          [p-label]="actionLabel"
          p-kind="tertiary"
          [p-size]="sizeActions"
        ></po-button>
      }
    </div>
  </div>

  @if (hasClose()) {
    <div class="po-toaster-close">
      <div class="po-toaster-divider"></div>
      <po-button
        #buttonClose
        class="po-toaster-button-close"
        (p-click)="onButtonClose($event)"
        p-icon="ICON_CLOSE"
        p-kind="tertiary"
        [p-size]="sizeActions"
      ></po-button>
    </div>
  }
</div>
`
    }]
  }], () => [], {
    toaster: [{
      type: ViewChild,
      args: ["toaster"]
    }],
    buttonClose: [{
      type: ViewChild,
      args: ["buttonClose"]
    }]
  });
})();
var PoToasterModule = class _PoToasterModule {
  static ɵfac = function PoToasterModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoToasterModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoToasterModule,
    declarations: [PoToasterComponent],
    imports: [CommonModule, PoIconModule, PoButtonModule],
    exports: [PoToasterComponent]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [CommonModule, PoIconModule, PoButtonModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoToasterModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, PoIconModule, PoButtonModule],
      declarations: [PoToasterComponent],
      exports: [PoToasterComponent]
    }]
  }], null, null);
})();
var PoNotificationModule = class _PoNotificationModule {
  static ɵfac = function PoNotificationModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoNotificationModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoNotificationModule,
    imports: [CommonModule, PoButtonModule, PoIconModule, PoToasterModule]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [CommonModule, PoButtonModule, PoIconModule, PoToasterModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoNotificationModule, [{
    type: NgModule,
    args: [{
      declarations: [],
      imports: [CommonModule, PoButtonModule, PoIconModule, PoToasterModule],
      exports: [],
      providers: [],
      bootstrap: []
    }]
  }], null, null);
})();
var PoThemeTypeEnum;
(function(PoThemeTypeEnum2) {
  PoThemeTypeEnum2[PoThemeTypeEnum2["light"] = 0] = "light";
  PoThemeTypeEnum2[PoThemeTypeEnum2["dark"] = 1] = "dark";
})(PoThemeTypeEnum || (PoThemeTypeEnum = {}));
var poThemeDefaultAA = {
  perComponent: {},
  onRoot: {
    // #region COMMON
    "--outline-width": "var(--border-width-md)",
    "--outline-width-focus-visible": "var(--border-width-md)"
  }
};
var poThemeDefaultAAA = {
  perComponent: {},
  onRoot: {
    // #region FONT
    "--font-family": "Roboto",
    "--font-family-theme": "Roboto",
    "--font-family-theme-bold": "Roboto-Bold",
    "--font-family-theme-extra-light": "Roboto-Condensed-Light",
    "--font-family-heading": "Roboto",
    "--font-family-code": "Monospace"
  }
};
var poThemeDensity = {
  small: {
    "--default-spacing-0": "0",
    "--default-spacing-xs": "var(--spacing-xs)",
    "--default-spacing-sm": "var(--spacing-sm)",
    "--default-spacing-md": "var(--spacing-md)",
    "--default-spacing-lg": "var(--spacing-lg)",
    "--default-spacing-xl": "var(--spacing-xl)",
    "--default-line-height-xs": "var(--line-height-xs)",
    "--default-spacing-squish-xs": "var(--spacing-squish-xs)"
  }
};
var poThemeDefaultActions = {
  /** Cor padrão. */
  default: "var(--color-brand-01-base)",
  /** Cor ao passar o mouse. */
  hover: "var(--color-brand-01-dark)",
  /** Cor quando pressionado. */
  pressed: "var(--color-brand-01-darker)",
  /** Cor quando desabilitado. */
  disabled: "var(--color-neutral-light-30)",
  /** Cor ao focar. */
  focus: "var(--color-brand-01-darkest)"
};
var poThemeDefaultNeutrals = {
  /** Tons de cinza claro. */
  light: {
    "00": "#ffffff",
    "05": "#fbfbfb",
    "10": "#eceeee",
    "20": "#dadedf",
    "30": "#b6bdbf"
  },
  /** Tons de cinza intermediários. */
  mid: {
    "40": "#9da7a9",
    "60": "#6e7c7f"
  },
  /** Tons de cinza escuro. */
  dark: {
    "70": "#4a5c60",
    "80": "#2c3739",
    "90": "#1d2426",
    "95": "#0b0e0e"
  }
};
var poThemeDefaultFeedback = {
  /** Cores para feedback negativo. */
  negative: {
    lightest: "#f6e6e5",
    lighter: "#e3aeab",
    light: "#d58581",
    base: "#be3e37",
    dark: "#9b2d27",
    darker: "#72211d",
    darkest: "#4a1512"
  },
  /** Cores para feedback informativo. */
  info: {
    lightest: "#e3e9f7",
    lighter: "#b0c1e8",
    light: "#7996d7",
    base: "#23489f",
    dark: "#173782",
    darker: "#0f2557",
    darkest: "#081536"
  },
  /** Cores para feedback positivo. */
  positive: {
    lightest: "#def7ed",
    lighter: "#7ecead",
    light: "#41b483",
    base: "#107048",
    dark: "#0f5236",
    darker: "#083a25",
    darkest: "#002415"
  },
  /** Cores para feedback de aviso. */
  warning: {
    lightest: "#fcf6e3",
    lighter: "#f7dd97",
    light: "#f1cd6a",
    base: "#efba2a",
    dark: "#d8a20e",
    darker: "#705200",
    darkest: "#473400"
  }
};
var poThemeDefaultBrands = {
  "01": {
    lightest: "#f2eaf6",
    lighter: "#d9c2e5",
    light: "#bd94d1",
    base: "#753399",
    dark: "#5b1c7d",
    darker: "#400e58",
    darkest: "#260538"
  },
  "02": {
    base: "#b92f72"
  },
  "03": {
    base: "#ffd464"
  }
};
var poThemeDefaultCategoricals = {
  "01": "#003DCC",
  "02": "#669900",
  "03": "#6626A6",
  "04": "#D44E2A",
  "05": "#008599",
  "06": "#B17F00",
  "07": "#AC2076",
  "08": "#9F0712"
};
var poThemeDefaultOverlayCategoricals = {
  "01": "#99B8FF",
  "02": "#DDFF99",
  "03": "#CCACEC",
  "04": "#EEB9AA",
  "05": "#99F1FF",
  "06": "#FFE299",
  "07": "#EFA9D4",
  "08": "#FB9DA3"
};
var poThemeDefaultLightValues = {
  perComponent: {},
  onRoot: {
    /* CATEGORICAL COLORS */
    "--color-caption-categorical-01": "#003DCC",
    "--color-caption-categorical-02": "#669900",
    "--color-caption-categorical-03": "#6626A6",
    "--color-caption-categorical-04": "#D44E2A",
    "--color-caption-categorical-05": "#008599",
    "--color-caption-categorical-06": "#B17F00",
    "--color-caption-categorical-07": "#AC2076",
    "--color-caption-categorical-08": "#9F0712",
    /* CATEGORICAL OVERLAY COLORS */
    "--color-caption-categorical-overlay-01": "#99B8FF",
    "--color-caption-categorical-overlay-02": "#DDFF99",
    "--color-caption-categorical-overlay-03": "#CCACEC",
    "--color-caption-categorical-overlay-04": "#EEB9AA",
    "--color-caption-categorical-overlay-05": "#99F1FF",
    "--color-caption-categorical-overlay-06": "#FFE299",
    "--color-caption-categorical-overlay-07": "#EFA9D4",
    "--color-caption-categorical-overlay-08": "#FB9DA3"
  }
};
var poThemeDefaultActionsDark = {
  /** Cor padrão. */
  default: "var(--color-brand-01-dark)",
  /** Cor ao passar o mouse. */
  hover: "var(--color-brand-01-darker)",
  /** Cor quando pressionado. */
  pressed: "var(--color-brand-01-darkest)",
  /** Cor quando desabilitado. */
  disabled: "var(--color-neutral-mid-40)",
  /** Cor ao focar. */
  focus: "var(--color-brand-01-darkest)"
};
var poThemeDefaultNeutralsDark = {
  /** Tons de cinza claro. */
  light: {
    "00": "#1c1c1c",
    "05": "#202020",
    "10": "#2b2b2b",
    "20": "#3b3b3b",
    "30": "#5a5a5a"
  },
  /** Tons de cinza intermediários. */
  mid: {
    "40": "#7c7c7c",
    "60": "#a1a1a1"
  },
  /** Tons de cinza escuro. */
  dark: {
    "70": "#c1c1c1",
    "80": "#d9d9d9",
    "90": "#eeeeee",
    "95": "#fbfbfb"
  }
};
var poThemeDefaultFeedbackDark = {
  /** Cores para feedback negativo. */
  negative: {
    lightest: "#4a1512",
    lighter: "#72211d",
    light: "#9b2d27",
    base: "#be3e37",
    dark: "#d58581",
    darker: "#e3aeab",
    darkest: "#f6e6e5"
  },
  /** Cores para feedback informativo. */
  info: {
    lightest: "#081536",
    lighter: "#0f2557",
    light: "#173782",
    base: "#0079b8",
    dark: "#7996d7",
    darker: "#b0c1e8",
    darkest: "#e3e9f7"
  },
  /** Cores para feedback positivo. */
  positive: {
    lightest: "#002415",
    lighter: "#083a25",
    light: "#0f5236",
    base: "#107048",
    dark: "#41b483",
    darker: "#7ecead",
    darkest: "#def7ed"
  },
  /** Cores para feedback de aviso. */
  warning: {
    lightest: "#473400",
    lighter: "#705200",
    light: "#d8a20e",
    base: "#efba2a",
    dark: "#f1cd6a",
    darker: "#f7dd97",
    darkest: "#fcf6e3"
  }
};
var poThemeDefaultBrandsDark = {
  "01": {
    lightest: "#260538",
    lighter: "#400e58",
    light: "#5b1c7d",
    base: "#753399",
    dark: "#bd94d1",
    darker: "#d9c2e5",
    darkest: "#f2eaf6"
  },
  "02": {
    base: "#b92f72"
  },
  "03": {
    base: "#ffd464"
  }
};
var poThemeDefaultCategoricalsDark = {
  "01": "#003DCC",
  "02": "#669900",
  "03": "#6626A6",
  "04": "#D44E2A",
  "05": "#008599",
  "06": "#B17F00",
  "07": "#AC2076",
  "08": "#9F0712"
};
var poThemeDefaultCategoricalsOverlayDark = {
  "01": "#99B8FF",
  "02": "#DDFF99",
  "03": "#CCACEC",
  "04": "#EEB9AA",
  "05": "#99F1FF",
  "06": "#FFE299",
  "07": "#EFA9D4",
  "08": "#FB9DA3"
};
var poThemeDefaultDarkValues = {
  perComponent: {
    /** TAB */
    ".po-tab-border-active": {
      "background-color": "var(--color)"
    },
    /** OVERLAY */
    "po-overlay": {
      "--color-overlay": "var(--color-neutral-light-20)"
    },
    /** MODAL */
    "po-modal": {
      "--color-overlay": "var(--color-neutral-light-20)"
    },
    /** TOASTER */
    "po-toaster": {
      "--color-icon": "var(--color-neutral-dark-80)",
      "--color-icon-warning": "var(--color-neutral-light-00)"
    },
    /** BADGE */
    "po-badge": {
      "--color": "var(--color-neutral-dark-95)"
    },
    "po-badge[p-status=warning]": {
      "--color": "var(--color-neutral-light-00)"
    },
    // LINK: item visitado
    "po-link": {
      "--text-color-visited": "var(--color-action-default)"
    },
    // focus e outline: po-rich-text-body/ poinfo/ po-list-view/ po-stepper-circle (mudar no po-style)
    "po-rich-text-body .po-rich-text-body:focus-visible, po-info .po-info-link:focus-visible, po-list-view a.po-list-view-title-link:focus-visible, po-stepper-circle .po-stepper-circle:focus-visible": {
      "border-color": "var(--color-action-default);",
      "outline-color": "var(--color-action-focus);",
      "outline-width": "var(--outline-width-focus-visible);",
      "outline-style": "solid;",
      "outline-offset": "2px;"
    },
    // background container
    "po-container": {
      "--background": "var(--color-neutral-light-00);"
    },
    // Background input disabled
    "div.po-lookup-filter-content input.po-input, input.po-input, po-datepicker, po-datepicker-range, po-decimal, po-email, po-input, po-login, po-lookup, po-number, po-password, po-url, po-combo, po-search, po-select, po-multiselect, po-timepicker": {
      "--background-disabled": "var(--color-neutral-light-20);"
    },
    // autocomplete dos inputs (setar no po-style)
    "po-input input:-webkit-autofill, po-datepicker input:-webkit-autofill, po-datepicker-range input:-webkit-autofill, po-decimal input:-webkit-autofill, po-email input:-webkit-autofill, po-input input:-webkit-autofill, po-login input:-webkit-autofill, po-lookup input:-webkit-autofill, po-number input:-webkit-autofill, po-password input:-webkit-autofill, po-url input:-webkit-autofill, po-combo input:-webkit-autofill": {
      "-webkit-background-clip": "text",
      "-webkit-text-fill-color": "#ffffff",
      "transition": "background-color 5000s ease-in-out 0s",
      "box-shadow": "inset 0 0 20px 20px #23232329"
    },
    "po-overlay, po-page-slide": {
      "--color-overlay": "var(--color-neutral-light-05)"
    },
    /** SELECT */
    "po-select": {
      "--color-hover": "var(--color-action-hover);"
    },
    /** DROPDOWN */
    ".po-listbox-group-header .po-tag": {
      "color": "var(--color-neutral-light-00);",
      "background-color": "var(--color-action-default);"
    }
  },
  onRoot: {
    /* CORES LEGADAS */
    "--color-neutral": "var(--color-neutral-dark-70)",
    "--color-secondary": "var(--color-action-default)",
    "--color-secondary-light-20": "var(--color-brand-01-lighter)",
    "--color-secondary-light-40": "var(--color-brand-01-light)",
    "--color-secondary-dark-20": "var(--color-brand-01-dark)",
    "--color-secondary-dark-40": "var(--color-brand-01-darker)",
    "--color-secondary-dark-80": "var(--color-brand-01-darkest)",
    "--color-black-alpha-10": "rgba(255, 255, 255, 0.1)",
    "--color-black-alpha-15": "rgba(255, 255, 255, 0.15)",
    "--color-black-alpha-30": "rgba(255, 255, 255, 0.3)",
    "--color-po-user-guide-overlay": "var(--color-neutral-light-20)",
    "--color-primary-light-80": "color-mix(in srgb, var(--color-brand-02-base) 80%, black)",
    "--color-primary-light-95": "color-mix(in srgb, var(--color-brand-02-base) 95%, black)",
    "--color-primary-alpha-50": "color-mix(in srgb, var(--color-brand-02-base) 50%, white)",
    "--color-primary-dark-20": "color-mix(in srgb, var(--color-brand-02-base) 20%, white)",
    "--color-primary-dark-40": "color-mix(in srgb, var(--color-brand-02-base) 40%, white)",
    "--color-secondary-dark-60-alpha-70": "color-mix(in srgb, var(--color-neutral-mid-60) 70%, white)",
    "--color-tertiary-light-90": "color-mix(in srgb, var(--color-brand-03-base) 90%, black)",
    "--color-tertiary-dark-5": "color-mix(in srgb, var(--color-brand-03-base) 5%, white)",
    /* PAGE  */
    "--color-page-background-color-page": "var(--color-neutral-light-05)",
    /* TOOLBAR BADGE */
    "--color-toolbar-color-badge-text": "var(--color-neutral-dark-95)",
    /* POPOVER */
    "--shadow-popover-box-shadow": "0 0 4px 0 var(--color-neutral-light-20)",
    "--shadow-popover-box-shadow-arrow": "-1px -1px 1px 0 var(--color-neutral-light-20)",
    /* CALENDAR */
    "--color-calendar-background-color-box-background-range": "var(--color-interval)",
    /* STEPPER */
    "--color-stepper-circle-disabled": "var(--color-neutral-mid-40)",
    "--color-stepper-bar-disabled": "var(--color-neutral-mid-40)",
    /* CATEGORICAL COLORS */
    "--color-caption-categorical-01": "#003DCC",
    "--color-caption-categorical-02": "#669900",
    "--color-caption-categorical-03": "#6626A6",
    "--color-caption-categorical-04": "#D44E2A",
    "--color-caption-categorical-05": "#008599",
    "--color-caption-categorical-06": "#B17F00",
    "--color-caption-categorical-07": "#AC2076",
    "--color-caption-categorical-08": "#9F0712",
    /* CATEGORICAL OVERLAY COLORS */
    "--color-caption-categorical-overlay-01": "#99B8FF",
    "--color-caption-categorical-overlay-02": "#DDFF99",
    "--color-caption-categorical-overlay-03": "#CCACEC",
    "--color-caption-categorical-overlay-04": "#EEB9AA",
    "--color-caption-categorical-overlay-05": "#99F1FF",
    "--color-caption-categorical-overlay-06": "#FFE299",
    "--color-caption-categorical-overlay-07": "#EFA9D4",
    "--color-caption-categorical-overlay-08": "#FB9DA3"
  }
};
var poThemeDefaultCategoricalsDarkAA = {
  "01": "#003DCC",
  "02": "#669900",
  "03": "#6626A6",
  "04": "#D44E2A",
  "05": "#008599",
  "06": "#B17F00",
  "07": "#AC2076",
  "08": "#9F0712"
};
var poThemeDefaultCategoricalsOverlayDarkAA = {
  "01": "#99B8FF",
  "02": "#DDFF99",
  "03": "#CCACEC",
  "04": "#EEB9AA",
  "05": "#99F1FF",
  "06": "#FFE299",
  "07": "#EFA9D4",
  "08": "#FB9DA3"
};
var poThemeDefaultDarkValuesAA = {
  perComponent: poThemeDefaultDarkValues.perComponent,
  onRoot: __spreadProps(__spreadValues({}, poThemeDefaultDarkValues.onRoot), {
    /* CATEGORICAL COLORS */
    "--color-caption-categorical-01": "#003DCC",
    "--color-caption-categorical-02": "#669900",
    "--color-caption-categorical-03": "#6626A6",
    "--color-caption-categorical-04": "#D44E2A",
    "--color-caption-categorical-05": "#008599",
    "--color-caption-categorical-06": "#B17F00",
    "--color-caption-categorical-07": "#AC2076",
    "--color-caption-categorical-08": "#9F0712",
    /* CATEGORICAL OVERLAY COLORS */
    "--color-caption-categorical-overlay-01": "#99B8FF",
    "--color-caption-categorical-overlay-02": "#DDFF99",
    "--color-caption-categorical-overlay-03": "#CCACEC",
    "--color-caption-categorical-overlay-04": "#EEB9AA",
    "--color-caption-categorical-overlay-05": "#99F1FF",
    "--color-caption-categorical-overlay-06": "#FFE299",
    "--color-caption-categorical-overlay-07": "#EFA9D4",
    "--color-caption-categorical-overlay-08": "#FB9DA3"
  })
};
var poThemeDefaultCategoricalsAA = {
  "01": "#003DCC",
  "02": "#669900",
  "03": "#6626A6",
  "04": "#D44E2A",
  "05": "#008599",
  "06": "#B17F00",
  "07": "#AC2076",
  "08": "#9F0712"
};
var poThemeDefaultOverlayCategoricalsAA = {
  "01": "#99B8FF",
  "02": "#DDFF99",
  "03": "#CCACEC",
  "04": "#EEB9AA",
  "05": "#99F1FF",
  "06": "#FFE299",
  "07": "#EFA9D4",
  "08": "#FB9DA3"
};
var poThemeDefaultLightValuesAA = {
  perComponent: poThemeDefaultLightValues.perComponent,
  onRoot: __spreadProps(__spreadValues({}, poThemeDefaultLightValues.onRoot), {
    /* CATEGORICAL COLORS */
    "--color-caption-categorical-01": "#003DCC",
    "--color-caption-categorical-02": "#669900",
    "--color-caption-categorical-03": "#6626A6",
    "--color-caption-categorical-04": "#D44E2A",
    "--color-caption-categorical-05": "#008599",
    "--color-caption-categorical-06": "#B17F00",
    "--color-caption-categorical-07": "#AC2076",
    "--color-caption-categorical-08": "#9F0712",
    /* CATEGORICAL OVERLAY COLORS */
    "--color-caption-categorical-overlay-01": "#99B8FF",
    "--color-caption-categorical-overlay-02": "#DDFF99",
    "--color-caption-categorical-overlay-03": "#CCACEC",
    "--color-caption-categorical-overlay-04": "#EEB9AA",
    "--color-caption-categorical-overlay-05": "#99F1FF",
    "--color-caption-categorical-overlay-06": "#FFE299",
    "--color-caption-categorical-overlay-07": "#EFA9D4",
    "--color-caption-categorical-overlay-08": "#FB9DA3"
  })
};
var poThemeDefaultLight = {
  color: {
    brand: poThemeDefaultBrands,
    action: poThemeDefaultActions,
    neutral: poThemeDefaultNeutrals,
    feedback: poThemeDefaultFeedback,
    categorical: poThemeDefaultCategoricals,
    "categorical-overlay": poThemeDefaultOverlayCategoricals
  }
};
var poThemeDefaultLightAA = {
  color: __spreadProps(__spreadValues({}, poThemeDefaultLight.color), {
    categorical: poThemeDefaultCategoricalsAA,
    "categorical-overlay": poThemeDefaultOverlayCategoricalsAA
  })
};
var poThemeDefaultDark = {
  color: {
    brand: poThemeDefaultBrandsDark,
    action: poThemeDefaultActionsDark,
    neutral: poThemeDefaultNeutralsDark,
    feedback: poThemeDefaultFeedbackDark,
    categorical: poThemeDefaultCategoricalsDark,
    "categorical-overlay": poThemeDefaultCategoricalsOverlayDark
  }
};
var poThemeDefaultDarkAA = {
  color: __spreadProps(__spreadValues({}, poThemeDefaultDark.color), {
    categorical: poThemeDefaultCategoricalsDarkAA,
    "categorical-overlay": poThemeDefaultCategoricalsOverlayDarkAA
  })
};
var poThemeDefault = {
  name: "default",
  type: [{
    light: poThemeDefaultLight,
    dark: poThemeDefaultDark,
    a11y: PoThemeA11yEnum.AAA
  }, {
    light: poThemeDefaultLightAA,
    dark: poThemeDefaultDarkAA,
    a11y: PoThemeA11yEnum.AA
  }],
  active: {
    type: PoThemeTypeEnum.light,
    a11y: PoThemeA11yEnum.AAA
  }
};
var PoThemeService = class _PoThemeService {
  document;
  renderer;
  theme;
  constructor(document2, rendererFactory) {
    this.document = document2;
    this.renderer = rendererFactory.createRenderer(null, null);
    this.setDefaultBaseStyle();
  }
  /**
   * Aplica um tema ao componente de acordo com o tipo de tema e o nível de acessibilidade especificados.
   *
   * Este método configura o tema do componente com base no objeto `themeConfig` fornecido, no `themeType` e no `a11yLevel`.
   * Além disso, ele pode opcionalmente salvar a preferência de tema no localStorage, se solicitado.
   *
   * @param {PoTheme} themeConfig - Configuração de tema a ser aplicada ao componente.
   * @param {PoThemeTypeEnum} [themeType=PoThemeTypeEnum.light] - (Opcional) Tipo de tema, podendo ser 'light' (claro) ou 'dark' (escuro). O tema claro é o padrão.
   * @param {PoThemeA11yEnum} [a11yLevel=PoThemeA11yEnum.AAA] - (Opcional) Nível de acessibilidade dos componentes, podendo ser AA ou AAA. Padrão é AAA.
   * @param {boolean} [persistPreference=true] - (Opcional) Define se a preferência de tema deve ser salva no
   * localStorage para persistência. Por padrão é `true`, ou seja, a preferência será salva automaticamente.
   */
  setTheme(themeConfig, themeType = PoThemeTypeEnum.light, a11yLevel = PoThemeA11yEnum.AAA, persistPreference = true) {
    if (themeConfig === poThemeDefault) {
      this.resetBaseTheme();
    }
    this.formatTheme(themeConfig, themeType, a11yLevel);
    const _themeActive = Array.isArray(themeConfig.type) && themeConfig.type.length >= 1 ? themeConfig.type.find((e2) => e2.a11y === a11yLevel) : themeConfig.type;
    const _themeType = _themeActive[PoThemeTypeEnum[themeType]];
    if (!_themeType) {
      return;
    }
    const colorStyles = _themeType.color ? this.generateThemeStyles(_themeType.color) : "";
    const perComponentStyles = _themeType.perComponent ? this.generatePerComponentStyles(_themeType.perComponent) : "";
    const onRootStyles = _themeType.onRoot ? this.generateAdditionalStyles(_themeType.onRoot) : "";
    const additionalStyles = this.generateAdditionalStyles(_themeType);
    const combinedStyles = `
      :root.${themeConfig.name}-${PoThemeTypeEnum[themeType]}-${a11yLevel} {
        ${colorStyles}
        ${perComponentStyles}
        ${onRootStyles}
        ${additionalStyles}
      }`;
    this.applyThemeStyles(combinedStyles);
    const defaultSize = localStorage.getItem("po-default-size");
    this.setDataDefaultSizeHTML(defaultSize, a11yLevel);
    document.documentElement.setAttribute("data-a11y", a11yLevel === PoThemeA11yEnum.AAA ? "AAA" : "AA");
    this.changeThemeType(themeConfig, persistPreference);
    this.dispatchEvent(themeConfig);
    const densityMode = localStorage.getItem("po-density-mode");
    this.setDensityMode(densityMode);
  }
  setDataDefaultSizeHTML(size, a11yLevel) {
    if (size === "small" && a11yLevel === PoThemeA11yEnum.AA) {
      document.documentElement.setAttribute("data-default-size", size);
    }
  }
  /**
   * Retorna o nível de acessibilidade configurado no tema.
   * Se não estiver configurado, retorna `AAA` como padrão.
   * @returns {PoThemeA11yEnum} O nível de acessibilidade, que pode ser `AA` ou `AAA`.
   */
  getA11yLevel() {
    return getA11yLevel();
  }
  /**
   * Define o tamanho `small` como padrão para componentes que não possuem um tamanho definido. Essa configuração é
   * aplicada globalmente apenas quando o nível de acessibilidade for `AA`. O valor definido é salvo no
   * `localStorage` sob a chave `po-default-size` e o atributo `data-default-size` é adicionado ao elemento HTML
   * para que os componentes possam aplicar o tamanho
   *
   * Exemplo de uso:
   *
   * ```typescript
   * import { poThemeDefault, PoThemeService, PoThemeTypeEnum, PoThemeA11yEnum } from '@po-ui/ng-components';
   *
   * private themeService = inject(PoThemeService);
   *
   * constructor() {
   *  this.themeService.setA11yDefaultSizeSmall(true);
   *  this.themeService.setTheme(poThemeDefault, PoThemeTypeEnum.light, PoThemeA11yEnum.AA);
   * }
   * ```
   *
   * > Para garantir que o tamanho `small` seja aplicado corretamente a todos os componentes, recomendamos
   * definir esta configuração **junto com o nível de acessibilidade `AA` na inicialização da aplicação**.
   * > Para ajustar a densidade visual dos componentes agrupadores (como pages, container, etc.), utilize também
   * o método `setDensityMode` conforme necessário.
   *
   * @param {boolean} enable Habilita ou desabilita o tamanho `small` globalmente.
   */
  setA11yDefaultSizeSmall(enable) {
    const a11yLevel = this.getA11yLevel();
    if (!this.isValidA11yLevel(a11yLevel)) return false;
    const defaultSize = enable ? "small" : "medium";
    document.documentElement.setAttribute("data-default-size", defaultSize);
    if (a11yLevel === PoThemeA11yEnum.AA && enable) {
      if (localStorage.getItem("po-default-size") !== defaultSize) {
        localStorage.setItem("po-default-size", defaultSize);
      }
      return true;
    }
    localStorage.removeItem("po-default-size");
    return false;
  }
  /**
   * Retorna o modo de adensamento dos componentes agrupadores.
   * Se não estiver configurado, retorna `medium` como padrão.
   * @returns {PoDensityMode} O modo de adensamento, que pode ser `small` ou `medium`.
   */
  getDensityMode() {
    return PoUtils.getDensityMode();
  }
  /**
   * Aplica o modo de adensamento compacto (`small`) ou espaçoso (`medium`) para os componentes agrupadores,
   * independentemente do nível de acessibilidade. O valor definido é salvo no `localStorage` sob a chave
   * `po-density-mode`.
   *
   * @param {'small' | 'medium'} mode Define o modo de densidade: `small` para compacto, `medium` para espaçoso.
   * O valor padrão é `medium`.
   */
  setDensityMode(mode) {
    if (!Object.values(PoDensityMode).includes(mode)) {
      mode = PoDensityMode.Medium;
    }
    localStorage.setItem("po-density-mode", mode);
    const styleElement = document.head.querySelector("#baseStyle");
    if (mode === PoDensityMode.Small) {
      const onRootTokens = __spreadValues(__spreadValues({}, poThemeDefaultAA.onRoot), poThemeDensity.small);
      this.setPerComponentAndOnRoot(void 0, poThemeDefaultAA.perComponent, onRootTokens);
    } else {
      if (styleElement) {
        let css = styleElement.textContent;
        Object.keys(poThemeDensity.small).forEach((token) => {
          const regex = new RegExp(`${token}:\\s*[^;]+;`, "g");
          css = css.replace(regex, "");
        });
        styleElement.textContent = css;
      }
      this.setDefaultBaseStyle();
    }
  }
  /**
   * @docsPrivate
   * Retorna a preferência global de tamanho dos componentes.
   *
   * @returns `'small'` ou `'medium'`.
   */
  getA11yDefaultSize() {
    return getA11yDefaultSize();
  }
  /**
   * @docsPrivate
   *
   * Aplica estilos customizados para o componente e para o root HTML, utilizando os tokens definidos.
   *
   * Esse método é chamado para inserir ou atualizar estilos no DOM, aplicando tanto tokens de `onRoot` (ex: `--font-family: 'Roboto'`)
   * quanto estilos específicos de componentes (`perComponent`, como `po-listbox [hidden]: { display: 'flex !important' }`).
   *
   * O seletor CSS gerado leva em consideração o tema (`type`) e as configurações de acessibilidade (`a11y`) do tema ativo.
   * A classe do tema é aplicada no HTML e pode ser formatada como `html[class*="-light-AA"]` para personalizações
   * em temas específicos.
   *
   * @param {PoThemeActive} active - Configuração do tema ativo:
   * @param {any} perComponent - Objeto contendo os estilos específicos para componentes a serem aplicados.
   * @param {any} onRoot - Objeto contendo tokens de estilo que serão aplicados diretamente no seletor `:root` do HTML.
   * @param {string | Array<string>} [classPrefix] - Prefixo(s) de classe para direcionamento preciso.
   *
   * @example
   * #### 1. Com prefixo único
   * ```typescript
   * setPerComponentAndOnRoot(
   *   { type: 'dark', a11y: 'AA' },
   *   { 'po-input': { background: '#222' } },
   *   { '--text-color': '#fff' },
   *   'myTheme'
   * );
   * ```
   * **Saída:**
   * ```css
   * :root[class*="-dark-AA"][class*="myTheme"] {
   *   --text-color: #fff;
   *   po-input { background: #222; }
   * }
   * ```
   *
   * #### 2. Com múltiplos prefixos
   * ```typescript
   * setPerComponentAndOnRoot(
   *   { type: 'light' },
   *   null,
   *   { '--primary': '#3e8ed0' },
   *   ['myTheme', 'portal-v2']
   * );
   * ```
   * **Saída:**
   * ```css
   * :root[class*="-light"][class*="myTheme"],
   * :root[class*="-light"][class*="portal-v2"] {
   *   --primary: #3e8ed0;
   * }
   * ```
   *
   * - Quando usado com array, gera um seletor CSS com múltiplos targets (separados por vírgula).
   * - Mantém a especificidade original do tema (`[class*="-type-a11y"]`) combinada com cada prefixo.
   *
   */
  setPerComponentAndOnRoot(active, perComponent, onRoot, classPrefix) {
    const perComponentStyles = perComponent ? this.generatePerComponentStyles(perComponent) : "";
    const onRootStyles = onRoot ? this.generateAdditionalStyles(onRoot) : "";
    let selector2 = ":root";
    const typeSelector = active?.type !== void 0 ? `-${PoThemeTypeEnum[active.type]}` : "";
    const accessibilitySelector = active?.a11y !== void 0 ? `-${PoThemeA11yEnum[active.a11y]}` : "";
    if (typeSelector && accessibilitySelector) {
      selector2 += `[class*="${typeSelector}${accessibilitySelector}"]`;
    } else if (!typeSelector && accessibilitySelector) {
      selector2 += `[class$="${accessibilitySelector}"]`;
    } else if (typeSelector) {
      selector2 += `[class*="${typeSelector}"]`;
    }
    if (classPrefix) {
      if (Array.isArray(classPrefix)) {
        selector2 = classPrefix.map((prefix) => selector2 + `[class*="${prefix}"]`).join(", ");
      } else {
        selector2 += `[class*="${classPrefix}"]`;
      }
    }
    const styleCss = `
      ${selector2} {
        ${perComponentStyles}
        ${onRootStyles}
      }
    `;
    let styleElement = this.document.head.querySelector("#baseStyle");
    if (!styleElement) {
      styleElement = this.renderer.createElement("style");
      styleElement.id = "baseStyle";
      this.renderer.appendChild(styleElement, this.renderer.createText(styleCss));
      this.renderer.insertBefore(this.document.head, styleElement, this.document.head.firstChild);
    } else {
      if (!styleElement.textContent.includes(styleCss.trim())) {
        this.renderer.appendChild(styleElement, this.renderer.createText(styleCss));
      }
    }
  }
  /**
   * @docsPrivate
   *
   * Gera estilos adicionais com base nos tokens de tema fornecidos, excluindo os tokens de cor.
   * @param theme Os tokens de tema contendo os estilos adicionais a serem gerados.
   * @returns Uma string contendo os estilos adicionais formatados.
   */
  generateAdditionalStyles(theme2) {
    return Object.entries(theme2).filter(([key2]) => !["color", "perComponent", "onRoot"].includes(key2)).map(([key2, value]) => `${key2}: ${value};`).join(" ");
  }
  /**
   * @docsPrivate
   *
   * Aplica os estilos de tema ao documento.
   * @param styleCss Os estilos CSS a serem aplicados.
   */
  applyThemeStyles(styleCss) {
    const styleElement = this.createStyleElement(styleCss, "theme");
    const existingStyleElement = document.head.querySelector("#theme");
    if (existingStyleElement) {
      this.renderer.removeChild(document.head, existingStyleElement);
    }
    const existingBaseStyle = document.head.querySelector("#baseStyle");
    const referenceNode = existingBaseStyle ? existingBaseStyle.nextSibling : document.head.firstChild;
    this.renderer.insertBefore(document.head, styleElement, referenceNode);
  }
  changeThemeType(theme2, persistPreference = true) {
    this.cleanThemeActive(persistPreference);
    if (persistPreference) {
      this.setThemeLocal(theme2);
    }
    document.getElementsByTagName("html")[0].classList.add(...[`${theme2.name}-${PoThemeTypeEnum[this.getActiveTypeFromTheme(theme2.active)]}-${PoThemeA11yEnum[this.getActiveA11yFromTheme(theme2.active)]}`]);
  }
  /**
   * Restaura e aplica as preferências visuais do usuário para o tema da aplicação, garantindo que essas preferências
   * sejam persistidas no `localStorage` para uso em recarregamentos futuros.
   *
   * @returns {PoTheme} O tema atualmente aplicado.
   */
  persistThemeActive() {
    const _theme = this.getThemeActive();
    const activeA11y = this.getActiveA11yFromTheme(_theme.active);
    this.setTheme(_theme, this.getActiveTypeFromTheme(_theme.active), activeA11y);
    const defaultSize = this.getA11yDefaultSize();
    localStorage.setItem("po-default-size", defaultSize);
    this.setDataDefaultSizeHTML(defaultSize, activeA11y);
    const densityMode = localStorage.getItem("po-density-mode");
    localStorage.setItem("po-density-mode", densityMode);
    return _theme;
  }
  formatTheme(themeConfig, themeType, a11yLevel) {
    themeConfig.name = themeConfig.name.toLowerCase().replace(/[^a-zA-Z ]/g, "").replace(/\s+/g, "-");
    themeConfig.active = {
      type: themeType,
      a11y: a11yLevel
    };
    this.theme = themeConfig;
  }
  applyTheme(theme2) {
    const _localTheme = this.getThemeActive();
    if (!theme2) {
      if (_localTheme) {
        this.persistThemeActive();
        return _localTheme;
      }
      return void 0;
    }
    const _type = this.getActiveTypeFromTheme(theme2.active);
    const _accessibility = this.getActiveA11yFromTheme(theme2.active);
    if (_localTheme && JSON.stringify(_localTheme) === JSON.stringify(theme2)) {
      this.persistThemeActive();
      return _localTheme;
    }
    this.formatTheme(theme2, _type, _accessibility);
    this.setTheme(theme2, _type, _accessibility);
    return theme2;
  }
  /**
   * Altera o tipo do tema armazenado e aplica os novos estilos ao documento.
   *
   * Este método altera o tipo do tema armazenado ativo (light/dark)
   *
   * @param {PoThemeTypeEnum} themeType O tipo de tema a ser aplicado, light ou dark.
   */
  changeCurrentThemeType(type) {
    const _theme = this.getThemeActive();
    typeof _theme.active === "object" ? _theme.active.type = type : _theme.active = type;
    this.changeThemeType(_theme);
    this.dispatchEvent(_theme);
  }
  /**
   * Método remove o tema armazenado e limpa todos os estilos de tema
   * aplicados ao documento.
   *
   * @param {boolean} [persistPreference=true] - (Opcional) Define se a preferência de tema não deve ser mantida no localStorage para persistência. `true` para remover, `false` para manter.
   */
  cleanThemeActive(persistPreference = true) {
    const themeSuffixes = ["-light-", "-dark-"];
    const htmlElement = document.getElementsByTagName("html")[0];
    Array.from(htmlElement.classList).forEach((className) => {
      if (themeSuffixes.some((suffix) => className.includes(suffix))) {
        htmlElement.classList.remove(className);
      }
    });
    if (persistPreference) {
      localStorage.removeItem("totvs-theme");
    }
  }
  getActiveTypeFromTheme(active) {
    return typeof active === "object" ? active.type : active;
  }
  getActiveA11yFromTheme(active) {
    return typeof active === "object" ? active.a11y : PoThemeA11yEnum.AAA;
  }
  isValidA11yLevel(level) {
    return level === PoThemeA11yEnum.AA || level === PoThemeA11yEnum.AAA;
  }
  /**
   * @docsPrivate
   *
   * Este método define um dados do tema e o armazena.
   * @param theme Os tokens de tema contendo os estilos adicionais a serem gerados.
   */
  setThemeLocal(theme2) {
    if (theme2) {
      localStorage.setItem("totvs-theme", JSON.stringify(theme2));
      this.theme = theme2;
    }
  }
  /**
   * Retorna o tema ativo como um observable. Este método funcionará apenas se o tema estiver armazenado no `localStorage`.
   *
   * @returns {PoTheme} Tema ativo.
   */
  getThemeActive() {
    try {
      const themeData = JSON.parse(localStorage.getItem("totvs-theme"));
      if (themeData && JSON.stringify(themeData) !== JSON.stringify(this.theme)) {
        this.theme = themeData;
      }
    } catch (error2) {
      console.error("Erro ao obter o tema do armazenamento local:", error2);
    }
    return this.theme;
  }
  /**
   * @docsPrivate
   *
   * Gera estilos CSS com base nos tokens de cores fornecidos.
   * @param css Os tokens de cor a serem usados para gerar os estilos.
   * @param id id do style a ser aplicado.
   * @returns Uma string contendo os estilos CSS gerados.
   */
  createStyleElement(css, id) {
    const styleElement = this.renderer.createElement("style");
    styleElement.id = id;
    this.renderer.appendChild(styleElement, this.renderer.createText(css));
    return styleElement;
  }
  /**
   * @docsPrivate
   *
   * Gera estilos CSS com base nos tokens de cores fornecidos.
   * @param themeColor Os tokens de cor a serem usados para gerar os estilos.
   * @returns Uma string contendo os estilos CSS gerados.
   */
  generateThemeStyles(themeColor2) {
    const selectBgIconStyle = this.getSelectBgIconsStyle(themeColor2);
    return [Object.entries(themeColor2).flatMap(([type, values]) => Object.entries(values).flatMap(([tonality, tonalityValues]) => {
      if (type === "action") {
        return [`--color-${type}-${tonality}: ${tonalityValues};`];
      } else if (type === "categorical" || type === "categorical-overlay") {
        return [`--color-caption-${type}-${tonality}: ${tonalityValues};`];
      } else {
        return Object.entries(tonalityValues).map(([level, colorValue]) => `--color-${type}-${tonality}-${level}: ${colorValue};`);
      }
    })).join(""), selectBgIconStyle].join("");
  }
  /**
   * @docsPrivate
   *
   * Gera estilos CSS com base nos tokens per Component fornecidos.
   * @param themePerComponent Os tokens de cor a serem usados para gerar os estilos.
   * @returns Uma string contendo os estilos CSS gerados.
   */
  generatePerComponentStyles(themePerComponent) {
    return Object.entries(themePerComponent).flatMap(([type, values]) => Object.entries(values).flatMap(([level, colorValue]) => [`${type} {${level}: ${colorValue};};`])).join("");
  }
  /**
   * Define o tema atual como o tema "PoUI Padrão".
   *
   * @param {PoThemeTypeEnum} type O tipo de Tema a ser aplicado, light / dark.
   */
  setDefaultTheme(type) {
    this.setTheme(poThemeDefault, type);
  }
  /**
   * @docsPrivate
   *
   * Retorna o estilo CSS para o fundo dos ícones do componente po-select, com base nas cores do tema.
   *
   * @param {PoThemeColor} themeColor - Objeto contendo as cores do tema.
   * @returns {string} - Estilo CSS para o fundo dos ícones do po-select.
   */
  getSelectBgIconsStyle(themeColor2) {
    let selectBgIcon = "";
    if (themeColor2?.brand?.["01"]?.dark) {
      const selector2 = "po-select .po-select-phosphor";
      selectBgIcon += `${selector2} { --background-image: url(${this.getSelectBgIcon(themeColor2.neutral.dark[70])}); };`;
    }
    if (themeColor2?.feedback?.negative?.base) {
      selectBgIcon += `po-select.ng-dirty.ng-invalid select.po-select-phosphor { background-image: url(${this.getSelectBgIcon(themeColor2.feedback.negative.base)}); };`;
    }
    if (themeColor2?.neutral?.light?.["30"]) {
      selectBgIcon += `po-select select.po-select-phosphor:disabled { background-image: url(${this.getSelectBgIcon(themeColor2.neutral.light["30"])}); };`;
    }
    return selectBgIcon;
  }
  /**
   * @docsPrivate
   *
   * Retorna a imagem SVG utilizada como fundo do po-select.
   *
   * @param {string} color Cor da Imagem - Utilizada no atributo 'fill'.
   * @returns {string} Imagem SVG utilizada no po-select.
   */
  getSelectBgIcon(color3) {
    let svg;
    svg = `"data:image/svg+xml,%3Csvg width='24' height='24' viewBox='0 0 256 256' fill='${color3.replace("#", "%23")}' `;
    svg = svg.concat(`xmlns='http://www.w3.org/2000/svg'%3E%3Cpath `);
    svg = svg.concat(`d='M213.66,101.66l-80,80a8,8,0,0,1-11.32,0l-80-80A8,8,0,0,1,53.66,90.34L128,164.69l74.34-74.35a8,8,0,0,1,11.32,11.32Z'`);
    svg = svg.concat(`%3E%3C/path%3E%3C/svg%3E");`);
    return svg;
  }
  dispatchEvent(theme2) {
    const evento = new CustomEvent("PoUiThemeChange", {
      detail: theme2
    });
    window.dispatchEvent(evento);
  }
  /**
   * Define o tipo (light/dark) quando um tema está sendo aplicado.
   *
   * @param {PoTheme} theme - Objeto contendo as definições de tema a serem aplicadas no componente.
   * @param {PoThemeTypeEnum} [themeType=PoThemeTypeEnum.light] - (Opcional) Tipo de tema a ser aplicado, podendo ser 'light' (claro) ou 'dark' (escuro). Por padrão, o tema claro é aplicado.
   */
  setThemeType(theme2, themeType = PoThemeTypeEnum.light) {
    const _accessibility = typeof theme2.active === "object" ? theme2.active.a11y : PoThemeA11yEnum.AAA;
    this.setTheme(theme2, themeType, _accessibility);
  }
  /**
   *  Define o tipo (light/dark) para um tema já ativo.
   *
   * @param {PoThemeTypeEnum} [themeType=PoThemeTypeEnum.light] - (Opcional) Tipo de tema a ser aplicado, podendo ser 'light' (claro) ou 'dark' (escuro). Por padrão, o tema claro é aplicado.
   */
  setCurrentThemeType(themeType = PoThemeTypeEnum.light) {
    const _theme = this.getThemeActive();
    this.setThemeType(_theme, themeType);
  }
  /**
   * Define o nível de acessibilidade quando um tema está sendo aplicado.
   *
   * @param {PoTheme} theme - Objeto contendo as definições de tema a serem aplicadas no componente.
   * @param {PoThemeA11yEnum} [a11y=PoThemeA11yEnum.AAA] - (Opcional) Nível de acessibilidade dos componentes podendo ser
   * AA ou AAA. Por padrão a acessibilidade é AAA.
   */
  setThemeA11y(theme2, a11y = PoThemeA11yEnum.AAA) {
    const _type = (typeof theme2.active === "object" ? theme2.active.type : theme2.active) || 0;
    this.setTheme(theme2, _type, a11y);
  }
  /**
   * Define o nível de acessibilidade para um tema já ativo.
   *
   * @param {PoThemeA11yEnum} [a11y=PoThemeA11yEnum.AAA] - (Opcional) Nível de acessibilidade dos componentes podendo ser
   * AA ou AAA. Por padrão a acessibilidade é AAA.
   */
  setCurrentThemeA11y(a11y = PoThemeA11yEnum.AAA) {
    const _theme = this.getThemeActive();
    this.setThemeA11y(_theme, a11y);
  }
  resetBaseTheme() {
    const styleElement = this.document.head.querySelector("#baseStyle");
    if (styleElement) {
      styleElement.textContent = "";
    }
  }
  setDefaultBaseStyle() {
    this.setPerComponentAndOnRoot(void 0, poThemeDefaultAAA.perComponent, poThemeDefaultAAA.onRoot);
    this.setPerComponentAndOnRoot({
      a11y: PoThemeA11yEnum.AA
    }, poThemeDefaultAA.perComponent, poThemeDefaultAA.onRoot);
    this.setPerComponentAndOnRoot({
      type: PoThemeTypeEnum.light
    }, poThemeDefaultLightValues.perComponent, poThemeDefaultLightValues.onRoot);
    this.setPerComponentAndOnRoot({
      type: PoThemeTypeEnum.light,
      a11y: PoThemeA11yEnum.AA
    }, poThemeDefaultLightValuesAA.perComponent, poThemeDefaultLightValuesAA.onRoot);
    this.setPerComponentAndOnRoot({
      type: PoThemeTypeEnum.dark
    }, poThemeDefaultDarkValues.perComponent, poThemeDefaultDarkValues.onRoot);
    this.setPerComponentAndOnRoot({
      type: PoThemeTypeEnum.dark,
      a11y: PoThemeA11yEnum.AA
    }, poThemeDefaultDarkValuesAA.perComponent, poThemeDefaultDarkValuesAA.onRoot);
  }
  static ɵfac = function PoThemeService_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoThemeService)(ɵɵinject(DOCUMENT), ɵɵinject(RendererFactory2));
  };
  static ɵprov = ɵɵdefineInjectable({
    token: _PoThemeService,
    factory: _PoThemeService.ɵfac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoThemeService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: Document,
    decorators: [{
      type: Inject,
      args: [DOCUMENT]
    }]
  }, {
    type: RendererFactory2
  }], null);
})();
var PoThemeModule = class _PoThemeModule {
  static ɵfac = function PoThemeModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoThemeModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoThemeModule
  });
  static ɵinj = ɵɵdefineInjector({
    providers: [PoThemeService]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoThemeModule, [{
    type: NgModule,
    args: [{
      providers: [PoThemeService],
      bootstrap: []
    }]
  }], null, null);
})();
var PoUserGuideModule = class _PoUserGuideModule {
  static ɵfac = function PoUserGuideModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoUserGuideModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoUserGuideModule,
    imports: [CommonModule]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [CommonModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoUserGuideModule, [{
    type: NgModule,
    args: [{
      declarations: [],
      imports: [CommonModule],
      exports: [],
      providers: [],
      bootstrap: []
    }]
  }], null, null);
})();
var PoServicesModule = class _PoServicesModule {
  static ɵfac = function PoServicesModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoServicesModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoServicesModule,
    declarations: [PoI18nPipe],
    imports: [PoColorPaletteModule, PoComponentInjectorModule, PoControlPositionModule, PoDateTimeModule, PoDialogModule, PoLanguageModule, PoMediaQueryModule, PoNotificationModule, PoThemeModule, PoUserGuideModule],
    exports: [PoColorPaletteModule, PoComponentInjectorModule, PoControlPositionModule, PoDateTimeModule, PoDialogModule, PoI18nPipe, PoMediaQueryModule, PoNotificationModule, PoThemeModule, PoUserGuideModule]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [PoColorPaletteModule, PoComponentInjectorModule, PoControlPositionModule, PoDateTimeModule, PoDialogModule, PoLanguageModule, PoMediaQueryModule, PoNotificationModule, PoThemeModule, PoUserGuideModule, PoColorPaletteModule, PoComponentInjectorModule, PoControlPositionModule, PoDateTimeModule, PoDialogModule, PoMediaQueryModule, PoNotificationModule, PoThemeModule, PoUserGuideModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoServicesModule, [{
    type: NgModule,
    args: [{
      declarations: [PoI18nPipe],
      imports: [PoColorPaletteModule, PoComponentInjectorModule, PoControlPositionModule, PoDateTimeModule, PoDialogModule, PoLanguageModule, PoMediaQueryModule, PoNotificationModule, PoThemeModule, PoUserGuideModule],
      exports: [PoColorPaletteModule, PoComponentInjectorModule, PoControlPositionModule, PoDateTimeModule, PoDialogModule, PoI18nPipe, PoMediaQueryModule, PoNotificationModule, PoThemeModule, PoUserGuideModule],
      providers: [],
      bootstrap: []
    }]
  }], null, null);
})();
var PoActiveOverlayModule = class _PoActiveOverlayModule {
  static ɵfac = function PoActiveOverlayModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoActiveOverlayModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoActiveOverlayModule
  });
  static ɵinj = ɵɵdefineInjector({
    providers: [PoActiveOverlayService]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoActiveOverlayModule, [{
    type: NgModule,
    args: [{
      providers: [PoActiveOverlayService],
      bootstrap: []
    }]
  }], null, null);
})();
var I18N_CONFIG = new InjectionToken("I18N_CONFIG");
var PoI18nBaseService = class _PoI18nBaseService {
  config;
  http;
  languageService;
  varI18n = {};
  contextDefault;
  useCache = false;
  servicesContext = {};
  constructor(config, http, languageService) {
    this.config = config;
    this.http = http;
    this.languageService = languageService;
    this.setConfig(config);
  }
  /**
   * <a id="get-language"></a>
   * Método que retorna o idioma padrão ativo.
   *
   * A busca deste idioma pelo método será feita na seguinte ordem:
   *
   *   1 - o idioma que foi armazenado no *localStorage*, através do método [`setLanguage()`](documentation/po-i18n#setLanguage).
   *
   *   2 - o valor inserido no módulo do i18n através do parâmetro `config`, sendo o idioma inserido na propriedade
   * `language` da interface [`PoI18nConfigDefault`](documentation/po-i18n#poI18nConfigDefault).
   *
   *   3 - o idioma do navegador utilizado.
   *
   * > Caso o idioma do navegador não seja suportado pelo PO (`pt`, `en`, `es` ou `ru`), será retornado valor `pt`.
   *
   * @returns {string} sigla do idioma padrão.
   */
  getLanguage() {
    return this.languageService.getLanguage();
  }
  getLiterals(options = {}) {
    const language = options["language"] ? options["language"].toLowerCase() : this.getLanguage();
    const context = options["context"] ? options["context"] : this.contextDefault;
    const literals = options["literals"] ? options["literals"] : [];
    return new Observable((observer) => {
      if (this.servicesContext[context]) {
        this.getLiteralsFromContextService(language, context, literals, observer);
      } else {
        this.getLiteralsFromContextConstant(language, context, literals, observer);
      }
    });
  }
  /**
   * Método que retorna o idioma padrão ativo, com somente a abreviação do idioma (duas primeiras letras).
   * Por exemplo: "pt" ou "es".
   *
   * A busca deste idioma é baseada no método [**getLanguage()**](/documentation/po-i18n#get-language).
   *
   * @returns {string} sigla do idioma padrão.
   */
  getShortLanguage() {
    return this.languageService.getShortLanguage();
  }
  /**
   * <a id="setLanguage"></a>
   * Método para alterar o idioma padrão do módulo do i18n.
   *
   * Ao utilizar este método, o idioma ficará gravado no armazenamento local do navegador, que será utilizado pelo
   * serviço do `i18n` para buscar as literais no idioma padrão.
   *
   * @param {string} language Sigla do idioma.
   *
   * Esta sigla deve ser composta por duas letras representando o idioma,
   * podendo ser adicionado outras duas letras representando o país, por exemplo: `pt`, `pt-BR`, `pt-br`, `en` ou `en-US`.
   *
   * > Caso seja informado um valor diferente deste padrão, o mesmo será ignorado.
   *
   * @param {boolean} reload Indica se a página atual poderá ser recarregada após a alteração do idioma.
   *
   * Este recurso pode ser útil para os usuários que utilizam o método `getLiterals()` do serviço do i18n para poder
   * buscar novamente as literais no novo idioma configurado.
   */
  setLanguage(language, reload = false) {
    if (!PoUtils.isLanguage(language)) {
      return;
    }
    this.languageService.setLanguage(language);
    if (reload) {
      PoUtils.reloadCurrentPage();
    }
  }
  setConfig(config) {
    if (config["default"]) {
      this.languageService.setLanguageDefault(config["default"]["language"]);
      this.contextDefault = config["default"]["context"] ? config["default"]["context"] : "";
      this.useCache = config["default"]["cache"] ? config["default"]["cache"] : false;
    }
    if (config["contexts"]) {
      this.setVarI18n(config["contexts"]);
      if (!this.contextDefault) {
        for (const context in config["contexts"]) {
          if (this.config["contexts"].hasOwnProperty(context)) {
            this.contextDefault = context;
            break;
          }
        }
      }
    }
  }
  // Processo de busca de um contexto que contém serviço.
  //    1 - Procura na variável I18n deste serviço
  //    2 - Procura no local storage (Se o cache estiver definido como true na configuração do módulo)
  //    3 - Dispara o serviço, mesmo que já tenha encontrado no local storage, para garantir a atualização
  //    4 - Se nenhuma literal for encontrada, então busca em pt-br
  getLiteralsFromContextService(language, context, literals, observer, translations = {}, languageAlternative = null) {
    const languageSearch = languageAlternative ? languageAlternative : language;
    translations = this.mergeObject(translations, this.searchInVarI18n(languageSearch, context, literals));
    if (this.countObject(translations) > 0) {
      observer.next(translations);
    }
    this.getLiteralsLocalStorageAndCache(languageSearch, context, literals, observer, translations, languageAlternative);
  }
  // Procura no local storage e em seguida no serviço
  // Caso não encontre nem no serviço, recomeça a busca em pt-br
  getLiteralsLocalStorageAndCache(language, context, literals, observer, translations, languageAlternative = null) {
    const languageSearch = languageAlternative ? languageAlternative : language;
    let translationTemp;
    if (this.useCache) {
      translationTemp = this.searchInLocalStorage(languageSearch, context, literals);
      if (this.countObject(translationTemp) > 0) {
        this.updateVarI18n(language, context, translationTemp);
        translations = this.mergeObject(translationTemp, translations);
        observer.next(translations);
      }
    }
    this.getHttpService(this.servicesContext[context], languageSearch, literals).subscribe((response) => {
      if (response) {
        this.updateLocalStorage(language, context, response);
        this.updateVarI18n(language, context, response);
        translationTemp = this.searchInVarI18n(language, context, literals);
        translations = this.mergeObject(translationTemp, translations);
        observer.next(translations);
      }
      if (literals.length > this.countObject(translations)) {
        if (languageAlternative === "pt-br") {
          translations = this.completeFaultLiterals(language, context, literals, translations);
          this.updateLocalStorage(language, context, translations);
          this.updateVarI18n(language, context, translations);
          observer.next(translations);
        } else {
          this.getLiteralsFromContextService(language, context, literals, observer, translations, "pt-br");
        }
      }
    });
  }
  // Procura pela lista de literais
  // Se não encontrar todas, procura em pt-br
  getLiteralsFromContextConstant(language, context, literals, observer, translations = {}) {
    translations = this.mergeObject(translations, this.searchInVarI18n(language, context, literals));
    if (this.countObject(translations) > 0) {
      observer.next(translations);
    }
    if (literals.length > 0) {
      if (literals.length > this.countObject(translations)) {
        if (language === "pt-br") {
          translations = this.completeFaultLiterals(language, context, literals, translations);
          observer.next(translations);
        } else {
          this.getLiteralsFromContextConstant("pt-br", context, literals, observer, translations);
        }
      }
    } else {
      if (this.countObject(translations) === 0 && language !== "pt-br") {
        this.getLiteralsFromContextConstant("pt-br", context, literals, observer, translations);
      }
      observer.next(translations);
      observer.complete();
    }
  }
  // Busca pelas literais no local storage
  searchInLocalStorage(language, context, literals) {
    const translations = {};
    if (literals.length > 0) {
      for (let i2 = 0; i2 < literals.length; i2++) {
        const literal = literals[i2];
        const translation = localStorage.getItem(language + "-" + context + "-" + literal);
        if (translation) {
          translations[literal] = translation;
        }
      }
    }
    return translations;
  }
  // Busca pelas literais na variável do serviço
  searchInVarI18n(language, context, literals) {
    let translations = {};
    if (this.varI18n[language] && this.varI18n[language][context]) {
      const content = this.varI18n[language][context];
      if (literals.length > 0) {
        for (let i2 = 0; i2 < literals.length; i2++) {
          const literal = literals[i2];
          if (content.hasOwnProperty(literal)) {
            translations[literal] = content[literal];
          }
        }
      } else {
        translations = __spreadValues({}, content);
      }
    }
    return translations;
  }
  // Atualiza o local storage
  updateLocalStorage(language, context, data) {
    if (this.useCache) {
      for (const literal of Object.keys(data)) {
        localStorage.setItem(language + "-" + context + "-" + literal, data[literal]);
      }
    }
  }
  // Atualiza a variável local com as literais com os objetos passados na configuração
  setVarI18n(contexts) {
    for (const context of Object.keys(contexts)) {
      const contextContent = contexts[context];
      for (const language of Object.keys(contextContent)) {
        const languageContent = contextContent[language];
        if (language === "url") {
          this.servicesContext[context] = languageContent;
        } else {
          this.updateVarI18n(language, context, languageContent);
        }
      }
    }
  }
  // Atualiza a variável local com as literais idioma e contexto
  updateVarI18n(language, context, data) {
    language = language.toLowerCase();
    if (!this.varI18n[language]) {
      this.varI18n[language] = {
        [context]: {}
      };
    }
    if (!this.varI18n[language][context]) {
      this.varI18n[language][context] = {};
    }
    this.varI18n[language][context] = this.mergeObject(data, this.varI18n[language][context]);
  }
  getHttpService(url, language, literals) {
    let param = "?language=" + language;
    if (literals.length > 0) {
      param += "&literals=" + literals.join();
    }
    url = url.replace(/\/$/, "");
    return this.http.get(`${url}${param}`);
  }
  // Completa com o nome da literais, as que não foram encontradas
  completeFaultLiterals(language, context, literals, translations) {
    for (let i2 = 0; i2 < literals.length; i2++) {
      const literal = literals[i2];
      if (!translations[literal]) {
        translations[literal] = literal;
      }
    }
    return translations;
  }
  // Conta os atributos do objeto
  countObject(obj) {
    return Object.keys(obj).length;
  }
  // Faz o merge dos objetos, sempre dando preferência para o primeiro objeto de parâmetro
  mergeObject(objPermanent, obj) {
    return __spreadValues(__spreadValues({}, obj), objPermanent);
  }
  static ɵfac = function PoI18nBaseService_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoI18nBaseService)(ɵɵdirectiveInject(I18N_CONFIG), ɵɵdirectiveInject(HttpClient), ɵɵdirectiveInject(PoLanguageService));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoI18nBaseService
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoI18nBaseService, [{
    type: Directive
  }], () => [{
    type: void 0,
    decorators: [{
      type: Inject,
      args: [I18N_CONFIG]
    }]
  }, {
    type: HttpClient,
    decorators: [{
      type: Inject,
      args: [HttpClient]
    }]
  }, {
    type: PoLanguageService
  }], null);
})();
var PoI18nService = class _PoI18nService extends PoI18nBaseService {
  constructor(configs, http, languageService) {
    const merged = Array.isArray(configs) ? mergePoI18nConfigs(configs) : configs;
    super(merged, http, languageService);
  }
  static ɵfac = function PoI18nService_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoI18nService)(ɵɵinject(I18N_CONFIG), ɵɵinject(HttpClient), ɵɵinject(PoLanguageService));
  };
  static ɵprov = ɵɵdefineInjectable({
    token: _PoI18nService,
    factory: _PoI18nService.ɵfac
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoI18nService, [{
    type: Injectable
  }], () => [{
    type: void 0,
    decorators: [{
      type: Inject,
      args: [I18N_CONFIG]
    }]
  }, {
    type: HttpClient
  }, {
    type: PoLanguageService
  }], null);
})();
function returnPoI18nService(configs, http, languageService) {
  const mergedObject = mergePoI18nConfigs(configs);
  return new PoI18nService(mergedObject, http, languageService);
}
function mergePoI18nConfigs(configList) {
  const mergedContexts = {};
  const selectedDefault = {
    language: "",
    context: ""
  };
  for (const currentConfig of configList) {
    if (!selectedDefault.language && currentConfig.default?.language) {
      selectedDefault.language = currentConfig.default.language;
    }
    if (!selectedDefault.context && currentConfig.default?.context) {
      selectedDefault.context = currentConfig.default.context;
    }
    if (selectedDefault.cache === void 0 && currentConfig.default?.cache !== void 0) {
      selectedDefault.cache = currentConfig.default.cache;
    }
    if (currentConfig.contexts) {
      Object.entries(currentConfig.contexts).forEach(([contextKey, langMap]) => {
        mergedContexts[contextKey] ??= {};
        Object.entries(langMap).forEach(([lang, translations]) => {
          if (lang === "url") {
            mergedContexts[contextKey][lang] = translations;
          } else {
            mergedContexts[contextKey][lang] = __spreadValues(__spreadValues({}, mergedContexts[contextKey][lang]), translations);
          }
        });
      });
    }
  }
  return {
    default: selectedDefault,
    contexts: mergedContexts
  };
}
var PoI18nModule = class _PoI18nModule {
  static config(config) {
    return {
      ngModule: _PoI18nModule,
      providers: [{
        provide: I18N_CONFIG,
        useValue: config,
        multi: true
      }, provideAppInitializer(() => {
        const initializerFn = initializeLanguageDefault(inject(I18N_CONFIG), inject(PoLanguageService));
        return initializerFn();
      }), {
        provide: PoI18nService,
        useFactory: returnPoI18nService,
        deps: [I18N_CONFIG, HttpClient, PoLanguageService]
      }]
    };
  }
  static ɵfac = function PoI18nModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoI18nModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoI18nModule,
    imports: [PoLanguageModule]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [PoLanguageModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoI18nModule, [{
    type: NgModule,
    args: [{
      imports: [PoLanguageModule]
    }]
  }], null, null);
})();
function initializeLanguageDefault(configs, languageService) {
  const config = configs.find((c) => c.default);
  return () => {
    if (config?.default.language) {
      languageService.setLanguageDefault(config.default.language);
    }
  };
}
var PoNotificationBaseService = class {
  // Array responsável por guardar a instância de po-toaster's superiores.
  stackTop = [];
  // Array responsável por guardar a instância de po-toaster's inferiores.
  stackBottom = [];
  // Duração da notificação ativa.
  defaultDuration = 9e3;
  /**
   * Emite uma notificação de sucesso.
   *
   * @param {PoNotification | string} notification Objeto com os dados da notificação ou somente a string com a mensagem da notificação.
   */
  success(notification) {
    this.createToaster(this.buildToaster(notification, PoToasterType.Success));
  }
  /**
   * Emite uma notificação de atenção.
   *
   * @param {PoNotification | string} notification Objeto com os dados da notificação ou somente a string com a mensagem da notificação
   */
  warning(notification) {
    this.createToaster(this.buildToaster(notification, PoToasterType.Warning));
  }
  /**
   * Emite uma notificação de erro.
   *
   * @param {PoNotification | string} notification Objeto com os dados da notificação ou somente a string com a mensagem da notificação
   */
  error(notification) {
    this.createToaster(this.buildToaster(notification, PoToasterType.Error));
  }
  /**
   * Emite uma notificação de informação.
   *
   * @param {PoNotification | string} notification Objeto com os dados da notificação ou somente a string com a mensagem da notificação
   */
  information(notification) {
    this.createToaster(this.buildToaster(notification, PoToasterType.Information));
  }
  /**
   * Define em milissegundos a duração padrão para as notificações.
   *
   * > Padrão 9 segundos.
   *
   * @param {number} defaultDuration Duração em milisegundos
   */
  setDefaultDuration(defaultDuration) {
    this.defaultDuration = defaultDuration;
  }
  /**
   * @docsPrivate
   *
   * Cria um objeto do tipo PoToaster de acordo o tipo.
   *
   * @param {PoNotification | string} notification Objeto PoNotification com os dados da notificação
   */
  buildToaster(notification, type) {
    let index = 0;
    let orientation;
    if (notification.orientation === void 0 || notification.orientation === PoToasterOrientation.Bottom) {
      index = this.stackBottom.length;
      orientation = PoToasterOrientation.Bottom;
    } else {
      index = this.stackTop.length;
      orientation = PoToasterOrientation.Top;
    }
    const sizeActions = validateSizeFn(notification.sizeActions, PoFieldSize);
    const toaster = {
      componentRef: void 0,
      message: notification.message || notification,
      type,
      orientation,
      action: notification.action,
      actionLabel: notification.actionLabel,
      position: index,
      mode: notification.mode || PoToasterMode.Alert,
      showClose: notification.showClose || true,
      sizeActions,
      supportMessage: notification.supportMessage,
      duration: notification.duration || this.defaultDuration
    };
    if (notification.action) {
      toaster.action = (param) => {
        notification.action();
        this.destroyToaster(param.componentRef);
      };
    }
    return toaster;
  }
};
var PoNotificationMaxLength = 5;
var PoNotificationFadeTime = 200;
var PoNotificationService = class _PoNotificationService extends PoNotificationBaseService {
  poComponentInjector;
  constructor(poComponentInjector) {
    super();
    this.poComponentInjector = poComponentInjector;
  }
  createToaster(toaster) {
    const componentRef = this.poComponentInjector.createComponentInApplication(PoToasterComponent);
    toaster.componentRef = componentRef;
    componentRef.changeDetectorRef.detectChanges();
    componentRef.instance.configToaster(toaster);
    const stack = toaster.orientation === PoToasterOrientation.Top ? this.stackTop : this.stackBottom;
    stack.push(componentRef);
    this.verifyLimitToaster(stack);
    this.observableOnClose(componentRef);
    if (!(toaster.action && toaster.actionLabel || toaster.type === PoToasterType.Error)) {
      timer(toaster.duration).pipe(takeWhile(() => componentRef.instance.alive)).subscribe(() => {
        this.destroyToaster(componentRef);
      });
    }
  }
  destroyToaster(toaster) {
    let stack;
    if (toaster.instance.orientation === PoToasterOrientation.Top) {
      stack = this.stackTop;
    } else {
      stack = this.stackBottom;
    }
    toaster.instance.setFadeOut();
    const index = stack.indexOf(toaster);
    stack.splice(index, 1);
    setTimeout(() => {
      this.poComponentInjector.destroyComponentInApplication(toaster);
      for (let count2 = 0; count2 < stack.length; count2++) {
        stack[count2].instance.changePosition(count2);
      }
    }, PoNotificationFadeTime);
  }
  observableOnClose(componentRef) {
    componentRef.instance.observableOnClose.subscribe(() => {
      this.destroyToaster(componentRef);
    });
  }
  verifyLimitToaster(stack) {
    if (stack.length > PoNotificationMaxLength) {
      this.destroyToaster(stack[0]);
    }
  }
  static ɵfac = function PoNotificationService_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoNotificationService)(ɵɵinject(PoComponentInjectorService));
  };
  static ɵprov = ɵɵdefineInjectable({
    token: _PoNotificationService,
    factory: _PoNotificationService.ɵfac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoNotificationService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: PoComponentInjectorService
  }], null);
})();
var poUserGuideLiteralsDefault = {
  en: {
    next: "Next",
    previous: "Previous",
    done: "Done",
    close: "Close",
    progressTemplate: "{{current}} of {{total}}"
  },
  es: {
    next: "Siguiente",
    previous: "Anterior",
    done: "Finalizar",
    close: "Cerrar",
    progressTemplate: "{{current}} de {{total}}"
  },
  pt: {
    next: "Próximo",
    previous: "Anterior",
    done: "Finalizar",
    close: "Fechar",
    progressTemplate: "{{current}} de {{total}}"
  },
  ru: {
    next: "Следующий",
    previous: "Предыдущий",
    done: "Завершить",
    close: "Закрыть",
    progressTemplate: "{{current}} из {{total}}"
  }
};
var PoUserGuideBaseService = class {
  _literals;
  language = poLocaleDefault;
  get defaultOptions() {
    const literals = this.literals;
    return {
      allowClose: true,
      showProgress: true,
      keyboardControl: true,
      overlayOpacity: 0.7,
      nextLabel: literals.next,
      previousLabel: literals.previous,
      doneLabel: literals.done,
      closeLabel: literals.close,
      progressTemplate: literals.progressTemplate
    };
  }
  /**
   * @optional
   *
   * @description
   *
   * Objeto com as literais usadas no `po-user-guide`.
   *
   * Existem duas maneiras de customizar o serviço, passando um objeto com todas as literais disponíveis:
   *
   * ```
   *  const customLiterals: PoUserGuideLiterals = {
   *    next: 'Próximo',
   *    previous: 'Anterior',
   *    done: 'Finalizar',
   *    close: 'Fechar'
   *  };
   * ```
   *
   * Ou passando apenas as literais que deseja customizar:
   *
   * ```
   *  const customLiterals: PoUserGuideLiterals = {
   *    next: 'Avançar'
   *  };
   * ```
   *
   * E para carregar as literais customizadas, basta passá-las ao método `setOptions`:
   *
   * ```
   * this.poUserGuide.setOptions({ literals: customLiterals }).start();
   * ```
   *
   * > O objeto padrão de literais será traduzido de acordo com o idioma do
   * [`PoI18nService`](/documentation/po-i18n) ou do browser.
   */
  get literals() {
    return this._literals || __spreadValues(__spreadValues({}, poUserGuideLiteralsDefault[poLocaleDefault]), poUserGuideLiteralsDefault[this.language]);
  }
  set literals(value) {
    if (value instanceof Object && !(value instanceof Array)) {
      this._literals = __spreadValues(__spreadValues(__spreadValues({}, poUserGuideLiteralsDefault[poLocaleDefault]), poUserGuideLiteralsDefault[this.language]), value);
    } else {
      this._literals = poUserGuideLiteralsDefault[this.language];
    }
  }
  /**
   * @docsPrivate
   *
   * Lista de passos do tour configurada pela aplicação consumidora através do método `setSteps`.
   * É reinicializada como um array vazio enquanto nenhum passo é configurado.
   */
  steps = [];
  /**
   * @docsPrivate
   *
   * Opções globais do tour configuradas pela aplicação consumidora através do método `setOptions`.
   */
  options = {};
  /**
   * @docsPrivate
   *
   * Índice, com base zero, do passo ativo durante a execução do tour.
   *
   * Permanece em `-1` enquanto não houver tour em andamento, sinalizando ausência de execução para os
   * métodos de consulta de estado expostos pela classe PoUserGuideService.
   */
  currentIndex = -1;
  /**
   * @docsPrivate
   *
   * `Subject` interno utilizado pela classe PoUserGuideService para emitir o evento de início do tour.
   *
   * É exposto publicamente apenas como `Observable` somente leitura através da propriedade `tourStart$`.
   */
  _tourStart = new Subject();
  /**
   * @docsPrivate
   *
   * `Subject` interno utilizado pela classe PoUserGuideService para emitir o evento de encerramento do tour.
   *
   * É exposto publicamente apenas como `Observable` somente leitura através da propriedade `tourEnd$`.
   */
  _tourEnd = new Subject();
  /**
   * @docsPrivate
   *
   * `Subject` interno utilizado pela classe PoUserGuideService para emitir os eventos de mudança de passo
   * durante a execução do tour.
   *
   * É exposto publicamente apenas como `Observable` somente leitura através da propriedade `stepChange$`.
   */
  _stepChange = new Subject();
  /**
   * `Observable` que emite um `PoUserGuideEvent` no início de cada execução do tour.
   *
   * É emitido exatamente uma vez por execução, imediatamente após a configuração da instância do tour
   * e antes da primeira emissão de `stepChange$`. As aplicações consumidoras podem assinar este
   * `Observable` para registrar telemetria do início do tour, exibir mensagens contextuais ou disparar
   * lógica de negócio dependente do início da jornada do usuário.
   */
  tourStart$ = this._tourStart.asObservable();
  /**
   * `Observable` que emite um `PoUserGuideEndEvent` no encerramento de cada execução do tour.
   *
   * É emitido exatamente uma vez por execução, sempre após a última emissão de `stepChange$` e após a
   * limpeza do estado interno do serviço. As aplicações consumidoras podem assinar este `Observable`
   * para registrar telemetria de conclusão ou de abandono do tour, executar limpezas de UI ou disparar
   * lógica de negócio dependente do encerramento da jornada do usuário.
   */
  tourEnd$ = this._tourEnd.asObservable();
  /**
   * `Observable` que emite um `PoUserGuideStepChangeEvent` a cada mudança de passo durante a execução do tour.
   *
   * As aplicações consumidoras podem assinar este `Observable` para reagir à navegação do usuário,
   * atualizar a UI conforme o passo ativo, registrar telemetria de progresso ou disparar lógica de
   * negócio contextual ao passo corrente.
   */
  stepChange$ = this._stepChange.asObservable();
  constructor(languageService) {
    this.language = languageService.getShortLanguage();
  }
  validateSteps(steps) {
    if (steps === null || steps === void 0) {
      throw new Error("PoUserGuideService: a lista de passos é obrigatória.");
    }
    if (!Array.isArray(steps)) {
      throw new Error("PoUserGuideService: a lista de passos deve ser um array.");
    }
    if (steps.length === 0) {
      throw new Error("PoUserGuideService: a lista de passos não pode ser vazia.");
    }
    const isBrowser = typeof document !== "undefined";
    for (let i2 = 0; i2 < steps.length; i2++) {
      const step = steps[i2];
      if (step === null || step === void 0 || typeof step !== "object") {
        throw new Error(`PoUserGuideService: passo no índice ${i2} é inválido.`);
      }
      if (step.content === null || step.content === void 0 || step.content.trim() === "") {
        throw new Error(`PoUserGuideService: o passo no índice ${i2} precisa ter a propriedade 'content' definida.`);
      }
      if (typeof step.element === "string" && isBrowser) {
        try {
          document.querySelector(step.element);
        } catch (e2) {
          throw new Error(`PoUserGuideService: seletor CSS inválido no passo no índice ${i2}: "${step.element}".`);
        }
      }
    }
  }
  resolveOptions(options) {
    const effectiveLiterals = options?.literals ? __spreadValues(__spreadValues(__spreadValues({}, poUserGuideLiteralsDefault[poLocaleDefault]), poUserGuideLiteralsDefault[this.language]), options.literals) : this.literals;
    const userProgressTemplate = options?.progressTemplate;
    const resolved = __spreadValues({
      allowClose: true,
      showProgress: true,
      keyboardControl: true,
      overlayOpacity: 0.7,
      nextLabel: effectiveLiterals.next,
      previousLabel: effectiveLiterals.previous,
      doneLabel: effectiveLiterals.done,
      closeLabel: effectiveLiterals.close,
      progressTemplate: effectiveLiterals.progressTemplate
    }, options ?? {});
    const overlayOpacity = resolved.overlayOpacity;
    if (typeof overlayOpacity !== "number" || !Number.isFinite(overlayOpacity)) {
      resolved.overlayOpacity = this.defaultOptions.overlayOpacity;
    } else {
      resolved.overlayOpacity = Math.min(1, Math.max(0, overlayOpacity));
    }
    if (typeof userProgressTemplate === "string" && !userProgressTemplate.includes("{{current}}") && !userProgressTemplate.includes("{{total}}")) {
      console.warn(`PoUserGuideService: o progressTemplate "${userProgressTemplate}" não contém os placeholders {{current}} ou {{total}}; o texto será exibido literalmente.`);
    }
    return resolved;
  }
};
var PoUserGuideAlignment;
(function(PoUserGuideAlignment2) {
  PoUserGuideAlignment2["Start"] = "start";
  PoUserGuideAlignment2["Center"] = "center";
  PoUserGuideAlignment2["End"] = "end";
})(PoUserGuideAlignment || (PoUserGuideAlignment = {}));
var PoUserGuidePosition;
(function(PoUserGuidePosition2) {
  PoUserGuidePosition2["Top"] = "top";
  PoUserGuidePosition2["Right"] = "right";
  PoUserGuidePosition2["Bottom"] = "bottom";
  PoUserGuidePosition2["Left"] = "left";
  PoUserGuidePosition2["Over"] = "over";
  PoUserGuidePosition2["Auto"] = "auto";
})(PoUserGuidePosition || (PoUserGuidePosition = {}));
var SCROLL_KEYS = /* @__PURE__ */ new Set(["ArrowUp", "ArrowDown", "ArrowLeft", "ArrowRight", "PageUp", "PageDown", "Home", "End", " "]);
function resolveScrollLockMode(options) {
  return options != null && options.allowScroll === true ? "free" : "blocked";
}
var PoUserGuideScrollLock = class {
  active = false;
  snapshot = null;
  wheelListener = null;
  touchListener = null;
  keydownListener = null;
  isActive() {
    return this.active;
  }
  lock() {
    if (!this.isBrowser() || this.active === true) {
      return;
    }
    this.snapshot = this.captureSnapshot();
    this.applyLockStyles(this.snapshot);
    const listenerOptions = {
      passive: false
    };
    this.wheelListener = (event) => {
      if (event.ctrlKey) {
        return;
      }
      event.preventDefault();
    };
    this.touchListener = (event) => event.preventDefault();
    this.keydownListener = (event) => {
      if (this.isScrollKey(event) && !this.isWithinEditableInPopover(event)) {
        event.preventDefault();
      }
    };
    document.addEventListener("wheel", this.wheelListener, listenerOptions);
    document.addEventListener("touchmove", this.touchListener, listenerOptions);
    document.addEventListener("keydown", this.keydownListener);
    this.active = true;
  }
  unlock() {
    if (this.active === false) {
      return;
    }
    if (this.snapshot !== null) {
      this.restoreSnapshot(this.snapshot);
    }
    const listenerOptions = {
      passive: false
    };
    if (this.wheelListener !== null) {
      document.removeEventListener("wheel", this.wheelListener, listenerOptions);
    }
    if (this.touchListener !== null) {
      document.removeEventListener("touchmove", this.touchListener, listenerOptions);
    }
    if (this.keydownListener !== null) {
      document.removeEventListener("keydown", this.keydownListener);
    }
    this.wheelListener = null;
    this.touchListener = null;
    this.keydownListener = null;
    this.snapshot = null;
    this.active = false;
  }
  isBrowser() {
    return typeof window !== "undefined" && typeof document !== "undefined";
  }
  captureSnapshot() {
    const html = document.documentElement;
    const body = document.body;
    const scrollbarWidth = Math.max(0, window.innerWidth - html.clientWidth);
    return {
      scrollX: window.scrollX,
      scrollY: window.scrollY,
      scrollbarWidth,
      documentElement: {
        overflow: html.style.overflow,
        overflowX: html.style.overflowX,
        overflowY: html.style.overflowY,
        position: html.style.position,
        top: html.style.top,
        left: html.style.left,
        width: html.style.width,
        paddingRight: html.style.paddingRight
      },
      body: {
        overflow: body.style.overflow,
        overflowX: body.style.overflowX,
        overflowY: body.style.overflowY,
        position: body.style.position,
        top: body.style.top,
        left: body.style.left,
        width: body.style.width,
        paddingRight: body.style.paddingRight
      }
    };
  }
  applyLockStyles(snapshot) {
    document.documentElement.style.overflow = "hidden";
    if (snapshot.scrollbarWidth > 0) {
      const previousPadding = parseFloat(snapshot.body.paddingRight);
      const basePadding = Number.isFinite(previousPadding) ? previousPadding : 0;
      document.body.style.paddingRight = `${basePadding + snapshot.scrollbarWidth}px`;
    }
  }
  restoreSnapshot(snapshot) {
    const html = document.documentElement;
    const body = document.body;
    html.style.overflow = snapshot.documentElement.overflow;
    html.style.overflowX = snapshot.documentElement.overflowX;
    html.style.overflowY = snapshot.documentElement.overflowY;
    html.style.position = snapshot.documentElement.position;
    html.style.top = snapshot.documentElement.top;
    html.style.left = snapshot.documentElement.left;
    html.style.width = snapshot.documentElement.width;
    html.style.paddingRight = snapshot.documentElement.paddingRight;
    body.style.overflow = snapshot.body.overflow;
    body.style.overflowX = snapshot.body.overflowX;
    body.style.overflowY = snapshot.body.overflowY;
    body.style.position = snapshot.body.position;
    body.style.top = snapshot.body.top;
    body.style.left = snapshot.body.left;
    body.style.width = snapshot.body.width;
    body.style.paddingRight = snapshot.body.paddingRight;
    window.scrollTo(snapshot.scrollX, snapshot.scrollY);
  }
  isScrollKey(event) {
    return SCROLL_KEYS.has(event.key);
  }
  isWithinEditableInPopover(event) {
    const target = event.target;
    const isEditable = target instanceof HTMLInputElement || target instanceof HTMLTextAreaElement || target instanceof HTMLElement && target.isContentEditable === true;
    if (!isEditable) {
      return false;
    }
    return target.closest(".driver-popover") !== null;
  }
};
var PoUserGuideService = class _PoUserGuideService extends PoUserGuideBaseService {
  sanitizer;
  driverFactoryCache = null;
  driverInstance = null;
  pendingEndReason = null;
  scrollLock = new PoUserGuideScrollLock();
  activeScrollLockEnabled = false;
  static stylesInjected = false;
  constructor(languageService, sanitizer) {
    super(languageService);
    this.sanitizer = sanitizer;
  }
  isBrowser() {
    return typeof window !== "undefined" && typeof document !== "undefined";
  }
  importDriver() {
    return import("./driver.js-SEOT6EMG.js");
  }
  loadDriverFactory() {
    return __async(this, null, function* () {
      if (this.driverFactoryCache !== null) {
        return this.driverFactoryCache;
      }
      try {
        const mod = yield this.importDriver();
        this.driverFactoryCache = mod.driver;
        return this.driverFactoryCache;
      } catch (error2) {
        throw new Error("PoUserGuideService: não foi possível carregar driver.js. Verifique se a dependência está instalada.");
      }
    });
  }
  injectStyles() {
    if (!this.isBrowser()) {
      return;
    }
    if (_PoUserGuideService.stylesInjected === true) {
      return;
    }
    const existingStyle = document.head.querySelector('style[data-po-user-guide-styles="true"]');
    if (existingStyle !== null) {
      _PoUserGuideService.stylesInjected = true;
      return;
    }
    const style2 = document.createElement("style");
    style2.dataset["poUserGuideStyles"] = "true";
    style2.textContent = `.po-user-guide-popover { font-family: var(--font-family-theme, system-ui, -apple-system, sans-serif); }`;
    document.head.appendChild(style2);
    _PoUserGuideService.stylesInjected = true;
  }
  sanitizeText(value) {
    if (typeof value !== "string") {
      return value;
    }
    return this.sanitizer.sanitize(SecurityContext.HTML, value);
  }
  mapStepsToDriveSteps(steps, options) {
    return steps.map((step, index) => {
      const popover = {
        title: this.sanitizeText(step.title),
        description: this.sanitizeText(step.content),
        side: step.position && step.position !== "auto" ? step.position : void 0,
        align: step.align ?? void 0,
        nextBtnText: this.sanitizeText(step.nextLabel ?? options.nextLabel),
        prevBtnText: this.sanitizeText(step.previousLabel ?? options.previousLabel),
        doneBtnText: this.sanitizeText(step.doneLabel ?? options.doneLabel),
        popoverClass: ["po-user-guide-popover", options.popoverClass].filter(Boolean).join(" "),
        showButtons: this.resolveShowButtons(step, options),
        onPopoverRender: (popoverDom) => this.handlePopoverRender(popoverDom, step, index, steps.length, options)
      };
      return {
        element: step.element,
        popover,
        onHighlightStarted: step.onBeforeHighlight !== void 0 ? () => step.onBeforeHighlight(step, index) : void 0,
        onHighlighted: step.onHighlighted !== void 0 ? () => step.onHighlighted(step, index) : void 0,
        onDeselected: step.onDeselected !== void 0 ? () => step.onDeselected(step, index) : void 0
      };
    });
  }
  handlePopoverRender(popoverDom, step, index, totalSteps, options) {
    if (popoverDom?.wrapper) {
      popoverDom.wrapper.setAttribute("role", "dialog");
      const ariaLabel = typeof step.title === "string" && step.title.length > 0 ? step.title : step.content.slice(0, 100);
      popoverDom.wrapper.setAttribute("aria-label", ariaLabel);
      this.applyPoButtonStyles(popoverDom);
      this.applyDoneButtonLabel(popoverDom, step, index, totalSteps, options);
      this.focusPopoverDescription(popoverDom);
    }
  }
  focusPopoverDescription(popoverDom) {
    const description = popoverDom.description;
    if (!description) {
      return;
    }
    description.setAttribute("tabindex", "-1");
    window.requestAnimationFrame(() => {
      description.focus({
        preventScroll: true
      });
    });
  }
  applyDoneButtonLabel(popoverDom, step, index, totalSteps, options) {
    const nextButton = popoverDom.nextButton;
    if (!nextButton || index !== totalSteps - 1) {
      return;
    }
    const doneLabel = step.doneLabel ?? options.doneLabel;
    if (doneLabel) {
      nextButton.textContent = doneLabel;
      nextButton.setAttribute("aria-label", doneLabel);
    }
  }
  applyPoButtonStyles(popoverDom) {
    const previousButton = popoverDom.previousButton;
    const nextButton = popoverDom.nextButton;
    const closeButton = popoverDom.closeButton;
    if (previousButton) {
      previousButton.classList.add("po-user-guide-button", "po-user-guide-button-secondary");
      previousButton.setAttribute("type", "button");
    }
    if (nextButton) {
      nextButton.classList.add("po-user-guide-button", "po-user-guide-button-primary");
      nextButton.setAttribute("type", "button");
    }
    if (closeButton) {
      closeButton.classList.add("po-user-guide-button-close");
      closeButton.setAttribute("type", "button");
      closeButton.setAttribute("aria-label", this.literals.close);
      this.applyCloseButtonIcon(closeButton);
    }
  }
  /**
   * Substitui o `&times;` nativo do Driver.js pelo ícone de fechar do PO UI (`ICON_CLOSE`).
   *
   * O ícone é referenciado pela classe da fonte (`an an-x`) via `AnimaliaIconDictionary`,
   * evitando duplicar o codepoint da fonte Animalia diretamente no CSS.
   */
  applyCloseButtonIcon(closeButton) {
    if (closeButton.querySelector(".po-user-guide-button-close-icon") !== null) {
      return;
    }
    const icon = document.createElement("i");
    icon.className = `po-user-guide-button-close-icon ${AnimaliaIconDictionary["ICON_CLOSE"]}`;
    icon.setAttribute("aria-hidden", "true");
    closeButton.textContent = "";
    closeButton.appendChild(icon);
  }
  buildDriverConfig(_startIndex) {
    const opts = this.options;
    return {
      steps: this.mapStepsToDriveSteps(this.steps, opts),
      allowClose: opts.allowClose,
      showProgress: opts.showProgress,
      keyboardControl: opts.keyboardControl,
      overlayColor: "var(--color-po-user-guide-overlay, rgb(0, 0, 0))",
      overlayOpacity: opts.overlayOpacity,
      nextBtnText: this.sanitizeText(opts.nextLabel),
      prevBtnText: this.sanitizeText(opts.previousLabel),
      doneBtnText: this.sanitizeText(opts.doneLabel),
      progressText: this.sanitizeText(opts.progressTemplate),
      popoverClass: ["po-user-guide-popover", opts.popoverClass].filter(Boolean).join(" "),
      stagePadding: 6,
      onHighlightStarted: (element, step, options) => this.handleHighlightStarted(element, step, options),
      onDestroyed: () => this.handleDestroyed(),
      onCloseClick: () => this.handleCloseClick()
    };
  }
  handleHighlightStarted(_element, _step, options) {
    const newIndex = this.resolveActiveIndex(options);
    if (newIndex === null) {
      return;
    }
    const previousIndex = this.currentIndex;
    let direction;
    if (previousIndex === -1) {
      direction = "start";
    } else if (newIndex === previousIndex + 1) {
      direction = "next";
    } else if (newIndex === previousIndex - 1) {
      direction = "previous";
    } else {
      direction = "goto";
    }
    this.currentIndex = newIndex;
    const event = {
      step: this.steps[newIndex],
      index: newIndex,
      direction,
      totalSteps: this.steps.length
    };
    if (typeof this.options.onStepChange === "function") {
      this.options.onStepChange(event);
    }
    this._stepChange.next(event);
  }
  resolveActiveIndex(options) {
    const candidates = [];
    if (options?.state != null) {
      candidates.push(options.state.activeIndex);
    }
    if (this.driverInstance !== null && typeof this.driverInstance.getActiveIndex === "function") {
      try {
        candidates.push(this.driverInstance.getActiveIndex());
      } catch (e2) {
      }
    }
    for (const candidate of candidates) {
      if (typeof candidate === "number" && Number.isInteger(candidate) && candidate >= 0 && candidate < this.steps.length) {
        return candidate;
      }
    }
    return null;
  }
  resolveShowButtons(step, options) {
    const showButtons = step.showButtons;
    if (options.allowClose === false && Array.isArray(showButtons)) {
      return showButtons.filter((button) => button !== "close");
    }
    if (options.allowClose === false) {
      return ["next", "previous"];
    }
    return showButtons ?? void 0;
  }
  handleDestroyed() {
    if (this.driverInstance === null && this.currentIndex === -1) {
      return;
    }
    const reason = this.pendingEndReason ?? "closed";
    const lastIndex = this.currentIndex >= 0 ? this.currentIndex : 0;
    const totalSteps = this.steps.length;
    this.driverInstance = null;
    this.currentIndex = -1;
    this.pendingEndReason = null;
    this.scrollLock.unlock();
    this.activeScrollLockEnabled = false;
    this._tourEnd.next({
      reason,
      lastIndex,
      totalSteps
    });
  }
  handleCloseClick() {
    if (this.options.allowClose === false) {
      return;
    }
    this.scrollLock.unlock();
    this.activeScrollLockEnabled = false;
    this.pendingEndReason = "closed";
    if (this.driverInstance !== null && typeof this.driverInstance.destroy === "function") {
      this.driverInstance.destroy();
    }
  }
  /**
   * @description
   *
   * Configura a lista de passos que serão exibidos durante a execução do tour guiado.
   *
   * @param {Array<PoUserGuideStep>} steps Lista de passos do tour a ser configurada. Deve conter ao menos um
   * passo válido com a propriedade `content` definida.
   *
   * @returns {this} A própria instância do serviço, possibilitando o encadeamento fluente de chamadas.
   *
   * @throws {Error} Quando `steps` é `null`, `undefined`, não é um `Array`, está vazio, contém um passo
   * inválido, contém um passo sem a propriedade `content` definida ou contém um passo cujo `element` é
   * uma string com sintaxe de seletor CSS inválida.
   */
  setSteps(steps) {
    this.validateSteps(steps);
    this.steps = [...steps];
    return this;
  }
  /**
   * @description
   *
   * Configura as opções globais que serão aplicadas durante a execução do tour guiado.
   *
   * @param {PoUserGuideOptions} [options] Objeto com as opções globais do tour a ser configurado. Quando
   * omitido ou `undefined`, apenas os valores padrão do PO UI são aplicados.
   *
   * @returns {this} A própria instância do serviço, possibilitando o encadeamento fluente de chamadas.
   */
  setOptions(options) {
    this.options = this.resolveOptions(options);
    return this;
  }
  /**
   * @description
   *
   * Inicia a execução do tour previamente configurado por meio de `setSteps` e, opcionalmente, `setOptions`.
   *
   * @override
   *
   * @param {number} [startIndex=0] Índice, com base zero, do passo a partir do qual o tour deve iniciar.
   * Deve ser um inteiro contido no intervalo `[0, steps.length - 1]`.
   *
   * @throws {Error} Quando a lista de passos ainda não foi configurada via `setSteps` ou está vazia.
   * @throws {Error} Quando `startIndex` não é um número inteiro contido em `[0, steps.length - 1]`.
   *
   * @returns {Promise<void>} `Promise` que é resolvida quando o tour é iniciado com sucesso e
   * rejeitada com um `Error` de mensagem padronizada quando ocorre falha na carga da dependência
   * `driver.js` ou na inicialização do tour.
   */
  start(startIndex = 0) {
    if (!this.isBrowser()) {
      console.warn("PoUserGuideService: start() está disponível apenas em ambiente de browser.");
      return Promise.resolve();
    }
    if (this.steps === null || this.steps === void 0 || this.steps.length === 0) {
      throw new Error("PoUserGuideService: a lista de passos não foi configurada. Chame setSteps() antes de start().");
    }
    if (typeof startIndex !== "number" || !Number.isInteger(startIndex) || startIndex < 0 || startIndex >= this.steps.length) {
      throw new Error(`PoUserGuideService: startIndex (${startIndex}) está fora do intervalo [0, ${this.steps.length - 1}].`);
    }
    if (this.driverInstance !== null) {
      this.close();
      this.scrollLock.unlock();
    }
    if (this.options === null || this.options === void 0 || Object.keys(this.options).length === 0) {
      this.options = this.resolveOptions();
    }
    this.injectStyles();
    const mode = resolveScrollLockMode(this.options);
    this.activeScrollLockEnabled = mode === "blocked";
    return this.loadDriverFactory().catch(() => {
      this.scrollLock.unlock();
      this.activeScrollLockEnabled = false;
      const loadError = new Error("PoUserGuideService: não foi possível carregar driver.js. Verifique se a dependência está instalada.");
      console.error(loadError);
      return Promise.reject(loadError);
    }).then((factory) => {
      try {
        const config = this.buildDriverConfig(startIndex);
        this.driverInstance = factory(config);
        if (this.activeScrollLockEnabled) {
          this.scrollLock.lock();
        }
        this._tourStart.next({
          totalSteps: this.steps.length,
          startIndex,
          timestamp: Date.now()
        });
        this.driverInstance.drive(startIndex);
      } catch (e2) {
        this.scrollLock.unlock();
        this.driverInstance = null;
        this.currentIndex = -1;
        this.pendingEndReason = null;
        this.activeScrollLockEnabled = false;
        const initError = new Error("PoUserGuideService: não foi possível inicializar o tour guiado. Verifique a configuração dos passos e das opções.");
        console.error(initError);
        return Promise.reject(initError);
      }
      return void 0;
    });
  }
  /**
   * @description
   *
   * Encerra o tour em execução, disparando, em sequência, o ciclo de limpeza de estado e a emissão de
   * `tourEnd$`.
   *
   * @override
   */
  close() {
    if (this.driverInstance === null) {
      return;
    }
    this.pendingEndReason = "closed";
    try {
      this.driverInstance.destroy();
    } finally {
      if (this.scrollLock.isActive()) {
        this.scrollLock.unlock();
        this.activeScrollLockEnabled = false;
      }
    }
  }
  /**
   * @description
   *
   * Encerra o tour em execução.
   *
   * @override
   */
  exit() {
    this.close();
  }
  /**
   * @description
   *
   * Avança o tour em execução para o próximo passo.
   *
   * @override
   */
  next() {
    if (this.driverInstance === null) {
      return;
    }
    if (this.currentIndex === this.steps.length - 1) {
      this.pendingEndReason = "completed";
      this.driverInstance.destroy();
      return;
    }
    this.driverInstance.moveNext();
  }
  /**
   * @description
   *
   * Retrocede o tour em execução para o passo anterior.
   *
   * @override
   */
  previous() {
    if (this.driverInstance === null || this.currentIndex <= 0) {
      return;
    }
    this.driverInstance.movePrevious();
  }
  /**
   * @description
   *
   * Move o tour diretamente para o passo identificado pelo `index` informado, que deve ser um número inteiro contido em `[0, steps.length - 1]`. Se o tour ainda não estiver em execução, o método inicia o tour a partir do passo indicado por `index`.
   *
   * @override
   *
   * @param {number} index Índice, com base zero, do passo de destino. Deve ser um inteiro contido
   * em `[0, steps.length - 1]`.
   *
   * @throws {Error} Quando `index` não é um número inteiro contido em `[0, steps.length - 1]`.
   */
  goTo(index) {
    if (typeof index !== "number" || !Number.isInteger(index) || index < 0 || index >= this.steps.length) {
      throw new Error(`PoUserGuideService: goTo(${index}) está fora do intervalo [0, ${this.steps.length - 1}].`);
    }
    if (this.driverInstance === null) {
      this.start(index);
      return;
    }
    this.driverInstance.moveTo(index);
  }
  /**
   * @description
   *
   * Indica se há um tour em execução no momento da consulta.
   *
   * @returns {boolean} `true` quando existe um tour em execução; `false` em qualquer outro caso (antes do primeiro `start()`,
   * após `close()`/`exit()` ou após o encerramento natural do tour).
   */
  isActive() {
    return this.driverInstance !== null && this.currentIndex >= 0;
  }
  /**
   * @description
   *
   * Retorna o `PoUserGuideStep` correspondente ao passo ativo no momento da consulta.
   *
   * @returns {PoUserGuideStep | null} O passo ativo, quando há um tour em execução; `null` em qualquer
   * outro caso.
   */
  getCurrentStep() {
    return this.isActive() ? this.steps[this.currentIndex] : null;
  }
  /**
   * @description
   *
   * Retorna o índice, com base zero, do passo ativo no momento da consulta.
   *
   * @returns {number} O índice, com base zero, do passo ativo, contido em `[0, steps.length - 1]`,
   * quando há um tour em execução; `-1` em qualquer outro caso.
   */
  getCurrentIndex() {
    return this.isActive() ? this.currentIndex : -1;
  }
  static ɵfac = function PoUserGuideService_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoUserGuideService)(ɵɵinject(PoLanguageService), ɵɵinject(DomSanitizer));
  };
  static ɵprov = ɵɵdefineInjectable({
    token: _PoUserGuideService,
    factory: _PoUserGuideService.ɵfac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoUserGuideService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: PoLanguageService
  }, {
    type: DomSanitizer
  }], null);
})();
var PoDatetimepickerLiterals = {
  en: {
    open: "Open calendar",
    clean: "Clear field",
    invalidDatetime: "Invalid date/time",
    datetimeOutOfRange: "Date/time out of range"
  },
  es: {
    open: "Calendario abierto",
    clean: "Limpiar campo",
    invalidDatetime: "Fecha/hora inválida",
    datetimeOutOfRange: "Fecha/hora fuera del rango"
  },
  pt: {
    open: "Abrir calendário",
    clean: "Limpar campo",
    invalidDatetime: "Data/hora inválida",
    datetimeOutOfRange: "Data/hora fora do período"
  },
  ru: {
    open: "Открыть календарь",
    clean: "Очистить поле",
    invalidDatetime: "Неверная дата/время",
    datetimeOutOfRange: "Дата/время вне диапазона"
  }
};
var PoDatetimepickerFormatByLocale = {
  en: "mm/dd/yyyy",
  es: "dd/mm/yyyy",
  pt: "dd/mm/yyyy",
  ru: "dd/mm/yyyy"
};
var PoDatetimepickerTimeFormatByLocale = {
  en: "12",
  es: "24",
  pt: "24",
  ru: "24"
};
var poDatepickerFormatDefault = "dd/mm/yyyy";
var PoDatetimepickerBaseComponent = class _PoDatetimepickerBaseComponent {
  languageService;
  _clean = false;
  _disabled = false;
  _initialSize = void 0;
  _format;
  _locale;
  _noAutocomplete = false;
  _placeholder = "";
  _readonly = false;
  _required = false;
  _size = void 0;
  _date;
  _timeValue = "";
  _resolvedMinDate;
  _resolvedMaxDate;
  hasValidatorRequired;
  objMask;
  onChangeModel = null;
  onTouchedModel = null;
  validatorChange;
  shortLanguage;
  // --- Inputs ---
  /**
   * @optional
   *
   * @description
   *
   * Define que o `calendar` e/ou tooltip serão incluídos no body da página e não dentro do componente.
   *
   * @default `false`
   */
  appendBox = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "appendBox"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-append-in-body",
    transform: convertToBoolean
  }));
  /**
   * @optional
   *
   * @description
   *
   * Aplica foco no elemento ao ser iniciado.
   *
   * @default `false`
   */
  autoFocus = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "autoFocus"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-auto-focus",
    transform: convertToBoolean
  }));
  /**
   * @optional
   *
   * @description
   *
   * Habilita ação para limpar o campo.
   *
   * @default `false`
   */
  clean = input(void 0, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "clean"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-clean"
  }));
  /**
   * @optional
   *
   * @description
   *
   * Define se o título do campo será exibido de forma compacta.
   *
   * @default `false`
   */
  compactLabel = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "compactLabel"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-compact-label",
    transform: convertToBoolean
  }));
  /**
   * @optional
   *
   * @description
   *
   * Desabilita o campo.
   *
   * @default `false`
   */
  disabled = input(void 0, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "disabled"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-disabled"
  }));
  /**
   * @optional
   *
   * @description
   *
   * Limita a exibição da mensagem de erro a duas linhas e exibe um tooltip com o texto completo.
   *
   * @default `false`
   */
  errorLimit = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "errorLimit"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-error-limit",
    transform: convertToBoolean
  }));
  /**
   * @optional
   *
   * @description
   *
   * Mensagem apresentada quando a data/hora for inválida ou fora do período.
   */
  errorPattern = model("", __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "errorPattern"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-error-pattern"
  }));
  /**
   * @optional
   *
   * @description
   *
   * Texto de apoio do campo.
   */
  help = input(void 0, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "help"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-help"
  }));
  /**
   * @optional
   *
   * @description
   *
   * Rótulo do campo.
   */
  label = input(void 0, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "label"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-label"
  }));
  /**
   * @optional
   *
   * @description
   *
   * Habilita a quebra automática do texto da propriedade `p-label`.
   *
   * @default `false`
   */
  labelTextWrap = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "labelTextWrap"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-label-text-wrap",
    transform: convertToBoolean
  }));
  /**
   * @optional
   *
   * @description
   *
   * Exibe um ícone de carregamento no lado direito do campo.
   *
   * @default `false`
   */
  loading = input(void 0, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "loading"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-loading"
  }));
  isLoading = computed(() => convertToBoolean(this.loading()), ...ngDevMode ? [{
    debugName: "isLoading"
  }] : (
    /* istanbul ignore next */
    []
  ));
  /**
   * @optional
   *
   * @description
   *
   * Idioma do componente.
   *
   * > O locale padrão será recuperado com base no [`PoI18nService`](/documentation/po-i18n) ou *browser*.
   */
  localeInput = input(void 0, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "localeInput"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-locale"
  }));
  /**
   * @optional
   *
   * @description
   *
   * Define uma data máxima para o `po-datetimepicker`.
   * Datas posteriores ao limite ficam desabilitadas no calendário.
   *
   * Aceita os formatos:
   * - `Date` object: `new Date(2026, 4, 31)`
   * - ISO string: `'2026-05-31'`
   * - ISO com hora: `'2026-05-31T23:59:59-03:00'`
   */
  maxDateInput = input(void 0, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "maxDateInput"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-max-date"
  }));
  /**
   * @optional
   *
   * @description
   *
   * Define o horário máximo permitido para seleção no timer.
   * Horários posteriores ao limite ficam desabilitados.
   *
   * Formato aceito: `HH:mm` ou `HH:mm:ss`.
   */
  maxTime = input(void 0, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "maxTime"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-max-time"
  }));
  /**
   * @optional
   *
   * @description
   *
   * Define uma data mínima para o `po-datetimepicker`.
   * Datas anteriores ao limite ficam desabilitadas no calendário.
   *
   * Aceita os formatos:
   * - `Date` object: `new Date(2026, 0, 1)`
   * - ISO string: `'2026-01-01'`
   * - ISO com hora: `'2026-01-01T00:00:00-03:00'`
   */
  minDateInput = input(void 0, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "minDateInput"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-min-date"
  }));
  /**
   * @optional
   *
   * @description
   *
   * Define o horário mínimo permitido para seleção no timer.
   * Horários anteriores ao limite ficam desabilitados.
   *
   * Formato aceito: `HH:mm` ou `HH:mm:ss`.
   */
  minTime = input(void 0, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "minTime"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-min-time"
  }));
  /**
   * @optional
   *
   * @description
   *
   * Define o intervalo entre os minutos exibidos no painel do timer.
   *
   * @default `5`
   */
  minuteInterval = input(void 0, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "minuteInterval"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-minute-interval"
  }));
  /**
   * @optional
   *
   * @description
   *
   * Nome do componente.
   */
  name = input(void 0, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "name"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "name"
  }));
  /**
   * @optional
   *
   * @description
   *
   * Define a propriedade nativa `autocomplete` do campo como `off`.
   *
   * @default `false`
   */
  noAutocomplete = input(void 0, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "noAutocomplete"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-no-autocomplete"
  }));
  /**
   * @optional
   *
   * @description
   *
   * Define se a indicação de campo opcional será exibida.
   *
   * @default `false`
   */
  optional = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "optional"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-optional",
    transform: convertToBoolean
  }));
  /**
   * @optional
   *
   * @description
   *
   * Mensagem que aparecerá enquanto o campo não estiver preenchido.
   */
  placeholder = input(void 0, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "placeholder"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-placeholder"
  }));
  /**
   * @optional
   *
   * @description
   *
   * Define as opções do componente de ajuda (po-helper).
   *
   * > Para mais informações acesse: https://po-ui.io/documentation/po-helper.
   */
  poHelperComponent = input(void 0, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "poHelperComponent"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-helper"
  }));
  /**
   * @optional
   *
   * @description
   *
   * Torna o componente somente leitura.
   *
   * @default `false`
   */
  readonly = input(void 0, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "readonly"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-readonly"
  }));
  /**
   * @optional
   *
   * @description
   *
   * Define que o campo será obrigatório.
   *
   * @default `false`
   */
  required = input(void 0, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "required"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-required"
  }));
  /**
   * @optional
   *
   * @description
   *
   * Define o intervalo entre os segundos exibidos no painel do timer.
   * Utilizado apenas quando `p-show-seconds` está ativo.
   *
   * @default `1`
   */
  secondInterval = input(void 0, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "secondInterval"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-second-interval"
  }));
  /**
   * @optional
   *
   * @description
   *
   * Exibe a mensagem setada na propriedade `p-error-pattern` se o campo estiver vazio e for requerido.
   *
   * > Necessário que a propriedade `p-required` esteja habilitada.
   *
   * @default `false`
   */
  showErrorMessageRequired = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "showErrorMessageRequired"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-required-field-error-message",
    transform: convertToBoolean
  }));
  /**
   * @optional
   *
   * @description
   *
   * Define se a indicação de campo obrigatório será exibida.
   *
   * @default `false`
   */
  showRequired = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "showRequired"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-show-required",
    transform: convertToBoolean
  }));
  /**
   * @optional
   *
   * @description
   *
   * Exibe a coluna de segundos no painel de seleção do timer.
   *
   * @default `false`
   */
  showSeconds = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "showSeconds"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-show-seconds",
    transform: convertToBoolean
  }));
  /**
   * @optional
   *
   * @description
   *
   * Define o tamanho do componente:
   * - `small`: altura do input como 32px (disponível apenas para acessibilidade AA).
   * - `medium`: altura do input como 44px.
   *
   * @default `medium`
   */
  size = input(void 0, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "size"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-size"
  }));
  /**
   * @optional
   *
   * @description
   *
   * Define o formato de exibição do timer.
   *
   * Valores válidos:
   * - `24`: formato de 24 horas (padrão para pt, es, ru)
   * - `12`: formato de 12 horas com indicador AM/PM (padrão para en)
   *
   * Quando não informado, o formato será determinado automaticamente pelo locale:
   * - `en` → 12h (AM/PM)
   * - `pt`, `es`, `ru` → 24h
   *
   * @default Determinado pelo locale
   */
  timerFormat = input(void 0, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "timerFormat"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-format-time"
  }));
  /**
   * @optional
   *
   * @description
   *
   * Define o formato de exibição da data.
   *
   * Valores válidos:
   * - `dd/mm/yyyy`
   * - `mm/dd/yyyy`
   * - `yyyy/mm/dd`
   *
   * Quando não informado, o formato será determinado automaticamente pelo locale:
   * - `en` → `mm/dd/yyyy`
   * - `pt`, `es`, `ru` → `dd/mm/yyyy`
   *
   * @default Determinado pelo locale
   */
  dateFormat = input(void 0, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "dateFormat"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-format-date"
  }));
  // --- Outputs ---
  /**
   * @optional
   *
   * @description
   *
   * Evento disparado ao sair do campo (blur).
   */
  onblur = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Evento disparado ao alterar valor do campo.
   */
  onchange = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Evento disparado quando uma tecla é pressionada enquanto o foco está no componente.
   */
  keydown = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Evento disparado sempre que o valor do model é alterado, seja por interação do usuário
   * ou por atualização programática (ex: `setValue`, `patchValue`, carregamento assíncrono).
   *
   * Diferentemente do `p-change`, que é disparado apenas por interação do usuário,
   * o `p-change-model` cobre todos os cenários de alteração de valor.
   *
   * Não emite quando o novo valor é idêntico ao anterior (deduplicação automática).
   */
  changeModel = new EventEmitter();
  modelLastUpdate;
  constructor(languageService) {
    this.languageService = languageService;
    effect(() => {
      const value = this.clean();
      this._clean = value === "" ? true : convertToBoolean(value);
    });
    effect(() => {
      const value = this.disabled();
      this._disabled = value === "" ? true : convertToBoolean(value);
      this.validateModel(this.getModelValue());
    });
    effect(() => {
      const value = this.localeInput();
      if (value) {
        this._locale = value.length >= 2 ? value : poLocaleDefault;
      } else {
        this._locale = this.shortLanguage;
      }
      this._format = this.resolveFormat();
      this.objMask = this.buildMask(replaceFormatSeparator(this.format, this.languageService.getDateSeparator(this.localeInput())));
      this.refreshValue(this._date);
    });
    effect(() => {
      const value = this.dateFormat();
      if (value) {
        this._format = this.resolveFormat();
        this.objMask = this.buildMask(replaceFormatSeparator(this.format, this.languageService.getDateSeparator(this.localeInput())));
        this.refreshValue(this._date);
      }
    });
    effect(() => {
      const value = this.noAutocomplete();
      this._noAutocomplete = convertToBoolean(value);
    });
    effect(() => {
      const value = this.placeholder();
      this._placeholder = isTypeof(value, "string") ? value : "";
    });
    effect(() => {
      const value = this.readonly();
      this._readonly = value === "" ? true : convertToBoolean(value);
    });
    effect(() => {
      const value = this.required();
      this._required = value === "" ? true : convertToBoolean(value);
      this.validateModel(this.getModelValue());
    });
    effect(() => {
      const value = this.size();
      this._initialSize = value;
      this.applySizeBasedOnA11y();
    });
    effect(() => {
      const value = this.minDateInput();
      if (!value) {
        this._resolvedMinDate = void 0;
      } else if (value instanceof Date) {
        this._resolvedMinDate = new Date(value.getFullYear(), value.getMonth(), value.getDate(), 0, 0, 0);
      } else {
        this._resolvedMinDate = convertIsoToDate(value, true, false);
      }
    });
    effect(() => {
      const value = this.maxDateInput();
      if (!value) {
        this._resolvedMaxDate = void 0;
      } else if (value instanceof Date) {
        this._resolvedMaxDate = new Date(value.getFullYear(), value.getMonth(), value.getDate(), 23, 59, 59);
      } else {
        this._resolvedMaxDate = convertIsoToDate(value, false, true);
      }
    });
  }
  // --- ControlValueAccessor ---
  writeValue(value) {
    if (!value) {
      this._date = void 0;
      this._timeValue = "";
      this.refreshValue(this._date);
      return;
    }
    if (value instanceof Date) {
      this._date = new Date(value.getFullYear(), value.getMonth(), value.getDate());
      this._timeValue = this.extractTimeFromDate(value);
    } else if (typeof value === "string") {
      this.processStringValue(value);
    }
    this.refreshValue(this._date);
    if (this._date && this._timeValue) {
      this.callOnChange(this.getModelValue());
    }
  }
  registerOnChange(fn) {
    this.onChangeModel = fn;
  }
  registerOnTouched(fn) {
    this.onTouchedModel = fn;
  }
  setDisabledState(isDisabled) {
    this._disabled = isDisabled;
  }
  registerOnValidatorChange(fn) {
    this.validatorChange = fn;
  }
  // --- Validator ---
  validate(c) {
    if (!this.hasValidatorRequired && this.showErrorMessageRequired() && c.hasValidator(Validators.required)) {
      this.hasValidatorRequired = true;
    }
    if (dateFailed(c.value)) {
      this.errorPattern.set(this.errorPattern() || PoDatetimepickerLiterals[this.locale]?.invalidDatetime || PoDatetimepickerLiterals["pt"].invalidDatetime);
      return {
        date: {
          valid: false
        }
      };
    }
    if (this.requiredFailed(c.value)) {
      return {
        required: {
          valid: false
        }
      };
    }
    if (this._date && !this.isDateInRange()) {
      return {
        date: {
          valid: false
        }
      };
    }
    if (this._timeValue && !this.isTimeInRange(this._timeValue)) {
      return {
        time: {
          valid: false
        }
      };
    }
    return null;
  }
  // --- Getters / Setters ---
  get date() {
    return this._date;
  }
  set date(value) {
    this._date = value;
  }
  get timeValue() {
    return this._timeValue;
  }
  set timeValue(value) {
    this._timeValue = value;
  }
  get format() {
    return this._format || this.resolveFormat();
  }
  get resolvedTimerFormat() {
    const explicit = this.timerFormat();
    if (explicit) {
      return explicit;
    }
    const localeTimeFormat = PoDatetimepickerTimeFormatByLocale[this.locale] || "24";
    return localeTimeFormat === "12" ? PoTimerFormat.Format12 : PoTimerFormat.Format24;
  }
  get is12HourFormat() {
    return this.resolvedTimerFormat === PoTimerFormat.Format12;
  }
  get isClean() {
    return this._clean;
  }
  get isDisabled() {
    return this._disabled || this.isLoading();
  }
  get isNoAutocomplete() {
    return this._noAutocomplete;
  }
  get placeholderValue() {
    return this._placeholder;
  }
  get isReadonly() {
    return this._readonly;
  }
  get isRequired() {
    return this._required;
  }
  get locale() {
    return this._locale || this.shortLanguage;
  }
  get resolvedSize() {
    return this._size ?? getDefaultSizeFn(PoFieldSize);
  }
  get hostSize() {
    return this.resolvedSize;
  }
  onThemeChange() {
    this.applySizeBasedOnA11y();
  }
  mapSizeToIcon(size) {
    return mapInputSizeToLoadingIcon(size);
  }
  getModelValue() {
    if (!this._date) {
      return "";
    }
    const dateISO = PoUtils.convertDateToISODate(this._date);
    const time = this._timeValue || "00:00";
    const timezone = this.getTimezoneOffset(this._date);
    return `${dateISO}T${time}${timezone}`;
  }
  getTimezoneOffset(date) {
    const referenceDate = date || /* @__PURE__ */ new Date();
    const offset = referenceDate.getTimezoneOffset();
    const offsetAbsolute = Math.abs(offset);
    const sign = offset <= 0 ? "+" : "-";
    const hours = ("00" + Math.floor(offsetAbsolute / 60)).slice(-2);
    const minutes = ("00" + offsetAbsolute % 60).slice(-2);
    return `${sign}${hours}:${minutes}`;
  }
  callOnChange(value, retry = true) {
    if (this.onChangeModel) {
      this.onChangeModel(value);
      this.controlChangeModelEmitter(value);
    } else if (retry) {
      setTimeout(() => this.callOnChange(value, false));
    }
  }
  controlChangeModelEmitter(value) {
    if (this.modelLastUpdate !== value) {
      this.changeModel.emit(value);
      this.modelLastUpdate = value;
    }
  }
  controlModel() {
    const modelValue = this.getModelValue();
    this.callOnChange(modelValue || "");
  }
  formatTimeForDisplay(time) {
    if (!time) {
      return this.is12HourFormat ? "12:00" : "00:00";
    }
    const parts = time.split(":");
    if (!this.showSeconds() && parts.length > 2) {
      time = `${parts[0]}:${parts[1]}`;
    }
    if (!this.is12HourFormat) {
      return time;
    }
    let hours = Number.parseInt(parts[0], 10);
    const minutes = parts[1];
    const seconds = this.showSeconds() && parts.length > 2 ? parts[2] : null;
    if (hours === 0) {
      hours = 12;
    } else if (hours > 12) {
      hours -= 12;
    }
    const hoursStr = ("0" + hours).slice(-2);
    return seconds ? `${hoursStr}:${minutes}:${seconds}` : `${hoursStr}:${minutes}`;
  }
  // --- Protected / Private ---
  buildMask(format2 = this.format) {
    let mask = format2.toUpperCase();
    mask = mask.replace(/DD/g, "99");
    mask = mask.replace(/MM/g, "99");
    mask = mask.replace(/YYYY/g, "9999");
    if (this.showSeconds()) {
      mask += " 99:99:99";
    } else {
      mask += " 99:99";
    }
    return new PoMask(mask, true);
  }
  validateModel(value) {
    if (this.validatorChange) {
      this.validatorChange();
    }
  }
  get resolvedMinDate() {
    return this._resolvedMinDate;
  }
  get resolvedMaxDate() {
    return this._resolvedMaxDate;
  }
  isDateInRange() {
    if (!this._date) {
      return true;
    }
    return PoUtils.validateDateRange(this._date, this.resolvedMinDate, this.resolvedMaxDate);
  }
  isTimeInRange(time) {
    if (!time) {
      return true;
    }
    const timeSeconds = this.timeToSeconds(time);
    const minTimeValue = this.minTime();
    const maxTimeValue = this.maxTime();
    if (minTimeValue) {
      const minSeconds = this.timeToSeconds(minTimeValue);
      if (timeSeconds < minSeconds) {
        return false;
      }
    }
    if (maxTimeValue) {
      const maxSeconds = this.timeToSeconds(maxTimeValue);
      if (timeSeconds > maxSeconds) {
        return false;
      }
    }
    return true;
  }
  timeToSeconds(time) {
    const parts = time.split(":");
    const hours = Number.parseInt(parts[0], 10) || 0;
    const minutes = Number.parseInt(parts[1], 10) || 0;
    const seconds = parts.length >= 3 ? Number.parseInt(parts[2], 10) || 0 : 0;
    return hours * 3600 + minutes * 60 + seconds;
  }
  resolveFormat() {
    const explicit = this.dateFormat?.();
    if (explicit) {
      const lower = explicit.toLowerCase();
      if (/dd/.test(lower) && /mm/.test(lower) && /yyyy/.test(lower)) {
        return lower;
      }
    }
    return PoDatetimepickerFormatByLocale[this.locale] || poDatepickerFormatDefault;
  }
  processStringValue(value) {
    if (value.includes("T")) {
      const tIndex = value.indexOf("T");
      const datePart = value.substring(0, tIndex);
      const timeAndTz = value.substring(tIndex + 1);
      const fullDate = new Date(value);
      if (!isNaN(fullDate.getTime())) {
        this._date = new Date(fullDate.getFullYear(), fullDate.getMonth(), fullDate.getDate());
        this._timeValue = this.extractTimeFromDate(fullDate);
      } else {
        const parsedDate = convertIsoToDate(datePart, false, false);
        this._date = parsedDate;
        this._timeValue = this.normalizeTimePart(timeAndTz);
      }
    } else {
      const parsedDate = convertIsoToDate(value, false, false);
      this._date = parsedDate;
      this._timeValue = "00:00";
    }
  }
  normalizeTimePart(timePart) {
    const timeOnly = timePart.replace(/Z$/, "").replace(/[+-]\d{2}:?\d{2}$/, "");
    return timeOnly.length >= 5 ? timeOnly.substring(0, 8) : timeOnly;
  }
  extractTimeFromDate(date) {
    const hours = ("0" + date.getHours()).slice(-2);
    const minutes = ("0" + date.getMinutes()).slice(-2);
    const seconds = ("0" + date.getSeconds()).slice(-2);
    if (this.showSeconds()) {
      return `${hours}:${minutes}:${seconds}`;
    }
    return `${hours}:${minutes}`;
  }
  requiredFailed(value) {
    return this._required && !this._disabled && !value;
  }
  applySizeBasedOnA11y() {
    const size = validateSizeFn(this._initialSize, PoFieldSize);
    this._size = size;
  }
  static ɵfac = function PoDatetimepickerBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoDatetimepickerBaseComponent)(ɵɵdirectiveInject(PoLanguageService));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoDatetimepickerBaseComponent,
    hostVars: 1,
    hostBindings: function PoDatetimepickerBaseComponent_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("PoUiThemeChange", function PoDatetimepickerBaseComponent_PoUiThemeChange_HostBindingHandler() {
          return ctx.onThemeChange();
        }, ɵɵresolveWindow);
      }
      if (rf & 2) {
        ɵɵattribute("p-size", ctx.hostSize);
      }
    },
    inputs: {
      appendBox: [1, "p-append-in-body", "appendBox"],
      autoFocus: [1, "p-auto-focus", "autoFocus"],
      clean: [1, "p-clean", "clean"],
      compactLabel: [1, "p-compact-label", "compactLabel"],
      disabled: [1, "p-disabled", "disabled"],
      errorLimit: [1, "p-error-limit", "errorLimit"],
      errorPattern: [1, "p-error-pattern", "errorPattern"],
      help: [1, "p-help", "help"],
      label: [1, "p-label", "label"],
      labelTextWrap: [1, "p-label-text-wrap", "labelTextWrap"],
      loading: [1, "p-loading", "loading"],
      localeInput: [1, "p-locale", "localeInput"],
      maxDateInput: [1, "p-max-date", "maxDateInput"],
      maxTime: [1, "p-max-time", "maxTime"],
      minDateInput: [1, "p-min-date", "minDateInput"],
      minTime: [1, "p-min-time", "minTime"],
      minuteInterval: [1, "p-minute-interval", "minuteInterval"],
      name: [1, "name"],
      noAutocomplete: [1, "p-no-autocomplete", "noAutocomplete"],
      optional: [1, "p-optional", "optional"],
      placeholder: [1, "p-placeholder", "placeholder"],
      poHelperComponent: [1, "p-helper", "poHelperComponent"],
      readonly: [1, "p-readonly", "readonly"],
      required: [1, "p-required", "required"],
      secondInterval: [1, "p-second-interval", "secondInterval"],
      showErrorMessageRequired: [1, "p-required-field-error-message", "showErrorMessageRequired"],
      showRequired: [1, "p-show-required", "showRequired"],
      showSeconds: [1, "p-show-seconds", "showSeconds"],
      size: [1, "p-size", "size"],
      timerFormat: [1, "p-format-time", "timerFormat"],
      dateFormat: [1, "p-format-date", "dateFormat"]
    },
    outputs: {
      errorPattern: "p-error-patternChange",
      onblur: "p-blur",
      onchange: "p-change",
      keydown: "p-keydown",
      changeModel: "p-change-model"
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoDatetimepickerBaseComponent, [{
    type: Directive
  }], () => [{
    type: PoLanguageService
  }], {
    appendBox: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-append-in-body",
        required: false
      }]
    }],
    autoFocus: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-auto-focus",
        required: false
      }]
    }],
    clean: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-clean",
        required: false
      }]
    }],
    compactLabel: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-compact-label",
        required: false
      }]
    }],
    disabled: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-disabled",
        required: false
      }]
    }],
    errorLimit: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-error-limit",
        required: false
      }]
    }],
    errorPattern: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-error-pattern",
        required: false
      }]
    }, {
      type: Output,
      args: ["p-error-patternChange"]
    }],
    help: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-help",
        required: false
      }]
    }],
    label: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-label",
        required: false
      }]
    }],
    labelTextWrap: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-label-text-wrap",
        required: false
      }]
    }],
    loading: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-loading",
        required: false
      }]
    }],
    localeInput: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-locale",
        required: false
      }]
    }],
    maxDateInput: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-max-date",
        required: false
      }]
    }],
    maxTime: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-max-time",
        required: false
      }]
    }],
    minDateInput: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-min-date",
        required: false
      }]
    }],
    minTime: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-min-time",
        required: false
      }]
    }],
    minuteInterval: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-minute-interval",
        required: false
      }]
    }],
    name: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "name",
        required: false
      }]
    }],
    noAutocomplete: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-no-autocomplete",
        required: false
      }]
    }],
    optional: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-optional",
        required: false
      }]
    }],
    placeholder: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-placeholder",
        required: false
      }]
    }],
    poHelperComponent: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-helper",
        required: false
      }]
    }],
    readonly: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-readonly",
        required: false
      }]
    }],
    required: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-required",
        required: false
      }]
    }],
    secondInterval: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-second-interval",
        required: false
      }]
    }],
    showErrorMessageRequired: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-required-field-error-message",
        required: false
      }]
    }],
    showRequired: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-show-required",
        required: false
      }]
    }],
    showSeconds: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-show-seconds",
        required: false
      }]
    }],
    size: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-size",
        required: false
      }]
    }],
    timerFormat: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-format-time",
        required: false
      }]
    }],
    dateFormat: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-format-date",
        required: false
      }]
    }],
    onblur: [{
      type: Output,
      args: ["p-blur"]
    }],
    onchange: [{
      type: Output,
      args: ["p-change"]
    }],
    keydown: [{
      type: Output,
      args: ["p-keydown"]
    }],
    changeModel: [{
      type: Output,
      args: ["p-change-model"]
    }],
    hostSize: [{
      type: HostBinding,
      args: ["attr.p-size"]
    }],
    onThemeChange: [{
      type: HostListener,
      args: ["window:PoUiThemeChange"]
    }]
  });
})();
var poCalendarContentOffset$1 = 8;
var poCalendarPositionDefault$1 = "bottom-left";
var providers$6 = [{
  provide: NG_VALUE_ACCESSOR,
  // eslint-disable-next-line
  useExisting: forwardRef(() => PoDatetimepickerComponent),
  multi: true
}, {
  provide: NG_VALIDATORS,
  // eslint-disable-next-line
  useExisting: forwardRef(() => PoDatetimepickerComponent),
  multi: true
}, PoControlPositionService];
var PoDatetimepickerComponent = class _PoDatetimepickerComponent extends PoDatetimepickerBaseComponent {
  iconDatepicker;
  calendarComponent;
  inputEl;
  dialogPicker;
  helperEl;
  datetimepickerField;
  renderer = inject(Renderer2);
  cd = inject(ChangeDetectorRef);
  controlPosition = inject(PoControlPositionService);
  id = `po-datetimepicker[${uuid()}]`;
  displayAdditionalHelp = false;
  el;
  literals;
  visible = false;
  currentPeriod = "AM";
  isInputFocused = false;
  clickListener;
  eventResizeListener;
  valueBeforeChange = "";
  hadValueOnOpen = false;
  isTodayPresetInProgress = false;
  constructor() {
    const languageService = inject(PoLanguageService);
    const el = inject(ElementRef);
    super(languageService);
    this.languageService = languageService;
    this.shortLanguage = this.languageService.getShortLanguage();
    this.el = el;
    const language = languageService.getShortLanguage();
    this.literals = __spreadValues({}, PoDatetimepickerLiterals[language]);
  }
  ngAfterViewInit() {
    this.setDialogPickerStyleDisplay("none");
    if (this.autoFocus()) {
      this.focus();
    }
    if (this.iconDatepicker?.buttonElement?.nativeElement) {
      this.renderer.setAttribute(this.iconDatepicker.buttonElement.nativeElement, "aria-label", this.literals.open);
    }
  }
  ngOnDestroy() {
    this.removeListeners();
  }
  togglePicker(focusInput = true) {
    if (this.isDisabled || this.isReadonly || !this.iconDatepicker?.buttonElement?.nativeElement) {
      return;
    }
    if (!this.visible) {
      this.visible = true;
      this.hadValueOnOpen = !!(this.date && this.timeValue);
      this.syncDateFromInput();
      this.setCalendarPosition();
      this.initializeListeners();
      this.syncCalendarAndTimer();
      requestAnimationFrame(() => {
        this.calendarComponent?.timerComponent?.initAllColumnOffsets();
      });
      this.renderer.setAttribute(this.inputEl.nativeElement, "aria-expanded", "true");
      this.renderer.setAttribute(this.iconDatepicker.buttonElement.nativeElement, "aria-expanded", "true");
    } else {
      this.inputEl.nativeElement.disabled = false;
      this.closeCalendar(focusInput);
      this.renderer.removeAttribute(this.inputEl.nativeElement, "aria-expanded");
      this.renderer.removeAttribute(this.iconDatepicker.buttonElement.nativeElement, "aria-expanded");
    }
  }
  closeCalendar(focusInput = true) {
    this.visible = false;
    this.removeListeners();
    this.setDialogPickerStyleDisplay("none");
    if (!this.verifyMobile() && focusInput) {
      this.focus();
    }
    requestAnimationFrame(() => {
      this.iconDatepicker?.buttonElement?.nativeElement?.focus();
    });
  }
  focus() {
    if (!this.isDisabled && this.inputEl?.nativeElement) {
      this.inputEl.nativeElement.focus();
    }
  }
  wasClickedOnPicker(event) {
    if (!this.dialogPicker || !this.iconDatepicker) {
      return;
    }
    if ((!this.dialogPicker.nativeElement.contains(event.target) || this.hasOverlayClass(event.target)) && !this.iconDatepicker.buttonElement.nativeElement.contains(event.target) && !this.hasAttrCalendar(event.target)) {
      this.closeCalendar();
    }
  }
  /* istanbul ignore next */
  verifyMobile() {
    return isMobile();
  }
  hasInvalidClass() {
    return this.el.nativeElement.classList.contains("ng-invalid") && this.el.nativeElement.classList.contains("ng-dirty") && (this.inputEl.nativeElement.value !== "" || this.showErrorMessageRequired() && (this.isRequired || this.hasValidatorRequired));
  }
  getErrorPattern() {
    return this.errorPattern() !== "" && this.hasInvalidClass() ? this.errorPattern() : "";
  }
  onKeyup($event) {
    if (this.isReadonly || $event?.target !== this.inputEl?.nativeElement) {
      return;
    }
    this.objMask?.keyup($event);
    if (this.objMask?.valueToModel || this.objMask?.valueToModel === "") {
      this.processKeyupWithMask();
    } else {
      this.clearDateTimeState();
      this.syncCalendarAndTimer();
    }
  }
  processKeyupWithMask() {
    const inputValue = this.inputEl.nativeElement.value;
    const minLength = this.getExpectedInputLength();
    if (this.objMask.valueToModel.length >= minLength) {
      this.tryParseCompleteInput(inputValue);
    } else {
      this.clearDateTimeState();
      this.controlModel();
      this.syncCalendarAndTimer();
    }
  }
  tryParseCompleteInput(inputValue) {
    const parsed = this.parseDateTimeFromInput(inputValue);
    if (parsed) {
      this.date = parsed.date;
      this.timeValue = this.normalizeTimeWithSeconds(parsed.time);
      if (this.is12HourFormat) {
        const hours = Number.parseInt(parsed.time.split(":")[0], 10);
        this.currentPeriod = hours >= 12 ? "PM" : "AM";
      }
      this.controlModel();
      this.syncCalendarAndTimer();
    } else {
      this.clearDateTimeState();
      this.controlModel();
      this.syncCalendarAndTimer();
    }
  }
  clearDateTimeState() {
    this.date = void 0;
    this.timeValue = "";
  }
  onKeydown($event) {
    if ($event?.target === this.inputEl?.nativeElement) {
      this.keydown.emit($event);
    }
    if (this.isReadonly) {
      return;
    }
    if ($event.key === "Escape" && this.visible) {
      this.togglePicker(false);
      $event.preventDefault();
      $event.stopPropagation();
      return;
    }
    if ($event?.target === this.inputEl?.nativeElement) {
      this.objMask?.keydown($event);
    }
  }
  onInputFocus() {
    this.isInputFocused = true;
  }
  onPeriodBlur() {
    this.isInputFocused = false;
  }
  eventOnBlur($event) {
    this.isInputFocused = false;
    this.onTouchedModel?.();
    this.objMask?.blur($event);
    this.onblur.emit();
    const inputValue = this.inputEl.nativeElement.value;
    if (!inputValue) {
      this.date = void 0;
      this.timeValue = "";
      this.callOnChange("");
      this.emitChangeIfDifferent("");
      this.syncCalendarAndTimer();
      return;
    }
    const minLength = this.getExpectedInputLength();
    if (this.objMask?.valueToModel && this.objMask.valueToModel.length >= minLength) {
      this.parseInputAndSync(inputValue);
      if (this.date && this.timeValue) {
        this.emitChangeIfDifferent(this.getModelValue());
      } else {
        this.emitChangeIfDifferent(this.literals.invalidDatetime);
      }
    } else if (inputValue) {
      this.date = void 0;
      this.timeValue = "";
      this.callOnChange(this.literals.invalidDatetime);
      this.emitChangeIfDifferent(this.literals.invalidDatetime);
      this.syncCalendarAndTimer();
    }
  }
  eventOnButtonKeydown(event) {
    if (event.key === "Tab" && !event.shiftKey && this.visible) {
      const firstCombo = this.dialogPicker?.nativeElement?.querySelector(".po-combo-first .po-combo-input");
      if (firstCombo) {
        event.preventDefault();
        event.stopPropagation();
        firstCombo.focus();
      }
    }
  }
  eventOnClick($event) {
    if (this.verifyMobile()) {
      $event.target.blur();
      setTimeout(() => this.togglePicker(), 0);
    } else if (!this.isReadonly) {
      this.objMask?.click($event);
    }
  }
  onFieldClick($event) {
    if (this.isDisabled || this.isReadonly) {
      return;
    }
    const target = $event.target;
    if (target.closest(".po-datetimepicker-field-period") || target.closest(".po-field-icon-container-right")) {
      return;
    }
    this.focus();
  }
  isFocusOnFirstCombo() {
    const first = this.dialogPicker?.nativeElement?.querySelector(".po-combo-first .po-combo-input");
    return first === document.activeElement;
  }
  eventOnCalendarKeydown(event) {
    if (event.key === "Tab" && event.shiftKey && this.isFocusOnFirstCombo()) {
      this.closeCalendar(false);
    }
  }
  onTimerBoundaryTab(event) {
    if (event.direction === "forward") {
      event.event.preventDefault();
      this.closeCalendar(false);
    }
  }
  onCalendarClose() {
    this.isTodayPresetInProgress = true;
    this.closeCalendar(false);
  }
  writeValue(value) {
    super.writeValue(value);
    this.valueBeforeChange = this.getModelValue();
  }
  refreshValue(value) {
    if (!this.inputEl) {
      return;
    }
    if (value && value instanceof Date && !Number.isNaN(value.getTime())) {
      if (this.is12HourFormat && this.timeValue) {
        const hours = Number.parseInt(this.timeValue.split(":")[0], 10);
        this.currentPeriod = hours >= 12 ? "PM" : "AM";
      }
      this.inputEl.nativeElement.value = this.formatToDisplay(value, this.timeValue);
    } else if (!value) {
      this.inputEl.nativeElement.value = "";
    }
    this.cd?.markForCheck();
  }
  onTimeChange(time) {
    if (!time) {
      return;
    }
    this.timeValue = this.normalizeTimeWithSeconds(time);
    if (this.date && !Number.isNaN(this.date.getTime())) {
      this.controlModel();
      this.refreshValue(this.date);
      this.emitChangeIfDifferent(this.getModelValue());
      if (this.isTodayPresetInProgress) {
        this.isTodayPresetInProgress = false;
      } else if (!this.hadValueOnOpen) {
        this.closeCalendar(true);
      }
      this.cd.markForCheck();
    }
  }
  onDateChange(date) {
    if (date === null || date === void 0) {
      return;
    }
    if (date === "") {
      this.date = void 0;
      this.timeValue = "";
      this.callOnChange("");
      this.emitChangeIfDifferent("");
      this.refreshValue(void 0);
      this.closeCalendar(false);
      this.cd.markForCheck();
      return;
    }
    const parsedDate = /* @__PURE__ */ new Date(date + "T00:00:00");
    if (Number.isNaN(parsedDate.getTime())) {
      return;
    }
    this.date = parsedDate;
    if (this.isTodayPresetInProgress) {
      return;
    }
    if (this.timeValue) {
      this.controlModel();
      this.refreshValue(this.date);
      this.emitChangeIfDifferent(this.getModelValue());
      return;
    }
    this.refreshValue(this.date);
  }
  clear(triggeredByKeyboard = false) {
    this.date = void 0;
    this.timeValue = "";
    this.currentPeriod = "AM";
    this.inputEl.nativeElement.value = "";
    if (this.objMask) {
      this.objMask.valueToModel = "";
      this.objMask.valueToInput = "";
    }
    if (triggeredByKeyboard) {
      setTimeout(() => {
        this.focus();
      }, 200);
    } else {
      this.focus();
    }
    this.callOnChange("");
    this.emitChangeIfDifferent("");
    if (this.calendarComponent) {
      this.calendarComponent.writeValue(null);
      if (this.calendarComponent.timerComponent) {
        this.calendarComponent.timerComponent.writeValue(null);
      }
    }
    this.cd.markForCheck();
  }
  setHelper(label, additionalHelpTooltip) {
    return setHelperSettings(label, additionalHelpTooltip, this.poHelperComponent(), this.hostSize);
  }
  /**
   * Método que exibe `p-helper` ou executa a ação definida em `p-helper{eventOnClick}`.
   * Para isso, será necessário configurar uma tecla de atalho utilizando o evento `p-keydown`.
   *
   * > Exibe ou oculta o conteúdo do componente `po-helper` quando o componente estiver com foco.
   *
   * ```
   * // Exemplo com p-label e p-helper
   * <po-datetimepicker
   *  #datetimepicker
   *  ...
   *  p-label="Label"
   *  [p-helper]="helperOptions"
   *  (p-keydown)="onKeyDown($event, datetimepicker)"
   * ></po-datetimepicker>
   * ```
   * ```
   * onKeyDown(event: KeyboardEvent, inp: PoDatetimepickerComponent): void {
   *  if (event.code === 'F9') {
   *    inp.showAdditionalHelp();
   *  }
   * }
   * ```
   */
  showAdditionalHelp() {
    this.displayAdditionalHelp = !this.displayAdditionalHelp;
    const helper = this.poHelperComponent();
    if (!this.label() && helper) {
      if (typeof helper !== "string" && typeof helper?.eventOnClick === "function") {
        helper.eventOnClick();
        return;
      }
      if (this.helperEl?.helperIsVisible()) {
        this.helperEl?.closeHelperPopover();
        return;
      }
      this.helperEl?.openHelperPopover();
      return;
    }
    return this.displayAdditionalHelp;
  }
  get dateTimeInputValue() {
    return this.inputEl.nativeElement.value;
  }
  formatToDisplay(date, time) {
    if (!date) {
      return "";
    }
    const day = ("0" + date.getDate()).slice(-2);
    const month = ("0" + (date.getMonth() + 1)).slice(-2);
    const year = PoUtils.formatYear(date.getFullYear());
    const separator = this.languageService.getDateSeparator(this.localeInput());
    let dateFormatted = this.format;
    dateFormatted = dateFormatted.replace("dd", day);
    dateFormatted = dateFormatted.replace("mm", month);
    dateFormatted = dateFormatted.replace("yyyy", year);
    dateFormatted = dateFormatted.replace(/\//g, separator);
    if (!time) {
      const timePlaceholder = this.showSeconds() ? "--:--:--" : "--:--";
      return `${dateFormatted} ${timePlaceholder}`;
    }
    const timeDisplay = this.formatTimeForDisplay(time);
    return `${dateFormatted} ${timeDisplay}`;
  }
  parseInputAndSync(inputValue) {
    const parsed = this.parseDateTimeFromInput(inputValue);
    if (parsed) {
      this.date = parsed.date;
      this.timeValue = this.normalizeTimeWithSeconds(parsed.time);
      if (this.is12HourFormat) {
        const hours = Number.parseInt(parsed.time.split(":")[0], 10);
        this.currentPeriod = hours >= 12 ? "PM" : "AM";
      }
      this.controlModel();
      this.syncCalendarAndTimer();
    } else {
      this.date = void 0;
      this.timeValue = "";
      this.callOnChange(this.literals.invalidDatetime);
    }
  }
  parseDateTimeFromInput(inputValue) {
    if (!inputValue) {
      return null;
    }
    const separator = this.languageService.getDateSeparator(this.localeInput());
    const datePartLength = 10;
    const datePart = inputValue.substring(0, datePartLength);
    const timePart = inputValue.substring(datePartLength + 1).trim();
    const date = this.getDateFromFormattedString(datePart, separator);
    if (!date) {
      return null;
    }
    const time = this.parseTimeFromInput(timePart);
    if (!time) {
      return null;
    }
    return {
      date,
      time
    };
  }
  getDateFromFormattedString(dateStr, separator) {
    const format2 = this.format.replace(/\//g, separator);
    const dayIndex = format2.indexOf("dd");
    const monthIndex = format2.indexOf("mm");
    const yearIndex = format2.indexOf("yyyy");
    if (dayIndex === -1 || monthIndex === -1 || yearIndex === -1) {
      return null;
    }
    const day = parseInt(dateStr.substring(dayIndex, dayIndex + 2), 10);
    const month = parseInt(dateStr.substring(monthIndex, monthIndex + 2), 10) - 1;
    const year = parseInt(dateStr.substring(yearIndex, yearIndex + 4), 10);
    if (isNaN(day) || isNaN(month) || isNaN(year)) {
      return null;
    }
    const date = new Date(year, month, day);
    PoUtils.setYearFrom0To100(date, year);
    if (date.getFullYear() !== year || date.getMonth() !== month || date.getDate() !== day) {
      return null;
    }
    return date;
  }
  parseTimeFromInput(timeStr) {
    if (!timeStr) {
      return null;
    }
    return this.is12HourFormat ? this.parse12HourTime(timeStr) : this.parse24HourTime(timeStr);
  }
  parse12HourTime(timeStr) {
    const periodMatch = /(AM|PM)$/i.exec(timeStr);
    const period = periodMatch ? periodMatch[1].toUpperCase() : this.currentPeriod;
    const timeOnly = periodMatch ? timeStr.replace(/(AM|PM)/i, "").trim() : timeStr.trim();
    const parts = timeOnly.split(":");
    if (parts.length < 2) {
      return null;
    }
    let hours = Number.parseInt(parts[0], 10);
    const minutes = Number.parseInt(parts[1], 10);
    const seconds = this.getSeconds(parts);
    if (!this.isValid12Hour(hours, minutes)) {
      return null;
    }
    hours = this.convertTo24Hour(hours, period);
    return this.buildFormattedTime(hours, minutes, seconds);
  }
  parse24HourTime(timeStr) {
    const parts = timeStr.split(":");
    if (parts.length < 2) {
      return null;
    }
    const hours = Number.parseInt(parts[0], 10);
    const minutes = Number.parseInt(parts[1], 10);
    const seconds = this.getSeconds(parts);
    if (!this.isValid24Hour(hours, minutes)) {
      return null;
    }
    return this.buildFormattedTime(hours, minutes, seconds);
  }
  getSeconds(parts) {
    return parts.length > 2 ? Number.parseInt(parts[2], 10) : null;
  }
  isValid12Hour(hours, minutes) {
    return !(Number.isNaN(hours) || Number.isNaN(minutes) || hours < 1 || hours > 12 || minutes < 0 || minutes > 59);
  }
  isValid24Hour(hours, minutes) {
    return !(Number.isNaN(hours) || Number.isNaN(minutes) || hours < 0 || hours > 23 || minutes < 0 || minutes > 59);
  }
  convertTo24Hour(hours, period) {
    if (period === "AM" && hours === 12) {
      return 0;
    }
    if (period === "PM" && hours !== 12) {
      return hours + 12;
    }
    return hours;
  }
  buildFormattedTime(hours, minutes, seconds) {
    const hoursStr = this.padTime(hours);
    const minutesStr = this.padTime(minutes);
    if (seconds !== null) {
      if (!this.isValidSeconds(seconds)) {
        return null;
      }
      return `${hoursStr}:${minutesStr}:${this.padTime(seconds)}`;
    }
    if (this.showSeconds()) {
      return null;
    }
    return `${hoursStr}:${minutesStr}`;
  }
  isValidSeconds(seconds) {
    return !Number.isNaN(seconds) && seconds >= 0 && seconds <= 59;
  }
  padTime(value) {
    return ("0" + value).slice(-2);
  }
  normalizeTimeWithSeconds(time) {
    if (!time || !this.showSeconds()) {
      return time;
    }
    const parts = time.split(":");
    if (parts.length === 2) {
      return `${time}:00`;
    }
    return time;
  }
  syncDateFromInput() {
    if (this.date) {
      return;
    }
    const inputValue = this.inputEl?.nativeElement?.value;
    if (!inputValue || inputValue.length < 10) {
      return;
    }
    const separator = this.languageService.getDateSeparator(this.localeInput());
    const fullParsed = this.parseDateTimeFromInput(inputValue);
    if (fullParsed) {
      this.date = fullParsed.date;
      this.timeValue = this.normalizeTimeWithSeconds(fullParsed.time);
      return;
    }
    const datePart = inputValue.substring(0, 10);
    const parsedDate = this.getDateFromFormattedString(datePart, separator);
    if (parsedDate) {
      this.date = parsedDate;
    }
  }
  syncCalendarAndTimer() {
    if (!this.calendarComponent) {
      return;
    }
    if (this.date) {
      this.calendarComponent.writeValue(this.date);
      if (this.calendarComponent.timerComponent && this.timeValue) {
        this.calendarComponent.timerComponent.writeValue(this.timeValue);
      } else if (this.calendarComponent.timerComponent) {
        this.calendarComponent.timerComponent.writeValue(null);
      }
    } else {
      this.calendarComponent.writeValue(null);
      if (this.calendarComponent.timerComponent) {
        this.calendarComponent.timerComponent.writeValue(null);
      }
    }
  }
  getExpectedInputLength() {
    let length = 16;
    if (this.showSeconds()) {
      length += 3;
    }
    return length;
  }
  setCalendarPosition() {
    this.setDialogPickerStyleDisplay("block");
    this.adjustCalendarPosition();
  }
  adjustCalendarPosition() {
    if (this.dialogPicker?.nativeElement && this.visible) {
      requestAnimationFrame(() => {
        const scrollHeight = this.dialogPicker.nativeElement.querySelector(".po-calendar-date-time")?.scrollHeight ?? this.dialogPicker.nativeElement.scrollHeight;
        const scrollWidth = this.dialogPicker.nativeElement.querySelector(".po-calendar-date-time")?.scrollWidth ?? this.dialogPicker.nativeElement.scrollWidth;
        this.dialogPicker.nativeElement.style.height = scrollHeight + "px";
        this.dialogPicker.nativeElement.style.width = scrollWidth + "px";
        this.controlPosition.setElements(this.dialogPicker.nativeElement, poCalendarContentOffset$1, this.datetimepickerField, ["top-left", "top-right", "bottom-left", "bottom-right"], false, true);
        this.controlPosition.adjustPosition(poCalendarPositionDefault$1);
      });
    }
  }
  initializeListeners() {
    this.clickListener = this.renderer.listen("document", "click", (event) => {
      this.wasClickedOnPicker(event);
    });
    this.eventResizeListener = this.renderer.listen("window", "resize", () => {
      this.closeCalendar();
    });
    window.addEventListener("scroll", this.onScroll, true);
  }
  removeListeners() {
    if (this.clickListener) {
      this.clickListener();
      this.clickListener = void 0;
    }
    if (this.eventResizeListener) {
      this.eventResizeListener();
      this.eventResizeListener = void 0;
    }
    window.removeEventListener("scroll", this.onScroll, true);
  }
  onScroll = () => {
    if (this.visible) {
      this.controlPosition.adjustPosition(poCalendarPositionDefault$1);
    }
  };
  setDialogPickerStyleDisplay(value) {
    if (this.dialogPicker?.nativeElement) {
      this.dialogPicker.nativeElement.style.display = value;
    }
  }
  hasOverlayClass(element) {
    return element?.classList?.contains("po-datetimepicker-calendar-overlay");
  }
  hasAttrCalendar(element) {
    const attrCalendar = "attr-calendar";
    return element?.hasAttribute?.(attrCalendar) || element?.parentElement?.hasAttribute?.(attrCalendar);
  }
  emitChangeIfDifferent(value) {
    if (value !== this.valueBeforeChange) {
      this.valueBeforeChange = value;
      this.onchange.emit(value);
    }
  }
  onPeriodKeydown(event) {
    if (event.key === "ArrowUp" || event.key === "ArrowDown") {
      event.preventDefault();
      if (!this.isReadonly && !this.isDisabled) {
        this.togglePeriod();
      }
      return;
    }
    if (event.key === "Backspace" || event.key === "Delete") {
      event.preventDefault();
      return;
    }
    if (event.key === "Tab" && event.shiftKey) {
      event.preventDefault();
      this.focus();
      return;
    }
    if (event.key === "Tab" && !event.shiftKey) {
      return;
    }
    if (event.key.length === 1) {
      event.preventDefault();
    }
  }
  onPeriodClick(event) {
    if (!this.isReadonly && !this.isDisabled) {
      this.togglePeriod();
    }
  }
  togglePeriod() {
    this.currentPeriod = this.currentPeriod === "AM" ? "PM" : "AM";
    const inputValue = this.inputEl.nativeElement.value;
    const minLength = this.getExpectedInputLength();
    if (this.objMask?.valueToModel && this.objMask.valueToModel.length >= minLength) {
      this.parseInputAndSync(inputValue);
      if (this.date && this.timeValue) {
        this.emitChangeIfDifferent(this.getModelValue());
      }
    }
  }
  static ɵfac = function PoDatetimepickerComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoDatetimepickerComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoDatetimepickerComponent,
    selectors: [["po-datetimepicker"]],
    viewQuery: function PoDatetimepickerComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c76, 5)(_c66, 5)(_c49, 7, ElementRef)(_c67, 5, ElementRef)(_c17, 5, PoHelperComponent)(_c77, 7, ElementRef);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.iconDatepicker = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.calendarComponent = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.inputEl = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.dialogPicker = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.helperEl = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.datetimepickerField = _t.first);
      }
    },
    hostBindings: function PoDatetimepickerComponent_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("keyup", function PoDatetimepickerComponent_keyup_HostBindingHandler($event) {
          return ctx.onKeyup($event);
        })("keydown", function PoDatetimepickerComponent_keydown_HostBindingHandler($event) {
          return ctx.onKeydown($event);
        });
      }
    },
    standalone: false,
    features: [ɵɵProvidersFeature(providers$6), ɵɵInheritDefinitionFeature],
    decls: 21,
    vars: 39,
    consts: [["outerContainer", ""], ["trigger", "cdkOverlayOrigin"], ["datetimepickerField", ""], ["inp", ""], ["sharedCalendarContent", ""], ["iconCalendar", ""], ["iconClean", ""], ["helperEl", ""], ["dialogPicker", ""], ["calendar", ""], [3, "p-id", "p-label", "p-disabled", "p-optional", "p-required", "p-show-required", "p-helper", "p-show-helper", "p-size", "p-text-wrap", "p-compact-label"], ["cdkOverlayOrigin", "", 1, "po-field-container-content", "po-field-container-content-datetimepicker"], [1, "po-field-container-input"], [1, "po-datetimepicker-field", "po-input", 3, "keydown", "click"], [1, "po-datetimepicker-field-segment"], ["type", "text", 1, "po-datetimepicker-inner-input", 3, "blur", "click", "focus", "id", "autocomplete", "disabled", "placeholder", "readonly", "required"], [1, "po-datetimepicker-field-segment", "po-datetimepicker-field-period"], [3, "p-size"], [1, "po-field-icon-container-right"], [1, "po-field-helper-button", 3, "p-size", "p-helper", "p-disabled", "p-append-in-body"], ["cdkConnectedOverlay", "", 3, "cdkConnectedOverlayOrigin", "cdkConnectedOverlayOpen", "cdkConnectedOverlayDisableClose"], [3, "p-append-in-body", "p-help", "p-disabled", "p-error-pattern", "p-error-limit", "p-size"], ["type", "text", "maxlength", "2", "readonly", "", "role", "spinbutton", 1, "po-datetimepicker-segment-input", "po-datetimepicker-period-input", 3, "keydown", "click", "focus", "blur", "value", "disabled"], ["p-icon", "ICON_CALENDAR_DOTS", "p-kind", "tertiary", 1, "po-datetimepicker-button", 3, "p-click", "keydown", "p-disabled", "p-size"], ["tabindex", "0", "role", "button", "p-icon", "ICON_FILL_CLEAR_CONTENT", 1, "po-icon-input", 3, "p-change-event", "keydown.enter", "keydown.space", "p-element-ref", "p-size"], [1, "po-button-vertical-divider"], [4, "ngTemplateOutlet"], ["tabindex", "-1", 3, "hidden"], [1, "po-datetimepicker-calendar-overlay"], ["p-mode", "date-time", 3, "p-change", "p-change-time", "p-close", "p-change-month-year", "p-timer-boundary-tab", "keydown", "p-size", "p-locale", "p-format", "p-min-date", "p-max-date", "p-max-time", "p-min-time", "p-minute-interval", "p-second-interval", "p-show-seconds"]],
    template: function PoDatetimepickerComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵdeclareLet(0);
        ɵɵelementStart(1, "po-field-container", 10)(2, "div", null, 0)(4, "div", 11, 1)(6, "div", 12)(7, "div", 13, 2);
        ɵɵlistener("keydown", function PoDatetimepickerComponent_Template_div_keydown_7_listener($event) {
          return ctx.onFieldClick($event);
        })("click", function PoDatetimepickerComponent_Template_div_click_7_listener($event) {
          return ctx.onFieldClick($event);
        });
        ɵɵelementStart(9, "div", 14)(10, "input", 15, 3);
        ɵɵlistener("blur", function PoDatetimepickerComponent_Template_input_blur_10_listener($event) {
          return ctx.eventOnBlur($event);
        })("click", function PoDatetimepickerComponent_Template_input_click_10_listener($event) {
          return ctx.eventOnClick($event);
        })("focus", function PoDatetimepickerComponent_Template_input_focus_10_listener() {
          return ctx.onInputFocus();
        });
        ɵɵelementEnd()();
        ɵɵconditionalCreate(12, PoDatetimepickerComponent_Conditional_12_Template, 2, 5, "div", 16);
        ɵɵconditionalCreate(13, PoDatetimepickerComponent_Conditional_13_Template, 1, 1, "po-loading-icon", 17)(14, PoDatetimepickerComponent_Conditional_14_Template, 4, 5, "div", 18);
        ɵɵelementEnd()();
        ɵɵconditionalCreate(15, PoDatetimepickerComponent_Conditional_15_Template, 2, 4, "po-helper", 19);
        ɵɵelementEnd();
        ɵɵconditionalCreate(16, PoDatetimepickerComponent_Conditional_16_Template, 1, 3, null, 20)(17, PoDatetimepickerComponent_Conditional_17_Template, 1, 1, "ng-container");
        ɵɵelementEnd();
        ɵɵconditionalCreate(18, PoDatetimepickerComponent_Conditional_18_Template, 1, 6, "po-field-container-bottom", 21);
        ɵɵelementEnd();
        ɵɵtemplate(19, PoDatetimepickerComponent_ng_template_19_Template, 5, 16, "ng-template", null, 4, ɵɵtemplateRefExtractor);
      }
      if (rf & 2) {
        const poHelper_r9 = ɵɵstoreLet(ctx.setHelper(ctx.label()));
        ɵɵadvance();
        ɵɵclassProp("po-date-picker-container-disabled", ctx.isDisabled);
        ɵɵproperty("p-id", ctx.id)("p-label", ctx.label())("p-disabled", ctx.isDisabled)("p-optional", ctx.optional())("p-required", ctx.isRequired)("p-show-required", ctx.showRequired())("p-helper", ctx.label() ? poHelper_r9.helperSettings : void 0)("p-show-helper", ctx.label() ? ctx.displayAdditionalHelp : void 0)("p-size", ctx.hostSize)("p-text-wrap", ctx.labelTextWrap())("p-compact-label", ctx.compactLabel());
        ɵɵadvance(6);
        ɵɵclassProp("po-input-icon-right", ctx.isClean && ctx.dateTimeInputValue && !ctx.isDisabled && !ctx.isReadonly)("po-datetimepicker-field-disabled", ctx.isDisabled)("po-datetimepicker-field-readonly", ctx.isReadonly)("po-datetimepicker-field-focused", ctx.isInputFocused);
        ɵɵattribute("data-inactive-component", ctx.isDisabled || ctx.isReadonly)("p-size", ctx.hostSize);
        ɵɵadvance(2);
        ɵɵattribute("data-value", ctx.dateTimeInputValue || ctx.placeholderValue || "                ");
        ɵɵadvance();
        ɵɵproperty("id", ctx.id)("autocomplete", ctx.isNoAutocomplete)("disabled", ctx.isDisabled)("placeholder", ctx.isDisabled ? "" : ctx.placeholderValue)("readonly", ctx.isReadonly)("required", ctx.isRequired);
        ɵɵattribute("data-focused-element", !ctx.isDisabled)("name", ctx.name())("data-append-in-body", ctx.appendBox());
        ɵɵadvance(2);
        ɵɵconditional(ctx.is12HourFormat && ctx.dateTimeInputValue ? 12 : -1);
        ɵɵadvance();
        ɵɵconditional(ctx.loading() ? 13 : 14);
        ɵɵadvance(2);
        ɵɵconditional(!ctx.label() && ctx.poHelperComponent() || !ctx.label() && poHelper_r9.hideAdditionalHelp ? 15 : -1);
        ɵɵadvance();
        ɵɵconditional(ctx.appendBox() ? 16 : 17);
        ɵɵadvance(2);
        ɵɵconditional(!ctx.isReadonly ? 18 : -1);
      }
    },
    dependencies: [NgTemplateOutlet, CdkConnectedOverlay, CdkOverlayOrigin, PoCleanComponent, PoHelperComponent, PoButtonComponent, PoLoadingIconComponent, PoCalendarComponent, PoFieldContainerBottomComponent, PoFieldContainerComponent],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoDatetimepickerComponent, [{
    type: Component,
    args: [{
      selector: "po-datetimepicker",
      providers: providers$6,
      standalone: false,
      template: `@let poHelper = setHelper(label());
<po-field-container
  [class.po-date-picker-container-disabled]="isDisabled"
  [p-id]="id"
  [p-label]="label()"
  [p-disabled]="isDisabled"
  [p-optional]="optional()"
  [p-required]="isRequired"
  [p-show-required]="showRequired()"
  [p-helper]="label() ? poHelper.helperSettings : undefined"
  [p-show-helper]="label() ? displayAdditionalHelp : undefined"
  [p-size]="hostSize"
  [p-text-wrap]="labelTextWrap()"
  [p-compact-label]="compactLabel()"
>
  <div #outerContainer>
    <div
      cdkOverlayOrigin
      #trigger="cdkOverlayOrigin"
      class="po-field-container-content po-field-container-content-datetimepicker"
    >
      <div class="po-field-container-input">
        <div
          #datetimepickerField
          class="po-datetimepicker-field po-input"
          [class.po-input-icon-right]="isClean && dateTimeInputValue && !isDisabled && !isReadonly"
          [class.po-datetimepicker-field-disabled]="isDisabled"
          [class.po-datetimepicker-field-readonly]="isReadonly"
          [class.po-datetimepicker-field-focused]="isInputFocused"
          [attr.data-inactive-component]="isDisabled || isReadonly"
          [attr.p-size]="hostSize"
          (keydown)="onFieldClick($event)"
          (click)="onFieldClick($event)"
        >
          <div
            class="po-datetimepicker-field-segment"
            [attr.data-value]="dateTimeInputValue || placeholderValue || '                '"
          >
            <input
              #inp
              type="text"
              class="po-datetimepicker-inner-input"
              [attr.data-focused-element]="!isDisabled"
              [attr.name]="name()"
              [id]="id"
              [autocomplete]="isNoAutocomplete"
              [disabled]="isDisabled"
              [placeholder]="isDisabled ? '' : placeholderValue"
              [readonly]="isReadonly"
              [required]="isRequired"
              [attr.data-append-in-body]="appendBox()"
              (blur)="eventOnBlur($event)"
              (click)="eventOnClick($event)"
              (focus)="onInputFocus()"
            />
          </div>
          @if (is12HourFormat && dateTimeInputValue) {
            <div
              class="po-datetimepicker-field-segment po-datetimepicker-field-period"
              [attr.data-value]="currentPeriod"
            >
              <input
                class="po-datetimepicker-segment-input po-datetimepicker-period-input"
                type="text"
                maxlength="2"
                readonly
                [value]="currentPeriod"
                [disabled]="isDisabled"
                [attr.tabindex]="isDisabled || isReadonly ? -1 : 0"
                role="spinbutton"
                [attr.aria-label]="currentPeriod"
                (keydown)="onPeriodKeydown($event)"
                (click)="onPeriodClick($event)"
                (focus)="onInputFocus()"
                (blur)="onPeriodBlur()"
              />
            </div>
          }
          @if (loading()) {
            <po-loading-icon [p-size]="mapSizeToIcon(hostSize)"></po-loading-icon>
          } @else {
            <div class="po-field-icon-container-right">
              @if (isClean && dateTimeInputValue && !isDisabled && !isReadonly) {
                <po-clean
                  #iconClean
                  tabindex="0"
                  role="button"
                  [attr.aria-label]="literals.clean"
                  class="po-icon-input"
                  p-icon="ICON_FILL_CLEAR_CONTENT"
                  [p-element-ref]="inputEl"
                  [p-size]="hostSize"
                  (p-change-event)="clear()"
                  (keydown.enter)="clear(true); $event.preventDefault()"
                  (keydown.space)="clear(true); $event.preventDefault()"
                >
                </po-clean>
                <div class="po-button-vertical-divider"></div>
              }
              <po-button
                #iconCalendar
                class="po-datetimepicker-button"
                [class.po-button-tertiary-danger]="getErrorPattern()"
                p-icon="ICON_CALENDAR_DOTS"
                p-kind="tertiary"
                [p-disabled]="isDisabled || isReadonly"
                [p-size]="hostSize"
                (p-click)="togglePicker(false)"
                (keydown)="eventOnButtonKeydown($event)"
              >
              </po-button>
            </div>
          }
        </div>
      </div>
      @if ((!this.label() && poHelperComponent()) || (!this.label() && poHelper.hideAdditionalHelp)) {
        <po-helper
          #helperEl
          class="po-field-helper-button"
          [p-size]="hostSize"
          [p-helper]="poHelper.helperSettings"
          [p-disabled]="isDisabled"
          [p-append-in-body]="appendBox()"
        >
        </po-helper>
      }
    </div>

    @if (appendBox()) {
      <ng-template
        cdkConnectedOverlay
        [cdkConnectedOverlayOrigin]="trigger"
        [cdkConnectedOverlayOpen]="true"
        [cdkConnectedOverlayDisableClose]="true"
      >
        <ng-container *ngTemplateOutlet="sharedCalendarContent"></ng-container>
      </ng-template>
    } @else {
      <ng-container *ngTemplateOutlet="sharedCalendarContent"></ng-container>
    }
  </div>

  @if (!isReadonly) {
    <po-field-container-bottom
      [p-append-in-body]="appendBox()"
      [p-help]="help()"
      [p-disabled]="isDisabled"
      [p-error-pattern]="getErrorPattern()"
      [p-error-limit]="errorLimit()"
      [p-size]="hostSize"
    ></po-field-container-bottom>
  }
</po-field-container>

<ng-template #sharedCalendarContent>
  <div #dialogPicker [class.po-datetimepicker-popup-calendar]="!verifyMobile()" tabindex="-1" [hidden]="!visible">
    @if (verifyMobile()) {
      <div class="po-datetimepicker-calendar-overlay"></div>
    }
    <po-calendar
      #calendar
      [class.po-datetimepicker-calendar-mobile]="verifyMobile()"
      p-mode="date-time"
      [p-size]="hostSize"
      [p-locale]="locale"
      [p-format]="resolvedTimerFormat"
      [p-min-date]="resolvedMinDate"
      [p-max-date]="resolvedMaxDate"
      [p-max-time]="maxTime()"
      [p-min-time]="minTime()"
      [p-minute-interval]="minuteInterval()"
      [p-second-interval]="secondInterval()"
      [p-show-seconds]="showSeconds()"
      (p-change)="onDateChange($event)"
      (p-change-time)="onTimeChange($event)"
      (p-close)="onCalendarClose()"
      (p-change-month-year)="adjustCalendarPosition()"
      (p-timer-boundary-tab)="onTimerBoundaryTab($event)"
      (keydown)="eventOnCalendarKeydown($event)"
    ></po-calendar>
  </div>
</ng-template>
`
    }]
  }], () => [], {
    iconDatepicker: [{
      type: ViewChild,
      args: ["iconCalendar"]
    }],
    calendarComponent: [{
      type: ViewChild,
      args: ["calendar"]
    }],
    inputEl: [{
      type: ViewChild,
      args: ["inp", {
        read: ElementRef,
        static: true
      }]
    }],
    dialogPicker: [{
      type: ViewChild,
      args: ["dialogPicker", {
        read: ElementRef,
        static: false
      }]
    }],
    helperEl: [{
      type: ViewChild,
      args: ["helperEl", {
        read: PoHelperComponent,
        static: false
      }]
    }],
    datetimepickerField: [{
      type: ViewChild,
      args: ["datetimepickerField", {
        read: ElementRef,
        static: true
      }]
    }],
    onKeyup: [{
      type: HostListener,
      args: ["keyup", ["$event"]]
    }],
    onKeydown: [{
      type: HostListener,
      args: ["keydown", ["$event"]]
    }]
  });
})();
var PoDatetimepickerModule = class _PoDatetimepickerModule {
  static ɵfac = function PoDatetimepickerModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoDatetimepickerModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoDatetimepickerModule,
    declarations: [PoDatetimepickerComponent],
    imports: [FormsModule, CommonModule, OverlayModule, PoCleanModule, PoHelperModule, PoButtonModule, PoLoadingModule, PoCalendarModule, PoFieldContainerModule],
    exports: [PoDatetimepickerComponent]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [FormsModule, CommonModule, OverlayModule, PoCleanModule, PoHelperModule, PoButtonModule, PoLoadingModule, PoCalendarModule, PoFieldContainerModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoDatetimepickerModule, [{
    type: NgModule,
    args: [{
      imports: [FormsModule, CommonModule, OverlayModule, PoCleanModule, PoHelperModule, PoButtonModule, PoLoadingModule, PoCalendarModule, PoFieldContainerModule],
      exports: [PoDatetimepickerComponent],
      declarations: [PoDatetimepickerComponent]
    }]
  }], null, null);
})();
var PoTimepickerModelFormat;
(function(PoTimepickerModelFormat2) {
  PoTimepickerModelFormat2["HourMinute"] = "HH:mm";
  PoTimepickerModelFormat2["HourMinuteSecond"] = "HH:mm:ss";
})(PoTimepickerModelFormat || (PoTimepickerModelFormat = {}));
var poTimepickerLiterals = {
  en: {
    field: "Time selection",
    open: "Open time picker",
    clean: "Clear field",
    invalidTime: "Invalid time",
    outOfRangeTime: "Time out of range",
    hour: "Hour",
    minute: "Minute",
    second: "Second",
    selectedTime: "Selected time"
  },
  es: {
    field: "Selección de hora",
    open: "Abrir selector de hora",
    clean: "Limpiar campo",
    invalidTime: "Hora no válida",
    outOfRangeTime: "Hora fuera del período",
    hour: "Hora",
    minute: "Minuto",
    second: "Segundo",
    selectedTime: "Hora seleccionada"
  },
  pt: {
    field: "Seleção de horário",
    open: "Abrir seletor de hora",
    clean: "Limpar campo",
    invalidTime: "Hora inválida",
    outOfRangeTime: "Hora fora do período",
    hour: "Hora",
    minute: "Minuto",
    second: "Segundo",
    selectedTime: "Horário selecionado"
  },
  ru: {
    field: "Выбор времени",
    open: "Открыть выбор времени",
    clean: "Очистить поле",
    invalidTime: "Недопустимое время",
    outOfRangeTime: "Время вне допустимого диапазона",
    hour: "Час",
    minute: "Минута",
    second: "Секунда",
    selectedTime: "Выбранное время"
  }
};
var PoTimepickerBaseComponent = class _PoTimepickerBaseComponent {
  languageService;
  cd;
  additionalHelpEventTrigger;
  /**
   * @optional
   *
   * @description
   *
   * Aplica foco no elemento ao ser iniciado.
   *
   * @default `false`
   */
  autoFocus = false;
  /**
   * @Input
   *
   * @optional
   *
   * @description
   * Define se o título do campo será exibido de forma compacta.
   *
   * @default `false`
   */
  compactLabel = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "compactLabel"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-compact-label",
    transform: convertToBoolean
  }));
  /** Nome do componente. */
  name;
  /**
   * @optional
   *
   * @description
   *
   * Define se a indicação de campo opcional será exibida.
   *
   * @default `false`
   */
  optional;
  /**
   * Mensagem apresentada quando o horário for inválido ou fora do período.
   *
   * > Por padrão, esta mensagem não é apresentada quando o campo estiver vazio, mesmo que ele seja requerido.
   */
  errorPattern = "";
  /**
   * @optional
   *
   * @description
   *
   * Limita a exibição da mensagem de erro a duas linhas e exibe um tooltip com o texto completo.
   *
   * @default `false`
   */
  errorLimit = false;
  /**
   * @optional
   *
   * @description
   *
   * Exibe a mensagem setada na propriedade `p-error-pattern` se o campo estiver vazio e for requerido.
   *
   * @default `false`
   */
  showErrorMessageRequired = false;
  /** Evento disparado ao sair do campo. */
  onblur = new EventEmitter();
  /** Evento disparado ao alterar valor do campo. */
  onchange = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Evento disparado sempre que o valor do model é alterado, seja por interação do usuário
   * ou por atualização programática (ex: `setValue`, `patchValue`, carregamento assíncrono).
   *
   * Diferentemente do `p-change`, que é disparado apenas por interação do usuário,
   * o `p-change-model` cobre todos os cenários de alteração de valor.
   *
   * Não emite quando o novo valor é idêntico ao anterior (deduplicação automática).
   */
  changeModel = new EventEmitter();
  /** Evento disparado quando uma tecla é pressionada enquanto o foco está no componente. */
  keydown = new EventEmitter();
  /**
   * @Input
   *
   * @optional
   *
   * @description
   *
   * Define as opções do componente de ajuda (po-helper).
   */
  poHelperComponent = input(void 0, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "poHelperComponent"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-helper"
  }));
  /**
   * @Input
   *
   * @optional
   *
   * @description
   *
   * Habilita a quebra automática do texto da propriedade `p-label`.
   *
   * @default `false`
   */
  labelTextWrap = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "labelTextWrap"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-label-text-wrap"
  }));
  /** Desabilita o campo. */
  disabled = false;
  /** Torna o elemento somente leitura. */
  readonly = false;
  /**
   * @optional
   *
   * @description
   *
   * Define que o campo será obrigatório.
   *
   * @default `false`
   */
  required = false;
  /** Habilita ação para limpar o campo. */
  clean = false;
  modelLastUpdate;
  onChangeModel = null;
  validatorChange;
  onTouchedModel = null;
  shortLanguage;
  isInvalid;
  hasValidatorRequired = false;
  objMask;
  _format = PoTimerFormat.Format24;
  _modelFormat;
  _maxTime;
  _minTime;
  _minuteInterval = 5;
  _secondInterval = 1;
  _showSeconds = false;
  _noAutocomplete = false;
  _placeholder;
  _loading = false;
  _size = void 0;
  _initialSize = void 0;
  _locale;
  _timeValue = "";
  _validationValue;
  _validationMinHour;
  _validationMaxHour;
  previousValue;
  pendingChangeValue = null;
  /**
   * @optional
   *
   * @description
   *
   * Define a propriedade nativa `autocomplete` do campo como `off`.
   *
   * @default `false`
   */
  set noAutocomplete(value) {
    this._noAutocomplete = value;
  }
  get noAutocomplete() {
    return this._noAutocomplete;
  }
  /**
   * @optional
   *
   * @description
   *
   * Mensagem que aparecerá enquanto o campo não estiver preenchido.
   *
   * Para personalizar os segmentos, informe o valor no formato `HH:mm` ou `HH:mm:ss`.
   */
  set placeholder(placeholder) {
    this._placeholder = isTypeof(placeholder, "string") ? placeholder : "";
  }
  get placeholder() {
    return this._placeholder;
  }
  set setDisabled(disabled) {
    this.disabled = disabled;
    this.validateModel(this._timeValue);
  }
  set setReadonly(readonly) {
    this.readonly = readonly;
  }
  set setRequired(required) {
    this.required = required;
    this.validateModel(this._timeValue);
  }
  /** Define se a indicação de campo obrigatório será exibida. */
  showRequired = false;
  /**
   * @optional
   *
   * @description
   *
   * Define o tamanho do componente:
   * - `small`: altura do input como 32px (disponível apenas para acessibilidade AA).
   * - `medium`: altura do input como 44px.
   *
   * @default `medium`
   */
  set size(value) {
    this._initialSize = value;
    this.applySizeBasedOnA11y();
  }
  get size() {
    return this._size ?? getDefaultSizeFn(PoFieldSize);
  }
  set setClean(clean) {
    this.clean = clean;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define o formato de exibição do timer.
   *
   * Valores válidos:
   * - `24`: formato de 24 horas (padrão)
   * - `12`: formato de 12 horas com indicador AM/PM
   *
   * @default `24`
   */
  set format(value) {
    this._format = Object.values(PoTimerFormat).includes(value) ? value : PoTimerFormat.Format24;
    this.updateMask();
    this.refreshValue(this._timeValue);
  }
  get format() {
    return this._format;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define o horário mínimo permitido. Formato: `HH:mm` ou `HH:mm:ss`.
   */
  set minTime(value) {
    this._minTime = this.isValidTimeString(value) ? value : void 0;
    this.validateModel(this._timeValue);
  }
  get minTime() {
    return this._minTime;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define o horário máximo permitido. Formato: `HH:mm` ou `HH:mm:ss`.
   */
  set maxTime(value) {
    this._maxTime = this.isValidTimeString(value) ? value : void 0;
    this.validateModel(this._timeValue);
  }
  get maxTime() {
    return this._maxTime;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define o intervalo entre os minutos exibidos no painel.
   *
   * @default `5`
   */
  set minuteInterval(value) {
    const parsed = parseInt(value, 10);
    this._minuteInterval = parsed > 0 && parsed < 60 ? parsed : 5;
  }
  get minuteInterval() {
    return this._minuteInterval;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define o intervalo entre os segundos exibidos no painel.
   *
   * @default `1`
   */
  set secondInterval(value) {
    const parsed = parseInt(value, 10);
    this._secondInterval = parsed > 0 && parsed < 60 ? parsed : 1;
  }
  get secondInterval() {
    return this._secondInterval;
  }
  /**
   * @optional
   *
   * @description
   *
   * Exibe a coluna de segundos no painel.
   *
   * @default `false`
   */
  set showSeconds(value) {
    this._showSeconds = value;
    this.updateMask();
    this.refreshValue(this._timeValue);
  }
  get showSeconds() {
    return this._showSeconds;
  }
  /**
   * @optional
   *
   * @description
   *
   * Padrão de formatação para saída do *model*.
   *
   * > Veja os valores válidos no *enum* `PoTimepickerModelFormat`.
   */
  set modelFormat(value) {
    if (Object.values(PoTimepickerModelFormat).includes(value)) {
      this._modelFormat = value;
    }
  }
  get modelFormat() {
    return this._modelFormat;
  }
  /**
   * @optional
   *
   * @description
   *
   * Idioma do componente.
   */
  set locale(value) {
    this._locale = value && value.length >= 2 ? value : this.shortLanguage;
    this.onLocaleChange();
  }
  get locale() {
    return this._locale || this.shortLanguage;
  }
  /**
   * @optional
   *
   * @description
   * Exibe um ícone de carregamento no lado direito do campo para sinalizar que uma operação está em andamento.
   *
   * @default `false`
   */
  set loading(value) {
    this._loading = value;
    this.cd?.markForCheck();
  }
  get loading() {
    return this._loading;
  }
  get isDisabled() {
    return this.disabled || this.loading;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define que o painel do timer será incluído no body da página.
   *
   * @default `false`
   */
  appendBox = false;
  get is12HourFormat() {
    return this._format === PoTimerFormat.Format12;
  }
  constructor(languageService, cd) {
    this.languageService = languageService;
    this.cd = cd;
  }
  ngOnInit() {
    this.shortLanguage = this.languageService.getShortLanguage();
    if (!this._locale) {
      this._locale = this.shortLanguage;
    }
    this.updateMask();
  }
  onThemeChange() {
    this.applySizeBasedOnA11y();
  }
  get autocomplete() {
    return this.noAutocomplete ? "off" : "on";
  }
  get timeValue() {
    return this._timeValue;
  }
  set timeValue(value) {
    this._timeValue = value;
  }
  // Constrói a máscara para o campo de input com base no formato.
  updateMask() {
    if (this._showSeconds) {
      this.objMask = new PoMask("99:99:99", true);
    } else {
      this.objMask = new PoMask("99:99", true);
    }
  }
  // Valida uma string de horário (HH:mm ou HH:mm:ss).
  isValidTimeString(value, minHour = 0, maxHour = 23) {
    if (!value || typeof value !== "string") {
      return false;
    }
    const match = /^(\d{2}):(\d{2})(?::(\d{2}))?$/.exec(value);
    if (!match) {
      return false;
    }
    const hours = parseInt(match[1], 10);
    const minutes = parseInt(match[2], 10);
    const seconds = match[3] !== void 0 ? parseInt(match[3], 10) : 0;
    if (Number.isNaN(hours) || Number.isNaN(minutes) || Number.isNaN(seconds)) {
      return false;
    }
    return hours >= minHour && hours <= maxHour && minutes >= 0 && minutes <= 59 && seconds >= 0 && seconds <= 59;
  }
  // Verifica se o horário está dentro dos limites min/max.
  isTimeInRange(time) {
    if (!time || !this.isValidTimeString(time)) {
      return true;
    }
    const timeMinutes = this.timeToMinutes(time);
    if (this._minTime) {
      const minMinutes = this.timeToMinutes(this._minTime);
      if (timeMinutes < minMinutes) {
        return false;
      }
    }
    if (this._maxTime) {
      const maxMinutes = this.timeToMinutes(this._maxTime);
      if (timeMinutes > maxMinutes) {
        return false;
      }
    }
    return true;
  }
  // Converte string de horário para minutos totais (ou segundos se tiver segundos).
  timeToMinutes(time) {
    const parts = time.split(":");
    const hours = parseInt(parts[0], 10) || 0;
    const minutes = parseInt(parts[1], 10) || 0;
    const seconds = parts.length >= 3 ? parseInt(parts[2], 10) || 0 : 0;
    return hours * 3600 + minutes * 60 + seconds;
  }
  // Formata o valor de saída do model conforme o iso-format configurado.
  formatOutput(time) {
    if (!time || !this.isValidTimeString(time)) {
      return "";
    }
    if (this._modelFormat === PoTimepickerModelFormat.HourMinuteSecond) {
      return time.length === 5 ? `${time}:00` : time;
    }
    if (this._modelFormat === PoTimepickerModelFormat.HourMinute) {
      return time.substring(0, 5);
    }
    return time;
  }
  setValidationValue(value, minHour, maxHour) {
    this._validationValue = value;
    this._validationMinHour = minHour;
    this._validationMaxHour = maxHour;
  }
  clearValidationValue() {
    this._validationValue = void 0;
    this._validationMinHour = void 0;
    this._validationMaxHour = void 0;
  }
  hasValidationValue() {
    return !!this._validationValue;
  }
  getValidationValue(controlValue) {
    if (typeof this._validationValue === "string" && this._validationValue !== "") {
      return this._validationValue;
    }
    return typeof controlValue === "string" ? controlValue : "";
  }
  getValidationHourRange() {
    if (typeof this._validationMinHour === "number" && typeof this._validationMaxHour === "number") {
      return {
        minHour: this._validationMinHour,
        maxHour: this._validationMaxHour
      };
    }
    return void 0;
  }
  isGeneratedErrorPattern(errorPattern) {
    if (!errorPattern) {
      return false;
    }
    return Object.values(poTimepickerLiterals).some((literal) => errorPattern === literal.invalidTime || errorPattern === literal.outOfRangeTime);
  }
  getDefaultInvalidTimeMessage() {
    const key2 = this.locale || poLocaleDefault;
    return poTimepickerLiterals[key2]?.invalidTime || poTimepickerLiterals.pt.invalidTime;
  }
  getDefaultOutOfRangeTimeMessage() {
    const key2 = this.locale || poLocaleDefault;
    return poTimepickerLiterals[key2]?.outOfRangeTime || poTimepickerLiterals.pt.outOfRangeTime;
  }
  // Executa a função onChange, aplicando formatOutput automaticamente.
  callOnChange(value) {
    const formatted = typeof value === "string" && value ? this.formatOutput(value) || value : value;
    if (this.onChangeModel) {
      if (formatted !== this.previousValue) {
        this.onChangeModel(formatted);
        this.previousValue = formatted;
      }
      this.pendingChangeValue = null;
    } else {
      this.pendingChangeValue = {
        value: formatted
      };
    }
    this.controlChangeModelEmitter(formatted);
  }
  controlChangeModelEmitter(value) {
    if (this.modelLastUpdate !== value) {
      this.changeModel.emit(value);
      this.modelLastUpdate = value;
    }
  }
  mapSizeToIcon(size) {
    return mapInputSizeToLoadingIcon(size);
  }
  setDisabledState(isDisabled) {
    this.disabled = isDisabled;
    this.cd.markForCheck();
  }
  registerOnChange(func) {
    this.onChangeModel = func;
    if (this.pendingChangeValue) {
      const pending = this.pendingChangeValue;
      this.pendingChangeValue = null;
      this.callOnChange(pending.value);
    }
  }
  registerOnTouched(func) {
    this.onTouchedModel = func;
  }
  registerOnValidatorChange(fn) {
    this.validatorChange = fn;
  }
  validate(c) {
    if (!this.hasValidationValue() && this.isValidTimeString(this.getValidationValue(c.value))) {
      this.errorPattern = this.isGeneratedErrorPattern(this.errorPattern) ? "" : this.errorPattern;
    }
    if (!this.hasValidatorRequired && this.showErrorMessageRequired && c.hasValidator(Validators.required)) {
      this.hasValidatorRequired = true;
    }
    const valueToValidate = this.getValidationValue(c.value);
    const validationHourRange = this.getValidationHourRange();
    const isValidTime = !valueToValidate || (validationHourRange ? this.isValidTimeString(valueToValidate, validationHourRange.minHour, validationHourRange.maxHour) : this.isValidTimeString(valueToValidate));
    if (valueToValidate && !isValidTime) {
      this.errorPattern = this.errorPattern || this.getDefaultInvalidTimeMessage();
      this.cd?.markForCheck();
      return {
        time: {
          valid: false
        }
      };
    }
    if (valueToValidate && !this.isTimeInRange(valueToValidate)) {
      this.errorPattern = this.errorPattern || this.getDefaultOutOfRangeTimeMessage();
      this.cd?.markForCheck();
      return {
        time: {
          valid: false
        }
      };
    }
    if (PoValidators.requiredFailed(this.required || this.hasValidatorRequired, this.disabled, c.value)) {
      this.cd?.markForCheck();
      return {
        required: {
          valid: false
        }
      };
    }
    return null;
  }
  validateModel(model2) {
    if (this.validatorChange) {
      this.validatorChange(model2);
    }
  }
  applySizeBasedOnA11y() {
    const size = validateSizeFn(this._initialSize, PoFieldSize);
    this._size = size;
  }
  onLocaleChange() {
    if (this.isGeneratedErrorPattern(this.errorPattern)) {
      const isOutOfRange = Object.values(poTimepickerLiterals).some((literal) => this.errorPattern === literal.outOfRangeTime);
      this.errorPattern = isOutOfRange ? this.getDefaultOutOfRangeTimeMessage() : this.getDefaultInvalidTimeMessage();
    }
  }
  static ɵfac = function PoTimepickerBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoTimepickerBaseComponent)(ɵɵdirectiveInject(PoLanguageService), ɵɵdirectiveInject(ChangeDetectorRef));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoTimepickerBaseComponent,
    hostVars: 1,
    hostBindings: function PoTimepickerBaseComponent_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("PoUiThemeChange", function PoTimepickerBaseComponent_PoUiThemeChange_HostBindingHandler() {
          return ctx.onThemeChange();
        }, ɵɵresolveWindow);
      }
      if (rf & 2) {
        ɵɵattribute("p-size", ctx.size);
      }
    },
    inputs: {
      additionalHelpEventTrigger: "additionalHelpEventTrigger",
      autoFocus: [2, "p-auto-focus", "autoFocus", convertToBoolean],
      compactLabel: [1, "p-compact-label", "compactLabel"],
      name: "name",
      optional: [0, "p-optional", "optional"],
      errorPattern: [0, "p-error-pattern", "errorPattern"],
      errorLimit: [0, "p-error-limit", "errorLimit"],
      showErrorMessageRequired: [0, "p-required-field-error-message", "showErrorMessageRequired"],
      poHelperComponent: [1, "p-helper", "poHelperComponent"],
      labelTextWrap: [1, "p-label-text-wrap", "labelTextWrap"],
      noAutocomplete: [2, "p-no-autocomplete", "noAutocomplete", convertToBoolean],
      placeholder: [0, "p-placeholder", "placeholder"],
      setDisabled: [2, "p-disabled", "setDisabled", convertToBoolean],
      setReadonly: [2, "p-readonly", "setReadonly", convertToBoolean],
      setRequired: [2, "p-required", "setRequired", convertToBoolean],
      showRequired: [0, "p-show-required", "showRequired"],
      size: [0, "p-size", "size"],
      setClean: [2, "p-clean", "setClean", convertToBoolean],
      format: [0, "p-format", "format"],
      minTime: [0, "p-min-time", "minTime"],
      maxTime: [0, "p-max-time", "maxTime"],
      minuteInterval: [0, "p-minute-interval", "minuteInterval"],
      secondInterval: [0, "p-second-interval", "secondInterval"],
      showSeconds: [2, "p-show-seconds", "showSeconds", convertToBoolean],
      modelFormat: [0, "p-model-format", "modelFormat"],
      locale: [0, "p-locale", "locale"],
      loading: [2, "p-loading", "loading", convertToBoolean],
      appendBox: [2, "p-append-in-body", "appendBox", convertToBoolean]
    },
    outputs: {
      onblur: "p-blur",
      onchange: "p-change",
      changeModel: "p-change-model",
      keydown: "p-keydown"
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoTimepickerBaseComponent, [{
    type: Directive
  }], () => [{
    type: PoLanguageService
  }, {
    type: ChangeDetectorRef
  }], {
    additionalHelpEventTrigger: [{
      type: Input
    }],
    autoFocus: [{
      type: Input,
      args: [{
        alias: "p-auto-focus",
        transform: convertToBoolean
      }]
    }],
    compactLabel: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-compact-label",
        required: false
      }]
    }],
    name: [{
      type: Input,
      args: ["name"]
    }],
    optional: [{
      type: Input,
      args: ["p-optional"]
    }],
    errorPattern: [{
      type: Input,
      args: ["p-error-pattern"]
    }],
    errorLimit: [{
      type: Input,
      args: ["p-error-limit"]
    }],
    showErrorMessageRequired: [{
      type: Input,
      args: ["p-required-field-error-message"]
    }],
    onblur: [{
      type: Output,
      args: ["p-blur"]
    }],
    onchange: [{
      type: Output,
      args: ["p-change"]
    }],
    changeModel: [{
      type: Output,
      args: ["p-change-model"]
    }],
    keydown: [{
      type: Output,
      args: ["p-keydown"]
    }],
    poHelperComponent: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-helper",
        required: false
      }]
    }],
    labelTextWrap: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-label-text-wrap",
        required: false
      }]
    }],
    noAutocomplete: [{
      type: Input,
      args: [{
        alias: "p-no-autocomplete",
        transform: convertToBoolean
      }]
    }],
    placeholder: [{
      type: Input,
      args: ["p-placeholder"]
    }],
    setDisabled: [{
      type: Input,
      args: [{
        alias: "p-disabled",
        transform: convertToBoolean
      }]
    }],
    setReadonly: [{
      type: Input,
      args: [{
        alias: "p-readonly",
        transform: convertToBoolean
      }]
    }],
    setRequired: [{
      type: Input,
      args: [{
        alias: "p-required",
        transform: convertToBoolean
      }]
    }],
    showRequired: [{
      type: Input,
      args: ["p-show-required"]
    }],
    size: [{
      type: Input,
      args: ["p-size"]
    }, {
      type: HostBinding,
      args: ["attr.p-size"]
    }],
    setClean: [{
      type: Input,
      args: [{
        alias: "p-clean",
        transform: convertToBoolean
      }]
    }],
    format: [{
      type: Input,
      args: ["p-format"]
    }],
    minTime: [{
      type: Input,
      args: ["p-min-time"]
    }],
    maxTime: [{
      type: Input,
      args: ["p-max-time"]
    }],
    minuteInterval: [{
      type: Input,
      args: ["p-minute-interval"]
    }],
    secondInterval: [{
      type: Input,
      args: ["p-second-interval"]
    }],
    showSeconds: [{
      type: Input,
      args: [{
        alias: "p-show-seconds",
        transform: convertToBoolean
      }]
    }],
    modelFormat: [{
      type: Input,
      args: ["p-model-format"]
    }],
    locale: [{
      type: Input,
      args: ["p-locale"]
    }],
    loading: [{
      type: Input,
      args: [{
        alias: "p-loading",
        transform: convertToBoolean
      }]
    }],
    appendBox: [{
      type: Input,
      args: [{
        alias: "p-append-in-body",
        transform: convertToBoolean
      }]
    }],
    onThemeChange: [{
      type: HostListener,
      args: ["window:PoUiThemeChange"]
    }]
  });
})();
var poTimerContentOffset = 8;
var poTimerPositionDefault = "bottom-left";
var PoTimepickerComponent = class _PoTimepickerComponent extends PoTimepickerBaseComponent {
  controlPosition = inject(PoControlPositionService);
  renderer = inject(Renderer2);
  dialogPicker;
  iconTimepicker;
  inputEl;
  minuteInputEl;
  secondInputEl;
  timepickerFieldEl;
  iconClean;
  helperEl;
  timerComponent;
  periodInputEl;
  /** Rótulo do campo. */
  label;
  /** Texto de apoio do campo. */
  help;
  displayAdditionalHelp = false;
  el;
  id = `po-timepicker[${uuid()}]`;
  visible = false;
  literals;
  // Valores de exibicao para os inputs de segmento individuais.
  hourDisplay = "";
  minuteDisplay = "";
  secondDisplay = "";
  periodDisplay = "";
  isSegmentFocused = false;
  ariaLiveMessage = "";
  eventListenerFunction;
  eventResizeListener;
  get cleanElementRef() {
    const combined = [this.hourDisplay, this.minuteDisplay, this.secondDisplay].filter((s) => s !== "").join(":");
    return {
      nativeElement: {
        value: combined
      }
    };
  }
  clickListener;
  timeoutChange;
  valueBeforeChange;
  constructor() {
    const languageService = inject(PoLanguageService);
    const cd = inject(ChangeDetectorRef);
    const el = inject(ElementRef);
    super(languageService, cd);
    this.languageService = languageService;
    this.cd = cd;
    this.shortLanguage = this.languageService.getShortLanguage();
    this.el = el;
    const language = languageService.getShortLanguage();
    this.literals = __spreadValues({}, poTimepickerLiterals[language]);
  }
  get hourPlaceholder() {
    return this.getCustomPlaceholderSegment(0) ?? "";
  }
  get minutePlaceholder() {
    return this.getCustomPlaceholderSegment(1) ?? "";
  }
  get secondPlaceholder() {
    return this.getCustomPlaceholderSegment(2) ?? "";
  }
  get isPlaceholder() {
    return !this.hourDisplay && !this.minuteDisplay && (!this.showSeconds || !this.secondDisplay);
  }
  get showSeparator() {
    if (!this.placeholder?.trim()) {
      return true;
    }
    if (this.hasValue()) {
      return true;
    }
    return this.placeholder.includes(":");
  }
  get customPlaceholderSegments() {
    if (!this.placeholder?.trim()) {
      return [];
    }
    return this.placeholder.split(":").map((segment) => segment.trim());
  }
  getCustomPlaceholderSegment(index) {
    return this.customPlaceholderSegments[index];
  }
  onHostFocusOut(event) {
    const relatedTarget = event.relatedTarget;
    const dialogEl = this.dialogPicker?.nativeElement;
    const isStillInsideComponent = relatedTarget && (this.timepickerFieldEl.nativeElement.contains(relatedTarget) || dialogEl?.contains(relatedTarget) || this.iconClean?.nativeElement?.contains(relatedTarget) || this.iconTimepicker?.buttonElement?.nativeElement?.contains(relatedTarget) || relatedTarget.contains(this.el.nativeElement));
    if (!isStillInsideComponent) {
      this.onblur.emit();
      if (this.visible) {
        this.closeTimer(false, true);
      }
    }
  }
  onKeydown($event) {
    if (this.readonly) {
      return;
    }
    if ($event.key === "Escape" && this.visible) {
      this.togglePicker(false);
      $event.preventDefault();
      $event.stopPropagation();
    }
    if ($event.key === "Tab" && $event.shiftKey && $event.target instanceof HTMLInputElement && this.visible && !$event.target.classList?.contains("po-timepicker-segment-input")) {
      this.togglePicker();
    }
  }
  ngAfterViewInit() {
    this.setDialogPickerStyleDisplay("none");
    if (this.autoFocus) {
      this.focus();
    }
  }
  ngOnChanges(changes) {
    if (changes.label) {
      this.displayAdditionalHelp = false;
    }
  }
  ngOnDestroy() {
    this.removeListeners();
  }
  onLocaleChange() {
    super.onLocaleChange();
    const lang = this.locale;
    this.literals = __spreadValues({}, poTimepickerLiterals[lang] || poTimepickerLiterals[this.shortLanguage]);
    this.cd.markForCheck();
  }
  emitAdditionalHelp() {
  }
  /**
   * Função que atribui foco ao componente.
   *
   * Para utilizá-la é necessário ter a instância do componente no DOM, podendo ser utilizado o ViewChild da seguinte forma:
   *
   * ```
   * import { PoTimepickerComponent } from '@po-ui/ng-components';
   *
   * ...
   *
   * @ViewChild(PoTimepickerComponent, { static: true }) timepicker: PoTimepickerComponent;
   *
   * focusTimepicker() {
   *   this.timepicker.focus();
   * }
   * ```
   */
  focus() {
    if (!this.isDisabled && this.inputEl?.nativeElement) {
      this.inputEl.nativeElement.focus();
    }
  }
  getAdditionalHelpTooltip() {
    return null;
  }
  togglePicker(focusInput = true) {
    if (this.isDisabled || this.readonly || !this.iconTimepicker?.buttonElement?.nativeElement) {
      return;
    }
    if (!this.visible) {
      this.visible = true;
      this.initializeListeners();
      this.renderer.setAttribute(this.inputEl.nativeElement, "aria-expanded", "true");
      this.renderer.setAttribute(this.iconTimepicker.buttonElement.nativeElement, "aria-expanded", "true");
      requestAnimationFrame(() => {
        this.setTimerPosition();
        this.timerComponent?.initAllColumnOffsets();
      });
    } else {
      this.inputEl.nativeElement.disabled = false;
      this.closeTimer(focusInput);
      this.renderer.removeAttribute(this.inputEl.nativeElement, "aria-expanded");
      this.renderer.removeAttribute(this.iconTimepicker.buttonElement.nativeElement, "aria-expanded");
    }
  }
  closeTimer(focusInput = true, skipRefocus = false) {
    this.completeSecondsOnClose();
    this.visible = false;
    this.removeListeners();
    this.setDialogPickerStyleDisplay("none");
    if (!this.verifyMobile() && focusInput) {
      this.focus();
    }
    if (!focusInput && !skipRefocus && this.clean && this.hasValue()) {
      setTimeout(() => {
        this.iconTimepicker.focus();
      });
    }
  }
  // Chamado quando o po-timer emite p-change com o horario selecionado.
  timerSelected(time) {
    if (!time) {
      this.clear();
      setTimeout(() => this.closeTimer(), 200);
      this.onchange.emit();
      return;
    }
    const hadValue = this.hasValue();
    this.onTouchedModel?.();
    this.clearValidationValue();
    if (this.isGeneratedErrorPattern(this.errorPattern)) {
      this.errorPattern = "";
    }
    this.timeValue = time;
    this.updateInputDisplay(time);
    this.updateAriaLiveMessage(time);
    this.callOnChange(time);
    this.validateModel(time);
    this.controlChangeEmitter();
    if (!hadValue) {
      this.iconTimepicker?.focus();
      this.closeTimer(false);
    }
  }
  wasClickedOnPicker(event) {
    if (!this.dialogPicker || !this.iconTimepicker) {
      return;
    }
    if (!this.dialogPicker.nativeElement.contains(event.target) && !this.iconTimepicker.buttonElement.nativeElement.contains(event.target)) {
      this.closeTimer();
    }
  }
  hasInvalidClass() {
    return this.hasValidationValue() || this.el.nativeElement.classList.contains("ng-invalid") && this.el.nativeElement.classList.contains("ng-dirty") && (this.hasValue() || this.showErrorMessageRequired && (this.required || this.hasValidatorRequired));
  }
  // Retorna true se algum segmento possui valor.
  hasValue() {
    return this.hourDisplay !== "" || this.minuteDisplay !== "" || this.secondDisplay !== "";
  }
  getErrorPattern() {
    return this.errorPattern !== "" && this.hasInvalidClass() ? this.errorPattern : "";
  }
  clear() {
    this.valueBeforeChange = this.timeValue;
    this.timeValue = "";
    this.clearValidationValue();
    this.hourDisplay = "";
    this.minuteDisplay = "";
    this.secondDisplay = "";
    this.periodDisplay = this.getDefaultPeriodDisplay();
    this.ariaLiveMessage = "";
    if (this.isGeneratedErrorPattern(this.errorPattern)) {
      this.errorPattern = "";
    }
    if (this.inputEl?.nativeElement) {
      this.inputEl.nativeElement.value = "";
    }
    if (this.minuteInputEl?.nativeElement) {
      this.minuteInputEl.nativeElement.value = "";
    }
    if (this.secondInputEl?.nativeElement) {
      this.secondInputEl.nativeElement.value = "";
    }
    this.callOnChange("");
    this.validateModel("");
    this.controlChangeEmitter();
  }
  clearAndFocus() {
    this.clear();
    setTimeout(() => {
      this.focus();
    }, 200);
  }
  eventOnBlur($event) {
    this.onTouchedModel?.();
    this.onblur.emit();
    this.validateAndUpdateModel();
    this.controlChangeEmitter();
  }
  eventOnClick($event) {
    if (this.verifyMobile()) {
      $event.target.blur();
      setTimeout(() => this.togglePicker(), 0);
    }
  }
  onKeyDown(event) {
    const isFieldFocused = this.el.nativeElement.contains(document.activeElement);
    const target = event.target;
    const isIconTarget = this.iconClean?.nativeElement?.contains(target) || this.iconTimepicker?.buttonElement?.nativeElement?.contains(target);
    if (isFieldFocused && !isIconTarget) {
      this.keydown.emit(event);
    }
  }
  onKeyPress(event) {
    if (event.key === "Tab" && event.shiftKey && !this.visible && this.clean && this.hasValue()) {
      this.iconClean.nativeElement?.focus();
      event.preventDefault();
      return;
    }
    if (event.key === "Tab" && event.shiftKey && !this.visible) {
      this.focusLastSegment();
      event.preventDefault();
      event.stopPropagation();
    }
  }
  // Trata o input em um campo de segmento (hora, minuto, segundo).
  onSegmentInput(event, segment) {
    const input2 = event.target;
    input2.value = input2.value.replace(/\D/g, "");
    switch (segment) {
      case "hour":
        this.hourDisplay = input2.value;
        break;
      case "minute":
        this.minuteDisplay = input2.value;
        break;
      case "second":
        this.secondDisplay = input2.value;
        break;
    }
    if (input2.value.length >= 2) {
      this.advanceToNextSegment(segment);
    }
    this.updateCombinedValue();
  }
  // Trata keydown em um campo de segmento para navegacao.
  onSegmentKeydown(event, segment) {
    if (this.handleSegmentNavigation(event, segment)) {
      return;
    }
    if (this.handleSegmentArrowKeys(event, segment)) {
      return;
    }
    if (this.handleSegmentNonNumericBlock(event)) {
      return;
    }
    if (event.key === "Tab" && !event.shiftKey && this.visible && this.isLastSegment(segment)) {
      this.focusTimer(event);
    }
  }
  handleSegmentNavigation(event, segment) {
    const input2 = event.target;
    if (event.key === "Tab" && event.shiftKey && this.visible && segment !== "hour") {
      this.advanceToPreviousSegment(segment);
      event.preventDefault();
      event.stopPropagation();
      return true;
    }
    if (event.key === "Tab" && !event.shiftKey && !this.visible) {
      if (this.advanceToNextSegment(segment)) {
        event.preventDefault();
        event.stopPropagation();
        return true;
      }
      return false;
    }
    if (event.key === "Backspace" && input2.value === "" && input2.selectionStart === 0) {
      this.advanceToPreviousSegment(segment);
      event.preventDefault();
      return true;
    }
    if (event.key === "ArrowLeft" && input2.selectionStart === 0) {
      this.advanceToPreviousSegment(segment);
      event.preventDefault();
      return true;
    }
    if (event.key === "ArrowRight" && input2.selectionStart === input2.value.length) {
      this.advanceToNextSegment(segment);
      event.preventDefault();
      return true;
    }
    return false;
  }
  handleSegmentArrowKeys(event, segment) {
    if (event.key !== "ArrowUp" && event.key !== "ArrowDown") {
      return false;
    }
    event.preventDefault();
    if (!this.readonly) {
      this.incrementSegment(segment, event.key === "ArrowUp" ? 1 : -1);
    }
    return true;
  }
  handleSegmentNonNumericBlock(event) {
    if (event.key.length === 1 && !/\d/.test(event.key) && !event.ctrlKey && !event.metaKey) {
      event.preventDefault();
      return true;
    }
    return false;
  }
  // Trata blur em um campo de segmento.
  onSegmentBlur(event) {
    this.isSegmentFocused = false;
    const sourceInput = event.target;
    const normalizedSegmentOnBlur = this.normalizeSingleDigitSegment(sourceInput);
    const relatedTarget = event.relatedTarget;
    const dialogEl = this.dialogPicker?.nativeElement;
    const isInternalFocus = relatedTarget && this.el.nativeElement.contains(relatedTarget) && (this.timepickerFieldEl?.nativeElement?.contains(relatedTarget) || dialogEl?.contains(relatedTarget) || this.iconClean?.nativeElement?.contains(relatedTarget) || this.iconTimepicker?.buttonElement?.nativeElement?.contains(relatedTarget));
    if (normalizedSegmentOnBlur) {
      this.updateCombinedValue();
    }
    if (isInternalFocus && this.shouldCommitForInternalFocusTarget(relatedTarget)) {
      this.onTouchedModel?.();
      this.validateAndUpdateModel();
      this.controlChangeEmitter();
      return;
    }
    if (!isInternalFocus) {
      this.onTouchedModel?.();
      this.validateAndUpdateModel();
      this.controlChangeEmitter();
    }
  }
  // Trata clique no wrapper do campo para focar o primeiro input de segmento.
  onFieldClick(event) {
    const target = event.target;
    if (!target.classList.contains("po-timepicker-segment-input") && target !== this.iconTimepicker.buttonElement?.nativeElement && target !== this.iconTimepicker.buttonElement?.nativeElement.querySelector("i") && !this.isDisabled && !this.readonly) {
      this.focus();
    }
  }
  // Trata foco em um campo de segmento.
  onSegmentFocus() {
    this.isSegmentFocused = true;
  }
  // Trata keydown no toggle de periodo AM/PM.
  onPeriodSegmentKeydown(event) {
    if (event.key === "ArrowUp" || event.key === "ArrowDown") {
      event.preventDefault();
      if (!this.readonly && !this.isDisabled) {
        this.togglePeriod();
      }
      return;
    }
    if (event.key === "Backspace" || event.key === "Delete") {
      event.preventDefault();
      return;
    }
    if (event.key === "Tab" && event.shiftKey) {
      event.preventDefault();
      this.focusPreviousPeriodSegment();
      return;
    }
    if (event.key === "Tab" && !event.shiftKey) {
      event.preventDefault();
      this.focusNextPeriodSegment(event);
    }
  }
  // Trata clique no toggle de periodo AM/PM.
  onPeriodSegmentClick(event) {
    if (!this.readonly && !this.isDisabled) {
      this.togglePeriod();
    }
  }
  refreshValue(value) {
    if (value && this.inputEl) {
      this.updateInputDisplay(this.normalizeTimeValueForDisplay(value));
    }
  }
  /**
   * Método que exibe `p-helper` ou executa a ação definida em `p-helper{eventOnClick}` ou em `p-additionalHelp`.
   */
  showAdditionalHelp() {
    this.displayAdditionalHelp = !this.displayAdditionalHelp;
    const helper = this.poHelperComponent();
    const isHelpEvt = this.isAdditionalHelpEventTriggered();
    if (!this.label && (helper || isHelpEvt)) {
      if (typeof helper !== "string" && typeof helper?.eventOnClick === "function") {
        helper.eventOnClick();
        return;
      }
      if (this.helperEl?.helperIsVisible()) {
        this.helperEl?.closeHelperPopover();
        return;
      }
      this.helperEl?.openHelperPopover();
      return;
    }
    return this.displayAdditionalHelp;
  }
  writeValue(value) {
    if (this.inputEl && value) {
      if (typeof value === "string" && this.isValidTimeString(value)) {
        this.timeValue = this.normalizeTimeValueForDisplay(value);
        this.clearValidationValue();
        this.updateInputDisplay(this.timeValue);
      } else {
        this.clearSegmentDisplays();
        this.clearValidationValue();
        this.timeValue = "";
      }
    } else if (this.inputEl) {
      if (this.hasValidationValue() && this.hasValue()) {
        this.timeValue = "";
        this.valueBeforeChange = this.timeValue;
        return;
      }
      this.clearSegmentDisplays();
      this.clearValidationValue();
      this.timeValue = "";
    }
    this.valueBeforeChange = this.timeValue;
    if (value != null) {
      this.controlChangeModelEmitter(this.formatOutput(this.timeValue) || this.timeValue);
    }
  }
  verifyMobile() {
    return isMobile();
  }
  handleCleanKeyboardTab(event) {
    if (event.key === "Tab" && !event.shiftKey) {
      this.iconTimepicker.buttonElement?.nativeElement.focus();
      event.preventDefault();
    }
    if (event.key === "Tab" && event.shiftKey) {
      this.focusLastSegment();
      event.preventDefault();
      event.stopPropagation();
    }
  }
  handleTimepickerButtonKeyboardTab(event) {
    if (this.shouldHandleTab(event)) {
      this.focusTimer(event);
    }
  }
  onTimerKeyDown(event) {
    if (!this.visible) {
      return;
    }
    if (event.key === "Escape") {
      event.preventDefault();
      event.stopPropagation();
      this.iconTimepicker.buttonElement?.nativeElement.focus();
      this.closeTimer(false);
    }
  }
  onTimerBoundaryTab(payload) {
    if (!this.visible) {
      return;
    }
    payload.event.preventDefault();
    payload.event.stopPropagation();
    this.iconTimepicker.buttonElement?.nativeElement.focus();
    this.closeTimer(false);
  }
  // Trata focusout no popup do timer para fechar quando o foco sai completamente.
  onTimerFocusOut(event) {
    if (!this.visible) {
      return;
    }
    const relatedTarget = event.relatedTarget;
    const dialogEl = this.dialogPicker?.nativeElement;
    if (relatedTarget && dialogEl && !dialogEl.contains(relatedTarget)) {
      if (!this.el.nativeElement.contains(relatedTarget)) {
        this.closeTimer(false);
      }
    } else if (!relatedTarget && dialogEl) {
      setTimeout(() => {
        if (this.visible && !this.el.nativeElement.contains(document.activeElement) && !dialogEl.contains(document.activeElement)) {
          this.closeTimer(false);
        }
      });
    }
  }
  setHelper(label) {
    return setHelperSettings(label, null, this.poHelperComponent(), this.size);
  }
  /**
   * Atualiza os valores exibidos nos inputs de segmento, convertendo para formato 12h com AM/PM quando necessário.
   * O timeValue interno sempre armazena em formato 24h (ISO), mas a exibição pode ser 12h.
   */
  updateInputDisplay(time) {
    if (!time) {
      this.clearSegmentDisplays();
      return;
    }
    const parts = time.split(":");
    if (this.is12HourFormat && this.isValidTimeString(time)) {
      let hours = parseInt(parts[0], 10);
      const period = hours >= 12 ? "PM" : "AM";
      if (hours === 0) {
        hours = 12;
      } else if (hours > 12) {
        hours = hours - 12;
      }
      this.hourDisplay = hours.toString().padStart(2, "0");
      this.minuteDisplay = parts[1] || "";
      this.secondDisplay = parts[2] || "";
      this.periodDisplay = period;
    } else {
      this.hourDisplay = parts[0] || "";
      this.minuteDisplay = parts[1] || "";
      this.secondDisplay = parts[2] || "";
      this.periodDisplay = "";
    }
    this.syncSegmentInputElements();
  }
  /** Atualiza a mensagem aria-live com o valor do horario para anuncio por leitores de tela. */
  updateAriaLiveMessage(time) {
    if (!time) {
      this.ariaLiveMessage = "";
      return;
    }
    const displayTime = this.is12HourFormat ? this.buildDisplayTime() : time;
    this.ariaLiveMessage = `${this.literals.selectedTime}: ${displayTime}`;
  }
  /** Constroi a string de horario no formato de exibicao atual (12h ou 24h). */
  buildDisplayTime() {
    const hour = this.hourDisplay || "--";
    const minute = this.minuteDisplay || "--";
    let display = `${hour}:${minute}`;
    if (this.showSeconds) {
      const second = this.secondDisplay || "--";
      display += `:${second}`;
    }
    if (this.is12HourFormat && this.periodDisplay) {
      display += ` ${this.periodDisplay}`;
    }
    return display;
  }
  /** Normaliza valores `HH:mm` para `HH:mm:00` quando segundos estão habilitados. */
  normalizeTimeValueForDisplay(value) {
    if (this.showSeconds && typeof value === "string" && value.length === 5) {
      return `${value}:00`;
    }
    return value;
  }
  /** Limpa todos os valores de exibicao dos segmentos e seus respectivos elementos input. */
  clearSegmentDisplays() {
    this.hourDisplay = "";
    this.minuteDisplay = "";
    this.secondDisplay = "";
    this.periodDisplay = this.getDefaultPeriodDisplay();
    this.syncSegmentInputElements();
  }
  getDefaultPeriodDisplay() {
    return this.is12HourFormat ? "AM" : "";
  }
  /** Sincroniza os valores dos elementos input nativos com as propriedades de exibicao. */
  syncSegmentInputElements() {
    requestAnimationFrame(() => {
      if (this.inputEl?.nativeElement) {
        this.inputEl.nativeElement.value = this.hourDisplay;
      }
      if (this.minuteInputEl?.nativeElement) {
        this.minuteInputEl.nativeElement.value = this.minuteDisplay;
      }
      if (this.secondInputEl?.nativeElement) {
        this.secondInputEl.nativeElement.value = this.secondDisplay;
      }
      const viewRef = this.cd;
      if (!viewRef.destroyed) {
        this.cd.markForCheck();
      }
    });
  }
  /**
   * Avança o foco para o próximo input de segmento ou elemento focável interno.
   * Retorna `true` se o foco foi movido, `false` se não há próximo elemento focável.
   */
  advanceToNextSegment(current) {
    if (current === "hour" && this.minuteInputEl?.nativeElement) {
      this.minuteInputEl.nativeElement.focus();
      this.minuteInputEl.nativeElement.select();
      return true;
    }
    if (current === "minute" && this.showSeconds && this.secondInputEl?.nativeElement) {
      this.secondInputEl.nativeElement.focus();
      this.secondInputEl.nativeElement.select();
      return true;
    }
    if (this.is12HourFormat && this.periodInputEl?.nativeElement && !this.isDisabled && !this.readonly && (current === "minute" && !this.showSeconds || current === "second")) {
      this.periodInputEl.nativeElement.focus();
      return true;
    }
    if (current === "minute" || current === "second") {
      return this.focusCleanOrButton();
    }
    return false;
  }
  /**
   * Tenta focar o botão de limpar ou o botão do timer.
   * Retorna `true` se o foco foi movido, `false` se nenhum elemento está focável.
   */
  focusCleanOrButton() {
    if (this.clean && this.hasValue() && !this.isDisabled && !this.readonly && this.iconClean?.nativeElement) {
      this.iconClean.nativeElement.focus();
      return true;
    }
    if (this.iconTimepicker?.buttonElement?.nativeElement && !this.isDisabled && !this.readonly) {
      this.iconTimepicker.buttonElement.nativeElement.focus();
      return true;
    }
    return false;
  }
  focusPreviousPeriodSegment() {
    if (this.showSeconds && this.secondInputEl?.nativeElement) {
      this.secondInputEl.nativeElement.focus();
      return;
    }
    if (this.minuteInputEl?.nativeElement) {
      this.minuteInputEl.nativeElement.focus();
    }
  }
  focusNextPeriodSegment(event) {
    if (this.visible) {
      this.focusTimer(event);
      return;
    }
    this.focusCleanOrButton();
  }
  /**
   * Retorna o foco para o input de segmento anterior.
   * Retorna `true` se o foco foi movido, `false` se não há segmento anterior focável.
   */
  advanceToPreviousSegment(current) {
    if (current === "minute" && this.inputEl?.nativeElement) {
      this.inputEl.nativeElement.focus();
      return true;
    }
    if (current === "second" && this.minuteInputEl?.nativeElement) {
      this.minuteInputEl.nativeElement.focus();
      return true;
    }
    return false;
  }
  /** Foca o ultimo input de segmento visivel. */
  focusLastSegment() {
    if (this.is12HourFormat && this.periodInputEl?.nativeElement) {
      this.periodInputEl.nativeElement.focus();
    } else if (this.showSeconds && this.secondInputEl?.nativeElement) {
      this.secondInputEl.nativeElement.focus();
    } else if (this.minuteInputEl?.nativeElement) {
      this.minuteInputEl.nativeElement.focus();
    } else {
      this.focus();
    }
  }
  /** Verifica se o segmento informado e o ultimo visivel. */
  isLastSegment(segment) {
    if (this.is12HourFormat) {
      return false;
    }
    if (this.showSeconds) {
      return segment === "second";
    }
    return segment === "minute";
  }
  /** Alterna entre AM e PM no display de periodo e atualiza o modelo. */
  togglePeriod() {
    const currentPeriod = this.periodDisplay || this.getDefaultPeriodDisplay();
    const targetPeriod = currentPeriod === "AM" ? "PM" : "AM";
    if (this.isPeriodBlocked(targetPeriod)) {
      return;
    }
    this.periodDisplay = targetPeriod;
    this.updateCombinedValue();
  }
  /**
   * Verifica se um período (AM/PM) está completamente fora do range min/max.
   * AM corresponde às horas 0-11 em 24h, PM às horas 12-23.
   */
  isPeriodBlocked(targetPeriod) {
    if (!this.is12HourFormat || !this.minTime && !this.maxTime) {
      return false;
    }
    const periodMinHour24 = targetPeriod === "AM" ? 0 : 12;
    const periodMaxHour24 = targetPeriod === "AM" ? 11 : 23;
    if (this.maxTime) {
      const maxHour = parseInt(this.maxTime.split(":")[0], 10) || 0;
      if (periodMinHour24 > maxHour) {
        return true;
      }
    }
    if (this.minTime) {
      const minHour = parseInt(this.minTime.split(":")[0], 10) || 0;
      if (periodMaxHour24 < minHour) {
        return true;
      }
    }
    return false;
  }
  /** Incrementa ou decrementa o valor de um segmento na direcao indicada (+1 ou -1), respeitando limites e intervalos. */
  incrementSegment(segment, direction) {
    switch (segment) {
      case "hour":
        this.incrementHourSegment(direction);
        break;
      case "minute":
        this.incrementIntervalSegment(direction, this.minuteInterval || 1, "minuteDisplay", this.minuteInputEl);
        break;
      case "second":
        this.incrementIntervalSegment(direction, this.secondInterval || 1, "secondDisplay", this.secondInputEl);
        break;
    }
    this.updateCombinedValue();
  }
  incrementHourSegment(direction) {
    const max3 = this.is12HourFormat ? 12 : 23;
    const min3 = this.is12HourFormat ? 1 : 0;
    let current = this.hourDisplay ? parseInt(this.hourDisplay, 10) : min3 - direction;
    current += direction;
    if (current > max3 || current < min3) {
      if (this.is12HourFormat) {
        const nextPeriod = (this.periodDisplay || this.getDefaultPeriodDisplay()) === "AM" ? "PM" : "AM";
        if (this.isPeriodBlocked(nextPeriod)) {
          return;
        }
        this.periodDisplay = nextPeriod;
      }
      current = current > max3 ? min3 : max3;
    }
    this.hourDisplay = current.toString().padStart(2, "0");
    if (this.inputEl?.nativeElement) {
      this.inputEl.nativeElement.value = this.hourDisplay;
    }
  }
  incrementIntervalSegment(direction, interval2, displayProp, inputRef) {
    let current = this[displayProp] ? parseInt(this[displayProp], 10) : -interval2 * direction;
    current += interval2 * direction;
    if (current >= 60) {
      current = 0;
    } else if (current < 0) {
      current = 60 - interval2;
    }
    this[displayProp] = current.toString().padStart(2, "0");
    if (inputRef?.nativeElement) {
      inputRef.nativeElement.value = this[displayProp];
    }
  }
  normalizeSingleDigitSegment(input2) {
    if (!input2 || !input2.classList.contains("po-timepicker-segment-input") || input2.value.length !== 1) {
      return false;
    }
    const normalizedValue = input2.value.padStart(2, "0");
    input2.value = normalizedValue;
    if (input2 === this.inputEl?.nativeElement) {
      this.hourDisplay = normalizedValue;
    } else if (input2 === this.minuteInputEl?.nativeElement) {
      this.minuteDisplay = normalizedValue;
    } else if (input2 === this.secondInputEl?.nativeElement) {
      this.secondDisplay = normalizedValue;
    }
    return true;
  }
  shouldCommitForInternalFocusTarget(target) {
    if (!target) {
      return false;
    }
    return this.iconTimepicker?.buttonElement?.nativeElement?.contains(target) || this.iconClean?.nativeElement?.contains(target) || !!target.closest(".po-field-helper-button");
  }
  /** Combina os valores dos segmentos em uma string de horario e atualiza o modelo. */
  updateCombinedValue() {
    if (!this.areSegmentsComplete()) {
      if (!this.hasValidationValue()) {
        this.clearValidationValue();
        if (this.isGeneratedErrorPattern(this.errorPattern)) {
          this.errorPattern = "";
        }
        this.timeValue = "";
        this.callOnChange("");
        this.validateModel(this.timeValue);
      }
      return;
    }
    const displayCombined = this.buildDisplayCombined();
    if (this.is12HourFormat && !this.isValidTimeString(displayCombined, 1, 12)) {
      this.applyInputValidationError(displayCombined, false, {
        minHour: 1,
        maxHour: 12
      });
      return;
    }
    const combined = this.is12HourFormat ? this.convertDisplayTo24h(displayCombined) : displayCombined;
    this.updateTimeFromInput(combined);
  }
  /** Monta a string parcial combinando os segmentos preenchidos e vazios. */
  buildPartialCombined() {
    const hour = this.hourDisplay || "";
    const minute = this.minuteDisplay || "";
    let combined = `${hour}:${minute}`;
    if (this.showSeconds) {
      const second = this.secondDisplay || "";
      combined += `:${second}`;
    }
    return combined;
  }
  areSegmentsComplete() {
    if (this.hourDisplay.length < 2 || this.minuteDisplay.length < 2) {
      return false;
    }
    return !(this.showSeconds && this.secondDisplay.length < 2);
  }
  buildDisplayCombined() {
    const displayHour = this.hourDisplay.padStart(2, "0");
    const minute = this.minuteDisplay.padStart(2, "0");
    let combined = `${displayHour}:${minute}`;
    if (this.showSeconds) {
      const second = this.secondDisplay.padStart(2, "0");
      combined += `:${second}`;
    }
    return combined;
  }
  convertDisplayTo24h(displayCombined) {
    let hourValue = parseInt(this.hourDisplay, 10);
    const currentPeriod = this.periodDisplay || this.getDefaultPeriodDisplay();
    this.periodDisplay = currentPeriod;
    if (currentPeriod === "AM" && hourValue === 12) {
      hourValue = 0;
    } else if (currentPeriod === "PM" && hourValue !== 12) {
      hourValue = hourValue + 12;
    }
    const hour = hourValue.toString().padStart(2, "0");
    const minuteAndSecond = displayCombined.substring(3);
    return `${hour}:${minuteAndSecond}`;
  }
  /** Completa valores de segmento incompletos e atualiza o modelo no blur. */
  validateAndUpdateModel() {
    if (this.hourDisplay && this.hourDisplay.length === 1) {
      this.hourDisplay = this.hourDisplay.padStart(2, "0");
      if (this.inputEl?.nativeElement) {
        this.inputEl.nativeElement.value = this.hourDisplay;
      }
    }
    if (this.minuteDisplay && this.minuteDisplay.length === 1) {
      this.minuteDisplay = this.minuteDisplay.padStart(2, "0");
      if (this.minuteInputEl?.nativeElement) {
        this.minuteInputEl.nativeElement.value = this.minuteDisplay;
      }
    }
    if (this.secondDisplay && this.secondDisplay.length === 1) {
      this.secondDisplay = this.secondDisplay.padStart(2, "0");
      if (this.secondInputEl?.nativeElement) {
        this.secondInputEl.nativeElement.value = this.secondDisplay;
      }
    }
    if (!this.areSegmentsComplete() && this.hasValue()) {
      const partial = this.buildPartialCombined();
      this.applyInputValidationError(partial, false);
      return;
    }
    this.updateCombinedValue();
  }
  updateTimeFromInput(rawValue) {
    if (!this.isValidTimeString(rawValue)) {
      this.applyInputValidationError(rawValue, false);
      return;
    }
    if (!this.isTimeInRange(rawValue)) {
      this.applyInputValidationError(rawValue, true);
      return;
    }
    this.clearValidationValue();
    if (this.isGeneratedErrorPattern(this.errorPattern)) {
      this.errorPattern = "";
    }
    this.timeValue = rawValue;
    this.updateAriaLiveMessage(rawValue);
    this.callOnChange(rawValue);
    this.validateModel(rawValue);
  }
  applyInputValidationError(rawValue, isOutOfRange, hourRange) {
    this.timeValue = "";
    this.setValidationValue(rawValue, hourRange?.minHour, hourRange?.maxHour);
    this.errorPattern = isOutOfRange ? this.getDefaultOutOfRangeTimeMessage() : this.getDefaultInvalidTimeMessage();
    this.callOnChange("");
    this.validateModel(rawValue);
  }
  controlChangeEmitter() {
    const currentValue = this.formatOutput(this.timeValue) || this.timeValue;
    if (currentValue !== this.valueBeforeChange) {
      this.valueBeforeChange = currentValue;
      clearTimeout(this.timeoutChange);
      this.timeoutChange = setTimeout(() => {
        this.onchange.emit(currentValue);
      }, 200);
    }
  }
  /**
   * Completa automaticamente os segundos com `:00` ao fechar o timer
   * quando `showSeconds=true` e o usuario preencheu apenas hora e minuto (HH:mm).
   *
   * Esse comportamento emite `callOnChange` e `controlChangeEmitter`, o que
   * significa que formularios reativos observando `valueChanges` receberao
   * uma emissao adicional no momento do fechamento do timer.
   */
  completeSecondsOnClose() {
    if (!this.showSeconds || !this.timeValue || this.timeValue.length !== 5 || !this.isValidTimeString(this.timeValue)) {
      return;
    }
    const committedTime = `${this.timeValue}:00`;
    this.timeValue = committedTime;
    this.updateInputDisplay(committedTime);
    this.callOnChange(committedTime);
    this.controlChangeEmitter();
  }
  initializeListeners() {
    this.clickListener = this.renderer.listen("document", "click", (event) => {
      this.wasClickedOnPicker(event);
    });
    this.eventResizeListener = this.renderer.listen("window", "resize", () => {
      this.closeTimer();
    });
    window.addEventListener("scroll", this.onScroll, true);
  }
  isAdditionalHelpEventTriggered() {
    return this.additionalHelpEventTrigger === "event";
  }
  onScroll = () => {
    this.controlPosition.adjustPosition(poTimerPositionDefault);
  };
  removeListeners() {
    if (this.clickListener) {
      this.clickListener();
    }
    if (this.eventResizeListener) {
      this.eventResizeListener();
    }
    window.removeEventListener("scroll", this.onScroll, true);
  }
  setDialogPickerStyleDisplay(value) {
    if (this.dialogPicker?.nativeElement) {
      this.dialogPicker.nativeElement.style.display = value;
    }
  }
  setTimerPosition() {
    this.setDialogPickerStyleDisplay("block");
    this.adjustTimerPosition();
  }
  adjustTimerPosition() {
    if (this?.dialogPicker?.nativeElement && this.visible) {
      const scrollHeight = this.dialogPicker.nativeElement.querySelector("po-timer")?.scrollHeight ?? this.dialogPicker.nativeElement.scrollHeight;
      const scrollWidth = this.dialogPicker.nativeElement.querySelector("po-timer")?.scrollWidth ?? this.dialogPicker.nativeElement.scrollWidth;
      this.dialogPicker.nativeElement.style.height = scrollHeight + "px";
      this.dialogPicker.nativeElement.style.width = scrollWidth + "px";
      this.controlPosition.setElements(this.dialogPicker.nativeElement, poTimerContentOffset, this.timepickerFieldEl || this.inputEl, ["top-left", "top-right", "bottom-left", "bottom-right"], false, true);
      this.controlPosition.adjustPosition(poTimerPositionDefault);
    }
  }
  shouldHandleTab(event) {
    return this.visible && !event.shiftKey;
  }
  focusTimer(event) {
    if (!this.timerComponent) {
      return;
    }
    event.preventDefault();
    this.timerComponent.focusFirstVisibleCell();
  }
  static ɵfac = function PoTimepickerComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoTimepickerComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoTimepickerComponent,
    selectors: [["po-timepicker"]],
    viewQuery: function PoTimepickerComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c67, 5, ElementRef)(_c78, 5)(_c49, 7, ElementRef)(_c79, 5, ElementRef)(_c80, 5, ElementRef)(_c81, 5, ElementRef)(_c69, 5, ElementRef)(_c17, 5, PoHelperComponent)(_c52, 5)(_c82, 5, ElementRef);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.dialogPicker = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.iconTimepicker = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.inputEl = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.minuteInputEl = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.secondInputEl = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.timepickerFieldEl = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.iconClean = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.helperEl = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.timerComponent = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.periodInputEl = _t.first);
      }
    },
    hostBindings: function PoTimepickerComponent_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("focusout", function PoTimepickerComponent_focusout_HostBindingHandler($event) {
          return ctx.onHostFocusOut($event);
        })("keydown", function PoTimepickerComponent_keydown_HostBindingHandler($event) {
          return ctx.onKeydown($event);
        });
      }
    },
    inputs: {
      label: [0, "p-label", "label"],
      help: [0, "p-help", "help"]
    },
    standalone: false,
    features: [ɵɵProvidersFeature([{
      provide: NG_VALUE_ACCESSOR,
      useExisting: forwardRef(() => _PoTimepickerComponent),
      multi: true
    }, {
      provide: NG_VALIDATORS,
      useExisting: forwardRef(() => _PoTimepickerComponent),
      multi: true
    }, PoControlPositionService]), ɵɵInheritDefinitionFeature, ɵɵNgOnChangesFeature],
    decls: 29,
    vars: 58,
    consts: [["outerContainer", ""], ["trigger", "cdkOverlayOrigin"], ["timepickerField", ""], ["inp", ""], ["minuteInput", ""], ["sharedTimerContent", ""], ["secondInput", ""], ["periodInput", ""], ["iconTimepicker", ""], ["iconClean", ""], ["helperEl", ""], ["dialogPicker", ""], ["timer", ""], [3, "p-disabled", "p-id", "p-label", "p-optional", "p-required", "p-show-required", "p-helper", "p-show-helper", "p-size", "p-text-wrap", "p-compact-label"], ["cdkOverlayOrigin", "", 1, "po-field-container-content"], [1, "po-field-container-input"], ["role", "group", 1, "po-timepicker-field", "po-input", 3, "click", "keydown"], [1, "po-timepicker-field-segment"], ["maxlength", "2", "type", "text", "inputmode", "numeric", "role", "spinbutton", 1, "po-timepicker-segment-input", 3, "input", "blur", "focus", "click", "keydown", "autocomplete", "disabled", "id", "readonly", "required", "placeholder"], [1, "po-timepicker-field-separator"], ["maxlength", "2", "type", "text", "inputmode", "numeric", "role", "spinbutton", "aria-valuemin", "0", "aria-valuemax", "59", 1, "po-timepicker-segment-input", 3, "input", "blur", "focus", "click", "keydown", "disabled", "readonly", "placeholder"], [1, "po-timepicker-field-segment", "po-timepicker-field-period"], [3, "p-size"], [1, "po-field-icon-container-right"], [1, "po-field-helper-button", 3, "p-size", "p-helper", "p-disabled", "p-append-in-body"], ["cdkConnectedOverlay", "", 3, "cdkConnectedOverlayOrigin", "cdkConnectedOverlayOpen", "cdkConnectedOverlayDisableClose"], ["aria-live", "polite", "aria-atomic", "true", 1, "po-sr-only"], [3, "p-append-in-body", "p-help", "p-disabled", "p-error-pattern", "p-error-limit", "p-size"], ["maxlength", "2", "type", "text", "role", "spinbutton", 1, "po-timepicker-segment-input", "po-timepicker-period-input", 3, "keydown", "click", "blur", "focus", "value", "disabled"], ["p-icon", "ICON_CLOCK", "p-kind", "tertiary", 1, "po-timepicker-button", 3, "keydown", "keydown.tab", "p-click", "p-aria-label", "p-disabled", "p-size"], ["tabindex", "0", "role", "button", "p-icon", "ICON_FILL_CLEAR_CONTENT", 1, "po-icon-input", 3, "p-change-event", "keydown", "keydown.enter", "keydown.space", "p-element-ref", "p-size"], [1, "po-button-vertical-divider"], [4, "ngTemplateOutlet"], ["tabindex", "-1", 3, "po-timepicker-popup-timer"], ["tabindex", "-1"], [1, "po-timepicker-timer-overlay"], [3, "p-change", "p-boundary-tab", "keydown", "focusout", "p-format", "p-locale", "p-show-seconds", "p-minute-interval", "p-second-interval", "p-min-time", "p-max-time", "p-size", "p-value"]],
    template: function PoTimepickerComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵdeclareLet(0);
        ɵɵelementStart(1, "po-field-container", 13)(2, "div", null, 0)(4, "div", 14, 1)(6, "div", 15)(7, "div", 16, 2);
        ɵɵlistener("click", function PoTimepickerComponent_Template_div_click_7_listener($event) {
          return ctx.onFieldClick($event);
        })("keydown", function PoTimepickerComponent_Template_div_keydown_7_listener($event) {
          return ctx.onKeyDown($event);
        });
        ɵɵelementStart(9, "div", 17)(10, "input", 18, 3);
        ɵɵlistener("input", function PoTimepickerComponent_Template_input_input_10_listener($event) {
          return ctx.onSegmentInput($event, "hour");
        })("blur", function PoTimepickerComponent_Template_input_blur_10_listener($event) {
          return ctx.onSegmentBlur($event);
        })("focus", function PoTimepickerComponent_Template_input_focus_10_listener() {
          return ctx.onSegmentFocus();
        })("click", function PoTimepickerComponent_Template_input_click_10_listener($event) {
          return ctx.eventOnClick($event);
        })("keydown", function PoTimepickerComponent_Template_input_keydown_10_listener($event) {
          return ctx.onSegmentKeydown($event, "hour");
        });
        ɵɵelementEnd()();
        ɵɵelementStart(12, "div", 19);
        ɵɵtext(13, " : ");
        ɵɵelementEnd();
        ɵɵelementStart(14, "div", 17)(15, "input", 20, 4);
        ɵɵlistener("input", function PoTimepickerComponent_Template_input_input_15_listener($event) {
          return ctx.onSegmentInput($event, "minute");
        })("blur", function PoTimepickerComponent_Template_input_blur_15_listener($event) {
          return ctx.onSegmentBlur($event);
        })("focus", function PoTimepickerComponent_Template_input_focus_15_listener() {
          return ctx.onSegmentFocus();
        })("click", function PoTimepickerComponent_Template_input_click_15_listener($event) {
          return ctx.eventOnClick($event);
        })("keydown", function PoTimepickerComponent_Template_input_keydown_15_listener($event) {
          return ctx.onSegmentKeydown($event, "minute");
        });
        ɵɵelementEnd()();
        ɵɵconditionalCreate(17, PoTimepickerComponent_Conditional_17_Template, 5, 9);
        ɵɵconditionalCreate(18, PoTimepickerComponent_Conditional_18_Template, 3, 5, "div", 21);
        ɵɵconditionalCreate(19, PoTimepickerComponent_Conditional_19_Template, 1, 1, "po-loading-icon", 22)(20, PoTimepickerComponent_Conditional_20_Template, 4, 6, "div", 23);
        ɵɵelementEnd()();
        ɵɵconditionalCreate(21, PoTimepickerComponent_Conditional_21_Template, 2, 4, "po-helper", 24);
        ɵɵelementEnd();
        ɵɵconditionalCreate(22, PoTimepickerComponent_Conditional_22_Template, 1, 3, null, 25)(23, PoTimepickerComponent_Conditional_23_Template, 1, 1, "ng-container");
        ɵɵtemplate(24, PoTimepickerComponent_ng_template_24_Template, 1, 1, "ng-template", null, 5, ɵɵtemplateRefExtractor);
        ɵɵelementStart(26, "span", 26);
        ɵɵtext(27);
        ɵɵelementEnd()();
        ɵɵconditionalCreate(28, PoTimepickerComponent_Conditional_28_Template, 1, 6, "po-field-container-bottom", 27);
        ɵɵelementEnd();
      }
      if (rf & 2) {
        const poHelper_r10 = ɵɵstoreLet(ctx.setHelper(ctx.label));
        ɵɵadvance();
        ɵɵclassProp("po-timepicker-container-disabled", ctx.isDisabled);
        ɵɵproperty("p-disabled", ctx.isDisabled)("p-id", ctx.id)("p-label", ctx.label)("p-optional", ctx.optional)("p-required", ctx.required)("p-show-required", ctx.showRequired)("p-helper", ctx.label ? poHelper_r10.helperSettings : void 0)("p-show-helper", ctx.label ? ctx.displayAdditionalHelp : void 0)("p-size", ctx.size)("p-text-wrap", ctx.labelTextWrap())("p-compact-label", ctx.compactLabel());
        ɵɵadvance(6);
        ɵɵclassProp("po-input-icon-right", ctx.clean && ctx.hasValue() && !ctx.isDisabled)("po-timepicker-field-disabled", ctx.isDisabled)("po-timepicker-field-readonly", ctx.readonly)("po-timepicker-field-focused", ctx.isSegmentFocused)("po-timepicker-field-placeholder", ctx.isPlaceholder);
        ɵɵattribute("data-inactive-component", ctx.isDisabled || ctx.readonly)("p-size", ctx.size)("data-append-in-body", ctx.appendBox)("aria-label", ctx.label || ctx.literals.field);
        ɵɵadvance(2);
        ɵɵattribute("data-value", ctx.hourDisplay || ctx.hourPlaceholder || "  ");
        ɵɵadvance();
        ɵɵproperty("autocomplete", ctx.autocomplete)("disabled", ctx.isDisabled)("id", ctx.id)("readonly", ctx.readonly)("required", ctx.required)("placeholder", ctx.isDisabled ? "" : ctx.hourPlaceholder);
        ɵɵattribute("aria-label", ctx.literals.hour)("aria-valuenow", ctx.hourDisplay ? +ctx.hourDisplay : null)("aria-valuemin", ctx.is12HourFormat ? 1 : 0)("aria-valuemax", ctx.is12HourFormat ? 12 : 23)("aria-valuetext", ctx.hourDisplay || null)("data-focused-element", !ctx.isDisabled)("name", ctx.name);
        ɵɵadvance(2);
        ɵɵclassProp("po-timepicker-field-separator-hidden", !ctx.showSeparator);
        ɵɵadvance(2);
        ɵɵattribute("data-value", ctx.minuteDisplay || ctx.minutePlaceholder || "  ");
        ɵɵadvance();
        ɵɵproperty("disabled", ctx.isDisabled)("readonly", ctx.readonly)("placeholder", ctx.isDisabled ? "" : ctx.minutePlaceholder);
        ɵɵattribute("aria-label", ctx.literals.minute)("aria-valuenow", ctx.minuteDisplay ? +ctx.minuteDisplay : null)("aria-valuetext", ctx.minuteDisplay || null);
        ɵɵadvance(2);
        ɵɵconditional(ctx.showSeconds ? 17 : -1);
        ɵɵadvance();
        ɵɵconditional(ctx.is12HourFormat && ctx.hasValue() ? 18 : -1);
        ɵɵadvance();
        ɵɵconditional(ctx.loading ? 19 : 20);
        ɵɵadvance(2);
        ɵɵconditional(!ctx.label && ctx.poHelperComponent() || !ctx.label && poHelper_r10.hideAdditionalHelp ? 21 : -1);
        ɵɵadvance();
        ɵɵconditional(ctx.appendBox ? 22 : 23);
        ɵɵadvance(5);
        ɵɵtextInterpolate(ctx.ariaLiveMessage);
        ɵɵadvance();
        ɵɵconditional(!ctx.readonly ? 28 : -1);
      }
    },
    dependencies: [NgTemplateOutlet, PoFieldContainerBottomComponent, PoFieldContainerComponent, CdkConnectedOverlay, CdkOverlayOrigin, PoCleanComponent, PoLoadingIconComponent, PoTimerComponent, PoButtonComponent, PoHelperComponent],
    encapsulation: 2,
    changeDetection: 0
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoTimepickerComponent, [{
    type: Component,
    args: [{
      selector: "po-timepicker",
      providers: [{
        provide: NG_VALUE_ACCESSOR,
        useExisting: forwardRef(() => PoTimepickerComponent),
        multi: true
      }, {
        provide: NG_VALIDATORS,
        useExisting: forwardRef(() => PoTimepickerComponent),
        multi: true
      }, PoControlPositionService],
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: `@let poHelper = setHelper(label);
<po-field-container
  [class.po-timepicker-container-disabled]="isDisabled"
  [p-disabled]="isDisabled"
  [p-id]="id"
  [p-label]="label"
  [p-optional]="optional"
  [p-required]="required"
  [p-show-required]="showRequired"
  [p-helper]="label ? poHelper.helperSettings : undefined"
  [p-show-helper]="label ? displayAdditionalHelp : undefined"
  [p-size]="size"
  [p-text-wrap]="labelTextWrap()"
  [p-compact-label]="compactLabel()"
>
  <div #outerContainer>
    <div cdkOverlayOrigin #trigger="cdkOverlayOrigin" class="po-field-container-content">
      <div class="po-field-container-input">
        <div
          #timepickerField
          class="po-timepicker-field po-input"
          [class.po-input-icon-right]="clean && hasValue() && !isDisabled"
          [class.po-timepicker-field-disabled]="isDisabled"
          [class.po-timepicker-field-readonly]="readonly"
          [class.po-timepicker-field-focused]="isSegmentFocused"
          [class.po-timepicker-field-placeholder]="isPlaceholder"
          [attr.data-inactive-component]="isDisabled || readonly"
          [attr.p-size]="size"
          [attr.data-append-in-body]="appendBox"
          [attr.aria-label]="label || literals.field"
          role="group"
          (click)="onFieldClick($event)"
          (keydown)="onKeyDown($event)"
        >
          <div class="po-timepicker-field-segment" [attr.data-value]="hourDisplay || hourPlaceholder || '  '">
            <input
              #inp
              class="po-timepicker-segment-input"
              maxlength="2"
              type="text"
              inputmode="numeric"
              role="spinbutton"
              [attr.aria-label]="literals.hour"
              [attr.aria-valuenow]="hourDisplay ? +hourDisplay : null"
              [attr.aria-valuemin]="is12HourFormat ? 1 : 0"
              [attr.aria-valuemax]="is12HourFormat ? 12 : 23"
              [attr.aria-valuetext]="hourDisplay || null"
              [attr.data-focused-element]="!isDisabled"
              [attr.name]="name"
              [autocomplete]="autocomplete"
              [disabled]="isDisabled"
              [id]="id"
              [readonly]="readonly"
              [required]="required"
              [placeholder]="isDisabled ? '' : hourPlaceholder"
              (input)="onSegmentInput($event, 'hour')"
              (blur)="onSegmentBlur($event)"
              (focus)="onSegmentFocus()"
              (click)="eventOnClick($event)"
              (keydown)="onSegmentKeydown($event, 'hour')"
            />
          </div>

          <div class="po-timepicker-field-separator" [class.po-timepicker-field-separator-hidden]="!showSeparator">
            :
          </div>

          <div class="po-timepicker-field-segment" [attr.data-value]="minuteDisplay || minutePlaceholder || '  '">
            <input
              #minuteInput
              class="po-timepicker-segment-input"
              maxlength="2"
              type="text"
              inputmode="numeric"
              role="spinbutton"
              [attr.aria-label]="literals.minute"
              [attr.aria-valuenow]="minuteDisplay ? +minuteDisplay : null"
              aria-valuemin="0"
              aria-valuemax="59"
              [attr.aria-valuetext]="minuteDisplay || null"
              [disabled]="isDisabled"
              [readonly]="readonly"
              [placeholder]="isDisabled ? '' : minutePlaceholder"
              (input)="onSegmentInput($event, 'minute')"
              (blur)="onSegmentBlur($event)"
              (focus)="onSegmentFocus()"
              (click)="eventOnClick($event)"
              (keydown)="onSegmentKeydown($event, 'minute')"
            />
          </div>

          @if (showSeconds) {
            <div class="po-timepicker-field-separator" [class.po-timepicker-field-separator-hidden]="!showSeparator">
              :
            </div>

            <div class="po-timepicker-field-segment" [attr.data-value]="secondDisplay || secondPlaceholder || '  '">
              <input
                #secondInput
                class="po-timepicker-segment-input"
                maxlength="2"
                type="text"
                inputmode="numeric"
                role="spinbutton"
                [attr.aria-label]="literals.second"
                [attr.aria-valuenow]="secondDisplay ? +secondDisplay : null"
                aria-valuemin="0"
                aria-valuemax="59"
                [attr.aria-valuetext]="secondDisplay || null"
                [disabled]="isDisabled"
                [readonly]="readonly"
                [placeholder]="isDisabled ? '' : secondPlaceholder"
                (input)="onSegmentInput($event, 'second')"
                (blur)="onSegmentBlur($event)"
                (focus)="onSegmentFocus()"
                (click)="eventOnClick($event)"
                (keydown)="onSegmentKeydown($event, 'second')"
              />
            </div>
          }

          @if (is12HourFormat && hasValue()) {
            <div
              class="po-timepicker-field-segment po-timepicker-field-period"
              [attr.data-value]="periodDisplay || 'AM'"
            >
              <input
                #periodInput
                class="po-timepicker-segment-input po-timepicker-period-input"
                maxlength="2"
                type="text"
                [value]="periodDisplay || 'AM'"
                [disabled]="isDisabled"
                [attr.tabindex]="isDisabled || readonly ? -1 : 0"
                role="spinbutton"
                [attr.aria-label]="periodDisplay || 'AM'"
                (keydown)="onPeriodSegmentKeydown($event)"
                (click)="onPeriodSegmentClick($event)"
                (blur)="onSegmentBlur($event)"
                (focus)="onSegmentFocus()"
              />
            </div>
          }

          @if (loading) {
            <po-loading-icon [p-size]="mapSizeToIcon(size)"></po-loading-icon>
          } @else {
            <div class="po-field-icon-container-right">
              @if (clean && hasValue() && !isDisabled && !readonly) {
                <po-clean
                  #iconClean
                  tabindex="0"
                  role="button"
                  [attr.aria-label]="literals.clean"
                  class="po-icon-input"
                  p-icon="ICON_FILL_CLEAR_CONTENT"
                  [p-element-ref]="cleanElementRef"
                  [p-size]="size"
                  (p-change-event)="clear()"
                  (keydown)="handleCleanKeyboardTab($event)"
                  (keydown.enter)="clearAndFocus(); $event.preventDefault()"
                  (keydown.space)="clearAndFocus(); $event.preventDefault()"
                >
                </po-clean>
                <div class="po-button-vertical-divider"></div>
              }
              <po-button
                #iconTimepicker
                class="po-timepicker-button"
                [class.po-button-tertiary-danger]="getErrorPattern()"
                p-icon="ICON_CLOCK"
                p-kind="tertiary"
                [p-aria-label]="literals.open"
                [p-disabled]="isDisabled || readonly"
                [p-size]="size"
                (keydown)="onKeyPress($event)"
                (keydown.tab)="handleTimepickerButtonKeyboardTab($event)"
                (p-click)="togglePicker(false)"
              >
              </po-button>
            </div>
          }
        </div>
      </div>
      @if ((!this.label && poHelperComponent()) || (!this.label && poHelper.hideAdditionalHelp)) {
        <po-helper
          #helperEl
          class="po-field-helper-button"
          [p-size]="size"
          [p-helper]="poHelper.helperSettings"
          [p-disabled]="isDisabled"
          [p-append-in-body]="appendBox"
        >
        </po-helper>
      }
    </div>

    @if (appendBox) {
      <ng-template
        cdkConnectedOverlay
        [cdkConnectedOverlayOrigin]="trigger"
        [cdkConnectedOverlayOpen]="true"
        [cdkConnectedOverlayDisableClose]="true"
      >
        <ng-container *ngTemplateOutlet="sharedTimerContent"></ng-container>
      </ng-template>
    } @else {
      <ng-container *ngTemplateOutlet="sharedTimerContent"></ng-container>
    }

    <ng-template #sharedTimerContent>
      @if (visible) {
        <div #dialogPicker [class.po-timepicker-popup-timer]="!verifyMobile()" tabindex="-1">
          @if (verifyMobile()) {
            <div class="po-timepicker-timer-overlay"></div>
          }
          <po-timer
            #timer
            [class.po-timepicker-timer-mobile]="verifyMobile()"
            [p-format]="format"
            [p-locale]="locale"
            [p-show-seconds]="showSeconds"
            [p-minute-interval]="minuteInterval"
            [p-second-interval]="secondInterval"
            [p-min-time]="minTime"
            [p-max-time]="maxTime"
            [p-size]="size"
            [p-value]="timeValue"
            (p-change)="timerSelected($event)"
            (p-boundary-tab)="onTimerBoundaryTab($event)"
            (keydown)="onTimerKeyDown($event)"
            (focusout)="onTimerFocusOut($event)"
          ></po-timer>
        </div>
      }
    </ng-template>

    <span class="po-sr-only" aria-live="polite" aria-atomic="true">{{ ariaLiveMessage }}</span>
  </div>

  @if (!readonly) {
    <po-field-container-bottom
      [p-append-in-body]="appendBox"
      [p-help]="help"
      [p-disabled]="isDisabled"
      [p-error-pattern]="getErrorPattern()"
      [p-error-limit]="errorLimit"
      [p-size]="size"
    ></po-field-container-bottom>
  }
</po-field-container>
`
    }]
  }], () => [], {
    dialogPicker: [{
      type: ViewChild,
      args: ["dialogPicker", {
        read: ElementRef,
        static: false
      }]
    }],
    iconTimepicker: [{
      type: ViewChild,
      args: ["iconTimepicker"]
    }],
    inputEl: [{
      type: ViewChild,
      args: ["inp", {
        read: ElementRef,
        static: true
      }]
    }],
    minuteInputEl: [{
      type: ViewChild,
      args: ["minuteInput", {
        read: ElementRef
      }]
    }],
    secondInputEl: [{
      type: ViewChild,
      args: ["secondInput", {
        read: ElementRef
      }]
    }],
    timepickerFieldEl: [{
      type: ViewChild,
      args: ["timepickerField", {
        read: ElementRef
      }]
    }],
    iconClean: [{
      type: ViewChild,
      args: ["iconClean", {
        read: ElementRef
      }]
    }],
    helperEl: [{
      type: ViewChild,
      args: ["helperEl", {
        read: PoHelperComponent,
        static: false
      }]
    }],
    timerComponent: [{
      type: ViewChild,
      args: ["timer", {
        static: false
      }]
    }],
    periodInputEl: [{
      type: ViewChild,
      args: ["periodInput", {
        read: ElementRef
      }]
    }],
    label: [{
      type: Input,
      args: ["p-label"]
    }],
    help: [{
      type: Input,
      args: ["p-help"]
    }],
    onHostFocusOut: [{
      type: HostListener,
      args: ["focusout", ["$event"]]
    }],
    onKeydown: [{
      type: HostListener,
      args: ["keydown", ["$event"]]
    }]
  });
})();
var PoTimepickerModule = class _PoTimepickerModule {
  static ɵfac = function PoTimepickerModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoTimepickerModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoTimepickerModule,
    declarations: [PoTimepickerComponent],
    imports: [CommonModule, FormsModule, PoFieldContainerModule, OverlayModule, PoCleanModule, PoLoadingModule, PoTimerModule, PoButtonModule, PoHelperModule],
    exports: [PoTimepickerComponent]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [CommonModule, FormsModule, PoFieldContainerModule, OverlayModule, PoCleanModule, PoLoadingModule, PoTimerModule, PoButtonModule, PoHelperModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoTimepickerModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, FormsModule, PoFieldContainerModule, OverlayModule, PoCleanModule, PoLoadingModule, PoTimerModule, PoButtonModule, PoHelperModule],
      exports: [PoTimepickerComponent],
      declarations: [PoTimepickerComponent]
    }]
  }], null, null);
})();
var PoProgressBarComponent = class _PoProgressBarComponent {
  ariaLabel;
  indeterminate;
  value;
  get valueScale() {
    return `${this.value / 100}`;
  }
  static ɵfac = function PoProgressBarComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoProgressBarComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoProgressBarComponent,
    selectors: [["po-progress-bar"]],
    inputs: {
      ariaLabel: [0, "p-aria-label", "ariaLabel"],
      indeterminate: [0, "p-indeterminate", "indeterminate"],
      value: [0, "p-value", "value"]
    },
    standalone: false,
    decls: 2,
    vars: 2,
    consts: [["role", "progressbar", "aria-valuemin", "0", "aria-valuemax", "100", "aria-live", "polite", 1, "po-progress-bar-default"], ["role", "progressbar", "aria-valuemin", "0", "aria-valuemax", "100", "aria-live", "polite", 1, "po-progress-bar-indeterminate-track"], [1, "po-progress-bar-element", "po-progress-bar-primary"], [1, "po-progress-bar-element", "po-progress-bar-secondary"], [1, "po-progress-bar-indeterminate-track-bar"]],
    template: function PoProgressBarComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵconditionalCreate(0, PoProgressBarComponent_Conditional_0_Template, 3, 4, "div", 0);
        ɵɵconditionalCreate(1, PoProgressBarComponent_Conditional_1_Template, 2, 1, "div", 1);
      }
      if (rf & 2) {
        ɵɵconditional(!ctx.indeterminate ? 0 : -1);
        ɵɵadvance();
        ɵɵconditional(ctx.indeterminate ? 1 : -1);
      }
    },
    encapsulation: 2,
    changeDetection: 0
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoProgressBarComponent, [{
    type: Component,
    args: [{
      selector: "po-progress-bar",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: `@if (!indeterminate) {
  <div
    role="progressbar"
    [attr.aria-label]="ariaLabel"
    [attr.aria-valuenow]="value"
    aria-valuemin="0"
    aria-valuemax="100"
    aria-live="polite"
    class="po-progress-bar-default"
  >
    <div class="po-progress-bar-element po-progress-bar-primary"></div>
    <div class="po-progress-bar-element po-progress-bar-secondary" [style.left]="'-' + (100 - value) + '%'"></div>
  </div>
}

@if (indeterminate) {
  <div
    class="po-progress-bar-indeterminate-track"
    role="progressbar"
    [attr.aria-label]="ariaLabel"
    aria-valuemin="0"
    aria-valuemax="100"
    aria-live="polite"
  >
    <div class="po-progress-bar-indeterminate-track-bar"></div>
  </div>
}
`
    }]
  }], null, {
    ariaLabel: [{
      type: Input,
      args: ["p-aria-label"]
    }],
    indeterminate: [{
      type: Input,
      args: ["p-indeterminate"]
    }],
    value: [{
      type: Input,
      args: ["p-value"]
    }]
  });
})();
var CIRCLE_STROKE_MEDIUM = 4;
var CIRCLE_STROKE_LARGE = 8;
var CIRCLE_MIN_RADIUS_WITH_CENTER_CONTENT = 24;
var CIRCLE_DEFAULT_RADIUS$1 = 45;
var PoProgressCircleComponent = class _PoProgressCircleComponent {
  indeterminate = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "indeterminate"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-indeterminate",
    transform: convertToBoolean
  }));
  showPercentage = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "showPercentage"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-show-percentage",
    transform: convertToBoolean
  }));
  size = input("large", __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "size"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-size",
    transform: (value) => value === "medium" ? "medium" : "large"
  }));
  status = input("default", __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "status"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-status",
    transform: (value) => ["default", "success", "error"].includes(value) ? value : "default"
  }));
  ariaLabelInput = input("", __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "ariaLabelInput"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-aria-label"
  }));
  ariaLabel = computed(() => {
    const status = this.status() !== "default" ? this.status() : "";
    const custom = this.ariaLabelInput();
    if (custom) {
      return `${status} ${custom}`;
    }
    return status;
  }, ...ngDevMode ? [{
    debugName: "ariaLabel"
  }] : (
    /* istanbul ignore next */
    []
  ));
  value = input(0, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "value"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-value"
  }));
  // 0 = não informado pelo usuário (usa lógica automática)
  radius = input(0, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "radius"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-radius"
  }));
  // Dimensão do container pai (fornecida pelo ResizeObserver do po-progress.component)
  parentSize = input(0, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "parentSize"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-parent-size"
  }));
  strokeWidth = computed(() => this.size() === "medium" ? CIRCLE_STROKE_MEDIUM : CIRCLE_STROKE_LARGE, ...ngDevMode ? [{
    debugName: "strokeWidth"
  }] : (
    /* istanbul ignore next */
    []
  ));
  /**
   * Raio base calculado de acordo com as regras de prioridade:
   *
   * 1. Se `radius > 0` (usuário informou radius explicitamente):
   *    - Se o pai tem dimensão: limita ao máximo que cabe no pai (evita estouro)
   *    - Se o pai não tem dimensão: usa o radius informado diretamente
   *
   * 2. Se `radius === 0` (usuário NÃO informou radius):
   *    - Se o pai tem dimensão: ocupa o máximo disponível no pai
   *    - Se o pai não tem dimensão: usa o default de 45
   */
  baseRadius = computed(() => {
    const parentDimension = this.parentSize();
    const userRadius = this.radius();
    if (userRadius > 0) {
      if (parentDimension > 0) {
        const maxFitRadius = parentDimension / 2 - this.strokeWidth();
        return Math.min(userRadius, Math.max(maxFitRadius, 0));
      }
      return userRadius;
    }
    if (parentDimension > 0) {
      return parentDimension / 2 - this.strokeWidth();
    }
    return CIRCLE_DEFAULT_RADIUS$1;
  }, ...ngDevMode ? [{
    debugName: "baseRadius"
  }] : (
    /* istanbul ignore next */
    []
  ));
  hasCenterContent = computed(() => !this.indeterminate() && (this.showPercentage() || this.status() === "error"), ...ngDevMode ? [{
    debugName: "hasCenterContent"
  }] : (
    /* istanbul ignore next */
    []
  ));
  effectiveRadius = computed(() => this.hasCenterContent() ? Math.max(this.baseRadius(), CIRCLE_MIN_RADIUS_WITH_CENTER_CONTENT) : this.baseRadius(), ...ngDevMode ? [{
    debugName: "effectiveRadius"
  }] : (
    /* istanbul ignore next */
    []
  ));
  viewBoxSize = computed(() => (this.effectiveRadius() + this.strokeWidth()) * 2, ...ngDevMode ? [{
    debugName: "viewBoxSize"
  }] : (
    /* istanbul ignore next */
    []
  ));
  center = computed(() => this.effectiveRadius() + this.strokeWidth(), ...ngDevMode ? [{
    debugName: "center"
  }] : (
    /* istanbul ignore next */
    []
  ));
  circumference = computed(() => 2 * Math.PI * this.effectiveRadius(), ...ngDevMode ? [{
    debugName: "circumference"
  }] : (
    /* istanbul ignore next */
    []
  ));
  dashOffset = computed(() => {
    const clampedValue = Math.max(0, Math.min(100, this.value() || 0));
    return this.circumference() - clampedValue / 100 * this.circumference();
  }, ...ngDevMode ? [{
    debugName: "dashOffset"
  }] : (
    /* istanbul ignore next */
    []
  ));
  naturalSize = computed(() => {
    const vb = this.viewBoxSize();
    const ps = this.parentSize();
    return ps > 0 && vb > ps ? `${ps}px` : `${vb}px`;
  }, ...ngDevMode ? [{
    debugName: "naturalSize"
  }] : (
    /* istanbul ignore next */
    []
  ));
  static ɵfac = function PoProgressCircleComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoProgressCircleComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoProgressCircleComponent,
    selectors: [["po-progress-circle"]],
    inputs: {
      indeterminate: [1, "p-indeterminate", "indeterminate"],
      showPercentage: [1, "p-show-percentage", "showPercentage"],
      size: [1, "p-size", "size"],
      status: [1, "p-status", "status"],
      ariaLabelInput: [1, "p-aria-label", "ariaLabelInput"],
      value: [1, "p-value", "value"],
      radius: [1, "p-radius", "radius"],
      parentSize: [1, "p-parent-size", "parentSize"]
    },
    decls: 2,
    vars: 2,
    consts: [["role", "progressbar", "aria-valuemin", "0", "aria-valuemax", "100", "aria-live", "polite", 1, "po-progress-circle-svg-wrapper", 3, "max-height", "max-width"], ["role", "progressbar", "aria-valuemin", "0", "aria-valuemax", "100", "aria-live", "polite", 1, "po-progress-circle-indeterminate"], ["role", "progressbar", "aria-valuemin", "0", "aria-valuemax", "100", "aria-live", "polite", 1, "po-progress-circle-svg-wrapper"], [1, "po-progress-circle-svg"], ["fill", "none", 1, "po-progress-circle-tray"], ["fill", "none", 1, "po-progress-circle-indicator"], [1, "po-progress-circle-info", "po-progress-circle-info-percentage"], [3, "class"], [1, "an", "an-warning-circle"], [1, "po-progress-circle-svg-wrapper"]],
    template: function PoProgressCircleComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵconditionalCreate(0, PoProgressCircleComponent_Conditional_0_Template, 6, 19, "div", 0);
        ɵɵconditionalCreate(1, PoProgressCircleComponent_Conditional_1_Template, 5, 15, "div", 1);
      }
      if (rf & 2) {
        ɵɵconditional(!ctx.indeterminate() ? 0 : -1);
        ɵɵadvance();
        ɵɵconditional(ctx.indeterminate() ? 1 : -1);
      }
    },
    encapsulation: 2,
    changeDetection: 0
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoProgressCircleComponent, [{
    type: Component,
    args: [{
      selector: "po-progress-circle",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: true,
      template: `@if (!indeterminate()) {
  <div
    class="po-progress-circle-svg-wrapper"
    role="progressbar"
    [attr.aria-valuenow]="value()"
    aria-valuemin="0"
    aria-valuemax="100"
    aria-live="polite"
    [attr.aria-label]="ariaLabel()"
    [style.max-height]="naturalSize()"
    [style.max-width]="naturalSize()"
  >
    <svg
      class="po-progress-circle-svg"
      [attr.viewBox]="'0 0 ' + viewBoxSize() + ' ' + viewBoxSize()"
      [attr.width]="viewBoxSize()"
      [attr.height]="viewBoxSize()"
    >
      <circle
        class="po-progress-circle-tray"
        [attr.cx]="center()"
        [attr.cy]="center()"
        [attr.r]="effectiveRadius()"
        fill="none"
      />

      <circle
        class="po-progress-circle-indicator"
        [attr.cx]="center()"
        [attr.cy]="center()"
        [attr.r]="effectiveRadius()"
        fill="none"
        [attr.stroke-dasharray]="circumference()"
        [attr.stroke-dashoffset]="dashOffset()"
      />
    </svg>

    @if (showPercentage() && status() !== 'error') {
      <span class="po-progress-circle-info po-progress-circle-info-percentage">{{ value() }}%</span>
    }

    @if (status() === 'error') {
      <span class="po-progress-circle-info po-progress-circle-info-error po-progress-circle-info-error-{{ size() }}">
        <span class="an an-warning-circle"></span>
      </span>
    }
  </div>
}

@if (indeterminate()) {
  <div
    class="po-progress-circle-indeterminate"
    role="progressbar"
    [attr.aria-label]="ariaLabel()"
    aria-valuemin="0"
    aria-valuemax="100"
    aria-live="polite"
  >
    <div class="po-progress-circle-svg-wrapper" [style.max-width]="naturalSize()" [style.max-height]="naturalSize()">
      <svg
        class="po-progress-circle-svg"
        [attr.viewBox]="'0 0 ' + viewBoxSize() + ' ' + viewBoxSize()"
        [attr.width]="viewBoxSize()"
        [attr.height]="viewBoxSize()"
      >
        <circle
          class="po-progress-circle-tray"
          [attr.cx]="center()"
          [attr.cy]="center()"
          [attr.r]="effectiveRadius()"
          fill="none"
        />

        <circle
          class="po-progress-circle-indicator"
          [attr.cx]="center()"
          [attr.cy]="center()"
          [attr.r]="effectiveRadius()"
          [attr.pathLength]="100"
          fill="none"
        />
      </svg>
    </div>
  </div>
}
`
    }]
  }], null, {
    indeterminate: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-indeterminate",
        required: false
      }]
    }],
    showPercentage: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-show-percentage",
        required: false
      }]
    }],
    size: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-size",
        required: false
      }]
    }],
    status: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-status",
        required: false
      }]
    }],
    ariaLabelInput: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-aria-label",
        required: false
      }]
    }],
    value: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-value",
        required: false
      }]
    }],
    radius: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-radius",
        required: false
      }]
    }],
    parentSize: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-parent-size",
        required: false
      }]
    }]
  });
})();
var PoProgressStatus;
(function(PoProgressStatus2) {
  PoProgressStatus2["Default"] = "default";
  PoProgressStatus2["Error"] = "error";
  PoProgressStatus2["Success"] = "success";
})(PoProgressStatus || (PoProgressStatus = {}));
var poProgressLiterals = {
  en: {
    cancel: "Cancel",
    retry: "Retry"
  },
  es: {
    cancel: "Cancelar",
    retry: "Procesar de nuevo"
  },
  pt: {
    cancel: "Cancelar",
    retry: "Tentar Novamente"
  },
  ru: {
    cancel: "Отмена",
    retry: "Повторить попытку"
  }
};
var PoProgressShape;
(function(PoProgressShape2) {
  PoProgressShape2["bar"] = "bar";
  PoProgressShape2["circle"] = "circle";
})(PoProgressShape || (PoProgressShape = {}));
var PoProgressSize;
(function(PoProgressSize2) {
  PoProgressSize2["medium"] = "medium";
  PoProgressSize2["large"] = "large";
})(PoProgressSize || (PoProgressSize = {}));
var poProgressMaxValue = 100;
var poProgressMinValue = 0;
var CIRCLE_DEFAULT_RADIUS = 0;
var CIRCLE_MIN_RADIUS = 24;
var PoProgressBaseComponent = class _PoProgressBaseComponent {
  /**
   * @optional
   *
   * @description
   *
   * Define um nome acessível para o elemento com `role="progressbar"`.
   *
   * Quando não informado, o componente utiliza o valor de `p-text` como alternativa, se disponível.
   */
  ariaLabel;
  /**
   * @optional
   *
   * @description
   *
   * Desabilita botão de cancelamento na parte inferior da barra de progresso.
   *
   * > Se nenhuma função for passada para o evento `(p-cancel)` ou a barra de progresso estiver com o status `PoProgressStatus.Success`,
   * o ícone de cancelamento não será exibido.
   *
   * > Não compatível com `p-shape="circle"`.
   *
   * @default `false`
   */
  disabledCancel = false;
  /**
   * @optional
   *
   * @description
   *
   * Informação adicional que aparecerá abaixo da barra de progresso ao lado direito.
   *
   * > Não compatível com `p-shape="circle"`.
   */
  info;
  /**
   * @optional
   *
   * @description
   *
   * Ícone que aparecerá ao lado do texto da propriedade `p-info`.
   *
   * Exemplo: `an an-check`.
   *
   * > Não compatível com `p-shape="circle"`.
   */
  infoIcon;
  /**
   * @optional
   *
   * @description
   *
   * Status da barra de progresso que indicará visualmente ao usuário
   * o andamento, por exemplo, se a mesma foi concluída com sucesso.
   *
   * @default `PoProgressStatus.Default`
   */
  status = PoProgressStatus.Default;
  /**
   * @optional
   *
   * @description
   *
   * Texto principal que aparecerá abaixo da barra de progresso no lado esquerdo.
   *
   * > Não compatível com `p-shape="circle"`.
   */
  text;
  /**
   * @optional
   *
   * @description
   *
   * Permite definir uma ação personalizada no componente `po-progress`, exibindo um botão no canto inferior direito
   * da barra de progresso. A ação deve implementar a interface **PoProgressAction**, possibilitando configurar:
   *
   * - **`label`**: Texto exibido no botão (opcional).
   * - **`icon`**: Ícone exibido no botão (opcional).
   * - **`type`**: Tipo do botão (`default` ou `danger`) para indicar a intenção da ação (opcional).
   * - **`disabled`**: Indica se o botão deve estar desabilitado (opcional).
   * - **`visible`**: Determina se o botão será exibido. Pode ser um valor booleano ou uma função que retorna um booleano (opcional).
   *
   * > Não compatível com `p-shape="circle"`.
   *
   * @example
   * **Exemplo de uso:**
   * ```html
   * <po-progress
   *  [p-value]="50"
   *  [p-custom-action]="customAction"
   *  (p-custom-action-click)="onCustomActionClick()"
   * ></po-progress>
   * ```
   *
   * ```typescript
   * customAction: PoProgressAction = {
   *   label: 'Baixar',
   *   icon: 'an an-download',
   *   type: 'default',
   *   visible: () => true
   * };
   *
   * onCustomActionClick() {
   *   console.log('Custom action triggered!');
   * }
   * ```
   *
   * **Cenários comuns:**
   * 1. **Download de Arquivos**: Exibir um botão para realizar o download de um arquivo associado à barra de progresso.
   * 2. **Cancelamento Personalizado**: Adicionar uma ação para interromper ou reverter uma operação em andamento.
   */
  customAction;
  /**
   * @optional
   *
   * @description
   *
   * Evento emitido quando o botão definido em `p-custom-action` é clicado. Este evento retorna informações
   * relacionadas à barra de progresso ou ao arquivo/processo associado, permitindo executar ações específicas.
   *
   * > Não compatível com `p-shape="circle"`.
   *
   * @example
   * **Exemplo de uso:**
   *
   * ```html
   * <po-progress
   *  [p-value]="50"
   *  [p-custom-action]="customAction"
   *  (p-custom-action-click)="onCustomActionClick()"
   * ></po-progress>
   * ```
   *
   * ```typescript
   * customAction: PoProgressAction = {
   *   label: 'Cancelar',
   *   icon: 'an an-x',
   *   type: 'danger',
   *   visible: true
   * };
   *
   * onCustomActionClick() {
   *   console.log('Custom action triggered!');
   * }
   * ```
   *
   * **Cenários comuns:**
   * 1. **Botão de Download**: Disparar o download do arquivo associado à barra de progresso.
   * 2. **Ação Condicional**: Realizar uma validação ou chamada de API antes de prosseguir com a ação.
   */
  customActionClick = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Evento que será disparado ao clicar no ícone de cancelamento ("x") na parte inferior da barra de progresso.
   *
   * Ao ser disparado, a função receberá como parâmetro o status atual da barra de progresso.
   *
   * > Se nenhuma função for passada para o evento ou a barra de progresso estiver com o status `PoProgressStatus.Success`,
   * o ícone de cancelamento não será exibido.
   *
   * > Não compatível com `p-shape="circle"`.
   */
  cancel = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Evento que será disparado ao clicar no ícone de tentar novamente na parte inferior da barra de progresso.
   *
   * > o ícone será exibido apenas se informar uma função neste evento e o status da barra de progresso for
   * `PoProgressStatus.Error`.
   *
   * > Não compatível com `p-shape="circle"`.
   */
  retry = new EventEmitter();
  _indeterminate;
  _value = 0;
  _size = "large";
  _sizeActions = void 0;
  _initialSizeActions = void 0;
  /**
   * @optional
   *
   * @description
   *
   * Define o formato visual do componente de progresso.
   *
   * Valores válidos:
   *  - `bar`: exibe o progresso em formato de barra.
   *  - `circle`: exibe o progresso em formato circular.
   *
   * @example
   * ```html
   * <po-progress p-shape="circle" [p-value]="50"></po-progress>
   * ```
   *
   * @default `bar`
   */
  shape = input(PoProgressShape.bar, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "shape"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-shape",
    transform: (value) => PoProgressShape[value] ? PoProgressShape[value] : PoProgressShape.bar
  }));
  /**
   * @optional
   *
   * @description
   *
   * Define o raio do círculo SVG em pixels. Permite ao usuário customizar o tamanho
   * do indicador circular ao utilizar `p-shape="circle"`.
   *
   * > O valor mínimo aceito é **24**.
   *
   * > Quando não informado, o componente calcula o raio automaticamente a partir do container pai.
   * Caso o container pai não possua dimensões definidas, o valor padrão de **45** será utilizado.
   *
   * > Não compatível com `p-shape="bar"`.
   *
   * @default `45` (automático)
   */
  radius = input(CIRCLE_DEFAULT_RADIUS, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "radius"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-radius",
    transform: (value) => {
      if (value === void 0 || value === null) {
        return CIRCLE_DEFAULT_RADIUS;
      }
      const intValue = convertToInt(value, CIRCLE_DEFAULT_RADIUS);
      return intValue > 0 ? Math.max(intValue, CIRCLE_MIN_RADIUS) : CIRCLE_DEFAULT_RADIUS;
    }
  }));
  /**
   * @optional
   *
   * @description
   *
   * Habilita o modo indeterminado na barra de progresso, que mostra uma animação fixa sem um valor estabelecido.
   *
   * Esta opção pode ser utilizada quando não souber quanto tempo levará para que um processo seja concluído.
   *
   * > Caso esta propriedade e a `p-value` seja habilitada, a propriedade `p-value` será ignorada.
   *
   * @default `false`
   */
  set indeterminate(indeterminate) {
    this._indeterminate = convertToBoolean(indeterminate);
  }
  get indeterminate() {
    return this._indeterminate;
  }
  /**
   * @optional
   *
   * @description
   *
   * Valor que representará o progresso.
   *
   * > Os valores aceitos são números inteiros de `0` à `100`.
   *
   * @default `0`
   */
  set value(value) {
    const integerValue = convertToInt(value, poProgressMinValue);
    const isProgressRangeValue = this.isProgressRangeValue(integerValue);
    this._value = isProgressRangeValue ? integerValue : poProgressMinValue;
  }
  get value() {
    return this._value;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define a expessura da barra de progresso.
   *
   * Valores válidos:
   *  - medium
   *  - large
   *
   * @default `large`
   */
  set size(value) {
    this._size = PoProgressSize[value] ? PoProgressSize[value] : PoProgressSize.large;
  }
  get size() {
    return this._size;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define o tamanho das ações no componente com excessão da barra de progresso que pode ser ajustada através da propriedade `p-size`:
   * - `small`: aplica a medida small de cada componente (disponível apenas para acessibilidade AA).
   * - `medium`: aplica a medida medium de cada componente.
   *
   * > Caso a acessibilidade AA não esteja configurada, o tamanho `medium` será mantido.
   * Para mais detalhes, consulte a documentação do [po-theme](https://po-ui.io/documentation/po-theme).
   *
   * > Não compatível com `p-shape="circle"`.
   *
   * @default `medium`
   */
  set sizeActions(value) {
    this._initialSizeActions = value;
    this.applySizeActionsBasedOnA11y();
  }
  get sizeActions() {
    return this._sizeActions ?? getDefaultSizeFn(PoFieldSize);
  }
  /**
   * @optional
   *
   * @description
   *
   * Ativa a exibição da porcentagem atual da barra de progresso.
   *
   * > Se utilizada no `p-shape="circle"` e o status estiver como `error`, a porcentagem não será exibida.
   *
   * @default `false`
   */
  showPercentage = false;
  onThemeChange() {
    this.applySizeActionsBasedOnA11y();
  }
  applySizeActionsBasedOnA11y() {
    this._sizeActions = validateSizeFn(this._initialSizeActions, PoFieldSize);
  }
  isProgressRangeValue(value) {
    return value >= poProgressMinValue && value <= poProgressMaxValue;
  }
  static ɵfac = function PoProgressBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoProgressBaseComponent)();
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoProgressBaseComponent,
    hostVars: 1,
    hostBindings: function PoProgressBaseComponent_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("PoUiThemeChange", function PoProgressBaseComponent_PoUiThemeChange_HostBindingHandler() {
          return ctx.onThemeChange();
        }, ɵɵresolveWindow);
      }
      if (rf & 2) {
        ɵɵattribute("p-size-actions", ctx.sizeActions);
      }
    },
    inputs: {
      ariaLabel: [0, "p-aria-label", "ariaLabel"],
      disabledCancel: [2, "p-disabled-cancel", "disabledCancel", convertToBoolean],
      info: [0, "p-info", "info"],
      infoIcon: [0, "p-info-icon", "infoIcon"],
      status: [0, "p-status", "status"],
      text: [0, "p-text", "text"],
      customAction: [0, "p-custom-action", "customAction"],
      shape: [1, "p-shape", "shape"],
      radius: [1, "p-radius", "radius"],
      indeterminate: [0, "p-indeterminate", "indeterminate"],
      value: [0, "p-value", "value"],
      size: [0, "p-size", "size"],
      sizeActions: [0, "p-size-actions", "sizeActions"],
      showPercentage: [2, "p-show-percentage", "showPercentage", convertToBoolean]
    },
    outputs: {
      customActionClick: "p-custom-action-click",
      cancel: "p-cancel",
      retry: "p-retry"
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoProgressBaseComponent, [{
    type: Directive
  }], null, {
    ariaLabel: [{
      type: Input,
      args: ["p-aria-label"]
    }],
    disabledCancel: [{
      type: Input,
      args: [{
        alias: "p-disabled-cancel",
        transform: convertToBoolean
      }]
    }],
    info: [{
      type: Input,
      args: ["p-info"]
    }],
    infoIcon: [{
      type: Input,
      args: ["p-info-icon"]
    }],
    status: [{
      type: Input,
      args: ["p-status"]
    }],
    text: [{
      type: Input,
      args: ["p-text"]
    }],
    customAction: [{
      type: Input,
      args: ["p-custom-action"]
    }],
    customActionClick: [{
      type: Output,
      args: ["p-custom-action-click"]
    }],
    cancel: [{
      type: Output,
      args: ["p-cancel"]
    }],
    retry: [{
      type: Output,
      args: ["p-retry"]
    }],
    shape: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-shape",
        required: false
      }]
    }],
    radius: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-radius",
        required: false
      }]
    }],
    indeterminate: [{
      type: Input,
      args: ["p-indeterminate"]
    }],
    value: [{
      type: Input,
      args: ["p-value"]
    }],
    size: [{
      type: Input,
      args: ["p-size"]
    }],
    sizeActions: [{
      type: Input,
      args: ["p-size-actions"]
    }, {
      type: HostBinding,
      args: ["attr.p-size-actions"]
    }],
    showPercentage: [{
      type: Input,
      args: [{
        alias: "p-show-percentage",
        transform: convertToBoolean
      }]
    }],
    onThemeChange: [{
      type: HostListener,
      args: ["window:PoUiThemeChange"]
    }]
  });
})();
var PoProgressComponent = class _PoProgressComponent extends PoProgressBaseComponent {
  language;
  literals;
  elementRef = inject(ElementRef);
  resizeObserver;
  observeTarget = null;
  get isAllowCancel() {
    return !!this.cancel.observers.length && this.status !== PoProgressStatus.Success;
  }
  get isAllowInfoError() {
    return !!(!this.infoIcon && this.info && this.status === PoProgressStatus.Error);
  }
  get isAllowRetry() {
    return !!this.retry.observers.length && this.status === PoProgressStatus.Error;
  }
  get statusClass() {
    if (this.status === PoProgressStatus.Success) {
      return "po-progress-success";
    }
    if (this.status === PoProgressStatus.Error) {
      return "po-progress-error";
    }
    return "po-progress-default";
  }
  poLanguageService = inject(PoLanguageService);
  router = inject(Router);
  parentSize = signal(0, ...ngDevMode ? [{
    debugName: "parentSize"
  }] : (
    /* istanbul ignore next */
    []
  ));
  ngOnInit() {
    this.language = this.poLanguageService.getShortLanguage();
    this.literals = __spreadValues({}, poProgressLiterals[this.language]);
    if (this.shape() === "circle") {
      const parent = this.elementRef.nativeElement.parentElement;
      if (parent) {
        if (this.hasAllocatedHeight(parent)) {
          this.measureAndSetParentSize(parent);
          this.observeTarget = parent;
        } else {
          const constrained = this.findConstrainedAncestor(parent);
          if (this.hasExplicitHeight(constrained)) {
            this.measureAndSetParentSize(constrained);
            this.observeTarget = constrained;
          }
        }
      }
    }
  }
  ngAfterViewInit() {
    if (this.shape() !== "circle" || !this.observeTarget) {
      return;
    }
    this.resizeObserver = new ResizeObserver((entries) => {
      const {
        width,
        height
      } = entries[0].contentRect;
      const size = Math.min(width, height);
      if (size > 0) {
        this.parentSize.set(size);
      }
    });
    this.resizeObserver.observe(this.observeTarget);
  }
  ngOnDestroy() {
    this.resizeObserver?.disconnect();
  }
  emitCancellation() {
    this.cancel.emit(this.status);
  }
  emitRetry() {
    this.retry.emit();
  }
  actionIsDisabled(action) {
    return isTypeof(action.disabled, "function") ? action.disabled(action) : action.disabled;
  }
  callAction() {
    this.customActionClick.emit();
  }
  isActionVisible(action) {
    if (action && (action.label || action.icon)) {
      return action.visible !== void 0 ? isTypeof(action.visible, "function") ? action.visible() : !!action.visible : true;
    }
  }
  hasExplicitHeight(element) {
    const h = element.style.height;
    return !!h;
  }
  hasAllocatedHeight(element) {
    const parent = element.parentElement;
    if (!parent) {
      return false;
    }
    return this.hasExplicitHeight(parent);
  }
  measureAndSetParentSize(element) {
    const computedStyle = getComputedStyle(element);
    const paddingTop = parseFloat(computedStyle.paddingTop) || 0;
    const paddingBottom = parseFloat(computedStyle.paddingBottom) || 0;
    const paddingLeft = parseFloat(computedStyle.paddingLeft) || 0;
    const paddingRight = parseFloat(computedStyle.paddingRight) || 0;
    const width = element.clientWidth - paddingLeft - paddingRight;
    const height = element.clientHeight - paddingTop - paddingBottom;
    const size = Math.min(width, height);
    if (size > 0) {
      this.parentSize.set(size);
    }
  }
  findConstrainedAncestor(start2) {
    let element = start2;
    let depth = 0;
    while (element && depth < 10) {
      if (this.hasExplicitHeight(element)) {
        return element;
      }
      if (!element.parentElement) {
        break;
      }
      element = element.parentElement;
      depth++;
    }
    return start2;
  }
  static ɵfac = /* @__PURE__ */ (() => {
    let ɵPoProgressComponent_BaseFactory;
    return function PoProgressComponent_Factory(__ngFactoryType__) {
      return (ɵPoProgressComponent_BaseFactory || (ɵPoProgressComponent_BaseFactory = ɵɵgetInheritedFactory(_PoProgressComponent)))(__ngFactoryType__ || _PoProgressComponent);
    };
  })();
  static ɵcmp = ɵɵdefineComponent({
    type: _PoProgressComponent,
    selectors: [["po-progress"]],
    standalone: false,
    features: [ɵɵInheritDefinitionFeature],
    decls: 3,
    vars: 3,
    consts: [[1, "po-progress", 3, "ngClass"], [3, "class", "p-aria-label", "p-indeterminate", "p-value", "p-size", "p-status", "p-show-percentage", "p-radius", "p-parent-size"], [1, "po-progress-description"], [3, "p-aria-label", "p-indeterminate", "p-value"], [1, "po-progress-info"], [1, "po-progress-info-left"], [3, "p-icon", "po-progress-info-icon-error"], ["p-icon", "ICON_EXCLAMATION", 1, "po-progress-info-icon-error"], [1, "po-progress-info-text", 3, "po-progress-info-text-error"], [1, "po-progress-info-right"], ["p-icon", "ICON_REFRESH", "p-kind", "tertiary", 3, "p-aria-label", "p-size"], [1, "po-progress-custom-button", 3, "p-danger", "p-disabled", "p-label", "p-icon", "p-size"], ["p-icon", "ICON_CLOSE", "p-kind", "secondary", 3, "p-disabled", "p-aria-label", "p-danger", "p-size"], [3, "p-label"], [3, "p-icon"], [1, "po-progress-info-text"], ["p-icon", "ICON_REFRESH", "p-kind", "tertiary", 3, "p-click", "p-aria-label", "p-size"], [1, "po-progress-custom-button", 3, "p-click", "p-danger", "p-disabled", "p-label", "p-icon", "p-size"], ["p-icon", "ICON_CLOSE", "p-kind", "secondary", 3, "p-click", "p-disabled", "p-aria-label", "p-danger", "p-size"], [3, "p-aria-label", "p-indeterminate", "p-value", "p-size", "p-status", "p-show-percentage", "p-radius", "p-parent-size"]],
    template: function PoProgressComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "div", 0);
        ɵɵconditionalCreate(1, PoProgressComponent_Conditional_1_Template, 12, 14);
        ɵɵconditionalCreate(2, PoProgressComponent_Conditional_2_Template, 1, 11, "po-progress-circle", 1);
        ɵɵelementEnd();
      }
      if (rf & 2) {
        ɵɵproperty("ngClass", ctx.statusClass);
        ɵɵadvance();
        ɵɵconditional(ctx.shape() === "bar" ? 1 : -1);
        ɵɵadvance();
        ɵɵconditional(ctx.shape() === "circle" ? 2 : -1);
      }
    },
    dependencies: [NgClass, PoButtonComponent, PoIconComponent, PoLabelComponent, PoProgressCircleComponent, PoProgressBarComponent],
    encapsulation: 2,
    changeDetection: 0
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoProgressComponent, [{
    type: Component,
    args: [{
      selector: "po-progress",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: `<div class="po-progress" [ngClass]="statusClass">
  @if (shape() === 'bar') {
    @if (text) {
      <div class="po-progress-description">
        <po-label [p-label]="text"></po-label>
      </div>
    }
    <po-progress-bar
      class="po-progress-bar po-progress-bar-{{ size }}"
      [p-aria-label]="ariaLabel || text"
      [p-indeterminate]="indeterminate"
      [p-value]="value"
    >
    </po-progress-bar>

    <div class="po-progress-info">
      <div class="po-progress-info-left">
        @if (infoIcon) {
          <po-icon [p-icon]="infoIcon" [class.po-progress-info-icon-error]="status === 'error'"></po-icon>
        }
        @if (isAllowInfoError) {
          <po-icon p-icon="ICON_EXCLAMATION" class="po-progress-info-icon-error"></po-icon>
        }
        @if (info) {
          <span class="po-progress-info-text" [class.po-progress-info-text-error]="status === 'error'">{{ info }}</span>
        }
      </div>

      <div class="po-progress-info-right">
        @if (showPercentage && !indeterminate) {
          <span>{{ value }}%</span>
        }

        @if (isAllowRetry) {
          <po-button
            p-icon="ICON_REFRESH"
            (p-click)="emitRetry()"
            [p-aria-label]="literals.retry"
            p-kind="tertiary"
            [p-size]="sizeActions"
          ></po-button>
        }

        @if (isActionVisible(customAction)) {
          <po-button
            class="po-progress-custom-button"
            [p-danger]="customAction.type === 'danger'"
            [p-disabled]="actionIsDisabled(customAction)"
            [p-label]="customAction.label || ''"
            [p-icon]="customAction.icon"
            [p-size]="sizeActions"
            (p-click)="callAction()"
          >
          </po-button>
        }

        @if (isAllowCancel) {
          <po-button
            p-icon="ICON_CLOSE"
            (p-click)="emitCancellation()"
            p-kind="secondary"
            [p-disabled]="disabledCancel"
            [p-aria-label]="literals.cancel"
            [p-danger]="true"
            [p-size]="sizeActions"
          ></po-button>
        }
      </div>
    </div>
  }

  @if (shape() === 'circle') {
    <po-progress-circle
      class="po-progress-circle po-progress-circle-{{ size }}"
      [p-aria-label]="ariaLabel || text"
      [p-indeterminate]="indeterminate"
      [p-value]="value"
      [p-size]="size"
      [p-status]="status"
      [p-show-percentage]="showPercentage"
      [p-radius]="radius()"
      [p-parent-size]="parentSize()"
    >
    </po-progress-circle>
  }
</div>
`
    }]
  }], null, null);
})();
var PoProgressModule = class _PoProgressModule {
  static ɵfac = function PoProgressModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoProgressModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoProgressModule,
    declarations: [PoProgressBarComponent, PoProgressComponent],
    imports: [CommonModule, PoButtonModule, PoIconModule, PoLabelModule, PoProgressCircleComponent],
    exports: [PoProgressComponent]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [CommonModule, PoButtonModule, PoIconModule, PoLabelModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoProgressModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, PoButtonModule, PoIconModule, PoLabelModule, PoProgressCircleComponent],
      exports: [PoProgressComponent],
      declarations: [PoProgressBarComponent, PoProgressComponent]
    }]
  }], null, null);
})();
var PoTimePipe = class _PoTimePipe {
  transform(time, format2) {
    const hourRegex = /^(([0-1][0-9])|(2[0-3])):[0-5][0-9]:[0-5][0-9][\.]?([0-9]{1,6})?$/g;
    if (isTypeof(time, "string") && hourRegex.test(time)) {
      const amountOfF = format2 ? format2.lastIndexOf("f") - format2.indexOf("f") + 1 : 0;
      const miliseconds = this.addDotMiliseconds(time.substring(9, 9 + amountOfF), amountOfF);
      const formatMiliseconds = this.getFormatMiliseconds(amountOfF);
      return this.formatValue(time, format2, formatMiliseconds, miliseconds);
    }
    return null;
  }
  addDotMiliseconds(miliseconds, amountOfF) {
    if (miliseconds && amountOfF) {
      miliseconds = "." + miliseconds;
    }
    return miliseconds;
  }
  formatValue(time, format2, formatMiliseconds, miliseconds) {
    if (time && format2) {
      const hour = time.substring(0, 2);
      const minutes = time.substring(3, 5);
      const seconds = time.substring(6, 8);
      format2 = format2.replace("HH", hour);
      format2 = format2.replace("mm", minutes);
      format2 = format2.replace("ss", seconds);
      format2 = format2.replace(`.${formatMiliseconds}`, miliseconds);
      return format2;
    } else {
      return time;
    }
  }
  getFormatMiliseconds(amountOfF) {
    let formatMiliseconds = "";
    for (let i2 = 0; i2 < amountOfF; i2++) {
      formatMiliseconds += "f";
    }
    return formatMiliseconds;
  }
  static ɵfac = function PoTimePipe_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoTimePipe)();
  };
  static ɵpipe = ɵɵdefinePipe({
    name: "po_time",
    type: _PoTimePipe,
    pure: true,
    standalone: false
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoTimePipe, [{
    type: Pipe,
    args: [{
      name: "po_time",
      standalone: false
    }]
  }], null, null);
})();
var PoTimeModule = class _PoTimeModule {
  static ɵfac = function PoTimeModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoTimeModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoTimeModule,
    declarations: [PoTimePipe],
    exports: [PoTimePipe]
  });
  static ɵinj = ɵɵdefineInjector({});
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoTimeModule, [{
    type: NgModule,
    args: [{
      declarations: [PoTimePipe],
      imports: [],
      exports: [PoTimePipe],
      providers: [],
      bootstrap: []
    }]
  }], null, null);
})();
var PoPageSlideBaseComponent = class _PoPageSlideBaseComponent {
  /**
   * @description
   *
   * Título da página.
   */
  title;
  /**
   * @description
   *
   * Subtítulo da página.
   */
  subtitle;
  /**
   * @optional
   *
   * @description
   *
   * Oculta o botão de encerramento da página.
   *
   * > Para evitar que o usuário fique sem forma de fechar, esta opção só é mantida quando houver uma
   * > ação alternativa de fechamento: `p-click-out` habilitado ou, no modo `full`, um
   * > [`po-page-slide-footer`](/documentation/po-page-slide-footer) com ação de fechar.
   * > Caso contrário, o botão de fechar é reexibido automaticamente ao abrir.
   *
   * @default `false`
   */
  hideClose = false;
  /**
   * @optional
   *
   * @description
   *
   * Define se permite o encerramento da página ao clicar fora da mesma.
   *
   * @default `false`
   */
  clickOut = false;
  /**
   * @optional
   *
   * @description
   *
   * Permite a expansão dinâmica da largura do `po-page-slide` quando `p-size` for `auto` (automático).
   * Propriedade necessária para correto funcionamento da `po-table` dentro do `po-page-slide`
   *
   * @default `false`
   */
  flexibleWidth = false;
  /**
   * @optional
   *
   * @description
   * Evento executado ao fechar o page slide.
   */
  closePageSlide = new EventEmitter();
  // Controla se a página está ou não oculta, por padrão é oculto.
  hidden = true;
  _componentsSize = void 0;
  _initialComponentsSize = void 0;
  _size = "md";
  /**
   * @optional
   *
   * @description
   *
   * Define o tamanho da página.
   *
   * Valores válidos:
   *  - `sm` (pequeno)
   *  - `md` (médio)
   *  - `lg` (grande)
   *  - `xl` (extra-grande)
   *  - `auto` (automático)
   *  - `full` (tela cheia): a página ocupa 100% da largura da tela e o overlay de fundo não é renderizado.
   *
   * > Todas as opções de tamanho, exceto `auto` e `full`, possuem uma largura máxima de **768px**.
   *
   * > No modo `full`, como não há overlay, o fechamento por clique fora (`p-click-out`) não é aplicável.
   * > O encerramento continua disponível pelo botão do cabeçalho, pela tecla `Escape`, pelo método `#close()`
   * > ou por um botão no [`po-page-slide-footer`](/documentation/po-page-slide-footer).
   *
   * @default `md`
   */
  set size(value) {
    const sizes = ["sm", "md", "lg", "xl", "auto", "full"];
    this._size = sizes.includes(value) ? value : "md";
  }
  get size() {
    return this._size;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define o tamanho dos componentes de formulário no template:
   * - `small`: aplica a medida small de cada componente (disponível apenas para acessibilidade AA).
   * - `medium`: aplica a medida medium de cada componente.
   *
   * > Caso a acessibilidade AA não esteja configurada, o tamanho `medium` será mantido.
   * Para mais detalhes, consulte a documentação do [po-theme](https://po-ui.io/documentation/po-theme).
   *
   * @default `medium`
   */
  set componentsSize(value) {
    this._initialComponentsSize = value;
    this.applySizeBasedOnA11y();
  }
  get componentsSize() {
    return this._componentsSize ?? getDefaultSizeFn(PoFieldSize);
  }
  onThemeChange() {
    this.applySizeBasedOnA11y();
  }
  applySizeBasedOnA11y() {
    const size = validateSizeFn(this._initialComponentsSize, PoFieldSize);
    this._componentsSize = size;
  }
  /**
   * Ativa a visualização da página.
   *
   * Para utilizá-la é necessário ter a instância do componente no DOM, podendo
   * ser utilizado o `ViewChild` da seguinte forma:
   *
   * ```typescript
   * import { PoPageSlideComponent } from '@po/ng-components';
   *
   * ...
   *
   * @ViewChild(PoPageSlideComponent, { static: true }) pageSlide: PoPageSlideComponent;
   *
   * public openPage() {
   *   this.pageSlide.open();
   * }
   * ```
   */
  open() {
    if (this.hideClose && !this.hasAlternativeCloseAction()) {
      this.hideClose = false;
    }
    this.hidden = false;
  }
  hasAlternativeCloseAction() {
    return this.clickOut;
  }
  /**
   * Encerra a visualização da página.
   *
   * Para utilizá-la é necessário ter a instância do componente no DOM, podendo
   * ser utilizado o `ViewChild` da seguinte forma:
   *
   * ```typescript
   * import { PoPageSlideComponent } from '@po-ui/ng-components';
   *
   * ...
   *
   * @ViewChild(PoPageSlideComponent, { static: true }) pageSlide: PoPageSlideComponent;
   *
   * public closePage() {
   *   this.pageSlide.close();
   * }
   * ```
   */
  close() {
    this.hidden = true;
    this.closePageSlide.emit();
  }
  static ɵfac = function PoPageSlideBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoPageSlideBaseComponent)();
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoPageSlideBaseComponent,
    hostVars: 1,
    hostBindings: function PoPageSlideBaseComponent_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("PoUiThemeChange", function PoPageSlideBaseComponent_PoUiThemeChange_HostBindingHandler() {
          return ctx.onThemeChange();
        }, ɵɵresolveWindow);
      }
      if (rf & 2) {
        ɵɵattribute("p-components-size", ctx.componentsSize);
      }
    },
    inputs: {
      title: [0, "p-title", "title"],
      subtitle: [0, "p-subtitle", "subtitle"],
      hideClose: [2, "p-hide-close", "hideClose", convertToBoolean],
      clickOut: [2, "p-click-out", "clickOut", convertToBoolean],
      flexibleWidth: [2, "p-flexible-width", "flexibleWidth", convertToBoolean],
      size: [0, "p-size", "size"],
      componentsSize: [0, "p-components-size", "componentsSize"]
    },
    outputs: {
      closePageSlide: "p-close"
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoPageSlideBaseComponent, [{
    type: Directive
  }], null, {
    title: [{
      type: Input,
      args: ["p-title"]
    }],
    subtitle: [{
      type: Input,
      args: ["p-subtitle"]
    }],
    hideClose: [{
      type: Input,
      args: [{
        alias: "p-hide-close",
        transform: convertToBoolean
      }]
    }],
    clickOut: [{
      type: Input,
      args: [{
        alias: "p-click-out",
        transform: convertToBoolean
      }]
    }],
    flexibleWidth: [{
      type: Input,
      args: [{
        alias: "p-flexible-width",
        transform: convertToBoolean
      }]
    }],
    closePageSlide: [{
      type: Output,
      args: ["p-close"]
    }],
    size: [{
      type: Input,
      args: ["p-size"]
    }],
    componentsSize: [{
      type: Input,
      args: ["p-components-size"]
    }, {
      type: HostBinding,
      args: ["attr.p-components-size"]
    }],
    onThemeChange: [{
      type: HostListener,
      args: ["window:PoUiThemeChange"]
    }]
  });
})();
var PoPageSlideFooterComponent = class _PoPageSlideFooterComponent {
  /**
   * @optional
   *
   * @description
   *
   * Desabilita o alinhamento padrão, à direita, dos botões de ações que ficam no rodapé da [`PoPageSlide`](/documentation/po-page-slide).
   *
   * > Caso a propriedade esteja habilitada, o alinhamento deverá ser a esquerda e pode ser personalizado.
   *
   * @default false
   */
  disabledAlign = false;
  static ɵfac = function PoPageSlideFooterComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoPageSlideFooterComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoPageSlideFooterComponent,
    selectors: [["po-page-slide-footer"]],
    inputs: {
      disabledAlign: [0, "p-disabled-align", "disabledAlign"]
    },
    standalone: false,
    ngContentSelectors: _c12,
    decls: 2,
    vars: 2,
    consts: [[1, "po-page-slide-footer"]],
    template: function PoPageSlideFooterComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵprojectionDef();
        ɵɵelementStart(0, "div", 0);
        ɵɵprojection(1);
        ɵɵelementEnd();
      }
      if (rf & 2) {
        ɵɵclassProp("po-page-slide-footer-align-right", !ctx.disabledAlign);
      }
    },
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoPageSlideFooterComponent, [{
    type: Component,
    args: [{
      selector: "po-page-slide-footer",
      standalone: false,
      template: '<div class="po-page-slide-footer" [class.po-page-slide-footer-align-right]="!disabledAlign">\n  <ng-content></ng-content>\n</div>\n'
    }]
  }], null, {
    disabledAlign: [{
      type: Input,
      args: ["p-disabled-align"]
    }]
  });
})();
var poPageSlideLiteralsDefault = {
  en: {
    close: "Close",
    label: "Panel"
  },
  es: {
    close: "Cerrar",
    label: "Panel"
  },
  pt: {
    close: "Fechar",
    label: "Painel"
  },
  ru: {
    close: "Закрывать",
    label: "Панель"
  }
};
var PoPageSlideComponent = class _PoPageSlideComponent extends PoPageSlideBaseComponent {
  poActiveOverlayService = inject(PoActiveOverlayService);
  languageService = inject(PoLanguageService);
  _pageContent;
  firstElement;
  id = uuid();
  loadingCompleted = new ReplaySubject();
  sourceElement;
  buttonAriaLabel;
  defaultDialogLabel;
  duration = "70ms";
  timing = "700ms Cubic-Bezier(0.35, 0, 0.1, 1)";
  focusEvent;
  pageSlideFooter;
  set pageContent(pageContent) {
    if (pageContent) {
      this._pageContent = pageContent;
      this.loadingCompleted.next();
    }
  }
  get pageContent() {
    return this._pageContent;
  }
  constructor() {
    super();
    this.setTimeFromCSS();
    this.buttonAriaLabel = this.getTextDefault();
    this.defaultDialogLabel = this.getLabelDefault();
  }
  get titleId() {
    return `po-page-slide-title-${this.id}`;
  }
  open() {
    this.sourceElement = document.activeElement;
    super.open();
    this.loadingCompleted.pipe(take(1)).pipe(delay(0)).subscribe(this.handleFocus.bind(this));
  }
  hasAlternativeCloseAction() {
    if (this.size === "full") {
      return !!this.pageSlideFooter;
    }
    return super.hasAlternativeCloseAction();
  }
  close() {
    if (this.poActiveOverlayService.activeOverlay.length > 0 && this.poActiveOverlayService.activeOverlay[this.poActiveOverlayService.activeOverlay.length - 1] === this.id) {
      this.poActiveOverlayService.activeOverlay.pop();
    }
    super.close();
    this.removeEventListeners();
    this.sourceElement.focus();
  }
  onClickOut(event) {
    if (this.clickOut && !this.pageContent.nativeElement.contains(event.target)) {
      this.close();
    }
  }
  setTimeFromCSS() {
    const rootStyles = getComputedStyle(document.documentElement);
    this.duration = rootStyles.getPropertyValue("--transition-duration").trim();
    this.timing = rootStyles.getPropertyValue("--transition-timing").trim();
  }
  getTextDefault() {
    const language = this.languageService.getShortLanguage();
    return poPageSlideLiteralsDefault[language].close;
  }
  getLabelDefault() {
    const language = this.languageService.getShortLanguage();
    return poPageSlideLiteralsDefault[language].label;
  }
  handleFocus() {
    this.poActiveOverlayService.activeOverlay.push(this.id);
    this.loadFirstElement();
    this.initFocus();
    document.addEventListener("focus", this.focusEvent, true);
  }
  initFocus() {
    this.focusEvent = (event) => {
      const isCdkOverlayListbox = event.target["closest"](".cdk-overlay-container") !== null;
      if (!this.pageContent.nativeElement.contains(event.target) && !isCdkOverlayListbox && this.poActiveOverlayService.activeOverlay[this.poActiveOverlayService.activeOverlay.length - 1] === this.id) {
        event.stopPropagation();
        this.firstElement.focus();
      }
    };
    if (this.hideClose) {
      this.firstElement.focus();
    } else {
      const elements = getFocusableElements(this.pageContent.nativeElement);
      const element = elements[0] || this.pageContent.nativeElement;
      element.focus();
    }
  }
  loadFirstElement() {
    this.firstElement = getFocusableElements(this.pageContent.nativeElement)[0] || this.pageContent.nativeElement;
  }
  removeEventListeners() {
    document.removeEventListener("focus", this.focusEvent, true);
    this.loadingCompleted.complete();
  }
  get fadeParams() {
    return {
      value: true,
      params: {
        duration: this.duration || "70ms"
      }
    };
  }
  get slideParams() {
    return {
      value: true,
      params: {
        timing: this.timing || "700ms cubic-bezier(0.35, 0, 0.1, 1)"
      }
    };
  }
  static ɵfac = function PoPageSlideComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoPageSlideComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoPageSlideComponent,
    selectors: [["po-page-slide"]],
    contentQueries: function PoPageSlideComponent_ContentQueries(rf, ctx, dirIndex) {
      if (rf & 1) {
        ɵɵcontentQuery(dirIndex, PoPageSlideFooterComponent, 5);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.pageSlideFooter = _t.first);
      }
    },
    viewQuery: function PoPageSlideComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c83, 5, ElementRef);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.pageContent = _t.first);
      }
    },
    standalone: false,
    features: [ɵɵProvidersFeature([]), ɵɵInheritDefinitionFeature],
    ngContentSelectors: _c85,
    decls: 1,
    vars: 1,
    consts: [["pageContent", ""], ["tabindex", "0", 1, "po-page-slide"], ["tabindex", "0", 1, "po-page-slide", 3, "keydown.esc"], [1, "po-page-slide-overlay"], ["role", "dialog", "aria-modal", "true", 3, "ngStyle"], ["tabindex", "-1", 1, "po-page-slide-content"], [1, "po-page-slide-header"], [1, "po-page-slide-title"], [1, "po-page-slide-subtitle"], ["p-icon", "ICON_CLOSE", "p-kind", "tertiary", 3, "p-aria-label", "p-size"], [1, "po-page-slide-body"], [1, "po-page-slide-footer-content"], [1, "po-page-slide-overlay", 3, "mousedown"], ["p-icon", "ICON_CLOSE", "p-kind", "tertiary", 3, "p-click", "p-aria-label", "p-size"]],
    template: function PoPageSlideComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵprojectionDef(_c84);
        ɵɵconditionalCreate(0, PoPageSlideComponent_Conditional_0_Template, 14, 16, "div", 1);
      }
      if (rf & 2) {
        ɵɵconditional(!ctx.hidden ? 0 : -1);
      }
    },
    dependencies: [NgStyle, PoButtonComponent, PoDividerComponent],
    encapsulation: 2,
    data: {
      animation: [trigger("fade", [transition(":enter", [style({
        opacity: 0
      }), group([animate(`{{duration}}`, style({
        opacity: 1
      })), query("@slide", animateChild())])]), transition(":leave", group([query("@slide", animateChild()), animate("150ms", style({
        opacity: 0
      }))]))]), trigger("slide", [transition(":enter", [style({
        transform: "translateX(50px)"
      }), animate(`{{timing }}`, style({
        transform: "none"
      }))]), transition(":leave", [animate("150ms", style({
        transform: "translateX(50px)"
      }))])])]
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoPageSlideComponent, [{
    type: Component,
    args: [{
      selector: "po-page-slide",
      providers: [],
      animations: [trigger("fade", [transition(":enter", [style({
        opacity: 0
      }), group([animate(`{{duration}}`, style({
        opacity: 1
      })), query("@slide", animateChild())])]), transition(":leave", group([query("@slide", animateChild()), animate("150ms", style({
        opacity: 0
      }))]))]), trigger("slide", [transition(":enter", [style({
        transform: "translateX(50px)"
      }), animate(`{{timing }}`, style({
        transform: "none"
      }))]), transition(":leave", [animate("150ms", style({
        transform: "translateX(50px)"
      }))])])],
      standalone: false,
      template: `@if (!hidden) {
  <div class="po-page-slide" tabindex="0" (keydown.esc)="close()" [@fade]="fadeParams">
    @if (size !== 'full') {
      <div class="po-page-slide-overlay" (mousedown)="onClickOut($event)"></div>
    }
    <div
      class="po-page-slide-container po-page-slide-right po-page-slide-{{ size }}"
      role="dialog"
      aria-modal="true"
      [attr.aria-labelledby]="title ? titleId : null"
      [attr.aria-label]="title ? null : defaultDialogLabel"
      [@slide]="slideParams"
      [ngStyle]="{ 'width': flexibleWidth ? '' : size === 'auto' ? 'auto' : '' }"
    >
      <div class="po-page-slide-content" tabindex="-1" #pageContent>
        <div class="po-page-slide-header">
          <div class="po-page-slide-title">
            <span [attr.id]="titleId">{{ title }}</span>
            @if (subtitle) {
              <div class="po-page-slide-subtitle">{{ subtitle }}</div>
            }
          </div>
          @if (!hideClose) {
            <po-button
              p-icon="ICON_CLOSE"
              p-kind="tertiary"
              (p-click)="close()"
              [p-aria-label]="buttonAriaLabel"
              [p-size]="componentsSize"
            >
            </po-button>
          }
        </div>
        <div class="po-page-slide-body">
          <ng-content></ng-content>
        </div>
        @if (pageSlideFooter) {
          <div class="po-page-slide-footer-content">
            <po-divider></po-divider>
            <ng-content select="po-page-slide-footer"></ng-content>
          </div>
        }
      </div>
    </div>
  </div>
}
`
    }]
  }], () => [], {
    pageSlideFooter: [{
      type: ContentChild,
      args: [PoPageSlideFooterComponent]
    }],
    pageContent: [{
      type: ViewChild,
      args: ["pageContent", {
        read: ElementRef
      }]
    }]
  });
})();
var PoPageSlideModule = class _PoPageSlideModule {
  static ɵfac = function PoPageSlideModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoPageSlideModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoPageSlideModule,
    declarations: [PoPageSlideComponent, PoPageSlideFooterComponent],
    imports: [CommonModule, FormsModule, PoButtonModule, PoDividerModule],
    exports: [PoPageSlideComponent, PoPageSlideFooterComponent]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [CommonModule, FormsModule, PoButtonModule, PoDividerModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoPageSlideModule, [{
    type: NgModule,
    args: [{
      declarations: [PoPageSlideComponent, PoPageSlideFooterComponent],
      exports: [PoPageSlideComponent, PoPageSlideFooterComponent],
      imports: [CommonModule, FormsModule, PoButtonModule, PoDividerModule]
    }]
  }], null, null);
})();
var PoTableIconComponent = class _PoTableIconComponent {
  /** Define se o ícone é clicável. */
  clickable;
  /** Cor do ícone. */
  color;
  /** Desabilitado. */
  disabled;
  /** Classe css do ícone. */
  icon;
  /** Texto do tooltip. */
  iconTooltip;
  /** Output click. */
  click = new EventEmitter();
  tooltip;
  get allowTooltip() {
    return !this.disabled && this.iconTooltip;
  }
  onClick(event) {
    if (this.clickable) {
      this.click.emit(event);
    }
  }
  tooltipMouseEnter() {
    if (this.allowTooltip) {
      this.tooltip = this.iconTooltip;
    }
  }
  tooltipMouseLeave() {
    this.tooltip = void 0;
  }
  static ɵfac = function PoTableIconComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoTableIconComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoTableIconComponent,
    selectors: [["po-table-icon"]],
    inputs: {
      clickable: [0, "p-clickable", "clickable"],
      color: [0, "p-color", "color"],
      disabled: [0, "p-disabled", "disabled"],
      icon: [0, "p-icon", "icon"],
      iconTooltip: [0, "p-icon-tooltip", "iconTooltip"]
    },
    outputs: {
      click: "p-click"
    },
    standalone: false,
    decls: 1,
    vars: 10,
    consts: [[3, "click", "mouseenter", "mouseleave", "ngClass", "p-icon", "p-tooltip", "p-append-in-body"]],
    template: function PoTableIconComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "po-icon", 0);
        ɵɵlistener("click", function PoTableIconComponent_Template_po_icon_click_0_listener($event) {
          return ctx.onClick($event);
        })("mouseenter", function PoTableIconComponent_Template_po_icon_mouseenter_0_listener() {
          return ctx.tooltipMouseEnter();
        })("mouseleave", function PoTableIconComponent_Template_po_icon_mouseleave_0_listener() {
          return ctx.tooltipMouseLeave();
        });
        ɵɵelementEnd();
      }
      if (rf & 2) {
        ɵɵclassMap(ɵɵinterpolate1("po-field-icon po-table-icon-content ", ctx.disabled ? "" : ctx.color));
        ɵɵproperty("ngClass", ɵɵpureFunction2(7, _c87, ctx.clickable, ctx.disabled))("p-icon", ctx.icon)("p-tooltip", ctx.tooltip)("p-append-in-body", true);
      }
    },
    dependencies: [NgClass, PoTooltipDirective, PoIconComponent],
    encapsulation: 2,
    changeDetection: 0
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoTableIconComponent, [{
    type: Component,
    args: [{
      selector: "po-table-icon",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: `<po-icon
  class="po-field-icon po-table-icon-content {{ disabled ? '' : color }}"
  [ngClass]="{ 'po-clickable': clickable, 'po-table-icon-disabled': disabled }"
  [p-icon]="icon"
  [p-tooltip]="tooltip"
  [p-append-in-body]="true"
  (click)="onClick($event)"
  (mouseenter)="tooltipMouseEnter()"
  (mouseleave)="tooltipMouseLeave()"
></po-icon>
`
    }]
  }], null, {
    clickable: [{
      type: Input,
      args: ["p-clickable"]
    }],
    color: [{
      type: Input,
      args: ["p-color"]
    }],
    disabled: [{
      type: Input,
      args: ["p-disabled"]
    }],
    icon: [{
      type: Input,
      args: ["p-icon"]
    }],
    iconTooltip: [{
      type: Input,
      args: ["p-icon-tooltip"]
    }],
    click: [{
      type: Output,
      args: ["p-click"]
    }]
  });
})();
var PoTableColumnIconComponent = class _PoTableColumnIconComponent {
  /** Definição da coluna que utiliza os icones. */
  column;
  /** Dados da linha da tabela. */
  row;
  _icons = [];
  /** Lista de colunas com ícones. */
  set icons(icons) {
    this._icons = this.convertToColumnIcon(icons);
  }
  get icons() {
    return this._icons;
  }
  click(columnIcon, event) {
    const isAbleAction = !this.isDisabled(columnIcon);
    if (isAbleAction) {
      if (columnIcon.action) {
        columnIcon.action(this.row, columnIcon);
      } else if (this.column.action) {
        this.column.action(this.row, columnIcon);
      }
      event.stopPropagation();
    }
  }
  getColor(column) {
    const color3 = typeof column.color === "function" ? column.color(this.row, column) : column.color;
    if (color3) {
      return `po-text-${color3}`;
    }
  }
  getIcon(column) {
    return column.icon || column.value;
  }
  isClickable(columnIcon) {
    return !!(!this.isDisabled(columnIcon) && (columnIcon.action || this.column.action));
  }
  isDisabled(column) {
    return column.disabled ? column.disabled(this.row) : false;
  }
  trackByFunction(index) {
    return index;
  }
  convertToColumnIcon(rowIcons) {
    if (Array.isArray(rowIcons)) {
      return rowIcons.map((rowIcon) => typeof rowIcon === "string" ? {
        value: rowIcon
      } : rowIcon);
    }
    if (typeof rowIcons === "string") {
      return [{
        value: rowIcons
      }];
    }
    return [];
  }
  static ɵfac = function PoTableColumnIconComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoTableColumnIconComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoTableColumnIconComponent,
    selectors: [["po-table-column-icon"]],
    inputs: {
      column: [0, "p-column", "column"],
      row: [0, "p-row", "row"],
      icons: [0, "p-icons", "icons"]
    },
    standalone: false,
    decls: 1,
    vars: 2,
    consts: [[3, "p-clickable", "p-color", "p-disabled", "p-icon", "p-icon-tooltip", "p-click", 4, "ngFor", "ngForOf", "ngForTrackBy"], [3, "p-click", "p-clickable", "p-color", "p-disabled", "p-icon", "p-icon-tooltip"]],
    template: function PoTableColumnIconComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵtemplate(0, PoTableColumnIconComponent_po_table_icon_0_Template, 1, 5, "po-table-icon", 0);
      }
      if (rf & 2) {
        ɵɵproperty("ngForOf", ctx.icons)("ngForTrackBy", ctx.trackByFunction);
      }
    },
    dependencies: [NgForOf, PoTableIconComponent],
    encapsulation: 2,
    changeDetection: 0
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoTableColumnIconComponent, [{
    type: Component,
    args: [{
      selector: "po-table-column-icon",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: '<po-table-icon\n  *ngFor="let column of icons; trackBy: trackByFunction"\n  [p-clickable]="isClickable(column)"\n  [p-color]="getColor(column)"\n  [p-disabled]="isDisabled(column)"\n  [p-icon]="getIcon(column)"\n  [p-icon-tooltip]="column.tooltip"\n  (p-click)="click(column, $event)"\n>\n</po-table-icon>\n'
    }]
  }], null, {
    column: [{
      type: Input,
      args: ["p-column"]
    }],
    row: [{
      type: Input,
      args: ["p-row"]
    }],
    icons: [{
      type: Input,
      args: ["p-icons"]
    }]
  });
})();
var PoTableColumnLabelComponent = class _PoTableColumnLabelComponent {
  value;
  ngOnInit() {
    this.checkValueHasLabel();
  }
  checkValueHasLabel() {
    return this.value?.label?.trim() ? true : false;
  }
  static ɵfac = function PoTableColumnLabelComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoTableColumnLabelComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoTableColumnLabelComponent,
    selectors: [["po-table-column-label"]],
    inputs: {
      value: [0, "p-value", "value"]
    },
    standalone: false,
    decls: 1,
    vars: 1,
    consts: [[3, "p-color", "p-value", "p-text-color", "p-icon", "p-type"]],
    template: function PoTableColumnLabelComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵconditionalCreate(0, PoTableColumnLabelComponent_Conditional_0_Template, 1, 5, "po-tag", 0);
      }
      if (rf & 2) {
        ɵɵconditional(ctx.checkValueHasLabel() ? 0 : -1);
      }
    },
    dependencies: [PoTagComponent],
    encapsulation: 2,
    changeDetection: 0
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoTableColumnLabelComponent, [{
    type: Component,
    args: [{
      selector: "po-table-column-label",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: '@if (checkValueHasLabel()) {\n  <po-tag\n    [p-color]="value?.color"\n    [p-value]="value?.label"\n    [p-text-color]="value?.textColor"\n    [p-icon]="value?.icon"\n    [p-type]="value?.type"\n  >\n  </po-tag>\n}\n'
    }]
  }], null, {
    value: [{
      type: Input,
      args: ["p-value"]
    }]
  });
})();
var PoTableColumnLinkComponent = class _PoTableColumnLinkComponent {
  action;
  disabled;
  link;
  openNewTab = false;
  row;
  value;
  get type() {
    if (this.action) {
      return "action";
    }
    return isExternalLink(this.link) ? "externalLink" : "internalLink";
  }
  static ɵfac = function PoTableColumnLinkComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoTableColumnLinkComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoTableColumnLinkComponent,
    selectors: [["po-table-column-link"]],
    inputs: {
      action: [0, "p-action", "action"],
      disabled: [0, "p-disabled", "disabled"],
      link: [0, "p-link", "link"],
      openNewTab: [0, "p-open-new-tab", "openNewTab"],
      row: [0, "p-row", "row"],
      value: [0, "p-value", "value"]
    },
    standalone: false,
    decls: 4,
    vars: 4,
    consts: [[1, "po-table-link", 3, "ngClass"], [3, "p-label", "p-open-new-tab"], [3, "p-label", "p-open-new-tab", "p-url"], [3, "p-action", "p-label", "p-open-new-tab"]],
    template: function PoTableColumnLinkComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "div", 0);
        ɵɵconditionalCreate(1, PoTableColumnLinkComponent_Case_1_Template, 1, 2, "po-link", 1)(2, PoTableColumnLinkComponent_Case_2_Template, 1, 3, "po-link", 2)(3, PoTableColumnLinkComponent_Case_3_Template, 1, 3, "po-link", 2);
        ɵɵelementEnd();
      }
      if (rf & 2) {
        let tmp_1_0;
        ɵɵproperty("ngClass", ɵɵpureFunction1(2, _c88, ctx.disabled));
        ɵɵadvance();
        ɵɵconditional((tmp_1_0 = ctx.type) === "action" ? 1 : tmp_1_0 === "externalLink" ? 2 : tmp_1_0 === "internalLink" ? 3 : -1);
      }
    },
    dependencies: [NgClass, PoLinkComponent],
    encapsulation: 2,
    changeDetection: 0
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoTableColumnLinkComponent, [{
    type: Component,
    args: [{
      selector: "po-table-column-link",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: `<div class="po-table-link" [ngClass]="{ 'po-table-link-disabled': disabled }">
  @switch (type) {
    @case ('action') {
      <po-link [p-label]="value" [p-open-new-tab]="openNewTab" (p-action)="action?.(value, row)"></po-link>
    }
    @case ('externalLink') {
      <po-link [p-label]="value" [p-open-new-tab]="openNewTab" [p-url]="link"></po-link>
    }
    @case ('internalLink') {
      <po-link [p-label]="value" [p-open-new-tab]="openNewTab" [p-url]="link"></po-link>
    }
  }
</div>
`
    }]
  }], null, {
    action: [{
      type: Input,
      args: ["p-action"]
    }],
    disabled: [{
      type: Input,
      args: ["p-disabled"]
    }],
    link: [{
      type: Input,
      args: ["p-link"]
    }],
    openNewTab: [{
      type: Input,
      args: ["p-open-new-tab"]
    }],
    row: [{
      type: Input,
      args: ["p-row"]
    }],
    value: [{
      type: Input,
      args: ["p-value"]
    }]
  });
})();
var PoSwitchLabelPosition;
(function(PoSwitchLabelPosition2) {
  PoSwitchLabelPosition2[PoSwitchLabelPosition2["Right"] = 0] = "Right";
  PoSwitchLabelPosition2[PoSwitchLabelPosition2["Left"] = 1] = "Left";
})(PoSwitchLabelPosition || (PoSwitchLabelPosition = {}));
var PoSwitchComponent = class _PoSwitchComponent extends PoFieldModel {
  changeDetector = inject(ChangeDetectorRef);
  injector = inject(Injector);
  switchContainer;
  helperEl;
  id = `po-switch[${uuid()}]`;
  // Parâmetro interno, não documentar
  value = false;
  _labelOff = "false";
  _labelOn = "true";
  _labelPosition = PoSwitchLabelPosition.Right;
  _loading = false;
  _formatModel = false;
  _size = void 0;
  _initialSize = void 0;
  statusChangesSubscription;
  /**
   * @optional
   *
   * @description
   * Define se o título do campo será exibido de forma compacta.
   *
   * Quando habilitado (`true`), o modo compacto afeta o conjunto composto por:
   * - `po-label`
   * - `p-requirement (showRequired)`
   * - `po-helper`
   *
   * Ou seja, todos os elementos relacionados ao título do campo
   * (rótulo, indicador de obrigatoriedade e componente auxiliar) passam
   * a seguir o comportamento de layout compacto.
   *
   * Também é possível definir esse comportamento de forma global,
   * uma única vez, na folha de estilo geral da aplicação, por meio
   * da customização dos tokens CSS:
   *
   * - `--field-container-title-justify`
   * - `--field-container-title-flex`
   *
   * Exemplo:
   *
   * ```
   * :root {
   *   --field-container-title-justify: flex-start;
   *   --field-container-title-flex: 0 1 auto;
   * }
   * ```
   *
   * Dessa forma, o layout compacto passa a ser o padrão da aplicação,
   * sem a necessidade de definir a propriedade individualmente em cada campo.
   *
   * @default `false`
   */
  compactLabel = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "compactLabel"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-compact-label",
    transform: convertToBoolean
  }));
  /**
   * @optional
   *
   * @description
   *
   * Indica se o `model` receberá o valor formatado pelas propriedades `p-label-on` e `p-label-off` ou
   * apenas o valor puro (sem formatação).
   *
   * > Por padrão será atribuído `false`.
   * @default `false`
   */
  set formatModel(format2) {
    this._formatModel = format2 || false;
  }
  get formatModel() {
    return this._formatModel;
  }
  /**
   * @optional
   *
   * @description
   *
   * Indica se o status do `model` será escondido visualmente ao lado do switch.
   *
   * > Por padrão será atribuído `false`.
   * @default `false`
   */
  hideLabelStatus = false;
  /**
   * @optional
   *
   * @description
   *
   * Posição de exibição do rótulo que fica ao lado do switch.
   *
   * > Por padrão exibe à direita.
   */
  set labelPosition(position) {
    this._labelPosition = position in PoSwitchLabelPosition ? parseInt(position, 10) : PoSwitchLabelPosition.Right;
  }
  get labelPosition() {
    return this._labelPosition;
  }
  /**
   * Texto exibido quando o valor do componente for `false`.
   *
   * @default `false`
   */
  set labelOff(label) {
    this._labelOff = label || "false";
  }
  get labelOff() {
    return this._labelOff;
  }
  /**
   * Texto exibido quando o valor do componente for `true`.
   *
   * @default `true`
   */
  set labelOn(label) {
    this._labelOn = label || "true";
  }
  get labelOn() {
    return this._labelOn;
  }
  /**
   * @optional
   *
   * @description
   * Exibe um ícone de carregamento substituindo o switch para sinalizar que uma operação está em andamento.
   *
   * @default `false`
   */
  set loading(value) {
    this._loading = value;
    this.changeDetector.markForCheck();
  }
  get loading() {
    return this._loading;
  }
  get isDisabled() {
    return this.disabled || this.loading;
  }
  /**
   * @optional
   *
   * @description
   *
   * Exibe a mensagem de erro configurada quando o campo estiver desligado(off/false).
   *
   *
   */
  fieldErrorMessage;
  /**
   * @optional
   *
   * @description
   *
   * Limita a exibição da mensagem de erro a duas linhas e exibe um tooltip com o texto completo.
   *
   * > Caso essa propriedade seja definida como `true`, a mensagem de erro será limitada a duas linhas
   * e um tooltip será exibido ao passar o mouse sobre a mensagem para mostrar o conteúdo completo.
   *
   * @default `false`
   */
  errorLimit = false;
  /**
   * @optional
   *
   * @description
   *
   * Define qual valor será considerado como inválido para exibir a mensagem da propriedade `p-field-error-message`.
   *
   * > Caso essa propriedade seja definida como `true`, a mensagem de erro será exibida quando o campo estiver ligado(on/true).
   *
   * @default `false`
   */
  invalidValue = false;
  /**
   * @optional
   *
   * @description
   *
   * Define o tamanho do componente:
   * - `small`: altura de 16px (disponível apenas para acessibilidade AA).
   * - `medium`: altura de 24px.
   *
   * > Caso a acessibilidade AA não esteja configurada, o tamanho `medium` será mantido.
   * Para mais detalhes, consulte a documentação do [po-theme](https://po-ui.io/documentation/po-theme).
   *
   * @default `medium`
   *
   */
  set size(value) {
    this._initialSize = value;
    this.applySizeBasedOnA11y();
  }
  get size() {
    return this._size ?? getDefaultSizeFn(PoFieldSize);
  }
  /**
   * @optional
   *
   * @description
   *
   * Define as opções do componente de ajuda (po-helper) que será exibido ao lado do label quando a propriedade `p-label` for definida, ou, ao lado do componente na ausência da propriedade `p-label`.
   * > Para mais informações acesse: https://po-ui.io/documentation/po-helper.
   *
   * > Ao configurar esta propriedade, o antigo ícone de ajuda adicional (`p-additional-help-tooltip` e `p-additional-help`) será ignorado.
   */
  poHelperComponent = input(void 0, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "poHelperComponent"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-helper"
  }));
  /**
   * @optional
   *
   * @description
   * Habilita a quebra automática do texto da propriedade `p-label`. Quando `p-label-text-wrap` for verdadeiro, o texto que excede
   * o espaço disponível é transferido para a próxima linha em pontos apropriados para uma
   * leitura clara.
   *
   * @default `false`
   */
  labelTextWrap = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "labelTextWrap"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-label-text-wrap"
  }));
  /**
   * @optional
   *
   * @description
   *
   * Evento disparado sempre que o valor do model é alterado, seja por interação do usuário
   * ou por atualização programática (ex: `setValue`, `patchValue`, carregamento assíncrono).
   *
   * Diferentemente do `p-change`, que é disparado apenas por interação do usuário,
   * o `p-change-model` cobre todos os cenários de alteração de valor.
   *
   * Não emite quando o novo valor é idêntico ao anterior (deduplicação automática).
   */
  changeModel = new EventEmitter();
  modelLastUpdate;
  el = inject(ElementRef);
  injectOptions = {
    self: true
  };
  control;
  ngOnChanges(changes) {
    if (changes.label) {
      this.displayAdditionalHelp = false;
    }
  }
  ngOnDestroy() {
    this.statusChangesSubscription?.unsubscribe();
  }
  /**
   * Função que atribui foco ao componente.
   *
   * Para utilizá-la é necessário ter a instância do componente no DOM, podendo ser utilizado o ViewChild da seguinte forma:
   *
   * ```
   * import { PoSwitchComponent } from '@po-ui/ng-components';
   *
   * ...
   *
   * @ViewChild(PoSwitchComponent, { static: true }) switch: PoSwitchComponent;
   *
   * focusSwitch() {
   *   this.switch.focus();
   * }
   * ```
   */
  focus() {
    if (!this.disabled) {
      this.switchContainer.nativeElement.focus();
    }
  }
  onBlur() {
    this.onTouched?.();
    if (this.getAdditionalHelpTooltip() && this.displayAdditionalHelp) {
      super.showAdditionalHelp(this.helperEl, this.poHelperComponent());
    }
  }
  getLabelPosition() {
    switch (this.labelPosition) {
      case PoSwitchLabelPosition.Left:
        return "left";
      case PoSwitchLabelPosition.Right:
        return "right";
      default:
        return "right";
    }
  }
  onKeyDown(event) {
    const isFieldFocused = document.activeElement === this.switchContainer?.nativeElement;
    if (event.which === PoKeyCodeEnum.space || event.keyCode === PoKeyCodeEnum.space) {
      event.preventDefault();
      this.eventClick();
    }
    if (isFieldFocused) {
      this.keydown.emit(event);
    }
  }
  changeValue(value) {
    if (this.value !== value) {
      this.value = value;
      if (this.formatModel) {
        if (this.value) {
          this.updateModel(this.labelOn);
        } else {
          this.updateModel(this.labelOff);
        }
      } else {
        this.updateModel(value);
      }
      this.emitChange(this.value);
      this.controlChangeModelEmitter(this.value);
    }
  }
  eventClick() {
    if (!this.disabled) {
      this.changeValue(!this.value);
    }
  }
  onWriteValue(value) {
    if (value !== this.value) {
      if (this.formatModel && !!value) {
        this.value = value.toLowerCase() === this.labelOn.toLowerCase();
      } else {
        this.value = !!value;
      }
      this.changeDetector.markForCheck();
    }
    if (value != null) {
      this.controlChangeModelEmitter(this.value);
    }
  }
  validate(control) {
    const value = this.value;
    const isRequired = {
      required: true
    };
    if (this.invalidValue && this.fieldErrorMessage) {
      return value === this.invalidValue ? isRequired : null;
    } else if (this.fieldErrorMessage) {
      return value === true ? null : isRequired;
    }
    return null;
  }
  ngAfterViewInit() {
    this.setControl();
  }
  onThemeChange() {
    this.applySizeBasedOnA11y();
  }
  setControl() {
    const ngControl = this.injector.get(NgControl, null, this.injectOptions);
    if (ngControl) {
      this.control = ngControl.control;
      if (this.control) {
        this.statusChangesSubscription = this.control.statusChanges.subscribe(() => {
          this.changeDetector.markForCheck();
        });
      }
    }
  }
  //Transforma o tamanho do input para o tamanho do ícone de loading correspondente
  mapSizeToIcon(size) {
    return mapInputSizeToLoadingIcon(size);
  }
  getErrorPattern() {
    return this.fieldErrorMessage && this.hasInvalidClass() ? this.fieldErrorMessage : "";
  }
  hasInvalidClass() {
    return this.el.nativeElement.classList.contains("ng-invalid") && this.el.nativeElement.classList.contains("ng-dirty");
  }
  setHelper(label, additionalHelpTooltip) {
    return setHelperSettings(label, additionalHelpTooltip, this.poHelperComponent(), this.size, this.isAdditionalHelpEventTriggered() ? this.additionalHelp : void 0);
  }
  /**
   * Método que exibe `p-helper` ou executa a ação definida em `p-helper{eventOnClick}` ou em `p-additionalHelp`.
   * Para isso, será necessário configurar uma tecla de atalho utilizando o evento `p-keydown`.
   *
   * > Exibe ou oculta o conteúdo do componente `po-helper` quando o componente estiver com foco.
   *
   * ```
   * //Exemplo com p-label e p-helper
   * <po-switch
   *  #switch
   *  ...
   *  p-label="Label do switch"
   *  [p-helper]="helperOptions"
   *  (p-keydown)="onKeyDown($event, switch)"
   * ></po-switch>
   * ```
   * ```typescript
   * onKeyDown(event: KeyboardEvent, inp: PoSwitchComponent): void {
   *  if (event.code === 'F9') {
   *    inp.showAdditionalHelp();
   *  }
   * }
   * ```
   */
  showAdditionalHelp() {
    return super.showAdditionalHelp(this.helperEl, this.poHelperComponent());
  }
  controlChangeModelEmitter(value) {
    if (this.modelLastUpdate !== value) {
      this.changeModel.emit(value);
      this.modelLastUpdate = value;
    }
  }
  applySizeBasedOnA11y() {
    const size = validateSizeFn(this._initialSize, PoFieldSize);
    this._size = size;
  }
  static ɵfac = /* @__PURE__ */ (() => {
    let ɵPoSwitchComponent_BaseFactory;
    return function PoSwitchComponent_Factory(__ngFactoryType__) {
      return (ɵPoSwitchComponent_BaseFactory || (ɵPoSwitchComponent_BaseFactory = ɵɵgetInheritedFactory(_PoSwitchComponent)))(__ngFactoryType__ || _PoSwitchComponent);
    };
  })();
  static ɵcmp = ɵɵdefineComponent({
    type: _PoSwitchComponent,
    selectors: [["po-switch"]],
    viewQuery: function PoSwitchComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c89, 7)(_c17, 5, PoHelperComponent);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.switchContainer = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.helperEl = _t.first);
      }
    },
    hostVars: 1,
    hostBindings: function PoSwitchComponent_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("PoUiThemeChange", function PoSwitchComponent_PoUiThemeChange_HostBindingHandler() {
          return ctx.onThemeChange();
        }, ɵɵresolveWindow);
      }
      if (rf & 2) {
        ɵɵattribute("p-size", ctx.size);
      }
    },
    inputs: {
      value: [2, "p-value", "value", convertToBoolean],
      compactLabel: [1, "p-compact-label", "compactLabel"],
      formatModel: [2, "p-format-model", "formatModel", convertToBoolean],
      hideLabelStatus: [2, "p-hide-label-status", "hideLabelStatus", convertToBoolean],
      labelPosition: [0, "p-label-position", "labelPosition"],
      labelOff: [0, "p-label-off", "labelOff"],
      labelOn: [0, "p-label-on", "labelOn"],
      loading: [2, "p-loading", "loading", convertToBoolean],
      fieldErrorMessage: [0, "p-field-error-message", "fieldErrorMessage"],
      errorLimit: [0, "p-error-limit", "errorLimit"],
      invalidValue: [0, "p-invalid-value", "invalidValue"],
      size: [0, "p-size", "size"],
      poHelperComponent: [1, "p-helper", "poHelperComponent"],
      labelTextWrap: [1, "p-label-text-wrap", "labelTextWrap"]
    },
    outputs: {
      changeModel: "p-change-model"
    },
    standalone: false,
    features: [ɵɵProvidersFeature([{
      provide: NG_VALUE_ACCESSOR,
      useExisting: forwardRef(() => _PoSwitchComponent),
      multi: true
    }, {
      provide: NG_VALIDATORS,
      useExisting: forwardRef(() => _PoSwitchComponent),
      multi: true
    }, {
      provide: NgControl,
      useExisting: forwardRef(() => _PoSwitchComponent),
      multi: false
    }]), ɵɵInheritDefinitionFeature, ɵɵNgOnChangesFeature],
    decls: 11,
    vars: 26,
    consts: [["switchContainer", ""], ["helperEl", ""], [3, "p-disabled", "p-id", "p-label", "p-helper", "p-show-helper", "p-size", "p-text-wrap", "p-compact-label"], [1, "po-field-container-content"], [1, "po-switch"], ["role", "switch", 1, "po-switch-container", 3, "blur", "click", "keydown", "id", "tabindex"], [1, "po-switch-loading-container"], [1, "po-switch-track"], [1, "po-switch-label", 3, "p-disabled", "p-label"], [3, "p-size", "p-helper", "p-disabled", "p-append-in-body"], [3, "p-append-in-body", "p-help", "p-disabled", "p-error-limit", "p-error-pattern", "p-size"], [3, "p-size"], [1, "po-switch-toggle"], ["p-icon", "ICON_OK", 1, "po-switch-icon"], [1, "po-switch-label", 3, "click", "p-disabled", "p-label"]],
    template: function PoSwitchComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵdeclareLet(0);
        ɵɵelementStart(1, "po-field-container", 2)(2, "div", 3)(3, "div", 4)(4, "div", 5, 0);
        ɵɵlistener("blur", function PoSwitchComponent_Template_div_blur_4_listener() {
          return ctx.onBlur();
        })("click", function PoSwitchComponent_Template_div_click_4_listener() {
          return ctx.eventClick();
        })("keydown", function PoSwitchComponent_Template_div_keydown_4_listener($event) {
          return ctx.onKeyDown($event);
        });
        ɵɵconditionalCreate(6, PoSwitchComponent_Conditional_6_Template, 2, 1, "div", 6)(7, PoSwitchComponent_Conditional_7_Template, 3, 2, "div", 7);
        ɵɵelementEnd();
        ɵɵconditionalCreate(8, PoSwitchComponent_Conditional_8_Template, 1, 2, "po-label", 8);
        ɵɵelementEnd();
        ɵɵconditionalCreate(9, PoSwitchComponent_Conditional_9_Template, 2, 4, "po-helper", 9);
        ɵɵelementEnd();
        ɵɵelement(10, "po-field-container-bottom", 10);
        ɵɵelementEnd();
      }
      if (rf & 2) {
        const poHelper_r4 = ɵɵstoreLet(ctx.setHelper(ctx.label, ctx.additionalHelpTooltip));
        ɵɵadvance();
        ɵɵproperty("p-disabled", ctx.isDisabled)("p-id", ctx.id)("p-label", ctx.label)("p-helper", ctx.label ? poHelper_r4.helperSettings : void 0)("p-show-helper", ctx.label ? ctx.displayAdditionalHelp : void 0)("p-size", ctx.size)("p-text-wrap", ctx.labelTextWrap())("p-compact-label", ctx.compactLabel());
        ɵɵadvance();
        ɵɵattribute("name", ctx.name);
        ɵɵadvance();
        ɵɵattribute("data-label-position", ctx.getLabelPosition());
        ɵɵadvance();
        ɵɵproperty("id", ctx.id)("tabindex", ctx.isDisabled ? -1 : 0);
        ɵɵattribute("aria-checked", ctx.value)("aria-disabled", ctx.isDisabled)("data-focused-element", !ctx.isDisabled)("data-inactive-component", ctx.isDisabled);
        ɵɵadvance(2);
        ɵɵconditional(ctx.loading ? 6 : 7);
        ɵɵadvance(2);
        ɵɵconditional(!ctx.hideLabelStatus && !ctx.loading ? 8 : -1);
        ɵɵadvance();
        ɵɵconditional(!ctx.label && ctx.poHelperComponent() || !ctx.label && poHelper_r4.hideAdditionalHelp ? 9 : -1);
        ɵɵadvance();
        ɵɵproperty("p-append-in-body", ctx.appendBox)("p-help", ctx.help)("p-disabled", ctx.isDisabled)("p-error-limit", ctx.errorLimit)("p-error-pattern", ctx.getErrorPattern())("p-size", ctx.size);
      }
    },
    dependencies: [PoFieldContainerBottomComponent, PoFieldContainerComponent, PoLabelComponent, PoIconComponent, PoHelperComponent, PoLoadingIconComponent],
    encapsulation: 2,
    changeDetection: 0
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoSwitchComponent, [{
    type: Component,
    args: [{
      selector: "po-switch",
      changeDetection: ChangeDetectionStrategy.OnPush,
      providers: [{
        provide: NG_VALUE_ACCESSOR,
        useExisting: forwardRef(() => PoSwitchComponent),
        multi: true
      }, {
        provide: NG_VALIDATORS,
        useExisting: forwardRef(() => PoSwitchComponent),
        multi: true
      }, {
        provide: NgControl,
        useExisting: forwardRef(() => PoSwitchComponent),
        multi: false
      }],
      standalone: false,
      template: '@let poHelper = setHelper(label, additionalHelpTooltip);\n<po-field-container\n  [p-disabled]="isDisabled"\n  [p-id]="id"\n  [p-label]="label"\n  [p-helper]="label ? poHelper.helperSettings : undefined"\n  [p-show-helper]="label ? displayAdditionalHelp : undefined"\n  [p-size]="size"\n  [p-text-wrap]="labelTextWrap()"\n  [p-compact-label]="compactLabel()"\n>\n  <div class="po-field-container-content" [attr.name]="name">\n    <div class="po-switch" [attr.data-label-position]="getLabelPosition()">\n      <div\n        #switchContainer\n        class="po-switch-container"\n        role="switch"\n        [attr.aria-checked]="value"\n        [attr.aria-disabled]="isDisabled"\n        [attr.data-focused-element]="!isDisabled"\n        [attr.data-inactive-component]="isDisabled"\n        [id]="id"\n        [tabindex]="isDisabled ? -1 : 0"\n        (blur)="onBlur()"\n        (click)="eventClick()"\n        (keydown)="onKeyDown($event)"\n      >\n        @if (loading) {\n          <div class="po-switch-loading-container">\n            <po-loading-icon [p-size]="mapSizeToIcon(size)"></po-loading-icon>\n          </div>\n        } @else {\n          <div class="po-switch-track">\n            <div class="po-switch-toggle" [attr.aria-label]="value === true ? labelOn : labelOff">\n              @if (value === true) {\n                <po-icon class="po-switch-icon" p-icon="ICON_OK"></po-icon>\n              }\n            </div>\n          </div>\n        }\n      </div>\n      @if (!hideLabelStatus && !loading) {\n        <po-label\n          class="po-switch-label"\n          [p-disabled]="isDisabled"\n          [p-label]="value === true ? labelOn : labelOff"\n          (click)="eventClick()"\n        >\n        </po-label>\n      }\n    </div>\n    @if ((!this.label && poHelperComponent()) || (!this.label && poHelper.hideAdditionalHelp)) {\n      <po-helper\n        #helperEl\n        [p-size]="size"\n        [p-helper]="poHelper.helperSettings"\n        [p-disabled]="isDisabled"\n        [p-append-in-body]="appendBox"\n      >\n      </po-helper>\n    }\n  </div>\n  <po-field-container-bottom\n    [p-append-in-body]="appendBox"\n    [p-help]="help"\n    [p-disabled]="isDisabled"\n    [p-error-limit]="errorLimit"\n    [p-error-pattern]="getErrorPattern()"\n    [p-size]="size"\n  ></po-field-container-bottom>\n</po-field-container>\n'
    }]
  }], null, {
    switchContainer: [{
      type: ViewChild,
      args: ["switchContainer", {
        static: true
      }]
    }],
    helperEl: [{
      type: ViewChild,
      args: ["helperEl", {
        read: PoHelperComponent,
        static: false
      }]
    }],
    value: [{
      type: Input,
      args: [{
        alias: "p-value",
        transform: convertToBoolean
      }]
    }],
    compactLabel: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-compact-label",
        required: false
      }]
    }],
    formatModel: [{
      type: Input,
      args: [{
        alias: "p-format-model",
        transform: convertToBoolean
      }]
    }],
    hideLabelStatus: [{
      type: Input,
      args: [{
        alias: "p-hide-label-status",
        transform: convertToBoolean
      }]
    }],
    labelPosition: [{
      type: Input,
      args: ["p-label-position"]
    }],
    labelOff: [{
      type: Input,
      args: ["p-label-off"]
    }],
    labelOn: [{
      type: Input,
      args: ["p-label-on"]
    }],
    loading: [{
      type: Input,
      args: [{
        alias: "p-loading",
        transform: convertToBoolean
      }]
    }],
    fieldErrorMessage: [{
      type: Input,
      args: ["p-field-error-message"]
    }],
    errorLimit: [{
      type: Input,
      args: ["p-error-limit"]
    }],
    invalidValue: [{
      type: Input,
      args: ["p-invalid-value"]
    }],
    size: [{
      type: Input,
      args: ["p-size"]
    }, {
      type: HostBinding,
      args: ["attr.p-size"]
    }],
    poHelperComponent: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-helper",
        required: false
      }]
    }],
    labelTextWrap: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-label-text-wrap",
        required: false
      }]
    }],
    changeModel: [{
      type: Output,
      args: ["p-change-model"]
    }],
    onThemeChange: [{
      type: HostListener,
      args: ["window:PoUiThemeChange"]
    }]
  });
})();
var poTableListManagerLiterals = {
  en: {
    up: "up",
    down: "down",
    otherColumns: "Other columns",
    fixedColumns: "Fixed"
  },
  es: {
    up: "arriba",
    down: "abajo",
    otherColumns: "Otras columnas",
    fixedColumns: "Fijado"
  },
  pt: {
    up: "acima",
    down: "abaixo",
    otherColumns: "Outras colunas",
    fixedColumns: "Fixo"
  },
  ru: {
    up: "вверх",
    down: "вниз",
    otherColumns: "Другие столбцы",
    fixedColumns: "зафиксированный"
  }
};
var PoTableListManagerComponent = class _PoTableListManagerComponent extends PoCheckboxGroupComponent {
  _componentsSize = void 0;
  changePosition = new EventEmitter();
  changeColumnFixed = new EventEmitter();
  columnsManager;
  /**
   * @optional
   *
   * @description
   *
   * Define o tamanho dos componentes de formulário no table:
   * - `small`: aplica a medida small de cada componente (disponível apenas para acessibilidade AA).
   * - `medium`: aplica a medida medium de cada componente.
   *
   * > Caso a acessibilidade AA não esteja configurada, o tamanho `medium` será mantido.
   * Para mais detalhes, consulte a documentação do [po-theme](https://po-ui.io/documentation/po-theme).
   *
   * @default `medium`
   */
  set componentsSize(value) {
    this._componentsSize = validateSizeFn(value, PoFieldSize);
  }
  get componentsSize() {
    return this._componentsSize ?? getDefaultSizeFn(PoFieldSize);
  }
  hideActionFixedColumns = false;
  literals;
  constructor(languageService, changeDetector) {
    super();
    const language = languageService.getShortLanguage();
    this.literals = __spreadValues(__spreadValues({}, poTableListManagerLiterals[poLocaleDefault]), poTableListManagerLiterals[language]);
  }
  emitChangePosition(option, direction) {
    if (!this.isFixed(option)) {
      const infoPosition = {
        option,
        direction
      };
      const hasDisabled = this.verifyArrowDisabled(option, direction);
      if (!hasDisabled) {
        this.changePosition.emit(infoPosition);
      }
    }
  }
  verifyArrowDisabled(option, direction) {
    const index = this.columnsManager.findIndex((el) => el.property === option.value);
    const existsDetail = this.columnsManager.some(function(el) {
      return el.property === "detail";
    });
    const valueSubtraction = existsDetail ? 2 : 1;
    if (index === 0 && direction === "up") {
      return true;
    }
    if (index !== 0 && direction === "up" && this.columnsManager[index - 1].fixed) {
      return true;
    }
    if (index === this.columnsManager.length - valueSubtraction && direction === "down") {
      return true;
    }
    return false;
  }
  emitFixed(option) {
    if (option.visible) {
      const index = this.columnsManager.findIndex((el) => el.property === option.value);
      if (this.columnsManager[index].fixed === null || this.columnsManager[index].fixed === void 0 || this.columnsManager[index].fixed === false) {
        this.columnsManager[index].fixed = true;
        option.fixed = true;
      } else {
        this.columnsManager[index].fixed = false;
        option.fixed = false;
      }
      this.changeColumnFixed.emit(option);
    }
  }
  isFixed(option) {
    const index = this.columnsManager.findIndex((el) => el.property === option.value);
    if (this.columnsManager[index].fixed === true) {
      return true;
    }
    return false;
  }
  existedFixedItem() {
    return this.columnsManager.some((option) => option["fixed"] === true);
  }
  checksIfHasFiveFixed(option) {
    const isMoreThanFive = this.columnsManager.filter((item) => item.fixed === true).length > 4;
    const isNotFixed = !this.isFixed(option);
    return isMoreThanFive && isNotFixed;
  }
  clickSwitch(option) {
    this.checkOption(option);
  }
  static ɵfac = function PoTableListManagerComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoTableListManagerComponent)(ɵɵdirectiveInject(PoLanguageService), ɵɵdirectiveInject(ChangeDetectorRef));
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoTableListManagerComponent,
    selectors: [["po-table-list-manager"]],
    inputs: {
      columnsManager: [0, "p-columns-manager", "columnsManager"],
      componentsSize: [0, "p-components-size", "componentsSize"],
      hideActionFixedColumns: [2, "p-hide-action-fixed-columns", "hideActionFixedColumns", convertToBoolean]
    },
    outputs: {
      changePosition: "p-change-position",
      changeColumnFixed: "p-change-fixed"
    },
    standalone: false,
    features: [ɵɵProvidersFeature([{
      provide: NG_VALUE_ACCESSOR,
      useExisting: forwardRef(() => _PoTableListManagerComponent),
      multi: true
    }]), ɵɵInheritDefinitionFeature],
    decls: 8,
    vars: 2,
    consts: [["listItemsManagerDefault", ""], [1, "po-table-list-manager-section"], [1, "po-table-list-manager-title-group-items"], [1, "po-table-list-manager-container", 3, "po-checkbox-group-item-disabled"], [1, "po-table-list-manager-container"], [4, "ngTemplateOutlet", "ngTemplateOutletContext"], [1, "po-table-list-manager-item"], [1, "po-table-list-manager-item-switch"], ["name", "switch", 3, "p-change", "keydown", "p-disabled", "p-value", "p-hide-label-status", "p-size"], [1, "po-table-list-manager-item-text"], [1, "po-table-list-manager-item-buttons"], [3, "p-icon", "p-disabled", "p-size"], ["p-tooltip-position", "left", "p-icon", "ICON_ARROW_UP", 3, "p-click", "p-tooltip", "p-disabled", "p-size"], ["p-tooltip-position", "top", "p-icon", "ICON_ARROW_DOWN", 3, "p-click", "p-tooltip", "p-disabled", "p-size"], [3, "p-click", "p-icon", "p-disabled", "p-size"]],
    template: function PoTableListManagerComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵconditionalCreate(0, PoTableListManagerComponent_Conditional_0_Template, 5, 1, "section", 1);
        ɵɵelementStart(1, "section", 1)(2, "p", 2);
        ɵɵtext(3);
        ɵɵelementEnd();
        ɵɵrepeaterCreate(4, PoTableListManagerComponent_For_5_Template, 1, 1, null, null, ctx.trackByFn, true);
        ɵɵelementEnd();
        ɵɵtemplate(6, PoTableListManagerComponent_ng_template_6_Template, 9, 14, "ng-template", null, 0, ɵɵtemplateRefExtractor);
      }
      if (rf & 2) {
        ɵɵconditional(ctx.existedFixedItem() ? 0 : -1);
        ɵɵadvance(3);
        ɵɵtextInterpolate(ctx.literals.otherColumns);
        ɵɵadvance();
        ɵɵrepeater(ctx.checkboxGroupOptionsView);
      }
    },
    dependencies: [NgTemplateOutlet, PoButtonComponent, PoTooltipDirective, PoSwitchComponent],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoTableListManagerComponent, [{
    type: Component,
    args: [{
      selector: "po-table-list-manager",
      providers: [{
        provide: NG_VALUE_ACCESSOR,
        useExisting: forwardRef(() => PoTableListManagerComponent),
        multi: true
      }],
      standalone: false,
      template: `@if (existedFixedItem()) {
  <section class="po-table-list-manager-section">
    <p class="po-table-list-manager-title-group-items">{{ literals.fixedColumns }}</p>
    @for (option of checkboxGroupOptionsView; track trackByFn($index)) {
      @if (isFixed(option)) {
        <li
          class="po-table-list-manager-container"
          [class.po-checkbox-group-item-disabled]="option.disabled || disabled"
        >
          <ng-container *ngTemplateOutlet="listItemsManagerDefault; context: { $implicit: option }"></ng-container>
        </li>
      }
    }
  </section>
}

<section class="po-table-list-manager-section">
  <p class="po-table-list-manager-title-group-items">{{ literals.otherColumns }}</p>
  @for (option of checkboxGroupOptionsView; track trackByFn($index)) {
    @if (!isFixed(option)) {
      <li class="po-table-list-manager-container" [class.po-checkbox-group-item-disabled]="option.disabled || disabled">
        @if (!isFixed(option)) {
          <ng-container *ngTemplateOutlet="listItemsManagerDefault; context: { $implicit: option }"></ng-container>
        }
      </li>
    }
  }
</section>

<ng-template #listItemsManagerDefault let-option>
  <div class="po-table-list-manager-item">
    <div class="po-table-list-manager-item-switch">
      <po-switch
        name="switch"
        (p-change)="clickSwitch(option)"
        (keydown)="onKeyDown($event, option)"
        [p-disabled]="option.disabled || disabled || isFixed(option)"
        [p-value]="checkedOptions[option.value]"
        [p-hide-label-status]="true"
        [p-size]="componentsSize"
      >
      </po-switch>
      <span
        class="po-table-list-manager-item-text"
        [class.po-table-list-manager-item-text-aa]="componentsSize === 'small'"
      >
        {{ option.label }}
      </span>
    </div>

    <div class="po-table-list-manager-item-buttons">
      @if (!hideActionFixedColumns) {
        <po-button
          [p-icon]="isFixed(option) ? 'ICON_PUSH_PIN_SLASH' : 'ICON_PUSH_PIN'"
          (p-click)="emitFixed(option)"
          [p-disabled]="!option.visible || checksIfHasFiveFixed(option)"
          [p-size]="componentsSize"
        >
        </po-button>
      }

      <po-button
        [p-tooltip]="literals.up"
        p-tooltip-position="left"
        p-icon="ICON_ARROW_UP"
        [p-disabled]="verifyArrowDisabled(option, 'up') || isFixed(option)"
        [p-size]="componentsSize"
        (p-click)="emitChangePosition(option, 'up')"
      >
      </po-button>

      <po-button
        [p-tooltip]="literals.down"
        p-tooltip-position="top"
        p-icon="ICON_ARROW_DOWN"
        [p-disabled]="verifyArrowDisabled(option, 'down') || isFixed(option)"
        [p-size]="componentsSize"
        (p-click)="emitChangePosition(option, 'down')"
      >
      </po-button>
    </div>
  </div>
</ng-template>
`
    }]
  }], () => [{
    type: PoLanguageService
  }, {
    type: ChangeDetectorRef
  }], {
    changePosition: [{
      type: Output,
      args: ["p-change-position"]
    }],
    changeColumnFixed: [{
      type: Output,
      args: ["p-change-fixed"]
    }],
    columnsManager: [{
      type: Input,
      args: ["p-columns-manager"]
    }],
    componentsSize: [{
      type: Input,
      args: ["p-components-size"]
    }],
    hideActionFixedColumns: [{
      type: Input,
      args: [{
        alias: "p-hide-action-fixed-columns",
        transform: convertToBoolean
      }]
    }]
  });
})();
var PoTableColumnManagerMaxColumnsDefault = 99999;
var poTableColumnManagerLiteralsDefault = {
  en: {
    columnsManager: "Columns manager",
    restoreDefault: "Restore default"
  },
  es: {
    columnsManager: "Gerente de columna",
    restoreDefault: "Restaurar por defecto"
  },
  pt: {
    columnsManager: "Gerenciador de colunas",
    restoreDefault: "Restaurar padrão"
  },
  ru: {
    columnsManager: "менеджер колонок",
    restoreDefault: "сброс настроек"
  }
};
var PoTableColumnManagerComponent = class _PoTableColumnManagerComponent {
  renderer;
  popover;
  pageSlideColumnsManager;
  columns = [];
  target;
  colunsDefault;
  lastVisibleColumnsSelected = [];
  visibleColumnsChange = new EventEmitter();
  // Evento disparado ao fechar o popover do gerenciador de colunas após alterar as colunas visíveis.
  // O po-table envia como parâmetro um array de string com as colunas visíveis atualizadas. Por exemplo: ["idCard", "name", "hireStatus", "age"].
  changeVisibleColumns = new EventEmitter();
  // Evento disparado ao alterar o estado de fixação de uma coluna no gerenciador de colunas.
  // O po-table envia como parâmetro um array de string com as propriedades das colunas fixas. Por exemplo: ["name", "age"].
  changeFixedColumns = output({
    alias: "p-change-fixed-columns"
  });
  initialColumns = new EventEmitter();
  hideActionFixedColumns = false;
  literals;
  columnsOptions = [];
  visibleColumns = [];
  columnUpdate;
  _maxColumns = PoTableColumnManagerMaxColumnsDefault;
  defaultColumns = [];
  resizeListener;
  restoreDefaultEvent;
  lastEmittedValue;
  minColumns = 1;
  _componentsSize = void 0;
  /**
   * @optional
   *
   * @description
   *
   * Define o tamanho dos componentes de formulário no table:
   * - `small`: aplica a medida small de cada componente (disponível apenas para acessibilidade AA).
   * - `medium`: aplica a medida medium de cada componente.
   *
   * > Caso a acessibilidade AA não esteja configurada, o tamanho `medium` será mantido.
   * Para mais detalhes, consulte a documentação do [po-theme](https://po-ui.io/documentation/po-theme).
   *
   * @default `medium`
   */
  set componentsSize(value) {
    this._componentsSize = validateSizeFn(value, PoFieldSize);
  }
  get componentsSize() {
    return this._componentsSize ?? getDefaultSizeFn(PoFieldSize);
  }
  set maxColumns(value) {
    this._maxColumns = convertToInt(value, PoTableColumnManagerMaxColumnsDefault);
  }
  get maxColumns() {
    return this._maxColumns;
  }
  constructor(renderer, languageService) {
    this.renderer = renderer;
    const language = languageService.getShortLanguage();
    this.literals = __spreadValues(__spreadValues({}, poTableColumnManagerLiteralsDefault[poLocaleDefault]), poTableColumnManagerLiteralsDefault[language]);
  }
  ngOnChanges(changes) {
    const {
      columns,
      maxColumns,
      target,
      lastVisibleColumnsSelected
    } = changes;
    if (target && target.firstChange) {
      this.initializeListeners();
    }
    if (columns) {
      this.onChangeColumns(columns);
    }
    if (maxColumns) {
      this.updateValues(this.columns);
    }
    if (lastVisibleColumnsSelected?.currentValue) {
      this.pageSlideColumnsManager.open();
    }
  }
  ngOnDestroy() {
    this.removeListeners();
  }
  // aqui chegam os eventos do checkbox e do close do popover que também é disparado no resize
  checkChanges(event = [], emit = false) {
    this.verifyToEmitChange(event);
    if (emit) {
      this.verifyToEmitVisibleColumns();
    }
  }
  restore() {
    this.restoreDefaultEvent = true;
    this.defaultColumns = this.removePropertyFixed(this.defaultColumns);
    const defaultColumns = this.getVisibleColumns(this.defaultColumns);
    this.initialColumns.emit(this.getVisibleColumns(this.colunsDefault));
    this.checkChanges(defaultColumns, this.restoreDefaultEvent);
    if (!this.hideActionFixedColumns) {
      this.changeFixedColumns.emit([]);
    }
  }
  changePosition({
    option,
    direction
  }) {
    const indexColumn = this.columns.findIndex((el) => el.property === option.value);
    const newColumn = [...this.columns];
    this.changePositionColumn(newColumn, indexColumn, direction);
    this.columns = newColumn;
    this.visibleColumnsChange.emit(this.columns);
  }
  emitColumnFixed(option) {
    const newColumn = [...this.columns];
    if (option) {
      newColumn.forEach((itemColumn) => {
        if (itemColumn.property === option.value) {
          itemColumn.fixed = option.fixed;
        }
      });
      const amountOfFixed = newColumn.filter((itemFixed) => itemFixed.fixed === true).length;
      const indexColumn = newColumn.findIndex((el) => el.property === option.value);
      const item = newColumn.splice(indexColumn, 1)[0];
      if (option.fixed) {
        newColumn.splice(amountOfFixed - 1, 0, item);
      }
      if (option.fixed === false) {
        newColumn.splice(amountOfFixed, 0, item);
      }
      this.columns = [...newColumn];
    }
    this.visibleColumnsChange.emit(newColumn);
    if (!this.hideActionFixedColumns) {
      const fixedColumns = newColumn.filter((col) => col.fixed === true).map((col) => col.property);
      this.changeFixedColumns.emit(fixedColumns);
    }
  }
  changePositionColumn(array, index, direction) {
    if (direction === "up") {
      array.splice(index, 0, array.splice(index - 1, 1)[0]);
    }
    if (direction === "down") {
      array.splice(index, 0, array.splice(index + 1, 1)[0]);
    }
  }
  verifyToEmitChange(event) {
    const newColumns = [...event];
    if (newColumns.length >= 1 && this.allowsChangeVisibleColumns()) {
      this.emitChangesToSelectedColumns(newColumns);
    }
    if (newColumns.length === 1) {
      const columnsOptions = this.mapTableColumnsToCheckboxOptions(this.columnUpdate);
      this.columnsOptions = this.disabledLastColumn(columnsOptions);
    }
  }
  emitChangesToSelectedColumns(newColumns) {
    this.visibleColumns = [...newColumns];
    const visibleTableColumns = this.getVisibleTableColumns(this.visibleColumns);
    this.visibleColumnsChange.emit(visibleTableColumns);
  }
  allowsChangeVisibleColumns() {
    const visibleTableColumns = this.getVisibleTableColumns(this.visibleColumns);
    return this.stringify(visibleTableColumns) !== this.stringify(this.columns);
  }
  verifyToEmitVisibleColumns() {
    if (this.restoreDefaultEvent) {
      this.verifyRestoreValues();
    } else {
      this.verifyOnClose();
    }
  }
  verifyRestoreValues() {
    const defaultColumns = [...this.defaultColumns];
    const defaultVisibleColumns = this.getVisibleColumns(defaultColumns);
    if (this.allowsChangeSelectedColumns(defaultVisibleColumns)) {
      this.visibleColumnsChange.emit(this.defaultColumns);
    }
    this.restoreDefaultEvent = false;
  }
  allowsChangeSelectedColumns(defaultVisibleColumns) {
    const visibleColumns = this.getVisibleColumns(this.columns);
    return !this.isEqualArrays(defaultVisibleColumns, visibleColumns);
  }
  verifyOnClose() {
    if (this.allowsEmission()) {
      this.emitVisibleColumns();
    }
  }
  emitVisibleColumns() {
    this.lastEmittedValue = [...this.visibleColumns];
    this.changeVisibleColumns.emit(this.visibleColumns);
  }
  allowsEmission() {
    const updatedVisibleColumns = this.visibleColumns ? [...this.visibleColumns] : [];
    const lastEmittedValue = this.lastEmittedValue ? [...this.lastEmittedValue] : [];
    const lastVisibleColumnsSelected = this.lastVisibleColumnsSelected ? [...this.lastVisibleColumnsSelected] : [];
    const lastVisibleColumns = this.getVisibleColumns(lastVisibleColumnsSelected);
    return this.isUpdate(updatedVisibleColumns, lastEmittedValue) || this.isFirstTime(updatedVisibleColumns, lastVisibleColumns);
  }
  isFirstTime(updatedVisibleColumns, lastVisibleColumns) {
    return !this.lastEmittedValue && !this.isEqualArrays(updatedVisibleColumns, lastVisibleColumns);
  }
  isUpdate(updatedVisibleColumns, lastEmittedValue) {
    return this.lastEmittedValue && !this.isEqualArrays(updatedVisibleColumns, lastEmittedValue);
  }
  isEqualArrays(first, second) {
    const one = first ? [...first] : [];
    const two = second ? [...second] : [];
    const firstSort = one.slice();
    const secondSort = two.slice();
    const firstString = JSON.stringify(firstSort);
    const secondString = JSON.stringify(secondSort);
    return firstString === secondString;
  }
  // desabilitará as colunas, que não estiverem selecionadas, após exeder o numero maximo de colunas.
  disableColumnsOptions(columns = []) {
    return columns.map((column) => __spreadProps(__spreadValues({}, column), {
      disabled: this.isDisableColumn(column.value)
    }));
  }
  getColumnTitleLabel(column) {
    return column.label || PoUtils.capitalizeFirstLetter(column.property);
  }
  /** Retorna um Array de column.property das colunas que são visiveis. */
  getVisibleColumns(columns) {
    let visibleColumns = [];
    columns.forEach((column) => {
      if (this.isVisibleColumn(column, visibleColumns)) {
        visibleColumns = [...visibleColumns, column.property];
      }
    });
    return visibleColumns;
  }
  isVisibleColumn(column, visibleColumns) {
    return column.visible !== false && visibleColumns.length < this.maxColumns && column.type !== "detail";
  }
  /** Retorna um Array PoTableColumn a partir das colunas visiveis no gerenciador de colunas. */
  getVisibleTableColumns(visibleColumns) {
    const columns = this.columns ? [...this.columns] : [];
    return columns.map((column) => __spreadProps(__spreadValues({}, column), {
      visible: visibleColumns.includes(column.property) || column.type === "detail"
    }));
  }
  initializeListeners() {
    this.resizeListener = this.renderer.listen("window", "resize", () => {
      if (this.popover && !this.popover.isHidden) {
        this.popover.close();
      }
    });
  }
  isDisableColumn(property) {
    return this.visibleColumns.length >= this.maxColumns ? !this.visibleColumns.includes(property) : false;
  }
  mapTableColumnsToCheckboxOptions(columns = []) {
    const tableColumns = [...columns];
    const columnsOptions = [];
    tableColumns.forEach((column) => {
      if (column.type !== "detail") {
        columnsOptions.push({
          value: column.property,
          label: this.getColumnTitleLabel(column),
          disabled: this.isDisableColumn(column.property),
          visible: column.visible
        });
      }
    });
    return columnsOptions;
  }
  disabledLastColumn(columns) {
    return columns.map((column) => __spreadProps(__spreadValues({}, column), {
      disabled: column.type !== "detail" && column.visible ? true : false
    }));
  }
  onChangeColumns(columns) {
    const {
      currentValue = [],
      previousValue = []
    } = columns;
    this.columnUpdate = columns.currentValue;
    if (!this.lastVisibleColumnsSelected && this.stringify(this.defaultColumns) !== this.stringify(currentValue)) {
      this.defaultColumns = [...currentValue];
    }
    if (this.stringify(previousValue) !== this.stringify(currentValue)) {
      this.updateValues(currentValue);
    }
  }
  updateValues(currentValue) {
    const visibleColumns = this.getVisibleColumns(currentValue);
    this.visibleColumns = [...visibleColumns];
    const columnsOptions = this.mapTableColumnsToCheckboxOptions(currentValue);
    this.columnsOptions = this.disableColumnsOptions(columnsOptions);
    this.checkChanges(visibleColumns, false);
  }
  removeListeners() {
    if (this.resizeListener) {
      this.resizeListener();
    }
  }
  stringify(columns) {
    return JSON.stringify(columns, (key2, value) => {
      if (key2 !== "icon" && key2 !== "searchService") {
        return value;
      }
    });
  }
  removePropertyFixed(arr) {
    return arr.map((obj) => {
      if (obj.hasOwnProperty("fixed")) {
        obj.fixed = false;
      }
      return obj;
    });
  }
  static ɵfac = function PoTableColumnManagerComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoTableColumnManagerComponent)(ɵɵdirectiveInject(Renderer2), ɵɵdirectiveInject(PoLanguageService));
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoTableColumnManagerComponent,
    selectors: [["po-table-column-manager"]],
    viewQuery: function PoTableColumnManagerComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(PoPopoverComponent, 5)(_c90, 5);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.popover = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.pageSlideColumnsManager = _t.first);
      }
    },
    inputs: {
      columns: [0, "p-columns", "columns"],
      target: [0, "p-target", "target"],
      colunsDefault: [0, "p-columns-default", "colunsDefault"],
      lastVisibleColumnsSelected: [0, "p-last-visible-columns-selected", "lastVisibleColumnsSelected"],
      hideActionFixedColumns: [2, "p-hide-action-fixed-columns", "hideActionFixedColumns", convertToBoolean],
      componentsSize: [0, "p-components-size", "componentsSize"],
      maxColumns: [0, "p-max-columns", "maxColumns"]
    },
    outputs: {
      visibleColumnsChange: "p-visible-columns-change",
      changeVisibleColumns: "p-change-visible-columns",
      changeFixedColumns: "p-change-fixed-columns",
      initialColumns: "p-initial-columns"
    },
    standalone: false,
    features: [ɵɵNgOnChangesFeature],
    decls: 7,
    vars: 10,
    consts: [["pageSlideColumnsManager", ""], ["p-size", "auto", 3, "p-close", "p-title", "p-click-out", "p-components-size"], [1, "po-table-column-manager-body"], ["name", "visibleColumns", "p-columns", "1", 3, "ngModelChange", "p-change", "p-change-position", "p-change-fixed", "ngModel", "p-columns-manager", "p-components-size", "p-options", "p-hide-action-fixed-columns"], [1, "po-table-column-manager-footer"], ["p-kind", "tertiary", 1, "po-table-column-manager-footer-restore", 3, "p-click", "p-label", "p-size"]],
    template: function PoTableColumnManagerComponent_Template(rf, ctx) {
      if (rf & 1) {
        const _r1 = ɵɵgetCurrentView();
        ɵɵelementStart(0, "po-page-slide", 1, 0);
        ɵɵlistener("p-close", function PoTableColumnManagerComponent_Template_po_page_slide_p_close_0_listener() {
          return ctx.checkChanges([], true);
        });
        ɵɵelementStart(2, "div")(3, "div", 2)(4, "po-table-list-manager", 3);
        ɵɵtwoWayListener("ngModelChange", function PoTableColumnManagerComponent_Template_po_table_list_manager_ngModelChange_4_listener($event) {
          ɵɵrestoreView(_r1);
          ɵɵtwoWayBindingSet(ctx.visibleColumns, $event) || (ctx.visibleColumns = $event);
          return ɵɵresetView($event);
        });
        ɵɵlistener("p-change", function PoTableColumnManagerComponent_Template_po_table_list_manager_p_change_4_listener($event) {
          return ctx.checkChanges($event, false);
        })("p-change-position", function PoTableColumnManagerComponent_Template_po_table_list_manager_p_change_position_4_listener($event) {
          return ctx.changePosition($event);
        })("p-change-fixed", function PoTableColumnManagerComponent_Template_po_table_list_manager_p_change_fixed_4_listener($event) {
          return ctx.emitColumnFixed($event);
        });
        ɵɵelementEnd()();
        ɵɵelementStart(5, "div", 4)(6, "po-button", 5);
        ɵɵlistener("p-click", function PoTableColumnManagerComponent_Template_po_button_p_click_6_listener() {
          return ctx.restore();
        });
        ɵɵelementEnd()()()();
      }
      if (rf & 2) {
        ɵɵproperty("p-title", ctx.literals.columnsManager)("p-click-out", true)("p-components-size", ctx.componentsSize);
        ɵɵadvance(4);
        ɵɵtwoWayProperty("ngModel", ctx.visibleColumns);
        ɵɵproperty("p-columns-manager", ctx.columns)("p-components-size", ctx.componentsSize)("p-options", ctx.columnsOptions)("p-hide-action-fixed-columns", ctx.hideActionFixedColumns);
        ɵɵadvance(2);
        ɵɵproperty("p-label", ctx.literals.restoreDefault)("p-size", ctx.componentsSize);
      }
    },
    dependencies: [NgControlStatus, NgModel, PoButtonComponent, PoPageSlideComponent, PoTableListManagerComponent],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoTableColumnManagerComponent, [{
    type: Component,
    args: [{
      selector: "po-table-column-manager",
      standalone: false,
      template: '<po-page-slide\n  #pageSlideColumnsManager\n  p-size="auto"\n  [p-title]="literals.columnsManager"\n  [p-click-out]="true"\n  [p-components-size]="componentsSize"\n  (p-close)="checkChanges([], true)"\n>\n  <div>\n    <div class="po-table-column-manager-body">\n      <po-table-list-manager\n        name="visibleColumns"\n        [(ngModel)]="visibleColumns"\n        p-columns="1"\n        [p-columns-manager]="columns"\n        [p-components-size]="componentsSize"\n        [p-options]="columnsOptions"\n        [p-hide-action-fixed-columns]="hideActionFixedColumns"\n        (p-change)="checkChanges($event, false)"\n        (p-change-position)="changePosition($event)"\n        (p-change-fixed)="emitColumnFixed($event)"\n      >\n      </po-table-list-manager>\n    </div>\n\n    <div class="po-table-column-manager-footer">\n      <po-button\n        class="po-table-column-manager-footer-restore"\n        p-kind="tertiary"\n        [p-label]="literals.restoreDefault"\n        [p-size]="componentsSize"\n        (p-click)="restore()"\n      >\n      </po-button>\n    </div>\n  </div>\n</po-page-slide>\n'
    }]
  }], () => [{
    type: Renderer2
  }, {
    type: PoLanguageService
  }], {
    popover: [{
      type: ViewChild,
      args: [PoPopoverComponent]
    }],
    pageSlideColumnsManager: [{
      type: ViewChild,
      args: ["pageSlideColumnsManager"]
    }],
    columns: [{
      type: Input,
      args: ["p-columns"]
    }],
    target: [{
      type: Input,
      args: ["p-target"]
    }],
    colunsDefault: [{
      type: Input,
      args: ["p-columns-default"]
    }],
    lastVisibleColumnsSelected: [{
      type: Input,
      args: ["p-last-visible-columns-selected"]
    }],
    visibleColumnsChange: [{
      type: Output,
      args: ["p-visible-columns-change"]
    }],
    changeVisibleColumns: [{
      type: Output,
      args: ["p-change-visible-columns"]
    }],
    changeFixedColumns: [{
      type: Output,
      args: ["p-change-fixed-columns"]
    }],
    initialColumns: [{
      type: Output,
      args: ["p-initial-columns"]
    }],
    hideActionFixedColumns: [{
      type: Input,
      args: [{
        alias: "p-hide-action-fixed-columns",
        transform: convertToBoolean
      }]
    }],
    componentsSize: [{
      type: Input,
      args: ["p-components-size"]
    }],
    maxColumns: [{
      type: Input,
      args: ["p-max-columns"]
    }]
  });
})();
var PoSearchAiResponseType;
(function(PoSearchAiResponseType2) {
  PoSearchAiResponseType2["filter"] = "filter";
  PoSearchAiResponseType2["chat"] = "chat";
  PoSearchAiResponseType2["custom"] = "custom";
})(PoSearchAiResponseType || (PoSearchAiResponseType = {}));
var PoInputBaseComponent = class _PoInputBaseComponent {
  cd;
  fieldContainer;
  helperEl;
  // Propriedade interna que define se o ícone de ajuda adicional terá cursor clicável (evento) ou padrão (tooltip).
  additionalHelpEventTrigger;
  /**
   *
   * @deprecated v23.x.x use `p-helper`
   *
   * @optional
   *
   * @description
   * Exibe um ícone de ajuda adicional, com o texto desta propriedade sendo passado para o popover do componente `po-helper`.
   * **Como boa prática, indica-se utilizar um texto com até 140 caracteres.**
   * > Requer um recuo mínimo de 8px se o componente estiver próximo à lateral da tela.
   *
   * > Essa propriedade está **depreciada** e será removida na versão `23.x.x`. Recomendamos utilizar a propriedade `p-helper` que oferece mais recursos e flexibilidade.
   */
  additionalHelpTooltip;
  /**
   * @optional
   *
   * @description
   *
   * Define que o popover (`p-helper` e/ou `p-error-limit`) será incluído no body da página e não
   * dentro do componente. Essa opção pode ser necessária em cenários com containers que possuem scroll ou overflow
   * escondido, garantindo o posicionamento correto do tooltip próximo ao elemento.
   *
   * > Quando utilizado com `p-helper`, leitores de tela como o NVDA podem não ler o conteúdo do popover.
   *
   * @default `false`
   */
  appendBox = false;
  /**
   * @optional
   *
   * @description
   *
   * Aplica foco no elemento ao ser iniciado.
   *
   * > Caso mais de um elemento seja configurado com essa propriedade, apenas o último elemento declarado com ela terá o foco.
   *
   * @default `false`
   */
  autoFocus = false;
  /**
   * @optional
   *
   * @description
   * Define se o título do campo será exibido de forma compacta.
   *
   * Quando habilitado (`true`), o modo compacto afeta o conjunto composto por:
   * - `po-label`
   * - `p-requirement (showRequired)`
   * - `po-helper`
   *
   * Ou seja, todos os elementos relacionados ao título do campo
   * (rótulo, indicador de obrigatoriedade e componente auxiliar) passam
   * a seguir o comportamento de layout compacto.
   *
   * Também é possível definir esse comportamento de forma global,
   * uma única vez, na folha de estilo geral da aplicação, por meio
   * da customização dos tokens CSS:
   *
   * - `--field-container-title-justify`
   * - `--field-container-title-flex`
   *
   * Exemplo:
   *
   * ```
   * :root {
   *   --field-container-title-justify: flex-start;
   *   --field-container-title-flex: 0 1 auto;
   * }
   * ```
   *
   * Dessa forma, o layout compacto passa a ser o padrão da aplicação,
   * sem a necessidade de definir a propriedade individualmente em cada campo.
   *
   * @default `false`
   */
  compactLabel = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "compactLabel"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-compact-label",
    transform: convertToBoolean
  }));
  /**
   * @optional
   *
   * @description
   *
   * Define o ícone que será exibido no início do campo.
   *
   * É possível usar qualquer um dos ícones da [Biblioteca de ícones](https://po-ui.io/icons). conforme exemplo abaixo:
   * ```
   * <po-input p-icon="an an-user" p-label="PO input"></po-input>
   * ```
   * Também é possível utilizar outras fontes de ícones, por exemplo a biblioteca *Font Awesome*, da seguinte forma:
   * ```
   * <po-input p-icon="fa fa-podcast" p-label="PO input"></po-input>
   * ```
   * Outra opção seria a customização do ícone através do `TemplateRef`, conforme exemplo abaixo:
   * ```
   * <po-input [p-icon]="template" p-label="input template ionic"></po-input>
   *
   * <ng-template #template>
   *  <ion-icon style="font-size: inherit" name="heart"></ion-icon>
   * </ng-template>
   * ```
   * > Para o ícone enquadrar corretamente, deve-se utilizar `font-size: inherit` caso o ícone utilizado não aplique-o.
   */
  icon;
  /**
   * @optional
   *
   * @description
   *
   * Sempre emite as alterações do model mesmo quando o valor atual for igual ao valor anterior.
   *
   * @default `false`
   */
  emitAllChanges = false;
  /** Rótulo do campo. */
  label;
  /** Texto de apoio do campo. */
  help;
  /** Nome e identificador do campo. */
  name;
  /**
   * @optional
   *
   * @description
   *
   * Realiza alguma validação customizada assíncrona no componente.
   * Aconselhamos a utilização dessa propriedade somente em componentes que não estejam
   * utilizando `Reactive Forms`. Em formulários reativos, pode-se utilizar o próprio `asyncValidators`.
   */
  errorAsyncProperties;
  /**
   * @description
   *
   * Mensagem que será apresentada quando o `pattern` ou a máscara não for satisfeita.
   *
   * > Por padrão, esta mensagem não é apresentada quando o campo estiver vazio, mesmo que ele seja requerido.
   * Para exibir a mensagem com o campo vazio, utilize a propriedade `p-required-field-error-message` em conjunto.
   */
  errorPattern = "";
  /**
   * @optional
   *
   * @description
   *
   * Limita a exibição da mensagem de erro a duas linhas e exibe um tooltip com o texto completo.
   *
   * > Caso essa propriedade seja definida como `true`, a mensagem de erro será limitada a duas linhas
   * e um tooltip será exibido ao passar o mouse sobre a mensagem para mostrar o conteúdo completo.
   *
   * @default `false`
   */
  errorLimit = false;
  /**
   * @optional
   *
   * @description
   *
   * Define se a indicação de campo opcional será exibida.
   *
   * > Não será exibida a indicação se:
   * - O campo conter `p-required`;
   * - Não possuir `p-help` e/ou `p-label`.
   *
   * @default `false`
   */
  optional;
  /**
   * @optional
   *
   * @description
   *
   * Exibe a mensagem setada na propriedade `p-error-pattern` se o campo estiver vazio e for requerido.
   *
   * > Necessário que a propriedade `p-required` esteja habilitada.
   *
   * @default `false`
   */
  showErrorMessageRequired = false;
  /**
   * @description
   *
   * Converte o conteúdo do campo em maiúsulo automaticamente.
   *
   */
  upperCase = false;
  _maskNoLengthValidation = false;
  /**
   * @description
   *
   * Controla como o componente aplica as validações de comprimento mínimo (`minLength`) e máximo (`maxLength`) quando há uma máscara (`p-mask`) definida.
   *
   * - Quando `true`, apenas os caracteres alfanuméricos serão contabilizados para a validação dos comprimentos.
   * - Quando `false`, todos os caracteres, incluindo os especiais da máscara, serão considerados na validação.
   *
   * > Esta propriedade é ignorada quando utilizada em conjunto com `p-mask-format-model`.
   *
   * Exemplo:
   * ```
   * <po-input
   *   p-mask="999-999"
   *   p-maxlength="6"
   *   p-minlength="4"
   *   p-mask-no-length-validation="true"
   * ></po-input>
   * ```
   * - Entrada: `123-456` → Validação será aplicada somente aos números, ignorando o caractere especial `-`.
   *
   * @default `false`
   */
  set maskNoLengthValidation(value) {
    this._maskNoLengthValidation = value;
    this.validateModel();
  }
  get maskNoLengthValidation() {
    return this._maskNoLengthValidation;
  }
  /**
   *
   * @deprecated v23.x.x use `p-helper`
   *
   * @optional
   *
   * @description
   * Evento disparado ao clicar no ícone de ajuda adicional.
   *
   * > Essa propriedade está **depreciada** e será removida na versão `23.x.x`. Recomendamos utilizar a propriedade `p-helper` que oferece mais recursos e flexibilidade.
   */
  additionalHelp = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Evento disparado ao sair do campo.
   */
  blur = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Evento disparado ao entrar do campo.
   */
  enter = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Evento disparado ao alterar valor e deixar o campo.
   */
  change = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Evento disparado sempre que o valor do model é alterado, seja por interação do usuário
   * ou por atualização programática (ex: `setValue`, `patchValue`, carregamento assíncrono).
   *
   * Diferentemente do `p-change`, que é disparado apenas por interação do usuário,
   * o `p-change-model` cobre todos os cenários de alteração de valor.
   *
   * Não emite quando o novo valor é idêntico ao anterior (deduplicação automática).
   */
  changeModel = new EventEmitter();
  /**
   * @optional
   *
   * @description
   * Evento disparado quando uma tecla é pressionada enquanto o foco está no componente.
   * Retorna um objeto `KeyboardEvent` com informações sobre a tecla.
   */
  keydown = new EventEmitter();
  displayAdditionalHelp = false;
  type;
  onChangePropagate = null;
  objMask;
  modelLastUpdate;
  isInvalid;
  hasValidatorRequired = false;
  subscription = new Subscription();
  onTouched = null;
  passedWriteValue = false;
  validatorChange;
  _loading = false;
  _maxlength;
  _minlength;
  _noAutocomplete = false;
  _placeholder = "";
  _size = void 0;
  _initialSize = void 0;
  /**
   * @optional
   *
   * @description
   *
   * Define a propriedade nativa `autocomplete` do campo como `off`.
   *
   * > No componente `po-password` será definido como `new-password`.
   *
   * Nos componentes `po-password` e `po-login` o valor padrão será `true`.
   *
   * @default `false`
   */
  set noAutocomplete(value) {
    this._noAutocomplete = value;
  }
  get noAutocomplete() {
    return this._noAutocomplete;
  }
  /**
   * @optional
   *
   * @description
   *
   * Mensagem que aparecerá enquanto o campo não estiver preenchido.
   *
   * @default ''
   */
  set placeholder(value) {
    this._placeholder = value || "";
  }
  get placeholder() {
    return this._placeholder;
  }
  /**
   * @description
   *
   * Se verdadeiro, desabilita o campo.
   *
   * @default `false`
   */
  // eslint-disable-next-line @typescript-eslint/member-ordering
  disabled = false;
  set setDisabled(disabled) {
    this.disabled = disabled;
    this.validateModel();
  }
  /**
   * @optional
   *
   * @description
   * Exibe um ícone de carregamento no lado direito do campo para sinalizar que uma operação está em andamento.
   *
   * @default `false`
   */
  set loading(value) {
    this._loading = value;
    this.cd?.markForCheck();
  }
  get loading() {
    return this._loading;
  }
  get isDisabled() {
    return this.disabled || this.loading;
  }
  /** Indica que o campo será somente leitura. */
  // eslint-disable-next-line @typescript-eslint/member-ordering
  readonly = false;
  set setReadonly(readonly) {
    this.readonly = readonly;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define que o campo será obrigatório.
   * > Esta propriedade é desconsiderada quando o input está desabilitado `(p-disabled)`.
   *
   * @default `false`
   */
  // eslint-disable-next-line @typescript-eslint/member-ordering
  required = false;
  set setRequired(required) {
    this.required = required;
    this.validateModel();
  }
  /**
   * @optional
   *
   * @description
   *
   * Define o tamanho do componente:
   * - `small`: altura do input como 32px (disponível apenas para acessibilidade AA).
   * - `medium`: altura do input como 44px.
   *
   * > Caso a acessibilidade AA não esteja configurada, o tamanho `medium` será mantido.
   * Para mais detalhes, consulte a documentação do [po-theme](https://po-ui.io/documentation/po-theme).
   *
   * @default `medium`
   */
  set size(value) {
    this._initialSize = value;
    this.applySizeBasedOnA11y();
  }
  get size() {
    return this._size ?? getDefaultSizeFn(PoFieldSize);
  }
  /**
   * Define se a indicação de campo obrigatório será exibida.
   *
   * > Não será exibida a indicação se:
   * - Não possuir `p-help` e/ou `p-label`.
   */
  showRequired = false;
  /** Se verdadeiro, o campo receberá um botão para ser limpo. */
  // eslint-disable-next-line @typescript-eslint/member-ordering
  clean = false;
  set setClean(clean) {
    this.clean = clean;
  }
  /**
   * @description
   *
   * Expressão regular para validar o campo.
   * Quando o campo possuir uma máscara `(p-mask)` será automaticamente validado por ela, porém
   * é possível definir um p-pattern para substituir a validação da máscara.
   */
  // eslint-disable-next-line @typescript-eslint/member-ordering
  pattern;
  set setPattern(pattern) {
    this.pattern = pattern;
    this.validateModel();
  }
  /**
   * @optional
   *
   * @description
   *
   * Indica a quantidade máxima de caracteres que o campo aceita.
   */
  set maxlength(value) {
    if (!isNaN(parseInt(value, 10))) {
      this._maxlength = parseInt(value, 10);
      this.validateModel();
    } else if (!value) {
      this._maxlength = void 0;
      this.validateModel();
    }
  }
  get maxlength() {
    return this._maxlength;
  }
  /**
   * @optional
   *
   * @description
   *
   * Indica a quantidade mínima de caracteres que o campo aceita.
   */
  set minlength(value) {
    if (!isNaN(parseInt(value, 10))) {
      this._minlength = parseInt(value, 10);
      this.validateModel();
    } else if (!value) {
      this._minlength = void 0;
      this.validateModel();
    }
  }
  get minlength() {
    return this._minlength;
  }
  /**
   * @description
   *
   * Indica uma máscara para o campo. Exemplos: (+99) (99) 99999?-9999, 99999-999, 999.999.999-99.
   * A máscara gera uma validação automática do campo, podendo esta ser substituída por um REGEX específico
   * através da propriedade p-pattern.
   * O campo será sinalizado e o formulário ficará inválido quando o valor informado estiver fora do padrão definido,
   * mesmo quando desabilitado.
   */
  // eslint-disable-next-line @typescript-eslint/member-ordering
  mask = "";
  set setMask(mask) {
    this.mask = mask;
    this.objMask = new PoMask(this.mask, this.maskFormatModel);
  }
  /**
   * @description
   *
   * Indica se o `model` receberá o valor formatado pela máscara ou apenas o valor puro (sem formatação).
   *
   * @default `false`
   */
  // eslint-disable-next-line @typescript-eslint/member-ordering
  maskFormatModel = false;
  set setMaskFormatModel(maskFormatModel) {
    this.maskFormatModel = maskFormatModel;
    if (this.objMask instanceof PoMask) {
      this.objMask.formatModel = this.maskFormatModel;
      this.validateModel();
    }
  }
  /**
   * @optional
   *
   * @description
   *
   * Define as opções do componente de ajuda (po-helper) que será exibido ao lado do label quando a propriedade `p-label` for definida, ou, ao lado do componente na ausência da propriedade `p-label`.
   * > Para mais informações acesse: https://po-ui.io/documentation/po-helper.
   *
   * > Ao configurar esta propriedade, o antigo ícone de ajuda adicional (`p-additional-help-tooltip` e `p-additional-help`) será ignorado.
   */
  poHelperComponent = input(void 0, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "poHelperComponent"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-helper"
  }));
  /**
   * @optional
   *
   * @description
   *
   * Habilita a quebra automática do texto da propriedade `p-label`. Quando `p-label-text-wrap` for verdadeiro, o texto que excede
   * o espaço disponível é transferido para a próxima linha em pontos apropriados para uma
   * leitura clara.
   *
   * @default `false`
   */
  labelTextWrap = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "labelTextWrap"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-label-text-wrap"
  }));
  constructor(cd) {
    this.cd = cd;
    this.objMask = new PoMask(this.mask, this.maskFormatModel);
  }
  ngOnDestroy() {
    this.subscription.unsubscribe();
  }
  onThemeChange() {
    this.applySizeBasedOnA11y();
  }
  callOnChange(value) {
    this.updateModel(value);
    this.controlChangeModelEmitter(value);
  }
  callUpdateModelWithTimeout(value) {
    setTimeout(() => this.updateModel(value));
  }
  controlChangeModelEmitter(value) {
    if (this.modelLastUpdate !== value || this.emitAllChanges) {
      this.changeModel.emit(value);
      this.modelLastUpdate = value;
    }
  }
  emitAdditionalHelp() {
    if (this.label && this.isAdditionalHelpEventTriggered()) {
      this.additionalHelp.emit();
    }
  }
  getAdditionalHelpTooltip() {
    return this.isAdditionalHelpEventTriggered() ? null : this.additionalHelpTooltip;
  }
  // Função implementada do ControlValueAccessor
  // Usada para interceptar os estados de habilitado via forms api
  setDisabledState(isDisabled) {
    this.disabled = isDisabled;
    this.cd?.markForCheck();
  }
  // Função implementada do ControlValueAccessor
  // Usada para interceptar as mudanças e não atualizar automaticamente o Model
  registerOnChange(func) {
    this.onChangePropagate = func;
  }
  // Função implementada do ControlValueAccessor
  // Usada para interceptar as mudanças e não atualizar automaticamente o Model
  registerOnTouched(func) {
    this.onTouched = func;
  }
  registerOnValidatorChange(fn) {
    this.validatorChange = fn;
  }
  /**
   * Método que exibe `p-helper` ou executa a ação definida em `p-helper{eventOnClick}` ou em `p-additionalHelp`.
   * Para isso, será necessário configurar uma tecla de atalho utilizando o evento `p-keydown`.
   *
   * > Exibe ou oculta o conteúdo do componente `po-helper` quando o componente estiver com foco.
   *
   * ```
   * // Exemplo com p-label e p-helper
   * <po-nome-component
   *  #component
   *  ...
   *  p-label="Label do componente"
   *  [p-helper]="helperOptions"
   *  (p-keydown)="onKeyDown($event, component)"
   * ></po-nome-component>
   * ```
   *
   * ```
   * ...
   * onKeyDown(event: KeyboardEvent, inp: PoNomeDoComponente): void {
   *  if (event.code === 'F9') {
   *    inp.showAdditionalHelp();
   *  }
   * }
   * ```
   */
  showAdditionalHelp() {
    this.displayAdditionalHelp = !this.displayAdditionalHelp;
    const helper = this.poHelperComponent();
    const isHelpEvt = this.isAdditionalHelpEventTriggered();
    if (!this.label && (helper || this.additionalHelpTooltip || isHelpEvt)) {
      if (isHelpEvt) {
        this.additionalHelp.emit();
      }
      if (typeof helper !== "string" && typeof helper?.eventOnClick === "function") {
        helper.eventOnClick();
        return;
      }
      if (this.helperEl?.helperIsVisible()) {
        this.helperEl?.closeHelperPopover();
        return;
      }
      this.helperEl?.openHelperPopover();
      return;
    }
    return this.displayAdditionalHelp;
  }
  updateModel(value) {
    if (this.onChangePropagate) {
      this.onChangePropagate(value);
    }
  }
  validate(c) {
    this.subscription?.unsubscribe();
    if (!this.hasValidatorRequired && this.showErrorMessageRequired && c.hasValidator(Validators.required)) {
      this.hasValidatorRequired = true;
    }
    if (requiredFailed(this.required, this.disabled, this.getScreenValue())) {
      this.isInvalid = true;
      this.cd?.markForCheck();
      return {
        required: {
          valid: false
        }
      };
    }
    if (maxlengpoailed(this.maxlength, this.getScreenValue(), this.maskFormatModel ? false : this.maskNoLengthValidation)) {
      this.isInvalid = true;
      this.cd?.markForCheck();
      return {
        maxlength: {
          valid: false
        }
      };
    }
    if (minlengpoailed(this.minlength, this.getScreenValue(), this.maskFormatModel ? false : this.maskNoLengthValidation)) {
      this.isInvalid = true;
      this.cd?.markForCheck();
      return {
        minlength: {
          valid: false
        }
      };
    }
    if (patternFailed(this.pattern, c.value)) {
      this.isInvalid = true;
      this.cd?.markForCheck();
      this.validatePatternOnWriteValue(c.value);
      return {
        pattern: {
          valid: false
        }
      };
    }
    if (this.errorPattern !== "") {
      this.subscription = c.statusChanges.pipe(switchMap((status) => {
        if (status === "INVALID") {
          this.isInvalid = true;
          this.cd?.markForCheck();
        }
        return [];
      })).subscribe();
    }
    this.isInvalid = false;
    return this.extraValidation(c);
  }
  // Função implementada do ControlValueAccessor
  writeValue(value) {
    this.writeValueModel(value);
    this.cd?.markForCheck();
  }
  validateModel() {
    if (this.validatorChange) {
      this.validatorChange();
    }
  }
  isAdditionalHelpEventTriggered() {
    return this.additionalHelpEventTrigger === "event" || this.additionalHelpEventTrigger === void 0 && this.additionalHelp.observed;
  }
  //Transforma o tamanho do input para o tamanho do ícone de loading correspondente
  mapSizeToIcon(size) {
    return mapInputSizeToLoadingIcon(size);
  }
  // utilizado para validar o pattern na inicializacao, fazendo dessa forma o campo fica sujo (dirty).
  validatePatternOnWriteValue(value) {
    if (value && this.passedWriteValue) {
      setTimeout(() => {
        this.updateModel(value);
      });
      this.passedWriteValue = false;
    }
  }
  applySizeBasedOnA11y() {
    const size = validateSizeFn(this._initialSize, PoFieldSize);
    this._size = size;
  }
  static ɵfac = function PoInputBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoInputBaseComponent)(ɵɵdirectiveInject(ChangeDetectorRef));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoInputBaseComponent,
    viewQuery: function PoInputBaseComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c91, 5, PoFieldContainerComponent)(_c17, 5, PoHelperComponent);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.fieldContainer = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.helperEl = _t.first);
      }
    },
    hostVars: 2,
    hostBindings: function PoInputBaseComponent_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("PoUiThemeChange", function PoInputBaseComponent_PoUiThemeChange_HostBindingHandler() {
          return ctx.onThemeChange();
        }, ɵɵresolveWindow);
      }
      if (rf & 2) {
        ɵɵattribute("p-loading", ctx.loading)("p-size", ctx.size);
      }
    },
    inputs: {
      additionalHelpEventTrigger: "additionalHelpEventTrigger",
      additionalHelpTooltip: [0, "p-additional-help-tooltip", "additionalHelpTooltip"],
      appendBox: [2, "p-append-in-body", "appendBox", convertToBoolean],
      autoFocus: [2, "p-auto-focus", "autoFocus", convertToBoolean],
      compactLabel: [1, "p-compact-label", "compactLabel"],
      icon: [0, "p-icon", "icon"],
      emitAllChanges: [2, "p-emit-all-changes", "emitAllChanges", convertToBoolean],
      label: [0, "p-label", "label"],
      help: [0, "p-help", "help"],
      name: "name",
      errorAsyncProperties: [0, "p-error-async-properties", "errorAsyncProperties"],
      errorPattern: [0, "p-error-pattern", "errorPattern"],
      errorLimit: [0, "p-error-limit", "errorLimit"],
      optional: [0, "p-optional", "optional"],
      showErrorMessageRequired: [0, "p-required-field-error-message", "showErrorMessageRequired"],
      upperCase: [2, "p-upper-case", "upperCase", convertToBoolean],
      maskNoLengthValidation: [2, "p-mask-no-length-validation", "maskNoLengthValidation", convertToBoolean],
      noAutocomplete: [2, "p-no-autocomplete", "noAutocomplete", convertToBoolean],
      placeholder: [0, "p-placeholder", "placeholder"],
      setDisabled: [2, "p-disabled", "setDisabled", convertToBoolean],
      loading: [2, "p-loading", "loading", convertToBoolean],
      setReadonly: [2, "p-readonly", "setReadonly", convertToBoolean],
      setRequired: [2, "p-required", "setRequired", convertToBoolean],
      size: [0, "p-size", "size"],
      showRequired: [0, "p-show-required", "showRequired"],
      setClean: [2, "p-clean", "setClean", convertToBoolean],
      setPattern: [0, "p-pattern", "setPattern"],
      maxlength: [0, "p-maxlength", "maxlength"],
      minlength: [0, "p-minlength", "minlength"],
      setMask: [0, "p-mask", "setMask"],
      setMaskFormatModel: [2, "p-mask-format-model", "setMaskFormatModel", convertToBoolean],
      poHelperComponent: [1, "p-helper", "poHelperComponent"],
      labelTextWrap: [1, "p-label-text-wrap", "labelTextWrap"]
    },
    outputs: {
      additionalHelp: "p-additional-help",
      blur: "p-blur",
      enter: "p-enter",
      change: "p-change",
      changeModel: "p-change-model",
      keydown: "p-keydown"
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoInputBaseComponent, [{
    type: Directive
  }], () => [{
    type: ChangeDetectorRef
  }], {
    fieldContainer: [{
      type: ViewChild,
      args: ["fieldContainer", {
        read: PoFieldContainerComponent,
        static: false
      }]
    }],
    helperEl: [{
      type: ViewChild,
      args: ["helperEl", {
        read: PoHelperComponent,
        static: false
      }]
    }],
    additionalHelpEventTrigger: [{
      type: Input
    }],
    additionalHelpTooltip: [{
      type: Input,
      args: ["p-additional-help-tooltip"]
    }],
    appendBox: [{
      type: Input,
      args: [{
        alias: "p-append-in-body",
        transform: convertToBoolean
      }]
    }],
    autoFocus: [{
      type: Input,
      args: [{
        alias: "p-auto-focus",
        transform: convertToBoolean
      }]
    }],
    compactLabel: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-compact-label",
        required: false
      }]
    }],
    icon: [{
      type: Input,
      args: ["p-icon"]
    }],
    emitAllChanges: [{
      type: Input,
      args: [{
        alias: "p-emit-all-changes",
        transform: convertToBoolean
      }]
    }],
    label: [{
      type: Input,
      args: ["p-label"]
    }],
    help: [{
      type: Input,
      args: ["p-help"]
    }],
    name: [{
      type: Input,
      args: ["name"]
    }],
    errorAsyncProperties: [{
      type: Input,
      args: ["p-error-async-properties"]
    }],
    errorPattern: [{
      type: Input,
      args: ["p-error-pattern"]
    }],
    errorLimit: [{
      type: Input,
      args: ["p-error-limit"]
    }],
    optional: [{
      type: Input,
      args: ["p-optional"]
    }],
    showErrorMessageRequired: [{
      type: Input,
      args: ["p-required-field-error-message"]
    }],
    upperCase: [{
      type: Input,
      args: [{
        alias: "p-upper-case",
        transform: convertToBoolean
      }]
    }],
    maskNoLengthValidation: [{
      type: Input,
      args: [{
        alias: "p-mask-no-length-validation",
        transform: convertToBoolean
      }]
    }],
    additionalHelp: [{
      type: Output,
      args: ["p-additional-help"]
    }],
    blur: [{
      type: Output,
      args: ["p-blur"]
    }],
    enter: [{
      type: Output,
      args: ["p-enter"]
    }],
    change: [{
      type: Output,
      args: ["p-change"]
    }],
    changeModel: [{
      type: Output,
      args: ["p-change-model"]
    }],
    keydown: [{
      type: Output,
      args: ["p-keydown"]
    }],
    noAutocomplete: [{
      type: Input,
      args: [{
        alias: "p-no-autocomplete",
        transform: convertToBoolean
      }]
    }],
    placeholder: [{
      type: Input,
      args: ["p-placeholder"]
    }],
    setDisabled: [{
      type: Input,
      args: [{
        alias: "p-disabled",
        transform: convertToBoolean
      }]
    }],
    loading: [{
      type: HostBinding,
      args: ["attr.p-loading"]
    }, {
      type: Input,
      args: [{
        alias: "p-loading",
        transform: convertToBoolean
      }]
    }],
    setReadonly: [{
      type: Input,
      args: [{
        alias: "p-readonly",
        transform: convertToBoolean
      }]
    }],
    setRequired: [{
      type: Input,
      args: [{
        alias: "p-required",
        transform: convertToBoolean
      }]
    }],
    size: [{
      type: Input,
      args: ["p-size"]
    }, {
      type: HostBinding,
      args: ["attr.p-size"]
    }],
    showRequired: [{
      type: Input,
      args: ["p-show-required"]
    }],
    setClean: [{
      type: Input,
      args: [{
        alias: "p-clean",
        transform: convertToBoolean
      }]
    }],
    setPattern: [{
      type: Input,
      args: ["p-pattern"]
    }],
    maxlength: [{
      type: Input,
      args: ["p-maxlength"]
    }],
    minlength: [{
      type: Input,
      args: ["p-minlength"]
    }],
    setMask: [{
      type: Input,
      args: ["p-mask"]
    }],
    setMaskFormatModel: [{
      type: Input,
      args: [{
        alias: "p-mask-format-model",
        transform: convertToBoolean
      }]
    }],
    poHelperComponent: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-helper",
        required: false
      }]
    }],
    labelTextWrap: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-label-text-wrap",
        required: false
      }]
    }],
    onThemeChange: [{
      type: HostListener,
      args: ["window:PoUiThemeChange"]
    }]
  });
})();
var PoInputGeneric = class _PoInputGeneric extends PoInputBaseComponent {
  inputEl;
  type = "text";
  el;
  valueBeforeChange;
  timeoutChange;
  subscriptionValidator = new Subscription();
  get autocomplete() {
    return this.noAutocomplete ? "off" : "on";
  }
  constructor(el, cd) {
    super(cd);
    this.el = el;
  }
  onKeydown(e2) {
    if (this.mask && !this.readonly && e2.target.keyCode !== 229) {
      this.objMask.keydown(e2);
      if (this.passedWriteValue) {
        this.validateClassesForMask(true);
      }
    }
  }
  onKeyup(e2) {
    if (this.mask && !this.readonly) {
      if (e2.target.keyCode !== 229) {
        this.objMask.keyup(e2);
      }
      this.callOnChange(this.objMask.valueToModel);
      if (this.errorAsyncProperties?.triggerMode === "changeModel") {
        this.verifyErrorAsync();
      }
    }
  }
  ngAfterViewInit() {
    this.afterViewInit();
  }
  afterViewInit() {
    this.verifyAutoFocus();
  }
  ngOnDestroy() {
    this.subscriptionValidator?.unsubscribe();
  }
  focus() {
    if (!this.disabled) {
      this.inputEl.nativeElement.focus();
    }
  }
  onKeyDown(event) {
    const isFieldFocused = document.activeElement === this.inputEl.nativeElement;
    if (isFieldFocused) {
      this.keydown.emit(event);
    }
  }
  verifyAutoFocus() {
    if (this.autoFocus) {
      this.focus();
    }
  }
  eventOnInput(e2) {
    let value = "";
    if (!this.mask) {
      value = this.validMaxLength(this.maxlength, e2.target.value);
      this.inputEl.nativeElement.value = value;
    } else {
      this.objMask.blur(e2);
      this.inputEl.nativeElement.value = this.objMask.valueToInput;
      value = this.objMask.valueToModel;
    }
    this.inputEl.nativeElement.value = this.upperCase ? String(this.inputEl.nativeElement.value).toUpperCase() : this.inputEl.nativeElement.value;
    value = this.upperCase ? value.toUpperCase() : value;
    this.callOnChange(value);
    if (this.errorAsyncProperties?.triggerMode === "changeModel") {
      this.verifyErrorAsync();
    }
  }
  validMaxLength(maxlength, value) {
    return (maxlength || maxlength === 0) && value.length > maxlength ? value.toString().substring(0, maxlength) : value;
  }
  eventOnFocus(e2) {
    this.valueBeforeChange = this.inputEl.nativeElement.value;
    this.enter.emit();
  }
  eventOnBlur(e2) {
    this.onTouched?.();
    if (this.mask) {
      this.objMask.blur(e2);
    }
    if (e2.type === "blur") {
      this.blur.emit();
      this.controlChangeEmitter();
    }
  }
  controlChangeEmitter() {
    const elementValue = this.inputEl.nativeElement.value;
    if (elementValue !== this.valueBeforeChange) {
      clearTimeout(this.timeoutChange);
      this.timeoutChange = setTimeout(() => {
        this.change.emit(elementValue);
        const errorAsync = this.errorAsyncProperties;
        if (errorAsync?.triggerMode === "change" || !errorAsync?.triggerMode) {
          this.verifyErrorAsync();
        }
      }, 200);
    }
  }
  eventOnClick(e2) {
    if (this.mask) {
      this.objMask.click(e2);
    }
  }
  hasInvalidClass() {
    return this.el.nativeElement.classList.contains("ng-invalid") && this.el.nativeElement.classList.contains("ng-dirty") && (this.inputEl.nativeElement.value !== "" || this.showErrorMessageRequired && (this.required || this.hasValidatorRequired));
  }
  verifyErrorAsync() {
    if (this.errorPattern !== "" && this.errorAsyncProperties?.errorAsync) {
      const errorAsync = this.errorAsyncProperties.errorAsync(this.inputEl.nativeElement.value);
      if (isObservable(errorAsync)) {
        this.subscriptionValidator.unsubscribe();
        this.subscriptionValidator = errorAsync.pipe(switchMap((error2) => {
          const element = this.el.nativeElement;
          if (error2) {
            element.classList.add("ng-invalid");
            element.classList.add("ng-dirty");
            this.cd.detectChanges();
          } else if (element.classList.contains("ng-invalid") && element.classList.contains("ng-dirty") && !this.isInvalid) {
            element.classList.remove("ng-invalid");
            this.cd.detectChanges();
          }
          return of("");
        })).subscribe();
      }
    }
  }
  getErrorPattern() {
    return this.errorPattern !== "" && this.hasInvalidClass() ? this.errorPattern : "";
  }
  validateClassesForPattern() {
    const value = this.getScreenValue();
    const element = this.el.nativeElement;
    if (value && !this.verifyPattern(this.pattern, value)) {
      element.classList.add("ng-invalid");
      element.classList.add("ng-dirty");
    } else {
      element.classList.remove("ng-invalid");
    }
  }
  validateClassesForMask(keyDown = false) {
    const element = this.el.nativeElement;
    const elementValue = this.inputEl.nativeElement.value;
    if (!keyDown && !elementValue) {
      element.classList.add("ng-invalid-mask");
    } else {
      element.classList.remove("ng-invalid-mask");
    }
  }
  verifyPattern(pattern, value) {
    return new RegExp(pattern).test(value);
  }
  clear(value) {
    this.onTouched?.();
    this.callOnChange(value);
    this.valueBeforeChange = void 0;
    this.controlChangeEmitter();
    if (this.errorAsyncProperties?.triggerMode === "changeModel") {
      this.verifyErrorAsync();
    }
  }
  writeValueModel(value) {
    this.passedWriteValue = true;
    if (this.inputEl) {
      if (value) {
        if (this.mask) {
          this.inputEl.nativeElement.value = this.objMask.controlFormatting(String(value));
          if (this.objMask.formatModel) {
            this.callUpdateModelWithTimeout(this.objMask.valueToModel);
          }
        } else {
          this.inputEl.nativeElement.value = value;
        }
      } else {
        this.inputEl.nativeElement.value = "";
        this.modelLastUpdate = "";
      }
    }
    if (value) {
      this.validateInitMask();
      this.changeModel.emit(value);
      this.verifyErrorAsync();
    }
  }
  getScreenValue() {
    const screenValue = this.inputEl && this.inputEl.nativeElement.value || void 0;
    if (this.type === "number") {
      const parsedValue = parseFloat(screenValue);
      return parsedValue || parsedValue === 0 ? parsedValue : null;
    } else {
      return screenValue;
    }
  }
  validateInitMask() {
    if (this.mask) {
      this.validateClassesForMask();
    }
  }
  setHelper(label, additionalHelpTooltip) {
    return setHelperSettings(label, additionalHelpTooltip, this.poHelperComponent(), this.size, this.isAdditionalHelpEventTriggered() ? this.additionalHelp : void 0);
  }
  static ɵfac = function PoInputGeneric_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoInputGeneric)(ɵɵdirectiveInject(ElementRef), ɵɵdirectiveInject(ChangeDetectorRef));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoInputGeneric,
    viewQuery: function PoInputGeneric_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c49, 7, ElementRef);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.inputEl = _t.first);
      }
    },
    hostBindings: function PoInputGeneric_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("keydown", function PoInputGeneric_keydown_HostBindingHandler($event) {
          return ctx.onKeydown($event);
        })("keyup", function PoInputGeneric_keyup_HostBindingHandler($event) {
          return ctx.onKeyup($event);
        });
      }
    },
    features: [ɵɵInheritDefinitionFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoInputGeneric, [{
    type: Directive
  }], () => [{
    type: ElementRef
  }, {
    type: ChangeDetectorRef
  }], {
    inputEl: [{
      type: ViewChild,
      args: ["inp", {
        read: ElementRef,
        static: true
      }]
    }],
    onKeydown: [{
      type: HostListener,
      args: ["keydown", ["$event"]]
    }],
    onKeyup: [{
      type: HostListener,
      args: ["keyup", ["$event"]]
    }]
  });
})();
var poSearchAiLiteralsDefault = {
  en: {
    clean: "Clear field",
    errorMessage: "AI search error"
  },
  es: {
    clean: "Limpiar campo",
    errorMessage: "Error en la búsqueda con IA"
  },
  pt: {
    clean: "Limpar campo",
    errorMessage: "Erro na busca com IA"
  },
  ru: {
    clean: "Очистить поле",
    errorMessage: "Ошибка поиска ИИ"
  }
};
function extractSearchAiColumns(columns) {
  if (!columns) {
    return [];
  }
  return columns.filter((col) => col.property && col.visible !== false && !col.searchAiIgnore).map((col) => ({
    property: col.property,
    label: col.label || col.property,
    type: col.type || "string"
  }));
}
var PoSearchAiService = class _PoSearchAiService {
  http;
  headers = new HttpHeaders({
    "X-PO-No-Message": "true"
  });
  constructor(http) {
    this.http = http;
  }
  /**
   * Envia a consulta para o endpoint de IA.
   *
   * @param url Endpoint configurado em `p-url`.
   * @param query Texto em linguagem natural digitado pelo usuário.
   * @param columns Metadados das colunas/campos disponíveis para a busca.
   * @param timeoutMs Tempo máximo de espera (ms) antes de abortar a requisição.
   */
  sendQuery(url, query2, columns, timeoutMs = 1e4) {
    const body = {
      query: query2,
      columns
    };
    return this.http.post(url, body, {
      headers: this.headers
    }).pipe(timeout(timeoutMs), catchError((error2) => {
      if (error2?.name === "TimeoutError") {
        return throwError(() => ({
          statusCode: 408,
          message: "AI search request timed out"
        }));
      }
      return throwError(() => ({
        statusCode: error2?.status || 500,
        message: error2?.status ? error2?.error?.message ?? error2?.message : "AI search request failed"
      }));
    }));
  }
  /**
   * Extrai e normaliza os metadados das colunas para envio à IA,
   * descartando colunas sem `property` ou marcadas para serem ignoradas.
   *
   * @param columns Lista de colunas no formato bruto do componente consumidor.
   */
  extractColumnsMetadata(columns) {
    return extractSearchAiColumns(columns);
  }
  static ɵfac = function PoSearchAiService_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoSearchAiService)(ɵɵinject(HttpClient));
  };
  static ɵprov = ɵɵdefineInjectable({
    token: _PoSearchAiService,
    factory: _PoSearchAiService.ɵfac
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoSearchAiService, [{
    type: Injectable
  }], () => [{
    type: HttpClient
  }], null);
})();
var PO_SEARCH_AI_DEFAULT_TIMEOUT = 1e4;
var PO_SEARCH_AI_DEFAULT_MIN_CONFIDENCE = 0.5;
var PoSearchAiBaseComponent = class _PoSearchAiBaseComponent extends PoInputGeneric {
  aiErrorMessage = "";
  aiLoading = false;
  aiSubscription;
  /**
   * @optional
   *
   * @description
   *
   * Evento disparado quando o filtro aplicado via IA é limpo, seja pela ação do usuário
   * ou programaticamente. Não emite valor.
   */
  clearEvent = output({
    alias: "p-clear"
  });
  /**
   * @optional
   *
   * @description
   *
   * Metadados das colunas/campos disponíveis para a busca por IA. Essas informações são
   * enviadas ao endpoint configurado em `p-url` para que a IA mapeie os termos digitados
   * para as propriedades reais dos dados.
   *
   * @default `[]`
   */
  columns = input([], __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "columns"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-columns"
  }));
  effectiveLiterals = computed(() => {
    const val = this.literals();
    return val && typeof val === "object" ? __spreadValues(__spreadValues({}, poSearchAiLiteralsDefault[this.language]), val) : poSearchAiLiteralsDefault[this.language];
  }, ...ngDevMode ? [{
    debugName: "effectiveLiterals"
  }] : (
    /* istanbul ignore next */
    []
  ));
  /**
   * @optional
   *
   * @description
   *
   * Evento disparado quando a chamada à API de IA falha (erro HTTP, timeout, etc.).
   * Emite um objeto `PoSearchAiError`.
   */
  error = output({
    alias: "p-error"
  });
  language;
  /**
   * @optional
   *
   * @description
   *
   * Objeto com os literais usados no componente. Permite sobrescrever as mensagens padrão
   * para internacionalização ou customização.
   */
  literals = input(void 0, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "literals"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-literals"
  }));
  /**
   * @optional
   *
   * @description
   *
   * Evento disparado quando a confiança da resposta da IA é menor que `p-min-confidence`.
   * Emite um objeto `PoSearchAiResult`, permitindo ao desenvolvedor decidir o que fazer
   * (ex: confirmar com o usuário antes de aplicar o filtro).
   */
  lowConfidence = output({
    alias: "p-low-confidence"
  });
  /**
   * @optional
   *
   * @description
   *
   * Nível mínimo de confiança (`0.0` a `1.0`) para que o resultado da IA seja considerado
   * confiável. Quando a confiança retornada for menor, o evento `p-low-confidence` é
   * emitido em vez de `p-result`.
   *
   * @default `0.5`
   */
  minConfidence = input(PO_SEARCH_AI_DEFAULT_MIN_CONFIDENCE, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "minConfidence"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-min-confidence",
    transform: (value) => {
      const parsed = Number(value);
      return parsed >= 0 && parsed <= 1 ? parsed : PO_SEARCH_AI_DEFAULT_MIN_CONFIDENCE;
    }
  }));
  /**
   * @optional
   *
   * @description
   *
   * Evento disparado quando a IA retorna um resultado com confiança maior ou igual a
   * `p-min-confidence`. Emite um objeto `PoSearchAiResult`.
   *
   * O campo `type` do resultado indica como o consumidor deve interpretar a resposta:
   *
   * - **`filter`** _(padrão)_: a IA retornou um filtro estruturado (ex: OData). Use `result.filter`
   *   para aplicar a consulta à fonte de dados — por exemplo, passando para um `po-table` via `p-filter`.
   *
   * - **`chat`**: a IA retornou uma resposta conversacional. Use `result.data` para exibir a mensagem
   *   ao usuário, por exemplo em um painel lateral ou tooltip.
   *
   * - **`custom`**: a IA retornou um payload genérico definido pelo backend. Use `result.data` para
   *   executar qualquer ação específica da aplicação (ex: navegação, abertura de modal, acionamento de comando).
   */
  result = output({
    alias: "p-result"
  });
  searchAiService;
  /**
   * @optional
   *
   * @description
   *
   * Tempo máximo de espera (em milissegundos) pela resposta da IA antes de abortar a
   * requisição e emitir `p-error` com `statusCode 408`.
   *
   * @default `10000`
   */
  timeout = input(PO_SEARCH_AI_DEFAULT_TIMEOUT, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "timeout"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-timeout",
    transform: (value) => {
      const parsed = Number(value);
      return parsed > 0 ? parsed : PO_SEARCH_AI_DEFAULT_TIMEOUT;
    }
  }));
  /**
   * @optional
   *
   * @description
   *
   * Endpoint (proxy) responsável por encaminhar a consulta para o provedor de IA.
   * Recebe `{ query, columns }` via `POST` e deve retornar `{ filter, description, confidence }`.
   *
   * > A integração com a LLM e a guarda de chaves devem ocorrer **no backend**, nunca no client-side.
   */
  url = input(void 0, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "url"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-url"
  }));
  constructor() {
    super(inject(ElementRef), inject(ChangeDetectorRef));
    this.searchAiService = inject(PoSearchAiService);
    this.language = inject(PoLanguageService).getShortLanguage();
  }
  getErrorPattern() {
    return this.aiErrorMessage || super.getErrorPattern();
  }
  extraValidation(_c197) {
    return null;
  }
  hasValue() {
    return !!this.getScreenValue();
  }
  ngOnDestroy() {
    super.ngOnDestroy?.();
    this.aiSubscription?.unsubscribe();
  }
  static ɵfac = function PoSearchAiBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoSearchAiBaseComponent)();
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoSearchAiBaseComponent,
    inputs: {
      columns: [1, "p-columns", "columns"],
      literals: [1, "p-literals", "literals"],
      minConfidence: [1, "p-min-confidence", "minConfidence"],
      timeout: [1, "p-timeout", "timeout"],
      url: [1, "p-url", "url"]
    },
    outputs: {
      clearEvent: "p-clear",
      error: "p-error",
      lowConfidence: "p-low-confidence",
      result: "p-result"
    },
    features: [ɵɵInheritDefinitionFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoSearchAiBaseComponent, [{
    type: Directive
  }], () => [], {
    clearEvent: [{
      type: Output,
      args: ["p-clear"]
    }],
    columns: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-columns",
        required: false
      }]
    }],
    error: [{
      type: Output,
      args: ["p-error"]
    }],
    literals: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-literals",
        required: false
      }]
    }],
    lowConfidence: [{
      type: Output,
      args: ["p-low-confidence"]
    }],
    minConfidence: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-min-confidence",
        required: false
      }]
    }],
    result: [{
      type: Output,
      args: ["p-result"]
    }],
    timeout: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-timeout",
        required: false
      }]
    }],
    url: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-url",
        required: false
      }]
    }]
  });
})();
var FOCUS_DELAY_MS = 200;
var PoSearchAiComponent = class _PoSearchAiComponent extends PoSearchAiBaseComponent {
  inp;
  iconSearchAiEl;
  id = `po-search-ai[${uuid()}]`;
  /* istanbul ignore next */
  constructor() {
    super();
  }
  ngOnChanges(changes) {
    if (changes.label) {
      this.displayAdditionalHelp = false;
    }
  }
  /**
   * Envia a consulta atual (valor do campo) para o endpoint de IA configurado em `p-url`.
   *
   * Caso a consulta esteja vazia ou `p-url` não esteja definido, nada é feito.
   * O resultado é emitido via `p-result` (ou `p-low-confidence` quando a confiança for baixa)
   * e falhas são emitidas via `p-error`.
   */
  search() {
    const query2 = (this.getScreenValue() || "").toString().trim();
    if (!query2 || !this.url() || this.isDisabled || this.readonly) {
      return;
    }
    this.aiSubscription?.unsubscribe();
    this.aiErrorMessage = "";
    this.el.nativeElement.classList.remove("ng-invalid");
    const columnsMetadata = this.searchAiService.extractColumnsMetadata(this.columns());
    this.aiLoading = true;
    this.cd?.detectChanges();
    this.aiSubscription = this.searchAiService.sendQuery(this.url(), query2, columnsMetadata, this.timeout()).subscribe({
      next: (response) => {
        const type = response?.type || (response?.filter ? PoSearchAiResponseType.filter : PoSearchAiResponseType.custom);
        const result = {
          query: query2,
          type,
          filter: response?.filter,
          description: response?.description,
          confidence: response?.confidence,
          data: response?.data
        };
        this.handleResponse(result);
      },
      error: (err) => this.handleError(query2, err)
    });
  }
  clearAndFocus() {
    this.clearSearch();
    setTimeout(() => this.inputEl?.nativeElement?.focus(), FOCUS_DELAY_MS);
  }
  handleCleanKeydown(event) {
    if (event.key !== "Tab") {
      return;
    }
    if (event.shiftKey) {
      this.inputEl?.nativeElement?.focus();
      event.preventDefault();
      event.stopPropagation();
    } else {
      this.iconSearchAiEl?.focus();
      event.preventDefault();
    }
  }
  /**
   * Limpa o filtro aplicado via IA, esvazia o campo e emite o evento `p-clear`.
   */
  clearSearch() {
    this.aiSubscription?.unsubscribe();
    this.aiErrorMessage = "";
    this.el.nativeElement.classList.remove("ng-invalid");
    this.aiLoading = false;
    if (this.inputEl?.nativeElement) {
      this.inputEl.nativeElement.value = "";
    }
    this.callOnChange("");
    this.clearEvent.emit();
    this.cd?.detectChanges();
  }
  /**
   * Manipula a tecla pressionada no campo: dispara a busca ao pressionar `Enter`.
   *
   * @param event Evento de teclado.
   */
  onSearchKeydown(event) {
    this.onKeyDown(event);
    if (event.key === "Enter") {
      event.preventDefault();
      this.search();
    }
  }
  finishProcessing() {
    this.cd?.detectChanges();
    this.inputEl?.nativeElement?.focus();
  }
  handleResponse(result) {
    this.aiLoading = false;
    const confidence = result.confidence ?? 1;
    if (confidence < this.minConfidence()) {
      this.lowConfidence.emit(result);
      this.finishProcessing();
      return;
    }
    this.result.emit(result);
    this.finishProcessing();
  }
  handleError(query2, err) {
    this.aiLoading = false;
    this.aiErrorMessage = err?.message || this.effectiveLiterals().errorMessage;
    this.el.nativeElement.classList.add("ng-invalid", "ng-dirty");
    this.error.emit({
      query: query2,
      statusCode: err?.statusCode || 500,
      message: err?.message || this.effectiveLiterals().errorMessage
    });
    this.finishProcessing();
  }
  static ɵfac = function PoSearchAiComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoSearchAiComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoSearchAiComponent,
    selectors: [["po-search-ai"]],
    viewQuery: function PoSearchAiComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c49, 7)(_c92, 5);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.inp = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.iconSearchAiEl = _t.first);
      }
    },
    standalone: false,
    features: [ɵɵProvidersFeature([PoSearchAiService, {
      provide: NG_VALUE_ACCESSOR,
      useExisting: forwardRef(() => _PoSearchAiComponent),
      multi: true
    }, {
      provide: NG_VALIDATORS,
      useExisting: forwardRef(() => _PoSearchAiComponent),
      multi: true
    }]), ɵɵInheritDefinitionFeature, ɵɵNgOnChangesFeature],
    decls: 16,
    vars: 32,
    consts: [["fieldContainer", ""], ["inp", ""], ["iconSearchAi", ""], ["iconClean", ""], ["helperEl", ""], [3, "p-disabled", "p-id", "p-label", "p-optional", "p-required", "p-show-required", "p-helper", "p-show-helper", "p-size", "p-text-wrap", "p-compact-label"], [1, "po-field-container-content"], [1, "po-field-container-content-helper-wrapper"], [1, "po-field-container-input"], ["aria-hidden", "true", 1, "po-search-ai-error-overlay"], ["aria-hidden", "true", 1, "po-search-ai-icon"], ["p-icon", "ICON_SEARCH"], [1, "po-input", 3, "blur", "focus", "input", "keydown", "autocomplete", "disabled", "id", "placeholder", "readonly", "required", "type"], [1, "po-field-icon-container-right"], [1, "po-field-helper-button", 3, "p-size", "p-helper", "p-disabled", "p-append-in-body"], [3, "p-append-in-body", "p-help", "p-disabled", "p-error-pattern", "p-error-limit", "p-size"], [3, "p-size"], [1, "po-search-ai-processing-icon", 3, "p-size"], ["p-icon", "ICON_MAGIC_WAND", "p-kind", "tertiary", 1, "po-search-ai-button", 3, "p-click", "p-disabled", "p-size"], ["tabindex", "0", "role", "button", "p-icon", "ICON_FILL_CLEAR_CONTENT", 1, "po-icon-input", 3, "p-change-event", "keydown", "keydown.enter", "keydown.space", "p-element-ref", "p-size"], [1, "po-button-vertical-divider"]],
    template: function PoSearchAiComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵdeclareLet(0);
        ɵɵelementStart(1, "po-field-container", 5, 0)(3, "div", 6)(4, "div", 7)(5, "div", 8);
        ɵɵconditionalCreate(6, PoSearchAiComponent_Conditional_6_Template, 1, 0, "div", 9);
        ɵɵelementStart(7, "div", 10);
        ɵɵelement(8, "po-icon", 11);
        ɵɵelementEnd();
        ɵɵelementStart(9, "input", 12, 1);
        ɵɵlistener("blur", function PoSearchAiComponent_Template_input_blur_9_listener($event) {
          return ctx.eventOnBlur($event);
        })("focus", function PoSearchAiComponent_Template_input_focus_9_listener($event) {
          return ctx.eventOnFocus($event);
        })("input", function PoSearchAiComponent_Template_input_input_9_listener($event) {
          return ctx.eventOnInput($event);
        })("keydown", function PoSearchAiComponent_Template_input_keydown_9_listener($event) {
          return ctx.onSearchKeydown($event);
        });
        ɵɵelementEnd();
        ɵɵconditionalCreate(11, PoSearchAiComponent_Conditional_11_Template, 2, 1, "div", 13)(12, PoSearchAiComponent_Conditional_12_Template, 2, 1, "div", 13)(13, PoSearchAiComponent_Conditional_13_Template, 4, 3, "div", 13);
        ɵɵelementEnd();
        ɵɵconditionalCreate(14, PoSearchAiComponent_Conditional_14_Template, 2, 4, "po-helper", 14);
        ɵɵelementEnd()();
        ɵɵconditionalCreate(15, PoSearchAiComponent_Conditional_15_Template, 1, 6, "po-field-container-bottom", 15);
        ɵɵelementEnd();
      }
      if (rf & 2) {
        const poHelper_r5 = ɵɵstoreLet(ctx.setHelper(ctx.label, ctx.additionalHelpTooltip));
        ɵɵadvance();
        ɵɵproperty("p-disabled", ctx.isDisabled)("p-id", ctx.id)("p-label", ctx.label)("p-optional", ctx.optional)("p-required", !!ctx.required)("p-show-required", ctx.showRequired)("p-helper", ctx.label ? poHelper_r5.helperSettings : void 0)("p-show-helper", ctx.label ? ctx.displayAdditionalHelp : false)("p-size", ctx.size)("p-text-wrap", ctx.labelTextWrap())("p-compact-label", ctx.compactLabel());
        ɵɵadvance(4);
        ɵɵclassProp("po-search-ai-processing", ctx.aiLoading)("po-search-ai-error", ctx.aiErrorMessage && !ctx.aiLoading);
        ɵɵadvance();
        ɵɵconditional(ctx.aiErrorMessage && !ctx.aiLoading ? 6 : -1);
        ɵɵadvance(3);
        ɵɵproperty("autocomplete", ctx.autocomplete)("disabled", ctx.isDisabled)("id", ctx.id)("placeholder", ctx.isDisabled ? "" : ctx.placeholder)("readonly", ctx.readonly || ctx.aiLoading)("required", ctx.required)("type", ctx.type);
        ɵɵattribute("aria-label", ctx.label || ctx.placeholder)("name", ctx.name)("data-focused-element", !ctx.isDisabled)("data-inactive-component", ctx.isDisabled || ctx.readonly)("p-size", ctx.size);
        ɵɵadvance(2);
        ɵɵconditional(ctx.loading ? 11 : ctx.aiLoading ? 12 : 13);
        ɵɵadvance(3);
        ɵɵconditional(!ctx.label && (ctx.poHelperComponent() || poHelper_r5.hideAdditionalHelp) ? 14 : -1);
        ɵɵadvance();
        ɵɵconditional(!ctx.readonly ? 15 : -1);
      }
    },
    dependencies: [PoButtonComponent, PoCleanComponent, PoFieldContainerBottomComponent, PoFieldContainerComponent, PoHelperComponent, PoIconComponent, PoLoadingIconComponent],
    encapsulation: 2,
    changeDetection: 0
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoSearchAiComponent, [{
    type: Component,
    args: [{
      selector: "po-search-ai",
      providers: [PoSearchAiService, {
        provide: NG_VALUE_ACCESSOR,
        useExisting: forwardRef(() => PoSearchAiComponent),
        multi: true
      }, {
        provide: NG_VALIDATORS,
        useExisting: forwardRef(() => PoSearchAiComponent),
        multi: true
      }],
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: `@let poHelper = setHelper(label, additionalHelpTooltip);
<po-field-container
  #fieldContainer
  [p-disabled]="isDisabled"
  [p-id]="id"
  [p-label]="label"
  [p-optional]="optional"
  [p-required]="!!required"
  [p-show-required]="showRequired"
  [p-helper]="label ? poHelper.helperSettings : undefined"
  [p-show-helper]="label ? displayAdditionalHelp : false"
  [p-size]="size"
  [p-text-wrap]="labelTextWrap()"
  [p-compact-label]="compactLabel()"
>
  <div class="po-field-container-content">
    <div class="po-field-container-content-helper-wrapper">
      <div
        class="po-field-container-input"
        [class.po-search-ai-processing]="aiLoading"
        [class.po-search-ai-error]="aiErrorMessage && !aiLoading"
      >
        @if (aiErrorMessage && !aiLoading) {
          <div class="po-search-ai-error-overlay" aria-hidden="true"></div>
        }
        <div class="po-search-ai-icon" aria-hidden="true">
          <po-icon p-icon="ICON_SEARCH"></po-icon>
        </div>
        <input
          #inp
          [attr.aria-label]="label || placeholder"
          [attr.name]="name"
          [attr.data-focused-element]="!isDisabled"
          [attr.data-inactive-component]="isDisabled || readonly"
          [attr.p-size]="size"
          [autocomplete]="autocomplete"
          class="po-input"
          [disabled]="isDisabled"
          [id]="id"
          [placeholder]="isDisabled ? '' : placeholder"
          [readonly]="readonly || aiLoading"
          [required]="required"
          [type]="type"
          (blur)="eventOnBlur($event)"
          (focus)="eventOnFocus($event)"
          (input)="eventOnInput($event)"
          (keydown)="onSearchKeydown($event)"
        />

        @if (loading) {
          <div class="po-field-icon-container-right">
            <po-loading-icon [p-size]="mapSizeToIcon(size)"></po-loading-icon>
          </div>
        } @else if (aiLoading) {
          <div class="po-field-icon-container-right">
            <po-loading-icon class="po-search-ai-processing-icon" [p-size]="mapSizeToIcon(size)"></po-loading-icon>
          </div>
        } @else {
          <div class="po-field-icon-container-right">
            @if (clean && hasValue() && !isDisabled && !readonly) {
              <po-clean
                #iconClean
                tabindex="0"
                role="button"
                [attr.aria-label]="effectiveLiterals().clean"
                class="po-icon-input"
                p-icon="ICON_FILL_CLEAR_CONTENT"
                [p-element-ref]="inputEl"
                [p-size]="size"
                (p-change-event)="clearSearch()"
                (keydown)="handleCleanKeydown($event)"
                (keydown.enter)="clearAndFocus(); $event.preventDefault()"
                (keydown.space)="clearAndFocus(); $event.preventDefault()"
              >
              </po-clean>
              <div class="po-button-vertical-divider"></div>
            }
            <po-button
              #iconSearchAi
              class="po-search-ai-button"
              p-icon="ICON_MAGIC_WAND"
              p-kind="tertiary"
              [p-disabled]="isDisabled || !!readonly"
              [p-size]="size"
              (p-click)="search()"
            >
            </po-button>
          </div>
        }
      </div>
      @if (!this.label && (poHelperComponent() || poHelper.hideAdditionalHelp)) {
        <po-helper
          #helperEl
          class="po-field-helper-button"
          [p-size]="size"
          [p-helper]="poHelper.helperSettings"
          [p-disabled]="isDisabled"
          [p-append-in-body]="appendBox"
        >
        </po-helper>
      }
    </div>
  </div>

  @if (!readonly) {
    <po-field-container-bottom
      [p-append-in-body]="appendBox"
      [p-help]="help"
      [p-disabled]="isDisabled"
      [p-error-pattern]="getErrorPattern()"
      [p-error-limit]="errorLimit"
      [p-size]="size"
    ></po-field-container-bottom>
  }
</po-field-container>
`
    }]
  }], () => [], {
    inp: [{
      type: ViewChild,
      args: ["inp", {
        static: true
      }]
    }],
    iconSearchAiEl: [{
      type: ViewChild,
      args: ["iconSearchAi"]
    }]
  });
})();
var KEYWORD_MAP = {
  and: "AND",
  or: "OR",
  not: "NOT",
  eq: "EQ",
  ne: "NE",
  gt: "GT",
  ge: "GE",
  lt: "LT",
  le: "LE",
  true: "BOOL",
  false: "BOOL",
  null: "NULL",
  contains: "CONTAINS",
  startswith: "STARTSWITH",
  endswith: "ENDSWITH",
  tolower: "TOLOWER",
  toupper: "TOUPPER"
};
var OPERAND_POSITION_TYPES = /* @__PURE__ */ new Set(["EQ", "NE", "GT", "GE", "LT", "LE", "LPAREN", "COMMA", "AND", "OR", "NOT", "START"]);
function tokenize(input2) {
  const tokens2 = [];
  let i2 = 0;
  while (i2 < input2.length) {
    if (/\s/.test(input2[i2])) {
      i2++;
      continue;
    }
    if (input2[i2] === "(") {
      tokens2.push({
        type: "LPAREN",
        value: "("
      });
      i2++;
      continue;
    }
    if (input2[i2] === ")") {
      tokens2.push({
        type: "RPAREN",
        value: ")"
      });
      i2++;
      continue;
    }
    if (input2[i2] === ",") {
      tokens2.push({
        type: "COMMA",
        value: ","
      });
      i2++;
      continue;
    }
    if (input2[i2] === "'") {
      i2++;
      let val = "";
      while (i2 < input2.length) {
        if (input2[i2] === "'" && input2[i2 + 1] === "'") {
          val += "'";
          i2 += 2;
        } else if (input2[i2] === "'") {
          i2++;
          break;
        } else {
          val += input2[i2++];
        }
      }
      tokens2.push({
        type: "STRING",
        value: val
      });
      continue;
    }
    if (input2[i2] === "-") {
      const prevType = tokens2.length > 0 ? tokens2.at(-1).type : "START";
      if (OPERAND_POSITION_TYPES.has(prevType) && /\d/.test(input2[i2 + 1] ?? "")) {
        let j = i2 + 1;
        while (j < input2.length && /[0-9.]/.test(input2[j])) {
          j++;
        }
        tokens2.push({
          type: "NUMBER",
          value: input2.slice(i2, j)
        });
        i2 = j;
        continue;
      }
    }
    if (/\d/.test(input2[i2])) {
      let j = i2;
      while (j < input2.length && /\d/.test(input2[j])) {
        j++;
      }
      const isDate = j - i2 === 4 && input2[j] === "-" && /\d{2}/.test(input2.slice(j + 1, j + 3)) && input2[j + 3] === "-" && /\d{2}/.test(input2.slice(j + 4, j + 6));
      if (isDate) {
        j += 6;
        tokens2.push({
          type: "DATE",
          value: input2.slice(i2, j)
        });
      } else {
        if (input2[j] === ".") {
          j++;
          while (j < input2.length && /\d/.test(input2[j])) {
            j++;
          }
        }
        tokens2.push({
          type: "NUMBER",
          value: input2.slice(i2, j)
        });
      }
      i2 = j;
      continue;
    }
    if (/[a-zA-Z_]/.test(input2[i2])) {
      let j = i2;
      while (j < input2.length && /\w/.test(input2[j])) {
        j++;
      }
      const word = input2.slice(i2, j);
      const kw = KEYWORD_MAP[word.toLowerCase()];
      tokens2.push(kw !== void 0 ? {
        type: kw,
        value: word
      } : {
        type: "IDENT",
        value: word
      });
      i2 = j;
      continue;
    }
    if (input2[i2] === "/") {
      const prev = tokens2.at(-1);
      if (prev?.type === "IDENT") {
        let j = i2 + 1;
        while (j < input2.length && /\w/.test(input2[j])) {
          j++;
        }
        prev.value += "/" + input2.slice(i2 + 1, j);
        i2 = j;
        continue;
      }
    }
    i2++;
  }
  tokens2.push({
    type: "EOF",
    value: ""
  });
  return tokens2;
}
function peek(tokens2, cur) {
  return tokens2[cur.pos].type;
}
function consume(tokens2, cur) {
  return tokens2[cur.pos++];
}
function expect(tokens2, cur, type) {
  const tk = consume(tokens2, cur);
  if (tk.type !== type) {
    throw new Error(`Expected ${type}, got ${tk.type} ('${tk.value}')`);
  }
  return tk;
}
function parseBoolExpr(tokens2, cur) {
  let left = parseAndExpr(tokens2, cur);
  while (peek(tokens2, cur) === "OR") {
    consume(tokens2, cur);
    const right = parseAndExpr(tokens2, cur);
    const l = left;
    const r = right;
    left = (item) => l(item) || r(item);
  }
  return left;
}
function parseAndExpr(tokens2, cur) {
  let left = parseNotExpr(tokens2, cur);
  while (peek(tokens2, cur) === "AND") {
    consume(tokens2, cur);
    const right = parseNotExpr(tokens2, cur);
    const l = left;
    const r = right;
    left = (item) => l(item) && r(item);
  }
  return left;
}
function parseNotExpr(tokens2, cur) {
  if (peek(tokens2, cur) === "NOT") {
    consume(tokens2, cur);
    const operand = parseNotExpr(tokens2, cur);
    return (item) => !operand(item);
  }
  return parsePrimary(tokens2, cur);
}
function parsePrimary(tokens2, cur) {
  if (peek(tokens2, cur) === "LPAREN") {
    consume(tokens2, cur);
    const expr = parseBoolExpr(tokens2, cur);
    expect(tokens2, cur, "RPAREN");
    return expr;
  }
  const tk = peek(tokens2, cur);
  if (tk === "CONTAINS" || tk === "STARTSWITH" || tk === "ENDSWITH") {
    const fn = tokens2[cur.pos].value.toLowerCase();
    consume(tokens2, cur);
    expect(tokens2, cur, "LPAREN");
    const fieldFn = parseValueExpr(tokens2, cur);
    expect(tokens2, cur, "COMMA");
    const valFn = parseValueExpr(tokens2, cur);
    expect(tokens2, cur, "RPAREN");
    return (item) => {
      const fieldVal = String(fieldFn(item) ?? "").toLowerCase();
      const searchVal = String(valFn(item) ?? "").toLowerCase();
      if (fn === "contains") {
        return fieldVal.includes(searchVal);
      }
      if (fn === "startswith") {
        return fieldVal.startsWith(searchVal);
      }
      return fieldVal.endsWith(searchVal);
    };
  }
  const leftFn = parseValueExpr(tokens2, cur);
  const op = parseCompOp(tokens2, cur);
  const rightFn = parseValueExpr(tokens2, cur);
  return (item) => applyCompOp(leftFn(item), op, rightFn(item));
}
function parseCompOp(tokens2, cur) {
  const tk = consume(tokens2, cur);
  switch (tk.type) {
    case "EQ":
      return "eq";
    case "NE":
      return "ne";
    case "GT":
      return "gt";
    case "GE":
      return "ge";
    case "LT":
      return "lt";
    case "LE":
      return "le";
    default:
      throw new Error(`Expected comparison operator, got ${tk.type} ('${tk.value}')`);
  }
}
function parseValueExpr(tokens2, cur) {
  const tk = peek(tokens2, cur);
  if (tk === "TOLOWER" || tk === "TOUPPER") {
    const fn = tokens2[cur.pos].value.toLowerCase();
    consume(tokens2, cur);
    expect(tokens2, cur, "LPAREN");
    const fieldToken = expect(tokens2, cur, "IDENT");
    expect(tokens2, cur, "RPAREN");
    const name = fieldToken.value;
    return (item) => {
      const v = String(resolveField(item, name) ?? "");
      return fn === "tolower" ? v.toLowerCase() : v.toUpperCase();
    };
  }
  if (tk === "IDENT") {
    const name = consume(tokens2, cur).value;
    return (item) => resolveField(item, name);
  }
  return parseLiteralFn(tokens2, cur);
}
function parseLiteralFn(tokens2, cur) {
  const tk = tokens2[cur.pos];
  consume(tokens2, cur);
  switch (tk.type) {
    case "STRING":
      return () => tk.value;
    case "NUMBER":
      return () => Number(tk.value);
    case "DATE":
      return () => new Date(tk.value);
    case "BOOL":
      return () => tk.value.toLowerCase() === "true";
    case "NULL":
      return () => null;
    default:
      throw new Error(`Unexpected token in value position: ${tk.type} ('${tk.value}')`);
  }
}
function resolveField(item, name) {
  const parts = name.split("/");
  let val = item;
  for (const part of parts) {
    if (val == null || typeof val !== "object") {
      return void 0;
    }
    val = val[part];
  }
  return val;
}
function applyCompOp(left, op, right) {
  if (op === "eq") {
    return looseEqual(left, right);
  }
  if (op === "ne") {
    return !looseEqual(left, right);
  }
  const cmp = compareValues(left, right);
  if (cmp === null) {
    return false;
  }
  if (op === "gt") {
    return cmp > 0;
  }
  if (op === "ge") {
    return cmp >= 0;
  }
  if (op === "lt") {
    return cmp < 0;
  }
  return cmp <= 0;
}
function looseEqual(a, b) {
  if (a === b) {
    return true;
  }
  if (a == null && b == null) {
    return true;
  }
  if (a == null || b == null) {
    return false;
  }
  if (typeof b === "number" && typeof a !== "number") {
    return Number(a) === b;
  }
  if (typeof a === "number" && typeof b !== "number") {
    return a === Number(b);
  }
  if (a instanceof Date && b instanceof Date) {
    return a.getTime() === b.getTime();
  }
  if (a instanceof Date && typeof b === "string") {
    return a.toISOString().startsWith(b);
  }
  if (b instanceof Date && typeof a === "string") {
    return b.toISOString().startsWith(a);
  }
  return String(a) === String(b);
}
function compareValues(a, b) {
  if (a == null || b == null) {
    return null;
  }
  if (a instanceof Date || b instanceof Date) {
    const da = a instanceof Date ? a : new Date(String(a));
    const db = b instanceof Date ? b : new Date(String(b));
    if (Number.isNaN(da.getTime()) || Number.isNaN(db.getTime())) {
      return null;
    }
    return da.getTime() - db.getTime();
  }
  const na = Number(a);
  const nb = Number(b);
  if (!Number.isNaN(na) && !Number.isNaN(nb)) {
    return na - nb;
  }
  return String(a).localeCompare(String(b));
}
function poSearchAiCompileFilter(filter3) {
  if (!filter3?.trim()) {
    return () => true;
  }
  try {
    const tokens2 = tokenize(filter3);
    const cur = {
      pos: 0
    };
    return parseBoolExpr(tokens2, cur);
  } catch (e2) {
    return () => true;
  }
}
function poSearchAiFilterItems(items, filter3) {
  if (!items?.length || !filter3?.trim()) {
    return items;
  }
  const predicate = poSearchAiCompileFilter(filter3);
  return items.filter(predicate);
}
var PoTableRowTemplateArrowDirection;
(function(PoTableRowTemplateArrowDirection2) {
  PoTableRowTemplateArrowDirection2["Left"] = "LEFT";
  PoTableRowTemplateArrowDirection2["Right"] = "RIGHT";
})(PoTableRowTemplateArrowDirection || (PoTableRowTemplateArrowDirection = {}));
var PoSearchFilterMode;
(function(PoSearchFilterMode2) {
  PoSearchFilterMode2[PoSearchFilterMode2["startsWith"] = 0] = "startsWith";
  PoSearchFilterMode2[PoSearchFilterMode2["contains"] = 1] = "contains";
  PoSearchFilterMode2[PoSearchFilterMode2["endsWith"] = 2] = "endsWith";
})(PoSearchFilterMode || (PoSearchFilterMode = {}));
var PoTableColumnSortType;
(function(PoTableColumnSortType2) {
  PoTableColumnSortType2["Ascending"] = "ascending";
  PoTableColumnSortType2["Descending"] = "descending";
})(PoTableColumnSortType || (PoTableColumnSortType = {}));
var PoTableService = class _PoTableService {
  http;
  headers = new HttpHeaders({
    "X-PO-No-Message": "true"
  });
  url;
  urlDelete;
  constructor(http) {
    this.http = http;
  }
  getFilteredItems(filteredParams) {
    const params = this.validateParams(filteredParams);
    return this.http.get(this.url, {
      headers: this.headers,
      params
    });
  }
  deleteItem(paramDelete, paramResponse) {
    const params = {
      [paramDelete]: paramResponse
    };
    return this.http.delete(this.urlDelete, {
      headers: this.headers,
      params
    });
  }
  setUrl(url, method) {
    if (method === "GET") {
      this.url = url;
    } else {
      this.urlDelete = url;
    }
  }
  scrollListener(componentListner) {
    return fromEvent(componentListner, "scroll").pipe(debounceTime(100));
  }
  validateParams(params) {
    return isTypeof(params, "object") && !Array.isArray(params) ? params : void 0;
  }
  static ɵfac = function PoTableService_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoTableService)(ɵɵinject(HttpClient));
  };
  static ɵprov = ɵɵdefineInjectable({
    token: _PoTableService,
    factory: _PoTableService.ɵfac
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoTableService, [{
    type: Injectable
  }], () => [{
    type: HttpClient
  }], null);
})();
var poTableContainer = ["border", "shadow"];
var poTableContainerDefault = "border";
var poTableParamDeleteApi = "id";
var poTableLiteralsDefault = {
  en: {
    noColumns: "Columns are not defined",
    noData: "No data found",
    noItem: "No selected item",
    oneItem: "1 selected item",
    multipleItems: "selected items",
    noVisibleColumn: "No visible column",
    loadingData: "Loading",
    loadMoreData: "Load more data",
    seeCompleteSubtitle: "See complete subtitle",
    completeSubtitle: "Complete subtitle",
    columnsManager: "Columns manager",
    bodyDelete: "Do you really want to delete this item?",
    cancel: "Cancel",
    delete: "Delete",
    deleteSuccessful: "Items removed successfully",
    deleteApiError: "An unexpected error occurred, please try again later",
    searchAiPlaceholder: "Describe what you are looking for and press Enter"
  },
  es: {
    noColumns: "Columnas no definidas",
    noData: "Datos no encontrados",
    noItem: "Ningún elemento seleccionado",
    oneItem: "1 elemento seleccionado",
    multipleItems: "elementos seleccionados",
    noVisibleColumn: "Sin columnas visibles",
    loadingData: "Cargando datos",
    loadMoreData: "Cargar más resultados",
    seeCompleteSubtitle: "Ver subtitulo completo",
    completeSubtitle: "Subtitulo completo",
    columnsManager: "Gerente de columna",
    bodyDelete: "¿Realmente desea eliminar este elemento?",
    cancel: "Cancelar",
    delete: "Borrar",
    deleteSuccessful: "Elementos eliminados con éxito",
    deleteApiError: "Ocurrió un error inesperado, inténtalo de nuevo más tarde",
    searchAiPlaceholder: "Describe lo que buscas y presiona Enter"
  },
  pt: {
    noColumns: "Nenhuma definição de colunas",
    noData: "Nenhum dado encontrado",
    noVisibleColumn: "Nenhuma coluna visível",
    noItem: "Nenhum item selecionado",
    oneItem: "1 item selecionado",
    multipleItems: "itens selecionados",
    loadingData: "Carregando",
    loadMoreData: "Carregar mais resultados",
    seeCompleteSubtitle: "Ver legenda completa",
    completeSubtitle: "Legenda completa",
    columnsManager: "Gerenciador de colunas",
    bodyDelete: "Deseja realmente excluir esse item?",
    cancel: "Cancelar",
    delete: "Excluir",
    deleteSuccessful: "Itens removidos com sucesso",
    deleteApiError: "Ocorreu um erro inesperado, tente novamente mais tarde!",
    searchAiPlaceholder: "Descreva o que procura e pressione Enter"
  },
  ru: {
    noColumns: "Нет определения столбца",
    noData: "Данные не найдены",
    noItem: "Нет выбранного элемента",
    oneItem: "1 элемент выбран",
    multipleItems: "выбранные элементы",
    noVisibleColumn: "нет видимых столбцов",
    loadingData: "Загрузка",
    loadMoreData: "Загрузка",
    seeCompleteSubtitle: "Посмотреть полный субтитр",
    completeSubtitle: "Полный заголовок",
    columnsManager: "менеджер колонок",
    bodyDelete: "Вы действительно хотите удалить этот элемент?",
    cancel: "Отмена",
    delete: "Удалить",
    deleteSuccessful: "Элементы успешно удалены",
    deleteApiError: "Произошла непредвиденная ошибка, повторите попытку позже",
    searchAiPlaceholder: "Опишите, что вы ищете, и нажмите Enter"
  }
};
var PoTableBaseComponent = class _PoTableBaseComponent {
  poDate;
  poTableService;
  /**
   * @optional
   *
   * @description
   *
   * Permite que o gerenciador de colunas, responsável pela definição de quais colunas serão exibidas, seja escondido.
   *
   * @default `false`
   */
  hideColumnsManager = false;
  /**
   * @optional
   *
   * @description
   *
   * Permite que as ações em lote, responsável por excluir e exibir a quantidade de itens, sejam escondidas.
   *
   * @default `true`
   */
  hideBatchActions = true;
  /**
   * @optional
   *
   * @description
   *
   * Habilita ou desabilita a quebra automática de texto. Quando ativada, o texto que excede
   * o espaço disponível é transferido para a próxima linha em pontos apropriados para uma
   * leitura clara.
   *
   * > Incompatível com `virtual-scroll`, que requer altura fixa nas linhas.
   *
   * @default `false`
   */
  textWrap = false;
  /**
   * @optional
   *
   * @description
   *
   * Permite que as ações para fixar uma coluna da tabela sejam escondidas.
   *
   * @default `false`
   */
  set hideActionFixedColumns(hide) {
    if (hide) {
      this.columns = this.removePropertyFixed(this.columns);
    }
    this._hideActionFixedColumns = hide;
  }
  get hideActionFixedColumns() {
    return this._hideActionFixedColumns;
  }
  /**
   * @optional
   *
   * @description
   *
   * Permite que o campo de pesquisa seja escondido.
   *
   * @default `true`
   */
  hideTableSearch = true;
  /**
   * @optional
   *
   * @description
   *
   * Permite fechar um detalhe ou row template automaticamente, ao abrir outro item.
   *
   * @default `false`
   */
  autoCollapse = false;
  /**
   * @optional
   *
   * @description
   *
   * Permite que seja adicionado o estado de carregamento no botão "Carregar mais resultados".
   *
   * @default `false`
   */
  loadingShowMore = false;
  /**
   * @optional
   *
   * @description
   *
   * Habilita em todas as colunas a opção de ordenação de dados. Caso a coluna seja do tipo 'data' ou 'dateTime' a
   * mesma deve respeitar os tipos de entrada definidos para que sejam ordenadas.
   *
   * @default `false`
   */
  sort = false;
  /**
   * @description
   *
   * Se verdadeiro, torna habilitado o botão "Carregar mais resultados".
   *
   * @default `false`
   */
  showMoreDisabled = false;
  /**
   * @description
   *
   * Habilita ou desabilita o estilo listrado da tabela (`striped`).
   * > Recomendado para tabelas com maior número de dados, facilitando a sua visualização na tabela.
   *
   * @default `false`
   */
  striped = false;
  /**
   * @description
   *
   * Esconde o *checkbox* para seleção de todas as linhas.
   *
   * > Sempre receberá *true* caso a seleção de apenas uma linha esteja ativa.
   *
   * @default `false`
   */
  hideSelectAll = false;
  /**
   * @description
   *
   * Define que somente uma linha da tabela pode ser selecionada.
   *
   * > Esta definição não se aplica aos itens filhos, os mesmos possuem comportamento independente do item pai.
   */
  singleSelect = false;
  /**
   * @description
   *
   * Permite selecionar um item da tabela clicando na linha.
   *
   * > Caso haja necessidade de selecionar o item apenas via radio ou checkbox, deve-se definir esta propriedade como `false`.
   *
   * @default `true`
   */
  selectableEntireLine = true;
  /**
   * @optional
   *
   * @description
   *
   * Define que a coluna de ações ficará no lado direito da tabela.
   *
   * @default `false`
   */
  actionRight = false;
  /**
   * @optional
   *
   * @description
   *
   * Define uma quantidade máxima de colunas que serão exibidas na tabela.
   *
   * Quando chegar no valor informado, as colunas que não estiverem selecionadas ficarão
   * desabilitadas e caso houver mais colunas visíveis do que o permitido, as excedentes
   * serão ignoradas por ordem de posição.
   */
  maxColumns;
  /**
   * @optional
   *
   * @description
   *
   * Define o modo de pesquisa utilizado no campo de busca, quando habilitado.
   * Valores definidos no enum: PoSearchFilterMode
   * > Obs: A pesquisa é realizada exclusivamente nos dados locais, ou seja, aqueles que foram
   * > renderizados na tabela.
   *
   * @default `startsWith`
   */
  filterType = PoSearchFilterMode.startsWith;
  /**
   * @optional
   *
   * @description
   *
   * Configura a busca por linguagem natural integrada à tabela, substituindo o campo de busca padrão
   * (`po-search`) pelo componente `po-search-ai` na barra de ações.
   *
   * Recebe um objeto do tipo `PoTableSearchAiField` com as configurações necessárias:
   * - `url` _(obrigatório)_: endpoint (proxy) de IA que traduz a consulta em linguagem natural para um filtro
   *   estruturado (OData v4). O backend deve seguir o contrato do
   *   [`po-sample-api`](https://github.com/po-ui/po-sample-api).
   * - `columns`: lista de colunas enviadas à IA; quando omitida, são derivadas de `p-columns`.
   * - `minConfidence`: confiança mínima para aplicação automática do filtro (padrão `0.5`).
   * - `timeout`: tempo máximo de espera pela resposta da IA em ms (padrão `10000`).
   * - `placeholder`: texto exibido como placeholder no campo.
   * - `literals`: literais customizadas do `po-search-ai`.
   * - `apply`: estratégia de aplicação do filtro — `'auto'` (padrão), `'parser'`, `'server'`, `'none'`
   *   ou uma função `(result: PoSearchAiResult) => void`.
   *
   * Quando esta propriedade está definida, os eventos `p-search-ai-result`, `p-search-ai-low-confidence`
   * e `p-search-ai-error` ficam disponíveis para tratamento customizado.
   */
  searchAiField = input(void 0, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "searchAiField"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-search-ai-field"
  }));
  /**
   * @optional
   *
   * @description
   * Evento executado quando todas as linhas são selecionadas por meio do *checkbox* que seleciona todas as linhas.
   */
  allSelected = new EventEmitter();
  /**
   * @optional
   *
   * @description
   * Evento executado quando a seleção das linhas é desmarcada por meio do *checkbox* que seleciona todas as linhas.
   */
  allUnselected = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Evento executado ao colapsar uma linha do `po-table`.
   *
   * > Como parâmetro o componente envia o item colapsado.
   */
  collapsed = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Evento executado ao expandir uma linha do `po-table`.
   *
   * > Como parâmetro o componente envia o item expandido.
   */
  expanded = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Evento executado após o método de exclusão ser finalizado.
   *
   * ```
   *<po-table
   *  (p-delete-items)="items = $event"
   * >
   *</po-table>
   * ```
   *
   *
   * > Como parâmetro o componente envia a lista atualizada, sem os itens excluídos.
   */
  eventDelete = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Evento executado ao selecionar uma linha do `po-table`.
   */
  selected = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Recebe uma ação de clique para o botão "Carregar mais resultados", caso nenhuma ação for definida o mesmo
   * não é visível.
   *
   * Recebe um objeto `{ column, type }` onde:
   *
   * - column (`PoTableColumn`): objeto da coluna que está ordenada.
   * - type (`PoTableColumnSortType`): tipo da ordenação.
   */
  showMore = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Evento executado ao ordenar colunas da tabela.
   *
   * Recebe um objeto `{ column, type }` onde:
   *
   * - column (`PoTableColumn`): objeto da coluna que foi clicada/ordenada.
   * - type (`PoTableColumnSortType`): tipo da ordenação.
   */
  sortBy = new EventEmitter();
  /**
   * @optional
   *
   * @description
   * Evento executado ao desmarcar a seleção de uma linha do `po-table`.
   */
  unselected = new EventEmitter();
  /**
   * @optional
   *
   * @description
   * Evento disparado ao fechar o page slide do gerenciador de colunas após alterar as colunas visíveis.
   *
   * O componente envia como parâmetro um array de string com as colunas visíveis atualizadas.
   * Por exemplo: ["idCard", "name", "hireStatus", "age"].
   */
  changeVisibleColumns = new EventEmitter();
  /**
   * @optional
   *
   * @description
   * Evento disparado ao alterar o estado de fixação de uma coluna no gerenciador de colunas.
   *
   * O componente envia como parâmetro um array de string com as propriedades das colunas fixas.
   * Por exemplo: ["name", "age"].
   *
   * > Incompatível com `p-hide-action-fixed-columns`. Quando esta propriedade estiver ativa, o evento não será disparado.
   *
   * @example
   *
   * ```html
   * <po-table
   *   [p-columns]="columns"
   *   [p-items]="items"
   *   (p-change-fixed-columns)="onFixedColumnsChange($event)">
   * </po-table>
   * ```
   */
  changeFixedColumns = output({
    alias: "p-change-fixed-columns"
  });
  /**
   * @optional
   *
   * @description
   * Evento disparado ao clicar no botão de restaurar padrão no gerenciador de colunas.
   *
   * O componente envia como parâmetro um array de string com as colunas configuradas inicialmente.
   * Por exemplo: ["idCard", "name", "hireStatus", "age"].
   */
  columnRestoreManager = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Evento emitido quando o `po-search-ai` retorna um resultado com confiança igual ou superior
   * ao `minConfidence` configurado em `p-search-ai-field`.
   *
   * O parâmetro enviado é um objeto `PoSearchAiResult` contendo:
   * - `filter`: string de filtro OData v4 gerada pela IA (ex: `"city eq 'SP' and salary gt 5000"`).
   * - `description`: descrição em linguagem natural do filtro aplicado.
   * - `confidence`: nível de confiança da resposta (0.0 a 1.0).
   *
   * > Quando `apply` for diferente de `'none'`, o filtro já é aplicado automaticamente pela tabela
   * > antes deste evento ser emitido.
   */
  searchAiResult = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Evento emitido quando o `po-search-ai` retorna um resultado cuja confiança é inferior ao
   * `minConfidence` configurado em `p-search-ai-field`. Nesse caso, o filtro **não** é aplicado
   * automaticamente.
   *
   * O parâmetro enviado é um objeto `PoSearchAiResult` com os mesmos campos de `p-search-ai-result`,
   * permitindo que o desenvolvedor decida como tratar o resultado de baixa confiança.
   */
  searchAiLowConfidence = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Evento emitido quando ocorre um erro na requisição ao endpoint de IA configurado em
   * `p-search-ai-field`.
   *
   * O parâmetro enviado é um objeto `PoSearchAiError` contendo:
   * - `statusCode`: código HTTP do erro (ex: `408` para timeout, `500` para erro interno).
   * - `message`: mensagem descritiva do erro.
   */
  searchAiError = new EventEmitter();
  allColumnsWidthPixels;
  columnMasterDetail;
  hasMainColumns = false;
  mainColumns = [];
  selectAll = false;
  sortedColumn = {
    property: null,
    ascending: true
  };
  subtitleColumns = [];
  page = 1;
  pageSize = 10;
  hasService = false;
  initialColumns;
  showBatchActions = false;
  itemsSelected = [];
  paramsFilter = {};
  filteredItems = [];
  initialized = false;
  fixedLayout = false;
  initialVisibleColumns = false;
  _componentsSize = void 0;
  _initialComponentsSize = void 0;
  _spacing;
  _initialSpacing = void 0;
  _filteredColumns;
  _actions = [];
  _columns = [];
  _container;
  _paramDelete = poTableParamDeleteApi;
  _height;
  _hideDetail = false;
  _items;
  _literals;
  _loading = false;
  _selectable;
  language = poLocaleDefault;
  _serviceApi;
  _serviceDeleteApi;
  poTableServiceSubscription;
  sortStore;
  _infiniteScrollDistance = 100;
  _infiniteScroll = false;
  _draggable = false;
  _hideActionFixedColumns = false;
  _virtualScroll = true;
  constructor(poDate, languageService, poTableService) {
    this.poDate = poDate;
    this.poTableService = poTableService;
    this.language = languageService.getShortLanguage();
  }
  /**
   * @optional
   *
   * @description
   *
   * Define o tamanho dos componentes de formulário no table:
   * - `small`: aplica a medida small de cada componente (disponível apenas para acessibilidade AA).
   * - `medium`: aplica a medida medium de cada componente.
   *
   * > Caso a acessibilidade AA não esteja configurada, o tamanho `medium` será mantido.
   * Para mais detalhes, consulte a documentação do [po-theme](https://po-ui.io/documentation/po-theme).
   *
   * @default `medium`
   */
  set componentsSize(value) {
    this._initialComponentsSize = value;
    this.applySizeBasedOnA11y();
  }
  get componentsSize() {
    return this._componentsSize ?? getDefaultSizeFn(PoFieldSize);
  }
  /**
   * @description
   *
   * Lista de itens da tabela.
   * > Se falso, será inicializado como um *array* vazio.
   */
  set items(items) {
    if (this.height) {
      this._items = Array.isArray(items) ? [...items] : [];
    } else {
      this._items = Array.isArray(items) ? items : [];
    }
    if (!this.hasItems) {
      this.selectAll = false;
    } else if (!this.hasColumns) {
      this.columns = this.getDefaultColumns(items[0]);
    }
    this.filteredItems = this.height ? [...this.items] : this.items;
    this.reapplySort();
    setTimeout(() => this.checkInfiniteScroll());
  }
  get items() {
    return this._items;
  }
  /**
   * @optional
   *
   * @description
   *
   * Lista das colunas da tabela, deve receber um *array* de objetos que implementam a interface `PoTableColumn`.
   * Por padrão receberá como valor a primeira coluna da lista de itens da tabela.
   * > Caso não encontre valor, a mensagem 'Nenhuma definição de colunas' será exibida.
   *
   */
  set columns(columns) {
    const hasColumnsWithVisible = columns?.find((column) => column.visible === true);
    if (this.initialColumns === void 0) {
      this.initialColumns = columns;
    }
    this._columns = columns || [];
    if (hasColumnsWithVisible && !this.initialVisibleColumns) {
      this.initialVisibleColumns = true;
    }
    if (this._columns.length) {
      this.setColumnLink();
    } else if (this.hasItems) {
      this._columns = this.getDefaultColumns(this.items[0]);
    }
    this.onChangeColumns();
  }
  get columns() {
    return this._columns;
  }
  /**
   * @optional
   *
   * @description
   *
   * Adiciona um contorno arredondado ao `po-table`, as opções são:
   * - `border`: com bordas/linhas.
   * - `shadow`: com sombras.
   *
   * @default `border`
   */
  set container(value) {
    this._container = poTableContainer.includes(value) ? value : poTableContainerDefault;
  }
  get container() {
    return this._container;
  }
  /**
   * @optional
   *
   * @description
   *
   * Adiciona o parâmetro a ser enviado para a requisição de DELETE.
   *
   * É necessário a utilização da propriedade `p-service-delete` em conjunto.
   *
   * @default `id`
   */
  set paramDeleteApi(value) {
    this._paramDelete = value && typeof value === "string" ? value : poTableParamDeleteApi;
  }
  get paramDeleteApi() {
    return this._paramDelete;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define a altura da tabela em *pixels* e fixa o cabeçalho.
   *
   * Ao utilizar essa propriedade será inserido o `virtual-scroll` na tabela melhorando a performance.
   */
  set height(height) {
    this._height = height;
  }
  get height() {
    return this._height;
  }
  /**
   * @optional
   *
   * @description
   *
   * Habilita a visualização da lista de detalhes de cada linha da coluna.
   *
   * @default `false`
   */
  set hideDetail(hideDetail) {
    this._hideDetail = hideDetail != null && hideDetail.toString() === "" ? true : convertToBoolean(hideDetail);
  }
  get hideDetail() {
    return this._hideDetail;
  }
  /**
   * @optional
   *
   * @description
   *
   * Objeto com as literais usadas no `po-table`.
   *
   * Existem duas maneiras de customizar o componente, passando um objeto com todas as literais disponíveis:
   *
   * ```
   *  const customLiterals: PoTableLiterals = {
   *    noColumns: 'Nenhuma definição de colunas',
   *    noData: 'Nenhum dado encontrado',
   *    noVisibleColumn: 'Nenhuma coluna visível',
   *    noItem: 'Nenhum item selecionado',
   *    oneItem: '1 item selecionado',
   *    multipleItems: 'itens selecionados',
   *    loadingData: 'Carregando',
   *    loadMoreData: 'Carregar mais resultados',
   *    seeCompleteSubtitle: 'Ver legenda completa',
   *    completeSubtitle: 'Legenda completa',
   *    columnsManager: 'Gerenciador de colunas',
   *    bodyDelete: 'Deseja realmente excluir esse item?',
   *    cancel: 'Cancelar',
   *    delete: 'Excluir',
   *    deleteSuccessful: 'Itens removidos com sucesso',
   *    deleteApiError: 'Ocorreu um erro inesperado, tente novamente mais tarde!',
   *  };
   * ```
   *
   * Ou passando apenas as literais que deseja customizar:
   *
   * ```
   *  const customLiterals: PoTableLiterals = {
   *    noData: 'Sem dados'
   *  };
   * ```
   *
   * E para carregar as literais customizadas, basta apenas passar o objeto para o componente.
   *
   * ```
   * <po-table
   *   [p-literals]="customLiterals">
   * </po-table>
   * ```
   *
   * > O objeto padrão de literais será traduzido de acordo com o idioma do
   * [`PoI18nService`](/documentation/po-i18n) ou do browser.
   */
  set literals(value) {
    if (value instanceof Object && !(value instanceof Array)) {
      this._literals = __spreadValues(__spreadValues(__spreadValues({}, poTableLiteralsDefault[poLocaleDefault]), poTableLiteralsDefault[this.language]), value);
    } else {
      this._literals = poTableLiteralsDefault[this.language];
    }
  }
  get literals() {
    return this._literals || poTableLiteralsDefault[this.language];
  }
  /**
   * @optional
   *
   * @description
   *
   * Bloqueia a interação do usuário com os dados da _table_.
   *
   * @default `false`
   */
  set loading(loading) {
    this._loading = convertToBoolean(loading);
  }
  get loading() {
    return this._loading;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define uma lista de ações.
   *
   * Quando houver apenas uma ação definida ela será exibida diretamente na coluna, caso contrário, o componente
   * se encarrega de agrupá-las exibindo o ícone [**an an-dots-three**](https://po-ui.io/icons) que listará as ações ao ser clicado.
   *
   * **A coluna de ações não será exibida quando:**
   *  - a lista conter valores inválidos ou indefinidos.
   *  - tenha uma única ação e a mesma não for visível.
   */
  set actions(actions2) {
    this._actions = actions2;
  }
  get actions() {
    return this._actions;
  }
  /**
   * @optional
   *
   * @description
   *
   * Permite a seleção de linhas na tabela e, caso a propriedade `p-single-select` esteja definida será possível
   * selecionar apenas uma única linha.
   *
   * **Importante:**
   *  - As linhas de detalhe definidas em `PoTableDetail` possuem comportamento independente da linha mestre;
   *  - Cada linha possui por padrão a propriedade dinâmica `$selected`, na qual é possível validar se a linha
   * está selecionada, por exemplo: `item.$selected` ou `item['$selected']`.
   *
   * @default `false`
   */
  set selectable(value) {
    this._selectable = value === "" ? true : convertToBoolean(value);
  }
  get selectable() {
    return this._selectable;
  }
  /**
   * @optional
   *
   * @description
   *
   * Se verdadeiro, ativa a funcionalidade de scroll infinito para a tabela e o botão "Carregar Mais" deixará de ser exibido. Ao chegar no fim da tabela
   * executará a função `p-show-more`.
   *
   * **Regras de utilização:**
   *  - O scroll infinito só funciona para tabelas que utilizam a propriedade `p-height` e que possuem o scroll já na carga inicial dos dados.
   *
   * @default `false`
   */
  set infiniteScroll(value) {
    this._infiniteScroll = convertToBoolean(value && this.height > 0);
  }
  get infiniteScroll() {
    return this._infiniteScroll;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define o percentual necessário para disparar o evento `p-show-more`, que é responsável por carregar mais dados na tabela. Caso o valor informado seja maior que 100 ou menor
   * que 0, o valor padrão será 100%
   *
   * **Exemplos:**
   *  - p-infinite-scroll-distance = 80: Quando atingir 80%  do scroll da tabela, o `p-show-more` será disparado.
   */
  set infiniteScrollDistance(value) {
    this._infiniteScrollDistance = value > 100 || value < 0 ? 100 : value;
  }
  get infiniteScrollDistance() {
    return this._infiniteScrollDistance;
  }
  /**
   * @optional
   *
   * @description
   *
   * URL da API responsável por retornar os registros.
   *
   * Ao realizar a busca de mais registros via paginação (Carregar mais resultados), será enviado os parâmetros `page` e `pageSize`, conforme abaixo:
   *
   * ```
   * url + ?page=1&pageSize=10
   * ```
   *
   * Caso utilizar ordenação, a coluna ordenada será enviada através do parâmetro `order`, por exemplo:
   * - Coluna decrescente:
   *   ```
   *    url + ?page=1&pageSize=10&order=-name
   *   ```
   *
   * - Coluna ascendente:
   *   ```
   *    url + ?page=1&pageSize=10&order=name
   *   ```
   *
   * > Esta URL deve retornar e receber os dados no padrão de [API do PO UI](https://po-ui.io/guides/api).
   */
  set serviceApi(service) {
    this._serviceApi = service;
    this.setService(this.serviceApi, "GET");
    this.hasService = !!service;
    this.showMoreDisabled = !this.hasService;
    this.page = 1;
    this.initializeData();
  }
  get serviceApi() {
    return this._serviceApi;
  }
  /**
   * @optional
   *
   * @description
   *
   * URL da API responsável por excluir os registros.
   *
   * Ao selecionar o botão de excluir itens, essa url será executada utilizando o parâmetro enviado na propriedade `p-param-delete-api`.
   * Caso ela não seja utilizada, o parâmetro padrão a ser enviado será `id`.
   *
   * > Esta URL deve retornar e receber os dados no padrão de [API do PO UI](https://po-ui.io/guides/api).
   */
  set serviceDeleteApi(service) {
    this._serviceDeleteApi = service;
    this.setService(this.serviceDeleteApi, "DELETE");
  }
  get serviceDeleteApi() {
    return this._serviceDeleteApi;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define o espaçamento interno das células, impactando diretamente na altura das linhas do table. Os valores
   * permitidos são definidos pelo enum **PoTableColumnSpacing**.
   *
   * > Em nível de acessibilidade **AA**, caso o valor de `p-spacing` não seja definido, o valor padrão será `extraSmall`
   * > nos seguintes cenários:
   * > - Quando o valor de `p-components-size` for `small`;
   * > - Quando o valor padrão dos componentes for configurado como `small` no
   * > [serviço de tema](https://po-ui.io/documentation/po-theme).
   *
   * @default `medium`
   */
  set spacing(value) {
    this._initialSpacing = value;
    this.applySpacingBasedOnA11y();
  }
  get spacing() {
    return this._spacing ?? this.getDefaultSpacing();
  }
  /**
   * @optional
   *
   * @description
   *
   * Define as colunas que serão filtradas no campo de pesquisa.
   * Aceita um array de strings, representando as colunas específicas que serão consideradas na filtragem.
   *
   */
  set filteredColumns(values) {
    this._filteredColumns = values;
  }
  get filteredColumns() {
    return this._filteredColumns;
  }
  get hasColumns() {
    return this.columns && this.columns.length > 0;
  }
  get hasItems() {
    return !!(this.items && this.items.length);
  }
  get nameColumnDetail() {
    return this.columnMasterDetail ? this.columnMasterDetail.property : null;
  }
  get validColumns() {
    const typesValid = ["string", "number", "boolean", "date", "time", "dateTime", "currency", "subtitle", "link", "label", "icon", "cellTemplate", "columnTemplate"];
    return this.columns.filter((col) => !col.type || typesValid.includes(col.type));
  }
  get visibleActions() {
    return this.actions !== void 0 && this.actions && this.actions.filter((action) => action && action.visible !== false);
  }
  getFilteredColumns() {
    this.filteredColumns = this.columns.filter((column) => column.visible !== false).map((column) => column.property || column.label);
  }
  get sortType() {
    return this.sortedColumn.ascending ? PoTableColumnSortType.Ascending : PoTableColumnSortType.Descending;
  }
  /**
   * @optional
   *
   * @description
   *
   * Habilita o modo drag and drop para as colunas da tabela.
   *
   * @default `false`
   */
  set draggable(draggable) {
    this._draggable = draggable || false;
  }
  get draggable() {
    return this._draggable;
  }
  /**
   * @optional
   *
   * @description
   *
   * Habilita o `virtual-scroll` na tabela para melhorar a performance com grandes volumes de dados.
   * Requer altura (`p-height`) para funcionar corretamente.
   *
   * > Incompatível com `p-text-wrap` e `master-detail`, pois o `virtual-scroll` exige altura fixa nas linhas.
   *
   * @default `true`
   */
  set virtualScroll(value) {
    this._virtualScroll = convertToBoolean(value && this.height > 0);
  }
  get virtualScroll() {
    return this._virtualScroll;
  }
  ngOnDestroy() {
    this.poTableServiceSubscription?.unsubscribe();
  }
  ngOnChanges(changes) {
    if (this.singleSelect || this.hideSelectAll) {
      this.selectAll = false;
      this.hideSelectAll = true;
    }
    if (changes.height) {
      this.calculateHeightTableContainer(this.height);
    }
    if ((changes.height || changes.items) && this.initialized) {
      this.changeHeaderWidth();
    }
    this.changeSizeLoading();
  }
  onThemeChange() {
    this.applySizeBasedOnA11y();
    this.applySpacingBasedOnA11y();
  }
  selectAllRows() {
    if (!this.hideSelectAll) {
      this.selectAll = !this.selectAll;
      this.items.forEach((item) => {
        item.$selected = this.selectAll;
        if (item[this.nameColumnDetail]) {
          const childItems = item[this.nameColumnDetail];
          childItems.forEach((childItem) => childItem.$selected = this.selectAll);
        }
      });
      this.emitSelectAllEvents(this.selectAll, [...this.items]);
      this.setSelectedList();
    }
  }
  selectRow(row) {
    row.$selected = !row.$selected;
    this.emitSelectEvents(row);
    this.configAfterSelectRow(this.items, row);
    if (row[this.nameColumnDetail] && (row.$selected === true || row.$selected === false)) {
      const childItems = row[this.nameColumnDetail];
      childItems.forEach((item) => item.$selected = row.$selected);
    }
    this.setSelectedList();
  }
  hasSelectableRow() {
    return this.selectable && this.selectableEntireLine;
  }
  selectDetailRow(event) {
    const {
      item,
      parentRow
    } = event;
    this.emitSelectEvents(item);
    this.updateParentRowSelection(parentRow);
  }
  updateParentRowSelection(parentRow) {
    const old = parentRow.$selected || null;
    const childItems = parentRow[this.nameColumnDetail];
    if (childItems.every((item) => item.$selected)) {
      parentRow.$selected = true;
    } else if (childItems.every((item) => !item.$selected)) {
      parentRow.$selected = false;
    } else {
      parentRow.$selected = null;
    }
    if (old != parentRow.$selected && !(old == null && parentRow.$selected === false)) {
      this.emitSelectEvents(parentRow);
    }
    this.configAfterSelectRow(this.items, parentRow);
    this.setSelectedList();
  }
  setSelectedList() {
    this.itemsSelected = [];
    this.items.forEach((item) => {
      if (item.$selected) {
        this.itemsSelected.push(item);
      }
    });
  }
  getClassColor(row, column) {
    return column.color ? `po-text-${this.getColumnColor(row, column)}` : "";
  }
  toggleDetail(row) {
    const rowShowDetail = row.$showDetail;
    if (this.autoCollapse) {
      this.collapseAllItems(this.items);
    }
    this.setShowDetail(row, !rowShowDetail);
    this.emitExpandEvents(row);
  }
  toggleRowAction(row) {
    const toggleShowAction = row.$showAction;
    this.items.forEach((item) => {
      if (item.$showAction) {
        item.$showAction = false;
      }
    });
    row.$showAction = !toggleShowAction;
  }
  sortColumn(column) {
    if (!this.sort || column.type === "detail" || column.sortable === false) {
      return;
    }
    this.sortedColumn.ascending = this.sortedColumn.property === column ? !this.sortedColumn.ascending : true;
    this.sortArray(column, this.sortedColumn.ascending);
    this.sortBy.emit({
      column,
      type: this.sortType
    });
    if (this.hasService && this.sort) {
      this.sortStore = {
        column,
        type: this.sortType
      };
    }
    this.sortedColumn.property = column;
  }
  onShowMore() {
    const sort3 = this.sortedColumn.property ? {
      column: this.sortedColumn.property,
      type: this.sortType
    } : void 0;
    if (this.hasService) {
      this.page++;
      this.loading = true;
      this.loadingShowMore = true;
      this.poTableServiceSubscription = this.getFilteredItems().subscribe((data) => {
        this.items = [...this.items, ...data.items];
        this.showMoreDisabled = !data.hasNext;
        this.loading = false;
        this.loadingShowMore = false;
      });
    }
    this.showMore.emit(sort3);
  }
  getFilteredItems(queryParams) {
    const filteredParams = this.getFilteredParams(queryParams);
    return this.poTableService.getFilteredItems(filteredParams);
  }
  setTableResponseProperties(data) {
    this.items = data.items || [];
    this.showMoreDisabled = !data.hasNext;
    this.loading = false;
  }
  initializeData(params) {
    if (this.hasService) {
      this.loading = true;
      this.paramsFilter = params;
      this.getFilteredItems(params).subscribe((data) => {
        this.setTableResponseProperties(data);
      });
    }
  }
  sortArray(column, ascending, item) {
    let itemsList;
    if (item) {
      itemsList = this.height ? [...item] : item;
    } else {
      itemsList = this.height ? [...this.filteredItems] : this.filteredItems;
    }
    itemsList.sort((leftSide, rightSide) => PoUtils.sortValues(leftSide[column.property], rightSide[column.property], ascending));
    this.filteredItems = itemsList;
  }
  getDefaultColumns(item) {
    const keys2 = Object.keys(item);
    return keys2.filter((key2) => typeof item[key2] !== "object").map((key2) => ({
      label: capitalizeFirstLetter(key2),
      property: key2
    }));
  }
  setShowDetail(rowIdentifier, isShowDetail) {
    const isRowIndex = typeof rowIdentifier === "number" && this.items[rowIdentifier];
    const row = isRowIndex ? this.items[rowIdentifier] : rowIdentifier;
    row.$showDetail = isShowDetail;
  }
  collapseAllItems(items) {
    for (const item of items) {
      if (item.$showDetail) {
        this.setShowDetail(item, false);
        this.emitExpandEvents(item);
      }
    }
  }
  configAfterSelectRow(rows, row) {
    if (this.singleSelect) {
      this.unselectOtherRows(rows, row);
    } else if (!this.hideSelectAll) {
      this.selectAll = this.isEverySelected(rows);
    }
  }
  emitExpandEvents(row) {
    row.$showDetail ? this.expanded.emit(row) : this.collapsed.emit(row);
  }
  emitSelectAllEvents(selectAll, rows) {
    selectAll ? this.allSelected.emit(rows) : this.allUnselected.emit(rows);
  }
  emitSelectEvents(row) {
    row.$selected ? this.selected.emit(row) : this.unselected.emit(row);
  }
  getColumnColor(row, column) {
    const columnColor = column.color;
    return isTypeof(columnColor, "function") ? columnColor(row, column.property) : columnColor;
  }
  // Retorna a coluna da lista de colunas que é do tipo detail
  getColumnMasterDetail() {
    return this.columns.find((col) => col.type === "detail");
  }
  // Colunas que são inseridas no <head> da tabela
  getMainColumns() {
    return this.validColumns.filter((col) => col.visible !== false);
  }
  // Retorna as colunas com status
  getSubtitleColumns() {
    return this.columns.filter((col) => col.type === "subtitle");
  }
  isEverySelected(items) {
    const someCheckedOrIndeterminate = (item) => item.$selected || item.$selected === null;
    const everyChecked = (item) => item.$selected;
    if (items.every(everyChecked)) {
      return true;
    }
    if (items.some(someCheckedOrIndeterminate)) {
      return null;
    }
    return false;
  }
  onChangeColumns() {
    this.setMainColumns();
    this.setColumnMasterDetail();
    this.setSubtitleColumns();
    this.getFilteredColumns();
  }
  setColumnLink() {
    this.columns.forEach((column) => {
      if (column["type"] === "link" && !column["link"]) {
        column["link"] = "link";
      }
    });
  }
  setColumnMasterDetail() {
    this.columnMasterDetail = this.getColumnMasterDetail();
  }
  setMainColumns() {
    this.mainColumns = this.getMainColumns();
    this.hasMainColumns = !!this.mainColumns.length;
    this.allColumnsWidthPixels = this.verifyWidthColumnsPixels();
  }
  setSubtitleColumns() {
    this.subtitleColumns = this.getSubtitleColumns();
  }
  unselectOtherRows(rows, row) {
    rows.forEach((item) => {
      if (item !== row) {
        item.$selected = false;
      }
    });
  }
  verifyWidthColumnsPixels() {
    return this.hasMainColumns ? this.mainColumns.every((column) => column.width && column.width.includes("px")) : false;
  }
  setService(service, method) {
    if (service && isTypeof(service, "string")) {
      this.poTableService.setUrl(service, method);
    }
  }
  getFilteredParams(queryParams) {
    const {
      page,
      pageSize,
      sortStore
    } = this;
    const filteredParams = {};
    const order = this.getOrderParam(sortStore);
    const params = __spreadValues({
      page,
      pageSize,
      order
    }, queryParams);
    for (const key2 in params) {
      if (params.hasOwnProperty(key2) && params[key2] !== void 0) {
        filteredParams[key2] = params[key2];
      }
    }
    return filteredParams;
  }
  getOrderParam(sort3 = {
    type: void 0
  }) {
    const {
      column,
      type
    } = sort3;
    if (!column) {
      return;
    }
    if (type === PoTableColumnSortType.Descending) {
      return `-${column.property}`;
    }
    return `${column.property}`;
  }
  removePropertyFixed(arr) {
    return arr.map((obj) => {
      if (obj.hasOwnProperty("fixed")) {
        obj.fixed = false;
      }
      return obj;
    });
  }
  applySizeBasedOnA11y() {
    const size = validateSizeFn(this._initialComponentsSize, PoFieldSize);
    this._componentsSize = size;
    this.calculateHeightTableContainer(this.height);
  }
  applySpacingBasedOnA11y() {
    if (Object.values(PoTableColumnSpacing).includes(this._initialSpacing)) {
      this._spacing = this._initialSpacing;
    } else {
      this._spacing = this.getDefaultSpacing();
    }
  }
  static ɵfac = function PoTableBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoTableBaseComponent)(ɵɵdirectiveInject(PoDateService), ɵɵdirectiveInject(PoLanguageService), ɵɵdirectiveInject(PoTableService));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoTableBaseComponent,
    hostVars: 2,
    hostBindings: function PoTableBaseComponent_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("PoUiThemeChange", function PoTableBaseComponent_PoUiThemeChange_HostBindingHandler() {
          return ctx.onThemeChange();
        }, ɵɵresolveWindow);
      }
      if (rf & 2) {
        ɵɵattribute("p-components-size", ctx.componentsSize)("p-spacing", ctx.spacing);
      }
    },
    inputs: {
      hideColumnsManager: [2, "p-hide-columns-manager", "hideColumnsManager", convertToBoolean],
      hideBatchActions: [2, "p-hide-batch-actions", "hideBatchActions", convertToBoolean],
      textWrap: [2, "p-text-wrap", "textWrap", convertToBoolean],
      hideActionFixedColumns: [0, "p-hide-action-fixed-columns", "hideActionFixedColumns"],
      hideTableSearch: [2, "p-hide-table-search", "hideTableSearch", convertToBoolean],
      autoCollapse: [2, "p-auto-collapse", "autoCollapse", convertToBoolean],
      loadingShowMore: [2, "p-loading-show-more", "loadingShowMore", convertToBoolean],
      sort: [2, "p-sort", "sort", convertToBoolean],
      showMoreDisabled: [2, "p-show-more-disabled", "showMoreDisabled", convertToBoolean],
      striped: [2, "p-striped", "striped", convertToBoolean],
      hideSelectAll: [2, "p-hide-select-all", "hideSelectAll", convertToBoolean],
      singleSelect: [2, "p-single-select", "singleSelect", convertToBoolean],
      selectableEntireLine: [2, "p-selectable-entire-line", "selectableEntireLine", convertToBoolean],
      actionRight: [2, "p-actions-right", "actionRight", convertToBoolean],
      maxColumns: [0, "p-max-columns", "maxColumns"],
      filterType: [0, "p-filter-type", "filterType"],
      searchAiField: [1, "p-search-ai-field", "searchAiField"],
      componentsSize: [0, "p-components-size", "componentsSize"],
      items: [0, "p-items", "items"],
      columns: [0, "p-columns", "columns"],
      container: [0, "p-container", "container"],
      paramDeleteApi: [0, "p-param-delete-api", "paramDeleteApi"],
      height: [0, "p-height", "height"],
      hideDetail: [0, "p-hide-detail", "hideDetail"],
      literals: [0, "p-literals", "literals"],
      loading: [0, "p-loading", "loading"],
      actions: [0, "p-actions", "actions"],
      selectable: [0, "p-selectable", "selectable"],
      infiniteScroll: [0, "p-infinite-scroll", "infiniteScroll"],
      infiniteScrollDistance: [0, "p-infinite-scroll-distance", "infiniteScrollDistance"],
      serviceApi: [0, "p-service-api", "serviceApi"],
      serviceDeleteApi: [0, "p-service-delete", "serviceDeleteApi"],
      spacing: [0, "p-spacing", "spacing"],
      filteredColumns: [0, "p-filtered-columns", "filteredColumns"],
      draggable: [0, "p-draggable", "draggable"],
      virtualScroll: [0, "p-virtual-scroll", "virtualScroll"]
    },
    outputs: {
      allSelected: "p-all-selected",
      allUnselected: "p-all-unselected",
      collapsed: "p-collapsed",
      expanded: "p-expanded",
      eventDelete: "p-delete-items",
      selected: "p-selected",
      showMore: "p-show-more",
      sortBy: "p-sort-by",
      unselected: "p-unselected",
      changeVisibleColumns: "p-change-visible-columns",
      changeFixedColumns: "p-change-fixed-columns",
      columnRestoreManager: "p-restore-column-manager",
      searchAiResult: "p-search-ai-result",
      searchAiLowConfidence: "p-search-ai-low-confidence",
      searchAiError: "p-search-ai-error"
    },
    features: [ɵɵNgOnChangesFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoTableBaseComponent, [{
    type: Directive
  }], () => [{
    type: PoDateService
  }, {
    type: PoLanguageService
  }, {
    type: PoTableService
  }], {
    hideColumnsManager: [{
      type: Input,
      args: [{
        alias: "p-hide-columns-manager",
        transform: convertToBoolean
      }]
    }],
    hideBatchActions: [{
      type: Input,
      args: [{
        alias: "p-hide-batch-actions",
        transform: convertToBoolean
      }]
    }],
    textWrap: [{
      type: Input,
      args: [{
        alias: "p-text-wrap",
        transform: convertToBoolean
      }]
    }],
    hideActionFixedColumns: [{
      type: Input,
      args: ["p-hide-action-fixed-columns"]
    }],
    hideTableSearch: [{
      type: Input,
      args: [{
        alias: "p-hide-table-search",
        transform: convertToBoolean
      }]
    }],
    autoCollapse: [{
      type: Input,
      args: [{
        alias: "p-auto-collapse",
        transform: convertToBoolean
      }]
    }],
    loadingShowMore: [{
      type: Input,
      args: [{
        alias: "p-loading-show-more",
        transform: convertToBoolean
      }]
    }],
    sort: [{
      type: Input,
      args: [{
        alias: "p-sort",
        transform: convertToBoolean
      }]
    }],
    showMoreDisabled: [{
      type: Input,
      args: [{
        alias: "p-show-more-disabled",
        transform: convertToBoolean
      }]
    }],
    striped: [{
      type: Input,
      args: [{
        alias: "p-striped",
        transform: convertToBoolean
      }]
    }],
    hideSelectAll: [{
      type: Input,
      args: [{
        alias: "p-hide-select-all",
        transform: convertToBoolean
      }]
    }],
    singleSelect: [{
      type: Input,
      args: [{
        alias: "p-single-select",
        transform: convertToBoolean
      }]
    }],
    selectableEntireLine: [{
      type: Input,
      args: [{
        alias: "p-selectable-entire-line",
        transform: convertToBoolean
      }]
    }],
    actionRight: [{
      type: Input,
      args: [{
        alias: "p-actions-right",
        transform: convertToBoolean
      }]
    }],
    maxColumns: [{
      type: Input,
      args: ["p-max-columns"]
    }],
    filterType: [{
      type: Input,
      args: ["p-filter-type"]
    }],
    searchAiField: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-search-ai-field",
        required: false
      }]
    }],
    allSelected: [{
      type: Output,
      args: ["p-all-selected"]
    }],
    allUnselected: [{
      type: Output,
      args: ["p-all-unselected"]
    }],
    collapsed: [{
      type: Output,
      args: ["p-collapsed"]
    }],
    expanded: [{
      type: Output,
      args: ["p-expanded"]
    }],
    eventDelete: [{
      type: Output,
      args: ["p-delete-items"]
    }],
    selected: [{
      type: Output,
      args: ["p-selected"]
    }],
    showMore: [{
      type: Output,
      args: ["p-show-more"]
    }],
    sortBy: [{
      type: Output,
      args: ["p-sort-by"]
    }],
    unselected: [{
      type: Output,
      args: ["p-unselected"]
    }],
    changeVisibleColumns: [{
      type: Output,
      args: ["p-change-visible-columns"]
    }],
    changeFixedColumns: [{
      type: Output,
      args: ["p-change-fixed-columns"]
    }],
    columnRestoreManager: [{
      type: Output,
      args: ["p-restore-column-manager"]
    }],
    searchAiResult: [{
      type: Output,
      args: ["p-search-ai-result"]
    }],
    searchAiLowConfidence: [{
      type: Output,
      args: ["p-search-ai-low-confidence"]
    }],
    searchAiError: [{
      type: Output,
      args: ["p-search-ai-error"]
    }],
    componentsSize: [{
      type: Input,
      args: ["p-components-size"]
    }, {
      type: HostBinding,
      args: ["attr.p-components-size"]
    }],
    items: [{
      type: Input,
      args: ["p-items"]
    }],
    columns: [{
      type: Input,
      args: ["p-columns"]
    }],
    container: [{
      type: Input,
      args: ["p-container"]
    }],
    paramDeleteApi: [{
      type: Input,
      args: ["p-param-delete-api"]
    }],
    height: [{
      type: Input,
      args: ["p-height"]
    }],
    hideDetail: [{
      type: Input,
      args: ["p-hide-detail"]
    }],
    literals: [{
      type: Input,
      args: ["p-literals"]
    }],
    loading: [{
      type: Input,
      args: ["p-loading"]
    }],
    actions: [{
      type: Input,
      args: ["p-actions"]
    }],
    selectable: [{
      type: Input,
      args: ["p-selectable"]
    }],
    infiniteScroll: [{
      type: Input,
      args: ["p-infinite-scroll"]
    }],
    infiniteScrollDistance: [{
      type: Input,
      args: ["p-infinite-scroll-distance"]
    }],
    serviceApi: [{
      type: Input,
      args: ["p-service-api"]
    }],
    serviceDeleteApi: [{
      type: Input,
      args: ["p-service-delete"]
    }],
    spacing: [{
      type: Input,
      args: ["p-spacing"]
    }, {
      type: HostBinding,
      args: ["attr.p-spacing"]
    }],
    filteredColumns: [{
      type: Input,
      args: ["p-filtered-columns"]
    }],
    draggable: [{
      type: Input,
      args: ["p-draggable"]
    }],
    virtualScroll: [{
      type: Input,
      args: ["p-virtual-scroll"]
    }],
    onThemeChange: [{
      type: HostListener,
      args: ["window:PoUiThemeChange"]
    }]
  });
})();
var PoTableCellTemplateDirective = class _PoTableCellTemplateDirective {
  templateRef;
  // Necessário manter templateRef para o funcionamento do cell template.
  constructor(templateRef) {
    this.templateRef = templateRef;
  }
  static ɵfac = function PoTableCellTemplateDirective_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoTableCellTemplateDirective)(ɵɵdirectiveInject(TemplateRef));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoTableCellTemplateDirective,
    selectors: [["", "p-table-cell-template", ""]],
    standalone: false
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoTableCellTemplateDirective, [{
    type: Directive,
    args: [{
      selector: "[p-table-cell-template]",
      standalone: false
    }]
  }], () => [{
    type: TemplateRef
  }], null);
})();
var PoTableColumnTemplateDirective = class _PoTableColumnTemplateDirective {
  templateRef;
  /**
   * @optional
   *
   * @description
   *
   * Variável responsável por armazenar a property da coluna da tabela que será adicionado o template.
   *
   * Caso não seja informada esta propriedade, serão apresentados normalmente os dados da coluna.
   */
  targetProperty;
  // Necessário manter templateRef para o funcionamento do column template.
  constructor(templateRef) {
    this.templateRef = templateRef;
  }
  static ɵfac = function PoTableColumnTemplateDirective_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoTableColumnTemplateDirective)(ɵɵdirectiveInject(TemplateRef));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoTableColumnTemplateDirective,
    selectors: [["", "p-table-column-template", ""]],
    inputs: {
      targetProperty: [0, "p-property", "targetProperty"]
    },
    standalone: false
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoTableColumnTemplateDirective, [{
    type: Directive,
    args: [{
      selector: "[p-table-column-template]",
      standalone: false
    }]
  }], () => [{
    type: TemplateRef
  }], {
    targetProperty: [{
      type: Input,
      args: ["p-property"]
    }]
  });
})();
var PoTableRowTemplateDirective = class _PoTableRowTemplateDirective {
  templateRef;
  /**
   * @optional
   *
   * @description
   *
   * Função que deve retornar um valor booleano, informando se apresentará o template da linha.
   * Serão passados o objeto da linha e o indice da mesma por parâmetro, para que seja possível
   * tomar uma decisão a partir do objeto da linha corrente.
   *
   * Caso não ser utilizado esta propriedade, serão apresentados todos os detalhes das linhas.
   *
   * @default `true`
   */
  poTableRowTemplateShow;
  _tableRowTemplateArrowDirection = PoTableRowTemplateArrowDirection.Left;
  /**
   * @optional
   *
   * @description
   *
   * Propriedade responsável por informar a posição do colapse que abrirá os detalhes da linha.
   *
   * @default `'LEFT'`
   */
  set tableRowTemplateArrowDirection(value) {
    value = value?.toUpperCase();
    this._tableRowTemplateArrowDirection = Object.values(PoTableRowTemplateArrowDirection).includes(value) ? value : PoTableRowTemplateArrowDirection.Left;
  }
  get tableRowTemplateArrowDirection() {
    return this._tableRowTemplateArrowDirection;
  }
  // Necessário manter templateRef para o funcionamento do row template.
  constructor(templateRef) {
    this.templateRef = templateRef;
  }
  static ɵfac = function PoTableRowTemplateDirective_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoTableRowTemplateDirective)(ɵɵdirectiveInject(TemplateRef));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoTableRowTemplateDirective,
    selectors: [["", "p-table-row-template", ""]],
    inputs: {
      poTableRowTemplateShow: [0, "p-table-row-template-show", "poTableRowTemplateShow"],
      tableRowTemplateArrowDirection: [0, "p-table-row-template-arrow-direction", "tableRowTemplateArrowDirection"]
    },
    standalone: false
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoTableRowTemplateDirective, [{
    type: Directive,
    args: [{
      selector: "[p-table-row-template]",
      standalone: false
    }]
  }], () => [{
    type: TemplateRef
  }], {
    poTableRowTemplateShow: [{
      type: Input,
      args: ["p-table-row-template-show"]
    }],
    tableRowTemplateArrowDirection: [{
      type: Input,
      args: ["p-table-row-template-arrow-direction"]
    }]
  });
})();
var poSearchLiteralsDefault = {
  en: {
    search: "Search",
    clean: "Clear search",
    all: "All",
    of: "of",
    next: "Next result",
    previous: "Previous result",
    result: "Result",
    footerActionListbox: "See all results",
    placeholderListbox: "Search"
  },
  es: {
    search: "Buscar",
    clean: "Borrar búsqueda",
    all: "Todo",
    of: "de",
    next: "Siguiente resultado",
    previous: "Resultado anterior",
    result: "Resultado",
    footerActionListbox: "Ver todos los resultados",
    placeholderListbox: "Buscar"
  },
  pt: {
    search: "Pesquisar",
    clean: "Limpar busca",
    all: "Todos",
    of: "de",
    next: "Próximo resultado",
    previous: "Resultado anterior",
    result: "Resultado",
    footerActionListbox: "Ver todos os resultados",
    placeholderListbox: "Pesquisar"
  },
  ru: {
    search: "Поиск",
    clean: "Очистить поиск",
    all: "Все",
    of: "из",
    next: "Следующий результат",
    previous: "Предыдущий результат",
    result: "Результат",
    footerActionListbox: "Посмотреть все результаты",
    placeholderListbox: "Поиск"
  }
};
var poSearchLiteralsDefaultExecute = {
  en: {
    search: "Search and Execute",
    placeholderListbox: "Search and Execute"
  },
  es: {
    search: "Buscar y ejecutar",
    placeholderListbox: "Buscar y ejecutar"
  },
  pt: {
    search: "Pesquisar e Executar ",
    placeholderListbox: "Pesquisar e Executar "
  },
  ru: {
    search: "Поиск и выполнение",
    placeholderListbox: "Поиск и выполнение"
  }
};
var PoSearchBaseComponent = class _PoSearchBaseComponent {
  language;
  _literals;
  _ariaLabel;
  _filterSelect;
  _size = void 0;
  _initialSize = void 0;
  _keysLabel = [];
  _disabled = false;
  _loading = false;
  /**
   * @optional
   *
   * @description
   *
   * Define um aria-label para o po-search.
   *
   * > Devido o componente não possuir uma label assim como outros campos de texto, o `aria-label` é utilizado para
   * acessibilidade.
   */
  set ariaLabel(value) {
    this._ariaLabel = value;
    if (value !== this.literals.search) {
      this._ariaLabel = `${this._ariaLabel} ${this.literals.search}`;
    }
  }
  get ariaLabel() {
    return this._ariaLabel;
  }
  /**
   * @optional
   *
   * @description
   *
   * Desabilita o po-search e não permite que o usuário interaja com o mesmo.
   *
   * @default `false`
   */
  set disabled(disabled) {
    this._disabled = convertToBoolean(disabled);
  }
  get disabled() {
    return this._disabled;
  }
  /**
   * @optional
   *
   * @description
   * Exibe um ícone de carregamento no lado direito do campo para sinalizar que uma operação está em andamento.
   *
   * > Incompatível com a propriedade `p-search-type` do tipo `locate`.
   *
   * @default `false`
   */
  set loading(value) {
    this._loading = convertToBoolean(value);
  }
  get loading() {
    return this._loading;
  }
  get isDisabled() {
    if (this.type === "locate") {
      return this.disabled;
    }
    return this.disabled || this.loading;
  }
  /**
   * @description
   *
   * Define os nomes das propriedades do objeto que serão utilizados para busca em `p-items`. Cada valor definido no
   * array será considerado durante a apresentação e filtragem dos itens.
   * Exemplo de uso:
   * ```typescript
   * const filterKeys: Array<string> = ['name', 'gender', 'planet', 'father'];
   * ```
   *
   * > Esta propriedade é ignorada quando utilizado com `p-filter-select` e incompatível com a propriedade
   * `p-search-type` do tipo `locate`.
   */
  filterKeys = [];
  /**
   * @description
   *
   * Habilita um seletor de filtros à esquerda do campo, permitindo a aplicação de filtros agrupados na busca ou sobre
   * os itens fornecidos em `p-items`. Automaticamente adiciona a opção **Todos**, com um mapeamento de todas as opções passadas.
   *
   * Exemplo de uso:
   * ```typescript
   * const filterSelect = [
   *   { label: 'personal', value: ['name', 'email', 'nickname'] },
   *   { label: 'address', value: ['country', 'state', 'city', 'street'] },
   *   { label: 'family', value: ['father', 'mother', 'dependents'] }
   * ];
   * ```
   *
   * > Ao ser habilitada, a propriedade `p-filter-keys` será ignorada. Esta propriedade é incompatível com a propriedade
   * `p-search-type` do tipo `locate`.
   */
  set filterSelect(values) {
    if (!Array.isArray(values) || values.length === 0 || values.every((value) => Object.keys(value).length === 0)) {
      this._filterSelect = void 0;
      return;
    }
    const _values = this.ensureFilterSelectOption(values);
    const allValues = _values.flatMap((e2) => e2.value);
    const uniqueValues = [...new Set(allValues)];
    const filterOptionAll = {
      label: this.literals.all,
      value: uniqueValues
    };
    this._filterSelect = [filterOptionAll, ..._values];
  }
  get filterSelect() {
    return this._filterSelect;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define o modo de pesquisa utilizado no campo de busca. Os valores permitidos são definidos pelo enum
   * **PoSearchFilterMode**.
   *
   * > Incompatível com a propriedade `p-search-type` do tipo `locate`.
   *
   * @default `startsWith`
   */
  filterType = PoSearchFilterMode.startsWith;
  /**
   * @optional
   *
   * @description
   *
   * Permite customizar o ícone de busca que acompanha o campo.
   *
   * É possível usar qualquer um dos ícones da [Biblioteca de ícones PO UI](https://po-ui.io/icons), conforme exemplo:
   * ```
   * <po-search p-icon="an an-user"></po-search>
   * ```
   * Também é possível utilizar outras fontes de ícones, por exemplo a biblioteca *Font Awesome*, desde que a biblioteca
   * esteja carregada no projeto:
   * ```
   * <po-search p-icon="fa fa-podcast"></po-search>
   * ```
   *
   * Outra opção seria a customização do ícone através do `TemplateRef`, conforme exemplo abaixo:
   * ```
   * <po-search [p-icon]="template"></po-search>
   *
   * <ng-template #template>
   *   <i class="fa fa-podcast" style="font-size: inherit;"></i>
   * </ng-template>
   * ```
   */
  icon;
  // Propriedade de uso interno.
  id;
  /**
   * @optional
   *
   * @description
   *
   * Lista de itens que serão utilizados para pesquisa.
   *
   * > Incompatível com a propriedade `p-search-type` do tipo `locate`.
   */
  items = [];
  /**
   * @optional
   *
   * @description
   *
   * Objeto com as literais usadas no `po-search`, permitindo personalizar os textos exibidos no componente.
   *
   * Para utilizar basta passar a literal que deseja customizar:
   *
   * ```
   *  const customLiterals: PoSearchLiterals = {
   *    search: 'Pesquisar',
   *    clean: 'Limpar',
   *  };
   * ```
   *
   * E para carregar a literal customizada, basta apenas passar o objeto para o componente.
   *
   * ```
   * <po-search
   *   [p-literals]="customLiterals">
   * </po-search>
   * ```
   *
   * > O objeto padrão de literais será traduzido de acordo com o idioma do [`PoI18nService`](/documentation/po-i18n) ou
   * do browser.
   */
  set literals(value) {
    if (value instanceof Object && !(value instanceof Array)) {
      this._literals = __spreadValues(__spreadValues(__spreadValues(__spreadValues({}, poSearchLiteralsDefault[poLocaleDefault]), poSearchLiteralsDefault[this.language]), this.type === "execute" ? poSearchLiteralsDefaultExecute[this.language] : {}), value);
    } else {
      this._literals = __spreadValues(__spreadValues({}, poSearchLiteralsDefault[this.language]), this.type === "execute" ? poSearchLiteralsDefaultExecute[this.language] : {});
    }
  }
  get literals() {
    return this._literals || __spreadValues(__spreadValues({}, poSearchLiteralsDefault[this.language]), this.type === "execute" ? poSearchLiteralsDefaultExecute[this.language] : {});
  }
  /**
   * @optional
   *
   * @description
   *
   * Define os valores do contador exibido ao usar a propriedade `p-search-type` do tipo `locate`, indicando a posição
   * atual e o total de ocorrências encontradas.
   * Exemplo de uso:
   * ```ts
   * locateSummary: PoSearchLocateSummary = { currentIndex: 0, total: 5 };
   * ```
   *
   * > Compatível com a propriedade `p-search-type` do tipo `locate`.
   */
  locateSummary = {
    currentIndex: 0,
    total: 0
  };
  /**
   * @optional
   *
   * @description
   * Nome e identificador do campo.
   *
   */
  name;
  /**
   * @optional
   *
   * @description
   *
   * Define a propriedade nativa `autocomplete` do campo como `off`.
   *
   * @default `false`
   */
  noAutocomplete = false;
  /**
   * @optional
   *
   * @description
   *
   * Determina a forma de realizar a pesquisa no componente. Valores aceitos:
   * - `action`: Realiza a busca a cada caractere digitado.
   * - `trigger`: Realiza a busca ao pressionar `enter` ou clicar no ícone de busca.
   * - `locate`: Modo manual. Exibe botões e contador, mas não executa buscas — controle é do desenvolvedor.
   * - `execute`: Executa uma ação ou realiza um redirecionamento ao selecionar um item no `listbox`.
   *    Para este tipo, é necessário informar as propriedades `action` ou `url` nos itens definidos em `p-items`.
   *
   * @default `action`
   */
  type = "action";
  /**
   * @optional
   *
   * @description
   *
   * Exibe uma lista (auto-complete) com as opções definidas em `p-filter-keys` ou `p-filter-select` enquanto realiza
   * uma busca, respeitando o `p-filter-type` como modo de pesquisa.
   *
   * > Incompatível com a propriedade `p-search-type` do tipo `locate`.
   *
   * @default `false`
   */
  showListbox = false;
  /**
   * @optional
   *
   * @description
   *
   * Define o tamanho do componente:
   * - `small`: altura do input como 32px (disponível apenas para acessibilidade AA).
   * - `medium`: altura do input como 44px.
   *
   * > Caso a acessibilidade AA não esteja configurada, o tamanho `medium` será mantido.
   * Para mais detalhes, consulte a documentação do [po-theme](https://po-ui.io/documentation/po-theme).
   *
   * @default `medium`
   */
  set size(value) {
    this._initialSize = value;
    this.applySizeBasedOnA11y();
  }
  get size() {
    return this._size ?? getDefaultSizeFn(PoFieldSize);
  }
  /**
   * @optional
   *
   * @description
   *
   * Define os nomes das propriedades do objeto que serão exibidos como rótulos (labels) no `listbox` quando a propriedade
   * `p-show-listbox` estiver habilitada.
   *
   * Deve ser informado um array de strings contendo até **3 propriedades**.
   *
   * Exemplo de uso:
   * ```ts
   * keysLabel: Array<string> = ['nome', 'email', 'country'];
   * ```
   */
  set keysLabel(value) {
    if (value && value.length > 3) {
      this._keysLabel = value.slice(0, 3);
    } else {
      this._keysLabel = value;
    }
  }
  get keysLabel() {
    return this._keysLabel;
  }
  /**
   * @optional
   *
   * @description
   *
   * Evento disparado ao sair do campo.
   */
  blur = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Evento disparado ao alterar valor do model.
   */
  changeModel = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Pode ser informada uma função que será disparada quando houver alterações nos filtros.
   *
   * > Incompatível com a propriedade `p-search-type` do tipo `locate`.
   */
  filter = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Pode ser informada uma função que será disparada quando houver alterações no input.
   *
   * > Incompatível com a propriedade `p-search-type` do tipo `locate`.
   */
  filteredItemsChange = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Evento emitido quando o campo de entrada (input) recebe foco.
   */
  focusEvent = new EventEmitter();
  /**
   * @optional
   *
   * @description
   * Evento disparado quando uma tecla é pressionada enquanto o foco está no componente.
   * Retorna um objeto `KeyboardEvent` com informações sobre a tecla.
   */
  keydown = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Pode ser informada uma função que será disparada quando houver click no listbox.
   *
   * > Incompatível com a propriedade `p-search-type` do tipo `locate`.
   */
  listboxOnClick = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Evento disparado ao clicar no controle "Próximo resultado".
   *
   * > Compatível com a propriedade `p-search-type` do tipo `locate`.
   */
  locateNext = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Evento disparado ao clicar no controle "Resultado anterior".
   *
   * > Compatível com a propriedade `p-search-type` do tipo `locate`.
   */
  locatePrevious = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Evento disparado ao clicar no botão de ação exibido no rodapé do `listbox`.
   * O texto exibido pode ser configurado por meio do literal `footerActionListbox`.
   */
  footerAction = new EventEmitter();
  constructor(languageService) {
    this.language = languageService.getShortLanguage();
  }
  onThemeChange() {
    this.applySizeBasedOnA11y();
  }
  ensureFilterSelectOption(values) {
    const _values = Array.isArray(values) ? values : Array.of(values);
    return _values.map((value) => typeof value === "object" ? value : {
      label: value,
      value
    });
  }
  //Transforma o tamanho do input para o tamanho do ícone de loading correspondente
  mapSizeToIcon(size) {
    return mapInputSizeToLoadingIcon(size);
  }
  applySizeBasedOnA11y() {
    const size = validateSizeFn(this._initialSize, PoFieldSize);
    this._size = size;
  }
  static ɵfac = function PoSearchBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoSearchBaseComponent)(ɵɵdirectiveInject(PoLanguageService));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoSearchBaseComponent,
    hostVars: 1,
    hostBindings: function PoSearchBaseComponent_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("PoUiThemeChange", function PoSearchBaseComponent_PoUiThemeChange_HostBindingHandler() {
          return ctx.onThemeChange();
        }, ɵɵresolveWindow);
      }
      if (rf & 2) {
        ɵɵattribute("p-size", ctx.size);
      }
    },
    inputs: {
      ariaLabel: [0, "p-aria-label", "ariaLabel"],
      disabled: [0, "p-disabled", "disabled"],
      loading: [0, "p-loading", "loading"],
      filterKeys: [0, "p-filter-keys", "filterKeys"],
      filterSelect: [0, "p-filter-select", "filterSelect"],
      filterType: [0, "p-filter-type", "filterType"],
      icon: [0, "p-icon", "icon"],
      id: [0, "p-id", "id"],
      items: [0, "p-items", "items"],
      literals: [0, "p-literals", "literals"],
      locateSummary: [0, "p-locate-summary", "locateSummary"],
      name: "name",
      noAutocomplete: [2, "p-no-autocomplete", "noAutocomplete", convertToBoolean],
      type: [0, "p-search-type", "type"],
      showListbox: [2, "p-show-listbox", "showListbox", convertToBoolean],
      size: [0, "p-size", "size"],
      keysLabel: [0, "p-keys-label", "keysLabel"]
    },
    outputs: {
      blur: "p-blur",
      changeModel: "p-change-model",
      filter: "p-filter",
      filteredItemsChange: "p-filtered-items-change",
      focusEvent: "p-focus",
      keydown: "p-keydown",
      listboxOnClick: "p-listbox-onclick",
      locateNext: "p-locate-next",
      locatePrevious: "p-locate-previous",
      footerAction: "p-footer-action-listbox"
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoSearchBaseComponent, [{
    type: Directive
  }], () => [{
    type: PoLanguageService
  }], {
    ariaLabel: [{
      type: Input,
      args: ["p-aria-label"]
    }],
    disabled: [{
      type: Input,
      args: ["p-disabled"]
    }],
    loading: [{
      type: Input,
      args: ["p-loading"]
    }],
    filterKeys: [{
      type: Input,
      args: ["p-filter-keys"]
    }],
    filterSelect: [{
      type: Input,
      args: ["p-filter-select"]
    }],
    filterType: [{
      type: Input,
      args: ["p-filter-type"]
    }],
    icon: [{
      type: Input,
      args: ["p-icon"]
    }],
    id: [{
      type: Input,
      args: ["p-id"]
    }],
    items: [{
      type: Input,
      args: ["p-items"]
    }],
    literals: [{
      type: Input,
      args: ["p-literals"]
    }],
    locateSummary: [{
      type: Input,
      args: ["p-locate-summary"]
    }],
    name: [{
      type: Input,
      args: ["name"]
    }],
    noAutocomplete: [{
      type: Input,
      args: [{
        alias: "p-no-autocomplete",
        transform: convertToBoolean
      }]
    }],
    type: [{
      type: Input,
      args: ["p-search-type"]
    }],
    showListbox: [{
      type: Input,
      args: [{
        alias: "p-show-listbox",
        transform: convertToBoolean
      }]
    }],
    size: [{
      type: Input,
      args: ["p-size"]
    }, {
      type: HostBinding,
      args: ["attr.p-size"]
    }],
    keysLabel: [{
      type: Input,
      args: ["p-keys-label"]
    }],
    blur: [{
      type: Output,
      args: ["p-blur"]
    }],
    changeModel: [{
      type: Output,
      args: ["p-change-model"]
    }],
    filter: [{
      type: Output,
      args: ["p-filter"]
    }],
    filteredItemsChange: [{
      type: Output,
      args: ["p-filtered-items-change"]
    }],
    focusEvent: [{
      type: Output,
      args: ["p-focus"]
    }],
    keydown: [{
      type: Output,
      args: ["p-keydown"]
    }],
    listboxOnClick: [{
      type: Output,
      args: ["p-listbox-onclick"]
    }],
    locateNext: [{
      type: Output,
      args: ["p-locate-next"]
    }],
    locatePrevious: [{
      type: Output,
      args: ["p-locate-previous"]
    }],
    footerAction: [{
      type: Output,
      args: ["p-footer-action-listbox"]
    }],
    onThemeChange: [{
      type: HostListener,
      args: ["window:PoUiThemeChange"]
    }]
  });
})();
var poSearchContainerOffset = 8;
var poSearchContainerPositionDefault = "bottom";
var ID_SEARCH_BUTTON_CLEAN = "search-button-clean";
var PoSearchComponent = class _PoSearchComponent extends PoSearchBaseComponent {
  languageService;
  renderer = inject(Renderer2);
  changeDetector = inject(ChangeDetectorRef);
  controlPosition = inject(PoControlPositionService);
  clickoutListener;
  eventResizeListener;
  _locateCounter;
  set locateCounter(element) {
    this._locateCounter = element;
    this.locateCounterResize = new ResizeObserver(() => {
      this.updatePaddingRightLocate(false);
    });
    if (this._locateCounter) {
      this.locateCounterResize.observe(this._locateCounter.nativeElement);
    }
  }
  get locateCounter() {
    return this._locateCounter;
  }
  poSearchInput;
  poListboxContainerElement;
  poListboxElement;
  poListbox;
  basePaddingRightSmall = 122;
  basePaddingRightMedium = 158;
  dynamicPaddingRight;
  listboxFilteredItems = [];
  filteredItems = [];
  listboxOpen = false;
  shouldMarkLetters = true;
  isFiltering = false;
  isInputFocused = false;
  listboxItemclicked = false;
  showSearchLocateControls = false;
  showNoResults = false;
  locateCounterResize;
  searchFilter = {};
  searchFilterSelectLabel;
  searchFilterSelectActions;
  idSearchButtonClean = ID_SEARCH_BUTTON_CLEAN;
  showFooterActionListbox = false;
  showSeparator = false;
  placeholderListbox;
  modelSelected;
  constructor() {
    const languageService = inject(PoLanguageService);
    super(languageService);
    this.languageService = languageService;
  }
  ngOnInit() {
    this.filteredItems = this.items;
    if (this.showListbox) {
      this.listboxFilteredItems = this.keysLabel.length === 0 ? this.listboxItems : this.items;
    }
    if (this.filterSelect) {
      this.createDropdownFilterSelect();
    }
    this.showFooterActionListbox = this.footerAction.observed;
  }
  ngOnChanges(changes) {
    if (changes.filterSelect?.currentValue) {
      this.createDropdownFilterSelect();
    }
    if (changes.type || changes.disabled) {
      this.updateShowSearchLocateControls();
      this.updatePaddingRightLocate(true);
    }
    if (changes.keysLabel) {
      this.showSeparator = this.keysLabel.length > 1;
    }
    this.configureSearchModeExecute(changes);
  }
  configureSearchModeExecute(changes) {
    if (changes.type?.currentValue === "execute") {
      this.showListbox = true;
      if (!this.keysLabel.length && this.filterKeys?.length) {
        this.keysLabel = [...this.filterKeys.slice(0, 3)];
      }
    }
  }
  ngOnDestroy() {
    this.removeListeners();
    if (this.locateCounterResize?.disconnect) {
      this.locateCounterResize.disconnect();
    }
  }
  clearSearch() {
    this.poSearchInput.nativeElement.value = "";
    this.onSearchChange("", true);
    this.adjustContainerPosition();
    this.changeDetector.detectChanges();
    if (!this.showFooterActionListbox) {
      this.onCloseListbox();
    }
  }
  onCleanKeydown(event) {
    const isEsc = event.key === "Escape";
    if (isEsc && this.type === "locate") {
      this.clearSearch();
      this.poSearchInput.nativeElement.focus();
      event.preventDefault();
      event.stopPropagation();
    }
  }
  onEnterKey(event) {
    if (this.type !== "locate") {
      if (this.listboxOpen) {
        if (!this.showFooterActionListbox) {
          this.closeListbox();
        }
      } else {
        this.onSearchChange(event.target.value, this.type === "trigger", true);
        this.closeListbox();
      }
    }
  }
  onSearchChange(searchText, activated, buttonClick) {
    const searchTextInitial = searchText;
    if (this.type === "locate" && this.changeModel.observed) {
      this.changeModel.emit(searchTextInitial);
      this.modelSelected = searchTextInitial;
      return;
    }
    if (searchText !== void 0) {
      searchText = searchText.toLowerCase();
    }
    this.isFiltering = true;
    if (activated && !this.listboxItemclicked) {
      this.updateFilteredItems(searchText);
      this.filteredItemsChange.emit(this.filteredItems);
      if (this.filterSelect) {
        this.searchFilter = __spreadProps(__spreadValues({}, this.searchFilter), {
          value: searchTextInitial
        });
      } else {
        this.searchFilter = {
          filter: this.filterKeys,
          value: searchTextInitial
        };
      }
      this.filter.emit(this.searchFilter);
      this.changeModel.emit(searchText);
      this.modelSelected = searchTextInitial;
    }
    if (this.showListbox && !buttonClick && (searchText.length > 0 || this.showFooterActionListbox)) {
      this.openListbox();
      this.listboxFilteredItems = this.getListboxFilteredItems(searchText);
      this.handlerPlaceholderListbox();
    } else {
      if (searchText.length === 0) {
        this.listboxFilteredItems = this.keysLabel.length === 0 ? this.listboxItems : this.items;
      }
    }
    if (this.listboxItemclicked) {
      this.listboxItemclicked = false;
    }
    this.changeDetector.detectChanges();
  }
  updateFilteredItems(searchText) {
    if (this.items && this.items.length > 0) {
      this.filteredItems = this.getFilteredItems(searchText);
    } else {
      this.filteredItems = [];
    }
  }
  getFilteredItems(searchText) {
    return this.items.filter((item) => this.itemMatchesFilter(item, searchText));
  }
  itemMatchesFilter(item, searchText) {
    const valuesToSearch = this.filterKeys.map((key2) => typeof item[key2] !== "string" ? String(item[key2]) : item[key2]).map((value) => value ? value.toLowerCase() : "");
    return valuesToSearch.some((value) => this.filterValue(value, searchText));
  }
  getListboxFilteredItems(searchText) {
    if (this.keysLabel.length > 0) {
      return searchText.length ? this.filteredItems : [];
    }
    return this.listboxItems.filter((item) => this.filterValue(item.value, searchText));
  }
  filterValue(value, searchText) {
    value = value?.toLowerCase();
    switch (this.filterType) {
      case PoSearchFilterMode.startsWith:
        return value?.startsWith(searchText);
      case PoSearchFilterMode.contains:
        return value?.includes(searchText);
      case PoSearchFilterMode.endsWith:
        return value?.endsWith(searchText);
      default:
        return false;
    }
  }
  get listboxItems() {
    return this.items.map((item) => this.filterKeys.map((key2) => item[key2]).map((item2) => typeof item2 !== "string" ? String(item2) : item2)).flat().map((value) => ({
      label: value,
      value
    })).filter((obj, index, self2) => index === self2.findIndex((o) => o.label === obj.label && o.value === obj.value));
  }
  get autocomplete() {
    return this.noAutocomplete ? "off" : "on";
  }
  onCloseListbox() {
    this.poSearchInput.nativeElement.focus();
    this.closeListbox();
    this.isFiltering = false;
  }
  onListboxClick(option, event) {
    if (event) {
      event.stopPropagation();
    }
    if (!event || event.code === "Enter") {
      this.listboxItemclicked = true;
    }
    let value = option.value;
    if (this.keysLabel.length) {
      value = option[this.keysLabel[0]];
    }
    this.poSearchInput.nativeElement.value = value;
    this.listboxOnClick.emit(value);
    if (this.keysLabel.length) {
      this.items.forEach((item) => delete item.selected);
    }
    this.onCloseListbox();
    if (this.type === "action") {
      this.listboxItemclicked = false;
      this.onSearchChange(value.toString(), true, true);
    }
  }
  onBlur() {
    this.isInputFocused = false;
    if (this.type === "locate") {
      this.updateShowSearchLocateControls();
      this.updatePaddingRightLocate(true);
    }
    if (this.blur.observed) {
      this.blur.emit();
    }
    if (this.listboxOpen && !this.showFooterActionListbox) {
      if (!this.poListbox.items.length) {
        this.closeListbox();
      } else {
        this.focusItem();
      }
    }
  }
  onFocus() {
    this.isInputFocused = true;
    if (this.type === "locate") {
      this.updateShowSearchLocateControls();
      this.updatePaddingRightLocate(false);
    }
    if (this.focusEvent.observed) {
      this.focusEvent.emit();
    }
    this.openListboxFooterAction();
  }
  onInputHandler(value) {
    if (this.type === "locate") {
      this.onSearchChange(value, false);
      this.updateShowSearchLocateControls();
      this.updatePaddingRightLocate(false);
    } else {
      this.onSearchChange(value, false);
      this.onSearchChange(value, ["action", "execute"].includes(this.type));
      this.adjustContainerPosition();
      this.changeDetector.detectChanges();
    }
  }
  onKeyDown(event) {
    const key2 = event.keyCode;
    this.keydown.emit(event);
    if (event.shiftKey && key2 === PoKeyCodeEnum.tab) {
      this.closeListbox();
      return;
    }
    if (key2 === PoKeyCodeEnum.tab) {
      this.closeListbox();
      return;
    }
    if (key2 === PoKeyCodeEnum.arrowDown) {
      event.preventDefault();
      if (!this.listboxOpen) {
        return;
      }
      this.focusItem();
      this.openListbox();
      return;
    }
    if (key2 === PoKeyCodeEnum.esc) {
      this.closeListbox();
      this.poSearchInput.nativeElement.focus();
      return;
    }
    if (key2 === PoKeyCodeEnum.enter && this.listboxOpen) {
      this.closeListbox();
      this.isFiltering = false;
    }
  }
  focusItem() {
    const listboxItemList = this.poListboxElement?.nativeElement?.querySelectorAll(".po-listbox-item");
    setTimeout(() => {
      Array.from(listboxItemList).forEach((el) => {
        el.tabIndex = -1;
        el.classList.remove("cdk-option-active");
      });
      const firstOption = listboxItemList[0];
      firstOption.focus();
      firstOption.classList.add("cdk-option-active");
    });
  }
  setContainerPosition() {
    if (this.poListboxContainerElement && this.poSearchInput) {
      this.controlPosition.setElements(this.poListboxContainerElement.nativeElement, poSearchContainerOffset, this.poSearchInput, ["top", "bottom"], true);
      this.adjustContainerPosition();
    }
  }
  adjustContainerPosition() {
    if (this.poListboxContainerElement && this.poSearchInput && this.showListbox) {
      this.controlPosition.adjustPosition(poSearchContainerPositionDefault);
    }
  }
  openListbox() {
    if (!this.listboxOpen) {
      this.listboxOpen = true;
      this.changeDetector.detectChanges();
      this.initializeListeners();
      this.poSearchInput.nativeElement.focus();
      this.setContainerPosition();
    }
  }
  closeListbox() {
    this.listboxOpen = false;
    this.changeDetector.detectChanges();
    this.removeListeners();
  }
  clickedOutsideInput(event) {
    if (this.listboxOpen && !this.poSearchInput?.nativeElement?.contains(event.target) && !this.poListboxElement?.nativeElement?.contains(event.target) && !(this.showFooterActionListbox && event.target.closest(`#${this.idSearchButtonClean}`))) {
      this.closeListbox();
    }
  }
  initializeListeners() {
    this.removeListeners();
    this.clickoutListener = this.renderer.listen("document", "click", (event) => {
      this.clickedOutsideInput(event);
    });
    this.eventResizeListener = this.renderer.listen("window", "resize", () => {
      setTimeout(() => this.adjustContainerPosition(), 250);
    });
    window.addEventListener("scroll", this.onScroll, true);
  }
  removeListeners() {
    if (this.clickoutListener) {
      this.clickoutListener();
    }
    if (this.eventResizeListener) {
      this.eventResizeListener();
    }
    window.removeEventListener("scroll", this.onScroll, true);
  }
  updatePaddingRightLocate(noValue) {
    const basePadding = this.size === PoFieldSize.Medium ? this.basePaddingRightMedium : this.basePaddingRightSmall;
    const hasValue = !!this.poSearchInput.nativeElement.value;
    if (this.type === "locate") {
      if (hasValue && this.locateCounter?.nativeElement) {
        const counterWidth = this.locateCounter.nativeElement.offsetWidth;
        const extraSpace = 8;
        this.dynamicPaddingRight = basePadding + counterWidth + extraSpace;
      } else {
        this.dynamicPaddingRight = noValue || this.disabled ? null : basePadding;
      }
    } else {
      this.dynamicPaddingRight = null;
    }
    this.changeDetector.detectChanges();
  }
  updateShowSearchLocateControls() {
    this.showSearchLocateControls = !!(this.type === "locate" && (this.isInputFocused || this.poSearchInput?.nativeElement?.value) && !this.disabled);
  }
  onScroll = () => {
    this.adjustContainerPosition();
  };
  getInputValue() {
    return this.poSearchInput?.nativeElement?.value ?? "";
  }
  createDropdownFilterSelect() {
    this.searchFilterSelectActions = [];
    if (!this.filterSelect) {
      return;
    }
    this.filterSelect.forEach((filterOption) => {
      const selectOption = {
        label: filterOption.label,
        action: () => this.changeFilterSelect(filterOption),
        selected: this.isSelected(filterOption)
      };
      this.searchFilterSelectActions.push(selectOption);
    });
    this.changeFilterSelect(this.filterSelect[0], false);
  }
  isSelected(filterOption) {
    return this.searchFilterSelectLabel === filterOption.label;
  }
  changeFilterSelect(filterOption, focus = true) {
    this.searchFilterSelectLabel = filterOption.label;
    this.filterKeys = Array.isArray(filterOption.value) ? [...filterOption.value] : [filterOption.value];
    if (!this.searchFilterSelectActions) {
      return;
    }
    this.searchFilterSelectActions.forEach((action) => action.selected = false);
    const selectAction = this.searchFilterSelectActions.find((action) => action.label === this.searchFilterSelectLabel);
    selectAction.selected = true;
    this.searchFilter = {
      filter: filterOption.label === this.literals.all ? ["all"] : filterOption.value
    };
    if (this.poSearchInput?.nativeElement && focus) {
      this.poSearchInput.nativeElement.focus();
    }
    if (this.type === "action") {
      this.onSearchChange(this.getInputValue(), true);
    }
  }
  handlerFooterActionListbox() {
    this.footerAction.emit();
    this.closeListbox();
  }
  openListboxFooterAction() {
    if (this.showFooterActionListbox && this.showListbox && !this.modelSelected) {
      this.listboxFilteredItems = [];
      this.handlerPlaceholderListbox();
      this.openListbox();
    }
  }
  handlerPlaceholderListbox() {
    if (this.showFooterActionListbox && !this.listboxFilteredItems.length && this.getInputValue().length == 0) {
      this.placeholderListbox = this.literals.placeholderListbox;
      return;
    }
    this.placeholderListbox = null;
  }
  static ɵfac = function PoSearchComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoSearchComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoSearchComponent,
    selectors: [["po-search"]],
    viewQuery: function PoSearchComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c93, 5)(_c94, 7, ElementRef)(_c95, 5, ElementRef)(_c96, 5, ElementRef)(_c50, 5);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.locateCounter = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.poSearchInput = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.poListboxContainerElement = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.poListboxElement = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.poListbox = _t.first);
      }
    },
    standalone: false,
    features: [ɵɵProvidersFeature([PoControlPositionService]), ɵɵInheritDefinitionFeature, ɵɵNgOnChangesFeature],
    decls: 13,
    vars: 28,
    consts: [["poSearchInput", ""], ["locateCounter", ""], ["poListboxContainerElement", ""], ["poListbox", "", "poListboxElement", ""], [1, "po-search"], [1, "po-search-select"], [1, "po-search-icon"], ["type", "text", 1, "po-search-input", 3, "input", "keyup.enter", "keydown", "blur", "focus", "autocomplete", "ariaLabel", "disabled", "placeholder"], [1, "po-search-buttons"], ["id", "summary", 1, "po-search-locate-counter"], [3, "id"], [1, "po-search-button", "po-search-button-loading"], ["type", "button", 1, "po-search-button", "po-search-button-clean", 3, "ariaLabel"], ["type", "button", 1, "po-search-button", "po-search-button-trigger", 3, "ariaLabel", "disabled"], [1, "po-search-locate-controls"], [1, "po-search-listbox-container", 3, "hidden"], [3, "p-label", "p-disabled", "p-actions", "p-size"], [3, "p-icon"], [3, "p-size"], ["type", "button", 1, "po-search-button", "po-search-button-clean", 3, "click", "keydown.enter", "keydown", "ariaLabel"], [1, "po-icon-input", 3, "p-size"], ["type", "button", 1, "po-search-button", "po-search-button-trigger", 3, "click", "keydown.enter", "ariaLabel", "disabled"], [1, "po-search-divider"], ["name", "btnArrowUp", 1, "po-search-button", 3, "click", "ariaLabel", "disabled"], ["p-icon", "ICON_ARROW_DOWN"], ["name", "btnArrowDown", 1, "po-search-button", 3, "click", "ariaLabel", "disabled"], ["p-icon", "ICON_ARROW_UP"], ["p-type", "option", 3, "p-selectcombo-item", "p-close", "p-footer-action-listbox-event", "p-items", "p-visible", "p-filter-mode", "p-should-mark-letter", "p-filtering", "p-search-value", "p-size", "p-literals", "p-separator", "p-keys-label", "p-footer-action-listbox", "p-placeholder-listbox"]],
    template: function PoSearchComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "div", 4);
        ɵɵconditionalCreate(1, PoSearchComponent_Conditional_1_Template, 2, 4, "div", 5);
        ɵɵconditionalCreate(2, PoSearchComponent_Conditional_2_Template, 2, 1, "div", 6);
        ɵɵelementStart(3, "input", 7, 0);
        ɵɵlistener("input", function PoSearchComponent_Template_input_input_3_listener($event) {
          return ctx.onInputHandler($event.target.value);
        })("keyup.enter", function PoSearchComponent_Template_input_keyup_enter_3_listener($event) {
          return ctx.onEnterKey($event);
        })("keydown", function PoSearchComponent_Template_input_keydown_3_listener($event) {
          return ctx.onKeyDown($event);
        })("blur", function PoSearchComponent_Template_input_blur_3_listener() {
          return ctx.onBlur();
        })("focus", function PoSearchComponent_Template_input_focus_3_listener() {
          return ctx.onFocus();
        });
        ɵɵelementEnd();
        ɵɵelementStart(5, "div", 8);
        ɵɵconditionalCreate(6, PoSearchComponent_Conditional_6_Template, 3, 3, "span", 9);
        ɵɵelementStart(7, "div", 10);
        ɵɵconditionalCreate(8, PoSearchComponent_Conditional_8_Template, 2, 1, "div", 11)(9, PoSearchComponent_Conditional_9_Template, 2, 2, "button", 12);
        ɵɵelementEnd();
        ɵɵconditionalCreate(10, PoSearchComponent_Conditional_10_Template, 2, 3, "button", 13);
        ɵɵconditionalCreate(11, PoSearchComponent_Conditional_11_Template, 6, 4, "div", 14);
        ɵɵelementEnd()();
        ɵɵconditionalCreate(12, PoSearchComponent_Conditional_12_Template, 5, 13, "div", 15);
      }
      if (rf & 2) {
        const poSearchInput_r4 = ɵɵreference(4);
        ɵɵclassProp("po-search-disabled", ctx.disabled)("po-search-loading", ctx.loading && ctx.type !== "locate")("po-search-filter-locate", ctx.type === "locate");
        ɵɵattribute("p-search-type", ctx.type === "locate")("p-size", ctx.size);
        ɵɵadvance();
        ɵɵconditional(ctx.filterSelect && ctx.type !== "locate" ? 1 : -1);
        ɵɵadvance();
        ɵɵconditional(ctx.type === "action" || ctx.type === "locate" || ctx.type === "execute" ? 2 : -1);
        ɵɵadvance();
        ɵɵstyleProp("padding-right", ctx.dynamicPaddingRight, "px");
        ɵɵclassProp("po-search-input-clean", !!poSearchInput_r4.value && !ctx.disabled && ctx.type !== "locate")("po-search-input-trigger", ctx.type === "trigger");
        ɵɵproperty("autocomplete", ctx.autocomplete)("ariaLabel", ctx.ariaLabel)("disabled", ctx.isDisabled)("placeholder", ctx.literals.search);
        ɵɵattribute("id", ctx.id || null)("name", ctx.name);
        ɵɵadvance(3);
        ɵɵconditional(ctx.type === "locate" && poSearchInput_r4.value && !ctx.disabled ? 6 : -1);
        ɵɵadvance();
        ɵɵproperty("id", ctx.idSearchButtonClean);
        ɵɵadvance();
        ɵɵconditional(ctx.loading && ctx.type !== "locate" ? 8 : !!poSearchInput_r4.value ? 9 : -1);
        ɵɵadvance(2);
        ɵɵconditional(ctx.type === "trigger" ? 10 : -1);
        ɵɵadvance();
        ɵɵconditional(ctx.showSearchLocateControls ? 11 : -1);
        ɵɵadvance();
        ɵɵconditional(ctx.type !== "locate" ? 12 : -1);
      }
    },
    dependencies: [PoCleanComponent, PoIconComponent, PoLoadingIconComponent, PoListBoxComponent, PoDropdownComponent],
    encapsulation: 2,
    changeDetection: 0
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoSearchComponent, [{
    type: Component,
    args: [{
      selector: "po-search",
      providers: [PoControlPositionService],
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: `<div
  class="po-search"
  [class.po-search-disabled]="disabled"
  [class.po-search-loading]="loading && type !== 'locate'"
  [class.po-search-filter-locate]="type === 'locate'"
  [attr.p-search-type]="type === 'locate'"
  [attr.p-size]="size"
>
  @if (filterSelect && type !== 'locate') {
    <div class="po-search-select">
      <po-dropdown
        [p-label]="searchFilterSelectLabel"
        [p-disabled]="isDisabled"
        [p-actions]="searchFilterSelectActions"
        [p-size]="size"
      >
      </po-dropdown>
    </div>
  }

  @if (type === 'action' || type === 'locate' || type === 'execute') {
    <div class="po-search-icon">
      <po-icon [p-icon]="icon ? icon : 'ICON_SEARCH'"></po-icon>
    </div>
  }

  <input
    #poSearchInput
    class="po-search-input"
    type="text"
    [attr.id]="id || null"
    [autocomplete]="autocomplete"
    [attr.name]="name"
    [ariaLabel]="ariaLabel"
    [class.po-search-input-clean]="!!poSearchInput.value && !disabled && type !== 'locate'"
    [class.po-search-input-trigger]="type === 'trigger'"
    [style.padding-right.px]="dynamicPaddingRight"
    [disabled]="isDisabled"
    [placeholder]="literals.search"
    (input)="onInputHandler($event.target.value)"
    (keyup.enter)="onEnterKey($event)"
    (keydown)="onKeyDown($event)"
    (blur)="onBlur()"
    (focus)="onFocus()"
  />

  <div class="po-search-buttons">
    @if (type === 'locate' && poSearchInput.value && !disabled) {
      <span
        #locateCounter
        class="po-search-locate-counter"
        id="summary"
        [attr.aria-label]="
          literals.result +
          ' ' +
          (locateSummary?.currentIndex ?? 0) +
          ' ' +
          literals.of +
          ' ' +
          (locateSummary?.total ?? 0)
        "
      >
        {{ locateSummary?.currentIndex ?? 0 }}/{{ locateSummary?.total ?? 0 }}
      </span>
    }

    <div [id]="idSearchButtonClean">
      @if (loading && type !== 'locate') {
        <div class="po-search-button po-search-button-loading">
          <po-loading-icon [p-size]="mapSizeToIcon(size)"></po-loading-icon>
        </div>
      } @else if (!!poSearchInput.value) {
        <button
          class="po-search-button po-search-button-clean"
          type="button"
          [ariaLabel]="literals.clean"
          (click)="clearSearch()"
          (keydown.enter)="clearSearch()"
          (keydown)="onCleanKeydown($event)"
        >
          <po-clean class="po-icon-input" [p-size]="size"></po-clean>
        </button>
      }
    </div>

    @if (type === 'trigger') {
      <button
        class="po-search-button po-search-button-trigger"
        type="button"
        [ariaLabel]="literals.search"
        [disabled]="isDisabled"
        (click)="onSearchChange(poSearchInput.value, true, true)"
        (keydown.enter)="onSearchChange(poSearchInput.value, true, true)"
      >
        <po-icon [p-icon]="icon ? icon : 'ICON_SEARCH'"> </po-icon>
      </button>
    }

    @if (showSearchLocateControls) {
      <div class="po-search-locate-controls">
        <div class="po-search-divider"></div>
        <button
          name="btnArrowUp"
          class="po-search-button"
          [ariaLabel]="literals.next"
          [disabled]="disabled || !locateSummary?.total"
          (click)="locateNext.emit()"
        >
          <po-icon p-icon="ICON_ARROW_DOWN"> </po-icon>
        </button>

        <button
          name="btnArrowDown"
          class="po-search-button"
          [ariaLabel]="literals.previous"
          [disabled]="disabled || !locateSummary?.total"
          (click)="locatePrevious.emit()"
        >
          <po-icon p-icon="ICON_ARROW_UP"> </po-icon>
        </button>
      </div>
    }
  </div>
</div>

@if (type !== 'locate') {
  <div #poListboxContainerElement class="po-search-listbox-container" [hidden]="!listboxOpen">
    <po-listbox
      #poListbox
      #poListboxElement
      p-type="option"
      [p-items]="listboxFilteredItems"
      (p-selectcombo-item)="onListboxClick($event, $event.event)"
      [p-visible]="listboxOpen"
      [p-filter-mode]="filterType"
      [p-should-mark-letter]="shouldMarkLetters"
      [p-filtering]="isFiltering"
      [p-search-value]="getInputValue()"
      [p-size]="size"
      (p-close)="onCloseListbox()"
      [p-literals]="literals"
      [p-separator]="showSeparator"
      [p-keys-label]="keysLabel"
      [p-footer-action-listbox]="showFooterActionListbox"
      (p-footer-action-listbox-event)="handlerFooterActionListbox()"
      [p-placeholder-listbox]="placeholderListbox"
    ></po-listbox>
  </div>
}
`
    }]
  }], () => [], {
    locateCounter: [{
      type: ViewChild,
      args: ["locateCounter", {
        static: false
      }]
    }],
    poSearchInput: [{
      type: ViewChild,
      args: ["poSearchInput", {
        read: ElementRef,
        static: true
      }]
    }],
    poListboxContainerElement: [{
      type: ViewChild,
      args: ["poListboxContainerElement", {
        read: ElementRef
      }]
    }],
    poListboxElement: [{
      type: ViewChild,
      args: ["poListboxElement", {
        read: ElementRef
      }]
    }],
    poListbox: [{
      type: ViewChild,
      args: ["poListbox"]
    }]
  });
})();
var PoTableDetailComponent = class _PoTableDetailComponent {
  decimalPipe;
  _componentsSize = void 0;
  /**
   * @optional
   *
   * @description
   *
   * Define o tamanho dos componentes de formulário no table:
   * - `small`: aplica a medida small de cada componente (disponível apenas para acessibilidade AA).
   * - `medium`: aplica a medida medium de cada componente.
   *
   * > Caso a acessibilidade AA não esteja configurada, o tamanho `medium` será mantido.
   * Para mais detalhes, consulte a documentação do [po-theme](https://po-ui.io/documentation/po-theme).
   *
   * @default `medium`
   */
  set componentsSize(value) {
    this._componentsSize = validateSizeFn(value, PoFieldSize);
  }
  get componentsSize() {
    return this._componentsSize ?? getDefaultSizeFn(PoFieldSize);
  }
  /**
   * Lista de itens do _detail_ da tabela.
   */
  items;
  /**
   * Linha do registro pai correspondente ao item de detalhe selecionado. Utilizado para gerenciar o estado de seleção do elemento pai,
   * permitindo que o mesmo seja atualizado para refletir a seleção de todos os filhos ou estado indeterminado.
   */
  parentRow;
  /**
   * Define se a tabela possui a opção de `selectable` habilitada.
   */
  isSelectable = false;
  /**
   * @optional
   *
   * @description
   *
   * Ação executada ao selecionar ou desmarcar a seleção de uma linha de detalhe do `po-table`.
   */
  selectRow = new EventEmitter();
  _detail;
  /**
   * Configuração da linha de detalhes.
   */
  set detail(value) {
    this._detail = this.returnPoTableDetailObject(value);
  }
  get detail() {
    return this._detail;
  }
  constructor(decimalPipe) {
    this.decimalPipe = decimalPipe;
  }
  get detailColumns() {
    return this.detail?.columns || [];
  }
  get typeHeaderInline() {
    return this.detail && !this.detail["typeHeader"] || this.detail["typeHeader"] === "inline";
  }
  get typeHeaderTop() {
    return this.detail && this.detail["typeHeader"] === "top";
  }
  formatNumberDetail(value, format2) {
    if (!format2) {
      return value;
    }
    return this.decimalPipe.transform(value, format2);
  }
  getColumnTitleLabel(detail) {
    return detail.label || PoUtils.capitalizeFirstLetter(detail.property);
  }
  getDetailData(item, detail) {
    const arrayProperty = detail.property.split(".");
    if (arrayProperty.length > 1) {
      const nestedProperties = arrayProperty;
      let value = item;
      for (const property of nestedProperties) {
        value = value[property] || value[property] === 0 ? value[property] : "";
      }
      return value;
    } else {
      return item[detail.property];
    }
  }
  onSelectRow(item) {
    item.$selected = !item.$selected;
    this.selectRow.emit({
      item,
      parentRow: this.parentRow
    });
  }
  returnPoTableDetailObject(value) {
    if (value && isTypeof(value, "object")) {
      if (value.columns) {
        value.columns.forEach((column) => column.property = column.property || column.column);
      }
      if (Array.isArray(value)) {
        return {
          columns: value
        };
      }
      if (value.columns) {
        return value;
      }
    }
  }
  static ɵfac = function PoTableDetailComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoTableDetailComponent)(ɵɵdirectiveInject(DecimalPipe));
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoTableDetailComponent,
    selectors: [["po-table-detail"]],
    inputs: {
      componentsSize: [0, "p-components-size", "componentsSize"],
      items: [0, "p-items", "items"],
      parentRow: [0, "p-parent-row", "parentRow"],
      isSelectable: [0, "p-selectable", "isSelectable"],
      detail: [0, "p-detail", "detail"]
    },
    outputs: {
      selectRow: "p-select-row"
    },
    standalone: false,
    decls: 5,
    vars: 1,
    consts: [[1, "po-table-master-detail"], [1, "po-table-detail-row", 3, "po-table-row-active"], [1, "po-table-header", "po-table-column-selectable"], [1, "po-table-header", "po-table-header-column", "po-table-header-master-detail"], [1, "po-table-header", "po-table-header-ellipsis"], [1, "po-table-header-flex"], [1, "po-table-detail-row"], [1, "po-table-column-master-detail-space"], [1, "po-table-column-master-detail", "po-table-master-detail-label", 3, "po-table-column-right"], [1, "po-table-column-master-detail-space-checkbox"], [1, "po-table-column", "po-table-column-selectable"], ["name", "checkbox", 3, "p-change", "p-checkboxValue", "p-size"], [1, "po-table-column-master-detail", "po-table-master-detail-label", 3, "click"]],
    template: function PoTableDetailComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "table", 0);
        ɵɵconditionalCreate(1, PoTableDetailComponent_Conditional_1_Template, 6, 1, "thead");
        ɵɵelementStart(2, "tbody");
        ɵɵrepeaterCreate(3, PoTableDetailComponent_For_4_Template, 5, 3, "tr", 1, ɵɵrepeaterTrackByIndex);
        ɵɵelementEnd()();
      }
      if (rf & 2) {
        ɵɵadvance();
        ɵɵconditional(ctx.typeHeaderTop ? 1 : -1);
        ɵɵadvance(2);
        ɵɵrepeater(ctx.items);
      }
    },
    dependencies: [PoCheckboxComponent, CurrencyPipe, DatePipe, PoTimePipe],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoTableDetailComponent, [{
    type: Component,
    args: [{
      selector: "po-table-detail",
      standalone: false,
      template: `<table class="po-table-master-detail">
  @if (typeHeaderTop) {
    <thead>
      <tr>
        @if (isSelectable) {
          <th class="po-table-header po-table-column-selectable"></th>
        }
        <th class="po-table-header po-table-header-column po-table-header-master-detail"></th>
        @for (detail of detail.columns; track $index) {
          <th class="po-table-header po-table-header-ellipsis">
            <div
              class="po-table-header-flex"
              [class.po-table-header-flex-right]="detail.type === 'currency' || detail.type === 'number'"
            >
              {{ getColumnTitleLabel(detail) }}
            </div>
          </th>
        }
      </tr>
    </thead>
  }
  <tbody>
    @for (item of items; track $index) {
      <tr class="po-table-detail-row" [class.po-table-row-active]="item.$selected && isSelectable">
        @if (isSelectable) {
          <td class="po-table-column-master-detail-space-checkbox"></td>
          <td class="po-table-column po-table-column-selectable">
            <po-checkbox
              name="checkbox"
              (p-change)="onSelectRow(item)"
              [p-checkboxValue]="item.$selected"
              [p-size]="componentsSize"
            ></po-checkbox>
          </td>
        } @else {
          <td class="po-table-column-master-detail-space"></td>
        }
        @for (detail of detailColumns; track $index) {
          <td
            class="po-table-column-master-detail po-table-master-detail-label"
            (click)="isSelectable ? onSelectRow(item) : 'javascript:;'"
            [class.po-table-column-right]="typeHeaderTop && (detail.type === 'currency' || detail.type === 'number')"
          >
            @if (typeHeaderInline) {
              <strong> {{ getColumnTitleLabel(detail) }}: </strong>
            }
            @switch (detail.type) {
              @case ('currency') {
                <span>{{ getDetailData(item, detail) | currency: detail.format : 'symbol' : '1.2-2' }}</span>
              }
              @case ('date') {
                <span>{{ getDetailData(item, detail) | date: detail.format || 'dd/MM/yyyy' }}</span>
              }
              @case ('time') {
                <span>{{ getDetailData(item, detail) | po_time: detail.format || 'HH:mm:ss.ffffff' }}</span>
              }
              @case ('dateTime') {
                <span>
                  {{ getDetailData(item, detail) | date: detail.format || 'dd/MM/yyyy HH:mm:ss' }}
                </span>
              }
              @case ('number') {
                <span>{{ formatNumberDetail(getDetailData(item, detail), detail.format) }}</span>
              }
              @default {
                <span>{{ getDetailData(item, detail) }}</span>
              }
            }
          </td>
        }
      </tr>
    }
  </tbody>
</table>
`
    }]
  }], () => [{
    type: DecimalPipe
  }], {
    componentsSize: [{
      type: Input,
      args: ["p-components-size"]
    }],
    items: [{
      type: Input,
      args: ["p-items"]
    }],
    parentRow: [{
      type: Input,
      args: ["p-parent-row"]
    }],
    isSelectable: [{
      type: Input,
      args: ["p-selectable"]
    }],
    selectRow: [{
      type: Output,
      args: ["p-select-row"]
    }],
    detail: [{
      type: Input,
      args: ["p-detail"]
    }]
  });
})();
var PoTableSubtitleCircleComponent = class _PoTableSubtitleCircleComponent {
  poColorPaletteService;
  /** Esconde a tooltip. */
  hideTitle = false;
  _subtitle;
  /** Objeto com os dados da legenda. */
  set subtitle(subtitle) {
    if (subtitle) {
      subtitle.color = this.poColorPaletteService.getColor(subtitle);
    }
    this._subtitle = subtitle;
  }
  get subtitle() {
    return this._subtitle;
  }
  constructor(poColorPaletteService) {
    this.poColorPaletteService = poColorPaletteService;
  }
  static ɵfac = function PoTableSubtitleCircleComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoTableSubtitleCircleComponent)(ɵɵdirectiveInject(PoColorPaletteService));
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoTableSubtitleCircleComponent,
    selectors: [["po-table-subtitle-circle"]],
    inputs: {
      hideTitle: [0, "p-hide-title", "hideTitle"],
      subtitle: [0, "p-subtitle", "subtitle"]
    },
    standalone: false,
    decls: 2,
    vars: 5,
    consts: [[3, "title"]],
    template: function PoTableSubtitleCircleComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "span", 0);
        ɵɵtext(1);
        ɵɵelementEnd();
      }
      if (rf & 2) {
        ɵɵclassMap(ɵɵinterpolate1("po-table-subtitle-circle po-", ctx.subtitle == null ? null : ctx.subtitle.color));
        ɵɵproperty("title", ctx.hideTitle ? "" : ctx.subtitle == null ? null : ctx.subtitle.label);
        ɵɵadvance();
        ɵɵtextInterpolate1(" ", ctx.subtitle == null ? null : ctx.subtitle.content, "\n");
      }
    },
    encapsulation: 2,
    changeDetection: 0
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoTableSubtitleCircleComponent, [{
    type: Component,
    args: [{
      selector: "po-table-subtitle-circle",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: `<span [title]="hideTitle ? '' : subtitle?.label" class="po-table-subtitle-circle po-{{ subtitle?.color }}">
  {{ subtitle?.content }}
</span>
`
    }]
  }], () => [{
    type: PoColorPaletteService
  }], {
    hideTitle: [{
      type: Input,
      args: ["p-hide-title"]
    }],
    subtitle: [{
      type: Input,
      args: ["p-subtitle"]
    }]
  });
})();
var PoTableShowSubtitleComponent = class _PoTableShowSubtitleComponent {
  /** Objeto com os dados da legenda. */
  subtitles;
  /** Propriedade que recebe as literais definidas no `po-table`. */
  literals;
  /** Define o tamanho dos elementos que possuem `p-size` dentro do componente. */
  componentsSize;
  static ɵfac = function PoTableShowSubtitleComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoTableShowSubtitleComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoTableShowSubtitleComponent,
    selectors: [["po-table-show-subtitle"]],
    inputs: {
      subtitles: [0, "p-subtitles", "subtitles"],
      literals: [0, "p-literals", "literals"],
      componentsSize: [0, "p-components-size", "componentsSize"]
    },
    standalone: false,
    decls: 8,
    vars: 3,
    consts: [["modal", ""], [1, "po-table-footer-show-subtitle", 3, "click"], [1, "po-table-footer-show-subtitle-container"], [1, "po-table-show-subtitle"], ["p-size", "sm", 3, "p-title", "p-components-size"], [1, "po-table-subtitle-footer-modal", "po-row"], ["p-hide-title", "true", 3, "p-subtitle"], [1, "po-table-subtitle-text"]],
    template: function PoTableShowSubtitleComponent_Template(rf, ctx) {
      if (rf & 1) {
        const _r1 = ɵɵgetCurrentView();
        ɵɵelementStart(0, "div", 1);
        ɵɵlistener("click", function PoTableShowSubtitleComponent_Template_div_click_0_listener() {
          ɵɵrestoreView(_r1);
          const modal_r2 = ɵɵreference(5);
          return ɵɵresetView(modal_r2.open());
        });
        ɵɵelementStart(1, "div", 2)(2, "span", 3);
        ɵɵtext(3);
        ɵɵelementEnd()()();
        ɵɵelementStart(4, "po-modal", 4, 0);
        ɵɵrepeaterCreate(6, PoTableShowSubtitleComponent_For_7_Template, 4, 2, "div", 5, ɵɵrepeaterTrackByIndex);
        ɵɵelementEnd();
      }
      if (rf & 2) {
        ɵɵadvance(3);
        ɵɵtextInterpolate(ctx.literals.seeCompleteSubtitle);
        ɵɵadvance();
        ɵɵproperty("p-title", ctx.literals.completeSubtitle)("p-components-size", ctx.componentsSize);
        ɵɵadvance(2);
        ɵɵrepeater(ctx.subtitles);
      }
    },
    dependencies: [PoModalComponent, PoTableSubtitleCircleComponent],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoTableShowSubtitleComponent, [{
    type: Component,
    args: [{
      selector: "po-table-show-subtitle",
      standalone: false,
      template: '<div class="po-table-footer-show-subtitle" (click)="modal.open()">\n  <div class="po-table-footer-show-subtitle-container">\n    <span class="po-table-show-subtitle">{{ literals.seeCompleteSubtitle }}</span>\n  </div>\n</div>\n\n<po-modal #modal p-size="sm" [p-title]="literals.completeSubtitle" [p-components-size]="componentsSize">\n  @for (subtitle of subtitles; track $index) {\n    <div class="po-table-subtitle-footer-modal po-row">\n      <po-table-subtitle-circle p-hide-title="true" [p-subtitle]="subtitle"> </po-table-subtitle-circle>\n      <span class="po-table-subtitle-text">{{ subtitle.label }}</span>\n    </div>\n  }\n</po-modal>\n'
    }]
  }], null, {
    subtitles: [{
      type: Input,
      args: ["p-subtitles"]
    }],
    literals: [{
      type: Input,
      args: ["p-literals"]
    }],
    componentsSize: [{
      type: Input,
      args: ["p-components-size"]
    }]
  });
})();
var PoTableSubtitleFooterComponent = class _PoTableSubtitleFooterComponent {
  element;
  renderer;
  /** Define o tamanho dos elementos que possuem `p-size` dentro do componente. */
  componentsSize;
  /** Propriedade que recebe as literais definidas no `po-table`. */
  literals;
  /** Propriedade que recebe as legendas definidas no `PoTableSubtitleCircleComponent`. */
  subtitles;
  showSubtitle;
  resizeListener;
  isVisible;
  timeoutResize;
  constructor(element, renderer) {
    this.element = element;
    this.renderer = renderer;
  }
  ngAfterViewInit() {
    this.initializeResizeListener();
    this.debounceResize();
  }
  ngDoCheck() {
    if (!this.isVisible && this.getContainerSize() > 0) {
      this.toggleShowCompleteSubtitle();
      this.isVisible = true;
    }
  }
  ngOnDestroy() {
    this.removeResizeListener();
  }
  debounceResize() {
    clearTimeout(this.timeoutResize);
    this.timeoutResize = setTimeout(() => {
      this.toggleShowCompleteSubtitle();
    });
  }
  getContainerSize() {
    return this.element.nativeElement.querySelector(".po-table-subtitle-footer-container").offsetWidth;
  }
  getItemsSize() {
    const items = this.element.nativeElement.querySelectorAll(".po-table-subtitle-footer");
    return Array.from(items).map((item) => item["offsetWidth"]).reduce((a, b) => a + b, 16);
  }
  initializeResizeListener() {
    this.resizeListener = this.renderer.listen("window", "resize", (event) => {
      this.debounceResize();
    });
  }
  removeResizeListener() {
    this.resizeListener();
  }
  toggleShowCompleteSubtitle() {
    const containerSize = this.getContainerSize();
    const itemsSize = this.getItemsSize();
    this.showSubtitle = itemsSize > containerSize;
  }
  static ɵfac = function PoTableSubtitleFooterComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoTableSubtitleFooterComponent)(ɵɵdirectiveInject(ElementRef), ɵɵdirectiveInject(Renderer2));
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoTableSubtitleFooterComponent,
    selectors: [["po-table-subtitle-footer"]],
    inputs: {
      componentsSize: [0, "p-components-size", "componentsSize"],
      literals: [0, "p-literals", "literals"],
      subtitles: [0, "p-subtitles", "subtitles"]
    },
    standalone: false,
    decls: 4,
    vars: 1,
    consts: [[1, "po-table-subtitle-footer-container"], [1, "po-table-subtitle-footer"], [3, "p-components-size", "p-literals", "p-subtitles"], ["p-hide-title", "true", 3, "p-subtitle"], [1, "po-table-subtitle-text"]],
    template: function PoTableSubtitleFooterComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "div", 0);
        ɵɵrepeaterCreate(1, PoTableSubtitleFooterComponent_For_2_Template, 4, 2, "div", 1, ɵɵrepeaterTrackByIndex);
        ɵɵconditionalCreate(3, PoTableSubtitleFooterComponent_Conditional_3_Template, 1, 3, "po-table-show-subtitle", 2);
        ɵɵelementEnd();
      }
      if (rf & 2) {
        ɵɵadvance();
        ɵɵrepeater(ctx.subtitles);
        ɵɵadvance(2);
        ɵɵconditional(ctx.showSubtitle ? 3 : -1);
      }
    },
    dependencies: [PoTableShowSubtitleComponent, PoTableSubtitleCircleComponent],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoTableSubtitleFooterComponent, [{
    type: Component,
    args: [{
      selector: "po-table-subtitle-footer",
      standalone: false,
      template: '<div class="po-table-subtitle-footer-container">\n  @for (subtitle of subtitles; track $index) {\n    <div class="po-table-subtitle-footer">\n      <po-table-subtitle-circle p-hide-title="true" [p-subtitle]="subtitle"> </po-table-subtitle-circle>\n      <span class="po-table-subtitle-text">{{ subtitle.label }}</span>\n    </div>\n  }\n  @if (showSubtitle) {\n    <po-table-show-subtitle [p-components-size]="componentsSize" [p-literals]="literals" [p-subtitles]="subtitles">\n    </po-table-show-subtitle>\n  }\n</div>\n'
    }]
  }], () => [{
    type: ElementRef
  }, {
    type: Renderer2
  }], {
    componentsSize: [{
      type: Input,
      args: ["p-components-size"]
    }],
    literals: [{
      type: Input,
      args: ["p-literals"]
    }],
    subtitles: [{
      type: Input,
      args: ["p-subtitles"]
    }]
  });
})();
var PoTableColumnFrozenDirective = class _PoTableColumnFrozenDirective {
  el;
  renderer;
  _frozen = true;
  get frozen() {
    return this._frozen;
  }
  set frozen(val) {
    this._frozen = val;
    if (!val) {
      this.renderer.removeClass(this.el.nativeElement, "po-table-column-fixed");
    } else {
      this.updateStickyPosition();
    }
  }
  alignFrozen = "left";
  constructor(el, renderer) {
    this.el = el;
    this.renderer = renderer;
  }
  ngAfterViewInit() {
    setTimeout(() => {
      this.updateStickyPosition();
    }, 300);
  }
  ngOnChanges(changes) {
    if (changes.frozen.previousValue && !changes.frozen.currentValue) {
      setTimeout(() => {
        this.resizeColumns();
      }, 100);
    }
  }
  /* istanbul ignore next */
  updateStickyPosition() {
    if (this._frozen) {
      if (this.alignFrozen === "right") {
        let right = 0;
        const next = this.el.nativeElement.nextElementSibling;
        if (next) {
          right = this.getOuterWidth(next) + (parseFloat(next.style.right) || 0);
        }
        this.el.nativeElement.style.right = right + "px";
      } else {
        let left = 0;
        const prev = this.el.nativeElement.previousElementSibling;
        if (prev && !prev.classList.contains("po-table-column-selectable") && !prev.classList.contains("po-table-column-actions") && !prev.classList.contains("po-table-header-single-action") && !prev.classList.contains("po-table-column-single-action") && !prev.classList.contains("po-table-column-detail-toggle") && !prev.classList.contains("po-table-header-master-detail")) {
          left = this.getOuterWidth(prev) + (parseFloat(prev.style.left) || 0);
        }
        this.el.nativeElement.style.left = left - 1 + "px";
        this.renderer.addClass(this.el.nativeElement, "po-table-column-fixed");
      }
      const filterRow = this.el.nativeElement.parentElement.nextElementSibling;
      if (filterRow) {
        const index = this.index(this.el.nativeElement);
        if (filterRow.children && filterRow.children[index]) {
          filterRow.children[index].style.left = this.el.nativeElement.style.left;
          filterRow.children[index].style.right = this.el.nativeElement.style.right;
        }
      }
    }
  }
  /* istanbul ignore next */
  resizeColumns() {
    const currentElement = this.el.nativeElement;
    const prevElements = [];
    let prevElement = currentElement.previousElementSibling;
    while (prevElement && prevElement.classList.contains("po-table-column-fixed")) {
      prevElements.push(prevElement);
      prevElement = prevElement.previousElementSibling;
    }
    if (prevElements.length >= 2) {
      let leftAccumulator = 0;
      for (let i2 = prevElements.length - 1; i2 >= 0; i2--) {
        const prevWidth = this.getOuterWidth(prevElements[i2], true);
        prevElements[i2].style.left = leftAccumulator - 1 + "px";
        leftAccumulator += prevWidth;
      }
    }
  }
  getOuterWidth(el, margin) {
    let width = el.offsetWidth;
    if (margin) {
      const style2 = getComputedStyle(el);
      width += parseFloat(style2.marginLeft) + parseFloat(style2.marginRight);
    }
    return width - 1;
  }
  index(element) {
    const children = element.parentNode.childNodes;
    let num = 0;
    for (let i2 = 0; i2 < children.length; i2++) {
      if (children[i2] === element) {
        return num;
      }
      if (children[i2].nodeType === 1) {
        num++;
      }
    }
    return -1;
  }
  static ɵfac = function PoTableColumnFrozenDirective_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoTableColumnFrozenDirective)(ɵɵdirectiveInject(ElementRef), ɵɵdirectiveInject(Renderer2));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoTableColumnFrozenDirective,
    selectors: [["", "pFrozenColumn", ""]],
    hostAttrs: [1, "p-element"],
    hostVars: 2,
    hostBindings: function PoTableColumnFrozenDirective_HostBindings(rf, ctx) {
      if (rf & 2) {
        ɵɵclassProp("po-frozen-column", ctx.frozen);
      }
    },
    inputs: {
      frozen: [0, "pFrozenColumn", "frozen"],
      alignFrozen: "alignFrozen"
    },
    standalone: false,
    features: [ɵɵNgOnChangesFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoTableColumnFrozenDirective, [{
    type: Directive,
    args: [{
      selector: "[pFrozenColumn]",
      host: {
        class: "p-element",
        "[class.po-frozen-column]": "frozen"
      },
      standalone: false
    }]
  }], () => [{
    type: ElementRef
  }, {
    type: Renderer2
  }], {
    frozen: [{
      type: Input,
      args: ["pFrozenColumn"]
    }],
    alignFrozen: [{
      type: Input
    }]
  });
})();
var PoTableComponent = class _PoTableComponent extends PoTableBaseComponent {
  changeDetector;
  decimalPipe;
  defaultService;
  tableRowTemplate;
  tableCellTemplate;
  tableColumnTemplates;
  noColumnsHeader;
  poPopupComponent;
  modalDelete;
  tableFooterElement;
  tableWrapperElement;
  tableTemplate;
  tableVirtualScroll;
  tableScrollable;
  columnManager;
  columnBatchActions;
  columnActionLeft;
  actionsIconElement;
  actionsElement;
  filterInput;
  poSearchInput;
  searchAiComponent;
  viewPort;
  poNotification = inject(PoNotificationService);
  heightTableContainer;
  heightTableVirtual;
  popupTarget;
  tableOpacity = 0;
  tooltipText;
  itemSize;
  lastVisibleColumnsSelected;
  tagColor;
  idRadio;
  inputFieldValue = "";
  JSON;
  newOrderColumns;
  sizeLoading = "sm";
  headerWidth;
  close = {
    action: () => {
      this.modalDelete.close();
    },
    label: this.literals.cancel,
    danger: true
  };
  confirm = {
    action: () => {
      this.deleteItems();
    },
    label: this.literals.delete
  };
  _columnManagerTarget;
  _columnManagerTargetFixed;
  differ;
  footerHeight;
  timeoutResize;
  visibleElement = false;
  scrollEvent$;
  subscriptionScrollEvent;
  subscriptionService = new Subscription();
  aiParserSubscription;
  clickListener;
  resizeListener;
  set columnManagerTarget(value) {
    this._columnManagerTarget = value;
    this.changeDetector.detectChanges();
  }
  get columnManagerTarget() {
    return this._columnManagerTarget;
  }
  set columnManagerTargetFixed(value) {
    this._columnManagerTargetFixed = value;
    this.changeDetector.detectChanges();
  }
  get columnManagerTargetFixed() {
    return this._columnManagerTargetFixed;
  }
  /* eslint-disable max-params */
  constructor(poDate, differs, renderer, poLanguageService, changeDetector, decimalPipe, defaultService) {
    super(poDate, poLanguageService, defaultService);
    this.changeDetector = changeDetector;
    this.decimalPipe = decimalPipe;
    this.defaultService = defaultService;
    this.JSON = JSON;
    this.differ = differs.find([]).create(null);
    this.clickListener = renderer.listen("document", "click", () => {
    });
    this.resizeListener = renderer.listen("window", "resize", (event) => {
      this.debounceResize();
    });
  }
  /* eslint-enable max-params */
  get hasRowTemplateWithArrowDirectionRight() {
    return this.tableRowTemplate?.tableRowTemplateArrowDirection === PoTableRowTemplateArrowDirection.Right;
  }
  get columnCount() {
    const columnCount = this.mainColumns.length + (this.hasItems && this.actions.length > 0 ? 1 : 0) + (this.hasItems && this.selectable ? 1 : 0) + (!this.hideDetail && this.columnMasterDetail !== void 0 ? 1 : 0) + this.countExtraColumns();
    return columnCount || 1;
  }
  get columnCountForMasterDetail() {
    return this.mainColumns.length + 1 + (this.actions.length > 0 ? 1 : 0) + (this.selectable ? 1 : 0);
  }
  get detailHideSelect() {
    const masterDetail = this.columnMasterDetail;
    return masterDetail && masterDetail.detail ? masterDetail.detail.hideSelect : false;
  }
  get hasVisibleActions() {
    return !!this.visibleActions.length;
  }
  get firstAction() {
    return this.visibleActions && this.visibleActions[0];
  }
  get hasFooter() {
    return this.hasItems && this.hasVisibleSubtitleColumns;
  }
  get hasMasterDetailColumn() {
    return this.hasMainColumns && this.hasItems && !this.hideDetail && !!(this.columnMasterDetail || this.hasRowTemplate);
  }
  get hasRowTemplate() {
    return !!this.tableRowTemplate;
  }
  get hasSelectableColumn() {
    return this.selectable && this.hasItems && this.hasMainColumns;
  }
  get hasValidColumns() {
    return !!this.validColumns.length;
  }
  get hasVisibleSubtitleColumns() {
    return this.subtitleColumns.some((column) => column.visible !== false);
  }
  get isSingleAction() {
    return this.visibleActions.length === 1;
  }
  get isDraggable() {
    return this.draggable;
  }
  get inverseOfTranslation() {
    if (!this.viewPort || !this.viewPort["_renderedContentOffset"]) {
      return "-0px";
    }
    const offset = this.viewPort["_renderedContentOffset"];
    return `-${offset}px`;
  }
  ngOnInit() {
    this.idRadio = `po-radio-${uuid()}`;
  }
  changeHeaderWidth() {
    if (this.noColumnsHeader) {
      this.headerWidth = this.noColumnsHeader?.nativeElement.offsetWidth;
    }
    this.changeDetector.detectChanges();
  }
  ngAfterViewInit() {
    this.initialized = true;
    this.changeHeaderWidth();
    this.changeSizeLoading();
    this.applyFixedColumns();
    this.initializeVisibleElement();
  }
  showMoreInfiniteScroll({
    target
  }) {
    const scrollPosition = target.offsetHeight + target.scrollTop;
    if (!this.showMoreDisabled && scrollPosition >= target.scrollHeight * (this.infiniteScrollDistance / 110)) {
      this.onShowMore();
    }
  }
  ngDoCheck() {
    this.applyFixedColumns();
    this.checkChangesItems();
    this.verifyCalculateHeightTableContainer();
    if (this.initialized) {
      this.initializeVisibleElement();
    }
  }
  ngOnDestroy() {
    this.removeListeners();
    this.subscriptionService?.unsubscribe();
  }
  /**
   * Método responsável por realizar busca no serviço de dados podendo informar filtros e com o retorno, atualiza a tabela.
   *
   * Caso não seja informado parâmetro, nada será adicionado ao GET, conforme abaixo:
   * ```
   * url + ?page=1&pageSize=10
   * ```
   * > Obs: os parâmetros `page` e `pageSize` sempre serão chamados independente de ser enviados outros parâmetros.
   *
   * Caso sejam informados os parâmetros `{ name: 'JOHN', age: '23' }`, todos serão adicionados ao GET, conforme abaixo:
   * ```
   * url + ?page=1&pageSize=10&name=JOHN&age=23
   * ```
   *
   * @param { { key: value } } queryParams Formato do objeto a ser enviado.
   * > Pode ser utilizada qualquer string como key, e qualquer string ou number como value.
   */
  applyFilters(queryParams) {
    this.page = 1;
    this.initializeData(queryParams);
  }
  /**
   * Verifica se columns possuem a propriedade width.
   */
  applyFixedColumns() {
    return !this.columns.some((column) => !column.width);
  }
  /**
   * Método que colapsa uma linha com detalhe quando executada.
   *
   * @param { number } rowIndex Índice da linha que será colapsada.
   * > Ao reordenar os dados da tabela, o valor contido neste índice será alterado conforme a ordenação.
   */
  collapse(rowIndex) {
    this.setShowDetail(rowIndex, false);
  }
  /**
   * Método que expande uma linha com detalhe quando executada.
   *
   * @param { number } rowIndex Índice da linha que será expandida.
   * > Ao reordenar os dados da tabela, o valor contido neste índice será alterado conforme a ordenação.
   */
  expand(rowIndex) {
    this.setShowDetail(rowIndex, true);
  }
  /**
   * Retorna as linhas do `po-table` que estão selecionadas.
   */
  getSelectedRows() {
    return this.items.filter((item) => item.$selected);
  }
  /**
   * Retorna as linhas do `po-table` que não estão selecionadas.
   */
  getUnselectedRows() {
    return this.items.filter((item) => !item.$selected);
  }
  /**
   * Desmarca as linhas que estão selecionadas.
   */
  unselectRows() {
    const columnDetail = this.nameColumnDetail;
    this.items.forEach((item) => {
      const detailItems = columnDetail ? item[columnDetail] : null;
      if (Array.isArray(detailItems)) {
        detailItems.forEach((detailItem) => {
          detailItem.$selected = false;
        });
      }
      item.$selected = false;
    });
    this.selectAll = false;
  }
  checkDisabled(row, column) {
    return column.disabled ? column.disabled(row) : false;
  }
  containsMasterDetail(row) {
    return row[this.nameColumnDetail] && row[this.nameColumnDetail].length;
  }
  executeTableAction(row, tableAction) {
    if (!row.disabled && !this.validateTableAction(row, tableAction)) {
      tableAction.action(row);
      this.toggleRowAction(row);
    }
  }
  /**
   * Desmarca uma linha que está selecionada.
   */
  unselectRowItem(itemfn) {
    this.toggleSelect(itemfn, false);
    if (this.items.every((item) => !item.$selected)) {
      this.selectAll = false;
    } else {
      this.selectAll = null;
    }
  }
  /**
   * Seleciona uma linha do 'po-table'.
   */
  selectRowItem(itemfn) {
    this.toggleSelect(itemfn, true);
    if (this.items.every((item) => item.$selected)) {
      this.selectAll = true;
    } else {
      this.selectAll = null;
    }
  }
  /**
   * Método responsável pela exclusão de itens em lote.
   * Caso a tabela esteja executando a propriedade `p-service-delete`, será necessário excluir 1 item por vez.
   *
   * Ao utilizar `p-service-delete` mas sem a propriedade `p-service-api`, será responsabilidade do usuário o tratamento
   * após a requisição DELETE ser executada.
   *
   * Caso a tabela utilize `p-height` e esteja sem serviço, é necessário a reatribuição dos itens utilizando o evento `(p-delete-items)`, por exemplo:
   *
   * ```
   *<po-table
   *  (p-delete-items)="items = $event"
   * >
   *</po-table>
   * ```
   */
  deleteItems() {
    const newItems = [...this.items];
    const newItemsFiltered = [...newItems].filter((item) => !item.$selected);
    if (!this.serviceDeleteApi) {
      this.deleteItemsLocal(newItems, newItemsFiltered);
    } else {
      this.deleteItemsService(newItemsFiltered);
    }
  }
  formatNumber(value, format2) {
    if (!format2) {
      return value;
    }
    return this.decimalPipe.transform(value, format2);
  }
  formatWithMask(value, mask) {
    if (!mask || value == null || value === "") {
      return value ?? "";
    }
    const rawValue = String(value).replace(/[^a-zA-Z\d]/g, "");
    let formatted = "";
    let rawIndex = 0;
    for (let maskIndex = 0; maskIndex < mask.length && rawIndex < rawValue.length; maskIndex++) {
      const maskChar = mask[maskIndex];
      const rawChar = rawValue[rawIndex];
      const isValid = this.isValidMaskChar(maskChar, rawChar);
      if (isValid === true) {
        formatted += rawChar;
        rawIndex++;
      } else if (isValid === false) {
        break;
      } else {
        formatted += maskChar;
      }
    }
    return formatted;
  }
  isValidMaskChar(maskChar, rawChar) {
    switch (maskChar) {
      case "9":
        return /\d/.test(rawChar);
      case "@":
        return /[a-zA-Z]/.test(rawChar);
      case "w":
        return /[a-zA-Z\d]/.test(rawChar);
      default:
        return null;
    }
  }
  getCellData(row, column) {
    const arrayProperty = column.property.split(".");
    if (arrayProperty.length > 1) {
      const nestedProperties = arrayProperty;
      let value = row;
      for (const property of nestedProperties) {
        value = value[property] || value[property] === 0 ? value[property] : "";
      }
      return value;
    } else {
      return row[column.property];
    }
  }
  getBooleanLabel(rowValue, columnBoolean) {
    if (rowValue || rowValue === false || rowValue === 0) {
      rowValue = PoUtils.convertToBoolean(rowValue);
      if (columnBoolean.boolean) {
        return rowValue ? columnBoolean.boolean.trueLabel || "Sim" : columnBoolean.boolean.falseLabel || "Não";
      } else {
        return rowValue ? "Sim" : "Não";
      }
    }
    return rowValue;
  }
  getColumnIcons(row, column) {
    const rowIcons = this.getCellData(row, column);
    if (column.icons) {
      if (Array.isArray(rowIcons)) {
        return this.mergeCustomIcons(rowIcons, column.icons);
      } else {
        return this.findCustomIcon(rowIcons, column);
      }
    }
    return rowIcons;
  }
  getColumnLabel(row, columnLabel) {
    return columnLabel.labels.find((labelItem) => this.getCellData(row, columnLabel) === labelItem.value);
  }
  getSubtitleColumn(row, subtitleColumn) {
    return subtitleColumn.subtitles.find((subtitleItem) => this.getCellData(row, subtitleColumn) === subtitleItem.value);
  }
  isShowMasterDetail(row) {
    return !this.hideDetail && this.nameColumnDetail && row.$showDetail && this.containsMasterDetail(row) && !this.hasRowTemplate;
  }
  isShowRowTemplate(row, index) {
    if (this.tableRowTemplate && this.tableRowTemplate.poTableRowTemplateShow) {
      return this.tableRowTemplate.poTableRowTemplateShow(row, index);
    }
    return true;
  }
  onClickLink(event, row, column) {
    if (!this.checkDisabled(row, column)) {
      event.stopPropagation();
    }
  }
  onChangeVisibleColumns(columns) {
    this.changeVisibleColumns.emit(columns);
  }
  onChangeFixedColumns(columns) {
    if (!this.hideActionFixedColumns) {
      this.changeFixedColumns.emit(columns);
    }
  }
  onColumnRestoreManager(value) {
    this.columnRestoreManager.emit(value);
  }
  onVisibleColumnsChange(columns) {
    this.columns = columns;
    this.changeDetector.detectChanges();
  }
  tooltipMouseEnter(event, column, row) {
    this.tooltipText = void 0;
    if (event.target.offsetWidth < event.target.scrollWidth && event.target.innerText.trim()) {
      return this.tooltipText = event.target.innerText;
    }
    if (column) {
      this.checkingIfColumnHasTooltip(column, row);
    }
  }
  tooltipMouseLeave() {
    this.tooltipText = void 0;
  }
  togglePopup(row, targetRef) {
    this.popupTarget = targetRef;
    this.changeDetector.detectChanges();
    this.poPopupComponent.toggle(row);
  }
  trackBy(index) {
    return index;
  }
  validateTableAction(row, tableAction) {
    if (typeof tableAction.disabled === "function") {
      return tableAction.disabled(row);
    } else {
      return tableAction.disabled;
    }
  }
  onOpenColumnManager() {
    this.lastVisibleColumnsSelected = [...this.columns];
  }
  onFilteredItemsChange(items) {
    if (this.sortedColumn.property) {
      this.sortArray(this.sortedColumn.property, this.sortedColumn.ascending, items);
    } else {
      this.filteredItems = items;
    }
  }
  get searchAiColumns() {
    if (this.searchAiField()?.columns) {
      return this.searchAiField().columns;
    }
    return extractSearchAiColumns(this.columns);
  }
  get searchAiPlaceholder() {
    return this.searchAiField()?.placeholder ?? this.literals.searchAiPlaceholder;
  }
  onAiResult(result) {
    this.searchAiResult.emit(result);
    const apply = this.searchAiField()?.apply ?? "auto";
    if (typeof apply === "function") {
      apply(result);
      return;
    }
    if (apply === "none") {
      return;
    }
    if (this.shouldDelegateToServer(apply)) {
      this.applyFilterViaServer(result);
      return;
    }
    if (apply === "parser" && this.hasService) {
      this.applyFilterViaParser(result);
      return;
    }
    this.applyFilterLocally(result);
  }
  onAiLowConfidence(result) {
    this.searchAiLowConfidence.emit(result);
  }
  onAiError(error2) {
    this.searchAiError.emit(error2);
  }
  onAiClear() {
    if (this.hasService) {
      this.page = 1;
      this.initializeData();
    } else {
      this.filteredItems = this.height ? [...this.items] : this.items;
    }
  }
  shouldDelegateToServer(apply) {
    return apply === "server" || apply === "auto" && this.hasService;
  }
  applyFilterViaServer(result) {
    this.page = 1;
    this.initializeData({
      $filter: result.filter
    });
  }
  applyFilterViaParser(result) {
    this.loading = true;
    this.aiParserSubscription?.unsubscribe();
    this.aiParserSubscription = this.getFilteredItems({
      pageSize: 9999,
      page: 1
    }).subscribe({
      next: (data) => {
        this.loading = false;
        this.filteredItems = poSearchAiFilterItems(data.items, result.filter);
      },
      error: () => {
        this.loading = false;
      }
    });
    this.subscriptionService.add(this.aiParserSubscription);
  }
  applyFilterLocally(result) {
    this.filteredItems = poSearchAiFilterItems(this.items, result.filter);
  }
  /**
   * Atualiza programaticamente o valor do campo de busca por IA (`po-search-ai`) integrado à tabela
   * via `p-search-ai-field`.
   *
   * Útil quando a aplicação precisa preencher a busca a partir de uma ação externa (por exemplo, o
   * clique em um botão que sugere uma consulta pronta), opcionalmente disparando a busca em seguida.
   *
   * > Só tem efeito quando a propriedade `p-search-ai-field` está configurada. Caso contrário, o método
   * > não executa nenhuma ação.
   *
   * @param { string } value Texto da consulta a ser inserido no campo de busca por IA.
   * @param { boolean } triggerSearch Quando `true`, dispara automaticamente a busca após preencher o
   * valor. Quando `false` _(padrão)_, apenas preenche o campo.
   *
   * @default `triggerSearch = false`
   *
   * @example
   *
   * ```typescript
   * @ViewChild(PoTableComponent) table: PoTableComponent;
   *
   * onSuggestionClick() {
   *   // apenas preenche o campo
   *   this.table.updateSearchAIQuery('clientes de SP com saldo acima de 500');
   *
   *   // preenche e já dispara a busca
   *   this.table.updateSearchAIQuery('clientes de SP com saldo acima de 500', true);
   * }
   * ```
   */
  updateSearchAIQuery(value, triggerSearch = false) {
    if (!this.searchAiField() || !this.searchAiComponent) {
      return;
    }
    this.searchAiComponent.writeValueModel(value);
    if (triggerSearch) {
      this.searchAiComponent.search();
    }
  }
  /**
   * Método que remove um item da tabela.
   *
   * @param { number | { key: value } } item Índice da linha ou o item que será removido.
   * > Ao remover o item, a linha que o representa será excluída da tabela.
   */
  removeItem(item) {
    if (item instanceof Object) {
      this.items = this.items.filter((filterItem) => filterItem !== item);
    } else if (typeof item === "number") {
      const index = item;
      this.items.splice(index, 1);
    }
  }
  /**
   * Método que atualiza um item da tabela.
   *
   * @param { number | { key: value } } item Índice da linha ou o item que será atualizado.
   * @param { { key: value } } updatedItem Item que foi atualizado.
   * > Ao atualizar o item, a informação será alterada na tabela.
   */
  updateItem(item, updatedItem) {
    if (typeof item === "number") {
      this.items.splice(item, 1, updatedItem);
    } else {
      const index = this.items.findIndex((indexItem) => indexItem === item);
      this.items.splice(index, 1, updatedItem);
    }
  }
  drop(event) {
    if (!this.mainColumns[event.currentIndex].fixed) {
      moveItemInArray(this.mainColumns, event.previousIndex, event.currentIndex);
      if (this.hideColumnsManager === false) {
        this.newOrderColumns = this.mainColumns;
        const detail = this.columns.filter((item) => item.property === "detail")[0];
        if (detail !== void 0) {
          this.newOrderColumns.push(detail);
        }
        this.columns.map((item, index) => {
          if (!item.visible) {
            this.newOrderColumns.splice(index, 0, item);
          }
        });
        this.columns = this.newOrderColumns;
        this.onVisibleColumnsChange(this.newOrderColumns);
      }
    }
  }
  getTemplate(column) {
    const template = this.tableColumnTemplates?.find((tableColumnTemplate) => tableColumnTemplate.targetProperty === column.property);
    if (!this.initialized) return null;
    if (template) {
      return template.templateRef;
    } else {
      console.warn(`Não foi possível encontrar o template para a coluna: ${column.property}, por gentileza informe a propriedade [p-property]`);
      return null;
    }
  }
  getWidthColumnManager() {
    return this.columnManager?.nativeElement.offsetWidth;
  }
  getColumnWidthActionsLeft() {
    return this.columnActionLeft?.nativeElement.offsetWidth;
  }
  hasSomeFixed() {
    return this.columns.some((item) => item.fixed === true);
  }
  calculateHeightTableContainer(height) {
    this.itemSize = PO_TABLE_ROW_HEIGHT_BY_SPACING[this.spacing] ?? PO_TABLE_ROW_HEIGHT_BY_SPACING[PoTableColumnSpacing.Medium];
    this.heightTableContainer = height ? height - this.getHeightTableFooter() : void 0;
    this.heightTableVirtual = this.heightTableContainer ? this.heightTableContainer - this.itemSize : void 0;
    this.setTableOpacity(1);
    this.changeDetector.markForCheck();
  }
  verifyCalculateHeightTableContainer() {
    if (this.height && this.verifyChangeHeightInFooter()) {
      this.footerHeight = this.getHeightTableFooter();
      this.calculateHeightTableContainer(this.height);
    }
  }
  checkInfiniteScroll() {
    if (this.hasInfiniteScroll()) {
      let scrollHeight = 0;
      if (this.virtualScroll) {
        scrollHeight = this.tableVirtualScroll.nativeElement.scrollHeight;
      } else {
        scrollHeight = this.tableScrollable.nativeElement.scrollHeight;
      }
      if (scrollHeight >= this.height) {
        this.includeInfiniteScroll();
      } else {
        this.infiniteScroll = false;
      }
    }
    this.changeDetector.detectChanges();
  }
  changesAfterDelete(newItemsFiltered) {
    this.selectAll = false;
    this.setSelectedList();
    this.modalDelete.close();
    this.poNotification.success(this.literals.deleteSuccessful);
    this.eventDelete.emit(newItemsFiltered);
  }
  changeSizeLoading() {
    const tableHeight = this.tableWrapperElement?.nativeElement?.offsetHeight;
    if (tableHeight <= 150 || this.componentsSize === PoFieldSize.Small) {
      this.sizeLoading = "sm";
    } else if (tableHeight > 150 && tableHeight < 260) {
      this.sizeLoading = "md";
    } else {
      this.sizeLoading = "lg";
    }
    this.changeDetector.detectChanges();
  }
  getDefaultSpacing() {
    return this.componentsSize === PoFieldSize.Small || getDefaultSizeFn(PoTableColumnSpacing) === PoTableColumnSpacing.Small ? PoTableColumnSpacing.ExtraSmall : PoTableColumnSpacing.Medium;
  }
  reapplySort() {
    const hasData = this.filteredItems && this.filteredItems.length > 0;
    const hasSortConfig = !!this.sortedColumn?.property;
    if (hasData && hasSortConfig) {
      this.sortArray(this.sortedColumn.property, this.sortedColumn.ascending);
    }
  }
  checkChangesItems() {
    const changesItems = this.differ.diff(this.items);
    if (changesItems && this.selectAll) {
      this.selectAll = null;
    }
    if (changesItems && !this.hasColumns && this.hasItems) {
      this.columns = this.getDefaultColumns(this.items[0]);
    }
  }
  checkingIfColumnHasTooltip(column, row) {
    if (column.type === "link" && column.tooltip && !this.checkDisabled(row, column)) {
      return this.tooltipText = column.tooltip;
    }
    if (column.type === "label") {
      const columnLabel = this.getColumnLabel(row, column);
      return this.tooltipText = columnLabel?.tooltip;
    }
  }
  countExtraColumns() {
    let extraColumns = 0;
    if (!this.columnMasterDetail && this.hasItems) {
      if ((this.hasMasterDetailColumn || this.hasRowTemplate) && this.hasMainColumns && !this.hasRowTemplateWithArrowDirectionRight) {
        extraColumns++;
      }
      if (this.hasRowTemplateWithArrowDirectionRight && this.hasMainColumns && (this.hasVisibleActions || this.hideColumnsManager)) {
        extraColumns++;
      }
    }
    return extraColumns;
  }
  debounceResize() {
    clearTimeout(this.timeoutResize);
    this.timeoutResize = setTimeout(() => {
      this.setTableOpacity(1);
      this.changeDetector.markForCheck();
    });
  }
  deleteItemsLocal(newItems, newItemsFiltered) {
    if (this.height) {
      this.items = newItemsFiltered;
    } else {
      let index = this.items.length - 1;
      newItems.slice().reverse().forEach((item) => {
        if (item.$selected) {
          this.removeItem(index);
        }
        index--;
      });
    }
    this.changesAfterDelete(newItemsFiltered);
    this.onFilteredItemsChange(newItemsFiltered);
  }
  deleteItemsService(newItemsFiltered) {
    this.subscriptionService.add(this.defaultService.deleteItem(this.paramDeleteApi, this.itemsSelected[0][this.paramDeleteApi]).subscribe({
      next: (value) => {
        if (this.hasService) {
          const filteredParams = __spreadProps(__spreadValues({}, this.paramsFilter), {
            pageSize: newItemsFiltered.length + 1,
            page: 1
          });
          this.loading = true;
          this.subscriptionService.add(this.defaultService.getFilteredItems(filteredParams).subscribe((items) => {
            this.setTableResponseProperties(items);
          }));
        }
        this.items = newItemsFiltered;
        this.changesAfterDelete(newItemsFiltered);
      },
      error: (error2) => {
        this.poNotification.error(this.literals.deleteApiError);
        this.modalDelete.close();
        this.eventDelete.emit(this.items);
      }
    }));
  }
  findCustomIcon(rowIcons, column) {
    const customIcon = column.icons.find((icon) => rowIcons === icon.value);
    return customIcon ? [customIcon] : void 0;
  }
  getHeightTableFooter() {
    return this.tableFooterElement ? this.tableFooterElement.nativeElement.offsetHeight : 0;
  }
  hasInfiniteScroll() {
    let scrollHeight = 0;
    if (this.virtualScroll && this.tableVirtualScroll) {
      scrollHeight = this.tableVirtualScroll.nativeElement.scrollHeight;
    }
    if (!this.virtualScroll && this.tableScrollable) {
      scrollHeight = this.tableScrollable.nativeElement.scrollHeight;
    }
    return this.infiniteScroll && this.hasItems && !this.subscriptionScrollEvent && this.height > 0 && scrollHeight > 0;
  }
  includeInfiniteScroll() {
    let element = null;
    if (this.virtualScroll) {
      element = this.tableVirtualScroll?.nativeElement;
    } else {
      element = this.tableScrollable.nativeElement.closest(".po-table-container-overflow");
    }
    if (element) {
      this.scrollEvent$ = this.defaultService.scrollListener(element);
      this.subscriptionScrollEvent = this.scrollEvent$.subscribe((event) => this.showMoreInfiniteScroll(event));
    }
    this.changeDetector.detectChanges();
  }
  mergeCustomIcons(rowIcons, customIcons) {
    const mergedIcons = [];
    rowIcons.forEach((columnValue) => {
      const foundCustomIcon = customIcons.find((customIcon) => columnValue === customIcon.icon || columnValue === customIcon.value);
      foundCustomIcon ? mergedIcons.push(foundCustomIcon) : mergedIcons.push(columnValue);
    });
    return mergedIcons;
  }
  initializeVisibleElement() {
    if (this.tableWrapperElement?.nativeElement.offsetWidth && !this.visibleElement) {
      this.debounceResize();
      this.checkInfiniteScroll();
      this.visibleElement = true;
    }
  }
  removeListeners() {
    if (this.resizeListener) {
      this.resizeListener();
    }
    if (this.clickListener) {
      this.clickListener();
    }
    if (this.subscriptionScrollEvent) {
      this.subscriptionScrollEvent.unsubscribe();
    }
  }
  setTableOpacity(value) {
    this.tableOpacity = value;
  }
  verifyChangeHeightInFooter() {
    return this.footerHeight !== this.getHeightTableFooter();
  }
  toggleSelect(compare2, selectValue) {
    if (typeof compare2 !== "function") {
      this.items.forEach((item) => {
        if (item === compare2) {
          item.$selected = selectValue;
        }
      });
    } else {
      this.items.forEach((item) => {
        if (compare2(item)) {
          item.$selected = selectValue;
        }
      });
    }
  }
  static ɵfac = function PoTableComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoTableComponent)(ɵɵdirectiveInject(PoDateService), ɵɵdirectiveInject(IterableDiffers), ɵɵdirectiveInject(Renderer2), ɵɵdirectiveInject(PoLanguageService), ɵɵdirectiveInject(ChangeDetectorRef), ɵɵdirectiveInject(DecimalPipe), ɵɵdirectiveInject(PoTableService));
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoTableComponent,
    selectors: [["po-table"]],
    contentQueries: function PoTableComponent_ContentQueries(rf, ctx, dirIndex) {
      if (rf & 1) {
        ɵɵcontentQuery(dirIndex, PoTableRowTemplateDirective, 7)(dirIndex, PoTableCellTemplateDirective, 5)(dirIndex, PoTableColumnTemplateDirective, 4);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.tableRowTemplate = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.tableCellTemplate = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.tableColumnTemplates = _t);
      }
    },
    viewQuery: function PoTableComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c97, 5, ElementRef)(_c36, 5)(PoModalComponent, 7)(_c98, 5, ElementRef)(_c99, 5, ElementRef)(_c100, 5, ElementRef)(_c101, 5, ElementRef)(_c102, 5, ElementRef)(_c103, 5, ElementRef)(_c104, 5, ElementRef)(_c105, 5, ElementRef)(_c106, 5)(_c94, 7, ElementRef)(PoSearchAiComponent, 5)(CdkVirtualScrollViewport, 5)(_c107, 5)(_c108, 5)(_c109, 5, ElementRef)(_c110, 5, ElementRef);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.noColumnsHeader = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.poPopupComponent = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.modalDelete = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.tableFooterElement = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.tableWrapperElement = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.tableTemplate = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.tableVirtualScroll = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.tableScrollable = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.columnManager = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.columnBatchActions = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.columnActionLeft = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.filterInput = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.poSearchInput = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.searchAiComponent = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.viewPort = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.columnManagerTarget = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.columnManagerTargetFixed = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.actionsIconElement = _t);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.actionsElement = _t);
      }
    },
    standalone: false,
    features: [ɵɵProvidersFeature([PoDateService, PoTableService]), ɵɵInheritDefinitionFeature],
    decls: 31,
    vars: 15,
    consts: [["tableContainerTemplate", ""], ["tableTemplate", ""], ["tableVirtualScrollTemplate", ""], ["popup", ""], ["poTableColumnDetail", ""], ["inputRadio", ""], ["inputCheckbox", ""], ["contentHeaderTemplate", ""], ["ActionsColumnTemplate", ""], ["modalDelete", ""], ["columnBatchActions", ""], ["columnManager", ""], ["columnManagerTarget", ""], ["tableWrapper", ""], ["poTableTbody", ""], ["tableFooter", ""], ["tableScrollable", ""], ["columnActionLeft", ""], ["noColumnsHeader", ""], ["tableVirtualScroll", ""], ["columnHeader", ""], ["popupTarget", ""], [1, "po-table-actions"], [1, "po-table-actions-batch-actions"], [1, "po-table-actions-column-manager"], [1, "po-table-search"], ["p-no-padding", ""], [1, "po-row", "po-table-footer-show-more", 3, "po-invisible"], [3, "p-actions", "p-size", "p-target"], [3, "p-columns", "p-max-columns", "p-target", "p-last-visible-columns-selected", "p-hide-action-fixed-columns", "p-columns-default", "p-components-size"], [3, "p-components-size", "p-title", "p-primary-action", "p-secondary-action", "p-click-out"], [1, "po-font-text-large"], [1, "po-table-actions-batch-actions__label", 3, "ngPlural"], ["ngPluralCase", "=0"], ["ngPluralCase", "=1"], ["ngPluralCase", "other"], [1, "po-table-actions-batch-actions__buttons"], ["p-icon", "ICON_DELETE", 3, "p-click", "p-danger", "p-disabled", "p-label", "p-size"], ["p-icon", "ICON_SETTINGS", "p-kind", "tertiary", "p-tooltip-position", "left", 3, "p-click", "p-aria-label", "p-tooltip", "p-size"], [3, "p-result", "p-low-confidence", "p-error", "p-clear", "p-url", "p-clean", "p-columns", "p-min-confidence", "p-timeout", "p-placeholder", "p-literals", "p-size"], [3, "p-filtered-items-change", "p-items", "p-filter-keys", "p-filter-type", "p-size"], [4, "ngTemplateOutlet"], [3, "p-text", "p-size"], [1, "po-table-main-container"], [1, "po-table-wrapper"], [1, "po-table-container", 3, "height"], [1, "po-table-footer"], [1, "po-table-container"], [1, "po-table-container-overflow", 3, "height"], [1, "po-table-container-overflow"], [3, "p-components-size", "p-literals", "p-subtitles"], [1, "po-row", "po-table-footer-show-more"], [1, "po-offset-xl-4", "po-offset-lg-4", "po-offset-md-3", "po-lg-4", "po-md-6", 3, "p-click", "p-disabled", "p-label", "p-loading", "p-size"], [1, "po-table", 3, "ngClass"], ["cdkDropList", "", "cdkDropListOrientation", "horizontal", 3, "cdkDropListDropped", "ngClass"], [1, "po-table-column-selectable", 3, "pointer-events"], [1, "po-table-header-column", "po-table-header-master-detail"], [3, "po-table-header-master-detail", "po-table-header-single-action"], [1, "po-table-header-column", "po-text-center"], [3, "po-table-header-single-action", "po-table-header-actions"], [1, "po-table-group-row"], [1, "po-table-column-selectable"], ["name", "selectAll", 3, "p-size", "p-checkboxValue"], ["name", "selectAll", 3, "p-change", "p-size", "p-checkboxValue"], [1, "po-table-header-fixed-inner", 3, "width"], [1, "po-table-header-fixed-inner"], ["cdkDrag", "", "cdkDragLockAxis", "x", 1, "po-table-header-ellipsis", "p-element", "po-frozen-column", 3, "width", "max-width", "min-width", "po-clickable", "ngClass", "ngStyle", "po-table-header-subtitle", "po-table-column-drag-box", "cdkDragDisabled", "pFrozenColumn"], ["cdkDrag", "", "cdkDragLockAxis", "x", 1, "po-table-header-ellipsis", "p-element", "po-frozen-column", 3, "click", "ngClass", "ngStyle", "cdkDragDisabled", "pFrozenColumn"], [1, "po-table-header-flex"], ["cdkDragHandle", "", "p-icon", "ICON_DRAG"], [4, "ngTemplateOutlet", "ngTemplateOutletContext"], [1, "po-table-header-ellipsis", "p-element", "po-frozen-column", 3, "width", "max-width", "min-width", "po-clickable", "ngClass", "ngStyle", "po-table-header-subtitle", "pFrozenColumn"], [1, "po-table-header-ellipsis", "p-element", "po-frozen-column", 3, "click", "ngClass", "ngStyle", "pFrozenColumn"], [1, "po-table-row", "po-table-row-no-data"], [1, "po-table-no-data", "po-text-center", 3, "colSpan"], [1, "po-table-row"], [1, "po-table-column-detail-toggle"], [3, "ngTemplateOutlet", "ngTemplateOutletContext"], [1, "p-element", "po-frozen-column", 3, "width", "max-width", "min-width", "po-table-column", "po-table-column-right", "po-table-column-center", "po-table-column-icons", "pFrozenColumn", "ngClass"], [1, "po-table-column-detail-toggle", 3, "click"], [1, "p-element", "po-frozen-column", 3, "click", "pFrozenColumn", "ngClass"], [1, "po-table-column-cell", "po-table-body-ellipsis", "notranslate", 3, "mouseenter", "mouseleave", "p-tooltip", "p-append-in-body"], [3, "p-action", "p-disabled", "p-link", "p-row", "p-value"], [3, "p-column", "p-icons", "p-row"], [3, "click", "p-action", "p-disabled", "p-link", "p-row", "p-value"], [3, "p-subtitle"], [3, "p-value"], [1, "po-table-row-template-container", 3, "colSpan"], [1, "po-table-column-detail", 3, "colSpan"], [3, "p-select-row", "p-selectable", "p-detail", "p-items", "p-parent-row", "p-components-size"], [3, "itemSize", "minBufferPx", "maxBufferPx"], [1, "po-table-header-sticky"], ["cdkDropList", "", "cdkDropListOrientation", "horizontal", 3, "cdkDropListDropped"], ["name", "selectAll", 3, "p-checkboxValue", "p-size"], ["name", "selectAll", 3, "p-change", "p-checkboxValue", "p-size"], [1, "po-table-header-ellipsis", "p-element", "po-frozen-column", "example-box", 3, "width", "max-width", "min-width", "po-clickable", "ngClass", "ngStyle", "po-table-header-subtitle", "pFrozenColumn"], [1, "po-table-header-ellipsis", "p-element", "po-frozen-column", "example-box", 3, "click", "ngClass", "ngStyle", "pFrozenColumn"], ["class", "po-table-group-row", 4, "cdkVirtualFor", "cdkVirtualForOf", "cdkVirtualForTrackBy"], [1, "p-element", "po-frozen-column", 3, "width", "max-width", "min-width", "po-table-column", "po-table-column-right", "po-table-column-center", "po-table-column-icons", "ngClass", "pFrozenColumn"], [1, "p-element", "po-frozen-column", 3, "click", "ngClass", "pFrozenColumn"], [1, "po-clickable", 3, "p-icon"], [3, "p-change-selected", "name", "p-checked", "p-size"], ["name", "checkbox", 3, "p-change", "p-checkboxValue", "p-size"], [1, "po-table-header-ellipsis", 3, "mouseenter", "mouseleave", "p-tooltip", "p-append-in-body"], [3, "po-table-header-icon-unselected", "po-table-header-icon-descending", "po-table-header-icon-ascending"], ["p-icon", "ICON_SORT"], ["p-icon", "ICON_SORT_ASC"], ["cdkDragHandle", "", "p-icon", "ICON_SORT_DESC"], [1, "po-table-column", "po-table-column-single-action", 3, "width", "max-width"], [1, "po-table-column-actions"], [1, "po-table-column", "po-table-column-single-action"], [1, "po-table-single-action", "po-clickable", 3, "po-table-action-disabled"], [1, "po-table-single-action", "po-clickable", 3, "click"], [1, "po-table-single-action-content", 3, "p-icon"], [1, "po-clickable", 3, "click"], ["p-icon", "ICON_MORE", 1, "po-field-icon"], [3, "p-visible-columns-change", "p-change-visible-columns", "p-change-fixed-columns", "p-initial-columns", "p-columns", "p-max-columns", "p-target", "p-last-visible-columns-selected", "p-hide-action-fixed-columns", "p-columns-default", "p-components-size"]],
    template: function PoTableComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "div", 22);
        ɵɵconditionalCreate(1, PoTableComponent_Conditional_1_Template, 9, 5, "div", 23);
        ɵɵconditionalCreate(2, PoTableComponent_Conditional_2_Template, 4, 3, "div", 24);
        ɵɵconditionalCreate(3, PoTableComponent_Conditional_3_Template, 2, 8, "div", 25)(4, PoTableComponent_Conditional_4_Template, 2, 4, "div", 25);
        ɵɵelementEnd();
        ɵɵconditionalCreate(5, PoTableComponent_Conditional_5_Template, 2, 1, "po-container", 26)(6, PoTableComponent_Conditional_6_Template, 8, 10);
        ɵɵtemplate(7, PoTableComponent_ng_template_7_Template, 8, 10, "ng-template", null, 0, ɵɵtemplateRefExtractor);
        ɵɵconditionalCreate(9, PoTableComponent_Conditional_9_Template, 3, 6, "div", 27);
        ɵɵtemplate(10, PoTableComponent_ng_template_10_Template, 14, 26, "ng-template", null, 1, ɵɵtemplateRefExtractor)(12, PoTableComponent_ng_template_12_Template, 15, 27, "ng-template", null, 2, ɵɵtemplateRefExtractor);
        ɵɵelement(14, "po-popup", 28, 3);
        ɵɵtemplate(16, PoTableComponent_ng_template_16_Template, 1, 1, "ng-template", null, 4, ɵɵtemplateRefExtractor)(18, PoTableComponent_ng_template_18_Template, 1, 3, "ng-template", null, 5, ɵɵtemplateRefExtractor)(20, PoTableComponent_ng_template_20_Template, 1, 2, "ng-template", null, 6, ɵɵtemplateRefExtractor)(22, PoTableComponent_ng_template_22_Template, 5, 6, "ng-template", null, 7, ɵɵtemplateRefExtractor)(24, PoTableComponent_ng_template_24_Template, 2, 2, "ng-template", null, 8, ɵɵtemplateRefExtractor);
        ɵɵconditionalCreate(26, PoTableComponent_Conditional_26_Template, 1, 7, "po-table-column-manager", 29);
        ɵɵelementStart(27, "po-modal", 30, 9)(29, "p", 31);
        ɵɵtext(30);
        ɵɵelementEnd()();
      }
      if (rf & 2) {
        ɵɵadvance();
        ɵɵconditional(ctx.hasValidColumns && ctx.itemsSelected.length > 0 && !ctx.hideBatchActions ? 1 : -1);
        ɵɵadvance();
        ɵɵconditional(ctx.hasValidColumns && !ctx.hideColumnsManager ? 2 : -1);
        ɵɵadvance();
        ɵɵconditional(ctx.searchAiField() ? 3 : !ctx.hideTableSearch && ctx.hasValidColumns ? 4 : -1);
        ɵɵadvance(2);
        ɵɵconditional(ctx.container ? 5 : 6);
        ɵɵadvance(4);
        ɵɵconditional(!ctx.infiniteScroll ? 9 : -1);
        ɵɵadvance(5);
        ɵɵproperty("p-actions", ctx.actions)("p-size", ctx.componentsSize)("p-target", ctx.popupTarget);
        ɵɵadvance(12);
        ɵɵconditional(!ctx.hideColumnsManager ? 26 : -1);
        ɵɵadvance();
        ɵɵproperty("p-components-size", ctx.componentsSize)("p-title", ctx.literals.delete)("p-primary-action", ctx.confirm)("p-secondary-action", ctx.close)("p-click-out", true);
        ɵɵadvance(3);
        ɵɵtextInterpolate(ctx.literals.bodyDelete);
      }
    },
    dependencies: [NgClass, NgTemplateOutlet, NgStyle, NgPlural, NgPluralCase, CdkFixedSizeVirtualScroll, CdkVirtualForOf, CdkVirtualScrollViewport, CdkDropList, CdkDrag, CdkDragHandle, PoButtonComponent, PoContainerComponent, PoLoadingOverlayComponent, PoModalComponent, PoPopupComponent, PoTooltipDirective, PoIconComponent, PoCheckboxComponent, PoRadioComponent, PoSearchAiComponent, PoSearchComponent, PoTableColumnIconComponent, PoTableColumnLabelComponent, PoTableColumnLinkComponent, PoTableColumnManagerComponent, PoTableDetailComponent, PoTableSubtitleCircleComponent, PoTableSubtitleFooterComponent, PoTableColumnFrozenDirective, LowerCasePipe, TitleCasePipe, CurrencyPipe, DatePipe, PoTimePipe],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoTableComponent, [{
    type: Component,
    args: [{
      selector: "po-table",
      providers: [PoDateService, PoTableService],
      standalone: false,
      template: `<div class="po-table-actions">
  @if (hasValidColumns && itemsSelected.length > 0 && !hideBatchActions) {
    <div #columnBatchActions class="po-table-actions-batch-actions">
      <div [ngPlural]="itemsSelected.length" class="po-table-actions-batch-actions__label">
        <strong>
          <ng-template ngPluralCase="=0">{{ literals.noItem }}</ng-template>
          <ng-template ngPluralCase="=1">{{ literals.oneItem }}</ng-template>
          <ng-template ngPluralCase="other">{{ itemsSelected.length }} {{ literals.multipleItems }}</ng-template>
        </strong>
      </div>
      <div class="po-table-actions-batch-actions__buttons">
        <po-button
          p-icon="ICON_DELETE"
          [p-danger]="true"
          [p-disabled]="itemsSelected.length > 1 && serviceDeleteApi !== undefined"
          [p-label]="literals.delete"
          [p-size]="componentsSize"
          (p-click)="modalDelete.open()"
        ></po-button>
      </div>
    </div>
  }

  @if (hasValidColumns && !hideColumnsManager) {
    <div #columnManager class="po-table-actions-column-manager">
      <po-button
        #columnManagerTarget
        p-icon="ICON_SETTINGS"
        p-kind="tertiary"
        p-tooltip-position="left"
        [p-aria-label]="literals.columnsManager"
        [p-tooltip]="literals.columnsManager"
        [p-size]="componentsSize"
        (p-click)="onOpenColumnManager()"
      ></po-button>
    </div>
  }

  @if (searchAiField()) {
    <div class="po-table-search">
      <po-search-ai
        [p-url]="searchAiField().url"
        [p-clean]="true"
        [p-columns]="searchAiColumns"
        [p-min-confidence]="searchAiField().minConfidence"
        [p-timeout]="searchAiField().timeout"
        [p-placeholder]="searchAiPlaceholder"
        [p-literals]="searchAiField().literals"
        [p-size]="componentsSize"
        (p-result)="onAiResult($event)"
        (p-low-confidence)="onAiLowConfidence($event)"
        (p-error)="onAiError($event)"
        (p-clear)="onAiClear()"
      >
      </po-search-ai>
    </div>
  } @else if (!hideTableSearch && hasValidColumns) {
    <div class="po-table-search">
      <po-search
        [p-items]="items"
        [p-filter-keys]="filteredColumns"
        [p-filter-type]="filterType"
        [p-size]="componentsSize"
        (p-filtered-items-change)="onFilteredItemsChange($event)"
      >
      </po-search>
    </div>
  }
</div>
@if (container) {
  <po-container p-no-padding>
    <ng-container *ngTemplateOutlet="tableContainerTemplate"></ng-container>
  </po-container>
} @else {
  <div [class.po-table-container-sticky]="loading">
    @if (loading) {
      <po-loading-overlay [p-text]="literals.loadingData" [p-size]="sizeLoading"></po-loading-overlay>
    }
    <div class="po-table-main-container">
      <div
        #tableWrapper
        class="po-table-wrapper"
        [class.po-table-header-fixed-columns-pixels]="allColumnsWidthPixels"
        [style.opacity]="tableOpacity"
      >
        @if (height) {
          <div class="po-table-container" [style.height.px]="heightTableContainer">
            <div #poTableTbody [class.po-table-container-fixed-inner]="virtualScroll">
              @if (virtualScroll) {
                <div>
                  <ng-container *ngTemplateOutlet="tableVirtualScrollTemplate"></ng-container>
                </div>
              }
              @if (!virtualScroll) {
                <div class="po-table-container-overflow" [style.height.px]="heightTableContainer">
                  <ng-container *ngTemplateOutlet="tableTemplate"></ng-container>
                </div>
              }
            </div>
          </div>
        }
        @if (!height) {
          <div>
            <ng-container *ngTemplateOutlet="tableTemplate"></ng-container>
          </div>
        }
      </div>
    </div>
  </div>
  @if (hasFooter) {
    <div class="po-table-footer">
      @for (column of subtitleColumns; track trackBy($index)) {
        <po-table-subtitle-footer
          [p-components-size]="componentsSize"
          [p-literals]="literals"
          [p-subtitles]="column.subtitles"
        >
        </po-table-subtitle-footer>
      }
    </div>
  }
}

<ng-template #tableContainerTemplate>
  <div [class.po-table-container-sticky]="loading">
    @if (loading) {
      <po-loading-overlay [p-text]="literals.loadingData" [p-size]="sizeLoading"></po-loading-overlay>
    }
    <div class="po-table-main-container">
      <div
        #tableWrapper
        class="po-table-wrapper"
        [class.po-table-header-fixed-columns-pixels]="allColumnsWidthPixels"
        [style.opacity]="tableOpacity"
      >
        @if (height) {
          <div class="po-table-container" [style.height.px]="heightTableContainer">
            <div #poTableTbody [class.po-table-container-fixed-inner]="virtualScroll">
              @if (virtualScroll) {
                <div>
                  <ng-container *ngTemplateOutlet="tableVirtualScrollTemplate"></ng-container>
                </div>
              }
              @if (!virtualScroll) {
                <div class="po-table-container-overflow" [style.height.px]="heightTableContainer">
                  <ng-container *ngTemplateOutlet="tableTemplate"></ng-container>
                </div>
              }
            </div>
          </div>
        }

        @if (!height) {
          <div>
            <ng-container *ngTemplateOutlet="tableTemplate"></ng-container>
          </div>
        }
      </div>
    </div>
  </div>

  @if (hasFooter) {
    <div class="po-table-footer">
      @for (column of subtitleColumns; track trackBy($index)) {
        <po-table-subtitle-footer
          [p-components-size]="componentsSize"
          [p-literals]="literals"
          [p-subtitles]="column.subtitles"
        >
        </po-table-subtitle-footer>
      }
    </div>
  }
</ng-template>

<!-- Show More Button -->
@if (!infiniteScroll) {
  <div
    class="po-row po-table-footer-show-more"
    [class.po-invisible]="showMore.observers.length === 0 && !hasService"
    #tableFooter
  >
    <po-button
      class="po-offset-xl-4 po-offset-lg-4 po-offset-md-3 po-lg-4 po-md-6"
      [p-disabled]="showMoreDisabled"
      [p-label]="literals.loadMoreData"
      [p-loading]="loadingShowMore"
      [p-size]="componentsSize"
      (p-click)="onShowMore()"
    >
    </po-button>
  </div>
}

<!-- Table default-->
<ng-template #tableTemplate>
  <table
    #tableScrollable
    class="po-table"
    [ngClass]="{
      'po-table-interactive': selectable || sort,
      'po-table-selectable': selectable,
      'po-table-striped': striped,
      'po-table-data-fixed-columns': applyFixedColumns(),
      'po-table-text-wrap-enabled': textWrap
    }"
    [attr.p-spacing]="spacing"
  >
    <thead
      [class.po-table-header-sticky]="height > 0 && !virtualScroll"
      [attr.p-spacing]="spacing ?? (componentsSize === 'small' ? 'extraSmall' : 'medium')"
    >
      <tr
        [ngClass]="!height ? { 'no-hover': hideSelectAll, 'po-table-column-drag': this.isDraggable } : ''"
        [class.po-table-header]="!height"
        cdkDropList
        cdkDropListOrientation="horizontal"
        (cdkDropListDropped)="drop($event)"
      >
        @if (hasSelectableColumn) {
          <th [style.pointer-events]="hideSelectAll ? 'none' : 'auto'" class="po-table-column-selectable">
            <div [class.po-table-header-fixed-inner]="height">
              @if (!hideSelectAll) {
                <po-checkbox
                  name="selectAll"
                  [p-size]="componentsSize"
                  (p-change)="selectAllRows()"
                  [p-checkboxValue]="selectAll === null ? 'mixed' : selectAll"
                ></po-checkbox>
              }
            </div>
          </th>
        }

        @if ((hasMasterDetailColumn || hasRowTemplate) && hasMainColumns && !hasRowTemplateWithArrowDirectionRight) {
          <th class="po-table-header-column po-table-header-master-detail"></th>
        }

        <!-- Coluna criada para caso as ações fiquem no lado esquerdo -->
        @if (!actionRight && hasItems && hasMainColumns && (visibleActions.length > 1 || isSingleAction)) {
          <th
            #columnActionLeft
            [class.po-table-header-master-detail]="!isSingleAction"
            [class.po-table-header-single-action]="isSingleAction"
          ></th>
        }

        @if (!hasMainColumns) {
          <th #noColumnsHeader class="po-table-header-column po-text-center" [attr.colspan]="columnCount">
            @if (height) {
              <div class="po-table-header-fixed-inner" [style.width.px]="hasValidColumns ? headerWidth : null">
                {{ hasValidColumns ? literals.noVisibleColumn : literals.noColumns }}
              </div>
            } @else {
              {{ hasValidColumns ? literals.noVisibleColumn : literals.noColumns }}
            }
          </th>
        }

        @if (this.isDraggable || hasSomeFixed()) {
          @for (column of mainColumns; track trackBy(i); let i = $index) {
            <th
              class="po-table-header-ellipsis p-element po-frozen-column"
              [style.width]="column.width"
              [style.max-width]="column.width"
              [style.min-width]="column.width"
              [attr.data-po-table-column-name]="column.label || (column.property | titlecase) | lowercase"
              [class.po-clickable]="(sort && column.sortable !== false) || hasService"
              [ngClass]="{
                'po-table-header-sorted':
                  sort &&
                  JSON.stringify(sortedColumn?.property) === JSON.stringify(column) &&
                  (sortedColumn.ascending || !sortedColumn.ascending)
              }"
              [ngStyle]="{
                'width':
                  height > 0 && !virtualScroll ? (!hasItems ? '100%' : applyFixedColumns() ? column.width : 'auto') : ''
              }"
              [class.po-table-header-subtitle]="column.type === 'subtitle'"
              [class.po-table-column-drag-box]="this.isDraggable"
              (click)="sortColumn(column)"
              cdkDrag
              cdkDragLockAxis="x"
              [cdkDragDisabled]="column.fixed ? 'true' : 'false'"
              [pFrozenColumn]="column.fixed"
            >
              <div
                class="po-table-header-flex"
                [class.po-table-header-fixed-inner]="height"
                [class.po-table-header-flex-right]="column.type === 'currency' || column.type === 'number'"
                [class.po-table-header-flex-center]="column.type === 'subtitle'"
              >
                @if (this.isDraggable && !column.fixed) {
                  <po-icon cdkDragHandle p-icon="ICON_DRAG"></po-icon>
                }
                <ng-container *ngTemplateOutlet="contentHeaderTemplate; context: { $implicit: column }"> </ng-container>
              </div>
            </th>
          }
        } @else {
          @for (column of mainColumns; track trackBy(i); let i = $index) {
            <th
              class="po-table-header-ellipsis p-element po-frozen-column"
              [style.width]="column.width"
              [style.max-width]="column.width"
              [style.min-width]="column.width"
              [attr.data-po-table-column-name]="column.label || (column.property | titlecase) | lowercase"
              [class.po-clickable]="(sort && column.sortable !== false) || hasService"
              [ngClass]="{
                'po-table-header-sorted':
                  sort &&
                  JSON.stringify(sortedColumn?.property) === JSON.stringify(column) &&
                  (sortedColumn.ascending || !sortedColumn.ascending)
              }"
              [ngStyle]="{
                'width':
                  height > 0 && !virtualScroll ? (!hasItems ? '100%' : applyFixedColumns() ? column.width : 'auto') : ''
              }"
              [class.po-table-header-subtitle]="column.type === 'subtitle'"
              (click)="sortColumn(column)"
              [pFrozenColumn]="column.fixed"
            >
              <div
                class="po-table-header-flex"
                [class.po-table-header-fixed-inner]="height"
                [class.po-table-header-flex-right]="column.type === 'currency' || column.type === 'number'"
                [class.po-table-header-flex-center]="column.type === 'subtitle'"
              >
                <ng-container *ngTemplateOutlet="contentHeaderTemplate; context: { $implicit: column }"> </ng-container>
              </div>
            </th>
          }
        }

        @if (hasRowTemplateWithArrowDirectionRight && (hasVisibleActions || hideColumnsManager) && hasMainColumns) {
          <th class="po-table-header-column po-table-header-master-detail"></th>
        }

        @if (
          hasVisibleActions &&
          actionRight &&
          hasItems &&
          hasMainColumns &&
          (visibleActions.length > 1 || isSingleAction)
        ) {
          <th
            [class.po-table-header-single-action]="isSingleAction"
            [class.po-table-header-actions]="!isSingleAction"
          ></th>
        }
      </tr>
    </thead>

    @if (!hasItems || !hasMainColumns) {
      <tbody class="po-table-group-row">
        <tr class="po-table-row po-table-row-no-data">
          <td [colSpan]="columnCount" class="po-table-no-data po-text-center">
            <span> {{ literals.noData }} </span>
          </td>
        </tr>
      </tbody>
    }

    @if (hasMainColumns) {
      @for (row of filteredItems; track trackBy(rowIndex); let rowIndex = $index) {
        <tbody class="po-table-group-row">
          <tr
            class="po-table-row"
            [class.po-table-row-active]="row.$selected || (row.$selected === null && selectable)"
          >
            @if (selectable) {
              <td class="po-table-column-selectable">
                <ng-container
                  *ngTemplateOutlet="singleSelect ? inputRadio : inputCheckbox; context: { $implicit: row }"
                >
                </ng-container>
              </td>
            }
            <!-- Valida se a origem do detail é pelo input do po-table ou pela diretiva -->
            @if (
              (columnMasterDetail && !hideDetail && !hasRowTemplate) ||
              (hasRowTemplate && !hasRowTemplateWithArrowDirectionRight)
            ) {
              <td class="po-table-column-detail-toggle" (click)="toggleDetail(row)">
                <ng-template
                  [ngTemplateOutlet]="poTableColumnDetail"
                  [ngTemplateOutletContext]="{ row: row, rowIndex: rowIndex }"
                >
                </ng-template>
              </td>
            }
            <!-- Coluna com as ações na esquerda (padrão)-->
            @if (!actionRight && (visibleActions.length > 1 || isSingleAction)) {
              <ng-template
                [ngTemplateOutlet]="ActionsColumnTemplate"
                [ngTemplateOutletContext]="{ row: row, rowIndex: rowIndex }"
              >
              </ng-template>
            }
            @for (column of mainColumns; track trackBy(columnIndex); let columnIndex = $index) {
              <td
                [style.width]="column.width"
                [style.max-width]="column.width"
                [style.min-width]="column.width"
                [class.po-table-column]="column.type !== 'icon'"
                [class.po-table-column-right]="column.type === 'currency' || column.type === 'number'"
                [class.po-table-column-center]="column.type === 'subtitle'"
                [class.po-table-column-icons]="column.type === 'icon'"
                [pFrozenColumn]="column.fixed"
                class="p-element po-frozen-column"
                [ngClass]="getClassColor(row, column)"
                (click)="hasSelectableRow() ? selectRow(row) : 'javascript:;'"
              >
                <div
                  class="po-table-column-cell po-table-body-ellipsis notranslate"
                  [p-tooltip]="tooltipText"
                  [p-append-in-body]="true"
                  (mouseenter)="tooltipMouseEnter($event, column, row)"
                  (mouseleave)="tooltipMouseLeave()"
                >
                  @switch (column.type) {
                    @case ('columnTemplate') {
                      <span>
                        <ng-container
                          *ngTemplateOutlet="getTemplate(column); context: { $implicit: getCellData(row, column) }"
                        >
                        </ng-container>
                      </span>
                    }
                    @case ('cellTemplate') {
                      <span>
                        <ng-container
                          *ngTemplateOutlet="tableCellTemplate?.templateRef; context: { row: row, column: column }"
                        >
                        </ng-container>
                      </span>
                    }
                    @case ('boolean') {
                      <span>
                        {{ getBooleanLabel(getCellData(row, column), column) }}
                      </span>
                    }
                    @case ('currency') {
                      <span>
                        {{ getCellData(row, column) | currency: column.format : 'symbol' : '1.2-2' }}
                      </span>
                    }
                    @case ('date') {
                      <span>
                        {{ getCellData(row, column) | date: column.format || 'dd/MM/yyyy' }}
                      </span>
                    }
                    @case ('time') {
                      <span>
                        {{ getCellData(row, column) | po_time: column.format || 'HH:mm:ss.ffffff' }}
                      </span>
                    }
                    @case ('dateTime') {
                      <span>
                        {{ getCellData(row, column) | date: column.format || 'dd/MM/yyyy HH:mm:ss' }}
                      </span>
                    }
                    @case ('number') {
                      <span>
                        {{ formatNumber(getCellData(row, column), column.format) }}
                      </span>
                    }
                    @case ('link') {
                      <po-table-column-link
                        [p-action]="column.action"
                        [p-disabled]="checkDisabled(row, column)"
                        [p-link]="row[column.link]"
                        [p-row]="row"
                        [p-value]="getCellData(row, column)"
                        (click)="onClickLink($event, row, column)"
                      >
                      </po-table-column-link>
                    }
                    @case ('icon') {
                      <po-table-column-icon [p-column]="column" [p-icons]="getColumnIcons(row, column)" [p-row]="row">
                      </po-table-column-icon>
                    }
                    @case ('subtitle') {
                      <span>
                        <po-table-subtitle-circle
                          [p-subtitle]="getSubtitleColumn(row, column)"
                        ></po-table-subtitle-circle>
                      </span>
                    }
                    @case ('label') {
                      <span>
                        <po-table-column-label [p-value]="getColumnLabel(row, column)"> </po-table-column-label>
                      </span>
                    }
                    @default {
                      <span>{{
                        column.mask ? formatWithMask(getCellData(row, column), column.mask) : getCellData(row, column)
                      }}</span>
                    }
                  }
                </div>
              </td>
            }
            @if (hasRowTemplateWithArrowDirectionRight) {
              <td class="po-table-column-detail-toggle" (click)="toggleDetail(row)">
                <ng-template
                  [ngTemplateOutlet]="poTableColumnDetail"
                  [ngTemplateOutletContext]="{ row: row, rowIndex: rowIndex }"
                >
                </ng-template>
              </td>
            }
            <!-- Coluna de açoes na direita -->
            @if (actionRight) {
              <ng-template
                [ngTemplateOutlet]="ActionsColumnTemplate"
                [ngTemplateOutletContext]="{ row: row, rowIndex: rowIndex }"
              >
              </ng-template>
            }
          </tr>
          @if (hasMainColumns && hasRowTemplate && row.$showDetail && isShowRowTemplate(row, rowIndex)) {
            <tr>
              <td class="po-table-row-template-container" [colSpan]="columnCountForMasterDetail">
                <ng-template
                  [ngTemplateOutlet]="tableRowTemplate.templateRef"
                  [ngTemplateOutletContext]="{ $implicit: row, rowIndex: rowIndex }"
                >
                </ng-template>
              </td>
            </tr>
          }
          @if (hasMainColumns && isShowMasterDetail(row)) {
            <tr>
              <td class="po-table-column-detail" [colSpan]="columnCountForMasterDetail">
                <po-table-detail
                  [p-selectable]="selectable && !detailHideSelect"
                  [p-detail]="columnMasterDetail.detail"
                  [p-items]="row[nameColumnDetail]"
                  [p-parent-row]="row"
                  [p-components-size]="componentsSize"
                  (p-select-row)="selectDetailRow($event)"
                >
                </po-table-detail>
              </td>
            </tr>
          }
        </tbody>
      }
    }
  </table>
</ng-template>

<!-- Table with virtual scroll -->
<ng-template #tableVirtualScrollTemplate>
  <cdk-virtual-scroll-viewport
    #tableVirtualScroll
    [itemSize]="itemSize"
    [style.height.px]="heightTableContainer"
    [minBufferPx]="heightTableContainer < 100 ? 100 : heightTableContainer"
    [maxBufferPx]="heightTableContainer < 200 ? 200 : heightTableContainer"
  >
    <table
      class="po-table"
      [ngClass]="{
        'po-table-interactive': selectable || sort,
        'po-table-selectable': selectable,
        'po-table-striped': striped,
        'po-table-data-fixed-columns': applyFixedColumns(),
        'po-table-text-wrap-enabled': textWrap
      }"
      [attr.p-spacing]="spacing"
    >
      <thead
        class="po-table-header-sticky"
        [style.top]="inverseOfTranslation"
        [attr.p-spacing]="spacing ?? (componentsSize === 'small' ? 'extraSmall' : 'medium')"
      >
        <tr
          [class.po-table-header]="!height"
          cdkDropList
          cdkDropListOrientation="horizontal"
          (cdkDropListDropped)="drop($event)"
        >
          @if (hasSelectableColumn) {
            <th [style.pointer-events]="hideSelectAll ? 'none' : 'auto'" class="po-table-column-selectable">
              <div [class.po-table-header-fixed-inner]="height">
                @if (!hideSelectAll) {
                  <po-checkbox
                    name="selectAll"
                    (p-change)="selectAllRows()"
                    [p-checkboxValue]="selectAll === null ? 'mixed' : selectAll"
                    [p-size]="componentsSize"
                  ></po-checkbox>
                }
              </div>
            </th>
          }

          @if ((hasMasterDetailColumn || hasRowTemplate) && hasMainColumns && !hasRowTemplateWithArrowDirectionRight) {
            <th class="po-table-header-column po-table-header-master-detail"></th>
          }

          <!-- Coluna criada para caso as ações fiquem no lado esquerdo -->
          @if (!actionRight && hasItems && hasMainColumns && (visibleActions.length > 1 || isSingleAction)) {
            <th
              #columnActionLeft
              [class.po-table-header-master-detail]="!isSingleAction"
              [class.po-table-header-single-action]="isSingleAction"
            ></th>
          }

          @if (!hasMainColumns) {
            <th #noColumnsHeader class="po-table-header-column po-text-center" [attr.colspan]="columnCount">
              @if (height) {
                <div class="po-table-header-fixed-inner" [style.width.px]="hasValidColumns ? headerWidth : null">
                  {{ hasValidColumns ? literals.noVisibleColumn : literals.noColumns }}
                </div>
              } @else {
                {{ hasValidColumns ? literals.noVisibleColumn : literals.noColumns }}
              }
            </th>
          }

          @if (this.isDraggable || hasSomeFixed()) {
            @for (column of mainColumns; track trackBy(i); let i = $index) {
              <th
                class="po-table-header-ellipsis p-element po-frozen-column"
                [style.width]="column.width"
                [style.max-width]="column.width"
                [style.min-width]="column.width"
                [attr.data-po-table-column-name]="column.label || (column.property | titlecase) | lowercase"
                [class.po-clickable]="(sort && column.sortable !== false) || hasService"
                [ngClass]="{
                  'po-table-header-sorted':
                    sort &&
                    JSON.stringify(sortedColumn?.property) === JSON.stringify(column) &&
                    (sortedColumn.ascending || !sortedColumn.ascending)
                }"
                [ngStyle]="{ 'width': !hasItems ? '100%' : applyFixedColumns() ? column.width : 'auto' }"
                [class.po-table-header-subtitle]="column.type === 'subtitle'"
                [class.po-table-column-drag-box]="this.isDraggable"
                (click)="sortColumn(column)"
                cdkDrag
                cdkDragLockAxis="x"
                [cdkDragDisabled]="column.fixed ? 'true' : 'false'"
                [pFrozenColumn]="column.fixed"
              >
                <div
                  class="po-table-header-flex"
                  [class.po-table-header-fixed-inner]="height"
                  [class.po-table-header-flex-right]="column.type === 'currency' || column.type === 'number'"
                  [class.po-table-header-flex-center]="column.type === 'subtitle'"
                >
                  @if (this.isDraggable && !column.fixed) {
                    <po-icon cdkDragHandle p-icon="ICON_DRAG"></po-icon>
                  }
                  <ng-container *ngTemplateOutlet="contentHeaderTemplate; context: { $implicit: column }">
                  </ng-container>
                </div>
              </th>
            }
          } @else {
            @for (column of mainColumns; track trackBy(i); let i = $index) {
              <th
                class="po-table-header-ellipsis p-element po-frozen-column example-box"
                [style.width]="column.width"
                [style.max-width]="column.width"
                [style.min-width]="column.width"
                [attr.data-po-table-column-name]="column.label || (column.property | titlecase) | lowercase"
                [class.po-clickable]="(sort && column.sortable !== false) || hasService"
                [ngClass]="{
                  'po-table-header-sorted':
                    sort &&
                    JSON.stringify(sortedColumn?.property) === JSON.stringify(column) &&
                    (sortedColumn.ascending || !sortedColumn.ascending)
                }"
                [ngStyle]="{ 'width': !hasItems ? '100%' : applyFixedColumns() ? column.width : 'auto' }"
                [class.po-table-header-subtitle]="column.type === 'subtitle'"
                (click)="sortColumn(column)"
                [pFrozenColumn]="column.fixed"
              >
                <div
                  class="po-table-header-flex"
                  [class.po-table-header-fixed-inner]="height"
                  [class.po-table-header-flex-right]="column.type === 'currency' || column.type === 'number'"
                  [class.po-table-header-flex-center]="column.type === 'subtitle'"
                >
                  <ng-container *ngTemplateOutlet="contentHeaderTemplate; context: { $implicit: column }">
                  </ng-container>
                </div>
              </th>
            }
          }

          @if (hasRowTemplateWithArrowDirectionRight && hasMainColumns && (hasVisibleActions || hideColumnsManager)) {
            <th class="po-table-header-column po-table-header-master-detail"></th>
          }

          @if (
            hasVisibleActions &&
            actionRight &&
            hasItems &&
            hasMainColumns &&
            (visibleActions.length > 1 || isSingleAction)
          ) {
            <th
              [class.po-table-header-single-action]="isSingleAction"
              [class.po-table-header-actions]="!isSingleAction"
            ></th>
          }
        </tr>
      </thead>

      @if (!hasItems || !hasMainColumns) {
        <tbody class="po-table-group-row">
          <tr class="po-table-row po-table-row-no-data">
            <td [colSpan]="columnCount" class="po-table-no-data po-text-center">
              <span> {{ literals.noData }} </span>
            </td>
          </tr>
        </tbody>
      }

      @if (hasMainColumns) {
        <tbody
          class="po-table-group-row"
          *cdkVirtualFor="let row of filteredItems; let rowIndex = index; trackBy: trackBy"
        >
          <tr
            class="po-table-row"
            [class.po-table-row-active]="row.$selected || (row.$selected === null && selectable)"
          >
            @if (selectable) {
              <td class="po-table-column-selectable">
                <ng-container
                  *ngTemplateOutlet="singleSelect ? inputRadio : inputCheckbox; context: { $implicit: row }"
                >
                </ng-container>
              </td>
            }
            <!-- Valida se a origem do detail é pelo input do po-table pela diretiva -->
            @if (
              (columnMasterDetail && !hideDetail && !hasRowTemplate) ||
              (hasRowTemplate && !hasRowTemplateWithArrowDirectionRight)
            ) {
              <td class="po-table-column-detail-toggle" (click)="toggleDetail(row)">
                <ng-template
                  [ngTemplateOutlet]="poTableColumnDetail"
                  [ngTemplateOutletContext]="{ row: row, rowIndex: rowIndex }"
                >
                </ng-template>
              </td>
            }
            <!-- Coluna com as ações na esquerda (padrão)-->
            @if (!actionRight && (visibleActions.length > 1 || isSingleAction)) {
              <ng-template
                [ngTemplateOutlet]="ActionsColumnTemplate"
                [ngTemplateOutletContext]="{ row: row, rowIndex: rowIndex }"
              >
              </ng-template>
            }
            @for (column of mainColumns; track trackBy(columnIndex); let columnIndex = $index) {
              <td
                [style.width]="column.width"
                [style.max-width]="column.width"
                [style.min-width]="column.width"
                [class.po-table-column]="column.type !== 'icon'"
                [class.po-table-column-right]="column.type === 'currency' || column.type === 'number'"
                [class.po-table-column-center]="column.type === 'subtitle'"
                [class.po-table-column-icons]="column.type === 'icon'"
                [ngClass]="getClassColor(row, column)"
                [pFrozenColumn]="column.fixed"
                class="p-element po-frozen-column"
                (click)="hasSelectableRow() ? selectRow(row) : 'javascript:;'"
              >
                <div
                  class="po-table-column-cell po-table-body-ellipsis notranslate"
                  [p-tooltip]="tooltipText"
                  [p-append-in-body]="true"
                  (mouseenter)="tooltipMouseEnter($event, column, row)"
                  (mouseleave)="tooltipMouseLeave()"
                >
                  @switch (column.type) {
                    @case ('columnTemplate') {
                      <span>
                        <ng-container
                          *ngTemplateOutlet="getTemplate(column); context: { $implicit: getCellData(row, column) }"
                        >
                        </ng-container>
                      </span>
                    }
                    @case ('cellTemplate') {
                      <span>
                        <ng-container
                          *ngTemplateOutlet="tableCellTemplate?.templateRef; context: { row: row, column: column }"
                        >
                        </ng-container>
                      </span>
                    }
                    @case ('boolean') {
                      <span>
                        {{ getBooleanLabel(getCellData(row, column), column) }}
                      </span>
                    }
                    @case ('currency') {
                      <span>
                        {{ getCellData(row, column) | currency: column.format : 'symbol' : '1.2-2' }}
                      </span>
                    }
                    @case ('date') {
                      <span>
                        {{ getCellData(row, column) | date: column.format || 'dd/MM/yyyy' }}
                      </span>
                    }
                    @case ('time') {
                      <span>
                        {{ getCellData(row, column) | po_time: column.format || 'HH:mm:ss.ffffff' }}
                      </span>
                    }
                    @case ('dateTime') {
                      <span>
                        {{ getCellData(row, column) | date: column.format || 'dd/MM/yyyy HH:mm:ss' }}
                      </span>
                    }
                    @case ('number') {
                      <span>
                        {{ formatNumber(getCellData(row, column), column.format) }}
                      </span>
                    }
                    @case ('link') {
                      <po-table-column-link
                        [p-action]="column.action"
                        [p-disabled]="checkDisabled(row, column)"
                        [p-link]="row[column.link]"
                        [p-row]="row"
                        [p-value]="getCellData(row, column)"
                        (click)="onClickLink($event, row, column)"
                      >
                      </po-table-column-link>
                    }
                    @case ('icon') {
                      <po-table-column-icon [p-column]="column" [p-icons]="getColumnIcons(row, column)" [p-row]="row">
                      </po-table-column-icon>
                    }
                    @case ('subtitle') {
                      <span>
                        <po-table-subtitle-circle
                          [p-subtitle]="getSubtitleColumn(row, column)"
                        ></po-table-subtitle-circle>
                      </span>
                    }
                    @case ('label') {
                      <span>
                        <po-table-column-label [p-value]="getColumnLabel(row, column)"> </po-table-column-label>
                      </span>
                    }
                    @default {
                      <span>{{
                        column.mask ? formatWithMask(getCellData(row, column), column.mask) : getCellData(row, column)
                      }}</span>
                    }
                  }
                </div>
              </td>
            }
            @if (hasRowTemplateWithArrowDirectionRight) {
              <td class="po-table-column-detail-toggle" (click)="toggleDetail(row)">
                <ng-template
                  [ngTemplateOutlet]="poTableColumnDetail"
                  [ngTemplateOutletContext]="{ row: row, rowIndex: rowIndex }"
                >
                </ng-template>
              </td>
            }
            <!-- Coluna de açoes na direita -->
            @if (actionRight) {
              <ng-template
                [ngTemplateOutlet]="ActionsColumnTemplate"
                [ngTemplateOutletContext]="{ row: row, rowIndex: rowIndex }"
              >
              </ng-template>
            }
          </tr>
          @if (hasMainColumns && hasRowTemplate && row.$showDetail && isShowRowTemplate(row, rowIndex)) {
            <tr>
              <td class="po-table-row-template-container" [colSpan]="columnCountForMasterDetail">
                <ng-template
                  [ngTemplateOutlet]="tableRowTemplate.templateRef"
                  [ngTemplateOutletContext]="{ $implicit: row, rowIndex: rowIndex }"
                >
                </ng-template>
              </td>
            </tr>
          }
          @if (hasMainColumns && isShowMasterDetail(row)) {
            <tr>
              <td class="po-table-column-detail" [colSpan]="columnCountForMasterDetail">
                <po-table-detail
                  [p-selectable]="selectable && !detailHideSelect"
                  [p-detail]="columnMasterDetail.detail"
                  [p-items]="row[nameColumnDetail]"
                  [p-parent-row]="row"
                  [p-components-size]="componentsSize"
                  (p-select-row)="selectDetailRow($event)"
                >
                </po-table-detail>
              </td>
            </tr>
          }
        </tbody>
      }
    </table>
  </cdk-virtual-scroll-viewport>
</ng-template>

<po-popup #popup [p-actions]="actions" [p-size]="componentsSize" [p-target]="popupTarget"> </po-popup>

<ng-template #poTableColumnDetail let-row="row" let-rowIndex="rowIndex">
  @if ((containsMasterDetail(row) && !hasRowTemplate) || (isShowRowTemplate(row, rowIndex) && hasRowTemplate)) {
    <po-icon [p-icon]="row.$showDetail ? 'ICON_ARROW_UP' : 'ICON_ARROW_DOWN'" class="po-clickable"> </po-icon>
  }
</ng-template>

<ng-template #inputRadio let-row>
  <po-radio
    [name]="idRadio"
    [p-checked]="row.$selected"
    [p-size]="componentsSize"
    (p-change-selected)="selectRow(row)"
  ></po-radio>
</ng-template>

<ng-template #inputCheckbox let-row>
  <po-checkbox
    name="checkbox"
    (p-change)="selectable ? selectRow(row) : 'javascript:;'"
    [p-checkboxValue]="row.$selected === null ? 'mixed' : row.$selected"
    [p-size]="componentsSize"
  ></po-checkbox>
</ng-template>

<ng-template #contentHeaderTemplate let-column>
  <span
    #columnHeader
    class="po-table-header-ellipsis"
    [p-tooltip]="tooltipText"
    [p-append-in-body]="true"
    (mouseenter)="tooltipMouseEnter($event)"
    (mouseleave)="tooltipMouseLeave()"
  >
    {{ column.label || (column.property | titlecase) }}
  </span>

  @if (sort && column.sortable !== false) {
    <span
      [class.po-table-header-icon-unselected]="JSON.stringify(sortedColumn?.property) !== JSON.stringify(column)"
      [class.po-table-header-icon-descending]="
        JSON.stringify(sortedColumn?.property) === JSON.stringify(column) && sortedColumn.ascending
      "
      [class.po-table-header-icon-ascending]="
        JSON.stringify(sortedColumn?.property) === JSON.stringify(column) && !sortedColumn.ascending
      "
    >
      @if (JSON.stringify(sortedColumn?.property) !== JSON.stringify(column)) {
        <po-icon p-icon="ICON_SORT"></po-icon>
      }
      @if (JSON.stringify(sortedColumn?.property) === JSON.stringify(column) && sortedColumn.ascending) {
        <po-icon p-icon="ICON_SORT_ASC"></po-icon>
      }
      @if (JSON.stringify(sortedColumn?.property) === JSON.stringify(column) && !sortedColumn.ascending) {
        <po-icon cdkDragHandle p-icon="ICON_SORT_DESC"></po-icon>
      }
    </span>
  }
</ng-template>

<!-- Template de ações -->
<ng-template #ActionsColumnTemplate let-row="row" let-rowIndex="rowIndex">
  @if (isSingleAction) {
    <td
      class="po-table-column po-table-column-single-action"
      [style.width.px]="height && actionRight ? getWidthColumnManager() : ''"
      [style.max-width.px]="height && !actionRight ? getColumnWidthActionsLeft() : ''"
      [style.width.px]="height && !actionRight ? getColumnWidthActionsLeft() : ''"
    >
      @if (firstAction.visible !== false) {
        <div
          class="po-table-single-action po-clickable"
          [class.po-table-action-disabled]="firstAction.disabled ? validateTableAction(row, firstAction) : false"
          (click)="executeTableAction(row, firstAction)"
        >
          @if (firstAction.icon) {
            <po-icon class="po-table-single-action-content" [p-icon]="firstAction.icon"></po-icon>
          }
          @if (firstAction.label) {
            <span>{{ firstAction.label }}</span>
          }
        </div>
      }
    </td>
  }

  @if (visibleActions.length > 1) {
    <td class="po-table-column-actions">
      <div #popupTarget class="po-clickable" (click)="togglePopup(row, popupTarget)">
        <po-icon class="po-field-icon" p-icon="ICON_MORE"></po-icon>
      </div>
    </td>
  }
</ng-template>

@if (!hideColumnsManager) {
  <po-table-column-manager
    [p-columns]="columns"
    [p-max-columns]="maxColumns"
    [p-target]="columnManagerTarget"
    [p-last-visible-columns-selected]="lastVisibleColumnsSelected"
    [p-hide-action-fixed-columns]="hideActionFixedColumns"
    (p-visible-columns-change)="onVisibleColumnsChange($event)"
    (p-change-visible-columns)="onChangeVisibleColumns($event)"
    (p-change-fixed-columns)="onChangeFixedColumns($event)"
    [p-columns-default]="initialColumns"
    [p-components-size]="componentsSize"
    (p-initial-columns)="onColumnRestoreManager($event)"
  >
  </po-table-column-manager>
}

<po-modal
  #modalDelete
  [p-components-size]="componentsSize"
  [p-title]="literals.delete"
  [p-primary-action]="confirm"
  [p-secondary-action]="close"
  [p-click-out]="true"
>
  <p class="po-font-text-large">{{ literals.bodyDelete }}</p>
</po-modal>
`
    }]
  }], () => [{
    type: PoDateService
  }, {
    type: IterableDiffers
  }, {
    type: Renderer2
  }, {
    type: PoLanguageService
  }, {
    type: ChangeDetectorRef
  }, {
    type: DecimalPipe
  }, {
    type: PoTableService
  }], {
    tableRowTemplate: [{
      type: ContentChild,
      args: [PoTableRowTemplateDirective, {
        static: true
      }]
    }],
    tableCellTemplate: [{
      type: ContentChild,
      args: [PoTableCellTemplateDirective]
    }],
    tableColumnTemplates: [{
      type: ContentChildren,
      args: [PoTableColumnTemplateDirective]
    }],
    noColumnsHeader: [{
      type: ViewChild,
      args: ["noColumnsHeader", {
        read: ElementRef
      }]
    }],
    poPopupComponent: [{
      type: ViewChild,
      args: ["popup"]
    }],
    modalDelete: [{
      type: ViewChild,
      args: [PoModalComponent, {
        static: true
      }]
    }],
    tableFooterElement: [{
      type: ViewChild,
      args: ["tableFooter", {
        read: ElementRef,
        static: false
      }]
    }],
    tableWrapperElement: [{
      type: ViewChild,
      args: ["tableWrapper", {
        read: ElementRef,
        static: false
      }]
    }],
    tableTemplate: [{
      type: ViewChild,
      args: ["tableTemplate", {
        read: ElementRef,
        static: false
      }]
    }],
    tableVirtualScroll: [{
      type: ViewChild,
      args: ["tableVirtualScroll", {
        read: ElementRef,
        static: false
      }]
    }],
    tableScrollable: [{
      type: ViewChild,
      args: ["tableScrollable", {
        read: ElementRef,
        static: false
      }]
    }],
    columnManager: [{
      type: ViewChild,
      args: ["columnManager", {
        read: ElementRef,
        static: false
      }]
    }],
    columnBatchActions: [{
      type: ViewChild,
      args: ["columnBatchActions", {
        read: ElementRef,
        static: false
      }]
    }],
    columnActionLeft: [{
      type: ViewChild,
      args: ["columnActionLeft", {
        read: ElementRef,
        static: false
      }]
    }],
    actionsIconElement: [{
      type: ViewChildren,
      args: ["actionsIconElement", {
        read: ElementRef
      }]
    }],
    actionsElement: [{
      type: ViewChildren,
      args: ["actionsElement", {
        read: ElementRef
      }]
    }],
    filterInput: [{
      type: ViewChild,
      args: ["filterInput"]
    }],
    poSearchInput: [{
      type: ViewChild,
      args: ["poSearchInput", {
        read: ElementRef,
        static: true
      }]
    }],
    searchAiComponent: [{
      type: ViewChild,
      args: [PoSearchAiComponent]
    }],
    viewPort: [{
      type: ViewChild,
      args: [CdkVirtualScrollViewport, {
        static: false
      }]
    }],
    columnManagerTarget: [{
      type: ViewChild,
      args: ["columnManagerTarget"]
    }],
    columnManagerTargetFixed: [{
      type: ViewChild,
      args: ["columnManagerTargetFixed"]
    }]
  });
})();
var PoSwitchModule = class _PoSwitchModule {
  static ɵfac = function PoSwitchModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoSwitchModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoSwitchModule,
    declarations: [PoSwitchComponent],
    imports: [CommonModule, FormsModule, PoFieldContainerModule, PoLabelModule, PoIconModule, PoHelperModule, PoLoadingModule],
    exports: [PoSwitchComponent]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [CommonModule, FormsModule, PoFieldContainerModule, PoLabelModule, PoIconModule, PoHelperModule, PoLoadingModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoSwitchModule, [{
    type: NgModule,
    args: [{
      declarations: [PoSwitchComponent],
      exports: [PoSwitchComponent],
      imports: [CommonModule, FormsModule, PoFieldContainerModule, PoLabelModule, PoIconModule, PoHelperModule, PoLoadingModule]
    }]
  }], null, null);
})();
var PoSearchAiModule = class _PoSearchAiModule {
  static ɵfac = function PoSearchAiModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoSearchAiModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoSearchAiModule,
    declarations: [PoSearchAiComponent],
    imports: [CommonModule, FormsModule, PoButtonModule, PoCleanModule, PoFieldContainerModule, PoHelperModule, PoIconModule, PoLoadingModule],
    exports: [PoSearchAiComponent]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [CommonModule, FormsModule, PoButtonModule, PoCleanModule, PoFieldContainerModule, PoHelperModule, PoIconModule, PoLoadingModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoSearchAiModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, FormsModule, PoButtonModule, PoCleanModule, PoFieldContainerModule, PoHelperModule, PoIconModule, PoLoadingModule],
      declarations: [PoSearchAiComponent],
      exports: [PoSearchAiComponent]
    }]
  }], null, null);
})();
var poDatepickerRangeLiteralsDefault = {
  en: {
    invalidFormat: "Date in invalid format",
    startDateGreaterThanEndDate: "Start date greater than end date",
    invalidDate: "Invalid date",
    dateOutOfPeriod: "Date out of period",
    open: "Open calendar",
    startDate: "Start date",
    endDate: "End date",
    clean: "Clear field"
  },
  es: {
    invalidFormat: "Fecha en formato no válido",
    startDateGreaterThanEndDate: "Fecha de inicio mayor que fecha final",
    invalidDate: "Fecha invalida",
    dateOutOfPeriod: "Fecha fuera de período",
    open: "Calendario abierto",
    startDate: "Fecha de inicio",
    endDate: "Fecha final",
    clean: "Limpiar campo"
  },
  pt: {
    invalidFormat: "Data no formato inválido",
    startDateGreaterThanEndDate: "Data inicial maior que data final",
    invalidDate: "Data inválida",
    dateOutOfPeriod: "Data fora do período",
    open: "Abrir calendário",
    startDate: "Data inicial",
    endDate: "Data final",
    clean: "Limpar campo"
  },
  ru: {
    invalidFormat: "Дата в неверном формате",
    startDateGreaterThanEndDate: "Дата начала больше даты окончания",
    invalidDate: "Недействительная дата",
    dateOutOfPeriod: "дата вне периода",
    open: "открытый календарь",
    startDate: "Дата начала",
    endDate: "Дата окончания",
    clean: "Очистить поле"
  }
};
var PoDatepickerRangeBaseComponent = class _PoDatepickerRangeBaseComponent {
  changeDetector;
  poDateService;
  languageService;
  // Propriedade interna que define se o ícone de ajuda adicional terá cursor clicável (evento) ou padrão (tooltip).
  additionalHelpEventTrigger;
  /* Nome do componente. */
  name;
  /**
   *
   * @deprecated v23.x.x use `p-helper`
   *
   * @optional
   *
   * @description
   * Exibe um ícone de ajuda adicional, com o texto desta propriedade sendo passado para o popover do componente `po-helper`.
   * **Como boa prática, indica-se utilizar um texto com até 140 caracteres.**
   * > Requer um recuo mínimo de 8px se o componente estiver próximo à lateral da tela.
   *
   * > Essa propriedade está **depreciada** e será removida na versão `23.x.x`. Recomendamos utilizar a propriedade `p-helper` que oferece mais recursos e flexibilidade.
   */
  additionalHelpTooltip;
  /**
   * @optional
   *
   * @description
   *
   * Define que o `calendar` e/ou tooltip (`p-additional-help-tooltip` e/ou `p-error-limit`) serão incluídos no body da
   * página e não dentro do componente. Essa opção pode ser necessária em cenários com containers que possuem scroll ou
   * overflow escondido, garantindo o posicionamento correto de ambos próximo ao elemento.
   *
   * > Quando utilizado com `p-helper`, leitores de tela como o NVDA podem não ler o conteúdo do popover.
   *
   * @default `false`
   */
  appendBox = false;
  /**
   * @optional
   *
   * @description
   *
   * Aplica foco no elemento ao ser iniciado.
   *
   * > Caso mais de um elemento seja configurado com essa propriedade, apenas o último elemento declarado com ela terá o foco.
   *
   * @default `false`
   */
  autoFocus = false;
  /**
   * @optional
   *
   * @description
   * Define se o título do campo será exibido de forma compacta.
   *
   * Quando habilitado (`true`), o modo compacto afeta o conjunto composto por:
   * - `po-label`
   * - `p-requirement (showRequired)`
   * - `po-helper`
   *
   * Ou seja, todos os elementos relacionados ao título do campo
   * (rótulo, indicador de obrigatoriedade e componente auxiliar) passam
   * a seguir o comportamento de layout compacto.
   *
   * Também é possível definir esse comportamento de forma global,
   * uma única vez, na folha de estilo geral da aplicação, por meio
   * da customização dos tokens CSS:
   *
   * - `--field-container-title-justify`
   * - `--field-container-title-flex`
   *
   * Exemplo:
   *
   * ```
   * :root {
   *   --field-container-title-justify: flex-start;
   *   --field-container-title-flex: 0 1 auto;
   * }
   * ```
   *
   * Dessa forma, o layout compacto passa a ser o padrão da aplicação,
   * sem a necessidade de definir a propriedade individualmente em cada campo.
   *
   * @default `false`
   */
  compactLabel = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "compactLabel"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-compact-label",
    transform: convertToBoolean
  }));
  /**
   * @optional
   *
   * @description
   *
   * Habilita a exibição dos presets padrão de intervalos de data no painel lateral do calendário.
   *
   * Aceita os seguintes valores:
   * - `true`: exibe todos os presets padrão (Amanhã, Hoje, Ontem, Últimos 7 dias, Últimos 14 dias, Últimos 30 dias, Últimos 3 meses, Últimos 6 meses).
   * - `false`: não exibe os presets padrão.
   * - `Array<string>`: exibe apenas os presets padrão cujos labels estejam no array informado.
   * > Label dos presets: tomorrow, today, yesterday, 7days, 14days, 30days, 3months, 6months.
   *
   * O preset "today" é obrigatório e será incluído automaticamente mesmo que não esteja no array.
   *
   * Caso `p-range-preset-options` também seja informado, os presets customizados serão exibidos junto aos presets padrão,
   * ordenados automaticamente por temporalidade com base na data início (Futuro → Presente → Passado).
   *
   * @default `false`
   */
  set rangePresets(value) {
    if (Array.isArray(value)) {
      this._rangePresets = value;
    } else if (value === "" || value === true || value === "true") {
      this._rangePresets = true;
    } else {
      this._rangePresets = false;
    }
  }
  get rangePresets() {
    return this._rangePresets;
  }
  _rangePresets = false;
  /**
   * @optional
   *
   * @description
   *
   * Lista de presets customizados de intervalos de data exibidos no painel lateral do calendário.
   *
   * Quando informado sem `p-range-presets`, exibe apenas os presets customizados.
   * Quando informado junto com `p-range-presets` habilitado, os presets customizados serão exibidos junto aos presets padrão,
   * ordenados automaticamente por temporalidade com base na data início (Futuro → Presente → Passado).
   *
   * Para utilizar presets customizados, informe um array de objetos que implementam a interface `PoCalendarRangePreset`.
   */
  rangePresetOptions;
  /**
   * @optional
   *
   * @description
   *
   * Define a ordenação dos presets na lista.
   *
   * Valores aceitos:
   * - `'asc'` (padrão): presets mais próximos de hoje aparecem primeiro.
   * - `'desc'`: presets mais distantes de hoje aparecem primeiro.
   *
   * A ordenação é aplicada dentro de cada grupo de temporalidade com base na data início (Futuro → Presente → Passado).
   *
   * @default `asc`
   */
  rangePresetsOrder = "asc";
  /**
   * @optional
   *
   * @description
   *
   * Texto de apoio do campo.
   */
  help;
  /**
   * @optional
   *
   * @description
   *
   * Rótulo do campo.
   */
  label;
  /**
   * @optional
   *
   * @description
   *
   * Define se a indicação de campo opcional será exibida.
   *
   * > Não será exibida a indicação se:
   * - O campo conter `p-required`;
   * - Não possuir `p-help` e/ou `p-label`.
   *
   * @default `false`
   */
  optional;
  /**
   * @optional
   *
   * @description
   *
   * Mensagem que aparecerá enquanto o campo não estiver preenchido.
   */
  set placeholder(value) {
    if (typeof value === "string") {
      this._placeholder = {
        start: value,
        end: value
      };
    } else if (value && typeof value === "object") {
      this._placeholder = {
        start: value.start || "",
        end: value.end || ""
      };
    } else {
      this._placeholder = {
        start: "",
        end: ""
      };
    }
  }
  get placeholder() {
    return this._placeholder;
  }
  /**
   * @optional
   *
   * @description
   *
   * Exibe a mensagem setada se o campo estiver vazio e for requerido.
   *
   * > Necessário que a propriedade `p-required` esteja habilitada.
   *
   */
  fieldErrorMessage;
  /**
   *
   * @deprecated v23.x.x use `p-helper`
   *
   * @optional
   *
   * @description
   * Evento disparado ao clicar no ícone de ajuda adicional.
   *
   * > Essa propriedade está **depreciada** e será removida na versão `23.x.x`. Recomendamos utilizar a propriedade `p-helper` que oferece mais recursos e flexibilidade.
   */
  additionalHelp = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Limita a exibição da mensagem de erro a duas linhas e exibe um tooltip com o texto completo.
   *
   * > Caso essa propriedade seja definida como `true`, a mensagem de erro será limitada a duas linhas
   * e um tooltip será exibido ao passar o mouse sobre a mensagem para mostrar o conteúdo completo.
   *
   * @default `false`
   */
  errorLimit = false;
  /**
   * @optional
   *
   * @description
   *
   * Define as opções do componente de ajuda (po-helper) que será exibido ao lado do label quando a propriedade `p-label` for definida, ou, ao lado do componente na ausência da propriedade `p-label`.
   * > Para mais informações acesse: https://po-ui.io/documentation/po-helper.
   *
   * > Ao configurar esta propriedade, o antigo ícone de ajuda adicional (`p-additional-help-tooltip` e `p-additional-help`) será ignorado.
   */
  poHelperComponent = input(void 0, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "poHelperComponent"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-helper"
  }));
  /**
   * @optional
   *
   * @description
   *
   * Habilita a quebra automática do texto da propriedade `p-label`. Quando `p-label-text-wrap` for verdadeiro, o texto que excede
   * o espaço disponível é transferido para a próxima linha em pontos apropriados para uma
   * leitura clara.
   *
   * @default `false`
   */
  labelTextWrap = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "labelTextWrap"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-label-text-wrap"
  }));
  /**
   * @optional
   *
   * @description
   *
   * Evento disparado ao alterar valor do campo.
   */
  onChange = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Evento disparado sempre que o valor do model é alterado, seja por interação do usuário
   * ou por atualização programática (ex: `setValue`, `patchValue`, carregamento assíncrono).
   *
   * Diferentemente do `p-change`, que é disparado apenas por interação do usuário,
   * o `p-change-model` cobre todos os cenários de alteração de valor.
   *
   * Não emite quando o novo valor é idêntico ao anterior (deduplicação automática).
   */
  changeModel = new EventEmitter();
  /**
   * @optional
   *
   * @description
   * Evento disparado quando uma tecla é pressionada enquanto o foco está no componente.
   * Retorna um objeto `KeyboardEvent` com informações sobre a tecla.
   */
  keydown = new EventEmitter();
  errorMessage = "";
  dateRange = {
    start: "",
    end: ""
  };
  displayAdditionalHelp = false;
  modelLastUpdate;
  format = "dd/mm/yyyy";
  isDateRangeInputFormatValid = true;
  isStartDateRangeInputValid = true;
  onTouchedModel;
  poMaskObject;
  hasValidatorRequired = false;
  _clean = false;
  _disabled;
  _endDate;
  _literals;
  _maxDate;
  _minDate;
  _noAutocomplete = false;
  _readonly = false;
  _required = false;
  _loading = false;
  _startDate;
  _locale;
  _size = void 0;
  _initialSize = void 0;
  _placeholder = {
    start: "",
    end: ""
  };
  language;
  onChangeModel;
  validatorChange;
  subscription = new Subscription();
  get isDateRangeInputValid() {
    return this.isDateRangeInputFormatValid && this.isStartDateRangeInputValid;
  }
  /**
   * @optional
   *
   * @description
   *
   * Habilita ação para limpar o campo.
   *
   * @default `false`
   */
  set clean(clean) {
    this._clean = clean;
  }
  get clean() {
    return this._clean;
  }
  /**
   * @optional
   *
   * @description
   *
   * Desabilita o campo.
   *
   * @default `false`
   */
  set disabled(value) {
    this._disabled = value;
    this.validateModel(this.dateRange);
  }
  get disabled() {
    return this._disabled;
  }
  /**
   * @optional
   *
   * @description
   *
   * Data final.
   */
  set endDate(date) {
    this._endDate = this.convertPatternDateFormat(date);
    this.dateRange.end = this.endDate;
    this.updateScreenByModel(this.dateRange);
    this.updateModel(this.dateRange);
  }
  get endDate() {
    return this._endDate;
  }
  /**
   * @optional
   *
   * @description
   *
   * Objeto com as literais usadas no `po-datepicker-range`.
   *
   * Existem duas maneiras de customizar o componente, passando um objeto com todas as literais disponíveis:
   *
   * ```
   *  const customLiterals: PoDatepickerRangeLiterals = {
   *    invalidFormat: 'Date in inconsistent format',
   *    startDateGreaterThanEndDate: 'End date less than start date'
   *  };
   * ```
   *
   * Ou passando apenas as literais que deseja customizar:
   *
   * ```
   *  const customLiterals: PoDatepickerRangeLiterals = {
   *    invalidFormat: 'Date in inconsistent format'
   *  };
   * ```
   *
   * E para carregar as literais customizadas, basta apenas passar o objeto para o componente.
   *
   * ```
   * <po-datepicker-range
   *   [p-literals]="customLiterals">
   * </po-datepicker-range>
   * ```
   *
   * > O objeto padrão de literais será traduzido de acordo com o idioma do
   * [`PoI18nService`](/documentation/po-i18n) ou do browser.
   */
  set literals(value) {
    if (value instanceof Object && !(value instanceof Array)) {
      this._literals = __spreadValues(__spreadValues(__spreadValues({}, poDatepickerRangeLiteralsDefault[poLocaleDefault]), poDatepickerRangeLiteralsDefault[this.language]), value);
    } else {
      this._literals = poDatepickerRangeLiteralsDefault[this.language];
    }
  }
  get literals() {
    return this._literals || poDatepickerRangeLiteralsDefault[this.language];
  }
  /**
   * @optional
   *
   * @description
   * Exibe um ícone de carregamento no lado direito do campo para sinalizar que uma operação está em andamento.
   *
   * @default `false`
   */
  set loading(value) {
    this._loading = value;
    this.changeDetector?.markForCheck();
  }
  get loading() {
    return this._loading;
  }
  get isDisabled() {
    return this.disabled || this.loading;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define uma data mínima para o `po-datepicker-range`.
   */
  set minDate(value) {
    if (value instanceof Date) {
      const year = value.getFullYear();
      const date = new Date(year, value.getMonth(), value.getDate(), 0, 0, 0);
      setYearFrom0To100(date, year);
      this._minDate = date;
    } else {
      this._minDate = convertIsoToDate(value, true, false);
    }
  }
  get minDate() {
    return this._minDate;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define uma data máxima para o `po-datepicker-range`.
   */
  set maxDate(value) {
    if (value instanceof Date) {
      const year = value.getFullYear();
      const date = new Date(year, value.getMonth(), value.getDate(), 23, 59, 59);
      setYearFrom0To100(date, year);
      this._maxDate = date;
    } else {
      this._maxDate = convertIsoToDate(value, false, true);
    }
  }
  get maxDate() {
    return this._maxDate;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define a propriedade nativa `autocomplete` do campo como `off`.
   *
   * @default `false`
   */
  set noAutocomplete(value) {
    this._noAutocomplete = value;
  }
  get noAutocomplete() {
    return this._noAutocomplete;
  }
  /**
   * @optional
   *
   * @description
   *
   * Indica que o campo será somente leitura.
   *
   * @default `false`
   */
  set readonly(value) {
    this._readonly = value;
    this.validateModel(this.dateRange);
  }
  get readonly() {
    return this._readonly;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define que o campo será obrigatório.
   *
   * @default `false`
   */
  set required(required) {
    this._required = required;
    this.validateModel(this.dateRange);
  }
  get required() {
    return this._required;
  }
  /**
   * Define se a indicação de campo obrigatório será exibida.
   *
   * > Não será exibida a indicação se:
   * - Não possuir `p-help` e/ou `p-label`.
   */
  showRequired = false;
  /**
   * @optional
   *
   * @description
   *
   * Define o tamanho do componente:
   * - `small`: altura do input como 32px (disponível apenas para acessibilidade AA).
   * - `medium`: altura do input como 44px.
   *
   * > Caso a acessibilidade AA não esteja configurada, o tamanho `medium` será mantido.
   * Para mais detalhes, consulte a documentação do [po-theme](https://po-ui.io/documentation/po-theme).
   *
   * @default `medium`
   */
  set size(value) {
    this._initialSize = value;
    this.applySizeBasedOnA11y();
  }
  get size() {
    return this._size ?? getDefaultSizeFn(PoFieldSize);
  }
  /**
   * @optional
   *
   * @description
   *
   * Data inicial.
   */
  set startDate(date) {
    this._startDate = this.convertPatternDateFormat(date);
    this.dateRange.start = this.startDate;
    this.updateScreenByModel(this.dateRange);
    this.updateModel(this.dateRange);
  }
  get startDate() {
    return this._startDate;
  }
  /**
   * @optional
   *
   * @description
   *
   * Idioma que o calendário utilizará para exibir as datas.
   *
   * > O locale padrão será recuperado com base no [`PoI18nService`](/documentation/po-i18n) ou *browser*.
   */
  set locale(value) {
    if (value) {
      this._locale = value.length >= 2 ? value : poLocaleDefault;
      this.poMaskObject = this.buildMask(replaceFormatSeparator(this.format, this.languageService.getDateSeparator(this.locale)));
    } else {
      this._locale = this.language;
      this.poMaskObject = this.buildMask(replaceFormatSeparator(this.format, this.languageService.getDateSeparator(this.locale)));
    }
  }
  get locale() {
    return this._locale || this.language;
  }
  constructor(changeDetector, poDateService, languageService) {
    this.changeDetector = changeDetector;
    this.poDateService = poDateService;
    this.languageService = languageService;
    this.language = languageService.getShortLanguage();
  }
  ngOnDestroy() {
    this.subscription?.unsubscribe();
  }
  onThemeChange() {
    this.applySizeBasedOnA11y();
  }
  // Função implementada do ControlValueAccessor
  // Usada para interceptar os estados de habilitado via forms api
  setDisabledState(isDisabled) {
    this.disabled = isDisabled;
    this.changeDetector.markForCheck();
  }
  //Transforma o tamanho do input para o tamanho do ícone de loading correspondente
  mapSizeToIcon(size) {
    return mapInputSizeToLoadingIcon(size);
  }
  // Função implementada do ControlValueAccessor
  // Usada para interceptar as mudanças e não atualizar automaticamente o Model
  registerOnChange(func) {
    this.onChangeModel = func;
  }
  // Função implementada do ControlValueAccessor
  // Usada para interceptar as mudanças e não atualizar automaticamente o Model
  registerOnTouched(func) {
    this.onTouchedModel = func;
  }
  registerOnValidatorChange(fn) {
    this.validatorChange = fn;
  }
  validate(control) {
    const value = control.value || {};
    const startDate = value.start ? this.convertPatternDateFormat(value.start) : "";
    const endDate = value.end ? this.convertPatternDateFormat(value.end) : "";
    if (!this.hasValidatorRequired && this.fieldErrorMessage && control.hasValidator(Validators.required)) {
      this.hasValidatorRequired = true;
    }
    if (this.requiredDateRangeFailed(startDate, endDate)) {
      this.errorMessage = "";
      return {
        required: {
          valid: false
        }
      };
    }
    if (!this.verifyValidDate(startDate, endDate)) {
      this.errorMessage = this.literals.invalidDate;
      return {
        date: {
          valid: false
        }
      };
    }
    if (this.dateRangeObjectFailed(control.value) || this.dateRangeFormatFailed(startDate, endDate)) {
      this.errorMessage = this.literals.invalidFormat;
      return {
        date: {
          valid: false
        }
      };
    }
    if (this.dateRangeFailed(startDate, endDate)) {
      this.errorMessage = this.literals.startDateGreaterThanEndDate;
      return {
        date: {
          valid: false
        }
      };
    }
    if (startDate && !this.validateDateInRange(startDate) || endDate && !this.validateDateInRange(endDate)) {
      this.errorMessage = this.literals.dateOutOfPeriod;
      return {
        date: {
          valid: false
        }
      };
    }
    if (this.fieldErrorMessage) {
      this.subscription?.unsubscribe();
      this.subscription = control.statusChanges.pipe(switchMap((status) => {
        if (status === "INVALID") {
          this.changeDetector.markForCheck();
        }
        return [];
      })).subscribe();
    }
    return null;
  }
  validateDateInRange(startDate) {
    return validateDateRange(convertIsoToDate(startDate, false, false), this._minDate, this._maxDate);
  }
  writeValue(dateRange) {
    this.resetDateRangeInputValidation();
    if (!dateRange || this.dateRangeObjectFailed(dateRange)) {
      this.dateRange = {
        start: "",
        end: ""
      };
    }
    if (!dateRange) {
      this.validateModel(this.dateRange);
    }
    if (this.dateRangeObjectFailed(dateRange)) {
      this.updateModel(dateRange);
    }
    if (this.isDateRangeObject(dateRange)) {
      this.dateRange = {
        start: this.convertPatternDateFormat(dateRange.start),
        end: this.convertPatternDateFormat(dateRange.end)
      };
      this.updateModel(this.dateRange);
    }
    this.updateScreenByModel(this.dateRange);
  }
  // Retorna um objeto do tipo PoMask com a mascara configurada.
  buildMask(format2 = this.format) {
    let mask = format2.toUpperCase();
    mask = mask.replace(/DD/g, "99");
    mask = mask.replace(/MM/g, "99");
    mask = mask.replace(/YYYY/g, "9999");
    return new PoMask(mask, true);
  }
  dateFormatFailed(value) {
    return value && !this.poDateService.isValidIso(value);
  }
  // Executa a função onChange
  updateModel(value) {
    const model2 = typeof value === "object" ? __spreadValues({}, value) : value;
    if (this.onChangeModel) {
      this.onChangeModel(model2);
    }
    this.controlChangeModelEmitter(model2);
  }
  controlChangeModelEmitter(value) {
    const current = JSON.stringify(this.modelLastUpdate);
    const incoming = JSON.stringify(value);
    if (current !== incoming) {
      this.changeModel.emit(value);
      this.modelLastUpdate = value && typeof value === "object" ? __spreadValues({}, value) : value;
    }
  }
  validateModel(value) {
    const model2 = __spreadValues({}, value);
    if (this.validatorChange) {
      this.validatorChange(model2);
    }
  }
  verifyValidDate(startDate, endDate) {
    if (startDate !== "" && endDate !== "") {
      return this.dateIsValid(startDate) && this.dateIsValid(endDate);
    } else if (startDate !== "") {
      return this.dateIsValid(startDate);
    } else {
      return this.dateIsValid(endDate);
    }
  }
  convertPatternDateFormat(value) {
    if (value instanceof Date) {
      return this.poDateService.convertDateToISO(value);
    }
    return value;
  }
  dateRangeFailed(startDate, endDate) {
    return !this.poDateService.isDateRangeValid(endDate, startDate) || !this.isStartDateRangeInputValid;
  }
  dateRangeFormatFailed(startDate, endDate) {
    return this.dateFormatFailed(endDate) || this.dateFormatFailed(startDate) || !this.isDateRangeInputFormatValid;
  }
  dateRangeObjectFailed(value) {
    return value && !this.isDateRangeObject(value);
  }
  isDateRangeObject(value) {
    return value && value.hasOwnProperty("start") && value.hasOwnProperty("end");
  }
  requiredDateRangeFailed(startDate, endDate) {
    return this.isDateRangeInputValid && PoValidators.requiredFailed(this.required || this.hasValidatorRequired, this.disabled, startDate) && PoValidators.requiredFailed(this.required || this.hasValidatorRequired, this.disabled, endDate);
  }
  dateIsValid(date) {
    const [strYear, strMonth, strDay] = date.split("-");
    const year = Number(strYear);
    const month = Number(strMonth);
    const day = Number(strDay);
    if (month === 1 || month === 3 || month === 5 || month === 7 || month === 8 || month === 10 || month === 12) {
      return day < 1 || day > 31 ? false : true;
    } else if (month === 4 || month === 6 || month === 9 || month === 11) {
      return day < 1 || day > 30 ? false : true;
    } else {
      if (year % 4 === 0 && year % 100 !== 0 || year % 400 === 0) {
        return day < 1 || day > 29 ? false : true;
      } else {
        return day < 1 || day > 28 ? false : true;
      }
    }
  }
  applySizeBasedOnA11y() {
    const size = validateSizeFn(this._initialSize, PoFieldSize);
    this._size = size;
  }
  static ɵfac = function PoDatepickerRangeBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoDatepickerRangeBaseComponent)(ɵɵdirectiveInject(ChangeDetectorRef), ɵɵdirectiveInject(PoDateService), ɵɵdirectiveInject(PoLanguageService));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoDatepickerRangeBaseComponent,
    hostVars: 1,
    hostBindings: function PoDatepickerRangeBaseComponent_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("PoUiThemeChange", function PoDatepickerRangeBaseComponent_PoUiThemeChange_HostBindingHandler() {
          return ctx.onThemeChange();
        }, ɵɵresolveWindow);
      }
      if (rf & 2) {
        ɵɵattribute("p-size", ctx.size);
      }
    },
    inputs: {
      additionalHelpEventTrigger: "additionalHelpEventTrigger",
      name: "name",
      additionalHelpTooltip: [0, "p-additional-help-tooltip", "additionalHelpTooltip"],
      appendBox: [2, "p-append-in-body", "appendBox", convertToBoolean],
      autoFocus: [2, "p-auto-focus", "autoFocus", convertToBoolean],
      compactLabel: [1, "p-compact-label", "compactLabel"],
      rangePresets: [0, "p-range-presets", "rangePresets"],
      rangePresetOptions: [0, "p-range-preset-options", "rangePresetOptions"],
      rangePresetsOrder: [0, "p-range-presets-order", "rangePresetsOrder"],
      help: [0, "p-help", "help"],
      label: [0, "p-label", "label"],
      optional: [0, "p-optional", "optional"],
      placeholder: [0, "p-placeholder", "placeholder"],
      fieldErrorMessage: [0, "p-field-error-message", "fieldErrorMessage"],
      errorLimit: [0, "p-error-limit", "errorLimit"],
      poHelperComponent: [1, "p-helper", "poHelperComponent"],
      labelTextWrap: [1, "p-label-text-wrap", "labelTextWrap"],
      clean: [2, "p-clean", "clean", convertToBoolean],
      disabled: [2, "p-disabled", "disabled", convertToBoolean],
      endDate: [0, "p-end-date", "endDate"],
      literals: [0, "p-literals", "literals"],
      loading: [2, "p-loading", "loading", convertToBoolean],
      minDate: [0, "p-min-date", "minDate"],
      maxDate: [0, "p-max-date", "maxDate"],
      noAutocomplete: [2, "p-no-autocomplete", "noAutocomplete", convertToBoolean],
      readonly: [2, "p-readonly", "readonly", convertToBoolean],
      required: [2, "p-required", "required", convertToBoolean],
      showRequired: [0, "p-show-required", "showRequired"],
      size: [0, "p-size", "size"],
      startDate: [0, "p-start-date", "startDate"],
      locale: [0, "p-locale", "locale"]
    },
    outputs: {
      additionalHelp: "p-additional-help",
      onChange: "p-change",
      changeModel: "p-change-model",
      keydown: "p-keydown"
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoDatepickerRangeBaseComponent, [{
    type: Directive
  }], () => [{
    type: ChangeDetectorRef
  }, {
    type: PoDateService
  }, {
    type: PoLanguageService
  }], {
    additionalHelpEventTrigger: [{
      type: Input
    }],
    name: [{
      type: Input,
      args: ["name"]
    }],
    additionalHelpTooltip: [{
      type: Input,
      args: ["p-additional-help-tooltip"]
    }],
    appendBox: [{
      type: Input,
      args: [{
        alias: "p-append-in-body",
        transform: convertToBoolean
      }]
    }],
    autoFocus: [{
      type: Input,
      args: [{
        alias: "p-auto-focus",
        transform: convertToBoolean
      }]
    }],
    compactLabel: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-compact-label",
        required: false
      }]
    }],
    rangePresets: [{
      type: Input,
      args: ["p-range-presets"]
    }],
    rangePresetOptions: [{
      type: Input,
      args: ["p-range-preset-options"]
    }],
    rangePresetsOrder: [{
      type: Input,
      args: ["p-range-presets-order"]
    }],
    help: [{
      type: Input,
      args: ["p-help"]
    }],
    label: [{
      type: Input,
      args: ["p-label"]
    }],
    optional: [{
      type: Input,
      args: ["p-optional"]
    }],
    placeholder: [{
      type: Input,
      args: ["p-placeholder"]
    }],
    fieldErrorMessage: [{
      type: Input,
      args: ["p-field-error-message"]
    }],
    additionalHelp: [{
      type: Output,
      args: ["p-additional-help"]
    }],
    errorLimit: [{
      type: Input,
      args: ["p-error-limit"]
    }],
    poHelperComponent: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-helper",
        required: false
      }]
    }],
    labelTextWrap: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-label-text-wrap",
        required: false
      }]
    }],
    onChange: [{
      type: Output,
      args: ["p-change"]
    }],
    changeModel: [{
      type: Output,
      args: ["p-change-model"]
    }],
    keydown: [{
      type: Output,
      args: ["p-keydown"]
    }],
    clean: [{
      type: Input,
      args: [{
        alias: "p-clean",
        transform: convertToBoolean
      }]
    }],
    disabled: [{
      type: Input,
      args: [{
        alias: "p-disabled",
        transform: convertToBoolean
      }]
    }],
    endDate: [{
      type: Input,
      args: ["p-end-date"]
    }],
    literals: [{
      type: Input,
      args: ["p-literals"]
    }],
    loading: [{
      type: Input,
      args: [{
        alias: "p-loading",
        transform: convertToBoolean
      }]
    }],
    minDate: [{
      type: Input,
      args: ["p-min-date"]
    }],
    maxDate: [{
      type: Input,
      args: ["p-max-date"]
    }],
    noAutocomplete: [{
      type: Input,
      args: [{
        alias: "p-no-autocomplete",
        transform: convertToBoolean
      }]
    }],
    readonly: [{
      type: Input,
      args: [{
        alias: "p-readonly",
        transform: convertToBoolean
      }]
    }],
    required: [{
      type: Input,
      args: [{
        alias: "p-required",
        transform: convertToBoolean
      }]
    }],
    showRequired: [{
      type: Input,
      args: ["p-show-required"]
    }],
    size: [{
      type: Input,
      args: ["p-size"]
    }, {
      type: HostBinding,
      args: ["attr.p-size"]
    }],
    startDate: [{
      type: Input,
      args: ["p-start-date"]
    }],
    locale: [{
      type: Input,
      args: ["p-locale"]
    }],
    onThemeChange: [{
      type: HostListener,
      args: ["window:PoUiThemeChange"]
    }]
  });
})();
var arrowLeftKey = 37;
var arrowRightKey = 39;
var backspaceKey = 8;
var poDatepickerRangeDateLengthDefault = 10;
var poCalendarContentOffset = 8;
var poCalendarPositionDefault = "bottom-left";
var providers$5 = [{
  provide: NG_VALUE_ACCESSOR,
  // eslint-disable-next-line
  useExisting: forwardRef(() => PoDatepickerRangeComponent),
  multi: true
}, {
  provide: NG_VALIDATORS,
  // eslint-disable-next-line
  useExisting: forwardRef(() => PoDatepickerRangeComponent),
  multi: true
}, PoControlPositionService];
var PoDatepickerRangeComponent = class _PoDatepickerRangeComponent extends PoDatepickerRangeBaseComponent {
  controlPosition = inject(PoControlPositionService);
  renderer = inject(Renderer2);
  cd = inject(ChangeDetectorRef);
  poLanguageService;
  dateRangeField;
  endDateInput;
  startDateInput;
  iconCalendar;
  calendarPicker;
  helperEl;
  iconClean;
  id = `po-datepicker-range[${uuid()}]`;
  isCalendarVisible = false;
  widthWithPresets = false;
  clickListener;
  eventResizeListener;
  poDatepickerRangeElement;
  MIN_CALENDAR_WIDTH_WITH_PRESETS = {
    medium: 624,
    small: 512
  };
  get autocomplete() {
    return this.noAutocomplete ? "off" : "on";
  }
  get enableCleaner() {
    return this.clean && (this.startDateInputValue || this.endDateInputValue) && !this.disabled && !this.readonly;
  }
  get endDateInputName() {
    return "end-date";
  }
  get endDateInputValue() {
    return this.endDateInput.nativeElement.value;
  }
  get getErrorMessage() {
    if (this.fieldErrorMessage && !this.errorMessage && this.hasInvalidClass()) {
      return this.fieldErrorMessage;
    }
    return this.errorMessage !== "" && this.hasInvalidClass() ? this.errorMessage : "";
  }
  get isDateRangeInputUncompleted() {
    return this.endDateInputValue.length < poDatepickerRangeDateLengthDefault && this.startDateInputValue.length < poDatepickerRangeDateLengthDefault;
  }
  get isDirtyDateRangeInput() {
    return this.endDateInputValue.length > 0 || this.startDateInputValue.length > 0;
  }
  get startDateInputName() {
    return "start-date";
  }
  get startDateInputValue() {
    return this.startDateInput.nativeElement.value;
  }
  constructor() {
    const changeDetector = inject(ChangeDetectorRef);
    const poLanguageService = inject(PoLanguageService);
    const poDateService = inject(PoDateService);
    const poDatepickerRangeElement = inject(ElementRef);
    super(changeDetector, poDateService, poLanguageService);
    this.changeDetector = changeDetector;
    this.poLanguageService = poLanguageService;
    this.poDatepickerRangeElement = poDatepickerRangeElement;
  }
  static getKeyCode(event) {
    return event.keyCode || event.which;
  }
  static getTargetElement(event) {
    return event.target || event.srcElement;
  }
  static isValidKey(keyCode) {
    const isNumericKey = keyCode >= 48 && keyCode <= 57;
    const isNumericNumpadKey = keyCode >= 96 && keyCode <= 105;
    return isNumericKey || isNumericNumpadKey;
  }
  ngAfterViewInit() {
    if (this.autoFocus) {
      this.focus();
    }
    if (this.iconCalendar?.buttonElement?.nativeElement) {
      this.renderer.setAttribute(this.iconCalendar.buttonElement.nativeElement, "aria-label", this.literals.open);
    }
  }
  ngOnInit() {
    this.poMaskObject = this.buildMask(replaceFormatSeparator(this.format, this.poLanguageService.getDateSeparator(this.locale)));
    this.widthWithPresets = window.innerWidth < this.MIN_CALENDAR_WIDTH_WITH_PRESETS[this.size];
  }
  ngOnChanges(changes) {
    if (changes.minDate || changes.maxDate) {
      this.validateModel(this.dateRange);
    }
    if (changes.locale) {
      if (this.dateRange) {
        this.updateScreenByModel(this.dateRange);
      }
      this.poMaskObject = this.buildMask(replaceFormatSeparator(this.format, this.poLanguageService.getDateSeparator(this.locale)));
    }
    if (changes.label) {
      this.displayAdditionalHelp = false;
    }
  }
  ngOnDestroy() {
    this.removeListeners();
  }
  clear() {
    this.resetDateRangeInputValidation();
    this.dateRange = {
      start: "",
      end: ""
    };
    this.updateScreenByModel(this.dateRange);
    this.updateModel(this.dateRange);
  }
  clearAndFocus() {
    this.clear();
    this.startDateInput.nativeElement.focus();
  }
  emitAdditionalHelp() {
    if (this.label && this.isAdditionalHelpEventTriggered()) {
      this.additionalHelp.emit();
    }
  }
  eventOnClick($event) {
    this.poMaskObject.click($event);
  }
  /**
   * Função que atribui foco ao componente.
   *
   * Para utilizá-la é necessário ter a instância do componente no DOM, podendo ser utilizado o ViewChild da seguinte forma:
   *
   * ```
   * import { PoDatepickerRangeComponent } from '@po-ui/ng-components';
   *
   * ...
   *
   * @ViewChild(PoDatepickerRangeComponent, { static: true }) datepickerRange: PoDatepickerRangeComponent;
   *
   * focusDatepickerRange() {
   *   this.datepickerRange.focus();
   * }
   * ```
   */
  focus() {
    if (!this.disabled) {
      this.startDateInput.nativeElement.focus();
    }
  }
  getAdditionalHelpTooltip() {
    return this.isAdditionalHelpEventTriggered() ? null : this.additionalHelpTooltip;
  }
  onBlur(event) {
    this.onTouchedModel?.();
    const isStartDateTargetEvent = event.target.name === this.startDateInputName;
    this.updateModelByScreen(isStartDateTargetEvent);
    this.removeFocusFromDatePickerRangeField();
  }
  onCalendarChange({
    start: start2,
    end: end2
  }) {
    const isStartDateTargetEvent = start2 && !end2;
    this.updateScreenByModel({
      start: start2 || "",
      end: end2 || ""
    });
    this.updateModelByScreen(isStartDateTargetEvent, start2 || "", end2 || "");
    if (start2 && end2) {
      setTimeout(() => {
        this.isCalendarVisible = false;
        this.cd.markForCheck();
        if (this.startDateInput?.nativeElement) {
          this.startDateInput.nativeElement.focus();
        }
      }, 300);
    } else if (!start2 && !end2) {
      this.closeCalendar();
    }
  }
  onFocus(event) {
    this.applyFocusOnDatePickerRangeField();
    this.poMaskObject.resetPositions(event);
  }
  onKeydown(event) {
    const isStartDateFocused = document.activeElement === this.startDateInput.nativeElement;
    const isEndDateFocused = document.activeElement === this.endDateInput.nativeElement;
    const isFieldFocused = isStartDateFocused || isEndDateFocused;
    if (this.readonly) {
      return;
    }
    if (this.isSetFocusOnBackspace(event)) {
      event.preventDefault();
      this.setFocusOnBackspace();
    } else {
      this.poMaskObject.keydown(event);
    }
    if (isFieldFocused) {
      this.keydown.emit(event);
    }
  }
  onKeyPress(event) {
    if (this.handleShiftTabWithCleaner(event)) return;
    if (this.handleShiftTabWithoutCleaner(event)) return;
    if (this.handleTabWithCalendarVisible(event)) return;
  }
  handleShiftTabWithCleaner(event) {
    if (event.key === "Tab" && event.shiftKey && !this.isCalendarVisible && this.enableCleaner) {
      this.iconClean.nativeElement?.focus();
      event.preventDefault();
      return true;
    }
    return false;
  }
  handleShiftTabWithoutCleaner(event) {
    if (event.key === "Tab" && event.shiftKey && !this.isCalendarVisible) {
      this.focus();
      event.preventDefault();
      event.stopPropagation();
      return true;
    }
    return false;
  }
  handleTabWithCalendarVisible(event) {
    if (event.key === "Tab" && !event.shiftKey && this.isCalendarVisible) {
      if (this.handleMobileNavigation(event)) return true;
      if (this.handlePresetNavigation(event)) return true;
      return this.handleComboNavigation(event);
    }
    return false;
  }
  handleMobileNavigation(event) {
    if (this.verifyMobile()) {
      const firstCombo = this.calendarPicker.nativeElement.querySelector(".po-combo-first .po-combo-input");
      if (firstCombo) {
        event.preventDefault();
        firstCombo.focus();
      }
      return true;
    }
    return false;
  }
  handlePresetNavigation(event) {
    const firstPreset = this.calendarPicker.nativeElement.querySelector(".po-calendar-preset-item .po-button");
    if (firstPreset && !this.calculateWidthWithPresets()) {
      event.preventDefault();
      firstPreset.focus();
      return true;
    }
    return false;
  }
  handleComboNavigation(event) {
    const firstCombo = this.calendarPicker.nativeElement.querySelector(".po-combo-first .po-combo-input");
    if (firstCombo) {
      event.preventDefault();
      firstCombo.focus();
      return true;
    }
    return false;
  }
  onKeydownDatepickerRange($event) {
    if (this.readonly) {
      return;
    }
    if ($event.key === "Escape" && this.isCalendarVisible) {
      this.isCalendarVisible = false;
      $event.preventDefault();
      $event.stopPropagation();
    }
    if ($event.key === "Tab" && $event.shiftKey && $event.target === this.startDateInput.nativeElement && this.isCalendarVisible) {
      this.isCalendarVisible = false;
    }
    if ($event.key === "Tab" && !$event.shiftKey && $event.target === this.iconClean?.nativeElement) {
      $event.preventDefault();
      $event.stopPropagation();
      this.iconCalendar.buttonElement?.nativeElement.focus();
    }
  }
  onKeyup(event) {
    if (this.readonly) {
      return;
    }
    const isStartDateTargetEvent = event.target.name === this.startDateInputName;
    this.setFocus(event);
    this.poMaskObject.keyup(event);
    this.updateModelWhenComplete(isStartDateTargetEvent, this.startDateInputValue, this.endDateInputValue);
  }
  resetDateRangeInputValidation() {
    this.isStartDateRangeInputValid = true;
    this.isDateRangeInputFormatValid = true;
  }
  /**
   * Método que exibe `p-helper` ou executa a ação definida em `p-helper{eventOnClick}` ou em `p-additionalHelp`.
   * Para isso, será necessário configurar uma tecla de atalho utilizando o evento `p-keydown`.
   *
   * > Exibe ou oculta o conteúdo do componente `po-helper` quando o componente estiver com foco.
   *
   * ```
   * // Exemplo com p-label e p-helper
   * <po-datepicker-range
   *  #datepickerRange
   *  ...
   *  p-label="Label do datepickerRange
   *  [p-helper]="helperOptions"
   *  (p-keydown)="onKeyDown($event, datepickerRange)"
   * ></po-datepicker-range>
   * ```
   * ```
   * ...
   * onKeyDown(event: KeyboardEvent, inp: PoDatepickerRangeComponent): void {
   *  if (event.code === 'F9') {
   *    inp.showAdditionalHelp();
   *  }
   * }
   * ```
   */
  showAdditionalHelp() {
    this.displayAdditionalHelp = !this.displayAdditionalHelp;
    const helper = this.poHelperComponent();
    const isHelpEvt = this.isAdditionalHelpEventTriggered();
    if (!this.label && (helper || this.additionalHelpTooltip || isHelpEvt)) {
      if (isHelpEvt) {
        this.additionalHelp.emit();
      }
      if (typeof helper !== "string" && typeof helper?.eventOnClick === "function") {
        helper.eventOnClick();
        return;
      }
      if (this.helperEl?.helperIsVisible()) {
        this.helperEl?.closeHelperPopover();
        return;
      }
      this.helperEl?.openHelperPopover();
      return;
    }
    return this.displayAdditionalHelp;
  }
  enableHorizontalMouseWheel() {
    const el = this?.calendarPicker.nativeElement.querySelector(".po-calendar-preset-list");
    el.addEventListener("wheel", (event) => {
      event.preventDefault();
      el.scrollLeft += event.deltaY;
    });
  }
  toggleCalendar() {
    if (this.disabled || this.readonly) {
      return;
    }
    this.isCalendarVisible = !this.isCalendarVisible;
    this.changeDetector.markForCheck();
    if (this.isCalendarVisible) {
      if (this.calculateWidthWithPresets()) {
        this.enableHorizontalMouseWheel();
      }
      this.setCalendarPosition();
      this.initializeListeners();
    } else {
      this.removeListeners();
    }
  }
  updateScreenByModel(model2) {
    const dateRange = {
      start: model2.start,
      end: model2.end
    };
    const isStartDateValid = this.poDateService.isDateRangeValid(dateRange.end, dateRange.start);
    const isDateValid = (date) => !this.dateFormatFailed(date) && isStartDateValid;
    const endDateFormated = isDateValid(dateRange.end) ? this.formatModelToScreen(dateRange.end) : "";
    const startDateFormated = isDateValid(dateRange.start) ? this.formatModelToScreen(dateRange.start) : "";
    this.endDateInput.nativeElement.value = endDateFormated;
    this.startDateInput.nativeElement.value = startDateFormated;
    this.changeDetector.detectChanges();
  }
  onCalendarKeyDown(event) {
    if (!this.isCalendarVisible) return;
    if (event.key === "Escape") {
      event.preventDefault();
      event.stopPropagation();
      this.iconCalendar.buttonElement?.nativeElement.focus();
      this.isCalendarVisible = false;
      return;
    }
    if (event.key === "Tab" && event.shiftKey && this.isFocusOnFirstCombo()) {
      if (this.verifyMobile()) {
        event.preventDefault();
        event.stopPropagation();
        this.iconCalendar.buttonElement?.nativeElement.focus();
        this.isCalendarVisible = false;
        return;
      }
      event.preventDefault();
      event.stopPropagation();
      const firstPreset = this.calendarPicker.nativeElement.querySelector(".po-calendar-preset-item .po-button");
      if (firstPreset && !this.calculateWidthWithPresets()) {
        firstPreset.focus();
        return;
      }
      this.iconCalendar.buttonElement?.nativeElement.focus();
      this.isCalendarVisible = false;
    }
  }
  setHelper(label, additionalHelpTooltip) {
    return setHelperSettings(label, additionalHelpTooltip, this.poHelperComponent(), this.size, this.isAdditionalHelpEventTriggered() ? this.additionalHelp : void 0);
  }
  onResize() {
    this.updateWidthWithPresets();
  }
  calculateWidthWithPresets() {
    const hasPresets = this.rangePresets || this.rangePresetOptions?.length > 0;
    if (!hasPresets) {
      return false;
    }
    const minWidth = this.MIN_CALENDAR_WIDTH_WITH_PRESETS[this.size];
    if (!minWidth) {
      return false;
    }
    return window.innerWidth < minWidth;
  }
  updateWidthWithPresets() {
    this.widthWithPresets = this.calculateWidthWithPresets();
  }
  verifyMobile() {
    return isMobile();
  }
  setCalendarPosition() {
    if (this.verifyMobile()) {
      return;
    }
    if (this?.calendarPicker.nativeElement && this.isCalendarVisible) {
      requestAnimationFrame(() => {
        const scrollHeight = this.calendarPicker.nativeElement.querySelector(".po-calendar")?.scrollHeight ?? this.calendarPicker.nativeElement.scrollHeight;
        const scrollWidth = this.calendarPicker.nativeElement.querySelector(".po-calendar")?.scrollWidth ?? this.calendarPicker.nativeElement.scrollWidth;
        this.calendarPicker.nativeElement.style.height = scrollHeight + "px";
        this.calendarPicker.nativeElement.style.width = scrollWidth + "px";
        this.controlPosition.setElements(this.calendarPicker.nativeElement, poCalendarContentOffset, this.dateRangeField, ["bottom-left", "bottom-right", "top-left", "top-right"], false, true);
        this.controlPosition.adjustPosition(poCalendarPositionDefault);
      });
    }
  }
  applyFocusOnDatePickerRangeField() {
    this.dateRangeField.nativeElement.classList.add("po-datepicker-range-field-focused");
  }
  formatDate(format2, day = "", month = "", year = "") {
    let dateFormatted = replaceFormatSeparator(format2 || this.format, this.poLanguageService.getDateSeparator(this.locale));
    day = day && day.includes("T") ? day.slice(0, 2) : day;
    dateFormatted = dateFormatted.replace("dd", ("0" + day).slice(-2));
    dateFormatted = dateFormatted.replace("mm", ("0" + month).slice(-2));
    dateFormatted = dateFormatted.replace("yyyy", String(year));
    return dateFormatted;
  }
  formatScreenToModel(value = "") {
    const [day, month, year] = value.split(this.poLanguageService.getDateSeparator(this.locale));
    return value ? this.formatDate("yyyy-mm-dd", day, month, year) : "";
  }
  formatModelToScreen(value = "") {
    const [year, month, day] = value.split("-");
    return value ? this.formatDate(this.format, day, month, year) : "";
  }
  getDateRangeFormatValidation(startDate, endDate, isStartDateTargetEvent) {
    this.setDateRangeInputValidation(startDate, endDate);
    return {
      isValid: this.isDateRangeInputFormatValid && this.isStartDateRangeInputValid && this.verifyValidDate(startDate, endDate),
      dateRangeModel: this.getValidatedModel(startDate, endDate, isStartDateTargetEvent)
    };
  }
  getValidatedModel(startDate, endDate, isStartDateTargetEvent) {
    const dateRangeModel = {
      start: "",
      end: ""
    };
    dateRangeModel.end = (isStartDateTargetEvent || this.isStartDateRangeInputValid) && !this.dateFormatFailed(endDate) ? endDate : "";
    dateRangeModel.start = (!isStartDateTargetEvent || this.isStartDateRangeInputValid) && !this.dateFormatFailed(startDate) ? startDate : "";
    return dateRangeModel;
  }
  hasAttrCalendar(element) {
    const attrCalendar = "attr-calendar";
    return element?.hasAttribute(attrCalendar) || element?.parentElement?.hasAttribute(attrCalendar);
  }
  hasInvalidClass() {
    return this.poDatepickerRangeElement.nativeElement.classList.contains("ng-invalid") && this.poDatepickerRangeElement.nativeElement.classList.contains("ng-dirty");
  }
  initializeListeners() {
    this.clickListener = this.renderer.listen("document", "click", (event) => {
      this.wasClickedOnPicker(event);
    });
    this.eventResizeListener = this.renderer.listen("window", "resize", () => {
      this.isCalendarVisible = false;
    });
    window.addEventListener("scroll", this.onScroll, true);
  }
  isAdditionalHelpEventTriggered() {
    return this.additionalHelpEventTrigger === "event" || this.additionalHelpEventTrigger === void 0 && this.additionalHelp.observed;
  }
  isEqualBeforeValue(startDate, endDate) {
    return this.isDateRangeInputFormatValid && endDate === this.dateRange.end && startDate === this.dateRange.start;
  }
  isFocusOnFirstCombo() {
    const first = this.calendarPicker.nativeElement.querySelector(".po-combo-first .po-combo-input");
    return first === document.activeElement;
  }
  isSetFocusOnBackspace(event) {
    return event.target.name === this.endDateInputName && this.endDateInput.nativeElement.selectionStart === 0 && this.endDateInput.nativeElement.selectionEnd === 0 && event.keyCode === backspaceKey;
  }
  onScroll = () => {
    if (this.isCalendarVisible && !this.verifyMobile()) {
      this.controlPosition.adjustPosition(poCalendarPositionDefault);
    }
  };
  removeFocusFromDatePickerRangeField() {
    this.dateRangeField.nativeElement.classList.remove("po-datepicker-range-field-focused");
  }
  setDateRangeInputValidation(startDate, endDate) {
    this.isStartDateRangeInputValid = this.poDateService.isDateRangeValid(endDate, startDate);
    this.isDateRangeInputFormatValid = !this.dateFormatFailed(startDate) && !this.dateFormatFailed(endDate);
  }
  setFocus(event) {
    const inputElement = _PoDatepickerRangeComponent.getTargetElement(event);
    const keyCode = _PoDatepickerRangeComponent.getKeyCode(event);
    const inputName = inputElement["name"];
    this.setFocusOnArrowLeft(keyCode, inputName);
    this.setFocusOnArrowRight(keyCode, inputName, inputElement);
    this.setFocusOnStartDateCompleted(keyCode, inputName);
  }
  setFocusAndPosition(position, inputElement, selectionRange) {
    this.focusOnElement(inputElement);
    setTimeout(() => {
      inputElement.nativeElement.setSelectionRange(selectionRange, selectionRange);
      this.poMaskObject.initialPosition = position;
      this.poMaskObject.finalPosition = position;
    });
  }
  focusOnElement(inputElement) {
    inputElement.nativeElement.focus();
  }
  removeListeners() {
    if (this.clickListener) {
      this.clickListener();
    }
    if (this.eventResizeListener) {
      this.eventResizeListener();
    }
    window.removeEventListener("scroll", this.onScroll, true);
  }
  setFocusOnArrowLeft(keyCode, inputName) {
    const isCursorAtStartOfInput = this.endDateInput.nativeElement.selectionStart === 0;
    if (inputName === this.endDateInputName && isCursorAtStartOfInput && keyCode === arrowLeftKey) {
      const inputLength = this.startDateInput.nativeElement.value.length;
      this.setFocusAndPosition(inputLength, this.startDateInput, inputLength);
    }
  }
  setFocusOnArrowRight(keyCode, inputName, inputElement) {
    const isCursorAtEndOfInput = this.startDateInput.nativeElement.selectionStart === inputElement.value.length;
    if (inputName === this.startDateInputName && isCursorAtEndOfInput && keyCode === arrowRightKey) {
      this.setFocusAndPosition(0, this.endDateInput, 0);
    }
  }
  setFocusOnBackspace() {
    const inputLength = this.startDateInput.nativeElement.value.length;
    this.startDateInput.nativeElement.value = this.startDateInputValue.slice(0, -1);
    this.setFocusAndPosition(inputLength, this.startDateInput, inputLength);
  }
  setFocusOnStartDateCompleted(keyCode, inputName) {
    const isLastKeyPressed = this.startDateInput.nativeElement.selectionStart === poDatepickerRangeDateLengthDefault;
    const isNewDateCompleted = this.startDateInputValue.length === poDatepickerRangeDateLengthDefault && isLastKeyPressed;
    const isValidKey = _PoDatepickerRangeComponent.isValidKey(keyCode);
    if (inputName === this.startDateInputName && isNewDateCompleted && isValidKey) {
      this.setFocusAndPosition(0, this.endDateInput, 0);
    }
  }
  updateModelWhenComplete(isStartDateTargetEvent, startDate, endDate) {
    const endDateFormatted = this.formatScreenToModel(endDate);
    const startDateFormatted = this.formatScreenToModel(startDate);
    const dateFormatValidation = this.getDateRangeFormatValidation(startDateFormatted, endDateFormatted, isStartDateTargetEvent);
    if (this.isEqualBeforeValue(startDateFormatted, endDateFormatted)) {
      this.resetDateRangeInputValidation();
      this.validateModel(this.dateRange);
      return;
    }
    if (dateFormatValidation.isValid) {
      this.dateRange = {
        start: startDateFormatted,
        end: endDateFormatted
      };
      this.updateModel(this.dateRange);
      this.onChange.emit(__spreadValues({}, this.dateRange));
    }
  }
  updateModelByScreen(isStartDateTargetEvent, startDate, endDate) {
    const endDateFormatted = endDate || this.formatScreenToModel(this.endDateInputValue);
    const startDateFormatted = startDate || this.formatScreenToModel(this.startDateInputValue);
    if (this.isDateRangeInputUncompleted && this.isDirtyDateRangeInput) {
      this.updateModel(this.dateRange);
      return;
    }
    if (this.isEqualBeforeValue(startDateFormatted, endDateFormatted)) {
      this.resetDateRangeInputValidation();
      this.validateModel(this.dateRange);
      return;
    }
    const dateFormatValidation = this.getDateRangeFormatValidation(startDateFormatted, endDateFormatted, isStartDateTargetEvent);
    if (dateFormatValidation.isValid) {
      this.dateRange = {
        start: startDateFormatted,
        end: endDateFormatted
      };
      this.updateModel(this.dateRange);
      this.onChange.emit(__spreadValues({}, this.dateRange));
    }
    if (!dateFormatValidation.isValid && this.verifyFormattedDates(startDateFormatted, endDateFormatted)) {
      this.dateRange = __spreadValues({}, dateFormatValidation.dateRangeModel);
      this.updateModel(dateFormatValidation.dateRangeModel);
    }
  }
  verifyFormattedDates(start2, end2) {
    return !!start2 || !!end2;
  }
  wasClickedOnPicker(event) {
    if (!this.isCalendarVisible) {
      return;
    }
    const calendarEl = this.calendarPicker?.nativeElement;
    const iconEl = this.iconCalendar?.buttonElement?.nativeElement;
    const target = event.target;
    if (calendarEl && iconEl && !calendarEl.contains(target) && !iconEl.contains(target) && !this.hasAttrCalendar(target)) {
      this.isCalendarVisible = false;
    }
    this.cd.markForCheck();
  }
  closeCalendar() {
    this.isCalendarVisible = false;
    setTimeout(() => {
      if (this.iconCalendar) {
        this.iconCalendar.focus();
      }
    });
  }
  static ɵfac = function PoDatepickerRangeComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoDatepickerRangeComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoDatepickerRangeComponent,
    selectors: [["po-datepicker-range"]],
    viewQuery: function PoDatepickerRangeComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c117, 7, ElementRef)(_c118, 7, ElementRef)(_c119, 7, ElementRef)(_c76, 5)(_c120, 5, ElementRef)(_c17, 5, PoHelperComponent)(_c69, 5, ElementRef);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.dateRangeField = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.endDateInput = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.startDateInput = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.iconCalendar = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.calendarPicker = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.helperEl = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.iconClean = _t.first);
      }
    },
    hostBindings: function PoDatepickerRangeComponent_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("keydown", function PoDatepickerRangeComponent_keydown_HostBindingHandler($event) {
          return ctx.onKeydownDatepickerRange($event);
        })("resize", function PoDatepickerRangeComponent_resize_HostBindingHandler() {
          return ctx.onResize();
        }, ɵɵresolveWindow);
      }
    },
    standalone: false,
    features: [ɵɵProvidersFeature(providers$5), ɵɵInheritDefinitionFeature, ɵɵNgOnChangesFeature],
    decls: 23,
    vars: 44,
    consts: [["trigger", "cdkOverlayOrigin"], ["dateRangeField", ""], ["startDateInput", ""], ["endDateInput", ""], ["sharedCalendarContent", ""], ["iconCalendar", ""], ["iconClean", ""], ["helperEl", ""], ["calendarPicker", ""], [3, "p-disabled", "p-id", "p-label", "p-optional", "p-required", "p-show-required", "p-helper", "p-show-helper", "p-size", "p-text-wrap", "p-compact-label"], ["cdkOverlayOrigin", "", 1, "po-field-container-content", "po-field-container-content-datepicker-range"], [1, "po-field-container-input"], ["role", "group", 1, "po-datepicker-range-field", "po-input"], [1, "po-datepicker-range-start-date"], ["maxlength", "10", "type", "text", 1, "po-datepicker-range-input", 3, "blur", "focus", "keydown", "keyup", "click", "autocomplete", "disabled", "id", "name", "readonly", "required", "placeholder"], [1, "po-datepicker-range-separator"], [1, "po-datepicker-range-end-date"], ["maxlength", "10", "type", "text", 1, "po-datepicker-range-input", 3, "blur", "focus", "keydown", "keyup", "click", "autocomplete", "disabled", "name", "readonly", "placeholder"], [3, "p-size"], [1, "po-field-icon-container-right"], [1, "po-field-helper-button", 3, "p-size", "p-helper", "p-disabled", "p-append-in-body"], [3, "p-append-in-body", "p-help", "p-disabled", "p-error-pattern", "p-error-limit", "p-size"], ["cdkConnectedOverlay", "", 3, "cdkConnectedOverlayOrigin", "cdkConnectedOverlayOpen", "cdkConnectedOverlayDisableClose"], ["p-icon", "ICON_CALENDAR_DOTS", "p-kind", "tertiary", 1, "po-datepicker-range-button", 3, "keydown", "p-click", "p-disabled", "p-size"], ["tabindex", "0", "role", "button", "p-icon", "ICON_FILL_CLEAR_CONTENT", 1, "po-icon-input", 3, "p-change-event", "keydown.enter", "keydown.space", "p-size"], [1, "po-button-vertical-divider"], [4, "ngTemplateOutlet"], [3, "hidden"], [1, "po-datepicker-calendar-overlay"], ["p-mode", "range", 3, "ngModelChange", "p-change-month-year", "keydown", "p-close", "ngModel", "p-max-date", "p-min-date", "p-locale", "p-size", "p-range-presets", "p-range-preset-options", "p-range-presets-order", "p-width-with-presets"], [1, "po-datepicker-calendar-overlay", 3, "click"]],
    template: function PoDatepickerRangeComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵdeclareLet(0);
        ɵɵelementStart(1, "po-field-container", 9)(2, "div", 10, 0)(4, "div", 11)(5, "div", 12, 1)(7, "div", 13)(8, "input", 14, 2);
        ɵɵlistener("blur", function PoDatepickerRangeComponent_Template_input_blur_8_listener($event) {
          return ctx.onBlur($event);
        })("focus", function PoDatepickerRangeComponent_Template_input_focus_8_listener($event) {
          return ctx.onFocus($event);
        })("keydown", function PoDatepickerRangeComponent_Template_input_keydown_8_listener($event) {
          return ctx.onKeydown($event);
        })("keyup", function PoDatepickerRangeComponent_Template_input_keyup_8_listener($event) {
          return ctx.onKeyup($event);
        })("click", function PoDatepickerRangeComponent_Template_input_click_8_listener($event) {
          return ctx.eventOnClick($event);
        });
        ɵɵelementEnd()();
        ɵɵelementStart(10, "div", 15);
        ɵɵtext(11, "-");
        ɵɵelementEnd();
        ɵɵelementStart(12, "div", 16)(13, "input", 17, 3);
        ɵɵlistener("blur", function PoDatepickerRangeComponent_Template_input_blur_13_listener($event) {
          return ctx.onBlur($event);
        })("focus", function PoDatepickerRangeComponent_Template_input_focus_13_listener($event) {
          return ctx.onFocus($event);
        })("keydown", function PoDatepickerRangeComponent_Template_input_keydown_13_listener($event) {
          return ctx.onKeydown($event);
        })("keyup", function PoDatepickerRangeComponent_Template_input_keyup_13_listener($event) {
          return ctx.onKeyup($event);
        })("click", function PoDatepickerRangeComponent_Template_input_click_13_listener($event) {
          return ctx.eventOnClick($event);
        });
        ɵɵelementEnd()();
        ɵɵconditionalCreate(15, PoDatepickerRangeComponent_Conditional_15_Template, 1, 1, "po-loading-icon", 18)(16, PoDatepickerRangeComponent_Conditional_16_Template, 4, 5, "div", 19);
        ɵɵelementEnd()();
        ɵɵconditionalCreate(17, PoDatepickerRangeComponent_Conditional_17_Template, 2, 4, "po-helper", 20);
        ɵɵelementEnd();
        ɵɵconditionalCreate(18, PoDatepickerRangeComponent_Conditional_18_Template, 1, 6, "po-field-container-bottom", 21);
        ɵɵelementEnd();
        ɵɵconditionalCreate(19, PoDatepickerRangeComponent_Conditional_19_Template, 1, 3, null, 22)(20, PoDatepickerRangeComponent_Conditional_20_Template, 1, 1, "ng-container");
        ɵɵtemplate(21, PoDatepickerRangeComponent_ng_template_21_Template, 4, 17, "ng-template", null, 4, ɵɵtemplateRefExtractor);
      }
      if (rf & 2) {
        const poHelper_r9 = ɵɵstoreLet(ctx.setHelper(ctx.label, ctx.additionalHelpTooltip));
        ɵɵadvance();
        ɵɵclassProp("po-date-picker-container-disabled", ctx.isDisabled);
        ɵɵproperty("p-disabled", ctx.isDisabled)("p-id", ctx.id)("p-label", ctx.label)("p-optional", ctx.optional)("p-required", ctx.required)("p-show-required", ctx.showRequired)("p-helper", ctx.label ? poHelper_r9.helperSettings : void 0)("p-show-helper", ctx.label ? ctx.displayAdditionalHelp : void 0)("p-size", ctx.size)("p-text-wrap", ctx.labelTextWrap())("p-compact-label", ctx.compactLabel());
        ɵɵadvance(3);
        ɵɵclassProp("readonly", ctx.readonly && !ctx.isDisabled && !ctx.loading);
        ɵɵadvance();
        ɵɵclassProp("po-datepicker-range-field-disabled", ctx.isDisabled)("po-input-icon-right", ctx.enableCleaner && !ctx.isDisabled);
        ɵɵattribute("disabled", ctx.isDisabled)("aria-label", ctx.label)("p-size", ctx.size);
        ɵɵadvance(2);
        ɵɵattribute("data-value", ctx.startDateInputValue || (ctx.placeholder == null ? null : ctx.placeholder.start));
        ɵɵadvance();
        ɵɵproperty("autocomplete", ctx.autocomplete)("disabled", ctx.isDisabled)("id", ctx.id)("name", ctx.startDateInputName)("readonly", ctx.readonly)("required", ctx.required)("placeholder", ctx.isDisabled ? "" : ctx.placeholder == null ? null : ctx.placeholder.start);
        ɵɵattribute("aria-label", ctx.literals.startDate)("data-focused-element", !ctx.isDisabled)("data-inactive-component", ctx.isDisabled || ctx.readonly);
        ɵɵadvance(5);
        ɵɵproperty("autocomplete", ctx.autocomplete)("disabled", ctx.isDisabled)("name", ctx.endDateInputName)("readonly", ctx.readonly)("placeholder", ctx.isDisabled ? "" : ctx.placeholder == null ? null : ctx.placeholder.end);
        ɵɵattribute("aria-label", ctx.literals.endDate);
        ɵɵadvance(2);
        ɵɵconditional(ctx.loading ? 15 : 16);
        ɵɵadvance(2);
        ɵɵconditional(!ctx.label && ctx.poHelperComponent() || !ctx.label && poHelper_r9.hideAdditionalHelp ? 17 : -1);
        ɵɵadvance();
        ɵɵconditional(!ctx.readonly ? 18 : -1);
        ɵɵadvance();
        ɵɵconditional(ctx.appendBox ? 19 : 20);
      }
    },
    dependencies: [NgTemplateOutlet, NgControlStatus, NgModel, CdkConnectedOverlay, CdkOverlayOrigin, PoButtonComponent, PoCalendarComponent, PoCleanComponent, PoFieldContainerBottomComponent, PoFieldContainerComponent, PoHelperComponent, PoLoadingIconComponent],
    encapsulation: 2,
    changeDetection: 0
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoDatepickerRangeComponent, [{
    type: Component,
    args: [{
      selector: "po-datepicker-range",
      changeDetection: ChangeDetectionStrategy.OnPush,
      providers: providers$5,
      standalone: false,
      template: `@let poHelper = setHelper(label, additionalHelpTooltip);
<po-field-container
  [class.po-date-picker-container-disabled]="isDisabled"
  [p-disabled]="isDisabled"
  [p-id]="id"
  [p-label]="label"
  [p-optional]="optional"
  [p-required]="required"
  [p-show-required]="showRequired"
  [p-helper]="label ? poHelper.helperSettings : undefined"
  [p-show-helper]="label ? displayAdditionalHelp : undefined"
  [p-size]="size"
  [p-text-wrap]="labelTextWrap()"
  [p-compact-label]="compactLabel()"
>
  <div
    cdkOverlayOrigin
    #trigger="cdkOverlayOrigin"
    class="po-field-container-content po-field-container-content-datepicker-range"
  >
    <div class="po-field-container-input" [class.readonly]="readonly && !isDisabled && !loading">
      <div
        #dateRangeField
        role="group"
        class="po-datepicker-range-field po-input"
        [class.po-datepicker-range-field-disabled]="isDisabled"
        [class.po-input-icon-right]="enableCleaner && !isDisabled"
        [attr.disabled]="isDisabled"
        [attr.aria-label]="label"
        [attr.p-size]="size"
      >
        <div class="po-datepicker-range-start-date" [attr.data-value]="startDateInputValue || placeholder?.start">
          <input
            #startDateInput
            class="po-datepicker-range-input"
            maxlength="10"
            type="text"
            [attr.aria-label]="literals.startDate"
            [autocomplete]="autocomplete"
            [attr.data-focused-element]="!isDisabled"
            [attr.data-inactive-component]="isDisabled || readonly"
            [disabled]="isDisabled"
            [id]="id"
            [name]="startDateInputName"
            [readonly]="readonly"
            [required]="required"
            [placeholder]="isDisabled ? '' : placeholder?.start"
            (blur)="onBlur($event)"
            (focus)="onFocus($event)"
            (keydown)="onKeydown($event)"
            (keyup)="onKeyup($event)"
            (click)="eventOnClick($event)"
          />
        </div>

        <div class="po-datepicker-range-separator">-</div>

        <div class="po-datepicker-range-end-date">
          <input
            #endDateInput
            class="po-datepicker-range-input"
            maxlength="10"
            type="text"
            [attr.aria-label]="literals.endDate"
            [autocomplete]="autocomplete"
            [disabled]="isDisabled"
            [name]="endDateInputName"
            [readonly]="readonly"
            [placeholder]="isDisabled ? '' : placeholder?.end"
            (blur)="onBlur($event)"
            (focus)="onFocus($event)"
            (keydown)="onKeydown($event)"
            (keyup)="onKeyup($event)"
            (click)="eventOnClick($event)"
          />
        </div>

        @if (loading) {
          <po-loading-icon [p-size]="mapSizeToIcon(size)"></po-loading-icon>
        } @else {
          <div class="po-field-icon-container-right">
            @if (enableCleaner) {
              <po-clean
                #iconClean
                tabindex="0"
                role="button"
                [attr.aria-label]="literals.clean"
                class="po-icon-input"
                p-icon="ICON_FILL_CLEAR_CONTENT"
                [p-size]="size"
                (p-change-event)="clear()"
                (keydown.enter)="clearAndFocus(); $event.preventDefault()"
                (keydown.space)="clearAndFocus(); $event.preventDefault()"
              ></po-clean>
              <div class="po-button-vertical-divider"></div>
            }
            <po-button
              #iconCalendar
              class="po-datepicker-range-button"
              [class.po-button-tertiary-danger]="getErrorMessage"
              p-icon="ICON_CALENDAR_DOTS"
              p-kind="tertiary"
              [p-disabled]="isDisabled || readonly"
              [p-size]="size"
              (keydown)="onKeyPress($event)"
              (p-click)="toggleCalendar()"
            >
            </po-button>
          </div>
        }
      </div>
    </div>
    @if ((!this.label && poHelperComponent()) || (!this.label && poHelper.hideAdditionalHelp)) {
      <po-helper
        #helperEl
        class="po-field-helper-button"
        [p-size]="size"
        [p-helper]="poHelper.helperSettings"
        [p-disabled]="isDisabled"
        [p-append-in-body]="appendBox"
      >
      </po-helper>
    }
  </div>

  @if (!readonly) {
    <po-field-container-bottom
      [p-append-in-body]="appendBox"
      [p-help]="help"
      [p-disabled]="isDisabled"
      [p-error-pattern]="getErrorMessage"
      [p-error-limit]="errorLimit"
      [p-size]="size"
    ></po-field-container-bottom>
  }
</po-field-container>

@if (appendBox) {
  <ng-template
    cdkConnectedOverlay
    [cdkConnectedOverlayOrigin]="trigger"
    [cdkConnectedOverlayOpen]="true"
    [cdkConnectedOverlayDisableClose]="true"
  >
    <ng-container *ngTemplateOutlet="sharedCalendarContent"></ng-container>
  </ng-template>
} @else {
  <ng-container *ngTemplateOutlet="sharedCalendarContent"></ng-container>
}

<ng-template #sharedCalendarContent>
  <div
    #calendarPicker
    [class.po-calendar-range-picker]="!verifyMobile()"
    [class.po-datepicker-range-calendar-mobile-container]="verifyMobile() && isCalendarVisible"
    [hidden]="!isCalendarVisible"
  >
    @if (verifyMobile()) {
      <div class="po-datepicker-calendar-overlay" (click)="closeCalendar()"></div>
    }
    <po-calendar
      p-mode="range"
      [class.po-datepicker-range-calendar-mobile]="verifyMobile()"
      [ngModel]="dateRange"
      [p-max-date]="maxDate"
      [p-min-date]="minDate"
      [p-locale]="locale"
      [p-size]="size"
      [p-range-presets]="rangePresets"
      [p-range-preset-options]="rangePresetOptions"
      [p-range-presets-order]="rangePresetsOrder"
      [p-width-with-presets]="widthWithPresets"
      (ngModelChange)="onCalendarChange($event)"
      (p-change-month-year)="setCalendarPosition()"
      (keydown)="onCalendarKeyDown($event)"
      (p-close)="closeCalendar()"
    ></po-calendar>
  </div>
</ng-template>
`
    }]
  }], () => [], {
    dateRangeField: [{
      type: ViewChild,
      args: ["dateRangeField", {
        read: ElementRef,
        static: true
      }]
    }],
    endDateInput: [{
      type: ViewChild,
      args: ["endDateInput", {
        read: ElementRef,
        static: true
      }]
    }],
    startDateInput: [{
      type: ViewChild,
      args: ["startDateInput", {
        read: ElementRef,
        static: true
      }]
    }],
    iconCalendar: [{
      type: ViewChild,
      args: ["iconCalendar"]
    }],
    calendarPicker: [{
      type: ViewChild,
      args: ["calendarPicker", {
        read: ElementRef
      }]
    }],
    helperEl: [{
      type: ViewChild,
      args: ["helperEl", {
        read: PoHelperComponent,
        static: false
      }]
    }],
    iconClean: [{
      type: ViewChild,
      args: ["iconClean", {
        read: ElementRef
      }]
    }],
    onKeydownDatepickerRange: [{
      type: HostListener,
      args: ["keydown", ["$event"]]
    }],
    onResize: [{
      type: HostListener,
      args: ["window:resize"]
    }]
  });
})();
function applyDecimalFormat(value, parsed, decimalSeparator, thousandSeparator) {
  if (parsed === null || parsed === void 0) {
    return {
      viewValue: "",
      modelValue: void 0,
      isValid: false
    };
  }
  if (value === null || value === void 0 || Number.isNaN(value)) {
    return {
      viewValue: "",
      modelValue: void 0,
      isValid: true
    };
  }
  const isNegative = value < 0;
  const absValue = Math.abs(value);
  const fixedStr = absValue.toFixed(parsed.decimalDigitCount);
  const [intStr, decStr] = fixedStr.split(".");
  if (intStr.length > parsed.integerDigitCount) {
    return {
      viewValue: "",
      modelValue: value,
      isValid: false
    };
  }
  const formattedInteger = formatIntegerPart(intStr, parsed, thousandSeparator);
  const formattedDecimal = formatDecimalPart(decStr ?? "", parsed);
  let viewValue = formattedInteger;
  if (parsed.decimalDigitCount > 0) {
    viewValue = `${viewValue}${decimalSeparator}${formattedDecimal}`;
  }
  if (isNegative && parsed.allowNegative) {
    viewValue = `-${viewValue}`;
  }
  viewValue = viewValue.replaceAll("​", "");
  return {
    viewValue,
    modelValue: value,
    isValid: true
  };
}
function getFormatLimits(parsed) {
  if (!parsed) {
    return null;
  }
  return {
    decimalsLength: parsed.decimalDigitCount,
    thousandMaxlength: parsed.integerDigitCount
  };
}
function parseDecimalFormat(format2) {
  if (!format2) {
    return null;
  }
  const allowNegative = format2.startsWith("-");
  const cleanFormat = allowNegative ? format2.substring(1) : format2;
  const lastDotIndex = cleanFormat.lastIndexOf(".");
  let integerPart;
  let decimalPart;
  if (lastDotIndex >= 0) {
    integerPart = cleanFormat.substring(0, lastDotIndex);
    decimalPart = cleanFormat.substring(lastDotIndex + 1);
  } else {
    integerPart = cleanFormat;
    decimalPart = "";
  }
  const integerFormat = integerPart.split("");
  const decimalFormat = decimalPart.split("");
  const groupSeparatorPositions = [];
  let digitIndex = 0;
  for (const char of integerFormat) {
    if (char === "," || char === ".") {
      groupSeparatorPositions.push(digitIndex);
    } else {
      digitIndex++;
    }
  }
  const integerDigitCount = integerFormat.filter((c) => isFormatDigit(c)).length;
  const decimalDigitCount = decimalFormat.filter((c) => isFormatDigit(c)).length;
  return {
    integerFormat,
    decimalFormat,
    groupSeparatorPositions,
    allowNegative,
    integerDigitCount,
    decimalDigitCount,
    originalFormat: format2
  };
}
function validateAgainstFormat(rawValue, parsed, decimalSeparator) {
  if (!parsed || !rawValue) {
    return true;
  }
  const parts = rawValue.split(decimalSeparator);
  const integerPartRaw = parts[0].replaceAll(/\D/g, "");
  const decimalPartRaw = (parts[1] || "").replaceAll(/\D/g, "");
  if (parsed.decimalDigitCount === 0 && parts.length > 1) {
    return false;
  }
  return integerPartRaw.length <= parsed.integerDigitCount && decimalPartRaw.length <= parsed.decimalDigitCount;
}
var SUPPRESSION_PLACEHOLDER = "​";
function formatDecimalPart(decStr, parsed) {
  const allDigitFormats = parsed.decimalFormat.filter((c) => isFormatDigit(c));
  const digitFormats = allDigitFormats.slice(0, parsed.decimalDigitCount);
  const paddedDec = decStr.padEnd(digitFormats.length, "0");
  const resultDigits = [];
  for (let i2 = 0; i2 < digitFormats.length; i2++) {
    resultDigits.push(paddedDec[i2]);
  }
  for (let i2 = digitFormats.length - 1; i2 >= 0; i2--) {
    if (digitFormats[i2] === "<" && resultDigits[i2] === "0") {
      resultDigits[i2] = "";
    } else {
      break;
    }
  }
  return resultDigits.join("");
}
function formatIntegerPart(intStr, parsed, thousandSeparator) {
  const allDigitFormats = parsed.integerFormat.filter((c) => isFormatDigit(c));
  const digitFormats = allDigitFormats.slice(allDigitFormats.length - parsed.integerDigitCount);
  const paddedInt = intStr.padStart(digitFormats.length, "0");
  const resultDigits = [];
  let suppressionActive = true;
  for (let i2 = 0; i2 < digitFormats.length; i2++) {
    const formatChar = digitFormats[i2];
    const digit = paddedInt[i2];
    if (formatChar === ">" && digit === "0" && suppressionActive) {
      resultDigits.push(SUPPRESSION_PLACEHOLDER);
    } else {
      if (formatChar === ">" || formatChar === "9") {
        suppressionActive = false;
      }
      resultDigits.push(digit);
    }
  }
  const isFormatTruncated = digitFormats.length < allDigitFormats.length;
  if (isFormatTruncated) {
    return insertGroupSeparatorsFromRight(resultDigits, parsed, thousandSeparator);
  }
  return insertGroupSeparators(resultDigits, parsed, thousandSeparator);
}
function insertGroupSeparatorsFromRight(digits, parsed, thousandSeparator) {
  if (parsed.groupSeparatorPositions.length === 0) {
    return typeof digits === "string" ? digits : digits.join("");
  }
  const digitArray = typeof digits === "string" ? digits.split("") : digits;
  const originalDigitCount = parsed.integerFormat.filter((c) => isFormatDigit(c)).length;
  const insertPositions = [];
  for (const pos of parsed.groupSeparatorPositions) {
    const fromRight = originalDigitCount - pos;
    const posInDigits = digitArray.length - fromRight;
    if (posInDigits > 0 && posInDigits < digitArray.length) {
      insertPositions.push(posInDigits);
    }
  }
  insertPositions.sort((a, b) => a - b);
  const result = [];
  let lastIdx = 0;
  for (const pos of insertPositions) {
    for (let i2 = lastIdx; i2 < pos; i2++) {
      result.push(digitArray[i2]);
    }
    const hasVisibleContent = result.join("").replaceAll("​", "").trim().length > 0;
    result.push(hasVisibleContent ? thousandSeparator : SUPPRESSION_PLACEHOLDER);
    lastIdx = pos;
  }
  for (let i2 = lastIdx; i2 < digitArray.length; i2++) {
    result.push(digitArray[i2]);
  }
  return result.join("");
}
function insertGroupSeparators(digits, parsed, thousandSeparator) {
  if (parsed.groupSeparatorPositions.length === 0) {
    return digits.join("");
  }
  const result = [];
  let digitIdx = 0;
  for (const fc of parsed.integerFormat) {
    if (fc === "," || fc === ".") {
      const hasVisibleContent = result.join("").replaceAll("​", "").trim().length > 0;
      result.push(hasVisibleContent ? thousandSeparator : SUPPRESSION_PLACEHOLDER);
    } else {
      result.push(digits[digitIdx]);
      digitIdx++;
    }
  }
  return result.join("");
}
function isFormatDigit(char) {
  return char === "9" || char === ">" || char === "<";
}
var poDecimalDefaultDecimalsLength = 2;
var poDecimalDefaultThousandMaxlength = 13;
var poDecimalMaxDecimalsLength = 15;
var poDecimalTotalLengthLimit = 16;
var PoDecimalComponent = class _PoDecimalComponent extends PoInputBaseComponent {
  el = inject(ElementRef);
  poLanguageService = inject(PoLanguageService);
  inputEl;
  id = `po-decimal[${uuid()}]`;
  _decimalsLength = poDecimalDefaultDecimalsLength;
  _thousandMaxlength = poDecimalDefaultThousandMaxlength;
  _locale;
  _min;
  _max;
  decimalSeparator;
  fireChange = false;
  isKeyboardAndroid = false;
  minusSign = "-";
  oldDotsLength = null;
  thousandSeparator;
  valueBeforeChange;
  subscriptionValidator = new Subscription();
  regex = {
    thousand: new RegExp("\\,", "g"),
    decimal: new RegExp("\\.", "g")
  };
  get autocomplete() {
    return this.noAutocomplete ? "off" : "on";
  }
  /**
   * @optional
   *
   * @description
   *
   * Quantidade máxima de casas decimais.
   *
   * > **Importante:**
   * - O valor máximo permitido é 15;
   * - A soma total de `p-decimals-length` com `p-thousand-maxlength` limita-se à 16;
   * - Esta propriedade sobrepõe apenas o valor **padrão** de `p-thousand-maxlength`;
   * - Caso `p-thousand-maxlength` tenha um valor definido, esta propriedade poderá receber apenas o valor restante do limite total (16).
   * - Quando utilizado com `p-display-format`, será respeitado o valor **mais restritivo** entre esta propriedade e o número de casas decimais definido no formato.
   *
   * @default `2`
   */
  set decimalsLength(value) {
    let decimalsLength = convertToInt(value);
    decimalsLength = this.isValueBetweenAllowed(decimalsLength, poDecimalMaxDecimalsLength) ? decimalsLength : poDecimalDefaultDecimalsLength;
    if (this.isGreaterThanTotalLengthLimit(decimalsLength, this.thousandMaxlength)) {
      this.thousandMaxlength = poDecimalTotalLengthLimit - decimalsLength;
    }
    this._decimalsLength = decimalsLength;
    this.refreshOnPropertyChange();
  }
  get decimalsLength() {
    return this._decimalsLength;
  }
  /**
   * @optional
   *
   * @description
   *
   * Quantidade máxima de dígitos antes do separador decimal.
   *
   * > **Importante:**
   * - O valor máximo permitido é 13;
   * - A soma total de `p-decimals-length` com `p-thousand-maxlength` limita-se à 16;
   * - Esta propriedade sobrepõe o valor definido em `p-decimals-length`.
   * - Quando utilizado com `p-display-format`, será respeitado o valor **mais restritivo** entre esta propriedade e o número de dígitos inteiros definido no
   * formato.
   *
   * @default `13`
   */
  set thousandMaxlength(value) {
    let thousandMaxlength = convertToInt(value);
    if (this.decimalsLength > poDecimalDefaultDecimalsLength && !thousandMaxlength) {
      thousandMaxlength = poDecimalTotalLengthLimit - this.decimalsLength;
    }
    thousandMaxlength = this.isValueBetweenAllowed(thousandMaxlength, poDecimalDefaultThousandMaxlength) ? thousandMaxlength : poDecimalDefaultThousandMaxlength;
    if (this.isGreaterThanTotalLengthLimit(this.decimalsLength, thousandMaxlength)) {
      this.decimalsLength = poDecimalTotalLengthLimit - thousandMaxlength;
    }
    this._thousandMaxlength = thousandMaxlength;
    this.refreshOnPropertyChange();
  }
  get thousandMaxlength() {
    return this._thousandMaxlength;
  }
  /**
   * @optional
   *
   * @description
   *
   * Informa o locale(país) para a formatação do valor.
   * Por padrão o valor será configurado segundo a o módulo [`I18n`](documentation/po-i18n)
   *
   * > Para ver quais linguagens suportadas acesse [`I18n`](documentation/po-i18n)
   *
   */
  set locale(locale) {
    this._locale = locale;
    this.setNumbersSeparators();
  }
  /**
   * @optional
   *
   * @description
   *
   * Define uma máscara de formatação numérica avançada para o campo.
   *
   * Simbologia suportada:
   * - `9`: Dígito obrigatório (preenche com zero à esquerda no blur);
   * - `>`: Supressão de zero à esquerda (dígito não obrigatório);
   * - `<`: Decimal flutuante, supressão de zeros à direita (dígito não obrigatório);
   * - `,` e `.`: Separadores de milhar/grupo e decimal (convertidos conforme `p-locale`);
   * - `-`: Sinal negativo (deve ser o primeiro caractere do formato).
   *
   * > **Importante:**
   * - A formatação via `p-display-format` é apenas visual; o valor do model é sempre o número puro.
   * - Quando o valor pré-preenchido não atende ao formato (overflow ou negativo sem `-`), o campo exibe com a formatação padrão do `po-decimal` (separadores de milhar + `p-decimals-length` casas decimais).
   * - Quando `p-decimals-length` ou `p-thousand-maxlength` são definidos, será respeitado o valor **mais restritivo** entre o formato e a propriedade.
   * - Incompatível com `p-mask`, `p-mask-format-model`, `p-mask-no-length-validation` e `p-pattern`.
   *
   * Exemplos de formato: `>>>,>>>,>>9.99`, `->>,>>9.99`, `999.9`, `>>>,>>9.9<<<<<<`
   *
   */
  displayFormat = input(void 0, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "displayFormat"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-display-format"
  }));
  formatParsed = signal(null, ...ngDevMode ? [{
    debugName: "formatParsed"
  }] : (
    /* istanbul ignore next */
    []
  ));
  /**
   * @optional
   *
   * @description
   *
   * Valor mínimo.
   */
  set min(value) {
    if (!isNaN(value)) {
      this._min = value;
      this.validateModel();
    } else if (!value) {
      this._min = void 0;
      this.validateModel();
    }
  }
  get min() {
    return this._min;
  }
  /**
   * @optional
   *
   * @description
   *
   * Valor máximo.
   */
  set max(value) {
    if (!isNaN(value)) {
      this._max = value;
      this.validateModel();
    } else if (!value) {
      this._max = void 0;
      this.validateModel();
    }
  }
  get max() {
    return this._max;
  }
  constructor() {
    const cd = inject(ChangeDetectorRef);
    super(cd);
    this.isKeyboardAndroid = !!navigator.userAgent.match(/Android/i);
    effect(() => {
      const formatValue = this.displayFormat();
      this.formatParsed.set(parseDecimalFormat(formatValue));
      this.refreshOnPropertyChange();
    });
  }
  ngOnInit() {
    this.setNumbersSeparators();
  }
  ngOnChanges(changes) {
    if (changes.label) {
      this.displayAdditionalHelp = false;
    }
  }
  setNumbersSeparators() {
    const {
      decimalSeparator,
      thousandSeparator
    } = this.poLanguageService.getNumberSeparators(this._locale);
    this.decimalSeparator = decimalSeparator;
    this.thousandSeparator = thousandSeparator;
    this.regex = {
      thousand: new RegExp("\\" + thousandSeparator, "g"),
      decimal: new RegExp("\\" + decimalSeparator, "g")
    };
  }
  ngAfterViewInit() {
    this.verifyAutoFocus();
  }
  ngOnDestroy() {
    this.subscriptionValidator?.unsubscribe();
  }
  clear(value) {
    this.callOnChange(value);
    this.controlChangeEmitter();
    if (this.errorAsyncProperties?.triggerMode === "changeModel") {
      this.verifyErrorAsync(value);
    }
  }
  extraValidation(abstractControl) {
    const value = abstractControl.value;
    const thousandValue = Math.trunc(value);
    this.errorPattern = this.errorPattern !== "Valor Inválido" ? this.errorPattern : "";
    if (minFailed(this.min, value)) {
      return {
        min: {
          valid: false
        }
      };
    }
    if (maxFailed(this.max, value)) {
      return {
        max: {
          valid: false
        }
      };
    }
    if (maxlengpoailed(this.thousandMaxlength, thousandValue) && this.thousandMaxlength < poDecimalDefaultThousandMaxlength || maxlengpoailed(poDecimalTotalLengthLimit, value)) {
      return {
        max: {
          valid: false
        }
      };
    }
    return null;
  }
  focus() {
    if (!this.disabled) {
      this.inputEl.nativeElement.focus();
    }
  }
  getScreenValue() {
    return this.inputEl ? this.inputEl.nativeElement.value : "";
  }
  hasInvalidClass() {
    return this.el.nativeElement.classList.contains("ng-invalid") && this.el.nativeElement.classList.contains("ng-dirty") && (this.getScreenValue() !== "" || this.showErrorMessageRequired && (this.required || this.hasValidatorRequired));
  }
  hasLetters(value = "") {
    return value.match(/[a-zA-Z:;+=_´`^~"'?!@#$%¨&*()><{}çÇ\[\]/\\|]+/);
  }
  hasNotSpace(value = "") {
    return value.match(/^\S*$/);
  }
  isValidNumber(event) {
    const keyValue = String.fromCharCode(event.which);
    const validKey = event.which !== 8 && event.which !== 0;
    return !this.hasLetters(keyValue) && this.hasNotSpace(keyValue) && validKey;
  }
  // função responsável por adicionar os zeros com as casa decimais ao sair do campo.
  onBlur(event) {
    this.onTouched?.();
    const value = event.target.value;
    if (value) {
      if (this.hasLetters(value) || this.containsMoreThanOneDecimalSeparator(value)) {
        this.setViewValue("");
        this.callOnChange(void 0);
        return;
      }
      const valueWithoutThousandSeparator = this.formatValueWithoutThousandSeparator(value);
      if (this.formatParsed()) {
        this.applyFormatOnBlurAndSetView(valueWithoutThousandSeparator, value);
      } else {
        const formatedViewValue = this.formatToViewValue(valueWithoutThousandSeparator);
        this.setViewValue(formatedViewValue);
        if (!formatedViewValue) {
          this.callOnChange(void 0);
          return;
        }
      }
    }
    this.blur.emit();
    this.controlChangeEmitter();
  }
  onFocus(event) {
    this.valueBeforeChange = this.getScreenValue();
    this.enter.emit();
  }
  onInput(event) {
    const selectionStart = event.target.selectionStart;
    const selectionEnd = event.target.selectionEnd;
    let modelValue;
    if (this.isKeyboardAndroid) {
      this.onInputKeyboardAndroid(event);
    }
    modelValue = this.formatValueWithoutThousandSeparator(event.target.value);
    modelValue = this.addZeroBefore(modelValue);
    if (this.formatParsed()) {
      const blockResult = this.validateFormatOnInput(event, modelValue, selectionStart);
      if (blockResult.blocked) {
        return;
      }
      modelValue = blockResult.modelValue;
    }
    const viewValue = this.formatParsed() ? this.formatMaskWithAblPositions(modelValue) : this.formatMask(modelValue);
    if (viewValue) {
      this.setViewValue(viewValue);
      this.setCursorInput(event, selectionStart, selectionEnd);
    }
    this.callOnChange(this.formatToModelValue(modelValue));
    if (this.errorAsyncProperties?.triggerMode === "changeModel") {
      this.verifyErrorAsync(this.formatToModelValue(modelValue));
    }
  }
  onInputKeyboardAndroid(event) {
    const inputValue = event.target.value;
    const selectionStart = event.target.selectionStart;
    const hasLetters = this.hasLetters(inputValue);
    if (hasLetters) {
      this.setViewValue(inputValue.replace(hasLetters[0], ""));
      return event.preventDefault();
    } else {
      const position = selectionStart - 1;
      const key2 = inputValue.charAt(position);
      this.setPositionValue(event);
      if (this.isValidKey(event, key2)) {
        this.setViewValue(inputValue);
      }
    }
  }
  onKeyDown(event) {
    const isFieldFocused = document.activeElement === this.inputEl.nativeElement;
    if (isFieldFocused) {
      this.keydown.emit(event);
    }
  }
  onKeyPress(event) {
    this.isValidKey(event);
  }
  writeValueModel(value) {
    let formatedViewValue;
    if (this.inputEl) {
      if (value || value === 0) {
        formatedViewValue = this.resolveViewValue(value);
        this.setViewValue(formatedViewValue);
      } else {
        this.setViewValue("");
      }
    }
    if (formatedViewValue) {
      this.change.emit(value);
      this.verifyErrorAsync(value);
    }
  }
  getErrorPatternMessage() {
    return this.errorPattern !== "" && this.hasInvalidClass() ? this.errorPattern : "";
  }
  // responsável por adicionar 0 antes da virgula (decimalSeparator).
  addZeroBefore(value) {
    const isDecimalSeparator = value === this.decimalSeparator;
    return isDecimalSeparator ? `0${value}` : value;
  }
  setHelper(label, additionalHelpTooltip) {
    return setHelperSettings(label, additionalHelpTooltip, this.poHelperComponent(), this.size, this.isAdditionalHelpEventTriggered() ? this.additionalHelp : void 0);
  }
  applyFormatOnBlur(value) {
    const effectiveParsed = this.getEffectiveFormatParsed();
    return applyDecimalFormat(value, effectiveParsed, this.decimalSeparator, this.thousandSeparator);
  }
  resolveViewValue(value) {
    if (this.formatParsed()) {
      const numValue = typeof value === "string" ? Number.parseFloat(value) : value;
      const result = this.applyFormatOnBlur(numValue);
      return result.isValid && result.viewValue?.trim() ? result.viewValue : this.formatToViewValue(value);
    }
    return this.formatToViewValue(value);
  }
  applyFormatOnBlurAndSetView(valueWithoutThousandSeparator, originalValue) {
    const cleanValue = valueWithoutThousandSeparator.replaceAll(/\s/g, "");
    const modelValue = this.formatToModelValue(cleanValue);
    if (modelValue === void 0 || modelValue === null) {
      this.setViewValue("");
      this.callOnChange(void 0);
    } else {
      const result = this.applyFormatOnBlur(modelValue);
      if (!result.isValid || !result.viewValue?.trim()) {
        const fallbackView = this.formatToViewValue(cleanValue);
        this.setViewValue(fallbackView || originalValue);
      } else {
        this.setViewValue(result.viewValue);
      }
    }
  }
  formatMaskWithAblPositions(value) {
    const effectiveParsed = this.getEffectiveFormatParsed();
    if (!effectiveParsed || effectiveParsed.groupSeparatorPositions.length === 0) {
      return value;
    }
    const sepIndex = value.indexOf(this.decimalSeparator);
    const integerPart = sepIndex >= 0 ? value.substring(0, sepIndex) : value;
    const decimalPart = sepIndex >= 0 ? value.substring(sepIndex) : "";
    const isNegative = integerPart.startsWith(this.minusSign);
    const digits = isNegative ? integerPart.substring(1) : integerPart;
    const result = insertGroupSeparatorsFromRight(digits, effectiveParsed, this.thousandSeparator);
    return `${isNegative ? this.minusSign : ""}${result}${decimalPart}`;
  }
  validateFormatOnInput(event, modelValue, selectionStart) {
    const effectiveParsed = this.getEffectiveFormatParsed();
    if (!effectiveParsed.allowNegative && modelValue.includes(this.minusSign)) {
      const withoutMinus = event.target.value.replaceAll("-", "");
      this.setViewValue(withoutMinus);
      event.target.setSelectionRange(selectionStart - 1, selectionStart - 1);
      modelValue = this.formatValueWithoutThousandSeparator(withoutMinus);
    }
    const isValid = validateAgainstFormat(modelValue, effectiveParsed, this.decimalSeparator);
    if (!isValid) {
      const previousValue = event.target.value.substring(0, selectionStart - 1) + event.target.value.substring(selectionStart);
      this.setViewValue(previousValue);
      event.target.setSelectionRange(selectionStart - 1, selectionStart - 1);
      return {
        blocked: true,
        modelValue
      };
    }
    return {
      blocked: false,
      modelValue
    };
  }
  getEffectiveFormatParsed() {
    const parsed = this.formatParsed();
    if (!parsed) {
      return null;
    }
    const limits = getFormatLimits(parsed);
    return __spreadProps(__spreadValues({}, parsed), {
      integerDigitCount: Math.min(limits.thousandMaxlength, this._thousandMaxlength),
      decimalDigitCount: Math.min(limits.decimalsLength, this._decimalsLength)
    });
  }
  getModelValueFromView() {
    if (!this.inputEl) {
      return void 0;
    }
    const currentView = this.getScreenValue();
    if (!currentView) {
      return void 0;
    }
    const rawValue = this.formatValueWithoutThousandSeparator(currentView);
    return this.formatToModelValue(rawValue);
  }
  refreshOnPropertyChange() {
    const modelValue = this.getModelValueFromView();
    if (modelValue == null || !this.inputEl) {
      return;
    }
    if (this.formatParsed()) {
      const result = this.applyFormatOnBlur(modelValue);
      const viewValue = result.isValid && result.viewValue?.trim() ? result.viewValue : this.formatToViewValue(String(modelValue));
      this.setViewValue(viewValue);
    } else {
      this.setViewValue(this.formatToViewValue(String(modelValue)));
    }
  }
  containsComma(value) {
    return value.includes(this.decimalSeparator);
  }
  containsMoreThanOneDecimalSeparator(value = "") {
    const foundComma = value.match(this.regex.decimal);
    return !!(foundComma && foundComma.length > 1);
  }
  controlChangeEmitter() {
    const elementValue = this.getScreenValue();
    if (elementValue !== this.valueBeforeChange) {
      this.fireChange = true;
      setTimeout(() => {
        this.change.emit(elementValue);
        const errorAsync = this.errorAsyncProperties;
        if (errorAsync?.triggerMode === "change" || !errorAsync?.triggerMode) {
          this.verifyErrorAsync(elementValue);
        }
      }, 200);
    }
  }
  formatMask(value) {
    if (value.match(this.regex.decimal)) {
      const regex = new RegExp(`(\\d)(?=(\\d{3})+(?!\\d)${this.decimalSeparator})`, "g");
      return value.toString().replace(regex, `$1${this.thousandSeparator}`);
    }
    return value.toString().replace(/(\d)(?=(\d{3})+(?!\d))/g, `$1${this.thousandSeparator}`);
  }
  formatToModelValue(value) {
    const formattedValue = this.replaceCommaToDot(value);
    const parsedValue = formattedValue ? parseFloat(Number(formattedValue).toFixed(this.decimalsLength)) : void 0;
    return parsedValue === 0 || parsedValue ? parsedValue : void 0;
  }
  formatToViewValue(value) {
    value = this.replaceCommaToDot(value);
    const numberValue = Number(value).toFixed(this.decimalsLength);
    const valueBeforeDot = this.getValueBeforeSeparator(numberValue, ".");
    const valueAfterDot = this.getValueAfterSeparator(numberValue, ".");
    const formatedNumber = this.formatMask(valueBeforeDot);
    if (formatedNumber === "NaN") {
      return "";
    }
    if (this.decimalsLength === 0) {
      return formatedNumber;
    } else {
      return `${formatedNumber}${this.decimalSeparator}${valueAfterDot}`;
    }
  }
  formatValueWithoutThousandSeparator(value = "") {
    return value.toString().replace(this.regex.thousand, "");
  }
  getValueAfterSeparator(value = "", separator) {
    return value.split(separator)[1] || "";
  }
  getValueBeforeSeparator(value = "", separator) {
    return value.split(separator)[0] || "";
  }
  hasLessDot(value) {
    if (value) {
      const dots = value.match(this.regex.thousand);
      const dotsLength = dots && dots.length;
      if (dotsLength < this.oldDotsLength) {
        this.oldDotsLength = dotsLength;
        return true;
      }
    }
    if (!value) {
      this.oldDotsLength = null;
    }
    return false;
  }
  hasMoreDot(value) {
    if (value) {
      const dots = value.match(this.regex.thousand);
      const dotsLength = dots && dots.length;
      if (dotsLength > this.oldDotsLength) {
        this.oldDotsLength = dotsLength;
        return true;
      }
    }
    if (!value) {
      this.oldDotsLength = null;
    }
    return false;
  }
  hasMinusSignInvalidPosition(event) {
    const keyIsMinusSign = event.key === this.minusSign;
    const selectionStart = event.target.selectionStart;
    return keyIsMinusSign && selectionStart !== 0;
  }
  isInvalidKey(event, charCode) {
    const isInvalidNumber = !this.isValidNumber(event);
    return this.verifyInsertComma(event) || this.verifyThousandLength(event) || this.verifyValueAfterComma(event) || this.verifyInsertMinusSign(event) || this.hasMinusSignInvalidPosition(event) || isInvalidNumber || this.validateCursorPositionBeforeSeparator(event) || this.verifyDecimalLengthIsZeroAndKeyPressedIsComma(charCode);
  }
  isGreaterThanTotalLengthLimit(decimalsMaxLength, thousandMaxlength) {
    return decimalsMaxLength + thousandMaxlength > poDecimalTotalLengthLimit;
  }
  isKeyDecimalSeparator(event) {
    return event.key === this.decimalSeparator || event.char === this.decimalSeparator;
  }
  isPositionAfterDecimalSeparator(positionCursor, value) {
    const indexComma = value && value.indexOf(this.decimalSeparator);
    if (indexComma && this.decimalsLength > 0) {
      return positionCursor > indexComma;
    }
  }
  isSelectionStartDifferentSelectionEnd(target) {
    return target.selectionStart !== target.selectionEnd;
  }
  isValidKey(event, key2) {
    const charCode = event.which || event.keyCode;
    const validKey = event.which === 8 || event.which === 0;
    if (validKey && !this.isKeyboardAndroid) {
      return;
    }
    if (key2) {
      event.key = key2;
    }
    if (this.isInvalidKey(event, charCode)) {
      event.preventDefault();
      return false;
    }
    return true;
  }
  isValueBetweenAllowed(value, maxAllowed) {
    return value >= 0 && value <= maxAllowed;
  }
  // Quando decimalsLength for 0 não deve permitir informar vírgula (decimalSeparator)
  verifyDecimalLengthIsZeroAndKeyPressedIsComma(charCode) {
    return charCode === 44 && this.decimalsLength === 0;
  }
  verifyAutoFocus() {
    if (this.autoFocus) {
      this.focus();
    }
  }
  setInitialSelectionRange(target, selectionStart, selectionEnd) {
    if (selectionStart === 1 && selectionEnd === 1) {
      return target.setSelectionRange(selectionStart + 1, selectionEnd + 1);
    }
    return target.setSelectionRange(selectionStart - 1, selectionEnd - 1);
  }
  replaceAt(value, index, replace) {
    return value.substring(0, index) + replace + value.substring(index + 1);
  }
  replaceCommaToDot(value = "") {
    if (this.decimalSeparator === ",") {
      value = value.toString().replace(this.regex.decimal, ".");
    }
    return value;
  }
  setCursorInput(event, selectionStart, selectionEnd) {
    const target = event.target;
    const viewValue = target.value;
    if (this.hasMoreDot(viewValue) || viewValue === "0" + this.decimalSeparator) {
      return target.setSelectionRange(selectionStart + 1, selectionEnd + 1);
    }
    if (this.hasLessDot(viewValue)) {
      this.setInitialSelectionRange(target, selectionStart, selectionEnd);
    }
    return target.setSelectionRange(selectionStart, selectionEnd);
  }
  setPositionValue(event) {
    const value = event.target.value;
    const position = event.target.selectionStart - 1;
    if (position > 0 && event.key === this.minusSign) {
      event.target.value = value.substring(0, position) + value.substr(position + 1);
    }
  }
  setViewValue(value) {
    this.inputEl.nativeElement.value = value;
  }
  validateCursorPositionBeforeSeparator(event) {
    const target = event.target;
    const originalValue = this.formatValueWithoutThousandSeparator(target.value);
    const valueBeforeSeparator = this.getValueBeforeSeparator(target.value, this.decimalSeparator);
    const valueBeforeSeparatorOriginal = this.getValueBeforeSeparator(originalValue, this.decimalSeparator);
    if (this.isSelectionStartDifferentSelectionEnd(target)) {
      return false;
    }
    return target.selectionStart <= valueBeforeSeparator.length && valueBeforeSeparatorOriginal.length === this.thousandMaxlength && !this.isKeyDecimalSeparator(event);
  }
  verifyErrorAsync(value) {
    if (this.errorPattern !== "" && this.errorAsyncProperties?.errorAsync) {
      const errorAsync = this.errorAsyncProperties.errorAsync(value);
      if (isObservable(errorAsync)) {
        this.subscriptionValidator?.unsubscribe();
        this.subscriptionValidator = errorAsync.pipe(switchMap((error2) => {
          const element = this.el.nativeElement;
          if (error2) {
            element.classList.add("ng-invalid");
            element.classList.add("ng-dirty");
            this.cd.detectChanges();
          } else if (element.classList.contains("ng-invalid") && element.classList.contains("ng-dirty") && !this.isInvalid) {
            element.classList.remove("ng-invalid");
            this.cd.detectChanges();
          }
          return of("");
        })).subscribe();
      }
    }
  }
  verifyThousandLength(event) {
    const target = event.target;
    const originalValue = this.formatValueWithoutThousandSeparator(target.value);
    const valueBeforeSeparatorOriginal = this.getValueBeforeSeparator(originalValue, this.decimalSeparator);
    if (this.isSelectionStartDifferentSelectionEnd(target)) {
      return false;
    }
    return valueBeforeSeparatorOriginal.length >= this.thousandMaxlength && !this.isKeyDecimalSeparator(event) && this.isPositionAfterDecimalSeparator(target.selectionStart - this.decimalsLength, target.value);
  }
  verifyInsertComma(e2) {
    const hasComma = this.containsComma(e2.target.value);
    return hasComma && e2.key === this.decimalSeparator;
  }
  verifyInsertMinusSign(event) {
    const value = event.target.value;
    const indexMinusSign = value.lastIndexOf(this.minusSign) !== -1;
    const positionMinusSign = value.lastIndexOf("-");
    const occurancesMinusSign = value.match(new RegExp("-", "g"));
    if (this.isKeyboardAndroid && indexMinusSign && occurancesMinusSign.length > 1) {
      event.target.value = this.replaceAt(value, positionMinusSign, "");
    }
    return indexMinusSign && event.key === this.minusSign;
  }
  verifyValueAfterComma(event) {
    const value = event.target.value;
    const selectionStart = event.target.selectionStart;
    const valueAfterSeparator = this.getValueAfterSeparator(value, this.decimalSeparator);
    return this.isPositionAfterDecimalSeparator(selectionStart, value) && valueAfterSeparator.length >= this.decimalsLength;
  }
  static ɵfac = function PoDecimalComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoDecimalComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoDecimalComponent,
    selectors: [["po-decimal"]],
    viewQuery: function PoDecimalComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c49, 7, ElementRef);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.inputEl = _t.first);
      }
    },
    inputs: {
      decimalsLength: [0, "p-decimals-length", "decimalsLength"],
      thousandMaxlength: [0, "p-thousand-maxlength", "thousandMaxlength"],
      locale: [0, "p-locale", "locale"],
      displayFormat: [1, "p-display-format", "displayFormat"],
      min: [0, "p-min", "min"],
      max: [0, "p-max", "max"]
    },
    standalone: false,
    features: [ɵɵProvidersFeature([{
      provide: NG_VALUE_ACCESSOR,
      useExisting: forwardRef(() => _PoDecimalComponent),
      multi: true
    }, {
      provide: NG_VALIDATORS,
      useExisting: forwardRef(() => _PoDecimalComponent),
      multi: true
    }]), ɵɵInheritDefinitionFeature, ɵɵNgOnChangesFeature],
    decls: 13,
    vars: 35,
    consts: [["inp", ""], ["helperEl", ""], [3, "p-disabled", "p-id", "p-label", "p-optional", "p-required", "p-show-required", "p-helper", "p-show-helper", "p-size", "p-text-wrap", "p-compact-label"], [1, "po-field-container-content"], [1, "po-field-container-content-helper-wrapper"], [1, "po-field-container-input"], [1, "po-field-icon-container-left"], ["inputmode", "decimal", "type", "text", 1, "po-input", 3, "blur", "focus", "input", "keypress", "keydown", "autocomplete", "ngClass", "disabled", "id", "placeholder", "readonly", "required"], [1, "po-field-icon-container-right"], [3, "p-size"], [1, "po-icon-input", 3, "p-element-ref", "p-size"], [1, "po-field-helper-button", 3, "p-size", "p-helper", "p-disabled", "p-append-in-body"], [3, "p-append-in-body", "p-help", "p-disabled", "p-error-limit", "p-error-pattern", "p-size"], [1, "po-field-icon", "po-icon-input", 3, "p-icon"], [1, "po-icon-input", 3, "p-change-event", "p-element-ref", "p-size"]],
    template: function PoDecimalComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵdeclareLet(0);
        ɵɵelementStart(1, "po-field-container", 2)(2, "div", 3)(3, "div", 4)(4, "div", 5);
        ɵɵconditionalCreate(5, PoDecimalComponent_Conditional_5_Template, 2, 5, "div", 6);
        ɵɵelementStart(6, "input", 7, 0);
        ɵɵlistener("blur", function PoDecimalComponent_Template_input_blur_6_listener($event) {
          return ctx.onBlur($event);
        })("focus", function PoDecimalComponent_Template_input_focus_6_listener($event) {
          return ctx.onFocus($event);
        })("input", function PoDecimalComponent_Template_input_input_6_listener($event) {
          return ctx.onInput($event);
        })("keypress", function PoDecimalComponent_Template_input_keypress_6_listener($event) {
          return ctx.onKeyPress($event);
        })("keydown", function PoDecimalComponent_Template_input_keydown_6_listener($event) {
          return ctx.onKeyDown($event);
        });
        ɵɵelementEnd();
        ɵɵelementStart(8, "div", 8);
        ɵɵconditionalCreate(9, PoDecimalComponent_Conditional_9_Template, 1, 1, "po-loading-icon", 9);
        ɵɵconditionalCreate(10, PoDecimalComponent_Conditional_10_Template, 1, 2, "po-clean", 10);
        ɵɵelementEnd()();
        ɵɵconditionalCreate(11, PoDecimalComponent_Conditional_11_Template, 2, 4, "po-helper", 11);
        ɵɵelementEnd()();
        ɵɵconditionalCreate(12, PoDecimalComponent_Conditional_12_Template, 1, 6, "po-field-container-bottom", 12);
        ɵɵelementEnd();
      }
      if (rf & 2) {
        const poHelper_r4 = ɵɵstoreLet(ctx.setHelper(ctx.label, ctx.additionalHelpTooltip));
        ɵɵadvance();
        ɵɵproperty("p-disabled", ctx.isDisabled)("p-id", ctx.id)("p-label", ctx.label)("p-optional", ctx.optional)("p-required", ctx.required)("p-show-required", ctx.showRequired)("p-helper", ctx.label ? poHelper_r4.helperSettings : void 0)("p-show-helper", ctx.label ? ctx.displayAdditionalHelp : void 0)("p-size", ctx.size)("p-text-wrap", ctx.labelTextWrap())("p-compact-label", ctx.compactLabel());
        ɵɵadvance(4);
        ɵɵconditional(ctx.icon ? 5 : -1);
        ɵɵadvance();
        ɵɵproperty("autocomplete", ctx.autocomplete)("ngClass", ɵɵpureFunction4(30, _c121, ctx.icon && ctx.size !== "small", ctx.icon && ctx.size === "small", (ctx.clean || ctx.loading) && ctx.size !== "small", (ctx.clean || ctx.loading) && ctx.size === "small"))("disabled", ctx.isDisabled)("id", ctx.id)("placeholder", ctx.isDisabled ? "" : ctx.placeholder)("readonly", ctx.readonly)("required", ctx.required);
        ɵɵattribute("data-focused-element", !ctx.isDisabled)("data-inactive-component", ctx.isDisabled || ctx.readonly)("max", ctx.max)("min", ctx.min)("name", ctx.name)("p-size", ctx.size);
        ɵɵadvance(3);
        ɵɵconditional(ctx.loading ? 9 : -1);
        ɵɵadvance();
        ɵɵconditional(!ctx.loading && ctx.clean && !ctx.isDisabled && !ctx.readonly ? 10 : -1);
        ɵɵadvance();
        ɵɵconditional(!ctx.label && ctx.poHelperComponent() || !ctx.label && poHelper_r4.hideAdditionalHelp ? 11 : -1);
        ɵɵadvance();
        ɵɵconditional(!ctx.readonly ? 12 : -1);
      }
    },
    dependencies: [NgClass, PoCleanComponent, PoFieldContainerBottomComponent, PoFieldContainerComponent, PoHelperComponent, PoIconComponent, PoLoadingIconComponent],
    encapsulation: 2,
    changeDetection: 0
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoDecimalComponent, [{
    type: Component,
    args: [{
      selector: "po-decimal",
      changeDetection: ChangeDetectionStrategy.OnPush,
      providers: [{
        provide: NG_VALUE_ACCESSOR,
        useExisting: forwardRef(() => PoDecimalComponent),
        multi: true
      }, {
        provide: NG_VALIDATORS,
        useExisting: forwardRef(() => PoDecimalComponent),
        multi: true
      }],
      standalone: false,
      template: `@let poHelper = setHelper(label, additionalHelpTooltip);
<po-field-container
  [p-disabled]="isDisabled"
  [p-id]="id"
  [p-label]="label"
  [p-optional]="optional"
  [p-required]="required"
  [p-show-required]="showRequired"
  [p-helper]="label ? poHelper.helperSettings : undefined"
  [p-show-helper]="label ? displayAdditionalHelp : undefined"
  [p-size]="size"
  [p-text-wrap]="labelTextWrap()"
  [p-compact-label]="compactLabel()"
>
  <div class="po-field-container-content">
    <div class="po-field-container-content-helper-wrapper">
      <div class="po-field-container-input">
        @if (icon) {
          <div class="po-field-icon-container-left">
            <po-icon
              class="po-field-icon po-icon-input"
              [class.po-field-icon-disabled]="isDisabled"
              [class.po-field-icon-aa]="size === 'small'"
              [p-icon]="icon"
            ></po-icon>
          </div>
        }

        <input
          #inp
          class="po-input"
          [attr.data-focused-element]="!isDisabled"
          [attr.data-inactive-component]="isDisabled || readonly"
          [attr.max]="max"
          [attr.min]="min"
          [attr.name]="name"
          [attr.p-size]="size"
          [autocomplete]="autocomplete"
          [ngClass]="{
            'po-input-icon-left': icon && size !== 'small',
            'po-input-icon-left-aa': icon && size === 'small',
            'po-input-icon-right': (clean || loading) && size !== 'small',
            'po-input-icon-right-aa': (clean || loading) && size === 'small'
          }"
          [disabled]="isDisabled"
          [id]="id"
          [placeholder]="isDisabled ? '' : placeholder"
          [readonly]="readonly"
          [required]="required"
          inputmode="decimal"
          type="text"
          (blur)="onBlur($event)"
          (focus)="onFocus($event)"
          (input)="onInput($event)"
          (keypress)="onKeyPress($event)"
          (keydown)="onKeyDown($event)"
        />

        <div class="po-field-icon-container-right">
          @if (loading) {
            <po-loading-icon [p-size]="mapSizeToIcon(size)"></po-loading-icon>
          }

          @if (!loading && clean && !isDisabled && !readonly) {
            <po-clean class="po-icon-input" [p-element-ref]="inputEl" [p-size]="size" (p-change-event)="clear($event)">
            </po-clean>
          }
        </div>
      </div>
      @if ((!this.label && poHelperComponent()) || (!this.label && poHelper.hideAdditionalHelp)) {
        <po-helper
          #helperEl
          class="po-field-helper-button"
          [p-size]="size"
          [p-helper]="poHelper.helperSettings"
          [p-disabled]="isDisabled"
          [p-append-in-body]="appendBox"
        >
        </po-helper>
      }
    </div>
  </div>

  @if (!readonly) {
    <po-field-container-bottom
      [p-append-in-body]="appendBox"
      [p-help]="help"
      [p-disabled]="isDisabled"
      [p-error-limit]="errorLimit"
      [p-error-pattern]="getErrorPatternMessage()"
      [p-size]="size"
    ></po-field-container-bottom>
  }
</po-field-container>
`
    }]
  }], () => [], {
    inputEl: [{
      type: ViewChild,
      args: ["inp", {
        read: ElementRef,
        static: true
      }]
    }],
    decimalsLength: [{
      type: Input,
      args: ["p-decimals-length"]
    }],
    thousandMaxlength: [{
      type: Input,
      args: ["p-thousand-maxlength"]
    }],
    locale: [{
      type: Input,
      args: ["p-locale"]
    }],
    displayFormat: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-display-format",
        required: false
      }]
    }],
    min: [{
      type: Input,
      args: ["p-min"]
    }],
    max: [{
      type: Input,
      args: ["p-max"]
    }]
  });
})();
var providers$4 = [{
  provide: NG_VALUE_ACCESSOR,
  // eslint-disable-next-line
  useExisting: forwardRef(() => PoEmailComponent),
  multi: true
}, {
  provide: NG_VALIDATORS,
  // eslint-disable-next-line
  useExisting: forwardRef(() => PoEmailComponent),
  multi: true
}];
var PoEmailComponent = class _PoEmailComponent extends PoInputGeneric {
  id = `po-email[${uuid()}]`;
  icon = "ICON_MAIL";
  type = "email";
  // Consideramos o uso do nosso pattern com a seguinte expressão.
  // Antes do símbolo @:
  // - não há limite de caracteres.
  // - não pode haver espaços em branco, caracteres acentuados, caracteres especiais ou símbolos.
  // - pode começar com letras, números, hífen ou undescore (underline).
  //
  // Depois do símbolo @:
  // - o domínio tem um limite de até 66 caracteres após um separador.
  // - separador deve ser um 'ponto' (.).
  // - o primeiro bloco pode conter letras, números, hífen ou underscore (underline).
  // - após o primeiro separador é permitido apenas letras.
  // - não pode haver espaços em branco, caracteres acentuados, caracteres especiais ou símbolos.
  //
  // Limite total de 254 caracteres para o e-mail.
  //
  // As recomendações foram consultadas nas RFC 1034, RFC 5321 e RFC 5322.
  //
  // RFC 1034 - https://datatracker.ietf.org/doc/html/rfc1034#section-3
  // RFC 5321 - https://datatracker.ietf.org/doc/html/rfc5321#section-4.5.3.1
  // RFC 5322 - https://datatracker.ietf.org/doc/html/rfc5322#section-3.4
  pattern = "^([\\w-]+(?:\\.[\\w-]+)*)@((?:[\\w-]+\\.)*\\w[\\w-]{0,66})\\.([A-Za-z]{2,66}(?:\\.[A-Za-z]{2})?)$";
  mask = "";
  listener = this.validateClassesForPattern.bind(this);
  /* istanbul ignore next */
  constructor() {
    const el = inject(ElementRef);
    const cd = inject(ChangeDetectorRef);
    super(el, cd);
    this.maxlength = 254;
  }
  ngOnChanges(changes) {
    if (changes.label) {
      this.displayAdditionalHelp = false;
    }
  }
  ngAfterViewInit() {
    setTimeout(() => {
      if (!this.onChangePropagate) {
        this.inputEl.nativeElement.addEventListener("keyup", this.listener);
      }
    });
    super.ngAfterViewInit();
  }
  ngOnDestroy() {
    if (!this.onChangePropagate) {
      this.inputEl.nativeElement.removeEventListener("keyup", this.listener);
    }
  }
  extraValidation(c) {
    return null;
  }
  static ɵfac = function PoEmailComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoEmailComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoEmailComponent,
    selectors: [["po-email"]],
    standalone: false,
    features: [ɵɵProvidersFeature(providers$4), ɵɵInheritDefinitionFeature, ɵɵNgOnChangesFeature],
    decls: 14,
    vars: 36,
    consts: [["fieldContainer", ""], ["inp", ""], ["helperEl", ""], [3, "p-disabled", "p-id", "p-label", "p-optional", "p-required", "p-show-required", "p-helper", "p-show-helper", "p-size", "p-text-wrap", "p-compact-label"], [1, "po-field-container-content"], [1, "po-field-container-content-helper-wrapper"], [1, "po-field-container-input"], [1, "po-field-icon-container-left"], [1, "po-input", 3, "blur", "click", "focus", "input", "keydown", "autocomplete", "disabled", "id", "placeholder", "readonly", "required", "type"], [1, "po-field-icon-container-right"], [3, "p-size"], [3, "class", "p-element-ref", "p-size"], [1, "po-field-helper-button", 3, "p-size", "p-helper", "p-disabled", "p-append-in-body"], [3, "p-append-in-body", "p-help", "p-disabled", "p-error-pattern", "p-error-limit", "p-size"], [3, "p-icon"], [3, "p-change-event", "p-element-ref", "p-size"]],
    template: function PoEmailComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵdeclareLet(0);
        ɵɵelementStart(1, "po-field-container", 3, 0)(3, "div", 4)(4, "div", 5)(5, "div", 6);
        ɵɵconditionalCreate(6, PoEmailComponent_Conditional_6_Template, 2, 8, "div", 7);
        ɵɵelementStart(7, "input", 8, 1);
        ɵɵlistener("blur", function PoEmailComponent_Template_input_blur_7_listener($event) {
          return ctx.eventOnBlur($event);
        })("click", function PoEmailComponent_Template_input_click_7_listener($event) {
          return ctx.eventOnClick($event);
        })("focus", function PoEmailComponent_Template_input_focus_7_listener($event) {
          return ctx.eventOnFocus($event);
        })("input", function PoEmailComponent_Template_input_input_7_listener($event) {
          return ctx.eventOnInput($event);
        })("keydown", function PoEmailComponent_Template_input_keydown_7_listener($event) {
          return ctx.onKeyDown($event);
        });
        ɵɵelementEnd();
        ɵɵelementStart(9, "div", 9);
        ɵɵconditionalCreate(10, PoEmailComponent_Conditional_10_Template, 1, 1, "po-loading-icon", 10);
        ɵɵconditionalCreate(11, PoEmailComponent_Conditional_11_Template, 1, 4, "po-clean", 11);
        ɵɵelementEnd()();
        ɵɵconditionalCreate(12, PoEmailComponent_Conditional_12_Template, 2, 4, "po-helper", 12);
        ɵɵelementEnd()();
        ɵɵconditionalCreate(13, PoEmailComponent_Conditional_13_Template, 1, 6, "po-field-container-bottom", 13);
        ɵɵelementEnd();
      }
      if (rf & 2) {
        const poHelper_r4 = ɵɵstoreLet(ctx.setHelper(ctx.label, ctx.additionalHelpTooltip));
        ɵɵadvance();
        ɵɵproperty("p-disabled", ctx.isDisabled)("p-id", ctx.id)("p-label", ctx.label)("p-optional", ctx.optional)("p-required", ctx.required)("p-show-required", ctx.showRequired)("p-helper", ctx.label ? poHelper_r4.helperSettings : void 0)("p-show-helper", ctx.label ? ctx.displayAdditionalHelp : void 0)("p-size", ctx.size)("p-text-wrap", ctx.labelTextWrap())("p-compact-label", ctx.compactLabel());
        ɵɵadvance(5);
        ɵɵconditional(ctx.icon ? 6 : -1);
        ɵɵadvance();
        ɵɵclassProp("po-input-icon-left", ctx.icon)("po-input-icon-left-aa", ctx.icon && ctx.size === "small")("po-input-icon-right", ctx.clean || ctx.loading)("po-input-icon-right-aa", (ctx.clean || ctx.loading) && ctx.size === "small");
        ɵɵproperty("autocomplete", ctx.autocomplete)("disabled", ctx.isDisabled)("id", ctx.id)("placeholder", ctx.isDisabled ? "" : ctx.placeholder)("readonly", ctx.readonly)("required", ctx.required)("type", ctx.type);
        ɵɵattribute("name", ctx.name)("data-focused-element", !ctx.isDisabled)("data-inactive-component", ctx.isDisabled || ctx.readonly)("p-size", ctx.size);
        ɵɵadvance(3);
        ɵɵconditional(ctx.loading ? 10 : -1);
        ɵɵadvance();
        ɵɵconditional(!ctx.loading && ctx.clean && !ctx.isDisabled && !ctx.readonly ? 11 : -1);
        ɵɵadvance();
        ɵɵconditional(!ctx.label && ctx.poHelperComponent() || !ctx.label && poHelper_r4.hideAdditionalHelp ? 12 : -1);
        ɵɵadvance();
        ɵɵconditional(!ctx.readonly ? 13 : -1);
      }
    },
    dependencies: [PoCleanComponent, PoFieldContainerBottomComponent, PoFieldContainerComponent, PoHelperComponent, PoIconComponent, PoLoadingIconComponent],
    encapsulation: 2,
    changeDetection: 0
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoEmailComponent, [{
    type: Component,
    args: [{
      selector: "po-email",
      changeDetection: ChangeDetectionStrategy.OnPush,
      providers: providers$4,
      standalone: false,
      template: `@let poHelper = setHelper(label, additionalHelpTooltip);
<po-field-container
  #fieldContainer
  [p-disabled]="isDisabled"
  [p-id]="id"
  [p-label]="label"
  [p-optional]="optional"
  [p-required]="required"
  [p-show-required]="showRequired"
  [p-helper]="label ? poHelper.helperSettings : undefined"
  [p-show-helper]="label ? displayAdditionalHelp : undefined"
  [p-size]="size"
  [p-text-wrap]="labelTextWrap()"
  [p-compact-label]="compactLabel()"
>
  <div class="po-field-container-content">
    <div class="po-field-container-content-helper-wrapper">
      <div class="po-field-container-input">
        @if (icon) {
          <div class="po-field-icon-container-left">
            <po-icon
              class="po-field-icon {{ disabled ? 'po-icon-input-disabled' : 'po-icon-input' }}"
              [class.po-field-icon-disabled]="isDisabled"
              [class.po-field-icon-aa]="size === 'small'"
              [p-icon]="icon"
            ></po-icon>
          </div>
        }

        <input
          #inp
          class="po-input"
          [attr.name]="name"
          [attr.data-focused-element]="!isDisabled"
          [attr.data-inactive-component]="isDisabled || readonly"
          [attr.p-size]="size"
          [autocomplete]="autocomplete"
          [class.po-input-icon-left]="icon"
          [class.po-input-icon-left-aa]="icon && size === 'small'"
          [class.po-input-icon-right]="clean || loading"
          [class.po-input-icon-right-aa]="(clean || loading) && size === 'small'"
          [disabled]="isDisabled"
          [id]="id"
          [placeholder]="isDisabled ? '' : placeholder"
          [readonly]="readonly"
          [required]="required"
          [type]="type"
          (blur)="eventOnBlur($event)"
          (click)="eventOnClick($event)"
          (focus)="eventOnFocus($event)"
          (input)="eventOnInput($event)"
          (keydown)="onKeyDown($event)"
        />
        <div class="po-field-icon-container-right">
          @if (loading) {
            <po-loading-icon [p-size]="mapSizeToIcon(size)"></po-loading-icon>
          }

          @if (!loading && clean && !isDisabled && !readonly) {
            <po-clean
              class="{{ disabled ? 'po-icon-input-disabled' : 'po-icon-input' }}"
              [p-element-ref]="inputEl"
              [p-size]="size"
              (p-change-event)="clear($event)"
            >
            </po-clean>
          }
        </div>
      </div>
      @if ((!this.label && poHelperComponent()) || (!this.label && poHelper.hideAdditionalHelp)) {
        <po-helper
          #helperEl
          class="po-field-helper-button"
          [p-size]="size"
          [p-helper]="poHelper.helperSettings"
          [p-disabled]="isDisabled"
          [p-append-in-body]="appendBox"
        >
        </po-helper>
      }
    </div>
  </div>

  @if (!readonly) {
    <po-field-container-bottom
      [p-append-in-body]="appendBox"
      [p-help]="help"
      [p-disabled]="isDisabled"
      [p-error-pattern]="getErrorPattern()"
      [p-error-limit]="errorLimit"
      [p-size]="size"
    ></po-field-container-bottom>
  }
</po-field-container>
`
    }]
  }], () => [], null);
})();
var PoInputComponent = class _PoInputComponent extends PoInputGeneric {
  inp;
  id = `po-input[${uuid()}]`;
  /* istanbul ignore next */
  constructor() {
    const el = inject(ElementRef);
    const cd = inject(ChangeDetectorRef);
    super(el, cd);
  }
  ngOnChanges(changes) {
    if (changes.label) {
      this.displayAdditionalHelp = false;
    }
  }
  extraValidation(c) {
    return null;
  }
  static ɵfac = function PoInputComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoInputComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoInputComponent,
    selectors: [["po-input"]],
    viewQuery: function PoInputComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c49, 7);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.inp = _t.first);
      }
    },
    standalone: false,
    features: [ɵɵProvidersFeature([{
      provide: NG_VALUE_ACCESSOR,
      useExisting: forwardRef(() => _PoInputComponent),
      multi: true
    }, {
      provide: NG_VALIDATORS,
      useExisting: forwardRef(() => _PoInputComponent),
      multi: true
    }]), ɵɵInheritDefinitionFeature, ɵɵNgOnChangesFeature],
    decls: 14,
    vars: 36,
    consts: [["fieldContainer", ""], ["inp", ""], ["helperEl", ""], [3, "p-disabled", "p-id", "p-label", "p-optional", "p-required", "p-show-required", "p-helper", "p-show-helper", "p-size", "p-text-wrap", "p-compact-label"], [1, "po-field-container-content"], [1, "po-field-container-content-helper-wrapper"], [1, "po-field-container-input"], [1, "po-field-icon-container-left"], [1, "po-input", 3, "blur", "click", "focus", "input", "keydown", "autocomplete", "disabled", "id", "placeholder", "readonly", "required", "type"], [1, "po-field-icon-container-right"], [3, "p-size"], [3, "class", "p-element-ref", "p-size"], [1, "po-field-helper-button", 3, "p-size", "p-helper", "p-disabled", "p-append-in-body"], [3, "p-append-in-body", "p-help", "p-disabled", "p-error-pattern", "p-error-limit", "p-size"], [3, "p-icon"], [3, "p-change-event", "p-element-ref", "p-size"]],
    template: function PoInputComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵdeclareLet(0);
        ɵɵelementStart(1, "po-field-container", 3, 0)(3, "div", 4)(4, "div", 5)(5, "div", 6);
        ɵɵconditionalCreate(6, PoInputComponent_Conditional_6_Template, 2, 8, "div", 7);
        ɵɵelementStart(7, "input", 8, 1);
        ɵɵlistener("blur", function PoInputComponent_Template_input_blur_7_listener($event) {
          return ctx.eventOnBlur($event);
        })("click", function PoInputComponent_Template_input_click_7_listener($event) {
          return ctx.eventOnClick($event);
        })("focus", function PoInputComponent_Template_input_focus_7_listener($event) {
          return ctx.eventOnFocus($event);
        })("input", function PoInputComponent_Template_input_input_7_listener($event) {
          return ctx.eventOnInput($event);
        })("keydown", function PoInputComponent_Template_input_keydown_7_listener($event) {
          return ctx.onKeyDown($event);
        });
        ɵɵelementEnd();
        ɵɵelementStart(9, "div", 9);
        ɵɵconditionalCreate(10, PoInputComponent_Conditional_10_Template, 1, 1, "po-loading-icon", 10);
        ɵɵconditionalCreate(11, PoInputComponent_Conditional_11_Template, 1, 4, "po-clean", 11);
        ɵɵelementEnd()();
        ɵɵconditionalCreate(12, PoInputComponent_Conditional_12_Template, 2, 4, "po-helper", 12);
        ɵɵelementEnd()();
        ɵɵconditionalCreate(13, PoInputComponent_Conditional_13_Template, 1, 6, "po-field-container-bottom", 13);
        ɵɵelementEnd();
      }
      if (rf & 2) {
        const poHelper_r4 = ɵɵstoreLet(ctx.setHelper(ctx.label, ctx.additionalHelpTooltip));
        ɵɵadvance();
        ɵɵproperty("p-disabled", ctx.isDisabled)("p-id", ctx.id)("p-label", ctx.label)("p-optional", ctx.optional)("p-required", ctx.required)("p-show-required", ctx.showRequired)("p-helper", ctx.label ? poHelper_r4.helperSettings : void 0)("p-show-helper", ctx.label ? ctx.displayAdditionalHelp : void 0)("p-size", ctx.size)("p-text-wrap", ctx.labelTextWrap())("p-compact-label", ctx.compactLabel());
        ɵɵadvance(5);
        ɵɵconditional(ctx.icon ? 6 : -1);
        ɵɵadvance();
        ɵɵclassProp("po-input-icon-left", ctx.icon)("po-input-icon-left-aa", ctx.icon && ctx.size === "small")("po-input-icon-right", ctx.clean || ctx.loading)("po-input-icon-right-aa", (ctx.clean || ctx.loading) && ctx.size === "small");
        ɵɵproperty("autocomplete", ctx.autocomplete)("disabled", ctx.isDisabled)("id", ctx.id)("placeholder", ctx.isDisabled ? "" : ctx.placeholder)("readonly", ctx.readonly)("required", ctx.required)("type", ctx.type);
        ɵɵattribute("name", ctx.name)("data-focused-element", !ctx.isDisabled)("data-inactive-component", ctx.isDisabled || ctx.readonly)("p-size", ctx.size);
        ɵɵadvance(3);
        ɵɵconditional(ctx.loading ? 10 : -1);
        ɵɵadvance();
        ɵɵconditional(!ctx.loading && ctx.clean && !ctx.isDisabled && !ctx.readonly ? 11 : -1);
        ɵɵadvance();
        ɵɵconditional(!ctx.label && ctx.poHelperComponent() || !ctx.label && poHelper_r4.hideAdditionalHelp ? 12 : -1);
        ɵɵadvance();
        ɵɵconditional(!ctx.readonly ? 13 : -1);
      }
    },
    dependencies: [PoCleanComponent, PoFieldContainerBottomComponent, PoFieldContainerComponent, PoHelperComponent, PoIconComponent, PoLoadingIconComponent],
    encapsulation: 2,
    changeDetection: 0
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoInputComponent, [{
    type: Component,
    args: [{
      selector: "po-input",
      providers: [{
        provide: NG_VALUE_ACCESSOR,
        useExisting: forwardRef(() => PoInputComponent),
        multi: true
      }, {
        provide: NG_VALIDATORS,
        useExisting: forwardRef(() => PoInputComponent),
        multi: true
      }],
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: `@let poHelper = setHelper(label, additionalHelpTooltip);
<po-field-container
  #fieldContainer
  [p-disabled]="isDisabled"
  [p-id]="id"
  [p-label]="label"
  [p-optional]="optional"
  [p-required]="required"
  [p-show-required]="showRequired"
  [p-helper]="label ? poHelper.helperSettings : undefined"
  [p-show-helper]="label ? displayAdditionalHelp : undefined"
  [p-size]="size"
  [p-text-wrap]="labelTextWrap()"
  [p-compact-label]="compactLabel()"
>
  <div class="po-field-container-content">
    <div class="po-field-container-content-helper-wrapper">
      <div class="po-field-container-input">
        @if (icon) {
          <div class="po-field-icon-container-left">
            <po-icon
              class="po-field-icon {{ disabled ? 'po-icon-input-disabled' : 'po-icon-input' }}"
              [class.po-field-icon-disabled]="isDisabled"
              [class.po-field-icon-aa]="size === 'small'"
              [p-icon]="icon"
            ></po-icon>
          </div>
        }

        <input
          #inp
          class="po-input"
          [attr.name]="name"
          [attr.data-focused-element]="!isDisabled"
          [attr.data-inactive-component]="isDisabled || readonly"
          [attr.p-size]="size"
          [autocomplete]="autocomplete"
          [class.po-input-icon-left]="icon"
          [class.po-input-icon-left-aa]="icon && size === 'small'"
          [class.po-input-icon-right]="clean || loading"
          [class.po-input-icon-right-aa]="(clean || loading) && size === 'small'"
          [disabled]="isDisabled"
          [id]="id"
          [placeholder]="isDisabled ? '' : placeholder"
          [readonly]="readonly"
          [required]="required"
          [type]="type"
          (blur)="eventOnBlur($event)"
          (click)="eventOnClick($event)"
          (focus)="eventOnFocus($event)"
          (input)="eventOnInput($event)"
          (keydown)="onKeyDown($event)"
        />
        <div class="po-field-icon-container-right">
          @if (loading) {
            <po-loading-icon [p-size]="mapSizeToIcon(size)"></po-loading-icon>
          }

          @if (!loading && clean && !isDisabled && !readonly) {
            <po-clean
              class="{{ disabled ? 'po-icon-input-disabled' : 'po-icon-input' }}"
              [p-element-ref]="inputEl"
              [p-size]="size"
              (p-change-event)="clear($event)"
            >
            </po-clean>
          }
        </div>
      </div>
      @if ((!this.label && poHelperComponent()) || (!this.label && poHelper.hideAdditionalHelp)) {
        <po-helper
          #helperEl
          class="po-field-helper-button"
          [p-size]="size"
          [p-helper]="poHelper.helperSettings"
          [p-disabled]="isDisabled"
          [p-append-in-body]="appendBox"
        >
        </po-helper>
      }
    </div>
  </div>

  @if (!readonly) {
    <po-field-container-bottom
      [p-append-in-body]="appendBox"
      [p-help]="help"
      [p-disabled]="isDisabled"
      [p-error-pattern]="getErrorPattern()"
      [p-error-limit]="errorLimit"
      [p-size]="size"
    ></po-field-container-bottom>
  }
</po-field-container>
`
    }]
  }], () => [], {
    inp: [{
      type: ViewChild,
      args: ["inp", {
        static: true
      }]
    }]
  });
})();
var providers$3 = [{
  provide: NG_VALUE_ACCESSOR,
  // eslint-disable-next-line
  useExisting: forwardRef(() => PoLoginComponent),
  multi: true
}, {
  provide: NG_VALIDATORS,
  // eslint-disable-next-line
  useExisting: forwardRef(() => PoLoginComponent),
  multi: true
}];
var PoLoginComponent = class _PoLoginComponent extends PoInputGeneric {
  id = `po-login[${uuid()}]`;
  type = "text";
  _noAutocompleteLogin = true;
  set noAutocomplete(value) {
    this._noAutocompleteLogin = convertToBoolean(value);
  }
  get noAutocomplete() {
    return this._noAutocompleteLogin;
  }
  get autocomplete() {
    return this.noAutocomplete ? "off" : "on";
  }
  /* istanbul ignore next */
  constructor() {
    const el = inject(ElementRef);
    const cd = inject(ChangeDetectorRef);
    super(el, cd);
  }
  ngOnChanges(changes) {
    if (changes.label) {
      this.displayAdditionalHelp = false;
    }
  }
  extraValidation(c) {
    return null;
  }
  static ɵfac = function PoLoginComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoLoginComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoLoginComponent,
    selectors: [["po-login"]],
    inputs: {
      noAutocomplete: [0, "p-no-autocomplete", "noAutocomplete"]
    },
    standalone: false,
    features: [ɵɵProvidersFeature(providers$3), ɵɵInheritDefinitionFeature, ɵɵNgOnChangesFeature],
    decls: 14,
    vars: 38,
    consts: [["inp", ""], ["helperEl", ""], [3, "p-disabled", "p-id", "p-label", "p-optional", "p-required", "p-show-required", "p-helper", "p-show-helper", "p-size", "p-text-wrap", "p-compact-label"], [1, "po-field-container-content"], [1, "po-field-container-content-helper-wrapper"], [1, "po-field-container-input"], [1, "po-field-icon-container-left"], ["p-icon", "ICON_USER"], [1, "po-input", "po-input-icon-left", 3, "blur", "click", "focus", "input", "keydown", "autocomplete", "disabled", "id", "placeholder", "readonly", "required", "type"], [1, "po-field-icon-container-right"], [3, "p-size"], [1, "po-icon-input", 3, "p-element-ref", "p-size"], [1, "po-field-helper-button", 3, "p-size", "p-helper", "p-disabled", "p-append-in-body"], [3, "p-help", "p-disabled", "p-error-pattern", "p-error-limit", "p-size"], [1, "po-icon-input", 3, "p-change-event", "p-element-ref", "p-size"]],
    template: function PoLoginComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵdeclareLet(0);
        ɵɵelementStart(1, "po-field-container", 2)(2, "div", 3)(3, "div", 4)(4, "div", 5)(5, "div", 6);
        ɵɵelement(6, "po-icon", 7);
        ɵɵelementEnd();
        ɵɵelementStart(7, "input", 8, 0);
        ɵɵlistener("blur", function PoLoginComponent_Template_input_blur_7_listener($event) {
          return ctx.eventOnBlur($event);
        })("click", function PoLoginComponent_Template_input_click_7_listener($event) {
          return ctx.eventOnClick($event);
        })("focus", function PoLoginComponent_Template_input_focus_7_listener($event) {
          return ctx.eventOnFocus($event);
        })("input", function PoLoginComponent_Template_input_input_7_listener($event) {
          return ctx.eventOnInput($event);
        })("keydown", function PoLoginComponent_Template_input_keydown_7_listener($event) {
          return ctx.onKeyDown($event);
        });
        ɵɵelementEnd();
        ɵɵelementStart(9, "div", 9);
        ɵɵconditionalCreate(10, PoLoginComponent_Conditional_10_Template, 1, 1, "po-loading-icon", 10);
        ɵɵconditionalCreate(11, PoLoginComponent_Conditional_11_Template, 1, 2, "po-clean", 11);
        ɵɵelementEnd()();
        ɵɵconditionalCreate(12, PoLoginComponent_Conditional_12_Template, 2, 4, "po-helper", 12);
        ɵɵelementEnd()();
        ɵɵconditionalCreate(13, PoLoginComponent_Conditional_13_Template, 1, 5, "po-field-container-bottom", 13);
        ɵɵelementEnd();
      }
      if (rf & 2) {
        const poHelper_r4 = ɵɵstoreLet(ctx.setHelper(ctx.label, ctx.additionalHelpTooltip));
        ɵɵadvance();
        ɵɵproperty("p-disabled", ctx.isDisabled)("p-id", ctx.id)("p-label", ctx.label)("p-optional", ctx.optional)("p-required", ctx.required)("p-show-required", ctx.showRequired)("p-helper", ctx.label ? poHelper_r4.helperSettings : void 0)("p-show-helper", ctx.label ? ctx.displayAdditionalHelp : void 0)("p-size", ctx.size)("p-text-wrap", ctx.labelTextWrap())("p-compact-label", ctx.compactLabel());
        ɵɵadvance(5);
        ɵɵclassMap(ɵɵinterpolate1("po-field-icon ", ctx.disabled ? "po-icon-input-disabled" : "po-icon-input"));
        ɵɵclassProp("po-field-icon-disabled", ctx.isDisabled)("po-field-icon-aa", ctx.size === "small");
        ɵɵadvance();
        ɵɵclassProp("po-input-icon-left-aa", ctx.size === "small")("po-input-icon-right", ctx.clean || ctx.loading)("po-input-icon-right-aa", (ctx.clean || ctx.loading) && ctx.size === "small");
        ɵɵproperty("autocomplete", ctx.autocomplete)("disabled", ctx.isDisabled)("id", ctx.id)("placeholder", ctx.isDisabled ? "" : ctx.placeholder)("readonly", ctx.readonly)("required", ctx.required)("type", ctx.type);
        ɵɵattribute("name", ctx.name)("p-size", ctx.size);
        ɵɵadvance(3);
        ɵɵconditional(ctx.loading ? 10 : -1);
        ɵɵadvance();
        ɵɵconditional(!ctx.loading && ctx.clean && !ctx.isDisabled && !ctx.readonly ? 11 : -1);
        ɵɵadvance();
        ɵɵconditional(!ctx.label && ctx.poHelperComponent() || !ctx.label && poHelper_r4.hideAdditionalHelp ? 12 : -1);
        ɵɵadvance();
        ɵɵconditional(!ctx.readonly ? 13 : -1);
      }
    },
    dependencies: [PoCleanComponent, PoFieldContainerBottomComponent, PoFieldContainerComponent, PoHelperComponent, PoIconComponent, PoLoadingIconComponent],
    encapsulation: 2,
    changeDetection: 0
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoLoginComponent, [{
    type: Component,
    args: [{
      selector: "po-login",
      changeDetection: ChangeDetectionStrategy.OnPush,
      providers: providers$3,
      standalone: false,
      template: `@let poHelper = setHelper(label, additionalHelpTooltip);
<po-field-container
  [p-disabled]="isDisabled"
  [p-id]="id"
  [p-label]="label"
  [p-optional]="optional"
  [p-required]="required"
  [p-show-required]="showRequired"
  [p-helper]="label ? poHelper.helperSettings : undefined"
  [p-show-helper]="label ? displayAdditionalHelp : undefined"
  [p-size]="size"
  [p-text-wrap]="labelTextWrap()"
  [p-compact-label]="compactLabel()"
>
  <div class="po-field-container-content">
    <div class="po-field-container-content-helper-wrapper">
      <div class="po-field-container-input">
        <div class="po-field-icon-container-left">
          <po-icon
            p-icon="ICON_USER"
            class="po-field-icon {{ disabled ? 'po-icon-input-disabled' : 'po-icon-input' }}"
            [class.po-field-icon-disabled]="isDisabled"
            [class.po-field-icon-aa]="size === 'small'"
          ></po-icon>
        </div>

        <input
          #inp
          class="po-input po-input-icon-left"
          [class.po-input-icon-left-aa]="size === 'small'"
          [attr.name]="name"
          [attr.p-size]="size"
          [autocomplete]="autocomplete"
          [class.po-input-icon-right]="clean || loading"
          [class.po-input-icon-right-aa]="(clean || loading) && size === 'small'"
          [disabled]="isDisabled"
          [id]="id"
          [placeholder]="isDisabled ? '' : placeholder"
          [readonly]="readonly"
          [required]="required"
          [type]="type"
          (blur)="eventOnBlur($event)"
          (click)="eventOnClick($event)"
          (focus)="eventOnFocus($event)"
          (input)="eventOnInput($event)"
          (keydown)="onKeyDown($event)"
        />

        <div class="po-field-icon-container-right">
          @if (loading) {
            <po-loading-icon [p-size]="mapSizeToIcon(size)"></po-loading-icon>
          }

          @if (!loading && clean && !isDisabled && !readonly) {
            <po-clean class="po-icon-input" [p-element-ref]="inputEl" [p-size]="size" (p-change-event)="clear($event)">
            </po-clean>
          }
        </div>
      </div>

      @if ((!this.label && poHelperComponent()) || (!this.label && poHelper.hideAdditionalHelp)) {
        <po-helper
          #helperEl
          class="po-field-helper-button"
          [p-size]="size"
          [p-helper]="poHelper.helperSettings"
          [p-disabled]="isDisabled"
          [p-append-in-body]="appendBox"
        >
        </po-helper>
      }
    </div>
  </div>

  @if (!readonly) {
    <po-field-container-bottom
      [p-help]="help"
      [p-disabled]="isDisabled"
      [p-error-pattern]="getErrorPattern()"
      [p-error-limit]="errorLimit"
      [p-size]="size"
    >
    </po-field-container-bottom>
  }
</po-field-container>
`
    }]
  }], () => [], {
    noAutocomplete: [{
      type: Input,
      args: ["p-no-autocomplete"]
    }]
  });
})();
var poLookupLiteralsDefault$1 = {
  en: {
    modalPrimaryActionLabel: "Select",
    modalSecondaryActionLabel: "Cancel",
    modalPlaceholder: "Search",
    modalTitle: "Select a record",
    modalTableNoColumns: poTableLiteralsDefault.en.noColumns,
    modalTableNoData: poTableLiteralsDefault.en.noData,
    modalTableLoadingData: poTableLiteralsDefault.en.loadingData,
    modalTableLoadMoreData: poTableLiteralsDefault.en.loadMoreData,
    modalAdvancedSearch: "Advanced search",
    modalAdvancedSearchTitle: "Advanced search",
    modalAdvancedSearchPrimaryActionLabel: "Filter",
    modalAdvancedSearchSecondaryActionLabel: "Return",
    modalDisclaimerGroupTitle: "Presenting results filtered by:"
  },
  es: {
    modalPrimaryActionLabel: "Seleccionar",
    modalSecondaryActionLabel: "Cancelar",
    modalPlaceholder: "Buscar",
    modalTitle: "Seleccione un registro",
    modalTableNoColumns: poTableLiteralsDefault.es.noColumns,
    modalTableNoData: poTableLiteralsDefault.es.noData,
    modalTableLoadingData: poTableLiteralsDefault.es.loadingData,
    modalTableLoadMoreData: poTableLiteralsDefault.es.loadMoreData,
    modalAdvancedSearch: "Búsqueda Avanzada",
    modalAdvancedSearchTitle: "Búsqueda Avanzada",
    modalAdvancedSearchPrimaryActionLabel: "Filtrar",
    modalAdvancedSearchSecondaryActionLabel: "Vuelve",
    modalDisclaimerGroupTitle: "Presentar resultados filtrados por:"
  },
  pt: {
    modalPrimaryActionLabel: "Selecionar",
    modalSecondaryActionLabel: "Cancelar",
    modalPlaceholder: "Pesquisar",
    modalTitle: "Selecione um registro",
    modalTableNoColumns: poTableLiteralsDefault.pt.noColumns,
    modalTableNoData: poTableLiteralsDefault.pt.noData,
    modalTableLoadingData: poTableLiteralsDefault.pt.loadingData,
    modalTableLoadMoreData: poTableLiteralsDefault.pt.loadMoreData,
    modalAdvancedSearch: "Busca avançada",
    modalAdvancedSearchTitle: "Busca Avançada",
    modalAdvancedSearchPrimaryActionLabel: "Filtrar",
    modalAdvancedSearchSecondaryActionLabel: "Voltar",
    modalDisclaimerGroupTitle: "Apresentando resultados filtrados por:"
  },
  ru: {
    modalPrimaryActionLabel: "Выбрать",
    modalSecondaryActionLabel: "Отменить",
    modalPlaceholder: "Поиск",
    modalTitle: "Выберите запись",
    modalTableNoColumns: poTableLiteralsDefault.ru.noColumns,
    modalTableNoData: poTableLiteralsDefault.ru.noData,
    modalTableLoadingData: poTableLiteralsDefault.ru.loadingData,
    modalTableLoadMoreData: poTableLiteralsDefault.ru.loadMoreData,
    modalAdvancedSearch: "Расширенный поиск",
    modalAdvancedSearchTitle: "Расширенный поиск",
    modalAdvancedSearchPrimaryActionLabel: "Фильтр",
    modalAdvancedSearchSecondaryActionLabel: "Назад",
    modalDisclaimerGroupTitle: "Представленные результаты отфильтрованы по:"
  }
};
var PoLookupModalBaseComponent = class _PoLookupModalBaseComponent {
  changeDetector;
  poModal;
  poTable;
  /**
   * Objeto com os campos que serão criados no busca avançada.
   *
   * > Caso não seja passado um objeto ou então ele esteja em branco o link de busca avançada ficará escondido.
   *
   * Exemplo de URL com busca avançada: http://localhost:3000/v1/heroes?filter=&page=1&pageSize=10`&name=Tony%20Stark&nickname=Homem%20de%20Ferro&email=irnman@marvel.com`
   *
   * Caso algum parâmetro seja uma lista, a concatenação é feita utilizando virgula.
   * Exemplo: http://localhost:3000/v1/heroes?filter=&page=1&pageSize=10`&name=Tony%20Stark,Peter%20Parker,Gohan`
   *
   */
  advancedFilters;
  /**
   * Lista das colunas da tabela.
   * Essa propriedade deve receber um array de objetos que implementam a interface PoLookupColumn.
   */
  columns;
  /** Lista de itens da tabela. */
  items;
  /** Classe de serviço com a implementação do cliente. */
  filterService;
  /** Classe de serviço com a implementação do cliente. */
  filterParams;
  /** Se verdadeiro, esconde o gerenciador de tarefas, responsável pela definição de quais colunas serão exibidas. */
  hideColumnsManager = false;
  /** Se verdadeiro, ativa a funcionalidade de scroll infinito para a tabela exibida no retorno da consulta. */
  infiniteScroll = false;
  /** Se verdadeiro, ativa a funcionalidade de multipla seleção. */
  multiple = false;
  /** Evento utilizado ao selecionar um registro da tabela. */
  model = new EventEmitter();
  /** Classe de serviço com items selecionados */
  selectedItems;
  /** Indica a coluna que será utilizada como descrição do campo e como filtro dentro da janela. */
  fieldLabel;
  /**
   * @description
   *
   * Indica a coluna que será utilizada como valor do campo.
   *
   * > Atenção: Caso não seja passada ou tenha o conteúdo incorreto, não irá atualizar o model do formulário.
   */
  fieldValue;
  /**
   * Define o espaçamento interno das células, impactando diretamente na altura das linhas do table dentro do modal. Os
   * valores permitidos são definidos pelo enum **PoTableColumnSpacing**.
   */
  spacing;
  /** Define o tamanho do componente. */
  size;
  /**
   * Habilita ou desabilita a quebra automática de texto. Quando ativada, o texto que excede
   * o espaço disponível é transferido para a próxima linha em pontos apropriados para uma
   * leitura clara.
   *
   * > Incompatível com `virtual-scroll`, que requer altura fixa nas linhas.
   *
   */
  textWrap = false;
  /**
   * @optional
   *
   * @description
   *
   * Habilita o `virtual-scroll` na tabela para melhorar a performance com grandes volumes de dados.
   * A altura da tabela já é pré-definida, portanto o `virtual-scroll` será ativado automaticamente.
   *
   * > Incompatível com `p-text-wrap` e `master-detail`, pois o `virtual-scroll` exige altura fixa nas linhas.
   *
   * @default `true`
   */
  virtualScroll = true;
  /**
   * @optional
   *
   * @description
   * Evento disparado ao fechar o popover do gerenciador de colunas após alterar as colunas visíveis.
   *
   * O componente envia como parâmetro um array de string com as colunas visíveis atualizadas.
   * Por exemplo: ["idCard", "name", "hireStatus", "age"].
   */
  changeVisibleColumns = new EventEmitter();
  /**
   * @optional
   *
   * @description
   * Evento disparado ao clicar no botão de restaurar padrão no gerenciador de colunas.
   *
   * O componente envia como parâmetro um array de string com as colunas configuradas inicialmente.
   * Por exemplo: ["idCard", "name", "hireStatus", "age"].
   */
  columnRestoreManager = new EventEmitter();
  hasNext = true;
  isLoading = false;
  page = 1;
  pageSize = 10;
  searchValue = "";
  appliedSearchValue = "";
  tableLiterals;
  // Propriedade da modal de busca avançada:
  advancedFilterModalTitle = "";
  dynamicFormValue = {};
  disclaimer;
  disclaimerGroup;
  isAdvancedFilter = false;
  primaryActionAdvancedFilter;
  secondaryActionAdvancedFilter;
  selecteds = [];
  sort;
  filterSubscription;
  searchSubscription;
  showMoreSubscription;
  disclaimerLabel;
  _literals;
  _title;
  language = poLocaleDefault;
  // eslint-disable-next-line @typescript-eslint/member-ordering
  primaryAction = {
    action: () => {
      let selectedsItems = [];
      if (!this.multiple) {
        this.items.forEach((element) => {
          if (element["$selected"]) {
            selectedsItems.push(element);
          }
        });
      } else {
        selectedsItems = this.selecteds;
      }
      this.model.emit(selectedsItems);
      this.poModal.close();
    },
    label: this.literals.modalPrimaryActionLabel
  };
  // eslint-disable-next-line @typescript-eslint/member-ordering
  secondaryAction = {
    action: () => {
      this.model.emit(null);
      this.poModal.close();
    },
    label: this.literals.modalSecondaryActionLabel
  };
  /** Objeto com as literais usadas no `po-lookup-modal`. */
  set literals(value) {
    if (value instanceof Object && !(value instanceof Array)) {
      this._literals = __spreadValues(__spreadValues(__spreadValues({}, poLookupLiteralsDefault$1[poLocaleDefault]), poLookupLiteralsDefault$1[this.language]), value);
      if (value.modalTitle) {
        this.title = this.literals.modalTitle;
      }
    } else {
      this._literals = poLookupLiteralsDefault$1[this.language];
    }
    this.primaryAction.label = this.literals.modalPrimaryActionLabel;
    this.secondaryAction.label = this.literals.modalSecondaryActionLabel;
    this.setTableLiterals();
  }
  get literals() {
    return this._literals || poLookupLiteralsDefault$1[this.language];
  }
  /** Título da modal. */
  set title(value) {
    this._title = isTypeof(value, "string") ? value : this.literals.modalTitle;
  }
  get title() {
    return this._title;
  }
  constructor(languageService, changeDetector) {
    this.changeDetector = changeDetector;
    this.language = languageService.getShortLanguage();
  }
  ngOnDestroy() {
    if (this.filterSubscription) {
      this.filterSubscription.unsubscribe();
    }
    if (this.searchSubscription) {
      this.searchSubscription.unsubscribe();
    }
    if (this.showMoreSubscription) {
      this.showMoreSubscription.unsubscribe();
    }
  }
  ngOnInit() {
    this.setAdvancedFilterModalProperties();
    this.initializeData();
    this.setTableLiterals();
  }
  createDisclaimer() {
    this.disclaimerGroup.disclaimers = [];
    this.searchValue = "";
    this.appliedSearchValue = "";
    for (const [key2, value] of Object.entries(this.dynamicFormValue)) {
      this.addDisclaimer(value, key2);
    }
    if (!Object.values(this.dynamicFormValue).some((v) => v !== null && typeof v !== "undefined")) {
      this.initializeData();
    }
  }
  addDisclaimer(value, property) {
    this.disclaimerLabel = "";
    const fieldFilter = this.advancedFilters.find((filter3) => filter3.property === property);
    this.disclaimer = {
      property
    };
    this.disclaimer.value = value;
    const labelProperty = fieldFilter.label || capitalizeFirstLetter(fieldFilter.property);
    if (fieldFilter.type === "currency" && value) {
      this.formatValueToCurrency(fieldFilter, value);
    }
    if (fieldFilter.type === "boolean" && (value === true || value === false)) {
      this.formatValueToBoolean(fieldFilter, value);
    }
    if (fieldFilter.options && value) {
      this.applyDisclaimerLabelValue(fieldFilter, value);
    }
    if (!this.disclaimerLabel) {
      this.disclaimerLabel = this.disclaimer.value;
    }
    this.disclaimer.label = `${labelProperty}: ${this.disclaimerLabel}`;
    this.disclaimerGroup.disclaimers = [...this.disclaimerGroup.disclaimers, this.disclaimer];
  }
  onChangeDisclaimerGroup() {
    if (!this.appliedSearchValue) {
      this.isLoading = true;
      this.searchValue = "";
      this.appliedSearchValue = "";
      this.searchFilteredItems();
    }
  }
  search() {
    this.page = 1;
    this.appliedSearchValue = this.searchValue;
    if (this.appliedSearchValue) {
      this.isLoading = true;
      this.disclaimerGroup.disclaimers = [];
      this.searchFilteredItems();
    } else {
      this.initializeData();
    }
  }
  searchFilteredItems() {
    this.searchSubscription = this.getFilteredItems(this.appliedSearchValue).pipe(catchError((error2) => {
      this.setLookupResponseProperties();
      return throwError(error2);
    })).subscribe((data) => this.setLookupResponseProperties(data), () => {
    });
  }
  showMoreEvent() {
    this.page++;
    this.isLoading = true;
    if (this.searchValue !== this.appliedSearchValue) {
      this.searchValue = this.appliedSearchValue;
    }
    this.showMoreSubscription = this.getFilteredItems(this.appliedSearchValue).pipe(catchError((error2) => {
      this.hasNext = false;
      this.isLoading = false;
      return throwError(error2);
    })).subscribe((data) => {
      this.items = [...this.items, ...data.items];
      this.hasNext = data.hasNext;
      this.isLoading = false;
      this.changeDetector.detectChanges();
      this.setSelectedItems();
    }, () => {
    });
  }
  //Método responsável por selecionar as linhas quando abre o modal.
  setSelectedItems() {
    this.selecteds.forEach((selectedItem) => {
      if (this.multiple) {
        this.poTable.selectRowItem((item) => item[this.fieldValue] === selectedItem.value);
      } else {
        this.poTable.selectRowItem((item) => item[this.fieldValue] === selectedItem[this.fieldValue]);
      }
    });
  }
  //Método responsável por criar os disclaimers quando abre o modal.
  setDisclaimersItems() {
    if (this.selectedItems && Array.isArray(this.selectedItems) && this.selectedItems.length > 0) {
      this.selecteds = [...this.selectedItems];
    } else if (this.selectedItems && !Array.isArray(this.selectedItems)) {
      this.selecteds = [this.selectedItems];
    } else {
      this.selecteds = [];
    }
  }
  applyDisclaimerLabelValue(field, filterValue) {
    const values = Array.isArray(filterValue) ? filterValue : [filterValue];
    const labels = values.map((optionValue) => {
      const findOption = field.options.find((option) => option.value === optionValue);
      return findOption.label;
    });
    if (labels.join()) {
      this.disclaimerLabel = labels.join(", ");
    }
  }
  formatValueToCurrency(field, filterValue) {
    const currencyLabel = new Intl.NumberFormat(field.locale ? field.locale : this.language, {
      minimumFractionDigits: 2
    }).format(filterValue);
    this.disclaimerLabel = currencyLabel;
  }
  formatValueToBoolean(field, filterValue) {
    let labelBoolean;
    if (filterValue) {
      labelBoolean = field.booleanTrue ? field.booleanTrue : filterValue;
    } else {
      labelBoolean = field.booleanFalse ? field.booleanFalse : filterValue;
    }
    this.disclaimerLabel = `${labelBoolean}`;
  }
  setAdvancedFilterModalProperties() {
    this.advancedFilterModalTitle = this.literals.modalAdvancedSearchTitle;
    this.disclaimerGroup = {
      title: this.literals.modalDisclaimerGroupTitle,
      disclaimers: []
    };
    this.primaryActionAdvancedFilter = {
      action: () => {
        this.destroyDynamicForm();
        this.isAdvancedFilter = false;
        this.page = 1;
        this.createDisclaimer();
      },
      label: this.literals.modalAdvancedSearchPrimaryActionLabel
    };
    this.secondaryActionAdvancedFilter = {
      action: () => {
        this.destroyDynamicForm();
        this.isAdvancedFilter = false;
      },
      label: this.literals.modalAdvancedSearchSecondaryActionLabel
    };
  }
  getAdvancedFilters(advancedParams) {
    if (advancedParams && advancedParams.length > 0) {
      const filters = {};
      let validatedAdvacendFilters;
      advancedParams.forEach((filter3) => {
        filters[filter3.property] = filter3.value instanceof Array ? filter3.value.join() : filter3.value;
        validatedAdvacendFilters = __spreadValues(__spreadValues({}, validatedAdvacendFilters), filters);
      });
      return validatedAdvacendFilters;
    }
    return void 0;
  }
  getFilteredItems(filter3) {
    const filteredParams = this.getFilteredParams(filter3);
    return this.filterService.getFilteredItems(filteredParams);
  }
  getFilteredParams(filter3) {
    const {
      page,
      pageSize,
      filterParams,
      sort: sort3
    } = this;
    const filteredParams = {};
    const order = this.getOrderParam(sort3);
    const advancedFilters = this.getAdvancedFilters(this.disclaimerGroup.disclaimers);
    const params = {
      filter: filter3,
      page,
      pageSize,
      order,
      filterParams,
      advancedFilters
    };
    for (const key2 in params) {
      if (params.hasOwnProperty(key2) && params[key2] !== void 0) {
        filteredParams[key2] = params[key2];
      }
    }
    return filteredParams;
  }
  getOrderParam(sort3 = {
    type: void 0
  }) {
    const {
      column,
      type
    } = sort3;
    if (!column) {
      return;
    }
    if (type === PoTableColumnSortType.Descending) {
      return `-${column.property}`;
    }
    return `${column.property}`;
  }
  initializeData() {
    this.isLoading = true;
    this.filterSubscription = this.getFilteredItems("").subscribe((data) => {
      this.setLookupResponseProperties(data);
    });
  }
  setLookupResponseProperties(data) {
    this.items = data?.items ?? [];
    this.hasNext = data?.hasNext ?? false;
    this.isLoading = false;
    this.changeDetector.detectChanges();
    this.setDisclaimersItems();
    this.setSelectedItems();
  }
  setTableLiterals() {
    this.tableLiterals = {
      "noColumns": this.literals.modalTableNoColumns,
      "noData": this.literals.modalTableNoData,
      "loadingData": this.literals.modalTableLoadingData,
      "loadMoreData": this.literals.modalTableLoadMoreData
    };
  }
  static ɵfac = function PoLookupModalBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoLookupModalBaseComponent)(ɵɵdirectiveInject(PoLanguageService), ɵɵdirectiveInject(ChangeDetectorRef));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoLookupModalBaseComponent,
    viewQuery: function PoLookupModalBaseComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(PoModalComponent, 7)(PoTableComponent, 7);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.poModal = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.poTable = _t.first);
      }
    },
    hostVars: 1,
    hostBindings: function PoLookupModalBaseComponent_HostBindings(rf, ctx) {
      if (rf & 2) {
        ɵɵattribute("p-size", ctx.size);
      }
    },
    inputs: {
      advancedFilters: [0, "p-advanced-filters", "advancedFilters"],
      columns: [0, "p-columns", "columns"],
      items: [0, "p-items", "items"],
      filterService: [0, "p-filter-service", "filterService"],
      filterParams: [0, "p-filter-params", "filterParams"],
      hideColumnsManager: [2, "p-hide-columns-manager", "hideColumnsManager", convertToBoolean],
      infiniteScroll: [2, "p-infinite-scroll", "infiniteScroll", convertToBoolean],
      multiple: [2, "p-multiple", "multiple", convertToBoolean],
      selectedItems: [0, "p-selected-items", "selectedItems"],
      fieldLabel: [0, "p-field-label", "fieldLabel"],
      fieldValue: [0, "p-field-value", "fieldValue"],
      spacing: [0, "p-spacing", "spacing"],
      size: [0, "p-size", "size"],
      textWrap: [2, "p-text-wrap", "textWrap", convertToBoolean],
      virtualScroll: [2, "p-virtual-scroll", "virtualScroll", convertToBoolean],
      literals: [0, "p-literals", "literals"],
      title: [0, "p-title", "title"]
    },
    outputs: {
      model: "p-change-model",
      changeVisibleColumns: "p-change-visible-columns",
      columnRestoreManager: "p-restore-column-manager"
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoLookupModalBaseComponent, [{
    type: Directive
  }], () => [{
    type: PoLanguageService
  }, {
    type: ChangeDetectorRef
  }], {
    poModal: [{
      type: ViewChild,
      args: [PoModalComponent, {
        static: true
      }]
    }],
    poTable: [{
      type: ViewChild,
      args: [PoTableComponent, {
        static: true
      }]
    }],
    advancedFilters: [{
      type: Input,
      args: ["p-advanced-filters"]
    }],
    columns: [{
      type: Input,
      args: ["p-columns"]
    }],
    items: [{
      type: Input,
      args: ["p-items"]
    }],
    filterService: [{
      type: Input,
      args: ["p-filter-service"]
    }],
    filterParams: [{
      type: Input,
      args: ["p-filter-params"]
    }],
    hideColumnsManager: [{
      type: Input,
      args: [{
        alias: "p-hide-columns-manager",
        transform: convertToBoolean
      }]
    }],
    infiniteScroll: [{
      type: Input,
      args: [{
        alias: "p-infinite-scroll",
        transform: convertToBoolean
      }]
    }],
    multiple: [{
      type: Input,
      args: [{
        alias: "p-multiple",
        transform: convertToBoolean
      }]
    }],
    model: [{
      type: Output,
      args: ["p-change-model"]
    }],
    selectedItems: [{
      type: Input,
      args: ["p-selected-items"]
    }],
    fieldLabel: [{
      type: Input,
      args: ["p-field-label"]
    }],
    fieldValue: [{
      type: Input,
      args: ["p-field-value"]
    }],
    spacing: [{
      type: Input,
      args: ["p-spacing"]
    }],
    size: [{
      type: HostBinding,
      args: ["attr.p-size"]
    }, {
      type: Input,
      args: ["p-size"]
    }],
    textWrap: [{
      type: Input,
      args: [{
        alias: "p-text-wrap",
        transform: convertToBoolean
      }]
    }],
    virtualScroll: [{
      type: Input,
      args: [{
        alias: "p-virtual-scroll",
        transform: convertToBoolean
      }]
    }],
    changeVisibleColumns: [{
      type: Output,
      args: ["p-change-visible-columns"]
    }],
    columnRestoreManager: [{
      type: Output,
      args: ["p-restore-column-manager"]
    }],
    literals: [{
      type: Input,
      args: ["p-literals"]
    }],
    title: [{
      type: Input,
      args: ["p-title"]
    }]
  });
})();
var PoLookupModalComponent = class _PoLookupModalComponent extends PoLookupModalBaseComponent {
  inputSearchEl;
  container;
  keyUpObservable = null;
  tableHeight;
  componentRef;
  dynamicForm;
  constructor() {
    const poLanguage = inject(PoLanguageService);
    const changeDetector = inject(ChangeDetectorRef);
    super(poLanguage, changeDetector);
  }
  ngOnInit() {
    super.ngOnInit();
    this.setTableHeight();
  }
  // Seleciona um item na tabela
  onSelect(item) {
    const formattedItem = __spreadValues({
      value: item[this.fieldValue],
      label: item[this.fieldLabel]
    }, item);
    if (this.multiple) {
      this.selectedItems = this.selectedItems ? [...this.selectedItems, formattedItem] : [formattedItem];
    } else {
      this.selectedItems = [formattedItem];
    }
    this.selecteds = [...this.selectedItems];
  }
  // Remove a seleção de um item na tabela
  onUnselect(unselectedItem) {
    if (this.multiple) {
      this.selectedItems = this.selectedItems.filter((item) => item.value !== unselectedItem[this.fieldValue]);
    } else {
      this.selectedItems = [];
    }
    this.selecteds = [...this.selectedItems];
  }
  onUnselectFromDisclaimer(removedDisclaimer) {
    this.selectedItems = this.selectedItems.filter((item) => item.value !== removedDisclaimer.value);
    if (this.selectedItems.length === 0) {
      this.selecteds = [];
    } else {
      this.selecteds = [...this.selectedItems];
    }
    this.poTable.unselectRowItem((item) => item[this.fieldValue] === removedDisclaimer.value);
  }
  // Seleciona todos os itens visíveis na tabela
  onAllSelected(items) {
    const newItems = items.filter((item) => !this.selectedItems?.some((selected) => selected[this.fieldValue] === item[this.fieldValue])).map((item) => __spreadValues({
      value: item[this.fieldValue],
      label: item[this.fieldLabel]
    }, item));
    this.selectedItems = [...this.selectedItems, ...newItems];
    this.selecteds = [...this.selectedItems];
  }
  // Remove a seleção de todos os itens visíveis na tabela
  onAllUnselected(items) {
    const newItems = this.selectedItems?.filter((item) => !items.some((selected) => selected[this.fieldValue] === item[this.fieldValue]));
    if (newItems?.length) {
      this.selectedItems = [...newItems];
      this.selecteds = [...this.selectedItems];
    } else {
      this.selectedItems = [];
      this.selecteds = [];
    }
  }
  onAllUnselectedTag(items) {
    this.poTable.unselectRows();
    this.selectedItems = [];
    this.selecteds = [];
  }
  openModal() {
    this.poModal.open();
  }
  sortBy(sort3) {
    const order = sort3.type === "ascending";
    sortArrayOfObjects(this.items, sort3.column.property, order);
    this.sort = sort3;
  }
  destroyDynamicForm() {
    if (this.componentRef) {
      this.componentRef.destroy();
    }
  }
  onAdvancedFilter() {
    this.setupModalAdvancedFilter();
    this.createDynamicForm();
  }
  setTableHeight() {
    const visibleRows = 10;
    const headerHeight = this.spacing === PoTableColumnSpacing.ExtraSmall ? 32 : 48;
    const footerHeight = this.size === PoFieldSize.Small ? 48 : 60;
    const currentRowHeight = PO_TABLE_ROW_HEIGHT_BY_SPACING[this.spacing];
    const extraHeight = this.infiniteScroll ? headerHeight : headerHeight + footerHeight;
    this.tableHeight = currentRowHeight * visibleRows + extraHeight;
  }
  setupModalAdvancedFilter() {
    this.dynamicFormValue = {};
    this.isAdvancedFilter = true;
  }
  createDynamicForm() {
    Promise.resolve().then(function() {
      return poDynamicForm_component;
    }).then(({
      PoDynamicFormComponent: PoDynamicFormComponent2
    }) => {
      this.componentRef = this.container.createComponent(PoDynamicFormComponent2);
      this.componentRef.instance.fields = this.advancedFilters;
      this.componentRef.instance.value = this.dynamicFormValue;
      this.componentRef.instance.componentsSize = this.size;
      this.componentRef.instance.formOutput.pipe(tap((form) => {
        this.dynamicForm = form;
        this.primaryActionAdvancedFilter.disabled = this.dynamicForm.invalid;
      }), switchMap((form) => form.valueChanges)).subscribe(() => {
        this.primaryActionAdvancedFilter.disabled = this.dynamicForm.invalid;
      });
      this.changeDetector.markForCheck();
    });
  }
  static ɵfac = function PoLookupModalComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoLookupModalComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoLookupModalComponent,
    selectors: [["po-lookup-modal"]],
    viewQuery: function PoLookupModalComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c1222, 5)(_c123, 5, ViewContainerRef);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.inputSearchEl = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.container = _t.first);
      }
    },
    standalone: false,
    features: [ɵɵInheritDefinitionFeature],
    decls: 18,
    vars: 33,
    consts: [["iconLookup", ""], ["inpsearch", ""], ["poTable", ""], ["container", ""], ["p-click-out", "false", "p-hide-close", "false", "p-size", "lg", 3, "p-components-size", "p-primary-action", "p-secondary-action", "p-title"], [3, "hidden"], [1, "po-lookup-header", "po-pull-right", 3, "p-optional"], [1, "po-lookup-filter-content"], [1, "po-field-icon-container-right"], ["p-icon", "ICON_SEARCH", 1, "po-field-icon", "po-icon-input", 3, "click"], ["name", "contentSearch", "type", "text", 1, "po-input", "po-input-icon-right", 3, "ngModelChange", "keydown.enter", "ngModel", "placeholder"], [1, "po-lookup-advanced-search"], [1, "po-md-12", 3, "p-disclaimers", "p-title"], [1, "po-row", "po-lookup-container-table"], [1, "po-md-12", 3, "p-selected", "p-unselected", "p-all-selected", "p-all-unselected", "p-show-more", "p-sort-by", "p-change-visible-columns", "p-restore-column-manager", "p-components-size", "p-selectable", "p-hide-detail", "p-single-select", "p-hide-action-fixed-columns", "p-sort", "p-columns", "p-height", "p-hide-columns-manager", "p-items", "p-literals", "p-loading", "p-show-more-disabled", "p-infinite-scroll", "p-spacing", "p-text-wrap", "p-virtual-scroll"], [1, "po-md-12", 3, "p-disclaimers"], ["tabindex", "0", 1, "po-lookup-advanced-search-link", 3, "click", "keydown.enter"], [1, "po-md-12", 3, "p-change", "p-disclaimers", "p-title"], [1, "po-md-12", 3, "p-remove", "p-remove-all", "p-disclaimers"]],
    template: function PoLookupModalComponent_Template(rf, ctx) {
      if (rf & 1) {
        const _r1 = ɵɵgetCurrentView();
        ɵɵelementStart(0, "po-modal", 4)(1, "div", 5)(2, "po-field-container", 6)(3, "div", 7)(4, "div", 8)(5, "po-icon", 9, 0);
        ɵɵlistener("click", function PoLookupModalComponent_Template_po_icon_click_5_listener() {
          return ctx.search();
        });
        ɵɵelementEnd()();
        ɵɵelementStart(7, "input", 10, 1);
        ɵɵtwoWayListener("ngModelChange", function PoLookupModalComponent_Template_input_ngModelChange_7_listener($event) {
          ɵɵrestoreView(_r1);
          ɵɵtwoWayBindingSet(ctx.searchValue, $event) || (ctx.searchValue = $event);
          return ɵɵresetView($event);
        });
        ɵɵlistener("keydown.enter", function PoLookupModalComponent_Template_input_keydown_enter_7_listener() {
          return ctx.search();
        });
        ɵɵelementEnd()();
        ɵɵconditionalCreate(9, PoLookupModalComponent_Conditional_9_Template, 3, 1, "div", 11);
        ɵɵelementEnd();
        ɵɵconditionalCreate(10, PoLookupModalComponent_Conditional_10_Template, 1, 2, "po-disclaimer-group", 12);
        ɵɵelementStart(11, "div", 13)(12, "po-table", 14, 2);
        ɵɵlistener("p-selected", function PoLookupModalComponent_Template_po_table_p_selected_12_listener($event) {
          return ctx.onSelect($event);
        })("p-unselected", function PoLookupModalComponent_Template_po_table_p_unselected_12_listener($event) {
          return ctx.onUnselect($event);
        })("p-all-selected", function PoLookupModalComponent_Template_po_table_p_all_selected_12_listener($event) {
          return ctx.onAllSelected($event);
        })("p-all-unselected", function PoLookupModalComponent_Template_po_table_p_all_unselected_12_listener($event) {
          return ctx.onAllUnselected($event);
        })("p-show-more", function PoLookupModalComponent_Template_po_table_p_show_more_12_listener() {
          return ctx.showMoreEvent();
        })("p-sort-by", function PoLookupModalComponent_Template_po_table_p_sort_by_12_listener($event) {
          return ctx.sortBy($event);
        })("p-change-visible-columns", function PoLookupModalComponent_Template_po_table_p_change_visible_columns_12_listener($event) {
          return ctx.changeVisibleColumns.emit($event);
        })("p-restore-column-manager", function PoLookupModalComponent_Template_po_table_p_restore_column_manager_12_listener($event) {
          return ctx.columnRestoreManager.emit($event);
        });
        ɵɵelementEnd()();
        ɵɵconditionalCreate(14, PoLookupModalComponent_Conditional_14_Template, 1, 1, "po-disclaimer-group", 15);
        ɵɵelementEnd();
        ɵɵelementStart(15, "div", 5);
        ɵɵelementContainer(16, null, 3);
        ɵɵelementEnd()();
      }
      if (rf & 2) {
        ɵɵproperty("p-components-size", ctx.size)("p-primary-action", ctx.isAdvancedFilter ? ctx.primaryActionAdvancedFilter : ctx.primaryAction)("p-secondary-action", ctx.isAdvancedFilter ? ctx.secondaryActionAdvancedFilter : ctx.secondaryAction)("p-title", ctx.isAdvancedFilter ? ctx.advancedFilterModalTitle : ctx.title);
        ɵɵadvance();
        ɵɵproperty("hidden", ctx.isAdvancedFilter && ctx.advancedFilters.length > 0);
        ɵɵadvance();
        ɵɵproperty("p-optional", false);
        ɵɵadvance(3);
        ɵɵclassProp("po-field-icon-aa", ctx.size === "small");
        ɵɵadvance(2);
        ɵɵtwoWayProperty("ngModel", ctx.searchValue);
        ɵɵproperty("placeholder", ctx.literals.modalPlaceholder);
        ɵɵattribute("p-size", ctx.size);
        ɵɵadvance(2);
        ɵɵconditional(ctx.advancedFilters && ctx.advancedFilters.length > 0 ? 9 : -1);
        ɵɵadvance();
        ɵɵconditional(!!ctx.disclaimerGroup ? 10 : -1);
        ɵɵadvance();
        ɵɵattribute("data-multiple", ctx.multiple);
        ɵɵadvance();
        ɵɵproperty("p-components-size", ctx.size)("p-selectable", true)("p-hide-detail", true)("p-single-select", !ctx.multiple)("p-hide-action-fixed-columns", true)("p-sort", true)("p-columns", ctx.columns)("p-height", ctx.tableHeight)("p-hide-columns-manager", ctx.hideColumnsManager)("p-items", ctx.items)("p-literals", ctx.tableLiterals)("p-loading", ctx.isLoading)("p-show-more-disabled", !ctx.hasNext)("p-infinite-scroll", ctx.infiniteScroll)("p-spacing", ctx.spacing)("p-text-wrap", ctx.textWrap)("p-virtual-scroll", ctx.virtualScroll);
        ɵɵadvance(2);
        ɵɵconditional(ctx.multiple ? 14 : -1);
        ɵɵadvance();
        ɵɵproperty("hidden", !ctx.isAdvancedFilter);
      }
    },
    dependencies: [DefaultValueAccessor, NgControlStatus, NgModel, PoDisclaimerGroupComponent, PoFieldContainerComponent, PoIconComponent, PoModalComponent, PoTableComponent],
    encapsulation: 2,
    changeDetection: 0
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoLookupModalComponent, [{
    type: Component,
    args: [{
      selector: "po-lookup-modal",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: `<po-modal
  p-click-out="false"
  p-hide-close="false"
  p-size="lg"
  [p-components-size]="size"
  [p-primary-action]="isAdvancedFilter ? primaryActionAdvancedFilter : primaryAction"
  [p-secondary-action]="isAdvancedFilter ? secondaryActionAdvancedFilter : secondaryAction"
  [p-title]="isAdvancedFilter ? advancedFilterModalTitle : title"
>
  <div [hidden]="isAdvancedFilter && advancedFilters.length > 0">
    <po-field-container class="po-lookup-header po-pull-right" [p-optional]="false">
      <div class="po-lookup-filter-content">
        <div class="po-field-icon-container-right">
          <po-icon
            #iconLookup
            p-icon="ICON_SEARCH"
            class="po-field-icon po-icon-input"
            [class.po-field-icon-aa]="size === 'small'"
            (click)="search()"
          >
          </po-icon>
        </div>

        <input
          #inpsearch
          class="po-input po-input-icon-right"
          [attr.p-size]="size"
          name="contentSearch"
          [(ngModel)]="searchValue"
          [placeholder]="literals.modalPlaceholder"
          (keydown.enter)="search()"
          type="text"
        />
      </div>

      @if (advancedFilters && advancedFilters.length > 0) {
        <div class="po-lookup-advanced-search">
          <span
            class="po-lookup-advanced-search-link"
            tabindex="0"
            (click)="onAdvancedFilter()"
            (keydown.enter)="onAdvancedFilter()"
          >
            {{ literals.modalAdvancedSearch }}
          </span>
        </div>
      }
    </po-field-container>

    <!-- DISCLAIMER -->
    @if (!!disclaimerGroup) {
      <po-disclaimer-group
        class="po-md-12"
        [p-disclaimers]="disclaimerGroup?.disclaimers"
        [p-title]="disclaimerGroup?.title"
        (p-change)="onChangeDisclaimerGroup()"
      >
      </po-disclaimer-group>
    }

    <div class="po-row po-lookup-container-table" [attr.data-multiple]="multiple">
      <po-table
        #poTable
        class="po-md-12"
        [p-components-size]="size"
        [p-selectable]="true"
        [p-hide-detail]="true"
        [p-single-select]="!multiple"
        [p-hide-action-fixed-columns]="true"
        [p-sort]="true"
        [p-columns]="columns"
        [p-height]="tableHeight"
        [p-hide-columns-manager]="hideColumnsManager"
        [p-items]="items"
        [p-literals]="tableLiterals"
        [p-loading]="isLoading"
        [p-show-more-disabled]="!hasNext"
        [p-infinite-scroll]="infiniteScroll"
        [p-spacing]="spacing"
        [p-text-wrap]="textWrap"
        [p-virtual-scroll]="virtualScroll"
        (p-selected)="onSelect($event)"
        (p-unselected)="onUnselect($event)"
        (p-all-selected)="onAllSelected($event)"
        (p-all-unselected)="onAllUnselected($event)"
        (p-show-more)="showMoreEvent()"
        (p-sort-by)="sortBy($event)"
        (p-change-visible-columns)="changeVisibleColumns.emit($event)"
        (p-restore-column-manager)="columnRestoreManager.emit($event)"
      >
      </po-table>
    </div>

    <!-- DISCLAIMER -->
    @if (multiple) {
      <po-disclaimer-group
        class="po-md-12"
        [p-disclaimers]="selecteds"
        (p-remove)="onUnselectFromDisclaimer($event.removedDisclaimer)"
        (p-remove-all)="onAllUnselectedTag($event)"
      >
      </po-disclaimer-group>
    }
  </div>
  <div [hidden]="!isAdvancedFilter">
    <ng-container #container> </ng-container>
  </div>
</po-modal>
`
    }]
  }], () => [], {
    inputSearchEl: [{
      type: ViewChild,
      args: ["inpsearch"]
    }],
    container: [{
      type: ViewChild,
      args: ["container", {
        read: ViewContainerRef
      }]
    }]
  });
})();
var PoLookupFilterService = class _PoLookupFilterService {
  httpClient;
  headers = new HttpHeaders({
    "X-PO-No-Message": "true"
  });
  url;
  fieldValue;
  multiple = false;
  constructor(httpClient) {
    this.httpClient = httpClient;
  }
  getFilteredItems(filteredItemsParams) {
    const _a2 = filteredItemsParams, {
      filterParams,
      advancedFilters
    } = _a2, restFilteredItemsParams = __objRest(_a2, [
      "filterParams",
      "advancedFilters"
    ]);
    const validatedFilterParams = this.validateParams(filterParams);
    const validatedAdvancedFilters = this.validateParams(advancedFilters);
    const params = __spreadValues(__spreadValues(__spreadValues({}, restFilteredItemsParams), validatedFilterParams), validatedAdvancedFilters);
    return this.httpClient.get(this.url, {
      headers: this.headers,
      params
    });
  }
  getObjectByValue(value, filterParams) {
    const validatedFilterParams = this.validateParams(filterParams);
    let newURL;
    let encodedValue;
    if (this.multiple) {
      encodedValue = encodeURIComponent(Array.isArray(value) ? value.join(",") : value);
      newURL = `${this.url}?${this.fieldValue}=${encodedValue}`;
    } else {
      encodedValue = encodeURIComponent(value);
      newURL = `${this.url}/${encodedValue}`;
    }
    return this.httpClient.get(newURL, {
      headers: this.headers,
      params: validatedFilterParams
    }).pipe(map((response) => "items" in response ? response.items : response));
  }
  setConfig(url, fieldValue, multiple) {
    this.url = url;
    this.fieldValue = fieldValue;
    this.multiple = multiple;
  }
  validateParams(params) {
    return isTypeof(params, "object") && !Array.isArray(params) ? params : void 0;
  }
  static ɵfac = function PoLookupFilterService_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoLookupFilterService)(ɵɵinject(HttpClient));
  };
  static ɵprov = ɵɵdefineInjectable({
    token: _PoLookupFilterService,
    factory: _PoLookupFilterService.ɵfac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoLookupFilterService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: HttpClient
  }], null);
})();
var PoLookupModalService = class _PoLookupModalService {
  poComponentInjector = inject(PoComponentInjectorService);
  selectValueEvent = new EventEmitter();
  componentRef = null;
  /**
   * Método responsável por abrir a modal de busca das informações.
   *
   * @param advancedFilters {Array<PoDynamicFormField>} Objeto utilizado para criar o busca avançada.
   * @param service {PoLookupFilter} Serviço responsável por realizar a busca no serviço dos dados.
   * @param columns {Array<PoLookupColumn>} Definição das colunas na modal de busca.
   * @param filterParams {any} Valor que será repassado aos métodos do serviço para auxiliar no filtro dos dados.
   * @param title {string} Definição do título da modal.
   * @param literals {PoLookupLiterals} Literais utilizadas no componente.
   * @param selectedItems {any} Valor que está selecionado que será repassado para o modal para apresentar na tabela.
   * @param fieldLabel {string} Valor que será utilizado como descrição do campo.
   * @param fieldValue {string} Valor que será utilizado como valor do campo.
   * @param changeVisibleColumns {function} Função que será executada quando for alterada a visibilidade das colunas.
   * @param columnRestoreManager {function} Função que será executada quando for restaurar as colunas padrão.
   */
  openModal(params) {
    const {
      advancedFilters,
      service,
      columns,
      filterParams,
      hideColumnsManager,
      title,
      literals,
      infiniteScroll,
      multiple,
      selectedItems,
      fieldLabel,
      fieldValue,
      size,
      spacing,
      textWrap,
      virtualScroll,
      changeVisibleColumns,
      columnRestoreManager
    } = params;
    this.componentRef = this.poComponentInjector.createComponentInApplication(PoLookupModalComponent);
    this.componentRef.instance.advancedFilters = advancedFilters;
    this.componentRef.instance.title = title;
    this.componentRef.instance.columns = columns;
    this.componentRef.instance.filterService = service;
    this.componentRef.instance.filterParams = filterParams;
    this.componentRef.instance.literals = literals;
    this.componentRef.instance.model.subscribe(($event) => {
      this.selectValue($event);
    });
    this.componentRef.instance.infiniteScroll = infiniteScroll;
    this.componentRef.instance.multiple = multiple;
    this.componentRef.instance.selectedItems = selectedItems;
    this.componentRef.instance.fieldLabel = fieldLabel;
    this.componentRef.instance.fieldValue = fieldValue;
    this.componentRef.instance.changeVisibleColumns = changeVisibleColumns;
    this.componentRef.instance.columnRestoreManager = columnRestoreManager;
    this.componentRef.instance.hideColumnsManager = hideColumnsManager;
    this.componentRef.instance.size = size;
    this.componentRef.instance.spacing = spacing;
    this.componentRef.instance.textWrap = textWrap;
    this.componentRef.instance.virtualScroll = virtualScroll;
    this.componentRef.changeDetectorRef.detectChanges();
    this.componentRef.instance.openModal();
  }
  setChangeColumns(columns) {
    if (this.componentRef !== null) {
      this.componentRef.instance.columns = columns;
      this.componentRef.changeDetectorRef.detectChanges();
    }
  }
  // Este metodo é chamado quando é selecionado um item na lookup modal.
  selectValue(value) {
    if (value) {
      this.selectValueEvent.emit(value);
    }
    this.componentRef.destroy();
  }
  static ɵfac = function PoLookupModalService_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoLookupModalService)();
  };
  static ɵprov = ɵɵdefineInjectable({
    token: _PoLookupModalService,
    factory: _PoLookupModalService.ɵfac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoLookupModalService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
var poLookupLiteralsDefault = {
  en: {
    search: "Search",
    clean: "Clean"
  },
  es: {
    search: "Buscar",
    clean: "Limpiar"
  },
  pt: {
    search: "Pesquisar",
    clean: "Apagar"
  },
  ru: {
    search: "Поиск",
    clean: "чистый"
  }
};
var PoLookupBaseComponent = class _PoLookupBaseComponent {
  defaultService;
  injector;
  poLookupModalService;
  _literals;
  language;
  _size = void 0;
  _initialSize = void 0;
  _spacing;
  inputEl;
  // Propriedade interna que define se o ícone de ajuda adicional terá cursor clicável (evento) ou padrão (tooltip).
  additionalHelpEventTrigger;
  /**
   *
   * @deprecated v23.x.x use `p-helper`
   *
   * @optional
   *
   * @description
   * Exibe um ícone de ajuda adicional, com o texto desta propriedade sendo passado para o popover do componente `po-helper`.
   * **Como boa prática, indica-se utilizar um texto com até 140 caracteres.**
   * > Requer um recuo mínimo de 8px se o componente estiver próximo à lateral da tela.
   *
   * > Essa propriedade está **depreciada** e será removida na versão `23.x.x`. Recomendamos utilizar a propriedade `p-helper` que oferece mais recursos e flexibilidade.
   */
  additionalHelpTooltip;
  /**
   * @optional
   *
   * @description
   *
   * Define que o popover (`p-helper` e/ou `p-error-limit`) será incluído no body da página e não
   * dentro do componente. Essa opção pode ser necessária em cenários com containers que possuem scroll ou overflow
   * escondido, garantindo o posicionamento correto do tooltip próximo ao elemento.
   *
   * > Quando utilizado com `p-helper`, leitores de tela como o NVDA podem não ler o conteúdo do popover.
   *
   * @default `false`
   */
  appendBox = false;
  /**
   * @optional
   *
   * @description
   *
   * Aplica foco no elemento ao ser iniciado.
   *
   * > Caso mais de um elemento seja configurado com essa propriedade, apenas o último elemento declarado com ela terá o foco.
   *
   * @default `false`
   */
  autoFocus = false;
  /**
   * Label do campo.
   *
   * > Quando utilizar esta propriedade o seu valor será utilizado como título da modal do componente caso não tenha
   * sido definido um `modalTitle` na propriedade `p-literals`.
   */
  label;
  /**
   * @description
   *
   * Objeto com as literais usadas no `po-lookup`.
   *
   * Existem duas maneiras de customizar o componente, passando um objeto com todas as literais disponíveis:
   *
   * ```
   *  const customLiterals: PoLookupLiterals = {
   *    modalPrimaryActionLabel: 'Select',
   *    modalSecondaryActionLabel: 'Cancel',
   *    modalPlaceholder: 'Search Value',
   *    modalTableNoColumns: 'No columns',
   *    modalTableNoData: 'No data',
   *    modalTableLoadingData: 'Loading data',
   *    modalTableLoadMoreData: 'Load more',
   *    modalTitle: 'Select a user',
   *    modalAdvancedSearch: 'Advanced search',
   *    modalAdvancedSearchTitle: 'Advanced search',
   *    modalAdvancedSearchPrimaryActionLabel: 'Filter',
   *    modalAdvancedSearchSecondaryActionLabel: 'Return',
   *    modalDisclaimerGroupTitle: 'Presenting results filtered by:'
   *  };
   * ```
   *
   * Ou passando apenas as literais que deseja customizar:
   *
   * ```
   *  const customLiterals: PoLookupLiterals = {
   *    modalPrimaryActionLabel: 'Select'
   *  };
   * ```
   *
   * E para carregar as literais customizadas, basta apenas passar o objeto para o componente.
   *
   * ```
   * <po-lookup
   *   [p-literals]="customLiterals">
   * </po-lookup>
   * ```
   *
   * > O objeto padrão de literais será traduzido de acordo com o idioma do
   * [`PoI18nService`](/documentation/po-i18n) ou do browser.
   */
  set literals(value) {
    if (value instanceof Object && !(value instanceof Array)) {
      this._literals = __spreadValues(__spreadValues(__spreadValues({}, poLookupLiteralsDefault[poLocaleDefault]), poLookupLiteralsDefault[this.language]), value);
    } else {
      this._literals = poLookupLiteralsDefault[this.language];
    }
  }
  get literals() {
    return this._literals || poLookupLiteralsDefault[this.language];
  }
  /** Texto de apoio do campo. */
  help;
  /** Mensagem que aparecerá enquanto o campo não estiver preenchido. */
  set placeholder(value) {
    this._placeholder = value || "";
  }
  get placeholder() {
    return this._placeholder;
  }
  /** Nome e Id do componente. */
  name;
  /**
   * @description
   *
   * Indica a coluna que será utilizada como valor do campo.
   *
   * > Atenção: Caso não seja passada ou tenha o conteúdo incorreto, não irá atualizar o model do formulário.
   */
  fieldValue;
  /** Indica a coluna que será utilizada como descrição do campo e como filtro dentro da janela. */
  set fieldLabel(value) {
    this._fieldLabel = value;
    this.keysDescription = [this.fieldLabel];
  }
  get fieldLabel() {
    return this._fieldLabel;
  }
  /** Valor que será repassado como parâmetro para a URL ou aos métodos do serviço que implementam a interface `PoLookupFilter`. */
  filterParams;
  /**
   * @optional
   *
   * @description
   *
   * Formato de exibição do campo.
   *
   * Recebe uma função que deve retornar uma *string* com o/os valores do objeto formatados para exibição, por exemplo:
   *
   * ```
   * fieldFormat(obj) {
   *   return `${obj.id} - ${obj.name}`;
   * }
   * ```
   * > Esta propriedade sobrepõe o valor da propriedade `p-field-label` na descrição do campo.
   *
   * Pode-se informar uma lista de propriedades que deseja exibir como descrição do campo, Por exemplo:
   * ```
   * <po-lookup
   *  ...
   *  [p-field-format]="['id','nickname']"
   *  ...
   * >
   *
   * Objeto retornado:
   *   {
   *      id:123,
   *      name: 'Kakaroto',
   *      nickname: 'Goku',
   *   }
   * Apresentação no campo: 123 - Goku
   * ```
   *
   * > Será utilizado ` - ` como separador.
   */
  fieldFormat;
  /**
   * Lista das colunas da tabela.
   * Essa propriedade deve receber um array de objetos que implementam a interface PoLookupColumn.
   */
  columns;
  /**
   * @optional
   *
   * @description
   *
   * Define se a indicação de campo opcional será exibida.
   *
   * > Não será exibida a indicação se:
   * - O campo conter `p-required`;
   * - Não possuir `p-help` e/ou `p-label`.
   *
   * @default `false`
   */
  optional;
  /**
   *
   * @optional
   *
   * @description
   *
   * Lista de objetos dos campos que serão criados na busca avançada.
   *
   * > Caso não seja passado um objeto ou então ele esteja em branco o link de busca avançada ficará escondido.
   *
   * > A busca avançada é construída a partir do `po-dynamic-form`. Em aplicações que não importam o `PoModule`,
   * como projetos *standalone* ou que utilizam módulos específicos, é necessário importar o `PoDynamicModule`
   * no componente ou módulo onde o `po-lookup` é utilizado, caso contrário será lançado o erro
   * `NG0201: No provider found for _TitleCasePipe` ao abrir a busca avançada.
   *
   * Exemplo de URL com busca avançada:
   *
   * ```
   * url + ?page=1&pageSize=20&name=Tony%20Stark&nickname=Homem%20de%20Ferro
   * ```
   *
   * Caso algum parâmetro seja uma lista, a concatenação é feita utilizando vírgula.
   * Exemplo:
   *
   * ```
   * url + ?page=1&pageSize=20&name=Tony%20Stark,Peter%20Parker,Gohan
   * ```
   *
   */
  advancedFilters;
  /**
   * @optional
   *
   * @description
   *
   * Permite que o gerenciador de colunas, responsável pela definição de quais colunas serão exibidas, seja escondido.
   *
   * @default `false`
   */
  hideColumnsManager = false;
  /**
   * @optional
   *
   * @description
   *
   * Ativa a funcionalidade de scroll infinito para a tabela exibida no retorno da consulta.
   *
   * @default `false`
   */
  infiniteScroll = false;
  /** Exibe um ícone que permite limpar o campo. */
  clean = false;
  /**
   * @optional
   *
   * @description
   *
   * Permite a seleção de múltiplos itens.
   *
   * > Quando habilitado o valor do campo passará a ser uma lista de valores, por exemplo: `[ 12345, 67890 ]`
   *
   * @default `false`
   */
  multiple = false;
  /**
   * @optional
   *
   * @description
   *
   * Define que a altura do componente será auto ajustável, possuindo uma altura minima porém a altura máxima será de acordo
   * com o número de itens selecionados e a extensão dos mesmos, mantendo-os sempre visíveis.
   *
   * @default `false`
   */
  autoHeight = false;
  /**
   * @optional
   *
   * @description
   *
   * Exibe a mensagem setada se o campo estiver vazio e for requerido.
   *
   * > Necessário que a propriedade `p-required` esteja habilitada.
   *
   */
  fieldErrorMessage;
  /**
   * @optional
   *
   * @description
   *
   * Limita a exibição da mensagem de erro a duas linhas e exibe um tooltip com o texto completo.
   *
   * > Caso essa propriedade seja definida como `true`, a mensagem de erro será limitada a duas linhas
   * e um tooltip será exibido ao passar o mouse sobre a mensagem para mostrar o conteúdo completo.
   *
   * @default `false`
   */
  errorLimit = false;
  /**
   * @optional
   *
   * @description
   *
   * Define o tamanho do componente:
   * - `small`: altura do input como 32px (disponível apenas para acessibilidade AA).
   * - `medium`: altura do input como 44px.
   *
   * > Caso a acessibilidade AA não esteja configurada, o tamanho `medium` será mantido.
   * Para mais detalhes, consulte a documentação do [po-theme](https://po-ui.io/documentation/po-theme).
   *
   * @default `medium`
   */
  set size(value) {
    this._initialSize = value;
    this.applySizeBasedOnA11y();
  }
  get size() {
    return this._size ?? getDefaultSizeFn(PoFieldSize);
  }
  /**
   * @optional
   *
   * @description
   *
   * Define o espaçamento interno das células, impactando diretamente na altura das linhas do table dentro do modal. Os
   * valores permitidos são definidos pelo enum **PoTableColumnSpacing**.
   *
   * > Em nível de acessibilidade **AA**, caso o valor de `p-spacing` não seja definido, o valor padrão será `extraSmall`
   * > nos seguintes cenários:
   * > - Quando o valor de `p-size` for `small`;
   * > - Quando o valor padrão dos componentes for configurado como `small` no
   * > [serviço de tema](https://po-ui.io/documentation/po-theme).
   *
   * @default `medium`
   */
  set spacing(value) {
    const isExtraSmall = value === PoTableColumnSpacing.ExtraSmall;
    const isA11yAAA = PoUtils.getA11yLevel() === PoThemeA11yEnum.AAA;
    if (Object.values(PoTableColumnSpacing).includes(value)) {
      if (isA11yAAA && isExtraSmall) {
        this._spacing = PoTableColumnSpacing.Medium;
      } else {
        this._spacing = value;
      }
    } else {
      this._spacing = this.getDefaultSpacing();
    }
  }
  get spacing() {
    return this._spacing ?? this.getDefaultSpacing();
  }
  /**
   * @optional
   *
   * @description
   *
   * Habilita ou desabilita a quebra automática de texto. Quando ativada, o texto que excede
   * o espaço disponível é transferido para a próxima linha em pontos apropriados para uma
   * leitura clara.
   *
   * Esta propriedade aplica-se ao texto contido nas células da tabela.
   *
   * > Incompatível com `virtual-scroll`, que requer altura fixa nas linhas.
   *
   * @default `false`
   */
  textWrap = false;
  /**
   * @optional
   *
   * @description
   *
   * Habilita o `virtual-scroll` na tabela para melhorar a performance com grandes volumes de dados.
   * A altura da tabela já é pré-definida, portanto o `virtual-scroll` será ativado automaticamente.
   *
   * > Incompatível com `p-text-wrap` e `master-detail`, pois o `virtual-scroll` exige altura fixa nas linhas.
   *
   * @default `true`
   */
  virtualScroll = true;
  /**
   * @optional
   *
   * @description
   *
   * Define as opções do componente de ajuda (po-helper) que será exibido ao lado do label quando a propriedade `p-label` for definida, ou, ao lado do componente na ausência da propriedade `p-label`.
   * > Para mais informações acesse: https://po-ui.io/documentation/po-helper.
   *
   * > Ao configurar esta propriedade, o antigo ícone de ajuda adicional (`p-additional-help-tooltip` e `p-additional-help`) será ignorado.
   */
  poHelperComponent = input(void 0, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "poHelperComponent"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-helper"
  }));
  /**
   * @optional
   *
   * @description
   *
   * Habilita a quebra automática do texto da propriedade `p-label`. Quando `p-label-text-wrap` for verdadeiro, o texto que excede
   * o espaço disponível é transferido para a próxima linha em pontos apropriados para uma
   * leitura clara.
   *
   * @default `false`
   */
  labelTextWrap = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "labelTextWrap"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-label-text-wrap"
  }));
  /**
   * @optional
   *
   * @description
   * Define se o título do campo será exibido de forma compacta.
   *
   * Quando habilitado (`true`), o modo compacto afeta o conjunto composto por:
   * - `po-label`
   * - `p-requirement (showRequired)`
   * - `po-helper`
   *
   * Ou seja, todos os elementos relacionados ao título do campo
   * (rótulo, indicador de obrigatoriedade e componente auxiliar) passam
   * a seguir o comportamento de layout compacto.
   *
   * Também é possível definir esse comportamento de forma global,
   * uma única vez, na folha de estilo geral da aplicação, por meio
   * da customização dos tokens CSS:
   *
   * - `--field-container-title-justify`
   * - `--field-container-title-flex`
   *
   * Exemplo:
   *
   * ```
   * :root {
   *   --field-container-title-justify: flex-start;
   *   --field-container-title-flex: 0 1 auto;
   * }
   * ```
   *
   * Dessa forma, o layout compacto passa a ser o padrão da aplicação,
   * sem a necessidade de definir a propriedade individualmente em cada campo.
   *
   * @default `false`
   */
  compactLabel = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "compactLabel"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-compact-label",
    transform: convertToBoolean
  }));
  /**
   *
   * @deprecated v23.x.x use `p-helper`
   *
   * @optional
   *
   * @description
   * Evento disparado ao clicar no ícone de ajuda adicional.
   *
   * > Essa propriedade está **depreciada** e será removida na versão `23.x.x`. Recomendamos utilizar a propriedade `p-helper` que oferece mais recursos e flexibilidade.
   */
  additionalHelp = new EventEmitter();
  /**
   * Evento será disparado quando ocorrer algum erro na requisição de busca do item.
   * Será passado por parâmetro o objeto de erro retornado.
   */
  onError = new EventEmitter();
  /**
   * @optional
   *
   * @description
   * Evento disparado quando uma tecla é pressionada enquanto o foco está no componente.
   * Retorna um objeto `KeyboardEvent` com informações sobre a tecla.
   */
  keydown = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Evento será disparado quando ocorrer alguma seleção.
   * Será passado por parâmetro o objeto com o valor selecionado.
   */
  selected = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   *  Evento que será disparado ao alterar o model.
   *  Por parâmetro será passado o novo valor.
   */
  change = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Evento disparado sempre que o valor do model é alterado, seja por interação do usuário
   * ou por atualização programática (ex: `setValue`, `patchValue`, carregamento assíncrono).
   *
   * Diferentemente do `p-change`, que é disparado apenas por interação do usuário,
   * o `p-change-model` cobre todos os cenários de alteração de valor.
   *
   * Não emite quando o novo valor é idêntico ao anterior (deduplicação automática).
   */
  changeModel = new EventEmitter();
  /**
   * @optional
   *
   * @description
   * Evento disparado ao fechar o popover do gerenciador de colunas após alterar as colunas visíveis.
   *
   * O componente envia como parâmetro um array de string com as colunas visíveis atualizadas.
   * Por exemplo: ["idCard", "name", "hireStatus", "age"].
   */
  changeVisibleColumns = new EventEmitter();
  /**
   * @optional
   *
   * @description
   * Evento disparado ao clicar no botão de restaurar padrão no gerenciador de colunas.
   *
   * O componente envia como parâmetro um array de string com as colunas configuradas inicialmente.
   * Por exemplo: ["idCard", "name", "hireStatus", "age"].
   */
  columnRestoreManager = new EventEmitter();
  displayAdditionalHelp = false;
  service;
  modelLastUpdate;
  selectedOptions = [];
  getSubscription;
  keysDescription;
  oldValue = "";
  valueToModel;
  oldValueToModel = null;
  // eslint-disable-next-line
  onTouched = null;
  resizeListener;
  isExpandedHeight = false;
  _disabled = false;
  _fieldLabel;
  _filterService;
  _loading = false;
  _noAutocomplete;
  _placeholder = "";
  _required = false;
  _autoHeight = false;
  autoHeightInitialValue;
  onChangePropagate = null;
  validatorChange;
  control;
  injectOptions = {
    self: true
  };
  /**
   * Serviço responsável por buscar os dados da tabela na janela. Pode ser informado um serviço que implemente a interface
   * `PoLookupFilter` ou uma URL.
   *
   * Quando utilizada uma URL de um serviço, será concatenada nesta URL o valor que deseja-se filtrar, por exemplo:
   *
   * ```
   * url + ?page=1&pageSize=20&filter=Peter
   * ```
   *
   * Caso utilizar ordenação, a coluna ordenada será enviada através do parâmetro `order`, por exemplo:
   * - Coluna decrescente:
   *   ```
   *    url + ?page=1&pageSize=20&filter=Peter&order=-name
   *   ```
   *
   * - Coluna ascendente:
   *   ```
   *    url + ?page=1&pageSize=20&filter=Peter&order=name
   *   ```
   *
   * Se for definido a propriedade `p-filter-params`, o mesmo também será concatenado. Por exemplo, para o
   * parâmetro `{ age: 23 }` a URL ficaria:
   *
   * ```
   * url + ?page=1&pageSize=20&age=23&filter=Peter
   * ```
   *
   * Ao iniciar o campo com valor, os registros serão buscados da seguinte forma:
   * ```
   * model = 1234;
   *
   * GET url/1234
   * ```
   *
   * Caso estiver com múltipla seleção habilitada:
   * ```
   * model = [1234, 5678]
   *
   * GET url?${fieldValue}=1234,5678
   * ```
   *
   * > Esta URL deve retornar e receber os dados no padrão de [API do PO UI](https://po-ui.io/guides/api) e utiliza os valores
   * definidos nas propriedades `p-field-label` e `p-field-value` para a construção do `po-lookup`.
   *
   * Caso o usuário digite um valor e pressione a tecla *TAB* para realizar a busca de um registro específico, o valor que se
   * deseja filtrar será codificado utilizando a função [encodeURIComponent](https://tc39.es/ecma262/#sec-encodeuricomponent-uricomponent)
   * e concatenado na URL da seguinte forma:
   *
   * ```
   * url/valor%20que%20se%20deseja%20filtrar
   * ```
   *
   * > Quando informado um serviço que implemente a interface `PoLookupFilter` o tratamento de encoding do valor a ser filtrado ficará a cargo do desenvolvedor.
   *
   */
  set filterService(filterService) {
    this._filterService = filterService;
    this.setService(this.filterService);
  }
  get filterService() {
    return this._filterService;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define a propriedade nativa `autocomplete` do campo como `off`.
   *
   * @default `false`
   */
  set noAutocomplete(value) {
    this._noAutocomplete = value;
  }
  get noAutocomplete() {
    return this._noAutocomplete;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define que o campo será obrigatório.
   * > Esta propriedade é desconsiderada quando o input está desabilitado `(p-disabled)`.
   *
   * @default `false`
   */
  set required(required) {
    this._required = required;
    this.validateModel(this.valueToModel);
  }
  get required() {
    return this._required;
  }
  /**
   * Define se a indicação de campo obrigatório seré exibida.
   *
   * > Não será exibida a indicação se:
   * - Não possuir `p-help` e/ou `p-label`.
   */
  showRequired = false;
  /**
   * @description
   *
   * Indica que o campo será desabilitado.
   *
   * @default false
   * @optional
   */
  set disabled(disabled) {
    this._disabled = disabled;
  }
  get disabled() {
    return this._disabled;
  }
  /**
   * @optional
   *
   * @description
   * Exibe um ícone de carregamento no lado direito do campo para sinalizar que uma operação está em andamento.
   *
   * @default `false`
   */
  set loading(value) {
    this._loading = value;
  }
  get loading() {
    return this._loading;
  }
  get isDisabled() {
    return this.disabled || this.loading;
  }
  constructor(defaultService, injector, poLookupModalService, languageService) {
    this.defaultService = defaultService;
    this.injector = injector;
    this.poLookupModalService = poLookupModalService;
    this.language = languageService.getShortLanguage();
  }
  ngOnDestroy() {
    if (this.getSubscription) {
      this.getSubscription.unsubscribe();
    }
  }
  ngOnInit() {
    this.initializeColumn();
  }
  ngAfterViewInit() {
    this.setControl();
  }
  cleanModel() {
    this.cleanViewValue();
    this.callOnChange(void 0);
  }
  ngOnChanges(changes) {
    if (changes.columns?.currentValue) {
      this.columns = changes.columns.currentValue;
      this.poLookupModalService?.setChangeColumns(this.columns);
    }
    if (changes.multiple && isTypeof(this.filterService, "string")) {
      this.service.setConfig(this.filterService, this.fieldValue, this.multiple);
    }
  }
  onThemeChange() {
    this.applySizeBasedOnA11y();
  }
  // Função implementada do ControlValueAccessor
  // Usada para interceptar os estados de habilitado via forms api
  setDisabledState(isDisabled) {
    this.disabled = isDisabled;
  }
  registerOnValidatorChange(fn) {
    this.validatorChange = fn;
  }
  // Função implementada do ControlValueAccessor.
  // Usada para interceptar as mudanças e não atualizar automaticamente o Model.
  registerOnChange(func) {
    this.onChangePropagate = func;
  }
  // Função implementada do ControlValueAccessor.
  // Usada para interceptar as mudanças e não atualizar automaticamente o Model.
  registerOnTouched(func) {
    this.onTouched = func;
  }
  // Seleciona o valor do model.
  selectValue(valueSelected) {
    this.valueToModel = valueSelected;
    if (this.multiple) {
      this.callOnChange(this.valueToModel);
    } else if (this.valueToModel) {
      this.callOnChange(this.valueToModel[this.fieldValue]);
    } else {
      this.callOnChange(void 0);
    }
    this.selected.emit(valueSelected);
  }
  callOnChange(value) {
    if (this.onChangePropagate) {
      this.onChangePropagate(value);
    }
    if (this.oldValueToModel !== this.valueToModel) {
      this.change.emit(value);
    }
    this.oldValueToModel = this.valueToModel;
    setTimeout(() => {
      if (this.autoHeight) {
        this.updateLookupInputHeight();
      }
    });
    this.controlChangeModelEmitter(value);
  }
  controlChangeModelEmitter(value) {
    if (this.modelLastUpdate !== value) {
      this.changeModel.emit(value);
      this.modelLastUpdate = value;
    }
  }
  //Transforma o tamanho do input para o tamanho do ícone de loading correspondente
  mapSizeToIcon(size) {
    return mapInputSizeToLoadingIcon(size);
  }
  searchById(value) {
    let checkedValue = value;
    if (typeof checkedValue === "string") {
      checkedValue = checkedValue.trim();
    }
    if (checkedValue !== "" && checkedValue !== ".") {
      const oldDisable = this.disabled;
      this.disabled = true;
      if (this.control) {
        setTimeout(() => this.control.markAsPending());
      }
      this.getSubscription = this.service.getObjectByValue(value, this.filterParams).pipe(finalize(() => {
        this.disabled = oldDisable;
        if (this.control) {
          this.control.updateValueAndValidity();
        }
      })).subscribe((element) => {
        if (element?.length || !Array.isArray(element) && element) {
          if (Array.isArray(element) && element.length > 1) {
            this.setDisclaimers(element);
            this.updateVisibleItems();
          }
          this.selectModel(Array.isArray(element) ? element : [element]);
        } else {
          this.cleanModel();
        }
      }, (error2) => {
        this.cleanModel();
        this.onError.emit(error2);
      });
    } else {
      this.cleanModel();
    }
  }
  validate(abstractControl) {
    if (PoValidators.requiredFailed(this.required, this.disabled, abstractControl.value)) {
      return {
        required: {
          valid: false
        }
      };
    }
  }
  writeValue(value) {
    if (value?.length || !Array.isArray(value) && value) {
      this.searchById(value);
    } else {
      this.cleanViewValue();
      this.controlChangeModelEmitter(void 0);
    }
  }
  // Retorna o Subscriber da propriedade getSubscription que ocorre
  // no evento de blur que executa o método searchEvent().
  getSubscriptionFunction() {
    return this.getSubscription;
  }
  cleanViewValue() {
    this.setDisclaimers([]);
    this.setViewValue("", {});
    this.oldValue = "";
    this.valueToModel = null;
  }
  // Formata a label do campo.
  getFormattedLabel(value) {
    return value ? this.keysDescription.map((column) => value[column]).join(" - ") : "";
  }
  // Chama o método writeValue e preenche o model.
  selectModel(options) {
    if (options.length) {
      this.selectedOptions = [...options];
      const newModel = this.multiple ? options.map((option) => option[this.fieldValue]) : options[0];
      this.selectValue(newModel);
      if (options.length === 1) {
        this.oldValue = options[0][this.fieldLabel];
        this.setViewValue(this.getFormattedLabel(options[0]), options[0]);
      }
    } else {
      this.selectValue(void 0);
      this.cleanViewValue();
    }
  }
  validateModel(model2) {
    if (this.validatorChange) {
      this.validatorChange(model2);
    }
  }
  setService(service) {
    if (isTypeof(service, "object")) {
      this.service = service;
    }
    if (service && isTypeof(service, "string")) {
      this.service = this.defaultService;
      this.service.setConfig(service, this.fieldValue, this.multiple);
    }
  }
  setControl() {
    const ngControl = this.injector.get(NgControl, null, this.injectOptions);
    if (ngControl) {
      this.control = ngControl.control;
    }
  }
  initializeColumn() {
    if (this.fieldLabel) {
      this.keysDescription = [this.fieldLabel];
    } else {
      this.keysDescription = [];
      this.keysDescription = this.columns.filter((element) => element.fieldLabel).map((element) => element.property);
    }
  }
  updateLookupInputHeight() {
    if (!this.inputEl) return;
    const height = this.inputEl.nativeElement.offsetHeight;
    if (this.size === PoFieldSize.Small) {
      this.isExpandedHeight = height > 32;
    } else {
      this.isExpandedHeight = height > 44;
    }
  }
  applySizeBasedOnA11y() {
    const size = validateSizeFn(this._initialSize, PoFieldSize);
    this._size = size;
  }
  static ɵfac = function PoLookupBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoLookupBaseComponent)(ɵɵdirectiveInject(PoLookupFilterService), ɵɵdirectiveInject(Injector), ɵɵdirectiveInject(PoLookupModalService), ɵɵdirectiveInject(PoLanguageService));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoLookupBaseComponent,
    viewQuery: function PoLookupBaseComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c49, 5, ElementRef);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.inputEl = _t.first);
      }
    },
    hostVars: 3,
    hostBindings: function PoLookupBaseComponent_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("PoUiThemeChange", function PoLookupBaseComponent_PoUiThemeChange_HostBindingHandler() {
          return ctx.onThemeChange();
        }, ɵɵresolveWindow);
      }
      if (rf & 2) {
        ɵɵattribute("p-size", ctx.size)("p-disabled", ctx.disabled)("p-loading", ctx.loading);
      }
    },
    inputs: {
      additionalHelpEventTrigger: "additionalHelpEventTrigger",
      additionalHelpTooltip: [0, "p-additional-help-tooltip", "additionalHelpTooltip"],
      appendBox: [2, "p-append-in-body", "appendBox", convertToBoolean],
      autoFocus: [2, "p-auto-focus", "autoFocus", convertToBoolean],
      label: [0, "p-label", "label"],
      literals: [0, "p-literals", "literals"],
      help: [0, "p-help", "help"],
      placeholder: [0, "p-placeholder", "placeholder"],
      name: "name",
      fieldValue: [0, "p-field-value", "fieldValue"],
      fieldLabel: [0, "p-field-label", "fieldLabel"],
      filterParams: [0, "p-filter-params", "filterParams"],
      fieldFormat: [0, "p-field-format", "fieldFormat"],
      columns: [0, "p-columns", "columns"],
      optional: [0, "p-optional", "optional"],
      advancedFilters: [0, "p-advanced-filters", "advancedFilters"],
      hideColumnsManager: [2, "p-hide-columns-manager", "hideColumnsManager", convertToBoolean],
      infiniteScroll: [2, "p-infinite-scroll", "infiniteScroll", convertToBoolean],
      clean: [2, "p-clean", "clean", convertToBoolean],
      multiple: [2, "p-multiple", "multiple", convertToBoolean],
      autoHeight: [2, "p-auto-height", "autoHeight", convertToBoolean],
      fieldErrorMessage: [0, "p-field-error-message", "fieldErrorMessage"],
      errorLimit: [0, "p-error-limit", "errorLimit"],
      size: [0, "p-size", "size"],
      spacing: [0, "p-spacing", "spacing"],
      textWrap: [2, "p-text-wrap", "textWrap", convertToBoolean],
      virtualScroll: [2, "p-virtual-scroll", "virtualScroll", convertToBoolean],
      poHelperComponent: [1, "p-helper", "poHelperComponent"],
      labelTextWrap: [1, "p-label-text-wrap", "labelTextWrap"],
      compactLabel: [1, "p-compact-label", "compactLabel"],
      filterService: [0, "p-filter-service", "filterService"],
      noAutocomplete: [2, "p-no-autocomplete", "noAutocomplete", convertToBoolean],
      required: [2, "p-required", "required", convertToBoolean],
      showRequired: [0, "p-show-required", "showRequired"],
      disabled: [2, "p-disabled", "disabled", convertToBoolean],
      loading: [2, "p-loading", "loading", convertToBoolean]
    },
    outputs: {
      additionalHelp: "p-additional-help",
      onError: "p-error",
      keydown: "p-keydown",
      selected: "p-selected",
      change: "p-change",
      changeModel: "p-change-model",
      changeVisibleColumns: "p-change-visible-columns",
      columnRestoreManager: "p-restore-column-manager"
    },
    features: [ɵɵNgOnChangesFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoLookupBaseComponent, [{
    type: Directive
  }], () => [{
    type: PoLookupFilterService
  }, {
    type: Injector,
    decorators: [{
      type: Inject,
      args: [Injector]
    }]
  }, {
    type: PoLookupModalService
  }, {
    type: PoLanguageService
  }], {
    inputEl: [{
      type: ViewChild,
      args: ["inp", {
        read: ElementRef,
        static: false
      }]
    }],
    additionalHelpEventTrigger: [{
      type: Input
    }],
    additionalHelpTooltip: [{
      type: Input,
      args: ["p-additional-help-tooltip"]
    }],
    appendBox: [{
      type: Input,
      args: [{
        alias: "p-append-in-body",
        transform: convertToBoolean
      }]
    }],
    autoFocus: [{
      type: Input,
      args: [{
        alias: "p-auto-focus",
        transform: convertToBoolean
      }]
    }],
    label: [{
      type: Input,
      args: ["p-label"]
    }],
    literals: [{
      type: Input,
      args: ["p-literals"]
    }],
    help: [{
      type: Input,
      args: ["p-help"]
    }],
    placeholder: [{
      type: Input,
      args: ["p-placeholder"]
    }],
    name: [{
      type: Input,
      args: ["name"]
    }],
    fieldValue: [{
      type: Input,
      args: ["p-field-value"]
    }],
    fieldLabel: [{
      type: Input,
      args: ["p-field-label"]
    }],
    filterParams: [{
      type: Input,
      args: ["p-filter-params"]
    }],
    fieldFormat: [{
      type: Input,
      args: ["p-field-format"]
    }],
    columns: [{
      type: Input,
      args: ["p-columns"]
    }],
    optional: [{
      type: Input,
      args: ["p-optional"]
    }],
    advancedFilters: [{
      type: Input,
      args: ["p-advanced-filters"]
    }],
    hideColumnsManager: [{
      type: Input,
      args: [{
        alias: "p-hide-columns-manager",
        transform: convertToBoolean
      }]
    }],
    infiniteScroll: [{
      type: Input,
      args: [{
        alias: "p-infinite-scroll",
        transform: convertToBoolean
      }]
    }],
    clean: [{
      type: Input,
      args: [{
        alias: "p-clean",
        transform: convertToBoolean
      }]
    }],
    multiple: [{
      type: Input,
      args: [{
        alias: "p-multiple",
        transform: convertToBoolean
      }]
    }],
    autoHeight: [{
      type: Input,
      args: [{
        alias: "p-auto-height",
        transform: convertToBoolean
      }]
    }],
    fieldErrorMessage: [{
      type: Input,
      args: ["p-field-error-message"]
    }],
    errorLimit: [{
      type: Input,
      args: ["p-error-limit"]
    }],
    size: [{
      type: Input,
      args: ["p-size"]
    }, {
      type: HostBinding,
      args: ["attr.p-size"]
    }],
    spacing: [{
      type: Input,
      args: ["p-spacing"]
    }],
    textWrap: [{
      type: Input,
      args: [{
        alias: "p-text-wrap",
        transform: convertToBoolean
      }]
    }],
    virtualScroll: [{
      type: Input,
      args: [{
        alias: "p-virtual-scroll",
        transform: convertToBoolean
      }]
    }],
    poHelperComponent: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-helper",
        required: false
      }]
    }],
    labelTextWrap: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-label-text-wrap",
        required: false
      }]
    }],
    compactLabel: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-compact-label",
        required: false
      }]
    }],
    additionalHelp: [{
      type: Output,
      args: ["p-additional-help"]
    }],
    onError: [{
      type: Output,
      args: ["p-error"]
    }],
    keydown: [{
      type: Output,
      args: ["p-keydown"]
    }],
    selected: [{
      type: Output,
      args: ["p-selected"]
    }],
    change: [{
      type: Output,
      args: ["p-change"]
    }],
    changeModel: [{
      type: Output,
      args: ["p-change-model"]
    }],
    changeVisibleColumns: [{
      type: Output,
      args: ["p-change-visible-columns"]
    }],
    columnRestoreManager: [{
      type: Output,
      args: ["p-restore-column-manager"]
    }],
    filterService: [{
      type: Input,
      args: ["p-filter-service"]
    }],
    noAutocomplete: [{
      type: Input,
      args: [{
        alias: "p-no-autocomplete",
        transform: convertToBoolean
      }]
    }],
    required: [{
      type: Input,
      args: [{
        alias: "p-required",
        transform: convertToBoolean
      }]
    }],
    showRequired: [{
      type: Input,
      args: ["p-show-required"]
    }],
    disabled: [{
      type: HostBinding,
      args: ["attr.p-disabled"]
    }, {
      type: Input,
      args: [{
        alias: "p-disabled",
        transform: convertToBoolean
      }]
    }],
    loading: [{
      type: HostBinding,
      args: ["attr.p-loading"]
    }, {
      type: Input,
      args: [{
        alias: "p-loading",
        transform: convertToBoolean
      }]
    }],
    onThemeChange: [{
      type: HostListener,
      args: ["window:PoUiThemeChange"]
    }]
  });
})();
var providers$2 = [PoLookupFilterService, PoLookupModalService, {
  provide: NG_VALUE_ACCESSOR,
  // eslint-disable-next-line
  useExisting: forwardRef(() => PoLookupComponent),
  multi: true
}, {
  provide: NG_VALIDATORS,
  // eslint-disable-next-line
  useExisting: forwardRef(() => PoLookupComponent),
  multi: true
}, {
  provide: NgControl,
  useExisting: forwardRef(() => PoLookupComponent),
  multi: false
}];
var PoLookupComponent = class _PoLookupComponent extends PoLookupBaseComponent {
  renderer = inject(Renderer2);
  cd = inject(ChangeDetectorRef);
  el = inject(ElementRef);
  helperEl;
  initialized = false;
  timeoutResize;
  visibleElement = false;
  heightGroupButtons = 44;
  disclaimers = [];
  visibleDisclaimers = [];
  id = `po-lookup[${uuid()}]`;
  modalSubscription;
  isCalculateVisibleItems = true;
  get autocomplete() {
    return this.noAutocomplete ? "off" : "on";
  }
  constructor() {
    const languageService = inject(PoLanguageService);
    const poLookupFilterService = inject(PoLookupFilterService);
    const poLookupModalService = inject(PoLookupModalService);
    const injector = inject(Injector);
    super(poLookupFilterService, injector, poLookupModalService, languageService);
  }
  ngAfterViewInit() {
    super.ngAfterViewInit();
    if (this.autoFocus) {
      this.focus();
    }
    this.initialized = true;
  }
  ngDoCheck() {
    const inputWidth = this.inputEl?.nativeElement.offsetWidth;
    if (inputWidth && !this.visibleElement && this.initialized || inputWidth && this.isCalculateVisibleItems) {
      this.debounceResize();
      this.visibleElement = true;
    }
    this.cd.markForCheck();
  }
  ngOnDestroy() {
    if (this.modalSubscription) {
      this.modalSubscription.unsubscribe();
    }
  }
  ngOnInit() {
    super.ngOnInit();
    this.initializeListeners();
  }
  ngOnChanges(changes) {
    if (changes.label) {
      this.displayAdditionalHelp = false;
    }
  }
  /**
   * Função que atribui foco ao componente.
   *
   * Para utilizá-la é necessário ter a instância do componente no DOM, podendo ser utilizado o ViewChild da seguinte forma:
   *
   * ```
   * import { PoLookupComponent } from '@po-ui/ng-components';
   *
   * ...
   *
   * @ViewChild(PoLookupComponent, { static: true }) lookup: PoLookupComponent;
   *
   * focusLookup() {
   *   this.lookup.focus();
   * }
   * ```
   */
  focus() {
    if (!this.disabled) {
      this.inputEl.nativeElement.focus();
    }
  }
  emitAdditionalHelp() {
    if (this.label && this.isAdditionalHelpEventTriggered()) {
      this.additionalHelp.emit();
    }
  }
  getAdditionalHelpTooltip() {
    return this.isAdditionalHelpEventTriggered() ? null : this.additionalHelpTooltip;
  }
  openLookup() {
    if (this.isAllowedOpenModal()) {
      const {
        advancedFilters,
        service,
        columns,
        filterParams,
        hideColumnsManager,
        literals,
        infiniteScroll,
        multiple,
        fieldLabel,
        fieldValue,
        size,
        spacing,
        textWrap,
        virtualScroll,
        changeVisibleColumns,
        columnRestoreManager
      } = this;
      const selectedItems = this.checkSelectedItems();
      this.poLookupModalService.openModal({
        advancedFilters,
        service,
        columns,
        filterParams,
        hideColumnsManager,
        title: this.label,
        literals,
        infiniteScroll,
        multiple,
        selectedItems,
        fieldLabel,
        fieldValue,
        size,
        spacing,
        textWrap,
        virtualScroll,
        changeVisibleColumns,
        columnRestoreManager
      });
      if (!this.modalSubscription) {
        this.modalSubscription = this.poLookupModalService.selectValueEvent.subscribe((selectedOptions) => {
          if (selectedOptions.length > 1 || this.disclaimers.length) {
            this.setDisclaimers(selectedOptions);
            this.updateVisibleItems();
          }
          this.selectModel(selectedOptions);
        });
      }
    }
  }
  checkSelectedItems() {
    if (this.multiple) {
      if (!this.disclaimers.length && this.valueToModel?.length) {
        return [__spreadValues({
          value: this.valueToModel[0],
          label: this.oldValue
        }, this.selectedOptions[0])];
      }
      return this.disclaimers;
    } else {
      return this.valueToModel;
    }
  }
  setDisclaimers(selectedOptions) {
    this.disclaimers = selectedOptions.map((selectedOption) => __spreadValues({
      value: selectedOption[this.fieldValue],
      label: selectedOption[this.fieldLabel]
    }, selectedOption));
    this.visibleDisclaimers = [...this.disclaimers];
    this.cd.markForCheck();
  }
  setViewValue(value, object) {
    if (this.inputEl && this.fieldFormat) {
      this.setInputValueWipoieldFormat(object);
    } else if (this.inputEl) {
      this.inputEl.nativeElement.value = this.valueToModel || this.valueToModel === 0 ? value : "";
    }
    this.cd.markForCheck();
  }
  getViewValue() {
    return this.inputEl.nativeElement.value;
  }
  getErrorPattern() {
    return this.fieldErrorMessage && this.hasInvalidClass() ? this.fieldErrorMessage : "";
  }
  hasInvalidClass() {
    return this.el.nativeElement.classList.contains("ng-invalid") && this.el.nativeElement.classList.contains("ng-dirty");
  }
  onBlur() {
    this.onTouched?.();
  }
  searchEvent() {
    this.onBlur();
    const value = this.getViewValue();
    if (this.oldValue?.toString() !== value) {
      this.searchById(value);
    }
  }
  closeDisclaimer(value) {
    this.disclaimers = this.disclaimers.filter((disclaimer) => disclaimer.value !== value);
    this.valueToModel = this.valueToModel.filter((model2) => model2 !== value);
    this.updateVisibleItems();
    this.callOnChange(this.valueToModel.length ? this.valueToModel : void 0);
  }
  updateVisibleItems() {
    if (this.disclaimers && this.disclaimers.length > 0) {
      this.visibleDisclaimers = [].concat(this.disclaimers);
    }
    this.debounceResize();
    if (!this.inputEl.nativeElement.offsetWidth) {
      this.isCalculateVisibleItems = true;
    }
  }
  debounceResize() {
    if (!this.autoHeight) {
      clearTimeout(this.timeoutResize);
      this.debounce(this.calculateVisibleItems.bind(this), 200);
    }
  }
  debounce(func, delay2) {
    this.timeoutResize = setTimeout(func, delay2);
  }
  getInputWidth() {
    return this.inputEl.nativeElement.offsetWidth - (this.clean ? 80 : 40);
  }
  getDisclaimersWidth() {
    const disclaimers = this.inputEl.nativeElement.querySelectorAll("po-disclaimer");
    return Array.from(disclaimers).map((disclaimer) => disclaimer["offsetWidth"]);
  }
  calculateVisibleItems() {
    const disclaimersWidth = this.getDisclaimersWidth();
    const inputWidth = this.getInputWidth();
    const extraDisclaimerSize = 38;
    const disclaimersVisible = disclaimersWidth[0];
    const newDisclaimers = [];
    const disclaimers = this.disclaimers;
    if (inputWidth > 0) {
      let sum2 = 0;
      let i2 = 0;
      for (i2 = 0; i2 < disclaimers.length; i2++) {
        sum2 += disclaimersWidth[i2];
        newDisclaimers.push(disclaimers[i2]);
        if (sum2 > inputWidth) {
          sum2 -= disclaimersWidth[i2];
          this.isCalculateVisibleItems = false;
          break;
        }
      }
      if (disclaimersVisible || !disclaimers.length) {
        if (i2 === disclaimers.length) {
          this.isCalculateVisibleItems = false;
          return;
        }
        if (sum2 + extraDisclaimerSize > inputWidth) {
          newDisclaimers.splice(-2, 2);
          const label = "+" + (disclaimers.length + 1 - i2).toString();
          newDisclaimers.push({
            value: "",
            label
          });
        } else {
          newDisclaimers.splice(-1, 1);
          const label = "+" + (disclaimers.length - i2).toString();
          newDisclaimers.push({
            value: "",
            label
          });
        }
      }
    }
    this.visibleDisclaimers = [...newDisclaimers];
  }
  onKeyDown(event) {
    const isFieldFocused = document.activeElement === this.inputEl.nativeElement;
    if (isFieldFocused) {
      this.keydown.emit(event);
    }
  }
  /**
   *
   * Método que exibe `p-helper` ou executa a ação definida em `p-helper{eventOnClick}` ou em `p-additionalHelp`.
   * Para isso, será necessário configurar uma tecla de atalho utilizando o evento `p-keydown`.
   *
   * > Exibe ou oculta o conteúdo do componente `po-helper` quando o componente estiver com foco.
   *
   * ```
   * // Exemplo com p-label e p-helper
   * <po-lookup
   *  #lookup
   *  ...
   *  p-label="Label do lookup"
   *  [p-helper]="helperOptions"
   *  (p-keydown)="onKeyDown($event, lookup)"
   * ></po-lookup>
   * ```
   * ```
   * ...
   * onKeyDown(event: KeyboardEvent, inp: PoLookupComponent): void {
   *  if (event.code === 'F9') {
   *    inp.showAdditionalHelp();
   *  }
   * }
   * ```
   */
  showAdditionalHelp() {
    this.displayAdditionalHelp = !this.displayAdditionalHelp;
    const helper = this.poHelperComponent();
    const isHelpEvt = this.isAdditionalHelpEventTriggered();
    if (!this.label && (helper || this.additionalHelpTooltip || isHelpEvt)) {
      if (isHelpEvt) {
        this.additionalHelp.emit();
      }
      if (typeof helper !== "string" && typeof helper?.eventOnClick === "function") {
        console.trace("Chamando eventOnClick do helper");
        helper.eventOnClick();
        return;
      }
      if (this.helperEl?.helperIsVisible()) {
        this.helperEl?.closeHelperPopover();
        return;
      }
      this.helperEl?.openHelperPopover();
      return;
    }
    return this.displayAdditionalHelp;
  }
  setHelper(label, additionalHelpTooltip) {
    return setHelperSettings(label, additionalHelpTooltip, this.poHelperComponent(), this.size, this.isAdditionalHelpEventTriggered() ? this.additionalHelp : void 0);
  }
  getDefaultSpacing() {
    return this.size === PoFieldSize.Small || getDefaultSizeFn(PoTableColumnSpacing) === PoTableColumnSpacing.Small ? PoTableColumnSpacing.ExtraSmall : PoTableColumnSpacing.Medium;
  }
  isAdditionalHelpEventTriggered() {
    return this.additionalHelpEventTrigger === "event" || this.additionalHelpEventTrigger === void 0 && this.additionalHelp.observed;
  }
  isAllowedOpenModal() {
    if (!this.service) {
      console.warn("No service informed");
    }
    return !!(this.service && !this.disabled);
  }
  formatFields(objectSelected, properties) {
    let formatedField;
    if (Array.isArray(properties)) {
      for (const property of properties) {
        if (objectSelected && objectSelected[property]) {
          if (!formatedField) {
            formatedField = objectSelected[property];
          } else {
            formatedField = formatedField + " - " + objectSelected[property];
          }
        }
      }
    }
    if (!formatedField) {
      formatedField = objectSelected[this.fieldValue];
    }
    return formatedField;
  }
  setInputValueWipoieldFormat(objectSelected) {
    const isEmpty = Object.keys(objectSelected).length === 0;
    let fieldFormated;
    if (Array.isArray(this.fieldFormat)) {
      fieldFormated = this.formatFields(objectSelected, this.fieldFormat);
    } else {
      fieldFormated = this.fieldFormat(objectSelected);
    }
    this.oldValue = isEmpty ? "" : fieldFormated;
    this.inputEl.nativeElement.value = isEmpty ? "" : fieldFormated;
  }
  initializeListeners() {
    this.resizeListener = this.renderer.listen("window", "resize", () => {
      this.updateVisibleItems();
    });
  }
  static ɵfac = function PoLookupComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoLookupComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoLookupComponent,
    selectors: [["po-lookup"]],
    viewQuery: function PoLookupComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c17, 5, PoHelperComponent);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.helperEl = _t.first);
      }
    },
    standalone: false,
    features: [ɵɵProvidersFeature(providers$2), ɵɵInheritDefinitionFeature, ɵɵNgOnChangesFeature],
    decls: 8,
    vars: 20,
    consts: [["poHelperTemplate", ""], ["helperEl", ""], ["inp", ""], ["iconLookup", ""], [3, "p-disabled", "p-id", "p-label", "p-optional", "p-required", "p-show-required", "p-helper", "p-show-helper", "p-size", "p-text-wrap", "p-compact-label"], [1, "po-field-container-content", "po-lookup", 3, "po-lookup-disabled", "po-lookup-loading"], [3, "p-append-in-body", "p-help", "p-disabled", "p-error-pattern", "p-error-limit", "p-size"], [1, "po-field-helper-button", 3, "p-size", "p-helper", "p-disabled", "p-append-in-body"], [1, "po-field-container-content", "po-lookup"], [1, "po-field-container-content-helper-wrapper"], [1, "po-field-container-input"], ["type", "text", 1, "po-lookup-input", "po-lookup-input-trigger", 3, "blur", "keydown", "ngClass", "id", "autocomplete", "disabled", "placeholder", "required"], [1, "po-field-icon-container-right", "po-lookup-buttons"], [1, "po-lookup-button", "po-lookup-button-loading"], ["tabindex", "0", "role", "button", 1, "po-lookup-button", "po-lookup-button-trigger", "po-field-icon", 3, "p-change-event", "click", "keydown.enter", "ariaLabel"], ["p-icon", "ICON_SEARCH"], [1, "po-lookup-loading-icon", 3, "p-size"], ["tabindex", "0", "role", "button", 1, "po-lookup-button", "po-lookup-button-clean", 3, "po-lookup-button-aa", "p-element-ref", "p-size"], ["tabindex", "0", "role", "button", 1, "po-lookup-button", "po-lookup-button-clean", 3, "p-change-event", "click", "keydown.enter", "p-element-ref", "p-size"], [4, "ngTemplateOutlet"], [1, "po-lookup-input", "po-input-icon-right", "po-icon-input", 3, "blur", "keydown", "tabindex", "ngClass"], [1, "po-lookup-input-placeholder"], [1, "po-lookup-input-disclaimer", 3, "p-label", "p-value", "p-hide-close", "po-clickable"], [1, "po-lookup-input-disclaimer", 3, "p-close-action", "p-label", "p-value", "p-hide-close"], ["tabindex", "0", "role", "button", 1, "po-lookup-button", "po-lookup-button-clean", 3, "p-element-ref", "p-size"]],
    template: function PoLookupComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵdeclareLet(0)(1);
        ɵɵtemplate(2, PoLookupComponent_ng_template_2_Template, 2, 4, "ng-template", null, 0, ɵɵtemplateRefExtractor);
        ɵɵelementStart(4, "po-field-container", 4);
        ɵɵconditionalCreate(5, PoLookupComponent_Conditional_5_Template, 12, 19, "div", 5)(6, PoLookupComponent_Conditional_6_Template, 15, 26, "div", 5);
        ɵɵelement(7, "po-field-container-bottom", 6);
        ɵɵelementEnd();
      }
      if (rf & 2) {
        const poHelper_r13 = ɵɵstoreLet(ctx.setHelper(ctx.label, ctx.additionalHelpTooltip));
        ɵɵadvance();
        ɵɵstoreLet(!ctx.label && (ctx.poHelperComponent() || poHelper_r13.hideAdditionalHelp));
        ɵɵadvance(3);
        ɵɵproperty("p-disabled", ctx.isDisabled)("p-id", ctx.id)("p-label", ctx.label)("p-optional", ctx.optional)("p-required", ctx.required)("p-show-required", ctx.showRequired)("p-helper", ctx.label ? poHelper_r13.helperSettings : void 0)("p-show-helper", ctx.label ? ctx.displayAdditionalHelp : void 0)("p-size", ctx.size)("p-text-wrap", ctx.labelTextWrap())("p-compact-label", ctx.compactLabel());
        ɵɵadvance();
        ɵɵconditional(!ctx.disclaimers.length ? 5 : 6);
        ɵɵadvance(2);
        ɵɵproperty("p-append-in-body", ctx.appendBox)("p-help", ctx.help)("p-disabled", ctx.isDisabled)("p-error-pattern", ctx.getErrorPattern())("p-error-limit", ctx.errorLimit)("p-size", ctx.size);
      }
    },
    dependencies: [NgClass, NgTemplateOutlet, PoCleanComponent, PoDisclaimerComponent, PoFieldContainerBottomComponent, PoFieldContainerComponent, PoHelperComponent, PoIconComponent, PoLoadingIconComponent],
    encapsulation: 2,
    changeDetection: 0
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoLookupComponent, [{
    type: Component,
    args: [{
      selector: "po-lookup",
      changeDetection: ChangeDetectionStrategy.OnPush,
      providers: providers$2,
      standalone: false,
      template: `@let poHelper = setHelper(label, additionalHelpTooltip);
@let needsHelper = !label && (poHelperComponent() || poHelper.hideAdditionalHelp);

<ng-template #poHelperTemplate>
  <po-helper
    #helperEl
    class="po-field-helper-button"
    [p-size]="size"
    [p-helper]="poHelper.helperSettings"
    [p-disabled]="isDisabled"
    [p-append-in-body]="appendBox"
  ></po-helper>
</ng-template>

<po-field-container
  [p-disabled]="isDisabled"
  [p-id]="id"
  [p-label]="label"
  [p-optional]="optional"
  [p-required]="required"
  [p-show-required]="showRequired"
  [p-helper]="label ? poHelper.helperSettings : undefined"
  [p-show-helper]="label ? displayAdditionalHelp : undefined"
  [p-size]="size"
  [p-text-wrap]="labelTextWrap()"
  [p-compact-label]="compactLabel()"
>
  @if (!disclaimers.length) {
    <div
      class="po-field-container-content po-lookup"
      [class.po-lookup-disabled]="disabled"
      [class.po-lookup-loading]="loading"
    >
      <div class="po-field-container-content-helper-wrapper">
        <div class="po-field-container-input">
          <input
            #inp
            class="po-lookup-input po-lookup-input-trigger"
            [ngClass]="clean && inp.value ? 'po-input-double-icon-right' : 'po-input-icon-right'"
            [id]="id"
            type="text"
            [autocomplete]="autocomplete"
            [disabled]="isDisabled"
            [placeholder]="placeholder"
            [required]="required"
            (blur)="searchEvent()"
            (keydown)="onKeyDown($event)"
          />
          <div class="po-field-icon-container-right po-lookup-buttons">
            @if (loading) {
              <div class="po-lookup-button po-lookup-button-loading">
                <po-loading-icon class="po-lookup-loading-icon" [p-size]="mapSizeToIcon(size)"></po-loading-icon>
              </div>
            } @else {
              @if (clean && !isDisabled && !!inp.value) {
                <po-clean
                  tabindex="0"
                  role="button"
                  [attr.aria-label]="literals.clean"
                  class="po-lookup-button po-lookup-button-clean"
                  [class.po-lookup-button-aa]="size === 'small'"
                  [p-element-ref]="inp"
                  [p-size]="size"
                  (p-change-event)="cleanModel()"
                  (click)="cleanModel(); $event.preventDefault()"
                  (keydown.enter)="cleanModel(); $event.preventDefault()"
                ></po-clean>
              }
            }
            <div
              #iconLookup
              tabindex="0"
              role="button"
              class="po-lookup-button po-lookup-button-trigger po-field-icon"
              [class.po-field-icon-disabled]="isDisabled"
              [ariaLabel]="literals.search"
              (p-change-event)="openLookup()"
              (click)="!isDisabled && openLookup(); $event.preventDefault()"
              (keydown.enter)="!isDisabled && openLookup(); $event.preventDefault()"
            >
              <po-icon
                p-icon="ICON_SEARCH"
                [class.po-field-icon]="!isDisabled"
                [class.po-field-icon-aa]="!isDisabled && size === 'small'"
              ></po-icon>
            </div>
          </div>
        </div>

        @if (needsHelper) {
          <ng-container *ngTemplateOutlet="poHelperTemplate"></ng-container>
        }
      </div>
    </div>
  } @else {
    <div
      class="po-field-container-content po-lookup"
      [class.po-lookup-disabled]="disabled"
      [class.po-lookup-loading]="loading"
    >
      <div class="po-field-container-content-helper-wrapper">
        <div class="po-field-container-input">
          <div
            #inp
            [tabindex]="isDisabled ? -1 : 0"
            class="po-lookup-input po-input-icon-right po-icon-input"
            [ngClass]="clean && inp.value ? 'po-input-double-icon-right' : 'po-input-icon-right'"
            [class.po-lookup-input-auto]="autoHeight"
            [class.po-lookup-input-static]="!autoHeight"
            [class.po-lookup-input-padding-button-clean]="clean || loading"
            [class.po-lookup-input-disabled]="isDisabled"
            [class.po-lookup-input-auto-dynamic-height]="isExpandedHeight"
            (blur)="onBlur()"
            (keydown)="onKeyDown($event)"
          >
            @if (placeholder && !disclaimers?.length) {
              <span class="po-lookup-input-placeholder">{{ placeholder }}</span>
            }
            @for (disclaimer of visibleDisclaimers; track $index) {
              <po-disclaimer
                class="po-lookup-input-disclaimer"
                [p-label]="disclaimer.label"
                [p-value]="disclaimer.value"
                [p-hide-close]="disclaimer.value === '' || isDisabled"
                [class.po-clickable]="disclaimer.value === '' && !isDisabled"
                (p-close-action)="closeDisclaimer(disclaimer.value)"
              ></po-disclaimer>
            }
          </div>
          <div class="po-field-icon-container-right po-lookup-buttons">
            @if (loading) {
              <div class="po-lookup-button po-lookup-button-loading">
                <po-loading-icon class="po-lookup-loading-icon" [p-size]="mapSizeToIcon(size)"></po-loading-icon>
              </div>
            } @else {
              @if (clean && !isDisabled && !!inp.value) {
                <po-clean
                  tabindex="0"
                  role="button"
                  [attr.aria-label]="literals.clean"
                  class="po-lookup-button po-lookup-button-clean"
                  [p-element-ref]="inp"
                  [p-size]="size"
                  (p-change-event)="cleanModel()"
                  (click)="cleanModel(); $event.preventDefault()"
                  (keydown.enter)="cleanModel(); $event.preventDefault()"
                ></po-clean>
              }
            }
            <div
              #iconLookup
              tabindex="0"
              role="button"
              class="po-lookup-button po-lookup-button-trigger po-field-icon"
              [class.po-field-icon-disabled]="isDisabled"
              [ariaLabel]="literals.search"
              (p-change-event)="openLookup()"
              (click)="!isDisabled && openLookup(); $event.preventDefault()"
              (keydown.enter)="!isDisabled && openLookup(); $event.preventDefault()"
            >
              <po-icon
                p-icon="ICON_SEARCH"
                [class.po-field-icon]="!isDisabled"
                [class.po-field-icon-aa]="!isDisabled && size === 'small'"
              ></po-icon>
            </div>
          </div>
        </div>

        @if (needsHelper) {
          <ng-container *ngTemplateOutlet="poHelperTemplate"></ng-container>
        }
      </div>
    </div>
  }

  <po-field-container-bottom
    [p-append-in-body]="appendBox"
    [p-help]="help"
    [p-disabled]="isDisabled"
    [p-error-pattern]="getErrorPattern()"
    [p-error-limit]="errorLimit"
    [p-size]="size"
  ></po-field-container-bottom>
</po-field-container>
`
    }]
  }], () => [], {
    helperEl: [{
      type: ViewChild,
      args: ["helperEl", {
        read: PoHelperComponent,
        static: false
      }]
    }]
  });
})();
var PoMultiselectOptionTemplateDirective = class _PoMultiselectOptionTemplateDirective {
  templateRef;
  // Necessário manter templateRef para o funcionamento do row template.
  constructor(templateRef) {
    this.templateRef = templateRef;
  }
  static ɵfac = function PoMultiselectOptionTemplateDirective_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoMultiselectOptionTemplateDirective)(ɵɵdirectiveInject(TemplateRef));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoMultiselectOptionTemplateDirective,
    selectors: [["", "p-multiselect-option-template", ""]],
    standalone: false
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoMultiselectOptionTemplateDirective, [{
    type: Directive,
    args: [{
      selector: "[p-multiselect-option-template]",
      standalone: false
    }]
  }], () => [{
    type: TemplateRef
  }], null);
})();
var PoMultiselectFilterMode;
(function(PoMultiselectFilterMode2) {
  PoMultiselectFilterMode2[PoMultiselectFilterMode2["startsWith"] = 0] = "startsWith";
  PoMultiselectFilterMode2[PoMultiselectFilterMode2["contains"] = 1] = "contains";
  PoMultiselectFilterMode2[PoMultiselectFilterMode2["endsWith"] = 2] = "endsWith";
})(PoMultiselectFilterMode || (PoMultiselectFilterMode = {}));
var PO_MULTISELECT_DEBOUNCE_TIME_DEFAULT = 400;
var PO_MULTISELECT_FIELD_LABEL_DEFAULT = "label";
var PO_MULTISELECT_FIELD_VALUE_DEFAULT = "value";
var poMultiselectContainerPositionDefault = "bottom";
var poMultiselectLiteralsDefault = {
  en: {
    noData: "No data found",
    placeholderSearch: "Search",
    selectAll: "Select all",
    selectItem: "Select items"
  },
  es: {
    noData: "Datos no encontrados",
    placeholderSearch: "Busca",
    selectAll: "Seleccionar todo",
    selectItem: "Seleccionar items"
  },
  pt: {
    noData: "Nenhum dado encontrado",
    placeholderSearch: "Buscar",
    selectAll: "Selecionar todos",
    selectItem: "Selecionar itens"
  },
  ru: {
    noData: "Данные не найдены",
    placeholderSearch: "искать",
    selectAll: "Выбрать все",
    selectItem: "Выбрать элементы"
  }
};
var PoMultiselectBaseComponent = class _PoMultiselectBaseComponent {
  cd;
  inputElement;
  // Propriedade interna que define se o ícone de ajuda adicional terá cursor clicável (evento) ou padrão (tooltip).
  additionalHelpEventTrigger;
  /**
   *
   * @deprecated v23.x.x use `p-helper`
   *
   * @optional
   *
   * @description
   * Exibe um ícone de ajuda adicional, com o texto desta propriedade sendo passado para o popover do componente `po-helper`.
   * **Como boa prática, indica-se utilizar um texto com até 140 caracteres.**
   * > Requer um recuo mínimo de 8px se o componente estiver próximo à lateral da tela.
   *
   * > Essa propriedade está **depreciada** e será removida na versão `23.x.x`. Recomendamos utilizar a propriedade `p-helper` que oferece mais recursos e flexibilidade.
   */
  additionalHelpTooltip;
  /**
   * @optional
   *
   * @description
   *
   * Aplica foco no elemento ao ser iniciado.
   *
   * > Caso mais de um elemento seja configurado com essa propriedade, apenas o último elemento declarado com ela terá o foco.
   *
   * @default `false`
   */
  autoFocus = false;
  /**
   * @optional
   *
   * @description
   * Define se o título do campo será exibido de forma compacta.
   *
   * Quando habilitado (`true`), o modo compacto afeta o conjunto composto por:
   * - `po-label`
   * - `p-requirement (showRequired)`
   * - `po-helper`
   *
   * Ou seja, todos os elementos relacionados ao título do campo
   * (rótulo, indicador de obrigatoriedade e componente auxiliar) passam
   * a seguir o comportamento de layout compacto.
   *
   * Também é possível definir esse comportamento de forma global,
   * uma única vez, na folha de estilo geral da aplicação, por meio
   * da customização dos tokens CSS:
   *
   * - `--field-container-title-justify`
   * - `--field-container-title-flex`
   *
   * Exemplo:
   *
   * ```
   * :root {
   *   --field-container-title-justify: flex-start;
   *   --field-container-title-flex: 0 1 auto;
   * }
   * ```
   *
   * Dessa forma, o layout compacto passa a ser o padrão da aplicação,
   * sem a necessidade de definir a propriedade individualmente em cada campo.
   *
   * @default `false`
   */
  compactLabel = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "compactLabel"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-compact-label",
    transform: convertToBoolean
  }));
  /**
   * @optional
   *
   * @description
   *
   * Evento disparado ao sair do campo.
   */
  blur = new EventEmitter();
  /** Label no componente. */
  label;
  /** Texto de apoio para o campo. */
  help;
  /**
   * @optional
   *
   * @description
   *
   * Define se a indicação de campo opcional será exibida.
   *
   * > Não será exibida a indicação se:
   * - O campo conter `p-required`;
   * - Não possuir `p-help` e/ou `p-label`.
   *
   * @default `false`
   */
  optional;
  /** Mensagem apresentada enquanto o campo estiver vazio. */
  placeholder = "";
  /**
   * @description
   *
   * Placeholder do campo de pesquisa.
   *
   * > Caso o mesmo não seja informado, o valor padrão será traduzido com base no idioma do navegador (pt, es e en).
   *
   * @default `Buscar`
   */
  placeholderSearch = "";
  /** Nome do componente. */
  name;
  /**
   * @optional
   *
   * @description
   *
   * Indica se o campo "Selecionar todos" será escondido.
   *
   * @default `false`
   */
  hideSelectAll;
  /**
   * @optional
   *
   * @description
   *
   * Exibe a mensagem setada se o campo estiver vazio e for requerido.
   *
   * > Necessário que a propriedade `p-required` esteja habilitada.
   *
   */
  fieldErrorMessage;
  /**
   * @optional
   *
   * @description
   *
   * Limita a exibição da mensagem de erro a duas linhas e exibe um tooltip com o texto completo.
   *
   * > Caso essa propriedade seja definida como `true`, a mensagem de erro será limitada a duas linhas
   * e um tooltip será exibido ao passar o mouse sobre a mensagem para mostrar o conteúdo completo.
   *
   * @default `false`
   */
  errorLimit = false;
  /**
   *
   * @deprecated v23.x.x use `p-helper`
   *
   * @optional
   *
   * @description
   * Evento disparado ao clicar no ícone de ajuda adicional.
   *
   * > Essa propriedade está **depreciada** e será removida na versão `23.x.x`. Recomendamos utilizar a propriedade `p-helper` que oferece mais recursos e flexibilidade.
   */
  additionalHelp = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Pode ser informada uma função que será disparada quando houver alterações no ngModel.
   */
  change = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Evento disparado sempre que o valor do model é alterado, seja por interação do usuário
   * ou por atualização programática (ex: `setValue`, `patchValue`, carregamento assíncrono).
   *
   * Diferentemente do `p-change`, que é disparado apenas por interação do usuário,
   * o `p-change-model` cobre todos os cenários de alteração de valor.
   *
   * Não emite quando o novo valor é idêntico ao anterior (deduplicação automática).
   */
  changeModel = new EventEmitter();
  /**
   * @optional
   *
   * @description
   * Evento disparado quando uma tecla é pressionada enquanto o foco está no componente.
   * Retorna um objeto `KeyboardEvent` com informações sobre a tecla.
   */
  keydown = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Define que o `listbox` e/ou popover (`p-helper` e/ou `p-error-limit`) serão incluídos no body da
   * página e não dentro do componente. Essa opção pode ser necessária em cenários com containers que possuem scroll ou
   * overflow escondido, garantindo o posicionamento correto de ambos próximo ao elemento.
   *
   * > Quando utilizado com `p-helper`, leitores de tela como o NVDA podem não ler o conteúdo do popover.
   *
   * @default `false`
   */
  appendBox = false;
  /**
   * @docsPrivate
   *
   * Determinar se o valor do compo deve retorna objeto do tipo {value: any, label: any}
   */
  controlValueWithLabel = false;
  /**
   * @optional
   *
   * @description
   *
   * Define a direção preferida para exibição do `listbox` em relação ao campo (`top` ou `bottom`).
   * Útil em casos onde o posicionamento automático não se comporta como esperado, como quando o componente está próximo
   * ao final do formulário ou do container visível. Na maioria dos casos, essa direção será respeitada; no entanto,
   * pode ser ajustada automaticamente conforme o espaço disponível na tela.
   *
   * @default `bottom`
   */
  listboxControlPosition = poMultiselectContainerPositionDefault;
  /**
   * @optional
   *
   * @description
   *
   * Define as opções do componente de ajuda (po-helper) que será exibido ao lado do label quando a propriedade `p-label` for definida, ou, ao lado do componente na ausência da propriedade `p-label`.
   * > Para mais informações acesse: https://po-ui.io/documentation/po-helper.
   *
   * > Ao configurar esta propriedade, o antigo ícone de ajuda adicional (`p-additional-help-tooltip` e `p-additional-help`) será ignorado.
   */
  poHelperComponent = input(void 0, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "poHelperComponent"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-helper"
  }));
  /**
   * @optional
   *
   * @description
   *
   * Habilita a quebra automática do texto da propriedade `p-label`. Quando `p-label-text-wrap` for verdadeiro, o texto que excede
   * o espaço disponível é transferido para a próxima linha em pontos apropriados para uma
   * leitura clara.
   *
   * @default `false`
   */
  labelTextWrap = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "labelTextWrap"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-label-text-wrap"
  }));
  selectedOptions = [];
  visibleOptionsDropdown = [];
  visibleTags = [];
  isServerSearching = false;
  isFirstFilter = true;
  filterSubject = new Subject();
  service;
  defaultService;
  displayAdditionalHelp = false;
  modelLastUpdate;
  // eslint-disable-next-line
  onModelTouched = null;
  clickOutListener;
  resizeListener;
  getObjectsByValuesSubscription;
  isExpandedHeight = false;
  _filterService;
  _debounceTime = 400;
  _disabled = false;
  _filterMode = PoMultiselectFilterMode.startsWith;
  _hideSearch = false;
  _literals;
  _loading = false;
  _options;
  _required = false;
  _sort = false;
  _autoHeight = false;
  _fieldLabel = PO_MULTISELECT_FIELD_LABEL_DEFAULT;
  _fieldValue = PO_MULTISELECT_FIELD_VALUE_DEFAULT;
  _size = void 0;
  _initialSize = void 0;
  language;
  lastLengthModel;
  onModelChange;
  validatorChange;
  autoHeightInitialValue;
  /**
   * @optional
   *
   * @description
   * Nesta propriedade pode ser informada a URL do serviço em que será realizado o filtro para carregamento da lista de itens no componente.
   *
   *Também existe a possibilidade de informar um serviço implementando a interface `PoMultiselectFilter`.
   *
   *Caso utilizado uma URL, o serviço deve ser retornado no padrão [API PO UI](https://po-ui.io/guides/api) e utilizar as propriedades `p-field-label` e `p-field-value` para a construção da lista de itens.
   *
   *Quando utilizada uma URL de serviço, então será concatenada nesta URL o valor que deseja-se filtrar da seguinte forma:
   *
   *```
   * // caso filtrar por "Peter"
   *  https://localhost:8080/api/heroes?filter=Peter
   *```
   *
   *E caso iniciar o campo com valor, os itens serão buscados da seguinte forma:
   *
   *```
   * // caso o valor do campo for [1234, 5678];
   *  https://localhost:8080/api/heroes?value=1234,5678
   *
   * //O *value* é referente ao `fieldValue`.
   *```
   *
   */
  set filterService(value) {
    if (value) {
      this._filterService = value;
      this.autoHeight = this.autoHeightInitialValue !== void 0 ? this.autoHeightInitialValue : true;
      this.options = [];
    }
  }
  get filterService() {
    return this._filterService;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define que a altura do componente será auto ajustável, possuindo uma altura minima porém a altura máxima será de acordo
   * com o número de itens selecionados e a extensão dos mesmos, mantendo-os sempre visíveis.
   *
   * > O valor padrão será `true` quando houver serviço (`p-filter-service`).
   *
   * @default `false`
   */
  set autoHeight(value) {
    this._autoHeight = value;
    this.autoHeightInitialValue = value;
  }
  get autoHeight() {
    return this._autoHeight;
  }
  /**
   * @optional
   *
   * @description
   * Esta propriedade define em quanto tempo (em milissegundos), aguarda para acionar o evento de filtro após cada pressionamento de tecla.
   *
   * > Será utilizada apenas quando houver serviço (`p-filter-service`) e somente será aceito valor maior do que *zero*.
   *
   * @default `400`
   */
  set debounceTime(value) {
    const parsedValue = parseInt(value, 10);
    this._debounceTime = !isNaN(parsedValue) && parsedValue > 0 ? parsedValue : PO_MULTISELECT_DEBOUNCE_TIME_DEFAULT;
  }
  get debounceTime() {
    return this._debounceTime;
  }
  /**
   * @optional
   *
   * @description
   *
   * Objeto com as literais usadas no `po-multiselect`.
   *
   * Existem duas maneiras de customizar o componente, passando um objeto com todas as literais disponíveis:
   *
   * ```
   *  const customLiterals: PoMultiselectLiterals = {
   *    noData: 'Nenhum dado encontrado',
   *    placeholderSearch: 'Buscar',
   *    selectAll: 'Select all',
   *    selectItem: 'Select items'
   *  };
   * ```
   *
   * Ou passando apenas as literais que deseja customizar:
   *
   * ```
   *  const customLiterals: PoMultiselectLiterals = {
   *    noData: 'Sem dados'
   *  };
   * ```
   *
   * E para carregar as literais customizadas, basta apenas passar o objeto para o componente:
   *
   * ```
   * <po-multiselect
   *   [p-literals]="customLiterals">
   * </po-po-multiselect>
   * ```
   *
   * > O objeto padrão de literais será traduzido de acordo com o idioma do
   * [`PoI18nService`](/documentation/po-i18n) ou do browser.
   */
  set literals(value) {
    if (value instanceof Object && !(value instanceof Array)) {
      this._literals = __spreadValues(__spreadValues(__spreadValues({}, poMultiselectLiteralsDefault[poLocaleDefault]), poMultiselectLiteralsDefault[this.language]), value);
    } else {
      this._literals = poMultiselectLiteralsDefault[this.language];
    }
  }
  get literals() {
    return this._literals || poMultiselectLiteralsDefault[this.language];
  }
  /**
   * @optional
   *
   * @description
   *
   * Define que o campo será obrigatório.
   *
   * > Esta propriedade é desconsiderada quando o input está desabilitado `(p-disabled)`.
   *
   * @default `false`
   */
  set required(required) {
    this._required = required;
    this.validateModel();
  }
  get required() {
    return this._required;
  }
  /**
   * Define se a indicação de campo obrigatório será exibida.
   *
   * > Não será exibida a indicação se:
   * - Não possuir `p-help` e/ou `p-label`.
   */
  showRequired = false;
  /**
   * @optional
   *
   * @description
   *
   * Define o tamanho do componente:
   * - `small`: altura do input como 32px (disponível apenas para acessibilidade AA).
   * - `medium`: altura do input como 44px.
   *
   * > Caso a acessibilidade AA não esteja configurada, o tamanho `medium` será mantido.
   * Para mais detalhes, consulte a documentação do [po-theme](https://po-ui.io/documentation/po-theme).
   *
   * @default `medium`
   */
  set size(value) {
    this._initialSize = value;
    this.applySizeBasedOnA11y();
  }
  get size() {
    return this._size ?? getDefaultSizeFn(PoFieldSize);
  }
  /**
   * @optional
   *
   * @description
   *
   * Indica que o campo será desabilitado.
   *
   * @default `false`
   */
  set disabled(disabled) {
    this._disabled = disabled;
    this.validateModel();
    this.updateVisibleItems();
  }
  get disabled() {
    return this._disabled;
  }
  /**
   * @optional
   *
   * @description
   * Exibe um ícone de carregamento no lado direito do campo para sinalizar que uma operação está em andamento.
   *
   * @default `false`
   */
  set loading(value) {
    this._loading = value;
    this.cd?.markForCheck();
  }
  get loading() {
    return this._loading;
  }
  get isDisabled() {
    return this.disabled || this.loading;
  }
  /**
   * @optional
   *
   * @description
   *
   * Esconde o campo de pesquisa existente dentro do dropdown do po-multiselect.
   *
   * @default `false`
   */
  set hideSearch(hideSearch) {
    this._hideSearch = hideSearch;
  }
  get hideSearch() {
    return this._hideSearch;
  }
  /**
   * @description
   *
   * Nesta propriedade deve ser definida uma lista de objetos que será exibida no multiselect.
   * Esta lista deve conter os valores e os labels que serão apresentados na tela.
   *
   * > Essa propriedade é imutável, ou seja, sempre que quiser atualizar a lista de opções disponíveis
   * atualize a referência do objeto:
   *
   * ```
   * // atualiza a referência do objeto garantindo a atualização do template
   * this.options = [...this.options, { value: 'x', label: 'Nova opção' }];
   *
   * // evite, pois não atualiza a referência do objeto podendo gerar atrasos na atualização do template
   * this.options.push({ value: 'x', label: 'Nova opção' });
   * ```
   * > A lista pode ser definida utilizando um array com o valor representando `value` e `label` das seguintes formas:
   *
   * ```
   * <po-multiselect name="multiselect" p-label="PO Multiselect" [p-options]="[{value: 1, label: 'One'}, {value: 2, label: 'two'}]"> </po-multiselect>
   * ```
   *
   * ```
   * <po-multiselect name="multiselect" p-label="PO Multiselect" [p-options]="[{name: 'Roger', age: 28}, {name: 'Anne', age: 35}]" p-field-label="name" p-field-value="age"> </po-multiselect>
   * ```
   *
   * - Aconselha-se utilizar valores distintos no `label` e `value` dos itens.
   */
  set options(options) {
    this._options = options;
  }
  get options() {
    return this._options;
  }
  /**
   * @optional
   *
   * @description
   *
   * Indica que a lista definida na propriedade p-options será ordenada pelo label antes de ser apresentada no
   * dropdown.
   *
   * @default `false`
   */
  set sort(sort3) {
    this._sort = sort3;
    this.validAndSortOptions();
  }
  get sort() {
    return this._sort;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define o modo de pesquisa utilizado no campo de busca, quando habilitado.
   * Valores definidos no enum: PoMultiselectFilterMode
   *
   * @default `startsWith`
   */
  set filterMode(filterMode) {
    this._filterMode = filterMode in PoMultiselectFilterMode ? filterMode : PoMultiselectFilterMode.startsWith;
    switch (this._filterMode.toString()) {
      case "startsWith":
        this._filterMode = PoMultiselectFilterMode.startsWith;
        break;
      case "contains":
        this._filterMode = PoMultiselectFilterMode.contains;
        break;
      case "endsWith":
        this._filterMode = PoMultiselectFilterMode.endsWith;
        break;
    }
  }
  get filterMode() {
    return this._filterMode;
  }
  /**
   * @optional
   *
   * @description
   * Deve ser informado o nome da propriedade do objeto que será utilizado para a conversão dos itens apresentados na lista do componente
   * (`p-options`), esta propriedade será responsável pelo texto de apresentação de cada item da lista.
   *
   * Necessário quando informar o serviço como URL e o mesmo não estiver retornando uma lista de objetos no padrão da interface
   * `PoMultiSelectOption`.
   *
   * @default `label`
   */
  set fieldLabel(value) {
    this._fieldLabel = value ? value : PO_MULTISELECT_FIELD_LABEL_DEFAULT;
    if (isTypeof(this.filterService, "string") && this.service) {
      this.service.fieldLabel = this._fieldLabel;
    }
  }
  get fieldLabel() {
    return this._fieldLabel;
  }
  /**
   * @optional
   *
   * @description
   * Deve ser informado o nome da propriedade do objeto que será utilizado para a conversão dos itens apresentados na lista do componente
   * (`p-options`), esta propriedade será responsável pelo valor de cada item da lista.
   *
   * Necessário quando informar o serviço como URL e o mesmo não estiver retornando uma lista de objetos no padrão da interface
   * `PoMultiSelectOption`.
   *
   * @default `value`
   */
  set fieldValue(value) {
    this._fieldValue = value ? value : PO_MULTISELECT_FIELD_VALUE_DEFAULT;
    if (isTypeof(this.filterService, "string") && this.service) {
      this.service.fieldValue = this._fieldValue;
    }
  }
  get fieldValue() {
    return this._fieldValue;
  }
  constructor(languageService, cd) {
    this.cd = cd;
    this.language = languageService.getShortLanguage();
  }
  ngOnInit() {
    if (this.filterService) {
      this.setService(this.filterService);
    }
    this.filterSubject.pipe(debounceTime(this.debounceTime), distinctUntilChanged(), tap(() => this.isServerSearching = true), switchMap((search) => this.applyFilter(search)), tap(() => this.isServerSearching = false)).subscribe();
    this.setLabelsAndValuesOptions();
    this.validAndSortOptions();
    this.updateList(this.options);
  }
  onThemeChange() {
    this.applySizeBasedOnA11y();
  }
  setService(service) {
    if (isTypeof(service, "object")) {
      this.service = service;
    } else {
      this.service = this.defaultService;
      this.service.configProperties(service, this.fieldLabel, this.fieldValue);
    }
    this.isFirstFilter = true;
  }
  validAndSortOptions() {
    if (this.options && this.options.length) {
      PoUtils.removeUndefinedAndNullOptionsWithFieldValue(this.options, this.fieldValue);
      PoUtils.removeDuplicatedOptionsWithFieldValue(this.options, this.fieldValue);
      this.setUndefinedLabels(this.options);
      if (this.sort) {
        PoUtils.sortOptionsByProperty(this.options, this.fieldLabel);
      }
    }
  }
  setUndefinedLabels(options) {
    options.forEach((option) => {
      if (!option[this.fieldLabel]) {
        option[this.fieldLabel] = option[this.fieldValue];
      }
    });
  }
  updateList(options) {
    if (options) {
      this.visibleOptionsDropdown = options;
    }
  }
  callOnChange(selectedOptions) {
    if (this.onModelChange) {
      const value = this.getValueUpdate(selectedOptions);
      this.onModelChange(value);
      this.eventChange(selectedOptions);
      this.controlChangeModelEmitter(value);
    }
    setTimeout(() => {
      if (this.autoHeight) {
        this.updateInputHeight();
      }
    });
  }
  eventChange(selectedOptions) {
    if (selectedOptions && this.lastLengthModel !== selectedOptions.length) {
      this.change.emit(selectedOptions);
    }
    this.lastLengthModel = selectedOptions ? selectedOptions.length : null;
  }
  controlChangeModelEmitter(value) {
    const current = JSON.stringify(this.modelLastUpdate);
    const incoming = JSON.stringify(value);
    if (current !== incoming) {
      this.changeModel.emit(value);
      this.modelLastUpdate = Array.isArray(value) ? [...value] : value;
    }
  }
  getValuesFromOptions(selectedOptions) {
    return selectedOptions && selectedOptions.length ? selectedOptions.map((option) => option[this.fieldValue]) : [];
  }
  getLabelByValue(value) {
    const index = this.options.findIndex((option) => option[this.fieldValue] === value);
    return this.options[index].label;
  }
  //Transforma o tamanho do input para o tamanho do ícone de loading correspondente
  mapSizeToIcon(size) {
    return mapInputSizeToLoadingIcon(size);
  }
  searchByLabel(search, options, filterMode) {
    if (search && options && options.length) {
      const newOptions = [];
      options.forEach((option) => {
        if (option[this.fieldLabel] && this.compareMethod(search, option, filterMode)) {
          newOptions.push(option);
        }
      });
      this.visibleOptionsDropdown = newOptions;
    } else {
      this.visibleOptionsDropdown = [...options];
    }
  }
  compareMethod(search, option, filterMode) {
    switch (filterMode) {
      case PoMultiselectFilterMode.startsWith:
        return this.startsWith(search, option);
      case PoMultiselectFilterMode.contains:
        return this.contains(search, option);
      case PoMultiselectFilterMode.endsWith:
        return this.endsWith(search, option);
    }
  }
  startsWith(search, option) {
    return option[this.fieldLabel].toLowerCase().startsWith(search.toLowerCase());
  }
  contains(search, option) {
    return option[this.fieldLabel].toLowerCase().indexOf(search.toLowerCase()) > -1;
  }
  endsWith(search, option) {
    return option[this.fieldLabel].toLowerCase().endsWith(search.toLowerCase());
  }
  validate(c) {
    if (requiredFailed(this.required, this.disabled, c.value)) {
      this.cd?.markForCheck();
      return {
        required: {
          valid: false
        }
      };
    }
    return null;
  }
  updateSelectedOptions(newOptions, options = this.options) {
    this.selectedOptions = [];
    if (newOptions.length === 0) {
      this.lastLengthModel = void 0;
    }
    if (this.filterService) {
      this.selectedOptions = newOptions;
    } else {
      newOptions.forEach((newOption) => {
        options.forEach((option) => {
          if (option[this.fieldValue] === newOption[this.fieldValue]) {
            this.selectedOptions.push(option);
          }
        });
      });
    }
    this.updateVisibleItems();
  }
  writeValue(values) {
    const originalValues = values;
    values = this.getValueWrite(values);
    if (values !== null && values !== void 0 && !Array.isArray(values)) {
      values = [values];
    }
    values = values ?? [];
    if (this.service && values.length) {
      this.getObjectsByValuesSubscription = this.service.getObjectsByValues(values).subscribe((options) => {
        this.updateSelectedOptions(options);
        this.callOnChange(this.selectedOptions);
      });
    } else {
      this.updateSelectedOptions(values.map((value) => ({
        [this.fieldValue]: value
      })));
      if (this.selectedOptions && this.selectedOptions.length < values.length) {
        this.callOnChange(this.selectedOptions);
      } else if (originalValues != null && (!Array.isArray(originalValues) || originalValues.length > 0)) {
        this.controlChangeModelEmitter(this.getValuesFromOptions(this.selectedOptions));
      }
    }
  }
  // Função implementada do ControlValueAccessor
  // Usada para interceptar os estados de habilitado via forms api
  setDisabledState(isDisabled) {
    this.disabled = isDisabled;
  }
  registerOnChange(fn) {
    this.onModelChange = fn;
  }
  registerOnTouched(fn) {
    this.onModelTouched = fn;
  }
  registerOnValidatorChange(fn) {
    this.validatorChange = fn;
  }
  getValueUpdate(selectedOptions) {
    if (this.controlValueWithLabel && selectedOptions?.length) {
      return selectedOptions.map((option) => ({
        value: option[this.fieldValue],
        label: option[this.fieldLabel]
      }));
    }
    return this.getValuesFromOptions(selectedOptions);
  }
  getValueWrite(data) {
    if (!this.controlValueWithLabel || data == null) {
      return data;
    }
    if (Array.isArray(data) && data.every((item) => item?.value !== void 0)) {
      return data.map((option) => option.value);
    }
    if (data?.value !== void 0) {
      return data.value;
    }
    return data;
  }
  setLabelsAndValuesOptions() {
    if (this.fieldLabel && this.fieldValue && this.options) {
      this.options.map((option) => {
        option.label = option[this.fieldLabel];
        option.value = option[this.fieldValue];
      });
    }
  }
  validateModel() {
    if (this.validatorChange) {
      this.validatorChange();
    }
  }
  updateInputHeight() {
    if (!this.inputElement) return;
    const height = this.inputElement.nativeElement.offsetHeight;
    if (this.size === PoFieldSize.Small) {
      this.isExpandedHeight = height > 32;
    } else {
      this.isExpandedHeight = height > 44;
    }
  }
  applySizeBasedOnA11y() {
    const size = validateSizeFn(this._initialSize, PoFieldSize);
    this._size = size;
  }
  static ɵfac = function PoMultiselectBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoMultiselectBaseComponent)(ɵɵdirectiveInject(PoLanguageService), ɵɵdirectiveInject(ChangeDetectorRef));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoMultiselectBaseComponent,
    viewQuery: function PoMultiselectBaseComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c22, 7, ElementRef);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.inputElement = _t.first);
      }
    },
    hostVars: 2,
    hostBindings: function PoMultiselectBaseComponent_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("PoUiThemeChange", function PoMultiselectBaseComponent_PoUiThemeChange_HostBindingHandler() {
          return ctx.onThemeChange();
        }, ɵɵresolveWindow);
      }
      if (rf & 2) {
        ɵɵattribute("p-size", ctx.size)("p-loading", ctx.loading);
      }
    },
    inputs: {
      additionalHelpEventTrigger: "additionalHelpEventTrigger",
      additionalHelpTooltip: [0, "p-additional-help-tooltip", "additionalHelpTooltip"],
      autoFocus: [2, "p-auto-focus", "autoFocus", convertToBoolean],
      compactLabel: [1, "p-compact-label", "compactLabel"],
      label: [0, "p-label", "label"],
      help: [0, "p-help", "help"],
      optional: [0, "p-optional", "optional"],
      placeholder: [0, "p-placeholder", "placeholder"],
      placeholderSearch: [0, "p-placeholder-search", "placeholderSearch"],
      name: "name",
      hideSelectAll: [2, "p-hide-select-all", "hideSelectAll", convertToBoolean],
      fieldErrorMessage: [0, "p-field-error-message", "fieldErrorMessage"],
      errorLimit: [0, "p-error-limit", "errorLimit"],
      appendBox: [2, "p-append-in-body", "appendBox", convertToBoolean],
      controlValueWithLabel: [2, "p-control-value-with-label", "controlValueWithLabel", convertToBoolean],
      listboxControlPosition: [0, "p-listbox-control-position", "listboxControlPosition"],
      poHelperComponent: [1, "p-helper", "poHelperComponent"],
      labelTextWrap: [1, "p-label-text-wrap", "labelTextWrap"],
      filterService: [0, "p-filter-service", "filterService"],
      autoHeight: [2, "p-auto-height", "autoHeight", convertToBoolean],
      debounceTime: [0, "p-debounce-time", "debounceTime"],
      literals: [0, "p-literals", "literals"],
      required: [2, "p-required", "required", convertToBoolean],
      showRequired: [0, "p-show-required", "showRequired"],
      size: [0, "p-size", "size"],
      disabled: [2, "p-disabled", "disabled", convertToBoolean],
      loading: [2, "p-loading", "loading", convertToBoolean],
      hideSearch: [2, "p-hide-search", "hideSearch", convertToBoolean],
      options: [0, "p-options", "options"],
      sort: [2, "p-sort", "sort", convertToBoolean],
      filterMode: [0, "p-filter-mode", "filterMode"],
      fieldLabel: [0, "p-field-label", "fieldLabel"],
      fieldValue: [0, "p-field-value", "fieldValue"]
    },
    outputs: {
      blur: "p-blur",
      additionalHelp: "p-additional-help",
      change: "p-change",
      changeModel: "p-change-model",
      keydown: "p-keydown"
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoMultiselectBaseComponent, [{
    type: Directive
  }], () => [{
    type: PoLanguageService
  }, {
    type: ChangeDetectorRef
  }], {
    inputElement: [{
      type: ViewChild,
      args: ["inputElement", {
        read: ElementRef,
        static: true
      }]
    }],
    additionalHelpEventTrigger: [{
      type: Input
    }],
    additionalHelpTooltip: [{
      type: Input,
      args: ["p-additional-help-tooltip"]
    }],
    autoFocus: [{
      type: Input,
      args: [{
        alias: "p-auto-focus",
        transform: convertToBoolean
      }]
    }],
    compactLabel: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-compact-label",
        required: false
      }]
    }],
    blur: [{
      type: Output,
      args: ["p-blur"]
    }],
    label: [{
      type: Input,
      args: ["p-label"]
    }],
    help: [{
      type: Input,
      args: ["p-help"]
    }],
    optional: [{
      type: Input,
      args: ["p-optional"]
    }],
    placeholder: [{
      type: Input,
      args: ["p-placeholder"]
    }],
    placeholderSearch: [{
      type: Input,
      args: ["p-placeholder-search"]
    }],
    name: [{
      type: Input,
      args: ["name"]
    }],
    hideSelectAll: [{
      type: Input,
      args: [{
        alias: "p-hide-select-all",
        transform: convertToBoolean
      }]
    }],
    fieldErrorMessage: [{
      type: Input,
      args: ["p-field-error-message"]
    }],
    errorLimit: [{
      type: Input,
      args: ["p-error-limit"]
    }],
    additionalHelp: [{
      type: Output,
      args: ["p-additional-help"]
    }],
    change: [{
      type: Output,
      args: ["p-change"]
    }],
    changeModel: [{
      type: Output,
      args: ["p-change-model"]
    }],
    keydown: [{
      type: Output,
      args: ["p-keydown"]
    }],
    appendBox: [{
      type: Input,
      args: [{
        alias: "p-append-in-body",
        transform: convertToBoolean
      }]
    }],
    controlValueWithLabel: [{
      type: Input,
      args: [{
        alias: "p-control-value-with-label",
        transform: convertToBoolean
      }]
    }],
    listboxControlPosition: [{
      type: Input,
      args: ["p-listbox-control-position"]
    }],
    poHelperComponent: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-helper",
        required: false
      }]
    }],
    labelTextWrap: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-label-text-wrap",
        required: false
      }]
    }],
    filterService: [{
      type: Input,
      args: ["p-filter-service"]
    }],
    autoHeight: [{
      type: Input,
      args: [{
        alias: "p-auto-height",
        transform: convertToBoolean
      }]
    }],
    debounceTime: [{
      type: Input,
      args: ["p-debounce-time"]
    }],
    literals: [{
      type: Input,
      args: ["p-literals"]
    }],
    required: [{
      type: Input,
      args: [{
        alias: "p-required",
        transform: convertToBoolean
      }]
    }],
    showRequired: [{
      type: Input,
      args: ["p-show-required"]
    }],
    size: [{
      type: Input,
      args: ["p-size"]
    }, {
      type: HostBinding,
      args: ["attr.p-size"]
    }],
    disabled: [{
      type: Input,
      args: [{
        alias: "p-disabled",
        transform: convertToBoolean
      }]
    }],
    loading: [{
      type: HostBinding,
      args: ["attr.p-loading"]
    }, {
      type: Input,
      args: [{
        alias: "p-loading",
        transform: convertToBoolean
      }]
    }],
    hideSearch: [{
      type: Input,
      args: [{
        alias: "p-hide-search",
        transform: convertToBoolean
      }]
    }],
    options: [{
      type: Input,
      args: ["p-options"]
    }],
    sort: [{
      type: Input,
      args: [{
        alias: "p-sort",
        transform: convertToBoolean
      }]
    }],
    filterMode: [{
      type: Input,
      args: ["p-filter-mode"]
    }],
    fieldLabel: [{
      type: Input,
      args: ["p-field-label"]
    }],
    fieldValue: [{
      type: Input,
      args: ["p-field-value"]
    }],
    onThemeChange: [{
      type: HostListener,
      args: ["window:PoUiThemeChange"]
    }]
  });
})();
var PoMultiselectFilterService = class _PoMultiselectFilterService {
  http = inject(HttpClient);
  fieldLabel = "label";
  fieldValue = "value";
  _url;
  messages = [];
  get url() {
    return this._url;
  }
  getFilteredData({
    value
  }) {
    const params = value ? {
      filter: value
    } : {};
    return this.http.get(this.url, {
      params
    }).pipe(map((response) => this.parseToArrayMultiselectOptions(response["items"])));
  }
  getObjectsByValues(value) {
    return this.http.get(`${this.url}?${this.fieldValue}=${value.toString()}`).pipe(map((response) => this.parseToArrayMultiselectOptions(response["items"])));
  }
  configProperties(url, fieldLabel, fieldValue) {
    this._url = url;
    this.fieldLabel = fieldLabel;
    this.fieldValue = fieldValue;
  }
  parseToArrayMultiselectOptions(items) {
    if (items && items.length > 0) {
      return items.map((item) => this.parseToMultiselectOption(item));
    }
    return [];
  }
  parseToMultiselectOption(item) {
    const label = item[this.fieldLabel];
    const value = item[this.fieldValue];
    return {
      [this.fieldLabel]: label,
      [this.fieldValue]: value
    };
  }
  static ɵfac = function PoMultiselectFilterService_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoMultiselectFilterService)();
  };
  static ɵprov = ɵɵdefineInjectable({
    token: _PoMultiselectFilterService,
    factory: _PoMultiselectFilterService.ɵfac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoMultiselectFilterService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
var PoMultiselectDropdownComponent = class _PoMultiselectDropdownComponent {
  cd = inject(ChangeDetectorRef);
  /** Propriedade que indica se deve exibir o loading. */
  isServerSearching = false;
  /** Propriedade que indica se o campo de pesquisa deverá ser escondido. */
  hideSearch = false;
  /** Propriedade que que recebe as literais definidas no componente `po-multiselect`. */
  literals;
  /** Placeholder do campo de pesquisa. */
  placeholderSearch;
  /** Propriedade que recebe a lista de opções selecionadas. */
  selectedOptions = [];
  /** Propriedade que recebe a lista com todas as opções. */
  options = [];
  /** Propriedade que recebe a lista de opções que deverão ser criadas no dropdown. */
  visibleOptions = [];
  /** Propriedade que indica se o campo "Selecionar todos" deverá ser escondido. */
  hideSelectAll = false;
  fieldValue;
  fieldLabel;
  multiselectTemplate;
  containerWidth;
  /** Tamanho do componente. */
  size;
  /** Evento disparado a cada tecla digitada na pesquisa. */
  changeSearch = new EventEmitter();
  /** Evento disparado a cada alteração na lista das opções selecionadas. */
  change = new EventEmitter();
  /**
   * Evento disparado quando for detectada uma ação que necessite fechar o dropdown.
   * Por exemplo, no caso de ser teclado TAB dentro do dropdown, então é disparado este evento
   * para notificar o componente principal que deve fechar o dropdown.
   */
  closeDropdown = new EventEmitter();
  container;
  divElement;
  listbox;
  scrollTop = 0;
  show = false;
  get hasOptions() {
    return !!this.options?.length;
  }
  scrollTo(index) {
    this.scrollTop = index <= 2 ? 0 : index * 44 - 88;
    this.cd.markForCheck();
  }
  isSelectedItem(option) {
    return this.selectedOptions.some((selectedItem) => selectedItem[this.fieldValue] === option[this.fieldValue]);
  }
  clickItem(check, options) {
    if (options) {
      this.updateSelectedValues(check, options);
    } else {
      const {
        selected,
        option
      } = check;
      this.updateSelectedValues(selected, option);
    }
  }
  onClickSelectAll() {
    const selectedValues = this.selectedOptions.map(({
      [this.fieldValue]: value
    }) => value);
    if (this.everyVisibleOptionsSelected(selectedValues)) {
      this.selectedOptions = [];
    } else {
      this.selectedOptions = this.uniqueSelectedOptions(selectedValues);
    }
    this.change.emit(this.selectedOptions);
  }
  updateSelectedValues(checked, option) {
    if (checked) {
      this.selectedOptions.push(option);
    } else {
      this.selectedOptions = this.selectedOptions.filter((selectedOption) => selectedOption[this.fieldValue] !== option[this.fieldValue]);
    }
    this.change.emit(this.selectedOptions);
  }
  everyVisibleOptionsSelected(selectedValues) {
    return this.visibleOptions?.every((visibleOption) => selectedValues.includes(visibleOption[this.fieldValue]));
  }
  someVisibleOptionsSelected(selectedValues) {
    return this.visibleOptions?.some((visibleOption) => selectedValues.includes(visibleOption[this.fieldValue]));
  }
  getStateSelectAll() {
    const selectedValues = this.selectedOptions.map(({
      [this.fieldValue]: value
    }) => value);
    if (this.everyVisibleOptionsSelected(selectedValues)) {
      return true;
    } else if (this.someVisibleOptionsSelected(selectedValues)) {
      return null;
    } else {
      return false;
    }
  }
  callChangeSearch(event) {
    this.changeSearch.emit(event);
  }
  controlVisibility(toOpen) {
    this.show = toOpen;
    setTimeout(() => {
      if (toOpen && this.listbox?.searchElement && !this.hideSearch) {
        this.listbox.searchElement.setFocus();
        this.listbox.searchElement.clean();
      }
    });
    this.cd.markForCheck();
  }
  uniqueSelectedOptions(selectedValues) {
    const newSelectedOptions = [...this.selectedOptions];
    for (const visibleOption of this.visibleOptions) {
      if (!selectedValues.includes(visibleOption[this.fieldValue])) {
        newSelectedOptions.push(visibleOption);
      }
    }
    return newSelectedOptions;
  }
  static ɵfac = function PoMultiselectDropdownComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoMultiselectDropdownComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoMultiselectDropdownComponent,
    selectors: [["po-multiselect-dropdown"]],
    viewQuery: function PoMultiselectDropdownComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c123, 7, ElementRef)(_c124, 7, ElementRef)(_c23, 5);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.container = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.divElement = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.listbox = _t.first);
      }
    },
    inputs: {
      isServerSearching: [0, "p-searching", "isServerSearching"],
      hideSearch: [0, "p-hide-search", "hideSearch"],
      literals: [0, "p-literals", "literals"],
      placeholderSearch: [0, "p-placeholder-search", "placeholderSearch"],
      selectedOptions: [0, "p-selected-options", "selectedOptions"],
      options: [0, "p-options", "options"],
      visibleOptions: [0, "p-visible-options", "visibleOptions"],
      hideSelectAll: [0, "p-hide-select-all", "hideSelectAll"],
      fieldValue: [0, "p-field-value", "fieldValue"],
      fieldLabel: [0, "p-field-label", "fieldLabel"],
      multiselectTemplate: [0, "p-multiselect-template", "multiselectTemplate"],
      containerWidth: [0, "p-container-width", "containerWidth"],
      size: [0, "p-size", "size"]
    },
    outputs: {
      changeSearch: "p-change-search",
      change: "p-change",
      closeDropdown: "p-close-dropdown"
    },
    standalone: false,
    decls: 7,
    vars: 17,
    consts: [["container", ""], ["divElement", ""], ["listbox", ""], [1, "po-multiselect-container", 3, "hidden"], [3, "scrollTop"], ["p-type", "check", 3, "p-change", "p-change-all", "p-change-search", "p-close", "p-items", "p-selected-options", "p-checkboxAllValue", "p-field-value", "p-field-value-search", "p-field-label", "p-literal-search", "p-literals", "p-is-searching", "p-hide-search", "p-hide-select-all", "p-template", "p-placeholder-search", "p-size", "p-container-width"]],
    template: function PoMultiselectDropdownComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "div", 3, 0)(2, "div", 4, 1);
        ɵɵelementContainerStart(4);
        ɵɵelementStart(5, "po-listbox", 5, 2);
        ɵɵlistener("p-change", function PoMultiselectDropdownComponent_Template_po_listbox_p_change_5_listener($event) {
          return ctx.clickItem($event);
        })("p-change-all", function PoMultiselectDropdownComponent_Template_po_listbox_p_change_all_5_listener() {
          return ctx.onClickSelectAll();
        })("p-change-search", function PoMultiselectDropdownComponent_Template_po_listbox_p_change_search_5_listener($event) {
          return ctx.callChangeSearch($event);
        })("p-close", function PoMultiselectDropdownComponent_Template_po_listbox_p_close_5_listener() {
          return ctx.closeDropdown.emit();
        });
        ɵɵelementEnd();
        ɵɵelementContainerEnd();
        ɵɵelementEnd()();
      }
      if (rf & 2) {
        ɵɵproperty("hidden", !ctx.show);
        ɵɵadvance(2);
        ɵɵproperty("scrollTop", ctx.scrollTop);
        ɵɵadvance(3);
        ɵɵproperty("p-items", ctx.visibleOptions)("p-selected-options", ctx.selectedOptions)("p-checkboxAllValue", ctx.getStateSelectAll())("p-field-value", ctx.fieldValue)("p-field-value-search", ctx.fieldValue)("p-field-label", ctx.fieldLabel)("p-literal-search", ctx.literals)("p-literals", ctx.literals)("p-is-searching", ctx.isServerSearching)("p-hide-search", ctx.hideSearch)("p-hide-select-all", ctx.hideSelectAll)("p-template", ctx.multiselectTemplate)("p-placeholder-search", ctx.placeholderSearch)("p-size", ctx.size)("p-container-width", ctx.containerWidth);
      }
    },
    dependencies: [PoListBoxComponent],
    encapsulation: 2,
    changeDetection: 0
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoMultiselectDropdownComponent, [{
    type: Component,
    args: [{
      selector: "po-multiselect-dropdown",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: '<div #container class="po-multiselect-container" [hidden]="!show">\n  <div [scrollTop]="scrollTop" #divElement>\n    <ng-container>\n      <po-listbox\n        #listbox\n        p-type="check"\n        [p-items]="visibleOptions"\n        [p-selected-options]="selectedOptions"\n        [p-checkboxAllValue]="getStateSelectAll()"\n        [p-field-value]="fieldValue"\n        [p-field-value-search]="fieldValue"\n        [p-field-label]="fieldLabel"\n        [p-literal-search]="literals"\n        [p-literals]="literals"\n        [p-is-searching]="isServerSearching"\n        [p-hide-search]="hideSearch"\n        [p-hide-select-all]="hideSelectAll"\n        [p-template]="multiselectTemplate"\n        [p-placeholder-search]="placeholderSearch"\n        [p-size]="size"\n        (p-change)="clickItem($event)"\n        (p-change-all)="onClickSelectAll()"\n        (p-change-search)="callChangeSearch($event)"\n        (p-close)="closeDropdown.emit()"\n        [p-container-width]="containerWidth"\n      >\n      </po-listbox>\n    </ng-container>\n  </div>\n</div>\n'
    }]
  }], null, {
    isServerSearching: [{
      type: Input,
      args: ["p-searching"]
    }],
    hideSearch: [{
      type: Input,
      args: ["p-hide-search"]
    }],
    literals: [{
      type: Input,
      args: ["p-literals"]
    }],
    placeholderSearch: [{
      type: Input,
      args: ["p-placeholder-search"]
    }],
    selectedOptions: [{
      type: Input,
      args: ["p-selected-options"]
    }],
    options: [{
      type: Input,
      args: ["p-options"]
    }],
    visibleOptions: [{
      type: Input,
      args: ["p-visible-options"]
    }],
    hideSelectAll: [{
      type: Input,
      args: ["p-hide-select-all"]
    }],
    fieldValue: [{
      type: Input,
      args: ["p-field-value"]
    }],
    fieldLabel: [{
      type: Input,
      args: ["p-field-label"]
    }],
    multiselectTemplate: [{
      type: Input,
      args: ["p-multiselect-template"]
    }],
    containerWidth: [{
      type: Input,
      args: ["p-container-width"]
    }],
    size: [{
      type: Input,
      args: ["p-size"]
    }],
    changeSearch: [{
      type: Output,
      args: ["p-change-search"]
    }],
    change: [{
      type: Output,
      args: ["p-change"]
    }],
    closeDropdown: [{
      type: Output,
      args: ["p-close-dropdown"]
    }],
    container: [{
      type: ViewChild,
      args: ["container", {
        read: ElementRef,
        static: true
      }]
    }],
    divElement: [{
      type: ViewChild,
      args: ["divElement", {
        read: ElementRef,
        static: true
      }]
    }],
    listbox: [{
      type: ViewChild,
      args: ["listbox"]
    }]
  });
})();
var poMultiselectContainerOffset = 8;
var poMultiselectInputPaddingRight = 52;
var poMultiselectSpaceBetweenTags = 8;
var literalsTagRemoveOthers = {
  pt: {
    remove: "Remover todos os itens selecionados"
  },
  ru: {
    remove: "Удалить все выбранные элементы"
  },
  es: {
    remove: "Eliminar todos los elementos seleccionados"
  },
  en: {
    remove: "Clear all selected items"
  }
};
var providers$1 = [PoMultiselectFilterService, PoControlPositionService, {
  provide: NG_VALUE_ACCESSOR,
  // eslint-disable-next-line
  useExisting: forwardRef(() => PoMultiselectComponent),
  multi: true
}, {
  provide: NG_VALIDATORS,
  // eslint-disable-next-line
  useExisting: forwardRef(() => PoMultiselectComponent),
  multi: true
}];
var PoMultiselectComponent = class _PoMultiselectComponent extends PoMultiselectBaseComponent {
  renderer = inject(Renderer2);
  changeDetector;
  el = inject(ElementRef);
  controlPosition = inject(PoControlPositionService);
  defaultService = inject(PoMultiselectFilterService);
  multiselectOptionTemplate;
  dropdownElement;
  dropdown;
  iconElement;
  outerContainer;
  helperEl;
  literalsTag;
  dropdownIcon = "ICON_ARROW_DOWN";
  dropdownOpen = false;
  initialized = false;
  hasMoreTag;
  timeoutResize;
  visibleElement = false;
  containerWidth;
  subscription = new Subscription();
  enterCloseTag = false;
  initCalculateItems = true;
  isCalculateVisibleItems = true;
  cacheOptions;
  focusOnTag = false;
  constructor() {
    const changeDetector = inject(ChangeDetectorRef);
    const languageService = inject(PoLanguageService);
    super(languageService, changeDetector);
    this.changeDetector = changeDetector;
    const language = languageService.getShortLanguage();
    this.literalsTag = __spreadValues(__spreadValues({}, literalsTagRemoveOthers[poLocaleDefault]), literalsTagRemoveOthers[language]);
  }
  ngAfterViewInit() {
    if (this.autoFocus) {
      this.focus();
    }
    this.initialized = true;
  }
  ngOnChanges(changes) {
    if (changes.label) {
      this.displayAdditionalHelp = false;
    }
    if (this.filterService && (changes.filterService || changes.fieldValue || changes.fieldLabel)) {
      this.setService(this.filterService);
    }
  }
  ngDoCheck() {
    const inputWidth = this.inputElement.nativeElement.offsetWidth;
    if (inputWidth && !this.visibleElement && this.initialized || inputWidth && this.isCalculateVisibleItems) {
      this.debounceResize();
      this.visibleElement = true;
    }
  }
  ngOnDestroy() {
    this.removeListeners();
    this.getObjectsByValuesSubscription?.unsubscribe();
    this.filterSubject?.unsubscribe();
    this.subscription.unsubscribe();
  }
  emitAdditionalHelp() {
    if (this.label && this.isAdditionalHelpEventTriggered()) {
      this.additionalHelp.emit();
    }
  }
  /**
   * Função que atribui foco ao componente.
   *
   * Para utilizá-la é necessário ter a instância do componente no DOM, podendo ser utilizado o ViewChild da seguinte forma:
   *
   * ```
   * import { PoMultiselectComponent } from '@po-ui/ng-components';
   *
   * ...
   *
   * @ViewChild(PoMultiselectComponent, { static: true }) multiselect: PoMultiselectComponent;
   *
   * focusMultiselect() {
   *   this.multiselect.focus();
   * }
   * ```
   */
  focus() {
    if (!this.disabled) {
      this.inputElement.nativeElement.focus();
    }
  }
  getAdditionalHelpTooltip() {
    return this.isAdditionalHelpEventTriggered() ? null : this.additionalHelpTooltip;
  }
  getInputWidth() {
    return this.el.nativeElement.querySelector(".po-multiselect-input").offsetWidth - poMultiselectInputPaddingRight;
  }
  getTagsWidth() {
    const tags = this.el.nativeElement.querySelectorAll("po-tag");
    return Array.from(tags).map((tag) => tag["offsetWidth"]);
  }
  getErrorPattern() {
    return this.fieldErrorMessage && this.hasInvalidClass() ? this.fieldErrorMessage : "";
  }
  hasInvalidClass() {
    return this.el.nativeElement.classList.contains("ng-invalid") && this.el.nativeElement.classList.contains("ng-dirty");
  }
  calculateVisibleItems() {
    this.hasMoreTag = false;
    const tagsWidth = this.getTagsWidth();
    const inputWidth = this.getInputWidth();
    const extraTagSize = 63;
    const tagsVisible = tagsWidth[0];
    this.visibleTags = [];
    if (inputWidth > 0) {
      let sum2 = 0;
      let i2 = 0;
      for (i2 = 0; i2 < this.selectedOptions.length; i2++) {
        sum2 += tagsWidth[i2] + poMultiselectSpaceBetweenTags;
        this.visibleTags.push(this.selectedOptions[i2]);
        if (sum2 > inputWidth) {
          sum2 -= tagsWidth[i2];
          this.isCalculateVisibleItems = false;
          break;
        }
      }
      if (tagsVisible || !this.selectedOptions.length) {
        if (i2 === this.selectedOptions.length) {
          this.isCalculateVisibleItems = false;
          return;
        }
        this.hasMoreTag = true;
        if (sum2 + extraTagSize > inputWidth) {
          this.visibleTags.splice(-2, 2);
          const label = "+" + (this.selectedOptions.length + 1 - i2).toString();
          this.visibleTags.push({
            [this.fieldValue]: "",
            [this.fieldLabel]: label
          });
        } else {
          this.visibleTags.splice(-1, 1);
          const label = "+" + (this.selectedOptions.length - i2).toString();
          this.visibleTags.push({
            [this.fieldValue]: "",
            [this.fieldLabel]: label
          });
        }
      }
      if (this.initCalculateItems) {
        setTimeout(() => {
          this.handleKeyboardNavigationTag();
        }, 300);
      }
      this.initCalculateItems = false;
    }
    this.changeDetector.markForCheck();
  }
  changeItems(changedItems) {
    this.updateSelectedOptions(changedItems);
    this.callOnChange(this.selectedOptions);
    if (this.autoHeight && this.dropdownOpen) {
      this.changeDetector.detectChanges();
      this.adjustContainerPosition();
    }
    setTimeout(() => {
      this.handleKeyboardNavigationTag();
    }, 300);
  }
  updateVisibleItems() {
    if (this.selectedOptions) {
      this.visibleTags = [].concat(this.selectedOptions);
    }
    this.debounceResize();
    if (!this.inputElement.nativeElement.offsetWidth) {
      this.isCalculateVisibleItems = true;
    }
  }
  debounceResize() {
    if (!this.autoHeight) {
      clearTimeout(this.timeoutResize);
      this.timeoutResize = setTimeout(() => {
        this.calculateVisibleItems();
      }, 200);
    }
    this.changeDetector.markForCheck();
  }
  onBlur(event) {
    if (typeof this.inputElement.nativeElement.getAttribute("aria-label") === "string" && this.inputElement.nativeElement.getAttribute("aria-label").includes("Unselected")) {
      this.inputElement.nativeElement.setAttribute("aria-label", this.label ? this.label : "");
    }
    this.onModelTouched?.();
    if (event.type === "blur") {
      this.blur.emit();
    }
  }
  onKeyDown(event) {
    const isFieldFocused = document.activeElement === this.inputElement.nativeElement;
    if (event.shiftKey && event.keyCode === PoKeyCodeEnum.tab && !this.focusOnTag) {
      this.controlDropdownVisibility(false);
    }
    this.focusOnTag = false;
    if (event.keyCode === PoKeyCodeEnum.tab && this.appendBox && this.dropdownOpen) {
      event.preventDefault();
      this.focusOnFirstItem();
      return;
    }
    if (event.keyCode === PoKeyCodeEnum.esc) {
      event.preventDefault();
      this.controlDropdownVisibility(false);
      return;
    }
    if (event.keyCode === PoKeyCodeEnum.arrowDown) {
      event.preventDefault();
      this.controlDropdownVisibility(true);
      this.dropdown?.listbox?.setFocus();
      return;
    }
    if (event.keyCode === PoKeyCodeEnum.enter && !this.enterCloseTag) {
      if (this.visibleTags.length === 0) {
        this.toggleDropdownVisibility();
        this.focus();
        return;
      } else {
        event.preventDefault();
        this.toggleDropdownVisibility();
        return;
      }
    }
    if (event.keyCode === PoKeyCodeEnum.space) {
      event.preventDefault();
      this.toggleDropdownVisibility();
    }
    this.enterCloseTag = false;
    if (isFieldFocused) {
      this.keydown.emit(event);
    }
  }
  toggleDropdownVisibility() {
    if (this.disabled) {
      return;
    }
    if (this.filterService) {
      this.applyFilterInFirstClick();
    }
    this.controlDropdownVisibility(!this.dropdownOpen);
  }
  onKeyDownDropdown(event, index) {
    if (event.key === "Tab" && event.target["className"].includes("po-listbox-item-type-check")) {
      event.preventDefault();
      this.inputElement.nativeElement.focus();
    }
    if (event.key === "Escape") {
      event.preventDefault();
      this.controlDropdownVisibility(false);
      setTimeout(() => {
        this.inputElement.nativeElement.focus();
      }, 50);
    }
  }
  openDropdown(toOpen) {
    if (toOpen && !this.disabled) {
      this.controlDropdownVisibility(true);
    }
  }
  controlDropdownVisibility(toOpen) {
    toOpen ? this.open() : this.close();
  }
  scrollToSelectedOptions() {
    if (this.selectedOptions && this.selectedOptions.length) {
      const index = this.options.findIndex((option) => option[this.fieldValue] === this.selectedOptions[0][this.fieldValue]);
      this.dropdown.scrollTo(index);
    }
  }
  setVisibleOptionsDropdown(options) {
    this.visibleOptionsDropdown = options;
    this.changeDetector.markForCheck();
  }
  changeSearch(event) {
    if (event.event.keyCode === PoKeyCodeEnum.arrowDown || event.event.keyCode === PoKeyCodeEnum.tab) {
      this.focusOnFirstItem();
    }
    if (event && event[this.fieldValue] !== void 0) {
      if (this.filterService) {
        this.filterSubject.next(event[this.fieldValue]);
      } else {
        this.searchByLabel(event[this.fieldValue], this.options, this.filterMode);
      }
    } else {
      this.setVisibleOptionsDropdown(this.options);
    }
    setTimeout(() => this.adjustContainerPosition());
  }
  focusOnFirstItem() {
    const items = this.dropdown.listbox.element.nativeElement.querySelectorAll(".po-listbox-item-type-check");
    const item = items.length > 0 ? items[0] : null;
    if (item) {
      item.focus();
    } else {
      this.close();
      this.inputElement.nativeElement.focus();
    }
  }
  closeTag(value, event) {
    let index;
    this.enterCloseTag = true;
    if (value == null || value === "" || typeof value === "string" && value.includes("+")) {
      index = null;
      const itemsNotInVisibleTags = this.selectedOptions.filter((option) => !this.visibleTags.includes(option));
      for (const option of this.visibleTags) {
        if (!this.selectedOptions.includes(option)) {
          this.selectedOptions.splice(this.visibleTags.length - 1, itemsNotInVisibleTags.length);
          this.updateVisibleItems();
          this.callOnChange(this.selectedOptions);
        }
      }
    } else {
      this.visibleTags = [];
      this.changeDetector.detectChanges();
      index = this.selectedOptions.findIndex((option) => option[this.fieldValue] === value);
      this.selectedOptions.splice(index, 1);
      this.updateVisibleItems();
      this.callOnChange(this.selectedOptions);
    }
    setTimeout(() => {
      this.focusOnNextTag(index, event);
    }, 300);
  }
  /**
   * Método que exibe `p-helper` ou executa a ação definida em `p-helper{eventOnClick}` ou em `p-additionalHelp`.
   * Para isso, será necessário configurar uma tecla de atalho utilizando o evento `p-keydown`.
   *
   * > Exibe ou oculta o conteúdo do componente `po-helper` quando o componente estiver com foco.
   *
   * ```
   * // Exemplo com p-label e p-helper
   * <po-multiselect
   *  #multiselect
   *  ...
   *  p-label="Label do multiselect"
   *  [p-helper]="helperOptions"
   *  (p-keydown)="onKeyDown($event, multiselect)"
   * ></po-multiselect>
   * ```
   * ```
   * ...
   * onKeyDown(event: KeyboardEvent, inp: PoMultiselectComponent): void {
   *  if (event.code === 'F9') {
   *    inp.showAdditionalHelp();
   *  }
   * }
   * ```
   */
  showAdditionalHelp() {
    this.displayAdditionalHelp = !this.displayAdditionalHelp;
    const helper = this.poHelperComponent();
    const isHelpEvt = this.isAdditionalHelpEventTriggered();
    if (!this.label && (helper || this.additionalHelpTooltip || isHelpEvt)) {
      if (isHelpEvt) {
        this.additionalHelp.emit();
      }
      if (typeof helper !== "string" && typeof helper?.eventOnClick === "function") {
        helper.eventOnClick();
        return;
      }
      if (this.helperEl?.helperIsVisible()) {
        this.helperEl?.closeHelperPopover();
        return;
      }
      this.helperEl?.openHelperPopover();
      return;
    }
    return this.displayAdditionalHelp;
  }
  wasClickedOnToggle(event) {
    if (this.dropdownOpen && !this.inputElement.nativeElement.contains(event.target) && !this.iconElement?.nativeElement?.contains(event.target) && !this.dropdownElement?.nativeElement?.contains(event.target)) {
      this.controlDropdownVisibility(false);
    }
  }
  applyFilter(value = "") {
    const param = {
      property: this.fieldLabel,
      value
    };
    return this.service.getFilteredData(param).pipe(catchError((err) => {
      this.isServerSearching = false;
      return of([]);
    }), tap((options) => {
      this.setOptionsByApplyFilter(options);
    }));
  }
  setHelper(label, additionalHelpTooltip) {
    return setHelperSettings(label, additionalHelpTooltip, this.poHelperComponent(), this.size, this.isAdditionalHelpEventTriggered() ? this.additionalHelp : void 0);
  }
  applyFilterInFirstClick() {
    if (this.isFirstFilter) {
      this.isServerSearching = true;
      this.filterSubject.next(new String());
    } else {
      this.options = [...this.cacheOptions];
    }
  }
  setOptionsByApplyFilter(items) {
    if (this.isFirstFilter) {
      this.cacheOptions = [...items];
      this.isFirstFilter = false;
    }
    this.options = [...items];
    this.setVisibleOptionsDropdown(this.options);
  }
  adjustContainerPosition() {
    this.controlPosition.adjustPosition(this.listboxControlPosition);
  }
  close() {
    this.dropdownIcon = "ICON_ARROW_DOWN";
    this.dropdown?.listbox?.searchElement?.resetKeys();
    this.dropdownOpen = false;
    this.dropdown?.controlVisibility(false);
    this.setVisibleOptionsDropdown(this.options);
    this.removeListeners();
  }
  focusOnNextTag(indexClosed, clickOrEnter) {
    if (clickOrEnter === "enter") {
      const tagRemoveElements = this.el.nativeElement.querySelectorAll(".po-tag-remove");
      indexClosed = indexClosed || indexClosed === 0 ? indexClosed : tagRemoveElements.length;
      if (tagRemoveElements.length === 0) {
        this.inputElement.nativeElement.focus();
        this.inputElement.nativeElement.setAttribute("aria-label", `Unselected items ${this.label}`);
        this.controlDropdownVisibility(true);
      }
      this.focusOnRemoveTag(tagRemoveElements, indexClosed);
    } else {
      indexClosed = 0;
    }
    this.handleKeyboardNavigationTag(indexClosed);
  }
  focusOnRemoveTag(tag, indexClosed) {
    if (tag.length === indexClosed) {
      tag[indexClosed - 1]?.focus();
    } else {
      tag[indexClosed]?.focus();
    }
  }
  handleKeyboardNavigationTag(initialIndex = 0) {
    this.subscription.unsubscribe();
    this.subscription = new Subscription();
    const tagRemoveElements = this.el.nativeElement.querySelectorAll(".po-tag-remove");
    this.initializeTagRemoveElements(tagRemoveElements, initialIndex);
  }
  setTabIndex(element, tabIndex) {
    element.setAttribute("tabindex", tabIndex);
  }
  handleArrowLeft(tagRemoveElements, index) {
    if (index > 0) {
      this.setTabIndex(tagRemoveElements[index], -1);
      tagRemoveElements[index - 1].focus();
      this.setTabIndex(tagRemoveElements[index - 1], 0);
    }
  }
  handleArrowRight(tagRemoveElements, index) {
    if (index < tagRemoveElements.length - 1) {
      this.setTabIndex(tagRemoveElements[index], -1);
      tagRemoveElements[index + 1].focus();
      this.setTabIndex(tagRemoveElements[index + 1], 0);
    }
  }
  handleKeyDown(event, tagRemoveElements, index) {
    const KEY_SPACE = "Space";
    const KEY_ARROW_LEFT = "ArrowLeft";
    const KEY_ARROW_RIGHT = "ArrowRight";
    this.focusOnTag = true;
    if (event.code === KEY_SPACE) {
      event.preventDefault();
      event.stopPropagation();
    }
    if (event.key === KEY_ARROW_LEFT) {
      this.handleArrowLeft(tagRemoveElements, index);
    } else if (event.key === KEY_ARROW_RIGHT) {
      this.handleArrowRight(tagRemoveElements, index);
    }
  }
  initializeTagRemoveElements(tagRemoveElements, initialIndex) {
    tagRemoveElements.forEach((tagRemoveElement, index) => {
      if (index === initialIndex) {
        this.setTabIndex(tagRemoveElements[initialIndex], 0);
      } else if (tagRemoveElements.length === initialIndex) {
        this.setTabIndex(tagRemoveElements[initialIndex - 1], 0);
      } else {
        this.setTabIndex(tagRemoveElement, -1);
      }
      this.subscription.add(fromEvent(tagRemoveElement, "keydown").subscribe((event) => {
        this.handleKeyDown(event, tagRemoveElements, index);
      }));
    });
  }
  initializeListeners() {
    this.clickOutListener = this.renderer.listen("document", "click", (event) => {
      this.wasClickedOnToggle(event);
    });
    this.resizeListener = this.renderer.listen("window", "resize", () => {
      this.updateVisibleItems();
      PoUtils.isMobile() ? this.adjustContainerPosition() : this.close();
    });
    window.addEventListener("scroll", this.onScroll, true);
  }
  isAdditionalHelpEventTriggered() {
    return this.additionalHelpEventTrigger === "event" || this.additionalHelpEventTrigger === void 0 && this.additionalHelp.observed;
  }
  onScroll = () => {
    this.adjustContainerPosition();
  };
  open() {
    this.dropdownIcon = "ICON_ARROW_UP";
    this.dropdownOpen = true;
    this.dropdown.controlVisibility(true);
    this.setVisibleOptionsDropdown(this.options);
    this.initializeListeners();
    this.scrollToSelectedOptions();
    this.changeDetector.detectChanges();
    this.setPositionDropdown();
    if (this.dropdownOpen) {
      this.setContainerWidth();
    }
  }
  removeListeners() {
    if (this.clickOutListener) {
      this.clickOutListener();
    }
    if (this.resizeListener) {
      this.resizeListener();
    }
    window.removeEventListener("scroll", this.onScroll, true);
    this.changeDetector.markForCheck();
  }
  setContainerWidth() {
    if (this.outerContainer) {
      this.containerWidth = this.outerContainer.nativeElement.offsetWidth;
    }
  }
  setPositionDropdown() {
    this.controlPosition.setElements(this.dropdown.container.nativeElement, poMultiselectContainerOffset, this.inputElement, ["top", "bottom"], true);
    this.adjustContainerPosition();
  }
  static ɵfac = function PoMultiselectComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoMultiselectComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoMultiselectComponent,
    selectors: [["po-multiselect"]],
    contentQueries: function PoMultiselectComponent_ContentQueries(rf, ctx, dirIndex) {
      if (rf & 1) {
        ɵɵcontentQuery(dirIndex, PoMultiselectOptionTemplateDirective, 7);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.multiselectOptionTemplate = _t.first);
      }
    },
    viewQuery: function PoMultiselectComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c125, 5, ElementRef)(_c125, 5)(_c02, 7, ElementRef)(_c44, 5, ElementRef)(_c17, 5, PoHelperComponent);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.dropdownElement = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.dropdown = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.iconElement = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.outerContainer = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.helperEl = _t.first);
      }
    },
    standalone: false,
    features: [ɵɵProvidersFeature(providers$1), ɵɵInheritDefinitionFeature, ɵɵNgOnChangesFeature],
    decls: 22,
    vars: 39,
    consts: [["outerContainer", ""], ["trigger", "cdkOverlayOrigin"], ["inputElement", ""], ["dropdownListbox", ""], ["iconElement", ""], ["helperEl", ""], ["dropdownElement", ""], [3, "p-disabled", "p-label", "p-optional", "p-required", "p-show-required", "p-helper", "p-show-helper", "p-size", "p-text-wrap", "p-compact-label"], ["cdkOverlayOrigin", "", 1, "po-field-container-content"], [1, "po-field-container-content-helper-wrapper"], [1, "po-field-container-input"], [1, "po-input-icon-right", "po-multiselect-input", 3, "keydown", "click", "blur", "tabindex"], ["ria-hidden", "true", 1, "po-multiselect-input-placeholder"], [3, "p-value", "p-literals", "p-removable", "po-clickable", "p-disabled", "p-append-in-body"], [1, "po-field-icon-container-right"], [3, "p-size"], [3, "p-icon", "class", "po-field-icon-aa", "ngClass"], [1, "po-field-helper-button", 3, "p-size", "p-helper", "p-disabled", "p-append-in-body"], ["cdkConnectedOverlay", "", 3, "cdkConnectedOverlayOrigin", "cdkConnectedOverlayOpen", "cdkConnectedOverlayDisableClose"], [3, "p-append-in-body", "p-help", "p-disabled", "p-error-pattern", "p-error-limit", "p-size"], [3, "p-close", "p-value", "p-literals", "p-removable", "p-disabled", "p-append-in-body"], [3, "p-icon", "ngClass"], [4, "ngTemplateOutlet"], [3, "p-change", "p-change-search", "p-close-dropdown", "keydown", "p-searching", "p-hide-search", "p-hide-select-all", "p-literals", "p-options", "p-visible-options", "p-selected-options", "p-placeholder-search", "p-field-value", "p-field-label", "p-multiselect-template", "p-size", "p-container-width"]],
    template: function PoMultiselectComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵdeclareLet(0);
        ɵɵelementStart(1, "div", null, 0)(3, "po-field-container", 7)(4, "div", 8, 1)(6, "div", 9)(7, "div", 10)(8, "div", 11, 2);
        ɵɵlistener("keydown", function PoMultiselectComponent_Template_div_keydown_8_listener($event) {
          return ctx.onKeyDown($event);
        })("click", function PoMultiselectComponent_Template_div_click_8_listener() {
          return !ctx.isDisabled && ctx.toggleDropdownVisibility();
        })("blur", function PoMultiselectComponent_Template_div_blur_8_listener($event) {
          return ctx.onBlur($event);
        });
        ɵɵconditionalCreate(10, PoMultiselectComponent_Conditional_10_Template, 3, 1, "div", 12);
        ɵɵrepeaterCreate(11, PoMultiselectComponent_For_12_Template, 1, 7, "po-tag", 13, ɵɵrepeaterTrackByIdentity);
        ɵɵelementStart(13, "div", 14);
        ɵɵconditionalCreate(14, PoMultiselectComponent_Conditional_14_Template, 1, 1, "po-loading-icon", 15)(15, PoMultiselectComponent_Conditional_15_Template, 2, 8, "po-icon", 16);
        ɵɵelementEnd()()();
        ɵɵconditionalCreate(16, PoMultiselectComponent_Conditional_16_Template, 2, 4, "po-helper", 17);
        ɵɵelementEnd()();
        ɵɵconditionalCreate(17, PoMultiselectComponent_Conditional_17_Template, 1, 3, null, 18)(18, PoMultiselectComponent_Conditional_18_Template, 1, 1, "ng-container");
        ɵɵelement(19, "po-field-container-bottom", 19);
        ɵɵelementEnd();
        ɵɵtemplate(20, PoMultiselectComponent_ng_template_20_Template, 2, 13, "ng-template", null, 3, ɵɵtemplateRefExtractor);
        ɵɵelementEnd();
      }
      if (rf & 2) {
        const poHelper_r9 = ɵɵstoreLet(ctx.setHelper(ctx.label, ctx.additionalHelpTooltip));
        const needsHelper_r10 = !ctx.label && (ctx.poHelperComponent() || poHelper_r9.hideAdditionalHelp);
        ɵɵadvance(3);
        ɵɵproperty("p-disabled", ctx.isDisabled)("p-label", ctx.label)("p-optional", ctx.optional)("p-required", ctx.required)("p-show-required", ctx.showRequired)("p-helper", ctx.label ? poHelper_r9.helperSettings : void 0)("p-show-helper", ctx.label ? ctx.displayAdditionalHelp : void 0)("p-size", ctx.size)("p-text-wrap", ctx.labelTextWrap())("p-compact-label", ctx.compactLabel());
        ɵɵadvance();
        ɵɵclassProp("po-multiselect-show", ctx.dropdownOpen);
        ɵɵadvance(4);
        ɵɵclassProp("po-multiselect-input-auto", ctx.autoHeight)("po-multiselect-input-static", !ctx.autoHeight)("po-multiselect-input-disabled", ctx.isDisabled)("po-multiselect-input-font", !ctx.disabled && !(ctx.visibleTags == null ? null : ctx.visibleTags.length))("po-multiselect-input-auto-dynamic-height", ctx.isExpandedHeight);
        ɵɵproperty("tabindex", ctx.isDisabled ? -1 : 0);
        ɵɵattribute("disabled", ctx.isDisabled)("aria-label", ctx.label)("data-focused-element", !ctx.isDisabled)("data-inactive-component", ctx.isDisabled)("p-size", ctx.size);
        ɵɵadvance(2);
        ɵɵconditional(!ctx.disabled && !ctx.loading && !(ctx.visibleTags == null ? null : ctx.visibleTags.length) ? 10 : -1);
        ɵɵadvance();
        ɵɵrepeater(ctx.visibleTags);
        ɵɵadvance(3);
        ɵɵconditional(ctx.loading ? 14 : 15);
        ɵɵadvance(2);
        ɵɵconditional(needsHelper_r10 ? 16 : -1);
        ɵɵadvance();
        ɵɵconditional(ctx.appendBox ? 17 : 18);
        ɵɵadvance(2);
        ɵɵproperty("p-append-in-body", ctx.appendBox)("p-help", ctx.help)("p-disabled", ctx.isDisabled)("p-error-pattern", ctx.getErrorPattern())("p-error-limit", ctx.errorLimit)("p-size", ctx.size);
      }
    },
    dependencies: [NgClass, NgTemplateOutlet, CdkConnectedOverlay, CdkOverlayOrigin, PoFieldContainerBottomComponent, PoFieldContainerComponent, PoHelperComponent, PoIconComponent, PoLoadingIconComponent, PoTagComponent, PoMultiselectDropdownComponent],
    encapsulation: 2,
    changeDetection: 0
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoMultiselectComponent, [{
    type: Component,
    args: [{
      selector: "po-multiselect",
      changeDetection: ChangeDetectionStrategy.OnPush,
      providers: providers$1,
      standalone: false,
      template: `@let poHelper = setHelper(label, additionalHelpTooltip);
@let needsHelper = !label && (poHelperComponent() || poHelper.hideAdditionalHelp);

<div #outerContainer>
  <po-field-container
    [p-disabled]="isDisabled"
    [p-label]="label"
    [p-optional]="optional"
    [p-required]="required"
    [p-show-required]="showRequired"
    [p-helper]="label ? poHelper.helperSettings : undefined"
    [p-show-helper]="label ? displayAdditionalHelp : undefined"
    [p-size]="size"
    [p-text-wrap]="labelTextWrap()"
    [p-compact-label]="compactLabel()"
  >
    <div
      cdkOverlayOrigin
      #trigger="cdkOverlayOrigin"
      class="po-field-container-content"
      [class.po-multiselect-show]="dropdownOpen"
    >
      <div class="po-field-container-content-helper-wrapper">
        <div class="po-field-container-input">
          <div
            #inputElement
            [tabindex]="isDisabled ? -1 : 0"
            [attr.disabled]="isDisabled"
            [attr.aria-label]="label"
            [attr.data-focused-element]="!isDisabled"
            [attr.data-inactive-component]="isDisabled"
            [attr.p-size]="size"
            class="po-input-icon-right po-multiselect-input"
            [class.po-multiselect-input-auto]="autoHeight"
            [class.po-multiselect-input-static]="!autoHeight"
            [class.po-multiselect-input-disabled]="isDisabled"
            [class.po-multiselect-input-font]="!disabled && !visibleTags?.length"
            [class.po-multiselect-input-auto-dynamic-height]="isExpandedHeight"
            (keydown)="onKeyDown($event)"
            (click)="!isDisabled && toggleDropdownVisibility()"
            (blur)="onBlur($event)"
          >
            @if (!disabled && !loading && !visibleTags?.length) {
              <div class="po-multiselect-input-placeholder" ria-hidden="true">
                <span>{{ placeholder ? placeholder : literals.selectItem }}</span>
              </div>
            }

            @for (tag of visibleTags; track tag; let i = $index) {
              <po-tag
                [p-value]="tag[fieldLabel]"
                [p-literals]="i + 1 === visibleTags.length && hasMoreTag ? literalsTag : undefined"
                [p-removable]="true"
                [class.po-clickable]="tag[fieldValue] === '' && !disabled"
                [p-disabled]="isDisabled"
                (p-close)="closeTag(tag[fieldValue], $event)"
                [p-append-in-body]="appendBox"
              ></po-tag>
            }

            <div class="po-field-icon-container-right">
              @if (loading) {
                <po-loading-icon [p-size]="mapSizeToIcon(size)"></po-loading-icon>
              } @else {
                <po-icon
                  p-icon="{{ dropdownIcon }}"
                  #iconElement
                  class="po-field-icon {{ disabled ? 'po-icon-input-disabled' : 'po-icon-input' }}"
                  [class.po-field-icon-aa]="size === 'small'"
                  [ngClass]="disabled ? 'po-field-icon-disabled' : ''"
                >
                </po-icon>
              }
            </div>
          </div>
        </div>

        @if (needsHelper) {
          <po-helper
            #helperEl
            class="po-field-helper-button"
            [p-size]="size"
            [p-helper]="poHelper.helperSettings"
            [p-disabled]="isDisabled"
            [p-append-in-body]="appendBox"
          >
          </po-helper>
        }
      </div>
    </div>

    @if (appendBox) {
      <ng-template
        cdkConnectedOverlay
        [cdkConnectedOverlayOrigin]="trigger"
        [cdkConnectedOverlayOpen]="true"
        [cdkConnectedOverlayDisableClose]="true"
      >
        <ng-container *ngTemplateOutlet="dropdownListbox"></ng-container>
      </ng-template>
    } @else {
      <ng-container *ngTemplateOutlet="dropdownListbox"> </ng-container>
    }

    <po-field-container-bottom
      [p-append-in-body]="appendBox"
      [p-help]="help"
      [p-disabled]="isDisabled"
      [p-error-pattern]="getErrorPattern()"
      [p-error-limit]="errorLimit"
      [p-size]="size"
    ></po-field-container-bottom>
  </po-field-container>

  <ng-template #dropdownListbox>
    <po-multiselect-dropdown
      #dropdownElement
      [p-searching]="isServerSearching"
      [p-hide-search]="hideSearch"
      [p-hide-select-all]="hideSelectAll"
      [p-literals]="literals"
      [p-options]="options"
      [p-visible-options]="visibleOptionsDropdown"
      [p-selected-options]="selectedOptions"
      [p-placeholder-search]="placeholderSearch"
      [p-field-value]="fieldValue"
      [p-field-label]="fieldLabel"
      [p-multiselect-template]="multiselectOptionTemplate"
      [p-size]="size"
      [p-container-width]="containerWidth"
      (p-change)="changeItems($event)"
      (p-change-search)="changeSearch($event)"
      (p-close-dropdown)="controlDropdownVisibility(false)"
      (keydown)="onKeyDownDropdown($event, 0)"
    >
    </po-multiselect-dropdown>
  </ng-template>
</div>
`
    }]
  }], () => [], {
    multiselectOptionTemplate: [{
      type: ContentChild,
      args: [PoMultiselectOptionTemplateDirective, {
        static: true
      }]
    }],
    dropdownElement: [{
      type: ViewChild,
      args: ["dropdownElement", {
        read: ElementRef
      }]
    }],
    dropdown: [{
      type: ViewChild,
      args: ["dropdownElement"]
    }],
    iconElement: [{
      type: ViewChild,
      args: ["iconElement", {
        read: ElementRef,
        static: true
      }]
    }],
    outerContainer: [{
      type: ViewChild,
      args: ["outerContainer ", {
        read: ElementRef
      }]
    }],
    helperEl: [{
      type: ViewChild,
      args: ["helperEl", {
        read: PoHelperComponent,
        static: false
      }]
    }]
  });
})();
var PoNumberBaseComponent = class _PoNumberBaseComponent extends PoInputGeneric {
  type = "number";
  invalidInputValueOnBlur = false;
  /* istanbul ignore next */
  constructor(elementRef, cd) {
    super(elementRef, cd);
  }
  eventOnInput(e2) {
    if (!this.mask) {
      let value = e2.target.value;
      const valueMaxlength = this.validMaxLength(this.maxlength, value);
      this.invalidInputValueOnBlur = false;
      if (value !== valueMaxlength) {
        value = valueMaxlength;
        this.inputEl.nativeElement.value = value;
      }
      this.callOnChange(this.formatNumber(value));
      if (this.errorAsyncProperties?.triggerMode === "changeModel") {
        this.verifyErrorAsync();
      }
    }
  }
  onBlur(event) {
    const target = event.target;
    this.invalidInputValueOnBlur = target.value === "" && !target.validity.valid;
    if (this.invalidInputValueOnBlur) {
      this.callOnChange("Valor Inválido");
    }
    this.eventOnBlur(event);
  }
  onKeyDown(event) {
    const isFieldFocused = document.activeElement === this.inputEl.nativeElement;
    if (!this.isKeyAllowed(event)) {
      event.stopPropagation();
      event.preventDefault();
    }
    if (isFieldFocused) {
      this.keydown.emit(event);
    }
  }
  validMaxLength(maxlength, value) {
    if (maxlength && value.length > maxlength) {
      const substringValue = value.toString().substring(0, maxlength);
      if (substringValue && this.isEndWithDot(substringValue)) {
        return substringValue.toString().substring(0, maxlength - 1);
      }
      return substringValue;
    }
    return value;
  }
  writeValueModel(value) {
    if (this.inputEl) {
      if (value || value === 0) {
        if (this.mask) {
          this.inputEl.nativeElement.value = this.objMask.controlFormatting(String(value));
          if (this.objMask.formatModel) {
            this.onChangePropagate(this.objMask.valueToModel);
          }
        } else {
          this.inputEl.nativeElement.value = value;
        }
      } else {
        this.inputEl.nativeElement.value = "";
      }
    }
    this.changeModel.emit(value);
    this.verifyErrorAsync();
  }
  isEndWithDot(value) {
    return value && value.lastIndexOf(".") === value.length - 1;
  }
  formatNumber(value) {
    return value ? Number(value) : null;
  }
  isKeyAllowed(event) {
    return this.isShortcut(event) || this.isControlKeys(event) || !this.isInvalidKey(event.key);
  }
  isInvalidKey(key2) {
    const validatesKey = new RegExp(/[a-zA-Z:;=_´`^~"'?!@#$%¨&*()><{}çÇ\[\]/\\|]+/);
    return validatesKey.test(key2);
  }
  isShortcut(event) {
    const key2 = event.keyCode;
    const ctrl = event.ctrlKey || event.metaKey;
    const keyA = key2 === 65;
    const keyC = key2 === 67;
    const keyX = key2 === 88;
    const keyV = key2 === 86;
    return ctrl && keyC || ctrl && keyV || ctrl && keyA || ctrl && keyX;
  }
  isControlKeys(event) {
    const controlKeys = ["Backspace", "ArrowLeft", "ArrowRight", "ArrowUp", "ArrowDown", "Left", "Right", "Up", "Down", "Tab", "Delete", "Home", "End", "Enter"];
    return controlKeys.indexOf(event.key) !== -1;
  }
  static ɵfac = function PoNumberBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoNumberBaseComponent)(ɵɵdirectiveInject(ElementRef), ɵɵdirectiveInject(ChangeDetectorRef));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoNumberBaseComponent,
    features: [ɵɵInheritDefinitionFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoNumberBaseComponent, [{
    type: Directive
  }], () => [{
    type: ElementRef
  }, {
    type: ChangeDetectorRef
  }], null);
})();
var PoNumberComponent = class _PoNumberComponent extends PoNumberBaseComponent {
  /**
   * @optional
   *
   * @description
   *
   * Indica uma máscara para o campo, porém é incompatível com o `po-number`.
   * > **Componentes compatíveis:** `po-input`,`po-decimal`.
   *
   * @override
   */
  mask = "";
  /**
   * @optional
   *
   * @description
   *
   * Intervalo.
   *
   * @default 1
   */
  step = "1";
  /** Valor mínimo.
   *
   * > Quando o valor mínimo for um número com decimais aconselha-se utilizar junto da propriedade `p-step` também passando a ela um valor decimal.
   */
  min;
  set setMin(min3) {
    this.min = !isNaN(min3) ? min3 : void 0;
    this.validateModel();
  }
  /** Valor máximo.
   *
   * > Quando o valor máximo for um número com decimais aconselha-se utilizar junto da propriedade `p-step` também passando a ela um valor decimal.
   */
  max;
  set setMax(max3) {
    this.max = !isNaN(max3) ? max3 : void 0;
    this.validateModel();
  }
  id = `po-number[${uuid()}]`;
  /* istanbul ignore next */
  constructor() {
    const el = inject(ElementRef);
    const cd = inject(ChangeDetectorRef);
    super(el, cd);
  }
  onWheel(event) {
    event.preventDefault();
  }
  ngOnChanges(changes) {
    if (changes.label) {
      this.displayAdditionalHelp = false;
    }
  }
  extraValidation(abstractControl) {
    this.errorPattern = this.errorPattern !== "Valor Inválido" ? this.errorPattern : "";
    if (minFailed(this.min, abstractControl.value)) {
      return {
        min: {
          valid: false
        }
      };
    }
    if (maxFailed(this.max, abstractControl.value)) {
      return {
        max: {
          valid: false
        }
      };
    }
    if (this.invalidInputValueOnBlur) {
      this.errorPattern = this.errorPattern || "Valor Inválido";
      return {
        number: {
          valid: false
        }
      };
    }
    return null;
  }
  getErrorPatternMessage() {
    return this.errorPattern !== "" && this.containsInvalidClass() ? this.errorPattern : "";
  }
  containsInvalidClass() {
    return this.el.nativeElement.classList.contains("ng-invalid") && this.el.nativeElement.classList.contains("ng-dirty") && (this.inputEl.nativeElement.value !== "" || this.showErrorMessageRequired && (this.required || this.hasValidatorRequired)) || this.invalidInputValueOnBlur;
  }
  static ɵfac = function PoNumberComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoNumberComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoNumberComponent,
    selectors: [["po-number"]],
    hostBindings: function PoNumberComponent_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("wheel", function PoNumberComponent_wheel_HostBindingHandler($event) {
          return ctx.onWheel($event);
        });
      }
    },
    inputs: {
      step: [0, "p-step", "step"],
      setMin: [0, "p-min", "setMin"],
      setMax: [0, "p-max", "setMax"]
    },
    standalone: false,
    features: [ɵɵProvidersFeature([{
      provide: NG_VALUE_ACCESSOR,
      useExisting: forwardRef(() => _PoNumberComponent),
      multi: true
    }, {
      provide: NG_VALIDATORS,
      useExisting: forwardRef(() => _PoNumberComponent),
      multi: true
    }]), ɵɵInheritDefinitionFeature, ɵɵNgOnChangesFeature],
    decls: 13,
    vars: 39,
    consts: [["inp", ""], ["helperEl", ""], [3, "p-disabled", "p-id", "p-label", "p-optional", "p-required", "p-show-required", "p-helper", "p-show-helper", "p-size", "p-text-wrap", "p-compact-label"], [1, "po-field-container-content"], [1, "po-field-container-content-helper-wrapper"], [1, "po-field-container-input"], [1, "po-field-icon-container-left"], ["type", "number", 1, "po-input", 3, "blur", "focus", "input", "keydown", "autocomplete", "disabled", "id", "placeholder", "readonly", "required", "tabindex"], [1, "po-field-icon-container-right"], [3, "p-size"], [1, "po-icon-input", 3, "p-default-value", "p-element-ref", "p-size"], [1, "po-field-helper-button", 3, "p-size", "p-helper", "p-disabled", "p-append-in-body"], [3, "p-append-in-body", "p-help", "p-disabled", "p-error-limit", "p-error-pattern", "p-size"], [1, "po-field-icon", "po-icon-input", 3, "p-icon"], [1, "po-icon-input", 3, "p-change-event", "p-default-value", "p-element-ref", "p-size"]],
    template: function PoNumberComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵdeclareLet(0);
        ɵɵelementStart(1, "po-field-container", 2)(2, "div", 3)(3, "div", 4)(4, "div", 5);
        ɵɵconditionalCreate(5, PoNumberComponent_Conditional_5_Template, 2, 5, "div", 6);
        ɵɵelementStart(6, "input", 7, 0);
        ɵɵlistener("blur", function PoNumberComponent_Template_input_blur_6_listener($event) {
          return ctx.onBlur($event);
        })("focus", function PoNumberComponent_Template_input_focus_6_listener($event) {
          return ctx.eventOnFocus($event);
        })("input", function PoNumberComponent_Template_input_input_6_listener($event) {
          return ctx.eventOnInput($event);
        })("keydown", function PoNumberComponent_Template_input_keydown_6_listener($event) {
          return ctx.onKeyDown($event);
        });
        ɵɵelementEnd();
        ɵɵelementStart(8, "div", 8);
        ɵɵconditionalCreate(9, PoNumberComponent_Conditional_9_Template, 1, 1, "po-loading-icon", 9);
        ɵɵconditionalCreate(10, PoNumberComponent_Conditional_10_Template, 1, 3, "po-clean", 10);
        ɵɵelementEnd()();
        ɵɵconditionalCreate(11, PoNumberComponent_Conditional_11_Template, 2, 4, "po-helper", 11);
        ɵɵelementEnd()();
        ɵɵconditionalCreate(12, PoNumberComponent_Conditional_12_Template, 1, 6, "po-field-container-bottom", 12);
        ɵɵelementEnd();
      }
      if (rf & 2) {
        const poHelper_r4 = ɵɵstoreLet(ctx.setHelper(ctx.label, ctx.additionalHelpTooltip));
        ɵɵadvance();
        ɵɵproperty("p-disabled", ctx.isDisabled)("p-id", ctx.id)("p-label", ctx.label)("p-optional", ctx.optional)("p-required", ctx.required)("p-show-required", ctx.showRequired)("p-helper", ctx.label ? poHelper_r4.helperSettings : void 0)("p-show-helper", ctx.label ? ctx.displayAdditionalHelp : void 0)("p-size", ctx.size)("p-text-wrap", ctx.labelTextWrap())("p-compact-label", ctx.compactLabel());
        ɵɵadvance(4);
        ɵɵconditional(ctx.icon ? 5 : -1);
        ɵɵadvance();
        ɵɵclassProp("po-input-icon-left", ctx.icon)("po-input-icon-left-aa", ctx.icon && ctx.size === "small")("po-input-icon-right", ctx.clean || ctx.loading)("po-input-icon-right-aa", (ctx.clean || ctx.loading) && ctx.size === "small");
        ɵɵproperty("autocomplete", ctx.autocomplete)("disabled", ctx.isDisabled)("id", ctx.id)("placeholder", ctx.isDisabled ? "" : ctx.placeholder)("readonly", ctx.readonly)("required", ctx.required)("tabindex", ctx.isDisabled ? -1 : 0);
        ɵɵattribute("data-focused-element", !ctx.isDisabled)("data-inactive-component", ctx.isDisabled || ctx.readonly)("max", ctx.max)("min", ctx.min)("name", ctx.name)("p-size", ctx.size)("step", ctx.step);
        ɵɵadvance(3);
        ɵɵconditional(ctx.loading ? 9 : -1);
        ɵɵadvance();
        ɵɵconditional(!ctx.loading && ctx.clean && !ctx.isDisabled && !ctx.readonly ? 10 : -1);
        ɵɵadvance();
        ɵɵconditional(!ctx.label && ctx.poHelperComponent() || !ctx.label && poHelper_r4.hideAdditionalHelp ? 11 : -1);
        ɵɵadvance();
        ɵɵconditional(!ctx.readonly ? 12 : -1);
      }
    },
    dependencies: [PoCleanComponent, PoFieldContainerBottomComponent, PoFieldContainerComponent, PoHelperComponent, PoIconComponent, PoLoadingIconComponent],
    encapsulation: 2,
    changeDetection: 0
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoNumberComponent, [{
    type: Component,
    args: [{
      selector: "po-number",
      changeDetection: ChangeDetectionStrategy.OnPush,
      providers: [{
        provide: NG_VALUE_ACCESSOR,
        useExisting: forwardRef(() => PoNumberComponent),
        multi: true
      }, {
        provide: NG_VALIDATORS,
        useExisting: forwardRef(() => PoNumberComponent),
        multi: true
      }],
      standalone: false,
      template: `@let poHelper = setHelper(label, additionalHelpTooltip);
<po-field-container
  [p-disabled]="isDisabled"
  [p-id]="id"
  [p-label]="label"
  [p-optional]="optional"
  [p-required]="required"
  [p-show-required]="showRequired"
  [p-helper]="label ? poHelper.helperSettings : undefined"
  [p-show-helper]="label ? displayAdditionalHelp : undefined"
  [p-size]="size"
  [p-text-wrap]="labelTextWrap()"
  [p-compact-label]="compactLabel()"
>
  <div class="po-field-container-content">
    <div class="po-field-container-content-helper-wrapper">
      <div class="po-field-container-input">
        @if (icon) {
          <div class="po-field-icon-container-left">
            <po-icon
              class="po-field-icon po-icon-input"
              [class.po-field-icon-disabled]="isDisabled"
              [class.po-field-icon-aa]="size === 'small'"
              [p-icon]="icon"
            ></po-icon>
          </div>
        }
        <input
          #inp
          class="po-input"
          type="number"
          [attr.data-focused-element]="!isDisabled"
          [attr.data-inactive-component]="isDisabled || readonly"
          [attr.max]="max"
          [attr.min]="min"
          [attr.name]="name"
          [attr.p-size]="size"
          [attr.step]="step"
          [autocomplete]="autocomplete"
          [class.po-input-icon-left]="icon"
          [class.po-input-icon-left-aa]="icon && size === 'small'"
          [class.po-input-icon-right]="clean || loading"
          [class.po-input-icon-right-aa]="(clean || loading) && size === 'small'"
          [disabled]="isDisabled"
          [id]="id"
          [placeholder]="isDisabled ? '' : placeholder"
          [readonly]="readonly"
          [required]="required"
          [tabindex]="isDisabled ? -1 : 0"
          (blur)="onBlur($event)"
          (focus)="eventOnFocus($event)"
          (input)="eventOnInput($event)"
          (keydown)="onKeyDown($event)"
        />

        <div class="po-field-icon-container-right">
          @if (loading) {
            <po-loading-icon [p-size]="mapSizeToIcon(size)"></po-loading-icon>
          }

          @if (!loading && clean && !isDisabled && !readonly) {
            <po-clean
              class="po-icon-input"
              [p-default-value]="null"
              [p-element-ref]="inputEl"
              [p-size]="size"
              (p-change-event)="clear($event)"
            >
            </po-clean>
          }
        </div>
      </div>

      @if ((!this.label && poHelperComponent()) || (!this.label && poHelper.hideAdditionalHelp)) {
        <po-helper
          #helperEl
          class="po-field-helper-button"
          [p-size]="size"
          [p-helper]="poHelper.helperSettings"
          [p-disabled]="isDisabled"
          [p-append-in-body]="appendBox"
        >
        </po-helper>
      }
    </div>
  </div>

  @if (!readonly) {
    <po-field-container-bottom
      [p-append-in-body]="appendBox"
      [p-help]="help"
      [p-disabled]="isDisabled"
      [p-error-limit]="errorLimit"
      [p-error-pattern]="getErrorPatternMessage()"
      [p-size]="size"
    >
    </po-field-container-bottom>
  }
</po-field-container>
`
    }]
  }], () => [], {
    step: [{
      type: Input,
      args: ["p-step"]
    }],
    setMin: [{
      type: Input,
      args: ["p-min"]
    }],
    setMax: [{
      type: Input,
      args: ["p-max"]
    }],
    onWheel: [{
      type: HostListener,
      args: ["wheel", ["$event"]]
    }]
  });
})();
var PoPasswordComponent = class _PoPasswordComponent extends PoInputGeneric {
  id = `po-password[${uuid()}]`;
  type = "password";
  visiblePassword = false;
  _hidePasswordPeek = false;
  _noAutocompletePassword = true;
  /**
   * @optional
   *
   * @description
   *
   * Permite esconder a função de espiar a senha digitada.
   *
   * @default `false`
   */
  set hidePasswordPeek(value) {
    this._hidePasswordPeek = value;
    if (value) {
      this.visiblePassword = false;
      this.type = "password";
    }
  }
  set noAutocomplete(value) {
    this._noAutocompletePassword = value;
  }
  get noAutocomplete() {
    return this._noAutocompletePassword;
  }
  get hidePasswordPeek() {
    return this._hidePasswordPeek;
  }
  get autocomplete() {
    return this.noAutocomplete ? "new-password" : "on";
  }
  /* istanbul ignore next */
  constructor() {
    const el = inject(ElementRef);
    const cd = inject(ChangeDetectorRef);
    super(el, cd);
  }
  ngOnChanges(changes) {
    if (changes.label) {
      this.displayAdditionalHelp = false;
    }
  }
  extraValidation(c) {
    return null;
  }
  showPassword() {
    this.visiblePassword = !this.visiblePassword;
    this.type = this.type === "password" ? "text" : "password";
  }
  static ɵfac = function PoPasswordComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoPasswordComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoPasswordComponent,
    selectors: [["po-password"]],
    inputs: {
      hidePasswordPeek: [2, "p-hide-password-peek", "hidePasswordPeek", convertToBoolean],
      noAutocomplete: [2, "p-no-autocomplete", "noAutocomplete", convertToBoolean]
    },
    standalone: false,
    features: [ɵɵProvidersFeature([{
      provide: NG_VALUE_ACCESSOR,
      useExisting: forwardRef(() => _PoPasswordComponent),
      multi: true
    }, {
      provide: NG_VALIDATORS,
      useExisting: forwardRef(() => _PoPasswordComponent),
      multi: true
    }]), ɵɵInheritDefinitionFeature, ɵɵNgOnChangesFeature],
    decls: 15,
    vars: 43,
    consts: [["inp", ""], ["helperEl", ""], [3, "p-disabled", "p-id", "p-label", "p-optional", "p-required", "p-show-required", "p-helper", "p-show-helper", "p-size", "p-text-wrap", "p-compact-label"], [1, "po-field-container-content"], [1, "po-field-container-content-helper-wrapper"], [1, "po-field-container-input"], [1, "po-field-icon-container-left"], ["p-icon", "ICON_LOCK"], [1, "po-input", "po-input-icon-left", 3, "blur", "click", "focus", "input", "keydown", "autocomplete", "disabled", "id", "placeholder", "readonly", "required", "type"], [1, "po-field-icon-container-right"], [3, "p-size"], [1, "po-icon-input", 3, "p-element-ref", "p-size"], [1, "po-field-icon", "po-icon-input", 3, "p-icon", "po-field-icon-aa", "ngClass", "po-field-icon-disabled-aa"], [1, "po-field-helper-button", 3, "p-size", "p-helper", "p-disabled", "p-append-in-body"], [3, "p-help", "p-disabled", "p-error-limit", "p-error-pattern", "p-size"], [1, "po-icon-input", 3, "p-change-event", "p-element-ref", "p-size"], [1, "po-field-icon", "po-icon-input", 3, "click", "p-icon", "ngClass"]],
    template: function PoPasswordComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵdeclareLet(0);
        ɵɵelementStart(1, "po-field-container", 2)(2, "div", 3)(3, "div", 4)(4, "div", 5)(5, "div", 6);
        ɵɵelement(6, "po-icon", 7);
        ɵɵelementEnd();
        ɵɵelementStart(7, "input", 8, 0);
        ɵɵlistener("blur", function PoPasswordComponent_Template_input_blur_7_listener($event) {
          return ctx.eventOnBlur($event);
        })("click", function PoPasswordComponent_Template_input_click_7_listener($event) {
          return ctx.eventOnClick($event);
        })("focus", function PoPasswordComponent_Template_input_focus_7_listener($event) {
          return ctx.eventOnFocus($event);
        })("input", function PoPasswordComponent_Template_input_input_7_listener($event) {
          return ctx.eventOnInput($event);
        })("keydown", function PoPasswordComponent_Template_input_keydown_7_listener($event) {
          return ctx.onKeyDown($event);
        });
        ɵɵelementEnd();
        ɵɵelementStart(9, "div", 9);
        ɵɵconditionalCreate(10, PoPasswordComponent_Conditional_10_Template, 1, 1, "po-loading-icon", 10);
        ɵɵconditionalCreate(11, PoPasswordComponent_Conditional_11_Template, 1, 2, "po-clean", 11);
        ɵɵconditionalCreate(12, PoPasswordComponent_Conditional_12_Template, 1, 6, "po-icon", 12);
        ɵɵelementEnd()();
        ɵɵconditionalCreate(13, PoPasswordComponent_Conditional_13_Template, 2, 4, "po-helper", 13);
        ɵɵelementEnd()();
        ɵɵconditionalCreate(14, PoPasswordComponent_Conditional_14_Template, 1, 5, "po-field-container-bottom", 14);
        ɵɵelementEnd();
      }
      if (rf & 2) {
        const inp_r5 = ɵɵreference(8);
        const poHelper_r6 = ɵɵstoreLet(ctx.setHelper(ctx.label, ctx.additionalHelpTooltip));
        ɵɵadvance();
        ɵɵproperty("p-disabled", ctx.isDisabled)("p-id", ctx.id)("p-label", ctx.label)("p-optional", ctx.optional)("p-required", ctx.required)("p-show-required", ctx.showRequired)("p-helper", ctx.label ? poHelper_r6.helperSettings : void 0)("p-show-helper", ctx.label ? ctx.displayAdditionalHelp : void 0)("p-size", ctx.size)("p-text-wrap", ctx.labelTextWrap())("p-compact-label", ctx.compactLabel());
        ɵɵadvance(5);
        ɵɵclassMap(ɵɵinterpolate1("po-field-icon ", ctx.disabled ? "po-icon-input-disabled" : "po-icon-input"));
        ɵɵclassProp("po-field-icon-disabled", ctx.isDisabled)("po-field-icon-aa", ctx.size === "small");
        ɵɵadvance();
        ɵɵclassProp("po-input-double-icon-right", !ctx.loading && ctx.clean && inp_r5.value && !ctx.hidePasswordPeek)("po-input-double-icon-right-aa", !ctx.loading && ctx.clean && inp_r5.value && !ctx.hidePasswordPeek && ctx.size === "small")("po-input-icon-right", ctx.clean || ctx.loading || !ctx.hidePasswordPeek)("po-input-icon-right-aa", (ctx.clean || ctx.loading || !ctx.hidePasswordPeek) && ctx.size === "small")("po-input-icon-left-aa", ctx.size === "small");
        ɵɵproperty("autocomplete", ctx.autocomplete)("disabled", ctx.isDisabled)("id", ctx.id)("placeholder", ctx.isDisabled ? "" : ctx.placeholder)("readonly", ctx.readonly)("required", ctx.required)("type", ctx.disabled ? "password" : ctx.type);
        ɵɵattribute("name", ctx.name)("p-size", ctx.size);
        ɵɵadvance(3);
        ɵɵconditional(ctx.loading ? 10 : -1);
        ɵɵadvance();
        ɵɵconditional(!ctx.loading && ctx.clean && !ctx.isDisabled && !ctx.readonly ? 11 : -1);
        ɵɵadvance();
        ɵɵconditional(!ctx.loading && !ctx.hidePasswordPeek && !ctx.isDisabled ? 12 : -1);
        ɵɵadvance();
        ɵɵconditional(!ctx.label && ctx.poHelperComponent() || !ctx.label && poHelper_r6.hideAdditionalHelp ? 13 : -1);
        ɵɵadvance();
        ɵɵconditional(!ctx.readonly ? 14 : -1);
      }
    },
    dependencies: [NgClass, PoCleanComponent, PoFieldContainerBottomComponent, PoFieldContainerComponent, PoHelperComponent, PoIconComponent, PoLoadingIconComponent],
    encapsulation: 2,
    changeDetection: 0
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoPasswordComponent, [{
    type: Component,
    args: [{
      selector: "po-password",
      changeDetection: ChangeDetectionStrategy.OnPush,
      providers: [{
        provide: NG_VALUE_ACCESSOR,
        useExisting: forwardRef(() => PoPasswordComponent),
        multi: true
      }, {
        provide: NG_VALIDATORS,
        useExisting: forwardRef(() => PoPasswordComponent),
        multi: true
      }],
      standalone: false,
      template: `@let poHelper = setHelper(label, additionalHelpTooltip);
<po-field-container
  [p-disabled]="isDisabled"
  [p-id]="id"
  [p-label]="label"
  [p-optional]="optional"
  [p-required]="required"
  [p-show-required]="showRequired"
  [p-helper]="label ? poHelper.helperSettings : undefined"
  [p-show-helper]="label ? displayAdditionalHelp : undefined"
  [p-size]="size"
  [p-text-wrap]="labelTextWrap()"
  [p-compact-label]="compactLabel()"
>
  <div class="po-field-container-content">
    <div class="po-field-container-content-helper-wrapper">
      <div class="po-field-container-input">
        <div class="po-field-icon-container-left">
          <po-icon
            p-icon="ICON_LOCK"
            class="po-field-icon {{ disabled ? 'po-icon-input-disabled' : 'po-icon-input' }}"
            [class.po-field-icon-disabled]="isDisabled"
            [class.po-field-icon-aa]="size === 'small'"
          >
          </po-icon>
        </div>

        <input
          #inp
          class="po-input po-input-icon-left"
          [attr.name]="name"
          [attr.p-size]="size"
          [autocomplete]="autocomplete"
          [class.po-input-double-icon-right]="!loading && clean && inp.value && !hidePasswordPeek"
          [class.po-input-double-icon-right-aa]="
            !loading && clean && inp.value && !hidePasswordPeek && size === 'small'
          "
          [class.po-input-icon-right]="clean || loading || !hidePasswordPeek"
          [class.po-input-icon-right-aa]="(clean || loading || !hidePasswordPeek) && size === 'small'"
          [class.po-input-icon-left-aa]="size === 'small'"
          [disabled]="isDisabled"
          [id]="id"
          [placeholder]="isDisabled ? '' : placeholder"
          [readonly]="readonly"
          [required]="required"
          [type]="disabled ? 'password' : type"
          (blur)="eventOnBlur($event)"
          (click)="eventOnClick($event)"
          (focus)="eventOnFocus($event)"
          (input)="eventOnInput($event)"
          (keydown)="onKeyDown($event)"
        />

        <div class="po-field-icon-container-right">
          @if (loading) {
            <po-loading-icon [p-size]="mapSizeToIcon(size)"> </po-loading-icon>
          }

          @if (!loading && clean && !isDisabled && !readonly) {
            <po-clean class="po-icon-input" [p-element-ref]="inputEl" [p-size]="size" (p-change-event)="clear($event)">
            </po-clean>
          }

          @if (!loading && !hidePasswordPeek && !isDisabled) {
            <po-icon
              [p-icon]="visiblePassword ? 'ICON_EYE' : 'ICON_EYE_OFF'"
              class="po-field-icon po-icon-input"
              [class.po-field-icon-aa]="size === 'small'"
              [ngClass]="!visiblePassword ? 'po-field-icon-disabled' : ''"
              [class.po-field-icon-disabled-aa]="size === 'small'"
              (click)="showPassword()"
            >
            </po-icon>
          }
        </div>
      </div>

      @if ((!this.label && poHelperComponent()) || (!this.label && poHelper.hideAdditionalHelp)) {
        <po-helper
          #helperEl
          class="po-field-helper-button"
          [p-size]="size"
          [p-helper]="poHelper.helperSettings"
          [p-disabled]="isDisabled"
          [p-append-in-body]="appendBox"
        >
        </po-helper>
      }
    </div>
  </div>
  @if (!readonly) {
    <po-field-container-bottom
      [p-help]="help"
      [p-disabled]="isDisabled"
      [p-error-limit]="errorLimit"
      [p-error-pattern]="getErrorPattern()"
      [p-size]="size"
    >
    </po-field-container-bottom>
  }
</po-field-container>
`
    }]
  }], () => [], {
    hidePasswordPeek: [{
      type: Input,
      args: [{
        alias: "p-hide-password-peek",
        transform: convertToBoolean
      }]
    }],
    noAutocomplete: [{
      type: Input,
      args: [{
        alias: "p-no-autocomplete",
        transform: convertToBoolean
      }]
    }]
  });
})();
var PoRichTextToolbarActions;
(function(PoRichTextToolbarActions2) {
  PoRichTextToolbarActions2["Color"] = "color";
  PoRichTextToolbarActions2["Align"] = "align";
  PoRichTextToolbarActions2["Format"] = "format";
  PoRichTextToolbarActions2["List"] = "list";
  PoRichTextToolbarActions2["Link"] = "link";
  PoRichTextToolbarActions2["Media"] = "media";
})(PoRichTextToolbarActions || (PoRichTextToolbarActions = {}));
var PoRichTextService = class _PoRichTextService {
  model = new Subject();
  emitModel(value) {
    this.model.next(value);
  }
  getModel() {
    return this.model.asObservable();
  }
  static ɵfac = function PoRichTextService_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoRichTextService)();
  };
  static ɵprov = ɵɵdefineInjectable({
    token: _PoRichTextService,
    factory: _PoRichTextService.ɵfac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoRichTextService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
var PoRichTextBaseComponent = class _PoRichTextBaseComponent {
  richTextService;
  cd;
  /**
   *
   * @deprecated v23.x.x use `p-helper`
   *
   * @optional
   *
   * @description
   * Exibe um ícone de ajuda adicional, com o texto desta propriedade sendo passado para o popover do componente `po-helper`.
   * **Como boa prática, indica-se utilizar um texto com até 140 caracteres.**
   * > Requer um recuo mínimo de 8px se o componente estiver próximo à lateral da tela.
   *
   * > Essa propriedade está **depreciada** e será removida na versão `23.x.x`. Recomendamos utilizar a propriedade `p-helper` que oferece mais recursos e flexibilidade.
   */
  additionalHelpTooltip;
  /**
   * @optional
   *
   * @description
   *
   * Define que o popover (`p-helper` e/ou `p-error-limit`) será incluído no body da página e não
   * dentro do componente. Essa opção pode ser necessária em cenários com containers que possuem scroll ou overflow
   * escondido, garantindo o posicionamento correto do tooltip próximo ao elemento.
   *
   * > Quando utilizado com `p-helper`, leitores de tela como o NVDA podem não ler o conteúdo do popover.
   *
   * @default `false`
   */
  appendBox = false;
  /**
   * @optional
   *
   * @description
   *
   * Aplica foco no elemento ao ser iniciado.
   *
   * > Caso mais de um elemento seja configurado com essa propriedade, apenas o último elemento declarado com ela terá o foco.
   *
   * @default `false`
   */
  autoFocus = false;
  /**
   * @optional
   *
   * @description
   *
   * Define se o alinhamento de texto será desabilitado.
   *
   * @default `false`
   */
  disabledTextAlign;
  /**
   * @optional
   *
   * @description
   *
   * Indica que o campo será desabilitado.
   *
   * @default `false`
   */
  disabled;
  /**
   * @optional
   *
   * @description
   * Exibe um ícone de carregamento no lado direito do campo para sinalizar que uma operação está em andamento.
   *
   * @default `false`
   */
  set loading(value) {
    this._loading = value;
    this.cd?.markForCheck();
  }
  get loading() {
    return this._loading;
  }
  get isDisabled() {
    return this.disabled || this.loading;
  }
  /**
   * @description
   *
   * Mensagem que será apresentada quando a propriedade required estiver habilitada e o campo for limpo após algo ser digitado.
   */
  errorMessage = "";
  /**
   * @optional
   *
   * @description
   *
   * Limita a exibição da mensagem de erro a duas linhas e exibe um tooltip com o texto completo.
   *
   * > Caso essa propriedade seja definida como `true`, a mensagem de erro será limitada a duas linhas
   * e um tooltip será exibido ao passar o mouse sobre a mensagem para mostrar o conteúdo completo.
   *
   * @default `false`
   */
  errorLimit = false;
  /**
   * @optional
   *
   * @description
   *
   * Texto de apoio do campo.
   */
  help;
  /**
   * @optional
   *
   * @description
   *
   * Rótulo do campo.
   */
  label;
  /**
   * @optional
   *
   * @description
   * Define se o título do campo será exibido de forma compacta.
   *
   * Quando habilitado (`true`), o modo compacto afeta o conjunto composto por:
   * - `po-label`
   * - `p-requirement (showRequired)`
   * - `po-helper`
   *
   * Ou seja, todos os elementos relacionados ao título do campo
   * (rótulo, indicador de obrigatoriedade e componente auxiliar) passam
   * a seguir o comportamento de layout compacto.
   *
   * Também é possível definir esse comportamento de forma global,
   * uma única vez, na folha de estilo geral da aplicação, por meio
   * da customização dos tokens CSS:
   *
   * - `--field-container-title-justify`
   * - `--field-container-title-flex`
   *
   * Exemplo:
   *
   * ```
   * :root {
   *   --field-container-title-justify: flex-start;
   *   --field-container-title-flex: 0 1 auto;
   * }
   * ```
   *
   * Dessa forma, o layout compacto passa a ser o padrão da aplicação,
   * sem a necessidade de definir a propriedade individualmente em cada campo.
   *
   * @default `false`
   */
  compactLabel = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "compactLabel"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-compact-label",
    transform: convertToBoolean
  }));
  /** Nome e identificador do campo. */
  name;
  /**
   * @optional
   *
   * @description
   *
   * Define se a indicação de campo opcional será exibida.
   *
   * > Não será exibida a indicação se:
   * - O campo conter `p-required`;
   * - Não possuir `p-help` e/ou `p-label`.
   *
   * @default `false`
   */
  optional;
  _hideToolbarActions = [];
  /**
   * @optional
   *
   * @description
   *
   * Define as ações da barra de ferramentas do `PoRichTextComponent` que serão ocultadas.
   * Aceita um único valor do tipo `PoRichTextToolbarActions` ou uma lista de valores.
   *
   * > Esta propriedade sobrepõe a configuração da propriedade `p-disabled-text-align` quando for passada como `false`, caso sejam definidas simultaneamente.
   *
   * @default `[]`
   *
   * @example
   * ```
   * // Oculta apenas o seletor de cores
   * component.hideToolbarActions = PoRichTextToolbarActions.Color;
   *
   * // Oculta as opções de alinhamento e link
   * component.hideToolbarActions = [PoRichTextToolbarActions.Align, PoRichTextToolbarActions.Link];
   * ```
   */
  set hideToolbarActions(actions2) {
    this._hideToolbarActions = Array.isArray(actions2) ? [...actions2] : [actions2];
  }
  get hideToolbarActions() {
    return this._hideToolbarActions;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define as opções do componente de ajuda (po-helper) que será exibido ao lado do label quando a propriedade `p-label` for definida, ou, ao lado do componente na ausência da propriedade `p-label`.
   * > Para mais informações acesse: https://po-ui.io/documentation/po-helper.
   *
   * > Ao configurar esta propriedade, o antigo ícone de ajuda adicional (`p-additional-help-tooltip` e `p-additional-help`) será ignorado.
   */
  poHelperComponent = input(void 0, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "poHelperComponent"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-helper"
  }));
  /**
   * @optional
   *
   * @description
   *
   * Habilita a quebra automática do texto da propriedade `p-label`. Quando `p-label-text-wrap` for verdadeiro, o texto que excede
   * o espaço disponível é transferido para a próxima linha em pontos apropriados para uma
   * leitura clara.
   *
   * @default `false`
   */
  labelTextWrap = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "labelTextWrap"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-label-text-wrap"
  }));
  /**
   *
   * @deprecated v23.x.x use `p-helper`
   *
   * @optional
   *
   * @description
   * Evento disparado ao clicar no ícone de ajuda adicional.
   *
   * > Essa propriedade está **depreciada** e será removida na versão `23.x.x`. Recomendamos utilizar a propriedade `p-helper` que oferece mais recursos e flexibilidade.
   */
  additionalHelp = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Evento disparado ao deixar o campo e que recebe como parâmetro o valor alterado.
   */
  change = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Evento disparado ao modificar valor do model e que recebe como parâmetro o valor alterado.
   */
  changeModel = new EventEmitter();
  /**
   * @optional
   *
   * @description
   * Evento disparado quando uma tecla é pressionada enquanto o foco está no componente.
   * Retorna um objeto `KeyboardEvent` com informações sobre a tecla.
   */
  keydown = new EventEmitter();
  displayAdditionalHelp = false;
  invalid = false;
  onChangeModel = null;
  value;
  _loading = false;
  _height;
  _placeholder;
  _readonly;
  _required;
  _size = void 0;
  _initialSize = void 0;
  validatorChange;
  // eslint-disable-next-line
  onTouched = null;
  /**
   * @optional
   *
   * @description
   *
   * Define a altura da área de edição de texto.
   *
   * > Altura mínima do componente é `94` e a altura máxima é `262`.
   */
  set height(height) {
    this._height = height;
  }
  get height() {
    return this._height;
  }
  /**
   * @optional
   *
   * @description
   *
   * Mensagem que aparecerá enquanto o campo não estiver preenchido.
   *
   * @default ''
   */
  set placeholder(value) {
    this._placeholder = value || "";
  }
  get placeholder() {
    return this._placeholder;
  }
  /**
   * @optional
   *
   * @description
   *
   * Indica que o campo será somente leitura.
   *
   * @default `false`
   */
  set readonly(value) {
    this._readonly = value;
  }
  get readonly() {
    return this._readonly;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define que o campo será obrigatório.
   *
   * @default `false`
   */
  set required(value) {
    this._required = value;
    this.validateModel(this.value);
  }
  get required() {
    return this._required;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define o tamanho do componente:
   * - `small`: altura dos buttons como 32px (disponível apenas para acessibilidade AA).
   * - `medium`: altura dos buttons como 44px.
   *
   * > Caso a acessibilidade AA não esteja configurada, o tamanho `medium` será mantido.
   * Para mais detalhes, consulte a documentação do [po-theme](https://po-ui.io/documentation/po-theme).
   *
   * @default `medium`
   */
  set size(value) {
    this._initialSize = value;
    this.applySizeBasedOnA11y();
  }
  get size() {
    return this._size ?? getDefaultSizeFn(PoFieldSize);
  }
  /**
   * Define se a indicação de campo obrigatório será exibida.
   *
   * > Não será exibida a indicação se:
   * - Não possuir `p-help` e/ou `p-label`.
   */
  showRequired = false;
  constructor(richTextService, cd) {
    this.richTextService = richTextService;
    this.cd = cd;
  }
  // Função implementada do ControlValueAccessor
  // Usada para interceptar os estados de habilitado via forms api
  setDisabledState(isDisabled) {
    this.disabled = isDisabled;
    this.cd.markForCheck();
  }
  onThemeChange() {
    this.applySizeBasedOnA11y();
  }
  // Função implementada do ControlValueAccessor
  // Usada para interceptar as mudanças e não atualizar automaticamente o Model
  registerOnChange(func) {
    this.onChangeModel = func;
  }
  // Função implementada do ControlValueAccessor
  // Usada para interceptar as mudanças e não atualizar automaticamente o Model
  registerOnTouched(func) {
    this.onTouched = func;
  }
  registerOnValidatorChange(fn) {
    this.validatorChange = fn;
  }
  validate(abstractControl) {
    if (PoValidators.requiredFailed(this.required, this.disabled, abstractControl.value)) {
      return {
        required: {
          valid: false
        }
      };
    }
  }
  writeValue(value) {
    this.value = value;
    this.richTextService.emitModel(value);
  }
  // Executa a função onChange
  updateModel(value) {
    if (this.onChangeModel) {
      this.onChangeModel(value);
    }
  }
  validateModel(value) {
    if (this.validatorChange) {
      this.validatorChange(value);
    }
  }
  applySizeBasedOnA11y() {
    const size = validateSizeFn(this._initialSize, PoFieldSize);
    this._size = size;
  }
  static ɵfac = function PoRichTextBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoRichTextBaseComponent)(ɵɵdirectiveInject(PoRichTextService), ɵɵdirectiveInject(ChangeDetectorRef));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoRichTextBaseComponent,
    hostVars: 1,
    hostBindings: function PoRichTextBaseComponent_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("PoUiThemeChange", function PoRichTextBaseComponent_PoUiThemeChange_HostBindingHandler() {
          return ctx.onThemeChange();
        }, ɵɵresolveWindow);
      }
      if (rf & 2) {
        ɵɵattribute("p-size", ctx.size);
      }
    },
    inputs: {
      additionalHelpTooltip: [0, "p-additional-help-tooltip", "additionalHelpTooltip"],
      appendBox: [2, "p-append-in-body", "appendBox", convertToBoolean],
      autoFocus: [2, "p-auto-focus", "autoFocus", convertToBoolean],
      disabledTextAlign: [2, "p-disabled-text-align", "disabledTextAlign", convertToBoolean],
      disabled: [2, "p-disabled", "disabled", convertToBoolean],
      loading: [2, "p-loading", "loading", convertToBoolean],
      errorMessage: [0, "p-error-message", "errorMessage"],
      errorLimit: [0, "p-error-limit", "errorLimit"],
      help: [0, "p-help", "help"],
      label: [0, "p-label", "label"],
      compactLabel: [1, "p-compact-label", "compactLabel"],
      name: "name",
      optional: [0, "p-optional", "optional"],
      hideToolbarActions: [0, "p-hide-toolbar-actions", "hideToolbarActions"],
      poHelperComponent: [1, "p-helper", "poHelperComponent"],
      labelTextWrap: [1, "p-label-text-wrap", "labelTextWrap"],
      height: [0, "p-height", "height"],
      placeholder: [0, "p-placeholder", "placeholder"],
      readonly: [2, "p-readonly", "readonly", convertToBoolean],
      required: [2, "p-required", "required", convertToBoolean],
      size: [0, "p-size", "size"],
      showRequired: [0, "p-show-required", "showRequired"]
    },
    outputs: {
      additionalHelp: "p-additional-help",
      change: "p-change",
      changeModel: "p-change-model",
      keydown: "p-keydown"
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoRichTextBaseComponent, [{
    type: Directive
  }], () => [{
    type: PoRichTextService
  }, {
    type: ChangeDetectorRef
  }], {
    additionalHelpTooltip: [{
      type: Input,
      args: ["p-additional-help-tooltip"]
    }],
    appendBox: [{
      type: Input,
      args: [{
        alias: "p-append-in-body",
        transform: convertToBoolean
      }]
    }],
    autoFocus: [{
      type: Input,
      args: [{
        alias: "p-auto-focus",
        transform: convertToBoolean
      }]
    }],
    disabledTextAlign: [{
      type: Input,
      args: [{
        alias: "p-disabled-text-align",
        transform: convertToBoolean
      }]
    }],
    disabled: [{
      type: Input,
      args: [{
        alias: "p-disabled",
        transform: convertToBoolean
      }]
    }],
    loading: [{
      type: Input,
      args: [{
        alias: "p-loading",
        transform: convertToBoolean
      }]
    }],
    errorMessage: [{
      type: Input,
      args: ["p-error-message"]
    }],
    errorLimit: [{
      type: Input,
      args: ["p-error-limit"]
    }],
    help: [{
      type: Input,
      args: ["p-help"]
    }],
    label: [{
      type: Input,
      args: ["p-label"]
    }],
    compactLabel: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-compact-label",
        required: false
      }]
    }],
    name: [{
      type: Input,
      args: ["name"]
    }],
    optional: [{
      type: Input,
      args: ["p-optional"]
    }],
    hideToolbarActions: [{
      type: Input,
      args: ["p-hide-toolbar-actions"]
    }],
    poHelperComponent: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-helper",
        required: false
      }]
    }],
    labelTextWrap: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-label-text-wrap",
        required: false
      }]
    }],
    additionalHelp: [{
      type: Output,
      args: ["p-additional-help"]
    }],
    change: [{
      type: Output,
      args: ["p-change"]
    }],
    changeModel: [{
      type: Output,
      args: ["p-change-model"]
    }],
    keydown: [{
      type: Output,
      args: ["p-keydown"]
    }],
    height: [{
      type: Input,
      args: ["p-height"]
    }],
    placeholder: [{
      type: Input,
      args: ["p-placeholder"]
    }],
    readonly: [{
      type: Input,
      args: [{
        alias: "p-readonly",
        transform: convertToBoolean
      }]
    }],
    required: [{
      type: Input,
      args: [{
        alias: "p-required",
        transform: convertToBoolean
      }]
    }],
    size: [{
      type: Input,
      args: ["p-size"]
    }, {
      type: HostBinding,
      args: ["attr.p-size"]
    }],
    showRequired: [{
      type: Input,
      args: ["p-show-required"]
    }],
    onThemeChange: [{
      type: HostListener,
      args: ["window:PoUiThemeChange"]
    }]
  });
})();
var poRichTextBodyCommands = ["bold", "italic", "underline", "justifyleft", "justifycenter", "justifyright", "justifyfull", "insertUnorderedList", "Createlink"];
var PoRichTextBodyComponent = class _PoRichTextBodyComponent {
  richTextService = inject(PoRichTextService);
  cd = inject(ChangeDetectorRef);
  bodyElement;
  height;
  label;
  modelValue;
  placeholder;
  readonly;
  disabled = false;
  loading = false;
  change = new EventEmitter();
  commands = new EventEmitter();
  keydown = new EventEmitter();
  selectedLink = new EventEmitter();
  size;
  shortcutCommand = new EventEmitter();
  value = new EventEmitter();
  blur = new EventEmitter();
  isLinkEditing;
  linkElement;
  timeoutChange;
  valueBeforeChange;
  modelSubscription;
  resizeObserver;
  hasScroll = false;
  hasValue = false;
  onWindowResize() {
    this.checkScrollState();
  }
  ngOnInit() {
    this.bodyElement.nativeElement.designMode = "on";
    this.modelSubscription = this.richTextService.getModel().subscribe((modelValue) => {
      this.modelValue = modelValue;
      this.bodyElement.nativeElement.innerHTML = "";
      this.updateValueWithModelValue();
      this.addClickListenerOnAnchorElements();
      this.updateHasValue();
      this.checkScrollState();
    });
    this.initResizeObserver();
  }
  ngOnChanges(changes) {
    if (changes["loading"]) {
      setTimeout(() => this.checkScrollState());
    }
  }
  ngOnDestroy() {
    this.modelSubscription?.unsubscribe();
    this.resizeObserver?.disconnect();
  }
  executeCommand(command) {
    this.bodyElement.nativeElement.focus();
    if (typeof command === "object") {
      if (command.command === "InsertHTML") {
        const {
          command: linkCommand,
          value: {
            urlLink
          },
          value: {
            urlLinkText
          }
        } = command;
        this.handleCommandLink(linkCommand, urlLink, urlLinkText);
      } else {
        document.execCommand(command.command, false, command.value);
      }
    } else {
      document.execCommand(command, false, null);
    }
    this.updateModel();
    this.value.emit(this.modelValue);
  }
  linkEditing(event) {
    this.isLinkEditing = !!event;
  }
  onBlur() {
    this.blur.emit();
    if (this.modelValue !== this.valueBeforeChange) {
      clearTimeout(this.timeoutChange);
      this.timeoutChange = setTimeout(() => {
        this.change.emit(this.modelValue);
      }, 200);
    }
  }
  focus() {
    this.bodyElement.nativeElement.focus();
  }
  onClick() {
    this.emitSelectionCommands();
  }
  onFocus() {
    this.valueBeforeChange = this.modelValue;
  }
  onKeyDown(event) {
    const keyK = event.keyCode === PoKeyCodeEnum.keyK;
    const isLinkShortcut = keyK && event.ctrlKey || keyK && event.metaKey;
    const isFieldFocused = document.activeElement === this.bodyElement.nativeElement;
    if (isLinkShortcut) {
      event.preventDefault();
      this.shortcutCommand.emit();
    }
    this.toggleCursorOnLink(event, "add");
    if (isFieldFocused) {
      this.keydown.emit(event);
    }
  }
  onKeyUp(event) {
    this.toggleCursorOnLink(event, "remove");
    this.removeBrElement();
    this.updateModel();
    this.emitSelectionCommands();
    this.updateHasValue();
    this.checkScrollState();
  }
  onPaste() {
    this.update();
    setTimeout(() => {
      this.addClickListenerOnAnchorElements();
      this.updateHasValue();
      this.checkScrollState();
    });
  }
  update() {
    setTimeout(() => this.updateModel());
    setTimeout(() => {
      this.removeBrElement();
      this.updateModel();
      this.emitSelectionCommands();
      this.updateHasValue();
      this.checkScrollState();
    });
  }
  //Transforma o tamanho do input para o tamanho do ícone de loading correspondente
  mapSizeToIcon(size) {
    return mapInputSizeToLoadingIcon(size);
  }
  addClickListenerOnAnchorElements() {
    this.bodyElement.nativeElement.querySelectorAll("a").forEach((element) => {
      element.addEventListener("click", this.onAnchorClick);
    });
  }
  emitSelectionCommands() {
    const commands = poRichTextBodyCommands.filter((command) => document.queryCommandState(command));
    const rgbColor = document.queryCommandValue("ForeColor");
    let hexColor;
    if (!PoUtils.isIE()) {
      hexColor = this.rgbToHex(rgbColor);
    }
    if (this.isCursorPositionedInALink()) {
      commands.push("Createlink");
    }
    this.selectedLink.emit(this.linkElement);
    this.commands.emit({
      commands,
      hexColor
    });
  }
  getTextSelection() {
    const textSelection = document.getSelection();
    if (!textSelection) {
      return;
    }
    const focusNode = textSelection.focusNode ? textSelection.focusNode.parentElement : void 0;
    const anchorNode = textSelection.anchorNode ? textSelection.anchorNode.parentNode : void 0;
    const node = focusNode || anchorNode;
    let tagName;
    if (node) {
      tagName = node["tagName"] || node["nodeName"];
      return {
        node,
        tagName
      };
    }
  }
  handleCommandLink(linkCommand, urlLink, urlLinkText) {
    if (PoUtils.isIE()) {
      this.insertHtmlLinkElement(urlLink, urlLinkText);
    } else {
      const linkValue = PoUtils.isFirefox() && !this.isLinkEditing ? `&nbsp;${this.makeLinkTag(urlLink, urlLinkText)}&nbsp;` : this.makeLinkTag(urlLink, urlLinkText);
      document.execCommand(linkCommand, false, linkValue);
    }
    this.addClickListenerOnAnchorElements();
  }
  // tratamento específico para IE pois não suporta o comando 'insertHTML'.
  insertHtmlLinkElement(urlLink, urlLinkText) {
    const selection = document.getSelection();
    const selectionRange = selection.getRangeAt(0);
    const elementLink = document.createElement("a");
    const elementlinkText = document.createTextNode(urlLinkText);
    elementLink.appendChild(elementlinkText);
    elementLink.href = urlLink;
    elementLink.setAttribute("target", "_blank");
    elementLink.classList.add("po-rich-text-link");
    selectionRange.deleteContents();
    selectionRange.insertNode(elementLink);
  }
  isCursorPositionedInALink() {
    const textSelection = this.getTextSelection();
    this.linkElement = void 0;
    let isLink = false;
    if (textSelection && textSelection.node && textSelection.tagName === "A") {
      this.linkElement = textSelection.node;
      isLink = true;
    } else if ((PoUtils.isFirefox() || PoUtils.isIEOrEdge()) && this.verifyCursorPositionInFirefoxIEEdge()) {
      isLink = true;
    } else {
      isLink = textSelection ? this.isParentNodeAnchor(textSelection) : false;
    }
    return isLink;
  }
  isParentNodeAnchor(textSelection) {
    let element = textSelection.node;
    let isLink = false;
    while (element && (element.tagName !== null || element.nodeName !== null)) {
      if (element.tagName === "A" || element.nodeName === "A") {
        this.linkElement = element;
        isLink = true;
        return isLink;
      }
      element = element.parentElement || element.parentNode;
    }
    this.linkElement = void 0;
    return isLink;
  }
  makeLinkTag(urlLink, urlLinkText) {
    return `<a class="po-rich-text-link" href="${urlLink}" target="_blank">${urlLinkText || urlLink}</a>`;
  }
  onAnchorClick = (event) => {
    const {
      target,
      ctrlKey,
      metaKey
    } = event;
    let url;
    let elementLink;
    if (ctrlKey || metaKey) {
      if (event.path) {
        event.path.forEach((element) => {
          if (element.nodeName === "A") {
            url = element.href;
            elementLink = element;
          }
        });
      } else {
        url = target.attributes.href.value;
        elementLink = target;
      }
      PoUtils.openExternalLink(url);
      elementLink.classList.remove("po-clickable");
    }
  };
  // Tratamento necessário para eliminar a tag <br> criada no firefox quando o body for limpo.
  removeBrElement() {
    const bodyElement = this.bodyElement.nativeElement;
    if (!bodyElement.innerText.trim() && bodyElement.childNodes.length === 1 && bodyElement.querySelector("br")) {
      bodyElement.querySelector("br").remove();
    }
  }
  rgbToHex(rgb) {
    const sep = rgb.indexOf(",") > -1 ? "," : " ";
    rgb = rgb.substr(4).split(")")[0].split(sep);
    let r = (+rgb[0]).toString(16);
    let g = (+rgb[1]).toString(16);
    let b = (+rgb[2]).toString(16);
    if (r.length === 1) {
      r = "0" + r;
    }
    if (g.length === 1) {
      g = "0" + g;
    }
    if (b.length === 1) {
      b = "0" + b;
    }
    return "#" + r + g + b;
  }
  toggleCursorOnLink(event, action) {
    const selection = document.getSelection();
    const element = selection.focusNode ? selection.focusNode.parentNode : void 0;
    const isCtrl = event.key === "Control";
    const isCommand = event.key === "Meta";
    const isOnCtrlLink = this.isCursorPositionedInALink() && (isCtrl || isCommand);
    if (element) {
      if (isOnCtrlLink) {
        element["classList"][action]("po-clickable");
      } else {
        const isClickable = element["classList"] && element["classList"].contains("po-clickable");
        if (isClickable) {
          element["classList"].remove("po-clickable");
        }
      }
      this.updateModel();
    }
  }
  updateModel() {
    this.modelValue = this.bodyElement.nativeElement.innerHTML;
    this.value.emit(this.modelValue);
  }
  updateValueWithModelValue() {
    if (this.modelValue) {
      this.bodyElement.nativeElement.insertAdjacentHTML("afterbegin", this.modelValue);
    }
  }
  verifyCursorPositionInFirefoxIEEdge() {
    const textSelection = document.getSelection();
    const nodeLink = textSelection.focusNode;
    let isLink = false;
    if (nodeLink && nodeLink.nodeName === "A") {
      this.linkElement = nodeLink;
      isLink = true;
    } else {
      const range = textSelection.getRangeAt(0);
      const fragmentDocument = range.cloneContents();
      const element = fragmentDocument.childNodes[0] || fragmentDocument.firstElementChild;
      this.linkElement = element && element.nodeName === "A" ? element : void 0;
      isLink = !!this.linkElement;
    }
    return isLink;
  }
  get bodyEl() {
    return this.bodyElement?.nativeElement;
  }
  updateHasValue() {
    const text = this.bodyEl?.innerText?.trim();
    this.hasValue = !!text;
  }
  checkScrollState() {
    const el = this.bodyEl;
    if (!el) return;
    this.hasScroll = el.scrollHeight > el.clientHeight;
    this.cd.markForCheck();
  }
  initResizeObserver() {
    const el = this.bodyEl;
    if (!el || typeof ResizeObserver === "undefined") {
      return;
    }
    this.resizeObserver = new ResizeObserver(() => {
      this.checkScrollState();
    });
    this.resizeObserver.observe(el);
  }
  static ɵfac = function PoRichTextBodyComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoRichTextBodyComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoRichTextBodyComponent,
    selectors: [["po-rich-text-body"]],
    viewQuery: function PoRichTextBodyComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c126, 7);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.bodyElement = _t.first);
      }
    },
    hostVars: 1,
    hostBindings: function PoRichTextBodyComponent_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("resize", function PoRichTextBodyComponent_resize_HostBindingHandler() {
          return ctx.onWindowResize();
        }, ɵɵresolveWindow);
      }
      if (rf & 2) {
        ɵɵattribute("p-size", ctx.size);
      }
    },
    inputs: {
      height: [0, "p-height", "height"],
      label: [0, "p-label", "label"],
      modelValue: [0, "p-model-value", "modelValue"],
      placeholder: [0, "p-placeholder", "placeholder"],
      readonly: [0, "p-readonly", "readonly"],
      disabled: [0, "p-disabled", "disabled"],
      loading: [0, "p-loading", "loading"],
      size: [0, "p-size", "size"]
    },
    outputs: {
      change: "p-change",
      commands: "p-commands",
      keydown: "p-keydown",
      selectedLink: "p-selected-link",
      shortcutCommand: "p-shortcut-command",
      value: "p-value",
      blur: "p-blur"
    },
    standalone: false,
    features: [ɵɵNgOnChangesFeature],
    decls: 4,
    vars: 15,
    consts: [["bodyElement", ""], [1, "po-field-container-rich-text-body"], [1, "po-rich-text-body", 3, "blur", "click", "cut", "focus", "keydown", "keyup", "paste", "tabindex"], [1, "po-field-icon-container-right", "po-rich-text-loading-container"], [3, "p-size"]],
    template: function PoRichTextBodyComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "div", 1)(1, "div", 2, 0);
        ɵɵlistener("blur", function PoRichTextBodyComponent_Template_div_blur_1_listener() {
          return ctx.onBlur();
        })("click", function PoRichTextBodyComponent_Template_div_click_1_listener() {
          return ctx.onClick();
        })("cut", function PoRichTextBodyComponent_Template_div_cut_1_listener() {
          return ctx.update();
        })("focus", function PoRichTextBodyComponent_Template_div_focus_1_listener() {
          return ctx.onFocus();
        })("keydown", function PoRichTextBodyComponent_Template_div_keydown_1_listener($event) {
          return ctx.onKeyDown($event);
        })("keyup", function PoRichTextBodyComponent_Template_div_keyup_1_listener($event) {
          return ctx.onKeyUp($event);
        })("paste", function PoRichTextBodyComponent_Template_div_paste_1_listener() {
          return ctx.onPaste();
        });
        ɵɵelementEnd();
        ɵɵconditionalCreate(3, PoRichTextBodyComponent_Conditional_3_Template, 2, 1, "div", 3);
        ɵɵelementEnd();
      }
      if (rf & 2) {
        ɵɵadvance();
        ɵɵstyleProp("height", ctx.height, "px");
        ɵɵclassProp("has-value", ctx.hasValue)("has-scroll", ctx.hasScroll)("has-loading", ctx.loading)("po-rich-text-disabled", ctx.disabled);
        ɵɵproperty("tabindex", ctx.disabled ? -1 : 0);
        ɵɵattribute("aria-label", ctx.label)("contenteditable", !ctx.readonly && !ctx.disabled)("data-placeholder", ctx.placeholder);
        ɵɵadvance(2);
        ɵɵconditional(ctx.loading ? 3 : -1);
      }
    },
    dependencies: [PoLoadingIconComponent],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoRichTextBodyComponent, [{
    type: Component,
    args: [{
      selector: "po-rich-text-body",
      standalone: false,
      template: '<div class="po-field-container-rich-text-body">\n  <div\n    #bodyElement\n    class="po-rich-text-body"\n    [class.has-value]="hasValue"\n    [class.has-scroll]="hasScroll"\n    [class.has-loading]="loading"\n    [class.po-rich-text-disabled]="disabled"\n    [tabindex]="disabled ? -1 : 0"\n    [attr.aria-label]="label"\n    [attr.contenteditable]="!readonly && !disabled"\n    [attr.data-placeholder]="placeholder"\n    [style.height.px]="height"\n    (blur)="onBlur()"\n    (click)="onClick()"\n    (cut)="update()"\n    (focus)="onFocus()"\n    (keydown)="onKeyDown($event)"\n    (keyup)="onKeyUp($event)"\n    (paste)="onPaste()"\n  ></div>\n  @if (loading) {\n    <div class="po-field-icon-container-right po-rich-text-loading-container">\n      <po-loading-icon [p-size]="mapSizeToIcon(size)"></po-loading-icon>\n    </div>\n  }\n</div>\n'
    }]
  }], null, {
    bodyElement: [{
      type: ViewChild,
      args: ["bodyElement", {
        static: true
      }]
    }],
    height: [{
      type: Input,
      args: ["p-height"]
    }],
    label: [{
      type: Input,
      args: ["p-label"]
    }],
    modelValue: [{
      type: Input,
      args: ["p-model-value"]
    }],
    placeholder: [{
      type: Input,
      args: ["p-placeholder"]
    }],
    readonly: [{
      type: Input,
      args: ["p-readonly"]
    }],
    disabled: [{
      type: Input,
      args: ["p-disabled"]
    }],
    loading: [{
      type: Input,
      args: ["p-loading"]
    }],
    change: [{
      type: Output,
      args: ["p-change"]
    }],
    commands: [{
      type: Output,
      args: ["p-commands"]
    }],
    keydown: [{
      type: Output,
      args: ["p-keydown"]
    }],
    selectedLink: [{
      type: Output,
      args: ["p-selected-link"]
    }],
    size: [{
      type: HostBinding,
      args: ["attr.p-size"]
    }, {
      type: Input,
      args: ["p-size"]
    }],
    shortcutCommand: [{
      type: Output,
      args: ["p-shortcut-command"]
    }],
    value: [{
      type: Output,
      args: ["p-value"]
    }],
    blur: [{
      type: Output,
      args: ["p-blur"]
    }],
    onWindowResize: [{
      type: HostListener,
      args: ["window:resize"]
    }]
  });
})();
var poRichTextLiteralsDefault = {
  en: {
    bold: "Bold",
    italic: "Italic",
    underline: "Underline",
    left: "Left",
    center: "Center",
    right: "Right",
    justify: "Justify",
    textColor: "Text color",
    unorderedList: "Bulleted list",
    insertLink: "Insert link",
    linkTextLabel: "Text with link",
    linkUrlTextHelper: "Paste the copied link from the browser into the text box above",
    linkUrlTextPlaceholder: "Enter the link address that will be attached to the text.",
    cancel: "Cancel",
    editLink: "Edit link",
    insert: "Insert",
    insertImage: "Insert image",
    urlImage: "URL image"
  },
  es: {
    bold: "Negrita",
    italic: "Cursiva",
    underline: "Subrayado",
    left: "Izquierda",
    center: "Centro",
    right: "Derecha",
    justify: "Justificado",
    textColor: "Color del texto",
    unorderedList: "Lista con viñetas",
    insertLink: "Insertar el link",
    linkTextLabel: "Texto con enlace",
    linkUrlTextHelper: "Pega el enlace copiado del navegador en el cuadro de texto de arriba",
    linkUrlTextPlaceholder: "Ingrese la dirección del enlace que se adjuntará al texto.",
    cancel: "Cancelar",
    editLink: "Editar enlace",
    insert: "Insertar",
    insertImage: "Insertar imagen",
    urlImage: "Imagen URL"
  },
  pt: {
    bold: "Negrito",
    italic: "Itálico",
    underline: "Sublinhado",
    left: "Esquerda",
    center: "Centro",
    right: "Direita",
    justify: "Justificado",
    textColor: "Cor do texto",
    unorderedList: "Lista com marcadores",
    insertLink: "Inserir link",
    linkTextLabel: "Texto com link",
    linkUrlTextHelper: "Cole o link copiado do navegador na caixa de texto acima",
    linkUrlTextPlaceholder: "Insira o endereço do link que será anexado ao texto",
    cancel: "Cancelar",
    editLink: "Editar link",
    insert: "Inserir",
    insertImage: "Inserir imagem",
    urlImage: "Imagem em URL"
  },
  ru: {
    bold: "Жирный",
    italic: "Италик",
    underline: "Подчеркивание",
    left: "Влево",
    center: "Центр",
    right: "Вправо",
    justify: "Растянуть",
    textColor: "Цвет текста",
    unorderedList: "Список",
    insertLink: "Вставьте ссылку",
    linkTextLabel: "Текст со ссылкой",
    linkUrlTextHelper: "Вставьте скопированную ссылку из браузера в текстовое поле выше",
    linkUrlTextPlaceholder: "Вставьте ссылку для привязки к тексту",
    cancel: "Отмена",
    editLink: "Редактировать ссылку",
    insert: "Вставить",
    insertImage: "Вставить изображение",
    urlImage: "Ссылка на изображение"
  }
};
var PoUploadStatus;
(function(PoUploadStatus2) {
  PoUploadStatus2[PoUploadStatus2["Uploaded"] = 0] = "Uploaded";
  PoUploadStatus2[PoUploadStatus2["Uploading"] = 1] = "Uploading";
  PoUploadStatus2[PoUploadStatus2["Error"] = 2] = "Error";
  PoUploadStatus2[PoUploadStatus2["None"] = 3] = "None";
})(PoUploadStatus || (PoUploadStatus = {}));
var PoUploadFile = class {
  // Nome do arquivo.
  name;
  // Arquivo bruto.
  rawFile;
  // Status de envio do arquivo.
  status;
  // Identificador do arquivo.
  uid;
  // Extensão do arquivo.
  extension;
  // Tamanho do arquivo em bytes;
  size;
  // propriedade para auxiliar a exibição do texto no componente progress
  displayName;
  // porcentagem utilizada para repassar ao componente progress
  percent;
  // indica que o tamanho do arquivo não é permitido
  sizeNotAllowed;
  // indica que a extensão do arquivo não é permitida
  extensionNotAllowed;
  // Propriedade interna que indica que o status de sucesso deve estar oculto
  hideDoneContent;
  // Propriedade interna para mensagem de erro
  errorMessage;
  // Propriedade interna para url da thumbnail
  thumbnailUrl;
  // Propriedade interna para controlar se houve erro ao carregar a imagem da thumbnail.
  imageError;
  constructor(file) {
    if (file) {
      this.name = file.name;
      this.displayName = `${file.name} - ${this.getFileSize(file.size)}`;
      this.extension = this.getExtension(file.name);
      this.size = file.size;
      this.rawFile = file;
      this.uid = this.generateUUID();
      this.status = PoUploadStatus.None;
    }
  }
  getExtension(value) {
    if (value) {
      const extension = value.substr(value.lastIndexOf("."));
      return extension.toLowerCase();
    }
    return "";
  }
  generateUUID() {
    function s4() {
      return Math.floor((1 + Math.random()) * 65536).toString(16).substring(1);
    }
    return s4() + s4() + "-" + s4() + "-" + s4() + "-" + s4() + "-" + s4() + s4() + s4();
  }
  // Retorna o tamanho do arquivo em KBytes.
  getFileSize(size) {
    let kbSize = 0;
    if (size) {
      kbSize = Math.ceil(size / 1024);
    }
    return `${kbSize} KB`;
  }
};
var PoUploadBaseService = class _PoUploadBaseService {
  http = inject(HttpClient);
  formField = "files";
  requests = [];
  /**
   * Método responsável por enviar os arquivos ao servidor, conforme o parâmetro URL.
   *
   * @param url URL da requisição a ser efetuada.
   * @param files Arquivos a serem enviados.
   * @param tOnUpload Função a ser executada quando o arquivo for enviado ao servidor.
   * @param uploadCallback Função que será executada enquanto os arquivos estiverem sendo enviados.
   * @param successCallback Função a ser executada quando a requisição for efetuada com sucesso.
   * @param errorCallback Função a ser executada quando a requisição foi efetuada com sucesso.
   */
  upload(url, files, headers, tOnUpload, uploadCallback, successCallback, errorCallback) {
    const filesLength = files.length;
    const uploadEvent = {
      data: {},
      extraFormData: {},
      file: null,
      url,
      headers
    };
    for (let i2 = 0; i2 < filesLength; i2++) {
      const formData = new FormData();
      const file = files[i2];
      const fileName = file.rawFile.name;
      formData.append(this.formField, file.rawFile, fileName);
      if (tOnUpload) {
        uploadEvent["file"] = file;
        tOnUpload.emit(uploadEvent);
        Object.keys(uploadEvent.extraFormData).forEach((key2) => {
          formData.append(key2, uploadEvent.extraFormData[key2]);
        });
        formData.append("data", JSON.stringify(uploadEvent.data));
        url = uploadEvent.url;
        headers = uploadEvent.headers;
      }
      this.sendFile(url, file, headers, formData, uploadCallback, successCallback, errorCallback);
    }
  }
  sendFile(url, file, headers, formData, uploadCallback, successCallback, errorCallback) {
    let request = new Subscription();
    request = this.getRequest(url, headers, formData).subscribe((event) => {
      if (event.type === HttpEventType.UploadProgress) {
        this.addRequest(file, request);
        const percentDone = Math.round(100 * event.loaded / event.total);
        uploadCallback(file, percentDone);
      } else if (event instanceof HttpResponse) {
        successCallback(file, event);
      }
    }, (err) => {
      errorCallback(file, err);
    });
  }
  getRequest(url, headers, formData) {
    const httpHeaders = new HttpHeaders(headers);
    const req = new HttpRequest("POST", url, formData, {
      reportProgress: true,
      headers: httpHeaders
    });
    return this.http.request(req);
  }
  stopRequestByFile(file, callback) {
    const requestObj = this.requests.find((req) => req.file.uid === file.uid);
    if (requestObj) {
      const request = requestObj.request;
      request.unsubscribe();
      this.removeRequest(requestObj);
      callback();
    }
  }
  removeRequest(requestObj) {
    const index = this.requests.indexOf(requestObj);
    this.requests.splice(index, 1);
  }
  addRequest(file, request) {
    const hasRequest = this.requests.some((req) => req.file.uid === file.uid);
    if (!hasRequest) {
      this.requests.push({
        file,
        request
      });
    }
  }
  static ɵfac = function PoUploadBaseService_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoUploadBaseService)();
  };
  static ɵprov = ɵɵdefineInjectable({
    token: _PoUploadBaseService,
    factory: _PoUploadBaseService.ɵfac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoUploadBaseService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
var PoUploadService = class _PoUploadService extends PoUploadBaseService {
  static ɵfac = /* @__PURE__ */ (() => {
    let ɵPoUploadService_BaseFactory;
    return function PoUploadService_Factory(__ngFactoryType__) {
      return (ɵPoUploadService_BaseFactory || (ɵPoUploadService_BaseFactory = ɵɵgetInheritedFactory(_PoUploadService)))(__ngFactoryType__ || _PoUploadService);
    };
  })();
  static ɵprov = ɵɵdefineInjectable({
    token: _PoUploadService,
    factory: _PoUploadService.ɵfac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoUploadService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
var poUploadLiteralsDefault = {
  en: {
    files: "files",
    folders: "folders",
    selectFile: "Select file",
    selectFiles: "Select files",
    selectFolder: "Select folder",
    startSending: "Start sending",
    dragFilesHere: "Drag files here",
    dragFoldersHere: "Drag folders here",
    selectFilesOnComputer: "or select files on your computer",
    selectFolderOnComputer: "or select folder on your computer",
    dropFilesHere: "Drop files here",
    dropFoldersHere: "Drop folders here",
    invalidDropArea: "{0} were not dropped in the correct area",
    invalidFileType: "Failed to load {0} file(s) as it is not the allowed file type.",
    invalidAmount: "Failed to load {0} file(s), as it exceeds the limit amount of files.",
    invalidFormat: "Failed to load {0} file(s), as it does not match the format(s): {1}.",
    invalidSize: "Failed to load {0} files(s), as it is not the allowed size: from {1} to {2}.",
    numberOfFilesAllowed: "{0} file(s) allowed",
    allowedFormats: "Accepted file formats: {0}.",
    allowedFileSizeRange: "Size limit per file: from {0} to {1}",
    maxFileSizeAllowed: "Size limit per file: {0} maximum",
    minFileSizeAllowed: "Size limit per file: {0} minimum",
    errorOccurred: "An error has occurred",
    sentWithSuccess: "Sent with success",
    doneText: "Done",
    uploadingText: "Uploading",
    tryAgain: "Try again",
    close: "Close file",
    preview: "Preview",
    thumbnail: "thumbnail",
    continue: "Continue"
  },
  es: {
    files: "archivos",
    folders: "carpetas",
    selectFile: "Seleccionar archivo",
    selectFiles: "Seleccionar archivos",
    selectFolder: "Seleccionar carpeta",
    startSending: "Iniciar carga",
    dragFilesHere: "Arrastra los archivos aquí",
    dragFoldersHere: "Arrastra las carpetas aquí",
    selectFilesOnComputer: "o selecciona los archivos en tu computadora",
    selectFolderOnComputer: "o selecciona la carpeta en tu computadora",
    dropFilesHere: "Deja los archivos aquí",
    dropFoldersHere: "Deja las carpetas aquí",
    invalidDropArea: "Los {0} no se insertaron en la ubicación correcta",
    invalidFileType: "Error al cargar {0} archivo (s) ya que no es el tipo de archivo permitido",
    invalidAmount: "Error al cargar {0} archivo (s) ya que excede la cantidad limite de archivos.",
    invalidFormat: "Error al cargar {0} archivo (s) ya que no coincide con el formato (s): {1}.",
    invalidSize: "Error al cargar {0} archivo (s) ya que no cumple con el tamaño permitido: desde {1} hasta {2}.",
    numberOfFilesAllowed: "{0} archivo(s) permitido(s)",
    allowedFormats: "Formatos aceptados: {0}.",
    allowedFileSizeRange: "Limite de tamaño de archivo: desde {0} hasta {1}",
    maxFileSizeAllowed: "Limite de tamaño de archivo: hasta {0}",
    minFileSizeAllowed: "Limite de tamaño de archivo: minimo {0}",
    errorOccurred: "Ocurrio un error",
    sentWithSuccess: "Enviado con éxito",
    doneText: "Hecho",
    uploadingText: "Subiendo",
    tryAgain: "Inténtalo de nuevo",
    close: "Cerrar archivo",
    preview: "Vista previa",
    thumbnail: "miniatura",
    continue: "Continuar"
  },
  pt: {
    files: "arquivos",
    folders: "diretórios",
    selectFile: "Selecionar arquivo",
    selectFiles: "Selecionar arquivos",
    selectFolder: "Selecionar pasta",
    startSending: "Iniciar envio",
    dragFilesHere: "Arraste os arquivos aqui",
    dragFoldersHere: "Arraste as pastas aqui",
    selectFilesOnComputer: "ou selecione os arquivos no computador",
    selectFolderOnComputer: "ou selecione a pasta no computador",
    dropFilesHere: "Solte os arquivos aqui",
    dropFoldersHere: "Solte as pastas aqui",
    invalidDropArea: "Os {0} não foram inseridos no local correto",
    invalidFileType: "Falha ao carregar {0} arquivo (s), pois não é o tipo de arquivo permitido",
    invalidAmount: "Falha ao carregar {0} arquivo(s), pois excede(m) a quantidade limite de arquivos.",
    invalidFormat: "Falha ao carregar {0} arquivo(s), pois não corresponde(m) ao(s) formato(s): {1}.",
    invalidSize: "Falha ao carregar {0} arquivo(s), pois não atende ao tamanho permitido: {1} até {2}.",
    numberOfFilesAllowed: "Quantidade máxima: {0} arquivo(s)",
    allowedFormats: "Formatos adotados: {0}.",
    allowedFileSizeRange: "Limite de tamanho por arquivo: de {0} até {1}",
    maxFileSizeAllowed: "Limite de tamanho por arquivo: até {0}",
    minFileSizeAllowed: "Limite de tamanho por arquivo: no mínimo {0}",
    errorOccurred: "Ocorreu um erro",
    sentWithSuccess: "Enviado com sucesso",
    doneText: "Concluido",
    uploadingText: "Enviando",
    tryAgain: "Tente novamente",
    close: "Fechar arquivo",
    preview: "Pré-visualizar",
    thumbnail: "miniatura",
    continue: "Continuar"
  },
  ru: {
    files: "файлы",
    folders: "папки с файлами",
    selectFile: "Выберите файл",
    selectFiles: "Выберите файлы",
    selectFolder: "Выберите папку с файлами",
    startSending: "Начать загрузку",
    dragFilesHere: "Перетащите файлы сюда",
    dragFoldersHere: "Перетащите сюда папки",
    selectFilesOnComputer: "или выберите файлы на компьютере",
    selectFolderOnComputer: "или выберите папку на вашем компьютере",
    dropFilesHere: "Оставьте файлы здесь",
    dropFoldersHere: "Перетащите сюда папки",
    invalidDropArea: "{0} не были вставлены в правильном месте.",
    invalidFileType: "Не удалось загрузить файлы {0}, так как это неверный тип файла",
    invalidAmount: "Não foi possível carregar os arquivos {0} porque eles excederam o limite de arquivos.",
    invalidFormat: "Не удалось загрузить файлы {0}, так как они не соответствуют формату (ам): {1}.",
    invalidSize: "Не удалось загрузить файлы {0}, поскольку они не соответствуют разрешенному размеру: от {1} до {2}.",
    numberOfFilesAllowed: "Максимальное количество: {0} файлов",
    allowedFormats: "Форматы приняты: {0}.",
    allowedFileSizeRange: "Ограничение размера файла: от {0} до {1}",
    maxFileSizeAllowed: "Ограничение размера файла: до {0}",
    minFileSizeAllowed: "Ограничение размера файла: не менее {0}",
    errorOccurred: "Произошла ошибка.",
    sentWithSuccess: "Успешно отправлено",
    doneText: "Сделанный",
    uploadingText: "Загрузка",
    tryAgain: "Попробуйте еще раз",
    close: "Закрыть файл",
    preview: "Просмотр",
    thumbnail: "миниатюра",
    continue: "Продолжить"
  }
};
var poUploadFormFieldDefault = "files";
var poUploadMaxFileSize = 31457280;
var poUploadMinFileSize = 0;
var PoUploadBaseComponent = class _PoUploadBaseComponent {
  uploadService;
  cd;
  // Propriedade interna que define se o ícone de ajuda adicional terá cursor clicável (evento) ou padrão (tooltip).
  additionalHelpEventTrigger;
  /**
   *
   * @deprecated v23.x.x use `p-helper`
   *
   * @optional
   *
   * @description
   * Exibe um ícone de ajuda adicional, com o texto desta propriedade sendo passado para o popover do componente `po-helper`.
   * **Como boa prática, indica-se utilizar um texto com até 140 caracteres.**
   * > Requer um recuo mínimo de 8px se o componente estiver próximo à lateral da tela.
   *
   * > Essa propriedade está **depreciada** e será removida na versão `23.x.x`. Recomendamos utilizar a propriedade `p-helper` que oferece mais recursos e flexibilidade.
   */
  additionalHelpTooltip;
  /**
   * @optional
   *
   * @description
   *
   * Define que o popover (`p-helper`) será incluído no body da página e não dentro do componente. Essa
   * opção pode ser necessária em cenários com containers que possuem scroll ou overflow escondido, garantindo o
   * posicionamento correto do tooltip próximo ao elemento.
   *
   * > Quando utilizado com `p-helper`, leitores de tela como o NVDA podem não ler o conteúdo do popover.
   *
   * @default `false`
   */
  appendBox = false;
  /**
   * @optional
   *
   * @description
   *
   * Aplica foco no elemento ao ser iniciado.
   *
   * > Caso mais de um elemento seja configurado com essa propriedade, apenas o último elemento declarado com ela terá o foco.
   *
   * @default `false`
   */
  autoFocus = false;
  /**
   * @optional
   *
   * @description
   *
   * Define em *pixels* a altura da área onde podem ser arrastados os arquivos. A altura mínima aceita é `160px`.
   *
   * > Esta propriedade funciona somente se a propriedade `p-drag-drop` estiver habilitada.
   *
   * @default `320`
   */
  dragDropHeight;
  /** Rótulo do campo. */
  label;
  /**
   * @optional
   *
   * @description
   * Define se o título do campo será exibido de forma compacta.
   *
   * Quando habilitado (`true`), o modo compacto afeta o conjunto composto por:
   * - `po-label`
   * - `p-requirement (showRequired)`
   * - `po-helper`
   *
   * Ou seja, todos os elementos relacionados ao título do campo
   * (rótulo, indicador de obrigatoriedade e componente auxiliar) passam
   * a seguir o comportamento de layout compacto.
   *
   * Também é possível definir esse comportamento de forma global,
   * uma única vez, na folha de estilo geral da aplicação, por meio
   * da customização dos tokens CSS:
   *
   * - `--field-container-title-justify`
   * - `--field-container-title-flex`
   *
   * Exemplo:
   *
   * ```
   * :root {
   *   --field-container-title-justify: flex-start;
   *   --field-container-title-flex: 0 1 auto;
   * }
   * ```
   *
   * Dessa forma, o layout compacto passa a ser o padrão da aplicação,
   * sem a necessidade de definir a propriedade individualmente em cada campo.
   *
   * @default `false`
   */
  compactLabel = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "compactLabel"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-compact-label",
    transform: convertToBoolean
  }));
  /** Texto de apoio para o campo. */
  help;
  /** URL que deve ser feita a requisição com os arquivos selecionados. */
  url;
  /** Define o valor do atributo `name` do componente. */
  name = "file";
  /**
   * @optional
   *
   * @description
   *
   * Define se o envio do arquivo será automático ao selecionar o mesmo.
   *
   * > Esta propriedade funciona somente se a propriedade `p-url` tiver um valor atribuído.
   *
   * @default `false`
   */
  autoUpload = false;
  /**
   * @optional
   *
   * @description
   *
   * Desabilita botão de remover o(s) arquivo(s) selecionado(s).
   *
   * @default `false`
   */
  disabledRemoveFile = false;
  /**
   * @optional
   *
   * @description
   *
   * Define se a indicação de campo opcional será exibida.
   *
   * > Não será exibida a indicação se:
   *  - O campo conter `p-required`;
   *  - Não possuir `p-help` e/ou `p-label`.
   *
   * @default `false`
   */
  optional;
  /**
   * @optional
   *
   * @description
   *
   * Exibe a pré-visualização de imagens ao anexá-las.
   *
   * > Propriedade funciona apenas em arquivos de formato de imagem (`.png`, `.jpg`, `.jpeg` e `.gif`).
   * Será ignorada em outros tipos de arquivo.
   *
   * @default `true`
   */
  showThumbnail = true;
  /** Objeto que contém os cabeçalhos que será enviado na requisição dos arquivos. */
  headers;
  /**
   * @optional
   *
   * @description
   *
   * Define se a propriedade `p-url` é obrigatória.
   *
   * Caso a propriedade seja definida como `false`:
   * - o botão de "Selecionar arquivo" ficará habilitado mesmo sem a propriedade `p-url` definida.
   * - o botão "Iniciar envio" ficará oculto até que a propriedade `p-url` seja definida.
   *
   * > Se utilizada com a propriedade `p-auto-upload` definida como `true` será necessário definir a propriedade `p-url`.
   *
   * @default `true`
   */
  requiredUrl = true;
  /**
   * @optional
   *
   * @description
   *
   * Define as opções do componente de ajuda (po-helper) que será exibido ao lado do label quando a propriedade `p-label` for definida, ou, ao lado do componente na ausência da propriedade `p-label`.
   * > Para mais informações acesse: https://po-ui.io/documentation/po-helper.
   *
   * > Ao configurar esta propriedade, o antigo ícone de ajuda adicional (`p-additional-help-tooltip` e `p-additional-help`) será ignorado.
   */
  poHelperComponent = input(void 0, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "poHelperComponent"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-helper"
  }));
  /**
   * @optional
   *
   * @description
   *
   * Habilita a quebra automática do texto da propriedade `p-label`. Quando `p-label-text-wrap` for verdadeiro, o texto que excede
   * o espaço disponível é transferido para a próxima linha em pontos apropriados para uma
   * leitura clara.
   *
   * @default `false`
   */
  labelTextWrap = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "labelTextWrap"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-label-text-wrap"
  }));
  /**
   *
   * @deprecated v23.x.x use `p-helper`
   *
   * @optional
   *
   * @description
   * Evento disparado ao clicar no ícone de ajuda adicional.
   *
   * > Essa propriedade está **depreciada** e será removida na versão `23.x.x`. Recomendamos utilizar a propriedade `p-helper` que oferece mais recursos e flexibilidade.
   */
  additionalHelp = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Define uma ação personalizada no componente `po-upload`, adicionando um botão no canto inferior direito
   * de cada barra de progresso associada aos arquivos enviados ou em envio.
   *
   * A ação deve implementar a interface **PoProgressAction**, permitindo configurar propriedades como:
   * - `label`: Texto do botão.
   * - `icon`: Ícone a ser exibido no botão.
   * - `type`: Tipo de botão (ex.: `danger` ou `default`).
   * - `disabled`: Indica se o botão deve estar desabilitado.
   * - `visible`: Indica se o botão deve estar visível.
   *
   * **Exemplo de uso:**
   *
   * ```html
   * <po-upload
   *  [p-custom-action]="customAction"
   *  (p-custom-action-click)="onCustomActionClick($event)">
   * </po-upload>
   * ```
   *
   * ```typescript
   * customAction: PoProgressAction = {
   *   label: 'Baixar',
   *   icon: 'an an-download',
   *   type: 'default',
   *   visible: true
   * };
   *
   * onCustomActionClick(file: PoUploadFile) {
   *   console.log(`Ação personalizada clicada para o arquivo: ${file.name}`);
   * }
   * ```
   */
  customAction;
  /**
   * @optional
   *
   * @description
   *
   * Define uma ou duas ações personalizadas do modal de pré-visualização, adicionando um botão ou dois botões no canto inferior direito
   * do modal.
   *
   * A ação deve implementar a interface **PoModalAction**, permitindo configurar propriedades como:
   * - `label`: Texto do botão.
   * - `action`: Ícone a ser exibido no botão.
   * - `danger`: Define a propriedade `p-danger` do botão.
   * - `disabled`: Indica se o botão deve estar desabilitado.
   * - `visible`: Indica se o botão deve estar visível.
   *
   * **Exemplo de uso:**
   *
   * ```html
   * <po-upload
   *  [p-custom-modal-actions]="customActions"
   * </po-upload>
   * ```
   *
   * ```typescript
   * customActions:  Array<PoModalAction> = [
   *  { label: 'Confirmar', action: this.confirmModal.bind(this) },
   *  { label: 'Cancelar', action: this.closeModal.bind(this) }
   *];
   *
   * ```
   */
  customModalActions;
  /**
   * @optional
   *
   * @description
   *
   * Evento emitido ao clicar na ação personalizada configurada no `p-custom-action`.
   *
   * O evento retorna o arquivo associado à barra de progresso onde a ação foi clicada,
   * permitindo executar operações específicas para aquele arquivo.
   *
   * **Exemplo de uso:**
   *
   * ```html
   * <po-upload
   *  [p-custom-action]="customAction"
   *  (p-custom-action-click)="onCustomActionClick($event)">
   * </po-upload>
   * ```
   *
   * ```typescript
   * customAction: PoProgressAction = {
   *   label: 'Baixar',
   *   icon: 'an an-download',
   *   type: 'default',
   *   visible: true
   * };
   *
   * onCustomActionClick(file: PoUploadFile) {
   *   console.log(`Ação personalizada clicada para o arquivo: ${file.name}`);
   *   // Lógica para download do arquivo
   *   this.downloadFile(file);
   * }
   *
   * downloadFile(file: PoUploadFile) {
   *   // Exemplo de download
   *   console.log(`Iniciando o download do arquivo: ${file.name}`);
   * }
   * ```
   */
  customActionClick = new EventEmitter();
  /**
   * @optional
   *
   * @description
   * Evento disparado quando uma tecla é pressionada enquanto o foco está no componente.
   * Retorna um objeto `KeyboardEvent` com informações sobre a tecla.
   */
  keydown = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Função que será executada no momento de realizar o envio do arquivo,
   * onde será possível adicionar informações ao parâmetro que será enviado na requisição.
   * É passado por parâmetro um objeto com o arquivo e as propriedades data e extraFormData,
   * que serão enviadas em conjunto com o arquivo na requisição, por exemplo:
   *
   * > data, nesta propriedade pode ser informado algum dado
   * ```
   *   event.data = {id: 'id do usuário'};
   * ```
   * > extraFormData, nesta propriedade pode ser informado algum dado solicitado pela API
   * > que não possa estar no objeto `data`, assim o conteúdo sará extraído do objeto e
   * > enviado como parâmetro
   * ```
   *   event.extraFormData = {id: 'id do usuário'};
   * ```
   */
  onUpload = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Evento será disparado ao clicar no ícone de fechar.
   * > Por parâmetro será passado o objeto do arquivo.
   */
  onCancel = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Evento será disparado ao abrir o modal de pré-visualização.
   * > Por parâmetro será passado o objeto do arquivo.
   */
  onOpenModalPreview = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Evento será disparado ao clicar no ícone de remover.
   * > Por parâmetro será passado o objeto do arquivo.
   */
  onRemove = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Evento será disparado quando ocorrer algum erro no envio do arquivo.
   * > Por parâmetro será passado o objeto do retorno que é do tipo `HttpErrorResponse`.
   */
  onError = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Evento será disparado quando o envio do arquivo for realizado com sucesso.
   * > Por parâmetro será passado o objeto do retorno que é do tipo `HttpResponse`.
   */
  onSuccess = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Função para atualizar o ngModel do componente, necessário quando não for utilizado dentro da *tag* `form`.
   *
   * Na versão 12.2.0 do Angular a verificação `strictTemplates` vem true como default. Portanto, para utilizar
   * two-way binding no componente deve se utilizar da seguinte forma:
   *
   * ```
   * <po-upload ... [ngModel]="UploadModel" (ngModelChange)="uploadModel = $event"> </po-upload>
   * ```
   *
   */
  ngModelChange = new EventEmitter();
  allowedExtensions;
  currentFiles;
  canHandleDirectory;
  displayAdditionalHelp = false;
  onModelChange;
  extensionNotAllowed = 0;
  quantityNotAllowed = 0;
  sizeNotAllowed = 0;
  onModelTouched = null;
  _directory;
  _disabled;
  _dragDrop = false;
  _fileRestrictions;
  _formField;
  _hideRestrictionsInfo;
  _hideSelectButton;
  _hideSendButton;
  _isMultiple;
  _literals;
  _loading = false;
  _required;
  _size = void 0;
  _initialSize = void 0;
  language;
  validatorChange;
  /**
   * @optional
   *
   * @description
   *
   * Permite a seleção de diretórios contendo um ou mais arquivos para envio.
   *
   * > A habilitação desta propriedade se restringe apenas à seleção de diretórios.
   *
   * > Definição não suportada pelo browser **Internet Explorer**, todavia será possível a seleção de arquivos padrão.
   *
   * @default `false`
   */
  set directory(value) {
    this._directory = value;
    this.canHandleDirectory = this._directory && !PoUtils.isIE() && !PoUtils.isMobile();
    this.setDirectoryAttribute(this.canHandleDirectory);
  }
  get directory() {
    return this._directory;
  }
  /**
   * @optional
   *
   * @description
   *
   * Exibe a área onde é possível arrastar e selecionar os arquivos. Quando estiver definida, omite o botão para seleção de arquivos
   * automaticamente.
   *
   * > Recomendamos utilizar apenas um `po-upload` com esta funcionalidade por tela.
   *
   * @default `false`
   */
  set dragDrop(value) {
    this._dragDrop = value;
  }
  get dragDrop() {
    return this._dragDrop;
  }
  /**
   * @optional
   *
   * @description
   *
   * Oculta visualmente as informações de restrições para o upload.
   *
   * @default `false`
   */
  set hideRestrictionsInfo(value) {
    this._hideRestrictionsInfo = value;
  }
  get hideRestrictionsInfo() {
    return this._hideRestrictionsInfo;
  }
  /**
   * @optional
   *
   * @description
   *
   * Omite o botão de seleção de arquivos.
   *
   * > Caso o valor definido seja `true`, caberá ao desenvolvedor a responsabilidade
   * pela chamada do método `selectFiles()` para seleção de arquivos.
   *
   * @default `false`
   */
  set hideSelectButton(value) {
    this._hideSelectButton = value;
  }
  get hideSelectButton() {
    return this._hideSelectButton;
  }
  /**
   * @optional
   *
   * @description
   *
   * Omite o botão de envio de arquivos.
   *
   * > Caso o valor definido seja `true`, caberá ao desenvolvedor a responsabilidade
   * pela chamada do método `sendFiles()` para envio do(s) arquivo(s) selecionado(s).
   *
   * @default `false`
   */
  set hideSendButton(value) {
    this._hideSendButton = value;
  }
  get hideSendButton() {
    return this._hideSendButton;
  }
  /**
   * @optional
   *
   * @description
   *
   * Objeto com as literais usadas no `po-upload`.
   *
   * Existem duas maneiras de customizar o componente:
   *
   * - passando um objeto implementando a interface `PoUploadLiterals` com todas as literais disponíveis;
   * - passando apenas as literais que deseja customizar:
   *   ```
   *    const customLiterals: PoUploadLiterals = {
   *      folders: 'Pastas',
   *      selectFile: 'Buscar arquivo',
   *      startSending: 'Enviar'
   *    };
   *   ```
   *
   * E para carregar as literais customizadas, basta apenas passar o objeto para o componente:
   *
   * ```
   * <po-upload
   *   [p-literals]="customLiterals">
   * </po-upload>
   * ```
   *
   * > O objeto padrão de literais será traduzido de acordo com o idioma do *browser* (pt, en, es, ru).
   */
  set literals(value) {
    if (value instanceof Object && !(value instanceof Array)) {
      this._literals = __spreadValues(__spreadValues(__spreadValues({}, poUploadLiteralsDefault[poLocaleDefault]), poUploadLiteralsDefault[this.language]), value);
    } else {
      this._literals = poUploadLiteralsDefault[this.language];
    }
  }
  get literals() {
    return this._literals || poUploadLiteralsDefault[this.language];
  }
  /**
   * @optional
   *
   * @description
   *
   * Objeto que segue a definição da interface `PoUploadFileRestrictions`,
   * que possibilita definir tamanho máximo/mínimo e extensão dos arquivos permitidos.
   */
  set fileRestrictions(restrictions) {
    this._fileRestrictions = this.initRestrictions(restrictions);
    this.setAllowedExtensions(restrictions);
  }
  get fileRestrictions() {
    return this._fileRestrictions;
  }
  /**
   * @optional
   *
   * @description
   *
   * Nome do campo de formulário que será enviado para o serviço informado na propriedade `p-url`.
   *
   * @default `files`
   */
  set formField(value) {
    this._formField = value && typeof value === "string" ? value : poUploadFormFieldDefault;
    this.getUploadService().formField = this.formField;
  }
  get formField() {
    return this._formField;
  }
  /**
   * @optional
   *
   * @description
   *
   * Indica que o campo será desabilitado.
   */
  set disabled(value) {
    this._disabled = value;
    this.validateModel(this.currentFiles);
  }
  get disabled() {
    return this._disabled;
  }
  /**
   * @optional
   *
   * @description
   * Exibe um ícone de carregamento no botão `Selecionar arquivo`, à esquerda do texto, sinalizando que uma operação está em andamento.
   *
   * > Incompatível com `p-drag-drop` e `p-hide-select-button`, pois o estado de loading depende da exibição do botão `Selecionar arquivo`.
   *
   * @default `false`
   */
  set loading(value) {
    this._loading = value;
    this.cd?.markForCheck();
  }
  get loading() {
    return this._loading;
  }
  get isDisabled() {
    return this.disabled || this.loading;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define se pode selecionar mais de um arquivo.
   *
   * > Se utilizada a `p-directory`, habilita-se automaticamente esta propriedade.
   */
  set isMultiple(value) {
    this._isMultiple = value;
  }
  get isMultiple() {
    return this.canHandleDirectory ? true : this._isMultiple;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define que o campo será obrigatório.
   *
   * @default `false`
   */
  set required(required) {
    this._required = required;
    this.validateModel(this.currentFiles);
  }
  get required() {
    return this._required;
  }
  /**
   * Define se a indicação de campo obrigatório será exibida.
   *
   * > Não será exibida a indicação se:
   * - Não possuir `p-help` e/ou `p-label`.
   */
  showRequired = false;
  /**
   * @optional
   *
   * @description
   *
   * Define o tamanho e as ações do componente:
   * - `small`: altura do button como 32px (disponível apenas para acessibilidade AA).
   * - `medium`: altura do button como 44px.
   *
   * > Caso a acessibilidade AA não esteja configurada, o tamanho `medium` será mantido.
   * Para mais detalhes, consulte a documentação do [po-theme](https://po-ui.io/documentation/po-theme).
   *
   * @default `medium`
   */
  set size(value) {
    this._initialSize = value;
    this.applySizeBasedOnA11y();
  }
  get size() {
    return this._size ?? getDefaultSizeFn(PoFieldSize);
  }
  constructor(uploadService, languageService, cd) {
    this.uploadService = uploadService;
    this.cd = cd;
    this.language = languageService.getShortLanguage();
  }
  onThemeChange() {
    this.applySizeBasedOnA11y();
  }
  // Função implementada do ControlValueAccessor
  // Usada para interceptar os estados de habilitado via forms api
  setDisabledState(isDisabled) {
    this.disabled = isDisabled;
    this.cd.markForCheck();
  }
  registerOnChange(fn) {
    this.onModelChange = fn;
  }
  registerOnTouched(fn) {
    this.onModelTouched = fn;
  }
  registerOnValidatorChange(fn) {
    this.validatorChange = fn;
  }
  validate(abstractControl) {
    if (PoValidators.requiredFailed(this.required, this.disabled, abstractControl.value)) {
      return {
        required: {
          valid: false
        }
      };
    }
  }
  writeValue(model2) {
    if (model2) {
      if (!isEquals(this.currentFiles, model2)) {
        this.currentFiles = this.parseFiles(model2);
      }
    } else {
      this.currentFiles = void 0;
    }
  }
  isExceededFileLimit(currentFilesLength) {
    return this.isMultiple && this.fileRestrictions && this.fileRestrictions.maxFiles > 0 && this.fileRestrictions.maxFiles <= currentFilesLength;
  }
  // Faz o parse dos arquivos selecionados para arquivos do formato PoUploadFile e atualiza os arquivos correntes.
  parseFiles(files) {
    let poUploadFiles = this.currentFiles || [];
    const filesLength = files.length;
    for (let i2 = 0; i2 < filesLength; i2++) {
      if (this.isExceededFileLimit(poUploadFiles.length)) {
        this.quantityNotAllowed = filesLength - this.fileRestrictions.maxFiles;
        break;
      }
      const file = new PoUploadFile(files[i2]);
      let currentFile = file;
      if (!this.checkRestrictions(file)) {
        currentFile = {
          uid: file.uid,
          displayName: file.displayName,
          name: file.name,
          extension: file.extension,
          size: file.size,
          status: 2,
          sizeNotAllowed: file.sizeNotAllowed,
          extensionNotAllowed: file.extensionNotAllowed,
          errorMessage: ""
        };
      }
      if (this.showThumbnail && currentFile.rawFile?.type?.startsWith("image/")) {
        PoUtils.convertImageToBase64(currentFile.rawFile).then((base64) => {
          currentFile.thumbnailUrl = base64;
          this.cd.detectChanges();
        });
      }
      poUploadFiles = this.insertFileInFiles(currentFile, poUploadFiles);
      this.sendFeedback(currentFile);
    }
    this.sendFeedback();
    return poUploadFiles;
  }
  validateModel(model2) {
    if (this.validatorChange) {
      this.validatorChange(model2);
    }
  }
  // Verifica se o arquivo está de acordo com as restrições.
  checkRestrictions(file) {
    const restrictions = this.fileRestrictions;
    if (restrictions) {
      const allowedExtensions = restrictions.allowedExtensions;
      const minFileSize = restrictions.minFileSize;
      const maxFileSize = restrictions.maxFileSize;
      const isAccept = allowedExtensions ? this.isAllowedExtension(file.extension, allowedExtensions) : true;
      const isAcceptSize = file.size >= minFileSize && file.size <= maxFileSize;
      if (!isAcceptSize) {
        file.sizeNotAllowed = true;
        this.sizeNotAllowed = this.sizeNotAllowed + 1;
      }
      if (!isAccept) {
        file.extensionNotAllowed = true;
      }
      return isAccept && isAcceptSize;
    }
    return true;
  }
  existsFileSameName(file, files) {
    return files.some((currentFile) => file.name === currentFile.name);
  }
  getUploadService() {
    return this.uploadService;
  }
  insertFileInFiles(newFile, files) {
    if (this.existsFileSameName(newFile, files)) {
      return this.updateExistsFileInFiles(newFile, files);
    }
    if (this.isMultiple) {
      files.push(newFile);
    } else {
      files.splice(0, files.length, newFile);
    }
    return files;
  }
  isAllowedExtension(extension, allowedExtensions = []) {
    const isAllowed = allowedExtensions.some((ext) => ext.toLowerCase() === extension);
    if (!isAllowed) {
      this.extensionNotAllowed = this.extensionNotAllowed + 1;
    }
    return isAllowed;
  }
  setAllowedExtensions(restrictions = {}) {
    const _allowedExtensions = restrictions.allowedExtensions || [];
    this.allowedExtensions = _allowedExtensions.join(",");
  }
  initRestrictions(restrictions) {
    if (!restrictions) {
      return;
    }
    const minFileSize = restrictions.minFileSize || poUploadMinFileSize;
    const maxFileSize = restrictions.maxFileSize || poUploadMaxFileSize;
    return __spreadProps(__spreadValues({}, restrictions), {
      maxFileSize,
      minFileSize
    });
  }
  updateExistsFileInFiles(newFile, files) {
    const fileIndex = files.findIndex((currentFile) => newFile.name === currentFile.name && currentFile.status !== PoUploadStatus.Uploaded);
    if (fileIndex !== -1) {
      files.splice(fileIndex, 1, newFile);
    }
    return files;
  }
  applySizeBasedOnA11y() {
    const size = validateSizeFn(this._initialSize, PoFieldSize);
    this._size = size;
  }
  static ɵfac = function PoUploadBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoUploadBaseComponent)(ɵɵdirectiveInject(PoUploadService), ɵɵdirectiveInject(PoLanguageService), ɵɵdirectiveInject(ChangeDetectorRef));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoUploadBaseComponent,
    hostVars: 2,
    hostBindings: function PoUploadBaseComponent_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("PoUiThemeChange", function PoUploadBaseComponent_PoUiThemeChange_HostBindingHandler() {
          return ctx.onThemeChange();
        }, ɵɵresolveWindow);
      }
      if (rf & 2) {
        ɵɵattribute("p-required-url", ctx.requiredUrl)("p-size", ctx.size);
      }
    },
    inputs: {
      additionalHelpEventTrigger: "additionalHelpEventTrigger",
      additionalHelpTooltip: [0, "p-additional-help-tooltip", "additionalHelpTooltip"],
      appendBox: [2, "p-append-in-body", "appendBox", convertToBoolean],
      autoFocus: [2, "p-auto-focus", "autoFocus", convertToBoolean],
      dragDropHeight: [0, "p-drag-drop-height", "dragDropHeight"],
      label: [0, "p-label", "label"],
      compactLabel: [1, "p-compact-label", "compactLabel"],
      help: [0, "p-help", "help"],
      url: [0, "p-url", "url"],
      name: "name",
      autoUpload: [0, "p-auto-upload", "autoUpload"],
      disabledRemoveFile: [2, "p-disabled-remove-file", "disabledRemoveFile", convertToBoolean],
      optional: [0, "p-optional", "optional"],
      showThumbnail: [0, "p-show-thumbnail", "showThumbnail"],
      headers: [0, "p-headers", "headers"],
      requiredUrl: [2, "p-required-url", "requiredUrl", convertToBoolean],
      poHelperComponent: [1, "p-helper", "poHelperComponent"],
      labelTextWrap: [1, "p-label-text-wrap", "labelTextWrap"],
      customAction: [0, "p-custom-action", "customAction"],
      customModalActions: [0, "p-custom-modal-actions", "customModalActions"],
      directory: [2, "p-directory", "directory", convertToBoolean],
      dragDrop: [2, "p-drag-drop", "dragDrop", convertToBoolean],
      hideRestrictionsInfo: [2, "p-hide-restrictions-info", "hideRestrictionsInfo", convertToBoolean],
      hideSelectButton: [2, "p-hide-select-button", "hideSelectButton", convertToBoolean],
      hideSendButton: [2, "p-hide-send-button", "hideSendButton", convertToBoolean],
      literals: [0, "p-literals", "literals"],
      fileRestrictions: [0, "p-restrictions", "fileRestrictions"],
      formField: [0, "p-form-field", "formField"],
      disabled: [2, "p-disabled", "disabled", convertToBoolean],
      loading: [2, "p-loading", "loading", convertToBoolean],
      isMultiple: [2, "p-multiple", "isMultiple", convertToBoolean],
      required: [2, "p-required", "required", convertToBoolean],
      showRequired: [0, "p-show-required", "showRequired"],
      size: [0, "p-size", "size"]
    },
    outputs: {
      additionalHelp: "p-additional-help",
      customActionClick: "p-custom-action-click",
      keydown: "p-keydown",
      onUpload: "p-upload",
      onCancel: "p-cancel",
      onOpenModalPreview: "p-open-modal-preview",
      onRemove: "p-remove",
      onError: "p-error",
      onSuccess: "p-success",
      ngModelChange: "ngModelChange"
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoUploadBaseComponent, [{
    type: Directive
  }], () => [{
    type: PoUploadService
  }, {
    type: PoLanguageService
  }, {
    type: ChangeDetectorRef
  }], {
    additionalHelpEventTrigger: [{
      type: Input
    }],
    additionalHelpTooltip: [{
      type: Input,
      args: ["p-additional-help-tooltip"]
    }],
    appendBox: [{
      type: Input,
      args: [{
        alias: "p-append-in-body",
        transform: convertToBoolean
      }]
    }],
    autoFocus: [{
      type: Input,
      args: [{
        alias: "p-auto-focus",
        transform: convertToBoolean
      }]
    }],
    dragDropHeight: [{
      type: Input,
      args: ["p-drag-drop-height"]
    }],
    label: [{
      type: Input,
      args: ["p-label"]
    }],
    compactLabel: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-compact-label",
        required: false
      }]
    }],
    help: [{
      type: Input,
      args: ["p-help"]
    }],
    url: [{
      type: Input,
      args: ["p-url"]
    }],
    name: [{
      type: Input,
      args: ["name"]
    }],
    autoUpload: [{
      type: Input,
      args: ["p-auto-upload"]
    }],
    disabledRemoveFile: [{
      type: Input,
      args: [{
        alias: "p-disabled-remove-file",
        transform: convertToBoolean
      }]
    }],
    optional: [{
      type: Input,
      args: ["p-optional"]
    }],
    showThumbnail: [{
      type: Input,
      args: ["p-show-thumbnail"]
    }],
    headers: [{
      type: Input,
      args: ["p-headers"]
    }],
    requiredUrl: [{
      type: HostBinding,
      args: ["attr.p-required-url"]
    }, {
      type: Input,
      args: [{
        alias: "p-required-url",
        transform: convertToBoolean
      }]
    }],
    poHelperComponent: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-helper",
        required: false
      }]
    }],
    labelTextWrap: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-label-text-wrap",
        required: false
      }]
    }],
    additionalHelp: [{
      type: Output,
      args: ["p-additional-help"]
    }],
    customAction: [{
      type: Input,
      args: ["p-custom-action"]
    }],
    customModalActions: [{
      type: Input,
      args: ["p-custom-modal-actions"]
    }],
    customActionClick: [{
      type: Output,
      args: ["p-custom-action-click"]
    }],
    keydown: [{
      type: Output,
      args: ["p-keydown"]
    }],
    onUpload: [{
      type: Output,
      args: ["p-upload"]
    }],
    onCancel: [{
      type: Output,
      args: ["p-cancel"]
    }],
    onOpenModalPreview: [{
      type: Output,
      args: ["p-open-modal-preview"]
    }],
    onRemove: [{
      type: Output,
      args: ["p-remove"]
    }],
    onError: [{
      type: Output,
      args: ["p-error"]
    }],
    onSuccess: [{
      type: Output,
      args: ["p-success"]
    }],
    ngModelChange: [{
      type: Output,
      args: ["ngModelChange"]
    }],
    directory: [{
      type: Input,
      args: [{
        alias: "p-directory",
        transform: convertToBoolean
      }]
    }],
    dragDrop: [{
      type: Input,
      args: [{
        alias: "p-drag-drop",
        transform: convertToBoolean
      }]
    }],
    hideRestrictionsInfo: [{
      type: Input,
      args: [{
        alias: "p-hide-restrictions-info",
        transform: convertToBoolean
      }]
    }],
    hideSelectButton: [{
      type: Input,
      args: [{
        alias: "p-hide-select-button",
        transform: convertToBoolean
      }]
    }],
    hideSendButton: [{
      type: Input,
      args: [{
        alias: "p-hide-send-button",
        transform: convertToBoolean
      }]
    }],
    literals: [{
      type: Input,
      args: ["p-literals"]
    }],
    fileRestrictions: [{
      type: Input,
      args: ["p-restrictions"]
    }],
    formField: [{
      type: Input,
      args: ["p-form-field"]
    }],
    disabled: [{
      type: Input,
      args: [{
        alias: "p-disabled",
        transform: convertToBoolean
      }]
    }],
    loading: [{
      type: Input,
      args: [{
        alias: "p-loading",
        transform: convertToBoolean
      }]
    }],
    isMultiple: [{
      type: Input,
      args: [{
        alias: "p-multiple",
        transform: convertToBoolean
      }]
    }],
    required: [{
      type: Input,
      args: [{
        alias: "p-required",
        transform: convertToBoolean
      }]
    }],
    showRequired: [{
      type: Input,
      args: ["p-show-required"]
    }],
    size: [{
      type: Input,
      args: ["p-size"]
    }, {
      type: HostBinding,
      args: ["attr.p-size"]
    }],
    onThemeChange: [{
      type: HostListener,
      args: ["window:PoUiThemeChange"]
    }]
  });
})();
var PoUploadDragDropAreaComponent = class _PoUploadDragDropAreaComponent {
  elementRef = inject(ElementRef);
  directoryCompatible;
  disabled;
  height;
  literals;
  overlay;
  selectFiles = new EventEmitter();
  selectFilesLinkElement;
  focus() {
    this.selectFilesLinkElement.nativeElement.focus();
  }
  static ɵfac = function PoUploadDragDropAreaComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoUploadDragDropAreaComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoUploadDragDropAreaComponent,
    selectors: [["po-upload-drag-drop-area"]],
    viewQuery: function PoUploadDragDropAreaComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c127, 5);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.selectFilesLinkElement = _t.first);
      }
    },
    inputs: {
      directoryCompatible: [0, "p-directory-compatible", "directoryCompatible"],
      disabled: [0, "p-disabled", "disabled"],
      height: [0, "p-height", "height"],
      literals: [0, "p-literals", "literals"],
      overlay: [0, "p-overlay", "overlay"]
    },
    outputs: {
      selectFiles: "p-select-files"
    },
    standalone: false,
    decls: 4,
    vars: 6,
    consts: [["selectFilesLink", ""], [1, "po-upload-drag-drop-area", 3, "ngClass"], [1, "po-upload-drag-drop-area-container"], ["p-icon", "ICON_UPLOAD_CLOUD", 1, "po-upload-drag-drop-area-overlay-icon"], [1, "po-upload-drag-drop-area-overlay-label"], ["p-icon", "ICON_UPLOAD_CLOUD", 1, "po-upload-drag-drop-area-icon"], [1, "po-upload-drag-drop-area-label"], [1, "po-upload-drag-drop-area-select-files", 3, "click", "disabled", "ngClass"]],
    template: function PoUploadDragDropAreaComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "div", 1)(1, "div", 2);
        ɵɵconditionalCreate(2, PoUploadDragDropAreaComponent_Conditional_2_Template, 3, 1)(3, PoUploadDragDropAreaComponent_Conditional_3_Template, 6, 6);
        ɵɵelementEnd()();
      }
      if (rf & 2) {
        ɵɵstyleProp("height", ctx.height, "px");
        ɵɵproperty("ngClass", ɵɵpureFunction1(4, _c128, ctx.disabled));
        ɵɵadvance(2);
        ɵɵconditional(ctx.overlay && !ctx.disabled ? 2 : 3);
      }
    },
    dependencies: [NgClass, PoIconComponent],
    encapsulation: 2,
    changeDetection: 0
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoUploadDragDropAreaComponent, [{
    type: Component,
    args: [{
      selector: "po-upload-drag-drop-area",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: `<div
  class="po-upload-drag-drop-area"
  [ngClass]="{ 'po-upload-drag-drop-area-disabled': disabled }"
  [style.height.px]="height"
>
  <div class="po-upload-drag-drop-area-container">
    @if (overlay && !disabled) {
      <po-icon p-icon="ICON_UPLOAD_CLOUD" class="po-upload-drag-drop-area-overlay-icon"></po-icon>
      <div class="po-upload-drag-drop-area-overlay-label">
        {{ directoryCompatible ? literals?.dropFoldersHere : literals?.dropFilesHere }}
      </div>
    } @else {
      <po-icon p-icon="ICON_UPLOAD_CLOUD" class="po-upload-drag-drop-area-icon"></po-icon>
      <div class="po-upload-drag-drop-area-label">
        {{ directoryCompatible ? literals?.dragFoldersHere : literals?.dragFilesHere }}
      </div>
      <button
        #selectFilesLink
        class="po-upload-drag-drop-area-select-files"
        [disabled]="disabled"
        [ngClass]="{ 'po-clickable': !disabled }"
        (click)="selectFiles.emit()"
      >
        {{ directoryCompatible ? literals?.selectFolderOnComputer : literals?.selectFilesOnComputer }}
      </button>
    }
  </div>
</div>
`
    }]
  }], null, {
    directoryCompatible: [{
      type: Input,
      args: ["p-directory-compatible"]
    }],
    disabled: [{
      type: Input,
      args: ["p-disabled"]
    }],
    height: [{
      type: Input,
      args: ["p-height"]
    }],
    literals: [{
      type: Input,
      args: ["p-literals"]
    }],
    overlay: [{
      type: Input,
      args: ["p-overlay"]
    }],
    selectFiles: [{
      type: Output,
      args: ["p-select-files"]
    }],
    selectFilesLinkElement: [{
      type: ViewChild,
      args: ["selectFilesLink"]
    }]
  });
})();
var PoUploadDragDropAreaOverlayComponent = class _PoUploadDragDropAreaOverlayComponent {
  renderer = inject(Renderer2);
  DragDropAreaFixed;
  directoryCompatible;
  disabled;
  literals;
  target;
  areaElement = new EventEmitter();
  ngAfterViewInit() {
    if (this.target) {
      this.setPosition(this.target);
    }
    this.areaElement.emit(this.DragDropAreaFixed.nativeElement);
  }
  setPosition(targetElement) {
    const boundingClientRect = targetElement.nativeElement.getBoundingClientRect();
    const clientRectKeys = ["bottom", "left", "height", "right", "top", "width"];
    clientRectKeys.forEach((clientRectKey) => {
      const clientRectValue = boundingClientRect[clientRectKey];
      this.renderer.setStyle(this.DragDropAreaFixed.nativeElement, clientRectKey, `${clientRectValue}px`);
    });
  }
  static ɵfac = function PoUploadDragDropAreaOverlayComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoUploadDragDropAreaOverlayComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoUploadDragDropAreaOverlayComponent,
    selectors: [["po-upload-drag-drop-area-overlay"]],
    viewQuery: function PoUploadDragDropAreaOverlayComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c130, 7, ElementRef);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.DragDropAreaFixed = _t.first);
      }
    },
    inputs: {
      directoryCompatible: [0, "p-directory-compatible", "directoryCompatible"],
      disabled: [0, "p-disabled", "disabled"],
      literals: [0, "p-literals", "literals"],
      target: [0, "p-target", "target"]
    },
    outputs: {
      areaElement: "p-area-element"
    },
    standalone: false,
    decls: 3,
    vars: 4,
    consts: [["DragDropAreaFixed", ""], [1, "po-overlay-fixed"], [1, "po-upload-drag-drop-area-overlay", 3, "p-directory-compatible", "p-disabled", "p-literals", "p-overlay"]],
    template: function PoUploadDragDropAreaOverlayComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "div", 1);
        ɵɵelement(1, "po-upload-drag-drop-area", 2, 0);
        ɵɵelementEnd();
      }
      if (rf & 2) {
        ɵɵadvance();
        ɵɵproperty("p-directory-compatible", ctx.directoryCompatible)("p-disabled", ctx.disabled)("p-literals", ctx.literals)("p-overlay", true);
      }
    },
    dependencies: [PoUploadDragDropAreaComponent],
    encapsulation: 2,
    changeDetection: 0
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoUploadDragDropAreaOverlayComponent, [{
    type: Component,
    args: [{
      selector: "po-upload-drag-drop-area-overlay",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: '<div class="po-overlay-fixed">\n  <po-upload-drag-drop-area\n    #DragDropAreaFixed\n    class="po-upload-drag-drop-area-overlay"\n    [p-directory-compatible]="directoryCompatible"\n    [p-disabled]="disabled"\n    [p-literals]="literals"\n    [p-overlay]="true"\n  >\n  </po-upload-drag-drop-area>\n</div>\n'
    }]
  }], null, {
    DragDropAreaFixed: [{
      type: ViewChild,
      args: ["DragDropAreaFixed", {
        read: ElementRef,
        static: true
      }]
    }],
    directoryCompatible: [{
      type: Input,
      args: ["p-directory-compatible"]
    }],
    disabled: [{
      type: Input,
      args: ["p-disabled"]
    }],
    literals: [{
      type: Input,
      args: ["p-literals"]
    }],
    target: [{
      type: Input,
      args: ["p-target"]
    }],
    areaElement: [{
      type: Output,
      args: ["p-area-element"]
    }]
  });
})();
var PoUploadDragDropDirective = class _PoUploadDragDropDirective {
  i18nPipe = inject(PoI18nPipe);
  notification = inject(PoNotificationService);
  areaElement;
  directoryCompatible;
  disabled;
  literals;
  dragLeave = new EventEmitter();
  dragOver = new EventEmitter();
  fileChange = new EventEmitter();
  timeout;
  files;
  invalidFileType;
  onDragLeave(event) {
    event.preventDefault();
    event.stopPropagation();
    this.timeout = setTimeout(() => this.dragLeave.emit(), 30);
  }
  onDragOver(event) {
    event.preventDefault();
    event.stopPropagation();
    clearTimeout(this.timeout);
    if (!this.disabled) {
      this.dragOver.emit();
    }
  }
  onDrop(event) {
    event.preventDefault();
    event.stopPropagation();
    this.getFilesFromDataTransferItems(event);
    this.dragLeave.emit();
  }
  getFilesFromDataTransferItems(event) {
    if (!this.disabled) {
      this.invalidFileType = 0;
      if (this.directoryCompatible) {
        this.getOnlyDirectories(event.dataTransfer.items).then(() => {
          this.sendFiles(event, this.files);
        });
      } else {
        const files = this.getOnlyFiles(event.dataTransfer);
        this.sendFiles(event, files);
      }
    }
  }
  // analisa as entradas recursivamente
  getFilesFromEntry(entry) {
    return __async(this, null, function* () {
      if (entry.isFile) {
        const file = yield this.readFile(entry);
        return [file];
      } else if (entry.isDirectory) {
        return yield this.readDirectory(entry);
      }
    });
  }
  getOnlyDirectories(dataTransferItems) {
    return __async(this, null, function* () {
      const entries = [];
      for (const item of dataTransferItems) {
        entries.push(item.webkitGetAsEntry());
      }
      this.files = [];
      for (const entry of entries) {
        if (entry.isFile) {
          this.invalidFileType++;
        } else {
          const newFiles = yield this.getFilesFromEntry(entry);
          this.files = this.files.concat(newFiles);
        }
      }
    });
  }
  // return only files. If it is a directory, invalidFileType counts.
  getOnlyFiles(dataTransfer) {
    const fileList = Array.from(dataTransfer.files);
    const entriesFiles = Array.from(dataTransfer.items).map((item) => item.webkitGetAsEntry());
    return fileList.reduce((newFiles, file) => {
      const entryFile = entriesFiles.find((entry) => entry.name === file.name);
      if (entryFile.isFile) {
        return newFiles.concat(file);
      } else {
        this.invalidFileType++;
      }
      return newFiles;
    }, []);
  }
  readFile(entry) {
    return new Promise((resolve) => {
      entry.file((file) => {
        resolve(file);
      });
    });
  }
  readDirectory(entry) {
    return __async(this, null, function* () {
      const dirReader = entry.createReader();
      let files = [];
      const newFiles = yield this.readDirectoryEntries(dirReader);
      files = files.concat(newFiles);
      return files;
    });
  }
  readDirectoryEntries(dirReader) {
    return new Promise((resolve) => {
      dirReader.readEntries((entries) => __async(this, null, function* () {
        let files = [];
        for (const entry of entries) {
          const itemFiles = yield this.getFilesFromEntry(entry);
          files = files.concat(itemFiles);
        }
        resolve(files);
      }));
    });
  }
  sendFeedback(invalidFiles) {
    if (invalidFiles) {
      this.setPipeArguments("invalidFileType", invalidFiles);
    }
  }
  sendFiles(event, files) {
    if (this.areaElement.contains(event.target)) {
      if (files.length > 0) {
        this.fileChange.emit(files);
      }
      this.sendFeedback(this.invalidFileType);
    } else {
      const invalidDropAreaArg = this.directoryCompatible ? this.literals.folders : this.literals.files;
      this.setPipeArguments("invalidDropArea", invalidDropAreaArg);
    }
  }
  // método responsável por setar os argumentos do i18nPipe.
  setPipeArguments(literalAttributes, args) {
    const pipeArguments = this.i18nPipe.transform(this.literals[literalAttributes], args);
    this.notification.information(pipeArguments);
  }
  static ɵfac = function PoUploadDragDropDirective_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoUploadDragDropDirective)();
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoUploadDragDropDirective,
    selectors: [["", "p-upload-drag-drop", ""]],
    hostBindings: function PoUploadDragDropDirective_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("dragleave", function PoUploadDragDropDirective_dragleave_HostBindingHandler($event) {
          return ctx.onDragLeave($event);
        }, ɵɵresolveDocument)("dragover", function PoUploadDragDropDirective_dragover_HostBindingHandler($event) {
          return ctx.onDragOver($event);
        }, ɵɵresolveDocument)("drop", function PoUploadDragDropDirective_drop_HostBindingHandler($event) {
          return ctx.onDrop($event);
        }, ɵɵresolveDocument);
      }
    },
    inputs: {
      areaElement: [0, "p-area-element", "areaElement"],
      directoryCompatible: [0, "p-directory-compatible", "directoryCompatible"],
      disabled: [0, "p-disabled", "disabled"],
      literals: [0, "p-literals", "literals"]
    },
    outputs: {
      dragLeave: "p-drag-leave",
      dragOver: "p-drag-over",
      fileChange: "p-file-change"
    },
    standalone: false,
    features: [ɵɵProvidersFeature([PoI18nPipe])]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoUploadDragDropDirective, [{
    type: Directive,
    args: [{
      selector: "[p-upload-drag-drop]",
      providers: [PoI18nPipe],
      standalone: false
    }]
  }], null, {
    areaElement: [{
      type: Input,
      args: ["p-area-element"]
    }],
    directoryCompatible: [{
      type: Input,
      args: ["p-directory-compatible"]
    }],
    disabled: [{
      type: Input,
      args: ["p-disabled"]
    }],
    literals: [{
      type: Input,
      args: ["p-literals"]
    }],
    dragLeave: [{
      type: Output,
      args: ["p-drag-leave"]
    }],
    dragOver: [{
      type: Output,
      args: ["p-drag-over"]
    }],
    fileChange: [{
      type: Output,
      args: ["p-file-change"]
    }],
    onDragLeave: [{
      type: HostListener,
      args: ["document:dragleave", ["$event"]]
    }],
    onDragOver: [{
      type: HostListener,
      args: ["document:dragover", ["$event"]]
    }],
    onDrop: [{
      type: HostListener,
      args: ["document:drop", ["$event"]]
    }]
  });
})();
var PoUploadDragDropHeightDefault = 320;
var PoUploadDragDropHeightMin = 160;
var PoUploadDragDropComponent = class _PoUploadDragDropComponent {
  changeDetector = inject(ChangeDetectorRef);
  dragDropOverlayElement;
  dragDropAreaComponent;
  directoryCompatible;
  disabled;
  literals;
  fileChange = new EventEmitter();
  selectFiles = new EventEmitter();
  areaElement;
  isDragOver = false;
  _dragDropHeight = PoUploadDragDropHeightDefault;
  set dragDropHeight(value) {
    const dragDropHeight = convertToInt(value, PoUploadDragDropHeightDefault);
    this._dragDropHeight = dragDropHeight < PoUploadDragDropHeightMin ? PoUploadDragDropHeightMin : dragDropHeight;
  }
  get dragDropHeight() {
    return this._dragDropHeight;
  }
  focus() {
    this.dragDropAreaComponent.focus();
  }
  onAreaElement(element) {
    this.areaElement = element;
    this.changeDetector.detectChanges();
  }
  onDragLeave() {
    this.isDragOver = false;
  }
  onDragOver() {
    this.isDragOver = true;
  }
  onFileChange(files) {
    this.isDragOver = false;
    this.fileChange.emit(files);
  }
  static ɵfac = function PoUploadDragDropComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoUploadDragDropComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoUploadDragDropComponent,
    selectors: [["po-upload-drag-drop"]],
    viewQuery: function PoUploadDragDropComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c131, 5, ElementRef)(PoUploadDragDropAreaComponent, 7);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.dragDropOverlayElement = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.dragDropAreaComponent = _t.first);
      }
    },
    inputs: {
      directoryCompatible: [0, "p-directory-compatible", "directoryCompatible"],
      disabled: [0, "p-disabled", "disabled"],
      literals: [0, "p-literals", "literals"],
      dragDropHeight: [0, "p-drag-drop-height", "dragDropHeight"]
    },
    outputs: {
      fileChange: "p-file-change",
      selectFiles: "p-select-files"
    },
    standalone: false,
    decls: 2,
    vars: 7,
    consts: [["dragDropOverlay", ""], [3, "p-directory-compatible", "p-disabled", "p-literals", "p-target"], ["p-upload-drag-drop", "", 3, "p-drag-leave", "p-drag-over", "p-file-change", "p-select-files", "p-area-element", "p-directory-compatible", "p-disabled", "p-height", "p-literals", "p-overlay"], [3, "p-area-element", "p-directory-compatible", "p-disabled", "p-literals", "p-target"]],
    template: function PoUploadDragDropComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵconditionalCreate(0, PoUploadDragDropComponent_Conditional_0_Template, 2, 4, "po-upload-drag-drop-area-overlay", 1);
        ɵɵelementStart(1, "po-upload-drag-drop-area", 2);
        ɵɵlistener("p-drag-leave", function PoUploadDragDropComponent_Template_po_upload_drag_drop_area_p_drag_leave_1_listener() {
          return ctx.onDragLeave();
        })("p-drag-over", function PoUploadDragDropComponent_Template_po_upload_drag_drop_area_p_drag_over_1_listener() {
          return ctx.onDragOver();
        })("p-file-change", function PoUploadDragDropComponent_Template_po_upload_drag_drop_area_p_file_change_1_listener($event) {
          return ctx.onFileChange($event);
        })("p-select-files", function PoUploadDragDropComponent_Template_po_upload_drag_drop_area_p_select_files_1_listener() {
          return ctx.selectFiles.emit();
        });
        ɵɵelementEnd();
      }
      if (rf & 2) {
        ɵɵconditional(ctx.isDragOver ? 0 : -1);
        ɵɵadvance();
        ɵɵproperty("p-area-element", ctx.areaElement)("p-directory-compatible", ctx.directoryCompatible)("p-disabled", ctx.disabled)("p-height", ctx.dragDropHeight)("p-literals", ctx.literals)("p-overlay", false);
      }
    },
    dependencies: [PoUploadDragDropAreaComponent, PoUploadDragDropAreaOverlayComponent, PoUploadDragDropDirective],
    encapsulation: 2,
    changeDetection: 0
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoUploadDragDropComponent, [{
    type: Component,
    args: [{
      selector: "po-upload-drag-drop",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: '@if (isDragOver) {\n  <po-upload-drag-drop-area-overlay\n    #dragDropOverlay\n    [p-directory-compatible]="directoryCompatible"\n    [p-disabled]="disabled"\n    [p-literals]="literals"\n    [p-target]="dragDropAreaComponent.elementRef"\n    (p-area-element)="onAreaElement($event)"\n  >\n  </po-upload-drag-drop-area-overlay>\n}\n\n<po-upload-drag-drop-area\n  p-upload-drag-drop\n  [p-area-element]="areaElement"\n  [p-directory-compatible]="directoryCompatible"\n  [p-disabled]="disabled"\n  [p-height]="dragDropHeight"\n  [p-literals]="literals"\n  [p-overlay]="false"\n  (p-drag-leave)="onDragLeave()"\n  (p-drag-over)="onDragOver()"\n  (p-file-change)="onFileChange($event)"\n  (p-select-files)="selectFiles.emit()"\n>\n</po-upload-drag-drop-area>\n'
    }]
  }], null, {
    dragDropOverlayElement: [{
      type: ViewChild,
      args: ["dragDropOverlay", {
        read: ElementRef
      }]
    }],
    dragDropAreaComponent: [{
      type: ViewChild,
      args: [PoUploadDragDropAreaComponent, {
        static: true
      }]
    }],
    directoryCompatible: [{
      type: Input,
      args: ["p-directory-compatible"]
    }],
    disabled: [{
      type: Input,
      args: ["p-disabled"]
    }],
    literals: [{
      type: Input,
      args: ["p-literals"]
    }],
    fileChange: [{
      type: Output,
      args: ["p-file-change"]
    }],
    selectFiles: [{
      type: Output,
      args: ["p-select-files"]
    }],
    dragDropHeight: [{
      type: Input,
      args: ["p-drag-drop-height"]
    }]
  });
})();
var PoUploadFileRestrictionsComponent = class _PoUploadFileRestrictionsComponent {
  changeDetector = inject(ChangeDetectorRef);
  maxFiles;
  literals;
  _allowedExtensions;
  _maxFileSize;
  _minFileSize;
  language;
  set allowedExtensions(value) {
    this._allowedExtensions = this.formatAllowedExtensions(value);
  }
  get allowedExtensions() {
    return this._allowedExtensions;
  }
  set maxFileSize(value) {
    this._maxFileSize = formatBytes(value);
  }
  get maxFileSize() {
    return this._maxFileSize;
  }
  set minFileSize(value) {
    this._minFileSize = formatBytes(value);
  }
  get minFileSize() {
    return this._minFileSize;
  }
  constructor() {
    const languageService = inject(PoLanguageService);
    this.language = languageService.getShortLanguage();
  }
  ngOnInit() {
    this.setLiterals();
  }
  formatAllowedExtensions(allowedExtensions) {
    const conjunction = {
      "pt": "e",
      "en": "and",
      "es": "y",
      "ru": "и"
    };
    return allowedExtensions ? allowedExtensions.join(", ").toUpperCase().replace(/,(?=[^,]*$)/, ` ${conjunction[this.language]}`) : void 0;
  }
  setLiterals() {
    this.literals = __spreadValues(__spreadValues({}, poUploadLiteralsDefault[poLocaleDefault]), poUploadLiteralsDefault[this.language]);
    this.changeDetector.detectChanges();
  }
  static ɵfac = function PoUploadFileRestrictionsComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoUploadFileRestrictionsComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoUploadFileRestrictionsComponent,
    selectors: [["po-upload-file-restrictions"]],
    inputs: {
      maxFiles: [0, "p-max-files", "maxFiles"],
      allowedExtensions: [0, "p-allowed-extensions", "allowedExtensions"],
      maxFileSize: [0, "p-max-file-size", "maxFileSize"],
      minFileSize: [0, "p-min-file-size", "minFileSize"]
    },
    standalone: false,
    decls: 3,
    vars: 3,
    template: function PoUploadFileRestrictionsComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵconditionalCreate(0, PoUploadFileRestrictionsComponent_Conditional_0_Template, 3, 4, "p");
        ɵɵconditionalCreate(1, PoUploadFileRestrictionsComponent_Conditional_1_Template, 3, 4, "p");
        ɵɵconditionalCreate(2, PoUploadFileRestrictionsComponent_Conditional_2_Template, 6, 4, "p");
      }
      if (rf & 2) {
        ɵɵconditional(ctx.maxFiles > 1 ? 0 : -1);
        ɵɵadvance();
        ɵɵconditional(ctx.allowedExtensions ? 1 : -1);
        ɵɵadvance();
        ɵɵconditional(ctx.minFileSize || ctx.maxFileSize ? 2 : -1);
      }
    },
    dependencies: [PoI18nPipe],
    encapsulation: 2,
    changeDetection: 0
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoUploadFileRestrictionsComponent, [{
    type: Component,
    args: [{
      selector: "po-upload-file-restrictions",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: "@if (maxFiles > 1) {\n  <p>\n    {{ literals?.numberOfFilesAllowed | poI18n: maxFiles }}\n  </p>\n}\n\n@if (allowedExtensions) {\n  <p>\n    {{ literals?.allowedFormats | poI18n: allowedExtensions }}\n  </p>\n}\n\n@if (minFileSize || maxFileSize) {\n  <p>\n    <span>{{ literals?.allowedSizes }}</span>\n    @if (minFileSize && maxFileSize) {\n      <span>\n        {{ literals?.allowedFileSizeRange | poI18n: [minFileSize, maxFileSize] }}\n      </span>\n    }\n    @if (minFileSize && !maxFileSize) {\n      <span>\n        {{ literals?.minFileSizeAllowed | poI18n: minFileSize }}\n      </span>\n    }\n    @if (maxFileSize && !minFileSize) {\n      <span>\n        {{ literals?.maxFileSizeAllowed | poI18n: maxFileSize }}\n      </span>\n    }\n  </p>\n}\n"
    }]
  }], () => [], {
    maxFiles: [{
      type: Input,
      args: ["p-max-files"]
    }],
    allowedExtensions: [{
      type: Input,
      args: ["p-allowed-extensions"]
    }],
    maxFileSize: [{
      type: Input,
      args: ["p-max-file-size"]
    }],
    minFileSize: [{
      type: Input,
      args: ["p-min-file-size"]
    }]
  });
})();
var PoUploadComponent = class _PoUploadComponent extends PoUploadBaseComponent {
  renderer = inject(Renderer2);
  i18nPipe = inject(PoI18nPipe);
  notification = inject(PoNotificationService);
  inputFile;
  poUploadDragDropComponent;
  uploadButton;
  modalComponent;
  helperEl;
  id = `po-upload[${uuid()}]`;
  infoByUploadStatus = {
    [PoUploadStatus.Uploaded]: {
      text: () => this.literals.sentWithSuccess,
      icon: "ICON_OK"
    },
    [PoUploadStatus.Error]: {
      text: () => this.literals.errorOccurred
    },
    [PoUploadStatus.Uploading]: {
      text: (percent) => percent + "%"
    }
  };
  progressStatusByFileStatus = {
    [PoUploadStatus.Uploaded]: PoProgressStatus.Success,
    [PoUploadStatus.Error]: PoProgressStatus.Error
  };
  calledByCleanInputValue = false;
  modalPrimaryAction;
  modalSecondaryAction;
  errorMessage;
  tooltipTitle = "";
  modalImageUrl = "";
  errorModalImage = false;
  constructor() {
    const uploadService = inject(PoUploadService);
    const languageService = inject(PoLanguageService);
    const cd = inject(ChangeDetectorRef);
    super(uploadService, languageService, cd);
  }
  get displayDragDrop() {
    return this.dragDrop && !PoUtils.isMobile();
  }
  get displaySendButton() {
    const currentFiles = this.currentFiles || [];
    return !this.hideSendButton && !this.autoUpload && currentFiles.length > 0 && this.hasFileNotUploaded && this.requiredUrl;
  }
  get selectFileButtonLabel() {
    if (this.canHandleDirectory) {
      return this.literals.selectFolder;
    } else if (this.isMultiple) {
      return this.literals.selectFiles;
    } else {
      return this.literals.selectFile;
    }
  }
  get hasMoreThanFourItems() {
    return this.currentFiles && this.currentFiles.length > 4;
  }
  get hasMultipleFiles() {
    return this.currentFiles && this.currentFiles.length > 1;
  }
  get hasFileNotUploaded() {
    if (Array.isArray(this.currentFiles)) {
      return this.currentFiles.some((file) => file.status !== PoUploadStatus.Uploaded && file.status !== PoUploadStatus.Error);
    }
    return false;
  }
  get isDisabled() {
    const currentFiles = this.currentFiles || [];
    return this.requiredUrl ? !!(this.hasAnyFileUploading(currentFiles) || !this.url || this.disabled || this.isExceededFileLimit(currentFiles.length)) : !!(this.hasAnyFileUploading(currentFiles) || this.autoUpload || this.disabled || this.isExceededFileLimit(currentFiles.length));
  }
  get isSelectButtonDisabled() {
    return this.loading || this.isDisabled;
  }
  get isFieldDisabled() {
    const isSelectButtonVisible = !this.hideSelectButton && !this.displayDragDrop;
    return isSelectButtonVisible && this.loading || this.disabled;
  }
  get maxFiles() {
    return this.isMultiple && this.fileRestrictions && this.fileRestrictions.maxFiles;
  }
  cancel(file, keydown) {
    if (this.disabledRemoveFile || keydown && keydown.code !== "Enter" && keydown.code !== "Space") return;
    if (file.status === PoUploadStatus.Uploading) {
      return this.stopUpload(file);
    }
    this.removeFile(file);
    if (file.status !== PoUploadStatus.Uploaded) {
      this.onCancel.emit(file);
    } else {
      this.onRemove.emit(file);
    }
  }
  ngOnChanges(changes) {
    if (changes["label"]) {
      this.displayAdditionalHelp = false;
    }
    if (changes["customModalActions"]) {
      if (this.customModalActions?.length > 0) {
        this.modalPrimaryAction = this.customModalActions[0];
        this.modalSecondaryAction = this.customModalActions[1] || void 0;
      } else {
        this.setPrimaryActionModal();
      }
    }
  }
  ngAfterViewInit() {
    if (!this.customModalActions?.length) {
      this.setPrimaryActionModal();
    }
    if (this.autoFocus) {
      this.focus();
    }
  }
  /** Método responsável por **limpar** o(s) arquivo(s) selecionado(s). */
  clear() {
    this.currentFiles = void 0;
    this.updateModel([]);
    this.cleanInputValue();
  }
  emitAdditionalHelp() {
    if (this.label && this.isAdditionalHelpEventTriggered()) {
      this.additionalHelp.emit();
    }
  }
  /**
   * Função que atribui foco ao componente.
   *
   * Para utilizá-la é necessário ter a instância do componente no DOM, podendo ser utilizado o ViewChild da seguinte forma:
   *
   * ```
   * import { PoUploadComponent } from '@po-ui/ng-components';
   *
   * ...
   *
   * @ViewChild(PoUploadComponent, { static: true }) upload: PoUploadComponent;
   *
   * focusUpload() {
   *   this.upload.focus();
   * }
   * ```
   */
  focus() {
    if (!this.disabled) {
      if (this.uploadButton) {
        this.uploadButton.focus();
        return;
      }
      if (this.displayDragDrop) {
        this.poUploadDragDropComponent.focus();
      }
    }
  }
  getAdditionalHelpTooltip() {
    return this.isAdditionalHelpEventTriggered() ? null : this.additionalHelpTooltip;
  }
  // Verifica se existe algum arquivo sendo enviado ao serviço.
  hasAnyFileUploading(files) {
    if (files && files.length) {
      return files.some((file) => file.status === PoUploadStatus.Uploading);
    }
    return false;
  }
  isAllowCancelEvent(status) {
    return status !== PoUploadStatus.Uploaded;
  }
  onFileChange(event) {
    if (this.calledByCleanInputValue) {
      this.calledByCleanInputValue = false;
      return event.preventDefault();
    }
    const files = event.target.files;
    this.updateFiles(files);
    this.cleanInputValue();
  }
  onFileChangeDragDrop(files) {
    this.updateFiles(files);
  }
  onKeyDown(event) {
    if (this.isUploadButtonFocused()) {
      this.keydown.emit(event);
    }
  }
  /**
   * Método responsável por fechar o modal.
   */
  closeModal() {
    this.errorModalImage = false;
    this.modalComponent.close();
    this.modalImageUrl = "";
  }
  openModal(file, keydown) {
    if (keydown && keydown.code !== "Enter" && keydown.code !== "Space") return;
    if (file?.thumbnailUrl && !file.errorMessage) {
      this.errorModalImage = false;
      this.modalComponent.open();
      this.modalImageUrl = file.thumbnailUrl;
      this.onOpenModalPreview.emit(file);
    }
  }
  // Remove o arquivo passado por parâmetro da lista dos arquivos correntes.
  removeFile(file) {
    const index = this.currentFiles.findIndex((f) => f.uid === file.uid);
    if (index !== -1) {
      this.currentFiles.splice(index, 1);
    }
    this.updateModel([...this.currentFiles]);
  }
  /** Método responsável por **abrir** a janela para seleção de arquivo(s). */
  selectFiles() {
    this.onModelTouched?.();
    this.calledByCleanInputValue = false;
    this.inputFile.nativeElement.click();
  }
  sendFeedback(file) {
    let sizeNotAllowed = this.sizeNotAllowed;
    let extensionNotAllowed = this.extensionNotAllowed;
    let quantityNotAllowed = this.quantityNotAllowed;
    if (file) {
      sizeNotAllowed = file.sizeNotAllowed ? 1 : void 0;
      extensionNotAllowed = file.extensionNotAllowed ? 1 : void 0;
      quantityNotAllowed = void 0;
    }
    if (sizeNotAllowed > 0) {
      const minFileSize = formatBytes(this.fileRestrictions.minFileSize);
      const maxFileSize = formatBytes(this.fileRestrictions.maxFileSize);
      const args = [this.sizeNotAllowed, minFileSize || "0", maxFileSize];
      this.setPipeArguments("invalidSize", args, file);
      this.sizeNotAllowed = 0;
    }
    if (extensionNotAllowed > 0) {
      const allowedExtensionsFormatted = this.fileRestrictions.allowedExtensions.join(", ").toUpperCase();
      const args = [this.extensionNotAllowed, allowedExtensionsFormatted];
      this.setPipeArguments("invalidFormat", args, file);
      this.extensionNotAllowed = 0;
    }
    if (quantityNotAllowed > 0) {
      const args = [this.quantityNotAllowed];
      this.setPipeArguments("invalidAmount", args);
      this.quantityNotAllowed = 0;
    }
  }
  /** Método responsável por **enviar** o(s) arquivo(s) selecionado(s). */
  sendFiles() {
    if (this.currentFiles && this.currentFiles.length) {
      this.uploadFiles(this.currentFiles);
    }
  }
  setDirectoryAttribute(canHandleDirectory) {
    if (canHandleDirectory) {
      this.renderer.setAttribute(this.inputFile.nativeElement, "webkitdirectory", "true");
    } else {
      this.renderer.removeAttribute(this.inputFile.nativeElement, "webkitdirectory");
    }
  }
  /**
   * Método que exibe `p-helper` ou executa a ação definida em `p-helper{eventOnClick}` ou em `p-additionalHelp`.
   * Para isso, será necessário configurar uma tecla de atalho utilizando o evento `p-keydown`.
   *
   * > Exibe ou oculta o conteúdo do componente `po-helper` quando o componente estiver com foco.
   *
   * ```
   * //Exemplo com p-label e p-helper
   * <po-upload
   *  #upload
   *  ...
   *  p-label="Label do upload"
   *  [p-helper]="helperOptions"
   *  (p-keydown)="onKeyDown($event, upload)"
   * ></po-upload>
   * ```
   * ```
   * ...
   * onKeyDown(event: KeyboardEvent, inp: PoUploadComponent): void {
   *  if (event.code === 'F9') {
   *    inp.showAdditionalHelp();
   *  }
   * }
   * ```
   */
  showAdditionalHelp() {
    this.displayAdditionalHelp = !this.displayAdditionalHelp;
    const helper = this.poHelperComponent();
    const isHelpEvt = this.isAdditionalHelpEventTriggered();
    if (!this.label && (helper || this.additionalHelpTooltip || isHelpEvt)) {
      if (isHelpEvt) {
        this.additionalHelp.emit();
      }
      if (typeof helper !== "string" && typeof helper?.eventOnClick === "function") {
        helper.eventOnClick();
        return;
      }
      if (this.helperEl?.helperIsVisible()) {
        this.helperEl?.closeHelperPopover();
        return;
      }
      this.helperEl?.openHelperPopover();
      return;
    }
    return this.displayAdditionalHelp;
  }
  stopUpload(file) {
    this.uploadService.stopRequestByFile(file, () => {
      if (this.autoUpload) {
        this.removeFile(file);
      } else {
        this.stopUploadHandler(file);
      }
      this.cd.markForCheck();
    });
  }
  trackByFn(index, file) {
    return file.uid;
  }
  uploadFiles(files) {
    const filesFiltered = files.filter((file) => file.status !== PoUploadStatus.Uploaded && !file.sizeNotAllowed && !file.extensionNotAllowed);
    if (files.length === 0) return;
    this.uploadService.upload(this.url, filesFiltered, this.headers, this.onUpload, (file, percent) => {
      this.uploadingHandler(file, percent);
    }, (file, eventResponse) => {
      this.responseHandler(file, PoUploadStatus.Uploaded);
      this.onSuccess.emit(eventResponse);
      setTimeout(() => {
        const currentFile = this.currentFiles.find((f) => f.uid === file.uid);
        if (currentFile) {
          currentFile.hideDoneContent = true;
          this.cd.detectChanges();
        }
      }, 500);
    }, (file, eventError) => {
      this.responseHandler(file, PoUploadStatus.Error);
      this.onError.emit(eventError);
    });
  }
  customClick(file) {
    if (this.customAction) {
      this.customActionClick.emit(file);
    }
  }
  setHelper(label, additionalHelpTooltip) {
    return setHelperSettings(label, additionalHelpTooltip, this.poHelperComponent(), this.size, this.isAdditionalHelpEventTriggered() ? this.additionalHelp : void 0);
  }
  actionIsDisabled(action) {
    return isTypeof(action.disabled, "function") ? action.disabled(action) : action.disabled;
  }
  isActionVisible(action) {
    if (!action || !action.label && !action.icon) {
      return false;
    }
    if (action.visible === void 0) {
      return true;
    }
    if (isTypeof(action.visible, "function")) {
      return action.visible();
    }
    return !!action.visible;
  }
  onImageError(file) {
    file.imageError = true;
  }
  showTooltipText(e2, text) {
    const element = e2.target;
    if (element.offsetWidth < element.scrollWidth) {
      this.tooltipTitle = text;
    } else {
      this.tooltipTitle = void 0;
    }
  }
  cleanInputValue() {
    this.calledByCleanInputValue = true;
    this.inputFile.nativeElement.value = "";
    this.cd.detectChanges();
  }
  isAdditionalHelpEventTriggered() {
    return this.additionalHelpEventTrigger === "event" || this.additionalHelpEventTrigger === void 0 && this.additionalHelp.observed;
  }
  isUploadButtonFocused() {
    return document.activeElement === this.uploadButton.buttonElement.nativeElement;
  }
  responseHandler(file, status) {
    file.status = status;
    file.percent = 100;
    this.cd.markForCheck();
  }
  // método responsável por setar os argumentos do i18nPipe de acordo com a restrição.
  setPipeArguments(literalAttributes, literalArguments, file) {
    const pipeArguments = this.i18nPipe.transform(this.literals[literalAttributes], literalArguments);
    this.errorMessage = pipeArguments;
    if (literalAttributes === "invalidAmount") {
      this.notification.information(pipeArguments);
    }
    if (file) {
      file.errorMessage = pipeArguments;
    }
  }
  setPrimaryActionModal() {
    this.modalPrimaryAction = {
      label: this.literals.continue,
      action: this.closeModal.bind(this)
    };
    this.modalSecondaryAction = void 0;
  }
  // Função disparada ao parar um envio de arquivo.
  stopUploadHandler(file) {
    file.status = PoUploadStatus.None;
    file.percent = 0;
    this.cd.markForCheck();
  }
  updateFiles(files) {
    this.currentFiles = this.parseFiles(files);
    this.updateModel([...this.currentFiles]);
    if (this.autoUpload) {
      this.uploadFiles(this.currentFiles);
    }
  }
  updateModel(files) {
    const modelFiles = this.mapCleanUploadFiles(files);
    this.onModelChange ? this.onModelChange(modelFiles) : this.ngModelChange.emit(modelFiles);
  }
  uploadingHandler(file, percent) {
    file.status = PoUploadStatus.Uploading;
    file.percent = percent;
    this.cd.markForCheck();
  }
  mapCleanUploadFiles(files) {
    const mapedByUploadFile = (progressFile) => {
      const _a2 = progressFile, {
        percent,
        displayName
      } = _a2, uploadFile = __objRest(_a2, [
        "percent",
        "displayName"
      ]);
      delete uploadFile.thumbnailUrl;
      return uploadFile;
    };
    return files.map(mapedByUploadFile);
  }
  static ɵfac = function PoUploadComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoUploadComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoUploadComponent,
    selectors: [["po-upload"]],
    viewQuery: function PoUploadComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c133, 7, ElementRef)(PoUploadDragDropComponent, 5)(_c134, 5)(_c135, 5)(_c17, 5, PoHelperComponent);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.inputFile = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.poUploadDragDropComponent = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.uploadButton = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.modalComponent = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.helperEl = _t.first);
      }
    },
    standalone: false,
    features: [ɵɵProvidersFeature([PoI18nPipe, PoUploadService, {
      provide: NG_VALUE_ACCESSOR,
      useExisting: forwardRef(() => _PoUploadComponent),
      multi: true
    }, {
      provide: NG_VALIDATORS,
      useExisting: forwardRef(() => _PoUploadComponent),
      multi: true
    }]), ɵɵInheritDefinitionFeature, ɵɵNgOnChangesFeature],
    decls: 15,
    vars: 34,
    consts: [["inputFile", ""], ["modal", ""], ["uploadButton", ""], [3, "p-disabled", "p-id", "p-label", "p-optional", "p-required", "p-show-required", "p-helper", "p-show-helper", "p-size", "p-text-wrap", "p-compact-label"], [1, "po-field-container-content"], [1, "po-upload"], ["type", "file", 1, "po-upload-input", 3, "change", "accept", "id", "disabled", "multiple", "required"], [3, "p-directory-compatible", "p-disabled", "p-drag-drop-height", "p-literals"], [1, "po-upload-file-restrictions", 3, "ngClass", "po-pb-1", "p-allowed-extensions", "p-max-files", "p-max-file-size", "p-min-file-size"], ["for", "file", 1, "po-upload-button", 3, "p-disabled", "p-loading", "p-label", "p-size"], [1, "po-upload-files-content"], ["p-kind", "primary", 1, "po-upload-send-button", 3, "po-mt-3", "p-disabled", "p-label", "p-size"], [3, "p-append-in-body", "p-help", "p-disabled", "p-size", "p-helper", "p-show-helper"], [3, "p-close", "p-components-size", "p-primary-action", "p-secondary-action", "p-title", "p-click-out"], [3, "src", "alt"], [1, "po-upload-thumbnail-broken"], [3, "p-file-change", "p-select-files", "p-directory-compatible", "p-disabled", "p-drag-drop-height", "p-literals"], [1, "po-upload-file-restrictions", 3, "ngClass", "p-allowed-extensions", "p-max-files", "p-max-file-size", "p-min-file-size"], ["for", "file", 1, "po-upload-button", 3, "p-click", "keydown", "p-disabled", "p-loading", "p-label", "p-size"], [1, "po-upload-progress-container", 3, "po-upload-container-error", "po-upload-status-uploaded"], [1, "po-upload-progress-container"], [1, "po-upload-content", 3, "ngClass"], [1, "po-upload-file-name"], [1, "po-upload-file-name-display"], [1, "po-upload-thumbnail"], [1, "po-upload-display-name", 3, "keydown", "click", "mouseover", "focus", "p-tooltip"], [1, "po-upload-buttons-content"], [1, "po-upload-custom-action-button", 3, "p-danger", "p-disabled", "p-label", "p-icon", "p-size"], [1, "po-upload-icon-focus", 3, "po-upload-cancel-disabled", "tabindex"], [1, "po-upload-info-bar", 3, "fade-out"], ["p-size", "medium", 1, "po-upload-progress", 3, "fade-out", "p-disabled-cancel", "p-value", "p-status", "p-size-actions"], [1, "po-upload-thumbnail", 3, "click", "keydown"], [1, "po-upload-thumbnail-icon"], ["p-icon", "ICON_PICTURE"], [1, "po-upload-thumbnail-img", 3, "error", "src", "alt"], ["p-icon", "ICON_PICTURE_BROKEN"], [1, "po-upload-custom-action-button", 3, "p-click", "p-danger", "p-disabled", "p-label", "p-icon", "p-size"], [1, "po-upload-icon-focus", 3, "keydown", "click", "tabindex"], [1, "po-upload-icon-close", 3, "p-icon"], [1, "po-upload-info-bar"], [1, "po-upload-content-error"], [1, "po-upload-value-percent"], ["p-icon", "ICON_EXCLAMATION", 1, "po-upload-icon-error"], [1, "po-upload-content-error-text"], [1, "po-upload-retry-link", 3, "p-label"], [1, "po-upload-retry-link", 3, "p-action", "p-label"], ["p-size", "medium", 1, "po-upload-progress", 3, "p-disabled-cancel", "p-value", "p-status", "p-size-actions"], ["p-kind", "primary", 1, "po-upload-send-button", 3, "p-click", "p-disabled", "p-label", "p-size"], [3, "error", "src", "alt"]],
    template: function PoUploadComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "po-field-container", 3)(1, "div", 4)(2, "div", 5)(3, "input", 6, 0);
        ɵɵlistener("change", function PoUploadComponent_Template_input_change_3_listener($event) {
          return ctx.onFileChange($event);
        });
        ɵɵelementEnd();
        ɵɵconditionalCreate(5, PoUploadComponent_Conditional_5_Template, 1, 4, "po-upload-drag-drop", 7);
        ɵɵconditionalCreate(6, PoUploadComponent_Conditional_6_Template, 1, 9, "po-upload-file-restrictions", 8);
        ɵɵconditionalCreate(7, PoUploadComponent_Conditional_7_Template, 2, 4, "po-button", 9);
        ɵɵconditionalCreate(8, PoUploadComponent_Conditional_8_Template, 3, 0, "div", 10);
        ɵɵconditionalCreate(9, PoUploadComponent_Conditional_9_Template, 1, 5, "po-button", 11);
        ɵɵelementEnd()();
        ɵɵelement(10, "po-field-container-bottom", 12);
        ɵɵelementEnd();
        ɵɵelementStart(11, "po-modal", 13, 1);
        ɵɵlistener("p-close", function PoUploadComponent_Template_po_modal_p_close_11_listener() {
          return ctx.closeModal.bind(ctx);
        });
        ɵɵconditionalCreate(13, PoUploadComponent_Conditional_13_Template, 1, 2, "img", 14)(14, PoUploadComponent_Conditional_14_Template, 2, 0, "div", 15);
        ɵɵelementEnd();
      }
      if (rf & 2) {
        const poHelper_r13 = ctx.setHelper(ctx.label, ctx.additionalHelpTooltip);
        ɵɵproperty("p-disabled", ctx.isDisabled || ctx.isFieldDisabled)("p-id", ctx.id)("p-label", ctx.label)("p-optional", ctx.optional)("p-required", ctx.required)("p-show-required", ctx.showRequired)("p-helper", ctx.label ? poHelper_r13.helperSettings : void 0)("p-show-helper", ctx.label ? ctx.displayAdditionalHelp : void 0)("p-size", ctx.size)("p-text-wrap", ctx.labelTextWrap())("p-compact-label", ctx.compactLabel());
        ɵɵadvance(3);
        ɵɵproperty("accept", ctx.allowedExtensions)("id", ctx.id)("disabled", ctx.isDisabled || ctx.loading)("multiple", ctx.isMultiple)("required", ctx.required);
        ɵɵattribute("name", ctx.name);
        ɵɵadvance(2);
        ɵɵconditional(ctx.displayDragDrop ? 5 : -1);
        ɵɵadvance();
        ɵɵconditional(ctx.fileRestrictions && !ctx.hideRestrictionsInfo ? 6 : -1);
        ɵɵadvance();
        ɵɵconditional(!ctx.hideSelectButton && !ctx.displayDragDrop ? 7 : -1);
        ɵɵadvance();
        ɵɵconditional(ctx.currentFiles && ctx.currentFiles.length ? 8 : -1);
        ɵɵadvance();
        ɵɵconditional(ctx.displaySendButton ? 9 : -1);
        ɵɵadvance();
        ɵɵproperty("p-append-in-body", ctx.appendBox)("p-help", ctx.help)("p-disabled", ctx.isFieldDisabled)("p-size", ctx.size)("p-helper", !ctx.label ? poHelper_r13.helperSettings : void 0)("p-show-helper", !ctx.label ? ctx.displayAdditionalHelp : void 0);
        ɵɵadvance();
        ɵɵproperty("p-components-size", ctx.size)("p-primary-action", ctx.modalPrimaryAction)("p-secondary-action", ctx.modalSecondaryAction)("p-title", ctx.literals.preview)("p-click-out", true);
        ɵɵadvance(2);
        ɵɵconditional(!ctx.errorModalImage ? 13 : 14);
      }
    },
    dependencies: [NgClass, PoButtonComponent, PoContainerComponent, PoFieldContainerBottomComponent, PoFieldContainerComponent, PoIconComponent, PoLinkComponent, PoModalComponent, PoProgressComponent, PoTooltipDirective, PoUploadDragDropComponent, PoUploadFileRestrictionsComponent],
    encapsulation: 2,
    changeDetection: 0
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoUploadComponent, [{
    type: Component,
    args: [{
      selector: "po-upload",
      changeDetection: ChangeDetectionStrategy.OnPush,
      providers: [PoI18nPipe, PoUploadService, {
        provide: NG_VALUE_ACCESSOR,
        useExisting: forwardRef(() => PoUploadComponent),
        multi: true
      }, {
        provide: NG_VALIDATORS,
        useExisting: forwardRef(() => PoUploadComponent),
        multi: true
      }],
      standalone: false,
      template: `@let poHelper = setHelper(label, additionalHelpTooltip);
<po-field-container
  [p-disabled]="isDisabled || isFieldDisabled"
  [p-id]="id"
  [p-label]="label"
  [p-optional]="optional"
  [p-required]="required"
  [p-show-required]="showRequired"
  [p-helper]="label ? poHelper.helperSettings : undefined"
  [p-show-helper]="label ? displayAdditionalHelp : undefined"
  [p-size]="size"
  [p-text-wrap]="labelTextWrap()"
  [p-compact-label]="compactLabel()"
>
  <div class="po-field-container-content">
    <div class="po-upload">
      <input
        #inputFile
        class="po-upload-input"
        type="file"
        [accept]="allowedExtensions"
        [attr.name]="name"
        [id]="id"
        [disabled]="isDisabled || loading"
        [multiple]="isMultiple"
        [required]="required"
        (change)="onFileChange($event)"
      />

      @if (displayDragDrop) {
        <po-upload-drag-drop
          [p-directory-compatible]="canHandleDirectory"
          [p-disabled]="isDisabled"
          [p-drag-drop-height]="dragDropHeight"
          [p-literals]="literals"
          (p-file-change)="onFileChangeDragDrop($event)"
          (p-select-files)="selectFiles()"
        >
        </po-upload-drag-drop>
      }

      @if (fileRestrictions && !hideRestrictionsInfo) {
        <po-upload-file-restrictions
          class="po-upload-file-restrictions"
          [ngClass]="{ 'po-upload-file-restrictions-drag-drop': displayDragDrop }"
          [class.po-pb-1]="!displayDragDrop"
          [p-allowed-extensions]="fileRestrictions?.allowedExtensions"
          [p-max-files]="maxFiles"
          [p-max-file-size]="fileRestrictions?.maxFileSize"
          [p-min-file-size]="fileRestrictions?.minFileSize"
        >
        </po-upload-file-restrictions>
      }

      @if (!hideSelectButton && !displayDragDrop) {
        <po-button
          #uploadButton
          class="po-upload-button"
          for="file"
          [p-disabled]="isSelectButtonDisabled"
          [p-loading]="loading"
          [p-label]="selectFileButtonLabel"
          [p-size]="size"
          (p-click)="selectFiles()"
          (keydown)="onKeyDown($event)"
        >
        </po-button>
      }

      @if (currentFiles && currentFiles.length) {
        <div class="po-upload-files-content">
          @for (file of currentFiles; track trackByFn($index, file)) {
            <div
              class="po-upload-progress-container"
              [class.po-upload-container-error]="file.status === 2"
              [class.po-upload-status-uploaded]="file.hideDoneContent"
            >
              <po-container>
                <div
                  [ngClass]="showThumbnail && file.thumbnailUrl ? 'po-upload-content-thumbnail' : 'po-upload-content'"
                  class="po-upload-content"
                >
                  <div class="po-upload-file-name">
                    <div class="po-upload-file-name-display">
                      @if (showThumbnail && file.thumbnailUrl) {
                        <div class="po-upload-thumbnail" (click)="openModal(file)" (keydown)="openModal(file, $event)">
                          @if (!file.imageError) {
                            <div class="po-upload-thumbnail-icon">
                              <po-icon p-icon="ICON_PICTURE"></po-icon>
                            </div>
                            <img
                              class="po-upload-thumbnail-img"
                              [src]="file.thumbnailUrl"
                              [alt]="literals.thumbnail"
                              (error)="onImageError(file)"
                            />
                          } @else {
                            <div class="po-upload-thumbnail-broken">
                              <po-icon p-icon="ICON_PICTURE_BROKEN"></po-icon>
                            </div>
                          }
                        </div>
                      }
                      <span
                        class="po-upload-display-name"
                        [class.po-upload-name-interactive]="showThumbnail && file.thumbnailUrl"
                        [p-tooltip]="tooltipTitle"
                        (keydown)="openModal(file, $event)"
                        (click)="openModal(file)"
                        (mouseover)="showTooltipText($event, file?.displayName)"
                        (focus)="showTooltipText($event, file?.displayName)"
                        >{{ file.displayName }}</span
                      >
                    </div>
                    <div class="po-upload-buttons-content">
                      @if (isActionVisible(customAction)) {
                        <po-button
                          class="po-upload-custom-action-button"
                          [p-danger]="customAction.type === 'danger'"
                          [p-disabled]="actionIsDisabled(customAction)"
                          [p-label]="customAction.label || ''"
                          [p-icon]="customAction.icon"
                          [p-size]="size"
                          (p-click)="customClick(file)"
                        >
                        </po-button>
                      }
                      @if (file.status !== 0 || onRemove?.observers?.length > 0) {
                        <div
                          class="po-upload-icon-focus"
                          [class.po-upload-cancel-disabled]="disabledRemoveFile"
                          [attr.aria-label]="literals.close"
                          [tabindex]="disabledRemoveFile ? -1 : 0"
                          (keydown)="cancel(file, $event)"
                          (click)="cancel(file)"
                        >
                          <po-icon
                            class="po-upload-icon-close"
                            [class.po-upload-icon-remove]="file.status === 0"
                            [p-icon]="file.status === 0 ? 'ICON_DELETE' : 'ICON_CLOSE'"
                          >
                          </po-icon>
                        </div>
                      }
                    </div>
                  </div>
                  @if (!file.hideDoneContent) {
                    <div class="po-upload-info-bar" [class.fade-out]="file.status === 0">
                      @if (file.status === 0) {
                        <span> {{ literals.doneText }} </span>
                      } @else if (file.status === 2) {
                        <div class="po-upload-content-error">
                          <po-icon class="po-upload-icon-error" p-icon="ICON_EXCLAMATION"></po-icon
                          ><span class="po-upload-content-error-text">
                            {{ file?.errorMessage || literals?.errorOccurred }}
                          </span>
                          @if (!file.errorMessage) {
                            <po-link
                              class="po-upload-retry-link"
                              [p-label]="literals.tryAgain"
                              (p-action)="uploadFiles([file])"
                            ></po-link>
                          }
                        </div>
                      } @else if (file.status === 1) {
                        <span>{{ literals.uploadingText }}</span>
                      } @else if (file.status === 3) {
                        <span>{{ literals.startSending }}</span>
                      }
                      @if (file.status !== 2) {
                        <span class="po-upload-value-percent">{{ file.percent || 0 }}%</span>
                      }
                    </div>
                  }
                </div>
                @if (file.status !== 2 && !file.hideDoneContent) {
                  <po-progress
                    [class.fade-out]="file.status === 0"
                    class="po-upload-progress"
                    p-size="medium"
                    [p-disabled-cancel]="disabledRemoveFile"
                    [p-value]="file.percent"
                    [p-status]="progressStatusByFileStatus[file.status]"
                    [p-size-actions]="size"
                  >
                  </po-progress>
                }
              </po-container>
            </div>
          }
        </div>
      }

      @if (displaySendButton) {
        <po-button
          class="po-upload-send-button"
          [class.po-mt-3]="hasMoreThanFourItems"
          p-kind="primary"
          [p-disabled]="isFieldDisabled || hasAnyFileUploading(currentFiles)"
          [p-label]="literals.startSending"
          [p-size]="size"
          (p-click)="uploadFiles(currentFiles)"
        ></po-button>
      }
    </div>
  </div>
  <po-field-container-bottom
    [p-append-in-body]="appendBox"
    [p-help]="help"
    [p-disabled]="isFieldDisabled"
    [p-size]="size"
    [p-helper]="!label ? poHelper.helperSettings : undefined"
    [p-show-helper]="!label ? displayAdditionalHelp : undefined"
  ></po-field-container-bottom>
</po-field-container>

<po-modal
  #modal
  [p-components-size]="size"
  [p-primary-action]="modalPrimaryAction"
  [p-secondary-action]="modalSecondaryAction"
  [p-title]="literals.preview"
  [p-click-out]="true"
  (p-close)="closeModal.bind(this)"
>
  @if (!errorModalImage) {
    <img [src]="modalImageUrl" [alt]="literals.thumbnail" (error)="errorModalImage = true" />
  } @else {
    <div class="po-upload-thumbnail-broken">
      <po-icon p-icon="ICON_PICTURE_BROKEN"></po-icon>
    </div>
  }
</po-modal>
`
    }]
  }], () => [], {
    inputFile: [{
      type: ViewChild,
      args: ["inputFile", {
        read: ElementRef,
        static: true
      }]
    }],
    poUploadDragDropComponent: [{
      type: ViewChild,
      args: [PoUploadDragDropComponent]
    }],
    uploadButton: [{
      type: ViewChild,
      args: ["uploadButton"]
    }],
    modalComponent: [{
      type: ViewChild,
      args: ["modal"]
    }],
    helperEl: [{
      type: ViewChild,
      args: ["helperEl", {
        read: PoHelperComponent,
        static: false
      }]
    }]
  });
})();
var PoUrlComponent = class _PoUrlComponent extends PoInputGeneric {
  id = `po-url[${uuid()}]`;
  icon = "ICON_WORLD";
  type = "url";
  pattern = "^((https|http):\\/\\/)?((([a-z\\d]([a-z\\d-]*[a-z\\d])*)\\.)+[a-z]{2,}|((\\d{1,3}\\.){3}\\d{1,3}))(\\:\\d+)?(\\/[-A-Za-z\\d%_.~+]*)*(\\?[;&A-Za-z\\d%_.~+=-]*)?(\\#[-A-Za-z\\d_]*)?$";
  mask = "";
  listener = this.validateClassesForPattern.bind(this);
  /* istanbul ignore next */
  constructor() {
    const el = inject(ElementRef);
    const cd = inject(ChangeDetectorRef);
    super(el, cd);
    this.maxlength = 254;
  }
  ngAfterViewInit() {
    setTimeout(() => {
      if (!this.onChangePropagate) {
        this.inputEl.nativeElement.addEventListener("keyup", this.listener);
      }
    });
    super.ngAfterViewInit();
  }
  ngOnDestroy() {
    if (!this.onChangePropagate) {
      this.inputEl.nativeElement.removeEventListener("keyup", this.listener);
    }
  }
  ngOnChanges(changes) {
    if (changes.label) {
      this.displayAdditionalHelp = false;
    }
  }
  extraValidation(c) {
    return null;
  }
  static ɵfac = function PoUrlComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoUrlComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoUrlComponent,
    selectors: [["po-url"]],
    standalone: false,
    features: [ɵɵProvidersFeature([{
      provide: NG_VALUE_ACCESSOR,
      useExisting: forwardRef(() => _PoUrlComponent),
      multi: true
    }, {
      provide: NG_VALIDATORS,
      useExisting: forwardRef(() => _PoUrlComponent),
      multi: true
    }]), ɵɵInheritDefinitionFeature, ɵɵNgOnChangesFeature],
    decls: 14,
    vars: 36,
    consts: [["fieldContainer", ""], ["inp", ""], ["helperEl", ""], [3, "p-disabled", "p-id", "p-label", "p-optional", "p-required", "p-show-required", "p-helper", "p-show-helper", "p-size", "p-text-wrap", "p-compact-label"], [1, "po-field-container-content"], [1, "po-field-container-content-helper-wrapper"], [1, "po-field-container-input"], [1, "po-field-icon-container-left"], [1, "po-input", 3, "blur", "click", "focus", "input", "keydown", "autocomplete", "disabled", "id", "placeholder", "readonly", "required", "type"], [1, "po-field-icon-container-right"], [3, "p-size"], [3, "class", "p-element-ref", "p-size"], [1, "po-field-helper-button", 3, "p-size", "p-helper", "p-disabled", "p-append-in-body"], [3, "p-append-in-body", "p-help", "p-disabled", "p-error-pattern", "p-error-limit", "p-size"], [3, "p-icon"], [3, "p-change-event", "p-element-ref", "p-size"]],
    template: function PoUrlComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵdeclareLet(0);
        ɵɵelementStart(1, "po-field-container", 3, 0)(3, "div", 4)(4, "div", 5)(5, "div", 6);
        ɵɵconditionalCreate(6, PoUrlComponent_Conditional_6_Template, 2, 8, "div", 7);
        ɵɵelementStart(7, "input", 8, 1);
        ɵɵlistener("blur", function PoUrlComponent_Template_input_blur_7_listener($event) {
          return ctx.eventOnBlur($event);
        })("click", function PoUrlComponent_Template_input_click_7_listener($event) {
          return ctx.eventOnClick($event);
        })("focus", function PoUrlComponent_Template_input_focus_7_listener($event) {
          return ctx.eventOnFocus($event);
        })("input", function PoUrlComponent_Template_input_input_7_listener($event) {
          return ctx.eventOnInput($event);
        })("keydown", function PoUrlComponent_Template_input_keydown_7_listener($event) {
          return ctx.onKeyDown($event);
        });
        ɵɵelementEnd();
        ɵɵelementStart(9, "div", 9);
        ɵɵconditionalCreate(10, PoUrlComponent_Conditional_10_Template, 1, 1, "po-loading-icon", 10);
        ɵɵconditionalCreate(11, PoUrlComponent_Conditional_11_Template, 1, 4, "po-clean", 11);
        ɵɵelementEnd()();
        ɵɵconditionalCreate(12, PoUrlComponent_Conditional_12_Template, 2, 4, "po-helper", 12);
        ɵɵelementEnd()();
        ɵɵconditionalCreate(13, PoUrlComponent_Conditional_13_Template, 1, 6, "po-field-container-bottom", 13);
        ɵɵelementEnd();
      }
      if (rf & 2) {
        const poHelper_r4 = ɵɵstoreLet(ctx.setHelper(ctx.label, ctx.additionalHelpTooltip));
        ɵɵadvance();
        ɵɵproperty("p-disabled", ctx.isDisabled)("p-id", ctx.id)("p-label", ctx.label)("p-optional", ctx.optional)("p-required", ctx.required)("p-show-required", ctx.showRequired)("p-helper", ctx.label ? poHelper_r4.helperSettings : void 0)("p-show-helper", ctx.label ? ctx.displayAdditionalHelp : void 0)("p-size", ctx.size)("p-text-wrap", ctx.labelTextWrap())("p-compact-label", ctx.compactLabel());
        ɵɵadvance(5);
        ɵɵconditional(ctx.icon ? 6 : -1);
        ɵɵadvance();
        ɵɵclassProp("po-input-icon-left", ctx.icon)("po-input-icon-left-aa", ctx.icon && ctx.size === "small")("po-input-icon-right", ctx.clean || ctx.loading)("po-input-icon-right-aa", (ctx.clean || ctx.loading) && ctx.size === "small");
        ɵɵproperty("autocomplete", ctx.autocomplete)("disabled", ctx.isDisabled)("id", ctx.id)("placeholder", ctx.isDisabled ? "" : ctx.placeholder)("readonly", ctx.readonly)("required", ctx.required)("type", ctx.type);
        ɵɵattribute("name", ctx.name)("data-focused-element", !ctx.isDisabled)("data-inactive-component", ctx.isDisabled || ctx.readonly)("p-size", ctx.size);
        ɵɵadvance(3);
        ɵɵconditional(ctx.loading ? 10 : -1);
        ɵɵadvance();
        ɵɵconditional(!ctx.loading && ctx.clean && !ctx.isDisabled && !ctx.readonly ? 11 : -1);
        ɵɵadvance();
        ɵɵconditional(!ctx.label && ctx.poHelperComponent() || !ctx.label && poHelper_r4.hideAdditionalHelp ? 12 : -1);
        ɵɵadvance();
        ɵɵconditional(!ctx.readonly ? 13 : -1);
      }
    },
    dependencies: [PoCleanComponent, PoFieldContainerBottomComponent, PoFieldContainerComponent, PoHelperComponent, PoIconComponent, PoLoadingIconComponent],
    encapsulation: 2,
    changeDetection: 0
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoUrlComponent, [{
    type: Component,
    args: [{
      selector: "po-url",
      changeDetection: ChangeDetectionStrategy.OnPush,
      providers: [{
        provide: NG_VALUE_ACCESSOR,
        useExisting: forwardRef(() => PoUrlComponent),
        multi: true
      }, {
        provide: NG_VALIDATORS,
        useExisting: forwardRef(() => PoUrlComponent),
        multi: true
      }],
      standalone: false,
      template: `@let poHelper = setHelper(label, additionalHelpTooltip);
<po-field-container
  #fieldContainer
  [p-disabled]="isDisabled"
  [p-id]="id"
  [p-label]="label"
  [p-optional]="optional"
  [p-required]="required"
  [p-show-required]="showRequired"
  [p-helper]="label ? poHelper.helperSettings : undefined"
  [p-show-helper]="label ? displayAdditionalHelp : undefined"
  [p-size]="size"
  [p-text-wrap]="labelTextWrap()"
  [p-compact-label]="compactLabel()"
>
  <div class="po-field-container-content">
    <div class="po-field-container-content-helper-wrapper">
      <div class="po-field-container-input">
        @if (icon) {
          <div class="po-field-icon-container-left">
            <po-icon
              class="po-field-icon {{ disabled ? 'po-icon-input-disabled' : 'po-icon-input' }}"
              [class.po-field-icon-disabled]="isDisabled"
              [class.po-field-icon-aa]="size === 'small'"
              [p-icon]="icon"
            ></po-icon>
          </div>
        }

        <input
          #inp
          class="po-input"
          [attr.name]="name"
          [attr.data-focused-element]="!isDisabled"
          [attr.data-inactive-component]="isDisabled || readonly"
          [attr.p-size]="size"
          [autocomplete]="autocomplete"
          [class.po-input-icon-left]="icon"
          [class.po-input-icon-left-aa]="icon && size === 'small'"
          [class.po-input-icon-right]="clean || loading"
          [class.po-input-icon-right-aa]="(clean || loading) && size === 'small'"
          [disabled]="isDisabled"
          [id]="id"
          [placeholder]="isDisabled ? '' : placeholder"
          [readonly]="readonly"
          [required]="required"
          [type]="type"
          (blur)="eventOnBlur($event)"
          (click)="eventOnClick($event)"
          (focus)="eventOnFocus($event)"
          (input)="eventOnInput($event)"
          (keydown)="onKeyDown($event)"
        />
        <div class="po-field-icon-container-right">
          @if (loading) {
            <po-loading-icon [p-size]="mapSizeToIcon(size)"></po-loading-icon>
          }

          @if (!loading && clean && !isDisabled && !readonly) {
            <po-clean
              class="{{ disabled ? 'po-icon-input-disabled' : 'po-icon-input' }}"
              [p-element-ref]="inputEl"
              [p-size]="size"
              (p-change-event)="clear($event)"
            >
            </po-clean>
          }
        </div>
      </div>
      @if ((!this.label && poHelperComponent()) || (!this.label && poHelper.hideAdditionalHelp)) {
        <po-helper
          #helperEl
          class="po-field-helper-button"
          [p-size]="size"
          [p-helper]="poHelper.helperSettings"
          [p-disabled]="isDisabled"
          [p-append-in-body]="appendBox"
        >
        </po-helper>
      }
    </div>
  </div>

  @if (!readonly) {
    <po-field-container-bottom
      [p-append-in-body]="appendBox"
      [p-help]="help"
      [p-disabled]="isDisabled"
      [p-error-pattern]="getErrorPattern()"
      [p-error-limit]="errorLimit"
      [p-size]="size"
    ></po-field-container-bottom>
  }
</po-field-container>
`
    }]
  }], () => [], null);
})();
var uploadRestrictions = [".apng", ".bmp", ".gif", ".ico", ".jpeg", ".jpg", ".png", ".svg"];
var PoRichTextImageModalComponent = class _PoRichTextImageModalComponent {
  languageService = inject(PoLanguageService);
  modal;
  modalImageForm;
  upload;
  size;
  command = new EventEmitter();
  savedCursorPosition;
  selection = document.getSelection();
  uploadModel;
  uploadRestrictions = {
    allowedExtensions: uploadRestrictions
  };
  urlImage;
  literals;
  modalCancelAction;
  modalConfirmAction;
  get isUploadValid() {
    return !!(this.uploadModel && this.uploadModel.length);
  }
  get isUrlValid() {
    return !!this.urlImage && this.modalImageForm && this.modalImageForm.valid;
  }
  constructor() {
    this.literals = __spreadValues({}, poRichTextLiteralsDefault[this.languageService.getShortLanguage()]);
    this.modalCancelAction = {
      label: this.literals.cancel,
      action: () => {
        this.modal.close();
        this.command.emit();
        this.retrieveCursorPosition();
        this.cleanUpFields();
      }
    };
    this.modalConfirmAction = {
      label: this.literals.insert,
      disabled: false,
      action: () => this.insertElementRef()
    };
  }
  openModal() {
    this.saveCursorPosition();
    this.modal.open();
  }
  cleanUpFields() {
    this.urlImage = void 0;
    this.uploadModel = void 0;
  }
  convertToBase64() {
    return __async(this, null, function* () {
      if (this.isUploadValid) {
        const uploadImage = this.uploadModel[0].rawFile;
        return yield PoUtils.convertImageToBase64(uploadImage);
      }
    });
  }
  emitCommand(value) {
    let command;
    if (value) {
      command = "insertImage";
      this.command.emit({
        command,
        value
      });
    }
  }
  insertElementRef() {
    return __async(this, null, function* () {
      let uploadImage;
      if (!this.urlImage) {
        uploadImage = yield this.convertToBase64();
      }
      this.retrieveCursorPosition();
      this.modal.close();
      if (this.isUrlValid || this.isUploadValid) {
        this.emitCommand(this.urlImage || uploadImage);
      }
      this.cleanUpFields();
    });
  }
  retrieveCursorPosition() {
    this.selection.collapse(this.savedCursorPosition[0], this.savedCursorPosition[1]);
  }
  saveCursorPosition() {
    this.savedCursorPosition = [this.selection.focusNode, this.selection.focusOffset];
  }
  static ɵfac = function PoRichTextImageModalComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoRichTextImageModalComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoRichTextImageModalComponent,
    selectors: [["po-rich-text-image-modal"]],
    viewQuery: function PoRichTextImageModalComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c135, 7)(_c137, 5)(_c138, 7);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.modal = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.modalImageForm = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.upload = _t.first);
      }
    },
    hostVars: 1,
    hostBindings: function PoRichTextImageModalComponent_HostBindings(rf, ctx) {
      if (rf & 2) {
        ɵɵattribute("p-size", ctx.size);
      }
    },
    inputs: {
      size: [0, "p-size", "size"]
    },
    outputs: {
      command: "p-command"
    },
    standalone: false,
    decls: 9,
    vars: 14,
    consts: [["modal", ""], ["modalImageForm", "ngForm"], ["upload", ""], ["p-hide-close", "", 3, "p-components-size", "p-primary-action", "p-secondary-action", "p-title"], [1, "po-row"], ["name", "upload", "p-drag-drop-height", "160", "p-hide-restrictions-info", "", "p-hide-send-button", "", "p-url", "x", 1, "po-md-12", 3, "ngModelChange", "ngModel", "p-drag-drop", "p-disabled", "p-restrictions", "p-show-thumbnail", "p-size"], ["name", "url", 1, "po-md-12", "po-mt-3", 3, "ngModelChange", "ngModel", "p-label", "p-disabled", "p-size"]],
    template: function PoRichTextImageModalComponent_Template(rf, ctx) {
      if (rf & 1) {
        const _r1 = ɵɵgetCurrentView();
        ɵɵelementStart(0, "po-modal", 3, 0)(2, "form", null, 1)(4, "div", 4)(5, "po-upload", 5, 2);
        ɵɵtwoWayListener("ngModelChange", function PoRichTextImageModalComponent_Template_po_upload_ngModelChange_5_listener($event) {
          ɵɵrestoreView(_r1);
          ɵɵtwoWayBindingSet(ctx.uploadModel, $event) || (ctx.uploadModel = $event);
          return ɵɵresetView($event);
        });
        ɵɵelementEnd()();
        ɵɵelementStart(7, "div", 4)(8, "po-url", 6);
        ɵɵtwoWayListener("ngModelChange", function PoRichTextImageModalComponent_Template_po_url_ngModelChange_8_listener($event) {
          ɵɵrestoreView(_r1);
          ɵɵtwoWayBindingSet(ctx.urlImage, $event) || (ctx.urlImage = $event);
          return ɵɵresetView($event);
        });
        ɵɵelementEnd()()()();
      }
      if (rf & 2) {
        const modal_r2 = ɵɵreference(1);
        ɵɵproperty("p-components-size", ctx.size)("p-primary-action", ctx.modalConfirmAction)("p-secondary-action", ctx.modalCancelAction)("p-title", ctx.literals.insertImage);
        ɵɵadvance(5);
        ɵɵtwoWayProperty("ngModel", ctx.uploadModel);
        ɵɵproperty("p-drag-drop", !modal_r2.isHidden)("p-disabled", ctx.isUrlValid)("p-restrictions", ctx.uploadRestrictions)("p-show-thumbnail", false)("p-size", ctx.size);
        ɵɵadvance(3);
        ɵɵtwoWayProperty("ngModel", ctx.urlImage);
        ɵɵproperty("p-label", ctx.literals.urlImage)("p-disabled", ctx.isUploadValid)("p-size", ctx.size);
      }
    },
    dependencies: [ɵNgNoValidate, NgControlStatus, NgControlStatusGroup, NgModel, NgForm, PoModalComponent, PoUploadComponent, PoUrlComponent],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoRichTextImageModalComponent, [{
    type: Component,
    args: [{
      selector: "po-rich-text-image-modal",
      standalone: false,
      template: '<po-modal\n  #modal\n  p-hide-close\n  [p-components-size]="size"\n  [p-primary-action]="modalConfirmAction"\n  [p-secondary-action]="modalCancelAction"\n  [p-title]="literals.insertImage"\n>\n  <form #modalImageForm="ngForm">\n    <div class="po-row">\n      <!-- po-upload desabilita o drag drop caso não tenha valor atribuido para a propriedade p-url -->\n      <po-upload\n        #upload\n        class="po-md-12"\n        name="upload"\n        [(ngModel)]="uploadModel"\n        p-drag-drop-height="160"\n        p-hide-restrictions-info\n        p-hide-send-button\n        p-url="x"\n        [p-drag-drop]="!modal.isHidden"\n        [p-disabled]="isUrlValid"\n        [p-restrictions]="uploadRestrictions"\n        [p-show-thumbnail]="false"\n        [p-size]="size"\n      >\n      </po-upload>\n    </div>\n\n    <div class="po-row">\n      <po-url\n        class="po-md-12 po-mt-3"\n        name="url"\n        [(ngModel)]="urlImage"\n        [p-label]="literals.urlImage"\n        [p-disabled]="isUploadValid"\n        [p-size]="size"\n      >\n      </po-url>\n    </div>\n  </form>\n</po-modal>\n'
    }]
  }], () => [], {
    modal: [{
      type: ViewChild,
      args: ["modal", {
        static: true
      }]
    }],
    modalImageForm: [{
      type: ViewChild,
      args: ["modalImageForm"]
    }],
    upload: [{
      type: ViewChild,
      args: ["upload", {
        static: true
      }]
    }],
    size: [{
      type: HostBinding,
      args: ["attr.p-size"]
    }, {
      type: Input,
      args: ["p-size"]
    }],
    command: [{
      type: Output,
      args: ["p-command"]
    }]
  });
})();
var PoRichTextLinkModalComponent = class _PoRichTextLinkModalComponent {
  languageService = inject(PoLanguageService);
  modal;
  modalLinkForm;
  size;
  command = new EventEmitter();
  linkEditing = new EventEmitter();
  savedCursorPosition;
  selection = document.getSelection();
  urlLink;
  urlLinkText;
  literals;
  modalCancelAction;
  modalConfirmAction;
  isLinkEditing;
  isSelectedLink;
  linkElement;
  savedSelection;
  constructor() {
    this.literals = __spreadValues({}, poRichTextLiteralsDefault[this.languageService?.getShortLanguage()]);
  }
  ngOnInit() {
    this.setModalCancelAction();
    this.setModalConfirmAction();
  }
  linkConfirmAction() {
    return this.isLinkEditing ? this.literals.editLink : this.literals.insertLink;
  }
  formModelValidate() {
    return this.modalConfirmAction.disabled = this.modalLinkForm?.invalid;
  }
  openModal(selectedLinkElement) {
    this.saveCursorPosition();
    this.prepareModalForLink(selectedLinkElement);
    this.modalConfirmAction.label = this.linkConfirmAction();
    this.modal.open();
  }
  selectedLink(linkElement) {
    this.isSelectedLink = !!linkElement;
    this.linkElement = linkElement;
  }
  checkIfIsEmpty(urlLink, urlLinkText) {
    return urlLinkText === void 0 || urlLinkText.trim() === "" ? urlLink : urlLinkText;
  }
  cleanUpFields() {
    this.urlLink = void 0;
    this.urlLinkText = void 0;
    this.isLinkEditing = false;
    this.isSelectedLink = false;
    this.linkElement = void 0;
  }
  formReset(control) {
    control.markAsPristine();
    control.markAsUntouched();
    control.updateValueAndValidity();
  }
  prepareModalForLink(selectedLinkElement) {
    this.saveSelectionText();
    if (this.modalLinkForm) {
      this.formReset(this.modalLinkForm.control);
    }
    setTimeout(() => {
      this.formModelValidate();
    });
    this.selectedLink(selectedLinkElement);
    if (this.isSelectedLink) {
      this.isLinkEditing = true;
      this.setLinkEditableForModal();
    }
    this.linkEditing.emit(this.isLinkEditing);
  }
  restoreSelection() {
    if (this.savedSelection) {
      if (this.selection) {
        this.selection.removeAllRanges();
        this.selection.addRange(this.savedSelection);
      }
      return true;
    } else {
      return false;
    }
  }
  retrieveCursorPosition() {
    this.selection.collapse(this.savedCursorPosition[0], this.savedCursorPosition[1]);
  }
  saveCursorPosition() {
    this.savedCursorPosition = [this.selection.focusNode, this.selection.focusOffset];
  }
  saveSelectionText() {
    if (this.selection.anchorNode !== null) {
      this.savedSelection = this.selection.getRangeAt(0);
      this.urlLinkText = this.selection.toString();
    } else {
      return null;
    }
  }
  setLinkEditableForModal() {
    this.urlLinkText = this.linkElement.innerText;
    this.urlLink = this.linkElement.getAttribute("href");
  }
  setModalCancelAction() {
    this.modalCancelAction = {
      label: this.literals.cancel,
      action: () => {
        this.modal.close();
        this.command.emit();
        this.retrieveCursorPosition();
        this.cleanUpFields();
      }
    };
  }
  setModalConfirmAction() {
    this.modalConfirmAction = {
      label: this.linkConfirmAction(),
      disabled: true,
      action: () => this.isLinkEditing ? this.toEditLink() : this.toInsertLink(this.urlLink, this.urlLinkText)
    };
  }
  toEditLink() {
    if (PoUtils.isIE()) {
      this.linkElement.parentNode.removeChild(this.linkElement);
    } else {
      this.linkElement.remove();
    }
    this.toInsertLink(this.urlLink, this.urlLinkText);
  }
  toInsertLink(urlLink, urlLinkText) {
    this.modal.close();
    this.restoreSelection();
    const urlLinkTextValue = this.checkIfIsEmpty(urlLink, urlLinkText);
    const urlAsExternalLink = isExternalLink(urlLink) ? urlLink : `http://${urlLink}`;
    const command = "InsertHTML";
    const value = {
      urlLink: urlAsExternalLink,
      urlLinkText: urlLinkTextValue
    };
    this.command.emit({
      command,
      value
    });
    this.cleanUpFields();
  }
  static ɵfac = function PoRichTextLinkModalComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoRichTextLinkModalComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoRichTextLinkModalComponent,
    selectors: [["po-rich-text-link-modal"]],
    viewQuery: function PoRichTextLinkModalComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c135, 7)(_c139, 5);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.modal = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.modalLinkForm = _t.first);
      }
    },
    hostVars: 1,
    hostBindings: function PoRichTextLinkModalComponent_HostBindings(rf, ctx) {
      if (rf & 2) {
        ɵɵattribute("p-size", ctx.size);
      }
    },
    inputs: {
      size: [0, "p-size", "size"]
    },
    outputs: {
      command: "p-command",
      linkEditing: "p-link-editing"
    },
    standalone: false,
    decls: 7,
    vars: 12,
    consts: [["modal", ""], ["modalLinkForm", "ngForm"], ["p-hide-close", "", 3, "p-components-size", "p-primary-action", "p-secondary-action", "p-title"], [1, "po-row"], ["name", "urlLinkText", "p-optional", "", 1, "po-md-12", "po-mb-2", 3, "ngModelChange", "ngModel", "p-label", "p-placeholder", "p-size"], ["name", "urlLink", "p-label", "Link", "p-required", "", 1, "po-md-12", 3, "ngModelChange", "p-change-model", "ngModel", "p-help", "p-placeholder", "p-size"]],
    template: function PoRichTextLinkModalComponent_Template(rf, ctx) {
      if (rf & 1) {
        const _r1 = ɵɵgetCurrentView();
        ɵɵelementStart(0, "po-modal", 2, 0)(2, "form", null, 1)(4, "div", 3)(5, "po-input", 4);
        ɵɵtwoWayListener("ngModelChange", function PoRichTextLinkModalComponent_Template_po_input_ngModelChange_5_listener($event) {
          ɵɵrestoreView(_r1);
          ɵɵtwoWayBindingSet(ctx.urlLinkText, $event) || (ctx.urlLinkText = $event);
          return ɵɵresetView($event);
        });
        ɵɵelementEnd();
        ɵɵelementStart(6, "po-url", 5);
        ɵɵtwoWayListener("ngModelChange", function PoRichTextLinkModalComponent_Template_po_url_ngModelChange_6_listener($event) {
          ɵɵrestoreView(_r1);
          ɵɵtwoWayBindingSet(ctx.urlLink, $event) || (ctx.urlLink = $event);
          return ɵɵresetView($event);
        });
        ɵɵlistener("p-change-model", function PoRichTextLinkModalComponent_Template_po_url_p_change_model_6_listener() {
          return ctx.formModelValidate();
        });
        ɵɵelementEnd()()()();
      }
      if (rf & 2) {
        ɵɵproperty("p-components-size", ctx.size)("p-primary-action", ctx.modalConfirmAction)("p-secondary-action", ctx.modalCancelAction)("p-title", ctx.linkConfirmAction());
        ɵɵadvance(5);
        ɵɵtwoWayProperty("ngModel", ctx.urlLinkText);
        ɵɵproperty("p-label", ctx.literals.linkTextLabel)("p-placeholder", ctx.literals.linkTextLabel)("p-size", ctx.size);
        ɵɵadvance();
        ɵɵtwoWayProperty("ngModel", ctx.urlLink);
        ɵɵproperty("p-help", ctx.literals.linkUrlTextHelper)("p-placeholder", ctx.literals.linkUrlTextPlaceholder)("p-size", ctx.size);
      }
    },
    dependencies: [ɵNgNoValidate, NgControlStatus, NgControlStatusGroup, NgModel, NgForm, PoModalComponent, PoInputComponent, PoUrlComponent],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoRichTextLinkModalComponent, [{
    type: Component,
    args: [{
      selector: "po-rich-text-link-modal",
      standalone: false,
      template: '<po-modal\n  #modal\n  p-hide-close\n  [p-components-size]="size"\n  [p-primary-action]="modalConfirmAction"\n  [p-secondary-action]="modalCancelAction"\n  [p-title]="linkConfirmAction()"\n>\n  <form #modalLinkForm="ngForm">\n    <div class="po-row">\n      <po-input\n        class="po-md-12 po-mb-2"\n        name="urlLinkText"\n        [(ngModel)]="urlLinkText"\n        p-optional\n        [p-label]="literals.linkTextLabel"\n        [p-placeholder]="literals.linkTextLabel"\n        [p-size]="size"\n      >\n      </po-input>\n\n      <po-url\n        class="po-md-12"\n        name="urlLink"\n        [(ngModel)]="urlLink"\n        p-label="Link"\n        p-required\n        [p-help]="literals.linkUrlTextHelper"\n        [p-placeholder]="literals.linkUrlTextPlaceholder"\n        [p-size]="size"\n        (p-change-model)="formModelValidate()"\n      >\n      </po-url>\n    </div>\n  </form>\n</po-modal>\n'
    }]
  }], () => [], {
    modal: [{
      type: ViewChild,
      args: ["modal", {
        static: true
      }]
    }],
    modalLinkForm: [{
      type: ViewChild,
      args: ["modalLinkForm"]
    }],
    size: [{
      type: HostBinding,
      args: ["attr.p-size"]
    }, {
      type: Input,
      args: ["p-size"]
    }],
    command: [{
      type: Output,
      args: ["p-command"]
    }],
    linkEditing: [{
      type: Output,
      args: ["p-link-editing"]
    }]
  });
})();
var poRichTextDefaultColor = "#000000";
var PoRichTextToolbarComponent = class _PoRichTextToolbarComponent {
  languageService = inject(PoLanguageService);
  colorPickerInput;
  toolbarElement;
  richTextImageModal;
  richTextLinkModal;
  command = new EventEmitter();
  modal = new EventEmitter();
  linkEditing = new EventEmitter();
  literals;
  alignButtons;
  formatButtons;
  listButtons;
  linkButtons;
  mediaButtons;
  _readonly;
  selectedLinkElement;
  _hideToolbarActions = [];
  formatToolbarAction = PoRichTextToolbarActions.Format;
  colorToolbarAction = PoRichTextToolbarActions.Color;
  alignToolbarAction = PoRichTextToolbarActions.Align;
  listToolbarAction = PoRichTextToolbarActions.List;
  linkToolbarAction = PoRichTextToolbarActions.Link;
  mediaToolbarAction = PoRichTextToolbarActions.Media;
  /**
   * @optional
   *
   * @description
   *
   * Define as ações da barra de ferramentas do `PoRichTextComponent` que serão ocultadas.
   * Aceita um único valor do tipo `PoRichTextToolbarActions` ou uma lista de valores.
   *
   * > Esta propriedade sobrepõe a configuração da propriedade `p-disabled-text-align` quando for passada como `false`, caso sejam definidas simultaneamente.
   *
   * @default `[]`
   *
   * @example
   * ```
   * // Oculta apenas o seletor de cores
   * component.hideToolbarActions = PoRichTextToolbarActions.Color;
   *
   * // Oculta as opções de alinhamento e link
   * component.hideToolbarActions = [PoRichTextToolbarActions.Align, PoRichTextToolbarActions.Link];
   * ```
   */
  set hideToolbarActions(actions2) {
    this._hideToolbarActions = Array.isArray(actions2) ? [...actions2] : [actions2];
  }
  get hideToolbarActions() {
    return this._hideToolbarActions;
  }
  set readonly(value) {
    this._readonly = value;
    this.toggleDisableButtons(this._readonly);
  }
  get readonly() {
    return this._readonly;
  }
  get isInternetExplorer() {
    return PoUtils.isIE();
  }
  disabled;
  size;
  constructor() {
    this.literals = __spreadValues({}, poRichTextLiteralsDefault[this.languageService?.getShortLanguage()]);
    this.alignButtons = [{
      command: "justifyleft",
      icon: "ICON_ALIGN_LEFT",
      tooltip: this.literals.left,
      action: this.emitAlignCommand.bind(this, "justifyleft"),
      selected: true
    }, {
      command: "justifycenter",
      icon: "ICON_ALIGN_CENTER",
      tooltip: this.literals.center,
      action: this.emitAlignCommand.bind(this, "justifycenter")
    }, {
      command: "justifyright",
      icon: "ICON_ALIGN_RIGHT",
      tooltip: this.literals.right,
      action: this.emitAlignCommand.bind(this, "justifyright")
    }, {
      command: "justifyfull",
      icon: "ICON_ALIGN_JUSTIFY",
      tooltip: this.literals.justify,
      action: this.emitAlignCommand.bind(this, "justifyfull")
    }];
    this.formatButtons = [{
      command: "bold",
      icon: "ICON_TEXT_BOLD",
      tooltip: this.literals.bold,
      action: this.emitCommand.bind(this, "bold")
    }, {
      command: "italic",
      icon: "ICON_TEXT_ITALIC",
      tooltip: this.literals.italic,
      action: this.emitCommand.bind(this, "italic")
    }, {
      command: "underline",
      icon: "ICON_TEXT_UNDERLINE",
      tooltip: this.literals.underline,
      action: this.emitCommand.bind(this, "underline")
    }];
    this.listButtons = [{
      command: "insertUnorderedList",
      icon: "ICON_LIST",
      tooltip: this.literals.unorderedList,
      action: this.emitCommand.bind(this, "insertUnorderedList")
    }];
    this.mediaButtons = [{
      tooltip: this.literals.insertImage,
      icon: "ICON_PICTURE",
      action: () => this.richTextImageModal.openModal()
    }];
    this.linkButtons = [{
      command: "Createlink",
      icon: "ICON_LINK",
      tooltip: `${this.literals.insertLink} (Ctrl + K)`,
      action: () => this.richTextLinkModal.openModal(this.selectedLinkElement)
    }];
  }
  ngAfterViewInit() {
    this.removeButtonFocus();
    if (this.showColor()) {
      this.setColorInColorPicker(poRichTextDefaultColor);
    }
  }
  changeTextColor(value) {
    const command = "foreColor";
    this.command.emit({
      command,
      value
    });
  }
  emitLinkEditing(isLinkEdit) {
    this.linkEditing.emit(isLinkEdit);
  }
  selectedLink(selectedLinkElement) {
    this.selectedLinkElement = selectedLinkElement;
  }
  setButtonsStates(obj) {
    if (!this.readonly) {
      this.alignButtons.forEach((button) => button.selected = obj.commands.includes(button.command));
      this.formatButtons.forEach((button) => button.selected = obj.commands.includes(button.command));
      this.listButtons[0].selected = obj.commands.includes(this.listButtons[0].command);
      this.linkButtons[0].selected = obj.commands.includes(this.linkButtons[0].command);
      this.setColorInColorPicker(obj.hexColor);
    }
  }
  // Verifica se uma ação específica do toolbar está oculta
  isActionHidden(action) {
    return this.hideToolbarActions.includes(action);
  }
  showColor() {
    return !this.isInternetExplorer && !this.isActionHidden(PoRichTextToolbarActions.Color);
  }
  shortcutTrigger() {
    this.richTextLinkModal.openModal(this.selectedLinkElement);
  }
  emitCommand(command) {
    this.command.emit(command);
  }
  emitAlignCommand(command) {
    const index = this.alignButtons.findIndex((btn) => btn.command === command);
    if (!this.alignButtons[index].selected) {
      this.alignButtons[index].selected = false;
    }
    this.command.emit(command);
  }
  removeButtonFocus() {
    const buttons = this.toolbarElement.nativeElement.querySelectorAll("button");
    buttons.forEach((button) => button.setAttribute("tabindex", "-1"));
  }
  setColorInColorPicker(color3) {
    this.colorPickerInput.nativeElement.value = color3;
  }
  toggleDisableButtons(state2) {
    this.alignButtons.forEach((button) => button.disabled = state2);
    this.formatButtons.forEach((button) => button.disabled = state2);
    this.listButtons[0].disabled = state2;
    this.linkButtons[0].disabled = state2;
    this.mediaButtons[0].disabled = state2;
  }
  static ɵfac = function PoRichTextToolbarComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoRichTextToolbarComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoRichTextToolbarComponent,
    selectors: [["po-rich-text-toolbar"]],
    viewQuery: function PoRichTextToolbarComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c140, 5, ElementRef)(_c141, 7)(PoRichTextImageModalComponent, 7)(PoRichTextLinkModalComponent, 7);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.colorPickerInput = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.toolbarElement = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.richTextImageModal = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.richTextLinkModal = _t.first);
      }
    },
    hostVars: 1,
    hostBindings: function PoRichTextToolbarComponent_HostBindings(rf, ctx) {
      if (rf & 2) {
        ɵɵattribute("p-size", ctx.size);
      }
    },
    inputs: {
      hideToolbarActions: [0, "p-hide-toolbar-actions", "hideToolbarActions"],
      readonly: [0, "p-readonly", "readonly"],
      disabled: [0, "p-disabled", "disabled"],
      size: [0, "p-size", "size"]
    },
    outputs: {
      command: "p-command",
      modal: "p-modal",
      linkEditing: "p-link-editing"
    },
    standalone: false,
    decls: 12,
    vars: 10,
    consts: [["toolbarElement", ""], ["richTextImageModal", ""], ["richTextLinkModal", ""], ["colorPickerInput", ""], [1, "po-rich-text-toolbar"], ["data-rich-text-toolbar", "format", 1, "po-rich-text-toolbar-button-align"], ["data-rich-text-toolbar", "color", 1, "po-rich-text-toolbar-button-align"], ["data-rich-text-toolbar", "align", 1, "po-rich-text-toolbar-button-align"], ["data-rich-text-toolbar", "list", 1, "po-rich-text-toolbar-button-align"], ["data-rich-text-toolbar", "link", 1, "po-rich-text-toolbar-button-align"], ["data-rich-text-toolbar", "media", 1, "po-rich-text-toolbar-button-align"], [3, "p-command", "p-size"], [3, "p-command", "p-link-editing", "p-size"], ["p-toggle", "multiple", 3, "p-buttons", "p-size"], [1, "po-rich-text-toolbar-color-picker-container"], ["type", "button", 1, "po-button", "po-button-default", "po-rich-text-toolbar-color-picker-button", 3, "disabled", "p-tooltip"], ["type", "color", 1, "po-rich-text-toolbar-color-picker-input", 3, "input", "disabled"], ["p-toggle", "single", 3, "p-buttons", "p-size"], [3, "p-buttons", "p-size"]],
    template: function PoRichTextToolbarComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "div", 4, 0);
        ɵɵconditionalCreate(2, PoRichTextToolbarComponent_Conditional_2_Template, 2, 2, "div", 5);
        ɵɵconditionalCreate(3, PoRichTextToolbarComponent_Conditional_3_Template, 5, 5, "div", 6);
        ɵɵconditionalCreate(4, PoRichTextToolbarComponent_Conditional_4_Template, 2, 2, "div", 7);
        ɵɵconditionalCreate(5, PoRichTextToolbarComponent_Conditional_5_Template, 2, 2, "div", 8);
        ɵɵconditionalCreate(6, PoRichTextToolbarComponent_Conditional_6_Template, 2, 2, "div", 9);
        ɵɵconditionalCreate(7, PoRichTextToolbarComponent_Conditional_7_Template, 2, 2, "div", 10);
        ɵɵelementEnd();
        ɵɵelementStart(8, "po-rich-text-image-modal", 11, 1);
        ɵɵlistener("p-command", function PoRichTextToolbarComponent_Template_po_rich_text_image_modal_p_command_8_listener($event) {
          return ctx.emitCommand($event);
        });
        ɵɵelementEnd();
        ɵɵelementStart(10, "po-rich-text-link-modal", 12, 2);
        ɵɵlistener("p-command", function PoRichTextToolbarComponent_Template_po_rich_text_link_modal_p_command_10_listener($event) {
          return ctx.emitCommand($event);
        })("p-link-editing", function PoRichTextToolbarComponent_Template_po_rich_text_link_modal_p_link_editing_10_listener($event) {
          return ctx.emitLinkEditing($event);
        });
        ɵɵelementEnd();
      }
      if (rf & 2) {
        ɵɵclassProp("po-rich-text-disabled", ctx.disabled);
        ɵɵadvance(2);
        ɵɵconditional(!ctx.isActionHidden(ctx.formatToolbarAction) ? 2 : -1);
        ɵɵadvance();
        ɵɵconditional(ctx.showColor() ? 3 : -1);
        ɵɵadvance();
        ɵɵconditional(!ctx.isActionHidden(ctx.alignToolbarAction) ? 4 : -1);
        ɵɵadvance();
        ɵɵconditional(!ctx.isActionHidden(ctx.listToolbarAction) ? 5 : -1);
        ɵɵadvance();
        ɵɵconditional(!ctx.isActionHidden(ctx.linkToolbarAction) ? 6 : -1);
        ɵɵadvance();
        ɵɵconditional(!ctx.isActionHidden(ctx.mediaToolbarAction) ? 7 : -1);
        ɵɵadvance();
        ɵɵproperty("p-size", ctx.size);
        ɵɵadvance(2);
        ɵɵproperty("p-size", ctx.size);
      }
    },
    dependencies: [PoButtonGroupComponent, PoTooltipDirective, PoRichTextImageModalComponent, PoRichTextLinkModalComponent],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoRichTextToolbarComponent, [{
    type: Component,
    args: [{
      selector: "po-rich-text-toolbar",
      standalone: false,
      template: '<div class="po-rich-text-toolbar" #toolbarElement [class.po-rich-text-disabled]="disabled">\n  @if (!isActionHidden(formatToolbarAction)) {\n    <div class="po-rich-text-toolbar-button-align" data-rich-text-toolbar="format">\n      <po-button-group p-toggle="multiple" [p-buttons]="formatButtons" [p-size]="size"> </po-button-group>\n    </div>\n  }\n\n  @if (showColor()) {\n    <div class="po-rich-text-toolbar-button-align" data-rich-text-toolbar="color">\n      <div class="po-rich-text-toolbar-color-picker-container">\n        <button\n          type="button"\n          class="po-button po-button-default po-rich-text-toolbar-color-picker-button"\n          [disabled]="readonly"\n          [p-tooltip]="literals.textColor"\n          [attr.aria-label]="literals.textColor"\n        >\n          <input\n            #colorPickerInput\n            class="po-rich-text-toolbar-color-picker-input"\n            type="color"\n            [disabled]="readonly"\n            (input)="changeTextColor($event.target.value)"\n            [attr.aria-label]="literals.textColor"\n          />\n        </button>\n      </div>\n    </div>\n  }\n\n  @if (!isActionHidden(alignToolbarAction)) {\n    <div class="po-rich-text-toolbar-button-align" data-rich-text-toolbar="align">\n      <po-button-group p-toggle="single" [p-buttons]="alignButtons" [p-size]="size"> </po-button-group>\n    </div>\n  }\n\n  @if (!isActionHidden(listToolbarAction)) {\n    <div class="po-rich-text-toolbar-button-align" data-rich-text-toolbar="list">\n      <po-button-group p-toggle="single" [p-buttons]="listButtons" [p-size]="size"> </po-button-group>\n    </div>\n  }\n\n  @if (!isActionHidden(linkToolbarAction)) {\n    <div class="po-rich-text-toolbar-button-align" data-rich-text-toolbar="link">\n      <po-button-group [p-buttons]="linkButtons" [p-size]="size"> </po-button-group>\n    </div>\n  }\n\n  @if (!isActionHidden(mediaToolbarAction)) {\n    <div class="po-rich-text-toolbar-button-align" data-rich-text-toolbar="media">\n      <po-button-group [p-buttons]="mediaButtons" [p-size]="size"> </po-button-group>\n    </div>\n  }\n</div>\n\n<po-rich-text-image-modal #richTextImageModal [p-size]="size" (p-command)="emitCommand($event)">\n</po-rich-text-image-modal>\n\n<po-rich-text-link-modal\n  #richTextLinkModal\n  [p-size]="size"\n  (p-command)="emitCommand($event)"\n  (p-link-editing)="emitLinkEditing($event)"\n>\n</po-rich-text-link-modal>\n'
    }]
  }], () => [], {
    colorPickerInput: [{
      type: ViewChild,
      args: ["colorPickerInput", {
        read: ElementRef
      }]
    }],
    toolbarElement: [{
      type: ViewChild,
      args: ["toolbarElement", {
        static: true
      }]
    }],
    richTextImageModal: [{
      type: ViewChild,
      args: [PoRichTextImageModalComponent, {
        static: true
      }]
    }],
    richTextLinkModal: [{
      type: ViewChild,
      args: [PoRichTextLinkModalComponent, {
        static: true
      }]
    }],
    command: [{
      type: Output,
      args: ["p-command"]
    }],
    modal: [{
      type: Output,
      args: ["p-modal"]
    }],
    linkEditing: [{
      type: Output,
      args: ["p-link-editing"]
    }],
    hideToolbarActions: [{
      type: Input,
      args: ["p-hide-toolbar-actions"]
    }],
    readonly: [{
      type: Input,
      args: ["p-readonly"]
    }],
    disabled: [{
      type: Input,
      args: ["p-disabled"]
    }],
    size: [{
      type: HostBinding,
      args: ["attr.p-size"]
    }, {
      type: Input,
      args: ["p-size"]
    }]
  });
})();
var providers = [{
  provide: NG_VALUE_ACCESSOR,
  // eslint-disable-next-line
  useExisting: forwardRef(() => PoRichTextComponent),
  multi: true
}, {
  provide: NG_VALIDATORS,
  // eslint-disable-next-line
  useExisting: forwardRef(() => PoRichTextComponent),
  multi: true
}, {
  provide: PoRichTextService
}];
var PoRichTextComponent = class _PoRichTextComponent extends PoRichTextBaseComponent {
  element = inject(ElementRef);
  bodyElement;
  richTextToolbar;
  helperEl;
  listener = this.validateClassesForRequired.bind(this);
  modelLastUpdate;
  toolbarActions = [];
  get errorMsg() {
    return this.errorMessage !== "" && !this.value && this.required && this.invalid ? this.errorMessage : "";
  }
  constructor() {
    const richTextService = inject(PoRichTextService);
    const cd = inject(ChangeDetectorRef);
    super(richTextService, cd);
  }
  ngOnInit() {
    this.toolbarActions = [...this.hideToolbarActions];
  }
  ngOnChanges(changes) {
    if (changes.hideToolbarActions || changes.disabledTextAlign) {
      this.toolbarActions = [...this.hideToolbarActions];
      this.updateAlignOnHideToolbarActionsList();
    }
    if (changes.label) {
      this.displayAdditionalHelp = false;
    }
  }
  ngAfterViewInit() {
    this.addKeyListeners();
    this.verifyAutoFocus();
    this.updateAlignOnHideToolbarActionsList();
  }
  ngOnDestroy() {
    if (!this.onChangeModel) {
      this.element.nativeElement.removeEventListener("keyup", this.listener);
      this.element.nativeElement.removeEventListener("keydown", this.listener);
      this.element.nativeElement.removeEventListener("cut", this.listener);
      this.element.nativeElement.removeEventListener("paste", this.listener);
    }
  }
  /**
   * Função que atribui foco ao componente.
   *
   * Para utilizá-la é necessário ter a instância do componente no DOM, podendo ser utilizado o ViewChild da seguinte forma:
   *
   * ```
   * import { PoRichTextComponent } from '@po-ui/ng-components';
   *
   * ...
   *
   * @ViewChild(PoRichTextComponent, { static: true }) richText: PoRichTextComponent;
   *
   * focusRichText() {
   *   this.richText.focus();
   * }
   * ```
   */
  focus() {
    this.bodyElement.focus();
  }
  onBlur() {
    this.onTouched?.();
  }
  onChangeValue(value) {
    this.change.emit(value);
  }
  onKeyDown(event) {
    this.keydown.emit(event);
  }
  /**
   * Método que exibe `p-helper` ou executa a ação definida em `p-helper{eventOnClick}` ou em `p-additionalHelp`.
   * Para isso, será necessário configurar uma tecla de atalho utilizando o evento `p-keydown`.
   *
   * > Exibe ou oculta o conteúdo do componente `po-helper` quando o componente estiver com foco.
   *
   * ```
   * // Exemplo com p-label e p-helper
   * <po-rich-text
   *  #richtext
   *  ...
   *  p-label="Label do richtext"
   *  [p-helper]="helperOptions"
   *  (p-keydown)="onKeyDown($event, richtext)"
   * ></po-rich-text>
   * ```
   * ```
   * ...
   * onKeyDown(event: KeyboardEvent, inp: PoRichTextComponent): void {
   *  if (event.code === 'F9') {
   *    inp.showAdditionalHelp();
   *  }
   * }
   * ```
   */
  showAdditionalHelp() {
    this.displayAdditionalHelp = !this.displayAdditionalHelp;
    const helper = this.poHelperComponent();
    const isHelpEvt = this.additionalHelp.observed;
    if (!this.label && (helper || this.additionalHelpTooltip || isHelpEvt)) {
      if (isHelpEvt) {
        this.additionalHelp.emit();
      }
      if (typeof helper !== "string" && typeof helper?.eventOnClick === "function") {
        helper.eventOnClick();
        return;
      }
      if (this.helperEl?.helperIsVisible()) {
        this.helperEl?.closeHelperPopover();
        return;
      }
      this.helperEl?.openHelperPopover();
      return;
    }
    return this.displayAdditionalHelp;
  }
  updateValue(value) {
    this.value = value;
    this.invalid = !value;
    this.controlChangeModelEmitter(this.value);
    this.updateModel(this.value);
  }
  addKeyListeners() {
    if (!this.onChangeModel) {
      this.element.nativeElement.addEventListener("keyup", this.listener);
      this.element.nativeElement.addEventListener("keydown", this.listener);
      this.element.nativeElement.addEventListener("cut", this.listener);
      this.element.nativeElement.addEventListener("paste", this.listener);
    }
  }
  controlChangeModelEmitter(value) {
    if (this.modelLastUpdate !== value) {
      this.changeModel.emit(value);
      this.modelLastUpdate = value;
    }
  }
  verifyAutoFocus() {
    if (this.autoFocus) {
      this.focus();
    }
  }
  validateClassesForRequired() {
    setTimeout(() => {
      const value = this.value;
      const element = this.element.nativeElement;
      if (!value && this.required) {
        element.classList.add("ng-invalid");
        element.classList.add("ng-dirty");
      } else {
        element.classList.remove("ng-invalid");
      }
    });
  }
  isAllActionsHidden() {
    return Object.values(PoRichTextToolbarActions).every((action) => this.toolbarActions.includes(action));
  }
  updateAlignOnHideToolbarActionsList() {
    if (this.disabledTextAlign && !this.toolbarActions.includes(PoRichTextToolbarActions.Align)) {
      this.toolbarActions.push(PoRichTextToolbarActions.Align);
    }
  }
  setHelper(label, additionalHelpTooltip) {
    return setHelperSettings(label, additionalHelpTooltip, this.poHelperComponent(), this.size, this.additionalHelp.observed ? this.additionalHelp : void 0);
  }
  static ɵfac = function PoRichTextComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoRichTextComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoRichTextComponent,
    selectors: [["po-rich-text"]],
    viewQuery: function PoRichTextComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(PoRichTextBodyComponent, 7)(PoRichTextToolbarComponent, 5)(_c17, 5, PoHelperComponent);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.bodyElement = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.richTextToolbar = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.helperEl = _t.first);
      }
    },
    standalone: false,
    features: [ɵɵProvidersFeature(providers), ɵɵInheritDefinitionFeature, ɵɵNgOnChangesFeature],
    decls: 7,
    vars: 21,
    consts: [["richTextBody", ""], ["richTextToolbar", ""], [3, "p-disabled", "p-label", "p-optional", "p-required", "p-show-required", "p-helper", "p-show-helper", "p-size", "p-text-wrap", "p-compact-label"], [1, "po-field-container-content"], [3, "p-change", "p-commands", "p-selected-link", "p-shortcut-command", "p-value", "p-blur", "p-keydown", "p-height", "p-label", "p-model-value", "p-placeholder", "p-readonly", "p-disabled", "p-loading", "p-size"], [3, "p-readonly", "p-disabled", "p-hide-toolbar-actions", "p-size"], [3, "p-append-in-body", "p-help", "p-disabled", "p-error-limit", "p-error-pattern", "p-size", "p-helper", "p-show-helper"], [3, "p-link-editing", "p-command", "p-readonly", "p-disabled", "p-hide-toolbar-actions", "p-size"]],
    template: function PoRichTextComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵdeclareLet(0);
        ɵɵelementStart(1, "po-field-container", 2)(2, "div", 3)(3, "po-rich-text-body", 4, 0);
        ɵɵlistener("p-change", function PoRichTextComponent_Template_po_rich_text_body_p_change_3_listener($event) {
          return ctx.onChangeValue($event);
        })("p-commands", function PoRichTextComponent_Template_po_rich_text_body_p_commands_3_listener($event) {
          return ctx.richTextToolbar == null ? null : ctx.richTextToolbar.setButtonsStates($event);
        })("p-selected-link", function PoRichTextComponent_Template_po_rich_text_body_p_selected_link_3_listener($event) {
          return ctx.richTextToolbar == null ? null : ctx.richTextToolbar.selectedLink($event);
        })("p-shortcut-command", function PoRichTextComponent_Template_po_rich_text_body_p_shortcut_command_3_listener() {
          return ctx.richTextToolbar == null ? null : ctx.richTextToolbar.shortcutTrigger();
        })("p-value", function PoRichTextComponent_Template_po_rich_text_body_p_value_3_listener($event) {
          return ctx.updateValue($event);
        })("p-blur", function PoRichTextComponent_Template_po_rich_text_body_p_blur_3_listener() {
          return ctx.onBlur();
        })("p-keydown", function PoRichTextComponent_Template_po_rich_text_body_p_keydown_3_listener($event) {
          return ctx.onKeyDown($event);
        });
        ɵɵelementEnd();
        ɵɵconditionalCreate(5, PoRichTextComponent_Conditional_5_Template, 2, 4, "po-rich-text-toolbar", 5);
        ɵɵelementEnd();
        ɵɵconditionalCreate(6, PoRichTextComponent_Conditional_6_Template, 1, 8, "po-field-container-bottom", 6);
        ɵɵelementEnd();
      }
      if (rf & 2) {
        const poHelper_r5 = ɵɵstoreLet(ctx.setHelper(ctx.label, ctx.additionalHelpTooltip));
        ɵɵadvance();
        ɵɵproperty("p-disabled", ctx.isDisabled)("p-label", ctx.label)("p-optional", ctx.optional)("p-required", ctx.required)("p-show-required", ctx.showRequired)("p-helper", ctx.label ? poHelper_r5.helperSettings : void 0)("p-show-helper", ctx.label ? ctx.displayAdditionalHelp : void 0)("p-size", ctx.size)("p-text-wrap", ctx.labelTextWrap())("p-compact-label", ctx.compactLabel());
        ɵɵadvance(2);
        ɵɵproperty("p-height", ctx.height)("p-label", ctx.label)("p-model-value", ctx.value)("p-placeholder", ctx.placeholder)("p-readonly", ctx.readonly)("p-disabled", ctx.isDisabled)("p-loading", ctx.loading)("p-size", ctx.size);
        ɵɵadvance(2);
        ɵɵconditional(!ctx.isAllActionsHidden() ? 5 : -1);
        ɵɵadvance();
        ɵɵconditional(!ctx.readonly ? 6 : -1);
      }
    },
    dependencies: [PoFieldContainerBottomComponent, PoFieldContainerComponent, PoRichTextBodyComponent, PoRichTextToolbarComponent],
    encapsulation: 2,
    changeDetection: 0
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoRichTextComponent, [{
    type: Component,
    args: [{
      selector: "po-rich-text",
      changeDetection: ChangeDetectionStrategy.OnPush,
      providers,
      standalone: false,
      template: '@let poHelper = setHelper(label, additionalHelpTooltip);\n<po-field-container\n  [p-disabled]="isDisabled"\n  [p-label]="label"\n  [p-optional]="optional"\n  [p-required]="required"\n  [p-show-required]="showRequired"\n  [p-helper]="label ? poHelper.helperSettings : undefined"\n  [p-show-helper]="label ? displayAdditionalHelp : undefined"\n  [p-size]="size"\n  [p-text-wrap]="labelTextWrap()"\n  [p-compact-label]="compactLabel()"\n>\n  <div class="po-field-container-content">\n    <po-rich-text-body\n      #richTextBody\n      [p-height]="height"\n      [p-label]="label"\n      [p-model-value]="value"\n      [p-placeholder]="placeholder"\n      [p-readonly]="readonly"\n      [p-disabled]="isDisabled"\n      [p-loading]="loading"\n      [p-size]="size"\n      (p-change)="onChangeValue($event)"\n      (p-commands)="richTextToolbar?.setButtonsStates($event)"\n      (p-selected-link)="richTextToolbar?.selectedLink($event)"\n      (p-shortcut-command)="richTextToolbar?.shortcutTrigger()"\n      (p-value)="updateValue($event)"\n      (p-blur)="onBlur()"\n      (p-keydown)="onKeyDown($event)"\n    >\n    </po-rich-text-body>\n    @if (!isAllActionsHidden()) {\n      <po-rich-text-toolbar\n        #richTextToolbar\n        [p-readonly]="readonly || isDisabled"\n        [p-disabled]="isDisabled"\n        [p-hide-toolbar-actions]="toolbarActions"\n        [p-size]="size"\n        (p-link-editing)="richTextBody.linkEditing($event)"\n        (p-command)="richTextBody.executeCommand($event)"\n      >\n      </po-rich-text-toolbar>\n    }\n  </div>\n  @if (!readonly) {\n    <po-field-container-bottom\n      [p-append-in-body]="appendBox"\n      [p-help]="help"\n      [p-disabled]="isDisabled"\n      [p-error-limit]="errorLimit"\n      [p-error-pattern]="errorMsg"\n      [p-size]="size"\n      [p-helper]="!label ? poHelper.helperSettings : undefined"\n      [p-show-helper]="!label ? displayAdditionalHelp : undefined"\n    ></po-field-container-bottom>\n  }\n</po-field-container>\n'
    }]
  }], () => [], {
    bodyElement: [{
      type: ViewChild,
      args: [PoRichTextBodyComponent, {
        static: true
      }]
    }],
    richTextToolbar: [{
      type: ViewChild,
      args: [PoRichTextToolbarComponent, {
        static: false
      }]
    }],
    helperEl: [{
      type: ViewChild,
      args: ["helperEl", {
        read: PoHelperComponent,
        static: false
      }]
    }]
  });
})();
var PoFieldValidateModel = class _PoFieldValidateModel extends PoFieldModel {
  changeDetector;
  /**
   * @optional
   *
   * @description
   *
   * Define se a indicação de campo opcional será exibida.
   *
   * > Não será exibida a indicação se:
   * - O campo conter `p-required`;
   * - Não possuir `p-help` e/ou `p-label`.
   *
   * @default `false`
   */
  optional = false;
  /**
   * @optional
   *
   * @description
   *
   * Define que o campo será obrigatório.
   *
   * @default `false`
   */
  required = false;
  /**
   * @optional
   *
   * @description
   *
   * Exibe a mensagem setada se o campo estiver vazio e for requerido.
   *
   * > Necessário que a propriedade `p-required` esteja habilitada.
   *
   */
  fieldErrorMessage;
  /**
   * @optional
   *
   * @description
   *
   * Limita a exibição da mensagem de erro a duas linhas e exibe um tooltip com o texto completo.
   *
   * > Caso essa propriedade seja definida como `true`, a mensagem de erro será limitada a duas linhas
   * e um tooltip será exibido ao passar o mouse sobre a mensagem para mostrar o conteúdo completo.
   *
   * @default `false`
   */
  errorLimit = false;
  /**
   *  Define se a indicação de campo obrigatório será exibida.
   *
   * > Não será exibida a indicação se:
   * - Não possuir `p-help` e/ou `p-label`.
   */
  showRequired = false;
  hasValidatorRequired = false;
  onValidatorChange;
  constructor(changeDetector) {
    super(changeDetector);
    this.changeDetector = changeDetector;
  }
  validate(abstractControl) {
    if (!this.hasValidatorRequired && this.fieldErrorMessage && abstractControl.hasValidator(Validators.required)) {
      this.hasValidatorRequired = true;
    }
    if (PoValidators.requiredFailed(this.required || this.hasValidatorRequired, this.disabled, abstractControl.value)) {
      this.changeDetector.markForCheck();
      return {
        required: {
          valid: false
        }
      };
    }
    return this.extraValidation(abstractControl);
  }
  registerOnValidatorChange(fn) {
    this.onValidatorChange = fn;
  }
  validateModel() {
    if (this.onValidatorChange) {
      this.onValidatorChange();
    }
  }
  static ɵfac = function PoFieldValidateModel_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoFieldValidateModel)(ɵɵdirectiveInject(ChangeDetectorRef));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoFieldValidateModel,
    inputs: {
      optional: [2, "p-optional", "optional", convertToBoolean],
      required: [2, "p-required", "required", convertToBoolean],
      fieldErrorMessage: [0, "p-field-error-message", "fieldErrorMessage"],
      errorLimit: [0, "p-error-limit", "errorLimit"],
      showRequired: [0, "p-show-required", "showRequired"]
    },
    features: [ɵɵInheritDefinitionFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoFieldValidateModel, [{
    type: Directive
  }], () => [{
    type: ChangeDetectorRef
  }], {
    optional: [{
      type: Input,
      args: [{
        alias: "p-optional",
        transform: convertToBoolean
      }]
    }],
    required: [{
      type: Input,
      args: [{
        alias: "p-required",
        transform: convertToBoolean
      }]
    }],
    fieldErrorMessage: [{
      type: Input,
      args: ["p-field-error-message"]
    }],
    errorLimit: [{
      type: Input,
      args: ["p-error-limit"]
    }],
    showRequired: [{
      type: Input,
      args: ["p-show-required"]
    }]
  });
})();
var PO_SELECT_FIELD_LABEL_DEFAULT = "label";
var PO_SELECT_FIELD_VALUE_DEFAULT = "value";
var PoSelectComponent = class _PoSelectComponent extends PoFieldValidateModel {
  el = inject(ElementRef);
  renderer = inject(Renderer2);
  selectElement;
  helperEl;
  /**
   * @optional
   *
   * @description
   *
   * Evento disparado ao sair do campo.
   */
  blur = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Evento disparado sempre que o valor do model é alterado, seja por interação do usuário
   * ou por atualização programática (ex: `setValue`, `patchValue`, carregamento assíncrono).
   *
   * Diferentemente do `p-change`, que é disparado apenas por interação do usuário,
   * o `p-change-model` cobre todos os cenários de alteração de valor.
   *
   * Não emite quando o novo valor é idêntico ao anterior (deduplicação automática).
   */
  changeModel = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Função para atualizar o ngModel do componente, necessário quando não for utilizado dentro da tag form.
   *
   * Na versão 12.2.0 do Angular a verificação `strictTemplates` vem true como default. Portanto, para utilizar
   * two-way binding no componente deve se utilizar da seguinte forma:
   *
   * ```
   * <po-select ... [ngModel]="selectModel" (ngModelChange)="selectModel = $event"> </po-select>
   * ```
   */
  ngModelChange = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Indica que o campo será somente para leitura.
   *
   * @default `false`
   */
  readonly = false;
  /**
   * @optional
   *
   * @description
   * Define se o título do campo será exibido de forma compacta.
   *
   * Quando habilitado (`true`), o modo compacto afeta o conjunto composto por:
   * - `po-label`
   * - `p-requirement (showRequired)`
   * - `po-helper`
   *
   * Ou seja, todos os elementos relacionados ao título do campo
   * (rótulo, indicador de obrigatoriedade e componente auxiliar) passam
   * a seguir o comportamento de layout compacto.
   *
   * Também é possível definir esse comportamento de forma global,
   * uma única vez, na folha de estilo geral da aplicação, por meio
   * da customização dos tokens CSS:
   *
   * - `--field-container-title-justify`
   * - `--field-container-title-flex`
   *
   * Exemplo:
   *
   * ```
   * :root {
   *   --field-container-title-justify: flex-start;
   *   --field-container-title-flex: 0 1 auto;
   * }
   * ```
   *
   * Dessa forma, o layout compacto passa a ser o padrão da aplicação,
   * sem a necessidade de definir a propriedade individualmente em cada campo.
   *
   * @default `false`
   */
  compactLabel = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "compactLabel"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-compact-label",
    transform: convertToBoolean
  }));
  /** Mensagem que aparecerá enquanto nenhuma opção estiver selecionada. */
  placeholder;
  displayValue;
  id = `po-select[${uuid()}]`;
  modelValue;
  modelLastUpdate;
  selectedValue;
  optionsDefault = [];
  listGroupOptions = [];
  optionWithoutGroup = [];
  isSafari = isSafari();
  onModelTouched;
  _fieldLabel = PO_SELECT_FIELD_LABEL_DEFAULT;
  _fieldValue = PO_SELECT_FIELD_VALUE_DEFAULT;
  _loading = false;
  _options;
  _size = void 0;
  _initialSize = void 0;
  /**
   * Nesta propriedade deve ser definido uma coleção de objetos que implementam a interface `PoSelectOption`,
   * ou uma coleção de objetos dentro de grupos diferentes, que seriam da interface `PoSelectOptionGroup`.
   *
   * Caso esta lista estiver vazia, o model será `undefined`.
   *
   * > Essa propriedade é imutável, ou seja, sempre que quiser atualizar a lista de opções disponíveis
   * atualize a referência do objeto:
   *
   * ```
   * // atualiza a referência do objeto garantindo a atualização do template
   * this.options = [...this.options, { value: 'x', label: 'Nova opção' }];
   *
   * // evite, pois não atualiza a referência do objeto podendo gerar atrasos na atualização do template
   * this.options.push({ value: 'x', label: 'Nova opção' });
   * ```
   *
   * > Para coleção de objetos dentro de grupos distintos será exibido a label e opções somente se a propriedade `options` possua valores.
   *  Sendo assim, a estrutura seguiria dessa forma:
   *
   * ```
   * this.options = [{
   *  label: 'Opções',
   *  options: [
   *    { value: 1, label: 'opção 1' },
   *    { value: 2, label: 'opção 2' }
   *  ],
   * }];
   * ```
   *
   * É possível a utilização de opções agrupadas e desagrupadas em conjunto, porém será feita a ordenação de exibir as opções
   * desagrupadas acima.
   *
   */
  set options(options) {
    this.listGroupOptions = [];
    this.optionWithoutGroup = [];
    if (this.fieldLabel && this.fieldValue && options) {
      options.map((option) => {
        if (this.isItemGroup(option)) {
          option.options.map((opt) => {
            opt.label = opt[this.fieldLabel];
            opt.value = opt[this.fieldValue];
          });
        } else {
          option.label = option[this.fieldLabel];
          option.value = option[this.fieldValue];
        }
      });
    }
    if (options) {
      this.optionsDefault = [...options];
      this.separateOptions();
      this.optionsDefault = [];
      this.optionsDefault = [...this.optionWithoutGroup, ...this.transformInArray(this.listGroupOptions)];
      this.onUpdateOptions();
      this._options = [...this.optionsDefault];
    }
  }
  get options() {
    return this._options;
  }
  /**
   * @optional
   *
   * @description
   * Deve ser informado o nome da propriedade do objeto que será utilizado para a conversão dos itens apresentados na lista do componente
   * (`p-options`), esta propriedade será responsável pelo texto de apresentação de cada item da lista.
   *
   * @default `label`
   */
  set fieldLabel(value) {
    this._fieldLabel = value || PO_SELECT_FIELD_LABEL_DEFAULT;
    if (this.options && this.options.length > 0) {
      this.options = [...this.options];
    }
  }
  get fieldLabel() {
    return this._fieldLabel;
  }
  /**
   * @optional
   *
   * @description
   * Deve ser informado o nome da propriedade do objeto que será utilizado para a conversão dos itens apresentados na lista do componente
   * (`p-options`), esta propriedade será responsável pelo valor de cada item da lista.
   *
   * @default `value`
   */
  set fieldValue(value) {
    this._fieldValue = value || PO_SELECT_FIELD_VALUE_DEFAULT;
    if (this.options && this.options.length > 0) {
      this.options = [...this.options];
    }
  }
  /**
   * @docsPrivate
   *
   * Determinar se o valor do compo deve retorna objeto do tipo {value: any, label: any}
   */
  controlValueWithLabel = false;
  get fieldValue() {
    return this._fieldValue;
  }
  /**
   * @optional
   *
   * @description
   * Exibe um ícone de carregamento no lado direito do campo para sinalizar que uma operação está em andamento.
   *
   * @default `false`
   */
  set loading(value) {
    this._loading = value;
    this.changeDetector?.markForCheck();
  }
  get loading() {
    return this._loading;
  }
  get isDisabled() {
    return this.disabled || this.loading;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define o tamanho do componente:
   * - `small`: altura do input como 32px (disponível apenas para acessibilidade AA).
   * - `medium`: altura do input como 44px.
   *
   * > Caso a acessibilidade AA não esteja configurada, o tamanho `medium` será mantido.
   * Para mais detalhes, consulte a documentação do [po-theme](https://po-ui.io/documentation/po-theme).
   *
   * @default `medium`
   */
  set size(value) {
    this._initialSize = value;
    this.applySizeBasedOnA11y();
  }
  get size() {
    return this._size ?? getDefaultSizeFn(PoFieldSize);
  }
  /**
   * @optional
   *
   * @description
   *
   * Define as opções do componente de ajuda (po-helper) que será exibido ao lado do label quando a propriedade `p-label` for definida, ou, ao lado do componente na ausência da propriedade `p-label`.
   * > Para mais informações acesse: https://po-ui.io/documentation/po-helper.
   *
   * > Ao configurar esta propriedade, o antigo ícone de ajuda adicional (`p-additional-help-tooltip` e `p-additional-help`) será ignorado.
   */
  poHelperComponent = input(void 0, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "poHelperComponent"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-helper"
  }));
  /**
   * @optional
   *
   * @description
   *
   * Habilita a quebra automática do texto da propriedade `p-label`. Quando `p-label-text-wrap` for verdadeiro, o texto que excede
   * o espaço disponível é transferido para a próxima linha em pontos apropriados para uma
   * leitura clara.
   *
   * @default `false`
   */
  labelTextWrap = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "labelTextWrap"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-label-text-wrap"
  }));
  /* istanbul ignore next */
  constructor() {
    const changeDetector = inject(ChangeDetectorRef);
    super(changeDetector);
  }
  ngOnChanges(changes) {
    if (changes.label) {
      this.displayAdditionalHelp = false;
    }
    if (changes.options?.currentValue) {
      this.options = changes.options.currentValue;
    }
  }
  onThemeChange() {
    this.applySizeBasedOnA11y();
  }
  /**
   * Função que atribui foco ao componente.
   *
   * Para utilizá-la é necessário ter a instância do componente no DOM, podendo ser utilizado o ViewChild da seguinte forma:
   *
   * ```
   * import { PoSelectComponent } from '@po-ui/ng-components';
   *
   * ...
   *
   * @ViewChild(PoSelectComponent, { static: true }) select: PoSelectComponent;
   *
   * focusSelect() {
   *   this.select.focus();
   * }
   * ```
   */
  focus() {
    if (!this.disabled) {
      this.selectElement.nativeElement.focus();
    }
  }
  getErrorPattern() {
    return this.fieldErrorMessage && this.hasInvalidClass() ? this.fieldErrorMessage : "";
  }
  hasInvalidClass() {
    return this.el.nativeElement.classList.contains("ng-invalid") && this.el.nativeElement.classList.contains("ng-dirty");
  }
  //Transforma o tamanho do input para o tamanho do ícone de loading correspondente
  mapSizeToIcon(size) {
    return mapInputSizeToLoadingIcon(size);
  }
  onBlur(event) {
    this.onModelTouched?.();
    if (event.type === "blur") {
      this.blur.emit();
    }
  }
  // Altera o valor ao selecionar um item.
  onSelectChange(value) {
    this.onModelTouched?.();
    if (value && this.options && this.options.length) {
      const optionFound = this.findOptionValue(value);
      if (optionFound) {
        this.updateValues(optionFound);
      }
    }
  }
  onUpdateOptions() {
    if (this.modelValue) {
      this.onSelectChange(this.modelValue);
    }
  }
  // Atualiza valores
  updateValues(option) {
    if (this.selectedValue !== option[this.fieldValue]) {
      this.selectedValue = option[this.fieldValue];
      this.selectElement.nativeElement.value = option[this.fieldValue];
      this.updateModel(this.getValueUpdate(option));
      this.displayValue = option[this.fieldLabel];
      this.emitChange(option[this.fieldValue]);
      this.controlChangeModelEmitter(option[this.fieldValue]);
    }
  }
  // Recebe as alterações do model
  onWriteValue(value) {
    value = this.getValueWrite(value);
    const optionFound = this.findOptionValue(value);
    if (optionFound) {
      this.selectElement.nativeElement.value = optionFound.value;
      this.selectedValue = optionFound[this.fieldValue];
      this.displayValue = optionFound[this.fieldLabel];
    } else if (validValue(this.selectedValue)) {
      this.selectElement.nativeElement.value = void 0;
      this.updateModel(void 0);
      this.selectedValue = void 0;
      this.displayValue = void 0;
    }
    this.modelValue = value;
    this.changeDetector.detectChanges();
    this.controlChangeModelEmitter(this.selectedValue);
  }
  extraValidation(c) {
    return null;
  }
  isItemGroup(item) {
    if (item.options) {
      return Array.isArray(item.options) ? true : false;
    }
    return false;
  }
  onKeyDown(event) {
    const isFieldFocused = document.activeElement === this.selectElement.nativeElement;
    if (isFieldFocused) {
      this.keydown.emit(event);
    }
  }
  registerOnTouched(fn) {
    this.onModelTouched = fn;
  }
  /**
   * Método que exibe `p-helper` ou executa a ação definida em `p-helper{eventOnClick}` ou em `p-additionalHelp`.
   * Para isso, será necessário configurar uma tecla de atalho utilizando o evento `p-keydown`.
   *
   * > Exibe ou oculta o conteúdo do componente `po-helper` quando o componente estiver com foco.
   *
   * ```
   * //Exemplo com p-label e p-helper
   * <po-select
   *  #select
   *  ...
   *  p-label="Label do select"
   *  [p-helper]="helperOptions"
   *  (p-keydown)="onKeyDown($event, select)"
   * ></po-select>
   * ```
   * ```typescript
   * onKeyDown(event: KeyboardEvent, inp: PoSelectComponent): void {
   *  if (event.code === 'F9') {
   *    inp.showAdditionalHelp();
   *  }
   * }
   * ```
   */
  showAdditionalHelp() {
    return super.showAdditionalHelp(this.helperEl, this.poHelperComponent());
  }
  setHelper(label, additionalHelpTooltip) {
    return setHelperSettings(label, additionalHelpTooltip, this.poHelperComponent(), this.size, this.isAdditionalHelpEventTriggered() ? this.additionalHelp : void 0);
  }
  controlChangeModelEmitter(value) {
    if (this.modelLastUpdate !== value) {
      this.changeModel.emit(value);
      this.modelLastUpdate = value;
    }
  }
  isEqual(value, inputValue) {
    if ((value || value === 0) && inputValue) {
      return value.toString() === inputValue.toString();
    }
    if (value === null && inputValue !== null || value === void 0 && inputValue !== void 0) {
      value = `${value}`;
    }
    return value === inputValue;
  }
  findOptionValue(value) {
    if (this.options) {
      return this.options.find((option) => this.isEqual(option.value, value));
    }
  }
  getValueUpdate(option) {
    if (this.controlValueWithLabel) {
      return {
        value: option[this.fieldValue],
        label: option[this.fieldLabel]
      };
    }
    return option[this.fieldValue];
  }
  getValueWrite(data) {
    if (!this.controlValueWithLabel || data == null) {
      return data;
    }
    if (data?.value !== void 0) {
      return data.value;
    }
    return data;
  }
  transformInArray(objectWithArray) {
    return objectWithArray.reduce((options, items) => {
      if (items.options) {
        return options.concat(items.options);
      }
      return [];
    }, []);
  }
  separateOptions() {
    this.optionsDefault.forEach((option) => {
      if (this.isItemGroup(option)) {
        this.validateOptions(option.options);
        this.listGroupOptions.push(option);
      } else {
        this.optionWithoutGroup.push(option);
      }
    });
    if (this.optionWithoutGroup.length > 0) {
      this.validateOptions(this.optionWithoutGroup);
    }
  }
  validateOptions(options) {
    PoUtils.removeDuplicatedOptions(options);
    PoUtils.removeUndefinedAndNullOptions(options);
  }
  applySizeBasedOnA11y() {
    const size = validateSizeFn(this._initialSize, PoFieldSize);
    this._size = size;
  }
  static ɵfac = function PoSelectComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoSelectComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoSelectComponent,
    selectors: [["po-select"]],
    viewQuery: function PoSelectComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c142, 7, ElementRef)(_c17, 5, PoHelperComponent);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.selectElement = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.helperEl = _t.first);
      }
    },
    hostVars: 2,
    hostBindings: function PoSelectComponent_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("PoUiThemeChange", function PoSelectComponent_PoUiThemeChange_HostBindingHandler() {
          return ctx.onThemeChange();
        }, ɵɵresolveWindow);
      }
      if (rf & 2) {
        ɵɵattribute("p-loading", ctx.loading)("p-size", ctx.size);
      }
    },
    inputs: {
      readonly: [2, "p-readonly", "readonly", convertToBoolean],
      compactLabel: [1, "p-compact-label", "compactLabel"],
      placeholder: [0, "p-placeholder", "placeholder"],
      options: [0, "p-options", "options"],
      fieldLabel: [0, "p-field-label", "fieldLabel"],
      fieldValue: [0, "p-field-value", "fieldValue"],
      controlValueWithLabel: [2, "p-control-value-with-label", "controlValueWithLabel", convertToBoolean],
      loading: [2, "p-loading", "loading", convertToBoolean],
      size: [0, "p-size", "size"],
      poHelperComponent: [1, "p-helper", "poHelperComponent"],
      labelTextWrap: [1, "p-label-text-wrap", "labelTextWrap"]
    },
    outputs: {
      blur: "p-blur",
      changeModel: "p-change-model",
      ngModelChange: "ngModelChange"
    },
    standalone: false,
    features: [ɵɵProvidersFeature([{
      provide: NG_VALUE_ACCESSOR,
      useExisting: forwardRef(() => _PoSelectComponent),
      multi: true
    }, {
      provide: NG_VALIDATORS,
      useExisting: forwardRef(() => _PoSelectComponent),
      multi: true
    }]), ɵɵInheritDefinitionFeature, ɵɵNgOnChangesFeature],
    decls: 14,
    vars: 29,
    consts: [["select", ""], ["helperEl", ""], [3, "p-disabled", "p-id", "p-label", "p-optional", "p-required", "p-show-required", "p-helper", "p-show-helper", "p-size", "p-text-wrap", "p-compact-label"], [1, "po-field-container-content"], [1, "po-field-container-content-helper-wrapper"], [1, "po-field-container-input"], [1, "po-select", "po-select-phosphor", 3, "blur", "change", "keydown", "disabled", "id", "required"], [1, "po-field-icon-container-right"], [1, "po-field-helper-button", 3, "p-size", "p-helper", "p-disabled", "p-append-in-body"], [3, "p-append-in-body", "p-help", "p-disabled", "p-error-pattern", "p-error-limit", "p-size"], [3, "disabled", "hidden", "selected", "value"], [3, "hidden"], [3, "disabled", "value"], [3, "label", "disabled"], [3, "value", "disabled"], [3, "p-size"]],
    template: function PoSelectComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵdeclareLet(0);
        ɵɵelementStart(1, "po-field-container", 2)(2, "div", 3)(3, "div", 4)(4, "div", 5)(5, "select", 6, 0);
        ɵɵlistener("blur", function PoSelectComponent_Template_select_blur_5_listener($event) {
          return ctx.onBlur($event);
        })("change", function PoSelectComponent_Template_select_change_5_listener($event) {
          return ctx.onSelectChange($event.target.value);
        })("keydown", function PoSelectComponent_Template_select_keydown_5_listener($event) {
          return ctx.onKeyDown($event);
        });
        ɵɵconditionalCreate(7, PoSelectComponent_Conditional_7_Template, 3, 3);
        ɵɵconditionalCreate(8, PoSelectComponent_Conditional_8_Template, 2, 1, "option");
        ɵɵconditionalCreate(9, PoSelectComponent_Conditional_9_Template, 2, 0);
        ɵɵconditionalCreate(10, PoSelectComponent_Conditional_10_Template, 2, 0);
        ɵɵelementEnd();
        ɵɵconditionalCreate(11, PoSelectComponent_Conditional_11_Template, 2, 1, "div", 7);
        ɵɵelementEnd();
        ɵɵconditionalCreate(12, PoSelectComponent_Conditional_12_Template, 2, 4, "po-helper", 8);
        ɵɵelementEnd()();
        ɵɵconditionalCreate(13, PoSelectComponent_Conditional_13_Template, 1, 6, "po-field-container-bottom", 9);
        ɵɵelementEnd();
      }
      if (rf & 2) {
        const poHelper_r6 = ɵɵstoreLet(ctx.setHelper(ctx.label, ctx.additionalHelpTooltip));
        ɵɵadvance();
        ɵɵproperty("p-disabled", ctx.isDisabled)("p-id", ctx.id)("p-label", ctx.label)("p-optional", ctx.optional)("p-required", ctx.required)("p-show-required", ctx.showRequired)("p-helper", ctx.label ? poHelper_r6.helperSettings : void 0)("p-show-helper", ctx.label ? ctx.displayAdditionalHelp : void 0)("p-size", ctx.size)("p-text-wrap", ctx.labelTextWrap())("p-compact-label", ctx.compactLabel());
        ɵɵadvance(4);
        ɵɵclassProp("po-select-loading", ctx.loading)("po-select-placeholder", !(ctx.selectedValue == null ? null : ctx.selectedValue.toString()) && !!ctx.placeholder);
        ɵɵproperty("disabled", ctx.isDisabled)("id", ctx.id)("required", ctx.required);
        ɵɵattribute("data-focused-element", !ctx.isDisabled)("data-inactive-component", ctx.disabled || ctx.readonly || ctx.loading)("name", ctx.name);
        ɵɵadvance(2);
        ɵɵconditional(!ctx.isSafari ? 7 : -1);
        ɵɵadvance();
        ɵɵconditional(ctx.isSafari ? 8 : -1);
        ɵɵadvance();
        ɵɵconditional(ctx.optionWithoutGroup.length > 0 ? 9 : -1);
        ɵɵadvance();
        ɵɵconditional(ctx.listGroupOptions.length > 0 ? 10 : -1);
        ɵɵadvance();
        ɵɵconditional(ctx.loading ? 11 : -1);
        ɵɵadvance();
        ɵɵconditional(!ctx.label && ctx.poHelperComponent() || !ctx.label && poHelper_r6.hideAdditionalHelp ? 12 : -1);
        ɵɵadvance();
        ɵɵconditional(!ctx.readonly ? 13 : -1);
      }
    },
    dependencies: [NgSelectOption, ɵNgSelectMultipleOption, PoFieldContainerBottomComponent, PoFieldContainerComponent, PoHelperComponent, PoLoadingIconComponent],
    encapsulation: 2,
    changeDetection: 0
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoSelectComponent, [{
    type: Component,
    args: [{
      selector: "po-select",
      changeDetection: ChangeDetectionStrategy.OnPush,
      providers: [{
        provide: NG_VALUE_ACCESSOR,
        useExisting: forwardRef(() => PoSelectComponent),
        multi: true
      }, {
        provide: NG_VALIDATORS,
        useExisting: forwardRef(() => PoSelectComponent),
        multi: true
      }],
      standalone: false,
      template: `@let poHelper = setHelper(label, additionalHelpTooltip);
<po-field-container
  [p-disabled]="isDisabled"
  [p-id]="id"
  [p-label]="label"
  [p-optional]="optional"
  [p-required]="required"
  [p-show-required]="showRequired"
  [p-helper]="label ? poHelper.helperSettings : undefined"
  [p-show-helper]="label ? displayAdditionalHelp : undefined"
  [p-size]="size"
  [p-text-wrap]="labelTextWrap()"
  [p-compact-label]="compactLabel()"
>
  <div class="po-field-container-content">
    <div class="po-field-container-content-helper-wrapper">
      <div class="po-field-container-input">
        <select
          #select
          class="po-select po-select-phosphor"
          [class.po-select-loading]="loading"
          [class.po-select-placeholder]="!selectedValue?.toString() && !!placeholder"
          [attr.data-focused-element]="!isDisabled"
          [attr.data-inactive-component]="disabled || readonly || loading"
          [attr.name]="name"
          [disabled]="isDisabled"
          [id]="id"
          [required]="required"
          (blur)="onBlur($event)"
          (change)="onSelectChange($event.target.value)"
          (keydown)="onKeyDown($event)"
        >
          @if (!isSafari) {
            @if (!selectedValue?.toString() || !!placeholder) {
              <option
                [disabled]="!!placeholder"
                [hidden]="!selectedValue?.toString() && !placeholder"
                [selected]="!selectedValue?.toString()"
                [value]="placeholder ?? ''"
              >
                {{ placeholder }}
              </option>
            }
            <option [hidden]="true">{{ displayValue }}</option>
          }

          @if (isSafari) {
            <option>{{ displayValue || placeholder }}</option>
          }

          @if (optionWithoutGroup.length > 0) {
            @for (item of optionWithoutGroup; track item) {
              <option [disabled]="readonly" [value]="item?.[this.fieldValue]">
                {{ item?.[this.fieldLabel] }}
              </option>
            }
          }
          @if (listGroupOptions.length > 0) {
            @for (item of listGroupOptions; track item) {
              @if (item?.options.length > 0) {
                <optgroup label="{{ item?.label }}" [disabled]="readonly">
                  @for (subItem of item.options; track subItem) {
                    <option [value]="subItem?.[this.fieldValue]" [disabled]="readonly">
                      {{ subItem?.[this.fieldLabel] }}
                    </option>
                  }
                </optgroup>
              }
            }
          }
        </select>

        @if (loading) {
          <div class="po-field-icon-container-right">
            <po-loading-icon [p-size]="mapSizeToIcon(size)"></po-loading-icon>
          </div>
        }
      </div>

      @if ((!this.label && poHelperComponent()) || (!this.label && poHelper.hideAdditionalHelp)) {
        <po-helper
          #helperEl
          class="po-field-helper-button"
          [p-size]="size"
          [p-helper]="poHelper.helperSettings"
          [p-disabled]="isDisabled"
          [p-append-in-body]="appendBox"
        >
        </po-helper>
      }
    </div>
  </div>

  @if (!readonly) {
    <po-field-container-bottom
      [p-append-in-body]="appendBox"
      [p-help]="help"
      [p-disabled]="isDisabled"
      [p-error-pattern]="getErrorPattern()"
      [p-error-limit]="errorLimit"
      [p-size]="size"
    ></po-field-container-bottom>
  }
</po-field-container>
`
    }]
  }], () => [], {
    selectElement: [{
      type: ViewChild,
      args: ["select", {
        read: ElementRef,
        static: true
      }]
    }],
    helperEl: [{
      type: ViewChild,
      args: ["helperEl", {
        read: PoHelperComponent,
        static: false
      }]
    }],
    blur: [{
      type: Output,
      args: ["p-blur"]
    }],
    changeModel: [{
      type: Output,
      args: ["p-change-model"]
    }],
    ngModelChange: [{
      type: Output,
      args: ["ngModelChange"]
    }],
    readonly: [{
      type: Input,
      args: [{
        alias: "p-readonly",
        transform: convertToBoolean
      }]
    }],
    compactLabel: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-compact-label",
        required: false
      }]
    }],
    placeholder: [{
      type: Input,
      args: ["p-placeholder"]
    }],
    options: [{
      type: Input,
      args: ["p-options"]
    }],
    fieldLabel: [{
      type: Input,
      args: ["p-field-label"]
    }],
    fieldValue: [{
      type: Input,
      args: ["p-field-value"]
    }],
    controlValueWithLabel: [{
      type: Input,
      args: [{
        alias: "p-control-value-with-label",
        transform: convertToBoolean
      }]
    }],
    loading: [{
      type: HostBinding,
      args: ["attr.p-loading"]
    }, {
      type: Input,
      args: [{
        alias: "p-loading",
        transform: convertToBoolean
      }]
    }],
    size: [{
      type: Input,
      args: ["p-size"]
    }, {
      type: HostBinding,
      args: ["attr.p-size"]
    }],
    poHelperComponent: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-helper",
        required: false
      }]
    }],
    labelTextWrap: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-label-text-wrap",
        required: false
      }]
    }],
    onThemeChange: [{
      type: HostListener,
      args: ["window:PoUiThemeChange"]
    }]
  });
})();
var PoTextareaBaseComponent = class _PoTextareaBaseComponent {
  cd;
  // Propriedade interna que define se o ícone de ajuda adicional terá cursor clicável (evento) ou padrão (tooltip).
  additionalHelpEventTrigger;
  /**
   *
   * @deprecated v23.x.x use `p-helper`
   *
   * @optional
   *
   * @description
   * Exibe um ícone de ajuda adicional, com o texto desta propriedade sendo passado para o popover do componente `po-helper`.
   * **Como boa prática, indica-se utilizar um texto com até 140 caracteres.**
   * > Requer um recuo mínimo de 8px se o componente estiver próximo à lateral da tela.
   *
   * > Essa propriedade está **depreciada** e será removida na versão `23.x.x`. Recomendamos utilizar a propriedade `p-helper` que oferece mais recursos e flexibilidade.
   */
  additionalHelpTooltip;
  /**
   * @optional
   *
   * @description
   *
   * Define que o popover (`p-helper` e/ou `p-error-limit`) será incluído no body da página e não
   * dentro do componente. Essa opção pode ser necessária em cenários com containers que possuem scroll ou overflow
   * escondido, garantindo o posicionamento correto do tooltip próximo ao elemento.
   *
   * > Quando utilizado com `p-helper`, leitores de tela como o NVDA podem não ler o conteúdo do popover.
   *
   * @default `false`
   */
  appendBox = false;
  /**
   * @optional
   *
   * @description
   *
   * Aplica foco no elemento ao ser iniciado.
   *
   * > Caso mais de um elemento seja configurado com essa propriedade, apenas o último elemento declarado com ela terá o foco.
   *
   * @default `false`
   */
  autoFocus = false;
  /**
   * @optional
   *
   * @description
   * Define se o título do campo será exibido de forma compacta.
   *
   * Quando habilitado (`true`), o modo compacto afeta o conjunto composto por:
   * - `po-label`
   * - `p-requirement (showRequired)`
   * - `po-helper`
   *
   * Ou seja, todos os elementos relacionados ao título do campo
   * (rótulo, indicador de obrigatoriedade e componente auxiliar) passam
   * a seguir o comportamento de layout compacto.
   *
   * Também é possível definir esse comportamento de forma global,
   * uma única vez, na folha de estilo geral da aplicação, por meio
   * da customização dos tokens CSS:
   *
   * - `--field-container-title-justify`
   * - `--field-container-title-flex`
   *
   * Exemplo:
   *
   * ```
   * :root {
   *   --field-container-title-justify: flex-start;
   *   --field-container-title-flex: 0 1 auto;
   * }
   * ```
   *
   * Dessa forma, o layout compacto passa a ser o padrão da aplicação,
   * sem a necessidade de definir a propriedade individualmente em cada campo.
   *
   * @default `false`
   */
  compactLabel = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "compactLabel"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-compact-label",
    transform: convertToBoolean
  }));
  /** Label do campo. */
  label;
  /** Texto de apoio do campo. */
  help;
  /** Nome e Id do componente. */
  name;
  /**
   * @optional
   *
   * @description
   *
   * Define se a indicação de campo opcional será exibida.
   *
   * > Não será exibida a indicação se:
   * - O campo conter `p-required`;
   * - Não possuir `p-help` e/ou `p-label`.
   *
   * @default `false`
   */
  optional;
  /**
   * @optional
   *
   * @description
   *
   * Exibe a mensagem setada se o campo estiver vazio e for requerido.
   *
   * > Necessário que a propriedade `p-required` esteja habilitada.
   *
   */
  fieldErrorMessage;
  /**
   * @optional
   *
   * @description
   *
   * Limita a exibição da mensagem de erro a duas linhas e exibe um tooltip com o texto completo.
   *
   * > Caso essa propriedade seja definida como `true`, a mensagem de erro será limitada a duas linhas
   * e um tooltip será exibido ao passar o mouse sobre a mensagem para mostrar o conteúdo completo.
   *
   * @default `false`
   */
  errorLimit = false;
  /**
   * @optional
   *
   * @description
   *
   * Define as opções do componente de ajuda (po-helper) que será exibido ao lado do label quando a propriedade `p-label` for definida, ou, ao lado do componente na ausência da propriedade `p-label`.
   * > Para mais informações acesse: https://po-ui.io/documentation/po-helper.
   *
   * > Ao configurar esta propriedade, o antigo ícone de ajuda adicional (`p-additional-help-tooltip` e `p-additional-help`) será ignorado.
   */
  poHelperComponent = input(void 0, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "poHelperComponent"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-helper"
  }));
  /**
   * @optional
   *
   * @description
   *
   * Habilita a quebra automática do texto da propriedade `p-label`. Quando `p-label-text-wrap` for verdadeiro, o texto que excede
   * o espaço disponível é transferido para a próxima linha em pontos apropriados para uma
   * leitura clara.
   *
   * @default `false`
   */
  labelTextWrap = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "labelTextWrap"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-label-text-wrap"
  }));
  /**
   *
   * @deprecated v23.x.x use `p-helper`
   *
   * @optional
   *
   * @description
   * Evento disparado ao clicar no ícone de ajuda adicional.
   *
   * > Essa propriedade está **depreciada** e será removida na versão `23.x.x`. Recomendamos utilizar a propriedade `p-helper` que oferece mais recursos e flexibilidade.
   */
  additionalHelp = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Evento disparado ao sair do campo.
   */
  blur = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Evento disparado ao entrar do campo.
   */
  enter = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Evento disparado ao alterar valor e deixar o campo.
   */
  change = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Evento disparado ao alterar valor do model.
   */
  changeModel = new EventEmitter();
  /**
   * @optional
   *
   * @description
   * Evento disparado quando uma tecla é pressionada enquanto o foco está no componente.
   * Retorna um objeto `KeyboardEvent` com informações sobre a tecla.
   */
  keydown = new EventEmitter();
  displayAdditionalHelp = false;
  _disabled = false;
  _loading = false;
  _maxlength;
  _minlength;
  _placeholder = "";
  _readonly = false;
  _required = false;
  _rows = 3;
  _size = void 0;
  _initialSize = void 0;
  modelLastUpdate;
  onChangePropagate = null;
  validatorChange;
  // eslint-disable-next-line
  onTouched = null;
  hasValidatorRequired = false;
  /** Placeholder, mensagem que aparecerá enquanto o campo não estiver preenchido. */
  set placeholder(value) {
    this._placeholder = value || "";
  }
  get placeholder() {
    return this._placeholder;
  }
  /**
   * @optional
   *
   * @description
   *
   * Indica que o campo será desabilitado.
   *
   * @default `false`
   */
  set disabled(disabled) {
    this._disabled = disabled;
  }
  get disabled() {
    return this._disabled;
  }
  /**
   * @optional
   *
   * @description
   * Exibe um ícone de carregamento no lado direito do campo para sinalizar que uma operação está em andamento.
   *
   * @default `false`
   */
  set loading(value) {
    this._loading = value;
    this.cd?.markForCheck();
  }
  get loading() {
    return this._loading;
  }
  get isDisabled() {
    return this.disabled || this.loading;
  }
  /**
   * @optional
   *
   * @description
   *
   * Indica que o campo será somente leitura.
   *
   * @default `false`
   */
  set readonly(readonly) {
    this._readonly = readonly;
  }
  get readonly() {
    return this._readonly;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define que o campo será obrigatório.
   * > Esta propriedade é desconsiderada quando o input está desabilitado `(p-disabled)`.
   *
   * @default `false`
   */
  set required(required) {
    this._required = required;
    this.validateModel();
  }
  get required() {
    return this._required;
  }
  /**
   * Define se a indicação de campo obrigatório será exibida.
   *
   * > Não será exibida a indicação se:
   * - Não possuir `p-help` e/ou `p-label`.
   */
  showRequired = false;
  /**
   * @optional
   *
   * @description
   *
   * Indica a quantidade mínima de caracteres que o campo aceita.
   */
  set minlength(minlength) {
    this._minlength = convertToInt(minlength);
    this.validateModel();
  }
  get minlength() {
    return this._minlength;
  }
  /**
   * @optional
   *
   * @description
   *
   * Indica a quantidade máxima de caracteres que o campo aceita.
   */
  set maxlength(maxlength) {
    this._maxlength = convertToInt(maxlength);
    this.validateModel();
  }
  get maxlength() {
    return this._maxlength;
  }
  /**
   * @optional
   *
   * @description
   *
   * Indica a quantidade de linhas que serão exibidas.
   *
   * @default `3`
   */
  set rows(value) {
    this._rows = isNaN(parseInt(value, 10)) || value < 3 ? 3 : parseInt(value, 10);
  }
  get rows() {
    return this._rows;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define o tamanho do componente:
   * - `small` (disponível apenas para acessibilidade AA)
   * - `medium`
   *
   * > Caso a acessibilidade AA não esteja configurada, o tamanho `medium` será mantido.
   * Para mais detalhes, consulte a documentação do [po-theme](https://po-ui.io/documentation/po-theme).
   *
   * @default `medium`
   */
  set size(value) {
    this._initialSize = value;
    this.applySizeBasedOnA11y();
  }
  get size() {
    return this._size ?? getDefaultSizeFn(PoFieldSize);
  }
  constructor(cd) {
    this.cd = cd;
  }
  onThemeChange() {
    this.applySizeBasedOnA11y();
    this.onAfterThemeChange();
  }
  onAfterThemeChange() {
  }
  callOnChange(value) {
    if (this.onChangePropagate) {
      this.onChangePropagate(value);
    }
    this.controlChangeModelEmitter(value);
  }
  controlChangeModelEmitter(value) {
    if (this.modelLastUpdate !== value) {
      this.changeModel.emit(value);
      this.modelLastUpdate = value;
    }
  }
  // Função implementada do ControlValueAccessor
  // Usada para interceptar os estados de habilitado via forms api
  setDisabledState(isDisabled) {
    this.disabled = isDisabled;
    this.cd.markForCheck();
  }
  // Funções `registerOnChange`, `registerOnTouched` e `registerOnValidatorChange` implementadas referentes ao ControlValueAccessor
  // usadas para interceptar as mudanças e não atualizar automaticamente o Model
  registerOnChange(func) {
    this.onChangePropagate = func;
  }
  registerOnTouched(func) {
    this.onTouched = func;
  }
  registerOnValidatorChange(func) {
    this.validatorChange = func;
  }
  validate(abstractControl) {
    if (!this.hasValidatorRequired && this.fieldErrorMessage && abstractControl.hasValidator(Validators.required)) {
      this.hasValidatorRequired = true;
    }
    if (PoValidators.requiredFailed(this.required, this.disabled, abstractControl.value)) {
      return {
        required: {
          valid: false
        }
      };
    }
    if (PoValidators.minlengpoailed(this.minlength, abstractControl.value)) {
      return {
        minlength: {
          valid: false
        }
      };
    }
    if (PoValidators.maxlengpoailed(this.maxlength, abstractControl.value)) {
      return {
        maxlength: {
          valid: false
        }
      };
    }
  }
  // Função implementada do ControlValueAccessor
  writeValue(value) {
    this.writeValueModel(value);
    this.cd.markForCheck();
  }
  //Transforma o tamanho do input para o tamanho do ícone de loading correspondente
  mapSizeToIcon(size) {
    return mapInputSizeToLoadingIcon(size);
  }
  validateModel() {
    if (this.validatorChange) {
      this.validatorChange();
    }
  }
  applySizeBasedOnA11y() {
    const size = validateSizeFn(this._initialSize, PoFieldSize);
    this._size = size;
  }
  static ɵfac = function PoTextareaBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoTextareaBaseComponent)(ɵɵdirectiveInject(ChangeDetectorRef));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoTextareaBaseComponent,
    hostVars: 1,
    hostBindings: function PoTextareaBaseComponent_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("PoUiThemeChange", function PoTextareaBaseComponent_PoUiThemeChange_HostBindingHandler() {
          return ctx.onThemeChange();
        }, ɵɵresolveWindow);
      }
      if (rf & 2) {
        ɵɵattribute("p-size", ctx.size);
      }
    },
    inputs: {
      additionalHelpEventTrigger: "additionalHelpEventTrigger",
      additionalHelpTooltip: [0, "p-additional-help-tooltip", "additionalHelpTooltip"],
      appendBox: [2, "p-append-in-body", "appendBox", convertToBoolean],
      autoFocus: [2, "p-auto-focus", "autoFocus", convertToBoolean],
      compactLabel: [1, "p-compact-label", "compactLabel"],
      label: [0, "p-label", "label"],
      help: [0, "p-help", "help"],
      name: "name",
      optional: [0, "p-optional", "optional"],
      fieldErrorMessage: [0, "p-field-error-message", "fieldErrorMessage"],
      errorLimit: [0, "p-error-limit", "errorLimit"],
      poHelperComponent: [1, "p-helper", "poHelperComponent"],
      labelTextWrap: [1, "p-label-text-wrap", "labelTextWrap"],
      placeholder: [0, "p-placeholder", "placeholder"],
      disabled: [2, "p-disabled", "disabled", convertToBoolean],
      loading: [2, "p-loading", "loading", convertToBoolean],
      readonly: [2, "p-readonly", "readonly", convertToBoolean],
      required: [2, "p-required", "required", convertToBoolean],
      showRequired: [0, "p-show-required", "showRequired"],
      minlength: [0, "p-minlength", "minlength"],
      maxlength: [0, "p-maxlength", "maxlength"],
      rows: [0, "p-rows", "rows"],
      size: [0, "p-size", "size"]
    },
    outputs: {
      additionalHelp: "p-additional-help",
      blur: "p-blur",
      enter: "p-enter",
      change: "p-change",
      changeModel: "p-change-model",
      keydown: "p-keydown"
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoTextareaBaseComponent, [{
    type: Directive
  }], () => [{
    type: ChangeDetectorRef
  }], {
    additionalHelpEventTrigger: [{
      type: Input
    }],
    additionalHelpTooltip: [{
      type: Input,
      args: ["p-additional-help-tooltip"]
    }],
    appendBox: [{
      type: Input,
      args: [{
        alias: "p-append-in-body",
        transform: convertToBoolean
      }]
    }],
    autoFocus: [{
      type: Input,
      args: [{
        alias: "p-auto-focus",
        transform: convertToBoolean
      }]
    }],
    compactLabel: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-compact-label",
        required: false
      }]
    }],
    label: [{
      type: Input,
      args: ["p-label"]
    }],
    help: [{
      type: Input,
      args: ["p-help"]
    }],
    name: [{
      type: Input,
      args: ["name"]
    }],
    optional: [{
      type: Input,
      args: ["p-optional"]
    }],
    fieldErrorMessage: [{
      type: Input,
      args: ["p-field-error-message"]
    }],
    errorLimit: [{
      type: Input,
      args: ["p-error-limit"]
    }],
    poHelperComponent: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-helper",
        required: false
      }]
    }],
    labelTextWrap: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-label-text-wrap",
        required: false
      }]
    }],
    additionalHelp: [{
      type: Output,
      args: ["p-additional-help"]
    }],
    blur: [{
      type: Output,
      args: ["p-blur"]
    }],
    enter: [{
      type: Output,
      args: ["p-enter"]
    }],
    change: [{
      type: Output,
      args: ["p-change"]
    }],
    changeModel: [{
      type: Output,
      args: ["p-change-model"]
    }],
    keydown: [{
      type: Output,
      args: ["p-keydown"]
    }],
    placeholder: [{
      type: Input,
      args: ["p-placeholder"]
    }],
    disabled: [{
      type: Input,
      args: [{
        alias: "p-disabled",
        transform: convertToBoolean
      }]
    }],
    loading: [{
      type: Input,
      args: [{
        alias: "p-loading",
        transform: convertToBoolean
      }]
    }],
    readonly: [{
      type: Input,
      args: [{
        alias: "p-readonly",
        transform: convertToBoolean
      }]
    }],
    required: [{
      type: Input,
      args: [{
        alias: "p-required",
        transform: convertToBoolean
      }]
    }],
    showRequired: [{
      type: Input,
      args: ["p-show-required"]
    }],
    minlength: [{
      type: Input,
      args: ["p-minlength"]
    }],
    maxlength: [{
      type: Input,
      args: ["p-maxlength"]
    }],
    rows: [{
      type: Input,
      args: ["p-rows"]
    }],
    size: [{
      type: Input,
      args: ["p-size"]
    }, {
      type: HostBinding,
      args: ["attr.p-size"]
    }],
    onThemeChange: [{
      type: HostListener,
      args: ["window:PoUiThemeChange"]
    }]
  });
})();
var PoTextareaComponent = class _PoTextareaComponent extends PoTextareaBaseComponent {
  el = inject(ElementRef);
  inputEl;
  textareaBodyEl;
  helperEl;
  id = `po-textarea[${uuid()}]`;
  valueBeforeChange;
  fireChange = false;
  hasScroll = false;
  hasValue = false;
  resizeObserver;
  constructor() {
    const cd = inject(ChangeDetectorRef);
    super(cd);
  }
  emitAdditionalHelp() {
    if (this.label && this.isAdditionalHelpEventTriggered()) {
      this.additionalHelp.emit();
    }
  }
  /**
   * Função que atribui foco ao componente.
   *
   * Para utilizá-la é necessário ter a instância do componente no DOM, podendo ser utilizado o ViewChild da seguinte forma:
   *
   * ```
   * import { PoTextareaComponent } from '@po-ui/ng-components';
   *
   * ...
   *
   * @ViewChild(PoTextareaComponent, { static: true }) textarea: PoTextareaComponent;
   *
   * focusTextarea() {
   *   this.textarea.focus();
   * }
   * ```
   */
  focus() {
    if (!this.disabled) {
      this.inputEl.nativeElement.focus();
    }
  }
  ngAfterViewInit() {
    if (this.autoFocus) {
      this.focus();
    }
    this.initResizeObserver();
    window.addEventListener("resize", this.onWindowResize);
  }
  onAfterThemeChange() {
    requestAnimationFrame(() => {
      this.syncContainerWidth();
      this.checkScrollState();
    });
  }
  ngOnChanges(changes) {
    if (changes.label) {
      this.displayAdditionalHelp = false;
    }
    if (changes.loading) {
      requestAnimationFrame(() => {
        this.syncContainerWidth();
        this.checkScrollState();
      });
    }
  }
  ngOnDestroy() {
    this.resizeObserver?.disconnect();
    window.removeEventListener("resize", this.onWindowResize);
  }
  getAdditionalHelpTooltip() {
    return this.isAdditionalHelpEventTriggered() ? null : this.additionalHelpTooltip;
  }
  getErrorPattern() {
    return this.fieldErrorMessage && this.hasInvalidClass() ? this.fieldErrorMessage : "";
  }
  hasInvalidClass() {
    return this.el.nativeElement.classList.contains("ng-invalid") && this.el.nativeElement.classList.contains("ng-dirty") && !this.inputEl.nativeElement.value && (this.required || this.hasValidatorRequired);
  }
  writeValueModel(value) {
    if (this.inputEl) {
      if (!value) {
        this.inputEl.nativeElement.value = "";
      } else {
        this.inputEl.nativeElement.value = value;
      }
      this.hasValue = !!value;
      this.checkScrollState();
    }
    if (value) {
      this.change.emit(value);
    }
  }
  validMaxLength(maxlength, value) {
    return maxlength && value.length > maxlength ? value.toString().substring(0, maxlength) : value;
  }
  eventOnInput(event) {
    const value = this.validMaxLength(this.maxlength, event.target.value);
    this.callOnChange(value);
    this.inputEl.nativeElement.value = value;
    this.hasValue = !!value;
    this.checkScrollState();
  }
  eventOnFocus() {
    this.valueBeforeChange = this.inputEl.nativeElement.value;
    this.enter.emit();
  }
  eventOnBlur() {
    this.onTouched?.();
    this.blur.emit();
    this.controlChangeEmitter();
  }
  controlChangeEmitter() {
    const elementValue = this.inputEl.nativeElement.value;
    if (elementValue !== this.valueBeforeChange) {
      this.change.emit(elementValue);
    }
  }
  onKeyDown(event) {
    const isFieldFocused = document.activeElement === this.inputEl.nativeElement;
    if (isFieldFocused) {
      this.keydown.emit(event);
    }
  }
  /**
   * Método que exibe `p-helper` ou executa a ação definida em `p-helper{eventOnClick}` ou em `p-additionalHelp`.
   * Para isso, será necessário configurar uma tecla de atalho utilizando o evento `p-keydown`.
   *
   * > Exibe ou oculta o conteúdo do componente `po-helper` quando o componente estiver com foco.
   *
   * ```
   * //Exemplo com p-label e p-helper
   * <po-textarea
   *  #textarea
   *  ...
   *  p-label="Label do textarea"
   *  [p-helper]="helperOptions"
   *  (p-keydown)="onKeyDown($event, textarea)"
   * ></po-textarea>
   * ```
   * ```
   * ...
   * onKeyDown(event: KeyboardEvent, inp: PoTextareaComponent): void {
   *  if (event.code === 'F9') {
   *    inp.showAdditionalHelp();
   *  }
   * }
   * ```
   */
  showAdditionalHelp() {
    this.displayAdditionalHelp = !this.displayAdditionalHelp;
    const helper = this.poHelperComponent();
    const isHelpEvt = this.isAdditionalHelpEventTriggered();
    if (!this.label && (helper || this.additionalHelpTooltip || isHelpEvt)) {
      if (isHelpEvt) {
        this.additionalHelp.emit();
      }
      if (typeof helper !== "string" && typeof helper?.eventOnClick === "function") {
        helper.eventOnClick();
        return;
      }
      if (this.helperEl?.helperIsVisible()) {
        this.helperEl?.closeHelperPopover();
        return;
      }
      this.helperEl?.openHelperPopover();
      return;
    }
    return this.displayAdditionalHelp;
  }
  isAdditionalHelpEventTriggered() {
    return this.additionalHelpEventTrigger === "event" || this.additionalHelpEventTrigger === void 0 && this.additionalHelp.observed;
  }
  setHelper(label, additionalHelpTooltip) {
    return setHelperSettings(label, additionalHelpTooltip, this.poHelperComponent(), this.size, this.isAdditionalHelpEventTriggered() ? this.additionalHelp : void 0);
  }
  onWindowResize = () => {
    this.syncContainerWidth();
    this.checkScrollState();
  };
  checkScrollState() {
    const el = this.inputEl?.nativeElement;
    if (!el) return;
    this.hasScroll = el.scrollHeight > el.clientHeight;
    this.cd.markForCheck();
  }
  syncContainerWidth() {
    const el = this.inputEl?.nativeElement;
    const body = this.textareaBodyEl?.nativeElement;
    if (!el || !body) return;
    body.style.width = this.loading && el.style.width ? "fit-content" : "";
  }
  initResizeObserver() {
    const el = this.inputEl?.nativeElement;
    const body = this.textareaBodyEl?.nativeElement;
    if (!el || !body || typeof ResizeObserver === "undefined") return;
    this.resizeObserver = new ResizeObserver(() => {
      requestAnimationFrame(() => {
        if (this.loading) {
          body.style.width = el.style.width ? "fit-content" : "";
        }
        this.checkScrollState();
      });
    });
    this.resizeObserver.observe(el);
  }
  static ɵfac = function PoTextareaComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoTextareaComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoTextareaComponent,
    selectors: [["po-textarea"]],
    viewQuery: function PoTextareaComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c49, 7, ElementRef)(_c143, 7, ElementRef)(_c17, 5, PoHelperComponent);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.inputEl = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.textareaBodyEl = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.helperEl = _t.first);
      }
    },
    standalone: false,
    features: [ɵɵProvidersFeature([{
      provide: NG_VALUE_ACCESSOR,
      useExisting: forwardRef(() => _PoTextareaComponent),
      multi: true
    }, {
      provide: NG_VALIDATORS,
      useExisting: forwardRef(() => _PoTextareaComponent),
      multi: true
    }]), ɵɵInheritDefinitionFeature, ɵɵNgOnChangesFeature],
    decls: 9,
    vars: 27,
    consts: [["textareaBody", ""], ["inp", ""], [3, "p-disabled", "p-id", "p-label", "p-optional", "p-required", "p-show-required", "p-helper", "p-show-helper", "p-size", "p-text-wrap", "p-compact-label"], [1, "po-field-container-content"], [1, "po-field-container-textarea-body"], [1, "po-textarea", 3, "blur", "focus", "input", "keydown", "disabled", "id", "placeholder", "readonly", "required", "rows"], [1, "po-field-icon-container-right", "po-textarea-loading-container"], [3, "p-append-in-body", "p-help", "p-disabled", "p-error-pattern", "p-error-limit", "p-size", "p-helper", "p-show-helper"], [3, "p-size"]],
    template: function PoTextareaComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵdeclareLet(0);
        ɵɵelementStart(1, "po-field-container", 2)(2, "div", 3)(3, "div", 4, 0)(5, "textarea", 5, 1);
        ɵɵlistener("blur", function PoTextareaComponent_Template_textarea_blur_5_listener() {
          return ctx.eventOnBlur();
        })("focus", function PoTextareaComponent_Template_textarea_focus_5_listener() {
          return ctx.eventOnFocus();
        })("input", function PoTextareaComponent_Template_textarea_input_5_listener($event) {
          return ctx.eventOnInput($event);
        })("keydown", function PoTextareaComponent_Template_textarea_keydown_5_listener($event) {
          return ctx.onKeyDown($event);
        });
        ɵɵelementEnd();
        ɵɵconditionalCreate(7, PoTextareaComponent_Conditional_7_Template, 2, 1, "div", 6);
        ɵɵelementEnd()();
        ɵɵconditionalCreate(8, PoTextareaComponent_Conditional_8_Template, 1, 8, "po-field-container-bottom", 7);
        ɵɵelementEnd();
      }
      if (rf & 2) {
        const poHelper_r3 = ɵɵstoreLet(ctx.setHelper(ctx.label, ctx.additionalHelpTooltip));
        ɵɵadvance();
        ɵɵproperty("p-disabled", ctx.isDisabled)("p-id", ctx.id)("p-label", ctx.label)("p-optional", ctx.optional)("p-required", ctx.required)("p-show-required", ctx.showRequired)("p-helper", ctx.label ? poHelper_r3.helperSettings : void 0)("p-show-helper", ctx.label ? ctx.displayAdditionalHelp : void 0)("p-size", ctx.size)("p-text-wrap", ctx.labelTextWrap())("p-compact-label", ctx.compactLabel());
        ɵɵadvance(4);
        ɵɵclassProp("has-scroll", ctx.hasScroll)("has-value", ctx.hasValue)("has-loading", ctx.loading);
        ɵɵproperty("disabled", ctx.isDisabled)("id", ctx.id)("placeholder", ctx.isDisabled ? "" : ctx.placeholder)("readonly", ctx.readonly)("required", ctx.required)("rows", ctx.rows);
        ɵɵattribute("name", ctx.name);
        ɵɵadvance(2);
        ɵɵconditional(ctx.loading ? 7 : -1);
        ɵɵadvance();
        ɵɵconditional(!ctx.readonly ? 8 : -1);
      }
    },
    dependencies: [PoFieldContainerBottomComponent, PoFieldContainerComponent, PoLoadingIconComponent],
    encapsulation: 2,
    changeDetection: 0
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoTextareaComponent, [{
    type: Component,
    args: [{
      selector: "po-textarea",
      changeDetection: ChangeDetectionStrategy.OnPush,
      providers: [{
        provide: NG_VALUE_ACCESSOR,
        useExisting: forwardRef(() => PoTextareaComponent),
        multi: true
      }, {
        provide: NG_VALIDATORS,
        useExisting: forwardRef(() => PoTextareaComponent),
        multi: true
      }],
      standalone: false,
      template: `@let poHelper = setHelper(label, additionalHelpTooltip);

<po-field-container
  [p-disabled]="isDisabled"
  [p-id]="id"
  [p-label]="label"
  [p-optional]="optional"
  [p-required]="required"
  [p-show-required]="showRequired"
  [p-helper]="label ? poHelper.helperSettings : undefined"
  [p-show-helper]="label ? displayAdditionalHelp : undefined"
  [p-size]="size"
  [p-text-wrap]="labelTextWrap()"
  [p-compact-label]="compactLabel()"
>
  <div class="po-field-container-content">
    <div #textareaBody class="po-field-container-textarea-body">
      <textarea
        #inp
        class="po-textarea"
        [class.has-scroll]="hasScroll"
        [class.has-value]="hasValue"
        [class.has-loading]="loading"
        (blur)="eventOnBlur()"
        (focus)="eventOnFocus()"
        (input)="eventOnInput($event)"
        (keydown)="onKeyDown($event)"
        [attr.name]="name"
        [disabled]="isDisabled"
        [id]="id"
        [placeholder]="isDisabled ? '' : placeholder"
        [readonly]="readonly"
        [required]="required"
        [rows]="rows"
      ></textarea>
      @if (loading) {
        <div class="po-field-icon-container-right po-textarea-loading-container">
          <po-loading-icon [p-size]="mapSizeToIcon(size)"></po-loading-icon>
        </div>
      }
    </div>
  </div>
  @if (!readonly) {
    <po-field-container-bottom
      [p-append-in-body]="appendBox"
      [p-help]="help"
      [p-disabled]="isDisabled"
      [p-error-pattern]="getErrorPattern()"
      [p-error-limit]="errorLimit"
      [p-size]="size"
      [p-helper]="!label ? poHelper.helperSettings : undefined"
      [p-show-helper]="!label ? displayAdditionalHelp : undefined"
    ></po-field-container-bottom>
  }
</po-field-container>
`
    }]
  }], () => [], {
    inputEl: [{
      type: ViewChild,
      args: ["inp", {
        read: ElementRef,
        static: true
      }]
    }],
    textareaBodyEl: [{
      type: ViewChild,
      args: ["textareaBody", {
        read: ElementRef,
        static: true
      }]
    }],
    helperEl: [{
      type: ViewChild,
      args: ["helperEl", {
        read: PoHelperComponent,
        static: false
      }]
    }]
  });
})();
var PoSearchModule = class _PoSearchModule {
  static ɵfac = function PoSearchModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoSearchModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoSearchModule,
    declarations: [PoSearchComponent],
    imports: [CommonModule, PoCleanModule, PoIconModule, PoLoadingModule, PoAccordionModule, FormsModule, PoListBoxModule, PoDropdownModule],
    exports: [PoSearchComponent]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [CommonModule, PoCleanModule, PoIconModule, PoLoadingModule, PoAccordionModule, FormsModule, PoListBoxModule, PoDropdownModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoSearchModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, PoCleanModule, PoIconModule, PoLoadingModule, PoAccordionModule, FormsModule, PoListBoxModule, PoDropdownModule],
      declarations: [PoSearchComponent],
      exports: [PoSearchComponent]
    }]
  }], null, null);
})();
var PoTableModule = class _PoTableModule {
  static ɵfac = function PoTableModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoTableModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoTableModule,
    declarations: [PoTableComponent, PoTableColumnIconComponent, PoTableColumnLabelComponent, PoTableColumnLinkComponent, PoTableColumnManagerComponent, PoTableListManagerComponent, PoTableDetailComponent, PoTableIconComponent, PoTableRowTemplateDirective, PoTableShowSubtitleComponent, PoTableSubtitleCircleComponent, PoTableSubtitleFooterComponent, PoTableCellTemplateDirective, PoTableColumnTemplateDirective, PoTableColumnFrozenDirective],
    imports: [CommonModule, FormsModule, ScrollingModule, DragDropModule, RouterModule, PoButtonModule, PoCheckboxGroupModule, PoContainerModule, PoLoadingModule, PoModalModule, PoPopoverModule, PoPopupModule, PoTagModule, PoTimeModule, PoTooltipModule, PoIconModule, PoCheckboxModule, PoRadioModule, PoLinkModule, PoPageSlideModule, PoSearchAiModule, PoSwitchModule, PoSearchModule],
    exports: [PoTableComponent, PoTableRowTemplateDirective, PoTableCellTemplateDirective, PoTableColumnTemplateDirective]
  });
  static ɵinj = ɵɵdefineInjector({
    providers: [DecimalPipe],
    imports: [CommonModule, FormsModule, ScrollingModule, DragDropModule, RouterModule, PoButtonModule, PoCheckboxGroupModule, PoContainerModule, PoLoadingModule, PoModalModule, PoPopoverModule, PoPopupModule, PoTagModule, PoTimeModule, PoTooltipModule, PoIconModule, PoCheckboxModule, PoRadioModule, PoLinkModule, PoPageSlideModule, PoSearchAiModule, PoSwitchModule, PoSearchModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoTableModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, FormsModule, ScrollingModule, DragDropModule, RouterModule, PoButtonModule, PoCheckboxGroupModule, PoContainerModule, PoLoadingModule, PoModalModule, PoPopoverModule, PoPopupModule, PoTagModule, PoTimeModule, PoTooltipModule, PoIconModule, PoCheckboxModule, PoRadioModule, PoLinkModule, PoPageSlideModule, PoSearchAiModule, PoSwitchModule, PoSearchModule],
      declarations: [PoTableComponent, PoTableColumnIconComponent, PoTableColumnLabelComponent, PoTableColumnLinkComponent, PoTableColumnManagerComponent, PoTableListManagerComponent, PoTableDetailComponent, PoTableIconComponent, PoTableRowTemplateDirective, PoTableShowSubtitleComponent, PoTableSubtitleCircleComponent, PoTableSubtitleFooterComponent, PoTableCellTemplateDirective, PoTableColumnTemplateDirective, PoTableColumnFrozenDirective],
      exports: [PoTableComponent, PoTableRowTemplateDirective, PoTableCellTemplateDirective, PoTableColumnTemplateDirective],
      providers: [DecimalPipe]
    }]
  }], null, null);
})();
var PoFieldModule = class _PoFieldModule {
  static ɵfac = function PoFieldModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoFieldModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoFieldModule,
    declarations: [PoDatepickerRangeComponent, PoDecimalComponent, PoEmailComponent, PoInputComponent, PoLoginComponent, PoLookupComponent, PoLookupModalComponent, PoMultiselectComponent, PoMultiselectDropdownComponent, PoMultiselectOptionTemplateDirective, PoNumberComponent, PoPasswordComponent, PoRichTextBodyComponent, PoRichTextComponent, PoRichTextImageModalComponent, PoRichTextLinkModalComponent, PoRichTextToolbarComponent, PoSelectComponent, PoTextareaComponent, PoUploadComponent, PoUploadDragDropAreaComponent, PoUploadDragDropAreaOverlayComponent, PoUploadDragDropComponent, PoUploadDragDropDirective, PoUploadFileRestrictionsComponent, PoUrlComponent],
    imports: [CommonModule, FormsModule, FormsModule, OverlayModule, PoButtonGroupModule, PoButtonModule, PoCalendarModule, PoCheckboxGroupModule, PoCheckboxModule, PoCleanModule, PoComboModule, PoContainerModule, PoDatepickerModule, PoDatetimepickerModule, PoDisclaimerGroupModule, PoDisclaimerModule, PoFieldContainerModule, PoHelperModule, PoIconModule, PoLabelModule, PoLinkModule, PoListBoxModule, PoLoadingModule, PoModalModule, PoProgressModule, PoRadioGroupModule, PoRadioModule, PoServicesModule, PoSearchAiModule, PoSwitchModule, PoTableModule, PoTagModule, PoTimepickerModule, PoTooltipModule],
    exports: [PoCheckboxGroupModule, PoCheckboxModule, PoCleanModule, PoComboModule, PoDatepickerModule, PoDatepickerRangeComponent, PoDatetimepickerModule, PoDecimalComponent, PoEmailComponent, PoFieldContainerModule, PoInputComponent, PoSearchAiModule, PoLabelModule, PoLinkModule, PoLoginComponent, PoLookupComponent, PoLookupModalComponent, PoMultiselectComponent, PoMultiselectOptionTemplateDirective, PoNumberComponent, PoPasswordComponent, PoRadioGroupModule, PoRadioModule, PoRichTextComponent, PoSelectComponent, PoSwitchModule, PoTagModule, PoTextareaComponent, PoTimepickerModule, PoUploadComponent, PoUrlComponent]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [CommonModule, FormsModule, FormsModule.withConfig({
      callSetDisabledState: "whenDisabledForLegacyCode"
    }), OverlayModule, PoButtonGroupModule, PoButtonModule, PoCalendarModule, PoCheckboxGroupModule, PoCheckboxModule, PoCleanModule, PoComboModule, PoContainerModule, PoDatepickerModule, PoDatetimepickerModule, PoDisclaimerGroupModule, PoDisclaimerModule, PoFieldContainerModule, PoHelperModule, PoIconModule, PoLabelModule, PoLinkModule, PoListBoxModule, PoLoadingModule, PoModalModule, PoProgressModule, PoRadioGroupModule, PoRadioModule, PoServicesModule, PoSearchAiModule, PoSwitchModule, PoTableModule, PoTagModule, PoTimepickerModule, PoTooltipModule, PoCheckboxGroupModule, PoCheckboxModule, PoCleanModule, PoComboModule, PoDatepickerModule, PoDatetimepickerModule, PoFieldContainerModule, PoSearchAiModule, PoLabelModule, PoLinkModule, PoRadioGroupModule, PoRadioModule, PoSwitchModule, PoTagModule, PoTimepickerModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoFieldModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, FormsModule, FormsModule.withConfig({
        callSetDisabledState: "whenDisabledForLegacyCode"
      }), OverlayModule, PoButtonGroupModule, PoButtonModule, PoCalendarModule, PoCheckboxGroupModule, PoCheckboxModule, PoCleanModule, PoComboModule, PoContainerModule, PoDatepickerModule, PoDatetimepickerModule, PoDisclaimerGroupModule, PoDisclaimerModule, PoFieldContainerModule, PoHelperModule, PoIconModule, PoLabelModule, PoLinkModule, PoListBoxModule, PoLoadingModule, PoModalModule, PoProgressModule, PoRadioGroupModule, PoRadioModule, PoServicesModule, PoSearchAiModule, PoSwitchModule, PoTableModule, PoTagModule, PoTimepickerModule, PoTooltipModule],
      exports: [PoCheckboxGroupModule, PoCheckboxModule, PoCleanModule, PoComboModule, PoDatepickerModule, PoDatepickerRangeComponent, PoDatetimepickerModule, PoDecimalComponent, PoEmailComponent, PoFieldContainerModule, PoInputComponent, PoSearchAiModule, PoLabelModule, PoLinkModule, PoLoginComponent, PoLookupComponent, PoLookupModalComponent, PoMultiselectComponent, PoMultiselectOptionTemplateDirective, PoNumberComponent, PoPasswordComponent, PoRadioGroupModule, PoRadioModule, PoRichTextComponent, PoSelectComponent, PoSwitchModule, PoTagModule, PoTextareaComponent, PoTimepickerModule, PoUploadComponent, PoUrlComponent],
      declarations: [PoDatepickerRangeComponent, PoDecimalComponent, PoEmailComponent, PoInputComponent, PoLoginComponent, PoLookupComponent, PoLookupModalComponent, PoMultiselectComponent, PoMultiselectDropdownComponent, PoMultiselectOptionTemplateDirective, PoNumberComponent, PoPasswordComponent, PoRichTextBodyComponent, PoRichTextComponent, PoRichTextImageModalComponent, PoRichTextLinkModalComponent, PoRichTextToolbarComponent, PoSelectComponent, PoTextareaComponent, PoUploadComponent, PoUploadDragDropAreaComponent, PoUploadDragDropAreaOverlayComponent, PoUploadDragDropComponent, PoUploadDragDropDirective, PoUploadFileRestrictionsComponent, PoUrlComponent],
      providers: []
    }]
  }], null, null);
})();
var PoInfoOrientation;
(function(PoInfoOrientation2) {
  PoInfoOrientation2["Horizontal"] = "horizontal";
  PoInfoOrientation2["Vertical"] = "vertical";
})(PoInfoOrientation || (PoInfoOrientation = {}));
var poInfoOrientationDefault = PoInfoOrientation.Vertical;
var PoInfoBaseComponent = class _PoInfoBaseComponent {
  /** Valor do rótulo a ser exibido. */
  label;
  /**
   * Ao informar uma URL, o conteúdo será exibido na forma de um *link* e ao ser clicado será redirecionado para a URL informada.
   *
   * > Caso informar `http://` será aberto uma nova aba.
   * Caso informar um caminho relativo, exemplo: `/customers`, será aberto na aba atual.
   *
   */
  url;
  /** Valor do conteúdo a ser exibido. */
  value;
  poInfoOrientation = PoInfoOrientation;
  _labelSize;
  _orientation = poInfoOrientationDefault;
  _size = void 0;
  _initialSize = void 0;
  /**
   * @optional
   *
   * @description
   *
   * Quantidade de [colunas](/guides/grid-system) usadas para a exibição da `p-label` quando o componente for
   * utilizado na orientação horizontal.
   *
   * Valores válidos:
   *  - `[1 .. 11]`
   *
   * > A propriedade `p-value` recebe o número de colunas restantes, por exemplo, se definido 3 colunas a mesma assume 9 colunas.
   */
  set labelSize(value) {
    if (isNaN(parseInt(value, 10))) {
      this._labelSize = void 0;
    } else {
      value = parseInt(value, 10);
      this._labelSize = value < 1 || value > 11 ? void 0 : value;
    }
  }
  get labelSize() {
    return this._labelSize;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define o layout de exibição.
   *
   * > Quando definido na horizontal, pode-se utilizar a propriedade `p-label-size` para um maior controle das informações exibidas.
   *
   * @default `vertical`
   */
  set orientation(value) {
    this._orientation = Object.values(PoInfoOrientation).includes(value) ? value : poInfoOrientationDefault;
  }
  get orientation() {
    return this._orientation;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define o tamanho do componente entre `small` ou `medium`.
   *
   * > Caso a acessibilidade AA não esteja configurada, o tamanho `medium` será mantido.
   * Para mais detalhes, consulte a documentação do [po-theme](https://po-ui.io/documentation/po-theme).
   *
   * @default `medium`
   */
  set size(value) {
    this._initialSize = value;
    this.applySizeBasedOnA11y();
  }
  get size() {
    return this._size ?? getDefaultSizeFn(PoFieldSize);
  }
  onThemeChange() {
    this.applySizeBasedOnA11y();
  }
  applySizeBasedOnA11y() {
    const size = validateSizeFn(this._initialSize, PoFieldSize);
    this._size = size;
  }
  static ɵfac = function PoInfoBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoInfoBaseComponent)();
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoInfoBaseComponent,
    hostVars: 1,
    hostBindings: function PoInfoBaseComponent_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("PoUiThemeChange", function PoInfoBaseComponent_PoUiThemeChange_HostBindingHandler() {
          return ctx.onThemeChange();
        }, ɵɵresolveWindow);
      }
      if (rf & 2) {
        ɵɵattribute("p-size", ctx.size);
      }
    },
    inputs: {
      label: [0, "p-label", "label"],
      url: [0, "p-url", "url"],
      value: [0, "p-value", "value"],
      labelSize: [0, "p-label-size", "labelSize"],
      orientation: [0, "p-orientation", "orientation"],
      size: [0, "p-size", "size"]
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoInfoBaseComponent, [{
    type: Directive
  }], null, {
    label: [{
      type: Input,
      args: ["p-label"]
    }],
    url: [{
      type: Input,
      args: ["p-url"]
    }],
    value: [{
      type: Input,
      args: ["p-value"]
    }],
    labelSize: [{
      type: Input,
      args: ["p-label-size"]
    }],
    orientation: [{
      type: Input,
      args: ["p-orientation"]
    }],
    size: [{
      type: Input,
      args: ["p-size"]
    }, {
      type: HostBinding,
      args: ["attr.p-size"]
    }],
    onThemeChange: [{
      type: HostListener,
      args: ["window:PoUiThemeChange"]
    }]
  });
})();
var PoInfoComponent = class _PoInfoComponent extends PoInfoBaseComponent {
  get isExternalLink() {
    return PoUtils.isExternalLink(this.url);
  }
  static ɵfac = /* @__PURE__ */ (() => {
    let ɵPoInfoComponent_BaseFactory;
    return function PoInfoComponent_Factory(__ngFactoryType__) {
      return (ɵPoInfoComponent_BaseFactory || (ɵPoInfoComponent_BaseFactory = ɵɵgetInheritedFactory(_PoInfoComponent)))(__ngFactoryType__ || _PoInfoComponent);
    };
  })();
  static ɵcmp = ɵɵdefineComponent({
    type: _PoInfoComponent,
    selectors: [["po-info"]],
    standalone: false,
    features: [ɵɵInheritDefinitionFeature],
    decls: 7,
    vars: 14,
    consts: [[1, "po-info"], [1, "po-info-container-title", 3, "ngClass"], [1, "po-info-label"], [3, "ngClass"], [1, "po-info-value"], [3, "p-label", "p-url", "p-open-new-tab"]],
    template: function PoInfoComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "div", 0)(1, "div", 1)(2, "span", 2);
        ɵɵtext(3);
        ɵɵelementEnd()();
        ɵɵelementStart(4, "div", 3);
        ɵɵconditionalCreate(5, PoInfoComponent_Conditional_5_Template, 2, 1)(6, PoInfoComponent_Conditional_6_Template, 2, 1, "span", 4);
        ɵɵelementEnd()();
      }
      if (rf & 2) {
        ɵɵclassProp("po-row", ctx.orientation === ctx.poInfoOrientation.Horizontal && ctx.labelSize);
        ɵɵadvance();
        ɵɵclassProp("po-info-label-horizontal", ctx.orientation === ctx.poInfoOrientation.Horizontal)("po-text-nowrap", ctx.orientation === ctx.poInfoOrientation.Horizontal && !ctx.labelSize);
        ɵɵproperty("ngClass", ctx.labelSize && ctx.orientation === ctx.poInfoOrientation.Horizontal ? "po-sm-" + ctx.labelSize : "");
        ɵɵadvance(2);
        ɵɵtextInterpolate1(" ", ctx.orientation === ctx.poInfoOrientation.Horizontal ? ctx.label + ":" : ctx.label, " ");
        ɵɵadvance();
        ɵɵclassProp("po-info-container-content", ctx.orientation !== ctx.poInfoOrientation.Horizontal)("po-info-value-horizontal", ctx.orientation === ctx.poInfoOrientation.Horizontal);
        ɵɵproperty("ngClass", ctx.labelSize && ctx.orientation === ctx.poInfoOrientation.Horizontal ? "po-sm-" + (12 - ctx.labelSize) : "");
        ɵɵadvance();
        ɵɵconditional(ctx.url ? 5 : 6);
      }
    },
    dependencies: [NgClass, PoLinkComponent],
    encapsulation: 2,
    changeDetection: 0
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoInfoComponent, [{
    type: Component,
    args: [{
      selector: "po-info",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: `<div class="po-info" [class.po-row]="orientation === poInfoOrientation.Horizontal && labelSize">
  <div
    class="po-info-container-title"
    [ngClass]="labelSize && orientation === poInfoOrientation.Horizontal ? 'po-sm-' + labelSize : ''"
    [class.po-info-label-horizontal]="orientation === poInfoOrientation.Horizontal"
    [class.po-text-nowrap]="orientation === poInfoOrientation.Horizontal && !labelSize"
  >
    <span class="po-info-label">
      {{ orientation === poInfoOrientation.Horizontal ? label + ':' : label }}
    </span>
  </div>

  <div
    [ngClass]="labelSize && orientation === poInfoOrientation.Horizontal ? 'po-sm-' + (12 - labelSize) : ''"
    [class.po-info-container-content]="orientation !== poInfoOrientation.Horizontal"
    [class.po-info-value-horizontal]="orientation === poInfoOrientation.Horizontal"
  >
    @if (url) {
      @if (isExternalLink) {
        <po-link [p-label]="value" [p-url]="url" [p-open-new-tab]="true"></po-link>
      } @else {
        <po-link [p-label]="value" [p-url]="url" [p-open-new-tab]="false"></po-link>
      }
    } @else {
      <span class="po-info-value">{{ value }}</span>
    }
  </div>
</div>
`
    }]
  }], null, null);
})();
var PoInfoModule = class _PoInfoModule {
  static ɵfac = function PoInfoModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoInfoModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoInfoModule,
    declarations: [PoInfoComponent],
    imports: [CommonModule, RouterModule, PoLinkModule],
    exports: [PoInfoComponent]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [CommonModule, RouterModule, PoLinkModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoInfoModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, RouterModule, PoLinkModule],
      declarations: [PoInfoComponent],
      exports: [PoInfoComponent],
      providers: [],
      schemas: []
    }]
  }], null, null);
})();
var PoDynamicFormBaseComponent = class _PoDynamicFormBaseComponent {
  /**
   * @optional
   *
   * @description
   *
   * Nome da propriedade, atribuída ao `PoDynamicFormField.property`, que iniciará o campo com foco.
   */
  autoFocus;
  /**
   * @description
   *
   * Coleção de objetos que implementam a interface `PoDynamicFormField`, para definição dos campos que serão criados
   * dinamicamente.
   *
   * > Ex: `[ { property: 'name' } ]`
   *
   * Regras de tipagem e criação dos componentes:
   *
   * - Caso o *type* informado seja *boolean* o componente criado será o `po-switch`.
   * - Caso o *type* informado seja *currency* e não seja informado um *mask* ou *pattern* o componente criado será o `po-decimal`,
   * caso seja informado um *mask* ou *pattern* o componente criado será o `po-input`.
   * - Caso o *type* informado seja *number* e não seja informado um *mask* ou *pattern* o componente criado será o `po-number`, caso seja
   * informado um *mask* ou *pattern* o componente criado será o `po-input`.
   * - Caso a lista possua a propriedade `options` e a mesma possua até 3 itens o componente criado será o `po-radio-group`
   * ou `po-checkbox-group` se informar a propriedade `optionsMulti`.
   * - Caso a mesma possua 3 ou mais itens, será criado o componente `po-select` ou, `po-multiselect` se a propriedade `optionsMulti`
   * for verdadeira.
   * - Caso o *type* informado seja *date* ou *datetime* o componente criado será o `po-datepicker`.
   * - Caso seja informado a propriedade `optionsService` o componente criado será o `po-combo`.
   * - Caso o *type* informado seja *time* o componente criado será um `po-input` podendo receber um *mask* para formatar
   * o valor exibido, caso não seja informado um *mask* o componente será criado com a máscara '99:99' por padrão.
   * - Caso a lista possua a propriedade `rows` e esta seja definida com valor maior ou igual a 3 o componente criado será
   * o `po-textarea`, caso o valor da propriedade `rows` seja menor que 3 o componente criado será o `po-input`.
   * - Caso seja informada a propriedade `secret` o componente criado será o `po-password`.
   * - Caso o *type* informado seja *string* o componente criado será o `po-input`.
   * > Ao alterar o valor das `properties`, visibilidade e/ou agrupamentos via container, os `fields` que utilizam serviço podem refazer as chamadas para as API's.
   * @default `[]`
   */
  fields;
  /**
   * Objeto que será utilizado como valor para exibir as informações, será recuperado e preenchido através do atributo *property*
   * dos objetos contidos na propridade `p-fields`.
   *
   * Pode iniciar com valor ou apenas com um objeto vazio que será preenchido conforme descrito acima.
   *
   * > Ex: `{ name: 'po' }`
   */
  value;
  /**
   * @optional
   *
   * @description
   *
   * Na inicialização do componente será repassado o objeto de formulário utilizado no componente,
   * podendo ser utilizado para validações e/ou detecção de mudança dos valores.
   *
   * Portanto existem duas maneiras de recuperar o formulário,
   * através de *template reference* e através do *output*, veja os exemplos abaixo:
   *
   * > *template reference*
   *
   * ```html
   *  <po-dynamic-form #dynamicForm>
   *  </po-dynamic-form>
   *
   *  <po-button p-label="Adicionar" [p-disabled]="dynamicForm?.form.invalid">
   *  </po-button>
   *
   * ```
   *
   * > *Output*
   *
   * ```html
   *  ...
   *  <po-dynamic-form (p-form)="getForm($event)">
   *  </po-dynamic-form>
   *
   *  <po-button p-label="Adicionar" [p-disabled]="dynamicForm?.invalid">
   *  </po-button>
   *  ...
   *
   * ```
   *
   * ```ts
   *  ...
   *
   *  export class AppComponent {
   *
   *    dynamicForm: NgForm;
   *
   *    getForm(form: NgForm) {
   *      this.dynamicForm = form;
   *    }
   *
   *  }
   * ```
   *
   * > Caso a propriedade `p-group-form` for verdadeira não será repassado o formulário, pois o mesmo utilizará
   * o formulário pai.
   */
  formOutput = new EventEmitter();
  /**
   * Função ou serviço que será executado na inicialização do componente.
   *
   * A propriedade aceita os seguintes tipos:
   * - `string`: *Endpoint* usado pelo componente para requisição via `POST`.
   * - `function`: Método que será executado.
   *
   * Ao ser executado, irá receber como parâmetro o objeto informado no `p-value`.
   *
   * O retorno desta função deve ser do tipo [PoDynamicFormLoad](documentation/po-dynamic-form#po-dynamic-form-load),
   * onde o usuário poderá determinar as novas atualizações dos campos, valores e determinar o campo a ser focado.
   *
   * Por exemplo:
   *
   * ```
   * onLoadFields(): PoDynamicFormLoad {
   *
   *   return {
   *     value: { cpf: undefined },
   *     fields: [
   *       { property: 'cpf' }
   *     ],
   *     focus: 'cpf'
   *   };
   * }
   *
   * ```
   * Para referenciar a sua função utilize a propriedade `bind`, por exemplo:
   * ```
   *  [p-load]="onLoadFields.bind(this)"
   * ```
   */
  load;
  /**
   * Função ou serviço para validar as **mudanças do formulário**.
   *
   * A propriedade aceita os seguintes tipos:
   * - `string`: *Endpoint* usado pelo componente para requisição via `POST`.
   * - `function`: Método que será executado.
   *
   * Ao ser executado, irá receber como parâmetro um objeto com o nome da propriedade
   * alterada e os valores atualizados do formulario, conforme a interface `PoDynamicFormFieldChanged`
   *
   *
   * O retorno desta função deve ser do tipo [PoDynamicFormValidation](documentation/po-dynamic-form#po-dynamic-form-validation),
   * onde o usuário poderá determinar as novas atualizações dos campos.
   * Por exemplo:
   *
   * ```
   * onChangeFields(changeValue): PoDynamicFormValidation {
   *
   * if (changeValue.property === 'state') {
   *
   *   return {
   *     value: { city: undefined },
   *     fields: [
   *       { property: 'city', options: this.getCity(changeValue.value.state) }
   *     ],
   *     focus: 'city'
   *   };
   * }
   *
   * ```
   * Para referenciar a sua função utilize a propriedade `bind`, por exemplo:
   * ```
   *  [p-validate]="this.myFunction.bind(this)"
   * ```
   *
   * > Se houver uma lista de campos para validação definida em `p-validate-fields`, a propriedade `validate` só receberá o disparo para os campos equivalentes.
   */
  validate;
  /**
   * @optional
   *
   * @description
   *
   * Lista que define os campos que irão disparar o validate do form.
   */
  validateFields;
  _componentsSize = void 0;
  _initialComponentsSize = void 0;
  _groupForm = false;
  /**
   * @optional
   *
   * @description
   *
   * Define o tamanho dos componentes de formulário no template:
   * - `small`: aplica a medida small de cada componente (disponível apenas para acessibilidade AA).
   * - `medium`: aplica a medida medium de cada componente.
   *
   * > Caso a acessibilidade AA não esteja configurada, o tamanho `medium` será mantido.
   * Para mais detalhes, consulte a documentação do [po-theme](https://po-ui.io/documentation/po-theme).
   *
   * @default `medium`
   */
  set componentsSize(value) {
    this._initialComponentsSize = value;
    this.applySizeBasedOnA11y();
  }
  get componentsSize() {
    return this._componentsSize ?? getDefaultSizeFn(PoFieldSize);
  }
  /**
   * @optional
   *
   * @description
   * Ao informar esta propriedade, o componente passará a utilizar o formulário pai para criar os `FormControl`
   * e com isso é possível recuperar o valor do formulário e suas validações a partir do formulário pai.
   *
   * ```html
   * <form #parentForm="ngForm">
   *
   *   <po-dynamic-form p-group-form [p-fields]="fields"></po-dynamic-form>
   *
   *  <po-button p-label="Adicionar" [p-disabled]="parentForm.invalid"></po-button>
   * </form>
   * ```
   */
  set groupForm(value) {
    this._groupForm = value === "" ? true : convertToBoolean(value);
  }
  get groupForm() {
    return this._groupForm;
  }
  /**
   * @optional
   *
   * @description
   *
   * Ao informar esta propriedade, o componente passará a emitir o valor a cada caractere digitado.
   *
   * Pode ser aplicado nos seguintes componentes:
   * - po-input
   * - po-number
   * - po-decimal
   * - po-textarea
   * - po-password
   *
   * Deve informar os campos que deseja receber as emissões na propriedade `p-validate-fields`.
   *
   *
   */
  validateOnInput = false;
  onThemeChange() {
    this.applySizeBasedOnA11y();
  }
  applySizeBasedOnA11y() {
    const size = validateSizeFn(this._initialComponentsSize, PoFieldSize);
    this._componentsSize = size;
  }
  static ɵfac = function PoDynamicFormBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoDynamicFormBaseComponent)();
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoDynamicFormBaseComponent,
    hostVars: 1,
    hostBindings: function PoDynamicFormBaseComponent_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("PoUiThemeChange", function PoDynamicFormBaseComponent_PoUiThemeChange_HostBindingHandler() {
          return ctx.onThemeChange();
        }, ɵɵresolveWindow);
      }
      if (rf & 2) {
        ɵɵattribute("p-components-size", ctx.componentsSize);
      }
    },
    inputs: {
      autoFocus: [0, "p-auto-focus", "autoFocus"],
      fields: [0, "p-fields", "fields"],
      value: [0, "p-value", "value"],
      load: [0, "p-load", "load"],
      validate: [0, "p-validate", "validate"],
      validateFields: [0, "p-validate-fields", "validateFields"],
      componentsSize: [0, "p-components-size", "componentsSize"],
      groupForm: [0, "p-group-form", "groupForm"],
      validateOnInput: [2, "p-validate-on-input", "validateOnInput", convertToBoolean]
    },
    outputs: {
      formOutput: "p-form"
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoDynamicFormBaseComponent, [{
    type: Directive
  }], null, {
    autoFocus: [{
      type: Input,
      args: ["p-auto-focus"]
    }],
    fields: [{
      type: Input,
      args: ["p-fields"]
    }],
    value: [{
      type: Input,
      args: ["p-value"]
    }],
    formOutput: [{
      type: Output,
      args: ["p-form"]
    }],
    load: [{
      type: Input,
      args: ["p-load"]
    }],
    validate: [{
      type: Input,
      args: ["p-validate"]
    }],
    validateFields: [{
      type: Input,
      args: ["p-validate-fields"]
    }],
    componentsSize: [{
      type: Input,
      args: ["p-components-size"]
    }, {
      type: HostBinding,
      args: ["attr.p-components-size"]
    }],
    groupForm: [{
      type: Input,
      args: ["p-group-form"]
    }],
    validateOnInput: [{
      type: Input,
      args: [{
        alias: "p-validate-on-input",
        transform: convertToBoolean
      }]
    }],
    onThemeChange: [{
      type: HostListener,
      args: ["window:PoUiThemeChange"]
    }]
  });
})();
var PoDynamicFormOperation = class {
  http;
  constructor(http) {
    this.http = http;
  }
  execute(action, param) {
    return typeof action === "string" ? this.post(action, param) : of(action(param));
  }
  post(url, body) {
    return this.http.post(url, body);
  }
  setFormDefaultIfEmpty(validateFields) {
    return validateFields || {
      value: {},
      fields: [],
      focus: void 0
    };
  }
};
var PoDynamicFormLoadService = class _PoDynamicFormLoadService extends PoDynamicFormOperation {
  constructor() {
    const http = inject(HttpClient);
    super(http);
  }
  createAndUpdateFieldsForm(loadedFields = [], fields = []) {
    return [...loadedFields].reduce((updatedFields, field) => {
      const index = updatedFields.findIndex((updatedField) => updatedField.property === field.property);
      const hasProperty = index >= 0;
      if (hasProperty) {
        updatedFields[index] = __spreadValues(__spreadValues({}, fields[index]), field);
      } else {
        updatedFields.push(field);
      }
      return updatedFields;
    }, [...fields]);
  }
  executeLoad(load, value) {
    return this.execute(load, value).pipe(map((loadedFormdData) => this.setFormDefaultIfEmpty(loadedFormdData)));
  }
  static ɵfac = function PoDynamicFormLoadService_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoDynamicFormLoadService)();
  };
  static ɵprov = ɵɵdefineInjectable({
    token: _PoDynamicFormLoadService,
    factory: _PoDynamicFormLoadService.ɵfac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoDynamicFormLoadService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [], null);
})();
var PoDynamicFormValidationService = class _PoDynamicFormValidationService extends PoDynamicFormOperation {
  constructor() {
    const http = inject(HttpClient);
    super(http);
  }
  sendFieldChange(field, value) {
    const changedValue = {
      property: field.property,
      value
    };
    return this.execute(field.validate, changedValue).pipe(map((validateFields) => this.setFieldDefaultIfEmpty(validateFields)));
  }
  sendFormChange(validate, field, value) {
    const changedValue = {
      property: field.property,
      value
    };
    return this.execute(validate, changedValue).pipe(map((validateFields) => this.setFormDefaultIfEmpty(validateFields)));
  }
  updateFieldsForm(validatedFields = [], fields = []) {
    return [...validatedFields].reduce((updatedFields, validatedField) => {
      const index = updatedFields.findIndex((field) => field.property === validatedField.property);
      const hasProperty = index >= 0;
      if (hasProperty) {
        updatedFields[index] = __spreadValues(__spreadValues({}, fields[index]), validatedField);
      }
      return updatedFields;
    }, [...fields]);
  }
  setFieldDefaultIfEmpty(validateFields) {
    return validateFields || {
      field: {}
    };
  }
  static ɵfac = function PoDynamicFormValidationService_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoDynamicFormValidationService)();
  };
  static ɵprov = ɵɵdefineInjectable({
    token: _PoDynamicFormValidationService,
    factory: _PoDynamicFormValidationService.ɵfac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoDynamicFormValidationService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [], null);
})();
var PoDynamicFieldType;
(function(PoDynamicFieldType2) {
  PoDynamicFieldType2["Boolean"] = "boolean";
  PoDynamicFieldType2["Currency"] = "currency";
  PoDynamicFieldType2["Decimal"] = "decimal";
  PoDynamicFieldType2["Date"] = "date";
  PoDynamicFieldType2["DateTime"] = "datetime";
  PoDynamicFieldType2["Time"] = "time";
  PoDynamicFieldType2["Number"] = "number";
  PoDynamicFieldType2["String"] = "string";
  PoDynamicFieldType2["Upload"] = "upload";
})(PoDynamicFieldType || (PoDynamicFieldType = {}));
function getGridColumnsClasses(gridColumns, offsetColumns, grid, offset, pull) {
  const systemGrid = {
    gridSm: grid?.smGrid || gridColumns || 12,
    gridMd: grid?.mdGrid || gridColumns || 6,
    gridLg: grid?.lgGrid || gridColumns || 4,
    gridXl: grid?.xlGrid || gridColumns || 3,
    offsetSm: offset?.smOffset || offsetColumns || 0,
    offsetMd: offset?.mdOffset || offsetColumns || 0,
    offsetLg: offset?.lgOffset || offsetColumns || 0,
    offsetXl: offset?.xlOffset || offsetColumns || 0,
    pullSm: pull?.smPull || 0,
    pullMd: pull?.mdPull || 0,
    pullLg: pull?.lgPull || 0,
    pullXl: pull?.xlPull || 0
  };
  return `po-sm-${systemGrid.gridSm} po-offset-sm-${systemGrid.offsetSm} po-pull-sm-${systemGrid.pullSm} po-md-${systemGrid.gridMd} po-offset-md-${systemGrid.offsetMd} po-pull-md-${systemGrid.pullMd} po-lg-${systemGrid.gridLg} po-offset-lg-${systemGrid.offsetLg} po-pull-lg-${systemGrid.pullLg} po-xl-${systemGrid.gridXl} po-offset-xl-${systemGrid.offsetXl} po-pull-xl-${systemGrid.pullXl}`;
}
function isVisibleField(field) {
  const containsVisible = "visible" in field;
  return containsVisible ? field.visible : true;
}
var PoDynamicUtil = {
  isVisibleField,
  getGridColumnsClasses
};
var PoDynamicSharedBase = class _PoDynamicSharedBase {
  visibleFields = [];
  containerFields = [];
  hasContainers = false;
  ensureFieldHasContainer(fields) {
    if (fields && fields.some((field) => field?.container)) {
      const firstFieldIndex = fields.findIndex((field) => field.order === 1);
      if (firstFieldIndex !== -1 && !fields[firstFieldIndex].container) {
        fields[firstFieldIndex].container = "";
      }
    }
  }
  setContainerFields() {
    this.hasContainers = this.visibleFields && this.visibleFields.some((field) => field.container);
    if (this.hasContainers) {
      this.containerFields = this.groupFieldsIntoContainers(this.visibleFields);
    }
  }
  groupFieldsIntoContainers(fields) {
    const groups = [[]];
    return fields.reduce((acc, field) => {
      if (field.container && acc[acc.length - 1].length > 0) acc.push([]);
      acc[acc.length - 1].push(field);
      return acc;
    }, groups);
  }
  static ɵfac = function PoDynamicSharedBase_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoDynamicSharedBase)();
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoDynamicSharedBase
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoDynamicSharedBase, [{
    type: Directive
  }], null, null);
})();
var PoDynamicFormFieldsBaseComponent = class _PoDynamicFormFieldsBaseComponent extends PoDynamicSharedBase {
  titleCasePipe;
  autoFocus;
  disabledForm;
  validate;
  formValidate = new EventEmitter();
  fieldsChange = new EventEmitter();
  // Evento disparado se existir optionsService em visibleField. Necessário resgatar referência do objeto selecionado para quando se tratar de recebimento de opções via serviço.
  objectValue = new EventEmitter();
  validateOnInput;
  _componentsSize = void 0;
  _initialComponentsSize = void 0;
  _fields;
  _validateFields;
  _value = {};
  // Define o tamanho dos componentes de formulário.
  set componentsSize(value) {
    this._initialComponentsSize = value;
    this.applySizeBasedOnA11y();
  }
  get componentsSize() {
    return this._componentsSize ?? getDefaultSizeFn(PoFieldSize);
  }
  // array de objetos que implementam a interface PoDynamicFormField, que serão exibidos no componente.
  set fields(value) {
    this._fields = Array.isArray(value) ? [...value] : [];
  }
  get fields() {
    return this._fields;
  }
  // valor que será utilizado para iniciar valor no componente.
  set value(value) {
    this._value = value && isTypeof(value, "object") ? value : {};
  }
  get value() {
    return this._value;
  }
  set validateFields(value) {
    this._validateFields = Array.isArray(value) ? [...value] : [];
  }
  get validateFields() {
    return this._validateFields;
  }
  constructor(titleCasePipe) {
    super();
    this.titleCasePipe = titleCasePipe;
  }
  compareTo(value, compareTo) {
    return value === compareTo;
  }
  onThemeChange() {
    this.applySizeBasedOnA11y();
  }
  // retorna um array com os objetos configurados e visíveis.
  getVisibleFields() {
    const visibleFields = [];
    this.fields.forEach((field) => {
      if (this.existsProperty(visibleFields, field.property)) {
        this.printError(`"po-dynamic-form" property "${field.property}" está duplicado. Interface: PoDynamicFormField.`);
        return;
      }
      if (!field["property"]) {
        this.printError('"po-dynamic-form" É obrigatório ser especificado um property.');
        return;
      }
      if (PoDynamicUtil.isVisibleField(field)) {
        visibleFields.push(this.createField(field));
      }
    });
    const _visibleFields = sortFields(visibleFields);
    this.ensureFieldHasContainer(_visibleFields);
    return _visibleFields;
  }
  // converte um array em string para um array de objetos que contem label e value.
  convertOptions(options) {
    const everyOptionString = options.every((option) => typeof option === "string");
    if (everyOptionString) {
      return options.map((value) => ({
        label: value,
        value
      }));
    }
    return options;
  }
  // cria um novo objeto com as classes de grid system, com control (tipo do componente) e label default.
  createField(field) {
    const control = this.getComponentControl(field);
    const options = !!field.options ? this.convertOptions(field.options) : void 0;
    const focus = this.hasFocus(field);
    const type = field?.type ? field.type.toLocaleLowerCase() : "string";
    const componentClass = PoDynamicUtil.getGridColumnsClasses(field.gridColumns, field.offsetColumns, {
      smGrid: field.gridSmColumns,
      mdGrid: field.gridMdColumns,
      lgGrid: field.gridLgColumns,
      xlGrid: field.gridXlColumns
    }, {
      smOffset: field.offsetSmColumns,
      mdOffset: field.offsetMdColumns,
      lgOffset: field.offsetLgColumns,
      xlOffset: field.offsetXlColumns
    }, {
      smPull: field.gridSmPull,
      mdPull: field.gridMdPull,
      lgPull: field.gridLgPull,
      xlPull: field.gridXlPull
    });
    return __spreadProps(__spreadValues({
      label: this.titleCasePipe.transform(field.property),
      maskFormatModel: this.compareTo(type, PoDynamicFieldType.Time)
    }, field), {
      componentClass,
      control,
      focus,
      options
    });
  }
  existsProperty(fields, property) {
    return fields.some((field) => field.property === property);
  }
  // recupera o componente de acordo com algumas regras do field.
  getComponentControl(field = {}) {
    const type = this.getFieldType(field);
    const forcedComponent = this.getForcedComponent(field);
    if (forcedComponent) {
      return forcedComponent;
    }
    return this.getComponentByType(field, type);
  }
  getFieldType(field) {
    return field?.type ? field.type.toLocaleLowerCase() : "string";
  }
  getForcedComponent(field) {
    const {
      forceBooleanComponentType
    } = field;
    if (forceBooleanComponentType) {
      return forceBooleanComponentType;
    }
    if (this.verifyForceOptionComponent(field)) {
      return field.forceOptionsComponentType;
    }
    return void 0;
  }
  getComponentByType(field, type) {
    return this.getNumericComponent(field, type) || this.getSelectionComponent(field) || this.getDateComponent(field, type) || this.getTextComponent(field) || "input";
  }
  getNumericComponent(field, type) {
    if (this.isNumberType(field, type)) {
      return "number";
    }
    if (this.isCurrencyType(field, type) || type === PoDynamicFieldType.Decimal) {
      return "decimal";
    }
    return void 0;
  }
  getSelectionComponent(field) {
    if (this.isSelect(field)) {
      return "select";
    }
    if (this.isRadioGroup(field)) {
      return "radioGroup";
    }
    if (this.isCheckboxGroup(field)) {
      return "checkboxGroup";
    }
    if (this.isMultiselect(field)) {
      return "multiselect";
    }
    if (this.isCombo(field)) {
      return "combo";
    }
    if (this.isLookup(field)) {
      return "lookup";
    }
    return void 0;
  }
  getDateComponent(field, type) {
    if (this.compareTo(type, PoDynamicFieldType.Boolean)) {
      return "switch";
    }
    if (this.compareTo(type, PoDynamicFieldType.DateTime)) {
      return "datetimepicker";
    }
    if (this.compareTo(type, PoDynamicFieldType.Date)) {
      return field.range ? "datepickerrange" : "datepicker";
    }
    if (this.compareTo(type, PoDynamicFieldType.Time)) {
      return "timepicker";
    }
    return void 0;
  }
  getTextComponent(field) {
    if (this.isTextarea(field)) {
      return "textarea";
    }
    if (this.isPassword(field)) {
      return "password";
    }
    if (this.isUpload(field)) {
      return "upload";
    }
    return void 0;
  }
  hasFocus(field) {
    return !!this.autoFocus && this.autoFocus === field.property;
  }
  isCheckboxGroup(field) {
    const {
      optionsService,
      optionsMulti,
      options
    } = field;
    return !optionsService && optionsMulti && !!options && options.length <= 3;
  }
  isCombo(field) {
    const {
      optionsService
    } = field;
    return !!optionsService && (isTypeof(optionsService, "string") || this.isComboFilter(optionsService));
  }
  isCurrencyType(field, type) {
    const {
      mask,
      pattern
    } = field;
    return this.compareTo(type, PoDynamicFieldType.Currency) && !mask && !pattern;
  }
  isLookupFilter(object) {
    return object && object.getObjectByValue !== void 0;
  }
  isComboFilter(object) {
    return object && object.getFilteredData !== void 0;
  }
  isLookup(field) {
    const {
      searchService
    } = field;
    return !!searchService && (isTypeof(searchService, "string") || this.isLookupFilter(searchService));
  }
  isMultiselect(field) {
    const {
      optionsService,
      optionsMulti,
      options
    } = field;
    return optionsMulti && (!!optionsService || !!options && options.length > 3);
  }
  isNumberType(field, type) {
    const {
      mask,
      pattern
    } = field;
    return this.compareTo(type, PoDynamicFieldType.Number) && !mask && !pattern;
  }
  isPassword(field) {
    const {
      secret
    } = field;
    return secret;
  }
  isRadioGroup(field) {
    const {
      optionsMulti,
      options
    } = field;
    return !optionsMulti && !!options && options.length <= 3;
  }
  isUpload(field) {
    const {
      url,
      type
    } = field;
    return url && type === "upload";
  }
  verifyForceOptionComponent(field) {
    const {
      optionsMulti,
      optionsService,
      forceOptionsComponentType
    } = field;
    if (forceOptionsComponentType && !optionsMulti && !optionsService) {
      return true;
    }
    return false;
  }
  isSelect(field) {
    const {
      optionsMulti,
      options
    } = field;
    return !optionsMulti && !!options && options.length > 3;
  }
  isTextarea(field) {
    const {
      rows
    } = field;
    return rows && rows >= 3;
  }
  printError(error2) {
    console.error(error2);
  }
  applySizeBasedOnA11y() {
    const size = validateSizeFn(this._initialComponentsSize, PoFieldSize);
    this._componentsSize = size;
  }
  static ɵfac = function PoDynamicFormFieldsBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoDynamicFormFieldsBaseComponent)(ɵɵdirectiveInject(TitleCasePipe));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoDynamicFormFieldsBaseComponent,
    hostVars: 1,
    hostBindings: function PoDynamicFormFieldsBaseComponent_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("PoUiThemeChange", function PoDynamicFormFieldsBaseComponent_PoUiThemeChange_HostBindingHandler() {
          return ctx.onThemeChange();
        }, ɵɵresolveWindow);
      }
      if (rf & 2) {
        ɵɵattribute("p-components-size", ctx.componentsSize);
      }
    },
    inputs: {
      autoFocus: [0, "p-auto-focus", "autoFocus"],
      disabledForm: [0, "p-disabled-form", "disabledForm"],
      validate: [0, "p-validate", "validate"],
      validateOnInput: [0, "p-validate-on-input", "validateOnInput"],
      componentsSize: [0, "p-components-size", "componentsSize"],
      fields: [0, "p-fields", "fields"],
      value: [0, "p-value", "value"],
      validateFields: [0, "p-validate-fields", "validateFields"]
    },
    outputs: {
      formValidate: "p-form-validate",
      fieldsChange: "p-fieldsChange",
      objectValue: "p-object-value"
    },
    features: [ɵɵInheritDefinitionFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoDynamicFormFieldsBaseComponent, [{
    type: Directive
  }], () => [{
    type: TitleCasePipe
  }], {
    autoFocus: [{
      type: Input,
      args: ["p-auto-focus"]
    }],
    disabledForm: [{
      type: Input,
      args: ["p-disabled-form"]
    }],
    validate: [{
      type: Input,
      args: ["p-validate"]
    }],
    formValidate: [{
      type: Output,
      args: ["p-form-validate"]
    }],
    fieldsChange: [{
      type: Output,
      args: ["p-fieldsChange"]
    }],
    objectValue: [{
      type: Output,
      args: ["p-object-value"]
    }],
    validateOnInput: [{
      type: Input,
      args: ["p-validate-on-input"]
    }],
    componentsSize: [{
      type: Input,
      args: ["p-components-size"]
    }, {
      type: HostBinding,
      args: ["attr.p-components-size"]
    }],
    fields: [{
      type: Input,
      args: ["p-fields"]
    }],
    value: [{
      type: Input,
      args: ["p-value"]
    }],
    validateFields: [{
      type: Input,
      args: ["p-validate-fields"]
    }],
    onThemeChange: [{
      type: HostListener,
      args: ["window:PoUiThemeChange"]
    }]
  });
})();
var PoDynamicContainerComponent = class _PoDynamicContainerComponent {
  content;
  fields;
  static ɵfac = function PoDynamicContainerComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoDynamicContainerComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoDynamicContainerComponent,
    selectors: [["po-dynamic-container"]],
    inputs: {
      content: [0, "p-content", "content"],
      fields: [0, "p-fields", "fields"]
    },
    standalone: false,
    decls: 3,
    vars: 0,
    consts: [[1, "po-dynamic-container"], [3, "p-title"], [1, "po-row"], [4, "ngTemplateOutlet", "ngTemplateOutletContext"]],
    template: function PoDynamicContainerComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "div", 0);
        ɵɵrepeaterCreate(1, PoDynamicContainerComponent_For_2_Template, 4, 5, "div", null, ɵɵrepeaterTrackByIdentity);
        ɵɵelementEnd();
      }
      if (rf & 2) {
        ɵɵadvance();
        ɵɵrepeater(ctx.fields);
      }
    },
    dependencies: [NgTemplateOutlet, PoContainerComponent],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoDynamicContainerComponent, [{
    type: Component,
    args: [{
      selector: "po-dynamic-container",
      standalone: false,
      template: '<div class="po-dynamic-container">\n  @for (fieldsInContainer of fields; track fieldsInContainer) {\n    <div>\n      <po-container [p-title]="fieldsInContainer[0].container">\n        <div class="po-row">\n          <ng-container *ngTemplateOutlet="content; context: { $implicit: fieldsInContainer }"> </ng-container>\n        </div>\n      </po-container>\n    </div>\n  }\n</div>\n'
    }]
  }], null, {
    content: [{
      type: Input,
      args: ["p-content"]
    }],
    fields: [{
      type: Input,
      args: ["p-fields"]
    }]
  });
})();
var PoDynamicFormFieldsComponent = class _PoDynamicFormFieldsComponent extends PoDynamicFormFieldsBaseComponent {
  validationService = inject(PoDynamicFormValidationService);
  changes = inject(ChangeDetectorRef);
  form = inject(NgForm);
  components;
  previousValue = {};
  constructor() {
    const titleCasePipe = inject(TitleCasePipe);
    super(titleCasePipe);
  }
  ngOnChanges(changes) {
    if (changes.fields) {
      this.visibleFields = this.getVisibleFields();
      if (changes.fields.previousValue && this.hasContainer()) {
        this.hasChangeContainer(changes.fields.previousValue, changes.fields.currentValue);
      }
      if (!changes.fields.previousValue || !changes.fields.currentValue) {
        this.setContainerFields();
      }
    }
    if (changes.value) {
      this.updatePreviousValue();
    }
  }
  focus(property) {
    const foundComponent = this.components.find((component) => component.name === property);
    if (foundComponent) {
      foundComponent.focus();
    }
  }
  isDisabled(field) {
    return field.disabled || this.disabledForm;
  }
  onChangeField(visibleField, objectValue) {
    return __async(this, null, function* () {
      const {
        property
      } = visibleField;
      const isBooleanType = visibleField.type === "boolean";
      const isChangedValueField = this.previousValue[property] !== this.value[property];
      if (visibleField.optionsService) {
        this.objectValue.emit(objectValue);
      }
      if ((this.form.dirty || isBooleanType) && isChangedValueField) {
        const {
          changedField,
          changedFieldIndex
        } = this.getField(property);
        if (changedField.validate) {
          yield this.validateField(changedField, changedFieldIndex, visibleField);
        }
        if (!this.validateOnInput) {
          this.triggerValidationOnForm(changedFieldIndex);
        }
      }
      this.updatePreviousValue();
    });
  }
  //emite o valor a cada caractere digitado no input
  onChangeFieldModel(visibleField) {
    if (this.validateOnInput) {
      const {
        property
      } = visibleField;
      const {
        changedFieldIndex
      } = this.getField(property);
      this.triggerValidationOnForm(changedFieldIndex);
    }
  }
  showAdditionalHelp(property) {
    const foundComponent = this.components.find((component) => component.name === property);
    if (foundComponent) {
      foundComponent.showAdditionalHelp();
    }
  }
  updatePreviousValue() {
    this.previousValue = JSON.parse(JSON.stringify(this.value));
  }
  trackBy(index, field) {
    return field.property;
  }
  applyFieldValidation(index, validatedField) {
    const field = this.fields[index];
    this.fields[index] = __spreadValues(__spreadValues({}, field), validatedField.field);
    this.updateFields();
    if (validatedField.hasOwnProperty("value")) {
      this.value[field.property] = validatedField.value;
    }
    this.changes.detectChanges();
    if (validatedField.focus) {
      this.focus(field.property);
    }
  }
  getField(property) {
    const changedFieldIndex = this.fields.findIndex((field) => field.property === property);
    const changedField = this.fields[changedFieldIndex];
    return {
      changedField,
      changedFieldIndex
    };
  }
  triggerValidationOnForm(changedFieldIndex) {
    const isValidatableField = this.validateFields?.length ? this.validateFieldsChecker(this.validateFields, this.fields[changedFieldIndex].property) : true;
    const hasValidationForm = this.validate && isValidatableField && this.formValidate.observers.length;
    if (hasValidationForm) {
      const updatedField = this.fields[changedFieldIndex];
      this.formValidate.emit(updatedField);
    }
  }
  updateFields() {
    this.fieldsChange.emit(this.fields);
    this.visibleFields = this.getVisibleFields();
    this.setContainerFields();
  }
  validateFieldsChecker(validateFields, propertyField) {
    return validateFields.some((validateFieldItem) => validateFieldItem === propertyField);
  }
  validateField(field, fieldIndex, visibleField) {
    return __async(this, null, function* () {
      const value = this.value[field.property];
      const previousDisabled = visibleField.disabled;
      visibleField.disabled = true;
      this.changes.detectChanges();
      try {
        const validatedField = yield this.validationService.sendFieldChange(field, value).toPromise();
        this.applyFieldValidation(fieldIndex, validatedField);
      } catch (e2) {
        visibleField.disabled = previousDisabled;
      }
    });
  }
  hasChangeContainer(previous, current) {
    const prevArray = previous.map((item, index) => ({
      container: item.container || null,
      property: item.property,
      index,
      order: item.order,
      visible: item.visible ?? true
    }));
    const currArray = current.map((item, index) => ({
      container: item.container || null,
      property: item.property,
      index,
      order: item.order,
      visible: item.visible ?? true
    }));
    const prevContainers = prevArray.filter((item) => item.container);
    const currContainers = currArray.filter((item) => item.container);
    const prevOrder = prevArray.filter((item) => item.order);
    const currOrder = currArray.filter((item) => item.order);
    const prevVisibleTrue = prevArray.filter((item) => item.visible === true);
    const currVisibleTrue = currArray.filter((item) => item.visible === true);
    const prevVisibleFalse = prevArray.filter((item) => !item.visible);
    const currVisibleFalse = currArray.filter((item) => !item.visible);
    if (prevContainers.length !== currContainers.length) {
      this.setContainerFields();
      return;
    }
    if (prevOrder.length !== currOrder.length) {
      this.setContainerFields();
      return;
    }
    if (prevVisibleTrue.length !== currVisibleTrue.length) {
      this.setContainerFields();
      return;
    }
    if (prevVisibleFalse.length !== currVisibleFalse.length) {
      this.setContainerFields();
      return;
    }
    if (currContainers.length) {
      this.handleChangesContainer(prevContainers, currContainers, "container");
    }
    if (currOrder.length) {
      this.handleChangesContainer(prevOrder, currOrder, "order");
    }
    if (currVisibleTrue.length) {
      this.handleChangesContainer(prevVisibleTrue, currVisibleTrue, "visible");
    }
    if (currVisibleFalse.length) {
      this.handleChangesContainer(prevVisibleFalse, currVisibleFalse, "visible");
    }
    const result = this.diffObjectsArray(previous, this.getVisibleFields());
    this.containerFields = this.updateFieldContainer(result, this.containerFields);
  }
  updateFieldContainer(changes, containerFields) {
    const mapchanges = new Map(changes.map((obj) => [obj.property, obj]));
    containerFields.forEach((subArray) => {
      subArray.forEach((subItem, index) => {
        const item = mapchanges.get(subItem.property);
        if (item) {
          subArray[index] = __spreadValues(__spreadValues({}, subItem), item);
        }
      });
    });
    return containerFields;
  }
  diffObjectsArray(oldArray, newArray) {
    const differences = [];
    newArray.forEach((newObj) => {
      const oldObj = oldArray.find((o) => o.property === newObj.property);
      if (!oldObj) {
        differences.push(__spreadValues({}, newObj));
      } else {
        const diff = {
          property: newObj.property
        };
        let hasDifferences = false;
        for (const key2 in newObj) {
          if (newObj[key2] !== oldObj[key2]) {
            diff[key2] = newObj[key2];
            hasDifferences = true;
          }
        }
        if (hasDifferences) {
          differences.push(diff);
        }
      }
    });
    return differences;
  }
  hasContainer() {
    return this.visibleFields?.some((field) => field.container) ?? false;
  }
  handleChangesContainer(prevContainers, currContainers, key2) {
    for (let i2 = 0; i2 < prevContainers.length; i2++) {
      const prev = prevContainers[i2];
      const curr = currContainers[i2];
      if (prev[key2] === curr[key2] && prev.index !== curr.index) {
        this.setContainerFields();
        return;
      }
      if (prev[key2] !== curr[key2] && prev.index === curr.index) {
        if (key2 === "order") {
          this.setContainerFields();
          return;
        }
        this.containerFields.forEach((subItem) => subItem.forEach((item) => {
          if (item.property === curr.property) {
            item[key2] = curr[key2];
          }
        }));
      }
      if (prev[key2] !== curr[key2] && prev.property !== curr.property) {
        this.setContainerFields();
        return;
      }
    }
  }
  static ɵfac = function PoDynamicFormFieldsComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoDynamicFormFieldsComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoDynamicFormFieldsComponent,
    selectors: [["po-dynamic-form-fields"]],
    viewQuery: function PoDynamicFormFieldsComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c144, 5);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.components = _t);
      }
    },
    standalone: false,
    features: [ɵɵProvidersFeature([PoDynamicFormValidationService], [{
      provide: ControlContainer,
      useExisting: NgForm
    }]), ɵɵInheritDefinitionFeature, ɵɵNgOnChangesFeature],
    decls: 5,
    vars: 1,
    consts: [["poContent", ""], ["poContentContainer", ""], ["component", ""], [1, "po-row", 3, "po-dynamic"], [1, "po-row"], [4, "ngTemplateOutlet", "ngTemplateOutletContext"], [1, "po-sm-12", 3, "p-label"], [3, "name", "ngModel", "ngClass", "additionalHelpEventTrigger", "p-additional-help-tooltip", "p-compact-label", "p-clean", "p-disabled", "p-error-pattern", "p-error-limit", "p-error-async", "p-auto-focus", "p-format", "p-help", "p-helper", "p-iso-format", "p-label", "p-loading", "p-locale", "p-max-date", "p-min-date", "p-mode", "p-no-autocomplete", "p-optional", "p-readonly", "p-required", "p-required-field-error-message", "p-size", "p-show-required", "p-year-range-limit", "p-placeholder"], [3, "name", "ngModel", "ngClass", "p-append-in-body", "p-clean", "p-compact-label", "p-disabled", "p-error-pattern", "p-error-limit", "p-format-date", "p-format-time", "p-help", "p-helper", "p-label", "p-loading", "p-locale", "p-max-date", "p-max-time", "p-min-date", "p-min-time", "p-minute-interval", "p-no-autocomplete", "p-optional", "p-placeholder", "p-readonly", "p-required", "p-required-field-error-message", "p-second-interval", "p-show-seconds", "p-show-required", "p-size"], [3, "name", "ngModel", "ngClass", "additionalHelpEventTrigger", "p-additional-help-tooltip", "p-clean", "p-compact-label", "p-disabled", "p-auto-focus", "p-help", "p-helper", "p-label", "p-literals", "p-loading", "p-max-date", "p-min-date", "p-no-autocomplete", "p-optional", "p-placeholder", "p-readonly", "p-required", "p-field-error-message", "p-error-limit", "p-show-required", "p-size", "p-range-presets", "p-range-preset-options", "p-range-presets-order"], [3, "name", "ngModel", "ngClass", "p-append-in-body", "p-compact-label", "p-clean", "p-disabled", "p-error-pattern", "p-error-limit", "p-auto-focus", "p-format", "p-help", "p-helper", "p-model-format", "p-label", "p-locale", "p-max-time", "p-min-time", "p-minute-interval", "p-no-autocomplete", "p-optional", "p-placeholder", "p-readonly", "p-required", "p-required-field-error-message", "p-second-interval", "p-show-required", "p-show-seconds", "p-size"], [3, "name", "ngModel", "ngClass", "additionalHelpEventTrigger", "p-additional-help-tooltip", "p-clean", "p-compact-label", "p-disabled", "p-error-pattern", "p-error-limit", "p-error-async-properties", "p-auto-focus", "p-help", "p-helper", "p-label", "p-loading", "p-mask-format-model", "p-mask-no-length-validation", "p-mask", "p-maxlength", "p-minlength", "p-no-autocomplete", "p-optional", "p-pattern", "p-required", "p-required-field-error-message", "p-show-required", "p-size", "p-icon", "p-placeholder", "p-readonly"], [3, "name", "ngModel", "ngClass", "additionalHelpEventTrigger", "p-additional-help-tooltip", "p-clean", "p-compact-label", "p-disabled", "p-error-pattern", "p-error-limit", "p-error-async-properties", "p-auto-focus", "p-help", "p-helper", "p-label", "p-loading", "p-min", "p-max", "p-maxlength", "p-minlength", "p-no-autocomplete", "p-optional", "p-step", "p-readonly", "p-required", "p-required-field-error-message", "p-show-required", "p-size", "p-icon", "p-placeholder"], [3, "name", "ngModel", "ngClass", "additionalHelpEventTrigger", "p-additional-help-tooltip", "p-clean", "p-compact-label", "p-error-pattern", "p-error-limit", "p-error-async-properties", "p-disabled", "p-decimals-length", "p-display-format", "p-thousand-maxlength", "p-icon", "p-auto-focus", "p-help", "p-helper", "p-label", "p-loading", "p-locale", "p-mask-no-length-validation", "p-min", "p-max", "p-maxlength", "p-minlength", "p-no-autocomplete", "p-optional", "p-readonly", "p-required", "p-required-field-error-message", "p-show-required", "p-size", "p-placeholder"], [3, "name", "ngModel", "ngClass", "additionalHelpEventTrigger", "p-additional-help-tooltip", "p-field-label", "p-field-value", "p-compact-label", "p-disabled", "p-help", "p-helper", "p-label", "p-loading", "p-optional", "p-options", "p-required", "p-field-error-message", "p-error-limit", "p-show-required", "p-size", "p-placeholder", "p-readonly"], [3, "name", "ngModel", "ngClass", "additionalHelpEventTrigger", "p-additional-help-tooltip", "p-columns", "p-compact-label", "p-auto-focus", "p-disabled", "p-help", "p-helper", "p-label", "p-optional", "p-options", "p-required", "p-field-error-message", "p-error-limit", "p-show-required", "p-size"], [3, "name", "ngModel", "ngClass", "additionalHelpEventTrigger", "p-additional-help-tooltip", "p-compact-label", "p-disabled", "p-error-limit", "p-field-error-message", "p-format-model", "p-help", "p-helper", "p-label", "p-label-off", "p-label-on", "p-label-position", "p-loading", "p-hide-label-status", "p-invalid-value", "p-size"], [3, "name", "ngModel", "ngClass", "additionalHelpEventTrigger", "p-additional-help-tooltip", "p-auto-focus", "p-compact-label", "p-disabled", "p-help", "p-helper", "p-label", "p-size"], ["p-emit-object-value", "", 3, "name", "ngModel", "ngClass", "additionalHelpEventTrigger", "p-additional-help-tooltip", "p-auto-focus", "p-compact-label", "p-clean", "p-disabled", "p-field-label", "p-field-value", "p-filter-params", "p-filter-service", "p-help", "p-helper", "p-infinite-scroll", "p-infinite-scroll-distance", "p-label", "p-literals", "p-loading", "p-options", "p-optional", "p-sort", "p-required", "p-field-error-message", "p-error-limit", "p-show-required", "p-icon", "p-placeholder", "p-filter-minlength", "p-filter-mode", "p-disabled-init-filter", "p-disabled-tab-filter", "p-debounce-time", "p-change-on-enter", "p-remove-initial-filter", "p-size", "p-listbox-control-position"], ["p-field-label", "label", "p-field-value", "value", 3, "name", "ngModel", "ngClass", "additionalHelpEventTrigger", "p-additional-help-tooltip", "p-compact-label", "p-clean", "p-columns", "p-disabled", "p-field-label", "p-field-value", "p-filter-params", "p-filter-service", "p-auto-focus", "p-auto-height", "p-help", "p-helper", "p-field-format", "p-infinite-scroll", "p-label", "p-literals", "p-loading", "p-multiple", "p-no-autocomplete", "p-optional", "p-required", "p-field-error-message", "p-error-limit", "p-show-required", "p-size", "p-placeholder", "p-advanced-filters"], [3, "name", "ngModel", "ngClass", "additionalHelpEventTrigger", "p-additional-help-tooltip", "p-columns", "p-compact-label", "p-auto-focus", "p-disabled", "p-help", "p-helper", "p-label", "p-optional", "p-options", "p-required", "p-show-required", "p-field-error-message", "p-error-limit", "p-size"], [3, "name", "ngModel", "ngClass", "additionalHelpEventTrigger", "p-additional-help-tooltip", "p-compact-label", "p-disabled", "p-auto-focus", "p-auto-height", "p-help", "p-helper", "p-label", "p-literals", "p-loading", "p-optional", "p-options", "p-required", "p-field-error-message", "p-error-limit", "p-show-required", "p-placeholder", "p-field-label", "p-field-value", "p-filter-service", "p-filter-mode", "p-debounce-time", "p-sort", "p-placeholder-search", "p-hide-search", "p-hide-select-all", "p-size", "p-listbox-control-position"], [3, "name", "ngModel", "ngClass", "additionalHelpEventTrigger", "p-additional-help-tooltip", "p-compact-label", "p-disabled", "p-auto-focus", "p-help", "p-helper", "p-label", "p-loading", "p-maxlength", "p-minlength", "p-optional", "p-readonly", "p-required", "p-field-error-message", "p-error-limit", "p-show-required", "p-rows", "p-size", "p-placeholder"], [3, "name", "ngModel", "ngClass", "additionalHelpEventTrigger", "p-additional-help-tooltip", "p-compact-label", "p-clean", "p-disabled", "p-error-pattern", "p-error-limit", "p-error-async-properties", "p-auto-focus", "p-help", "p-helper", "p-hide-password-peek", "p-icon", "p-label", "p-loading", "p-maxlength", "p-minlength", "p-no-autocomplete", "p-optional", "p-pattern", "p-readonly", "p-required", "p-required-field-error-message", "p-show-required", "p-size", "p-placeholder"], [3, "ngModel", "ngClass", "additionalHelpEventTrigger", "p-additional-help-tooltip", "p-auto-upload", "p-compact-label", "p-directory", "p-disabled", "p-drag-drop", "p-drag-drop-height", "p-restrictions", "p-form-field", "p-headers", "p-help", "p-helper", "p-hide-restrictions-info", "p-hide-select-button", "p-hide-send-button", "p-multiple", "p-label", "p-literals", "p-loading", "name", "p-custom-action", "p-optional", "p-required", "p-show-required", "p-show-thumbnail", "p-size", "p-url"], [3, "ngModelChange", "p-change", "p-change-model", "p-additional-help", "p-keydown", "name", "ngModel", "ngClass", "additionalHelpEventTrigger", "p-additional-help-tooltip", "p-compact-label", "p-clean", "p-disabled", "p-error-pattern", "p-error-limit", "p-error-async", "p-auto-focus", "p-format", "p-help", "p-helper", "p-iso-format", "p-label", "p-loading", "p-locale", "p-max-date", "p-min-date", "p-mode", "p-no-autocomplete", "p-optional", "p-readonly", "p-required", "p-required-field-error-message", "p-size", "p-show-required", "p-year-range-limit", "p-placeholder"], [3, "ngModelChange", "p-change", "p-change-model", "p-keydown", "name", "ngModel", "ngClass", "p-append-in-body", "p-clean", "p-compact-label", "p-disabled", "p-error-pattern", "p-error-limit", "p-format-date", "p-format-time", "p-help", "p-helper", "p-label", "p-loading", "p-locale", "p-max-date", "p-max-time", "p-min-date", "p-min-time", "p-minute-interval", "p-no-autocomplete", "p-optional", "p-placeholder", "p-readonly", "p-required", "p-required-field-error-message", "p-second-interval", "p-show-seconds", "p-show-required", "p-size"], [3, "ngModelChange", "p-additional-help", "p-change", "p-change-model", "p-keydown", "name", "ngModel", "ngClass", "additionalHelpEventTrigger", "p-additional-help-tooltip", "p-clean", "p-compact-label", "p-disabled", "p-auto-focus", "p-help", "p-helper", "p-label", "p-literals", "p-loading", "p-max-date", "p-min-date", "p-no-autocomplete", "p-optional", "p-placeholder", "p-readonly", "p-required", "p-field-error-message", "p-error-limit", "p-show-required", "p-size", "p-range-presets", "p-range-preset-options", "p-range-presets-order"], [3, "ngModelChange", "p-change", "p-change-model", "p-keydown", "name", "ngModel", "ngClass", "p-append-in-body", "p-compact-label", "p-clean", "p-disabled", "p-error-pattern", "p-error-limit", "p-auto-focus", "p-format", "p-help", "p-helper", "p-model-format", "p-label", "p-locale", "p-max-time", "p-min-time", "p-minute-interval", "p-no-autocomplete", "p-optional", "p-placeholder", "p-readonly", "p-required", "p-required-field-error-message", "p-second-interval", "p-show-required", "p-show-seconds", "p-size"], [3, "ngModelChange", "p-additional-help", "p-change", "p-change-model", "p-keydown", "name", "ngModel", "ngClass", "additionalHelpEventTrigger", "p-additional-help-tooltip", "p-clean", "p-compact-label", "p-disabled", "p-error-pattern", "p-error-limit", "p-error-async-properties", "p-auto-focus", "p-help", "p-helper", "p-label", "p-loading", "p-mask-format-model", "p-mask-no-length-validation", "p-mask", "p-maxlength", "p-minlength", "p-no-autocomplete", "p-optional", "p-pattern", "p-required", "p-required-field-error-message", "p-show-required", "p-size", "p-icon", "p-placeholder", "p-readonly"], [3, "ngModelChange", "p-additional-help", "p-change", "p-change-model", "p-keydown", "name", "ngModel", "ngClass", "additionalHelpEventTrigger", "p-additional-help-tooltip", "p-clean", "p-compact-label", "p-disabled", "p-error-pattern", "p-error-limit", "p-error-async-properties", "p-auto-focus", "p-help", "p-helper", "p-label", "p-loading", "p-min", "p-max", "p-maxlength", "p-minlength", "p-no-autocomplete", "p-optional", "p-step", "p-readonly", "p-required", "p-required-field-error-message", "p-show-required", "p-size", "p-icon", "p-placeholder"], [3, "ngModelChange", "p-additional-help", "p-change", "p-change-model", "p-keydown", "name", "ngModel", "ngClass", "additionalHelpEventTrigger", "p-additional-help-tooltip", "p-clean", "p-compact-label", "p-error-pattern", "p-error-limit", "p-error-async-properties", "p-disabled", "p-decimals-length", "p-display-format", "p-thousand-maxlength", "p-icon", "p-auto-focus", "p-help", "p-helper", "p-label", "p-loading", "p-locale", "p-mask-no-length-validation", "p-min", "p-max", "p-maxlength", "p-minlength", "p-no-autocomplete", "p-optional", "p-readonly", "p-required", "p-required-field-error-message", "p-show-required", "p-size", "p-placeholder"], [3, "ngModelChange", "p-additional-help", "p-change", "p-change-model", "p-keydown", "name", "ngModel", "ngClass", "additionalHelpEventTrigger", "p-additional-help-tooltip", "p-field-label", "p-field-value", "p-compact-label", "p-disabled", "p-help", "p-helper", "p-label", "p-loading", "p-optional", "p-options", "p-required", "p-field-error-message", "p-error-limit", "p-show-required", "p-size", "p-placeholder", "p-readonly"], [3, "ngModelChange", "p-additional-help", "p-change", "p-change-model", "p-keydown", "name", "ngModel", "ngClass", "additionalHelpEventTrigger", "p-additional-help-tooltip", "p-columns", "p-compact-label", "p-auto-focus", "p-disabled", "p-help", "p-helper", "p-label", "p-optional", "p-options", "p-required", "p-field-error-message", "p-error-limit", "p-show-required", "p-size"], [3, "ngModelChange", "p-additional-help", "p-change", "p-change-model", "p-keydown", "name", "ngModel", "ngClass", "additionalHelpEventTrigger", "p-additional-help-tooltip", "p-compact-label", "p-disabled", "p-error-limit", "p-field-error-message", "p-format-model", "p-help", "p-helper", "p-label", "p-label-off", "p-label-on", "p-label-position", "p-loading", "p-hide-label-status", "p-invalid-value", "p-size"], [3, "ngModelChange", "p-additional-help", "p-change", "p-change-model", "p-keydown", "name", "ngModel", "ngClass", "additionalHelpEventTrigger", "p-additional-help-tooltip", "p-auto-focus", "p-compact-label", "p-disabled", "p-help", "p-helper", "p-label", "p-size"], ["p-emit-object-value", "", 3, "ngModelChange", "p-change", "p-change-model", "p-keydown", "p-additional-help", "name", "ngModel", "ngClass", "additionalHelpEventTrigger", "p-additional-help-tooltip", "p-auto-focus", "p-compact-label", "p-clean", "p-disabled", "p-field-label", "p-field-value", "p-filter-params", "p-filter-service", "p-help", "p-helper", "p-infinite-scroll", "p-infinite-scroll-distance", "p-label", "p-literals", "p-loading", "p-options", "p-optional", "p-sort", "p-required", "p-field-error-message", "p-error-limit", "p-show-required", "p-icon", "p-placeholder", "p-filter-minlength", "p-filter-mode", "p-disabled-init-filter", "p-disabled-tab-filter", "p-debounce-time", "p-change-on-enter", "p-remove-initial-filter", "p-size", "p-listbox-control-position"], ["p-field-label", "label", "p-field-value", "value", 3, "ngModelChange", "p-change", "p-change-model", "p-additional-help", "p-change-visible-columns", "p-keydown", "p-restore-column-manager", "name", "ngModel", "ngClass", "additionalHelpEventTrigger", "p-additional-help-tooltip", "p-compact-label", "p-clean", "p-columns", "p-disabled", "p-field-label", "p-field-value", "p-filter-params", "p-filter-service", "p-auto-focus", "p-auto-height", "p-help", "p-helper", "p-field-format", "p-infinite-scroll", "p-label", "p-literals", "p-loading", "p-multiple", "p-no-autocomplete", "p-optional", "p-required", "p-field-error-message", "p-error-limit", "p-show-required", "p-size", "p-placeholder", "p-advanced-filters"], [3, "ngModelChange", "p-additional-help", "p-change", "p-change-model", "p-keydown", "name", "ngModel", "ngClass", "additionalHelpEventTrigger", "p-additional-help-tooltip", "p-columns", "p-compact-label", "p-auto-focus", "p-disabled", "p-help", "p-helper", "p-label", "p-optional", "p-options", "p-required", "p-show-required", "p-field-error-message", "p-error-limit", "p-size"], [3, "ngModelChange", "p-change", "p-change-model", "p-additional-help", "p-keydown", "name", "ngModel", "ngClass", "additionalHelpEventTrigger", "p-additional-help-tooltip", "p-compact-label", "p-disabled", "p-auto-focus", "p-auto-height", "p-help", "p-helper", "p-label", "p-literals", "p-loading", "p-optional", "p-options", "p-required", "p-field-error-message", "p-error-limit", "p-show-required", "p-placeholder", "p-field-label", "p-field-value", "p-filter-service", "p-filter-mode", "p-debounce-time", "p-sort", "p-placeholder-search", "p-hide-search", "p-hide-select-all", "p-size", "p-listbox-control-position"], [3, "ngModelChange", "p-additional-help", "p-change", "p-change-model", "p-keydown", "name", "ngModel", "ngClass", "additionalHelpEventTrigger", "p-additional-help-tooltip", "p-compact-label", "p-disabled", "p-auto-focus", "p-help", "p-helper", "p-label", "p-loading", "p-maxlength", "p-minlength", "p-optional", "p-readonly", "p-required", "p-field-error-message", "p-error-limit", "p-show-required", "p-rows", "p-size", "p-placeholder"], [3, "ngModelChange", "p-additional-help", "p-change", "p-change-model", "p-keydown", "name", "ngModel", "ngClass", "additionalHelpEventTrigger", "p-additional-help-tooltip", "p-compact-label", "p-clean", "p-disabled", "p-error-pattern", "p-error-limit", "p-error-async-properties", "p-auto-focus", "p-help", "p-helper", "p-hide-password-peek", "p-icon", "p-label", "p-loading", "p-maxlength", "p-minlength", "p-no-autocomplete", "p-optional", "p-pattern", "p-readonly", "p-required", "p-required-field-error-message", "p-show-required", "p-size", "p-placeholder"], [3, "ngModelChange", "p-custom-action-click", "p-error", "p-success", "p-upload", "p-additional-help", "p-change", "p-change-model", "p-keydown", "ngModel", "ngClass", "additionalHelpEventTrigger", "p-additional-help-tooltip", "p-auto-upload", "p-compact-label", "p-directory", "p-disabled", "p-drag-drop", "p-drag-drop-height", "p-restrictions", "p-form-field", "p-headers", "p-help", "p-helper", "p-hide-restrictions-info", "p-hide-select-button", "p-hide-send-button", "p-multiple", "p-label", "p-literals", "p-loading", "name", "p-custom-action", "p-optional", "p-required", "p-show-required", "p-show-thumbnail", "p-size", "p-url"], [3, "p-fields", "p-content"]],
    template: function PoDynamicFormFieldsComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵconditionalCreate(0, PoDynamicFormFieldsComponent_Conditional_0_Template, 2, 6, "div", 3);
        ɵɵtemplate(1, PoDynamicFormFieldsComponent_ng_template_1_Template, 2, 0, "ng-template", null, 0, ɵɵtemplateRefExtractor)(3, PoDynamicFormFieldsComponent_ng_template_3_Template, 1, 2, "ng-template", null, 1, ɵɵtemplateRefExtractor);
      }
      if (rf & 2) {
        ɵɵconditional(ctx.visibleFields && ctx.visibleFields.length > 0 ? 0 : -1);
      }
    },
    dependencies: [NgClass, NgTemplateOutlet, NgControlStatus, NgModel, PoDividerComponent, PoCheckboxGroupComponent, PoCheckboxComponent, PoComboComponent, PoDatepickerComponent, PoDatepickerRangeComponent, PoDatetimepickerComponent, PoDecimalComponent, PoInputComponent, PoLookupComponent, PoMultiselectComponent, PoNumberComponent, PoPasswordComponent, PoRadioGroupComponent, PoSelectComponent, PoSwitchComponent, PoTextareaComponent, PoTimepickerComponent, PoUploadComponent, PoDynamicContainerComponent],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoDynamicFormFieldsComponent, [{
    type: Component,
    args: [{
      selector: "po-dynamic-form-fields",
      viewProviders: [{
        provide: ControlContainer,
        useExisting: NgForm
      }],
      providers: [PoDynamicFormValidationService],
      standalone: false,
      template: `@if (visibleFields && visibleFields.length > 0) {
  <div class="po-row" [class.po-dynamic]="hasContainers">
    <ng-container
      *ngTemplateOutlet="
        hasContainers ? poContentContainer : poContent;
        context: { $implicit: hasContainers ? containerFields : visibleFields }
      "
    ></ng-container>
  </div>
}

<ng-template #poContent let-fields>
  @for (field of fields; track trackBy($index, field)) {
    @if (field?.divider?.trim()) {
      <po-divider class="po-sm-12" [p-label]="field.divider"> </po-divider>
    }
    @if (compareTo(field.control, 'datepicker')) {
      <po-datepicker
        #component
        [name]="field.property"
        [(ngModel)]="value[field.property]"
        [ngClass]="field.componentClass"
        [additionalHelpEventTrigger]="field.additionalHelp ? 'event' : 'noEvent'"
        [p-additional-help-tooltip]="field.additionalHelpTooltip"
        [p-compact-label]="field.compactLabel"
        [p-clean]="field.clean"
        [p-disabled]="isDisabled(field)"
        [p-error-pattern]="field.errorMessage"
        [p-error-limit]="field.errorLimit"
        [p-error-async]="field.errorAsyncFunction"
        [p-auto-focus]="field.focus"
        [p-format]="field.format"
        [p-help]="field.help"
        [p-helper]="field.helper"
        [p-iso-format]="field.isoFormat"
        [p-label]="field.label"
        [p-loading]="field.loading"
        [p-locale]="field.locale"
        [p-max-date]="field.maxValue"
        [p-min-date]="field.minValue"
        [p-mode]="field.mode"
        [p-no-autocomplete]="field.noAutocomplete"
        [p-optional]="field.optional"
        [p-readonly]="field.readonly"
        [p-required]="field.required"
        [p-required-field-error-message]="field.requiredFieldErrorMessage"
        [p-size]="field.size || componentsSize"
        [p-show-required]="field.showRequired"
        [p-year-range-limit]="field.yearRangeLimit"
        (p-change)="onChangeField(field)"
        (p-change-model)="onChangeFieldModel(field)"
        [p-placeholder]="field.placeholder"
        (p-additional-help)="field.additionalHelp?.($event)"
        (p-keydown)="field.keydown?.($event)"
      >
      </po-datepicker>
    }
    @if (compareTo(field.control, 'datetimepicker')) {
      <po-datetimepicker
        #component
        [name]="field.property"
        [(ngModel)]="value[field.property]"
        [ngClass]="field.componentClass"
        [p-append-in-body]="field.appendInBody"
        [p-clean]="field.clean"
        [p-compact-label]="field.compactLabel"
        [p-disabled]="isDisabled(field)"
        [p-error-pattern]="field.errorMessage"
        [p-error-limit]="field.errorLimit"
        [p-format-date]="field.format"
        [p-format-time]="field.formatTime"
        [p-help]="field.help"
        [p-helper]="field.helper"
        [p-label]="field.label"
        [p-loading]="field.loading"
        [p-locale]="field.locale"
        [p-max-date]="field.maxValue"
        [p-max-time]="field.maxTime"
        [p-min-date]="field.minValue"
        [p-min-time]="field.minTime"
        [p-minute-interval]="field.minuteInterval"
        [p-no-autocomplete]="field.noAutocomplete"
        [p-optional]="field.optional"
        [p-placeholder]="field.placeholder"
        [p-readonly]="field.readonly"
        [p-required]="field.required"
        [p-required-field-error-message]="field.requiredFieldErrorMessage"
        [p-second-interval]="field.secondInterval"
        [p-show-seconds]="field.showSeconds"
        [p-show-required]="field.showRequired"
        [p-size]="field.size || componentsSize"
        (p-change)="onChangeField(field)"
        (p-change-model)="onChangeFieldModel(field)"
        (p-keydown)="field.keydown?.($event)"
      >
      </po-datetimepicker>
    }
    @if (compareTo(field.control, 'datepickerrange')) {
      <po-datepicker-range
        #component
        [name]="field.property"
        [(ngModel)]="value[field.property]"
        [ngClass]="field.componentClass"
        [additionalHelpEventTrigger]="field.additionalHelp ? 'event' : 'noEvent'"
        [p-additional-help-tooltip]="field.additionalHelpTooltip"
        [p-clean]="field.clean"
        [p-compact-label]="field.compactLabel"
        [p-disabled]="isDisabled(field)"
        [p-auto-focus]="field.focus"
        [p-help]="field.help"
        [p-helper]="field.helper"
        [p-label]="field.label"
        [p-literals]="field.literals"
        [p-loading]="field.loading"
        [p-max-date]="field.maxValue"
        [p-min-date]="field.minValue"
        [p-no-autocomplete]="field.noAutocomplete"
        [p-optional]="field.optional"
        [p-placeholder]="field.placeholder"
        [p-readonly]="field.readonly"
        [p-required]="field.required"
        [p-field-error-message]="field.errorMessage"
        [p-error-limit]="field.errorLimit"
        [p-show-required]="field.showRequired"
        [p-size]="field.size || componentsSize"
        [p-range-presets]="field.rangePresets"
        [p-range-preset-options]="field.rangePresetOptions"
        [p-range-presets-order]="field.rangePresetsOrder"
        (p-additional-help)="field.additionalHelp?.($event)"
        (p-change)="onChangeField(field)"
        (p-change-model)="onChangeFieldModel(field)"
        (p-keydown)="field.keydown?.($event)"
      >
      </po-datepicker-range>
    }
    @if (compareTo(field.control, 'timepicker')) {
      <po-timepicker
        #component
        [name]="field.property"
        [(ngModel)]="value[field.property]"
        [ngClass]="field.componentClass"
        [p-append-in-body]="field.appendBox"
        [p-compact-label]="field.compactLabel"
        [p-clean]="field.clean"
        [p-disabled]="isDisabled(field)"
        [p-error-pattern]="field.errorMessage"
        [p-error-limit]="field.errorLimit"
        [p-auto-focus]="field.focus"
        [p-format]="field.format"
        [p-help]="field.help"
        [p-helper]="field.helper"
        [p-model-format]="field.modelFormat"
        [p-label]="field.label"
        [p-locale]="field.locale"
        [p-max-time]="field.maxValue"
        [p-min-time]="field.minValue"
        [p-minute-interval]="field.minuteInterval"
        [p-no-autocomplete]="field.noAutocomplete"
        [p-optional]="field.optional"
        [p-placeholder]="field.placeholder"
        [p-readonly]="field.readonly"
        [p-required]="field.required"
        [p-required-field-error-message]="field.requiredFieldErrorMessage"
        [p-second-interval]="field.secondInterval"
        [p-show-required]="field.showRequired"
        [p-show-seconds]="field.showSeconds"
        [p-size]="field.size || componentsSize"
        (p-change)="onChangeField(field)"
        (p-change-model)="onChangeFieldModel(field)"
        (p-keydown)="field.keydown?.($event)"
      >
      </po-timepicker>
    }
    @if (compareTo(field.control, 'input')) {
      <po-input
        #component
        [name]="field.property"
        [(ngModel)]="value[field.property]"
        [ngClass]="field.componentClass"
        [additionalHelpEventTrigger]="field.additionalHelp ? 'event' : 'noEvent'"
        [p-additional-help-tooltip]="field.additionalHelpTooltip"
        [p-clean]="field.clean"
        [p-compact-label]="field.compactLabel"
        [p-disabled]="isDisabled(field)"
        [p-error-pattern]="field.errorMessage"
        [p-error-limit]="field.errorLimit"
        [p-error-async-properties]="field.errorAsyncProperties"
        [p-auto-focus]="field.focus"
        [p-help]="field.help"
        [p-helper]="field.helper"
        [p-label]="field.label"
        [p-loading]="field.loading"
        [p-mask-format-model]="field.maskFormatModel"
        [p-mask-no-length-validation]="field.maskNoLengthValidation"
        [p-mask]="field.mask"
        [p-maxlength]="field.maxLength"
        [p-minlength]="field.minLength"
        [p-no-autocomplete]="field.noAutocomplete"
        [p-optional]="field.optional"
        [p-pattern]="field.pattern"
        [p-required]="field.required"
        [p-required-field-error-message]="field.requiredFieldErrorMessage"
        [p-show-required]="field.showRequired"
        [p-size]="field.size || componentsSize"
        (p-additional-help)="field.additionalHelp?.($event)"
        (p-change)="onChangeField(field)"
        (p-change-model)="onChangeFieldModel(field)"
        (p-keydown)="field.keydown?.($event)"
        [p-icon]="field.icon"
        [p-placeholder]="field.placeholder"
        [p-readonly]="field.readonly"
      >
      </po-input>
    }
    @if (compareTo(field.control, 'number')) {
      <po-number
        #component
        [name]="field.property"
        [(ngModel)]="value[field.property]"
        [ngClass]="field.componentClass"
        [additionalHelpEventTrigger]="field.additionalHelp ? 'event' : 'noEvent'"
        [p-additional-help-tooltip]="field.additionalHelpTooltip"
        [p-clean]="field.clean"
        [p-compact-label]="field.compactLabel"
        [p-disabled]="isDisabled(field)"
        [p-error-pattern]="field.errorMessage"
        [p-error-limit]="field.errorLimit"
        [p-error-async-properties]="field.errorAsyncProperties"
        [p-auto-focus]="field.focus"
        [p-help]="field.help"
        [p-helper]="field.helper"
        [p-label]="field.label"
        [p-loading]="field.loading"
        [p-min]="field.minValue"
        [p-max]="field.maxValue"
        [p-maxlength]="field.maxLength"
        [p-minlength]="field.minLength"
        [p-no-autocomplete]="field.noAutocomplete"
        [p-optional]="field.optional"
        [p-step]="field.step"
        [p-readonly]="field.readonly"
        [p-required]="field.required"
        [p-required-field-error-message]="field.requiredFieldErrorMessage"
        [p-show-required]="field.showRequired"
        [p-size]="field.size || componentsSize"
        (p-additional-help)="field.additionalHelp?.($event)"
        (p-change)="onChangeField(field)"
        (p-change-model)="onChangeFieldModel(field)"
        (p-keydown)="field.keydown?.($event)"
        [p-icon]="field.icon"
        [p-placeholder]="field.placeholder"
      >
      </po-number>
    }
    @if (compareTo(field.control, 'decimal')) {
      <po-decimal
        #component
        [name]="field.property"
        [(ngModel)]="value[field.property]"
        [ngClass]="field.componentClass"
        [additionalHelpEventTrigger]="field.additionalHelp ? 'event' : 'noEvent'"
        [p-additional-help-tooltip]="field.additionalHelpTooltip"
        [p-clean]="field.clean"
        [p-compact-label]="field.compactLabel"
        [p-error-pattern]="field.errorMessage"
        [p-error-limit]="field.errorLimit"
        [p-error-async-properties]="field.errorAsyncProperties"
        [p-disabled]="isDisabled(field)"
        [p-decimals-length]="field.decimalsLength"
        [p-display-format]="field.displayFormat"
        [p-thousand-maxlength]="field.thousandMaxlength"
        [p-icon]="field.icon"
        [p-auto-focus]="field.focus"
        [p-help]="field.help"
        [p-helper]="field.helper"
        [p-label]="field.label"
        [p-loading]="field.loading"
        [p-locale]="field.locale"
        [p-mask-no-length-validation]="field.maskNoLengthValidation"
        [p-min]="field.minValue"
        [p-max]="field.maxValue"
        [p-maxlength]="field.maxLength"
        [p-minlength]="field.minLength"
        [p-no-autocomplete]="field.noAutocomplete"
        [p-optional]="field.optional"
        [p-readonly]="field.readonly"
        [p-required]="field.required"
        [p-required-field-error-message]="field.requiredFieldErrorMessage"
        [p-show-required]="field.showRequired"
        [p-size]="field.size || componentsSize"
        (p-additional-help)="field.additionalHelp?.($event)"
        (p-change)="onChangeField(field)"
        (p-change-model)="onChangeFieldModel(field)"
        (p-keydown)="field.keydown?.($event)"
        [p-placeholder]="field.placeholder"
      >
      </po-decimal>
    }
    @if (compareTo(field.control, 'select')) {
      <po-select
        #component
        [name]="field.property"
        [(ngModel)]="value[field.property]"
        [ngClass]="field.componentClass"
        [additionalHelpEventTrigger]="field.additionalHelp ? 'event' : 'noEvent'"
        [p-additional-help-tooltip]="field.additionalHelpTooltip"
        [p-field-label]="field.fieldLabel"
        [p-field-value]="field.fieldValue"
        [p-compact-label]="field.compactLabel"
        [p-disabled]="isDisabled(field)"
        [p-help]="field.help"
        [p-helper]="field.helper"
        [p-label]="field.label"
        [p-loading]="field.loading"
        [p-optional]="field.optional"
        [p-options]="field.options"
        [p-required]="field.required"
        [p-field-error-message]="field.errorMessage"
        [p-error-limit]="field.errorLimit"
        [p-show-required]="field.showRequired"
        [p-size]="field.size || componentsSize"
        (p-additional-help)="field.additionalHelp?.($event)"
        (p-change)="onChangeField(field)"
        (p-change-model)="onChangeFieldModel(field)"
        (p-keydown)="field.keydown?.($event)"
        [p-placeholder]="field.placeholder"
        [p-readonly]="field.readonly"
      >
      </po-select>
    }
    @if (compareTo(field.control, 'radioGroup')) {
      <po-radio-group
        #component
        [name]="field.property"
        [(ngModel)]="value[field.property]"
        [ngClass]="field.componentClass"
        [additionalHelpEventTrigger]="field.additionalHelp ? 'event' : 'noEvent'"
        [p-additional-help-tooltip]="field.additionalHelpTooltip"
        [p-columns]="field.columns || 3"
        [p-compact-label]="field.compactLabel"
        [p-auto-focus]="field.focus"
        [p-disabled]="isDisabled(field)"
        [p-help]="field.help"
        [p-helper]="field.helper"
        [p-label]="field.label"
        [p-optional]="field.optional"
        [p-options]="field.options"
        [p-required]="field.required"
        [p-field-error-message]="field.errorMessage"
        [p-error-limit]="field.errorLimit"
        [p-show-required]="field.showRequired"
        [p-size]="field.size || componentsSize"
        (p-additional-help)="field.additionalHelp?.($event)"
        (p-change)="onChangeField(field)"
        (p-change-model)="onChangeFieldModel(field)"
        (p-keydown)="field.keydown?.($event)"
      >
      </po-radio-group>
    }
    @if (compareTo(field.control, 'switch')) {
      <po-switch
        #component
        [name]="field.property"
        [(ngModel)]="value[field.property]"
        [ngClass]="field.componentClass"
        [additionalHelpEventTrigger]="field.additionalHelp ? 'event' : 'noEvent'"
        [p-additional-help-tooltip]="field.additionalHelpTooltip"
        [p-compact-label]="field.compactLabel"
        [p-disabled]="isDisabled(field)"
        [p-error-limit]="field.errorLimit"
        [p-field-error-message]="field.errorMessage"
        [p-format-model]="field.formatModel"
        [p-help]="field.help"
        [p-helper]="field.helper"
        [p-label]="field.label"
        [p-label-off]="field.booleanFalse"
        [p-label-on]="field.booleanTrue"
        [p-label-position]="field.labelPosition"
        [p-loading]="field.loading"
        [p-hide-label-status]="field.hideLabelStatus"
        [p-invalid-value]="field.invalidValue"
        [p-size]="field.size || componentsSize"
        (p-additional-help)="field.additionalHelp?.($event)"
        (p-change)="onChangeField(field)"
        (p-change-model)="onChangeFieldModel(field)"
        (p-keydown)="field.keydown?.($event)"
      >
      </po-switch>
    }
    @if (compareTo(field.control, 'checkbox')) {
      <po-checkbox
        #component
        [name]="field.property"
        [(ngModel)]="value[field.property]"
        [ngClass]="field.componentClass"
        [additionalHelpEventTrigger]="field.additionalHelp ? 'event' : 'noEvent'"
        [p-additional-help-tooltip]="field.additionalHelpTooltip"
        [p-auto-focus]="field.focus"
        [p-compact-label]="field.compactLabel"
        [p-disabled]="isDisabled(field)"
        [p-help]="field.help"
        [p-helper]="field.helper"
        [p-label]="field.label"
        [p-size]="field.size || componentsSize"
        (p-additional-help)="field.additionalHelp?.($event)"
        (p-change)="onChangeField(field)"
        (p-change-model)="onChangeFieldModel(field)"
        (p-keydown)="field.keydown?.($event)"
      >
      </po-checkbox>
    }
    @if (compareTo(field.control, 'combo')) {
      <po-combo
        #component
        [name]="field.property"
        [(ngModel)]="value[field.property]"
        [ngClass]="field.componentClass"
        p-emit-object-value
        [additionalHelpEventTrigger]="field.additionalHelp ? 'event' : 'noEvent'"
        [p-additional-help-tooltip]="field.additionalHelpTooltip"
        [p-auto-focus]="field.focus"
        [p-compact-label]="field.compactLabel"
        [p-clean]="field.clean"
        [p-disabled]="isDisabled(field)"
        [p-field-label]="field.fieldLabel"
        [p-field-value]="field.fieldValue"
        [p-filter-params]="field.params"
        [p-filter-service]="field.optionsService"
        [p-help]="field.help"
        [p-helper]="field.helper"
        [p-infinite-scroll]="field.infiniteScroll"
        [p-infinite-scroll-distance]="field.infiniteScrollDistance || 100"
        [p-label]="field.label"
        [p-literals]="field.literals"
        [p-loading]="field.loading"
        [p-options]="field.options"
        [p-optional]="field.optional"
        [p-sort]="field.sort"
        [p-required]="field.required"
        [p-field-error-message]="field.errorMessage"
        [p-error-limit]="field.errorLimit"
        [p-show-required]="field.showRequired"
        (p-change)="onChangeField(field, $event)"
        (p-change-model)="onChangeFieldModel(field)"
        [p-icon]="field.icon"
        [p-placeholder]="field.placeholder"
        [p-filter-minlength]="field.filterMinlength"
        [p-filter-mode]="field.filterMode"
        [p-disabled-init-filter]="field.disabledInitFilter"
        [p-disabled-tab-filter]="field.disabledTabFilter"
        [p-debounce-time]="field.debounceTime"
        [p-change-on-enter]="field.changeOnEnter"
        [p-remove-initial-filter]="field.removeInitialFilter"
        [p-size]="field.size || componentsSize"
        (p-keydown)="field.keydown?.($event)"
        (p-additional-help)="field.additionalHelp?.($event)"
        [p-listbox-control-position]="field.listboxControlPosition"
      >
      </po-combo>
    }
    @if (compareTo(field.control, 'lookup')) {
      <po-lookup
        #component
        [name]="field.property"
        [(ngModel)]="value[field.property]"
        p-field-label="label"
        p-field-value="value"
        [ngClass]="field.componentClass"
        [additionalHelpEventTrigger]="field.additionalHelp ? 'event' : 'noEvent'"
        [p-additional-help-tooltip]="field.additionalHelpTooltip"
        [p-compact-label]="field.compactLabel"
        [p-clean]="field.clean"
        [p-columns]="field.columns"
        [p-disabled]="isDisabled(field)"
        [p-field-label]="field.fieldLabel || 'label'"
        [p-field-value]="field.fieldValue || 'value'"
        [p-filter-params]="field.params"
        [p-filter-service]="field.searchService"
        [p-auto-focus]="field.focus"
        [p-auto-height]="field.autoHeight"
        [p-help]="field.help"
        [p-helper]="field.helper"
        [p-field-format]="field.format"
        [p-infinite-scroll]="field.infiniteScroll"
        [p-label]="field.label"
        [p-literals]="field.literals"
        [p-loading]="field.loading"
        [p-multiple]="field.multiple"
        [p-no-autocomplete]="field.noAutocomplete"
        [p-optional]="field.optional"
        [p-required]="field.required"
        [p-field-error-message]="field.errorMessage"
        [p-error-limit]="field.errorLimit"
        [p-show-required]="field.showRequired"
        [p-size]="field.size || componentsSize"
        (p-change)="onChangeField(field)"
        (p-change-model)="onChangeFieldModel(field)"
        [p-placeholder]="field.placeholder"
        [p-advanced-filters]="field.advancedFilters"
        (p-additional-help)="field.additionalHelp?.($event)"
        (p-change-visible-columns)="field.changeVisibleColumns?.($event)"
        (p-keydown)="field.keydown?.($event)"
        (p-restore-column-manager)="field.columnRestoreManager?.($event)"
      >
      </po-lookup>
    }
    @if (compareTo(field.control, 'checkboxGroup')) {
      <po-checkbox-group
        #component
        [name]="field.property"
        [(ngModel)]="value[field.property]"
        [ngClass]="field.componentClass"
        [additionalHelpEventTrigger]="field.additionalHelp ? 'event' : 'noEvent'"
        [p-additional-help-tooltip]="field.additionalHelpTooltip"
        [p-columns]="field.columns || 3"
        [p-compact-label]="field.compactLabel"
        [p-auto-focus]="field.focus"
        [p-disabled]="isDisabled(field)"
        [p-help]="field.help"
        [p-helper]="field.helper"
        [p-label]="field.label"
        [p-optional]="field.optional"
        [p-options]="field.options"
        [p-required]="field.required"
        [p-show-required]="field.showRequired"
        [p-field-error-message]="field.errorMessage"
        [p-error-limit]="field.errorLimit"
        [p-size]="field.size || componentsSize"
        (p-additional-help)="field.additionalHelp?.($event)"
        (p-change)="onChangeField(field)"
        (p-change-model)="onChangeFieldModel(field)"
        (p-keydown)="field.keydown?.($event)"
      >
      </po-checkbox-group>
    }
    @if (compareTo(field.control, 'multiselect')) {
      <po-multiselect
        #component
        [name]="field.property"
        [(ngModel)]="value[field.property]"
        [ngClass]="field.componentClass"
        [additionalHelpEventTrigger]="field.additionalHelp ? 'event' : 'noEvent'"
        [p-additional-help-tooltip]="field.additionalHelpTooltip"
        [p-compact-label]="field.compactLabel"
        [p-disabled]="isDisabled(field)"
        [p-auto-focus]="field.focus"
        [p-auto-height]="field.autoHeight"
        [p-help]="field.help"
        [p-helper]="field.helper"
        [p-label]="field.label"
        [p-literals]="field.literals"
        [p-loading]="field.loading"
        [p-optional]="field.optional"
        [p-options]="field.options"
        [p-required]="field.required"
        [p-field-error-message]="field.errorMessage"
        [p-error-limit]="field.errorLimit"
        [p-show-required]="field.showRequired"
        (p-change)="onChangeField(field)"
        (p-change-model)="onChangeFieldModel(field)"
        [p-placeholder]="field.placeholder"
        [p-field-label]="field.fieldLabel"
        [p-field-value]="field.fieldValue"
        [p-filter-service]="field.optionsService"
        [p-filter-mode]="field.filterMode"
        [p-debounce-time]="field.debounceTime"
        [p-sort]="field.sort"
        [p-placeholder-search]="field.placeholderSearch"
        [p-hide-search]="field.hideSearch"
        [p-hide-select-all]="field.hideSelectAll"
        [p-size]="field.size || componentsSize"
        (p-additional-help)="field.additionalHelp?.($event)"
        (p-keydown)="field.keydown?.($event)"
        [p-listbox-control-position]="field.listboxControlPosition"
      >
      </po-multiselect>
    }
    @if (compareTo(field.control, 'textarea')) {
      <po-textarea
        #component
        [name]="field.property"
        [(ngModel)]="value[field.property]"
        [ngClass]="field.componentClass"
        [additionalHelpEventTrigger]="field.additionalHelp ? 'event' : 'noEvent'"
        [p-additional-help-tooltip]="field.additionalHelpTooltip"
        [p-compact-label]="field.compactLabel"
        [p-disabled]="isDisabled(field)"
        [p-auto-focus]="field.focus"
        [p-help]="field.help"
        [p-helper]="field.helper"
        [p-label]="field.label"
        [p-loading]="field.loading"
        [p-maxlength]="field.maxLength"
        [p-minlength]="field.minLength"
        [p-optional]="field.optional"
        [p-readonly]="field.readonly"
        [p-required]="field.required"
        [p-field-error-message]="field.errorMessage"
        [p-error-limit]="field.errorLimit"
        [p-show-required]="field.showRequired"
        [p-rows]="field.rows"
        [p-size]="field.size || componentsSize"
        (p-additional-help)="field.additionalHelp?.($event)"
        (p-change)="onChangeField(field)"
        (p-change-model)="onChangeFieldModel(field)"
        (p-keydown)="field.keydown?.($event)"
        [p-placeholder]="field.placeholder"
      >
      </po-textarea>
    }
    @if (compareTo(field.control, 'password')) {
      <po-password
        #component
        [name]="field.property"
        [(ngModel)]="value[field.property]"
        [ngClass]="field.componentClass"
        [additionalHelpEventTrigger]="field.additionalHelp ? 'event' : 'noEvent'"
        [p-additional-help-tooltip]="field.additionalHelpTooltip"
        [p-compact-label]="field.compactLabel"
        [p-clean]="field.clean"
        [p-disabled]="isDisabled(field)"
        [p-error-pattern]="field.errorMessage"
        [p-error-limit]="field.errorLimit"
        [p-error-async-properties]="field.errorAsyncProperties"
        [p-auto-focus]="field.focus"
        [p-help]="field.help"
        [p-helper]="field.helper"
        [p-hide-password-peek]="field.hidePasswordPeek"
        [p-icon]="field.icon"
        [p-label]="field.label"
        [p-loading]="field.loading"
        [p-maxlength]="field.maxLength"
        [p-minlength]="field.minLength"
        [p-no-autocomplete]="field.noAutocomplete"
        [p-optional]="field.optional"
        [p-pattern]="field.pattern"
        [p-readonly]="field.readonly"
        [p-required]="field.required"
        [p-required-field-error-message]="field.requiredFieldErrorMessage"
        [p-show-required]="field.showRequired"
        [p-size]="field.size || componentsSize"
        (p-additional-help)="field.additionalHelp?.($event)"
        (p-change)="onChangeField(field)"
        (p-change-model)="onChangeFieldModel(field)"
        (p-keydown)="field.keydown?.($event)"
        [p-placeholder]="field.placeholder"
      >
      </po-password>
    }
    @if (compareTo(field.control, 'upload')) {
      <po-upload
        #component
        [(ngModel)]="value[field.property]"
        [ngClass]="field.componentClass"
        [additionalHelpEventTrigger]="field.additionalHelp ? 'event' : 'noEvent'"
        [p-additional-help-tooltip]="field.additionalHelpTooltip"
        [p-auto-upload]="field.autoUpload"
        [p-compact-label]="field.compactLabel"
        [p-directory]="field.directory"
        [p-disabled]="isDisabled(field)"
        [p-drag-drop]="field.dragDrop"
        [p-drag-drop-height]="field.dragDropHeight"
        [p-restrictions]="field.restrictions"
        [p-form-field]="field.formField"
        [p-headers]="field.headers"
        [p-help]="field.help"
        [p-helper]="field.helper"
        [p-hide-restrictions-info]="field.hideRestrictionsInfo"
        [p-hide-select-button]="field.hideSelectButton"
        [p-hide-send-button]="field.hideSendButton"
        [p-multiple]="field.multiple"
        [p-label]="field.label"
        [p-literals]="field.literals"
        [p-loading]="field.loading"
        [name]="field.property"
        [p-custom-action]="field.customAction"
        (p-custom-action-click)="field.customActionClick($event)"
        (p-error)="field.onError($event)"
        (p-success)="field.onSuccess($event)"
        (p-upload)="field.onUpload($event)"
        [p-optional]="field.optional"
        [p-required]="field.required"
        [p-show-required]="field.showRequired"
        [p-show-thumbnail]="field.showThumbnail ?? true"
        [p-size]="field.size || componentsSize"
        (p-additional-help)="field.additionalHelp?.($event)"
        (p-change)="onChangeField(field)"
        (p-change-model)="onChangeFieldModel(field)"
        (p-keydown)="field.keydown?.($event)"
        [p-url]="field.url"
      >
      </po-upload>
    }
  }
</ng-template>

<ng-template #poContentContainer let-fields>
  <po-dynamic-container [p-fields]="fields" [p-content]="poContent"></po-dynamic-container>
</ng-template>
`
    }]
  }], () => [], {
    components: [{
      type: ViewChildren,
      args: ["component"]
    }]
  });
})();
var PoDynamicFormComponent = class _PoDynamicFormComponent extends PoDynamicFormBaseComponent {
  changes;
  loadService;
  validationService;
  fieldsComponent;
  disabledForm;
  displayAdditionalHelp = false;
  _form;
  onLoadSubscription;
  sendFormSubscription;
  comboOptionSubject = new Subject();
  set form(value) {
    setTimeout(() => {
      this._form = value;
      this.emitForm();
    });
  }
  get form() {
    return this._form || {};
  }
  constructor(changes, loadService, validationService) {
    super();
    this.changes = changes;
    this.loadService = loadService;
    this.validationService = validationService;
  }
  ngOnDestroy() {
    this.removeListeners();
  }
  ngOnInit() {
    if (this.load) {
      this.loadDataOnInitialize();
    }
  }
  /**
   * Função que atribui foco ao campo desejado.
   *
   * Para utilizá-la é necessário capturar a instância do `dynamic form`, como por exemplo:
   *
   * ``` html
   * <po-dynamic-form #dynamicForm [p-fields]="fields"></po-dynamic-form>
   * ```
   *
   * ``` javascript
   * import { PoDynamicFormComponent, PoDynamicFormField } from '@po-ui/ng-components';
   *
   * ...
   *
   * @ViewChild('dynamicForm', { static: true }) dynamicForm: PoDynamicFormComponent;
   *
   * fields: Array<PoDynamicFormField> = [
   *   { property: 'fieldOne' },
   *   { property: 'fieldTwo' }
   * ];
   *
   * fieldFocus() {
   *   this.dynamicForm.focus('fieldTwo');
   * }
   * ```
   *
   * @param {string} property Nome da propriedade atribuída ao `PoDynamicFormField.property`.
   */
  focus(property) {
    this.fieldsComponent.focus(property);
  }
  getObjectValue() {
    return this.comboOptionSubject.asObservable();
  }
  sendObjectValue(objectValue) {
    this.comboOptionSubject.next(objectValue);
  }
  /**
   * Método que exibe `p-helper` ou executa a ação definida em `p-helper{eventOnClick}` ou em `p-additionalHelp`.
   * Para isso, será necessário configurar uma tecla de atalho utilizando o evento `keydown`.
   *
   * ```
   * import { PoDynamicModule } from '@po-ui/ng-components';
   * ...
   * @ViewChild('dynamicForm', { static: true }) dynamicForm: PoDynamicFormComponent;
   *
   * fields: Array<PoDynamicFormField> = [
   *  {
   *    property: 'name',
   *    ...
   *    help: 'Mensagem de ajuda.',
   *    helper: 'Mensagem de ajuda complementar com o componente po-helper implementado.',
   *    keydown: this.onKeyDown.bind(this, 'name')
   *  },
   * ]
   *
   * onKeyDown(property: string, event: KeyboardEvent): void {
   *  if (event.code === 'F9') {
   *    this.dynamicForm.showAdditionalHelp(property);
   *  }
   * }
   * ```
   *
   * @param { string } property Identificador da coluna.
   */
  showAdditionalHelp(property) {
    this.fieldsComponent.showAdditionalHelp(property);
  }
  validateForm(field) {
    const previousFocusElement = document.activeElement;
    this.disableForm(true);
    const errorOnValidation = () => this.disableForm(false);
    this.sendFormSubscription = this.validationService.sendFormChange(this.validate, field, this.value).subscribe(this.applyFormValidation(previousFocusElement), errorOnValidation);
  }
  applyFormUpdatesOnLoad(previousFocusElement) {
    return (dynamicFormData) => {
      this.updateModelOnLoad(dynamicFormData);
      this.disableForm(false);
      this.setFocusOnFieldByProperty(dynamicFormData.focus, previousFocusElement);
    };
  }
  applyFormValidation(previousFocusElement) {
    return (dynamicFormData) => {
      this.updateModelWithValidation(dynamicFormData);
      this.disableForm(false);
      this.setFocusOnFieldByProperty(dynamicFormData.focus, previousFocusElement);
    };
  }
  disableForm(value) {
    this.disabledForm = value;
    this.changes.detectChanges();
  }
  emitForm() {
    if (!this.groupForm && this.formOutput.observers.length) {
      this.formOutput.emit(this.form);
    }
  }
  loadDataOnInitialize() {
    const previousFocusElement = document.activeElement;
    this.disabledForm = true;
    const errorOnLoad = () => this.disabledForm = false;
    this.onLoadSubscription = this.loadService.executeLoad(this.load, this.value).subscribe(this.applyFormUpdatesOnLoad(previousFocusElement), errorOnLoad);
  }
  removeListeners() {
    if (this.onLoadSubscription) {
      this.onLoadSubscription.unsubscribe();
    }
    if (this.sendFormSubscription) {
      this.sendFormSubscription.unsubscribe();
    }
  }
  setFocusOnFieldByProperty(property, previousFocusElement) {
    if (property) {
      setTimeout(() => this.focus(property));
    } else {
      previousFocusElement["focus"]();
    }
  }
  updateModelOnLoad(loadedFormData) {
    Object.assign(this.value, loadedFormData.value);
    this.fields = this.loadService.createAndUpdateFieldsForm(loadedFormData.fields, this.fields);
  }
  updateModelWithValidation(formData) {
    Object.assign(this.value, formData.value);
    this.fieldsComponent.updatePreviousValue();
    this.fields = this.validationService.updateFieldsForm(formData.fields, this.fields);
  }
  static ɵfac = function PoDynamicFormComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoDynamicFormComponent)(ɵɵdirectiveInject(ChangeDetectorRef), ɵɵdirectiveInject(PoDynamicFormLoadService), ɵɵdirectiveInject(PoDynamicFormValidationService));
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoDynamicFormComponent,
    selectors: [["po-dynamic-form"]],
    viewQuery: function PoDynamicFormComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c145, 5)(_c146, 5);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.fieldsComponent = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.form = _t.first);
      }
    },
    standalone: false,
    features: [ɵɵInheritDefinitionFeature],
    decls: 2,
    vars: 1,
    consts: [["fieldsComponent", ""], ["dynamicForm", "ngForm"], [3, "p-auto-focus", "p-components-size", "p-fields", "p-value"], [3, "p-fieldsChange", "p-object-value", "p-form-validate", "p-fields", "p-auto-focus", "p-disabled-form", "p-components-size", "p-validate", "p-validate-fields", "p-validate-on-input", "p-value"]],
    template: function PoDynamicFormComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵconditionalCreate(0, PoDynamicFormComponent_Conditional_0_Template, 2, 4, "po-dynamic-form-fields", 2)(1, PoDynamicFormComponent_Conditional_1_Template, 4, 8, "form");
      }
      if (rf & 2) {
        ɵɵconditional(ctx.groupForm ? 0 : 1);
      }
    },
    dependencies: [ɵNgNoValidate, NgControlStatusGroup, NgForm, PoDynamicFormFieldsComponent],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoDynamicFormComponent, [{
    type: Component,
    args: [{
      selector: "po-dynamic-form",
      standalone: false,
      template: '@if (groupForm) {\n  <po-dynamic-form-fields\n    #fieldsComponent\n    [p-auto-focus]="autoFocus"\n    [p-components-size]="componentsSize"\n    [p-fields]="fields"\n    [p-value]="value"\n  >\n  </po-dynamic-form-fields>\n} @else {\n  <form #dynamicForm="ngForm">\n    <po-dynamic-form-fields\n      #fieldsComponent\n      [(p-fields)]="fields"\n      [p-auto-focus]="autoFocus"\n      [p-disabled-form]="disabledForm"\n      [p-components-size]="componentsSize"\n      [p-validate]="validate"\n      [p-validate-fields]="validateFields"\n      [p-validate-on-input]="validateOnInput"\n      [p-value]="value"\n      (p-object-value)="sendObjectValue($event)"\n      (p-form-validate)="validateForm($event)"\n    >\n    </po-dynamic-form-fields>\n  </form>\n}\n'
    }]
  }], () => [{
    type: ChangeDetectorRef
  }, {
    type: PoDynamicFormLoadService
  }, {
    type: PoDynamicFormValidationService
  }], {
    fieldsComponent: [{
      type: ViewChild,
      args: ["fieldsComponent"]
    }],
    form: [{
      type: ViewChild,
      args: ["dynamicForm"]
    }]
  });
})();
var poDynamicForm_component = Object.freeze({
  __proto__: null,
  PoDynamicFormComponent
});
var PoDynamicViewService = class _PoDynamicViewService {
  httpClient = inject(HttpClient);
  headers = new HttpHeaders({
    "X-PO-No-Message": "true"
  });
  url;
  getObjectByValue(value, filterParams) {
    const validatedFilterParams = this.validateParams(filterParams);
    const encodedValue = encodeURIComponent(value);
    const newURL = `${this.url}/${encodedValue}`;
    return this.httpClient.get(newURL, {
      headers: this.headers,
      params: validatedFilterParams
    }).pipe(map((response) => "items" in response ? response.items : response));
  }
  onLoad(url, value) {
    return this.httpClient.post(url, value).toPromise();
  }
  setConfig(url) {
    this.url = url;
  }
  validateParams(params) {
    return isTypeof(params, "object") && !Array.isArray(params) ? params : void 0;
  }
  static ɵfac = function PoDynamicViewService_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoDynamicViewService)();
  };
  static ɵprov = ɵɵdefineInjectable({
    token: _PoDynamicViewService,
    factory: _PoDynamicViewService.ɵfac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoDynamicViewService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
var PoDynamicViewBaseComponent = class _PoDynamicViewBaseComponent extends PoDynamicSharedBase {
  currencyPipe;
  datePipe;
  decimalPipe;
  timePipe;
  titleCasePipe;
  dynamicViewService;
  comboFilterService;
  multiselectFilterService;
  /**
   * @optional
   *
   * @description
   *
   * Possibilita executar uma função quando o componente é inicializado.
   *
   * A propriedade aceita os seguintes tipos:
   * - **String**: Endpoint usado pelo componente para requisição via `POST`.
   * - **Function**: Método que será executado na inicialização do componente.
   *
   * Para os dois tipos de utilização da propriedade espera-se o seguinte retorno:
   *
   * ```
   * {
   *   value: {
   *     cnpj: '**************', // altera valor do campo
   *     updated: (new Date()).toString() // atribui valor ao campo novo
   *   },
   *   fields: [
   *     { property: 'updated', tag: true } // inclui campo novo
   *   ]
   * }
   * ```
   * > **value**: any = atribui novo valor do model.
   *
   * > **fields**: `Array<PoDynamicViewField>` = Lista de campos que deseja alterar as propriedades,
   * caso enviar um campo a mais será criado um novo campo.
   *
   * - Para esconder/remover campos precisa informar no field a propriedade `visible = false`.
   *
   */
  load;
  service;
  _componentsSize = void 0;
  _initialComponentsSize = void 0;
  _fields = [];
  _showAllValue = false;
  _value = {};
  /**
   * @optional
   *
   * @description
   *
   * Define o tamanho dos componentes no template entre `small` ou `medium`.
   *
   * > Caso a acessibilidade AA não esteja configurada, o tamanho `medium` será mantido.
   * Para mais detalhes, consulte a documentação do [po-theme](https://po-ui.io/documentation/po-theme).
   *
   * @default `medium`
   */
  set componentsSize(value) {
    this._initialComponentsSize = value;
    this.applySizeBasedOnA11y();
  }
  get componentsSize() {
    return this._componentsSize ?? getDefaultSizeFn(PoFieldSize);
  }
  /**
   * @optional
   *
   * @description
   *
   * Lista de objetos que implementam a interface `PoDynamicView`.
   *
   * > Ex: `[ { property: 'age' } ]`
   *
   * Regras de tipagem e formatação dos valores exibidos:
   *
   * - Caso o *type* informado seja *currency* e não seja informado o *format* o mesmo recebe "'BRL', 'symbol', '1.2-2'"
   * como formato padrão.
   * - Caso o *type* informado seja *date* e não seja informado o *format* o mesmo recebe 'dd/MM/yyyy' como formato padrão.
   * - Caso o *type* informado seja *dateTime* e não seja informado o *format* o mesmo recebe 'dd/MM/yyyy HH:mm:ss' como formato padrão.
   * - Caso o *type* informado seja *number* e não seja informado o *format* o mesmo não será formatado.
   * - Caso o *type* informado seja *time* e não seja informado o *format* o mesmo recebe 'HH:mm:ss.ffffff' como formato padrão.
   *
   * > As propriedades informadas serão exibidas mesmo não contendo valor de referência no objeto da propriedade `p-value`.
   *
   * @default `[]`
   */
  set fields(fields) {
    this._fields = Array.isArray(fields) ? [...fields] : [];
  }
  get fields() {
    return this._fields;
  }
  /**
   * @optional
   *
   * @description
   *
   * Indica se exibirá todas as informações contidas dentro do objeto informado na propriedade `p-value`.
   *
   * @default `false`
   */
  set showAllValue(value) {
    this._showAllValue = convertToBoolean(value);
  }
  get showAllValue() {
    return this._showAllValue;
  }
  /**
   * @optional
   *
   * @description
   *
   * Permite a quebra de linha no texto do `p-value`, aplicando-a onde há `\n`.
   *
   * ```
   * <po-dynamic-view
   *   [p-value]="{ description: 'Primeira linha\nSegunda linha' }"
   *   [p-text-wrap]="true"
   * ></po-dynamic-view>
   * ```
   *
   * Saída:
   * ```
   * Primeira linha
   * Segunda linha
   * ```
   * @default `false`
   */
  textWrap = false;
  /**
   * @description
   *
   * Objeto que será utilizado para exibir as informações dinâmicas, o valor será recuperado através do atributo *property*
   * dos objetos contidos na propridade `p-fields`.
   *
   * > Ex: `{ age: '35' }`
   */
  set value(value) {
    this._value = value && isTypeof(value, "object") ? value : {};
  }
  get value() {
    return this._value;
  }
  constructor(currencyPipe, datePipe, decimalPipe, timePipe, titleCasePipe, dynamicViewService, comboFilterService, multiselectFilterService) {
    super();
    this.currencyPipe = currencyPipe;
    this.datePipe = datePipe;
    this.decimalPipe = decimalPipe;
    this.timePipe = timePipe;
    this.titleCasePipe = titleCasePipe;
    this.dynamicViewService = dynamicViewService;
    this.comboFilterService = comboFilterService;
    this.multiselectFilterService = multiselectFilterService;
  }
  onThemeChange() {
    this.applySizeBasedOnA11y();
  }
  getFieldOrderRetroactive(position, index = 1) {
    if (position === index) {
      return position;
    }
    return this.fields.findIndex((field) => field.order === index) > -1 ? this.getFieldOrderRetroactive(position, index + 1) : index;
  }
  getFieldOrder(field, index) {
    const position = this.getFieldOrderRetroactive(index + 1);
    return this.fields.findIndex((e2) => e2.order === position) > -1 ? this.getFieldOrder(field, position) : position;
  }
  getConfiguredFields(useSearchService = true) {
    const newFields = [];
    this.fields.forEach((field, index) => {
      field.order = field.order || this.getFieldOrder(field, index);
      if (!PoDynamicUtil.isVisibleField(field)) {
        return;
      }
      if (!field.searchService && !field.optionsService) {
        newFields.push(this.createField(field));
        return;
      }
      const hasValue = this.value[field.property]?.length || !Array.isArray(this.value[field.property]) && this.value[field.property] && useSearchService || field.container;
      if (hasValue) {
        const _field = this.returnValues(__spreadValues({}, field), "");
        newFields.push(_field);
        if (field.searchService) {
          if (typeof field.searchService === "object") {
            this.service = field.searchService;
          } else if (typeof field.searchService === "string") {
            this.service = this.dynamicViewService;
            this.service.setConfig(field.searchService);
          }
        } else if (field.optionsService) {
          if (field.optionsMulti) {
            if (typeof field.optionsService === "object") {
              this.service = field.optionsService;
            } else {
              this.service = this.multiselectFilterService;
              this.service.configProperties(field.optionsService, field.fieldLabel || "label", field.fieldValue || "value");
            }
          } else {
            if (typeof field.optionsService === "object") {
              this.service = field.optionsService;
            } else {
              this.service = this.comboFilterService;
              this.service.configProperties(field.optionsService, field.fieldLabel || "label", field.fieldValue || "value");
            }
          }
        }
        this.createFieldWithService(field, newFields, _field);
      }
    });
    const _sortedField = sortFields(newFields);
    this.ensureFieldHasContainer(_sortedField);
    return _sortedField;
  }
  // retorna fields ligado ao value mais os atributos do value que não possuiam fields.
  getMergedFields() {
    const mergedFields = [...this.getConfiguredFields()];
    this.getValueFields().forEach((valueField) => {
      const fieldIndex = mergedFields.findIndex((field) => field.property === valueField.property);
      const property = valueField.property;
      if (fieldIndex === -1) {
        mergedFields.push(this.createField({
          property
        }));
      }
    });
    return mergedFields;
  }
  // retorna o objeto value como fields.
  getValueFields() {
    return Object.keys(this.value).map((property) => this.createField({
      property
    }));
  }
  createField(field) {
    const property = field.property;
    let value;
    if (field.isArrayOrObject && this.value[property]) {
      value = this.transformArrayValue(this.value[property], field);
    } else if (field.fieldLabel) {
      value = this.transformFieldLabel(property, field);
    }
    if (!value) {
      value = this.transformValue(field.type, this.value[property], field.format);
    }
    return this.returnValues(field, value);
  }
  createFieldWithService(field, newFields, oldField) {
    const property = field.property;
    this.searchById(this.value[property], field).subscribe((response) => {
      const value = response;
      const allValues = this.returnValues(field, value);
      const oldFieldIndex = newFields.indexOf(newFields.find((field2) => field2 === oldField));
      newFields.splice(oldFieldIndex, 1, allValues);
      sortFields(newFields);
      this.setContainerFields();
    });
  }
  returnValues(field, value) {
    const property = field.property;
    const classesGridColumns = getGridColumnsClasses(field.gridColumns, field.offsetColumns, {
      smGrid: field.gridSmColumns,
      mdGrid: field.gridMdColumns,
      lgGrid: field.gridLgColumns,
      xlGrid: field.gridXlColumns
    }, {
      smOffset: field.offsetSmColumns,
      mdOffset: field.offsetMdColumns,
      lgOffset: field.offsetLgColumns,
      xlOffset: field.offsetXlColumns
    }, {
      smPull: field.gridSmPull,
      mdPull: field.gridMdPull,
      lgPull: field.gridLgPull,
      xlPull: field.gridXlPull
    });
    return __spreadValues({
      property,
      value,
      label: this.titleCasePipe.transform(property),
      cssClass: classesGridColumns
    }, field);
  }
  searchById(value, field) {
    if (typeof value === "string") {
      value = value.trim();
    }
    if (value !== "") {
      if (field.optionsMulti) {
        return this.service.getObjectsByValues(value, field.params).pipe(map((res) => this.transformArrayValue(res, field))).pipe(catchError(() => of(null)));
      } else {
        return this.service.getObjectByValue(value, field.params).pipe(map((res) => this.transformArrayValue(res, field))).pipe(catchError(() => of(null)));
      }
    } else {
      return of(null);
    }
  }
  transformArrayValue(valueProperty, field) {
    const valueArray = Array.isArray(valueProperty) ? valueProperty : [valueProperty];
    let labels;
    if (Array.isArray(field.format)) {
      labels = valueArray.map((objectData) => this.formatField(objectData, field.format));
    } else {
      const arrayWithLabel = valueArray.map((item) => ({
        value: item[field.fieldValue] || item.value,
        label: item[field.fieldLabel] || item.label
      }));
      labels = arrayWithLabel.map((optionValue) => {
        if (optionValue.label) {
          const labelTranformed = this.transformValue(field.type, optionValue.label, field.format);
          if (field.concatLabelValue && optionValue.value) {
            return `${labelTranformed} - ${optionValue.value}`;
          } else {
            return labelTranformed;
          }
        }
      });
    }
    if (labels[0] !== void 0 && labels.join()) {
      return labels.join(", ");
    } else {
      valueProperty = "";
      return void 0;
    }
  }
  transformFieldLabel(property, field) {
    if (field.concatLabelValue && field.fieldLabel && field.fieldValue && !field.isArrayOrObject) {
      const transformedValue = this.transformValue(field.type, this.value[field.fieldLabel], field.format);
      return `${transformedValue} - ${this.value[field.fieldValue]}`;
    }
    if (field.fieldLabel && !field.concatLabelValue && !field.isArrayOrObject) {
      this.value[property] = this.value[field.fieldLabel];
    }
    return void 0;
  }
  transformValue(type, value, format2) {
    let transformedValue = value;
    switch (type) {
      case "currency":
        transformedValue = this.currencyPipe.transform(value, format2 || "BRL", "symbol", "1.2-2");
        break;
      case "date":
        transformedValue = this.datePipe.transform(value, format2 || "dd/MM/yyyy");
        break;
      case "dateTime":
        transformedValue = this.datePipe.transform(value, format2 || "dd/MM/yyyy HH:mm:ss");
        break;
      case "number":
        transformedValue = this.decimalPipe.transform(value, format2);
        break;
      case "time":
        transformedValue = this.timePipe.transform(value, format2 || "HH:mm:ss.ffffff");
        break;
    }
    return transformedValue;
  }
  formatField(objectSelected, properties) {
    let formattedField;
    if (Array.isArray(properties)) {
      for (const property of properties) {
        if (objectSelected && objectSelected[property]) {
          if (!formattedField) {
            formattedField = objectSelected[property];
          } else {
            formattedField += " - " + objectSelected[property];
          }
        }
      }
    }
    return formattedField;
  }
  applySizeBasedOnA11y() {
    const size = validateSizeFn(this._initialComponentsSize, PoFieldSize);
    this._componentsSize = size;
  }
  static ɵfac = function PoDynamicViewBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoDynamicViewBaseComponent)(ɵɵdirectiveInject(CurrencyPipe), ɵɵdirectiveInject(DatePipe), ɵɵdirectiveInject(DecimalPipe), ɵɵdirectiveInject(PoTimePipe), ɵɵdirectiveInject(TitleCasePipe), ɵɵdirectiveInject(PoDynamicViewService), ɵɵdirectiveInject(PoComboFilterService), ɵɵdirectiveInject(PoMultiselectFilterService));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoDynamicViewBaseComponent,
    hostVars: 1,
    hostBindings: function PoDynamicViewBaseComponent_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("PoUiThemeChange", function PoDynamicViewBaseComponent_PoUiThemeChange_HostBindingHandler() {
          return ctx.onThemeChange();
        }, ɵɵresolveWindow);
      }
      if (rf & 2) {
        ɵɵattribute("p-components-size", ctx.componentsSize);
      }
    },
    inputs: {
      load: [0, "p-load", "load"],
      componentsSize: [0, "p-components-size", "componentsSize"],
      fields: [0, "p-fields", "fields"],
      showAllValue: [0, "p-show-all-value", "showAllValue"],
      textWrap: [0, "p-text-wrap", "textWrap"],
      value: [0, "p-value", "value"]
    },
    features: [ɵɵInheritDefinitionFeature]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoDynamicViewBaseComponent, [{
    type: Directive
  }], () => [{
    type: CurrencyPipe
  }, {
    type: DatePipe
  }, {
    type: DecimalPipe
  }, {
    type: PoTimePipe
  }, {
    type: TitleCasePipe
  }, {
    type: PoDynamicViewService
  }, {
    type: PoComboFilterService
  }, {
    type: PoMultiselectFilterService
  }], {
    load: [{
      type: Input,
      args: ["p-load"]
    }],
    componentsSize: [{
      type: Input,
      args: ["p-components-size"]
    }, {
      type: HostBinding,
      args: ["attr.p-components-size"]
    }],
    fields: [{
      type: Input,
      args: ["p-fields"]
    }],
    showAllValue: [{
      type: Input,
      args: ["p-show-all-value"]
    }],
    textWrap: [{
      type: Input,
      args: ["p-text-wrap"]
    }],
    value: [{
      type: Input,
      args: ["p-value"]
    }],
    onThemeChange: [{
      type: HostListener,
      args: ["window:PoUiThemeChange"]
    }]
  });
})();
var PoImageBaseComponent = class _PoImageBaseComponent {
  isBase64 = false;
  _source;
  /**
   * @optional
   *
   * @description
   *
   * Define a altura da imagem em *pixels*. Caso não seja definida,
   * atribui o tamanho da imagem
   */
  height;
  /**
   * @optional
   *
   * @description
   *
   * Defini o texto alternativo descrevendo a imagem.
   */
  alternate;
  /**
   * @optional
   *
   * @description
   *
   * Fonte da imagem que pode ser um caminho local (`./assets/images/logo-black-small.png`)
   * ou um servidor externo (`https://po-ui.io/assets/images/logo-black-small.png`).
   */
  set source(value) {
    if (isValidImageBase64(value)) {
      this.isBase64 = true;
    }
    this._source = value;
  }
  get source() {
    return this._source;
  }
  /**
   * @optional
   *
   * @description
   *
   * Defini a prioridade de carregamento da imagem.
   * > Para as imagens com carregamento prioritátio ativo é necessário incluir
   * > uma tag link no head do arquivo index.html da sua aplicação.
   *
   * ```
   * <link rel="preconnect" href="<url_base_da_imagem>">
   * ```
   *
   * @default `false`
   */
  priority = false;
  /**
   * @optional
   *
   * @description
   *
   * Defini o carregamento que pode ser dos tipo:
   *
   * — lazy
   * — eager
   * — auto
   *
   * > Não é permitido definir esta propriedade em conjunto com a propriedade `p-priority`.
   */
  loading;
  static ɵfac = function PoImageBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoImageBaseComponent)();
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoImageBaseComponent,
    inputs: {
      height: [0, "p-height", "height"],
      alternate: [0, "p-alt", "alternate"],
      source: [0, "p-src", "source"],
      priority: [0, "p-priority", "priority"],
      loading: [0, "p-loading", "loading"]
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoImageBaseComponent, [{
    type: Directive
  }], null, {
    height: [{
      type: Input,
      args: ["p-height"]
    }],
    alternate: [{
      type: Input,
      args: ["p-alt"]
    }],
    source: [{
      type: Input,
      args: ["p-src"]
    }],
    priority: [{
      type: Input,
      args: ["p-priority"]
    }],
    loading: [{
      type: Input,
      args: ["p-loading"]
    }]
  });
})();
var PoImageComponent = class _PoImageComponent extends PoImageBaseComponent {
  static ɵfac = /* @__PURE__ */ (() => {
    let ɵPoImageComponent_BaseFactory;
    return function PoImageComponent_Factory(__ngFactoryType__) {
      return (ɵPoImageComponent_BaseFactory || (ɵPoImageComponent_BaseFactory = ɵɵgetInheritedFactory(_PoImageComponent)))(__ngFactoryType__ || _PoImageComponent);
    };
  })();
  static ɵcmp = ɵɵdefineComponent({
    type: _PoImageComponent,
    selectors: [["po-image"]],
    standalone: false,
    features: [ɵɵInheritDefinitionFeature],
    decls: 1,
    vars: 1,
    consts: [[1, "po-container-image", 3, "p-height"], [1, "po-image", 3, "priority", "loading", "fill", "ngSrc"], [1, "po-image", 3, "loading", "src"]],
    template: function PoImageComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵconditionalCreate(0, PoImageComponent_Conditional_0_Template, 3, 2, "po-container", 0);
      }
      if (rf & 2) {
        ɵɵconditional(ctx.source ? 0 : -1);
      }
    },
    dependencies: [PoContainerComponent, NgOptimizedImage],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoImageComponent, [{
    type: Component,
    args: [{
      selector: "po-image",
      standalone: false,
      template: '@if (source) {\n  <po-container class="po-container-image" [p-height]="height">\n    @if (!isBase64) {\n      <img\n        class="po-image"\n        [attr.alt]="alternate"\n        [priority]="priority"\n        [loading]="loading"\n        [fill]="true"\n        [ngSrc]="source"\n      />\n    } @else {\n      <img class="po-image" [attr.alt]="alternate" [loading]="loading" [src]="source" />\n    }\n  </po-container>\n}\n'
    }]
  }], null, null);
})();
var PoDynamicViewComponent = class _PoDynamicViewComponent extends PoDynamicViewBaseComponent {
  initChanges;
  constructor() {
    const currencyPipe = inject(CurrencyPipe);
    const datePipe = inject(DatePipe);
    const decimalPipe = inject(DecimalPipe);
    const timePipe = inject(PoTimePipe);
    const titleCasePipe = inject(TitleCasePipe);
    const dynamicViewService = inject(PoDynamicViewService);
    const comboFilterService = inject(PoComboFilterService);
    const multiselectFilterService = inject(PoMultiselectFilterService);
    super(currencyPipe, datePipe, decimalPipe, timePipe, titleCasePipe, dynamicViewService, comboFilterService, multiselectFilterService);
  }
  ngOnChanges(changes) {
    if (this.load && !this.initChanges) {
      this.initChanges = false;
    } else {
      this.initChanges = true;
    }
    if ((changes.fields || changes.value || changes.showAllValue) && this.initChanges) {
      this.visibleFields = this.getVisibleFields();
      this.setContainerFields();
    }
  }
  ngOnInit() {
    if (this.load) {
      this.updateValuesAndFieldsOnLoad().finally(() => {
        this.setContainerFields();
      });
    }
  }
  setFieldValue(field) {
    if (field.options) {
      if (field.optionsMulti) {
        const selectedOptions = field.options.filter((option) => Array.isArray(field.value) && field.value.some((value) => value === option.value));
        return selectedOptions.length ? selectedOptions.map((option) => option.label) : field.value;
      } else {
        const selectedOption = field.options.find((option) => option.value === field.value);
        return selectedOption ? selectedOption.label : field.value;
      }
    } else if (field.type === "boolean" && "booleanTrue" in field && "booleanFalse" in field) {
      return field.value ? field.booleanTrue : field.booleanFalse;
    } else {
      return field.value;
    }
  }
  containsLineBreak(value) {
    return value && value.includes("\n");
  }
  getValuesAndFieldsFromLoad() {
    return __async(this, null, function* () {
      let valueAndFieldsFromLoad;
      if (typeof this.load === "string") {
        valueAndFieldsFromLoad = yield this.dynamicViewService.onLoad(this.load, this.value);
      } else if (typeof this.load === "function") {
        valueAndFieldsFromLoad = this.load();
      }
      return valueAndFieldsFromLoad || {};
    });
  }
  getVisibleFields() {
    if (this.showAllValue) {
      return this.getMergedFields();
    }
    return this.value && this.fields.length ? this.getConfiguredFields() : this.getValueFields();
  }
  setFieldOnLoad(fieldOnLoad) {
    const index = this.fields.findIndex((field) => field.property === fieldOnLoad.property);
    if (index >= 0) {
      this.fields[index] = __spreadValues(__spreadValues({}, this.fields[index]), fieldOnLoad);
    } else {
      this.fields.push(__spreadValues({}, fieldOnLoad));
    }
  }
  setFieldsOnLoad(fields) {
    if (fields) {
      fields.forEach((fieldOnLoad) => {
        this.setFieldOnLoad(fieldOnLoad);
      });
    }
  }
  setValueOnLoad(newValue) {
    Object.assign(this.value, newValue);
  }
  updateValuesAndFieldsOnLoad() {
    return __async(this, null, function* () {
      const {
        value,
        fields
      } = yield this.getValuesAndFieldsFromLoad();
      this.setValueOnLoad(value);
      this.setFieldsOnLoad(fields);
      this.visibleFields = this.getVisibleFields();
      this.initChanges = true;
    });
  }
  static ɵfac = function PoDynamicViewComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoDynamicViewComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoDynamicViewComponent,
    selectors: [["po-dynamic-view"]],
    standalone: false,
    features: [ɵɵInheritDefinitionFeature, ɵɵNgOnChangesFeature],
    decls: 11,
    vars: 1,
    consts: [["poContent", ""], ["poContentContainer", ""], ["poInfo", ""], ["poTag", ""], ["poImage", ""], [1, "po-row", "po-dynamic-view", 3, "po-dynamic"], [1, "po-row", "po-dynamic-view"], [4, "ngTemplateOutlet", "ngTemplateOutletContext"], [1, "po-sm-12", 3, "p-label"], [3, "p-fields", "p-content"], [3, "ngClass", "p-label", "p-size", "p-value"], [3, "ngClass", "p-color", "p-icon", "p-label", "p-text-color", "p-value"], [3, "p-alt", "p-height", "p-src"]],
    template: function PoDynamicViewComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵconditionalCreate(0, PoDynamicViewComponent_Conditional_0_Template, 2, 6, "div", 5);
        ɵɵtemplate(1, PoDynamicViewComponent_ng_template_1_Template, 2, 0, "ng-template", null, 0, ɵɵtemplateRefExtractor)(3, PoDynamicViewComponent_ng_template_3_Template, 1, 2, "ng-template", null, 1, ɵɵtemplateRefExtractor)(5, PoDynamicViewComponent_ng_template_5_Template, 1, 6, "ng-template", null, 2, ɵɵtemplateRefExtractor)(7, PoDynamicViewComponent_ng_template_7_Template, 1, 6, "ng-template", null, 3, ɵɵtemplateRefExtractor)(9, PoDynamicViewComponent_ng_template_9_Template, 1, 3, "ng-template", null, 4, ɵɵtemplateRefExtractor);
      }
      if (rf & 2) {
        ɵɵconditional(ctx.visibleFields.length ? 0 : -1);
      }
    },
    dependencies: [NgClass, NgTemplateOutlet, PoDividerComponent, PoInfoComponent, PoTagComponent, PoImageComponent, PoDynamicContainerComponent],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoDynamicViewComponent, [{
    type: Component,
    args: [{
      selector: "po-dynamic-view",
      standalone: false,
      template: '@if (visibleFields.length) {\n  <div class="po-row po-dynamic-view" [class.po-dynamic]="hasContainers">\n    <ng-container\n      *ngTemplateOutlet="\n        hasContainers ? poContentContainer : poContent;\n        context: { $implicit: hasContainers ? containerFields : visibleFields }\n      "\n    ></ng-container>\n  </div>\n}\n\n<ng-template #poContent let-fields>\n  @for (field of fields; track field) {\n    @if (field?.divider?.trim()) {\n      <po-divider class="po-sm-12" [p-label]="field.divider"> </po-divider>\n    }\n    <ng-container\n      *ngTemplateOutlet="\n        !field.tag && !field.image ? poInfo : !field.image ? poTag : poImage;\n        context: { $implicit: field }\n      "\n    >\n    </ng-container>\n  }\n</ng-template>\n\n<ng-template #poContentContainer let-fields>\n  <po-dynamic-container [p-fields]="fields" [p-content]="poContent"></po-dynamic-container>\n</ng-template>\n\n<ng-template #poInfo let-field>\n  <po-info\n    [ngClass]="field.cssClass"\n    [class.po-info-value-pre]="textWrap && containsLineBreak(setFieldValue(field))"\n    [p-label]="field.label"\n    [p-size]="componentsSize"\n    [p-value]="setFieldValue(field)"\n  >\n  </po-info\n></ng-template>\n\n<ng-template #poTag let-field>\n  <po-tag\n    [ngClass]="field.cssClass"\n    [p-color]="field.color"\n    [p-icon]="field.icon"\n    [p-label]="field.label"\n    [p-text-color]="field.textColor"\n    [p-value]="setFieldValue(field)"\n  >\n  </po-tag>\n</ng-template>\n\n<ng-template #poImage let-field>\n  <po-image [p-alt]="field.alt" [p-height]="field.height" [p-src]="field.value"> </po-image>\n</ng-template>\n'
    }]
  }], () => [], null);
})();
var PoImageModule = class _PoImageModule {
  static ɵfac = function PoImageModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoImageModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoImageModule,
    declarations: [PoImageComponent],
    imports: [CommonModule, PoContainerModule, NgOptimizedImage],
    exports: [PoImageComponent]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [CommonModule, PoContainerModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoImageModule, [{
    type: NgModule,
    args: [{
      declarations: [PoImageComponent],
      imports: [CommonModule, PoContainerModule, NgOptimizedImage],
      exports: [PoImageComponent]
    }]
  }], null, null);
})();
var PoDynamicModule = class _PoDynamicModule {
  static ɵfac = function PoDynamicModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoDynamicModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoDynamicModule,
    declarations: [PoDynamicFormComponent, PoDynamicFormFieldsComponent, PoDynamicViewComponent, PoDynamicContainerComponent],
    imports: [CommonModule, FormsModule, PoDividerModule, PoInfoModule, PoFieldModule, PoTagModule, PoTimeModule, PoImageModule, PoContainerModule],
    exports: [PoDynamicFormComponent, PoDynamicViewComponent, PoDynamicContainerComponent]
  });
  static ɵinj = ɵɵdefineInjector({
    providers: [CurrencyPipe, DatePipe, DecimalPipe, PoTimePipe, TitleCasePipe, PoDynamicFormLoadService, PoDynamicFormValidationService, PoDynamicViewService, PoComboFilterService, PoMultiselectFilterService],
    imports: [CommonModule, FormsModule, PoDividerModule, PoInfoModule, PoFieldModule, PoTagModule, PoTimeModule, PoImageModule, PoContainerModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoDynamicModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, FormsModule, PoDividerModule, PoInfoModule, PoFieldModule, PoTagModule, PoTimeModule, PoImageModule, PoContainerModule],
      declarations: [PoDynamicFormComponent, PoDynamicFormFieldsComponent, PoDynamicViewComponent, PoDynamicContainerComponent],
      exports: [PoDynamicFormComponent, PoDynamicViewComponent, PoDynamicContainerComponent],
      providers: [CurrencyPipe, DatePipe, DecimalPipe, PoTimePipe, TitleCasePipe, PoDynamicFormLoadService, PoDynamicFormValidationService, PoDynamicViewService, PoComboFilterService, PoMultiselectFilterService]
    }]
  }], null, null);
})();
var PoFilterChipBaseComponent = class _PoFilterChipBaseComponent {
  /**
   * @optional
   *
   * @description
   *
   * Define se o chip está desabilitado, impedindo qualquer interação do usuário.
   *
   * Quando habilitado, o chip não responde a cliques nem a eventos de teclado (Enter/Space).
   *
   * @default `false`
   */
  disabled = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "disabled"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-disabled",
    transform: convertToBoolean
  }));
  /**
   * @optional
   *
   * @description
   *
   * Define o rótulo de texto exibido no chip.
   */
  label = input(void 0, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "label"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-label"
  }));
  /**
   * @optional
   *
   * @description
   *
   * Define o estado de seleção do chip.
   *
   * @default `false`
   */
  selected = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "selected"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-selected",
    transform: convertToBoolean
  }));
  /**
   * @optional
   *
   * @description
   *
   * Evento disparado após a alteração do estado de seleção do *chip*. Retorna o objeto PoFilterChipSelectedChange
   * modificado.
   */
  selectedChange = output({
    alias: "p-selected-change"
  });
  static ɵfac = function PoFilterChipBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoFilterChipBaseComponent)();
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoFilterChipBaseComponent,
    inputs: {
      disabled: [1, "p-disabled", "disabled"],
      label: [1, "p-label", "label"],
      selected: [1, "p-selected", "selected"]
    },
    outputs: {
      selectedChange: "p-selected-change"
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoFilterChipBaseComponent, [{
    type: Directive
  }], null, {
    disabled: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-disabled",
        required: false
      }]
    }],
    label: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-label",
        required: false
      }]
    }],
    selected: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-selected",
        required: false
      }]
    }],
    selectedChange: [{
      type: Output,
      args: ["p-selected-change"]
    }]
  });
})();
var PoFilterChipComponent = class _PoFilterChipComponent extends PoFilterChipBaseComponent {
  _selected = signal(false, ...ngDevMode ? [{
    debugName: "_selected"
  }] : (
    /* istanbul ignore next */
    []
  ));
  initialized = false;
  isSelected = computed(() => this._selected(), ...ngDevMode ? [{
    debugName: "isSelected"
  }] : (
    /* istanbul ignore next */
    []
  ));
  constructor() {
    super();
    this.setupSelectedEffect();
  }
  onClick() {
    this.toggle();
  }
  onKeydown(event) {
    if (this.disabled()) {
      return;
    }
    event.preventDefault();
    this.toggle();
  }
  setupSelectedEffect() {
    effect(() => {
      const value = this.selected();
      this._selected.set(value);
      if (this.initialized) {
        this.selectedChange.emit({
          label: this.label() || "",
          selected: value
        });
      }
      this.initialized = true;
    });
  }
  toggle() {
    if (this.disabled()) {
      return;
    }
    const newValue = !this._selected();
    this._selected.set(newValue);
    this.selectedChange.emit({
      label: this.label() || "",
      selected: newValue
    });
  }
  static ɵfac = function PoFilterChipComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoFilterChipComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoFilterChipComponent,
    selectors: [["po-filter-chip"]],
    features: [ɵɵInheritDefinitionFeature],
    decls: 4,
    vars: 9,
    consts: [["role", "option", "aria-selected", "false", 1, "po-filter-chip-wrapper", 3, "click", "keydown.enter", "keydown.space"], ["p-icon", "ICON_OK", 1, "po-filter-chip-icon"], [1, "po-filter-chip-label"]],
    template: function PoFilterChipComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "span", 0);
        ɵɵlistener("click", function PoFilterChipComponent_Template_span_click_0_listener() {
          return ctx.onClick();
        })("keydown.enter", function PoFilterChipComponent_Template_span_keydown_enter_0_listener($event) {
          return ctx.onKeydown($event);
        })("keydown.space", function PoFilterChipComponent_Template_span_keydown_space_0_listener($event) {
          return ctx.onKeydown($event);
        });
        ɵɵconditionalCreate(1, PoFilterChipComponent_Conditional_1_Template, 1, 0, "po-icon", 1);
        ɵɵelementStart(2, "span", 2);
        ɵɵtext(3);
        ɵɵelementEnd()();
      }
      if (rf & 2) {
        ɵɵclassProp("po-filter-chip-selected", ctx.isSelected())("po-filter-chip-disabled", ctx.disabled());
        ɵɵattribute("aria-selected", ctx.isSelected())("aria-disabled", ctx.disabled() || null)("tabindex", ctx.disabled() ? -1 : 0);
        ɵɵadvance();
        ɵɵconditional(ctx.isSelected() ? 1 : -1);
        ɵɵadvance(2);
        ɵɵtextInterpolate(ctx.label());
      }
    },
    dependencies: [PoIconModule, PoIconComponent],
    encapsulation: 2,
    changeDetection: 0
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoFilterChipComponent, [{
    type: Component,
    args: [{
      selector: "po-filter-chip",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: true,
      imports: [PoIconModule],
      template: '<span\n  class="po-filter-chip-wrapper"\n  role="option"\n  aria-selected="false"\n  [attr.aria-selected]="isSelected()"\n  [attr.aria-disabled]="disabled() || null"\n  [attr.tabindex]="disabled() ? -1 : 0"\n  [class.po-filter-chip-selected]="isSelected()"\n  [class.po-filter-chip-disabled]="disabled()"\n  (click)="onClick()"\n  (keydown.enter)="onKeydown($event)"\n  (keydown.space)="onKeydown($event)"\n>\n  @if (isSelected()) {\n    <po-icon class="po-filter-chip-icon" p-icon="ICON_OK"></po-icon>\n  }\n  <span class="po-filter-chip-label">{{ label() }}</span>\n</span>\n'
    }]
  }], () => [], null);
})();
var PoFilterChipModule = class _PoFilterChipModule {
  static ɵfac = function PoFilterChipModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoFilterChipModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoFilterChipModule,
    imports: [PoFilterChipComponent],
    exports: [PoFilterChipComponent]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [PoFilterChipComponent]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoFilterChipModule, [{
    type: NgModule,
    args: [{
      imports: [PoFilterChipComponent],
      exports: [PoFilterChipComponent]
    }]
  }], null, null);
})();
var poGaugeMinHeight = 300;
var PoGaugeBaseComponent = class _PoGaugeBaseComponent {
  _description;
  _height = poGaugeMinHeight;
  _ranges = [];
  _title;
  _value;
  /**
   * @optional
   *
   * @description
   *
   * Define a exibição dos valores de `from` - `to` entre parênteses caso haja definição de `p-ranges`.
   *
   * @default `false`
   */
  showFromToLegend = false;
  /**
   * @optional
   *
   * @description
   *
   * Define a exibição do ponteiro caso haja definição de `p-ranges`.
   *
   * @default `true`
   */
  showPointer = true;
  /**
   * @optional
   *
   * @description
   *
   * Objeto com as configurações usadas no `po-gauge`.
   *
   * É possível, por exemplo, esconder as funcionalidades do header,
   * ou habilitar uma legenda com `From` `To` da seguinte forma:
   *
   * ```
   *  chartOptions: PoGaugeOptions = {
   *    showFromToLegend: true,
   *    header: {
   *      hideExpand: true,
   *    },
   *  };
   * ```
   */
  options;
  /**
   * @optional
   *
   * @description
   *
   * Define o texto que será exibido no gauge.
   * Há dois posicionamentos para ele:
   * - Se houver definição para `p-ranges`, o descritivo será exibido no topo do container, ficando acima do gauge;
   * - Na ausência de `p-ranges`, será incorporado dentro do arco do gauge, e abaixo de `p-value`.
   *
   * > Para uma melhor experiência do usuário, é recomendado um descritivo breve e com poucas palavras.
   * > Indicamos a utilização da nova propriedade `descriptionChart` em `p-options`.
   */
  set description(value) {
    this._description = value;
  }
  get description() {
    return this._description;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define a altura do gauge.
   *
   * O valor mínimo aceito é 300px.
   *
   * @default `300px`
   */
  set height(value) {
    this._height = this.setGaugeHeight(value);
  }
  get height() {
    return this._height;
  }
  /**
   * @optional
   *
   * @description
   *
   * Definição para o alcance de cores. Ao adicionar pelo menos um item na lista,
   * incorpora-se o ponteiro que assinala o valor passado em `p-value`.
   * Se o valor de `p-value` for inferior ao mínimo valor definido em `PoGaugeRanges.from`, o domínio mínimo do gauge será `p-value`.
   * A mesma regra prevalece para valores máximos.
   */
  set ranges(value) {
    this._ranges = Array.isArray(value) ? value : [];
  }
  get ranges() {
    return this._ranges;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define o título do gauge.
   */
  set title(value) {
    this._title = value;
  }
  get title() {
    return this._title;
  }
  /**
   * @optional
   *
   * @description
   *
   * Valor referente ao valor da série. Seu comportamento segue a seguintes regras:
   *
   * - Sem `p-ranges`: Os valores passados para `p-value` e `p-description` serão centralizados no interior do arco.
   * A base do valor será percentual tendo como base os alcances entre zero e 100%. Se passado um valor superior a 100,
   * A colorização do gauge será completa e o valor passado será exibido no interior do arco.
   * - Com `p-ranges`: A descrição será exibida acima do gauge e haverá um ponteiro marcando o valor passado em `p-value`.
   * Considerando que o alcance em `ranges` é aberto, então a escala de `p-value` será em relação ao menor/maior alcance
   * absoluto definido em `p-ranges`.
   * Se passado um `p-value` inferior em relação ao mínimo valor definido em `p-ranges`, o domínio mínimo do gauge partirá de `p-value`.
   * A mesma regra prevalece para valores máximos.
   *
   */
  set value(gaugeValue) {
    if (typeof gaugeValue === "number" || typeof gaugeValue === "string" && gaugeValue.trim()) {
      const value = !isNaN(gaugeValue) ? Number(gaugeValue) : void 0;
      this._value = value;
    } else {
      this._value = void 0;
    }
  }
  get value() {
    return this._value;
  }
  setGaugeHeight(height) {
    const gaugeHeight = convertToInt(height);
    return gaugeHeight && gaugeHeight > poGaugeMinHeight ? gaugeHeight : poGaugeMinHeight;
  }
  static ɵfac = function PoGaugeBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoGaugeBaseComponent)();
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoGaugeBaseComponent,
    inputs: {
      showFromToLegend: [0, "p-show-from-to-legend", "showFromToLegend"],
      showPointer: [0, "p-show-pointer", "showPointer"],
      options: [0, "p-options", "options"],
      description: [0, "p-description", "description"],
      height: [0, "p-height", "height"],
      ranges: [0, "p-ranges", "ranges"],
      title: [0, "p-title", "title"],
      value: [0, "p-value", "value"]
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoGaugeBaseComponent, [{
    type: Directive
  }], null, {
    showFromToLegend: [{
      type: Input,
      args: ["p-show-from-to-legend"]
    }],
    showPointer: [{
      type: Input,
      args: ["p-show-pointer"]
    }],
    options: [{
      type: Input,
      args: ["p-options"]
    }],
    description: [{
      type: Input,
      args: ["p-description"]
    }],
    height: [{
      type: Input,
      args: ["p-height"]
    }],
    ranges: [{
      type: Input,
      args: ["p-ranges"]
    }],
    title: [{
      type: Input,
      args: ["p-title"]
    }],
    value: [{
      type: Input,
      args: ["p-value"]
    }]
  });
})();
var PoGaugeComponent = class _PoGaugeComponent extends PoGaugeBaseComponent {
  series = [];
  valuesMultiple;
  ngOnInit() {
    if (this.options?.showContainerGauge !== false) {
      this.options = __spreadProps(__spreadValues({}, this.options), {
        showContainerGauge: true
      });
    }
  }
  ngOnChanges(changes) {
    const {
      description,
      ranges,
      value,
      options
    } = changes;
    if (description || options) {
      this.options = __spreadProps(__spreadValues({}, this.options), {
        showContainerGauge: this.options?.showContainerGauge !== false,
        descriptionChart: this.description || options?.currentValue?.descriptionChart
      });
    }
    if (ranges || value) {
      if (this.ranges?.length) {
        this.series = [...this.ranges];
        this.valuesMultiple = this.value;
      } else if (value?.currentValue) {
        this.series = [{
          data: this.value
        }];
      } else {
        this.series = [];
      }
    }
  }
  static ɵfac = /* @__PURE__ */ (() => {
    let ɵPoGaugeComponent_BaseFactory;
    return function PoGaugeComponent_Factory(__ngFactoryType__) {
      return (ɵPoGaugeComponent_BaseFactory || (ɵPoGaugeComponent_BaseFactory = ɵɵgetInheritedFactory(_PoGaugeComponent)))(__ngFactoryType__ || _PoGaugeComponent);
    };
  })();
  static ɵcmp = ɵɵdefineComponent({
    type: _PoGaugeComponent,
    selectors: [["po-gauge"]],
    standalone: false,
    features: [ɵɵInheritDefinitionFeature, ɵɵNgOnChangesFeature],
    decls: 1,
    vars: 6,
    consts: [[3, "p-height", "p-options", "p-series", "p-title", "p-type", "p-value-gauge-multiple"]],
    template: function PoGaugeComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelement(0, "po-chart", 0);
      }
      if (rf & 2) {
        ɵɵproperty("p-height", ctx.height)("p-options", ctx.options)("p-series", ctx.series)("p-title", ctx.title)("p-type", "gauge")("p-value-gauge-multiple", ctx.valuesMultiple);
      }
    },
    dependencies: [PoChartComponent],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoGaugeComponent, [{
    type: Component,
    args: [{
      selector: "po-gauge",
      standalone: false,
      template: `<po-chart
  [p-height]="height"
  [p-options]="options"
  [p-series]="series"
  [p-title]="title"
  [p-type]="'gauge'"
  [p-value-gauge-multiple]="valuesMultiple"
></po-chart>
`
    }]
  }], null, null);
})();
var PoGaugeModule = class _PoGaugeModule {
  static ɵfac = function PoGaugeModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoGaugeModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoGaugeModule,
    declarations: [PoGaugeComponent],
    imports: [CommonModule, PoChartModule],
    exports: [PoGaugeComponent]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [CommonModule, PoChartModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoGaugeModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, PoChartModule],
      declarations: [PoGaugeComponent],
      exports: [PoGaugeComponent]
    }]
  }], null, null);
})();
var PoGridCellActionComponent = class _PoGridCellActionComponent {
  position = "";
  value;
  constructor() {
  }
  onKeyDownContent(event) {
    if (event.keyCode === 13) {
      event.preventDefault();
    }
  }
  static ɵfac = function PoGridCellActionComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoGridCellActionComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoGridCellActionComponent,
    selectors: [["po-grid-cell-action"]],
    inputs: {
      position: [0, "p-position", "position"],
      value: [0, "p-value", "value"]
    },
    standalone: false,
    decls: 5,
    vars: 2,
    consts: [["contentElement", ""], ["tabindex", "0", 1, "po-grid-cell-action-content", 3, "keydown", "id"]],
    template: function PoGridCellActionComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "div")(1, "div", 1, 0);
        ɵɵlistener("keydown", function PoGridCellActionComponent_Template_div_keydown_1_listener($event) {
          return ctx.onKeyDownContent($event);
        });
        ɵɵelementStart(3, "span");
        ɵɵtext(4);
        ɵɵelementEnd()()();
      }
      if (rf & 2) {
        ɵɵadvance();
        ɵɵproperty("id", ctx.position);
        ɵɵadvance(3);
        ɵɵtextInterpolate(ctx.value || " ");
      }
    },
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoGridCellActionComponent, [{
    type: Component,
    args: [{
      selector: "po-grid-cell-action",
      standalone: false,
      template: `<div>
  <div
    #contentElement
    [id]="position"
    class="po-grid-cell-action-content"
    tabindex="0"
    (keydown)="onKeyDownContent($event)"
  >
    <span>{{ value || '&nbsp;' }}</span>
  </div>
</div>
`
    }]
  }], () => [], {
    position: [{
      type: Input,
      args: ["p-position"]
    }],
    value: [{
      type: Input,
      args: ["p-value"]
    }]
  });
})();
var PoGridCellComponent = class _PoGridCellComponent {
  changeDetectorRef = inject(ChangeDetectorRef);
  align = "left";
  freeze = false;
  tabIndex = -1;
  position = "";
  readonly = false;
  required = false;
  valueChange = new EventEmitter();
  inputElement;
  contentElement;
  _value = "";
  edit = false;
  editValue = "";
  _width;
  set width(value) {
    this._width = value;
    if (this.freeze && !this._width) {
      this._width = 100;
    }
  }
  get width() {
    return this._width ? `${this._width}px` : "100%";
  }
  set value(value) {
    this._value = value;
    this.valueChange.emit(this._value);
  }
  get value() {
    return this._value;
  }
  onKeyDownContent(event) {
    if (!event.ctrlKey && (event.keyCode === 8 || event.keyCode === 46)) {
      if (this.readonly) {
        return;
      }
      event.preventDefault();
      this.value = "";
      return;
    }
    if (event.keyCode === 13) {
      event.preventDefault();
      this.onEditCell(this.value);
      return;
    }
    if (event.keyCode >= 65 && event.keyCode <= 90 || event.keyCode >= 48 && event.keyCode <= 57) {
      event.preventDefault();
      this.onEditCell(event.key);
    }
  }
  dblclick(event) {
    event.preventDefault();
    this.onEditCell(this.value);
  }
  onBlurInput() {
    this.value = this.editValue;
    this.editValue = void 0;
    this.edit = false;
  }
  onKeyDownInput(event) {
    if (event.keyCode === 13) {
      event.target.blur();
      this.changeDetectorRef.detectChanges();
      this.contentElement.nativeElement.focus();
    }
    if (event.keyCode === 27) {
      this.editValue = void 0;
      this.edit = false;
      this.changeDetectorRef.detectChanges();
      this.contentElement.nativeElement.focus();
      event.stopPropagation();
    }
  }
  onEditCell(value) {
    if (this.readonly) {
      return;
    }
    this.editValue = value;
    this.edit = true;
    this.changeDetectorRef.detectChanges();
    this.inputElement.nativeElement.focus();
  }
  static ɵfac = function PoGridCellComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoGridCellComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoGridCellComponent,
    selectors: [["po-grid-cell"]],
    viewQuery: function PoGridCellComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c22, 5)(_c46, 5);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.inputElement = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.contentElement = _t.first);
      }
    },
    inputs: {
      align: [0, "p-align", "align"],
      freeze: [0, "p-freeze", "freeze"],
      tabIndex: [0, "p-tab-index", "tabIndex"],
      position: [0, "p-position", "position"],
      readonly: [0, "p-readonly", "readonly"],
      required: [0, "p-required", "required"],
      width: [0, "p-width", "width"],
      value: [0, "p-value", "value"]
    },
    outputs: {
      valueChange: "p-valueChange"
    },
    standalone: false,
    decls: 3,
    vars: 1,
    consts: [["inputElement", ""], ["contentElement", ""], [1, "po-grid-cell-input", 3, "id", "ngModel"], [3, "id", "class", "tabindex"], [1, "po-grid-cell-input", 3, "ngModelChange", "keydown.arrowup", "keydown.arrowdown", "keydown.arrowleft", "keydown.arrowright", "keydown", "blur", "id", "ngModel"], [3, "keydown", "dblclick", "id", "tabindex"]],
    template: function PoGridCellComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "div");
        ɵɵconditionalCreate(1, PoGridCellComponent_Conditional_1_Template, 2, 2, "input", 2)(2, PoGridCellComponent_Conditional_2_Template, 4, 8, "div", 3);
        ɵɵelementEnd();
      }
      if (rf & 2) {
        ɵɵadvance();
        ɵɵconditional(ctx.edit ? 1 : 2);
      }
    },
    dependencies: [DefaultValueAccessor, NgControlStatus, NgModel],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoGridCellComponent, [{
    type: Component,
    args: [{
      selector: "po-grid-cell",
      standalone: false,
      template: `<div>
  @if (edit) {
    <input
      [id]="position"
      #inputElement
      class="po-grid-cell-input"
      [(ngModel)]="editValue"
      (keydown.arrowup)="$event.stopPropagation()"
      (keydown.arrowdown)="$event.stopPropagation()"
      (keydown.arrowleft)="$event.stopPropagation()"
      (keydown.arrowright)="$event.stopPropagation()"
      (keydown)="onKeyDownInput($event)"
      (blur)="onBlurInput()"
    />
  } @else {
    <div
      #contentElement
      [id]="position"
      class="po-grid-cell-content po-grid-cell-{{ align }} {{ !value && required ? 'po-grid-cell-required' : '' }}"
      (keydown)="onKeyDownContent($event)"
      tabindex="{{ readonly ? -1 : 0 }}"
      (dblclick)="dblclick($event)"
    >
      <span>{{ value || '&nbsp;' }}</span>
    </div>
  }
</div>
`
    }]
  }], null, {
    align: [{
      type: Input,
      args: ["p-align"]
    }],
    freeze: [{
      type: Input,
      args: ["p-freeze"]
    }],
    tabIndex: [{
      type: Input,
      args: ["p-tab-index"]
    }],
    position: [{
      type: Input,
      args: ["p-position"]
    }],
    readonly: [{
      type: Input,
      args: ["p-readonly"]
    }],
    required: [{
      type: Input,
      args: ["p-required"]
    }],
    valueChange: [{
      type: Output,
      args: ["p-valueChange"]
    }],
    inputElement: [{
      type: ViewChild,
      args: ["inputElement"]
    }],
    contentElement: [{
      type: ViewChild,
      args: ["contentElement"]
    }],
    width: [{
      type: Input,
      args: ["p-width"]
    }],
    value: [{
      type: Input,
      args: ["p-value"]
    }]
  });
})();
var PoGridHeadComponent = class _PoGridHeadComponent {
  align = "left";
  boolean = false;
  position = "";
  tabIndex = -1;
  title;
  _width;
  set width(value) {
    this._width = value;
  }
  get width() {
    return this._width ? `${this._width}px` : "100%";
  }
  static ɵfac = function PoGridHeadComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoGridHeadComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoGridHeadComponent,
    selectors: [["po-grid-head"]],
    inputs: {
      align: [0, "p-align", "align"],
      boolean: [0, "p-freeze", "boolean"],
      position: [0, "p-position", "position"],
      tabIndex: [0, "p-tab-index", "tabIndex"],
      title: [0, "p-title", "title"],
      width: [0, "p-width", "width"]
    },
    standalone: false,
    decls: 4,
    vars: 7,
    consts: [[3, "id", "tabindex"]],
    template: function PoGridHeadComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "div")(1, "div", 0)(2, "span");
        ɵɵtext(3);
        ɵɵelementEnd()()();
      }
      if (rf & 2) {
        ɵɵadvance();
        ɵɵclassMap(ɵɵinterpolate1("po-grid-head-content po-grid-head-", ctx.align));
        ɵɵproperty("tabindex", ɵɵinterpolate(ctx.title ? 0 : -1))("id", ctx.position);
        ɵɵadvance(2);
        ɵɵtextInterpolate(ctx.title || " ");
      }
    },
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoGridHeadComponent, [{
    type: Component,
    args: [{
      selector: "po-grid-head",
      standalone: false,
      template: `<div>
  <div [id]="position" class="po-grid-head-content po-grid-head-{{ align }}" tabindex="{{ title ? 0 : -1 }}">
    <span>{{ title || '&nbsp;' }}</span>
  </div>
</div>
`
    }]
  }], null, {
    align: [{
      type: Input,
      args: ["p-align"]
    }],
    boolean: [{
      type: Input,
      args: ["p-freeze"]
    }],
    position: [{
      type: Input,
      args: ["p-position"]
    }],
    tabIndex: [{
      type: Input,
      args: ["p-tab-index"]
    }],
    title: [{
      type: Input,
      args: ["p-title"]
    }],
    width: [{
      type: Input,
      args: ["p-width"]
    }]
  });
})();
var PoGridComponent = class _PoGridComponent {
  changeDetectorRef = inject(ChangeDetectorRef);
  elRef = inject(ElementRef);
  tableElement;
  tableWrapper;
  /**
   * @description
   *
   * Ações disparadas quando uma linha do grid é manipulada.
   */
  rowActions = {};
  /**
   * Lista com os dados que serão exibidos no grid.
   */
  data = [];
  lastCell = "0-0";
  lastRow = 0;
  lastColumn = 0;
  currencyCell = "0-0";
  currencyRow = 0;
  currencyColumn = 0;
  currencyObj;
  logger = false;
  width = "100%";
  widporeeze = 0;
  widthActions = 0;
  resizeListener;
  timeoutResize;
  _columns = [];
  /**
   * Colunas exibidas no grid.
   */
  set columns(value) {
    this._columns = [...value];
    this._columns.forEach((column) => {
      column.label = column.label || column.property;
      if (column.freeze === true) {
        column.cssWidth = `${column.width || 100}px`;
      } else {
        column.cssWidth = column.width ? `${column.width}px` : "100%";
      }
    });
  }
  get columns() {
    return this._columns.filter((column) => column.freeze !== true && column.action !== true);
  }
  constructor() {
    const renderer = inject(Renderer2);
    this.debounceResize();
    this.resizeListener = renderer.listen("window", "resize", (event) => {
      this.debounceResize();
    });
  }
  ngOnDestroy() {
    if (this.resizeListener) {
      this.resizeListener();
    }
  }
  get freezeColumns() {
    const freezeColumns = this._columns.filter((column) => column.freeze === true);
    this.widporeeze = freezeColumns.reduce((prev, current) => prev + (current.width || 100), 0);
    return freezeColumns;
  }
  get actionColumns() {
    const actionsColumns = this._columns.filter((column) => column.action === true);
    this.widthActions = actionsColumns.length > 0 ? 56 : 0;
    return actionsColumns;
  }
  cancelRow(event, row) {
    const el = this.getEventPath(event).find((element) => element.id);
    if (!el) {
      return;
    }
    const [x] = el.id.split("-");
    if (this.isEmptyRow(x)) {
      if (!this.removeRow(event, row)) {
        return;
      }
      this.currencyObj = Object.assign({}, this.data[this.currencyRow - 1]);
    } else {
      this.data[+x - 1] = Object.assign({}, this.currencyObj);
      setTimeout(() => this.selectCell(this.currencyRow, this.currencyColumn));
    }
  }
  removeRow(event, row) {
    if (this.rowActions.beforeRemove && !this.rowActions.beforeRemove(Object.assign({}, row))) {
      return false;
    }
    const index = this.data.indexOf(row);
    this.data.splice(index, 1);
    this.changeDetectorRef.detectChanges();
    if (this.data.length === 0 || index === this.data.length) {
      this.selectCell(this.currencyRow - 1, this.currencyColumn);
    } else {
      this.selectCell(this.currencyRow, this.currencyColumn);
    }
    return true;
  }
  tableKeydown(event, direction) {
    let [row, col] = event.target.id.split("-");
    row = +row;
    col = +col;
    let prow = +row;
    let pcol = +col;
    if (direction === "down") {
      if (row <= this.data.length) {
        prow++;
      }
      if (row === this.data.length) {
        if (row === 0 || !this.isEmptyRow(row)) {
          if (this.saveRow(row)) {
            if (!this.insertRow()) {
              return;
            }
          } else {
            return;
          }
        } else {
          prow--;
        }
      }
    } else if (direction === "up" && row > 0) {
      prow--;
    } else if (direction === "left" && col > 0) {
      pcol--;
    } else if (direction === "right" && col < this._columns.length - 1) {
      pcol++;
    } else if (direction === "next") {
      if (col < this._columns.length - 1) {
        pcol++;
        event.preventDefault();
      } else if (row < this.data.length) {
        pcol = 0;
        prow++;
        event.preventDefault();
      }
    } else if (direction === "prior") {
      if (col > 0) {
        pcol--;
        event.preventDefault();
      } else if (row > 0) {
        pcol = this._columns.length - 1;
        prow--;
        event.preventDefault();
      }
    }
    if (this.currencyCell === `${prow}-${pcol}`) {
      return;
    }
    if (prow !== this.currencyRow && row > 0 && this.data.length >= row) {
      if (!this.isEmptyRow(row)) {
        if (!this.saveRow(row)) {
          return;
        }
      } else {
        if (!this.removeRow(event, row)) {
          return;
        }
      }
    }
    if (this.currencyRow !== prow) {
      this.currencyObj = Object.assign({}, this.data[prow - 1]);
    }
    this.lastCell = event.target.id;
    this.lastRow = row;
    this.lastColumn = col;
    this.selectCell(prow, pcol);
  }
  tableClick(event) {
    const el = this.getEventPath(event).find((element) => element.id);
    if (!el) {
      this.selectCell(this.currencyRow, this.currencyColumn);
      return;
    }
    if (this.currencyCell === el.id) {
      return;
    }
    const [row, col] = el.id.split("-");
    const prow = +row;
    const pcol = +col;
    if (prow !== this.currencyRow) {
      if (this.currencyRow > 0) {
        if (!this.isEmptyRow(this.currencyRow)) {
          if (!this.saveRow(this.currencyRow)) {
            this.selectCell(this.currencyRow, this.currencyColumn);
            return;
          }
        } else {
          if (!this.removeRow(event, row)) {
            return;
          }
        }
      }
      this.currencyObj = Object.assign({}, this.data[prow - 1]);
    }
    this.lastCell = this.currencyCell;
    this.lastRow = this.currencyRow;
    this.lastColumn = this.currencyColumn;
    this.currencyCell = el.id;
    this.currencyRow = prow;
    this.currencyColumn = pcol;
  }
  saveRow(row) {
    const obj = this.data[row - 1];
    if (!Object.keys(obj).some((prop) => obj[prop] !== this.currencyObj[prop])) {
      return true;
    }
    if (this.rowActions.beforeSave && !this.rowActions.beforeSave(obj, this.currencyObj)) {
      return false;
    }
    const requireds = [];
    this.columns.forEach((column) => {
      if (column.required === true && !obj[column.property]) {
        requireds.push(column.property);
      }
    });
    return requireds.length === 0;
  }
  insertRow() {
    const obj = {};
    if (this.rowActions.beforeInsert && !this.rowActions.beforeInsert(obj)) {
      return false;
    }
    this.data.push(obj);
    this.changeDetectorRef.detectChanges();
    return true;
  }
  isEmptyRow(row) {
    const obj = this.data[row - 1];
    if (!obj) {
      return false;
    }
    const filled = Object.keys(obj).some((property) => obj[property]);
    return !filled;
  }
  selectCell(row, col) {
    const nextCell = this.elRef.nativeElement.querySelector(`[id='${row}-${col}']`);
    if (nextCell) {
      this.currencyCell = `${row}-${col}`;
      this.currencyRow = row;
      this.currencyColumn = col;
      nextCell.focus();
    }
  }
  debounceResize() {
    clearTimeout(this.timeoutResize);
    this.timeoutResize = setTimeout(() => {
      const widthTableWrapper = this.tableWrapper.nativeElement.offsetWidth;
      this.width = `${widthTableWrapper - (this.widporeeze + 8) - this.widthActions}px`;
    }, 100);
  }
  getEventPath(event) {
    return event.path || event.composedPath();
  }
  static ɵfac = function PoGridComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoGridComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoGridComponent,
    selectors: [["po-grid"]],
    viewQuery: function PoGridComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c147, 7)(_c148, 7);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.tableElement = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.tableWrapper = _t.first);
      }
    },
    inputs: {
      rowActions: [0, "p-row-actions", "rowActions"],
      data: [0, "p-data", "data"],
      columns: [0, "p-columns", "columns"]
    },
    standalone: false,
    decls: 17,
    vars: 10,
    consts: [["wrapper", ""], ["table", ""], ["divFreezeColumns", ""], ["divColumns", ""], ["divActionColumns", ""], [1, "po-grid-wrapper", "po-grid-row-wrapper"], [1, "po-grid"], [1, "po-grid-body"], [1, "po-grid-row", 3, "keydown.arrowup", "keydown.arrowdown", "keydown.arrowleft", "keydown.arrowright", "keydown.tab", "keydown.shift.tab", "click", "contextmenu", "dblclick"], [1, "po-grid-cell", "po-grid-cell-freeze"], [1, "po-grid-cell", "po-grid-cell-normal"], [4, "ngTemplateOutlet"], [1, "po-grid-cell", "po-grid-cell-action"], [1, "po-grid", "po-grid-freeze"], [1, "po-grid-header-group"], [1, "po-grid-row"], ["p-freeze", "true", 1, "po-grid-title", 3, "width", "max-width", "min-width", "p-position", "p-width", "p-align", "p-title"], ["p-freeze", "true", 1, "po-grid-title", 3, "p-position", "p-width", "p-align", "p-title"], [1, "po-grid-row", 3, "keydown.control.delete", "keydown.esc"], ["p-freeze", "true", 1, "po-grid-cell", 3, "width", "max-width", "min-width", "p-align", "p-readonly", "p-required", "p-position", "p-width", "p-value"], ["p-freeze", "true", 1, "po-grid-cell", 3, "p-valueChange", "p-align", "p-readonly", "p-required", "p-position", "p-width", "p-value"], [1, "po-grid", "po-grid-normal"], [1, "po-grid-title", 3, "width", "max-width", "min-width", "p-align", "p-position", "p-title", "p-width"], [1, "po-grid-title", 3, "p-align", "p-position", "p-title", "p-width"], [1, "po-grid-cell", 3, "width", "max-width", "min-width", "p-align", "p-readonly", "p-required", "p-position", "p-value", "p-width"], [1, "po-grid-cell", 3, "p-valueChange", "p-align", "p-readonly", "p-required", "p-position", "p-value", "p-width"], [1, "po-grid", "po-grid-actions"], [1, "po-grid-title", 3, "p-position", "p-title", "p-align"], [1, "po-grid-cell", 3, "p-position", "p-value"]],
    template: function PoGridComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "div", 5, 0);
        ɵɵconditionalCreate(2, PoGridComponent_Conditional_2_Template, 11, 9, "div");
        ɵɵelementStart(3, "div", 6)(4, "div", 7)(5, "div", 8, 1);
        ɵɵlistener("keydown.arrowup", function PoGridComponent_Template_div_keydown_arrowup_5_listener($event) {
          return ctx.tableKeydown($event, "up");
        })("keydown.arrowdown", function PoGridComponent_Template_div_keydown_arrowdown_5_listener($event) {
          return ctx.tableKeydown($event, "down");
        })("keydown.arrowleft", function PoGridComponent_Template_div_keydown_arrowleft_5_listener($event) {
          return ctx.tableKeydown($event, "left");
        })("keydown.arrowright", function PoGridComponent_Template_div_keydown_arrowright_5_listener($event) {
          return ctx.tableKeydown($event, "right");
        })("keydown.tab", function PoGridComponent_Template_div_keydown_tab_5_listener($event) {
          return ctx.tableKeydown($event, "next");
        })("keydown.shift.tab", function PoGridComponent_Template_div_keydown_shift_tab_5_listener($event) {
          return ctx.tableKeydown($event, "prior");
        })("click", function PoGridComponent_Template_div_click_5_listener($event) {
          return ctx.tableClick($event);
        })("contextmenu", function PoGridComponent_Template_div_contextmenu_5_listener($event) {
          return ctx.tableClick($event);
        })("dblclick", function PoGridComponent_Template_div_dblclick_5_listener($event) {
          return ctx.tableClick($event);
        });
        ɵɵconditionalCreate(7, PoGridComponent_Conditional_7_Template, 2, 1, "div", 9);
        ɵɵelementStart(8, "div", 10);
        ɵɵtemplate(9, PoGridComponent_ng_container_9_Template, 1, 0, "ng-container", 11);
        ɵɵelementEnd();
        ɵɵconditionalCreate(10, PoGridComponent_Conditional_10_Template, 2, 1, "div", 12);
        ɵɵelementEnd()()()();
        ɵɵtemplate(11, PoGridComponent_ng_template_11_Template, 8, 0, "ng-template", null, 2, ɵɵtemplateRefExtractor)(13, PoGridComponent_ng_template_13_Template, 8, 0, "ng-template", null, 3, ɵɵtemplateRefExtractor)(15, PoGridComponent_ng_template_15_Template, 8, 0, "ng-template", null, 4, ɵɵtemplateRefExtractor);
      }
      if (rf & 2) {
        const divColumns_r30 = ɵɵreference(14);
        ɵɵadvance(2);
        ɵɵconditional(ctx.logger ? 2 : -1);
        ɵɵadvance(5);
        ɵɵconditional(ctx.freezeColumns.length > 0 ? 7 : -1);
        ɵɵadvance();
        ɵɵstyleProp("width", ctx.width)("max-width", ctx.width)("min-width", ctx.width);
        ɵɵadvance();
        ɵɵproperty("ngTemplateOutlet", divColumns_r30);
        ɵɵadvance();
        ɵɵconditional(ctx.actionColumns.length > 0 ? 10 : -1);
      }
    },
    dependencies: [NgTemplateOutlet, PoGridCellActionComponent, PoGridCellComponent, PoGridHeadComponent, JsonPipe],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoGridComponent, [{
    type: Component,
    args: [{
      selector: "po-grid",
      standalone: false,
      template: `<div class="po-grid-wrapper po-grid-row-wrapper" #wrapper>
  @if (logger) {
    <div>
      lastCell: {{ lastCell }} | currencyCell: {{ currencyCell }} <br />
      lastRow: {{ lastRow }} | currencyRow: {{ currencyRow }} <br />
      lastColumn: {{ lastColumn }} | currencyColumn: {{ currencyColumn }} <br />
      <hr />
      currencyObj: {{ currencyObj | json }}
      <hr />
    </div>
  }

  <div class="po-grid">
    <div class="po-grid-body">
      <div
        class="po-grid-row"
        #table
        (keydown.arrowup)="tableKeydown($event, 'up')"
        (keydown.arrowdown)="tableKeydown($event, 'down')"
        (keydown.arrowleft)="tableKeydown($event, 'left')"
        (keydown.arrowright)="tableKeydown($event, 'right')"
        (keydown.tab)="tableKeydown($event, 'next')"
        (keydown.shift.tab)="tableKeydown($event, 'prior')"
        (click)="tableClick($event)"
        (contextmenu)="tableClick($event)"
        (dblclick)="tableClick($event)"
      >
        @if (freezeColumns.length > 0) {
          <div class="po-grid-cell po-grid-cell-freeze">
            <ng-container *ngTemplateOutlet="divFreezeColumns"></ng-container>
          </div>
        }

        <div
          class="po-grid-cell po-grid-cell-normal"
          [style.width]="width"
          [style.max-width]="width"
          [style.min-width]="width"
        >
          <ng-container *ngTemplateOutlet="divColumns"></ng-container>
        </div>

        @if (actionColumns.length > 0) {
          <div class="po-grid-cell po-grid-cell-action">
            <ng-container *ngTemplateOutlet="divActionColumns"></ng-container>
          </div>
        }
      </div>
    </div>
  </div>
</div>

<!-- Coluna com as colunas congeladas -->
<ng-template #divFreezeColumns>
  <div class="po-grid po-grid-freeze">
    <div class="po-grid-header-group">
      <div class="po-grid-row">
        @for (column of freezeColumns; track column; let i = $index) {
          <po-grid-head
            class="po-grid-title"
            [style.width]="column.cssWidth"
            [style.max-width]="column.cssWidth"
            [style.min-width]="column.cssWidth"
            p-freeze="true"
            p-position="{{ '0-' + i }}"
            [p-width]="column.width"
            [p-align]="column.align"
            [p-title]="column.label"
          >
          </po-grid-head>
        }
      </div>
    </div>

    <div class="po-grid-body">
      @for (row of data; track row; let j = $index) {
        <div
          class="po-grid-row"
          (keydown.control.delete)="removeRow($event, row)"
          (keydown.esc)="cancelRow($event, row)"
        >
          @for (column of freezeColumns; track column; let i = $index) {
            <po-grid-cell
              class="po-grid-cell"
              [style.width]="column.cssWidth"
              [style.max-width]="column.cssWidth"
              [style.min-width]="column.cssWidth"
              p-freeze="true"
              [p-align]="column.align"
              [p-readonly]="column.readonly"
              [p-required]="column.required"
              p-position="{{ j + 1 + '-' + i }}"
              [p-width]="column.width"
              [(p-value)]="row[column.property]"
            >
            </po-grid-cell>
          }
        </div>
      }
    </div>
  </div>
</ng-template>
<!-- FIM: Coluna com as colunas congeladas -->

<!-- Coluna com as colunas nornais -->
<ng-template #divColumns>
  <div class="po-grid po-grid-normal">
    <div class="po-grid-header-group">
      <div class="po-grid-row">
        @for (column of columns; track column; let i = $index) {
          <po-grid-head
            class="po-grid-title"
            [style.width]="column.cssWidth"
            [style.max-width]="column.cssWidth"
            [style.min-width]="column.cssWidth"
            [p-align]="column.align"
            p-position="{{ '0-' + (i + freezeColumns.length) }}"
            [p-title]="column.label"
            [p-width]="column.width"
          >
          </po-grid-head>
        }
      </div>
    </div>

    <div class="po-grid-body">
      @for (row of data; track row; let j = $index) {
        <div
          class="po-grid-row"
          (keydown.control.delete)="removeRow($event, row)"
          (keydown.esc)="cancelRow($event, row)"
        >
          @for (column of columns; track column; let i = $index) {
            <po-grid-cell
              class="po-grid-cell"
              [style.width]="column.cssWidth"
              [style.max-width]="column.cssWidth"
              [style.min-width]="column.cssWidth"
              [p-align]="column.align"
              [p-readonly]="column.readonly"
              [p-required]="column.required"
              p-position="{{ j + 1 + '-' + (i + freezeColumns.length) }}"
              [(p-value)]="row[column.property]"
              [p-width]="column.width"
            >
            </po-grid-cell>
          }
        </div>
      }
    </div>
  </div>
</ng-template>
<!-- FIM: Coluna com as colunas nornais -->

<!-- Coluna com as colunas de ação -->
<ng-template #divActionColumns>
  <div class="po-grid po-grid-actions">
    <div class="po-grid-header-group">
      <div class="po-grid-row">
        @for (column of actionColumns; track column; let i = $index) {
          <po-grid-head
            class="po-grid-title"
            p-position="{{ '0-' + (i + freezeColumns.length + columns.length) }}"
            [p-title]="column.label"
            [p-align]="column.align"
          >
          </po-grid-head>
        }
      </div>
    </div>

    <div class="po-grid-body">
      @for (row of data; track row; let j = $index) {
        <div
          class="po-grid-row"
          (keydown.control.delete)="removeRow($event, row)"
          (keydown.esc)="cancelRow($event, row)"
        >
          @for (column of actionColumns; track column; let i = $index) {
            <po-grid-cell-action
              class="po-grid-cell"
              p-position="{{ j + 1 + '-' + (i + freezeColumns.length + columns.length) }}"
              [p-value]="row[column.property]"
            >
            </po-grid-cell-action>
          }
        </div>
      }
    </div>
  </div>
</ng-template>
<!-- FIM: Coluna com as colunas de ação -->
`
    }]
  }], () => [], {
    tableElement: [{
      type: ViewChild,
      args: ["table", {
        static: true
      }]
    }],
    tableWrapper: [{
      type: ViewChild,
      args: ["wrapper", {
        static: true
      }]
    }],
    rowActions: [{
      type: Input,
      args: ["p-row-actions"]
    }],
    data: [{
      type: Input,
      args: ["p-data"]
    }],
    columns: [{
      type: Input,
      args: ["p-columns"]
    }]
  });
})();
var PoGridModule = class _PoGridModule {
  static ɵfac = function PoGridModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoGridModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoGridModule,
    declarations: [PoGridCellActionComponent, PoGridCellComponent, PoGridComponent, PoGridHeadComponent],
    imports: [CommonModule, FormsModule, PoFieldModule],
    exports: [PoGridComponent]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [CommonModule, FormsModule, PoFieldModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoGridModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, FormsModule, PoFieldModule],
      declarations: [PoGridCellActionComponent, PoGridCellComponent, PoGridComponent, PoGridHeadComponent],
      exports: [PoGridComponent]
    }]
  }], null, null);
})();
var poListViewLiteralsDefault = {
  en: {
    hideDetails: "Hide details",
    loadMoreData: "Load more data",
    noData: "No data found",
    selectAll: "Select all",
    showDetails: "Show details"
  },
  es: {
    hideDetails: "Ocultar detalles",
    loadMoreData: "Cargar más resultados",
    noData: "Datos no encontrados",
    selectAll: "Seleccionar todos",
    showDetails: "Mostrar detalles"
  },
  pt: {
    hideDetails: "Ocultar detalhes",
    loadMoreData: "Carregar mais resultados",
    noData: "Nenhum dado encontrado",
    selectAll: "Selecionar todos",
    showDetails: "Exibir detalhes"
  },
  ru: {
    hideDetails: "Скрыть детали",
    loadMoreData: "Загрузить больше результатов",
    noData: "Данные не найдены",
    selectAll: "Выбрать все",
    showDetails: "Посмотреть детали"
  }
};
var PoListViewBaseComponent = class _PoListViewBaseComponent {
  /** Recebe uma propriedade que será utilizada para recuperar o valor do objeto que será usado como link para o título. */
  propertyLink;
  /** Recebe uma propriedade que será utilizada para recuperar o valor do objeto que será exibido como o título de cada item. */
  propertyTitle;
  /**
   * @optional
   *
   * @description
   *
   * Recebe uma ação, que será executada quando clicar no botão "Carregar mais resultados".
   *
   * > Caso nenhuma ação for definida o mesmo não ficará visível.
   */
  showMore = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Ação que será executada ao clicar no título.
   *
   * Ao ser disparado, o método inserido na ação irá receber como parâmetro o item da lista clicado.
   */
  titleAction = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Ação que será executada ao clicar no botão exibir detalhes.
   *
   * Ao ser disparado, o método passa como parâmetros os detalhes que serão exibidos.
   */
  showDetail = new EventEmitter();
  popupTarget;
  selectAll = false;
  showHeader = false;
  _actions;
  _componentsSize = void 0;
  _initialComponentsSize = void 0;
  _height;
  _hideSelectAll;
  _items;
  _literals;
  _select;
  _showMoreDisabled;
  language = poLocaleDefault;
  /**
   * @optional
   *
   * @description
   *
   * Lista de ações que serão exibidas no componente.
   */
  set actions(value) {
    this._actions = Array.isArray(value) ? value : [];
  }
  get actions() {
    return this._actions;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define o tamanho dos componentes de formulário no template:
   * - `small`: aplica a medida small de cada componente (disponível apenas para acessibilidade AA).
   * - `medium`: aplica a medida medium de cada componente.
   *
   * > Caso a acessibilidade AA não esteja configurada, o tamanho `medium` será mantido.
   * Para mais detalhes, consulte a documentação do [po-theme](https://po-ui.io/documentation/po-theme).
   *
   * @default `medium`
   */
  set componentsSize(value) {
    this._initialComponentsSize = value;
    this.applySizeBasedOnA11y();
  }
  get componentsSize() {
    return this._componentsSize ?? getDefaultSizeFn(PoFieldSize);
  }
  /**
   * @optional
   *
   * @description
   *
   * Define a altura do `po-list-view` em *pixels*.
   */
  set height(height) {
    this._height = height;
  }
  get height() {
    return this._height;
  }
  /**
   * @description
   *
   * Esconde o *checkbox* para seleção de todos os itens.
   *
   * @default `false`
   */
  set hideSelectAll(hideSelectAll) {
    this._hideSelectAll = convertToBoolean(hideSelectAll);
    this.showMainHeader();
  }
  get hideSelectAll() {
    return this._hideSelectAll;
  }
  /** Lista de itens que serão exibidos no componente. */
  set items(value) {
    this._items = Array.isArray(value) ? value : [];
  }
  get items() {
    return this._items;
  }
  /**
   * @optional
   *
   * @description
   *
   * Objeto com as literais usadas no `po-list-view`.
   *
   * Existem duas maneiras de customizar o componente, passando um objeto com todas as literais disponíveis:
   *
   * ```
   *  const customLiterals: PoListViewLiterals = {
   *    hideDetail: 'Ocultar detalhes completamente',
   *    loadMoreData: 'Mais dados',
   *    showDetail: 'Mostrar mais detalhes',
   *    selectAll: 'Selecionar todos os itens'
   *  };
   * ```
   *
   * Ou passando apenas as literais que deseja customizar:
   *
   * ```
   *  const customLiterals: PoListViewLiterals = {
   *    showDetail: 'Mostrar mais detalhes'
   *  };
   * ```
   *
   * E para carregar as literais customizadas, basta apenas passar o objeto para o componente.
   *
   * ```
   * <po-list-view
   *   [p-literals]="customLiterals">
   * </po-list-view>
   * ```
   *
   * > O objeto padrão de literais será traduzido de acordo com o idioma do
   * [`PoI18nService`](/documentation/po-i18n) ou do browser.
   */
  set literals(value) {
    if (value instanceof Object && !(value instanceof Array)) {
      this._literals = __spreadValues(__spreadValues(__spreadValues({}, poListViewLiteralsDefault[poLocaleDefault]), poListViewLiteralsDefault[this.language]), value);
    } else {
      this._literals = poListViewLiteralsDefault[this.language];
    }
  }
  get literals() {
    return this._literals || poListViewLiteralsDefault[this.language];
  }
  /**
   * @optional
   *
   * @description
   *
   * Habilita um *checkbox* para cada item da lista. Todos os items possuem a propriedade dinâmica `$selected` para identificar se o
   * item está selecionado, por exemplo:
   *
   * ```
   *  item.$selected
   *
   *  // ou
   *
   *  item['$selected']
   * ```
   *
   * @default `false`
   */
  set select(select) {
    this._select = convertToBoolean(select);
    this.showMainHeader();
  }
  get select() {
    return this._select;
  }
  /**
   * @optional
   *
   * @description
   *
   * Indica que o botão `Carregar Mais Resultados` será desabilitado.
   */
  set showMoreDisabled(value) {
    this._showMoreDisabled = convertToBoolean(value);
  }
  get showMoreDisabled() {
    return this._showMoreDisabled;
  }
  constructor(languageService) {
    this.language = languageService.getShortLanguage();
  }
  onClickAction(listViewAction, item) {
    const cleanItem = this.deleteInternalAttrs(item);
    if (listViewAction.action) {
      listViewAction.action(cleanItem);
    }
  }
  onShowMore() {
    this.showMore.emit();
  }
  runTitleAction(listItem) {
    const itemWithPublicProperties = this.deleteInternalAttrs(listItem);
    this.titleAction.emit(itemWithPublicProperties);
  }
  selectAllListItems() {
    if (!this.hideSelectAll) {
      this.selectAll = !this.selectAll;
      this.items.forEach((item) => {
        item.$selected = this.selectAll;
      });
    }
  }
  selectListItem(row) {
    row.$selected = !row.$selected;
    this.selectAll = this.checkIfItemsAreSelected(this.items);
  }
  onThemeChange() {
    this.applySizeBasedOnA11y();
  }
  applySizeBasedOnA11y() {
    const size = validateSizeFn(this._initialComponentsSize, PoFieldSize);
    this._componentsSize = size;
  }
  deleteInternalAttrs(item) {
    const itemCopy = item ? __spreadValues({}, item) : void 0;
    for (const key2 in itemCopy) {
      if (itemCopy.hasOwnProperty(key2) && key2.startsWith("$")) {
        delete itemCopy[key2];
      }
    }
    return itemCopy;
  }
  checkIfItemsAreSelected(items) {
    const someCheckedOrIndeterminate = (item) => item.$selected || item.$selected === null;
    const everyChecked = (item) => item.$selected;
    if (items.every(everyChecked)) {
      return true;
    }
    if (items.some(someCheckedOrIndeterminate)) {
      return null;
    }
    return false;
  }
  showMainHeader() {
    this.showHeader = !!(this.select && !this.hideSelectAll && this.items && this.items.length);
  }
  static ɵfac = function PoListViewBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoListViewBaseComponent)(ɵɵdirectiveInject(PoLanguageService));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoListViewBaseComponent,
    hostVars: 1,
    hostBindings: function PoListViewBaseComponent_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("PoUiThemeChange", function PoListViewBaseComponent_PoUiThemeChange_HostBindingHandler() {
          return ctx.onThemeChange();
        }, ɵɵresolveWindow);
      }
      if (rf & 2) {
        ɵɵattribute("p-components-size", ctx.componentsSize);
      }
    },
    inputs: {
      propertyLink: [0, "p-property-link", "propertyLink"],
      propertyTitle: [0, "p-property-title", "propertyTitle"],
      actions: [0, "p-actions", "actions"],
      componentsSize: [0, "p-components-size", "componentsSize"],
      height: [0, "p-height", "height"],
      hideSelectAll: [0, "p-hide-select-all", "hideSelectAll"],
      items: [0, "p-items", "items"],
      literals: [0, "p-literals", "literals"],
      select: [0, "p-select", "select"],
      showMoreDisabled: [0, "p-show-more-disabled", "showMoreDisabled"]
    },
    outputs: {
      showMore: "p-show-more",
      titleAction: "p-title-action",
      showDetail: "p-show-detail"
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoListViewBaseComponent, [{
    type: Directive
  }], () => [{
    type: PoLanguageService
  }], {
    propertyLink: [{
      type: Input,
      args: ["p-property-link"]
    }],
    propertyTitle: [{
      type: Input,
      args: ["p-property-title"]
    }],
    showMore: [{
      type: Output,
      args: ["p-show-more"]
    }],
    titleAction: [{
      type: Output,
      args: ["p-title-action"]
    }],
    showDetail: [{
      type: Output,
      args: ["p-show-detail"]
    }],
    actions: [{
      type: Input,
      args: ["p-actions"]
    }],
    componentsSize: [{
      type: Input,
      args: ["p-components-size"]
    }, {
      type: HostBinding,
      args: ["attr.p-components-size"]
    }],
    height: [{
      type: Input,
      args: ["p-height"]
    }],
    hideSelectAll: [{
      type: Input,
      args: ["p-hide-select-all"]
    }],
    items: [{
      type: Input,
      args: ["p-items"]
    }],
    literals: [{
      type: Input,
      args: ["p-literals"]
    }],
    select: [{
      type: Input,
      args: ["p-select"]
    }],
    showMoreDisabled: [{
      type: Input,
      args: ["p-show-more-disabled"]
    }],
    onThemeChange: [{
      type: HostListener,
      args: ["window:PoUiThemeChange"]
    }]
  });
})();
var PoListViewContentTemplateDirective = class _PoListViewContentTemplateDirective {
  templateRef;
  /**
   * @optional
   *
   * @description
   *
   * Função que deve retornar um valor do tipo `string`, que será utilizado como o título de cada item da lista.
   *
   * > Por parâmetro será enviado o item corrente, onde poderá utilizá-lo para recuperar dados sobre o mesmo.
   */
  title;
  // Necessário manter templateRef para o funcionamento do row template.
  constructor(templateRef) {
    this.templateRef = templateRef;
  }
  static ɵfac = function PoListViewContentTemplateDirective_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoListViewContentTemplateDirective)(ɵɵdirectiveInject(TemplateRef));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoListViewContentTemplateDirective,
    selectors: [["", "p-list-view-content-template", ""]],
    inputs: {
      title: [0, "p-title", "title"]
    },
    standalone: false
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoListViewContentTemplateDirective, [{
    type: Directive,
    args: [{
      selector: "[p-list-view-content-template]",
      standalone: false
    }]
  }], () => [{
    type: TemplateRef
  }], {
    title: [{
      type: Input,
      args: ["p-title"]
    }]
  });
})();
var PoListViewDetailTemplateDirective = class _PoListViewDetailTemplateDirective {
  templateRef;
  /**
   * @optional
   *
   * @description
   *
   * Função que deve retornar um valor do tipo `boolean`, que será utilizado como a validação para que o detalhe de item
   * da lista inicie aberto ou fechado.
   *
   */
  showDetail;
  // Necessário manter templateRef para o funcionamento do row template.
  constructor(templateRef) {
    this.templateRef = templateRef;
  }
  static ɵfac = function PoListViewDetailTemplateDirective_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoListViewDetailTemplateDirective)(ɵɵdirectiveInject(TemplateRef));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoListViewDetailTemplateDirective,
    selectors: [["", "p-list-view-detail-template", ""]],
    inputs: {
      showDetail: [0, "p-show-detail", "showDetail"]
    },
    standalone: false
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoListViewDetailTemplateDirective, [{
    type: Directive,
    args: [{
      selector: "[p-list-view-detail-template]",
      standalone: false
    }]
  }], () => [{
    type: TemplateRef
  }], {
    showDetail: [{
      type: Input,
      args: ["p-show-detail"]
    }]
  });
})();
var PoListViewComponent = class _PoListViewComponent extends PoListViewBaseComponent {
  changeDetector = inject(ChangeDetectorRef);
  listViewContentTemplate;
  listViewDetailTemplate;
  poPopupComponent;
  popupActions = [];
  differ;
  constructor() {
    const differs = inject(IterableDiffers);
    const languageService = inject(PoLanguageService);
    super(languageService);
    this.differ = differs.find([]).create(null);
  }
  get hasContentTemplate() {
    return !!this.listViewContentTemplate;
  }
  get hasDetailTemplate() {
    return !!this.listViewDetailTemplate;
  }
  get displayShowMoreButton() {
    return this.items && this.items.length > 0 && this.showMore.observers.length > 0;
  }
  get titleHasAction() {
    return this.titleAction.observers.length > 0;
  }
  ngAfterContentInit() {
    this.initShowDetail();
  }
  ngDoCheck() {
    this.checkItemsChange();
  }
  checkTitleType(item) {
    if (this.propertyLink && item[this.propertyLink]) {
      return item[this.propertyLink].startsWith("http") ? "externalLink" : "internalLink";
    }
    return "noLink";
  }
  getItemTitle(item) {
    return this.hasContentTemplate && this.listViewContentTemplate.title ? this.listViewContentTemplate.title(item) : item[this.propertyTitle];
  }
  hasItems() {
    return this.items && this.items.length > 0;
  }
  returnBooleanValue(listViewAction, item, property) {
    return PoUtils.isTypeof(listViewAction[property], "function") ? listViewAction[property](item) : listViewAction[property];
  }
  trackBy(index) {
    return index;
  }
  togglePopup(item, targetRef) {
    this.popupTarget = targetRef;
    this.popupActions = this.getVisibleActions(item);
    this.changeDetector.detectChanges();
    this.poPopupComponent.toggle(item);
  }
  onAnimationEvent(event, detail) {
    this.showDetail.emit(detail);
  }
  // Avalia a visibilidade das ações por item, passando o item corrente.
  getVisibleActions(item) {
    return this.actions?.filter((action) => this.returnBooleanValue(action, item, "visible") !== false) ?? [];
  }
  checkItemsChange() {
    const changesItems = this.differ.diff(this.items);
    if (changesItems && this.selectAll) {
      this.selectAll = null;
    }
    if (changesItems && this.items && this.items.length && this.select && !this.hideSelectAll) {
      this.showHeader = true;
    }
  }
  initShowDetail() {
    if (this.items && this.items.length > 0 && this.hasDetailTemplate && this.listViewDetailTemplate.showDetail) {
      this.items.forEach((item) => item.$showDetail = this.listViewDetailTemplate.showDetail(item));
    }
  }
  static ɵfac = function PoListViewComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoListViewComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoListViewComponent,
    selectors: [["po-list-view"]],
    contentQueries: function PoListViewComponent_ContentQueries(rf, ctx, dirIndex) {
      if (rf & 1) {
        ɵɵcontentQuery(dirIndex, PoListViewContentTemplateDirective, 7)(dirIndex, PoListViewDetailTemplateDirective, 7);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.listViewContentTemplate = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.listViewDetailTemplate = _t.first);
      }
    },
    viewQuery: function PoListViewComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c36, 7);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.poPopupComponent = _t.first);
      }
    },
    standalone: false,
    features: [ɵɵInheritDefinitionFeature],
    decls: 9,
    vars: 12,
    consts: [["popup", ""], ["popupTarget", ""], [1, "po-list-view-main-header"], [1, "po-list-view-main-content"], [3, "p-no-padding"], [1, "po-mb-2", "po-text-center"], [3, "p-actions", "p-size", "p-target"], [1, "po-checkbox-group-item"], [1, "po-list-view-main-select"], ["name", "selectAll", 3, "ngModelChange", "p-change", "click", "ngModel", "p-label", "p-size"], [1, "po-list-view-container-no-data", "po-text-center"], [1, "po-list-view-container"], [1, "po-list-view-header"], [1, "po-list-view-title", 3, "ngSwitch"], [1, "po-list-view-select"], ["class", "po-list-view-title-link", "target", "_blank", 3, "href", "click", 4, "ngSwitchCase"], ["class", "po-list-view-title-link", 3, "routerLink", "click", 4, "ngSwitchCase"], ["class", "po-list-view-title-no-link", 3, "po-list-view-title-link", "click", 4, "ngSwitchCase"], [1, "po-list-view-actions"], [1, "po-list-view-more-actions"], [1, "po-list-view-content"], [1, "po-list-view-detail"], [1, "po-list-view-detail-button"], ["name", "checkbox", 3, "ngModelChange", "p-change", "click", "ngModel", "p-size"], ["target", "_blank", 1, "po-list-view-title-link", 3, "click", "href"], [1, "po-list-view-title-link", 3, "click", "routerLink"], [1, "po-list-view-title-no-link", 3, "click"], [3, "p-disabled", "p-icon", "p-label", "p-danger", "p-size"], [3, "p-click", "p-disabled", "p-icon", "p-label", "p-danger", "p-size"], [1, "po-list-view-more-icon", "po-clickable", 3, "click"], ["p-icon", "ICON_MORE"], [3, "ngTemplateOutlet", "ngTemplateOutletContext"], ["p-kind", "tertiary", 3, "p-click", "p-label", "p-size"], [3, "p-click", "p-disabled", "p-label", "p-size"]],
    template: function PoListViewComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "div");
        ɵɵconditionalCreate(1, PoListViewComponent_Conditional_1_Template, 4, 3, "div", 2);
        ɵɵelementStart(2, "div", 3);
        ɵɵconditionalCreate(3, PoListViewComponent_Conditional_3_Template, 4, 2, "po-container", 4);
        ɵɵrepeaterCreate(4, PoListViewComponent_For_5_Template, 14, 12, "po-container", 4, ctx.trackBy, true);
        ɵɵelementEnd()();
        ɵɵconditionalCreate(6, PoListViewComponent_Conditional_6_Template, 2, 3, "div", 5);
        ɵɵelement(7, "po-popup", 6, 0);
      }
      if (rf & 2) {
        ɵɵstyleProp("height", ctx.height, "px");
        ɵɵclassProp("po-list-view-main-container-header", ctx.showHeader)("po-list-view-main-container", !ctx.showHeader);
        ɵɵadvance();
        ɵɵconditional(ctx.showHeader ? 1 : -1);
        ɵɵadvance(2);
        ɵɵconditional(!ctx.hasItems() ? 3 : -1);
        ɵɵadvance();
        ɵɵrepeater(ctx.items);
        ɵɵadvance(2);
        ɵɵconditional(ctx.displayShowMoreButton ? 6 : -1);
        ɵɵadvance();
        ɵɵproperty("p-actions", ctx.popupActions)("p-size", ctx.componentsSize)("p-target", ctx.popupTarget);
      }
    },
    dependencies: [NgTemplateOutlet, NgSwitch, NgSwitchCase, NgControlStatus, NgModel, RouterLink, PoButtonComponent, PoIconComponent, PoPopupComponent, PoCheckboxComponent, PoContainerComponent],
    encapsulation: 2,
    data: {
      animation: [trigger("showHideDetail", [state("*", style({
        "overflow-y": "visible"
      })), state("void", style({
        "overflow-y": "hidden"
      })), transition("* => void", [style({
        height: "*",
        "overflow-y": "hidden"
      }), animate(100, style({
        height: 0
      }))]), transition("void => *", [style({
        height: "0"
      }), animate(100, style({
        height: "*"
      }))])])]
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoListViewComponent, [{
    type: Component,
    args: [{
      selector: "po-list-view",
      animations: [trigger("showHideDetail", [state("*", style({
        "overflow-y": "visible"
      })), state("void", style({
        "overflow-y": "hidden"
      })), transition("* => void", [style({
        height: "*",
        "overflow-y": "hidden"
      }), animate(100, style({
        height: 0
      }))]), transition("void => *", [style({
        height: "0"
      }), animate(100, style({
        height: "*"
      }))])])],
      standalone: false,
      template: `<div
  [class.po-list-view-main-container-header]="showHeader"
  [class.po-list-view-main-container]="!showHeader"
  [style.height.px]="height"
>
  @if (showHeader) {
    <div class="po-list-view-main-header">
      <div class="po-checkbox-group-item">
        <div class="po-list-view-main-select">
          <po-checkbox
            name="selectAll"
            [(ngModel)]="selectAll"
            [p-label]="literals.selectAll"
            [p-size]="componentsSize"
            (p-change)="selectAllListItems()"
            (click)="selectAllListItems()"
          ></po-checkbox>
        </div>
      </div>
    </div>
  }

  <div class="po-list-view-main-content">
    @if (!hasItems()) {
      <po-container [p-no-padding]="true">
        <div class="po-list-view-container-no-data po-text-center">
          <span> {{ literals.noData }} </span>
        </div>
      </po-container>
    }

    @for (item of items; track trackBy($index); let index = $index) {
      @let visibleActionsForItem = getVisibleActions(item);

      <po-container [p-no-padding]="true">
        <div class="po-list-view-container">
          <div class="po-list-view-header">
            <div class="po-list-view-title" [ngSwitch]="checkTitleType(item)">
              @if (select) {
                <div class="po-list-view-select">
                  <po-checkbox
                    [(ngModel)]="item.$selected"
                    name="checkbox"
                    [p-size]="componentsSize"
                    (p-change)="selectListItem(item)"
                    (click)="selectListItem(item)"
                  ></po-checkbox>
                </div>
              }

              <a
                *ngSwitchCase="'externalLink'"
                class="po-list-view-title-link"
                target="_blank"
                [href]="item[propertyLink]"
                (click)="runTitleAction(item)"
              >
                {{ getItemTitle(item) }}
              </a>
              <a
                *ngSwitchCase="'internalLink'"
                class="po-list-view-title-link"
                [routerLink]="item[propertyLink]"
                (click)="runTitleAction(item)"
              >
                {{ getItemTitle(item) }}
              </a>
              <span
                *ngSwitchCase="'noLink'"
                class="po-list-view-title-no-link"
                [class.po-list-view-title-link]="titleHasAction"
                (click)="titleHasAction && runTitleAction(item)"
              >
                {{ getItemTitle(item) }}
              </span>
            </div>

            @if (visibleActionsForItem.length > 0 && visibleActionsForItem.length <= 2) {
              <div class="po-list-view-actions">
                @for (action of visibleActionsForItem; track action) {
                  <po-button
                    [p-disabled]="returnBooleanValue(action, item, 'disabled')"
                    [p-icon]="action.icon"
                    [p-label]="action.label"
                    [p-danger]="action.type === 'danger'"
                    [p-size]="componentsSize"
                    (p-click)="onClickAction(action, item)"
                  >
                  </po-button>
                }
              </div>
            }

            @if (visibleActionsForItem.length > 2) {
              <div class="po-list-view-more-actions">
                <div #popupTarget class="po-list-view-more-icon po-clickable" (click)="togglePopup(item, popupTarget)">
                  <po-icon p-icon="ICON_MORE"></po-icon>
                </div>
              </div>
            }
          </div>

          @if (hasContentTemplate) {
            <div class="po-list-view-content">
              <ng-template
                [ngTemplateOutlet]="listViewContentTemplate.templateRef"
                [ngTemplateOutletContext]="{ $implicit: item, index: index }"
              >
              </ng-template>
            </div>
          }

          @if (hasDetailTemplate && item.$showDetail) {
            <div
              @showHideDetail
              (@showHideDetail.start)="item.$showDetail ? onAnimationEvent($event, item) : 'undefined'"
              class="po-list-view-detail"
            >
              <ng-template
                [ngTemplateOutlet]="listViewDetailTemplate.templateRef"
                [ngTemplateOutletContext]="{ $implicit: item, index: index }"
              >
              </ng-template>
            </div>
          }
        </div>

        @if (hasDetailTemplate) {
          <div class="po-list-view-detail-button">
            <po-button
              p-kind="tertiary"
              [p-label]="item.$showDetail ? literals.hideDetails : literals.showDetails"
              [p-size]="componentsSize"
              (p-click)="item.$showDetail = !item.$showDetail"
            >
            </po-button>
          </div>
        }
      </po-container>
    }
  </div>
</div>

@if (displayShowMoreButton) {
  <div class="po-mb-2 po-text-center">
    <po-button
      [p-disabled]="showMoreDisabled"
      [p-label]="literals.loadMoreData"
      [p-size]="componentsSize"
      (p-click)="onShowMore()"
    >
    </po-button>
  </div>
}

<po-popup #popup [p-actions]="popupActions" [p-size]="componentsSize" [p-target]="popupTarget"> </po-popup>
`
    }]
  }], () => [], {
    listViewContentTemplate: [{
      type: ContentChild,
      args: [PoListViewContentTemplateDirective, {
        static: true
      }]
    }],
    listViewDetailTemplate: [{
      type: ContentChild,
      args: [PoListViewDetailTemplateDirective, {
        static: true
      }]
    }],
    poPopupComponent: [{
      type: ViewChild,
      args: ["popup", {
        static: true
      }]
    }]
  });
})();
var PoListViewModule = class _PoListViewModule {
  static ɵfac = function PoListViewModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoListViewModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoListViewModule,
    declarations: [PoListViewComponent, PoListViewContentTemplateDirective, PoListViewDetailTemplateDirective],
    imports: [CommonModule, FormsModule, RouterModule, PoButtonModule, PoIconModule, PoPopupModule, PoCheckboxModule, PoContainerModule],
    exports: [PoListViewComponent, PoListViewContentTemplateDirective, PoListViewDetailTemplateDirective]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [CommonModule, FormsModule, RouterModule, PoButtonModule, PoIconModule, PoPopupModule, PoCheckboxModule, PoContainerModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoListViewModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, FormsModule, RouterModule, PoButtonModule, PoIconModule, PoPopupModule, PoCheckboxModule, PoContainerModule],
      declarations: [PoListViewComponent, PoListViewContentTemplateDirective, PoListViewDetailTemplateDirective],
      exports: [PoListViewComponent, PoListViewContentTemplateDirective, PoListViewDetailTemplateDirective],
      providers: [],
      schemas: []
    }]
  }], null, null);
})();
var poLogoLiteralsDefault = {
  en: {
    logomarcaHome: "Home logo"
  },
  es: {
    logomarcaHome: "Logomarca inicio"
  },
  pt: {
    logomarcaHome: "Logomarca início"
  },
  ru: {
    logomarcaHome: "Дом Логомарка"
  }
};
var MAX_LENGHT = 125;
var PoLogoComponent = class _PoLogoComponent {
  poLanguageService;
  literals;
  _logo;
  _logoAlt;
  _link = "./";
  /**
   * Define uma classe para o elemento `img` do componente.
   *
   * > **Importante**
   * > A classe deve ser definida apenas quando houver necessidade de ajustar o componente dentro de outro componente.
   *
   */
  className = "po-logo";
  /**
   * Define se o componente terá o elemento âncora para uma rota específica.
   *
   * > Caso seja definido como false, o componente apenas renderizará o elemento `img`.
   * > Caso seja definido como true, a rota será `./`.
   * > Caso seja definido como string, a rota será a string passada.
   *
   * @default `true`
   */
  set link(value) {
    this._link = value !== false ? value : false;
  }
  get link() {
    return this._link;
  }
  /**
   * Definie o caminho para a imagem, que será exibida como logomarca.
   *
   */
  set logo(value) {
    this._logo = isTypeof(value, "string") && value.trim() ? value : void 0;
  }
  get logo() {
    return this._logo;
  }
  /**
   * Define o texto alternativo para a logomarca.
   *
   * > **Importante**
   * > Caso esta propriedade não seja definida o texto padrão será "Logomarca início".
   */
  set logoAlt(value) {
    this._logoAlt = isTypeof(value, "string") && value.trim() ? this.maxLength(value) : this.literals.logomarcaHome;
  }
  get logoAlt() {
    return this._logoAlt;
  }
  constructor(poLanguageService) {
    this.poLanguageService = poLanguageService;
    this.literals = __spreadValues(__spreadValues({}, poLogoLiteralsDefault[this.poLanguageService?.getLanguageDefault()]), poLogoLiteralsDefault[this.poLanguageService?.getShortLanguage()]);
    this._logoAlt = this.literals.logomarcaHome;
  }
  maxLength(value) {
    return value.length > MAX_LENGHT ? value.toString().substring(0, MAX_LENGHT) : value;
  }
  static ɵfac = function PoLogoComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoLogoComponent)(ɵɵdirectiveInject(PoLanguageService));
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoLogoComponent,
    selectors: [["po-logo"]],
    inputs: {
      className: [0, "p-class", "className"],
      link: [0, "p-link", "link"],
      logo: [0, "p-logo", "logo"],
      logoAlt: [0, "p-logo-alt", "logoAlt"]
    },
    standalone: false,
    decls: 2,
    vars: 1,
    consts: [[3, "href"], [3, "alt", "class", "src", "title"], [3, "alt", "src", "title"]],
    template: function PoLogoComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵconditionalCreate(0, PoLogoComponent_Conditional_0_Template, 2, 6, "a", 0)(1, PoLogoComponent_Conditional_1_Template, 1, 5, "img", 1);
      }
      if (rf & 2) {
        ɵɵconditional(ctx.link !== false ? 0 : 1);
      }
    },
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoLogoComponent, [{
    type: Component,
    args: [{
      selector: "po-logo",
      standalone: false,
      template: `@if (link !== false) {
  <a [href]="link === true ? './' : link">
    <img [alt]="logoAlt" [class]="className" [src]="logo" [title]="logoAlt" />
  </a>
} @else {
  <img [alt]="logoAlt" [class]="className" [src]="logo" [title]="logoAlt" />
}
`
    }]
  }], () => [{
    type: PoLanguageService
  }], {
    className: [{
      type: Input,
      args: ["p-class"]
    }],
    link: [{
      type: Input,
      args: ["p-link"]
    }],
    logo: [{
      type: Input,
      args: ["p-logo"]
    }],
    logoAlt: [{
      type: Input,
      args: ["p-logo-alt"]
    }]
  });
})();
var PoLogoModule = class _PoLogoModule {
  static ɵfac = function PoLogoModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoLogoModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoLogoModule,
    declarations: [PoLogoComponent],
    imports: [CommonModule],
    exports: [PoLogoComponent]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [CommonModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoLogoModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule],
      declarations: [PoLogoComponent],
      exports: [PoLogoComponent]
    }]
  }], null, null);
})();
var PoMenuPanelItemsService = class _PoMenuPanelItemsService {
  subjectChild = new Subject();
  subjectParent = new Subject();
  // Recebe do po-menu-panel-item sua informação de click.
  receiveFromChildMenuClicked() {
    return this.subjectChild.asObservable();
  }
  // Recebe do po-menu-panel as informações processadas do click de um po-menu-panel-item.
  receiveFromParentMenuClicked() {
    return this.subjectParent.asObservable();
  }
  // Envia informações do click do po-menu-panel-item para o po-menu
  sendToParentMenuClicked(menu) {
    this.subjectChild.next(menu);
  }
  // Envia para os po-menu-panel-item a resposta do processamento de click de um po-menu-panel-item.
  sendToChildMenuClicked(menu) {
    this.subjectParent.next(menu);
  }
  static ɵfac = function PoMenuPanelItemsService_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoMenuPanelItemsService)();
  };
  static ɵprov = ɵɵdefineInjectable({
    token: _PoMenuPanelItemsService,
    factory: _PoMenuPanelItemsService.ɵfac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoMenuPanelItemsService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
var PoMenuPanelItemComponent = class _PoMenuPanelItemComponent {
  menuItemsService = inject(PoMenuPanelItemsService);
  menuItemInternal;
  itemsSubscription;
  ngOnDestroy() {
    this.itemsSubscription.unsubscribe();
  }
  ngOnInit() {
    this.subscribeMenuClickedFromParent();
  }
  clickMenuItem(event) {
    if (!(event.ctrlKey || event.metaKey)) {
      event.preventDefault();
      this.menuItemsService.sendToParentMenuClicked(this.menuItemInternal);
    }
  }
  activateMenu(menu) {
    this.menuItemInternal.isSelected = this.menuItemInternal.id === menu.id;
  }
  processMenuItem(menu) {
    if (this.menuItemInternal.type === "internalLink") {
      this.activateMenu(menu.active);
    }
  }
  subscribeMenuClickedFromParent() {
    this.itemsSubscription = this.menuItemsService.receiveFromParentMenuClicked().subscribe((menu) => {
      this.processMenuItem(menu);
    });
  }
  static ɵfac = function PoMenuPanelItemComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoMenuPanelItemComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoMenuPanelItemComponent,
    selectors: [["po-menu-panel-item"]],
    inputs: {
      menuItemInternal: [0, "p-menu-item-internal", "menuItemInternal"]
    },
    standalone: false,
    decls: 5,
    vars: 3,
    consts: [["menuItemTemplate", ""], [3, "routerLink"], [3, "href"], ["href", "javascript:;"], [4, "ngTemplateOutlet"], ["p-tooltip-position", "right", 1, "po-menu-panel-item", 3, "click", "p-tooltip"]],
    template: function PoMenuPanelItemComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵconditionalCreate(0, PoMenuPanelItemComponent_Conditional_0_Template, 2, 2, "a", 1);
        ɵɵconditionalCreate(1, PoMenuPanelItemComponent_Conditional_1_Template, 2, 2, "a", 2);
        ɵɵconditionalCreate(2, PoMenuPanelItemComponent_Conditional_2_Template, 2, 1, "a", 3);
        ɵɵtemplate(3, PoMenuPanelItemComponent_ng_template_3_Template, 2, 5, "ng-template", null, 0, ɵɵtemplateRefExtractor);
      }
      if (rf & 2) {
        ɵɵconditional(ctx.menuItemInternal.type === "internalLink" ? 0 : -1);
        ɵɵadvance();
        ɵɵconditional(ctx.menuItemInternal.type === "externalLink" ? 1 : -1);
        ɵɵadvance();
        ɵɵconditional(ctx.menuItemInternal.type === "noLink" ? 2 : -1);
      }
    },
    dependencies: [NgTemplateOutlet, RouterLink, PoTooltipDirective],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoMenuPanelItemComponent, [{
    type: Component,
    args: [{
      selector: "po-menu-panel-item",
      standalone: false,
      template: `<!-- menu com link interno -->
@if (menuItemInternal.type === 'internalLink') {
  <a [routerLink]="menuItemInternal.link">
    <ng-container *ngTemplateOutlet="menuItemTemplate"></ng-container>
  </a>
}

<!-- menu com link externo -->
@if (menuItemInternal.type === 'externalLink') {
  <a [href]="menuItemInternal.link">
    <ng-container *ngTemplateOutlet="menuItemTemplate"></ng-container>
  </a>
}

<!-- menu sem link -->
@if (menuItemInternal.type === 'noLink') {
  <a href="javascript:;">
    <ng-container *ngTemplateOutlet="menuItemTemplate"></ng-container>
  </a>
}

<ng-template #menuItemTemplate>
  <div
    class="po-menu-panel-item"
    p-tooltip-position="right"
    [class.po-menu-panel-item-selected]="menuItemInternal.isSelected"
    [p-tooltip]="menuItemInternal.label"
    (click)="clickMenuItem($event)"
  >
    <span class="{{ menuItemInternal.icon }}"></span>
  </div>
</ng-template>
`
    }]
  }], null, {
    menuItemInternal: [{
      type: Input,
      args: ["p-menu-item-internal"]
    }]
  });
})();
var poDefaultLogo = "https://po-ui.io/assets/po-logos/po_black.svg";
var PoMenuPanelBaseComponent = class _PoMenuPanelBaseComponent {
  _menus;
  _logo = poDefaultLogo;
  /** Lista dos itens do `po-menu-panel`. Se o valor estiver indefinido ou inválido, será inicializado como um array vazio. */
  set menus(menus) {
    this._menus = Array.isArray(menus) ? menus : [];
    this.setMenuExtraProperties(this._menus);
    this.validateMenus(this._menus);
  }
  get menus() {
    return this._menus;
  }
  /**
   * @optional
   *
   * @description
   *
   * Caminho para a logomarca localizada na parte superior do menu.
   *
   * > **Importante**
   * > Caso seja indefinida será aplicada a imagem default do PO UI.
   */
  set logo(src) {
    this._logo = src ?? poDefaultLogo;
  }
  get logo() {
    return this._logo;
  }
  /**
   * Define o texto alternativo para a logomarca.
   *
   * > **Importante**
   * > Caso esta propriedade não seja definida o texto padrão será "Logomarca início".
   *
   * @default `Logomarca início`
   */
  logoAlt;
  setMenuExtraProperties(menus) {
    menus.forEach((menuItem) => this.setMenuItemProperties(menuItem));
  }
  setMenuItemProperties(menuItem) {
    menuItem.id = menuItem.id || uuid();
    menuItem.type = this.setMenuType(menuItem);
  }
  setMenuType(menuItem) {
    if (!menuItem.link) {
      return "noLink";
    }
    if (isExternalLink(menuItem.link)) {
      return "externalLink";
    }
    return "internalLink";
  }
  validateMenu(menuItem) {
    if (!menuItem.label) {
      throw new Error("O atributo PoMenuPanelItem.label não pode ser vazio.");
    }
    if (!menuItem.icon) {
      throw new Error("O atributo PoMenuPanelItem.icon não pode ser vazio.");
    }
  }
  validateMenus(menus) {
    menus.forEach((menu) => this.validateMenu(menu));
  }
  static ɵfac = function PoMenuPanelBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoMenuPanelBaseComponent)();
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoMenuPanelBaseComponent,
    inputs: {
      menus: [0, "p-menus", "menus"],
      logo: [0, "p-logo", "logo"],
      logoAlt: [0, "p-logo-alt", "logoAlt"]
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoMenuPanelBaseComponent, [{
    type: Directive
  }], null, {
    menus: [{
      type: Input,
      args: ["p-menus"]
    }],
    logo: [{
      type: Input,
      args: ["p-logo"]
    }],
    logoAlt: [{
      type: Input,
      args: ["p-logo-alt"]
    }]
  });
})();
var PoMenuPanelComponent = class _PoMenuPanelComponent extends PoMenuPanelBaseComponent {
  location = inject(Location);
  menuItemsService = inject(PoMenuPanelItemsService);
  router = inject(Router);
  activeMenuItem;
  linkActive;
  routeSubscription;
  itemSubscription;
  ngOnDestroy() {
    this.itemSubscription.unsubscribe();
    this.routeSubscription.unsubscribe();
  }
  ngOnInit() {
    this.subscribeToMenuItem();
    this.subscribeToRoute();
  }
  activateMenuByUrl(urlPath, menus) {
    if (menus) {
      return menus.some((menu) => {
        if (getFormattedLink(menu.link) === urlPath) {
          this.activateMenuItem(menu);
          return true;
        }
      });
    }
  }
  activateMenuItem(menu) {
    this.activeMenuItem = menu;
    this.linkActive = getFormattedLink(menu.link);
    this.menuItemsService.sendToChildMenuClicked({
      active: this.activeMenuItem,
      activatedByRoute: true
    });
  }
  checkActiveMenuByUrl(urlPath) {
    if (!this.linkActive || this.linkActive !== urlPath) {
      this.activateMenuByUrl(urlPath, this.menus);
    }
  }
  clickMenuItem(menu) {
    if (menu.action) {
      menu.action(menu);
    }
    if (menu.type === "externalLink") {
      openExternalLink(menu.link);
    } else if (menu.type === "internalLink") {
      this.activateMenuItem(menu);
    }
  }
  subscribeToRoute() {
    this.routeSubscription = this.router.events.subscribe((rounterEvent) => {
      if (rounterEvent instanceof NavigationEnd) {
        this.checkActiveMenuByUrl(this.location.path());
      }
    });
  }
  subscribeToMenuItem() {
    this.itemSubscription = this.menuItemsService.receiveFromChildMenuClicked().subscribe((menu) => this.clickMenuItem(menu));
  }
  static ɵfac = /* @__PURE__ */ (() => {
    let ɵPoMenuPanelComponent_BaseFactory;
    return function PoMenuPanelComponent_Factory(__ngFactoryType__) {
      return (ɵPoMenuPanelComponent_BaseFactory || (ɵPoMenuPanelComponent_BaseFactory = ɵɵgetInheritedFactory(_PoMenuPanelComponent)))(__ngFactoryType__ || _PoMenuPanelComponent);
    };
  })();
  static ɵcmp = ɵɵdefineComponent({
    type: _PoMenuPanelComponent,
    selectors: [["po-menu-panel"]],
    standalone: false,
    features: [ɵɵInheritDefinitionFeature],
    decls: 7,
    vars: 1,
    consts: [[1, "po-menu-panel"], [1, "po-menu-panel-logo-container"], ["p-class", "po-menu-panel-logo", 3, "p-logo", "p-logo-alt"], [1, "po-menu-panel-container"], [1, "po-menu-panel-inner"], [1, "po-menu-panel-item-wrapper"], [3, "p-menu-item-internal"]],
    template: function PoMenuPanelComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "div", 0)(1, "div", 1);
        ɵɵconditionalCreate(2, PoMenuPanelComponent_Conditional_2_Template, 1, 2, "po-logo", 2);
        ɵɵelementEnd();
        ɵɵelementStart(3, "nav", 3)(4, "div", 4);
        ɵɵrepeaterCreate(5, PoMenuPanelComponent_For_6_Template, 2, 3, "div", 5, ɵɵrepeaterTrackByIdentity);
        ɵɵelementEnd()()();
      }
      if (rf & 2) {
        ɵɵadvance(2);
        ɵɵconditional(ctx.logo ? 2 : -1);
        ɵɵadvance(3);
        ɵɵrepeater(ctx.menus);
      }
    },
    dependencies: [PoLogoComponent, PoMenuPanelItemComponent],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoMenuPanelComponent, [{
    type: Component,
    args: [{
      selector: "po-menu-panel",
      standalone: false,
      template: '<div class="po-menu-panel">\n  <div class="po-menu-panel-logo-container">\n    @if (logo) {\n      <po-logo p-class="po-menu-panel-logo" [p-logo]="logo" [p-logo-alt]="logoAlt"></po-logo>\n    }\n  </div>\n\n  <nav class="po-menu-panel-container">\n    <div class="po-menu-panel-inner">\n      @for (menu of menus; track menu; let menuIndex = $index) {\n        <div class="po-menu-panel-item-wrapper">\n          <po-menu-panel-item\n            [class.po-menu-panel-item-first]="menuIndex === 0"\n            [p-menu-item-internal]="menu"\n          ></po-menu-panel-item>\n        </div>\n      }\n    </div>\n  </nav>\n</div>\n'
    }]
  }], null, null);
})();
var PoMenuPanelModule = class _PoMenuPanelModule {
  static ɵfac = function PoMenuPanelModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoMenuPanelModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoMenuPanelModule,
    declarations: [PoMenuPanelComponent, PoMenuPanelItemComponent],
    imports: [CommonModule, RouterModule, PoFieldModule, PoLogoModule, PoTooltipModule, PoIconModule],
    exports: [PoMenuPanelComponent]
  });
  static ɵinj = ɵɵdefineInjector({
    providers: [PoMenuPanelItemsService],
    imports: [CommonModule, RouterModule, PoFieldModule, PoLogoModule, PoTooltipModule, PoIconModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoMenuPanelModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, RouterModule, PoFieldModule, PoLogoModule, PoTooltipModule, PoIconModule],
      declarations: [PoMenuPanelComponent, PoMenuPanelItemComponent],
      exports: [PoMenuPanelComponent],
      providers: [PoMenuPanelItemsService]
    }]
  }], null, null);
})();
var poMenuFilterLiteralsDefault = {
  en: {
    search: "Search"
  },
  es: {
    search: "Buscar"
  },
  pt: {
    search: "Pesquisar"
  },
  ru: {
    search: "Поиск"
  }
};
var PoMenuFilterComponent = class _PoMenuFilterComponent {
  languageService = inject(PoLanguageService);
  componentsSize;
  loading;
  enableCollapse = false;
  // utilizado para repassar ao po-clean
  inputFilterElement;
  filter = new EventEmitter();
  literals;
  constructor() {
    this.literals = __spreadValues(__spreadValues({}, poMenuFilterLiteralsDefault[this.languageService?.getLanguageDefault()]), poMenuFilterLiteralsDefault[this.languageService?.getShortLanguage()]);
  }
  filterItems(search) {
    this.filter.emit(search);
  }
  static ɵfac = function PoMenuFilterComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoMenuFilterComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoMenuFilterComponent,
    selectors: [["po-menu-filter"]],
    viewQuery: function PoMenuFilterComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c150, 7, ElementRef);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.inputFilterElement = _t.first);
      }
    },
    inputs: {
      componentsSize: [0, "p-components-size", "componentsSize"],
      loading: [0, "p-loading", "loading"],
      enableCollapse: [0, "p-enable-collapse", "enableCollapse"]
    },
    outputs: {
      filter: "p-filter"
    },
    standalone: false,
    decls: 8,
    vars: 11,
    consts: [["inputFilter", ""], [1, "po-menu-filter-container"], ["type", "text", 1, "po-menu-filter", 3, "keyup", "placeholder"], [1, "po-menu-filter-search-icon-container"], ["p-icon", "ICON_SEARCH", 1, "po-menu-filter-icon", "po-field-icon", 3, "po-field-icon-aa"], [3, "p-size"], [1, "po-field-icon-container-right"], [3, "p-change-event", "p-element-ref", "p-size"], ["p-icon", "ICON_SEARCH", 1, "po-menu-filter-icon", "po-field-icon"]],
    template: function PoMenuFilterComponent_Template(rf, ctx) {
      if (rf & 1) {
        const _r1 = ɵɵgetCurrentView();
        ɵɵelementStart(0, "div", 1)(1, "input", 2, 0);
        ɵɵlistener("keyup", function PoMenuFilterComponent_Template_input_keyup_1_listener() {
          ɵɵrestoreView(_r1);
          const inputFilter_r2 = ɵɵreference(2);
          return ɵɵresetView(ctx.filterItems(inputFilter_r2.value));
        });
        ɵɵelementEnd();
        ɵɵelementStart(3, "div", 3);
        ɵɵconditionalCreate(4, PoMenuFilterComponent_Conditional_4_Template, 1, 2, "po-icon", 4);
        ɵɵconditionalCreate(5, PoMenuFilterComponent_Conditional_5_Template, 1, 1, "po-loading-icon", 5);
        ɵɵelementEnd();
        ɵɵelementStart(6, "div", 6)(7, "po-clean", 7);
        ɵɵlistener("p-change-event", function PoMenuFilterComponent_Template_po_clean_p_change_event_7_listener() {
          ɵɵrestoreView(_r1);
          const inputFilter_r2 = ɵɵreference(2);
          return ɵɵresetView(ctx.filterItems(inputFilter_r2.value));
        });
        ɵɵelementEnd()()();
      }
      if (rf & 2) {
        ɵɵclassProp("po-menu-filter-container-hidden", ctx.enableCollapse);
        ɵɵadvance();
        ɵɵclassProp("po-menu-filter-aa", ctx.componentsSize === "small");
        ɵɵproperty("placeholder", ctx.literals.search);
        ɵɵadvance(2);
        ɵɵclassProp("po-menu-filter-search-icon-container-aa", ctx.componentsSize === "small");
        ɵɵadvance();
        ɵɵconditional(!ctx.loading ? 4 : -1);
        ɵɵadvance();
        ɵɵconditional(ctx.loading ? 5 : -1);
        ɵɵadvance(2);
        ɵɵproperty("p-element-ref", ctx.inputFilterElement)("p-size", ctx.componentsSize);
      }
    },
    dependencies: [PoCleanComponent, PoLoadingIconComponent, PoIconComponent],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoMenuFilterComponent, [{
    type: Component,
    args: [{
      selector: "po-menu-filter",
      standalone: false,
      template: `<div class="po-menu-filter-container" [class.po-menu-filter-container-hidden]="enableCollapse">
  <input
    #inputFilter
    type="text"
    class="po-menu-filter"
    [class.po-menu-filter-aa]="componentsSize === 'small'"
    [placeholder]="literals.search"
    (keyup)="filterItems(inputFilter.value)"
  />

  <div
    class="po-menu-filter-search-icon-container"
    [class.po-menu-filter-search-icon-container-aa]="componentsSize === 'small'"
  >
    @if (!loading) {
      <po-icon
        p-icon="ICON_SEARCH"
        class="po-menu-filter-icon po-field-icon"
        [class.po-field-icon-aa]="componentsSize === 'small'"
      ></po-icon>
    }
    @if (loading) {
      <po-loading-icon [p-size]="componentsSize === 'small' ? 'xs' : 'sm'"></po-loading-icon>
    }
  </div>
  <div class="po-field-icon-container-right">
    <po-clean
      [p-element-ref]="inputFilterElement"
      [p-size]="componentsSize"
      (p-change-event)="filterItems(inputFilter.value)"
    ></po-clean>
  </div>
</div>
`
    }]
  }], () => [], {
    componentsSize: [{
      type: Input,
      args: ["p-components-size"]
    }],
    loading: [{
      type: Input,
      args: ["p-loading"]
    }],
    enableCollapse: [{
      type: Input,
      args: ["p-enable-collapse"]
    }],
    inputFilterElement: [{
      type: ViewChild,
      args: ["inputFilter", {
        read: ElementRef,
        static: true
      }]
    }],
    filter: [{
      type: Output,
      args: ["p-filter"]
    }]
  });
})();
var PoMenuHeaderTemplateDirective = class _PoMenuHeaderTemplateDirective {
  templateRef;
  // Necessário manter templateRef para o funcionamento do row template.
  constructor(templateRef) {
    this.templateRef = templateRef;
  }
  static ɵfac = function PoMenuHeaderTemplateDirective_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoMenuHeaderTemplateDirective)(ɵɵdirectiveInject(TemplateRef));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoMenuHeaderTemplateDirective,
    selectors: [["", "p-menu-header-template", ""]],
    standalone: false
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoMenuHeaderTemplateDirective, [{
    type: Directive,
    args: [{
      selector: "[p-menu-header-template]",
      standalone: false
    }]
  }], () => [{
    type: TemplateRef
  }], null);
})();
var PoMenuItemsService = class _PoMenuItemsService {
  subjectParent = new Subject();
  subjectChild = new Subject();
  // Envia informações do click do po-menu-item para o po-menu
  sendToParentMenuClicked(menu) {
    this.subjectChild.next(menu);
  }
  // Recebe do po-menu-item sua informação de click.
  receiveFromChildMenuClicked() {
    return this.subjectChild.asObservable();
  }
  // Envia para os po-menu-item a resposta do processamento de click de um po-menu-item.
  sendToChildMenuClicked(menu) {
    this.subjectParent.next(menu);
  }
  // Recebe do po-menu as informações processadas do click de um po-menu-item.
  receiveFromParentMenuClicked() {
    return this.subjectParent.asObservable();
  }
  static ɵfac = function PoMenuItemsService_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoMenuItemsService)();
  };
  static ɵprov = ɵɵdefineInjectable({
    token: _PoMenuItemsService,
    factory: _PoMenuItemsService.ɵfac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoMenuItemsService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
var poMenuItemSubItemSize = 98;
var PoMenuItemComponent = class _PoMenuItemComponent {
  menuItemsService = inject(PoMenuItemsService);
  // Ação que será chamada ao clicar no item.
  action;
  // Indica se contém algum item filho com o badge.
  badgeAlert;
  // Cor do badge.
  badgeColor;
  // Indica se o menu está colapsado
  collapsedMenu;
  // Ícone de menu
  icon;
  // Identificador do item.
  id;
  // Indica se o item está aberto (menu agrupado)
  isOpened;
  // Texto que aparecerá representando o item.
  label;
  // Indica qual em nível do po-menu encontra-se.
  level;
  // Link do item.
  link;
  // Texto que aparecerá representando o item.
  shortLabel;
  // Indica o tipo de item, como 'internalLink' ou 'subItems'.
  type;
  menuSubItems;
  isSelectedSubItem;
  maxHeight = 0;
  itemSubscription;
  _badgeValue;
  _isSelected = false;
  _isSubItem = false;
  _subItems;
  // Valor do badge.
  set badgeValue(badgeValue) {
    this._badgeValue = convertToInt(badgeValue);
  }
  get badgeValue() {
    return this._badgeValue;
  }
  // Indica se o item está selecionado.
  set isSelected(value) {
    this._isSelected = convertToBoolean(value);
    this.isSelectedSubItem = this.isSelected && this.isSubItem;
  }
  get isSelected() {
    return this._isSelected;
  }
  // Indica se o item é um sub item
  set isSubItem(value) {
    this._isSubItem = convertToBoolean(value);
  }
  get isSubItem() {
    return this._isSubItem;
  }
  // Lista de sub-items.
  set subItems(subitems) {
    this._subItems = subitems;
    if (this.isOpened) {
      this.calcMenuSubItemsMaxHeight();
    }
  }
  get subItems() {
    return this._subItems;
  }
  get canShowBadge() {
    return this.type !== "subItems" && (this.badgeValue || this.badgeValue === 0) && this.badgeValue >= 0;
  }
  ngOnDestroy() {
    this.itemSubscription.unsubscribe();
  }
  ngOnInit() {
    this.itemSubscription = this.menuItemsService.receiveFromParentMenuClicked().subscribe((menu) => {
      this.processMenuItem(menu);
    });
  }
  clickMenuItem(event) {
    if (!(event.ctrlKey || event.metaKey)) {
      event.preventDefault();
      this.menuItemsService.sendToParentMenuClicked({
        link: this.link,
        action: this.action,
        id: this.id,
        icon: this.icon,
        label: this.label,
        level: this.level,
        subItems: this.subItems,
        isSelected: this.isSelected,
        isOpened: this.isOpened,
        shortLabel: this.shortLabel,
        type: this.type
      });
    }
  }
  accordionAnimation(menuActive, menuOpened, hasSubItemOpened, activatedByRoute) {
    if (this.id === menuOpened["id"]) {
      this.maxHeight = this.subItems.length * poMenuItemSubItemSize;
    }
    if (hasSubItemOpened) {
      this.maxHeight = menuOpened["isOpened"] ? this.maxHeight + menuOpened.subItems.length * poMenuItemSubItemSize : this.maxHeight - menuOpened.subItems.length * poMenuItemSubItemSize;
      if (activatedByRoute) {
        this.maxHeight = this.getMinimumHeight(0, this, menuActive);
      }
    }
  }
  activateMenu(menu) {
    this.isSelected = menu && this.id === menu.id;
  }
  calcMenuSubItemsMaxHeight() {
    setTimeout(() => {
      const subItems = Array.from(this.menuSubItems.nativeElement.querySelectorAll(".po-menu-item"));
      subItems.forEach((menuItem) => this.maxHeight += menuItem.offsetHeight);
    });
  }
  getMinimumHeight(minimumHeight, menuItem, menuActive) {
    minimumHeight += poMenuItemSubItemSize;
    if (menuItem.subItems && this.hasSubItem(menuItem.subItems, menuActive["id"])) {
      for (let index = 0; index < menuItem.subItems.length; index++) {
        minimumHeight = this.getMinimumHeight(minimumHeight, menuItem.subItems[index], menuActive);
      }
    }
    return minimumHeight;
  }
  groupedMenu(menuActive, menuOpened, activatedByRoute = false) {
    const hasSubItemOpened = menuOpened && this.id !== menuOpened["id"] ? this.hasSubItem(this.subItems, menuOpened["id"]) : false;
    this.isOpened = this.isMenuOpened(menuOpened, hasSubItemOpened);
    this.isSelected = menuActive && !this.isOpened ? this.hasSubItem(this.subItems, menuActive["id"]) : false;
    if (!this.isOpened) {
      this.maxHeight = 0;
      return;
    }
    this.accordionAnimation(menuActive, menuOpened, hasSubItemOpened, activatedByRoute);
  }
  hasSubItem(subItems, id) {
    if (subItems) {
      return subItems.some((item) => item["id"] === id ? true : this.hasSubItem(item.subItems, id));
    }
  }
  isMenuOpened(menuOpened, hasSubItemOpened) {
    if (menuOpened) {
      return this.id === menuOpened["id"] ? menuOpened["isOpened"] : hasSubItemOpened;
    }
    return false;
  }
  processMenuItem(menu) {
    if (this.type === "internalLink") {
      this.activateMenu(menu.active);
      return;
    }
    if (this.type === "subItems") {
      this.groupedMenu(menu.active, menu.grouped, menu.activatedByRoute);
    }
  }
  static ɵfac = function PoMenuItemComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoMenuItemComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoMenuItemComponent,
    selectors: [["po-menu-item"]],
    viewQuery: function PoMenuItemComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c151, 5);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.menuSubItems = _t.first);
      }
    },
    inputs: {
      action: [0, "p-action", "action"],
      badgeAlert: [0, "p-badge-alert", "badgeAlert"],
      badgeColor: [0, "p-badge-color", "badgeColor"],
      collapsedMenu: [0, "p-collapsed-menu", "collapsedMenu"],
      icon: [0, "p-icon", "icon"],
      id: [0, "p-id", "id"],
      isOpened: [0, "p-is-opened", "isOpened"],
      label: [0, "p-label", "label"],
      level: [0, "p-level", "level"],
      link: [0, "p-link", "link"],
      shortLabel: [0, "p-short-label", "shortLabel"],
      type: [0, "p-type", "type"],
      badgeValue: [0, "p-badge-value", "badgeValue"],
      isSelected: [0, "p-is-selected", "isSelected"],
      isSubItem: [0, "p-is-sub-item", "isSubItem"],
      subItems: [0, "p-sub-items", "subItems"]
    },
    standalone: false,
    decls: 7,
    vars: 5,
    consts: [["menuItemTemplate", ""], ["menuSubItems", ""], ["role", "menuitem", "p-tooltip-position", "right", 1, "po-menu-item-link", 3, "routerLink", "p-tooltip", "p-append-in-body"], ["role", "menuitem", "p-tooltip-position", "right", 1, "po-menu-item-link", 3, "href", "p-tooltip", "p-append-in-body"], ["href", "javascript:;", "role", "menuitem", "p-tooltip-position", "right", 1, "po-menu-item-link", 3, "p-tooltip", "p-append-in-body"], ["role", "none", 1, "po-menu-item-link"], ["role", "menuitem", 1, "po-menu-item-link", "po-clickable"], ["role", "menuitem", "p-tooltip-position", "right", 1, "po-menu-item-link", 3, "keydown.enter", "keydown.space", "routerLink", "p-tooltip", "p-append-in-body"], [4, "ngTemplateOutlet"], ["role", "menuitem", "p-tooltip-position", "right", 1, "po-menu-item-link", 3, "keydown.enter", "keydown.space", "href", "p-tooltip", "p-append-in-body"], ["href", "javascript:;", "role", "menuitem", "p-tooltip-position", "right", 1, "po-menu-item-link", 3, "keydown.enter", "keydown.space", "p-tooltip", "p-append-in-body"], ["role", "group", 1, "po-menu-sub-items", 3, "hidden"], [1, "po-menu-sub-items-list"], [1, "po-menu-sub-items-list-item"], ["p-is-sub-item", "", 3, "p-action", "p-badge-alert", "p-badge-color", "p-badge-value", "p-id", "p-label", "p-level", "p-link", "p-sub-items", "p-type"], ["p-tooltip-position", "right", 1, "po-menu-item", 3, "click", "keydown.enter", "keydown.space", "p-tooltip", "p-append-in-body", "tabindex"], ["aria-hidden", "true", 1, "po-menu-icon-item", 3, "p-icon"], [1, "po-menu-group-icon", 3, "p-icon"], ["aria-hidden", "true", 3, "ngClass", "p-color", "p-value"], ["aria-hidden", "true", 1, "po-color-07", 3, "ngClass"]],
    template: function PoMenuItemComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵconditionalCreate(0, PoMenuItemComponent_Conditional_0_Template, 2, 5, "a", 2);
        ɵɵconditionalCreate(1, PoMenuItemComponent_Conditional_1_Template, 2, 5, "a", 3);
        ɵɵconditionalCreate(2, PoMenuItemComponent_Conditional_2_Template, 2, 4, "a", 4);
        ɵɵconditionalCreate(3, PoMenuItemComponent_Conditional_3_Template, 2, 1, "div", 5);
        ɵɵconditionalCreate(4, PoMenuItemComponent_Conditional_4_Template, 7, 7, "div", 6);
        ɵɵtemplate(5, PoMenuItemComponent_ng_template_5_Template, 7, 30, "ng-template", null, 0, ɵɵtemplateRefExtractor);
      }
      if (rf & 2) {
        ɵɵconditional(ctx.type === "internalLink" ? 0 : -1);
        ɵɵadvance();
        ɵɵconditional(ctx.type === "externalLink" ? 1 : -1);
        ɵɵadvance();
        ɵɵconditional(ctx.type === "noLink" ? 2 : -1);
        ɵɵadvance();
        ɵɵconditional(ctx.type === "noData" ? 3 : -1);
        ɵɵadvance();
        ɵɵconditional(ctx.type === "subItems" ? 4 : -1);
      }
    },
    dependencies: [NgClass, NgTemplateOutlet, RouterLink, PoBadgeComponent, PoIconComponent, PoTooltipDirective, _PoMenuItemComponent],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoMenuItemComponent, [{
    type: Component,
    args: [{
      selector: "po-menu-item",
      standalone: false,
      template: `<!-- menu com link interno -->
@if (type === 'internalLink') {
  <a
    class="po-menu-item-link"
    role="menuitem"
    [attr.aria-label]="label"
    [routerLink]="link"
    (keydown.enter)="clickMenuItem($event)"
    (keydown.space)="clickMenuItem($event)"
    p-tooltip-position="right"
    [p-tooltip]="collapsedMenu && shortLabel ? shortLabel : undefined"
    [p-append-in-body]="true"
  >
    <ng-container *ngTemplateOutlet="menuItemTemplate"></ng-container>
  </a>
}
<!-- menu com link externo -->
@if (type === 'externalLink') {
  <a
    class="po-menu-item-link"
    role="menuitem"
    [attr.aria-label]="label"
    [href]="link"
    (keydown.enter)="clickMenuItem($event)"
    (keydown.space)="clickMenuItem($event)"
    p-tooltip-position="right"
    [p-tooltip]="collapsedMenu && shortLabel ? shortLabel : undefined"
    [p-append-in-body]="true"
  >
    <ng-container *ngTemplateOutlet="menuItemTemplate"></ng-container>
  </a>
}
<!-- menu sem link -->
@if (type === 'noLink') {
  <a
    class="po-menu-item-link"
    href="javascript:;"
    role="menuitem"
    [attr.aria-label]="label"
    (keydown.enter)="clickMenuItem($event)"
    (keydown.space)="clickMenuItem($event)"
    p-tooltip-position="right"
    [p-tooltip]="collapsedMenu && shortLabel ? shortLabel : undefined"
    [p-append-in-body]="true"
  >
    <ng-container *ngTemplateOutlet="menuItemTemplate"></ng-container>
  </a>
}
<!-- menu sem dados -->
@if (type === 'noData') {
  <div class="po-menu-item-link" role="none">
    <ng-container *ngTemplateOutlet="menuItemTemplate"></ng-container>
  </div>
}
<!-- menu com sub itens -->
@if (type === 'subItems') {
  <div class="po-menu-item-link po-clickable" role="menuitem" [attr.aria-expanded]="isOpened ? true : false">
    <ng-container *ngTemplateOutlet="menuItemTemplate"></ng-container>
    <div
      #menuSubItems
      class="po-menu-sub-items"
      [hidden]="collapsedMenu || !isOpened"
      [attr.aria-expanded]="isOpened ? true : false"
      [attr.aria-hidden]="!isOpened ? true : false"
      role="group"
      [style.maxHeight.px]="maxHeight"
    >
      <ul class="po-menu-sub-items-list">
        @for (subItem of subItems; track $index) {
          <li class="po-menu-sub-items-list-item" [attr.aria-level]="subItem.level">
            <po-menu-item
              p-is-sub-item
              [p-action]="subItem.action"
              [p-badge-alert]="subItem.badgeAlert"
              [p-badge-color]="subItem.badge ? subItem.badge.color : undefined"
              [p-badge-value]="subItem.badge ? subItem.badge.value : undefined"
              [p-id]="subItem.id"
              [p-label]="subItem.label"
              [p-level]="subItem.level"
              [p-link]="subItem.link"
              [p-sub-items]="subItem.subItems"
              [p-type]="subItem.type"
            ></po-menu-item>
          </li>
        }
      </ul>
    </div>
  </div>
}

<ng-template #menuItemTemplate>
  <div
    p-tooltip-position="right"
    [p-tooltip]="type === 'subItems' && collapsedMenu && shortLabel ? shortLabel : undefined"
    [p-append-in-body]="true"
    class="po-menu-item"
    [tabindex]="type === 'subItems' ? 0 : -1"
    [attr.aria-label]="label"
    [attr.role]="type === 'subItems' ? 'menuitem' : undefined"
    [class.po-menu-icon-container]="level === 1 && icon"
    [class.po-menu-item-selected]="isSelected"
    [class.po-menu-item-level-two]="level === 2"
    [class.po-menu-item-level-three]="level === 3"
    [class.po-menu-item-level-four]="level === 4"
    [class.po-menu-item-grouper-up]="type === 'subItems' && isOpened"
    [class.po-menu-item-grouper-down]="type === 'subItems' && !isOpened"
    [class.po-menu-item-no-data]="type === 'noData'"
    [class.po-menu-sub-item-selected]="isSelectedSubItem"
    (click)="clickMenuItem($event)"
    (keydown.enter)="clickMenuItem($event)"
    (keydown.space)="clickMenuItem($event)"
  >
    @if (level === 1 && icon) {
      <po-icon aria-hidden="true" class="po-menu-icon-item" [p-icon]="icon"></po-icon>
    }

    @if (type === 'subItems' && !collapsedMenu) {
      <po-icon
        class="po-menu-group-icon"
        [p-icon]="isOpened ? 'ICON_ARROW_UP po-field-icon' : 'ICON_ARROW_DOWN po-field-icon'"
      ></po-icon>
    }

    <span [class.po-menu-icon-label]="level === 1 && icon">
      {{ label }}
    </span>

    @if (canShowBadge) {
      <po-badge
        aria-hidden="true"
        [ngClass]="!collapsedMenu ? 'po-menu-badge-align' : 'po-menu-badge-align-collapsed'"
        [p-color]="badgeColor"
        [p-value]="badgeValue"
      ></po-badge>
    }

    @if (badgeAlert) {
      <span
        aria-hidden="true"
        class="po-color-07"
        [ngClass]="!collapsedMenu ? 'po-menu-badge-alert' : 'po-menu-badge-alert-collapsed'"
      ></span>
    }
  </div>
</ng-template>
`
    }]
  }], null, {
    action: [{
      type: Input,
      args: ["p-action"]
    }],
    badgeAlert: [{
      type: Input,
      args: ["p-badge-alert"]
    }],
    badgeColor: [{
      type: Input,
      args: ["p-badge-color"]
    }],
    collapsedMenu: [{
      type: Input,
      args: ["p-collapsed-menu"]
    }],
    icon: [{
      type: Input,
      args: ["p-icon"]
    }],
    id: [{
      type: Input,
      args: ["p-id"]
    }],
    isOpened: [{
      type: Input,
      args: ["p-is-opened"]
    }],
    label: [{
      type: Input,
      args: ["p-label"]
    }],
    level: [{
      type: Input,
      args: ["p-level"]
    }],
    link: [{
      type: Input,
      args: ["p-link"]
    }],
    shortLabel: [{
      type: Input,
      args: ["p-short-label"]
    }],
    type: [{
      type: Input,
      args: ["p-type"]
    }],
    menuSubItems: [{
      type: ViewChild,
      args: ["menuSubItems"]
    }],
    badgeValue: [{
      type: Input,
      args: ["p-badge-value"]
    }],
    isSelected: [{
      type: Input,
      args: ["p-is-selected"]
    }],
    isSubItem: [{
      type: Input,
      args: ["p-is-sub-item"]
    }],
    subItems: [{
      type: Input,
      args: ["p-sub-items"]
    }]
  });
})();
var PoMenuGlobalService = class _PoMenuGlobalService {
  applicationMenu = new Subject();
  menus = new Subject();
  removedApplicationMenu = new Subject();
  menuId = new Subject();
  menuChanges = new Subject();
  // eslint-disable-next-line @typescript-eslint/member-ordering
  receiveApplicationMenu$ = this.applicationMenu.asObservable();
  // eslint-disable-next-line @typescript-eslint/member-ordering
  receiveMenus$ = this.menus.asObservable();
  // eslint-disable-next-line @typescript-eslint/member-ordering
  receiveId$ = this.menuId.asObservable();
  // eslint-disable-next-line @typescript-eslint/member-ordering
  receiveOnChange$ = this.menuChanges.asObservable();
  // eslint-disable-next-line @typescript-eslint/member-ordering
  receiveRemovedApplicationMenu$ = this.removedApplicationMenu.asObservable();
  sendApplicationMenu(menu) {
    this.applicationMenu.next(menu);
  }
  sendMenus(menus) {
    this.menus.next(menus);
  }
  sendId(id) {
    this.menuId.next(id);
  }
  sendChanges(menus) {
    this.menuChanges.next(menus);
  }
  sendRemovedApplicationMenu(id) {
    this.removedApplicationMenu.next(id);
  }
  static ɵfac = function PoMenuGlobalService_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoMenuGlobalService)();
  };
  static ɵprov = ɵɵdefineInjectable({
    token: _PoMenuGlobalService,
    factory: _PoMenuGlobalService.ɵfac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoMenuGlobalService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
var PoMenuService = class _PoMenuService {
  http = inject(HttpClient);
  _url;
  get url() {
    return this._url;
  }
  configProperties(url) {
    this._url = url;
  }
  getFilteredData(search, params) {
    const filterParams = __spreadValues({
      search
    }, params);
    return this.http.get(this.url, {
      params: filterParams
    }).pipe(map((response) => response && response.items));
  }
  static ɵfac = function PoMenuService_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoMenuService)();
  };
  static ɵprov = ɵɵdefineInjectable({
    token: _PoMenuService,
    factory: _PoMenuService.ɵfac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoMenuService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
var poMenuLiteralsDefault = {
  en: {
    itemNotFound: "Item not found",
    emptyLabelError: "Attribute PoMenuItem.label can not be empty",
    close: "Close menu",
    open: "Open menu"
  },
  es: {
    itemNotFound: "Elemento no encontrado",
    emptyLabelError: "El atributo PoMenuItem.label no puede ser vacío",
    close: "Cerrar menú",
    open: "Abrir menú"
  },
  pt: {
    itemNotFound: "Item não encontrado",
    emptyLabelError: "O atributo PoMenuItem.label não pode ser vazio",
    close: "Fechar menu",
    open: "Abrir menu"
  },
  ru: {
    itemNotFound: "Предмет не найден",
    emptyLabelError: "Атрибут PoMenuItem.label не может быть пустым",
    close: "Закрыть меню",
    open: "Открыть меню"
  }
};
var PoMenuBaseComponent = class _PoMenuBaseComponent {
  menuGlobalService;
  menuService;
  languageService;
  allowIcons;
  allowCollapseMenu;
  allowCollapseHover;
  filteredItems;
  filterService;
  literals;
  _collapsed = false;
  _componentsSize = void 0;
  _initialComponentsSize = void 0;
  _filter = false;
  _searchTreeItems = false;
  _level;
  _maxLevel = 4;
  _menus = [];
  _params;
  _service;
  _logoLink = true;
  _menuId;
  /**
   * @optional
   *
   * @description
   *
   * Expande e Colapsa (retrai) o menu automaticamente.
   *
   * @default `false`
   */
  automaticToggle = false;
  /**
   * @optional
   *
   * @description
   *
   * Evento emitido toda vez que o estado do menu muda, enviando `true` quando expandido e `false` quando colapsado.
   *
   * ```html
   * <po-menu (p-toggle)="onMenuToggle($event)"></po-menu>
   * ```
   */
  toggleChange = output({
    alias: "p-toggle"
  });
  /**
   * @optional
   *
   * @description
   *
   * Colapsa (retrai) o menu e caso receba o valor `false` expande o menu.
   *
   * > Utilize esta propriedade para iniciar o menu colapsado.
   *
   * > Ao utilizar os métodos [`colapse`](documentation/po-menu#colapseMethod), [`expand`](documentation/po-menu#expandMethod) e
   * [`toggle`](documentation/po-menu#toggleMethod) o valor desta propriedade não é alterado.
   *
   * **Importante:**
   *
   * > O menu será colapsado/expandido apenas se todos os itens de menu tiverem valor nas propriedades `icon` e `shortLabel`.
   *
   * @default `false`
   */
  set collapsed(collapsed) {
    this._collapsed = convertToBoolean(collapsed);
    this.allowCollapseHover = this._collapsed;
    this.validateCollapseClass();
  }
  get collapsed() {
    return this._collapsed;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define o tamanho dos componentes de formulário no menu:
   * - `small`: aplica a medida small de cada componente (disponível apenas para acessibilidade AA).
   * - `medium`: aplica a medida medium de cada componente.
   *
   * > Caso a acessibilidade AA não esteja configurada, o tamanho `medium` será mantido.
   * Para mais detalhes, consulte a documentação do [po-theme](https://po-ui.io/documentation/po-theme).
   *
   * @default `medium`
   */
  set componentsSize(value) {
    this._initialComponentsSize = value;
    this.applySizeBasedOnA11y();
  }
  get componentsSize() {
    return this._componentsSize ?? getDefaultSizeFn(PoFieldSize);
  }
  /** Lista dos itens do menu. Se o valor estiver indefinido ou inválido, será inicializado como um array vazio. */
  set menus(menus) {
    this._menus = Array.isArray(menus) ? menus : [];
    this.menuGlobalService.sendMenus(menus);
    setTimeout(() => {
      const urlRouter = this.checkingRouterChildrenFragments();
      this.checkActiveMenuByUrl(urlRouter);
    });
  }
  get menus() {
    return this._menus;
  }
  get maxLevel() {
    return this._maxLevel;
  }
  /**
   * @optional
   *
   * @description
   *
   * Habilita um campo para pesquisa no menu.
   * A pesquisa é realizada em todos os níveis do menu e busca apenas pelos itens que contém uma ação e/ou link definidos,
   * ou também, pode ser realizada através de um serviço definido na propriedade `p-service`.
   *
   * > O campo de pesquisa é desabilitado se o menu estiver colapsado.
   *
   * @default `false`
   */
  set filter(filter3) {
    this._filter = filter3 === "" ? true : convertToBoolean(filter3);
    this.filteredItems = [...this._menus];
  }
  get filter() {
    return this._filter;
  }
  /**
   * @optional
   *
   * @description
   *
   * Quando ativado, a pesquisa também retornará itens agrupadores além dos itens que contêm uma ação e/ou link definidos.
   * Isso pode ser útil quando se deseja encontrar rapidamente categorias ou seções do menu.
   *
   * > É necessário que a propriedade `p-filter` esteja habilitada.
   *
   * @default `false`
   */
  set searchTreeItems(searchTreeItems) {
    this._searchTreeItems = searchTreeItems === "" ? true : convertToBoolean(searchTreeItems);
    this.filteredItems = [...this._menus];
  }
  get searchTreeItems() {
    return this._searchTreeItems;
  }
  /**
   * @optional
   *
   * @description
   *
   * Nesta propriedade deve ser informada a URL do serviço em que será utilizado para realizar o filtro de itens do
   * menu quando realizar uma busca. Caso haja a necessidade de customização, pode ser informado um
   * serviço implementando a interface `PoMenuFilter`.
   *
   * Caso utilizada uma URL, o serviço deve retornar os dados conforme o
   * [Guia de implementação de APIs](https://po-ui.io/guides/api) do PO UI.
   *
   * Quando utilizada uma URL de serviço, será realizado um *GET* na URL informada, passando o valor digitado
   * no parâmetro `search`, veja exemplo:
   *
   * > O filtro no serviço será realizado caso contenha no mínimo três caracteres no campo de busca, por exemplo `tot`.
   *
   * ```
   * <po-menu p-service="/api/v1/fnd/menu">
   * </po-menu>
   *
   * Requisição: GET /api/v1/fnd/menu?search=contas
   * ```
   *
   * > É necessário que propriedade `p-filter` esteja habilitada.
   */
  set service(value) {
    this._service = value || void 0;
    this.configService(this.service);
  }
  get service() {
    return this._service;
  }
  /**
   * @optional
   *
   * @description
   *
   * Deve ser informado um objeto que deseja-se utilizar na requisição de filtro dos itens de menu.
   *
   * Caso utilizado um serviço customizado, implementando a interface `PoMenuFilter`, o valor desta propriedade
   * será passado como parâmetro, na função `getFilteredData`.
   *
   * Quando utilizada uma URL de serviço, será realizado um *GET* na URL informada, passando os valores informados
   * nesta propriedade em conjunto com o parâmetro `search`, veja exemplo:
   *
   * ```
   * <po-menu p-service="/api/v1/fnd/menu" [p-params]="{ company: 1, user: 297767512 }">
   * </po-menu>
   *
   * Requisição: GET /api/v1/fnd/menu?search=contas&company=1&user=297767512
   * ```
   */
  set params(value) {
    this._params = value && isTypeof(value, "object") ? value : void 0;
  }
  get params() {
    return this._params;
  }
  /**
   * @optional
   *
   * @description
   *
   * Caminho para a logomarca, que será exibida quando o componente estiver expandido, localizada na parte superior.
   *
   * > **Importante:**
   * - Caso esta propriedade estiver indefinida ou inválida o espaço para logomarca será removido.
   * - Como boa prática, indica-se utilizar imagens com até `24px` de altura e `224px` de largura,
   * caso ultrapassar esses valores a imagem será readequada no espaço disponível.
   */
  logo;
  set menuid(id) {
    this.menuGlobalService.sendId(id);
    this._menuId = id;
  }
  get menuid() {
    return this._menuId;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define o texto alternativo para a logomarca.
   *
   * > **Importante**
   * > Caso esta propriedade não seja definida o texto padrão será "Logomarca início".
   *
   * @default `Logomarca início`
   */
  logoAlt;
  /**
   * @optional
   *
   * @description
   *
   * Caminho para a logomarca, que será exibida quando o componente estiver colapsado, localizada na parte superior.
   *
   * > **Importante:**
   * - Caso esta propriedade estiver indefinida ou inválida passa a assumir o valor informado na propriedade `p-logo` e na ausência desta o
   * espaço para logomarca será removido.
   * - Como boa prática, indica-se utilizar imagens com até `48px` de altura e `48px` de largura,
   * caso ultrapassar esses valores a imagem será readequada no espaço disponível.
   * - Caso não informar um valor, esta propriedade passa a assumir o valor informado na propriedade `p-logo`.
   */
  shortLogo;
  // propriedade interna
  onlyMenuItem = false;
  /**
   * @optional
   *
   * @description
   * Define o link para a rota ao clicar no logo do menu.
   *
   * - Se o valor for uma string, define a rota para o link informado.
   * - Se for `false`, o logo não terá link associado.
   * - Se for `true`, o logo terá a rota padrão `./`.
   *
   * @default `true`
   */
  set logoLink(value) {
    this._logoLink = value === false ? false : value || true;
  }
  get logoLink() {
    return this._logoLink;
  }
  constructor(menuGlobalService, menuService, languageService) {
    this.menuGlobalService = menuGlobalService;
    this.menuService = menuService;
    this.languageService = languageService;
    this.literals = __spreadValues(__spreadValues({}, poMenuLiteralsDefault[this.languageService?.getLanguageDefault()]), poMenuLiteralsDefault[this.languageService?.getShortLanguage()]);
  }
  onThemeChange() {
    this.applySizeBasedOnA11y();
  }
  setMenuExtraProperties() {
    this.allowIcons = !!this.menus.length;
    this.allowCollapseMenu = !!this.menus.length;
    this.menus.forEach((menuItem) => {
      this._level = 1;
      this.allowIcons = this.allowIcons ? validValue(menuItem.icon) : false;
      this.allowCollapseMenu = this.allowCollapseMenu && this.allowIcons ? validValue(menuItem.shortLabel) : false;
      this.removeBadgeAlert(menuItem);
      this.setMenuItemProperties(menuItem);
      if (menuItem.subItems) {
        this._level++;
        this.processSubItems(menuItem);
      }
    });
  }
  setMenuItemProperties(menuItem) {
    menuItem["id"] = menuItem["id"] || uuid();
    menuItem["level"] = this._level;
    menuItem["type"] = this.setMenuType(menuItem);
  }
  validateMenus(menus) {
    menus.forEach((menu) => this.validateMenu(menu));
  }
  setMenuType(menuItem) {
    if (menuItem.subItems && menuItem.subItems.length > 0 && this._level < this.maxLevel) {
      return "subItems";
    }
    if (!menuItem.link) {
      return "noLink";
    }
    if (isExternalLink(menuItem.link)) {
      return "externalLink";
    }
    return "internalLink";
  }
  configService(service) {
    if (typeof service === "string" && service.trim()) {
      this.menuService.configProperties(service);
      this.filterService = this.menuService;
    } else if (typeof service === "object" && service.getFilteredData) {
      this.filterService = service;
    } else {
      this.filterService = void 0;
    }
  }
  processSubItems(menu) {
    menu.subItems.forEach((menuItem, index, menuItems) => {
      const previousItem = menuItems[index - 1];
      if (previousItem && previousItem.subItems) {
        this._level = previousItem["level"];
      }
      if (this._level <= this.maxLevel) {
        this.setMenuItemProperties(menuItem);
        if (menuItem.subItems) {
          this._level++;
          this.processSubItems(menuItem);
        }
      }
      if (!menu["badgeAlert"]) {
        menu = this.setMenuBadgeAlert(menu, menuItem);
      }
    });
    menu.subItems = Object.assign([], menu.subItems);
  }
  removeBadgeAlert(menuItem) {
    if (menuItem["badgeAlert"]) {
      delete menuItem["badgeAlert"];
    }
    if (menuItem.subItems) {
      menuItem.subItems.forEach((subItem) => this.removeBadgeAlert(subItem));
    }
  }
  setMenuBadgeAlert(parent, child) {
    const childHasSubItems = child.subItems && child.subItems.length;
    const childHasBadgeAlert = child["badgeAlert"];
    const childHasBadge = child.badge && convertToInt(child.badge.value) >= 0;
    parent["badgeAlert"] = childHasBadgeAlert || childHasBadge && !childHasSubItems;
    return parent;
  }
  validateMenu(menuItem) {
    if (!menuItem.label || menuItem.label.trim() === "") {
      throw new Error(this.literals.emptyLabelError);
    } else if (menuItem.subItems) {
      menuItem.subItems.forEach((subItem) => {
        this.validateMenu(subItem);
      });
    }
  }
  applySizeBasedOnA11y() {
    const size = validateSizeFn(this._initialComponentsSize, PoFieldSize);
    this._componentsSize = size;
  }
  static ɵfac = function PoMenuBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoMenuBaseComponent)(ɵɵdirectiveInject(PoMenuGlobalService), ɵɵdirectiveInject(PoMenuService), ɵɵdirectiveInject(PoLanguageService));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoMenuBaseComponent,
    hostVars: 1,
    hostBindings: function PoMenuBaseComponent_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("PoUiThemeChange", function PoMenuBaseComponent_PoUiThemeChange_HostBindingHandler() {
          return ctx.onThemeChange();
        }, ɵɵresolveWindow);
      }
      if (rf & 2) {
        ɵɵattribute("p-components-size", ctx.componentsSize);
      }
    },
    inputs: {
      automaticToggle: [2, "p-automatic-toggle", "automaticToggle", convertToBoolean],
      collapsed: [0, "p-collapsed", "collapsed"],
      componentsSize: [0, "p-components-size", "componentsSize"],
      menus: [0, "p-menus", "menus"],
      filter: [0, "p-filter", "filter"],
      searchTreeItems: [0, "p-search-tree-items", "searchTreeItems"],
      service: [0, "p-service", "service"],
      params: [0, "p-params", "params"],
      logo: [0, "p-logo", "logo"],
      menuid: [0, "p-menu-id", "menuid"],
      logoAlt: [0, "p-logo-alt", "logoAlt"],
      shortLogo: [0, "p-short-logo", "shortLogo"],
      onlyMenuItem: [0, "p-only-menu-item", "onlyMenuItem"],
      logoLink: [0, "p-logo-link", "logoLink"]
    },
    outputs: {
      toggleChange: "p-toggle"
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoMenuBaseComponent, [{
    type: Directive
  }], () => [{
    type: PoMenuGlobalService
  }, {
    type: PoMenuService
  }, {
    type: PoLanguageService
  }], {
    automaticToggle: [{
      type: Input,
      args: [{
        alias: "p-automatic-toggle",
        transform: convertToBoolean
      }]
    }],
    toggleChange: [{
      type: Output,
      args: ["p-toggle"]
    }],
    collapsed: [{
      type: Input,
      args: ["p-collapsed"]
    }],
    componentsSize: [{
      type: Input,
      args: ["p-components-size"]
    }, {
      type: HostBinding,
      args: ["attr.p-components-size"]
    }],
    menus: [{
      type: Input,
      args: ["p-menus"]
    }],
    filter: [{
      type: Input,
      args: ["p-filter"]
    }],
    searchTreeItems: [{
      type: Input,
      args: ["p-search-tree-items"]
    }],
    service: [{
      type: Input,
      args: ["p-service"]
    }],
    params: [{
      type: Input,
      args: ["p-params"]
    }],
    logo: [{
      type: Input,
      args: ["p-logo"]
    }],
    menuid: [{
      type: Input,
      args: ["p-menu-id"]
    }],
    logoAlt: [{
      type: Input,
      args: ["p-logo-alt"]
    }],
    shortLogo: [{
      type: Input,
      args: ["p-short-logo"]
    }],
    onlyMenuItem: [{
      type: Input,
      args: ["p-only-menu-item"]
    }],
    logoLink: [{
      type: Input,
      args: ["p-logo-link"]
    }],
    onThemeChange: [{
      type: HostListener,
      args: ["window:PoUiThemeChange"]
    }]
  });
})();
var poMenuDebounceTime = 400;
var poMenuMinLength = 3;
var poMenuRootLevel = 1;
var PoMenuComponent = class _PoMenuComponent extends PoMenuBaseComponent {
  changeDetector = inject(ChangeDetectorRef);
  element = inject(ElementRef);
  renderer = inject(Renderer2);
  router = inject(Router);
  menuItemsService = inject(PoMenuItemsService);
  menuHeaderTemplate;
  activeMenuItem;
  collapsedMobile;
  filterLoading = false;
  groupedMenuItem;
  id = uuid();
  linkActive;
  mobileOpened = false;
  noData = false;
  timeoutFilter;
  afterViewInitWasCalled = false;
  filteringItems = false;
  menuInitialized = false;
  menuPrevious = null;
  resizeListener;
  itemSubscription;
  routeSubscription;
  /* eslint-disable max-params */
  constructor() {
    const menuGlobalService = inject(PoMenuGlobalService);
    const menuService = inject(PoMenuService);
    const languageService = inject(PoLanguageService);
    super(menuGlobalService, menuService, languageService);
  }
  /* eslint-enable max-params */
  get isActiveItemMenuSubMenu() {
    return this.activeMenuItem["level"] > this.groupedMenuItem["level"];
  }
  get enableCollapse() {
    return this.isCollapsed && !this.collapsedMobile;
  }
  get enableCollapseButton() {
    return this.allowCollapseMenu && !this.collapsed && !this.mobileOpened;
  }
  get hasFooter() {
    return this.enableCollapseButton || this.enableCollapse;
  }
  get isCollapsed() {
    return this.allowCollapseMenu && this.collapsed;
  }
  ngDoCheck() {
    if (this.filteringItems && this.filter) {
      return;
    }
    const menuCurrent = this.stringify(this.menus);
    if (this.menuPrevious !== menuCurrent || !this.menuInitialized) {
      this.updateMenu();
      this.validateCollapseClass();
    }
  }
  ngOnDestroy() {
    this.itemSubscription.unsubscribe();
    this.routeSubscription.unsubscribe();
    if (this.resizeListener) {
      this.resizeListener();
    }
    this.menuGlobalService.sendRemovedApplicationMenu(this.id);
  }
  ngOnInit() {
    this.subscribeToMenuItem();
    this.subscribeToRoute();
    this.menuGlobalService.sendId(this.menuid);
  }
  ngOnChanges(changes) {
    if (changes["menus"] && this.afterViewInitWasCalled) {
      this.menuGlobalService.sendChanges(changes["menus"].currentValue);
      this.changeDetector.detectChanges();
    }
  }
  ngAfterViewInit() {
    this.menuGlobalService.sendApplicationMenu(this);
    this.afterViewInitWasCalled = true;
  }
  activateMenuByUrl(urlPath, menus) {
    if (menus) {
      const urlPathWithoutLastFragment = urlPath.substr(0, urlPath.lastIndexOf("/"));
      return menus.some((menu) => {
        const formattedMenuLink = getFormattedLink(menu.link);
        const menuLinkPath = `${urlPathWithoutLastFragment}${formattedMenuLink.substr(formattedMenuLink.lastIndexOf("/"))}`;
        if (menuLinkPath === urlPath && menuLinkPath === formattedMenuLink) {
          this.linkActive = formattedMenuLink;
          this.activateMenuItem(menu);
          return true;
        } else {
          return this.activateMenuByUrl(urlPath, menu.subItems);
        }
      });
    }
  }
  checkActiveMenuByUrl(urlPath) {
    if (!this.linkActive || this.linkActive !== urlPath) {
      this.activateMenuByUrl(urlPath, this.menus);
    }
  }
  /**
   * <a id="colapseMethod"></a>
   *
   * Método para colapsar (retrair) o menu.
   */
  collapse() {
    this.validateToggleMenu(true);
  }
  debounceFilter(filter3) {
    clearTimeout(this.timeoutFilter);
    this.timeoutFilter = setTimeout(() => {
      this.filterProcess(filter3);
    }, poMenuDebounceTime);
  }
  /**
   * <a id="expandMethod"></a>
   *
   * Método para expandir (aumentar) o menu.
   */
  expand() {
    this.validateToggleMenu(false);
  }
  subscribeToMenuItem() {
    this.itemSubscription = this.menuItemsService.receiveFromChildMenuClicked().subscribe((menu) => {
      this.clickMenuItem(menu);
    });
  }
  subscribeToRoute() {
    this.routeSubscription = this.router.events.subscribe((val) => {
      if (val instanceof NavigationEnd || val instanceof NavigationCancel) {
        const urlRouter = this.checkingRouterChildrenFragments();
        this.checkActiveMenuByUrl(urlRouter);
      }
    });
  }
  /**
   * <a id="toggleMethod"></a>
   * Método que colapsa e expande o menu alternadamente.
   *
   * > Os métodos apenas vão colapsar/expandir o menu se:
   *  - Todos os itens de menu tiverem valor nas propriedades `icon` e `shortLabel`.
   */
  toggle() {
    this.validateToggleMenu(!this.collapsed);
  }
  toggleMenuMobile() {
    this.mobileOpened = !this.mobileOpened;
    this.collapsedMobile = this.collapsed && this.mobileOpened;
    this.validateCollapseClass(this.collapsedMobile);
    if (PoUtils.isMobile()) {
      return;
    }
    if (this.mobileOpened) {
      this.createResizeListener();
    }
  }
  onMouseEnter() {
    if (this.collapsed && this.automaticToggle) {
      this.setCollapsed(false);
      this.allowCollapseHover = true;
    }
  }
  onMouseLeave() {
    if (!this.collapsed && this.allowCollapseHover && this.automaticToggle) {
      this.setCollapsed(true);
    }
  }
  checkingRouterChildrenFragments() {
    const childrenPrimary = this.router.parseUrl(this.router.url).root.children["primary"];
    return childrenPrimary ? `/${childrenPrimary.segments.map((it) => it.path).join("/")}` : "";
  }
  validateCollapseClass(collapsedMobile = false) {
    const wrapper = this.element.nativeElement.parentNode;
    this.renderer[this.isCollapsed && !collapsedMobile ? "addClass" : "removeClass"](wrapper, "po-collapsed-menu");
  }
  activateCollapseSubMenuItem() {
    this.clearGroupMenuIfFirstLevel(this.activeMenuItem);
    if (!this.collapsed && this.activeMenuItem["level"] > poMenuRootLevel && this.isActiveItemMenuSubMenu) {
      this.openParentMenu(this.activeMenuItem);
    }
  }
  activateMenuItem(menu) {
    this.activeMenuItem = menu;
    this.linkActive = menu.link;
    if (this.activeMenuItem["level"] > poMenuRootLevel) {
      this.openParentMenu(this.activeMenuItem);
    } else {
      this.groupedMenuItem = null;
    }
    this.menuItemsService.sendToChildMenuClicked({
      active: this.activeMenuItem,
      grouped: this.groupedMenuItem,
      activatedByRoute: true
    });
  }
  areSubMenus(menus) {
    return menus.every((menu) => menu["level"] > poMenuRootLevel);
  }
  clearGroupMenuIfFirstLevel(activeMenuItem) {
    if (activeMenuItem["level"] === poMenuRootLevel) {
      this.groupedMenuItem = void 0;
    }
  }
  clickMenuItem(menu) {
    if (menu.action) {
      this.executeMenuAction(menu);
    }
    if (menu["type"] === "externalLink") {
      PoUtils.openExternalLink(menu.link);
    } else if (menu["type"] === "internalLink") {
      this.activateMenuItem(menu);
    } else if (menu["type"] === "subItems") {
      if (this.filteringItems) {
        this.filteringItems = false;
      }
      this.groupMenuItem(menu);
    }
    if (menu["type"] !== "subItems") {
      this.mobileOpened = false;
    }
  }
  convertToMenuItemFiltered(menuItem = {
    label: "",
    link: "",
    action: () => {
    }
  }) {
    const {
      label,
      link,
      action
    } = menuItem;
    const menuItemFiltered = {
      label,
      link,
      action
    };
    this.setMenuItemProperties(menuItemFiltered);
    return menuItemFiltered;
  }
  createResizeListener() {
    this.resizeListener = this.renderer.listen("window", "resize", () => {
      this.toggleResize();
      this.resizeListener();
    });
  }
  executeMenuAction(menu) {
    menu.action(menu);
  }
  filterItems(filter3) {
    return __async(this, null, function* () {
      const trimFilter = filter3 && filter3.trim();
      if (trimFilter) {
        this.filteredItems = [];
        this.filteredItems = this.filterService ? yield this.filterOnService(trimFilter) : this.filterLocalItems(trimFilter);
        this.filteringItems = true;
      } else {
        this.filteredItems = [...this.menus];
        this.filteringItems = false;
      }
    });
  }
  filterLocalItems(filter3) {
    const filteredItems = [];
    this.findItems(this.menus, filter3.toLowerCase(), filteredItems);
    return filteredItems;
  }
  filterProcess(filter3) {
    this.filterLoading = true;
    this.filterItems(filter3).then(() => {
      this.filterLoading = false;
      this.showNoData();
      this.changeDetector.detectChanges();
      this.menuItemsService.sendToChildMenuClicked({
        active: this.activeMenuItem,
        grouped: this.groupedMenuItem
      });
    }).catch((error2) => {
      this.filterLoading = false;
      Promise.reject(error2);
    });
  }
  filterOnService(search = "") {
    return __async(this, null, function* () {
      if (search.length >= poMenuMinLength) {
        return yield this.filterService.getFilteredData(search, this.params).pipe(map((menuItemsFiltered) => menuItemsFiltered.map((menuItem) => this.convertToMenuItemFiltered(menuItem)))).toPromise();
      } else {
        return this.filteredItems;
      }
    });
  }
  findItems(menus, filter3, filteredItems) {
    menus.forEach((menu) => {
      const hasAction = this.searchTreeItems ? this.searchTreeItems : menu.action || menu.link;
      const labelHasFilter = menu.label.toLowerCase().includes(filter3);
      if (labelHasFilter && hasAction) {
        const newMenu = __spreadValues({}, menu);
        if (newMenu.subItems?.length) {
          if (!this.searchTreeItems) {
            delete newMenu.subItems;
          }
          newMenu["type"] = this.setMenuType(newMenu);
        }
        filteredItems.push(newMenu);
      }
      if (menu.subItems) {
        this.findItems(menu.subItems, filter3, filteredItems);
      }
    });
  }
  findParent(menus, menuItem) {
    const getParent = function(menuItems, id) {
      if (menuItems) {
        for (let index = 0; index < menuItems.length; index++) {
          const menu = menuItems[index];
          if (menu.subItems && menu.subItems.find((subItem) => subItem["id"] === id)) {
            return menu;
          }
          const found = getParent(menu.subItems, id);
          if (found) {
            return found;
          }
        }
      }
    };
    return getParent(menus, menuItem["id"]);
  }
  findRootParent(menus, menu) {
    const findParent = this.findParent;
    const getRootParent = function(menuItems, menuItem) {
      let parent = findParent(menuItems, menuItem);
      if (parent["level"] !== poMenuRootLevel) {
        parent = getRootParent(menuItems, parent);
      }
      return parent;
    };
    return getRootParent(menus, menu);
  }
  getActiveMenuParent(menus, activeMenuItem, groupedMenuItem) {
    if (this.areSubMenus([groupedMenuItem, activeMenuItem])) {
      return this.findRootParent(menus, activeMenuItem);
    }
  }
  groupMenuItem(menu) {
    if (this.collapsed) {
      this.toggleMenuCollapse();
    }
    menu["isOpened"] = !menu["isOpened"];
    this.groupedMenuItem = menu;
    if (this.activeMenuItem && menu["isOpened"] && this.isActiveItemMenuSubMenu && this.isRootMenuEqualGroupedMenu(this.menus, this.activeMenuItem, menu)) {
      this.activateMenuItem(this.activeMenuItem);
    }
    this.menuItemsService.sendToChildMenuClicked({
      active: this.activeMenuItem,
      grouped: this.groupedMenuItem
    });
  }
  isRootMenuEqualGroupedMenu(menus, activeMenuItem, groupedMenuItem) {
    const activeMenuRootParent = this.findRootParent(menus, activeMenuItem);
    return activeMenuRootParent["id"] === groupedMenuItem["id"];
  }
  openParentMenu(childMenu) {
    const parent = this.findParent(this.menus, childMenu);
    parent["isOpened"] = true;
    this.groupedMenuItem = parent;
  }
  showNoData() {
    this.noData = this.filteredItems.length === 0;
  }
  stringify(menus) {
    return JSON.stringify(this.menus, (key2, value) => {
      if (key2 !== "icon") {
        return value;
      }
    });
  }
  toggleGroupedMenuItem() {
    this.groupedMenuItem["isOpened"] = !this.collapsed && this.allowCollapseMenu;
  }
  toggleMenuCollapse(collapsed = false) {
    this.setCollapsed(collapsed);
    if (this.groupedMenuItem && this.activeMenuItem) {
      this.groupedMenuItem = this.getActiveMenuParent(this.menus, this.activeMenuItem, this.groupedMenuItem) || this.groupedMenuItem;
      this.toggleGroupedMenuItem();
    }
    if (this.activeMenuItem) {
      this.activateCollapseSubMenuItem();
      this.menuItemsService.sendToChildMenuClicked({
        active: this.activeMenuItem,
        grouped: this.groupedMenuItem,
        activatedByRoute: true
      });
    }
    this.updateMenu();
  }
  toggleResize() {
    if (this.mobileOpened) {
      this.mobileOpened = false;
      this.collapsedMobile = false;
      this.validateCollapseClass(this.collapsedMobile);
    }
  }
  validateToggleMenu(collapsed) {
    if (!this.allowCollapseMenu) {
      return;
    }
    this.toggleMenuCollapse(collapsed);
  }
  updateMenu() {
    this.menuInitialized = true;
    this.setMenuExtraProperties();
    this.filteredItems = [...this.menus];
    this.menuPrevious = this.stringify(this.menus);
    this.validateMenus(this.menus);
  }
  setCollapsed(value) {
    this.collapsed = value;
    this.toggleChange.emit(!value);
  }
  static ɵfac = function PoMenuComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoMenuComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoMenuComponent,
    selectors: [["po-menu"]],
    contentQueries: function PoMenuComponent_ContentQueries(rf, ctx, dirIndex) {
      if (rf & 1) {
        ɵɵcontentQuery(dirIndex, PoMenuHeaderTemplateDirective, 7);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.menuHeaderTemplate = _t.first);
      }
    },
    standalone: false,
    features: [ɵɵProvidersFeature([PoMenuItemsService, PoMenuService]), ɵɵInheritDefinitionFeature, ɵɵNgOnChangesFeature],
    decls: 8,
    vars: 1,
    consts: [["menuCustomTemplate", ""], ["menuFilterTemplate", ""], ["menuItemsTemplate", ""], [4, "ngTemplateOutlet", "ngTemplateOutletContext"], [1, "po-menu-overlay"], [1, "po-menu-mobile", "po-clickable", 3, "click"], ["p-icon", "ICON_MENU"], [1, "po-menu", 3, "ngClass"], [1, "po-menu-container", 3, "mouseenter", "mouseleave"], [1, "po-menu-header"], [1, "po-menu-header-container-logo"], [1, "po-menu-header-template"], [1, "po-menu-body"], [1, "po-menu-outer"], [1, "po-menu-inner"], [1, "po-menu-nav"], [1, "po-menu-footer"], [1, "po-menu-overlay", 3, "click"], [3, "p-class", "p-logo", "p-logo-alt", "p-link"], [4, "ngTemplateOutlet"], ["type", "button", 1, "po-menu-collapse-button-icon", 3, "click"], ["aria-hidden", "true"], [3, "p-icon"], [3, "p-filter", "p-enable-collapse", "p-loading", "p-components-size"], ["role", "menu", 1, "po-menu-list"], ["role", "presentation", 1, "po-menu-list-item"], ["role", "presentation", 1, "po-menu-list-item", 3, "po-menu-item-first"], ["p-icon", "ICON_INFO", "p-type", "noData", 3, "p-level", "p-label", "p-short-label"], [3, "p-action", "p-badge-alert", "p-badge-color", "p-badge-value", "p-collapsed-menu", "p-icon", "p-id", "p-label", "p-level", "p-link", "p-short-label", "p-sub-items", "p-type"]],
    template: function PoMenuComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵconditionalCreate(0, PoMenuComponent_Conditional_0_Template, 1, 3, "ng-container")(1, PoMenuComponent_Conditional_1_Template, 16, 12);
        ɵɵtemplate(2, PoMenuComponent_ng_template_2_Template, 1, 1, "ng-template", null, 0, ɵɵtemplateRefExtractor)(4, PoMenuComponent_ng_template_4_Template, 1, 3, "ng-template", null, 1, ɵɵtemplateRefExtractor)(6, PoMenuComponent_ng_template_6_Template, 6, 3, "ng-template", null, 2, ɵɵtemplateRefExtractor);
      }
      if (rf & 2) {
        ɵɵconditional(ctx.onlyMenuItem ? 0 : 1);
      }
    },
    dependencies: [NgClass, NgTemplateOutlet, PoLogoComponent, PoIconComponent, PoMenuFilterComponent, PoMenuItemComponent],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoMenuComponent, [{
    type: Component,
    args: [{
      selector: "po-menu",
      providers: [PoMenuItemsService, PoMenuService],
      standalone: false,
      template: `@if (onlyMenuItem) {
  <ng-container *ngTemplateOutlet="menuItemsTemplate; context: { showFilter: true, showTemplate: true }"></ng-container>
} @else {
  @if (mobileOpened) {
    <div class="po-menu-overlay" (click)="toggleMenuMobile()"></div>
  }
  <div>
    <div class="po-menu-mobile po-clickable" (click)="toggleMenuMobile()">
      <po-icon p-icon="ICON_MENU"></po-icon>
    </div>
    <div class="po-menu" [attr.aria-expanded]="!collapsed" [ngClass]="{ 'po-menu-animation': mobileOpened }">
      <div class="po-menu-container" (mouseenter)="onMouseEnter()" (mouseleave)="onMouseLeave()">
        <div class="po-menu-header">
          @if (logo || (shortLogo && enableCollapse)) {
            <div class="po-menu-header-container-logo">
              <po-logo
                [p-class]="enableCollapse ? 'po-menu-short-logo' : 'po-menu-logo'"
                [p-logo]="enableCollapse ? shortLogo || logo : logo"
                [p-logo-alt]="logoAlt"
                [p-link]="logoLink"
              ></po-logo>
            </div>
          }
          @if (!enableCollapse && menuHeaderTemplate) {
            <div class="po-menu-header-template">
              <ng-container *ngTemplateOutlet="menuCustomTemplate"></ng-container>
            </div>
          }
          @if (filter) {
            <ng-container *ngTemplateOutlet="menuFilterTemplate"></ng-container>
          }
        </div>
        <div class="po-menu-body">
          <div class="po-menu-outer">
            <div class="po-menu-inner">
              <nav class="po-menu-nav">
                <ng-container
                  *ngTemplateOutlet="menuItemsTemplate; context: { showFilter: false, showTemplate: false }"
                ></ng-container>
              </nav>
            </div>
          </div>
        </div>
      </div>
      @if (hasFooter) {
        <div class="po-menu-footer">
          <button
            class="po-menu-collapse-button-icon"
            type="button"
            [attr.aria-label]="enableCollapseButton ? literals.close : literals.open"
            (click)="toggle()"
          >
            @if (enableCollapseButton) {
              <span aria-hidden="true">{{ literals.close }}</span>
            }
            <po-icon [p-icon]="enableCollapse ? 'ICON_MENU_OPEN' : 'ICON_MENU_CLOSE'"></po-icon>
          </button>
        </div>
      }
    </div>
  </div>
}

<ng-template #menuCustomTemplate>
  <ng-container *ngTemplateOutlet="menuHeaderTemplate.templateRef"></ng-container>
</ng-template>

<ng-template #menuFilterTemplate>
  <po-menu-filter
    [p-enable-collapse]="enableCollapse"
    [p-loading]="filterLoading"
    [p-components-size]="componentsSize"
    (p-filter)="debounceFilter($event)"
  ></po-menu-filter>
</ng-template>

<ng-template #menuItemsTemplate let-showFilter="showFilter" let-showTemplate="showTemplate">
  @if (menuHeaderTemplate && showTemplate) {
    <ng-container *ngTemplateOutlet="menuCustomTemplate"></ng-container>
  }

  @if (filter && showFilter) {
    <ng-container *ngTemplateOutlet="menuFilterTemplate"></ng-container>
  }

  <ul class="po-menu-list" role="menu">
    @if (noData) {
      <li class="po-menu-list-item" role="presentation">
        <po-menu-item
          p-icon="ICON_INFO"
          p-type="noData"
          [p-level]="1"
          [p-label]="literals.itemNotFound"
          [p-short-label]="literals.itemNotFound"
        ></po-menu-item>
      </li>
    }
    @for (menu of filteredItems; track menu; let menuIndex = $index) {
      <li
        class="po-menu-list-item"
        role="presentation"
        [attr.aria-level]="menu.level"
        [class.po-menu-item-first]="menuIndex === 0"
      >
        <po-menu-item
          [p-action]="menu.action"
          [p-badge-alert]="menu.badgeAlert"
          [p-badge-color]="menu.badge?.color"
          [p-badge-value]="menu.badge?.value"
          [p-collapsed-menu]="enableCollapse"
          [p-icon]="allowIcons ? menu.icon : null"
          [p-id]="menu.id"
          [p-label]="menu.label"
          [p-level]="menu.level"
          [p-link]="menu.link"
          [p-short-label]="menu.shortLabel"
          [p-sub-items]="menu.subItems"
          [p-type]="menu.type"
        ></po-menu-item>
      </li>
    }
  </ul>
</ng-template>
`
    }]
  }], () => [], {
    menuHeaderTemplate: [{
      type: ContentChild,
      args: [PoMenuHeaderTemplateDirective, {
        static: true
      }]
    }]
  });
})();
var PoMenuModule = class _PoMenuModule {
  static ɵfac = function PoMenuModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoMenuModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoMenuModule,
    declarations: [PoMenuComponent, PoMenuFilterComponent, PoMenuHeaderTemplateDirective, PoMenuItemComponent],
    imports: [CommonModule, RouterModule, PoBadgeModule, PoFieldModule, PoLoadingModule, PoLogoModule, PoIconModule, PoTooltipModule],
    exports: [PoMenuComponent, PoMenuHeaderTemplateDirective]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [CommonModule, RouterModule, PoBadgeModule, PoFieldModule, PoLoadingModule, PoLogoModule, PoIconModule, PoTooltipModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoMenuModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, RouterModule, PoBadgeModule, PoFieldModule, PoLoadingModule, PoLogoModule, PoIconModule, PoTooltipModule],
      declarations: [PoMenuComponent, PoMenuFilterComponent, PoMenuHeaderTemplateDirective, PoMenuItemComponent],
      exports: [PoMenuComponent, PoMenuHeaderTemplateDirective]
    }]
  }], null, null);
})();
var PoNavbarActionComponent = class _PoNavbarActionComponent {
  router = inject(Router);
  action;
  icon;
  label;
  link;
  tooltip;
  click() {
    if (this?.action) {
      this.action();
      return;
    }
    if (this?.link) {
      return this.openUrl(this.link);
    }
  }
  openUrl(url) {
    if (isExternalLink(url)) {
      return PoUtils.openExternalLink(url);
    }
    if (url) {
      return this.router.navigate([url]);
    }
  }
  static ɵfac = function PoNavbarActionComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoNavbarActionComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoNavbarActionComponent,
    selectors: [["po-navbar-action"]],
    inputs: {
      action: [0, "p-action", "action"],
      icon: [0, "p-icon", "icon"],
      label: [0, "p-label", "label"],
      link: [0, "p-link", "link"],
      tooltip: [0, "p-tooltip-text", "tooltip"]
    },
    standalone: false,
    decls: 1,
    vars: 2,
    consts: [["tabindex", "0", 1, "po-navbar-action-content", "po-clickable", 3, "click", "p-icon", "p-tooltip"]],
    template: function PoNavbarActionComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "po-icon", 0);
        ɵɵlistener("click", function PoNavbarActionComponent_Template_po_icon_click_0_listener() {
          return ctx.click();
        });
        ɵɵelementEnd();
      }
      if (rf & 2) {
        ɵɵproperty("p-icon", ctx.icon)("p-tooltip", ctx.tooltip);
      }
    },
    dependencies: [PoTooltipDirective, PoIconComponent],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoNavbarActionComponent, [{
    type: Component,
    args: [{
      selector: "po-navbar-action",
      standalone: false,
      template: '<po-icon\n  tabindex="0"\n  class="po-navbar-action-content po-clickable"\n  [p-icon]="icon"\n  [p-tooltip]="tooltip"\n  (click)="click()"\n></po-icon>\n'
    }]
  }], null, {
    action: [{
      type: Input,
      args: ["p-action"]
    }],
    icon: [{
      type: Input,
      args: ["p-icon"]
    }],
    label: [{
      type: Input,
      args: ["p-label"]
    }],
    link: [{
      type: Input,
      args: ["p-link"]
    }],
    tooltip: [{
      type: Input,
      args: ["p-tooltip-text"]
    }]
  });
})();
var PoNavbarActionPopupComponent = class _PoNavbarActionPopupComponent {
  iconActions;
  getLastIconAction() {
    if (this.iconActions && this.iconActions.length) {
      return this.iconActions[this.iconActions.length - 1].icon;
    }
  }
  static ɵfac = function PoNavbarActionPopupComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoNavbarActionPopupComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoNavbarActionPopupComponent,
    selectors: [["po-navbar-action-popup"]],
    inputs: {
      iconActions: [0, "p-icon-actions", "iconActions"]
    },
    standalone: false,
    decls: 5,
    vars: 3,
    consts: [["target", ""], ["popup", ""], ["tabindex", "0", 1, "po-navbar-action-popup-content", "po-clickable", 3, "click"], [3, "p-icon"], ["p-size", "medium", 3, "p-actions", "p-target"]],
    template: function PoNavbarActionPopupComponent_Template(rf, ctx) {
      if (rf & 1) {
        const _r1 = ɵɵgetCurrentView();
        ɵɵelementStart(0, "div", 2, 0);
        ɵɵlistener("click", function PoNavbarActionPopupComponent_Template_div_click_0_listener() {
          ɵɵrestoreView(_r1);
          const popup_r2 = ɵɵreference(4);
          return ɵɵresetView(popup_r2.toggle());
        });
        ɵɵelement(2, "po-icon", 3);
        ɵɵelementEnd();
        ɵɵelement(3, "po-popup", 4, 1);
      }
      if (rf & 2) {
        const target_r3 = ɵɵreference(1);
        ɵɵadvance(2);
        ɵɵproperty("p-icon", ctx.getLastIconAction());
        ɵɵadvance();
        ɵɵproperty("p-actions", ctx.iconActions)("p-target", target_r3);
      }
    },
    dependencies: [PoPopupComponent, PoIconComponent],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoNavbarActionPopupComponent, [{
    type: Component,
    args: [{
      selector: "po-navbar-action-popup",
      standalone: false,
      template: '<div tabindex="0" #target class="po-navbar-action-popup-content po-clickable" (click)="popup.toggle()">\n  <po-icon [p-icon]="getLastIconAction()"></po-icon>\n</div>\n\n<po-popup #popup p-size="medium" [p-actions]="iconActions" [p-target]="target"> </po-popup>\n'
    }]
  }], null, {
    iconActions: [{
      type: Input,
      args: ["p-icon-actions"]
    }]
  });
})();
var PoNavbarActionsComponent = class _PoNavbarActionsComponent {
  _iconActions;
  set iconActions(actions2) {
    this._iconActions = actions2.map((action) => __spreadProps(__spreadValues({}, action), {
      separator: true,
      url: action.link
    }));
  }
  get iconActions() {
    return this._iconActions;
  }
  static ɵfac = function PoNavbarActionsComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoNavbarActionsComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoNavbarActionsComponent,
    selectors: [["po-navbar-actions"]],
    inputs: {
      iconActions: [0, "p-icon-actions", "iconActions"]
    },
    standalone: false,
    decls: 3,
    vars: 1,
    consts: [[1, "po-navbar-action", 3, "p-action", "p-icon", "p-label", "p-link", "p-tooltip-text"], [1, "po-navbar-action-popup", 3, "p-icon-actions"]],
    template: function PoNavbarActionsComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵrepeaterCreate(0, PoNavbarActionsComponent_For_1_Template, 1, 5, "po-navbar-action", 0, ɵɵrepeaterTrackByIdentity);
        ɵɵelement(2, "po-navbar-action-popup", 1);
      }
      if (rf & 2) {
        ɵɵrepeater(ctx.iconActions);
        ɵɵadvance(2);
        ɵɵproperty("p-icon-actions", ctx.iconActions);
      }
    },
    dependencies: [PoNavbarActionComponent, PoNavbarActionPopupComponent],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoNavbarActionsComponent, [{
    type: Component,
    args: [{
      selector: "po-navbar-actions",
      standalone: false,
      template: '@for (iconAction of iconActions; track iconAction) {\n  <po-navbar-action\n    class="po-navbar-action"\n    [p-action]="iconAction.action"\n    [p-icon]="iconAction.icon"\n    [p-label]="iconAction.label"\n    [p-link]="iconAction.link"\n    [p-tooltip-text]="iconAction.tooltip"\n  >\n  </po-navbar-action>\n}\n\n<po-navbar-action-popup class="po-navbar-action-popup" [p-icon-actions]="iconActions"> </po-navbar-action-popup>\n'
    }]
  }], null, {
    iconActions: [{
      type: Input,
      args: ["p-icon-actions"]
    }]
  });
})();
var PoNavbarActionsModule = class _PoNavbarActionsModule {
  static ɵfac = function PoNavbarActionsModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoNavbarActionsModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoNavbarActionsModule,
    declarations: [PoNavbarActionComponent, PoNavbarActionPopupComponent, PoNavbarActionsComponent],
    imports: [CommonModule, PoPopupModule, PoTooltipModule, PoIconModule],
    exports: [PoNavbarActionsComponent]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [CommonModule, PoPopupModule, PoTooltipModule, PoIconModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoNavbarActionsModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, PoPopupModule, PoTooltipModule, PoIconModule],
      declarations: [PoNavbarActionComponent, PoNavbarActionPopupComponent, PoNavbarActionsComponent],
      exports: [PoNavbarActionsComponent]
    }]
  }], null, null);
})();
var PoNavbarItemNavigationIconComponent = class _PoNavbarItemNavigationIconComponent {
  disabled;
  icon;
  click = new EventEmitter();
  static ɵfac = function PoNavbarItemNavigationIconComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoNavbarItemNavigationIconComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoNavbarItemNavigationIconComponent,
    selectors: [["po-navbar-item-navigation-icon"]],
    inputs: {
      disabled: [0, "p-disabled", "disabled"],
      icon: [0, "p-icon", "icon"]
    },
    outputs: {
      click: "p-click"
    },
    standalone: false,
    decls: 2,
    vars: 5,
    consts: [["tabindex", "0", 3, "click", "ngClass"], [3, "p-icon"]],
    template: function PoNavbarItemNavigationIconComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "div", 0);
        ɵɵlistener("click", function PoNavbarItemNavigationIconComponent_Template_div_click_0_listener() {
          return ctx.disabled ? void 0 : ctx.click.emit(ctx.icon);
        });
        ɵɵelement(1, "po-icon", 1);
        ɵɵelementEnd();
      }
      if (rf & 2) {
        ɵɵproperty("ngClass", ɵɵpureFunction2(2, _c155, ctx.disabled, !ctx.disabled));
        ɵɵadvance();
        ɵɵproperty("p-icon", ctx.icon);
      }
    },
    dependencies: [NgClass, PoIconComponent],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoNavbarItemNavigationIconComponent, [{
    type: Component,
    args: [{
      selector: "po-navbar-item-navigation-icon",
      standalone: false,
      template: `<div
  [ngClass]="{ 'po-navbar-item-navigation-icon-disabled': disabled, 'po-clickable': !disabled }"
  tabindex="0"
  (click)="disabled ? undefined : click.emit(icon)"
>
  <po-icon [p-icon]="icon"></po-icon>
</div>
`
    }]
  }], null, {
    disabled: [{
      type: Input,
      args: ["p-disabled"]
    }],
    icon: [{
      type: Input,
      args: ["p-icon"]
    }],
    click: [{
      type: Output,
      args: ["p-click"]
    }]
  });
})();
var PoNavbarItemNavigationComponent = class _PoNavbarItemNavigationComponent {
  disableLeft;
  disableRight;
  click = new EventEmitter();
  static ɵfac = function PoNavbarItemNavigationComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoNavbarItemNavigationComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoNavbarItemNavigationComponent,
    selectors: [["po-navbar-item-navigation"]],
    inputs: {
      disableLeft: [0, "p-disable-left", "disableLeft"],
      disableRight: [0, "p-disable-right", "disableRight"]
    },
    outputs: {
      click: "p-click"
    },
    standalone: false,
    decls: 2,
    vars: 2,
    consts: [["p-icon", "ICON_ARROW_LEFT", 1, "po-navbar-item-navigation-icon", 3, "p-click", "p-disabled"], ["p-icon", "ICON_ARROW_RIGHT", 1, "po-navbar-item-navigation-icon", 3, "p-click", "p-disabled"]],
    template: function PoNavbarItemNavigationComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "po-navbar-item-navigation-icon", 0);
        ɵɵlistener("p-click", function PoNavbarItemNavigationComponent_Template_po_navbar_item_navigation_icon_p_click_0_listener($event) {
          return ctx.click.emit($event);
        });
        ɵɵelementEnd();
        ɵɵelementStart(1, "po-navbar-item-navigation-icon", 1);
        ɵɵlistener("p-click", function PoNavbarItemNavigationComponent_Template_po_navbar_item_navigation_icon_p_click_1_listener($event) {
          return ctx.click.emit($event);
        });
        ɵɵelementEnd();
      }
      if (rf & 2) {
        ɵɵproperty("p-disabled", ctx.disableLeft);
        ɵɵadvance();
        ɵɵproperty("p-disabled", ctx.disableRight);
      }
    },
    dependencies: [PoNavbarItemNavigationIconComponent],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoNavbarItemNavigationComponent, [{
    type: Component,
    args: [{
      selector: "po-navbar-item-navigation",
      standalone: false,
      template: '<po-navbar-item-navigation-icon\n  class="po-navbar-item-navigation-icon"\n  p-icon="ICON_ARROW_LEFT"\n  [p-disabled]="disableLeft"\n  (p-click)="click.emit($event)"\n>\n</po-navbar-item-navigation-icon>\n\n<po-navbar-item-navigation-icon\n  class="po-navbar-item-navigation-icon"\n  p-icon="ICON_ARROW_RIGHT"\n  [p-disabled]="disableRight"\n  (p-click)="click.emit($event)"\n>\n</po-navbar-item-navigation-icon>\n'
    }]
  }], null, {
    disableLeft: [{
      type: Input,
      args: ["p-disable-left"]
    }],
    disableRight: [{
      type: Input,
      args: ["p-disable-right"]
    }],
    click: [{
      type: Output,
      args: ["p-click"]
    }]
  });
})();
var PoNavbarItemNavigationModule = class _PoNavbarItemNavigationModule {
  static ɵfac = function PoNavbarItemNavigationModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoNavbarItemNavigationModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoNavbarItemNavigationModule,
    declarations: [PoNavbarItemNavigationComponent, PoNavbarItemNavigationIconComponent],
    imports: [CommonModule, RouterModule, PoIconModule],
    exports: [PoNavbarItemNavigationComponent]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [CommonModule, RouterModule, PoIconModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoNavbarItemNavigationModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, RouterModule, PoIconModule],
      declarations: [PoNavbarItemNavigationComponent, PoNavbarItemNavigationIconComponent],
      exports: [PoNavbarItemNavigationComponent]
    }]
  }], null, null);
})();
var PoNavbarItemComponent = class _PoNavbarItemComponent {
  action;
  clickable;
  label;
  link;
  click = new EventEmitter();
  get type() {
    return isExternalLink(this.link) ? "externalLink" : "internalLink";
  }
  itemClick(label, link) {
    if (this.action) {
      this.action({
        label,
        link
      });
    }
    this.click.emit();
  }
  static ɵfac = function PoNavbarItemComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoNavbarItemComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoNavbarItemComponent,
    selectors: [["po-navbar-item"]],
    inputs: {
      action: [0, "p-action", "action"],
      clickable: [0, "p-clickable", "clickable"],
      label: [0, "p-label", "label"],
      link: [0, "p-link", "link"]
    },
    outputs: {
      click: "p-click"
    },
    standalone: false,
    decls: 2,
    vars: 1,
    consts: [["target", "_blank", 1, "po-navbar-item-link", 3, "po-clickable", "href"], [1, "po-navbar-item-link", 3, "po-clickable", "routerLink"], ["target", "_blank", 1, "po-navbar-item-link", 3, "click", "href"], [1, "po-navbar-item-link", 3, "click", "routerLink"]],
    template: function PoNavbarItemComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵconditionalCreate(0, PoNavbarItemComponent_Case_0_Template, 2, 4, "a", 0)(1, PoNavbarItemComponent_Case_1_Template, 2, 4, "a", 1);
      }
      if (rf & 2) {
        let tmp_0_0;
        ɵɵconditional((tmp_0_0 = ctx.type) === "externalLink" ? 0 : tmp_0_0 === "internalLink" ? 1 : -1);
      }
    },
    dependencies: [RouterLink],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoNavbarItemComponent, [{
    type: Component,
    args: [{
      selector: "po-navbar-item",
      standalone: false,
      template: `@switch (type) {
  @case ('externalLink') {
    <a class="po-navbar-item-link" [class.po-clickable]="clickable" [href]="link" target="_blank" (click)="itemClick()">
      {{ label }}
    </a>
  }
  @case ('internalLink') {
    <a
      class="po-navbar-item-link"
      [class.po-clickable]="clickable"
      [routerLink]="link"
      (click)="itemClick(label, link)"
    >
      {{ label }}
    </a>
  }
}
`
    }]
  }], null, {
    action: [{
      type: Input,
      args: ["p-action"]
    }],
    clickable: [{
      type: Input,
      args: ["p-clickable"]
    }],
    label: [{
      type: Input,
      args: ["p-label"]
    }],
    link: [{
      type: Input,
      args: ["p-link"]
    }],
    click: [{
      type: Output,
      args: ["p-click"]
    }]
  });
})();
var PoNavbarItemsComponent = class _PoNavbarItemsComponent {
  router = inject(Router);
  navbarItemsContainer;
  allNavbarItems;
  items;
  selectedItem;
  routeSubscription;
  ngOnDestroy() {
    this.routeSubscription.unsubscribe();
  }
  ngOnInit() {
    this.subscribeToRoute();
  }
  checkActiveItemByUrl(urlRouter) {
    const urlArray = urlRouter.split("/");
    let counter = urlArray.length;
    while (counter >= 0) {
      const url = urlArray.slice(0, counter).join("/");
      this.selectedItem = this.items.find((item) => item.link === url);
      if (this.selectedItem) {
        break;
      }
      counter--;
    }
  }
  checkRouterChildrenFragments() {
    const childrenPrimary = this.router.parseUrl(this.router.url).root.children["primary"];
    return childrenPrimary ? `/${childrenPrimary.segments.map((it) => it.path).join("/")}` : "/";
  }
  subscribeToRoute() {
    this.routeSubscription = this.router.events.subscribe((val) => {
      if (val instanceof NavigationEnd || val instanceof NavigationCancel) {
        const urlRouter = this.checkRouterChildrenFragments();
        this.checkActiveItemByUrl(urlRouter);
      }
    });
  }
  static ɵfac = function PoNavbarItemsComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoNavbarItemsComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoNavbarItemsComponent,
    selectors: [["po-navbar-items"]],
    viewQuery: function PoNavbarItemsComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c156, 7, ElementRef)(PoNavbarItemComponent, 5, ElementRef);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.navbarItemsContainer = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.allNavbarItems = _t);
      }
    },
    inputs: {
      items: [0, "p-items", "items"]
    },
    standalone: false,
    decls: 5,
    vars: 0,
    consts: [["navbarItemsContainer", ""], [1, "po-navbar-items-container"], [1, "po-navbar-item", 3, "po-navbar-item-selected"], [1, "po-navbar-item"], [3, "p-action", "p-clickable", "p-label", "p-link"]],
    template: function PoNavbarItemsComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "nav")(1, "ul", 1, 0);
        ɵɵrepeaterCreate(3, PoNavbarItemsComponent_For_4_Template, 2, 6, "li", 2, ɵɵrepeaterTrackByIdentity);
        ɵɵelementEnd()();
      }
      if (rf & 2) {
        ɵɵadvance(3);
        ɵɵrepeater(ctx.items);
      }
    },
    dependencies: [PoNavbarItemComponent],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoNavbarItemsComponent, [{
    type: Component,
    args: [{
      selector: "po-navbar-items",
      standalone: false,
      template: '<nav>\n  <ul class="po-navbar-items-container" #navbarItemsContainer>\n    @for (item of items; track item) {\n      <li class="po-navbar-item" [class.po-navbar-item-selected]="selectedItem === item">\n        <po-navbar-item\n          [p-action]="item.action"\n          [p-clickable]="selectedItem !== item"\n          [p-label]="item.label"\n          [p-link]="item.link"\n        >\n        </po-navbar-item>\n      </li>\n    }\n  </ul>\n</nav>\n'
    }]
  }], null, {
    navbarItemsContainer: [{
      type: ViewChild,
      args: ["navbarItemsContainer", {
        read: ElementRef,
        static: true
      }]
    }],
    allNavbarItems: [{
      type: ViewChildren,
      args: [PoNavbarItemComponent, {
        read: ElementRef
      }]
    }],
    items: [{
      type: Input,
      args: ["p-items"]
    }]
  });
})();
var PoNavbarItemsModule = class _PoNavbarItemsModule {
  static ɵfac = function PoNavbarItemsModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoNavbarItemsModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoNavbarItemsModule,
    declarations: [PoNavbarItemComponent, PoNavbarItemsComponent],
    imports: [CommonModule, RouterModule],
    exports: [PoNavbarItemsComponent]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [CommonModule, RouterModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoNavbarItemsModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, RouterModule],
      declarations: [PoNavbarItemComponent, PoNavbarItemsComponent],
      exports: [PoNavbarItemsComponent]
    }]
  }], null, null);
})();
var PoNavbarLogoComponent = class _PoNavbarLogoComponent {
  logo;
  logoAlt;
  static ɵfac = function PoNavbarLogoComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoNavbarLogoComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoNavbarLogoComponent,
    selectors: [["po-navbar-logo"]],
    inputs: {
      logo: [0, "p-logo", "logo"],
      logoAlt: [0, "p-logo-alt", "logoAlt"]
    },
    standalone: false,
    decls: 1,
    vars: 1,
    consts: [["p-class", "po-navbar-logo-image", 3, "p-logo", "p-logo-alt"]],
    template: function PoNavbarLogoComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵconditionalCreate(0, PoNavbarLogoComponent_Conditional_0_Template, 1, 2, "po-logo", 0);
      }
      if (rf & 2) {
        ɵɵconditional(ctx.logo ? 0 : -1);
      }
    },
    dependencies: [PoLogoComponent],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoNavbarLogoComponent, [{
    type: Component,
    args: [{
      selector: "po-navbar-logo",
      standalone: false,
      template: '@if (logo) {\n  <po-logo p-class="po-navbar-logo-image" [p-logo]="logo" [p-logo-alt]="logoAlt"></po-logo>\n}\n'
    }]
  }], null, {
    logo: [{
      type: Input,
      args: ["p-logo"]
    }],
    logoAlt: [{
      type: Input,
      args: ["p-logo-alt"]
    }]
  });
})();
var poNavbarLiteralsDefault$1 = {
  en: {
    navbarLinks: "Navbar links"
  },
  es: {
    navbarLinks: "Navbar links"
  },
  pt: {
    navbarLinks: "Navbar links"
  },
  ru: {
    navbarLinks: "Navbar связи"
  }
};
var PoNavbarBaseComponent = class _PoNavbarBaseComponent {
  // Menu que esta sendo exibido na pagina corrente.
  applicationMenu;
  _iconActions = [];
  _items = [];
  _literals;
  _logo;
  _shadow = false;
  language = poLocaleDefault;
  /**
   * @deprecated v23.x.x
   *
   * @optional
   *
   * @description
   *
   * Define uma lista de ações apresentadas em ícones no lado direito do `po-navbar`.
   */
  set iconActions(value) {
    this._iconActions = Array.isArray(value) ? value : [];
  }
  get iconActions() {
    return this._iconActions;
  }
  /**
   * @deprecated v23.x.x
   *
   * @optional
   *
   * @description
   *
   * Define uma lista de items do `po-navbar`.
   */
  set items(value) {
    this._items = Array.isArray(value) ? value : [];
  }
  get items() {
    return this._items;
  }
  /**
   * @deprecated v23.x.x
   *
   * @optional
   *
   * @description
   *
   * Objeto com a literal usada na propriedade `p-literals`.
   *
   * Para customizar a literal, basta declarar um objeto do tipo `PoNavbarLiterals` conforme exemplo abaixo:
   *
   * ```
   *  const customLiterals: PoNavbarLiterals = {
   *    navbarLinks: 'Itens de navegação'
   *  };
   * ```
   *
   * E para carregar as literais customizadas, basta apenas passar o objeto para o componente.
   *
   * ```
   * <po-navbar
   *   [p-literals]="customLiterals">
   * </po-navbar>
   * ```
   *
   * > O objeto padrão de literais será traduzido de acordo com o idioma do
   * [`PoI18nService`](/documentation/po-i18n) ou do browser.
   */
  set literals(value) {
    if (value instanceof Object && !(value instanceof Array)) {
      this._literals = __spreadValues(__spreadValues(__spreadValues({}, poNavbarLiteralsDefault$1[poLocaleDefault]), poNavbarLiteralsDefault$1[this.language]), value);
    } else {
      this._literals = poNavbarLiteralsDefault$1[this.language];
    }
  }
  get literals() {
    return this._literals || poNavbarLiteralsDefault$1[this.language];
  }
  /**
   * @deprecated v23.x.x
   *
   * @optional
   *
   * @description
   *
   * Define a logo apresentada `po-navbar`.
   */
  set logo(value) {
    this._logo = value;
    if (this.applicationMenu) {
      this.validateMenuLogo();
    }
  }
  get logo() {
    return this._logo;
  }
  /**
   * @deprecated v23.x.x
   *
   * @optional
   *
   * @description
   *
   * Define o texto alternativo para a logomarca.
   *
   * > **Importante**
   * > Caso esta propriedade não seja definida o texto padrão será "Logomarca início".
   *
   * @default `Logomarca início`
   */
  logoAlt;
  /**
   * @deprecated v23.x.x
   *
   * @optional
   *
   * @description
   *
   * Aplica uma sombra na parte inferior do `po-navbar`.
   *
   * @default `false`
   */
  set shadow(value) {
    this._shadow = convertToBoolean(value);
  }
  get shadow() {
    return this._shadow;
  }
  constructor(languageService) {
    this.language = languageService.getShortLanguage();
  }
  static ɵfac = function PoNavbarBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoNavbarBaseComponent)(ɵɵdirectiveInject(PoLanguageService));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoNavbarBaseComponent,
    inputs: {
      iconActions: [0, "p-icon-actions", "iconActions"],
      items: [0, "p-items", "items"],
      literals: [0, "p-literals", "literals"],
      logo: [0, "p-logo", "logo"],
      logoAlt: [0, "p-logo-alt", "logoAlt"],
      shadow: [0, "p-shadow", "shadow"]
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoNavbarBaseComponent, [{
    type: Directive
  }], () => [{
    type: PoLanguageService
  }], {
    iconActions: [{
      type: Input,
      args: ["p-icon-actions"]
    }],
    items: [{
      type: Input,
      args: ["p-items"]
    }],
    literals: [{
      type: Input,
      args: ["p-literals"]
    }],
    logo: [{
      type: Input,
      args: ["p-logo"]
    }],
    logoAlt: [{
      type: Input,
      args: ["p-logo-alt"]
    }],
    shadow: [{
      type: Input,
      args: ["p-shadow"]
    }]
  });
})();
var poNavbarNavigationWidth = 88;
var poNavbarMenuMedia = 768;
var poNavbarMatchMedia = `(max-width: ${poNavbarMenuMedia}px)`;
var poNavbarTiming = "250ms ease";
var PoNavbarComponent = class _PoNavbarComponent extends PoNavbarBaseComponent {
  renderer = inject(Renderer2);
  builder = inject(AnimationBuilder);
  changeDetector = inject(ChangeDetectorRef);
  menuGlobalService = inject(PoMenuGlobalService);
  navbarItemsElement;
  navbarItems;
  disableRight;
  showItemsNavigation = false;
  windowResizeListener;
  _menuComponent;
  isNavbarUpdateMenu = false;
  id = uuid();
  mediaQuery;
  offset = 0;
  player;
  menuItems;
  previousMenuComponentId;
  previousMenusItems = [];
  applicationMenuSubscription;
  menusSubscription;
  removedMenuSubscription;
  get navbarItemNavigationDisableLeft() {
    return this.offset === 0;
  }
  get navbarItemNavigationDisableRight() {
    return this.disableRight && this.offset !== 0;
  }
  set menuComponent(menu) {
    this._menuComponent = menu;
    this.previousMenuComponentId = menu?.id || this.previousMenuComponentId;
  }
  get isCollapsedMedia() {
    return window.innerWidth < poNavbarMenuMedia;
  }
  constructor() {
    const poLanguageService = inject(PoLanguageService);
    super(poLanguageService);
    this.windowResizeListener = this.renderer.listen(window, "resize", this.displayItemsNavigation.bind(this));
  }
  ngOnInit() {
    this.menusSubscription = this.menuGlobalService.receiveMenus$.subscribe((newMenus) => {
      const previousMenusiIsNavbarLinks = this.previousMenusItems?.length === 1 && this.previousMenusItems[0].id === this.id;
      if (this.applicationMenu && this.isCollapsedMedia && this.isNavbarUpdateMenu && previousMenusiIsNavbarLinks) {
        this.isNavbarUpdateMenu = false;
        this.applicationMenu.menus = [{
          label: this.literals.navbarLinks,
          subItems: this.items,
          id: this.id
        }, ...newMenus];
      }
      this.isNavbarUpdateMenu = false;
      this.previousMenusItems = newMenus;
    });
    this.removedMenuSubscription = this.menuGlobalService.receiveRemovedApplicationMenu$.subscribe((removedMenuId) => {
      this.applicationMenu = this.applicationMenu && this.previousMenuComponentId === removedMenuId ? this.applicationMenu : void 0;
      this.changeDetector.detectChanges();
      if (!this.applicationMenu && this.mediaQuery) {
        this.mediaQuery.removeListener(this.onMediaQueryChange);
      }
    });
    this.applicationMenuSubscription = this.menuGlobalService.receiveApplicationMenu$.pipe(delay(100)).subscribe((newMenu) => {
      this.applicationMenu = this.previousMenuComponentId === newMenu.id ? void 0 : newMenu;
      this.changeDetector.detectChanges();
      if (this.applicationMenu) {
        this.initNavbarMenu();
      }
    });
  }
  ngAfterViewInit() {
    this.displayItemsNavigation();
  }
  ngOnDestroy() {
    if (this.mediaQuery) {
      this.mediaQuery.removeListener(this.onMediaQueryChange);
    }
    this.removedMenuSubscription?.unsubscribe();
    this.applicationMenuSubscription?.unsubscribe();
    this.menusSubscription?.unsubscribe();
  }
  navigateItems(orientation) {
    orientation === "left" ? this.navigateLeft() : this.navigateRight();
    this.animate(this.offset);
  }
  validateMenuLogo() {
    if (this.applicationMenu.logo && this.logo) {
      this.applicationMenu.logo = void 0;
      this.changeDetector.detectChanges();
    }
  }
  allNavbarItemsWidth() {
    return this.navbarItems.allNavbarItems.reduce((previous, current) => previous + current.nativeElement.offsetWidth, 0);
  }
  animate(offset) {
    const animation2 = this.buildTransitionAnimation(offset);
    this.player = animation2.create(this.navbarItems.navbarItemsContainer.nativeElement);
    this.player.play();
  }
  buildTransitionAnimation(offset) {
    return this.builder.build([animate(poNavbarTiming, keyframes([style({
      transform: `translateX(${-offset}px)`
    })]))]);
  }
  changeNavbarMenuItems(isCollapsedMedia, navbarItems, label) {
    if (isCollapsedMedia) {
      this.applicationMenu.menus = [{
        label,
        subItems: navbarItems,
        id: this.id
      }, ...this.applicationMenu.menus];
    } else {
      this.applicationMenu.menus = this.applicationMenu.menus.filter((m2) => m2.id !== this.id);
    }
    this.isNavbarUpdateMenu = true;
    this.changeDetector.detectChanges();
  }
  calculateLeftNavigation() {
    let calculatedOffset;
    this.navbarItems.allNavbarItems.some((navbarItem) => {
      const navbarItemOffset = navbarItem.nativeElement.offsetLeft;
      const navbarItemWidth = navbarItem.nativeElement.offsetWidth;
      if (navbarItemOffset >= this.offset) {
        calculatedOffset = navbarItemOffset - (this.navbarItemsWidth() - navbarItemWidth);
        return true;
      }
    });
    return calculatedOffset;
  }
  calculateRightNavigation(itemBreakPoint) {
    let calculatedOffset;
    this.navbarItems.allNavbarItems.some((navbarItem) => {
      const offsetLeft = navbarItem.nativeElement.offsetLeft;
      const finalPosition = navbarItem.nativeElement.offsetWidth + offsetLeft;
      if (itemBreakPoint < finalPosition) {
        calculatedOffset = offsetLeft;
        return true;
      }
    });
    return calculatedOffset;
  }
  displayItemsNavigation() {
    this.showItemsNavigation = this.navbarItemsWidth() < this.allNavbarItemsWidth() + poNavbarNavigationWidth;
    this.changeDetector.detectChanges();
    if (this.offset !== 0) {
      this.setOffsetToZero();
      this.animate(this.offset);
    }
  }
  initNavbarMenu() {
    this.mediaQuery = window.matchMedia(poNavbarMatchMedia);
    if (this.isCollapsedMedia) {
      this.changeNavbarMenuItems(true, this.items, this.literals.navbarLinks);
    }
    this.validateMenuLogo();
    this.mediaQuery.addListener(this.onMediaQueryChange);
  }
  navbarItemsWidth() {
    return this.navbarItemsElement.nativeElement.offsetWidth;
  }
  navigateLeft() {
    this.disableRight = false;
    this.offset = this.calculateLeftNavigation();
    if (this.offset < 0) {
      this.setOffsetToZero();
    }
  }
  navigateRight() {
    const maxAllowedOffset = this.allNavbarItemsWidth() - this.navbarItemsWidth();
    const itemBreakPoint = this.offset + this.navbarItemsWidth();
    this.offset = this.calculateRightNavigation(itemBreakPoint);
    this.validateMaxOffset(maxAllowedOffset);
  }
  onMediaQueryChange = (changed) => {
    this.changeNavbarMenuItems(changed.matches, this.items, this.literals.navbarLinks);
  };
  setOffsetToZero() {
    this.offset = 0;
  }
  validateMaxOffset(maxAllowedOffset) {
    if (this.offset >= maxAllowedOffset) {
      this.offset = maxAllowedOffset;
      this.disableRight = true;
    }
  }
  static ɵfac = function PoNavbarComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoNavbarComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoNavbarComponent,
    selectors: [["po-navbar"]],
    viewQuery: function PoNavbarComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(PoNavbarItemsComponent, 7, ElementRef)(PoNavbarItemsComponent, 7)(PoMenuComponent, 5);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.navbarItemsElement = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.navbarItems = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.menuComponent = _t.first);
      }
    },
    standalone: false,
    features: [ɵɵInheritDefinitionFeature],
    decls: 6,
    vars: 12,
    consts: [[1, "po-navbar", 3, "ngClass"], [1, "po-navbar-logo", 3, "ngClass", "p-logo"], [1, "po-navbar-items", 3, "p-items"], [1, "po-navbar-item-navigation", 3, "p-disable-left", "p-disable-right"], [1, "po-navbar-actions", 3, "p-icon-actions"], [3, "p-menus"], [1, "po-navbar-item-navigation", 3, "p-click", "p-disable-left", "p-disable-right"]],
    template: function PoNavbarComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "header", 0);
        ɵɵelement(1, "po-navbar-logo", 1)(2, "po-navbar-items", 2);
        ɵɵconditionalCreate(3, PoNavbarComponent_Conditional_3_Template, 1, 2, "po-navbar-item-navigation", 3);
        ɵɵelement(4, "po-navbar-actions", 4);
        ɵɵelementEnd();
        ɵɵconditionalCreate(5, PoNavbarComponent_Conditional_5_Template, 1, 1, "po-menu", 5);
      }
      if (rf & 2) {
        ɵɵproperty("ngClass", ɵɵpureFunction1(7, _c157, ctx.shadow));
        ɵɵadvance();
        ɵɵproperty("ngClass", ɵɵpureFunction2(9, _c158, !!ctx.applicationMenu, !ctx.logo))("p-logo", ctx.logo);
        ɵɵadvance();
        ɵɵproperty("p-items", ctx.items);
        ɵɵadvance();
        ɵɵconditional(ctx.showItemsNavigation ? 3 : -1);
        ɵɵadvance();
        ɵɵproperty("p-icon-actions", ctx.iconActions);
        ɵɵadvance();
        ɵɵconditional(!ctx.applicationMenu ? 5 : -1);
      }
    },
    dependencies: [NgClass, PoMenuComponent, PoNavbarActionsComponent, PoNavbarItemNavigationComponent, PoNavbarItemsComponent, PoNavbarLogoComponent],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoNavbarComponent, [{
    type: Component,
    args: [{
      selector: "po-navbar",
      standalone: false,
      template: `<header class="po-navbar" [ngClass]="{ 'po-navbar-shadow': shadow }">
  <po-navbar-logo
    class="po-navbar-logo"
    [ngClass]="{ 'po-navbar-logo-menu': !!applicationMenu, 'po-navbar-no-logo': !logo }"
    [p-logo]="logo"
  >
  </po-navbar-logo>

  <po-navbar-items class="po-navbar-items" [p-items]="items"> </po-navbar-items>

  @if (showItemsNavigation) {
    <po-navbar-item-navigation
      class="po-navbar-item-navigation"
      [p-disable-left]="navbarItemNavigationDisableLeft"
      [p-disable-right]="navbarItemNavigationDisableRight"
      (p-click)="navigateItems($event)"
    >
    </po-navbar-item-navigation>
  }

  <po-navbar-actions class="po-navbar-actions" [p-icon-actions]="iconActions"> </po-navbar-actions>
</header>

@if (!applicationMenu) {
  <po-menu [p-menus]="items"> </po-menu>
}
`
    }]
  }], () => [], {
    navbarItemsElement: [{
      type: ViewChild,
      args: [PoNavbarItemsComponent, {
        read: ElementRef,
        static: true
      }]
    }],
    navbarItems: [{
      type: ViewChild,
      args: [PoNavbarItemsComponent, {
        static: true
      }]
    }],
    menuComponent: [{
      type: ViewChild,
      args: [PoMenuComponent]
    }]
  });
})();
var PoNavbarModule = class _PoNavbarModule {
  static ɵfac = function PoNavbarModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoNavbarModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoNavbarModule,
    declarations: [PoNavbarComponent, PoNavbarLogoComponent],
    imports: [CommonModule, PoIconModule, PoLogoModule, PoMenuModule, PoNavbarActionsModule, PoNavbarItemNavigationModule, PoNavbarItemsModule],
    exports: [PoNavbarComponent]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [CommonModule, PoIconModule, PoLogoModule, PoMenuModule, PoNavbarActionsModule, PoNavbarItemNavigationModule, PoNavbarItemsModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoNavbarModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, PoIconModule, PoLogoModule, PoMenuModule, PoNavbarActionsModule, PoNavbarItemNavigationModule, PoNavbarItemsModule],
      declarations: [PoNavbarComponent, PoNavbarLogoComponent],
      exports: [PoNavbarComponent]
    }]
  }], null, null);
})();
var PoPageContentBaseComponent = class {
  resizeListener;
};
var PoPageContentComponent = class _PoPageContentComponent extends PoPageContentBaseComponent {
  renderer = inject(Renderer2);
  elementRef = inject(ElementRef);
  ngZone = inject(NgZone);
  cd = inject(ChangeDetectorRef);
  contentOpacity = 0;
  height = "90%";
  constructor() {
    super();
    this.initializeListeners();
  }
  ngAfterViewInit() {
    this.recalculateHeaderSize();
  }
  ngOnDestroy() {
    this.removeListeners();
  }
  /**
   * Recalcula a altura do po-page-content.
   * Chamado internamente pelo po-page-default quando inputs que afetam
   * o tamanho do header mudam (title, subtitle, breadcrumb, headerType, theme).
   */
  recalculateHeaderSize() {
    setTimeout(() => {
      this.setHeightContent();
      this.contentOpacity = 1;
      this.cd.markForCheck();
    });
  }
  setHeightContent() {
    const poPageDiv = this.elementRef.nativeElement.closest(".po-page");
    const contentHost = this.elementRef.nativeElement;
    if (!poPageDiv) {
      const contentTop2 = contentHost.getBoundingClientRect().top;
      const newHeight2 = window.innerHeight - contentTop2;
      this.height = newHeight2 > 0 ? `${newHeight2}px` : "90%";
      return;
    }
    if (poPageDiv.closest(".po-page-content")) {
      this.height = "auto";
      return;
    }
    const pageHeader = poPageDiv.querySelector(":scope > po-page-header");
    const gap = parseFloat(getComputedStyle(poPageDiv).gap) || 0;
    let contentTop;
    if (pageHeader) {
      contentTop = pageHeader.getBoundingClientRect().bottom + gap;
    } else {
      contentTop = poPageDiv.getBoundingClientRect().top;
    }
    const newHeight = window.innerHeight - contentTop;
    this.height = newHeight > 0 ? `${newHeight}px` : "90%";
  }
  initializeListeners() {
    this.ngZone.runOutsideAngular(() => {
      this.resizeListener = this.renderer.listen("window", "resize", () => {
        this.ngZone.run(() => this.recalculateHeaderSize());
      });
    });
  }
  removeListeners() {
    if (this.resizeListener) {
      this.resizeListener();
    }
  }
  static ɵfac = function PoPageContentComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoPageContentComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoPageContentComponent,
    selectors: [["po-page-content"]],
    standalone: false,
    features: [ɵɵInheritDefinitionFeature],
    ngContentSelectors: _c12,
    decls: 2,
    vars: 4,
    consts: [[1, "po-page-content"]],
    template: function PoPageContentComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵprojectionDef();
        ɵɵelementStart(0, "div", 0);
        ɵɵprojection(1);
        ɵɵelementEnd();
      }
      if (rf & 2) {
        ɵɵstyleProp("height", ctx.height)("opacity", ctx.contentOpacity);
      }
    },
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoPageContentComponent, [{
    type: Component,
    args: [{
      selector: "po-page-content",
      standalone: false,
      template: '<div class="po-page-content" [style.height]="height" [style.opacity]="contentOpacity">\n  <ng-content></ng-content>\n</div>\n'
    }]
  }], () => [], null);
})();
var PoPageHeaderType;
(function(PoPageHeaderType2) {
  PoPageHeaderType2["primary"] = "primary";
  PoPageHeaderType2["secondary"] = "secondary";
  PoPageHeaderType2["tertiary"] = "tertiary";
})(PoPageHeaderType || (PoPageHeaderType = {}));
var PoPageActionsLayout;
(function(PoPageActionsLayout2) {
  PoPageActionsLayout2["default"] = "default";
  PoPageActionsLayout2["dropdown"] = "dropdown";
  PoPageActionsLayout2["mixed"] = "mixed";
})(PoPageActionsLayout || (PoPageActionsLayout = {}));
var poPageDefaultLiteralsDefault = {
  en: {
    otherActions: "Other actions"
  },
  es: {
    otherActions: "Otras acciones"
  },
  pt: {
    otherActions: "Outras ações"
  },
  ru: {
    otherActions: "Другие действия"
  }
};
var backNavigationAriaLabels = {
  en: "Back",
  es: "Volver",
  pt: "Voltar",
  ru: "Назад"
};
var refreshAriaLabels = {
  en: "Refresh",
  es: "Actualizar",
  pt: "Atualizar",
  ru: "Обновить"
};
var PoPageDefaultBaseComponent = class _PoPageDefaultBaseComponent {
  poPageContent;
  visibleActions = [];
  language;
  _actions = [];
  _breadcrumb;
  _componentsSize = void 0;
  _initialComponentsSize = void 0;
  _literals;
  _pageActionsLayout = PoPageActionsLayout.default;
  _pageHeaderType = PoPageHeaderType.primary;
  _subtitle;
  _title;
  /**
   * @optional
   *
   * @description
   *
   * Define a lista de ações que serão exibidas no cabeçalho da página.
   *
   * Recebe um array de objetos que implementam a interface `PoPageAction`.
   *
   * > O comportamento de exibição pode ser customizado através da propriedade `p-page-actions-layout`.
   *
   * @default `[]`
   */
  set actions(actions2) {
    this._actions = Array.isArray(actions2) ? actions2 : [];
    this.visibleActions = this.getVisibleActions();
    this.setDropdownActions();
  }
  get actions() {
    return this._actions;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define o sistema de navegação que indica o caminho da página atual na hierarquia da aplicação.
   *
   * Recebe um objeto que implementa a interface `PoBreadcrumb`.
   *
   * > Compatível com o cabeçalho (`p-page-header-type`) do tipo `primary`.
   */
  set breadcrumb(value) {
    this._breadcrumb = value;
    setTimeout(() => this.poPageContent?.recalculateHeaderSize());
  }
  get breadcrumb() {
    return this._breadcrumb;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define o tamanho dos componentes de formulário no template:
   * - `small`: aplica a medida small de cada componente (disponível apenas para acessibilidade AA).
   * - `medium`: aplica a medida medium de cada componente.
   *
   * > Caso a acessibilidade AA não esteja configurada, o tamanho `medium` será mantido.
   * Para mais detalhes, consulte a documentação do [po-theme](https://po-ui.io/documentation/po-theme).
   *
   * @default `medium`
   */
  set componentsSize(value) {
    this._initialComponentsSize = value;
    this.applySizeBasedOnA11y();
  }
  get componentsSize() {
    return this._componentsSize ?? getDefaultSizeFn(PoFieldSize);
  }
  /**
   * @optional
   *
   * @description
   *
   * Define o conteúdo do po-helper informativo exibido ao lado do subtítulo da página.
   *
   * Quando não houver subtítulo (`p-subtitle`), o po-helper será exibido logo abaixo do título.
   *
   * Aceita uma string simples (exibida como conteúdo) ou um objeto do tipo `PoHelperOptions`
   * para configuração avançada (título, conteúdo, tipo, ações).
   *
   * Exemplo de uso:
   * ```html
   * <po-page-default
   *   p-title="Cadastro"
   *   p-subtitle="Preencha os dados"
   *   [p-helper]="{ title: 'Ajuda', content: 'Informações sobre o cadastro' }"
   * ></po-page-default>
   * ```
   *
   * @default `info`
   */
  helper = input(void 0, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "helper"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-helper",
    transform: this.transformPageHelper.bind(this)
  }));
  /**
   * @optional
   *
   * @description
   *
   * Permite a customização das literais utilizadas no componente.
   *
   * Para customizar, basta passar um objeto parcial ou completo que implemente a interface `PoPageDefaultLiterals`.
   *
   * Exemplo de uso:
   *
   * ```html
   * <po-page-default [p-literals]="customLiterals"></po-page-default>
   * ```
   *
   * ```typescript
   * const customLiterals: PoPageDefaultLiterals = {
   *   otherActions: 'Mais opções'
   * };
   * ```
   *
   * > O valor padrão será traduzido de acordo com o idioma configurado no [`PoI18nService`](/documentation/po-i18n) ou navegador.
   */
  set literals(value) {
    if (value instanceof Object && !(value instanceof Array)) {
      this._literals = __spreadValues(__spreadValues(__spreadValues({}, poPageDefaultLiteralsDefault[poLocaleDefault]), poPageDefaultLiteralsDefault[this.language]), value);
    } else {
      this._literals = poPageDefaultLiteralsDefault[this.language];
    }
  }
  get literals() {
    return this._literals || poPageDefaultLiteralsDefault[this.language];
  }
  /**
   * @optional
   *
   * @description
   *
   * Define o layout de exibição das ações no cabeçalho.
   *
   * Aceita valores do enum `PoPageActionsLayout`.
   *
   * > Em telas reduzidas (< 480px) as ações fora do *dropdown* que possuam a propriedade `PoPageAction.icon` definida
   * exibirão apenas o ícone.
   *
   * @default `default`
   */
  set pageActionsLayout(value) {
    const strValue = typeof value === "string" ? value : "";
    this._pageActionsLayout = PoPageActionsLayout[strValue] ? PoPageActionsLayout[strValue] : PoPageActionsLayout.default;
  }
  get pageActionsLayout() {
    return this._pageActionsLayout;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define o tipo de cabeçalho da página.
   *
   * Aceita valores do enum `PoPageHeaderType`.
   *
   * @default `primary`
   */
  set pageHeaderType(value) {
    const strValue = typeof value === "string" ? value : "";
    this._pageHeaderType = PoPageHeaderType[strValue] ? PoPageHeaderType[strValue] : PoPageHeaderType.primary;
    setTimeout(() => this.poPageContent?.recalculateHeaderSize());
  }
  get pageHeaderType() {
    return this._pageHeaderType;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define a função de callback executada ao clicar no botão de atualização (refresh) ao lado do subtítulo da página.
   *
   * Quando não houver subtítulo (`p-subtitle`), o refresh será exibido logo abaixo do título.
   *
   * > Esta propriedade possui precedência sobre a configuração de `p-helper`.
   *
   * Exemplo de uso:
   * ```html
   * <po-page-default
   *   p-title="Dashboard"
   *   [p-refresh]="onRefresh"
   * ></po-page-default>
   * ```
   */
  refresh = input(void 0, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "refresh"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-refresh"
  }));
  refreshCustomAction = computed(() => {
    const fn = this.refresh();
    if (fn && typeof fn === "function") {
      return {
        icon: "ICON_REFRESH",
        ariaLabel: refreshAriaLabels[this.language] || refreshAriaLabels["en"],
        action: fn
      };
    }
    return void 0;
  }, ...ngDevMode ? [{
    debugName: "refreshCustomAction"
  }] : (
    /* istanbul ignore next */
    []
  ));
  /**
   * @optional
   *
   * @description
   *
   * Define o título principal da página.
   */
  set title(title) {
    this._title = title;
    setTimeout(() => this.poPageContent?.recalculateHeaderSize());
  }
  get title() {
    return this._title;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define um texto de apoio ou informações adicionais logo abaixo do título principal.
   *
   * Suporta formatação básica com as tags `<b>` (negrito), `<strong>` (negrito), `<i>` (itálico), `<em>` (itálico) e
   * `<u>` (sublinhado).
   *
   * Exemplo:
   * ```typescript
   * subtitle = 'Texto <b>importante</b> com <em>destaque</em> e <u>sublinhado</u>';
   * ```
   *
   * > Requer que `p-title` esteja definido.
   */
  set subtitle(value) {
    this._subtitle = value;
    setTimeout(() => this.poPageContent?.recalculateHeaderSize());
  }
  get subtitle() {
    return this._subtitle;
  }
  /**
   * @optional
   *
   * @description
   *
   * Evento disparado ao clicar no botão voltar exibido no cabeçalho.
   *
   * > Botão exibido apenas quando a propriedade `p-page-header-type` está configurada como `secondary`.
   */
  back = output({
    alias: "p-back"
  });
  constructor(languageService) {
    this.language = languageService.getShortLanguage();
  }
  onThemeChange() {
    this.applySizeBasedOnA11y();
    setTimeout(() => this.poPageContent?.recalculateHeaderSize());
  }
  applySizeBasedOnA11y() {
    const size = validateSizeFn(this._initialComponentsSize, PoFieldSize);
    this._componentsSize = size;
  }
  transformPageHelper(value) {
    if (value && typeof value === "string") {
      return {
        content: value,
        type: "info"
      };
    }
    if (value && typeof value === "object" && !value.type) {
      return __spreadProps(__spreadValues({}, value), {
        type: "info"
      });
    }
    return value;
  }
  static ɵfac = function PoPageDefaultBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoPageDefaultBaseComponent)(ɵɵdirectiveInject(PoLanguageService));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoPageDefaultBaseComponent,
    viewQuery: function PoPageDefaultBaseComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(PoPageContentComponent, 7);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.poPageContent = _t.first);
      }
    },
    hostVars: 1,
    hostBindings: function PoPageDefaultBaseComponent_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("PoUiThemeChange", function PoPageDefaultBaseComponent_PoUiThemeChange_HostBindingHandler() {
          return ctx.onThemeChange();
        }, ɵɵresolveWindow);
      }
      if (rf & 2) {
        ɵɵattribute("p-components-size", ctx.componentsSize);
      }
    },
    inputs: {
      actions: [0, "p-actions", "actions"],
      breadcrumb: [0, "p-breadcrumb", "breadcrumb"],
      componentsSize: [0, "p-components-size", "componentsSize"],
      helper: [1, "p-helper", "helper"],
      literals: [0, "p-literals", "literals"],
      pageActionsLayout: [0, "p-page-actions-layout", "pageActionsLayout"],
      pageHeaderType: [0, "p-page-header-type", "pageHeaderType"],
      refresh: [1, "p-refresh", "refresh"],
      title: [0, "p-title", "title"],
      subtitle: [0, "p-subtitle", "subtitle"]
    },
    outputs: {
      back: "p-back"
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoPageDefaultBaseComponent, [{
    type: Directive
  }], () => [{
    type: PoLanguageService
  }], {
    poPageContent: [{
      type: ViewChild,
      args: [PoPageContentComponent, {
        static: true
      }]
    }],
    actions: [{
      type: Input,
      args: ["p-actions"]
    }],
    breadcrumb: [{
      type: Input,
      args: ["p-breadcrumb"]
    }],
    componentsSize: [{
      type: Input,
      args: ["p-components-size"]
    }, {
      type: HostBinding,
      args: ["attr.p-components-size"]
    }],
    helper: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-helper",
        required: false
      }]
    }],
    literals: [{
      type: Input,
      args: ["p-literals"]
    }],
    pageActionsLayout: [{
      type: Input,
      args: ["p-page-actions-layout"]
    }],
    pageHeaderType: [{
      type: Input,
      args: ["p-page-header-type"]
    }],
    refresh: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-refresh",
        required: false
      }]
    }],
    title: [{
      type: Input,
      args: ["p-title"]
    }],
    subtitle: [{
      type: Input,
      args: ["p-subtitle"]
    }],
    back: [{
      type: Output,
      args: ["p-back"]
    }],
    onThemeChange: [{
      type: HostListener,
      args: ["window:PoUiThemeChange"]
    }]
  });
})();
var PoPageBaseComponent = class {
};
var PoPageComponent = class _PoPageComponent extends PoPageBaseComponent {
  static ɵfac = /* @__PURE__ */ (() => {
    let ɵPoPageComponent_BaseFactory;
    return function PoPageComponent_Factory(__ngFactoryType__) {
      return (ɵPoPageComponent_BaseFactory || (ɵPoPageComponent_BaseFactory = ɵɵgetInheritedFactory(_PoPageComponent)))(__ngFactoryType__ || _PoPageComponent);
    };
  })();
  static ɵcmp = ɵɵdefineComponent({
    type: _PoPageComponent,
    selectors: [["po-page"]],
    standalone: false,
    features: [ɵɵInheritDefinitionFeature],
    ngContentSelectors: _c12,
    decls: 2,
    vars: 0,
    consts: [[1, "po-page"]],
    template: function PoPageComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵprojectionDef();
        ɵɵelementStart(0, "div", 0);
        ɵɵprojection(1);
        ɵɵelementEnd();
      }
    },
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoPageComponent, [{
    type: Component,
    args: [{
      selector: "po-page",
      standalone: false,
      template: '<div class="po-page">\n  <ng-content> </ng-content>\n</div>\n'
    }]
  }], null, null);
})();
var PAGE_SUBTITLE_ALLOWED_TAGS = ["b", "i", "u", "strong", "em"];
var PoPageHeaderBaseComponent = class _PoPageHeaderBaseComponent {
  /** Título da página. */
  title;
  /** Define o conteúdo do po-helper. */
  helper = input(void 0, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "helper"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-helper"
  }));
  /** Define a ação customizada para o po-helper (uso interno entre componentes). */
  customAction = input(void 0, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "customAction"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-custom-action"
  }));
  /** Define o tamanho dos componentes no header. */
  size;
  /** Define o tipo de header: `primary`, `secondary` ou `tertiary`. */
  type = "primary";
  _breadcrumb;
  _subtitle;
  _subtitleFragments = [];
  /** Objeto com propriedades do breadcrumb. */
  set breadcrumb(value) {
    this._breadcrumb = value;
  }
  get breadcrumb() {
    return this._breadcrumb;
  }
  /** Subtítulo da página. */
  set subtitle(value) {
    this._subtitle = value;
    this._subtitleFragments = parseSafeText(value, PAGE_SUBTITLE_ALLOWED_TAGS);
  }
  get subtitle() {
    return this._subtitle;
  }
  get subtitleFragments() {
    return this._subtitleFragments;
  }
  static ɵfac = function PoPageHeaderBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoPageHeaderBaseComponent)();
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoPageHeaderBaseComponent,
    inputs: {
      title: [0, "p-title", "title"],
      helper: [1, "p-helper", "helper"],
      customAction: [1, "p-custom-action", "customAction"],
      size: [0, "p-size", "size"],
      type: [0, "p-type", "type"],
      breadcrumb: [0, "p-breadcrumb", "breadcrumb"],
      subtitle: [0, "p-subtitle", "subtitle"]
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoPageHeaderBaseComponent, [{
    type: Directive
  }], null, {
    title: [{
      type: Input,
      args: ["p-title"]
    }],
    helper: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-helper",
        required: false
      }]
    }],
    customAction: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-custom-action",
        required: false
      }]
    }],
    size: [{
      type: Input,
      args: ["p-size"]
    }],
    type: [{
      type: Input,
      args: ["p-type"]
    }],
    breadcrumb: [{
      type: Input,
      args: ["p-breadcrumb"]
    }],
    subtitle: [{
      type: Input,
      args: ["p-subtitle"]
    }]
  });
})();
var PoPageHeaderComponent = class _PoPageHeaderComponent extends PoPageHeaderBaseComponent {
  static ɵfac = /* @__PURE__ */ (() => {
    let ɵPoPageHeaderComponent_BaseFactory;
    return function PoPageHeaderComponent_Factory(__ngFactoryType__) {
      return (ɵPoPageHeaderComponent_BaseFactory || (ɵPoPageHeaderComponent_BaseFactory = ɵɵgetInheritedFactory(_PoPageHeaderComponent)))(__ngFactoryType__ || _PoPageHeaderComponent);
    };
  })();
  static ɵcmp = ɵɵdefineComponent({
    type: _PoPageHeaderComponent,
    selectors: [["po-page-header"]],
    standalone: false,
    features: [ɵɵInheritDefinitionFeature],
    ngContentSelectors: _c160,
    decls: 6,
    vars: 6,
    consts: [[1, "po-page-header"], [1, "po-page-header-breadcrumb"], [1, "po-page-header-container"], [1, "po-page-header-title-container"], [3, "p-favorite-service", "p-items", "p-params-service", "p-size"], [1, "po-page-header-subtitle"], [1, "po-page-header-title"], [1, "po-page-header-subtitle-text"], [3, "po-text-bold", "po-text-italic", "po-text-underline"], [3, "p-custom-action", "p-size"], [3, "p-helper", "p-size"]],
    template: function PoPageHeaderComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵprojectionDef(_c159);
        ɵɵelementStart(0, "div", 0);
        ɵɵconditionalCreate(1, PoPageHeaderComponent_Conditional_1_Template, 2, 4, "div", 1);
        ɵɵelementStart(2, "div", 2);
        ɵɵprojection(3);
        ɵɵconditionalCreate(4, PoPageHeaderComponent_Conditional_4_Template, 4, 3, "div", 3);
        ɵɵprojection(5, 1);
        ɵɵelementEnd()();
      }
      if (rf & 2) {
        ɵɵclassProp("po-page-header-secondary", ctx.type === "secondary")("po-page-header-tertiary", ctx.type === "tertiary");
        ɵɵadvance();
        ɵɵconditional(ctx.type === "primary" && ctx.breadcrumb && ctx.breadcrumb.items.length ? 1 : -1);
        ɵɵadvance(3);
        ɵɵconditional(ctx.title ? 4 : -1);
      }
    },
    dependencies: [PoBreadcrumbComponent, PoHelperComponent],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoPageHeaderComponent, [{
    type: Component,
    args: [{
      selector: "po-page-header",
      standalone: false,
      template: `<div
  class="po-page-header"
  [class.po-page-header-secondary]="type === 'secondary'"
  [class.po-page-header-tertiary]="type === 'tertiary'"
>
  <!-- Breadcrumb - only shown for primary -->
  @if (type === 'primary' && breadcrumb && breadcrumb.items.length) {
    <div class="po-page-header-breadcrumb">
      <po-breadcrumb
        [p-favorite-service]="breadcrumb.favorite"
        [p-items]="breadcrumb.items"
        [p-params-service]="breadcrumb.params"
        [p-size]="size"
      >
      </po-breadcrumb>
    </div>
  }

  <div class="po-page-header-container">
    <!-- Back navigation button - only for secondary, projected from parent -->
    <ng-content select="[po-page-header-navigation]"></ng-content>

    <!-- Titulo e SubTitulo-->
    @if (title) {
      <div class="po-page-header-title-container">
        @if (title) {
          @if (type === 'secondary' || type === 'tertiary') {
            <h3 class="po-page-header-title">
              {{ title }}
            </h3>
          } @else {
            <h2 class="po-page-header-title">
              {{ title }}
            </h2>
          }
        }
        @if (title && subtitle) {
          <div class="po-page-header-subtitle">
            <span class="po-page-header-subtitle-text">
              @for (fragment of subtitleFragments; track $index) {
                <span
                  [class.po-text-bold]="fragment.bold"
                  [class.po-text-italic]="fragment.italic"
                  [class.po-text-underline]="fragment.underline"
                  >{{ fragment.text }}</span
                >
              }
            </span>
            @if (customAction()) {
              <po-helper [p-custom-action]="customAction()" [p-size]="size"></po-helper>
            } @else if (helper()) {
              <po-helper [p-helper]="helper()" [p-size]="size"></po-helper>
            }
          </div>
        }
        @if (title && !subtitle) {
          @if (customAction()) {
            <div class="po-page-header-subtitle">
              <po-helper [p-custom-action]="customAction()" [p-size]="size"></po-helper>
            </div>
          } @else if (helper()) {
            <div class="po-page-header-subtitle">
              <po-helper [p-helper]="helper()" [p-size]="size"></po-helper>
            </div>
          }
        }
      </div>
    }

    <!-- Operações -->
    <ng-content></ng-content>
  </div>
</div>
`
    }]
  }], null, null);
})();
var PoPageDefaultComponent = class _PoPageDefaultComponent extends PoPageDefaultBaseComponent {
  renderer = inject(Renderer2);
  router = inject(Router);
  backIcon = AnimaliaIconDictionary.ICON_ARROW_BACK;
  backNavigationLabel;
  limitPrimaryActions = 3;
  dropdownActions;
  isMobile;
  maxWidthMobile = 480;
  _primaryKindUsed = false;
  resizeUnlisten;
  constructor() {
    const languageService = inject(PoLanguageService);
    super(languageService);
    this.backNavigationLabel = backNavigationAriaLabels[this.language] || backNavigationAriaLabels["en"];
  }
  ngAfterContentInit() {
    this.setIsMobile();
    this.setDropdownActions();
    this.resizeUnlisten = this.renderer.listen("window", "resize", (event) => {
      this.onResize(event);
    });
  }
  ngOnChanges(changes) {
    this.setDropdownActions();
  }
  ngOnDestroy() {
    if (this.resizeUnlisten) {
      this.resizeUnlisten();
    }
  }
  actionIsDisabled(action) {
    return isTypeof(action.disabled, "function") ? action.disabled(action) : action.disabled;
  }
  actionIsVisible(action) {
    return isTypeof(action.visible, "function") ? action.visible(action) : action.visible;
  }
  callAction(item) {
    if (item.url) {
      isExternalLink(item.url) ? PoUtils.openExternalLink(item.url) : this.router.navigate([item.url]);
    } else if (item.action) {
      item.action();
    }
  }
  hasPageHeader() {
    this.visibleActions = this.getVisibleActions();
    this._primaryKindUsed = false;
    if (this.pageHeaderType === "secondary") {
      return true;
    }
    if (this.pageHeaderType === "tertiary") {
      return !!(this.title || this.visibleActions?.length);
    }
    return !!(this.title || this.visibleActions?.length || this.breadcrumb?.items.length);
  }
  setDropdownActions() {
    if (this.pageActionsLayout === "dropdown") {
      this.dropdownActions = this.visibleActions;
    } else if (this.pageActionsLayout === "mixed") {
      this.dropdownActions = this.visibleActions.slice(1);
    } else {
      if (this.visibleActions.length > this.limitPrimaryActions) {
        this.dropdownActions = this.visibleActions.slice(this.limitPrimaryActions - 1);
      } else {
        this.dropdownActions = [];
      }
    }
  }
  getVisibleActions() {
    return this.actions.filter((action) => this.actionIsVisible(action) !== false);
  }
  // Retorna o kind validado da ação. Apenas 'primary' e 'secondary' são permitidos.
  // Valores inválidos são ignorados, retornando o fallback informado.
  // Somente uma ação pode ter kind 'primary' — a primeira encontrada mantém,
  // as demais caem para 'secondary'.
  getActionKind(action, fallback) {
    if (action.kind === "primary") {
      if (!this._primaryKindUsed) {
        this._primaryKindUsed = true;
        return "primary";
      }
      return "secondary";
    }
    return action.kind === "secondary" ? action.kind : fallback;
  }
  onResize(event) {
    const width = event.target.innerWidth;
    if (width < this.maxWidthMobile) {
      this.isMobile = true;
      this.limitPrimaryActions = 2;
      this.setDropdownActions();
    } else {
      this.isMobile = false;
      this.limitPrimaryActions = 3;
      this.setDropdownActions();
    }
  }
  setIsMobile() {
    if (window.innerWidth < this.maxWidthMobile) {
      this.isMobile = true;
      this.limitPrimaryActions = 2;
    }
  }
  static ɵfac = function PoPageDefaultComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoPageDefaultComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoPageDefaultComponent,
    selectors: [["po-page-default"]],
    standalone: false,
    features: [ɵɵInheritDefinitionFeature, ɵɵNgOnChangesFeature],
    ngContentSelectors: _c12,
    decls: 10,
    vars: 1,
    consts: [["actionsDropdown", ""], ["actionsMixed", ""], ["actionsDefault", ""], ["p-position", "bottom-right", 3, "p-label", "p-actions", "p-size"], [3, "p-label", "p-actions", "p-size"], [3, "p-kind", "p-danger", "p-disabled", "p-icon", "p-label", "p-aria-label", "p-size"], [3, "p-click", "p-kind", "p-danger", "p-disabled", "p-icon", "p-label", "p-aria-label", "p-size"], [3, "p-kind", "p-danger", "p-disabled", "p-icon", "p-label", "p-size"], [3, "p-kind", "p-disabled", "p-icon", "p-label", "p-danger", "p-size"], [3, "p-click", "p-kind", "p-danger", "p-disabled", "p-icon", "p-label", "p-size"], [3, "p-click", "p-kind", "p-disabled", "p-icon", "p-label", "p-danger", "p-size"], [3, "p-type", "p-helper", "p-custom-action", "p-size", "p-subtitle", "p-title"], [3, "p-breadcrumb", "p-helper", "p-custom-action", "p-size", "p-subtitle", "p-title"], ["po-page-header-navigation", "", "p-kind", "tertiary", 3, "p-click", "p-icon", "p-size", "p-aria-label"], [1, "po-page-header-actions"], [4, "ngTemplateOutlet"], [4, "ngTemplateOutlet", "ngTemplateOutletContext"]],
    template: function PoPageDefaultComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵprojectionDef();
        ɵɵelementStart(0, "po-page");
        ɵɵtemplate(1, PoPageDefaultComponent_ng_template_1_Template, 1, 1, "ng-template", null, 0, ɵɵtemplateRefExtractor)(3, PoPageDefaultComponent_ng_template_3_Template, 2, 2, "ng-template", null, 1, ɵɵtemplateRefExtractor)(5, PoPageDefaultComponent_ng_template_5_Template, 4, 4, "ng-template", null, 2, ɵɵtemplateRefExtractor);
        ɵɵconditionalCreate(7, PoPageDefaultComponent_Conditional_7_Template, 3, 1);
        ɵɵelementStart(8, "po-page-content");
        ɵɵprojection(9);
        ɵɵelementEnd()();
      }
      if (rf & 2) {
        ɵɵadvance(7);
        ɵɵconditional(ctx.hasPageHeader() ? 7 : -1);
      }
    },
    dependencies: [NgTemplateOutlet, PoButtonComponent, PoDropdownComponent, PoPageComponent, PoPageContentComponent, PoPageHeaderComponent],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoPageDefaultComponent, [{
    type: Component,
    args: [{
      selector: "po-page-default",
      standalone: false,
      template: `<po-page>
  <!-- Shared action templates -->
  <ng-template #actionsDropdown>
    @if (visibleActions.length) {
      <po-dropdown
        p-position="bottom-right"
        [p-label]="literals.otherActions"
        [p-actions]="visibleActions"
        [p-size]="componentsSize"
      ></po-dropdown>
    }
  </ng-template>

  <ng-template #actionsMixed let-kindFallback="kindFallback">
    @if (visibleActions.length > 1) {
      <po-dropdown
        [p-label]="literals.otherActions"
        [p-actions]="dropdownActions"
        [p-size]="componentsSize"
      ></po-dropdown>
    }
    @if (visibleActions[0]) {
      <po-button
        [p-kind]="getActionKind(visibleActions[0], kindFallback)"
        [p-danger]="visibleActions[0].type === 'danger'"
        [p-disabled]="actionIsDisabled(visibleActions[0])"
        [p-icon]="visibleActions[0].icon"
        [p-label]="isMobile && visibleActions[0].icon ? '' : visibleActions[0].label"
        [p-aria-label]="visibleActions[0].label"
        [p-size]="componentsSize"
        (p-click)="callAction(visibleActions[0])"
      ></po-button>
    }
  </ng-template>

  <ng-template #actionsDefault>
    @if (visibleActions.length > limitPrimaryActions) {
      <po-dropdown
        [p-label]="literals.otherActions"
        [p-actions]="dropdownActions"
        [p-size]="componentsSize"
      ></po-dropdown>
    }
    @if (visibleActions.length === 3 && visibleActions[2] && !isMobile) {
      <po-button
        [p-kind]="getActionKind(visibleActions[2], 'secondary')"
        [p-danger]="visibleActions[2].type === 'danger'"
        [p-disabled]="actionIsDisabled(visibleActions[2])"
        [p-icon]="visibleActions[2].icon"
        [p-label]="visibleActions[2].label"
        [p-size]="componentsSize"
        (p-click)="callAction(visibleActions[2])"
      ></po-button>
    }
    @if (visibleActions[1] && (visibleActions.length === 2 || !isMobile)) {
      <po-button
        [p-kind]="getActionKind(visibleActions[1], 'secondary')"
        [p-disabled]="actionIsDisabled(visibleActions[1])"
        [p-icon]="visibleActions[1].icon"
        [p-label]="visibleActions[1].label"
        [p-danger]="visibleActions[1].type === 'danger'"
        [p-size]="componentsSize"
        (p-click)="callAction(visibleActions[1])"
      ></po-button>
    }
    @if (visibleActions[0]) {
      <po-button
        [p-kind]="getActionKind(visibleActions[0], 'primary')"
        [p-danger]="visibleActions[0].type === 'danger'"
        [p-disabled]="actionIsDisabled(visibleActions[0])"
        [p-icon]="visibleActions[0].icon"
        [p-label]="isMobile && visibleActions[0].icon ? '' : visibleActions[0].label"
        [p-aria-label]="visibleActions[0].label"
        [p-size]="componentsSize"
        (p-click)="callAction(visibleActions[0])"
      ></po-button>
    }
  </ng-template>

  <!-- HEADER -->
  @if (hasPageHeader()) {
    @switch (pageHeaderType) {
      @case ('secondary') {
        <po-page-header
          [p-type]="'secondary'"
          [p-helper]="refreshCustomAction() ? undefined : helper()"
          [p-custom-action]="refreshCustomAction()"
          [p-size]="componentsSize"
          [p-subtitle]="subtitle"
          [p-title]="title"
        >
          <!-- Back navigation button (icon-only, aria-label for screen readers) -->
          <po-button
            po-page-header-navigation
            p-kind="tertiary"
            [p-icon]="backIcon"
            [p-size]="componentsSize"
            [p-aria-label]="backNavigationLabel"
            (p-click)="back.emit()"
          ></po-button>

          <div class="po-page-header-actions">
            @switch (pageActionsLayout) {
              @case ('dropdown') {
                <ng-container *ngTemplateOutlet="actionsDropdown"></ng-container>
              }
              @case ('mixed') {
                <ng-container *ngTemplateOutlet="actionsMixed; context: { kindFallback: 'secondary' }"></ng-container>
              }
              @default {
                <ng-container *ngTemplateOutlet="actionsDefault"></ng-container>
              }
            }
          </div>
        </po-page-header>
      }
      @case ('tertiary') {
        <po-page-header
          [p-type]="'tertiary'"
          [p-helper]="refreshCustomAction() ? undefined : helper()"
          [p-custom-action]="refreshCustomAction()"
          [p-size]="componentsSize"
          [p-subtitle]="subtitle"
          [p-title]="title"
        >
          <div class="po-page-header-actions">
            @switch (pageActionsLayout) {
              @case ('dropdown') {
                <ng-container *ngTemplateOutlet="actionsDropdown"></ng-container>
              }
              @case ('mixed') {
                <ng-container *ngTemplateOutlet="actionsMixed; context: { kindFallback: 'secondary' }"></ng-container>
              }
              @default {
                <ng-container *ngTemplateOutlet="actionsDefault"></ng-container>
              }
            }
          </div>
        </po-page-header>
      }
      @default {
        <po-page-header
          [p-breadcrumb]="breadcrumb"
          [p-helper]="refreshCustomAction() ? undefined : helper()"
          [p-custom-action]="refreshCustomAction()"
          [p-size]="componentsSize"
          [p-subtitle]="subtitle"
          [p-title]="title"
        >
          <div class="po-page-header-actions">
            @switch (pageActionsLayout) {
              @case ('dropdown') {
                <ng-container *ngTemplateOutlet="actionsDropdown"></ng-container>
              }
              @case ('mixed') {
                <ng-container *ngTemplateOutlet="actionsMixed; context: { kindFallback: 'primary' }"></ng-container>
              }
              @default {
                <ng-container *ngTemplateOutlet="actionsDefault"></ng-container>
              }
            }
          </div>
        </po-page-header>
      }
    }
  }

  <!-- CONTENT -->
  <po-page-content>
    <ng-content></ng-content>
  </po-page-content>
</po-page>
`
    }]
  }], () => [], null);
})();
var poPageDetailLiteralsDefault = {
  en: {
    back: "Back",
    edit: "Edit",
    remove: "Remove"
  },
  es: {
    back: "Volver",
    edit: "Editar",
    remove: "Eliminar"
  },
  pt: {
    back: "Voltar",
    edit: "Editar",
    remove: "Remover"
  },
  ru: {
    back: "возвращение",
    edit: "редактировать",
    remove: "удаление"
  }
};
var PoPageDetailBaseComponent = class _PoPageDetailBaseComponent {
  poPageContent;
  /** Objeto com propriedades do breadcrumb. */
  breadcrumb;
  /**
   * Evento que será disparado ao clicar no botão de "Voltar".
   *
   * ```
   * <po-page-detail (p-back)="myBackFunction()">
   * </po-page-detail>
   * ```
   *
   * > Caso não utilizar esta propriedade, o botão de "Voltar" não será exibido.
   */
  back = new EventEmitter();
  /**
   * Evento que será disparado ao clicar no botão de "Editar".
   *
   * ```
   * <po-page-detail (p-edit)="myEditFunction()">
   * </po-page-detail>
   * ```
   *
   * > Caso não utilizar esta propriedade, o botão de "Editar" não será exibido.
   */
  edit = new EventEmitter();
  /**
   * Evento que será disparado ao clicar no botão de "Remover".
   *
   * ```
   * <po-page-detail (p-remove)="myRemoveFunction()">
   * </po-page-detail>
   * ```
   *
   * > Caso não utilizar esta propriedade, o botão de "Remover" não será exibido.
   */
  remove = new EventEmitter();
  _componentsSize = void 0;
  _initialComponentsSize = void 0;
  _literals;
  _title;
  language;
  /**
   * @optional
   *
   * @description
   *
   * Define o tamanho dos componentes de formulário no template:
   * - `small`: aplica a medida small de cada componente (disponível apenas para acessibilidade AA).
   * - `medium`: aplica a medida medium de cada componente.
   *
   * > Caso a acessibilidade AA não esteja configurada, o tamanho `medium` será mantido.
   * Para mais detalhes, consulte a documentação do [po-theme](https://po-ui.io/documentation/po-theme).
   *
   * @default `medium`
   */
  set componentsSize(value) {
    this._initialComponentsSize = value;
    this.applySizeBasedOnA11y();
  }
  get componentsSize() {
    return this._componentsSize ?? getDefaultSizeFn(PoFieldSize);
  }
  /**
   * @optional
   *
   * @description
   *
   * Objeto com as literais usadas no `po-page-detail`.
   *
   * Existem duas maneiras de customizar o componente, passando um objeto com todas as literais disponíveis:
   *
   * ```
   *  const customLiterals: PoPageDetailLiterals = {
   *    edit: 'Edição',
   *    remove: 'Exclusão',
   *    back: 'Menu'
   *  };
   * ```
   *
   * Ou passando apenas as literais que deseja customizar:
   *
   * ```
   *  const customLiterals: PoPageDetailLiterals = {
   *    remove: 'Excluir registro permanentemente'
   *  };
   * ```
   *
   * E para carregar as literais customizadas, basta apenas passar o objeto para o componente.
   *
   * ```
   * <po-page-detail
   *   [p-literals]="customLiterals">
   * </po-page-detail>
   * ```
   *
   * > O objeto padrão de literais será traduzido de acordo com o idioma do
   * [`PoI18nService`](/documentation/po-i18n) ou do browser.
   */
  set literals(value) {
    if (value instanceof Object && !(value instanceof Array)) {
      this._literals = __spreadValues(__spreadValues(__spreadValues({}, poPageDetailLiteralsDefault[poLocaleDefault]), poPageDetailLiteralsDefault[this.language]), value);
    } else {
      this._literals = poPageDetailLiteralsDefault[this.language];
    }
  }
  get literals() {
    return this._literals || poPageDetailLiteralsDefault[this.language];
  }
  /** Título da página. */
  set title(title) {
    this._title = title;
    setTimeout(() => this.poPageContent.recalculateHeaderSize());
  }
  get title() {
    return this._title;
  }
  /**
   * @optional
   *
   * @description
   *
   * Subtitulo do Header da página.
   *
   * Suporta formatação básica com as tags `<b>` (negrito), `<strong>` (negrito), `<i>` (itálico), `<em>` (itálico) e
   * `<u>` (sublinhado).
   *
   * Exemplo:
   * ```typescript
   * subtitle = 'Status: <b>Active</b> | Role: <i>Administrator</i>';
   * ```
   *
   * > Requer que `p-title` esteja definido.
   */
  subtitle;
  constructor(languageService) {
    this.language = languageService.getShortLanguage();
  }
  onThemeChange() {
    this.applySizeBasedOnA11y();
  }
  applySizeBasedOnA11y() {
    const size = validateSizeFn(this._initialComponentsSize, PoFieldSize);
    this._componentsSize = size;
  }
  static ɵfac = function PoPageDetailBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoPageDetailBaseComponent)(ɵɵdirectiveInject(PoLanguageService));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoPageDetailBaseComponent,
    viewQuery: function PoPageDetailBaseComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(PoPageContentComponent, 7);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.poPageContent = _t.first);
      }
    },
    hostVars: 1,
    hostBindings: function PoPageDetailBaseComponent_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("PoUiThemeChange", function PoPageDetailBaseComponent_PoUiThemeChange_HostBindingHandler() {
          return ctx.onThemeChange();
        }, ɵɵresolveWindow);
      }
      if (rf & 2) {
        ɵɵattribute("p-components-size", ctx.componentsSize);
      }
    },
    inputs: {
      breadcrumb: [0, "p-breadcrumb", "breadcrumb"],
      componentsSize: [0, "p-components-size", "componentsSize"],
      literals: [0, "p-literals", "literals"],
      title: [0, "p-title", "title"],
      subtitle: [0, "p-subtitle", "subtitle"]
    },
    outputs: {
      back: "p-back",
      edit: "p-edit",
      remove: "p-remove"
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoPageDetailBaseComponent, [{
    type: Directive
  }], () => [{
    type: PoLanguageService
  }], {
    poPageContent: [{
      type: ViewChild,
      args: [PoPageContentComponent, {
        static: true
      }]
    }],
    breadcrumb: [{
      type: Input,
      args: ["p-breadcrumb"]
    }],
    back: [{
      type: Output,
      args: ["p-back"]
    }],
    edit: [{
      type: Output,
      args: ["p-edit"]
    }],
    remove: [{
      type: Output,
      args: ["p-remove"]
    }],
    componentsSize: [{
      type: Input,
      args: ["p-components-size"]
    }, {
      type: HostBinding,
      args: ["attr.p-components-size"]
    }],
    literals: [{
      type: Input,
      args: ["p-literals"]
    }],
    title: [{
      type: Input,
      args: ["p-title"]
    }],
    subtitle: [{
      type: Input,
      args: ["p-subtitle"]
    }],
    onThemeChange: [{
      type: HostListener,
      args: ["window:PoUiThemeChange"]
    }]
  });
})();
var PoPageDetailComponent = class _PoPageDetailComponent extends PoPageDetailBaseComponent {
  hasAnyAction() {
    return this.hasEvent("back") || this.hasEvent("edit") || this.hasEvent("remove");
  }
  hasEditFn(property) {
    if (property === "icon") {
      return this.hasEvent("edit") ? "" : "ICON_DELETE";
    } else if (property === "type") {
      return this.hasEvent("edit") ? "secondary" : "primary";
    } else {
      return "";
    }
  }
  hasEditOrRemoveFn(property) {
    if (property === "icon") {
      return this.hasEvent("edit") || this.hasEvent("remove") ? "" : "ICON_ARROW_LEFT";
    } else if (property === "type") {
      return this.hasEvent("edit") || this.hasEvent("remove") ? "secondary" : "primary";
    } else {
      return "";
    }
  }
  hasEvent(event) {
    return !!this[event].observers.length;
  }
  hasPageHeader() {
    return !!(this.title || this.hasAnyAction() || this.breadcrumb && this.breadcrumb.items.length);
  }
  static ɵfac = /* @__PURE__ */ (() => {
    let ɵPoPageDetailComponent_BaseFactory;
    return function PoPageDetailComponent_Factory(__ngFactoryType__) {
      return (ɵPoPageDetailComponent_BaseFactory || (ɵPoPageDetailComponent_BaseFactory = ɵɵgetInheritedFactory(_PoPageDetailComponent)))(__ngFactoryType__ || _PoPageDetailComponent);
    };
  })();
  static ɵcmp = ɵɵdefineComponent({
    type: _PoPageDetailComponent,
    selectors: [["po-page-detail"]],
    standalone: false,
    features: [ɵɵInheritDefinitionFeature],
    ngContentSelectors: _c12,
    decls: 4,
    vars: 1,
    consts: [[3, "p-breadcrumb", "p-size", "p-subtitle", "p-title"], [1, "po-page-header-actions"], [3, "p-icon", "p-label", "p-kind", "p-size"], ["p-danger", "", 3, "p-icon", "p-label", "p-kind", "p-size"], ["p-icon", "ICON_EDIT", "p-kind", "primary", 3, "p-label", "p-size"], [3, "p-click", "p-icon", "p-label", "p-kind", "p-size"], ["p-danger", "", 3, "p-click", "p-icon", "p-label", "p-kind", "p-size"], ["p-icon", "ICON_EDIT", "p-kind", "primary", 3, "p-click", "p-label", "p-size"]],
    template: function PoPageDetailComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵprojectionDef();
        ɵɵelementStart(0, "po-page");
        ɵɵconditionalCreate(1, PoPageDetailComponent_Conditional_1_Template, 2, 5, "po-page-header", 0);
        ɵɵelementStart(2, "po-page-content");
        ɵɵprojection(3);
        ɵɵelementEnd()();
      }
      if (rf & 2) {
        ɵɵadvance();
        ɵɵconditional(ctx.hasPageHeader() ? 1 : -1);
      }
    },
    dependencies: [PoButtonComponent, PoPageComponent, PoPageContentComponent, PoPageHeaderComponent],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoPageDetailComponent, [{
    type: Component,
    args: [{
      selector: "po-page-detail",
      standalone: false,
      template: `<po-page>
  <!-- HEADER -->
  @if (hasPageHeader()) {
    <po-page-header [p-breadcrumb]="breadcrumb" [p-size]="componentsSize" [p-subtitle]="subtitle" [p-title]="title">
      <!-- OPERATIONS -->
      @if (hasAnyAction()) {
        <div class="po-page-header-actions">
          @if (hasEvent('back')) {
            <po-button
              [p-icon]="hasEditOrRemoveFn('icon')"
              [p-label]="literals.back"
              [p-kind]="hasEditOrRemoveFn('type')"
              [p-size]="componentsSize"
              (p-click)="back.emit()"
            >
            </po-button>
          }
          @if (hasEvent('remove')) {
            <po-button
              p-danger
              [p-icon]="hasEditFn('icon')"
              [p-label]="literals.remove"
              [p-kind]="hasEditFn('type')"
              [p-size]="componentsSize"
              (p-click)="remove.emit()"
            >
            </po-button>
          }
          @if (hasEvent('edit')) {
            <po-button
              p-icon="ICON_EDIT"
              p-kind="primary"
              [p-label]="literals.edit"
              [p-size]="componentsSize"
              (p-click)="edit.emit()"
            >
            </po-button>
          }
        </div>
      }
    </po-page-header>
  }

  <!-- CONTENT -->
  <po-page-content>
    <ng-content> </ng-content>
  </po-page-content>
</po-page>
`
    }]
  }], null, null);
})();
var poPageEditLiteralsDefault = {
  en: {
    cancel: "Cancel",
    save: "Save",
    saveNew: "Save and New"
  },
  es: {
    cancel: "Cancelar",
    save: "Guardar",
    saveNew: "Guardar y Nuevo"
  },
  pt: {
    cancel: "Cancelar",
    save: "Salvar",
    saveNew: "Salvar e Novo"
  },
  ru: {
    cancel: "отменить",
    save: "экономить",
    saveNew: "Сохранить и новый"
  }
};
var PoPageEditBaseComponent = class _PoPageEditBaseComponent {
  poPageContent;
  /** Objeto com propriedades do breadcrumb. */
  breadcrumb;
  /** Desabilita botões de submissão (save e saveNew) */
  disableSubmit;
  /**
   * Evento que será disparado ao clicar no botão de "Cancelar".
   *
   * ```
   * <po-page-edit (p-cancel)="myCancelFunction()">
   * </po-page-edit>
   * ```
   *
   * > Caso não utilizar esta propriedade, o botão de "Cancelar" não será exibido.
   */
  cancel = new EventEmitter();
  /**
   * Evento que será disparado ao clicar no botão de "Salvar".
   *
   * ```
   * <po-page-edit (p-save)="mySaveFunction()">
   * </po-page-edit>
   * ```
   *
   * > Caso não utilizar esta propriedade, o botão de "Salvar" não será exibido.
   */
  save = new EventEmitter();
  /**
   * Evento que será disparado ao clicar no botão de "Salvar e Novo".
   *
   * ```
   * <po-page-edit (p-save-new)="mySaveNewFunction()">
   * </po-page-edit>
   * ```
   *
   * > Caso não utilizar esta propriedade, o botão de "Salvar e Novo" não será exibido.
   */
  saveNew = new EventEmitter();
  _componentsSize = void 0;
  _initialComponentsSize = void 0;
  _literals;
  _title;
  language;
  /**
   * @optional
   *
   * @description
   *
   * Define o tamanho dos componentes de formulário no template:
   * - `small`: aplica a medida small de cada componente (disponível apenas para acessibilidade AA).
   * - `medium`: aplica a medida medium de cada componente.
   *
   * > Caso a acessibilidade AA não esteja configurada, o tamanho `medium` será mantido.
   * Para mais detalhes, consulte a documentação do [po-theme](https://po-ui.io/documentation/po-theme).
   *
   * @default `medium`
   */
  set componentsSize(value) {
    this._initialComponentsSize = value;
    this.applySizeBasedOnA11y();
  }
  get componentsSize() {
    return this._componentsSize ?? getDefaultSizeFn(PoFieldSize);
  }
  /**
   * @optional
   *
   * @description
   *
   * Objeto com as literais usadas no `po-page-edit`.
   *
   * Existem duas maneiras de customizar o componente, passando um objeto com todas as literais disponíveis:
   *
   * ```
   *  const customLiterals: PoPageEditLiterals = {
   *    cancel: 'Voltar',
   *    save: 'Confirmar',
   *    saveNew: 'Confirmar e criar um novo'
   *  };
   * ```
   *
   * Ou passando apenas as literais que deseja customizar:
   *
   * ```
   *  const customLiterals: PoPageEditLiterals = {
   *    cancel: 'Cancelar processo'
   *  };
   * ```
   *
   * E para carregar as literais customizadas, basta apenas passar o objeto para o componente.
   *
   * ```
   * <po-page-edit
   *   [p-literals]="customLiterals">
   * </po-page-edit>
   * ```
   *
   * > O objeto padrão de literais será traduzido de acordo com o idioma do
   * [`PoI18nService`](/documentation/po-i18n) ou do browser.
   */
  set literals(value) {
    if (value instanceof Object && !(value instanceof Array)) {
      this._literals = __spreadValues(__spreadValues(__spreadValues({}, poPageEditLiteralsDefault[poLocaleDefault]), poPageEditLiteralsDefault[this.language]), value);
    } else {
      this._literals = poPageEditLiteralsDefault[this.language];
    }
  }
  get literals() {
    return this._literals || poPageEditLiteralsDefault[this.language];
  }
  /** Título da página. */
  set title(title) {
    this._title = title;
    setTimeout(() => this.poPageContent.recalculateHeaderSize());
  }
  get title() {
    return this._title;
  }
  /**
   * @optional
   *
   * @description
   *
   * Subtitulo do Header da página.
   *
   * Suporta formatação básica com as tags `<b>` (negrito), `<strong>` (negrito), `<i>` (itálico), `<em>` (itálico) e
   * `<u>` (sublinhado).
   *
   * Exemplo:
   * ```typescript
   * subtitle = 'Fields marked with <b>*</b> are <u>required</u>';
   * ```
   *
   * > Requer que `p-title` esteja definido.
   */
  subtitle;
  constructor(languageService) {
    this.language = languageService.getShortLanguage();
  }
  onThemeChange() {
    this.applySizeBasedOnA11y();
  }
  applySizeBasedOnA11y() {
    const size = validateSizeFn(this._initialComponentsSize, PoFieldSize);
    this._componentsSize = size;
  }
  static ɵfac = function PoPageEditBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoPageEditBaseComponent)(ɵɵdirectiveInject(PoLanguageService));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoPageEditBaseComponent,
    viewQuery: function PoPageEditBaseComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(PoPageContentComponent, 7);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.poPageContent = _t.first);
      }
    },
    hostVars: 1,
    hostBindings: function PoPageEditBaseComponent_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("PoUiThemeChange", function PoPageEditBaseComponent_PoUiThemeChange_HostBindingHandler() {
          return ctx.onThemeChange();
        }, ɵɵresolveWindow);
      }
      if (rf & 2) {
        ɵɵattribute("p-components-size", ctx.componentsSize);
      }
    },
    inputs: {
      breadcrumb: [0, "p-breadcrumb", "breadcrumb"],
      disableSubmit: [0, "p-disable-submit", "disableSubmit"],
      componentsSize: [0, "p-components-size", "componentsSize"],
      literals: [0, "p-literals", "literals"],
      title: [0, "p-title", "title"],
      subtitle: [0, "p-subtitle", "subtitle"]
    },
    outputs: {
      cancel: "p-cancel",
      save: "p-save",
      saveNew: "p-save-new"
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoPageEditBaseComponent, [{
    type: Directive
  }], () => [{
    type: PoLanguageService
  }], {
    poPageContent: [{
      type: ViewChild,
      args: [PoPageContentComponent, {
        static: true
      }]
    }],
    breadcrumb: [{
      type: Input,
      args: ["p-breadcrumb"]
    }],
    disableSubmit: [{
      type: Input,
      args: ["p-disable-submit"]
    }],
    cancel: [{
      type: Output,
      args: ["p-cancel"]
    }],
    save: [{
      type: Output,
      args: ["p-save"]
    }],
    saveNew: [{
      type: Output,
      args: ["p-save-new"]
    }],
    componentsSize: [{
      type: Input,
      args: ["p-components-size"]
    }, {
      type: HostBinding,
      args: ["attr.p-components-size"]
    }],
    literals: [{
      type: Input,
      args: ["p-literals"]
    }],
    title: [{
      type: Input,
      args: ["p-title"]
    }],
    subtitle: [{
      type: Input,
      args: ["p-subtitle"]
    }],
    onThemeChange: [{
      type: HostListener,
      args: ["window:PoUiThemeChange"]
    }]
  });
})();
var PoPageEditComponent = class _PoPageEditComponent extends PoPageEditBaseComponent {
  getIcon(icon) {
    if (icon === "cancel") {
      return this.isPrimaryAction("cancel") ? "ICON_CLOSE" : "";
    }
    if (icon === "saveNew") {
      return this.isPrimaryAction("saveNew") ? "ICON_OK" : "";
    }
    return "";
  }
  getType(type) {
    const isCancelPrimaryAction = type === "cancel" && this.isPrimaryAction("cancel");
    const isSaveNewPrimaryAction = type === "saveNew" && this.isPrimaryAction("saveNew");
    return isCancelPrimaryAction || isSaveNewPrimaryAction ? "primary" : "secondary";
  }
  hasAnyAction() {
    return this.hasEvent("cancel") || this.hasEvent("saveNew") || this.hasEvent("save");
  }
  hasPageHeader() {
    return !!(this.title || this.hasAnyAction() || this.breadcrumb && this.breadcrumb.items.length);
  }
  hasEvent(event) {
    return !!this[event].observers.length;
  }
  isPrimaryAction(action) {
    const hasSaveAction = this.hasEvent("save");
    if (action === "saveNew") {
      return !hasSaveAction;
    }
    if (action === "cancel") {
      const hasSaveNewAction = this.hasEvent("saveNew");
      return !hasSaveNewAction && !hasSaveAction;
    }
    return false;
  }
  static ɵfac = /* @__PURE__ */ (() => {
    let ɵPoPageEditComponent_BaseFactory;
    return function PoPageEditComponent_Factory(__ngFactoryType__) {
      return (ɵPoPageEditComponent_BaseFactory || (ɵPoPageEditComponent_BaseFactory = ɵɵgetInheritedFactory(_PoPageEditComponent)))(__ngFactoryType__ || _PoPageEditComponent);
    };
  })();
  static ɵcmp = ɵɵdefineComponent({
    type: _PoPageEditComponent,
    selectors: [["po-page-edit"]],
    standalone: false,
    features: [ɵɵInheritDefinitionFeature],
    ngContentSelectors: _c12,
    decls: 4,
    vars: 1,
    consts: [[3, "p-breadcrumb", "p-size", "p-subtitle", "p-title"], [1, "po-page-header-actions"], [3, "p-icon", "p-label", "p-kind", "p-size"], [3, "p-disabled", "p-icon", "p-label", "p-kind", "p-size"], ["p-icon", "ICON_OK", "p-kind", "primary", 3, "p-disabled", "p-label", "p-size"], [3, "p-click", "p-icon", "p-label", "p-kind", "p-size"], [3, "p-click", "p-disabled", "p-icon", "p-label", "p-kind", "p-size"], ["p-icon", "ICON_OK", "p-kind", "primary", 3, "p-click", "p-disabled", "p-label", "p-size"]],
    template: function PoPageEditComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵprojectionDef();
        ɵɵelementStart(0, "po-page");
        ɵɵconditionalCreate(1, PoPageEditComponent_Conditional_1_Template, 2, 5, "po-page-header", 0);
        ɵɵelementStart(2, "po-page-content");
        ɵɵprojection(3);
        ɵɵelementEnd()();
      }
      if (rf & 2) {
        ɵɵadvance();
        ɵɵconditional(ctx.hasPageHeader() ? 1 : -1);
      }
    },
    dependencies: [PoButtonComponent, PoPageComponent, PoPageContentComponent, PoPageHeaderComponent],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoPageEditComponent, [{
    type: Component,
    args: [{
      selector: "po-page-edit",
      standalone: false,
      template: `<po-page>
  <!-- HEADER -->
  @if (hasPageHeader()) {
    <po-page-header [p-breadcrumb]="breadcrumb" [p-size]="componentsSize" [p-subtitle]="subtitle" [p-title]="title">
      <!-- OPERATIONS -->
      @if (hasAnyAction()) {
        <div class="po-page-header-actions">
          @if (hasEvent('cancel')) {
            <po-button
              [p-icon]="getIcon('cancel')"
              [p-label]="literals.cancel"
              [p-kind]="getType('cancel')"
              [p-size]="componentsSize"
              (p-click)="cancel.emit()"
            >
            </po-button>
          }
          @if (hasEvent('saveNew')) {
            <po-button
              [p-disabled]="disableSubmit"
              [p-icon]="getIcon('saveNew')"
              [p-label]="literals.saveNew"
              [p-kind]="getType('saveNew')"
              [p-size]="componentsSize"
              (p-click)="saveNew.emit()"
            >
            </po-button>
          }
          @if (hasEvent('save')) {
            <po-button
              p-icon="ICON_OK"
              p-kind="primary"
              [p-disabled]="disableSubmit"
              [p-label]="literals.save"
              [p-size]="componentsSize"
              (p-click)="save.emit()"
            >
            </po-button>
          }
        </div>
      }
    </po-page-header>
  }

  <!-- CONTENT -->
  <po-page-content>
    <ng-content> </ng-content>
  </po-page-content>
</po-page>
`
    }]
  }], null, null);
})();
var poPageListLiteralsDefault = {
  en: {
    otherActions: "Other actions"
  },
  es: {
    otherActions: "Otras acciones"
  },
  pt: {
    otherActions: "Outras ações"
  },
  ru: {
    otherActions: "Другие действия"
  }
};
var PoPageListBaseComponent = class _PoPageListBaseComponent {
  poPageContent;
  /**
   * @optional
   *
   * @description
   *
   * Objeto que implementa as propriedades da interface `PoBreadcrumb`.
   */
  breadcrumb;
  /**
   * @description
   *
   * Objeto que implementa as propriedades da interface `PoPageFilter`.
   */
  filter;
  /**
   * @optional
   *
   * @description
   *
   * Valor padrão na busca rápida ao inicializar o componente
   *
   */
  quickSearchValue;
  visibleActions = [];
  language;
  resizeListener;
  _actions = [];
  _disclaimerGroup;
  _componentsSize = void 0;
  _initialComponentsSize = void 0;
  _literals;
  _title;
  /**
   * @optional
   *
   * @description
   *
   * Nesta propriedade deve ser definido um array de objetos que implementam a interface `PoPageAction`.
   */
  set actions(actions2) {
    this._actions = Array.isArray(actions2) ? actions2 : [];
    this.visibleActions = this.actions.filter((action) => action.visible !== false);
    this.setDropdownActions();
  }
  get actions() {
    return this._actions;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define o tamanho dos componentes de formulário no template:
   * - `small`: aplica a medida small de cada componente (disponível apenas para acessibilidade AA).
   * - `medium`: aplica a medida medium de cada componente.
   *
   * > Caso a acessibilidade AA não esteja configurada, o tamanho `medium` será mantido.
   * Para mais detalhes, consulte a documentação do [po-theme](https://po-ui.io/documentation/po-theme).
   *
   * @default `medium`
   */
  set componentsSize(value) {
    this._initialComponentsSize = value;
    this.applySizeBasedOnA11y();
  }
  get componentsSize() {
    return this._componentsSize ?? getDefaultSizeFn(PoFieldSize);
  }
  /**
   * @optional
   *
   * @description
   *
   * Objeto que implementa as propriedades da interface `PoDisclaimerGroup`.
   */
  set disclaimerGroup(value) {
    if (!value) {
      value = {};
    }
    this._disclaimerGroup = value;
  }
  get disclaimerGroup() {
    return this._disclaimerGroup;
  }
  /**
   * @optional
   *
   * @description
   *
   * Objeto com as literais usadas no `po-page-list`.
   *
   * Existem duas maneiras de customizar o componente, passando um objeto com todas as literais disponíveis:
   *
   * ```
   *  const customLiterals: PoPageListLiterals = {
   *    otherActions: 'Mais ações'
   *  };
   * ```
   *
   * Ou passando apenas as literais que deseja customizar:
   *
   * ```
   *  const customLiterals: PoPageListLiterals = {
   *    otherActions: 'Ações da página'
   *  };
   * ```
   *
   * E para carregar as literais customizadas, basta apenas passar o objeto para o componente.
   *
   * ```
   * <po-page-list
   *   [p-literals]="customLiterals">
   * </po-page-list>
   * ```
   *
   * > O valor padrão será traduzido de acordo com o idioma configurado no [`PoI18nService`](/documentation/po-i18n) ou *browser*.
   */
  set literals(value) {
    if (value instanceof Object && !(value instanceof Array)) {
      this._literals = __spreadValues(__spreadValues(__spreadValues({}, poPageListLiteralsDefault[poLocaleDefault]), poPageListLiteralsDefault[this.language]), value);
    } else {
      this._literals = poPageListLiteralsDefault[this.language];
    }
  }
  get literals() {
    return this._literals || poPageListLiteralsDefault[this.language];
  }
  /** Título da página. */
  set title(title) {
    this._title = title;
    setTimeout(() => this.poPageContent.recalculateHeaderSize());
  }
  get title() {
    return this._title;
  }
  /**
   * @optional
   *
   * @description
   *
   * Subtitulo do Header da página.
   *
   * Suporta formatação básica com as tags `<b>` (negrito), `<strong>` (negrito), `<i>` (itálico), `<em>` (itálico) e
   * `<u>` (sublinhado).
   *
   * Exemplo:
   * ```typescript
   * subtitle = 'Manage <b>active</b> and <i>pending</i> processes';
   * ```
   *
   * > Requer que `p-title` esteja definido.
   */
  subtitle;
  constructor(languageService) {
    this.language = languageService.getShortLanguage();
  }
  onThemeChange() {
    this.applySizeBasedOnA11y();
  }
  applySizeBasedOnA11y() {
    const size = validateSizeFn(this._initialComponentsSize, PoFieldSize);
    this._componentsSize = size;
  }
  static ɵfac = function PoPageListBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoPageListBaseComponent)(ɵɵdirectiveInject(PoLanguageService));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoPageListBaseComponent,
    viewQuery: function PoPageListBaseComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(PoPageContentComponent, 7);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.poPageContent = _t.first);
      }
    },
    hostVars: 1,
    hostBindings: function PoPageListBaseComponent_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("PoUiThemeChange", function PoPageListBaseComponent_PoUiThemeChange_HostBindingHandler() {
          return ctx.onThemeChange();
        }, ɵɵresolveWindow);
      }
      if (rf & 2) {
        ɵɵattribute("p-components-size", ctx.componentsSize);
      }
    },
    inputs: {
      breadcrumb: [0, "p-breadcrumb", "breadcrumb"],
      filter: [0, "p-filter", "filter"],
      quickSearchValue: [0, "p-quick-search-value", "quickSearchValue"],
      actions: [0, "p-actions", "actions"],
      componentsSize: [0, "p-components-size", "componentsSize"],
      disclaimerGroup: [0, "p-disclaimer-group", "disclaimerGroup"],
      literals: [0, "p-literals", "literals"],
      title: [0, "p-title", "title"],
      subtitle: [0, "p-subtitle", "subtitle"]
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoPageListBaseComponent, [{
    type: Directive
  }], () => [{
    type: PoLanguageService
  }], {
    poPageContent: [{
      type: ViewChild,
      args: [PoPageContentComponent, {
        static: true
      }]
    }],
    breadcrumb: [{
      type: Input,
      args: ["p-breadcrumb"]
    }],
    filter: [{
      type: Input,
      args: ["p-filter"]
    }],
    quickSearchValue: [{
      type: Input,
      args: ["p-quick-search-value"]
    }],
    actions: [{
      type: Input,
      args: ["p-actions"]
    }],
    componentsSize: [{
      type: Input,
      args: ["p-components-size"]
    }, {
      type: HostBinding,
      args: ["attr.p-components-size"]
    }],
    disclaimerGroup: [{
      type: Input,
      args: ["p-disclaimer-group"]
    }],
    literals: [{
      type: Input,
      args: ["p-literals"]
    }],
    title: [{
      type: Input,
      args: ["p-title"]
    }],
    subtitle: [{
      type: Input,
      args: ["p-subtitle"]
    }],
    onThemeChange: [{
      type: HostListener,
      args: ["window:PoUiThemeChange"]
    }]
  });
})();
var PoPageListComponent = class _PoPageListComponent extends PoPageListBaseComponent {
  renderer = inject(Renderer2);
  router = inject(Router);
  changeDetector = inject(ChangeDetectorRef);
  filterInput;
  advancedSearch;
  dropdownActions;
  isMobile;
  limitPrimaryActions = 3;
  callFunction = callFunction;
  isRecalculate = true;
  maxWidthMobile = 480;
  /* istanbul ignore next */
  constructor() {
    const languageService = inject(PoLanguageService);
    super(languageService);
    this.initializeListeners();
  }
  ngOnInit() {
    this.advancedSearch = this.initializeFixedLiterals();
  }
  ngAfterContentInit() {
    this.setIsMobile();
    this.setDropdownActions();
  }
  ngOnChanges(changes) {
    this.setDropdownActions();
  }
  ngOnDestroy() {
    this.removeListeners();
  }
  actionIsDisabled(action) {
    return isTypeof(action.disabled, "function") ? action.disabled(action) : action.disabled;
  }
  callAction(item) {
    if (item.url) {
      isExternalLink(item.url) ? PoUtils.openExternalLink(item.url) : this.router.navigate([item.url]);
    } else if (item.action) {
      item.action();
    }
  }
  hasPageHeader() {
    return !!(this.title || this.visibleActions && this.visibleActions.length || this.breadcrumb && this.breadcrumb.items.length);
  }
  hasCustomFilterSize() {
    if (!this.filter) {
      return false;
    }
    return this.filter.width >= 1 && this.filter.width <= 6;
  }
  filterSizeClass(width) {
    const smWidth = Math.max(this.filter?.advancedAction ? 6 : 2, width);
    const mdWidth = Math.max(this.filter?.advancedAction ? 4 : 1, width);
    if (this.filter?.advancedAction) {
      width = Math.max(width, 2);
    }
    return `po-sm-${smWidth} po-md-${mdWidth} po-lg-${width} po-xl-${width}`;
  }
  setDropdownActions() {
    if (this.visibleActions.length > this.limitPrimaryActions) {
      this.dropdownActions = this.visibleActions.slice(this.limitPrimaryActions - 1);
    }
  }
  callActionFilter(field) {
    this.filter[field](this.filterInput.nativeElement.value);
    this.changeDetector.detectChanges();
  }
  /**
   * Limpa o campo de pesquisa.
   */
  clearInputSearch() {
    this.filterInput.nativeElement.value = null;
  }
  onkeypress(key2) {
    if (key2 === 13) {
      this.callActionFilter("action");
    }
  }
  // Recebe evento change do disclaimer e recalcula tela
  onChangeDisclaimerGroup(disclaimers) {
    if (disclaimers && disclaimers.length && this.isRecalculate || disclaimers.length === 0 && !this.isRecalculate) {
      this.poPageContent.recalculateHeaderSize();
      this.isRecalculate = !this.isRecalculate;
    }
    if (this.disclaimerGroup && this.disclaimerGroup.change) {
      this.disclaimerGroup.change(disclaimers);
    }
  }
  onRemoveDisclaimer(removeData) {
    if (this.disclaimerGroup.remove) {
      this.disclaimerGroup.remove(removeData);
    }
  }
  onRemoveAllDisclaimers(removedDisclaimers) {
    if (this.disclaimerGroup.removeAll) {
      this.disclaimerGroup.removeAll(removedDisclaimers);
    }
  }
  initializeFixedLiterals() {
    const literal = {
      pt: {
        advancedSearch: "Busca avançada"
      },
      en: {
        advancedSearch: "Advanced search"
      },
      es: {
        advancedSearch: "Búsqueda avanzada"
      },
      ru: {
        advancedSearch: "полный поиск"
      }
    };
    return literal[this.language].advancedSearch;
  }
  initializeListeners() {
    this.resizeListener = this.renderer.listen("window", "resize", (event) => {
      this.onResize(event);
    });
  }
  removeListeners() {
    this.resizeListener();
  }
  onResize(event) {
    const width = event.target.innerWidth;
    if (width < this.maxWidthMobile) {
      this.isMobile = true;
      this.limitPrimaryActions = 2;
      this.setDropdownActions();
    } else {
      this.isMobile = false;
      this.limitPrimaryActions = 3;
      this.setDropdownActions();
    }
  }
  setIsMobile() {
    if (window.innerWidth < this.maxWidthMobile) {
      this.isMobile = true;
      this.limitPrimaryActions = 2;
    }
  }
  static ɵfac = function PoPageListComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoPageListComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoPageListComponent,
    selectors: [["po-page-list"]],
    viewQuery: function PoPageListComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c106, 5);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.filterInput = _t.first);
      }
    },
    standalone: false,
    features: [ɵɵInheritDefinitionFeature, ɵɵNgOnChangesFeature],
    ngContentSelectors: _c12,
    decls: 4,
    vars: 1,
    consts: [["filterInput", ""], [3, "po-page-list-header-padding", "p-breadcrumb", "p-title", "p-size", "p-subtitle"], [3, "p-breadcrumb", "p-title", "p-size", "p-subtitle"], [1, "po-page-list-container"], [1, "po-page-list-operations"], [1, "po-page-list-actions"], ["p-kind", "primary", 3, "p-danger", "p-disabled", "p-icon", "p-label", "p-size"], [3, "p-danger", "p-disabled", "p-label", "p-size"], [3, "p-actions", "p-label", "p-size"], [1, "po-page-list-filter-wrapper", 3, "ngClass"], [3, "po-page-list-disclaimer-group", "p-disclaimers", "p-hide-remove-all", "p-title"], ["p-kind", "primary", 3, "p-click", "p-danger", "p-disabled", "p-icon", "p-label", "p-size"], [3, "p-click", "p-danger", "p-disabled", "p-label", "p-size"], [1, "po-field-container-content", "po-field-container-input", "po-page-filter-content", 3, "ngClass"], [1, "po-field-icon-container-right"], ["p-icon", "ICON_SEARCH", 1, "po-field-icon", "po-icon-input", 3, "click"], ["name", "model", "type", "text", 1, "po-input", "po-input-icon-right", 3, "keypress", "placeholder", "value"], [1, "po-page-list-filter-search"], ["tabindex", "0", 1, "po-page-list-filter-search-link", 3, "click", "keydown.enter"], [3, "p-change", "p-remove", "p-remove-all", "p-disclaimers", "p-hide-remove-all", "p-title"]],
    template: function PoPageListComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵprojectionDef();
        ɵɵelementStart(0, "po-page");
        ɵɵconditionalCreate(1, PoPageListComponent_Conditional_1_Template, 10, 14, "po-page-header", 1);
        ɵɵelementStart(2, "po-page-content");
        ɵɵprojection(3);
        ɵɵelementEnd()();
      }
      if (rf & 2) {
        ɵɵadvance();
        ɵɵconditional(ctx.hasPageHeader() ? 1 : -1);
      }
    },
    dependencies: [NgClass, PoButtonComponent, PoDisclaimerGroupComponent, PoDropdownComponent, PoIconComponent, PoPageComponent, PoPageContentComponent, PoPageHeaderComponent],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoPageListComponent, [{
    type: Component,
    args: [{
      selector: "po-page-list",
      standalone: false,
      template: `<po-page>
  <!-- HEADER -->
  @if (hasPageHeader()) {
    <po-page-header
      [class.po-page-list-header-padding]="filter && !visibleActions.length"
      [p-breadcrumb]="breadcrumb"
      [p-title]="title"
      [p-size]="componentsSize"
      [p-subtitle]="subtitle"
    >
      <div class="po-page-list-container">
        <!-- OPERATIONS -->
        <div class="po-page-list-operations">
          <div class="po-page-list-actions" [class.po-page-list-actions-padding]="filter">
            @if (visibleActions[0]) {
              <po-button
                p-kind="primary"
                [p-danger]="visibleActions[0].type === 'danger'"
                [p-disabled]="actionIsDisabled(actions[0])"
                [p-icon]="visibleActions[0].icon"
                [p-label]="visibleActions[0].label"
                [p-size]="componentsSize"
                (p-click)="callAction(visibleActions[0])"
              >
              </po-button>
            }
            @if (visibleActions[1] && (visibleActions.length === 2 || !isMobile)) {
              <po-button
                [p-danger]="visibleActions[1].type === 'danger'"
                [p-disabled]="actionIsDisabled(actions[1])"
                [p-label]="visibleActions[1].label"
                [p-size]="componentsSize"
                (p-click)="callAction(visibleActions[1])"
              >
              </po-button>
            }
            @if (visibleActions.length === 3 && visibleActions[2] && !isMobile) {
              <po-button
                [p-danger]="visibleActions[2].type === 'danger'"
                [p-disabled]="actionIsDisabled(visibleActions[2])"
                [p-label]="visibleActions[2].label"
                [p-size]="componentsSize"
                (p-click)="callAction(visibleActions[2])"
              >
              </po-button>
            }
            @if (visibleActions.length > limitPrimaryActions) {
              <po-dropdown [p-actions]="dropdownActions" [p-label]="literals.otherActions" [p-size]="componentsSize">
              </po-dropdown>
            }
          </div>
          <!-- FILTER -->
          @if (filter) {
            <div
              class="po-page-list-filter-wrapper"
              [ngClass]="[
                hasCustomFilterSize() ? filterSizeClass(filter.width) : '',
                (visibleActions?.length > 0 || subtitle) && title ? 'po-page-list-filter-extra-margin-top' : ''
              ]"
            >
              <div
                class="po-field-container-content po-field-container-input po-page-filter-content"
                [ngClass]="[
                  hasCustomFilterSize()
                    ? filter.advancedAction
                      ? 'po-page-filter-input-variable-size'
                      : 'po-page-filter-input-variable-size-wo-adv-search'
                    : ''
                ]"
              >
                <div class="po-field-icon-container-right">
                  <po-icon
                    p-icon="ICON_SEARCH"
                    class="po-field-icon po-icon-input"
                    [class.po-field-icon-aa]="componentsSize === 'small'"
                    (click)="callActionFilter('action')"
                  >
                  </po-icon>
                </div>
                <input
                  #filterInput
                  class="po-input po-input-icon-right"
                  [class.po-input-icon-right-aa]="componentsSize === 'small'"
                  [attr.p-size]="componentsSize"
                  name="model"
                  type="text"
                  [placeholder]="filter.placeholder || ''"
                  (keypress)="onkeypress($event.keyCode)"
                  [value]="quickSearchValue || ''"
                />
              </div>
              @if (filter.advancedAction) {
                <div class="po-page-list-filter-search">
                  <span
                    class="po-page-list-filter-search-link"
                    tabindex="0"
                    (click)="callActionFilter('advancedAction')"
                    (keydown.enter)="callActionFilter('advancedAction')"
                  >
                    {{ advancedSearch }}
                  </span>
                </div>
              }
            </div>
          }
        </div>
        <!-- DISCLAIMER -->
        @if (!!disclaimerGroup) {
          <po-disclaimer-group
            [class.po-page-list-disclaimer-group]="!!disclaimerGroup?.disclaimers?.length"
            [p-disclaimers]="disclaimerGroup?.disclaimers"
            [p-hide-remove-all]="disclaimerGroup?.hideRemoveAll"
            [p-title]="disclaimerGroup?.title"
            (p-change)="onChangeDisclaimerGroup($event)"
            (p-remove)="onRemoveDisclaimer($event)"
            (p-remove-all)="onRemoveAllDisclaimers($event)"
          >
          </po-disclaimer-group>
        }
      </div>
    </po-page-header>
  }

  <!-- CONTENT -->
  <po-page-content>
    <ng-content></ng-content>
  </po-page-content>
</po-page>
`
    }]
  }], () => [], {
    filterInput: [{
      type: ViewChild,
      args: ["filterInput"]
    }]
  });
})();
var PoPageModule = class _PoPageModule {
  static ɵfac = function PoPageModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoPageModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoPageModule,
    declarations: [PoPageComponent, PoPageContentComponent, PoPageDefaultComponent, PoPageDetailComponent, PoPageEditComponent, PoPageHeaderComponent, PoPageListComponent],
    imports: [CommonModule, FormsModule, RouterModule, PoBreadcrumbModule, PoButtonModule, PoDisclaimerGroupModule, PoDropdownModule, PoFieldModule, PoHelperModule, PoIconModule, PoLanguageModule, PoModalModule, PoPageSlideModule],
    exports: [PoPageDefaultComponent, PoPageDetailComponent, PoPageEditComponent, PoPageListComponent, PoPageSlideComponent]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [CommonModule, FormsModule, RouterModule, PoBreadcrumbModule, PoButtonModule, PoDisclaimerGroupModule, PoDropdownModule, PoFieldModule, PoHelperModule, PoIconModule, PoLanguageModule, PoModalModule, PoPageSlideModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoPageModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, FormsModule, RouterModule, PoBreadcrumbModule, PoButtonModule, PoDisclaimerGroupModule, PoDropdownModule, PoFieldModule, PoHelperModule, PoIconModule, PoLanguageModule, PoModalModule, PoPageSlideModule],
      declarations: [PoPageComponent, PoPageContentComponent, PoPageDefaultComponent, PoPageDetailComponent, PoPageEditComponent, PoPageHeaderComponent, PoPageListComponent],
      exports: [PoPageDefaultComponent, PoPageDetailComponent, PoPageEditComponent, PoPageListComponent, PoPageSlideComponent]
    }]
  }], null, null);
})();
var PoSkeletonAnimation;
(function(PoSkeletonAnimation2) {
  PoSkeletonAnimation2["none"] = "none";
  PoSkeletonAnimation2["pulse"] = "pulse";
  PoSkeletonAnimation2["shimmer"] = "shimmer";
})(PoSkeletonAnimation || (PoSkeletonAnimation = {}));
var PoSkeletonSize;
(function(PoSkeletonSize2) {
  PoSkeletonSize2["xs"] = "xs";
  PoSkeletonSize2["sm"] = "sm";
  PoSkeletonSize2["md"] = "md";
  PoSkeletonSize2["lg"] = "lg";
  PoSkeletonSize2["xl"] = "xl";
  PoSkeletonSize2["xxl"] = "2xl";
})(PoSkeletonSize || (PoSkeletonSize = {}));
var PoSkeletonType;
(function(PoSkeletonType2) {
  PoSkeletonType2["normal"] = "normal";
  PoSkeletonType2["primary"] = "primary";
  PoSkeletonType2["content"] = "content";
})(PoSkeletonType || (PoSkeletonType = {}));
var PoSkeletonVariant;
(function(PoSkeletonVariant2) {
  PoSkeletonVariant2["text"] = "text";
  PoSkeletonVariant2["rectangle"] = "rectangle";
  PoSkeletonVariant2["square"] = "square";
  PoSkeletonVariant2["circle"] = "circle";
})(PoSkeletonVariant || (PoSkeletonVariant = {}));
var PoSkeletonBaseComponent = class _PoSkeletonBaseComponent {
  /**
   * @optional
   *
   * @description
   *
   * Define a variante visual do skeleton.
   *
   * Valores válidos:
   * - `text`: Simula uma linha de texto (altura padrão: 1em)
   * - `rectangle`: Forma retangular (proporção 3:1 por padrão)
   * - `square`: Forma quadrada (largura e altura iguais)
   * - `circle`: Forma circular (largura e altura iguais)
   *
   * @default `text`
   */
  variant = input(PoSkeletonVariant.text, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "variant"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-variant"
  }));
  /**
   * @optional
   *
   * @description
   *
   * Define o tipo visual do skeleton, alterando sua cor de fundo.
   *
   * Valores válidos:
   * - `normal`: Cor neutra clara (padrão)
   * - `primary`: Cor neutra média
   * - `content`: Fundo branco
   *
   * @default `normal`
   */
  type = input(PoSkeletonType.normal, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "type"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-type"
  }));
  /**
   * @optional
   *
   * @description
   *
   * Define o tipo de animação do skeleton.
   *
   * Valores válidos:
   * - `none`: Sem animação
   * - `pulse`: Animação de pulsação
   * - `shimmer`: Animação de brilho deslizante
   *
   * @default `shimmer`
   */
  animation = input(PoSkeletonAnimation.shimmer, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "animation"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-animation"
  }));
  /**
   * @optional
   *
   * @description
   *
   * Define o tamanho do skeleton para as variantes pré-definidas (`rectangle`, `square`, `circle`).
   *
   * Valores válidos:
   * - `xs`: Extra pequeno (24px para square/circle, 72px x 24px para rectangle)
   * - `sm`: Pequeno (32px para square/circle, 96px x 32px para rectangle)
   * - `md`: Médio (48px para square/circle, 144px x 48px para rectangle)
   * - `lg`: Grande (64px para square/circle, 192px x 64px para rectangle)
   * - `xl`: Extra grande (96px para square/circle, 288px x 96px para rectangle)
   * - `2xl`: Extra extra grande (144px para square/circle, 432px x 144px para rectangle)
   *
   * Esta propriedade é ignorada quando `p-width` ou `p-height` são definidos explicitamente.
   *
   * @default `md`
   */
  size = input(PoSkeletonSize.md, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "size"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-size"
  }));
  /**
   * @optional
   *
   * @description
   *
   * Define a largura do skeleton.
   * Aceita valores CSS válidos (px, %, em, rem).
   *
   * Quando definido, sobrescreve a largura padrão da variante.
   *
   * @default `100%` para variante `text`, tamanho baseado em `p-size` para outras variantes
   */
  width = input("", __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "width"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-width"
  }));
  /**
   * @optional
   *
   * @description
   *
   * Define a altura do skeleton.
   * Aceita valores CSS válidos (px, %, em, rem).
   *
   * Quando definido, sobrescreve a altura padrão da variante.
   */
  height = input("", __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "height"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-height"
  }));
  /**
   * @optional
   *
   * @description
   *
   * Define o raio da borda do skeleton.
   * Aceita valores CSS válidos (px, %, em, rem).
   *
   * Esta propriedade sobrescreve o border-radius padrão de cada variante.
   */
  borderRadius = input("", __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "borderRadius"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-border-radius"
  }));
  /**
   * @optional
   *
   * @description
   *
   * Define a descrição acessível do conteúdo que está sendo carregado.
   *
   * Este texto será anunciado por leitores de tela, garantindo que usuários de tecnologias assistivas
   * sejam informados sobre o estado de carregamento.
   *
   * **Quando usar:**
   * - Use em **skeletons únicos** ou no **primeiro skeleton de um grupo** com descrição contextual
   * - Evite usar em múltiplos skeletons dentro da mesma área sem contexto, para não causar repetição excessiva
   *
   * **Exemplos de uso:**
   *
   * ```html
   * <!-- ✅ BOM: Skeleton único com contexto -->
   * <po-skeleton p-aria-label="Carregando perfil do usuário"></po-skeleton>
   *
   * <!-- ✅ BOM: Grupo de skeletons - apenas o primeiro anuncia -->
   * <po-skeleton p-aria-label="Carregando lista de produtos"></po-skeleton>
   * <po-skeleton p-type="primary"></po-skeleton>
   * <po-skeleton p-variant="circle"></po-skeleton>
   *
   * <!-- ❌ EVITE: Múltiplos skeletons com a mesma label -->
   * <po-skeleton p-aria-label="Carregando"></po-skeleton>  <!-- "Carregando" -->
   * <po-skeleton p-aria-label="Carregando"></po-skeleton>  <!-- "Carregando" -->
   * <po-skeleton p-aria-label="Carregando"></po-skeleton>  <!-- "Carregando" (repetitivo!) -->
   * ```
   *
   * > **Boas práticas de acessibilidade:**
   * > - Forneça contexto específico na label para que os usuários entendam o que está carregando
   *
   * > - Em casos complexos, considere usar um único `<div role="status">` para todo o grupo
   * >   de skeletons, ao invés de múltiplas labels idênticas, para evitar repetição excessiva de anúncios
   */
  ariaLabel = input("", __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "ariaLabel"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-aria-label"
  }));
  static ɵfac = function PoSkeletonBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoSkeletonBaseComponent)();
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoSkeletonBaseComponent,
    inputs: {
      variant: [1, "p-variant", "variant"],
      type: [1, "p-type", "type"],
      animation: [1, "p-animation", "animation"],
      size: [1, "p-size", "size"],
      width: [1, "p-width", "width"],
      height: [1, "p-height", "height"],
      borderRadius: [1, "p-border-radius", "borderRadius"],
      ariaLabel: [1, "p-aria-label", "ariaLabel"]
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoSkeletonBaseComponent, [{
    type: Directive
  }], null, {
    variant: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-variant",
        required: false
      }]
    }],
    type: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-type",
        required: false
      }]
    }],
    animation: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-animation",
        required: false
      }]
    }],
    size: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-size",
        required: false
      }]
    }],
    width: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-width",
        required: false
      }]
    }],
    height: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-height",
        required: false
      }]
    }],
    borderRadius: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-border-radius",
        required: false
      }]
    }],
    ariaLabel: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-aria-label",
        required: false
      }]
    }]
  });
})();
var PoSkeletonComponent = class _PoSkeletonComponent extends PoSkeletonBaseComponent {
  sizeMap = {
    [PoSkeletonSize.xs]: {
      square: 24,
      circle: 24,
      rectangleWidth: 72,
      rectangleHeight: 24
    },
    [PoSkeletonSize.sm]: {
      square: 32,
      circle: 32,
      rectangleWidth: 96,
      rectangleHeight: 32
    },
    [PoSkeletonSize.md]: {
      square: 48,
      circle: 48,
      rectangleWidth: 144,
      rectangleHeight: 48
    },
    [PoSkeletonSize.lg]: {
      square: 64,
      circle: 64,
      rectangleWidth: 192,
      rectangleHeight: 64
    },
    [PoSkeletonSize.xl]: {
      square: 96,
      circle: 96,
      rectangleWidth: 288,
      rectangleHeight: 96
    },
    [PoSkeletonSize.xxl]: {
      square: 144,
      circle: 144,
      rectangleWidth: 432,
      rectangleHeight: 144
    }
  };
  defaultTextDimensions = {
    width: "100%",
    height: "1em"
  };
  defaultBorderRadius = {
    [PoSkeletonVariant.text]: void 0,
    [PoSkeletonVariant.square]: void 0,
    [PoSkeletonVariant.rectangle]: void 0,
    [PoSkeletonVariant.circle]: "50%"
  };
  computedStyles = computed(() => {
    const styles = {};
    const variant = this.variant();
    const size = this.size();
    const sizeConfig = this.sizeMap[size];
    const defaultDimensions = this.getDimensions(variant, sizeConfig);
    styles["width"] = this.width() || defaultDimensions.width || "";
    styles["height"] = this.height() || defaultDimensions.height || "";
    const borderRadiusValue = this.borderRadius();
    const defaultRadius = this.defaultBorderRadius[variant];
    if (borderRadiusValue || defaultRadius) {
      styles["border-radius"] = borderRadiusValue || defaultRadius;
    }
    return styles;
  }, ...ngDevMode ? [{
    debugName: "computedStyles"
  }] : (
    /* istanbul ignore next */
    []
  ));
  computedClasses = computed(() => ({
    "po-skeleton": true,
    [`po-skeleton-${this.variant()}`]: true,
    [`po-skeleton-type-${this.type()}`]: true,
    [`po-skeleton-animation-${this.animation()}`]: true
  }), ...ngDevMode ? [{
    debugName: "computedClasses"
  }] : (
    /* istanbul ignore next */
    []
  ));
  getDimensions(variant, sizeConfig) {
    switch (variant) {
      case PoSkeletonVariant.text:
        return this.defaultTextDimensions;
      case PoSkeletonVariant.square:
        return {
          width: `${sizeConfig.square}px`,
          height: `${sizeConfig.square}px`
        };
      case PoSkeletonVariant.circle:
        return {
          width: `${sizeConfig.circle}px`,
          height: `${sizeConfig.circle}px`
        };
      case PoSkeletonVariant.rectangle:
        return {
          width: `${sizeConfig.rectangleWidth}px`,
          height: `${sizeConfig.rectangleHeight}px`
        };
      default:
        return {
          width: "",
          height: ""
        };
    }
  }
  static ɵfac = /* @__PURE__ */ (() => {
    let ɵPoSkeletonComponent_BaseFactory;
    return function PoSkeletonComponent_Factory(__ngFactoryType__) {
      return (ɵPoSkeletonComponent_BaseFactory || (ɵPoSkeletonComponent_BaseFactory = ɵɵgetInheritedFactory(_PoSkeletonComponent)))(__ngFactoryType__ || _PoSkeletonComponent);
    };
  })();
  static ɵcmp = ɵɵdefineComponent({
    type: _PoSkeletonComponent,
    selectors: [["po-skeleton"]],
    standalone: false,
    features: [ɵɵInheritDefinitionFeature],
    decls: 2,
    vars: 3,
    consts: [["aria-hidden", "true", 1, "po-skeleton-wrapper", 3, "ngClass", "ngStyle"], ["role", "status", "aria-live", "polite", 1, "po-skeleton-aria-label"]],
    template: function PoSkeletonComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelement(0, "div", 0);
        ɵɵconditionalCreate(1, PoSkeletonComponent_Conditional_1_Template, 2, 1, "span", 1);
      }
      if (rf & 2) {
        ɵɵproperty("ngClass", ctx.computedClasses())("ngStyle", ctx.computedStyles());
        ɵɵadvance();
        ɵɵconditional(ctx.ariaLabel() ? 1 : -1);
      }
    },
    dependencies: [NgClass, NgStyle],
    encapsulation: 2,
    changeDetection: 0
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoSkeletonComponent, [{
    type: Component,
    args: [{
      selector: "po-skeleton",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: '<div class="po-skeleton-wrapper" [ngClass]="computedClasses()" [ngStyle]="computedStyles()" aria-hidden="true"></div>\n@if (ariaLabel()) {\n  <span class="po-skeleton-aria-label" role="status" aria-live="polite">{{ ariaLabel() }}</span>\n}\n'
    }]
  }], null, null);
})();
var PoSkeletonModule = class _PoSkeletonModule {
  static ɵfac = function PoSkeletonModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoSkeletonModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoSkeletonModule,
    declarations: [PoSkeletonComponent],
    imports: [CommonModule],
    exports: [PoSkeletonComponent]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [CommonModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoSkeletonModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule],
      declarations: [PoSkeletonComponent],
      exports: [PoSkeletonComponent]
    }]
  }], null, null);
})();
var PoSlideCirclesComponent = class _PoSlideCirclesComponent {
  /** Index do slide atual. */
  currentSlideIndex;
  /** Itens do slide. */
  items;
  /** Evento emitido ao clicar em um controle. */
  click = new EventEmitter();
  static ɵfac = function PoSlideCirclesComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoSlideCirclesComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoSlideCirclesComponent,
    selectors: [["po-slide-circles"]],
    inputs: {
      currentSlideIndex: [0, "p-current-slide-index", "currentSlideIndex"],
      items: [0, "p-items", "items"]
    },
    outputs: {
      click: "p-click"
    },
    standalone: false,
    decls: 3,
    vars: 0,
    consts: [[1, "po-slide-circles"], [1, "po-slide-circle", 3, "po-slide-active-circle"], [1, "po-slide-circle", 3, "click"]],
    template: function PoSlideCirclesComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "div", 0);
        ɵɵrepeaterCreate(1, PoSlideCirclesComponent_For_2_Template, 1, 2, "div", 1, ɵɵrepeaterTrackByIndex);
        ɵɵelementEnd();
      }
      if (rf & 2) {
        ɵɵadvance();
        ɵɵrepeater(ctx.items);
      }
    },
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoSlideCirclesComponent, [{
    type: Component,
    args: [{
      selector: "po-slide-circles",
      standalone: false,
      template: '<div class="po-slide-circles">\n  @for (item of items; track $index; let index = $index) {\n    <div\n      class="po-slide-circle"\n      [class.po-slide-active-circle]="index === currentSlideIndex"\n      (click)="click.emit(index)"\n    ></div>\n  }\n</div>\n'
    }]
  }], null, {
    currentSlideIndex: [{
      type: Input,
      args: ["p-current-slide-index"]
    }],
    items: [{
      type: Input,
      args: ["p-items"]
    }],
    click: [{
      type: Output,
      args: ["p-click"]
    }]
  });
})();
var poSlideIntervalDefault = 4e3;
var PoSlideBaseComponent = class _PoSlideBaseComponent {
  /**
   * @optional
   *
   * @description
   *
   * Define a exibição das setas de navegação.
   *
   * @default `false`
   */
  hideArrows = false;
  _interval = poSlideIntervalDefault;
  _height;
  _slides;
  /**
   * @optional
   *
   * @description
   * Altura do po-slide, caso seja slide com template customizado, não assume o valor `default`.
   *
   * @default `336`
   */
  set height(value) {
    this._height = convertToInt(value);
  }
  get height() {
    return this._height;
  }
  /**
   * @optional
   *
   * @description
   *
   * Valor em milissegundos que define o tempo de troca dos slides, caso o valor seja menor que `1000` os slides não trocam automaticamente.
   *
   * @default `4000`
   */
  set interval(value) {
    this._interval = convertToInt(value, poSlideIntervalDefault);
    this._interval >= 1e3 ? this.startInterval() : this.cancelInterval();
  }
  get interval() {
    return this._interval;
  }
  /**
   * @description
   *
   * Array de imagens ou dados para o slide, pode ser de três formas:
   *
   * - Array implementando objetos da interface `PoSlideItem`:
   *   ```
   *   [{ image: '/assets/image-1', action: 'imageClick.bind(this)'}, { image: '/assets/image-2' }]
   *   ```
   * - Array de `strings` com os caminhos das imagens:
   *   ```
   *   ['/assets/image-1', '/assets/image-2' ]
   *   ```
   * - Array com lista de itens (para utilizar template):
   *   ```
   *   [{ label: '1', img: '/assets/image-1' }, { label: '2', img: '/assets/image-1' }]
   *   ```
   *
   * > As setas de navegação e o controle com círculos apenas serão renderizados caso possua mais de um slide.
   */
  set slides(value) {
    this._slides = value;
    this.setSlideItems(value);
    if (value && value.length) {
      this.startSlide();
    }
  }
  get slides() {
    return this._slides;
  }
  static ɵfac = function PoSlideBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoSlideBaseComponent)();
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoSlideBaseComponent,
    inputs: {
      hideArrows: [2, "p-hide-arrows", "hideArrows", convertToBoolean],
      height: [0, "p-height", "height"],
      interval: [0, "p-interval", "interval"],
      slides: [0, "p-slides", "slides"]
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoSlideBaseComponent, [{
    type: Directive
  }], null, {
    hideArrows: [{
      type: Input,
      args: [{
        alias: "p-hide-arrows",
        transform: convertToBoolean
      }]
    }],
    height: [{
      type: Input,
      args: ["p-height"]
    }],
    interval: [{
      type: Input,
      args: ["p-interval"]
    }],
    slides: [{
      type: Input,
      args: ["p-slides"]
    }]
  });
})();
var PoSlideContentTemplateDirective = class _PoSlideContentTemplateDirective {
  templateRef;
  // Necessário manter templateRef para o funcionamento do row template.
  constructor(templateRef) {
    this.templateRef = templateRef;
  }
  static ɵfac = function PoSlideContentTemplateDirective_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoSlideContentTemplateDirective)(ɵɵdirectiveInject(TemplateRef));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoSlideContentTemplateDirective,
    selectors: [["", "p-slide-content-template", ""]],
    standalone: false
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoSlideContentTemplateDirective, [{
    type: Directive,
    args: [{
      selector: "[p-slide-content-template]",
      standalone: false
    }]
  }], () => [{
    type: TemplateRef
  }], null);
})();
var PoSlideItemComponent = class _PoSlideItemComponent {
  itemElement;
  /** Ação executada ao clicar em uma imagem */
  action;
  /** Texto alternativo quando a imagem não é encontrada */
  alt;
  /** Dados para o template customizado */
  data;
  /** Caminho da imagem */
  image;
  /** Altura da imagem */
  imageHeight;
  /** Template customizado */
  template;
  /** Link executado ao clicar em uma imagem */
  link;
  isIEOrEdge = isIEOrEdge();
  setLinkType() {
    if (!this.template && this.link) {
      return this.link.startsWith("http") ? "externalLink" : "internalLink";
    }
    return "noLink";
  }
  static ɵfac = function PoSlideItemComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoSlideItemComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoSlideItemComponent,
    selectors: [["po-slide-item"]],
    viewQuery: function PoSlideItemComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c164, 7);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.itemElement = _t.first);
      }
    },
    inputs: {
      action: [0, "p-action", "action"],
      alt: [0, "p-alt", "alt"],
      data: [0, "p-data", "data"],
      image: [0, "p-image", "image"],
      imageHeight: [0, "p-image-height", "imageHeight"],
      template: [0, "p-template", "template"],
      link: [0, "p-link", "link"]
    },
    standalone: false,
    decls: 9,
    vars: 5,
    consts: [["slideItem", ""], ["slideItemTemplate", ""], ["slideItemImage", ""], [1, "po-slide-item"], [1, "po-slide-item-link", 3, "routerLink"], [1, "po-slide-item-link", 3, "href"], [3, "ngClass"], [";", ""], [3, "ngTemplateOutlet", "ngTemplateOutletContext"], [3, "click", "ngClass"], [1, "po-slide-item-content"], [1, "po-slide-image", 3, "alt", "src", "height"], [1, "po-slide-image", 3, "alt", "src"]],
    template: function PoSlideItemComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "div", 3, 0);
        ɵɵconditionalCreate(2, PoSlideItemComponent_Case_2_Template, 2, 8, "a", 4)(3, PoSlideItemComponent_Case_3_Template, 2, 8, "a", 5)(4, PoSlideItemComponent_Case_4_Template, 2, 9, "a", 6);
        ɵɵelementEnd();
        ɵɵtemplate(5, PoSlideItemComponent_ng_template_5_Template, 1, 1, "ng-template", 7, 1, ɵɵtemplateRefExtractor)(7, PoSlideItemComponent_ng_template_7_Template, 1, 1, "ng-template", null, 2, ɵɵtemplateRefExtractor);
      }
      if (rf & 2) {
        let tmp_5_0;
        ɵɵstyleProp("background-image", !ctx.template && ctx.isIEOrEdge ? "url(" + ctx.image + ")" : "");
        ɵɵclassProp("po-slide-item-background-image", !ctx.template && ctx.isIEOrEdge);
        ɵɵadvance(2);
        ɵɵconditional((tmp_5_0 = ctx.setLinkType()) === "internalLink" ? 2 : tmp_5_0 === "externalLink" ? 3 : tmp_5_0 === "noLink" ? 4 : -1);
      }
    },
    dependencies: [NgClass, NgTemplateOutlet, RouterLink],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoSlideItemComponent, [{
    type: Component,
    args: [{
      selector: "po-slide-item",
      standalone: false,
      template: `<div
  #slideItem
  class="po-slide-item"
  [class.po-slide-item-background-image]="!template && isIEOrEdge"
  [style.background-image]="!template && isIEOrEdge ? 'url(' + image + ')' : ''"
>
  @switch (setLinkType()) {
    <!-- slide com link interno -->
    @case ('internalLink') {
      <a class="po-slide-item-link" [routerLink]="link">
        <ng-container
          [ngTemplateOutlet]="slideItemImage"
          [ngTemplateOutletContext]="{ item: { alt: alt, image: image } }"
        >
        </ng-container>
      </a>
    }
    <!-- slide com link externo -->
    @case ('externalLink') {
      <a class="po-slide-item-link" [href]="link">
        <ng-container
          [ngTemplateOutlet]="slideItemImage"
          [ngTemplateOutletContext]="{ item: { alt: alt, image: image } }"
        >
        </ng-container>
      </a>
    }
    <!-- slide sem link -->
    @case ('noLink') {
      <a
        [ngClass]="action ? 'po-slide-item-link' : 'po-slide-item-no-link'"
        (click)="action ? action(data) : undefined"
      >
        <ng-container
          [ngTemplateOutlet]="template ? slideItemTemplate : slideItemImage"
          [ngTemplateOutletContext]="{ item: { alt: alt, image: image, data: data } }"
        >
        </ng-container>
      </a>
    }
  }
</div>

<ng-template #slideItemTemplate let-item="item" ; let-index="index">
  @if (template) {
    <div class="po-slide-item-content">
      <ng-template
        [ngTemplateOutlet]="template.templateRef"
        [ngTemplateOutletContext]="{ $implicit: item.data, index: index }"
      >
      </ng-template>
    </div>
  }
</ng-template>

<ng-template #slideItemImage let-item="item">
  @if (item.image && !isIEOrEdge) {
    <img class="po-slide-image" [alt]="item.alt" [src]="item.image" [style.height.px]="imageHeight" />
  }
</ng-template>
`
    }]
  }], null, {
    itemElement: [{
      type: ViewChild,
      args: ["slideItem", {
        static: true
      }]
    }],
    action: [{
      type: Input,
      args: ["p-action"]
    }],
    alt: [{
      type: Input,
      args: ["p-alt"]
    }],
    data: [{
      type: Input,
      args: ["p-data"]
    }],
    image: [{
      type: Input,
      args: ["p-image"]
    }],
    imageHeight: [{
      type: Input,
      args: ["p-image-height"]
    }],
    template: [{
      type: Input,
      args: ["p-template"]
    }],
    link: [{
      type: Input,
      args: ["p-link"]
    }]
  });
})();
var PoSlideControlComponent = class _PoSlideControlComponent {
  /** Tipo de controle que aceita os valores: 'previous' e 'next'. */
  control;
  /** Evento emitido ao clicar em um controle. */
  click = new EventEmitter();
  static ɵfac = function PoSlideControlComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoSlideControlComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoSlideControlComponent,
    selectors: [["po-slide-control"]],
    inputs: {
      control: [0, "p-control", "control"]
    },
    outputs: {
      click: "p-click"
    },
    standalone: false,
    decls: 3,
    vars: 4,
    consts: [[1, "po-slide-arrow-circle", 3, "click"], [3, "p-icon"]],
    template: function PoSlideControlComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "div")(1, "div", 0);
        ɵɵlistener("click", function PoSlideControlComponent_Template_div_click_1_listener() {
          return ctx.click.emit();
        });
        ɵɵelement(2, "po-icon", 1);
        ɵɵelementEnd()();
      }
      if (rf & 2) {
        ɵɵclassMap(ɵɵinterpolate1("po-slide-control-", ctx.control));
        ɵɵadvance(2);
        ɵɵproperty("p-icon", ctx.control === "next" ? "ICON_ARROW_RIGHT" : "ICON_ARROW_LEFT");
      }
    },
    dependencies: [PoIconComponent],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoSlideControlComponent, [{
    type: Component,
    args: [{
      selector: "po-slide-control",
      standalone: false,
      template: `<div class="po-slide-control-{{ control }}">
  <div class="po-slide-arrow-circle" (click)="click.emit()">
    <po-icon [p-icon]="control === 'next' ? 'ICON_ARROW_RIGHT' : 'ICON_ARROW_LEFT'"></po-icon>
  </div>
</div>
`
    }]
  }], null, {
    control: [{
      type: Input,
      args: ["p-control"]
    }],
    click: [{
      type: Output,
      args: ["p-click"]
    }]
  });
})();
var poSlideDefaultHeight = 336;
var poSlideIntervalMin = 1e3;
var poSlideMinHeight = 192;
var poSlideTiming = "250ms ease";
var PoSlideComponent = class _PoSlideComponent extends PoSlideBaseComponent {
  builder = inject(AnimationBuilder);
  slideContentTemplate;
  slide;
  itemsElements;
  currentSlideIndex = 0;
  imageHeight;
  slideItems = [];
  slideItemWidth;
  isLoaded = false;
  player;
  setInterval;
  resize$ = new Subject();
  resizeSubscription;
  get hasElements() {
    return !!this.slide.nativeElement.offsetWidth && !!this.itemsElements && !!this.itemsElements.length;
  }
  get isImageSlide() {
    return !this.slideContentTemplate;
  }
  get offset() {
    return this.currentSlideIndex * this.slideItemWidth;
  }
  get hasSlides() {
    return !!this.slides && !!this.slides.length;
  }
  onResize() {
    this.resize$.next({});
  }
  ngOnInit() {
    this.resizeSubscription = this.resize$.pipe(debounceTime(150)).subscribe(() => {
      this.setSlideItemWidth();
      this.goToItem(this.currentSlideIndex);
    });
  }
  ngDoCheck() {
    if (!this.isLoaded && this.hasElements) {
      this.setSlideItemWidth();
      this.isLoaded = true;
      if (this.hasSlides) {
        this.startSlide();
      }
    }
  }
  ngOnChanges(changes) {
    if (changes.height) {
      this.setSlideHeight(this.height);
    }
  }
  ngOnDestroy() {
    this.resizeSubscription?.unsubscribe();
  }
  /**
   * Método que retorna o index do slide atual
   *
   * ```
   * @ViewChild('slideComponent', { static: true }) slideComponent: PoSlideComponent;
   *  myFunction() {
   *    let currentIndex = this.slideComponent.getCurrentSlideIndex();
   * }
   *
   * ```
   */
  getCurrentSlideIndex() {
    return this.currentSlideIndex;
  }
  goToItem(index) {
    if (this.interval > poSlideIntervalMin) {
      this.startInterval();
    }
    this.currentSlideIndex = index;
    this.animate(this.offset);
  }
  nextControl() {
    if (this.interval > poSlideIntervalMin) {
      this.startInterval();
    }
    this.next();
  }
  /**
   * Método para chamar o próximo slide.
   *
   * ```
   * @ViewChild('slideComponent', { static: true }) slideComponent: PoSlideComponent;
   *
   * myFunction() {
   *  this.slideComponent.next();
   * }
   * ```
   */
  next() {
    if (this.currentSlideIndex + 1 === this.slideItems.length) {
      this.currentSlideIndex = 0;
      this.animate(0);
      return;
    }
    this.currentSlideIndex = (this.currentSlideIndex + 1) % this.slideItems.length;
    this.animate(this.offset);
  }
  /**
   * Método para chamar o slide anterior.
   *
   * ```
   * @ViewChild('slideComponent', { static: true }) slideComponent: PoSlideComponent;
   *
   * myFunction() {
   *  this.slideComponent.previous();
   * }
   * ```
   */
  previous() {
    if (this.currentSlideIndex === 0) {
      this.currentSlideIndex = this.slideItems.length - 1;
      this.animate(this.offset);
      return;
    }
    this.currentSlideIndex = (this.currentSlideIndex - 1 + this.slideItems.length) % this.slideItems.length;
    this.animate(this.offset);
  }
  previousControl() {
    if (this.interval > poSlideIntervalMin) {
      this.startInterval();
    }
    this.previous();
  }
  setSlideHeight(height) {
    this.setHeight(height);
  }
  cancelInterval() {
    clearInterval(this.setInterval);
  }
  setSlideItems(slides) {
    if (this.hasSlides) {
      this.slideContentTemplate ? this.createArrayForTemplate(slides) : this.createArrayFromSlides(slides);
    } else {
      this.slideItems = [];
      this.cancelInterval();
    }
  }
  startSlide() {
    this.setSlideHeight(this.height);
    if (this.interval > poSlideIntervalMin) {
      this.startInterval();
    }
    this.currentSlideIndex = 0;
  }
  startInterval() {
    if (this.setInterval) {
      this.cancelInterval();
    }
    this.setInterval = this.hasSlides && this.hasElements ? setInterval(() => {
      this.next();
    }, this.interval) : void 0;
  }
  animate(offset) {
    if (this.hasElements) {
      const animation2 = this.buildTransitionAnimation(offset);
      this.player = animation2.create(this.slide.nativeElement);
      this.player.play();
    }
  }
  buildTransitionAnimation(offset) {
    return this.builder.build([animate(poSlideTiming, keyframes([style({
      transform: `translateX(-${offset}px)`
    })]))]);
  }
  createArrayForTemplate(slides) {
    this.slideItems = [...slides];
  }
  createArrayFromSlides(slides) {
    const isStringArray = slides.every((item) => typeof item === "string");
    if (isStringArray) {
      slides.forEach((element) => this.slideItems.push({
        image: `${element}`
      }));
    } else {
      this.slideItems = [...slides];
    }
  }
  setDefaultHeight(height) {
    if (height && height <= poSlideMinHeight || !height && this.isImageSlide) {
      this.slide.nativeElement.style.height = `${poSlideDefaultHeight}px`;
      this.imageHeight = poSlideDefaultHeight;
    } else {
      this.imageHeight = void 0;
    }
  }
  setHeight(height) {
    if (height && height > poSlideMinHeight) {
      this.slide.nativeElement.style.height = `${height}px`;
      this.imageHeight = height;
    } else {
      this.setDefaultHeight(height);
    }
  }
  setSlideItemWidth() {
    if (this.hasElements) {
      this.slideItemWidth = this.itemsElements.first.itemElement.nativeElement.getBoundingClientRect().width;
    }
  }
  static ɵfac = /* @__PURE__ */ (() => {
    let ɵPoSlideComponent_BaseFactory;
    return function PoSlideComponent_Factory(__ngFactoryType__) {
      return (ɵPoSlideComponent_BaseFactory || (ɵPoSlideComponent_BaseFactory = ɵɵgetInheritedFactory(_PoSlideComponent)))(__ngFactoryType__ || _PoSlideComponent);
    };
  })();
  static ɵcmp = ɵɵdefineComponent({
    type: _PoSlideComponent,
    selectors: [["po-slide"]],
    contentQueries: function PoSlideComponent_ContentQueries(rf, ctx, dirIndex) {
      if (rf & 1) {
        ɵɵcontentQuery(dirIndex, PoSlideContentTemplateDirective, 7);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.slideContentTemplate = _t.first);
      }
    },
    viewQuery: function PoSlideComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c168, 7)(PoSlideItemComponent, 5);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.slide = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.itemsElements = _t);
      }
    },
    hostBindings: function PoSlideComponent_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("resize", function PoSlideComponent_resize_HostBindingHandler() {
          return ctx.onResize();
        }, ɵɵresolveWindow);
      }
    },
    standalone: false,
    features: [ɵɵInheritDefinitionFeature, ɵɵNgOnChangesFeature],
    decls: 9,
    vars: 3,
    consts: [["slide", ""], [1, "po-slide-wrapper"], [1, "po-slide-outer"], [1, "po-slide-inner"], [3, "p-action", "p-data", "p-image", "p-image-height", "p-template", "p-alt", "p-link"], ["p-control", "previous"], ["p-control", "next"], [3, "p-current-slide-index", "p-items"], ["p-control", "previous", 3, "p-click"], ["p-control", "next", 3, "p-click"], [3, "p-click", "p-current-slide-index", "p-items"]],
    template: function PoSlideComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "div", 1)(1, "div", 2)(2, "div", 3, 0);
        ɵɵrepeaterCreate(4, PoSlideComponent_For_5_Template, 1, 7, "po-slide-item", 4, ɵɵrepeaterTrackByIndex);
        ɵɵelementEnd();
        ɵɵconditionalCreate(6, PoSlideComponent_Conditional_6_Template, 1, 0, "po-slide-control", 5);
        ɵɵconditionalCreate(7, PoSlideComponent_Conditional_7_Template, 1, 0, "po-slide-control", 6);
        ɵɵelementEnd();
        ɵɵconditionalCreate(8, PoSlideComponent_Conditional_8_Template, 1, 2, "po-slide-circles", 7);
        ɵɵelementEnd();
      }
      if (rf & 2) {
        ɵɵadvance(4);
        ɵɵrepeater(ctx.slideItems);
        ɵɵadvance(2);
        ɵɵconditional(!ctx.hideArrows && ctx.hasSlides && ctx.slides.length > 1 ? 6 : -1);
        ɵɵadvance();
        ɵɵconditional(!ctx.hideArrows && ctx.hasSlides && ctx.slides.length > 1 ? 7 : -1);
        ɵɵadvance();
        ɵɵconditional(ctx.hasSlides && ctx.slides.length > 1 ? 8 : -1);
      }
    },
    dependencies: [PoSlideCirclesComponent, PoSlideControlComponent, PoSlideItemComponent],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoSlideComponent, [{
    type: Component,
    args: [{
      selector: "po-slide",
      standalone: false,
      template: '<div class="po-slide-wrapper">\n  <div class="po-slide-outer">\n    <div class="po-slide-inner" #slide>\n      @for (item of slideItems; track $index; let index = $index) {\n        <po-slide-item\n          [p-action]="item.action"\n          [p-data]="item"\n          [p-image]="item.image"\n          [p-image-height]="imageHeight"\n          [p-template]="slideContentTemplate"\n          [p-alt]="item.alt"\n          [p-link]="item.link"\n        >\n        </po-slide-item>\n      }\n    </div>\n\n    @if (!hideArrows && hasSlides && slides.length > 1) {\n      <po-slide-control p-control="previous" (p-click)="previousControl()"> </po-slide-control>\n    }\n\n    @if (!hideArrows && hasSlides && slides.length > 1) {\n      <po-slide-control p-control="next" (p-click)="nextControl()"> </po-slide-control>\n    }\n  </div>\n\n  @if (hasSlides && slides.length > 1) {\n    <po-slide-circles [p-current-slide-index]="currentSlideIndex" [p-items]="slideItems" (p-click)="goToItem($event)">\n    </po-slide-circles>\n  }\n</div>\n'
    }]
  }], null, {
    slideContentTemplate: [{
      type: ContentChild,
      args: [PoSlideContentTemplateDirective, {
        static: true
      }]
    }],
    slide: [{
      type: ViewChild,
      args: ["slide", {
        static: true
      }]
    }],
    itemsElements: [{
      type: ViewChildren,
      args: [PoSlideItemComponent]
    }],
    onResize: [{
      type: HostListener,
      args: ["window:resize"]
    }]
  });
})();
var PoSlideModule = class _PoSlideModule {
  static ɵfac = function PoSlideModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoSlideModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoSlideModule,
    declarations: [PoSlideCirclesComponent, PoSlideComponent, PoSlideControlComponent, PoSlideContentTemplateDirective, PoSlideItemComponent],
    imports: [CommonModule, RouterModule, PoContainerModule, PoIconModule],
    exports: [PoSlideComponent, PoSlideContentTemplateDirective]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [CommonModule, RouterModule, PoContainerModule, PoIconModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoSlideModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, RouterModule, PoContainerModule, PoIconModule],
      declarations: [PoSlideCirclesComponent, PoSlideComponent, PoSlideControlComponent, PoSlideContentTemplateDirective, PoSlideItemComponent],
      exports: [PoSlideComponent, PoSlideContentTemplateDirective],
      providers: [],
      schemas: []
    }]
  }], null, null);
})();
var PoStepperStatus;
(function(PoStepperStatus2) {
  PoStepperStatus2["Active"] = "active";
  PoStepperStatus2["Default"] = "default";
  PoStepperStatus2["Disabled"] = "disabled";
  PoStepperStatus2["Done"] = "done";
  PoStepperStatus2["Error"] = "error";
})(PoStepperStatus || (PoStepperStatus = {}));
var PoStepComponent = class _PoStepComponent {
  elementRef = inject(ElementRef);
  /**
   * @optional
   *
   * @description
   *
   * Função chamada quando o próximo *step* for clicado ou quando o método `PoStepperComponent.next()` for chamado.
   * Ao retornar `true` define que esse *step* ficará ativo e o atual como concluído (*done*).
   * Também aceita funções que retornem `Observable<boolean>`. Ao retornar um `Observable<boolean>`,
   * garanta que esse `Observable` será completado, caso houver algum erro durante o processo não será possível prosseguir
   * ao próximo *step*.
   *
   * Ao ser disparada, a mesma receberá por parâmetro o `PoStepComponent` atual.
   *
   * O contexto da função que será chamada, será o mesmo que o do `PoStepComponent`, então para poder alterar
   * para o contexto do componente que o está utilizando, pode ser utilizado a propriedade `bind` do Javascript.
   * Por exemplo, para a função `validate`:
   *
   * ```
   * <po-step p-label="Step 1" [p-can-active-next-step]="validate.bind(this)">
   * ...
   * </po-step>
   * ```
   */
  canActiveNextStep;
  /** Título que será exibido descrevendo o passo (*step*). */
  label = "";
  // ID do step
  id = uuid();
  _status = PoStepperStatus.Disabled;
  // Estado de exibição do `po-step`.
  set status(status) {
    this._status = status;
    this.setDisplayOnActiveOrError();
  }
  get status() {
    return this._status;
  }
  /**
   * @optional
   *
   * @description
   * Define o ícone padrão do step em seu status *default*.
   * Esta propriedade permite usar ícones da [Biblioteca de ícones](https://po-ui.io/icons).
   * ```
   * <po-stepper>
   *    ...
   *    <po-step p-icon-default="an an-map-pin"></po-step>
   * </po-stepper>
   * ```
   * Outra opção seria a customização do ícone através do `TemplateRef`, conforme exemplo abaixo:
   * ```
   * <po-stepper>
   *    ...
   *    <po-step [p-icon-default]="template"></po-step>
   * </po-stepper>
   *
   * <ng-template #template>
   *    <i class="an an-shopping-cart"></i>
   * </ng-template
   * ```
   * > Deve-se usar `font-size: inherit` para ajustar ícones que não se ajustam automaticamente.
   */
  iconDefault;
  ngAfterContentInit() {
    this.setDisplayOnActiveOrError();
  }
  setDisplayOnActiveOrError() {
    this.elementRef.nativeElement.style.display = this.status === PoStepperStatus.Active || this.status === PoStepperStatus.Error ? "" : "none";
  }
  static ɵfac = function PoStepComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoStepComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoStepComponent,
    selectors: [["po-step"]],
    inputs: {
      canActiveNextStep: [0, "p-can-active-next-step", "canActiveNextStep"],
      label: [0, "p-label", "label"],
      status: [0, "p-status", "status"],
      iconDefault: [0, "p-icon-default", "iconDefault"]
    },
    standalone: false,
    ngContentSelectors: _c12,
    decls: 1,
    vars: 0,
    template: function PoStepComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵprojectionDef();
        ɵɵprojection(0);
      }
    },
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoStepComponent, [{
    type: Component,
    args: [{
      selector: "po-step",
      standalone: false,
      template: "<ng-content></ng-content>\n"
    }]
  }], null, {
    canActiveNextStep: [{
      type: Input,
      args: ["p-can-active-next-step"]
    }],
    label: [{
      type: Input,
      args: ["p-label"]
    }],
    status: [{
      type: Input,
      args: ["p-status"]
    }],
    iconDefault: [{
      type: Input,
      args: ["p-icon-default"]
    }]
  });
})();
var poMediumStepSize = 32;
var poLargeStepSize = 48;
var PoStepperCircleComponent = class _PoStepperCircleComponent {
  // Alinhamento do *step*.
  alignCenter;
  // Conteúdo que irá aparecer no círculo do *step*.
  content;
  // Ícone para o status Active do *step*.
  iconActive;
  // Ícone para o status Done do *step*.
  iconDone;
  // Ícone para o status default do *step*.
  iconDefault;
  // Define se serão exibidos ícones no lugar de números nos steps.
  icons;
  // Tamanho do *step-circle*.
  size;
  // Status do *step*.
  status;
  get isActive() {
    return this.status === PoStepperStatus.Active;
  }
  get isDefault() {
    return this.status === PoStepperStatus.Default;
  }
  get isDisabled() {
    return this.status === PoStepperStatus.Disabled;
  }
  get isDone() {
    return this.status === PoStepperStatus.Done;
  }
  get isError() {
    return this.status === PoStepperStatus.Error;
  }
  get isLargeStep() {
    return this.size >= poLargeStepSize;
  }
  get isMediumStep() {
    return this.size >= poMediumStepSize && !this.isLargeStep;
  }
  static ɵfac = function PoStepperCircleComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoStepperCircleComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoStepperCircleComponent,
    selectors: [["po-stepper-circle"]],
    inputs: {
      alignCenter: [0, "p-align-center", "alignCenter"],
      content: [0, "p-content", "content"],
      iconActive: [0, "p-step-icon-active", "iconActive"],
      iconDone: [0, "p-step-icon-done", "iconDone"],
      iconDefault: [0, "p-icon-default", "iconDefault"],
      icons: [0, "p-icons", "icons"],
      size: [0, "p-size", "size"],
      status: [0, "p-status", "status"]
    },
    standalone: false,
    decls: 3,
    vars: 11,
    consts: [[1, "po-stepper-circle", 3, "ngClass"], [1, "po-stepper-circle-active"], [1, "po-stepper-circle-content", 3, "p-icon"], [1, "po-stepper-circle-content", 3, "p-icon", "po-stepper-circle-content-lg", "po-stepper-circle-content-md"]],
    template: function PoStepperCircleComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "div", 0);
        ɵɵconditionalCreate(1, PoStepperCircleComponent_Conditional_1_Template, 2, 5, "div", 1)(2, PoStepperCircleComponent_Conditional_2_Template, 1, 1);
        ɵɵelementEnd();
      }
      if (rf & 2) {
        ɵɵstyleProp("height", ctx.size, "px")("width", ctx.size, "px");
        ɵɵclassProp("po-stepper-circle-center", ctx.alignCenter);
        ɵɵproperty("ngClass", ɵɵpureFunction2(8, _c169, !ctx.isActive && !ctx.isError, ctx.isDone));
        ɵɵadvance();
        ɵɵconditional(ctx.isActive || ctx.isError ? 1 : 2);
      }
    },
    dependencies: [NgClass, PoIconComponent],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoStepperCircleComponent, [{
    type: Component,
    args: [{
      selector: "po-stepper-circle",
      standalone: false,
      template: `<div
  class="po-stepper-circle"
  [class.po-stepper-circle-center]="alignCenter"
  [ngClass]="{ 'po-stepper-circle-border': !isActive && !isError, 'po-stepper-circle-done': isDone }"
  [style.height.px]="size"
  [style.width.px]="size"
>
  @if (isActive || isError) {
    <div class="po-stepper-circle-active">
      <po-icon
        class="po-stepper-circle-content"
        [p-icon]="iconActive ? iconActive : 'ICON_EDIT'"
        [class.po-stepper-circle-content-lg]="isLargeStep"
        [class.po-stepper-circle-content-md]="isMediumStep"
      ></po-icon>
    </div>
  } @else {
    @if (!isActive) {
      <po-icon
        class="po-stepper-circle-content"
        [p-icon]="isDone ? iconDone || 'ICON_OK' : iconDefault ? iconDefault : icons ? 'ICON_INFO' : ''"
        [class.po-stepper-circle-content-lg]="isLargeStep"
        [class.po-stepper-circle-content-md]="isMediumStep"
      >
        {{ !icons && !isDone && !iconDefault ? content : '' }}
      </po-icon>
    }
  }
</div>
`
    }]
  }], null, {
    alignCenter: [{
      type: Input,
      args: ["p-align-center"]
    }],
    content: [{
      type: Input,
      args: ["p-content"]
    }],
    iconActive: [{
      type: Input,
      args: ["p-step-icon-active"]
    }],
    iconDone: [{
      type: Input,
      args: ["p-step-icon-done"]
    }],
    iconDefault: [{
      type: Input,
      args: ["p-icon-default"]
    }],
    icons: [{
      type: Input,
      args: ["p-icons"]
    }],
    size: [{
      type: Input,
      args: ["p-size"]
    }],
    status: [{
      type: Input,
      args: ["p-status"]
    }]
  });
})();
var PoStepperOrientation;
(function(PoStepperOrientation2) {
  PoStepperOrientation2["Horizontal"] = "horizontal";
  PoStepperOrientation2["Vertical"] = "vertical";
})(PoStepperOrientation || (PoStepperOrientation = {}));
var poStepperOrientationDefault = PoStepperOrientation.Horizontal;
var PoStepperBaseComponent = class _PoStepperBaseComponent {
  /**
   * @optional
   *
   * @description
   *
   * <a id="stepIconsProperty"></a>
   *
   * Habilita a exibição de ícone ao invés de número no centro do círculo dos *steps*.
   *
   * @default `false`
   */
  stepIcons;
  /**
   * @optional
   *
   * @description
   *
   * Define o tamanho dos *steps* em *pixels*, possibilitando um maior destaque.
   *
   * O valor informado deve ser entre `24` e `64`.
   *
   * > Valores que não se enquadrarem a esta regra serão ignorados, mantendo-se o valor *default*.
   *
   * @default `24`
   */
  stepSize;
  /** Ação que será executada quando o usuário mudar o passo do `po-stepper`. */
  onChangeStep = new EventEmitter();
  _alignCenter = true;
  _orientation = poStepperOrientationDefault;
  _sequential = true;
  _step = 1;
  _steps = [];
  /**
   * @optional
   *
   * @description
   *
   * Define o alinhamento dos *steps* e *labels* no *stepper*, dependendo da orientação.
   *
   * - Quando `true`, ficam centralizados em ambas as orientações (horizontal e vertical).
   * - Quando `false`, ficam alinhados à esquerda na orientação horizontal e ao topo na orientação vertical.
   *
   * @default `true`
   */
  set alignCenter(alignCenter) {
    this._alignCenter = convertToBoolean(alignCenter);
  }
  get alignCenter() {
    return this._alignCenter;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define a orientação de exibição do `po-stepper`.
   *
   * > Veja os valores válidos no *enum* [PoStepperOrientation](documentation/po-stepper#stepperOrientation).
   *
   * @default `PoStepperOrientation.Horizontal`
   */
  set orientation(value) {
    this._orientation = Object.values(PoStepperOrientation).includes(value) ? value : poStepperOrientationDefault;
  }
  get orientation() {
    return this._orientation;
  }
  /**
   * @optional
   *
   * @description
   *
   * Controla o passo atual do `po-stepper`.
   *
   * > Ao utilizar esta propriedade e também utilizar o componente [**po-step**](/documentation/po-step),
   * o valor desta propriedade será ignorada permanecendo a definição do [**po-step**](/documentation/po-step).
   *
   * @default `1`
   */
  set step(step) {
    if (step >= 1 && step <= this.steps.length) {
      this._step = step;
      this._steps[this._step - 1].status = PoStepperStatus.Active;
    }
  }
  get step() {
    return this._step;
  }
  /**
   * @optional
   *
   * @description
   *
   * Lista dos itens do stepper. Se o valor estiver indefinido ou inválido, será inicializado como um array vazio.
   *
   * > Ao utilizar esta propriedade e também utilizar o componente [**po-step**](/documentation/po-step),
   * o valor desta propriedade será ignorada permanecendo a definição do [**po-step**](/documentation/po-step).
   */
  set steps(steps) {
    this._steps = Array.isArray(steps) ? steps : [];
    this._steps.forEach((step) => {
      step.status = step.status ?? PoStepperStatus.Default;
      step.id = step.id ?? uuid();
    });
    this.initializeSteps();
  }
  get steps() {
    return this._steps;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define se o `po-stepper` será sequencial ou aleatório.
   *
   * > Ao utilizar o componente [**po-step**](/documentation/po-step), o valor desta propriedade sempre será verdadeiro.
   *
   * @default `true`
   */
  set sequential(sequential) {
    this._sequential = convertToBoolean(sequential);
  }
  get sequential() {
    return this._sequential;
  }
  /**
   * @optional
   *
   * @description
   * Permite definir o ícone do step no status concluído.
   * Esta propriedade permite usar ícones da [Biblioteca de ícones](https://po-ui.io/icons)
   * ```
   * <po-stepper p-step-icon-done="an an-check-circle">
   *    ...
   * </po-stepper>
   * ```
   * Outra opção seria a customização do ícone através do `TemplateRef`, conforme exemplo abaixo:
   * ```
   * <po-stepper [p-step-icon-done]="doneIcon">
   *    ...
   * </po-stepper>
   *
   * <ng-template #doneIcon>
   *    <i class="an an-check-fat"></i>
   * </ng-template>
   * ```
   * > Deve-se usar `font-size: inherit` para ajustar ícones que não se ajustam automaticamente.
   *
   * @default `po-icon-ok`
   */
  iconDone;
  /**
   * @optional
   *
   * @description
   * Permite definir o ícone do step no status ativo.
   * Esta propriedade permite usar ícones da [Biblioteca de ícones](https://po-ui.io/icons).
   * ```
   * <po-stepper p-step-icon-active="an an-pencil-simple-line">
   *    ...
   * </po-stepper>
   * ```
   * Para customizar o ícone através do `TemplateRef`, veja a documentação da propriedade `p-step-icon-done`.
   *
   * > Deve-se usar `font-size: inherit` para ajustar ícones que não se ajustam automaticamente.
   *
   * @default `po-icon-edit`
   */
  iconActive;
  /**
   * @optional
   *
   * @description
   *
   * Desabilita o clique nos steps.
   *
   * @default `false`
   */
  disabledClick = false;
  initializeSteps() {
    const hasStatus = this._steps.some((step) => step.status !== PoStepperStatus.Default);
    if (!hasStatus && this.step === 1) {
      this.step = 1;
    }
  }
  static ɵfac = function PoStepperBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoStepperBaseComponent)();
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoStepperBaseComponent,
    inputs: {
      stepIcons: [0, "p-step-icons", "stepIcons"],
      stepSize: [0, "p-step-size", "stepSize"],
      alignCenter: [0, "p-align-center", "alignCenter"],
      orientation: [0, "p-orientation", "orientation"],
      step: [0, "p-step", "step"],
      steps: [0, "p-steps", "steps"],
      sequential: [0, "p-sequential", "sequential"],
      iconDone: [0, "p-step-icon-done", "iconDone"],
      iconActive: [0, "p-step-icon-active", "iconActive"],
      disabledClick: [0, "p-disable-click", "disabledClick"]
    },
    outputs: {
      onChangeStep: "p-change-step"
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoStepperBaseComponent, [{
    type: Directive
  }], null, {
    stepIcons: [{
      type: Input,
      args: ["p-step-icons"]
    }],
    stepSize: [{
      type: Input,
      args: ["p-step-size"]
    }],
    onChangeStep: [{
      type: Output,
      args: ["p-change-step"]
    }],
    alignCenter: [{
      type: Input,
      args: ["p-align-center"]
    }],
    orientation: [{
      type: Input,
      args: ["p-orientation"]
    }],
    step: [{
      type: Input,
      args: ["p-step"]
    }],
    steps: [{
      type: Input,
      args: ["p-steps"]
    }],
    sequential: [{
      type: Input,
      args: ["p-sequential"]
    }],
    iconDone: [{
      type: Input,
      args: ["p-step-icon-done"]
    }],
    iconActive: [{
      type: Input,
      args: ["p-step-icon-active"]
    }],
    disabledClick: [{
      type: Input,
      args: ["p-disable-click"]
    }]
  });
})();
var PoStepperLabelComponent = class _PoStepperLabelComponent {
  renderer = inject(Renderer2);
  changeDetectorRef = inject(ChangeDetectorRef);
  // Alinhamento da label.
  alignCenter;
  // Conteúdo da label.
  content;
  // Informa o status da etapa.
  status;
  // Informa se a orientação do stepper é vertical.
  isVerticalOrientation;
  labelElement;
  disabledClick = false;
  displayedContent;
  tooltipContent;
  maxLabelLength = 100;
  ngAfterViewInit() {
    this.updateLabel();
    this.changeDetectorRef.detectChanges();
  }
  ngOnChanges(changes) {
    if (changes["content"] || changes["isVerticalOrientation"]) {
      this.updateLabel();
      this.changeDetectorRef.detectChanges();
    }
  }
  onMouseOver() {
    this.updateTooltip();
    this.renderer.addClass(this.labelElement.nativeElement, "hovered");
  }
  onMouseOut() {
    this.renderer.removeClass(this.labelElement.nativeElement, "hovered");
  }
  updateLabel() {
    if (!this.labelElement) return;
    const originalContent = this.content;
    let displayedContent = originalContent;
    if (this.isVerticalOrientation && originalContent.length > this.maxLabelLength) {
      displayedContent = originalContent.substring(0, this.maxLabelLength) + "...";
    }
    this.displayedContent = displayedContent;
    this.updateTooltip();
  }
  updateTooltip() {
    if (this.labelElement) {
      const element = this.labelElement.nativeElement;
      const isTextOverflowing = element.scrollWidth > element.clientWidth;
      const isTextTooLong = this.isVerticalOrientation && this.content.length > this.maxLabelLength;
      this.tooltipContent = isTextOverflowing || isTextTooLong ? this.content : null;
    }
  }
  static ɵfac = function PoStepperLabelComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoStepperLabelComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoStepperLabelComponent,
    selectors: [["po-stepper-label"]],
    viewQuery: function PoStepperLabelComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c170, 5);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.labelElement = _t.first);
      }
    },
    hostBindings: function PoStepperLabelComponent_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("mouseover", function PoStepperLabelComponent_mouseover_HostBindingHandler() {
          return ctx.onMouseOver();
        })("mouseout", function PoStepperLabelComponent_mouseout_HostBindingHandler() {
          return ctx.onMouseOut();
        });
      }
    },
    inputs: {
      alignCenter: [0, "p-align-center", "alignCenter"],
      content: [0, "p-content", "content"],
      status: [0, "p-status", "status"],
      isVerticalOrientation: [0, "p-vertical-orientation", "isVerticalOrientation"],
      disabledClick: [0, "p-disable-click", "disabledClick"]
    },
    standalone: false,
    features: [ɵɵNgOnChangesFeature],
    decls: 3,
    vars: 14,
    consts: [["labelElement", ""], ["aria-hidden", "true", 1, "po-stepper-label", 3, "mouseover", "mouseout", "p-tooltip"]],
    template: function PoStepperLabelComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "div", 1, 0);
        ɵɵlistener("mouseover", function PoStepperLabelComponent_Template_div_mouseover_0_listener() {
          return ctx.onMouseOver();
        })("mouseout", function PoStepperLabelComponent_Template_div_mouseout_0_listener() {
          return ctx.onMouseOut();
        });
        ɵɵtext(2);
        ɵɵelementEnd();
      }
      if (rf & 2) {
        ɵɵclassProp("po-link", ctx.status !== "disabled" && !ctx.disabledClick)("po-stepper-disabled-click", ctx.disabledClick)("po-stepper-label-active", ctx.status === "active" || ctx.status === "error")("po-stepper-label-done", ctx.status === "done")("po-stepper-label-vertical", ctx.isVerticalOrientation)("po-stepper-label-center", ctx.alignCenter);
        ɵɵproperty("p-tooltip", ctx.tooltipContent);
        ɵɵadvance(2);
        ɵɵtextInterpolate1(" ", ctx.displayedContent, "\n");
      }
    },
    dependencies: [PoTooltipDirective],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoStepperLabelComponent, [{
    type: Component,
    args: [{
      selector: "po-stepper-label",
      standalone: false,
      template: `<div
  #labelElement
  class="po-stepper-label"
  [class.po-link]="status !== 'disabled' && !this.disabledClick"
  [class.po-stepper-disabled-click]="this.disabledClick"
  [class.po-stepper-label-active]="status === 'active' || status === 'error'"
  [class.po-stepper-label-done]="status === 'done'"
  [class.po-stepper-label-vertical]="isVerticalOrientation"
  [class.po-stepper-label-center]="alignCenter"
  [p-tooltip]="tooltipContent"
  aria-hidden="true"
  (mouseover)="onMouseOver()"
  (mouseout)="onMouseOut()"
>
  {{ displayedContent }}
</div>
`
    }]
  }], null, {
    alignCenter: [{
      type: Input,
      args: ["p-align-center"]
    }],
    content: [{
      type: Input,
      args: ["p-content"]
    }],
    status: [{
      type: Input,
      args: ["p-status"]
    }],
    isVerticalOrientation: [{
      type: Input,
      args: ["p-vertical-orientation"]
    }],
    labelElement: [{
      type: ViewChild,
      args: ["labelElement"]
    }],
    disabledClick: [{
      type: Input,
      args: ["p-disable-click"]
    }],
    onMouseOver: [{
      type: HostListener,
      args: ["mouseover"]
    }],
    onMouseOut: [{
      type: HostListener,
      args: ["mouseout"]
    }]
  });
})();
var poStepperStepSizeDefault = 24;
var poStepperStepSizeMax = 64;
var poStepLiteralsDefault = {
  en: {
    label: "Step"
  },
  es: {
    label: "Paso"
  },
  pt: {
    label: "Passo"
  }
};
var PoStepperStepComponent = class _PoStepperStepComponent {
  // Alinhamento do *step* e da label.
  alignCenter;
  // Conteúdo que será repassado para o componente `p-circle-content` através da propriedade `p-content`.
  circleContent;
  // Informa o status da próxima etapa.
  nextStatus;
  // Define a orientação de exibição.
  orientation;
  // Label do *step*.
  set label(value) {
    this._label = isTypeof(value, "string") ? value : `${this.literals.label} ${this.circleContent}`;
  }
  get label() {
    return this._label;
  }
  // Define o estado de exibição.
  set status(value) {
    this._status = value ? value : void 0;
    if (this.status === PoStepperStatus.Active) {
      this.activated.emit();
    }
  }
  get status() {
    return this._status;
  }
  set stepIcons(stepIcons) {
    this._stepIcons = convertToBoolean(stepIcons);
  }
  get stepIcons() {
    return this._stepIcons;
  }
  // Tamanho do `step` em *pixels*, possibilitando um maior destaque.
  // O valor informado deve ser entre `24` e `64`.
  set stepSize(value) {
    this._stepSize = value >= poStepperStepSizeDefault && value <= poStepperStepSizeMax ? value : poStepperStepSizeDefault;
  }
  get stepSize() {
    return this._stepSize;
  }
  set iconDefault(value) {
    this._iconDefault = value;
  }
  get iconDefault() {
    return this._iconDefault;
  }
  set iconDone(value) {
    this._iconDone = value;
  }
  get iconDone() {
    return this._iconDone;
  }
  set iconActive(value) {
    this._iconActive = value;
  }
  get iconActive() {
    return this._iconActive;
  }
  // Informa se a orientação do stepper é vertical.
  isVerticalOrientation;
  // Evento que será emitido quando o status do *step* estiver ativo (`PoStepperStatus.Active`).
  activated = new EventEmitter();
  // Evento que será emitido ao clicar no *step*.
  click = new EventEmitter();
  // Evento que será emitido ao focar no *step* e pressionar a tecla *enter*.
  enter = new EventEmitter();
  disabledClick = false;
  literals = __spreadValues(__spreadValues({}, poStepLiteralsDefault[poLocaleDefault]), poStepLiteralsDefault[getShortBrowserLanguage()]);
  stepSizeOriginal;
  _label;
  _status;
  _stepIcons = false;
  _stepSize = poStepperStepSizeDefault;
  _iconDefault;
  _iconDone;
  _iconActive;
  get minHeightCircle() {
    if (this.stepSize === 24) {
      return 32;
    }
    return this.isVerticalOrientation ? Math.max(this.stepSize, 24) + 8 : null;
  }
  get minWidthCircle() {
    if (this.isVerticalOrientation && this.stepSize === 24) {
      return 32;
    }
    return null;
  }
  ngOnChanges(changes) {
    if (this.stepSizeOriginal === void 0 || changes["stepSize"]) {
      this.stepSizeOriginal = this._stepSize;
    }
    if (changes["status"] || changes["stepSize"]) {
      this.setDefaultStepSize();
    }
  }
  getStatusClass(status) {
    switch (status) {
      case PoStepperStatus.Active:
        return "po-stepper-step-default";
      case PoStepperStatus.Disabled:
        return "po-stepper-step-disabled";
      case PoStepperStatus.Done:
        return "po-stepper-step-default";
      case PoStepperStatus.Error:
        return "po-stepper-step-error";
      default:
        return "po-stepper-step-default";
    }
  }
  onClick() {
    if (this.status !== PoStepperStatus.Disabled && !this.disabledClick) {
      this.click.emit();
    }
  }
  onEnter(event) {
    if (this.status !== PoStepperStatus.Disabled && !this.disabledClick) {
      event?.preventDefault();
      this.enter.emit();
    }
  }
  setDefaultStepSize() {
    if (this._stepSize === poStepperStepSizeDefault && this._status === PoStepperStatus.Active) {
      this._stepSize = poStepperStepSizeDefault + 8;
    } else if (this.stepSizeOriginal === poStepperStepSizeDefault && (this._status === PoStepperStatus.Error || this._status === PoStepperStatus.Active)) {
      this._stepSize = poStepperStepSizeDefault + 8;
    } else {
      this._stepSize = this.stepSizeOriginal;
    }
  }
  static ɵfac = function PoStepperStepComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoStepperStepComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoStepperStepComponent,
    selectors: [["po-stepper-step"]],
    inputs: {
      alignCenter: [0, "p-align-center", "alignCenter"],
      circleContent: [0, "p-circle-content", "circleContent"],
      nextStatus: [0, "p-next-status", "nextStatus"],
      orientation: [0, "p-orientation", "orientation"],
      label: [0, "p-label", "label"],
      status: [0, "p-status", "status"],
      stepIcons: [0, "p-step-icons", "stepIcons"],
      stepSize: [0, "p-step-size", "stepSize"],
      iconDefault: [0, "p-icon-default", "iconDefault"],
      iconDone: [0, "p-step-icon-done", "iconDone"],
      iconActive: [0, "p-step-icon-active", "iconActive"],
      isVerticalOrientation: [0, "p-vertical-orientation", "isVerticalOrientation"],
      disabledClick: [0, "p-disable-click", "disabledClick"]
    },
    outputs: {
      activated: "p-activated",
      click: "p-click",
      enter: "p-enter"
    },
    standalone: false,
    features: [ɵɵNgOnChangesFeature],
    decls: 4,
    vars: 27,
    consts: [["role", "tab", 1, "po-stepper-step", 3, "click", "keydown.enter", "keydown.space", "ngClass", "tabindex"], [1, "po-stepper-step-container"], [3, "p-align-center", "p-content", "p-icons", "p-size", "p-status", "p-icon-default", "p-step-icon-done", "p-step-icon-active"], [1, "po-stepper-step-label-position", 3, "p-align-center", "p-content", "p-status", "p-vertical-orientation", "p-disable-click"]],
    template: function PoStepperStepComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "div", 0);
        ɵɵlistener("click", function PoStepperStepComponent_Template_div_click_0_listener() {
          return ctx.onClick();
        })("keydown.enter", function PoStepperStepComponent_Template_div_keydown_enter_0_listener($event) {
          return ctx.onEnter($event);
        })("keydown.space", function PoStepperStepComponent_Template_div_keydown_space_0_listener($event) {
          return ctx.onEnter($event);
        });
        ɵɵelementStart(1, "div", 1);
        ɵɵelement(2, "po-stepper-circle", 2);
        ɵɵconditionalCreate(3, PoStepperStepComponent_Conditional_3_Template, 1, 6, "po-stepper-label", 3);
        ɵɵelementEnd()();
      }
      if (rf & 2) {
        ɵɵclassProp("po-stepper-disabled-click", ctx.disabledClick);
        ɵɵproperty("ngClass", ctx.getStatusClass(ctx.status))("tabindex", ctx.status === "disabled" || ctx.disabledClick ? -1 : 0);
        ɵɵattribute("aria-selected", ctx.status === "active")("aria-current", ctx.status === "active" ? "step" : null)("aria-disabled", ctx.status === "disabled")("aria-required", ctx.status === "error");
        ɵɵadvance(2);
        ɵɵstyleProp("min-width", ctx.minWidthCircle, "px")("min-height", ctx.minHeightCircle, "px");
        ɵɵclassProp("po-stepper-circle-vertical", ctx.isVerticalOrientation)("po-stepper-circle-vertical-center", ctx.alignCenter)("po-stepper-circle-horizontal", !ctx.isVerticalOrientation);
        ɵɵproperty("p-align-center", ctx.alignCenter)("p-content", ctx.circleContent)("p-icons", ctx.stepIcons)("p-size", ctx.stepSize)("p-status", ctx.status)("p-icon-default", ctx.iconDefault)("p-step-icon-done", ctx.iconDone)("p-step-icon-active", ctx.iconActive);
        ɵɵadvance();
        ɵɵconditional(ctx.label ? 3 : -1);
      }
    },
    dependencies: [NgClass, PoStepperCircleComponent, PoStepperLabelComponent],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoStepperStepComponent, [{
    type: Component,
    args: [{
      selector: "po-stepper-step",
      standalone: false,
      template: `<div
  class="po-stepper-step"
  [ngClass]="getStatusClass(status)"
  role="tab"
  [tabindex]="status === 'disabled' || disabledClick ? -1 : 0"
  [attr.aria-selected]="status === 'active'"
  [attr.aria-current]="status === 'active' ? 'step' : null"
  [attr.aria-disabled]="status === 'disabled'"
  [attr.aria-required]="status === 'error'"
  [class.po-stepper-disabled-click]="disabledClick"
  (click)="onClick()"
  (keydown.enter)="onEnter($event)"
  (keydown.space)="onEnter($event)"
>
  <div class="po-stepper-step-container">
    <po-stepper-circle
      [p-align-center]="alignCenter"
      [p-content]="circleContent"
      [p-icons]="stepIcons"
      [p-size]="stepSize"
      [p-status]="status"
      [p-icon-default]="iconDefault"
      [p-step-icon-done]="iconDone"
      [p-step-icon-active]="iconActive"
      [class.po-stepper-circle-vertical]="isVerticalOrientation"
      [class.po-stepper-circle-vertical-center]="alignCenter"
      [class.po-stepper-circle-horizontal]="!isVerticalOrientation"
      [style.minWidth.px]="minWidthCircle"
      [style.minHeight.px]="minHeightCircle"
    >
    </po-stepper-circle>

    @if (label) {
      <po-stepper-label
        class="po-stepper-step-label-position"
        [p-align-center]="alignCenter"
        [p-content]="label"
        [p-status]="status"
        [p-vertical-orientation]="isVerticalOrientation"
        [attr.aria-label]="label"
        [p-disable-click]="disabledClick"
      >
      </po-stepper-label>
    }
  </div>
</div>
`
    }]
  }], null, {
    alignCenter: [{
      type: Input,
      args: ["p-align-center"]
    }],
    circleContent: [{
      type: Input,
      args: ["p-circle-content"]
    }],
    nextStatus: [{
      type: Input,
      args: ["p-next-status"]
    }],
    orientation: [{
      type: Input,
      args: ["p-orientation"]
    }],
    label: [{
      type: Input,
      args: ["p-label"]
    }],
    status: [{
      type: Input,
      args: ["p-status"]
    }],
    stepIcons: [{
      type: Input,
      args: ["p-step-icons"]
    }],
    stepSize: [{
      type: Input,
      args: ["p-step-size"]
    }],
    iconDefault: [{
      type: Input,
      args: ["p-icon-default"]
    }],
    iconDone: [{
      type: Input,
      args: ["p-step-icon-done"]
    }],
    iconActive: [{
      type: Input,
      args: ["p-step-icon-active"]
    }],
    isVerticalOrientation: [{
      type: Input,
      args: ["p-vertical-orientation"]
    }],
    activated: [{
      type: Output,
      args: ["p-activated"]
    }],
    click: [{
      type: Output,
      args: ["p-click"]
    }],
    enter: [{
      type: Output,
      args: ["p-enter"]
    }],
    disabledClick: [{
      type: Input,
      args: ["p-disable-click"]
    }]
  });
})();
var PoStepperComponent = class _PoStepperComponent extends PoStepperBaseComponent {
  changeDetector = inject(ChangeDetectorRef);
  poSteps;
  currentActiveStep;
  previousActiveStepIndex = null;
  get currentStepIndex() {
    return this.step - 1;
  }
  get isVerticalOrientation() {
    return this.orientation === "vertical";
  }
  get stepList() {
    return this.usePoSteps && this.poSteps || this.steps;
  }
  get stepSizeCircle() {
    return this.calculateDividerPosition();
  }
  get usePoSteps() {
    return !!this.poSteps.length;
  }
  ngAfterContentInit() {
    this.activeFirstStep();
    this.poSteps.changes.subscribe(() => {
      this.controlStepsStatus(this.poSteps.first);
    });
  }
  /**
   * Altera o status do *step* para ativo.
   *
   * > Este método é valido apenas para as implementações que utilizam o componente [**po-step**](/documentation/po-step).
   *
   * @param {number} index Índice do `po-step` que se deseja ativar.
   */
  active(index) {
    if (!this.usePoSteps) {
      return;
    }
    const stepsArray = this.getPoSteps();
    if (index < 0 || index >= stepsArray.length) {
      return;
    }
    const step = stepsArray[index];
    this.changeStep(index, step);
  }
  /**
   * Ativa o primeiro *step*.
   *
   * > Este método é valido apenas para as implementações que utilizam o componente [**po-step**](/documentation/po-step).
   */
  first() {
    if (!this.usePoSteps) {
      return;
    }
    const firstStep = this.poSteps.first;
    const firstStepIndex = 0;
    this.changeStep(firstStepIndex, firstStep);
  }
  /**
   * Ativa o próximo *step*.
   *
   * > Este método é valido apenas para as implementações que utilizam o componente [**po-step**](/documentation/po-step).
   */
  next() {
    if (!this.usePoSteps) {
      return;
    }
    const {
      steps,
      stepIndex
    } = this.getStepsAndIndex(this.currentActiveStep);
    const nextIndex = stepIndex + 1;
    if (nextIndex >= steps.length) {
      return;
    }
    const nextStep = steps[nextIndex];
    this.changeStep(nextIndex, nextStep);
  }
  /**
   * Ativa o *step* anterior.
   *
   * > Este método é valido apenas para as implementações que utilizam o componente [**po-step**](/documentation/po-step).
   */
  previous() {
    if (!this.usePoSteps) {
      return;
    }
    const {
      steps,
      stepIndex
    } = this.getStepsAndIndex(this.currentActiveStep);
    const previousIndex = stepIndex - 1;
    if (previousIndex < 0) {
      return;
    }
    const previousStep = steps[previousIndex];
    this.changeStep(previousIndex, previousStep);
  }
  changeStep(stepIndex, step) {
    if (!step || this.currentActiveStep && this.currentActiveStep.id === step.id) {
      return;
    }
    this.allowNextStep(stepIndex, step).pipe(take(1)).subscribe((nextStepAllowed) => {
      if (nextStepAllowed) {
        const isDifferentStep = !this.currentActiveStep || step.id !== this.currentActiveStep.id;
        if (this.usePoSteps && isDifferentStep) {
          this.controlStepsStatus(step);
          this.onChangeStep.emit(step);
        } else if (!this.usePoSteps && stepIndex !== this.currentStepIndex) {
          this.onChangeStep.emit(stepIndex + 1);
        }
      }
    });
  }
  getNextPoSteps(stepIndex) {
    const poSteps = this.getPoSteps();
    return poSteps[stepIndex + 1];
  }
  getNextSteps(stepIndex) {
    return this.steps[stepIndex + 1];
  }
  getPreviousPoSteps(stepIndex) {
    const poSteps = this.getPoSteps();
    return poSteps[stepIndex - 1];
  }
  getPreviousSteps(stepIndex) {
    return this.steps[stepIndex - 1];
  }
  isDashedBorder(step, index) {
    const nextStepPoSteps = this.getNextPoSteps(index);
    const nextStepSteps = this.getNextSteps(index);
    return !(step.status === "active" && (nextStepPoSteps?.status === "done" || nextStepSteps?.status === "done")) && step.status !== "done" && (this.usePoSteps || this.sequential);
  }
  isDashedBorderTop(step, index) {
    const previousStepPoSteps = this.getPreviousPoSteps(index);
    const getPreviousSteps = this.getPreviousSteps(index);
    return (step.status === "done" && previousStepPoSteps?.status !== "done" && previousStepPoSteps?.status !== "active" || step.status !== "done") && (step.status === "done" && getPreviousSteps?.status !== "done" && getPreviousSteps?.status !== "active" || step.status !== "done") && (step.status !== "active" || step.status === "active" && getPreviousSteps?.status === "error") && (step.status === "default" && getPreviousSteps?.status !== "done" || step.status === "disabled" || getPreviousSteps?.status !== "done") && previousStepPoSteps?.status !== "done" && (this.usePoSteps || this.sequential);
  }
  onStepActive(step) {
    this.currentActiveStep = step;
    const {
      stepIndex
    } = this.getStepsAndIndex(this.currentActiveStep);
    this.poSteps.forEach((stepChild, i2) => {
      if (i2 < stepIndex) {
        stepChild.status = PoStepperStatus.Done;
      }
    });
  }
  trackByFn(step) {
    return step.id;
  }
  activeFirstStep() {
    const hasStepActive = this.poSteps.some((poStep) => poStep.status === PoStepperStatus.Active);
    if (this.usePoSteps && !hasStepActive) {
      this.changeStep(0, this.poSteps.first);
    }
  }
  allowNextStep(nextStepIndex, step) {
    if (!this.sequential) {
      return of(true);
    }
    if (this.hasStepWithCanActiveNextStep() && this.hasDefaultBeforeDone(nextStepIndex)) {
      return of(false);
    }
    const isAllowNextStep$ = this.checkAllowNextStep(nextStepIndex);
    return typeof isAllowNextStep$ === "boolean" ? of(isAllowNextStep$) : isAllowNextStep$;
  }
  canActiveNextStep(currentActiveStep = {}, nextStepIndex) {
    const isCurrentStep = this.isCurrentStep(nextStepIndex);
    if (!currentActiveStep.canActiveNextStep) {
      if (!isCurrentStep) {
        currentActiveStep.status = PoStepperStatus.Done;
      }
      return of(true);
    }
    const canActiveNextStep$ = this.getCanActiveNextStepObservable(currentActiveStep);
    return of(this.isBeforeStep(nextStepIndex)).pipe(mergeMap((isBefore) => {
      if (isBefore && !isCurrentStep) {
        return canActiveNextStep$.pipe(tap((isCanActiveNextStep) => {
          currentActiveStep.status = isCanActiveNextStep ? PoStepperStatus.Done : PoStepperStatus.Default;
        }), map(() => true));
      } else {
        return canActiveNextStep$;
      }
    }), tap((isCanActiveNextStep) => {
      if (!this.isBeforeStep(nextStepIndex) && !isCurrentStep) {
        this.updateStepStatus(currentActiveStep, isCanActiveNextStep);
      }
    }), catchError((err) => {
      currentActiveStep.status = PoStepperStatus.Error;
      return throwError(err);
    }));
  }
  checkAllowNextStep(nextStepIndex) {
    return this.usePoSteps ? this.canActiveNextStep(this.currentActiveStep, nextStepIndex) : of(this.steps.slice(this.step, nextStepIndex).every((step) => step.status === PoStepperStatus.Done));
  }
  getCanActiveNextStepObservable(currentActiveStep) {
    const canActiveNextStep = currentActiveStep.canActiveNextStep(currentActiveStep);
    return canActiveNextStep instanceof Observable ? canActiveNextStep : of(canActiveNextStep);
  }
  hasDefaultBeforeDone(nextStepIndex) {
    return this.getPoSteps().slice(this.step, nextStepIndex).some((step) => step.status === PoStepperStatus.Default);
  }
  isCurrentStep(stepIndex) {
    return this.currentActiveStep && this.getPoSteps().findIndex((step) => step.id === this.currentActiveStep.id) === stepIndex;
  }
  controlStepsStatus(step) {
    if (this.usePoSteps) {
      const {
        steps,
        stepIndex: currentStepIndex
      } = this.getStepsAndIndex(step);
      if (!this.hasStepWithCanActiveNextStep()) {
        this.updatePreviousStepStatus(steps, currentStepIndex);
      }
      this.setStepAsActive(step);
      this.handleNextStep(steps, currentStepIndex);
      this.previousActiveStepIndex = currentStepIndex;
      this.changeDetector.detectChanges();
    }
  }
  calculateDividerPosition() {
    return this.stepSize >= 24 && this.stepSize <= 64 ? this.stepSize : 24;
  }
  getStepperStatusByCanActive(canActiveNextStep) {
    return canActiveNextStep ? PoStepperStatus.Done : PoStepperStatus.Error;
  }
  getStepsAndIndex(step = {}) {
    const steps = this.getPoSteps();
    const stepIndex = steps.findIndex((poStep) => poStep.id === step.id);
    return {
      steps,
      stepIndex
    };
  }
  getPoSteps() {
    return this.poSteps.toArray();
  }
  handleNextStep(steps, currentStepIndex) {
    const nextStep = steps[currentStepIndex + 1];
    const currentStep = steps[currentStepIndex];
    const isNextStepDisabled = nextStep && nextStep.status === PoStepperStatus.Disabled;
    if (!this.hasStepWithCanActiveNextStep() && isNextStepDisabled) {
      this.setNextStepAsDefault(steps[currentStepIndex]);
      if (this.isBeforeStep(currentStepIndex)) {
        this.setFinalSteppersAsDisabled(currentStepIndex);
      }
    }
    if (this.hasStepWithCanActiveNextStep() && isNextStepDisabled) {
      this.setNextStepAsDefault(currentStep);
      this.setFinalSteppersAsDisabled(currentStepIndex);
    }
  }
  hasStepWithCanActiveNextStep() {
    return this.getPoSteps().some((step) => step.canActiveNextStep);
  }
  isBeforeStep(stepIndex) {
    const currentActiveStepIndex = () => this.getPoSteps().findIndex((step) => step.id === this.currentActiveStep.id);
    return !!this.currentActiveStep && currentActiveStepIndex() >= stepIndex;
  }
  setFinalSteppersAsDisabled(stepIndex) {
    this.getPoSteps().filter((step, index) => step && index >= stepIndex + 2).forEach((step) => step.status = PoStepperStatus.Disabled);
  }
  setStepAsActive(step) {
    step.status = PoStepperStatus.Active;
  }
  setNextStepAsDefault(currentStep) {
    const {
      steps,
      stepIndex
    } = this.getStepsAndIndex(currentStep);
    const nextIndex = stepIndex + 1;
    if (nextIndex < this.poSteps.length) {
      steps[nextIndex].status = PoStepperStatus.Default;
    }
  }
  updatePreviousStepStatus(steps, currentStepIndex) {
    if (this.previousActiveStepIndex !== null && this.previousActiveStepIndex !== currentStepIndex) {
      steps[this.previousActiveStepIndex].status = PoStepperStatus.Done;
    }
  }
  updateStepStatus(currentActiveStep, isCanActiveNextStep) {
    currentActiveStep.status = this.getStepperStatusByCanActive(isCanActiveNextStep);
  }
  static ɵfac = /* @__PURE__ */ (() => {
    let ɵPoStepperComponent_BaseFactory;
    return function PoStepperComponent_Factory(__ngFactoryType__) {
      return (ɵPoStepperComponent_BaseFactory || (ɵPoStepperComponent_BaseFactory = ɵɵgetInheritedFactory(_PoStepperComponent)))(__ngFactoryType__ || _PoStepperComponent);
    };
  })();
  static ɵcmp = ɵɵdefineComponent({
    type: _PoStepperComponent,
    selectors: [["po-stepper"]],
    contentQueries: function PoStepperComponent_ContentQueries(rf, ctx, dirIndex) {
      if (rf & 1) {
        ɵɵcontentQuery(dirIndex, PoStepComponent, 4);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.poSteps = _t);
      }
    },
    standalone: false,
    features: [ɵɵInheritDefinitionFeature],
    ngContentSelectors: _c12,
    decls: 4,
    vars: 9,
    consts: [["role", "tablist"], [4, "ngFor", "ngForOf", "ngForTrackBy"], ["class", "po-stepper-content", 4, "ngIf"], [1, "po-stepper-step-wrapper"], [3, "po-stepper-step-bar-top", "po-stepper-step-dashed-border-vertical", "left", 4, "ngIf"], [3, "p-activated", "p-click", "p-enter", "p-align-center", "p-circle-content", "p-label", "p-orientation", "p-status", "p-step-icons", "p-step-size", "p-next-status", "p-icon-default", "p-step-icon-active", "p-step-icon-done", "p-vertical-orientation", "p-disable-click"], [3, "po-stepper-step-bar-bottom", "po-stepper-step-bar-bottom-center", "po-stepper-step-bar-right", "po-stepper-step-bar-center", "po-stepper-step-dashed-border", "po-stepper-step-dashed-border-vertical", "top", "left", 4, "ngIf"], [1, "po-stepper-content"]],
    template: function PoStepperComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵprojectionDef();
        ɵɵelementStart(0, "div")(1, "div", 0);
        ɵɵtemplate(2, PoStepperComponent_ng_container_2_Template, 5, 17, "ng-container", 1);
        ɵɵelementEnd();
        ɵɵtemplate(3, PoStepperComponent_div_3_Template, 2, 0, "div", 2);
        ɵɵelementEnd();
      }
      if (rf & 2) {
        ɵɵclassMap(ɵɵinterpolate1("po-stepper-", ctx.orientation));
        ɵɵadvance();
        ɵɵclassMap(ɵɵinterpolate1("po-stepper-container-", ctx.orientation));
        ɵɵadvance();
        ɵɵproperty("ngForOf", ctx.stepList)("ngForTrackBy", ctx.trackByFn);
        ɵɵadvance();
        ɵɵproperty("ngIf", ctx.usePoSteps);
      }
    },
    dependencies: [NgForOf, NgIf, PoStepperStepComponent],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoStepperComponent, [{
    type: Component,
    args: [{
      selector: "po-stepper",
      standalone: false,
      template: '<div class="po-stepper-{{ orientation }}">\n  <div role="tablist" class="po-stepper-container-{{ orientation }}">\n    <ng-container *ngFor="let step of stepList; let index = index; trackBy: trackByFn">\n      <div class="po-stepper-step-wrapper">\n        <div\n          *ngIf="index > 0"\n          [class.po-stepper-step-bar-top]="alignCenter && isVerticalOrientation"\n          [class.po-stepper-step-dashed-border-vertical]="\n            alignCenter && isVerticalOrientation && isDashedBorderTop(step, index)\n          "\n          [style.left.px]="\n            isVerticalOrientation ? (!stepSizeCircle || stepSizeCircle === 24 ? 16 : stepSizeCircle / 2) : null\n          "\n        ></div>\n\n        <po-stepper-step\n          [class.po-stepper-step-position-center]="alignCenter"\n          [p-align-center]="alignCenter"\n          [p-circle-content]="index + 1"\n          [p-label]="step.label"\n          [p-orientation]="orientation"\n          [p-status]="step.status"\n          [p-step-icons]="stepIcons"\n          [p-step-size]="stepSize"\n          [p-next-status]="poSteps.get(index + 1)?.status"\n          [p-icon-default]="step.iconDefault"\n          [p-step-icon-active]="iconActive"\n          [p-step-icon-done]="iconDone"\n          [p-vertical-orientation]="isVerticalOrientation"\n          [p-disable-click]="disabledClick"\n          (p-activated)="onStepActive(step)"\n          (p-click)="changeStep(index, step)"\n          (p-enter)="changeStep(index, step)"\n        >\n        </po-stepper-step>\n\n        <div\n          *ngIf="index < stepList.length - 1"\n          [class.po-stepper-step-bar-bottom]="isVerticalOrientation"\n          [class.po-stepper-step-bar-bottom-center]="alignCenter && isVerticalOrientation"\n          [class.po-stepper-step-bar-right]="!isVerticalOrientation"\n          [class.po-stepper-step-bar-center]="alignCenter && !isVerticalOrientation"\n          [class.po-stepper-step-dashed-border]="!isVerticalOrientation && isDashedBorder(step, index)"\n          [class.po-stepper-step-dashed-border-vertical]="isVerticalOrientation && isDashedBorder(step, index)"\n          [style.top.px]="!isVerticalOrientation ? (stepSizeCircle === 24 ? 16 : stepSizeCircle / 2) : null"\n          [style.left.px]="\n            isVerticalOrientation ? (!stepSizeCircle || stepSizeCircle === 24 ? 16 : stepSizeCircle / 2) : null\n          "\n        ></div>\n      </div>\n    </ng-container>\n  </div>\n\n  <div *ngIf="usePoSteps" class="po-stepper-content">\n    <ng-content></ng-content>\n  </div>\n</div>\n'
    }]
  }], null, {
    poSteps: [{
      type: ContentChildren,
      args: [PoStepComponent]
    }]
  });
})();
var PoStepperModule = class _PoStepperModule {
  static ɵfac = function PoStepperModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoStepperModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoStepperModule,
    declarations: [PoStepComponent, PoStepperCircleComponent, PoStepperComponent, PoStepperLabelComponent, PoStepperStepComponent],
    imports: [CommonModule, PoIconModule, PoTooltipModule],
    exports: [PoStepComponent, PoStepperComponent]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [CommonModule, PoIconModule, PoTooltipModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoStepperModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, PoIconModule, PoTooltipModule],
      declarations: [PoStepComponent, PoStepperCircleComponent, PoStepperComponent, PoStepperLabelComponent, PoStepperStepComponent],
      exports: [PoStepComponent, PoStepperComponent]
    }]
  }], null, null);
})();
var PoTabBaseComponent = class _PoTabBaseComponent {
  /** Rótulo da aba. */
  label;
  /**
   * @optional
   *
   * @description
   *
   * Esconde o botão de fechar a aba.
   *
   * > Atenção: Propriedade disponível somente no `po-context-tabs`.
   *
   * @default `false`
   */
  hideClose = false;
  /** ID da Tab */
  id = uuid();
  /** Método disparado ao clicar na aba. */
  click = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Evento disparado ao clicar no botão de fechar a aba.
   *
   * > Atenção: Propriedade disponível somente no `po-context-tabs`.
   *
   */
  closeTab = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Evento emitido quando a aba é ativada.
   * Emite a instância do componente da aba que foi ativada.
   */
  activatedTab = output({
    alias: "p-activated-tab"
  });
  _active = false;
  _disabled = false;
  _hide = false;
  widthButton;
  /**
   * @optional
   *
   * @description
   *
   * Ativa a aba exibindo seu conteúdo.
   *
   * > Sugere-se utilizar na aba de conteúdo inicial.
   *
   * @default `false`
   */
  set active(active) {
    this._active = convertToBoolean(active);
    this.setDisplayOnActive();
  }
  get active() {
    return this._active;
  }
  /**
   * @optional
   *
   * @description
   *
   * Desabilita a aba.
   *
   * @default `false`
   */
  set disabled(disabled) {
    this._disabled = convertToBoolean(disabled);
  }
  get disabled() {
    return this._disabled;
  }
  /**
   * @optional
   *
   * @description
   *
   * Oculta a aba.
   *
   * > Atenção: Para correto funcionamento do componente, indicamos essa propriedade para esconder e exibir e não *ngIf.
   *
   * @default `false`
   */
  set hide(hide) {
    this._hide = convertToBoolean(hide);
  }
  get hide() {
    return this._hide;
  }
  static ɵfac = function PoTabBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoTabBaseComponent)();
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoTabBaseComponent,
    inputs: {
      label: [0, "p-label", "label"],
      hideClose: [2, "p-hide-close", "hideClose", convertToBoolean],
      id: "id",
      active: [0, "p-active", "active"],
      disabled: [0, "p-disabled", "disabled"],
      hide: [0, "p-hide", "hide"]
    },
    outputs: {
      click: "p-click",
      closeTab: "p-close-tab",
      activatedTab: "p-activated-tab"
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoTabBaseComponent, [{
    type: Directive
  }], null, {
    label: [{
      type: Input,
      args: ["p-label"]
    }],
    hideClose: [{
      type: Input,
      args: [{
        alias: "p-hide-close",
        transform: convertToBoolean
      }]
    }],
    id: [{
      type: Input,
      args: ["id"]
    }],
    click: [{
      type: Output,
      args: ["p-click"]
    }],
    closeTab: [{
      type: Output,
      args: ["p-close-tab"]
    }],
    activatedTab: [{
      type: Output,
      args: ["p-activated-tab"]
    }],
    active: [{
      type: Input,
      args: ["p-active"]
    }],
    disabled: [{
      type: Input,
      args: ["p-disabled"]
    }],
    hide: [{
      type: Input,
      args: ["p-hide"]
    }]
  });
})();
var PoTabsService = class _PoTabsService {
  onChangesTriggeredSource = new Subject();
  onChangesTriggeredActiveSource = new Subject();
  onChangesTriggered$ = this.onChangesTriggeredSource.asObservable();
  triggerActiveOnChanges$ = this.onChangesTriggeredActiveSource.asObservable();
  triggerOnChanges(tab) {
    this.onChangesTriggeredSource.next(tab);
  }
  triggerActiveOnChanges(tab) {
    this.onChangesTriggeredActiveSource.next(tab);
  }
  static ɵfac = function PoTabsService_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoTabsService)();
  };
  static ɵprov = ɵɵdefineInjectable({
    token: _PoTabsService,
    factory: _PoTabsService.ɵfac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoTabsService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
var PoTabComponent = class _PoTabComponent extends PoTabBaseComponent {
  elementRef = inject(ElementRef);
  tabsService = inject(PoTabsService);
  // Propriedade interna utilizada no po-context-tabs
  removed = false;
  // Propriedade interna utilizada no po-context-tabs
  showTooltip = false;
  ngAfterContentInit() {
    this.setDisplayOnActive();
  }
  ngOnChanges(changes) {
    setTimeout(() => {
      this.tabsService.triggerOnChanges(this);
      if (changes?.active?.currentValue) {
        this.tabsService.triggerActiveOnChanges(this);
      }
    }, 100);
  }
  setDisplayOnActive() {
    this.elementRef.nativeElement.style.display = this.active ? "" : "none";
  }
  static ɵfac = /* @__PURE__ */ (() => {
    let ɵPoTabComponent_BaseFactory;
    return function PoTabComponent_Factory(__ngFactoryType__) {
      return (ɵPoTabComponent_BaseFactory || (ɵPoTabComponent_BaseFactory = ɵɵgetInheritedFactory(_PoTabComponent)))(__ngFactoryType__ || _PoTabComponent);
    };
  })();
  static ɵcmp = ɵɵdefineComponent({
    type: _PoTabComponent,
    selectors: [["po-tab"]],
    standalone: false,
    features: [ɵɵInheritDefinitionFeature, ɵɵNgOnChangesFeature],
    ngContentSelectors: _c12,
    decls: 1,
    vars: 0,
    template: function PoTabComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵprojectionDef();
        ɵɵprojection(0);
      }
    },
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoTabComponent, [{
    type: Component,
    args: [{
      selector: "po-tab",
      standalone: false,
      template: "<ng-content></ng-content>\n"
    }]
  }], null, null);
})();
var PoTabsBaseComponent = class _PoTabsBaseComponent {
  _size = void 0;
  _initialSize = void 0;
  /**
   * @optional
   *
   * @description
   *
   * Define o tamanho do componente:
   * - `small`: altura dos tabs como 32px (disponível apenas para acessibilidade AA).
   * - `medium`: altura dos tabs como 44px.
   *
   * > Caso a acessibilidade AA não esteja configurada, o tamanho `medium` será mantido.
   * Para mais detalhes, consulte a documentação do [po-theme](https://po-ui.io/documentation/po-theme).
   *
   * @default `medium`
   */
  set size(value) {
    this._initialSize = value;
    this.applySizeBasedOnA11y();
  }
  get size() {
    return this._size ?? getDefaultSizeFn(PoFieldSize);
  }
  onThemeChange() {
    this.applySizeBasedOnA11y();
  }
  applySizeBasedOnA11y() {
    const size = validateSizeFn(this._initialSize, PoFieldSize);
    this._size = size;
  }
  static ɵfac = function PoTabsBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoTabsBaseComponent)();
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoTabsBaseComponent,
    hostVars: 1,
    hostBindings: function PoTabsBaseComponent_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("PoUiThemeChange", function PoTabsBaseComponent_PoUiThemeChange_HostBindingHandler() {
          return ctx.onThemeChange();
        }, ɵɵresolveWindow);
      }
      if (rf & 2) {
        ɵɵattribute("p-size", ctx.size);
      }
    },
    inputs: {
      size: [0, "p-size", "size"]
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoTabsBaseComponent, [{
    type: Directive
  }], null, {
    size: [{
      type: Input,
      args: ["p-size"]
    }, {
      type: HostBinding,
      args: ["attr.p-size"]
    }],
    onThemeChange: [{
      type: HostListener,
      args: ["window:PoUiThemeChange"]
    }]
  });
})();
var PoTabButtonComponent = class _PoTabButtonComponent {
  changeDetector = inject(ChangeDetectorRef);
  elementRef = inject(ElementRef);
  tabButtom;
  // Desabilita o botão
  disabled;
  // Identificador do componente
  id;
  // Rótulo do botão
  label;
  // Função sera emitida quando a tab ficar ativada
  activated = new EventEmitter();
  // Função sera emitida quando a tab ficar desabilitada ou escondida
  changeState = new EventEmitter();
  // Método recebido do usuário para ser disparado quando clicar na aba
  click = new EventEmitter();
  _active;
  _hide;
  widthButton;
  // Ativa o botão
  set active(value) {
    this._active = value;
    this.emitActivated();
  }
  get active() {
    return this._active;
  }
  // Oculta o botão
  set hide(value) {
    this._hide = convertToBoolean(value);
    this.setDisplayOnHide();
  }
  get hide() {
    return this._hide;
  }
  ngAfterViewInit() {
    this.widthButton = this.tabButtom.nativeElement.offsetWidth;
    this.changeDetector.detectChanges();
  }
  ngOnChanges(changes) {
    if (changes.hide && changes.hide.currentValue || changes.disabled && changes.disabled.currentValue) {
      this.changeState.emit(this);
    }
  }
  onClick() {
    if (!this.disabled) {
      this.click.emit(this.id);
    }
  }
  emitActivated() {
    if (this.active) {
      this.activated.emit(this);
    }
  }
  setDisplayOnHide() {
    this.elementRef.nativeElement.style.display = this.hide ? "none" : "";
  }
  static ɵfac = function PoTabButtonComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoTabButtonComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoTabButtonComponent,
    selectors: [["po-tab-button"]],
    viewQuery: function PoTabButtonComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c171, 7);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.tabButtom = _t.first);
      }
    },
    inputs: {
      disabled: [0, "p-disabled", "disabled"],
      id: [0, "p-id", "id"],
      label: [0, "p-label", "label"],
      active: [0, "p-active", "active"],
      hide: [0, "p-hide", "hide"]
    },
    outputs: {
      activated: "p-activated",
      changeState: "p-change-state",
      click: "p-click"
    },
    standalone: false,
    features: [ɵɵNgOnChangesFeature],
    decls: 5,
    vars: 16,
    consts: [["tabButtom", ""], ["role", "tab", "p-tooltip-position", "top", 1, "po-tab-button-default", 3, "click", "keyup.enter", "p-tooltip", "id", "ngClass"], [1, "po-tab-button-label", "po-text-ellipsis"], [3, "ngClass"]],
    template: function PoTabButtonComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "div", 1, 0);
        ɵɵlistener("click", function PoTabButtonComponent_Template_div_click_0_listener() {
          return ctx.onClick();
        })("keyup.enter", function PoTabButtonComponent_Template_div_keyup_enter_0_listener() {
          return ctx.onClick();
        });
        ɵɵelementStart(2, "span", 2);
        ɵɵtext(3);
        ɵɵelementEnd()();
        ɵɵelement(4, "div", 3);
      }
      if (rf & 2) {
        ɵɵclassProp("po-tab-button-active", ctx.active)("po-tab-button-disabled", ctx.disabled);
        ɵɵproperty("p-tooltip", ctx.widthButton > 239 ? ctx.label : "")("id", ctx.id)("ngClass", ɵɵpureFunction1(11, _c172, !ctx.disabled && !ctx.hide));
        ɵɵattribute("tabindex", ctx.disabled ? null : 0)("aria-selected", ctx.active);
        ɵɵadvance(3);
        ɵɵtextInterpolate1(" ", ctx.label, " ");
        ɵɵadvance();
        ɵɵproperty("ngClass", ɵɵpureFunction2(13, _c173, ctx.active, !ctx.active));
      }
    },
    dependencies: [NgClass, PoTooltipDirective],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoTabButtonComponent, [{
    type: Component,
    args: [{
      selector: "po-tab-button",
      standalone: false,
      template: `<div
  #tabButtom
  role="tab"
  [p-tooltip]="widthButton > 239 ? label : ''"
  p-tooltip-position="top"
  class="po-tab-button-default"
  [id]="id"
  [ngClass]="{ 'po-tab-focusable': !disabled && !hide }"
  [attr.tabindex]="disabled ? null : 0"
  [attr.aria-selected]="active"
  [class.po-tab-button-active]="active"
  [class.po-tab-button-disabled]="disabled"
  (click)="onClick()"
  (keyup.enter)="onClick()"
>
  <span class="po-tab-button-label po-text-ellipsis">
    {{ label }}
  </span>
</div>
<div [ngClass]="{ 'po-tab-border-active': active, 'po-tab-border-disabled': !active }"></div>
`
    }]
  }], null, {
    tabButtom: [{
      type: ViewChild,
      args: ["tabButtom", {
        static: true
      }]
    }],
    disabled: [{
      type: Input,
      args: ["p-disabled"]
    }],
    id: [{
      type: Input,
      args: ["p-id"]
    }],
    label: [{
      type: Input,
      args: ["p-label"]
    }],
    activated: [{
      type: Output,
      args: ["p-activated"]
    }],
    changeState: [{
      type: Output,
      args: ["p-change-state"]
    }],
    click: [{
      type: Output,
      args: ["p-click"]
    }],
    active: [{
      type: Input,
      args: ["p-active"]
    }],
    hide: [{
      type: Input,
      args: ["p-hide"]
    }]
  });
})();
var PoTabDropdownComponent = class _PoTabDropdownComponent {
  elementRef = inject(ElementRef);
  popover;
  button;
  // Rótulo do `po-tab-button`
  label;
  // Lista de abas
  tabs = [];
  size;
  // Evento que será emitido ao ativar uma aba
  activated = new EventEmitter();
  // Evento que será emitido a aba for desabilitada ou ocultada
  changeState = new EventEmitter();
  // Evento de click
  click = new EventEmitter();
  isDropdownOpen = false;
  dropdownStyles = {};
  ngAfterViewInit() {
    this.setDropdownPosition();
  }
  toggleDropdown() {
    this.isDropdownOpen = !this.isDropdownOpen;
    if (this.isDropdownOpen) {
      this.setDropdownPosition();
    }
  }
  closeAndReturnToButtom() {
    this.closeDropdown();
    this.button.focus();
  }
  closeDropdown() {
    this.isDropdownOpen = false;
  }
  get buttonElement() {
    return this.button.buttonElement;
  }
  setDropdownPosition() {
    const buttonRect = this.buttonElement.nativeElement.getBoundingClientRect();
    const tabsContainerRect = this.buttonElement.nativeElement.closest(".po-tabs-container").getBoundingClientRect();
    const dropdownWidth = 300;
    const isInsidePage = this.elementRef.nativeElement.closest(".po-page-content");
    this.dropdownStyles = this.calculateDropdownStyles(buttonRect, tabsContainerRect, dropdownWidth, isInsidePage);
  }
  calculateDropdownStyles(buttonRect, tabsContainerRect, dropdownWidth, isInsidePage) {
    if (isInsidePage) {
      return {
        top: `${tabsContainerRect.height + 4 + window.scrollY}px`,
        maxWidth: `${dropdownWidth}px`,
        right: `${tabsContainerRect.right - buttonRect.right}px`
      };
    }
    let rightPosition = tabsContainerRect.width - buttonRect.right;
    rightPosition = Math.max(0, rightPosition);
    return {
      top: `${tabsContainerRect.bottom + 4 + window.scrollY}px`,
      maxWidth: `${dropdownWidth}px`,
      right: `${rightPosition}px`
    };
  }
  onClickOutside(event) {
    if (this.isDropdownOpen && !this.elementRef.nativeElement.contains(event.target)) {
      this.closeDropdown();
    }
  }
  static ɵfac = function PoTabDropdownComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoTabDropdownComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoTabDropdownComponent,
    selectors: [["po-tab-dropdown"]],
    viewQuery: function PoTabDropdownComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c16, 7)(PoButtonComponent, 7);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.popover = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.button = _t.first);
      }
    },
    hostBindings: function PoTabDropdownComponent_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("click", function PoTabDropdownComponent_click_HostBindingHandler($event) {
          return ctx.onClickOutside($event);
        }, ɵɵresolveDocument);
      }
    },
    inputs: {
      label: [0, "p-label", "label"],
      tabs: [0, "p-tabs", "tabs"],
      size: [0, "p-size", "size"]
    },
    outputs: {
      activated: "p-activated",
      changeState: "p-change-state",
      click: "p-click"
    },
    standalone: false,
    decls: 4,
    vars: 3,
    consts: [["button", ""], ["listbox", "", "poListBoxRef", ""], [1, "po-tab-dropdown-content", "po-tab-button-default"], ["p-kind", "tertiary", "p-icon", "ICON_ARROW_DOWN", 3, "p-click", "p-aria-label", "p-size"], [1, "po-tab-dropdown-container", 3, "ngStyle"], ["p-type", "action", 3, "p-activated-tabs", "p-change-state-tabs", "p-click-tabs", "p-close", "p-items", "p-is-tabs", "p-size"]],
    template: function PoTabDropdownComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "div", 2)(1, "po-button", 3, 0);
        ɵɵlistener("p-click", function PoTabDropdownComponent_Template_po_button_p_click_1_listener() {
          return ctx.toggleDropdown();
        });
        ɵɵelementEnd()();
        ɵɵconditionalCreate(3, PoTabDropdownComponent_Conditional_3_Template, 4, 4, "div", 4);
      }
      if (rf & 2) {
        ɵɵadvance();
        ɵɵproperty("p-aria-label", ctx.label)("p-size", ctx.size);
        ɵɵadvance(2);
        ɵɵconditional(ctx.isDropdownOpen ? 3 : -1);
      }
    },
    dependencies: [NgStyle, PoButtonComponent, PoListBoxComponent],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoTabDropdownComponent, [{
    type: Component,
    args: [{
      selector: "po-tab-dropdown",
      standalone: false,
      template: '<div class="po-tab-dropdown-content po-tab-button-default">\n  <po-button\n    #button\n    p-kind="tertiary"\n    [p-aria-label]="label"\n    p-icon="ICON_ARROW_DOWN"\n    [p-size]="size"\n    (p-click)="toggleDropdown()"\n  >\n  </po-button>\n</div>\n\n@if (isDropdownOpen) {\n  <div class="po-tab-dropdown-container" [ngStyle]="dropdownStyles">\n    <po-listbox\n      #listbox\n      #poListBoxRef\n      p-type="action"\n      [p-items]="tabs"\n      [p-is-tabs]="true"\n      [p-size]="size"\n      (p-activated-tabs)="activated.emit($event)"\n      (p-change-state-tabs)="changeState.emit($event)"\n      (p-click-tabs)="closeDropdown(); click.emit($event)"\n      (p-close)="closeAndReturnToButtom()"\n    >\n    </po-listbox>\n  </div>\n}\n'
    }]
  }], null, {
    popover: [{
      type: ViewChild,
      args: ["popover", {
        static: true
      }]
    }],
    button: [{
      type: ViewChild,
      args: [PoButtonComponent, {
        static: true
      }]
    }],
    label: [{
      type: Input,
      args: ["p-label"]
    }],
    tabs: [{
      type: Input,
      args: ["p-tabs"]
    }],
    size: [{
      type: Input,
      args: ["p-size"]
    }],
    activated: [{
      type: Output,
      args: ["p-activated"]
    }],
    changeState: [{
      type: Output,
      args: ["p-change-state"]
    }],
    click: [{
      type: Output,
      args: ["p-click"]
    }],
    onClickOutside: [{
      type: HostListener,
      args: ["document:click", ["$event"]]
    }]
  });
})();
var poTabsLiterals = {
  en: {
    moreTabs: "More",
    close: "Close Tab"
  },
  es: {
    moreTabs: "Más",
    close: "Cerrar pestaña"
  },
  pt: {
    moreTabs: "Mais",
    close: "Fechar Aba"
  },
  ru: {
    moreTabs: "Ещё",
    close: "Закрыть вкладку"
  }
};
var poTabsMaxNumberOfTabs = 5;
var PoTabsComponent = class _PoTabsComponent extends PoTabsBaseComponent {
  changeDetector = inject(ChangeDetectorRef);
  languageService = inject(PoLanguageService);
  tabsService = inject(PoTabsService);
  // Tabs utilizados no ng-content
  tabsChildren;
  tabButton;
  tabDropdown;
  containerTabs;
  maxNumberOfTabs = poTabsMaxNumberOfTabs;
  literals;
  tabsDefault = [];
  tabsDropdown = [];
  initializeCalculation = true;
  initializeComponent = false;
  initCheckChangesTab = false;
  quantityTabsButton;
  defaultLastTabWidth;
  previousActiveTab;
  subscription = new Subscription();
  subscriptionTabsService = new Subscription();
  subscriptionTabActive = new Subscription();
  constructor() {
    super();
    const languageService = this.languageService;
    const language = languageService.getShortLanguage();
    this.literals = __spreadValues({}, poTabsLiterals[language]);
  }
  ngOnInit() {
    this.subscriptionTabsService = this.tabsService.onChangesTriggered$.subscribe((tab) => {
      this.updateTabsState(false, tab);
      if (this.initializeComponent) {
        this.handleKeyboardNavigationTab();
      }
    });
    this.subscriptionTabActive = this.tabsService.triggerActiveOnChanges$.subscribe((tab) => {
      const isTabInDropdown = this.tabsDropdown.some((t) => t.id === tab.id);
      if (isTabInDropdown) {
        this.onTabActiveByDropdown(tab, false);
      }
    });
  }
  ngAfterViewInit() {
    this.calculateTabs(this.initializeCalculation);
    this.initializeCalculation = false;
    this.updateTabsState();
    this.changeDetector.detectChanges();
    this.handleKeyboardNavigationTab();
    this.initializeComponent = true;
    setTimeout(() => {
      this.initCheckChangesTab = true;
    }, 500);
  }
  ngAfterContentInit() {
    this.updateTabsState(true);
  }
  ngOnDestroy() {
    this.subscription.unsubscribe();
    this.subscriptionTabsService.unsubscribe();
    this.subscriptionTabActive.unsubscribe();
  }
  get isShowTabDropdown() {
    return this.tabsChildren["_results"].slice(this.quantityTabsButton).filter((item) => !item.hide).length;
  }
  // tabs que serão apresentadas na aba "Mais"
  get overflowedTabs() {
    return this.tabsChildren["_results"].slice(this.quantityTabsButton);
  }
  get tabs() {
    return this.tabsChildren["_results"].slice(0, this.quantityTabsButton);
  }
  get tabsChildrenArray() {
    return this.tabsChildren.toArray();
  }
  closeListbox() {
    if (this.tabDropdown && this.tabDropdown.isDropdownOpen) {
      this.tabDropdown.closeDropdown();
    }
  }
  isVisibleTab(tab) {
    return !tab.hide;
  }
  onTabActiveByDropdown(tab, eventEmitter = true) {
    this.changeTabPositionByDropdown(tab);
    const lastTabWidth = this.defaultLastTabWidth > 0 ? this.defaultLastTabWidth : this.tabsChildren.find((e2) => e2 === this.tabsChildrenArray[this.quantityTabsButton - 1])?.widthButton;
    this.reorderTabs(tab);
    tab.widthButton = lastTabWidth;
    this.tabButton.last.nativeElement.style.width = `${lastTabWidth}px`;
    this.handleKeyboardNavigationTab();
    if (eventEmitter) {
      tab.click.emit(tab);
    }
  }
  // Função disparada quando alguma tab ficar ativa
  onTabActive(tab) {
    this.previousActiveTab = this.tabsChildren.find((tabChild) => tabChild.active && tabChild.id !== tab.id);
    tab.activatedTab.emit(tab);
    this.deactivateTab();
  }
  // funcao será disparada quando mudar o estado do poTab para desabilitado ou escondido.
  onTabChangeState(tab) {
    if (tab.active) {
      tab.active = false;
      this.activeDistinctTab();
      this.changeDetector.detectChanges();
    }
  }
  reorderTabs(tabToReorder, lastTab) {
    const tabsArray = this.tabsChildrenArray;
    const tabIndex = tabsArray.findIndex((item) => item.id === tabToReorder.id);
    if (tabIndex !== -1) {
      if (lastTab) {
        const lastTabShowed = tabsArray.findIndex((item) => item.id === lastTab.id);
        const [tab] = tabsArray.splice(tabIndex, 1);
        tabsArray.splice(lastTabShowed, 0, tab);
      } else {
        const [tab] = tabsArray.splice(tabIndex, 1);
        tabsArray.splice(this.quantityTabsButton - 1, 0, tab);
      }
    }
    this.tabsChildren.reset(tabsArray);
    this.changeDetector.detectChanges();
  }
  // selectiona a aba informada por parametro, caso houver click faz a emição do evento.
  selectedTab(tab) {
    tab.active = true;
    if (tab.click) {
      tab.click.emit(tab);
    }
    const isTabInDropdown = this.overflowedTabs.some((t) => t.id === tab.id);
    if (isTabInDropdown) {
      this.onTabActiveByDropdown(tab);
    }
    this.changeDetector.detectChanges();
  }
  trackByFn(_i, tab) {
    return tab.id;
  }
  // ativa a previousActiveTab ou primeira tab encontrada.
  activeDistinctTab() {
    if (this.previousActiveTab) {
      this.previousActiveTab.active = true;
    } else {
      this.activeFirstTab();
    }
  }
  // Ativa a primeira Tab que não estiver desabilitada ou escondida.
  activeFirstTab() {
    this.tabs.some((tabChild) => {
      if (!tabChild.disabled && !tabChild.hide) {
        tabChild.active = true;
        return true;
      }
    });
  }
  // Movimenta a tab da visão de tabs para o dropdown, e vice-versa.
  changeTabPositionByDropdown(tabToActivate, byContextsTabs = false) {
    let lastTab;
    if (byContextsTabs) {
      const tabsDefault = this.tabButton.filter((tab) => !tab.nativeElement.hidden);
      lastTab = tabsDefault[tabsDefault.length - 1];
      this.tabsDefault = this.tabsDefault.filter((tab) => tab.id !== lastTab.nativeElement.id);
      lastTab = {
        id: lastTab.nativeElement.id
      };
    } else {
      lastTab = this.tabsDefault[this.tabsDefault.length - 1];
      this.tabsDefault = this.tabsDefault.filter((tab) => tab.id !== lastTab.id);
    }
    this.tabsDefault.push(tabToActivate);
    const _tabsDropdown = this.tabsDropdown.filter((tab) => tab.id !== tabToActivate.id);
    _tabsDropdown.unshift(lastTab);
    this.tabsDropdown = _tabsDropdown;
    tabToActivate.active = true;
    this.onTabActive(tabToActivate);
  }
  // desativa previousActiveTab e dispara a detecção de mudança.
  deactivateTab() {
    if (this.previousActiveTab) {
      this.previousActiveTab.active = false;
      this.changeDetector.detectChanges();
    }
  }
  updateTabsState(initialState = false, tab) {
    this.defaultLastTabWidth = this.tabButton?.last?.nativeElement?.getBoundingClientRect().width;
    if (this.defaultLastTabWidth <= 0) {
      return;
    }
    this.executeTabsState(initialState);
  }
  executeTabsState(initialState, idByContextTabs) {
    let lastTabChildren;
    if (idByContextTabs) {
      lastTabChildren = this.tabsChildrenArray?.find((tab) => tab.id === idByContextTabs);
    } else {
      lastTabChildren = this.tabsChildrenArray[this.quantityTabsButton - 1];
    }
    if (lastTabChildren) {
      lastTabChildren.widthButton = this.defaultLastTabWidth;
    }
    if (this.tabsChildren) {
      const _tabsChildren = [...this.tabsChildrenArray];
      if (initialState) {
        this.tabsDefault = _tabsChildren;
      } else {
        this.tabsDefault = _tabsChildren.slice(0, this.quantityTabsButton);
        this.tabsDropdown = _tabsChildren.slice(this.quantityTabsButton);
      }
    }
  }
  calculateTabs(initializeCalculation) {
    if (initializeCalculation) {
      let sumOfWidth = 150;
      const screenSize = this.containerTabs.nativeElement.offsetWidth;
      const listTabButton = [];
      if (this.tabButton?.length) {
        this.tabButton.forEach((element) => {
          const width = element.nativeElement.offsetWidth;
          if (sumOfWidth + width <= screenSize || screenSize < 1) {
            listTabButton.push(element);
          }
          sumOfWidth += width;
        });
      }
      this.quantityTabsButton = listTabButton.length;
    }
  }
  handleKeyboardNavigationTab(initialIndex = 0) {
    this.subscription.unsubscribe();
    this.subscription = new Subscription();
    if (this.tabButton?.length) {
      const tabFocusable = this.tabButton.filter((element, index) => element.nativeElement.children[0]?.id === this.tabsChildren["_results"][index]?.id).map((el) => {
        if (el.nativeElement.children[0].classList.contains("po-tab-focusable")) {
          return el.nativeElement.children[0];
        }
      }).filter(Boolean);
      if (tabFocusable) {
        this.initializeTabAccessibilityElements(tabFocusable, initialIndex);
      }
    }
  }
  /**
   * Função que atribui o número de tabs fora do dropdown.
   *
   * Para utilizá-la é necessário ter a instância do componente no DOM, podendo ser utilizado o ViewChild da seguinte forma:
   *
   * ```
   * import { PoTabsComponent } from '@po-ui/ng-components';
   *
   * ...
   *
   * @ViewChild('poTab', { static: true }) poTab: PoTabsComponent;
   *
   * changeQuantityTabs() {
   *   this.poTab.setQuantityTabsButton(1); //Número de tabs
   * }
   * ```
   */
  setQuantityTabsButton(number) {
    this.quantityTabsButton = number;
  }
  initializeTabAccessibilityElements(tabRemoveElements, initialIndex) {
    tabRemoveElements.forEach((tabRemoveElement, index) => {
      if (index === initialIndex) {
        this.setTabIndex(tabRemoveElements[initialIndex], 0);
      } else if (tabRemoveElements.length === initialIndex) {
        this.setTabIndex(tabRemoveElements[initialIndex - 1], 0);
      } else {
        this.setTabIndex(tabRemoveElement, -1);
      }
      this.subscription.add(fromEvent(tabRemoveElement, "keydown").subscribe((event) => {
        this.handleKeyDown(event, tabRemoveElements, index);
      }));
      if (index !== 0) {
        this.subscription.add(fromEvent(tabRemoveElements, "blur").subscribe(() => {
          this.setTabIndex(tabRemoveElements[index], -1);
          this.setTabIndex(tabRemoveElements[0], 0);
        }));
      }
    });
  }
  setTabIndex(element, tabIndex) {
    element.setAttribute("tabindex", tabIndex);
  }
  handleKeyDown(event, tabRemoveElements, index) {
    const KEY_SPACE = "Space";
    const KEY_ARROW_LEFT = "ArrowLeft";
    const KEY_ARROW_RIGHT = "ArrowRight";
    const KEY_HOME = "Home";
    const KEY_END = "End";
    if (event.code === KEY_SPACE || event.code === KEY_HOME || event.code === KEY_END) {
      event.preventDefault();
      event.stopPropagation();
    }
    if (event.key === KEY_ARROW_LEFT) {
      this.handleArrowLeft(tabRemoveElements, index);
    } else if (event.key === KEY_ARROW_RIGHT) {
      this.handleArrowRight(tabRemoveElements, index);
    } else if (event.key === KEY_HOME) {
      this.handleHomeKey(tabRemoveElements, index);
    } else if (event.key === KEY_END) {
      this.handleEndKey(tabRemoveElements, index);
    }
  }
  handleArrowLeft(tabRemoveElements, index) {
    if (index > 0) {
      this.setTabIndex(tabRemoveElements[index], -1);
      tabRemoveElements[index - 1].focus();
      this.setTabIndex(tabRemoveElements[index - 1], 0);
    } else {
      this.setTabIndex(tabRemoveElements[index], -1);
      tabRemoveElements[tabRemoveElements.length - 1].focus();
      this.setTabIndex(tabRemoveElements[tabRemoveElements.length - 1], 0);
    }
  }
  handleHomeKey(tabRemoveElements, index) {
    if (index > 0) {
      this.setTabIndex(tabRemoveElements[index], -1);
      tabRemoveElements[0].focus();
      this.setTabIndex(tabRemoveElements[0], 0);
    }
  }
  handleEndKey(tabRemoveElements, index) {
    if (index < tabRemoveElements.length - 1) {
      this.setTabIndex(tabRemoveElements[index], -1);
      tabRemoveElements[tabRemoveElements.length - 1].focus();
      this.setTabIndex(tabRemoveElements[tabRemoveElements.length - 1], 0);
    }
  }
  handleArrowRight(tabRemoveElements, index) {
    if (index < tabRemoveElements.length - 1) {
      this.setTabIndex(tabRemoveElements[index], -1);
      tabRemoveElements[index + 1].focus();
      this.setTabIndex(tabRemoveElements[index + 1], 0);
    } else {
      this.setTabIndex(tabRemoveElements[index], -1);
      tabRemoveElements[0].focus();
      this.setTabIndex(tabRemoveElements[0], 0);
    }
  }
  /**
   * Ativa a aba correspondente ao `id` informado.
   *
   * Para utilizá-la é necessário ter a instância do componente no DOM, podendo ser utilizado o ViewChild da seguinte forma:
   *
   * ```
   * import { PoTabsComponent } from '@po-ui/ng-components';
   *
   * ...
   *
   * @ViewChild('poTab', { static: true }) poTab: PoTabsComponent;
   *
   * focusOnTab() {
   *   this.poTab.activateTab('meu-id-da-aba');
   * }
   * ```
   *
   * @param id Identificador único da aba a ser ativada.
   */
  activateTab(id) {
    const tab = this.tabsChildrenArray.find((x) => x.id === id);
    if (tab) {
      this.selectedTab(tab);
    }
  }
  static ɵfac = function PoTabsComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoTabsComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoTabsComponent,
    selectors: [["po-tabs"]],
    contentQueries: function PoTabsComponent_ContentQueries(rf, ctx, dirIndex) {
      if (rf & 1) {
        ɵɵcontentQuery(dirIndex, PoTabComponent, 4);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.tabsChildren = _t);
      }
    },
    viewQuery: function PoTabsComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c174, 7)(_c175, 7, ElementRef)(_c176, 5, ElementRef);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.tabDropdown = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.containerTabs = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.tabButton = _t);
      }
    },
    standalone: false,
    features: [ɵɵInheritDefinitionFeature],
    ngContentSelectors: _c12,
    decls: 9,
    vars: 1,
    consts: [["containerTabs", ""], ["tabButton", ""], ["tabDropdown", ""], [1, "po-tabs-container"], [1, "po-tabs-header"], ["role", "tablist", 1, "po-tabs-button-wrapper"], [1, "po-tab-button", 3, "p-active", "p-disabled", "p-hide", "p-id", "hidden", "p-label", "id"], [1, "po-tab-dropdown", 3, "po-tab-dropdown-device", "p-label", "p-size", "p-tabs"], [1, "po-tabs-content"], [1, "po-tab-button", 3, "keyup.enter", "p-activated", "p-change-state", "p-click", "p-active", "p-disabled", "p-hide", "p-id", "hidden", "p-label", "id"], [1, "po-tab-dropdown", 3, "p-change-state", "p-click", "p-label", "p-size", "p-tabs"]],
    template: function PoTabsComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵprojectionDef();
        ɵɵelementStart(0, "div", 3)(1, "div", 4, 0)(3, "div", 5);
        ɵɵrepeaterCreate(4, PoTabsComponent_For_5_Template, 2, 8, "po-tab-button", 6, ctx.trackByFn, true);
        ɵɵconditionalCreate(6, PoTabsComponent_Conditional_6_Template, 2, 5, "po-tab-dropdown", 7);
        ɵɵelementEnd()()();
        ɵɵelementStart(7, "div", 8);
        ɵɵprojection(8);
        ɵɵelementEnd();
      }
      if (rf & 2) {
        ɵɵadvance(4);
        ɵɵrepeater(ctx.tabs);
        ɵɵadvance(2);
        ɵɵconditional(ctx.isShowTabDropdown ? 6 : -1);
      }
    },
    dependencies: [PoTabButtonComponent, PoTabDropdownComponent],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoTabsComponent, [{
    type: Component,
    args: [{
      selector: "po-tabs",
      standalone: false,
      template: '<div class="po-tabs-container">\n  <div #containerTabs class="po-tabs-header">\n    <div class="po-tabs-button-wrapper" role="tablist">\n      @for (tab of tabs; track trackByFn($index, tab)) {\n        <po-tab-button\n          #tabButton\n          class="po-tab-button"\n          [p-active]="tab.active"\n          [p-disabled]="tab.disabled"\n          [p-hide]="tab.hide"\n          [p-id]="tab.id"\n          [hidden]="tab.hide"\n          [attr.disabled]="tab.disabled"\n          [p-label]="tab.label"\n          [id]="tab.id"\n          (keyup.enter)="closeListbox()"\n          (p-activated)="onTabActive(tab)"\n          (p-change-state)="onTabChangeState(tab)"\n          (p-click)="selectedTab(tab)"\n        >\n        </po-tab-button>\n      }\n\n      @if (isShowTabDropdown) {\n        <po-tab-dropdown\n          #tabDropdown\n          class="po-tab-dropdown"\n          [class.po-tab-dropdown-device]="!tabsDefault?.length"\n          [p-label]="literals.moreTabs"\n          [p-size]="size"\n          [p-tabs]="overflowedTabs"\n          (p-change-state)="onTabChangeState($event)"\n          (p-click)="onTabActiveByDropdown($event)"\n        >\n        </po-tab-dropdown>\n      }\n    </div>\n  </div>\n</div>\n<div class="po-tabs-content">\n  <ng-content></ng-content>\n</div>\n'
    }]
  }], () => [], {
    tabsChildren: [{
      type: ContentChildren,
      args: [PoTabComponent]
    }],
    tabButton: [{
      type: ViewChildren,
      args: ["tabButton", {
        read: ElementRef
      }]
    }],
    tabDropdown: [{
      type: ViewChild,
      args: ["tabDropdown", {
        static: true
      }]
    }],
    containerTabs: [{
      type: ViewChild,
      args: ["containerTabs", {
        read: ElementRef,
        static: true
      }]
    }]
  });
})();
var PoTabsModule = class _PoTabsModule {
  static ɵfac = function PoTabsModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoTabsModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoTabsModule,
    declarations: [PoTabButtonComponent, PoTabComponent, PoTabDropdownComponent, PoTabsComponent],
    imports: [CommonModule, PoPopoverModule, PoDropdownModule, PoTooltipModule, PoButtonModule, PoListBoxModule],
    exports: [PoTabComponent, PoTabsComponent, PoTabButtonComponent, PoTabDropdownComponent]
  });
  static ɵinj = ɵɵdefineInjector({
    providers: [PoTabsService],
    imports: [CommonModule, PoPopoverModule, PoDropdownModule, PoTooltipModule, PoButtonModule, PoListBoxModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoTabsModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, PoPopoverModule, PoDropdownModule, PoTooltipModule, PoButtonModule, PoListBoxModule],
      declarations: [PoTabButtonComponent, PoTabComponent, PoTabDropdownComponent, PoTabsComponent],
      exports: [PoTabComponent, PoTabsComponent, PoTabButtonComponent, PoTabDropdownComponent],
      providers: [PoTabsService]
    }]
  }], null, null);
})();
var PoContextTabButtonComponent = class _PoContextTabButtonComponent extends PoTabButtonComponent {
  hideClose = false;
  showTooltip = false;
  literals;
  close = new EventEmitter();
  // Função sera emitida quando ocorre mudança da visibilidade da tab
  changeVisible = new EventEmitter();
  activeCloseIcon = false;
  afterViewChecked = false;
  ngAfterViewInit() {
    this.afterViewChecked = true;
    this.widthButton = this.tabButtom.nativeElement.offsetWidth;
    this.changeDetector.detectChanges();
  }
  ngOnChanges(changes) {
    if (changes.hide?.currentValue || changes.disabled?.currentValue) {
      this.changeState.emit(this);
    }
    if (!changes.hide?.firstChange && changes.hide && this.afterViewChecked) {
      this.changeVisible.emit(this);
    }
  }
  closeTab(event) {
    if (event.code === "ArrowLeft" || event.code === "ArrowRight") {
      event.preventDefault();
      event.stopPropagation();
    }
    if ((!event.key || event?.key === "Enter") && !this.disabled) {
      event.preventDefault();
      event.stopPropagation();
      this.close.emit(this.tabButtom);
    }
  }
  onFocusIn() {
    if (!this.disabled) {
      this.activeCloseIcon = true;
    }
  }
  onFocusOut() {
    this.activeCloseIcon = false;
  }
  static ɵfac = /* @__PURE__ */ (() => {
    let ɵPoContextTabButtonComponent_BaseFactory;
    return function PoContextTabButtonComponent_Factory(__ngFactoryType__) {
      return (ɵPoContextTabButtonComponent_BaseFactory || (ɵPoContextTabButtonComponent_BaseFactory = ɵɵgetInheritedFactory(_PoContextTabButtonComponent)))(__ngFactoryType__ || _PoContextTabButtonComponent);
    };
  })();
  static ɵcmp = ɵɵdefineComponent({
    type: _PoContextTabButtonComponent,
    selectors: [["po-context-tab-button"]],
    inputs: {
      hideClose: [0, "p-hide-close", "hideClose"],
      showTooltip: [0, "p-show-tooltip", "showTooltip"],
      literals: [0, "p-literals", "literals"]
    },
    outputs: {
      close: "p-close",
      changeVisible: "p-change-visible"
    },
    standalone: false,
    features: [ɵɵInheritDefinitionFeature, ɵɵNgOnChangesFeature],
    decls: 7,
    vars: 18,
    consts: [["tabButtom", ""], ["role", "tab", "p-tooltip-position", "top", 1, "po-tab-button-default", 3, "click", "focusin", "focusout", "keyup.enter", "p-tooltip", "id", "ngClass"], [1, "po-tab-button-content"], [1, "po-text-ellipsis", 3, "ngClass"], [1, "po-tab-button-content-close-icon"], [3, "ngClass"], [1, "po-tab-button-content-close-icon", 3, "click", "keydown"], ["p-icon", "ICON_CLOSE", 1, "po-tab-button-icon-close", 3, "tabIndex"]],
    template: function PoContextTabButtonComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "div", 1, 0);
        ɵɵlistener("click", function PoContextTabButtonComponent_Template_div_click_0_listener() {
          return ctx.onClick();
        })("focusin", function PoContextTabButtonComponent_Template_div_focusin_0_listener() {
          return ctx.onFocusIn();
        })("focusout", function PoContextTabButtonComponent_Template_div_focusout_0_listener() {
          return ctx.onFocusOut();
        })("keyup.enter", function PoContextTabButtonComponent_Template_div_keyup_enter_0_listener() {
          return ctx.onClick();
        });
        ɵɵelementStart(2, "div", 2)(3, "span", 3);
        ɵɵtext(4);
        ɵɵelementEnd();
        ɵɵconditionalCreate(5, PoContextTabButtonComponent_Conditional_5_Template, 2, 2, "div", 4);
        ɵɵelementEnd()();
        ɵɵelement(6, "div", 5);
      }
      if (rf & 2) {
        ɵɵclassProp("po-tab-button-active", ctx.active)("po-tab-button-disabled", ctx.disabled);
        ɵɵproperty("p-tooltip", ctx.widthButton > 239 || ctx.showTooltip ? ctx.label : "")("id", ctx.id)("ngClass", ɵɵpureFunction1(13, _c172, !ctx.disabled && !ctx.hide));
        ɵɵattribute("tabindex", ctx.disabled ? null : 0)("aria-selected", ctx.active);
        ɵɵadvance(3);
        ɵɵproperty("ngClass", !ctx.hideClose ? "po-context-tab-button-label" : "po-tab-button-label");
        ɵɵadvance();
        ɵɵtextInterpolate1(" ", ctx.label, " ");
        ɵɵadvance();
        ɵɵconditional(!ctx.hideClose ? 5 : -1);
        ɵɵadvance();
        ɵɵproperty("ngClass", ɵɵpureFunction2(15, _c173, ctx.active, !ctx.active));
      }
    },
    dependencies: [NgClass, PoIconComponent, PoTooltipDirective],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoContextTabButtonComponent, [{
    type: Component,
    args: [{
      selector: "po-context-tab-button",
      standalone: false,
      template: `<div
  #tabButtom
  role="tab"
  [p-tooltip]="widthButton > 239 || showTooltip ? label : ''"
  p-tooltip-position="top"
  class="po-tab-button-default"
  [id]="id"
  [ngClass]="{ 'po-tab-focusable': !disabled && !hide }"
  [attr.tabindex]="disabled ? null : 0"
  [attr.aria-selected]="active"
  [class.po-tab-button-active]="active"
  [class.po-tab-button-disabled]="disabled"
  (click)="onClick()"
  (focusin)="onFocusIn()"
  (focusout)="onFocusOut()"
  (keyup.enter)="onClick()"
>
  <div class="po-tab-button-content">
    <span [ngClass]="!hideClose ? 'po-context-tab-button-label' : 'po-tab-button-label'" class="po-text-ellipsis">
      {{ label }}
    </span>
    @if (!hideClose) {
      <div class="po-tab-button-content-close-icon" (click)="closeTab($event)" (keydown)="closeTab($event)">
        <po-icon
          [tabIndex]="activeCloseIcon ? 0 : -1"
          [attr.aria-label]="literals.close"
          p-icon="ICON_CLOSE"
          class="po-tab-button-icon-close"
        ></po-icon>
      </div>
    }
  </div>
</div>
<div [ngClass]="{ 'po-tab-border-active': active, 'po-tab-border-disabled': !active }"></div>
`
    }]
  }], null, {
    hideClose: [{
      type: Input,
      args: ["p-hide-close"]
    }],
    showTooltip: [{
      type: Input,
      args: ["p-show-tooltip"]
    }],
    literals: [{
      type: Input,
      args: ["p-literals"]
    }],
    close: [{
      type: Output,
      args: ["p-close"]
    }],
    changeVisible: [{
      type: Output,
      args: ["p-change-visible"]
    }]
  });
})();
var PoContextTabsComponent = class _PoContextTabsComponent extends PoTabsComponent {
  byQuantityFunction;
  initialTabsWidth = [];
  sumOfWidth = 0;
  get isShowTabDropdown() {
    const tabsShowed = this.tabsChildren["_results"].filter((item) => !item.hide);
    if (tabsShowed.length > this.quantityTabsButton) {
      return true;
    }
    return false;
  }
  // tabs que serão apresentadas na aba "Mais"
  get overflowedTabs() {
    return this.tabsChildren["_results"].filter((item) => !item.hide)?.slice(this.quantityTabsButton);
  }
  get tabs() {
    return this.tabsChildren["_results"];
  }
  closeTab(tab) {
    tab.closeTab.emit(tab);
    const tabWidth = tab.widthButton || this.initialTabsWidth.find((initialTab) => initialTab.id === tab.id)?.width;
    this.tabsChildren["_results"] = this.tabsChildren["_results"].filter((item) => item.id !== tab.id);
    tab.removed = true;
    tab.hide = true;
    this.onTabChangeState(tab);
    tab.elementRef.nativeElement.remove();
    this.afterRemoveTab(tabWidth);
  }
  onChangeVisibilityTab(tab) {
    if (this.initialTabsWidth?.length) {
      const currentInitialTab = this.initialTabsWidth.find((t) => t.id === tab.id);
      let tabWidth = tab.widthButton || currentInitialTab.width;
      if (!tab.hide) {
        const currentTabElement = this.tabButton.find((t) => t.nativeElement.id === tab.id);
        tabWidth = currentTabElement.nativeElement.offsetWidth;
        currentInitialTab.width = tabWidth;
        tab.widthButton = tabWidth;
        this.changeDetector.detectChanges();
        this.calculateTabs(true);
        this.changeDetector.detectChanges();
      } else {
        this.afterRemoveTab(tabWidth);
      }
    }
  }
  afterRemoveTab(widthTab) {
    this.quantityTabsButton = this.quantityTabsButton - 1;
    this.changeDetector.detectChanges();
    const screenSize = this.containerTabs.nativeElement.offsetWidth;
    this.sumOfWidth = this.sumOfWidth - widthTab;
    for (const t of this.overflowedTabs) {
      if (!t.hide) {
        const firstItemDropdown = this.tabButton.find((tb) => tb.nativeElement.id === t.id);
        const width = this.initialTabsWidth.find((i2) => i2.id === firstItemDropdown.nativeElement.id)?.width;
        if (this.sumOfWidth + width <= screenSize || screenSize < 1 || this.quantityTabsButton < this.byQuantityFunction) {
          t.showTooltip = false;
          t.widthButton = void 0;
          firstItemDropdown.nativeElement.hidden = false;
          this.sumOfWidth += width;
          firstItemDropdown.nativeElement.style.width = "";
          firstItemDropdown.nativeElement.style.display = "inline-block";
          this.quantityTabsButton = this.quantityTabsButton + 1;
          if (this.byQuantityFunction > 0 && this.byQuantityFunction === this.quantityTabsButton) {
            break;
          }
        } else {
          break;
        }
      }
    }
    this.changeDetector.detectChanges();
    this.handleKeyboardNavigationTab();
  }
  calculateTabs(initializeCalculation) {
    if (initializeCalculation) {
      this.sumOfWidth = 150;
      const screenSize = this.containerTabs.nativeElement.offsetWidth;
      const listTabButton = [];
      const isFirstCalculation = this.initialTabsWidth.length === 0;
      if (this.tabButton?.length) {
        let initDropdown = false;
        let index = 0;
        this.tabButton.forEach((element) => {
          const width = element.nativeElement.offsetWidth;
          if (isFirstCalculation) {
            this.initialTabsWidth.push({
              id: element.nativeElement.id,
              width
            });
          }
          if (this.byQuantityFunction && index > this.byQuantityFunction - 1) {
            initDropdown = true;
          }
          if (!initDropdown && (this.sumOfWidth + width <= screenSize || screenSize < 1 || index < this.byQuantityFunction)) {
            if (!element.nativeElement.hidden) {
              listTabButton.push(element);
              this.sumOfWidth += width;
            }
          } else {
            initDropdown = true;
            element.nativeElement.style.display = "none";
            element.nativeElement.hidden = true;
          }
          if (element.nativeElement.hidden && index <= this.byQuantityFunction - 1) return;
          index++;
        });
      }
      this.quantityTabsButton = this.byQuantityFunction || listTabButton.length;
    }
  }
  updateTabsState(initialState = false, tabs) {
    if (tabs && this.initCheckChangesTab) {
      this.checkChangesTabs();
    }
    const allVisibleItems = this.tabButton?.filter((tab) => !tab.nativeElement.hidden);
    this.defaultLastTabWidth = allVisibleItems?.[allVisibleItems.length - 1]?.nativeElement.offsetWidth;
    if (!this.defaultLastTabWidth) {
      return;
    }
    this.executeTabsState(initialState, allVisibleItems?.[allVisibleItems.length - 1]?.nativeElement.id);
  }
  checkChangesTabs() {
    let index = 0;
    this.tabButton.forEach((tab, indexTab) => {
      const currentTab = this.initialTabsWidth.find((t) => t.id === tab.nativeElement.id);
      const quantityTabs = this.byQuantityFunction || this.quantityTabsButton;
      if (tab.nativeElement.hidden && !this.tabsChildren["_results"][indexTab]?.hide) {
        tab.nativeElement.style.display = "inline-block";
        tab.nativeElement.hidden = false;
      }
      if (!currentTab) {
        this.initialTabsWidth.push({
          id: tab.nativeElement.id,
          width: tab.nativeElement.offsetWidth
        });
        if (index > quantityTabs) {
          tab.nativeElement.style.display = "none";
          tab.nativeElement.hidden = true;
        }
        this.tabsChildren["_results"] = this.tabsChildren["_results"].filter((item) => !item.removed);
      }
      if (tab.nativeElement.hidden && index <= quantityTabs) return;
      index++;
    });
    this.calculateTabs(true);
  }
  onTabActiveByDropdown(tab, eventEmitter = true) {
    if (tab.disabled) {
      this.onTabChangeState(tab);
      return;
    }
    const initialWidth = this.initialTabsWidth.find((t) => t.id === tab.id);
    this.changeTabPositionByDropdown(tab, true);
    const showedTabs = this.tabButton.toArray().filter((tab2) => !tab2.nativeElement.hidden);
    const lastTab = showedTabs[showedTabs.length - 1];
    const lastTabWidth = lastTab.nativeElement.offsetWidth;
    lastTab.nativeElement.style.display = "none";
    lastTab.nativeElement.hidden = true;
    const currentTabIndex = this.tabButton.toArray().findIndex((t) => t.nativeElement.id === tab.id);
    const currentTab = this.tabButton.toArray()[currentTabIndex].nativeElement;
    currentTab.hidden = false;
    currentTab.style.display = "inline-block";
    this.reorderTabs(tab, lastTab.nativeElement);
    tab.widthButton = lastTabWidth;
    if (initialWidth.width > lastTabWidth) {
      tab.showTooltip = true;
    }
    currentTab.style.width = `${lastTabWidth}px`;
    this.handleKeyboardNavigationTab();
    if (eventEmitter) {
      tab.click.emit(tab);
    }
  }
  /**
   * Função que atribui o número de tabs fora do dropdown.
   *
   * Para utilizá-la é necessário ter a instância do componente no DOM, podendo ser utilizado o ViewChild da seguinte forma:
   *
   * ```
   * import { PoContextTabsComponent } from '@po-ui/ng-components';
   *
   * ...
   *
   * @ViewChild('poContextTabs', { static: true }) poContextTabs: PoContextTabsComponent;
   *
   * changeQuantityTabs() {
   *   this.poContextTabs.setQuantityTabsButton(1); //Número de context-tabs
   * }
   * ```
   */
  setQuantityTabsButton(number) {
    let callAfterFunction = false;
    const currentQuantity = this.byQuantityFunction || this.quantityTabsButton;
    if (number > currentQuantity) {
      callAfterFunction = true;
    }
    this.byQuantityFunction = number;
    if (!callAfterFunction) {
      this.quantityTabsButton = number;
      this.calculateTabs(true);
    } else {
      this.afterRemoveTab(0);
      this.quantityTabsButton = number;
    }
  }
  static ɵfac = /* @__PURE__ */ (() => {
    let ɵPoContextTabsComponent_BaseFactory;
    return function PoContextTabsComponent_Factory(__ngFactoryType__) {
      return (ɵPoContextTabsComponent_BaseFactory || (ɵPoContextTabsComponent_BaseFactory = ɵɵgetInheritedFactory(_PoContextTabsComponent)))(__ngFactoryType__ || _PoContextTabsComponent);
    };
  })();
  static ɵcmp = ɵɵdefineComponent({
    type: _PoContextTabsComponent,
    selectors: [["po-context-tabs"]],
    standalone: false,
    features: [ɵɵInheritDefinitionFeature],
    ngContentSelectors: _c12,
    decls: 8,
    vars: 3,
    consts: [["containerTabs", ""], ["tabButton", ""], ["tabDropdown", ""], [1, "po-tabs-container"], [1, "po-tabs-header"], ["role", "tablist", 1, "po-tabs-button-wrapper"], [4, "ngFor", "ngForOf", "ngForTrackBy"], ["class", "po-tab-dropdown", 3, "po-tab-dropdown-device", "p-label", "p-size", "p-tabs", "p-change-state", "p-click", 4, "ngIf"], [1, "po-tabs-content"], ["class", "po-tab-button", 3, "p-active", "p-disabled", "p-show-tooltip", "p-hide", "p-id", "hidden", "p-hide-close", "p-label", "p-literals", "id", "keyup.enter", "p-activated", "p-change-state", "p-change-visible", "p-click", "p-close", 4, "ngIf"], [1, "po-tab-button", 3, "keyup.enter", "p-activated", "p-change-state", "p-change-visible", "p-click", "p-close", "p-active", "p-disabled", "p-show-tooltip", "p-hide", "p-id", "hidden", "p-hide-close", "p-label", "p-literals", "id"], [1, "po-tab-dropdown", 3, "p-change-state", "p-click", "p-label", "p-size", "p-tabs"]],
    template: function PoContextTabsComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵprojectionDef();
        ɵɵelementStart(0, "div", 3)(1, "div", 4, 0)(3, "div", 5);
        ɵɵtemplate(4, PoContextTabsComponent_ng_container_4_Template, 2, 1, "ng-container", 6)(5, PoContextTabsComponent_po_tab_dropdown_5_Template, 2, 5, "po-tab-dropdown", 7);
        ɵɵelementEnd()()();
        ɵɵelementStart(6, "div", 8);
        ɵɵprojection(7);
        ɵɵelementEnd();
      }
      if (rf & 2) {
        ɵɵadvance(4);
        ɵɵproperty("ngForOf", ctx.tabs)("ngForTrackBy", ctx.trackByFn);
        ɵɵadvance();
        ɵɵproperty("ngIf", ctx.isShowTabDropdown);
      }
    },
    dependencies: [NgForOf, NgIf, PoTabDropdownComponent, PoContextTabButtonComponent],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoContextTabsComponent, [{
    type: Component,
    args: [{
      selector: "po-context-tabs",
      standalone: false,
      template: '<div class="po-tabs-container">\n  <div #containerTabs class="po-tabs-header">\n    <div class="po-tabs-button-wrapper" role="tablist">\n      <ng-container *ngFor="let tab of tabs; trackBy: trackByFn">\n        <po-context-tab-button\n          *ngIf="!tab.removed"\n          #tabButton\n          class="po-tab-button"\n          [p-active]="tab.active"\n          [p-disabled]="tab.disabled"\n          [p-show-tooltip]="tab.showTooltip"\n          [p-hide]="tab.hide"\n          [p-id]="tab.id"\n          [hidden]="tab.hide"\n          [p-hide-close]="tab.hideClose"\n          [attr.disabled]="tab.disabled"\n          [p-label]="tab.label"\n          [p-literals]="literals"\n          [id]="tab.id"\n          (keyup.enter)="closeListbox()"\n          (p-activated)="onTabActive(tab)"\n          (p-change-state)="onTabChangeState(tab)"\n          (p-change-visible)="onChangeVisibilityTab(tab)"\n          (p-click)="selectedTab(tab)"\n          (p-close)="closeTab(tab)"\n        >\n        </po-context-tab-button>\n      </ng-container>\n\n      <po-tab-dropdown\n        #tabDropdown\n        *ngIf="isShowTabDropdown"\n        class="po-tab-dropdown"\n        [class.po-tab-dropdown-device]="!tabsDefault?.length"\n        [p-label]="literals.moreTabs"\n        [p-size]="size"\n        [p-tabs]="overflowedTabs"\n        (p-change-state)="onTabChangeState($event)"\n        (p-click)="onTabActiveByDropdown($event)"\n      >\n      </po-tab-dropdown>\n    </div>\n  </div>\n</div>\n<div class="po-tabs-content">\n  <ng-content></ng-content>\n</div>\n'
    }]
  }], null, null);
})();
var PoContextTabsModule = class _PoContextTabsModule {
  static ɵfac = function PoContextTabsModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoContextTabsModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoContextTabsModule,
    declarations: [PoContextTabsComponent, PoContextTabButtonComponent],
    imports: [CommonModule, PoIconModule, PoTooltipModule, PoTabsModule],
    exports: [PoContextTabsComponent]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [CommonModule, PoIconModule, PoTooltipModule, PoTabsModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoContextTabsModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, PoIconModule, PoTooltipModule, PoTabsModule],
      declarations: [PoContextTabsComponent, PoContextTabButtonComponent],
      exports: [PoContextTabsComponent]
    }]
  }], null, null);
})();
var poToolbarActionsIconDefault = "ICON_MORE";
var PoToolbarActionsComponent = class _PoToolbarActionsComponent {
  /** Define uma lista de ações. */
  actions;
  _actionsIcon = poToolbarActionsIconDefault;
  /** Define o ícone das ações. */
  set actionsIcon(icon) {
    this._actionsIcon = isTypeof(icon, "string") || icon instanceof TemplateRef ? icon : poToolbarActionsIconDefault;
  }
  get actionsIcon() {
    return this._actionsIcon;
  }
  static ɵfac = function PoToolbarActionsComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoToolbarActionsComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoToolbarActionsComponent,
    selectors: [["po-toolbar-actions"]],
    inputs: {
      actions: [0, "p-actions", "actions"],
      actionsIcon: [0, "p-actions-icon", "actionsIcon"]
    },
    standalone: false,
    decls: 6,
    vars: 3,
    consts: [["toolbarActions", ""], ["popup", ""], [1, "po-toolbar-actions", 3, "click"], [1, "po-toolbar-icon", "po-clickable", 3, "p-icon"], ["p-size", "medium", 3, "p-actions", "p-target"]],
    template: function PoToolbarActionsComponent_Template(rf, ctx) {
      if (rf & 1) {
        const _r1 = ɵɵgetCurrentView();
        ɵɵelementStart(0, "div", 2);
        ɵɵlistener("click", function PoToolbarActionsComponent_Template_div_click_0_listener() {
          ɵɵrestoreView(_r1);
          const popup_r2 = ɵɵreference(5);
          return ɵɵresetView(popup_r2.toggle());
        });
        ɵɵelementStart(1, "span", null, 0);
        ɵɵelement(3, "po-icon", 3);
        ɵɵelementEnd()();
        ɵɵelement(4, "po-popup", 4, 1);
      }
      if (rf & 2) {
        const toolbarActions_r3 = ɵɵreference(2);
        ɵɵadvance(3);
        ɵɵproperty("p-icon", ctx.actionsIcon);
        ɵɵadvance();
        ɵɵproperty("p-actions", ctx.actions)("p-target", toolbarActions_r3);
      }
    },
    dependencies: [PoPopupComponent, PoIconComponent],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoToolbarActionsComponent, [{
    type: Component,
    args: [{
      selector: "po-toolbar-actions",
      standalone: false,
      template: '<div class="po-toolbar-actions" (click)="popup.toggle()">\n  <span #toolbarActions>\n    <po-icon class="po-toolbar-icon po-clickable" [p-icon]="actionsIcon"></po-icon>\n  </span>\n</div>\n\n<po-popup #popup p-size="medium" [p-actions]="actions" [p-target]="toolbarActions"> </po-popup>\n'
    }]
  }], null, {
    actions: [{
      type: Input,
      args: ["p-actions"]
    }],
    actionsIcon: [{
      type: Input,
      args: ["p-actions-icon"]
    }]
  });
})();
var PoToolbarBaseComponent = class _PoToolbarBaseComponent {
  /**
   * @optional
   *
   * @description
   *
   * Define uma lista de ações que serão exibidas ao clicar no ícone declarado em `p-actions-icon`.
   */
  actions;
  /**
   * @optional
   *
   * @description
   *
   * Define um [ícone](https://po-ui.io/icons) para a propriedade `p-actions`.
   *
   * É possível usar qualquer um dos ícones da [Biblioteca de ícones](https://po-ui.io/icons). conforme exemplo abaixo:
   * ```
   * <po-toolbar p-actions-icon="an an-user" [p-actions]="actions"></po-toolbar>
   * ```
   * Também é possível utilizar outras fontes de ícones, por exemplo a biblioteca *Font Awesome*, da seguinte forma:
   * ```
   * <po-toolbar p-actions-icon="far fa-comment-alt" [p-actions]="actions"></po-toolbar>
   * ```
   * Outra opção seria a customização do ícone através do `TemplateRef`, conforme exemplo abaixo:
   * ```
   * <po-toolbar [p-actions-icon]="template" [p-actions]="actions"></po-toolbar>
   *
   * <ng-template #template>
   *  <ion-icon style="font-size: inherit" name="heart"></ion-icon>
   * </ng-template>
   * ```
   * > Para o ícone enquadrar corretamente, deve-se utilizar `font-size: inherit` caso o ícone utilizado não aplique-o.
   *
   * > Caso não haja ações definidas em `p-actions`, o ícone não será exibido.
   *
   * @default `an-dots-three`
   */
  actionsIcon;
  /** Define o objeto que será o cabeçalho da lista de ações com as informações do perfil. */
  profile;
  /** Define uma lista de ações que serão exibidas ao clicar no ícone do perfil. */
  profileActions;
  /** Se falso, oculta o ícone de notificações. */
  showNotification = true;
  /** Título do *toolbar* e aplicação. */
  title;
  /**
   * @optional
   *
   * @description
   *
   * Lista de ações da notificação.
   */
  notificationActions;
  _notificationNumber;
  /**
   * @optional
   *
   * @description
   *
   * Número de notificações.
   */
  set notificationNumber(value) {
    this._notificationNumber = convertToInt(value, 0);
  }
  get notificationNumber() {
    return this._notificationNumber;
  }
  get isShowProfile() {
    return !!(this.profile || this.profileActions);
  }
  static ɵfac = function PoToolbarBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoToolbarBaseComponent)();
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoToolbarBaseComponent,
    inputs: {
      actions: [0, "p-actions", "actions"],
      actionsIcon: [0, "p-actions-icon", "actionsIcon"],
      profile: [0, "p-profile", "profile"],
      profileActions: [0, "p-profile-actions", "profileActions"],
      showNotification: [0, "p-show-notification", "showNotification"],
      title: [0, "p-title", "title"],
      notificationActions: [0, "p-notification-actions", "notificationActions"],
      notificationNumber: [0, "p-notification-number", "notificationNumber"]
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoToolbarBaseComponent, [{
    type: Directive
  }], null, {
    actions: [{
      type: Input,
      args: ["p-actions"]
    }],
    actionsIcon: [{
      type: Input,
      args: ["p-actions-icon"]
    }],
    profile: [{
      type: Input,
      args: ["p-profile"]
    }],
    profileActions: [{
      type: Input,
      args: ["p-profile-actions"]
    }],
    showNotification: [{
      type: Input,
      args: ["p-show-notification"]
    }],
    title: [{
      type: Input,
      args: ["p-title"]
    }],
    notificationActions: [{
      type: Input,
      args: ["p-notification-actions"]
    }],
    notificationNumber: [{
      type: Input,
      args: ["p-notification-number"]
    }]
  });
})();
var PoToolbarNotificationComponent = class _PoToolbarNotificationComponent {
  cdr = inject(ChangeDetectorRef);
  notificationRef;
  notificationActions;
  _notificationNumber = 0;
  set notificationNumber(value) {
    this._notificationNumber = Number.isInteger(value) ? value : 0;
  }
  get notificationNumber() {
    return this._notificationNumber;
  }
  ngAfterViewInit() {
    this.cdr.detectChanges();
  }
  static ɵfac = function PoToolbarNotificationComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoToolbarNotificationComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoToolbarNotificationComponent,
    selectors: [["po-toolbar-notification"]],
    viewQuery: function PoToolbarNotificationComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c177, 5, ElementRef);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.notificationRef = _t.first);
      }
    },
    inputs: {
      notificationActions: [0, "p-notification-actions", "notificationActions"],
      notificationNumber: [0, "p-notification-number", "notificationNumber"]
    },
    standalone: false,
    features: [ɵɵProvidersFeature([PoControlPositionService])],
    decls: 6,
    vars: 3,
    consts: [["notification", ""], ["popup", ""], [1, "po-toolbar-notification", "po-clickable", 3, "click"], ["p-icon", "ICON_NOTIFICATION", 1, "po-toolbar-icon"], [1, "po-toolbar-notification-badge"], ["p-size", "medium", 3, "p-actions", "p-target"]],
    template: function PoToolbarNotificationComponent_Template(rf, ctx) {
      if (rf & 1) {
        const _r1 = ɵɵgetCurrentView();
        ɵɵelementStart(0, "div", 2);
        ɵɵlistener("click", function PoToolbarNotificationComponent_Template_div_click_0_listener() {
          ɵɵrestoreView(_r1);
          const popup_r2 = ɵɵreference(5);
          return ɵɵresetView(popup_r2.toggle());
        });
        ɵɵelement(1, "po-icon", 3, 0);
        ɵɵconditionalCreate(3, PoToolbarNotificationComponent_Conditional_3_Template, 2, 1, "div", 4);
        ɵɵelementEnd();
        ɵɵelement(4, "po-popup", 5, 1);
      }
      if (rf & 2) {
        ɵɵadvance(3);
        ɵɵconditional(ctx.notificationNumber ? 3 : -1);
        ɵɵadvance();
        ɵɵproperty("p-actions", ctx.notificationActions)("p-target", ctx.notificationRef);
      }
    },
    dependencies: [PoPopupComponent, PoIconComponent],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoToolbarNotificationComponent, [{
    type: Component,
    args: [{
      selector: "po-toolbar-notification",
      providers: [PoControlPositionService],
      standalone: false,
      template: '<div class="po-toolbar-notification po-clickable" (click)="popup.toggle()">\n  <po-icon #notification p-icon="ICON_NOTIFICATION" class="po-toolbar-icon"></po-icon>\n  @if (notificationNumber) {\n    <div class="po-toolbar-notification-badge">\n      {{ notificationNumber }}\n    </div>\n  }\n</div>\n\n<po-popup #popup p-size="medium" [p-actions]="notificationActions" [p-target]="notificationRef"> </po-popup>\n'
    }]
  }], null, {
    notificationRef: [{
      type: ViewChild,
      args: ["notification", {
        read: ElementRef
      }]
    }],
    notificationActions: [{
      type: Input,
      args: ["p-notification-actions"]
    }],
    notificationNumber: [{
      type: Input,
      args: ["p-notification-number"]
    }]
  });
})();
var PoToolbarProfileComponent = class _PoToolbarProfileComponent {
  /** Objeto que implementa a interface `PoToolbarProfile`. */
  profile;
  /** Define uma lista de ações. */
  profileActions;
  get profileAvatar() {
    return this.profile ? this.profile.avatar : void 0;
  }
  static ɵfac = function PoToolbarProfileComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoToolbarProfileComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoToolbarProfileComponent,
    selectors: [["po-toolbar-profile"]],
    inputs: {
      profile: [0, "p-profile", "profile"],
      profileActions: [0, "p-profile-actions", "profileActions"]
    },
    standalone: false,
    features: [ɵɵProvidersFeature([PoControlPositionService])],
    decls: 6,
    vars: 4,
    consts: [["profileElement", ""], ["popup", ""], [1, "po-toolbar-profile", "po-clickable", 3, "click"], ["p-size", "xs", 3, "p-src"], ["p-size", "medium", 3, "p-actions", "p-target"], ["p-popup-header-template", "", 1, "po-toolbar-profile-item-header"], ["p-size", "sm", 1, "po-toolbar-profile-item-avatar", 3, "p-src"], [1, "po-toolbar-profile-item-header-title"], [1, "po-toolbar-profile-item-header-subtitle"]],
    template: function PoToolbarProfileComponent_Template(rf, ctx) {
      if (rf & 1) {
        const _r1 = ɵɵgetCurrentView();
        ɵɵelementStart(0, "div", 2, 0);
        ɵɵlistener("click", function PoToolbarProfileComponent_Template_div_click_0_listener() {
          ɵɵrestoreView(_r1);
          const popup_r2 = ɵɵreference(4);
          return ɵɵresetView(popup_r2.toggle());
        });
        ɵɵelement(2, "po-avatar", 3);
        ɵɵelementEnd();
        ɵɵelementStart(3, "po-popup", 4, 1);
        ɵɵconditionalCreate(5, PoToolbarProfileComponent_Conditional_5_Template, 6, 3, "div", 5);
        ɵɵelementEnd();
      }
      if (rf & 2) {
        const profileElement_r4 = ɵɵreference(1);
        ɵɵadvance(2);
        ɵɵproperty("p-src", ctx.profileAvatar);
        ɵɵadvance();
        ɵɵproperty("p-actions", ctx.profileActions)("p-target", profileElement_r4);
        ɵɵadvance(2);
        ɵɵconditional((ctx.profile == null ? null : ctx.profile.title) ? 5 : -1);
      }
    },
    dependencies: [PoAvatarComponent, PoPopupComponent],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoToolbarProfileComponent, [{
    type: Component,
    args: [{
      selector: "po-toolbar-profile",
      providers: [PoControlPositionService],
      standalone: false,
      template: '<div #profileElement class="po-toolbar-profile po-clickable" (click)="popup.toggle()">\n  <po-avatar p-size="xs" [p-src]="profileAvatar"></po-avatar>\n</div>\n\n<po-popup #popup p-size="medium" [p-actions]="profileActions" [p-target]="profileElement">\n  @if (profile?.title) {\n    <div p-popup-header-template class="po-toolbar-profile-item-header">\n      <po-avatar class="po-toolbar-profile-item-avatar" p-size="sm" [p-src]="profileAvatar"> </po-avatar>\n      <div>\n        <div class="po-toolbar-profile-item-header-title">\n          {{ profile.title }}\n        </div>\n        @if (profile.subtitle) {\n          <div class="po-toolbar-profile-item-header-subtitle">\n            {{ profile.subtitle }}\n          </div>\n        }\n      </div>\n    </div>\n  }\n</po-popup>\n'
    }]
  }], null, {
    profile: [{
      type: Input,
      args: ["p-profile"]
    }],
    profileActions: [{
      type: Input,
      args: ["p-profile-actions"]
    }]
  });
})();
var PoToolbarComponent = class _PoToolbarComponent extends PoToolbarBaseComponent {
  titleService = inject(Title);
  ngOnInit() {
    this.setTitle(this.title);
  }
  setTitle(newTitle) {
    this.titleService.setTitle(newTitle);
  }
  static ɵfac = /* @__PURE__ */ (() => {
    let ɵPoToolbarComponent_BaseFactory;
    return function PoToolbarComponent_Factory(__ngFactoryType__) {
      return (ɵPoToolbarComponent_BaseFactory || (ɵPoToolbarComponent_BaseFactory = ɵɵgetInheritedFactory(_PoToolbarComponent)))(__ngFactoryType__ || _PoToolbarComponent);
    };
  })();
  static ɵcmp = ɵɵdefineComponent({
    type: _PoToolbarComponent,
    selectors: [["po-toolbar"]],
    standalone: false,
    features: [ɵɵInheritDefinitionFeature],
    decls: 7,
    vars: 4,
    consts: [[1, "po-toolbar"], [1, "po-toolbar-title"], [1, "po-toolbar-group-icon"], [3, "p-actions", "p-actions-icon"], [3, "p-notification-actions", "p-notification-number"], [3, "p-profile", "p-profile-actions"]],
    template: function PoToolbarComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "div", 0)(1, "div", 1);
        ɵɵtext(2);
        ɵɵelementEnd();
        ɵɵelementStart(3, "div", 2);
        ɵɵconditionalCreate(4, PoToolbarComponent_Conditional_4_Template, 1, 2, "po-toolbar-actions", 3);
        ɵɵconditionalCreate(5, PoToolbarComponent_Conditional_5_Template, 1, 2, "po-toolbar-notification", 4);
        ɵɵconditionalCreate(6, PoToolbarComponent_Conditional_6_Template, 1, 2, "po-toolbar-profile", 5);
        ɵɵelementEnd()();
      }
      if (rf & 2) {
        ɵɵadvance(2);
        ɵɵtextInterpolate(ctx.title);
        ɵɵadvance(2);
        ɵɵconditional(ctx.actions && ctx.actions.length ? 4 : -1);
        ɵɵadvance();
        ɵɵconditional(ctx.showNotification && ctx.notificationActions ? 5 : -1);
        ɵɵadvance();
        ɵɵconditional(ctx.isShowProfile ? 6 : -1);
      }
    },
    dependencies: [PoToolbarActionsComponent, PoToolbarNotificationComponent, PoToolbarProfileComponent],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoToolbarComponent, [{
    type: Component,
    args: [{
      selector: "po-toolbar",
      standalone: false,
      template: '<div class="po-toolbar">\n  <div class="po-toolbar-title">{{ title }}</div>\n\n  <div class="po-toolbar-group-icon">\n    @if (actions && actions.length) {\n      <po-toolbar-actions [p-actions]="actions" [p-actions-icon]="actionsIcon"> </po-toolbar-actions>\n    }\n\n    @if (showNotification && notificationActions) {\n      <po-toolbar-notification\n        [p-notification-actions]="notificationActions"\n        [p-notification-number]="notificationNumber"\n      >\n      </po-toolbar-notification>\n    }\n\n    @if (isShowProfile) {\n      <po-toolbar-profile [p-profile]="profile" [p-profile-actions]="profileActions"> </po-toolbar-profile>\n    }\n  </div>\n</div>\n'
    }]
  }], null, null);
})();
var PoToolbarModule = class _PoToolbarModule {
  static ɵfac = function PoToolbarModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoToolbarModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoToolbarModule,
    declarations: [PoToolbarActionsComponent, PoToolbarComponent, PoToolbarNotificationComponent, PoToolbarProfileComponent],
    imports: [CommonModule, PoAvatarModule, PoPopupModule, PoIconModule],
    exports: [PoToolbarComponent]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [CommonModule, PoAvatarModule, PoPopupModule, PoIconModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoToolbarModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, PoAvatarModule, PoPopupModule, PoIconModule],
      declarations: [PoToolbarActionsComponent, PoToolbarComponent, PoToolbarNotificationComponent, PoToolbarProfileComponent],
      exports: [PoToolbarComponent],
      providers: [],
      schemas: []
    }]
  }], null, null);
})();
var poTreeViewMaxLevel = 4;
var PoTreeViewBaseComponent = class _PoTreeViewBaseComponent {
  /**
   * @optional
   *
   * @description
   *
   * Ação que será disparada ao colapsar um item.
   *
   * > Como parâmetro o componente envia o item colapsado.
   */
  collapsed = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Ação que será disparada ao expandir um item.
   *
   * > Como parâmetro o componente envia o item expandido.
   */
  expanded = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Ação que será disparada ao selecionar um item.
   *
   * > Como parâmetro o componente envia o item selecionado.
   */
  selected = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Ação que será disparada ao desfazer a seleção de um item.
   *
   * > Como parâmetro o componente envia o item que foi desmarcado.
   */
  unselected = new EventEmitter();
  _componentsSize = void 0;
  _initialComponentsSize = void 0;
  _items = [];
  _selectable = false;
  _maxLevel = poTreeViewMaxLevel;
  _singleSelect = false;
  // armazena o value do item selecionado
  selectedValue;
  /**
   * @optional
   *
   * @description
   *
   * Define o tamanho dos componentes de formulário:
   * - `small`: aplica a medida small de cada componente (disponível apenas para acessibilidade AA).
   * - `medium`: aplica a medida medium de cada componente.
   *
   * > Caso a acessibilidade AA não esteja configurada, o tamanho `medium` será mantido.
   * Para mais detalhes, consulte a documentação do [po-theme](https://po-ui.io/documentation/po-theme).
   *
   * @default `medium`
   */
  set componentsSize(value) {
    this._initialComponentsSize = value;
    this.applySizeBasedOnA11y();
  }
  get componentsSize() {
    return this._componentsSize ?? getDefaultSizeFn(PoFieldSize);
  }
  /**
   * Lista de itens do tipo `PoTreeViewItem` que será renderizada pelo componente.
   */
  inputedItems;
  set items(value) {
    this._items = Array.isArray(value) ? this.getItemsByMaxLevel(value) : [];
  }
  get items() {
    return this._items;
  }
  /**
   * @optional
   *
   * @description
   *
   * Habilita uma caixa de seleção para selecionar e/ou desmarcar um item da lista.
   *
   * @default false
   */
  set selectable(value) {
    this._selectable = convertToBoolean(value);
  }
  get selectable() {
    return this._selectable;
  }
  /**
   * @optional
   *
   * @description
   *
   * Habilita a seleção para item único atráves de po-radio.
   *
   * @default false
   */
  set singleSelect(value) {
    this._singleSelect = convertToBoolean(value);
  }
  get singleSelect() {
    return this._singleSelect;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define o máximo de níveis para o tree-view.
   *
   * > O valor padrão é 4
   *
   * @default 4
   */
  set maxLevel(value) {
    this._maxLevel = convertToInt(value, poTreeViewMaxLevel);
  }
  get maxLevel() {
    return this._maxLevel;
  }
  onThemeChange() {
    this.applySizeBasedOnA11y();
  }
  emitExpanded(treeViewItem) {
    const event = treeViewItem.expanded ? "expanded" : "collapsed";
    this[event].emit(__spreadValues({}, treeViewItem));
  }
  emitSelected(treeViewItem) {
    const event = treeViewItem.selected ? "selected" : "unselected";
    this.selectedValue = treeViewItem.value;
    const _a2 = treeViewItem, {
      subItems
    } = _a2, rest = __objRest(_a2, [
      "subItems"
    ]);
    const treeViewToEmit = this.singleSelect ? __spreadValues({}, rest) : treeViewItem;
    this.updateItemsOnSelect(treeViewToEmit);
    this[event].emit(__spreadValues({}, treeViewToEmit));
  }
  addChildItemInParent(childItem, parentItem) {
    if (!parentItem.subItems) {
      parentItem.subItems = [];
    }
    parentItem.subItems.push(childItem);
  }
  // caso houver parentItem:
  //  - expande o parentItem caso o filho estiver expandido;
  //  - adiciona o childItem no parentItem;
  //  - marca o parentItem caso conter subItems marcodos ou nulos;
  // Se não conter parentItem, adiciona o childItem no items.
  addItem(items, childItem, parentItem, isNewItem) {
    if (parentItem) {
      if (isNewItem) {
        this.expandParentItem(childItem, parentItem);
      }
      this.addChildItemInParent(childItem, parentItem);
      if (!this.singleSelect) {
        this.selectItemBySubItems(parentItem);
      }
      items.push(parentItem);
    } else {
      items.push(childItem);
    }
  }
  selectAllItems(items, isSelected) {
    items.forEach((item) => {
      if (item.subItems) {
        this.selectAllItems(item.subItems, isSelected);
      }
      item.selected = item.isSelectable !== false ? isSelected : false;
    });
  }
  selectItemBySubItems(item) {
    item.selected = this.everyItemSelected(item.subItems);
  }
  // retornará:
  //  - true: se todos os items estiverem marcados;
  //  - null: se no minimo um item esteja marcado ou nullo (indeterminate)
  //  - false: caso não corresponda em nenhuma das opções acima, no caso, nenhum marcado ou nulo;
  everyItemSelected(items = []) {
    const itemsLength = items.length;
    const lengthCheckedItems = items.filter((item) => item.selected).length;
    if (itemsLength && itemsLength === lengthCheckedItems) {
      return true;
    }
    const hasIndeterminateItems = items.filter((item) => item.selected || item.selected === null).length;
    if (hasIndeterminateItems) {
      return null;
    }
    return false;
  }
  // expande o item pai caso o filho estiver expandido.
  expandParentItem(childItem, parentItem) {
    if (childItem.expanded) {
      parentItem.expanded = true;
    }
  }
  getItemsByMaxLevel(items = [], level = 0, parentItem, newItems = []) {
    items.forEach((item) => {
      const _a2 = item, {
        subItems
      } = _a2, currentItem = __objRest(_a2, [
        "subItems"
      ]);
      if (level === this.maxLevel) {
        return;
      }
      if (Array.isArray(subItems)) {
        if (currentItem.selected) {
          this.selectAllItems(subItems, currentItem.selected);
        }
        this.getItemsByMaxLevel(subItems, ++level, currentItem);
        --level;
      }
      if (item.selected) {
        this.selectedValue = currentItem.value;
      }
      this.addItem(newItems, currentItem, parentItem, true);
    });
    return newItems;
  }
  getItemsWithParentSelected(items = [], parentItem, newItems = []) {
    items.forEach((item) => {
      const _a2 = item, {
        subItems
      } = _a2, currentItem = __objRest(_a2, [
        "subItems"
      ]);
      if (Array.isArray(subItems)) {
        this.getItemsWithParentSelected(subItems, currentItem);
      }
      this.addItem(newItems, currentItem, parentItem);
    });
    return newItems;
  }
  updateItemsOnSelect(selectedItem) {
    if (selectedItem.subItems && !this.singleSelect) {
      this.selectAllItems(selectedItem.subItems, selectedItem.selected);
    }
    this._items = this.getItemsWithParentSelected(this.items);
  }
  applySizeBasedOnA11y() {
    const size = validateSizeFn(this._initialComponentsSize, PoFieldSize);
    this._componentsSize = size;
  }
  static ɵfac = function PoTreeViewBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoTreeViewBaseComponent)();
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoTreeViewBaseComponent,
    hostVars: 1,
    hostBindings: function PoTreeViewBaseComponent_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("PoUiThemeChange", function PoTreeViewBaseComponent_PoUiThemeChange_HostBindingHandler() {
          return ctx.onThemeChange();
        }, ɵɵresolveWindow);
      }
      if (rf & 2) {
        ɵɵattribute("p-components-size", ctx.componentsSize);
      }
    },
    inputs: {
      componentsSize: [0, "p-components-size", "componentsSize"],
      inputedItems: [0, "p-items", "inputedItems"],
      selectable: [0, "p-selectable", "selectable"],
      singleSelect: [0, "p-single-select", "singleSelect"],
      maxLevel: [0, "p-max-level", "maxLevel"]
    },
    outputs: {
      collapsed: "p-collapsed",
      expanded: "p-expanded",
      selected: "p-selected",
      unselected: "p-unselected"
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoTreeViewBaseComponent, [{
    type: Directive
  }], null, {
    collapsed: [{
      type: Output,
      args: ["p-collapsed"]
    }],
    expanded: [{
      type: Output,
      args: ["p-expanded"]
    }],
    selected: [{
      type: Output,
      args: ["p-selected"]
    }],
    unselected: [{
      type: Output,
      args: ["p-unselected"]
    }],
    componentsSize: [{
      type: Input,
      args: ["p-components-size"]
    }, {
      type: HostBinding,
      args: ["attr.p-components-size"]
    }],
    inputedItems: [{
      type: Input,
      args: ["p-items"]
    }],
    selectable: [{
      type: Input,
      args: ["p-selectable"]
    }],
    singleSelect: [{
      type: Input,
      args: ["p-single-select"]
    }],
    maxLevel: [{
      type: Input,
      args: ["p-max-level"]
    }],
    onThemeChange: [{
      type: HostListener,
      args: ["window:PoUiThemeChange"]
    }]
  });
})();
var PoTreeViewService = class _PoTreeViewService {
  expandedEvent = new Subject();
  selectedEvent = new Subject();
  emitExpandedEvent(treeViewItem) {
    return this.expandedEvent.next(treeViewItem);
  }
  emitSelectedEvent(treeViewItem) {
    return this.selectedEvent.next(treeViewItem);
  }
  onExpand() {
    return this.expandedEvent.asObservable();
  }
  onSelect() {
    return this.selectedEvent.asObservable();
  }
  static ɵfac = function PoTreeViewService_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoTreeViewService)();
  };
  static ɵprov = ɵɵdefineInjectable({
    token: _PoTreeViewService,
    factory: _PoTreeViewService.ɵfac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoTreeViewService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
var PoTreeViewItemHeaderComponent = class _PoTreeViewItemHeaderComponent {
  inputCheckbox;
  componentsSize;
  item;
  selectable = false;
  singleSelect;
  expanded = new EventEmitter();
  selected = new EventEmitter();
  selectedValue;
  idRadio = `po-radio[${uuid()}]`;
  get hasSubItems() {
    return !!(this.item.subItems && this.item.subItems.length);
  }
  static ɵfac = function PoTreeViewItemHeaderComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoTreeViewItemHeaderComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoTreeViewItemHeaderComponent,
    selectors: [["po-tree-view-item-header"]],
    viewQuery: function PoTreeViewItemHeaderComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c178, 5);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.inputCheckbox = _t.first);
      }
    },
    inputs: {
      componentsSize: [0, "p-components-size", "componentsSize"],
      item: [0, "p-item", "item"],
      selectable: [0, "p-selectable", "selectable"],
      singleSelect: [0, "p-single-select", "singleSelect"],
      selectedValue: [0, "p-selected-value", "selectedValue"]
    },
    outputs: {
      expanded: "p-expanded",
      selected: "p-selected"
    },
    standalone: false,
    decls: 4,
    vars: 2,
    consts: [["inputRadio", ""], [1, "po-tree-view-item-header"], [1, "po-tree-view-item-header-button"], [1, "po-tree-view-item-header-label", 3, "po-tree-view-item-header-padding"], [1, "po-tree-view-item-header-button", 3, "click"], ["p-icon", "ICON_ARROW_DOWN", 1, "po-tree-view-item-header-button-icon"], [1, "po-tree-view-item-header-checkbox", 3, "po-tree-view-item-header-padding", "name", "ngModel", "p-label", "p-value", "p-checked", "p-disabled", "p-size"], [1, "po-tree-view-item-header-checkbox", 3, "po-tree-view-item-header-padding", "p-label", "ngModel", "p-disabled", "p-size"], [1, "po-tree-view-item-header-checkbox", 3, "ngModelChange", "p-change-selected", "name", "ngModel", "p-label", "p-value", "p-checked", "p-disabled", "p-size"], [1, "po-tree-view-item-header-checkbox", 3, "ngModelChange", "p-change", "p-label", "ngModel", "p-disabled", "p-size"], [1, "po-tree-view-item-header-label"]],
    template: function PoTreeViewItemHeaderComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "div", 1);
        ɵɵconditionalCreate(1, PoTreeViewItemHeaderComponent_Conditional_1_Template, 2, 2, "button", 2);
        ɵɵconditionalCreate(2, PoTreeViewItemHeaderComponent_Conditional_2_Template, 2, 1)(3, PoTreeViewItemHeaderComponent_Conditional_3_Template, 2, 3, "span", 3);
        ɵɵelementEnd();
      }
      if (rf & 2) {
        ɵɵadvance();
        ɵɵconditional(ctx.hasSubItems ? 1 : -1);
        ɵɵadvance();
        ɵɵconditional(ctx.selectable ? 2 : 3);
      }
    },
    dependencies: [NgControlStatus, NgModel, PoCheckboxComponent, PoRadioComponent, PoIconComponent],
    encapsulation: 2,
    changeDetection: 0
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoTreeViewItemHeaderComponent, [{
    type: Component,
    args: [{
      selector: "po-tree-view-item-header",
      changeDetection: ChangeDetectionStrategy.OnPush,
      standalone: false,
      template: '<div class="po-tree-view-item-header">\n  @if (hasSubItems) {\n    <button class="po-tree-view-item-header-button" (click)="expanded.emit($event)">\n      <po-icon\n        p-icon="ICON_ARROW_DOWN"\n        class="po-tree-view-item-header-button-icon"\n        [class.po-tree-view-item-header-button-icon-transform]="item.expanded"\n      >\n      </po-icon>\n    </button>\n  }\n\n  @if (selectable) {\n    @if (singleSelect) {\n      <po-radio\n        class="po-tree-view-item-header-checkbox"\n        [class.po-tree-view-item-header-padding]="!hasSubItems"\n        #inputRadio\n        [name]="idRadio"\n        [(ngModel)]="item.selected"\n        [p-label]="item.label"\n        [p-value]="item.value"\n        [p-checked]="item.value === selectedValue"\n        (p-change-selected)="selected.emit(item)"\n        [p-disabled]="item.isSelectable === false"\n        [p-size]="componentsSize"\n      >\n      </po-radio>\n    } @else {\n      <po-checkbox\n        class="po-tree-view-item-header-checkbox"\n        [class.po-tree-view-item-header-padding]="!hasSubItems"\n        [p-label]="item.label"\n        [(ngModel)]="item.selected"\n        (p-change)="selected.emit(item)"\n        [p-disabled]="item.isSelectable === false"\n        [p-size]="componentsSize"\n      >\n      </po-checkbox>\n    }\n  } @else {\n    <span class="po-tree-view-item-header-label" [class.po-tree-view-item-header-padding]="!hasSubItems">\n      {{ item.label }}\n    </span>\n  }\n</div>\n'
    }]
  }], null, {
    inputCheckbox: [{
      type: ViewChild,
      args: ["inputCheckbox"]
    }],
    componentsSize: [{
      type: Input,
      args: ["p-components-size"]
    }],
    item: [{
      type: Input,
      args: ["p-item"]
    }],
    selectable: [{
      type: Input,
      args: ["p-selectable"]
    }],
    singleSelect: [{
      type: Input,
      args: ["p-single-select"]
    }],
    expanded: [{
      type: Output,
      args: ["p-expanded"]
    }],
    selected: [{
      type: Output,
      args: ["p-selected"]
    }],
    selectedValue: [{
      type: Input,
      args: ["p-selected-value"]
    }]
  });
})();
var PoTreeViewItemComponent = class _PoTreeViewItemComponent {
  treeViewService = inject(PoTreeViewService);
  componentsSize;
  item;
  selectable;
  singleSelect;
  selectedValue;
  get hasSubItems() {
    return !!(this.item.subItems && this.item.subItems.length);
  }
  onClick(event) {
    event.preventDefault();
    event.stopPropagation();
    this.item.expanded = !this.item.expanded;
    this.treeViewService.emitExpandedEvent(__spreadValues({}, this.item));
  }
  onSelect(selectedItem) {
    this.treeViewService.emitSelectedEvent(__spreadValues({}, selectedItem));
  }
  trackByFunction(index) {
    return index;
  }
  static ɵfac = function PoTreeViewItemComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoTreeViewItemComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoTreeViewItemComponent,
    selectors: [["po-tree-view-item"]],
    inputs: {
      componentsSize: [0, "p-components-size", "componentsSize"],
      item: [0, "p-item", "item"],
      selectable: [0, "p-selectable", "selectable"],
      singleSelect: [0, "p-single-select", "singleSelect"],
      selectedValue: [0, "p-selected-value", "selectedValue"]
    },
    standalone: false,
    decls: 3,
    vars: 6,
    consts: [[1, "po-tree-view-item"], [3, "p-expanded", "p-selected", "p-components-size", "p-item", "p-selectable", "p-single-select", "p-selected-value"], [1, "po-tree-view-item-group"], [3, "p-components-size", "p-item", "p-selectable", "p-single-select", "p-selected-value"]],
    template: function PoTreeViewItemComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "li", 0)(1, "po-tree-view-item-header", 1);
        ɵɵlistener("p-expanded", function PoTreeViewItemComponent_Template_po_tree_view_item_header_p_expanded_1_listener($event) {
          return ctx.onClick($event);
        })("p-selected", function PoTreeViewItemComponent_Template_po_tree_view_item_header_p_selected_1_listener() {
          return ctx.onSelect(ctx.item);
        });
        ɵɵelementEnd();
        ɵɵconditionalCreate(2, PoTreeViewItemComponent_Conditional_2_Template, 2, 1, "ul", 2);
        ɵɵelementEnd();
      }
      if (rf & 2) {
        ɵɵadvance();
        ɵɵproperty("p-components-size", ctx.componentsSize)("p-item", ctx.item)("p-selectable", ctx.selectable)("p-single-select", ctx.singleSelect)("p-selected-value", ctx.selectedValue);
        ɵɵadvance();
        ɵɵconditional(ctx.hasSubItems ? 2 : -1);
      }
    },
    dependencies: [_PoTreeViewItemComponent, PoTreeViewItemHeaderComponent],
    encapsulation: 2,
    data: {
      animation: [trigger("toggleBody", [transition(":enter", [style({
        "overflow-y": "hidden",
        visibility: "hidden",
        opacity: 0,
        height: "0"
      }), animate(200, style({
        height: "*"
      })), animate(100, style({
        opacity: 1
      }))]), transition(":leave", [style({
        "overflow-y": "hidden",
        visibility: "visible",
        opacity: 1,
        height: "*"
      }), animate(200, style({
        height: 0
      })), animate(100, style({
        opacity: 0
      }))])])]
    },
    changeDetection: 0
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoTreeViewItemComponent, [{
    type: Component,
    args: [{
      selector: "po-tree-view-item",
      changeDetection: ChangeDetectionStrategy.OnPush,
      animations: [trigger("toggleBody", [transition(":enter", [style({
        "overflow-y": "hidden",
        visibility: "hidden",
        opacity: 0,
        height: "0"
      }), animate(200, style({
        height: "*"
      })), animate(100, style({
        opacity: 1
      }))]), transition(":leave", [style({
        "overflow-y": "hidden",
        visibility: "visible",
        opacity: 1,
        height: "*"
      }), animate(200, style({
        height: 0
      })), animate(100, style({
        opacity: 0
      }))])])],
      standalone: false,
      template: '<li class="po-tree-view-item">\n  <po-tree-view-item-header\n    [p-components-size]="componentsSize"\n    [p-item]="item"\n    [p-selectable]="selectable"\n    [p-single-select]="singleSelect"\n    [p-selected-value]="selectedValue"\n    (p-expanded)="onClick($event)"\n    (p-selected)="onSelect(item)"\n  >\n  </po-tree-view-item-header>\n\n  @if (hasSubItems) {\n    <ul class="po-tree-view-item-group">\n      @if (item.expanded) {\n        <div @toggleBody>\n          @for (subItem of item.subItems; track trackByFunction($index)) {\n            <po-tree-view-item\n              [p-components-size]="componentsSize"\n              [p-item]="subItem"\n              [p-selectable]="selectable"\n              [p-single-select]="singleSelect"\n              [p-selected-value]="selectedValue"\n            >\n            </po-tree-view-item>\n          }\n        </div>\n      }\n    </ul>\n  }\n</li>\n'
    }]
  }], null, {
    componentsSize: [{
      type: Input,
      args: ["p-components-size"]
    }],
    item: [{
      type: Input,
      args: ["p-item"]
    }],
    selectable: [{
      type: Input,
      args: ["p-selectable"]
    }],
    singleSelect: [{
      type: Input,
      args: ["p-single-select"]
    }],
    selectedValue: [{
      type: Input,
      args: ["p-selected-value"]
    }]
  });
})();
var PoTreeViewComponent = class _PoTreeViewComponent extends PoTreeViewBaseComponent {
  treeViewService = inject(PoTreeViewService);
  get hasItems() {
    return !!(this.items && this.items.length);
  }
  ngOnInit() {
    this.treeViewService.onExpand().subscribe((treeViewItem) => {
      this.emitExpanded(treeViewItem);
    });
    this.treeViewService.onSelect().subscribe((treeViewItem) => {
      this.emitSelected(treeViewItem);
    });
  }
  ngOnChanges(changes) {
    if (changes?.["inputedItems"]) {
      this.items = this.inputedItems;
    }
  }
  trackByFunction(index) {
    return index;
  }
  static ɵfac = /* @__PURE__ */ (() => {
    let ɵPoTreeViewComponent_BaseFactory;
    return function PoTreeViewComponent_Factory(__ngFactoryType__) {
      return (ɵPoTreeViewComponent_BaseFactory || (ɵPoTreeViewComponent_BaseFactory = ɵɵgetInheritedFactory(_PoTreeViewComponent)))(__ngFactoryType__ || _PoTreeViewComponent);
    };
  })();
  static ɵcmp = ɵɵdefineComponent({
    type: _PoTreeViewComponent,
    selectors: [["po-tree-view"]],
    standalone: false,
    features: [ɵɵProvidersFeature([PoTreeViewService]), ɵɵInheritDefinitionFeature, ɵɵNgOnChangesFeature],
    decls: 1,
    vars: 1,
    consts: [["p-no-padding", ""], [1, "po-tree-view"], [3, "p-components-size", "p-item", "p-selectable", "p-single-select", "p-selected-value"]],
    template: function PoTreeViewComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵconditionalCreate(0, PoTreeViewComponent_Conditional_0_Template, 4, 0, "po-container", 0);
      }
      if (rf & 2) {
        ɵɵconditional(ctx.hasItems ? 0 : -1);
      }
    },
    dependencies: [PoContainerComponent, PoTreeViewItemComponent],
    encapsulation: 2,
    changeDetection: 0
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoTreeViewComponent, [{
    type: Component,
    args: [{
      selector: "po-tree-view",
      changeDetection: ChangeDetectionStrategy.OnPush,
      providers: [PoTreeViewService],
      standalone: false,
      template: '@if (hasItems) {\n  <po-container p-no-padding>\n    <ul class="po-tree-view">\n      @for (item of items; track trackByFunction($index)) {\n        <po-tree-view-item\n          [p-components-size]="componentsSize"\n          [p-item]="item"\n          [p-selectable]="selectable"\n          [p-single-select]="singleSelect"\n          [p-selected-value]="selectedValue"\n        >\n        </po-tree-view-item>\n      }\n    </ul>\n  </po-container>\n}\n'
    }]
  }], null, null);
})();
var PoTreeViewModule = class _PoTreeViewModule {
  static ɵfac = function PoTreeViewModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoTreeViewModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoTreeViewModule,
    declarations: [PoTreeViewComponent, PoTreeViewItemComponent, PoTreeViewItemHeaderComponent],
    imports: [CommonModule, FormsModule, PoContainerModule, PoFieldModule, PoIconModule],
    exports: [PoTreeViewComponent]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [CommonModule, FormsModule, PoContainerModule, PoFieldModule, PoIconModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoTreeViewModule, [{
    type: NgModule,
    args: [{
      declarations: [PoTreeViewComponent, PoTreeViewItemComponent, PoTreeViewItemHeaderComponent],
      exports: [PoTreeViewComponent],
      imports: [CommonModule, FormsModule, PoContainerModule, PoFieldModule, PoIconModule]
    }]
  }], null, null);
})();
var poWidgetLiteralsDefault = {
  en: {
    help: "Help",
    configuration: "Settings"
  },
  es: {
    help: "Ayuda",
    configuration: "Ajustes"
  },
  pt: {
    help: "Ajuda",
    configuration: "Configurações"
  },
  ru: {
    help: "Помощь",
    configuration: "Настройки"
  }
};
var PO_WIDGET_TAG_POSITION = ["right", "top", "bottom"];
var PO_WIDGET_TAG_POSITION_DEFAULT = "right";
var PoWidgetBaseComponent = class _PoWidgetBaseComponent {
  _size = void 0;
  _initialSize = void 0;
  /** Descrição da segunda ação. */
  /**
   * @optional
   *
   * @description
   * Define o label e exibe a ação secundária no footer do componente.
   *
   * > Exibida apenas quando `p-primary-label` estiver definida.
   */
  secondaryLabel;
  /**
   * @optional
   *
   * @description
   *
   * Caso verdadeiro o botão da ação `p-primary-label` ativará o modo `danger`.
   *
   * > Incompatível com o tipo **tertiary** da propriedade `p-kind-primary-action`.
   *
   * @default `false`
   */
  dangerPrimaryAction = false;
  /**
   * @optional
   *
   * @description
   *
   * Caso verdadeiro o botão da ação `p-secondary-label` ativará o modo `danger`.
   *
   * > Incompatível com o tipo **tertiary** da propriedade `p-kind-primary-action`.
   *
   * @default `false`
   */
  dangerSecondaryAction = false;
  /**
   * @optional
   *
   * @description
   *
   * Define o estilo do botão da ação `p-primary-label`, conforme o enum `PoButtonKind`.
   *
   * @default `tertiary`
   */
  kindPrimaryAction;
  /**
   * @optional
   *
   * @description
   *
   * Define o estilo do botão da ação `p-secondary-label`, conforme o enum `PoButtonKind`.
   *
   * @default `tertiary`
   */
  kindSecondaryAction;
  /**
   * @optional
   *
   * @description
   *
   * Label da tag exibida no header.
   *
   * > Quando a tag atingir uma largura máxima de 15rem (240px), será truncado com reticências.
   * O conteúdo completo poderá ser visualizado ao passar o mouse sobre a tag, por meio do tooltip.
   */
  tagLabel;
  /**
   * @optional
   *
   * @description
   *
   * Define o tipo da `p-tag`, conforme o enum **PoTagType**.
   *
   * Valores válidos:
   *  - `success`: cor verde utilizada para simbolizar sucesso ou êxito.
   *  - `warning`: cor amarela que representa aviso ou advertência.
   *  - `danger`: cor vermelha para erro ou aviso crítico.
   *  - `info`: cor azul claro que caracteriza conteúdo informativo.
   *  - `neutral`: cor cinza claro para uso geral.
   *
   * @default `success`
   */
  tagType;
  /**
   * @optional
   *
   * @description
   *
   * Define o ícone exibido ao lado do label da `p-tag`.
   *
   * É possível usar qualquer um dos ícones da [Biblioteca de ícones PO UI](https://po-ui.io/icons), conforme exemplo:
   * ```
   * <po-widget p-tag-icon="an an-user"></po-widget>
   * ```
   * Também é possível utilizar outras fontes de ícones, por exemplo a biblioteca *Font Awesome*, desde que a biblioteca
   * esteja carregada no projeto:
   * ```
   * <po-widget p-tag-icon="fa fa-podcast"></po-widget>
   * ```
   *
   * Outra opção seria a customização do ícone através do `TemplateRef`, conforme exemplo abaixo:
   * ```
   * <po-widget [p-tag-icon]="template"></po-widget>
   *
   * <ng-template #template>
   *   <i class="fa fa-podcast" style="font-size: inherit;"></i>
   * </ng-template>
   * ```
   * > Para o ícone enquadrar corretamente, deve-se utilizar `font-size: inherit` caso o ícone utilizado não aplique-o.
   */
  tagIcon;
  /**
   * @optional
   *
   * @description
   * Define o posicionamento da `po-tag` no cabeçalho do Widget:
   * - `right`: posicionada no canto superior direito do cabeçalho.
   * - `top`: posicionada à esquerda, acima do título (quando houver).
   * - `bottom`: posicionada à esquerda, abaixo do título (quando houver).
   *
   * @default `right`
   */
  tagPosition = input("right", __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "tagPosition"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-tag-position",
    transform: this.transformTagPosition
  }));
  /**
   * @optional
   *
   * @description
   *
   * Lista de ações exibidas no header do componente.
   * As propriedades das ações seguem a interface `PoPopupAction`.
   */
  actions = [];
  /**
   * @optional
   *
   * @description
   *
   * Define o tamanho dos botões do componente:
   * - `small`: altura de 32px (disponível apenas para acessibilidade AA).
   * - `medium`: altura de 44px.
   *
   * > Caso a acessibilidade AA não esteja configurada, o tamanho `medium` será mantido.
   * Para mais detalhes, consulte a documentação do [po-theme](https://po-ui.io/documentation/po-theme).
   *
   * @default `medium`
   */
  set size(value) {
    this._initialSize = value;
    this.applySizeBasedOnA11y();
  }
  get size() {
    return this._size ?? getDefaultSizeFn(PoFieldSize);
  }
  /**
   * @optional
   *
   * @description
   *
   * Define o avatar a ser exibido à esquerda no Widget.
   */
  avatar = input(void 0, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "avatar"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-avatar",
    transform: this.transformAvatar
  }));
  /**
   * @optional
   *
   * @description
   *
   * Evento disparado quando o usuário clicar no componente.
   * > Quando este evento está em uso, uma sombra (shadow) é aplicada automaticamente ao componente.
   */
  click = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Evento disparado quando a propriedade `p-disabled` for alterada.
   */
  onDisabled = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Evento disparado ao clicar na ação `p-primary-label`.
   */
  primaryAction = new EventEmitter();
  /**
   * @optional
   *
   * @description
   *
   * Evento disparado ao clicar na ação `p-secondary-label`.
   */
  secondaryAction = new EventEmitter();
  /**
   * @optional
   *
   * @description
   * Evento disparado ao clicar em **Configurações** incluído no menu de ações do header.
   */
  setting = new EventEmitter();
  /**
   * @optional
   *
   * @description
   * Evento disparado ao clicar no título definido em `p-title`.
   */
  titleAction = new EventEmitter();
  containerHeight = "auto";
  id = uuid();
  _background;
  _disabled = false;
  _height;
  _help;
  _noShadow = false;
  _primary = false;
  _primaryLabel;
  _title;
  /**
   * @optional
   *
   * @description
   *
   * Define uma imagem de fundo.
   * > Se a imagem escolhida intervir na legibilidade do texto contido no `p-widget`,
   * pode-se utilizar a propriedade `p-primary` em conjunto para que os textos fiquem na cor branca.
   *
   */
  set background(value) {
    this._background = value && typeof value === "string" ? value : void 0;
  }
  get background() {
    return this._background;
  }
  /**
   * @optional
   *
   * @description
   *
   * Desabilita o componente.
   *
   * @default `false`
   */
  set disabled(value) {
    this._disabled = convertToBoolean(value);
    this.onDisabled.emit(this.disabled);
  }
  get disabled() {
    return this._disabled;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define a altura do componente.
   * > Caso não seja informado valor, a propriedade irá assumir o tamanho do conteúdo.
   */
  set height(value) {
    this._height = parseInt(value, 10);
  }
  get height() {
    return this._height;
  }
  /**
   * @optional
   *
   * @description
   *
   * Link de ajuda incluído no menu de ações do header.
   */
  set help(value) {
    this._help = isTypeof(value, "string") ? value : "";
  }
  get help() {
    return this._help;
  }
  /**
   *
   * @optional
   *
   * @description
   *
   * Desabilita a sombra do componente quando o mesmo for clicável.
   * > A sombra é exibida por padrão apenas quando o evento `p-click` está definido.
   *
   * @default `true`
   */
  set noShadow(value) {
    this._noShadow = value === "" ? true : convertToBoolean(value);
  }
  get noShadow() {
    return this._noShadow;
  }
  /**
   * @optional
   *
   * @description
   *
   * Opção para que o `po-widget` fique em destaque.
   *
   * @default `false`
   */
  set primary(value) {
    this._primary = value === "" ? true : convertToBoolean(value);
  }
  get primary() {
    return this._primary;
  }
  /**
   * @optional
   *
   * @description
   *
   * Define o label e exibe a ação primária no footer do componente.
   *
   */
  set primaryLabel(value) {
    this._primaryLabel = isTypeof(value, "string") ? value : "";
  }
  get primaryLabel() {
    return this._primaryLabel;
  }
  /**
   * @optional
   *
   * @description
   *
   * Título do componente.
   *
   * > Quando o conteúdo exceder o espaço disponível, o texto será truncado com reticências.  O conteúdo completo poderá
   * ser visualizado ao passar o mouse sobre a tag, por meio do tooltip.
   */
  set title(value) {
    this._title = isTypeof(value, "string") ? value : "";
  }
  get title() {
    return this._title;
  }
  onThemeChange() {
    this.applySizeBasedOnA11y();
  }
  applySizeBasedOnA11y() {
    const size = validateSizeFn(this._initialSize, PoFieldSize);
    this._size = size;
  }
  transformAvatar(value) {
    if (!value) {
      return value;
    }
    const result = __spreadValues({}, value);
    if (result?.size) {
      result.size = validateAvatarSize(result.size);
    }
    if (result?.widthCustomTemplate) {
      const numericValue = Number(result.widthCustomTemplate.replace(/\D/g, ""));
      result.widthCustomTemplate = numericValue ? `${Math.min(numericValue, 50)}%` : void 0;
    }
    return result;
  }
  transformTagPosition(value) {
    return PO_WIDGET_TAG_POSITION.includes(value) ? value : PO_WIDGET_TAG_POSITION_DEFAULT;
  }
  static ɵfac = function PoWidgetBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoWidgetBaseComponent)();
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoWidgetBaseComponent,
    hostVars: 1,
    hostBindings: function PoWidgetBaseComponent_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("PoUiThemeChange", function PoWidgetBaseComponent_PoUiThemeChange_HostBindingHandler() {
          return ctx.onThemeChange();
        }, ɵɵresolveWindow);
      }
      if (rf & 2) {
        ɵɵattribute("p-size", ctx.size);
      }
    },
    inputs: {
      secondaryLabel: [0, "p-secondary-label", "secondaryLabel"],
      dangerPrimaryAction: [2, "p-danger-primary-action", "dangerPrimaryAction", convertToBoolean],
      dangerSecondaryAction: [2, "p-danger-secondary-action", "dangerSecondaryAction", convertToBoolean],
      kindPrimaryAction: [0, "p-kind-primary-action", "kindPrimaryAction"],
      kindSecondaryAction: [0, "p-kind-secondary-action", "kindSecondaryAction"],
      tagLabel: [0, "p-tag", "tagLabel"],
      tagType: [0, "p-tag-type", "tagType"],
      tagIcon: [0, "p-tag-icon", "tagIcon"],
      tagPosition: [1, "p-tag-position", "tagPosition"],
      actions: [0, "p-actions", "actions"],
      size: [0, "p-size", "size"],
      avatar: [1, "p-avatar", "avatar"],
      background: [0, "p-background", "background"],
      disabled: [0, "p-disabled", "disabled"],
      height: [0, "p-height", "height"],
      help: [0, "p-help", "help"],
      noShadow: [0, "p-no-shadow", "noShadow"],
      primary: [0, "p-primary", "primary"],
      primaryLabel: [0, "p-primary-label", "primaryLabel"],
      title: [0, "p-title", "title"]
    },
    outputs: {
      click: "p-click",
      onDisabled: "p-on-disabled",
      primaryAction: "p-primary-action",
      secondaryAction: "p-secondary-action",
      setting: "p-setting",
      titleAction: "p-title-action"
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoWidgetBaseComponent, [{
    type: Directive
  }], null, {
    secondaryLabel: [{
      type: Input,
      args: ["p-secondary-label"]
    }],
    dangerPrimaryAction: [{
      type: Input,
      args: [{
        alias: "p-danger-primary-action",
        transform: convertToBoolean
      }]
    }],
    dangerSecondaryAction: [{
      type: Input,
      args: [{
        alias: "p-danger-secondary-action",
        transform: convertToBoolean
      }]
    }],
    kindPrimaryAction: [{
      type: Input,
      args: ["p-kind-primary-action"]
    }],
    kindSecondaryAction: [{
      type: Input,
      args: ["p-kind-secondary-action"]
    }],
    tagLabel: [{
      type: Input,
      args: ["p-tag"]
    }],
    tagType: [{
      type: Input,
      args: ["p-tag-type"]
    }],
    tagIcon: [{
      type: Input,
      args: ["p-tag-icon"]
    }],
    tagPosition: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-tag-position",
        required: false
      }]
    }],
    actions: [{
      type: Input,
      args: ["p-actions"]
    }],
    size: [{
      type: Input,
      args: ["p-size"]
    }, {
      type: HostBinding,
      args: ["attr.p-size"]
    }],
    avatar: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-avatar",
        required: false
      }]
    }],
    click: [{
      type: Output,
      args: ["p-click"]
    }],
    onDisabled: [{
      type: Output,
      args: ["p-on-disabled"]
    }],
    primaryAction: [{
      type: Output,
      args: ["p-primary-action"]
    }],
    secondaryAction: [{
      type: Output,
      args: ["p-secondary-action"]
    }],
    setting: [{
      type: Output,
      args: ["p-setting"]
    }],
    titleAction: [{
      type: Output,
      args: ["p-title-action"]
    }],
    background: [{
      type: Input,
      args: ["p-background"]
    }],
    disabled: [{
      type: Input,
      args: ["p-disabled"]
    }],
    height: [{
      type: Input,
      args: ["p-height"]
    }],
    help: [{
      type: Input,
      args: ["p-help"]
    }],
    noShadow: [{
      type: Input,
      args: ["p-no-shadow"]
    }],
    primary: [{
      type: Input,
      args: ["p-primary"]
    }],
    primaryLabel: [{
      type: Input,
      args: ["p-primary-label"]
    }],
    title: [{
      type: Input,
      args: ["p-title"]
    }],
    onThemeChange: [{
      type: HostListener,
      args: ["window:PoUiThemeChange"]
    }]
  });
})();
var BORDER_WIDTH_SM = 1;
var SPACING_XS = 8;
var SPACING_SM = 16;
var SPACING_MD = 24;
var TARGET_SIZE_AAA = 44;
var TARGET_SIZE_AA = 32;
var PoWidgetComponent = class _PoWidgetComponent extends PoWidgetBaseComponent {
  cd = inject(ChangeDetectorRef);
  poTheme = inject(PoThemeService);
  popupTarget;
  literals;
  hasContent = false;
  poPopupComponent;
  buttonPopUp;
  wrapperInfo;
  wrapperTitle;
  tagElement;
  contentContainer;
  get showTitleAction() {
    return !!this.titleAction.observers[0];
  }
  //#region Avatar
  avatarSrc = computed(() => this.avatar()?.src, ...ngDevMode ? [{
    debugName: "avatarSrc"
  }] : (
    /* istanbul ignore next */
    []
  ));
  avatarSize = computed(() => this.avatar()?.size, ...ngDevMode ? [{
    debugName: "avatarSize"
  }] : (
    /* istanbul ignore next */
    []
  ));
  avatarCustomTemplate = computed(() => this.avatar()?.customTemplate, ...ngDevMode ? [{
    debugName: "avatarCustomTemplate"
  }] : (
    /* istanbul ignore next */
    []
  ));
  avatarWidthCustomTemplate = computed(() => this.avatar()?.widthCustomTemplate && this.avatar()?.customTemplate ? `${this.avatar().widthCustomTemplate}` : void 0, ...ngDevMode ? [{
    debugName: "avatarWidthCustomTemplate"
  }] : (
    /* istanbul ignore next */
    []
  ));
  //#endregion
  constructor() {
    const languageService = inject(PoLanguageService);
    super();
    const language = languageService.getShortLanguage();
    this.literals = __spreadValues(__spreadValues({}, poWidgetLiteralsDefault[poLocaleDefault]), poWidgetLiteralsDefault[language]);
  }
  ngAfterViewInit() {
    this.updateContent();
  }
  get a11Level() {
    return this.poTheme.getA11yLevel();
  }
  ngOnChanges(changes) {
    if (changes["help"] || changes["actions"]) {
      this.checkDefaultActions();
    }
    if (changes["title"] || changes["tagLabel"]) {
      this.cd.detectChanges();
    }
    this.updateContent();
    this.setHeight(this.height);
  }
  ngOnInit() {
    this.setHeight(this.height);
    this.checkDefaultActions();
    this.showTooltip();
    this.cd.detectChanges();
  }
  hasTitleHelpOrSetting() {
    return !!this.title || !!this.help || !!this.setting.observers[0] || !!this.tagLabel || !!this?.actions.length;
  }
  onClick(event) {
    if (this.click.observed && !this.disabled) {
      this.click.emit(event);
    }
  }
  onKeyDown(event) {
    if (this.click.observed && !this.disabled && (event.which === PoKeyCodeEnum.space || event.keyCode === PoKeyCodeEnum.space)) {
      this.click.emit(event);
      event.preventDefault();
    }
  }
  openHelp() {
    if (!this.disabled) {
      window.open(this.help, "_blank");
    }
  }
  runPrimaryAction() {
    if (!this.disabled) {
      this.primaryAction.emit();
    }
  }
  runSecondaryAction() {
    if (!this.disabled) {
      this.secondaryAction.emit();
    }
  }
  runTitleAction(event) {
    if (!this.disabled) {
      event.stopPropagation();
      this.titleAction.emit();
    }
  }
  setHeight(height) {
    this.checkDefaultActions();
    if (!height) {
      this.containerHeight = `auto`;
      return;
    }
    const buttonSmall = TARGET_SIZE_AA + SPACING_SM + SPACING_XS;
    const buttonMedium = TARGET_SIZE_AAA + SPACING_SM + SPACING_XS;
    const tagPosition = this.tagPosition();
    const actionsHeight = this.a11Level === PoThemeA11yEnum.AA && this.size === "small" ? buttonSmall : buttonMedium;
    const actionsButton = this.a11Level === PoThemeA11yEnum.AA && this.size === "small" ? buttonSmall : buttonMedium;
    const tagInRight = this.tagLabel && tagPosition === "right";
    let bodyHeight = height - BORDER_WIDTH_SM * 2;
    if ((this.title || tagInRight) && !this.actions.length) {
      bodyHeight -= SPACING_MD + SPACING_SM + SPACING_XS;
    }
    if (this.actions.length) {
      bodyHeight -= actionsHeight;
    }
    if (this.primaryLabel || this.secondaryLabel) {
      bodyHeight -= actionsButton;
    }
    if (this.tagLabel && (tagPosition === "top" || tagPosition === "bottom")) {
      bodyHeight -= SPACING_MD + SPACING_XS;
    }
    this.containerHeight = `${bodyHeight}px`;
  }
  settingOutput() {
    if (!this.disabled) {
      this.setting.emit();
    }
  }
  togglePopup(targetRef, event) {
    event.stopPropagation();
    this.popupTarget = targetRef;
    this.cd.detectChanges();
    this.poPopupComponent.toggle();
  }
  showTooltip() {
    const sumGap = this.tagLabel ? 12 : 6;
    return this.title && this.wrapperTitle?.nativeElement.offsetWidth + sumGap + (this.tagElement?.nativeElement.offsetWidth || 0) >= this.wrapperInfo?.nativeElement.offsetWidth;
  }
  closePopUp() {
    this.buttonPopUp?.focus();
  }
  checkDefaultActions() {
    if (this.setting.observed && !this.actions.some((action) => action.$id === "widget_configuration")) {
      this.actions = [...this.actions, {
        icon: "ICON_SETTINGS",
        label: this.literals.configuration,
        type: "default",
        action: this.settingOutput.bind(this),
        $id: "widget_configuration"
      }];
    }
    if (this.help && !this.actions.some((action) => action.$id === "widget_help")) {
      this.actions = [...this.actions, {
        icon: "ICON_HELP",
        label: this.literals.help,
        type: "default",
        action: this.openHelp.bind(this),
        $id: "widget_help"
      }];
    }
    if (!this.help) {
      this.actions = this.actions.filter((action) => action.$id !== "widget_help");
    }
  }
  updateContent() {
    const el = this.contentContainer?.nativeElement;
    if (!el) {
      return;
    }
    const existContent = Array.from(el.childNodes).some((node) => {
      if (node.nodeType === Node.TEXT_NODE) {
        return (node.textContent || "").trim().length > 0;
      }
      if (node.nodeType === Node.ELEMENT_NODE) {
        return true;
      }
      return false;
    });
    this.hasContent = existContent;
    this.cd.detectChanges();
  }
  static ɵfac = function PoWidgetComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoWidgetComponent)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoWidgetComponent,
    selectors: [["po-widget"]],
    viewQuery: function PoWidgetComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c36, 7)(_c179, 5)(_c180, 5)(_c181, 5)(_c182, 5, ElementRef)(_c183, 5, ElementRef);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.poPopupComponent = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.buttonPopUp = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.wrapperInfo = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.wrapperTitle = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.tagElement = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.contentContainer = _t.first);
      }
    },
    standalone: false,
    features: [ɵɵInheritDefinitionFeature, ɵɵNgOnChangesFeature],
    ngContentSelectors: _c12,
    decls: 14,
    vars: 21,
    consts: [["contentContainer", ""], ["popup", ""], ["tag", ""], ["wrapperInfo", ""], ["wrapperTitle", ""], ["popupTarget", ""], ["buttonPopUp", ""], ["tagElement", ""], [1, "po-widget-container", 3, "click", "keydown", "tabindex"], [1, "po-widget-container__avatar", 3, "width", "height", "po-widget-container__avatar_pb"], [1, "po-widget-container__content"], [1, "po-widget-header", 3, "po-widget-header__tag-top-actions"], ["p-no-border", "", 3, "p-height"], [1, "po-widget-body"], [1, "po-header-content-fallback"], [1, "po-widget-footer"], [3, "p-close", "p-actions", "p-size", "p-target"], [1, "po-widget-container__avatar"], [1, "po-widget-container__avatar__po-avatar"], [1, "po-widget-container__avatar__template"], [3, "p-size", "p-src"], [3, "ngTemplateOutlet"], [1, "po-widget-header"], [1, "po-widget-header__tag-top"], [1, "po-widget-wrapper-info"], [1, "po-widget-wrapper-tag"], [1, "po-widget-title-action", "po-widget-text", 3, "p-tooltip"], [1, "po-widget-text", 3, "p-tooltip"], [1, "po-widget-button-wrapper"], [1, "po-widget-header__tag-bottom"], [1, "po-widget-title-action", "po-widget-text", 3, "click", "p-tooltip"], [1, "po-widget-button-wrapper", 3, "click", "keydown"], ["p-icon", "ICON_MORE_OUT_VERT_FILL", "p-kind", "tertiary", 3, "p-size", "p-disabled"], [1, "po-widget-actions", 3, "click", "keydown"], [1, "po-widget-actions", 3, "p-click", "id", "p-label", "p-danger", "p-kind", "p-disabled", "p-size"], [1, "po-widget-actions"], ["p-force-icon", "", 3, "p-value", "p-icon", "p-type", "p-color", "p-text-color"]],
    template: function PoWidgetComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵprojectionDef();
        ɵɵelementStart(0, "div", 8);
        ɵɵlistener("click", function PoWidgetComponent_Template_div_click_0_listener($event) {
          return ctx.onClick($event);
        })("keydown", function PoWidgetComponent_Template_div_keydown_0_listener($event) {
          return ctx.onKeyDown($event);
        });
        ɵɵconditionalCreate(1, PoWidgetComponent_Conditional_1_Template, 3, 8, "div", 9);
        ɵɵelementStart(2, "div", 10);
        ɵɵconditionalCreate(3, PoWidgetComponent_Conditional_3_Template, 10, 7, "div", 11);
        ɵɵelementStart(4, "po-container", 12)(5, "div", 13, 0);
        ɵɵprojection(7, 0, _c184);
        ɵɵelementEnd();
        ɵɵconditionalCreate(8, PoWidgetComponent_Conditional_8_Template, 2, 0, "span", 14);
        ɵɵelementEnd();
        ɵɵconditionalCreate(9, PoWidgetComponent_Conditional_9_Template, 4, 8, "div", 15);
        ɵɵelementEnd()();
        ɵɵelementStart(10, "po-popup", 16, 1);
        ɵɵlistener("p-close", function PoWidgetComponent_Template_po_popup_p_close_10_listener() {
          return ctx.closePopUp();
        });
        ɵɵelementEnd();
        ɵɵtemplate(12, PoWidgetComponent_ng_template_12_Template, 2, 5, "ng-template", null, 2, ɵɵtemplateRefExtractor);
      }
      if (rf & 2) {
        ɵɵstyleProp("background-image", ctx.background ? "url(" + ctx.background + ")" : void 0);
        ɵɵclassProp("po-clickable", ctx.click.observers.length)("po-widget", !ctx.primary)("po-widget-disabled", ctx.disabled)("po-widget-primary", ctx.primary)("po-widget-no-shadow", ctx.click.observers.length && ctx.noShadow);
        ɵɵproperty("tabindex", ctx.disabled || !ctx.click.observers.length ? -1 : 0);
        ɵɵadvance();
        ɵɵconditional(ctx.avatarCustomTemplate() || ctx.avatarSrc() ? 1 : -1);
        ɵɵadvance(2);
        ɵɵconditional(ctx.hasTitleHelpOrSetting() ? 3 : -1);
        ɵɵadvance();
        ɵɵproperty("p-height", ctx.containerHeight);
        ɵɵadvance(4);
        ɵɵconditional(!ctx.hasContent && ctx.height ? 8 : -1);
        ɵɵadvance();
        ɵɵconditional(ctx.primaryLabel ? 9 : -1);
        ɵɵadvance();
        ɵɵproperty("p-actions", ctx.actions)("p-size", ctx.size)("p-target", ctx.popupTarget);
      }
    },
    dependencies: [NgTemplateOutlet, PoAvatarComponent, PoButtonComponent, PoContainerComponent, PoPopupComponent, PoTagComponent, PoTooltipDirective],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoWidgetComponent, [{
    type: Component,
    args: [{
      selector: "po-widget",
      standalone: false,
      template: `<div
  class="po-widget-container"
  [class.po-clickable]="click.observers.length"
  [class.po-widget]="!primary"
  [class.po-widget-disabled]="disabled"
  [class.po-widget-primary]="primary"
  [class.po-widget-no-shadow]="click.observers.length && noShadow"
  [style.background-image]="background ? 'url(' + background + ')' : undefined"
  (click)="onClick($event)"
  (keydown)="onKeyDown($event)"
  [tabindex]="disabled || !click.observers.length ? -1 : 0"
>
  @if (avatarCustomTemplate() || avatarSrc()) {
    <div
      class="po-widget-container__avatar"
      [style.width]="avatarWidthCustomTemplate()"
      [style.height]="\`\${height - 2}px\`"
      [class.po-widget-container__avatar_pb]="primaryLabel"
    >
      @if (!avatarCustomTemplate() && avatarSrc()) {
        <div class="po-widget-container__avatar__po-avatar">
          <po-avatar [p-size]="avatarSize()" [p-src]="avatarSrc()" />
        </div>
      }

      @if (avatarCustomTemplate()) {
        <div class="po-widget-container__avatar__template">
          <ng-template [ngTemplateOutlet]="avatarCustomTemplate()"></ng-template>
        </div>
      }
    </div>
  }

  <div class="po-widget-container__content">
    @if (hasTitleHelpOrSetting()) {
      <div
        class="po-widget-header"
        [class.po-widget-header__tag-top-actions]="tagLabel && tagPosition() === 'top' && actions?.length"
      >
        @if (tagLabel && tagPosition() === 'top') {
          <div class="po-widget-header__tag-top">
            <ng-template [ngTemplateOutlet]="tag"></ng-template>
          </div>
        }

        <div class="po-widget-wrapper-info">
          <div #wrapperInfo class="po-widget-wrapper-tag">
            @if (showTitleAction) {
              <span
                #wrapperTitle
                class="po-widget-title-action po-widget-text"
                (click)="runTitleAction($event)"
                [p-tooltip]="title && showTooltip() ? title : ''"
              >
                {{ title }}
              </span>
            } @else {
              <p #wrapperTitle class="po-widget-text" [p-tooltip]="title && showTooltip() ? title : ''">
                {{ title }}
              </p>
            }

            @if (tagLabel && tagPosition() === 'right') {
              <ng-template [ngTemplateOutlet]="tag"></ng-template>
            }
          </div>

          @if (actions?.length) {
            <div
              class="po-widget-button-wrapper"
              #popupTarget
              (click)="togglePopup(popupTarget, $event)"
              (keydown)="$event.stopPropagation()"
            >
              <po-button
                #buttonPopUp
                [p-size]="size"
                p-icon="ICON_MORE_OUT_VERT_FILL"
                [p-disabled]="disabled"
                p-kind="tertiary"
              >
              </po-button>
            </div>
          }
        </div>

        @if (tagLabel && tagPosition() === 'bottom') {
          <div class="po-widget-header__tag-bottom">
            <ng-template [ngTemplateOutlet]="tag"></ng-template>
          </div>
        }
      </div>
    }

    <po-container p-no-border [p-height]="containerHeight">
      <div #contentContainer class="po-widget-body">
        <ng-content #refContent></ng-content>
      </div>
      @if (!hasContent && height) {
        <span class="po-header-content-fallback">.</span>
      }
    </po-container>

    @if (primaryLabel) {
      <div class="po-widget-footer">
        <div class="po-widget-actions" (click)="$event.stopPropagation()" (keydown)="$event.stopPropagation()">
          <po-button
            class="po-widget-actions"
            id="primaryAct-{{ id }}"
            [p-label]="primaryLabel"
            (p-click)="runPrimaryAction()"
            [p-danger]="dangerPrimaryAction"
            [p-kind]="kindPrimaryAction || 'tertiary'"
            [p-disabled]="disabled"
            [p-size]="size"
          ></po-button>
        </div>

        @if (secondaryLabel) {
          <div class="po-widget-actions" (click)="$event.stopPropagation()" (keydown)="$event.stopPropagation()">
            <po-button
              class="po-widget-actions"
              id="secondaryAct-{{ id }}"
              [p-label]="secondaryLabel"
              (p-click)="runSecondaryAction()"
              [p-danger]="dangerSecondaryAction"
              [p-kind]="kindSecondaryAction || 'tertiary'"
              [p-disabled]="disabled"
              [p-size]="size"
            ></po-button>
          </div>
        }
      </div>
    }
  </div>
</div>

<po-popup #popup [p-actions]="actions" [p-size]="size" [p-target]="popupTarget" (p-close)="closePopUp()"> </po-popup>

<ng-template #tag>
  <po-tag
    #tagElement
    [p-value]="tagLabel"
    [p-icon]="tagIcon"
    [p-type]="disabled ? null : tagType ? tagType : 'success'"
    [p-color]="disabled ? '#ECECEE' : null"
    [p-text-color]="disabled ? '#36364A' : null"
    p-force-icon
  />
</ng-template>
`
    }]
  }], () => [], {
    poPopupComponent: [{
      type: ViewChild,
      args: ["popup", {
        static: true
      }]
    }],
    buttonPopUp: [{
      type: ViewChild,
      args: ["buttonPopUp"]
    }],
    wrapperInfo: [{
      type: ViewChild,
      args: ["wrapperInfo"]
    }],
    wrapperTitle: [{
      type: ViewChild,
      args: ["wrapperTitle"]
    }],
    tagElement: [{
      type: ViewChild,
      args: ["tagElement", {
        read: ElementRef
      }]
    }],
    contentContainer: [{
      type: ViewChild,
      args: ["contentContainer", {
        read: ElementRef
      }]
    }]
  });
})();
var PoWidgetModule = class _PoWidgetModule {
  static ɵfac = function PoWidgetModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoWidgetModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoWidgetModule,
    declarations: [PoWidgetComponent],
    imports: [CommonModule, PoAvatarModule, PoButtonModule, PoContainerModule, PoIconModule, PoPopupModule, PoTagModule, PoTooltipModule],
    exports: [PoWidgetComponent]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [CommonModule, PoAvatarModule, PoButtonModule, PoContainerModule, PoIconModule, PoPopupModule, PoTagModule, PoTooltipModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoWidgetModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, PoAvatarModule, PoButtonModule, PoContainerModule, PoIconModule, PoPopupModule, PoTagModule, PoTooltipModule],
      exports: [PoWidgetComponent],
      declarations: [PoWidgetComponent]
    }]
  }], null, null);
})();
var poNavbarLiteralsDefault = {
  en: {
    headerLinks: "Header links",
    notifications: "Notifications"
  },
  es: {
    headerLinks: "Header links",
    notifications: "Notificaciones"
  },
  pt: {
    headerLinks: "Header links",
    notifications: "Notificações"
  },
  ru: {
    headerLinks: "Header связи",
    notifications: "Уведомления"
  }
};
var PoHeaderBaseComponent = class _PoHeaderBaseComponent {
  _menuItems = [];
  menuCollapseJoin = [];
  menuCollapseJoinExternal = [];
  _brand;
  _literals;
  language = poLocaleDefault;
  themeChangeSignal = signal(0, ...ngDevMode ? [{
    debugName: "themeChangeSignal"
  }] : (
    /* istanbul ignore next */
    []
  ));
  /**
   * @optional
   *
   * @description
   *
   * Número de itens dentro do botão de overflow. Caso a largura do header não suportar a quantidade de itens passadas, um botão com itens será criado.
   * Essa propriedade possibilita a escolha de quantos itens estarão dentro do botão de overflow.
   *
   * > Ao utilizar essa propriedade o `po-header` não irá realizar o calculo automatíco de itens.
   *
   */
  amountMore;
  /**
   * @optional
   *
   * @description
   *
   * Esconde o botão de menu colapsado.
   *
   */
  hideButtonMenu;
  /**
   * @optional
   *
   * @description
   *
   * Habilita campo para filtrar itens no menu
   *
   */
  filterMenu;
  //Habilita apenas o evento ao clicar no menu hamburguer desabilitando o side-menu lateral
  sideMenuOnlyAction = false;
  //Habilita mais de 3 itens na seção de tools.
  //Propriedade para uso interno, não indicamos uso dessa propriedade
  forceActionTools = false;
  //propriedade interna
  notChangeContext = false;
  /**
   * @optional
   *
   * @description
   *
   * Propriedade para configurar a seção de brand do `po-header`
   *
   * Caso seja enviada uma string, apenas o logo sera mostrado com o valor da string passada.
   *
   */
  set brand(value) {
    if (typeof value === "string") {
      this._brand = {
        logo: value,
        link: "/"
      };
    } else {
      this._brand = __spreadProps(__spreadValues({}, value), {
        link: value?.link || "/"
      });
    }
  }
  get brand() {
    return this._brand;
  }
  /**
   * @optional
   *
   * @description
   *
   * Propriedade para configurar a seção de tools do `po-header`
   *
   * > Máximo de 3 itens, o componente irá ignorar os itens caso seja mandado mais itens que o suportado.
   *
   */
  actionsTools = [];
  /**
   * @optional
   *
   * @description
   *
   * Propriedade para configurar a seção de headerUser do `po-header`
   *
   */
  headerUser;
  /**
   * @optional
   *
   * @description
   *
   * Propriedade para configurar a seção de menu do `po-header`.
   * Cada item pode receber uma label e uma ação
   *
   * > Os itens irão ficar visíveis em uma tela de até 960px
   *
   */
  set menuItems(items) {
    this._menuItems = items.map((item) => __spreadProps(__spreadValues({}, item), {
      id: item.id || this.generateRandomId(),
      $internalRoute: item.link && !isExternalLink(item.link) ? true : false
    }));
  }
  /**
   * @optional
   *
   * @description
   *
   * Lista dos itens do menu. Se o valor estiver indefinido ou inválido, será inicializado como um array vazio.
   *
   * > O menu poderá ser aberto via botão hamburguer quando a tela tiver menos que 960px
   *
   */
  menuCollapse = [];
  /**
   * @optional
   *
   * @description
   *
   * Template customiado que será renderizado após os itens definidos na propriedade `p-menu-items`
   *
   */
  headerTemplate;
  /**
   * @optional
   *
   * @description
   *
   * Objeto com a literal usada na propriedade `p-literals`.
   *
   * Para customizar a literal, basta declarar um objeto do tipo `PoHeaderLiterals` conforme exemplo abaixo:
   *
   * ```
   *  const customLiterals: PoHeaderLiterals = {
   *    headerLinks: 'Itens de navegação',
   *    notifications: 'Mensagens'
   *  };
   * ```
   *
   * E para carregar as literais customizadas, basta apenas passar o objeto para o componente.
   *
   * ```
   * <po-header
   *   [p-literals]="customLiterals">
   * </po-header>
   * ```
   *
   * > O objeto padrão de literais será traduzido de acordo com o idioma do
   * [`PoI18nService`](/documentation/po-i18n) ou do browser.
   */
  set literals(value) {
    if (value instanceof Object && !(value instanceof Array)) {
      this._literals = __spreadValues(__spreadValues(__spreadValues({}, poNavbarLiteralsDefault[poLocaleDefault]), poNavbarLiteralsDefault[this.language]), value);
    } else {
      this._literals = poNavbarLiteralsDefault[this.language];
    }
  }
  get literals() {
    return this._literals || poNavbarLiteralsDefault[this.language];
  }
  /**
   * @optional
   *
   * @Input
   *
   * @description
   *
   * Define o tamanho do componente:
   * - `small`: altura de 44px (disponível apenas para acessibilidade AA).
   * - `medium`: altura de 56px.
   *
   * > Caso a acessibilidade AA não esteja configurada, o tamanho `medium` será mantido.
   * Para mais detalhes, consulte a documentação do [po-theme](https://po-ui.io/documentation/po-theme).
   *
   * @default `medium`
   */
  sizeInput = input("", __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "sizeInput"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-size"
  }));
  size = computed(() => {
    this.themeChangeSignal();
    const value = this.sizeInput();
    return value ? validateSizeFn(value, PoFieldSize) : getDefaultSizeFn(PoFieldSize);
  }, ...ngDevMode ? [{
    debugName: "size"
  }] : (
    /* istanbul ignore next */
    []
  ));
  get hostSize() {
    return this.sizeInput() ? validateSizeFn(this.sizeInput(), PoFieldSize) : getDefaultSizeFn(PoFieldSize);
  }
  /**
   * @optional
   *
   * @description
   *
   * Evento emitido ao clicar no botão para colapsar ou expandir menu.
   *
   */
  colapsedMenuEvent = new EventEmitter();
  constructor(languageService) {
    this.language = languageService.getShortLanguage();
  }
  get menuItems() {
    return this._menuItems;
  }
  generateRandomId() {
    return String(Math.floor(Math.random() * 9999 + 1));
  }
  onThemeChange() {
    this.themeChangeSignal.update((v) => v + 1);
    requestAnimationFrame(() => {
      try {
        this.updateMenu();
      } catch (e2) {
        console.error("updateMenu with errors. probably tried to execute before the component was rendered.");
      }
    });
  }
  static ɵfac = function PoHeaderBaseComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoHeaderBaseComponent)(ɵɵdirectiveInject(PoLanguageService));
  };
  static ɵdir = ɵɵdefineDirective({
    type: _PoHeaderBaseComponent,
    hostVars: 1,
    hostBindings: function PoHeaderBaseComponent_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("PoUiThemeChange", function PoHeaderBaseComponent_PoUiThemeChange_HostBindingHandler() {
          return ctx.onThemeChange();
        }, ɵɵresolveWindow);
      }
      if (rf & 2) {
        ɵɵattribute("p-size", ctx.hostSize);
      }
    },
    inputs: {
      amountMore: [0, "p-amount-more", "amountMore"],
      hideButtonMenu: [0, "p-hide-button-menu", "hideButtonMenu"],
      filterMenu: [2, "p-filter-menu", "filterMenu", convertToBoolean],
      sideMenuOnlyAction: [0, "p-side-menu-only-action", "sideMenuOnlyAction"],
      forceActionTools: [0, "p-force-actions-tools", "forceActionTools"],
      notChangeContext: [0, "p-not-change-context", "notChangeContext"],
      brand: [0, "p-brand", "brand"],
      actionsTools: [0, "p-actions-tools", "actionsTools"],
      headerUser: [0, "p-header-user", "headerUser"],
      menuItems: [0, "p-menu-items", "menuItems"],
      menuCollapse: [0, "p-menus", "menuCollapse"],
      headerTemplate: [0, "p-header-template", "headerTemplate"],
      literals: [0, "p-literals", "literals"],
      sizeInput: [1, "p-size", "sizeInput"]
    },
    outputs: {
      colapsedMenuEvent: "p-colapsed-menu"
    }
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoHeaderBaseComponent, [{
    type: Directive
  }], () => [{
    type: PoLanguageService
  }], {
    amountMore: [{
      type: Input,
      args: ["p-amount-more"]
    }],
    hideButtonMenu: [{
      type: Input,
      args: ["p-hide-button-menu"]
    }],
    filterMenu: [{
      type: Input,
      args: [{
        alias: "p-filter-menu",
        transform: convertToBoolean
      }]
    }],
    sideMenuOnlyAction: [{
      type: Input,
      args: ["p-side-menu-only-action"]
    }],
    forceActionTools: [{
      type: Input,
      args: ["p-force-actions-tools"]
    }],
    notChangeContext: [{
      type: Input,
      args: ["p-not-change-context"]
    }],
    brand: [{
      type: Input,
      args: ["p-brand"]
    }],
    actionsTools: [{
      type: Input,
      args: ["p-actions-tools"]
    }],
    headerUser: [{
      type: Input,
      args: ["p-header-user"]
    }],
    menuItems: [{
      type: Input,
      args: ["p-menu-items"]
    }],
    menuCollapse: [{
      type: Input,
      args: ["p-menus"]
    }],
    headerTemplate: [{
      type: Input,
      args: ["p-header-template"]
    }],
    literals: [{
      type: Input,
      args: ["p-literals"]
    }],
    sizeInput: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-size",
        required: false
      }]
    }],
    hostSize: [{
      type: HostBinding,
      args: ["attr.p-size"]
    }],
    colapsedMenuEvent: [{
      type: Output,
      args: ["p-colapsed-menu"]
    }],
    onThemeChange: [{
      type: HostListener,
      args: ["window:PoUiThemeChange"]
    }]
  });
})();
var PoHeaderbrandComponent = class _PoHeaderbrandComponent {
  cd;
  router;
  resizeSub;
  showTitleTooltip = false;
  smallLogo = false;
  targetRef;
  titleBrand;
  brand;
  clickMenu = new EventEmitter();
  hideButtonMenu;
  // itens do menu caso não seja enviado um menu externo pelo usuário
  menuCollapse = [];
  externalMenu;
  size = input(void 0, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "size"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-size"
  }));
  constructor(cd, router) {
    this.cd = cd;
    this.router = router;
  }
  ngAfterViewInit() {
    this.showTitleTooltip = this.showTooltip;
    this.resizeSub = fromEvent(window, "resize").pipe(debounceTime(200), map(() => window.innerWidth), startWith(window.innerWidth)).subscribe((width) => {
      this.smallLogo = width <= 960;
    });
  }
  ngOnDestroy() {
    this.resizeSub?.unsubscribe();
  }
  ngOnChanges(changes) {
    if (changes["brand"]) {
      this.showTitleTooltip = this.showTooltip;
      this.cd.detectChanges();
    }
  }
  onClickLogo() {
    this.brand?.action?.();
    if (this.brand.link) {
      if (PoUtils.isExternalLink(this.brand.link)) {
        window.open(this.brand.link, "_blank");
      } else {
        this.router.navigateByUrl(this.brand.link);
      }
    }
  }
  get showTooltip() {
    return this.titleBrand?.nativeElement.offsetWidth >= 151;
  }
  static ɵfac = function PoHeaderbrandComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoHeaderbrandComponent)(ɵɵdirectiveInject(ChangeDetectorRef), ɵɵdirectiveInject(Router));
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoHeaderbrandComponent,
    selectors: [["po-header-brand"]],
    viewQuery: function PoHeaderbrandComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c15, 7, ElementRef)(_c185, 5);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.targetRef = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.titleBrand = _t.first);
      }
    },
    inputs: {
      brand: [0, "p-brand", "brand"],
      hideButtonMenu: [0, "p-hide-button-menu", "hideButtonMenu"],
      menuCollapse: [0, "p-menus", "menuCollapse"],
      externalMenu: [0, "p-external", "externalMenu"],
      size: [1, "p-size", "size"]
    },
    outputs: {
      clickMenu: "p-click-menu"
    },
    standalone: false,
    features: [ɵɵNgOnChangesFeature],
    decls: 8,
    vars: 5,
    consts: [["titleBrand", ""], ["target", ""], [1, "po-header-nav-brand"], ["p-icon", "ICON_MENU", "p-kind", "tertiary", 1, "po-nav-header-medium", 3, "p-size"], ["p-icon", "ICON_MENU", "p-kind", "tertiary", 3, "p-size"], [1, "po-header-nav-brand-logo"], [1, "po-header-nav-brand-logo-small"], [1, "po-header-nav-brand-text", 3, "p-tooltip"], ["p-icon", "ICON_MENU", "p-kind", "tertiary", 1, "po-nav-header-medium", 3, "p-click", "p-size"], ["p-icon", "ICON_MENU", "p-kind", "tertiary", 3, "p-click", "p-size"], [1, "po-header-nav-brand-logo", 3, "click"], ["alt", "Logo", 3, "src"], [1, "po-header-nav-brand-logo-small", 3, "click"]],
    template: function PoHeaderbrandComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "div", 2);
        ɵɵconditionalCreate(1, PoHeaderbrandComponent_Conditional_1_Template, 2, 1, "po-button", 3)(2, PoHeaderbrandComponent_Conditional_2_Template, 2, 1, "po-button", 4);
        ɵɵconditionalCreate(3, PoHeaderbrandComponent_Conditional_3_Template, 2, 1, "div", 5);
        ɵɵconditionalCreate(4, PoHeaderbrandComponent_Conditional_4_Template, 2, 1, "div", 6);
        ɵɵelementStart(5, "span", 7, 0);
        ɵɵtext(7);
        ɵɵelementEnd()();
      }
      if (rf & 2) {
        ɵɵadvance();
        ɵɵconditional(ctx.externalMenu ? 1 : ctx.menuCollapse.length && !ctx.hideButtonMenu ? 2 : -1);
        ɵɵadvance(2);
        ɵɵconditional((ctx.brand == null ? null : ctx.brand.logo) && !ctx.smallLogo ? 3 : -1);
        ɵɵadvance();
        ɵɵconditional(((ctx.brand == null ? null : ctx.brand.logo) || (ctx.brand == null ? null : ctx.brand.smallLogo)) && ctx.smallLogo ? 4 : -1);
        ɵɵadvance();
        ɵɵproperty("p-tooltip", ctx.showTooltip ? ctx.brand == null ? null : ctx.brand.title : "");
        ɵɵadvance(2);
        ɵɵtextInterpolate(ctx.brand == null ? null : ctx.brand.title);
      }
    },
    dependencies: [PoButtonComponent, PoTooltipDirective],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoHeaderbrandComponent, [{
    type: Component,
    args: [{
      selector: "po-header-brand",
      standalone: false,
      template: `<div class="po-header-nav-brand">
  @if (externalMenu) {
    <po-button
      #target
      class="po-nav-header-medium"
      [p-size]="size()"
      p-icon="ICON_MENU"
      p-kind="tertiary"
      (p-click)="clickMenu.emit()"
    >
    </po-button>
  } @else if (menuCollapse.length && !hideButtonMenu) {
    <po-button [p-size]="size()" #target p-icon="ICON_MENU" p-kind="tertiary" (p-click)="clickMenu.emit()"> </po-button>
  }

  @if (brand?.logo && !smallLogo) {
    <div class="po-header-nav-brand-logo" (click)="onClickLogo()">
      <img [src]="brand.logo" alt="Logo" />
    </div>
  }

  @if ((brand?.logo || brand?.smallLogo) && smallLogo) {
    <div class="po-header-nav-brand-logo-small" (click)="onClickLogo()">
      <img [src]="brand?.smallLogo || brand?.logo" alt="Logo" />
    </div>
  }
  <span #titleBrand [p-tooltip]="showTooltip ? brand?.title : ''" class="po-header-nav-brand-text">{{
    brand?.title
  }}</span>
</div>
`
    }]
  }], () => [{
    type: ChangeDetectorRef
  }, {
    type: Router
  }], {
    targetRef: [{
      type: ViewChild,
      args: ["target", {
        read: ElementRef,
        static: true
      }]
    }],
    titleBrand: [{
      type: ViewChild,
      args: ["titleBrand"]
    }],
    brand: [{
      type: Input,
      args: ["p-brand"]
    }],
    clickMenu: [{
      type: Output,
      args: ["p-click-menu"]
    }],
    hideButtonMenu: [{
      type: Input,
      args: ["p-hide-button-menu"]
    }],
    menuCollapse: [{
      type: Input,
      args: ["p-menus"]
    }],
    externalMenu: [{
      type: Input,
      args: ["p-external"]
    }],
    size: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-size",
        required: false
      }]
    }]
  });
})();
var poHeaderMoreLiteralsDefault = {
  en: {
    more: "More"
  },
  es: {
    more: "Más"
  },
  pt: {
    more: "Mais"
  },
  ru: {
    more: "Более"
  }
};
var PoHeaderMenuItemComponent = class _PoHeaderMenuItemComponent {
  cd;
  router;
  buttonMoreRef;
  lastItemSelected;
  selectedItem = false;
  literals;
  routeSubscription;
  resizeSubscription;
  buttonMoreElement;
  poPopupElement;
  item;
  itemOverFlow;
  overflowButton = false;
  size = input(void 0, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "size"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-size"
  }));
  itemClick = new EventEmitter();
  constructor(cd, router, languageService) {
    this.cd = cd;
    this.router = router;
    const language = languageService.getShortLanguage();
    this.literals = __spreadValues(__spreadValues({}, poHeaderMoreLiteralsDefault[poLocaleDefault]), poHeaderMoreLiteralsDefault[language]);
  }
  ngOnInit() {
    this.subscribeToRoute();
    this.resizeSubscription = fromEvent(window, "resize").pipe(debounceTime(300)).subscribe(() => {
      this.checkActiveItemByUrl(this.checkRouterChildrenFragments());
    });
  }
  ngOnChanges(changes) {
    if (changes["itemOverFlow"]) {
      this.cd.detectChanges();
    }
    if (changes["item"]) {
      this.checkActiveItemByUrl(this.checkRouterChildrenFragments());
      this.cd.detectChanges();
    }
  }
  ngOnDestroy() {
    this.routeSubscription?.unsubscribe();
    this.resizeSubscription?.unsubscribe();
  }
  openListButtonMore() {
    this.cd.detectChanges();
    this.poPopupElement.toggle();
  }
  onKeyDownButtonMore(event) {
    if (event.code === "Space" || event.code === "Enter") {
      event.preventDefault();
      this.openListButtonMore();
    }
  }
  onKeyDownButtonList(event, item) {
    if (event.code === "Space" || event.code === "Enter") {
      event.preventDefault();
      this.onAction(item);
    }
  }
  onAction(item, itemDefault = true) {
    if (itemDefault) {
      item.action?.();
    }
    if (!itemDefault) {
      this.lastItemSelected = item;
    } else {
      this.itemClick.emit({
        item
      });
    }
    if (item.link) {
      if (PoUtils.isExternalLink(item.link)) {
        window.open(item.link, "_blank");
      } else {
        this.router.navigateByUrl(item.link);
      }
    }
  }
  onClosePopup() {
    this.buttonMoreElement?.nativeElement.focus();
    if (this.lastItemSelected) {
      this.itemClick.emit({
        item: this.lastItemSelected,
        focus: true
      });
    }
    this.lastItemSelected = null;
  }
  checkActiveItemByUrl(urlRouter) {
    const urlArray = urlRouter.split("/");
    let counter = urlArray.length;
    while (counter >= 0) {
      const url = urlArray.slice(0, counter).join("/");
      this.selectedItem = this.item?.link === url;
      if (this.selectedItem) {
        break;
      }
      counter--;
    }
  }
  checkRouterChildrenFragments() {
    const childrenPrimary = this.router.parseUrl(this.router.url).root.children["primary"];
    return childrenPrimary ? `/${childrenPrimary.segments.map((it) => it.path).join("/")}` : "/";
  }
  subscribeToRoute() {
    this.routeSubscription = this.router.events.subscribe((val) => {
      if (val instanceof NavigationEnd || val instanceof NavigationCancel) {
        const urlRouter = this.checkRouterChildrenFragments();
        this.checkActiveItemByUrl(urlRouter);
        this.cd.detectChanges();
      }
    });
  }
  static ɵfac = function PoHeaderMenuItemComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoHeaderMenuItemComponent)(ɵɵdirectiveInject(ChangeDetectorRef), ɵɵdirectiveInject(Router), ɵɵdirectiveInject(PoLanguageService));
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoHeaderMenuItemComponent,
    selectors: [["po-header-menu-item"]],
    viewQuery: function PoHeaderMenuItemComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c186, 5, ElementRef)(PoPopupComponent, 5);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.buttonMoreElement = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.poPopupElement = _t.first);
      }
    },
    inputs: {
      item: [0, "p-item", "item"],
      itemOverFlow: [0, "p-item-overflow", "itemOverFlow"],
      overflowButton: [0, "p-button-overflow", "overflowButton"],
      size: [1, "p-size", "size"]
    },
    outputs: {
      itemClick: "p-item-click"
    },
    standalone: false,
    features: [ɵɵNgOnChangesFeature],
    decls: 3,
    vars: 2,
    consts: [["buttonMoreElement", "", "overflowButton", ""], ["menusubmenuitem", ""], ["popup", ""], ["tabindex", "0", 1, "po-header-nav-submenu-more"], ["tabindex", "0", 1, "po-header-nav-submenu", 3, "p-tooltip"], [3, "p-size", "p-actions", "p-target", "p-hide-arrow"], ["tabindex", "0", 1, "po-header-nav-submenu-more", 3, "click", "keydown"], ["role", "button", 1, "po-header-nav-submenu-button-more"], [1, "po-header-icon", "po-header-nav-submenu-more-icon", 3, "p-icon"], ["tabindex", "0", 1, "po-header-nav-submenu", 3, "click", "keydown", "p-tooltip"], [1, "po-header-nav-submenu-button"], [3, "p-click-item", "p-close", "p-size", "p-actions", "p-target", "p-hide-arrow"]],
    template: function PoHeaderMenuItemComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵconditionalCreate(0, PoHeaderMenuItemComponent_Conditional_0_Template, 7, 5, "li", 3)(1, PoHeaderMenuItemComponent_Conditional_1_Template, 5, 6, "li", 4);
        ɵɵconditionalCreate(2, PoHeaderMenuItemComponent_Conditional_2_Template, 2, 4, "po-popup", 5);
      }
      if (rf & 2) {
        ɵɵconditional(ctx.overflowButton ? 0 : 1);
        ɵɵadvance(2);
        ɵɵconditional(ctx.overflowButton ? 2 : -1);
      }
    },
    dependencies: [PoIconComponent, PoTooltipDirective, PoPopupComponent],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoHeaderMenuItemComponent, [{
    type: Component,
    args: [{
      selector: "po-header-menu-item",
      standalone: false,
      template: `@if (overflowButton) {
  <li
    #buttonMoreElement
    #overflowButton
    class="po-header-nav-submenu-more"
    tabindex="0"
    (click)="openListButtonMore()"
    (keydown)="onKeyDownButtonMore($event)"
  >
    <div
      role="button"
      [attr.aria-expanded]="poPopupElement?.showPopup"
      class="po-header-nav-submenu-button-more"
      [class.open]="poPopupElement?.showPopup"
    >
      <span>{{ literals.more }}</span>
      <po-icon
        class="po-header-icon po-header-nav-submenu-more-icon"
        [p-icon]="poPopupElement?.showPopup ? 'ICON_ARROW_UP' : 'ICON_ARROW_DOWN'"
      ></po-icon>
    </div>
  </li>
} @else {
  <li
    #menusubmenuitem
    tabindex="0"
    [p-tooltip]="item?.label"
    class="po-header-nav-submenu"
    (click)="onAction(item)"
    (keydown)="onKeyDownButtonList($event, item)"
    [attr.aria-current]="selectedItem"
  >
    <div
      class="po-header-nav-submenu-button"
      [attr.aria-selected]="item?.$selected"
      [class.submenu-item-selected]="selectedItem"
    >
      <span>{{ item?.label }}</span>
    </div>
  </li>
}

@if (overflowButton) {
  <po-popup
    #popup
    [p-size]="size()"
    [p-actions]="itemOverFlow"
    [p-target]="buttonMoreElement"
    [p-hide-arrow]="true"
    (p-click-item)="onAction($event, false)"
    (p-close)="onClosePopup()"
  >
  </po-popup>
}
`
    }]
  }], () => [{
    type: ChangeDetectorRef
  }, {
    type: Router
  }, {
    type: PoLanguageService
  }], {
    buttonMoreElement: [{
      type: ViewChild,
      args: ["buttonMoreElement", {
        read: ElementRef
      }]
    }],
    poPopupElement: [{
      type: ViewChild,
      args: [PoPopupComponent]
    }],
    item: [{
      type: Input,
      args: ["p-item"]
    }],
    itemOverFlow: [{
      type: Input,
      args: ["p-item-overflow"]
    }],
    overflowButton: [{
      type: Input,
      args: ["p-button-overflow"]
    }],
    size: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-size",
        required: false
      }]
    }],
    itemClick: [{
      type: Output,
      args: ["p-item-click"]
    }]
  });
})();
var PoHeaderToolsComponent = class _PoHeaderToolsComponent {
  router;
  cd;
  _actionTools = [];
  buttonActionElements;
  buttonActionComponents;
  poPopupActions;
  poPopoverActions;
  forceActionTools = false;
  set actionTools(value) {
    if (this.forceActionTools) {
      this._actionTools = value;
    } else {
      this._actionTools = value.slice(0, 3);
    }
    this.cd.detectChanges();
  }
  literals;
  size = input(void 0, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "size"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-size"
  }));
  get actionTools() {
    return this._actionTools;
  }
  get popoverIndexes() {
    return this.actionTools.map((action, index) => action.popover ? index : -1).filter((index) => index !== -1);
  }
  headerTemplate;
  constructor(router, cd) {
    this.router = router;
    this.cd = cd;
  }
  onClickAction(index) {
    const action = this.actionTools[index];
    action.action?.();
    if (this.actionTools[index].items && !this.actionTools[index].popover) {
      const popup = this.poPopupActions.get(index);
      popup?.toggle();
    }
    this.checkLink(action);
  }
  checkSelected(index) {
    if (this.actionTools[index].items && !this.actionTools[index].popover) {
      const popup = this.poPopupActions?.get(index);
      if (popup?.showPopup) {
        return true;
      }
      return false;
    } else {
      const realPopoverIndex = this.popoverIndexes.indexOf(index);
      const popover = this.poPopoverActions?.get(realPopoverIndex);
      if (popover && !popover?.isHidden) {
        return true;
      }
      return false;
    }
  }
  onClosePopup(index) {
    this.buttonActionComponents.get(index).focus();
  }
  getAriaLabel(action) {
    let label = action?.tooltip || "";
    if (action?.badge) {
      label += `, ${action.badge} ${this.literals?.notifications}`;
    }
    return label;
  }
  checkLink(item) {
    if (item.link) {
      if (PoUtils.isExternalLink(item.link)) {
        window.open(item.link, "_blank");
      } else {
        this.router.navigateByUrl(item.link);
      }
    }
  }
  static ɵfac = function PoHeaderToolsComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoHeaderToolsComponent)(ɵɵdirectiveInject(Router), ɵɵdirectiveInject(ChangeDetectorRef));
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoHeaderToolsComponent,
    selectors: [["po-header-tools"]],
    viewQuery: function PoHeaderToolsComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c187, 5, ElementRef)(_c187, 5)(_c188, 5)(_c189, 5);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.buttonActionElements = _t);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.buttonActionComponents = _t);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.poPopupActions = _t);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.poPopoverActions = _t);
      }
    },
    inputs: {
      forceActionTools: [0, "p-force-actions-tools", "forceActionTools"],
      actionTools: [0, "p-actions-tools", "actionTools"],
      literals: [0, "p-literals", "literals"],
      size: [1, "p-size", "size"],
      headerTemplate: [0, "p-header-template", "headerTemplate"]
    },
    standalone: false,
    decls: 4,
    vars: 1,
    consts: [["buttonActionElement", ""], ["badge", ""], ["poPopupAction", ""], ["poPopoverAction", ""], [1, "po-header-nav-tools"], [3, "po-header-nav-item-small"], [4, "ngTemplateOutlet"], ["p-kind", "tertiary", 3, "p-click", "p-tooltip", "p-icon", "p-aria-expanded", "p-aria-label", "p-size"], [3, "p-close", "p-open", "p-actions", "p-target", "p-hide-arrow", "p-size"], ["p-position", "bottom-right", 3, "p-width", "p-target", "p-hide-arrow", "p-corner-aligned"], [1, "po-header-badge"], [1, "po-header-status", 3, "p-value"], ["p-position", "bottom-right", 3, "p-close", "p-open", "p-width", "p-target", "p-hide-arrow", "p-corner-aligned"]],
    template: function PoHeaderToolsComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "div", 4);
        ɵɵconditionalCreate(1, PoHeaderToolsComponent_Conditional_1_Template, 1, 1, "ng-container");
        ɵɵrepeaterCreate(2, PoHeaderToolsComponent_For_3_Template, 8, 14, "li", 5, _forTrack1);
        ɵɵelementEnd();
      }
      if (rf & 2) {
        ɵɵadvance();
        ɵɵconditional(ctx.headerTemplate ? 1 : -1);
        ɵɵadvance();
        ɵɵrepeater(ctx.actionTools);
      }
    },
    dependencies: [NgTemplateOutlet, PoButtonComponent, PoBadgeComponent, PoPopoverComponent, PoTooltipDirective, PoPopupComponent],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoHeaderToolsComponent, [{
    type: Component,
    args: [{
      selector: "po-header-tools",
      standalone: false,
      template: `<div class="po-header-nav-tools">
  @if (headerTemplate) {
    <ng-container *ngTemplateOutlet="headerTemplate"></ng-container>
  }
  @for (action of actionTools; track action?.label; let i = $index) {
    <li [class.po-header-nav-item-small]="i !== actionTools.length - 1">
      <po-button
        #buttonActionElement
        [p-tooltip]="action?.tooltip"
        [p-icon]="action?.badge ? badge : action?.icon || 'an an-dots-nine'"
        p-kind="tertiary"
        (p-click)="onClickAction(i)"
        [class.po-header-selected-tool]="checkSelected(i)"
        [p-aria-expanded]="checkSelected(i)"
        [p-aria-label]="getAriaLabel(action)"
        [p-size]="size()"
      >
      </po-button>

      <ng-template #badge>
        <i class="po-header-badge" [class]="action?.icon || 'an an-bell'">
          <po-badge class="po-header-status" [p-value]="action?.badge"></po-badge>
        </i>
      </ng-template>

      <po-popup
        #poPopupAction
        [p-actions]="action?.items"
        [p-target]="buttonActionElements?.get(i)"
        [p-hide-arrow]="true"
        (p-close)="onClosePopup(i); action?.onClose?.(action?.label)"
        (p-open)="action?.onOpen?.(action?.label)"
        [p-size]="size()"
      >
      </po-popup>

      @if (action?.popover) {
        <po-popover
          #poPopoverAction
          p-position="bottom-right"
          [p-width]="action.popover.width"
          [p-target]="buttonActionElements?.get(i)"
          [p-hide-arrow]="true"
          [p-corner-aligned]="true"
          (p-close)="action?.onClose?.(action?.label)"
          (p-open)="action?.onOpen?.(action?.label)"
        >
          <ng-container *ngTemplateOutlet="action.popover?.content"></ng-container>
        </po-popover>
      }
    </li>
  }
</div>
`
    }]
  }], () => [{
    type: Router
  }, {
    type: ChangeDetectorRef
  }], {
    buttonActionElements: [{
      type: ViewChildren,
      args: ["buttonActionElement", {
        read: ElementRef
      }]
    }],
    buttonActionComponents: [{
      type: ViewChildren,
      args: ["buttonActionElement"]
    }],
    poPopupActions: [{
      type: ViewChildren,
      args: ["poPopupAction"]
    }],
    poPopoverActions: [{
      type: ViewChildren,
      args: ["poPopoverAction"]
    }],
    forceActionTools: [{
      type: Input,
      args: ["p-force-actions-tools"]
    }],
    actionTools: [{
      type: Input,
      args: ["p-actions-tools"]
    }],
    literals: [{
      type: Input,
      args: ["p-literals"]
    }],
    size: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-size",
        required: false
      }]
    }],
    headerTemplate: [{
      type: Input,
      args: ["p-header-template"]
    }]
  });
})();
var poHeaderCustomerLiteralsDefault = {
  en: {
    labelUser: "Open user Navigation"
  },
  es: {
    labelUser: "Abrir navegación de usuario"
  },
  pt: {
    labelUser: "Abrir navegação do usuário"
  },
  ru: {
    labelUser: "Открыть навигацию пользователя"
  }
};
var PoHeaderCustomerComponent = class _PoHeaderCustomerComponent {
  literals;
  poPopupAction;
  poPopoverAction;
  headerUser;
  size = input(void 0, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "size"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    alias: "p-size"
  }));
  constructor(languageService) {
    const language = languageService.getShortLanguage();
    this.literals = __spreadValues(__spreadValues({}, poHeaderCustomerLiteralsDefault[poLocaleDefault]), poHeaderCustomerLiteralsDefault[language]);
  }
  onClickPopup() {
    if (this.headerUser.items && !this.headerUser.popover) {
      this.poPopupAction.toggle();
    }
  }
  onClickUserSection() {
    this.headerUser.action?.();
  }
  onClickClosePopover() {
    this.poPopoverAction.close();
  }
  onKeyDownCustomer(event) {
    if (event.code === "Space" || event.code === "Enter") {
      this.headerUser.action?.();
      if (!this.headerUser.popover) {
        this.poPopupAction.toggle();
      } else if (this.poPopoverAction.isHidden) {
        this.poPopoverAction.open();
      } else {
        this.poPopoverAction.close();
      }
    }
  }
  static ɵfac = function PoHeaderCustomerComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoHeaderCustomerComponent)(ɵɵdirectiveInject(PoLanguageService));
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoHeaderCustomerComponent,
    selectors: [["po-header-customer"]],
    viewQuery: function PoHeaderCustomerComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c188, 5)(_c189, 5);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.poPopupAction = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.poPopoverAction = _t.first);
      }
    },
    inputs: {
      headerUser: [0, "p-header-user", "headerUser"],
      size: [1, "p-size", "size"]
    },
    standalone: false,
    decls: 16,
    vars: 8,
    consts: [["targetRef", ""], ["poPopupAction", ""], ["poPopoverAction", ""], [1, "po-header-nav-tools", 3, "click", "keydown"], ["tabindex", "0", "role", "menu", 1, "po-header-nav-customer-container", 3, "click"], [1, "po-header-nav-customer-brand"], ["alt", "User", 3, "src"], [1, "po-header-nav-customer-divider"], [1, "po-header-nav-customer-avatar-wrapper"], [1, "po-header-nav-customer-avatar"], ["alt", "Avatar", 3, "src"], ["p-size", "small", 1, "po-header-nav-avatar-status", 3, "p-status"], [1, "po-header-nav-customer-avatar-wrapper", "po-header-icon-small"], ["p-position", "bottom-left", 3, "p-actions", "p-target", "p-hide-arrow", "p-size"], ["p-position", "bottom-right", 3, "p-width", "p-target", "p-hide-arrow", "p-corner-aligned"], ["p-position", "bottom-left", 3, "p-close", "p-open", "p-actions", "p-target", "p-hide-arrow", "p-size"], ["p-position", "bottom-right", 3, "p-close", "p-open", "p-width", "p-target", "p-hide-arrow", "p-corner-aligned"], [4, "ngTemplateOutlet"]],
    template: function PoHeaderCustomerComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "div", 3);
        ɵɵlistener("click", function PoHeaderCustomerComponent_Template_div_click_0_listener() {
          return ctx.onClickUserSection();
        })("keydown", function PoHeaderCustomerComponent_Template_div_keydown_0_listener($event) {
          return ctx.onKeyDownCustomer($event);
        });
        ɵɵelementStart(1, "li", 4, 0);
        ɵɵlistener("click", function PoHeaderCustomerComponent_Template_li_click_1_listener() {
          return ctx.onClickPopup();
        });
        ɵɵelementStart(3, "div", 5);
        ɵɵelement(4, "img", 6);
        ɵɵelementEnd();
        ɵɵelement(5, "div", 7);
        ɵɵelementStart(6, "div", 8)(7, "div", 9);
        ɵɵelement(8, "img", 10);
        ɵɵelementEnd();
        ɵɵconditionalCreate(9, PoHeaderCustomerComponent_Conditional_9_Template, 1, 1, "po-badge", 11);
        ɵɵelementEnd()();
        ɵɵelementStart(10, "li", 12)(11, "div", 9);
        ɵɵelement(12, "img", 10);
        ɵɵelementEnd();
        ɵɵconditionalCreate(13, PoHeaderCustomerComponent_Conditional_13_Template, 1, 1, "po-badge", 11);
        ɵɵelementEnd()();
        ɵɵconditionalCreate(14, PoHeaderCustomerComponent_Conditional_14_Template, 2, 4, "po-popup", 13);
        ɵɵconditionalCreate(15, PoHeaderCustomerComponent_Conditional_15_Template, 3, 5, "po-popover", 14);
      }
      if (rf & 2) {
        ɵɵadvance();
        ɵɵattribute("aria-label", ctx.literals.labelUser);
        ɵɵadvance(3);
        ɵɵproperty("src", ctx.headerUser == null ? null : ctx.headerUser.customerBrand, ɵɵsanitizeUrl);
        ɵɵadvance(4);
        ɵɵproperty("src", ctx.headerUser == null ? null : ctx.headerUser.avatar, ɵɵsanitizeUrl);
        ɵɵadvance();
        ɵɵconditional((ctx.headerUser == null ? null : ctx.headerUser.status) ? 9 : -1);
        ɵɵadvance(3);
        ɵɵproperty("src", ctx.headerUser == null ? null : ctx.headerUser.avatar, ɵɵsanitizeUrl);
        ɵɵadvance();
        ɵɵconditional((ctx.headerUser == null ? null : ctx.headerUser.status) ? 13 : -1);
        ɵɵadvance();
        ɵɵconditional((ctx.headerUser == null ? null : ctx.headerUser.items == null ? null : ctx.headerUser.items.length) ? 14 : -1);
        ɵɵadvance();
        ɵɵconditional((ctx.headerUser == null ? null : ctx.headerUser.popover) ? 15 : -1);
      }
    },
    dependencies: [NgTemplateOutlet, PoBadgeComponent, PoPopoverComponent, PoPopupComponent],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoHeaderCustomerComponent, [{
    type: Component,
    args: [{
      selector: "po-header-customer",
      standalone: false,
      template: '<div class="po-header-nav-tools" (click)="onClickUserSection()" (keydown)="onKeyDownCustomer($event)">\n  <li\n    #targetRef\n    tabindex="0"\n    class="po-header-nav-customer-container"\n    (click)="onClickPopup()"\n    role="menu"\n    [attr.aria-label]="literals.labelUser"\n  >\n    <div class="po-header-nav-customer-brand">\n      <img [src]="headerUser?.customerBrand" alt="User" />\n    </div>\n\n    <div class="po-header-nav-customer-divider"></div>\n\n    <div class="po-header-nav-customer-avatar-wrapper">\n      <div class="po-header-nav-customer-avatar">\n        <img [src]="headerUser?.avatar" alt="Avatar" />\n      </div>\n      @if (headerUser?.status) {\n        <po-badge class="po-header-nav-avatar-status" p-size="small" [p-status]="headerUser.status"></po-badge>\n      }\n    </div>\n  </li>\n\n  <li class="po-header-nav-customer-avatar-wrapper po-header-icon-small">\n    <div class="po-header-nav-customer-avatar">\n      <img [src]="headerUser?.avatar" alt="Avatar" />\n    </div>\n    @if (headerUser?.status) {\n      <po-badge class="po-header-nav-avatar-status" p-size="small" [p-status]="headerUser.status"></po-badge>\n    }\n  </li>\n</div>\n\n@if (headerUser?.items?.length) {\n  <po-popup\n    #poPopupAction\n    [p-actions]="headerUser.items"\n    [p-target]="targetRef"\n    [p-hide-arrow]="true"\n    p-position="bottom-left"\n    (p-close)="headerUser?.onClose?.()"\n    (p-open)="headerUser?.onOpen?.()"\n    [p-size]="size()"\n  >\n  </po-popup>\n}\n\n@if (headerUser?.popover) {\n  <po-popover\n    #poPopoverAction\n    p-position="bottom-right"\n    [p-width]="headerUser.popover.width"\n    [p-target]="targetRef"\n    [p-hide-arrow]="true"\n    [p-corner-aligned]="true"\n    (p-close)="headerUser?.onClose?.()"\n    (p-open)="headerUser?.onOpen?.()"\n  >\n    <ng-container *ngTemplateOutlet="headerUser.popover.content"></ng-container>\n  </po-popover>\n}\n'
    }]
  }], () => [{
    type: PoLanguageService
  }], {
    poPopupAction: [{
      type: ViewChild,
      args: ["poPopupAction"]
    }],
    poPopoverAction: [{
      type: ViewChild,
      args: ["poPopoverAction"]
    }],
    headerUser: [{
      type: Input,
      args: ["p-header-user"]
    }],
    size: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "p-size",
        required: false
      }]
    }]
  });
})();
var PoHeaderComponent = class _PoHeaderComponent extends PoHeaderBaseComponent {
  cd;
  elRef;
  menuGlobalService;
  afterViewInitWascalled = false;
  showMenu = false;
  visibleMenuItems = [...this.menuItems];
  overflowItems = [];
  showOverflow = false;
  resizeSubscription;
  menusSubscription;
  applicationMenuSubscription;
  menuIdSubscription;
  menuonChangesSubscription;
  removedMenuSubscription;
  previousMenuComponentId;
  resizeSub;
  menuExternal = [];
  existMenuExternal = false;
  applicationMenu;
  id = uuid();
  currentWidth = 0;
  previousMenusItems = [];
  buttonFirstAction;
  buttonFirstActionRef;
  navElement;
  menuWrapperBrand;
  menuWrapperTools;
  overflowButton;
  overflowButtonComponentEl;
  menuSubItems;
  menuHeaderTemplate;
  set menuComponent(menu) {
    this.previousMenuComponentId = menu?.id || this.previousMenuComponentId;
  }
  constructor(cd, elRef, menuGlobalService, poLanguageService) {
    super(poLanguageService);
    this.cd = cd;
    this.elRef = elRef;
    this.menuGlobalService = menuGlobalService;
  }
  ngOnInit() {
    this.menuonChangesSubscription = this.menuGlobalService.receiveOnChange$.subscribe((_newMenus) => {
      if (!this.applicationMenu) return;
      if (this.currentWidth <= 960) {
        this.handleSmallSreen();
      } else {
        this.handleLargeSreen();
      }
      this.cd.detectChanges();
    });
    this.menusSubscription = this.menuGlobalService.receiveMenus$.subscribe((newMenus) => {
      this.previousMenusItems = [...newMenus].filter((item) => item.id !== this.id);
    });
    this.removedMenuSubscription = this.menuGlobalService.receiveRemovedApplicationMenu$.subscribe((removedMenuId) => {
      this.applicationMenu = this.applicationMenu && (this.previousMenuComponentId === removedMenuId || this.notChangeContext) ? this.applicationMenu : void 0;
      if (this.currentWidth <= 960) {
        this.handleSmallSreen();
      } else {
        this.handleLargeSreen();
      }
      this.cd.detectChanges();
    });
    this.menuIdSubscription = this.menuGlobalService.receiveId$.subscribe((id) => {
      if (id !== "po-header-nav-bar") {
        this.existMenuExternal = true;
      }
    });
    this.applicationMenuSubscription = this.menuGlobalService.receiveApplicationMenu$.pipe(delay(100)).subscribe((newMenu) => {
      if (this.applicationMenu && this.notChangeContext && this.previousMenuComponentId !== newMenu.id) {
        return;
      }
      this.applicationMenu = this.previousMenuComponentId === newMenu.id ? void 0 : newMenu;
      if (this.applicationMenu) {
        this.updateMenu();
        this.combineItemsExternal();
        if (this.currentWidth <= 960) {
          this.handleSmallSreen();
        }
      }
      this.cd.detectChanges();
    });
    this.resizeSub = fromEvent(window, "resize").pipe(debounceTime(200), map(() => window.innerWidth), startWith(window.innerWidth)).subscribe((width) => {
      this.currentWidth = width;
      if (width <= 960) {
        this.handleSmallSreen();
      } else {
        this.handleLargeSreen();
      }
    });
  }
  ngOnChanges(changes) {
    if (changes["amountMore"]) {
      this.updateButtonMore();
    }
    if (changes["menuCollapse"] && this.afterViewInitWascalled) {
      if (this.currentWidth <= 960) {
        this.handleSmallSreen();
      } else {
        this.handleLargeSreen();
      }
    }
    if (changes["menuItems"] && this.afterViewInitWascalled) {
      this.updateMenu();
      this.combineItemsExternal();
      if (this.currentWidth <= 960) {
        this.handleSmallSreen();
      }
      setTimeout(() => {
        this.updateMenu();
        this.cd.detectChanges();
      });
    }
  }
  ngAfterViewInit() {
    this.updateMenu();
    this.cd.detectChanges();
    this.resizeSubscription = fromEvent(window, "resize").pipe(debounceTime(300)).subscribe(() => {
      this.updateMenu();
    });
    this.combineItems();
    this.combineItemsExternal();
    this.afterViewInitWascalled = true;
  }
  ngOnDestroy() {
    this.resizeSub?.unsubscribe();
    this.removedMenuSubscription?.unsubscribe();
    this.applicationMenuSubscription?.unsubscribe();
    this.menusSubscription?.unsubscribe();
    this.menuIdSubscription?.unsubscribe();
    this.menuonChangesSubscription?.unsubscribe();
  }
  toggleOverflowDropdown() {
    this.showOverflow = !this.showOverflow;
  }
  updateMenu() {
    if (this.amountMore) {
      this.updateButtonMore();
      return;
    }
    const hostElement = this.elRef.nativeElement;
    const parent = hostElement.parentElement;
    let screenWidth;
    if (parent?.getBoundingClientRect().width) {
      screenWidth = parent.getBoundingClientRect().width;
    } else {
      screenWidth = document.documentElement.clientWidth;
    }
    const brandWidth = this.menuWrapperBrand.nativeElement.offsetWidth;
    const toolWidth = this.menuWrapperTools.nativeElement.offsetWidth;
    const spacingAndMoreButton = 170;
    const remaining = screenWidth - brandWidth - toolWidth - spacingAndMoreButton;
    const temp = [...this.visibleMenuItems];
    this.visibleMenuItems = [...this.menuItems];
    this.overflowItems = [];
    this.cd.detectChanges();
    const itemWidths = this.menuSubItems.toArray().map((el) => el.nativeElement.offsetWidth + 16);
    this.visibleMenuItems = temp;
    let usedWidth = 0;
    for (let i2 = 0; i2 < this.menuItems.length; i2++) {
      const itemWidth = itemWidths[i2];
      if (usedWidth + itemWidth >= remaining) {
        this.overflowItems = [...this.overflowItems, this.menuItems[i2]];
      }
      usedWidth += itemWidths[i2];
    }
    this.visibleMenuItems = [...this.menuItems].filter((item) => !this.overflowItems.some((overflow) => overflow.id === item.id));
    this.cd.detectChanges();
    if (this.overflowItems.length) {
      this.showOverflow = true;
    } else {
      this.showOverflow = false;
    }
  }
  updateButtonMore() {
    let itemVisible;
    let itemsInFlow;
    if (this.amountMore <= 0) {
      itemVisible = [...this.menuItems];
      itemsInFlow = [];
    } else if (this.amountMore >= this.menuItems.length) {
      itemVisible = [];
      itemsInFlow = [...this.menuItems];
    } else {
      itemVisible = this.menuItems.slice(0, this.menuItems.length - this.amountMore);
      itemsInFlow = this.menuItems.slice(-this.amountMore);
    }
    this.visibleMenuItems = [...itemVisible];
    this.overflowItems = [...itemsInFlow];
    if (this.overflowItems.length) {
      this.showOverflow = true;
    } else {
      this.showOverflow = false;
    }
  }
  onClickMenu() {
    if (this.menuCollapseJoin.length) {
      this.showMenu = !this.showMenu;
    }
    if (!this.sideMenuOnlyAction) {
      this.applicationMenu?.toggleMenuMobile();
    }
    this.cd.detectChanges();
    this.colapsedMenuEvent.emit();
  }
  onCloseMenu() {
    this.showMenu = false;
  }
  handleSmallSreen() {
    if (this.applicationMenu && !this.notChangeContext) {
      this.applicationMenu.menus = [{
        label: this.literals.headerLinks,
        subItems: [...this.menuCollapseJoinExternal],
        id: this.id
      }, ...this.previousMenusItems];
    } else {
      this.menuCollapseJoin = this.combineItems();
    }
    this.cd.detectChanges();
  }
  handleLargeSreen() {
    if (this.applicationMenu && !this.notChangeContext) {
      this.applicationMenu.menus = [...this.previousMenusItems];
    } else {
      this.menuCollapseJoin = [...this.menuCollapse];
    }
    this.cd.detectChanges();
  }
  combineItems() {
    const toolActions = this.actionsTools.map((item) => ({
      label: item.label,
      action: item.action,
      link: item.link
    }));
    const menuActions = this.menuItems.map((item) => ({
      label: item.label,
      action: item.action,
      link: item.link
    }));
    const joinMenu = {
      label: this.literals.headerLinks,
      subItems: [...menuActions, ...toolActions],
      id: this.id
    };
    return [...this.menuCollapse, joinMenu];
  }
  combineItemsExternal() {
    const toolActions = this.actionsTools.map((item) => ({
      label: item.label,
      action: item.action,
      link: item.link
    }));
    const menuActions = this.menuItems.map((item) => ({
      label: item.label,
      action: item.action,
      link: item.link
    }));
    this.menuCollapseJoinExternal = [...menuActions, ...toolActions];
  }
  onSelected({
    item,
    focus
  }) {
    if (item.$internalRoute) {
      this.menuItems = this.menuItems.map((menuItem) => __spreadProps(__spreadValues({}, menuItem), {
        $selected: menuItem.id === item.id
      }));
    }
    this.updateMenu();
    this.combineItems();
    this.combineItemsExternal();
    if (focus) {
      this.overflowButtonComponentEl.onClosePopup();
    }
  }
  static ɵfac = function PoHeaderComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoHeaderComponent)(ɵɵdirectiveInject(ChangeDetectorRef), ɵɵdirectiveInject(ElementRef), ɵɵdirectiveInject(PoMenuGlobalService), ɵɵdirectiveInject(PoLanguageService));
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoHeaderComponent,
    selectors: [["po-header"]],
    contentQueries: function PoHeaderComponent_ContentQueries(rf, ctx, dirIndex) {
      if (rf & 1) {
        ɵɵcontentQuery(dirIndex, PoMenuHeaderTemplateDirective, 7);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.menuHeaderTemplate = _t.first);
      }
    },
    viewQuery: function PoHeaderComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(_c190, 5, ElementRef)(_c191, 5, ElementRef)(_c192, 5)(_c193, 5)(_c194, 7)(_c194, 5)(PoMenuComponent, 5)(_c195, 5, ElementRef);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.buttonFirstAction = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.navElement = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.menuWrapperBrand = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.menuWrapperTools = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.overflowButton = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.overflowButtonComponentEl = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.menuComponent = _t.first);
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.menuSubItems = _t);
      }
    },
    standalone: false,
    features: [ɵɵInheritDefinitionFeature, ɵɵNgOnChangesFeature],
    decls: 15,
    vars: 13,
    consts: [["navElement", ""], ["menuWrapperBrand", ""], ["menuWrapperItens", ""], ["menuWrapperTools", ""], ["menusubmenuitem", ""], ["overflowButton", "", "menusubmenuitem", ""], [1, "po-header-nav"], [3, "p-click-menu", "p-brand", "p-menus", "p-external", "p-hide-button-menu", "p-size"], [1, "po-header-nav-submenus-wrapper"], [3, "p-item", "p-size"], [3, "p-button-overflow", "p-item-overflow", "p-size"], [1, "po-header-tools-wrapper"], [3, "p-force-actions-tools", "p-actions-tools", "p-header-template", "p-literals", "p-size"], [3, "p-header-user", "p-size"], [3, "p-item-click", "p-item", "p-size"], [3, "p-item-click", "p-button-overflow", "p-item-overflow", "p-size"], [1, "po-header-overlay", 3, "click"], [1, "po-header-side-menu"], [1, "po-header-sidemenu-outer"], ["p-menu-id", "po-header-nav-bar", 3, "p-only-menu-item", "p-menus", "p-filter"], [4, "p-menu-header-template"], [4, "ngTemplateOutlet"]],
    template: function PoHeaderComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "nav", 6, 0)(2, "div", null, 1)(4, "po-header-brand", 7);
        ɵɵlistener("p-click-menu", function PoHeaderComponent_Template_po_header_brand_p_click_menu_4_listener() {
          return ctx.onClickMenu();
        });
        ɵɵelementEnd()();
        ɵɵelementStart(5, "ul", 8, 2);
        ɵɵrepeaterCreate(7, PoHeaderComponent_For_8_Template, 2, 2, "po-header-menu-item", 9, _forTrack2);
        ɵɵelementEnd();
        ɵɵconditionalCreate(9, PoHeaderComponent_Conditional_9_Template, 3, 3, "po-header-menu-item", 10);
        ɵɵelementStart(10, "ul", 11, 3);
        ɵɵelement(12, "po-header-tools", 12);
        ɵɵconditionalCreate(13, PoHeaderComponent_Conditional_13_Template, 1, 2, "po-header-customer", 13);
        ɵɵelementEnd()();
        ɵɵconditionalCreate(14, PoHeaderComponent_Conditional_14_Template, 5, 7);
      }
      if (rf & 2) {
        ɵɵadvance(4);
        ɵɵproperty("p-brand", ctx.brand)("p-menus", ctx.menuCollapseJoin)("p-external", ctx.applicationMenu)("p-hide-button-menu", ctx.hideButtonMenu)("p-size", ctx.size());
        ɵɵadvance(3);
        ɵɵrepeater(ctx.visibleMenuItems);
        ɵɵadvance(2);
        ɵɵconditional(ctx.showOverflow && ctx.overflowItems.length ? 9 : -1);
        ɵɵadvance(3);
        ɵɵproperty("p-force-actions-tools", ctx.forceActionTools)("p-actions-tools", ctx.actionsTools)("p-header-template", ctx.headerTemplate)("p-literals", ctx.literals)("p-size", ctx.size());
        ɵɵadvance();
        ɵɵconditional((ctx.headerUser == null ? null : ctx.headerUser.customerBrand) && (ctx.headerUser == null ? null : ctx.headerUser.avatar) ? 13 : -1);
        ɵɵadvance();
        ɵɵconditional(!ctx.sideMenuOnlyAction && !ctx.applicationMenu && ctx.menuCollapseJoin.length ? 14 : -1);
      }
    },
    dependencies: [NgTemplateOutlet, PoMenuComponent, PoMenuHeaderTemplateDirective, PoHeaderbrandComponent, PoHeaderMenuItemComponent, PoHeaderToolsComponent, PoHeaderCustomerComponent],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoHeaderComponent, [{
    type: Component,
    args: [{
      selector: "po-header",
      standalone: false,
      template: '<nav #navElement class="po-header-nav">\n  <div #menuWrapperBrand>\n    <po-header-brand\n      [p-brand]="brand"\n      (p-click-menu)="onClickMenu()"\n      [p-menus]="menuCollapseJoin"\n      [p-external]="applicationMenu"\n      [p-hide-button-menu]="hideButtonMenu"\n      [p-size]="size()"\n    ></po-header-brand>\n  </div>\n\n  <ul #menuWrapperItens class="po-header-nav-submenus-wrapper">\n    @for (item of visibleMenuItems; track item.id) {\n      <po-header-menu-item\n        #menusubmenuitem\n        [p-item]="item"\n        (p-item-click)="onSelected($event)"\n        [p-size]="size()"\n      ></po-header-menu-item>\n    }\n  </ul>\n\n  @if (showOverflow && overflowItems.length) {\n    <po-header-menu-item\n      #overflowButton\n      #menusubmenuitem\n      [p-button-overflow]="true"\n      [p-item-overflow]="overflowItems"\n      (p-item-click)="onSelected($event)"\n      [p-size]="size()"\n    ></po-header-menu-item>\n  }\n\n  <ul #menuWrapperTools class="po-header-tools-wrapper">\n    <po-header-tools\n      [p-force-actions-tools]="forceActionTools"\n      [p-actions-tools]="actionsTools"\n      [p-header-template]="headerTemplate"\n      [p-literals]="literals"\n      [p-size]="size()"\n    ></po-header-tools>\n\n    @if (headerUser?.customerBrand && headerUser?.avatar) {\n      <po-header-customer [p-header-user]="headerUser" [p-size]="size()"></po-header-customer>\n    }\n  </ul>\n</nav>\n\n@if (!sideMenuOnlyAction && !applicationMenu && menuCollapseJoin.length) {\n  <div class="po-header-overlay" [class.show]="showMenu" (click)="onCloseMenu()"></div>\n\n  <aside class="po-header-side-menu" [class.open]="showMenu">\n    <div class="po-header-sidemenu-outer">\n      <po-menu\n        [p-only-menu-item]="true"\n        [p-menus]="menuCollapseJoin"\n        [p-filter]="filterMenu"\n        p-menu-id="po-header-nav-bar"\n      >\n        <div *p-menu-header-template>\n          <ng-template *ngTemplateOutlet="menuHeaderTemplate?.templateRef"></ng-template>\n        </div>\n      </po-menu>\n    </div>\n  </aside>\n}\n'
    }]
  }], () => [{
    type: ChangeDetectorRef
  }, {
    type: ElementRef
  }, {
    type: PoMenuGlobalService
  }, {
    type: PoLanguageService
  }], {
    buttonFirstAction: [{
      type: ViewChild,
      args: ["buttonFirstAction", {
        read: ElementRef
      }]
    }],
    navElement: [{
      type: ViewChild,
      args: ["navElement", {
        read: ElementRef
      }]
    }],
    menuWrapperBrand: [{
      type: ViewChild,
      args: ["menuWrapperBrand"]
    }],
    menuWrapperTools: [{
      type: ViewChild,
      args: ["menuWrapperTools"]
    }],
    overflowButton: [{
      type: ViewChild,
      args: ["overflowButton", {
        static: true
      }]
    }],
    overflowButtonComponentEl: [{
      type: ViewChild,
      args: ["overflowButton"]
    }],
    menuSubItems: [{
      type: ViewChildren,
      args: ["menusubmenuitem", {
        read: ElementRef
      }]
    }],
    menuHeaderTemplate: [{
      type: ContentChild,
      args: [PoMenuHeaderTemplateDirective, {
        static: true
      }]
    }],
    menuComponent: [{
      type: ViewChild,
      args: [PoMenuComponent]
    }]
  });
})();
var PoHeaderModule = class _PoHeaderModule {
  static ɵfac = function PoHeaderModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoHeaderModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoHeaderModule,
    declarations: [PoHeaderComponent, PoHeaderbrandComponent, PoHeaderMenuItemComponent, PoHeaderToolsComponent, PoHeaderCustomerComponent],
    imports: [CommonModule, PoDropdownModule, PoIconModule, PoButtonModule, PoBadgeModule, PoAvatarModule, PoPopoverModule, PoTooltipModule, PoPopupModule, PoMenuModule, RouterModule],
    exports: [PoHeaderComponent]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [CommonModule, PoDropdownModule, PoIconModule, PoButtonModule, PoBadgeModule, PoAvatarModule, PoPopoverModule, PoTooltipModule, PoPopupModule, PoMenuModule, RouterModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoHeaderModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, PoDropdownModule, PoIconModule, PoButtonModule, PoBadgeModule, PoAvatarModule, PoPopoverModule, PoTooltipModule, PoPopupModule, PoMenuModule, RouterModule],
      declarations: [PoHeaderComponent, PoHeaderbrandComponent, PoHeaderMenuItemComponent, PoHeaderToolsComponent, PoHeaderCustomerComponent],
      exports: [PoHeaderComponent]
    }]
  }], null, null);
})();
var PO_MODULES = [PoAccordionModule, PoAvatarModule, PoBreadcrumbModule, PoButtonModule, PoButtonGroupModule, PoCalendarModule, PoChartModule, PoContainerModule, PoContextMenuModule, PoDisclaimerGroupModule, PoDisclaimerModule, PoDividerModule, PoDropdownModule, PoDynamicModule, PoFieldModule, PoFilterChipModule, PoGaugeModule, PoGridModule, PoIconModule, PoInfoModule, PoListViewModule, PoListBoxModule, PoLoadingModule, PoLogoModule, PoMenuModule, PoMenuPanelModule, PoModalModule, PoNavbarModule, PoOverlayModule, PoPageModule, PoPopoverModule, PoPopupModule, PoProgressModule, PoSlideModule, PoStepperModule, PoTableModule, PoTabsModule, PoContextTabsModule, PoTagModule, PoToolbarModule, PoTreeViewModule, PoWidgetModule, PoLinkModule, PoLabelModule, PoImageModule, PoPageSlideModule, PoSwitchModule, PoSearchModule, PoBadgeModule, PoSkeletonModule, PoToasterModule, PoHelperModule, PoHeaderModule, PoTimerModule];
var PoComponentsModule = class _PoComponentsModule {
  static ɵfac = function PoComponentsModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoComponentsModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoComponentsModule,
    imports: [PoAccordionModule, PoAvatarModule, PoBreadcrumbModule, PoButtonModule, PoButtonGroupModule, PoCalendarModule, PoChartModule, PoContainerModule, PoContextMenuModule, PoDisclaimerGroupModule, PoDisclaimerModule, PoDividerModule, PoDropdownModule, PoDynamicModule, PoFieldModule, PoFilterChipModule, PoGaugeModule, PoGridModule, PoIconModule, PoInfoModule, PoListViewModule, PoListBoxModule, PoLoadingModule, PoLogoModule, PoMenuModule, PoMenuPanelModule, PoModalModule, PoNavbarModule, PoOverlayModule, PoPageModule, PoPopoverModule, PoPopupModule, PoProgressModule, PoSlideModule, PoStepperModule, PoTableModule, PoTabsModule, PoContextTabsModule, PoTagModule, PoToolbarModule, PoTreeViewModule, PoWidgetModule, PoLinkModule, PoLabelModule, PoImageModule, PoPageSlideModule, PoSwitchModule, PoSearchModule, PoBadgeModule, PoSkeletonModule, PoToasterModule, PoHelperModule, PoHeaderModule, PoTimerModule],
    exports: [PoAccordionModule, PoAvatarModule, PoBreadcrumbModule, PoButtonModule, PoButtonGroupModule, PoCalendarModule, PoChartModule, PoContainerModule, PoContextMenuModule, PoDisclaimerGroupModule, PoDisclaimerModule, PoDividerModule, PoDropdownModule, PoDynamicModule, PoFieldModule, PoFilterChipModule, PoGaugeModule, PoGridModule, PoIconModule, PoInfoModule, PoListViewModule, PoListBoxModule, PoLoadingModule, PoLogoModule, PoMenuModule, PoMenuPanelModule, PoModalModule, PoNavbarModule, PoOverlayModule, PoPageModule, PoPopoverModule, PoPopupModule, PoProgressModule, PoSlideModule, PoStepperModule, PoTableModule, PoTabsModule, PoContextTabsModule, PoTagModule, PoToolbarModule, PoTreeViewModule, PoWidgetModule, PoLinkModule, PoLabelModule, PoImageModule, PoPageSlideModule, PoSwitchModule, PoSearchModule, PoBadgeModule, PoSkeletonModule, PoToasterModule, PoHelperModule, PoHeaderModule, PoTimerModule]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [PO_MODULES, PoAccordionModule, PoAvatarModule, PoBreadcrumbModule, PoButtonModule, PoButtonGroupModule, PoCalendarModule, PoChartModule, PoContainerModule, PoContextMenuModule, PoDisclaimerGroupModule, PoDisclaimerModule, PoDividerModule, PoDropdownModule, PoDynamicModule, PoFieldModule, PoFilterChipModule, PoGaugeModule, PoGridModule, PoIconModule, PoInfoModule, PoListViewModule, PoListBoxModule, PoLoadingModule, PoLogoModule, PoMenuModule, PoMenuPanelModule, PoModalModule, PoNavbarModule, PoOverlayModule, PoPageModule, PoPopoverModule, PoPopupModule, PoProgressModule, PoSlideModule, PoStepperModule, PoTableModule, PoTabsModule, PoContextTabsModule, PoTagModule, PoToolbarModule, PoTreeViewModule, PoWidgetModule, PoLinkModule, PoLabelModule, PoImageModule, PoPageSlideModule, PoSwitchModule, PoSearchModule, PoBadgeModule, PoSkeletonModule, PoToasterModule, PoHelperModule, PoHeaderModule, PoTimerModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoComponentsModule, [{
    type: NgModule,
    args: [{
      imports: PO_MODULES,
      exports: PO_MODULES,
      providers: [],
      bootstrap: [],
      declarations: []
    }]
  }], null, null);
})();
var PoGuardsModule = class _PoGuardsModule {
  static ɵfac = function PoGuardsModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoGuardsModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoGuardsModule
  });
  static ɵinj = ɵɵdefineInjector({});
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoGuardsModule, [{
    type: NgModule,
    args: [{
      declarations: [],
      imports: [],
      exports: [],
      providers: [],
      bootstrap: []
    }]
  }], null, null);
})();
var poHttpInterceptorDetailLiteralsDefault = {
  en: {
    closeButton: "Close",
    details: "Details",
    detail: "Detail",
    error: "Error",
    warning: "Warning",
    information: "Information",
    success: "Success"
  },
  es: {
    closeButton: "Cerrar",
    details: "Detalles",
    detail: "Detalle",
    error: "Error",
    warning: "Advertencia",
    information: "Informacion",
    success: "Éxito"
  },
  pt: {
    closeButton: "Fechar",
    details: "Detalhes",
    detail: "Detalhe",
    error: "Erro",
    warning: "Aviso",
    information: "Informação",
    success: "Sucesso"
  },
  ru: {
    closeButton: "Закрывать",
    details: "Детали",
    detail: "Деталь",
    error: "Ошибка",
    warning: "Предупреждение",
    information: "Информация",
    success: "Yспех"
  }
};
var colors = {
  success: "color-11",
  error: "color-07",
  warning: "color-08",
  info: ""
};
var PoHttpInterceptorDetailComponent = class _PoHttpInterceptorDetailComponent {
  languageService;
  modal;
  closed = new EventEmitter();
  details = [];
  title;
  language;
  literals;
  // eslint-disable-next-line @typescript-eslint/member-ordering
  primaryAction;
  constructor(languageService) {
    this.languageService = languageService;
    this.language = this.languageService?.getShortLanguage();
    this.literals = poHttpInterceptorDetailLiteralsDefault[this.language];
  }
  ngOnInit() {
    this.primaryAction = {
      action: () => this.close(),
      label: this.literals.closeButton
    };
  }
  set detail(details) {
    if (details && details.length) {
      this.details = this.filterByValidDetails(details);
    }
    this.title = this.formatTitle(this.details);
  }
  close() {
    this.modal.close();
    this.closed.emit();
  }
  formatDetailItemTitle(detail) {
    return detail.detailTitle ? detail.detailTitle : detail.code ? `${detail.code} - ${detail.message}` : detail.message;
  }
  open() {
    this.modal.open();
  }
  typeColor(type) {
    return colors[type];
  }
  typeValue(type) {
    return poHttpInterceptorDetailLiteralsDefault[this.language][type] || type;
  }
  addValidDetail(newDetails, detail) {
    return detail.message ? newDetails.concat(this.getValidDetailProperties(detail)) : newDetails;
  }
  getValidDetailProperties({
    code,
    message,
    detailedMessage,
    type,
    detailTitle
  }) {
    return detailTitle ? {
      code,
      message,
      detailedMessage,
      type,
      detailTitle
    } : {
      code,
      message,
      detailedMessage,
      type
    };
  }
  filterByValidDetails(details) {
    return details.reduce((newDetails, detail) => this.addValidDetail(newDetails, detail), []);
  }
  formatTitle(details) {
    return details.length > 1 ? `${this.literals.details} (${details.length})` : details.length === 1 ? this.formatDetailItemTitle(details[0]) : this.literals.detail;
  }
  static ɵfac = function PoHttpInterceptorDetailComponent_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoHttpInterceptorDetailComponent)(ɵɵdirectiveInject(PoLanguageService));
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _PoHttpInterceptorDetailComponent,
    selectors: [["po-http-interceptor-detail"]],
    viewQuery: function PoHttpInterceptorDetailComponent_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuery(PoModalComponent, 7);
      }
      if (rf & 2) {
        let _t;
        ɵɵqueryRefresh(_t = ɵɵloadQuery()) && (ctx.modal = _t.first);
      }
    },
    standalone: false,
    decls: 6,
    vars: 3,
    consts: [["body", ""], ["p-hide-close", "", "p-size", "lg", 3, "p-primary-action", "p-title"], [1, "po-row"], [1, "po-md-12", "po-mt-1", "po-mb-1"], [3, "ngTemplateOutlet", "ngTemplateOutletContext"], [3, "p-label"], [1, "po-row", "po-mb-1"], [3, "p-color", "p-value"]],
    template: function PoHttpInterceptorDetailComponent_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵelementStart(0, "po-modal", 1)(1, "div", 2);
        ɵɵconditionalCreate(2, PoHttpInterceptorDetailComponent_Conditional_2_Template, 3, 0, "po-accordion", 3)(3, PoHttpInterceptorDetailComponent_Conditional_3_Template, 1, 5, "ng-container", 4);
        ɵɵelementEnd()();
        ɵɵtemplate(4, PoHttpInterceptorDetailComponent_ng_template_4_Template, 5, 3, "ng-template", null, 0, ɵɵtemplateRefExtractor);
      }
      if (rf & 2) {
        ɵɵproperty("p-primary-action", ctx.primaryAction)("p-title", ctx.title);
        ɵɵadvance(2);
        ɵɵconditional(ctx.details.length > 1 ? 2 : 3);
      }
    },
    dependencies: [NgTemplateOutlet, PoAccordionComponent, PoAccordionItemComponent, PoModalComponent, PoTagComponent],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoHttpInterceptorDetailComponent, [{
    type: Component,
    args: [{
      selector: "po-http-interceptor-detail",
      standalone: false,
      template: '<po-modal p-hide-close p-size="lg" [p-primary-action]="primaryAction" [p-title]="title">\n  <div class="po-row">\n    @if (details.length > 1) {\n      <po-accordion class="po-md-12 po-mt-1 po-mb-1">\n        @for (detail of details; track detail) {\n          <po-accordion-item [p-label]="formatDetailItemTitle(detail)">\n            <ng-template [ngTemplateOutlet]="body" [ngTemplateOutletContext]="{ detail }"></ng-template>\n          </po-accordion-item>\n        }\n      </po-accordion>\n    } @else {\n      <ng-container [ngTemplateOutlet]="body" [ngTemplateOutletContext]="{ detail: details[0] || {} }"></ng-container>\n    }\n  </div>\n</po-modal>\n\n<ng-template #body let-detail="detail">\n  @if (detail.type) {\n    <div class="po-row po-mb-1">\n      <po-tag [p-color]="typeColor(detail.type)" [p-value]="typeValue(detail.type)"></po-tag>\n    </div>\n  }\n\n  @if (detail.detailTitle) {\n    <p>\n      <strong>{{ detail.code }}</strong>\n    </p>\n  }\n\n  <div class="po-row">\n    <p>{{ detail.detailedMessage }}</p>\n  </div>\n</ng-template>\n'
    }]
  }], () => [{
    type: PoLanguageService
  }], {
    modal: [{
      type: ViewChild,
      args: [PoModalComponent, {
        static: true
      }]
    }]
  });
})();
var poHttpInterceptorLiterals = {
  pt: {
    help: "Ajuda",
    details: "Detalhes",
    serverNotResponse: "Servidor não está respondendo"
  },
  en: {
    help: "Help",
    details: "Details",
    serverNotResponse: "Server is not responding"
  },
  es: {
    help: "Ayuda",
    details: "Detalles",
    serverNotResponse: "El servidor no está respondiendo"
  },
  ru: {
    help: "Помогите",
    details: "Детали",
    serverNotResponse: "Сервер не отвечает"
  }
};
var NO_ERROR_HEADER_PARAM = "X-PO-No-Error";
var NO_MESSAGE_HEADER_PARAM = "X-PO-No-Message";
var PoHttpInterceptorBaseService = class {
  componentInjector;
  notification;
  languageService;
  notificationTypes = ["success", "warning", "error", "information"];
  literals;
  httpInterceptorDetailComponent = void 0;
  constructor(componentInjector, notification, languageService) {
    this.componentInjector = componentInjector;
    this.notification = notification;
    this.languageService = languageService;
    this.literals = poHttpInterceptorLiterals[this.languageService?.getShortLanguage()];
  }
  intercept(request, next) {
    const cloneRequest = request.clone();
    request = request && this.hasParameters(request) ? this.cloneRequestWithoutParameters(request) : request;
    return next.handle(request).pipe(tap((response) => {
      if (response instanceof HttpResponse) {
        this.processResponse(response, cloneRequest);
      }
    }, (error2) => {
      this.processErrorResponse(error2, cloneRequest);
    }));
  }
  processResponse(response, request) {
    const hasNoMessageParam = this.hasNoMessageParam(request);
    if (!hasNoMessageParam && response.body && response.body._messages) {
      const messages = response.body._messages;
      if (messages instanceof Array) {
        messages.forEach((message) => {
          this.showNotification(message);
        });
      } else {
        this.showNotification(messages);
      }
    }
  }
  processErrorResponse(response, request) {
    const errorResponse = response.status !== 0 ? response.error : {
      code: 0,
      message: this.literals.serverNotResponse,
      detailedMessage: response.message
    };
    const hasNoErrorParam = this.hasNoErrorParam(request);
    const hasNoMessageParam = this.hasNoMessageParam(request);
    const errorResponseValidTypes = this.notificationTypes.slice(1);
    if (errorResponse && errorResponse.message && !hasNoErrorParam && !hasNoMessageParam) {
      this.showNotification(__spreadProps(__spreadValues({}, errorResponse), {
        type: errorResponseValidTypes.includes(errorResponse.type) ? errorResponse.type : "error"
      }));
    }
  }
  cloneRequestWithoutParameters(request) {
    const headers = request.headers.delete(NO_ERROR_HEADER_PARAM).delete(NO_MESSAGE_HEADER_PARAM);
    return request.clone({
      headers
    });
  }
  createModal(responseMessage) {
    const details = responseMessage.details ? [responseMessage, ...responseMessage.details] : [responseMessage];
    this.httpInterceptorDetailComponent = this.componentInjector.createComponentInApplication(PoHttpInterceptorDetailComponent);
    this.httpInterceptorDetailComponent.instance.detail = details;
    this.httpInterceptorDetailComponent.instance.closed.subscribe(() => this.destroyModal());
    this.httpInterceptorDetailComponent.instance.open();
  }
  destroyModal() {
    if (this.httpInterceptorDetailComponent) {
      this.componentInjector.destroyComponentInApplication(this.httpInterceptorDetailComponent);
      this.httpInterceptorDetailComponent = void 0;
    }
  }
  hasMessage(responseMessage) {
    const hasMessageProperties = responseMessage.message;
    return responseMessage && hasMessageProperties;
  }
  hasNoErrorParam(request) {
    const noErrorParam = request && request.headers.get(NO_ERROR_HEADER_PARAM);
    return noErrorParam && noErrorParam.toString().toLocaleLowerCase() === "true";
  }
  hasNoMessageParam(request) {
    const noMessageParam = request && request.headers.get(NO_MESSAGE_HEADER_PARAM);
    return noMessageParam && noMessageParam.toString().toLocaleLowerCase() === "true";
  }
  hasParameters(request) {
    return request.headers.has(NO_ERROR_HEADER_PARAM) || request.headers.has(NO_MESSAGE_HEADER_PARAM);
  }
  showNotification(response) {
    if (!this.hasMessage(response)) {
      return;
    }
    const typeNotification = this.notificationTypes.includes(response.type) ? response.type : "information";
    const notificationAction = this.generateNotificationAction(response);
    this.notification[typeNotification]({
      message: response.message,
      actionLabel: notificationAction.label,
      action: notificationAction.action
    });
  }
  generateDetailModal(responseMessage) {
    return () => {
      if (!this.httpInterceptorDetailComponent) {
        this.createModal(responseMessage);
      }
    };
  }
  generateNotificationAction(responseMessage) {
    let notificationAction;
    let notificationLabel;
    if (responseMessage.helpUrl && !(responseMessage.detailedMessage || responseMessage.details)) {
      notificationLabel = this.literals.help;
      notificationAction = this.generateUrlHelpFunction(responseMessage.helpUrl);
    } else if (responseMessage.detailedMessage || responseMessage.details) {
      notificationLabel = this.literals.details;
      notificationAction = this.generateDetailModal(responseMessage);
    }
    return {
      label: notificationLabel,
      action: notificationAction
    };
  }
  generateUrlHelpFunction(helpUrl) {
    return () => {
      window.open(helpUrl, "_blank");
    };
  }
};
var PoHttpInterceptorService = class _PoHttpInterceptorService extends PoHttpInterceptorBaseService {
  constructor(notification, componentInjector, languageService) {
    super(componentInjector, notification, languageService);
  }
  static ɵfac = function PoHttpInterceptorService_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoHttpInterceptorService)(ɵɵinject(PoNotificationService), ɵɵinject(PoComponentInjectorService), ɵɵinject(PoLanguageService));
  };
  static ɵprov = ɵɵdefineInjectable({
    token: _PoHttpInterceptorService,
    factory: _PoHttpInterceptorService.ɵfac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoHttpInterceptorService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: PoNotificationService
  }, {
    type: PoComponentInjectorService
  }, {
    type: PoLanguageService
  }], null);
})();
var PoHttpInterceptorModule = class _PoHttpInterceptorModule {
  static ɵfac = function PoHttpInterceptorModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoHttpInterceptorModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoHttpInterceptorModule,
    declarations: [PoHttpInterceptorDetailComponent],
    imports: [CommonModule, PoAccordionModule, PoModalModule, PoNotificationModule, PoTagModule]
  });
  static ɵinj = ɵɵdefineInjector({
    providers: [PoHttpInterceptorService, PoNotificationService, {
      provide: HTTP_INTERCEPTORS,
      useClass: PoHttpInterceptorService,
      multi: true
    }],
    imports: [CommonModule, PoAccordionModule, PoModalModule, PoNotificationModule, PoTagModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoHttpInterceptorModule, [{
    type: NgModule,
    args: [{
      imports: [CommonModule, PoAccordionModule, PoModalModule, PoNotificationModule, PoTagModule],
      declarations: [PoHttpInterceptorDetailComponent],
      providers: [PoHttpInterceptorService, PoNotificationService, {
        provide: HTTP_INTERCEPTORS,
        useClass: PoHttpInterceptorService,
        multi: true
      }]
    }]
  }], null, null);
})();
var PoHttpRequesControltService = class _PoHttpRequesControltService {
  controlHttpRequest = new Subject();
  getControlHttpRequest() {
    return this.controlHttpRequest.asObservable();
  }
  send(value = 0) {
    this.controlHttpRequest.next(value);
  }
  static ɵfac = function PoHttpRequesControltService_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoHttpRequesControltService)();
  };
  static ɵprov = ɵɵdefineInjectable({
    token: _PoHttpRequesControltService,
    factory: _PoHttpRequesControltService.ɵfac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoHttpRequesControltService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], null, null);
})();
var noCountPendingRequests = "X-PO-No-Count-Pending-Requests";
var screenLock = "X-PO-Screen-Lock";
var PoHttpRequestInterceptorService = class _PoHttpRequestInterceptorService {
  controlHttpRequest;
  poComponentInjector;
  loadingOverlayComponent = void 0;
  pendingRequests = 0;
  overlayRequests = 0;
  constructor(controlHttpRequest, poComponentInjector) {
    this.controlHttpRequest = controlHttpRequest;
    this.poComponentInjector = poComponentInjector;
  }
  intercept(request, next) {
    const requestClone = request.clone();
    request = this.requestCloneWithoutHeaderParam([noCountPendingRequests, screenLock], request);
    this.setCountPendingRequests(true, requestClone);
    this.setCountOverlayRequests(true, requestClone);
    return next.handle(request).pipe(finalize(() => {
      this.setCountPendingRequests(false, requestClone);
      this.setCountOverlayRequests(false, requestClone);
    }));
  }
  getCountPendingRequests() {
    return this.controlHttpRequest.getControlHttpRequest();
  }
  buildLoading() {
    if (!this.loadingOverlayComponent) {
      this.loadingOverlayComponent = this.poComponentInjector.createComponentInApplication(PoLoadingOverlayComponent);
      this.loadingOverlayComponent.instance.screenLock = true;
      this.loadingOverlayComponent.instance.changeDetector.detectChanges();
    }
  }
  destroyLoading() {
    if (this.loadingOverlayComponent) {
      this.poComponentInjector.destroyComponentInApplication(this.loadingOverlayComponent);
      this.loadingOverlayComponent = void 0;
    }
  }
  requestCloneWithoutHeaderParam(headersParams, request) {
    let isRequestClone = false;
    headersParams.forEach((headerParam) => {
      if (request.headers.has(headerParam)) {
        request = request.clone({
          headers: request.headers.delete(headerParam)
        });
        isRequestClone = true;
      }
    });
    return isRequestClone ? request.clone({
      headers: request.headers
    }) : request;
  }
  setCountPendingRequests(isIncrement, request) {
    const hasCountPendingRequestHeaderParam = request.headers.has(noCountPendingRequests);
    const headerParam = request.headers.get(noCountPendingRequests);
    if (hasCountPendingRequestHeaderParam && headerParam.toString().toLowerCase() === "true") {
      return;
    }
    this.pendingRequests += isIncrement ? 1 : -1;
    this.controlHttpRequest.send(this.pendingRequests);
  }
  setCountOverlayRequests(isIncrement, request) {
    const hasOverlayRequestHeaderParam = request.headers.has(screenLock);
    if (hasOverlayRequestHeaderParam) {
      const headerParam = request.headers.get(screenLock);
      if (headerParam.toString().toLowerCase() === "false") {
        return;
      }
      this.overlayRequests += isIncrement ? 1 : -1;
      this.overlayRequests > 0 ? this.buildLoading() : this.destroyLoading();
    }
  }
  static ɵfac = function PoHttpRequestInterceptorService_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoHttpRequestInterceptorService)(ɵɵinject(PoHttpRequesControltService), ɵɵinject(PoComponentInjectorService));
  };
  static ɵprov = ɵɵdefineInjectable({
    token: _PoHttpRequestInterceptorService,
    factory: _PoHttpRequestInterceptorService.ɵfac,
    providedIn: "root"
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoHttpRequestInterceptorService, [{
    type: Injectable,
    args: [{
      providedIn: "root"
    }]
  }], () => [{
    type: PoHttpRequesControltService
  }, {
    type: PoComponentInjectorService
  }], null);
})();
var PoHttpRequestModule = class _PoHttpRequestModule {
  static ɵfac = function PoHttpRequestModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoHttpRequestModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoHttpRequestModule,
    imports: [PoLoadingModule]
  });
  static ɵinj = ɵɵdefineInjector({
    providers: [PoHttpRequesControltService, PoHttpRequestInterceptorService, PoComponentInjectorService, {
      provide: HTTP_INTERCEPTORS,
      useClass: PoHttpRequestInterceptorService,
      multi: true
    }],
    imports: [PoLoadingModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoHttpRequestModule, [{
    type: NgModule,
    args: [{
      imports: [PoLoadingModule],
      providers: [PoHttpRequesControltService, PoHttpRequestInterceptorService, PoComponentInjectorService, {
        provide: HTTP_INTERCEPTORS,
        useClass: PoHttpRequestInterceptorService,
        multi: true
      }]
    }]
  }], null, null);
})();
var PoInterceptorsModule = class _PoInterceptorsModule {
  static ɵfac = function PoInterceptorsModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoInterceptorsModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoInterceptorsModule,
    imports: [PoHttpInterceptorModule, PoHttpRequestModule],
    exports: [PoHttpInterceptorModule, PoHttpRequestModule]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [PoHttpInterceptorModule, PoHttpRequestModule, PoHttpInterceptorModule, PoHttpRequestModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoInterceptorsModule, [{
    type: NgModule,
    args: [{
      declarations: [],
      imports: [PoHttpInterceptorModule, PoHttpRequestModule],
      exports: [PoHttpInterceptorModule, PoHttpRequestModule],
      providers: [],
      bootstrap: []
    }]
  }], null, null);
})();
var PoDecimalFormatPipe = class _PoDecimalFormatPipe {
  languageService = inject(PoLanguageService);
  transform(value, format2, locale) {
    if (value === null || value === void 0 || !format2) {
      return "";
    }
    const numValue = typeof value === "string" ? Number.parseFloat(value) : value;
    if (Number.isNaN(numValue)) {
      return "";
    }
    const parsed = parseDecimalFormat(format2);
    const {
      decimalSeparator,
      thousandSeparator
    } = this.languageService.getNumberSeparators(locale);
    const result = applyDecimalFormat(numValue, parsed, decimalSeparator, thousandSeparator);
    return result.viewValue;
  }
  static ɵfac = function PoDecimalFormatPipe_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoDecimalFormatPipe)();
  };
  static ɵpipe = ɵɵdefinePipe({
    name: "poDecimalFormat",
    type: _PoDecimalFormatPipe,
    pure: true
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoDecimalFormatPipe, [{
    type: Pipe,
    args: [{
      name: "poDecimalFormat",
      standalone: true
    }]
  }], null, null);
})();
var PoDecimalFormatModule = class _PoDecimalFormatModule {
  static ɵfac = function PoDecimalFormatModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoDecimalFormatModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoDecimalFormatModule,
    imports: [PoDecimalFormatPipe],
    exports: [PoDecimalFormatPipe]
  });
  static ɵinj = ɵɵdefineInjector({});
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoDecimalFormatModule, [{
    type: NgModule,
    args: [{
      imports: [PoDecimalFormatPipe],
      exports: [PoDecimalFormatPipe]
    }]
  }], null, null);
})();
var PoPipesModule = class _PoPipesModule {
  static ɵfac = function PoPipesModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoPipesModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoPipesModule,
    imports: [PoTimeModule, PoDecimalFormatModule],
    exports: [PoTimeModule, PoDecimalFormatModule]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [PoTimeModule, PoDecimalFormatModule, PoTimeModule, PoDecimalFormatModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoPipesModule, [{
    type: NgModule,
    args: [{
      declarations: [],
      imports: [PoTimeModule, PoDecimalFormatModule],
      exports: [PoTimeModule, PoDecimalFormatModule],
      providers: [],
      bootstrap: []
    }]
  }], null, null);
})();
var PoModule = class _PoModule {
  static ɵfac = function PoModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _PoModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _PoModule,
    imports: [PoComponentsModule, PoDirectivesModule, PoGuardsModule, PoInterceptorsModule, PoPipesModule, PoServicesModule],
    exports: [PoComponentsModule, PoDirectivesModule, PoInterceptorsModule, PoGuardsModule, PoPipesModule, PoServicesModule]
  });
  static ɵinj = ɵɵdefineInjector({
    providers: [PoNotificationService],
    imports: [PoComponentsModule, PoDirectivesModule, PoGuardsModule, PoInterceptorsModule, PoPipesModule, PoServicesModule, PoComponentsModule, PoDirectivesModule, PoInterceptorsModule, PoGuardsModule, PoPipesModule, PoServicesModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(PoModule, [{
    type: NgModule,
    args: [{
      declarations: [],
      imports: [PoComponentsModule, PoDirectivesModule, PoGuardsModule, PoInterceptorsModule, PoPipesModule, PoServicesModule],
      exports: [PoComponentsModule, PoDirectivesModule, PoInterceptorsModule, PoGuardsModule, PoPipesModule, PoServicesModule],
      providers: [PoNotificationService],
      bootstrap: []
    }]
  }], null, null);
})();
var ForceBooleanComponentEnum;
(function(ForceBooleanComponentEnum2) {
  ForceBooleanComponentEnum2["switch"] = "switch";
  ForceBooleanComponentEnum2["checkbox"] = "checkbox";
})(ForceBooleanComponentEnum || (ForceBooleanComponentEnum = {}));
var ForceOptionComponentEnum;
(function(ForceOptionComponentEnum2) {
  ForceOptionComponentEnum2["radioGroup"] = "radioGroup";
  ForceOptionComponentEnum2["select"] = "select";
})(ForceOptionComponentEnum || (ForceOptionComponentEnum = {}));
function validatePropertyOnLifeCycle(decoratorProperties, lifecycleName, callback) {
  const {
    target,
    property
  } = decoratorProperties;
  const emptyFunction = () => {
  };
  const lifecycleFunctionClone = target[lifecycleName] || emptyFunction;
  Object.defineProperty(target, lifecycleName, {
    value: function() {
      callback.call(this, property, target);
      lifecycleFunctionClone.call(this);
    }
  });
}
function changeValueByCallback(decoratorProperties, decoratorName, callback) {
  const {
    target,
    property,
    originalDescriptor
  } = decoratorProperties;
  const privatePropertyName = createPrivateProperty(target, property, decoratorName);
  return {
    get: getter(originalDescriptor, privatePropertyName),
    set: setter(originalDescriptor, callback, privatePropertyName)
  };
}
function setter(originalDescriptor, callback, privatePropertyName) {
  return function(value) {
    if (originalDescriptor && originalDescriptor.set) {
      originalDescriptor.set.bind(this)(callback(value));
    }
    this[privatePropertyName] = callback(value);
  };
}
function getter(originalDescriptor, privatePropName) {
  return function() {
    return originalDescriptor && originalDescriptor.get ? originalDescriptor.get.bind(this)() : this[privatePropName];
  };
}
function createPrivateProperty(target, propertyName, decoratorName) {
  const privatePropName = `$$__${propertyName}`;
  if (Object.prototype.hasOwnProperty.call(target, privatePropName)) {
    console.warn(`The prop "${privatePropName}" is already exist, it will be overrided by ${decoratorName} decorator.`);
  }
  Object.defineProperty(target, privatePropName, {
    configurable: true,
    writable: true
  });
  return privatePropName;
}
function InputBoolean() {
  return function(target, property, originalDescriptor) {
    const decoratorProperties = {
      target,
      property,
      originalDescriptor
    };
    return changeValueByCallback(decoratorProperties, "InputBoolean", convertToBoolean);
  };
}
function InputRequired() {
  return function(target, property) {
    const decoratorProperties = {
      target,
      property
    };
    validatePropertyOnLifeCycle(decoratorProperties, "ngOnInit", validateProperty);
  };
}
function validateProperty(property, target) {
  if (this[property] == null) {
    console.warn(`${target.constructor.name}: ${property} is required, but was not provided`);
  }
}

export {
  PoDividerSize,
  PoDividerComponent,
  PoDividerModule,
  AnimaliaIconDictionary,
  PoIconComponent,
  PoIconModule,
  PoTagOrientation,
  PoTagType,
  poLanguageDefault,
  poLocales,
  poLocaleDefault,
  poLocaleDecimalSeparatorList,
  poLocaleThousandSeparatorList,
  poLocaleDateSeparatorList,
  PoThemeA11yEnum,
  PoTableColumnSpacing,
  PoLanguageService,
  PO_CONTROL_POSITIONS,
  PoTooltipDirective,
  PoTagComponent,
  PoTooltipModule,
  PoTagModule,
  PoLanguageModule,
  PoAccordionItemComponent,
  PoAccordionComponent,
  PoAccordionModule,
  PoAvatarComponent,
  PoAvatarModule,
  PoBadgeComponent,
  PoBadgeModule,
  PoLinkComponent,
  PoOverlayComponent,
  LOADING_ICON_COMPONENT,
  PoLoadingIconComponent,
  PoLoadingComponent,
  PoLoadingOverlayComponent,
  PoCheckboxSize,
  PoButtonKind,
  PoButtonSize,
  PoButtonType,
  PoButtonComponent,
  PoOverlayModule,
  PoLoadingModule,
  PoButtonModule,
  PoPopoverComponent,
  PoHelperComponent,
  PoLinkModule,
  PoPopoverModule,
  PoHelperModule,
  PoLabelComponent,
  PoDragBaseDirective,
  PoDropListBaseDirective,
  PoDropListDirective,
  PoDragDirective,
  PoDragDropModule,
  PoDirectivesModule,
  PoFieldContainerBottomComponent,
  PoFieldContainerComponent,
  PoCheckboxComponent,
  PoItemListComponent,
  PoSearchListComponent,
  PoListBoxComponent,
  PoPopupComponent,
  poBreadcrumbLiterals,
  PoBreadcrumbComponent,
  PoLabelModule,
  PoFieldContainerModule,
  PoCheckboxModule,
  PoListBoxModule,
  PoPopupModule,
  PoBreadcrumbModule,
  PoButtonGroupToggle,
  PoButtonGroupComponent,
  PoButtonGroupModule,
  PoTimerFormat,
  PoTimerBaseComponent,
  PoTimerComponent,
  PoTimerModule,
  PoCalendarMode,
  PoDateService,
  PoDateTimeModule,
  PO_CALENDAR_DEFAULT_RANGE_PRESETS,
  PoComboFilterMode,
  PoComboOptionTemplateDirective,
  PoCleanComponent,
  PoComboComponent,
  PoCalendarComponent,
  PoCleanModule,
  PoComboModule,
  PoCalendarModule,
  PoChartLabelFormat,
  poChartLiteralsDefault,
  PoChartType,
  PoChartComponent,
  PoChartModule,
  PoContainerComponent,
  PoContainerModule,
  PoContextMenuComponent,
  PoContextMenuModule,
  PoDisclaimerComponent,
  PoDisclaimerModule,
  PoDisclaimerGroupComponent,
  PoDisclaimerGroupModule,
  PoDropdownComponent,
  PoDropdownModule,
  PoCheckboxGroupComponent,
  PoCheckboxGroupModule,
  PoRadioComponent,
  PoRadioGroupComponent,
  PoRadioModule,
  PoRadioGroupModule,
  PoMask,
  PoDatepickerIsoFormat,
  PoDatepickerComponent,
  PoDatepickerModule,
  PoColorPaletteService,
  PoColorPaletteModule,
  PoComponentInjectorService,
  PoComponentInjectorModule,
  PoControlPositionModule,
  PoModalFooterComponent,
  PoActiveOverlayService,
  PoModalComponent,
  PoModalModule,
  PoDialogType,
  poDialogAlertLiteralsDefault,
  poDialogConfirmLiteralsDefault,
  PoDialogComponent,
  PoDialogService,
  PoDialogModule,
  PoI18nPipe,
  PoMediaQueryService,
  PoMediaQueryModule,
  PoToasterMode,
  PoToasterOrientation,
  PoToasterType,
  poToasterLiterals,
  PoToasterComponent,
  PoToasterModule,
  PoNotificationModule,
  PoThemeTypeEnum,
  poThemeDefaultAA,
  poThemeDefaultAAA,
  poThemeDefaultActions,
  poThemeDefaultNeutrals,
  poThemeDefaultFeedback,
  poThemeDefaultBrands,
  poThemeDefaultCategoricals,
  poThemeDefaultOverlayCategoricals,
  poThemeDefaultLightValues,
  poThemeDefaultActionsDark,
  poThemeDefaultNeutralsDark,
  poThemeDefaultFeedbackDark,
  poThemeDefaultBrandsDark,
  poThemeDefaultCategoricalsDark,
  poThemeDefaultCategoricalsOverlayDark,
  poThemeDefaultDarkValues,
  poThemeDefaultCategoricalsDarkAA,
  poThemeDefaultCategoricalsOverlayDarkAA,
  poThemeDefaultDarkValuesAA,
  poThemeDefaultCategoricalsAA,
  poThemeDefaultOverlayCategoricalsAA,
  poThemeDefaultLightValuesAA,
  poThemeDefaultLight,
  poThemeDefaultDark,
  poThemeDefault,
  PoThemeService,
  PoThemeModule,
  PoUserGuideModule,
  PoServicesModule,
  PoActiveOverlayModule,
  I18N_CONFIG,
  PoI18nService,
  returnPoI18nService,
  mergePoI18nConfigs,
  PoI18nModule,
  initializeLanguageDefault,
  PoNotificationService,
  poUserGuideLiteralsDefault,
  PoUserGuideBaseService,
  PoUserGuideAlignment,
  PoUserGuidePosition,
  PoUserGuideService,
  PoDatetimepickerComponent,
  PoDatetimepickerModule,
  PoTimepickerModelFormat,
  PoTimepickerComponent,
  PoTimepickerModule,
  PoProgressStatus,
  PoProgressShape,
  PoProgressSize,
  PoProgressComponent,
  PoProgressModule,
  PoTimePipe,
  PoTimeModule,
  PoPageSlideFooterComponent,
  poPageSlideLiteralsDefault,
  PoPageSlideComponent,
  PoPageSlideModule,
  PoSwitchLabelPosition,
  PoSwitchComponent,
  PoSearchAiResponseType,
  PoSearchAiService,
  PoSearchAiComponent,
  poSearchAiCompileFilter,
  poSearchAiFilterItems,
  PoTableRowTemplateArrowDirection,
  PoSearchFilterMode,
  PoTableColumnSortType,
  PoTableCellTemplateDirective,
  PoTableColumnTemplateDirective,
  PoTableRowTemplateDirective,
  PoSearchComponent,
  PoTableColumnFrozenDirective,
  PoTableComponent,
  PoSwitchModule,
  PoSearchAiModule,
  PoDatepickerRangeComponent,
  applyDecimalFormat,
  getFormatLimits,
  parseDecimalFormat,
  validateAgainstFormat,
  insertGroupSeparatorsFromRight,
  PoDecimalComponent,
  PoEmailComponent,
  PoInputComponent,
  PoLoginComponent,
  PoLookupModalComponent,
  PoLookupComponent,
  PoMultiselectOptionTemplateDirective,
  PoMultiselectFilterMode,
  PoMultiselectComponent,
  PoNumberComponent,
  PoPasswordComponent,
  PoRichTextToolbarActions,
  PoUploadStatus,
  PoUploadFile,
  PoUploadComponent,
  PoUrlComponent,
  PoRichTextComponent,
  PoSelectComponent,
  PoTextareaComponent,
  PoSearchModule,
  PoTableModule,
  PoFieldModule,
  PoInfoOrientation,
  PoInfoComponent,
  PoInfoModule,
  PoDynamicFieldType,
  PoDynamicSharedBase,
  PoDynamicContainerComponent,
  PoDynamicFormComponent,
  PoImageComponent,
  PoDynamicViewComponent,
  PoImageModule,
  PoDynamicModule,
  PoFilterChipBaseComponent,
  PoFilterChipComponent,
  PoFilterChipModule,
  PoGaugeComponent,
  PoGaugeModule,
  PoGridComponent,
  PoGridModule,
  PoListViewContentTemplateDirective,
  PoListViewDetailTemplateDirective,
  PoListViewComponent,
  PoListViewModule,
  PoLogoComponent,
  PoLogoModule,
  PoMenuPanelComponent,
  PoMenuPanelModule,
  PoMenuHeaderTemplateDirective,
  PoMenuGlobalService,
  PoMenuComponent,
  PoMenuModule,
  PoNavbarComponent,
  PoNavbarModule,
  PoPageHeaderType,
  PoPageActionsLayout,
  PoPageDefaultComponent,
  PoPageDetailComponent,
  PoPageEditComponent,
  PoPageListComponent,
  PoPageModule,
  PoSkeletonAnimation,
  PoSkeletonSize,
  PoSkeletonType,
  PoSkeletonVariant,
  PoSkeletonComponent,
  PoSkeletonModule,
  PoSlideContentTemplateDirective,
  PoSlideComponent,
  PoSlideModule,
  PoStepperStatus,
  PoStepComponent,
  PoStepperOrientation,
  PoStepperComponent,
  PoStepperModule,
  PoTabsService,
  PoTabComponent,
  PoTabButtonComponent,
  PoTabDropdownComponent,
  poTabsLiterals,
  PoTabsComponent,
  PoTabsModule,
  PoContextTabButtonComponent,
  PoContextTabsComponent,
  PoContextTabsModule,
  PoToolbarComponent,
  PoToolbarModule,
  PoTreeViewComponent,
  PoTreeViewModule,
  PoWidgetComponent,
  PoWidgetModule,
  PoHeaderComponent,
  PoHeaderModule,
  PoComponentsModule,
  PoGuardsModule,
  PoHttpInterceptorService,
  PoHttpInterceptorModule,
  PoHttpRequestInterceptorService,
  PoHttpRequestModule,
  PoInterceptorsModule,
  PoDecimalFormatPipe,
  PoDecimalFormatModule,
  PoPipesModule,
  PoModule,
  ForceBooleanComponentEnum,
  ForceOptionComponentEnum,
  InputBoolean,
  InputRequired
};
//# sourceMappingURL=chunk-7PTJI5MC.js.map
